---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: ai-workshop-build-a-neural-network-with-pytorch-lightning-2023
url: "https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023"
duration_minutes: 90
duration: 1h 30m
level: Intermediate
updated: 12/8/2023
learners: 4073
skills:
  - PyTorch
  - Artificial Intelligence (AI)
  - Artificial Neural Networks
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D4E0DAQEO3pew_MlbVA/learning-public-crop_675_1200/B4EZjoiaKRHoAY-/0/1756247991159?e=2147483647&amp;v=beta&amp;t=AIiUEPc8EOC4sT4UnM38a1ccb8MFyTRBIl0P7wMot0I"
linkedin_topic: Artificial Intelligence (AI)
learning_paths:
  - '[[Advance Your Skills in Deep Learning and Neural Networks]]'
prev_courses:
  - '[[Deep Learning with Python- Convolutional Neural Networks]]'
next_courses:
  - '[[Deep Learning with Python- Optimizing Deep Learning Models]]'
path_nav: '[{"path":"Advance Your Skills in Deep Learning and Neural Networks","position":4,"total":8,"prev":"Deep Learning with Python- Convolutional Neural Networks","next":"Deep Learning with Python- Optimizing Deep Learning Models"}]'
path_count: 1
tags:
  - course
  - topic/artificial-intelligence
  - skill/pytorch
  - skill/artificial-intelligence-ai
  - skill/artificial-neural-networks
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Artificial%20Intelligence%20(AI)/AI%20Workshop-%20Build%20a%20Neural%20Network%20with%20PyTorch%20Lightning%20(2023).md)

![AI Workshop: Build a Neural Network with PyTorch Lightning (2023)](https://media.licdn.com/dms/image/v2/D4E0DAQEO3pew_MlbVA/learning-public-crop_675_1200/B4EZjoiaKRHoAY-/0/1756247991159?e=2147483647&amp;v=beta&amp;t=AIiUEPc8EOC4sT4UnM38a1ccb8MFyTRBIl0P7wMot0I)

# AI Workshop: Build a Neural Network with PyTorch Lightning (2023)

> If you’re looking for hands-on AI practice, this workshop-style coding course was designed for you. Join instructor Janani Ravi as she shows you how to build a neural network with PyTorch Lightning, the open-source library from Python that provides an interface for the popular deep learning framework PyTorch. Explore the core components of building a neural network with PyTorch, including setting 

> [LinkedIn Learning](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023) | 1h 30m | Intermediate | 4K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [AI workshop: Build a neural network with PyTorch Lightning](#ai-workshop-build-a-neural-network-with-pytorch-lightning)
  - [Prerequisites](#prerequisites)
  - [Quick overview of neural networks](#quick-overview-of-neural-networks)
- [**1. Building a Neural Network with PyTorch**](#1-building-a-neural-network-with-pytorch) (8 videos)
  - [Setting up the virtual environment](#setting-up-the-virtual-environment)
  - [Loading and exploring regression data](#loading-and-exploring-regression-data)
  - [Preprocessing data for training](#preprocessing-data-for-training)
  - [Creating a simple neural network](#creating-a-simple-neural-network)
  - [Setting up the dataset and DataLoader](#setting-up-the-dataset-and-dataloader)
  - [Training a neural network using PyTorch](#training-a-neural-network-using-pytorch)
  - [Visualizing losses and evaluating models](#visualizing-losses-and-evaluating-models)
  - [Building and training a more complex neural network](#building-and-training-a-more-complex-neural-network)
- [**2. Using PyTorch Lightning to Build a Regression Model**](#2-using-pytorch-lightning-to-build-a-regression-model) (3 videos)
  - [Encapsulating data using a LightningDataModule](#encapsulating-data-using-a-lightningdatamodule)
  - [Encapsulating a model using a LightningModule](#encapsulating-a-model-using-a-lightningmodule)
  - [Training the model using the PyTorch Lightning Trainer](#training-the-model-using-the-pytorch-lightning-trainer)
- [**3. Using PyTorch Lightning to Build a Classification Model**](#3-using-pytorch-lightning-to-build-a-classification-model) (4 videos)
  - [Loading and exploring classification data](#loading-and-exploring-classification-data)
  - [Creating a LightningDataModule](#creating-a-lightningdatamodule)
  - [Creating a LightningModule](#creating-a-lightningmodule)
  - [Training a classification model and evaluating metrics](#training-a-classification-model-and-evaluating-metrics)
- [**Conclusion**](#conclusion) (1 videos)
  - [Summary and next steps](#summary-and-next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [AI workshop: Build a neural network with PyTorch Lightning](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/ai-workshop-build-a-neural-network-with-pytorch-lightning?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/ai-workshop-build-a-neural-network-with-pytorch-lightning?u=76281980&t=0)** Hi, and welcome to this course. [[AI Workshop- Build a Neural Network with PyTorch Lightning]]. Since this course is an AI workshop, for most of this course we'll be performing hands-on coding. We'll build a neural network with [[PyTorch]] and we'll see how we can write cleaner, more modular reusable code with PyTorch Lightning. Now, before we get to the demos, just a little bit of an overview of PyTorch and PyTorch lightning. First, what exactly is PyTorch? Here is a definition from the PyTorch documentation. It's an optimized tensor library for [[Deep Learning]] using GPUs and CPUs. The tensors here refer to multi-dimensional arrays that can be trained in a distributed manner. Another way to look at PyTorch, also from the documentation. It's an open-source machine learning framework based on the [[Python (Programming Language)|Python]] programming language. It has simple and intuitive APIs which accelerate the path from research [[Prototyping]] to production deployment. The PyTorch framework is primarily used to build deep learning neural network models, and its APIs are so simple that with just basic knowledge of Python, you should be able to work in PyTorch right away. Now, hopefully, you've worked with [[Neural Networks]] before, and this is not your first neural network course. Neural network models, you know, are just directed acyclic graphs. PyTorch uses something known as dynamic computation graphs. This means you can build the graph for the model and execute it right away. This makes it easier and more flexible to build complex
>
> **[1:35](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/ai-workshop-build-a-neural-network-with-pytorch-lightning?u=76281980&t=95)** neural networks. Now, PyTorch is deeply integrated with NumPy. You can set up your data in the form of NumPy arrays and convert those to PyTorch tensors and vice-versa very easily. PyTorch has native support for training on GPUs. You can have your model parameters and data all moved to the GPU available on the machine that you're running training and the entire training process will run there. PyTorch also uses a powerful library called Autograd for automatic differentiation. Automatic differentiation is an important part of training a neural network model. This involves computing partial derivatives of the loss function with respect to every model parameters, and then using that information to tweak model parameters to minimize the loss of a network. The PyTorch library contains everything that you need to build neural networks. You have classes for the layers of a neural network, different kinds of layers, optimizers that you use to train neural networks, loss functions for different kinds of models, serializers to serialize the model out to disk. The PyTorch framework is flexible and easy to use. When you use PyTorch directly to build and train neural networks, you get access to a low-level API for model training. And this is great when you really want to configure and customize your model and want very granular control over the training process. But if you want to be abstracted away from the details of model training,
>
> **[3:08](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/ai-workshop-build-a-neural-network-with-pytorch-lightning?u=76281980&t=188)** using the PyTorch framework directly is not a great choice. PyTorch has many repetitive tasks and a lot of boilerplate code, so it's often very tedious to write code in PyTorch to train your model. If you want to avoid working with all of these nitty-gritty details and want your model code to be cleaner and more modular, well, you use PyTorch Lightning. PyTorch Lightning is an open-source lightweight wrapper or framework built on top of PyTorch that simplifies the training and research process for deep learning models. So this is something important. PyTorch Lightning is just a wrapper, so you can't do anything in Lightning that you can't do with PyTorch. The fact is, it's just much easier to work with PyTorch Lightning. PyTorch Lightning abstracts away all of the nitty-gritty details and really reduces the boilerplate code that you have to write for training models. Whatever you need to do to build and train neural networks, PyTorch Lightning will give you a high-level interface for this. You can define data sets, define models, set up training loops, and log your experiments all using this high-level interface. What are some of the advantages of using PyTorch Lightning? Well, your code is much cleaner because most of the repetitive code and training loops that you have to use in PyTorch is abstracted away. Your code is also more modular with Lightning. It encourages a modular design by separating the different parts of the training process into well-defined components such as the model DataLoaders and training logic. Cleaner
>
> **[4:43](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/ai-workshop-build-a-neural-network-with-pytorch-lightning?u=76281980&t=283)** modular code results in better reproducibility of your model and data. Lightning provides built-in support for experiment logging and tracking, as well. Lightning support for distributed training is more straightforward because you do not need to move your model parameters and data to specific devices to actually train on that device. Lightning offers a callback system, allowing you to add custom functionality at various points during the training process without modifying the core training loop. Instead of writing complex for loops to train your model, the trainer class in Lightning abstracts away many training loop details. If you feel that PyTorch Lightning does not offer you the flexibility that you need for model training, well, you can use Lightning along with the PyTorch API. So they are interoperable, giving you experiment flexibility.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (33), [[Neural Networks]] (6), [[Deep Learning]] (2), [[Python (Programming Language)|Python]] (2), [[Prototyping]] (1)
> **Definitions:** is an  (3), is a  (1), known as (1)
> **Env Vars:** api (2), gpu (1)
> **Prerequisites:** set up (2), configure (1)
> **CLI Commands:** python (2)
> **Exercise Files:** boilerplate (2)
> **Documentation:** the documentation (1)
> **Analogies:** such as (1)

#### [Prerequisites](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/prerequisites?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/prerequisites?u=76281980&t=0)** Before you get hands-on and that will be very soon, let's quickly look at some of the prereqs you need to have to make the most of your learning. The first thing here is you need to have a basic understanding of machine learning. This is not a beginner machine learning course. Instead, it's an AI workshop, which means we'll get hands-on with demos right away. A basic understanding of machine learning, regression, and classification models will really help you. Also, you need to have a basic understanding of how [[Neural Networks]] work. In the next movie, we'll quickly go through how neural networks function, but that's more of revision rather than explaining all of the nitty gritty of neural network training. So basic understanding of neural networks would really help. And finally, because we are going to be coding a lot using [[Python (Programming Language)|Python]], you should be comfortable programming in Python.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Neural Networks]] (2), [[Python (Programming Language)|Python]] (2)
> **CLI Commands:** python (2), make (1)
> **Prerequisites:** you need to have (3)
> **Cross-References:** in the next (1)

#### [Quick overview of neural networks](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/quick-overview-of-neural-networks?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/quick-overview-of-neural-networks?u=76281980&t=0)** Let's do a quick revision of how [[Neural Networks]] work before we get down to the demos. Remember, this is not a comprehensive overview or a look at neural network training, but a quick overview to give you the main points to remember as you dive into the code. Neural network models are made up of layers and how these layers are arranged and connected make up the architecture of the model. You can think of every layer in a neural network as being connected to other layers in the neural network. The way neural networks function is that every layer in the neural network is responsible for extracting a different detail from the underlying data, and all of the layers put together make predictions. Now, the first layer here in our neural network, that's the input layer. This is where we feed in the input data, whether during the training process or for predictions. The final predictions of the neural network are available from the last layer, that is the output layer. Between the input and output layers, you have one or more hidden layers and these hidden layers transform the data. These transformations are applied as the data flow through the layers of the model. The operation of each hidden layer is to extract a different bit of information from the data that passes through. In a neural network, every layer is made up of active learning units called neurons. They are called active learning units because it's these neurons that are identifying patterns and making generalizations from the
>
> **[1:39](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/quick-overview-of-neural-networks?u=76281980&t=99)** data that passes through the network. Neurons are fed inputs and they produce outputs. And these inputs and outputs are essentially interconnections in the model. The output of every neuron may be connected to one or more neurons in the layer after it. And how these connections are set up, well, that's a part of the neural network architecture. Now, we've said that neurons are active learning units, but what exactly is a neuron? Each neuron is nothing but a mathematical function. Each neuron applies this function that you see here at the bottom to its inputs. It computes the weighted X values, X values are the input, adds a bias, and applies an activation function on Wx + b to compute the final output Y. The first of these mathematical functions that the neural network applies is responsible for learning linear relationships that exist in the data. A neuron receives a vector of inputs. You can think of these as X1 through Xn, and it basically applies a weight value to each element of the vector. These weights are associated with the connections that flow into the neuron. Wx + b is the first mathematical operation of the neuron, and this operation is responsible for learning linear relationships that exist in data. The second mathematical function that a neuron applies to its inputs is the activation function. The activation function is responsible for learning non-linear relationships that exist in data.
>
> **[3:13](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/quick-overview-of-neural-networks?u=76281980&t=193)** Popular activation functions include the rectified linear unit or ReLU, there is, the sigmoid activation function, and many others. The choice of activation function is a part of the neural network design. The weights and biases of all of the neurons in your neural network make up the trainable parameters of the model. These weights and biases are what are found during the training process. You can think of the objective of the training process of a neural network is to find the weights and biases for all of the interconnections that minimizes the loss of the model. The loss here is essentially a measure of how far the predictions of the model are from the actual values in the training data. We know that model parameters are found during the training process of the neural network. But how does training work? Here is a very high level explanation. During the training process, we feed training data in batches through the network and get predictions using the current parameters of the model. These predictions, at least to start off with, will not be very good ones. We'll measure how good the predictions of the model are by computing the loss. The loss represents how far is the prediction of the model from the actual labels in the training data. Once we have the loss function, we'll compute gradients, where gradients are just the partial derivatives of the loss with respect to each parameter
>
> **[4:46](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/quick-overview-of-neural-networks?u=76281980&t=286)** in model training. These gradients give us a sense of how to tweak the model parameters to minimize the loss of the model. We then make a backward pass through the model to update parameters to minimize the loss. And this forward pass to get predictions and then backward pass to update the model parameters continues through the entire training process until the model parameters converge. The entire objective of the training process is to minimize the loss of the network, and thus improve the predictions of the model. This minimization of the loss of the network is done using an optimization algorithm called gradient descent.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Neural Networks]] (2)
> **CLI Commands:** make (4), find (1)
> **Definitions:** is a  (3)
> **Prerequisites:** set up (1)


### 1. Building a Neural Network with PyTorch

[↑ Back to Table of Contents](#table-of-contents)

#### [Setting up the virtual environment](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/setting-up-the-virtual-environment?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/setting-up-the-virtual-environment?u=76281980&t=0)** In this course, we'll first build a simple neural network model for regression using [[PyTorch]]. And you'll see that there are a lot of granular details that you need to know about model building in order to work with PyTorch directly. There will be a lot of boilerplate code. Then we'll basically build the same neural network using PyTorch Lightning. With PyTorch Lightning, we'll eliminate a lot of the boilerplate code and create reusable components. You'll see how much cleaner the code is with PyTorch Lightning. Before we do any of that, let's set up a virtual environment within which we'll install PyTorch and build and train our neural network models. Here I am on my local machine and I have [[Python (Programming Language)|Python]] installed. Make sure you have a recent version of Python. You can see that I'm working with Python 3.10.9. I'll now create a Python virtual environment. A virtual environment is just an isolated environment for Python projects, ensuring that each project can have its own dependencies regardless of what dependencies other projects may have. This command creates a virtual environment called pytorch_venv using the venv module. When you create a virtual environment, this sets up a directory under your current working directory with the name of your virtual environment. You can see the pytorch_venv directory here. This is where all of the packages that we install in the virtual environment will be set up and stored. Now, to activate the virtual environment, you'll run the source command pytorch_venv/bin/activate. This will activate the virtual environment.
>
> **[1:35](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/setting-up-the-virtual-environment?u=76281980&t=95)** Notice that my prompt has changed. The name of the virtual environment is now part of the prompt. The same version of Python that I had installed on my local machine should now be available here in my virtual environment. It's possible to create virtual environments using different versions of Python, but I'm happy with the Python version that I have. In order to be able to work within this virtual environment on a Jupyter Notebook, let's install the ipykernel module in Python. Use pip install to install the latest version of ipykernel. This is the Python package that provides the kernel for Jupyter Notebook and Jupyter Lab. The kernel is just the computational engine that executes the code on the notebook. Now, once we have this installed, run jupyter kernelspec list to see what Python kernels you have available. You can see I have just the one Python 3. I'll now install the kernel associated with my virtual environment using this command here, python -m ipykernel install the kernel with name pytorch_venv. Remember, pytorch_venv is a folder in our current working directory, and it is that folder that will be used to set up the kernel. Thus, we have a kernel with our virtual environment available when we use Jupyter Notebooks. Run jupyter kernelspec list once again, and you can see the pytorch_venv kernel. That's the kernel that we'll use to run our code. We are within our virtual environment. We've installed the pytorch_venv kernel. Let's bring up the Jupyter Notebook server so that we can
>
> **[3:10](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/setting-up-the-virtual-environment?u=76281980&t=190)** work within Jupyter Notebook to build and train models using PyTorch and PyTorch Lightning. Here, let's open up a new notebook, click on "New" and make sure that you choose pytorch_venv as the kernel that you want to run your code on. If you choose this kernel, we'll be running within our virtual environment. On the top right, notice the kernel. It's pytorch_venv. If you happen to be in some other kernel and you need to switch, simply select the kernel dropdown menu here on this page, and there you'll find an option to switch your kernel. Meanwhile, let's rename this notebook and give it a meaningful name. Let's call this TrainingRegressionModelUsingPyTorch because first, we'll work with PyTorch, which is much more low-level and involves much more boilerplate in order to understand the different components that you'll use to build a neural network model and then we'll switch to PyTorch Lightning. In order to have more screen space for code, I'm going to toggle the header and toggle the toolbar here in this notebook. So we are only left with the code cells. We don't have all this extra stuff here on top.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (13), [[PyTorch]] (10)
> **CLI Commands:** python (13), make (2), pip (1), find (1)
> **Prerequisites:** install (7), set up (3), make sure you have (1)
> **Code Identifiers:** pytorch_venv (9)
> **Tools:** jupyter (8)
> **UI Navigation:** toggle (2), click on (1), select the (1), dropdown (1), switch to (1)
> **Versions:** python 3 (2), 10.9 (1)
> **Exercise Files:** boilerplate (3)

#### [Loading and exploring regression data](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/loading-and-exploring-regression-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/loading-and-exploring-regression-data?u=76281980&t=0)** The first thing I'm going to do here is put in some code to ignore certain warnings in this notebook. Now, generally, it's not a good practice to ignore warnings in your [[Python (Programming Language)|Python]] code, but at the time of this recording, the Seaborn Library throw some internal warnings which we can't really eliminate. There is an issue being tracked for Seaborn right now. The next release should fix it, but since I did not want the demo screen to be cluttered with warnings, I added these ignore statements at the very top of my code. This is the issue that I refer to. By the time you are watching this course, it's likely that it's fixed and you can just get rid of these warnings. We'll be using the scikit-learn module in Python in order to preprocess our data. So go ahead and pip install scikit-learn. We'll use functions such as train, test, split, etc. to get training and test data to train our model. So scikit-learn is a package that you'll need. And I install this in the virtual environment. Next, of course, we are going to be using [[PyTorch]] to train our model. So you'll need to pip install the PyTorch framework, as well. You can see the PyTorch framework version that I'm working on is 2.0.1. Another PyTorch-related module that you need is Torch metrics. This is the library that contains metrics to evaluate our model's mean square error for regression, no accuracy, precision, and recall for classification. So make sure you pip install and have this module ready within your virtual environment. Now within this virtual environment,
>
> **[1:33](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/loading-and-exploring-regression-data?u=76281980&t=93)** I need to set up other commonly used libraries for [[Data Processing]] and analysis such as [[Pandas (Software)|pandas]]. So make sure you pip install pandas and have that available. Another library that we need is the Seaborn Library. This is what we'll use for visualization. Seaborn depends on matplotlib, so the matplotlib library should also be installed. Now that we have the libraries that we need, let's confirm the PyTorch version that we are using. I'm running PyTorch version 2.0.1, so make sure you have a recent version of PyTorch greater than or equal to two in order to be able to run these demos. I'll now set up a bunch of import statements for all of the libraries and classes that we need for this demo. I'll use NumPy, pandas, Seaborn, matplotlib, Torch, scikit-learn. We won't go through these import statements right now, we'll discuss each class or function as we use it. The dataset that we'll be using to train our regression model is an insurance charges dataset and is present here within this datasets directory. Under my current working directory, you can see this insurance.csv file. This is the file that I'm going to read into my notebook using pandas. The dataset is fairly simple. The records contain details of insurance, customers, age, gender, bmi, number of children, whether they smoke or not, and the region in which they live. These are all the features of the data. We'll try to use this information to predict how much they've been charged for insurance.
>
> **[3:07](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/loading-and-exploring-regression-data?u=76281980&t=187)** The label column is charges. Like I said, this is a fairly simple and small dataset, perfect for training a simple regression model using PyTorch. The shape of the data shows us that there are a total of 1338 records. Let's make sure every column in this data is of the right type. For that, I run the info method on a pandas data frame and you can see that the types are correct. Age, bmi, number of children, and charges are numeric columns, the remaining are string columns or categorical columns. Let's just use a few visualization techniques to understand the data that we are working with. Let's take a look at a histogram of the charges information. You can see that for a vast majority of customers, the insurance charges tend to be under $15,000. You can see that on the x-axis. However, there are a few customers for whom insurance charges tend to be very high in the order of $40,000 to $50,000. In this dataset, all of the feature variables are relevant to predict the insurance charges for customers, but one of the most significant features is this smoker feature. Whether you're a smoker or not, heavily influences your insurance charges. You can see that for non-smokers, the insurance charges tend to be much lower than for smokers. This boxplot makes that very clear. Also, how old the customer is influences insurance charges. You can see a scatterplot of insurance charges versus age, and there is a linear relationship.
>
> **[4:42](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/loading-and-exploring-regression-data?u=76281980&t=282)** You can see that insurance charges tend to increase with age. But for each age, they seem to be different bands of charges. Now that we've understood the data that we're working with, let's quickly split the data into training and test data using train_test_split. X features include all columns except charges, the Y values that we're trying to predict are the insurance charges. train_test_split will split the features and labels so that we have 80% of the data to train our neural network model and 20% of the data to validate the model. Thousand and seventy records for training and 268 records for validation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (8), [[Pandas (Software)|Pandas]] (5), [[Python (Programming Language)|Python]] (2), [[Data Processing]] (1)
> **CLI Commands:** pip (4), make (4), python (2)
> **Prerequisites:** install (5), you'll need (2), set up (2), make sure you have (1)
> **Definitions:** is a  (3), is an  (1)
> **Versions:** 2.0.1 (1), version 2 (1), 0.1 (1)
> **Code Identifiers:** train_test_split (2)
> **Analogies:** such as (2)
> **File Paths:** insurance.csv (1)

#### [Preprocessing data for training](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/preprocessing-data-for-training?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/preprocessing-data-for-training?u=76281980&t=0)** Now that we have training and validation data, the next step is to preprocess the data so that we can feed that into a machine learning model. Now, neural network models and all machine learning models only understand numeric values. You can't feed in strings to those models, which means you need to numerically encode all of your categorical variables, which may be represented as strings. Now, there are different ways to numerically encode this data. We'll use one-hot encoding. There are three categorical columns in our data; sex, smoker, and region. All of these are nominal categorical values. That means that there is no inherent ordering across categories for any of these columns. And so one-hot encoding is perfect in such a situation. We instantiate the one-hot encoder categorical transformer, handle unknown is ignored. That is, if we encounter unknown values, this transformer will simply ignore them. Drop is equal to first specifies the methodology to use to drop one of the categories in the feature. Let's say the smoker column can have two possible values, yes or no. In the final output, you'll have just one column, either smoker yes or smoker no with zero/one values indicating whether the customer is a smoker or not. Sparse output set to false means that the resulting one-hot encoded values will not be in the sparse representation. It will be a complete representation. Let's instantiate a column transformer next to perform
>
> **[1:35](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/preprocessing-data-for-training?u=76281980&t=95)** the preprocessing, and this column transformer uses the one-hot encoder for categorical values. For the remaining numeric columns, we'll just pass through, meaning the numeric columns won't be affected, only the categorical columns will be affected. Once we've instantiated the transformer, let's pre-process our data. I call preprocessor.fit_transform on the training data, and using the computed values on the training data, we simply call transform on the validation data. This ensures that the properties computed on the training data are what we use to transform the validation data, and we are left with eight features in our data after one-hot encoding. Let's take a look at the training data and you'll find that it's just a NumPy array. We don't have the corresponding columns. If you want to understand this data, let's convert it to a temporary data frame and see how the data has been transformed. So we have one column for gender, male, yes/no; one column for smoker, yes/no; and then columns for northwest, southeast, and southwest. Notice that the categorical feature southeast, that column has been dropped. A value of all zeros for northwest, southeast, and southwest essentially indicates that the region is southeast. The numeric columns; age, bmi, and children, they have been passed through unaffected. The Y values are still in the data frame format. Let's convert those to the NumPy format, as well. And from NumPy arrays,
>
> **[3:07](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/preprocessing-data-for-training?u=76281980&t=187)** later on, we'll convert these to Torch tensors. We haven't yet completed the preprocessing of our data. We've converted all of the columns to numeric values. And I'm now going to standard scale these numeric values. Standard scaling involves converting all numeric values to z scores, that is, expressing every feature in terms of number of standard deviations from the mean. When you feed in numeric values to [[Neural Networks]], neural networks perform much better with smaller numeric values, and also when the individual features do not have wildly different ranges. And standard scaling is one way to preserve the information in individual features, but also have them all centered around zero and expressed using small numeric values. So I call fit_transform on the training data, and the mean and standard deviation computed on the training data will be used to transform the validation data. Notice I call just transform on X_val. And now if you look at the training data, you can see that the numbers are very small, usually between -3 and +3. Standard scaling or standardization computes the mean for each feature, subtracts the mean from every value, and then divides by the standard deviation so that the values are expressed in terms of z scores or number of standard deviations away from the mean. Now, the Y values that we need to train our model are currently in the form of a single-dimensional array or a vector.
>
> **[4:41](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/preprocessing-data-for-training?u=76281980&t=281)** In order to feed them into a neural network, they need to be in the form of a multi-dimensional array. So instead of a vector of length size, we'll have a multi-dimensional array of dimensions size comma one. And that's what this reshape operation accomplishes. It's the same Y values, the charges, in the form of a multi-dimensional array. As I mentioned before, neural networks work better when you're dealing with small numeric values. Now, our insurance charges vary from zero to about $50,000, $60,000. These are not small numeric values. And in order to make our neural network training more robust, more likely to converge, I'm going to use the min_max_scaler to scale all insurance charges to be expressed in the range zero to one. That's what the min_max_scaler does by default. Now, all insurance charges are expressed as values between zero and one. We now have our input features and labels that we'll use to train our neural network. However, they are in the NumPy format. I'm now going to convert them to Torch tensors. torch.from_numpy will convert NumPy arrays to Torch tensors. Now, Torch tensors are the primary [[Data Structures]] used in [[PyTorch]] for building neural networks and other ML models. Torch tensors are multi-dimensional arrays like NumPy, but they support distributed training using GPUs. They are used in neural network training because they support
>
> **[6:16](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/preprocessing-data-for-training?u=76281980&t=376)** automatic differentiation for gradient computation and essential part of the training process of a neural network.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Neural Networks]] (4), [[Data Structures]] (1), [[PyTorch]] (1)
> **Code Identifiers:** fit_transform (2), min_max_scaler (2), from_numpy (1)
> **Definitions:** means that (2), is a  (1)
> **CLI Commands:** find (1), make (1)

#### [Creating a simple neural network](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-simple-neural-network?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-simple-neural-network?u=76281980&t=0)** At this point, our training data is processed and ready to be used to train a neural network model. So the first thing we'll do is set up a very simple neural network. In fact, the simplest possible neural network, one with just one neuron and no activation function. So all this neural network will be able to learn is linear relationships in the data. Since the dataset that we're using is a simple straightforward one, you'll find that this very simple neural network also does fairly well and can be used to build a fairly decent regression model. Let's see how we set up this very simple neural network. Here I've defined a class called SimpleNeuralNet that inherits from the [[PyTorch]] nn.Module class. nn.Module is a base class for all [[Neural Networks]] that you'll build using PyTorch. It gives you a wide range of functionality that makes the development and management of neural networks easier and more sustainable. Within the init method of the nn.Module class, you'll specify the layers of the neural network. Here are init method, takes in one input argument, the number of features in the training data, and within that we have just one layer, and that layer has just one neuron. We instantiate that layer using the nn.Linear object. Notice this linear layer takes in num features as input, that is all of the features in our training data, and has just one neuron.
>
> **[1:32](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-simple-neural-network?u=76281980&t=92)** You override the forward function in the base nn.Module class to specify the forward pass-through the neural network. Notice the forward function here takes in the record as an input argument, that is, a training data and essentially invokes the linear layer on the training data. So you're passing the input data through that linear layer. And this transformed output x is what we return. The nn.Module class gives you other bits of functionality that will be useful, such as moving all of the parameters of the neural network to a CPU or GPU device so that it can be trained on that device. Now that we have our very first simple neural network, let's explore and understand it. Let's instantiate the neural network. The number of features that we have are eight. Remember that's the number of columns in the X train data. Printing out the neural network will give us a string representation of the layers in the net. We have just one linear layer with a single neuron and no activation function. Before a neural network is trained, these weights and biases are initialized to random values. You can actually access the weights and biases of each layer by running a for loop through the layers in your model. Here, for every linear layer in the model, we have just one. I print out the weights and biases, and you can see that there are eight weights corresponding to the eight features that are going to be fed into the single neuron in our linear layer
>
> **[3:05](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-simple-neural-network?u=76281980&t=185)** and one bias value. These weights and biases are the model parameters that will be found during the training process of the model. These weights and biases will converge to some values, allowing the model to make predictions. You can actually count the number of parameters that this model is going to train. Here is a count parameters function that will help us do that. You can iterate over all of the model parameters by accessing the parameters function on the model, and if the parameter requires gradients, that is, gradients will be computed, you know that it's a trainable parameter. And by running this, you see that we have a total of nine trainable parameters for this very simple neural network, eight weights and one bias. Training a neural network involves using a loss function. This loss determines how good the neural network is at any point in time. You can think of the loss as representing how far away the predictions of the neural network are from the actual target values. Since this is a regression model, the loss function that we'll use is going to be the mean squared error loss available in F.mse_loss. Let's compute the loss on predictions made by our untrained model. Notice that we pass train inputs through the model and then compare those predictions with the train targets. And then we compute the loss function. And the loss here is 0.5406. The loss is not really meaningful unless we see how the loss falls
>
> **[4:40](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-simple-neural-network?u=76281980&t=280)** as we train the model. But this is just to show you how the loss function is computed. Let's get and view the predictions from our model at this point in time. Remember we haven't trained the model at all. The model parameters all have random values. So these predictions are actually just random predictions. What we are seeing here is some of the steps that we'll be performing during the model training process. Once the model has been trained and we get the final predictions from the model, we'll evaluate the model using the r-square score and by computing the mean squared error. The R2Score and the MeanSquaredError classes are available as a part of Torch metrics regression. We instantiate those objects, MSE and r2score, and we compute these values on Lines 8 and 9 by passing in the predictions from the model and the actual target values. Again, remember the model has not been trained. So both the mean squared error, that's just the loss, and the r squared will essentially have random values. The r squared is actually negative.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (2), [[Neural Networks]] (2)
> **Definitions:** is a  (4)
> **Env Vars:** cpu (1), gpu (1), mse (1)
> **CLI Commands:** find (1), make (1)
> **Prerequisites:** set up (2)
> **Code Identifiers:** mse_loss (1)
> **Versions:** 0.5406 (1)
> **Analogies:** such as (1)

#### [Setting up the dataset and DataLoader](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/setting-up-the-dataset-and-dataloader?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/setting-up-the-dataset-and-dataloader?u=76281980&t=0)** In order to feed our training data into our [[PyTorch]] neural network in batches, we are going to be using a tensor dataset and a tensor DataLoader. In PyTorch, the dataset and DataLoader are foundational classes provided by the torch.utils.data module. These facilitate the loading, processing, and batching of data. Dataset is an abstract base class in PyTorch and we'll be using the derived tensor dataset class. You can think of a dataset as representing a collection of data items. And this tensor dataset that we've instantiated here holds our training data. We instantiate a tensor dataset using the train inputs and train targets, and here are the first five records in the tensor dataset. Just a heads up that here we've used one of the built-in dataset classes, the tensor dataset, but it's also possible for you to create your own custom dataset by deriving from the dataset base class. Once we've set up a dataset, when we actually access the training data to train our model, we'll want to load the data in batches. We may want to shuffle the data or use multiple workers to speed up data loading. All of that is done via the DataLoader. I've instantiated a DataLoader here and specified a batch size of 8. For the training data, I've also set shuffle equal to true. So when we feed the data into our model for training, it will be shuffled. It will not come in any predictable pattern. The DataLoader is an iterable that allows us to iterate over the
>
> **[1:35](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/setting-up-the-dataset-and-dataloader?u=76281980&t=95)** data in batches. On Line 5 you can see I create an iterator and call next on it, and this will allow us to see the first batch of training data. Notice that we have eight records here because we specified batch Size 8. We've now created a dataset and DataLoader for our training data. Let's do the same for validation data. First, we convert X and Y value to the tensor format. And once that's done, we'll instantiate a tensor dataset for our validation data. And then we'll instantiate a DataLoader using this tensor dataset. Notice that when we instantiate a DataLoader for the validation data, I haven't specified shuffle equal to true. Validation data is only used to evaluate the model and does not need shuffling. Here is the first batch of records from the validation data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (3)
> **Definitions:** is an  (2)
> **Warnings:** heads up (1)
> **Prerequisites:** set up (1)

#### [Training a neural network using PyTorch](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-a-neural-network-using-pytorch?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-a-neural-network-using-pytorch?u=76281980&t=1)** We are now ready to start training our neural network. I'm going to set up a dictionary called loss_stats which will hold the values of the training loss and validation loss for each epoch. We'll run 100 epochs of training and epoch, as you likely already know, is one pass through the entire training data. The next thing we need to do is figure out on what device we'll run this training. Now, because I'm running on my local machine, I do not have access to a GPU, but it's possible that you are running this on [[Google]] Colab or a machine where a GPU is available. What we are doing here is a check to see whether a GPU is available. If it is, we'll use the GPU. Otherwise, we'll use the CPU. So if torch.cuda.is_available returns true, that is, we have a GPU, then the device will be CUDA. If torch.backends.mps.is_available is true, this means that the new metal performance shaders backend for GPU training and acceleration is available on your machine, so the device will be mps. This is likely to be available if you're working on a new Mac device, or if neither of these options is true, we'll just go with CPU training and device will be CPU. You can see here that we're using the CPU device because I do not have a GPU available. We'll now instantiate and train our neural network using [[PyTorch]]. Remember PyTorch is a lower-level API and it does not abstract us away from the details of neural network training.
>
> **[1:32](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-a-neural-network-using-pytorch?u=76281980&t=92)** And there will be a lot of boilerplate code involved. I instantiate the simple neural network num features is equal to eight, and I call to device in order to move the model parameters of the neural network to whatever device we are using for training either CPU or GPU. Moving to the device that you're using for training is a part of PyTorch's boilerplate. For all of your training data and your model, you have to move them to the right device so that the training occurs on the right device. The objective of training a neural network is to update your model parameters for every iteration of training, so as to minimize the loss function. In PyTorch, it's the optimizer that actually updates the model parameters using gradient values. So I've instantiated an optimizer for that purpose. The optimizer I've used here is the SGD or the stochastic gradient descent optimizer. There are several different optimizers available as a part of the PyTorch framework. SGD is a commonly used straightforward optimizer. The optimizer takes in the model parameters that need to be updated, as well as the learning rate, which are set to 10^-2. The learning rate determines the step size for how the model parameters converge to their optimal values. Too larger step size, your model may not converge, too smaller learning rate, and your model may take too long to converge. This 0.01 works well for this particular model, and that's why I've selected it.
>
> **[3:07](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-a-neural-network-using-pytorch?u=76281980&t=187)** Next, let's set up the training loop for our model. And here is where you'll really see the boilerplate code. And you'll find that when we use PyTorch Lightning in the next demo, most of this code will be eliminated. First, I have a for loop to iterate over the number of epochs of training that we'll run. I initialize the training epoch loss to zero, we'll reset this for every epoch, and then we make sure that the model is in training mode by calling model.train. In the training mode, gradients will be computed so that model parameters can be updated using those gradients. Gradients are just partial derivatives of the loss function with respect to individual model parameters, and these partial derivatives are used to determine how model parameters should be tweaked to minimize the loss function. This is all you need to understand conceptually about gradients. For every epoch, we run another for loop, iterating over each batch of the training data. This is on Line 7. Now, for each batch of training, we zero out the optimizers gradients so that gradients that were computed previously do not affect this particular batch. On Lines 12 and 13, we move our training data to the device that we are using for training, whether it's a GPU or a CPU. Notice the boilerplate to device code here. Next, we make a forward pass through the model, this is on Line 16, for the first batch of training data and get predictions.
>
> **[4:40](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-a-neural-network-using-pytorch?u=76281980&t=280)** This forward pass uses the current value of the model parameters. We then compute the loss of the model at this stage by invoking the loss function, which takes in the prediction and the Y values, and then we perform a gradient descent. We make a backward pass-through the model by calling train_loss.backward. This is where gradients are computed, are partial derivatives, and optimizer.step will then use those gradients to tweak our model parameter values. And then on Line 25, we add the current training loss for this batch to the training loss of the epoch as a whole. All of the steps you see from Line 9 through Line 25 is repeated for every batch of data in each epoch. Model parameters will be updated for every batch in each epoch, and the loss function will be minimized to improve the model. After each epoch of training, we'll run the validation data through the model and evaluate the model's performance. You can see the with torch.no_grad with block on Line 28, that's within the outer for loop for the epoch, but outside of the for loop for the individual batches of training. The torch.no_grad method turns off gradient computation for the model, so gradients will not be computed when we pass through the validation data. I initialize the validation epoch loss to zero on Line 30, and on Line 32, we switch the model over to the evaluation state by calling
>
> **[6:15](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-a-neural-network-using-pytorch?u=76281980&t=375)** model.eval. This is the state for evaluating the model. Then once again I have a nested for loop where we iterate over the batches of validation data. On Lines 36 and 37, we move the validation data to the device that we are using for training, CPU or GPU. We get the predictions on the validation data on Line 39, compute the loss on Line 41, and we add the loss of this batch to the validation epoch loss. On Lines 45 and 46, we compute the training loss and validation loss for the entire epoch, and then append that information to our loss stats dictionary. This is on Lines 48 and 49. On Line 51, for each epoch, we print out the training loss and the validation loss. And that's it. This is the training process. It's simple, but there is really a lot of boilerplate code involved. Let's look at the output of training. Because this is a very simple neural network, training runs through very quickly. You can see that after the first epoch of training, training loss was at 0.07 and validation at 0.01, but then training loss falls to 0.011, validation loss falls to 0.008. Training loss further falls to 0.010 and stays there for the remaining epochs. That is the lowest value of loss for this particular neural network that our model was able to achieve.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (6), [[Google]] (1)
> **Env Vars:** gpu (10), cpu (7), sgd (2), cuda (1), api (1)
> **Code Identifiers:** is_available (2), no_grad (2), loss_stats (1), train_loss (1)
> **Versions:** 0.01 (2), 0.07 (1), 0.011 (1), 0.008 (1), 0.010 (1)
> **Definitions:** is a  (5), means that (1)
> **Exercise Files:** boilerplate (5)
> **CLI Commands:** make (3), find (1)
> **Prerequisites:** set up (2)

#### [Visualizing losses and evaluating models](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/visualizing-losses-and-evaluating-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/visualizing-losses-and-evaluating-models?u=76281980&t=1)** Now that we have a trained model, let's visualize how the training loss and validation loss change over epochs of training. And for that, I'm going to set up a data frame with the training loss and validation loss along with the epochs. This information is available in the loss [[Statistics]] that we've manually populated in the training process. And we now have this in the form of a data frame. We have the epochs, then whether it's training or validation loss and the corresponding value. The head shows us all of the training losses, and the tail of this data frame contains all of the validation losses. Now with this information in the data frame format, visualizing this using a Seaborn lineplot is very straightforward. We'll have epochs on the x-axis and training and validation losses on the y-axis. And here is what the line chart looks like. The training loss and the validation loss falls drastically in the first two or three epochs, but remains constant after that. Two to three epochs of training was sufficient for this model. The model does not improve beyond that. Let's now compute the R-square score of the model on the validation data. Ideally, we should have a separate test dataset for this purpose, but because the dataset was fairly small, let's just do it with the validation data. We turn off gradients with torch.no_grad, switch the model to eval mode, model.eval, and we iterate over every batch in the validation data. Move the features to the device. This is on Line 9.
>
> **[1:33](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/visualizing-losses-and-evaluating-models?u=76281980&t=93)** Get the predictions from the model. y_pred will hold the predictions from the model, y_true will hold the labels from the actual data. Let's quickly look at the format of the actual data that is in y_true. What we have here is a list of tensors where each tensor contains a prediction for one batch of data. You can check this out on your own. The prediction data will also be in the same format. I'll now perform a torch.stack operation that will give us the actual values in the form of a single tensor, rather than a list of tensors. This is what torch.stack outputs. I now have a single tensor with all actual values. I'll use torch.stack on the predicted values so that we now have a single tensor with all predicted values, as well. Now that the data is in this form, let's compute the mean squared error and R-square score for this model on the validation data. I instantiate mean squared error and R-square score and move those to the device as well and compute the two values. You can see that the R-square of this model is 0.797, which is a fairly good score.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Statistics]] (1)
> **Code Identifiers:** y_true (2), no_grad (1), y_pred (1)
> **Definitions:** is a  (2)
> **Versions:** 0.797 (1)
> **Prerequisites:** set up (1)

#### [Building and training a more complex neural network](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/building-and-training-a-more-complex-neural-network?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/building-and-training-a-more-complex-neural-network?u=76281980&t=0)** What I've done here is scroll back up in our Jupyter Notebook to where we had defined our simple neural network. Remember we mentioned that this neural network contains just a single linear neuron with no activation function. When you use a neural network with a single neuron, essentially what you're doing is performing [[Linear Regression]] that you would with a traditional machine learning library such as scikit-learn. I'm now going to replace this neural network to have a more complex one with several layers and interconnections. I've still called it SimpleNeuralNet. We still inherit from the nn.Module base class, but you can see that overall the neural network has many more layers. The layers are defined in the init method. We take in the number of features as an input argument, and Layer 1 comprises of 16 neurons. Notice I instantiate nn.Linear num features, that is, the input and the number of neurons is 16. Layer 1 is the first layer in our neural network. The output of this linear layer will be fed to the second linear layer. This is Layer 2 instantiated once again using nn.Linear. The dimensionality of the inputs to the second layer is 16, and this needs to match the number of neurons in the previous layer. So Layer 1 has 16 neurons and this matches the number of input features fed into Layer 2. Remember this is because the output of Layer 1 is fed into Layer 2.
>
> **[1:33](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/building-and-training-a-more-complex-neural-network?u=76281980&t=93)** The number of neurons in Layer 2 is 32. And then we have Layer 3, the third linear layer in our neural network. The input features to the third linear layer is 32, and this should match the number of neurons in the previous layer which is 32. Layer 3 has just 16 neurons. And then finally, we have the last linear layer, that is the output layer, layer out. It accepts 16 input features as input and produces just one output. The 16 features as input corresponds to the 16 neurons in the previous layer. Each of the three layers; Layer 1, Layer 2, Layer 3 have an activation function, and the activation function that I've chosen here is ReLU activation. Activation functions are what allow us to learn nonlinear relationships that might exist in the data. Now, the forward function defines the forward pass-through the neural network. We receive the inputs as an input argument to this forward function, we apply Layer 1 and then the ReLU activation function. Then the outputs are then passed through to Layer 2, and then ReLU activation again. And the outputs are passed through to Layer 3, and we have the ReLU activation yet again. The transformed data after passing through three layers is finally passed through the output layer. And the output layer's prediction is what we return from the forward pass. The predict method is invoked when you use this model to
>
> **[3:08](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/building-and-training-a-more-complex-neural-network?u=76281980&t=188)** get predictions. And the predict method makes the same forward pass-through the neural network. Now, let's quickly execute all of the remaining steps here in this Jupyter Notebook. Here is a representation of the model, three linear layers and output layer and ReLU activation. Now this model will have many more parameters since we have a large number of layers and many interconnections between layers. The weights and biases of each layer have been initialized to random values. Let's count the number of parameters in this model, and you can see that this model has 1233 parameters. You are familiar with the remaining steps here. I'll just quickly execute them and let's go directly to where we set up the dataset and DataLoader and train our model. Here is where we instantiate the dataset. You've already seen how that works before. And once we have the dataset, we set up the DataLoader. This is for the training data. We do the exact same thing for the validation data, as well. Set up the dataset and the DataLoader. Again, we initialize the loss_stats dictionary and we'll train for 100 epochs. We then see what device is available and that is stored in the device variable. Now that we've set all this up, instantiate the simple neural network and move the model parameters to the device that we're using for training. Once again, initialize the optimizer, which will update the model parameters during the training process.
>
> **[4:41](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/building-and-training-a-more-complex-neural-network?u=76281980&t=281)** Next, we actually train the model. We train for 100 epochs, and after each epoch, we pass the validation dataset through the model and compute the validation loss. I'm going to kickstart the training process and let's see how this more complex neural network performs. You can see that initially our training loss was 0.041, the validation loss is the same. And as we run through epochs of training, notice how both the training loss and the validation loss drops. Until at the very end, after about 99 epochs, our training loss is 0.007 and validation loss 0.005. Let's set up the data frame with the training loss and the validation loss so that we can plot and visualize the values. Execute this visualization code and you'll be able to see how the training loss and validation loss fall over epochs of training. Both losses fall until about 45 epochs of training, after which they remain fairly steady. I'll hit "Shift-Enter" to get the predictions from the model and the actual values from the data. I'll use torch.stack to get the predictions and the actual values in a single tensor. And let's compute the R-square and the mean squared error. Notice the R-square score has gone up. It's now 0.881. So a more complex neural network that included an activation function improve the performance of our model.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linear Regression]] (1)
> **Versions:** 0.041 (1), 0.007 (1), 0.005 (1), 0.881 (1)
> **Prerequisites:** set up (3)
> **Tools:** jupyter (2)
> **Code Identifiers:** loss_stats (1)
> **Cross-References:** we mentioned (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)


### 2. Using PyTorch Lightning to Build a Regression Model

[↑ Back to Table of Contents](#table-of-contents)

#### [Encapsulating data using a LightningDataModule](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/encapsulating-data-using-a-lightningdatamodule?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/encapsulating-data-using-a-lightningdatamodule?u=76281980&t=0)** At this point, we've successfully built and trained a neural network to perform regression, but we used [[PyTorch]] and not PyTorch Lightning. It was important that you see how the model is built using PyTorch first, so that you can see how much cleaner our code is when we use PyTorch Lightning. We'll eliminate most of the boilerplate code that you saw for training loops and feeding data in batches, iterating through number of epochs, moving model parameters, and data to the right device. All of that code will just disappear, and we'll build our model in a very clean manner with PyTorch Lightning. PyTorch Lightning is a lightweight PyTorch wrapper that helps researchers and developers organize their PyTorch code and streamline the training process. PyTorch Lightning eliminates boilerplate code, simplifies the training loop, and provides a more structured approach to PyTorch programming without compromising flexibility. Pip install lightning to get PyTorch Lightning installed on your local machine. You need the lightning module in addition to the PyTorch framework that we've already installed. Let's go ahead and set up the import statements for the various libraries that we'll be using. The imports here are the same as in the previous demo, but I have a few additional imports for PyTorch Lightning. The one on Line 9, you can see import lightning.pytorch as pl. In addition to abstracting away the training process of a neural network, the training loop, and other code associated with it,
>
> **[1:34](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/encapsulating-data-using-a-lightningdatamodule?u=76281980&t=94)** PyTorch Lightning also makes available the lightning data module. pl.LightningDataModule allows us to abstract and organize the data-related aspects of our [[Deep Learning]] model. It's a way to decouple the [[Data Processing]] steps, the loading, preprocessing, and splitting of data from the model training logic. When we structure all of our data processing tasks to be encapsulated within a lightning data module class, we are centralizing all data-related operations in one place and encapsulating all of our code to make it more modular and reusable. Here, I've specified the skeleton of the insurance data module class that inherits from pl.LightningDataModule. And you can see that there are several functions of the base class that I'm about to override. The functions have all been named in very meaningful way. So you know exactly what goes in each of these functions. Let's start by adding in the code for the init method here in this insurance data module. This is where we'll initialize various parameters we may want to use with the data. The only one we have is the batch size. In the prepare_data function is where you access the data wherever it's stored, maybe you'll need to download the data, and you can also perform a little bit of preprocessing if needed. The only thing I do here is to read in the insurance.csv file into a [[Pandas (Software)|pandas]] data frame insurance data. This method is not invoked in a distributed manner and usually
>
> **[3:08](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/encapsulating-data-using-a-lightningdatamodule?u=76281980&t=188)** called on a single GPU. Next, we have setup. This is where you will split the dataset and apply whatever transformations and pre-processing that you need. This will be called on every GPU separately. An input argument to the setup function is what stage the model is currently running. This can be the fit stage, that is, training or it can be the validation or test stages. Here we are keeping things simple, so I'll apply the data transformations all in the fit stage or the training stage of the model. The [[Data Transformation]] operations that you see here should all be very familiar to you because these are the steps we carried out in the previous demo. On Lines 9 and 10, we extract the X features and Y values. On Line 12, I check whether the stage is fit or stage is none. So in the training phase, we split the data into training and validation. This is on Lines 13 and 14. The code on Lines 16 through 28 is where we one-hot encode the categorical features in our data. On Lines 30 and 31, we convert the Y values to NumPy arrays. On Line 33 through 36, we standard scale our features. On Lines 38 through 41, we minmax scale our targets. That is the Y values. And on Lines 43 through 47, we convert all our NumPy arrays into tensors. We'll feed in data to our model in the form of tensors. The training and validation data are available as member variables
>
> **[4:45](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/encapsulating-data-using-a-lightningdatamodule?u=76281980&t=285)** of this class; train_inputs, train_targets, val_inputs, and val_targets. The feature and target tensors need to be instantiated as DataLoaders. The train_dataloader method returns the DataLoader for the training data. The steps here are again familiar. We instantiate a tensor dataset, and using that, we instantiate a DataLoader. Notice that I've specified num workers equal to four for the DataLoader. This is because I have four cores on my machine, and this will allow me to load data in parallel using those four cores. The train_dataloader function returns an instance of the DataLoader for the training data. In exactly the same way, I've overridden the val_dataloader function. This is just a DataLoader for the validation data. We instantiate a tensor dataset and use that to instantiate a validation DataLoader. num_workers, again, set to four, so that four workers running on four cores can be used to load this data. When we actually train our model, this insurance data module will be passed in as an input argument to the trainer object that we'll use. And the individual methods of this data module will be invoked at the right point in time to get access to the right bits of data needed for training and validation. Let's just make sure that the insurance data module works as expected. I'm going to create an object of the insurance data module. Let's now call prepare data and set up manually so that
>
> **[6:19](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/encapsulating-data-using-a-lightningdatamodule?u=76281980&t=379)** the data is available. And now I'm going to invoke the train_dataloader, and I'll print out one batch of the training data. And you can see that there are eight records in this batch. In a similar way, let me access the val_dataloader and I'll print out one batch of the validation data, as well. And we have eight records in this batch. Observe how by using a data module to manage all of the [[Data Preparation]] and processing operations, we've created a modular bit of code that can be reused anywhere.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (15), [[Data Processing]] (2), [[Deep Learning]] (1), [[Pandas (Software)|Pandas]] (1), [[Data Transformation]] (1)
> **Code Identifiers:** train_dataloader (3), val_dataloader (2), prepare_data (1), train_inputs (1), train_targets (1)
> **Prerequisites:** set up (2), setup (2), install (1), you'll need (1)
> **CLI Commands:** make (2), pip (1)
> **Exercise Files:** boilerplate (2), download the (1)
> **Env Vars:** gpu (2)
> **File Paths:** insurance.csv (1)
> **Definitions:** is a  (1)

#### [Encapsulating a model using a LightningModule](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/encapsulating-a-model-using-a-lightningmodule?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/encapsulating-a-model-using-a-lightningmodule?u=76281980&t=0)** In the previous demo, we built a simple regression model using [[PyTorch]]. Now in this demo, I'm going to build the same neural network that we used before, but this time I'm going to use PyTorch Lightning. What I've defined here on screen is the skeleton of a class that derives from pl.LightningModule. Just like the lightning data module encapsulates all of the data-related operations, a lightning module is a fundamental class in PyTorch Lightning that encapsulates everything related to our [[Deep Learning]] model. It extends the functionality of the nn.Module class that we used in PyTorch to build up our neural network. The lightning module adds additional methods and structures that streamline the training, validation, testing, and prediction processes of the model. A lightning module organizes your PyTorch code into different sections, and each section is a different method that you override from the lightning module base class. You can see that I have six different methods here. These correspond to the six different sections into which lightning module organizes your code. Let's look at and understand each of these step by step, starting with init. The init method, and in addition, there is also a setup method that I've not overridden, this is where you'll define your module. Whatever we had specified in the nn.Module init method, I've essentially moved that code in here to the init method of lightning module.
>
> **[1:33](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/encapsulating-a-model-using-a-lightningmodule?u=76281980&t=93)** The init method takes in a number of parameters for the model, num features is the number of input features and the learning rate, which I've set to 0.01. This learning rate parameter will be used by the optimizer that we'll instantiate. Now within the init method, you can see I've set up the layers of the neural network. There are three layers, and then the final output layer. And the activation function that we'll use for the three layers is the ReLU activation. This network is exactly the same network that we've used in our earlier demo. An interesting thing to note here is the code on Line 11 where I call self.save_hyperparameters. save_hyperparameters is a method in the lightning module base class. When you invoke self.save_hyperparameters here in the init method, all of the input arguments to init, here we have two, num features and learning rate, will be saved as hyperparameters and will be accessible via the self.hparams object. Let me explain what I mean by adding the code here in the configure optimizers method. This is the method where you will instantiate and set up any optimizers and schedulers that you use to train your model. I once again use the stochastic gradient descent optimizer, SGD, I pass in the parameters of the model available in self.parameters, and I pass in the learning rate of the optimizer using self.hparams.learning rate. The learning rate that we passed in as an input argument to init on
>
> **[3:10](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/encapsulating-a-model-using-a-lightningmodule?u=76281980&t=190)** Line 3 has been saved in this hparams object that we access here on Line 13 because we invoked self.save_hyperparameters on line 11. self.save_hyperparameters thus saves all input arguments that you pass into init. The forward function is where you define the forward pass-through the neural network, and here we define the same forward pass as we did before in PyTorch. We pass the inputs through the three linear layers. Each layer has the ReLU activation. And then finally, we pass through the last output layer and return the final value. Now if you remember in PyTorch, we set up a training loop, and within that, we define the training process of a model. This is what you'll define here in training step. This training step function will be invoked in a loop. What you define here are the operations that need to be performed on a single batch of training data. You can see the input arguments a batch of data is passed in. We access the X features and Y values from this batch. We make a forward pass through the model by calling self.forward on the X features. On Lines 26 and 27, we instantiate the mean squared error loss function and compute the loss for this batch of predictions. And then we simply call self.log and log the loss out. Actually, displaying the loss on screen and logging it out, well, PyTorch Lightning will take care of that automatically.
>
> **[4:44](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/encapsulating-a-model-using-a-lightningmodule?u=76281980&t=284)** To define the validation process of your model, you'll simply specify validation on one batch of data here in the validation step method. Well, validation is straightforward. Once again, we make a forward pass with a batch of data using self.forward, we compute the mean squared error loss on the validation data using the loss function, and we log out the validation loss. And we have a predict step that you can override here. In order to make predictions on the data, we access the X variables and Y values from the batch and simply make a forward pass through the model to get predictions. You can see here that the entire model training code, without the additional boilerplate of moving the model to a device or the training loop, has been encapsulated here in one class. Let me just instantiate and print out a string representation of our model defined in this lightning module class. And in the next movie, we'll see how we will actually train a model using PyTorch Lightning.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (9), [[Deep Learning]] (1)
> **Code Identifiers:** save_hyperparameters (5)
> **Prerequisites:** set up (3), setup (1), configure (1)
> **CLI Commands:** make (4)
> **Definitions:** is a  (3)
> **Env Vars:** sgd (1)
> **Versions:** 0.01 (1)
> **Cross-References:** in the next (1)

#### [Training the model using the PyTorch Lightning Trainer](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-the-model-using-the-pytorch-lightning-trainer?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-the-model-using-the-pytorch-lightning-trainer?u=76281980&t=1)** We've defined our model, our training step, and validation step nicely encapsulated in a [[PyTorch]] Lightning module, and we are now ready to train our model. If you remember in PyTorch, you had to write a lot of code to actually train your model and get validation metrics for your model after each epoch. In addition, in order to ensure that your model trains on a GPU, if a GPU is present, you had to also move the model parameters as well as the X and Y values that you're using to train your model to the right device for training. Now, keep all of that in mind while we see how easy it is to train a model using PyTorch Lightning. You see those five lines of code here on screen, including the import statement, well, that's all the code you need to run a training loop, run validation at the end of every epoch, and display all of that nicely to screen. No nested for loops, no moving the model to the right device, no loss computation on training and validation data, no optimizer step, no loss.backward. Nothing. So what's the code that we write? First, let's take a look at the import statement, where I import a CSVLogger that will log the details of the training process out to screen in a CSV format. Next, I instantiate the insurance data module class, which encapsulates all of the [[Data Preparation]] and processing operations for the data that I plan to use to train the model. Next, on Line 5, I instantiate a CSVLogger class
>
> **[1:37](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-the-model-using-the-pytorch-lightning-trainer?u=76281980&t=97)** to write the logs out to the logs subfolder under my current working directory. The actual training process will be taken care of by the pl.Trainer class. This is a central class that manages and automates the entire training process, and abstracts away a lot of the boilerplate code typically associated with training loops, distributed training, and evaluation. I instantiate the trainer, specify I want to run a maximum of 50 epochs of training, passing the CSVLogger so that logs are generated in my current working directory. And that's it. I call trainer.fit. Trainer.fit takes in an instance of a lightning module, that is the model that we want to train, and a data module, that is a data set we should use for training. Trainer.fit will invoke the right methods on the model class, as well as the data class to get the right data for training and validation, and it will run the training process for 50 epochs. Our training process has begun. Observed that the trainer automatically checks to see whether a GPU or a TPU is available. If not, it will just run training on the CPU. The trainer also shows you the number of trainable parameters in the model. We had manually computed this earlier in PyTorch. We have about 1.2K or 1200 parameters. As the training process continues, you'll see for every epoch of training, the trainer prints out the epoch,
>
> **[3:11](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-the-model-using-the-pytorch-lightning-trainer?u=76281980&t=191)** so you can see Epoch 16 here on screen, the training loss at this epoch, and the validation loss at this epoch. We had set show progress bar to true when we logged out our training loss and validation loss. And that's why you see these progress bars on screen, as well. At Epoch 49 are 50th epoch, the training process is complete. The training loss at this point in time is 0.00598, and the validation loss is 0.00477. Now that we have a trained model, let's see how easy it is to get predictions from the model. I call trainer.predict, pass the model in, and specify the validation DataLoader as the DataLoader for the data for which I want predictions. And you can see the predictions from the model output here on screen. The output format of predictions gives us every batch of predictions in a separate tensor. Let's get all of these in a stacked format by using torch.cat to concatenate them. This will give us a single tensor with all the predictions from our model for the validation data. In order to compute metrics for the model, I need the actual values or the labels from the validation data as well, and I extract this by using a for loop. Now that I have this, let's stack the labels as well so we get a single tensor with all of the labels from our data. Now that we have the predictions as well as the labels, all that's left to do is to compute the mean squared error and
>
> **[4:46](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-the-model-using-the-pytorch-lightning-trainer?u=76281980&t=286)** the R-square score for this model. And since this is the exact same neural network as the one we built in PyTorch, the R-square score is also the same 0.882. If you remember, we passed in a CSVLogger to the pl.Trainer object that we had instantiated to log out the metrics during the training process. We can now access these metrics. They'll be in a metrics.csv file in your log directory. The log directory is accessible via the trainer instance, trainer.logger.log_dir/metrics.csv. This is a data frame that I read in, which gives us the training loss, validation loss for every epoch. We do a little bit of pre-processing with this metrics data frame. For instance, I drop the step column and then I go ahead and display the metrics as a line plot. Along the x-axis, we have the epochs of training. Along the y-axis, we have the training and validation losses. The blue line represents the training loss and the orange dotted line is the validation loss. Now, you've trained the same model using PyTorch Lightning, and you can see how much simpler and more intuitive the code was.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (6), [[Data Preparation]] (1)
> **Env Vars:** gpu (3), csv (1), tpu (1), cpu (1)
> **Versions:** 0.00598 (1), 0.00477 (1), 0.882 (1)
> **Definitions:** is a  (3)
> **File Paths:** metrics.csv (1), trainer.logger.log_dir/metrics.csv (1)
> **CLI Commands:** cat (1)
> **Code Identifiers:** log_dir (1)
> **Exercise Files:** boilerplate (1)


### 3. Using PyTorch Lightning to Build a Classification Model

[↑ Back to Table of Contents](#table-of-contents)

#### [Loading and exploring classification data](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/loading-and-exploring-classification-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/loading-and-exploring-classification-data?u=76281980&t=0)** We've seen how easy it is to build and train a neural network using [[PyTorch]] Lightning. Let's get some more practice with this. And this time, we'll build and train a classification model. Here I am on a new Jupyter Notebook. Since we'll be exploring and visualizing a new dataset comprising of classification data, I'm going to ignore some warnings that will display when I use Seaborn for visualization. By the time you're watching this course, you're unlikely to need this bit of code if you're using the latest version of Seaborn. Next, I set up the import statements for all of the libraries that I'll use. This involves data access, [[Data Processing]] libraries. You can see that I've also imported lightning.pytorch as pl on Line 10. Because this is a classification model, we'll evaluate this model with a different set of metrics. On Line 13 and 14, you can see I import accuracy and F1Score. These are the metrics we'll use to evaluate the model. I have the data that we'll use to train the model in the datasets folder here. This is a churn modeling dataset. We'll have information for a number of customers, and we'll use that information to predict whether the customer churned or not. Let's read in and take a look at the data before we actually set up a lightning data module to encapsulate all our data operations. I'm going to read in from the Churn_Modelling.csv file under datasets. Here in this dataset, we have information for bank customers, row number, customer ID, last name, credit score, gender, age, tenure, whether the customer has
>
> **[1:37](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/loading-and-exploring-classification-data?u=76281980&t=97)** a credit card or not. And finally, the last column here is exited. That column contains the labels that we are trying to predict. Exited equal to one, meaning the customer churned. Zero means the customer did not churn. When you take a look at the columns here in this dataset, it's pretty clear that there are certain columns which are not really relevant in predicting whether the customer churned or not. For example, columns such as row number, customer ID, or even the surname or last name of a customer. Those are bits of information not really relevant to figuring out churn. Those are some columns we'll get rid of during data preprocessing. Next, let's take a look at the data types for the various columns to make sure they are all of the right type. A quick glance over the column shows me that numeric values are numeric types, either integers or floats, and categorical values are of type objects or strings. Things look good so far. This dataset contains fields with missing values. We'll clean up this data by using dropna to drop any records which have missing values, that's the code on Line 1, and then this dataset also contains a few duplicate records. And we'll eliminate duplicates by calling drop_duplicates on a [[Pandas (Software)|pandas]] data frame. And once both of these are done, the number of records we are left with for training as well as validation data is about 10,000 records. The classification model that we're trying to train is a binary classification model.
>
> **[3:10](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/loading-and-exploring-classification-data?u=76281980&t=190)** We are trying to predict whether a bank customer exited the relationship or not. We are trying to predict churn. If you look at the value counts for the exited field, you can see that this data set is very skewed. Eight thousand out of the 10,000 customers did not churn and only 2,000 did. You can actually view the same information using a nice Seaborn count plot. Based on the exited values, you can see that there are many more customers who haven't exited as compared with customers who have exited or churned. Once again, this is a skewed dataset, and I'll give you a heads-up right now that we won't actually be mitigating the skewness of this data. So I won't perform any sampling to balance out the customers who have churned and who've not churned. So there is a limit to how good our binary classification model can be. If your training data is skewed, it's hard to build a great classification model. But since our focus is primarily on learning to use PyTorch Lightning to build a neural network model, we'll work with the skewed data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (3), [[Data Processing]] (1), [[Pandas (Software)|Pandas]] (1)
> **Definitions:** is a  (5)
> **Analogies:** for example (1), such as (1)
> **Prerequisites:** set up (2)
> **File Paths:** churn_modelling.csv (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** drop_duplicates (1)
> **Tools:** jupyter (1)

#### [Creating a LightningDataModule](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-lightningdatamodule?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-lightningdatamodule?u=76281980&t=0)** If you remember the previous regression demo where we used [[PyTorch]] Lightning, the first thing we set up was a data module class. This data module encapsulated all of the [[Data Preparation]], processing, and other operations to work with our training and validation data. Here is our BankCustomerChurnDataModule which inherits from the pl.LightningDataModule base class. Now, in the init method is where we initialize the parameters of the data. We just have the batch size, and that's the only input argument to the init method. In prepare_data is where we load the data from wherever it's available and perform a few preprocessing operations to set up our data. The prepare_data method when we train our model is run on a single GPU. Here I just read the CSV file from the datasets folder, and then I drop all of the records with missing values using dropna and I drop records that are duplicates by invoking drop_duplicates. The setup function is where we perform data transformations and preprocessing and data splitting. It takes in the stage as an input argument. Now, the first thing I do in setup is drop those columns that are not relevant for prediction. The X features will not include exited. That's of course the target. We have to drop that. But we'll also not include row number, customer ID, and surname because these fields do not have any predictive power to figure out whether a customer churned or not. The Y value or the target of the classification is the exited
>
> **[1:39](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-lightningdatamodule?u=76281980&t=99)** column that I assign on Line 18. Now, I'll preprocess the data only if stage is equal to fit or stage is equal to none. That is only in the training phase. The training phase will get the training as well as validation data and preprocess them both. In the training stage on Lines 21 and 22, I first use train_test_split from scikit-learn to split the data, 80% for training, and the remaining 20% to evaluate the model. On Lines 24 through 37, we one-hot encode the categorical values present in the data. The only categorical features include geography and gender, and we instantiate the one-hot encoder and then use a column transformer to one-hot encode these values. On Lines 36 and 37 is where we actually perform the one-hot encoding by calling fit_transform on the training data and transform on the validation data. On Line 39, we convert the Y values to NumPy arrays, and on Lines 41 through 45, we use the standard scalar to standardize all numeric values. And finally, on Lines 47 through 51, we take our training as well as validation data and convert them all to PyTorch tensors. And then, of course, we have the train_dataloader function, which returns the DataLoader for the training data. We instantiate a tensor dataset and then we use that to instantiate a DataLoader.
>
> **[3:12](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-lightningdatamodule?u=76281980&t=192)** We load the data using four workers since I have four cores on my machine. And the code in the validation DataLoader is identical, we instantiate a tensor dataset with the validation data and use that to instantiate a DataLoader. Let's quickly check that our data module works just fine. I instantiate the bank_customer_churn data module, call prepare_data, and then set up. I'll now access the train_dataloader, and let's take a look at one batch of training data. Once again, we've used a batch size of eight. After all of the pre-processing that we've performed, let's see the number of features that we have in the input data. We access one record in the training data, use the shape property, and get the second dimension, which will give us a number of features. It's equal to 11. Remember, we need this information to set up the layers of our neural network model. Next, let's look at one batch of the validation data to make sure that the validation DataLoader is also working fine. And you can see that indeed it is. All that's left for us is to set up a lightning module with our model, and train the model using a lightning trainer object.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (2), [[Data Preparation]] (1)
> **Code Identifiers:** prepare_data (3), train_dataloader (2), drop_duplicates (1), train_test_split (1), fit_transform (1)
> **Prerequisites:** set up (5), setup (2)
> **Env Vars:** gpu (1), csv (1)
> **CLI Commands:** make (1)

#### [Creating a LightningModule](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-lightningmodule?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-lightningmodule?u=76281980&t=1)** We are now ready to define our [[PyTorch]] Lightning module, which will hold our model as well as have the steps required for training and validating our model. Here is the class that I have defined, LitBinaryClassificationModule. We are going to be performing binary classification, predicting whether customers churned or not. We inherit from the lightning module base class. The init method is where we set up our model. This is where we define the model architecture and its layers. Now, init takes in two input arguments, the number of features in the input data and the learning rate that I've set to 0.001. I'm using a smaller learning rate here. My model comprises of three linear layers again, Layer 1, Layer 2, and Layer 3. And each of the three linear layers has a ReLU activation. And these are initialized separately as Activation 1, Activation 2, and Activation 3. The final output is also a linear layer. This is the output layer on Line 12. The output of this layer will be a [[Probability]] score, and this is the probability score that we'll use to determine whether a particular customer churned or not. Once again, I call self.save_hyperparameters on Line 13. This will cause num features and learning rate both to be saved as members of the hparams object that can then be accessed by the optimizer. The forward function is where we define what a forward pass-through
>
> **[1:35](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-lightningmodule?u=76281980&t=95)** the model looks like. You can see we pass the input through the three layers, each layer has the ReLU activation, and finally, we pass the data through the output layer. This will give us the final predictions that we return. The training step defines the operations for a forward pass through the model for a single batch of data. The batch is passed in as an input argument, we access the X features and the Y variables, and then we do a forward pass through the model by invoking self on the input features. A forward pass through the model will output logits for classification. The term "logits" refers to the raw unnormalized prediction scores output by a model for each class or category. Since it's binary classification, we'll have just one logits score. Now, the loss function that we use for our binary classification model is the BCE with logits loss. BCE here stands for binary cross-entropy. The BCE with logits loss converts the raw logit scores into probabilities. It squashes the output logits to be probability values in the range zero to one. It then computes the binary cross-entropy loss, which is essentially computing the difference between two probability distributions. The probability distributions of the actual value versus the predicted values. On Line 30, we compute the BCE with logits loss for one batch of data,
>
> **[3:08](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-lightningmodule?u=76281980&t=188)** and we log that out as a training loss along with a progress bar. In the validation step, we perform predictions on one batch of validation data, so you can see the code on Lines 37 through 42 is the same. We compute logits and then compute the loss. However, here in the validation data, we want the actual predictions from the model. And we compute that by converting the raw logits scores to probability scores using the torch.sigmoid activation. This will give us probability scores in the range zero to one. torch.round of this probability score will give us the model prediction, zero for not exited, one for exited. Next, on Lines 46 through 49, we compute the accuracy as well as the F1Score of the classification model. The accuracy tells us how many predictions the model got right, but because we have a skewed dataset, the accuracy is not a great measure of how good this model is. The F1Score here represents a trade-off between precision and recall, metrics that are better suited to evaluate models trained on skewed data. Precision is the proportion of positive identifications the model got right, and recall measures of the positive identifications in the dataset, how many was the model able to correctly identify. In the validation step, we log out the validation loss, the validation accuracy, and the validation F1Score. The predict step just gives us the predictions from the model.
>
> **[4:44](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/creating-a-lightningmodule?u=76281980&t=284)** Here we just make a forward pass through the model. In configure_optimizers, you can see that we've set up an Adam optimizer. The Adam optimizer is an adaptive learning rate optimization algorithm, very popular and widely used in the real world.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Probability]] (8), [[PyTorch]] (1)
> **Env Vars:** bce (4)
> **Definitions:** refers to (1), stands for (1), is an  (1)
> **Code Identifiers:** save_hyperparameters (1), configure_optimizers (1)
> **CLI Commands:** make (1)
> **Versions:** 0.001 (1)
> **Prerequisites:** set up (1)

#### [Training a classification model and evaluating metrics](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-a-classification-model-and-evaluating-metrics?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-a-classification-model-and-evaluating-metrics?u=76281980&t=0)** Now that we've defined our lightning module, let's instantiate our classification module. We need to pass in the number of features. Here is what the layers of the model look like. We know that the training process of this model is very straightforward using a [[PyTorch]] trainer. Once again, I use a CSVLogger to log out the training metrics in a CSV file. I instantiate the data module on Line 3, the logger on Line 5, and the trainer, which will actually run the training and validation process on Line 7. We'll run for a maximum of 20 epochs of training and start the training process by calling trainer.fit. The trainer will identify that we don't have a GPU here on this machine, and it will automatically train the model using the CPU. Now, the training process took about 6 or 7 minutes to run. Here at the end of one epoch of training, validation accuracy is 0.819, so accuracy is high. The F1 score is just 0.268. I let this run through again for all 20 epochs. And at Epoch 19, let's take a look at the various course. Validation accuracy is 0.859, it has risen, the validation F1 score is 0.41, much better than before, but not great. Now for some reason, the validation F1 score doesn't display correctly here when we look at the output of the training of the model. We'll actually compute the accuracy, precision, recall, and F1 score for the validation data in just a bit.
>
> **[1:38](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-a-classification-model-and-evaluating-metrics?u=76281980&t=98)** Meanwhile, let's access the metrics for the training process of this model in the metrics.csv file under the training logs directory. In addition to the training and validation loss, we also logged out the validation accuracy and the validation F1 scores. Those additional metrics are also available here as part of the logs. Let's now go ahead and plot all of these metrics in a nice visualization, a Seaborn line plot. And here you can see four different lines corresponding to the four metrics that were tracked during the training process. The orange dotted line on top is the validation accuracy, the green dotted line is the validation F1 score, and the remaining two lines represent the training and validation losses, respectively. Next, I'll call model.predict on the validation data. So we get predictions from the model and we get them as a stacked tensor. So we have one tensor with all the predictions. The predictions here are in the form of raw unnormalized logits scores. In order to get these scores in the form of [[Probability]] values between zero and one, you need to apply the torch.sigmoid function and then torch.round will give us the actual predictions in the form of zero/one values. Let's get the actual values from the validation data. We'll have to compare the predictions from the model against these actual values. We need to call torch.stack on these labels as well in order to get them all in a single tensor.
>
> **[3:11](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/training-a-classification-model-and-evaluating-metrics?u=76281980&t=191)** Now that we have the predictions and the actual values, let's compute some evaluation metrics for the validation data. We'll compute accuracy, precision, recall, and the F1 score. Let's go ahead and hit "Shift-Enter" and we'll see how the model is. Accuracy is quite good at 0.859. Precision, which is the proportion of positive identifications of exited customers that the model got right is quite high, 0.785. The recall score is on the lower side, 0.464. Of the customers identified as exited or churned by the model, how many were actually right? This is what recall measures. And finally, we have the F1 score, which is the trade-off between precision and recall, and that's the 0.58. That's great. At this point, you've successfully built and trained a classification model using PyTorch Lightning.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (2), [[Probability]] (1)
> **Versions:** 0.859 (2), 0.819 (1), 0.268 (1), 0.41 (1), 0.785 (1)
> **Env Vars:** csv (1), gpu (1), cpu (1)
> **File Paths:** metrics.csv (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Summary and next steps](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/summary-and-next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-workshop-build-a-neural-network-with-pytorch-lightning-2023/summary-and-next-steps?u=76281980&t=0)** And this demo brings us to the very end of this AI workshop on building a neural network with [[PyTorch]] Lightning. Now, we started this course off with a quick overview of PyTorch and PyTorch Lightning, and we discussed how PyTorch Lightning allows us to write cleaner and more modular code for model training. We then got hands-on and we trained a regression model using the PyTorch APIs. We identified that there was a lot of boilerplate code here in the model building and training process, and we eliminated a lot of this boilerplate repetitive code in the next demo where we train the same regression model, but this time we used PyTorch Lightning. And then in order to get some more practice with PyTorch Lightning for model training, we built and trained a classification model using PyTorch Lightning. Well, this brings us to the very end of this AI workshop. If you're interested in [[Neural Networks]] and you want to study further, here are some other courses on [[LinkedIn]] learning you might want to watch. [[TensorFlow]]: Neural networks and working with tables and [[Hands-On PyTorch Machine Learning]] are both great courses that might be a good fit for you. Well, that's it from me here today. I hope you had fun in this AI workshop. Thank you for listening.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PyTorch]] (9), [[Neural Networks]] (2), [[LinkedIn]] (1), [[TensorFlow]] (1)
> **Exercise Files:** boilerplate (2)
> **Cross-References:** we discussed (1)


## Instructor

- [[Janani Ravi]]

## Resources

- Exercise files available

## Skills Covered

- PyTorch
- Artificial Intelligence (AI)
- Artificial Neural Networks

## Path Context

### In [[Advance Your Skills in Deep Learning and Neural Networks]]
← [[Deep Learning with Python- Convolutional Neural Networks]] | **4 of 8** | [[Deep Learning with Python- Optimizing Deep Learning Models]] →

## Appears In

- [[Advance Your Skills in Deep Learning and Neural Networks]]

## Related Courses

_Courses sharing skills:_

- [[AI Workshop- Build a Neural Network with PyTorch Lightning]] — Artificial Intelligence (AI), Artificial Neural Networks, PyTorch
- [[Hands-On PyTorch Machine Learning]] — Artificial Intelligence (AI), PyTorch
- [[AI Workshop- Hands-on with GANs Using Dense Neural Networks (2023)]] — Artificial Intelligence (AI), Artificial Neural Networks
- [[LinkedIn AI Academy AI-100- 2 Supervised Learning with Neural Networks]] — Artificial Intelligence (AI), Artificial Neural Networks
- [[Creating a Book Search Engine from Scratch Using Java and GitHub Copilot]] — Artificial Intelligence (AI)

---

[↑ Back to top](#)