---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: deep-learning-with-tensorflow-insights-and-innovations
url: "https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations"
duration_minutes: 186
duration: 3h 6m
level: Intermediate
updated: 10/9/2024
learners: 6680
skills:
  - TensorFlow
  - Artificial Intelligence (AI)
  - Deep Learning
exercise_files: true
github: "https://github.com/LinkedInLearning/deep-learning-with-tensorflow-insights-and-innovations-3835125/codespaces"
thumbnail: "https://media.licdn.com/dms/image/v2/D4D0DAQF3huhzfFmWxQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1727298293042?e=2147483647&amp;v=beta&amp;t=af9IfGCNoyKPJWP3Ac9Z2NwjY3ArNnirXXHptH9X7nQ"
linkedin_topic: Artificial Intelligence (AI)
learning_paths:
  - '[Advance Your Skills in Natural Language Processing](../../Paths/Artificial%20Intelligence%20(AI)/Learning%20Paths/Advance%20Your%20Skills%20in%20Natural%20Language%20Processing.md)'
prev_courses:
  - '[Building NLP Pipelines with spaCy](Building%20NLP%20Pipelines%20with%20spaCy.md)'
next_courses:
  - '[AI Workshop- Build a Neural Network with PyTorch Lightning](AI%20Workshop-%20Build%20a%20Neural%20Network%20with%20PyTorch%20Lightning.md)'
path_nav: '[{"path":"Advance Your Skills in Natural Language Processing","position":4,"total":6,"prev":"Building NLP Pipelines with spaCy","next":"AI Workshop- Build a Neural Network with PyTorch Lightning"}]'
path_count: 1
tags:
  - course
  - topic/artificial-intelligence
  - topic/software-development
  - skill/tensorflow
  - skill/artificial-intelligence-ai
  - skill/deep-learning
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Artificial%20Intelligence%20(AI)/Deep%20Learning%20with%20TensorFlow-%20Insights%20and%20Innovations.md)

![Deep Learning with TensorFlow: Insights and Innovations](https://media.licdn.com/dms/image/v2/D4D0DAQF3huhzfFmWxQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1727298293042?e=2147483647&amp;v=beta&amp;t=af9IfGCNoyKPJWP3Ac9Z2NwjY3ArNnirXXHptH9X7nQ)

# Deep Learning with TensorFlow: Insights and Innovations

> As the AI landscape continuously evolves, understanding the foundational and emerging aspects of deep learning becomes crucial for professionals looking to stay ahead in tech-related fields. This course helps to bridge that gap by offering updated insights on how to leverage the power of TensorFlow applications. Discover the latest features and best practices of TensorFlow with practical applicati

> [LinkedIn Learning](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations) | 3h 6m | Intermediate | 7K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (1 videos)
  - [Welcome to deep learning with TensorFlow](#welcome-to-deep-learning-with-tensorflow)
- [**1. Diving into Codespaces**](#1-diving-into-codespaces) (2 videos)
  - [Codespaces: Your ready-to-use workspace](#codespaces-your-ready-to-use-workspace)
  - [Setting up TensorFlow with Codespaces](#setting-up-tensorflow-with-codespaces)
- [**2. Understanding TensorFlow**](#2-understanding-tensorflow) (5 videos)
  - [TensorFlow essentials](#tensorflow-essentials)
  - [TensorFlow simplified for NumPy users](#tensorflow-simplified-for-numpy-users)
  - [Machine learning workflow](#machine-learning-workflow)
  - [Challenge: Normalizing tensors](#challenge-normalizing-tensors)
  - [Solution: Step-by-step statistical analysis](#solution-step-by-step-statistical-analysis)
- [**3. Building Your First Model**](#3-building-your-first-model) (8 videos)
  - [Let's create a TensorFlow model](#lets-create-a-tensorflow-model)
  - [Preprocessing and feeding data into your model](#preprocessing-and-feeding-data-into-your-model)
  - [Monitor training and validation](#monitor-training-and-validation)
  - [Success metrics](#success-metrics)
  - [Save and reuse trained models](#save-and-reuse-trained-models)
  - [Autoencoders: A gentle introduction to generative models](#autoencoders-a-gentle-introduction-to-generative-models)
  - [Challenge: My first TensorFlow model](#challenge-my-first-tensorflow-model)
  - [Solution: Building blocks to TensorFlow mastery](#solution-building-blocks-to-tensorflow-mastery)
- [**4. Mastering TensorBoard**](#4-mastering-tensorboard) (2 videos)
  - [Visualizing success with TensorBoard](#visualizing-success-with-tensorboard)
  - [Deep dives into training metrics](#deep-dives-into-training-metrics)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Welcome to deep learning with TensorFlow](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/welcome-to-deep-learning-with-tensorflow?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/welcome-to-deep-learning-with-tensorflow?u=76281980&t=0)** - Ready to turn your ideas into AI powered reality? We are going to take [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) and transform the way you think about data and machine learning. Whether it's setting up your workspace in [Codespaces](../../Skills/Software%20Development/Codespaces.md), or building the models that can predict the future, or housing prices in this case, this course is designed to give you the skills to make it happen with [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), TensorFlow, and some of the most other powerful libraries like NumPy and Matplotlib, we will be creating, training, and fine-tuning models that deliver results. I'm Isil, Founder of DigiFab AI, and a [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Top Voice. Together, we are going to master TensorFlow and turn your data into gold.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (3), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** make (1), python (1)
> **Speakers:** - ready (1)


### 1. Diving into Codespaces

[↑ Back to Table of Contents](#table-of-contents)

#### [Codespaces: Your ready-to-use workspace](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/codespaces-your-ready-to-use-workspace?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/codespaces-your-ready-to-use-workspace?u=76281980&t=0)** - [Instructor] In this course, we are leveraging [GitHub](../../Skills/Software%20Development/GitHub.md) [Codespaces](../../Skills/Software%20Development/Codespaces.md) to give you a cloud-powered development environment. This tool makes coding more flexible and accessible than ever before. Whether you prefer working directly from Visual Studio Code or through a browser-based editor, Codespaces has got you covered. Let's explore how you can harness this tool to enhance your learning experience. First, access the repository by signing into your GitHub account, and navigating to the course repository containing all the materials you'll need. Once there, locate the code tab on the repository page. It's typically near the ED file tab on the upper right corner. Click Code, find Codespaces, and then click the plus sign to create a new codespace. After a brief setup, a browser-based editor will launch, providing you with a fully functional development environment right at your fingertips. If you have already created a codespace previously, it will show up down below. Just click on the three dots, and then select the Open in Browser option. You can also choose to open it in Visual Studio Code for a desktop experience if that's your preference. Once the Codespaces Web Version is ready, it will look something like this. You can also open it in Visual Studio Code Desktop by clicking on the Codespaces
>
> **[1:32](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/codespaces-your-ready-to-use-workspace?u=76281980&t=92)** on the lower left corner of the webpage, and then selecting Open in Visual Studio Code Desktop. This gives you the flexibility to work in whichever environment you are most comfortable with. Now, let's take a look at the folder structure. Within the SRC folder, let's go ahead and expand it. You will find the exercise files neatly organized by chapter. Each coding lesson starts with a begin.py and ends with an end.py. If it's a challenge and a solution pair, it will say challenge and solution respectively. You will run your code using the built-in terminal right below. So, it's doing its final setup as we see in the terminal. So, for those of you working on a mobile device or who don't have access to the Exercise Files, don't worry. You can still follow along and absorb the material during the walkthroughs. In addition to the src folder, we also have the output folder where the artifacts produced by your code will be stored. This structure is designed to keep everything organized, so you can focus solely on learning and applying what you learned. In addition to these folders, we also have the log folder, which we save models to then visualize in TensorBoard. So, after going over these folders,
>
> **[3:05](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/codespaces-your-ready-to-use-workspace?u=76281980&t=185)** we pretty much covered the organization here. So, let's get coding. We will start with the begin.py files and then progress through the lessons, transforming your code into the end.py files. This transition from the initial to the final state of each script will mirror your growth in understanding [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) with [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md). GitHub Codespaces is a leap toward modern, convenient, and collaborative coding education. It eliminates the setup barriers, allowing you to focus entirely on learning and application. Ready to start? Let's code, create, and launch into the cloud.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Codespaces](../../Skills/Software%20Development/Codespaces.md) (6), [GitHub](../../Skills/Software%20Development/GitHub.md) (3), [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (1), [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) (1)
> **Tools:** visual studio (4), github (3), terminal (2)
> **File Paths:** begin.py (2), end.py (2)
> **Prerequisites:** setup (3), you'll need (1)
> **CLI Commands:** find (2)
> **UI Navigation:** click on (1), select the (1)
> **Exercise Files:** exercise files (2)
> **Env Vars:** src (1)

#### [Setting up TensorFlow with Codespaces](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/setting-up-tensorflow-with-codespaces?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/setting-up-tensorflow-with-codespaces?u=76281980&t=0)** - [Lecturer] In this session, we will be setting up [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) using [GitHub](../../Skills/Software%20Development/GitHub.md) [Codespaces](../../Skills/Software%20Development/Codespaces.md), which simplifies the process significantly. So the beauty of Codespaces is that it automatically configures your environment, thanks to the requirements.txt file included in the course repository. So here, I have placed the requirements.txt file under the src folder. So let's go ahead and expand the src folder. Scroll down and then you will see the requirements.txt file. So once you create or open your codespace, the setup process will begin automatically. The requirements.txt file contains all the necessary packages, including TensorFlow, and Codespaces will handle the installation for you. You will see the setup logs in the terminal below, as everything is configured, ensuring you are ready to dive into the coding exercises. After the setup completes, TensorFlow is ready to go in your codespace. Whether you are running your code directly in the browser-based editor, like we have open right now, or in Visual Studio Code on your desktop by clicking the left pane on the Codespaces and then choosing Open VS Code Desktop, and then it will give you a prompt and it will say you can close this web browser right now. And you are going to go ahead and extend GitHub Codespaces to open this URI. So regardless of your environment, whether it's web or the desktop version,
>
> **[1:33](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/setting-up-tensorflow-with-codespaces?u=76281980&t=93)** your development environment will be fully equipped to handle all TensorFlow-related tasks. So no need to worry about manual installations or configuration hiccups. Codespaces takes care of it all. This seamless setup allows you to focus on what matters most. Learning and coding. Ready to explore TensorFlow? Let's dive in and start experimenting with powerful machine-learning models right within your Codespace.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Codespaces](../../Skills/Software%20Development/Codespaces.md) (6), [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (5), [GitHub](../../Skills/Software%20Development/GitHub.md) (2)
> **Tools:** github (2), terminal (1), visual studio (1), vs code (1)
> **File Paths:** requirements.txt (4)
> **Prerequisites:** setup (4)
> **Env Vars:** uri (1)
> **UI Navigation:** scroll down (1)
> **Speakers:** - [lecturer] (1)


### 2. Understanding TensorFlow

[↑ Back to Table of Contents](#table-of-contents)

#### [TensorFlow essentials](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/tensorflow-essentials?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/tensorflow-essentials?u=76281980&t=0)** - [Instructor] In this session, we are going to explore how you can become a master chef in the world of [Neural Networks](../../Skills/Artificial%20Intelligence%20(AI)/Neural%20Networks.md) using [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md). Think of Keras as your sauce chef, making things easier. But we are also going to dive into the kitchen ourselves and get our hands dirty with TensorFlow's lower-level API for more control and customization. Why use TensorFlow's lower-level API? Think of Keras as your sauce chef, handling the basics so you can focus on creating great recipes or [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) models. Keras simplifies model building, but sometimes you need more control, like adding a secret ingredient or adjusting the cooking time. This API allows you to write custom loss functions, metrics, initializers, and more. So think of it as like having the freedom to tweak every aspect of your dish to perfection. So let's go over and do a quick tour of TensorFlow. So think of TensorFlow as like a high-powered kitchen appliance, versatile and efficient for numerical computations and machine learning tasks. It's developed by [Google](../../Glossary/Service/Google.md)'s Brain Team. It's the secret sauce behind many of Google's services, like Google Photos and search. So TensorFlow has become a staple in the deep learning community, much like a go-to cookbook in a chef's kitchen. So what are some of TensorFlow's futures?
>
> **[1:33](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/tensorflow-essentials?u=76281980&t=93)** First of all, similarity to NumPy. If you have used the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) library NumPy, TensorFlow will feel familiar, but with the added power of GPU support. So keep in mind at the time of this recording, [Codespaces](../../Skills/Software%20Development/Codespaces.md) do not support GPU. So for the sake of this class, we won't be utilizing GPU, but just know that if you are utilizing TensorFlow in your local environment, and if you do have access to a GPU support, TensorFlow is very similar to NumPy with added GPU support. So imagine NumPy as your basic kitchen tools and TensorFlow as those same tools with turbo boosts. Let's talk about distributed computing. It's like having multiple cooks in the kitchen, each handling different tasks simultaneously across multiple devices. This ensures your complex meal or your deep learning model gets done faster and more efficiently. So let's look at just-in-time compiler next. This feature optimizes your computations for speed and efficiency like a turbocharged blender that prepares everything in no time. So next, let's look at computation graphs. These graphs allow you to export and run your computations in different environments from Python to [Java](../../Skills/Software%20Development/Java.md) on [Android](../../Glossary/Sdk/Android.md). It's like having a recipe that works perfectly, whether you are cooking at home or in a professional kitchen. Next, reverse mode differentiation. It's a fancy [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md),
>
> **[3:06](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/tensorflow-essentials?u=76281980&t=186)** but this helps in optimizing your recipes, in other words, models by efficiently computing gradients. Think of it as having a culinary GPS that guides you through the exact steps needed for the perfect dish. So let's look at TensorFlow Python API. First of all, it includes high-level deep learning APIs. Tf.keras makes model building very straightforward and intuitive. It's like a ready-made mix where you just add water and that's it, you have a cake. Next is low level deep learning API. Tf.nn and tf.GradientType give you the fine control needed for custom operations. It's like having access to individual spices and herbs to create your own unique blend. Next, let's look at mathematical operations. Libraries like tf.math, tf.linalgebra, which is linalg, and tf.signal are very essential tools for your mathematical needs. So think of them like your measuring spoons, mixers, and whisks for precise cooking. Next is visualization. Tf.summary integrates with TensorBoard for visualizing your models and their performance. This is like presenting your dish on a beautiful platter to impress your guests. Next up is TensorFlow architecture. At its core, TensorFlow's execution engine ensures efficient operations across multiple devices
>
> **[4:43](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/tensorflow-essentials?u=76281980&t=283)** like a well-coordinated kitchen staff. It can handle tasks on CPUs, GPUs, and even TPUs for faster performance. It supports various languages, including Python, C++, Java, and [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md), making it a versatile tool in your toolkit. This is like having a multi-language cookbook that works anywhere and everywhere. Next, TensorFlow Lite allows you to run models on mobile devices, taking your culinary creations on the go, like a food truck bringing your gourmet meals to the streets. So to conclude, TensorFlow's high level API, Keras, is like your trusty sauce chef. It's great for quick and easy meals, but when you need to prepare a gourmet feast, TensorFlow's lower-level API gives you the control to customize every aspect of your model. This allows you to fine-tune your creations and optimize them for specific tasks. The distributed computing feature is like having a team of sauce chefs in the kitchen, each working on different parts of the meal to ensure everything is perfectly timed and cooked. The just-in-time compiler optimizes your computations, ensuring your models run efficiently and quickly. So with TensorFlow's flexible architecture, you can export and run your computation graphs in different environments, ensuring consistency and performance
>
> **[6:16](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/tensorflow-essentials?u=76281980&t=376)** no matter where you are. This makes TensorFlow a powerful and versatile tool in the world of machine learning.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (19), [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) (5), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (4), [Google](../../Glossary/Service/Google.md) (3), [Java](../../Skills/Software%20Development/Java.md) (2)
> **Env Vars:** api (7), gpu (5), gps (1)
> **Analogies:** it's like (4), think of it as (2), similar to (1), imagine (1)
> **CLI Commands:** python (4)
> **Definitions:** in other words (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [TensorFlow simplified for NumPy users](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/tensorflow-simplified-for-numpy-users?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/tensorflow-simplified-for-numpy-users?u=76281980&t=0)** - [Instructor] In this session, we will explore how to use [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) like NumPy. TensorFlow's API revolves around tensors, which are essentially multi-dimensional arrays similar to NumPy's ndarray. These tensors are crucial for creating custom cost functions, metrics, custom layers and more. You can create a tensor using tf.constant. For example, tf.constant, and then you insert the elements of the tensor. This creates a 2x3 matrix of floats. Scalars can also be created like tf.constant and you input the scaler, like 42 in our case. Tensors have properties such as shape and data type, which you can access using t.shape and t.dtype. Indexing tensors works much like NumPy. You can use slicing, for example, t open parenthesis, square brackets, colon dot one, two colon. So this defines which part of the tensor you would like to access to using the indexing function. Tensor operations are also very straightforward, such as adding 10 to a tensor, t plus 10. This is equivalent to using tf.n t comma 10. Other operations like tf.square and tf.transpose are also available. TensorFlow provides all the basic math operations you need, such as tf.add, tf.multiply,
>
> **[1:35](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/tensorflow-simplified-for-numpy-users?u=76281980&t=95)** and tf.square. These are equivalent to NumPy functions like np.add and np.sum. So there are some key differences we should be pointing out here. For example, transposing a tensor requires tf.transpose instead of t.T in NumPy. Additionally, TensorFlows reduce operations like tf.reduce_sum and tf.reduce_max may have slight variations in precision compared to NumPy. Keras also has a low level API located in keras.backend. Functions like square, exp and sqrt are available. These functions typically call the corresponding TensorFlow operations. If you want your code to be portable across Keras implementations, use these functions. For example, using Keras' backend, you can write k.square (K.transpose(t)) + 10. Tensors play nicely with Numpy. You can create a Tensor from a NumPy array using tf.constant open parenthesis np.array, and you insert the array. Converting Tensors back to Numpy arrays is simple with t.numpy or np.array(t). You can also apply TensorFlow operations directly on NumPy arrays, such as tf.square, open parenthesis np.array, and you insert the array.
>
> **[3:11](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/tensorflow-simplified-for-numpy-users?u=76281980&t=191)** Type conversions can significantly affect performance. TensorFlow does not perform automatic type conversions. Use tf.cast when necessary. For example, tf.constant(40.,dtype=tf.float64) can be cast using tf.cast(t2, tf.float32). Variables in TensorFlow are like constant Tensors, but they can be modified. Use tf.variable for values that change during training. Variables can be updated using methods like assign, assign_add and more. For example, you can double the values in a variable V using v.assign(2*v). TensorFlow supports several other [Data Structures](../../Skills/Software%20Development/Data%20Structures.md). For example, Sparse Tensors, which we can access by typing tf.SparseTensor represent Tensors with many zeros efficiently. Next, Tensor arrays. tf.TensorArray. These are list of Tensors. Next is Ragged tensors. We access it by typing tf.RaggedTensor. They represent lists of tensors with varying lengths. Next is String Tensors, tf.string. They handle byte strings and Unicode strings. Sets and queues are also supported for more advanced data operations. With these operations, variables
>
> **[4:44](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/tensorflow-simplified-for-numpy-users?u=76281980&t=284)** and data structures at your disposal, you are now ready to customize your models and training [Algorithms](../../Skills/Software%20Development/Algorithms.md) using TensorFlow in ways that feel familiar if you come from a NumPy background.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (9), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (2), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1)
> **Analogies:** for example (7), such as (4), similar to (1)
> **Code Identifiers:** reduce_sum (1), reduce_max (1), assign_add (1)
> **Env Vars:** api (2)
> **Speakers:** - [instructor] (1)

#### [Machine learning workflow](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/machine-learning-workflow?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/machine-learning-workflow?u=76281980&t=0)** - [Instructor] Now that we have covered [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) and its simplified interface for NumPy users, it's now time to dive into the machine learning workflow. Think of it as your recipe book for crafting successful ML projects, ensuring you have a structured approach to tackle any task from appetizers to main courses. So let's start with the big picture, the machine learning project checklist. This is your roadmap to success. First, frame the problem and look at the big picture. Next, get the data. Follow by explore the data to gain insights. Then prepare the data for machine learning [Algorithms](../../Skills/Software%20Development/Algorithms.md). Then explore different models and shortlist the best ones. After that, fine tune your models and combine them into a great solution and present your solution. Finally, launch, monitor, and maintain your system. Remember, this checklist can be adopted to suit your specific needs and situation like swapping out ingredients based on what's in the season. The first step in the checklist is framing the problem. This involves gathering information from stakeholders and understanding the business objective. For example, if we want to predict a district's median housing price, we need to know why this prediction is important. Defining the objective helps determine the algorithm's performance measures, and effort required. It's like knowing the destination before starting a journey,
>
> **[1:37](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/machine-learning-workflow?u=76281980&t=97)** or in our metaphorical case, knowing the dish we want to prepare before gathering ingredients. Next, we need to get the data. In our example, we will use the California housing prices dataset based on the 1990 California census. In this particular case, it doesn't matter that it's a little bit of a older dataset. It serves the purpose perfectly for us to learn [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) with TensorFlow. This dataset has been slightly modified for instructional purposes. Remember, understanding your data source is crucial for any machine learning project, much like knowing where your ingredients come from before you start cooking. Moving on to exploring the data. Once we have the data, we explore it to gain insights. Using [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) libraries like [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md), we can look at the first few rows with the head function, and then we can get an overview with the info function and check [Statistics](../../Skills/Data%20Science/Statistics.md) with the describe function. Visualizing data with histograms and scatter plots helps identify patterns and anomalies. This step is like examining the ingredients before cooking, ensuring everything is fresh and ready to go. Now let's prepare the data. Cleaning the data is essential. For instance, handling missing values with scikit-learn's SimpleImputer. Categorical data needs encoding, which can be done using OrdinalEncoder or OneHotEncoder.
>
> **[3:15](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/machine-learning-workflow?u=76281980&t=195)** Categorical encoding is nothing but a fancy way of saying changing the labels and categories into numbers, which machine learning algorithms can understand. Future scaling ensures all numerical attributes are on the same scale, using tools like StandardScaler. Therefore, if we have a relatively huge number and we have a very small number, those differences does not impact the machine learning algorithm negatively. This preparation sets the stage for effective machine learning, like marinating the meat or chopping vegetables before cooking. With our data ready, we move to selecting and training a model. We start with simple models like [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) to get a baseline. Then try more complex models like Decision Tree regressor or Random Forest Regressor. Use cross validation with scikit-learn's cross_val_score function to evaluate models. This is like trying different recipes to find the best one, ensuring your final dish is delicious. So after selecting a model, [Fine Tuning](../../Skills/Software%20Development/Fine%20Tuning.md) is crucial. We use GridSearchCV to explore hyper parameter combinations to see which combinations are performing the best to be able to select the best ones or we use RandomizedSearchCV for large spaces. We also use ensemble methods like bagging, boosting, or stacking, which can further improve performance
>
> **[4:49](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/machine-learning-workflow?u=76281980&t=289)** by combining models. Think of this step as adjusting the seasoning to perfect your dish, ensuring the flavors are just right. Once your model is fine tuned, it's now time to present your solution. Summarize your findings, highlight what worked and what didn't, and clearly state any assumptions and limitations. Use clear visualizations and memorable statements. For example, median income is the number one predictor of housing prices. Presentation is key to making your work understandable and impactful, like plating your dish beautifully before serving. Finally, we launch, monitor and maintain the system. Deploy our model using suitable tools and platforms. Continuously monitor its performance and retrain as necessary to ensure ongoing accuracy. Maintaining the system is like keeping a car running smoothly with regular checkups and maintenance. So following a structured machine learning workflow helps you manage complex projects much more efficiently. From framing the problem to launching and maintaining the system, each step is crucial for building robust and reliable models. Keep refining your process and adopt a checklist to your needs for successful machine learning projects. Remember, this is an example of a general machine learning workflow. When using TensorFlow, the workflow will be similar, but with specific tools and methods
>
> **[6:23](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/machine-learning-workflow?u=76281980&t=383)** suited to TensorFlow's framework. Understanding both the general workflow and the specifics of TensorFlow will give you a comprehensive toolkit for tackling a wide range of machine learning projects. Happy coding!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (5), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (2), [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **Analogies:** picture (2), for example (2), think of it as (1), it's like (1), for instance (1)
> **CLI Commands:** python (1), find (1)
> **Code Identifiers:** cross_val_score (1)
> **UI Navigation:** select the (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Normalizing tensors](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/challenge-normalizing-tensors?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/challenge-normalizing-tensors?u=76281980&t=0)** - [Instructor] In this section, we are going to dive into a hands-on coding challenge that will help solidify our understanding of [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) operations. So the focus here will be on normalizing tensors and computing basic [Statistics](../../Skills/Data%20Science/Statistics.md) like mean and standard deviation. So let's open up the code spaces on the web browser, and you will see a screen like this. Then on the left pane, let's go ahead and enlarge the SRC folder, and open up the 02_04_challenge [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) file. So we will approach this particular challenge, just like a homework assignment, and then we will walk through the solution together. This exercise will give you practical experience in manipulating tensors and using TensorFlows to map functions. So let's go ahead and minimize the left pane by clicking the Explorer, and you will see that the left window will disappear so we can see more of the code. So let's start with our challenge. Imagine you are given a tensor, a two dimensional array of numbers, and that's filled with random integers, between zero and nine. So your task is to normalize this tensor so that all the values fall between zero and one. Again, let's remember why we do that, because very different values in our dataset, affect the machine learning models negatively. So by normalizing the values between zero and one,
>
> **[1:35](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/challenge-normalizing-tensors?u=76281980&t=95)** we introduce some sort of a standardization in our data so that our machine learning algorithm performs better. So our task here is to normalize this tensor so that all the values fall between zero and one, but that's not all. After normalizing the tensor, you will also need to calculate the mean and standard deviation of the values in this normalized tensor. So in this challenge code, we have the normalized tensors as none. So you are going to go ahead and replace it with your code that has the values between zero and one. Then you are going to go ahead and compute the mean and standard deviation of the normalized tensor. So right now, we have a placeholder, which is mean equal to none, and standard deviation equal to none. So these will be replaced with your code. So let's break down what this challenge is asking us to do. Let's go ahead and run this challenge code first by finding the little three angle on the upper right, and let's enlarge the terminal window. The warnings are okay for this case, and they indicate that we are not using all of the functions TensorFlow provides, and that's all right. So when we break down what this challenge is asking us to do, number one is the tensor creation. So we start by creating a four by four TensorFlow filled with random integers. We can see an example in the terminal window right now. So the TensorFlow tensor filled with random integers, in this case, and it's four by four,
>
> **[3:08](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/challenge-normalizing-tensors?u=76281980&t=188)** the integer range from zero to nine. But here's a key point. The tensor is created as an integer tensor, and TensorFlow operations are sensitive to data types. Next is normalization, as we can see in the printout that we have created in the terminal. So normalized tensor is not calculated yet. The challenge asks us to normalize the tensor values, so they are all in between zero and one. To do this, we will need to scale the values by dividing them by the maximum possible value, which, in this case, is nine. However, since we are dividing, we need to ensure our tensor is of a floating point type, which means the number has the decimal points. Otherwise, we will run into issues because dividing integers, might not give us the results we expect. After normalizing the tensor, we need to compute two statistical measures. What are those? They are the mean, which gives us the average value of the tensor elements, and the standard deviation, which tells us how much the values deviate from the mean. So right now, the code skeleton has placeholders where your solution would go. They are marked by none. So your challenge is to fill in these gaps. Let's go ahead and give it a try.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (5), [Statistics](../../Skills/Data%20Science/Statistics.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **Tools:** terminal (3)
> **Analogies:** just like (1), imagine (1)
> **CLI Commands:** python (1)
> **Env Vars:** src (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Step-by-step statistical analysis](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-step-by-step-statistical-analysis?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-step-by-step-statistical-analysis?u=76281980&t=0)** (upbeat instrumental music)
>
> **[0:06](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-step-by-step-statistical-analysis?u=76281980&t=6)** - [Instructor] Now, let's walk through the solution together and see how we can solve this challenge. First, we are going to continue from our challenge code, and the end code will be stored in 02_05_solution.py. So, if we would like to open this up from the beginning it will look something like this. And if you are just opening this up the terminal may be also clean. So, now let's walk through how to solve this challenge. We will first start again from our challenge code. Find the src folder from the left pane and click on 02_04_challenge [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) file. First of all, we will go through the solution here, and the completed code will be available in 02_05_solution Python file. So, first we need to ensure that the tensor is in a floating point format before we try to normalize it. This is crucial because dividing integers might not yield the correct normalized values, and here is how we can do it. So, we are going to go ahead and continue on after creating a [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) tensor with shape of four by four, we will start by saying cast the tensor to float before normalization. And then let's go ahead and do that, so we will say tensor equal to, we will use tf.cast, and then we will have tensor comma tf.float32.
>
> **[1:50](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-step-by-step-statistical-analysis?u=76281980&t=110)** So, this will take care of creating a float before normalization. Next, we are going to go ahead and normalize the tensor to have values between zero and one. So, for that we can remove the task and hint from the challenge and start normalizing it. So, normalized_tensor will be, in this case, equal to tensor divided by 9.0. So, to normalize the tensor we divide all elements by the maximum value in our original range, which was 9.0. So, after taking care of that we can now compute the mean and the standard deviation as they were asked from us in the challenge. So, let's go ahead and take care of those by using TensorFlow built-in functions. So, we are going to go ahead and start with mean. So, we will remove the task here and compute the mean and standard deviation of the normalized tensor. So, to do that we will use a built-in function called tf.reduce_mean, and then we will give the normalized tensor as a input. And done, we took care of the calculation of the mean. Now, next we will go ahead and calculate the standard deviation. So, this was compute the mean of the normalized tensor, and this is the compute the standard deviation of the normalized tensor next.
>
> **[3:25](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-step-by-step-statistical-analysis?u=76281980&t=205)** So, let's go ahead and do that. And again, we will use a built-in function for that, so standard deviation will be equal to tf.math.reduce_std.
>
> **[3:43](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-step-by-step-statistical-analysis?u=76281980&t=223)** So, this will give us the standard deviation, and we will input the normalized tensor here. So, that's it, it will take care of our solution. So, you can go ahead and run this here, or you can choose to go to the solution file, which will be the same solution like we walked through from the challenge code, and you can go ahead and run it. Let's go ahead and enlarge the terminal screen here. And here, let's walk through what this solution does. First of all, we start by tensor casting. We begin by casting our integer tensor to a floating point tensor. This step is critical because it allows us to perform the division needed for normalization without losing the precision. So, we did this casting here, which is tensor equals to tf.cast tensor comma tf.float32. This step is critical because it allows us to perform the division needed for normalization without losing precision. Next, we did the normalization, so we normalize the tensor by dividing each element by 9.0. This scales all values to fall between zero and one, which is what we want. Next, we went and calculated the mean and the standard deviation. When we calculate the mean and standard deviation using TensorFlow's reduce_mean and reduce_std functions it became really straightforward because we took advantage of the built-in functions. These functions efficiently compute the required [Statistics](../../Skills/Data%20Science/Statistics.md)
>
> **[5:20](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-step-by-step-statistical-analysis?u=76281980&t=320)** across the entire tensor. And there you have it. The solution effectively meets the challenge requirements and demonstrates important TensorFlow operations. So, we can go ahead and review the printouts in the terminal. First, we are printing the original tensor we created. Then we went ahead and printed the normalized tensor, which has the values between zero and 9.0. And then we went ahead and printed the calculated mean, followed by standard deviation that we can see in the terminal. So, during this exercise we learned how to normalize tensors and compute basic statistics like the mean and standard deviation. We did all of that using TensorFlow. So, these kinds of hands-on challenges are excellent for reinforcing the concepts we have covered and gives you practical coding experience. Remember, mastering TensorFlow is all about practice. The more you play around with these tensors and functions the more comfortable you will become. Keep experimenting, and I will see you in the next session where we will explore even more advanced topics.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (6), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2), [Statistics](../../Skills/Data%20Science/Statistics.md) (2)
> **Code Identifiers:** reduce_mean (2), reduce_std (2), normalized_tensor (1)
> **Versions:** 9.0 (4)
> **Tools:** terminal (4)
> **CLI Commands:** python (2), find (1)
> **UI Navigation:** click on (1), go to (1)
> **File Paths:** 02_05_solution.py (1)
> **Cross-References:** in the next (1)


### 3. Building Your First Model

[↑ Back to Table of Contents](#table-of-contents)

#### [Let's create a TensorFlow model](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/let-s-create-a-tensorflow-model?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/let-s-create-a-tensorflow-model?u=76281980&t=0)** - [Instructor] In this session, we will take our first step into building a [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) model from scratch. We will go through the entire process. We will simplify it. That way we can start from the understanding of the problem to creating and training the model and understanding how we can go from A to Z. So let's go ahead and open up the code spaces environment. And before we dive into the code, let's understand what we are doing here and what's the goal. So here we are trying to solve a simple regression problem. The aim here is to predict a continuous target variable based on some input features. For this example, we will use the California housing data set, where the goal is simple. We want to predict the median house value for California districts given several features like median income, population, and housing occupancy. So let's go ahead and look at the left pane here and find the SRC folder. And then once you find it, enlarge it and click on the 03_01_begin [python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) file. Before we start, let's go over the libraries that we have available here that we are utilizing. First we are calling the SK learn dot datasets and we are importing [Fetch](../../Skills/Web%20Development/Fetch.md) California housing. So this is something we do that allows us to load the California housing dataset later on in the code,
>
> **[1:34](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/let-s-create-a-tensorflow-model?u=76281980&t=94)** which is a regression dataset where the goal is to predict, again, the housing prices. Next we go ahead and from skLearn model selection, we import train test split function, which is a utility to split the dataset into training, validation, and test sets. Next, we go ahead and import standard scaler from SK Learn pre-processing. And standard scaler is nothing but a tool to standardize features by removing the mean and scaling to unit variance. Next we go ahead and import TensorFlow as TF, which is a open source library for machine learning and [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) and is the main focus of our course. Now let's go ahead and load the data and split the datasets. We start with loading the California housing dataset into a variable called housing, by saying housing equal to fetch California housing, open and close parenthesis. Next we go ahead and use the train test split here and we input housing that data, housing that target, and then we give it random state 42, which splits the dataset for us into a full training set and a test set. Next we use train test split again. This time, we further split the full training set into a smaller training set and a validation set. Next we move on and we scale the futures. We use a scaler called StandardScaler.
>
> **[3:07](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/let-s-create-a-tensorflow-model?u=76281980&t=187)** This initializes the StandardScaler object. Then we go ahead and use the fit transform function. This fits the scaler to the training data and transforms it, scaling the features to have mean zero, and variance one. Then we go ahead and use the scaler dot transform on X validation data. We call it X underscore valid. And then we use the same for X test data. We use scaler dot transform on X test and we call it X underscore test. So this transforms the validation and test sets using the same scaler fit to the training data. This ensures that all data sets are scaled in the same way. Next, we will start and build a TensorFlow model here. To make some space, we can minimize the left window by clicking the explorer icon here. So it'll give us more horizontal space here to work with. Let's go ahead and define the model as TF dot keras dot sequential. That's a model that's a linear stack of layers. Then we go ahead and fill this in as follows. So we will go ahead and add a densely-connected and fully-connected layer with 30 neurons and a relu activation. So let's go ahead and do that. We will take advantage of keras here dot layers dot dense, and then we will give 30 here which has 30 neurons.
>
> **[4:42](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/let-s-create-a-tensorflow-model?u=76281980&t=282)** And then we will use the activation function of relu. Let's go ahead and define the activation function and then we will say input shape equals to X underscore train, and then we will give it a shape which specifies the input dimension. In other words, the number of features here. So we will go ahead and say one colon, close parenthesis. Next we will add an output layer with just one neuron. So we will do that by saying TF dot keras dot layers dot dense. And then, give it one to indicate that we have an output layer with one neuron which is suitable for regression tasks, which is predicting a single continuous value, which is the housing prices here. So once we define the model, next what we do, we go ahead and compile it. So for that we say, model dot compile and we will give it loss equal to mean squared error. And we are going to go ahead and assign the optimizer to be the stochastic gradient descent, SGD. Next we are going to go ahead and create the model with history equal to model dot fit. And then we are going to give it to X train, Y train. We are going to give 20 epochs here to have 20 iterations.
>
> **[6:18](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/let-s-create-a-tensorflow-model?u=76281980&t=378)** Next, we are going to give the validation data to be X underscore validation, Y underscore validation. And we are all set. We just created the history by model dot fit. And next we are going to go ahead and evaluate the model. We are going to assign MSE test, mean squared error test, model dot evaluate, and then we are going to give X underscore test, Y underscore test. It's always a good practice to go ahead and print what the test result says. So, mean squared error on the test set. And next we are going to go ahead and call that from the upper bow that we just created. So what we did here, we went ahead and imported the necessary libraries. Then we fetched the California housing data. We did the test train splits, we did the standardization, and then we went ahead and standardized our data. We standardized X underscore train, X underscore valid, X underscore test. Then we went ahead and created the model here. The model has one input layer and one output layer. Next, we compiled the model. We gave the mean squared error loss function as well as stochastic gradient descent optimizer. Then we went ahead and trained the model here by model dot fit.
>
> **[7:50](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/let-s-create-a-tensorflow-model?u=76281980&t=470)** Next, we evaluated the model by mean squared error. Finally, we printed it. So this should be the same as zero three underscore zero one underscore and python file. So let's enlarge the left pane here and continue on from the end file next. And then simply run by clicking the little triangle on the upper right window. So it'll go through 20 iterations as we defined it to go. So after that, it will go ahead and give us the print out of means squared error as we told it to in the function. So the mean squared error for this problem is 0.36. So in this video, we take our first steps into building a TensorFlow model from scratch. Let's move on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (4), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2), [Fetch](../../Skills/Web%20Development/Fetch.md) (2), [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) (1)
> **CLI Commands:** find (2), python (2), make (1)
> **Definitions:** is a  (3), in other words (1)
> **Env Vars:** src (1), sgd (1), mse (1)
> **Code Identifiers:** sklearn (1)
> **Versions:** 0.36 (1)
> **UI Navigation:** click on (1)
> **Best Practices:** good practice (1)

#### [Preprocessing and feeding data into your model](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=0)** - [Instructor] Let's move on to the next step in our journey with [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md). In this section, we will dive into the critical process of data pre-processing. Pre-processing is the hero in any machine learning workflow. It ensures that your data is in the best possible shape before it's fed into your model, which directly influences the quality of your model's predictions. As they say, garbage in, garbage out. So pre-processing is the golden step in any machine learning workflow. Let's get started by exploring the dataset we will be working with. Then we will move on to scaling, visualizing, and preparing it for our model. First of all, let's open up [Codespaces](../../Skills/Software%20Development/Codespaces.md) and then find the src folder, and click on the 03_02_begin.py. So this is where we will begin coding and we will end up in 03_02_end.py. So let's go ahead and start with importing libraries here. So we will have to have [pandas](../../Skills/Software%20Development/Pandas%20(Software).md), numpy, matplotlib, seaborn, sklearn, and tensorflow. So our task here is to see what's missing and start adding them one by one. So let's go ahead and start with Pandas, import pandas as pd, and then import numpy as np. Then we will go ahead and import the visualization libraries such as matplotlib, and then we will need pyplot to be particular,
>
> **[1:36](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=96)** then we will call it plt. Next we will import the other visualization library, that is Seaborn, that is built on top of Matplotlib, and then we will call it as sns. Then we have sklearn.datasets, import fetch_california_housing. Then we have sklearn.model_selection, import test_train_split. Then we have sklearn.preprocessing import StandardScaler. We also would like to import one more thing here, which is the bread and butter of our course, which is tensorflow as tf. So that sums up our libraries that we will be working with here. Again, Pandas is used for [Data Manipulation](../../Skills/Data%20Science/Data%20Manipulation.md) and analysis. NumPy used for numerical computations. It's like the fancy calculator for our programs. Then we have matplotlib.pyplot and seaborn. These are for data visualization. Seaborn is built on top of Matplotlib and provides a high level interface of drawing attractive and informative statistical graphics. Then we have fetch_california_housing dataset, which loads the California housing dataset for us. Then we have test_train_split, which splits the dataset into training, validation, and test sets, followed by StandardScaler, which scales features by removing the mean and scaling to unit variance. Next, go ahead and continue on with the code.
>
> **[3:10](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=190)** So we will [Fetch](../../Skills/Web%20Development/Fetch.md) and prepare the dataset like we did in the earlier section. We create a housing variable, and then we go ahead and use the fetch_california_housing, which loads the California housing dataset into the variable housing. Next we have Pandas. Let's go ahead and actually create it right here. So after the housing variable, we will go ahead and do convert to DataFrame for easier visualization,
>
> **[3:43](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=223)** and we are basically preparing our data in the best format possible for easy and straightforward visualization. We will call this housing_df. So we will call pd.DataFrame to convert this data into a Pandas DataFrame. So data will be housing.data that we just installed. Then we will have columns equal to housing.feature_names. Then we will go ahead and say housing_df, Target, and this will be equal to housing.target. So this is how we define the target, which adds the target variable, in this case, housing prices to the DataFrame. So let's go ahead and start visualizing the data from here on. So let's modify the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) code for that. We will have visualizing the data from now on, and we will start by displaying the first few rows, see how our data looks like, to see the structure. So let's go ahead and do that. So print, let's call the DataFrame, housing_df.head. This will print the first few rows of our dataset. Next we can move on and create the SNS plot, which is a histogram plot with a kernel density estimate of the target variable to visualize the distribution of house values.
>
> **[5:18](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=318)** So for that, we will have to go and start visualizing the distribution, visualizing the, these notes are very important. Imagine that you come back to this code in a couple months. We may not remember everything about the code that we have written, so it's always a good practice to go ahead and drop some comments on what we are doing. So here we have the visualizing the distribution of the target variable. So in this case we will go ahead and start the figure by plt.figure. So this is actually initializing our figure right here, and then we will give it a size. So let's go ahead and do that, figsize, and in this case we will have 10.6. Then we will start a histogram plot. So for that, what we will do is we will start by coding sns.histplot, and then we will open parenthesis, housing_df, and then we will have Target, and then we will go ahead and call bins equal to 50, and then kde equals to be True, which is the kernel density estimate. This is the estimate of the target variable to visualize the distribution of housing values. So let's go ahead and correct the typo here to be True. Great. So after that, let's go ahead and give it a title. So the title in this case is
>
> **[6:52](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=412)** going to be Distribution of House Values.
>
> **[7:02](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=422)** Let's go ahead and give it a xlabel. So the xlabel here in this case would be Median House Value. Then we will have plt.ylabel, and it will say Frequency. So after we give the ylabel, let's go ahead and save this figure by saying plt.savefig. And then we can say simply the location, and all the figures will be located in the output folder as you can see on the left pane. So we will say output/, the chapter name, 03_02_, and then let's call it distribution_plot.png. So that takes care of saving the plot. Next we are going to move on to the pairplot. So let's go ahead and say pairplot of the features to understand relationships. So this will create a pairplot of all features and the target variable to visualize potential relationships between them. Let's go ahead and create it right now. So we will use the Seaborn library and it's a simple function called pairplot. And this pairplot function will take housing_df. Then we will go ahead and do the same thing, save figure, and then we will save it into the output,
>
> **[8:37](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=517)** and then it will have 03_02_feature_pairplot.png.
>
> **[8:48](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=528)** Okay, then we can pretty much go ahead and plt.close. So let's do that, so it doesn't remain open here. And let's go ahead and do the same thing on the previous figure as well. Okay, next we are moving on splitting the data into training, validation, and test sets. So first we split the data into training, validation, and test sets. And then the second split, divide the full training set into a smaller training and validation sets. So this is not new, therefore we can move a little bit quickly on this one. So we move on from here, and we go ahead and standardize the data using StandardScaler. Next, then we assign X_train, and then we say X_valid, and then X_test. So let's recap what we have done after we splitted the data into training, validation, and test sets. We introduce the StandardScaler, which scales the features to have mean of 0 and variance of 1, and then we applied fit_transform, which fits the scaler on the training data and transforms it. And then we use transform, which applies the same transformation to the validation and test sets to ensure consistency here. So first of all, we use fit_transform for the X_train, and then we apply the same transformation to the validation and test sets to ensure consistency.
>
> **[10:22](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=622)** Next, we will go ahead and visualize the scaled features. So for that, we will create a scaled train DataFrame. So let's go ahead and do that right now. We will say, visualizing the first few scaled training data points,
>
> **[10:46](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=646)** and then we say scaled_train_df equal to pd.DataFrame.
>
> **[10:59](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=659)** And then we will give the data to be X_train. Let's go ahead and correct this, X_train, and then columns to be housing.feature_names.
>
> **[11:18](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=678)** So these will be our columns. So let's go ahead and create the scaled_train_df, and then we will create a variable called Target. And notice I'm changing single quotes and double quotes here and there. As long as you start with the same quote and end with the same quote style, you are fine. It doesn't matter which quote you use. So this will be equal to y_train. Okay, so after that, we will go ahead and create a box plot. So before creating a box plot, let's make sure that we are printing the scaled_train_df, so at least a few rows. So let's go ahead and do that here, print scaled_train_df.head. So let's back up here and summarize what we have done. So what we have done in the first pd.DataFrame is we converted the scaled training data back into a DataFrame for easier visualization. Next, we added the Target variable to the scaled training DataFrame. So now our DataFrame contains the Target variable. Next, we are now getting ready to visualize the distribution of the scaled features. So let's go ahead and do that now, visualizing the distribution of the scaled features.
>
> **[12:53](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=773)** So for that, we are going to go ahead and create a new plot, plt.figure. And then we will again give the dimensions of this, figure size 12 to the 8. And let's make sure we give the inner parenthesis as well. Then we go ahead and call the Seaborn. What we call now is the boxplot. So this plot's a box plot to visualize the distribution of scaled features. So after that, we give what the data is, which in our case is scaled_train_df, and then we will go ahead and drop the Target variable. So we will say .drop, and then inside we will say, okay, drop the Target column. So columns equal to Target. So what will happen here is we will have all the data, but the Target column, which means the features, right? So we will have all the features here. Next we are going to say plt.title, and then we can call it Distribution of Scaled Features.
>
> **[14:07](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=847)** Okay? Next we will say plt.xticks. And then we will have a rotation of 45. And next plt.savefig again. Let's give it a location and name. So it's again the output folder that we have been using. And then we give the section name 03_02_, and then we will say, this is scaled_features_distribution.png.
>
> **[14:44](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=884)** Next we will go ahead and close this. So after we take care of the scaled features distribution plot using the Seaborn library, next is initializing a simple sequential model. So let's go ahead and continue on with that. So we will say initializing a simple sequential model,
>
> **[15:13](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=913)** and then model will be equal to tf.keras.Sequential.
>
> **[15:23](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=923)** So this initializes the sequential model. So let's go ahead and open parenthesis, then we will say tf.keras, and then we will have .layers.Dense, and then we will give it 30 neurons. So this adds a dense, fully connected layer with 30 neurons. This is similar to the model that we had built earlier. We will have a activation as relu. So let's go ahead and do that here. Next we will give the input shape to set the number of features in the training data, which is the input_shape equals to X_train.shape, 1 onwards.
>
> **[16:16](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=976)** Okay, comma. And then what we do is we go ahead and add the output layer with one neuron next. So to do that, we again call tf.keras.layers, and then this time we say Dense, and then we give it one layer. Next we have close parenthesis, close parenthesis. Then we go ahead and compile the model. So let's make a title for that, compile the model. So we will say model.compile, configures the model for training. We have to give the loss. So we will say loss equals two mean squared error. So we will say mean_squared_error, and then we will give a optimizer, which is the stochastic gradient descent in our case. So let's go ahead and do that, optimizer equal to sgd. So this is our optimizer here. So after that, we go ahead and train the model. So let's go ahead and do that, training the model. And for that, we use model.fit, and we give it X_train, and then we give the features and the target variable, y_train. And then we give the epochs to be 20,
>
> **[17:48](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=1068)** which is the number of iterations. And then we give the validation_data equal to, we have already prepared those to be X_valid and y_valid.
>
> **[18:05](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=1085)** We have to go ahead and assign it to a variable called history. So after training the model, we go ahead and evaluate it. So we start with evaluating the model. After the title, let's go ahead and do that. So we will use the mean squared error for evaluation. So we will say, mse_test equal to model.evaluate, and then this will take X_test. Basically compare it with X_test to y_test, and then it will come up with a mean squared error for us. Then let's go ahead and print result by saying print the mean, just like we did earlier, squared error on test set. And then we will give it the mse_test. Great. So to summarize, the code started by loading and exploring the California housing dataset, visualize the data distribution and feature relationships. It splits the data into training, and validation, and test sets, and then scales the features. And after visualizing the scaled data, it builds and trains a simple neural network using TensorFlow. Finally, the model is evaluated on the test set, and the mean squared error is printed to assess the models performance. So if you followed along, great. If not, open up 03_02_end.py,
>
> **[19:43](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=1183)** and give it a run by clicking the little triangle. And then let's go through the results once again before we wrap up this particular session of pre-processing and how it affects the TensorFlow models. So it should take just a couple minutes, not too long, and it should start printing our results here. These warnings are okay and are expected. So this is our dataset. So we have median income, house age, average rooms, average bedrooms, population, average occupancy, latitude, longitude, and target. Target is kind of the normalized value of home prices. So here we are seeing a snapshot of our housing dataset that we printed in the beginning. So this is what we are seeing here by print. So it's running in the background still, and we can watch the progress in the terminal and see how our code is doing. And then after that, we are able to go ahead and make some comparisons and judgments based off of the results. So it's going over 20 iterations as we asked it to. So it should be quick, 'cause we eliminated using the GPU as well, and it should start printing out the mean squared error on the test set for us, which is 0.35. And the output is highlighting the distribution plot that we have. So we can see the distribution on median house value by opening up the output folder and finding the particular session we are in, which is 03_02_distribution_plot.
>
> **[21:18](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/preprocessing-and-feeding-data-into-your-model?u=76281980&t=1278)** Next, we can see feature pairplots, and we might have to [Zoom](../../Skills/Software%20Development/Zoom.md) in to see the feature pairplots. So this is how target is changing with house age, average rooms, average bedrooms, et cetera, and all the other features that we can see here. We are able to look at the relationships between them. Next we can see the scaled features distributions. So these are our median income, house age, average rooms, average bedrooms, population, average occupancy, latitude and longitude after we did the scaling. Finally, we evaluated the model and then printed the mean squared error as shown in the terminal. So this sums up our section, how pre-processing affects the TensorFlow model. See you in the next session.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (6), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (6), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (1), [Data Manipulation](../../Skills/Data%20Science/Data%20Manipulation.md) (1), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Code Identifiers:** housing_df (5), scaled_train_df (5), fetch_california_housing (3), test_train_split (2), feature_names (2)
> **CLI Commands:** make (4), find (1), python (1)
> **Analogies:** such as (1), it's like (1), imagine (1), similar to (1), just like (1)
> **File Paths:** 03_02_end.py (2), 03_02_begin.py (1)
> **Env Vars:** sns (1), gpu (1)
> **Versions:** 10.6 (1), 0.35 (1)
> **Tools:** terminal (2)

#### [Monitor training and validation](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/monitor-training-and-validation?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/monitor-training-and-validation?u=76281980&t=0)** - [Instructor] Now that we have successfully pre-processed our data and then build our machine learning model, it's time to move on to the exciting part, training our [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) model and then going ahead and looking at its performance visually. This will give us a clear understanding of how well our model is learning and where it might be facing challenges. So, let's go ahead and meet at the [Codespaces](../../Skills/Software%20Development/Codespaces.md) main page. And then from here, we are going to find the src folder and then click on the 03_03_begin [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) file, indicating that this is the beginning Python file that we will be starting with. And then, we will end up in 03_03_ and Python file. So, we will go ahead and import our libraries as usual, tensorflow, matplotlib and sklearn libraries, followed by loading the data from fetch_california_housing dataset again. We are going to go ahead and do our train test splits to create our train test splits, as well as the validation data. Next, we are going to move on and scale the data here. Again, we will have a scaler. We will fit_transform on the X train, followed by transform only on X validation and X tests, followed by defining the model. Here, we have defined a model before, so let's go ahead and review what it includes.
>
> **[1:34](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/monitor-training-and-validation?u=76281980&t=94)** First of all, we start with tf.keras.layers.Dense with 30 neurons, and then we give a activation energy of relu and then we will give it a input shape. So, the hidden layer here is the first layer, which we give 30 neurons. It introduces the non-linearity into the model. It allows it to learn complex patterns here. Since we are predicting a continuous value, we give a single neuron for the output layer, which is fine. Then we use mean_squared_error for the loss, and for the optimizer, we use stochastic gradient descent again. So, let's go ahead and take it from here and once again, train the model followed by visualizing the performance of the model. So let's go ahead and say Train the model here. And then we will go ahead and create a variable called history. And then we will say model.fit, and what we give here is now X_train, y_train and then the number of iterations, in other words, epochs, would be 20, followed by validation_data, which we have already prepared up above, right? So, for the validation data, what are we going to give? We are going to give the X_ validation and then y_ validation. That's it.
>
> **[3:08](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/monitor-training-and-validation?u=76281980&t=188)** So, this is what we do for training the model one line. Next is the key point of the session is how do we visualize the training model? This is essential to be able to look at the training model and see how we can improve it, how we can analyze it. So first of all, we are going to call plot, and then we will have history, and then we will have history again, and we will call the loss here and we will label it as Training Loss. Next, we are going to move on and create the validation loss in the plot. So again, plt.plot open parentheses history.history and then we are going to give validation loss here, followed by let's give it a label so it's easy to read and we will appreciate these labels when we look at it in a second. Validation Loss. Follow by giving a title to our plot so that we know what we are working with, so let's make the title clear. Let's call it Training and Validation Loss. So, that will be pretty explanatory. However, perhaps also, let's go ahead and add over Epoch so that we know that that's what we are working with, right? So, this is the title, as well as we have xlabels. So, let's call them xlabels.
>
> **[4:45](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/monitor-training-and-validation?u=76281980&t=285)** That is the epochs that we work with. And then plt.ylabels. That is the loss that we are working with. We are going to call plt.legend. And then let's go ahead and save this figure as usual. Remember when we are working with code spaces, plot.show does not work at the time of recording this video. It might change in the future, but plt.show does not work on Codespaces. So, there are workarounds, but no need for those complications. We are going ahead and saving it to our output folder. And what is the other benefit of saving it in the output folder is we can open it any time, right? We can go back and visit it and continue analyzing it in the future. So, we have a record. Let's go ahead and quickly see what we have done. So, we created a plot that allows us to visualize how well the model is learning and where it might be over fitting. So, we went ahead and created a PLT plot history loss, and then that is the Training Loss and then Validation Loss. That is the validation loss we are working with. We titled it. Then we gave the X labels and Y labels and we saved the figure. So those are some of the steps we went through. Next, let's go ahead and evaluate the model on the test set. For that, we will call mse_test, mean squared error test, model.evaluate, and we give it X_test, y_test, right?
>
> **[6:22](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/monitor-training-and-validation?u=76281980&t=382)** So, that is giving us the mse_test, and we will go ahead and print the result of this mse_test. So, Mean Squared Error on Test Set, right? And we are going to go ahead and say mse_test. So that is it. So it will help us print it in the end. So, this is it. So, we created the training and validation monitoring process here. Why is that important? It's always important how our model is behaving over iterations, over epochs. So, this is pretty much the same exact Python code with the, again, looking at the left pane under the src folder, 03_03_ and Python file. So, find it and open it and compare it with the code you wrote. Make sure it's matching. If it's not, perhaps spend a little bit more time and try to understand what we are doing here when we visualize the training process particularly, and then evaluating the model on the test set. I'm going to go ahead and run it with this little triangle on the upright. It will go over 20 iterations, 20 epochs. It should take a couple minutes, not too long, and it will save a output file for us, which is in a PNG form. We will go ahead and open it and then we will make some analysis on it. So, our Mean Squared Error on Test Set is about 0.35 if we round it.
>
> **[7:57](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/monitor-training-and-validation?u=76281980&t=477)** So, this is our MSE on the test set. Now, let's go ahead and see the visualization. So, I'm going to find the output folder, and then I will click on the left pane, the output folder, and then it will show here 03_03_training_validation_loss.png. So, this is our Training and Validation Loss over Epochs. So let's go ahead and spend some time how we can analyze it. Again, just as a reminder, even though we set the random state, there is still some randomization occurring in the data, how it's split and how it's behaving, and your graph may be slightly different than mine. And if you run the script multiple times, you may even see different results slightly, and that's okay, and that's expected. So, looking at this graph, we have the blue line as training loss and orange line as validation loss over epochs, which we have 20 epochs, in other words, iterations. And then on the Y-axis, we have the loss. So, when we look at the blue line, which is the training loss, training loss decreases rapidly within the first few epochs and then stabilizes close to zero. This indicates that the model quickly learned to fit the training data very well, achieving a low error rate on the training set. Next is validation loss. So, this is represented with orange line. The validation loss also increases rapidly,
>
> **[9:32](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/monitor-training-and-validation?u=76281980&t=572)** but it starts at a much higher value compared to the training loss. Similar to the training loss, the validation loss drops significantly in the first few epochs and stabilizes at a value close to the training loss. The fact that the validation loss closely follows the training loss after the initial few epochs suggests that the model is generalizing well and is not over fitting to the training data. So, both the training and validation losses decrease sharply between the first few epochs, indicating that the model quickly learned from the data and adjusted its parameters effectively. The validation loss closely follows the training loss after the initial adjustment period, which implies that the model generalizes well to unseen data. The stabilization of both losses after a few epochs indicates that the model has converged, meaning that the further training is unlikely to lead to any significant improvements in performance. So, this training process suggests that the model was able to effectively learn from the training data and generalize to the validation data, resulting in low loss values for both. So, the absence of divergence between training and validation losses is a positive sign indicating that the model is not over fitting. The rapid convergence also suggests that the chosen model architecture and training step are well suited to the problem at hand.
>
> **[11:09](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/monitor-training-and-validation?u=76281980&t=669)** So to summarize, in this section, we have seen how to monitor training, validation loss after creating a model and how to make conclusions out of it. Let's move on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (5), [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (2), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (2)
> **CLI Commands:** python (5), make (4), find (3)
> **Code Identifiers:** mse_test (4), fetch_california_housing (1), fit_transform (1), mean_squared_error (1), y_train (1)
> **Env Vars:** plt (1), png (1), mse (1)
> **Definitions:** in other words (2), is a  (1)
> **UI Navigation:** click on (2)
> **Versions:** 0.35 (1)
> **Analogies:** similar to (1)

#### [Success metrics](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/success-metrics?u=76281980)

#### [Save and reuse trained models](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=0)** - [Instructor] In this video, we are going to explore how to save and reuse train models in [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md). Being able to save a model after training and load it later is a very important step in any machine learning project. This allows us to share our models, deploy them into production environments, or continue training them at a later time. Let's go ahead and open up the SRC folder from our [Codespaces](../../Skills/Software%20Development/Codespaces.md) environment. Find 03_05_begin [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) file. So this Python file includes where we left off in the last session and then we are going to go ahead and continue. Here we have already built and trained a simple Feedforward neural network using the California housing dataset. The model consists of two dense layers and we have trained it for 20 epochs, in other words, iterations. Now we are going to move on to saving this trained model. Let's go ahead and do that. So we will be able to save the model with just one simple line. So let's go ahead and title this comment, Save the trained model, and we are going to save this model into the output directory that we have been working with. So model.save and we are going to save it to the output folder, 03_05_trained_model.h5. So we go ahead and save the trained model.
>
> **[1:35](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=95)** Next we will go ahead and load the model back and we will call it loaded_model this time, loaded_model equal to Tensorflow .keras.models and then we will call load_model and then we'll go ahead and give the same path that we have assigned this model to be saved. So we are loading it back from its location. Next we are going to go ahead and evaluate the loaded model to confirm it's correctly loaded. So evaluate the loaded model to confirm it's correctly loaded. So for that, we are going to go ahead and say loaded_test results equal to loaded_model.evaluate
>
> **[2:31](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=151)** and we are going to say x_test, comma, y_test and then return dictionary to be True. Let's go ahead and print this. So we are going to say print f "Loaded Model Test Results" to be loaded_test_results, test_results. Okay, so it's going to print the test results for the loaded model here. Next we will compare the predictions from the original model and the loaded model to ensure that they are identical. So let's go ahead and do that next. Compare predictions from the original and loaded models. We'll go ahead and say original predictions equal to model.predict x_test and then the loaded predictions which we we'll compare will be equal to loaded model.predict x_test.
>
> **[3:46](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=226)** Here we first load the saved model from the .h5 file using tf.keras.models.load_model, then we make predictions on the test dataset with both the original and the loaded models. Next, we are going to verify that the predictions are the same. So we are going to assert to verify that the predictions are the same. So we are going to use the assert tf.reduce_all and then we are going to use tf.abs. Then we are going to take the differential of original predictions, which are coming from the predictions that we are making here, minus the loaded predictions, which are coming from the loaded model. Are these less than one to the power of minus five? So we are comparing the differential between the original predictions and the loaded predictions and checking if the differential is less than 10 to the power of minus five, which is a very small number, close to zero. And then we can go ahead and add this line of predictions differ between original and loaded models if that's not the case, so we give this in between quotes.
>
> **[5:19](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=319)** So let's recap what we have done here. We assert to verify that the predictions are the same between the original predictions and loaded predictions. So this operation that we introduce here, tf.abs, calculates the absolute difference between the predictions made by the original model and the loaded model for each instance in the dataset. The result is a tenor where each element represents the absolute difference between the correspondent predictions of the two models. Then we go ahead and check if that difference is less than 10 to the power of minus five. If the condition evaluates to True, the program continues to execute normally. If the condition is False, then the program raises an assertion error and the error message will be predictions differ between original and loaded models. Then finally, what we do with the tf.reduce_all is used to reduce this tenor of boolean values from this comparison into a single boolean value. It returns True if all elements in the tensor satisfy the condition, in other words, if the differences are smaller than 10 to the power of minus five and then falls otherwise with this error message. Great. Now we can move on to the visualization next. So we are going to visualize original and loaded model predictions next,
>
> **[6:54](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=414)** so visualization of original and loaded model predictions.
>
> **[7:05](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=425)** So let's go ahead and initialize the figure, plot that figure, and then we are going to give the figure size to be 14, 7 and then we are going to go ahead and say plt.plot. Starting with original predictions, label original model predictions. Let's correct a typo here. Original predictions, and we label it as such. Then we are going to go ahead and say plt.plot loaded_predictions this time and then we are going to label it as label equals to Loaded Model Predictions,
>
> **[7:58](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=478)** and then to differentiate between these two, let's go ahead and use line style, and then let's use the dashed line style here. Okay, next we are going to give this plot a title, plt.title, original versus loaded model predictions, Original versus Loaded Model Sredictions. Next we are going to say plt.xlabel to give it the label of the XXs, which is sample index. Then you're going to do the same for the Y label and it'll have predicted value. Then plt.legend legend. Then let's go ahead, and as always, save this visualization, plt.savefigure and give it the output location that we always have been utilizing here for any figure that we have been building. So the same thing here, 03, this time 05, and let's call it model_comparison.png. So, great. In this particular section, we have saved the trained model. We loaded the model back. We evaluated the loaded model to confirm it's correctly loaded. We compare predictions from the original and loaded models. Next we assert to verify that the predictions are same.
>
> **[9:34](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=574)** We go ahead and visualize the original and loaded model predictions. Then we finally save the visualization. If you have been coding together, great. If not, that's also great. You can pick it back up at 03_04_end Python file and give it a quick run. Then we will open the figure that we created and look at the outputs of our code and start analyzing our results. Give it a couple minutes and then we will be able to see the results for it. So after a couple of minutes, we will see that loaded_model test results are printed right here. We name them as such in the printout in the code, and then when you scroll up above, you will see that we have named the original model results as test results, so we will be able to see it right here. So we have the test results from the model we created here, and then next we have the loaded model test results where we are loading the model back and printing its results here. You'll notice that the test results from the original model and the loaded model are identical, which is what we expect. That means the model was successfully saved and loaded without any loss of information. This is crucial because it confirms that the model's performance remains consistent across different instances. So the fact that the predictions are exactly the same implies that the process of saving and loading the model
>
> **[11:08](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=668)** worked perfectly here. It preserved the model's weights, architecture, and overall functionality. So when we enlarge the terminal, you can see the results, both of them, the test results and loaded model test results together here, and once again, confirm that they are identical. Next, we are going to open up the output folder and then find the figure that we have created here, which is 03_05_model_comparison. Let's minimize the terminal window so that we can see the model compares and graph a little bigger. So in blue we see the original model predictions, and in orange we see the loaded model predictions. So this plot effectively demonstrates that the loaded model performs identically to the original model, ensuring that the model's predictions are reliable and consistent even after being saved and restored. How can we make that conclusion? Because on this plot, the predictions of the original and the loaded models overlap almost perfectly. The fact that the predictions of both models are indistinguishable confirms that the save load process worked correctly, maintaining the model's integrity here. The plot visually validates the assertion check we did, ensuring that any minor numerical differences
>
> **[12:43](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=763)** between the original and loaded models' predictions are negligible and within acceptable bounds. To summarize, this plot effectively demonstrates that the loaded model performs identically to the original model. This ensures that the model's predictions are reliable and consistent even after being saved and restored. Also, notice in the same output folder, we have saved our model here under 03_05_trained_model.h5.
>
> **[13:19](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/save-and-reuse-trained-models?u=76281980&t=799)** So this summarizes how to save and reload models.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (2), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (1)
> **Code Identifiers:** loaded_model (4), x_test (3), load_model (2), reduce_all (2), loaded_test (1)
> **CLI Commands:** python (3), find (2), make (2)
> **Definitions:** is a  (3), in other words (2)
> **Tools:** terminal (2)
> **UI Navigation:** open the (1), scroll up (1)
> **Env Vars:** src (1)
> **Cross-References:** in the last (1)

#### [Autoencoders: A gentle introduction to generative models](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=0)** - [Instructor] In this section, we will dive into using [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) to build and train an autoencoder for anomaly detection. This autoencoder will be applied to the same California housing dataset we have been working with. We will focus on reconstructing input features and identifying any potential anomalies in the dataset. So you may be asking, what is an autoencoder? Well, we can think of it like a type of neural network that learns to compress data into a lower dimensional space and then reconstruct it. By examining, how well the data is reconstructed, we can identify outliers or anomalies that the autoencoder struggles to recreate. So let's meet again in the code spaces environment and from the left pane, find the SRC folder. Then go ahead and find the 03_06_begin [python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) pile. So we start with loading and pre-processing data as we usually do. The dataset we are working with here is the same California housing dataset, and it's loaded and split into training, validation and test sets. After we do the load and prepare the dataset, we move on to the scaling step and we go ahead and fit_transform on the X_train. Then we only transform on the X_validation and X_test datasets.
>
> **[1:35](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=95)** Next we go ahead and define the autoencoder model. The encoder here compresses the input data into a lower dimensional representation, in other words, 32 dimensions. It consists of three layers here, the first layer has 128 neurons, second layer has 64, and the third layer has 32 neurons. It consists of three layers progressively reducing the size of the data. Next, we move on to the decoder. The decoder reconstructs the input data from the compress representation. It mirrors the encoder, but in reverse, expanding the compressed data back to the original input dimensions. So the autoencoder is a combination of the encoder and decoder that we have defined up above. So we call it from the TensorFlow.keras.Sequential. And then we feed in encoder and decoder that we have defined up above, encoder definition and decoder definition. So we create an autoencoder here, which is a combination of the encoder and decoder. It's trained to minimize the difference between the input data and its reconstruction. Then we go ahead and compile the autoencoder and we use mean squared error as the loss function, and we use Adam as the optimizer. We then have a preparation for data for visualization here.
>
> **[3:12](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=192)** Then we have initialization of the history where we will go ahead and train the autoencoder in the next step. So what we will do next is we will train the autoencoder to reconstruct input data. It uses both the training data and validation data for this task. The number of epochs will be set to 20. So this is our beginning Python file where we loaded the necessary libraries, prepared the dataset, did our splits, did the scaling. Then we define the autoencoder model with the encoder and decoder. Then we define autoencoder. Then we compiled our autoencoder. We prepared the data for visualization, and now we are continuing on training the autoencoder. So let's go ahead and start from the placeholder here. So we will say train the autoencoder, and then we will continue with the training of the autoencoder here. So we are going to go ahead and call it history and history = autoencoder.fit. And then we will say X_train and then X_train. So the first occurrence of X_train is the input to the autoencoder. It represents the original data that we want the autoencoder to learn to encode and then decode. Then we use a second X_train here, which is the target output for the autoencoder.
>
> **[4:48](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=288)** Since the autoencoder goal here is to reconstruct the input data, the expected output during training is the same as the input. Next we go ahead and move on and we give the number of iterations. Let's go ahead and use again, 20 here, which is the epoch. Then we use validation data. Let's make space here by minimizing the left pane. So validation_data will be equal to the X_valid,
>
> **[5:22](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=322)** and then we are going to use the same x_valid. Okay, so what we did is we went ahead and trained the autoencoder to reconstruct the input data. It uses both the training data and the validation data for this task, and it's using 20 epochs. Then we already have prepared up above the data visualization for the epoch's validation loss graph up above so we don't have to go ahead and define this again. So what we will do next is we will call this function that we prepared already that came at the begin file for us, and we will go ahead and call that in the next part of the code. So let's do that here. So we will call it plot the training loss and save it. So we will call the function that we define up above and then we will feed the history into it. Next, we will go ahead and visualize the original versus reconstructed data using line plots. For that visualization, we will go ahead and create a function. Let's title what we are doing first. So visualize the original versus reconstructed data using line plots. So for this we are going to go ahead and say,
>
> **[6:57](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=417)** definition plot_reconstruction. Let's scroll up a bit and then this will take model and it'll take n = 10. So N is the first n samples in the test set. Let's correct the typo here and it'll go ahead and plot both the original and reconstructed values for each future. So what will this allow us to do? It'll allow us to visually assess the accuracy of the reconstruction using this function. So let's go ahead and say samples = X_test and it will take n, and next we will say, reconstructed samples = model.predict(samples).
>
> **[7:54](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=474)** Let's prepare the figure here. So figure, axs = plt.subplots(n, 1, figsize=(10, 20)).
>
> **[8:16](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=496)** So we will go ahead and loop for i in range(n) axs[i].plot and will take samples of i
>
> **[8:33](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=513)** and we will label it as this will be the original. Next, we will go ahead and do axs[i].plot (reconstructed_samples). So this will be our reconstructed samples of i, and then we will label this as reconstructed, and then we will have a line style of this so that we can differentiate between the two. So let's go ahead and read our code once again, make sure it's typo free. We created a function called plot reconstruction with model and 10 samples. Then we go ahead and create the samples. We say reconstructed samples = model.predict(samples). Then we prepare our figure. We make a loop in the range of n, and then we create the original. Next we create the reconstructed. Next we will go ahead and add the legend to it. So axs[i].legend(), then we will go ahead and save it. So fig.savefig("output/03_06_reconstruction_plot.png")
>
> **[10:04](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=604)** as always. So we are going to save our figure into the output directory. Next, all we have to do is we will call this function. So let's call the function next, plot of reconstruction, not loss, reconstruction, and then we will give the autoencoder here. So this is the autoencoder we have created up above. So that is what we are feeding into this function as the model and then we have n samples. So that's it. So what this function will do is this function will compare the original data to the reconstructed data for the first n samples in the test set. It plots both the original and reconstructed values for each feature, and it'll allow us to visually assess the accuracy of the construction. So let's go ahead and enlarge the left pane. This code then should be similar to the 03_06_end python file. So let's go ahead and run the end file and then let it do its magic with the autoencoder. Then we will go ahead and expand the output directory, and then we will comment on the analysis that the code brings. So let it run for a couple of minutes. So we will go through 20 epochs here. So once it's completed, let's go ahead and scroll up and see the output directory.
>
> **[11:38](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=698)** Notice that we have created 03_06_loss_plot. Then also we have created 03_06_reconstruction_plot.png. So we have created two figures. Let's start from the first one. Notice, I'm minimizing the terminal by dragging this down, dragging this line down to create more space into the area we are looking at for the figure analysis. So let's look at the training loss plot like we have open right now. The orange line showcases the validation loss, and the blue line shows the loss here, which is the training loss. So we also have on the X labels, the epochs, the iterations, which we have given 20 on the X label and Y label is the loss. So the training loss, which is the blue line, is very low, and it stays pretty consistent. So this indicates that the model is learning to reconstruct the training data well. When we look at the validation loss, which is the orange line, the validation loss is highly variable and much higher than the training loss. This suggests that the model is struggling to generalize to unseen data, indicating potential overfeeding or a lack of capacity to generalize. Next, we are moving onto the reconstruction plot. So we have multiple plots here. The series of line plots compare the original data,
>
> **[13:15](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=795)** which is the solid blue lines to the reconstructed data for multiple samples. So the reconstructed data is the dashed orange line, and original data is the blue solid line. The reconstructed data closely follows the original data in most cases, but there are slight discrepancies. So this shows that the autoencoder is generally effective at reconstructing the input data, but it may have some limitations, especially in capturing all the details. So what can we say to conclude about autoencoder step? So this autoencoder model was successful. It trained successfully to reconstruct the input data and the results show a reasonable construction capability, especially for the training data. The discrepancy in validation loss indicates that the model might not generalize well, especially to unseen data. It suggests that we might want to consider some further tuning, or we might consider a more complex model in order to improve the performance. When we look at the reconstruction plots, they visually confirm that while the model captures the general trend of the data, there are still small errors in the reconstruction and they are particularly noticeable in the validation set. So to conclude, we can say that autoencoders are a type of neural network
>
> **[14:47](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/autoencoders-a-gentle-introduction-to-generative-models?u=76281980&t=887)** designed to learn efficient representation of data, typically for the purpose of dimensionality reduction. The key behind this is that it tries to learn an encoding, in other words, a compressed representation of the input data and then reconstructs the input from its encoding. So that wraps up our encoding session.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (2)
> **CLI Commands:** python (3), make (3), find (2)
> **Code Identifiers:** fit_transform (1), validation_data (1), x_valid (1), plot_reconstruction (1), reconstructed_samples (1)
> **Definitions:** in other words (2), is a  (2), is an  (1)
> **Cross-References:** in the next (2)
> **UI Navigation:** scroll up (2)
> **Env Vars:** src (1)
> **Tools:** terminal (1)

#### [Challenge: My first TensorFlow model](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/challenge-my-first-tensorflow-model?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/challenge-my-first-tensorflow-model?u=76281980&t=0)** - [Instructor] Wow, we have covered a lot of ground with [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) with [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md). Next, we are going to work on a challenge where you get to practice the learnings from importing libraries to standardization, to pre-processing, to creating the model, training the model, evaluating the model, and finally, creating the figures to see the results of our deep learning model. So if you are ready, meet me in the code spaces, and from the left pane, find the SRC folder and find the 03_07_challenge [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) file. So in this Python file, you will see that we have the libraries pre-installed for us, including TensorFlow, SK Learn data sets, SK Learn model selection, SK Learn pre-processing, Mat Plot Lib, and so on. So in the challenge file, we can already see that the California housing data set is preloaded for us and assigned to housing. Next, our data set is split into X train, X validation, Y train, and Y validation for us. So we will go ahead and tackle the problem as follows. First of all, your tasks will start with standardizing the data. As you can see, we have the sections that are commented out, and in the solution, what you are going to do is you are going to fill in after these comments with your own code. So you are going to start with standardizing the data. After that, you are going to work on building the model
>
> **[1:35](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/challenge-my-first-tensorflow-model?u=76281980&t=95)** with two hidden layers and one output layer. Then you are going to compile the model, followed by training the model, then evaluate the model on the test set. Finally, you are going to plot the training and validation loss graph in the output directory, the one that we have been using right here on the left pain. So now is the time to review everything we have learned, go ahead and give it a shot, and I will see you in the solution video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) (2), [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (2), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2)
> **CLI Commands:** find (2), python (2)
> **Env Vars:** src (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Building blocks to TensorFlow mastery](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=0)** (playful music)
>
> **[0:05](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=5)** - [Instructor] Welcome to the solution video. Now we are going to start from the challenge code, which is 03_07_challenge.py, and we will make our way to the solution [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) file, which is 03_08_solution.py. So let's go ahead and close the left window by clicking Explorer here, and then we are going to go ahead and continue with our challenge code to make it our way to the solution code. So let's go ahead and review what we have so far. We have important and necessary libraries such as [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md), sklearn, matplotlib. Then we went ahead with loading and preparing the dataset. We are again using the California housing. Then we split the data to have X train, X validation, y train, y validation. Next we are moving on to the solution code, which is starting with standardizing the data. So this was the number one task we have coming from the challenge video. So let's go ahead and work on standardizing the data next. So to standardize the data, we are going to start by standard scaler. So we are going to call the StandardScaler here, and then assign the scaler to be equal to StandardScaler open and close parentheses. Next we are going to fit transform the X train data by calling scaler.fit_transform, and then we are going to call X_train. Next, we are going to transform X validation.
>
> **[1:39](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=99)** Notice we are not using the fit transform rather we are using the transform only for X validation. So it's scaler.transform, and then it will take the X_valid. Next we are going to use X_test, and it'll have scaler.transform and it'll have X_test. So this will wrap up our first task, which was standardizing the data. Next we are starting to our second task. Next we are going to continue with building the model with two hidden layers and one output layer as asked in the challenge video. So we are going to go ahead and start building the model using TensorFlow Keras, and then Sequential. So let's go ahead and do that. And then we are going to open parenthesis. Then what we are going to do next is we are going to call tf.keras.layers.Dense, and we are going to use 30 neurons with the activation function of relu. And then we are going to go ahead and assign input_shape to be our X_train.shape, and then open parenthesis, 1, and then close the parenthesis, comma. So this makes up our first input layer with 30 neurons with activation function of relu,
>
> **[3:13](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=193)** and then we define the input shape. Next, we are going to make the second layer as asked from the challenge video. So let's go ahead and do the second layer. So we are going to make again, 30 neurons with the activation function of relu. So this will make our second layer, and then we will have a final layer, which is the output layer with just one neuron. So let's go ahead and do that. And let's remove the unnecessary spaces here to make our code more Pythonic and clean. And there we have it. We created two hidden layers with 30 neurons with activation function of relu, and then one output layer. Next we are going to move on to the next challenge, which is compiling the model. After defining the architecture, now we will compile the model using mean squared error as the loss function and stochastic gradient descent as the optimizer. So let's go ahead and do that. So we can do it in one line, model.compile, and then we are going to give it the loss function of mean_squared_error. And then SGD, stochastic gradient descent, as the optimizer. Again, MSE is a commonly used loss function for regression tasks, which is what we are doing right here, where the goal is to minimize the difference
>
> **[4:48](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=288)** between the predicted and actual values. SGD is a straightforward and effective optimizer for training [Neural Networks](../../Skills/Artificial%20Intelligence%20(AI)/Neural%20Networks.md), particularly suitable for smaller models. Next, we are going to move on training the model. So once our model is compiled, we will move on with the training step. So the model will learn by minimizing the loss function during the training phase. So let's go ahead and code it up. So we will call it history, and then we will call model.fit to train the model, and then it will take X_train, y_train,
>
> **[5:29](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=329)** epochs equal to 20, and then we will give the validation data to be X validation and Y validation, X_valid, y_valid.
>
> **[5:49](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=349)** So in this step we are training the model for 20 epochs and monitor both the training and validation loss to ensure that the model is learning effectively and not overfitting to the training data. Validation loss provides insight into how well the model generalizes to unseen data. So after training, we can move on to evaluating the model on the test set. So how are we going to do that? We will utilize mean squared error for that. So let's go ahead and start coding, mse_test equal to, and then all we do here is we are going to call model.evaluate, and then we will use X_test and y_test. So the mean squared error on the test set offers a quantitative measure of our model's performance on unseen data particularly. So it provides a key indicator of how well the model has learned from the training data. So after that, we will move on and we will tackle the visualization step. So for that, we will plot the training and validation loss. This plot will help us determine whether our model has overfitted, underfitted, or is generalizing well. So let's go ahead and start with plt.plot. And then let's scroll up so that we can see more of the code, plt.plot, and it'll take history.history.
>
> **[7:29](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=449)** And then it will take loss, label, and we will label it as Loss. Next, we are going to say plt.plot. This time we are getting ready for validation loss. So history.history, val_loss, label equals, this time we are going to call it Validation loss. Great. Next, we are going to go ahead and label the X axis and Y axis. So let's start with the X axis, plt.xlabel. It will be Epochs. And then Y label will be nothing but Loss. Okay, after that, we are going to say plt.legend. Okay, next, what we will do is we will go ahead and save this figure into the output directory, so that we can use it in the future. Let's go ahead and give the path, output/, and notice we are doing this for the solution. So the solution will be 03_08_loss_plot.png.
>
> **[9:04](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=544)** So this is the path we are providing for our loss plot figure. So this wraps up our challenge with the plot, which helps us determine whether our model has overfitted, underfitted, or generalizing well. This is a very essential step, and seeing it visually gives a lot of explanation for our analysis regarding our model. So in this challenge, we tackled building, training, and evaluation of a simple neural network using TensorFlow. We will go ahead and compare this code now with the challenge code, and if we have any differences, go ahead and address them by opening the src folder from the left pane, and finding 03_08_solution.py. So once you are done, give it a run, and then we will look at the results. It will take a couple of minutes to go through all the epochs and creation of the plots, and then we will together analyze what the plot means and what conclusions we can make from this challenge. Epoch 1, 2, 3. It's going pretty fast. So it should finish rather quickly. And it's finished. So what we see here printed in the terminal is mean squared error on test set is 0.31. So let's go ahead and now find our plot from the left pane by enlarging the output directory
>
> **[10:39](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=639)** and finding the 03_08_loss_plot.png.
>
> **[10:48](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=648)** So after our script is completed, we can see mean squared error on test set to be 0.31. So then we went ahead and opened the figure from the output folder, which is titled as 03_08_loss_plot.png. So let's go ahead and evaluate this figure and understand what this figure is telling us. So this plot shows the training loss, which is the blue line, and validation loss, which is the orange line, over 20 iterations, 20 epochs during the training of a neural network. What are the key observations we can take away from this plot? The validation loss starts relatively high and increases significantly within the first few epochs reaching a peak of over 5. Well, what does it indicate? This indicates that the model initially struggles to generalize well to the validation data. The validation loss fluctuates quite a bit in the early epochs with sharp increases and decreases. So this could suggest that the model is unstable in the beginning, possibly due to the learning rate or random initialization of weights. Next around epoch 5, we can see that the validation loss experiences a significant drop and then continues to decrease, stabilizing at a much lower level. Well, what does it suggest?
>
> **[12:21](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=741)** This suggests that the model eventually learns to generalize better to the validation data. The training loss decreases smoothly and remains low throughout the training process, indicating that the model is learning the training data consistently without much difficulty. So by the end of the training, which is after around epoch 7, both the training and validation losses are quite low and relatively stable we can say. This indicates that the model has learned the patterns in the data and is not overfitting. So what can we take away from this? Well, initial instability. The significant fluctuations in validation loss during the first few epochs suggest that the model initially has difficulty finding a stable solution. Well, this could be due to various factors such as the learning rate, weight initialization, or even the complexity of our data. The training loss is consistently lower than the validation loss, which is quite typical, but should be monitored. However, since the validation loss stabilizes at a low value, there is no strong indication of overfitting here. Given that the loss values stabilize after a few epochs, we can consider implementing a early stopping criteria in the future. This would help us avoid unnecessary training beyond this point,
>
> **[13:53](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=833)** and it would save computational resources. Despite the initial instability, the model eventually generalizes quite well as evidenced by the stable and low validation loss towards the end of the training. So finally, we can say this plot indicates that the model faced challenges in the early stages of training, particularly with generalizing to validation data, but it eventually found a stable solution with both training and validation losses converging to low values. What does it suggest? Well, this suggests that the model learned effectively, although we observed some initial instability. So this wraps up our solution. Let's go back to the solution code and revise what we have done. We have imported the libraries. We have loaded and prepared the data set. We did the split and created X train, X validation, y train, and y validation data sets. We standardized the data using StandardScaler. We started with fit_transform on the X train, and we only use transform for the X validation and X test. So then we went ahead and built the model with two hidden layers. First of all, we started with 30 neurons. We gave the activation function of relu, and then we have another layer, which also has 13 neurons with activation function of relu. Then we have an output layer. Next, we compiled the model. We used mean squared error and we used stochastic gradient descent as an optimizer. Then we went ahead and trained the model.
>
> **[15:26](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/solution-building-blocks-to-tensorflow-mastery?u=76281980&t=926)** We evaluated the model on the test set. We printed out mean squared error on test set. Finally, we went ahead and plot the training and validation losses, and we took away some observations from that plot. So great job tackling on this challenge and the solution. Celebrate what you have learned, and I will see you in the next session.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (3), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Neural Networks](../../Skills/Artificial%20Intelligence%20(AI)/Neural%20Networks.md) (1)
> **CLI Commands:** make (7), python (1), find (1)
> **Code Identifiers:** fit_transform (2), input_shape (1), mean_squared_error (1), y_train (1), y_valid (1)
> **File Paths:** 03_08_solution.py (2), 03_07_challenge.py (1)
> **Env Vars:** sgd (2), mse (1)
> **Definitions:** is a  (3)
> **Versions:** 0.31 (2)
> **Cross-References:** go back to (1), in the next (1)


### 4. Mastering TensorBoard

[↑ Back to Table of Contents](#table-of-contents)

#### [Visualizing success with TensorBoard](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=0)** - [Instructor] So before we dive into the code, let's talk about TensorBoard. Visit www.[tensorflow.org/tensorboard](https://tensorflow.org/tensorboard). So what is TensorBoard? TensorBoard is a powerful visualization tool that comes with [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md). It allows us to monitor and visualize various metrics during the training process. What are those? Those are loss, accuracy, and even the weights and biases of our model. This helps us understand how well our model is learning and where we might need to make adjustments to make it even better. You may be asking, this is great, but why do we use it? Well, because it provides real time insights into the model training process. It helps us in diagnosing problems like overfitting or underfitting by visualizing metrics, and it also facilitates hyperparameter tuning by comparing different training runs. So let's go back to our code and incorporate TensorBoard to our project. So please go ahead and meet me in your [Codespaces](../../Skills/Software%20Development/Codespaces.md). Find the SRC folder from the left pane and find 04_01_begin [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) file. So let's start with the initial setup for our TensorFlow model where we will be using the California housing data set again. We will break down each step to see how TensorBoard fits into our workflow here.
>
> **[1:36](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=96)** To begin with, we are going to start from the begin file and then we will end up in 04_01_end Python file from the left pane under the SRC folder. So let's start from the begin file and make our way to the end file. First of all, we start with incorporating the libraries. Notice we have an addition here, which is from tensorflow.keras.callbacks import TensorBoard. That's where we invite the TensorBoard to our project. In this initial code block that I'm highlighting here, we are importing all the necessary libraries from TensorFlow.keras.callbacks import TensorBoard being the new edition. That's the key to enable our realtime monitoring here. Next, we are moving on to data loading and preparation. First, we need to load and prepare our dataset. Again, we are using the California housing dataset for this exercise. We load the data, then we do our splits, and we create extra X validation by train and Y validation. Just as a reminder, the validation set will help us monitor how well our model generalizes to unseen data during the training. So after that, then we go ahead with the standardization. We use the standard scaler, we fit transform the X train, and we transform the X validation and X test data sets.
>
> **[3:10](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=190)** Next step is define the model. Let's go ahead and minimize this left pane. So we see more of the code here. We will build a simple neural network here, like we did last time. Again, we have two hidden layers with activation functions of relu and 30 neurons as well as one output layer here. You may be asking why are we using just one neuron in the output layer? Because we are predicting this single value of the housing price here. Next we are going to compile the model. This involves defining the optimizer and the loss function. We are going to use adam optimizer and we are using mean squared error as the loss function. This is standard for regression tasks. Now let's go ahead and set up TensorBoard. This is where TensorBoard comes into play. We will set up a callback that logs data for TensorBoard during the training process. So how do we do that? We create a log_dir, which is log directory. That includes the current date and time. This helps us keep track of different training runs and the TensorBoard callback will log data to this directory and we can visualize it later using TensorBoard. Next, we have a placeholder for training. So we are going to go ahead and continue from here. So let's go ahead and train the model here. So we are going to say history equal to model.fit and we are quite familiar with this step.
>
> **[4:49](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=289)** We've done this a couple of times. x_train, y_train, give it 20 epochs,
>
> **[5:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=300)** validation data is X validation, Y validation,
>
> **[5:09](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=309)** and what is new here is the callbacks. So we are going to say callbacks equal to tensorboard callback.
>
> **[5:22](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=322)** So here in this step, we are training the model for 20 epochs. The validation data helps us monitor performance and the TensorBoard callback logs everything. After running this, we can start TensorBoard to visualize the training process. Next, we are moving on to the model evaluation. So once training is done, now it's time for us to evaluate the model on the test data to see how well it performs on unseen data. So we are going to again use mean squared error. We can do the evaluation sms_test equal to model.evaluate, and then we will have x_test and y_test. Next, as always, we are going to go ahead and print this, Mean Squared Error on Test Set, mse_test.
>
> **[6:27](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=387)** So we are evaluating the model on the test set and the mean squared error gives us a good measure of its performance here. Next we are going to go ahead and visualize the training and validation loss. So let's go ahead and do that. We are going to create a new function. First of all, let's comment the title of what we are doing. So this is a function to plot training and validation loss. So we will go ahead and create a function here. So it'll be called plot_loss, and it will take history here. Next we are going to go ahead and plt.plot and we will incorporate loss first, so history.history, open parenthesis and loss, comma, label, and we will label it as Loss. Let's not forget the ending quote here. Parenthesis always tell us by changing colors. So let's go ahead to the other line, plt.plot. This time, we are going to take care of validation loss. So history.history, and then we will call the validation loss here. Let's make sure we have underscore. Next label is validation loss. Then we have the X label, plt.xlabel,
>
> **[8:04](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=484)** and it'll have the epochs. Y label will be loss, plt.ylabel. That would be Loss, and then let's move on. We are going to have the plt.legend. Next we are going to have plt.grid, True, then we will go ahead, and save it into the output directory, so let's do that. plt.savefigure output, and then let's name the chapter and the session name loss_plot.png. Okay, this is our loss plot. So this was the function, and now we are going to go ahead and call this plot and save the loss. Let's pay attention to the lower and upper cases here, and then we will call the function plot_loss history. So let's go ahead and review what we've done. I have noticed that typo here, so let's go ahead and correct it to be label. So we defined a plot loss function, which takes care of the loss and validation loss, and we label the X axis as epochs, Y axis as loss. We do the legend, we take care of the grid,
>
> **[9:38](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=578)** and we save the figure into the output directory. Then we call this function to run the function on the history here. Okay, great, and when we scroll up, always make sure that we have everything spelled out correctly. So I'm noticing that we should have model.fit here. Perfect. Always double check everything. So what we have done here, we trained the model here. History equals to model.fit, x_train, Y_train, epochs equal to 20. We gave the validation data and we provided the callbacks. Next, we evaluated the model and then we are printing out the mean squared error on test set. Next, we define the function to plot training and validation loss. Finally, we call that function to save the loss figure. Great. So let's go ahead and open up the 04_01_end Python file to compare our work. Make sure that it's matching with the updates we have done to the begin file, and then once you verify that it's matching, let's go ahead and run this. Okay, our script ended with a .32 mean squared error on tested. Next, we are going to incorporate TensorBoard. So we will first navigate to the directory where our logs are saved, and we will run a command in the terminal to open it up.
>
> **[11:13](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=673)** So let's go ahead and do that. First of all, let's go and find where our logs are saved. We provided this in the code. They are saved under the logs/fit directory, so they are saved right here. So this is a great information to keep in mind because that's what we will provide right now. This is the key takeaway from this session. So we will go ahead and incorporate TensorBoard by typing tensorboard on the terminal and then --logged directory. This is where we provide where our logs live. So we gave it to be logs/fit from our code right here, so that's what we are providing here. So let's go ahead and click run to execute this. So what it'll do is it'll give us a URL, and we will go ahead and open this by clicking control and click. So it will open up a new webpage for us, and this is how we monitor and visualize the model's training. That is crucial for successful machine learning projects. This is a very, very important tool to monitor and analyze our results. So what are we seeing here? Well, on the left panel, we have several training runs listed, each identified by a timestamp and label, such as training and validation.
>
> **[12:46](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=766)** The different colors we see represent different runs or datasets, for example, training and validation. These runs are organized by the date and time, which allows us to compare different training sessions. And one folder right here is represented by the folder name. To showcase that, we can structure it by date or we can structure it by the chapter or folder names, whichever works for your project needs in the future. Then we see some tabs up above. So we see time series, scalers, graphs, distributions, and histograms. Time series is where we are currently focused right now, displaying how metrics evolve over time, and when we click on scalers, this tracks metrics like loss and accuracy. When we click on graphs, we can visualize the model architecture here and learn more about the models. And when we click on distributions and histograms, these show the distribution of weights and biases across layers in the model over time. Now let's come back to the time series and talk about bias histogram cards. So the main panel displays histograms for the biases in our models' layers. These histograms show how the biases are distributed
>
> **[14:23](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=863)** after each training step or epoch, which provides insights into how the model's parameters are evolving during training. We also see different colors like purple, orange. These correspond to different training runs. This allows us to compare how the distributions change across runs. Then we have the settings panel. In the settings panel, we can actually expand it or hide it. So in the settings panel, we can see and adjust the visualization. So we can change the horizontal axis or enable step selection here and adjust the smoothing of scaler plots. So we can do different setting adjustments here. So what this all means, well, we can compare multiple runs. The dashboard allows us to compare multiple training runs side by side, which is very useful for hyperparameter tuning or assessing the impact of changes in our model architecture. While we can monitor model health by visualizing the distribution of biases or weights, we can monitor the health of our model during training. For example, if the histograms are very narrow or skewed, it could indicate issues like vanishing or exploding gradients. And when it comes to [Fine Tuning](../../Skills/Software%20Development/Fine%20Tuning.md) visualizations, the setting allows us to customize how data is displayed. It makes it easier to interpret the results
>
> **[15:58](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/visualizing-success-with-tensorboard?u=76281980&t=958)** and make decisions about our training process. So overall, what we see in this TensorBoard dashboard is very powerful step for monitoring and comparing the training of different models or different rus of the same model. This helps us to make data-driven decisions about model adjustments and improvements.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (5), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (1), [Fine Tuning](../../Skills/Software%20Development/Fine%20Tuning.md) (1)
> **CLI Commands:** make (8), find (3), python (3)
> **Code Identifiers:** x_train (2), plot_loss (2), log_dir (1), y_train (1), sms_test (1)
> **UI Navigation:** click on (3), scroll up (1), navigate to (1)
> **Definitions:** is a  (4), we call this (1)
> **Env Vars:** src (2), url (1)
> **Analogies:** for example (2), such as (1)
> **Prerequisites:** set up (2), setup (1)

#### [Deep dives into training metrics](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=0)** - [Instructor] In this session, we are going to build on what we learned previously about TensorBoard and model training. So please meet me in your [Codespaces](../../Skills/Software%20Development/Codespaces.md), find the src folder from your left panel and find 04_02_begin.py file. So let's start with the begin code, and then make our way to match with the end code right here, which is 04_02_end.py file. So this setup is similar to what we have seen before, but it leaves out some key components that we will complete together and match to the end [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) file. So this part should be familiar. We are loading the california_housing data set, split it into training, validation, and test sets, and then standardize the data so that each feature contributes equally to the learning process. Next, we are building the model. We build a simple neural network model with two hidden layers and compile it using mean squared error, MSE as the loss function, and stochastic gradient descent, SGD as the optimizer. This is a basic feedforward neural network with two layers of 30 neurons each, followed by an output layer that predicts the housing price. So we have placeholders here for setting up TensorBoard logging and training the model. These placeholders indicate
>
> **[1:36](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=96)** where we will insert the TensorBoard logging functionality and training process. So we have a placeholder here where we define the log_dir, then we have the tensorboard_callback, then we have the training placeholder. So let's scroll up and add some of the libraries that are missing from this begin code in order to incorporate TensorBoard. So from [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md).keras.callbacks import TensorBoard. So from tensorflow.keras.callbacks import TensorBoard. And what else we would like to import here is we would like to import OS, import os, because we would like to define a certain folder instead of a datetime to log our model into the log directory. So after loading these two additional libraries, we can go ahead and move on with the TensorBoard logging setup. So let's scroll down to our placeholder here. Now we are going to complete this code. We will delete the none and then we will provide the os, operating system, path.join. So this is where we defined the directory for storing our logs. Imagine that you would like
>
> **[3:09](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=189)** to name a specific directory here. This is how you do it. So we say os.path.join. Next, we are going to give the directory. Okay, we want to be in logs, we want to be in fit, and then we want to be, let's give the chapter name here, 04_02. Next, let's go ahead and fill in the tensorboard_callback. So here, we will set up the log data, including histograms of weights and biases, which can be very useful when we visualize the training process later in TensorBoard. So let's go ahead and say TensorBoard, log_dir equals to up to the log directory we just defined up above. Then we say histogram_freq=1, and then we say histogram_freq=1. Next, we have the training placeholder. So let's go ahead and train the model. So this again is familiar, however, we will do this again just to remind ourselves that we will define a callbacks section in this training. So model.fit, parenthesis, X_train, y_train, epochs = 20,
>
> **[4:44](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=284)** validation_data=(X_valid, y_valid),
>
> **[4:54](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=294)** callbacks=tensorboard_callback, parenthesis.
>
> **[5:02](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=302)** So here, we train the model to include the tensorboard_callback in the callbacks parameter. Let's make sure we have correct parenthesis. We don't, we can tell by the red parenthesis here that it's complaining that it doesn't have a closing pair. So let's make it equal now with the closing parenthesis. So we train the model here to include the tensorboard_callback in the callbacks parameter. This will ensure that all relevant data is logged and ready for us to analyze in TensorBoard. So we will go ahead and evaluate the model next. So let's do that next. Evaluate the model on the test set. So this is familiar to us, msc_test, mean squared error, model.evaluate(X_test, y_test).
>
> **[6:08](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=368)** Then we will go ahead and print the result, f"Mean Squared Error in Test Set: {mse_test}". Then we go ahead and say plot the training and validation loss, plt.plot. We start with the loss history.history, and then go ahead and insert the loss, comma, we will label it as Loss. Okay, next we are going to give the validation loss. We can copy it. That way, it's quicker. All we do is change the loss to val_loss and the name to Validation Loss. After that, we go ahead and give the xlabel and ylabel, plt.xlabel('Epochs'), and then plt.ylabel('Loss'), then plt.legend(). And let's go ahead and save it to the output directory, plt.savefig, and then we are going to again save this to the output directory. Give the correct chapter name,
>
> **[7:40](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=460)** session name, loss_plot.png. So let's go ahead and give it another look. Make sure that everything is typed out correctly. So what we did is we went ahead and added two more imports here. One was the TensorBoard import, the other one was the operating system import. Then we already had load and prepare filled in, standardization filled in, build the model filled in. We compile the model, then we set up the TensorBoard logging. So here, we give what directory we want this to be saved, the logs to be saved, and then we go ahead and define a tensorboard_callback here, which takes where the log_dir is and it takes the histogram_freq here. Next, we train the model here, and the difference here was we provide the callbacks to be tensorboard_callback. Then we go ahead and evaluate the model with MSE. Next, we plot the training and validation loss here. So in this session, we have moved from a partially completed script to a fully functioning one that logs data to TensorBoard, trains a neural network, and evaluates the model's performance. TensorBoard is a very powerful tool that allows us to monitor and fine-tune our models in real time. This makes it an essential part of any deep-learning workflow. So go ahead and open the 04_02_end.py file.
>
> **[9:18](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=558)** So this should be matching with what we have done. Just give it another look and try to summarize to yourself what we have done here. What is the takeaway here? The takeaway again was we created a new directory here and then we set up TensorBoard logging in this section. We defined where we would like this log directory to be saved. We gave the settings we would like. And then finally, we can go ahead and give it a run here. So give it a couple minutes, and it ended with a .33, Mean Squared Error on Test. We can go ahead and open up the TensorBoard here again. But before that, let's summarize. This section introduced a more organized and structured approach to setting up TensorBoard using the os.path.join. So this is how we can join and define directories. We went ahead and filled in all the placeholders from the begin file and we reached to the end file. This section emphasized clean and consistent file management, particularly when we are dealing with like multiple experiments so that everything is organized. That way, we can find from the left panel that we have logs here. And then as you see, this shows as green, meaning that we just updated the 04_02 directory, which is a very organized way of saving our logs.
>
> **[10:52](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=652)** So we have train, we have validation, as opposed to just saving it with date and time, which also has its benefits. You can make the judgment on what your purpose is and why are you saving the logs. However, now we got to see two different ways of how we can log our model performance here. Now as a next step, we can again call our TensorBoard from the terminal, and open up a webpage to display our performance of the model here. So let's go ahead and start typing on the terminal window, tensorboard, dash, dash, and we have to give a log directory, which is logs/fit. That's where we saved out all of our data here. So we go ahead and click Enter, and it gives us a webpage here. So we Control + click on this to open it up, and there we have it, all of our data. So let's start from the left pane here. We have the left sidebar. This shows a list of all the different training and validation runs logged into the TensorBoard. So we can just select the chapter four, section two here. The color coding also helps us distinguish between different runs and phases. For example, if it's training or if it's validation. So we can go ahead and check only this folder, 04_02/train and validation for now.
>
> **[12:29](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=749)** Then we can go ahead and look at the bias/histogram first. So the bias/histogram shows the distribution of bias values in the neural network's layers during the training phase. The x-axis here represents the range of bias values, and the y-axis shows the frequency of those values. The spike or peaks in the histogram can indicate how the biases are being adjusted during the training. So bias is important. Monitoring the distribution of biases can give us insights into how the model is learning. If the biases are consistently high or low, it might indicate an issue with the learning rate or data distribution. Moving on to epoch_learning_rate. This graph shows the learning rate used during each epoch of the training. In this particular example, learning rate shows .01 across all epochs as indicated by the flat line. X-axis represents the number of epochs and y-axis shows the learning rate value. So why is this important? Understanding how the learning rate behaves during training is crucial because it influences the speed and stability of learning. A constant learning rate might be sufficient, but in some cases, we might want to adjust it dynamically. Moving on to epoch_loss. So let's go ahead and enlarge this. So this plot shows the loss over epochs for both training and validation phases.
>
> **[14:05](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=845)** So the x-axis here represents the number of epochs, which is 20, and then y-axis shows the loss value here. So you can make it larger or smaller by Alt + scroll. as so. So the blue curve represents the training loss here, while the pink curve represents the validation loss. So why is this important? This is important because it's a key graph for understanding how well our model is learning. Ideally, both training and validation losses should decrease and converge. If the validation loss is much higher or stops decreasing, it might indicate overfitting. So in our case, they are both decreasing and converging. Next, moving on to evaluation_loss_vs_iterations. Let's go ahead and click on it to enlarge and see the graph. So this graph shows the loss during evaluation. In other words, validation across different iterations. So the x-axis represents the iterations and the y-axis shows the loss value. The curve helps us see how the model's performance changes as it continues to evaluate on the validation set. It's important because it helps us diagnose how stable the model is during evaluation. If the loss continues to decrease,
>
> **[15:38](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=938)** it means the model is improving. However, if it fluctuates or starts to increase, we might be seeing signs of overfeeding. Then finally, let's go ahead and click on the kernel, which shows the kernel/histogram. This histogram displays the distribution of the kernel. In other words, weights. This is showing the weights during the training phase. So similar to the bias/histogram, the x-axis represents the range of kernel values, and y-axis here is showing the frequency. The peaks and shape of the histogram can indicate how the weights are being adjusted throughout the training. Why is this important? Monitoring kernel weights is crucial for understanding how the model parameters evolve over time. Significant shifts or patterns might indicate issues like vanishing gradients or improper initialization. So to summarize what's important here, the Runs panel on the left, it helps compare different training and validation sessions, and we can run and we can select them by clicking on these boxes or deselect them by clicking again. Then we went over the bias/histogram, which monitors the distribution of biases in the model. We talked about epoch_learning_rate, which shows the learning rate across epochs. Then we went over epoch_loss, which tracks training and validation loss over epochs.
>
> **[17:13](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/deep-dives-into-training-metrics?u=76281980&t=1033)** Then we went over evaluation_loss_vs_iterations. This evaluates model stability during validation. Then finally, we went over kernel/histogram, which observes the evaluation of weights during training. So by looking at these graphs, we understand how TensorBoard provides insights into the training process, which makes it easy to diagnose and improve the model performance. So in this session, we saw how we can organize our logs into a specific folder, and then how we can see the results from the TensorBoard and how we can evaluate them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (2), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **Code Identifiers:** tensorboard_callback (7), log_dir (3), histogram_freq (3), epoch_learning_rate (2), epoch_loss (2)
> **CLI Commands:** make (6), find (3), python (1)
> **UI Navigation:** click on (3), scroll up (1), scroll down (1), open the (1), select the (1)
> **Prerequisites:** set up (3), setup (2), initialization (1)
> **Definitions:** is a  (3), in other words (2)
> **Analogies:** similar to (2), imagine (1), for example (1)
> **File Paths:** 04_02_end.py (2), 04_02_begin.py (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/deep-learning-with-tensorflow-insights-and-innovations/next-steps?u=76281980&t=0)** - You made it! What a journey it's been. You've learned how to set up your environment, build powerful models, and tackle real world problems like predicting housing prices. But don't stop here, the skills you gain are just the beginning. Whether you want to refine your models, explore new data sets, or dive deeper into [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md)'s advanced features, the next step is yours to take. And remember, every experiment, every line of code, is a step closer to mastery. You've earned this, download your certificate and let the world know what you've achieved. Keep the conversation going, follow me on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md), visit my website @[digifab.ai](https://digifab.ai) and let's keep pushing the boundaries together. Thanks for trusting me to be a part of your learning journey. The world of AI is wide open and you've got the tools to make your mark. Keep going, keep experimenting, and I'll see you in the next course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** make (1)
> **URLs:** [digifab.ai](https://digifab.ai) (1)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)
> **Prerequisites:** set up (1)
> **Speakers:** - you (1)


## Instructor

- [Isil Berkun](../../Instructors/Software%20Development/Isil%20Berkun.md)

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/deep-learning-with-tensorflow-insights-and-innovations-3835125/codespaces)

## Skills Covered

- TensorFlow
- Artificial Intelligence (AI)
- Deep Learning

## Path Context

### In [Advance Your Skills in Natural Language Processing](../../Paths/Artificial%20Intelligence%20(AI)/Learning%20Paths/Advance%20Your%20Skills%20in%20Natural%20Language%20Processing.md)
← [Building NLP Pipelines with spaCy](Building%20NLP%20Pipelines%20with%20spaCy.md) | **4 of 6** | [AI Workshop- Build a Neural Network with PyTorch Lightning](AI%20Workshop-%20Build%20a%20Neural%20Network%20with%20PyTorch%20Lightning.md) →

## Appears In

- [Advance Your Skills in Natural Language Processing](../../Paths/Artificial%20Intelligence%20(AI)/Learning%20Paths/Advance%20Your%20Skills%20in%20Natural%20Language%20Processing.md)

## Related Courses

_Courses sharing skills:_

- [Deep Learning- Model Optimization and Tuning (2022)](Deep%20Learning-%20Model%20Optimization%20and%20Tuning%20(2022).md) — Artificial Intelligence (AI), Deep Learning
- [Deep Learning with Python- Optimizing Deep Learning Models](Deep%20Learning%20with%20Python-%20Optimizing%20Deep%20Learning%20Models.md) — Artificial Intelligence (AI), Deep Learning
- [Deep Learning with Python- Convolutional Neural Networks](Deep%20Learning%20with%20Python-%20Convolutional%20Neural%20Networks.md) — Artificial Intelligence (AI), Deep Learning
- [Deep Learning Getting Started](Deep%20Learning%20Getting%20Started.md) — Artificial Intelligence (AI), Deep Learning
- [TensorFlow- Working with NLP](TensorFlow-%20Working%20with%20NLP.md) — Artificial Intelligence (AI), TensorFlow

---

[↑ Back to top](#)