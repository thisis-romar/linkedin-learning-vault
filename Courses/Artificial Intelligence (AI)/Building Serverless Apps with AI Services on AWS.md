---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: building-serverless-apps-with-ai-services-on-aws
url: "https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws"
duration_minutes: 190
duration: 3h 10m
level: Intermediate
updated: 5/17/2024
learners: 1884
skills:
  - Serverless Computing
  - Amazon Web Services (AWS)
exercise_files: true
github: "https://github.com/LinkedInLearning/buidling-serverless-apps-AI-4454235"
thumbnail: "https://media.licdn.com/dms/image/v2/D4D0DAQFP8u36n9BS5g/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1715735518041?e=2147483647&amp;v=beta&amp;t=sbjC63InFEcvXgjVORp-8qXaoe_V1xbQMkCbkc5KTH4"
linkedin_topic: Artificial Intelligence (AI)
learning_paths:
  - '[Advance Your AWS Development Skills- Exploring Complex Application Development](../../Paths/Artificial%20Intelligence%20(AI)/Learning%20Paths/Advance%20Your%20AWS%20Development%20Skills-%20Exploring%20Complex%20Application%20Development.md)'
prev_courses:
  - '[AWS Security Best Practices for Developers](AWS%20Security%20Best%20Practices%20for%20Developers.md)'
next_courses:
  - '[Building Intelligent Chatbots on AWS](Building%20Intelligent%20Chatbots%20on%20AWS.md)'
path_nav: '[{"path":"Advance Your AWS Development Skills- Exploring Complex Application Development","position":5,"total":6,"prev":"AWS Security Best Practices for Developers","next":"Building Intelligent Chatbots on AWS"}]'
path_count: 1
tags:
  - course
  - topic/artificial-intelligence
  - topic/cloud-computing
  - topic/software-development
  - skill/serverless-computing
  - skill/amazon-web-services-aws
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Artificial%20Intelligence%20(AI)/Building%20Serverless%20Apps%20with%20AI%20Services%20on%20AWS.md)

![Building Serverless Apps with AI Services on AWS](https://media.licdn.com/dms/image/v2/D4D0DAQFP8u36n9BS5g/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1715735518041?e=2147483647&amp;v=beta&amp;t=sbjC63InFEcvXgjVORp-8qXaoe_V1xbQMkCbkc5KTH4)

# Building Serverless Apps with AI Services on AWS

> Looking to pair the flexibility and efficiency of serverless architecture with the power of generative AI? In this course, instructor Marcia Villalba shows you how to build serverless applications using AI services on AWS. The course focuses on integrating various services such as Amazon AI Service, Amazon Bedrock, AWS SAM, AWS Step Functions, and EventBridge. Along the way, practice your new skil

> [LinkedIn Learning](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws) | 3h 10m | Intermediate | 2K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Serverless apps with AI services on AWS](#serverless-apps-with-ai-services-on-aws)
  - [What you should know](#what-you-should-know)
  - [What you are going to build in this course](#what-you-are-going-to-build-in-this-course)
- [**1. Introduction to AI**](#1-introduction-to-ai) (5 videos)
  - [Introduction to AI](#introduction-to-ai)
  - [Introduction to Amazon AI Services](#introduction-to-amazon-ai-services)
  - [Introduction to Gen AI](#introduction-to-gen-ai)
  - [Introduction to Amazon Bedrock](#introduction-to-amazon-bedrock)
  - [Challenges when building AI and Gen AI applications](#challenges-when-building-ai-and-gen-ai-applications)
- [**2. Getting started with Serverless**](#2-getting-started-with-serverless) (3 videos)
  - [What is serverless?](#what-is-serverless)
  - [Why serverless is great to build AI apps](#why-serverless-is-great-to-build-ai-apps)
  - [Using Amazon Bedrock from serverless services](#using-amazon-bedrock-from-serverless-services)
- [**3. Infrastructure as code**](#3-infrastructure-as-code) (4 videos)
  - [The important role of infrastructure as code](#the-important-role-of-infrastructure-as-code)
  - [Introducing AWS SAM](#introducing-aws-sam)
  - [Installing and configuring AWS SAM](#installing-and-configuring-aws-sam)
  - [Getting started with the project](#getting-started-with-the-project)
- [**4. Introduction to Step Functions**](#4-introduction-to-step-functions) (9 videos)
  - [Understanding Step Functions](#understanding-step-functions)
  - [Creating your first workflow in the AWS console](#creating-your-first-workflow-in-the-aws-console)
  - [Creating a workflow as IaC](#creating-a-workflow-as-iac)
  - [Introduction to ASL](#introduction-to-asl)
  - [Introduction to flow states](#introduction-to-flow-states)
  - [Step Functions integrations](#step-functions-integrations)
  - [Modifying the workflow with simple integration](#modifying-the-workflow-with-simple-integration)
  - [Challenge: Modify the state machine to add an integration to AI service](#challenge-modify-the-state-machine-to-add-an-integration-to-ai-service)
  - [Solution: Modify the state machine to add an integration to AI service](#solution-modify-the-state-machine-to-add-an-integration-to-ai-service)
- [**5. Building a Workflow with AI**](#5-building-a-workflow-with-ai) (8 videos)
  - [Modifying the existing state machine to use Amazon Bedrock](#modifying-the-existing-state-machine-to-use-amazon-bedrock)
  - [Challenge: Integrate with Amazon Bedrock](#challenge-integrate-with-amazon-bedrock)
  - [Solution:  Integrate with Amazon Bedrock](#solution-integrate-with-amazon-bedrock)
  - [What is event-driven architecture?](#what-is-event-driven-architecture)
  - [How to trigger a state machine when there is a new S3 file](#how-to-trigger-a-state-machine-when-there-is-a-new-s3-file)
  - [How to send an email from the state machine](#how-to-send-an-email-from-the-state-machine)
  - [Challenge: Integrate to EventBridge event bus to send an event](#challenge-integrate-to-eventbridge-event-bus-to-send-an-event)
  - [Solution: Integrate to EventBridge event bus to send an event](#solution-integrate-to-eventbridge-event-bus-to-send-an-event)
- [**6. Build a Chatbot with GenAI**](#6-build-a-chatbot-with-genai) (9 videos)
  - [What is an Amazon Bedrock agent?](#what-is-an-amazon-bedrock-agent)
  - [What you are going to build](#what-you-are-going-to-build)
  - [Creating a Lambda function for the agent](#creating-a-lambda-function-for-the-agent)
  - [Creating an Open API for the function](#creating-an-open-api-for-the-function)
  - [What is an Amazon Bedrock knowledge base?](#what-is-an-amazon-bedrock-knowledge-base)
  - [Creating a knowledge base](#creating-a-knowledge-base)
  - [Creating an Amazon Bedrock agent](#creating-an-amazon-bedrock-agent)
  - [Challenge: Add another function to the agent](#challenge-add-another-function-to-the-agent)
  - [Solution: Add another function to the agent](#solution-add-another-function-to-the-agent)
- [**Conclusion**](#conclusion) (3 videos)
  - [What we built](#what-we-built)
  - [Cleaning up the environment](#cleaning-up-the-environment)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Serverless apps with AI services on AWS](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/serverless-apps-with-ai-services-on-aws?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/serverless-apps-with-ai-services-on-aws?u=76281980&t=0)** - [Artificial intelligence](../../Topics/Artificial%20Intelligence%20(AI).md) is in everybody's mind right now. But as a developer, how you can get started building AI applications without knowing about machine learning or being a data scientist? The answer is very relying on third party APIs that provide the functionality you're looking for. There are many services out there that offer [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) models ready to use and APIs to solve problems with AI. In this course, I will help you get started orchestrating AI applications using a programmatic approach and generative AI. I will cover services like AWS Step Functions, Amazon Bedrock, and many AWS AI services. I'm Marcia Villalba, principal developer advocate at AWS, and I have been developing software for over 20 years. I'm a big proponent of [Cloud Computing](../../Topics/Cloud%20Computing.md), serverless and automation. So let's get started with this course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (2), [Artificial intelligence](../../Topics/Artificial%20Intelligence%20(AI).md) (1), [Cloud Computing](../../Topics/Cloud%20Computing.md) (1)
> **CLI Commands:** aws (3)
> **Env Vars:** aws (3)
> **Speakers:** - artificial (1)

#### [What you should know](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-you-should-know?u=76281980&t=0)** - [Instructor] This is an intermediate level course. In order to follow along, you need to have a bit of knowledge in different areas. You will code part of the project of this course using [Node.js](../../Skills/Web%20Development/Node.js.md) and [JavaScript](../../Skills/Software%20Development/JavaScript.md). It'll be great if you have some experience with this programming language. If you don't have any, you can find basic courses on Node.js in our library. However, if you're a senior developer with experience in another language, I'm pretty sure that you can follow along with no problems. You don't need to have any knowledge of AWS. During the course, you will need an AWS account. If you don't have one and you don't know how to create it, check the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning Library for a course that will help you. Many of the services that we are going to use are covered in the AWS Free Tier. However, Amazon Bedrock knowledge base has a cost during the time it's used. This is why it's so important that if you stop working in this course, you go to the last section of the course, and follow the cleanup instructions to remove all the resources that you have deployed. I will be developing the projects of this course using Visual Studio Code on a Mac. However, you can use any code editor you feel comfortable with and follow the course on [Windows](../../Glossary/Service/Windows.md), or [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) machine as all the tools are supported in all the platforms.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Node.js](../../Skills/Web%20Development/Node.js.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [Windows](../../Glossary/Service/Windows.md) (1), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1)
> **CLI Commands:** aws (3), node (2), find (1)
> **Env Vars:** aws (3)
> **File Paths:** node.js (2)
> **Tools:** visual studio (1)
> **UI Navigation:** go to (1)
> **Definitions:** is an  (1)
> **Prerequisites:** you need to have (1)

#### [What you are going to build in this course](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-you-are-going-to-build-in-this-course?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-you-are-going-to-build-in-this-course?u=76281980&t=0)** - [Narrator] In this video, let's look at what problems you are going to solve during this course. I will call these problems the projects. There will be two projects in this course, and by the end of the course, you'll have two working applications that you can use to keep experimenting and learning. These projects come from use cases and working experience. They're a simplification of very common problems that organizations face. The first project is a document processing application in which the users upload a video or audio file to permanent storage, and that file gets processed in real time. The processing of these files involves different AI services like transcription, translation, and [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md). When the workflow completes different parts of the process notifies the stakeholders by sending an email or by putting an event into an event bus so other applications can process the results further if needed. The second project is a chatbot that talks in real time with the users in natural language. The user requests to chatbot the weather of a city and the chatbot will get this information from external APIs and internal data sources and provide the information to the customer in a friendly manner. Both projects are example of orchestration of AI applications, and in this course you'll learn how these two projects differ and how much they resemble, and how you can build similar solutions for your use cases.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (1)
> **Definitions:** is a  (2)
> **Speakers:** - [narrator] (1)


### 1. Introduction to AI

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction to AI](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-ai?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-ai?u=76281980&t=0)** - [Presenter] This course is called Building Serverless Applications with [Artificial Intelligence](../../Topics/Artificial%20Intelligence%20(AI).md). That's why I want to start by introducing what artificial intelligence means and some important concepts. Also, I want to show you some use cases where you can see artificial intelligence in action. Artificial intelligence or AI is, as I will refer it in the future, is a general term for making computers perform some tasks that require human intelligence. As developers, we code a set of instructions on the computer and the computer follows the steps in order to perform a task. When working with an AI applications, developers don't code the steps. They just ask the computer to use its intelligence to produce a result. For example, we might want the computer to tell us what it sees in an image or to analyze the sentiment of a text, or to classify some items in different categories, or we can ask the computer to write a story based on a prompt. There are many techniques in order to get a computer to think to produce this intelligent response. All these techniques are under the AI umbrella. Machine learning or ML is a technique that makes computers learn from data and experience to improve their performance on some task. Machine learning uses different [Algorithms](../../Skills/Software%20Development/Algorithms.md) to learn from the data and make inferences without explicit programming. Then there is [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md). That is a subset of ML. Deep learning uses multi-layered
>
> **[1:33](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-ai?u=76281980&t=93)** artificial neurals network. This categorization tries to mimic how the human brain works. [Artificial Neural Networks](../../Skills/Artificial%20Intelligence%20(AI)/Artificial%20Neural%20Networks.md) learn by example, similar to how humans learns. This consists of at least three layers of networks: an input layer, a hidden layer, and an output layer. Each layer contains nodes or neurons, which have weighted inputs that compute the output. The value of the weights is what needs to be trained to produce a desire output. And finally, there is [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md). That is a subset of deep learning. We'll have a full video later in this section about this topic. Another way to classify AI is by narrow AI and general AI. Narrow AI is about getting machines to do one task very well, like image recognition or playing chess. General AI means computers can do everything that humans do and more. We are not there yet. AI has been around for a while. For example, many organizations have trained models on how to detect fraudulent operations. For this, they use different classification algorithms that can label a transaction as fraudulent or not. Personalized recommendations is another use case that you might have seen in your favorite [E-Commerce](../../Skills/Web%20Development/E-Commerce.md) or streaming platforms. These companies train machine learning models with customers behaviors and obtain personalized recommendations for those specific customers improving their experience.
>
> **[3:09](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-ai?u=76281980&t=189)** Your email service provider might detect spam. These companies train specific models to recognize unwanted spam emails and remove them from your inbox. Translation tools: You might have used an automated translation tools many times in which you input the original text in one language and get the result in another. This is achieved by training language-specific models to translate. These models can not create new text. They just translate [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) by word, and in some cases, they can understand common phrases. There are many more use cases. I cannot cover them all here, but AI nowadays is everywhere and more and more developers are asked to build applications that use it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Artificial intelligence](../../Topics/Artificial%20Intelligence%20(AI).md) (4), [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) (3), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (2), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (2), [Artificial Neural Networks](../../Skills/Artificial%20Intelligence%20(AI)/Artificial%20Neural%20Networks.md) (1)
> **Definitions:** is a  (4), is called (1)
> **Analogies:** for example (2), similar to (1)
> **CLI Commands:** make (1)
> **Cross-References:** later in (1)
> **Speakers:** - [presenter] (1)

#### [Introduction to Amazon AI Services](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-amazon-ai-services?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-amazon-ai-services?u=76281980&t=0)** - [Instructor] In the previous video, you learned what AI is. Let's go one step further and look what services you can find in AWS to help you build AI applications. At AWS, there is the AI ML stack that represents all the services and solutions that AWS offers to their customers to build applications. The stack is divided into three layers. The first layer is the ML frameworks and infrastructure. This layer is usually for ML experts and providers. You can find here the different frameworks that AWS offers, the different silicone solutions like Inferentia and Trainium and the core service that is [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) to to run all these. Organizations that are experts in ML and AI use this layer to build everything from scratch. The second layer is the Amazon SageMaker layer. This layer is for the ML experts that want to tune existing models and take advantage of all the tools that AWS offers. The top layer is the AI services that are catered for the consumers. This is a layer we are going to focus this course on. Here you can find our [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) services that we are going to talk about in the following video, and also the specialized and core AI services. Lets look at this layer in a little bit more detail. The services that are in this layer are ready to use AI services that allow you to add intelligence to your applications.
>
> **[1:34](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-amazon-ai-services?u=76281980&t=94)** These services integrate with your applications easily using an API, and you can use them to address common use cases like recommendations, recognize things in images and videos, and the text frauds. If you navigate to the AWS AI Service page, you can find all the services that are inside the category. If you navigate to the AWS AI Service page, you can find all the services that are inside this category, group by categories. And if you click in the AI service tab on that page, you can go directly to each service and learn more about it. All these services might seem very different, but they are easily integrated in your applications using APIs or the AWS SDK as we are going to see later in this course. One of the AI services that you're going to use in this course is Amazon Transcribe. That is a service that automatically converts a speech to text. This is a very useful service as you can pass a video or audio file, and it returns a transcript of ease. Transcribe has many features. One of the most useful is that you can obtain a subtitle file from a video or an audio. Later in this course you'll learn how to use transcribe in your applications.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) (1), [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (1)
> **CLI Commands:** aws (8), find (5)
> **Env Vars:** aws (8), ec2 (1), api (1), sdk (1)
> **Cross-References:** later in (2), previous video (1)
> **Definitions:** is a  (3)
> **UI Navigation:** navigate to (2)
> **Speakers:** - [instructor] (1)

#### [Introduction to Gen AI](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-gen-ai?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-gen-ai?u=76281980&t=0)** - [Instructor] When we introduced AI in this section, we also briefly introduced [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) as a subset of [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md), but we didn't talk about it. Let's do that in this video. To understand a bit better why generative AI or [GenAI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) is so powerful, you need to understand the two types of ML models that are out there. Deterministic models and probabilistic models. First, there are deterministic models. These are the traditional models where data scientists train a model with data sets for a specific problem. For example, classifying images. The data is labeled and then the model is trained with that data. These models can later predict the label of a new image. For example, you can train a model to classify images based on your product categories, and when there is a new image, the model can add the right label to that image based on what it learned. These models always return the same result for the same input, so given an image, you always get the same category. Then there are the probabilistic models. These models are also trained with large data sets and they're trained to generate the next token given an input. The probabilistic models might return different answers to the same input. These are the models that we will see in generative AI. That is why when we ask the same questions to GenAI chatbot, it may produce a different answer. A generative AI model always tries to predict what is the next token given an input.
>
> **[1:34](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-gen-ai?u=76281980&t=94)** For example, if we say to the model, Today, I want to, the model will try to fill the gap with store, [office](../../Skills/Web%20Development/Microsoft%20Office.md), mall, but not with tomato, as tomatoes are not a place. The next token generated based on the input and the learned experience of the model. Therefore, generative AI is a type of AI that can create new content, like videos, images, stories, audios, and more. AI attempts to mimic human intelligence and GenAI is the next step in this journey. GenAI works with ML models, very large models that are pre-trained on a vast amount of data. These are called foundational models. Foundational models or FMs are ML models trained on a broad spectrum of generalized and unlabeled data, and they can perform a variety of general task. You can see different use cases for GenAI nowadays in applications that use it to enhance customer experience, to boost employee productivity, or to optimize business processes. For example, chatbots or virtual assistance are a great example of a use case for generative AI to improve customer experience, as they can interact with customers using natural language and perform different actions that the customer needs. In this course, you will learn how to build a simple chatbot. Code generation or [Content Creation](../../Skills/Software%20Development/Content%20Creation.md) tools are great examples of how AI helps to boost employees' productivity.
>
> **[3:08](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-gen-ai?u=76281980&t=188)** For example, Amazon Code Whisperer is a code companion that you install in your IDE, and it will do code recommendations as you type, making you solve problems faster. Another use case for GenAI is to process documents. You're going to see an example of this in this course when we are going to take a video and automate the processing of it using AI and GenAI.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Genai](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (7), [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (6), [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) (1), [Office](../../Skills/Web%20Development/Microsoft%20Office.md) (1), [Content Creation](../../Skills/Software%20Development/Content%20Creation.md) (1)
> **Analogies:** for example (5)
> **Definitions:** is a  (3)
> **Env Vars:** ide (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)

#### [Introduction to Amazon Bedrock](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-amazon-bedrock?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-amazon-bedrock?u=76281980&t=0)** - [Instructor] Amazon Bedrock is the AI service that you are going to be using in this course for your projects. But what is Bedrock? Bedrock is a fully-managed service that offers a selection of high-performing foundational models along with a broad set of capabilities to build [gen AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) applications. Let's go into that definition and break it out into parts. Bedrock is a serverless service. You will learn what it means to be serverless in the next section. Bedrock offers many foundational models. Bedrock hosts those foundational models, and you, using the same API, can switch the model of your application. You can find models from AI21, Anthropic, Cohere, Meta, Stability AI, and Amazon. All these models can be used in different use cases, so you have to choose the right model for the right problem. You can also adapt your models with data, either by fine-tuning them or using your own datasets or by creating knowledge basis of your data. At the end of the course, you are going to learn how to create these knowledge bases. And then Bedrock offers a set of capabilities to build gen AI applications, capabilities that range from RAG, [retrieval-augmented generation](../../Skills/Artificial%20Intelligence%20(AI)/Retrieval-Augmented%20Generation%20(RAG).md), Bedrock agents, Guardrails, and many other features that allows you to build secure gen AI applications.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Gen ai](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (3), [Retrieval-augmented generation](../../Skills/Artificial%20Intelligence%20(AI)/Retrieval-Augmented%20Generation%20(RAG).md) (1)
> **Env Vars:** api (1), ai21 (1), rag (1)
> **Definitions:** is a  (2)
> **CLI Commands:** find (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Challenges when building AI and Gen AI applications](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenges-when-building-ai-and-gen-ai-applications?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenges-when-building-ai-and-gen-ai-applications?u=76281980&t=0)** - [Instructor] Up to now, you learned what AI and [Gen AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) are, their use cases, and what AWS offers you to build those AI applications. But if you are taking this course, most probably you're a developer or an architect, not a data scientist, or maybe not an expert in ML. You might be wondering how I can build AI applications. That is a very valid question. Nowadays, as you saw in this section, you don't need to be an expert in ML to build intelligent applications. You can rely on AI services provided by AWS or third parties to build your applications. At the end, everything is an endpoint. It is important that you think of these AI services as you were using any other API in the past. Building applications using APIs to external services is very common. Calling a booking service, a reservation API, or a ticketing system. Now there are APIs for AI services. However, some of these APIs are like APIs we are familiar with. You send the request formatted in a specific format and you get a response in a format that your application is familiar with. In other cases, like when working with Gen AI and foundational models, you need to craft these calls using what is known [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md). This course won't get into the details of prompt engineer, but you can find in the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning Library a course dedicated to this topic. Another challenge you will face
>
> **[1:35](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenges-when-building-ai-and-gen-ai-applications?u=76281980&t=95)** when building AI applications is that you'll need to orchestrate how you are going to call all these different AI services. You will need to transform the data from one service to another, chain the prompts together, or stitch different AI services together. In this course, we are going to focus on this challenge. You'll learn two different strategies to orchestrate your applications, one programmatic and one using [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) and agents.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Gen ai](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (2), [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (1)
> **Env Vars:** aws (2), api (2)
> **CLI Commands:** aws (2), find (1)
> **Definitions:** is a  (1), is an  (1)
> **Cross-References:** as you saw (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)


### 2. Getting started with Serverless

[↑ Back to Table of Contents](#table-of-contents)

#### [What is serverless?](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-serverless?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-serverless?u=76281980&t=0)** - [Instructor] We already introduced AI in the previous section. Now it's time to introduce the other keyword in the name of this course: Serverless. What does it mean that an application is serverless? One of the first things people say to me whenever I mention the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) serverless is that there are still servers in serverless. Yes, there are. However, with serverless, we don't manage those servers. They're abstracted away. Similar analogy happens with virtual servers. They are bare metal machines running providing capacity to the virtual servers, but you don't need to manage them. Serverless applications are some levels of abstraction up. There are three main characteristics of serverless services. You don't need to manage infrastructure, or your configuration of infrastructure is minimal, you pay for what you use and the service automatically scales up and down following your needs. When people think of serverless, they think about compute, and that is not complete. Serverless services can be a compute service in which to run your code, as well as managed services that you can use to build your applications. Sometimes those services are refer as functions as a service, or FaaS. And the managed services are back end as a service, or BaaS. Most serverless applications are composed of managed services with functions to implement the specific business logic. Back end as a service
>
> **[1:31](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-serverless?u=76281980&t=91)** or managed services are quite an old concept. Amazon S3 was one of the first back end as a services and AWS launched it in 2006. S3 allows you to store objects in the cloud without provisioning any storage infrastructure. It scales the storage capacity as much as you need, and you pay exactly for what you use. In this course, you will learn a lot of managed services and the AI services fall in this category, Amazon Bedrock as well, and also AWS Step Functions and EventBridge, two services that provides a lot of the logic for the applications that you will be building in this course. One of the first example of function as a service is AWS Lambda. AWS Lambda was launched in 2014. Lambda allows you to run code in the cloud without the need to deploy any server. Just write a simple function and upload it to the cloud. You are going to use AWS Lambda together with Amazon Bedrock in one of the last section of this course to provide business logic to a [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) chatbot.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (1)
> **CLI Commands:** aws (5)
> **Env Vars:** aws (5)
> **Speakers:** - [instructor] (1)

#### [Why serverless is great to build AI apps](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/why-serverless-is-great-to-build-ai-apps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/why-serverless-is-great-to-build-ai-apps?u=76281980&t=0)** - [Instructor] Now you know what serverless means. You learn about what services AWS offers to build AI applications and you know some challenges that developer face when building AI applications. Let's talk about why serverless is a great technology to build AI applications. The first reason is that all the AI services that AWS offers and also Amazon Bedrock are serverless. You don't need to provision or manage any infrastructure to use these services. You have an API that you call from your applications and AWS does all the heavy lifting for you. An architecture like this is recommended using other serverless components to embrace the [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) of the application. So all the different services that [compose](../../Glossary/Framework/Jetpack%20Compose.md) the application can scale together, and you don't need to plan the capacity you need for your application. For orchestration of the data and calling these endpoints, serverless is a great way to go, as there are already services built in AWS that provides solutions to these problems. For example, you can use AWS Step Functions to create the logic for the application, call all the different services, and transform the data as you need. Step Functions is a serverless service that provides [Workflow Management](../../Skills/DevOps/Workflow%20Management.md) with no code start and scalability built in. You can also take advantage of Amazon EventBridge for creating an application in real time that responds to different events, such as customer uploading a file or clicking a button. Amazon EventBridge is a fully managed event bus that allows you to route events
>
> **[1:33](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/why-serverless-is-great-to-build-ai-apps?u=76281980&t=93)** between different services and application without the need to provision any infrastructure. Or you can use AWS Lambda to add business logic to your application or to perform complex data transformations. AWS Lambda is a serverless compute service that scales according to the usage. In all the serverless services, you don't need to pay for any infrastructure that you don't use. One of the main benefits of using serverless services like this is that you don't need to write a lot of extra code or boilerplate or code that doesn't add any value. The code you don't write is the code that you don't need to maintain in the future. Another benefit is that because all the services are serverless, you only pay for the resources you consume. You can iterate your application, build multiple versions very fast without worrying about having a lot of resources that are not used.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) (2), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1), [Workflow Management](../../Skills/DevOps/Workflow%20Management.md) (1)
> **Env Vars:** aws (7), api (1)
> **CLI Commands:** aws (7)
> **Definitions:** is a  (5)
> **Analogies:** for example (1), such as (1)
> **Exercise Files:** boilerplate (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [instructor] (1)

#### [Using Amazon Bedrock from serverless services](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/using-amazon-bedrock-from-serverless-services?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/using-amazon-bedrock-from-serverless-services?u=76281980&t=0)** - [Instructor] You can use Amazon Bedrock from AWS Lambda and AWS Step Functions with ease. If you want to use Bedrock with AWS Lambda, you just need to use the AWS SDK. The AWS SDK is a [Software Development](../../Topics/Software%20Development.md) kit and it allows you to call the AWS services using an API. In this example, I'm using AWS SDK for [JavaScript](../../Skills/Software%20Development/JavaScript.md) version 3 to invoke a Bedrock model. You need to pass the configuration to the SDK and invoke the model. The configuration contains the prompt, some data that the model needs, and then the model name. The result of invoking the model will be returned to the function. If you're using Amazon Bedrock from Lambda, you need to give permissions to the function to access the particular foundational model that you want to invoke. For that in your function configuration, you need to give that permission. In this example, I show you how you can configure it with AWS SAM. We are going to learn more about SAM in a following video. I will leave you a video with some resources for demos using Lambda if you want to go this way, as I will not cover this in the course. If you want to use Bedrock with Step Functions, there is even a simpler way. In the workflow, you can just drag and drop Bedrock integration and configure it accordingly. Don't worry if you don't know this part yet. I will show you how to do Bedrock with Step Functions in detail in a future video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Software Development](../../Topics/Software%20Development.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Env Vars:** aws (8), sdk (4), sam (2), api (1)
> **CLI Commands:** aws (8)
> **Prerequisites:** configure (2)
> **Versions:** version 3 (1)
> **UI Navigation:** drag and drop (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 3. Infrastructure as code

[↑ Back to Table of Contents](#table-of-contents)

#### [The important role of infrastructure as code](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/the-important-role-of-infrastructure-as-code?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/the-important-role-of-infrastructure-as-code?u=76281980&t=0)** - [Instructor] I know this is a serverless and AI course. You might be wondering why there is even a video about this topic. Because [infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) is one of the fundamental practices when working with serverless applications in production at the scale. Let's start by answering the question, what is infrastructure as code? Infrastructure as code involves using a high level programming language to control the infrastructure of IT services. When you treat your infrastructure as code, you can start applying the same techniques to the infrastructure that you apply to the code, like testing, code reviews, automatic testing, and others. When the infrastructure is in the coded form, you can replicate it many times, minimizing the errors. All of these will improve the quality of your infrastructure. Infrastructure as code means that you are not going to the console of your cloud provider and type and click and tick check boxes to create the infrastructure. It means that you are going to create scripts in where you define the infrastructure of your application. You will define your Lambda functions, your step functions workflows, the rules that you're going to use for event breach, and any AWS resource as code. It can be in YAML, [JSON](../../Skills/Web%20Development/JSON.md), or even [JavaScript](../../Skills/Software%20Development/JavaScript.md) or [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), depending on the infrastructure as code framework you choose. You then store these as scripts in a code repository as you do with code and put them through code reviews and tests. As with code, when these scripts change, you have a register of who made the changes and why.
>
> **[1:35](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/the-important-role-of-infrastructure-as-code?u=76281980&t=95)** And you don't need to go to the AWS console to create these resources. You can store all the code in the code repository and use CI/CD pipelines to deploy the infrastructure automatically and replicate the same infrastructure in multiple AWS accounts. Some advantages of infrastructure as code are, you can manage the infrastructure programmatically. No need for manual configurations that can generate many errors. It's possible to deploy the same infrastructure into multiple environments such as production, testing, and development. One employee can manage a massive infrastructure. You gain development speed as you can now reuse pieces of your infrastructure you already have in other projects and evolve the infrastructure with more security. And the chances of creating bugs get reduced. There are many ways to build applications using infrastructure as code. If you're working with AWS, the native way is to use AWS CloudFormation, or you can use AWS SAM Serverless Application Model like we are going to do in this course. Later in this chapter, you will learn more about AWS SAM. Or you can use AWS CDK [Cloud Development](../../Skills/Software%20Development/Cloud%20Development.md) Kit, a framework that allows you to define [Cloud Infrastructure](../../Skills/Cloud%20Computing/Cloud%20Infrastructure.md) with real code. Or some third party framework like [Terraform](../../Skills/DevOps/Terraform.md), serverless frameworks, and many others. It doesn't matter which one you choose. The important thing is that you use one.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (8), [JSON](../../Skills/Web%20Development/JSON.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Cloud Development](../../Skills/Software%20Development/Cloud%20Development.md) (1)
> **Env Vars:** aws (8), sam (2), yaml (1), json (1), cdk (1)
> **CLI Commands:** aws (8), python (1), cd (1), terraform (1)
> **Definitions:** means that (2), is a  (1)
> **Cross-References:** later in (1)
> **Tools:** aws console (1)
> **UI Navigation:** go to (1)
> **Analogies:** such as (1)

#### [Introducing AWS SAM](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introducing-aws-sam?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introducing-aws-sam?u=76281980&t=0)** - [Instructor] In the previous video, you learned that building serverless applications directly on the AWS console is tedious and prone to error. Usually, this is not the way you want to build production applications. For production applications, you want to have a process that can be maintained over time and replicated in different environments, such as testing, development, and production. When building serverless applications for production, it's recommended to use [Infrastructure as Code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md). You can implement the Infrastructure as Code practice in different ways when building serverless applications. One way is to code the infrastructure directly and package the scripts and deploy them to the cloud. Another way is to use a framework that already has some of the tools to assist you in developing and deploying applications. In this course, you will use AWS SAM, AWS SAM, Serverless Application Model. It's an open source framework for building serverless applications. AWS SAM has two parts. First, the transform templates. These templates help you to express serverless infrastructure with a simple notation. You can easily define functions, APIs, and database tables that are quite common resources needed in serverless applications. The infrastructure is defined in a YAML file, and then it can be deployed with the help of the SAM [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) to the cloud. That is the second component of AWS SAM. Some templates are built on top of AWS CloudFormation. During the packaging process, some templates get transformed
>
> **[1:33](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introducing-aws-sam?u=76281980&t=93)** into CloudFormation templates, and because of this, you can also use CloudFormation syntax to extend some templates. AWS CloudFormation is the AWS Infrastructure as Code syntax for defining AWS resources. This is very portable within different accounts, and it's very maintainable, as some templates contains the definitions and configuration of the AWS services that make your application. All these can be deployed as one entity. Don't be afraid of the code here. We are going to get into that in the following videos. SAM comes with a rich set of features available to developers. AWS SAM local. SAM local allows you to do local testing and debugging of your cloud applications. It provides an emulation of some of the serverless services in your local machines so you can test without taking your code to the cloud. AWS SAM Accelerate. SAM Accelerate allows you to increase your development speed when working on Lambda functions by making parallel builds of multiple components, allowing you to update your code in the cloud without redeploying the infrastructure or getting all the logs for your functions aggregated in your terminal. Code deployment. SAM integrates with many CI/CD tools, like AWS CodeBuild, AWS CodeDeploy, and AWS CodePipeline. Also, it provides simple commands to create CI/CD pipelines using the AWS SAM Pipelines command. If you want to learn more about AWS SAM,
>
> **[3:05](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introducing-aws-sam?u=76281980&t=185)** check the product page so you can learn more about the different features it provides. This course uses SAM to build and deploy the infrastructure, but it will not cover many of the advanced features that SAMs provide.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (3), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (1)
> **Env Vars:** aws (17), sam (14), yaml (1), cli (1)
> **CLI Commands:** aws (17), cd (2), make (1)
> **Tools:** aws console (1), terminal (1)
> **Cross-References:** previous video (1)
> **Analogies:** such as (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [instructor] (1)

#### [Installing and configuring AWS SAM](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/installing-and-configuring-aws-sam?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/installing-and-configuring-aws-sam?u=76281980&t=0)** - [Instructor] Now that we know why it's important to use a framework like AWS SAM for developing serverless application, let's install it and configure it in your computer. Before installing AWS SAM, you need to have an AWS account, an IAM admin user, an access key and secret key for the user to access AWS programmatically. If you don't have those prerequisites, follow the video creating IAM users in the Learning Lambda course in the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning Library. Also, you need to have an AWS [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) installed and configured in your machine. For doing that, you can follow the installations instructions in the installation guide of the AWS CLI for your operating system. After you install the CLI, you can check the version that you have installed by going to your terminal and typing aws --version. In the AWS SAM prerequisites, you can see all the prerequisites I already mentioned. Have your AWS account, create your IAM users, have an access key and secret access key, install the AWS CLI, and finally, you need to use the AWS CLI to configure the AWS credentials. You can check those steps and follow along before installing SAM. When you have fulfilled all the SAM prerequisites, you can move on to the installation instructions. You can find them in the SAM page, and if you go down,
>
> **[1:37](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/installing-and-configuring-aws-sam?u=76281980&t=97)** you can see the installation instructions for each of the operating systems. Now you have all the prerequisites from AWS perspective, but you might be missing Docker in order to run SAM, so if you don't have it, install it, and follow the installation instructions from SAM CLI in your operating system. When you're ready, you can go to your terminal and type sam --version to make sure that it's installed and the latest version has been installed in your computer.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (6), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Env Vars:** aws (12), sam (8), cli (6), iam (3)
> **CLI Commands:** aws (13), find (1), docker (1), make (1)
> **Prerequisites:** install (4), configure (2), you need to have (2)
> **Tools:** terminal (2)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)

#### [Getting started with the project](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/getting-started-with-the-project?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/getting-started-with-the-project?u=76281980&t=0)** - [Instructor] In this video you're going to create the AWS sam project that you will be using for half of the course for the project1. The first thing we want to do is to create a folder in where to put that project. So I will make mkdir project1 and then just go inside. Good. There I can create a template file, touch template.yml. This will allow me to put the all the infrastructure inside a file. We are going to do that in a second, at least for just a demo example. And then I'm going to create another file called app.js. And here is where I will put the code of my function that we are going to test. So if we go to the Visual Studio Code, we can see the two files. They're empty. So the first thing we want to do is to just add some infrastructure. This is a super simple sam template that we know is a sam template because of this, we tell CloudFormation to transform this template into CloudFormation is a sam template. And then we have only one resource that is our function and it's the type of a function. And then we have the properties where they can find the code in the root directory. And the handler is where in which file the code is, in this case in the app.js, in the method handler, and then the runtime for our function that is node 20. So we can save this and then we can go to the code for our function. As well it's empty, but we can write something.
>
> **[1:36](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/getting-started-with-the-project?u=76281980&t=96)** We can put in the handle method because that's the method that we define in the infrastructure. I don't know, whatever we want the function to do that is in this case run Hello World. Now we are ready to deploy this to the cloud. In order to deploy to the cloud, we are going to use sam. We will do the command sam deploy --guided. This will guide us through the deploying configuration and we will need to run this command only once when we deploy the project for the first time. So let's put a name, sam [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) project 1. Then we need to pick the AWS region. I have this region by default and the region in this case is important because we are going to use Bedrock for our project. And Bedrock is not yet available in all AWS regions. So I'll recommend you to pick either Virginia, Oregon, or other regions where Bedrock is available. I'm pick Oregon. That is the one that you have seen there. That is US West 2. So I will leave that one and then I will go through all the defaults because I don't need any more configuration for this. What is happening here, a new file called samconfig.toml is getting created with all this configuration in place and then it's creating all the resources for my CloudFormation stack. In this case, it's just role and a function that later will be expanding into a lot of more things,
>
> **[3:09](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/getting-started-with-the-project?u=76281980&t=189)** but for now it's just a simple role and a function. Now we can see in the terminal that the creation of these resources is in process. It will take a few minutes to get it done. And we can see also in our AWS account if we go to CloudFormation that the stack is being created. So you can see here sam linkedin project and we can see exactly the same events that we have in on the terminal. So now the creation is complete. So we can go back to our terminal and we can run the invoke to see if this function is working. So this is the command that we need sam remote invoke function. And the stack name that we just put the MyFunction is the name of the resource in the CloudFormation template. And we can see here that the invoking of the function is working, it returned a Hello World so we are good. Our first function is working.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2)
> **CLI Commands:** aws (4), make (1), mkdir (1), find (1), node (1)
> **Exercise Files:** template (7)
> **File Paths:** app.js (2), template.yml (1), samconfig.toml (1)
> **Env Vars:** aws (4)
> **Tools:** terminal (3), visual studio (1)
> **UI Navigation:** go to (3)
> **Definitions:** is a  (3)


### 4. Introduction to Step Functions

[↑ Back to Table of Contents](#table-of-contents)

#### [Understanding Step Functions](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/understanding-step-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/understanding-step-functions?u=76281980&t=0)** - [Instructor] In this course you learn about AI and the challenges that developers face when building these kind of applications. One challenge that we mentioned was the need to orchestrate the calls to the different AI services, how to transform the data from one service to another, how to chain prompts together, or how to stitch different services together. This challenge is solved by orchestrating the application. There are many ways to do that. We could build a big function or method that does the orchestration. The main issue with the solution is that all the calls are coupled together, and if we want to change something, we need to change also the main function. In addition, testing and maintaining this piece of code is complex as it has so many integrations with different services. Also, this code doesn't really add any value as it's mostly boiler plate to call different services. In addition, if the orchestration is too complex, the code might be hard to read and understand in the future. What if we could replace that orchestration code with something more visual, something that when you open it after six months, you can tell exactly what it's doing. For that, we can use a service that allow us to build workflows to orchestrate the calls to the multiple APIs. As we are in the AWS world, we can use AWS step functions for this purpose. AWS step functions is a serverless workflow service that make it easy to coordinate multiple AWS services,
>
> **[1:35](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/understanding-step-functions?u=76281980&t=95)** calls to third-party APIs and other integrations into scalable, serverless workflows. As the functions uses a visual workflow that are made of a series of steps, workflows are made of steps that can invoke different APIs in a reliable and scalable matter. This steps perform a discrete operations such as calling an API or executing Lambda function. In addition, steps can be connected in different ways like in parallel or in series. Later in this section, you will learn about the different flow mechanisms. A step function's automatically triggered, and track each step, handles the errors, and follow the workflow definition to ensure that your application executes in the right order every time. So when you use the step functions, there are many use cases for this service. Here is my top ones. Microservice orchestration. This is the use case of our problem, orchestrating services. Instead of having a big function, we use a workflow. [Batch Processing](../../Skills/Data%20Science/Batch%20Processing.md). Step functions are great for processing large batches of data. Some of the flow states allow 10,000 parallel executions. Data migration. You can use step functions to build your data migration scripts to move data from one service to another. Machine learning. Step functions can be used to build end-to-end machine learning workflows that combine [Data Preparation](../../Skills/Data%20Science/Data%20Preparation.md) model training, evaluation, deployment, and retraining steps. I will leave you a link in the resources of this video
>
> **[3:11](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/understanding-step-functions?u=76281980&t=191)** with an article on how one of the companies is doing this. Manual processes. Step functions is an ideal service if you need to build a workflow that contains a manual process, like a user accepting a request or something in that area, as it provides different asynchronous integration mechanisms. Finally, let's summarize some of the benefits of step functions. Removing boilerplate. By using step functions, you can avoid a lot of the boilerplate that is needed to invoke an AWS service or HTP endpoint as a step function provides direct integration. In this example, you can see in the left the code that you need to write in Lambda function to perform a GetItem from Dynamo, and how the same code looks in step function. Visual workflows. Step functions provide the workflow studio to build and visualize all your workflows. Error handling. Step functions provide different error handling mechanisms built in, like retrys, cache, timeouts, and redrives. End-to-end tracing. You can visualize every execution of a workflow, how long each step took, and what are the inputs and outputs for that step, allowing you to debug the application. Scaling. Step functions is a serverless service that scale. You can increase the traffic and the amount of parallel execution workflows will have no problem on handle that scale.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Batch Processing](../../Skills/Data%20Science/Batch%20Processing.md) (1), [Data Preparation](../../Skills/Data%20Science/Data%20Preparation.md) (1)
> **Env Vars:** aws (5), api (1), htp (1)
> **CLI Commands:** aws (5), make (1)
> **Definitions:** is a  (2), is an  (1)
> **Cross-References:** we mentioned (1), later in (1)
> **Exercise Files:** boilerplate (2)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Creating your first workflow in the AWS console](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-your-first-workflow-in-the-aws-console?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-your-first-workflow-in-the-aws-console?u=76281980&t=0)** - [Instructor] Great. Now you know a little bit about the functions. So it's time to create our first workflow so you can see it in action. For this, we are going to the AWS Management console and to the workflow studio. Later on this series we are going to build everything as infrastructures code, but I think it's good that you see how it's done in the console. So the first thing you need to do is to go to step functions. If you don't know where it is, just type on this bar step functions and it will take you here. So now it's time to create the first state machine. So go to state machines in the left and create a new one. We are going to pick a blank canvas so we can make sure that we make everything ourself. This is the workflow studio and it has a lot of visual components that help us to build. We can see the design here and if we press here, we can see the code of the state machine. In this case, it's very empty. And finally, the configuration of the state machine, like the name. So let's go to the design and throw here a lambda function. You can see here all the actions that are available and you can search. There are over 9,000 of them, so you might need to search. But the most popular is the invoke lambda function. And we have Lambda function from our previous video. So we can do that. Later we are going to see the flow mechanisms more in detail so you can find them here as well. So let's grab the Lambda function and throw it in the diagram. Now our state machine will run this Lambda function,
>
> **[1:37](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-your-first-workflow-in-the-aws-console?u=76281980&t=97)** but what Lambda function it is, well we can configure it here. The first thing we need to do is to select it from the dropdown. This will look for all the lambda functions that we have in our account in this particular region, and it will show it to us. And this is the one that we just deployed in the previous video. So now we can create the state machine. This will create the role for us and it will give permissions to the state machine to invoke this lambda function and also to put information into the observability tools, in this case x-ray. So I will just confirm and this will create everything for me. Now it's being created. When the state machine is created we can start an execution. Here you can put any input that you need. In our case, we are just testing, so we can just start an execution and see what happens. This should execute this lambda function that we just created. If we go here, we can see the input to the lambda function to that particular state. The output, that is the hello [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) message that our lambda function provides. And then some details on running this API. We invoke lambda function. We can go to the logs of the lambda function in particular. We can see how long it durates the time that this lambda was implicated. And we can see also the definition for this task in code, how it looks. If we go down, we can see all the events that executed in order for this state machine to complete.
>
> **[3:13](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-your-first-workflow-in-the-aws-console?u=76281980&t=193)** The execution started, then we started planning that task and then we execute that task successfully and then the execution of the state machine completed. So this is a very interesting visual so we can understand how our workflow worked.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **UI Navigation:** go to (4), dropdown (1)
> **CLI Commands:** make (2), aws (1), find (1)
> **Env Vars:** aws (1), api (1)
> **Cross-References:** previous video (2)
> **Definitions:** is a  (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [Creating a workflow as IaC](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-workflow-as-iac?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-workflow-as-iac?u=76281980&t=0)** - [Instructor] In the previous video, you learned how to create a workflow from the AWS console. But we have talk how important is to have all the resources that we want built as [infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md). So let's build that workflow as infrastructure as code. So we are back into our SAM project, the one that we created some videos ago. And here we are going to template YAML. Now we need to create a new resource for that. I'm going to create a state machine. Make this smaller. So the name is going to be my state machine of the type state machine. And then here there are some properties that we need. The first one is where we are going to find the definition of the state machine. All the state machines have a definition that can be a YAML or a [JSON](../../Skills/Web%20Development/JSON.md). Even if we are working on the visual studio console, we can see the definition here it is for the one that we just created previously. So we need to give a directory or this state machine will be defined. Then we have the definition substitutions, and we are going to cover that in a second. But this will point the function that we created previously to the function that will execute in the workflow. So this is how you create these connections. And then finally, we are giving permissions to the state machine to invoke that Lambda function. Do you remember when we created the workflow in the console
>
> **[1:35](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-workflow-as-iac?u=76281980&t=95)** that it was creating the permission for us? Well, if we do it as infrastructure as code, we need to do it ourselves. And all the resources in AWS are born without permissions, meaning that if we don't give the permissions to the state machine to invoke the Lambda, it will not be able to do it even if they're in the same account and created by me. So now that we have the infrastructure, the next thing we need to do is to create the definition for our state machine. So we are going to put that directory there and then we are going to create this file. You can see the file has a name statemachine.asl.yaml. ASL is the Amazon state language and is a language that originates from JSON that defines state machines. We are not going to get into the details on how to do that right now. So we are going to just go to our state machine that we created previously, go to edit, and then in code you can do actions, export definition as a YAML file. And this will get you the definition of this state machine as a YAML that you can use in your project. So I will open that and paste it here. So here is the definition. We can just copy it and paste it. So you can see here that this is the exact same definition of our state machine.
>
> **[3:09](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-workflow-as-iac?u=76281980&t=189)** And if you're using Visual Studio Code, you can get the AWS extension for Visual Studio Code. And that will enable this little button here that will allow you to render the state machine. So you can preview what is going on in this state machine. But if you see there is only one task, that is the one state that is the Lambda invoke and it's pointing to this function name. And in order to make it simpler and more re-utilizable for you, if you want to download this, I'll change this to MyFunctionArn. And this is the definition substitutions, meaning now anything that has this MyFunctionArn and is pointing up here will be the one that will appear here. And because everybody that is doing this course will have a different function name function, function ARN, then they can just download the code and re-utilize it. So it's very important not to leave these hard coded values in our state machines so we can deploy this in multiple accounts, in multiple regions, in multiple environments, whatever we want. So this will link to this function. So now let's deploy this and see what happens because we already have deployed some template before I just running sam deploy. And this will be uploading the changes into CloudFormation. And in this case, we'll be creating the state machine with all the resources needed to support the state machine.
>
> **[4:44](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-workflow-as-iac?u=76281980&t=284)** And we can see here that is going to create a new role for my state machine and a new workflow. And then it'll update my function as well. So we need to wait for a moment for this to complete, and then we can see it in action, for example from the console. We can go to state machines and we'll see it here when it's ready. So I think it's this one that we just created here. If we go to the definitions, we can see it here. And we can also start an execution in the same way that we did with the previous one. This will invoke the Lambda function and you can explore and see that it's exactly the same as the previous one.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (3), [JSON](../../Skills/Web%20Development/JSON.md) (2)
> **Env Vars:** yaml (4), aws (3), json (2), sam (1), asl (1)
> **CLI Commands:** aws (3), make (2), find (1)
> **Tools:** visual studio (3), aws console (1)
> **UI Navigation:** go to (4)
> **Exercise Files:** template (2), download the (1)
> **File Paths:** statemachine.asl.yaml (1)
> **Cross-References:** previous video (1)

#### [Introduction to ASL](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-asl?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-asl?u=76281980&t=0)** - [Instructor] In the previous two videos, you built a workflow. In the first one, we built it from the console. Using the workflow studio, you drag and drop components and boom, the workflow wrote itself. In the second video, you built it using [infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md), but you copy and paste the workflow that you build previously without really understanding what was going on. In this video, let's look a little bit more about ASL Amazon State Language. That is the language used to define these workflows. ASL is a [JSON](../../Skills/Web%20Development/JSON.md) base, and you can also represent this as a YAML as you saw in the previous video. Structural language used to define your state machine, a collection of states that do work. These are called task states, that determine which states the transition makes, these are the flow states. And error handle needed. There is a full page documenting ASL. I will not be providing you a course on it, but I want to go over some of the most important fields and how a simple workflow is constructed so you can build your own. The first object we want to define is the workflow itself. A workflow has an array of states and you need to define which one is the initial one. Defining the states is very important. Each state must have a unique name and have a type. This refers to the type of the state that we are going to learn in the next session, but for this example, we have a task state.
>
> **[1:35](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-asl?u=76281980&t=95)** Each type of state has different required attributes. For example, for a task state, the resource is a must-have. A state needs to have a next state or end state in order to tell step functions where to go next. Handling the data between the states is important. Each state has an input and an output and step functions will pass those around if you don't do anything. But sometimes you need to modify part of the input or access a specific attribute. Here is where the dollar sign becomes very useful. When you want to access a field in the input path, you can use the dollar sign and name the attribute. All the fields that are referenced using the dollar sign need to be defined with a dot dollar sign. Another really cool feature about ASL is the use of intrinsic functions. This allows you to perform simple programming functions directly in the workflow. For example, you can create a new ID, manipulate strings and arrays and code and decode by 64 strings, perform math operations and much more. In this example, the intrinsic function builds a new string that says hello to a user with its first and last name. Those values are coming from the input of the workflow or from the state input in this case. I will leave you a list of all the intrinsic functions available in the resources of this video.
>
> **[3:08](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-asl?u=76281980&t=188)** We'll be using a lot of intrinsic functions in this course, so you will see them in action in no time. And finally, I want to show you error handling. Any state type can encounter errors, and when the state reports an error, you can edit the default course of action of the state machine. You can define retries, fallback states, or catch a specific error. All of that can be defined using ASL directly in our workflow. This example shows a state that has a retried configure and a fallback state when those retries are exhausted and will still have an error. Don't worry if you are not yet confident writing all this by hand. Step functions offers the Workflow Studio that can help you out. And if you want to have the Workflow Sudio in your IDE like Visual Studio code, you can check a video I leave you in the resources of this course where you can learn more about how to do that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Env Vars:** asl (5), json (1), yaml (1), ide (1)
> **Definitions:** is a  (3), refers to (1)
> **Cross-References:** as you saw (1), previous video (1), in the next (1)
> **Analogies:** for example (2)
> **Tools:** visual studio (1)
> **UI Navigation:** drag and drop (1)
> **Prerequisites:** configure (1)

#### [Introduction to flow states](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-flow-states?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-flow-states?u=76281980&t=0)** - [Presenter] In the previous video, you learn about ASL and workflows. And I mentioned many times the flow states, and the different types of states that a workflow can have. In this video, let's look at those states types in more detail. A state is a step in a workflow, and this state performs a discreet operation. As you learn in the first video about step functions. This operation can be executing work, or performing some flow mechanism. Let's start with the first type of state that is the most common one. That is the task state. This is the state type that performs work. A state of this type can call another AWS service like a Lambda function, or you can call a third party HTTP endpoint. The parallel state can be used to create parallel branches of execution in your state machine. A parallel state provides each branch with a copy of its own input data, iterates output that is an array with one element for each of the branches containing the output for that branch. If a parallel branch fails, the whole parallel state fails. The map state can be used to run a set of steps for each element in an input array. While the parallel state executes multiple branches of steps using the same input, a map state will execute the same step for multiple entries of an array in the input. The map state can be used
>
> **[1:34](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-flow-states?u=76281980&t=94)** to build dynamically parallel fan out, and scatter gather patterns in minutes. Map state has two modes, distributed and in-line mode. When we work with the in-line map state, the concurrent iterations are limited to 40 items. If your input array has more than 40 items, then the following iterations won't begin until the previous iterations are completed. You can also define the maximum concurrency to be lower than 40. The output of the map state is a chason array, with the output from each iteration, and if any iteration fails, the entire map state fails, and all the iterations are terminated. Then we have the distributed mode that supports up to 10,000 concurrent executions. You can as well configure the maximum concurrency to your needs. The input for this state can be passed as the form of a previous state, or it can iterate over objects that are in an S3 bucket, a [JSON](../../Skills/Web%20Development/JSON.md) file, or a comma separated value pass in S3. Iterations in this case, start as a child workflow execution in their own independent execution history. The output of this map state is a JSON array with an output for each iteration, and can be stored in S3. Then we have a set of different states that can help you to build the flow of your workflow. Choice, adds branching logic to a state machine.
>
> **[3:08](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/introduction-to-flow-states?u=76281980&t=188)** It's like a switch. A pass state passes its input to its output without performing any work. This is a handy state to transform state data. If you need to perform some changes in the state, pass it's a great way of doing it. We'll look more about this later on in the demos. Await states delays the state machine from continuing for a specific amount of time. You can make that the state machine waits for a few seconds up to a year. When the state machine is waiting, its execution is not paused. And finally, we have succeed and failed states. Succeed states stops and execution successfully. While a failed state stops the execution of the state machine and market as a failure, unless it is caught by a catch block.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (2)
> **Env Vars:** json (2), asl (1), aws (1), http (1)
> **Definitions:** is a  (4), is an  (1)
> **CLI Commands:** aws (1), make (1)
> **Cross-References:** previous video (1)
> **Analogies:** it's like (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [presenter] (1)

#### [Step Functions integrations](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/step-functions-integrations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/step-functions-integrations?u=76281980&t=0)** - [Instructor] When we start out learning about Step Functions, you learn some use cases like the Step Functions are great for manual workflows or that is well suited to connect multiple [Microservices](../../Skills/Software%20Development/Microservices.md) together. But how this is achieved? That is possible because of the different integrations patterns that Step Function provides. In this video, you'll learn the free ones that are available. Service integration patterns are how Step Functions integrate directly with the different AWS services and legacy services and manual services. You can call a service from a workflow and let Step Function progress to the next state immediately after it gets an HTTP response. You can call a service and have Step Functions pass the workflow until the job completes, or you can call a service with a TaskToken and have Step Function pause the workflow until the TaskToken is returned. Request response integration pattern. The first service integration pattern to cover is the simplest one. That is the request response. To implement this pattern, you need to specify in a service the resource string of your task type and provide the API, as we see in the slide. This is how we have been invoking the Lambda function in the previous videos. When this happens, Step Functions calls the APIs and wait for an HTTP response, and then it progresses to the next state. In this diagram, we can see that it calls a GetObject API from S3. This is sync request, so when S3 responds
>
> **[1:37](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/step-functions-integrations?u=76281980&t=97)** with the object, Step Functions will move to the next step in the workflow. The next step is also a sync request. It's invoking lambda function synchronously. This means that the Step Function will wait for the Lambda to complete its execution, and then it will get the result from the function in its output. In the case that we want to invoke the Lambda function asynchronously, then Step Functions will start the function and move to the next step without waiting for the output of the function. As is the case when calling EventBridge, this is an asynchronous API. Step Functions calls to publish an event, and then gets back an acknowledgement and moves back to the next step. The second integration pattern available in Step Functions allows you to call a long-running API and have Step Functions pause the workflow until the job completes. This pattern is very useful, so your state machine can be paused and not cost anything during that time. This pattern is only available for standard state machines that can run up to one year. This means that your state machine can be waiting up for one year. When working with this standard state machine, you only pay for state transitions, and here, because the wait is managed by Step Functions, you don't have to pay for any of that idle time. This can be done only with the services that we have, an integrated integration like AWS Batch, Amazon ECS or Step Functions. On the slide, you can see all the services
>
> **[3:10](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/step-functions-integrations?u=76281980&t=190)** that we have support for, and this integration pattern doesn't support AWS SDK integrations. To have a workflow wait for a request to complete before progressing to the next state, you need to specify in the Resource field, in your task state definition with the .sync suffix appended to your API URI. For example, if you have a state machine that queries a data set using Athena, that service has support for .sync calls. The Step Function will do a .sync call to Athena and then pause itself until the service completes the job. When Athena completes querying the data, the workflow resumes from the next state. Here is a normal response request integration to get the results from Athena, and then calls EventBridge with an asynchronous API to put an event into the event bus. The last integration pattern available for Step Functions is the wait for callback. Here, you can call a service, any service, not limited to the optimized integrations, with the TaskToken and Step Functions will post the workflow until that TaskToken is returned. This pattern is super useful when we want to integrate a workflow to a legacy service, external service, or manual operation in some kind. In this example, you can see how to call the API with a WaitForTaskToken. Just append the TaskToken request at the end of the API call. You can get the TaskToken from the context object.
>
> **[4:44](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/step-functions-integrations?u=76281980&t=284)** Your implementation will need to store the token somewhere and return it later to restart execution. In this example, we have a workflow that put a message in a queue to start some kind of work. Maybe this is invoking some legacy service that is fronted by a queue. If the task succeed, then the workflow completes and if the task fails, then a function will be invoked to handle the function. When the state queue works, a message to the queue will be sent with the TaskToken. This queue can trigger any kind of service on the other side. It's not relevant for this example. When the task succeed, the state machine needs to resume, and that can be done with the API SendTaskSuccess and passing the task the TaskToken. If the task fail, the workflow can be resumed with a SendTaskFailure API call and the TaskToken. We don't need to know in any of these cases the execution ID or the name or ARN of the Step Functions, just the TaskToken.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Microservices](../../Skills/Software%20Development/Microservices.md) (1)
> **Env Vars:** api (10), aws (3), http (2), ecs (1), sdk (1)
> **Definitions:** means that (2), is an  (1), is a  (1)
> **CLI Commands:** aws (3)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Modifying the workflow with simple integration](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=0)** - [Marcia] Now that you have learned a bit of ASL, and the different state types, it's time to modify our existing workflow, and add a request response integration to an AI service. So we are going back to our project, and we are going to replace this lambda function that we are using in our state machine to call a service called transcribe. Transcribe is a service that allow us to do transcription. So basically you can upload an audio or a video, and it will get the text from there, transcription from what they're saying. And it has an API, I will leave you the link for this API in the resources. And it has a lot of different things that it can do. We are going to use the API StartTranscriptionJob, transcribe this API is asynchronous, so we will start the job then transcribe will do whatever it needs to do and we can get back to it. So let's go to our state machine and modify it. So let's open the state machine, and here always in the side, you can verify how it looks. I will close it so we have a better view of the code. I remove the state, lambda invoke, because we don't want it anymore. And I will add a new state called transcribe text. And we need to make sure that we are calling transcribe text at the start.
>
> **[1:38](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=98)** So now, this is a task and it's calling this API, the StartTranscriptionJob. It has some parameters, and you can see the parameters in the API definition. So these are coming from the API definition. You can see here the request syntax, and what are the request parameters. So you can check that out there. But it requires us to have a file from S3 in the format of URI. And here you can see that I'm using already intrinsic functions to format this. The input is the bucket name that I'm getting from the input of the state machine and the object key of the file that I will be getting from there. Then the TranscriptionJobName. I will use the execution name of this state machine that I'm getting from the context of the state machine. So it's unique. The input language will be in English for my case, but you can do whatever you want. You can do it from Spanish to English or whatever. Just make sure that you change this code as you need, and then transcribe will save that transcription into bucket. So we can pass the output bucket name here as a definition substitution. So we are going to do that in a second in our infrastructure. And then the key for that file will be another intrinsic function that will have the name of the original file.txt. So in that case, this is how it's going to get formatted.
>
> **[3:16](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=196)** So we can go now to our infrastructure, and start adding the different infrastructure bits that we need. Let's remove the function, we don't need it anymore. And here instead, we can create a new bucket. Buckets in AWS needs to have a unique name. So I will call it something, and you cannot use it, exactly the same name, because it will not work for you. So something like that, [LinkedIn](../../Skills/Web%20Development/LinkedIn.md)-serverless-ai-marcia-input-bucket. Then we are going to modify the state machine. The definition already stay the same because it's the same, but now we have a transcription bucket where the output will go and also the input. So we are going to use the same bucket for the inputs and outputs. So we can replace that, and then we are going to give policies, permissions to read, and write into that bucket. So we can do that. Let's remove that lambda policy that we have, and change it for permissions to read and write. These policies, if you don't know, they're coming from the SAM policies. So this is something specific for SAM that it provides you a list of policies already defined. You can find this link in the internet where there is all the different policies that SAM will make for you so you don't need to write them.
>
> **[4:52](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=292)** In the case of transcribe, we don't have a managed policy, so we need to build it ourself. And for that we are going to use the typical way of writing policies in AWS that is creating a statement with an allow, an action, in this case to start a transcription job over a resource, and because transcribe is a managed service that we don't create any resource on, we just give the asterisk. And then one thing we can do is to output in our terminal the bucket name so we know what this end up being called. So this is our infrastructure, this is our state machine. We can deploy it and see what happens. So let's go to the project folder and do sam deploy. Now, it will take a while, it will deploy and we can check it out. This will take a moment. It's creating all the different resources. We can see all the resources that are being created, and removed in here in the CloudFormation stack changeset. We can see that we are adding a bucket, that we are modifying the state machine and it's roles, and we are deleting the function and it's roll. So it will delete automatically that function that we removed from the template. So we don't need to do that manually. So that's really handy, but it takes a little while to do everything. As always, you can see it from your CloudFormation if you don't like to see it from the template, if you go to CloudFormation, you can see it from here. Well, now, the update is completed, so that's good.
>
> **[6:28](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=388)** We can go and check the state machine.
>
> **[6:37](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=397)** Here is our state machine with our definition, transcribe text. So this is what we have created. Then we need to go to S3, and we can see the bucket that we just created. We have the output of the bucket here. This is a name, remember to make unique name. So here is our bucket and here, we can upload a new file. So I'll just grab something from my computer, and I will upload it there. So I'm bringing a file from my computer. I am uploading it manually using the console, but if you want, you could upload it from the [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md). This file is a step function file that I recorded. Little piece of introduction to step functions, short video, and it takes around four, three minutes to upload. So you need to wait until this is uploaded, and then we can see what happens with the transcription. Okay, now, after the file uploaded, we can go to our state machine, and we can start the execution. Now, we need to pass a parameter to the execution. So we need to pass the bucket name, and the object name.
>
> **[8:08](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=488)** So we can see here that we are getting that from the input. So that's the first thing we are going to do. I will grab this, and I will be taking it here, so we can build that input. So it needs to be something in this format.
>
> **[8:36](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=516)** And then the name of the object is something like this. So this is our input. We can save it if we want, and we will be always using the same input. So for now, I will just keep it in my Visual Studio Code, because we might need to start this machine with this input many times. Remember the name of the bucket is the one that you created. The name of the file is the one that you just uploaded. So we can start execution. So what will happen now is that this will start, and it will finish right away. And it doesn't mean that the transcription is done. This means that the transcription job has started, but the transcription is not ready. So if we go to the bucket, we can see here that there is no transcription yet. So what happened now? What happened is that our StartTranscriptionJob is an asynchronous thing. It's an asynchronous API. And in order to be able to process this transcription, we need to get the transcription status of the job to make sure that it's completed. How we do that? Well, first, we need to modify our state machine. So let's go here and let's go to our state machine, and let's modify it. So the first step is correct, transcribe text.
>
> **[10:07](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=607)** But instead of finishing here, we are going to have another step, and we are going to call this one, Wait For Transcribe. So now we are going to use a wait state in order to wait for our transcription job to be completed. So let's add it here, Wait For Transcribe, and this will wait for one minute, and check the transcription job. Then we'll do the check of the transcription job by calling that API, Get Transcription Job, and we want to check on the transcription job ID that we created previously. So here is the TranscriptionJobName and this is the same. So it's the execution ID. And then we are going to use a choice state that will tell us, depending on the status of this job, if it's completed or it has failed. And if not, we keep on waiting, because the transcription is not yet done. If the job failed for any reason, we are going to failed state, and we mark the whole execution as failed. If not, we go to a succeed state. So now, we can look at the our state machine.
>
> **[11:43](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=703)** I know, it became a little bit bigger than it was before. Let's deploy it and before deploying it, let's see at the permissions, because we have here only permissions to start the transcription job, and we need to add the permission to get the transcription job. Permissions are granular, and we want to give the right permissions to the right APIs. And with that, now we can deploy it. And while we deploy it, I can explain you, and show you a little bit what is going on. Now it's deploying. Let's close this for a while, and let's go here to our transcription, our state machine. So we can see now that our state machine has many states, and they're all a little bit different. They're all different types of states. We have transcribed text, and check transcription job status, there are tasks and they're calling the API for transcription and AI service. Then we have a wait state, that wait for 60 seconds, and this will wait for 60 seconds, check the status of the job, if the job is completed or not, it will be decided by this choice state that will check on a variable that you will get from the result of the job status. And if it's completed, it will finish with a successful. And if it is completed but has failed, it will finish with a failure. But if it's yet not completed, it will keep on waiting for 60 seconds more, check the status, and then evaluate again. So in this way we are awaiting on our state machine.
>
> **[13:17](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=797)** And you may be wondering, Marcia, why you are not using the integration patterns that you show us. Well, first, the wait for asynchronous task, the .sync that we talked before, is not available for transcribe. We could use the callback token, the task token integration, but then we will need something like a lambda function, or something like that to do this iteration for us in a way. So this is one of the optimal ways to do this particular integration. So good, now, it's updated. So let's go back to our state machine. Let's see it now, the definition, it updated and now, we can start an execution. We can either start an execution from here, and pass the input that we have saved in Visual Studio Code. Or if you're lazy, you can start open the last execution, and click new execution, and it will start with exactly the same parameters. So we can see now how this works in action. So this is in progress. As you can see, the transcribed text went very fast, but the transcription is not ready yet. So now, we'll wait for one minute in order to check for the transcription job status, and we will see this updating in real time as it goes. One thing that you can do is adjust those 60 seconds to something that is more suitable for the size of the videos that you are uploading. I put 60 seconds because it's a safe bet,
>
> **[14:52](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=892)** but you might want to put it to 10, or 20 seconds so it will go faster. Remember, because we are in a standard workflow, we are going to pay for transitions. So we might not want to do a lot of iterations, a big loop here because we are going to be paying for that. So finding the optimal amount of transition time, of wait time is important. So yes. So if you click in any of the states, it will open that, you can see everything went green. That's good. And if you have zoomed out a little bit, because this is just so big for you to see, you can see exactly the same in the side. You can see the input, the output, the details of the state, how long it took and all those things. So go ahead and click and now, we can go to our S3 packet, and we can see here their result that we have. This is transcribed text so you can download it, and you can see it in your download files. So open it. So here it is, it's a very ugly file, but at the beginning you can see the transcript status is completed, the transcript of the file of the video in this case. And then you can see some pronunciation, and the confidence of each of the slots. And some of them don't have a very good confidence. Some of them has better, and the more native the English speaker is
>
> **[16:27](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-workflow-with-simple-integration?u=76281980&t=987)** the better it will be. But yeah, so this is the file.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (1)
> **Env Vars:** api (10), sam (3), aws (2), asl (1), uri (1)
> **UI Navigation:** go to (9), open the (2)
> **CLI Commands:** make (5), aws (2), find (1)
> **Definitions:** is a  (5), means that (1), is an  (1)
> **Tools:** visual studio (2), terminal (1)
> **Exercise Files:** template (2)
> **File Paths:** file.txt (1)

#### [Challenge: Modify the state machine to add an integration to AI service](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-modify-the-state-machine-to-add-an-integration-to-ai-service?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-modify-the-state-machine-to-add-an-integration-to-ai-service?u=76281980&t=0)** - [Instructor] In the previous video, you modify the workshop to transcribe a video file that is uploaded to us free. Now we can add another step that will translate that transcribed video into any language that we want. For example, I will be using Spanish as the destination language, but pick anything that you like. For this challenge, you will use Amazon Translate service, another of the AWS AI services. Amazon Translate API is asynchronous for small amount of text. This is how the API looks for translate. This is the name, the API name that you will need in your state machine, and these are the parameters. In my case, the source language will be English, the target language will be Spanish, and the text is whatever is transcribed before. So I imagine if you're thinking about this, this is the expected workflow that you are going to build. Basically, it'll be the output of the transcription, and pass it to a translation, and we should be good. But if you try to do this, you will notice something weird in that translation. The translation file that you will get as a result has a lot of unnecessary text that we are not interested. I'm wondering if you can polish the solution a little bit more to translate just the right amount of text, meaning the transcription text.
>
> **[1:33](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-modify-the-state-machine-to-add-an-integration-to-ai-service?u=76281980&t=93)** Let me give you a couple of hints on how you can achieve this. This is the final workflow that you should have. That red box is the steps that you need to add to your existing workflow, and maybe you're a little bit confused what they do. Let's look at them a little bit in more detail. So the transcription job stores a transcribed text in an S3 bucket in a file there, so until there, we are good. This is the output of the transcription job. This is the file structure of how that file looks. So if you don't load the file, you will notice that it looks something like this. This is a [JSON](../../Skills/Web%20Development/JSON.md) object, and it has a lot of information. It's a very long file, I just summarized it for you. But the part that we are interested is in the transcribed text, that thing that is inside results, transcripts, transcript. So we need to clean that result out, and for that we need to get the object from S3. For that, we are going to use the get object API that S3 provides. When we get the object, we'll have it in our payload from our state machine, and we can use a pass state type to just grab the part of the object of the JSON that we are interested in. And with that, then we can pass it to translate to do the translation on only the transcribed text. In the following video, you can see how I solve this problem step by step.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (2)
> **Env Vars:** api (4), json (2), aws (1)
> **Analogies:** for example (1), imagine (1)
> **CLI Commands:** aws (1)
> **Cross-References:** previous video (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Modify the state machine to add an integration to AI service](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-modify-the-state-machine-to-add-an-integration-to-ai-service?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-modify-the-state-machine-to-add-an-integration-to-ai-service?u=76281980&t=0)** - [Instructor] Let me show you my solution for the challenge number one. So we have our statemachine here, and it looks something like this. So we need to add those three states here for getting the file from S3, doing the cleanup, and do a translation. So let's do that. Let's check where this one ends. So somewhere here. And the first thing we want to do is to get that transcribed or the transcription. So for that we are going to create a new state called Get Transcribed File. And this state will be called whenever the transcription is completed. So now instead of calling Succeed, we just call that. And this one, what it does is calls the getObject from the S3 bucket. And the bucket is the one that we have in our definition substitution for the state machine, that input bucket that we created. And here you can see that we are using intrinsic function to get the file name. This is very complex, but basically, you can always start looking for the intrinsic functions from the button in. So here we have the TranscriptionJob URI file. We are splitting it based on these dashes,
>
> **[1:37](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-modify-the-state-machine-to-add-an-integration-to-ai-service?u=76281980&t=97)** we are getting the portion number four. And in that way is how we are going to build this key. That will be in the perfect transcribed/ and whatever comes from here. So this is a change I'm going to do in the code in general to have our results organized somehow so then we don't end up in a mess because now everything goes to the root of the bucket file and things will starting to get complex. So to tackle this, where I will go is up to where we are doing the transcribe. In the output key, I'm going to change this for something like this. So now we are storing that transcription into that transcribed folder in the bucket. So it's easy to know that all the transcriptions will be inside this folder, and later on, you will thank me because it's something that will cause problems if not. So let's go back to our Get Transcribed File. So we have the bucket, that is that bucket. The key that is that transcribed file that we just created. The API is the getObject, and you can always search for these APIs in something like that. AWS API GetObject S3, and you can always find it right away. And here you can find all the APIs for S3
>
> **[3:13](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-modify-the-state-machine-to-add-an-integration-to-ai-service?u=76281980&t=193)** and you can always find all the responses and requests for that particular API. And then we'll do a ResultSelector. In this case, we are going to get the whole body of the file and we are going to put it in this ResultPath called transcription. The next thing we want to do is to add a new state step called clean output. So this is our past state that basically what will grab is the transcription that we have in this place and it will put it in the transcribed text, that this is the thing that we are going to send to translate. So we are basically grabbing the whole object from S3 and then we are just grabbing that particular information from that [JSON](../../Skills/Web%20Development/JSON.md) file. And then finally, we are calling the Translate API. And this is the API that will be calling Amazon Translate with that API. And we have the language of the source that is English and the target is Spanish. And this is the text that is the one that we just put in this attribute. So now we're good, we should be fine. We can save and we can see what happens if our state machine is doing what we want. Yes, it's looking good. One more thing before we move on to deploy and test is to give permissions to this state machine to use translate. So let's do that.
>
> **[4:48](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-modify-the-state-machine-to-add-an-integration-to-ai-service?u=76281980&t=288)** Make sure you have the right indentations because YAML are like that. You might have them wrong and then nothing works. And then with this, now we are ready to deploy. So let's do sam deploy and let's see what happens. So we can see in the stack chainset that the StateMachine is going to get modified and also the role because we are giving these new permissions. So this will take a little bit and then we can test it. To test it, you can use that input that we have saved. I have it here in the Visual Studio Code. And that will help us to like being able to test. So this is the input that we want to pass to state machine. That is the bucket name that we created and the file that we uploaded. So let's go back to the state machine. We can make sure that the definition changed. So let's refresh this. Let's check the definition. And it has the new states, the starter execution, the space, that input, and the start. Again, this will take a little while because we have the 60 seconds for transcribe, but then after that, this should be quite fast because translate is synchronous. Amount of text that we are translating is quite small. So then we should be good. So we are waiting now for transcribe. We can also open if you want S3 so you can see what is going on there.
>
> **[6:27](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-modify-the-state-machine-to-add-an-integration-to-ai-service?u=76281980&t=387)** And now you can see that we have a folder for transcribed. So here is where the transcribed files are going to go. And when these finish, then everything will turn green in a second and we can see the results. So now we have finished the waiting with the transcription is successful. Now it's calling transcribe. We are getting the transcribed file. We are cleaning the output and we are doing the translation. So here in the output we can see our translation. You can see here the translation of that file. It's in Spanish. It looks quite okay. And if we go to our S3 bucket, we can see here that we have the transcribed file that we have originally in the root of the file. So everything looks good. Now we are ready to move on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Env Vars:** api (6), uri (1), aws (1), json (1), yaml (1)
> **CLI Commands:** find (3), make (2), aws (1)
> **Code Identifiers:** getobject (2)
> **Cross-References:** go back to (2)
> **Tools:** visual studio (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)


### 5. Building a Workflow with AI

[↑ Back to Table of Contents](#table-of-contents)

#### [Modifying the existing state machine to use Amazon Bedrock](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-existing-state-machine-to-use-amazon-bedrock?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-existing-state-machine-to-use-amazon-bedrock?u=76281980&t=0)** - [Instructor] Let's jump directly into adding [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) to our workflow. In this video, you will learn how to call Bedrock from your workflow, and what we want to do is to create a summary of the translated text. So let's look at our state machine. We can check it out here. This is state machine that we have. First we have the transcription of the text. We have a video, we upload it. Then that transcription gets translated. And now what we want to do is use Generative AI to get a summary of that translated text in the original language. So in order to do that, we need to modify a little bit our existing state machine. I will add somewhere here the invocation of Bedrock. But in order to invoke Bedrock, we need to transform the data that we have from the translation. So the first thing we want to do is to create the prompt. So we need to create the prompt that we are going to give the [Gen AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) model. Then we want to combine the prompt with the translated text. Then we need to create the place where we are going to store these results from invoking Bedrock. Then we invoke Bedrock. So there is quite a lot of work that we need to do here in order to get this working. So let's go after the translation
>
> **[1:34](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-existing-state-machine-to-use-amazon-bedrock?u=76281980&t=94)** and start adding the different bits and pieces. So the first thing we want to do is to create the prompt. So here you can see I added a new state called add basic prompt to the output. And it's the type pass. And what it does, it has a prompt, "Given the transcript provided at the end of this prompt, "return a summary of 160 characters of the transcript. "Keep the original language of the transcript. "If the transcript is provided in Spanish, "return the summary in Spanish, and here is the transcript." And you can see that there is nothing. So that's the next step. We need to combine the translation with the prompt. So we are going to add another pass state called Combine Prompt that will format these basic prompt together with the translated text. So that's what we want. And then finally, what we need in order to get prepped is another state, another pass state in where we are going to create the URI or where we are going to store their result of invoking Bedrock. So for that, we are again formatting a string with an URI. And this is a S3 URI in the transcription bucket in the directory results. And the name of this file will be the file name. This is a variable that we don't have in our application,
>
> **[3:11](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-existing-state-machine-to-use-amazon-bedrock?u=76281980&t=191)** so we need to modify a little bit our states that we have before to add those in place. So we are going to the clean output. And here where we are getting the transcript in the transcribed text that we did before, we will create a new variable here, called file name. And here we are going to get the file name of where this transcription is. So this is something we want to have. And then in the translate text, we need to do a couple of modifications. First we want to preserve that input data. So we are going to create that. And then the resolve path of this translation is going to get stored in this transcripted text. So then we can get it from here. And one last thing is that we need to call this state from here. So now we are only missing the Bedrock invoke model. So this is the way that we can invoke Bedrock directly from our workflows. So this is a task type event, and we are calling this API. That is the invoke model. We are going to use the foundation model AI 21 Ultra. Then we are going to pass the output,
>
> **[4:50](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-existing-state-machine-to-use-amazon-bedrock?u=76281980&t=290)** this URI that we created in the previous step. And the prompt is the one that we just completed in the steps before that. We have a maximum [Tokens](../../Skills/Web%20Development/Tokens.md) that we can configure. And then we are putting the results in this results variable, but it's also getting stored in this URI. So we are going to check that one. So we can save this. And then we need to go to the template YAML and give permissions to our state machine to access Bedrock. So that's another thing we need to do. So we go to the state machine, into the policies, and we give permissions to Bedrock. So that's great. Now we can deploy this, but before deploying, we need to do one thing in our AWS account that is in Bedrock. So we can search for Bedrock or if you have not used it yet, or you have it in your recent, and then you can go to the sidebar, all the way down, model access. And in here you need to manage the model access, select the ones that you want to use. For this demo, we are using Jurassic-2-Ultra, but you can use whatever makes you happy. Select them. And then because I already have the permissions, I cannot do it. But select them all, save changes. And that will grant your account permissions to use that model.
>
> **[6:23](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-existing-state-machine-to-use-amazon-bedrock?u=76281980&t=383)** So that's an important thing. So now we are ready to do some deploy and test this in the cloud. So this will now modify our state machine, our workflow, and the role of our workflow because we are giving permissions to the state machine to access Bedrock. So we need to wait for all of these things to get updated. So if we look now while that deploys to our new state machine, you can see that this is a very long bar. We get the file, we transcribe it, then we translate it, and then we create the prompt. We combine the prompt with the translated file. We create the URI where we want to store the results. Then we involve Bedrock, and then we are done. So this is deployed. Let's go to our state machine, and we can start a new execution. If you remember, we have saved the input. So that's something you want to grab. We will fix that in a second, in a few videos. Now we start the state machine, and this execution will start. So again, the transcription will take around a minute because we are waiting for the transcription. And this is a wait task that is 60 seconds. So we need to wait for that long. But this is a short video, so it should be done in those 60 seconds. And then after that we can check the status of everything.
>
> **[8:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/modifying-the-existing-state-machine-to-use-amazon-bedrock?u=76281980&t=480)** So now our state machine finished, and we can see the results. So we can check all the different states and well, there is quite a lot of information, but let's go to the S3 bucket. S3. Let's see our bucket. And let's go into the results folder. That's the folder where we decided we want to have the file. Here it is. We download it, and then we can open it with some kind of visualization. I open it with Firefox. That makes a nice view of the [JSON](../../Skills/Web%20Development/JSON.md). And if we go to completions, we can see here in data, the text that it is "Step Functions in a visual workflow tool "consisting of a series of discrete operations," and blah, blah, blah. This is like the summary of this video that is about step functions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (2), [Gen ai](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (1), [Tokens](../../Skills/Web%20Development/Tokens.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Env Vars:** uri (6), api (1), yaml (1), aws (1)
> **UI Navigation:** go to (6), select the (1)
> **Definitions:** is a  (6)
> **CLI Commands:** aws (1)
> **Tools:** firefox (1)
> **Exercise Files:** template (1)
> **Prerequisites:** configure (1)

#### [Challenge: Integrate with Amazon Bedrock](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-integrate-with-amazon-bedrock?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-integrate-with-amazon-bedrock?u=76281980&t=0)** (intriguing music)
>
> **[0:05](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-integrate-with-amazon-bedrock?u=76281980&t=5)** - [Instructor] Great. Now you know how to integrate a workflow, step-functions workflow with Amazon Bedrock. So I have a challenge for you. Can you now make Amazon Bedrock write a poem for you instead of a summary of the video? And can you make that in parallel of the summarization that you have done before? So this is the workflow I'm asking you for. You can see here that you do the transcription, you get the transcribed file with a clean output, and then we have a parallel state that from one side we have what we have been doing, the translation and the summarization to Bedrock, And then that red outline shows that we want to have Bedrock to create a poem based on the transcription file that we have for it. So the input for the workflow doesn't change, it's exactly the same, the bucket and the object name. And for the parallel branch, the input is at least a transcription file, the transcription text from the video or the audio. And if you need to add more things, feel free to add. The results should be stored in that results directory in the S3 bucket with the name, for example, poem, the name of the original file, the [JSON](../../Skills/Web%20Development/JSON.md). And in the next video I will show you the solution that I came out from this challenge. One thing is that I just put a poem to put something a little bit funny, but feel free to ask [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) for other things. You can ask the top most important concepts of the video or to create a social media post out of the video
>
> **[1:43](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-integrate-with-amazon-bedrock?u=76281980&t=103)** or whatever you imagine, so feel free to do that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1), [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (1)
> **CLI Commands:** make (2)
> **Analogies:** for example (1), imagine (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (intriguing music) (1)

#### [Solution:  Integrate with Amazon Bedrock](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-integrate-with-amazon-bedrock?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-integrate-with-amazon-bedrock?u=76281980&t=0)** - [Instructor] Let me show you how I solve the challenge number two. The first thing we want to do is to go to our state machine and modify it. So we have the state machine and we want to add a parallel branch here. So basically starting from the clean output is where we want the parallel branch. So let's go to the clean output, and after that we can add the prioritization. So let's go up, clean output, and let's add a parallel state. So here you will see, this is how you write the parallel state. You put the name, parallel state, whatever, just any name. Type parallel, next succeed, that is the end. And then you need to define the branches. So you can have as many as you need. And you will define where these branches start. So the same way that you define the workflow at the beginning that you say start at and then you have an array of states, you will do the same for the parallel branch. So we want to start at translate text. This is our first branch. So I will grab all these and then I will make it so that the indentation goes under the states, so it's in the right level. This is YAWL, so indentation is very important. So you can see here that now it's good.
>
> **[1:35](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-integrate-with-amazon-bedrock?u=76281980&t=95)** Then we need to change this clean output to parallel, so things are looking good. And then it says that we don't have a terminal state, so don't worry, we will go to the bottom of this and we will change this last state in our branch, in our original branch from next succeed to end true because that's the end of the branch. So this should be a functional state machine, but we don't have still the solution to the problem. So now we can add another branch. So we can say, "Hey, let's add another branch." So we need to make sure that we are in the right indentation, somewhere here. And let's go to the bottom of this, ticky, ticky, ticky. And StartAt, and then here we can add the first state. So the states will be very similar to the ones that we already have. We have the first thing we are going to do is to create the prompt. Then we are combining the prompt with the, in this case, the transcribed text. And then we are going to create the URI, where we want to store that file. And then finally, we invoke bedrock. So everything, it's very similar to what you did before. So let's put this, the first state in the parallel branch is to add, create the prompt for the output,
>
> **[3:14](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-integrate-with-amazon-bedrock?u=76281980&t=194)** so that's how we are going to do. And this is a silly prompt, "Given the transcript provided, at the end of the prompt, return a poem of eight lines based on the transcript. Here is the transcript." Then we are going to combine the prompts together with the original text. So we are going to do that. This is exactly the same as we have done before. But instead of using the translation, we're using the transcribe. Then we are going to create the URI for storing the file. So this is again, another intrinsic function, very similar to the one we have done before with the URI of S3 and storing that with the file name, poem. And finally, we are going to invoke bedrock. And now we are going to call exactly the same model. The URI is the one that we just built. The prompt is that poem prompt press the transcribed file, and then we'll get the result here. So with that, then we should have everything that we need to deploy, so let's do some deploy. And let's see what happens when this is deployed. So we can see now that our state machine has a branch here for translating the text and doing the summarization. And then another branch here to make a poem
>
> **[4:48](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-integrate-with-amazon-bedrock?u=76281980&t=288)** out of the transcribed file. But again, you can do anything. It doesn't need to be a poem. So this is updating. Now it's done. Let's go to our state machine. Let's go here to new execution. You have to input what you have and we can start the execution. The input is exactly the same as before. We are not yet working on that. And now we need to wait for a little bit for the transcription to finalize. And then we will see how the parallel works. So now our state machine complete. And you can see here the parallel has two branches and they are both in green, so everything is good. And then if we go back to our results folder, we should see the poem here and we can download it. This can be quite a mess because sometimes [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) is not that great at making poems. But, well, let's see. Let's open this with Firefox and see what happens inside the completitions. If we go to text, well, has the functions of a workflow service. So it didn't make much of a poem, but (chuckles) you can play with this and see what happens. But you might need to tweak the prompt that you pass. So here is the prompt that we have passed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (1)
> **Env Vars:** uri (4), yawl (1)
> **UI Navigation:** go to (5)
> **CLI Commands:** make (4)
> **Tools:** terminal (1), firefox (1)
> **Analogies:** similar to (2)
> **Cross-References:** go back to (1)
> **Definitions:** is a  (1)

#### [What is event-driven architecture?](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-event-driven-architecture?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-event-driven-architecture?u=76281980&t=0)** - [Instructor] Have you noticed that we have been inputting the file name and the location manually every time we want to run the workflow? Wouldn't it be great if the workflow starts every time you upload a new file to the bucket? Here is where adding some event-driven characteristics might help us out. In this video, let's look at a brief introduction of what event-driven architectures are. Event-driven architecture is an architecture style that uses events and asynchronous communication to loosely couple an applications component Event-driven architectures can help you to boost agility and build reliable scalable applications. Before getting started with event-driven architectures, it's important to understand the difference between tight and loose coupling. Coupling is the measure of dependency each component of an application has on one another. For example, a component needs to call another in order to complete its task and the order in which things are called matter. Tightly coupled systems can be particularly effective if the application has few components or if a single team or developers owns the entire application. However, when components are more tightly coupled, it becomes increasingly likely that a change or operational issue in one component will propagate to others. For complex systems with many teams involved, having a system that is tightly coupled can have drawbacks.
>
> **[1:35](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-event-driven-architecture?u=76281980&t=95)** When components are tightly interdependent, it can be difficult and risky to make changes isolated to a single component without affecting others. This can slow development processes and reduce the feature velocity. Tightly coupled components can also affect an application [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) and availability. If two components depends on one another, a synchronous responses, a failure in one will cause the other to fail. These failures can reduce the application overall fault tolerance. For example, an [E-Commerce](../../Skills/Web%20Development/E-Commerce.md) application might have multiple services: orders, billing, shipping, inventory, and make a synchronous change of calls between the services. A failure in one of those services will impact all the others. Reducing the coupling is the art of reducing interdependency between components and the awareness each component must have of one another. Event-driven architectures achieve loose coupling through asynchronous communications via events. This happens when one component doesn't need another to respond. Instead, the first component might send an event and continue without impact should the second component delay or fail. When communicating with events, components only need to be aware of the independent events. Then, they require knowledge of transmitting component
>
> **[3:09](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-event-driven-architecture?u=76281980&t=189)** or any other components behavior like error handling or retrial logic. So, as long as the event remains the same, changes in a single component won't impact the others. This allows making changes to an application less risky. When asynchronous events abstracts components from one another, complex applications become more resilient and maintainable. For example, in this case, we have event producers and event consumers all connected using an event broker. We are going to see more about event brokers a little bit when we play with event bridge. The benefits of event-driven architectures are a shift in mindset is required when building an event-driven architecture due to the unique characteristics and considerations of asynchronous systems. Build and deploy applications independently in loosely coupled applications. Development teams working on individual services have fewer dependencies. Changing one service will have less risk on impacting others. Build new features using events without changing existing applications. Extend your applications since components emit events, event-driven architectures can easily be extensible. Events can also be analyzed for business reports and audits if needed. Scale and fail components independently in loosely coupled components. Applications with loosely coupled components have fewer single points of failure
>
> **[4:43](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-event-driven-architecture?u=76281980&t=283)** as well as increased [Resiliency](../../Skills/Software%20Development/Resiliency.md). Event-driven architectures possess three key components. Event producers that publish events, for example, it can be websites, [Microservices](../../Skills/Software%20Development/Microservices.md), [internet of things](../../Skills/Software%20Development/Internet%20of%20Things%20(IoT).md) devices, AWS service, or [software as a service](../../Glossary/Concept/SaaS.md) applications. Event consumers are the downstream components that get activated when events arrive. Multiple consumers may be found in the same event. Consuming events include starting workflows, running analysis, updating [Databases](../../Skills/Software%20Development/Databases.md), doing some kind of compute. Event brokers mediate between producers and consumers of events. They grab the events and know where to route them into the event consumers. Usually these include event routers that push events to target and event stores from which consumers can pull events. And finally, we have the event concept, that is something we have been mentioning a lot in this video. An event is the signal that something changed in our system, something changed in the state of our system. For example, a shopping cart, or some order, or a credit card application, whatever. Events occur in the past, like the order is created, the application submitted, and they're immutable, they cannot be changed. This helps in distributed systems because there are no change in components to keep in sync.
>
> **[6:20](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-event-driven-architecture?u=76281980&t=380)** Events are observed and not direct. A component emits an event with no particular destination in mind, nor aware of the downstream components that might consume the event.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) (1), [E-Commerce](../../Skills/Web%20Development/E-Commerce.md) (1), [Resiliency](../../Skills/Software%20Development/Resiliency.md) (1), [Microservices](../../Skills/Software%20Development/Microservices.md) (1), [Internet of things](../../Skills/Software%20Development/Internet%20of%20Things%20(IoT).md) (1)
> **Analogies:** for example (5)
> **CLI Commands:** make (2), aws (1)
> **Env Vars:** aws (1)
> **Definitions:** is an  (1)
> **Warnings:** be aware (1)
> **Prerequisites:** getting started (1)
> **Speakers:** - [instructor] (1)

#### [How to trigger a state machine when there is a new S3 file](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-trigger-a-state-machine-when-there-is-a-new-s3-file?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-trigger-a-state-machine-when-there-is-a-new-s3-file?u=76281980&t=0)** - [Instructor] Great! Now you know a bit about event-driven architectures. Why we want our systems to [react](../../Skills/Web%20Development/React.js.md) to events that are happening. So, how you can trigger a step function workflow to react to those events? There are many ways to invoke a workflow. You can do it by calling an API and integrating it directly to API gateway. Or from other services, like running Lambda function using the AWS SDK, and invoking the Step machine. Or you can add it as a target of an EventBridge rule to react when there is something going on, like an event happening on our state machine inbox. In our example, we want to trigger the workflow every time that there is a new file in a particular bucket. So for that, we need to add an EventBridge rule that targets that workflow when there is a new object in the bucket. This might sounds a little scary, but it's very simple to do, and let's see how to that in the code. So let's go to our code here. And the first thing we want to do is to modify the template.yml. So we have this bucket that we created quite early on in this course, this InputBucket. It's here. And for that, we need to add a little bit of configuration. The configuration we are going to add
>
> **[1:37](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-trigger-a-state-machine-when-there-is-a-new-s3-file?u=76281980&t=97)** is the NotificationConfiguration with EventBridge. So this, what we we'll do is, we'll enable the notifications to EventBridge, it will send events to the EventBridge. The EventBus was that something has happened in this packet. Either a file has been created, deleted, modify, or whatever, so now our bucket is omitting events, something happened. We now need to modify our workflow to get trigger when there is a new change in that bucket. So let's go here, under Policies, and let's create a new attribute that says Events. And here under Events, we can add the new event. And this is an S3MediaTrigger, we can put any name we want. From the Type, EventBridgeRule. And we are listening to the default EventBus. There is many EventBuses that we can configure in EventBridge, for our particular case, we care about the default one. And the Pattern that we are listening of the event is aws.s3. The object has been created in the InputBucket. That is the one that we have here. And in the directory uploaded, we don't have that directory yet. So you can go and create it in your bucket. I already have done it, and uploaded the file there so we can test faster.
>
> **[3:11](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-trigger-a-state-machine-when-there-is-a-new-s3-file?u=76281980&t=191)** So this is the directory, uploaded, and the file. And whenever we have a new file in this directory, then the state machine will trigger. This is great, but we need to do one more change into our state machine in order to make sure that everything is working, and that is to work to store the transcribed file. 'Cause now we are just grabbing this name from the input. And if we continue in this line, what will happen is that the name of the input will be 'uploaded.', whatever the file name is, and that's not what we want. We want to clean it up a little bit. So I will do a little bit of [Data Transformation](../../Skills/Software%20Development/Data%20Transformation.md) here and use a little bit more intrinsic functions, where I'm formatting the output key to be stored in transcribed directory. And we are basically splitting the string that we get from the details. And we are just getting the first part, that is basically the position 0 is uploaded and the position 1 is the name of the object. So that's what we are getting, and we are pasting it here. So now with this, we can then do some deploy. And these will modify our infrastructure, will modify our packet, and it will modify our state machine. So you can see here that we have the new trigger.
>
> **[4:49](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-trigger-a-state-machine-when-there-is-a-new-s3-file?u=76281980&t=289)** That we are adding is an AWS rule. We are adding also a role for that, and we are modifying the input bucket. So now the stack has been deployed, we can go to our state machine. And now we don't need to trigger it, it'll just trigger automatically. So I'll [Zoom](../../Skills/Software%20Development/Zoom.md) this out a little bit so you can see the new execution coming in. And then I will go to the bucket. And here, we are going to upload it. I already uploaded the file, so I will just rename this file a little bit. So then the notification will get trigger, and this will create an object, create that notification. So I will have saved that change. And now we should see that the state machine is running. So if you don't see it, just refresh. And you can see it that it's running. You don't need to create and start an execution every time. And if you click on the running state machine, you'll see the workflow that you're familiar with. And you can see here the execution, input and output. So this has quite a lot of information, but if you remember the input that you have saved to work initially, it has this structured detail bucket. The name of the bucket, object, the key. So this is exactly the same format
>
> **[6:24](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-trigger-a-state-machine-when-there-is-a-new-s3-file?u=76281980&t=384)** that we had before, with more information, but the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the information is being ignored by the state machine. So we need to wait a little bit for this to complete, but it will complete successfully. And now everything will get stored in its directories. So now we have the results in the result folder, the transcriptions in the transcribed folder, and the uploaded in the upload folder. We can remove this. We don't need it here anymore. And everything should be now in its own directory. Why this is good, because then we make sure that everything, it's inside the directory, so if we create new files here, this will trigger the state machine. But because everything is going to be stored in its own directory, it will not trigger the state machine. So good. Now we have completed. This is done.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (3), [Data Transformation](../../Skills/Software%20Development/Data%20Transformation.md) (1), [Zoom](../../Skills/Software%20Development/Zoom.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** aws (3), make (2)
> **Env Vars:** api (2), aws (2), sdk (1)
> **UI Navigation:** go to (3), click on (1)
> **Definitions:** is a  (2), is an  (2)
> **File Paths:** template.yml (1)
> **Exercise Files:** template (1)
> **Prerequisites:** configure (1)

#### [How to send an email from the state machine](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-send-an-email-when-from-the-state-machine?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-send-an-email-when-from-the-state-machine?u=76281980&t=0)** - [Instructor] Now you know how to trigger the workflow automatically from an event when a new object is uploaded to the S3 bucket. But what about getting a notification when the workflow ends, like an email? We can easily make the workflow send notifications in the form of an event breach event or an email using the Amazon Simple Notification Service, SNS. In this video, I will show you how you can do the second one, like sending an email, and you're going to send a notification email to yourself when the poem is created. So, in order to do that, first we need to create the resources in our infrastructure. We are going to use SNS. SNS is a service that allow us to send notifications. It can be in the form of push notifications or emails or just notifications into a topic that some system or third party service can read. In our case, we are just sending an email. So it's very simple to create a notification topic. We just build that notification, the topic, and then we add a subscription. And in this case, we are going to use email, so I'm going to add a subscription email. And I'm going to use a parameter, so we can define this email in our parameters file, and I don't need to put it in the [infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md). So now, the next time you deploy this, you do some deploy --guided as you did in the first time,
>
> **[1:34](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-send-an-email-when-from-the-state-machine?u=76281980&t=94)** and you will get the prompt to put the email. You will see it in a second. So now, when we have the notification here, we have the email, then the next step is to give permissions to the state machine to put a message into that topic. So we are going to the policies, and we are going to use one managed policy to publish a message into that topic. So, that's the whole thing that we need to do in our infrastructure. Then, we need to modify a little bit our state machine. We want to add, after the poem is generated, that the notification is sent. So, what we are going to do is we are going to create a new state at the end of everything. So let's look for the poem. This is the branch of the poem. So, after we invoke Bedrock somewhere here, we are going to send email notifications. So we need to change this to be something like this. And now, we are going to send this email notification that is calling the API, SNS published a message to the topic, and the topic, ARN, is something that we need also to pass to our state machine and that we do in our infrastructure.
>
> **[3:07](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-send-an-email-when-from-the-state-machine?u=76281980&t=187)** I forgot, it's important. So in the definition substitutions, you just send the notification topic that you just created here. And now it'll be appearing here. And the message is, "A new poem has been created. "Check the S3 bucket for more info." We could send here the link to the load, the file, but we need to add more logic and more [Data Transformation](../../Skills/Software%20Development/Data%20Transformation.md) into this workflow in order to do that. If you want to do it, go ahead and do it. In the resources of this course, I will leave you links with [GitHub](../../Skills/Software%20Development/GitHub.md) repos that does that, so if you want to go and check it out, it will be there. But for now, let's just make this notification. So now, we do sam deploy, and then we put --guided, and here we are going to be prompt our configurations, so I will be putting the stack name. I just push enter to confirm everything, and here we have the email. So I will put the email and then I will move forward with confirming everything. And now, the next time we deploy, we don't need to run the configuration. It's just this time, because we modify the parameters. So now we wait for these to complete, and then we can move forward. Good, now our step function is deployed
>
> **[4:43](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-send-an-email-when-from-the-state-machine?u=76281980&t=283)** and our infrastructure, we can go back to our state machine. We can refresh this. Let's see if the definition is already refreshed, and we can see that we have the email there. So before moving on, you might need to go to your email, and you might need to confirm your subscription. You will get something like this in your email, and you press confirm subscription, and that will make sure that you receive the emails that SNS will send you. So now we can test, and to test, you might need to go again to your uploaded folder and rename this file. Or then you can pass whatever we were passing in the past, that will also work. So I would just save this. This will trigger the state machine. So we can go in executions, and we can see that one execution is running. And we can wait for all these to complete until we get the email notification, so this takes a few minutes, and then you should check your email and see that email notification appearing. Good, it's completing. So you should see the notification when this finish uploading. If it didn't, you just wait for a few seconds. Good, and you should get an email that looks something like this. The new poem has been created.
>
> **[6:16](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/how-to-send-an-email-when-from-the-state-machine?u=76281980&t=376)** Check the S3 bucket for more information. So, that's the email.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (1), [Data Transformation](../../Skills/Software%20Development/Data%20Transformation.md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Env Vars:** sns (5), api (1), arn (1)
> **CLI Commands:** make (3)
> **Cross-References:** go back to (1)
> **Tools:** github (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Integrate to EventBridge event bus to send an event](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-integrate-to-eventbridge-event-bus-to-send-an-event?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-integrate-to-eventbridge-event-bus-to-send-an-event?u=76281980&t=0)** - [Instructor] In this challenge, I want you to send an event from the workflow to an EventBridge event path when the translated summary is ready. For doing this, you will need to add a new state to the workflow to send this message as you did when you sent an email in the previous video. This is how the workflow should look after you finish this challenge. Here is the information you need in order to create the task event to put an EventBridge event. The API name is the one that you can see in the screen, and these are the parameters that you can copy and paste them into the workflow and add whatever you want in the message. Don't forget to give permissions to the workflow to put events in EventBridge. Here is the managed policy you need to use in order to do that. To test this, you will need to create an EventBridge rule that matches that event. So when an event with some of the properties that we are interested come in, it match against that particular rule. Then you can add a target to that rule. In this case, we can add a CloudWatch log group. So a log entry will appear in the log group every time the rule matches. Let me show you how to modify the template YAML so you can do the testing in your example. So here we have our code. It's the template YAML with the bucket notification topic on the state machine.
>
> **[1:36](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-integrate-to-eventbridge-event-bus-to-send-an-event?u=76281980&t=96)** And now we need to add a couple of more things. The first thing we want to add is the testing rule. So we can add it somewhere here. And this is an EventBridge rule with a event pattern. So basically everything that matches this source will be matching against this rule. So when you create your rule, make sure that the source of the rule is this one. You can create other pattern matching techniques, but let's keep it simple. And then let's have a target, and in this case, the TestingLogGroup. We have not created that one yet. So let's create a CloudWatch log group. Let's put it here. So the target for this rule is whenever our event comes in, it matches this rule and then it puts a message in this log group, CloudWatch log group. And the only thing we need to do is to give a name. One important thing that we are missing, and this is something you might need to copy paste because it's a kind of lot of [JSON](../../Skills/Web%20Development/JSON.md), is the permissions. So because EventBridge needs permissions in order to write and create the log streams, you need to give permissions to EventBridge to do that. So we will create a policy. This is a policy that allows EventBridge to put the log streams and to put the logs into the log group we just created. So you can see that this policy has two parts. The first one is to create the log streams that allows EventBridge to create the log streams into that log group that we just created here.
>
> **[3:15](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-integrate-to-eventbridge-event-bus-to-send-an-event?u=76281980&t=195)** And the second one is allowing EventBridge to put logs into that log group. And the thing is that we want to make sure is that the rule matches the one that we just created. So this is a condition. You can remove this if you want, but with all this now, you should be able to do the challenge. So in the next video, I will show you how I solve this challenge.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Env Vars:** yaml (2), api (1), json (1)
> **Definitions:** is a  (2), is an  (1)
> **CLI Commands:** make (2)
> **Cross-References:** previous video (1), in the next (1)
> **Exercise Files:** template (2)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Integrate to EventBridge event bus to send an event](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-integrate-to-eventbridge-event-bus-to-send-an-event?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-integrate-to-eventbridge-event-bus-to-send-an-event?u=76281980&t=0)** (lively music)
>
> **[0:05](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-integrate-to-eventbridge-event-bus-to-send-an-event?u=76281980&t=5)** - [Instructor] So let me show you how I solved this challenge. So the first thing you need to do is to add the infrastructure that I showed you when I presented the challenge, the TestingRule, the LogGroup and the permissions for EventBridge to put logs into the LogGroup and to create log streams. So you should have that. Then I mentioned that you need to give permissions to the StateMachine to be able to put events into EventBridge. So that's what we are going to do. So in the template.yml, we go to our workflow and in there, we will use a managed policy that looks something like this. It's called EventBridgePutEventsPolicy. And the name of the bus is the EventBus for default. That's where we want to put our events. Then we want to create the new state. So we go to statemachine and we look for where we want the state to be. So we want it to be in the summary. So after we translate the text and we invoke Bedrock, so it's in the branch of Translate Text. So let's go down after we invoke Bedrock and somewhere here, we will create our new state. And this state will be putting events in EventBridge. So we need to change the previous step to do a Next and that it goes to the right place. And then here, always make sure
>
> **[1:39](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-integrate-to-eventbridge-event-bus-to-send-an-event?u=76281980&t=99)** that your indentation is correctly 'cause this is YAML and YAML is always a problem if you have wrong indentation. So our new state is a Task state and the API is the one that you had in the examples of this challenge. And the parameters are the ones that I gave you. So the detail, it's a message and in this case, I'm just passing the file name. You can pass whatever you want. The DetailType is the execution name. The EventBus is default and the source is the StateMachinePutEvent. And in here, you need to really make sure that the source matches the one that you put in the rule definition. If not, then this will never match. So this needs to be exactly the same. And with this in place, now you can go to your console and test it and do sam deploy. And then this will deploy the StateMachine with all the rules and everything you created into the cloud, and then you're ready to test. So I already deployed it before this, so we can speed it up and we can see the StateMachine here. We can start a new execution or we can re-upload the object in S3. You pick that. I will start a new execution by providing the input that we have saved so it works in a similar way. Then packet name and the object key. And then while this executes, I can show you a little bit
>
> **[3:13](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-integrate-to-eventbridge-event-bus-to-send-an-event?u=76281980&t=193)** of what happened in the EventBridge side. So we have here EventBridge. So if you have never used it, you should search in the box EventBridge and you will be taken into this window. And then if you go down, you can go to rules. And in here, you need to make sure that you are in the default bus. And you can see the rule that we created. You can see here that the event pattern is the source. So it only needs to match events with this source exactly like this. And then if we go here, we have the target. And the target is a CloudWatch log group. And here you can find the ARN for it. So if we click on the log group, it'll open it. I already tested this, so there should be some messages in that log group, but we can test it together in a second when the StateMachine finish executing. So you can see that there are different log streams because we gave permissions to EventBridge to create log streams. And then inside the log streams, there are log events that also EventBridge has permissions to put. And the events look something like this. You can see that there are the events coming from the StateMachine. It shows all the information from the StateMachine that we wanted. And the message is that URL from S3, and we can wait for a second to this finish executing. Now it finish. You can see here that the event has been put
>
> **[4:47](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-integrate-to-eventbridge-event-bus-to-send-an-event?u=76281980&t=287)** and here we can see the definition and the output and all this information. So if we refresh, well, it's actually here. It already refresh it for us and we can click and we can see that the event is exactly the same. So this is the solution for the challenge.

> [!info]- Semantic Content
>
> **Env Vars:** yaml (2), api (1), arn (1), url (1)
> **UI Navigation:** go to (4), click on (1)
> **CLI Commands:** make (3), find (1)
> **Definitions:** is a  (2)
> **File Paths:** template.yml (1)
> **Exercise Files:** template (1)
> **Best Practices:** always make sure (1)
> **Speakers:** - [instructor] (1)


### 6. Build a Chatbot with GenAI

[↑ Back to Table of Contents](#table-of-contents)

#### [What is an Amazon Bedrock agent?](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-an-amazon-bedrock-agent?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-an-amazon-bedrock-agent?u=76281980&t=0)** - [Instructor] In the first project of this course, you learn how to create a workflow to orchestrate your application. You program that orchestrator using step functions. You decide what was needed to call, in what order, with which parameters, you manually program all the [Data Transformation](../../Skills/Software%20Development/Data%20Transformation.md) that was needed in order to call those services. What if you could use [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) to orchestrate all those calls and do that for you? In this part of the course, you are going to learn how you can do that. For that, the first thing you need to know is that you're going to use an agent. An agent is trying to mimic human behaviors. Like traditional applications, these agents don't follow a script. They can remember, retrieve, and interact with different systems. Based on what the user ask them to do, they will decide the calls and the data transformations to the different services and [Databases](../../Skills/Software%20Development/Databases.md) by using reasoning. Agents are ideal for synchronous communications between users and machines. As the user can ask the agents to do many things and the agents can return the answer to the users. For example, agents are very good at doing customer service roles or to be sales representative, or to help customers to answer questions that they need to access, for example, to organization data that the model is not trained with. These agents, for example, can book flights,
>
> **[1:33](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-an-amazon-bedrock-agent?u=76281980&t=93)** file insurance claims, order replacement parts by interacting with different company systems. When working with Amazon Bedrock, you can build Bedrock agents. Bedrock agents plan and execute a multi-step task by calling different APIs and looking into different data sources. In this section, you'll learn how to create a Bedrock agent. Bedrock agents analyze the request that the user makes, create a plan on how to orchestrate all the different calls, and then performs all the actions to fulfill the user's request. When working with Bedrock agents, you can do all these without engineering prompts, managed session context, or manually connecting the systems, and sometimes it feels a little bit like magic. Because it feels like magic, Bedrock agents gives you visibility on the chain-of-thought, reasoning for transparency. With this, you can also troubleshoot the agent and modify the configuration for better responses.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Data Transformation](../../Skills/Software%20Development/Data%20Transformation.md) (1), [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **Analogies:** for example (3)
> **Warnings:** troubleshoot (1)
> **Speakers:** - [instructor] (1)

#### [What you are going to build](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-you-are-going-to-build?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-you-are-going-to-build?u=76281980&t=0)** - [Presenter] Great! You're going to build an agent, but what really are you going to build? In this video, you will learn a little bit more of the architecture of the agent you're going to be implemented in this project. Your agent will be a weather agent. You can ask questions to the agent about like, what is the weather for today in a particular city? Or you can ask similar questions of what is the weather forecast in the next five days for a particular city? And the agent will get an answer to you in a friendly way that is readable, that is nice, and that it displayed good, so not adjacent. You are going to build this using an Amazon Bedrock agent and an Amazon Bedrock knowledge base and AWS Lambda functions. A knowledge base in a nutshell is a data source of proprietary information that your agents can access to answer some query that the user has. But don't worry if you don't understand what it is. We have a whole video dedicated to knowledge bases. The agent will connect to the knowledge base and to the function to resolve the query that the customer has. Our agent will respond to two types of queries, as I show you: the weather for a location right now, for today, and the weather for a location in the next five days. Each of these questions will be answered by calling a different API as the agent doesn't have access to the internet directly to figure this out by himself. For that, the agent will call Lambda function
>
> **[1:35](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-you-are-going-to-build?u=76281980&t=95)** and that function will call an external service to get the weather. The cost to third-party service are very specific, and the agent needs to know how to populate all the different parameters that the function needs in order to call that weather service. For that, every functionality that the agents needs from the Lambda functions needs to be defined with an OpenAPI schema. In our example, our OpenAPI definition will have two APIs defined: one for the weather today and one for the weather forecast in the next five days. In that schema definition, you will find all the inputs and output parameters for each of the functionalities. You will see how this is done later in this section. In our example, the third-party API takes us an input a latitude and a longitude. To make it easier for the user, we want the user to ask for the weather using the city name and the country as nobody really knows the latitude and longitude of the different cities. Here is where the knowledge base comes into play. The knowledge base will have a list of all the cities with its latitude and longitude. When they use a request the weather forecast for a city, the agent will go and [Fetch](../../Skills/Web%20Development/Fetch.md) the latitude and longitude for that city from the knowledge base before calling the Lambda function. And it will pass to the Lambda function, only the latitude and the longitude instead of the city name. And with all this in place, you will have a working agent. Let's build it in the following videos.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Cross-References:** in the next (3), later in (1)
> **CLI Commands:** aws (1), find (1), make (1)
> **Env Vars:** api (2), aws (1)
> **Definitions:** is a  (1)
> **Speakers:** - [presenter] (1)

#### [Creating a Lambda function for the agent](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-lambda-function-for-the-agent?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-lambda-function-for-the-agent?u=76281980&t=0)** - [Instructor] Now you know what an agent is, but we are not going to start building an agent yet, because the first step in order to have an agent is to develop the functions that the agent needs in order to fulfill the user request. So our agent will be giving the weather for now. That's one of the things that the user can request. So we want to create a function that calls this external API to get the current weather for a particular location. For doing this, we are going to use a service that is called Explore California (Weather), that is built by the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) team. This is a fake weather service so the data that you will getting back looks weird if you know the place. So don't panic, but it returns a five-day forecast for 7,000 cities around the world. It's fake, but it works for the demo purposes. I will leave you this PDF where you can find the API definition as an attachment so you can get it and you can see how it works. And if you want to get the real values, you can see how easy is with the code that I'm providing you to exchange this API with a real API that gives you the weather. So now that you know that, let's go to the project and start building. So the first thing we want to do is to create a new directory. I will call it project2. And in here what I will do is I create the template.yml. So I will put in the template.yml
>
> **[1:36](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-lambda-function-for-the-agent?u=76281980&t=96)** basically similar start that we had before and we will have one Lambda function for now. This Lambda function will get the current weather for our user and it'll have the code based on the root directory of the project2 in a file called app in the method code handler. So we can go and create that. And you'll see that this function has some weird things and this is not something to be worried. It's because of the integration with Bedrock. So when we define these Lambda functions, you need to make sure that your Lambda functions get triggered and understand the event object that Bedrock is sending, and also can respond in a way that Bedrock can understand. So I will leave you this link as well in the video's material where you can find what is the input that Bedrock will send to Lambda and also what is the response that Lambda should send to Bedrock. But we'll see this in a second in more detail when we get started building our function. So the first thing we are going to do is to use a third-party library called axios. And this will allow us to call third-party libraries like this Get Weather API. Then we are going to start building the handler. And for that, we are going to get an event object. If you want to know what is in your event object, you are always free to print it out in the console so you can see what is there. We can get the apiPath.
>
> **[3:10](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-lambda-function-for-the-agent?u=76281980&t=190)** And if you see this apiPath, it's in the input and it's here. And this will tell us what is the API that Bedrock agent decided that wants to execute because we could have many functionalities inside one function. So in this case, we can do if the Weather API is weather. That's the one that we might want to direct this call. Then we can get the latitude and the longitude from the parameters. And again, these parameters are coming from these parameters that are here in the event object. How I know which one is one and two? We'll see it in a second when we define the API for this. Then the next thing I'm going to do is to call the third-party API to get the weather, pass in that latitude, that longitude and the amount of days that I'm interested in getting. Now it's one, it's the current weather and then I'm populating that response object with the weather for today. Now let's build the response that the function will have. So the response is this object called result. And you can see that it's a very similar format that the one that is in the documentation. A lot of these things in this result object comes from the event object itself. So basically we are just passing it through the actionGroup, the apiPath, the httpMethod, all those are just basically getting from the event object
>
> **[4:47](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-lambda-function-for-the-agent?u=76281980&t=287)** and passing it to the response. Then we can decide if this was a successful code, what is the response in this case, the weather for today. And then if there is some sessionAttributes or promptSessionAttributes that we want to be aware. Let's send that back to Lambda. And this is done. Well, kind of because we are missing the calling of the third-party API. So let's add that. And this is the part that you can replace with whatever API you're using. If you don't want to use the fake one, don't use it, just use your own URL. And here we just pass the latitude, the longitude, the amount of cities we want the result and the unit I wanted in Celsius. And then we call the response. And because this returns many days, I will just get the amount of days, it's an array. If I want one day, I need to do one minus one. So I get zero position and I will just return that back. So now we have our function, we have our template.yml. We are missing one thing that is to initialize this project 'cause this is a Node project and we might want to initialize it so then we can run the install axios. This is a dependency, a third-party dependency that we need to have in our Node modules and our package.[JSON](../../Skills/Web%20Development/JSON.md) before deploying so it works. Now the next step is to deploy this and we would like to deploy it
>
> **[6:22](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-lambda-function-for-the-agent?u=76281980&t=382)** with sam deploy --guided and follow the steps as we have here. So sam-linkedin-serverless-ai-project2
>
> **[6:34](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-lambda-function-for-the-agent?u=76281980&t=394)** can be a good name. Then the region is the one where you have enabled Bedrock or if you want to use a different region, make sure that you have enabled Bedrock. But we have enabled it for this region in the previous exercise and now we go through all the defaults and we wait for this to deploy. While this deploy, I will be creating a new file here called payload.jsonN. And in here I want to put some testing data so we can test this function. So this will be kind of brief event object that the Lambda will get from Bedrock, the weather API, the "httpMethod": "GET", the actionGroup we can call whatever we want. And the parameters here for the city. The city, if you don't know, it's Montevideo, Uruguay. So we can test and invoke the API. So now let's do that. Let's clear a little bit here. The function has been deployed and we can do sam remote invoke. That's just the stack name. This is the stack name that you just created, the deployed. If you don't remember it, you can always check it in your samconfig file. It's there. Then the event-file is that payload.json. So make sure that your payload.json is inside the same directory. And this is the name of the function, the GetWeatherFunction. So let's click enter and this will invoke the function in the cloud. And we can see here the response of the function.
>
> **[8:08](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-a-lambda-function-for-the-agent?u=76281980&t=488)** So to make sure that it makes sense, it has the same format that what we expect. And you can see here the response body, this is the date of today, the temperature, it doesn't make any sense because in Uruguay, it never gets to minus degrees, but well, maximum temperature, season minimal, season maximal, condition_name and you can see watch out for storms. And there is some videos and things like that that we can see. So this is working. Now, you have a working function that takes a latitude and a longitude and returns a JSON with the weather for that city. As you see, the JSON is not very friendly and it has a lot of information that we don't need. But don't worry, let's see what kind of magic the agent can do in order to get a response that is friendly for the user. You will see it in the following videos.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (5), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2)
> **Env Vars:** api (13), json (2), pdf (1), url (1)
> **Code Identifiers:** apipath (3), actiongroup (2), httpmethod (2), sessionattributes (1), promptsessionattributes (1)
> **CLI Commands:** make (5), find (2), node (2)
> **File Paths:** template.yml (3), payload.json (2), package.json (1)
> **Definitions:** is a  (3), is called (1)
> **Exercise Files:** template (3)
> **Warnings:** be aware (1), watch out (1)

#### [Creating an Open API for the function](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-open-api-for-the-function?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-open-api-for-the-function?u=76281980&t=0)** - [Instructor] You might be wondering how the agent knows which function to call and how to call it. I told you that there is some magic involved, but honestly, there is just an API definition for this to work. For the agent to know what is in the API, you need a file that defines that API so the agent will know how to invoke it and when to invoke it. In this video, I will show you how to do that. In order to fulfill the user request, the agent will call one or more Lambda functions. But in order to do so, it needs to understand what each function does, what parameters the function needs as an input, and what it produce as an output. For doing that, Amazon Bedrock agents use open API definition that you can write for each of your Lambda functions. So let's do that. The first thing we want to do is to create a new file, called whatever you want, but I will call it openAPI.[JSON](../../Skills/Web%20Development/JSON.md). So this file will have the definition for our open API. It's a json as the title says, and it has some structure. If you want to learn more, just search in the internet for the definition. This is standardized definition. And the first thing we want to do is to write the info about the API. title, Weather Agent API, descriptions, exposes the weather agent functionality. And then the interesting bits is the paths. So the path is for each of our API path
>
> **[1:36](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-open-api-for-the-function?u=76281980&t=96)** that you remember that when we created the app, we have here this API path. So for each of those, we need to have a definition. And then we can define if it's a get or something, but let's keep it with the get. So here, the first important thing is to have a description of the API path. We are using [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md), and generative AI looks at texts like human will do. So here, descriptions becomes very, very important. And this is something you can keep on tweaking and exploring if the definition doesn't help you. This one, I put gets the weather for today for a specific latitude and longitude that represents a city. The next thing we want is the parameters. So the parameters are another very important thing that we need to define for each of our API paths. Do you remember that I told you that we have parameter one is the latitude, and parameter two is the longitude. Well, it comes from here. Parameter one is the latitude, and then parameter two is the longitude. This is how Bedrock and the Lambda will know in which order they are. And again here, the description is very, very important. Just make sure that you state what is this parameter. And then finally the response. And this is the final bit of our definition. We have one API. So here in the response,
>
> **[3:08](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-open-api-for-the-function?u=76281980&t=188)** we will say the description is very important. What is that we are returning? Today's weather description for the given city. We can be more descriptive, yes, but yeah, that's what it is. And then because we are returning a json, we want to make sure that we are returning it in that format. So now we are ready. We have our API definition here. And what we can do is upload it to the cloud. So, there is many ways to do it, but I would like to create a bucket for this particular purpose. So I will be creating a new S3 bucket, and I will create it in the infrastructure so then it's easy to remove. And I will call this bucket Agent Bucket. So let's create it here. And this bucket will have a name, and the name, it will come from your parameters because you know buckets need to have a unique name. And if I give one here and then you deploy this file, it will fail. So when we create this, we were going to do some deploy that's just guided again, because we want these parameters to update. We can then, here, put the bucket name that we want. Feel free to do whatever you want. I will put this one. You cannot use it because I might have this one already owned by me. And then we say yes to all the defaults. This will update the configuration and create the bucket and will not touch the Lambda function
>
> **[4:41](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-open-api-for-the-function?u=76281980&t=281)** because we have not touched it. And then when the bucket is created, we can upload that openAPI.json file, and we are ready to continue with our agent. So, let's wait for this to deploy. So we have our bucket in place. And now that is created, let's upload that open API file to the bucket. To do so, you can run this command, aws S3 cp, copy the API json to that bucket, and that will make the trick for you, upload the file into the bucket. And if you go to your AWS console, you will see it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (5), [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (2)
> **Env Vars:** api (15), aws (1)
> **CLI Commands:** make (3), aws (2), cp (1)
> **File Paths:** openapi.json (2)
> **Code Identifiers:** openapi (2)
> **Tools:** aws console (1)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)

#### [What is an Amazon Bedrock knowledge base?](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-an-amazon-bedrock-knowledge-base?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-an-amazon-bedrock-knowledge-base?u=76281980&t=0)** - [Instructor] In a previous video, I mentioned knowledge bases. Let's explore what are those in this video. You can use a knowledge base to connect foundational models in Bedrock to your company data for retrieval augmented generation, or what is called as RAG. RAG is the technique that invokes fetching data from a company at the source, and enriching the prompt with that data to deliver more relevant and accurate responses. Knowledge bases for Amazon Bedrock is a fully-managed RAG that allows you to customize your foundational model's responses with contextual and relevant company data. This is really, really useful when you don't want to retrain the models because the models are big and it's a lot of information, but you just want to enrich that response with your own data. So you can get models to access a specific information from your organization to generate more relevant context, specific and accurate responses without the need to retrain the model. And when this information is fetched from the knowledge base, it comes with a source attribution to improve transparency and hallucinations. Knowledge bases for Amazon Bedrock automates the end-to-end RAG workflow, including the ingestion, retrieval, and prompt augmentation, eliminating the need for you to write custom code to integrate the data sources and manage the queries. With your project, you're going to build a knowledge base
>
> **[1:33](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-is-an-amazon-bedrock-knowledge-base?u=76281980&t=93)** with a list of cities with its latitude and longitude. The agent will know how to [Fetch](../../Skills/Web%20Development/Fetch.md) the data and how to use it to call the lambda function in the next step. But you'll see that a knowledge base can have all kind of organizational information. You can put your sales data, your inventory, a collection of reports with trends of your company's marketing, and the knowledge base will make sure that the data is accessible when you create it and when you query it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Env Vars:** rag (4)
> **Cross-References:** previous video (1), in the next (1)
> **Definitions:** is called (1), is a  (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Creating a knowledge base](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-knowledge-base?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-knowledge-base?u=76281980&t=0)** - [Instructor] Now that you know what a knowledge base is, let's create one. The first thing we want to do is to get some data to populate that knowledge base. For that and for our demo purposes, what we'll do is get a list of cities, latitudes and longitudes from this website called SimpleMaps that allows you to download these files. So if you click download, you will be able to get these comma separated value file that it looks something like this. It has the city and then the latitude and longitude for thousands and thousands of cities, so this is very convenient. So you download that, I put it in the project, so it's easy to know where it is. But then the next thing you need to do is to upload it to S3, so the Bedrock Knowledge Base can find it. And for doing that, we are going to use the same command that we did before. That is the AWS S3, copy the name of the file to the bucket that we created in the previous video. So we wait for a second, that uploads this file, and then we are ready to go and create the knowledge base. So let's wait for this a second and now let's go to our AWS console. We could create the knowledge base as [infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md), but because this is quite a long process, I want to show it to you from the console.
>
> **[1:34](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-knowledge-base?u=76281980&t=94)** But if you would like to know how it's done as infrastructure as code, I will give you some material in the resources of this video. So in Bedrock, you go here, Knowledge base, and then you can create a knowledge base. So here we will put a name, we will call it the cities-lat, longitude, I don't know. And then here we will put a description for this knowledge base. We can put something like, "List of cities with its latitude and longitude."
>
> **[2:14](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-knowledge-base?u=76281980&t=134)** We will create the permissions, we will let this do it for us, and if you want to add some tags, you can do that next. Now let's choose the bucket that we created previously and look for that file. So we can browse that bucket and you can see here, that is this worldcities CSV file. So we can get the URL, here, copy, URL, and we can paste it here and then we can click Next. The next thing we want to do is to select the embeddings model and this is how the data is going to be transformed into vectors. I will just put the Titan, but feel free to pick the one that you want. And then you can decide what kind of database you're going to use to store that. I will recommend to go for the create a new one unless you have something already so let's create, Next. And now you are ready to get started so let's create the knowledge base. This will take a while because it needs to basically grab the data, transform it into vectors, and store it in the vector database. And also it needs supervision, the Amazon OpenSearch Serverless vector store in our account, so this is a process that will take several minutes to complete.
>
> **[3:50](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-knowledge-base?u=76281980&t=230)** When this is ready, I will be back and we can test it together. So now the knowledge base is created, you can see here, and we can make sure that the data is synced. So it'll take a second to sync the data source, if something change in the S3 bucket and then we can test it. So you can see here that there is a testing knowledge base, and here we can test that the knowledge base that what we want. So we need to wait for the sync to complete, and then we can test. You can see here that the data is syncing. It should be done in any time and this takes a little while. While we wait, we can select a model in where we want to test. I will use the Claude here, if it allows me then... So [Zoom](../../Skills/Software%20Development/Zoom.md) out, I cannot see the apply. And now we have the model and we just need to wait for this to finish. And then we can start testing the knowledge base. So now that the knowledge base is created and sync, we can test, so we selected the model already. So we can put something like this, and then we wait for the response to be generated.
>
> **[5:29](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-knowledge-base?u=76281980&t=329)** What will happen here is that it will go to the source and look for the answer. So it show us here the source details, so we can see the chunk where it's looking for the information. So somewhere here it says Paris, and it's returning the information for that. We can do the same with other cities like Madrid, and it should return as well, that information. So here we can see the Madrid and we can do some configurations on the different search type so I keep the default, but you can play and see which one is better. For now, I just keep that default one and if we want to check, I don't know... For another city, we can run the same. And this will always go to the source and find the details. And this is the knowledge base, now we are ready to get the agent.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (2), [Zoom](../../Skills/Software%20Development/Zoom.md) (1)
> **CLI Commands:** find (2), aws (2), make (1)
> **Env Vars:** aws (2), url (2), csv (1)
> **UI Navigation:** go to (2), select the (1)
> **Definitions:** is a  (2)
> **Cross-References:** previous video (1)
> **Tools:** aws console (1)
> **Speakers:** - [instructor] (1)

#### [Creating an Amazon Bedrock agent](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-amazon-bedrock-agent?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-amazon-bedrock-agent?u=76281980&t=0)** - [Instructor] Now you have all the pieces that you need to create your agent. You have the function that calls that external API to get the weather with the latitude and longitude. You have the open API definition, so the agent should know how to call that function. You also have the knowledge base, with a list of cities, names, and the latitude and longitude, so we can figure that out. And let's now build an agent that can provide the current weather in a friendly manner. So, we are going to create an agent from the console again. So, Agents. And here, we can create an agent. We can put a name, agent1, and then we can put some kind of description, just whatever you want. And we will have user input, so this means that we'll have a chat with the user, so that's something we want. And we will let these consult to create the permissions for us. So, let's go Next. Now we need to select what model we want. We will use Anthropic and Claude 2.1. And this is important, these are the instructions for the agent, what the agent will be doing. So, this one, you might need to tune a couple of times in order to get something that works for you. But I will write something like this. You're an agent designed to help customers get the weather details for a city. The customers will provide you with the city name and country, and you will need to translate that information into the latitude and longitude for the city. With that information,
>
> **[1:34](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-amazon-bedrock-agent?u=76281980&t=94)** you can [Fetch](../../Skills/Web%20Development/Fetch.md) the weather information for the customer in a friendly manner. And here, we have the action groups, and this is the calls to the different lambda functions. So we can call agent-lambda-1, and then some description, lambda that returns the weather. Here, we look for the function that we have deployed previously. And here what we need is the URI of the openAPI. So, we can search for this bucket, and the openAPI definition. So, we can copy the URI, and then paste it here. You can also Browse S3 from here and get that information. And then we click Next. Then it's time to configure the knowledge base, we only have one. And again, here we need to have some instructions to the knowledge base. And we can put something like this, "Search for the latitude and longitude for the city and country that the user has provided in the input." Good. Now we review this. We can modify it later if we want, but we can create agent. This will take a little while, and when this is completed, we can test. But before testing, we need to do one thing, that is to give permissions to the agent to access the lambda function, because if we try it now, it will fail. So one thing that you need to have now is the agent ARN, and you have it here.
>
> **[3:07](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-amazon-bedrock-agent?u=76281980&t=187)** So, keep that one in mind. And then let's go to the template YAML from the project2, and add a couple things here. First, we want to add the parameter, AgentARN, so we can pass it in our parameters, everybody will have a different one. And then we will get some permissions for lambda. So, we will put it here. And what this will do is we allow the agent to invoke the lambda function from bedrock. So, this is important for us to be able to call the function. So, let's save this, and run sam deploy --guided so we can update those parameters. The stack name, everything stays the same, but now we need to pass the agent ARN. So, let's grab it from here, paste it here, and then let's go with all the defaults. Now this should deploy the template YAML with the parameters that we just changed, so that's good. You can see the ARN for the agent is updated, so when we test the agent, it will just work. Let's wait for this to deploy, and then we can go to test. So now, we have everything ready, and we are ready to test. Let's ask the agent for something, so we can, "Give me the weather for today for Paris,"
>
> **[4:40](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-amazon-bedrock-agent?u=76281980&t=280)** and see if this returns something. It should be looking for the latitude and longitude in the knowledge base. And then it should invoke the Lambda function to give us the weather. So, let's see what happens. I don't know, it didn't understood the city. So, we can give it again. This is because we have asked the agent that it accepts user input, so it will chat with us, so if it doesn't understand something, it will ask for more information, so this is kind of the smart part of the agent, even though we have given the city in the prompt, in the initial prompt. So, "Here is the weather for today in Paris, this is the date, the condition is partially clouded. Temperature range from 5 to 9." So, you can see, this is very different from what the [JSON](../../Skills/Web%20Development/JSON.md) is returning from the external API, it's formatted. But we can see here the trace, and this is something that is quite interesting when we are working with this [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) orchestration. So, we can click here, and we can see what the agent is doing. So, this is all the orchestration the agent is creating, and it's based on all the prompts that we passed, and the API. So, "The user has confirmed the city is Paris, now I will look for that using the search tool." So you can see, now it's going to knowledge base, and it's asking what is the latitude and longitude for Paris, and then the knowledge base is responding,
>
> **[6:15](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/creating-an-amazon-bedrock-agent?u=76281980&t=375)** "This it is." And if we continue going down, then we have step three. Let's see what is going on here. We have the information. And in some point, we should be calling the lambda function. But you can look at all this information, and it says, "Well, I have the latitude and longitude for Paris from the search tool is this one, now I will use these to get the coordinates to get the weather for today in Paris." And you can see, it creates that invocation input that we have defined in the lambda functions, this thing that we needed, the weather parameters it knows with API, and which parameters to pass, and then it will call the action group, and then it will execute it, and it will get the JSON back, and then it'll transform it. So, all of this, the agent will take the decision by itself.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (2), [Fetch](../../Skills/Web%20Development/Fetch.md) (1), [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (1)
> **Env Vars:** api (5), arn (3), uri (2), yaml (2), json (2)
> **Code Identifiers:** openapi (2)
> **UI Navigation:** go to (2)
> **Exercise Files:** template (2)
> **Prerequisites:** configure (1), you need to have (1)
> **Versions:** 2.1 (1)
> **Definitions:** means that (1)

#### [Challenge: Add another function to the agent](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-add-another-function-to-the-agent?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-add-another-function-to-the-agent?u=76281980&t=0)** (lively music)
>
> **[0:05](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/challenge-add-another-function-to-the-agent?u=76281980&t=5)** - [Instructor] I have another challenge for you. Can you add new functionality to the agent that it can also return the weather for the next five days for a city? This is how the agent should look after you make the change. Weather forecast for Berlin, Germany. And here's the five-day weather forecast for Berlin, Germany, and it shows you the five days. So this is more or less what you need to do. Some information that you might want to have in mind. You can use the same Weather API, at least the one that is provided by [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning allows you to return like 10 days. So you might need to modify the code that we have before so you can get the five-day forecast, which only one day. And don't forget to add the API definition to the OpenAPI.[JSON](../../Skills/Web%20Development/JSON.md) and upload it to S3 so that it's ready for the agent to use. And when you do that change, you can go to the agent, work in draft and click prepare. That will get the OpenAPI information into the agent so it'll get ready for you to test. In the next video, I will show you how I solve this problem.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Env Vars:** api (2)
> **File Paths:** openapi.json (1)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **UI Navigation:** go to (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Add another function to the agent](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-add-another-function-to-the-agent?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-add-another-function-to-the-agent?u=76281980&t=0)** - [Instructor] Let me show you how I solve the challenge number four. The first thing I did was to go to our function, our existing function, and modify. Do remember the API path. So I create a new one. We have weather that returns the current weather, and then I created one that says weather for the next five days. The inputs are the same. And then I call this get weather with five instead of one. So let's see, because I also did a modification in this get weather method that now I'm doing a splice between zero and the amount of days. So if I get like one day it will return the position that is in zero. If I get five days, it'll return from zero to five. And that's the array and resulting. So we can deploy this and test it. So some deploy this will deploy it to the cloud and then we can run and invoke the function. For invoking the function, we can test with the payload that we had and make sure that the one day also works. And we can also test with a new payload that I created. So we can have payload number two here, that test with the five days forecast. So we can see how both of these APIs work.
>
> **[1:36](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-add-another-function-to-the-agent?u=76281980&t=96)** So let's wait for this to complete the deployment and test it. So now this is deployed and we can do a remote sum invoke with the payload one, this only test one day. So let's see if the result's still working, invokes the lambda function and we can see that the response and the response body is here. It has one day, so that's good. And then we can run the same query, but with the payload two. And that is invoking the API for the five days forecast. And let's see if it returns five days. And now we can see here 1, 2, 3, 4, 5. So everything is good. So the next thing we need to do is to modify the open API [JSON](../../Skills/Web%20Development/JSON.md). So I already did that. We have the existing one here with the weather and that stays the same. And then I added the weather five days, exactly the same name as you can find in the function. Get. And I added some descriptions, get the weather for the next five days for a specific latitude and longitude that represents a city. The parameters are the same, but I make sure that we change if there is anything about the current weather. And then the response. Now we are returning an array
>
> **[3:08](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-add-another-function-to-the-agent?u=76281980&t=188)** and I specify exactly what is inside that array so it's easier for bedrock to understand. After this, we can make sure that we upload that file to S3 so it's updated. And when we have that, we can go back to our agent and prepare it. This will grab all the information from the open API or whatever change in the function and bring it to the working draft. So now we are ready to test. Let's test this. And the first query we can run is the weather forecast for Berlin, Germany, to make sure that it returns the five days weather forecast now. And here is the response. Here is the five day weather forecast for Berlin, March 21. March 23, March 24, March 25. That's great. And if we click again, we can see the trace and we can ask also for the forecast for today for Berlin, Germany. And let's see if this still understand us. Let's ask it a little bit different. One thing, if you start seeing that your prompts are not being understood, you can always change and play with the prompts here. This is an advanced thing and I will not recommend it unless you really know what you're doing. But usually prompts are the hardest part with [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md).
>
> **[4:43](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/solution-add-another-function-to-the-agent?u=76281980&t=283)** So you might need to try out different ones and fine tune them in order to get the right results. So I have not done this for this demo, but if you want to use input production, that is the thing that will take you the most time. So you can always debug the results by looking here. And sometimes you can also clear the chat to start a new session and also might help. So here you can see that now it worked. Sometimes because we have the whole history in this chat, then it gets a little bit confused. So if you want to try again, just clean the history and start again. And that will start with no session information and you might get a result. So these are a couple of tips for debugging the agents to have in mind.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1), [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (1)
> **CLI Commands:** make (4), find (1)
> **Env Vars:** api (4), json (1)
> **UI Navigation:** go to (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [What we built](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-have-we-built?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-have-we-built?u=76281980&t=0)** - [Instructor] In this course, you built two projects. Let's review them. The first project was an asynchronous orchestration project in which you upload a file, like an audio or video to S3, and that triggers a workflow. The workflow contains the orchestration for an application that uses different AI services and [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md). The application called different AI services to perform tasks like transcription, translation, and generative AI to produce a summary and a poem. Also, the workflow at the end sends an email when the process is done or puts an event to event breach when the summary is completed. This project is a good example of event-driven architectures as the workflows respond to environment events and also puts events into different event buses and notifications when things are completed. These orchestrations are great for asynchronous applications and when you have a series of tasks that you need to accomplish to get a result, when you need to combine multiple AI services, or you want to call different foundational models. Also, this orchestration approach is great when you need control over [Data Transformation](../../Skills/Software%20Development/Data%20Transformation.md) and service calls, as you control the API calls and you transform the data as you need to. However you need to plan for all the application path and if some new personality gets added, you'll need to change the orchestration implementation.
>
> **[1:35](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-have-we-built?u=76281980&t=95)** The second project was a synchronous orchestration creating a chatbot using generative AI. This project was built using bedrock agents, knowledge bases, and Lambda functions. This chatbot responds to a customer asking for the weather in a city, the current weather, or the weather in the next five days. The agent, in order to [Fetch](../../Skills/Web%20Development/Fetch.md) their answers from the API to answer these questions, needs to enrich the request with data obtained from the knowledge base. So it will get the latitude and the longitude from the knowledge base, and then call the external weather API with that latitude and longitude for the city that the user has provided. Here, the agent is the one responsible for creating the orchestration, for making the calls to the different services, to deciding which services need to be called and when. It will fetch the data needed from the data sources and then call the APIs as needed. This solution is great for synchronous scenarios where you don't know the exact workflow that the user will do, or that the user might not provide all the necessary information to fulfill the request, as the agent can ask for more information if they need it, and consult data sources to fill the gaps of knowledge. However, as the data transformations and calls to the different services are orchestrated by the agents, the results that you can obtain are unpredictable. And the way to improve those results is by tuning natural language prompts
>
> **[3:09](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/what-have-we-built?u=76281980&t=189)** that are built into the agent, and in some cases can be harder to configure and to get right than traditional integration. I hope that by the end of this course, you feel confident in deciding which orchestration model can be the best for your problem and how to leverage generative AI and AI as a developer, as there are many services out there to help you to build your application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (4), [Fetch](../../Skills/Web%20Development/Fetch.md) (2), [Data Transformation](../../Skills/Software%20Development/Data%20Transformation.md) (1)
> **Env Vars:** api (3)
> **Prerequisites:** you'll need (1), configure (1)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Cleaning up the environment](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/cleaning-up-the-environment?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/cleaning-up-the-environment?u=76281980&t=0)** - [Instructor] When you finish this course or you want to stop working on this course for an extended period of time, I recommend you remove all the resources created from your AWS account. Most of the services you use in this course are built in a pay-per-use manner. However, it's a good practice to remove everything you're not going to use and it's important to know that the knowledge base that you created if you leave it on can be quite expensive, so make sure you turn it off immediately after completing the course. All the resources created during this course, like AWS Lambda, S3 buckets, the functions workflows that were created with AWS SAM are easy to remove using the AWS [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md). Other resources like the Bedrock agents and knowledge bases, you will need to delete them from the console. Project1 infrastructure was created with AWS SAM and is quite easy to clean up, but the first thing you need to do is to remove everything that you have in the bucket, like the uploaded videos, all the transcriptions and everything so you can remove the bucket as well. So go to the bucket and then basically delete everything that is in there. You can do this as well from the CLI if you want to, but it's very easy. Just permanently delete all the different files. When you're ready, now you can go to the project and type sam delete to remove everything that it's in your project,
>
> **[1:33](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/cleaning-up-the-environment?u=76281980&t=93)** so make sure you're in the project directory. I'm in project1, sam delete, and then it will ask me if I want to delete the stack. I will say yes. Then it will ask me if I want to remove the bucket for SAM where all the artifacts are. I will say yes, and then everything will start getting deleted. It will delete everything that is defined in the stack so we can make sure that nothing was left behind. This takes a while because it's deleting everything, but you can always recover everything by just running sam deploy in your AWS account and you will get back everything. When everything is deleted, now you can move to deleting project number two. Removing project number two is a little bit more tricky because the first thing you need to do is as you did with project number one, go to the S3 bucket, remove everything you have in the bucket. In this case, it's the OpenAPI.[JSON](../../Skills/Web%20Development/JSON.md) and the wolrdcities.csv file. Then you can go to the project number two and type SAM DELETE there to remove the bucket and all the Lambda functions, and all the different things that you created. And finally, when you delete everything from the S3 bucket and from the SAM infrastructure, you can go to Bedrock and delete the agent and the knowledge base. So go to the knowledge base that you created. You can click here and say Delete. This will take a while and then you can go to the agent as well
>
> **[3:08](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/cleaning-up-the-environment?u=76281980&t=188)** and click Delete. Then everything is getting deleted and you can find that the agent was deleted successfully and the knowledge base has been deleted successfully. So you're good to go. Remember, you can redeploy any part of the application that was built with AWS SAM easily just by running the command sam deploy. If you have that SAM configuration file that was created before, you don't need to add the --guided because you have already the stack configured. If you don't have that file because you remove it and you're just getting the project back from [GitHub](../../Skills/Software%20Development/GitHub.md), you just type sam deploy --guided and input all the parameter's configurations as we have been doing during this course and you're ready to go. SAM deploy will recreate all the AWS resources with new ARNs, Amazon Resource Names, but the applications will work exactly as before. For other parts that you build using the AWS console, you will need to follow the steps that we provided in this course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Env Vars:** aws (9), sam (8), cli (2), delete (1)
> **CLI Commands:** aws (9), make (3), find (1)
> **UI Navigation:** go to (7)
> **File Paths:** openapi.json (1), wolrdcities.csv (1)
> **Tools:** github (1), aws console (1)
> **API Endpoints:** delete  (1)
> **Definitions:** is a  (1)

#### [Next steps](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/next-steps?u=76281980&t=0)** - [Marcia] And this bring us to the end of the course. At this point, you should be able to understand how to use AWS AI services and Amazon Bedrock, how to create a workflow using Step Functions, and how to create an Amazon Bedrock agent with a knowledge base and Lambda functions. Also you should be able to create and deploy many of these things using [infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md), in this case, AWS SAM. And you can test these different resources as part of your application, but this is not the end of your learning. I encourage you to keep experimenting and learning about these projects and also use the projects that you have built, extend them, add them more things, and play around because there is a lot of things that you can do in order to improve these projects and learn by doing so. If you want more resources to learn more, I leave you here, my YouTube channel where I have over 300 videos related to AWS and serverless and [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md). Serverless Land is the website that aggregates a lot of the serverless content curated by AWS serverless developer advocates. You can find there examples on AWS SAM. Other infrastructure as code like [Terraform](../../Skills/DevOps/Terraform.md) and CDK, blog posts are learning guides to keep on learning. You can follow me on social media, for example, Twitter, [Instagram](../../Glossary/Service/Instagram.md), or [LinkedIn](../../Skills/Web%20Development/LinkedIn.md). I always update them with news and announcements from different AWS serverless services.
>
> **[1:34](https://www.linkedin.com/learning/building-serverless-apps-with-ai-services-on-aws/next-steps?u=76281980&t=94)** And don't forget to check back here often as we continue to add more AWS and serverless courses to our LinkedIn Learning library. Again, my name is Marcia Silva and thanks for watching.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2), [Generative AI](../../Skills/Artificial%20Intelligence%20(AI)/Generative%20AI.md) (1), [Terraform](../../Skills/DevOps/Terraform.md) (1), [Instagram](../../Glossary/Service/Instagram.md) (1)
> **Env Vars:** aws (7), sam (2), cdk (1)
> **CLI Commands:** aws (7), find (1), terraform (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [marcia] (1)


## Instructor

- [Marcia Villalba](../../Instructors/Artificial%20Intelligence%20(AI)/Marcia%20Villalba.md)

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/buidling-serverless-apps-AI-4454235)

## Skills Covered

- Serverless Computing
- Amazon Web Services (AWS)

## Path Context

### In [Advance Your AWS Development Skills- Exploring Complex Application Development](../../Paths/Artificial%20Intelligence%20(AI)/Learning%20Paths/Advance%20Your%20AWS%20Development%20Skills-%20Exploring%20Complex%20Application%20Development.md)
← [AWS Security Best Practices for Developers](AWS%20Security%20Best%20Practices%20for%20Developers.md) | **5 of 6** | [Building Intelligent Chatbots on AWS](Building%20Intelligent%20Chatbots%20on%20AWS.md) →

## Appears In

- [Advance Your AWS Development Skills- Exploring Complex Application Development](../../Paths/Artificial%20Intelligence%20(AI)/Learning%20Paths/Advance%20Your%20AWS%20Development%20Skills-%20Exploring%20Complex%20Application%20Development.md)

## Related Courses

_Courses sharing skills:_

- [Building Serverless Applications in AWS](../Cloud%20Computing/Building%20Serverless%20Applications%20in%20AWS.md) — Amazon Web Services (AWS), Serverless Computing
- [AWS- Monitoring, Logging, and Remediation (2021)](../Cloud%20Computing/AWS-%20Monitoring%2C%20Logging%2C%20and%20Remediation%20(2021).md) — Amazon Web Services (AWS)
- [Complete Guide to AWS Software Deployment](../Cloud%20Computing/Complete%20Guide%20to%20AWS%20Software%20Deployment.md) — Amazon Web Services (AWS)
- [Choosing A Cloud Platform For Developers Aws Azure And Gcp](../Cloud%20Computing/Choosing%20A%20Cloud%20Platform%20For%20Developers%20Aws%20Azure%20And%20Gcp.md) — Amazon Web Services (AWS)
- [Twilio Essentials Unit 3- All Together Now](../Software%20Development/Twilio%20Essentials%20Unit%203-%20All%20Together%20Now.md) — Serverless Computing

---

[↑ Back to top](#)