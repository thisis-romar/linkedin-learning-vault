---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: creating-a-book-search-engine-from-scratch-using-java-and-github-copilot
url: "https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot"
duration_minutes: 131
duration: 2h 11m
level: Intermediate
updated: 12/11/2024
learners: 3365
skills:
  - GitHub
  - Artificial Intelligence (AI)
  - Spring Boot
  - GitHub Copilot
  - Java
exercise_files: true
github: "https://github.com/hhimanshu/book-search"
thumbnail: "https://media.licdn.com/dms/image/v2/D4E0DAQHuCLqk6OsB8g/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1732734297442?e=2147483647&amp;v=beta&amp;t=-rsRdhbTnzvP3JGFTel1lu1q5kNjqW99M-vr6oUIho4"
linkedin_topic: Artificial Intelligence (AI)
learning_paths:
  - '[[Master GitHub Copilot]]'
prev_courses:
  - '[[Responsible GitHub Copilot- Creating Reliable Code Ethically]]'
next_courses:
  - '[[Refactoring with GitHub Copilot]]'
path_nav: '[{"path":"Master GitHub Copilot","position":6,"total":8,"prev":"Responsible GitHub Copilot- Creating Reliable Code Ethically","next":"Refactoring with GitHub Copilot"}]'
path_count: 1
tags:
  - course
  - topic/artificial-intelligence
  - topic/software-development
  - skill/github
  - skill/artificial-intelligence-ai
  - skill/spring-boot
  - skill/github-copilot
  - skill/java
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Artificial%20Intelligence%20(AI)/Creating%20a%20Book%20Search%20Engine%20from%20Scratch%20Using%20Java%20and%20GitHub%20Copilot.md)

![Creating a Book Search Engine from Scratch Using Java and GitHub Copilot](https://media.licdn.com/dms/image/v2/D4E0DAQHuCLqk6OsB8g/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1732734297442?e=2147483647&amp;v=beta&amp;t=-rsRdhbTnzvP3JGFTel1lu1q5kNjqW99M-vr6oUIho4)

# Creating a Book Search Engine from Scratch Using Java and GitHub Copilot

> In this intermediate-level course, you will learn how to build a comprehensive book search project using Java, Maven, Spring Boot, PostgreSQL, and Docker. Download and prepare book data from a public archive, then design and implement a PostgreSQL database schema optimized for full-text search. Containerize the database with Docker to streamline the development process. Learn how to create RESTful

> [LinkedIn Learning](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot) | 2h 11m | Intermediate | 3K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Modern Java development](#modern-java-development)
  - [Course overview and objectives](#course-overview-and-objectives)
  - [Prerequisites and learning goals](#prerequisites-and-learning-goals)
- [**1. Setting Up Java, Maven, and Spring Boot**](#1-setting-up-java-maven-and-spring-boot) (6 videos)
  - [Setting up Java](#setting-up-java)
  - [Installing Maven](#installing-maven)
  - [Setting up a Maven project](#setting-up-a-maven-project)
  - [Setting up Visual Studio Code](#setting-up-visual-studio-code)
  - [Introduction to the Spring Boot framework](#introduction-to-the-spring-boot-framework)
  - [Creating a hello world API](#creating-a-hello-world-api)
- [**2. Dockerizing the Project with PostgreSQL**](#2-dockerizing-the-project-with-postgresql) (2 videos)
  - [Setting up Docker](#setting-up-docker)
  - [Configuring PostgreSQL in Docker](#configuring-postgresql-in-docker)
- [**3. Designing the Database Schema and Implementing Full-Text Search**](#3-designing-the-database-schema-and-implementing-full-text-search) (5 videos)
  - [Designing the book schema](#designing-the-book-schema)
  - [Create a database schema in PostgreSQL](#create-a-database-schema-in-postgresql)
  - [Navigating the database using a web browser](#navigating-the-database-using-a-web-browser)
  - [Introduction to full-text search](#introduction-to-full-text-search)
  - [Implementing full-text search in PostgreSQL](#implementing-full-text-search-in-postgresql)
- [**4. Ingesting and Validating Data**](#4-ingesting-and-validating-data) (3 videos)
  - [Overview of data ingestion](#overview-of-data-ingestion)
  - [Automating data ingestion](#automating-data-ingestion)
  - [Validating data integrity](#validating-data-integrity)
- [**5. Implementing Business Logic and Writing Tests**](#5-implementing-business-logic-and-writing-tests) (3 videos)
  - [Introduction to business logic](#introduction-to-business-logic)
  - [Developing business logic functions](#developing-business-logic-functions)
  - [Writing unit tests](#writing-unit-tests)
- [**6. Designing and Creating APIs**](#6-designing-and-creating-apis) (3 videos)
  - [Developing the service layer](#developing-the-service-layer)
  - [Implementing the book search API](#implementing-the-book-search-api)
  - [Writing integration tests](#writing-integration-tests)
- [**Conclusion**](#conclusion) (1 videos)
  - [Conclusion](#conclusion)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Modern Java development](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/modern-java-development?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/modern-java-development?u=76281980&t=0)** - [Harit] Want to build a production grade book search engine? In this course, you'll create a powerful search application using [[Java]], [[Spring Boot]], [[PostgreSQL]] and Docker, the exact tech stack that modern companies use. This project will make your portfolio stand out. You will learn to implement full tech search, create restful APIs and containerize your application. We will use modern development tools like VS Code and [[GitHub Copilot]] to build efficiently following industry best practices every step of the way. I'm Harit Humanshu, co-founder of Bonsai Labs where we develop AI powered software solutions for startups. I'll guide you through building a project that you can proudly showcase to your employers. Let's create a production ready search engine together.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Java]] (1), [[Spring Boot]] (1), [[PostgreSQL]] (1), [[GitHub Copilot]] (1)
> **CLI Commands:** docker (1), make (1)
> **Tools:** vs code (1), github (1)
> **Speakers:** - [harit] (1)

#### [Course overview and objectives](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/course-overview-and-objectives?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/course-overview-and-objectives?u=76281980&t=0)** - [Instructor] Welcome to the course on developing a book search engine. Now, in this course, we are going to create a book search engine with the existing data. But before we dive into details and start doing things, it makes sense to [[Zoom]] out a little bit and see how this course will make progress. So in order for us to start the project, the first thing we would need is a database. So every machine works differently, so in order to set up the database correctly and in a repeatable fashion, we are going to set up this database inside a Docker container. And once we have our Docker container up and running with this database, the next thing that we need to do is to ingest this data with books and authors information. Now, for this course, we have already created the CSV files that are sitting in the remote. And what we need to do in this course is to create the ingestion script that will take this data from CSV and insert in the database. Now, with that, we will have our setup ready, which means we have a Docker container that is hosting a database, and that database contains the set of CSV files that contains information about books and their authors. Now, once we have that, the next thing that we are going to do is to, we need to write the business logic. Now, what's business logic? The business logic are the set of functions that interact with the database in order to find the information from this database. Now, as you're building the book search engine,
>
> **[1:34](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/course-overview-and-objectives?u=76281980&t=94)** we are going to dive deeper into how the full text search in the case of database would work. But once we have those nitty-gritty details hashed out, we are going to write set of functions that will be utilized to [[Fetch]] information from this database. Now, this business logic still is a way to write code, but how would your consumers, or how would the callers will execute this business logic? And that's where the final piece will come in, where we are going to create the set of APIs, and these APIs will be utilized by consumers, which will call the business logic. And the business logic will in turn go to the database, fetch this information and send it back to the API callers. Now, in summary, this is what we are going to create. There are lot of things that we need to do right from the scratch. So now that you have the zoomed out view, now let's go back to the next step and see what are some of the things you should know before starting this course and what technologies are we going to make this happen? So see you next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Fetch]] (2), [[Zoom]] (1)
> **CLI Commands:** docker (3), make (2), find (1)
> **Env Vars:** csv (3), api (1)
> **Prerequisites:** set up (2), setup (1)
> **Definitions:** is a  (2)
> **Cross-References:** go back to (1)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)

#### [Prerequisites and learning goals](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/prerequisites-and-learning-goals?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/prerequisites-and-learning-goals?u=76281980&t=0)** - [Instructor] In order to continue with this course, let's find out what all technologies we are going to use and what are some of the things that you must know before taking this course. Now, we are going to write the entire code in [[Java]] programming language, so we are going to use the latest Java. We may or may not be using or utilizing the latest Java features, but familiarity with the language is important. So this is not an introductory course on Java. So knowing Java is going to definitely help you in making progress with this course. Now, the second thing is that we are going to leverage [[Spring Boot]] for majority of our parts when writing the APIs or when interacting with the database. But we assume that you have interacted with Spring Boot already, and you are going to utilize some of your acquired learning in creating the book search engine. Now, if Spring Boot is new for you, there are a lot of fantastic courses already available in the library that you may want to search and pursue before you continue with this course. Now, familiarity with Docker is extremely important. However, it's not a must have because as we make progress, we are going to provide you with a set of commands that you are going to run after installing the Docker. So even if you don't know a lot about Docker, don't worry about it because this course is not about Docker. We are only going to utilize a part of Docker in order to make progress in this course. Now, this is also not an introductory course on [[Databases]]. So we'll assume that you know how databases work, which means how data gets ingested,
>
> **[1:33](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/prerequisites-and-learning-goals?u=76281980&t=93)** writing the [[SQL]] queries. This is all going to be important. We are going to leverage [[PostgreSQL]], but if you have used [[MySQL]] in the past, that is fantastic because there are not a lot of differences between the two when writing the SQL queries. So you will do just fine. And, finally, when we create the RESTful APIs, we will assume that you know about the concept of RESTful APIs, what resources are, what instances are, what are HTTP methods, for example. But if you already know about these concepts, you will make great progress because we are going to leverage the Spring Boot in order to create the GET APIs. We are not going to create new data, we are just going to query the data. So if you know what restful APIs are in general, you will do just fine. Now, one highlight for this course is we are going to leverage [[GitHub Copilot]] for this course. Now, what does this mean? This means that as we write code, we are going to take the assistance from GitHub Copilot, which will help us assist and write code faster. It also means that at parts I will show you the ways where you can get operationally efficient by writing more code upfront or do a pair programming with GitHub Copilot by writing more tests, and this will speed up your entire process. The reason of introducing GitHub Copilot is that more and more, GitHub Copilot has been utilized by organizations of all sizes to speed up the developer productivity. Now, the version of GitHub Copilot that I'm going to use is the latest one. So I'm going to work with the [[GPT-4|GPT-4o]] model, and I also want to ensure that I communicate this upfront that GitHub Copilot is a paid subscription.
>
> **[3:09](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/prerequisites-and-learning-goals?u=76281980&t=189)** So you would need to have a paid license before you can start writing code with GitHub Copilot. But if you don't have one and if you really want to see what all things you can do with GitHub Copilot, this is a fantastic course because I'm going to show you all the different ways where GitHub Copilot can help us write the code, test the code, and if we get stuck, how we can interact with the GitHub Copilot to resolve the issues that we may not be even aware of. Great, so now that we have the logistics part and prerequisites hashed out, let's go into the learning objectives on what are you going to get out of this course. So the first thing that you will learn is how to design and implement a RESTful API in the context of writing a book search engine. The other thing that you will learn is how to write the business logic that handles the interaction all the way by taking the inputs from the API and connecting to the database and doing the round trip. The third thing that you would learn is how to configure a Docker container and host a database locally, which takes away a lot of infrastructure issues away because some of you may be working on [[Windows]] and some other people might be working on [[Linux]] or Mac. Docker is a common platform, and this will allow you to have repeatable builds and repeatable infrastructure setups on your local machine. I'm also going to touch a lot about the full text search capabilities within the [[PostgreSQL|Postgres]]. So Postgres supports full-text search and a lot of use cases might be handled upfront and you may not need to have a paid subscription for a lot of search services like Elastic or [[Microsoft Azure|Azure]]. So I'm going to dive deeper into this
>
> **[4:44](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/prerequisites-and-learning-goals?u=76281980&t=284)** to help you explore the full-text search, and this will help you assess whether or not this is a good use case in your current role and at the current work. And finally, we are going to look at how to stitch all these components together that we saw in the diagram earlier, starting from scratch and how to have a database and all the way of creating an API that can interact with the database and return research results. So this is going to be a fantastic journey, and I hope to see you in the next step where we will put on a doing hat and start making progress on the course that we have to make. All right, I'll see you next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub Copilot]] (11), [[Java]] (5), [[Spring Boot]] (4), [[Databases]] (2), [[SQL]] (2)
> **CLI Commands:** docker (7), make (4), find (1), mysql (1)
> **Tools:** github (11)
> **Env Vars:** api (3), sql (2), http (1), gpt (1)
> **Definitions:** is a  (4), means that (2)
> **API Endpoints:** get  (1)
> **Cross-References:** in the next (1)
> **Analogies:** for example (1)


### 1. Setting Up Java, Maven, and Spring Boot

[↑ Back to Table of Contents](#table-of-contents)

#### [Setting up Java](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-java?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-java?u=76281980&t=0)** - [Instructor] Welcome back. In this module, we are going to install all the necessary software that will help us make progress during this course. The first thing that we will do is install [[Java]]. In order to install and manage multiple Java version, the best thing that we can do is install SDKMAN. If you visit [sdkman.io](https://sdkman.io), you will see a command here. This is a curl command, which means, if you are working either on [[Linux]] or Mac and possibly on [[Windows]] as well, you should be able to take this command, go to the Terminal, hit Enter after pasting it, and install SDKMAN. Since I'm running on Mac, I will go to the Terminal, paste the command, and hit Enter. And in few moments, SDKMAN will be installed on my machine. Now, in order to make sure that it's really installed, I will run sdk version. And we will see that SDKMAN is indeed installed. Now, in order to install Java, we are going to first see what all versions of Java are available. So, for that, I will run sdk list java. And you will see that there is a huge list that comes up here. Now, we are going to focus on the OpenJDK version, so I'm also going to run this command again, and then grep for open. So, as we can see, we have multiple Java versions available here. And the first thing that we will do is install the latest Java version. So I'm going to copy this version over here, run sdk install java, and paste this version. As we run...
>
> **[1:36](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-java?u=76281980&t=96)** In few moments, Java version 24 with patch version 18 will be installed on our machine. So it's doing its thing, and within few moments, we should have it ready. So the download is now complete, and one thing that it has done, it has set this Java version as a default Java version. So if I run java -version, we will see the same version 24 with 18 as a minor or a patch version available to us. Now if you want to install multiple versions, you can continue with the same exercise, so you can say sdk install java, and paste this version. And then the same exercise will happen once over again where the JDK version 24 but with patch version 17 will be downloaded and will be further installed on our machine. So the Java version 24 with patch version 17 has been installed, and now it is asking if we want to make it as a default one, and we will say yes for now. And if we run the java -version command again, we can see, now, it is using the patch version 17. So now we have two version of Java installed, and if you want to switch between one versus the other, all you need to do is sdk use java, and then use the Java version that you want to use as the command.
>
> **[3:12](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-java?u=76281980&t=192)** Now we are using the patch version 18, so if we say java -version, we are back on version 18. What we've done so far, we have used SDKMAN, we have installed SDKMAN on our machine, and using SDKMAN, we have installed two Java versions. We have ensured that the Java is working on our machine, and we also saw how we can switch between multiple Java version using SDK. Fantastic. Now that this part is done, we will move on to the next step where we are going to install Maven to start a new project.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Java]] (22), [[Linux]] (1), [[Windows]] (1)
> **Prerequisites:** install (11)
> **Versions:** version 24 (4), version 18 (3), version 17 (3)
> **Env Vars:** sdkman (7), jdk (1), sdk (1)
> **CLI Commands:** make (3), curl (1), grep (1)
> **Tools:** terminal (2)
> **UI Navigation:** go to (2)
> **Definitions:** is a  (2)

#### [Installing Maven](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/installing-maven?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/installing-maven?u=76281980&t=0)** - [Instructor] In order to manage our [[Java]] project for compilation, building, and testing, we are going to leverage Apache Maven, which is the build tool of our choice. Now, if you go to maven.[apache.org](https://apache.org), you will see a link that says Install. If you go to the Install, it has information on how to do that. This is important if you are on [[Linux]] or [[Windows]]. But for Mac, I'm going to show you a faster way to install Maven on your machine. Now, if you are not using Homebrew already on Mac, Homebrew is a package manager for Mac operating system. If you don't have it available on your machine, all you need to do is to copy this command, go to the terminal, paste, and hit Enter. And in few moments, you will have Homebrew on your machine installed. To ensure that you have Homebrew running, you type brew -v, and you see a Homebrew version. Now, in order to install Maven, all you need to do is say brew install maven. And within few moments, Maven will be installed on your machine. Now, since Maven is already installed on my machine, I got Maven 3.9.9 is already installed, but you will see a different message. But at the end of the command, you will have Maven up and running. To ensure that Maven is up and running, type mvn -v, and hit Enter. And then it'll show you that it is using Maven home, which is available here,
>
> **[1:33](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/installing-maven?u=76281980&t=93)** and what Java version it is leveraging. So, which means, at this point in time, we have Java 24 that is available on our machine, we have installed Maven successfully, and when we checked the version for Maven, it told me that it is 3.9.9, and it was able to successfully locate Java version 24. So we have two things up and running already. Let's move on to the third step, where we are going to create a new project.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Java]] (4), [[Linux]] (1), [[Windows]] (1)
> **CLI Commands:** apache (2), brew (2), mvn (1)
> **Prerequisites:** install (5)
> **Versions:** 3.9.9 (2), java 24 (1), version 24 (1)
> **UI Navigation:** go to (3)
> **URLs:** [apache.org](https://apache.org) (1)
> **Tools:** terminal (1)
> **Definitions:** is a  (1)

#### [Setting up a Maven project](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-a-maven-project?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-a-maven-project?u=76281980&t=0)** - [Instructor] All right, now that we have Maven and [[Java]] installed, let's create our project. I'm going to paste the command first and then I'm going to explain what it does. So I'm using the Maven command and the next part says archetype generate, which means we want to generate a new project based on an archetype, which is a template. And the template that we are providing is based on the Maven archetype quick start. So what this does is when we run this command, it sets up a basic project structure with a single Java file so that it's easier for us to get up and started running and focus on the code that we need to write. Now we need two more things, which is the group ID, which is generally the reverse domain name, in this case, I've used the initials from my name with com.h2. And this is the ID for the project, which is book-search, which is the project that we are working on. And we've also made the interactive mode false, which means we don't want this in an interactive mode. We'd rather have the project up and running so that by the end of the command, we have everything set up and it doesn't ask us a bunch of questions. All right, now that we have it up and running, let me just run this command. And in few moments, you will see a build success, which means your project is now ready. So I can go to book-search and here the command that I'm going to run to make sure everything works fine is mvn clean install.
>
> **[1:36](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-a-maven-project?u=76281980&t=96)** Now, mvn clean install is two parts: clean and install. Clean does the removal of all the artifacts if they have been created to make sure everything starts from scratch. And install is a lifecycle wherein Maven first compiles a code base and it builds it, and then runs the test, and finally creates the artifact, which is a JAR file in case of Java projects. I'll clear the screen and I hit Enter. And as we see, it ran the entire lifecycle. It ran one test and nothing failed, which means one test ran and everything was successful. And it also created the JAR file I was talking about on this location. So if I do ls of target directory, this is the JAR file that it has created. Now we can also run this project, but before that, let me show you what code it is going to execute. So if I do less src/main/java and then com/h2, which is the group ID, and within that there is a file called App.java, which is created by that archetype or the template by default. So if we are going to run this app class, it should print Hello World on the console. So let's run that and we'll do mvn exec and we are going to provide the argument on which class we want to run or which is the main class that should execute if we execute the JAR file. And as I hit Enter, it printed Hello World on the console. So to summarize what we did,
>
> **[3:09](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-a-maven-project?u=76281980&t=189)** we created a fresh project from scratch using the Maven archetype. We ran the clean install which made sure that there were no compilation issues that has ran successfully. Everything worked fine. And then finally, we ran the project main file to ensure that our main class is executed and printing Hello World on the console. All right, now that we have the project set up running, it makes sense to switch gears and move from terminal to an IDE where we are going to write code. So I'll see you next where we are going to review some code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Java]] (5)
> **Prerequisites:** install (5), set up (2)
> **CLI Commands:** mvn (3), make (2), ls (1)
> **Env Vars:** jar (4), ide (1)
> **Definitions:** is a  (4)
> **Exercise Files:** template (3)
> **File Paths:** app.java (1)
> **Tools:** terminal (1)

#### [Setting up Visual Studio Code](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-visual-studio-code?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-visual-studio-code?u=76281980&t=0)** - [Instructor] Alright, so in this project, I'm going to use Visual Studio Code, which is an IDE of my choice, and it is a very popular IDE available for free for virtually every developer on the planet. Now, we are going to also use [[GitHub Copilot]], which as I mentioned in my, one of the past videos that this is a paid subscription. So if you have it, that's fantastic. If not, I will show you how you can leverage GitHub Copilot once you have subscription and how it can make you more productive in your day-to-day work. Now, if you don't have VS Code already installed, you may want to click on this button, I'm on mac. It's saying macOSX, but it already has the installers for other operating systems too. Now, since I have already installed VS Code, I'm going to switch gears and go to the VS Code that I have opened already. Now, this is the file explorer. I have not opened anything yet, so I will go back here. And the important thing that I want to mention here is the concept of extensions. And the two extensions that I want you to install if you are using the GitHub Copilot, or you might already have them installed is, GitHub Copilot, I've already installed. So it's saying uninstall, but if you don't have it installed, there will be an install button here. So with this, it'll help you to write code in the IDE when you are opening up a file. And the other one is a GitHub Copilot chat. This is a chat window separate from your IDE file where you can chat with GitHub Copilot
>
> **[1:34](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-visual-studio-code?u=76281980&t=94)** and ask certain questions and it can help you write code and explain things in much better way. So now, since I have both of these extension installed, I will switch gears and show you how this looks like in the chat. So this is GitHub Copilot chat and you will see it here as part of the icon on the extension. And the first thing it asks you to do is to sign in. So I'm going to allow, and this takes me to a page wherein I need to verify my session. It comes to my [github.com](https://github.com) and I need to continue to allow the extension to use my credentials. And I'm back on GitHub Copilot chat and I see Ask [[Microsoft Copilot|Copilot]], which means I'm successfully authenticated. And now, I can use GitHub Copilot chat in my IDE. Now, you may see a very different window, but don't worry about it. GitHub Copilot is slowly releasing newer versions of GitHub Copilot chat. So the experience on how to interact with Copilot chat is upgrading almost every single day. So we are looking at new features coming up soon. So this is the experience that I have right now. Now, in order to open the project, I am going to go back to the terminal and I'm going to type in a command code, insiders. And this is because I'm using the insider build. If you're using VS Code, this will be code, or otherwise, you can also click open folder and open the code from there. And once I do that, the code opens up
>
> **[3:08](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-visual-studio-code?u=76281980&t=188)** as I move things around. This is the explorer window. I see book search here and I see three things, two directories and one file. I click on pom.xml. So this is the main file that holds the blueprint with all the dependencies that we are going to use in our project. Since we were using an archetype, the only dependency that we have is [[JUnit]], and we are going to leverage that when we are going to write the test. But other than that, there is nothing new here. Now, the target directory is created when we ran mvn clean install command and it updated everything inside it. And this is the book search jar file that we saw from the command line. The most interesting piece where we are going to spend most of our time is under SRC directory and there are two directories inside it: main and test. Test where all our tests will be located. And in main, you will see com and h2 as nested directory. This is the group ID, if you remember, when we were creating the maven create command for the project. That is being used to create this package. And outdoor [[Java]] is the main class that is executed when we ran mvn run exec command. So this is pretty much about it. We have seen our code base, we have extensions installed, so we are all set. We can start working on our code base starting in the next video. Alright, so this is all about looking
>
> **[4:43](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-visual-studio-code?u=76281980&t=283)** at the code in the editor. We are not running any code at the moment, but let's make a change. So if we say hello book search, we want to ensure that the changes that we are making will work fine. I'm going to open up the terminal and I'm going to run mvn clean install and everything worked fine. So we will run mvn exec command again. I'll clean the screen, run again. And now, it says hello book search. Alright, so what we have done so far is, we have installed multiple versions of Java, we are using Java version 24 here. We installed Maven, we created the project using Maven, we ran the entire lifecycle using command line. We looked at the code in IDE, we installed the extensions. The extensions are already installed, but I hope that you may have installed the extensions by now, if not already. And we have made changes to our code and ran it successfully from the command line. So we are almost done with all the infrastructure work that we need to do before we can start focusing on the project work. So I'll see you in the next video. There is one final piece that I want to run through before we start focusing on the project-specific parts. So I'll see you next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub Copilot]] (11), [[Java]] (3), [[Microsoft Copilot|Copilot]] (2), [[GitHub]] (1), [[JUnit]] (1)
> **Tools:** github (12), vs code (4), command line (3), terminal (2), visual studio (1)
> **Env Vars:** ide (6), src (1)
> **CLI Commands:** mvn (4), make (2)
> **UI Navigation:** click on (2), open the (2), go to (1)
> **Definitions:** is a  (4), is an  (1)
> **Prerequisites:** install (4), before we start (1)
> **Cross-References:** in the next (2), coming up (1)

#### [Introduction to the Spring Boot framework](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/introduction-to-spring-boot-framework?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/introduction-to-spring-boot-framework?u=76281980&t=0)** - [Instructor] In this course, we are going to leverage [[Spring Boot]]. If you look at their Featured section, it outlines why this is one of the most popular projects. First, it enables the creation of self-contained Spring applications. Then it directly embeds Tomcat, Jetty, or Undertow. That eliminates the need of deploying the WAR files in order to create servers. It also offers an opinionated starter dependencies to streamline the build configuration, and it automatically configures the Spring and third-party libraries whenever possible. Finally, it provides the production-grade features built into the framework such as metrics, health checks, and externalized configuration. And one of the most important thing is the community support. So Spring community is thriving, and if you get stuck in creating your code and understanding any parts of the code base that leverages Spring Boot, the community is always there to help you, which means you will never feel like you are the only one trying to juggle with this problem. Somebody in the community may have seen before and would be able to help you out. So I hope that this was useful. This is not the course on Spring Boot. We assume that you know Spring Boot before. If not, there are good courses available on [[LinkedIn]] Learning that you can take to get better at these Spring Boot skills. However, in this course, we will use Spring Boot, and whatever part we are going to touch as part of the framework, we will do our best to make sure that you understand the code that we are writing. All right, with that, I will see you in the next video
>
> **[1:35](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/introduction-to-spring-boot-framework?u=76281980&t=95)** where we are going to create our first Hello World API using Spring Boot.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Spring Boot]] (7), [[LinkedIn]] (1)
> **Env Vars:** war (1), api (1)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Creating a hello world API](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/creating-a-hello-world-api?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/creating-a-hello-world-api?u=76281980&t=0)** - [Instructor] As we mentioned that we are going to use [[Spring Boot]] for creating our API project, we need to install the Spring Boot dependencies, so that we can continue to make progress in the project. Now, I've opened up the pom.xml. This is where all the dependencies must go in. This is where I'm going to show you the first use case on where [[GitHub Copilot]] could be useful. So for that, I'm going to open up chat, and I'm going to pass in the context, my question, which is, "I'm creating API based project using Spring Boot.
>
> **[0:39](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/creating-a-hello-world-api?u=76281980&t=39)** "Help me add necessary dependencies "so that I can create APIs." And I as a hit enter, GitHub Copilot took our pom.xml as a reference, as an input, and then it created the three steps that we would need, and it created the updated pom.xml, but it did not apply the changes yet. It just showed us what it would look like. And on the right, we still have our pom.xml, and on the left is the suggestion or is the code that GitHub Copilot thinks that we would need in order to add Spring Boot dependency. Now, as I hover on this, you will see this popup coming up with a different set of buttons. The most important one, which is a new release that has happened is if I click here and click on the plus one, it says, "Apply in Editor." And as I click, there's a dialogue that appears that says it's applying the changes from the left, which was the solution, to the right, which is where our file is. And it doesn't directly apply, rather it creates a (indistinct) between the suggested and what exists, and it shows the potential changes that we may want to apply. Now it says that we need the Spring Boot parent, Spring Boot starter web, and Spring Boot starter test with the test dependency, and our original dependency on (indistinct) is still intact.
>
> **[2:13](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/creating-a-hello-world-api?u=76281980&t=133)** There are three things that we can do. We can see show changes, discard if we don't like it, or accept. And now in this case, I'm going to accept and see what happens. So our changes have been applied now, but do they work? So I'll hit save, command S, go to my terminal and run mvn clean install and see if this works. So, the build was successful, which means now our project has necessary Spring Boot dependencies and our existing code didn't break at all. So what we are going to do next is we should create a simple Hello World API that runs using Spring Boot and make sure that everything that we have done so far works correctly. So I'm going to go to src, main, com S2, do a right click and insert a new file called HelloController.[[Java]]. And we are going to create the [[Representational State Transfer (REST)|rest]] controller here. So for that, what I'm going to do is I'm going to annotate this saying RestController. As I type Rest, you can see the controller is in italics, and it's grayed out, which means that this is a suggestion that is coming from GitHub Copilot, not GitHub Copilot Chat, rather GitHub Copilot. If you remember that there were two extensions we installed, GitHub Copilot and GitHub Copilot Chat. This is GitHub Copilot. Now, if I want to accept this, all I need to do is hit tab, which is visible here. So I will hit tab, and two things happened.
>
> **[3:48](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/creating-a-hello-world-api?u=76281980&t=228)** The RestController got auto-populated, and there was a necessary dependency that added in the file. Now the second thing that we need to do is to create a endpoint that will be used when we run our application. So, I will first create a function, public string C. GitHub Copilot is now giving a suggestion, say hello as a function name, and hello Spring Boot should be returned as a string. If I want to accept, I will hit tab, and the code will be completed for me. So, as you can see, GitHub Copilot is smart, and it understands the context. And as a developer, when I'm writing code, it paired programs with me, but it thinks faster and ahead of what I'm trying to do and gives us the possible solutions that we can use. I use the first suggestion, but there are ways we can see other alternatives suggestion that we can use, and I will show you those as we make progress in this project. Now this is just a function but not the endpoints, which means we need to create the endpoint here. So I will say add GetMapping, so again, GitHub Copilot is helping us here. And I hit tab and it says, "This is the endpoint that you need." Great, so now we have one endpoint. So if the server is running, /hello is where we should go to make sure our project is working fine. But before this can be injected into our application, there are two remaining things that we need to do. So let me make this a bit closer. The first thing that we need to do is go to our app. Right now, it's running the system.out.println,
>
> **[5:24](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/creating-a-hello-world-api?u=76281980&t=324)** which is not what we need moving forward. We want to make it as a Spring Boot application. So I'm going to type SpringBoot, and again, GitHub Copilot was fast enough and it says, "You might be thinking about this." And I will say yes by heading tab, and the necessary import is already added to the file. So, since this is there, the last thing that we need on this site is we want to say SpringApplication. And again, GitHub Copilot says, "You want to run the App.class with the args." And I would say, "Yes, I really want to do this." So now that we have connected our main application with our Spring Boot application, let's go back and run the application again to make sure that everything works fine. So I will do a WinClean install here, and everything works fine. Now in this case, now we should start our server. So we are going to use a different command, which is ambient spring-boot:run command, and as I hit enter, we will see that necessary dependencies have been downloaded and our application should be up and running in a few moment. So, as you can see, Spring is here, so Spring Boot Controller is injected at the right place, and it says our application is running on port 8080. So in order to test that, we are going to go to our browser and change this to local host 8080. And if you remember, the endpoint was hello,
>
> **[6:58](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/creating-a-hello-world-api?u=76281980&t=418)** so I will go to the hello endpoint as I hit enter. Here we go. We get the, Hello, Spring Boot! So what we've just done is we've created the Spring Boot application. We have leveraged GitHub Copilot to help us add the dependency, help us create the controller, inject the Spring Boot application at the entry point, and we were able to run the application. Now, in order to make any change, I can go back and say, "Hello, Spring Boot application," and double exclamation. But this is not going to change anything if I still go ahead and refresh. This is still the same output as before. It's because our application needs to be recompiled and packaged before we can run it. So what I'm going to do moving forward is I will say mvn string-boot run, but before that I will also run mvn clean install and run both the commands together. So, the first command will do its job by making sure everything works fine, creating the jar file, and the second one will start the spring application based on the latest jar file. So now that the application is running, we can go back and if I now hit refresh, we get Hello, Spring Boot application!! Fantastic, so with that, we have taken care of most of the infrastructure pieces by creating the project from scratch, having the necessary dependencies. We would need more dependencies as we start interacting with the database. But the baseline is set. We already have the application code. We have the Spring Boot application, and we've created our Hello World API,
>
> **[8:31](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/creating-a-hello-world-api?u=76281980&t=511)** and all of this is working perfectly fine. Fantastic, so I will see you in the next module, and we'll shift gears, and we will focus on the database so that we can populate the books before we can start searching on them. All right, I hope that this was useful, and I'll see you next time.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Spring Boot]] (19), [[GitHub Copilot]] (15), [[Representational State Transfer (REST)|Rest]] (2), [[Java]] (1)
> **Tools:** github (15), terminal (1)
> **CLI Commands:** make (8), mvn (3)
> **UI Navigation:** go to (5), click on (1)
> **File Paths:** pom.xml (4), hellocontroller.java (1)
> **Env Vars:** api (4)
> **Prerequisites:** install (4)
> **Cross-References:** we mentioned (1), coming up (1), in the next (1)


### 2. Dockerizing the Project with PostgreSQL

[↑ Back to Table of Contents](#table-of-contents)

#### [Setting up Docker](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-docker?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-docker?u=76281980&t=0)** - [Instructor] All right. So now let's go back to our project overview. We want to build a book search API, and when we created this entire diagram, we realized that everything starts from the database. Because unless and until we have a database that contains set of books, there is no point in writing the APIs or business logic because there is no data that supports this business logic or the APIs. So, in this module, we are going to build the database. And in order to do that, we are going to leverage Docker. Now, if I go to the website for Docker, Docker is the platform for developing, shipping, and running applications in containers. And these containers are nothing but lightweight, standalone, executable package, which can package any piece of software inside it. And the most important benefit that we need from Docker in our course is the concept of portability, which means once we have a container running on my machine, when I show you, you will be able to replicate the exact same thing on your own machine. And best of all, if you are happy with what you see, you can even take the same container in the product and deployment. So if I click on the Discover Desktop, we'll go to the overview of Docker Desktop, which is what we are going to install in just a moment. And it contains a lot of different services. And the one that we are going to use is Docker [[Jetpack Compose|Compose]]. Even though Docker Compose says that it's for defining and running multiple container applications, we are only going to start a single container
>
> **[1:35](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/setting-up-docker?u=76281980&t=95)** that contains our [[PostgreSQL|Postgres]] database. Now, in order to install the Docker technology, you go back to the homepage, to [docker.com](https://docker.com), and over here, you will see this button that says Download Docker Desktop. Now, the installers are available for almost every operating system. So go ahead and click on the button that is most relevant for your operating system and continue with the installation process. Now, I already have Docker installed locally, but once you are finished with your setup and start the Docker Desktop, the way to identify whether or not it's working is by going on the terminal and type docker -V, and you will see the Docker version up and running. So if you have reached this far, fantastic because now, as per this diagram, you already have the technology up and running on a machine, that is Docker. And in the next step, we are going to set up the database container inside Docker and connect with this database.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Jetpack Compose|Compose]] (2), [[PostgreSQL|Postgres]] (1)
> **CLI Commands:** docker (16)
> **Tools:** docker desktop (3), terminal (1)
> **UI Navigation:** go to (2), click on (2)
> **Prerequisites:** install (2), setup (1), set up (1)
> **Cross-References:** go back to (1), in the next (1)
> **URLs:** [docker.com](https://docker.com) (1)
> **Env Vars:** api (1)

#### [Configuring PostgreSQL in Docker](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/configuring-postgresql-in-docker?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/configuring-postgresql-in-docker?u=76281980&t=0)** - [Instructor] All right, so now that the Docker is up and running on our machine, the next thing we need to do is to set up a [[PostgreSQL|Postgres]] database inside the Docker container. Now in order to do that, we are going to leverage [[GitHub Copilot]]. Let's assume that we don't know anything about Docker. All we need to do is to run a Postgres [[SQL]] container inside the Docker. So we are going to ask GitHub Copilot to help us with this task. So I'm going to say, create a Docker [[Jetpack Compose|compose]] yml file. So docker-compose.yml is a file that is a blueprint of what the infrastructure would look like. We provide that file as an input to Docker and Docker does the [[Representational State Transfer (REST)|rest]] of the things. So we are going to create that blueprint by asking GitHub Copilot to create the docker-compose.yml file. So we are saying, create the docker-compose.yml file that hosts a Postgres SQL database, latest. So we are going to use the latest tag, which means the latest version of Postgres database. The container name should be library-db. The database name should be library. The user is admin and password is admin123. Now this is only for local purposes. Please do not use this password production. It's not very secure,
>
> **[1:33](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/configuring-postgresql-in-docker?u=76281980&t=93)** but we are just trying to set up everything locally. This can be externalized into a configuration file later, but that is out of the scope. So I'm just going to focus on getting the container up and running. So we asked our GitHub Copilot friend to create the yml file with what database we need, what the container name should be, what the database name and user and password should be. We also need to expose the port so that we can access the database from our machine. So we are going to say we also need to expose the db to local port 5432. This is usually the port where Postgres servers expose their database. So with this, let's send this to GitHub Copilot and see what output we get. Interesting. So within few moments, as we can see, we have this snippet of code. It's using version 3.8 of Docker desktop. And then it's taking the image which is Postgres latest, which means that it adheres to our request. It added the right container name, the database name, the username and password, and it exposed the container port 5432 with our local port 5432. Fantastic. But we don't know whether or not it's going to run, and also that this is only a snippet of code. This is not added to our code base yet. So for that, what we are going to do as we hover, click on these three dots and say insert into a new file and it comes here.
>
> **[3:07](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/configuring-postgresql-in-docker?u=76281980&t=187)** But as you can see on the top, the file name is nothing. It's not saved yet. So as I hit S, command S, this is where we need to save our Docker compose yml at the root of the project. So this would be Docker compose, docker-compose.yml file. So as I saved it, identified that this is docker-compose.yml, let's look at the code structure. So it's sitting at the root of the project and everything else looks good so far. So the time has come for us to test it. So let's open up the terminal and we are going to run the command called docker-compose. Up is a command to create the containers and down is a command to remove or stop the containers. So we are going to use Docker compose up and minus D, which means run in a detached mode, which means in a background mode. So let's run and see what do we get. Okay, so we got the message that the volume was created. The volume is where the data is hosted inside the container and it said that the container library DB is running. So let me just hide a few things so that we can focus on terminal at the moment. Okay, so now that we have this message, how do we know it's running? For that, we are going to run docker-ps command. And as we can see, library-db is actually running
>
> **[4:43](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/configuring-postgresql-in-docker?u=76281980&t=283)** with the image for Postgres latest and it has exposed the port 5432 from container on our machine on our local host at 5432. It's been running, when was it created, what its status is, what the container ID is. Great. So now that we know it's up and running, is there a way I can connect it from my own machine and see if this is working? Turns out that there is a way, by running Docker exec command. So I will type Docker exec minus I for interactive, T's to attach the terminal. So Docker exec minus IT. And then what's the name of the container? So it's library-db. And then once it connects to the db, what command do we want to run? So since it's a Postgres database, we want to run P is equal command, which is to execute the terminal with the user as admin and with the database call library, which is what we created here. So let's see if this works. Fantastic. So now as you can see, this database is not on our machine but inside a container. And the container is hosted by Docker. So from our own machine, we were able to get into Docker container and were able to execute Postgres command. And this is telling us that we are inside the database at the moment. How do we know what database exists? So \L and hit enter. Didn't work. So \L is how we run it.
>
> **[6:20](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/configuring-postgresql-in-docker?u=76281980&t=380)** Semicolon is not the right thing to add. So it lists the database that we created. So as we can see, we already have the database created for us. And inside the Docker container, we were able to connect to it, using Docker. Think about how much time we saved rather than fiddling around with our own machine configuration in order to install the Postgres database. Now we also need to find out what version of Postgres we are running. So we'll say select version. And I think here, I need to do this. So select version with the semicolon and we know that we are running Postgres 16.4. So fantastic. We have reached to the next step of our project, which means we leveraged GitHub Copilot. Let me just open that up. We leveraged GitHub Copilot to create the Docker compose AML file for us. And then using this file, we were able to set up the infrastructure. We were also able to execute or get into the database terminal and we were able to execute the command successfully. To exit, I believe we do /q and we are back in our own machines because this is the project route directory. All right, so now that we have all these things set up, let's go to our next step.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PostgreSQL|Postgres]] (12), [[Jetpack Compose|Compose]] (11), [[GitHub Copilot]] (7), [[SQL]] (2), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** docker (26), find (1)
> **Tools:** github (7), terminal (5), docker desktop (1)
> **Definitions:** is a  (5), means that (1)
> **File Paths:** docker-compose.yml (5)
> **Prerequisites:** set up (4), install (1)
> **Ports:** port 5432 (4)
> **Env Vars:** sql (2), aml (1)


### 3. Designing the Database Schema and Implementing Full-Text Search

[↑ Back to Table of Contents](#table-of-contents)

#### [Designing the book schema](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/designing-the-book-schema?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/designing-the-book-schema?u=76281980&t=0)** - [Instructor] Alright, welcome back. So far, we have focused on creating a database inside a Docker container. Now it's a good time for us to move on to the next step where we are going to insert books information in this database. Now all the books information are available on a CSU file sitting in a remote [[GitHub]] gist. And I'm going to take a small sample of that. There are data for almost 20,000 books, along with their author information. I'm copying the first four rows, and going to put that in a spreadsheet so that we can look easily. So we have lot of dimensions here, starting with book ID, title, author, rating, description, and so on and so forth. And I've copied four books' information. And what we need to do is not take this entire CSV and dump in a single database table, because that is not a good design. At the very minimum in the context of this course, we want to split the author's table separate from the books table, because one book may have more than one author as a collaborator, and one author can have more than one books. So we want to map that many to many relationship. If you look at it visually, how this would look like. So we will have a books table. We will have a books table in the database, then we will also have the authors' table in the database. And then we will have a new table called Books, Authors. And the ID from the books will map into book ID in book authors, and the author ID from the author table
>
> **[1:34](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/designing-the-book-schema?u=76281980&t=94)** will map as authorized in the books author table. This way we can map the CSV data into three database tables. And this will help us create queries that are useful in the context of the business logic, which we will cover in the future module. So I hope that this was useful. Now that we have seen what data looks like and how we want to shape it, let's move on to the next step and create the schema in [[PostgreSQL|Postgres]] database.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (1), [[PostgreSQL|Postgres]] (1)
> **Env Vars:** csv (2), csu (1)
> **CLI Commands:** docker (1)
> **Tools:** github (1)
> **Speakers:** - [instructor] (1)

#### [Create a database schema in PostgreSQL](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/create-database-schema-in-postgresql?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/create-database-schema-in-postgresql?u=76281980&t=0)** - [Instructor] All right, so now I'm back in my VS Code, and we have seen all this code before. Now, in order to create a schema, I'm going to first create a file inside a folder called DB, and call it create_schema.[[SQL]]. Now, in order to create this schema, we need to know what columns it would contain. So we are going to go back to the original CSV that we had, and I'm going to copy all the names for the columns. I will come back to my SQL file, and I will write a comment that we are using, we are using [[PostgreSQL]] 16.4, which is the latest one, and CSV Header is below. And the reason I'm doing that is because I want the help from [[GitHub Copilot]] to help me design the schema. So in the next step, I'm going to write a comment which it already understands, but I'm going to put the comment for clarity, Create the books table, and then I'm going to say Create the authors table, and then I'm going to say Create the book_authors table. See, all of this gray out content is the context taken by GitHub Copilot as I'm typing, and giving me the suggestion, which is exactly the suggestion that I'm needing right now. So as I mentioned in the video before, is that we are going to create these three tables,
>
> **[1:33](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/create-database-schema-in-postgresql?u=76281980&t=93)** and I have copied the CSV Header from the file that I will show you once again from here. It's a CSV file setting in [[GitHub]] Gist, and I pasted it here so that I can get help from GitHub Copilot in designing this schema as well. And we have already mentioned that we are going to leverage PostgreSQL syntax. So now as a type CREATE TABLE, in a moment, I should see, or I haven't seen anything yet, so I'm going to say books, and all of a sudden see this greater content has come up, which is taking all the information available in the CSV Header below and assigned a relevant data type for each and every column. So I will accept it for now, and I don't exactly need all of this information. For example, since we said that author information is going to be in a different table altogether, so I'm going to delete it, and I also don't like the way it has taken the casing. I prefer the column names to have underscore between them, all lowercase. I'm going to say that this is, I will not change it here, but I will change it here. This is a book_id. I am going to keep it the way it was before, and I'm going to change this to book_format. and publish_date, and the first_publish_date, and liked_percent, and this way, everything looks good to me.
>
> **[3:07](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/create-database-schema-in-postgresql?u=76281980&t=187)** Now we are going to create the authors table. So as I hit enter, GitHub Copilot gave me a suggestion that you want to create the authors table, and it took the author_id, so it understood that I'm no longer interested in the camel casing, so it's using underscore in space, so very smart based on the input that I just gave above, and it is taking the author_id and the author_name as the two important things that we would want to keep. I will take it here. I'm only changing the author_name to be name, because we are setting in the context of authors_table, so everything looks good here. And finally, we need to create the table, so I hit Enter, and again a blob of content as a suggestion came in, and now it's saying that you're creating books_authors, we have book_id and author_id. They both must not be null. And the PRIMARY KEY is a candidate key between the book_id and the author_id, and the FOREIGN keys all look good, so I'm going to hit Tab, and that's it. So as you can see, it was pretty easy taking help from GitHub Copilot to design this schema. In this case, I've worked with [[PostgreSQL|Postgres]], but I may not know all the data types, but having the helping hand from GitHub Copilot to do the programming with me is extremely useful. As you can see, in just a few minutes, we were able to create the schema. So now that we have created our schema, it's time for us to create the tables in the database. But before we do that, there is one more thing that we need do to ensure that as soon as we start our database container, these tables are created, and for that, I need to go to docker-[[Jetpack Compose|compose]].yaml file.
>
> **[4:42](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/create-database-schema-in-postgresql?u=76281980&t=282)** And in under volumes, I will add a new part, and I will say from db, take the create_schema.sql, and as you can see, GitHub Copilot is giving me a hint that you want to put this in the docker-entrypoint under the create_schema.sql. I will say in instead of doing as a create_schema, I will type it as init.sql. So what this line does is when the container starts for Postgres, it looks for the initialization scripts that it can execute when the container starts. And all we are saying here is when the container start, take the create_schema.sql which we created right here, and put that under the docker-entrypoint-init.sql. With that, when we start our container, we will see these database tables getting executed and created. Now the moment of truth, we have to go ahead and run it, so I will make this screen a little bigger. I am going to see what's running right now, so I will say docker ps, and only library-db is running, so first thing I need to do is to remove that, so I'm going to say docker-compose down -v, and it is going to remove everything from the docker process, so if I do docker ps, nothing is here. So now when we are going to run the docker-compose up to bring up the containers -d, which means in detached mode, let's see what happens. So it says that it has created the volume, it has created the network,
>
> **[6:17](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/create-database-schema-in-postgresql?u=76281980&t=377)** and it has created the library-db as well. So we need to ensure that this is indeed the case, so I'm going to run the docker exec, and I'm inside the Docker container inside the database because I'm looking at the psql, and first thing, I will list all the [[Databases]]. So library is there. You can also make sure that that is indeed your current selected database, but if you see library over here, and then I'm going to slash d, and as a hit Enter, you can see that it has created authors table, books table, and books_authors table, which is exactly what we did in the context of our schema design. What we can also do is we can say slash d authors, and we can see that author_id and author_name is currently mapping to the schema that we mentioned. If I do slash d for books, you will see exactly the same amount of information, which is mapping with our schema, which is a blueprint. Fantastic, so what we've done just now is we took out the CSV Header, and then we took help from GitHub Copilot in designing the schema. We are creating three different tables, book, authors, and books_authors, and then we updated the docker-compose.yaml file to take the SQL and put that as a part of initialization script. We removed our existing Docker containers, and when we restarted the initialization script, executed and created all these tables. Great, so at this point in time,
>
> **[7:50](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/create-database-schema-in-postgresql?u=76281980&t=470)** we have our database inside a Docker container, and within the database, we have three tables. Now, these three tables are still empty, so in the next step, we are going to insert some data and play around with the query, and I'll see you there to learn more about this full text search.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SQL]] (8), [[GitHub Copilot]] (8), [[Jetpack Compose|Compose]] (4), [[PostgreSQL]] (2), [[PostgreSQL|Postgres]] (2)
> **Code Identifiers:** create_schema (5), author_id (5), book_id (3), author_name (3), books_authors (3)
> **CLI Commands:** docker (13), make (2), psql (1)
> **Env Vars:** csv (6), sql (2), create (1), table (1), primary (1)
> **Tools:** github (9), vs code (1)
> **File Paths:** create_schema.sql (4), docker-compose.yaml (2), init.sql (1), docker-entrypoint-init.sql (1)
> **Cross-References:** in the next (2), go back to (1), we mentioned (1)
> **Definitions:** is a  (3)

#### [Navigating the database using a web browser](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/navigating-database-using-web-browser?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/navigating-database-using-web-browser?u=76281980&t=0)** - [Instructor] Now that we have seen we have created all the tables necessary, it's time for us to insert the data in the database. However, it'll get cumbersome to work with the command line when we are working with a lot of data and running a search query for example. So in this case, we are going to change our direction a little bit and work with an admin tool, which makes it easier to work with the database. For that, we are going to install another piece of software, which is called pgAdmin. PgAdmin is one of the most popular open source software to work with the [[PostgreSQL|Postgres]] database. Now when you go to the download section, they have a variety of ways where we can download pgAdmin, and we are going to look at the container mode. It's because we are always trying to be transparent in the way we bring up our infrastructure. And if I click on the documentation and then go to the examples, this is how you can actually start pgAdmin inside your Docker container. Now, one thing that when you start a pgAdmin, it would ask you to connect with an existing database, which is our library database. But since our library database is inside a Docker container, how are they going to connect to each other? Visually, what this means is, we are already running our database inside a Docker container. And if we also start our pgAdmin inside a Docker container, let's say this is our pgAdmin,
>
> **[1:35](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/navigating-database-using-web-browser?u=76281980&t=95)** how will this pgAdmin connect to the database inside a Docker container? So for that, what we need to do is to bring pgAdmin and the database on the same network. If they're on the same network, they can see each other and they can talk to each other. What this means is that we are going to create a link between our pgAdmin container with our database container. Now I showed this to you visually, let's go back to the command line and run this in order to make sure that we are able to manage the database from admin. I'm going to come back to my IDE and quit this setup right now. And if I do a docker PS, there is only library-db, so what I need to do is to docker-[[Jetpack Compose|compose]] down -v and there is no container running at the moment. And what we need to do is to, I will start the container once again by saying docker-compose up -d. And as I hit enter, I like your attention to be paid here. So when a Docker compose started the container, it automatically creates a network called as book-search_default. Now since our network already knows about this database, what we need to do is when we start our pgAdmin, is to connect to the same network. So first thing that we need to do is to pull the image for pgAdmin, and I'm pasting the command here. It's a docker-pull dpage-pgadmin4.
>
> **[3:11](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/navigating-database-using-web-browser?u=76281980&t=191)** As I hit enter it is using the latest tag and I have the image downloaded locally. And the next thing is we need to start the container. So I'm going to paste the command and explain that in a minute. So what we are doing is we are running the container on port 80 with all the usual defaults that we saw on this page about the username and the password for the pgAdmin to access. So when you go to the pgAdmin that I will show you in a moment, you need to log in with the user ID and password. So this is the user ID and the password. We use the same pgAdmin username and the default password here. And I gave a name to the container as well, so that it's easier for us to look at when we do docker PS, and see here. So we are saying that this container should be available or attached to the network book-search_default, which is this one. And with that, as a hit enter, the container started. So if I do a docker ps, I should see two containers. Let me just slide this down so it's easier to look at. So one is the library-db, which contains our database and the tables, which are empty at the moment, and the admin, which you're going to run. And this admin is going to control or manage the database that we created. Now if we look here, it says the pgAdmin is available locally on port 80.
>
> **[4:46](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/navigating-database-using-web-browser?u=76281980&t=286)** So which means we should go to our browser, and if I do a local host for 80, I see a login screen. And this is the login screen for pgAdmin. Now remember, it has a default email and a password. So this is what we need. So I'm going to copy the username, paste it here, and then copy the password and paste it here. And as we hit login, we have the pgAdmin access at the moment. Now this is just the admin at the moment. It doesn't know which database to connect to. So for that, if you look into the servers, it's currently empty. We are going to register a new server. Let's give the name and call it admin. And in the connection it needs the host name, host name for where your database is installed. Now, if you remember correctly in the diagram, this host is inside the docker container, and the name of this host is library-db. How do I know that is when I did the docker PS command, this is where our database is. So library-db is what we need when connecting with the database here. So I put the library-db, the user is admin and the password is admin123. Let me type it again, admin123. How do I know that? It's because when we created our container, we gave it a user ID and the password. Now with this, if I hit save, bam. So now we are inside our database
>
> **[6:20](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/navigating-database-using-web-browser?u=76281980&t=380)** so we can see the library database. If I go back to the Docker compose file, the name of the Postgres database was library. So we are able to find it here. And if I go to Schemas, and then if I go to tables, we see three tables, authors, books, and book authors. Fantastic, so what we just did is we created an alternate way to connect to our database running inside the Docker container, so that we no longer have to run longer queries on command line. Now in the next video, we are going to insert some data and start writing some queries. So I will see you there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Jetpack Compose|Compose]] (4), [[PostgreSQL|Postgres]] (2)
> **Code Identifiers:** pgadmin (17), search_default (2)
> **CLI Commands:** docker (16), make (1), find (1)
> **UI Navigation:** go to (6), click on (1)
> **Cross-References:** go back to (2), in the next (1)
> **Tools:** command line (3)
> **Ports:** port 80 (2)
> **Prerequisites:** install (1), setup (1)

#### [Introduction to full-text search](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/introduction-to-full-text-search?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/introduction-to-full-text-search?u=76281980&t=0)** - [Instructor] So, now we are looking at the three tables, authors, books and books authors in the context of pgAdmin that is running in the browser on local host port 80, if I just press it here again. So it says you're going to do this, and I'm going to come back to the database under library schemas, and these three tables are here. So first thing, let's take a look if we have any data available. When I run this, I don't see anything. The tables are empty. So the first thing we need to do is we need to insert the data. And for that, I'm going to make use of this window, which is the query, and I'm going to say, "Yes, I'm manually editing it," so I'm going to delete this. And I'm going to paste insert statements. I prepared this early on. So, we are inserting into a book's table, and we are inserting all these dimensions, and we are creating one, two, three, four different rows. So we are inserting four books into the books table right now. To execute, I'm going to hit this button, and it says you inserted four rows. In order to look at it again, let's repeat the process, all rows, and now we have four books' information available and we have all the data populated. We have the ID, title, rating, description, and so on and so forth. Now, let's see if we have to query for a book
>
> **[1:34](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/introduction-to-full-text-search?u=76281980&t=94)** and what that query would look like. So, there is already setting star from public books, and then we will say where, and let's run from the title. Title like, and then we are going to say, "We are searching for books on [[Algorithms]]." And as a I hit run, I didn't find anything here. Why so? If you remember correctly, when we inserted the data, if I go look at the data once again, we had books on algorithms, so why it didn't find? So if we go back, we see that our algorithms is lowercase, but the one available in the database table is capital A. So if I change this to here and run it again, it'll find the book. The other way could be is that we make use of lower function available in the database and then change it to lowercase and then run it again, and then we find it again. So, the idea is that the like queries that we run has to be paired with lower in order to find, or in order to do the case insensitive searches. Now this can be quite tricky when you are working with the production systems, because you don't know what your user is going to enter as a query. It's really unlikely that you can enforce the casing
>
> **[3:08](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/introduction-to-full-text-search?u=76281980&t=188)** and the types and what words they're going to insert. We have searched for algorithms. They may type algorithm or algorithmic. And in order to have a really good search system, queries like this should always work fine. Now this is one area where the like queries don't work as much as we want, and we need to change our attention into a full text search engine. And thankfully, [[PostgreSQL|Postgres]] supports the full text search. And let's quickly take a look on how full text search index works behind the scene. For that, I've created a spreadsheet, and I have entered name of 10 books over here, and I've also created the index here. So before I do a side-by-side comparison, let me just duplicate this tab, open it, and then now I think it'll be easier for me to explain. So on the left hand side, we have the book names. So when we search using where name like something, it searches on the raw titles, which means it is doing a table scan, and it is doing its best to find or match the row that matches the user query. In the cases of the full text search index, an index is a separate entity that is created on the data. And what a search index does, it breaks the words available in the title or on the dimension that you are interested in. And what it does, it creates the term, which means what [[Microsoft Word|word]] it found and where it found that. So for example, let's take the word called concepts.
>
> **[4:45](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/introduction-to-full-text-search?u=76281980&t=285)** So it's saying it found the word concept in book ID five. So if you go and look at the book ID five, the concepts word exist. Now, if there are more than one book that matches that word, for example, systems, it says systems is available in book ID two and five, so we can go and look at book ID two has systems, and book ID five has systems. So in the case of full text search, when the user query arrives, a full text search engine performs a lot of different features, such as normalization, stemming, and based on that, it is able to come to the point where it knows what term it needs to look up into this index. And as you can imagine, scanning is a different ball game. It takes more time and resources versus doing a lookup where you know what word it is and finding which book ID is, it's much faster and scalable. So behind the scene, inverted search index is one of the things that search index provides, and there are a lot more features that are useful when working with the full text search engines. Now as I mentioned, Postgres provides a set of functions that you can use to do the full text search index. So in the next video we are going to create our own search index in Postgres and run the queries. So I'll see you next, and we will do it together.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (6), [[Algorithms]] (4), [[PostgreSQL|Postgres]] (3)
> **CLI Commands:** find (6), make (2)
> **Analogies:** for example (2), such as (1), imagine (1)
> **Definitions:** is a  (2)
> **Code Identifiers:** pgadmin (1)
> **Ports:** port 80 (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Implementing full-text search in PostgreSQL](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-full-text-search-in-postgresql?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-full-text-search-in-postgresql?u=76281980&t=0)** - [Instructor] All right, so as we can see we have four books, and now we need to create a full text search index. And in order to do that, the first thing that we need to do is to create a vector, and we will do that on the books table. So, I'm going to paste the command. So we are altering the books table, and we are adding a new column called search_vector of type tsvector, and this is where a similar inverted index that we saw in the spreadsheet, that was an example, but this one is where the index will be stored. So, first thing is we need to create this column. So I'm going to run this, and it says, "Query return successfully." So I'm going to see what we have now, so running everything. So if I go all the way at the end, we have search vector, which is of type tsvector, but all of the data is null. So the next thing we need to do is to populate this search index. So, we are going to run the update script, and let me just paste this query first. So, we are updating the books table, and we are setting the information in the tsvector. So, we are converting it using the English language, and we are using three columns to create the search index, which is title, description, and ISBN. And A, B, C refers to the priority, so it means that when the search happens, if the search matches first in the title, it'll get the higher preference over the description
>
> **[1:36](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-full-text-search-in-postgresql?u=76281980&t=96)** and over ISPN and so on and so forth. And we are doing a to_tsvector, which means we are taking the exact content that is available in these columns and performing the rules, such as normalization and stemming so that they are stored in the index in a format which is easier to look up. Now I'm going to run this query and see what happens. It gave me an error because I selected only tsvector, so I'm going to select this entire piece of code and then run it again, and it updated the query. So it says it updated four rows. So we'll go back to the first select statement, run it, and then go all the way to the right. And now, as you can see, it created this search index and populated this index for us. It looks similar, but it's not exactly similar, it is much more involved with regard on how this activity works. I gave you a very simple example on (indistinct) search index, but this code that is implemented by [[PostgreSQL|Postgres]] is much more involved and advanced. So now that we have our search vector updated, we need to start making our search queries. So for that, what I'm going to do is take a query that I already created about a select statement, paste it so that we can talk through it. So, we are selecting title, and we are also using ts_rank, which is going to provide us the relevance or the ranking score on what the search score was.
>
> **[3:12](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-full-text-search-in-postgresql?u=76281980&t=192)** And the way it works is we tell it which column contains our vector and then we are taking our query, which is [[Algorithms]] in English language, and we are converting that into a query object. Now, you see this to_tsquery appeared twice. It's because any query that the user is going to provide you, which is algorithms in this case, needs to pass in through to_tsquery in order to make it a queryable object so that when it is searched against the search vector, they both are doing apples-to-apples comparison. Now, we are using it two times, first time is to help create the relevance score, and the second time is to actually make the filter query. So we are first doing the select, what all we need. We need title and we need the rank. We are searching in the books table and in the WHERE clause we are saying WHERE search_vector, which is the column which we populated a few minutes ago. And we are using the user query and converting that into a to_tsquery. So we are saying select all these things from the table where the search vector is a queryable object based on the user query called algorithms. And we are ordering by the rank, which is the rank, a ts_rank, and let's go ahead and run this now. So when we run, we found only one book with a score. And this is one way where when the user provides algorithms and it matches anywhere in the database, it is being looked up into the search index and getting returned. Now, this same query can get much more advanced. So for example, if somebody is searching for anything
>
> **[4:49](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-full-text-search-in-postgresql?u=76281980&t=289)** that has algorithms or designs, so in this case I will say algorithms or design, the pipe is or, and as we have used to_tsquery in two places, I need to make this change at two places. And if I now go ahead select this and run, we found two rows matching the criteria. If I move this a little bit here, the first one is matching because of the design keyword, and the second one is matching because of the algorithm's keyword with a different score altogether. So this is where the user is giving you A or B kind of queries. The same query can be updated using the pipe operator. If you want to model A and B, you use the and symbol, and I'm going to change it here again. And if I select and run it again, nothing matched because there is no such book that contains the [[Microsoft Word|word]] algorithms and design. But if you go back and change it to or condition, we found two books as we saw a few moments ago. So let me just run this. We saw two books. Now the point is that how this to_tsquery works, so in order to find how this works, I'm going to copy this and say select and paste it here. So we are saying give me the output for calling this function to_tsquery on this query, which is algorithms or design. So I'm going to select and run it. So, behind the scenes, see what it did? From algorithms, it went to algorithm, which means it performed these stemming and from design, it kept design.
>
> **[6:21](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-full-text-search-in-postgresql?u=76281980&t=381)** So if I change it to designers and algorithms to algorithmic, algorithmic, and then run it again, let's see if anything changes. So it changed it again. I think I had a spelling mistake. It was algorithmic. Let me just run it again. So changed it back to algorithm and design. So, the algorithm, the search index creation is smart enough to know how to normalize and how to stem the words so that it's easier to match based on the user query, because the user query is also going through the same function for normalization and stemming. And the matching is easier, because it does a lookup, finds which book ID it is, and returns the result. Now, if I changed it to capital Algorithmic and D capital and then run it again, it again changed it to the same word because it normalized from capital A to small a and small d from capital D. Great, so I hope that this gave you some sense of how full text search in Postgres works. Now this is a very small subset that I have covered, but if you're interested in finding more or you have a use case which is similar or require more changes, I would recommend looking at the Postgres documentation, chapter 12 on full text search, and you will find all the different sort of things, including the examples. All right, fantastic. So to recap what we have done so far,
>
> **[7:54](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-full-text-search-in-postgresql?u=76281980&t=474)** so we have a docker container, we have a database, we are leveraging pgAdmin in Docker to manage the database. We have successfully inserted four books, and then now we have created a search vector, which is the search index on the books, and we saw how the full text search queries work in the context of Postgres database. Great, now we are only working in the context of four books. As I mentioned that, we have 20,000 books that we have to work on. So, in the next step what we are going to do is we are going to write code that can insert the books information sitting remote somewhere on [[GitHub]] servers into our database that is sitting in our Docker containers before we move on to business logic and APIs. All right, fantastic, and I will see you in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Algorithms]] (10), [[PostgreSQL|Postgres]] (4), [[Microsoft Word|Word]] (2), [[GitHub]] (1)
> **Code Identifiers:** to_tsquery (6), search_vector (2), ts_rank (2), to_tsvector (1), pgadmin (1)
> **CLI Commands:** make (3), docker (3), find (2)
> **Env Vars:** where (2), isbn (1), ispn (1)
> **Cross-References:** in the next (2), go back to (1), as we saw (1)
> **Definitions:** is a  (2), refers to (1), means that (1)
> **SQL:** where (2)
> **Analogies:** such as (1), for example (1)


### 4. Ingesting and Validating Data

[↑ Back to Table of Contents](#table-of-contents)

#### [Overview of data ingestion](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/overview-of-data-ingestion?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/overview-of-data-ingestion?u=76281980&t=0)** - [Instructor] All right. So, now, in this set of videos, we are going to focus on inserting the data from CSV file that is setting on a [[GitHub]] gist, and then inserting that into our database that is running inside Dockerfile. Now, as we have seen, we already have the CSV file, which is books.csv, and it contains 20,000 books along with their author information, and a lot more things. And what we need to do, as we saw in the previous videos, we designed the schema that taking that CSV flat file, we want to insert them into three different tables, books, authors, and a mapping between books and their authors. So in order to achieve that, what we are going to do is focusing on this line that goes from Remote to Docker, we are going to divide this into three steps. The first step is download, the second step is parse CSV, and the third step is insert in DB. So in the next video, we are going to look at all the three steps that will help us to take the data from a remote file directly into our database in those three different tables as we have talked about.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (1)
> **Env Vars:** csv (4)
> **Cross-References:** as we saw (1), in the next (1)
> **File Paths:** books.csv (1)
> **CLI Commands:** docker (1)
> **Tools:** github (1)
> **Speakers:** - [instructor] (1)

#### [Automating data ingestion](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/automating-data-ingestion?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/automating-data-ingestion?u=76281980&t=0)** - [Instructor] I'm back in VS Code now, and before we look at how this code will run, we need two dependencies. The first one is to connect with the [[PostgreSQL|Postgres]] database, and the second dependency we need is to parse the CSV file. So I will head over to pom.xml where all our dependencies are already available. And now I'm going to ask [[GitHub Copilot]] to help us insert those dependencies. Now one way to do that is I do a right click here, and I say [[Microsoft Copilot|Copilot]], and Editor Inline Chat, and there is a Command + I on Mac as a shortcut. So I'll click here, and this window pops up. And I'm going to write my prompt here. I'm going to say add the dependency of Postgres and OpenCSV.
>
> **[0:55](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/automating-data-ingestion?u=76281980&t=55)** OpenCSV we will use to parse the CSV file into the [[Java]] objects, and Postgres is, once we have those objects and we want to connect to Postgres and insert data, that's when we would need it. I will hit Enter or I can also click here. So Copilot is generating, and as you can see it gave us this suggestion. We can either accept or discard, but let's review that. So it's taking the Postgres dependency and it's taking the OpenCSV dependencies. They both look good so I'm going to click Accept. Now one thing I always make sure is that GitHub Copilot is a model that predicts the next [[Microsoft Word|word]]. So it may not always have the latest information. So for example, in this case of version, so if I want to know what the latest version is, I'm currently on VS Code, and I'm going to hit Control + Space, and then a screen will appear, and it'll tell me what is the latest version available. So as you can see, 42.7.4 was updated on 23rd of August. So I'm going to hit Tab to take this version, and I'll do the same thing for OpenCSV, Control + Space, and 5.9 is the latest version. So I'm going to hit Tab and accept. Once I have that, I need to make sure that the current code compiles well. So I'm going to open up my terminal, and I have a shortcut here, Option + F12. So my terminal opened up, so I am going to type mvn clean compile, which will make sure that I have all the dependencies
>
> **[2:28](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/automating-data-ingestion?u=76281980&t=148)** and all the code that I have is compiling well. So everything looks good. So now we are ready to look at the code that will do the download, parse, and ingest. Now for the purposes of this demo and keep the video to a reasonable size, I've already written that code. So I'm going to work with you and explain how this works. So the file is called DbImporter.java, and it is sitting inside com.h2 package. As I click here, this file is fairly long, so it takes some time to write. So I have done that part already, and I will explain step by step what's exactly is going on. So this is the class. So first of all we have set of imports. These imports are coming from Java directly. And line 9 to 13, all the imports are coming from OpenCSV. Now in order to connect with database, we have three variables, which is the URL, user, and the password. This is only for the demo purposes. If you are using a [[Microsoft Products|products]] in grid system, I would request not to check this in into your source control, either [[GitHub]], GitLab, or whatever system you use. Rather keep this in a configuration file and load them on runtime. Now the other variable we have is the GitHub gists file, which is exactly the file that we have been working with. Let me show you. So this URL is copied from here. This is the same URL that is pasted here. All right, now this is our main method, which means when we will run this class, this will be the method that will be executed. I have bunch of System.out.println.
>
> **[4:02](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/automating-data-ingestion?u=76281980&t=242)** So we will know on the console what step we are on. So step one is downloading the CSV, step two is parsing, and step three is ingesting into the database. And finally, we print that, the data ingested. So going to the first step, which is downloadCSV, it is a method. If I click here, it's right here from line 47 to 52. All it does it takes the URL string, which if you see here is a CSV URL, which is nothing but the GitHub gists file pointing to the books.csv. So downloadCSV takes that URL, it opens the HTTP connection, and then it returns the input stream, which is the series of bytes that comes down through the network. And we take that in the csvStream variable here and pass that to our second step, which is parseCSV, that takes the csvStream here. So now let's go and look at the parseCSV. So parseCSV starts at line 55 and ends at line 74. The whole purpose of this function is to take the stream and convert them into the list of records where each record is the array of values. So every row split by comma is the value that we will get. And the way it does that, the most important thing where we should start is line 63 to 65. What it does, it creates a CSVReaderBuilder, which means it takes a reader, which is a BufferedReader, and this BufferedReader comes from the original csvStream
>
> **[5:35](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/automating-data-ingestion?u=76281980&t=335)** that we downloaded from the internet. So it takes that reader, creates a CSV builder, and we also tell him what parser we need to use. So our parser is a CSV build parser with a separator comma. So with these two information, our csvReader is ready. So that is an object. And with that object we can start iterating on the object from line 68 to 70. As long as we have anything available in that csvReader, we add it to the records, which is nothing but the list of records. And finally, when everything is done, we close the csvReader to save on the resources and not to leak our program, and then return that set of records back to the caller. So if we go to the main function, we looked at the first part which is download, the second part, which is the parse, and this parse returns a list of records. And third and the final thing is inserting the data into the records. This is where we have the set of records sitting as Java objects, and now we need to send them to Postgres database. Now let's look at this code. So insert data, it looks like a big piece of code, but pretty straightforward to understand. So let's dive into that. The first thing is we create the connection to the database by passing in the URL, user, and the password. Then we have a bunch of [[SQL]] statements. The first SQL statement is how we are going to insert the author name into the database. The second one is if we have the author
>
> **[7:11](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/automating-data-ingestion?u=76281980&t=431)** in the database already in that table, we get the author_id back. The third one is it prepares the INSERT statement. It it's exactly the same INSERT statement that we saw when we were working with pgAdmin. And once it inserts, we are saying give me the book_id. So here we get the book_id, and the select author statement, we get the author_id. And if this is a new author, we insert that into the author and return the author_id. And finally for the book_authors, we insert into book_authors this book_id and the author_id. So all in all you have seen the most important piece, which is, what queries are we running? So the remaining thing that remains is, how do we map the array of objects into these SQL statements? For that, we start with preparing the PreparedStatement and, for each statement that we saw from line 80 to 83, we prepare the PreparedStatement to work with database. The first thing that we do from line 92 to line 96 is we read the header row from the CSV and we create an lookup map from the name of the column to the index it is sitting on, because it is ordered. And that helps us here when we start mapping the data from line 101 to line 114, right here to here. As you can see, all we are doing here is we are creating these variables by looking up into the headerMap
>
> **[8:45](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/automating-data-ingestion?u=76281980&t=525)** for that specific record in the list of records. Now that we have mapped everything, we are now ready to execute DB statements. So the first thing we do is we insert authors into the table from line 117 to line 133, which means if author already exists, we get the ID of the author. If not, we insert and then we get the author_id. We keep that in the author_id on line 117, and that's it. Then on line 135 all the way up until line 176, this is, because books has the most amount of information. In the PreparedStatement, we start keeping the data on specific indexes. And with that, once we have all this information available, we execute by sending the data to the database in books table. Then we get the book_id, and then finally, we are ready to insert data into the books_authors table. And this is exactly where we do those things. So we have only two set of information in book_authors, book_id and the author_id, and we set them on the respective order, and then we execute update. Finally, when all of this is executed, this is still happening in the context of database transaction, so nothing is committed. So online 178, we do a conn.commit, which means whatever we have done so far, if that is perfectly fine as per ACID rules, you just go ahead and save this in the database. Since we are working in the context of external system,
>
> **[10:20](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/automating-data-ingestion?u=76281980&t=620)** we have put everything under the try-catch. So line 87 wraps all the way up until line 179 so that if any wrong thing happens, if there is anything that is not correct, we roll back the connection. So if there are any partial things that could have updated in the database, we don't keep them. And finally, we just set the auto commit to true. So if it's rolled back, auto commit to true, so nothing is persisted. If everything was fine, you set auto commit to true and it's committed, and we close the connection. And with that we have seen long set of code, which boils down to three steps, which is download, parse, and insert. Now that we've seen the three steps, it's time for run it and make sure that everything exists in the database. However, before we run this code, we should clean up everything in our database so that everything starts fresh and we have consistent clean data available in our database. So I'm going to pgAdmin, and I'm going to click on books, and say Truncate, Truncate Cascade. It's because this table has relationship to the other tables, which is books_authors, and we want to truncate all of that data. So we will say yes. And we got the message here, table truncated. I am going to also truncate authors table. So Truncate Cascade. Yes. So we got the message, table truncated. And then finally for the books_authors, I am going to do the same thing just to make sure everything is fine.
>
> **[11:56](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/automating-data-ingestion?u=76281980&t=716)** All right, so at this point in time, if I click on authors, View, All Rows, nothing should be there, empty. If I click on books, View/Edit Data, All Rows, nothing, it's empty. And for the book_authors, View/Edit Data, All Rows, it's all empty, fantastic. So now let's go back to our code and try to execute the importer and see what we get next. I'm back into the code now. I'm going to open up my terminal. And I'm going to run mvn clean install, and I have this command available for me here. So we are running mvn clean install, and then we are running the application, main application, which is DbImporter in this context. So we want to run the com.h2.DbImporter, and that's what exactly we are doing here. So we'll clean compile, install the package, and execute this class. Let's run it. So all well. Now it is running the ingestion, and inserting into the database. It may take a little while to complete depending upon the internet speed. And it said build success, which means all the four steps, which is download, parse, and ingestion. It's complete. So our program is working perfectly fine. In the next video, we will go into the database and run some queries to make sure that the data ingested is correct. So I'll see you there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PostgreSQL|Postgres]] (6), [[Java]] (4), [[GitHub]] (3), [[SQL]] (3), [[GitHub Copilot]] (2)
> **Code Identifiers:** author_id (7), book_id (5), book_authors (4), csvstream (3), parsecsv (3)
> **Env Vars:** csv (7), url (7), sql (3), insert (2), f12 (1)
> **Tools:** github (5), terminal (3), vs code (2), gitlab (1)
> **CLI Commands:** make (6), mvn (3)
> **Definitions:** is a  (7), is called (1), is an  (1)
> **UI Navigation:** click on (3), go to (1)
> **File Paths:** pom.xml (1), dbimporter.java (1), books.csv (1)

#### [Validating data integrity](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/validating-data-integrity?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/validating-data-integrity?u=76281980&t=0)** - [Instructor] I am back in pgAdmin now, and we want to ensure that we have all the data available. So, I will first click on Authors, and I will open up the Query Tool. And with this, we'll be able to query anywhere in all the tables. I will say select count star from authors and as a run.
>
> **[0:27](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/validating-data-integrity?u=76281980&t=27)** So, we have 50 authors, which is correct because when this data was prepared there were 50 authors and then select count star from books. And as I mentioned that there are 20,000 books. So, we saw 20,000 books here. And then finally, we want to know how many books and authors combination we have. So, we'll say count star from books_authors, and as I run. It's 20,000, which means that for every book and every author, we have a combination available. Now, we can also look at the data. So, I can say select star from authors, limit 10. And then if I execute this, we have 10 authors. We can change this to books and then run it and we have books data. And then what we need is we need to run it for books_authors and run it again and we have the mapping available. Great, now, a very simple query could be we want to know, given an author name what all books they have authored. So, which means that we will need to work with all the three tables, books, authors and book_authors because the data is spread around these three tables. So, I'm going to write a query for that. I would say select b.star, which where will be books. I'm going to write that
>
> **[2:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/validating-data-integrity?u=76281980&t=120)** and a.name as author_name and from books.
>
> **[2:11](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/validating-data-integrity?u=76281980&t=131)** So, we are taking the majority of information from books and now we are going to join on books_authors. I'm going to call this ba on b.book_id which equals to ba.book_id. And then I'm also going to join authors, woops, join authors a, and this a comes here, authors a and I'm say on ba.author_id
>
> **[2:53](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/validating-data-integrity?u=76281980&t=173)** is Equals to a.author_id. And then we need to find out which author we are talking here. We'll say where a.name is equals to Steve Wozniak. And then I'm going to run this query now and let's see what we get. Okay, so I have a typo here. So, this is what the book says, and as I run, I'm getting, oh, because I forgot B here. And then as I hit run again, we got all the books authored by Steve Wozniak and the name of the author is right here. And if I change it to some other author, let's say Sundar Pichai, and run it again, we got different set of books authored by Sundar Pichai. Now, all these books that we have created in this dataset are fabricated, which means they're not original books, but we wanted to showcase the ability, if you have a dataset like this in a CSG file and you spread it around into different tables, how you would write the queries. Great so, we have identified that the data seems correct in this case, so, all good. So, we are now going to now keep starting our database setup from scratch. We'll keep it as is because we have most data available. So, let's go back and look into the books table once more. So, we'll say star from books and then run it. Now, as you can see,
>
> **[4:27](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/validating-data-integrity?u=76281980&t=267)** since we were starting the database from scratch, we have lost our column for search index. So, let's go ahead and add that on books. So, we are going to add the ALTER TABLE on books and add the search_vector tsvector. So, we've got that. And once we have the vector column available, we need to update our search_vector based on the query that we saw before. So, I'm going to run that. So, it takes a bit of time and now it's ready. So, we can see that for the first 10 columns. And as you can see now our search_vector is ready. Fantastic, so, now that our database is set up with the right set of books downloaded from [[GitHub]] gist and our search index is ready, we are ready to go onto the next step where we are going to write functions in [[Java]] to [[Fetch]] the data based on the user queries.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (1), [[Java]] (1), [[Fetch]] (1)
> **Code Identifiers:** books_authors (3), search_vector (3), book_id (2), author_id (2), pgadmin (1)
> **Env Vars:** csg (1), alter (1), table (1)
> **Definitions:** means that (2)
> **Prerequisites:** setup (1), set up (1)
> **CLI Commands:** find (1)
> **SQL:** alter table (1)
> **Tools:** github (1)


### 5. Implementing Business Logic and Writing Tests

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction to business logic](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/introduction-to-business-logic?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/introduction-to-business-logic?u=76281980&t=0)** - [Instructor] Now we have three tables: Books, Authors, and BooksAuthors, and we need a way where user can ask queries and we can return the relevant information back to the users. So how do we do that? Shall we hand over the database access directly to them? That is not often ideal, because first of all, you want to create an abstraction between what user asks and how that information is given to them. So consider this example only for the books at the moment. You want people to be able to search for books given a search term. Now, you need to define what that query may look like, and you need to create that query. Query it from the database, and then return the results back to the user. So whatever I've talked is a set of procedure that we need to write, and that set of procedure sits between the user query and how to get the data specific to that user query. And that's business logic. The business logic is a way of saying, given a specific user input, you need to sanitize or make sure that the user input is correct before going and doing the work of identifying what needs to be done to that query. And whatever is the output of that query, you send it back to the user. So whenever you abstract the access to your database and internal infrastructure from the user queries, and that's what essentially business logic is. Now, if you're focusing purely on the books we have, if I [[Zoom]] out little bit, we have a table for books
>
> **[1:36](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/introduction-to-business-logic?u=76281980&t=96)** where this is a simplification, but we have ID and the book title. So far, we have the table. But what we need to do is to create the entity using [[Spring Data]] where each entity maps to a specific record in the database table. And then we need to abstract the concept of entity into a repository where repository maps to the set of rows or all the rows in a specific table. So, so far, the way we have interacted with the database, we have not made use of spring data. We have not created any entity or any repository, and this is essential in order to write the business logic. So in the next video, we'll start writing the entity repository and we'll create the test to make sure that our code is able to access the database that is running inside the Docker container.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Spring Data]] (2), [[Zoom]] (1)
> **CLI Commands:** make (2), docker (1)
> **Definitions:** is a  (3)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Developing business logic functions](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/developing-business-logic-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/developing-business-logic-functions?u=76281980&t=0)** - [Instructor] I'm back in VS Code now, and the first thing I will do is open pom.xml. And we need to add the dependency for [[Spring Boot]] Data JPA, which I've done right now. And I will make sure that all the code compiles correctly. And as I hit run, everything is working fine. Now I will go back to the src, main, this is the package com.h2, and I'm going to create two directories. The first one is to hold the entity. The second one is to hold the repository. And we'll work with the two at the moment. So I will create a new file called Book.[[Java]], and this is where we are going to map to entity books. And I'm going to call it entity, so it added the import automatically. And the second is we need to add it to the Table. And it's taking the name Book, but we know that table name is books, so I'm going to take that. Now we know that there were a lot of fields in the book, so I'm going to copy and paste to save time. And I'm going to fix certain imports. But the idea is that our bookID is the primary ID. And the GenerationType is IDENTITY. And we are going to import the java.[[SQL]] Date. And we also have the searchVector, which maps to the column search_vector. And the columnDefinition is tsvector.
>
> **[1:34](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/developing-business-logic-functions?u=76281980&t=94)** This should not be new to you. We have already looked into the specific videos in the past, where we have gone through the details of each one of those. Now that we have all the getters, I'm going to leverage [[GitHub Copilot]] to help us write all the getter methods. So I'm going to do Cmd + I as a shortcut, and I will write, create getter methods for all fields. And as I hit Enter, GitHub Copilot in a few moments will start creating all the getter methods for every field that we have. All I need to do here is to click Accept, and we have the entity up and running. One thing that I also want to do is to override toString. And then as I hit Enter, it is creating the toString methods so that when we log anything on the terminal, we get all the information here. I will accept and remove the searchVector because this is not something I would be looking into. So this is all we have. So we have the entity ready. So going back to the diagram, we have the entity, now we need to create the repository that uses this entity. So as a next step, I'm going to right-click on repository, New File, and this is BookRepository.java. And this is not the class. This is the interface that will extend
>
> **[3:08](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/developing-business-logic-functions?u=76281980&t=188)** the JpaRepository, and it has a type of Book, which is our entity, with the Long, which represents the ID in our system. And with that, we have a repository that maps to the entity. And within this repository, we are going to introduce a function that will search for the books. So I will write list of book, and GitHub Copilot is giving a suggestion that I don't need right now. I need searchBooks. And it's taking the method here and the parameter, but I need the (@Param("searchTerm") String searchTerm). And I'm going to import the List, java.util.List. And since the searchBooks is making use of the tsvector, we are going to embed the query that we have used in the past. So I'm going to hit Tab, and then in the value, it is giving me a template for the query that I should use, and this seems like what we have written in the past, so I'm going to hit Tab. Then there is a nativeQuery. This is a nativeQuery because we are going to translate this into [[PostgreSQL|Postgres]] raw query and that is what is going to execute when being called. So, so far so good. So what we have right now is we have entity and we have repository. So, all good so far. So with that, we have created a system
>
> **[4:42](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/developing-business-logic-functions?u=76281980&t=282)** wherein we can interact with our database. There is one final thing that is missing, and I will cover that in the next video when we are going to execute the test, so I'll see you there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Java]] (4), [[GitHub Copilot]] (3), [[Spring Boot]] (1), [[SQL]] (1), [[PostgreSQL|Postgres]] (1)
> **Code Identifiers:** searchvector (2), tostring (2), searchbooks (2), searchterm (2), nativequery (2)
> **Tools:** github (3), vs code (1), terminal (1)
> **File Paths:** pom.xml (1), book.java (1), java.sql (1), bookrepository.java (1)
> **Env Vars:** jpa (1), identity (1)
> **Cross-References:** go back to (1), in the next (1)
> **Definitions:** is a  (2)
> **CLI Commands:** make (1)

#### [Writing unit tests](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/writing-unit-tests?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/writing-unit-tests?u=76281980&t=0)** - [Instructor] We have the entity and we have repository and according to us this should work. But how do we validate that this indeed is working? And this is where the idea of testing shines. Testing is a way to ensure that as a developer, whatever code I'm writing and I'm responsible for, I have an automated way of verifying that the code executes as expected and it returns the results as desired. So far, we have created the entity and the repository and just by saying this should work doesn't matter, because when it is deployed in the production, we must make sure that this is the correct code. So what I will do now is I will write a first test for the repository. We don't need to write the test for the entity because it'll be mostly mocking. So I want to write the real test that connects with the database, and the book repository is the first place we can do that because we have a meaningful business logic for searchBooks that executes a valid [[SQL]] query given a search term. So in order to do that, I'm going to do a right-click and then says Go to Test. And there is a window that pops up that says Search files and Generate test. I'm going to click Generate test because I know I have not written any test so far. I click here and then it matches the same structure that we have here, which is com.h2.repository. And then it says BookRepositoryTest, but it is not going to add it in the same src main [[Java]], instead it'll put it under src test java.
>
> **[1:33](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/writing-unit-tests?u=76281980&t=93)** So you will see in just a moment as I hit Enter. As I hit Enter, it asks for one more thing that what all methods you would like to test. Since we have just searchBooks, I'm going to select all and then click OK. So on the left, as you can see, this came in test directory, java com h2 repository, BookRepositoryTest. So now that we have the book repository test, we need to ensure that this function is going to connect with the database and will validate that it is able to [[Fetch]] the books from the collection based on the query. Since we are testing it at the [[Spring Data]] layer, the first annotation I need is DataJpaTest. I will hit the Tab, and the import automatically is added. The second thing that we need is we need to auto-wire the bean. Auto-wire the bean for the BookRepository. [[Microsoft Copilot|Copilot]] is giving the solution. I'll hit Tab, and I will accept it. So I have that, and it says that we are not even using it right now, but that is not an issue. We are just now going to write a test that can test all the books. So now what we are going to do is List<Books> and I'm hitting Tab and it is calling the searchBooks for search term java. I'm going to fix the imports. So we are taking the util.List and the book is coming from the entity. So I'm going to change the query term to [[Algorithms]]
>
> **[3:09](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/writing-unit-tests?u=76281980&t=189)** because this is what we have been using so far. And then how do we ensure that we do get the test? So just by printing is not going to help because in the case of test, you validate by writing assert statements. So I'm going to say assertTrue that book size is greater than zero. And the reason I'm testing that is because I don't know how many books for algorithms exist in that table, but I do know that some exist. So the size should be at least greater than zero. What we can also do is you can print the books found on the console, and I can do that by saying System.out.println, and I will say book title is this. And then I'm going to add these three arrows to ensure that it catches the highlight on the terminal. Now, the two things that we still need to do, first thing is we have not specified where our database is and we don't want to hard-code it either. Now, since we are going to run the test, we need to create a properties file and test directory under resources folders. So this is resources and the file name is application.properties. And within this application.properties we need to specify a few properties that I've pasted it here, but I will explain what they're doing. The first one is the datasource.url, which means where our database is, along with the user ID and password and which driver we are using.
>
> **[4:41](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/writing-unit-tests?u=76281980&t=281)** So we are using [[PostgreSQL|Postgres]] driver. Now for JPA, what we are saying is that, when we are using the SQL dialect, we are using the Postgres SQL dialect, we are telling Hibernate to not run any DDL scripts, which means do not create any table because we've already done so. And the third one, it says either show the SQL on the console or not. It's false right now, but I can change it to true. With that, when our test runs because of the DataJpaTest, it'll make sure that it reads the application properties under the test resources file, and this will allow it to connect to the database. Now there's one final piece, but let's run it first in order to identify what we are missing, and then we will connect to it. So first thing is I will run mvn clean test. This will also make sure that the code compiles successfully. So it's hanging somewhere here and we are not sure where. So it failed, and let's say why it failed. So it fails because it's saying IllegalStateException, failed to replace the DataSource with an embedded database for tests. So by default in the test, Spring Data looks for the embedded database like in-memory S2 database. But since we are connecting directly with the actual database, we need to tell it to not use the embedded database. And the way to do that is at the top of the test, we will say AutoConfigTestDatabase, and then we say replace to NONE,
>
> **[6:15](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/writing-unit-tests?u=76281980&t=375)** which means do not use the embedded database. We have already provided that in the application.properties. Please use that. With that, I'm going to clean the screen and we are going to run the script again and see if that works. And within few moments, yes it did work. So we had one test that is running, and as you can see we've got all the books that have [[Microsoft Word|word]] algorithms in it, and there are a lot of them. More than I can count. And I will hide this print statement and we will run it again to see if we can see the SQL query that is being run on the console. So as you can see here, it's connecting, and here is our query. So it's running the Hibernate SELECT statement that we put on the BookRepository. Great. So what we've done so far is that we have validated that every entity and the repository code that we have written, we have an associated automated test to ensure that our business logic is indeed working. Great progress. So now there is one final step and we are getting closer to the part where we can create the APIs and take in the user queries. So I will see you next, and we will make progress there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SQL]] (5), [[Java]] (4), [[Algorithms]] (3), [[Spring Data]] (2), [[PostgreSQL|Postgres]] (2)
> **Env Vars:** sql (5), jpa (1), ddl (1), none (1), select (1)
> **CLI Commands:** make (4), mvn (1)
> **Code Identifiers:** searchbooks (3), asserttrue (1)
> **UI Navigation:** right-click (1), go to (1)
> **Definitions:** is a  (2)
> **SQL:** select (1)
> **Tools:** terminal (1)


### 6. Designing and Creating APIs

[↑ Back to Table of Contents](#table-of-contents)

#### [Developing the service layer](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/developing-the-service-layer?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/developing-the-service-layer?u=76281980&t=0)** - Now that we have built our entity and repository that can access the database, we still need a good, clean API design so that the details of our repository or what persistence layer we are using are not exposed to our consumers. So, before we get onto the RESTful API, we need one level of abstraction. So, what we are going to do is we are going to create a service layer, and this service layer is going to connect with the repository layer. Now, there are many benefits of having the service layer in the API. First, it abstracts away what the persistence mechanism are we using. So today, you might be using relational database, but tomorrow you might be going on [[NoSQL]] database such as [[MongoDB]]. The other benefit it provides, it gives you an opportunity so that the details of your database objects either one specific record, or if you want to merge the information from two different tables. All of this is possible in the service layer because repository layer only access one table. But at the service layer you can connect to multiple tables, merge the results and send it back so you get that opportunity. And the third and final thing that I see as a benefit is it sanitizes the user input. So, as you mentioned, that we are building it for users, and users are going to send any information that they like, we would want to take an opportunity in the service layer to ensure that the search term is correct,
>
> **[1:33](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/developing-the-service-layer?u=76281980&t=93)** because if the search term is not correct, what's the point of going all the way to repository and accessing the database? It's a waste of time and resources. So, before we jump onto building the RESTful API, let's build the service layer first. I am back into the code base right now, and we already have the entity in the repository layer. So, I'm going to create a new folder called Service, and I'm going to create a new file here called BookService.[[Java]]. Now, this is a class, and the first thing that we are going to annotate it with, we are going to call it as Service. And then, we need to wire the bean. So, we'll use the Autowired annotation, and we are going to have the bean wired for book repository. And now, you may have seen that I'm using more and more [[GitHub Copilot]] to do the code complete so that I can talk more, explain a bit better. And the productivity that I'm getting with GitHub Copilot is also accurate. So, I'm going to import the book repository here. And the function that we need here is called public list of book. And we need it for search, so searchBooks. And it says that you need the keyword, and you call the BookRepository, and you return the searchBooks, whatever information that you get. So, let me just first import the necessary classes. So, I got this one, I'm going to rename this to search term
>
> **[3:12](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/developing-the-service-layer?u=76281980&t=192)** 'cause that's the term I like the most. And as I mentioned that one of the opportunity you get, is if this search term is sent by the user, you may want to validate if it's correct. So, I'm going to say if. And see, GitHub Copilot saying is if do you want to do this thing? Do you want to check for the null and the empty? I will say yes, this is what I want to do, but I don't want to do a exhaustive search, I don't want to return all the books, rather I want to throw the error. So, as I type throw, it changed its suggestion, and it said, "Okay, so you want to throw the illegal argument exception that it cannot be empty," and I will say yes, and I need to close the code block. And now, our code is ready. So like before, how do we know it's working? So, we are going to create the test. So, I'm going to do Go to Test, and then Generate test. And we are going to do it on the same path, but under the test directory, I will hit Enter and I will say searchBooks. So, it already created some stubs, so first thing that we need to do is we need to tell that this is a SpringBootTest, and it imported the necessary classes. And the other thing that we need to do, is first, we need to Autowire. But in this case, we are going to Autowire the service which GitHub Copilot exactly got it. We don't need the repository, we are abstracting away from repository and going to the service layer. So, we need the BookService.
>
> **[4:47](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/developing-the-service-layer?u=76281980&t=287)** So, I'm going to change the names so that GitHub Copilot can help me write the test. testSearchBooks is not exactly demonstrating what use case. So, I'm going to say when term is empty. And as I hit Enter, let's see what it has to say, searchTerm, it took the empty string. And now, it's saying that when you will make a call to searchBooks, you're expecting the illegal argument exception, and this is exactly what I am expecting. So, I took the necessary report, and that's it! And we are going to write one more test. So, GitHub Copilot is helping me out on that, and it's writing the second test case for null. And the third test case, it is saying, I don't want to do it for the blank, so let's do when the search term is valid. When the search term is valid. So, in this case, it took the keyword Spring, and I'm going to change it to [[Algorithms]], and I'm going to take this into List of book, books. And I need to have the necessary imports here, so I need a Java (indistinct) and I need this entity. And now, we need to assert some things. So, we will say assertTrue that books.size is greater than 0. This is based on one of the previous tests that we did,
>
> **[6:23](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/developing-the-service-layer?u=76281980&t=383)** but now we are doing it at the service layer. So, now that we have our service layer code, which abstracts away the repository. See, because the repositories here, in our test, we are only dealing with a BookService, we don't know what repository we are using. And then, it also helps us sanitize or validate the search term before it sends the query to the repository. Now, we have three test. Now, the moment of truth, we need to go ahead and test it out, so I'm going to type MVN clean test. And within few moments, all our tests should pass. If not, we'll figure out what's going on. Yep, it passed. But how do I know if it is working? So, I'm going to change it to equals 0 just to make it fail. This will help us assert that this test class is indeed executing. So, it failed. So, which means, our test class is indeed executing, I'm going to change it and run it once again so that we ensure that all the tests in this code base are passing. And this is a way as a developer to verify and communicate that the code is working as expected. Alright, so now this is done, we are good. So, going back to the example that we started with, we have successfully created the service layer. Now, the final thing is we need to create the [[Representational State Transfer (REST)|REST]] API that interacts with the service layer next. So, let's go ahead and create that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub Copilot]] (6), [[Java]] (2), [[NoSQL]] (1), [[MongoDB]] (1), [[Algorithms]] (1)
> **Code Identifiers:** searchbooks (4), testsearchbooks (1), searchterm (1), asserttrue (1)
> **Env Vars:** api (5), mvn (1), rest (1)
> **Tools:** github (6)
> **Definitions:** is a  (3), is called (1)
> **CLI Commands:** make (2), mvn (1)
> **File Paths:** bookservice.java (1)
> **Cross-References:** in the next (1)

#### [Implementing the book search API](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-the-book-search-api?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-the-book-search-api?u=76281980&t=0)** - [Instructor] So we are almost there, and we have our user sitting here, and they need to connect with an API, a RESTful API, to search for the books. So in this piece, what we are going to do, we are going to create a controller layer that defines the RESTful API. And this controller layer is going to talk to the service layer. So when user is going to interact with our API, they will connect with the controller layer. The controller layer is going to pass the information to service layer. Service layer is going to verify, sanitize the input, search from the database, take the result and give it back to the controller, which goes back to the user. All right, with that, I will go back to the code base. And now I'm going to create a new folder called controller. And within this, I'm going to create my first file, which is BookController.[[Java]]. Now this is a class, and the first thing that I need to mention is that this is a RestController. And once I do that, I also need to tell what endpoint this will expose. So this is going to be provided by RequestMapping and using books. I'm using the suggestion from [[GitHub Copilot]]. And first I will do the auto wiring. So I will do this for the BookService. So now our controller is only going to talk to the service and not repository.
>
> **[1:34](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-the-book-search-api?u=76281980&t=94)** I'm going to add the import here. And now I need the RequestMapping, or I need a GetMapping for search of the books. So this is the search endpoint under books that our users are going to utilize. So I will say public list of Book, searchBooks,
>
> **[2:01](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-the-book-search-api?u=76281980&t=121)** and all we are doing is we are calling the BookService and searchBooks. But before that we need the search term that we can pass in. First I will import all the necessary classes, and then I also need a RequestParam. So we are taking this in the RequestParam rather than the body, so RequestParam. And I would say that this is String searchTerm, and I'm going to import this class as well and pass the searchTerm here. And with that, our search endpoint is ready. So when the user goes to /books/search?searchTerm and provide the value, this is where the code will be executed. But we still need to write the test before we can look at from the user's perspective that this is going to work. So I'm going to create a new test. So, Go to Test, Generate, Enter, and then yes, searchBooks is where we need. So first thing we need to mention that this is a SpringBootTest. And then we also need to mention one more thing, which is that we are using the webEnvironment and when the server start it, can assign any random port. With that, the first thing that we need is, in order to test as the user in an automated fashion, is we need a TestRestTemplate. So I'm going to get that here first. And private TestRestTemplate.
>
> **[3:37](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-the-book-search-api?u=76281980&t=217)** This is what we are going to use to make calls as the user. Now that I have it here, or I'm going to write the implementation for this testSearchBooks. So first is my searchTerm, searchTerm is [[Algorithms]]. And then what we need to do is we need to call the restTemplate.getForEntity, and I'm going to say, /books/search?searchTerm={searchTerm},
>
> **[4:18](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-the-book-search-api?u=76281980&t=258)** that we are going to provide. Now the responseType is going to be a Book array, not the list, because this is crossing the wire. So this is going to be the Book class, and we are going to pass the searchTerm. Now let me keep this in the list. Let me make this a little bit bigger. So what we need to do is we need to take the response into a variable. So we'll take that as the response. And now that we have the response, we can start doing the assert. So first thing is we are going to get all the books from the response, and then we will assert that the books are not null. And let me take the import, and then we are going to do assertTrue, and then that the books.length is greater than 0, and take that import as well. Now in order to make sure that we do get the response back, let me also print all the searched books on the console. So I will do System.out.println, and run this. Fantastic. So two things that we did. We created our controller layer, which will define the entry point for the user query. And then we wrote a test that will start the server on the random port and will test for searchTerm calling algorithms. Now let's go ahead and run the test. Okay, so it says build succeeded, test passed,
>
> **[5:54](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/implementing-the-book-search-api?u=76281980&t=354)** which means our test is passing. Let's go ahead and see if we got the books. So as you can see, we get a lot of books, and we are getting each and every information about the book. It's because we added the override for toString. So we get all this information. Great. So with that, we have successfully created our RESTful API to search the books that connects to the database and comes back as the result. Now there's one final piece that I would like to show is, how can you start the server and hit the API endpoint and see the results? So I will show you in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Algorithms]] (2), [[Java]] (1), [[GitHub Copilot]] (1)
> **Code Identifiers:** searchterm (9), searchbooks (3), webenvironment (1), testsearchbooks (1), resttemplate (1)
> **Env Vars:** api (6)
> **CLI Commands:** make (3)
> **Definitions:** is a  (3)
> **Cross-References:** go back to (1), in the next (1)
> **File Paths:** bookcontroller.java (1)
> **Tools:** github (1)

#### [Writing integration tests](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/writing-integration-tests?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/writing-integration-tests?u=76281980&t=0)** - [Instructor] All right, so the whole point of building this entire project is so that you can run the server and your users can interact with it. But so far, we have written all this code and haven't started the server even once. Now let's do that now. And to give you some idea, it'll fail first, and I want that to fail so that I can give you idea on why this is failing. So that in future, if you see some issues like that, you will know why that would have happened. So I'm going to run the mvn spring-boot:run command and I'm expecting that this will work and it is trying to run at the moment, it is trying to bring up the server, but it failed. It failed because it is trying to find the data source which it can connect to and it could not find one. The question is why is it the case that it was not able to find the data source? I mean, we are running all our tests and everything is working fine. So turns out when we were running it on the test side, we provided it with a file called test resources, application_properties that contain everything. But it was only working in the context of test and not domain application. So what we need to do is we need to create the same file, and I'm going to call it resources, and copy this file to src main resources as well. So that is first thing. The second thing is when we start our application,
>
> **[1:34](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/writing-integration-tests?u=76281980&t=94)** our application is bundled as a JAR file and then deployed on a server, which means the [[Spring Boot]] application needs to have all the information baked into one JAR file so that it can find everything before it starts the server. For that, there is one final piece that we need to do, is we need to add the plugin for Spring Boot. I'm just adding it here now. So in the build process, we are adding the Spring Boot Maven Plugin, which will ensure that when the server starts, it has all the information available to start the server, which means it'll be able to collect information from application properties and start the server and run it. Now with these two things, if we compile the package and run it again, it should work. So let's try it out. So I'm going to mvn clean install. So we'll first make sure everything works and the package is created successfully. And that worked. And then we are going to run the same command again. I'm going to clean the screen and start again. So now our server is running, as you can see on this line, it says started on port 8080. So at this point in time, our server is running. So let's go to the browser and test it out. So I'm going to open localhost:8080 and our initial endpoint was hello controller that we wrote when we were starting the project. It is still working, and that is a good testament on the server running and giving us the response. Now let's go back and add the search. So under the books,
>
> **[3:10](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/writing-integration-tests?u=76281980&t=190)** we need search term and the search term is [[Algorithms]] here, exactly how we wrote it in the test. So let me hit Enter. And see, we have a lot of information available here. I will click on Pretty-print so that we can see the [[JSON]]-based data here for each and every book. And as you can imagine, there are a lot of books here. Now, when you want to communicate your API with your users, one very popular way is to do it by creating a collection on Postman and send it out. So let me just take this URL, copy it, and let's go back to the Postman and I'm going to paste it here and hit Enter. And we are going to get exactly same response here. So what you can do is you can take this, you can save the request, for example, in a collection, and you can communicate this with your users that the new API for searching the books is now available. So I hope that this was useful. Thank you.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Spring Boot]] (3), [[Algorithms]] (1), [[JSON]] (1)
> **CLI Commands:** find (4), mvn (2), make (1)
> **Env Vars:** jar (2), api (2), json (1), url (1)
> **Ports:** port 8080 (1), :8080 (1)
> **Tools:** postman (2)
> **UI Navigation:** go to (1), click on (1)
> **Analogies:** imagine (1), for example (1)
> **Code Identifiers:** application_properties (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Conclusion](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/conclusion?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/creating-a-book-search-engine-from-scratch-using-java-and-github-copilot/conclusion?u=76281980&t=0)** - [Instructor] Congratulations. We successfully finished our course. To remind, we started with the journey where user can search for the books in the database and along this journey we started with some nitty gritty details. We started setting up the database in a Docker container, then we pulled the CSV files from the remote and inserted that in the database inside a Docker container. We wrote the business logic and APIs and along each step, we ensured that each line of code that we are writing is correct by writing automated test. I hope that you got the value of the time that you spent on this course and if you found this course useful, feel free to share it on [[LinkedIn]] with your extended network and let them know that they will find this course useful as well. Again, thank you very much for your time and I will see you in the future in some other course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[LinkedIn]] (1)
> **CLI Commands:** docker (2), find (1)
> **Env Vars:** csv (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [[Harit Himanshu]]

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/hhimanshu/book-search)

## Skills Covered

- GitHub
- Artificial Intelligence (AI)
- Spring Boot
- GitHub Copilot
- Java

## Path Context

### In [[Master GitHub Copilot]]
← [[Responsible GitHub Copilot- Creating Reliable Code Ethically]] | **6 of 8** | [[Refactoring with GitHub Copilot]] →

## Appears In

- [[Master GitHub Copilot]]

## Related Courses

_Courses sharing skills:_

- [[Practical GitHub Copilot]] — Artificial Intelligence (AI), GitHub, GitHub Copilot
- [[Exploring the Future of Development with GitHub Copilot Workspace]] — Artificial Intelligence (AI), GitHub Copilot
- [[AI Pair Programming with GitHub Copilot]] — Artificial Intelligence (AI), GitHub Copilot
- [[Capstone- Building Production Features with Copilot]] — Artificial Intelligence (AI), GitHub Copilot
- [[Responsible AI Development with GitHub Copilot]] — Artificial Intelligence (AI), GitHub Copilot

---

[↑ Back to top](#)