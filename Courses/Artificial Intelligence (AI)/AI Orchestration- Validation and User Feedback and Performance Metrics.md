---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: ai-orchestration-validation-and-user-feedback-and-performance-metrics
url: "https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics"
duration_minutes: 121
duration: 2h 1m
level: Intermediate
updated: 4/7/2025
learners: 2763
skills:
  - Large Language Model Operations (LLMOps)
  - Artificial Intelligence (AI)
  - Feedback Control Systems
  - Computer System Validation
  - AI Orchestration
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D4E0DAQHyS4nS85rA4g/learning-public-crop_675_1200/B4EZXYz8qNHgAc-/0/1743099252030?e=2147483647&amp;v=beta&amp;t=YxTJxwwg6wJZztD81ut9mtz07Pv609aCBQfU7V_wvbQ"
linkedin_topic: Artificial Intelligence (AI)
learning_paths:
  - '[[Building AI Products- Architecture and Orchestration Professional Certificate by LinkedIn Learning]]'
prev_courses:
  - '[[AI Orchestration- Developing and Testing Your AI Prototype]]'
path_nav: '[{"path":"Building AI Products- Architecture and Orchestration Professional Certificate by LinkedIn Learning","position":5,"total":5,"prev":"AI Orchestration- Developing and Testing Your AI Prototype","next":null}]'
path_count: 1
tags:
  - course
  - topic/artificial-intelligence
  - topic/software-development
  - skill/large-language-model-operations-llmops
  - skill/artificial-intelligence-ai
  - skill/feedback-control-systems
  - skill/computer-system-validation
  - skill/ai-orchestration
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Artificial%20Intelligence%20(AI)/AI%20Orchestration-%20Validation%20and%20User%20Feedback%20and%20Performance%20Metrics.md)

![AI Orchestration: Validation and User Feedback and Performance Metrics](https://media.licdn.com/dms/image/v2/D4E0DAQHyS4nS85rA4g/learning-public-crop_675_1200/B4EZXYz8qNHgAc-/0/1743099252030?e=2147483647&amp;v=beta&amp;t=YxTJxwwg6wJZztD81ut9mtz07Pv609aCBQfU7V_wvbQ)

# AI Orchestration: Validation and User Feedback and Performance Metrics

> Ready to get up to speed with the latest, most cutting-edge trends in AI orchestration? In this course, instructor Janani Ravi provides a comprehensive overview of how to effectively evaluate and integrate user feedback into AI models. Focusing on both traditional models and large language models (LLMs), Janani introduces essential concepts such as validation techniques, performance metrics, user 

> [LinkedIn Learning](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics) | 2h 1m | Intermediate | 3K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Prerequisites](#prerequisites)
  - [Quick overview of this course](#quick-overview-of-this-course)
- [**1. Validating ML Models and LLMs**](#1-validating-ml-models-and-llms) (10 videos)
  - [Validation in the ML workflow](#validation-in-the-ml-workflow)
  - [Types of cross-validation](#types-of-cross-validation)
  - [Regular and k-fold cross-validation](#regular-and-k-fold-cross-validation)
  - [Stratified cross-validation and nested cross-validation](#stratified-cross-validation-and-nested-cross-validation)
  - [K-fold cross-validation](#k-fold-cross-validation)
  - [Validating LLMs](#validating-llms)
  - [Offline validation](#offline-validation)
  - [Golden datasets](#golden-datasets)
  - [Benchmarking](#benchmarking)
  - [AI validating AI](#ai-validating-ai)
- [**2. Evaluating ML Models and LLMs**](#2-evaluating-ml-models-and-llms) (17 videos)
  - [Evaluating models using metrics](#evaluating-models-using-metrics)
  - [Evaluating regression models](#evaluating-regression-models)
  - [Evaluating classification models](#evaluating-classification-models)
  - [Evaluating clustering models](#evaluating-clustering-models)
  - [Accuracy precision recall](#accuracy-precision-recall)
  - [Evaluating large language models (LLMs)](#evaluating-large-language-models-llms)
  - [Human evaluation](#human-evaluation)
  - [Statistical methods for LLM evaluation](#statistical-methods-for-llm-evaluation)
  - [ROUGE scores](#rouge-scores)
  - [BLEU score](#bleu-score)
  - [METEOR score](#meteor-score)
  - [Perplexity](#perplexity)
  - [Model-based methods for LLM evaluation](#model-based-methods-for-llm-evaluation)
  - [Natural language inference](#natural-language-inference)
  - [BLEURT](#bleurt)
  - [Judge models](#judge-models)
  - [LLM evaluation](#llm-evaluation)
- [**3. Collecting and Using User Feedback in LLMs**](#3-collecting-and-using-user-feedback-in-llms) (4 videos)
  - [User feedback in LLMs](#user-feedback-in-llms)
  - [Implicit and explicit feedback](#implicit-and-explicit-feedback)
  - [Reinforcement learning](#reinforcement-learning)
  - [Reinforcement learning from human feedback](#reinforcement-learning-from-human-feedback)
- [**Conclusion**](#conclusion) (1 videos)
  - [Summary and next steps](#summary-and-next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Prerequisites](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/prerequisites?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/prerequisites?u=76281980&t=1)** - [Instructor] Hi, and welcome to this course on Validation, User Feedback and [[Performance Metrics]], part of the [[AI Orchestration]] Series. Now, before we get down to the content of this course, here are some prereqs that you need to have to make the most of your learning. You need to be familiar with how machine learning and [[Artificial Intelligence (AI)|artificial intelligence]] models work. You need to be familiar with basic models such as regression, classification, clustering, and the basic principles of a [[Generative AI]] and [[Large Language Models (LLM)|large language models]]. You need to have a basic understanding of how we validate machine learning models, and you should be familiar with performance metrics for different kinds of models. If you feel that you have these prerequisites, then this course is the right one for you.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Performance Metrics]] (2), [[AI Orchestration]] (1), [[Artificial Intelligence (AI)|Artificial intelligence]] (1), [[Generative AI]] (1), [[Large Language Models (LLM)|Large language models]] (1)
> **Prerequisites:** you need to have (2)
> **CLI Commands:** make (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Quick overview of this course](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/quick-overview-of-this-course?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/quick-overview-of-this-course?u=76281980&t=1)** - [Instructor] Let's quickly discuss what I'm going to be covering here in this course. A quick overview. We'll first talk about validation as a part of [[AI Orchestration]]. Validation is a critical step in the AI workflow because it ensures models perform reliably across different scenarios. In traditional machine learning, this includes techniques, like cross-validation and key fold validation, to assess how well the model generalizes. For LLMs, validation expands to evaluating output diversity, bias, mitigation, fairness testing, and ethical considerations. We'll discuss methods like offline validation, benchmarking with golden data sets and AI-assisted evaluation to help ensure robustness before deployment. The next chapter will cover [[Performance Metrics]] for AI orchestration. Assessing AI performance requires the right metrics tailored to different model types. With traditional ML models, you'll use metrics such as R square for regression, accuracy, precision, and recall for classification, and the silhouette score for clustering. LLMs, however, require more nuanced evaluation through statistical techniques like BLEU, ROUGE, METEOR Scores, and model-based approaches such as NLI and LLM judges. If I'm throwing acronyms at you, don't worry. We'll do an overview of all of these techniques in this course. And the final chapter of this course, we'll talk about user feedback for AI orchestration. User feedback is essential for improving AI models, particularly [[Large Language Models (LLM)|large language models]].
>
> **[1:33](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/quick-overview-of-this-course?u=76281980&t=93)** We'll discuss two different types of user feedback, explicit, in the form of user ratings or corrections, or implicit, in the form of click-through rates or dwell time. We'll also discuss [[Reinforcement Learning]] from human feedback, or RLHF, which helps align models with human preferences. And we'll also discuss how fine-tuning foundation models based on real-world interactions ensures AI systems remain relevant, reliable, and aligned with user needs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[AI Orchestration]] (3), [[Performance Metrics]] (1), [[Large Language Models (LLM)|Large language models]] (1), [[Reinforcement Learning]] (1)
> **Env Vars:** bleu (1), rouge (1), meteor (1), nli (1), llm (1)
> **Analogies:** such as (2)
> **Cross-References:** next chapter (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 1. Validating ML Models and LLMs

[↑ Back to Table of Contents](#table-of-contents)

#### [Validation in the ML workflow](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/validation-in-the-ml-workflow?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/validation-in-the-ml-workflow?u=76281980&t=1)** - [Instructor] Let's discuss the role of validation in the machine learning workflow. Now, here is what a typical ML workflow looks like. We have the step where we explore and process the data that we are going to use to train the models. This involves retrieving raw data from various sources, cleaning and exploring the data, preparing and transform the data so that it's suitable for machine learning, and this might include steps like feature scaling and coding categorical variables, and splitting data into training and test. Once we have the data ready, we have the modeling phase where we develop and train the model. This is where we select the appropriate algorithm, a regression model, a classification model, maybe [[Neural Networks]], and immediately after training, that's when we validate or evaluate the model. And then of course, the final step in the workflow is where we deploy the model to production, where the trained and validated model runs maybe at an endpoint or in a web service. This is where we'll use the model for real time predictions, and we'll constantly monitor and update the model as well as the data. Our focus is entirely within the modeling phase on the validation step. This is where we'll measure the model's performance, using the right metrics based on the kind of model that we are working with. This is where we validate the model's ability to generalize to unseen data. This takes us right to the definition of validation, a critical step in the machine learning workflow that occurs after the model has been trained, and it involves evaluating the model's performance
>
> **[1:35](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/validation-in-the-ml-workflow?u=76281980&t=95)** on a separate dataset that was not used during training. This helps assess how well the model can generalize to new unseen data. So why do we validate models? The core focus of validation is on measuring the generalizability or generalization of the model. This ensures that the model performs well on unseen data, which is the ultimate goal for real world applications. This is where we'll identify whether the model has overfit on the data. That is, it's too tailored to the training data. Or underfit on the data. The model is too simple to capture patterns. The validation phase is also where we perform hyperparameter optimization or tuning. This is where we check whether the chosen model and its settings, that is its hyperparameters, are optimal for the given task. This is where we'll compare multiple models to select the best performing one on our data. Validation is useful for robustness and bias detection. This ensures the model is reliable, fair, and robust across diverse data scenarios. This involves testing for robustness to noisy data with adversarial examples or edge cases. The validation phase is where we assess the performance metric that we've chosen to evaluate our model. This is what allows us to ensure that the evaluation aligns with task objectives and that the quality of our dataset supports meaningful conclusions. An important task in the validation phase is to see whether your model has overfit on the training data.
>
> **[3:08](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/validation-in-the-ml-workflow?u=76281980&t=188)** Overfitting occurs when a machine learning model learns the training data too well, including its noise and random fluctuations, leading to poor generalization to new unseen data. The symptoms of overfitting is that the model performs exceptionally well on training data, but poorly on test and validation data, indicating that it has memorized rather than learned patterns from the training data. Model overfitting typically occurs when the model that you've chosen is too complex, that is has too many features or too many parameters relative to the amount of training data that you have. Mitigating overfitting involves techniques like cross validation, simplifying the model, may be reducing features or adding regularization, or increasing the size and diversity of the training dataset. Here is an example of an overfitted model and an underfitted model. Now, the black crosses that you see, they represent the training data. The green crosses are the test data or the validation data. The green dotted line represents the true pattern of the underlying data. The purple line in the form of a curve represents our optimal model, which doesn't perfectly capture the true pattern, but is close. Now, the orange line that you see, that's an overfitted model. Observe that it captures the patterns in the training data overly well, but basically does not generalize well to the test data. The horizontal gray line is an underfitted model
>
> **[4:43](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/validation-in-the-ml-workflow?u=76281980&t=283)** that does not capture the complexities of the data on which it is trained.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Neural Networks]] (1)
> **UI Navigation:** select the (2)
> **Definitions:** is an  (2)
> **Speakers:** - [instructor] (1)

#### [Types of cross-validation](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/types-of-cross-validation?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/types-of-cross-validation?u=76281980&t=1)** - [Instructor] We'll first discuss validation techniques for traditional machine learning models. These can be regression classification or clustering models, and they're typically implemented using [[Decision Trees]], [[Logistic Regression]], [[Linear Regression]], or [[Neural Networks]]. These models are typically validated by splitting your data into training and test data. You split your data into two parts, a training set and a test set. The model learns patterns from the training set and is then evaluated on the unseen test set to estimate its performance on new unseen data. Typically you use about 80% of your data for training and 20% for testing, but this can vary based on the dataset size and characteristics. This technique is simple to implement and computationally inexpensive, making it a popular choice for initial model evaluation. Or you can use cross-validation techniques. This involves dividing your data into multiple folds or subsets. The model is trained on all folds except one which is held out for validation. This process is repeated multiple times with each fold serving as the validation set once. The final performance metric is the average across all folds. Cross-validation provides a more robust estimate of model performance compared to a single train-test split, as it reduces the influence of data variability on the evaluation. Let's understand exactly how cross-validation works. Remember, this is a technique to evaluate a model's performance by splitting the dataset into multiple subsets.
>
> **[1:35](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/types-of-cross-validation?u=76281980&t=95)** You'll train the model on some subsets, and you'll validate it on the remaining subset that you've not trained on, and you'll do this iteratively. Now, it turns out there are many different types of cross-validation. There is regular or plain [[Vanilla]] cross-validation where the dataset is split randomly into training and validation subsets multiple times. The model is trained and evaluated repeatedly on different random splits, and the [[Performance Metrics]] are averaged to provide a general estimate. Then there is K-fold cross-validation, where the dataset is divided into K approximately equal sized folds. In each iteration, one fold is used as a validation set, and the remaining K minus one folds are used for training. The process is repeated K times, and the average performance across all folds gives a reliable estimate of model generalization. Stratified cross-validation is similar to K-fold cross-validation, but ensures that each fold maintains the same class distribution as the original dataset. This is especially useful for imbalanced datasets as it provides a fair representation of all classes in the training and validation sets. Nested cross-validation is used for multi-selection and hyper parameter tuning with two loops of cross-validation. There is an outer loop that evaluates the model's performance and an inner loop that optimizes the model's hyper parameters. This prevents overfitting during hyper parameter tuning and provides an unbiased estimate of generalization ability. The leave-one-out cross-validation is a special case
>
> **[3:12](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/types-of-cross-validation?u=76281980&t=192)** of K-fold cross-validation where K is equal to the number of data points. So your validation dataset is essentially just one data point. For every iteration, you use one data point as the validation set and the [[Representational State Transfer (REST)|rest]] of the data for training. It's computationally expensive, especially if you have large datasets. But if you have a small dataset, this maximizes the training data. And then we have hold-out validation, which is basically the same thing as the train-test split. So your data is split into two subsets, a large training set, and a smaller held out validation set. The model is trained on the training set and evaluated on the held out set. This is often used as an additional validation step or in conjunction with cross-validation to confirm how well the model generalizes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Decision Trees]] (1), [[Logistic Regression]] (1), [[Linear Regression]] (1), [[Neural Networks]] (1), [[Vanilla]] (1)
> **Definitions:** is a  (2), is an  (1)
> **Speakers:** - [instructor] (1)

#### [Regular and k-fold cross-validation](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/regular-and-k-fold-cross-validation?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/regular-and-k-fold-cross-validation?u=76281980&t=1)** - [Instructor] Let's discuss the first four in more detail, and the remaining two are variations of the first four. Let's start with regular cross-validation where data is randomly split into two subsets. A training set used to train the model and a validation set used to evaluate its performance. This is not done just once. This process is repeated multiple times with different random splits of data in each iteration to ensure a comprehensive evaluation. The model's [[Performance Metrics]] are averaged across all iterations, providing a more reliable estimate of its centralization ability compared to a single trained test split. A very popular cross-validation technique is k-Fold cross-validation. This is a more robust technique for evaluating model performance compared to a simple train test split. By dividing the data into multiple folds and iteratively using each fold as a validation set, cross-validation reduces the variance in the performance estimate that can arise from a single potentially biased trained test split. So how does this work? The data is divided into k-Folds of approximately equal size. In each iteration, one fold is held out as the validation set, and the model is trained on the remaining k minus one folds. This process is repeated k times with each fold serving as the validation set exactly once. Thus, every data point is used for both training and validation, providing a more comprehensive evaluation of the model's performance.
>
> **[1:33](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/regular-and-k-fold-cross-validation?u=76281980&t=93)** By averaging the performance metrics across all k-Folds, cross-validation provides a more consistent and stable estimate of the model's centralization ability and mitigates the problem of overfitting on the training data. Now, how do you choose the value of k in k-Fold cross-validation? k is the number of folds that you split your data into. Now this involves a trade off between computational cost and the reliability of the performance estimate. Common values for k include five and 10, which give you a good balance between these two factors. If you're using smaller values of k, say two or three, this leads to faster computation as the model needs to be trained and evaluated fewer times. However, with smaller k, the size of the validation set is larger, which can result in a less reliable performance estimate. Larger values of K, such as say 10 or 20, provide a more robust estimate, but of course, your computational cost of running cross-validation will be higher. Generally, k depends on your size of your data set. For large data sets, smaller values of k may be sufficient to obtain a reliable performance estimate. For smaller data sets, larger values of k may be necessary to reduce the variance in the estimate. Here is an example of how k-fold cross-validation works. Here, my training data has been divided into five folds. This is five-fold cross-validation. In the first iteration, one of these folds will be held out to validate your model, while the remaining four folds will be used to train your model.
>
> **[3:06](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/regular-and-k-fold-cross-validation?u=76281980&t=186)** In the next iteration, a different fold will be used for validation, and the remaining folds will be used to train your model. And in this manner for five-fold cross-validation, there are a total of five iterations and observe that every fold is the validation set exactly once.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Performance Metrics]] (2)
> **Definitions:** is a  (1), is an  (1)
> **Cross-References:** in the next (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Stratified cross-validation and nested cross-validation](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/stratified-cross-validation-and-nested-cross-validation?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/stratified-cross-validation-and-nested-cross-validation?u=76281980&t=1)** - [Narrator] Next let's discuss how stratified cross-validation works. This is a variation of k-fold cross-validation that is particularly useful for imbalanced datasets where the distribution of classes is uneven. In stratified cross-validation, the folds are created in such a manner that the class distribution in each fold is approximately the same as the overall class distribution in the entire dataset. Maintaining the class distribution across folds ensures that the model is trained and evaluated on a representative sample of each class. This is important for imbalanced datasets as random splits may result in folds with a disproportionate number of samples from one class, and this could lead to a biased model. Now if you use stratified cross-validation, this provides a fairer evaluation compared to random splits, especially when dealing with classification problems where accurate predictions of records that belong to the minority class is very, very important. Stratified cross-validation helps prevent the model from being overly influenced by the records that belong to the majority class. Now let's say for a classification problem, this is what the original population looks like. The data points are evenly divided into four classes. Now stratified sampling ensures that the sample is representative of the original population. So if the original population has an equal number of the four categories of data points, the stratified sample will have an equal number
>
> **[1:35](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/stratified-cross-validation-and-nested-cross-validation?u=76281980&t=95)** of the four categories. A stratified sample is thus a representative of the original population. Next let's discuss nested cross-validation where we have two loops running. It's best to visualize this. Here's what the process of nested cross-validation looks like. Here we have the complete dataset that's initially available for training, validation, and testing. The outer loop of nested cross-validation divides the dataset into multiple folds. In each iteration of the outer loop, one fold is reserved as the test set, while the remaining data is used for training and validation. This ensures that the final test evaluation is performed on data that the model has never seen during training or tuning. The training and validation subset from the outer loop is further split into folds in the inner loop. In the inner loop, cross-validation is applied to finetune the model's hyperparameters. Each fold in the inner loop alternates between being the validation set and part of the training set. This ensures optimal hyperparameter selection without overfitting the data.

> [!info]- Semantic Content
>
> **Definitions:** is a  (1)
> **Best Practices:** it's best to (1)
> **Speakers:** - [narrator] (1)

#### [K-fold cross-validation](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/k-fold-cross-validation?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/k-fold-cross-validation?u=76281980&t=1)** - [Instructor] At this point, you have a conceptual understanding of cross validation. In this demo, we'll see how it works in practice using the scikit-learn library, which offers built-in classes for k-fold cross validation. I'll first set up the imports for all of the different libraries that I'll use, [[Pandas (Software)|pandas]], numpy, and scikit-learn, and matplotlib. The dataset that I'll work with is the bank_churn_prediction dataset that I've downloaded from Kaggle. The original link to the dataset is available in the notebook that I have shared with you. Observe that we have a number of different features here, and we'll use these features of customers, things like their credit score, gender, age, tenure, balance, et cetera to predict whether the customer churned. That is the target variable in the exited column. Let's do some basic cleaning and pre-processing of our data. When I run the df.isnull().sum() function, you can see there are a few records with null values in some fields. Since there are very few null values, we can just go ahead and drop those records. And let's run df.isnull().sum() once again to verify that we have no null values in our data. The columns in this dataset contain a few features that are irrelevant, that do not have predictive power, RowNumber, CustomerId, and the Surname of the customer, well, those don't really help predict whether a customer churns or not. I'm going to go ahead and drop those features. This will leave us with features that we consider relevant for our classification model, CreditScore, Geography, Tenure, Balance, number of [[Microsoft Products|products]] bought,
>
> **[1:34](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/k-fold-cross-validation?u=76281980&t=94)** whether they have a credit card or not, and so on. The next step is for us to encode the categorical features in this data. We'll start with the Gender column. The possible values for gender in this dataset are male and female, so I'm going to use label encoding to represent these values using 0 and 1. A Gender value of 0 represents female, and a value of 1 represents male. We can use label encoding because this dataset does not contain other gender categories. The only other categorical feature in our dataset is Geography. I'm going to use one hot encoding to encode this categorical value, the pd.get_dummies function in pandas will allow me to perform one hot encoding. I specify Geography as the column, pass in the data frame, drop_first=True will drop the column corresponding to one of the Geography categories. So the three possible values for Geography, France, Spain, and Germany, will be represented using two columns. And if you look at the resulting data frame, notice we have two new columns, Geography_Germany and Geography_Spain. If you look at the first record, this has a value of 0 for both Geography_Germany and Geography_Spain, this indicates that that particular customer is from France. Let's split our data into features and target. X contains the features that we use to train the model, and y will contain the target, or the class that we are trying to predict. Now, before we perform any machine-learning operation, let's call y.value_counts()
>
> **[3:09](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/k-fold-cross-validation?u=76281980&t=189)** to understand the nature of this data set. Notice that this is an imbalanced data set. For majority of the customers, nearly 8,000 customers have not exited. Exited is equal to 0. Only about 2,000 customers have churned, with Exited value equal to 1. While selecting a classification model to train on this data, I need to keep this in mind. Let's quickly check the data types of our features. Observe that we are now left with numeric features. Gender and Geography, those are the only non-numeric features. We'll prepare our numeric features for machine-learning by standardizing them. I have my numeric columns on line one, I instantiate the StandardScaler on line three, and I call fit_transform to transform the data. I've kind of taken a shortcut here to just transform the entire dataset. Normally, you'd fit the scaler only on the training data, and use the mean and standard deviation of the training data to transform the test data. Because this is a simple demo, I've transformed the entire data in one go. Now with our transformed data set, let's now split into training data and test data. train_test_split will perform this split for us, with a test size of 0.2, we use 20% of the data to evaluate our model that is our test set. stratify=y makes this a stratified split, meaning the proportion of categories, or classes, in the churn column, that is the Exited column, will be maintained across the training set and the test set.
>
> **[4:45](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/k-fold-cross-validation?u=76281980&t=285)** This gives us around 8,000 records to train the model and 2,000 records to test our model. Before we perform cross validation, I'm going to train a baseline RandomForestClassifier, and when I instantiate the model, I set class_weight='balanced' This adjusts the weight of each class based on their frequencies in the training data. So if you have imbalanced classes, the model will give more weight to the minority class to prevent bias towards the majority class. And that's exactly what we want. On line two, we fit on the training data. And on line four, we make predictions on the test data. We then compute four scores on the model, accuracy, precision, recall, and f1 score, and we print those out to screen. And here you can see that the model is okay, the accuracy, that is the number of predictions the model got correct, is high, almost 86%, 0.859. The precision of the model is high too. Of the predictions of churn that the model made, 75% of 0.75 are correct. The recall, however, is low, just 0.46. This means that the model was able to identify only 46% of the customers who churned. And that leads to the low F1 score of 0.57. Now let's train this model using fivefold cross validation. We instantiate the RandomForestClassifier, as before. And on line three, I call cross_val_score, and pass in the random forest model that I want to train, the training data, X_train and y_train, the target. cv=5 indicates that this is fivefold cross validation.
>
> **[6:21](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/k-fold-cross-validation?u=76281980&t=381)** And the scoring function that this training will use is the f1 score. We then print out the F1 score for each fold that's present in the cv_scores_5 variable, and then we print out the mean F1 and the standard deviation of the F1 score. Let's run this, and here you can see the F1 score for each fold. Observe that the F1 scores range from 0.532 to 0.608. And the mean F1 score is about 0.5596. This is a more robust measure of the random forest classification model than the baseline model that we trained earlier. If you remember, we discussed the fact that the value of k in k-fold cross validation is a trade off between computational cost and the reliability of the performance estimate. Common values for k tend to be between 5 and 10. Here, I'm using k values between 2 and 10, and I'm going to perform cross validation using all these k values. On lines three and four, I've initialized lists to store the mean F1 scores and the standard deviation of the F1 scores for all these k values. On line six, I run a for loop to iterate through each k, instantiate a random forest model, perform cross validation, this is on lines 9 through 11, and calculate the mean F1 score and the standard deviation of the F1 scores for each value of k. I then print those out to screen on lines 15 and 16. And here you can see the performance of the model for different values of k.
>
> **[7:56](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/k-fold-cross-validation?u=76281980&t=476)** This is actually better visualized in the form of a plot. Here is a visualization that shows you the stability of the F1 score based on the value of k in k-fold cross validation. We have the number of cross validation folds on the x-axis, and the mean F1 score on the y-axis. You can see that, overall, the mean F1 score is fairly steady for different values of k. The vertical error bars are larger for larger values of k, from k 7 through 10, indicating that the model's performance has higher variance across different validation sets for higher values of k. But overall, the model's performance is fairly consistent.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Pandas (Software)|Pandas]] (2), [[Microsoft Products|Products]] (1)
> **Code Identifiers:** bank_churn_prediction (1), get_dummies (1), drop_first (1), value_counts (1), fit_transform (1)
> **Versions:** 0.2 (1), 0.859 (1), 0.75 (1), 0.46 (1), 0.57 (1)
> **Definitions:** is a  (4), is an  (1), means that (1)
> **CLI Commands:** make (1)
> **Cross-References:** we discussed (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Validating LLMs](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/validating-llms?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/validating-llms?u=76281980&t=1)** - [Instructor] The most interesting and exciting models that we work with today are [[Large Language Models (LLM)|large language models]]. They have the potential to revolutionize various industries by generating high-quality human text, translating languages, and answering questions. But they can also produce outputs that are factually incorrect or nonsensical, and this is referred to as hallucinations. With this in mind, let's talk about why is it necessary to validate these LLMs? LLMs are trained on massive datasets, and that data may contain inaccuracies, biases, and outdated information. A model is only as good as its training data, which means that such models can generate outputs that are factually incorrect or misleading. And to address this, we need to fix the quality of the training data. Now, we've all worked with LLMs, and we know that they can sometimes hallucinate information, meaning they generate outputs that seem plausible but are not supported by the training data. This can occur when the model tries to fill in gaps in its knowledge or when it's presented with ambiguous or incomplete prompts. This can be mitigated by grounding models using techniques such as [[Retrieval-Augmented Generation (RAG)|retrieval-augmented generation]], or RAG, or using robust validation. Human language is complex and nuanced, which means it can be difficult for LLMs to consistently produce accurate and reliable outputs. All of this put together means that it's dangerous to use unvalidated large language models in any kind of real-world production environment.
>
> **[1:37](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/validating-llms?u=76281980&t=97)** Human beings are biased, and these biases are reflected in the training data used to train these models. LLMs may inherit societal biases, leading to outputs that perpetuate stereotypes and unfair associations. The biases may be subtle but can have a significant impact, especially in applications like hiring or [[Decision-Making]]. There are many implications of biased models. They can harm marginalized groups. People lose trust in models that are not fair and basically won't adopt them. If you have biased hiring recommendations that perpetuate stereotypes, well, no one's going to be using it. LLMs are also capable of generating harmful content, such as hate speech or misinformation, and can be exploited for malicious purposes. LLMs today are being widely used to spread propaganda or adversarial attacks. There are several categories of techniques that you can use to validate these LLMs. Offline validation is a broad approach to validation where the LLMs are tested using static, pre-collected datasets without [[Real-Time]] interaction. Offline validation is a high-level strategy and can encompass techniques like golden datasets and benchmarking, and we'll talk about those next. Golden datasets provide carefully curated, high-quality, ground truth data that is used for evaluating LLMs. Rather than being a validation technique by itself, golden datasets are a resource that is used for validating LLMs.
>
> **[3:12](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/validating-llms?u=76281980&t=192)** Benchmarking is a technique to compare model performance against predefined standards or other models using specified metrics. The benchmarking process may use resources such as golden datasets, and benchmarking is a key part of offline validation. Benchmarking relies on curated datasets and serves as a specific evaluation method under the offline validation umbrella. AI validating AI is a relatively new technique in validation where an AI system, basically a large language model, is used to evaluate and critique the output of other AI systems. This technique is usually used as a complement to traditional human- and dataset-driven validation by automating some aspects of the evaluation. This technique is not necessarily a replacement for other techniques.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Large Language Models (LLM)|Large language models]] (2), [[Retrieval-Augmented Generation (RAG)|Retrieval-augmented generation]] (1), [[Decision-Making]] (1), [[Real-Time]] (1)
> **Definitions:** is a  (5), means that (2)
> **Analogies:** such as (3)
> **Env Vars:** rag (1)
> **Speakers:** - [instructor] (1)

#### [Offline validation](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/offline-validation?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/offline-validation?u=76281980&t=1)** - [Instructor] Let's discuss each of these in turn, starting with offline validation. This is where a trained model is evaluated using a pre collected static dataset. So the evaluation happens without any [[Real-Time]] interaction or feedback. Essentially simulating real-world scenarios with historical data. Its main feature is the use of a fixed dataset, which is often split into training and testing sets. The primary goal is to assess a model's performance before deploying it live. Here are the main points of offline validation. This involves evaluating a trained model using a pre-collected static dataset without real-time interaction. The fixed dataset is typically split into training and testing sets where the model learns from the training data and is tested on the unseen data to see how well it generalizes. The objective is to be able to identify issues with the model early on in the development process to see whether the model performs well on unseen diverse data. A major advantage is that a static data set gives you a controlled environment for a repeatable testing and consistent evaluation. There are some clear benefits to offline validation. It provides an early indication of how well a model will generalize to new unseen data. By evaluating the model on a held out test set, developers can gain insights into the model's ability to make accurate predictions on data it hasn't encountered during training. Offline validation can help detect
>
> **[1:33](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/offline-validation?u=76281980&t=93)** and mitigate potential biases in the model potential. By analyzing the model's performance on different subsets of the data, developers can identify if the model is making unfair or discriminatory predictions based on certain features or attributes. Offline validation tends to be much more cost-effective as compared to live testing, so it's a way to detect and identify potential problems early in the model development phase. Here is a quick comparison of offline validation versus online validation. Notice that offline validation works on pre-collected historical data. There is no direct user interaction, evaluation based on predefined metrics, and it's for initial assessment of the model and for risk mitigation. Online validation, on the other hand, uses real-time live data and is in the dynamic real-world environment. Users interact with the model directly, and the model evaluation is based on user feedback and observed behavior of the model. Online validation is used for monitoring performance optimization and A/B testing of models. So when would you use offline validation? It's often used for model benchmarking when you want to compare the performance of different models or [[Algorithms]] on a standardized dataset. Offline validation can be used for algorithm selection as well when you evaluate different algorithms to determine the best fit for a particular task. Offline validation is often used for hyper parameter tuning, where you optimize the parameters of a model
>
> **[3:06](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/offline-validation?u=76281980&t=186)** to improve its performance, the design parameters of a model. You can use it to test specific scenarios, so you want to evaluate the model's performance on three defined scenarios or edge cases. And finally, it's used for an initial assessment of the model before deploying it to a live environment.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Real-Time]] (3), [[Algorithms]] (2)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Golden datasets](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/golden-datasets?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/golden-datasets?u=76281980&t=1)** - [Instructor] Next, let's talk about golden datasets. These are high-quality datasets that have been carefully curated and validated to serve as a ground truth for evaluating and debugging AI/ML models. Golden datasets represent the ideal standard against which model performance is measured. They're typically hand-labeled and they contain diverse, accurate, and unbiased samples to ensure robust validation across different scenarios. When working with LLMs, golden datasets are not used as the primary source of data for training or fine-tuning a model. Instead, they're used to assess the quality of the answers generated by the LLM. So you'd send questions to the LLM and compare the LLM's answers to the expert answers in the golden dataset. Creating a golden dataset is not easy. You have to gather the relevant data from diverse and authoritative sources and make sure that there is no low-quality data to maintain your dataset's integrity. You may have to make use of domain experts to assign accurate labels or annotations on the data, and you need an additional team to cross-verify annotations to ensure that they're correct. After all the pre-processing and the labeling of the golden dataset, you need to combine and curate the annotated data into a cohesive dataset that others can use. Once you've done that, you'll test and evaluate the golden dataset. You'll use it to evaluate model performance
>
> **[1:34](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/golden-datasets?u=76281980&t=94)** and compare results against established standards or previous models to assess improvements. To summarize, golden datasets are high-quality, meticulously curated datasets that serve as the ground truth for evaluating and debugging models. They tend to be hard to create. They are hand-labeled and contain diverse, accurate, and unbiased samples. These datasets are not used for training or fine-tuning LLMs, but rather for evaluating the LLMs' outputs. Questions from the dataset can be sent to one or more LLMs, and their answers are compared against expert-labeled answers to assess the quality of these models. Golden datasets are extremely useful though. They help developers track progress of their models, debug issues, and make decisions during model development. They give you objective ways to evaluate if your model is getting better or not.

> [!info]- Semantic Content
>
> **CLI Commands:** make (3)
> **Env Vars:** llm (3)
> **Speakers:** - [instructor] (1)

#### [Benchmarking](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/benchmarking?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/benchmarking?u=76281980&t=1)** - [Instructor] Let's move on and discuss benchmarking. This is a systematic process of evaluating a model's performance against pre-defined standards or competitor models. Models are evaluated on specific tasks in a quantitative manner, rather than qualitatively. Benchmarking involves developing benchmark tasks that simulate real world scenarios and challenges. Models are then evaluated on how well they perform on these tasks. Qualities like fluency, coherence, domain knowledge, and terminology expertise are measured. Benchmarking involves evaluating the performance of [[Large Language Models (LLM)|large language models]] against standardized datasets, such as golden datasets, using metrics to assess their accuracy, efficiency, and robustness. Commonly used metrics for large language models include accuracy, F1 score, perplexity, and latency, which is the time to inference. These provide insights into the model's language understanding, generation quality, and computational efficiency. We'll be discussing these metrics in more detail in just a bit. By using objective metrics, benchmarking enables comparison of LLMs across tasks like natural language understanding, question answering, or multitask settings. It highlights the strengths and weaknesses of models for specific applications. Benchmarking thus provides a standardized repeatable framework for developers and researchers to assess progress and select the best-performing model for a task.
>
> **[1:34](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/benchmarking?u=76281980&t=94)** Here are some common metrics used for evaluating large language models. We'll dive into some of these in more detail in a bit. Accuracy measures the proportion of correct predictions made by the model out of the total predictions. This is commonly used in classification and question answering tasks. The F1 score is the harmonic mean of precision and recall, and provides a balanced evaluation of the model's ability to correctly identify both positive and negative cases. Precision identifies how much of the positive category a model gets correct, and recall identifies how much of the positive category the model was able to correctly identify from the underlying data. Perplexity evaluates how well a language model predicts a sequence of words. Lower perplexity indicates better performance in generating coherent text. BEU scores where BLEU stands for Bilingual Evaluation Understudy measures the quality of text generation by comparing model generated text with reference text. This is commonly used in machine translation. Latency or inference time is the time it takes for the model to generate a response or make a prediction. As you might imagine, this is critical for [[Real-Time]] applications and just for a good [[User Experience (UX)|user experience]]. Here are some common benchmark datasets used for LLM evaluation. SQuAD stands for the Stanford Question Answering Dataset. This is a reading comprehension dataset where models must answer questions based on a given passage of text. SuperGLUE, where GLUE is an acronym
>
> **[3:08](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/benchmarking?u=76281980&t=188)** for General Language Understanding Evaluation, is a suite of challenging [[Natural Language Processing (NLP)|natural language processing]] tasks for evaluating general language understanding, tasks like entailment, core reference resolution, and common sense reasoning. Entailment determines whether a given hypothesis logically follows from a given premise in natural language inference, and core reference reasoning involves identifying when different expressions in a text refer to the same entity. For example, Alice and then she in the same sentence. MMLU stands for massive Multitask Language Understanding, which is a dataset for assessing multitask performance across different domains like science, humanities, and mathematics. The [[Microsoft]] MARCO dataset, which stands for Microsoft Machine Reading Comprehension, is a large dataset for evaluating models on [[Information Retrieval]] and passage ranking tasks. Common Crawl is a massive web crawl dataset often used for pre-training and benchmarking LLMs for general text understanding and generation. All of the large language models that are in use today have been benchmarked on these datasets, and here, you can see how they've performed across these benchmarks. Notice that [[GPT-4]] evaluated on SQuAD performs quite well with an accuracy and F1 score of 88% and above, but notice its inference time, 220 milliseconds. PaLM 2, an earlier version of [[Google]]'s [[Gemini]] model, did reasonably well on the SQuAD dataset,
>
> **[4:43](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/benchmarking?u=76281980&t=283)** not as well as GPT-4, and its inference time is high too, 140 milliseconds.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Large Language Models (LLM)|Large language models]] (4), [[Microsoft]] (2), [[GPT-4]] (2), [[Real-Time]] (1), [[User Experience (UX)|User experience]] (1)
> **Definitions:** is a  (6), stands for (4), is an  (1)
> **Env Vars:** gpt (2), beu (1), bleu (1), llm (1), glue (1)
> **Analogies:** such as (1), imagine (1), for example (1)
> **CLI Commands:** make (1)
> **UI Navigation:** select the (1)
> **Speakers:** - [instructor] (1)

#### [AI validating AI](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/ai-validating-ai?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/ai-validating-ai?u=76281980&t=1)** - [Instructor] Now, let's talk more about AI validating AI. This refers to the process of using [[Artificial Intelligence (AI)|artificial intelligence]] models or systems to validate, evaluate, or debug other AI models. And if you think about it, it is quite ingenious because this approach automates many aspects of traditional validation tasks, which means increased efficiency and ability to handle large scale validation needs. So you can have LLMs used to validate the responses of other LLMs, or specialized AI models that can be used to assess the outputs of, say, [[Computer Vision]] systems. As you might imagine, there are benefits as well as risks associated with this approach. This approach is scalable. AI validation excels at handling large scale validation tasks efficiently, particularly important in domains where vast amounts of data need to be processed and analyzed, such as image recognition or [[Natural Language Processing (NLP)|natural language processing]]. By getting rid of human subjectivity, AI validation ensures that uniform evaluation criteria are applied consistently across AI models being assessed. Once again, this is obvious. AI validation performs validation tasks much faster than manual approaches, which means your development and deployment cycle will be accelerated. By reducing the reliance on manual labor for validation, AI validation can significantly lower costs associated with AI development and deployment. As with everything, there is a trade off involved. Here are the risks associated with this approach, the risk of compounding errors.
>
> **[1:33](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/ai-validating-ai?u=76281980&t=93)** If the AI model responsible for validation has inherent flaws or biases, it's pretty obvious that these biases will be propagated to the new model being trained. We know that complex AI models tend to be black-boxes. [[Neural Networks]] struggle to explain their validation decisions in a way that is easily understandable to humans. And this lack of transparency can make it challenging to identify the root cause of validation failures, and of course, the most obvious risk or challenge where you get the reliable validation model in the first place. Your validation model needs to be high quality and needs to be trained on diverse and representative datasets, thoroughly tested before it can be used for validation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Artificial Intelligence (AI)|Artificial intelligence]] (1), [[Computer Vision]] (1), [[Natural Language Processing (NLP)|Natural language processing]] (1), [[Neural Networks]] (1)
> **Definitions:** refers to (1), is a  (1)
> **Analogies:** imagine (1), such as (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)


### 2. Evaluating ML Models and LLMs

[↑ Back to Table of Contents](#table-of-contents)

#### [Evaluating models using metrics](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-models-using-metrics?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-models-using-metrics?u=76281980&t=1)** - [Instructor] Moving on from validation, let's turn our attention to some of the metrics that you'll use to evaluate models, starting with traditional models. We'll discuss three broad tasks, which typically use traditional machine learning models. Regression, which predicts a continuous numerical value. Home price or temperature based on input features by modeling relationships between variables. Classification tasks categorizes input data into predefined classes or labels. Spam versus non-spam emails based on patterns learned from the label data. And finally, clustering, groups unlabeled data into clusters based on similarities of data points and identifies hidden structures or patterns without predefined labels. Here, regression and classification are examples of [[Supervised Learning]] techniques. Both require label data. While clustering is an example of an unsupervised learning technique, the training data is not labeled. Each of these different kinds of models have their own set of metrics. Now we use these metrics to validate models. Model evaluation will help us determine how well a model generalizes to unseen data. This is what allows us to gain confidence in our model's ability to make accurate predictions in the real world. Which metric would you pick for a model? Well, there are different strokes for different folks. Different model types require different evaluation metrics. For example, regression models are evaluated using metrics like R-Squared,
>
> **[1:33](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-models-using-metrics?u=76281980&t=93)** while classification models utilize metrics such as precision recall and F1 score. Our objective here is to bridge the gap between model development and deployment. By evaluating a model, we can identify any potential issues or limitations before deploying it to production.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Supervised Learning]] (1)
> **Analogies:** for example (1), such as (1)
> **CLI Commands:** make (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Evaluating regression models](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-regression-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-regression-models?u=76281980&t=1)** - [Instructor] The metric that's most commonly used to evaluate regression models is R square. The R square score measures how well a regression model explains the variability in the target variable. How well does the model capture trends in the data? R square values lie in the range, 0 to 1. A value of 0 means the model doesn't explain any variability in the target variable, what it's trying to predict. 1 means the model perfectly explains the variability in the data. It's possible for you to have negative values of R square if the model is very, very poor. Another thing to keep in mind is that a higher R square score indicates a better fit, but a higher R square score does not guarantee a good model. You must consider other details, like whether the model is overfit on the data. Now, why might your R square score be negative? If your model is so bad that it's worse than simply predicting the average of the target variable, the R square value will be negative. Now, there are several reasons why the R square might be negative. If you picked irrelevant or misleading features in the training data, the predictions might be inaccurate. A negative R square might indicate that you've chosen the wrong kind of model. Let's say you've picked a linear model when the data itself is non-linear, that may lead to poor predictions. If there are extreme outliers in your data, this can increase the error of your model, and may lead to a negative R square.
>
> **[1:35](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-regression-models?u=76281980&t=95)** In short, if the R square is negative, it means the model is worse than predicting the mean, and it's effectively useless, and you need to reevaluate. Now, I'd mentioned the fact that a high R square can indicate that the model is overfit on the data. Now, how do you figure this out? Now, consider this data on the x and y axis. Here, the x-axis represents the features of the model, and the y-axis represents the target variable, and there is a clear pattern to this data. Now, if your regression model can be represented by this horizontal line, well, that's basically predicting the mean. If the predictions of your model are worse than the mean, your model will have a negative R square. Now, here is an idea of a good model. This model accurately captures the trend and generalizes well, leading to a higher R square score. Clearly, the model represented by this purple line does a lot better than just predicting the mean. Now here is an example of an overfit model. This model will have a deceptively high R square, because it overfits the noise in the data, but generalizes poorly to new data points that have the same trend as the original data, but are not exactly the same.

> [!info]- Semantic Content
>
> **Definitions:** is an  (2), is a  (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [Evaluating classification models](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-classification-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-classification-models?u=76281980&t=1)** - [Instructor] Let's now talk about classification models. There are four main metrics used to evaluate these models. Our discussion will be in terms of binary classification models, but we can extend these to multi-class classification as well. The first is accuracy. This is the percentage of correct predictions made by the model across all predictions made. While simple, this metric may not always be reliable, especially if your data set is imbalanced. Another metric is precision. This measures how many of the predictive positive instances were actually positive. A higher precision score is important when false positives tend to be expensive, like in spam detection. Another metric is a recall, also often referred to as the sensitivity of a model. This measures how many of the actual positive instances in the underlying data were correctly identified by the model. This is crucial when missing a positive instance is critical, such as in medical diagnoses. If you'd like a good balance of precision and recall for your model, you choose the F1 score as a metric. This is the harmonic mean of precision and recall, and it balances both metrics. This is often the metric used for imbalance data sets. When evaluating classification models, you often make use of the confusion matrix, which is a table used to evaluate the performance of these models by comparing predicted classifications from the model with actual ground truth values. This table allows us to easily compute
>
> **[1:34](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-classification-models?u=76281980&t=94)** the accuracy, precision recall, and the F1 score of models. Here is what the confusion matrix might look like for a particular classification model. Let's say the model is a binary classifier detecting whether an incoming email is spam or not. The rules in this table represent ground truth values, actual positive spam identifications, and actual negative spam identifications. The columns in this table represent the predictions from the model, predicted positive spam and predicted negative spam. The actual positive values which were predicted positive are the true positive predictions of the model. These are represented by TP. The actual negative values from the data, which were predicted negative are true negatives, or TN. The predicted negative from the model which were actually positive, these are false negatives, or FN, and the predicted positive from the model which were actually negative are false positives, or FP. To compute the precision of the model, you'd look at the first column. The true positive divided by the total number of positive predictions will give you prediction. To compute recall, you'd look at the actual positive row. The number of predicted positives divided by the total number of actual positives will give you the recall score. The accuracy of the model uses the mean diagonal, all of the correct predictions from the model that is in the mean diagonal, divided by the total number of predictions. In summary, precision measures
>
> **[3:08](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-classification-models?u=76281980&t=188)** how many predicted positives are actually positive. That is true positives divided by true positives plus false positives. A higher precision means fewer false positives. Recall measures how many actual positives were predicted by the model. These are the true positives divided by the sum of true positives, plus false negatives. And there is a trade off here. Increasing the precision typically reduces recall and vice versa. So if you make the model stricter about predicting positive values to get a higher precision score, you might miss some true positives. that is, get a lower recall. Loosening the model threshold can capture more positives, but increase false positives and result in lower precision. There is no right answer about how strict the model ought to be. The optimal balance depends on the problem. For example, in fraud detection, recall might be more important to catch as many fraudulent transactions as possible, even at the cost of some false positives.

> [!info]- Semantic Content
>
> **Definitions:** is a  (4)
> **CLI Commands:** make (2)
> **Analogies:** such as (1), for example (1)
> **Speakers:** - [instructor] (1)

#### [Evaluating clustering models](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-clustering-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-clustering-models?u=76281980&t=1)** - [Instructor] Let's move on to discuss clustering where we try to group similar data points in the same cluster. A common metric used to evaluate clustering models is the silhouette score. The silhouette score ranges from -1 to +1. A score of +1 indicates that the object is well-matched to its own cluster and poorly matched to neighboring clusters. A score of 0 suggests that the object is on or very close to the decision boundary between two neighboring clusters. A score of -1 indicates that the object is probably assigned to the wrong cluster. The silhouette score can thus be used to assess the quality of clustering by helping assess the appropriateness of the chosen number of clusters. A higher silhouette score generally indicates better defined clusters with good separation between the cluster data points. You can visualize silhouette scores using a silhouette plot. In this particular visualization, there are three clusters represented by the different colors, every bar here in this visualization, the horizontal bars, represents an observation, and on the X-axis, we have the silhouette score for the individual data points. The observations in cluster 2 in orange indicate that the points in this cluster have relatively high silhouette score. This is indicative of good clustering. The purple cluster, cluster 1, the observations in this cluster show a slightly lower silhouette score than cluster 2, which may indicate that they're somewhat less well-clustered.
>
> **[1:33](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-clustering-models?u=76281980&t=93)** Now, cluster 3 in green, notice the observations in this cluster have a broader distribution of silhouette scores, with some silhouette scores being negative. The value of 0.553 shown at the top of the plot is the average silhouette score for all the observations in the dataset across all three clusters.

> [!info]- Semantic Content
>
> **Versions:** 0.553 (1)
> **Speakers:** - [instructor] (1)

#### [Accuracy precision recall](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/accuracy-precision-recall?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/accuracy-precision-recall?u=76281980&t=1)** - [Instructor] In this demo, we'll train a classification model and evaluate it using accuracy, precision, and recall. And we'll see that a model with high accuracy is not necessarily a good model. Go ahead and import all of the libraries that we need. Once again, we'll be using scikit-learn to train our model. This dataset that we'll use is an advertising dataset that I downloaded from Kaggle, and I've already split this data into training data and test data. Let's take a look at the training data, and you can see that the dataset is fairly simple. We'll use features such as daily time spent on site, age of the customer, area income, daily internet usage, and whether the customer is male or not, to predict whether the customer clicked on a particular ad. Now, once again, this is a very imbalanced data set. If you look at the click counts, you can see that a majority of customers, 602, did not click on an ad, and very few customers, 148, did click on an ad. The imbalanced nature of this dataset becomes more [[Stark (Accessibility Software)|stark]] when you visualize this using a bar plot. Notice that the vast majority of records are customers who did not click on an ad. If you were to look at the test data, you'll find the same imbalance there for customers who did or did not click on an ad. Now let's split our dataset into features and labels. x_train contains the features, and y_train contains the label that we are trying to predict whether a customer clicked on an ad. And we'll do the same for the test data as well.
>
> **[1:34](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/accuracy-precision-recall?u=76281980&t=94)** All of the columns are numeric or categorically encoded. Let's apply standardization to the numeric columns using a column transformer. This is just a pre-processor that we use to pre-process both the training data as well as the test data. I'm going to instantiate a scikit-learn pipeline that will allow me to incorporate both the pre-processor and the model itself. I have my pre-processor, and the model is a [[Logistic Regression]] model, and I call fit on the training data to train the model. Let's now use this model for predictions and then see how this model performs. Now if you look at the predicted values from the model, something should jump out at you here. Notice that all of the predictions from the model say zero, or the customer did not click on the ad. You know the dataset is imbalanced, and when you see an output like this, you know that this model isn't a very good one. But if you were to only look at the accuracy of the model, it's extremely high at 81.2%. The accuracy alone does not give us an idea of whether this model actually performs well or not. You can see that recall and precision scores are both displayed as zero. Precision is zero because it's undefined. There are no positive predictions, no predictions that the user clicked on an ad. Recall is zero again, because there are no positive predictions. Now, the reason for this is that there are not enough training samples of records where the customer has clicked on an ad. Now, one way to mitigate this is by oversampling the records
>
> **[3:10](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/accuracy-precision-recall?u=76281980&t=190)** that belong to the minority class. Now, what I've done here is filtered out the records of individuals who did not click on an ad and individuals who did click on an ad. The majority size here is the number of records we have for customers who did not click on an ad. We then use resample to oversample the records of the minority class. Resample will oversample the records in the clicked-on-ad data frame, replace equal to true indicate sampling with replacement, that is records that have been sampled once may be sampled again. End samples equal to majority size will ensure that we have as many records of the minority class as we do for the majority class. And if you look at the shape of the data sets, you can see that our oversampled, clicked-on-ad dataset now has 602 records, exactly equal to the number of records of individuals that did not click on an ad. I then use pd.concat to create a balanced dataset. The individuals who clicked on an ad, we use the oversampled data set, and we'll work with the original data for individuals who did not click on an ad. And now, if you look at the count of records for the individual classes, you can see that the data set is balanced. The 602 records that we have for the minority class, Clicked on Ad equal to one, that's because of oversampling the minority data. We'll now train our model on this balanced dataset and see whether our model is able to identify the minority class better this time around.
>
> **[4:44](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/accuracy-precision-recall?u=76281980&t=284)** We'll set up a pipeline, once again called pipeline.fit for model training, and let's call pipeline.predict and look at the y-paired values. This time around, observe that you see a few ones. The model was able to successfully predict at least a few customers who did click on an ad. Now let's recompute the accuracy, precision, and recall of the model. Notice that the accuracy score has fallen. The model overall gets many more predictions wrong. The precision score is low, 0.21, so when the model predicts that an individual has clicked on an ad, it's right just 20% of the time. The recall score is pretty decent. Of all of the individuals who did click on an ad, about 50% were identified. So, we have this model here, which has many false positives, but if false positives don't matter as much in your use case, this model is better than the previous one, which predicted no customers clicking on the ad.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Stark (Accessibility Software)|Stark]] (1), [[Logistic Regression]] (1)
> **UI Navigation:** click on (12)
> **Definitions:** is a  (2), is an  (1)
> **Code Identifiers:** x_train (1), y_train (1)
> **Versions:** 81.2 (1), 0.21 (1)
> **CLI Commands:** find (1)
> **Analogies:** such as (1)
> **Prerequisites:** set up (1)

#### [Evaluating large language models (LLMs)](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-large-language-models-llms?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-large-language-models-llms?u=76281980&t=1)** - [Instructor] We've discussed the metrics that we'd use to evaluate traditional machine-learning models. How would we evaluate [[Large Language Models (LLM)|large language models]]? But first, let's talk about why evaluation of LLMs is even necessary. LLMs are increasing the used in applications that directly impact users, such as chat bots, [[Content Creation]] tools, and even medical diagnosis systems. This means it's very important to ensure that the outputs of these models are accurate, relevant to what the user wants, and safe for end users. This is especially important because LLMs can be used to influence opinions, disseminate information, and even spread propaganda. We've already discussed that LLMs are prone to hallucinations. They can sometimes generate incorrect or nonsensical outputs, and they can exhibit biases present in the vast data sets that they are trained on. Evaluating LLMs helps detect these issues, such as hallucinations, biases, or inconsistencies in responses. Evaluation also helps align model performance with application-specific goals. LLMs are used in a wide range of applications, and each application has its own specific requirements and goals, and it's important that model performance meet these goals. For example, when LLM is used for medical diagnosis, that might need a different evaluation approach compared to an LLM used for creative writing. Since LLMs are different from other models that we've discussed so far, evaluating them is fairly unique, and it's a fairly new problem as well.
>
> **[1:36](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-large-language-models-llms?u=76281980&t=96)** Traditional models focus on predictions, such as predicting stock prices or classifying images. LLM evaluation emphasizes textual quality, coherence, and factual correctness. We want LLMs to be fluent, their answers to be relevant, and they need to have the ability to generate human-like text. While traditional model evaluation often relies heavily on quantitative metrics, LLM evaluation requires a combination of both quantitative metrics, such as blue scores and perplexity, and qualitative assessments, human evaluation. This is because the quality of text is often subjective and cannot be fully captured by numbers alone. Quantitative metrics are useful because they provide measurable evidence of performance, while qualitative assessments offer a deeper understanding of the model's capabilities. LLM evaluations are about striking a balance between task-specific accuracy and general user satisfaction. Accuracy is important, but it's also important to consider how well the model meets user expectations, and whether it provides a positive experience. This involves evaluating aspects like, "How well does the model understand the user intent?" "Does it generate engaging responses?" "Does it adapt to different conversational styles?" As you might imagine, LLM evaluation is much harder than traditional model evaluation, because there is no single ground truth to compare the output with. Unlike tasks which have clear-cut answers, LLM outputs lack this ground truth.
>
> **[3:11](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-large-language-models-llms?u=76281980&t=191)** This makes the evaluation subjective, and different evaluators may have different opinions on what is exactly a good response and how it looks. There is no one correct answer, and this makes evaluation complicated. Even though we have evaluation metrics, they can be inconsistent across different tasks and domains. So a metric that you'd use with the chatbot may not be suitable for evaluating machine translation. LLM evaluations tend to be resource-intensive because they need to have a human angle. Human-in-the-loop evaluations, while often considered the gold standard, are very resource-intensive and time-consuming, and it's really hard to scale human evaluation of models, which means you have to explore many alternative evaluation techniques and consider the trade off across these. Here are some common evaluation metrics. We have accuracy based metrics, which measure how well the model generates factually correct and relevant responses. This includes exact match accuracy, F1 score, and blue score. I'm going to be using a number of different terms here, don't worry, I'll explain each of these in a bit. Then we have fluency metrics. These metrics assess how natural the generated text sounds. This can include grammatical correctness and coherence. Metrics that fall in this category include perplexity and rouge scores. We then have bias metrics,
>
> **[4:43](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/evaluating-large-language-models-llms?u=76281980&t=283)** which evaluate the presence of biases in the model outputs, such as gender bias or racial bias. There are diversity metrics, which measure the variety and originality of the generated text. And coherence metrics that assess how well the generated text flows and maintains a logical structure.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Large Language Models (LLM)|Large language models]] (1), [[Content Creation]] (1)
> **Env Vars:** llm (8)
> **Analogies:** such as (5), for example (1), imagine (1)
> **Speakers:** - [instructor] (1)

#### [Human evaluation](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/human-evaluation?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/human-evaluation?u=76281980&t=0)** - [Instructor] So what are the techniques that you'd use to evaluate LLM's? We have human evaluation where there are actual real people reading the output of LLM's and grading its responses. We have statistical evaluation, which refers to the use of quantitative measures such as accuracy, precision, F1 score, to evaluate the performance of LLM's by analyzing their output against predefined criteria or data sets. And then we have model-based evaluation that analyzes the internal workings of the model, including how well it generalizes to unseen data, how it handles different kinds of inputs, and its overall robustness and adaptability in different contexts and scenarios. Now, human evaluation is by far the most time consuming and resource intensive, but is an important step at generating good models because really there's no feedback like human feedback, however, subjective it is. Here are some techniques used in human evaluation of models. The first is pairwise comparison, and you might have actually seen this when you work with [[ChatGPT]]. This method involves presenting two model outputs to human evaluators, and asking them to choose the better one based on some specific criteria. This helps identify user preferences and compare the relative quality of different models or maybe different versions of the same model. When using ChatGPT I've often been asked which response do I prefer? Well, I'm acting as a human evaluator in that situation. Likert skills are used
>
> **[1:33](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/human-evaluation?u=76281980&t=93)** to rate different aspects of the model's output, such as fluency, coherence, relevance, and overall quality. This gives you a more granular assessment of the model's performance, and allows for a quantitative analysis of human judgment. And then we have expert evaluation, which is useful for LLM's that operate in specialized fields. This involves domain experts to assess the quality and accuracy of the model's output. Experts can provide valuable insights and identify errors that may not be apparent to non-experts. If you're using a model for medical diagnosis or in the legal field, you need domain experts.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[ChatGPT]] (2)
> **Env Vars:** llm (4)
> **Definitions:** refers to (1), is an  (1)
> **Analogies:** such as (2)
> **Speakers:** - [instructor] (1)

#### [Statistical methods for LLM evaluation](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/statistical-methods-for-llm-evaluation?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/statistical-methods-for-llm-evaluation?u=76281980&t=1)** - [Narrator] Another broad category of techniques to evaluate LLMs is statistical evaluation. Statistical metrics provide a way to objectively assess the performance of LLMs by quantifying how well they perform on specific tasks, such as classification, generation of text, or prediction. Now, the good thing about statistical evaluation is that this is a standardized approach to evaluate models across different data sets. This makes it easy for developers to compare different [[Algorithms]] or different versions of a model. This is often used by developers and data scientists to identify areas where the model needs improvement, or guide the tuning of model parameters and figure out when a model meets the desired performance threshold, and whether it's ready for deployment. Statistical techniques lay the foundation for future improvement of the model. Let's discuss a few common statistical metrics used to evaluate LLMs. ROUGE scores, where ROUGE stands for Recall-Oriented Understudy for Gisting Evaluation, measures the overlap of n-grams between the model's output and reference outputs. The focus is on recall-based evaluation, typically used for tasks like text summarization. BLEU scores, where blue stands for Bilingual Evaluation Understudy, compares n-grams in the output with n-grams in the reference translation, and uses this to assess the quality of text. This is often used in machine translation tasks.
>
> **[1:35](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/statistical-methods-for-llm-evaluation?u=76281980&t=95)** METEOR, where METEOR stands for Metric for Evaluation of Translation with Explicit Ordering, takes into account synonymy, stemming and [[Microsoft Word|word]] order, and gives us a more flexible evaluation for machine translation and text generation task compared to BLEU scores. Perplexity scores measure how well a language model predicts a sample of text. Lower perplexity indicates better predictive performance, and this is commonly used in tasks like language modeling. WEAT, or Word Embedding. Association Tests is used to evaluate bias in word embeddings by measuring the association between specific sets of words, say gender or racial terms, and other categories, say occupations or traits. A strong association could indicate bias within the model's embeddings.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (3), [[Algorithms]] (1)
> **Env Vars:** rouge (2), bleu (2), meteor (2), weat (1)
> **Definitions:** stands for (3), is a  (1)
> **Analogies:** such as (1)
> **Speakers:** - [narrator] (1)

#### [ROUGE scores](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/rouge-scores?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/rouge-scores?u=76281980&t=1)** - [Instructor] We'll understand each of these in more detail, starting with ROUGE scores. This is not a single metric. Instead, it's a set of metrics designed to evaluate the quality of text generated by machine models, used for tasks like summarization and sometimes even machine translation. The generated text is compared with reference text where the reference text is often human-generated and it tries to evaluate how well the generated text overlaps with the reference content. Now, the term recall-oriented in this measurement focuses on recall, meaning it emphasizes how much relevant information from the reference text is captured in the generated output. Now, ROUGE is not a single score, but instead there are several variants. There is ROUGE-N, there is ROUGE-L, and all of these assess different levels of similarities such as N-gram overlap, sentence structure similarity through longest common subsequences and so on. You need to understand the limitations of ROUGE scores as well. It's effective for measuring content overlap, but it does not include things like fluency, coherence, or grammatical correctness in the generated text. The most important detail that ROUGE measures is content overlap. How much of the important content from the reference text appears in the generated text. This is often computed by comparing the N-grams like bigrams or trigrams in the two texts. ROUGE captures sentence structure similarity through the ROUGE-L score. This metric evaluates the longest common subsequence,
>
> **[1:37](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/rouge-scores?u=76281980&t=97)** which reflects how well the generated text maintains the sentence structure and the [[Microsoft Word|word]] order in the original. A variant of the ROUGE metric, ROUGE-S, assesses the overlap of skip bigrams, which are word pairs that can be separated by other words. This helps capture relationships between keywords that may not necessarily be next to one another or adjacent in the text. In terms of metrics, ROUGE emphasizes recall rather than precision, which means it focuses on how much of the important information in the reference text is captured by the generated text. Next, let's take a quick overview of the different variants of the ROUGE scores. ROUGE-N measures the overlap of N-grams, where N-grams are sequences of N words between the generated and reference text, so you can measure the overlap of unigrams or bigrams. Those will be ROUGE 1 and ROUGE 2. This score helps evaluate how well the model captures keyword combinations. Sentence-level structure similarity is captured using ROUGE-L scores where L stands for longest common subsequence between the generated text and the reference. You can use this to evaluate how well the model preserves the order of words. ROUGE-S measures the overlap of skip bigrams, which are pairs of words in the generated text that can be separated by other words. This evaluates the model's ability to capture related words even if they're not adjacent or next to one another. ROUGE-W is a weighted version of ROUGE-L
>
> **[3:10](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/rouge-scores?u=76281980&t=190)** that considers the length of the longest common subsequence, and it gives more importance to longer sequences of matching words. ROUGE-SU compares skip bigrams and unigrams to evaluate both word and sequence overlap, and it gives you a broader measure of tech similarity.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (3)
> **Env Vars:** rouge (20)
> **Definitions:** stands for (1), is a  (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [BLEU score](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/bleu-score?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/bleu-score?u=76281980&t=1)** - [Instructor] Next, let's discuss BLEU scores. This measures the quality of machine-generated text by comparing n-grams in the generated output with n-grams in one or more reference text. Higher the overlap, better the quality of the generated text. Now, this part of BLEU scores is similar to ROUGE scores where it compares n-grams. This evaluates how well the n-grams, unigrams, bigrams, trigrams, etcetera, overlap across the generated and reference text. Now, BLEU scores calculates the precision of n-grams, how many of the generated n-grams appear in the reference, and applies a brevity penalty to avoid overly short translations. So this idea of a brevity penalty is a little different from how ROUGE scores work. BLEU scores also take into account [[Microsoft Word|word]] order within n-grams and penalize generated text that has the correct words, but in a different sequence compared to the reference. A higher BLEU score reflects a greater similarity between the generated text and the reference, suggesting that the model has produced a translation or summary that closely matches human-created outputs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **Env Vars:** bleu (5), rouge (2)
> **Definitions:** is a  (1)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)

#### [METEOR score](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/meteor-score?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/meteor-score?u=76281980&t=1)** - [Instructor] The METEOR score evaluates models by combining precision and recall. This evaluates machine generated text by considering how many words in the generated text match the reference, that's precision, and how many words in the reference appear in the generated text, that's the recall. This provides a balance between the two. Now, unlike blue scores, METEOR incorporates synonym matching, allowing it to recognize words with similar meanings, thus improving the evaluation of content even if synonyms of the [[Microsoft Word|word]] are used instead of the exact word itself. METEOR accounts for stemming, which is a common technique used in [[Natural Language Processing (NLP)|natural language processing]]. METEOR recognizes words with the same root. So run and running are the same. Swim and swimming are the same. And it incorporates word order to better evaluate sentence structure and fluency.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (3), [[Natural Language Processing (NLP)|Natural language processing]] (1)
> **Env Vars:** meteor (4)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Perplexity](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/perplexity?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/perplexity?u=76281980&t=1)** - [Instructor] Another common metric for evaluating [[Large Language Models (LLM)|large language models]] is perplexity, and this is where [perplexity.ai](https://perplexity.ai) gets its name from. Perplexity is a measure of how well a language model predicts a sample of text. Lower perplexity indicates better predictive performance. To be more precise, perplexity measures how well a language model predicts the next [[Microsoft Word|word]] in a sequence based on a likelihood of a word occurring given all of the words that have been generated so far, all of the preceding words. Now, how do you interpret perplexity? A lower perplexity value indicates better performance, which means the model is more confident in its predictions, and the distribution of the words predicted by the model is closer to the true distribution of words in the text. Now, the formula that perplexity uses is fairly complex. It's calculated as the inverse [[Probability]] of the correct word based on the model's prediction, normalized by the number of words with the formula typically being the exponent of the average negative log likelihood. I basically got this definition from the documentation. You don't need to know the details of how perplexity is calculated, however, you do need to know the intuition behind the perplexity score. Perplexity is related to entropy where entropy measures the uncertainty in predicting the next word. Lower entropy corresponds to lower perplexity, that is the model is less uncertain or rather more certain when it predicts the next word.
>
> **[1:35](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/perplexity?u=76281980&t=95)** Perplexity is used to evaluate language models in the language modeling task where the goal is to predict a sequence of words accurately, often used in natural language text generation. Using perplexity to evaluate your models have several benefits. Unlike metrics like BLEU scores, ROUGE scores, or METEOR, which require human generated reference text for comparison, perplexity is computed solely based on the language model's own predictions and the input text completely eliminating the need for reference data sets. Keep in mind that these reference data sets are hard to collate and collect. These are the golden data sets. Perplexity directly evaluates how well a model predicts the next word in a sequence, and thus gives us a better idea into the internal workings of the model, on the model's internal predictive ability, rather than relying on external comparisons. The perplexity score is useful in unsupervised tasks where we don't have a reference data set. Since perplexity does not depend on human references, it can be used in language modeling, text generation, where a reference data set may not always be available or practical.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (6), [[Large Language Models (LLM)|Large language models]] (1), [[Probability]] (1)
> **Env Vars:** bleu (1), rouge (1), meteor (1)
> **URLs:** [perplexity.ai](https://perplexity.ai) (1)
> **Documentation:** the documentation (1)
> **Definitions:** is a  (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [Model-based methods for LLM evaluation](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/model-based-methods-for-llm-evaluation?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/model-based-methods-for-llm-evaluation?u=76281980&t=1)** - [Instructor] We can now focus on the third category of evaluation techniques, model-based evaluation. This focuses on analyzing the internal behavior and outputs of the language model itself rather than comparing it against human-generated reference data. This approach uses various quantitative measures, like perplexity, accuracy, and loss functions, to evaluate how well the model performs in terms of generating coherent, accurate, and contextually-relevant outputs. The emphasis here is on the model's ability to generalize to unseen new data and ensuring that the model only memorizes specific training examples, but also adapts well to new real-world inputs. Here are some broad techniques used for model-based evaluation of LLMs. Natural Language Influence, or NLI, evaluates how well the model understands and infers relationships between pairs of sentences, such as entailment, contradiction, or neutral. Entailment, as we've discussed before, is determining whether a given hypothesis logically follows from a given premise. Analyze about assessing the model's reasoning and comprehension skills. BLEURT, Bilingual Evaluation Understudy with Representations from Transformers, is a neural network-based metric that evaluates text generation by comparing the model's output with human references, incorporating both semantic and syntactic understanding. Another interesting technique is the use of LLM judges. These are specialized models or tools
>
> **[1:36](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/model-based-methods-for-llm-evaluation?u=76281980&t=96)** used to rate the quality of text generated by other language models. They assess factors like fluency, relevance, and coherence, and give you a more nuanced evaluation based on pre-trained judgment models. This is an example of AI validating AI.

> [!info]- Semantic Content
>
> **Env Vars:** nli (1), bleurt (1), llm (1)
> **Definitions:** is a  (1), is an  (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Natural language inference](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/natural-language-inference?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/natural-language-inference?u=76281980&t=1)** - [Instructor] Let's dive a little deeper into each of these, starting with NLI or natural language inference. This determines the relationship between generated text and a reference statement. This categorizes the text as entailment. That is, a hypothesis that follows from a premise, whether the generated text is a contradiction of the reference or just neutral. NLI is useful for tasks like summarization and question answering, where it helps evaluate whether the generated text aligns with or contradicts factual information provided in the reference. It also categorizes entailment versus contradiction. That is, the model checks if the generated output supports the reference, that is, entailment, contradicts it, that's a contradiction, or is unrelated, that is neutral, and this reflects the model's ability to understand and maintain logical consistency. Using NLI, LLMs can be evaluated on their factual accuracy and their ability to generate text that remains logically consistent with information that's already known. It's a way to see whether a model is hallucinating or not. Let's understand the basic structure of natural language inference. We have a premise. This is the given reference statement or text. It's the source of information that the model is tasked with understanding or relating to. This could be some kind of factual statement that the model is supposed to use to draw conclusions from or answer questions on. A hypothesis is the generated output
>
> **[1:36](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/natural-language-inference?u=76281980&t=96)** or statement that's being evaluated. This represents the model's interpretation, generated text or answer based on the premise. The hypothesis is what the NLI model examines to determine what its relationship is with the premise. If the hypothesis logically follows from the premise, this is classified as entailment. This means the hypothesis is a true statement given the premise. So let's say your premise was something like the sky is blue and the hypothesis says the sky has a color. The relationship between the hypothesis and the premise is that of entailment. If the hypothesis contradicts the premise, meaning the two statements cannot both be true at the same time, it's classified as a contradiction. So for the same premise, the sky is blue, let's say the hypothesis says the sky is red. The relationship is a contradiction. Now, if the hypothesis neither supports nor contradicts the premise, that is it is independent or unrelated, it's classified as neutral. So for the same premise, the sky is blue, let's say the hypothesis is something about the cat is sleeping. The relationship is neutral. Let's understand this with another example. I have my original premise here, a senior is waiting at the window of a restaurant that serves sandwiches. If the model's response is a person waits to be served his food, that's an example of entailment. Now, if the model's response is a man is looking to order a grilled cheese sandwich, this is an example of a neutral categorization, where really the hypothesis
>
> **[3:11](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/natural-language-inference?u=76281980&t=191)** doesn't really have anything to do with the premise. Let's say the hypothesis is a man is waiting in line for the bus. That's a contradiction. A senior is actually waiting in line at a restaurant.

> [!info]- Semantic Content
>
> **Definitions:** is a  (5), is an  (1)
> **Env Vars:** nli (4)
> **CLI Commands:** cat (1)
> **Speakers:** - [instructor] (1)

#### [BLEURT](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/bleurt?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/bleurt?u=76281980&t=1)** - [Instructor] We'll now move on to the BLEURT technique for model-based evaluation. BLEURT is a learning-based evaluation metric that leverages the power of the BERT model, where BERT stands for Bidirectional Encoder Representations from Transformers. This is a pre-trained language model, and can be used to assess text similarity with a deeper understanding of semantics. Unlike traditional models that rely on exact [[Microsoft Word|word]] matches, BLEURT considers both lexical and contextual alignment between generated text and reference text. Here is a summary of what BLEURT is. A learning based evaluation metric that leverages BERT, a pre-trained language model, to assess tech similarity with a deeper understanding of semantics. Unlike traditional metrics, BLEURT evaluates both lexical and contextual alignment. Lexical alignment, because it checks whether the words in the generated text and reference texts are similar, and contextual alignment, because it looks beyond just words and assesses if the meaning and intent of both texts are the same, even if different words are used. BLEURT is trained on large data sets of human evaluation data, making it better positioned to reflect human judgment by learning subtle preferences and criteria used in text assessment. Now by utilizing BERT's contextual embeddings, remember, that's the language model used for this BLEURT technique, BLEURT provides a more accurate and reliable measure of text quality
>
> **[1:32](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/bleurt?u=76281980&t=92)** compared to metrics based solely on word overlaps. BLEURT makes use of the BERT model. BERT stands for Bidirectional Encoder Representations from Transformers, and this was developed by [[Google]] AI back in 2018, and it revolutionized [[Natural Language Processing (NLP)|natural language processing]] by introducing a deep pre-trained bidirectional model for understanding language context. BERT is bidirectional, and this means it considers the context of a word from both its left and the right, and this allows BERT a more nuanced understanding of language compared to unidirectional models, which only process sentences from say, left to right. BERT is built on the Transformer architecture, and it leverages attention mechanisms to model relationships between all words in a sentence, irrespective of the position or distance of the words from one another. Bert is trained using a technique called masked language modeling, where the training data has random words in sentences masked, and the model is trained by predicting those words. This enables contextual learning. BERT also learns sentence relationships by predicting if one sentence logically flows from another. That is next sentence prediction. Bert is a very popular and widely used model across NLP tasks like sentiment analysis, text classification, question answering, and summarization. Here are the steps involved in evaluating models with BERT. The first is the BERT pre-training phase,
>
> **[3:05](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/bleurt?u=76281980&t=185)** where the model is pre-trained on large scale text data using unsupervised learning to understand language at the contextual level. BLEURT then undergoes additional pre-training on synthetic sentence pairs, where these pairs are created automatically. These might include pairs of sentences with varying levels of similarity, paraphrases, rephrased sentences, or altered text. This allows the model to understand similarity relationships between different sentence structures and semantics. BLEURT is further fine-tuned using human evaluation data from public data sets, such as the WMT Metrics Shared Task, where humans have rated the quality of text translations or outputs. For specialized applications, BLEURT can be fine-tuned further using domain-specific human evaluation data, tailoring it better to a particular task or industry.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (3), [[Google]] (1), [[Natural Language Processing (NLP)|Natural language processing]] (1)
> **Env Vars:** bleurt (12), bert (12), nlp (1), wmt (1)
> **Definitions:** is a  (4), stands for (2)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Judge models](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/judge-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/judge-models?u=76281980&t=1)** - [Instructor] We'll now discuss model-based evaluation with LLM judges. LLM judges are specialized language models trained to evaluate the quality of text generated by other LLMs, and they focus on aspects such as fluency, coherence, relevance, and adherence to instructions. Is the response relevant to the prompt that the model has been given? Traditional metrics, such as BLEU score or ROUGE scores, are quantitative metrics. LLM judges, however, leverage their own language understanding to assess text quality in a holistic manner, and they often provide more human-like evaluations and can capture nuances. LLM judge models are fine-tuned on human evaluation data or expert annotated data sets, and this is what allows them to mimic human judgment in evaluating outputs for tasks, such as summarization, translation, and text generation. These judge models often tend to be used in benchmarks and comparative evaluations and help developers rank and improve LLMs by providing qualitative insights into generated outputs beyond simple quantitative metrics. There are several models that are commonly used as judge models today. OpenAI provides dedicated evaluation APIs or fine-tuned models designed for assessing other LLMs, particularly in tasks like summarization, translation, and reasoning. Anthropic's Claude models are often used as judges for evaluating conversational coherence
>
> **[1:34](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/judge-models?u=76281980&t=94)** and ethical considerations in AI outputs, and this is primarily because Claude is a safety-focused model. Open access models, like BLOOM from BigScience and OPT from Meta, are often fine-tuned as evaluation judges in research, particularly for benchmarks requiring open source solutions. We've already discussed the BERT model earlier. There are fine-tuned versions of the Bert models, RoBERTa, DistilBERT. These are fine-tuned on human evaluation data sets and are often used as judges for tasks like ranking outputs or assessing factual consistency. LLM judge models are examples of AI validating AI, and there are several benefits to using these LLM judges. They can mimic human judgment by assessing text for fluency, coherence, relevance, factual accuracy, and provide qualitative insights that go beyond traditional metrics, like BLEU scores or ROUGE scores. Unlike [[Microsoft Word|word]] overlap metrics, LLM judges often evaluate the overall meaning and context of text, giving you a more holistic assessment. They'll consider paraphrasing of text, semantic similarity, and have nuanced language understanding. LLM judges may start with foundation models that can be fine-tuned for specific evaluation tasks, maybe summarization, maybe translation, conversational quality. This makes them versatile across different applications of LLMs. LLM judges can evaluate large-scale data sets or outputs from multiple models efficiently,
>
> **[3:07](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/judge-models?u=76281980&t=187)** which means you can iterate faster in model development. We know that human evaluation is slow and may not always be consistent. LLM judges provide consistent evaluations and avoid the variability and subjectivity that can arise from human evaluations. Here is an example of LLM evaluation workflow where an LLM judge is used to score and evaluate the outputs of a large language model. The LLM test case contains all of the components needed for the evaluation, the initial prompt or query given to the LLM, the generated text or response from the LLM, the retrieval context or additional information. This is then passed to an LLM judge scorer, which analyzes the test case and all of its components and assigns a score based on predefined evaluation criteria. This could be fluency, coherence, or relevance. The judge models are often capable of providing a reason or explanation for the assigned score. This helps developers understand the strengths and weaknesses of the generated output. The assigned score is then compared to a predefined threshold to determine whether the output meets the required standard.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **Env Vars:** llm (16), bleu (2), rouge (2), bloom (1), opt (1)
> **Analogies:** such as (3)
> **Definitions:** is a  (1), is an  (1)
> **Speakers:** - [instructor] (1)

#### [LLM evaluation](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/llm-evaluation?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/llm-evaluation?u=76281980&t=1)** - [Instructor] In this demo, we'll work with a framework used for evaluating [[Large Language Models (LLM)|large language models]] or LLM's DeepEval. DeepEval allows you to use various metrics including accuracy, coherence, and factual consistency to evaluate LLMs. It allows developers to test model responses systematically, often incorporating human feedback and automated scoring methods. In this demo, we'll do a very quick overview of how DeepEval can be used to evaluate large language models. If you'd like to learn more about DeepEval, here is their [[GitHub]] page, confident ai/deepeval. And here, you'll get complete instructions for how the LLM evaluation framework works, how you can install and get set up with DeepEval, and all of the basic and advanced use cases. DeepEval is available as a [[Python (Programming Language)|Python]] library and all you need to do is pip install this library on your machine. So let's go back to our code here and pip install DeepEval. In addition to DeepEval, I'm going to install the Open AI library because I'm going to be making calls to the OpenAI APIs. We'll use DeepEval to evaluate the responses of the [[GPT-4|GPT-4o]] model, and I also install the [[Hugging Face]] transformers library and the datasets library. We'll need these libraries to access the SQuAD dataset, the question answering dataset from Stanford, which we'll use to evaluate the OpenAI GPT-4o model. When I first initialized DeepEval, I encountered an error because it was unable
>
> **[1:33](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/llm-evaluation?u=76281980&t=93)** to install the PyArrow library that it depends on. PyArrow is used for fast [[Data Processing]], particularly for handling columnar data in memory. DeepEval failed when it tried to compile PyArrow on my machine. And to work around this, I install the pre-compiled binary for PyArrow using the command that you see here on screen, just a heads up in case you encounter the same issue. Next, I'll import all of the Python libraries that we need for this particular demo. Next, I need to create and configure an OpenAI key, so that I can make API calls to OpenAI's GPT-4o model. Now, I happen to have a paid account on the OpenAI platform, and I've already pre-created a key. So here I am on platform.[openai.com](https://openai.com). I already have an account here. This is a paid account. You can create an account and just top it up with $5. That's more than sufficient. We'll be making just one or two API calls. Head over to your profile and here under API Keys, I used the create new secret key button to create a new key. I've already created a key here called DeepEval, and that's what I'll be using in my code. Now, you are, of course, free to evaluate the output of any other large language model. I set the [[OpenAI API]] key in my environment variable using os.environ, and this will allow me to make API calls to OpenAI. Now, we'll see how OpenAI performs on question answering, and the ground truth dataset that we'll use here is the SQuAD dataset,
>
> **[3:06](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/llm-evaluation?u=76281980&t=186)** the Stanford question answering dataset. I use the load dataset function to load in this dataset into our program. The original dataset is large. I'm only going to load in 5% of the data. In fact, you can load in 1% if you want to. We'll just use five records. On lines five through eight, I'm going to access five records from the SQuAD dataset. The SQuAD dataset variable will contain the ID from the original dataset, the question and the answer. I just extract these and I'll print out the five records that we'll use to evaluate the OpenAI GPT-4o model out to screen. These questions all happen to be about fire departments. What is the New York City Fire Department's motto? What does FDNY stand for? And so on. And notice there are answers available as well. These are the ground truth answers. We'll now get the responses from an OpenAI model. Instantiate the OpenAI client, and I'm going to make a request to the OpenAI APIs. I have a utility function here, get openai responses, which takes in the questions as an input argument. For each question, I have a for loop on line three. On lines seven through 18, I call client.chat.completions.create which is the API call to an OpenAI model. The model name is gpt-4o. The system prompt says you are a helpful assistant, respond in an extremely succinct manner. For example, to the question, when did the scholastic magazine
>
> **[4:39](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/llm-evaluation?u=76281980&t=279)** of Notre Dame begin publishing? Respond with September, 1876. So we want to-the-point answers. This system prompt ensures that the model will not give long answers to our questions, and we'll give succinct responses that we can then evaluate. On line 16, I pass in our question prompt to the model and extract the answer on line 20. On line 22, we append the answer to the responses list and return responses on line 28. Now, let's get the responses from our LLM and then use DeepEval to evaluate those responses. On line one, I extract the questions from our SQuAD dataset. It has just five questions, remember? On line three, I invoke get openai responses and get the LLM responses. And then I print out the responses on lines seven and eight. Let's see what the model responses are like. Under generated answers, we have the original question and the responses from the model. Now, it's time for us to use DeepEval to compare these responses to ground truth values. And I'll set up a helper function for this as well. Evaluate with DeepEval OpenAI. This takes in the dataset with the ground truth answers and the answers variable contains the responses from our OpenAI model. We run a for loop through our dataset and the responses from the model. This is on line four. Now, for each question, we set up an LLM test case. I've instantiated the test case on lines seven through 11. We pass in three parameters to each test case.
>
> **[6:13](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/llm-evaluation?u=76281980&t=373)** The original question that is the input, the model's response, that is the actual output and the ground truth answer that is the expected output. On line 13, I instantiate the answer relevancy metric in DeepEval with the threshold of 0.5. This measures how well a model's response aligns with the input query or context. This will tell us whether the generated answer from GPT-4o is on-topic, meaningful, and whether it directly addresses the question. I've specified a threshold of 0.5 here. Every test case will assess the model's response with the ground truth and assign a score to the model's response. The threshold here defines the minimum score required for a response to be considered relevant. You can make things more strict by specifying a higher threshold. On lines 15 through 19, I specify a custom metric to evaluate the model's response using GEval. The name of the metric is correctness, indicating that it evaluates how accurate the model's response is. The criteria is a plain English explanation of the evaluation rule. The rule here checks to see whether the models generated output matches the expected correct answer. The evaluation params specify what the framework should compare. The metric compares the actual output that is the response generated by the model with the expected output, the correct or ideal response. Now, that we've set up the test case and the metrics, assert test will run the test case and evaluate the metrics for that test case.
>
> **[7:48](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/llm-evaluation?u=76281980&t=468)** On lines 25 and 26 for each test case, I measure the answer relevancy and the correctness metrics. And I store the results in the metric results dictionary. For each metric, I have a score as well as a reason. And on lines 44 through 50, I append the details of every test case to the result. This includes the question, the ground truth, the model answer, and the metrics computed. Now, this is what we are going to invoke to evaluate our model. This will take about two to three minutes to run through four, five test cases. The first test case resulted in an error indicating that the model's response was wrong. And down below there is a second error indicating a second response was wrong, but the remaining responses seem to be correct. Now, it's possible for us to look at the final results by iterating over the variable that I returned. I'll print out the question, the ground truth, the model answer, the answer relevancy score, the answer relevancy reason, the correctness score, and the correctness reason. And you'll see that the output is very, very interesting. OpenAI gets the first question completely wrong. What is the second largest fire department force in the world? The ground truth says New York City Fire Department, but the model says Tokyo Fire Department. Because the model's answer is completely wrong, all of the scores are set to none. There are no reasons as well. Let's look at the second one now. The motto of the New York City Fire Department is New York's bravest, and the model gets it absolutely right.
>
> **[9:21](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/llm-evaluation?u=76281980&t=561)** The relevancy score is one, and you can see the reason for that. The output is perfectly aligned with the input request. The correctness score is lower than one 0.86 because the output matches the expected output, but has some unnecessary quotation marks. Let's go down to the last question. How many firefighters work for the New York City Fire Department? The ground truth says 11,080. The model says approximately 11,000. The answer relevancy score is one because the answer was very relevant to the question, but the correctness score is 0.37 because the numeric value is close, but not exact. So you can see from this simple example that the DeepEval framework is a useful tool for evaluating LLMs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Large Language Models (LLM)|Large language models]] (2), [[Python (Programming Language)|Python]] (2), [[GitHub]] (1), [[Hugging Face]] (1), [[Data Processing]] (1)
> **Env Vars:** api (6), llm (5), gpt (5), fdny (1)
> **Prerequisites:** install (7), set up (4), configure (1)
> **CLI Commands:** make (4), python (2), pip (2)
> **Versions:** 0.5 (2), 0.86 (1), 0.37 (1)
> **Definitions:** is a  (4)
> **URLs:** [openai.com](https://openai.com) (1)
> **Cross-References:** go back to (1)


### 3. Collecting and Using User Feedback in LLMs

[↑ Back to Table of Contents](#table-of-contents)

#### [User feedback in LLMs](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/user-feedback-in-llms?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/user-feedback-in-llms?u=76281980&t=1)** - [Instructor] We'll now move on to discussing the role of user feedback in [[AI Orchestration]], and this is particularly needed for [[Large Language Models (LLM)|large language models]]. Let's first talk about how the process of user feedback works. Users engage with the AI system, providing inputs or utilizing its services. The system then collects feedback from the users based on their interactions, and this may include explicit feedback in the form of ratings or comments, or implicit data, such as usage patterns. The feedback that's collected is used to retrain or fine-tune the AI model, allowing it to learn from real-world usage and improve its performance over time. The updated model, which is now enhanced by incorporating user feedback, is expected to perform better and give you more accurate or relevant outputs. The improved model is then deployed back into the production environment where users can interact with the refined system. And this process continues. It's this feedback loop that drives improvement of models over time. How does user feedback help? This ensures that AI models are aligned with the real-world use cases, and effectively address the challenges and requirements of how they're used. It doesn't matter how well the model does in testing and development. It's the real-world interactions and the quality of the real-world interactions that drive model adoption. Feedback from users can help identify blind spots and biases within AI models that may not be apparent from the data alone.
>
> **[1:36](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/user-feedback-in-llms?u=76281980&t=96)** By providing diverse perspectives and highlighting potential issues, user feedback contributes to the development of more fair and unbiased AI systems. And really, this process overall improves trust and adoption in AI. AI systems become more responsive to user needs and preferences, and this fosters a sense of collaboration between users and developers. Collecting and incorporating user feedback comes with its own set of challenges. The first challenge is: How do you get high-quality, actionable feedback at scale? You want feedback that's both relevant and informative, but this can be difficult, especially when dealing with a large and diverse user base. Now, feedback may not always point in one direction, so addressing these conflicts or subjective feedback can be difficult and challenging. Different users may have different opinions and preferences, so it's difficult for developers to figure out the best course of action. And then once you have the feedback, how do you incorporate it into your iterative development lifecycle? That's another challenge. This requires effective mechanisms for collecting, analyzing, and incorporating feedback, and this should be part of the development process and automated if possible.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[AI Orchestration]] (1), [[Large Language Models (LLM)|Large language models]] (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Implicit and explicit feedback](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/implicit-and-explicit-feedback?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/implicit-and-explicit-feedback?u=76281980&t=1)** - [Instructor] There are two types of user feedback that you can incorporate into your AI development lifecycle. The first is implicit feedback, which refers to the indirect signals or cues derived from user interactions within a system. Implicit feedback is feedback that's gathered without explicit participation from the user. These are signals which are gathered by observing and analyzing user behavior within the system, whether it's an [[E-Commerce]] site or your app. Explicit feedback refers to direct input or signals which are actively provided by users to express their opinions such as ratings. This can express preferences, satisfaction with the system, anything. This feedback is typically solicited through specific mechanisms designed to gather user input. Review comments or surveys are another example. Let's talk about implicit feedback first. These are indirect signals or cues derived from user interactions within a system without requiring explicit user participation. How long does the user stay on a page? Does the user click around a lot? Does the user interact with the page a lot? All of these are examples of implicit feedback. This feedback relies on interpreting user behavior as an indicator of their preferences, satisfaction, or dissatisfaction with the system. Clicks, dwell time, or query reformulation, all of these provide valuable insights into the underlying needs and intentions of the user. If you have to type in five different prompts to get a response, the model clearly isn't performing well.
>
> **[1:35](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/implicit-and-explicit-feedback?u=76281980&t=95)** You can apply models to analyze patterns in these user interactions and then infer preferences or dissatisfaction based on these patterns. This involves using machine learning techniques to identify like correlations between user behavior and desired outcomes. Implicit feedback is great for several reasons. It's collected passively. It's non-intrusive in nature. It does not interrupt the user's flow or require active input. Because of its non-intrusive nature, implicit feedback is highly scalable. It can be gathered from all users of the system interacting with the system, and it provides a large and comprehensive data set for analysis. Explicit feedback often has limited participation. For example, measuring the number of students who turn up for a class is a better indicator of the class's popularity than comparing survey feedback. Implicit feedback is collected continuously in [[Real-Time]] as users interact with the system, and this allows developers immediate insights into user behavior and allows them to dynamically adapt the system to improve experience. Of course, implicit feedback has its own set of challenges. An important challenge with implicit feedback is the potential for ambiguity in interpreting user actions. For example, a user may stay for a long time on a particular response, this could indicate either interest in the response or confusion with the response. It's important that these signals be accurately interpreted, and that requires careful analysis.
>
> **[3:08](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/implicit-and-explicit-feedback?u=76281980&t=188)** Implicit feedback can be prone to biases stemming from user behavior or the context of the interactions. For instance, users might be more likely to interact with popular or frequently recommended items leading to a bias towards those items in the feedback data. Here are some of the commonly used implicit feedback metrics. Click-through rates or CTRs measures whether users click on a response or suggestion provided by the LLM. This indicates relevance or interest. Dwell time tracks how long a user spends reading or interacting with a generated response. This suggests satisfaction or could suggest confusion as well. Session abandonment observes that users stop engaging mid session. This can signal dissatisfaction or that the response fail to meet their expectations. If the model is asked to rephrase or repeat a response, this can signal the LLMs response was unclear or irrelevant. Now, let's talk about explicit feedback. Explicit feedback includes direct input or signals provided by users. This allows users to express their opinions, preferences, satisfaction, or dissatisfaction with the system. This feedback is typically solicited through specific mechanisms designed to gather input. Examples include rating systems such as star ratings or thumbs up and down buttons where users explicitly rate the quality or helpfulness of a response. This also includes surveys or questionnaires
>
> **[4:40](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/implicit-and-explicit-feedback?u=76281980&t=280)** that ask users to provide detailed feedback on their experience with the system. This can also be text corrections or suggestions provided by users to improve a chat bots response. Explicit feedback is actively solicited from users often through prompts or requests within the system. This means that with explicit feedback, users are aware that they're providing feedback and encouraged to provide thoughtful input. Explicit feedback is often used for supervised [[Fine Tuning]] of machine learning models because the signals are very, very clear and it makes it easier to align models with user preferences. What are some of the pros and cons of explicit feedback? The first advantage is that it provides clear and unambiguous signals about user satisfaction or dissatisfaction. Explicit feedback offers higher quality data for model training compared with implicit feedback. Because it's direct, it accurately reflects what the user is looking for. Of course, explicit feedback has its own set of challenges. The first thing is it typically has a lower volume compared to implicit feedback because users often don't take the time to respond to explicit feedback. This could be because of user fatigue or just general reluctance. Another problem is potential biases. Explicit feedback may not represent all users equally as certain user groups tend to be more or less likely to provide feedback. Someone who's unhappy with the offering may be more likely to provide explicit feedback than someone who's happy.
>
> **[6:13](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/implicit-and-explicit-feedback?u=76281980&t=373)** Here is a table that summarizes everything that we've discussed so far about implicit feedback and explicit feedback, the definition, where the feedback comes from, the advantages, and the challenges.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[E-Commerce]] (1), [[Real-Time]] (1), [[Fine Tuning]] (1)
> **Definitions:** refers to (2), is a  (2), means that (1)
> **Analogies:** such as (2), for example (2), for instance (1)
> **Env Vars:** llm (1)
> **UI Navigation:** click on (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [instructor] (1)

#### [Reinforcement learning](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/reinforcement-learning?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/reinforcement-learning?u=76281980&t=1)** - [Instructor] The importance of incorporating user feedback brings us to a discussion of [[Reinforcement Learning]] from human feedback, or RLHF. This technique of incorporating user feedback makes use of reinforcement learning, hence the name RLHF. Let's talk about what exactly this machine learning technique is all about. This is an ML paradigm where agents learn by interacting with an environment. The agent takes actions, receives feedback in the form of rewards or penalties, and adjusts its behavior to maximize cumulative rewards over time. Reinforcement learning is essentially a trial and error process, which allows the agent to learn optimal strategies for achieving its goals. Let's understand what reinforcement learning is all about. This machine learning paradigm involves the use of an agent where the agents learn by interacting with an environment. The agent takes actions, and for every action there is either a reward or a penalty. Think about a robot learning to climb a hill. If it goes further up a hill, that's a reward. If it kind of stumbles or goes backward, that's a penalty. So there are several key components of a reinforcement learning system. There is the agent, the environment, the actions that the agent takes, and the rewards or penalties that the agent receives. The agent is the learner. The environment is the context in which the agent operates. The actions are the choices the agent makes, which way to step, for example, for the robot. And the rewards are the feedback signals
>
> **[1:34](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/reinforcement-learning?u=76281980&t=94)** that guide the learning process. Reinforcement learning agents often employ policies and value functions to guide their [[Decision-Making]]. A policy just defines the agent's strategy for selecting actions in different states. While a value function estimates the long-term value of being in a particular state or taking a specific action. Here is a way to visualize the process of a reinforcement learning. The agent here is the decision-maker and the decision-maker could be an AI model that learned through trial and error. It observes the current situation that is the state and takes an action based on its learning so far. Now the environment is the system or task that the agent interacts with. The environment responds to the agent's actions by providing a new state or a situation and some feedback for the action that the agent performed. Now, the state here refers to the current condition or context observed by the agent, and it represents the environment status. Based on the current state and the policy that the agent has learned so far, the agent performs an action. The action is the choice or decision made by the agent, which affects the environment. This could be the robot moving one step in a particular direction. The action can be good or bad based on whether it moves the agent closer to its goals or further away from its goals. The reward is the feedback the environment provides after the agent's action, indicating how good or bad the action was, and the agent's goal is to maximize these rewards over time.
>
> **[3:10](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/reinforcement-learning?u=76281980&t=190)** This sets up a learning loop. The agent repeatedly observes the current state, takes an action, receives feedback, a reward or maybe a penalty, and then updates his behavior based on this interaction to improve its decision-making. So how does reinforcement learning apply to LLMs? Well, the environment is the LLM's responses and the user interactions. This includes the prompts provided to the LLM, the generated outputs, and any other feedback provided by the user. The actions taken by the LLM agent are the generated outputs that can be text, code, images, or some other kind of content. The LLM aims to generate outputs that are relevant, informative, and aligned with user expectations. Rewards in reinforcement learning are typically derived from user feedback. This can include explicit feedback, such as ratings or corrections, or implicit feedback, such as interaction patterns or click-through rates. These reward signals guide the LLM to learn which outputs are preferred by users. And as we've discussed before, there are different types of feedback that can be incorporated in reinforcement learning. Explicit feedback in the form of direct user ratings, corrections, or explicitly stated preferences are valuable sources of reward signals for RL. Implicit feedback such as interaction patterns, click-through rates, and other behavioral signals can also be used as an implicit feedback mechanism for RL. While less direct, implicit feedback can provide valuable insights into user preferences and engagement, or you can combine explicit and implicit feedback
>
> **[4:43](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/reinforcement-learning?u=76281980&t=283)** to create a more balanced and robust reward model for RL. This approach leverages the strengths of both feedback types, provides a more comprehensive understanding of user preferences, and guiding LLM improvement. So how does reward modeling work? The first step in reward modeling is to collect and pre-process feedback data from various sources, including explicit feedback, implicit feedback, and even human interactions. This data needs to be clean, formatted, and transformed to a suitable representation for training a reward model. Reward models are usually trained to predict reward signals based on LLM outputs and user feedback. These can be simple regression models or can be more complex [[Neural Networks]], depending on how complex your reward function is. Make sure you take steps to prevent reward hacking. This is when the LLM learns to exploit loopholes in the reward function to maximize rewards without actually achieving the desired behavior. So the model doesn't improve, but it still accumulates rewards. This can lead to unintended consequences and clearly suboptimal performance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Reinforcement Learning]] (10), [[Decision-Making]] (2), [[Neural Networks]] (1)
> **Env Vars:** llm (8), rlhf (2)
> **Analogies:** such as (3), for example (1)
> **Definitions:** is an  (1), is a  (1), refers to (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Reinforcement learning from human feedback](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/reinforcement-learning-from-human-feedback?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/reinforcement-learning-from-human-feedback?u=76281980&t=1)** - [Instructor] [[Reinforcement Learning]] from Human Feedback, or RLHF, is a training method where a model is fine-tuned using human preferences. It works by calculating human feedback on different model outputs, training a reward model to predict human preferences, and then using reinforcement learning to optimize the model for better responses. Let's look at a visualization of how the RLHF model works. There are different components here. There is the prompts dataset, the language model, the reward model, and the ranking of the output. Let's look at how this is used to fine-tune LLMs. The prompt dataset is a dataset of prompts containing various questions or tasks for the language model to respond to. Many of these prompts are sampled and they serve as an input to the initial language model. The prompts are passed through the initial pre-trained language model, which generates text responses based on its training. Now these outputs are raw and may not align well with human preferences. Human evaluators review the generated text and score it based on quality, relevance, or any other criteria that you think of. The outputs are then ranked. That is they're given relative rankings from the best to the worst, or you can use scoring systems to indicate which responses are better. Now you use all of this information to train a different model called the reward model or a preference model. This model is trained using the prompt, the original prompt, the output from the LLM
>
> **[1:33](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/reinforcement-learning-from-human-feedback?u=76281980&t=93)** and the human score that was assigned. The reward model learns to predict human preferences and assigns a reward score to model outputs. And this approximates human feedback. The reward model is then used to guide the language model through reinforcement learning. The language model generates outputs and then receives rewards or penalties from the reward model, and it updates its parameters to maximize the cumulative rewards that it learns. Reinforcement learning from human feedback at its core uses reinforcement learning. RLHF is a method for fine-tuning language models by incorporating human feedback to align their outputs with user preferences and expectations. Humans evaluate and rank model-generated outputs based on quality, relevance, and correctness, creating a data set of preferences. A reward model is then trained using the collected feedback to predict the quality of outputs and provide a reward signal for reinforcement learning. The language model is then fine-tuned using reinforcement learning [[Algorithms]]. Example algorithms include the proximal policy optimization, and the objective of the language model is to maximize the rewards predicted by the reward model. [[ChatGPT]] has been fine-tuned using RLHF. ChatGPT collects human feedback on generated responses where human annotators rank outputs based on relevance, coherence, and safety.
>
> **[3:06](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/reinforcement-learning-from-human-feedback?u=76281980&t=186)** So there'll be, say, four possible outputs and they'll be ranked from the best to the worst. A reward model is then trained using these rankings to evaluate how well ChatGPT's outputs align with human preferences. And this guides the fine-tuning process. ChatGPT uses reinforcement learning to optimize its responses where it iteratively improves by maximizing rewards predicted by the reward model. RLHF enables ChatGPT to generate more helpful, contextually appropriate and safer responses, aligning its behavior with user expectations and other considerations such as ethical considerations.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Reinforcement Learning]] (8), [[ChatGPT]] (5), [[Algorithms]] (2)
> **Env Vars:** rlhf (5), llm (1)
> **Definitions:** is a  (3)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Summary and next steps](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/summary-and-next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/ai-orchestration-validation-and-user-feedback-and-performance-metrics/summary-and-next-steps?u=76281980&t=1)** - [Narrator] And this brings us to the end of this course on validation, user feedback, and [[Performance Metrics]] in [[AI Orchestration]]. We started this course off by looking at validation techniques in traditional machine learning models, such as regression, classification, and clustering, as well as in modern [[Large Language Models (LLM)|large language models]]. We then moved on to discussing performance metrics for models. Again, we discussed traditional models as well as performance metrics for LLMs. We saw that LLMs are evaluated based on human feedback, statistical techniques, or by using LLM judges. And finally, we moved on to incorporating user feedback to improve models. And here we specifically discussed [[Reinforcement Learning]] and reinforcement learning with human feedback used with LLMs. [[LinkedIn]] Learning has a number of other courses on AI Orchestration that you might find interesting. "Planning and Orchestrating for Observability," "Designing the Prototype Architecture and Data Strategy," and finally, "Developing and Testing your AI Prototype." All of these will significantly enhance your learning in addition to this course. Well, that's it from me here today. It's time for me to say goodbye. Thank you for listening.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Performance Metrics]] (3), [[AI Orchestration]] (2), [[Reinforcement Learning]] (2), [[Large Language Models (LLM)|Large language models]] (1), [[LinkedIn]] (1)
> **CLI Commands:** find (1)
> **Env Vars:** llm (1)
> **Cross-References:** we discussed (1)
> **Analogies:** such as (1)
> **Speakers:** - [narrator] (1)


## Instructor

- [[Janani Ravi]]

## Resources

- Exercise files available

## Skills Covered

- Large Language Model Operations (LLMOps)
- Artificial Intelligence (AI)
- Feedback Control Systems
- Computer System Validation
- AI Orchestration

## Path Context

### In [[Building AI Products- Architecture and Orchestration Professional Certificate by LinkedIn Learning]]
← [[AI Orchestration- Developing and Testing Your AI Prototype]] | **5 of 5**

## Part of

- [[Building AI Products- Architecture and Orchestration Professional Certificate by LinkedIn Learning]]

## Appears In

- [[Building AI Products- Architecture and Orchestration Professional Certificate by LinkedIn Learning]]

## Related Courses

_Courses sharing skills:_

- [[Advanced LLMOps- Deploying and Managing LLMs in Production]] — Artificial Intelligence (AI), Large Language Model Operations (LLMOps)
- [[LLMOps in Practice- A Deep Dive]] — Artificial Intelligence (AI), Large Language Model Operations (LLMOps)
- [[Generative Ai And Llmops Building Blocks And Applications]] — Artificial Intelligence (AI), Large Language Model Operations (LLMOps)
- [[AI Orchestration- Foundations]] — Artificial Intelligence (AI), AI Orchestration
- [[Azure Ai For Developers Ai Design Foundations]] — Artificial Intelligence (AI), Large Language Model Operations (LLMOps)

---

[↑ Back to top](#)