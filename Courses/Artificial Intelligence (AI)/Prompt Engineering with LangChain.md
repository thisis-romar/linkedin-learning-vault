---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: prompt-engineering-with-langchain
url: "https://www.linkedin.com/learning/prompt-engineering-with-langchain"
duration_minutes: 321
duration: 5h 21m
level: Intermediate
updated: 4/18/2024
learners: 44046
skills:
  - LangChain
  - Artificial Intelligence (AI)
  - Prompt Engineering
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQHu5CCKBy4pyA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1713212432463?e=2147483647&amp;v=beta&amp;t=IYFJ7ivG7FaMm2fPpWCfZiBWLW_ckaXzz2Q58sD-r0k"
linkedin_topic: Artificial Intelligence (AI)
learning_paths:
  - '[Prompt Engineering Skills for Developers](../../Paths/Artificial%20Intelligence%20(AI)/Learning%20Paths/Prompt%20Engineering%20Skills%20for%20Developers.md)'
prev_courses:
  - '[ChatGPT- Perfecting Your Output with Templates in Prompts](ChatGPT-%20Perfecting%20Your%20Output%20with%20Templates%20in%20Prompts.md)'
next_courses:
  - '[Building Advanced AI Apps with Prompt Flow](Building%20Advanced%20AI%20Apps%20with%20Prompt%20Flow.md)'
path_nav: '[{"path":"Prompt Engineering Skills for Developers","position":5,"total":6,"prev":"ChatGPT- Perfecting Your Output with Templates in Prompts","next":"Building Advanced AI Apps with Prompt Flow"}]'
path_count: 1
tags:
  - course
  - topic/artificial-intelligence
  - skill/langchain
  - skill/artificial-intelligence-ai
  - skill/prompt-engineering
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Artificial%20Intelligence%20(AI)/Prompt%20Engineering%20with%20LangChain.md)

![Prompt Engineering with LangChain](https://media.licdn.com/dms/image/v2/D560DAQHu5CCKBy4pyA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1713212432463?e=2147483647&amp;v=beta&amp;t=IYFJ7ivG7FaMm2fPpWCfZiBWLW_ckaXzz2Q58sD-r0k)

# Prompt Engineering with LangChain

> This course provides a comprehensive yet concise introduction to LangChain, a powerful framework for large language model (LLM) applications. Starting with the basics of LLMs, instructor Harpreet Sahota explores the key features and capabilities of LangChain, showing you how to integrate it with various systems and gain hands-on experience in building practical applications. Whether you're a seaso

> [LinkedIn Learning](https://www.linkedin.com/learning/prompt-engineering-with-langchain) | 5h 21m | Intermediate | 44K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (1 videos)
  - [Create powerful LLM driven applications](#create-powerful-llm-driven-applications)
- [**1. Introduction to Language Models**](#1-introduction-to-language-models) (1 videos)
  - [What are language models?](#what-are-language-models)
- [**2. LLMs and Text Generation**](#2-llms-and-text-generation) (4 videos)
  - [How do language models generate text?](#how-do-language-models-generate-text)
  - [Base LLMs vs. instruction-tuned LLMs](#base-llms-vs-instruction-tuned-llms)
  - [Training, fine-tuning, and in-context learning](#training-fine-tuning-and-in-context-learning)
  - [Prompt engineering](#prompt-engineering)
- [**3. Components of LangChain**](#3-components-of-langchain) (7 videos)
  - [What is LangChain?](#what-is-langchain)
  - [LangChain overview](#langchain-overview)
  - [Model I/O: Interface with language models](#model-io-interface-with-language-models)
  - [Retrieval: Interface with application-specific data](#retrieval-interface-with-application-specific-data)
  - [Chains: Construct sequences of calls](#chains-construct-sequences-of-calls)
  - [Agents: Let chains choose tools based on high-level directives](#agents-let-chains-choose-tools-based-on-high-level-directives)
  - [Memory: Persist application state between runs of a chain](#memory-persist-application-state-between-runs-of-a-chain)
- [**4. Basics of Prompting**](#4-basics-of-prompting) (2 videos)
  - [Prompt basics](#prompt-basics)
  - [Principles and tactics for prompting](#principles-and-tactics-for-prompting)
- [**5. Prompt Templates Deep Dive**](#5-prompt-templates-deep-dive) (17 videos)
  - [Introduction to prompt templates](#introduction-to-prompt-templates)
  - [Multi-input prompt templates](#multi-input-prompt-templates)
  - [Chat prompt template](#chat-prompt-template)
  - [Serializing prompts](#serializing-prompts)
  - [Zero-shot prompts](#zero-shot-prompts)
  - [Custom prompt templates](#custom-prompt-templates)
  - [Prompt pipelining](#prompt-pipelining)
  - [Chat prompt pipelining](#chat-prompt-pipelining)
  - [Prompt composition](#prompt-composition)
  - [Few-shot prompt templates](#few-shot-prompt-templates)
  - [Few-shot prompt templates for chat](#few-shot-prompt-templates-for-chat)
  - [Introduction to example selectors](#introduction-to-example-selectors)
  - [Length-based example selector](#length-based-example-selector)
  - [Max marginal relevance example selector](#max-marginal-relevance-example-selector)
  - [N-gram overlap example selector](#n-gram-overlap-example-selector)
  - [Semantic similarity example selector](#semantic-similarity-example-selector)
  - [Partial prompt templates](#partial-prompt-templates)
- [**6. Prompting Techniques**](#6-prompting-techniques) (7 videos)
  - [Chain of thought](#chain-of-thought)
  - [Self-consistency](#self-consistency)
  - [Self-ask](#self-ask)
  - [ReAct](#react)
  - [RAG](#rag)
  - [FLARE](#flare)
  - [Plan and execute](#plan-and-execute)
- [**7. Prompt Management a.k.a. PromptOps**](#7-prompt-management-aka-promptops) (6 videos)
  - [Prompt management](#prompt-management)
  - [LangSmith](#langsmith)
  - [LangSmith walkthrough](#langsmith-walkthrough)
  - [Prompt versioning in LangSmith](#prompt-versioning-in-langsmith)
  - [LangSmith deep dive](#langsmith-deep-dive)
  - [Managing prompt length for agents](#managing-prompt-length-for-agents)
- [**8. The LLM Landscape**](#8-the-llm-landscape) (2 videos)
  - [Applications of language models](#applications-of-language-models)
  - [The LLM landscape](#the-llm-landscape)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Create powerful LLM driven applications](https://www.linkedin.com/learning/prompt-engineering-with-langchain/create-powerful-llm-driven-applications?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/create-powerful-llm-driven-applications?u=76281980&t=0)** - Hi, I'm Harpreet Sahota. For over 10 years, I've worked in various roles using statistical and machine learning. I've never been more excited about the potential of this technology to change the world than I have been at this particular moment in time. And it's all because of one specific advancement in machine learning. [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) or LLMs. LLMs are at the forefront of a technological revolution. And as with any powerful tool, harnessing its potential requires a robust framework. Enter [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md). A orchestration framework that will give you the building blocks to create powerful LLM-driven applications. I designed this course to be a comprehensive introduction to LangChain. I hope you're excited to embark on your journey into AI and [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md). Are you ready? Let's go.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (2), [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (1), [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md) (1)
> **Env Vars:** llm (1)
> **Speakers:** - hi (1)


### 1. Introduction to Language Models

[↑ Back to Table of Contents](#table-of-contents)

#### [What are language models?](https://www.linkedin.com/learning/prompt-engineering-with-langchain/what-are-language-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/what-are-language-models?u=76281980&t=0)** - [Instructor] Our world is increasingly shaped by technology, from asking a digital assistant about the weather, to translating documents into your native language. Our interactions with technologies is becoming deeply rooted in natural language. Language models are increasingly becoming the heart of these interactions, the backbone of our daily digital engagements. They help machines understand and generate human language, making our interactions with technology smoother and more intuitive. A language model is a machine learning model trained to understand, generate, and interact with human languages. These models learn patterns, structures, and nuances of a language. When given a [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) or a sequence, a language model predicts what might come next based on what it's learned. This is similar to how after hearing the phrase "peanut butter", and many of us might instinctively complete with "jelly". In practice, a language model gives the [Probability](../../Skills/Data%20Science/Probability.md) of a certain word sequence being valid. It's important to note that validity here doesn't strictly refer to the grammatical correctness of the generated text, it's about understanding human language's nuances, its context and its intricacies. Instead, it's about how closely the sequence mirrors human-like writing patterns. This is achieved by training these models on vast amounts of textual data, enabling them to understand context, nuances,
>
> **[1:35](https://www.linkedin.com/learning/prompt-engineering-with-langchain/what-are-language-models?u=76281980&t=95)** and intricacies of human language. There are primarily two types of language models, probabilistic language models, and neural network-based language models. Probabilistic language models are typically based on n-gram probabilities. They predict the next word from the preceding n words. An n-gram is a sequence of n items from a text. For instance, "I love dogs" has a bigram inside of it. Actually, it has two bigrams, "I love" and "love dogs". The biggest limitation for these probabilistic language models lies in their inability to capture deep context. Neural network-based language models [excel](../../Skills/Data%20Science/Microsoft%20Excel.md) at predicting the next word in a sequence using mechanisms like attention for contextual understanding. When referring to [large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md), or LLMs, we're often talking about the transformer architecture, which is a type of deep neural network architecture. Transformers understand the contextual relationship of words in a sequence using a mechanism called self-attention. Transformers can be categorized into the following varieties, encoder only, like the BERT model, which stands for Bidirectional Encoder Representation Transformer. Decoder-only, like the GPT, the generative pre-trained transformer.
>
> **[3:09](https://www.linkedin.com/learning/prompt-engineering-with-langchain/what-are-language-models?u=76281980&t=189)** And the encoder-decoder, like the T5 model. LLMs have some key features that distinguish them from previous language models. First is that these large language models display surprising emergent abilities that were not observed in smaller models. For example, GPT-3 can handle few-shot tasks through in-context learning, something its predecessor GPT-2 struggled with. Accessing LLMs is primarily done through a prompting interface like the [ChatGPT](../../Skills/Artificial%20Intelligence%20(AI)/ChatGPT.md) UI or the [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) API from OpenAI. This means users need to understand how LLMs function and format your prompts appropriately. Developing LLMs blurs the lines between peer research and engineering. Training these models demands practical experience in large-scale [Data Processing](../../Skills/Database%20Management/Data%20Processing.md) and distributed parallel training. As we become increasingly reliant on AI assistance, a foundational grasp of language models equips us to better appreciate this technology's marvel. It empowers us to utilize these tools to their fullest potential and critically evaluate them when necessary. Remember, these models will start to underpin more and more advanced technologies, from search engines to digital assistants built into tools that you're already using every day,
>
> **[4:41](https://www.linkedin.com/learning/prompt-engineering-with-langchain/what-are-language-models?u=76281980&t=281)** to even operating systems. LLMs represent a significant leap in AI capabilities, and they'll continue to shape and redefine our digital experiences.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (4), [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (2), [Probability](../../Skills/Data%20Science/Probability.md) (1), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1), [ChatGPT](../../Skills/Artificial%20Intelligence%20(AI)/ChatGPT.md) (1)
> **Env Vars:** gpt (4), bert (1), api (1)
> **Definitions:** is a  (3), stands for (1)
> **Analogies:** similar to (1), for instance (1), for example (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)


### 2. LLMs and Text Generation

[↑ Back to Table of Contents](#table-of-contents)

#### [How do language models generate text?](https://www.linkedin.com/learning/prompt-engineering-with-langchain/how-do-language-models-generate-text?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/how-do-language-models-generate-text?u=76281980&t=0)** - [Instructor] In this course, we'll treat LLMs as a black box. They're essentially text in, text out machines, but I want to provide you with some intuition for how they generate text. At a high level, this is how LLMs generate text. You start with an initial sequence and then use a LLM to generate the next token. Then you append that to the sequence and repeat the process until you've got the desired sequence. A token, in language models, is a piece or chunk of text, which can be as short as one character or as long as one [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md), or it could be punctuation, like parentheses, question marks, et cetera. Okay, but how exactly does the LLM generate text? Well, the entire process can be broken down into three primary steps: encoding, prediction, and decoding. In encoding, the initial sequence is processed by the LLM. The LLM will then predict the next token based on what it's seen. Then that predicted token is added to the sequence, and this process repeats. In the encoding step. The input text is converted into a sequence of vectors that the LLM can process. The LLM is going to use an embedding layer to map each word in the input token to a vector representation. Instead of treating words as discreet units, embeddings capture the semantic meaning of words by placing semantically similar words closer in the vector space.
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/how-do-language-models-generate-text?u=76281980&t=94)** For instance, the words cat and kitten might have vectors that are close together, reflecting their similar meanings. With the encoded input, the LLM predicts the most likely next word or sequence of words based on the received input. This involves using the learned knowledge of language patterns to generate the most likely output sequence. The self-attention mechanism ensures that words are considered about each other, allowing for deeper comprehension and context-aware prediction. The LLM uses a softmax layer to produce a [Probability](../../Skills/Data%20Science/Probability.md) distribution over the possible next words or sequences. A softmax is just a mathematical function that's going to convert a vector of numbers into a probability distribution. Once the softmax layer outputs a probability distribution, overall potential next words or sequences, the model can select the most probable one. Finally, the LLM is going to translate that vector representation back into human-readable text, generating the output sequence word by word. The output sequence is generated one word at a time, with each new word being generated based on the previous words in the sequence. The quality of the generated text depends on the size in terms of the number of parameters of the model and the amount and quality of training data that was used to train the model, as well as how many [Tokens](../../Skills/Web%20Development/Tokens.md) were seen during training. I just want to take a moment to talk about randomness,
>
> **[3:08](https://www.linkedin.com/learning/prompt-engineering-with-langchain/how-do-language-models-generate-text?u=76281980&t=188)** variation and human influence in LLM outputs. While LLMs inherently possess a element of randomness, which leads to varied responses for the same query, humans do have some tools to influence and refine these outputs. The balance between machine unpredictability and human control is achieved through parameters like temperature, top-k, and top-p. The temperature parameter is what controls the model's, quote unquote, creativity. A lower temperature makes the model more deterministic. As the temperature increases, the model introduces more variability. For instance, at a temperature of zero, the model might consistently complete the phrase The sky is, with blue. As you increase the temperature, the model might produce varied outputs like the sky is falling or the sky is filled with stars. You could, of course, always pick the token with the highest probability, but that might get the model stuck in a repetitive loop, and that's where parameters like top-k and top-p come in. By adjusting these parameters, you can control the breadth of potential outputs. So top-k is going to restrict the model to pick from the top-k tokens sorted by probability. In contrast, top-p selects based on the cumulative probability of top tokens. So a lower temperature means less randomness.
>
> **[4:41](https://www.linkedin.com/learning/prompt-engineering-with-langchain/how-do-language-models-generate-text?u=76281980&t=281)** A temperature of zero will almost always yield the same output, and lower temperatures, which are less than one, are appropriate when performing tasks that have a, quote unquote, correct answer, like answering questions or summarization, or if you really need the model to stick to a particular output. Top-k is more rigid, and that's going to focus on the most probable tokens. Top-p is more flexible, which considers tokens whose probabilities add up to some threshold p. These parameters are going to allow you to balance deterministic outputs and creative variability. While LLMs might inherently lean towards randomness, these tools offer a degree of predictability and control, ensuring that LLMs' outputs align more closely with your intent. Language models are powerful tools for generating text and understanding language, and they typically follow a three-step process. Encoding, prediction, and decoding. We can balance creativity and control and their outputs by adjusting parameters like temperature, top-k and top-p. These models play a crucial role in our digital world, from search engines to virtual assistants and continue to advance AI and [natural language processing](../../Skills/Artificial%20Intelligence%20(AI)/Natural%20Language%20Processing%20(NLP).md). Having a little bit of an understanding of your workings is going to empower you to maximize their potential and evaluate their outputs effectively, which will shape the future of technology and communication.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (7), [Probability](../../Skills/Data%20Science/Probability.md) (6), [Tokens](../../Skills/Web%20Development/Tokens.md) (5), [Natural language processing](../../Skills/Artificial%20Intelligence%20(AI)/Natural%20Language%20Processing%20(NLP).md) (1)
> **Env Vars:** llm (10)
> **Analogies:** for instance (2)
> **CLI Commands:** cat (1)
> **UI Navigation:** select the (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Base LLMs vs. instruction-tuned LLMs](https://www.linkedin.com/learning/prompt-engineering-with-langchain/base-llms-vs-instruction-tuned-llms?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/base-llms-vs-instruction-tuned-llms?u=76281980&t=0)** - [Instructor] In this course, we'll primarily be working out of Jupyter Notebooks. There's a couple of ways that you can run a Jupyter Notebook. One is, of course, you could download the Jupyter Notebook and run it locally on your own machine. Or if you'd prefer, you can go to [Google](../../Glossary/Service/Google.md) Colab and run the notebooks there. So you'll have to go to Colab.research.[google.com](https://google.com) if you choose to run it in Google Colab. Please ensure that you do have a Gmail account or a Google account. That's the only way you'll be able to use Colab. But once you've gone ahead and done that, all you've got to do is hit the Upload tab, go to Browse, pick whichever notebook it is that we're working on, and it'll upload. And you can work along with me, like so. And that's all you've got to do. I think it's important for you to know the difference between a base instruction tuned and chat tuned language model. And the best way to get a good feel of the difference between these is just through some code. So that's what we're going to go ahead and do here. First thing we want to do is install the huggingface_hub and [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md). We'll import a number of libraries that we need to be successful for this section. Now, if you haven't already, you're going to need to sign up for a [Hugging Face](../../Glossary/Service/Hugging%20Face.md). So the way you could do that is just by going to [huggingface.co](https://huggingface.co). Just click on Sign Up, and you'll be prompted to enter some information here.
>
> **[1:35](https://www.linkedin.com/learning/prompt-engineering-with-langchain/base-llms-vs-instruction-tuned-llms?u=76281980&t=95)** Once you enter in that information, you can go to your profile up here. Click on your name. Go to Settings. Scroll down to Access [Tokens](../../Skills/Web%20Development/Tokens.md). And then just go ahead and copy that access token, and you'll paste that here. So Hugging Face, there's a couple of ways you can work with models from Hugging Face. One, probably the most common way is to just download the models locally and run it using their Transformers library. Another way is actually Hugging Face has inference endpoints. So not all the models that are available on the model hub are available through the inference endpoints. So that's something to keep in mind. Just for this section, making it so that we're just using APIs here so you don't need to worry about having a GPU, we are just going to use the Hugging Face inference endpoint here. You'll learn about this as we progress along the course so don't worry too much about it right now, but this is just helping us set up a chain to generate text. So let's start by seeing a base model in action. So a base language model, these are essentially the starting point of the "process," right? These base language models are designed to predict the next [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) in a sequence based on the training data they've been exposed to. However, they're not tailored to answer questions, carry out conversations, or solve problems. So for instance, if you give a prompt, like, "write a story about some place,"
>
> **[3:07](https://www.linkedin.com/learning/prompt-engineering-with-langchain/base-llms-vs-instruction-tuned-llms?u=76281980&t=187)** a base LLM might complete the sentence in a way that's related to the topic, but it won't answer a specific question, engage in dialogue, or really follow the instruction the way that you intended to. Some examples of base LLMs, GT3, GT2, you know, in this case we're using Mistral 7B. You know, these are language models that just have been trained on vast, diverse data sets. Throughout this process, they really get a good understanding of the patterns in human language, and so they're able to predict the next token or the next word with ease. So the thing about these base language models, again, is that they could sometimes struggle with specific prompts or instructions like tone or keyword. So in this case, we are going to use a base model, Mistral 7B in this case. And we're going to pass it this input story, sorry, this input prompt, "write a story about the Dasuya Punjab." And you can see here that it's not really following the instruction, it's kind of just completing the text for us. It's kind of just given us a little bit of factual information about this place, which it could be making some stuff up here, but that's okay. Now let's see what happens within instruction tuned LLM. So an instruction tuned LLM, they're developed from the base LLM through further training and what's called "[Fine Tuning](../../Skills/Software%20Development/Fine%20Tuning.md)," to follow specific instructions that are provided in the input, rather than merely just, you know, predicting the next word in a token.
>
> **[4:41](https://www.linkedin.com/learning/prompt-engineering-with-langchain/base-llms-vs-instruction-tuned-llms?u=76281980&t=281)** So they're specialized in answering questions or executing commands. So for instance, they will respond appropriately to prompts like, "write a story about such and such place." So to create an instruction tuned LLM, you take a base LLM, and then you train it further using a large data set that covers sample instructions and the expected responses. And so this further training is going to help the model understand and execute instructions that are provided in the input. So the instruction model that we're going to use here is Mistral 7B Instruct version 0.2. Interesting thing to keep in mind with these language models is the use of special tokens. And we'll see the effect of these in a second. But remember the prompt here was "write a story about Dasuya Punjab." And here we can see it's actually writing a actual story, right? "It's a small town, nestled in here, blah, blah, blah." You know, it has more of a story feel than what was up here for the base language model output, where it was just kind of stating facts. So Mistral 7B Instruct actually, and most base language models that are kind of on the smaller range, will have special tokens that you need to prepend and append them with. And I just want to take a moment here to look at the difference in in generation. So here Mistral is actually, you know, if you go to their model card on Hugging Face, they say that in order for you to take advantage of the instruction tuned, you should start your sequence with these special tokens, and end your sequence
>
> **[6:15](https://www.linkedin.com/learning/prompt-engineering-with-langchain/base-llms-vs-instruction-tuned-llms?u=76281980&t=375)** with this special token. And you can see here by including these special tokens, it does a far better job at following the instructions. So now just something real subtle, I'm just including an extra white space here and an extra white space here. And you would think that the model is going to output pretty much the exact same thing that it does up here, but you know, you'll be surprised that it actually doesn't. So you know, you could see that it's a bit different than that. So something interesting to keep in mind. It's a little nuance of working with these smaller language models and it could be extremely frustrating, but such as the way when you're working on language models, and you'll get used to it. And finally, there's the chat tuned LLMs. You know, classic example of chat tuned LLM, of course, is [ChatGPT](../../Skills/Artificial%20Intelligence%20(AI)/ChatGPT.md). And you can see the history that I have here with ChatGPT, where I ask to write a story, it writes a story with a lot of veracity, and then I can tell it, you know, "Actually, make it about a man named Harpreet." And you know, we could change it accordingly. So it has much more of a conversational feel to it. So this is something important to keep in mind. If you go up to base model and try to interact with it like it's a chat model, you'll be disappointed. Likewise, if you have an instruction model and you try to interact with the instruction model as if it was a chat model, you might be frustrated that it does not remember any of the chat history. But this is a good distinction for you to understand, and hopefully you enjoyed this session.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Hugging Face](../../Glossary/Service/Hugging%20Face.md) (6), [Tokens](../../Skills/Web%20Development/Tokens.md) (5), [Google](../../Glossary/Service/Google.md) (4), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (3), [ChatGPT](../../Skills/Artificial%20Intelligence%20(AI)/ChatGPT.md) (2)
> **Env Vars:** llm (7), gpu (1), gt3 (1), gt2 (1)
> **UI Navigation:** go to (6), click on (2), scroll down (1)
> **Tools:** colab (4), jupyter (3)
> **Warnings:** keep in mind (4)
> **Analogies:** for instance (2), such as (1)
> **URLs:** [google.com](https://google.com) (1), [huggingface.co](https://huggingface.co) (1)
> **Exercise Files:** download the (2)

#### [Training, fine-tuning, and in-context learning](https://www.linkedin.com/learning/prompt-engineering-with-langchain/training-fine-tuning-and-in-context-learning?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/training-fine-tuning-and-in-context-learning?u=76281980&t=0)** - [Presenter] In the previous section, you heard me talk about base, instruction-tuned, and chat-tuned models. When you're working with [large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md), it's important that you familiarize yourself with key terms that are fundamental to understanding their functionality and development. These terms include pre-training, fine-tuning, in-context learning, and [retrieval-augmented generation](../../Skills/Artificial%20Intelligence%20(AI)/Retrieval-Augmented%20Generation%20(RAG).md). Understanding these concepts is crucial for anyone working with or studying large language models. Let's start by talking about pre-training. So pre-training is the initial process of teaching a language model to understand and generate human-like text. It involves feeding a massive dataset of text to the model. The text can come from books, website, or other written material. The primary goal in this phase is to enable the model to recognize patterns in language, including grammar, [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) usage, and stylistic elements. Through this pre-training process, the model is going to develop a base understanding of language, which it can use to generate coherent and contextually-appropriate responses. Fine-tuning is adjusting the base model to specialize in specific tasks or styles of text, or to tailor the behavior of a model. This process involves training the model further on a more specialized dataset. So for example, if the goal is to make the model proficient in legal language,
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/training-fine-tuning-and-in-context-learning?u=76281980&t=94)** it would be fine-tuned on legal documents. Or if you wanted to make a model that is capable of following instructions, you would take that base language model and further train it on a instruction following dataset. Likewise, if you want to take a base model and tune it for chat purposes, you'd use a chat dataset that has examples of multi-term conversations. In either one of those situations, the goal of fine-tuning is to adapt the general language understanding of the model to specific domains, styles, or applications. The model becomes more accurate and relevant in the context of the specialized data it was fine tuned on. In-context learning refers to the ability of the model to adapt and respond based on the immediate context provided in the prompt. This doesn't involve additional training. It just relies on the model's existing knowledge and its ability to interpret and respond to the context within a given input. So to enable the model to understand and respond appropriately to new unseen prompts by leveraging context is the main goal here. So the model can then generate responses that are relevant to the specific prompt, even if the prompt presents information or asks for a style of response that the model wasn't explicitly trained or fine-tuned on. In essence, training provides
>
> **[3:06](https://www.linkedin.com/learning/prompt-engineering-with-langchain/training-fine-tuning-and-in-context-learning?u=76281980&t=186)** the foundational language understanding. Fine-tuning adapts this understanding to specific domains or styles. And in-context learning allows the model to apply this understanding flexibly based on the immediate input it receives. Then there's also retrieval-augmented generation, also known as RAG. RAG is a hybrid approach that combines language models with traditional [Information Retrieval](../../Skills/Data%20Science/Information%20Retrieval.md) techniques. So this is going to enhance a language model's responses using external data. So RAG embeds a dataset into vectors and then stores that into vector database. Upon receiving a query from the user, it's going to search the vector database for contextually-relevant information and then take that information and stuff it into the prompt of the language model so that we can augment that prompt. The goal here is to provide more accurate, context-aware, and up-to-date responses, especially when dealing with specific, detailed, or rapidly-changing information. RAG results in responses that are enriched with external data. This is going to reduce inaccuracies and enhance the relevancy of the information provided by a large language model. So to sum it all up, grasping the core concepts of pre-training, fine-tuning, in-context learning, and retrieval-augmented generation is super important for effectively working with
>
> **[4:38](https://www.linkedin.com/learning/prompt-engineering-with-langchain/training-fine-tuning-and-in-context-learning?u=76281980&t=278)** and understanding large language models. Each term here has a unique aspect of LLM development, and it reflects the intricate balance between foundational learning, specialized adaptation, contextual responsiveness, and data-enriched generation that defines how AI engineers are using language models in their applications.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (3), [Retrieval-augmented generation](../../Skills/Artificial%20Intelligence%20(AI)/Retrieval-Augmented%20Generation%20(RAG).md) (3), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [Information Retrieval](../../Skills/Data%20Science/Information%20Retrieval.md) (1)
> **Env Vars:** rag (4), llm (1)
> **Definitions:** refers to (1), known as (1), is a  (1)
> **CLI Commands:** make (2)
> **Analogies:** for example (1)
> **Speakers:** - [presenter] (1)

#### [Prompt engineering](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-engineering?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-engineering?u=76281980&t=0)** - [Presenter] [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md) and in-context learning are related, but distinct concepts. And in this module, you get a high-level overview of what prompt engineering is. Prompt engineering involves strategically crafting and structuring the input or prompt to a language model to guide it towards generating a specific type of response. This is like giving detailed instructions to achieve a desired output. In-context learning refers to the model's ability to understand and respond to new information or requests based on the context provided within the prompt itself. It's about the model's capability to adapt its responses based on the input without additional training. So in essence, prompt engineering is the art and science of crafting effective prompts to guide LLMs in generating desired outputs, while in-context learning is a characteristic of the model itself, describing how it processes and responds to the input it receives. The clearer and more specific your prompts, the better the final output. In the context of LLMs, prompt engineering ensures that the model understands the user's intent and delivers accurate and relevant responses. So why is prompt engineering important? Well, at its core, prompt engineering is about communication. It's the bridge between human intent and machine understanding. A well-crafted prompt can guide an LLM
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-engineering?u=76281980&t=94)** to produce accurate, relevant, and context-aware responses. On the other hand, a poorly-designed prompt can lead to ambiguous or incorrect outputs. Thus, mastering prompt engineering is essential for anyone looking to leverage LLMs effectively. Prompt engineering, which is the process of generating responses from [large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) by providing them with specific inputs, has undergone significant advancements since its inception. Initially, the main focus of prompt engineering was on formulating the right questions to extract the desired answers from a model. With the evolution of large language models, the techniques for prompting have improved. With the introduction of early large language models, prompts still function mainly as queries to extract information. But as these models evolved, so did the intricacies of prompt crafting. The development of models like GPT-3 marked a significant shift in large language models. Unlike its predecessors, GPT-3 could generate responses that weren't explicitly present in the training data, indicating an ability to generalize from input prompts in more abstract ways. This advancement led to new prompting techniques, such as zero-shot, one-shot, and few-shot learning, where models could perform tasks without extensive prior examples.
>
> **[3:08](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-engineering?u=76281980&t=188)** Engineers can now craft prompts that not only requested information, but also structured tasks to guide the LLM towards a particular style of response or reasoning path. As prompt engineering has continued to develop, it's become a crucial component of LLM utility, moving beyond simple question and answer formats to include more creative and complex interactions. The focus has shifted to optimizing prompts for the best possible outcomes. Recognizing that the way a prompt is structured can significantly influence a model's output. While basic prompting techniques have their place, they might fall short in some situations. This is where advanced techniques come into play. For example, chain-of-thought prompting. This technique improves a language model's reasoning capabilities. By providing examples paired with a rationale, LLMs can generate a rationale along with their answers, enhancing their reasoning performance. LLMs can also be integrated with external [Databases](../../Skills/Software%20Development/Databases.md) to reduce errors and enhance the accuracy of responses. This gives them a richer context and ensures that their information is relevant and accurate. The future of prompting includes developing more intricate prompts that can handle a wide range of tasks and inquiries, and enhance an LLM's ability to interpret
>
> **[4:42](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-engineering?u=76281980&t=282)** and respond to these prompts accurately. Prompt engineering is not just a technical skill, it's an art. It requires a deep understanding of a model's capabilities and what your desired outcome is. Understanding prompt engineering is crucial whether you're a developer, researcher, or AI enthusiast. It's the key to unlocking the true potential of LLMs and ensuring that they serve us in the best way possible. As LLMs grow in complexity and capability, prompt engineering becomes even more critical. [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md), with this forward-thinking approach, an extensive toolkit, is poised to lead this change. By simplifying the development process and fostering a collaborative ecosystem, LangChain is shaping the future of prompt engineering and redefining how we interact with AI.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md) (15), [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (4), [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (2), [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **Env Vars:** llm (4), gpt (2)
> **Definitions:** refers to (1), is a  (1)
> **Analogies:** such as (1), for example (1)
> **Speakers:** - [presenter] (1)


### 3. Components of LangChain

[↑ Back to Table of Contents](#table-of-contents)

#### [What is LangChain?](https://www.linkedin.com/learning/prompt-engineering-with-langchain/what-is-langchain?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/what-is-langchain?u=76281980&t=0)** - [Instructor] Understanding and generating human-like text is a remarkable achievement, made possible by [large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) like [ChatGPT](../../Skills/Artificial%20Intelligence%20(AI)/ChatGPT.md) and [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md). These models offer many possibilities, from creating human-like text to understanding complex queries, and revolutionizing our interaction with machines. However, utilizing these models in practical applications can be challenging, which is where [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) comes in. It's a solid framework that's essential to harness the potential of these powerful tools. LangChain's a framework for developing applications powered by large language models. It enables applications that are context-aware. One of the standout features of LangChain is its ability to connect to context sources. Context can be prompt instructions, few short examples, or content to ground its response in. Whether you're integrating with a database, a [Cloud Storage](../../Skills/Cloud%20Computing/Cloud%20Storage.md) solution, or online repository, LangChain ensures the context is accessible and ready for processing. It also enables applications that are able to reason, meaning applications are relying on a large language model to reason about how to answer based on provided context, what actions to take, et cetera. This means that the framework can dynamically interact with this environment, making applications feel alive in responses.
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/what-is-langchain?u=76281980&t=94)** It's not just about answering a query, it's about understanding the context and providing a solution. The LangChain library consists of several packages including the core libraries, LangChain templates, LangServe, and LangSmith. These [products](../../Skills/Software%20Development/Microsoft%20Products.md) work together to make an entire application lifecycle simple and efficient. In this course, we're going to focus on the LangChain [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) library which offers composable tools and integrations for working with large language models. These components are modular and easy to use, whether you're using the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the LangChain framework or not. Additionally, built-in assemblages of components called Off-the-Shelf chains, allow you to easily accomplish higher level tasks. You can get started immediately with these prebuilt chains or customize them using the individual components to create your own chains. The LangChain Library consists of several different packages including the base abstractions and the LangChain expression language. In LangChain core, third party integrations in LangChain community and chains, agents, and retrieval strategies that make up an applications' cognitive architecture in LangChain. LangChain templates is a collection of easily deployable reference architectures, and these are just designed to streamline various tasks. LangServe is a library that enables the deployment
>
> **[3:07](https://www.linkedin.com/learning/prompt-engineering-with-langchain/what-is-langchain?u=76281980&t=187)** of LangChain applications as a REST API. LangSmith is a developer platform that allows debugging, testing, evaluation, and monitoring of chains built on any LLM framework and seamlessly integrates with LangChain. We'll have a brief intro into this in the prompt management section of the course. These products work together to make the entire application lifecycle simple and efficient. First, you can write your applications using LangChain and start quickly with the reference templates. Next, LangSmith can be used to inspect, test, and monitor your chains so you can make improvements and deploy with confidence. Finally, you can turn any chain into an API with LangServe. LangChain offers a modular, easy-to-use and customizable framework for working with large language models. Its main value proposition includes composable tools, integrations, and prebuilt chains for higher level tasks. LangChain is a powerful tool in the AI toolkit. By simplifying the integration of LLMs into real-world applications, LangChain is paving the way for a future where our interactions with machines are more natural, intuitive, and productive. As we stand at the cusp of a new era in human-machine interaction, LangChain and [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md) will undoubtedly play pivotal roles. For developers, businesses and curious minds,
>
> **[4:42](https://www.linkedin.com/learning/prompt-engineering-with-langchain/what-is-langchain?u=76281980&t=282)** now is the time to dive deep into the fascinating world and framework of LangChain and really unlock the potential of large language models for your business and for your customers.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (22), [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (5), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [ChatGPT](../../Skills/Artificial%20Intelligence%20(AI)/ChatGPT.md) (1)
> **Definitions:** is a  (5), means that (1)
> **CLI Commands:** make (4), python (1)
> **Env Vars:** api (2), gpt (1), rest (1), llm (1)
> **Speakers:** - [instructor] (1)

#### [LangChain overview](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langchain-overview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langchain-overview?u=76281980&t=0)** - [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) has several core modules. A module in LangChain is a reusable component that provides specific functionality for building AI systems. Each of the following modules abstracts away complex implementation details and exposes a simplified API. Models allows you to interface effortlessly with language models. Retrieval allows you to access application-specific data seamlessly. Chains allow you to construct sequence of calls with ease. Agents let chains smartly select tools based on high-level directives. Memory helps you maintain application state between chain runs. And callbacks, for example, will allow you to stream and log every chain step. So let's just do a brief overview of each of these modules, and then after this lesson, we'll go into the code for each one of these. To develop a language model-powered application, you need a language model. Model I/O makes it easy to swap and dynamically build prompts, models, and output parsers. The module includes the following components. Prompts, which are just input messages that guide the model's response. Prompt templates are predefined recipes for generating prompts, and LangChain has a number of different prompt templates that we can use. We will do a very, very deep dive into those prompt templates in a future session. Language models.
>
> **[1:33](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langchain-overview?u=76281980&t=93)** The LangChain framework provides interfaces for base LLMs and chat models. LLMs can take a text string as input and then return a text string, while chat models take a list of chat messages as input and return a chat message as output. Then there's also output parsers. So after an LLM returns text, you can use an output parser to structure the output and obtain more structured outputs if needed. Many applications that utilize LLMs require specific domain knowledge that's not present in the original training dataset. The retrieval module is responsible for sourcing, preparing, embedding, storing, and retrieving external data to enhance the language model's generation process. It offers features such as basic retrieval and advanced techniques like retrieval augmented generation. So this is a module that contains all the document loaders, transformers, text embeddings, vector stores, and retrievers that are all there to help you [Fetch](../../Skills/Web%20Development/Fetch.md) relevant documents and improve the accuracy of generated answers. You can use a large language model in isolation for simple applications, however, chaining LLMs together with other components is a better solution for more complex applications. This approach allows you to build modular and maintainable applications. So to achieve this, you can use LangChain's chains. So a single LLM can handle simple prompts and simple applications,
>
> **[3:07](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langchain-overview?u=76281980&t=187)** but more complex ones require chaining LLMs with other components. So for instance, you might create a chain that receives user input, formats it with the prompt template, and then passes the formatted response to another LLM instance. That way you can build more complex chains by combining multiple chains or chains with other components. Agents module enables the creation of [AI Agents](../../Skills/Artificial%20Intelligence%20(AI)/AI%20Agents.md) that can converse naturally and perform real-world actions using tools. The module manages conversation flow, tool integration, and memory, and combines language model capabilities with executable tools. The agent's core components include the base agent class, some tool abstractions, the agent executor, as well as conversational agents and various other agent types. Memory module in LangChain allows agents to maintain context and reason over time. It provides interfaces for storing agent states across multiple inferences. It handles the complexity of different storage options through a simple unified API. And it's essential for LangChain agents to reason. The callbacks module provides a way to hook into events and LangChain to enable logging, monitoring, streaming, and other custom integrations. Note that we're not going to get into callbacks for this course. LangChain offers a suite of core modules that empower developers to harness the full potential of language models. These modules collectively provide a powerful toolkit
>
> **[4:41](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langchain-overview?u=76281980&t=281)** for building intelligent, context-aware applications. And in the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of this section, we are going to jump right into code and start seeing all of this in action.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (9), [Fetch](../../Skills/Web%20Development/Fetch.md) (1), [AI Agents](../../Skills/Artificial%20Intelligence%20(AI)/AI%20Agents.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Env Vars:** llm (3), api (2)
> **Definitions:** is a  (3)
> **Analogies:** for example (1), such as (1), for instance (1)
> **Exercise Files:** template (1)
> **Warnings:** note that (1)
> **Speakers:** - langchain (1)

#### [Model I/O: Interface with language models](https://www.linkedin.com/learning/prompt-engineering-with-langchain/model-i-o-interface-with-language-models?u=76281980)

#### [Retrieval: Interface with application-specific data](https://www.linkedin.com/learning/prompt-engineering-with-langchain/retrieval-interface-with-application-specific-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/retrieval-interface-with-application-specific-data?u=76281980&t=0)** - [Instructor] Now that we know how to send data to a language model and parse that data, let's talk about another core component of [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md). This component is called retrieval. So we'll start the notebook off by installing our dependencies, importing a couple of preliminaries that we need, set our OpenAI key, download a document. That's essentially what we're doing here. The document that we're downloading is the golden_hymns_of_epictetus. We're going to go ahead and just parse this text file to a [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) object. That's pretty much all we're doing. Just cleaning this up a little bit. And you can see here that this is a document of 23,500 words. So real quick, I want to give you a high-level overview of this task called [retrieval-augmented generation](../../Skills/Artificial%20Intelligence%20(AI)/Retrieval-Augmented%20Generation%20(RAG).md). So language models are trained on kind of a fixed dataset, right? There's so much data out in the world that a language model did not have access to during training. For example, you might be working at a company, your company's internal documentation most definitely was not available to a language model during its pre-training phase, but it would be nice to give a language model access to that information. And so this is where retrieval comes in. This is where this task of retrieval-augmented generation fits in. Essentially, the flow is like this. We've got some external sources of data. We're going to load that external source of data. We're going to transform that data by splitting it into chunks. We'll take those chunks of data,
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/retrieval-interface-with-application-specific-data?u=76281980&t=94)** create vector embeddings from it. We're going to take those vector embeddings, push them to a vector store. And then once it's in the vector store, we can retrieve relevant documents to a query from the vector store using some type of similarity. You know, it could be cosine similarity, BM25. There's a number of different retrieval methods. That's a high-level overview of how retrieval-augmented generation works. But let's kind of dive a little bit deeper into that. Let's talk about retrieval in LangChain. Essentially, it's just the process of fetching and retrieving relevant data or documents from an external source, right? And like I mentioned before, this is useful because it's going to allow you to incorporate external data into the language model's context. Because if we're providing additional context and information that's not present in the training data, you're going to enhance the generation process and improve the quality and relevance of the generated response. So to start off with, we need document loaders, and that's what we're going to talk about right now. So document loaders and LangChain are used to load data from various sources into what's called a LangChain document object. A document is essentially a just a piece of text with some associated [Metadata](../../Skills/Web%20Development/Metadata.md). And document loaders give us a convenient way to [Fetch](../../Skills/Web%20Development/Fetch.md) data from these sources, which could be text files, web pages, video transcripts, database. It could be a wide number of things. And the abstractions in LangChain just make it easy for us to connect to this data and use that data as context for language model. The most simplest document loader is the TextLoader.
>
> **[3:12](https://www.linkedin.com/learning/prompt-engineering-with-langchain/retrieval-interface-with-application-specific-data?u=76281980&t=192)** All it does is reads in a file as text and places it all into one document object. And so we're going to do that here with the document that we just downloaded, right? So we see here that this thing that we have, this thing that we've loaded is a list. And this list, every element of this list is a LangChain document object. There are a ton of different document loaders in LangChain. I encourage you to go check out the documentation. You can see here that LangChain provides connection to a wide variety of data sources. So go ahead and check that out and know that you have options available to you. So how does a document loader work in LangChain? So you're going to bring in your document loader of choice, enter LangChain environment, instantiate the loader, and then use that loader's load method to turn files into LangChain documents. The whole purpose of a document loader is just to tweak the document to fit your model's needs. And so part of what we need to make that happen is something called a text splitter. So a text splitter is just going to cut up long chunks of text into manageable, meaningful parts. This helps keep related text together and fit text into the model's processing window. We are going to make use of what's called the RecursiveCharacterTextSplitter. It smartly splits up text into chunks, and then it uses a series of separators. If a chunk is too big, it's going to try a different separator.
>
> **[4:45](https://www.linkedin.com/learning/prompt-engineering-with-langchain/retrieval-interface-with-application-specific-data?u=76281980&t=285)** And this process is repeated until all the chunks are the right size. So a high-level overview of splitting, how it works is we are going to select the best separator to divide the text, and then it's going to continue to split any oversized chunks recursively. We're going to look at the character count or [Tokens](../../Skills/Web%20Development/Tokens.md) to size up those chunks. Then we're going to allow for some overlap between chunks for some contextual continuity. And then we can optionally track the start index of chunks for reference. This gives us a nice way to kind of fetch what we need from a document, like we don't need to pull in the whole text, right? So let's see this in action. Here, we are going to define a chunk_size of 1,000 with an overlap of 50. The length_function we need to define as well. This is the regular python length_function. And we're going to add the start index. And let's take a look at what happens here. So we've split up our text, and here's what we have. So these are two separate chunks of text. We can see here, are these only the works of Providence within us? What [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) suffice, so on and so forth. And so these are what these text chunks look like. Now that we have the text chunks, we're going to need to do embeddings. So let's go ahead and talk about that. So text embedding models for retrieval in LangChain are used to represent text documents
>
> **[6:20](https://www.linkedin.com/learning/prompt-engineering-with-langchain/retrieval-interface-with-application-specific-data?u=76281980&t=380)** in a high dimensional vector space, where the similarity between vectors are going to correspond to the semantic similarity between corresponding documents. And so these models are going to capture the semantic meaning of text and allow for an efficient retrieval of the similar documents that are based on embeddings. So we're not going to go too deep into embeddings conceptually, but you're free to look that up. We'll just essentially treat it as a black box here. Once we've instantiated our embedding model, we need to construct a vector store and a vector store retriever. And to construct a vector store retriever, you first need to load documents using the document loader. Then you're going to split the documents into smaller chunks using the text splitter. And then now you can finally generate vector embeddings for the text chunks using an embeddings model like OpenAI. And then finally, you're going to create a vector store using those generated embeddings. Once that vector store is constructed, you're going to use it as a retriever to query the results. So LangChain abstracts a lot of that away from us, and it's just a one-liner. And this one-liner is going to essentially turn our texts into embeddings and push it into a vector store. That's all that's happening here. We're using a FAISS vector store. So that's [Facebook](../../Glossary/Service/Facebook.md) AI Similarity Search. I believe that's what that stands for, but it's just an in-memory vector store. For this course, that's all we're going to use is in-memory vector stores. We're not going to use anything fancy like [Weaviate](../../Glossary/Service/Weaviate.md) or [Pinecone](../../Glossary/Service/Pinecone.md) or anything like that.
>
> **[7:51](https://www.linkedin.com/learning/prompt-engineering-with-langchain/retrieval-interface-with-application-specific-data?u=76281980&t=471)** I'm mostly going to stick to FAISS. Great. So now that we have instantiated a vector store, let's talk a little bit more in depth about vector store retrievers. Once a vector store is constructed, you're going to use this as a retriever to query the text. So the vector store retriever supports a lot of different methods, including similarity_search, max_marginal_relevant_search. You know, you can also set a similarity_score_threshold or specify the number of top documents to receive. What you see here below, we're just going to use a similarity_search method of the vector store, and you can think of this as a search tool. You give it a piece of text, tell it how many results you want, and then it's going to return a list of documents that are most similar to the given text. So if you've got specific requirements, like you only want documents from a certain author, you can use filters to specify that. So here what we're going to do is we're going to pass this query and we're going to fetch the most relevant documents from that text that we've embedded that answered this question. And if you're curious about the different settings that you can pass to the vector store, you can just do this, go to vector store similarity_search, copy, paste that here, hit it with the question mark. You can see the different arguments that we have here. So we're using just the default arguments. So we're hitting it and getting the top four documents. I'm not using any filters. That's pretty much it. Fetch_k is a number of documents to fetch before filtering.
>
> **[9:25](https://www.linkedin.com/learning/prompt-engineering-with-langchain/retrieval-interface-with-application-specific-data?u=76281980&t=565)** So what's happening is that the vector store retriever is going to go in there and it's going to fetch 20 documents. And then from those 20 documents, it's going to filter it down to four of the most relevant documents, essentially. And there we have that. All right, now that we know how to retrieve context from a vector store, it is time to generate some text. So to generate text, we essentially need to construct a prompt. And this prompt needs to be able to accommodate the retrieve text that we just got from our vector store retriever. So we'll go ahead and do our imports. We're going to use a RetrievalQA chain, we're going to send you a prompt template, and we're going to use ChatOpenAI. The template that we're going to use is going to look like this, string of text that just says, "Use the following pieces of context to answer the question. If you don't know the answer, just say, 'Ah, snap homie. I ain't going to front. I don't know,' and don't try to make up an answer. Use three sentence maximum, relevant analogies, and keep the answer as concise that as possible." And this is where we are going to inject the retrieved context right here. This is where we're going to inject our question. All right, so now that we've got our template constructed, we are going to create a QA prompt. This is just going to be a prompt template that's based on the template we just saw. Instantiate our large language model. Just use [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) in this case with the temperature of zero. And then we're going to set up a RetrievalQA chain. We're going to pass the RetrievalQA chain, our language model,
>
> **[10:57](https://www.linkedin.com/learning/prompt-engineering-with-langchain/retrieval-interface-with-application-specific-data?u=76281980&t=657)** the vector store retriever, and then the prompt. We have our query here, "What do grief, fear, envy, and desire stem from?" And then we're going to take this query and fetch a result. And here we have the result, "Grief, fear, envy, and desire stem from the mind's focus on external factors and the lack of inner tranquility." So essentially what's happened here is that all the context we saw that, for example, this context that we saw gets injected right here, our questions there, and then we get the answer out. So we're using retrieve context to augment our prompt. So the preferred method to do anything nowadays in LangChain is to use the LangChain expression language. And so that's what we're going to do here. I'm going to show you how to do retrieval using the LangChain expression language. So first thing we do is going to integrate context in the question. So we have a prompt template. that's going to include placeholders for the context in question. Then we're going to set up a retriever with a in-memory store for document retrieval, we're going to create what's called a runnable component. Then in this case, we're going to use a RunnableParallel that's going to combine document search results with the user's question, and a RunnablePassthrough, which is going to pass the user's question unchained to the language model. And the steps are like this. Essentially we create a RunnableParallel with two entries, context and question. We're going to feed this dictionary to the prompt component, which is going to construct a prompt
>
> **[12:32](https://www.linkedin.com/learning/prompt-engineering-with-langchain/retrieval-interface-with-application-specific-data?u=76281980&t=752)** using the user's question and retrieve documents. The model component is going to evaluate the prompt using the OpenAI LLM. And then finally, the output parser is going to transform the response into a readable python string. So it's the end to end process is going from document retrieval and prompt creation to model evaluation and output parsing, and it's a very seamless flow that's going to integrate these components for us using the expression language. And it looks like so. You see here, we've got the response from the language model. I'm not going to go too depth into what runnables are in LangChain. I encourage you to check out the documentation to learn more about RunnableParallels and RunnablePassthroughs. For right now, good enough to know that we need these to essentially pass data between our chains and between the components of LangChain. And there you have it. You're now familiar with retrieval in LangChain.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (19), [Fetch](../../Skills/Web%20Development/Fetch.md) (6), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [Retrieval-augmented generation](../../Skills/Artificial%20Intelligence%20(AI)/Retrieval-Augmented%20Generation%20(RAG).md) (3), [Metadata](../../Skills/Web%20Development/Metadata.md) (1)
> **Code Identifiers:** similarity_search (3), length_function (2), golden_hymns_of_epictetus (1), chunk_size (1), max_marginal_relevant_search (1)
> **CLI Commands:** make (4), python (3), snap (1)
> **Exercise Files:** template (6)
> **Definitions:** is a  (4), is called (1), stands for (1)
> **Env Vars:** faiss (2), bm25 (1), llm (1)
> **Analogies:** for example (2), similar to (1)
> **Documentation:** the documentation (2)

#### [Chains: Construct sequences of calls](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chains-construct-sequences-of-calls?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chains-construct-sequences-of-calls?u=76281980&t=0)** - [Instructor] Let's talk about chains in [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md). So in one sentence, a chain is just an end-to-end wrapper around multiple individual components which are executed in some defined order. And these are really core to LangChain. They allow you to go beyond just a single API call to a language model, and instead kind of chain together multiple calls in a logical sequence. Chains are really what allow you to combine multiple components together to create a single coherent application. So some reasons you might want to use a chain is just to break down complex tasks into smaller steps that could be handled sequentially or by different models or utilities, which does not allow you to leverage different strengths of different systems. You can also add state and memory between calls. So the output of one call can be fed as the input to the next call to provide context and state. You can do additional processing like filtering or validation logic between calls. It also simplifies the debugging process as well. So there's some foundational chain types in LangChain. These are the LLMChain, the RouterChain, SimpleSequentialChain, and TransformChain. These used to be standalone kind of modules and objects in LangChain, but now we can implement these all using the LangChain Expression Language. So we're not going to use the former abstraction for this course. We're just going to use LangChain Expression Language
>
> **[1:33](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chains-construct-sequences-of-calls?u=76281980&t=93)** for each one of these. So let's start now with a LLMChain, right? So now we know that we have the brand new [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) model available to us. Let's go ahead and use that. And we've talked about a chain before. And essentially, at a high level, all the chain does is help you chain together calls to a language model, and we just pass a prompt to a language model. And in this simple chain, here's what we're going to do. So we're going to go ahead and import OpenAI. We're going to import a prompt template. We're then going to instantiate a language model. In this case, ChatOpenAI, the brand new GPT model that came out today. We're going to create a prompt template. And this prompt template is going to say, "Write a comedic, parody rap about the following topic:" And topic is going to be our input variable. And so the chain is constructed like this, just prompt being passed the language model. So when we invoke the chain, we're going to pass the chain a dictionary. That dictionary is going to contain the input variable to the prompt template as well as the value for the prompt. So in this case, ({"topic": wasabi flavoured saki:}) and let's see what we get. All right, so you can see here we get the content out like so. So that's nice and all, but why don't we do something to make our lives just a bit easier.
>
> **[3:08](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chains-construct-sequences-of-calls?u=76281980&t=188)** So I'm going to go ahead and import a StrOutputParser. And let's go ahead and copy this chain, this part again, right? And I'm just going to chain a StrOutputParser here and you can just see the difference that we'll have in output. And you can see by adding the StrOutputParser here, we get a much cleaner output than what we had up here. You see here with the AIMessage content, you know, a better thing to do would've probably saved this to a variable and print it and you could have got, like, a even better printout. So let's go ahead and just do that real quick just to kind of show you. So we can go like this. I'm just going to wrap this in a print statement. All right, and you can see the response here. By using the StrOutputParser and wrapping it in a print statement, we get something that looks a lot more visually appealing than what we saw before. And here we have a parody rap about wasabi flavored saki. All right, so now let's go ahead and use the ChatPromptTemplate. And so when we use the ChatPromptTemplate, we pass a list of messages and this list will have the system prompt. In this case, "You are PaRappa the Rapper. You spit hot fire flows like lava." And the human message is, "Write a comedic, parody rap about the following topic:"
>
> **[4:41](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chains-construct-sequences-of-calls?u=76281980&t=281)** So let's go ahead and run that chain. In this case, I've already attached the StrOutputParser and we'll go ahead and wait on that response. So you can see here that we got the output like so. Now, if you're like me and you get tired of just waiting for the output all at once, something you could do is actually create a stream. And let's see what the streaming looks like. And you can see streaming, we kind of see it happen right in front of our eyes rather than waiting for the entire call to language model to complete and just print out all at once. So you do have that option if you're building like a chat bot for a user interface. You can have this type of streaming effect if you'd like. And there you have it. We've got our song about wasabi flavored saki. Now, let's go ahead and talk about [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) in LangChain. All right, so let's talk about routing in LangChain. So, routing is a technique that's going to allow you to create chains of non-deterministic steps where the result of a previous step is going to define the next step. And so this is helpful to add structure and consistency to an interaction with a LLM. And there are two main ways to perform routing in LangChain. One is to use a RunnableBranch. Two is to use a Custom Factory Function that's going to take the input of a previous step and then return a runnable.
>
> **[6:13](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chains-construct-sequences-of-calls?u=76281980&t=373)** So it's important to note that this function should only return a runnable and not execute it. So, to illustrate both methods, let's consider a two-step sequence, right? The first step is going to classify an input question as being about literature, history, biology, philosophy, or other. And then from there we're going to route it to a corresponding prompt chain. So let's go ahead and set up some prompt templates here. We'll change this out to be the brand new model that was released. And you'll see here that we define some prompt templates, a literature_template, a history_template, biology_template, philosophy_template, and a general_prompt. So, instantiate that. And so a RunnableBranch is basically just a list of, you know, a list of condition and runnable pairs. So it's essentially a list of two pools, a condition, runnable pair and a default runnable. So when we invoke this with an input, it's going to evaluate each condition in the list until it finds the first one that evaluates the true, then it's going to run the corresponding runnable with that input. If there's nothing provided, it's going to run the default runnable. So in this case, the RunnableBranch, you can see here it's just a bunch of two pools where we have the topic and then the associated prompt. So let's go ahead and construct that runnable prompt_branch. Now, what we're going to do is define a class that simply just classifies
>
> **[7:48](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chains-construct-sequences-of-calls?u=76281980&t=468)** what topic a particular query belongs to. So we're going to have this classifier function be created, and you can look at the LangChain documentation for this function. I'm not going to get too into that right now. Instantiate our language model. He's the brand new model that was released. Then we're going to bind this classifier function to the language model. We're going to have the output parser that's going to parse the output from that function. And then finally, construct a chain. And all this chain does is classify the topic of the query. So now, we're going to construct our final chain. And essentially, what this final chain is going to do is going to take a user's question, classify what this question is about. Once we have the classification of what this question is about, we're going to send it to the appropriate prompt template and get a response from the language model. So let's see that in action here. So now, we can execute this chain in one of two ways. One is using the invoke method. And so as you recall, for the invoke method, we're going to have to wait for the chain to complete before we get a response. Or alternatively, we can just stream the response. And because, you know, we're in a video here, we're going to stream it just for the aesthetics. So the questionnaire is, "Describe the Stoic philosophy for a good life." And here Stoic philosophy, originate from the ancient Greek, later developed in Rome, so on and so forth. And it goes here and it gives us the appropriate kind of response. So, what's happening is this input is going to go in,
>
> **[9:24](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chains-construct-sequences-of-calls?u=76281980&t=564)** be classified into one of these particular topics. Once we figure out what topic this question belongs to, we're going to get the associated prompt. In this case, the associated prompt here is the philosophy prompt. And this is going to be the prompt that eventually is sent to the language model, and that's how we go ahead and get our answer. Next up, we're going to talk about sequential chains in LangChain. All right, so now let's talk about sequential chains. So sometimes you might want a series of calls to a language model, and you might want to take the output from one call and use it as the input to another call. So a sequential chain is going to allow you to connect multiple chains and [compose](../../Glossary/Framework/Jetpack%20Compose.md) them into pipelines that execute some specific scenario. So we're going to go ahead and see that in action here. Of course, change this out to the brand new model that was released on January 25th, 2024. All right, so what we're going to do is have our necessary imports. We're going to instantiate our language models and we're going to set up a couple of templates. One template is for an initial rap. Another template is for a diss track. And so now we're going to create a template from the defined prompts. So prompt_template_one and prompt_template_two. Next, we're going to define the final chain. And so how the final chain works is it starts by taking the first prompt template that we define prompt_template_one, passing that to the language model,
>
> **[10:57](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chains-construct-sequences-of-calls?u=76281980&t=657)** and using a structured output parser to just get a string. With that string, we're going to pass it directly to the second prompt. And then we're going to take that, pass the language model, and get the output. So remember, this is kind of like the flow that's going to happen. What we pass into this chain is going to be sent here to the topic. And so this is going to generate something from the language model. Once that's generated from the language model, whatever the output is of this prompt template is going to get fed into here. And then we're going to have a response. So, to see it in action, of course you can invoke or you can stream. We are going to stream because it's always more fun to see streaming text. And what's happening behind the scene, the reason it's taking a little bit of time is that that first call to the language model, right, needs to execute. Once that's executed, it gets fed to the second prompt. And what we're seeing in front of our eyes is actually the diss track prompt the response of that. So that's what we see here. And so that's what we do with sequential chains. We're essentially taking the output of one language model call, passing it to another language model call. And that's basically how it works. All right, so let's talk about transformation chains. So very often you might need to modify the inputs as they move from one component to another. So to illustrate this, we're just going to create a fictitious transformation that receives a lengthy body of text. It's going to filter out all
>
> **[12:31](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chains-construct-sequences-of-calls?u=76281980&t=751)** but the first x number of characters and then send that to a series of steps to summarize it. So, to complete that, we're just going to download some text from Project Gutenberg, and this is essentially sending the text to a variable called meditations. You can see here the thing that we're actually downloading is Marcus Aurelius's meditations. Construct our prompt template, and now we have the runnable. So the runnable is just going to apply an anonymous function of Lambda to the text and just split it up. In this case, it's going to take the text, split it by new line characters, and just take, in this case, the characters from position 921 to 1021. Going to take that, pass into our prompt, which is, "Summarize this text:" Pass that to the language model. In this case, we're going to pass it to the newest language model from OpenAI. And we're going to go ahead and have an output. So I'm going to go ahead and make this, actually use the streaming output. All right, so we'll do that. So runnable.stream, and just go ahead and invoke the runnable sequence here. And so you can see we have the summary for that bit of text. So now, what we're going to do is have another prompt that just is going to rephrase the text in the style of a '90s gangster rapper.
>
> **[14:04](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chains-construct-sequences-of-calls?u=76281980&t=844)** Of course, we'll update the model that we're going to use and run. And so essentially it's going to take that text. In this case, the meditations text. It's going to take the characters from position 921 to 1021 and then pass it to this prompt where it is going to be asked to rephrase that bit of text. In the style of 90s gangster rapper passionately speaking to his homies. And we'll get back a rephrased response. And there we have it. Focus on being as real as a Roman, as a dude. Do your thing with deep seriousness, love, freedom, and fairness. Awesome. Forget all that other noise in your head. And there you have it. A great response, a great rephrasing of Marcus Aurelius meditations. And there we have it. So now we've got a good understanding of how chains work in LangChain. You've seen all of this implemented using the LangChain Expression Language, which is now the preferred method to construct chains in LangChain. I'll see you in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (14), [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) (4), [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) (1), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1)
> **Exercise Files:** template (11)
> **Code Identifiers:** prompt_template_one (2), literature_template (1), history_template (1), biology_template (1), philosophy_template (1)
> **Env Vars:** gpt (2), api (1), llm (1)
> **CLI Commands:** make (2)
> **Prerequisites:** set up (2)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)

#### [Agents: Let chains choose tools based on high-level directives](https://www.linkedin.com/learning/prompt-engineering-with-langchain/agents-let-chains-choose-tools-based-on-high-level-directives?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/agents-let-chains-choose-tools-based-on-high-level-directives?u=76281980&t=0)** - [Instructor] Agents in [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) are systems that use a language model to interact with tools. they could be used for tasks such as grounded question answering, interacting with APIs, or taking action. LangChain provides a standard interface for agents, a selection of agents to choose from, and example of end-to-end agents. So one thing I want to drive home here is, I know it sounds fancy, but at the end of the day, agents are really nothing more than a prompting strategy. A sophisticated prompting strategy, but at the end of the day, agents are just prompts. So let's talk about the difference between agents and chains. So the core idea of an agent is to use an LLM to choose a sequence of actions. So in chains, a sequence of actions is hard coded through the prompt template, through code. But an agent, a language model, is going to be used as a reasoning engine to determine what actions to take and in which order. So an agent essentially is a language model that is interacting with tools or environments. So agents involve a language model. So language model is going to make decisions about what actions to take. It's going to take those actions, observe the results, and then repeat the process until a desired outcome is achieved. So it's different from a chain in that a chain is nothing but a sequence of calls, whether to a language model or some other utility. So chains focus on the flow of information and computation, but an agent is focused on decision making and interaction with the environment.
>
> **[1:33](https://www.linkedin.com/learning/prompt-engineering-with-langchain/agents-let-chains-choose-tools-based-on-high-level-directives?u=76281980&t=93)** And these can be used for a wide variety of applications such as personal assistance, question answering, chat bots, querying tablet or data, interact with APIs, so on and so forth. Essentially an agent just uses an LLM as a reasoning engine, but it's still using prompts. It's nothing more than a prompting strategy. An agent though it needs access to tools and it needs a toolkit. So tools and toolkits just are what provide additional functionality and capabilities to agents. So tools are individual components that perform a specific task like retrieving information from an external source or processing data. Toolkits on the other hand, these are collections of tools that are designed to work together and provide a more comprehensive set of functionalities. So why do agents even need tools? Well, when we provide an agent with a tool, it becomes powerful. It becomes a system that can execute and implement something on your behalf. So the combination of an agent's decision making ability with the functionality provided by tools is going to allow it to perform a wide range of tasks efficiently. Agents also need memory, and we'll go into memory deeper a bit later, but an agent and LangChain needs memory to store and retrieve information during decision making. So memory is what allows an agent to really maintain context and remember the previous interactions it had. This is crucial for getting that personalized and coherent responses. So let's go ahead and instantiate a language model. I like to set the temperature to zero
>
> **[3:08](https://www.linkedin.com/learning/prompt-engineering-with-langchain/agents-let-chains-choose-tools-based-on-high-level-directives?u=76281980&t=188)** every time I'm using an agent, every time I need a language model to, say, kind of a structured output or adhere strictly to a prompt, I set that temperature to zero, so that's what we're going to do here. Now that we've instantiated our language model, let's equip it with some tools. So we can either pass tools as arguments when initializing the toolkit, or either individually initialize those desired tools. So tools can be selected from the native tools that LangChain has available. Or you could just define functions by yourself if you want to, you can look at the documentation to get a idea of some of the tools that LangChain has available to it. Feel free to scroll through the documentation. What I'm going to do is I'm going to use DDG search, so that's DuckDuckGo search. The reason I'm using DuckDuckGo is because it does not require an API key, so that's one less API key that you have to manage. We're going to use the LLM math tool and a Wikipedia tool that will search Wikipedia. So we are now equipping this language model with some tools. Now we need to instantiate and initialize the agent. So there's two ways that you can initialize the agent using the agent executor or initializing the agent. Let's first talk about the initialize agent. So this is essentially a function that's a convenience function provided by LangChain and it just simplifies the process of creating an agent. It's going to take the agent class, the language model, and a list of tools as an input, and then it'll automatically initialize the agent with that language model and the tools. So let's go ahead and set up a query here.
>
> **[4:41](https://www.linkedin.com/learning/prompt-engineering-with-langchain/agents-let-chains-choose-tools-based-on-high-level-directives?u=76281980&t=281)** Who's the current chief AI scientist at Meta? When was he born? Where was he born? What's the current temperature there? That's going to be the query. We're going to initialize the agent, we give it the tools, we give it the LLM. And this agent right here, what we pass here is zero-shot-[react](../../Skills/Web%20Development/React.js.md)-description, this is essentially the agent type. And again, remember, it's nothing more than a prompt. We'll inspect the prompt a little bit later when we see how to do an agent and create an agent in the new format. But now we're going to go ahead and run this agent with that query. Take note of this deprecation warning, we're going to see how to create agents in a new way, starting in v0.2 in a little bit further down in this notebook. So here we're getting an idea into the thought process of the language model. So you recall we gave the language model this query, and you can see it's thinking to itself. First I need to find out who the current chief scientist is. After that I need to do this and then I need to do that. And so it starts doing each one of these. So you can see here the thought it has, okay? It does a DuckDuckGo search. It gets Yann LeCun's place of birth, finds out that he was born somewhere in France, and now it says, "Okay, I need to find the current temperature there." So it's going and getting the current temperature in that place and returning it to us. There we have it. Now let's talk about the other way that we can initialize an agent. And that is using the agent executor.
>
> **[6:15](https://www.linkedin.com/learning/prompt-engineering-with-langchain/agents-let-chains-choose-tools-based-on-high-level-directives?u=76281980&t=375)** So the agent executor is responsible for executing the agent's actions and then managing the agent's memory. So it's going to take an agent, a set of tools and an optional memory object as its input. And so that's how we instantiate it here. We got a prompt, we're going to instantiate the language model, instantiate our set of tools, and then we're going to create a open AI function agent. We're going to send that to the agent executor, and then have it execute this query. All right, as you can see here, it's thinking through and figuring out what the actual current temperature is, and we'll get a response in just a second. All right, so the forecast tonight is negative 13 C for Monday January 22, 2024. So it gave us the weather from a few days ago. I think that's just a fault of the DuckDuckGo search, not the agent itself. All right, so let's talk about agents and memory now. So as we stated before, an agent is stateless, which implies that it can't recall past interactions, but we can make it remember by feeding it with the previous chat history. So it's important to note that the variable name of chat history needs to always be chat_history to be compatible with the prompt that we're going to use here. If we modify the prompt, we can use a different variable name as well, but it's essentially going to work like this. So it's, get our messages set up
>
> **[7:48](https://www.linkedin.com/learning/prompt-engineering-with-langchain/agents-let-chains-choose-tools-based-on-high-level-directives?u=76281980&t=468)** and we're going to have message history, and we're going to have an agent with history by making it a runable with message history. Pass it the agent executor. This is needed right here because in most real world scenarios, the session ID is needed. Not going to be used for what we're doing here because we're just using a simple in-memory chat message history. And then we can now invoke. So you can see here we're running the same query that we defined earlier, which is what's the current weather in Winnipeg? So it's going to go through here, get the current weather in Winnipeg. And then I'm going to hit it with the follow up query. What do you reckon I should wear then? And so it has context of what I asked it before, so it knows that it's cold in Winnipeg. And it's going to say, given the current conditions of this temperature, here's how you should dress. So pretty cool, an agent with history. Next up we're going to talk about how to create agents in LangChain v0.2 and beyond. Starting in LangChain v0.2, they're actually going to stop using initialize agent in favor of individual agent methods because they believe that this is going to be more clear. So you take a look at the affected agents in the change log here, and the most recent change log was from January 5th. And you can look at the agents that were actually affected
>
> **[9:24](https://www.linkedin.com/learning/prompt-engineering-with-langchain/agents-let-chains-choose-tools-based-on-high-level-directives?u=76281980&t=564)** by kind of scrolling down here. The chat agent, conversational agent, conversational chat agent, so on and so forth, so we're going to see that in action here. So what we're going to do is just create kind of a retrieval agent, basically is what we're going to set up here. And you'll see the flow here. We're going to instantiate the language model, all right? Once we've instantiated the language model, all this is doing right here is just some retrieval stuff, not necessarily pertinent to the construction of an agent. Here we're setting up the tools. Again, the tools we're creating is the search tool, which is going to use DuckDuckGo search as well as a retrieval tool. Here's the prompt that we're going to use for the agent. And now this is the part that becomes impacted here. So we're going to define our agent using the create_openai_functions_agent, give it the language model, the tools, and the prompt, and then we create the agent executor. And we can invoke like so. And so this pattern, still being kind of figured out, the documentation is not fully flushed out for how to create agents in this new way. But then again, we have a bit of time to create agents how you've learned before. But starting in v0.2 and beyond, we're going to be using this pattern. And by then hopefully the documentation becomes a lot better. And here we have the answer that we're looking for. This answer is for the question, how can LangSmith help with testing? And there we have it.
>
> **[11:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/agents-let-chains-choose-tools-based-on-high-level-directives?u=76281980&t=660)** Now we know all about agents in LangChain and how to use them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (9), [React](../../Skills/Web%20Development/React.js.md) (1)
> **Env Vars:** llm (4), api (2), ddg (1)
> **CLI Commands:** make (2), find (2)
> **Versions:** v0.2 (4)
> **Documentation:** the documentation (4)
> **Prerequisites:** set up (3)
> **Code Identifiers:** chat_history (1), create_openai_functions_agent (1)
> **Analogies:** such as (2)

#### [Memory: Persist application state between runs of a chain](https://www.linkedin.com/learning/prompt-engineering-with-langchain/memory-persist-application-state-between-runs-of-a-chain?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/memory-persist-application-state-between-runs-of-a-chain?u=76281980&t=0)** - [Instructor] If you've seen the movie "Finding Nemo," then you might remember the character Dory, who is notorious for her short term memory loss. LLMs, especially chat-based ones are a lot like that. You need a way to ensure your system doesn't pull a Dory. Luckily, [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) has a memory module that will help you out. So what is memory in LangChain? It's essentially just a module that's responsible for persisting state between calls of a chain or an agent. And this is going to help the language model remember previous interactions and use that information to make better decisions. So this is going to provide a standard interface for a persisting state between calls of a chain or agent, which is then going to enable the language model to have memory and context. So what does the memory module do? So it enables, again, the language model to have memory and context. This can help the language model make more informed decisions if it's able to remember past interactions. The stored information can be accessed and utilized during subsequent interactions. Every memory system in LangChain does two tasks. It reads and it writes. So every chain has some core logic that requires some specific inputs. Some inputs are going to originate from the user while others are going to derive from memory. So during the invocation of a chain, what's going to happen is that it's going to access memory twice,
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/memory-persist-application-state-between-runs-of-a-chain?u=76281980&t=94)** if it has memory. First it's going to read a memory to supplement the user input before executing the core logic. Then after it processes, but before responding, it's going to write the current runs data to the memory for feature reference. So there's a few different types of memory in LangChain. The first one we're going to talk about is the conversation buffer memory. This is pretty straightforward, simple memory that's just going to store chat messages in a buffer and pass them to the prompt template. I'm going to show you right up front how to use memory with LangChain expression language. And then the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the notebook, we're just going to use the legacy chain interface, but you should be able to abstract away quite easily. So here we're just setting up the system message. In this case, I'm going to ask the language model to be Spider Punk, Hobart Brown from Earth 138. I'm going to instantiate the language model. And we set up our prompt template. This is going to be a chat prompt template. I want you to pay attention to the fact that we have the messages placeholder here. And so now we're also going to instantiate conversation buffer memory, and let's look at the memory variables. So we have the history memory variable, and right now it is empty. The reason it's empty is because we haven't even interacted with the language model yet. Now we're going to go ahead and set up our chain, the runnable pass through, we're going to assign history to it and it's going to have this variable name. I'm going to send all that into the prompt,
>
> **[3:06](https://www.linkedin.com/learning/prompt-engineering-with-langchain/memory-persist-application-state-between-runs-of-a-chain?u=76281980&t=186)** send all that to the LLM. And then finally, we're going to send some inputs to the LLM and invoke the chain. And let's take a look at what we have. So once the chain is invoked, we also just got to make sure that we save the context, right? So we have the inputs, this is what we're going to save for the memory, and the output is going to be the response context. So in this case, all this stuff here. So let's take a look at the memory variables. Here we got the history, and what's the history? It's just the human message chat message, right? And so now let's run this again with a different input. And you can see here it remembered my name, Harpreet. So that's the nice part about having memory. So let's go ahead and see some other types of memory. We'll look first at the conversation buffer window memory. This helps keep conversations fresh. So just keeps track of past interactions in the conversation. So it does this by maintaining a list of the most recent interactions and then only using the last K of those interactions. This is going to help ensure that the buffer doesn't become too large and it allows for a sliding window of the most recent interactions to be kept. This type of memory is really useful for keeping the history of past interactions small and manageable. By only capturing the most recent interactions, it's going to help prevent the buffer, again,
>
> **[4:40](https://www.linkedin.com/learning/prompt-engineering-with-langchain/memory-persist-application-state-between-runs-of-a-chain?u=76281980&t=280)** from becoming too large, which can be overwhelming. And you might even exceed the context window of your language model. So let's go ahead and take a look at conversation buffer window memory. I'm going to use this with a legacy chain, the conversation chain, and we'll run it like this. It's the same prompt on passing, but we're, you know, we're not using a system prompt or anything like that. And we'll just run the next few chains because I want you to see what is happening. So you could see here the first time we ran the chain didn't really say anything about the previous interactions. When we run it again, it's got the history of the last interaction. We run it again, it's got the last couple of histories, right? And when we run it again, it's got the last few histories. And then finally this chain, you'll notice that we're going to drop off these and only keep the last three interactions. So we'll wait for that to finish. And we can see here that once we got to this point, remember we're only keeping the last three messages. And so we've had three interactions already, right? We've had this first, second, third interaction, and the next run, we'll see that this part has popped off. So it's no longer there, right here. And so that's what conversation buffer window memory does.
>
> **[6:21](https://www.linkedin.com/learning/prompt-engineering-with-langchain/memory-persist-application-state-between-runs-of-a-chain?u=76281980&t=381)** It just is a sliding window of the last K interactions. Now let's see conversation summary memory. So in order to condense information from a conversation over time, we can use conversation summary memory. This memory type is designed to keep track of all interactions that happened during a conversation, and it could be useful to only use the most recent ones. So you would need to use conversation summary memory when you just want a concise representation of the conversation's history without using too many [Tokens](../../Skills/Web%20Development/Tokens.md). This is going to allow the model to understand the overall context and key points of the conversation, while not flooding the context window with excessive details. You can tell if you need conversation summary memory if you find that the conversation history is becoming too long and complex for the model to handle effectively. By using conversation summary memory you can condense the conversation into a more manageable summary, which is going to make it easier for the model to process and respond accurately. So we'll go ahead and run it here. So we're not giving it a system message or anything, we're just saying, hey, what's up? And so we see the current conversation, hey, what's up? And then we run it here. The current conversation has a summary and we can see the summary again. And so that's it. That's the memory module in LangChain. We can use this in agents, we can use this in chains.
>
> **[7:55](https://www.linkedin.com/learning/prompt-engineering-with-langchain/memory-persist-application-state-between-runs-of-a-chain?u=76281980&t=475)** And it's really useful in situations, for example, like a chat bot or a virtual assistant when you need that language model to have context of previous interactions. And there we have it. With this video we've seen all of the core components of LangChain. The next section, we'll move on to some basics of prompting and then a deep dive into prompt templates.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (7), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Tokens](../../Skills/Web%20Development/Tokens.md) (1)
> **CLI Commands:** make (4), find (1)
> **Exercise Files:** template (3)
> **Env Vars:** llm (2)
> **Prerequisites:** set up (2)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)


### 4. Basics of Prompting

[↑ Back to Table of Contents](#table-of-contents)

#### [Prompt basics](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-basics?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-basics?u=76281980&t=0)** - [Instructor] In the early days of language models, you needed a different model for every language task. Those days are gone. Transformers and transfer learning have made things much simpler. You have one model that is like a Swiss Army knife that can be used for various language tasks. You give this model a prompt, which essentially is just a string of text, and the model will give you a answer. The better your prompt, the better the answer. So what's the big deal with prompts? Well, it is a phenomena called in-context learning. And this refers to methods for steering an LLM's behavior towards your desired outcome without updating the model weights. This approach is rooted in practical testing, and the success of various prompt strategies can differ significantly across models. This means that it's up to you to hack around and play around with prompts to see which one is going to work for your task. As a result, this means that there is extensive trial and error, along with some intuitive reasoning, both of which are going to be essential. Prompts are the key to using LLMs effectively. This is about smartly communicating with a model to get your desired results. And you accomplish this by creating clear, concise, and easily understandable prompts. But here's the thing, there's no one perfect prompt. You need to test the different ones to see which one is going to work best. So what makes a good prompt? Well, there's four elements to a prompt. One is the instruction. This is going to guide the model for its task.
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-basics?u=76281980&t=94)** Two is context. This is just extra background information that's going to refine the model's answer. This can be inserted manually, or sourced from [Databases](../../Skills/Software%20Development/Databases.md) or APIs. Three is input data. This is often a question or topic that you're feeding to the model. And finally, the output indicator. This is going to signal how you want the model to answer. Grasping the makeup and elements of a prompt is key to using language models effectively. You don't need all these elements every single time. It's going to vary by task. The key is understanding and combining them effectively. It's often best to put the instruction towards the end of your prompt. This ensures that the model will hone in on the task instead of just expanding on the background. As you refine the art of [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md), the guidelines I just presented are going to give you a reliable starting point for designing effective prompts.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Databases](../../Skills/Software%20Development/Databases.md) (1), [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md) (1)
> **Definitions:** means that (2), essentially is (1), is a  (1), refers to (1)
> **Env Vars:** llm (1)
> **Best Practices:** the key is (1)
> **Speakers:** - [instructor] (1)

#### [Principles and tactics for prompting](https://www.linkedin.com/learning/prompt-engineering-with-langchain/principles-and-tactics-for-prompting?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/principles-and-tactics-for-prompting?u=76281980&t=0)** - [Instructor] In this video, I want to share some principles for getting the most out of your prompts. So we'll start the notebook off as we usually do, install our dependencies and set our API key. In this case, we're going to be using a model from the HuggingFaceHub. So that's the API key that you want to have handy. Next, we'll import our libraries and define a function that's going to execute a LLM chain for us. Essentially just taking a prompt, instantiate a model from the HuggingFaceHub, construct a llm chain, and then invoke that chain with some input. The model we're going to pull from the hub is going to be Mistral-7B-Instruct-v0.2. So now we're using the HuggingFaceHub inference endpoint. So we're not actually going to be downloading the model locally. The first principle I want to talk about is clarity. When you're constructing your prompts, clarity is key. Ambiguity is going to be your biggest enemy. The clearer your instructions are, the more accurate the model's response will be. So make sure you use delimiters like triple quotes, back ticks or angle brackets to distinctly separate your instructions from the context and content of your prompt. This way, you're ensuring that the model doesn't confuse the instructions. You're giving it with the context that you're providing it. After we instantiate our text to summarize and the template, you'll notice that I'm going to be constructing a prompt template. When constructing this prompt template, you'll notice that I'm adding these [Tokens](../../Skills/Web%20Development/Tokens.md) here. The prefix and suffix tokens, these are special tokens
>
> **[1:33](https://www.linkedin.com/learning/prompt-engineering-with-langchain/principles-and-tactics-for-prompting?u=76281980&t=93)** that the model is going to use to know when the instruction starts and ends. This isn't really important for the OpenAI models that we'll use in the majority of this course, but it becomes really important when you're using smaller language models. In the case of Mistral-7B-Instruct, we need to include these special tokens because that's how the instruction model was trained. Putting it together, our prompt is going to look something like this. Now we can run the LLM and get a result, and you can see here we have the result. Providing clear and detailed instructions when asking 'em all to perform a task is crucial, so on and so forth. The next principle I want to discuss is asking the model for structured output. So a well-structured output is going to be easier to interpret and process, especially for downstream tasks. So it's important that you define the format you need upfront. If you're looking for data that could be easily parsed, then request outputs and formats like [JSON](../../Skills/Web%20Development/JSON.md) or XML. This is going to ensure that you get your output in a structured and organized manner. So here I'm just essentially asking the model to come up with some imaginary movie titles and give me a JSON object that has the movie ID title, director and genre. And we construct our prompt template, and it's going to look like this. Send that to the model, and you'll see here that it does in fact output as a JSON.
>
> **[3:11](https://www.linkedin.com/learning/prompt-engineering-with-langchain/principles-and-tactics-for-prompting?u=76281980&t=191)** You can also ask a model to structure its output based on whether or not a condition was satisfied. So the first thing that you want to do is, you know, ask the model to determine if the text meets some specific criteria. For example, does this text contain a sequence of instructions? Then the model can analyze this text to confirm if the criteria is actually fulfilled. Then the model will respond based on whether the conditions are met. If it's met, then proceed with that specified action. If not, then state that this condition wasn't satisfied and you know, tell the model to do something else. So you want to clearly convey whether the conditions were satisfied and the reasoning behind the response. So you'll notice we're asking the model to do that here. We're giving the model some input text. Essentially, this input text is how to reset your password. The actual instructions we're telling the model is that you're going to get some text that's going to be enclosed in these angle brackets, determine if this text has some type of procedure. If it does, then give us a list formatted like so. The title and the individual steps. If it doesn't, then just respond with no procedural guide found. And here's the input that we give the text. And instead you the prompt template. Here's what the prompt is going to look like. Run the llm_chain. And then we can print our result. And you can see here that it does in fact
>
> **[4:45](https://www.linkedin.com/learning/prompt-engineering-with-langchain/principles-and-tactics-for-prompting?u=76281980&t=285)** structure it the way that we want. The next principle is a strategy for complex queries. Here you can ask the model to outline its thought process. So when you're dealing with a complex query, you want to allow the model to have enough time to process information thoroughly. So there's two strategies you can use here. The first strategy is detailing the steps needed to complete a task, and then demanding output in a specific format. So for complex tasks, you want to ask the model to break them down into smaller steps. Just as humans often find step-by-step instructions helpful, you'll find that the model will find it helpful if it could follow a logical sequence or chain of reasoning before arriving at a final answer. The second strategy is instructing the model to work out its solution before jumping to a conclusion. This is going to help the model to thoroughly process the task at hand before delivering the output. So here you'll see we're prompting the model with the falling template, like determine if the student solution is correct or incorrect. You know, this is how you're going to do it. Work out the solution, compare your solution to students. Don't decide if the student solution is correct until you did the problem yourself. Here you can see that we're a pending in suffix here to just respond with the following format. And then this is the actual input question that we're going to send the model. So we run that here, create a prompt template,
>
> **[6:19](https://www.linkedin.com/learning/prompt-engineering-with-langchain/principles-and-tactics-for-prompting?u=76281980&t=379)** and this is the fully constructed prompt. And here is the actual result. So you see here that the model is going to respond in the way that we ask it to. So of course, the result you're looking at here is going to be a bit underwhelming. This is from a smaller model, it's a 7 billion parameter model. But if you were past this prompt to [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md), it would do a excellent job at solving that question. Finally, I want to talk about the importance of balancing context and clarity. What I mean here is don't overload your prompt. Context is essential, but too much can be counterproductive. So you want to strike a balance. So prioritize the most critical information in your prompt. For the larger models that we're going to be dealing with throughout the remainder of this course. For example, GPT-4, it's smart enough to know when it requires additional context. So if it does require additional context, it will request it. And to sum it all up, these are the principles you want to keep in mind when constructing your prompt. Clarity is essential. You are able to request a structured output if you need it. You can ask the model to verify if some conditions are met. You can prompt the model in such a way that it could handle complex queries. And you always want to balance context with clarity.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Tokens](../../Skills/Web%20Development/Tokens.md) (4), [JSON](../../Skills/Web%20Development/JSON.md) (3), [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) (2)
> **Env Vars:** json (3), api (2), llm (2), gpt (2), xml (1)
> **Exercise Files:** template (7)
> **CLI Commands:** find (3), make (1)
> **Analogies:** for example (2)
> **Code Identifiers:** llm_chain (1)
> **Versions:** v0.2 (1)
> **Definitions:** is a  (1)


### 5. Prompt Templates Deep Dive

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction to prompt templates](https://www.linkedin.com/learning/prompt-engineering-with-langchain/introduction-to-prompt-templates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/introduction-to-prompt-templates?u=76281980&t=0)** - [Instructor] Prompt templates are a core concept and object in [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md). Before we get into prompt templates, just refresh your memory on the structure of a prompt. So remember that a prompt has multiple components, the instructions and the external information or context as well as the user input or query plus some output indicator. Not all prompts will require all of these components, but a good prompt, will use two or more of them. So what is a prompt template then? So a prompt template is a tool that we use to produce a prompt in a consistent and repeatable manner. So once you've laid out a prompt, you've got the instructions, you've got a place to put external information or context, a place to put the user query, you can turn that into a template. And so essentially what a prompt template is, is just a text string. This is known as the template. And this template can take input from users to generate a prompt. The template might have the instructions for language models, some examples to do in context learning or a specific question to guide the model's response. It helps to create a more accurate and efficient prompt generation process. And a prompt template will take some input and then format the template to include that input. Let me show you this in action. So we'll start by importing the prompt template from langchain, and then we'll instantiate the template itself. So the template itself is a string, and this string will have a place for the query. So we start off by importing the prompt template.
>
> **[1:37](https://www.linkedin.com/learning/prompt-engineering-with-langchain/introduction-to-prompt-templates?u=76281980&t=97)** We'll define a string, we'll call it template. Template will have a place for a user input, so an input variable that will be wrapped in curly brackets. So we go ahead and instantiate the template. And there's two ways that you can actually instantiate this prompt template. So you can instantiate the prompt template using the initializer. And if you're using the initializer, you have to explicitly pass in a argument called input variable, which will be a list of all the input variables in your template. And in this case, we have one input variable and that is query. And you can see the prompt will look like this. And so what we could do now is we can pass a value for query and the prompt template will format it appropriately. So you can see here the query is being passed to the prompt template and parsed within the prompt template. And this is what ultimately gets sent to the language model. The recommended way to instantiate a prompt template, however, is using the from template method. So when you instantiate this using from template, all you have to do is pass in your template string and langchain knows exactly what to do under the hood. And you can see here using the from template method, the input variable is already there for us. And you can format in the same exact way. And you'll see that we end up with the same exact prompt that we end up passing to the large language model. So let's go ahead and pass this to the large language model and see what we end up with. So here you'll notice we're going to use gpt-3.5-turbo.
>
> **[3:13](https://www.linkedin.com/learning/prompt-engineering-with-langchain/introduction-to-prompt-templates?u=76281980&t=193)** I find generation times tend to be a lot quicker with gpt-3.5turbo, so we'll make use of that. We've instantiated our chain, so we have our prompt template, the language model, and the output parser. And now we can go ahead and invoke the chain to get a response. And of course we can let that response run or we could stream the response like so. So now you can see here we have our llm chain. Recall that our llm chain is constructed using the prompt template, the language model, and an output parser. So we don't need to explicitly format the prompt when we are using the langchain expression language. We just pass in the value for the input variable. So you can see here now we're going to pass a new query into the llm chain. So the way the llm chain will work is it'll take this dictionary here, it'll grab the value for query, in this case why is a SoftMax function used in NNs. It'll format the prompt template appropriately. It'll take that prompt template, pass it through to the large language model. The large language model will get a response and that response will be parsed for us. So let's see this again in action here. And you can see we have the answer. So now we can pass in another query just to kind of show you for completeness. In this case, I'm going to ask the language model to gimme the training loop for psychic learn. And let's observe the answer.
>
> **[4:48](https://www.linkedin.com/learning/prompt-engineering-with-langchain/introduction-to-prompt-templates?u=76281980&t=288)** So sklearn doesn't have a built-in training loop. You would typically use the fit method to train a model, so exactly right. Awesome. So you could use [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) string manipulation to create a prompt, but prompt template is more legible and it works with any number of input variables. So here what we're going to do now is define a function. This function, we'll call it the get advice function. It's going to create a chain for us essentially. And all we need to do to this function is just pass in a string input for a topic on which we want to get advice from. So you can see here we instantiate our language model, define the prompt template. In this case, we're going to be using the from template method, which is recommended, construct our chain, and then we'll stream our output. So here we'll get advice on how to balance so many priorities when you don't have free time. And so now we have like a reusable kind of function to get advice. And you can see here we'll get an advice for learning new things. Great, and you can see here the language model gives us great advice. And so all this is doing is, it's taking the string that we have defined here and here, for example, balancing so many priorities, getting over addiction to learning new things, it's going to inject that into the topic and send it to the language model. Great, so we can see here that the prompt that we've used right now is just a single
>
> **[6:21](https://www.linkedin.com/learning/prompt-engineering-with-langchain/introduction-to-prompt-templates?u=76281980&t=381)** input variable prompt. As it turns out, lanechain allows you to create prompts with multiple input variables, and that's what we're going to see how to do next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (4), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **Exercise Files:** template (34)
> **Definitions:** is a  (4), known as (1)
> **CLI Commands:** find (1), make (1), python (1)
> **Best Practices:** recommended (2)
> **Versions:** 3.5 (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Multi-input prompt templates](https://www.linkedin.com/learning/prompt-engineering-with-langchain/multi-input-prompt-templates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/multi-input-prompt-templates?u=76281980&t=0)** - [Presenter] We're going to pick up where we left off in the same notebook, but this time we're going to talk about multi input prompts. So the first video we saw about prompt templates showed you how to construct a prompt template with just one input variable. You could, however, construct prompt templates with more than one variable. And so we'll do that here. So the first thing I want to start by doing, of course, is always import the prompt template and import a language model. We'll go ahead and initialize the language model, and then we'll create a function here to construct a prompt template. Now I want you to notice that I'm using the prompt template here using the initializer. So I'm explicitly stating what the input variables are and then passing the prompt. When we do it this way, we're going to have to format the prompt template. And this function right here will simply just print the prompt for us. And then finally, we will go ahead and invoke the chain with that prompt. So let's go ahead and look at that. So you can see here we get the answer. In this case we're not streaming, so it'll take just a second to get the answer up. All right, so you can see here we were not streaming, so we just printed the answer out, and here you can see we've formatted the movie with this value, "Jatt da Pajama Uuchaa Ho Gayaa," with the actor, "AP Dhillon." And then we get the genre as well as the synopsis, exactly what it is that we had asked for, the movie and the actor, and it gave us a synopsis and genre. So now what I want to do is go through a live exercise for you. We are going to rewrite this code together.
>
> **[1:36](https://www.linkedin.com/learning/prompt-engineering-with-langchain/multi-input-prompt-templates?u=76281980&t=96)** Using not the initializer, but just the recommended method, which is using "from" template. And then we'll go ahead and add in streaming as well. So this is inevitably something you might come across in the future. You'll have to rewrite code that is not using the link chain expression language into using the link chain expression language. And I want to show you how to do it live so that you're not kind of overwhelmed by it at all, and it's really not that hard. So we start with our prompt template. We'll just add the "from_template" method because we're adding from template, we can remove the input variables and just have the template be the argument. We're going to use lane chain expression language, so that means we don't need to explicitly format our text so we can actually remove this. We'll remove this part. We don't really care about printing out the prompt. And now what we're going to do is rewrite this last part by first constructing an LLM chain. So let's create an LLM chain. This LLM chain will consist of the prompt, which will be passed to the language model, and we'll be sure to use the output parser so that we get some clean text out. This LLM chain will then be invoked, and in this case we'll invoke it by passing the movie title as well as the main actor. Let's go ahead and do that. So remember, to invoke it, we need to pass a dictionary. The dictionary's keys are going to be the movie title, And this is the argument that we're passing in.
>
> **[3:11](https://www.linkedin.com/learning/prompt-engineering-with-langchain/multi-input-prompt-templates?u=76281980&t=191)** And again, we also need to pass in the main actor, so this will also be a key like so. And we'll pass in the main actor. Right, so in lane chain expression language, we invoke the chain by passing in a dictionary, the dictionary's keys will be the input variables of the prompt and the value is whatever it is that we want to put inside that placeholder. Of course, we can send this to something called response, maybe we call it response variable. And this way it'll go ahead and just print out the output. Now if we want to change it to streaming, we can just go ahead and actually comment this out. For streaming, we say "for chunk in" the "llm_chain.stream,"
>
> **[3:56](https://www.linkedin.com/learning/prompt-engineering-with-langchain/multi-input-prompt-templates?u=76281980&t=236)** and we're going to pass in this exact same dictionary, so I'll go ahead and copy that dictionary here.
>
> **[4:08](https://www.linkedin.com/learning/prompt-engineering-with-langchain/multi-input-prompt-templates?u=76281980&t=248)** So pass in the dictionary like so. And what we're going to say is "print(chunk)," I have that as the end, and "flush=True." Great. And so now we can go ahead and invoke the chain like so. And you can see here, it gives us the answer that we were looking for. And you can see here, again, just printed a couple more times just for illustrated purposes, and we get the answer just as we wish. So we get a synopsis and the genre for a fictional movie called "Amritsar: 1984", starring "Gurdaas Mann." And then another one starring "Diljit Dosanjh." Sounds like awesome movie tiles and something that I would like to watch. All right, so now that we've seen how to construct multi input prompt templates, we are now going to look at chat prompt templates. So I'll see you in the next video.

> [!info]- Semantic Content
>
> **Exercise Files:** template (9)
> **Env Vars:** llm (4)
> **Code Identifiers:** from_template (1), llm_chain (1)
> **Cross-References:** in the next (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [presenter] (1)

#### [Chat prompt template](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chat-prompt-template?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chat-prompt-template?u=76281980&t=0)** - [Instructor] Let's wrap up our introduction to prompt templates by talking about chat prompt templates. The prompt to a chat model is a list of chat messages. Each chat message is associated with content and an additional parameter called role. For example, in the OpenAI chat completions API a chat message can be associated with an AI assistant, a human, or a system role. Chat prompt template dot from messages accepts a list as the argument, and each element in that list is a message representation, which is usually a tuple of the role and content. You can also pass in instances of message prompt templates like system message, prompt templates, or human message prompt templates. So to summarize this chat prompt templates dot from messages, we'll accept a list. Each element in the list is a message representation. One option for the message representation is a message prompt template subclass like the system message prompt template. So we're going to see all of this in action right now. So of course begin by importing what we need. We'll instantiate a LLM here using GPT 3.5. I'll set the temperature up a little bit to be 0.8. You can toggle around with that and you'll see here chat prompt template dot from messages. We're passing a list
>
> **[1:33](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chat-prompt-template?u=76281980&t=93)** and each element in that list is a tuple and we have the role followed by the content of the role. And now take a look here, you'll see that we have some placeholders for input variables. So without me running the cell, take a moment, take a second and tell me what you think the input variables in this chat prompt template are. If you said name and user input, you are right. So the template itself has a attribute called messages and you can look at the message and this is what I was talking about when I said instance of a message prompt template. So when we initialize the chat prompt template like we did here using the tuples under the hood [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) will go ahead and automatically convert it into the appropriate message prompt template. So in this case, I've got the system human and AI message prompt templates. We can of course format the messages. In this case we'll format the message so that the name of the AI is Robo Talker and the user input is talk robo to me. So I'll go ahead and do that and we'll see what the message looks like once it's been formatted. So your helpful yet slightly quirky and cheeky AI bot, your name is Robo Talker and the human message is talk robo to me. So of course we can use LLM dot invoke and print out the messages
>
> **[3:06](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chat-prompt-template?u=76281980&t=186)** or we can construct a chain using the LangChain expression language. So let's go ahead and do that. Of course, we can invoke the chain like so, but we'll go ahead and just stream the output. And you can see here robo talker is in fact talking robo to us. So the other way to instantiate the chat prompt template is to just create instances of the appropriate message prompt template, so the system message, human message, and then build that like so. So our template will look like this. So we have a chat prompt template object with the input variable and the appropriate messages. And you'll see here, this is the exact same object that we have up here is a chat prompt template. You can in fact confirm that by going type template. So we'll reinstantiate that and see the type here chat prompt template. Right? So going back here, we're doing the same thing. We've got a chat prompt template, input variables of course is going to be text. The messages is a list of messages in this case, just a system message and a human message. We can get the response like so by invoking it and formatting the messages or use a preferred method of constructing a LLM chain. Let's go ahead and do that. And we can pass in to the LLM chain a dictionary.
>
> **[4:42](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chat-prompt-template?u=76281980&t=282)** The dictionary has a key, which is the same as the input variable for the template. And the value is what we want that placeholder to be. And of course we'll go ahead and stream the output. And there we have it. We get a response that is actually quite funny, but that's because we asked them to be super freaking rude. So that's it for your introduction to prompt templates. We're going to go and take a further deep dive into prompt templates in the next several videos. So I'll see you in the next one.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (2)
> **Exercise Files:** template (18)
> **Definitions:** is a  (7)
> **Env Vars:** llm (4), api (1), gpt (1)
> **Versions:** 3.5 (1), 0.8 (1)
> **Cross-References:** in the next (2)
> **UI Navigation:** toggle (1)
> **Analogies:** for example (1)

#### [Serializing prompts](https://www.linkedin.com/learning/prompt-engineering-with-langchain/serializing-prompts?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/serializing-prompts?u=76281980&t=0)** - [Instructor] Serializing prompts in [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) allows you to efficiently store and share your prompts. So instead of relying solely on [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) code, you can actually save prompts as files. This makes it much easier to version and distribute your prompts. Some key features of prompt serialization in LangChain is that it supports both the [JSON](../../Skills/Web%20Development/JSON.md) and YAML formats. These are human readable formats and they're popular and ideal for serialization. You can choose to store everything in a single file or distribute components like templates or examples across multiple files. This flexibility caters to different use cases, whether you're dealing with lengthy templates, extensive examples, or reusable components. LangChain also provides a singular entry point to load prompts. This streamlines the process regardless of the prompt type. There's a couple core design principles for the serialization of prompts in a LangChain. One is that they be serialized in human readable formats. So JSON and YAML are the primary methods for prompt serialization because of their readability. Other things like examples might support different serialization methods. Also, adaptable storage. So whether you prefer to consolidate a file or you know, segmented storage, LangChain can accommodate both of those approaches. By leveraging serialization in LangChain you're ensuring that there's a seamless experience for managing and sharing your prompts, which is essential
>
> **[1:32](https://www.linkedin.com/learning/prompt-engineering-with-langchain/serializing-prompts?u=76281980&t=92)** for prompt management. So here what we're going to do is instantiate a prompt template. So you can see here we just have our template. We'll go ahead and in this case, we will instantiate from the initializer, and then we're going to save it. So you can see here the prompt is saved as a JSON object. You can also print the contents of the JSON object out like so. Now, you can also load this prompt later if you'd like. So illustrate an example of loading a prompt by just downloading one from the LangChain prompt hub, right? You can see here, we just download a prompt, and here's the input variables to the prompt. We have summary and new lines. You can also, of course, print the prompt as well after you load it. And you can see what the prompt looks like. So serializing prompts in LangChain is as easy as that. A bit later in the course, you'll be introduced to the LangChain Hub and the Lang Smith, and you'll see just how you can actually properly manage prompts in a more version controlled manner. But this should be enough to get you started with sharing your prompts.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (9), [JSON](../../Skills/Web%20Development/JSON.md) (4), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **Env Vars:** json (4), yaml (2)
> **Exercise Files:** template (2)
> **CLI Commands:** python (1)
> **Cross-References:** later in (1)
> **Speakers:** - [instructor] (1)

#### [Zero-shot prompts](https://www.linkedin.com/learning/prompt-engineering-with-langchain/zero-shot-prompts?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/zero-shot-prompts?u=76281980&t=0)** - [Instructor] Because [large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) were trained on such a vast amount of data, they possess a surprising feature. And that feature is the ability to do what's called zero shot learning. And Zero Shot learning facilitates something called zero shot prompting. So what is Zero Shot prompting? Well, it involves asking the model to perform a task without giving it any specific examples beforehand. This is going to allow the language model to generate responses to a prompt without requiring specific examples or even being specifically trained for that task. That's because of the way language models were pre-trained. They're trained on vast quantities of data, and because of this, again, they're able to execute tasks with what's called the zero shot. For example, to classify a text as neutral, negative, or positive, you might prompt the language model by saying, classify the text into neutral, negative, or positive. And the model could respond with, for example, neutral for a statement like, I think the vacation is okay, but zero shot prompting doesn't produce the satisfactory outcomes that you're looking for. You can also include demonstrations or examples in the prompt, and that would shift more towards few shot prompting. But it's fascinating that large language models possess this ability to do tasks even if they weren't specifically trained for that task. So let's see some of this in action. So we're going to start by importing our language models, output parsers and prompt template.
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/zero-shot-prompts?u=76281980&t=94)** We're going to create a zero shot class here that will be initialized with a language model. We are going to have a prompt here that we're going to construct from a template. We're going to create the LLM chain, which consists of the output parser and the chain itself. And then we can invoke the chain by creating the prompt, creating the chain, and invoking it. So let's go ahead and do that. So we create an instance of our zero shot chain. And so now we can pass a template and a input to the language model. So here we've got a sarcasm template where we're just simply asking the language model to classify the user statement, which is going to be delimited by brackets as sarcastic or not sarcastic. And you can see here it does in fact classify it correctly. Another example of zero shot prompting is asking the model to classify the user statement as appropriate or inappropriate. Again, just another example of zero shot prompting. You see here it is appropriately classifying it. Zero shot prompting isn't limited to situations like we just described. You can also use zero shot prompting for named entity recognition. Essentially, named entity recognition is kind of like a smart highlighter that's going to find
>
> **[3:08](https://www.linkedin.com/learning/prompt-engineering-with-langchain/zero-shot-prompts?u=76281980&t=188)** and label specific information in the text. So this could spot and categorize names, places, companies, and more. It's great to quickly understand who, what and where without really reading every single [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md). And this is great for sorting through documents to find the most important bits. So let's see named entity recognition in action. So we start by initializing a template, which is identifying, categorize the names and the text limited by these brackets. Here's what we're going to pass in this sentence. We're going to go ahead and invoke our chain with the template and the input, and we'll get our results. So Barack Obama is a person from United States Honolulu, so on and so forth. You can see here that it's able to identify all that information. You could even have a template that is going to take somebody's information, for example, their name, contact info, so on and so forth. And then write a cover letter. In this case, the cover letter we're going to write, it's going to be in the most pompous way possible that's guaranteeing that this person won't be hired. So again, this is not like a task these model is specifically trained on, but because it was exposed to such a wide, wide range of data during this pre-training phase, it's picked up this emergent ability to do these type of tasks. So you can see here it kind
>
> **[4:41](https://www.linkedin.com/learning/prompt-engineering-with-langchain/zero-shot-prompts?u=76281980&t=281)** of does write the cover letter, but it almost writes it as a recommendation letter. So a zero shot prompting does have its limits, of course. So sometimes the AI's first guess isn't going to be quite right. And this is especially true of tricky tasks. We can't really steer the AI's responses as precisely as we might like with zero shot prompting. When topics are niche or jargon heavy, the AI might not be able to quote unquote speak our language as zero shot prompts are great for quick texts, but it might stumble if there's a lot of text that you're sending into the prompt. So as great as zero shout prompting is, it could be hit or miss as great for quick tasks, but less so for nuanced or lengthy context.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (2), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Exercise Files:** template (7)
> **Analogies:** for example (3), kind of like (1)
> **CLI Commands:** find (2)
> **Env Vars:** llm (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Custom prompt templates](https://www.linkedin.com/learning/prompt-engineering-with-langchain/custom-prompt-templates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/custom-prompt-templates?u=76281980&t=0)** - [Instructor] Custom prompt templates in [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) allow you to dynamically generate prompts tailored to your specific needs. You might need a custom prompt template if you want to include unique instructions, examples, or context for your model, if you want to dynamically insert task-specific information into the prompt, or if you simply want full control over the prompt structure and formatting. To create a custom prompt template, there's two things you need to do. One is specify the input variables and you need to expose these through the input variables attribute. Two is you need to implement a format method that's going to generate the prompt string. So these default prompt templates that you've seen so far, for example, prompt template and string prompt template, these only allow inserting simple string substitutions into the template. So for example, this prompt template here is simply going to allow me to just enter in a string. This is useful for simple cases, but it doesn't allow you to dynamically customize the prompt based on your input. A custom prompt template, on the other hand, is going to allow you to dynamically assemble prompts however you need for your particular use case. This is giving you full programmatic control over your prompt generation. So let's see an example of this in action. So we're going to create a FunctionExplainerPromptTemplate. And this class is going to be designed to take a function's name as a input, [Fetch](../../Skills/Web%20Development/Fetch.md) its source code, and then format that into a prompt.
>
> **[1:36](https://www.linkedin.com/learning/prompt-engineering-with-langchain/custom-prompt-templates?u=76281980&t=96)** So this class is a custom template that's going to take a function. So it's not going to take just the name, it's going to take the entire function as input. And it's going to inspect the function to get the source code. Then it's going to format that into the prompt template. So let's see this in action. So we've got our source code here that we are going to define this function, get_source_code. We have a prompt template here that we're going to create, just a string. Given the name of a function and source code, generate a English language explanation of that function. And here's what we're going to do now. We're going to create a FunctionExplainerPromptTemplate class. It's in a subclass from the string prompt template and the base model. And you can see here, it's just a custom prompt template that takes in a function's name as input and formats the prompt template to provide the source code of the function. We'll have a validator here that just validates the input variables. We are implementing the format method that's going to format the prompt using the function's name and source code. So we'll retrieve the source code of the provided function, and then we're going to format the prompt using the function name and the source code. And this is just a attribute to return the type of prompt that we have, in this case, a function-explainer. So now we can instantiate this function-explainer, right?
>
> **[3:14](https://www.linkedin.com/learning/prompt-engineering-with-langchain/custom-prompt-templates?u=76281980&t=194)** So before we do that, let's pull this up here. Let's run that. And let's look at the type of object function explainer is, right? And we can see that is a FunctionExplainerPromptTemplate. So now we can generate the prompt like so. We're going to pass the function name. In this case, it's get_source_code. And you can see that this is now the prompt that we are going to send to our large language model. So we'll go ahead and run that like so, and we'll get a response shortly, right. There we have it, function get_source_code takes in a function, returns source code, so on and so forth. Let's go ahead and rewrite this using the LangChain expression language. I'll start actually by importing the appropriate output parser, which we'll do here. And we'll construct it in the usual way. Of course, we need to define our LLM. So we've actually already done that above, right? So I don't need to define the LLM again, done it above, but I do need to figure out what I need to put for the prompt, right? And so for the prompt, it's actually just going to be the fn_explainer. We'll pipe that to the LLM, and, of course, use the output parser.
>
> **[4:49](https://www.linkedin.com/learning/prompt-engineering-with-langchain/custom-prompt-templates?u=76281980&t=289)** Finally, we can stream. The input variable to our function, if you recall, is going to be the function name. We'll pass the function_name. Function name, in this case, is get_source_code, right? So we'll say for chunk in this. We're going to print the chunk and put that and flush equal to true. We can invoke it like so. And here we have another example. It follows the same exact pattern what we saw above. In this case, we're going to have a AlgorithmOptimizerPromptTemplate. It's going to, again, accept a function. It's going to validate the input variables. It's going to format that for us. We have an attribute here for prompt_type. We have a function that we're going to define. And then, of course, we can go ahead and run that. So let's see this in action here. Define the function. The algorithm_function is the input variable. In this case, we can get the recursive function here and we'll get the answer like this. Well first, we will format the prompt template, get the answer like so. And, of course, for completeness, let's see how we would write this using the LangChain expression language,
>
> **[6:21](https://www.linkedin.com/learning/prompt-engineering-with-langchain/custom-prompt-templates?u=76281980&t=381)** so we have the LLM chain. In this case, the prompt we're going to pass is going to be the algo_optimizer. And we'll send this to the LLM, send that to the output parser. We'll say for chunk in llm_chain.stream. In this case, we're passing in a dictionary. And the input variable, in this case, can be algorithm_function, which will be the recursive, what do we call it? We call it a recursive factorial. We'll print the chunk. We'll say end. There's that. And flush is true. And, of course, get the answer like so. So you could see here that customizing a prompt template allows you so much more flexibility than just passing in a simple string. So here, like we're sending in to our prompt template actual functions, and we're able to parse those functions and do what we need to do to get a response from our large language model. So this opens up a whole range of possibilities.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (3), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Exercise Files:** template (16), source code (9)
> **Code Identifiers:** get_source_code (4), algorithm_function (2), fn_explainer (1), function_name (1), prompt_type (1)
> **Env Vars:** llm (5)
> **Definitions:** is a  (2)
> **Analogies:** for example (2)
> **Speakers:** - [instructor] (1)

#### [Prompt pipelining](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-pipelining?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-pipelining?u=76281980&t=0)** - [Instructor] Prompt Pipelining is a powerful tool that's designed for those who seek modular and efficient approaches to prompt design. There are several use cases where prompt pipelining can be useful. For example, if you want to reuse prompt components like introductions, instructions, examples, et cetera, across multiple prompts. Or if you want to split up a long prompt into smaller, logical chunks. Another way you can use prompt pipelines are when you want to dynamically assemble prompts based on conditional or other logics. Prompt pipelining will allow you to build prompts on the fly from those reusable parts. You can also mix static text with templates that contain variables so that static text can provide structure while the templates can inject dynamic content. You could also [compose](../../Glossary/Framework/Jetpack%20Compose.md) chat prompts from message templates and static messages. Each piece can get appended as a new message. You can also build up prompts from user provided components for customization. Prompt pipelining allows prompts to be assembled from these modular parts. Basically, you can use a prompt pipeline in any situation where you want to build up prompts in a reusable and composable way. This is where prompt pipelining will give you a nice, clean interface to do that. Let's start by talking about string prompt pipelining. So for string prompts, templates are going to be interconnected in sequence,
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-pipelining?u=76281980&t=94)** so you have the liberty to either utilize direct prompts or strings but it's essential to note that the initial element in the sequence should always be a prompt template object. So let's see this in action here. Here we're going to of course, import the prompt template. Our prompt here is going to be a pipeline like so. So you can see the input variables are coming from both of these elements. And we can add more to this like so by a simple + sign. And so let's just kind of print this out so it looks a bit cleaner. prompt.template.
>
> **[2:23](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-pipelining?u=76281980&t=143)** You can see here that it's just being built upon, and of course if we format it, it'll look like so. So at this point you're probably wondering," Okay, I've seen multi-input prompts. What's the difference between prompt pipelining and multi-input prompts?" Well, prompt pipelining composes multiple prompts into a single final prompt, whereas a multi-input prompt just allows you to pass multiple inputs to a single prompt template. With pipelining, each component can be formatted independently before being joined. Multi-input prompt will just format a single template. Pipelining allows you to join components in a linear sequence, whereas a multi-input prompt is just a single template that's handling multiple inputs. Pipelining also allows the reuse of components like introductions, examples, et cetera. Multi-inputs prompts are focused on handling multiple query inputs. So pipelining output is a single prompt, whereas multi-input output is the completion based on multiple inputs. Pipelining lets you build up prompts modularly. Multi-input prompts is just one template handling multiple inputs. So to summarize that, prompt pipelining composes multiple prompts into a final prompt in a linear sequence. Multi-input prompts just allow a single template to handle multiple input variables in parallel. Pipelining focuses on modular prompt building
>
> **[3:58](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-pipelining?u=76281980&t=238)** while multi-input prompts handle multiple query inputs. So let's see this in use in a chain. So we have our prompt defined above here, and this case, that prompt is going to be " I'm heading to (destination), recommend a great (activity). Also any local delicacies I should try?" So this is the prompt that we're now about to format. We'll just instantiate an output parser. We'll construct our prompt chain and remember the two input variables we're going to pass in here and we get the response like so. And now to this prompt, before we execute this, I want to show you what the result is here. So now we're going to add to this prompt a piece here, " How do I greet the locals in a jolly, informal manner?" And then we can look at the prompt here and you can see that we now have that part as part of the prompt. Send that like so and we get the response. Another example use case, let's say we want to build up a travel chat bot. So we'll create a travel chat bot class. It will take in a base prompt. We're then going to have the ability to just add stuff to the prompt. And then we can of course run the chain. We can run it like so, and of course, build upon it like that. And again, like so.
>
> **[5:33](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-pipelining?u=76281980&t=333)** So essentially the travel chat bot, all I did was just create a reasonable class that we can use and we can pass in actually any number of things to this, but we'll use it as chat bot. Next up, we're going to take a look at the Chat Prompt pipelines.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1)
> **Exercise Files:** template (8)
> **Definitions:** is a  (2)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Chat prompt pipelining](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chat-prompt-pipelining?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chat-prompt-pipelining?u=76281980&t=0)** - [Instructor] Let's now talk about chat prompt templates. So we're going to pick up in the exact same notebook where we left off in the previous video. Recall that a chat prompt is made up of a list of messages. And for developer experience, [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) has added a convenient way to create these prompts. In pipelines, each new element is going to be a new message in the final prompt. So there's a few key things that you need to know about chat prompt pipelining in LangChain. One is that chat prompt pipelining makes it easy for you to create chat prompts using reusable message components. So each addition to the pipeline translates to a new message in the final chat prompt. You can seamlessly integrate static messages, such as human messages or AI messages, with variables containing message templates. The culmination of all this is a unified chat prompt template, which is great for formatting and integrating into LangChain. This structure is going to promote the effortless reuse of components, including instructions and examples. This approach makes it easy to dynamically construct chat prompts using modular blocks. This ensures flexibility and efficiency. So the chat prompt pipelining composes chat prompts from reusable message templates and static messages. It's going to allow you to dynamically construct prompts from a logical blocks in a user-friendly way. The end result is going to be a single
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chat-prompt-pipelining?u=76281980&t=94)** reusable chat prompt template. So let's see this in action here. We'll start by importing chat prompt templates and human message prompt templates. We'll import as well the message templates for human, AI, and system message. We'll instantiate and output parser for our chain. And, of course, the LLM. So here's going to be the prompts. So we're going to set the scene with a cockney-themed system message, so "Welcome to the East End Cockney Chat." And so we're going to construct a new flow with dry humor, right? So we have a new prompt. We're going to start with that prompt that we defined above, the system message prompt. To that, we're going to add a human message. To that, we're going to add the AI message. And then the input, which is going to come now from the user. So, of course, we can format the message like so. So this input, "No, what about it?" will be passed into there. Now let's go ahead and print the prompt so we can see what it looks like. Let's make it a little bit easier to look at, so we can say print new_prompt. This will have the dot messages attribute and we can look at that. Oh, a little bit of live debugging there. Now, we can see what the messages look like. Great. So let's go ahead now and create a chain where we have our new prompt. We're going to send this to our language model and parse it with our output parser. There we go, and so this is how we can do prompt pipelining
>
> **[3:11](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chat-prompt-pipelining?u=76281980&t=191)** using chat prompt messages. In the next video, we're going to pick up right where we leave off here in this notebook, and we're going to talk about prompt composition.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (3)
> **Cross-References:** previous video (1), in the next (1)
> **Exercise Files:** template (2)
> **CLI Commands:** make (1)
> **Code Identifiers:** new_prompt (1)
> **Env Vars:** llm (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)

#### [Prompt composition](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-composition?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-composition?u=76281980&t=0)** - [Instructor] Picking up in the same notebook where we left off in the previous video. Let's now talk about prompt composition. Prompt composition is useful when you want to reuse parts of prompts. This could be done using the pipeline prompt. A pipeline prompt has two main parts. One is the final prompt, which is the prompt that will be returned, and the final prompt itself is actually made up of our pipeline prompts, which are a list of two pools, which consist of a string name and a prompt template. Each prompt template will be formatted and then passed to a future prompt template as a variable with the same name. Let's see this in action, let's go ahead and import the pipeline prompt template and the regular prompt template. I'm going to construct a full prompt, which is going to have the following input variables, introduction, example, and start. So let's construct that full prompt. So let's just real quickly inspect the full prompt so we can look at the input variables, right? So the input variables are going to be example, introduction and start. Now what I'm going to do is I'm going to define a introduction template. So the introduction prompt, we'll have an input variable. Let's take a look at that, right?
>
> **[1:32](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-composition?u=76281980&t=92)** I'll have you guess what it is as I am typing it out. Input variables going to be person. And then we'll have another example template. So here's an example of an interaction Q and A, whatever example prompt here. And of course you can guess what the input variables are going to be for this example prompt. It'll be example Q and example A. And then finally we're going to have a start template. And the start template has one input variable called input. Great. Now let's go ahead and take a look at that full prompt that we defined at the very top of this section. Right? So note, the full prompt has the input variables example, introduction and start. What I'm going to do now is create a list of two pools where now introduction is going to be the introduction prompt, example is going to be that example prompt. And then start will be the start prompt. We're going to put all this together and come up with a pipeline prompt. Now I want you to take a second here to think about what the input variables are now going to be for this pipeline prompt. So we started off with the full prompt and we get these pipeline prompts. So take a second to think about what the input variables are going to be.
>
> **[3:06](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-composition?u=76281980&t=186)** The input variables are now the input variables from each one of these prompts here, right? Let me just format this for you so we can see exactly what the full prompt is going to look like. It's going to look something like this, right? So now we can construct our chain. So we'll construct our chain by instantiating the language model, setting up the chain, and then invoking the chain. Unfortunately, using this pattern, we're not able to stream the outputs, so we're just going to have to invoke the chain and wait for the response. There you go, seems accurate that Elon Musk's favorite social media site is in fact, Twitter. Here is another example. Let's say you want to do a cooking show chat bot. You can, you know, do the same exact pattern that we've done before. Instantiate the full template. I have various introduction templates. So these introduction templates are set here, but of course you can define them outside. You'd have to rewrite the class, but of course you can define these as inputs to the class and you can run them like so. And unfortunately, this pattern with the prompt composition does not yet support streaming. So we do have to wait for the answer in these scenarios. And there you have it, so now you've got an understanding of prompt pipelining for chat messages as well as composition for prompts.

> [!info]- Semantic Content
>
> **Exercise Files:** template (10)
> **Cross-References:** previous video (1)
> **Speakers:** - [instructor] (1)

#### [Few-shot prompt templates](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates?u=76281980&t=0)** - [Instructor] Earlier in the course, we talked about the ability of [large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) to perform tasks with zero-shot prompting, meaning they've never seen this example before and they weren't specifically trained on that task. There's also a emergent ability in large language models called few-shot learning. So few-shot learning is just a form of in-context learning where in the prompt you're providing a few examples to help the language model perform on the tasks that you want it to. [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) offers a FewShotPromptTemplate that's going to allow you to provide a few examples to prime the model before your main prompt. This is useful for providing relevant context and, quote, unquote, warming up the model on your task. So when do you want to use a FewShotPromptTemplate? Well, you want to use it if your task requires some background context to perform well. For example, summarization tasks often benefit from example summaries. If you want to bias the model towards a certain style or tone, example outputs can help with that. So FewShotPromptTemplates just allow providing sample input/output to prime the model for your task. Use it when you need extra context style priming or have very short prompts. To create a FewShotPromptTemplate, you need to provide a list of example dictionaries with input and output keys. Each example should be a dictionary
>
> **[1:33](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates?u=76281980&t=93)** with the keys being the input variable and the values being the values for those input variables, then you want to create a formatter for the few shot examples, and then you can feed these examples and the formatter to the FewShotPromptTemplate. This is going to return a prompt with your examples, then the main input. So some of the key aspects here are examples need to demonstrate the desired input/output format. Examples should provide some relevant context, and the main prompt should be formatted to match the style of the examples. This allows priming the model in a more nuanced way than just providing a generic instruction like answer the following geography question. So the FewShotPromptTemplate allows you to assemble a prompt that's going to guide the model using these targeted examples rather than just high level instructions. This again will result in more nuanced prompt that's going to prime your model for the specific task. So we start by importing the FewShotPromptTemplate from a LangChain, and we'll have a set of examples here. So this example is going to be a list of dictionaries. Dictionary is going to be some question and then some answers. This answer is really intended to be the way you'd want the model to kind of think through, answer to your question. So we're providing it a bunch of different examples for how I want this thing to go and answer any question I give it.
>
> **[3:06](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates?u=76281980&t=186)** So we've got a list of examples there. And again, each element of the list is a dictionary. The dictionary is going to have the key for the input variable along with this value. And we're going to go ahead and create our example prompt template like so. Now we're going to feed the examples and the formatter to the FewShotPromptTemplate. So we have the examples here, and then the example prompt. Putting a suffix here because I want the prompt to end with Question:, and output indicator, if you recall, for the model. And the input variable here is going to be input. And so you can see here our final prompt and we have the question here, and the model should complete its answer like so. So let's go ahead and actually let's turn down the temperature down to 0.1 and see what the response looks like. And there you go. So you see that it's actually answering in the way that we have described it with those examples, which is exactly the behavior that we want. Now we can actually engineer the prompt, and you'll see what I mean by that. So language models are continuing to expand their context capacity. It's becoming more common to incorporate
>
> **[4:40](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates?u=76281980&t=280)** a lot of different examples in your prompt. But as the diversity and number of classes increase, manually curating examples for every class is a challenging task. And this results in a reliance on kind of the random selection from a collection of examples. What I want to do here is create some few-shot examples in a more smart way. And I'm going to do that using a dataset. And the dataset we're going to be using here is from [Hugging Face](../../Glossary/Service/Hugging%20Face.md). It's the intent classification dataset. And so you can see here we've got a intent, and then you know the accompanying classification, right? So let's just take again a look at what that looks like. And it looks something like this, right? So now I'm going to define a function, and this function is going to generate a prefix string for the prompt, and it's going to help guide the language model in the intent classification. So it's going to take a dataset, which in this case is going to be a list of dictionaries or a [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) DataFrame needs to have the text and the intent columns, and it's going to return a formatted string. So let's go ahead and see what that looks like here. So the prefix is going to be like so. Each input is associated with the user intent. Choose from the following intents, and these are all the different intents that the model is going to choose from. Now we're going to get examples, and that's what this function is going to do. So extract one random example from each unique intent present in the dataset,
>
> **[6:15](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates?u=76281980&t=375)** and let's see what that looks like once we run this. All right, so here we have some random examples that we're going to be using for intent classification. And then finally, we're going to construct the prompt. So the prompt is going to be a k-shot prompt, meaning, you know, however, many examples we want to incorporate. It's going to have to prefix the examples and then the target text that we want to classify. We'll construct the prompt template using the initializer. And the final prompt that we're going to use is going to be a FewShotPromptTemplate. The example_prompt will be our prompt template that we defined above. The examples will be those examples that we have selected. To get the prefix, we'll use this get prefix function. The suffix is text, so this is the output indicator and the input will be the text variable. So let's see what this looks like. So each input belongs is associated with the user intent. Obviously, I made a spelling error there. You can go ahead and remove that belongs, but [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) will be smart enough to know what I meant. So each input is associated with a user intent. Choose from the following intents. And then I'm giving it examples, right? Here's some text. This is the label, here's some text, there's a label, so on and so forth, right? So now what I want the language model to do is give me the label for this, Save Free Smoke by AP Dhillon to my songs. And let's see what it comes up with and add to playlist,
>
> **[7:48](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates?u=76281980&t=468)** and there you have it. And so this is how FewShotPromptTemplates work. In the next video, I'll show you how to use FewShotPromptTemplates for chat prompt templates. So I'll see you in that video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (2), [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (2), [Hugging Face](../../Glossary/Service/Hugging%20Face.md) (1), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1), [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) (1)
> **Cross-References:** earlier in (1), we talked about (1), in the next (1)
> **Exercise Files:** template (3)
> **Definitions:** is a  (2)
> **Code Identifiers:** example_prompt (1)
> **Env Vars:** gpt (1)
> **Versions:** 0.1 (1)
> **Analogies:** for example (1)

#### [Few-shot prompt templates for chat](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates-for-chat?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates-for-chat?u=76281980&t=0)** - [Instructor] Few-shot examples are used to provide sample interactions to a chat model, which helps guide its behavior. The purpose of few-shot prompt templates is to select examples dynamically based on the input and format them into a final prompt to provide for the model. These templates demonstrate the desired input and output format as well as the content. This helps ensure that the model can provide relevant and accurate responses. For chat models, few-shot examples typically consist of a simulated conversation between a human and a AI speaker. Each example has a human message input, and expected AI response output. few-shot examples for chat models differ from few-shot examples for standard language models in the following ways. One is they're formatted as dialogues, rather than just input-output pairs. Two, the examples model a conversation between two speakers rather than just an input to an output. Three is that they help teach the conversational flow and tone in addition to context. To create a few-shot example for a chat model, you need to do a few things. One is come up with a few different example conversations that are going to be representative of your use case. You want to format them as alternating human and AI messages. Then you want to pass this to your chat model wrapped in the few-shot chat message prompt template. The chat model will condition on these examples
>
> **[1:37](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates-for-chat?u=76281980&t=97)** when it's making its predictions. So to sum that up, few-shot chat examples demonstrate the desired conversational flow to the model through these formatted message examples. This is going to guide the model to converse in the way that you want it to converse. Let's talk briefly about fixed examples. Fixed examples are the most basic and common few-shot prompting techniques. This way you can select a chain, evaluate it, and avoid worrying about additional moving parts in production. So the basic components of the template are examples, which is going to be a list of dictionary examples to include in the final prompt, and an example prompt, which is going to convert each example into one or more messages through its format messages method. A common example would be to convert each example into one human message and one AI message response, or a human message, followed by a function call message. Later on, you'll see how to dynamically select prompts using example selectors. So let's see this in action, and here are some examples that we want to use. These are all related to "Back to the Future." So pretty much what I'm saying is if I ask you a question about "Back to the Future," respond about the movie. If the question is not related to "Back to the Future," just respond with a verse from the song, "Power of Love," which was on the "Back to the Future" soundtrack. So let's go ahead and assemble the prompt,
>
> **[3:13](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates-for-chat?u=76281980&t=193)** and you can see it looks something like this. Now we have our system message. You're a "Back to the Future" fanatic bot. You only answer questions about "Back to the Future." If you're asked a question that is not related to any of the "Back to the Future" movies, simply reply with lyrics from the soundtrack of the movie. So the final prompt, let's print out what the final prompt looks like. And you can see here it's got the example. So it's the template, the system message template, the few-shot chat message prompt template. These are the examples, the human message, which we're going to pass in the AI message. So let's go ahead and instantiate the language model. Construct a chain, and let's stream our response. Input Burger King gym diner. So let's just switch this up a little bit. What scene did we see the Burger King gym and diner in? Let's see how well GPT-3.5 knows "Back to the Future," 'cause this is the opening scene of "Back to the Future" when he's skateboarding behind a Jeep. Yeah, so GPT-3.5 is in fact not a "Back to the Future" fanatic. Power of love is curious thing, makes one man weep, another man sing. So obviously, the answer of this should have been enchantment under the sea dance. It got that part right, so that's quite nice.
>
> **[4:47](https://www.linkedin.com/learning/prompt-engineering-with-langchain/few-shot-prompt-templates-for-chat?u=76281980&t=287)** It responded with that for my name, a verse from the song. Alright, so now we see how to use few-shot templates for chat. We also see that GPT-3.5 doesn't know much about "Back to the Future," sadly, but I encourage you to think about now how you can use few-shot templates for chat in your applications.

> [!info]- Semantic Content
>
> **Exercise Files:** template (5)
> **Env Vars:** gpt (3)
> **Versions:** 3.5 (3)
> **Speakers:** - [instructor] (1)

#### [Introduction to example selectors](https://www.linkedin.com/learning/prompt-engineering-with-langchain/introduction-to-example-selectors?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/introduction-to-example-selectors?u=76281980&t=0)** - [Instructor] Example selectors in [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) are classes that are responsible for selecting which examples to include in a prompt. They're useful when you have a large number of examples available, but need to select a subset of them to include in your prompt. Some key things to know about example selectors is that they implement a select_examples method, which takes in the input variables and returns a list of examples to include in the prompt. There are several different strategies for selecting examples. For example, you can select based on semantic similarity to the input, you can select based on maximal marginal relevance, so you can balance similarity and diversity using that. Or you can select based on prompt length. Example selectors allow prompts to dynamically choose examples based on the inputs rather than having fixed examples. They help you manage long prompts by only including the most relevant examples for the given inputs. New example selectors can be implemented using the BaseExampleSelector class and defining a custom select_examples method. So let's go ahead and implement a CustomExampleSelector before going into some of the example selectors that LangChain offers us out of the box. So here what we're going to do is we're going to implement a random example selector class. This is going to inherit from the BaseExampleSelector class,
>
> **[1:35](https://www.linkedin.com/learning/prompt-engineering-with-langchain/introduction-to-example-selectors?u=76281980&t=95)** and it needs to implement the select_examples method. So that's what we're going to do here. You can see that we need to first import the BaseExampleSelector, and then we can define our class here, which is going to be called CustomExampleSelector, which inherits from the BaseExampleSelector, and we implement the add_example and select_example methods.
>
> **[2:03](https://www.linkedin.com/learning/prompt-engineering-with-langchain/introduction-to-example-selectors?u=76281980&t=123)** So now we can have some examples just for some recipes and initialize the example selector. Great. Now let's see this thing in action. Note that the use of this pattern that you see here, a recipe, a recipe, this is just a placeholder for a generic representation, it doesn't affect the outcome of the method. In a more sophisticated example selector the input variables might be used to filter or prioritize the selection of examples based on some criteria. But in the provided example right here, we're not utilizing that. So here's our example selectors. You can see these are examples that we have. We can add an example to that and now we can randomly select an example. And there you have it, there's a introduction to example selectors. Now we're going to see several different example selectors in action.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (2)
> **Code Identifiers:** select_examples (3), add_example (1), select_example (1)
> **Analogies:** for example (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Length-based example selector](https://www.linkedin.com/learning/prompt-engineering-with-langchain/length-based-example-selector?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/length-based-example-selector?u=76281980&t=0)** - [Instructor] One of the simplest ways to select examples for your prompt is by length. And so what we're going to see how to do in this video is use the length based example selector, which does what it says on the tin. It selects examples based on their length. This is useful for situations where you're worried about the total length of the constructed prompt, especially given the context limitations of some models. So we'll start by just importing the prompt template, as well as the few shot prompt template, and the length based example selector. We'll define our examples. We will define our example prompt, and then now we can use the length based example selector. So essentially, the primary goal of this selector is to adjust the number of examples included in the prompt based on the length of the input and the examples themselves. Again, this is useful when you're worried about the total length of the constructed prompt. So it works like this. When you're adding a new example using the add example method. The example is formatted using the provided example prompt, and its length is calculated using the get text length function. This length is then stored in the example text lengths list. So when we're selecting examples using select examples, the method will calculate the length of the provided input. It's going to determine how much length is left for examples by subtracting the input length from max length. And it's going to iteratively check
>
> **[1:33](https://www.linkedin.com/learning/prompt-engineering-with-langchain/length-based-example-selector?u=76281980&t=93)** each example's length against the remaining length. If an example fits, it's going to be added to the list of selected examples, and its length is going to be subtracted from the remaining length. And this process will continue until the remaining length is exhausted, or all examples have been considered. And this allows it to dynamically select more examples for shorter inputs, and fewer examples for longer inputs, to keep the overall prompt length under a max length. So in summary, it's going to select examples based on length to try to construct prompts that don't exceed the context window of the model. And so it does so just like this. So we instantiate the length based example selector with our examples, the example prompt, and the max length. And we have our dynamic prompt here. We can put in a long prompt, and you can see for the long prompt, it doesn't select any examples because we do not have any space left. We can also give it a short string. We can say, for example, short string feeling wired. And then you can see here that actually ends up selecting examples. Same prompt, but it's selecting now based on length. You can see here this input was longer than that entire length that we're giving at 15, and it did not add any examples, whereas this is less than that, and we do have an example. That's it for the length based selectors. Coming up, we'll pick up on the same spot in the notebook and learn about max marginal relevance.

> [!info]- Semantic Content
>
> **Exercise Files:** template (2)
> **Analogies:** just like (1), for example (1)
> **Cross-References:** coming up (1)
> **Speakers:** - [instructor] (1)

#### [Max marginal relevance example selector](https://www.linkedin.com/learning/prompt-engineering-with-langchain/max-marginal-relevance-example-selector?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/max-marginal-relevance-example-selector?u=76281980&t=0)** - [Instructor] Maximal marginal relevance, or MMR, is a technique often used in [Information Retrieval](../../Skills/Data%20Science/Information%20Retrieval.md) to balance between the relevance of documents, or examples in this case, and the diversity among them. The idea is to avoid redundancy in the selected set of documents or examples while ensuring that they're still relevant to the query. Here's how it works. First, there is a relevance calculation that's done. So for each example we calculate its cosine similarity with the input. This gives us a measure of how relevant each example is to the input. There's also a diversity calculation. For each example, we are going to calculate its cosine similarity with the already selected examples. This will give us a measure of how similar the example is to what's already been chosen. And then there's the MMR score calculation. For each example, we are going to compute its MMR score as a combination of its relevance to the input and its diversity from the already selected examples. And the formula is typically expressed like so. MMR = lambda times relevance minus one minus lambda times diversity. Lambda is a parameter between zero and one that controls the trade-off between relevance and diversity.
>
> **[1:35](https://www.linkedin.com/learning/prompt-engineering-with-langchain/max-marginal-relevance-example-selector?u=76281980&t=95)** Then we will iteratively select the examples with the highest MMR score, add it to the selected set, and then update the diversity calculations for the remaining examples. This process will continue until a stopping criteria is met, such as a predefined number of examples or until the MMR score falls below some threshold. In [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md), the MMR is implemented in the MaxMarginalRelevanceExampleSelector. And we can take a look at the examples that we have here in the example prompt. So given an input, produce a output and these are going to serve as a set of few shot examples. Let's talk in a little bit more detail about how this class is going to be used in LangChain. So the MaxMarginalRelevanceExampleSelector is a specialized class that's designed to select examples based on the MMR criteria. So again, MMR is balancing the trade off between the relevance, so how similar an example is to a given query, and diversity, how different the selected examples are from each other. And this is going to give us a comprehensive set of examples that are relevant to the query, yet diverse. So this class, the MaxMarginalRelevanceExampleSelector, we are going to use the from_examples method
>
> **[3:10](https://www.linkedin.com/learning/prompt-engineering-with-langchain/max-marginal-relevance-example-selector?u=76281980&t=190)** to create a instance of this class. So we're going to initialize this with a set of examples, as well as the embeddings model that we're going to use, a vector store. And then two other parameters for k and fetch_k. Fetch_k is the number of examples to initially [Fetch](../../Skills/Web%20Development/Fetch.md) before re-ranking them using the MMR algorithm. And k is the number of actual examples that we want to include in the prompt. If you want more information about the MMR selector, you can always run the help on it. Great, so now we've got our MMR selector instantiated. Next, we need to create a prompt template. In this case, we're going to leverage a few shot prompt template. So the example_selector is now the argument that we are going to pass in to this. Along with the example_selector, we have the prompt, the prefix of the prompt is what's the opposite of, suffix will be the input where we'll inject that particular adjective and then the output indicator. And so we can then run the following. So we get the adjective buzzed. And according to this, these are the two most similar examples to that. That's all for the MMR selector. In the next video, we are going to see the NGramOverlapExampleSelector in action, picking up from the exact same notebook
>
> **[4:45](https://www.linkedin.com/learning/prompt-engineering-with-langchain/max-marginal-relevance-example-selector?u=76281980&t=285)** that we're working in right now.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (2), [Information Retrieval](../../Skills/Data%20Science/Information%20Retrieval.md) (1), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Env Vars:** mmr (13)
> **Definitions:** is a  (4)
> **Code Identifiers:** example_selector (2), from_examples (1)
> **Exercise Files:** template (2)
> **Cross-References:** in the next (1)
> **UI Navigation:** select the (1)
> **Analogies:** such as (1)

#### [N-gram overlap example selector](https://www.linkedin.com/learning/prompt-engineering-with-langchain/n-gram-overlap-example-selector?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/n-gram-overlap-example-selector?u=76281980&t=0)** - [Instructor] The n-gramOverlapExampleSelector selects and orders examples based on which examples are most similar to the input according to an n-gram overlap score. But what is an n-gram? So consider the following sentence, "Sunsets are always beautiful." From this sentence, you can construct a unigram, bigram, and a fourgram. Unigram would just be a sequence of individual words, so this sentence actually has four unigrams. Bigrams would be pairs of words, so this sentence has three bigrams, sunsets are, are always, and always beautiful, so on and so forth. And the N for n-gram just indicates that you can have as many in a sequence as you'd like. So that value of N is entirely up to you. But here I've represented n-grams of bigrams, trigrams, and four grams. The n-gram overlap score relies on the sentence blue score for overlap calculation. And this overlap score is actually a floating point number between zero and one. And it is going to select and order examples based on their n-gram overlap score with a given input. So this overlap score is again, a metric used in machine translation to evaluate a translated sentence's quality. So the selector provides the option to establish a threshold score.
>
> **[1:35](https://www.linkedin.com/learning/prompt-engineering-with-langchain/n-gram-overlap-example-selector?u=76281980&t=95)** So examples with a n-gram overlap score less than or equal to this threshold will be excluded, by default the threshold is set at negative 1.0, which means it will not exclude any examples, but only reorder them. In the case of a threshold value being passed that is greater than 1.0, the function is going to exclude all examples and return an empty list. The threshold is equal to 0.0. It'll sort the examples based on their n-gram overlap score and exclude those that have no n-gram overlap score with the input. Let's go ahead and instantiate the n-gram overlap selector. We're going to use these examples and we're going to set up our prompt. Now, when we instantiate the n-gram overlap selector, you notice that we need to pass in a list of examples, and that's what we're doing here. We also need to pass in an example prompt, which we've just defined above. And of course, this is where we'll set the threshold. I'm going to keep it right now actually at negative 1.0. Now that we have our example selector instantiated, we can construct our few shop prompt template. We'll pass the example selector, we'll pass the example prompt as well as the prefix suffix and indicate what the input variable is. So let's take a look at something real quick.
>
> **[3:10](https://www.linkedin.com/learning/prompt-engineering-with-langchain/n-gram-overlap-example-selector?u=76281980&t=190)** I want to show you what the example prompt is. See here we've got one value, the input and the other value here, the output and the template is just simply this input output, right? The dynamic prompt now is going to take sentence as an input, and then it's going to help us to parse through our examples. It'll inject the sentence there and it'll construct a prompt for us. So let's see what that looks like here. So here is this constructed prompt. You see here we left the threshold at negative one. So all it is doing is not excluding any examples, but simply reordering them based on their overlap. Here's another sentence, right? And now we can play around with a little bit of the threshold. So actually I'm going to copy the same exact sentence. Let's see what happens when we toggle the threshold to a different value. In this case, I'm going to set the value to zero. So setting the value to zero is just going to sort the examples based on their n-gram overlap score and then exclude those that have no n-gram overlap with the input. So you see here, it looks like there is no actual n-gram overlaps with this sentence, but what if we kick this number up a notch, maybe .25, and it'll be the same. Now, if we set this to be a negative value 0.25, we can see here that a value less than zero is just going to
>
> **[4:50](https://www.linkedin.com/learning/prompt-engineering-with-langchain/n-gram-overlap-example-selector?u=76281980&t=290)** behave the same as if it was negative one. And we can see another example here. And that's pretty much a high level overview of how the n-gram overlap selector works. You're free to use this with better examples if you'd like and get more of an intuition about how it works. But you now understand how the n-gram overlap selector works and how you might be able to use it to select the correct examples to include as part of your prompt.

> [!info]- Semantic Content
>
> **Versions:** 1.0 (3), 0.0 (1), 0.25 (1)
> **UI Navigation:** toggle (1), select the (1)
> **Exercise Files:** template (2)
> **Code Identifiers:** gramoverlapexampleselector (1)
> **Definitions:** is an  (1)
> **Analogies:** similar to (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Semantic similarity example selector](https://www.linkedin.com/learning/prompt-engineering-with-langchain/semantic-similarity-example-selector?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/semantic-similarity-example-selector?u=76281980&t=0)** - [Instructor] The semantic similarity example selector is designed to select examples based on their semantic similarity to a given input. So it's a type of example selector that's going to leverage embeddings and a vector store to find the most semantically similar examples. In order to instantiate this, we need to define a few things in the constructor. That is the vector store. The number of examples to select the example keys, these are optional to filter the examples if they're provided. Only these keys will be considered when selecting the examples and also the input keys. And these are optional keys to filter the input. So if provided the searches based on these input variables, instead of considering all the variables. It has the same methods that we've seen with previous example selectors, that is add example, select examples, and it can be instantiated with the from examples method. So again, recap on how it works. It's leveraging embeddings to represent the semantic meaning of examples and input variables. When you want to select examples that are semantically similar to a given input, you can provide the input variables to the select examples method. The method will then query the vector store to find the most similar examples based on their embeddings. And then the return examples can be filtered based on the examples keys if they're provided.
>
> **[1:35](https://www.linkedin.com/learning/prompt-engineering-with-langchain/semantic-similarity-example-selector?u=76281980&t=95)** So let's take a look at the examples again, it is these input output pairs. Our example prompt is going to look like that. All right, so we're go ahead and instantiate the example selector by passing in our examples. The embedding model that we're going to use, the vector store and the K, meaning the number of similar examples we want to include in the prompt. And then we also instantiate the prompt. So again, this will be our few shot template. We're going to pass in the example selector to the view shot template. We'll pass in the example prompt, which we defined above, as well as the prefix, suffix, and the input variable. Now we can go ahead and run this, and you can see here that we get the 1, 2, 3, 4 most similar input output pairs to this sentence.
>
> **[2:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/semantic-similarity-example-selector?u=76281980&t=154)** And there you have it, this we can finally send into our language model and get an answer and a response. So I encourage you to write the code for that on your own. You've seen how to construct a change several times by this point, so I'm sure you can take it from here.

> [!info]- Semantic Content
>
> **CLI Commands:** find (2)
> **Exercise Files:** template (2)
> **UI Navigation:** select the (1)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)

#### [Partial prompt templates](https://www.linkedin.com/learning/prompt-engineering-with-langchain/partial-prompt-templates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/partial-prompt-templates?u=76281980&t=0)** - [Instructor] Partial prompt templates in [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) offer a flexible way to work with prompt templates by allowing users to predefine a subset of required values. This is especially beneficial when some values are known beforehand. This enables a more streamlined approach to formatting the remaining values later on. LangChain provides two primary methods for creating partial prompt templates. One is partial with strings. This is going to allow users to input string values for specific variables during the creation of the partial prompt template, and is ideal for scenarios where certain variable values are obtained earlier than others. Next is partial with functions. This is going to enable users to input functions that return specific values. This is useful for dynamic variables like date and time, which need to be fetched in real time. Let's see this in action. So I want you to consider a prompt that necessitates multiple variables. So if certain values like name and location are already known, a partial prop template can be crafted with those preset values. This partial template can then be used more efficiently, requiring only the input of the remaining variables, such as time. For instance, if you have a personalized story prompt
>
> **[1:35](https://www.linkedin.com/learning/prompt-engineering-with-langchain/partial-prompt-templates?u=76281980&t=95)** that might need variables like name, location, and time. If the name and location are predetermined, a partial template can be created with those values. This simpler partial template can then be utilized to gather only the outstanding variables like time. And by using the partial with strings method, you can optimize your workflows and ensure that the process isn't halted due to the unavailability of some source variables. So let's see this in action here. So we're going to take this prompt template, format it with Alice. Oh, what happened here? Notice we have a key error for user activity. Huh. Well, we can circumvent this by casting prompts as partial and saying that the username is Alice. And we see here that we have a prompt, the template, hello, username, the partial variable being prefilled to Alice, and this being the remaining input variable. And now we can finally pass that in. Hello Alice, hope you're enjoying your time at the Wonderland Resort chasing our white rabbits. Using functions in partial prompt templates gives you a dynamic way to incorporate [Real-Time](../../Skills/Database%20Management/Real-Time.md) data into your prompts. This approach shines when you need to consistently [Fetch](../../Skills/Web%20Development/Fetch.md) and update certain variables. A good example is when you're integrating real-time conditions like the current weather.
>
> **[3:09](https://www.linkedin.com/learning/prompt-engineering-with-langchain/partial-prompt-templates?u=76281980&t=189)** So imagine designing a website greeting that adjusts based on the day's weather. So manually updating the weather condition in the prompt every time is not practical. Also, passing it as a regular input each time will be super tedious. So to streamline this, you can use a function within the partial prompt template that will automatically fetch the current weather whenever the prompt is used. So here we have a function here that's just going to get weather, we're going to define a prompt template. The partial prompt template will be this prompt template casted as partial, and the weather value will be determined by this function, and we can take a look at that right now with everything parsed in. Great, and here's another example as well. And there you have it, now you've seen the flexibility that partial prompt templates provide you. So you don't need to fill in the values for the input variables all at once, you can cast them as partial, pre-fill some values, and then leave other ones to be inputted further along in the process.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (2), [Real-Time](../../Skills/Database%20Management/Real-Time.md) (2), [Fetch](../../Skills/Web%20Development/Fetch.md) (2)
> **Exercise Files:** template (11)
> **Analogies:** such as (1), for instance (1), imagine (1)
> **Speakers:** - [instructor] (1)


### 6. Prompting Techniques

[↑ Back to Table of Contents](#table-of-contents)

#### [Chain of thought](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chain-of-thought?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chain-of-thought?u=76281980&t=0)** - [Instructor] Before we get into chain-of-thought prompting, I just want to point out that for this notebook, you'll notice that we're using a T4 GPU. This is a GPU that is available to you on the free tier of [Google](../../Glossary/Service/Google.md) Colab. And we're using the T4, because we're going to make use of a embeddings model to embed a data set, and you'll see when we do that further along in this notebook. But for now, let's go ahead and jump into chain-of-thought prompting. So chain-of-thought prompting was introduced by Wei et al. in a 2022 paper called, "Chain-of-Thought Prompting Elicits Reasoning "in [Large Language Models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md)." Chain-of-thought prompting emerged from the need to enhance the reasoning capabilities of large language models when faced with complex tasks. But what is chain-of-thought prompting? Well, it is a technique that guides language models through a series of intermediate reasoning steps. This method involves providing a few demonstrations of the chain-of-thought as exemplars during prompting. So instead of directly providing an answer, the model details its reasoning forming a chain of thoughts leading to the conclusion. The idea is to mimic a step-by-step thought process that could lead to an answer. So for example, if we look at this example here, the question is, "Roger has five tennis balls. "He buys two more cans of tennis balls. "Each can has three tennis balls.
>
> **[1:33](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chain-of-thought?u=76281980&t=93)** "How many tennis balls does he have now?" So think about how you would think through that. You'd probably say, "Okay, Roger starts with five, "buys two more, he's got five plus two times three, "that's six, that gives me 11." So you're kind of thinking through it that way. Instead of just saying the answer is 11, the chain-of-thought prompting will actually incorporate what I just said, those actual intermediate reasoning steps. And you can see here that it ends up, in this case for this model, getting the correct answer. So, when would you use chain-of-thought prompting? Well, you'd use it when you want your language model to break down multi-step problems. This will allow the model to allocate computational resources to these tasks that require intricate reasoning. You want to use this in combination with few-shot prompting. Chain-of-thought can be combined with few-shot, and remember, few-shot is where the model is given a few examples to guide its response. This combination actually ends up being especially effective for complex tasks. Chain-of-thought prompting also offers interpretability, so by detailing its quote, unquote thought process, the model is able to provide insights into its reasoning. This can be applied to various tasks, including arithmetic, common sense, reasoning, and symbolic manipulation. So chain-of-though prompting significantly advances language models, emphasizing a reasoning and interpretability.
>
> **[3:08](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chain-of-thought?u=76281980&t=188)** It ensures more accurate and insightful responses, especially for complex tasks. So, here's what we're going to do. We're going to download this data set from [Hugging Face](../../Glossary/Service/Hugging%20Face.md). This is a few chain-of-though collection data set. It's got 1.8 million rows. We're going to subset just to take 10,000 of those rows, and here you can see what this looks like. I'm kind of slicing into see an example in the fullest. So here we have the question, "What about Neptune did NASA propose in 2003 "in their Vision Mission Studies?" Options, yes, no, and then gives rationale, so we have the question, we have the rationale for how to think through it, and then the final answer. What we're going to do now is we're going to take this data set, and we are going to embed this data set and send it to a vector database. We're going to use the BGE embeddings from Hugging Face, and now we're going to construct our prompt template. So we've got the prefix, consider the following as examples for how to reason, then we'll have our examples template. And this is where we'll inject the query, the rationale, and the response. And then we say, "Using a similar reasoning approach, "answer the user's question delimited by backticks. "Take a deep breath, "break down the user's query step-by-step, "and provide a clear chain of though in your response." So this is just a ending salutation, "Take a deep breath and break down step-by-step,"
>
> **[4:41](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chain-of-thought?u=76281980&t=281)** tends to improve a language model's ability to do chain-of-though prompting. If you wanted to, you can even offer the language model a tip, and that apparently seems to work really well, as well. All right, so we have a large number of examples to embed and send to the vector store. So this cell below is going to take a few minutes to run. It could take up to three to five minutes. So, we're going to first instantiate the max marginal relevance example selector. We've already learned about this. We're going to give it the list of examples to select from, the embeddings model, the vector store, and the number of examples to produce, so we'll go ahead and run that. Once we have the example selector instantiated, we're then going to construct our prompt, the MMR prompt, so again, we've seen how to do this. Pass in our example selector, the example prompt, the prefix, the suffix, and of course, what the input variable is. And here, we have a query. So, "Here's a short story: A lifter uses a barbell, "but moves a jump rope in a wider arc. "The object likely to travel further "is A, the jump rope B, the barbell. "What is the most sensical answer "between a jump rope and a barbell?" And we're going to pass this to the language model. And as soon as the embeddings model is done, we're going to run this cell, so we'll be back in just a couple of minutes. So it took just about three minutes for the embedding model to embed the 10,000 examples that we had. And now we have our prompt, which is formatted as such. So again, we have our query here, and it's being sent
>
> **[6:21](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chain-of-thought?u=76281980&t=381)** to the max marginal relevance example selector, and we're getting the examples that are most similar to this query. And that way we are hoping to get our language model to reason about this query correctly. So you can see here, this is what our prompt looks like. And now we can construct our LLM chain. We'll be sure to set the temperature to be 0.0. So we've instantiated our LLM, we've constructed our LLM chain with the prompt, language model, and the output parser, and we can run this to get a response. And there you go, you see how it's thinking through step-by-step, and it's following along these examples that it has here. So it's got a query, it's thinking through, and then finally responds with the answer. We can try that again with a different query. You can see here it is doing a step-by-step type of reasoning, and it's able to produce eventually a good answer, so let's see increases. I think that matches with the intuition here. So, "Given the fact that: Inhaling breathing in "is increasing the size of the chest "and decreasing air pressure inside the lungs. "Answer the question: If Mona is done with a race "and her chest contracts, what happens to the amount "of air pressure in her lungs, increases or decreases?"
>
> **[7:55](https://www.linkedin.com/learning/prompt-engineering-with-langchain/chain-of-thought?u=76281980&t=475)** So if her chest contracts, I guess that means she is breathing in, and that decreases the air pressure inside the lungs, so the opposite action, chest contracting, so on and so forth, and you see here the language model does a convincing job of reasoning through this. I'm going to go ahead and give this response, the W. So chain-of-though prompting, as you've seen here, is essentially just a few-shot prompt with a specific type of example. This example breaks down how to reason through a process and then invites the language model to think step-by-step before providing its final answer.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (2), [Hugging Face](../../Glossary/Service/Hugging%20Face.md) (2), [Google](../../Glossary/Service/Google.md) (1)
> **Env Vars:** llm (3), gpu (2), nasa (1), bge (1), mmr (1)
> **Definitions:** is a  (3)
> **Versions:** 1.8 (1), 0.0 (1)
> **Exercise Files:** template (2)
> **Analogies:** for example (1), similar to (1)
> **CLI Commands:** make (1)
> **Tools:** colab (1)

#### [Self-consistency](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-consistency?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-consistency?u=76281980&t=0)** - [Instructor] Self-consistency prompting was introduced in the March 2022 paper, "Self-Consistency Improves Chain of Thought Reasoning in [Large Language Models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md)," by Wang, et al. So what is self-consistency prompting? The idea behind self-consistency is that complex reasoning problems often have multiple valid ways of thinking that lead to the same correct answer. By exploring these diverse reasoning paths, you can achieve a more reliable and consistent answer. How's this different, though, from other prompting techniques? Well, traditional chain of thought prompting, which we've already seen, prompts a language model to generate a series of short sentences that mimic the reasoning process a person might use to solve a task. Self-consistency, instead of just taking the most probable reasoning path, will sample multiple paths and then determine the most consistent answer among them. Unlike methods that require additional training, human annotations, or auxiliary models, self-consistency is unsupervised and works directly with the pre-trained language models. So how do we construct a self-consistency prompt? Well, we start off first with a chain of thought prompt. So we start by prompting the language model using chain of thought prompting. Then we're going to sample diverse reasoning paths. So instead of greedy decoding
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-consistency?u=76281980&t=94)** the optimal reasoning path, we're going to use a sample and marginalized decoding procedure. So this involves sampling from the language models decoder to generate a diverse set of reasoning paths. And then we're going to marginalize the reasoning paths. So each reasoning path might lead to a different answer. We're going to determine the optimal answer by marginalizing out the sampled reasoning paths to find the most consistent answer in the final set. So here's an example. "If there are three cars in the parking lot and two more cars arrive, how many cars are in the parking lot?" Instead of directly answering five, the model might respond with, "There are three cars in the parking lot already, two are arrived. Now there are three plus two, five. The answer is five," right? That is the chain of thought prompting. Self-consistency now, let's talk about what this is. So for a question about how much Janet makes from selling eggs, the model might generate multiple reasoning paths. For example, you know, she has got 16 minus 3 minus 4, 9 eggs left. So she makes 2 times 9. This means she sells the remainder for so on and so forth. And you could see here that there's a few different reasoning paths that we're looking at. So the model can then aggregate these reasoning paths and determine the most consistent answer, which in this case would be $18 per day. So self-consistency, it improves the reasoning capabilities of language models by exploring diverse reasoning paths
>
> **[3:09](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-consistency?u=76281980&t=189)** and selecting the most consistent answer. This method has been shown to provide a significant performance boost on various arithmetic and common sense reasoning benchmarks. So, let's go ahead and set up a chain of thought prompt. So first thing we need to do is follow the same pattern from the chain of thought lessons. We're going to download the chain of thought prompt dataset from [Hugging Face](../../Glossary/Service/Hugging%20Face.md), download the embedding model from Hugging Face, create a prompt template for a chain of thought, create the example selector, and then, finally, construct the prompt. So that's what we're going to be doing here. Subsetting the dataset to 10,000 data points. Here we are downloading the embedding model. Again, BGE-base embeddings. Here we're going to construct our prompt templates. And finally, instantiate the example selector. In this case, again, we're going to use max marginal relevance, passing the selected examples, the embeddings model, vector store, and the number of examples we want to select. Construct our MMR prompt, like so. Passing the example selector, example prompt, the prefix, suffix, and the input variables. So recall prefix and suffix, are going to be the same as our chain of thought reasoning. All right. And now, what we're going to do is have this query and construct our prompt. So now the self-consistency prompt
>
> **[4:46](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-consistency?u=76281980&t=286)** is going to look like so. So we're going to say, "Based on the responses, delimited by these brackets, to the following query, which is going to be delimited by triple backticks, return the response that occurs most frequently." So we have our self-consistency prompt. So we're going to use the n parameter here to generate alternative responses. So you can increase n to explore different variations. So we're going to use a default model, GPT-3.5 Turbo-Instruct here. We're going to get five responses out. And we'll have the LLM generate a number of generations. We're now going to take these generations and parse them to get our final answer. Right. So we had these generations. Went through the generations. Parsed everything here that came after response. Gave this list again to the LLM. The LLM responds with the most frequent response. "There are 29 computers in the server room." And so that is self-consistency prompting. It's a combination of chain of thought prompting, sampling diverse reasoning paths, and then from those diverse reasoning paths, getting the answer that occurs most frequently.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Hugging Face](../../Glossary/Service/Hugging%20Face.md) (2), [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (1)
> **Env Vars:** llm (3), bge (1), mmr (1), gpt (1)
> **Exercise Files:** download the (1), template (1)
> **CLI Commands:** find (1)
> **Versions:** 3.5 (1)
> **Analogies:** for example (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Self-ask](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-ask?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-ask?u=76281980&t=0)** - [Instructor] The Self-Ask prompting method was introduced in October, 2022 in a paper titled "Measuring and Narrowing the Compositionally Gap in Language Models" by Ofir Press and others. This paper investigates how well language models can perform tasks that require compositional reasoning. This means solving problems by combining answers to smaller sub-problems. The researchers introduced a metric called the compositionally gap to measure how often models can answer all the sub problems correctly, yet fail to generate an overall solution. They found as models like GPT-3 get bigger, their ability to answer single hop questions improves faster than multi-hop questions. This means the compositionally gap doesn't decrease with bigger models. While these models remember and recall more facts, they don't necessarily get better at combining those facts to reason through complex problems. The paper introduces a new prompting method called Self-Ask to address the compositionally gap. Here's how it works. Instead of directly answering a given question like you would with direct prompting, the model first asks itself follow-up questions. The model then answers these self-generated follow-up questions. After addressing the follow-ups,
>
> **[1:33](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-ask?u=76281980&t=93)** the model proceeds to answer the initial question. The Self-Ask technique allows the model to reason more explicitly. The structured nature of this method also makes it easy to integrate a search engine to answer the follow-up questions. Further enhancing the model's accuracy. It's like the model is having an internal dialogue with itself to better understand and answer a question. It breaks down a complex problem into smaller parts, addresses each part and it combines the answers to provide a comprehensive response, I've included a pseudocode implementation from the papers [GitHub](../../Skills/Software%20Development/GitHub.md) repo here for you to look at. The [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) code that you'll see in this notebook has the main function self_ask, which starts with an initial question. And if the model generates follow-up questions, the system tries to get an answer from an external source. This process continues until the model has a final answer. The helper functions are placeholders based on the initial code and would need actual implementations. Initially, the workflow of the algorithm is like this. We begin with framing the initial question. We then enter a loop where if a follow-up question is detected, an external answer is sought. This external answer is added to the current prompt and the model's called again. If there's no external, it proceeds to seek the final answer. This process is going to be repeated until a final answer is formulated.
>
> **[3:06](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-ask?u=76281980&t=186)** So just kind of walking through the code real quick. We see here there's a loop until we have no more follow-up questions. We're going to extract the follow-up question from the model's response. We're going to get an external answer for the follow-up question. We're then going to add that external answer to the current prompt. If no external answer is found, we're just going to continue the conversation with the model. We'll check if a final answer is provided. If not, get one. And we have a few helper functions that are not implemented here, but you know, if we assume that they are implemented, this is what these are going to do. The frame questions function is going to frame the question based on the desired format. So for simplicity, we'll just return the question here 'cause it's not implemented. call_gpt is going to interact with the GPT model and return its response. get_last_line is going to extract the last line from the provided text. extract_questions is going to extract the follow-up question from the provided text. And then get_answer is going to get an answer for the provided question from an external source, like a search engine. You don't have to worry about implementing this code yourself because [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) makes Self-Ask easy. So we're going to go ahead and instantiate a large language model and search. For search we're going to use DuckDuckGo. I'm using DuckDuckGo because it does not require an API key. Now what we're going to do is create the tool,
>
> **[4:43](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-ask?u=76281980&t=283)** give it a name. In this case a name must be intermediate answer. The function this tool is going to use is the run function and we give it a description. We're going to pull a prompt from the LangChain prompt hub. You'll see this in the prompt management section, but essentially we're just going to pull the Self-Ask prompt, which looks like this. And we're not going to use this exact prompt, but we are going to use this prompt as inspiration. So let me show you the prompt we're actually going to use, we're going to use a prompt that's specific to [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) type of questions. So you can see here the examples I've come up with model the prompt template that we pulled from the hub where we have questions, see if there are follow-up questions needed. Example follow-up example, immediate answer. Example follow-up example, immediate answer. And then finally final answer. So go ahead and instantiate that. We'll do that from template. And what we're going to do here is bind the LLM to stop generating text when it outputs the string intermediate answer. So this is going to be used in the Self-Ask with Search Agent to have the LLM stop generating after each intermediate answer so that the agent can process the intermediate answer and ask a follow-up before having the LLM continue generating again. The intermediate answer string, again is acting as a delimiter
>
> **[6:17](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-ask?u=76281980&t=377)** that's going to signal to stop generation and parse the intermediate answer. So if we bind a stop sequence, it's going to allow for more fine-grain control over the LLM generation because we have this custom stopping condition. So this type of binding of a stop condition to an LLM is very useful in agentic frameworks like Self-Ask where you want the LLM to generate text in a structured back and forth manner. And here's just a clip from the actual paper about how this thing works, like an example of it, right? So I'll let you go ahead and explore this in the notebook on your own. So we'll go ahead and construct our agent, so we need to import the AgentExecutor. We have an output parser, in this case the Self-Ask output parser has its own output parser. We've got the scratch pad here for the agent's scratch pad. And we're going to format the logs to strings. Instantiate an LLM. We defined an LLM already above, but we're going to bind a stopping condition to it. In this case that stopping condition is intermediate answer. And now we'll set up our agent. We'll give it a agent scratch pad, tell it the observation that it's looking for and a prefix. Again, this is the agent being constructed with LangChain expression language. So we'll pipe all this to the prompt to the LLM with the stopping condition and then parse the answer. And we'll run it right here. And everything is instantiated.
>
> **[7:51](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-ask?u=76281980&t=471)** And now we're going to execute the agent. So I'm asking it a question called what is the RWKV architecture for LLM? So this is a new architecture is definitely not in [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) training data. So this is definitely going to be something that the model has to look up. So let's go ahead and see how the agent goes about answering this question. So now we can have a insight into how Self-Ask with the search works. So it gets this question. Essentially is asking itself, do I need to do any search? It says yes. So it's going to do this follow up. Okay, what does RWKV stand for? Gets the intermediate answer. It stands for random window key value. What's the purpose of this? Designed to improve efficiency and effectiveness of language models. And it parses that answer and then gives us that nice answer right there. And you know, we can even verify this. We can say RWKV architecture. Right? And we can see that this was introduced in May, 2023. What is the RWKV architecture? We can look that up here. So Receptance Weighted Key Value. And so clearly the LLM hallucinated this fact because it is not called the random window key value mechanism, unless of course we look that up, let's see. No. All right. So of course this does not mitigate the hallucination aspect of language models. So obviously use your agents with care. Let's see how it works on this,
>
> **[9:26](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-ask?u=76281980&t=566)** the direct preference optimization question. This was also a method that was introduced in mid 2023. So it was not included in the training data. In this case, I could tell you that this is correct. So direct preference optimization does differ from traditional RLHF and that DPO focused directly on learning from preferences between outcomes. Yeah, so this is absolutely correct. So that's awesome to see here. I invite you to go verify whether or not its answer to RWKV is hallucinated or not. But you know, we are working with language models. These are probabilistic machines, right? So be careful when using your agent and how it's being used because language models are still prone to hallucination. This is just simply showing you the new way that LangChain recommends that you start constructing your agents. Here it recommends that you start constructing your agents explicitly with create_self_ask_with_search. And then we will have the same pattern, instantiate the LLM. We'll pull the prompt. We need to give our agents tools. The only difference here is instead of the code we saw up here where we instantiate our agent, like, so, all we have to do is say, create_self_ask_with_search_agent. Pass it the LLM, the tools and the prompt. And send that to the AgentExecutor. And then we can go ahead and invoke this and see what the answer is.
>
> **[11:02](https://www.linkedin.com/learning/prompt-engineering-with-langchain/self-ask?u=76281980&t=662)** Yes, Hippie Sabotage is in fact an American electronic music duo from my hometown of Sacramento, California. So Self-Ask search is a interesting and novel technique. It does elicit agentic behavior in language models. The reason we're including this in this course is because honestly agents are nothing more than a prompting strategy for language models. So next up we're going to see a few different types of agents, but first we'll see the [react](../../Skills/Web%20Development/React.js.md) agent followed by retrieve augmented generation and more interesting stuff. So I'll see you in the next lesson.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (4), [GitHub](../../Skills/Software%20Development/GitHub.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) (1), [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) (1)
> **Env Vars:** llm (13), rwkv (5), gpt (3), api (1), rlhf (1)
> **Code Identifiers:** self_ask (1), call_gpt (1), get_last_line (1), extract_questions (1), get_answer (1)
> **Definitions:** is a  (2), essentially is (1), stands for (1)
> **Exercise Files:** template (2), github repo (1)
> **CLI Commands:** python (1)
> **Cross-References:** in the next (1)
> **Tools:** github (1)

#### [ReAct](https://www.linkedin.com/learning/prompt-engineering-with-langchain/react?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/react?u=76281980&t=0)** - [Instructor] The [ReAct](../../Skills/Web%20Development/React.js.md) prompting framework was introduced in a 2022 paper by researchers at [Google](../../Glossary/Service/Google.md) called ReAct: Synergizing Reasoning and Acting in Language Models. ReAct stands for Reasoning and Acting. It's a paradigm that was introduced to synergize reasoning and acting in language models. It prompts models to generate both verbal reasoning traces and task specific actions in an interleaved manner. The goal is to allow models to induce track and update action plans, handle exceptions, and gather additional information from external sources. So we can compare four prompting methods here. 1A is the standard prompting method where we just give the model a question and it gives us an answer. B is chain of thought where we provide the model with examples for how to reason. C is just action only where we give the model examples of actions. And D is the reason and acting together. So you'll see here this method interleaves both reasoning and action. So the ReAct prompts here are prompting the model to produce these reasoning traces and these actions side by side. So this allows for more kind of dynamic adjustments as the model continues its prompts. You can see here that they also have an interaction
>
> **[1:38](https://www.linkedin.com/learning/prompt-engineering-with-langchain/react?u=76281980&t=98)** with an external environment. So models can actively interact with external sources like Wikipedia to gather and incorporate more data into their reasoning paths. So ReAct generates this trajectory that's more interpretable and it really helps us see the model's quote unquote decision making process in a more transparent manner. Turns out that this framework is good for a wide range of tasks, from question answering and fact verification to text games and web navigation. So ReAct stands out because traditional methods focus on either reasoning or action. So ReAct does both simultaneously, so this is enhancing a model's capabilities. Unlike models that solely rely on internal knowledge, ReAct can actively seek [Real-Time](../../Skills/Database%20Management/Real-Time.md) information from external sources. This could potentially reduce hallucinations. This interleaved reasoning and action step makes the models decisions more understandable to humans. ReAct is an approach in language model prompting that merges internal reasoning with external data retrieval for more informed and reliable decision making. So how do we implement ReAct in [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md)? It's implemented as an agent type. So the ReAct agent is going to use the ReAct framework to determine which tools to use based on the tool's description. It could be used in various scenarios such as chatbots, document question answering and more. The ReAct agent is designed to be context aware
>
> **[3:11](https://www.linkedin.com/learning/prompt-engineering-with-langchain/react?u=76281980&t=191)** and it can reason about how to answer based on the provided context. It's one of the available agents in LangChain and it does really truly leverage the power of language models to make informed decisions and generate responses. So we start by importing our libraries. Again, using DuckDuckGo because it does not require an API key. We're also including Wikipedia here. And we instantiate the tools, so we have the search tool instantiated, give the search tool a name and a description. Instantiate the Wikipedia tool, also give it a name and description. Put these tools together in a list and let's just call that tool belt. We'll go ahead and pull the prompt from LangChain Hub. I just want to go ahead and print out the prompt template for you. And you can see, it is similar to what we saw previously with Self Ask, but we have these explicit action steps here. The input variables are going to be the agent scratch pad, input tool's name and tools. We'll go ahead and instantiate [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) as our language model. We're going to use the preferred way now, the new preferred way to create a ReAct agent, that's just creating it explicitly like so. We then create the agent executor and now this ReAct agent with the tools we give it, we're going to ask it a question, who is playing in the Super Bowl in 2024? Where will it be played? So this is being recorded on Thursday, February 1st. The Super Bowl is not for another 10 days.
>
> **[4:43](https://www.linkedin.com/learning/prompt-engineering-with-langchain/react?u=76281980&t=283)** So let's go ahead and see what the reasoning traces are for this. And you can see here, the language model is kind of being more explicit in its thought process, right? It's saying I need to go find out what teams are playing. Great, so that means I need to do a current event search. What to the search for? Super Bowl 2024, teams and location. Gets an answer, parses that answer, and gives it right back to us. So remember that LangChain has a ton of tools that you can use in an agent. So I encourage you to go ahead and look at the documentation for tools here. There are a ton of tools that you can use. You can always just go to [GitHub](../../Skills/Software%20Development/GitHub.md), go to the source code, it's what I always recommend. Click on libs and you'll see LangChain community
>
> **[5:39](https://www.linkedin.com/learning/prompt-engineering-with-langchain/react?u=76281980&t=339)** and the tools here. And these are all the different tools that you can use within the ReAct prompting framework. There's a tool here for archive. You can build a ReAct agent that can search archive for you. Bing search, DuckDuckGo search is the one that we used. So there are a wide range of tools that you can use and it's essentially all going to follow the same pattern that I just showed you in this notebook. So I encourage you to go hack around with some of these tools and do some interesting stuff. That's it for the ReAct framework. Again, remember, this is an agent, and an agent does nothing more than a prompting strategy. I know it sounds science fiction-ish to use the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) agent, but really that's all an agent is. I do also encourage you to look into the source code to actually see what's happening under the hood of an agent. For us, this abstraction is very high level, but, you know, you dig down deep into it, there's a number of different calls happening. So again, I recommend bookmarking LangChain's GitHub. You can go to LangChain libraries, go to core, LangChain core, and you can look at the agents here and take a deep dive into what it is that the agent is actually doing. And you can see here, it's just a number of different calls. And if you're curious how the DuckDuckGo search works, you know, you can always go back to LangChain community tools,
>
> **[7:12](https://www.linkedin.com/learning/prompt-engineering-with-langchain/react?u=76281980&t=432)** look into DuckDuckGo search, look in the source code for the tool itself, and you get a better idea of what it is that this abstraction is doing. So I encourage you to satisfy any curiosity, any curious itches you have by referring strictly to the source code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (18), [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (9), [GitHub](../../Skills/Software%20Development/GitHub.md) (2), [Google](../../Glossary/Service/Google.md) (1), [Real-Time](../../Skills/Database%20Management/Real-Time.md) (1)
> **UI Navigation:** go to (4), click on (1)
> **Exercise Files:** source code (4), template (1)
> **Definitions:** is an  (2), stands for (1)
> **CLI Commands:** make (1), find (1)
> **Env Vars:** api (1), gpt (1)
> **Tools:** github (2)
> **Documentation:** the documentation (1)

#### [RAG](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=0)** - [Instructor] Retrieval Augmented Generation or RAG for short is a technique that's designed to enhance the capabilities of [large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) by allowing them access to external knowledge sources. This was a technique that was introduced by Meta AI in September of 2020 and emphasized the potential for language models to [excel](../../Skills/Data%20Science/Microsoft%20Excel.md) in knowledge intensive tasks. In the original Meta paper, they combined an [Information Retrieval](../../Skills/Data%20Science/Information%20Retrieval.md) component with a text generation model, allowing the LLM to access the latest information for generating reliable outputs. This approach was especially beneficial for knowledge intensive tasks where the model needs to provide accurate and up-to-date information. While LLMs have shown remarkable capabilities in understanding and generating human-like text, they have some severe limitations. One is that they could be expensive to train, or fine tune. Two is that their knowledge is static. It's based on the latest training data and it doesn't update with new information. And finally, they sometimes provide incorrect information with high confidence, a phenomenon known as hallucinating. So RAG is able to address these challenges by combining the power of LLMs with the ability to retrieve real time information from external sources. So how does RAG work?
>
> **[1:32](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=92)** There's two components, essentially. One is the retrieval component, where we're given an input in the form of a user question or a query. The RAG is going to retrieve the relevant documents from an external source such as Wikipedia. Then in the generation component, the retrieved documents are going to be combined with the original prompt to form a new enriched augmented context. This context is going to be fed to the language model, which is going to produce and generate a final output. So unlike traditional language models with static knowledge, RAG can access up-to-date information, which is adapting it to situations where facts evolve. RAG allows for integrating new information without needing expensive retraining of the entire LLM. By retrieving information from trusted sources, RAG is going to reduce the chance that the model might hallucinate or provide incorrect information. RAG can be used in a wide variety of practical applications, such as answering questions about dynamic or evolving topics, assisting in domains where real time accuracy is crucial, like medical or legal inquiries or enhancing chatbots or visual assistance to provide more factual and updated responses. In summary, RAG is a powerful technique
>
> **[3:05](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=185)** that's bridging the gap between the vast knowledge that's encapsulated within the weights of a large language model and the dynamic ever evolving real time information in the real world. By combining retrieval and generation RAG's ensuring that language models are knowledgeable while also being relevant, current, and up-to-date. So before building out a RAG system, I think it's essential to familiarize yourself with the components that make the process possible. Each one of these components has a specific role in ensuring that the RAG system operates correctly. At the heart of the system is the language model. This is the core model that's responsible for generating the human-like text response. The vector store is where all of the magic happens. So the vector store is a dedicated storage system that's housing embeddings and their corresponding text data. So it's ensuring quick and efficient retrieval. The vector store retrieval, you could think of this as a search engine of the system. The vector store retriever is going to [Fetch](../../Skills/Web%20Development/Fetch.md) relevant documents by comparing the vector similarities between the user query and what's in the vector database. This way, it's fetching the most relevant document to be put into the context. There's also the embedder. So before storing or retrieving data, we need to convert this text into a format that could be ingested by the Vector Store, and that's an embedding.
>
> **[4:39](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=279)** So the embedding is going to take on this role. It's going to transform text into these vector representations. We also need a prompt. Every interaction starts with the user query or statement. The prompt's going to capture this initial input and it sets the stage for the retrieval and generation process. You also need document loaders. There's so much data out there to process. So the document loader is critical. It imports and reads documents and prepares them to be chunked and embedded. That brings us to the next point, document chunkers. So to make the data more manageable and efficient for retrieval document chunker's going to break the documents down into smaller, more digestible pieces. Last but not least is the user input. So the user input is going to capture the query or statement provided by the end user, and then this is going to be converted into an embedding using the same embedding model that we use to embed our documents. And this is what we are going to fetch documents relative to is this user input. So let's talk about the index subsystem of RAG. So to begin with, we need to load our data using document loaders. So this step is crucial in the process. Tech splitters break down large documents into smaller sections. This is helpful for both indexing data and inputting this into the model. Large sections are difficult to search through and can't fit into a model's limited context window. So that's why we need to split our large document
>
> **[6:12](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=372)** up into chunks. We also need to store and index our splits so that we can search them later. And this is commonly done with a vector store and an embeddings model. Next is retrieve and generate. So retrieve means given a user's input, given a user's query, relevant splits are going to be retrieved from the storage, vector storage using a retriever. The chat model or the LLM is going to produce an answer using a prompt that includes the question as well as the retrieved data. These work together as an orchestrated flow that's going to transform a user's query into a texturally rich and accurate response. So there are many, many document loaders in [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md). I'm not going to go into every single one of them in this notebook, but by all means, check out the documentation here so you can get a good idea of all the different document loaders that you have available to you. I mean, there's document loaders for CSVs, for Dropbox, there's document loaders for Excel, there's document loaders for even WhatsApp. If you wanted to use document loader for WhatsApp, we have one for that as well. So document loaders in LangChain, they're used to load data from the different sources, and these are going to be loaded in as document objects. A document object is just a piece of text with some [Metadata](../../Skills/Web%20Development/Metadata.md). So document loaders give you a convenient way to fetch data from the different sources like text files, web pages, or transcripts of the videos. The main purpose of document loaders is to retrieve data
>
> **[7:49](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=469)** and prepare it for further processing. Document loaders expose a load method that's going to fetch data from the source and return it as a document object. Some document loaders also support what's called lazy loading, which allows data to be loaded into memory only when it's needed. In general, document loaders all follow the same pattern: import the loader class, create an instance of the loader, provide the path to a directory. And then after we create an instance of the loader class, we just point to where our data is. Then we use a load method of the loader instance to load all the files in the directory and convert them into a LangChain document format. So here we're just going to use a web-based loader. I'm going to pull some web pages here. Right, so these are going to be loaded. And just to give you an idea of what one of these objects looks like, let's take a look at this, right? So here we have a list, right? We've got a list, and each element of the list is a document object. So what is a document object? You can see here it's got page content. And the page content here is essentially all the text that is on that webpage. There's also associated metadata.
>
> **[9:25](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=565)** So having the entire text of a webpage might not always work out because language models have limited context window. So that's what we need to split documents and chunk them up. And we do that with text splitters. So text splitters in LangChain are used to split long pieces of text into smaller semantically meaningful chunks. At a high level, they work like this. They split the text up into small semantically meaningful chunks. We start combining these small chunks into a larger chunk until you reach a certain size, and this is going to be measured by some function. Once we reach that size, we make that chunk its own piece of text and then start creating a new chunk of text with a little bit of overlap if you'd like. So this means there's two different axes along which you can customize your text splitter: how the text is split, and how the chunk size is measured. So the default recommended text splitter is the recursive character text splitter. This text splitter is going to take a list of characters, it's then going to try to create chunks based on splitting on the first character, but if any chunks are too large, it then moves to the next character, so on and so forth. By default, it tries to split on new line characters and blanks. In addition to controlling which characters you can split on, you can also control a few other things. For example, the length function, you know, how the length of chunks is calculated. This is going to default to just counting number of characters,
>
> **[11:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=660)** but it's pretty common to pass a token counter here as well. Chunk size is going to be the maximum size of your chunks as measured by some length function. Chunk overlap is going to be the maximum overlap between chunks. So it could be nice to have some overlap to maintain a continuity between the chunks. And add start index is whether to include the starting position of each chunk within the original document in the metadata. And so here we are going to import our recursive character text splitter. We're going to instantiate that as a text splitter. We'll use a chunk size of 500, overlap of 50 as measured by the length function. And we use the transform documents method of the text splitter on the object that we want to split. And let's just take a look at one of these, for example, yolo_nas_chunks. The length of this is 51 and we can see what one element of this thing looks like. You could see here it's just a little bit of text here. Right? We have some text, right? Not the entire blog like we're getting before. Next, we need to create the index system. So the index system is responsible for preparing and organizing the data for retrieval. So there's some steps that we need to do
>
> **[12:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=754)** to create the index system. And one is we got to load our documents. So we need to import and read that amount of text. We chunk them up like we just did, and now we're going to finally embed them. So we're going to convert these chunks into vector representations so that way we can search them using some type of nearest neighbors algorithm. Then we're going to store these embeddings. So we'll use the Open AI's embedding model here. In fact, we'll have to change this. Just be langchain_openai. We'll just use a in memory vector store, FAISS and local file store. We'll go ahead and create our embedder. Right and added our documents to the vector store. Just a note here, CacheBackedEmbeddings, this is just embeddings that are stored temporarily. They're cached temporarily, so we don't need to recompute them. So that's what we're doing here, CacheBackedEmbeddings. And now we have a retrieval system. So as the name suggests, this subsystem is going to fetch the most relevant information based on the user's query. So there's a few steps in the retrieval system. One is to obtain the user's query. Then we're going to transform the user's query into a vector format. That's basically what we just did in the index part. We're going to search the vector store for documents with embeddings that closely match our user's query.
>
> **[14:09](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=849)** Then we're going to return the top matches. This ensures that the most relevant information is provided in the context. So let's go ahead and do that. So we create a vector store and then a retriever here. We're instantiating this as a retriever. We'll use OpenAI. We'll just change this to the latest open AI model. Then it's the augment system. So this just is enhancing the LLMs input prompt with the retrieve context, and it's ensuring that the model has all the information it needs to generate a comprehensive response. So we start with the original user query, then we're going to merge the initial prompt with the context that's retrieved from the vector store. So we now have an augmented prompt. So this augmented prompt is then going to be given to the language model as input. And then after language model does its thing, it's going to generate a response and give us an answer. Right, so in order to get the language model to do its thing, let's go ahead and install LangChain Hub. We're going to do that so we can pull a prompt from the hub. Just quick function here. This is just formatting documents. It's adding two new line characters between each of the documents that we are inserting into the context. Here we are setting up our RAG chain. So here is the retriever. We're going to be getting the context from the retriever.
>
> **[15:44](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=944)** Format documents, this is going to be this function here and that's going to format the documents correctly. And then question is going to be inserted through this runnable pass through. So a runnable pass through just allows you to pass inputs unchanged or with the addition of extra keys. So it's used really to assign data to a new key in the map, essentially. So we're going to be assigning some new data to question. I encourage you to go ahead and take a look at the LangChain documentation. If you go to LangChain right here, type in RunnablePassthrough, and you can read more about the runnable passthrough here. Great, so we've got our RAG chain instantiated. The prompt, let's take a look at the prompt. It's a chat prompt template. You can see here the prompt is like so. So you're an assistant for question answering task. Use the following pieces of retrieve context to answer the question. Right, so we're injecting the user's question here, whatever we're fetching from the user. So that's what we're getting from this runnable passthrough is that question. And then that context is what we're going to get from the retriever, and then the output indicator for the language model to give the answer. And we can confirm the input variable is like so. And there we have it. Let's go ahead and invoke and see what the generation is. All right, so the answer here, you know,
>
> **[17:19](https://www.linkedin.com/learning/prompt-engineering-with-langchain/rag?u=76281980&t=1039)** what does Neural Architecture Search have to do with how Deci creates its models? You see here, this is pulling information from the documents. DeciCoder, we have some information here as well. And we can always get the sources as well. So we can pull the sources that were used to create that query as well. And so that's doing, so right here. And you can see here, these are all the documents that were fetched and injected into the query. Right, so now you're equipped with the ability to use this language model and augment this language model with private company data or with documents that maybe this model wasn't exposed to during training. So it's a great skill to have.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (7), [Fetch](../../Skills/Web%20Development/Fetch.md) (5), [Metadata](../../Skills/Web%20Development/Metadata.md) (3), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (2), [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (1)
> **Env Vars:** rag (15), llm (3), faiss (1)
> **Definitions:** is a  (5), known as (1)
> **Analogies:** such as (2), for example (2)
> **CLI Commands:** make (3)
> **Code Identifiers:** yolo_nas_chunks (1), langchain_openai (1)
> **Documentation:** the documentation (1)
> **UI Navigation:** go to (1)

#### [FLARE](https://www.linkedin.com/learning/prompt-engineering-with-langchain/flare?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/flare?u=76281980&t=0)** - [Instructor] FLARE stands for Forward-Looking Active REtrieval augmented generation. It's method that was introduced in a 2023 paper titled "Active Retrieval Augmented Generation". The original implementation can be found in the paper's [GitHub](../../Skills/Software%20Development/GitHub.md) repo, which the [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) implementation draws heavily from. FLARE is a method that enhances the capabilities of a large language model by actively retrieving information from external sources during the generation process. The motivation behind FLARE is to address the tendencies of language models to hallucinate or produce factually inaccurate outputs. By actively retrieving relevant information during the generation process, FLARE aims to produce more accurate and informed outputs. So the basic idea is we start answering a question. If the language model starts to generate [Tokens](../../Skills/Web%20Development/Tokens.md) that it's uncertain about based on log probs, it's going to look up some relevant documents. It'll then use those documents to continue generating, and it's going to repeat this process until it's finished. The tokens that the model is uncertain about are highlighted, and the LLM is called to generate a question that would lead to that answer. So how is FLARE different from other rag methods? Traditional retrieval augmented generation methods
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/flare?u=76281980&t=94)** typically retrieve information once based on the input and then generate an output. This approach can be limited, especially for generating long text. FLARE differs in that it actively decides when and what to retrieve throughout the generation process. It uses a prediction of the upcoming sentence to anticipate future content, which is then used as a query to retrieve relevant documents. If the generated sentence contains low-confidence tokens, it retrieves relevant information and generates the sentence again. So what does a prompt look like in FLARE? The prompt for FLARE with retrieval instructions involves guiding the language model to generate search queries when necessary while generating an answer. An example is, for example, "The colors on the flag "of Ghana have the following meaning. "Red is for," and then maybe the language model here wants to search for the Ghana red flag meaning, "the blood of martyrs." In the direct FLARE method, the next sentence generated by the language model is used to decide when and what to retrieve. If the language model is confident about the sentence, it's accepted without retrieving additional information. If not, the sentence is used to formulate search queries to retrieve relevant documents, and then that sentence is regenerated.
>
> **[3:09](https://www.linkedin.com/learning/prompt-engineering-with-langchain/flare?u=76281980&t=189)** In order to set up this chain, we need three things, a language model to generate an answer, a language model to generate hypothetical questions and a retriever to use to look up answers. The language model that we use to generate the answer needs to return log probs so we can identify uncertain tokens. For that reason, it's highly recommended that you use the OpenAI wrapper. So again, that's not the ChatOpenAI, it's the OpenAI regular model. So the language model we use to generate hypothetical questions to use in retrieval could be anything. In this notebook, we're going to use ChatOpenAI because it's fast and cheap. And the retriever can be anything. In this notebook, we're going to use the archive loader. Other important parameters to consider is the max generation length parameter, which is the number of tokens to generate before stopping to check if the language model's uncertain. And then also MinProb, so any tokens generated with a [Probability](../../Skills/Data%20Science/Probability.md) below this will be considered as uncertain. So let's go ahead and import what we need, and that's what we're going to do here. And it's the same kind of pattern as we saw in the rag lecture, so the language model, the embeddings. In this case, we're going to [Fetch](../../Skills/Web%20Development/Fetch.md) documents from archive. Rather ironically, I'm going to fetch the FLARE paper itself, instantiate a text splitter, set the chunk size,
>
> **[4:44](https://www.linkedin.com/learning/prompt-engineering-with-langchain/flare?u=76281980&t=284)** set the chunk overlap, split the documents, create that vector store, and then create the vector store retriever. So we'll go ahead and set up our FLARE chain. FLARE chain is instantiated like so. We have the FLAREChain class .from_llm. It's not yet implemented using LangChain expression language, so we can use the legacy instantiation of a chain. And we pass in our language model, pass in our retriever, the max generation length, the min probability to consider for uncertainty, set verbose to true so we can kind of see the printouts from the language model as it's getting the answer, and then have it handle parsing errors for us. And let's go ahead and take a look here. We call invoke on some query. And you can see here it's parsing through the documents and you really do get to see it in action. So you can see here it's generating questions for itself and then coming up with an answer. And I encourage you to kind of look through some of these intermediate steps before it goes and gives us a final answer, and then you'll get a better idea of how it's kind of looking forward, so to speak. So let's see a printout here. Asking to get the steps in the FLARE pipeline. So there's the existing partial response, another partial response,
>
> **[6:19](https://www.linkedin.com/learning/prompt-engineering-with-langchain/flare?u=76281980&t=379)** and it uses these partial response to generate some questions, then fetches the appropriate documents that might be related to those questions. Then it'll finally parse all that together and give us a output once it's finished. All right, so you can see here this has been running for well over five minutes, so this must be a tough question for this to answer. but something I want to call out here is the kind of internal reasoning you're seeing the model do. So you're seeing here that it goes through a chain here, then it goes into a question generation chain. And so there's answering its own questions and here are the questions that it kind of generates. So it's asking itself these questions and then kind of going through the document itself. And you can see these kind of reasoning steps here. And actually, this did end up finishing, it took roughly six minutes. So let's go ahead and see what the final answer is. I'll scroll all the way down here. And here it says "FLARE starts with the user input x "as the initial query to retrieve documents "to generate the first sentence "to bootstrap the iterative generation process "for the following steps. "The tempo-rary next sentence is generated "and checked for low-probability tokens." It sounds exactly like what we had described just earlier. "If present, relevant documents are retrieved "to regenerate the next sentence. "This process continues until the end of the generation." And there you have it, we have a FLARE. So just to kind of recap the steps, the basic idea is to start answering a question.
>
> **[7:53](https://www.linkedin.com/learning/prompt-engineering-with-langchain/flare?u=76281980&t=473)** If the language model starts to generate tokens that it's uncertain about, then we prompt it to look up relevant documents. We then use those documents to continue generating and then repeat this process until it's finished. So kind of recapping it in more concrete steps, step one, start with the user input and the initial retrieved results. Step two is generate a temporary next sentence, check if that contains low-probability tokens. Step three, if the sentence does in fact contain low-confidence tokens, retrieve relevant documents and regenerate the sentence. And then this is repeated iteratively until it ends. So in the case here, you saw this generation took six minutes. So it seems the model was quite uncertain about what it was generating throughout the process, but it eventually got there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Tokens](../../Skills/Web%20Development/Tokens.md) (10), [Probability](../../Skills/Data%20Science/Probability.md) (4), [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (2), [Fetch](../../Skills/Web%20Development/Fetch.md) (2), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Env Vars:** flare (15), llm (1)
> **Definitions:** stands for (1), is a  (1), is called (1)
> **Cross-References:** as we saw (1), coming up (1)
> **Prerequisites:** set up (2)
> **Code Identifiers:** from_llm (1)
> **Tools:** github (1)
> **Exercise Files:** github repo (1)

#### [Plan and execute](https://www.linkedin.com/learning/prompt-engineering-with-langchain/plan-and-execute?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/plan-and-execute?u=76281980&t=0)** - [Instructor] Plan-and-Solve prompting was introduced in a May 2023 paper titled, "Plan-and-Solve Prompting: Improving Zero-shot Chain-of-Thought Reasoning in [Large Language Models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md)," by Lei Wang et al. So what is Plan-and-Solve prompting? Well, essentially, it's a method that's been proposed to address the shortcomings of a Zero-shot Chain-of-Thought prompting. So let's quickly talk about what Zero-shot Chain-of-Thought prompting is. Zero-shot Chain-of-Thought prompting is a technique that's used to elicit complex, multi-step reasoning in large language models without having to craft these intricate step-by-step examples, right? So if you recall, with Zero-shot prompting, what do we do? We simply asked the language model to perform a task. And if you recall, in Chain-of-Thought prompting, we asked the model to come up with its reasoning steps by providing you with several examples of how to reason about something. So we give it a question and examples of how to reason through it. So Zero-shot Chain-of-Thought prompting, actually we do away with the examples and we just add a simple, almost magical phrase to our prompt. And that phrase is, "Let's think step by step." And this approach is really helping improve the reasoning abilities, the Zero-shot reasoning abilities of language models in a wide variety of tasks. A lot of experimental results have shown that Zero-shot Chain-of-Thought prompting significantly outperforms Zero-shot prompting and even Chain-of-Thought prompting
>
> **[1:33](https://www.linkedin.com/learning/prompt-engineering-with-langchain/plan-and-execute?u=76281980&t=93)** in a few different scenarios. So here we can see a difference between Zero-shot Chain-of-Thought prompting, which is in example A, and Plan-and-Solve prompting, which is in example B, and then example C, here's what's called a answer extraction, but you could see that the main difference here is in Zero-shot chain-of-thought prompting, we're we're adding that magic phrase, "Let's think step by step," but we're doing something different in Plan-and-Solve prompting. So let's talk about exactly what we're doing in Plan-and-Solve prompting. We're asking the language model to devise a plan and then carry out that plan. So, essentially, Plan-and-Solve prompting is dividing this task into two main components. The planning phase, where the model is going to first devise a plan by breaking the entire task into smaller sub-tasks, and then the solving phase, where the model's going to execute the subtasks according to the plan. So instead of just prompting the model with let's think step by step, Plan-and-Solve prompting is going to say, "Let's first understand the problem and devise a Plan-and-Solve the problem. Then let's carry out the plan and solve the problem step by step." There is a enhanced version of Plan-and-Solve prompting called Plan-and-Solve Plus. So this will include more detailed instructions to kind of address calculation errors and help improve the quality of the generated reasoning steps. And so an example of Plan-and-Solve Plus
>
> **[3:06](https://www.linkedin.com/learning/prompt-engineering-with-langchain/plan-and-execute?u=76281980&t=186)** would be the one right here. So example C. So we're asking it to extract kind of that actual percentage. Plan-and-Solve prompting is really promising because it's providing more structured guidance to the model. It's helping ensure that the model thinks step by step and plans its reasoning process before kind of just predicting the next token. This leads to more coherent and accurate reasoning chains. The papers experiment show that the Plan-and-Solve Plus prompting consistently outperform Zero-shot Chain-of-Thought across various data sets. It's actually comparable and even superior to other methods like Zero-shot-Program-of-Thought prompting. Despite manual demonstrations, the Plan-and-Solve Plus prompting actually outperforms 8-shot Chain-of-Thought prompting in arithmetic reasoning tasks. In [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md), Plan-and-Solve prompting is executed via the Plan-and-Execute agents. So remember, an agent is really nothing more than a prompting strategy. So in LangChain, the Plan-and-Solve prompting framework or Plan-and-Solve prompting methodology is implemented as a plan-and-execute agent. So the Plan-and-Execute agent is just going to first plan what it needs to do and then execute those sub-tasks. So there's a couple of key components to the Plan-and-Execute agents. There's the planner, which is going to plan out the steps and deal with whatever ambiguities or edge cases might arise to the extent that it can. Then there's the executor.
>
> **[4:40](https://www.linkedin.com/learning/prompt-engineering-with-langchain/plan-and-execute?u=76281980&t=280)** So this will be an action agent that's going to take the high-level objective from the planner and determine what tools it needs to use to achieve those objectives. And surprisingly, or not, this is done in two steps. There's the planning and the executing stage. So the planning stage, again, just mapping out the steps that's required to achieve the objective, and the language model's reasonability is going to be harnessed to plan and deal with any ambiguities or edge cases. Then in the execution stage, it's going to be a separate agent that's going to be equipped with the necessary tools, and it's going to take over in this phase. So whatever the high-level objectives it receive from the planner, it's going to determine what tools it needs and execute on that. So Plan-and-Execute agents are really effective when the objective you have requires some complex planning and coordination of multiple subtasks. So these agents can handle tasks that involve multiple steps and multiple dependencies between the steps. So consider the complexity of what it is that you're trying to do. And if what you are trying to do requires you to plan and coordinate some actions, then that might lend itself this prompting framework and this agent kind of usage. If you can achieve your objective through some simple sequence of steps without much planning, then you might want to look for another type of agent, a simpler agent. So let's go ahead and see this in action in LangChain.
>
> **[6:15](https://www.linkedin.com/learning/prompt-engineering-with-langchain/plan-and-execute?u=76281980&t=375)** Start by a necessary import. So within LangChain, Plan-and-Execute is housed in the experimental module. So we're going to go ahead and define our agent. In this case, let's actually use [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md)-0125-preview. We're going to instantiate the math_chain. So this is just a particular chain in LangChain where it'll make use of the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) interpreter to actually do some math calculations. DuckDuckGoSearch, using this because it doesn't require an API key. And then, of course, let's set up our agent tools. Implement the planner. So again, we'll use the newest instance of GPT-4. Always set the temperature to zero when you're using an agent because you don't want the language model to be creative at all here. You want it to explicitly follow your instructions. So we go ahead and we can look at the planner. Now let's look at the prompt of the planner. So we can take a look at the prompt message from the agent. And you can see here, it's saying, "Let's first understand the problem and devise a plan to solve the problem. Please output the plan starting with the header 'Plan:' and then followed by numbered list of steps," right? So this is a prompt that we are giving the planner. Then we, of course, instantiate our executor. So the executor's just going to use the same exact model. We're going to use GPT-4 as well for the executor. We can make it even more explicit like this
>
> **[7:48](https://www.linkedin.com/learning/prompt-engineering-with-langchain/plan-and-execute?u=76281980&t=468)** if it is helpful to you. But it's the exact same model that we're going to use. We could say executor_model, right? And instantiate that, like so. And so now we can print the prompt template for the executor. We tell the agents what tools it has access to. So the search tool. And this is coming from the actual prompt of the search tool. And we can execute. We can see how it is working. So we're entering the planning stage, right? Here's my query that is being sent to the agent. So this is essentially being input into the prompt template for the actual planning agent, right? And the agent you can see here is kind of thinking through what it needs to do. All right, well I need to go and I need to look up the current temperature. And you can scroll all the way here and you can see that it is using DuckDuckGoSearch. What is it looking in DuckDuckGoSearch? It's looking for the current temperature in Winnipeg, Manitoba. Get some results. It's outputting as a [JSON](../../Skills/Web%20Development/JSON.md), because in the prompt for the DuckDuckGo tool, it is asking it to respond in JSON. So it does that. So here's the next step, all right, look at the current temperature. All right, the current temperature is this, and then it's a new executor chain. Now it's saying, "Okay, well I need to go and get the absolute value of it," right?
>
> **[9:24](https://www.linkedin.com/learning/prompt-engineering-with-langchain/plan-and-execute?u=76281980&t=564)** And it gives me the answer. And, of course, you could try this with any number of questions, if you'd like. So we can say something along the lines of, "When is the next webinar on Direct Policy Optimization?"
>
> **[9:48](https://www.linkedin.com/learning/prompt-engineering-with-langchain/plan-and-execute?u=76281980&t=588)** and we'll let this run live for us. So we're entering the Plan-and-Execute chain. And you can see here the reasoning steps, search for upcoming webinars. And you can see here the next step, okay, I need to find the date, time, and how to register. Then the step here, give them the above steps. Respond to the user's question, right? And you can see the output here. So to find information on upcoming webinars, a search was conducted, but the search details, date, time, registration were not provided, all right? So it's saying that it went to look for this and did not find an answer, which is reasonable. So we can go and add maybe more details. When is the next webinar on Direct Policy Optimization from [DeepLearning.ai](https://DeepLearning.ai)? And so we're being more specific with the query. And let's see if this actually does help the Plan-and-execute agent find the answer. So here the step is like, "Hey, let's visit official [DeepLearning.ai](https://DeepLearning.ai) website or their social media channels. Look for any announcements," right? So these are the steps in the reasoning process that it's coming up with, right? You can see here that it's got this little bit of information here where it's actually telling us what DPO pipeline is. That's quite interesting. All right, so it says here, "The agent, after conducting searches and reviewing the steps taken,
>
> **[11:22](https://www.linkedin.com/learning/prompt-engineering-with-langchain/plan-and-execute?u=76281980&t=682)** specific information was not found," right? So you can see, of course, this also has some limitations, but it's valuable in the sense that it is actually taking reasoning steps and outlining how it needs to go about finding an answer. So I encourage you to play with the Plan-and-Execute agent. Use different sets of tools. You know where to go to find tools and use this and give it a shot, and see how you can use it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (5), [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) (3), [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (2), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **CLI Commands:** find (5), make (2), python (1)
> **Env Vars:** gpt (2), json (2), api (1), dpo (1)
> **Definitions:** is a  (3)
> **Code Identifiers:** math_chain (1), executor_model (1)
> **URLs:** [deeplearning.ai](https://deeplearning.ai) (2)
> **Exercise Files:** template (2)
> **Prerequisites:** required to (1), set up (1)


### 7. Prompt Management a.k.a. PromptOps

[↑ Back to Table of Contents](#table-of-contents)

#### [Prompt management](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-management?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-management?u=76281980&t=0)** - [Instructor] As you've come to learn throughout this course, [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md) is an important part of working with [large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md). A well-crafted prompt can lead to insightful, accurate responses. While a poorly structured one may result in irrelevant or inaccurate information. But prompt engineering presents a unique set of challenges. LLMs exhibit a remarkable sensitivity to even the most minor changes to a prompt, a mere tweaking and wording, adding or removing punctuation, a subtle shift in phrasing or even just adding an extra space or new line character will drastically and dramatically transform a model's output. This high sensitivity requires careful consideration with every element in the prompt. We need to [Version Control](../../Skills/Web%20Development/Version%20Control.md) prompts the same way we version control code. As you've come to see throughout the course, finding the optimal prompt often involves a time-consuming process of trial and error. It's an iterative journey and a systematic approach. You carefully adjust the prompt, you evaluate the impact of that change, and then you go and refine that prompt further. Given the number of iterations and slight changes made during prompt optimization, it could become challenging to remember or track what changes were made between different runs. This can lead to confusion and difficulty understanding which alterations led to a particular outcome. You need a way to examine and revisit previous versions
>
> **[1:36](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-management?u=76281980&t=96)** or restore a previous version of a prompt using a prompt management tool. Like [Software Development](../../Topics/Software%20Development.md), Prompt engineering needs a robust system for managing in version controlling prompts. This system should involve meticulously documenting each change to a prompt along with the corresponding outputs. This approach is essential for systematically analyzing and refining prompts. Imagine a librarian meticulously cataloging books. In a similar way, a practical prompt to management system would organize and track the evolution of prompts and enable a more structured and effective optimization process. Remember, LLMs are super sensitive to even tiny prompt changes. Perfecting prompts is a journey of trial, error, and patience. You need a system that will keep track of every tweak so you can avoid confusion and understand what changes lead to a particular outcome. You need a robust, prompt management system for a smooth and effective optimization process. Just like a jeweler or a librarian, precision and organization are keys to success in prompt engineering, and that's what this section is about. You're going to learn a prompt management system that comes built into [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) called LangSmith.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md) (4), [Version Control](../../Skills/Web%20Development/Version%20Control.md) (2), [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (1), [Software Development](../../Topics/Software%20Development.md) (1), [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (1)
> **Definitions:** is an  (1), is a  (1)
> **Analogies:** imagine (1), just like (1)
> **Speakers:** - [instructor] (1)

#### [LangSmith](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith?u=76281980&t=0)** - [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) gives you a streamlined approach to [Prototyping](../../Skills/Software%20Development/Prototyping.md) applications and agents using [large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md). But the journey from prototyping to deploying LLM applications requires extensive iteration on prompts, chains, and agents to attain high-quality outputs. This is where LangSmith comes in. LangSmith is a unified platform that's designed to assist in debugging, testing, and monitoring LLM applications. For those working with LLMs, LangSmith is useful in several scenarios. If you're developing a new chain, agent, or set of tools, LangSmith can help you quickly identify and resolve issues. LangSmith helps in visualizing the relationships and usage patterns of different components, like chains, LLMs, and retrievers. This is crucial for understanding the overall structure and functionality of your application. LangSmith also enables the evaluation of various prompts and LLMs for a specific component, aiding in selecting the most effective combinations for your application. It also allows for repeated testing of a chain over a data set to confirm consistent quality, which is an essential step in ensuring the reliability of your LLM-based applications. For capturing your usage traces and leveraging LLMs or analytics pipelines for insight generation, LangSmith provides you a platform
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith?u=76281980&t=94)** that simplifies and streamlines this process. So to sum it all up, LangSmith is a platform that's designed to streamline the development and deployment of LLM-based applications. It includes tools for debugging, testing, and monitoring, and simplifies the transition from prototype to fully operational [products](../../Skills/Software%20Development/Microsoft%20Products.md). LangSmith also facilitates extensive tweaking of prompts and components for optimal performance in production environments. It's ideal for developers facing challenges, managing, and refining LLM capabilities. And in the next video, we'll get into the LangSmith UI and see what it's all about before seeing some coding examples.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Prototyping](../../Skills/Software%20Development/Prototyping.md) (2), [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (1), [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (1), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1)
> **Env Vars:** llm (5)
> **Definitions:** is a  (2), is an  (1)
> **Cross-References:** in the next (1)
> **Speakers:** - langchain (1)

#### [LangSmith walkthrough](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-walkthrough?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-walkthrough?u=76281980&t=0)** - [Instructor] In order to use LangSmith, you first have to sign up and obtain an API key. You could do that by going to smith.[langchain.com](https://langchain.com). And as you'll see, you've got multiple options to sign up. You can sign up with Discord, with your [GitHub](../../Skills/Software%20Development/GitHub.md), with [Google](../../Glossary/Service/Google.md) or with your email address. But once you've signed up for LangSmith, you'll be brought to a page that looks something like this. Now, first thing you want to do is go and get your API key. So you go down to here where it says API Keys. Scroll down to the bottom and hit create API Key. When you hit create API Key, a pop-up box will come up and that pop-up box will have a API key for you to use. For example like this. You'll need to copy this API key and paste it somewhere where you won't lose it. Now I'll go ahead and delete that API key so that nobody else can take it and use it. But you want to make sure you store that API key somewhere safe. Now let's go ahead and poke around the UI for LangSmith. You'll see here that you've got a number of different tabs that you can navigate to. You've got projects, annotation cues, deployments, dataset and training and the hub. Projects will be just all the different runs that you are doing with LangSmith. And we'll do one a bit later. Annotation allows you to add human feedback to your LLM projects.
>
> **[1:35](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-walkthrough?u=76281980&t=95)** Deployments is based on LangServe, which we're not going to be covering in this course. You can add new datasets to LangSmith simply by uploading here. You'll see here I have some datasets. These were related to some benchmarking I was doing with a comparing and contrasting to language models for their agentic behavior. And then you finally have the LangSmith hub. Now the hub is a place where you can download prompts. This is also a place where you can push and pull prompts too. So this is going to be the central repository for you to push and pull hubs to and from. So to create a new prompt, you just hit that plus button. You'll create a name for the prompt, a description of the prompt, a read-me, what it's used for, the language that prompt is in, a model that you want to associate it with. Any type of settings and then you can hit whether it's private or public. So let's go ahead and fill that out. So this prompt name that we're going to use here, I'll put in [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning Example. Now you'll see there, apparently you can only use lowercase letters. I'm going to go ahead and add some hyphens here for readability. Let's say LinkedIn Learning example. Description, this is a prompt that we are using in the LangSmith section
>
> **[3:10](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-walkthrough?u=76281980&t=190)** of the LinkedIn Learning course titled [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md) with [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md). And we'll spell learning correctly. Read-me, this is entirely up to you. I'll just say nothing to say here. Particular use case that we'll do here. We're just going to come up with a basic kind of example. So I'll just put something like maybe code writing as the use case. Language will be English. Model we'll use this for, we'll use it for any one of these models you could choose. You could even choose to leave that blank. License, we can leave that blank. I'll make this private so only we can see it. We'll hit save, right? And now we can start actually creating the prompt. So to create the prompt, we'll just do this. Alright, so we can come up with a template here. For this one I'll say you're an expert in [PyTorch](../../Skills/Artificial%20Intelligence%20(AI)/PyTorch.md) [Hugging Face](../../Glossary/Service/Hugging%20Face.md) and OpenAI. You're helping a user write code to complete their work. And of course, just use whatever best practices you'd like to create the template here. I'm just doing this for illustrative purposes. You're helping a user write code to complete their work. Be brief and speak as if you were
>
> **[4:45](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-walkthrough?u=76281980&t=285)** a punk rocker. Great. And so we can play around with this here. So here the OpenAI model they've got some settings that we can use. Text DaVinci, we're not going to use that. Let's just use GPT-3.5 turbo. We've got other options here, but let's just stick with GPT-3.5 turbo and instruct. Drop the temperature lower. You can see all the different settings that you have available to you. Of course, these are also available to you in pure LangSmith, even though we didn't mess with any of these in the course. And we can say, enter a value here. Write the PyTorch training loop. And go ahead, hit start. And of course we need to enter a [OpenAI API](../../Skills/Artificial%20Intelligence%20(AI)/OpenAI%20API.md) key. So that's where you would enter this information here. You can go ahead and enter your OpenAI API key and hit start. And you'll see here that we're getting a response, right? And we can play around with the prompt here. See kind of minor tweaks, what they do. For example, you're expert in PyTorch, Hugging Face and OpenAI. You know everything about prompt engineering and [large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md), right? And then maybe you want to add a delimiter here, right? And then we can add a question here, like what's the best prompt to get a LLM to write code for Hugging Face models?
>
> **[6:22](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-walkthrough?u=76281980&t=382)** Just kind of as an example, right? Hit Shift + Enter and it'll run. Now what we're going to do is actually commit this. And once we commit this, we'll see how we can play around with this programmatically. So I'll go ahead and commit this, and in the next video we'll see how we can programmatically play around with this prompt, make changes to it, and then push it back to the prompt up here.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (3), [PyTorch](../../Skills/Artificial%20Intelligence%20(AI)/PyTorch.md) (3), [Hugging Face](../../Glossary/Service/Hugging%20Face.md) (3), [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (2), [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md) (2)
> **Env Vars:** api (11), llm (2), gpt (2)
> **CLI Commands:** make (3)
> **Versions:** 3.5 (2)
> **Tools:** discord (1), github (1)
> **UI Navigation:** scroll down (1), navigate to (1)
> **Exercise Files:** template (2)
> **Definitions:** is a  (2)

#### [Prompt versioning in LangSmith](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-versioning-in-langsmith?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-versioning-in-langsmith?u=76281980&t=0)** - [Instructor] Utilizing the latest version of a prompt can lead to some unpredictable issues in production environments. For more stable and consistent deployments, it's advisable to use specific prompt versions. [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) Hub supports pulling prompts by their specific commit hash. This ensures that you're using the exact version of the prompt needed for your application. To specify the version, you'd use a pattern like this. You simply just add the version tag to the prompt ID in the pull command. Here's a [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) snippet for reference. Let's go ahead and load a prompt from the LangChain Hub. Specifically. We are going to load this prompt, which we have created in the previous video. So we'll write a function here that is just going to pull the prompt from LangChain and then push the prompt from LangChain. We will instantiate an LLM, because we want to do some generations with it. The repo is going to be the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md)-learning-example, which is what this is called, and it's coming from my user. And so we can see the prompt as so. We can construct the prompt chain and then get a result. I'll go ahead and let that run and then we'll pretty print out the results. All right, and you can see we've got the result here. I simply just ask, "How do I generate text
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-versioning-in-langsmith?u=76281980&t=94)** with a [Hugging Face](../../Glossary/Service/Hugging%20Face.md) transformers model?" And it goes on and it tells me exactly what it is I need to do. Now what I want to talk about is actually how to upload a prompt to LangChain Hub. So we saw how easy it was to retrieve a prompt from LangChain. It's quite user friendly, and the nice thing about this is that uploading your prompts to LangChain is quite user friendly as well. This is nice, because it's going to allow you to share and manage your own custom prompts efficiently. So to upload a prompt to LangChain Hub, it's quite simple. You start by creating a simple prompt. Then you can ensure that it meets the criteria and standards that's expected by the LangChain Hub. And the upload process is quite easy. It's simply two key components that you need. You need your account handle. So this is the unique identifier within the LangChain Hub. So for example, mine was datascienceharp, which happens to be my username everywhere. Then there's the name of the prompt. So it's kind of a descriptive name for your prompt. And so the one that we just pulled in this example was the Linkedin-learning-example. Once you have that, it's real simple to upload your prompt to the LangChain Hub. It's just this right here. You fill in your account handle and the name of your prompt, and then just push it. So what we're actually going to do now is we're going to make an amendment to the prompt that we have just pulled from LangChain Hub. So if you recall, our prompt looks something like this. Now to add to this prompt, since it is just a string,
>
> **[3:12](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-versioning-in-langsmith?u=76281980&t=192)** we can simply do this type of pattern here, plus equal. We'll see what our prompt looks like now, right? And you can see here that you write code that follows PEP8 standards and just being appended right to it, right? Of course, you can also follow principles for prompt pipelining that we had discussed before, right? So we can say something like prompt is equal to prompt plus, and then have whatever string that we want, right? And that's, you know, for example, "You also hate being nice," for whatever reason, right? Just kind of an example to show you what different ways you have to amend the prompt. And you can see here we've got that there. Great. Now what we're going to do is we're going to push this to LangChain Hub. Great. And you can see here this is the version of that prompt. So when we go back to LangChain Hub, you'll see here that this was updated just a few seconds ago. You can see the different commits that we have here. So we can see here, this was the initial commit that we had seen. This right here is now reflecting the changes that we have just made. And this specific version of the prompt now has the version appended to it. So we can copy that, and we can actually just pull this now into our environment. Use that helper function that I had defined before. We could just pass the arguments here
>
> **[4:44](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-versioning-in-langsmith?u=76281980&t=284)** for the particular repo. And then the version here. So we can just copy that version from down here. Of course, I may have over-engineered the thing by having this helper function, because you can always just pull the prompt just like this, right, like so. Right. And now we have the prompt as such. And of course, we can pass this like we normally would. So we have our LLM chain that we have defined. Let's just redefine a new one. We'll say chain_new. This is going to be the prompt with the LLM. We'll also just have our string up with parser in as well. So from langchain_community.output_parsers import.
>
> **[5:49](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-versioning-in-langsmith?u=76281980&t=349)** of course, it's not from langchain_community, this should actually just be coming from langchain.
>
> **[6:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-versioning-in-langsmith?u=76281980&t=360)** Live debugging in action. You'd think that I would remember how to do all this by now. All right, so it's actually from langchain_core, my friends. So we're pulling from langchain_core. We'll have an output parser, we'll add that output parser here. And of course, we'll just pass this to our language model and generate some text. So we'll just use streaming for this example. So for chunk in chain_new.stream. And you'll remember that the stream will take a dictionary, and that's going to be question for the input variable, right? Question. The question I want to ask it is, how about, "How do I hack an API?" We'll see if it chooses to answer that question or not. I'll print chunk, end equals this, flush equals True. And we'll get our response here. All right. So it's, you know, just saying, "Don't be illegally tinkering with an API." So yeah, it's quite nice. It is responding in a punkish manner like it's supposed to. And yeah, it's giving us good advice. "Being a punk rocker in the tech world isn't about breaking the rules just because." There you go. So now I hope you understand the importance of doing this type of managing and versioning of prompts, right? You want to treat prompts just like you would treat code.
>
> **[7:35](https://www.linkedin.com/learning/prompt-engineering-with-langchain/prompt-versioning-in-langsmith?u=76281980&t=455)** So you want to make sure that whatever application you're building, that you're loading, whatever specific version of the prompt that you need, so that you can ensure stability in the deployment, this is going to safeguard you against any type of unintended consequences of ongoing updates, because things are changing rapidly. So remember that with prompt versioning, you and your team can freely commit new versions to the prompt repo. This flexibility is really going to give you a more dynamic environment for experimentation and for improvement. [Version Control](../../Skills/Web%20Development/Version%20Control.md) plays a crucial role in preventing the accidental deployment of untested or under-validated components. So select versions of prompts that are proven. That way you're able to maintain the integrity and reliability of your system. Prompt versioning also has an added benefit of just streamlining your development process. This, you know, allows for [agile](../../Skills/DevOps/Agile%20Development.md) development and iteration without risking your production environment. So incorporate prompt versioning into your LangChain workflow, not just because it's the best practice for stability and reliability, but because it's really a strategic approach to having more innovation and collaboration, but in a efficient manner. So prompt versioning is key to [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md). Treat your prompts like you would treat code and you will be in good shape.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (16), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Hugging Face](../../Glossary/Service/Hugging%20Face.md) (1), [Version Control](../../Skills/Web%20Development/Version%20Control.md) (1)
> **Code Identifiers:** chain_new (2), langchain_community (2), langchain_core (2), output_parsers (1)
> **Env Vars:** llm (3), api (2), pep8 (1)
> **Analogies:** for example (2), just like (2)
> **CLI Commands:** make (2), python (1)
> **Cross-References:** previous video (1), go back to (1)
> **Definitions:** is called (1)
> **Best Practices:** best practice (1)

#### [LangSmith deep dive](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-deep-dive?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-deep-dive?u=76281980&t=0)** - [Instructor] In this notebook, we're going to do a bit of a deep dive into LangSmith. Now, this notebook was co-authored with my friends at the AI Makerspace. I highly recommend you check out their YouTube channel for hands-down the best educational content on all things LLM and all things AI engineering. Be sure to connect with Chris Alexiuk and Greg Loughnane on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md). I've learned so much from Chris and Greg about AI engineering and working with language models. So I'm going to really quickly breeze through everything in the first half of this notebook because you've seen this all before. We're instantiating an LLM, setting up Nest sync AO so that we could do asynchronous stuff in a Jupyter Notebook. We're instantiating a document loader. This document loader is actually just a site map loader. So it's going to go through here, the site map for [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) and just essentially it's loading in all the docs. So we can take a look at this once it's done. All right, and we can see here that we have a list of documents, right, which is 169 elements, right? Each one of these document objects is essentially the entire text for a particular page. So this page is generating usable text with AI, so on and so forth. And you can check out the blog itself if you'd like by go in here, right?
>
> **[1:35](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-deep-dive?u=76281980&t=95)** So essentially, this is what we've done. We've loaded the entirety of the LangChain blog into a list where each page in the LangChain blog is a document object now. We're going to split the LangChain blog up. We'll use a chunk_size of 256, chunk_overlap of 16. And so we've got 2014 elements of documents. So just to verify, right, you can take a look at one of these split_documents, right? Let's look at the first element in this list, the 42nd element of this list. And you can just see it's just a chunk of text. We've got the associated [Metadata](../../Skills/Web%20Development/Metadata.md) with it as well. It's just text. We are going to instantiate a embeddings model. We're going to go ahead and set up a vector store now. I use capture, so I don't see all the printout from all the stuff installing, just keeps the notebook clean. We're then going to instantiate the vectorstore and set up a vectorstore retriever, right? So we've seen this before, right? If you're forgetting what this process is, you can go back to the retrieval section of the course or you can look at the video on retrieval augmented generation because that's essentially what we're doing here, right? So we're going to let this work and do its thing. So right now what's happening is all those chunks of documents are being passed to our embeddings model. Those embeddings is just a fixed length vector, right? It's just vectorizing each one of those chunks, putting it into this vector database, right? And now we have a retriever that is set up. So what's going to happen is
>
> **[3:09](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-deep-dive?u=76281980&t=189)** that a user is going to input a query. That query will be embedded using the exact same embedding model that we used to embed all the vectors in this vectorstore. It's going to [Fetch](../../Skills/Web%20Development/Fetch.md) similar documents and inject that right into this prompt_template right here. So of course, we are familiar with prompt_templates. We know what this is all about. We also know all about how to create a chain using LangChain expression language. And here we are just going to invoke and get a response and we get a response. Great. All right, so now, this is where we're going to see some LangSmith action here. So it's going to be cool to see this side by side with the LangSmith screen here. So let's go to the project here, just setting environment variables so that LangSmith knows that we're now tracking the LangChain API key. You should already have that. I'll go ahead and enter mine. So let's look what's going to happen here. So let's go ahead and invoke this generation and see what happens here on our Projects page. Now, you can see here that we have a new project that just was opened up, right? So this is the current date and time, right? And this project has now kicked off, right? And we could take a look in depth what is happening under the hood. So we have a RunnableSequence. What's happening in the RunnableSequence. We've got, you know, this question coming in. These are all the documents that are being retrieved.
>
> **[4:42](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-deep-dive?u=76281980&t=282)** We get all this source information, location, all this great metadata, and we get the output from the LLM as well. Let's go ahead and dig a bit deeper here now. So now what we're going to do is we're just going to create a testing dataset. So let's go ahead and create a testing dataset. I'm going to go ahead and navigate to the dataset page here and you'll see what will happen, right? This will take a second to load into its thing here. So we're getting this warning because apparently I had passed in the wrong name for an embeddings model, but it's okay. This will just work just fine for us. Let's go ahead and make sure that our dataset has generated, which it has, that's the current date and time. And now we have a dataset here. You can click in on an element of the dataset. You can see here, here's a question, here's the page content that is retrieved for that. We can also do some evaluation as well. In this case, we're going to do some evaluation on, you know, harmfulness and some additional evals as well. So let's go ahead and just run this and we'll see it come up on our Projects here. You'll see here that we also get a nice link that we can click on. So let's go ahead and just click on that link, right? Here's the question. We get all this wonderful metadata. You can see all these runs are still pending. This is all, again, you can go to Dataset and Testing.
>
> **[6:16](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-deep-dive?u=76281980&t=376)** This is going to be saved here, Projects as well. This will be saved here as well. So you can see how many [Tokens](../../Skills/Web%20Development/Tokens.md) we've used, cost 15 cents to do all this stuff. And you can see all these particular runs that are happening, right? So here's the harmfulness criteria that is being checked against, right? And we get the response here from the language model, right? And so we get all this wonderful tracing, so much detail. You can see all the detail that we get from LangSmith. So we get really full visibility into our [Application Development](../../Skills/Software%20Development/Application%20Development.md). Here, of course, if we want to do some more re-ranking, we could do that. So you'll need your Cohere API to do that. So this is just kind of added extra stuff here and I will see our new chain. So let's go back to Projects here. We have a new evaluation criteria. The code itself isn't that important. I just want to show you how you can see all this stuff in LangSmith. So we'll go ahead and let this run, right? We get a direct link to our project that we can check out. Of course, it's pulled up right here as well. We can take a look here and you can see all of these evaluations that are running. Get full visibility into all this wonderful information. You can add your feedback here if you'd like to. There's metadata associated with it, or we get information in terms of latency and cost.
>
> **[7:51](https://www.linkedin.com/learning/prompt-engineering-with-langchain/langsmith-deep-dive?u=76281980&t=471)** And you know, you can click into this and each one of these, you know, we can see the scoring. You even click into this a little bit further if you'd like. And you could see all the different bits of contents that's retrieved. You can click on Open Run and get even more under the hood, see all the retrieved context and the response from OpenAI here. You could see how with LangSmith, you get full visibility, transparency into the development of your application. So that's it for this video. This next video is actually not about LangSmith, but it's about how to manage the length of your prompts.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (6), [Metadata](../../Skills/Web%20Development/Metadata.md) (4), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [Fetch](../../Skills/Web%20Development/Fetch.md) (1), [Tokens](../../Skills/Web%20Development/Tokens.md) (1)
> **UI Navigation:** click on (3), go to (2), navigate to (1)
> **Code Identifiers:** chunk_size (1), chunk_overlap (1), split_documents (1), prompt_template (1), prompt_templates (1)
> **Env Vars:** llm (3), api (2)
> **Prerequisites:** set up (3), you'll need (1)
> **Cross-References:** go back to (2), next video (1)
> **CLI Commands:** make (1)
> **Documentation:** the docs (1)

#### [Managing prompt length for agents](https://www.linkedin.com/learning/prompt-engineering-with-langchain/managing-prompt-length-for-agents?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/managing-prompt-length-for-agents?u=76281980&t=0)** - [Instructor] You've seen agents that work in [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) and if you recall, agents use tools, but every time they call a tool, the result of that call is added back to the prompt so that the agent can go and plan the next action. But depending on what tools are being used and how those tools are being called, the prompt for this agent can grow very large. In fact, it can go larger than the context window for the model. Luckily, LangChain expression language makes it easy for us to add a custom function that will help us manage the size of our prompt within a chain or an agent. All right, so let's go ahead and instantiate a couple of tools for our agent 'cause as you know, agents need a toolkit. Two tools we're going to use is Wikipedia Search and Duck Duck Go Search. We'll go ahead and create a tool belt out of that and just call it tools and we'll go ahead and create a chat prompt template. So here we're essentially telling the system message that you are the world's greatest research assistant. You know exactly where and what to look for when you're given a query. You've been a research assistant to people like Yann LeCun Geoffry Hinton, and Francois Challet. So I'm hinting to this particular system message that I want the model to help me with [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) concepts. Go ahead and instantiate an LLM in this case we'll use chat OpenAI. I'll use the GPT-3.5 Turbo model because the prompts will get a bit long here. So I want to keep the cost to you down low. You can however swap this out for any model that you'd like.
>
> **[1:32](https://www.linkedin.com/learning/prompt-engineering-with-langchain/managing-prompt-length-for-agents?u=76281980&t=92)** So go ahead and do that. I'll go ahead and instantiate the agent before I run this bit of code. However, we'll instantiate the agent. First thing we'll do, we've seen this pattern before. We need to give the agent a scratch pad. In this case, I want the agent to format function to messages for OpenAI. Got the prompt and we're going to bind a function. And the functions that we're going to bind are the tools. And I'll go ahead and send this to the OpenAI function agent output parser and set up the agent executor. We're going to log everything to LangSmith. So let's go ahead and log into LangSmith here. So I'll go ahead and resize the [Windows](../../Glossary/Service/Windows.md), right? So I've gone ahead and logged into LangSmith. So what I'm going to do is invoke the agent with this particular prompt. Why does deep learning work? Is there some inherent property of a models architecture that makes it capable of learning? Or is it more about the data that is trained on any theorems or hypotheses that you can find that support this? Is there something special about how humans generate data? What are the philosophical implications? You can see here like unpacking the prompt in with a lot of questions and a lot of things that the model is going to go search for. So kind of artificially long prompt just to kind of demonstrate what will happen when we have that and how we're going to rectify and remedy that. We'll go ahead, start the agent executor and we'll refresh this. We'll see here that we've got our run happening here, right? And you can see here it's choosing
>
> **[3:05](https://www.linkedin.com/learning/prompt-engineering-with-langchain/managing-prompt-length-for-agents?u=76281980&t=185)** to use Wikipedia over Duck Duck Go search. And we get an error here. So we can go ahead and actually see the error here. The model is maximum context length is that many [Tokens](../../Skills/Web%20Development/Tokens.md), but we sent that many tokens. Of course, we can look at that in LangChain here. You can see the error has how it errors out. So in this case, we capture the error there and we can see the intermediate kind of results. So find something via Duck Duck Go search here and it pulls this up. You could see that it's then going to Wikipedia so on and so forth. So typical things that you would expect to see with LangSmith gives you that visibility. Looks like you can look at even more detail by looking at some parallel runs and stuff that was hidden. And of course we see here the total number of tokens that went in and how much that run cost us, of course. So we're familiar with this in LangSmith of course. What we could do now is actually define a function that is going to help condense the prompt. So we've seen the situation here. We ran out of context window before we can get the final answer. So what we're going to do is build out some prompt handling logic. So essentially what this is doing, it's saying, okay, if our messages have too many tokens, we need to start dropping the earliest tokens, the earliest AI function message pairs, right?
>
> **[4:39](https://www.linkedin.com/learning/prompt-engineering-with-langchain/managing-prompt-length-for-agents?u=76281980&t=279)** And write all this logic to handle that aspect of it. Now we'll instantiate the agent and we're going to pipe it actually from prompt to this function. So we added this bit to our agent. We'll still bind the tools and we'll still parse in the usual way. And of course, instantiate the agent executor and invoke the agent. And we can of course look right here in a LangSmith to see that run, which is happening as we speak. This is the current time we're in the current run. This is the agents running here. And of course we can see everything that is happening under the hood. LangChain gives us that visibility and we can look at all the [Metadata](../../Skills/Web%20Development/Metadata.md) can add feedback here as well. And let's go ahead and see if we get a final answer here from the language model. Alright, and so you can see this time, same exact prompt. We just added some logic that kind of compresses and condenses the prompt and we get the agent to run successfully. So it says here, deep learning works due to a combination of factors related to the model's architecture and the data it's trained on. One important aspect is the universal approximation theorem, which states that the [Neural Networks](../../Skills/Artificial%20Intelligence%20(AI)/Neural%20Networks.md) with a sufficient number of parameters can approximate any continuous function. And then unfortunately it cuts off here as the answer. So just an illustrative example for
>
> **[6:12](https://www.linkedin.com/learning/prompt-engineering-with-langchain/managing-prompt-length-for-agents?u=76281980&t=372)** how you can manage prompt size in one way.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Tokens](../../Skills/Web%20Development/Tokens.md) (5), [LangChain](../../Skills/Artificial%20Intelligence%20(AI)/LangChain.md) (4), [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) (3), [Windows](../../Glossary/Service/Windows.md) (1), [Metadata](../../Skills/Web%20Development/Metadata.md) (1)
> **CLI Commands:** find (2)
> **Env Vars:** llm (1), gpt (1)
> **Versions:** 3.5 (1)
> **Exercise Files:** template (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)


### 8. The LLM Landscape

[↑ Back to Table of Contents](#table-of-contents)

#### [Applications of language models](https://www.linkedin.com/learning/prompt-engineering-with-langchain/applications-of-language-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/applications-of-language-models?u=76281980&t=0)** - [Instructor] [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) are revolutionizing various industries by powering smart virtual assistants, analyzing customer sentiment, and more. Let's discuss some of the use cases and applications of large language models. LLM chatbots are being used to personalize customer interactions, to engage in natural language conversations, understand customer queries, and provide relevant responses. LLM-driven virtual assistants for automated [Customer Support](../../Skills/IT%20Help%20Desk/Customer%20Support.md) handle common customer inquiries, guide users, and offer [Real-Time](../../Skills/Database%20Management/Real-Time.md) support. They also analyze customer feedback to determine sentiment towards [products](../../Skills/Software%20Development/Microsoft%20Products.md) or services, and they're even being used to communicate in multiple languages, breaking down language barriers, and providing a global reach for businesses. In social media, LLMs are being used to automate article writing. They're assisting journalists in generating drafts and brainstorming ideas. They're also being used for blog and social media post creation. They're helping creators generate engaging blog and social media content. They're also generating product descriptions to assist ecommerce platforms in creating compelling product descriptions. There's also a potential risk for LLM-generated content that's similar to existing online content, which would lead to unintentional plagiarism. Within ecommerce, LLMs are powering real-time translation tools, which are instantaneously translating text or speech
>
> **[1:34](https://www.linkedin.com/learning/prompt-engineering-with-langchain/applications-of-language-models?u=76281980&t=94)** from one language to another. They're also being used to automatically translate documents for global businesses. Language models are also being used to help translate user interfaces and other textual elements for international markets. However, while LLMs promise efficiency, there's a need to ensure the authenticity and human touch in interactions. Within finance, language models are being used for fraud detection and prevention because they can analyze [Financial Data](../../Skills/Software%20Development/Financial%20Data.md) and detect and prevent fraud. LLMs are also being used to analyze financial news and market data for investment [Decision-Making](../../Skills/Data%20Science/Decision-Making.md). They're also being used to analyze customer data for loan underwriting and credit risk assessment. But as with anything, the challenge lies in ensuring [Data Security](../../Skills/Data%20Science/Data%20Security.md) and accurate interpretation of complex financial data. Marketing and advertising was one of the biggest first wins for language models, because they're used for content categorization for personalized recommendations. They're also being used to play a role in targeted advertising and marketing campaign. LLMs are also changing the search engine game completely, because now, they're being used to enhance search engine results by better understanding user queries. However, there is a potential risk of LLMs generating content that misrepresents a brand's voice or could be perceived as misleading. Within cyber law, LLMs are being used
>
> **[3:08](https://www.linkedin.com/learning/prompt-engineering-with-langchain/applications-of-language-models?u=76281980&t=188)** to analyze [Privacy](../../Skills/Data%20Science/Privacy.md) policies and ensure adherence to privacy laws. They're also being used to assist in legal research and case analysis within cyber law. LLMs also can analyze data related to security breaches and other cyber incidents. The challenge, of course, is ensuring accurate interpretation of the intricate legal language and maintaining data confidentiality. LLMs are also being used increasingly in healthcare to support healthcare professionals with medical diagnoses, to assist healthcare professionals in literature review, and research analysis within biomedicine. They're also being used to analyze patient data to support personalized treatment recommendations. However, misdiagnosis or incorrect treatment suggestions by LLMs can have serious consequences. Ensuring these models are thoroughly tested and validated for medical applications is critical. We've touched on just some of the potential applications of large language models. I hope you're seeing that these models are ushering in a new era of innovation across wide variety of sectors. From healthcare, where they're supporting medical diagnosis, to finance, where they aid in fraud prevention, the language models are transforming decision-making and boosting efficiency. However, it's vital to exercise caution and diligence when employing LLMs, particularly in sensitive areas, like healthcare and law. This is to mitigate risks and ensure
>
> **[4:42](https://www.linkedin.com/learning/prompt-engineering-with-langchain/applications-of-language-models?u=76281980&t=282)** that we have the highest quality outcomes. Looking ahead, the potential for large language model-driven innovation is immense. The future promises exciting opportunities for these models to revolutionize further how we work, learn, and communicate.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) (3), [Real-Time](../../Skills/Database%20Management/Real-Time.md) (2), [Financial Data](../../Skills/Software%20Development/Financial%20Data.md) (2), [Decision-Making](../../Skills/Data%20Science/Decision-Making.md) (2), [Privacy](../../Skills/Data%20Science/Privacy.md) (2)
> **Env Vars:** llm (3)
> **Definitions:** is a  (1)
> **Analogies:** similar to (1)
> **Warnings:** caution (1)
> **Speakers:** - [instructor] (1)

#### [The LLM landscape](https://www.linkedin.com/learning/prompt-engineering-with-langchain/the-llm-landscape?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/prompt-engineering-with-langchain/the-llm-landscape?u=76281980&t=0)** - [Instructor] Since the Transformer architecture was introduced in 2017 and the BERT model was introduced in 2019, both by [Google](../../Glossary/Service/Google.md), LLMs have revolutionized [natural language processing](../../Skills/Artificial%20Intelligence%20(AI)/Natural%20Language%20Processing%20(NLP).md), content generation and beyond. The last few years have seen a surge in the development and deployment of LLMs. As these models grow in capabilities, complexities, and parameter count, a pivotal question emerges for businesses and developers. Should you lean towards open-source models or opt for the closed source alternatives? Open-source models have democratized access to state-of-the-art AI capabilities. Open-source LLMs have publicly accessible source codes which anyone can use, modify, and distribute. This means developers, researchers, and enthusiasts can work together to develop, improve, and customize these models. These open-source models encourage innovation, knowledge sharing, and collective development efforts. Models like [LLaMA](../../Skills/Artificial%20Intelligence%20(AI)/LLaMA.md) 2, Alpaca, and Falcon have carved niches for themselves, excelling in dialogue situations, instruction following tasks, and text generation respectively. Llama and Llama 2 were developed by Meta AI. These models are designed for dialogue situations and are trained on a vast corpus of publicly available data. The Falcon series of models was developed by the Technology Innovation Institute. Falcon models are optimized for text generation
>
> **[1:35](https://www.linkedin.com/learning/prompt-engineering-with-langchain/the-llm-landscape?u=76281980&t=95)** and chatbot functionality. Deci AI produces models that [excel](../../Skills/Data%20Science/Microsoft%20Excel.md) at fast inference while maintaining quality, resulting in efficiency and cost savings. There's a number of startups out there that are pumping out open-source models. For example, Mistral and Stability, and there's far too many to name, but these models are hitting the open-source scene and providing developers with a wide range of alternatives. The open-source ecosystem offers a number of benefits to developers. One is that developers can tailor models to their specific needs from full fine-tuning to [Prompt Engineering](../../Skills/Artificial%20Intelligence%20(AI)/Prompt%20Engineering.md). While the initial setup might demand more resources, the long-term benefits, especially at scale, are undeniable. With the ability to self-host businesses can ensure [Data Privacy](../../Skills/Data%20Science/Data%20Privacy.md), which is paramount concern in today's digital landscape. Closed-source LLMs is the land of proprietary titans. These closed-source LLMs are developed and maintained by organizations or companies that do not make their source code publicly available. As a result, the specific details of their architecture, training data, and [Algorithms](../../Skills/Software%20Development/Algorithms.md) are usually not accessible to the public. These models are often created as commercial [products](../../Skills/Software%20Development/Microsoft%20Products.md) that might require licenses or subscriptions to use. A few models to watch in the closed-source space
>
> **[3:08](https://www.linkedin.com/learning/prompt-engineering-with-langchain/the-llm-landscape?u=76281980&t=188)** include OpenAI's GPT series, GPT-3.5, [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md), GPT-4 turbo have set new benchmarks in various tasks, excelling in text completion, translation, and question answering. Anthropic has a model called Claude, which is a cutting edge model that stands out in research, offering capabilities like editing, rewriting, summarizing, and more. Cohere is another company that has models like Command XL and Command Medium, which excel at interpreting instruction like prompts and making them ideal for chatbots. There are several other closed-source models and companies that are making waves. For example, models like Jurassic-2 or Google's PaLm, or Writer's Palmyra. Closed-source models have their pros and cons. They're accessible via API, and this brings along with it its own set of advantages. One is that it's easy to use. It requires minimal expertise, so businesses can integrate these models into their applications easily. These APIs offer a [swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) route for companies looking to deploy AI powered features quickly being proprietary. These models often receive regular updates, which ensure that they remain at the forefront of the AI landscape. However, they come with limitations and customization, potential data privacy concerns, and costs that can escalate at scale. Here's some things to take into consideration
>
> **[4:41](https://www.linkedin.com/learning/prompt-engineering-with-langchain/the-llm-landscape?u=76281980&t=281)** when making the choice between open-source and closed-source models. First is the expertise of your team. Does your team possess deep machine learning or NLP knowledge, or are you just looking for a plug and play solution? While closed-source models might seem economical, initially, open-source model can offer cost benefits in the long run. If your application demands specific customizations, open-source models provide the flexibility that you need. For applications that are handling sensitive data, open-source models with their self-hosting capabilities offer an edge. The LLM landscape is dynamic with constant innovations, reshaping the train. As businesses staying informed, evaluating the pros and cons and making choices that align with long-term strategies are imperative. Whether you lean towards the open-source vanguard or those proprietary titans, one thing is clear. [Large language models](../../Skills/Artificial%20Intelligence%20(AI)/Large%20Language%20Models%20(LLM).md) are here to stay and they promise to redefine the future of AI powered applications.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LLaMA](../../Skills/Artificial%20Intelligence%20(AI)/LLaMA.md) (3), [Google](../../Glossary/Service/Google.md) (2), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (2), [Data Privacy](../../Skills/Data%20Science/Data%20Privacy.md) (2), [GPT-4](../../Skills/Artificial%20Intelligence%20(AI)/GPT-4.md) (2)
> **Env Vars:** gpt (4), bert (1), api (1), nlp (1), llm (1)
> **Analogies:** for example (2)
> **CLI Commands:** make (1)
> **Versions:** 3.5 (1)
> **Exercise Files:** source code (1)
> **Definitions:** is a  (1)
> **Prerequisites:** setup (1)


## Instructor

- [Harpreet Sahota](../../Instructors/Artificial%20Intelligence%20(AI)/Harpreet%20Sahota.md)

## Resources

- Exercise files available

## Skills Covered

- LangChain
- Artificial Intelligence (AI)
- Prompt Engineering

## Path Context

### In [Prompt Engineering Skills for Developers](../../Paths/Artificial%20Intelligence%20(AI)/Learning%20Paths/Prompt%20Engineering%20Skills%20for%20Developers.md)
← [ChatGPT- Perfecting Your Output with Templates in Prompts](ChatGPT-%20Perfecting%20Your%20Output%20with%20Templates%20in%20Prompts.md) | **5 of 6** | [Building Advanced AI Apps with Prompt Flow](Building%20Advanced%20AI%20Apps%20with%20Prompt%20Flow.md) →

## Appears In

- [Prompt Engineering Skills for Developers](../../Paths/Artificial%20Intelligence%20(AI)/Learning%20Paths/Prompt%20Engineering%20Skills%20for%20Developers.md)

## Related Courses

_Courses sharing skills:_

- [AI Agents Made Simple- Your Zero-to-Hero Guide to Prompting for Work Automation](AI%20Agents%20Made%20Simple-%20Your%20Zero-to-Hero%20Guide%20to%20Prompting%20for%20Work%20Automation.md) — Artificial Intelligence (AI), Prompt Engineering
- [Prompt Engineering and AI Agents with ChatGPT](Prompt%20Engineering%20and%20AI%20Agents%20with%20ChatGPT.md) — Artificial Intelligence (AI), Prompt Engineering
- [Generative AI for Data Engineering- ChatGPT Power Tips](Generative%20AI%20for%20Data%20Engineering-%20ChatGPT%20Power%20Tips.md) — Artificial Intelligence (AI), Prompt Engineering
- [Prompt Engineering With Gemini](Prompt%20Engineering%20With%20Gemini.md) — Artificial Intelligence (AI), Prompt Engineering
- [Multimodal Prompting with Google's Project Gemini](Multimodal%20Prompting%20with%20Google's%20Project%20Gemini.md) — Artificial Intelligence (AI), Prompt Engineering

---

[↑ Back to top](#)