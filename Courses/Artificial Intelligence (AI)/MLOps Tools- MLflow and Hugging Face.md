---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: mlops-tools-mlflow-and-hugging-face
url: "https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face"
duration_minutes: 314
duration: 5h 14m
level: Intermediate
updated: 9/25/2024
learners: 3045
skills:
  - MLflow
  - Machine Learning
  - MLOps
  - Artificial Intelligence (AI)
  - Hugging Face Products
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D4E0DAQETfg1FS6Ddug/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1727129495332?e=2147483647&amp;v=beta&amp;t=N0mHXgJiGkQgJF_WSRhi9Lxw7EpFW-1onQtKPrdk9pQ"
linkedin_topic: Artificial Intelligence (AI)
learning_paths:
  - '[[MLOps Essentials for Developers and AI Engineers- Tools, Pipelines, Security]]'
prev_courses:
  - '[[Generative AI and Predictive AI in the Cloud- Foundational Concepts and Scenarios]]'
next_courses:
  - '[[Data Preparation, Feature Engineering, and Augmentation for AI Models]]'
path_nav: '[{"path":"MLOps Essentials for Developers and AI Engineers- Tools, Pipelines, Security","position":4,"total":10,"prev":"Generative AI and Predictive AI in the Cloud- Foundational Concepts and Scenarios","next":"Data Preparation, Feature Engineering, and Augmentation for AI Models"}]'
path_count: 1
tags:
  - course
  - topic/artificial-intelligence
  - topic/data-science
  - skill/mlflow
  - skill/machine-learning
  - skill/mlops
  - skill/artificial-intelligence-ai
  - skill/hugging-face-products
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Artificial%20Intelligence%20(AI)/MLOps%20Tools-%20MLflow%20and%20Hugging%20Face.md)

![MLOps Tools: MLflow and Hugging Face](https://media.licdn.com/dms/image/v2/D4E0DAQETfg1FS6Ddug/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1727129495332?e=2147483647&amp;v=beta&amp;t=N0mHXgJiGkQgJF_WSRhi9Lxw7EpFW-1onQtKPrdk9pQ)

# MLOps Tools: MLflow and Hugging Face

> In this course, learn how to master MLflow and Hugging Face, two powerful open-source platforms for MLOps. Starting with MLflow, learn how to streamline the machine learning lifecycle, manage projects and models, use the tracking UI system, and interact with registered models. Then, get an introduction to Hugging Face, starting with an overview of the Hugging Face Hub, repositories, and Hugging Fa

> [LinkedIn Learning](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face) | 5h 14m | Intermediate | 3K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (1 videos)
  - [MLOps platforms introduction](#mlops-platforms-introduction)
- [**1. Introduction to MLflow**](#1-introduction-to-mlflow) (4 videos)
  - [Overview of MLflow](#overview-of-mlflow)
  - [Installing and using MLflow](#installing-and-using-mlflow)
  - [Introduction to the tracking UI](#introduction-to-the-tracking-ui)
  - [Parameters, version, artifacts, and metrics](#parameters-version-artifacts-and-metrics)
- [**2. MLflow Projects**](#2-mlflow-projects) (4 videos)
  - [Working with MLflow projects](#working-with-mlflow-projects)
  - [Create an MLflow project](#create-an-mlflow-project)
  - [Run projects from remote Git repositories](#run-projects-from-remote-git-repositories)
  - [Connecting MLflow to Databricks](#connecting-mlflow-to-databricks)
- [**3. MLflow Models**](#3-mlflow-models) (4 videos)
  - [Components of the MLflow package](#components-of-the-mlflow-package)
  - [Use a registry with an MLflow model](#use-a-registry-with-an-mlflow-model)
  - [Referencing artifacts with the API](#referencing-artifacts-with-the-api)
  - [Saving and serving MLflow models](#saving-and-serving-mlflow-models)
- [**4. Introduction to Hugging Face**](#4-introduction-to-hugging-face) (5 videos)
  - [What is Hugging Face?](#what-is-hugging-face)
  - [Overview of the Hugging Face Hub](#overview-of-the-hugging-face-hub)
  - [Introduction to the Hugging Face Hub](#introduction-to-the-hugging-face-hub)
  - [Using Hugging Face repositories](#using-hugging-face-repositories)
  - [Using Hugging Face Spaces](#using-hugging-face-spaces)
- [**5. Introduction to Applied Hugging Face**](#5-introduction-to-applied-hugging-face) (3 videos)
  - [Introduction to applied Hugging Face](#introduction-to-applied-hugging-face)
  - [Using GPU-enabled Codespaces](#using-gpu-enabled-codespaces)
  - [Using the Hugging Face CLI](#using-the-hugging-face-cli)
- [**6. Using Hugging Face**](#6-using-hugging-face) (6 videos)
  - [Using the Model Hub](#using-the-model-hub)
  - [Downloading models](#downloading-models)
  - [Working with models](#working-with-models)
  - [Adding datasets](#adding-datasets)
  - [Using datasets](#using-datasets)
  - [Working with datasets](#working-with-datasets)
- [**7. Packaging Hugging Face**](#7-packaging-hugging-face) (4 videos)
  - [Hugging Face and FastAPI](#hugging-face-and-fastapi)
  - [Containerizing Hugging Face](#containerizing-hugging-face)
  - [Running FastAPI with Hugging Face](#running-fastapi-with-hugging-face)
  - [CI/CD packaging with GitHub Actions](#cicd-packaging-with-github-actions)
- [**8. Hugging Face and Azure ML Studio**](#8-hugging-face-and-azure-ml-studio) (5 videos)
  - [Hugging Face and Azure ML Studio](#hugging-face-and-azure-ml-studio)
  - [Registering a Hugging Face dataset on Azure](#registering-a-hugging-face-dataset-on-azure)
  - [Registering a Hugging Face model on Azure](#registering-a-hugging-face-model-on-azure)
  - [Inspecting a Hugging Face dataset on Azure](#inspecting-a-hugging-face-dataset-on-azure)
  - [Azure ML Python SDK](#azure-ml-python-sdk)
- [**9. Hugging Face Automation**](#9-hugging-face-automation) (4 videos)
  - [Using GitHub Actions for model deployments](#using-github-actions-for-model-deployments)
  - [Using Azure Container Registry](#using-azure-container-registry)
  - [Automating packaging with Azure Container Registry](#automating-packaging-with-azure-container-registry)
  - [Automating packaging Docker Hub](#automating-packaging-docker-hub)
- [**10. Hugging Face with Azure Containers**](#10-hugging-face-with-azure-containers) (4 videos)
  - [Create an Azure container application](#create-an-azure-container-application)
  - [Configure an Azure container application](#configure-an-azure-container-application)
  - [Deploy Hugging Face to Azure](#deploy-hugging-face-to-azure)
  - [Troubleshooting container deployment](#troubleshooting-container-deployment)
- [**11. Fine-Tuning and ONNX Exporting**](#11-fine-tuning-and-onnx-exporting) (4 videos)
  - [Introduction to fine-tuning theory](#introduction-to-fine-tuning-theory)
  - [Performing fine-tuning](#performing-fine-tuning)
  - [Introduction to ONNX and Hugging Face](#introduction-to-onnx-and-hugging-face)
  - [Exporting Hugging Face models to ONNX](#exporting-hugging-face-models-to-onnx)
- [**12. Hugging Face Spaces**](#12-hugging-face-spaces) (3 videos)
  - [Introduction to Hugging Face Spaces](#introduction-to-hugging-face-spaces)
  - [Hugging Face Spaces walkthrough](#hugging-face-spaces-walkthrough)
  - [Deploying to Hugging Face Spaces](#deploying-to-hugging-face-spaces)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [MLOps platforms introduction](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/mlops-platforms-intro?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/mlops-platforms-intro?u=76281980&t=0)** - Welcome to this course where we will cover [[MLOps]] platforms. So specifically some of the open source MLOps platforms. And we'll start with ML Flow, which is a powerful platform that allows you to deploy your models and capture all of the metrics, all of the things that happens while you're building, while you're training your models, and then deploying them. It gives you a normalized way of deployment and a reproducible environment. We'll see everything that you have to do in order to have these reproducible environments, regardless of the cloud provider, [[Microsoft Azure|Azure]] or GCP or even AWS. It'll essentially work anywhere. That's a powerful thing right there that you might be interested in exploring. Then we'll move on to [[Hugging Face]], although Hugging Face, the actual platform, the services, the Hugging Face provides are not open source, their libraries are. We'll look at Transformers, we'll look at data sets, and we'll see how to interact with that to get off-the-shelf models that are pre-trained and we'll interact with them. We'll see how to deploy them, how to package them, how to push them to other cloud providers like Azure, and use different container registries. And finally, we'll see a little bit of Onyx as well, which is a different way, a different format that you can export and import to and from, these Onyx format, which allows you to produce a model in a different framework,
>
> **[1:35](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/mlops-platforms-intro?u=76281980&t=95)** like say [[TensorFlow]], and then get it onto Onyx and put it into other devices that might not have everything you need for, say, TensorFlow. So it gives you another way to normalization, which is a powerful concept in machine learning operations as well as in machine learning in general. So we'll additionally cover some advanced Hugging Face applications and things like [[Fine Tuning]] and even using GPU, like using off-the-shelf models and fine tuning them, making some slight changes and adapting that to a new purpose. And all of that will allow you to interact with all of these and gain some powerful knowledge to have normalized deployments, interact with powerful models that are existing today, produced by very important companies, and give you flexibility to deployment anywhere. And that deployment can happen with Onyx or other registries or other cloud providers. In essence, this course will allow you to do many different things related to machine learning operations and I'm excited that you're here. So let's go through all of those and hopefully by the end of it, you feel confident enough to interact with all of these technologies and start applying them in your day to day with machine learning.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (4), [[MLOps]] (2), [[Microsoft Azure|Azure]] (2), [[TensorFlow]] (2), [[Fine Tuning]] (2)
> **Env Vars:** gcp (1), aws (1), gpu (1)
> **Definitions:** is a  (3)
> **CLI Commands:** aws (1)
> **Speakers:** - welcome (1)


### 1. Introduction to MLflow

[↑ Back to Table of Contents](#table-of-contents)

#### [Overview of MLflow](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/overview-of-mlflow?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/overview-of-mlflow?u=76281980&t=0)** - [Instructor] Tracking is one of those components that [[MLflow]] is able to do. So here we have a little bit of like a quick overview of what that tracking looks like. So you have abilities to do perform certain things that are actually prefixed with log and we'll see those in detail later. But you can log parameters. You can see here that the parameters are basically key value pairs and those key value pairs can be anything. So you can see here regularization and 0.1. It can be anything. And if you are keeping some sort of consistency in your product when you're training machine learning models or doing any kind of changes, you can keep track of those while the run is actually moving around and making progress. So aside from parameters, which are key value pairs to log absolutely anything, you can log metrics, metrics can also be anything as well. And here you can see accuracy 0.1. So you're actually capturing certain things that are making sense for what you're trying to train. So the difference between metrics and parameters is that parameters can be, are key value pairs, but you can see those as when you're going to be training and you're going to be passing some values around and you want to capture those. And finally, you're going to be logging artifacts. Now logging artifacts means basically not that,
>
> **[1:35](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/overview-of-mlflow?u=76281980&t=95)** you know, initially you can think about this as like, oh, well boy, it's going to report something as a log. But no, it's actually going to capture. So say for example, model pkl, which is stands for pickle, and it's going to push that binary to a remote server. Next, I want to show you, well, this is kind of like the UI, which we'll see like a newer version of the UI later on on this course. But I want to scroll here to show you the project. So a little bit of the high overview of the projects. Again, these are three components we just saw tracking. Now we're going to look at Projects. And finally we'll take a look at Models. So Projects is a way of normalizing with configuration, plain text configuration like this one that allows you to keep track of everything that you're doing while you're going to perform some actions in your machine learning project. So you can name is one of the most basic things that you can do, here, these files using a Conda environment and pointing to a conda yaml file. Again, we'll see in detail what that means and what are some of the options that you can do there. But essentially what this means is that it's configuration in a text-based file or text-based framework, rather, that you can keep track when you're doing your machinery operations and you can put them in [[Version Control]]. That means that if I come in tomorrow and change these [[Python (Programming Language)|python]] valid data pipe
>
> **[3:09](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/overview-of-mlflow?u=76281980&t=189)** to do something different, to use some other file and I make a mistake, well I can always come back and make sure that this doesn't have a typo. So those are kind of like the things that version control will give you the ability of doing. So I'm going to scroll all the way down to models now, so that now you can actually see what that means. Essentially, you're tracking a variety of different ways of creating a model, of producing a model. And here you will see that what Python functions here, instructing MLflow how it should load this model, how it should be working with what versions of the machine learning libraries are going to be using. So in this case, sklearn is going to be version 19, zero dot 19 dot one and it's going to produce this model pkl. So that's it, that's a very high overview of MLflow and some of the components that we'll see in detail later.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MLflow]] (3), [[Version Control]] (2), [[Python (Programming Language)|Python]] (2)
> **CLI Commands:** python (2), make (2)
> **Versions:** 0.1 (2), version 19 (1)
> **Definitions:** stands for (1), is a  (1), means that (1)
> **Analogies:** kind of like (2), for example (1)
> **Speakers:** - [instructor] (1)

#### [Installing and using MLflow](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/installing-and-using-mlflow?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/installing-and-using-mlflow?u=76281980&t=0)** - [Instructor] Installing [[MLflow]] is very straightforward if you have a [[Python (Programming Language)|Python]] environment, I have something that I've already activated here. It doesn't matter if it's a virtual environment, or a conda environment, or something else. In this case, it just happens to be a conda environment. But we're going to go ahead and install MLflow. So I'm going to say pip install mlflow, and that should work pretty straightforward. It should take a couple of seconds, and then complete. That is effectively how you install MLflow. Just using Pip, if you have some other ways of installing, well, primarily Pip would be the one thing that you want to be using. So, once MLflow completes installing, there's a few things that you can actually do. Before I show you kind of how to interact with MLflow directly, I want to show you what MLflow UI will look like. So I'm going to say mlflow, space, ui, and return. And that will run a server. Now, this UI is running locally, so you can see here it's running a port of 5,000 so I'm going to click here. And it's opening up on our window which I'm going to pull into the view here. So you can see that it has some minimal interface with a few things that look interesting. However, there's nothing here that you could say, well, I can't see anything that would be related to models except for you have like a chart here.
>
> **[1:36](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/installing-and-using-mlflow?u=76281980&t=96)** But the thing is that this is empty so not entirely super useful. But you will see once we start putting things in there, then things will start looking much, much interesting. So let's switch the view back to VS Code. And we're going to create here a new file, and we're going to call it... This is going to be test-mlflow. So it's a very simple script, it's just a Python script. I'm going to leave the terminal there running because it's our UI. So we're going to say, for example, from mlflow import log_metric, and log_param, and log_artifact. This is something that we'll see later into more detail. But basically we'll do the infamous if_name is main that basically tells Python, hey, run this, or straight away, like if I run it on the terminal, execute this as a script. So we can start logging some params. So for example, we can say, let's say threshold here, and we can say something like 3. So that's a parameter. Or we could say verbosity, can't type verbosity. We can say, let's say for example, DEBUG. You're logging parameters, you're logging things that you are requiring for your training to effectively work, and then be trained, and it could be anything that you want. Similarly, we can log some metrics.
>
> **[3:09](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/installing-and-using-mlflow?u=76281980&t=189)** So if you're capturing, say, for example, timestamp, we could say, this would be any arbitrary number. And we could say, time to complete, let's call it TTC for abbreviation purposes, and let's say this took 33 seconds. So you start seeing that you can start logging other kinds of things. And finally, we can actually log an artifact which can be an actual file. So we can say, this file could exist already, but we can say, for example, well, we don't need to write it with Python. We can go here and say, new file. And we can say, this is our produced-dataset.csv. So we can say this is empty, and doesn't have anything there, we're going to save it. I'm going to close it out. And then when we want to log an artifact, we are going to say log_artifact. And then we are going to say that the artifact is going to be produced-dataset.csv. All right, so I'm going to save these, and then I'm going to run a new terminal. And I am going to say python test-mlflow, and see what happens. Alright, so that completed,
>
> **[4:41](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/installing-and-using-mlflow?u=76281980&t=281)** and I'm going to go back to the web UI, and see what happened. So I'm going to refresh, and we should start seeing that nine seconds ago, I was able to run some things. So this is a little bit big so let me try to make it slightly more readable there. It's going to be a little bit smaller. But you can see here that it's capturing things. So metrics, which we talked about time to complete. Now I call the TTC, and you could see that that's captured here and timestamp. So you have metrics separated there, and we have parameters, we said threshold was going to be 3. And verbosity was going to be DEBUG. So you start seeing those things there, and then the source is right there. Let's click here and see what happens. How about the artifacts? Well, this is the nice thing is that we can click here, and it tells me lots of [[Metadata]] about the parameter. So that's a very, very quick overview on how to install MLflow, and how to start using it with the tracking UI.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MLflow]] (12), [[Python (Programming Language)|Python]] (5), [[Metadata]] (1)
> **CLI Commands:** python (5), pip (3), make (1)
> **Code Identifiers:** log_artifact (2), log_metric (1), log_param (1), if_name (1)
> **Analogies:** for example (5)
> **Env Vars:** debug (2), ttc (2)
> **Tools:** terminal (3), vs code (1)
> **Prerequisites:** install (4)
> **File Paths:** produced-dataset.csv (2)

#### [Introduction to the tracking UI](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-tracking-ui?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-tracking-ui?u=76281980&t=0)** - [Instructor] Let's do a little bit of more digging into the tracking server and the UI. So, one of the things that we're doing here is that we're going to log some metrics. Specifically, we're going to be producing the metrics. I'm not going to run, actually, but I'm going to simulate having certain metric names like CPU, RAM and disk. And I'm going to, at random, capture one of those, and I'm going to select any percentage from zero to 100. So, for 40 iterations I'm going to log a metric, either CPU, RAM or disk. And for any number between zero and 100, I'm going to run that. I'm going to let that [[MLflow]] capture that in the UI, in the tracking server, capturing metrics. But let's go back to how that tracking server, or a tracking UI, looks like. So I'm going to go here. This is running from the terminal. If you remember, I was running that with MLflow Space UI, and we run this before, so I have a little bit of parameters already captured that you saw the threshold, and debug, and metrics. But now I want to create a new experiment. So, before I run the metrics, I want to capture those metrics in a new experiment. So, how do you go ahead and do that? Well, you need to run a new... You can either let MLflow create a new ID for you,
>
> **[1:35](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-tracking-ui?u=76281980&t=95)** or you go ahead and create one. So let's go ahead and create one. To create one, I'm going to go back to my editor. I'm going to open up a new terminal. And I'm going to run MLflow, and I'm going to run a specific command. So I'm going to say "mlflow experiments"... And I'm going to say "create" and... Create with --experiment-name. And that name is going to be produce-metrics. So I'm going to create that. And I'm going to get an ID. So now with that ID, you can actually instruct MLflow, anything, really, that runs MLflow, that it should track this new experiment. So you can always also check back the UI. So if I go here and I hit Refresh, you can see that produce-metrics is right here. There's nothing there, nothing has been run. And I need to go ahead and start telling MLflow to capture stuff in there. So I'm going to go back to the terminal. And now the way I can do this... Well, outside of running the script, or outside of running the process, I can actually export an environment variable. I'm going to call it MLFLOW. Well, I'm not going to call it, but it's what it accepts, which is EXPERIMENT. And it's going to be _ID. I'm going to say this is 2. I'm going to run [[Python (Programming Language)|python]] produce-metrics.py. So that's going to produce a lot of metrics.
>
> **[3:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-tracking-ui?u=76281980&t=188)** And I'm going to go back to the UI and I'm going to refresh. And you can see that a few seconds ago I was able to produce that, and it captures some metrics, CPU, disk, and RAM. So again, these numbers, these percentages, are made up out of a random choice. But the important thing is that it'll allow you to drill down and do some operations here that I want to show you. So I'm going to click. I'm going to click here on the Start (indistinct). Start Time, sorry. You can see that the metrics that were captured are three. Now, why there were three? Well, because those were the only three options, even though that I was able to log them for about 30 times each. So let's take a look at CPU. So I'm going to click CPU, and you can see that for every single one time that I created that you could see that I was able to log 52 here, then 78, then 45.. What else? 10, 62. So that's kind of like trying to capture... Well, not trying, but effectively capturing every single iteration of every single metric that I'm pushing forward to. And down here I'm simulating from zero to 100. And you can see here that nothing went beyond 80. Let's go back and take a look at some of the other metrics. So we had disk. So let's take a look at disk. Oh, here we capture a little bit more. So we had the 69, 92, 44. So that's pretty good.
>
> **[4:40](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-tracking-ui?u=76281980&t=280)** So it could give you, like, a very good, you know, operation here, where you can query, you can query these, you can even change the smoothness, how smooth you want this to be. And all kinds of different things that you can have in the UI. Now, I'm running all of these locally, but you could try to set this to a remote server. I can actually show you that if I go back to the terminal and I do ls, you can see that there's an ML runs. And if I do three ML runs, there are lots of different experiments running. So you can see that the ID 1 has the metrics, the params, the tags. ID 2, which is kind of like what we just run right now, has several artifacts and metrics and params. So it gives you structure, and that's really good. Now, I want to go ahead and try to look at produce-metrics here. And then I'm going to... Instead of doing, for i in range(40), I'm going to make these more thorough. So I'm going to make it 400 times. And instead of CPU, RAM, and disk, I'm just going to change these to... I'm going to change it to just CPU and see. Well, effectively that means that the log_metric is always going to be CPU. So I want to show you this and how that looks, and that works.
>
> **[6:14](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-tracking-ui?u=76281980&t=374)** And I'm going to... For 400 times, 400 durations, I'm going to select a number from zero to 100 and see how that works. All right, so that means that I am going to run these Python produce-metrics for the same MLflow experiments. So ID 2. So I'm going to run that, it's going to run for 400 times. And now I'm going to go back to the UI. So here I'm going to go, I'm going to go back. Go back again. Produce-metrics. Now there's a new run that came in, and the CPU was about 60. So let's take a look at that, open up metrics, open up the CPU graph. And now look at that, a more realistic graph, right? Because we have 400 points. So you might be running a loop over your experiment training or machine learning model training, and you want to capture all of these metrics or you're going to get something that looks kind of like this, that looks really, really nice and gives you a really good overview. So those are some of the features. And lastly, one of the things I wanted to show you was if you come back to produce-metrics, you can query the metrics, and not only the metrics, but in this case we could say metrics, say CPU, more than, say 88. And well, there's nothing. We can say, let's say for example, more than... Or 14, and run. And only the one that I run just a minute ago was able to produce CPU more than 14. The previous one didn't.
>
> **[7:48](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-tracking-ui?u=76281980&t=468)** So if I want to say, you know... I'm basically querying, almost like a [[SQL]] query. There's nothing there. You can see that I can do all kinds of different queries. So imagine having several hundred experiments for produce-metrics, or whatever you want to call these, experiment name, and then query that. It is a very powerful way. And you can also filter with All Runs, or selected runs. You could say model with versions and without versions. Again, very, very useful. And this is not only... I only show metrics here, but you could do the same querying and the same type of filtering for parameters and artifacts as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MLflow]] (10), [[Python (Programming Language)|Python]] (2), [[SQL]] (1)
> **Env Vars:** cpu (12), ram (4), mlflow (1), experiment (1), sql (1)
> **Cross-References:** go back to (6)
> **CLI Commands:** python (2), make (2), ls (1)
> **Analogies:** kind of like (3), for example (1), imagine (1)
> **Tools:** terminal (4)
> **Definitions:** means that (1), is a  (1)
> **File Paths:** produce-metrics.py (1)

#### [Parameters, version, artifacts, and metrics](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/parameters-version-artifacts-and-metrics?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/parameters-version-artifacts-and-metrics?u=76281980&t=0)** - [Instructor] Let's go deeper into parameters, versions, artifacts, and metrics. So we've already seen how we can produce tons of metrics and log them to the UI, and we're going to, I'm going to try to run this databricks/[[MLflow]] example, which happens to be in the documentation. I'm going to run this and then I'll show you kind of, like, what the files look like. So I'm going to execute this, it's going to [[Fetch]] the project, and it's getting into an error. All right, so let's see what's going on here. So one of the things that mlflow is kind of, like, strict about is that the changing parameter values is not allowed. So you can see here that the initial value was five and then kind of, like, tried to log it with a 5.0. So not good. And it actually, the exception message is pretty useful here because you can see that the parameter's being logged kind of, like, this is not okay, right? Because you're saying, hey, this is a parameter of depth three and then parameter of the five. So why is this not allowed? I think it's very helpful because what's happening here is you're logging parameters on how you would run things, say, for example, in the command line interface. So if you're running a command and passing --debug or log level rather equals warning, you're not going to probably change that warning to say debug because you've already run that, right? So it doesn't make sense.
>
> **[1:33](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/parameters-version-artifacts-and-metrics?u=76281980&t=93)** So mlflow here will complain when that happens. In this case, you would be able to kind of, like, override some parameter if you actually absolutely 100% need to do that. Now, don't do that, I don't think. So, like, I think, like, the right way is to set your parameters right from the get go and then stick with those and then try to log them. So that's what happened. So let's take a look at what that example looks like. So I'm going to go to the mlflow example and I'm going to actually go here to, because I've already forked the example. And let's take a look at train.py. So train.py will use the famous wine quality CSV dataset and is going to try to train it. That's fine. And let's take a look at where this is logging. So we're passing that value of five, which is alpha right here. So you can see here that the alpha is float c*rv1 if it's more than one. Otherwise it's going to be 0.5. So that is changing somehow because right here it is complaining about it. But the thing is that we need to ensure that this is actually set correctly and not a problem.
>
> **[3:07](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/parameters-version-artifacts-and-metrics?u=76281980&t=187)** So line 44, it's right here. And so I've forked the example.py to prevent this problem. I'm going to go back and I am going to show you... Go back again. I'm going to show you kind of, like, the change that I did here to make this work. So I'm just forcing it to be an integer and not worry about it, all right? So with that, let's go and back to the editor and run it again and see what happens. So back to Visual Studio Code. And instead of running for, let me see, running for the Databricks example, we're going to run this one right here. And I'm going to say alpha five and that's not this one. So let's say, so this one with alpha, not alpha four, but alpha five. I'm going to run this and it's going to take a second. All right, so that completed and you can see some values are being set out. Now, if you remember, those values are being captured in the train.py that we saw. So we go back to the browser, you can see those values are right here and that's why those are getting printed. But, you know, printing stuff on the terminal, it's only useful if you're running it directly, not if you're logging these metrics. And mlflow is capturing all of that information for you,
>
> **[4:43](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/parameters-version-artifacts-and-metrics?u=76281980&t=283)** including logging the actual model. Now, in this case it's using log model. So it's using a helper from scikit-learn to say, hey, I'm going to produce a model for scikit-learn and I'm going to capture, it's being captured here as lr. So that's basically right here on elastic net is producing that and the calling to fit with the train_x and train_y is producing that model. And then we're kind of capturing there. All right, so that's what's going on. Let's take a look at what the UI says. So I'm going to refresh here and we have several runs now. We have one that failed. So let's take a look at that one that failed. So we can see that the command that ran was the one with Databricks, and that's good because that's the one that doesn't work because of the problem with logging. And you can see here that alpha was five. And this is interesting because it's actually capturing other defaults that are being used with that mlflow run. So that's interesting. Let's take a look at the parameters. The parameters again was alpha was five, so remember it was logged in as five and then it was kind of, like, wanted to change to 5.0, which is a float, and that's not okay. So the ratio is 0.1, that's fine. Some metrics, there are no metrics. Let's see if there are tags, no tags and no artifacts. Why there aren't any artifacts, because the run failed.
>
> **[6:18](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/parameters-version-artifacts-and-metrics?u=76281980&t=378)** So you can see here the status is failed. So let's go back and let's take a look. And the last one that we run, which is this one, so very nice, we have a model right here. Let's take a closer look. So alpha was five already. Parameters was able to log them fine. And then we see that there are three metrics now. We have MAER2 and RMSE. So if we click those, it's just a single value that never change. It was always 0.85. And if you remember and I go back to the text data here on Visual Studio Code, that is right here on RMSE. All right, so we run that with five, the alpha five, that's kind of, like, the default. But we can do a few more runs. Let's run it with three and we're changing values on the fly. And then let's change it to 2.5 or 2.4 rather. That actually created a problem because I'm changing a collision there, but, like, let's make an integer and not use a float. That will work. So, all right, so we did a few runs and I'm taking advantage that this machine is able to run this very fairly quickly. I'm going to go back here and take a look at all of the runs. So now they're being captured and you can see here there are several variations. So why is this important? Well, we're seeing now that this is fairly populated. We're actually training with scikit-learn
>
> **[7:53](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/parameters-version-artifacts-and-metrics?u=76281980&t=473)** and the parameters are changing a little bit. You can see the 2.4 failed. That's why the metrics didn't, were not captured, but we're also have the ability to look at some of the models. So you can see here that scikit-learn is actually getting captured. And I can look at that experiment and look at the artifact. So let's take a look at the artifact, look at everything that got captured. So we have the ML model, we have the con.yaml, we have the model PKL, which is the pickle file, and then we have all of these, you know, we can do the predictions. I mean, this is pretty remarkable because it's capturing absolutely everything that would be needed to run. So, say, for example, if you want to do that on the [[Pandas (Software)|pandas]] data frame, I mean, that's tremendous. So we can import mlflow, load the capture.log model from it tells you exactly where it's coming from, and load the model as a [[Python (Programming Language)|Python]] model and then do some predictions. So let's try it out and see if that works. So we're going to go to our editor and we're going to try to run, try to create an IPython notebook and let's say test IPython notebook. And we're going to paste that and run it and it's going to install some dependencies. I'm going to close this and it's going to run. All right, so this is running and you can see here that,
>
> **[9:26](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/parameters-version-artifacts-and-metrics?u=76281980&t=566)** well, it took 3.8 seconds and I got some warnings in the prediction with pandas, but that's fine. What I wanted to show you is that, well, data was not being defined. There's something else that I need to define there, but it is a good start of, like, how you can very quickly get access to that loaded model. So even if I don't have pandas here, I could just run this and it would be able to fetch that log model and load that from this mlflow helper called load model from this Python. So very, very good thorough way of interacting with the UI and the previous runs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MLflow]] (8), [[Pandas (Software)|Pandas]] (3), [[Fetch]] (2), [[Python (Programming Language)|Python]] (2)
> **Versions:** 5.0 (2), 2.4 (2), 0.5 (1), 0.1 (1), 0.85 (1)
> **File Paths:** train.py (3), example.py (1), con.yaml (1)
> **CLI Commands:** make (3), python (2)
> **Env Vars:** rmse (2), csv (1), maer2 (1), pkl (1)
> **Tools:** visual studio (2), command line (1), terminal (1)
> **Definitions:** is a  (3)
> **Cross-References:** go back to (2)


### 2. MLflow Projects

[↑ Back to Table of Contents](#table-of-contents)

#### [Working with MLflow projects](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-mlflow-projects?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-mlflow-projects?u=76281980&t=0)** - [Instructor] Let's see some of the components for working with an [[MLflow]] project. So we're going to take a quick look here this is the Databricks documentation in [[Microsoft Azure|Azure]], but the reason I picked this, it's not because it's actually part of the Azure documentation, but it doesn't have anything to do with Azure. But this is one of the benefits of using MLflow, which is that you have this consistency. It doesn't matter where you're running. You can run locally, you can run on Databricks, you can run on Databricks on Azure as well. So the components are going to remain the same. You're going to be using something else remotely, but in essence, you're still going to need these things, the project names, the Conda environment specified in a YAML file, and then a [[Python (Programming Language)|Python]] or a Shells script. You can see here any .py or .sh. I recommend sticking with Python because that will give you the ability of using kind of like the same language as you know, everything that is on Conda and all of the libraries already for [[Data Science]], not necessarily like Shells script, unless you have a very good reason to do that. Next, I want to show you like the YAML file here for the YAML project. One of the things that happens with project, and I'll show you this in a second, is that because it doesn't have a dot an extension, a dotted extension, you won't get that YAML nice syntax highlighting.
>
> **[1:35](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-mlflow-projects?u=76281980&t=95)** So let's go through some of the things that you will see here. First, you start by declaring the name. In this case, they're using an example of my project. You specify the Conda environment file. We'll see this in a second what that means. And then we'll have these entry points. You can have as many entry points as you want, but you should have at least one. This will allow you to basically use one of these to run a script with different types of parameters and even path or something like more like a number, in this case it's a float. And you can see it defaults to 0.1. Alright, so that's kind of like a quick overview of the documentation. Let's go to the exploratory example that I have here. In this example directory I have an ML project, and this is what I was explaining that doesn't have the [[GitHub]] can't tell like what type of file is this because it doesn't have the dot extension. It doesn't have a .yaml, .yml that identifies this as a YAML file. Similar to what we saw in the documentation you have a name, in this case it's a name, it's a data set validation. I'm going to have a Conda environment there and I'm going to have a single main entry point with some parameters and a single command right here. So I'm going to go back and I'm going to show you what this Python script does. So the Python script is actually using Click, but it doesn't matter. Click is a framework for producing Python command line tools, but it doesn't matter
>
> **[3:09](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-mlflow-projects?u=76281980&t=189)** that I'm using Click. You can use anything you like or nothing at all and use a plain Python script with no frameworks at all. So what this is doing, it's actually reading a CSV file and logging some metrics. You can see here if the logging metric is a Boolean it's true, then things will happen over here, otherwise it won't. And it will check for unnamed columns and zero columns, like if there's a count that doesn't have any items, then that will be counted and reported there. And I'm logging metrics and we'll see how those look. Alright. And then finally I want to show you the Conda environment. So the conduct environment is a special file for Conda that is specifies what the dependencies are. So in this case, if you were to create a new Conda environment, it would be named Exploratory. It will be using the Conda forge channels only, or the only one channel here is the Conda forge channel, not channels. And these are the list of dependencies. So we're going to take a look at in detail these later, but just enough to know right now that this is a way to specify your dependencies and get a repeatable environment going. Alright, so these are the components that you will see normally in an ML project and kind of like in this case, well I have the CSV I didn't show you the CSV, but we'll see as an example what that looks like. It can be anything really.
>
> **[4:43](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-mlflow-projects?u=76281980&t=283)** And here you could train, validate and do all kinds of other things in a repeatable manner.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (6), [[Microsoft Azure|Azure]] (4), [[MLflow]] (2), [[Data Science]] (1), [[GitHub]] (1)
> **Env Vars:** yaml (5), csv (3)
> **CLI Commands:** python (6)
> **Definitions:** is a  (4)
> **Analogies:** kind of like (3), similar to (1)
> **Documentation:** the documentation (2)
> **Tools:** github (1), command line (1)
> **Versions:** 0.1 (1)

#### [Create an MLflow project](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/create-an-mlflow-project?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/create-an-mlflow-project?u=76281980&t=0)** - [Instructor] Now let's go and create an actual [[MLflow]] project. So I've listed a couple of the steps that you will need, but let's start with the dependencies. And for this project, we'll be using an example project called wine.ratings.csv. It's the dataset, is just a single row, but it has many problems in it. Actually, let's take a look at what this CSV looks like. And it is, well actually, not this one, this one's very large. But what I want to show you is not that one but the one on exploratory. And this carriage.csv file, it's only a single row. And it has problems with some of the columns. You can see here, grape doesn't have any values. And [[GitHub]] doesn't render that, but there's some carriage returns here on this field, right there on notes. So the problem with carriage returns is, that might be problematic because some tools might not work with it. So we're going to catch those, we're going to report those and we're going to make MLFlow work with that and run and install everything that we need to. So that's in the exploratory one. And going back to the demo, so if we go here on how to create a project, how to run it. So I'm going to create an MLFlow project. We're going to use the small CSV data file, and then we're going to start with the dependencies. We're going to create a a conda environment named exploratory. I'm going to use [[Python (Programming Language)|Python]] 3.8
>
> **[1:33](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/create-an-mlflow-project?u=76281980&t=93)** because that's what I know that works with the tools that I want to use. Then I'm going to activate that environment. If you've never used Conda, it's essentially the same as a, well not the same, but similar to a Python virtual environment. You create one, then you activate one, and then everything that you install in that environment will go to that environment and on to your system or other types of environments. So there are contained dependencies in that same environment. It can be a little bit overwhelming. If you want to do anything very specific or different than these basic examples, I suggest you go through the documentation for Conda. So now that that we've created the environment, we've activated the environment, then the way you create that conda_env.yaml file is by exporting it. And once you export that, you'll end up with a file that is similar to this. In this case I named it exploratory. You can name it anything you want. And that name, it can be anything you want, really. It's just a way of identifying what it is. The channels, I think by the default you get a conda-forge, but if you want something specific, for example, I know [[PyTorch]] wants its specific channels. And then it list the dependencies. So here we have some base dependencies, and that's fine. And then we are going to use pip. You can see we can use pip to actually go and install these extra dependencies at the very bottom right there. So not only we're declaring pip as a dependency, but we're actually then saying, "Hey, by the way, I want pip to actually install [[Pandas (Software)|pandas]] and MLFlow,"
>
> **[3:09](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/create-an-mlflow-project?u=76281980&t=189)** and we have some specific versions there. And then if you're installing or adding or removing things, this is a good command to remember. You're going to make the changes to the conda_env.yaml and then update that and then use the --prune, means that anything that is no longer defined in the conda_env.yaml is going to be removed. So if you don't remove anything, that's fine, but if you do, then it's going to stay consistent, right? Let's go and take a look at exploratory. But before we do that, we're going to see, you know, we already went through the MLproject. It is going to be run a single entry point. It's going to be this Python validate, we already saw the evaluation. It is this validate.py file here. I see here it is going to run a few commands going through the CSV and then report on it depending on what we're doing. All right, so now we're going to go back to my terminal. We're going to do conda activate exploratory, which I just did, but you'll see that it is showing up here. And then now I'm going to be running MLFlow. So mlflow run, dot for signaling that this is the specific directory that I want, and I am going to say -P filename=, this is going to be carriage... I don't remember the name of the file. So let's see, carriage.csv, there we go. So mlflow run . -P filename=carriage.csv.
>
> **[4:47](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/create-an-mlflow-project?u=76281980&t=287)** So let's talk a little bit about what's going on here. So MLFlow run is what we're going to use to run the project. That means that this is the actual project. This is, exploratory is where this lives. Dash P, it's, we're starting to pass in parameters. So if you remember, if I go back very quickly to here, not to the validate.py but to the MLproject file, you'll see here that the parameters listed here have log, max_errors and filename. So filename is right there, max_errors and log. Because log and max_errors have defaults, you can see here that default is True and the default is 1, but filename does require a parameter. I need to specify those. All right, so now that I know what I have to pass in, filename=carriage.csv, and I'm going to run this and see what happens. All right, so that ran very quickly. You can see there's a lot of output here, but special attention to this one. It knows that my Python version is coming from a conda environment. It found that there was an existing conda environment. This one, it reused it and didn't create one from scratch, which is great. And then you run python validate.py True because that's the default, and then 1 because that's also the other default. And then you figure out a absolute path towards that carriage.csv file in it. This would guarantee that regardless of where you're running these, it will still work. So if you clone this repository
>
> **[6:20](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/create-an-mlflow-project?u=76281980&t=380)** or want to run these on your own, the path will be slightly different. But if you pass the same thing, mlflow run . -P filename=carriage.csv, I'm sorry, right there, it will work the same. All right, so it succeeded. So what would happen if I do something that doesn't exist? So say for example, fake.csv. So what I would expect is an error and then you go error, right here is the error, "Got value fake.csv for parameter filename, but no such file or directory was found." And what would happen if I don't pass any parameters? So if I do something like that, well, in this case, "No value given for missing parameters: 'filename'" because remember, filename doesn't have anything that signals a default value. If you wanted to pass any of the other ones, if we take a look at the MLproject one, we see a log and max_error. So we want to pass mlflow dot run -P filename=carriage.csv, and then we say -P max_errors. We could say, you know, 10 for example. And then we will run that, and that will get captured again over here and it would be transparent. So you have the ability to pass extra parameters that are not captured in the defaults if you want to tweak around and play around with those. So that's it. That's how you run an MLFlow project.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MLflow]] (11), [[Python (Programming Language)|Python]] (5), [[GitHub]] (1), [[PyTorch]] (1), [[Pandas (Software)|Pandas]] (1)
> **File Paths:** carriage.csv (7), conda_env.yaml (3), validate.py (3), fake.csv (2), wine.ratings.csv (1)
> **CLI Commands:** python (5), pip (4), make (2)
> **Code Identifiers:** max_errors (4), conda_env (3), max_error (1)
> **Analogies:** for example (3), similar to (2)
> **Definitions:** means that (2), is a  (1), is an  (1)
> **Prerequisites:** install (4)
> **Env Vars:** csv (3)

#### [Run projects from remote Git repositories](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/run-projects-from-remote-git-repositories?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/run-projects-from-remote-git-repositories?u=76281980&t=0)** - [Instructor] [[MLflow]] has the ability of running remote projects if you pass in [[GitHub]] URL, so one of the things that I want to show you is that how is that possible with an example repository that is actually pretty wildly used on the MLflow documentation everywhere. This very short project similar to what we've seen already in the previous video. So this is on the MLflow repository organization rather slash mlflow-example that's the repository, it's a poly one. You see that it's fairly simple and doesn't get updated too often. It has the same structure we've seen before we have a MLproject file over here. Very simple just in lines [[Python (Programming Language)|python]] train.py and there's alpha which is this value right there is 0.5. And then the l1 ratio it defaults to 0.1. And then if we go and take a look at that train.py file it is also actually pretty straight forward. It will do a couple of different things it will start run and use those values. You can see right there l1 and alpha right here. So let's take a look and try to run this project. So how exactly are we going to run it? We are going to go here
>
> **[1:33](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/run-projects-from-remote-git-repositories?u=76281980&t=93)** and what we're going to do is go to the terminal and go and run it but to run these we will need to select one piece of information here the SSH based address. I'm going to copy that and I'm going to use it to run it in the terminal. So I'm going to do this. And I'm going to say mlflow run and then that address right there and I'm going to say I don't know if it's going to ask me to pass in a parameter. Let's just try it like that and see what happens. All right so it fetches the project and then it starts running. And then it succeeds. So that's pretty straight forward. Let's take a look at some of the things that happened. [[Fetch]] is the project from GitHub so it goes right to the source right there to GitHub that's the URL that I passed in. And then it grabs the master branch and it looked at an mlflow environment that already existed. Apparently, I already ran this. So that's why this mlflow 2b6b69a3 already existed. So it just ran that again. It ran this elastic model and outputs a couple of values right there. The documentation says that this also excepts alpah0.7 and see what happens. I think this tanks when I pass something like that. Well it didn't it actually worked.
>
> **[3:07](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/run-projects-from-remote-git-repositories?u=76281980&t=187)** If take a look at the mlflow or the MLproject rather and you go here and see the parameters alpha defaults to 0.5 and l1 ratio falls to 0.1. So there you go that's kind of like how you run if a have a repository that is an MLproject and you run it remotely you see the technique that I just showed you, you're able to run that and fetch this automatically everything with mlflow.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MLflow]] (9), [[GitHub]] (3), [[Fetch]] (2), [[Python (Programming Language)|Python]] (1)
> **Tools:** github (3), terminal (2)
> **Versions:** 0.5 (2), 0.1 (2)
> **Env Vars:** url (2), ssh (1)
> **File Paths:** train.py (2)
> **CLI Commands:** python (1), ssh (1)
> **Analogies:** similar to (1), kind of like (1)
> **Cross-References:** previous video (1)

#### [Connecting MLflow to Databricks](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/connecting-mlflow-to-databricks?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/connecting-mlflow-to-databricks?u=76281980&t=0)** - [Instructor] There are a couple of things I want to show you here on how to connect [[MLflow]] to Databricks. There you have several options, and MLflow is pretty flexible. Let's start here with the documentation from Databricks itself. It doesn't matter here that it's referencing [[Amazon Web Services (AWS)|Amazon Web Services]], because any of these will work. As you know, you can install MLflow locally, and what you want is to make sure that everything is connected. So here, it goes about the talking about the community edition, but don't worry about that. It really doesn't matter. What you need to, whenever you're configuring MLflow to communicates with a Databricks hosted tracking server, is to ensure that you are exporting the MLflow tracking URI, and here, this might not look correct, it might look like this is an example, but it isn't. What it needs here is Databricks. And optionally, you can use Databricks username and password if you want to authenticate straight away, or you can generate a [[Representational State Transfer (REST)|rest]] API token. When you generate a rest API token, and create your your credentials file, then you can do, instead of a username and password, then you have the option to use the host and token instead of doing this. So it's there, you have a couple of options that you can use
>
> **[1:37](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/connecting-mlflow-to-databricks?u=76281980&t=97)** to connect. Next, it tells you to configure MLflow applications, but we're not going to go through that. But definitely, you can... Well, it tells you right here, MLflow tracking URI, Databricks, and then MLflow experiments create. But in essence, what it means here is that if you set these variables correctly, then you should be good to go. Now sometimes, generating a token might be tricky, and I suggest going through the docs that generate a personal access token. It's pretty straightforward. You would have to go to a hosted version of Databricks, which I'll show you in a second. So this is my hosted Databricks that I have running here, and I believe if you come here, well actually let's go step by step with the docs. It says click on the settings tab on lower left corner of your Databricks workspace, and then to user settings. So let's go to the user settings really quick. I'm going to click here, User Settings. And you can see here that it already generated one, but it can create, generate a new token. So say for example, I'm going to say this is an example token, and this is a good thing that it has like a lifetime in days that are (indistinct) to 90. So three months there, and then it will go and expire and go away. A good security practice. So you generate that, and you get one time access to these token, and then you are done.
>
> **[3:11](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/connecting-mlflow-to-databricks?u=76281980&t=191)** I'm not going to use that right now, but it is how you would create that. All right, so next up, I want to show you how to connect MLflow to [[Microsoft Azure|Azure]] Machine Learning. And you can use the configuration there if you want to make MLflow work with Azure Machine Learning Studio, and Azure Machine Learning Registry in this case. Because it's not the only thing you want, you have many different options to connect MLflow to other remote services. So in this case, I also want to show you how to do it with Azure Machine Learning, and Azure Machinery Registry. So they talk here about an MLflow URI. So there's a couple of ways that you can get that. You can either build it manually, let's see if they have the way on... Yeah, I don't think they explain here how to do that manually, but they do have a way of setting that manually. And the way you do that manually, I'll show you with this notebook here. You can see here that the Azure MLflow URI uses these notations. So Azure ML, colon slash slash. You'll have to set up these variables, the region, your subscription ID, your resource group, and the workspace name. So if you have that with Azure Machine Learning, you'll be able to connect directly, and then that's it. So no problem there by tracking,
>
> **[4:46](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/connecting-mlflow-to-databricks?u=76281980&t=286)** but I want to show you another way that you can use to get that. And it is going to the portal, and then the MLflow tracking URI will show there, and it has these very convenient link here to copy that to the clipboard, and then you can export it. So there you go. There's like three different ways, two different services that you can connect MLflow to a remote tracking server. One for tracking models, but you can also use it to track any other types of information.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MLflow]] (15), [[Microsoft Azure|Azure]] (8), [[Representational State Transfer (REST)|Rest]] (2), [[Amazon Web Services (AWS)|Amazon web services]] (1)
> **Env Vars:** uri (5), api (2)
> **Documentation:** the docs (2), the documentation (1)
> **UI Navigation:** go to (2), click on (1)
> **Definitions:** is an  (2), is a  (1)
> **Prerequisites:** install (1), configure (1), set up (1)
> **CLI Commands:** make (2)
> **Analogies:** for example (1)


### 3. MLflow Models

[↑ Back to Table of Contents](#table-of-contents)

#### [Components of the MLflow package](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/components-of-mlflow-package?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/components-of-mlflow-package?u=76281980&t=0)** - [Instructor] Let's go through some of the information required to understand what [[MLflow]] Models are. So, MLflow Models is just a way of formatting for how a model, a machine learning model, should be interacted with, where it came from, what are the dependencies surrounding this model, how to interact with it, how to run it, how to serve it, and essentially, hopefully how it was also built. So I'm using the MLflow documentation here, but I'll show you a little bit extra, so that you have a good idea on how this looks. But I want to go through a little bit of the storage format. So you'll have these files in, whenever you're dealing with a model, particularly the MLmodel file, which is again, if you're following the convention that they're using, you know, for ML project, it's a YAML file, and MLmodel will be a YAML file as well. So just be mindful that when you open that, it might not look quite good, especially in text data that don't recognize that as a YAML file. Then you'll have a couple of things. You'll have the actual model, in this case is a .pkl file, a P-K-L, that's a [[Python (Programming Language)|Python]]-based model there. Then you'll have your conda.yaml, which we've already seen how that looks like.
>
> **[1:33](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/components-of-mlflow-package?u=76281980&t=93)** We'll have a Python environment that we'll also take a look, and then perhaps even requirements.text for things that we install right there. Alright, so enough of the MLflow documentation. Let's take a look about a thing that I actually built using ML Model. And I created this project, and we'll take a look how to build it next, but I want to show you how it looks in Visual Studio Code. So if I went through the actual run, and you can see here we have all of these files, we have the requirements one, we have the python_model.pkl, the Python environment ML Model, and conda.yaml. We'll start by the ML Model. Interesting enough, VS Code did manage to find that this is a YAML file. So the syntax highlighting is working correctly there. So that's great. And let's take a look at some of the things that we're seeing here. So this is a Python function. We haven't covered what a Python function is in the context of building machine learning models and MLflow, but we'll take a look at that next. But it indicates a flavor. A flavor is just a way of telling them a flow, how it should work with this model. So you have different flavors, ONNX is a flavor, scikit-learn is another flavor. So you have different ways of interacting, different supportive ways to deal with loading
>
> **[3:09](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/components-of-mlflow-package?u=76281980&t=189)** and interacting with the model. All right, so we have the artifact path. In this case, I went straight into the path that it had that model. So that does the artifact path cloudpickle_version. So you start getting into some of the specifics behind that. It was built using Python 3.8.13. This is important because if I was using Python, let's say something ridiculous like Python 3.1, then this wouldn't necessarily be compatible. So this is already advertising something pretty crucial and important. And this is important because before things like MLflow, I definitely got into problems at some point trying to run auto ML Models that came from a service that I didn't have the dependencies or Python versions, and I had to find these by trial and error. Alright, mlflow_version, perfect. It was using 1.28 was the UUID and run_id, so I did run these to create that. And what's the signature, the signature is important because it tells us what the input and outputs expected are for interacting with this. All right, moving on, let's take a look at the conda.yaml. Nothing really out of the ordinary. We've already seen this file before. We'll take a look at the python_env.yaml file. And now this tells you a little bit more about all of the stuff regarding Python, about what the Python version is, what the build dependencies were and what are the extra requirements. And these are here requirements.text.
>
> **[4:44](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/components-of-mlflow-package?u=76281980&t=284)** So MLflow, let's see, CloudPickle, iPython, ooh, Transformers. So this indicates that this might be coming from [[Hugging Face]], and we'll see how to get this going in a separate lesson. But this is already important because it basically allows us to have lots of information regarding how this model came to be. And again, it is just a normalized way, whether we can see about information specific to this model, Python versions and where it came from and how to work and interact with it. You can see here the python_function loader_module is using mlflow.sklearn. So it's native support for scikit.learn models and that's how this works right there. And it tells you specifically the flavor is scikit.learn and it tells you how to do that. But this one has two flavors, right? Scikit.learn and Python function. And the one that I show you in VS Code was using Python function, so there you go. We'll see next some of the other components of an MLflow package.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (14), [[MLflow]] (9), [[Hugging Face]] (1)
> **CLI Commands:** python (14), find (2)
> **Code Identifiers:** python_model (1), cloudpickle_version (1), mlflow_version (1), run_id (1), python_env (1)
> **Env Vars:** yaml (4), onnx (1), uuid (1)
> **File Paths:** conda.yaml (3), python_env.yaml (1)
> **Versions:** python 3 (2), 8.13 (1), 1.28 (1)
> **Tools:** vs code (2), visual studio (1)
> **Definitions:** is a  (3)

#### [Use a registry with an MLflow model](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/use-a-registry-with-an-mlflow-model?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/use-a-registry-with-an-mlflow-model?u=76281980&t=0)** - [Instructor] Using a registry with an [[MLflow]] model is something that you should start getting used to. This feature of MLflow is not unique to MLflow. You can actually register models and I believe almost all cloud providers have like a solution for this where you can register those models and keep changing the versions that are associated with those models, especially when models keep changing, which is a very useful feature. Alright, so how do we do this exactly with MLflow? I've documented a couple of the things that you can do here. Basically, you have many different options on registering models and then retrieving them. But essentially what it is, is that you are training or producing models, or creating new models, or creating new environment for those models. And then you want to find some way to register them. You can use an existing model and register it and start that way. Now this doesn't give you much, it basically creates an empty placeholder with almost no information. You can do this through the API or DSDK, as you can see here. But you can also use the user interface and I'll show you a little bit on how that looks. Now, I'm not going to show you how to retrieve or update models right now, but definitely to create a a new model,
>
> **[1:37](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/use-a-registry-with-an-mlflow-model?u=76281980&t=97)** you'll have to produce an artifact, and the way to produce an artifact is by using the log_model when you're training it. All right, so we've seen that we can do that, but I haven't told you the whole story. Now, if you want to try this out locally, which I highly recommend you do, you will want to run a server. And the way to do this is kind of tricky, so you have to have a couple of things in order. I'm going to paste a whole command here. All right, so the server started, but like I said, I want to show you exactly what's going on here. So let's take a look. MLflow server is the command and sub command that you want to use. The backend-store-uri, I'm going to use sqlite. Sqlite is an embedded database. You should have that available on your system otherwise, you'll have to install sqlite three. And this is just pointing to a path, it's the name of the file on the current path, and then the default artifact route points to /tmp, which I'm just using as a temporary solution. And then the host is 127.0.0.0.1 and running on port 5,000. Alright, so that is how it runs. And you can see here that it has put it, and now if I go to the UI, I go to my browser over here. And I go here. It will show that I have this available.
>
> **[3:12](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/use-a-registry-with-an-mlflow-model?u=76281980&t=192)** So let's create a model to get started. You can use the UI, you can say for example, t5-onnx and that will create an empty model. So that'll give you the description, tags, and that way, with this, you can then interact with this because this is exactly the same as using this API clause over here. So that is how you start interacting with the different models and use the register. Especially, and I'm going to show you here, there's a couple of other things that you may want to be doing. Once you've created this model, you can see here that there's a couple differences. You can register a model, you will use MLflow register model. But then you can use the create registered model to create a new registered model. And if the name exists, it'll fail because this requires a unique name. So make sure that you're mindful of the names that you have to use there. But then you can also do this manually. I think this is very cumbersome, if you want to try to do this manually. So try to make sure that whenever you're doing this are like special occasions, in very few situations where you have to actually point to the run_id and where exactly that lives.
>
> **[4:48](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/use-a-registry-with-an-mlflow-model?u=76281980&t=288)** So that's it, that is how you interact. That is how you log the model as you can see here. And then interact with this server that allows you to store model information as we did before.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MLflow]] (6)
> **CLI Commands:** make (2), find (1)
> **Env Vars:** api (2), dsdk (1)
> **Code Identifiers:** log_model (1), run_id (1)
> **Versions:** 127.0.0 (1), 0.1 (1)
> **UI Navigation:** go to (2)
> **Definitions:** is a  (1), is an  (1)
> **Ports:** port 5 (1)

#### [Referencing artifacts with the API](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/referencing-artifacts-with-the-api?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/referencing-artifacts-with-the-api?u=76281980&t=0)** - [Instructor] Referencing and interacting with models that are living in a remote registry is a thing that... It's kind of like taking a step further when you're working with [[MLflow]]. So we've already seen a little bit of the examples here of how you can register one, but we haven't seen how to retrieve or update these models. First, let's take a look at what I have here. I've run a couple of experiments already. These experiments were run a few minutes ago, and these experiments were able to log an actual model, but there's a couple of things that are not really straightforward, and it might take a while. It definitely took me a while to figure out, and I want to show you. So what I want to do is I want to open my Visual Studio Code here, and I have a couple of things here. So I have this Jupyter Notebook with some examples on how to register models and how to interact with the API dynamically, but let's take a look, first, at how I log the model. So I have this small script here, and I really like [[Hugging Face]]. I think it's tremendous, and what this script is doing is actually using Hugging Face, so it's inputting the pipeline from transformers, and then it's creating these [[Python (Programming Language)|Python]] class that basically tells MLflow how exactly it needs to interact with this model.
>
> **[1:36](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/referencing-artifacts-with-the-api?u=76281980&t=96)** So the way it interacts with the Hugging Face model is it sets the tokenizer and model in the constructor, and then it defines a single method that, in this case, is translating English to German, that's fine. It constructs a model input and output, and, finally, it goes and it creates the tracking starts that run, and it logs the model. There's a couple of key items here. First one is the log model. You can see here that whenever you're logging a model, you're producing an artifact. You're producing that model, and, here, you're capturing all of the details. In the beginning, I didn't have a registered model name. So that caused that it wouldn't show up on the registry. So in this case, I decided to call this the t5-small-summarizer, and a little bit of the details there about what the model is, and it's now waiting for registration, and it's using some of the parameters that I created. Key item here is the tracking URI has to be set. I wasn't setting the tracking URI, and that was causing the model not to be registered. So if you're not setting the tracking URI, basically, you won't get registered, and you won't know because there's no output or messages saying, "By the way, there's no tracking URI." So make sure that the tracking URI is always set to what you're expecting, and then when you're interacting with it,
>
> **[3:11](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/referencing-artifacts-with-the-api?u=76281980&t=191)** when you want to poke around with that model, then you have to make sure that, in this case, I'm doing that dynamically. I'm running on port 5000, it's running locally, and I'm setting that again. I am not sure why that's not default. I believe that default is the file system, and so there's that disparity, because you're expecting things to be running on the server, but they're not. All right, so let's go and open up my terminal real quick, and in my terminal, I'm going to run this log model and see what happens. I'm going to say python log_model. This is just a very straightforward, very quick, and it is running that and successfully registered model t5-small-summarizer and created version 1. All right, so let's go to my web browser, and then if I refresh these, you'll see that there's now 16 seconds ago. I run that, it created a model. That's fine. We have some versions. That's excellent. So now, what I want is go here to Models and see what's going on there. So if I click on Models, you will see that, now, t5-small-summarizer appears, and that Version 1 is there. If I click here, there's a couple of things that are interesting.
>
> **[4:44](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/referencing-artifacts-with-the-api?u=76281980&t=284)** If I click on version 1 again, and then I go and click on the Source Run, several different things will come up. These are very, very interesting. First, you'll have captured the ML model, which we saw before, the Python environment. It allows you to have Copy, Paste, essentially Copy Paste code that you can paste on Spark data frame. So you can run these and interact with this, which is really tremendous. If you want to do this on the [[Pandas (Software)|Pandas]] data frame, you absolutely can do that as well. So very powerful, very complete solution there if you want to interact with all these models, because they're capturing everything, really like even the parameters. Well, in this case, I didn't use any parameters. You'll see the Metrics. There's nothing there. I don't believe I used tags, so there's no tags, but the Artifacts, definitely there. Requirements, you can see transformers, torch. There's all kinds of different things there. All right, so that sets the t5-summarizer, and, now, let's go back to the Visual Studio Code and let's start interacting with this. First, we're going to import MLflow, so the tracking URI, and create a client object, that's perfect. Now, we're going to register a model and let's see what happens. This model must not exist. So we don't have a t5-onnx, and this is just a name. Perfect, so register that, and if we go back to the URI, to the web service, if I click Models,
>
> **[6:23](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/referencing-artifacts-with-the-api?u=76281980&t=383)** you can see that t5-onnx is there. There's no version though, and the reason there's no version is because I just created it with a name. If I click here, there's nothing going on, nothing very useful. Let's go back and continue with executing ourselves. We can definitely delete a register model as well. If I click that, Delete a registered model, t5-onnx, you'll see that if I load this, it's gone. Now, let's play a little bit with t5-small-summarizer. Click on this, and you can see there's no description. The stage is None. So let's make some changes there. What we're going to do is we're going to update the description, and I'm going to say, "This is the T5 model in an ONNX version 1.6 using Opset 12." So if I run this cell and executes, it completes the API call, and if I go here and it automatically... I didn't even have to reload, and the description was updated perfectly for version 1. You can see here that the description says now that this is the T5 model in an ONNX version 1.6. All right, so that's a good overview of how you can interact with these models when you're registering them in a tracking URI, in a tracking server, in a remote registry. I'm using a local server here, but this would, essentially, feel the same regardless of what remote API you're using, and it is... We didn't cover absolutely everything that you can do, of course, but this should allow you to get you started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MLflow]] (3), [[Hugging Face]] (3), [[Python (Programming Language)|Python]] (3), [[Pandas (Software)|Pandas]] (1)
> **Env Vars:** uri (8), api (3), onnx (2)
> **CLI Commands:** python (3), make (3)
> **Versions:** version 1 (6)
> **Tools:** visual studio (2), terminal (2), jupyter (1)
> **UI Navigation:** click on (4), go to (1)
> **Code Identifiers:** log_model (1)
> **Ports:** port 5000 (1)

#### [Saving and serving MLflow models](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/saving-and-serving-mlflow-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/saving-and-serving-mlflow-models?u=76281980&t=0)** - Now that you know how to register some of the models that you'll be working with, it's time to find about how to interact with them. And one way that you can do that is by serving them. We've already seen a little bit of these script that I created to log that model, which has a couple of components. The first component is this one where we have the summarizer class, which inherits from this [[Python (Programming Language)|Python]] model class. So as you know, this is so that you can, so that ML flow understands how to interact with that. And in this case, we've used a hugging phase, API so that we can do some predictions. In reality what we're doing is translating English to German. As you can see here, we're going to get the input and then we're going to use that input to the tokenizer. And then the predict method is going to return the actual output. Now we've also, and this is pretty important, especially when you're serving, we've also determined that we have the model input and model output right there. And we are defining how to interact with this model. Now, when we run the script, we were able to log that model run.
>
> **[1:34](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/saving-and-serving-mlflow-models?u=76281980&t=94)** You can see here that we did log it and we used that registered model name t5 small summarizer. Now you can interact with this model from local runs, and that is how we're going to do that. And the way, and I put this right here at the very bottom, the curl command that you can use to make that happen. All right so I'm going to go to my terminal and see how we can make that work. Okay so I'm here in my terminal and I have ML flow installed. I have a couple of ML runs already. You can see if I list the contents there, you'll see that there are several. So let's go through some of the components you will need to know in order to run from a local run. So you can do ML flow models serve, and I'm going to pick this run ID 151650, which is right here, right that one. So that one is the one that we're going to use and you can see has kind of like a path slash model and I'm going to use port 5,001. And the reason is because if I don't use port 5,001 and let's try it out really quickly, so that you can see kind of like what you'll be getting into is an error condition.
>
> **[3:07](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/saving-and-serving-mlflow-models?u=76281980&t=187)** You are getting this error condition because the port is already in use, as you can see here, connection in use port 5,000. So that is because I have already a tracking server or a tracking URI running already. So I am going to use, I'm going to use port 5,001. So if I run this, you'll see that it was able to boot up correctly and it is running already. Now good thing that I was able to save that curl command so that I can interact with it in the terminal. So remember this is serving an API and one of the things that this will do is that like if I go to a browser and go to this address, nothing will happen. It only has one end point, which is invocations. And this is true for anything that you will serve with ML flow. So here on this new tab, I have the command pasted and the command is going to use that same URL or URI 127.0.0.1 on port 5,001. And you can see that I am also using invocations. This is critical because if you don't use that path, it will get you into trouble. And remember those inputs and outputs? That is defined here. You can see I'm using columns, text and the data. And this is kind of like a,
>
> **[4:39](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/saving-and-serving-mlflow-models?u=76281980&t=279)** almost like a [[Pandas (Software)|pandas]], it is a pandas format. You can see here that the format is pandas split. Alright, so let's see what happens if I run this. So I'm not summarizing but doing a translation to German. And I wanted to translate today's a perfect day to practice automation skills. We can of course do all kinds of different things here and we can say, we can say something, something different. Say for example, curl can be difficult to get right, and we can run that and curl can- Well I can't read German, but you get the gist of it. So if we go back to the other tab on my terminal, you'll see that interaction in fact is happening correctly with a little bit of output. You know there's some warnings about the max length. The tokenizer is not working quite correctly given the type of input but that's fine. We are able to interact with it. Now, one of the caveats here, and we'll see these in other videos, is that you are kind of like left in the dark as to how this will work. So I'm going to cancel the serving so that you can see a little bit of what is going on in this specific run right there. So I'm going to have several runs and I'm going to say Emma runs a list of contents
>
> **[6:14](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/saving-and-serving-mlflow-models?u=76281980&t=374)** of the directory and zero. And you can see there's plenty. Let's find all of the runs that have models in it. So actually what I want to do is not, I did the the wrong rep here, so I'm trying to find anything that has a model and see if the contents are there. So there you go. So you have a few, just a few runs that were actually able to produce one of those models. So if I go and grab, so for example the first one, and I do three so that we can see what the contents are you should already be familiar with this type of setup. This is the magic that allows ML flow to understand what is going on, how to serve it, what are the requirements, where is the model, what the Python environment looks like, and conduct environment if anything that's kind of like what it needs to do. And the ML model explaining what it is and how it works. So in fact, if we were able to look at that ML model file and if we concatenate the contents of that run, you can see here that it has the actual signature and it has a timestamp and all kinds of different useful information that again we've already seen for ML flow. So that's super useful. And that is the those are the parameters, those are the things,
>
> **[7:45](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/saving-and-serving-mlflow-models?u=76281980&t=465)** that [[Metadata]] that allows ML flow to serve a model. So there you go. That's how you serve a model that was previously run from an an ML run with a capture ML run on a local disc.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Pandas (Software)|Pandas]] (3), [[Python (Programming Language)|Python]] (2), [[Metadata]] (1)
> **CLI Commands:** curl (4), find (3), python (2), make (2)
> **Analogies:** kind of like (5), for example (2)
> **Ports:** port 5 (5)
> **Env Vars:** api (2), uri (2), url (1)
> **Tools:** terminal (4)
> **UI Navigation:** go to (3)
> **Definitions:** is an  (1), is a  (1)


### 4. Introduction to Hugging Face

[↑ Back to Table of Contents](#table-of-contents)

#### [What is Hugging Face?](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/what-is-hugging-face?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/what-is-hugging-face?u=76281980&t=0)** - [Alfredo] Let's dive in into a little bit about what [[Hugging Face]] is. Now, sometimes people don't see Hugging Face as a platform, but in fact it is. You can see here right off, like the main website, we see models, datasets, spaces, and docs. We'll cover a few of those in detail later, but it is enough to say that we're going to be able to interact with and host models and create our own version of our models, work with datasets, existing ones and our own as well. So the way I see it, is that it's a platform, a way where we can cross-collaborate with everything machine learning. Now I've gone ahead and created my own profile, an account, and you can see here that there's models and datasets. I don't have anything yet. I'll have to fix that pretty soon, but this is the way, again, the way I see it is kind of like [[GitHub]]. Just like GitHub isfor software engineering, I see Hugging Face being the same thing, but for machine learning, collaboration, and interaction. The other thing that I want to show you is once I created my account, I had these programmatic access using the hubs, [[Python (Programming Language)|Python]] client library. We'll get into a little bit more details as to what the hub is, but you can start seeing here that sometimes it's not only the website, but you have libraries that you can use
>
> **[1:34](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/what-is-hugging-face?u=76281980&t=94)** to create repositories with specific names and with a specific purpose, like say, for example, a model or a dataset or even a space. Now spaces are its own special a thing, and we'll cover those later as well, but just wanted to show you that you have the ability to interact with Hugging Face in many different ways. And then the last thing I want to show you is that, and this is the Python Package Index. This is where PIP goes and finds itself packages. There are a couple things I want to show you. First is, the first one is Transformers, and if you search for Transformers, and you go to this one right here, version 4.22.2 in this case, you'll see that the familiar Hugging Face logo is right there, and this is in fact coming from Hugging Face. So this Hugging Face itself providing open source tooling, open source libraries to interact with models. And here, Transformers is specifically for text, vision, and audio, and it is definitely a joy to use, and we'll see a couple of things that we can use, and then you have all these kinds of demos available. Now, the one thing I want to show you in relation to this. I'm going to go back here.
>
> **[3:07](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/what-is-hugging-face?u=76281980&t=187)** So you see here T5 and GPT-2 mentioned. Let's go back to the website, to the main website over on this, the first one, the first step, rather, and I'm going to search for GPT-2. I'm going to say that, I'm going to hit return, and then you'll have several different things. So this means that you have support for [[PyTorch]], [[TensorFlow]], TF Lite, It uses several different things here, and you have this model card. The model card allows you to demo or explain what this is about and give you a little bit of information. You can look at this, you can deploy it. Hugging Face has its own way of deploying its models. You have like a hosted API where you can do live inference and you have like the endpoints, and even SageMaker, Direct SageMaker, SageMaker Connection. So that's pretty interesting, and I'm going to scroll back here because I'm going to show you, well, here's the hosted inference API. So let's try to compute that and see what happens, and then we'll see these being completed by the model. So pretty good, pretty useful, and what I was telling you about before, and I mentioned Spaces a little bit, was that you have the ability to use this GPT-2 model and do something else with it and repurpose it and fine tune it to your own needs. And all of these are lots of different accounts
>
> **[4:42](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/what-is-hugging-face?u=76281980&t=282)** that have been doing something with it. Now here you have examples, you have a little bit of documentation, and then we saw Transformers, but you can see here pipeline and set_seed. So Transformers allows you to have an abstraction library with Python to do text generation. In this case, this example is going to be using text generation. You can see here, and then it's like GPT-2, and again, we'll see all of this in detail later, but you get the output. So essentially four lines of Python if you're counting the import statements, and then you get something really powerful out. So you get libraries, hosting data sets, hosting models, and interaction all from Hugging Face in a web UI that allows cross-collaboration for anything machine learning.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (9), [[Python (Programming Language)|Python]] (4), [[GitHub]] (2), [[PyTorch]] (1), [[TensorFlow]] (1)
> **Env Vars:** gpt (4), api (2), pip (1)
> **CLI Commands:** python (4), pip (1)
> **Analogies:** kind of like (1), just like (1), for example (1), it's like (1)
> **Versions:** version 4 (1), 22.2 (1)
> **Tools:** github (2)
> **Code Identifiers:** set_seed (1)
> **Cross-References:** go back to (1)

#### [Overview of the Hugging Face Hub](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/overview-of-the-hugging-face-hub?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/overview-of-the-hugging-face-hub?u=76281980&t=0)** - [Instructor] Let's do a quick overview of the [[Hugging Face]] hub. Now, the Hugging Face hub means that you will be interacting with repositories, models, data sets, spaces, and even perhaps organizations and endpoints, depending on what you're trying to do. So I have the main, for my account, the main Hugging Face space. So if I go ahead and go towards the New button over here, you'll see that you have three main options. If I go to Model, well, that's effectively a repository. So I'm going to click here and you'll see that I get prompted to create a new model. So in here, you can actually fill out the names and have a couple options. If you've worked before with [[GitHub]], this will feel very, very familiar. It's similar interactions, and you can do the same thing here with the Hugging Face [[CLI]]. So you'll have to specify who's the owner, in this case, I'm the only owner. I don't have, I'm not part of any organization. And I'll put the model name, for example, example model name, and then the license, I'll pick, you know, probably ,say something like MIT, if it's supported, I think I scrolled past by it. I can say MIT and if it's public or private. So once that's available, that is uploaded, I can consume these from anywhere, either with the libraries from Hugging Face
>
> **[1:35](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/overview-of-the-hugging-face-hub?u=76281980&t=95)** or directly from this page. But there's more to the hub than these. And I'll continue exploring some of the other things. The data set is, it's the same interface. And again, you can do this with the command line tool as well, where you can put information here as to what the dataset is. Now, datasets similarly will allow you to have those files hosted in. And all of these, by the way, are GitHub repositories. So both the dataset, the model, they're all GitHub. GitHub, not GitHub, [[Git]] repositories where you can interact with and have full requests and changes and even clone the repositories locally so that you can have some interaction. You'll have to have Git LFS. And actually this is covered in the documentation. If I go back here, you can see here that to clone your model or dataset locally, it'll require LFS. So I think newer versions of Git, the command line tool will come with LFS installed. But if it doesn't, you can see here it's Git LFS installed, and then that's good tool and then you can clone it pretty straightforward, like that. And similarly, going back to this, this was with a data set. If we go back and say Space, this is the one that I wanted to show you. Space is allows you to have a hosted version of your own application using Hugging Face.
>
> **[3:12](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/overview-of-the-hugging-face-hub?u=76281980&t=192)** And in this case it has options of, here it tells you Streamlit or Gradio, but in fact you have these three options. You can use Streamlit, Gradio or Static. What does this mean? Like, if you want to demonstrate what your hosted application can do, and you want to do that interactively, you can use any of these options to do just that. So the space will allow you to have some of those running and make it really, really interactive. So that's it, that's kind of like a big high level overview. I didn't cover organizations, but essentially that's if you're having a group of people or your own company and you're working with all machine learning engineers and you want to create your own or new organization, then you will do that, similar to what you would do with GitHub and an organization, when you join a company, say, for example, let's see some of these ones right here. Well, these ones are not organizations, but you'll get, [[Google]] is an organization. So we go, let's take a look at that one. And in this case, Google is the organization and this is the project. So if I was collaborating, probably here, Google have to give me access to their own organization and help me contribute to this. And I don't want to translate this right now. And that's a quick overview of what the hub is, essentially a few different components that we discussed.
>
> **[4:50](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/overview-of-the-hugging-face-hub?u=76281980&t=290)** We talked a little bit about data sets, spaces, organizations, and models and repositories.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (6), [[GitHub]] (6), [[Git]] (4), [[Google]] (3), [[CLI]] (1)
> **Tools:** github (6), command line (2)
> **Env Vars:** lfs (4), mit (2), cli (1)
> **CLI Commands:** git (4), make (1)
> **Analogies:** for example (2), kind of like (1), similar to (1)
> **Cross-References:** we discussed (1)
> **Documentation:** the documentation (1)
> **UI Navigation:** go to (1)

#### [Introduction to the Hugging Face Hub](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-hugging-face-hub?u=76281980)

> [!transcript]- Transcript
>
> **[0:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-hugging-face-hub?u=76281980&t=8)** - [Instructor] [[Hugging Face]] Hub is really about just a few [[Microsoft Products|products]]. First, you have the ability to log in and create an account, and you can see here's my profile and I have settings enabled as well. And if we go to settings, the most important aspect of your settings beyond putting your full name in homepage, et cetera, is to actually create an access token. The access token allows you to programmatically interact with the Hugging Face Hub. So what it means is that from [[GitHub]] actions, you could push maybe artifacts into Hugging Face from a development environment, you could go through and read and write data, for example, a data set, or you could configure a Spaces application as well. So I would say this is the first part of the Hugging Face Hub is make sure you got an account and you've created an access token and save that token somewhere secure. The next thing to be aware of is the models. So if we go to models here, this is really the heart and soul of a large component of Hugging Face. At this time you can see there's 80,000 models, I'm sure this is growing by the month, and it could be into the millions at some point. And these models here are composed of multiple tasks. And you can see the tasks. We have image classification, translation, et cetera. And if we go through here, you can even pull down even higher level categories like [[Computer Vision]].
>
> **[1:42](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-hugging-face-hub?u=76281980&t=102)** These are all models that deal with computer vision. [[Natural Language Processing (NLP)|Natural language processing]], like translation or doing text generation. We also have audio. In this case, you could see things like automatic [[Speech Recognition]]. In fact, if we select automatic speech recognition here, one of the things you'll notice here is that they have a sort by the most downloads. You can see all the different downloads here for speech recognition. And you could take a look at which particular model you may want to play around with when you're dealing with models. So really the ranking here are actually good ways to actually figure out what it is you want to do when you're dealing with a particular category. And in fact, one of the more popular recent additions is OpenAI Whisper, and notice that this openai/whisper-large, you can actually use Hugging Face to actually include it and transcribe things inside of a project. So that's models. Now, if we go to data sets, the next thing to be aware of with data sets is that there are also a large amount of data sets. And the reason why these data sets are interesting is that they can be used for [[Fine Tuning]]. So this means that you can customize these pre-trained models and make them even more accurate for particular problems you're solving. So here we say fine grained tasks like language modeling, multi-class classification, et cetera.
>
> **[3:18](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-hugging-face-hub?u=76281980&t=198)** These are all useful places to use these datasets. And what's also helpful is that if you go to the dataset, you can see here that there are the structure, information about the dataset. You can even preview it and you can even select right here, this API call and just query it inside of your terminal. And finally, you can even go and train inside of your Hugging Face environment. In general here, the dataset is one of the more useful aspects of the Hugging Face platform. And you can also upload data into the dataset. The last key component here is Spaces. And what's useful about Spaces is the way to make machine learning applications. And it is quite easy to actually create one. If you just say create new space, it'll ask you for the name of the space and then what technology you want to use. Streamlit, Gradio, Static. And you can actually share this out with a particular license if you want. You can also go through and look at other models. And this is actually a great way to get familiar with certain things. If you want to take a look at, you know, some potential example of something, you can go through here and figure out, for example, the files themselves. You can actually read through, and look at the particular files that were used for something. Here's another one, [[Stable Diffusion]] demo. We can see there's an application file right here
>
> **[4:50](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-the-hugging-face-hub?u=76281980&t=290)** and they're using Gradio for their Spaces application. So really it's a place that collects models, data sets, spaces to use it programmatically. You would go in and create a profile, create a settings section where you use an API key, and we're going to get into this in a little bit.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (8), [[Speech Recognition]] (3), [[Computer Vision]] (2), [[Microsoft Products|Products]] (1), [[GitHub]] (1)
> **UI Navigation:** go to (4)
> **CLI Commands:** make (3)
> **Env Vars:** api (2)
> **Tools:** github (1), terminal (1)
> **Analogies:** for example (2)
> **Warnings:** be aware (2)
> **Definitions:** means that (1)

#### [Using Hugging Face repositories](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-repositories?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-repositories?u=76281980&t=0)** - [Instructor] Let's get started with [[Hugging Face]] repositories. So here what I have is the default page from Hugging Face. I've logged in. I don't have any models or datasets yet on my account. Let's take a look. You can see here that I have the options to create one, but I'm going to follow the documentation from Hugging Face to be able to create a repository. So I am going to go and use one of the libraries. I'm going to open up my terminal over here. I'm going to create a new virtual environment, well, I'm going to call the venv module, and I'm just going to call it venv as well. And then next I'm going to activate this as always. And now that I'm activated, I'm going to follow the instructions from Hugging Face, which tell me to install this huggingface_hub, which is what we're going to go through today. And it's going to go ahead and install some dependencies. And next I want to log in. So to log in is, I'm going to go [[Hugging Face|huggingface]]-[[CLI]] login. And then that's going to tell me that I now need a token. So I have to go through and generate a token from this URL over here. And that's fine, I'm going to go back to here, and I'm going to say, well, let's actually go ahead and copy this, and open up that tab. And I am going to go ahead and create a new token.
>
> **[1:35](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-repositories?u=76281980&t=95)** So [[Tokens]] are kind of like the ability of like have like temporary passwords for the same thing, for authenticating to the same place. I'm going to say this is for a demo, and I want to not only read, but I want to also write. So I'm going to generate the token and I'm going to have this available now. I'm not going to show it even though that is temporary, but I'm going to copy. So I'm going to, I'll be using this little icon right there, and later if I want to remove it, I can definitely go ahead and delete it. And then that's fine. So I'm going to go and copy that, and then go back to the terminal over here. I'm going to paste it over there. And then hit Return. And then it says that the token has been saved to /Users, .huggingface/token, that's perfectly fine. Authenticated through [[Git]]-credential store. But this is in the helper defined on your machine. You might have to re-authenticate when pushing to the Hugging Face Hub. Run the following command in your terminal in case you want to see this credential helper as a default. To demonstrate what I want to do, right now, I want to show you the website or the URL rather for the welcome site. So essentially what I want to do is right here, where we've already installed Hugging Face Hub. Now we've done the CLI login, and now I want to create a repository. So I'm not going to use the website interface. So what we want here is huggingface-cli repo create, and then we're going to do,
>
> **[3:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-repositories?u=76281980&t=188)** we'll figure out the repo name in a second. And then what I want to do is upload a model. I have a model already, a preexisting model, so I'm going to do that. So I'm going to go back to the terminal where I've authenticated already. I'm going to paste this. This is not exactly correct. So let's go ahead and change this. I'm going to say demo-onnx is going to be the repository I'm going to create. And the type is model. That's fine. I'm going to hit Return and see what happens. All right, you're about to create alfredodeza/demo-onnx. Proceed? Yes, I want to proceed. Let's go ahead, and perfect. So that has been created. Now let's take a look and see what's going on here. And I've opened, and now there's no model card, that's fine. There are no files and, but we could definitely upload some files. So I can do that over here. And you can see that I can definitely start pushing some of the things that I want. So let's take a look at again here at the welcome page and see what else we can do. So now let's try to add a model. So I'm going to be able to upload a model and then see how we can get that into our repositories. Now that my repository has been created, I'm going to clone it back again. So I'm going to create this repo inside of my repo demo, and that's empty. And I'm going to say, I'm going to clone this, and now I will have, I'll go to demo-onnx.
>
> **[4:44](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-repositories?u=76281980&t=284)** And now it's empty except a single file. So I'm going to add this t5 very large model that I have here, and I'm going to put it here and I'm going to add it. And then that's going to take a quite a chunk of processing there, and I'm going to push it back to Hugging Face. So I'm going to say a git push, and then I'm going to have to authenticate again. Now that is fine. I'm going to say that's my username, and then my password is going to be the one that I used before. So that's one of the problems that you may have if you're trying to authenticate over the command line tool. But I'm going to go ahead and paste my token. So now that I'm back on Hugging Face, I want to upload the model that I had before. So I'm going to click here on Add file and upload new files. If I wanted to create a new file, I would have to go through the text file versions. I mean, you can see here, that if I click there, you'll slash, then the name of your file, then the contents, and then I can commit. But I don't want that. I want to upload some files. I'm going to click here. And then I am going to drag them from my window, from my operating system right here. So I'm going to put this file over there. It's going to start uploading, and I'm going to commit directly to the main branch. And those changes are fine. And I'm going to say commit changes. So that's going to upload.
>
> **[6:16](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-repositories?u=76281980&t=376)** And this is going to take a second, because this is a very, very large model. So I can see that my operating system is reporting that I'm uploading, well, at a very, a fairly fast rate. I'm able to push it almost 60 megabytes per second. So this is going to take a second. And you can see there that it was able to go through and push 439 megabytes of these t5 encoders. So that's great. The file is stored with Git LFS, it's too big to display, of course, because it's very large. And now the next thing, if I wanted to make this available to others and make it easier for us to consume, I would have to go to the model card over here and then start uploading details about how to use this, and it's essentially a README.md markdown file that will allow me to display how this would work. So if I go, say, for example, to show you a different model, if I go here to Models, let's say for example, this one, stable-diffusion. This right here is the model card. So you see that it is just a markdown file that you can add, allow to put some documentation, some useful links. Essentially this is the README file. So there you go, that is how you would go in, and make some updates, create a repository and upload a model to this repository.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (8), [[Hugging Face|Huggingface]] (3), [[CLI]] (3), [[Git]] (3), [[Tokens]] (1)
> **CLI Commands:** git (3), make (3)
> **Env Vars:** url (2), readme (2), cli (1), lfs (1)
> **Tools:** terminal (4), command line (1)
> **Cross-References:** go back to (3)
> **Analogies:** for example (2), kind of like (1)
> **Documentation:** the documentation (1), the readme (1)
> **UI Navigation:** go to (2)

#### [Using Hugging Face Spaces](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-spaces?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-spaces?u=76281980&t=0)** - [Instructor] Let's see what [[Hugging Face]] Spaces is all about. We've already done a little bit of work with models and we've interacted with Hugging Face, the platform itself a little bit already, but right now we're going to concentrate in creating a Space. So to do that, I am going to go ahead and create a new Space. I'm going to click here on this button over here called New and I'm going to say I'm going to create a new Space, create an interactive machine learning demo. So what that is, what a Space is, it will allow us to get this machine learning demo going. So we'll see what are some of the options. Now, you can also do this on the command line tool, but I'll concentrate using the web interface. I have already my account over here, and we'll create a new Space name, we'll set up a license, and then I'll pick up Gradio. You have three different options. You have Streamlit, you have the ability of using that. I'm going to use Gradio, like I said, but you can also do a static application. So this is the interactive portion of it, and we'll see why that is important in a second. So for this one, we'll say space-demo and we'll be using a model and we'll see how that looks. And for license, MIT is fine for now. You can actually pick any license that you may want, like I said, and we're going to make this use Gradio and we're going to leave it public.
>
> **[1:33](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-spaces?u=76281980&t=93)** You can have the option to make it private, and that's fine. And so that's essentially it, like three different, well, four options, the name, the license, the Space SDK, in this case I'm going to use Gradio and then I'm going to make it public. Next I'm going to create Space. This is going to take a quick second and you can see here that this is pretty much ready to go. So let's explore a couple of the things that are needed here. First, there's always, this is a [[GitHub]] repository. Next, are you going to have to create in app.py. This is a requirement. It is kind of like the main entry point, and we're going to be using Gradio. So here you have this example that imports Gradio as gr and then it builds the interface and then it launches it. And once you're done, you can commit your changes and push them to your application. And then that's done. I think that for now we're going to, oh, and one last thing is that the requirements, if you have special requirements, you have to add them. So those will get automatically installed and it takes a few seconds. So let's, instead of like going through the command line tool and showing you all of that, I want to go ahead and I'm going to create a file. So I'm going to go to files over here and I am going to create that app.py. So you can see already, have a README and you can see that there's a little bit of stuff
>
> **[3:10](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-spaces?u=76281980&t=190)** that was added automatically. So I'm going to add a file and here I'm going to say create new file and I'm going to say app.py. And then I'm going to paste a pre-made, ready-to-go app.py that I already have. And we'll go through it. So I pasted that. Let's take a quick look at what are some of the things that I have pasted. So first of all, I am going to use transformers and I'm going to use from transformers, I'm going to use specifically, I'm going to use the pipeline, and then I'm going to create a summarizer application. Now, we haven't gone through some of the APIs needed for transformers, for Hugging Face and how this all works, but to demo the ability of interactive demo, you have to deal a little bit with this, and this is going to be enough. I mean, you don't need to understand all of the inner workings on how all of these are set up, but we'll cover those later. But this is going to be very useful to understand the Spaces and how the demo works. All right, so we have a single function called translate, and it's going to accept a single input called text. We're going to make sure that we're not getting any quotes or we're transforming, changing, replacing quotes and transforming, changing ampersands. That's fine. This is optional. I left it there because I think it's a good idea.
>
> **[4:43](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-spaces?u=76281980&t=283)** Then the result is this one line, and when I say minimum length is 180, and then we're going to return these, which is effectively the text. Next we're going to create the interface. And the interface for Gradio is going to specify the single function. You can see here that it's the name of the function app here. And then the inputs is going to be a text box. What does this mean? That when we create this, it's going to show us a text box for input. And then when I say lines is 10, that's going to be kind of like the Space that we're going to have available to input our text and then a placeholder. Then we're going to say, "Hey, enter some text to summarize." You can put here whatever you want. And then the outputs is going to be a text output. This is useful because there are outputs that are, for example, image outputs. And then finally this is going to launch that. Alright, so I'm going to commit that directly and I'm going to do that. You can press the button and then up here you can see that there's this thing called, the icon has changed to Building. So let's take a quick look at, I mean, I can't click there, it's just like an image, but I can click view log. So I'm going to click here and see what's going on. Alright, so there's a lot of things here going on. And you can see these mostly looks like a Docker log output. And that's right, this is Docker log output. And you can see here that it's using [[Python (Programming Language)|Python]] 3.8 and it's copying requirements.txt,
>
> **[6:20](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-spaces?u=76281980&t=380)** and it's creating some, some of the defaults that this application will need to run. Now that's all good except we now are getting into trouble because requirements.txt is a file that we do not have. So why is that important? Well, because if I go back to our app.py, we're importing, we're using transformers, but we're not importing it and we're using [[TensorFlow]] and we're not installing it. Sorry, like we are importing it, but we're not installing. So let's go ahead and create another file. Now you'll see that here we're building up here, but because we don't have the requirements, the application will try to start behind the scenes and it will fail. So we'll probably, if I'm not fast enough, we'll see that this becomes an error. All right, so I'm going to go to Files again, I'm going to create a new file and I'm going to call these requirements.txt. So I'm going to say requirements.txt, and then I'm going to put transformers, I'm going to use 4.21.2 and TensorFlow, 2.9.1. Those are things that I know will work with this demo. And then I'll commit them directly to the main branch. So I'm going to say create requirements.txt, and then commit new file. And then that's going to trigger the building again. So if I click logs again, you can see here
>
> **[7:54](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-spaces?u=76281980&t=474)** what I was telling you before. Application startup is telling me the module not found, module name transformer is not there. So this is an error and it's something to be aware when you can see here that we already got into trouble, so this is runtime error and that's fine. So when you have a problem with the things that you're adding to the Space, you'll get into this runtime error and red button here if you're not taking a look. So verifying once again the logs, well it's no longer outputting anything because right now it should be building again. So let's wait a second and wait until that building works and it completes so that we can check these Space running. Alright, let's take a look at the logs here. Several things have been going on and you can see here that the dynamic downloading of the models is happening and a lot of the things required for these to work effectively is going on behind the scenes. I clicked on the logs and you can see that the logs are coming from the container. There, that might be kind of small to see, but it says logs/container and it keeps scrolling down here. And you can see now that the application is starting to load. So the important thing here is that all this is happening automatically. I'm not doing anything except adding a new file.
>
> **[9:27](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-spaces?u=76281980&t=567)** And then at the very end you can see that the application is now running, it has a port, and that's perfect. So I'm going to go back to our application space-demo and I'm going to click here and let's see what happens. Alright, so I've clicked there and there's a couple of different things that are happening here. First I have the text box. Remember we added enter text to summarize, well, that's here, that's the placeholder. And then we have the output. So because I want to do summarization, I'm going to paste a lot of text in here and hopefully it will show up in the output and will interact with the model. So you have a page here called [[MLOps]] from like the definition from MLOps from Wikipedia. I'm going to select all of that and I'm going to copy, go back to our space-demo, enter the text to summarize, paste that, and then I'm going to hit submit. Now because there are different types of settings for the pricing models on the underlying compute that is processing all of these, these will take quite a long time. Actually, while this is going, I'll show you some of the settings here. So I'm going to click here, open up a new tab, and you can see that I'm using the CPU basics. So two virtual CPUs and 16 gigs of ram. So this is not going to be a very fast option for what we're trying to do, which is interact with a T5 model and do summarization. So I would recommend that if you're going
>
> **[11:04](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-spaces?u=76281980&t=664)** to do something like this, any of these will work very, very well. Well, actually even the T4 small sounds like a good deal right there at almost half a dollar an hour. But if you want super fast, any of these will work. Now, if you are making it public, of course you're going to get hammered with these instances over there. So let's take a look at space-demo. Let's see. It's still processing. By my estimation, this will take around 90 seconds. Oh. It took 70 seconds or around 70 seconds, but I was close. So there you go. That's a summarization. And we did a couple of things to make this work. We created the Gradio app, we created the requirements of text, we installed our dependencies. The Gradio app allow us to create this text input. The buttons are already there. I didn't create any of the buttons and the nice CSS and the nice interactive, and you can see here I'm getting a little bit of a garbage there, but that's fine. I think this is a very solid summarization. So this effectively allows you to create a nice interactive demo for any application that you may have. And you can see now that this is now running and you can also always access the logs. So there you go. Hopefully this is useful and you can start creating your demos.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (3), [[TensorFlow]] (2), [[MLOps]] (2), [[GitHub]] (1), [[Python (Programming Language)|Python]] (1)
> **File Paths:** app.py (5), requirements.txt (5)
> **CLI Commands:** make (5), docker (2), python (1)
> **Env Vars:** mit (1), sdk (1), readme (1), cpu (1), css (1)
> **Definitions:** is a  (4), is an  (1)
> **Versions:** python 3 (1), 4.21.2 (1), 2.9.1 (1)
> **Cross-References:** go back to (3)
> **Tools:** command line (2), github (1)


### 5. Introduction to Applied Hugging Face

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction to applied Hugging Face](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-applied-hugging-face?u=76281980)

> [!transcript]- Transcript
>
> **[0:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-applied-hugging-face?u=76281980&t=8)** - Hi, my name is Noah Gift and I'm going to introduce you to [[Hugging Face]], a really popular new platform for dealing with large data sets, pre-trained models and also demoing those [[Microsoft Products|products]]. And what's exciting about this particular course is that we're going to cover three different categories. In the first section of the course, we're going to talk about how to get data into Hugging Face, how to put models into Hugging Face and then actually pull that back down inside of development environments, like GPU-enabled [[GitHub]] [[Codespaces]]. Next, in the second section, we'll cover how to fine tune trained models, so essentially get the latest models from the top brands in the world that are doing AI and ML, take those models, for example, a summarization model, fine tune it a little bit with custom data and then take that model and put it back into your Hugging Face model repository so you can use it later in the future. We'll also talk about how you can use the GPUs inside of GitHub Codespaces or some other GPU-enabled environment to speed up that transfer learning process. Finally, in the third section, we're going to talk about Spaces, a great way to demo your portfolio of building things with machine learning, including Streamlit and also Gradio. Both of these are great platforms to build products. So let's go ahead and get started. We have a lot to cover.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (4), [[Microsoft Products|Products]] (2), [[GitHub]] (2), [[Codespaces]] (2)
> **Env Vars:** gpu (2)
> **Tools:** github (2)
> **Cross-References:** later in (1)
> **Analogies:** for example (1)
> **Speakers:** - hi (1)

#### [Using GPU-enabled Codespaces](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-gpu-enabled-codespaces?u=76281980)

> [!transcript]- Transcript
>
> **[0:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-gpu-enabled-codespaces?u=76281980&t=8)** - [Instructor] Let's dive into [[GitHub]] [[Codespaces]], which happened to be one of the best possible locations for students to use GPU-enabled workspaces. You can take a look at how this works is that you have this temporary environment that you can actually code in, and it actually has, in many cases, much more power than your laptop, and it has the ability to even create prebuilt containers. And, so, those prebuilt images launch up very, very quickly. And you also are able to use the full power of Visual Studio. So let's take a look at how I have done this in my classrooms at Duke University. I have a organization here, and this organization has an [[MLOps]]-template that I've created. So notice that I use this functionality called Public Template and if I select this icon here and go to settings, and if we scroll down here, you can see that I've made this a template repository. Now, the reason why this is so powerful is that I've also gone inside of here, and I have a dot dev container environment, which allows me to customize this development environment and it will preload whatever I put in this docker file. So I say use the dev container universal build, so use the standard GitHub container here, install some packages like FFmpeg or [[Python (Programming Language)|Python]] 3.8 or GCC.
>
> **[1:42](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-gpu-enabled-codespaces?u=76281980&t=102)** Also go through here and install my packages. So I use the mink file to do a installation, and then I also am able to actually configure the Visual Studio Code environment, as well, and enable things like the NVIDIA CUDA drivers so that when I later want to go through and use GPU features I can. So once I've got all that set up, then I can actually go through here and launch a new instance, or I can use this template to create a new repo. Let's go ahead and take a look at both. So first, if we go to code here, I could actually create a new code space, and this would be a new development environment and if I go and I customize this and I select inside of this machine type, notice there's a bunch of different options here, including very powerful 16-core machines that are great for CPU tasks, or in this case, the thing I want to use for dealing with [[Hugging Face|HuggingFace]] is I want to use a six-core GPU, six-core machine with one GPU and 112 gigabytes of RAM and 128 gigabytes of storage. So for a university setting, if I want to use pre-trained models, do inference, do training, do fine-tuning, this is really the recommended environment to set up. Now, once you've gone through that, you can launch it and it'll go ahead and create an environment.
>
> **[3:13](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-gpu-enabled-codespaces?u=76281980&t=193)** Now, I've gone through and I've used this whole template to create a new repo called HuggingFace tutorials, and that's what we're going to be covering in this course. And, in fact, I've already launched this particular environment. So, I'm going to go ahead and go into this code space here. Let's take a look at some of the things that I've done. So, one of the things that I've done is I have a virtual environment set up, and this allows me to have a specific version of Python that is going to be targeted towards a installation that I've already got set up, so I don't have to install anything. I've also got all my requirements pinned here. So, notice here that I have lots of different packages like fast API, I have Jupiter, you know, I also have, in fact, HuggingFace data here. So I have the Transformers, I have the dataset, I have the tokenizers, I have even accuracy metrics inside of here, like the rouge score are all created inside of this pinned environment. And, finally, I have a Utils directory, which allows me to test out the GPU functionality of my machine. Now, let's go ahead and take a look at this real quick. Notice I have something called Verify PI Torch. And what this does is it says, "Let's make sure that the GPU CUDA is available, basically the SDK for the NVIDIA GPU." So if I type in Python and I type in Utils and verify [[PyTorch]],
>
> **[4:49](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-gpu-enabled-codespaces?u=76281980&t=289)** anybody that uses my template can launch this and they can see, "Yes, in fact, this is working, we've got this thing enabled." The other thing that we can do to test things out is that in this Util's directory, I've uploaded a audio file that is a narration of the "Gettysburg Address". And this is a very small file here, and what I do is I use the Whisper tool, which is available from OpenAI that does automatic [[Speech Recognition]] to transcribe that file. So let's go ahead and take a look really quick at what Whisper is. So, if we take a look at Whisper, this is a open-source automatic speech recognition system. And, in fact, we can see the architecture here. They have the encoder block, the decoder block, and inside of this, it's able to go through that pipeline and give us these great transcriptions and also transcribe from one language to another. It's also available inside of HuggingFace. And we can see here that it's under Whisper large and you can actually import this directly inside of your project by just following these instructions. But we can, in fact, even more quickly just run the Whisper utility because I've already installed it on this system, and let's go ahead and do that. And if I run this, this is going to kick off a Whisper job and I can launch
>
> **[6:23](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-gpu-enabled-codespaces?u=76281980&t=383)** inside of here another environment to monitor the GPU. So, if I type in NVIDIA SMI dash L one, this will actually kick off a monitoring tool that will allow me to view the GPU usage while this is transcribing things. And, so, here we have Whisper loading a very large language model, and this language model is going to take that audio file, and it's going to convert it to a text format by using the GPU that's enabled on this platform. So, it takes a little bit of time to ramp everything up, but once it does, it will go through here and step-by-step transcribe each of the components of this particular project. Here we go. We see that the GPU is has definitely been spiking up and down. So it's actually doing some work here. Here we go, it's spiking again, and it's doing this work. And we can see then, in fact, the GPU is being summarized or is using the transcription to take that audio file and convert it into a text-based format. And here we go. It's working. All the libraries are loaded, and the next step will be to step-by-step transcribe it and notice how it directly corresponds with the the GPU load here.
>
> **[7:56](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-gpu-enabled-codespaces?u=76281980&t=476)** And once it's done, then the GPU load will go back down to zero. So, really, in a nutshell, this is one of the advantages of using this GPU-based environment is that I can work alongside of the HuggingFace ecosystem and use pre-trained models.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face|Huggingface]] (5), [[Python (Programming Language)|Python]] (3), [[GitHub]] (2), [[Speech Recognition]] (2), [[Codespaces]] (1)
> **Env Vars:** gpu (15), nvidia (3), cuda (2), gcc (1), cpu (1)
> **Prerequisites:** set up (4), install (3), configure (1)
> **Exercise Files:** template (6)
> **CLI Commands:** python (3), docker (1), make (1)
> **Tools:** github (2), visual studio (2)
> **UI Navigation:** go to (2), scroll down (1)
> **Definitions:** is a  (2)

#### [Using the Hugging Face CLI](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-cli?u=76281980)

> [!transcript]- Transcript
>
> **[0:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-cli?u=76281980&t=8)** - [Presenter] One of the more powerful ways to get involved with [[Hugging Face]] is to use the Hugging Face command line interface. In general, I'm a huge fan of using command line tools, and you can see here we have Hugging Face [[CLI]] that's installed. And if you just run it, it'll give you a help menu. One of the first things that I would recommend doing is looking at some of the tools that are sub command. So for example, if we go to Hugging Face login, one of the things that we see here is that you can actually log in by going to the [[Tokens]] directory. And inside of the tokens settings inside of Hugging Face, this will be something you can set up and then pass in and then authenticate. And now once you've done that, Hugging Face knows who you are when you're developing in a workspace like this. So what this means is if you go back here, and we go to the help menu, and we take a look at the next command, which is "whoami," it'll know who I am. There we go. It says, "noahgift." That's right, it shows that I'm a member of two Duke organizations, and that's a really useful component of the authenticated experience. If I wanted to log out, I could also go ahead and do a logout command here. And if you look at the help menu, it just tells you exactly what the command does. And then also repos, right? So we could also go through here and go to repo, and that would be another command that we could do. And there's also the integration of large files
>
> **[1:44](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-hugging-face-cli?u=76281980&t=104)** and multi-part upload. I would say one of the, the commands that I use quite a bit that's included here is the scan cache. And if we go through here and we type in "scan-cache," one of the nice things that happens with this tool is it tells you locally what is actually on your machine. And this can be invaluable when you're going to go through and take a look at things like, do I need to free up this space or some other utility? So definitely take a look at the Hugging Face CLI, make sure you have a token so you can authenticate, and then you can actually use it to interface with Hugging Face Hub.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (8), [[CLI]] (2), [[Tokens]] (2)
> **UI Navigation:** go to (3)
> **Env Vars:** cli (2)
> **Tools:** command line (2)
> **Prerequisites:** set up (1), make sure you have (1)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Speakers:** - [presenter] (1)


### 6. Using Hugging Face

[↑ Back to Table of Contents](#table-of-contents)

#### [Using the Model Hub](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-the-model-hub?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-the-model-hub?u=76281980&t=0)** - Let's do a quick overview of what the model hub is. There's more to it than we've already seen. We're using models, data sets, and spaces. So we've already covered a couple of of those things. So I think that is a good idea to kind of give one other quick overview as to what we have here with [[Hugging Face]]. So I'm going to go ahead and start with models. And within models, if I click here, you'll have a few sections here to the left and primarily tasks, library, data sets and languages. All of these are kind of like labels or categorizations that you can see to try to filter out things that you might want to be working with. Specifically, for example, if I were to go to the languages section and say, for example, I want to do something with Spanish, which might be interesting to me, can click on these and then suddenly all of the models here will start getting filters. So that's, that's a good thing. And then you can keep going on to filter out. So if I want to say, I want to do summarization, so you can see now summarization and Spanish are now selected. And you can see here that summarization only for Spanish will be kind of like what are we're seeing here. So you can see here, there's bert2bert_shared Spanish-finetuned=summarization. That sounds like a pretty interesting,
>
> **[1:36](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-the-model-hub?u=76281980&t=96)** and it filters out, like I said, kind of like the things that we've already looked at. So if I click on the X right here, then I see all of the other ones that are available for summarization. So next I'm going to click on one of these models and we've already seen a little bit of what the model card is, and the model card allows you to, I'll describe better, but this is up to you, the user, you have to create a read me that makes sense and just like on a [[GitHub]] repository. And you'll have these hosted inference API. As you know, we've already created a little bit of that, but some of these models will have the ability to interact with this. So here's summarization. So we can see, compute and see what happens. And there you go. Well, the computation diamond CPU was cached. That was why the response was almost immediate because the text doesn't change. Alright, so summarization, pretty cool there. Next you have the ability to deploy these. And for deploying you have Amazon sage makers or you can use the inference API on on your own account. Now the other thing that you can do is that you have examples. You have Amazon SageMaker, but you can also use it in transformers. And I really like these extra documentation because you have ready to use copy paste snippets that you can start doing
>
> **[3:12](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-the-model-hub?u=76281980&t=192)** for doing inference in your own code. So if you wanted to clone the repo, remember everything with Hugging Face, especially with models, will need LFS, so that needs to be enabled. And then you can copy any of these and then start playing around with that. All right, so beyond models, we have datasets. We've already covered a few of those, but let's take a look at some of the interesting task categories here for the dataset. So let's start, for example, let's say text generation. Within text generation, say for example, we wanted something with wiki text. Let's take a look at what we have here. Now, the dataset preview, we do have a dataset preview, and we have the ability to do a split for test training and validation. That is really nifty because sometimes the specific dataset that you have, you want something for testing, another one for validation. And by the way, and we'll cover these a little bit later, on the more hands-on examples, you have access through the data sets library in [[Python (Programming Language)|Python]], and select which one of these are going to be working for. Like are you going to do the test, a training, the validation, and then you can even create a subset there as well. Let's take a look at the code dataset viewer. And here you can see a little bit of what is covered. So you have tons of different snippets that you can take a look to try to see
>
> **[4:47](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-the-model-hub?u=76281980&t=287)** where the data is and how it looks. Let's take a look at training. I'm pretty sure this is going to be more thorough, but here you can see there's tons of information and you start getting a look and feel of what that is going to be. And you have all kinds of different sections here. You have, this is a raw V one. I don't know what that exactly means, but it's just a different type of subset of the dataset. And then as always, you can poke around the files and this is when you go into the files, you'll see that you have the ability to see the commits. Here, there's 11 commits, and it's just like a GitHub repository really. But you start seeing that there's a README dataset, infos. Let's take a look at the infos. You get to see the description, and this is kind of like what Hugging Face does behind the scenes to try to figure out how to present these best. And there's this Python file it seems here to try to do, let's see, we have citation, we have it uses the data sets, the data sets library over there for a nested class or a China class, do class inheritances there with Python. So all in all very good ways. You can see here some of the the languages it supports, the source data sets, what are the source data sets. That's always a good idea to take a look. And finally we have models, data sets, spaces.
>
> **[6:21](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-the-model-hub?u=76281980&t=381)** And spaces, like I said, we've already seen some of those. And you have the ability to play around with radio when I close this. But essentially you have the ability to interact with it. So you have, let's say vendi_score. The other ones, more interactive with some music even. And you can construct these very easily, like we've seen before. So again, Hugging Face, when you hear, what is the Hugging Face hub, it refers to all of these components working together for giving you a one stop shop so you can play around, demo, interact, contribute, and, and reuse a lot of these different things that are available to you from the platform.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (5), [[Python (Programming Language)|Python]] (3), [[GitHub]] (2)
> **Analogies:** kind of like (4), for example (4), just like (2)
> **Env Vars:** api (2), cpu (1), lfs (1), readme (1)
> **UI Navigation:** click on (3), go to (1)
> **CLI Commands:** python (3)
> **Definitions:** is a  (2), refers to (1)
> **Tools:** github (2)
> **Code Identifiers:** vendi_score (1)

#### [Downloading models](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/downloading-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/downloading-models?u=76281980&t=0)** - [Instructor] I have a Jupyter Notebook here. We're using [[Python (Programming Language)|Python]] 3.8, and I'm going to show you how you can, well, a couple of ways that you can download models with [[Hugging Face]]. Usually, what you want is everything dynamic. That's the true nature of Hugging Face libraries. Here, we've installed transformers and the Python library from Hugging Face and we import the pipeline. So I'm going to go ahead and get that started. It's going to take, it was very fast. And then we're going to say that we want some summarization. We are going to pass in the task that we want to complete, and we're going to specify that the model is going to be the t5. That's also the model and the tokenizer, and we want truncation and the framework is [[TensorFlow]]. So I'm going to run that. This is going to take a quick second. So what is going on here behind the scenes is that it is getting downloaded locally. Now it doesn't specify where it is. It doesn't matter because what we want to do is to be able to use the model. The pipeline allows us, it's an abstraction for the model, and it's a generic abstraction. So I can change these to say, for example, bert-cased. I think it's the r1. We can definitely change that. I mean, I think it's bert-cased. And the way to do that, by the way, is if I go here, you can see, well that's the t5-small. But if I go to Hugging Face and I look for, there we go, roberta, base-uncased. If I click that one,
>
> **[1:33](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/downloading-models?u=76281980&t=93)** then I will want to use this one, and then go back here and then paste that to the model. So that's the model ID. I am not sure if there's a tokenizer for bert-base-uncased, but we can try and see if we get into trouble. So there we go, it is actually downloading, and this is what I wanted to show you because it's downloading the tf_model.h5. You see it's 511 megabytes. So it's effectively downloading that for summarization. Now you can see that I'm getting into trouble here, but because that's necessarily the right model for summarization. So let's go back here, change this to T5-small, and run this again. But I just wanted to show you that so that you could see, because I've run these before, it didn't show the downloading. So you can see here, there's no download, there's no progress being showed at all. All right, so for summarization, I have a file here called [[MLflow]].txt. It's something that I've used before for doing a little bit of a definition of what MLflow is, and it has tons of different text in here. I'm going to go back to the Jupyter Notebook. I'm going to see how we can do summarization with this pipeline. So I'm going to run this, and it's probably going to get me a single line of the summarization using all of the defaults. Of course, this is going to take a quick second. Let's just wait it out. Okay, so that took a little bit long in my compute system, and I got that big text summarized to use.
>
> **[3:10](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/downloading-models?u=76281980&t=190)** Just this, "Cookiecutter template helps streamlining, allows the data team to focus more on implementation of the model." So, "Tool, platform that manages the ML life cycle." So that's pretty good. So that is how behind the scenes it is downloading. So we saw how just by calling it, it would download. Why would you want to do that? Because when you're building, say a container, you don't want to necessarily build a container that'll take, you know, five gigabytes if the model is tremendously big. But what you want is dynamic loading. Now what if you really wanted to have that model locally and inspected around? So, oops, sorry for scrolling super fast. I'm going to show you these. If you install the Hugging Face library, you're able to use this hf_hub_download, and you need the repo_id and then the filename. Let's take a look at what does this library need. So the repo_id will be the name of the repository. So if I go back to the browser, t5-small, this was bert-base-uncased, t5-small will be this one, right? So this is a repo_id right here, and what we want to understand is what type of file we're going to download. So from the repository, we're going to be able to download a single file. So we're going to go to files here, and any of these files are going to be fair game, are going to be okay to use to download. So I think I have, I think it's the pytorch_model one that I have,
>
> **[4:47](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/downloading-models?u=76281980&t=287)** but I can, you know, I could do the tf_model.h5. So we can change that to download this one. So tf_model.h5, I'm going to go back here. I'm going to say t5, (keyboard clacking) and let's double check here, tf_model.h5, tf_model.h5. And I'm going to run this, and it's going to take, it's downloading 240-something megabytes' worth of a file. And then the resulting operation is going to be a path. So now this means that I have a cache from Hugging Face, and you can see the models. In this case, it's t5-small and it's cached in that. So whenever I'm using tf_model like from t5-small, then Hugging Face will use the cache so that I don't have to redownload these over and over again. In fact, if I run this again, you'll see that it's immediate. It took like .1 second because it's just looking at the cache. One last way that I want to show you for downloading files from the hub is by using, I'm going to show you this piece of documentation here on the website. So I'm going to hit over to my browser. I open a new tab. What I want to show you here is the Hugging Face documentation on how to download documents, but this is using [[Git]]. So for doing this, I'm going to scroll down here, you're going to have to use two things, git lfs install.
>
> **[6:23](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/downloading-models?u=76281980&t=383)** So make sure you have LFS, the ability to do LFS, Large File System installed. And then git clone <MODEL URL>. Which one's the MODEL URL? Well, let's take a look at bert-base-uncased. The MODEL URL would be, let me see, I think it would be this one right here. So in that case, you will go in and get those files because this is effectively a repository. So if I go here to files, you can see here there's even a way to contribute. You have the ability to download all of these files using Git. So if I go back here to documentation, that's the MODEL URL. That's where you will be effectively using to do the cloning. So that's it, that's how you download models locally from Hugging Face, I showed you like a few different ways, actually three different ways. One is dynamic, one using the libraries, and the other one was, let's see, from using pipeline, which is the regular way we use the libraries from Hugging Face, Then we did the hf, Hugging Face hub download, and the r1 using Git with git lfs installed and enabled.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (11), [[Git]] (6), [[Python (Programming Language)|Python]] (2), [[MLflow]] (2), [[TensorFlow]] (1)
> **Code Identifiers:** tf_model (6), repo_id (3), hf_hub_download (1), pytorch_model (1)
> **Env Vars:** model (4), url (4), lfs (2)
> **CLI Commands:** git (6), python (2), make (1)
> **UI Navigation:** go to (2), scroll down (1)
> **Prerequisites:** install (2), make sure you have (1)
> **Cross-References:** go back to (2)
> **Tools:** jupyter (2)

#### [Working with models](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-models?u=76281980&t=8)** - [Instructor] Let's take a look at this [[Hugging Face]] model ecosystem here. There's more than just one thing you would do with Hugging Face because there are interactive workflows. First, one of the things you may want to start with, and probably most people start with, is you go to Hugging Face, look at all the models, you know, hundred thousands of them, and go ahead and use them in potentially a Spaces app that's hosted on the Hugging Face platform, right? So this is really the first place to play around. Now, one of the problems with this is that GPU is not something you're going to get by default, and so as a result of that, you may want to use a workspace, like for example [[GitHub]] workspaces, that actually have access to GPUs, which will significantly speed up your performance, right? So that's really helpful. Likewise, if you had a chameleon tool potentially that you're using, GPU could be integrated with that command line tool. Likewise, if you wanted to, you could build out a fast API or a Gradio app or something like this that would also be able to use a GPU as well. So there's lots of different ways that you could use it. In general, these are the four that we'll cover briefly. Now the other thing you can do is you can actually just download the model itself, and we'll cover that in detail as well, but one of the things to be aware of with this is that when you do download, you may want to pin a revision,
>
> **[1:42](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-models?u=76281980&t=102)** and the idea here with pinning a revision is that you're able to specifically get the same version over and over, and if it's a large model, like five gigabytes, you're not going to have to download it over and over again, because you can save it locally on your file system. The other thing to be aware of is that you can also just, you know, upload this through the web, right? You can go to the web interface and put this inside of the Hugging Face ecosystem. We're going to do it in a second. And then finally, the last thing you can do is you can fine tune potentially with the GPU, and let's say, you know, GitHub Code Spaces, and then push that model back into the Hugging Face ecosystem, right? So there's a lot of really cool things that you can do when you're playing around with the Hugging Face ecosystem. It really depends on what type of problem you're solving, and this would be, again, push back to the model repository as you make changes that are helpful, like better accuracy, that is fine tuned for the model that you're using. Okay, let's go ahead and take a look at how we would actually accomplish this. The first thing I would recommend is go to Hugging Face. Take a look all the models that are available, and pick the one that you want to use, and in particular, I think again, the interface here where you look at the most downloads or most likes, these are a good idea. The [[Google]] Pegasus is a good one to play around with here. Notice what it does is allows us to just play around with it
>
> **[3:18](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-models?u=76281980&t=198)** without having to do a bunch of stuff, and so if I go through here and I say compute, right? It goes through here and it is able to summarize all that into one sentence. Now, if I wanted to use this locally, notice here that I could actually go ahead and put that into some code and build out some kind of a solution, right? So first step is play around with it inside of the interface. Second, potentially look at the community, look at some of the things that are happening here, and potentially look at some of the code spaces that are using this, and then go from there. Now what we're going to do is we're going to go over to a GitHub code space here, and we're going to actually do something similar. I'm going to use a command line tool. In this case, Click, and I'm going to use Beautiful Soup, Wikipedia, also a newspaper library that can interface with our code. And if I go ahead and I run this and I say main --help, notice all the different things that we can do. So we can encode, we can summarize, and if I go through here and I say let's look at what kind of summarize code I've got built up, one of the things that we can do is we can actually summarize a Wikipedia page just by putting it in. So how do we do this? Well, first I use the Wikipedia library here, which is a very small amount of code. You can say it's just this, right? I just say Wikipedia page essentially, and then what I do is I put this into the Hugging Face model here,
>
> **[4:54](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-models?u=76281980&t=294)** and we can say right here, here's our Hugging Face model. We have a tokenizer that prepares the data for us, and then at the end, we can just put this into a command line tool. And in this case right here, I just created a Click command line tool called Summarize, and actually it will choose some kind of a model as a default, but we could also change it out based on whatever it is we want it to do. So the first thing I would recommend would be, let's go ahead and do that [[Python (Programming Language)|Python]] example here. So again, if we we look at this, summarize, it's going to tell us that if we say --Wikipedia page Python Programming that it should be able to go and summarize this Python programming page. So what is it going to do? Well, we can take a look real quick while it's running. It's going to go to Wikipedia, right here, the Python programming language page, and it's going to grab all of this text in this page, and it's going to create a nice little summary of what's happening on this particular page. And if we go back here, we can see it summarizing the text from that particular problem. There we go, and it created a nice summary of what it is that we were trying to solve here. And if I wanted to, because I've enabled these flags here for model, I could swap it out for lots of different models that appear right here. So this is, I think, one of the better ways to play around with the Hugging Face interface. Now the next thing I'm going to show you here
>
> **[6:28](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-models?u=76281980&t=388)** is that if you wanted to, you could also save the model locally, and how would you do this? Well, in this piece of code here, I say from transformers import pipeline, and then I say model, and I save it in particular, this is the model and this is actually the revision. So when you download it, it'll print out the revision number and you can say, I want to just save this particular revision. And once it's saved, you can actually unzip it, and in fact, keep it locally on your machine. So in this case, we see this downloaded model here, and then in order to use it, if I want to have a really fast performance, every time I load it, I can just say load model. And look, we just go here, and we say here's a text file I want you to read. And in this case, we want to actually use the pre-trained model, that's actually on disc, which is right here, and it'll actually go through and run this summarization. So I'm going to go ahead and run this, and what it's going to do is going to look at this text file, which is just the Gettysburg Address, and it's going to summarize it. So we just say Python load model. It's going to look for this text file, use the pre-trained model that's been downloaded onto the disk, and then actually do its summarization. There we go. It was actually extremely fast, because one, it's got GPU2, it's actually on the disk. So we've covered, I think, almost everything with the exception potentially of uploading a model.
>
> **[8:03](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-models?u=76281980&t=483)** Now, how would we do this? Well, a really easy way to upload the model would be to grab a second learn model. In this case, I've got one in the noahgift Python [[MLOps]] Cookbook. I have model.joblib. I'm going to go ahead and grab this and click download. Once I download, it's very easy to go over to my own interface here on my profile, and I can go down to models right here, and you can see that I could put new models into my ecosystem right here. So if I go here, I say, here's my model. Let's go ahead and upload a file. I can just take that file that I just downloaded and put this directly inside of this repository, and then now we've got a model all set up for us. And in fact, it knows that it's a joblib model, and I could tweak it out further and further. So in a nutshell, really there is a lot to be said for going through here and bringing out the different interfaces of what it is you're trying to build and pulling up exactly the the kind of ecosystem that you want to pull up, right? So do you want to use the model? Do you want to download the model? Do you want to upload the model? Do you want to do a fine tuned retraining of the model? These are all different things that are important to consider when you're dealing with Hugging Face.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (12), [[Python (Programming Language)|Python]] (6), [[GitHub]] (3), [[Google]] (1), [[MLOps]] (1)
> **CLI Commands:** python (6), make (1), unzip (1)
> **Tools:** command line (4), github (3)
> **Env Vars:** gpu (4), api (1), gpu2 (1)
> **UI Navigation:** go to (4)
> **Definitions:** is a  (3)
> **Exercise Files:** download the (2)
> **Warnings:** be aware (2)

#### [Adding datasets](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/adding-datasets?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/adding-datasets?u=76281980&t=0)** - [Instructor] Let's go through some of the steps that you will need to know in order to add some datasets. So, I have here my account, and you can see that I've already added, well actually, updated the dataset one day ago. Well, I'll show you a couple of the things that you can do in order to create your own dataset. So in [[Hugging Face]] in the webpage, in the main UI, however it is, make sure that you have an account. I've logged in and I've authenticated, so that's why my profile shows up. So, make sure that that's a thing for you, as well. And then don't head over to actually Datasets here, because that's kind of like more of the exploratory datasets that you might want to take a look and want to reuse. Now, what you want to do is click on your profile. So, I'm going to click over here and then I'm going to say I want to add a new dataset. So, how does this work? I'm going to click here and then, it is essentially, as we've seen all throughout Hugging Face, a new repository. Now, there's a distinction here. It says this is going to be a dataset repository, right? But behind the scenes it is still a [[Git]] repo, and you are going to be uploading some files. So here, we could say something like this is going to be temporary, so I'm going to say temporary-dataset and the license. Sure, why don't we do Apache? You can pick any license that you actually prefer or you feel like you're more comfortable with.
>
> **[1:35](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/adding-datasets?u=76281980&t=95)** So then, you want to click Create dataset. And of course, this can also be done through the command line tool as I've shown you before. Now, once this loads, you have a couple of different things. First, there's nothing available. So, you can see here that there's a Dataset Preview. So, these are features related to a dataset. What we're going to be doing is trying to add an actual dataset. There's a couple of things that you can do here. You can either upload a file. So, if we go here and say Files, you can see here that there's gitattributes and a basic README, so you can add a file and add a CSV file directly. Now, adding a CSV file directly, I'm going to click Upload files and then select a file to upload that. All right, so I'm going to drag a file here or click to browse. So, I'm just going to drag one and then get that uploaded and then commit. So, I'm dragging this CSV file. It's a common separator value file. It's called test.csv, and as always, this is going to require me to make a commit. So, the default message for this is fine. I'm going to commit the changes and the test.csv has been committed. Now, one thing to note is that this is just 81 kilobytes, so it's very, very small. Now, if I go to temporary-dataset and I click on on here, you'll see that now, the dataset preview is right there and you even have an API. So, let's take a look at the API. You have the ability to use curl to get that,
>
> **[3:10](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/adding-datasets?u=76281980&t=190)** actually the whole dataset, or if you want to see if there are splits, we'll take a look at what those splits are. But essentially, it gives you the ability to have different ways to access these. And of course, you can also clone this repository because behind the scenes, as I've told you before, this is a Git repository and you can use the datasets library from Hugging Face to interact with that. Now, here we have like a few columns. You can see here there's a name. It has detected the types, region, variety, rating, ratings of float64. That sounds about correct. And it is a dataset about wines and ratings and I've been able to upload that. So, that is how you add datasets to Hugging Face, and then anyone that wants to interact with this, like I said, has the ability to actually use temporary-dataset, or if you want to actually use the datasets library, you will be able to do that with the data with this name, which is my account, and then the name. So, I'm going to show you opening up a Jupyter Notebook here and then we'll see how that goes. Let me open up a VS Code with a Jupyter Notebook. So, I have this Jupyter Notebook here. I'm going to execute this again, make sure that this runs. I have the datasets library installed, and now, I am going to load that dataset that we just uploaded.
>
> **[4:43](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/adding-datasets?u=76281980&t=283)** So, the way to do that is going to be, say, for example, dataset. It's going to be datasets.load. We're going to use the load_dataset and this was alfredodeza/ what was it? Demo-dataset, right? So, let's just triple check that's true. It's actually temporary-dataset, so you have to be careful with the naming. Otherwise, this is not going to work out. So, I'm going to say temporary-dataset, and I'm going to run this. And this is going to [[Fetch]]. It's going to fetch that. It's going to cache it. You can see here it's using a cache and then it's going to download that. That's perfect. So now, we have a dataset. Let's explore what that dataset is. I'm going to click Code and let's do dataset and then execute the cell. And it's a dataset dictionary. And the first thing will show it's a test. So we can actually get that test, is this is a dictionary behind the scenes and we can say as_pandas, and I think this should work and this should return a [[Pandas (Software)|pandas]], oh. So, it is not. So let's try to poke a little bit more dataset, test_dataset, and then let's do that. And then let's poke around what this test_dataset object is. Now, we have some features
>
> **[6:16](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/adding-datasets?u=76281980&t=376)** and I'm pretty sure that, oh, it's to_pandas. It's not as_pandas. So, if we run that, it will create a data from, so there you go. That is how you add very quickly a CSV. Now, I show you one way, which is through the UI, and you can do this with different other ways, including using the [[GitHub]] repository or even using the [[CLI]].

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (4), [[Git]] (2), [[Fetch]] (2), [[Pandas (Software)|Pandas]] (1), [[GitHub]] (1)
> **CLI Commands:** make (4), git (2), apache (1), curl (1)
> **Env Vars:** csv (4), api (2), readme (1), cli (1)
> **Code Identifiers:** as_pandas (2), test_dataset (2), load_dataset (1), to_pandas (1)
> **Tools:** jupyter (3), command line (1), vs code (1), github (1)
> **UI Navigation:** click on (2), go to (1)
> **Definitions:** is a  (3)
> **File Paths:** test.csv (2)

#### [Using datasets](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-datasets?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-datasets?u=76281980&t=0)** - [Instructor] We've seen how to add a CSV file directly into [[Hugging Face]], and then start serving that with the datasets' repository and access it either with the UI, so through here, or using the datasets Hugging Face library. So one thing that you could do that is slightly different, I'm going to show you things that you can actually implement, is to add a custom files, these are optional, for CSV, tab separator values, and I think even [[JSON]], you can add them directly into the UI. But I'll show you a couple things that are extra that you can do here. Now, I've added the same wine-ratings, but with different splits. The splits allows you to have access to a subset of the same data so that you don't have to, or to make it easier for your users if they want to have different parts of the data. So here, because I've added a few of those supporting files, you can see here that now I have a split. If I click here, I have test and I have validation. So if training, will have, I'm going to scroll here, it's truncated to a hundred rows, but it has around 2 or 3,000 I believe, rows. And if I change this to say test, it will just have a couple hundred, I think it's just 200. Now doesn't have an ID. So that's kind of problematic there, but I'm telling you that that's actually the case. So that is why, that is why this is useful. And again, if we go here to list the split names
>
> **[1:35](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-datasets?u=76281980&t=95)** for this dataset, this is why, this is why this is useful, because now you can see what the splits are and you can actually get a specific split. You can see here, even the split that you will get, if you do the curl command, will be the test one. Now, if you don't want the test one and you want the one, the full one, like the train, that will be this one right here. So how do you do that? Let me show you a few files that you can add. And again, these are all optional. The first thing that you'll notice is that the train one is 13 megabytes and that will have the LFS icon in there. That means that you have to enable LFS if you're adding this on the command line tool. If I click here, it will download the file, we don't want to download the file. Again, this is the LFS. If you do it on the command line tool, that's [[Git]] lfs install, and that will install some pre hooks, pre commit hooks, and pre push hooks that will enable that LFS. It'll actually work. If you don't do that, you'll get into trouble. So don't worry, if you do that, you'll have some output that shows you right on the terminal what's going on. All right, so a few files are going on here. Let me show you first the wine_ratings.py. So this file is, there's a sample actually that you can use. And what it is is that you can get a little bit of the particulars of this dataset. I've put that this was created by myself. I created it back in, well, in 2022.
>
> **[3:09](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-datasets?u=76281980&t=189)** I add a short description, it was a homepage. That's, you know, fine, you can actually add those inside the UI. But then we start getting into particulars of these datasets. So I added a version here, and this is captured by the UI. We can also have a little bit of the features. The features here actually are what are some of the columns that we have. So you can see here, that I have name, region, and I added that rating is actually a float and everything else is a string. So then we continue to, well this is the information, and then we return this object, the DatasetInfo object, with all of those things. And now we get into the split generator. So what are the split generators? We are telling these what are the specific splits that we want to provide through the API to any user downloading this. So you can see here there's a train one, there's a validation one, and there's a test one. This is fairly, fairly common, and the template that Hugging Face offers for creating these datasets, optional files, all have these pre-made. So you don't have to remember all the syntax. And you can of course, reference these wine_ratings.py file whenever you want to get a quick refresher on how to do these. And finally you can generate examples from the dataset if you want to generate a little bit of samples on how to do this. All right, so let's go back
>
> **[4:44](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-datasets?u=76281980&t=284)** and show you why these file paths are important. If I go back to Files, we should be able to see that there is a test one, there you go, a train, and a validation.csv. Now train is the main one, is the full one, is the big CSV. But, why are these right here? Because it's allowed. When you have CSV files, Hugging Face allows you to get that in there. But this could live somewhere else, and that's why you may want to consider an optional file for loading. So a file that is not hosted, that is not in Hugging Face, we'll need to have like a loader, and the loader will point, and I'll show you. Let's go to another datasets here. So say for example, blimp, if we click on this one, there's a dataset preview, that's perfect. Let's take a look at the files. So if we look at the files, there's no data sitting there. There's a dummy here. You know, there's a little bit of a, let me see if there's some stuff in there. Yeah, so there is some dummy data. But if we take a look at blimp.py, which is the loader, let's take a look at what the URLs are. So you can see that the download URL will be on [[GitHub]]. So this loader will know how these URLs are going to go through and [[Fetch]] that data and provide it through Hugging Face,
>
> **[6:18](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-datasets?u=76281980&t=378)** which is a pretty nifty way of serving things. And you can see here that the features, these are the actual column names. So that's fine. And then we go into the generate examples. Very, very similar to what I was having. Now, the split generators, you'll see that it will use the URLs. So here, download URLs. It has a downloaded_file, downloaded_urls. You'll have a way of defining where these resources live so that you can access them. So if we go back to the wine_ratings one, now you have, we've explained what these CSV files are here for, what this loading file is here for. The README has some information, which is called a card for datasets, which is the documentation for how people can use your dataset. And I haven't been good, because I haven't added much information there. And the last thing is this dataset_info file, which is again, super optional, and you don't, it has the description. Citation is effectively something that is generated from this wine_ratings file. It has the class, and the info, and all of those nifty files. Now if you remember, this has, if I go click here, this will have these different splits and we can actually do some things there. Now you can use this in a dataset library. So if you want, this example is great, because then it has everything.
>
> **[7:50](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-datasets?u=76281980&t=470)** You can clone the repo. Remember that you have to, you have to do git lfs install, which is this one right here. That'll make sure that you have, your good to go. But if you don't want to get all of those super large files sent to your computer, then you can do that. So we're going to do this one right here. I'm going to copy that, and I'm going to go to the Jupyter Notebook and see how we can work with that. All right, so we are here in the Jupyter Notebook, and I'm going to paste what Hugging Face was suggesting we should be doing. And I'm going to run this and we're going to see, I'm going to poke around this dataset. Oh, actually it found a cached dataset, so it didn't have to download anything. So if I run this, it's again going to be super fast, because I've already downloaded that. So let's poke around this dataset object, see what it returns. And as always, it's going to return a DatasetDict. Now this dictionary will have the splits, the train, the test one, and the validation one. So you can see here, this is 200 rows, 200 rows, and the train, the main one, will have 32,000 rows, which is like the complete dataset. And you have the features, name, region, variety, ratings. So you could essentially have something customized where you only have, say for example, region and rating in a dataset that is custom. And you're advertising this through this configuration. And so say for example, we only want the test one,
>
> **[9:27](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-datasets?u=76281980&t=567)** and we want to get the test one. So test dataset, well, we're getting the auto complete, like the nifty auto complete over there. And we could say test_dataset.to_pandas, to a [[Pandas (Software)|Pandas]] DataFrame, we're going to call that. And we're going to assign this to the DF variable. So we run that, that's perfect. And now we can say df.describe and see what is going on here with this dataset. And you can see there is, in fact, 200 rows. We have some values there on the rating, and that's perfect. We have the ratings usually, go all the way from 85 usually to 100, but this dataset only has a wine that goes up all the way up until 99. So there you go. That is what you need to know when you are dealing with the specifics of a dataset on Hugging Face, with all of these different ways of enhancing a dataset. We saw something that was very simple before. Now this is a slightly like a step above that, where you are able to do and interact with different splits and define those with a custom loading file.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (8), [[Git]] (2), [[JSON]] (1), [[GitHub]] (1), [[Fetch]] (1)
> **Env Vars:** csv (5), lfs (4), json (1), api (1), url (1)
> **Code Identifiers:** wine_ratings (4), downloaded_file (1), downloaded_urls (1), dataset_info (1), test_dataset (1)
> **Tools:** command line (2), jupyter (2), terminal (1), github (1)
> **Definitions:** is a  (4), means that (1), is called (1)
> **CLI Commands:** make (2), git (2), curl (1)
> **File Paths:** wine_ratings.py (2), validation.csv (1), blimp.py (1)
> **Exercise Files:** download the (2), template (1), clone the repo (1)

#### [Working with datasets](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-datasets?u=76281980)

> [!transcript]- Transcript
>
> **[0:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-datasets?u=76281980&t=8)** - [Instructor] Here we're in the [[Hugging Face]] interface, and I'm going to talk a little bit about datasets here. Now, datasets are one of the raw materials for working with the Hugging Face hub. And in fact, one of the best ways to look at a dataset is to go to the datasets interface here and filter through things like the most likes, for example, and in fact, here's a really useful one, Wikipedia. We can take a look at this and say that the Wikipedia dataset contains cleaned up articles of all languages, and it's from the dump. So, super useful. Someone could actually get inside of here and grab this entire Wikipedia dataset here and get it from a particular date and go through here and do whatever they needed to do. For example, make instances of the dataset, get a very large file. Now, if you wanted to grab a slightly smaller version, you can take a look at Glue here, which is a very popular dataset, and if we go to the dataset viewer here, this really shows you how it's designed for machine learning, right? So that's one of the differences here, is it's really the raw material so if we go to Split, you can see I can grab the validation, I can grab the test, right? I can go through here and pick subsets of the data and I can actually get this information inside of a fine tuned job, for example. So this is previewing the data, and it's also fairly easy to write some code
>
> **[1:43](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-datasets?u=76281980&t=103)** that talks to this and pulls it into your local workspace. But what if you want to upload your own dataset? Well, fortunately it's fairly straightforward. If we go to the profile section here, notice how there's New Model, New Dataset, New Space. We'll just go to New Dataset right here. And we can call this one nba dataset. And inside of this nba dataset, I would just go ahead and say create dataset and all I would need to do now is put this dataset inside of this location. So how would I do this? I would just go to files and say add file. So this is one of the easiest ways to add things to your information here. And notice that I'm going to grab some data from a previous Kaggle project I did five years ago. I'm going to go ahead and just upload it inside of here and I'm going to say upload files. Select something that's on my desktop, see if we can find this particular dataset. Here we go, here's this nba data set. And it's very straightforward to then immediately start using it because this particular piece of data has now been uploaded and I can actually use this inside of a particular project. So once you've gone through here, you've set it up, you can go to the dataset viewer, it should be available in just a second and I also could curl this dataset and grab it into a particular project.
>
> **[3:16](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-datasets?u=76281980&t=196)** So let's go ahead and take a look at a dataset that I've already uploaded previously. If I go back to my profile here and I look at data sets here, here's one that I've uploaded the exact same dataset. And one of the things that we'll notice is that this particular dataset has the information about it, it has the API call that I could actually use if I wanted to curl it. And in fact, let's go ahead and try that. If I go here and I say copy and I want to go into this workspace that I've got set up here, I could just paste that command in and you'll see that in fact, it is able to actually do a curl, right? So it's a really useful way to interact with my data. I can also go to the dataset viewer and look at all the different stats here, as well. Now, the really useful part next, once you've got it inside of there, would be to actually go into the interface and interact with it. So what I'm going to do is I'm going to go to download data right here. And you can see that there's not much code to programmatically download the data. You just say, "from datasets import load_dataset". If you want, you could pretty-print it and then you just put the name of the organization and the name of the dataset location that you created, in this case social-power-nba. At that point, I can print out the features and then I can print out one of the examples as well. In this case, I can print out this particular record
>
> **[4:49](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-datasets?u=76281980&t=289)** so let's go ahead and do that. We'll just go ahead and say, "[[Python (Programming Language)|python]] hugging-face/download_data.py" and notice that it is able to go to Hugging Face, grab this data set, and in fact, pull this one record directly out of here and it has all of the information about this particular player, right? And if I wanted to change it or build it into a command line tool or whatever it is I need it to do, it would be pretty straightforward to actually do more to it. Now, another way that I would recommend that would be a great way to interact with datasets is to use ipython. In fact, if you go through here and you do this, this is one of the more useful ways to interact with a dataset would be to load in the dataset into a variable right here and then you could actually go through and have a little bit more of an interaction with the particular dataset. So in this case, notice that I did grab that player again, but what if I wanted to grab the next record? Well, it's pretty easy now because I interactively could play around with it and here we see this player is Anthony Davis. If I wanted to grab a third player, I could go through here and I could play around with it. So once you've got this dataset loaded here, there are a couple really good options to interact with it. One would be to use ipython, a second would be to use a command line tool library like Qlik, for example, and to programmatically play around with it.
>
> **[6:22](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/working-with-datasets?u=76281980&t=382)** Again, though, you also could just go back to this interface, look at the dataset card if you wanted to, and notice how it says this API call, you could just go through here and copy it and just run it and also get the entire bit of information as well. So there's lots of different ways to interact with a dataset on the Hugging Face hub.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (4), [[Python (Programming Language)|Python]] (1)
> **UI Navigation:** go to (10)
> **CLI Commands:** curl (3), make (1), find (1), python (1)
> **Analogies:** for example (4)
> **Code Identifiers:** load_dataset (1), download_data (1)
> **Env Vars:** api (2)
> **Cross-References:** go back to (2)
> **Tools:** command line (2)


### 7. Packaging Hugging Face

[↑ Back to Table of Contents](#table-of-contents)

#### [Hugging Face and FastAPI](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-and-fastapi?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-and-fastapi?u=76281980&t=0)** - [Instructor] Let's take a look at [[Hugging Face|huggingface]] working with FastAPI, which is a web framework from [[Python (Programming Language)|Python]] that allows you to expose an API, and in this case it's going to be, of course, a machine learning API. So we have a couple of files here in a couple of directories. Let's take a look first how huggingface and FastAPI work together. We're going to concentrate in this web app directory, and it's only a single file called main.py, and we're trying to make it super simple so that it is very straightforward. All right, so I'm going to go in into the code and let's go step by step and see what is going to be doing. First, we're going to get our imports done. We're going to be using the pipeline from Transformers, which is an abstraction, actually, that allows you to interact with various different models from Transformers. Now, that abstracts away all of the complexity from dealing with different types of models. That is going to be fine. Then from FastAPI, we're going to import the FastAPI to create our application, and then we're going to import [[HTML]] response because we are going to be using a little bit of HTML. And finally we're going to do a little bit of typing. That is why we're going to import this base model over here. All right, moving on. We have the generator, so that's going to instantiate. It's going to download dynamically the,
>
> **[1:33](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-and-fastapi?u=76281980&t=93)** in this case, the GPT2 model from huggingface, and we're going to concentrate specifically on the text generation part. So we're going to do some text generation. I'm going to try to make this a little bit bigger. There we go. So once we do that GPT2 generation, we're going to instantiate and I'm going to, I'm actually going from top to bottom, so I'm going to instantiate here the FastAPI, similar to Flask, if you're familiar with Flask. We're going to create this class so that we can define exactly what our inputs are for this HTTP API and that's going to be that the [[JSON]] body with a key of text and the value is going to be string. So that's how we define our schema that is going to be accepting for JSON body that our endpoint is going to be accepting. Then we're going to expose slash, so the root of this web application, and we're going to return an HTML response, which is this right here. Nothing too complex is really actually very bad HTML form there, but oversimplifying things to concentrating the actual example. Alright, and finally we are going to generate, we're going to generate, we're going to expose this generate endpoint, which is this one right here, and that generate endpoint is going to allow us to interact. It's going to send requests to that /generate
>
> **[3:07](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-and-fastapi?u=76281980&t=187)** on our hosted API and use the generator from line six right here and use it here for results and then return the first item that those square brackets in the zero in the middle means that we'll return the first item. So that's all we are going to need. I'm going to go very quickly through the requirements, which is the last thing I want to show you so that you can take a look at how this is going to work. So a couple of the things that we're going to use, well, we're going to use Transformers, we're going to use [[TensorFlow]] for, because Transformers will need either [[PyTorch]] or TensorFlow installed for this to work. Then we're going to be using FastAPI with that version. And then to run all of these, we're going to use uvicorn, which is the recommended server that FastAPI works with. So that's a quick overview how huggingface and FastAPI would work together to serve a machine learning model.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face|Huggingface]] (4), [[HTML]] (4), [[JSON]] (2), [[TensorFlow]] (2), [[Python (Programming Language)|Python]] (1)
> **Env Vars:** api (4), html (4), gpt2 (2), json (2), http (1)
> **CLI Commands:** make (2), python (1)
> **Definitions:** is a  (1), is an  (1)
> **File Paths:** main.py (1)
> **Analogies:** similar to (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [instructor] (1)

#### [Containerizing Hugging Face](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/containerizing-hugging-face?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/containerizing-hugging-face?u=76281980&t=0)** - [Instructor] Now that we have seen a quick overview of this [[Hugging Face|huggingface]]-ghcr, and we'll see later why that is important. Now that we've seen a little bit of the kind of, like, how huggingface would work with FastAPI, let's look at how we would containerize huggingface. So we have already the application, but now we want to see some of these files and specifically the Docker file and how is the web application going to be part of this container. So what we're going to be doing here right now is going to take a look at Docker file and then see what are the important parts when you're constructing, you're building, you're containerizing this web application. So the first thing that, and this is just 13 lines of a configuration file for Docker. It's called a Docker file. If you're familiar with Docker, then this will feel very straightforward. If you're not, it is just a configuration file so that we know how to construct this container. We're going to start from the top. We're going to use the [[Python (Programming Language)|Python]] 3.8. And why Python 3.8? Well, I'll tell you in a second when we see the requirements, because once we see the requirements, you'll understand it. As of this day, it is Python 3.8 is kind of, like, my go-to version because some of these requirements will not be able to get installed.
>
> **[1:33](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/containerizing-hugging-face?u=76281980&t=93)** Next we set the working directory to be /webapp and we go ahead and install the dependencies inside the container. Next, we copy all of the contents from the local web application into the remote or inside the container/webapp directory. Finally, and this is best practice, we're going to be using the entry point, and here the executable is uvicore, which is the command line tool, the Python server that is going to help us serve FastAPI. Finally, we're going to use this command to serve it when we start the container, it's going to be --host and then we're going to bind to any address and we're going to use main:app. All right, so let's take a look very quickly at the requirements, although we've quickly seen that before. And I'm going to click here and then go to requirements and see why that is important. Well, the important thing is that [[TensorFlow]] has some issues with other Python versions that are not Python 3.8. So I'm going for what I know works, and this is something that you as an engineer, either a data engineer or ML ops engineer or even a machine learning engineer that is dealing with dependencies have to go through, find what works and find how you can make sure that whatever you are packaging, like in this case I am, that is going to work very well.
>
> **[3:06](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/containerizing-hugging-face?u=76281980&t=186)** So that is how you would go and package something like huggingface. Notice that I just listed dependencies. And then next step, we've seen how this works with the web app and we've seen the main.py and how we're doing the inference and how we're generating essentially an interaction with the text generation, with the GPT2 model, how that comes to be a container. So those are the important files and that is how you would go ahead and configure that containerization.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (6), [[Hugging Face|Huggingface]] (4), [[TensorFlow]] (1)
> **CLI Commands:** python (6), docker (5), find (2), make (1)
> **Versions:** python 3 (4)
> **Prerequisites:** install (1), configure (1)
> **File Paths:** main.py (1)
> **Env Vars:** gpt2 (1)
> **Tools:** command line (1)
> **UI Navigation:** go to (1)

#### [Running FastAPI with Hugging Face](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/running-fastapi-with-hugging-face?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/running-fastapi-with-hugging-face?u=76281980&t=0)** - [Instructor] Now that we've seen the components of containerizing [[Hugging Face]] application using FastAPI, we need to actually go ahead and build that container. The first thing that I want to do before I build a container, I want to show you how, let's open up the Dockerfile again and close this. How is it that we're going to be running it? So effectively, we need to run this uvicorn, and then host. It is important to run it locally because otherwise, you're going to not be able to verify that after containerization is going to work. So let's make sure that this works locally. I'm going to run and create a virtual environment in solo dependencies, run uvicorn, and see if that works. And then if it works, perfect, I'll create the the Docker container. So I'm going to go back and create a terminal, Create New Terminal, and then I'm going to close this up and make this the sole window. And then I am going to run python3.8, and I'm going to specify that I want the virtual environment module, which is -m venv, and I'm going to create a venv, virtual environment. So I'm going to do these, and depending on your system, you might get something that wants to have like a dependency. And yes, we want to make sure that VSCode will use that. So it will use that interpreter. And now we're going to do, we're going to source venv/bin/activate. And now that we've activate it, we're going to go and install our dependencies. Now I know that that comes from requirements.txt,
>
> **[1:34](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/running-fastapi-with-hugging-face?u=76281980&t=94)** so I'm going to use pip. And this is going to take a second, so I'm going to go ahead and run it, and then come back when it completes. All right, so that completed. And one thing that we want to do now is to make sure that uvicorn is accessible. So I'm going to do which uvicorn, and you can see that that's running there. So now we need to go and open up our Dockerfile, take a look at our Dockerfile. So I'm going to make this window a little bit smaller. There we go. So what we want is to capture the command. So let's go ahead and change directories into webapp. And now we're going to do uvicorn --host 0.0.0.0, which means bind to anything, and then I'm going to say main:app, which is the [[Python (Programming Language)|Python]] module is main and app is the application for uvicorn. So I'm going to run this. I'll probably get a message that this is now running on port 8000, which is the default port for running uvicorn applications. So Hugging Face will get those models downloaded, and then we have that available on port 8000. So we want to open that, and then you can see that we have the self-documenting API to interact with the GPT2 model and generate some text. So I'm going to go to /docs and I'm going to say go ahead. And this is the interactive FastAPI, which is Swagger, the OpenAPI. And we have the text generation when I want to try it out. And this is probably going to take a quick second
>
> **[3:11](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/running-fastapi-with-hugging-face?u=76281980&t=191)** while it interacts with the model. So it's effectively interacting with the API. Now this is not necessarily tremendously important, but it allows you to get a little bit of interaction. So you can see here that it was able to generate what it seems like a little bit of code. So string.pair, and it basically created a function, I believe. So that's fine. We verified that it works. I'm going to go back here to the terminal. I'm going to Control + C, and now I want to show you how to actually build this container. So I'm going to make this a bigger window, and then I'm going to close this. The first thing that you want to make sure is that you have Docker installed. In this case, I do have Docker installed. And what you need to do is make sure that you are in the same path where that Docker file is. So to build this container, I'm going to say docker build, and I'm going to say, I have to tag it. So in this case, I'm going to say [[Hugging Face|huggingface]], and that's the container name. And then I'm going to use a label saying local. And I am going to use dot to signal or to tell Docker the context, the main, the root directory where the build going to happen is right there. So I'm going to run this, and this is going to take a second because it's going to be downloading several layers, and then it's going to be installing the dependencies. All right, so that went really fast. Usually, it's not this fast,
>
> **[4:44](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/running-fastapi-with-hugging-face?u=76281980&t=284)** but the reason it's this fast is because I was able to run it before. And so some of these layers were not necessary to download them again. Now we want to run it and we want to make sure that this also works as a container, not only building it. So I'm going to say docker run, and I'm going to make it interactive. And I am also going to expose the port 8000
>
> **[5:15](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/running-fastapi-with-hugging-face?u=76281980&t=315)** from the container locally to port 8000 on this machine. And then I am going to run huggingface:local. And then let's see what happens. So this is going to take a second, and it's almost like running a new application. All right, so that seems like it's available. I'm going to open that up again, and I immediately get a bad gateway. Why is that? Well, because this is still trying to load this amount again. Now you already saw that it downloaded in the beginning, now it's downloading them again. Why is it doing that? Well, because this time, it's in a container. It is no longer running locally. It's running in these containers. So you can see now that the port 8000, now that the application is up and running. So let's go back to our tab here that said bad gateway. I'm going to do a refresh, and then you can see that it's loading. And you can see now that the output here in the logs, it's a 200 OK. This is effectively interacting with Hugging Face and FastAPI through the OpenAPI through Swagger, which is what we did before. So I'm going to go ahead and interact with these. I'm going to say /docs, I'm going to run the local OpenAPI, and I'm going to try to test it out with something else that is not a string. So we can say, for example, what's the definition of [[MLOps]] is, and then we're going to say,
>
> **[6:53](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/running-fastapi-with-hugging-face?u=76281980&t=413)** I want to see if that actually works and generates something interesting. These will take a couple of seconds on my system, and we'll just wait it out until it completes. All right, so that completed and we get our curl output there, and we are going to get, "The definition of MLOps is the implementation of a simple, flexible, and portable tool to manage files of a kind known as [[Microservices]]." Well, not quite what I was looking for here, but we verified that this works both running locally and running in a container. We also saw how to build these with Docker and containerizer properly with some commands.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (3), [[Hugging Face|Huggingface]] (2), [[MLOps]] (2), [[Python (Programming Language)|Python]] (1), [[Microservices]] (1)
> **CLI Commands:** make (10), docker (8), python3 (1), pip (1), python (1)
> **Ports:** port 8000 (5)
> **Env Vars:** api (2), gpt2 (1)
> **Tools:** terminal (3)
> **File Paths:** requirements.txt (1)
> **Versions:** 0.0.0 (1)
> **Cross-References:** go back to (1)

#### [CI/CD packaging with GitHub Actions](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/ci-cd-packaging-with-github-actions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/ci-cd-packaging-with-github-actions?u=76281980&t=0)** - [Instructor] Let's go back to our [[Hugging Face|huggingface]] repository. We've already seen the containerization, how it works and FastAPI, and how all of these can be created, creating a build image. But assuming some of the YAML ops, some of the machine learning operations workflows that you'll be doing, one of the things that you want to keep in mind is that you need to automate. So if you had to publish these containers somehow, let's see if you open the Dockerfile and you go through, you go through all of these steps and then you build this container and then you push it to a registry, could be the Docker Hub, could be some other registry like a private registry, [[Microsoft Azure|Azure]] has its own container registry, Amazon as well, AWS, all of them have their own different destinations. If you had to do this manually, there's a few steps involved, right? You have to make sure that you have Docker installed, that these things are not changing, and then you're tagging properly, but you need to always think about automation. Automation is the foundational process where you'll have everything go or, as much as possible, go in one click. So I'm going to show you one way that we can accomplish this by using [[GitHub]] Actions. Actions are possible to do right here. You can see there's this button called Actions in GitHub. Essentially, you can do this anywhere, but Actions is pretty straightforward because it's already in GitHub.
>
> **[1:31](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/ci-cd-packaging-with-github-actions?u=76281980&t=91)** So what I've done here is I've create a workflow. And a workflow can be created by either creating, clicking on Actions and adding a new workflow. I'll show you right here what happens if I click New workflow. I can search any workflow, but I can say, for example, build, let's see if I can do this with container, if something happens. So there you go, Publish Docker Container. So you would select something like this one, and see you have also Deploy a container to an Azure Web App, but let's take a look at what the Publish Docker Container will look like. So you click on Configure, and the beautiful thing about this is that you will be able to have everything already pre-done for you. So, well, here's a cosign in, a few things you might want. But everything's already done for you, and all that you need to do is, well, this is a lot of sign-in stuff to make sure that you're actually publishing a Docker container. But you can, you can use GitHub Actions to pre-do everything that you need to do. And essentially you could just use this as is because you can see here that the REGISTRY is [ghcr.io](https://ghcr.io). That means that's the GitHub container registry. So you don't need to set any other account. You'll be able to authenticate very straightforward with the same credentials as the GitHub Action. All right, so why am I showing you this? Well, I'm going to click Cancel.
>
> **[3:04](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/ci-cd-packaging-with-github-actions?u=76281980&t=184)** The reason I'm showing you this is because you don't need to write this from scratch. You can certainly use this repository as an example. I'll show you the one that I created here that is very straightforward. I'm not signed in the container. It's just a very simple, very straightforward process that you can use as a foundational one to build upon. So let's go, let's go through some of the steps. Basic workflow to help you get started. You can name this whatever you want. Then the controls. Basically, I'm commenting out the push and pull_request. I'm not publishing any containers when I'm merging, but you can certainly uncomment these. And whenever you're making a change or pushing a change to, say, the main branch, you are able to, you're able to publish effectively when there's a change pushed to main. Now, a thing that, it's oddly worded or oddly phrased is this workflow_dispatch thing. It allows you to, and I'll show you in a second, to trigger, to trigger a build by, manually, by clicking a button. You have an option to do this. Next, well, you have to define jobs and then build, and where is it going to run? Well, this is going to run in [[Ubuntu]]-latest. That's fine. And then what are the steps? Well, the first steps, the first step is always to check out the repository, contents of this repository. And then this Set up Docker Buildx means that you'll get all of the tooling necessary to do a Docker build.
>
> **[4:40](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/ci-cd-packaging-with-github-actions?u=76281980&t=280)** And the next, we're going to authenticate to the GitHub container registry. You can see here that the authentication is already taken care of. These two variables are already defined. I don't need to add anything. They're already there. You don't need to change anything. So, so far, I think we are good to go here with authenticating and setting up the environment. Next, there's these little weird couple lines here. All that means is that we will make a lowercase representation of our GITHUB_REPOSITORY name. So if our repository name had uppercasing or camel casing, this will make sure that that's right, and it's all lowercase, and that it's a constraint that we must follow for publishing the container. And then, lastly, Build and push container image to registry. You can see here push is true, like if you wanted to just build it and not push, then that will be false. And then, finally, what are the tags? So remember I was talking about doing this manually, having to tag every time? Well, this would be using the github.sha which is the commit hash used for the commit the previous, this change. All right, so that is the workflow, the full automation. And you could actually essentially copy-paste this and put it in your own repository, and which is where you don't need to even change anything here because I'm not using any variables or any hard-coded variables, that is. I'm going to show you how to run this.
>
> **[6:13](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/ci-cd-packaging-with-github-actions?u=76281980&t=373)** I'm going to go to Actions, and then I am going to go to the CI. Remember that was a name. That's what this means. I'm going to click here, CI. You can see I run it like a few months ago. And that workflow_dispatch, remember we had that workflow_dispatch that is oddly named? That's what allows you to do this. I'm going to click here, Run workflow, and run workflow from main. I'm going to build that. It's going to take a second to appear over here because this is all done asynchronously. And then once it does, we'll see how that log appears. All right, so you can see that the workflow run was successfully requested. Now, it appears, it's queued. Now, it's in progress. I'm going to click here and see how that looks. All right, so this is what you would be presented with. You will see that the status is in progress, the build is going on, what are the jobs. If you have a workflow that requires many different jobs, you will see that that is going on there. And now I want to check, I want to check on the logs. I'm going to click on build, and you can see that the Set up job, Run actions/checkout. Remember we were going through some of the, sorry for the scrolling, but this happens automatically. Remember we were going through some of the processes. There are some of the steps in the workflow, checkout will be one, the other one, to set up Build, Docker Build, authenticating to the container registry,

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (8), [[Microsoft Azure|Azure]] (2), [[Hugging Face|Huggingface]] (1), [[Ubuntu]] (1)
> **CLI Commands:** docker (8), make (4), aws (1)
> **Tools:** github (8)
> **UI Navigation:** click on (2), go to (2), open the (1)
> **Code Identifiers:** workflow_dispatch (3), pull_request (1)
> **Env Vars:** yaml (1), aws (1), registry (1), github_repository (1)
> **Prerequisites:** set up (3), configure (1)
> **Definitions:** means that (2), is a  (1)


### 8. Hugging Face and Azure ML Studio

[↑ Back to Table of Contents](#table-of-contents)

#### [Hugging Face and Azure ML Studio](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-azure-ml-studio?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-azure-ml-studio?u=76281980&t=0)** - [Instructor] I'm going to be doing a quick overview of what [[Microsoft Azure|Azure]] Machine Learning Studio is. I'm here in the portal from [[Microsoft Azure]], and I'm going to create or go to the Azure Machine Learning or Azure Machine Learning Studio section here and I'm going to create a new workspace. And these are the steps that you would have to follow if you want to get a little bit involved with this service. So like always, you are always going to have to have a resource group. I'm going to do demo ml, I'm going to call it that. And the workspace name, I'm going to do the same. I usually tend to do the same for the workspace name and the resource group, especially if I want to do something like a demo. And then I'm going to review and create, that's going to go through some validation steps. And then, I'm going to finally click create. So once that's done, it's going to initialize and submit the deployment. It's going to be in progress and that's going to take a quick while. So let's come back when that deployment is complete and we can load Azure Machine Learning Studio. Alright, so the deployment is complete. So we're going to go to the resource. Going to the resource means that we will be able to launch the studio. So I've clicked on launching the studio there and that will be going pretty fast. And we are going to see a couple of interesting things regarding a couple of the different things that we are going to be doing with Hugging Faces specifically.
>
> **[1:34](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-azure-ml-studio?u=76281980&t=94)** We are going to be registering a [[Hugging Face]] data set. Next, we're going to register a Hugging Face model and finally, we'll do some inspection. So how does that work? Well, the first thing that we need to take a look is this section over here. So this section over here allows us to do various different operations. You can always go to the create new if you're adding something new, like say for example, a dataset, which I'll show you later, or a model which also show you later. But essentially, this is your main section. Now, depending on how zoomed in you are, like right now, I'm pretty zoomed in, your view might might change a little bit, but I usually try to do demos in this way. Alright, so you have this main section here. We're going to be looking at data and then we're going to be looking at models. So these two in particular, let's take a look at data. We can actually create data. We're able to create a data asset. So creating a data asset, they call it a data asset because it can be several different things. I tend to call them data sets. And the way we're going to be doing that later is by creating one. So say for example, we wanted to test over here in the description test, I'm going to go very quickly here. I'm going to select the tabular one.
>
> **[3:07](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-azure-ml-studio?u=76281980&t=187)** And then next, you are going to see that you have different types of origins for or sources for those. You can even play around with Azure open data sets and even from web files. From web files, it means that you would only need to paste a web URL and do some [[Data Validation]]. So I'm not going to go through that because we're going to do that later when we start registering a data set with or going through the actual steps of doing that with Hugging Face, but we're actually just reviewing how these set will work. And again, we're working primarily with data sets and now with models. So let's go to models here. And if I go to models, we will do something similar. We would click register and we could do from local files from our job output. And all of these actions are available also through the Azure SDK. I'm going to go here to the home. And within here, we're not going to be touching on a few of these different things like auto ML and designer notebooks. You can definitely play around with notebooks. But you have a fully dedicated environment for doing all kinds of machine learning operations. Alright, so that is a very, very quick overview of how to create an Azure Machine Learning studio workspace and how are the components of Azure Machine Learning Studio are going to work with the next steps with Hugging Face.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (8), [[Hugging Face]] (4), [[Microsoft Azure]] (1), [[Data Validation]] (1)
> **UI Navigation:** go to (5), select the (1)
> **Definitions:** means that (2), is a  (1)
> **Env Vars:** url (1), sdk (1)
> **Analogies:** for example (2)
> **Speakers:** - [instructor] (1)

#### [Registering a Hugging Face dataset on Azure](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/registering-a-hugging-face-dataset-on-azure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/registering-a-hugging-face-dataset-on-azure?u=76281980&t=0)** - [Instructor] Now, let's see how we can download a dataset from [[Hugging Face]]. For this operation, we're going to be using the data sets, or the dataset library from Hugging Face. And here you can see that I'm going to be using the wine ratings dataset from my account, and I'm going to go ahead and click this one that is very, very handy. I really like it. It has all of the examples that we need. So, we essentially need to have these from datasets, import load dataset, to get these dataset, you have to make sure that you have your PIP installing datasets. It's effectively PIP installed data sets. I already done that, so I'm going to copy that, and I'm going to use a Jupyter notebook so that I can load that and make that work. So, I'm going to head over to [[Microsoft Azure|Azure]] studio code and I already have data sets. I made sure that that's working. But I'm going to paste from datasets, import loaded dataset, and then I'm going to execute on line three and then make sure that that is loaded. All right, so that downloaded a csv data, and now I'm going to create a new cell. And let's poke around what this data set contains. So, I'm going to run this and as suspected, we're going to have three, three different types of distinct types of data. And I really want to have the full trained one. So, in order to get that, I have to say train.
>
> **[1:38](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/registering-a-hugging-face-dataset-on-azure?u=76281980&t=98)** So, I'm going to say this is going to be a, you know, the train, train data set. I'm going to say that equals to that, and I'm going to run that. That's perfect. And now if I poke a train data set, and I run these, you'll see that now I have like all of those 3,200 rows. Now, what is it that I want? Well, I want to download it locally. So I am going to, I'm going to, there's a lot of information here. I want to make it go to [[Pandas (Software)|pandas]]. So, I'm going to say df, which is stands for data frame. I'm going to send it over to pandas and see what I get. Perfect, so that works. And now I can say df, describe, yep, let's see what happens there, perfect. So 32,000 rows. That's perfect. That's exactly what I want. Now, what I'm going to be doing is I'm going to send it to a csv file. Alright, so I'm going to click code again. So, I'm going to do df and I'm going to say to exactly to csv, and I'm going to say perfect wine ratings. csv index false. That's fine, that's going to create, it's going to create the csv file over there, right? I like to do this with pandas. If there's another method that you'd like to use, that's fine as well.
>
> **[3:10](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/registering-a-hugging-face-dataset-on-azure?u=76281980&t=190)** But for this is going to be useful to just use pandas. All right, so I'm going to execute these. And you can see here that my handy wine ratings file, it's over here. And then you, we have all kinds of different things. What are we going to do? We're going to register these on Azure. So, in order to do that, we're going to head over to the web browser. And then instead of going here, I'm going to go to Azure Machinery Studio. And then I'm going to go and head over to data. I'm going to click create on create, I'm going to say wine dataset. This is a wine dataset from Hugging Face. And the type is going to be, this is going to be tabular. And I'm clicking next. And then we're going to save from, I'm going to upload it from my local, my local drive, which is fine. So, local files, then next. And then we're going to, this is fine, this is the storage. Like where, like what type of storage I want, Azure blob storage is fine. I'm going to click next. And now I'm going to say, yeah, fine. If the path already exists, and I'm going to click upload, it's going to bring up this, a small window. I'm going to select the wine ratings. I know that it, this is very small,
>
> **[4:43](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/registering-a-hugging-face-dataset-on-azure?u=76281980&t=283)** but this is essentially what we're going to do. I'm going to open that, I'm going to upload that, that was 12 megabytes. I'm going to click next. And then that's going to do some validation behind the scenes. See that validation that, well that completed. Yes, this is a format is limited comma, UTF-8. Perfect, skip rows, I don't want to skip any, column headers. All files have the same headers, that's fine. And that's pretty good. We have name region variety, rating and then, which is exactly what the data set is, if you remember, if I go back here and click out of that name, region, variety, rating and some notes. So exactly the same, nothing has changed. So I'm going to go next and now here behind the scenes, Azure has done a lot of validation on some of the column names and what type of, what type of data we're dealing here. This is more than the decimal is kind of like a, well, I was looking more for like a float, but a decimal with a dot. That sounds great. And that sounds perfect. So, it was able to correctly do that. So, you can change that. If, for example, this was like a bullion, you would change that here, that's perfect. On click next. And then finally I'm going to click create. So, it's going to create the dataset, they call it now, data assets. I still call this datasets, that's fine. And what is the thing here is that we have,
>
> **[6:20](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/registering-a-hugging-face-dataset-on-azure?u=76281980&t=380)** we have versions. So, do you see here files in dataset one and current version one? That means that every single change, every time I'm pushing a new version to this data set will get this uploaded. And this is more meaningful if you're doing this programmatically with say for example the Azure [[CLI]] or using some of the APIs where you might be doing this programmatically and changing this. But I wanted to show you using the UI so that you can get like a more, more paused, a more easy way of understanding what are some of the things that you will be seeing instead of me running several commands and making something happen. But this is programmatically accessible through the [[Python (Programming Language)|Python]] API as well as other, other SDKs, other languages that are supported. So, you could say for if I have a hundred versions here, you could say, you know, I want version 10, not version 100. And then that would also work. So there you go. That is how you do download a Hugging Face, data set locally, and then you get to upload it up to Azure Machine Learning Studio.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (7), [[Hugging Face]] (4), [[Pandas (Software)|Pandas]] (4), [[CLI]] (1), [[Python (Programming Language)|Python]] (1)
> **CLI Commands:** make (4), pip (2), python (1)
> **Env Vars:** pip (2), utf (1), cli (1), api (1)
> **Definitions:** is a  (2), stands for (1), means that (1)
> **UI Navigation:** go to (2), select the (1)
> **Analogies:** for example (2), kind of like (1)
> **Versions:** version 10 (1), version 100 (1)
> **Tools:** jupyter (1)

#### [Registering a Hugging Face model on Azure](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/registering-a-hugging-face-model-on-azure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/registering-a-hugging-face-model-on-azure?u=76281980&t=0)** - [Instructor] I have the GPT-2 model here loaded on the [[Hugging Face]] web UI. And what I'm going to be doing is looking at how to use it in Transformers. So essentially I want to find out how does the API look. So I'm going to click here and this very nice card is going to open up. I'm not going to clone it. Although for effects of what we're going to try to do right now, which is downloading first and then pushing it over to [[Microsoft Azure|Azure]] Machinery Studio, what we're going to be doing is not, I mean you can absolutely a hundred percent do a [[Git]] clone of GPT-2 and then upload whatever files you want. But we're not going to do that. We are going to do it with code. So we're going to use it with Transformers. I'm going to copy these and I actually have it already good to go. And I've pre-installed a [[PyTorch]] and Transformers already for this to work. So I'm going to open Visual Studio Code and you can see here I've already pasted that over here. So I'm going to run this code. And the model, well, I don't really need the tokenizer the model was pre downloaded before, that's why it took 3.8 seconds. If you have not run this code before, it will take a few seconds to run. All right, so now that model is downloaded in the local system somewhere. And now I'm going to do here is, I'm going to say model save pre-trained.
>
> **[1:36](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/registering-a-hugging-face-model-on-azure?u=76281980&t=96)** Yes, I want to use that and yes, I want to use GPT-2. So what do you think that will do? Let's run it and find out. I'm going to run this, take a a few seconds and now I have a GPT-2 directory and inside of there I have this config.[[JSON]] which essentially tells me lots of [[Metadata]] for the model that I just downloaded. And I have this PyTorch model bin. So this PyTorch model bin is GPT-2, that is loadable or consumable with PyTorch itself. So, alright, so now I have the bin there and I'm going to go back to the browser and then I'm going to open Azure Machinery Studio. Now remember before we're looking at the one data set, we got that uploaded. Now I'm going to go home and what we're going to do now is we are going to take a look at the model. So the model is the one that will allow us to register a model. So again, you can do this programmatically with the SDK with [[Python (Programming Language)|Python]], you can, I believe maybe the Azure [[CLI]], for sure the SDK. So if you're doing this programmatically with Python in a automated way, you can definitely do this. I'm going to click register and I'm going to save from local files. Again, model type I am going to do, I'm going to let it as unspecified type, which is fine. And I'm going to select the model file or folders.
>
> **[3:13](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/registering-a-hugging-face-model-on-azure?u=76281980&t=193)** I'm going to click here and then browse file. Alright, so that loaded. And instead of downloading the CSV like before, and I know this is kind of tiny, but here I'm going to say I want to upload the PyTorch_model.bin. So 486.75 megabytes. That is fine. I'm going to click next and then I'm going to say this is GPT-2. And then for the description, let's go here and do a little bit of this description over there. I'm going to go here, put it here. And then what version is it? Well, for this we're going to just use whatever the default is. In this case it's going to be one, I'm not going to use any tags. I'm going to click next and I'm going to click register. Okay, so that completed and it took quite a second. Remember I was uploading almost 500 megabytes worth of data and we have our model GPT-2 right here. And then we have a little bit of metadata. But why would you want to have these registered on Azure Machine Learning Studio? Well, there's various different things and various reasons why you may want to do that. First is that you have the ability to have a simple versioning scheme. And this is not tied only to anything with Hugging Face. You can do it with ONNX and all kinds of other models as well or types. But you can also deploy it like if you wanted
>
> **[4:46](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/registering-a-hugging-face-model-on-azure?u=76281980&t=286)** to do something with this model you wanted to deploy, you could deploy to, look at this, to a batch endpoint, to a real time endpoint. You could share the model you have. Right now we only have one version, but imagine having like 10 versions. We could check if explanations of fairness, we could like have a cluster and do some analysis and do all kinds of different things there. And this asset ID at the very bottom would allow us to retrieve that model if we wanted to and have some interaction. Alright, so that is how you would download a Hugging Face model using Transformers to a local directory and then upload that, register that as a version model on Azure Machine Learning Studio.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (5), [[PyTorch]] (4), [[Hugging Face]] (3), [[Metadata]] (2), [[Python (Programming Language)|Python]] (2)
> **Env Vars:** gpt (7), sdk (2), api (1), cli (1), csv (1)
> **CLI Commands:** find (2), python (2), git (1)
> **Versions:** 3.8 (1), 486.75 (1)
> **File Paths:** config.json (1)
> **Cross-References:** go back to (1)
> **Tools:** visual studio (1)
> **UI Navigation:** select the (1)

#### [Inspecting a Hugging Face dataset on Azure](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/inspecting-a-hugging-face-dataset-on-azure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/inspecting-a-hugging-face-dataset-on-azure?u=76281980&t=0)** - [Instructor] Back on the [[Microsoft Azure|Azure]] Machine Learning Studio interface we remember we already have our dataset, our wine dataset, and we want to do some exploratory analysis. So if we go to explore up here, this is going to generate a little bit of data, but like nothing really like, this is kind of like a working a little bit with [[Pandas (Software)|pandas]] and you're showing kind of like the head and it is giving you a little bit of like a preview. But the other thing that you can do is like, if you go and click profile, and for what we're trying to do here, I'm going to make this a little bit smaller so you can have a bigger picture. You can see here that get a little bit of like informational stuff that you'll have here. You have Napa Valley, the regions, Oregon, Tuscany, and you have all kinds of different information that you get from the get go without running anything specific on the dataset. Variety and ratings, you can have ratings on wines go from all the way from 85 to a little bit over 100, and you can see that there's almost nothing. There's only 88 out of the 3,200 that are in the range of 97 to 99. So, and we don't have anything, I think on 100, but you get a little bit of information and this is already very useful. So now that we have that here,
>
> **[1:35](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/inspecting-a-hugging-face-dataset-on-azure?u=76281980&t=95)** one of the things that you can do besides exploring, that you can see actually here we already talked a little bit about the rating and the notes and the region. You can see that the notes, because it's all text and string, there's nothing there. One thing that I wanted to show you is this way of consuming. So now these would be slightly different than doing it with [[Hugging Face]]. You essentially come here and that's the [[Python (Programming Language)|Python]] SDK. So there you go, some details. You can create newer versions if I don't want to essentially override, we want to make some changes. And then I would do that same process of uploading these. Now I can also do that with the SDK, but then that wouldn't matter. All right. So that is how you do some exploratory work over here with data sets and take a look at how that looks and how to work with it and what type of things we will be dealing with. And this is all coming from Hugging Face.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (2), [[Microsoft Azure|Azure]] (1), [[Pandas (Software)|Pandas]] (1), [[Python (Programming Language)|Python]] (1)
> **CLI Commands:** make (2), python (1)
> **Env Vars:** sdk (2)
> **Analogies:** kind of like (1), picture (1)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)

#### [Azure ML Python SDK](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/azureml-python-sdk?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/azureml-python-sdk?u=76281980&t=0)** - [Instructor] We've gone through a couple of the things that you can do with [[Hugging Face]] models and data sets and uploading those to [[Microsoft Azure|Azure]]. But I wanted to take a moment to describe a little bit of the Azure Machine Learning [[Python (Programming Language)|Python]] SDK. It is a great way to programmatically interact with what we've already seen within the portal and Azure Machine Learning Studio. So all of the steps that I've already covered in the UI mostly are going to have parity with the Python SDK. This allows you to do all kinds of different things, including setting up infrastructure, configuring nodes. You can see here that some of the key areas that are essential here for this SDK. I specifically like the ability to be able to deploy web services. So you can actually deploy and serve a model right away, but you can also get trigger training models, and doing that with specialized infrastructure. Now specifically, I want to use a couple of the things that we've already touched on. One is registering a model. So I'm going to search this because this is a very, very, very intense and very thorough documentation. So there's lots you can, I don't know if you can see my [[Cursor]] there, but we're not even halfway through, and there's lots to cover. So let's just start with the model and how is it that you can actually go and register?
>
> **[1:36](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/azureml-python-sdk?u=76281980&t=96)** I'm going to go a little bit towards here, and this is pretty powerful right here because once you have your model, and you can see here that you can name it, you know what the model is, kind of like the [[Forms]] that we've already filled out when we were creating that. Now that's, I think that's very powerful. Once you are able to authenticate, you can interact with the model. You can even delete it later. You can download it. So basically allows you to do, and you can run this locally as well. So you don't need to be running on the cloud as long as authentication is done. So I'm going to scroll all the way to the top so that you can see how you can authenticate on that. I'm going to go way to the top. This is stable versus experimental. There's always new features coming up, but workspace is kind of like the main thing that you need to be aware with when you're interacting with all of these models, and actually Azure in reality. So the main piece whenever you're interacting with anything in Azure and machine learning, you have to create these, it's always referenced in the docs as WS, and that stands for workspace. So you can see here workspace create. You have for the name of your workspace, remember we created a workspace specifically for Azure Machinery Studio, and then there's subscription ID, the resource group. And if it doesn't exist, it has this ability of creating the resource group for you in any case and what's the location.
>
> **[3:09](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/azureml-python-sdk?u=76281980&t=189)** And then you're able to get that going. So once that's done, you're going to be able to use that workspace in any environment really. But you can actually save that to a configuration file, and that is known as the config adjacent. You can save it like that and then reuse it whenever you want to reload it. So different ways of actually getting that authentication, getting that already done for you to select the right machine learning workspace. Now last, what I want to show you is the dataset APIs that you can do here. So I'm going to scroll almost all the way to the bottom here. And this is the model deployment. And this is exactly the part where I wanted to show you what this dataset is. So dataset is kind of like what we did before with Hugging Face datasets. You have the same ability of registering a dataset using the APIs here. You can see we're importing from AzureML.core import dataset. And then you define that this is delimited files. In this case, a dataset already exists in Azure, and you're able to use that, and it has the ability to send those to [[Pandas (Software)|Pandas]] dataframe, which is pretty useful. And you have the ability to inject or retrieve those data sets from also URL paths.
>
> **[4:46](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/azureml-python-sdk?u=76281980&t=286)** You can definitely, there's the ability to do registering as well, but that doesn't seem to be covered here. But definitely lots of other abilities to explore more what you can do with the Python SDK. And so why is this is all relevant? Because once you're in the Azure infrastructure, in the Azure Cloud, you can use these things that came from Hugging Face for your own exploring and your own training of your own models and your own deployment. And it's very useful to have it and leverage the power of a cloud service provider like Azure.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (10), [[Hugging Face]] (3), [[Python (Programming Language)|Python]] (3), [[Forms]] (1), [[Pandas (Software)|Pandas]] (1)
> **Env Vars:** sdk (4), url (1)
> **Definitions:** is a  (2), stands for (1), known as (1)
> **CLI Commands:** python (3)
> **Analogies:** kind of like (3)
> **Cross-References:** coming up (1)
> **Documentation:** the docs (1)
> **UI Navigation:** select the (1)


### 9. Hugging Face Automation

[↑ Back to Table of Contents](#table-of-contents)

#### [Using GitHub Actions for model deployments](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-github-actions-for-model-deployments?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-github-actions-for-model-deployments?u=76281980&t=0)** - Let's go through some of the things that you'll have to know and understand in order to be able to use [[GitHub]] actions for model deployments and specifically [[Hugging Face|Huggingface]]. So we're going to take a look and we're going to be working with this repository called "Huggingface Deploy [[Microsoft Azure|Azure]]." And what we're going to be doing here is go through some of the details that are required and then we'll take several different approaches to actually deploy. So the first thing that I want to show you is that this is a web application similar to what we've seen before in, in many of the examples is going to be using GPT-2 for doing some text generation live through the FastAPI that is being exposed. So there's nothing much to it. Let's take a look, a quick look at the, the dependencies and then the docker file and then we'll go through some of the more interesting parts on the GitHub action side of things. So Transformers, let's see [[TensorFlow]] FastAPI and Uvicorn so that we are able to run these. So that's, those are the requirements. Let's take a look very quickly. The Docker file, the docker files just 15 lines long and just very, very few things that are actually important. We have the installation of the requirements, we copy the web application. We're exposing port 8,000. The entry point is Uvicorn, which is in charge of launching actually making these application run.
>
> **[1:34](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-github-actions-for-model-deployments?u=76281980&t=94)** So that's, that's very good. And then we are running the host 0.0.0. So basically binding to any address and then making sure that main app is that is coming from web app is is actually being run. Alright, so those are the details. Let's take a look now at GitHub actions. For that we're going to go to the GitHub/workflows, going to look at main.yml and let's take a look at a few things that are going to be happening here. First, I'm going to make sure that the Azure container app name is called "demo-container." In this case, if you want something else you'll have to update that. The Azure group name. One thing that I tend to follow is that I tend to use the same name that way remember and I can destroy the resources when I'm done. Next. I have all of these commont out because I don't want to automatically trigger for now, every time I push to the main branch. You can actually uncommon this if you're ready to, to do this like in a production environment where anything will get triggered once the changes are made to the main branch. And what we're going to have here when we're going to be working with is a workflow_dispatch, which allows us to manually trigger. This is oddly named and it's kind of like difficult to remember, but it's something that is useful to know when you're starting to work and design your workflow for a pipeline using GitHub actions. Alright, so let's see, what are some of the things that we're going to be doing?
>
> **[3:06](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-github-actions-for-model-deployments?u=76281980&t=186)** So this is going to start with creating a docker image. Set ups, sets up docker buildx. That means that everything that is needed for building the docker image will happen there. It is going to authenticate to different registries in this case, this one's registering or authenticating to the GitHub Container Registry. That is what [ghcr.io](https://ghcr.io) means, is the GitHub Container Registry. And you don't need to add any secrets because all those are already set up for you. Then we're going to mangle the, the name and make sure that everything's lowercase. That's why it is using the shell actually to, to make sure that those are set. Index is going to build, push the con, the contents to the GitHub Container Registry using the name of the repository and the commit. Now on the deployment side of things, this is going to stay very consistent except for a couple of things that we'll see in a second. First, we're going to authenticate login to Azure and I'll show you how, how to make sure those Azure credentials are set. Next we're going to do this operation again where we're making sure that everything's lowercased in the naming conventions. And finally we're going to deploy to a container app. We're going to use the Azure [[CLI]] action and we're going to use this snippet here, this inline script that is going to allow us to do that. In this case it is authenticating to the, to ghcr. You have container registry is passing some, some secrets.
>
> **[4:41](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-github-actions-for-model-deployments?u=76281980&t=281)** You can see here that there's a secrets.pat and that is what is going to take to deploy that. Now I understand that this is a lot to take in. It is not very demonstrative, but what I wanted to do is take you through step by step, on everything that you will have to do. Some things will change here a little bit depending on the target destination that we're going to be working with. So say for example, if we're going to be using Docker, we're going to use the Azure Container Registry. While some of these details are going to be used for things. We've already seen examples on using, the GitHub Container Registry, we will be using something slightly different. And for now we were, we are going to, concentrate only in the packaging and the pushing to different container, different, different destinations, specifically Docker Hub and Azure. The Azure Container Registry we're not yet going to deploy. We'll be seeing that a little bit later. Alright, so that is a quick overview on the, the GitHub actions needed for model deployments.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (10), [[Microsoft Azure|Azure]] (9), [[Hugging Face|Huggingface]] (2), [[TensorFlow]] (1), [[CLI]] (1)
> **CLI Commands:** docker (8), make (4)
> **Tools:** github (10)
> **Definitions:** is a  (4), is called (1), means that (1)
> **Analogies:** similar to (1), kind of like (1), for example (1)
> **Env Vars:** gpt (1), cli (1)
> **File Paths:** main.yml (1)
> **Code Identifiers:** workflow_dispatch (1)

#### [Using Azure Container Registry](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-azure-container-registry?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-azure-container-registry?u=76281980&t=0)** - [Instructor] One of the different destinations that we're going to cover is using [[Microsoft Azure|Azure]] Container Registry. So in order to create an Azure container Registry, well, what we need to do is go through the portal in Azure, make sure you're authenticated, and then take a look at the registry. So we're going to open here when I look for this on the same registry, and we're going to select the container registries, and there's no container registries to display, of course, and we're going to be creating a new one. So I'm going to go ahead and create container registry and go through the steps. So in this case, I'm going to use a very specific subscription that I have here. I am going to create a new resource group, and I'm going to call it demo-container. That sounds okay. And I'm be using these for other services I'm going to be creating in Azure so that I can deploy these containers later. So the registry name is going to be demo-alfredo. That way I make sure that it is unique. In this case, resource names may contain only alphanumeric characters, so I'm going to remove the dash and make that compliance. So there we go. I don't think Canada Central is good for me. I'm going to try and pick and choose East US and hopefully that works. Availability zones is not something that I need for this. The SKU standard sounds okay. There are other premiums and basic operations that are also needed.
>
> **[1:34](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/using-azure-container-registry?u=76281980&t=94)** So this is fine for now, I'm going to review and create. So the validation pass there, I'm going to click create, and this is going to take a second. You're going to see that the deployment is initialized and now it's in progress. And this is going to take a while and we'll just wait it out. All right, so that went very, very fast. Let's go to the resource and take a look at some of these things that we will be needing here. So in this case, we are required to do some things. So let's take a look at the access keys. And the access keys, in this case, we want to actually have the ability to log in, to log into to this. And we need to make sure that whenever we're able to log in, we have the ability to have an admin user. This is a requirement to use Azure Container Registry when we're pushing images, this is the one that you'll be using. So in this case, you can see here that there's two passwords. There's a username. The username is actually demoalfredo. That's fine. And there's some passwords I'm going to be using, There's another password if you need to use two passwords And you have the ability to regenerate those as well. This is exactly what you need to log in and authenticate to an Azure Container Registry.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (6)
> **CLI Commands:** make (4)
> **UI Navigation:** select the (1), go to (1)
> **Env Vars:** sku (1)
> **Definitions:** is a  (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)

#### [Automating packaging with Azure Container Registry](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/automating-packaging-with-azure-container-registry?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/automating-packaging-with-azure-container-registry?u=76281980&t=0)** - [Instructor] Let's automate the packaging with the [[Microsoft Azure|Azure]] Container Registry that we were able to create before. In order to automate that, let's take a look again at the workflow file here, which is this YAML file, and we'll need to make some changes to these defaults that I already have. So you can see here that I have this login to [[GitHub]] Container Registry. I am going to be using the Azure Container Registry. So this will need to get updated. And then the other thing that we'll need to update is this tag so that it has the actual server from the container registry that we created. All right, so we're going to have to make those changes. And then the other thing is I'm going to have to use the username and password from the container registry. Those are going to be secrets. I'm going to have to update those. So now that we know what the things that I need to update, I'm going to go to settings and create those secrets for my repository over here. So I have secrets. I'm going to say I want secrets for actions because this is what we need. And I'm going to click new repository secret. This is going to be, I'm going to prefix everything with ACR, which is Azure Container Registry. I'm going to say username, and then I'm going to go back to the portal where I have this demoalfredo registry. And the registry name is demoalfredo. The username is demoalfredo.
>
> **[1:32](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/automating-packaging-with-azure-container-registry?u=76281980&t=92)** Yes, I want to copy that and then I'm going to add it as a secret. Remember, once you add it as a secret, you don't have access to the plain text anymore, and this will be part of a secret. You can update it and delete it, but not read it back again. So one thing to be mindful about. So I'm going to click add secret. So perfect, I have one ACR username, then I have another one, which is the ACR password. So I'm going to see ACR password, and then the password is going to be one of these two. Let me pick the first one. I'm going to copy that to the clipboard and then back to action secrets. I'm going to paste that here. I'm going to add that secret. And now I have these two secrets. So perfect. So that's good to know that this is now ready and set. And now we can go and update our workflow file. So I'm going to go to the GitHub workflows directory and open the YAM: file and go ahead and edit. I can use right here, the web interface to do this. It is going to be pretty, pretty straightforward. So these are some things that I'm not going to need right now because I'm not going to be doing a deployment. I am just going to be doing the containerization and pushing out. So if you remember, I need to make sure that the registry is updated. So let's take a look again back at the portal. And that login is basically the login server. So I'm going to copy that, go back to the GitHub actions, go to registry and make sure that this value is updated.
>
> **[3:07](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/automating-packaging-with-azure-container-registry?u=76281980&t=187)** So in this case it's demoalfredo, like if you're using something else, that will probably change. And the username is going to be, this is going to be secrets. And this is going to be ACR username. Remember we were adding those as the secrets in our repository. This is going to be secrets. And this is not going to be token, this is going to be ACR_password. Next, we're going to let take a look at the next step. So that's lowercase the repo name that stays the same. And then we're going to build and push to container to image to, let's call it to a ACR registry. Push is true. That means that you have the option to actually not use anything in there. And the tags are going to require this registry name again. So I'm going to copy that. I'm going to put it over here. I'm going to say that. So demoalfredo.[azurezurecr.io](https://azurezurecr.io) and then it's going to go to the repository environment. And the GitHub SHA is going to be the file Docker file is fine. That's exactly what I have already. You've already seen the Docker file. Now I'm going to start a commit, update main.yaml. That sounds fine. I'm going to commit the changes. All right, so changes have been committed and now what we need to do is actually, well, we're double checking that our changes are there. We have the username and password and we have here the things that we need to do to, to tell Docker where he needs to push it.
>
> **[4:40](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/automating-packaging-with-azure-container-registry?u=76281980&t=280)** Now I'm going to actually go ahead and trigger. So I'm going to go to the action tab and you can see here that there's nothing on, because there are no workflows run. But our little workflow yaml has this thing, the title over there trigger auto deployment. I want to run the workflow. This is what allows me to run it manually. And when I click run the workflow and see what happens, this is going to take a second to update. And once stat's done, we'll come back here to take a look. Oh actually there we go. That was pretty fast. But this will take, you can see was queued. Now it's in progress. This will take a second because it needs to build. Let's take a look at some of the logs and you can see it's logging and authenticating and it's building the image. And then we'll push it. Once it's done, we'll come back and see what the result is. Alright, so that completed and it took about a little bit over three minutes to complete. And we were able to, well be mindful that everything that you name in the step will be used here in the logs. This is not post logging into GitHub Container Registry. This is ACR. And you can see here that the ACR registry made change that we made that was being captured. Alright, so that's great. We've been able to post to the Azure Container Registry. That means that if I go here, back to this over and I do an overview, I can say activity log, I will be able to see that there's some operations. There we go. You can see that logging credentials was used, some operations were created.
>
> **[6:17](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/automating-packaging-with-azure-container-registry?u=76281980&t=377)** Well, this is actually happening with the subscription when we were creating this and were creating or updating the container registry. So that's pretty good. Let's take a look at some of the things that we have here. We have repositories and we have the alfredodeza/[[Hugging Face|huggingface]] azure acr that actually came from using that automation. We can have here, we can see the tags last modified like, well just a few minutes ago. Tag count, manifest count one. And this is actually coming from, this repository name is coming from the actual tag that we use. So there you go. That is how you use this automation to push containers to the Azure Container Registry.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (6), [[GitHub]] (5), [[Hugging Face|Huggingface]] (1)
> **Env Vars:** acr (8), yaml (1), yam (1), sha (1)
> **CLI Commands:** make (4), docker (3)
> **UI Navigation:** go to (5), open the (1)
> **Tools:** github (5)
> **Cross-References:** go back to (2)
> **Definitions:** means that (2)
> **File Paths:** main.yaml (1)

#### [Automating packaging Docker Hub](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/automating-packaging-dockerhub?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/automating-packaging-dockerhub?u=76281980&t=0)** - [Instructor] Let's take a look at how we can package our machine learning model with [[GitHub]] Actions and push it to Docker Hub. So I have my repository here, and in here, we're going to take a look at the workflows that are going to be configured for what we want to try to do. Let's see all of the components that are actually needed to make that happen. So I'm going to go into the workflows directory under .github, and I'm going to open this YAML file. So this is very, very straight forward. What we need to do is just define when this is going to get triggered. In this case, anytime that I'm making a push to the main branch. And you can configure this in any way you want, we're not going to dive into the details as to how all of these different options work on GitHub Actions. But we are also going to add the workflow dispatch so that we can trigger these manually, and you'll see these manual triggering later on in a few minutes. Alright, so moving on. We're going to bring down the project as well, we're going to check out all of the components, all of the code, all of the files. And we're going to use the docker/login-action, at the version one. So this means that we'll use these provided by Docker and it will allow us to essentially authenticate to Docker. It requires using the username and password,
>
> **[1:35](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/automating-packaging-dockerhub?u=76281980&t=95)** which I'll show you in a second how to do this. Essentially, this is username and your access token. Once that is done and you've successfully authenticated to Docker Hub, you will be able to push the container to Docker Hub. In this case, I want to actually build and push. So I'll set the context, I'll say that the Docker file is the one at the root level, and "push: true" means that yes, in fact don't only build it but also push it once you are done. And the tag is going to be [[Hugging Face|huggingface]]-fastapi: latest. Great. So now, what are these things and how do I get them? Well, we go to hub.[docker.com](https://docker.com), I have that here. If you are logged in, you will see your repositories. I can actually log out and so you can see this as well. This is what you would get if you go to hub.[docker.com](https://docker.com), then you would go and sign in as I was doing before and you would be able to see all of your repositories again. Alright, so how do you get those [[Tokens]]? You will go to settings, Account Settings right here. And then you will go to Security, and then you will see that your Access Tokens are right here. So some of these are auto generated, some of these are manual. You would go to New Access Token right there, click on that, and create a token description. So you'll be giving a description, so say for example,
>
> **[3:10](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/automating-packaging-dockerhub?u=76281980&t=190)** "GitHub Actions Packaging" for "ML," and then you would generate access permissions: read, write, and delete. Sounds okay to me for the type of things that we want to do because we want to actually push that. And then, you will get something like this. This will allow you to paste this inside your GitHub Actions. So you need to grab that and make sure that that's available. So now you have these, you have your username, you have your token. What you need to do is go back to your GitHub repository and you're going to go to Settings. And then in Settings, we're going to scroll down to "Secrets and variables," and you're going to select Actions. So I'm going to click on Actions, and you're going to add them right here with a "New repository secret." You can see that I've already added DOCKER_HUB_ACCESS_TOKEN and DOCKER_HUB_USERNAME. So make sure that whatever you're adding here is exactly the same as the ones that we were seeing in the workflow. So again, if we go very quickly to check the code and we go to .github/workflows and the docker-image, those need to match these ones right here. So I know they match and I know they're already set and correctly done, so I'm going to go ahead and trigger that. How do I trigger that? Go to Actions. I select Docker Image CI, which is the one workflow that I have, I click on that.
>
> **[4:45](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/automating-packaging-dockerhub?u=76281980&t=285)** And then because I have workflow_dispatch, I'm going to click on "Run workflow." I'm going to click on "Run Workflow," and then that is going to take a second, and then it's going to show up right here once it's ready. And there we go, it shows up. Now, I'm going to click on this one, which I had just run a few minutes ago, and you can see it finished under three minutes, pretty fast. In two minutes, 18 seconds, it was able to push no problem. So if I go back to Docker Hub and exit out of here, and I go back to where I was before, you will see that huggingface-fastapi is right there. This is using the tag that I set on the GitHub Action, and you can see that the latest appears right here, coming up seven minutes ago. So once the run completes, I'll get an updated version here. So that is very quickly how we can set up and push to Docker Hub if you have an account, and do it with your [[Hugging Face]] models.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (8), [[Hugging Face|Huggingface]] (2), [[Tokens]] (2), [[Hugging Face]] (1)
> **CLI Commands:** docker (13), make (3)
> **UI Navigation:** go to (8), click on (6), scroll down (1)
> **Tools:** github (8)
> **Cross-References:** go back to (3), coming up (1)
> **Env Vars:** yaml (1), docker_hub_access_token (1), docker_hub_username (1)
> **URLs:** [docker.com](https://docker.com) (2)
> **Definitions:** means that (2)


### 10. Hugging Face with Azure Containers

[↑ Back to Table of Contents](#table-of-contents)

#### [Create an Azure container application](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/create-an-azure-container-application?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/create-an-azure-container-application?u=76281980&t=0)** - [Instructor] Now, what are [[Microsoft Azure|Azure]] Container Apps? It's the ability to have serverless infrastructure for your deployments. It allows you to deploy an actual container and not think about anything else. And this is a very powerful way to deploy [[Hugging Face]] models, and in fact actually, pretty much anything machine learning models wise. And you will see that you have instance scale and the ability to select different things when you're creating one. So I'm going to go ahead and going to create a Container Application, which is the first step in order to try to deploy. And then, we'll go and try to go through the steps that will be needed in order to have deployment. So let's take a look at the process here when I click "Create container app." When I click this, this is going to load the subscription, that's fine. The resource group, we've been using one that is demo-container, and that will be great, that's exactly what we need. The container app name is pretty important because if we choose something here, whatever name we pick will be used for our automation later. And actually, I want to show you here kind of like, why is that important? Because here, we will need to update these values. So the Azure container app name is demo-container, the Azure group name is also named the same.
>
> **[1:35](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/create-an-azure-container-application?u=76281980&t=95)** These are options, and these are variables actually, so these values can be whatever you're using. So demo-container is fine. So if I go back here, demo-container is correct, and container app name will have to be something unique. So if I was doing only demo, let's see, well that still works and that's fine. But demo-container sounds fine to me. I don't understand why this is suggesting Canada Central, you know, that's fine. I want East US. And we will be doing "Review and create." And then this looks correct to me. And now, let's see, "Accepting traction from anywhere," on Port 8,000. So these are some of the settings that we'll be using. And this looks okay, so I'm going to go ahead... Let's actually take a quick look at some of the things that we have here. Alright, so we have, well, this quickstart image. Let's take a look at the quickstart image, "Simple hello world container," that sounds fine. This all looks okay, and the target port is 80, we'll be able to change those later. Alright, so I'm going to go click Create, and then wait a second until it completes. Alright, so this completed. So let's go to the resource here, I'm going to click going to the resource. This has loaded, this is fine.
>
> **[3:10](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/create-an-azure-container-application?u=76281980&t=190)** Let's take a look at some of the different things that we have here. So we have an Application URL. Actually, if I go there, we'll have a, "Welcome to Azure Container Apps," which is the basic example container that's going to be used. And we are going to be using some of those. Now, you have the ability to pass in secrets, set up continuous deployment, and manage your app through revisions. So that looks pretty good. We have monitoring as well, so if there are any problems, we can take a look there. And then, we can see what Ingress looks like. And here, we'll have the ability to map some of the ports. Now remember, if I go here to my tab, remember that if I'm using Hugging Face, FastAPI, the Uvicorn setting, if I take a look at the Docker file over here, we'll be using port 8,000. So we'll need to make sure that those are updated. But this effectively allows us to have everything ready. So I'm going to make that tiny change, I'm going to make sure that this is port 8,000 on the target port, and the Ingress type, HTTP for now. "Accepting traffic from anywhere," that looks okay. And I'm going to click Save, and then that's it. This is going to update the Ingress, and then that is going to be mapped when we do an actual deployment to Azure.
>
> **[4:45](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/create-an-azure-container-application?u=76281980&t=285)** Alright, update Ingress has been completed. And there you go, this is how you create a Container App. And then, we'll go and proceed to see how we can automate and push something so that this is working, serving our Hugging Face with FastAPI model and application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (5), [[Hugging Face]] (3)
> **CLI Commands:** make (3), docker (1)
> **Ports:** port 8 (3)
> **Env Vars:** url (1), http (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)
> **Analogies:** kind of like (1)
> **Prerequisites:** set up (1)

#### [Configure an Azure container application](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/configure-azure-container-application?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/configure-azure-container-application?u=76281980&t=0)** - [Instructor] After creating your container out, there are a couple of things that you will need to make sure that are set. First is the ability to ensure and triple check, and we've already gone through this, but triple check that the ingress, let's take a look at this is, well, first of all, that it is of course enabled, that ingress traffic is accepting traffic from anywhere. You can also say, limited to container apps environments. So anything behind the firewall on [[Microsoft Azure|Azure]]. Ingress type http is fine for now. Remember, we don't have SSL enabled and I'm going to scroll through here and transport this auto. That's fine. And insecure connections are, we can be allow or disallow, but specifically, we've already changed this to port 8000. Make sure that this is set and map correctly to the exact port that you're going to be using. I know UB Corn uses port 8000, so that's why I went ahead and did it and made that change before. The other thing is that for the type of model, for the type of service that we want, we have a thing here in our application that we'll need to make sure it is changed. That is in the containers side of things here in application. So let's take a quick look and see what happens there. All right, so I've clicked on containers and the container here is, well, I basically can choose whatever I want.
>
> **[1:33](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/configure-azure-container-application?u=76281980&t=93)** But in here, you can see some of the simple hello work container, where is it coming from, what's the name. But in this case, it's coming from Azure Container Registry, Azure Docs, that's fine. And then we can go here and see that in this case, the container resource allocation has a quarter of a CPU core and only a half a gig of RAM. That is tiny, that is barely, barely enough to get this container up and running. We will need to make some changes here so that when we're deploying [[Hugging Face]] and the fast API application, we will be able to make changes here and actually be able to load. So you have to go through the edit and deploy. And we're not going to deploy right now. But we're going to make some edits here on the performance on these containers. So I'm going to click on this simple hello world one, and I'm going to go and say I want to, these are some of the basics and here I'm going to scroll and I want to say that this is going to need, let me see, the maximum is two cores. If I do three, it won't let me. And half a gig is not enough, so I'm going to say four gigs. I'm going to save that. And then you can take a look at scale. And this is the scale rule setting.
>
> **[3:06](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/configure-azure-container-application?u=76281980&t=186)** This is great because it can go through minimum of zero, a maximum of 10. It can be more, it can be 30, I think zero to 10 is fine and that sounds pretty good. And then you go to create. Now, we don't need to change this right this second, but whenever we are, this is to create and deploy a new revision. But when we're done pushing our image, we'll need to come here and change this and I'll show you how that works. But these are things that we'll need to do anyways, and you need to be aware where these options are. Next time when we're deploying, I'll go and change these very fast and not go into deep explanation. All right, so we've gone through a couple of the things here that you will need to know on containers, on what the scale is, and how that looks. And you can see here that you can make that change regardless on the scale rules. And you can make those change there as well. So it actually takes you there. It's kind of confusing because it takes you there to the same thing. Now, if you want to set up authentication, if you have some models that you don't want to have completely open in the wild, then you would add that here. We're not going to set that up right now. Finally, you have continuous deployment which allows you to sign in with [[GitHub]] and update GitHub for you so that it sets up the options that you want. So here you can see here the demoalfredo
>
> **[4:38](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/configure-azure-container-application?u=76281980&t=278)** is already setting the registry in the images demo container. We're not going to be doing that because we already have everything ready to deploy. So we're going to skip this for now and then head over to do the automation on the next lesson.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (3), [[GitHub]] (2), [[Hugging Face]] (1)
> **CLI Commands:** make (8)
> **Env Vars:** ssl (1), cpu (1), ram (1), api (1)
> **Ports:** port 8000 (2)
> **Tools:** github (2)
> **UI Navigation:** click on (1), go to (1)
> **Warnings:** be aware (1)
> **Prerequisites:** set up (1)

#### [Deploy Hugging Face to Azure](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploy-hugging-face-to-azure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploy-hugging-face-to-azure?u=76281980&t=0)** - [Instructor] Now we're ready to deploy our [[Hugging Face]] application, our Hugging Face containerized application to [[Microsoft Azure|Azure]]. We have several things in place, but there are a couple more things that we're going to need to complete this full automation. I'm going to scroll here down on this repository. We're not going to necessarily need a personal access token from [[GitHub]] because we are not going to be using GitHub Container Registry. This is something that you would use if you were using a container registry on GitHub. We are going to be using Azure Container Registry itself, so don't pay attention to the generate a PAT. We are going to need an Azure Service Principle. And then what we're going to be doing then, once we capture that output of running this command, is we're going to be able to add it as a secret with Azure credentials. So what you need to do is run this command, and you need to be very aware of the Azure subscription ID. It is something that you'll have to be able to follow it and find it like actually with these links. But it is something that is everywhere. You go on the portal, the Azure subscription ID will be there. So you run that command and you capture that output and it'll be [[JSON]]. So let's take this step by step. I'm going to scroll all the way up here, go to settings, then go to Secrets, once again.
>
> **[1:33](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploy-hugging-face-to-azure?u=76281980&t=93)** I'm going to click Secrets, then going to click actions, and then here we're going to have these three ones. We've already seen the Azure Container Registry password, the username, but now we are going to add the Azure_Credentials, and that is so that we can authenticate to Azure and tell Azure, hey, we have a new image, it's been built. You need to publish it and get that deployment updated. All right, so once you have that, we're going to go back to the actions so that we can see what are some of the things that we need to change. So GitHub workflows, and I made a couple of changes already. I'm going to go to the main.yaml, the only workflow, the only yaml file, and then I'm going to scroll all the way down. And I should have been able to update these, but I didn't, and I hard coded them. This is just something to keep in mind. I forgot to update those over there. But let's walk through kind of like the process here that we're going to be following to make this happen. So first, as you know, and we've already done a little bit of this, we're going to set up the Docker build. We're going to log into the actually Azure Container Registry. You can see here that we tried before using the GitHub Container Registry, but it uses the same action. So I forgot to mention here that we're going to be using Azure, that's the name, that doesn't matter. You can say anything, it's just a string to identify what's happening here. This is the important part.
>
> **[3:05](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploy-hugging-face-to-azure?u=76281980&t=185)** The registry is [demoalfredoazurecr.io](https://demoalfredoazurecr.io), Azure Container Registry IO. That's the address we used before, the ACR_username and the password, which was generated when we created that. We're going to make sure that the name is lowercase and then we're going to build. We're going to build and push container to the ACR registry. Now you can see here that the tags is very important, because I'm using demoalfredo.[azurecr.io](https://azurecr.io), which is exactly where we need to push. Otherwise this wouldn't be able to understand. If you're missing this part, which is something that is pretty crucial, what will happen is Docker behind the scenes would assume that you're pushing to the Docker hub. And in this case, we're not pushing to the Docker hub, we're pushing to the Azure Container Registry. All right, so once all of that building is done, it's going to push that to the container registry. Then we're going to authenticate to Azure again. And this is where the service principle credentials.json output that is going to happen once you run that command, it's captured here at AZURE_CREDENTIALS. That is how you're going to authenticate. And then next we're going to finally deploy to the container app. Now I'm going to make this slightly bigger. Let me see if I can do this. I'm going to make this a little bit bigger. Actually, let me go and do 175% because I want to cover some of these things
>
> **[4:38](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploy-hugging-face-to-azure?u=76281980&t=278)** that we want to run here. So we're going to use the the Azure [[CLI]] action, and that allows us to call some Azure CLI commands using GitHub action. So first we're going to say that we are going to install the extensions automatically without a prompt. That allows us to ensure that the container app extension is going to be installed, regardless if it's available or not. Then we're going to say on that first line, I'm going to keep scrolling here, on that first line, we're going to say that demo-container is the name of our container. That's also the group of the container. And we're going to say that the server is demoalfredo.[azurecr.io](https://azurecr.io), which does a container registry. Then we have to pass in the username and password. So we're setting those secrets, those authentication things that Azure will need to know. So this is effectively setting that on the Azure side of things. Then we're going to update so that we are using two CPUs and four gigs of RAM, which is, we're basically maxing out the container, what Azure allows us for container applications. And then we're going to do one last update, and I hope you can see that here, which is going to be that we're saying what the image is and what the SHA specifically is. And in this case I'm using demoalfredo.[azurecr.io/alfredodeza](https://azurecr.io/alfredodeza) which, if you remember, that's a registry,
>
> **[6:14](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploy-hugging-face-to-azure?u=76281980&t=374)** that's a repo actually, and the [[Hugging Face|huggingface]]-azure-acr is the repository that I used to build this. And I actually named my container the same. And finally the GitHub SHA, which is the commit where all of this action happened. Okay, so those are all of the steps. Because I want to try to make this go a little bit faster, I already went ahead and triggered these. So I'm going to go to actions, and you can see I was able to trigger the auto deployments. So basically you go here and you run the workflow manually, and then that's fine. So let's take a look at this last trigger auto deployment. And you can see here that the build took four minutes and the deployment took a minute and 45 seconds. Let's take a quick look at the build, which we've already seen before. It sets up the job, sets Docker Buildx, which I've already explained that that's the environment for being able to build with Docker. And then it builds the container and pushes it to the container registry. So once that's done, we go onto the deployment, which is something that you haven't seen yet, which we've already seen before on the yaml, but not actually as an actual deployment, as an actual GitHub action item, a GitHub workflow that has run. We authenticated to Azure login and that worked perfectly. And then we deploy to the container app, and inside the container app we have all kinds of different outputs. You can see here that the identity is already protected because we're using secrets.
>
> **[7:47](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploy-hugging-face-to-azure?u=76281980&t=467)** And once that's done, it completes the job. So what does completing the job mean? Well, a good thing that I want to show you here is I'm going to open up a new tab, I'm going to scroll all the way to the bottom so that you can see a couple of things that you need to make sure. Make sure that you have enough RAM, well, some of these things are going to be covered next, but specifically I wanted to tell you how to tell the logs. And it's this specific command that you want to run. And I want to show you my terminal as I was deploying these. Now this is my terminal, and you can see here that the AZ container apps, logs, show name, demo container resource group, demo container follow, that's the command that I need to do. I've already authenticated on the command line and this is what we will be looking at in order to see what's going on. Now I ran it and this is effectively going and showing me the output. Now we are seeing all of these funky characters here because that's color coded, but whenever I'm running that container on the command line, it will go ahead and download, remember Hugging Face downloads models dynamically. So this is downloading the model and we're seeing the [[Microsoft Products|products]] here. 30%, 40%, 50%, 60%. I'm going to scroll super fast here so I can go all the way to the bottom. And then once that's done, then Uvicorn is telling us that it starts, the process is waiting for application startup, and then it starts. And remember it's mapped to port 8000, and you need to make sure that that port is mapped
>
> **[9:21](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploy-hugging-face-to-azure?u=76281980&t=561)** on the ingress on the container app. All right, so once it completes, well, I'm pretty sure this is already running. I can see that there's not anymore logs. And you can see here that there's some 200s. That's me trying out the application. So I'm going to change over to the browser and see how that looks. All right, on the browser I want to show you the Azure container application and some of the things that you should be looking at when this is completed. First, it might sound suspicious that this is still the simple-hello-work-container. Well, I didn't update that. What I did is I overwrote its values. You can see that the image source, it's coming from the Azure Container Registry, but the registry has changed. This is demoalfredo.[azurecr.io](https://azurecr.io), which is exactly what we created before, and now the image is no longer the default one, it's now huggingface-azure-acr and the image tag, well that's the latest SHA that we created over there. And then the container resource allocation is now updated to use two CPU cores and a memory size of four gigabytes. All right, so those are two things that I wanted to show you there. But one thing that I wanted to show you additionally is that this is now deployed in demo-container. And you can see here there's a SHA identifier, eastus.[azurecontainerapps.io](https://azurecontainerapps.io). That's effectively the application running in the cloud. So now let's try it out.
>
> **[10:54](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploy-hugging-face-to-azure?u=76281980&t=654)** I'm going to go to /docs and that's going to load the Swagger, the fast API Swagger implementation of openAPI. All right, so this loaded and we can play around a little bit with the openAPI. We're going to effectively try it out. We've already seen how to do this and we're going to say cloud deployments, I'm going to give it a prompt, cloud deployments are, and then going to execute this so that we can do some text generation. It's going to execute, it's going to talk to that model. All right, that took a second. We get the cURL command and then we get the request URL and then generated text. "These cloud deployments are expected to come from our key players; customers in their respective field, developers, data, and customers with a large user base." All right, so that seems pretty okay. And this is effectively how you automate specifically with GitHub actions deploying Hugging Face to Azure using the Azure Container Registry. You can use any registry you want, but you'll have to remember to update the required settings in order to properly authenticate and pull from those sources of truth, those sources of where your container will be living.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (26), [[GitHub]] (10), [[Hugging Face]] (4), [[JSON]] (2), [[CLI]] (2)
> **CLI Commands:** make (8), docker (6), find (1), az (1), curl (1)
> **Env Vars:** sha (4), cli (2), ram (2), pat (1), json (1)
> **Tools:** github (10), terminal (2), command line (2)
> **URLs:** [azurecr.io](https://azurecr.io) (4), [demoalfredoazurecr.io](https://demoalfredoazurecr.io) (1), [azurecontainerapps.io](https://azurecontainerapps.io) (1)
> **UI Navigation:** go to (5)
> **Code Identifiers:** openapi (2), curl (1)
> **File Paths:** main.yaml (1), credentials.json (1)

#### [Troubleshooting container deployment](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/troubleshooting-container-deployment?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/troubleshooting-container-deployment?u=76281980&t=0)** - So now we've dealt with a broken container. It doesn't start. But what happens if you have the incorrect port? Well, let's make that change again. We're going to clean this up. Make that syntax error go away and change the port to something else. So I'm going to remove these syntax error so it doesn't bother us anymore. And here I'm going to put, say Port 8000. And I'm going to commit those changes and say setting up port 8000. And then commit those changes. And what this is going to do is, as you know, we have automation now. It's going to build this container, push it to the [[GitHub]] container Registry, and then it's going to tell [[Microsoft Azure|Azure]] that it needs to pull this new container and try to run it up again. So let's click on this one. You'll see that it's building the container. That's going to take a few seconds, hopefully not too long, and then it will go back and deploy. So let's just wait a minute until these setting up port 8000 commit gets built and deployed and pushed to Azure. Okay, so our action was successfully completed. It gives us the URL. So I'm going to click on that and what's going to happen? I'm going to click this, you know,
>
> **[1:32](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/troubleshooting-container-deployment?u=76281980&t=92)** will just stay there loading. Nothing seems to happen. So what you need to do is, again, go back to the portal. You'll get here. And you need to go back to the deployment center, click on that, and then check the logs. And let's make this a little bit bigger. There we go. And you will see that it's waiting, waiting for response to warm up for a container. It's been waiting there for 61 seconds. So that's, that's not good. That's not good. And you can see here something interesting. Docker run port 80. So this is, this is crucial right here. And nothing is telling you that you have the wrong port. So what it will happen is that this will stay there for a while. It will try to load that and it'll eventually time out. Once it times out, it will tell you. But all this time you'll be waiting and seeing, trying to see if there's anything going on. So you definitely want to check that there's a smaller threshold there to check for the port. So you might want to have a test or something that prevents changes like this happening. Now for the problems with the syntax error, I was scrolling all the way back
>
> **[3:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/troubleshooting-container-deployment?u=76281980&t=188)** and I was able to see here in the logs that were captured that the syntax error, I'm going to go all the way up. I'm going to scroll pretty fast. And I've scrolled almost all the way to the top. And you can see here that we have the syntax error that I introduced before. So it does end up showing in the logs. It's just somehow pretty hard to catch. But if you go through and pour through all of the logs, you will be able to see it. Now if I go all the way to the bottom, you'll see that it's still waiting for a response because it can't get any responses from port 80. So you now have a couple of things that can go wrong. Wrong port, and wrong or invalid code, like a syntax error that breaks your container when it tries to start and it gets to Azure and gets deployed. So that's it. Those are two things that you need to watch out for.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (3), [[GitHub]] (1)
> **Ports:** port 8000 (3), port 80 (2)
> **CLI Commands:** make (3), docker (1)
> **UI Navigation:** click on (3)
> **Cross-References:** go back to (2)
> **Env Vars:** url (1)
> **Tools:** github (1)
> **Warnings:** watch out (1)


### 11. Fine-Tuning and ONNX Exporting

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction to fine-tuning theory](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-fine-tuning-theory?u=76281980)

> [!transcript]- Transcript
>
> **[0:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-fine-tuning-theory?u=76281980&t=8)** - [Instructor] Let's take a look at some of the advantages of Transfer Learning as opposed to other types of machine learning. A very classic type of machine learning is supervised machine learning. Many people have heard about it and it really is about taking historical data, training a model on their historical data to then make a prediction, like for example, the points for a player could it predict the salary that that player would make in the future, right? That would be a classic supervised machine learning problem. And in particular with NLP here, we can see here that you would have some news data. You would go through and create a summarization model, and then that could be trained on lots of data and maybe the model would be a large language model, so there would be billions of parameters and it would take a very expensive process to create a prediction. And then, if you have a different kind of NLP problem and you have a different domain, in this case, there would be a literature data set, you'd have to do the same thing again and create a different kind of news summarization model. It also could be billions of parameters and be very expensive. So, the problem here is that, for many people, many organizations, they don't have the data, really the raw data to begin with, and they also don't have the compute resources to do really this global scale type supervised machine learning. Fortunately, this is where transfer learning comes in, and really this is one of the advantages of the [[Hugging Face]] platform is that you can take a news data set, for example,
>
> **[1:42](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-fine-tuning-theory?u=76281980&t=102)** or some dataset that was trained in a particular domain, and you can take the body of that model and you can actually replace the HEAD, in this case, the News Summarization Model HEAD. You can fine-tune it with a smaller amount of data and then serve out a prediction. Similarly, you could also fine-tune something on a totally different domain. So, let's say this was trained on a news dataset. You could take a literature dataset, and again, use the body, which could have billions of parameters inside of it, and be trained by some of the top machine learning engineers in the world, and machine learning researchers, and then you would just take a little bit of new data, create an evaluation metric, and then put the HEAD of that particular fine-tuned job with some new data, and then you would be able to serve out a prediction. So, the idea here is that you can create high-quality models that are trained extremely efficiently and on another domain, right? So, this is one of the key advantages of transfer learning and how it applies to the Hugging Face platform.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (2)
> **Env Vars:** head (3), nlp (2)
> **CLI Commands:** make (2)
> **Analogies:** for example (2)
> **API Endpoints:** head  (1)
> **Speakers:** - [instructor] (1)

#### [Performing fine-tuning](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/performing-fine-tuning?u=76281980)

> [!transcript]- Transcript
>
> **[0:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/performing-fine-tuning?u=76281980&t=8)** - [Instructor] Let's go ahead and take a look at the official [[Hugging Face]] documentation here and take a look at this course. You can see there's lots of different useful things in this course, but I want to take a look at the [[Fine Tuning]] section here, fine tuning a pre-trained model, and take a look at how I could actually implement that myself. So I'm going to go ahead and select this button here. And one of the nice things about this course is it opens up a CoLab notebook and it gives us a nice overview of the sections as well. So we can see here it's not a lot of code here, it's just a section here that installs some software. It then goes through and has a tokenize function. It then has a trainer, which goes right here. And then we go ahead and we do a train and it will actually do the fine tune operation and then show us exactly what's happening. Now, if I wanted to run this myself and get better and better accuracy, what I could do is use the CoLab environment here. Now what's nice about this is if I connect to the runtime, by default it's going to connect to a CPU based runtime here, and we can actually verify that if I say change runtime type. Notice I don't have any [[Hardware]] acceleration. What's nice about this is it allows me to benchmark what would happen if I needed to fine tune a model by training it with a CPU. And so we can actually go through here and say runtime run all, and this will actually allow me to go through and kick off an installation first.
>
> **[1:43](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/performing-fine-tuning?u=76281980&t=103)** And we can see that it does take a little bit of time when you're using CoLab to install software because of the fact that it is not preloaded on this machine, unlike your own workspace or something like [[GitHub]] code spaces. Next, if we scroll down here, it's going through and it's running the next section, which is loading this dataset, which could take a little bit of time here. And then once it's gone through and loaded the dataset, the next thing that it's going to do is it's going to go through here and set up the training job. So give this a second here. We can go through here, we can get the pre-trained model right here, that's the next step that's happening. And once this is complete, then we can actually go through the fine tuning step here. So this is a good benchmark here to show what would happen if we were going to fine tune a model using more of like commodity type hardware, a CPU or a CoLab notebook that only has CPU available. What would we get? And here we go. We can actually go into transformers here and take a look at this. And we can see here that it is in fact doing three epochs and we can actually see that it will take quite some time. You can see here, this is the training job. It's going to take a while to run this. And in fact, of the 1377 steps,
>
> **[3:20](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/performing-fine-tuning?u=76281980&t=200)** it's only done four so far, right? So we can probably extrapolate that could take us, who knows, maybe 20, 30 minutes to get this running. So I'm going to leave this running, and if I go back to that same code here and I take a look at running it inside of a GitHub Codespace with GPU enabled, what we can do is basically implement the same code. So I'm going to say from datasets import load_datasets, grab the transformer pieces of code that I need, like the training sets, and then I'm going to go through here and I'm going to download the data set that I'm going to use, which in this case would be the glue data set, create a tokenize function. And then in here I would just set up all of the different things I would need for my model. So in this case, I could create my own compute metrics section, and then I would just tell the trainer itself what is my data that I'm going to use. And again, it's the data that we downloaded earlier. And then I can go through here and say, here's my evaluation metric, and then let's go ahead and run it. So what's great is I can actually do nvidia-smi -l 1 to kick off the the GPU monitoring.
>
> **[4:38](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/performing-fine-tuning?u=76281980&t=278)** And then next I can actually go through here and pop this on the screen above and actually run the training job. So let's go ahead and do this, we'll go ahead and say [[Python (Programming Language)|python]] hugging-face/ft we'll say fine tuning course example. So I'm going to take the example from the official documentation. I'm going to run it inside of this particular environment. So there will be some things that will take a little bit of time. For example, anytime you need to download data, it'll take a little bit of time, but because we have these GPU resources available, it should significantly change what's happening. And we can actually do a comparison against that fine tuning job that's running in CoLab. So here we go, we found a cached data set. So this is again, a huge advantage of having your own environment. And we're going to now train this fine tuned model and get better accuracy. So let's go ahead and run this. Here we go. It says number of examples 408, epochs, you can see here it's just really running through this very, very quickly. And look, we can see that the GPU is actually being hit. And if we go back to CoLab here, look at this, it's still stuck on basically step 30 here. And here we see it's just grinding through all of the different steps that are necessary. And also we can see here it's saturating this particular GPU, which is a Tesla V100,
>
> **[6:13](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/performing-fine-tuning?u=76281980&t=373)** a very nice, powerful machine here. And as we're training the fine tuning job, we can actually get whatever metric we're caring about, maybe it's the F1 score is what we're caring about, is that we want this F1 score to get better and better so that we're able to have the metric that we're optimizing for improved. And as we improve this metric, this will help us build out really a fine tuned model that's using custom data, right? So this is a good example of some of the advantages of having your own GPU available and fine tuning. You can get your own improved accuracy. Look, you can see here this was maybe the baseline and now here's our new improved accuracy. And if we needed to as well, what we could do is we could actually push this change every time it makes a new improvement. We could actually tweak the code a little bit right here under training arguments and we could say, look, I want to push this to Hugging Face into a new model that's hosted. And you can see this is kind of the same concept that other people have done on Hugging Face. So if we go back to Hugging Face real quick and we look at models, and let's take a look at summarization, you can take a look. This is similar to what potentially people have been doing here, right? In this particular model here you can see that in fact it's based on the original [[Facebook]] model, but it's been fine tuned
>
> **[7:49](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/performing-fine-tuning?u=76281980&t=469)** and it's been pushed into this particular directory. So you can do the same thing yourself by doing fine tuning. And again, if you're using these GPU resources here, look, it's already done. It's complete. It's a great way to experiment with fine tuning your own models.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Fine Tuning]] (9), [[Hugging Face]] (4), [[Hardware]] (2), [[GitHub]] (2), [[Python (Programming Language)|Python]] (1)
> **Env Vars:** gpu (7), cpu (4), v100 (1)
> **Tools:** colab (6), github (2)
> **Cross-References:** go back to (3)
> **Definitions:** is a  (3)
> **Prerequisites:** set up (2), install (1)
> **Analogies:** for example (1), similar to (1)
> **CLI Commands:** python (1)

#### [Introduction to ONNX and Hugging Face](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-onnx-and-hugging-face?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-onnx-and-hugging-face?u=76281980&t=0)** - [Instructor] Let's go through some of the concepts that you'll need in order to work with [[Hugging Face]] and Onnx. And Onnx specifically, I want to show you the main landing page here, the get started page, because it has a couple of things that are useful to understand before we dive in into the details. So first off is that you have the ability to work regardless of the framework that you're choosing to create the model or to work with the model, you can export to a normalized, or that is like an Onnx format. Now the Onnx format will allow you to deploy to different types of architectures and regardless of where that machine learning framework came from. And then finally, you'll have [[Cloud Services]] that work natively with the Onnx former format so that you can interact with it and not specifically be tied to an actual framework. Now, exporting to the Onnx format is specifically what we're going to concentrate on today, and we'll take a quick look at what the Onnx Model Zoo is and we'll work a little bit with, not a specifically a, a reference tutorial, but we'll be using Hugging Face, which is what we've been covering. Now, inferencing using Onnx and exporting to another framework are definitely things that I highly suggest you do and go through. Now let's take a look at the repository Hugging Face dash Onnx, which I put together. Let's take a very quick look at the environment, the YML file and see what are the dependencies
>
> **[1:34](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-onnx-and-hugging-face?u=76281980&t=94)** you'll be working with. Alright, so primarily the dependencies are [[Python (Programming Language)|Python]] three eight. I'm going to be installing [[PyTorch]] from the PyTorch channel. This is a Conda environment file. That means that we'll be using Conda behind the scenes to work with these. And we're going to of course install the Hugging Face Transformers library that has this special notation that the square brackets that say, Hey, I'm going to be working with Onnx. That will pull in some extra dependencies they'll be needing. Finally, the Onnx runtime and a couple of extra things for working with Jupiter notebooks. So pretty straightforward, not many dependencies. And this will get you up and running in no time. So next, the other things that we need to do is, well, we're going to explore a little bit of the notebooks, in this part we're going to do the introductory portion of it. So let's go quickly and run this notebook with, with like include a notebook with Conda and install dependency. So that's going to be the introduction notebook right here. So this is the Read me, this is the same repository and run new Visual studio code here. And going to take a look at what is this about. Alright, so the first thing that you need to make sure is that ensure that you are using the, the one, the right interpreter. Here I'm using the kernel, it has the Conda base one that I just created
>
> **[3:07](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-onnx-and-hugging-face?u=76281980&t=187)** and I made sure that Conda installed all the dependencies. So here have like a brief introduction. This is again, kind of like what you'll need to install with Conda like this already doesn't match exactly what you need to install. Please refer to the environment, the YML file that has everything you need to is going to be updated with exactly the, the things that you need to do. Alright, so Hugging Face has the ability to work with configurations that are already ready to use without you having to figure out anything. For some of the most, like the top, like the top models, the most popular models like BART, BERT, Open AI GPT-2, roBERTa, and T-5. I mean these are, you know, highly popular, highly desirable models that you can work with. So once you've installed all the dependencies, you can make sure that everything works by executing the, the help menu. So you can use the transformers that Onnx dash dash help that will give you, essentially what you're using here is the Onnx portion of the libraries with the Transformers library so that you can start interacting with it. So what you're going to be doing this, in this way of running Python dash m using the Onnx module. So Transformers dot Onnx as a command line tool. So let's poke around a little bit of the help menu output here.
>
> **[4:42](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-onnx-and-hugging-face?u=76281980&t=282)** This is the way that you verify that this has been installed and everything is ready to go. So here you have, you know, the, the module and then you pass in. What, what features, and we're going to be looking at the features that are available. Opsets is the type of opset for Onnx And we won't be diving into that. And you can see here, you can export from, from [[TensorFlow]] or PyTorch. We're going to be using PyTorch by default and you have a little bit of extra settings here that we're not going to be touching either. And then one thing that you are required to do is using the positional argument, the output, and we'll see how that works. And you can essentially name the Onnx model, the resulting Onnx model with anything, any other name that you may want. Alright, so scrolling down here, primarily the thing that we want to know is what type of configuration we are going to be using. So here you can see that I'm importing from the models. I'm going to Transformers dot models dot roBERTa, I'm going to import roBERTa config and roBERTa Onnx config. These come from the Transformers library itself. So by running this code, I'm going to be able to see that I do have a couple of different types of features. These features, these configurations are available to, you know, I can select one or the other and I can, I can poke around what, what exactly is available for me from, in this case,
>
> **[6:17](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-onnx-and-hugging-face?u=76281980&t=377)** the roBERTa model. Alright, so I do have a little bit of suggested reading here, the Hugging Face transformer serialization, that's really good. But essentially what I wanted to cover today are a couple of things. Basically how you get all of these dependencies installed. We used Onnx, rather Conda, we made sure that the Conda, the Conda kernel was selected. We were able to verify works, we poked around some of the, the different, different features and and flags that we're going to be using and how is that going to work. And we're going to see that in a different video. But specifically the configuration was something really, really crucial. One last thing I want to show you is the model Sue. If you've never heard about a model Sue, it's the [github.com/Onnx/models](https://github.com/Onnx/models). It's called the Onnx Sue because that is where people tend to store, or the Onnx organization rather tends to store some of the Onnx models. So let's, let's quickly go through here. So the Read me has a lot of information but, and this is specifically I want to talk about machine comprehension. So we're going to look at that and we have GPT two, bert squad, but I wanted to show you roBERTa, which I really like, want to hide these and use cases. So we have dependencies. The model, you have a description and you can even go ahead and download. So you have roBERTa Base roBERTa sequence classification. So you can download these. These are stored on [[GitHub]] using LFS. And it is, you're able to go through a little bit
>
> **[7:52](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-onnx-and-hugging-face?u=76281980&t=472)** of the details of the model. So you can see here what OnxX version is supported and the opposite version. So those are two crucial details you will need to understand for Onnx if you are going to be working with that. So you have even a little bit of information on the pre-processing and the output and how, what should be expected if you are going to be doing a little bit of working, working with, working with Onnx. So you can even see here that it's using high interface transformers already because this, the sequence classification and tokenize are coming from there as well. So whenever you're hearing, hey, let's take a look at the Onnx model sue. That means you are looking at Onnx slash models, the GitHub organization and the models github repository. So that's it. Those are the key concepts that you'll need to know in order to work with Onnx and Hugging Face.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (7), [[PyTorch]] (4), [[GitHub]] (4), [[Python (Programming Language)|Python]] (2), [[Cloud Services]] (1)
> **Code Identifiers:** roberta (8)
> **Env Vars:** yml (2), gpt (2), bart (1), bert (1), lfs (1)
> **Prerequisites:** install (4), you'll need (3)
> **Tools:** github (4), visual studio (1), command line (1)
> **CLI Commands:** python (2), make (2)
> **Definitions:** is a  (1), means that (1)
> **URLs:** [github.com](https://github.com) (1)

#### [Exporting Hugging Face models to ONNX](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/exporting-hugging-face-models-to-onnx?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/exporting-hugging-face-models-to-onnx?u=76281980&t=0)** - [Instructor] Now let's do a little bit of a hands-on experimentation here with [[Hugging Face]] in ONNX. And going to look at exporting the exporting Jupyter notebook that I have here already running. Again, make sure that you have the right kernel installed. In this case, I'm using the one that is from Conda that has all of the dependencies installed. All right, so first off, let's find the features. In this case, you're going to import the features module from the ONNX library here on the transformers at ONNX module, rather. And you can import the features manager. And then for these specifically, for these example specifically, when I be doing with DistilBERT, and want to look at what those, what those features are. And I'll tell you in a second why that is important. So let's, it's connecting to the kernel. This is going to take a second, and we'll wait it out until that completes. So right, so these completed, and what are the features? Well, this model has different things, different tasks that it can do, right? So it can have sequence classification, for example, multiple choice, token classification, or question answering, which is something that I really enjoy working with with models that do question answering. All right, so why is this important? Why would you need to understand that? Well, if you know exactly what you need and have a model that is doing something very specific, then let's say for example, question answering, then you can say, well, I want to port over the DistilBERT model to ONNX just to do question answering.
>
> **[1:37](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/exporting-hugging-face-models-to-onnx?u=76281980&t=97)** And the way you accomplish that is by again, working with the transformers, that ONNX module as a command line tool. And you can pass in [[Python (Programming Language)|Python]] -m transformers.onnx. Actually, let's just walk through some of these things you will need to know. For the first, you're going to be using transformers.onnx. Again, you're using the module as a command line tool because of the -m flag right here. Next, we are going to pass in the model. Like exactly what's the model that you want to be working with. In this case is going to be the DistilBERT-base-uncased-finetuned-sst-2-English. Well, that's a mouthful, but that is exactly the model that we're going to working with. And then we're going to say the feature that we want is the question answering. And lastly, and it's, this is kind of hard to see here, we have a dot. Now this dot is a pretty important because it means that we want to produce the model right here in this same directory. If we wanted something else, we could do something like a path, and then the name to the model that we want to. I just want to generate that right there. So that is going to allow us to generate that. So I'm going to run this, it's going to take a few seconds. And it's going to start producing, producing output. So now, right now it's downloading the DistilBERT based on case finetuned model. So let's scroll through the output and see what's going on here. What you need to look for at the end is specifically the model validation. So you can see here, "All good, model saved at: model.onnx". If I would've passed in a path,
>
> **[3:09](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/exporting-hugging-face-models-to-onnx?u=76281980&t=189)** you would've been something slightly different. Or if I want a different name. And you can see here that the model output names, these are the ones that you'll be needing to work with. So if I go back to the Explorer, you can see now the Explorer is saying, hey, you have something going on here. And that's our model that has been produced right here. The model, that ONNX, which is exactly this one coming from right there. So there you go. That is how you would like, let me scroll here all the way to the top, you would find the features. In this case, we're producing one for question answering. And this is how you would work with Hugging Face in coordination with ONNX to port models from Hugging Face over to ONNX and produce a working model for ONNX.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (3), [[Python (Programming Language)|Python]] (1)
> **Env Vars:** onnx (9)
> **CLI Commands:** find (2), make (1), python (1)
> **Tools:** command line (2), jupyter (1)
> **Definitions:** is a  (1), means that (1)
> **Analogies:** for example (2)
> **Cross-References:** go back to (1)
> **Speakers:** - [instructor] (1)


### 12. Hugging Face Spaces

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction to Hugging Face Spaces](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-hugging-face-spaces?u=76281980)

> [!transcript]- Transcript
>
> **[0:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-hugging-face-spaces?u=76281980&t=8)** - [Instructor] Let's take a look at [[Hugging Face]] spaces. The idea here is that you can discover amazing machine learning apps made by the community, or what you can do as well is you can create your own new space. It's really a good idea to take a look here at some of the new stuff that's being developed, which is really cool. And we can actually look at most likes, for example. We have [[DALL-E]] mini, [[Stable Diffusion]]. These are all kind of fun things to play around with. I think this is a good one to start with, Stable Diffusion. Enter your prompt. We can just say, you know, Mickey Mouse, Mickey Mouse on the moon, right? Something kind of fun, and it will go through here and it will run a machine learning prediction based on essentially the pre-trained model that's hosted on Hugging Face, and here we go. We got some great images here. And that's kind of the fun part about this, is we get to play around with this particular model. And if we go to the files right here and we go to apps, we can say all of the different things that are happening in the set of app, this particular application. And we can actually go through here and play around with this particular application and really steal ideas that are good ideas from other people. So the other thing we can do besides playing around with models is that we also can go over to the model section itself and even find particular categories. So again, if we go through here and we look at, for example,
>
> **[1:43](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-hugging-face-spaces?u=76281980&t=103)** text, and we look at something like summarization, and we go to one of these summarization models like [[Google]] Pegasus. What's nice about this is if I scroll down here, we can look inside and see who has actually used this in one of their applications. So this is a great way to actually play around with a particular model first when you're trying to explore it. So, look, this is pretty easy, right? If I wanted to go through and, for example, take some [[Python (Programming Language)|Python]] Wikipedia page code and paste it inside of here, we could actually try it out and it would go through here and do our text summarization. And again, what's nice about this after it's done, is we could go through to the files and we could take a look at the app and we could actually copy exactly the code inside of here and play around with it. So that's really the main takeaway here when you're dealing with Spaces. Now, probably the most fun thing to do though would be to go over to the Spaces environment and create your own. So let's go ahead and do that, we'll go ahead and say Create new Space. And I'm going to go ahead and I'm going to call this hello5000. And I'm going to select one of these technologies, Streamlit, which is a machine learning visualization technology, Gradio, which is the one that's most closely aligned with Hugging Face or Static. Let's go ahead and pick Gradio and we'll go ahead and say Create Space.
>
> **[3:15](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/introduction-to-hugging-face-spaces?u=76281980&t=195)** Now, once this has been created, I could clone this and really work with it in another environment, and I'll show how to do that in a little bit. Or what we could do is we could actually just play with it from right here. And this is actually not a bad idea. So if we go through here and I go to the file section here and I go ahead and I say Add, and say Create a new file, I can actually play around with it right here. So we just call this app dot py and I can actually paste this in. And what will happen is once I paste it in, immediately, it'll start building and I can actually look at the build log in real time and take a look at it. Here we go, we can see by default it installs some, you know, packages that are, that are very common and it pushes this image locally here. And as it's building, we can actually take a look at the process. If there's errors, it would also show up here. And then finally when it's done, it'll actually give us a Hello World application. Alright, so here we have another version of the same thing, right? This is just a kind of a Hello World application. We can take a look at the code and if I want to play with it, notice it's running. If we go to this application and we type in something, you can see in action, it works, right? So that's the idea is you can prototype things very, very quickly. Most of the time, you probably want to prototype a data set or a model, but you can also play around with just Hello World applications like that. And that's really the usefulness of the Hugging Face space.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (4), [[Stable Diffusion]] (2), [[DALL-E]] (1), [[Google]] (1), [[Python (Programming Language)|Python]] (1)
> **UI Navigation:** go to (5), scroll down (1)
> **Definitions:** is a  (3)
> **Analogies:** for example (3)
> **CLI Commands:** find (1), python (1)
> **Env Vars:** dall (1)
> **Speakers:** - [instructor] (1)

#### [Hugging Face Spaces walkthrough](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-spaces-walkthrough?u=76281980)

> [!transcript]- Transcript
>
> **[0:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-spaces-walkthrough?u=76281980&t=8)** - [Instructor] All right, so you're convinced you love [[Hugging Face]] Spaces because again, according to the documentation, it offers a simple way to host ML demo apps directly on your profile, but what if you want to learn a little bit more about it? Well, I would say, first thing is look at the docs right here. We can see some of the demos. This is a great place to kind of look through things, look at the dependencies, all those kinds of things. We can also look at the fact that you can do GPU upgrades. So for example, if this is a side project, you could potentially ask for the ability to do GPU, right? Which would be pretty neat. And also you could install other packages like, for example, [[PyTorch]], JAX, et cetera. The other thing you can do, if we look at the Gradio Spaces here, is it gives you a bunch of documentation about how to actually build your own applications. And really the idea here is that you can actually load pre-existing applications and build them very quickly. So one of the classic ones is going to be the Hot Dog Classifier here. So if I go to the documentation here, what I could do is I could just copy this code, but where do I run it? I could run it inside of the Spaces interface, which is not bad, but I also could just run it inside of [[GitHub]] Code Spaces, which itself is a great interface for building demos. So inside of here, I have a Hugging Face directory and I'm going to CD into there and I'm going to call this hotdog. And what's nice about this
>
> **[1:42](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-spaces-walkthrough?u=76281980&t=102)** is that I can actually get this very fast feedback loop by running things inside of this directory here. And so in fact, I'll go ahead and piece that code in like this. And notice there's not a lot of code. I say "import gradio as gr", I also have to in invoke the transformers, right? In this case, this is somebody's hotdog or not pipeline right here, and then I would actually upload a particular image that has a hotdog or doesn't have a hotdog. So let's go ahead and run this. We'll say "[[Python (Programming Language)|python]] hotdog", and let's go ahead and get this thing launched. So this will take just a second and it will again use our nice GPU interface here to play around with the official documentation and what's great about this is that I can also play around iteratively with this particular application. Aha, there we go. We can actually drop an image right here. And so what I would need to do is find a picture of a hotdog somewhere on the internet, and I found one and I'm going to take it, I'm going to save the image locally on my desktop. I'm going to go ahead and select this and then grab that hotdog image. Here we go. The other thing we can do is we can just go to this user's profile, look at this application and look, I dropped the hotdog on here and it says, "Yep, hot dog." It was able to figure it out. And so I can either play with it
>
> **[3:17](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-spaces-walkthrough?u=76281980&t=197)** inside of somebody else's interface or again, get it running locally here and iterate on it. Now in this particular example, that was a hotdog, but what if I wanted to go through here and look at maybe another kind of a demo? And so I'm going to go into these Gradio demos here and I have one that is a text generation demo. And if we take a look at this, it's very simple. I do "from transformers import pipeline", I say "import gradio as gr", I define the model, which is going to be a text generation model, I then define a predict function, and then I make the interface, which is very straightforward here, and gives us the ability to use it. So this is really nice because the feedback loop, again, is very quick because it's local and it's all installed inside of this environment. Let's go ahead and take a look at how this works. Here we go. I'm going to look at the documentation, too. It says that it's using GPT2, here we go. And we can say "the fastest man in the world".
>
> **[4:29](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/hugging-face-spaces-walkthrough?u=76281980&t=269)** And let's go ahead and see what happens. It says, "The fastest man in the world is Mike Tice, ESPN". "CJ's been able to make at least 50 tackles this year." "He's been a dominant tackler at defensive tackle", right? So we have some nice output here for maybe like a chat bot or some other kind of interface here and I've been able to really, really quickly prototype it because I'm using the ability to make an interface and also wire in some code. Now, you don't need to only build out things that work with models, although that's one of the more useful things, you also can take a look at data sets. And in this case, this is a Gradio example that has a Supersoaker Defective Product Prediction and I can put some description data in here and look, I load the interface, I load the Hugging Face Scikit Learn Tabular-Playground, and I can pass in the title and I can pass in the description and I can play around with things as well. So let's go ahead and try that. We'll say "python dataset_gradio". Again, we go through here and we launch it. And this allows me to also play around with things like datasets, right? And make new rows and do all kinds of really fun things inside of a interface here and submit things. And so what's really useful about this kind of a interface here is that I have the ability to rapidly prototype both dataset attributes as well as model attributes and this is really the advantage of using Gradio and Hugging Face Spaces.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (4), [[Python (Programming Language)|Python]] (2), [[PyTorch]] (1), [[GitHub]] (1)
> **CLI Commands:** make (4), python (2), cd (1), find (1)
> **Env Vars:** gpu (3), jax (1), gpt2 (1), espn (1)
> **Definitions:** is a  (5)
> **Documentation:** the documentation (3), the docs (1)
> **Analogies:** for example (2), picture (1)
> **UI Navigation:** go to (2)
> **Code Identifiers:** dataset_gradio (1)

#### [Deploying to Hugging Face Spaces](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploying-hugging-face-spaces?u=76281980)

> [!transcript]- Transcript
>
> **[0:08](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploying-hugging-face-spaces?u=76281980&t=8)** - [Instructor] Let's take a look at the lifecycle of how to do a [[Continuous Delivery (CD)|continuous delivery]] of a [[Hugging Face]] Spaces application using [[GitHub]]. You can see here that I could grab a token from Hugging Face, right? That's one of the key things to be aware of. Once I take that token, I would put this into the GitHub Codespace environment, as well as the GitHub Actions environment. From here, this allows me to do anything I need to do, like, for example, push a model back into Hugging Face, as well as interact with the Spaces Application. In this case, I could test first the Gradio Application here. Let's say, for example, Text Summarization, and then later, automatically push that change directly to Spaces. So, this is continuous delivery of machine learning applications. Now, let's go ahead and take a look at how that would work in practice. What I would do is I would go to GitHub to the workflow here, and notice, I would say Sync to Hugging Face. Under the main branch, I would run this on [[Ubuntu]]-latest, and I would have my secret that would be set up inside of Hugging Face. I would go ahead and add this whatever location the space lives in. In this case, this particular one would be noahgiftdemo, and then I would push this automatically every time I make a change to this repository, so I could add in steps as well for testing other things. And now if we look at the application,
>
> **[1:43](https://www.linkedin.com/learning/mlops-tools-mlflow-and-hugging-face/deploying-hugging-face-spaces?u=76281980&t=103)** it's a very straightforward, right, a very simple application here that does a summarization pipeline. So, how would we actually make sure that this continuous delivery would actually work? Well, pretty easy. What we would do is use our Codespace environment, and you can see here I've got a couple of 'em running. So, what I'm going to do is I'm going to toggle over to one of 'em, and here we go. We've got this application right here, and notice that when I run this locally, it'll actually launch an interface. And once I've launched this interface here, I can actually browse it directly inside of this environment. And look, I pasted in some [[Python (Programming Language)|Python]] code. I pasted in some information about the Python language, and I had it summarize it. And there we go, we actually were able to summarize that change. So, if I clear it and I want to go through here and grab that same data again, we can see the same application workflow again. So, it's a great way to prototype out what it is you're building, and use, in fact, both the local development environment as well as the continuous delivery process of the GitHub ecosystem here. In fact, this synchronization, if it was failing, in this case, it was, we would have to double check if there was some kind of a lint problem or there was a test problem, and this is really the beauty of doing continuous delivery using both GitHub as well as Hugging Face Spaces.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hugging Face]] (6), [[GitHub]] (6), [[Continuous Delivery (CD)|Continuous delivery]] (5), [[Python (Programming Language)|Python]] (2), [[Ubuntu]] (1)
> **Tools:** github (6)
> **CLI Commands:** make (2), python (2)
> **UI Navigation:** go to (1), toggle (1)
> **Analogies:** for example (2)
> **Warnings:** be aware (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [[Alfredo Deza]]
- [[Pragmatic AI Labs]]

## Resources

- Exercise files available

## Skills Covered

- MLflow
- Machine Learning
- MLOps
- Artificial Intelligence (AI)
- Hugging Face Products

## Path Context

### In [[MLOps Essentials for Developers and AI Engineers- Tools, Pipelines, Security]]
← [[Generative AI and Predictive AI in the Cloud- Foundational Concepts and Scenarios]] | **4 of 10** | [[Data Preparation, Feature Engineering, and Augmentation for AI Models]] →

## Appears In

- [[MLOps Essentials for Developers and AI Engineers- Tools, Pipelines, Security]]

## Related Courses

_Courses sharing skills:_

- [[Introduction to MLSecOps]] — Artificial Intelligence (AI), Machine Learning, MLOps
- [[MLOps Essentials- Model Deployment and Monitoring]] — Artificial Intelligence (AI), Machine Learning, MLOps
- [[MLOps Essentials- Model Development and Integration]] — Artificial Intelligence (AI), Machine Learning, MLOps
- [[Machine Learning and AI Foundations- Clustering and Association]] — Artificial Intelligence (AI), Machine Learning
- [[Machine Learning and AI- Advanced Decision Trees with SPSS]] — Artificial Intelligence (AI), Machine Learning

---

[↑ Back to top](#)