---
type: course
cssclasses:
  - lle-course
  - lle-level-beginner
slug: devops-foundations-your-first-project-24355651
url: "https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651"
duration_minutes: 237
duration: 3h 57m
level: Beginner
updated: 7/29/2024
learners: 79197
skills:
  - DevOps
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQEpgiTxu5Hhzg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1718999721632?e=2147483647&amp;v=beta&amp;t=AXa-UVZXmn6BchOxkEdy32o4JsYaeLwHjHmRShYWw3I"
linkedin_topic: DevOps
learning_paths:
  - '[Getting Started with DevOps](../../Paths/DevOps/Learning%20Paths/Getting%20Started%20with%20DevOps.md)'
prev_courses:
  - '[DevOps Foundations- Monitoring and Observability](DevOps%20Foundations-%20Monitoring%20and%20Observability.md)'
path_nav: '[{"path":"Getting Started with DevOps","position":5,"total":5,"prev":"DevOps Foundations- Monitoring and Observability","next":null}]'
path_count: 1
tags:
  - course
  - topic/devops
  - skill/devops
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/DevOps/DevOps%20Foundations-%20Your%20First%20Project.md)

![DevOps Foundations: Your First Project](https://media.licdn.com/dms/image/v2/D560DAQEpgiTxu5Hhzg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1718999721632?e=2147483647&amp;v=beta&amp;t=AXa-UVZXmn6BchOxkEdy32o4JsYaeLwHjHmRShYWw3I)

# DevOps Foundations: Your First Project

> Curious about how to put DevOps principles into practice? This course can help. Join instructor and DevOps expert Carlos Nunez as he demonstrates how to apply DevOps principles to a sample app for a fictitious travel booking company. Throughout the course, Carlos shows you how to leverage popular tools and methodologies to make updating, testing, and releasing the app a breeze. After explaining ho

> [LinkedIn Learning](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651) | 3h 57m | Beginner | 79K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Your first project, DevOps-ified!](#your-first-project-devops-ified)
  - [What you should know](#what-you-should-know)
  - [Configuring AWS](#configuring-aws)
- [**1. The Project/The Mission**](#1-the-projectthe-mission) (3 videos)
  - [So what is this DevOps thing anyway?](#so-what-is-this-devops-thing-anyway)
  - [Your application, pre-DevOps](#your-application-pre-devops)
  - [Your application, post-DevOps](#your-application-post-devops)
- [**2. Testing Locally with Docker**](#2-testing-locally-with-docker) (4 videos)
  - [Bring production to your laptop with Docker](#bring-production-to-your-laptop-with-docker)
  - [Installing Docker](#installing-docker)
  - [Writing your first Dockerfile](#writing-your-first-dockerfile)
  - [Writing your first Docker Compose manifest](#writing-your-first-docker-compose-manifest)
- [**3. Getting Started with Git**](#3-getting-started-with-git) (4 videos)
  - [Version control: The secret weapon of DevOps](#version-control-the-secret-weapon-of-devops)
  - [Installing Git](#installing-git)
  - [Initializing your first Git repository](#initializing-your-first-git-repository)
  - [Getting familiar with the Git workflow](#getting-familiar-with-the-git-workflow)
- [**4. Collaborative Version Control**](#4-collaborative-version-control) (6 videos)
  - [Beyond local repositories](#beyond-local-repositories)
  - [Introducing GitLab](#introducing-gitlab)
  - [Creating the GitLab Docker Compose service](#creating-the-gitlab-docker-compose-service)
  - [Starting the GitLab Compose service](#starting-the-gitlab-compose-service)
  - [Creating our first GitLab project](#creating-our-first-gitlab-project)
  - [Connecting Git to GitLab](#connecting-git-to-gitlab)
- [**5.  Testing Your App with RSpec, Capybara, and Selenium**](#5-testing-your-app-with-rspec-capybara-and-selenium) (9 videos)
  - [Test often, ship often](#test-often-ship-often)
  - [Setting up your test](#setting-up-your-test)
  - [Viewing our test harness in Docker Compose](#viewing-our-test-harness-in-docker-compose)
  - [Updating the Dockerfile](#updating-the-dockerfile)
  - [Running the test](#running-the-test)
  - [Finding criteria for a unit test](#finding-criteria-for-a-unit-test)
  - [Writing the unit test](#writing-the-unit-test)
  - [Setting up Selenium](#setting-up-selenium)
  - [Running your test with Selenium](#running-your-test-with-selenium)
- [**6. Infrastructure as Code with OpenTofu**](#6-infrastructure-as-code-with-opentofu) (10 videos)
  - [Deploy with confidence with infrastructure as code](#deploy-with-confidence-with-infrastructure-as-code)
  - [Finding the version of OpenTofu to use](#finding-the-version-of-opentofu-to-use)
  - [Adding OpenTofu to Docker Compose](#adding-opentofu-to-docker-compose)
  - [AWS deployment explained](#aws-deployment-explained)
  - [Writing your integration test](#writing-your-integration-test)
  - [Writing OpenTofu code](#writing-opentofu-code)
  - [Reviewing the OpenTofu plan](#reviewing-the-opentofu-plan)
  - [Applying the OpenTofu plan](#applying-the-opentofu-plan)
  - [Running your integration test](#running-your-integration-test)
  - [Destroying the website from AWS S3](#destroying-the-website-from-aws-s3)
- [**7. CI/CD as Code**](#7-cicd-as-code) (8 videos)
  - [Shipping fast with CI/CD](#shipping-fast-with-cicd)
  - [Introducing GitLab CI](#introducing-gitlab-ci)
  - [Writing our first Gitlab CI workflow](#writing-our-first-gitlab-ci-workflow)
  - [Configuring sensitive variables](#configuring-sensitive-variables)
  - [Creating our first Gitlab CI runner](#creating-our-first-gitlab-ci-runner)
  - [Putting it all together](#putting-it-all-together)
  - [Challenge: Change the website title](#challenge-change-the-website-title)
  - [Solution: Change the website title](#solution-change-the-website-title)
- [**8. Conclusion**](#8-conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Your first project, DevOps-ified!](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/your-first-project-devopsified-24080523?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/your-first-project-devopsified-24080523?u=76281980&t=0)** Hard silos, configuration differences and engineers just not talking to each other are some of the biggest reasons for slow and error-prone software deployments. This can cause massive problems. Downtime in production can cost companies over $5,000 per minute. That's a lot. Applying a little bit of [DevOps](../../Topics/DevOps.md) to any project goes a long way towards solving these problems. My name is Carlos Nuñez. I've been a systems administrator and software engineer for over 15 years. I love DevOps. In this course, I'll teach you how to apply technical DevOps principles to a website. Together we'll use Docker, [Git](../../Skills/Software%20Development/Git.md), automated tests, CI, CD and [Infrastructure as Code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) to take this website all the way into the cloud. Ready to go on your first DevOps journey? Let's do it!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (4), [Git](../../Skills/Software%20Development/Git.md) (1), [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (1)
> **CLI Commands:** docker (1), git (1), cd (1)

#### [What you should know](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/what-you-should-know-24079507?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/what-you-should-know-24079507?u=76281980&t=0)** Here's what you should know to be successful in this course. Being comfortable with the [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) shell, such as Bash, Ash or Zsh, will help you get the most out of this course. Having basic programming experience will help as well, but it is not required. While we'll be using Vim as our editor of choice, experience with any other IDE or editor will do. I've included all of the demo files that I'll be using throughout the course for you, which you can download under the exercise tab to follow along. For each demo, there's a beginning and an end state. We're also going to host Explore California in AWS or [Amazon Web Services](../../Skills/Cloud%20Computing/Amazon%20Web%20Services%20(AWS).md), so you'll need an AWS account if you would like to do this with us. Note that this will cost some money. To create an account, simply open your browser, visit console.aws.[amazon.com](https://amazon.com), click "Create a new AWS account" and follow the instructions provided. If your company has a corporate AWS account, ask your administrator for help on getting started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1)
> **CLI Commands:** aws (5)
> **Env Vars:** aws (4), ide (1)
> **Tools:** bash (1), zsh (1), vim (1)
> **Prerequisites:** you'll need (1), getting started (1)
> **URLs:** [amazon.com](https://amazon.com) (1)
> **Analogies:** such as (1)
> **Warnings:** note that (1)

#### [Configuring AWS](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-aws-24080513?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-aws-24080513?u=76281980&t=0)** We're going to be doing some things with [Amazon Web Services](../../Skills/Cloud%20Computing/Amazon%20Web%20Services%20(AWS).md) in this course. But before we do that, let's learn how to create temporary administrator AWS credentials both in the browser and in our terminal. The first thing we're going to do is open a browser and visit console.aws.[amazon.com](https://amazon.com). If you're prompted to log in, go ahead and log in. If you haven't created an account, go back to what you should know and visit the documentation there to get started with that. Once you're logged in, take note of the region that's to the right of the gear icon over here. If you click on it, you'll see in orange the region that was selected for you. You can think of a region within AWS as a collection of data centers that provide AWS services. The region in orange is the region closest to you. So in my example, the closest region to me is the Ohio region and its code is US East 2. Save this code somewhere, as we're going to be using it later. The next thing we're going to do is create a user that we can log in with in the AWS [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md), which we'll download and install later. So let's go ahead and click on the search text box up here and type in "IAM." In the results, click on IAM to continue. Next we're going to click on Users. Then we're going to click on Create User. In the next page, you'll be asked to give your user a name. This can be anything. You can just use your own name or you can use "Carlos" here. You can leave the checkbox underneath it unchecked. Click Next and then click Next again,
>
> **[1:35](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-aws-24080513?u=76281980&t=95)** since we're not going to be adding this user to any groups. In the review screen, make sure that your username is the username you typed in earlier and then click Create User. The next thing that we're going to do is create a role that allows us to become an administrator. To do that I'm going to click on Roles. Then I'm going to click on Create Role. Then I'm going to click on AWS Account. Then I'm going to click on Require External ID. This is a password that will allow our user to use this role. This can be anything. I'm going to go ahead and type in "supersecret" for example. Then we're going to click on Next. And then we're going to check Administrator Access up here. Then we're going to click on Next and give our role a name. Let's call this role "Example Admin Role." You can enter a description if you like, but it's optional. Once you enter the role name, scroll down and click on Create Role. Once that role is created, click on View Role. Then click on Trust Relationships. Then click on Edit Trust Policy. And then, where it says root over here, go ahead and type in "user". Then your username, like Carlos for example. Once you're done with that, click on Update Policy to apply the changes. The next thing that we're going to do is generate credentials
>
> **[3:09](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-aws-24080513?u=76281980&t=189)** for our user, so that we can assume the role that we just created. So to do that let's click on Users. Click on the user that you just created. Then click on Create Access Key. Scroll down and click on Other. Then click on Next. You can give the set of keys that will be created a description. It's completely optional. Afterwards, click on Create Access Key to create the access key. We're going to leave this page up because we're going to be using it very soon. The next thing we're going to do is link our user with the role that we created. Before I do that I'm going to open a new tab. Go back to console.aws.[amazon.com](https://amazon.com). Go back to the IAM service by typing "IAM" in the search box, then clicking IAM in the results. Then I'm going to click on Roles. Search for the Example Admin Role that we created. And then we're going to leave this page up for a second. The next thing that we're going to do after we open this tab is open a terminal. Since I'm on a Mac, what I'm going to do is I'm going to type Command + Space, type in "Terminal" and hit Enter. If you're on [Windows](../../Glossary/Service/Windows.md), you can just click the Start button, type in "Terminal" and then click on the result. Before beginning, make sure that you have the AWS CLI installed. You can follow the instructions on the link below to do that. Once the AWS CLI is installed, the first thing we're going to do is configure the AWS CLI to use the user that we created. So to do that I'm going to type "aws configure"
>
> **[4:46](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-aws-24080513?u=76281980&t=286)** and then I'm going to hit Enter. Here I'm going to be asked to provide an access key. So to do that I'm going to go back into my browser, click on the Create Access Key tab that I opened earlier, then I'm going to copy the access key over here, go back to my terminal, paste it in and hit Enter. Now I'm going to be asked for the secret access key. So to get that I'm going to go back into my browser. Click on the Copy button next to the secret access key stars. Go back to my terminal. Paste it in and hit Enter. Next I'm going to enter the region name that we saved earlier. So in my case it's US East 2. And then I'm going to hit Enter. Finally I'm going to provide [JSON](../../Skills/Web%20Development/JSON.md) for the output format. And then I'm going to hit Enter. Now I'm going to use the "aws sts assume-role" command to get a set of temporary credentials that I can use later in the course. The first argument that I'm going to provide to this is the "role-arn" argument. To get this role-arn, I'm going to go back into my browser and click on the Copy button next to my ARN here. Once I do that, I'm going to go back into my terminal and paste it in. Next I'm going to provide that password that I created earlier. To do that I'm going to type "--external-id" and then I'm going to provide the password that I created earlier, which is "supersecret" in this case. Finally I'm going to provide "--role-session-name" to give the STS session for the temporary set of credentials that we're
>
> **[6:23](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-aws-24080513?u=76281980&t=383)** about to create a name. This can be anything. You can call it "MySession," for example. Before continuing, let's make sure that your command looks something like this. And if it does, you can hit Enter. Once you do that, you'll see this huge JSON blob over here. These are your temporary credentials. Now that we've created a set of temporary credentials, let's see how we can use them to do privileged actions within AWS. So as a test, let's run an AWS command with our own user to confirm that we don't have permissions to do something. So let's for example, use the AWS IAM list users command to list the users that are available in our account. As you can see, I don't have the permissions to do that with the user that I created, the Carlos user. So now what we're going to do is we're going to use this JSON object up here to become the temporary user that STS created for us. That should be an admin user, because we created credentials using the example admin role that we created in the console. So we're going to do this in three steps. The first thing we're going to do is we're going to type in "export AWS_ACCESS_KEY_ID" followed by an equal sign. And then we're going to copy this access key ID up here and paste it in.
>
> **[7:55](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-aws-24080513?u=76281980&t=475)** Next, we're going to provide the environment variable "AWS_SECRET_ACCESS_KEY", put an equal sign and then we're going to copy the secret key up here and paste it in. And I'd recommend using single quotes around it just to avoid any weirdness with your shell. Finally we're going to create another environment variable called "AWS_SESSION_TOKEN". Then we'll put an equal sign and then we're going to copy this really long string over here. And then, in single quotes, paste it in. And then finally we're going to create another environment variable called "AWS_REGION". And this is going to be set to the region that we saw earlier, which in our case is US East 2. And then after we do all of that we're going to run that "aws iam list-users" command again to see what changes. Now, as you can see, we're able to get a list of users from the IAM service because we ourselves are an admin with these temporary credentials. So you might need to run this assume-role command several times throughout this course as these credentials expire within about an hour or so. So if you're going through the course and you get an error similar to what we saw when we ran iam list-users the first time, simply rerun that assumed-role command. It'll generate a new JSON object with new credentials, and then you could run the export commands that we just saw to link
>
> **[9:32](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-aws-24080513?u=76281980&t=572)** our shell with the new set of credentials. And then once you do that, everything should just work.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (4), [JSON](../../Skills/Web%20Development/JSON.md) (4), [Amazon web services](../../Skills/Cloud%20Computing/Amazon%20Web%20Services%20(AWS).md) (1), [Windows](../../Glossary/Service/Windows.md) (1)
> **Env Vars:** aws (11), iam (7), cli (4), json (4), sts (2)
> **UI Navigation:** click on (26), scroll down (2), checkbox (1)
> **CLI Commands:** aws (16), make (3)
> **Cross-References:** go back to (5), in the next (1), later in (1)
> **Tools:** terminal (7)
> **Analogies:** for example (4), similar to (1)
> **Prerequisites:** configure (2), install (1)


### 1. The Project/The Mission

[↑ Back to Table of Contents](#table-of-contents)

#### [So what is this DevOps thing anyway?](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/so-what-is-this-devops-thing-anyway-24082449?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/so-what-is-this-devops-thing-anyway-24082449?u=76281980&t=0)** Here's a typical conversation between a systems engineer and a software engineer that has happened at least once on every company on this planet. Our software engineer goes to our systems engineer and says, "Hey, my app is running really slowly in production. I'm not sure why. Can you see if something's up with the platform?" Our systems engineer says, "Sure, yeah, I'll have a look." Two weeks later, our systems engineer comes back with, "Hey, um, the platform is looking fine, I can't find anything wrong with it. Maybe it's something in your code?" The software engineer replies, say, "okay, yeah, we'll have a look. Thanks." Now this conversation seems innocent enough, but there's actually a lot going on here. The devil's in the details. So this conversation usually doesn't happen between two engineers sitting next to each other. Usually this conversation happens in the form of helpdesk tickets. This can add lots of waiting. Both engineers have a lot going on, both engineers have to be able to find tickets and respond to them, it can just add a lot of slowness. Furthermore, our software engineer knew a lot about their app, enough to know that something was wrong that wasn't coming from their app, but the problem here is that that engineer didn't know anything about the platform that that software is running on. Conversely, our systems engineer knew everything about the infrastructure that's powering their platform, and they knew enough to know that the platform is fine, but they know nothing about the software that's running on it.
>
> **[1:36](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/so-what-is-this-devops-thing-anyway-24082449?u=76281980&t=96)** So, because neither side knows a lot about the other side, usually there's a lot of undocumented work happening behind the scenes to fix things and this usually manifests with some scripts or documentation, or just some commands and knowledge, and some senior engineers head that get run when things go wrong. But the biggest problem here is that neither engineer knew that stuff was going wrong until it was way too late. Unfortunately, this happens all of the time. This happens when existing software just doesn't work as well, or when new software is getting released. So what does this actually mean in real life? What happens after this conversation's over? Well, work that should have taken minutes to do can often take months or even years to accomplish in the most extreme cases. A byproduct of this is that any downtime that happens can grind operations to a halt, because a lot of the tribal knowledge that people rely on to fix things is hard to find. People just don't know about it. And worse, if our senior engineer decides to leave the company or just go on vacation, then everyone is scrambling to figure out what to do next. The net result of this is that production becomes a scary place. No one wants to make changes because no one wants to rock the boat. No one wants to break anything because no one wants to have to scramble and run around trying to figure out how to fix things.
>
> **[3:14](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/so-what-is-this-devops-thing-anyway-24082449?u=76281980&t=194)** What if things were different? Could there be a better way of operating here? What if our engineers shared knowledge that they knew about each other so that they could know about each other's domains? What if they work together when things go wrong instead of communicating with each other through helpdesk tickets? What if they could feed off of each other through this increased knowledge and increase collaboration to do their jobs better? That's what the [DevOps](../../Topics/DevOps.md) ethos is all about. DevOps is all about bringing engineers together by removing silos, improving communication, and documenting everything as code. Even though the name DevOps implies that this culture is just for developers and operators, DevOps can actually be for everyone. It's important to know that DevOps isn't just a job, it's a culture. It's a way of working. It's a way of being. So you can remove silos, improve communication, and document everything in every part of the engineering process and in every part of the business. Here are some common examples of implementations of this. There's been lots of talk over the years about bringing DevOps to security in the form of [DevSecOps](../../Skills/DevOps/DevSecOps.md). There's also been lots of talk about bringing DevOps to product management through ProdOps or finance through FinOps. We've even seen the DevOps culture be embraced by [Business Development](../../Skills/Data%20Science/Business%20Development.md) in the form of BizOps. Everyone can benefit from working together. There are three big tenets that comprise the DevOps way of being.
>
> **[4:53](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/so-what-is-this-devops-thing-anyway-24082449?u=76281980&t=293)** The first big tenet is having better documentation. DevOps believes that having better documentation leads to better communication. Since you're not relying on tribal knowledge, you're relying on shared knowledge that everybody knows. Better communication can lead to better automation by taking that documentation that we have, taking the more manual grunt work that's a part of it, and automating that stuff away so that people can focus on being the best engineers that they can be. Finally, DevOps believes that all of this can be accomplished through code, because if we can express everything as code, then we have a way of expressing everything in a common language that all engineers and all people within the business can communicate with. In the next video, we're going to learn about Explore California. Explore California is California's premier travel agency, and they're having lots of issues that DevOps can help fix. We're going to take them on a journey to help them embrace all of these things that make companies like Explore California be able to ship better, faster, and more securely.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (12), [DevSecOps](../../Skills/DevOps/DevSecOps.md) (1)
> **CLI Commands:** find (3), make (2)
> **Cross-References:** in the next (1)

#### [Your application, pre-DevOps](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/your-application-pre-devops-24080516?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/your-application-pre-devops-24080516?u=76281980&t=0)** Let's set the scene. Explore California, a fictitious travel planning company, is located in the heart of San Jose, California. Explore California's website, primarily used to connect curious explorers with California's beautiful landscape and scenery, has over 100,000 unique visitors per month, and orders placed through their website account for over 70% of their $5 billion a year revenue. That is no easy feat. Before Explore California, embraced [DevOps](../../Topics/DevOps.md), changes to the website were very painful. In 2021 alone, all of the downtime experienced by the website was enough to send 40% of their users flocking to explore Texas. Consequently, any changes made to production required lots of coordination, largely through lots of paperwork and process. This was horrible for engineering. No one except production engineers were allowed to deploy changes into the production infrastructure, and because production releases only happened once per month, teams spent a lot of time working on merge conflicts across hundreds of branches across many [Git](../../Skills/Software%20Development/Git.md) repositories. Explore California had environmental differences as well. Changes to production needed to pass through the Development, QA, and UAT environments before they are considered ready for production. All of these servers were configured very differently. Dev was created entirely by hand, and the two engineers that set it up left a long time ago. Many teams spent a lot of time waiting on other teams to fix their stuff as a result of this.
>
> **[1:33](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/your-application-pre-devops-24080516?u=76281980&t=93)** It's no surprise that someone says some variant of "It worked in Dev, not sure why it doesn't work in Production" every day in [Slack](../../Skills/Software%20Development/Slack.md). Unfortunately, testing Explore California locally isn't any better. Laptops issued to productions here run [Windows 10](../../Skills/Network%20and%20System%20Administration/Windows%2010.md), which is very different from the versions of [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) they run in production. Furthermore, every developer set up their machine differently, so Explore California looked very different from machine to machine. Running tests was hard and took a long time, so most developers didn't do it, much to the annoyance of QA. Explore California realized that this is very painful. They wanted to ship features faster and more reliably. How do DevOps fix this for them? Let's take a look at the next video and find out.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [DevOps](../../Topics/DevOps.md) (2), [Git](../../Skills/Software%20Development/Git.md) (1), [Slack](../../Skills/Software%20Development/Slack.md) (1), [Windows 10](../../Skills/Network%20and%20System%20Administration/Windows%2010.md) (1), [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) (1)
> **CLI Commands:** git (1), find (1)
> **Env Vars:** uat (1)
> **Cross-References:** next video (1)
> **Tools:** slack (1)
> **Prerequisites:** set up (1)

#### [Your application, post-DevOps](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/your-application-post-devops-24081429?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/your-application-post-devops-24081429?u=76281980&t=0)** After losing another 7% of their traffic to Explore Texas during the last high season, the CTO of Explore California and her team decided that something had to change. After spending two weeks building a value stream map outlining how new features reach customers, she learned that it takes six months for customers to see changes to Explore California. Explore Texas, on the other hand, adds new things to their website every week. She knew they could do better. She created a challenge for engineering, product and operations in hope of fixing this problem: "Build and deploy the smallest change you can to production in two days." Though extremely challenging, Explore California engineering rose to the challenge. Devs learned about how Docker containers make it easy to test Prod-like instances of Explore on their own machines. After seeing how much faster containers made things, every dev team tripled down on it. By moving Explore California to Docker container images and using Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md) to create entire test environments of the website, the engineering team was able to shave off weeks of time from their release cycles. Engineering also realized that talking through tickets and waiting around for people to help wasn't working. After going all in on [Git](../../Skills/Software%20Development/Git.md) for [Version Control](../../Skills/Web%20Development/Version%20Control.md), they thought, "We already have GitLab. Why not use issues to track changes to the site and use merge requests to coordinate releases?" Engineering leadership spent two months getting every team trained up on GitLab. This was time well spent. Engineers were happy that they could communicate in the same place
>
> **[1:33](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/your-application-post-devops-24081429?u=76281980&t=93)** they could see changes to the code. Some teams even added automation to get notifications and updates on [Slack](../../Skills/Software%20Development/Slack.md) automatically. The first release coordinated through merge requests was 20 times faster than before, and they only got faster after that. Finally, engineering leadership realized that the only way they'd be able to fix their downtime was to invest in tests. They paused new features for a month and had teams focus hard on building and automating their tests. Engineering teams also embraced [continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md) to add automated testing to their release process. This paid off in a big way. Not only was Explore California able to enjoy their first downtime-free month after their month-long freeze, but their automated tests sped up their release process by weeks. Three months after accepting the CTO's challenge, Explore California engineering was able to deploy a simple feature into production in two days. Two months after that, engineering was able to release complex features every week. These days they release several times per day. They even have an award-winning mobile app on iOS and [Android](../../Glossary/Sdk/Android.md). They've gotten so good at building and releasing software several travelers have left reviews saying something like, "I thought of something that Explore California could have done better, and their website was updated the next day. That was wild." Doubling down on [DevOps](../../Topics/DevOps.md) pays off.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1), [Git](../../Skills/Software%20Development/Git.md) (1), [Version Control](../../Skills/Web%20Development/Version%20Control.md) (1), [Slack](../../Skills/Software%20Development/Slack.md) (1), [Continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md) (1)
> **CLI Commands:** docker (3), make (1), git (1)
> **Tools:** gitlab (2), slack (1)
> **Env Vars:** cto (2)
> **Code Identifiers:** ios (1)


### 2. Testing Locally with Docker

[↑ Back to Table of Contents](#table-of-contents)

#### [Bring production to your laptop with Docker](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/bring-production-to-your-laptop-with-docker-24078552?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/bring-production-to-your-laptop-with-docker-24078552?u=76281980&t=0)** We learned earlier that engineering at Explore California struggles with their website behaving differently depending on the environment that it's in. This is a common problem in the world of [Software Development](../../Topics/Software%20Development.md). So common, in fact, that it's often called the "it works on my machine" problem in books, conference talks, and one particularly famous meme. There are many reasons why this happens. Sometimes your computer has a lot of tools and software the app depends on that aren't on other machines. Other times, configuration for your app might come from different places depending on where it is. Maybe a block of source code in your app depends on files or [Hardware](../../Topics/Hardware.md) on your computer that other computers don't have. [Configuration Management](../../Skills/Software%20Development/Configuration%20Management.md) tools like Chef, [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) and Puppet solve it by allowing you to write code in markup languages to describe what machines need to have in order to run your app. Other tools like HashiCorp's Vagrant let you write code to create entire [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) to run your app in. However, these tools can be cumbersome for developers who just want to run their code in other places safely. With configuration management tools, you often need to know details about the underlying operating system your markup will run in. With Vagrant, you need to know what "hardware" your virtual machine needs to have, and often need to configure the operating system before installing or building your app inside of it. Docker aims to take a simpler approach. Docker is software that allows developers to package their apps into images that run on containers. Images are created from lightweight configuration files
>
> **[1:33](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/bring-production-to-your-laptop-with-docker-24078552?u=76281980&t=93)** that describe everything your app needs to run. Unlike virtual machines, containers are virtualized operating systems that are configured with just enough to run your app and nothing else. Because containers are created from images, as long as a machine can run Docker, your app will run and behave the same regardless of where it is. As a result, you can build and run your app quickly, safely, and even cost-effectively. In this chapter, we'll install Docker and see how easy Explore California can move their website into a container. In later chapters, we'll take that a step further by using Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md) to automate tests and deploy our site to the greater internet. I'm super excited! I hope you are too!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Hardware](../../Topics/Hardware.md) (2), [Configuration Management](../../Skills/Software%20Development/Configuration%20Management.md) (2), [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) (2), [Software Development](../../Topics/Software%20Development.md) (1), [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) (1)
> **CLI Commands:** docker (5), ansible (1)
> **Prerequisites:** configure (1), install (1)
> **Exercise Files:** source code (1)
> **Definitions:** is a  (1)

#### [Installing Docker](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-docker-24077621?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-docker-24077621?u=76281980&t=0)** Installing Docker is easy. Let's learn how to do that on [Windows](../../Glossary/Service/Windows.md) and the Mac. We will be installing Docker Desktop. Docker Desktop provides a really convenient front end for interacting with containers. Since Docker technically only works on [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) machines, Docker Desktop also handles creating a high performance Linux VM behind the scenes and makes using it easy and seamless. Let's go over a few requirements that you'll need to have in order to install Docker Desktop. On the Mac, you'll need to be running at least macOS 10.15 or newer. On Windows, you need to be running [Windows 10](../../Skills/Network%20and%20System%20Administration/Windows%2010.md) or above. Regardless of whether you'll be using Mac or Windows, you'll need to have at least 4 gigabytes of free RAM on both. Let's start with the Mac. What I'm going to do is open a browser. I'm using Safari in this case, however [Google](../../Glossary/Service/Google.md) Chrome, Brave, Edge, Firefox, whatever you like to use is totally fine. What I'm going to do from here is click on the address bar and visit [docker.com](https://docker.com). On this page I'm going to hover over [Products](../../Skills/Software%20Development/Microsoft%20Products.md), then click on Docker Desktop. Once I'm here, you'll see a big blue button that says "Download for your operating system". So in my case I'm using a Mac on an Apple Silicon chip. However, if you don't see the right operating system or processor that you're using listed here, you can just hover over the button and click on the version that you need. I'm going to click on this button, and then once the download finishes I'm going to double
>
> **[1:35](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-docker-24077621?u=76281980&t=95)** click on Docker.dmg. That's going to bring me to the desktop. What I'm going to do from here is take this Docker icon on the left and drag it over to applications on the right. The applications window might pop up. If it does, that's totally fine. What's important is that you see this copying window over here that's telling you that Docker is being moved into the applications directory and is in the process of being installed. Great! We are installed. So what I'm going to do is click on Docker, or double click on it. Mac will ask you if you are sure you want to open this file. Since we definitely do want to use Docker, I'm going to click Open. Now you'll be asked to accept this subscription service agreement. This doesn't apply for us because we are using it as individuals. If you are a company using this, you might want to read this, but since we're just individuals here, I'm going to click Accept to move on, and now it's going to ask me whether I want to use recommended settings, or if I want to use advanced settings. That allows you to configure things about the virtual machine. We don't need to configure any of those right now. So I'm just going to leave the default option checked. And then I'm going to click Finish. You'll be asked to put in your password. So go ahead and put that in when you're prompted. If you have a watch or something I'll ask you to double tap on your watch or use Touch ID. After I click okay, the installation will continue and then you'll be presented with this window right here.
>
> **[3:10](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-docker-24077621?u=76281980&t=190)** You do not have to sign up to use Docker Desktop, so go ahead and click Continue Without Signing In. Then it's going to ask you to fill out a survey. You can put anything you want here. Let's say we're platform engineers, because we are. And then you can select what you're going to be using Docker for. You can fill this out if you want. I'm going to just check I Don't Know. There's a lot of stuff we're going to be doing with it. And I'll press Continue. And now that we're here, Docker Desktop is ready. One other way of knowing whether Docker Desktop is ready is by paying attention to the whale icon on the taskbar, on the upper right-hand corner. This icon should have a set of boxes here not moving. So let me restart the Docker Desktop engine to show you what I mean. So as you can see, the boxes on the Docker whale will move while Docker Desktop is starting. Once Docker Desktop is ready, those boxes will no longer be moving. If you see an exclamation next to the whale, that is fine. For now, you can safely ignore that. Another way of knowing whether Docker Desktop is running is by opening Docker Desktop, like we have here, and paying attention to the lower left-hand corner. If you see an "engine running" banner over here on the bottom left, then you know that the engine is running. Now there's one final test that you can run to check that Docker is successfully installed, and that's while using Docker itself. Let me show you what I mean. To do that, I'm going to open a terminal...
>
> **[4:46](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-docker-24077621?u=76281980&t=286)** ...by typing "terminal" into Spotlight, and then hitting Enter. And that's going to bring me to our terminal over here. Now to check that Docker is installed you can simply type "Docker" and then hit Enter. You'll get a long help menu over here that tells you that Docker is successfully installed. The next test that you can do to verify that the Docker engine is running is by typing "docker run --rm hello-world". This will retrieve a Docker image called "hello-world" and create a container from it. Because we specified the "--rm" option over here, Docker will delete this container after it's done running this app, which is a simple app that prints a nice "Hello World" style message like the one that you can see on your screen. Installing Desktop on Windows is just as easy. First, I'm going to open a browser. I'm going to use [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) Edge in this case, but again, Chrome, Brave, Firefox, whatever you use is totally fine. From here I'm going to click on the address bar and then visit [docker.com](https://docker.com). Just like we did on Mac I'm going to hover over Products, then click on Docker Desktop. And then from here I'm going to click on the big blue button over here. Like I said earlier, it might not pick the correct operating system and processor type. So all you need to do is just hover over the button and then click on the one that you need. That's going to download our installer, as you can see on the upper right-hand corner.
>
> **[6:22](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-docker-24077621?u=76281980&t=382)** Once that's done I'm going to click on this .exe folder up here to begin the installation process. First, it's going to verify your package. That might take a few minutes depending on the speed of your machine. Once that's done, it's going to ask you whether you want to use Windows Subsystem for Linux version 2, or Wsl2 for short, instead of Hyper-V, which I recommend. It is much more performant, so we're going to leave that checked. And whether you want to have a shortcut added to your desktop, which you can check or uncheck depending on your preferences. I'm going to leave it checked, and then I'm going to click Okay to continue the installation process. Again, this will take a few minutes depending on the speed of your machine, but it should not take very long. Once the installation completes, you'll be asked to restart. Go ahead and click this big button to go ahead and do that. Once your machine restarts, Docker Desktop will start automatically. If it doesn't start automatically, you can start it by either clicking on the Docker Desktop icon on your desktop, if you added it to your desktop, or by clicking the Start Menu and then selecting Docker Desktop. Either way will get you to the same place. Now, just like on the Mac, you'll be asked to accept the subscription service agreement. I'm going to go ahead and accept it. And also, just like the Mac, you'll be asked whether you want to use recommended settings, which will configure your VM automatically, or advanced settings, which lets you tweak various things. I'm going to leave Use Recommended Settings checked and
>
> **[7:57](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-docker-24077621?u=76281980&t=477)** then click Finish. Again, you do not have to sign up to use Docker Desktop, so just click Continue Without Signing In and then you'll get the same survey as before. So I'll select the same answers as before. And then the darker engine will start. Just like on the Mac you can click on the taskbar on the bottom right-hand corner and look for the whale over here, with the boxes moving up and down it back. If the boxes are moving, that means that Docker Desktop is starting. Docker Desktop is ready when those boxes stop moving. Once again, if you see an exclamation mark next to the Docker whale, do not worry, everything is fine. You can also check that Docker Desktop is running by looking for the "engine running" banner on the bottom left-hand corner of Docker Desktop. Just like on the Mac, you can also check that Docker is running in the terminal like I'm about to do now. And then... There we have it. Now we've installed Docker on Mac and Windows. So let's go ahead and use it and get Explore California on containers. I'm excited. Let's go!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Windows](../../Glossary/Service/Windows.md) (6), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (3), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (2), [Windows 10](../../Skills/Network%20and%20System%20Administration/Windows%2010.md) (1), [Google](../../Glossary/Service/Google.md) (1)
> **CLI Commands:** docker (45), rm (2)
> **Tools:** docker desktop (21), terminal (4), firefox (2), safari (1)
> **UI Navigation:** click on (13), select the (1)
> **Prerequisites:** you'll need (3), configure (3), install (1)
> **Analogies:** just like (5)
> **Best Practices:** recommended (3)
> **URLs:** [docker.com](https://docker.com) (2)

#### [Writing your first Dockerfile](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-dockerfile-24080512?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-dockerfile-24080512?u=76281980&t=0)** The life of a Docker container image starts with a single file: the Dockerfile. Dockerfiles are manifests that describe the image that your container will use and what will run within it. When we create the container that will run our website, Docker will do four things with it. First, it will read and parse the Dockerfile. Second, it will [Fetch](../../Skills/Web%20Development/Fetch.md) the parent image that the Dockerfile requested or the scratch base image if you're starting from a blank slate. Third, it will run any commands that are defined within the Dockerfile on top of that base image or parent image. And finally, if defined, Docker will set the image's entry point or the process that will run when this container runs, along with any arguments provided to it. Now, unlike traditional [Configuration Management](../../Skills/Software%20Development/Configuration%20Management.md) tools like Chef, Puppet or [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md), all of the configuration, dependencies and other environment settings that your application needs to run itself can be defined within the Dockerfile. Additionally, while you can write scripts that perform the installation steps that are going to be noted within the Dockerfile, those are not required. So, with that, let's open up our editor and write a really simple Dockerfile for our app. Now, before we dive in, let's take a look at the exercise files bundle that you downloaded for the course. Your exercise file should look something like this. You'll see two folders for each chapter that come with an exercise file: a "Before" folder, and an "After" folder. Our website is going to be in the "02_03_before"
>
> **[1:38](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-dockerfile-24080512?u=76281980&t=98)** folder, as you can see here. We're going to use the terminal to copy this directory into a working directory. That working directory will contain our Dockerfile that we're going to create, along with everything else that we will be creating throughout this journey. Afterwards, we'll write our Dockerfile and build an image from it. Let's open the terminal by typing Command + Space, then typing "terminal" and hitting Enter. Now, let's first make sure that we're in the right place. Your terminal should automatically put you in your home directory, which looks like this tilde over here. If you're not in that directory or if you can't see that tilde, just type "cd ~" to take you right there. Cool. Now that we're all in our home directory, let's create our working directory by typing "mkdir explore- california-website". Finally, let's copy that website folder into it. We can do that by typing "cp -r", then the folder that you downloaded your exercise files into or, in my case, "~/Downloads/ exercise_files... ...then /02_03_before/website". Afterwards, put a space. Then "./ explore-california-website". "." is short for our current directory, which is our home directory
>
> **[3:12](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-dockerfile-24080512?u=76281980&t=192)** right now. We need to specify "-r" over here to cp to tell it that we're copying a directory and want to copy recursively. So once you typed out the command, which should look like this, hit Enter. Once that's done, run "ls ./explore- california-website" to confirm that your website directory was copied over. You should see website like shown here. If you do then you're ready to move on. All right, let's clear the screen by typing Ctrl + L, then let's type "cd ./explore" + Tab to move into our working directory. The Tab autocompletes links, that saves us time so that we don't have to type the entire Explore California website into our terminal. From here, let's type "touch Dockerfile" to create a blank Dockerfile. Then, finally, open Dockerfile in your preferred editor. While I'm using Vim here, any editor like Visual Studio Code, IntelliJ or Eclipse will also work. All Dockerfiles start with a "FROM" command. "FROM" tells Docker which image to use as a base for the commands that we're about to run. While most Dockerfiles will probably use a pre-existing image, like [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) or Debian, you don't always have to. If you want to start from a completely blank slate,
>
> **[4:44](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-dockerfile-24080512?u=76281980&t=284)** you'll want to use from scratch here. Note that blank slate means blank slate. The scratch image is completely empty. You'll need to configure everything yourself, so I don't recommend doing this, but if your situation requires it, then knock yourself out. Let's remove scratch here and make this image "FROM hello-world". That's the same image that we used earlier to confirm that Docker is working. Next we can also add a label underneath FROM. Labels help describe your image to other users. A popular label that gets added is the "maintainer" label. This tells people who to contact if they have any questions or run into trouble. Finishing this line, I'm going to add an equal sign, then a double quote. Then you can add your name here like Carlos Nuñez. Then an ankle bracket and then your email address. Then a closing angle bracket and then a closing double quote. Putting that all together, you should have "LABEL maintainer = your name". And then, in angle brackets, your email address. The next thing that we're going to do is copy that website directory that we created earlier into our Docker image. We can do that by typing "COPY website /". COPY, as the name implies, copies stuff into your image. Very handy for us since we want our image to have a website in it. And there you have it. You wrote your first Dockerfile. As you can see, it doesn't do very much. But don't worry, our Dockerfile will become much
>
> **[6:19](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-dockerfile-24080512?u=76281980&t=379)** more useful as our journey with Explore California continues. Now what I'm going to do is I'm going to save this file and quit my editor. So for those following along with what I'm doing in Vim, what I'm going to do is type Escape or Control + [. Then I'm going to type ":w" to save my file. And then I'm going to type ":q" to exit my editor. Right, now that we're back in our terminal let's clear our screen and type "docker build --tag explore- california-website", and then a dot. Let's break this command down "Docker build" tells Docker that we are going to build a new Docker image. The "tag" option tells Docker that we want to give this image a name, or "explore-california-website", in our case,. The dot at the end is not a typo. This tells Docker where to find its context, or the files it needs to locate everything needed by the Dockerfile. Like we learned earlier, the dot is short for our current working directory, so that will just tell Docker to use explore-california-website, since that's the directory that we are currently in. Now let's run this and watch Docker do its thing. Cool. It looks like this image was successfully built and tagged. Let's verify that by running Docker images to make sure that it exists in the list of images on my computer. As you can see, explore-california-website is in the output that Docker image is generated.
>
> **[7:54](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-dockerfile-24080512?u=76281980&t=474)** There it is, we successfully built our app's first container image. Now, if you ever want to find out more about what any particular command does, you can always run "docker", then the command like "build --help". That will give you a help menu like we saw earlier when we tested Docker. Finally, let's run it. This part's really easy. Let's clear our screen to organize, and then all we have to do here is run "docker run explore-california-website". This will create a container from our explore-california-website Docker image. Since Docker does not delete containers by default after they're done doing their thing, I like to add "--rm" before the image name, or "explore-california-website" in this case, to have Docker do that. Let's hit Enter and see what happens. As you can see, we see a "Hello from Docker!" message over here. That confirms that our image was successfully built. So there you have it. We did it. We built a Docker container image.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (1), [Configuration Management](../../Skills/Software%20Development/Configuration%20Management.md) (1), [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md) (1), [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) (1)
> **CLI Commands:** docker (24), make (3), cd (2), cp (2), find (2)
> **Tools:** terminal (6), vim (2), visual studio (1), intellij (1)
> **Env Vars:** copy (2), label (1)
> **Exercise Files:** exercise file (2), exercise files (1)
> **Definitions:** short for (2)
> **Prerequisites:** you'll need (1), configure (1)
> **Code Identifiers:** exercise_files (1)

#### [Writing your first Docker Compose manifest](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-docker-compose-manifest-24075618?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-docker-compose-manifest-24075618?u=76281980&t=0)** We recently wrote a Dockerfile to build a Docker image that will run a local copy of Explore California, similar to what we would have in production. We used Docker commands to get a container of that image running, and we confirmed that our container works from using "docker build" and "docker run" to build and run our image, respectively. However, what if we want to run a website along our website or test some persistence code? What if we want to run a separate site to test that Explore California can talk to it? Now we could use Docker commands to create that infrastructure pretty easily. But how do we link all of that together? We could run a Docker command for every piece of infrastructure we want to stand up, and then use container networks to connect them all. We could even write scripts to help us automate this. You definitely don't want to do this. Keeping track of the containers you're starting and stopping with Docker commands will get really tiring once you're dealing with tens, hundreds, thousands, or tens of thousands of containers. And that's before spending the effort maintaining all of those scripts. Container networks also work really well. But what happens when you want to run multiple instances of Explore California on the same machine? It gets tricky. Enter Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md). Docker Compose is a simple and lightweight platform for running and linking multiple containerized apps together. Docker Compose uses two things to accomplish this.
>
> **[1:34](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-docker-compose-manifest-24075618?u=76281980&t=94)** First, it uses Compose manifests written in YAML to define the containerized apps that will run within the Compose network and the Docker Compose command-line tool to parse and create containers from that manifest. Best of all, the containers that run within Compose use pure Docker under the hood to run and manage everything. So let's write a Compose manifest to run our Explore California website within Docker Compose. We will then use the Docker Compose [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) tool to run that manifest and, like we did for docker run, we'll confirm that the container image for our website starts and that everything works as expected. Cool. So let's go ahead and open our terminal to get started. Before we begin, let's make sure that you're in the Explore California website directory that we created earlier. If you're not, you can always run "cd ~/explore-california-website" to bring you right back in. Once we've done that, we'll use the "touch" tool like we did earlier to create a new file called "docker-compose.yaml". We could name this whatever we want, but the Docker Compose command-line tool looks for files with this name by default. So we'll just use that to keep things easy. Now that we've created this file, let's open it and do some stuff. Every docker compose file starts with the line services. You'll see many Compose manifest files
>
> **[3:07](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-docker-compose-manifest-24075618?u=76281980&t=187)** start with "version", like this, but that line is now optional in recent versions of Docker Compose. "Version", well... describes the version of the Compose manifest schema that you're going to be using. This is often used to enable older Compose features that work better with certain kinds of services or situations. But to keep things simple, we're going to go ahead and leave that out for now. Okay, back to services. The simplest way to create a service looks like this. This will create a service called "service_name" that will build an image from the Docker Hub called "image_name". While we know that this won't work because we don't have this or this, let's dissect what's going on here to figure out what Compose will see. "Service_name" is the name of the service. This can be anything. Notice how it's indented two spaces to the right of services. This is intentional, as YAML expects an object of services underneath it. Why an object? Well, I could probably spend three courses explaining the fine nuances of YAML and [JSON](../../Skills/Web%20Development/JSON.md), but no one wants to see those, and [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) won't hire me to record them, so just trust me on this one. Image tells Docker the name of the image to use for the service. By default, Compose will expect this image to come from Docker Hub or a private Docker registry. "Hold up, Carlos. Our image isn't in the Docker Hub. How will Compose know what to do with it?," you're probably asking. Fortunately, Compose understands how to build local Dockerfiles,
>
> **[4:41](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-docker-compose-manifest-24075618?u=76281980&t=281)** so let's rewrite our service name example to fix that. So I'm going to delete this by typing D twice. Then I'm going to press A to go back up to service name, then an Enter. And then I'm going to type "build:". And then on the line I'm going to type four... six spaces. And then I'm going to type "context:", space and then a dot. So all we needed to do is simply replace "image" with "build" and then provide the arguments that we would feed to "docker build" right underneath it, which is indented two spaces to the right of build, or six spaces from services. So there we have it, our first Docker Compose manifest. Just like our Dockerfile, this does not do very much, but that will change before you know it, so don't worry. So let's go ahead and save this and then go back into our terminal to start the service that we just created. Starting services with Docker Compose is very easy. Let's clear our screen to organize ourselves and then run "docker-compose up". This will start all of the services that we created in our Compose manifest. Very easy. So as you can see, we see the Hello World message that we saw earlier, along with some additional information about which service this message came from, or in our case, the service_name service. When we're done with the services in our Compose manifest, all we have to do is run "docker-compose down". This will delete any containers, networks and temporary volumes
>
> **[6:17](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-first-docker-compose-manifest-24075618?u=76281980&t=377)** that compose created while your services were running. Now, in newer versions of Docker Desktop you can also type "docker compose up", notice that there's no hyphen here, to do the exact same thing that we did before. And you can also run "docker compose down". Again, it's the exact same. Your preference. While this Compose manifest doesn't do very much right now, it will become super important when we help move Explore California into the cloud. However, if you're itching to learn more about Compose, check out Nicole Rifkin's excellent course "[Learning Docker Compose](Learning%20Docker%20Compose.md)", right here on LinkedIn Learning.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (33), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **CLI Commands:** docker (33), make (1), cd (1)
> **Env Vars:** yaml (3), cli (1), json (1)
> **Code Identifiers:** service_name (2), image_name (1)
> **Tools:** terminal (2), docker desktop (1)
> **Analogies:** similar to (1), just like (1)
> **File Paths:** docker-compose.yaml (1)
> **Definitions:** is a  (1)


### 3. Getting Started with Git

[↑ Back to Table of Contents](#table-of-contents)

#### [Version control: The secret weapon of DevOps](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/version-control-the-secret-weapon-of-devops-24078553?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/version-control-the-secret-weapon-of-devops-24078553?u=76281980&t=0)** Writing code is easy. Explore California is really good at this, otherwise they wouldn't be California's premier travel agency. Storing source code, however, is a little trickier. What happens when an engineer accidentally writes a bug and needs to roll things back? What if some engineers want to work together on a feature but they live three hours apart? How can Explore California easily maintain different versions of their recommendation service, for example, that helps travelers choose awesome activities based on their preferences. [Version Control](../../Skills/Web%20Development/Version%20Control.md) systems were built to solve these exact problems and more. VCSs, for short, are tools that make it easy for teams to write software together safely. VCSs track every change made to code. This enables teams to do things with their projects that used to be really difficult in the past, like rolling back buggy changes or maintaining multiple versions of code at the same time. While there are many VCSs out there, almost all of them fall into two categories: centralized VCSs and distributed VCSs, or DVCSs for short. Centralized VCSs require developers to "check in" their changes to one or more servers whenever they make changes. While these systems ensure that there's only one "true representation" of source code out there, developers working on software tracked by these systems need to have a connection to those servers at all times. This can be challenging for developers with slow network connections,
>
> **[1:33](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/version-control-the-secret-weapon-of-devops-24078553?u=76281980&t=93)** for example. SVN, CVS, and Perforce are some common examples of centralized VCSs that are still in use today and are quite popular, actually. Decentralized VCSs take a different approach. These systems allow developers to store source code projects and their histories on their machine, and track changes completely locally. While developers can push changes up to a centralized server, it is not required. While this works really well for ensuring that developers everywhere can contribute to projects, you never really know which version of a repository is the "true version" in a decentralized model. Ensuring that source code history is correct becomes challenging as well, since there can be many different versions of a codebase that have different sets of changes applied onto them. Mercurial, Fossil, and Explore California's favorite, [Git](../../Skills/Software%20Development/Git.md), are some examples of these version control systems. Explore tried many of these systems, they really favored Git, though. They liked how easy it was to create and unify different representations of their software. While Git's command-line tool was a little challenging to learn at first, they really appreciated how flexible it can be. Now, we're going to do some cool things with Git in this course. But before we do, let's learn some common words and phrases that we'll use during our journey. A repository is a place where source code and its history lives. The history part is important since that's a big part of what Git tracks when changes are made.
>
> **[3:06](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/version-control-the-secret-weapon-of-devops-24078553?u=76281980&t=186)** Copies of a repository that live in centralized infrastructure are considered remote repositories, or remotes for short. Changes made to local copies of these repositories are pushed up to remotes. Speaking of local copies, let's talk about clones. A clone is a decentralised copy of a repository that you can pull into your machine. Every change you make to the clone is stored locally. Now you can push those changes back up to the remote at any time, but you also don't have to. It is your preference. Branches are different representations of code in a repository. Since Git tracks the history of how source code changed, it uses branches to track multiple histories simultaneously. You can think of branches like versions of a repository. The "official state" of the code is stored in a trunk branch. This is usually called "main," but it goes by many different names. Bringing code changes from two branches together is called a "merge." As you can imagine, this can be really simple or really complicated depending on the changes being unified. We're going to learn how to install Git in the next video. After we install Git, we're going to use it to create or initialize Explore California's repository. Once that's done, we'll start learning a really simple workflow that we'll be using to track, log and push changes. However, I want to get something controversial out of the way now. We are going to use Git's command-line client, or the Git
>
> **[4:42](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/version-control-the-secret-weapon-of-devops-24078553?u=76281980&t=282)** [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) for short, in this course. Now, I'm not going to lie, there are definitely thorny areas within the Git command-line client. However, there are three big advantages that you will gain from getting comfortable with using Git in the terminal. First, if you work with multiple systems per machine, the Git client is much more likely to be installed on those systems than, say, Tortoise. That only makes you infinitely more productive in my opinion. Second, most of the graphical clients that exist use the Git CLI under the hood. That's not an accident. The maintainers of Git and every feature that Git is capable of is available in the CLI client first and foremost. You might learn how to do something with Git and VS Code, for example, but if you know how to do it with the Git CLI, you can do that thing everywhere and anywhere at any time. Finally, and most importantly, knowing how to use the Git CLI makes you super valuable in the job market. All right folks, let's strap ourselves in and get going with Git. Yeehaw!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (20), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (5), [Version Control](../../Skills/Web%20Development/Version%20Control.md) (2)
> **CLI Commands:** git (20), make (3)
> **Env Vars:** cli (5), svn (1), cvs (1)
> **Exercise Files:** source code (6)
> **Analogies:** for example (3), imagine (1)
> **Definitions:** is a  (3)
> **Tools:** terminal (1), vs code (1)
> **Prerequisites:** install (2)

#### [Installing Git](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-git-24078551?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-git-24078551?u=76281980&t=0)** Before we get going with [Git](../../Skills/Software%20Development/Git.md), let's learn how to install it on [Windows](../../Glossary/Service/Windows.md) and the Mac. Let's start with the Mac. To install Git, all you have to do is type "git". That's it. On some installations of macOS, a pop up will appear asking you to install the macOS command-line tools. If you see this, just click Okay to begin the installation and it will only take a few minutes to complete. Once that's done, type "git --version" like you see on the bottom of the screen to confirm that git was installed correctly. This should print a version string similar to this. Now let's take a look at how to install it on Windows. To begin, click on the Start button. Search for your preferred browser, or Edge in this case, and open it. After the browser opens, click on your address bar and visit [gitforwindows.org](https://gitforwindows.org). This will bring you to the official Git project for Windows. Once you're here, click on the big Download button to download Git. Git should automatically download. There it is. Once it's done, just double-click it to begin the installation. Once the wizard pops up, go ahead and click Next. The installer will ask you where to install Git. You can leave the default folder there. Click Next. Again over here it will ask you to select components that you'd like to install. You can just leave all of these as is and then click Next to continue.
>
> **[1:32](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-git-24078551?u=76281980&t=92)** This is telling you what Git will look like in the Start Menu. Go ahead and click Next to continue. Then this allows you to select the default editor you'd like to use. You can select your preferred editor over here, or you can leave the default, which is Vim, and then click Next to continue. Over here you can select the default branch name for new repositories like we learned earlier. Every Git repository has a trunk repository that the "official version" of the repository lives in. A lot of projects use "main" for this. If you let Git decide, it will use "master" as the default name. I prefer "main", so I'm going to click Override the Default Branch Name and then click Next. This allows you to use Git from either Git Bash, which is the terminal that Git comes with, or any other third-party software like the terminal application that we were using earlier. I'm going to leave this option checked and then click Next to continue. This window allows you to select the SSH client that Git uses to [Fetch](../../Skills/Web%20Development/Fetch.md) remote repositories. I recommend using the Bundled OpenSSH option, as the other option will require you to install SSH yourself, which you don't want to do and neither do I. So I'm just going to select this one and click Next. Just like the screen before this, this allows you to select the OpenSSL library, which is what's used for presenting certificates when you clone repositories from their website variants,
>
> **[3:09](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-git-24078551?u=76281980&t=189)** which we'll see an example of later. Go ahead and use the default option, then click Next. This determines the line endings that Git uses. I highly prefer using Unix-style line endings, as Windows style line endings tend to cause problems in a lot of Git projects. So let's go ahead and check Commit Unix-Style Line Endings and then click Next. Here we get to select the terminal emulator that Git Bash uses. It can either be cmd or mintty, which I prefer, so we'll go ahead and leave that checked and then hit Enter. This screen allows you to select the default behavior that the Git "pull" command, which we'll learn more about later in this course, uses. I recommend leaving the default here and then clicking Next. This allows you to select where secrets, like username and passwords when you're cloning Git repositories, lives. We can leave this as is and then click Next. These options over here allow you to tweak the performance of Git. You can leave these as is and then click Next. And you can leave this as is and click Install since we won't be using any experimental options. That was a lot, but we're finally there. We're installing Git. Once this finishes, we should be able to use it. Great. So now the installation is finished let's uncheck View Release Notes, since we don't need to see that right now, and then click Finish. Alright. Now that we've installed Git, let's go ahead and use it. To do that I'm going to click on the Start Menu.
>
> **[4:41](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/installing-git-24078551?u=76281980&t=281)** I'm going to type "terminal". And then I'm going to hit Enter. Once here, all we need to do is the same things we did on the Mac. First we'll type "git" to make sure that it's installed. And then we'll type "git --version" to verify the version of Git that we have. In this case, here it is, that confirms that Git is installed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (28), [Windows](../../Glossary/Service/Windows.md) (4), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **CLI Commands:** git (28), ssh (2), make (1)
> **UI Navigation:** select the (6), click on (4), double-click (1)
> **Prerequisites:** install (8)
> **Tools:** terminal (4), bash (2), vim (1)
> **Code Identifiers:** macos (2)
> **Env Vars:** ssh (2)
> **Analogies:** similar to (1), just like (1)

#### [Initializing your first Git repository](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/initializing-your-first-git-repository-24080518?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/initializing-your-first-git-repository-24080518?u=76281980&t=0)** Now that we've installed [Git](../../Skills/Software%20Development/Git.md), let's initialize our first Git repository. To begin, let's make sure that we're in the Explore California website directory. If you're not, you can just type "cd ~/explore-california- website" to take you right there. Initializing a Git repository is very easy. All you need to do to do that is type "git" in it. That's it. This folder is now a Git repository. Wild stuff. This command adds a hidden folder called ".git". We can see it by running "ls -a". Here it is. This folder, amongst other things, adds a few small files that will track the changes to your code. In the next video, we'll save our changes by committing our exercise files to our local Git repository. Progress!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (7)
> **CLI Commands:** git (7), make (1), cd (1), ls (1)
> **Cross-References:** in the next (1)
> **Exercise Files:** exercise files (1)

#### [Getting familiar with the Git workflow](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/getting-familiar-with-the-git-workflow-24079509?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/getting-familiar-with-the-git-workflow-24079509?u=76281980&t=0)** Just because you initialized this directory as a [Git](../../Skills/Software%20Development/Git.md) repository doesn't mean that Git is automatically tracking your work. We'll need to run a few more commands to tell Git about the files that we want to version with it. But, before we run those commands, let's take a look at the workflow that we're going to be using. We'll be adding more stuff to this workflow as we go on throughout this course. The first thing we'll do is add files into our staging area. That's represented up here on the top. The staging area is the place where Git actually tracks files. We'll use the "git add" command to do that. Afterwards we'll move down clockwise to the second box over here, where we'll record or "commit" our changes to our repository. Committing tells Git about the changes that you want to save into your repository. We can do that with the "git commit" command. We'll be able to add a message to the commit describing what we changed and why. Once we're ready to commit all of our changes, we move over to the left where we push them into a remote repository using "git push". We'll do that once we set up our remote repository later in the course. So, with that out of the way, let's head back into our terminal and give this workflow a test drive. So I've run "ls -a" over here to show us all of the files that are in our Git repository. I want to track all of these files. So to do that I'm going to run "git add .". Remember, "." is our current working directory.
>
> **[1:37](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/getting-familiar-with-the-git-workflow-24079509?u=76281980&t=97)** So this will tell Git to stage all of the files in our working directory into the repository that we created. As you can see, "git add" returns nothing after doing this. This means that our staging was successful. Now if I wanted to track a single file, like let's say I've made changes to just the Dockerfile, I could run "git add Dockerfile" over here instead, but we don't need to do that because we ran this command already. Now that we've staged our code, it's time to tell Git we mean business. And we do that by committing our changes. So let's first run "git status" to see what Git is tracking. As we can see, there is a lot that gate is tracking. It's tracking our entire website and it's tracking our Dockerfile and our docker-[compose](../../Glossary/Framework/Jetpack%20Compose.md).yaml manifest. What it isn't tracking is this ".git" directory. That is a private directory that is just for your local Git client. This will never get tracked. So, now let's go ahead and create a commit to save these changes. So I'm going to type "git commit" and hit Enter. Git will open your default code editor upon doing this. In most terminals this is going to be VI. Not Vim, VI, there is a difference. However, if you want to change that, you can simply do ":q" like I did earlier, and then just type "EDITOR" in caps, equals, and then the editor of your choice.
>
> **[3:10](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/getting-familiar-with-the-git-workflow-24079509?u=76281980&t=190)** Let's say it's Vim, or Visual Studio Code, and then type "git commit" right after that. There are a lot of opinions about what your "git commit" message should look like, so I'll just show you my favorite. I'm going to type "I" to enter insert mode and begin typing. The first lines of my commit messages are really short summaries of the change that I'm going to make. These are usually less than 50 characters in length. We call this the summary of the commit. Since we've begun tracking all of Explore California's site to our repository for the first time, I'll type "initialized git repository" here. Now this command is really simple, so I could just end the message here. However, some commits require slightly more explanation. If I wanted to add more information about this commit, I would add two new lines, or hit Enter two times, and then I'd write whatever I wanted to write here. This area is called the "description." You can add as much stuff as you'd like to add here. I've seen commit messages that had enough paragraphs to create a short novel. The nice thing about the setup is that the only message fellow Explore California engineers will see when they're working in [GitHub](../../Skills/Software%20Development/GitHub.md) or GitLab or something like that, is the summary, which is our first line at the top. It's quick and easy. So I could add a description here like "added the website, our Dockerfile and our docker-compose.yaml manifest." We could add that, but this commit is really simple. So I'm just going to go ahead and remove this.
>
> **[4:45](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/getting-familiar-with-the-git-workflow-24079509?u=76281980&t=285)** However, sometimes it's easier if you can't come up with a summary for your commit to type the description first and then once you type the description, then you think about how you want to summarize that description and then make that the summary of your commit. So let's go ahead and get rid of that. Just to review the commands that I used in Vim, I typed capital V to highlight my line, then I typed J to move down the line, Then I typed X to get rid of all that junk, and then I'm going to save this command by typing ":wq". That's W to write our changes and Q to quit. Upon doing that, Git tells us that our changes are tracked by saying absolutely nothing. But you can confirm that by typing "git status" and then seeing that Git tells us that our working tree is clean, that just means that our commit is tracked and ready to be pushed up. We'll push it up later in the course when we set up GitLab.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (26), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (2), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **CLI Commands:** git (26), docker (2), make (2), ls (1)
> **Tools:** vim (3), gitlab (2), terminal (1), visual studio (1), github (1)
> **Definitions:** means that (2), is a  (2), we call this (1), is called (1)
> **Prerequisites:** set up (2), setup (1)
> **File Paths:** docker-compose.yaml (2)
> **Env Vars:** editor (1)
> **Cross-References:** later in (1)


### 4. Collaborative Version Control

[↑ Back to Table of Contents](#table-of-contents)

#### [Beyond local repositories](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/beyond-local-repositories-24079510?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/beyond-local-repositories-24079510?u=76281980&t=0)** Earlier, we learned that [Git](../../Skills/Software%20Development/Git.md) is a decentralized [Version Control](../../Skills/Web%20Development/Version%20Control.md) system, or a DVCS. This allows you to develop your code locally without having to push your changes to a centralized server. Developing locally is nice, but eventually you're going to want to collaborate with other people. This means that eventually you're going to have to merge code together. So what do you do when it's time to merge? In the past, this was usually done over email with patch files. This was slow and very cumbersome. However, and surprisingly, Git actually still supports merging changes this way. This is because Git was actually built by Linus Torvalds, who was the founder of [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md), and emailing or posting diffs was a big part of how Linux was built. You could also email an entire repository to other people in compressed format, but this is also very slow. Finally, you could use the Git [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) to push changes to a remote repository and then use commands like "git merge" or "git rebase" to merge in all of these changes. But just saying all of that is crazy complex, and it does require a lot of commands, and it does get complicated very quickly. This is why over time, development platforms were built. Development platforms are systems that make it easy for developers to work on source code together, while retaining all of the benefits of distributed version control. This is accomplished in three big ways. First, development platforms provide code search.
>
> **[1:35](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/beyond-local-repositories-24079510?u=76281980&t=95)** Code search makes it easy to find what you're looking for, no matter how large or small the codebase is. Development platforms also provide visual histories that make it easy to see how your code has changed over time. However, and in my opinion, by far the biggest benefit that development platforms provide is the idea of a "pull" or a "merge request." Pull or merge requests allow developers to perform just-in-time [Code Review](../../Skills/Software%20Development/Code%20Review.md). This is really nice because developers could gather together and review a change together before they push those changes into a trunk branch like main. This is an alternative to doing a code review like some of the ones that I've been part of, and some of the ones that I've caused, where you create a single commit that has thousands of lines of changes that are spread across hundreds of files. No one wants to see that. And merge requests make this much easier to go through. Pull or merge requests also provide an audit trail of decisions that were made during a merge. This can be really helpful for keeping track of changes that occurred during software releases. Many of these tools also come with a ton of additional features like [Project Tracking](../../Skills/Network%20and%20System%20Administration/Project%20Tracking.md), static webpage hosting, and my favorite, built-in CI/CD, or [continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md) and [continuous delivery](../../Skills/Software%20Development/Continuous%20Delivery%20(CD).md). We'll learn about both of those things later in this course. I'm very excited! There are many development platforms available on the market today. These are just an example of a few of them. In the next video, we'll talk about the development
>
> **[3:08](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/beyond-local-repositories-24079510?u=76281980&t=188)** platform that Explore California decided to go with and how we're going to use it throughout this course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (6), [Version Control](../../Skills/Web%20Development/Version%20Control.md) (2), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (2), [Code Review](../../Skills/Software%20Development/Code%20Review.md) (2), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (1)
> **CLI Commands:** git (6), make (3), find (1), cd (1)
> **Definitions:** is a  (1), means that (1), is an  (1)
> **Env Vars:** dvcs (1), cli (1)
> **Cross-References:** later in (1), in the next (1)
> **Exercise Files:** source code (1)

#### [Introducing GitLab](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/introducing-gitlab-24080514?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/introducing-gitlab-24080514?u=76281980&t=0)** In the last course, we learned about development platforms that make [Version Control](../../Skills/Web%20Development/Version%20Control.md) and team-wide collaboration easy. [GitHub](../../Skills/Software%20Development/GitHub.md), [Bitbucket](../../Skills/Software%20Development/Bitbucket.md), and GitLab were some of the examples provided. In this course, we're going to be using the open-source version of GitLab. GitLab was created by Dmytro Zaporozhets and Sid Sijbrandij in 2011. GitLab was created as an open-source alternative to platforms like GitHub that you can install on your own systems. Like the development platforms that we saw in the last video, GitLab provides [Git](../../Skills/Software%20Development/Git.md) for version control, of course, but it also provides a lot of other neat tools, like its own build system called GitLab CI, project management tools, webpage hosting, and more. You can install GitLab on just about anything, from Raspberry Pis to extremely large [Kubernetes](../../Skills/DevOps/Kubernetes.md) clusters on your cloud provider of choice. GitLab is also very popular. It's used by some of the largest open-source projects on the internet, and some of the largest corporations in the world. It's even used by governments and banks in completely offline environments. In this course, we're going to help Explore California move into GitLab. We're going to use Git to version control our software with it, and later in this course use GitLab CI to build and deploy our websites into the cloud.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Version Control](../../Skills/Web%20Development/Version%20Control.md) (3), [GitHub](../../Skills/Software%20Development/GitHub.md) (2), [Git](../../Skills/Software%20Development/Git.md) (2), [Bitbucket](../../Skills/Software%20Development/Bitbucket.md) (1), [Kubernetes](../../Skills/DevOps/Kubernetes.md) (1)
> **Tools:** gitlab (10), github (2), bitbucket (1)
> **CLI Commands:** git (2), make (1)
> **Cross-References:** in the last (2), later in (1)
> **Prerequisites:** install (2)

#### [Creating the GitLab Docker Compose service](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-the-gitlab-docker-compose-service-24077626?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-the-gitlab-docker-compose-service-24077626?u=76281980&t=0)** In this video, we're going to learn how to run GitLab on our computer locally with Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md). This will help Explore California host GitLab on their own servers. To save time, the exercise files for this chapter contains the Docker Compose manifest you'll need to start GitLab. That manifests is located in the 04_ 03_before directory. So let's go ahead and grab it. The first thing that we're going to make sure of is that we're in the Explore California website directory. If you're not there, just run the "cd" command that we learned about earlier to take us right back in there. From here, let's run "cp -r" and then the folder that you stored your exercise files in. So in my case that's going to be home directory slash downloads slash exercise files, followed by 04_03_before, followed by docker-compose.yaml. And then we're going to go ahead and store that in our directory here called docker-compose.yaml, like this. Now, note that this will overwrite the Docker Compose manifest that we created earlier. However, the one that we're about to copy into our working directory is the one that we're going to be using for the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the course, so you're not missing anything by doing this. Alright, once you're done with that, go ahead and open docker-compose.yaml in your editor. Now, there's a lot of stuff going on here. Fortunately, we're not going to look at all of this stuff right now.
>
> **[1:32](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-the-gitlab-docker-compose-service-24077626?u=76281980&t=92)** We'll break it up piece by piece throughout the course. However, the service that I want you to pay attention to right now is the GitLab service down here. Let's go ahead and take a closer look at the service to see what's going on. Now, to make it easier for me to refer to stuff that I'm looking at, I'm going to go ahead and type in ":set number", Enter, to enable line numbers like you see over here on the left. So let's start with line number 102 which I can get to easily by typing ":102". This is the container image that we're going to use to start up GitLab. So like we learned about earlier when we were experimenting with the build keyword, this allows us to select an image that is in the Docker Hub or in a private repository instead of building our own. If you are doing this on an Intel-based machine, you're going to want to uncomment line 99. Like this, I pressed X two times, and then delete line 102 by going to 102, and then pressing D twice. That will select the Intel version of GitLab. If you're using an Apple-Silicon-based Mac or you're using an Arm-based [Windows](../../Glossary/Service/Windows.md) machine, you can leave the Docker Compose file as is. The next property to look at is the host name. Eventually we're going to use GitLab in a browser. So this is the address that we're going to need to reach GitLab. Now you've probably noticed that gitlab.[example.com](https://example.com) is probably not a real fully-qualified domain
>
> **[3:09](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-the-gitlab-docker-compose-service-24077626?u=76281980&t=189)** name, or a real FQDN. That is okay. We're going to perform a little bit of a sleight of hand to work around this. That, we'll do in a bit. Next up are the list of ports down here on lines 127 through 130. This property tells Compose to map or expose network ports within the container to ports on your machine. The number to the right of the colon, like we can see here on line 128, is the port within the container to be exposed. The number on the left is the port within your host that this port will be mapped to. We're mapping three ports to make GitLab work within Docker compose: 80, 443 and 22. 80 and 443 use the same ports on your host as a container, No surprises there. However, port 22 is a little interesting. This is the default port for SSH. Many machines might already be running an SSH server in the background, so if we try to use port 22 on our machine as well, Docker Compose will likely complain that this port is already in use. To avoid that, this example maps port 22 on the container to port 2222 on our machine. While GitLab can still fail to start if something is using port 2222 on our machine, this is much more unlikely to happen. Now, as you can see, there are a lot of properties that I am not covering here just to save time. If you're interested in learning more about what each of those properties do, I would encourage you to check out the Docker Compose documentation. Getting there is really easy.
>
> **[4:43](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-the-gitlab-docker-compose-service-24077626?u=76281980&t=283)** All you have to do is open a browser and visit docs.[docker.com/compose](https://docker.com/compose). You can also view one of the many courses on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) learning that go super in-depth into Docker Compose. Alright, now that our Compose service for GitLab is ready to go, it's time to start it. We'll do that in the next video. So before we do that, let's go ahead and close this file and prepare our terminal to do that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (14), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Windows](../../Glossary/Service/Windows.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** docker (13), make (3), ssh (2), cd (1), cp (1)
> **Tools:** gitlab (12), terminal (1)
> **Ports:** port 22 (3), port 2222 (2), :102 (1)
> **File Paths:** docker-compose.yaml (3)
> **Env Vars:** ssh (2), fqdn (1)
> **Exercise Files:** exercise files (3)
> **URLs:** [example.com](https://example.com) (1), [docker.com](https://docker.com) (1)

#### [Starting the GitLab Compose service](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/starting-the-gitlab-compose-service-24080519?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/starting-the-gitlab-compose-service-24080519?u=76281980&t=0)** Let's continue on and start up GitLab. Starting GitLab is straightforward. We can do it in three steps. First, we're going to create a virtual network for our GitLab containers to talk to each other, called "gitlab". For my networking folks watching this, you can think of container networks like private VLANs that are entirely operated by Docker's container runtime. We'll do this by typing "docker network create gitlab". Done. This long IDE over here is Docker's way of telling us that the network was created. Next, we need to make gitlab.[example.com](https://example.com) reality. We can trick our system into doing that by adding a line into a special file called /etc/hosts, or as it's lovingly called, our /etc/hosts file. Every application that is internet or network-facing checks this file for DNS records first before asking name servers on the network or the internet for clues. By doing this, we're basically telling our browser that gitlab.[example.com](https://example.com) is and always will be whatever IP address that you decide to give it. In this case, since we want this address to point to our machine, we're going to use the special loopback address 127.0.0.1 to accomplish that. This is a really neat trick. So let's go ahead and do that now. I'm going to delete all of that stuff. And instead I'm going to write "sudo sh -c" and then, in double quotes, I'm going to type "echo", then a semicolon, then "echo", and then a single quote, 127.0.0.1,
>
> **[1:38](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/starting-the-gitlab-compose-service-24080519?u=76281980&t=98)** then a space, then "gitlab.[example.com](https://example.com)". Then a closed single quote followed by two right brackets, or right angle brackets rather, and then, following that, our /etc/hosts file. And then finally we're going to close this with a double quote. So a couple of things to note. We're going to make sure we have two single quotes. We're also going to make sure that our two right angle brackets are inside of the double quotes over here. And you're also going to make sure that you have "echo" spelled out twice like we have here. So what this command is going to do is it's going to use echo to write an entry into our /etc/hosts file that basically tells our system, "Look, I don't care what [Google](../../Glossary/Service/Google.md) says, I don't care what my ISP says. Gitlab.[example.com](https://example.com) is and always will be on our own machine no matter what." Now, we're providing echo twice here because on some systems, running this without echo twice will just add this line to the end of /etc/hosts, which can cause problems. So this echo over here guarantees that this line that we're adding in single quotes will be on its own line. Now, before we hit Enter, let's make sure of one really important thing. You want to make sure that you have two right angle brackets here, not one. The reasoning for that, and the reason why I'm emphasizing this is because if you only use one /etc/hosts will be overridden entirely by this statement over here.
>
> **[3:14](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/starting-the-gitlab-compose-service-24080519?u=76281980&t=194)** There are some applications that depend on /etc/hosts being properly maintained so that can mess stuff up. So we want to make sure that we have two right angle brackets to prevent that. Since we have two over here, we are safe to run this command. So I'm going to go ahead and hit Enter. If you're asked for your password go ahead and enter it. And then what I'm going to do is I'm going to cat /etc/hosts to make sure that it looks okay. And there we go. What we want to see here when we run this command, is that "127.0.0.1 gitlab.[example.com](https://example.com)" is on its own line at the bottom of the file. Now that we ran this command, let's open a browser and visit gitlab.[example.com](https://example.com). Now if you have Safari it might try to auto resolve this to [gitlab.com](https://gitlab.com). That's not what we want. So let's go ahead and type [https://gitlab](https://gitlab) .[example.com](https://example.com). As you can see, we get an error message when we try to go there. And while DNS is usually the root of all problems, in this case this one is pretty easy. GitLab just isn't ready yet. We haven't started it yet, so let's go back into our terminal to fix that. To start GitLab, all we need to do is —let's clear our screen to organize— "docker [compose](../../Glossary/Framework/Jetpack%20Compose.md) up gitlab". We can see that a lot of stuff is happening here. So let's just take a few minutes to leave it be and let GitLab start. There's a lot of moving parts that help make GitLab as awesome as it is.
>
> **[4:48](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/starting-the-gitlab-compose-service-24080519?u=76281980&t=288)** So while all of those parts are starting, go ahead and take a long walk, make some coffee, or just simply step away while GitLab initializes. Have fun and I'll be here when you get back. Alright, welcome back. Let's go back to our browser and see whether GitLab is ready yet. Now, if I go to my address bar and type in gitlab.[example.com](https://example.com). As we can see we get a "connection is not private" error message. This is a little different than what we saw before, but it's actually great progress. This means that our GitLab instance is working. However, our browser doesn't know about the self-signed certificate that GitLab created for itself. This is completely safe in this case. So let's click Show Details and then click Visit This Website. So cool, we have a login page. But what's the password? Well, to answer that, we need to go back to our terminal. So let's head back there. Now since we can't use this terminal because it's doing "GitLabby" things, let's open another terminal over here. We'll learn how to hide all of that noise later. So let's make sure that we're in the Explore California website, which it seems like we are. That's good. We can confirm that by typing "ls" and we can see that we have the files that we expect to be there. So we are in a good spot here. So before we can log in to GitLab, we need to set a password for the root user that GitLab creates. There are two ways of doing this.
>
> **[6:21](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/starting-the-gitlab-compose-service-24080519?u=76281980&t=381)** The first way is easy. We can get the password that GitLab creates for us by running "docker compose exec gitlab grep 'Password:'", in single quotes with a colon at the end, "/etc/gitlab/initial_root_password". There it is. Just like that, we got a password. However, GitLab deletes this file after you shut it down for the first time. While this won't be a problem if you save this password somewhere convenient, if you're like me, you'll forget and then you'll lose the ability to log in. Fortunately, there is another way. Let's clear our screen to organize. This works every time, no matter how many times you bounce GitLab. So to do that we're going to type "docker compose exec gitlab gitlab-rake", and then in double quotes "gitlab:password:reset" and then in square brackets "[root]". This command allows us to set a password for the root user. You'll be asked to enter the new password twice. You can make it anything you want. And then once that's done, it tells you that the password has been successfully updated. Now that we've done that, let's go back to our GitLab login page. Type that password in again in the password field, and finally either hit Enter or click Sign In. If you get this prompt, you can either save the password or click Not Now.
>
> **[7:55](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/starting-the-gitlab-compose-service-24080519?u=76281980&t=475)** It's completely up to you. Boom, we're in. Nice! In the next video, we'll create our first project and repository in GitLab, and then we'll make sure that our local [Git](../../Skills/Software%20Development/Git.md) repository can talk to GitLab.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (3), [Google](../../Glossary/Service/Google.md) (1), [Git](../../Skills/Software%20Development/Git.md) (1)
> **Tools:** gitlab (36), terminal (4), safari (1)
> **CLI Commands:** make (13), docker (6), sudo (1), cat (1), ls (1)
> **URLs:** [example.com](https://example.com) (8), [gitlab.com](https://gitlab.com) (1), [https://gitlab](https://gitlab) (1)
> **Env Vars:** dns (2), ide (1), isp (1)
> **Versions:** 127.0.0 (3)
> **Cross-References:** go back to (2), in the next (1)
> **Definitions:** is a  (2), means that (1)

#### [Creating our first GitLab project](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-our-first-gitlab-project-24080517?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-our-first-gitlab-project-24080517?u=76281980&t=0)** Now that we got GitLab running, let's create a project. If you see this, Check Your Signup Restrictions window up here, we can safely ignore it by clicking acknowledge. It's just telling us that anyone can access our GitLab instance, which isn't a concern for us because we're running GitLab locally. First, let's create a group to put our new project into. Click on Groups over here on the left. Then on New Group over here on the right. Then click Create Group again when prompted. Let's first get this group a name. "Explore California" sounds good I think. Next, scroll down a little bit and find this Public button over here. We're going to make this a public group. We could use the next few options down here to customize what our group looks like and who can access projects within it, but since this is just going to be used by us, we'll leave everything from here on down as is. So finally, click Create Group and there's the group. We're now ready to create a project. To begin, click on Create New Project over here. Then click on Create Blank Project. Give your project a name like, I don't know, "Website," since we're working with Explore California's website. And just like our group, we're going to go ahead and make this a public project. Next I'm going to uncheck Initialize Repository with a Readme. Since we created a [Git](../../Skills/Software%20Development/Git.md) repository locally already, we'll get an error when we push our changes if we were to leave this checked due to a merge conflict. So let's go ahead and make sure that that's unchecked.
>
> **[1:35](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-our-first-gitlab-project-24080517?u=76281980&t=95)** Then finally we can click Create Project. And, there it is, our first project. Okay. Check out this message here on the top. GitLab is recommending that we create an SSH key. Since we can use SSH to clone Git repositories, uploading an SSH key will allow us to essentially log in to GitLab whenever we push our changes. So let's go ahead and go into our terminal to create one. We'll need to create our SSH key locally. So the way to do that is by typing "ssh-keygen". You'll be asked a few questions after you do this. First it'll ask you where you want to save the private key that it creates. Let's save it to a file called "/tmp/gitlab". Next, we can optionally enter a passphrase if we were concerned about this key being left out and about. Since we're doing this all locally, let's just hit Enter to leave it blank. Hit Enter again when prompted and done. If you see some cool art down here, then your SSH key is ready to go. So we actually created a little more than a single key. In fact, we created two keys here. We created a public key and a private key. The public key is your name and address. You can give this to anyone. It's your name and address. Who cares? The private key is the key to your house. In most cases, you don't want anyone to have this. Otherwise you'll have surprising people entering your place. And that would be very bad. You can think of SSH as the lock to your house that protects
>
> **[3:10](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-our-first-gitlab-project-24080517?u=76281980&t=190)** the things you love. SSH can accept any public key you throw at it, but the only way it will unlock the lock is if the right private key associated with that public key is also presented somehow. Now that we know this, we know what to give GitLab here. When GitLab or other Git-based dev platforms ask for your SSH key like it's asking up here, they're really asking you for your public key. This way, it can save that public key to your account so that when you locally present your private key upon trying to push your changes, GitLab knows that it's you trying to do stuff. All right, let's head back into our terminal and finish this off. So to get our private key I'm going to run "cat /tmp/ gitlab.pub". This file is automatically created by SSH keygen, so all we're doing is just using cat to print it. Now we're going to copy all of this over here. And then we're going to head back into GitLab. Finally, click on Add SSH Key. Click on Add New Key. Then paste the key that you just copied. Underneath it you can give this key a title, like let's say you name it "yourself", like "Carlos", or you can just name it "My Awesome Key." And then we can click Add Key. Nice. The key is added and we can now use it to log in to GitLab when we clone and push repositories. In the next video, we'll configure Git so that we can
>
> **[4:43](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-our-first-gitlab-project-24080517?u=76281980&t=283)** pull this repo down to our computer and test that we can push changes. Exciting!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (4)
> **CLI Commands:** ssh (11), git (4), make (3), cat (2), find (1)
> **Tools:** gitlab (12), terminal (2)
> **Env Vars:** ssh (10)
> **UI Navigation:** click on (5), scroll down (1)
> **Cross-References:** in the next (1)
> **Analogies:** just like (1)
> **Best Practices:** the key is (1)

#### [Connecting Git to GitLab](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/connecting-git-to-gitlab-24081431?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/connecting-git-to-gitlab-24081431?u=76281980&t=0)** Now that we've created our project and added our SSH key to our GitLab account, it's time to get [Git](../../Skills/Software%20Development/Git.md) and GitLab talking to each other. Click on the GitLab logo on the upper left-hand corner to go back to your home dashboard. Then click on "Explore California / website" to go to the repo we created earlier. We're going to retrieve or "clone" this repository into our machine. To begin, click on the blue Code button. Then click on the clipboard underneath SSH. Let's hold on to that for now. Before we move on, let's have a look at our workflow again. As you can see here, we've added a few more steps to it. When working with remote repositories like ones from GitLab, the first thing that you're going to do is you're going to clone the repository onto your machine, like shown over here on the left. Fortunately, you'll only need to do this once. Since we've already initialized the repository locally, we'll connect our repository with GitLab by adding our GitLab repository as an origin. We'll see how soon. Eventually, you'll want to keep changes made into that repo up to date with your local copy. We can use Git pull to do this. To preserve the order or history of our changes, we can rebase changes, push up the GitLab with our changes by adding the "--rebase" option to the end of "git pull". We'll be doing this frequently, so you'll see an example of that in action soon. After you pull in your remote changes, you'll "git add", "git commit" and "git push" like we saw before.
>
> **[1:37](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/connecting-git-to-gitlab-24081431?u=76281980&t=97)** Cool. Let's head back into the terminal and connect our local repository to GitLab. Before we continue, let's run "git status" to make sure that we've committed all of the changes that we've made thus far. As you can see, it looks like we forgot to commit the docker-[compose](../../Glossary/Framework/Jetpack%20Compose.md).yaml file that we created earlier. So let's go ahead and fix that. First we'll run "git add docker-compose.yaml". I press the Tab key to autocomplete that. Next we'll run "git commit" to commit our changes. However, before we run this command, we need to add a little bit of information to Git so that it knows who we are, basically, when we create these commits. What do I mean by this? If I type "git log" over here, we can see that our author is a default author that Git created on the spot based on my username and any emails that I had associated with the system. However, if you don't have this information on your computer, Git might throw an error message saying that you don't have an author name or author email specified. So to work around this, what I'm going to do is basically set that information now. So to do that I'm going to first clear my screen, just to organize things. Then I'm going to type "git config --global author.name". And then after you type this you can just put your name. "Carlos Nuñez", for example. Now we're going to do the same thing but for our email address. Like I said earlier, Git was used by [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) contributors
>
> **[3:13](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/connecting-git-to-gitlab-24081431?u=76281980&t=193)** to contribute patches and changes to Linux. So because all of this was done super distributed all over the world, having emails attached to those commits was really important, and it's still important today. This is how people know how to contact the person that made the change. So what I'm going to do is I replaced "name" with "email", like you can see here. So now what I'm going to do is replace my name with my email address. There we go. Next I'm going to repeat these changes, but instead of "author" I'm going to go ahead and put "user" here. So I'm just going back to the author name and email commands and replacing "author" with "user". Now that I've done all that, I am finally ready to make a commit message with my information on the commit. So let's go ahead and do that by typing "git commit". Just like before, it's going to ask for a summary and a description. So let's make our summary something simple, like "Added Docker Compose services." And again you can be as creative as you want with the description. However I'm going to keep it simple and just write something like this, "Includes things like GitLab". Done. So I'm going to exit Vim by pressing Ctrl + [ and then typing ":w" to write my changes and "q" to quit Vim. And there we go. Now let's see what our git log, which shows us all of our commits, looks like now that I've added these configurations.
>
> **[4:51](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/connecting-git-to-gitlab-24081431?u=76281980&t=291)** As you can see, the difference is pretty obvious. Our first commit just had this generic [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) user information that was on the system. But after making those configurations, it now has my name and my email on it, which looks a little bit more professional. So now that I've made that commit, if I run "git status" again, we can see that our working tree is clean, which means that we're ready to test whether we can connect to GitLab. So the way that we're going to do that is —clearing our screen— by running "git clone". Then we'll paste the command fragment that we copied from GitLab earlier in here. We'll be given this scary looking message here. This is just SSH telling us that it hasn't connected to gitlab.[example.com](https://example.com) before, and it's simply asking us whether we want to trust it based on the fingerprint shown here. Since we do trust the server, I'm going to go ahead and type in "yes" to continue. Now that we did that, we get another error message that's telling us that we don't have permissions. But this error message actually makes sense, though. A few minutes ago, we created an SSH key to identify ourselves on GitLab. However, just because we created this key doesn't mean that SSH now knows about it. Because Git uses SSH behind the scenes to [Fetch](../../Skills/Web%20Development/Fetch.md) this repo, what we basically did by doing this was, to go back to our house analogy from earlier, trying to unlock a house without any keys. That would be one really unsafe house. So to fix this, we're going to create an SSH
>
> **[6:26](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/connecting-git-to-gitlab-24081431?u=76281980&t=386)** config file that maps our GitLab instance to our private key. So to begin. I'm going to clear my screen, then type in "touch ~ /.ssh/config". That creates our config file. So once we're done with that we're going to open it in our editor. We're going to add two lines here. The first line that we're going to add is "host gitlab.[example.com](https://example.com)". Underneath that we're going to add two spaces. And then we're going to add "IdentityFile /tmp /gitlab". That's it. That's all we need to do to tell SSH about our private key for our GitLab instance. So we're going to go ahead and save this file and exit. And then we're going to retry our clone command from earlier. So we get a warning here telling us that we've cloned an empty repository. But that's actually good. That's what we want because our project is new and has nothing in it. So we were able to successfully clone, which means we're able to successfully talk to GitLab. I mentioned earlier that we're already working in the Git repository. So what we actually want to do here is link our existing repository with this GitLab project. So we don't need that repo that we cloned earlier, so let's go ahead and delete it. Now that we've done that let's clear our screen and type in "git remote add origin". And then we're going to go ahead and once again paste in the command fragment that we copied from GitLab earlier.
>
> **[8:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/connecting-git-to-gitlab-24081431?u=76281980&t=480)** This will tell our Git repository about a remote repository or a remote that we're referring to as origin here. So let's go ahead and hit Enter here. And there it is. Finally, let's confirm that we can push our changes up to GitLab. To do that all we need to do is type "git push". However, we get this error here. This one's expected. Git is telling us that this branch that we created doesn't have an upstream branch. Let's break that sentence down. When we committed the files we added earlier, Git automatically created a branch called "main". Every local branch that Git creates needs to have a reference in its remote so that it knows where to pull changes from and push changes to. So in our case, our remote is GitLab. And so we call that remote branch and upstream branch. So Git was really nice here and gave us the command that we need to run to fix this. So I'm just copying this and pasting it down here. And there it goes. Our changes have been made. So finally, let's log in to GitLab and confirm that it got our changes. So I'm going back into the browser and simply refreshing the page. And would you look at that? If you see the message right above the main branch reference right here, we can see that the commit message that we made just now is here. We can also see that the repo here has the same files that we've been
>
> **[9:36](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/connecting-git-to-gitlab-24081431?u=76281980&t=576)** working on since we started our journey. So GitLab is up and running, y'all. So in the next few videos we're going to be doing some really exciting stuff. We're going to be adding some tests to our website so that we can actually flex our [DevOps](../../Topics/DevOps.md) muscles a little bit, and then we're going to start exploring the worlds of [Infrastructure as Code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) and CI/CD. Very exciting! Stick around, you're going to have a lot of fun.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (28), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (3), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **CLI Commands:** git (28), ssh (9), make (3), docker (3), cd (1)
> **Tools:** gitlab (24), vim (2), terminal (1)
> **Env Vars:** ssh (8)
> **UI Navigation:** click on (4), go to (1)
> **Cross-References:** earlier in (1), go back to (1), in the next (1)
> **File Paths:** docker-compose.yaml (2)
> **URLs:** [example.com](https://example.com) (2)


### 5.  Testing Your App with RSpec, Capybara, and Selenium

[↑ Back to Table of Contents](#table-of-contents)

#### [Test often, ship often](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/test-often-ship-often-24081427?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/test-often-ship-often-24081427?u=76281980&t=0)** Let's review what we've accomplished so far. We've installed Docker and Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md) onto our machine. We've also confirmed that we can build container images from Dockerfiles and create containers from them. We also learned about [Version Control](../../Skills/Web%20Development/Version%20Control.md) systems and how they make developing software as a team awesome. From there, we've created a [Git](../../Skills/Software%20Development/Git.md) repository, used Docker Compose to stand up an instance of GitLab, and pushed our repository along with the Explore California website into it. This is some serious progress. Let's crank this up a notch by changing our website. Let's get right into it. Let's make sure we're in the Explore California website directory, which we are. From there we're going to CD into the website directory. Then we're going to go ahead and open index.htm in our favorite editor. And then once here we're going to change text and r... Hang on a second. Let's get [DevOps](../../Topics/DevOps.md) here for a little bit. How do we know that the change we're about to make actually works? We could load the website in a browser locally and see our changes. That totally works. Web developers all over the world test like this. But what happens when we want to automate this testing? What if we were debugging some new backend code for our website that only shows up once rare specific conditions are met? What if this codebase was much larger and much older than the one we're using, and making even the smallest change within it can cause absolute chaos. We can solve all of these problems and more by writing tests. [Software Testing](../../Skills/Software%20Development/Software%20Testing.md) is exactly what the name implies: using software to test our software.
>
> **[1:36](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/test-often-ship-often-24081427?u=76281980&t=96)** We use changes to verify that our software works in all sorts of conditions. This way, when we add that small change to the spaghetti of old code that a coworker handed to us before retiring and spending their days on the beach with cocktails, we know that it won't break things, or if it does, why and how those breaks occur. There is no such thing as too many tests. I'll say that again for my folks in the back. There is no such thing as too many tests. The more tests your software has, the better guarantees you can make about how the software will behave in the real world. SQLite, one of the most popular [Databases](../../Skills/Software%20Development/Databases.md) in the world, has over 92 million lines of test code, despite the code itself only being roughly 156,000 lines large. SQLite, being as well tested as it is, is one of the biggest reasons why it is so popular. Their maintainers test SQLite in just about every condition it finds itself in, and any new bugs that people submit against the project turns into, you guessed it, more tests. Now we're definitely going to update Explore California in this course, but before we do that, we're going to add two different kinds of tests to verify that our change will work as expected. A unit test that tests the single change we're going to add, and an integration test that verifies that the entire website works after making that change. Since we need a test framework to run these tests with,
>
> **[3:10](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/test-often-ship-often-24081427?u=76281980&t=190)** we'll also add three popular testing tools to our Compose manifest: RSpec, a very popular Ruby-based test framework; Capybara, a Ruby library that makes it easier to connect RSpec with [Selenium](../../Skills/Software%20Development/Selenium.md); and finally, Selenium, a tool that automates testing websites in real browsers. This course only scratches the surface of testing software. There are lots of great courses right here on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) learning that I would recommend checking out if you want to go deeper into this topic. Richard Wells's "[Unit Testing](../../Skills/Software%20Development/Unit%20Testing.md) and Test Driven Development in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md)" or Meaghan Lewis's "[Test Automation Foundations](../Software%20Development/Test%20Automation%20Foundations.md)" are some pretty good courses to get started with. So let's grab ourselves a coffee, tea, or whatever your favorite drink is, fire up that terminal and get into it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (3), [Selenium](../../Skills/Software%20Development/Selenium.md) (2), [Version Control](../../Skills/Web%20Development/Version%20Control.md) (1), [Git](../../Skills/Software%20Development/Git.md) (1), [DevOps](../../Topics/DevOps.md) (1)
> **CLI Commands:** make (4), docker (3), ruby (2), git (1), cd (1)
> **Tools:** gitlab (1), terminal (1)

#### [Setting up your test](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/setting-up-your-test-24076580?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/setting-up-your-test-24076580?u=76281980&t=0)** In this video, we will write a few simple unit tests for our web app with Rspec, Capybara and [Selenium](../../Skills/Software%20Development/Selenium.md). After that, we'll use our newfound Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md) knowledge to run those tests within Docker Compose. Let's get started. Let's talk about what unit tests are. First, unit tests test a single component within your software. These are the smallest and simplest tests in your collection of tests, or what we call a test suite. Since unit tests only test singular components, they should be small, plentiful, and very fast. They should be very easy to write. So easy, in fact, that you can write them before you write any actual code. If you are writing a test and are thinking, "How do I configure my test to access this database or disk?", then that is probably not a unit test. Those kinds of tests are more appropriate for integration tests, which we'll talk about later. Let's first talk about Rspec. Rspec is a Ruby-based testing framework. I like using it because its domain-specific language, or DSL, makes writing readable tests really easy to do. Since we're testing a mostly static website, we'll need something that works well with [HTML](../../Skills/Web%20Development/HTML.md) and maybe some computed CSS. We could use something that requests raw HTML and parses the response, but that will quickly grow cumbersome. Instead, we can use a testing framework for websites and web apps that does this work for us. Capybara is a good example of this. Like Spec, Capybara provides a domain-specific language for interacting with websites and testing output. It interacts with websites called drivers.
>
> **[1:33](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/setting-up-your-test-24076580?u=76281980&t=93)** Speaking of drivers, while Capybara comes with a super simple Ruby-based web driver called rack test that fetches and parses HTML output, it does not support computing [JavaScript](../../Skills/Software%20Development/JavaScript.md) or CSS at all. This makes testing things like, "Do we have the right background on this page?" impossible to do. This is where Selenium comes in. Selenium is a platform for automating web browsers through code. You can pronounce it Selenium or Selenium. It's completely your preference. Code communicates with Selenium through web drivers. Selenium supports Chrome, Firefox, and Internet Explorer by default. While we will use Chrome for this course, in real life I would probably test against Firefox, Internet Explorer, and Edge as well as some other browsers so that we can make sure that most of the people using Explore California can actually use it. Now that we know the basics, let's get into our terminal and set up some tests. First, make sure that you're in the Explore California website directory that we've been using so far in this course. You'll be able to see it here. If not, you can just type "cd ~/explore-california-website" to bring you right back to it. Okay, next let's make sure that GitLab is up and running. Since we're going to be committing our changes frequently. We can do that by running "docker compose ps" and hitting Enter. If you see GitLab in the output, which you can see over here, then you're good to continue. If not, simply run "docker compose up -d gitlab" to start GitLab in the background like we did before.
>
> **[3:07](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/setting-up-your-test-24076580?u=76281980&t=187)** After you do that, open up a browser and visit gitlab.[example.com](https://example.com). You should see a login page or a page like this. Remember this can take a few minutes to get going, so don't fret if GitLab isn't working right away. Now that we've confirmed that GitLab is up, let's go back into our terminal and get started. The first thing I'm going to do is create a folder called "spec". So let's use "mkdir spec" to do that. I like to organize my tests by the kinds of tests that I'm writing. Since we're writing unit tests. Let's create a folder called "unit" underneath "spec", like this. Now let's start writing our test file. By default, Rspec looks for files that end with "_spec .rb". Since we're going to be writing tests against the home page, let's create a file within spec/unit using the touch tool called "spec/unit/page_spec.rb". Now that we've created it, let's go ahead and open it up in our preferred editor. Most Rspec tests start with a describe block. Describe blocks organize tests within a test suite into different scenarios. So let's go ahead and create one below that looks something like this. Since we're testing that we can render the homepage, I decided to call this test "Example page render unit tests", but you can call it anything you want. All Rspec tests go inside of describe blocks. You can think of describe blocks as a collection of tests that are related
>
> **[4:42](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/setting-up-your-test-24076580?u=76281980&t=282)** to each other. Every single test inside of the describe block gets described with an "it" block like this. Now let's use a really simple test, like checking that the logo of Explore California is visible. So what I'm going to do is replace "test name" with "Should show the Explore California logo", like this. And there we have it. We have a really simple test case inside of a describe block. Like I said, you can have multiple tests within the describe block. Ideally they should be related to each other. So since we're testing an example page render and we're testing that the home page renders, we could have other tests like "It should show the example California summary" or "It should show the example California navigation bar", or nav bar. But for now, we're only going to have one test case, just to keep things simple. We'll add the actual code that executes this test later. But for now let's go ahead and save our changes and exit our editor. Now we're ready to create our test harness within Docker Compose. But before we do that, let's go ahead and commit our changes. So, remember, our workflow is going to be "[Git](../../Skills/Software%20Development/Git.md) add", "git commit" and "git push". We're going to be doing this more rapidly than we've done it before. So if you forget the workflow or if you just want a refresher of how everything works, go ahead and visit our Git and GitLab chapter where we go really in-depth into that. So first I'm going to run "git add spec" to add the folder
>
> **[6:18](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/setting-up-your-test-24076580?u=76281980&t=378)** that we just created. Afterwards I'm going to run "git commit" to open up our commit in our editor. Let's go ahead and add a really simple commit for this one. Let's say "Added simple tests". Again you're free to create whatever description you'd like. I'm going to use a description like "We're making this change to verify that our spec is working." So after I write that, I'm going to write and quit. And now we're ready to push. But before I do, I want to make sure that I have the latest changes from GitLab by running "git pull rebase". Now, this is a really good practice to get into the habit of doing, because if we were working with multiple developers, there might be lots of changes in our upstream branch that we want to pull down to make sure that our history aligns with what GitLab is seeing. So "git pull rebase" allows us to do this while making sure that our history is in sync with what GitLab has. Great, so now that we've done all of that, we are finally ready to push. Done. Once again, we're going to be doing this much more quickly in future videos. So if you get lost, go back to our Git chapter where we talk about this in much more depth.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (9), [Selenium](../../Skills/Software%20Development/Selenium.md) (7), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (5), [HTML](../../Skills/Web%20Development/HTML.md) (3), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **CLI Commands:** git (9), docker (5), make (5), ruby (2), cd (1)
> **Tools:** gitlab (11), firefox (2), terminal (2)
> **Documentation:** spec (8)
> **Env Vars:** html (3), css (2), dsl (1)
> **Definitions:** is a  (4)
> **Prerequisites:** configure (1), set up (1)
> **File Paths:** spec/unit/page_spec.rb (1)

#### [Viewing our test harness in Docker Compose](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/viewing-our-test-harness-in-docker-compose-24080522?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/viewing-our-test-harness-in-docker-compose-24080522?u=76281980&t=0)** Now that we have a skeleton for our tests, we're ready to run our tests within Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md). As always, before going any further, make sure that you're in the Explore California website working directory that we've been using or run "cd ~/explore- california-website" to go back into it. I've created the Compose service and an updated version of the Dockerfile we created earlier to save us some time. While we already have the updated Compose service in our docker-compose.yaml, we'll need to obtain the updated Dockerfile that goes along with it. This file is in the "05_03_before" folder within your exercise file bundle. Like we did before, let's run "cp", then the path to your exercise files bundle, or "~/Downloads/exercise_files/" in my case. Then, "05_03_before/Dockerfile" and then copy it right into our Explore California website directory. Now, let's make sure that we have it. You should see Dockerfile in the list over here. And then when you open Dockerfile with Vim, you should see that it looks a little bit different. However, we're going to take a much closer look at this Dockerfile in the next video. Cool! Let's check out our docker-compose.yaml and explore how we've defined our unit test service. You're going to see references to actual PWD in some of the stuff you'll be looking at here. Don't worry about that for now. We're going to talk about that in a later video.
>
> **[1:34](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/viewing-our-test-harness-in-docker-compose-24080522?u=76281980&t=94)** Let's scroll down to the unit test service below and have a look at three important properties that are associated with it. First, we can see that unit test creates a custom Docker image using the "build" keyword down here. We're doing this because in this case, it's easier to create a custom image that has Rspec, [Selenium](../../Skills/Software%20Development/Selenium.md) and Capybara baked into it than to find one on the Docker Hub. Second, we can see that our unit test service has two "depend on" clauses, one on the website service and another on the Selenium service. This is a cool feature within Docker Compose that prevents your services from starting unless conditions within under services have been met. In our case, our unit test service won't start until the website and Selenium services start and are healthy. What do we mean by healthy? Well, let's scroll up to the website service and find out. As you've probably guessed, the website service starts a local copy of Explore California. We're using the super popular Nginx image from the Docker Hub to do this. Instead of copying our website into a Docker image with a custom Dockerfile, we're using a volume mount, which you can see here to bind our website folder to a special directory that this Nginx uses to serve our web page. We're also using port binding, like we did with our GitLab service, to make this website, which is served on port 80 within the container, available locally on our host on port 8080. Check out the "Installing GitLab" video from earlier if you'd like to learn more about how port mapping works. Finally, take a look at this health check property over here.
>
> **[3:08](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/viewing-our-test-harness-in-docker-compose-24080522?u=76281980&t=188)** This is what I meant by "healthy" earlier. Health check allows you to define a test that Docker will run against the container to determine whether the service is working or not. The test is usually a simple-cell command like the one that you see here. Here we're using "curl" to load the website locally within the container. If curl successfully retrieves the website, then our service is healthy and our unit tests will start. The timeout property down here tells curl that it has 10 seconds to do this before Docker respectfully, but brutally, terminates this process. Additionally, the "start _period" property down here tells Docker to wait 60 seconds before starting our tests. This property is useful for giving services time to start up before Docker becomes annoying by constantly asking it "Are we ready?" "Are we ready yet?" "Are we ready yet?" "Are we ready yet?" What happens if this fails? That's where "interval" and "retries" come into play. Retries tells Docker how many times to retry the test command, while interval specifies how long to wait before each attempt. Given all of this, if our curl command fails, Docker will try the command again four more times while waiting 10 seconds between each attempt. That was a lot. While we didn't cover everything about how health checks work, you can always visit docs.[docker.com/compose](https://docker.com/compose) to learn more. Let's head back to our unit tests to learn more about that final property. The command property down here. This command property feeds arguments into the entrypoint that we saw earlier within the Dockerfile to change
>
> **[4:42](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/viewing-our-test-harness-in-docker-compose-24080522?u=76281980&t=282)** how Rspec works. In this case, what we're doing is we're giving it the "pattern" argument over here, and then we're giving it the second argument which specifies what tests to run. The way this works is it will look in our unit directory for any test that has "_spec.rb", like we talked about in the last video. We'll learn why we're using "/tests" down here instead of "/spec" in the next video, when we go deeper into the Dockerfile. Cool. Now that we know what our unit test service is doing, let's exit our editor and run some tests. However, don't forget to commit your changes to GitLab. Let's go ahead and do that now. We'll use "[Git](../../Skills/Software%20Development/Git.md) status" to see what's changed. As you can see, we introduced a new Dockerfile. So let's go ahead and add that into our staging area. Let's go ahead and commit those changes. I'm not going to include a description in this commit, but you're more than welcome to. And then let's push our changes up.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (7), [Selenium](../../Skills/Software%20Development/Selenium.md) (3), [Git](../../Skills/Software%20Development/Git.md) (1)
> **CLI Commands:** docker (15), curl (4), make (3), find (2), nginx (2)
> **Cross-References:** in the next (2), we talked about (1), in the last (1)
> **Tools:** gitlab (3), vim (1)
> **File Paths:** docker-compose.yaml (2), _spec.rb (1)
> **Ports:** port 80 (1), port 8080 (1)
> **UI Navigation:** scroll down (1), scroll up (1)
> **Exercise Files:** exercise file (1), exercise files (1)

#### [Updating the Dockerfile](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/updating-the-dockerfile-24082448?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/updating-the-dockerfile-24082448?u=76281980&t=0)** In the last video, we copied an updated version of the Dockerfile into our working directory so that we can start RSpec and run our unit tests. Let's take a closer look at it now. Go ahead and open our Dockerfile in your preferred editor. So as you can see, the Dockerfile doesn't look that much different than the Dockerfile that we created at the beginning of this course. For example, we have a "FROM" line that tells the Dockerfile what are our parent or base images. In our case, we're using version 3.0 of Ruby from the Docker Hub. Right underneath that, we have a "LABEL" specifying who's maintaining this image and where to contact them. Looks exactly the same as before. And we also have a "COPY" line that copies the spec directory into a folder within the Docker image, called "/tests". This is why in Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md) we're starting our unit tests from "/tests" instead of "/spec" or something similar. However, we have two new things inside of this Dockerfile that we haven't seen before. We have a "RUN"command and we also have an "ENTRYPOINT"command. Let's take a closer look at these. RUN commands in Dockerfiles like this... Well, run commands. These commands are executed in what are called intermediate containers, and any files or other data generated by the command gets stored in an intermediate image layer. In fact, every single line in this Dockerfile after the LABEL line is in a separate image layer.
>
> **[1:32](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/updating-the-dockerfile-24082448?u=76281980&t=92)** You can think of Docker images as basically a set of layers squished together, kind of like a stack of pancakes. Pretty cool. Now this "gem install" command is a Ruby adjacent command that installs various libraries or executables into your environment. So in this case, we're using gem install to install RSpec, Capybara and [Selenium](../../Skills/Software%20Development/Selenium.md) WebDriver. This one requires some explaining. Selenium WebDriver does not install Selenium. This only installs libraries that allow our test code to talk to a web browser driven by Selenium. In our docker-compose.yaml, we saw that our unit test service is dependent on a Selenium service in order to start. That Selenium service is what actually runs Selenium. So if you're interested in learning how that's configured, go back into the docker-compose.yaml and take a look at the Selenium service. Finally, we have an ENTRYPOINT down here. This entrypoint gives any containers created by this image a default command to execute. In our case, our default command is going to be Rspec. This entrypoint, i.e. the one that is not commented out like the one below, is written in what's called a "command form." If we wanted to, we could do what we're doing down here and write the entrypoint in something called "shell form." There are a few differences between the two, but the biggest difference between them that we'll focus on for now is that it affects how Rspec starts up. In command form,
>
> **[3:08](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/updating-the-dockerfile-24082448?u=76281980&t=188)** Docker will configure Rspec containers to start Rspec, the actual binary Rspec, when they launch. In shell form. Docker will configure these containers to run "/bin/ sh -c rspec" instead. This makes a big difference when providing arguments to the container using "docker compose run" or "docker run". In command form, those arguments will go directly to the Rspec binary, so the "--pattern" argument and that last argument that specified where our tests are go directly to Rspec in command form. In shell form, those arguments will go to sh instead. The consequence of that is that there isn't any guarantee that Rspec, the binary Rspec, will receive those arguments. This can cause a lot of problems with certain tools, so I typically prefer to write my entrypoints in command form like this. If you're interested in learning more about the differences between command form and shell form, I highly recommend checking out my course "Learning Docker", where I go into it in much more detail. You can also check out my follow-up course, "Docker Essentials Training", which really drills into the differences between these [Forms](../../Skills/Web%20Development/Forms.md) and also gives you some examples of how you can write your entrypoint in other ways. Alright, we're done here. So let's go ahead and exit our editor and continue on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Selenium](../../Skills/Software%20Development/Selenium.md) (8), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (4), [Forms](../../Skills/Web%20Development/Forms.md) (1)
> **CLI Commands:** docker (12), ruby (2)
> **Env Vars:** label (2), run (2), entrypoint (2), copy (1)
> **Prerequisites:** install (4), configure (2)
> **File Paths:** docker-compose.yaml (2)
> **Documentation:** spec (2)
> **Analogies:** for example (1), kind of like (1)
> **Versions:** version 3 (1)

#### [Running the test](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/running-the-test-24081428?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/running-the-test-24081428?u=76281980&t=0)** It's time. Time to run our tests, that is. To begin, we're going to use "docker [compose](../../Glossary/Framework/Jetpack%20Compose.md) run --rm unit-tests". Now, if we use "docker compose up" here, it would run our tests along with all of the other services in our Compose file, like GitLab, the website, etc. So using "docker compose run" ensures that the only thing that runs are our tests. So let's run it. You'll see our image building here, as expected. This might take a few minutes depending on your internet connection, so stretch your legs or hang tight and we will be back in a moment. Alright, welcome back. We can see here that we have this little green dot over here to the left of our screen. That green dot means a lot. The dot means that Rspec found one test, i.e. the test that we wrote earlier. It being green means that the test passed, which is expected because our test doesn't do anything yet. Success! Rspec is working and can run tests. Let's make this test actually do something. We'll do that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (4)
> **CLI Commands:** docker (3), rm (1), make (1)
> **Definitions:** means that (2)
> **Cross-References:** in the next (1)
> **Tools:** gitlab (1)

#### [Finding criteria for a unit test](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/finding-criteria-for-our-unit-test-24076579?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/finding-criteria-for-our-unit-test-24076579?u=76281980&t=0)** So far we were able to get Rspec working in Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md). However, it's not testing very much like we saw before, so let's fix that by writing an actual test against our actual Explore California website. Let's get right into it by opening "spec/unit/page_spec.rb" in our editor. Just like we saw before, it doesn't do a whole lot, so we should make it do something like, for example, test that the Explore California logo is there. Now, if we were developing Explore California from scratch, I would have probably started with the test verifying that the site had a logo and that the logo was in the right place. However, since we're [DevOps](../../Topics/DevOps.md) to find an existing website that doesn't have very much in the way of tests, we're going to write a simpler test that just checks that the logo exists. So let's begin by using our browser to see what the existing source code for the page looks like. Let's quit our editor for now and run "docker compose up -d website". Now, if you ran the unit test that we did in the last video, there isn't going to be much that happens here because your website should already be running. Like we saw earlier when we started GitLab, the -d argument tells Docker to take the logs that are coming from this website container and shove them into the background so that we don't have to see them while it's running. Now that we did that, let's go back into our browser, open a new tab, and visit our local instance of Explore California by going to localhost:8080. Cool. Explore California is up and running, so feel free to spend a few minutes looking around and getting
>
> **[1:36](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/finding-criteria-for-our-unit-test-24076579?u=76281980&t=96)** comfortable if you'd like before moving on. So we can see that the Explore California logo is right here. We'll need some properties about this logo to test that it's actually there. So we can do that by looking at the source code for it and seeing if we can find something unique about it. If you're using Safari like I am, you'll need to enable developer mode by hitting the Command + Comma keys on your keyboard, clicking on the Advanced tab and then clicking on Show Develop Menu in the menu bar. Now that you've done this, you should be able to right-click the logo and click on Inspect Element. Once you do that, it'll take you right there. So as you can see, there are several interesting properties about this logo's [HTML](../../Skills/Web%20Development/HTML.md) object here. First, this "a" element tells us that the logo is also a logo that you can click on. The "href=index.htm" property tells us that clicking on this link will take us to the index of this web page. We can test that by clicking on All Tours and then clicking on the logo. And we're right back there. Secondly, we can also see that this "a" tag has two more properties: "title" and "class". Specifically, we can see that this logo has a "class" set to logo. Aha! That's our unique thing that we can test against. So with that in tow, let's go back to our test file and link the two together.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (2), [DevOps](../../Topics/DevOps.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **CLI Commands:** docker (3), find (2), make (1)
> **UI Navigation:** click on (2), in the menu (1), right-click (1)
> **Cross-References:** in the last (1), go back to (1)
> **Tools:** gitlab (1), safari (1)
> **Exercise Files:** source code (2)
> **Analogies:** just like (1), for example (1)
> **File Paths:** spec/unit/page_spec.rb (1)

#### [Writing the unit test](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-the-unit-test-24080515?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-the-unit-test-24080515?u=76281980&t=0)** Now that we know how to find our "logo" with code, let's outline what we want to do as pseudocode here. I like to use pseudocode as a placeholder that helps me translate my thoughts to code. So inside of the "it" block let me describe what I want to do. First, I want to visit localhost:8080. Then, I want to get the page's source code. Then, I want to find an "a" element whose "class" is set to "logo".
>
> **[0:34](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-the-unit-test-24080515?u=76281980&t=34)** And that's it. Let's make this code a little more code-like. So when I say I want to visit 8080, a more code-like representation of this could be something like 'visit ("[http://localhost:8080](http://localhost:8080)")'. Then, to get the page's source code, a more code-like representation of this could be something like "get(page_html)". And then finally, when I want to find that element, which of course I misspelled, and then I misspelled it again... That looks better. This could be represented with something like 'verify_exists (a.class == "logo")'. Again, pseudocode is a nice tool to help you visualize what you want to do. There is no right way to write pseudocode. So if your pseudocode doesn't look like this, that's totally fine. It's not real! It's just meant to help you visualize what you want to do. We know that Capybara links Rspec to web pages, but how do we do this with Capybara? Let's take a look at its documentation to see. To do that, I'm going to go to my browser and visit [https://github.com](https://github.com)/ teamcapybara/capybara. That will take us right to Capybara's source code on [GitHub](../../Skills/Software%20Development/GitHub.md), which also happens to be its documentation. Now that we're looking at Capybara's source code and documentation, let's go line by line in our pseudocode to see if we can find equivalents in Capybara to what we want to do. So going back to our code, the first thing that we want to
>
> **[2:09](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-the-unit-test-24080515?u=76281980&t=129)** do is visit localhost 8080. Let's see what happens when we go back to our documentation and search for "visit." And maybe a parenthesis, since we're looking for a function. Well, wouldn't you know it? It looks like we have a visit property over here, and all we need to do is, inside of these parentheses, provide the URL that we want to visit. So I'm going to go back to my source code and replace this line over here with two spaces. And I can just leave it exactly as it is. Really, really convenient. I'm going to go ahead and get rid of the comment also. Now let's go back and see if we can find that "a" element that we were looking for earlier. In [HTML](../../Skills/Web%20Development/HTML.md) land, we could use something called a "selector" to find a specific element on a page. So let's see what happens when we search for "selector." That's not useful. Let's keep trying. There we go! How about that? It looks like we have a page object that has a method called "has_selector?" where in the parentheses you can provide the properties that you're looking for on the page. Really, really convenient. So now that we found this all we have to do is copy that line, go back to our source code, remove this "get" stuff over here, align our cursor with "visit" and then paste that right in. Next, we're not interested in "table tr", since we're not looking for a table, we're looking for an "a" element with a class set to "logo." So to do that I'm going to go ahead and change "table tr"to "a.logo".
>
> **[3:43](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-the-unit-test-24080515?u=76281980&t=223)** This dot right here tells Capybara that I'm interested in a property within the "a" element, specifically I'm interested in the "logo" property. Okay, now that we have some code that looks like it can get our logo, what I'm going to do is I'm actually going to get rid of this "verify_exists" pseudocode here, since our code up here pretty much does exactly what we were looking for there. Then I'm going to go ahead and save this. And then I'm going to use Command + T to open a new tab, Ctrl + L to clear the screen. Then I'm going to make sure that I'm in the Explore California website directory. If you're not you can just type "cd ~/explore-california-website" to go right back in there. And then I'm going to type "docker [compose](../../Glossary/Framework/Jetpack%20Compose.md) run --rm unit-tests". But I'm not going to hit Enter just yet. If you recall from a previous video, our Dockerfile copies our tests into the container image. However, because we changed our test, this means that we also need to update that container image. We can easily do that by configuring our run command to rebuild our container image before running our tests. So to do that, I'm going to use Ctrl + A to go to the beginning of the line. Then I'm going to use Ctrl + F to move one character to the right a few times, and then right behind "--rm" I'm going to add a "--build". That's what tells Docker Compose to rebuild our container image. Now we're going to go ahead and hit Enter to run our tests. This isn't quite what we expected. We got a little bit of an error here. It looks like we need to add a few more bits to our test
>
> **[5:18](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-the-unit-test-24080515?u=76281980&t=318)** before it can fly. "Visit" is a method that's provided by Capybara. The "undefined method visit" error that we see right below it is telling us that Rspec wasn't able to find that method, but that's hardly surprising because if we go back to our test, we can see that there's nothing in our test that tells Rspec to actually use Capybara. We can easily fix that by going to the top of the page and adding a few lines of code. For those of you all that are following along with me in Vim, I'm going to go ahead and do this by typing lowercase "g", lowercase "g", twice. Then I'm going to press lowercase "i" to enter insert mode. And then I'm going to add three lines. I'm going to add 'require "capybara"', 'Require "capybara/dsl"', and then I'm going to add "include Capybara::DSL". And then I'm going to add two lines to separate those from our test. Let's explain what's going on here. The first two lines up here tell Ruby, which is what runs Rspec, to include the Capybara libraries into our test. This last line imports the methods that are provided by the DSL module within the Capybara library into our test. That's what enables us to use "visit" down here. Let's go ahead and save this with ":w", and then use Ctrl + Tab to go back to our other tab. Clear our screen and run "docker compose run --build
>
> **[6:51](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-the-unit-test-24080515?u=76281980&t=411)** --rm unit-tests" again. Now we get another error. This time we're seeing an error that says that "rack test requires a rack application, but none was given." This error is interesting and requires a teeny bit of explanation. Just like we explained in the last video, rack test is the driver that Capybara uses by default to load websites with. It's also written in Ruby and it is very fast, we just haven't configured it yet. So let's go back to Capybara's documentation and search for "racktest," one [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md), to figure out how to configure it. We get a link there, I just clicked it and there it is. So it seems like all we need to do is copy this code right here into our test. This code registers a new instance of the rack test driver called "RackTest" up here, which also happens to be the same name of the default driver that Capybara uses. But wait, there is a catch. If you scroll up just a little bit, you can see this sentence: "Furthermore, you cannot use the RackTest driver to test a remote application or to access remote URLs e.g. redirects to external sites, external APIs, or OAuth services that your application might interact with." Even though our website is running within a Docker container from the perspective of the unit test service, it is an external website. So what do we do? This is where [Selenium](../../Skills/Software%20Development/Selenium.md) comes in. Here's how we're going to fix this... ...in the next video. We're going to stand up a Selenium server, in Docker of course, that spins up instances of Chrome on demand. Afterwards, we're going to use the Selenium WebDriver that we
>
> **[8:27](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-the-unit-test-24080515?u=76281980&t=507)** installed earlier to have our tests tell Selenium to visit our web page and test the selector that we provided in our test over here. Let's do it!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Selenium](../../Skills/Software%20Development/Selenium.md) (4), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (3), [GitHub](../../Skills/Software%20Development/GitHub.md) (2), [HTML](../../Skills/Web%20Development/HTML.md) (1), cursor (1)
> **CLI Commands:** find (7), docker (5), rm (3), make (2), ruby (2)
> **Cross-References:** go back to (6), previous video (1), in the last (1), in the next (1)
> **Exercise Files:** source code (6)
> **Definitions:** is a  (3), means that (1), is an  (1)
> **Code Identifiers:** verify_exists (2), page_html (1), has_selector (1)
> **Env Vars:** dsl (2), url (1), html (1)
> **URLs:** [localhost:8080](https://localhost:8080) (1), [http://localhost:8080](http://localhost:8080) (1), [https://github.com](https://github.com) (1)

#### [Setting up Selenium](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/setting-up-selenium-24075622?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/setting-up-selenium-24075622?u=76281980&t=0)** Alright, let's jump right into setting up [Selenium](../../Skills/Software%20Development/Selenium.md). We need to set this up in two places, our test and our [Compose](../../Glossary/Framework/Jetpack%20Compose.md) file. Let's start with our test. We can go there by just using Ctrl + R and typing in Vim and finding the command that ends in "spec.rb". Unfortunately, the Capybara documentation doesn't have an example of setting up Selenium in a way that works with Docker easily. So to simplify things, the exercise files for this chapter have a version of this test that has all the code you need to enable that configuration. So what we're going to do is we're going to go to the second tab that we opened earlier, clear all this out with Control + L, and then we're going to copy the updated version of page_spec from our exercise files bundle. We can find it in the "05_08_before" folder. So what we're going to do here is we're going to type in "cp" and then you're going to provide the path to your exercise files. So in my case it's going to be "~/Downloads /exercise_files". Then we're going to type in "05_08 _before". And then we're going to copy "page_spec.rb" into "./spec/unit-test", or just "unit". And that's it. When you're done, go to the first tab and if you're using Vim you'll see that it warns you that this file has changed.
>
> **[1:31](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/setting-up-selenium-24075622?u=76281980&t=91)** So let's go ahead and press capital L to load the change file. If you're using Visual Studio Code or something like that, you might get a similar pop up. It'll just look different. Okay, let's go through the changes that I've made here. The first change that I made is adding 'require "selenium- webdriver"' right underneath 'require "capybara/dsl"'. No surprises there. We need Selenium WebDriver to use Selenium. Next I registered a new driver down here called "selenium". There's a lot of properties going on down here, so let's turn on line numbers by typing in ":set number" and going through some of these changes. So on line 10 I have this URL property here. The URL property tells the web driver where a Selenium server is. Notice that we're talking to a host called "selenium" here. That's going to be the address of the Docker Compose service that we created called "selenium", which we'll take a look at in a few minutes. The "options" property from lines 11 through line 17 configures Chrome in a way that works more efficiently within Docker. Finally, on line 22, this "default_driver" property tells Capybara to use Selenium instead of rack test. Now that we looked at our modified test, let's take a look at the Docker Compose file to see how the Selenium service is configured. So for those following along with me in Vim, I'm going to type ":e", and then I'm going to type "docker-compose.yaml".
>
> **[3:03](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/setting-up-selenium-24075622?u=76281980&t=183)** I used the Tab key to autocomplete that. Now that we're here, I'm going to scroll down a little bit just so that I can put this closer to the center of the page. Now we can see here that we're doing a lot of the same tricks that we did for the website service earlier. We're using this image over here to stand up an instance of Selenium. Notice on line 31 that I have another copy of this image if you're using the Intel chip. So if you're running all of this on an Intel Mac, go ahead and uncomment line 31 and comment line 33. However, if you're using a Mac with the Apple Silicon chip, or if you're using an Arm-based machine, you can just simply leave it as it is. We also have a few port mappings down here. Again, if you ran into the issue that I talked about earlier with 5900 being occupied or in use, you can change it to something else. In my case, I changed it to 5959. And then we also have a health check here that uses pretty much the same health check as our website, except this time it's using curl to test whether port 4444 is up. That's where the Selenium Hub or a component within Selenium is running. Now that we've taken a look at our Selenium service within Docker Compose as well as our modified tests, let's go ahead and quit this and move on. However, this is a good time to commit and push our changes, so let's go ahead and do that before moving on. Again, I'm going to use "[Git](../../Skills/Software%20Development/Git.md) status" to see what's changed.
>
> **[4:38](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/setting-up-selenium-24075622?u=76281980&t=278)** I'm using "git add" to add my changes and then I'm using "git commit" to add my changes. I'm going to say "Add unit test and updated Compose services". I'm not going to add a description here, you can if you like. And then I'm going to write and quit those changes. And then I'm going to push those changes up to GitLab. There we go. We're ready to move on!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Selenium](../../Skills/Software%20Development/Selenium.md) (15), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (6), [Git](../../Skills/Software%20Development/Git.md) (3)
> **CLI Commands:** docker (6), git (3), find (1), cp (1), curl (1)
> **Tools:** vim (3), visual studio (1), gitlab (1)
> **Code Identifiers:** page_spec (2), exercise_files (1), default_driver (1)
> **File Paths:** spec.rb (1), page_spec.rb (1), docker-compose.yaml (1)
> **UI Navigation:** go to (2), scroll down (1)
> **Exercise Files:** exercise files (3)
> **Env Vars:** url (2)

#### [Running your test with Selenium](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/running-your-test-with-selenium-24079511?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/running-your-test-with-selenium-24079511?u=76281980&t=0)** Right on! Let's use our "docker [compose](../../Glossary/Framework/Jetpack%20Compose.md) run" command from earlier to see what's changed. Remember, if you get this error, that means that you need to rebuild your container image before running our unit test. So to fix that, all we need to do is just add "--build" before or after the "--rm". And boom! Our tests are passing again. Here's a sweet trick. The [Selenium](../../Skills/Software%20Development/Selenium.md) service we created exposes something called a VNC server. You can think of VNC as a way of being able to drive or use a computer remotely, very similar to Remote Desktop, if you've used that before. If you have VNC installed on your system, you can see Selenium drive the browser while the test is running. Let me show you how. Now, since I'm on a Mac, I can use the screen sharing application which supports VNC. If you're on [Windows](../../Glossary/Service/Windows.md), there are two VNC clients that I recommend in particular. Let's take a look at them. The first client that I'd recommend is called RealVNC, and you can get to it by typing "[realvnc.com](https://realvnc.com)" in the address bar. While this looks like a much more "enterprisey" VNC client, it's actually free to use. So what you can do is you can click on [Products](../../Skills/Software%20Development/Microsoft%20Products.md) and then click on Download Now. Then you can look for RealVNC Connect for Windows and then just click on Download for Windows. Another option is TightVNC. To use that you can go to [tightvnc.com](https://tightvnc.com). This is an open-source VNC client, very easy to use and
>
> **[1:37](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/running-your-test-with-selenium-24079511?u=76281980&t=97)** has great features. So to download it all you have to do is click on Get your Free Copy of TightVNC or click on Download Now over here, and then click on the installer for Windows 64 or 32 bit, depending on your processor architecture, to download the installer and then follow the instructions that appear. Right, let's go back into the Mac. I'm going to type Command + Space, "screen sharing". You should see this little screen sharing window over here. All you have to do is type in "vnc://localhost5900" or the port that you changed in the Docker Compose file to work around 5900. So in my case it's going to be 5959. After that I'm going to click Connect or hit Enter. And then you'll be asked for a password. The password is very secure, I always forget, it's just "secret". This brings us to the desktop for our Selenium Docker Compose service. I want to reiterate this: We are looking at a desktop that is running inside of a container. How cool is that? Anyway, going back to our terminal, what we're going to do here is simply rerun the test. So I'm just going to hit the Up key and run that test again. And then after I hit Enter I'm going to go back to my VNC client. Let's go ahead and do that. That was crazy fast right? I hope you caught that. So what happened in that super quick blip of a browser that you saw? Our Selenium service received a command from Capybara to start Chrome with the options in our Selenium WebDriver configuration.
>
> **[3:15](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/running-your-test-with-selenium-24079511?u=76281980&t=195)** Then use that URL property to render the Explore California page that served in another container called "website". And then it sent the results that it got back to Capybara for further processing. Since it was so fast, let's just go ahead and do that again. I love doing this. Look at that. Amazing. So fast. And just like we saw before, we get a green dot indicating that our test passed. There we have it. We used Rspec and Selenium to write a really simple test against Explore California entirely within Docker. We're now ready to take this to the cloud. In the next set of videos, we'll upload our website into AWS and then use Rspec again to test against that with integration tests to test our site on the actual internet. Prepare yourself for more [DevOps](../../Topics/DevOps.md)!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Selenium](../../Skills/Software%20Development/Selenium.md) (6), [Windows](../../Glossary/Service/Windows.md) (4), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (3), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1), [DevOps](../../Topics/DevOps.md) (1)
> **Env Vars:** vnc (8), url (1), aws (1)
> **UI Navigation:** click on (6), go to (1)
> **CLI Commands:** docker (4), rm (1), aws (1)
> **Definitions:** means that (1), is called (1), is an  (1)
> **URLs:** [realvnc.com](https://realvnc.com) (1), [tightvnc.com](https://tightvnc.com) (1)
> **Cross-References:** go back to (1), in the next (1)
> **Analogies:** similar to (1), just like (1)


### 6. Infrastructure as Code with OpenTofu

[↑ Back to Table of Contents](#table-of-contents)

#### [Deploy with confidence with infrastructure as code](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/deploy-with-confidence-with-infrastructure-as-code-24075620?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/deploy-with-confidence-with-infrastructure-as-code-24075620?u=76281980&t=0)** Thanks to you, Explore California now has a simple test suite that they can use to confidently change the website with ease. Because our tests run in Docker, we can [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) assured that the site and its tests will behave the same no matter where they are. However, we have one big task left. We need to put our site somewhere. Explore California has chosen to use [Amazon Web Services](../../Skills/Cloud%20Computing/Amazon%20Web%20Services%20(AWS).md), or AWS, as its platform of choice. There are lots of ways that we can do this. Let's take a look at them now. We could use the web GUIs for each AWS service we choose to use. They are well designed and make doing complex things easy, like uploading websites, creating [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md), and even running built applications without managing any infrastructure at all. However, this will introduce serious scaling challenges for Explore California. Clicking through GUIs takes time. It also requires people to physically click things, sometimes lots of things. Adding files to AWS Object Storage Service, S3, takes over 20 clicks. This is fine if you're testing something or doing a task that you won't do very often, but that's not the case for us. Remember, the CTO wants changes to get shipped in two days or faster. Explore California is only going to get bigger too, so doing things this way will slow us down a lot. This is where [Infrastructure as Code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) steps in. Infrastructure as Code, or IaC for short, is a technique that uses command-line tools and/or [application programming interfaces](../../Skills/Software%20Development/Application%20Programming%20Interfaces%20(API).md) or APIs to automate the creation and
>
> **[1:36](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/deploy-with-confidence-with-infrastructure-as-code-24075620?u=76281980&t=96)** configuration of infrastructure like virtual machines, networking equipment, storage devices, and security systems. CLIs and APIs are often much faster than using GUIs. They change less frequently and can be incorporated into other tooling, like shell scripts to automate many tasks. Let's take a look at what our scenario above looks like with a dash of IaC applied onto it. All of the services available on AWS can be configured through a command-line client called AWS [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md). With this tool, the 20 clicks we needed to interact with S3 became just two commands. That's quite the improvement, but we can do better than that. As you can see here on this slide, AWS CLI is just a front end to the vast network of APIs available within AWS. If we wanted to, we could use any programming language, like the TypeScript shown here, to write code that does the same thing into API calls. This would allow Explore California to have the code they used to create infrastructure be the same as the code they use to maintain the websites. That's powerful stuff. In this chapter, we're going to use a tool called OpenTofu. OpenTofu is an open-source version of [Terraform](../../Skills/DevOps/Terraform.md), a tool from HashiCorp. We're going to use OpenTofu to write and deploy Explore California's website into AWS S3. OpenTofu uses the API technique we saw earlier to deploy infrastructure across multiple platforms, with a single language and a single code base. OpenTofu also compares changes to your infrastructure code against
>
> **[3:12](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/deploy-with-confidence-with-infrastructure-as-code-24075620?u=76281980&t=192)** the state of your actual infrastructure. It does this every time you need to deploy, so that you get exactly the infrastructure you need when you need it. We're going to start by getting OpenTofu running within Docker, since we're all about containers in this shop. After that, we're going to write an end-to-end test that will verify that our site is live on the internet. From there, we'll write the OpenTofu configuration code needed to make that test pass, and then do an OpenTofu run to apply it. Finally, we'll run our end-to-end test and see how we did. Let's do it!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) (2), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Amazon web services](../../Skills/Cloud%20Computing/Amazon%20Web%20Services%20(AWS).md) (1), [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (1)
> **CLI Commands:** aws (8), docker (2), make (2), terraform (1)
> **Env Vars:** aws (8), cli (2), api (2), cto (1)
> **Definitions:** is a  (1), is an  (1)

#### [Finding the version of OpenTofu to use](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/finding-the-version-of-opentofu-to-use-24082450?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/finding-the-version-of-opentofu-to-use-24082450?u=76281980&t=0)** Before we dive into OpenTofu, let's find the latest version of OpenTofu that's available for us to use. To do that, I'm going to open a web browser like you see here, and then visit [github.com/opentofu/opentofu](https://github.com/opentofu/opentofu). That's going to bring us to OpenTofu's source code on [GitHub](../../Skills/Software%20Development/GitHub.md). From here, what I'm going to do is I'm going to click on Main, and then I'm going to click on Tags. And that will give us all of the versions that are available for us to use. We're going to use the highest-number version on this list that does not have an alpha, a beta or an rc reference in the name. In our case, that's going to be version 1.71. Now that we have our version number in tow, let's head on over to docker-[compose](../../Glossary/Framework/Jetpack%20Compose.md).yaml so that we can use it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [GitHub](../../Skills/Software%20Development/GitHub.md) (2), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1)
> **CLI Commands:** find (1), docker (1)
> **Tools:** github (2)
> **UI Navigation:** click on (2)
> **File Paths:** docker-compose.yaml (1)
> **URLs:** [github.com](https://github.com) (1)
> **Versions:** version 1 (1)
> **Exercise Files:** source code (1)

#### [Adding OpenTofu to Docker Compose](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/adding-opentofu-to-docker-compose-24082452?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/adding-opentofu-to-docker-compose-24082452?u=76281980&t=0)** Now that we got the version of OpenTofu to use, let's go ahead and add it to our docker-[compose](../../Glossary/Framework/Jetpack%20Compose.md).yaml. To do that, I'm going to type "vim", then "docker-compose.yaml" to open it in my editor. Next I'm going to look for the OpenTofu service. It should be pretty early in the manifest file. However, if you cannot find it, all you have to do is search for it, like I did here. So let's move to the image line down here. To save time, I went ahead and created the OpenTofu service for you However, you might be using an older version of OpenTofu in your copy of the exercise files. If that's the case, simply remove the version that's there and replace it with the version that we found on [GitHub](../../Skills/Software%20Development/GitHub.md). Next, let's pay attention to the volumes that we're mounting down here. The first volume that we're mounting is this "website" folder. That's where our website is. The second folder is the folder that we're going to use to store our configuration code that's going to get Explore California into the cloud. That folder is going to live in our current working directory, or PWD, like you can see here in a folder called "infra". Now, you might see actual "PWD" over here to the left of that. We can ignore that for now, but we'll explain why that's there later on. So that's it. That's all we need to get OpenTofu to work. So let's go ahead and take it for a spin. I'm going to save my changes here and quit my editor. And then I'm going to run "docker compose run --rm opentofu version". This will send the version command to the OpenTofu application
>
> **[1:37](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/adding-opentofu-to-docker-compose-24082452?u=76281980&t=97)** within our OpenTofu service. If we're successful, we should get a version number back like 1.7.1 in our case. So here we're pulling the image. This should be pretty quick, but it will vary depending on the speed of your network connection. So here we go. Cool. We got our version number back right here. And it matches the version that we copied from GitHub. Great. Now, you might be thinking, "Don't we need to create that 'infra' folder that we mounted before?" As it happens, we actually don't. If you type "ls" over here you can see that Docker created it for us right here. This is a convenient side effect of using bind mounted volumes. If the directory we're asking to mount doesn't exist, Docker will create it for us automatically. However, big note here. Note that Docker will always create directories when this happens. So what does that mean? Let's go back to the docker-compose.yaml to illustrate what I mean here. Let's say we created a new volume mount underneath this called "some_file.txt", and we mounted it into the container at "/file". Well, some_file.txt doesn't actually exist in our working directory. So what's going to happen when I run this? Well, let's find out. I'm going to write and quit my change. And then I'm going to rerun that OpenTofu version command from earlier. As you can see, it still works, no problem. However, if I do an "ls" over here, we can see that there's a file
>
> **[3:12](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/adding-opentofu-to-docker-compose-24082452?u=76281980&t=192)** called "some_file.txt". This is very deceiving. Let me show you why. So it looks like a file, right? So I should be able to do "cat some_file.txt". But it's a directory. What happened there? Well that's the side effect of using bind mounted volumes that don't exist. If that file doesn't exist, Docker will always create it as a directory. So you'll get errors like the one that we saw before if this happens. So for now let's go ahead and remove that. So you don't actually need that. Save our changes. And then let's go ahead and remove that pesky fake file from our working directory. And now that we did all of that let's go ahead and move on. However, before we continue, let's go ahead and make sure that we push any changes that we've made to our [Git](../../Skills/Software%20Development/Git.md) repository up to GitLab. So as you can see we have a single file that was changed. Let's go ahead and add it. Let's commit that change. I'm going to use the "-m" flag that's associated with commit so that I can create a single-line commit. This is really handy if you have a change that's really small, you don't want to add a description, you just want to add a single line. So in my case I'm going to say "Update OpenTofu version to 1.7.1", "docker-compose.yaml", and then before I post my changes, I'm going to use "git pull rebase" to make sure that I'm receiving whatever changes were made on GitLab into my clone, and then I'm going to
>
> **[4:46](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/adding-opentofu-to-docker-compose-24082452?u=76281980&t=286)** push my changes. Let's move on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (5), [GitHub](../../Skills/Software%20Development/GitHub.md) (2), [Git](../../Skills/Software%20Development/Git.md) (2)
> **CLI Commands:** docker (9), find (2), ls (2), make (2), git (2)
> **File Paths:** docker-compose.yaml (4), some_file.txt (4)
> **Tools:** github (2), gitlab (2), vim (1)
> **Code Identifiers:** some_file (4)
> **Env Vars:** pwd (2)
> **Versions:** 1.7.1 (2)
> **Cross-References:** go back to (1)

#### [AWS deployment explained](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/aws-deployment-explained-24082453?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/aws-deployment-explained-24082453?u=76281980&t=0)** In our last video, we went over how to get OpenTofu up and running in Docker through Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md). In this video, we're going to use that knowledge to create our first OpenTofu deployment in AWS. Very exciting stuff! But before we begin, let's briefly talk about what Explore California will look like within AWS. [Amazon Web Services](../../Skills/Cloud%20Computing/Amazon%20Web%20Services%20(AWS).md), or AWS, is a cloud provider that enables users to create all sorts of computing resources throughout the world. AWS offers many services to meet many different needs from simple [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) with Elastic Cloud Compute, or [EC2](../../Skills/DevOps/Amazon%20EC2.md), to entire pipelines for processing massive amounts of data very, very quickly, like AWS Kinesis Firehose. Like we learned when we started this chapter, one of the big advantages AWS provides is that nearly every interaction with it is done through API calls. This is nice because this gives people many ways of interacting with AWS. From the web console at console.aws.[amazon.com](https://amazon.com), to several programming languages through the AWS SDKs. A very popular AWS service that we've been referring to in the last few videos is the Simple Storage Service, or S3 for short. S3 is a service where anyone can store nearly anything into buckets. Yes, objects are stored in things called S3 buckets. Access to those objects can be finely controlled. You can have things available to the entire internet or have them restricted to just a few users. Many websites and companies around the world store petabytes of data into AWS daily.
>
> **[1:36](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/aws-deployment-explained-24082453?u=76281980&t=96)** S3 comes with a really handy web-hosting feature, where it can basically host static websites based on files within the bucket. Explore California is a static site. This seems like a match made in heaven. Thus, our OpenTofu deployment will basically look something like this. First we're going to create an S3 bucket. Then we're going to configure that S3 bucket as a website. Then we're going to copy the Explore California website into that bucket. And then we're going to use Rspec to test that our website is accessible from the wider internet. Are you as excited as I am? You should be! Let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1), [Amazon web services](../../Skills/Cloud%20Computing/Amazon%20Web%20Services%20(AWS).md) (1), [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) (1), [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (1)
> **CLI Commands:** aws (11), docker (2)
> **Env Vars:** aws (10), ec2 (1), api (1)
> **Definitions:** is a  (3)
> **URLs:** [amazon.com](https://amazon.com) (1)
> **Prerequisites:** configure (1)

#### [Writing your integration test](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-integration-test-24079508?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-integration-test-24079508?u=76281980&t=0)** Just like our website, we don't want to begin by blindly writing and deploying things. We want to begin with a test that we can run that will automatically verify that everything is working. That's from the knowledge that we learned from the video to do exactly that. We're going to write a simple test that validates that the website loads when we hit the URL within AWS S3. Since we're testing an instance of this website on real infrastructure, we can call this an "integration test." In some videos, you'll hear me call this an "end-to-end test." In some contexts, integration tests and end-to-end tests are different things, but in this context we can use them interchangeably. So if you hear me say end-to-end test or integration test, they're the same things in this journey. So let's start by creating a folder within "spec" called "integration". After that, we're going to create a new "spec" file inside of "integration" called "website_spec.rb". Now that we created the website_spec.rb file, let's go ahead and open it in our editor. So just like we saw in our unit test, our test suite is going to be wrapped around a describe block. So let's go ahead and write that now. Let's call this test suite "real website", something simple. After we recreate our test suite, let's go ahead and create a single test case in here with an "it" block, just like we did with our unit tests. So let's call it "It should be Explore California." Again,
>
> **[1:31](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-integration-test-24079508?u=76281980&t=91)** this can be called anything you'd like. I'm just calling it this in this example. Now in a new tab or a split pane like I'm about to do, or in a new window, let's go ahead and open that unit test so that we can use it as a handy reference for what we're about to do next. So just so that we orient ourselves, our unit test is over here on the left side, and our integration test is over here on the right side. If you're using them like I am to switch between panes, all you have to do is hit the Ctrl key and hold it, then press W twice like this. You'll see the cursor over here move into the pain that you're currently in. So right now this gray cursor is over here on the right side. The color might be different depending on the terminal that you're using. Just look for a block like this or a small underline. And then if I want to move back to the left side I'm just going to hit Ctrl. I'm going to hold it. Then I'm going to press W, then W and there we go. Our cursor moved over to the left side. So that's how you switch between two panes in Vim. So now what we want to do is copy a couple of things from our unit test over here on the left to our integration test on the right. So I'm going to use capital V to highlight this line, then J twice to copy our "requires". And I'm going to press J again to copy our "include". I'm going to press Y to copy all of that. Then I'm going to press Ctrl + W + W to go to the right side. I'm going to press lowercase G twice to go to the top. Then I'm going to press I to enter insert mode,
>
> **[3:04](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-integration-test-24079508?u=76281980&t=184)** Enter to add a new line, the Up key to go back up, and then I'm going to press Ctrl + O to temporarily go out of insert mode into edit mode or normal mode. And then I'm going to press P to paste in what I just copied. Then I'm going to go Up, Ctrl + O once again to get out of insert mode, press D + D, or lowercase D twice, to get rid of that empty line. And then I'm going to press either Ctrl + [ or Escape to go back into normal mode. And you can see that by the "INSERT" banner on the bottom left having gone away. Great! So now we have all the libraries we need to run a Capybara test in [Selenium](../../Skills/Software%20Development/Selenium.md). So what I'm going to do next is I'm going to go copy this Capybara register driver block over here so that we can configure Capybara to talk to our Selenium instance. So I'm going to go ahead and highlight this, copy it, Ctrl + W + W to go to the right side. And then I'm just going to press the P key to paste it right underneath "include". But you can also see that it pasted it right above "describe", which is really nice. So let's make sure that we have everything we need. Ah, right, we need this copy bar of default driver lines. Let's go ahead and copy that. W + W to go to the right side. P underneath N to paste it in. And there we go. Done. Now we definitely have everything we need. So let's go back to our unit test. And let's just go ahead and press Ctrl + W + C to get rid of it.
>
> **[4:40](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-integration-test-24079508?u=76281980&t=280)** Now it doesn't delete the file. The file is still there. Please don't worry. We still have everything that we worked on before. So what do we want to do instead of this "it" block over here? That's where our test actually is. We want to do two things. We want to confirm that the website is reachable, and we also want to confirm that it's the Explore California site that we know and love. We can do that in one test. So let's go ahead and write an example of that. So first we can have a "visit" here. And let's use the same "visit" that we used before. And then right underneath that, just like we did for our unit test, we can have an "expect" statement here that confirms that some property on the page equals something that we want. We could use 'a.class == "logo"' like we did in the last test. But we should have something higher fidelity than that, right? Because we want to make sure that when we go to that page on the internet, we're actually talking to Explore California. So we could use the page title. What do I mean by this? Let's go into our browser and go back to Explore California briefly. So I'm going to go to the address bar and type "localhost:8080". There is our website. And then I'm going to just right-click on the logo and click on Inspect Element. So there's a lot of other stuff that we can use to verify that this is actually indeed Explore California. One thing that I like to do is just make sure that the title of the page matches what I expect. So we can see here that the title of our page is "Welcome to
>
> **[6:15](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-integration-test-24079508?u=76281980&t=375)** Explore California." This is a pretty good sign that tells me that I am actually looking at Explore California for real. So I'm going to go ahead and use the page title in my "expect" statement. So let's go back. Let's go ahead and change this by removing all of this stuff and then using "page.title". And then after that we'll put a '.to eq "Welcome to Explore California"'. A couple of notes about what we're looking at. ".title" is a special property of the "page" object. Remember "page" is an object that Capybara provides for us when we're running tests. That represents the page that we got from Selenium. The ".to" property over here is the operator within RSpec that allows us to make an assertion. An assertion is basically the test. It's a condition that we can specify that makes our test true, that we expect to have happen. There are many different kinds of assertions we can make, but the one that we're using over here is "eq", which is short for "equals". So what we're writing here is we're expecting the title of the page, page.title, to equal "Welcome to Explore California." There are many different types of assertions that you can make with RSpec. Our RSpec calls these assertion types "matchers". So if you're interested in learning more about that, just check out the URL that's on the bottom of this video to go to the documentation from RSpec, where they list out all of the matchers that you can use. Perfect. Let's save this and turn our attention to the Docker
>
> **[7:50](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-integration-test-24079508?u=76281980&t=470)** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) file. So, just like Selenium, website and OpenTofu, I wrote the Compose service that we're going to be using to run our end-to-end test or integration test. I handily called them "integration test." So we can see that down here. All you have to do is search for integration tests. Or if you're in Vim, just press the Forward Slash key and then type "integration tests". So as you can see it looks exactly the same as our unit test up here. The only difference, if I turn on line numbers, is lines 55 and 68. As you can see there is a single difference between them, and that is the folder that I'm expecting my test to be in. On line 68, for my integration test, I'm expecting to use the "integration" folder, so it's only going to load integration or end-to-end tests, like the one that we just wrote. Whereas on line 55 when we wrote our unit tests, it's only going to load the unit tests that we wrote for Explore California, which are in the "unit" folder. This way we're not running integration tests, which can take a lot longer to execute, alongside of our really fast, really numerous unit tests. And vice versa, of course. So before we commit our changes and push them up to GitLab, let's take a look at that integration test one more time. Specifically, let's look at the "visit" property down here. Now, as you probably noticed, this is going to run our test against localhost.
>
> **[9:24](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-integration-test-24079508?u=76281980&t=564)** However, we want to write an end-to-end test that tests against the version of Explore California that is on the internet. Right? So this isn't going to work, but we also don't know what that URL is going to be in advance, especially since it's going to be in some random bucket or some bucket with a random name with a random URL that AWS S3 is going to assign to it. So how do we account for that unknown property? Well, one way we can do this is with environment variables. Environment variables are a lightweight way of providing configuration to running applications. When you're in a shell like Bash, when you define or export environment variables into your shell, your operating system will automatically provide those environment variables to every application that is created within that shell session. While applications still need to add some code to read those environment variables and do stuff with them, applications always get their parent's shell environment variables. No ifs, ands or buts. So how do we use that property here? Well, like I said, we need to change our code to use environment variables. With Ruby, we can do that with the built-in object "ENV" in all caps. ENV is what's called a "hash map" in Ruby that has all the environment variables that our Ruby script knows about. So let's go ahead and assume that we're going to create one called "website_url".
>
> **[10:56](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-your-integration-test-24079508?u=76281980&t=656)** Now environment variables can be blank. They could just not be defined. So how do we fail this test if that isn't defined yet? Well, one way we can do that is above the describe block. We can say something like "raise", which is the way to create errors in Ruby, '"Please set the website URL" if ENV ['WEBSITE_URL']' is not defined or if it's nil. This will make our test fail if we forget to define this environment variable in our shell. With all of that, let's go ahead and save our changes and push our work up to GitLab. So "[Git](../../Skills/Software%20Development/Git.md) status" to check what's changed. "Git add" to add the changes into our staging area. "Git commit". Commit those changes. Let's say "We're adding integration tests" and I'll add a description here saying "This also introduces a new environment variable... a dependency on a new environment variable called WEBSITE_URL". Awesome. I'm going to save that commit and now I'm going to "git pull rebase". You almost saw me type "git push" there. I want to "git pull rebase" to make sure that I get the changes from other developers. Up -to-date, we have no other changes. Now I'm ready to "git push". And there we go. So let's go ahead and write some infrastructure code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (7), [Selenium](../../Skills/Software%20Development/Selenium.md) (4), cursor (3), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (2)
> **CLI Commands:** make (8), git (7), ruby (4), aws (2), docker (1)
> **Env Vars:** url (5), env (3), aws (2), website_url (2), insert (1)
> **UI Navigation:** go to (7), right-click (1)
> **Tools:** vim (2), gitlab (2), terminal (1), bash (1)
> **Definitions:** is a  (3), is an  (1), short for (1)
> **Analogies:** just like (5)
> **Code Identifiers:** website_spec (2), website_url (1)

#### [Writing OpenTofu code](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-opentofu-code-24077627?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-opentofu-code-24077627?u=76281980&t=0)** We're ready to create OpenTofu configuration that allows us to do this deployment. Since our OpenTofu file will have a number of moving parts, I've written it for you in advance. The configuration that we'll be using is called "website.tf", and you can find it within your exercise files bundle in the "06_05_before" folder. So just like we've done for the other exercise files, let's go ahead and copy this into our "infra" directory. So what we're going to do is "cp", then add the path to your exercise files, again in our case it's going to be "Downloads/exercise_files", then "06_05_before". And then after that we'll type in "website.tf". And then we're going to put it in the "infra" folder. Great. Now that we have it let's dive in. So let's go into "infra", "website.tf" to open it in your editor. The first thing we'll focus on is the differences between OpenTofu and [Terraform](../../Skills/DevOps/Terraform.md). As of this writing, there are none. OpenTofu is a fork of Terraform. So if you were using Terraform in the past, or if you're using Terraform for this course, everything that you're looking at is interchangeable. So what you're looking at here, the Terraform code can also be run in Terraform in exactly the same way. The next thing we'll focus on is the name of the file. OpenTofu configuration files always end in ".tf". When OpenTofu applies your configuration to your target folder, it uses resources defined in every
>
> **[1:37](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-opentofu-code-24077627?u=76281980&t=97)** .tf file it can find in OpenTofu's working directory. Now with that out of the way, let's focus on the code in front of us. As you can see, there is a lot happening. We won't go through everything, but there is enough here to familiarize ourselves with the basics of Tofu. First, let's look at the structure of this code. Let's talk about that a little bit. OpenTofu configuration files are written in a domain-specific language, or a DSL, called the HashiCorp Configuration Language, or HCL. HCL claims to be simple enough for programmers and non-programmers alike to define the resources that they want to create. Now, it's important to note that DSLs like HCL are not programming languages, while HCL does allow you to do some simple things like if/else conditional statements and for loops, it is very limited in what it can do compared to a full-blown programming language like [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), Ruby or Golang. If doing that style of [Infrastructure as Code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) interests you, there's a great project called Pulumi. Pulumi by Pulumi Labs is a great alternative for software developers that want to define infrastructure with a real-deal programming language. You can check it out at [pulumi.com](https://pulumi.com). Alright, moving on, we're going to search for the keyword "resource". And then let's scroll down a little bit so that we can see it more clearly. We're going to focus on three key primitives that open top supports. First, there are resources, which you can see examples of over here.
>
> **[3:16](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-opentofu-code-24077627?u=76281980&t=196)** Resources are representations of actual infrastructure that you can create. So for example, if I scroll up a little bit, this AWS S3 bucket resource, for example, will create an actual bucket within AWS S3. There are way too many types of resources that you can create with OpenTofu. You can even create resources that do literally nothing. Even though we're using OpenTofu to describe our infrastructure, you can actually learn about these resources in Terraform's documentation. Click on the link on the bottom of your screen to learn more. The next primitive that we're going to look at are variables, which you can find by scrolling up and searching for "variable". Variables allow you to define your resources from outside of your configuration code. There are several ways of defining variables, but we're only going to look at two of them. The first way is with this "variable" keyword. The "variable" keyword has a name. So like website domain name over here. And inside of it you can define several things. For example, you can define a description which provides a description of the variable. And you can provide a default value in case you don't want your configuration code to fail if you forget to define your variable. So for example, our default over here is "[explorecalifornia.org](https://explorecalifornia.org)". Another way of defining variables is with local variables, which you can see examples of by searching up and scrolling for "locals". Local variables, which are shown here, are similar to variables in that they allow you to change the configuration of your resources
>
> **[4:50](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-opentofu-code-24077627?u=76281980&t=290)** throughout your configuration code. However, the difference between these and the variable that we saw earlier is that you cannot set these local variables from outside of your configuration. So for example, this mine types local over here can only be set within this configuration code. So I can't change this when I go to apply my infrastructure with the OpenTofu [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md). The next primitive that we have are outputs. So you can search for them by scrolling down and looking for "output". Outputs allow you to export various information about resources that you've created in your configuration code. This is useful for a number of things. If you're writing scripts to interact with OpenTofu, you can take this output and do further things with it, or you can just have it available so that when people want to inquire about what's been deployed into AWS with OpenTofu, you can have these outputs describe exactly that. This website URL output is a good example of this. You might recall in the last video that we created a reference to an environment variable in our integration test called "WEBSITE_URL". Can you guess where that environment variable is going to come from? That's right. It's going to come from this output right here. We'll see how we can get the value of this output when we apply this configuration in a later video. Now while I only went deep into three OpenTofu primitives, there are many others that you can create. For example, you can create data sources that obtain information from your provider to reuse later, and modules to, say, take all of
>
> **[6:28](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-opentofu-code-24077627?u=76281980&t=388)** this configuration and create hundreds of websites, for example, with it repeatedly. We won't go deep into these concepts to save time, but fortunately, [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning has tons of content on Terraform that can help you learn more. For example, again, since OpenTofu and Terraform are interchangeable, you can check out Josh Samuelson's "Learning Terraform" course if you're interested in everything that Terraform or OpenTofu have to offer. If you absolutely must go deeper, David Swersky's "Advanced Terraform" has you covered. Okay, before we go, why don't we talk about the thing that you were thinking of when you were looking at this? What the heck is this code actually doing? I'm glad you asked. Like I said earlier, let's go resource by resource and see exactly what's going on. I went to the top with G + G and now I'm going to search for "resource". So the first thing that we're doing is we're creating an AWS S3 bucket called "website". The type of thing that we're creating is always going to be the first thing that you provide after the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) "resource". And then the name of that resource is always going to be the second thing. So we're creating an AWS S3 bucket type of resource called "website". That's going to create as a bucket that's going to be the permutation of these two properties or these two resources, a random string called "bucket_name_prefix", and our "website_domain_name" variable. So our random string "resource" is up here, that just creates a random string, eight characters long,
>
> **[8:03](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-opentofu-code-24077627?u=76281980&t=483)** all lowercase with no special characters. Nothing exciting going on over there. There are several other properties that we need to define in order to configure our S3 bucket, to make it available for public use. That's what this AWS S3 Bucket Ownership Controls and AWS S3 Public Access Block objects are doing. What makes this a website though, is this AWS S3 Bucket Website Configuration resource. That was a mouthful. There are two things that we can configure here. We can configure the index document, which is the thing that you see when you go to that bucket's website, and the error document, which is what you see when a page that we try to load does not exist. Finally, what actually copies our stuff into the website is this S3 object "website resource". Now this is actually pretty cool. Remember how I said earlier that HCL is not a programming language, but has some things that you can do that you would see in a programming language? This is a great example of that. So this "for_each" line over here is basically taking every file that's underneath "website" and adding it to our bucket with these properties down here. This is pretty cool because by doing this, we don't have to write a script that uses the AWS CLI to copy all of those files into our bucket. So now that we've seen the configuration code for our infrastructure, as well as explain what's going on in the next video, we're going to take this
>
> **[9:37](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-opentofu-code-24077627?u=76281980&t=577)** configuration and turn it into real things that we can touch and test. Onward!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Terraform](../../Skills/DevOps/Terraform.md) (12), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** terraform (12), aws (9), find (3), cp (1), python (1)
> **Env Vars:** aws (9), hcl (5), cli (2), dsl (1), url (1)
> **Analogies:** for example (8), just like (1), similar to (1)
> **Definitions:** is a  (5), is called (1)
> **UI Navigation:** go to (2), scroll down (1), scroll up (1), click on (1)
> **Code Identifiers:** exercise_files (1), bucket_name_prefix (1), website_domain_name (1), for_each (1)
> **Exercise Files:** exercise files (3)

#### [Reviewing the OpenTofu plan](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/reviewing-the-opentofu-plan-24077624?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/reviewing-the-opentofu-plan-24077624?u=76281980&t=0)** Infrastructure provision with OpenTofu is done in two steps. The plan step and the apply step. The plan step shows you what OpenTofu is going to create, change or delete. This way you can spot-check your work and make sure that OpenTofu will create what you're expecting to have created. The apply step, well, applies those changes. Let's take the HCL that we saw earlier and turn it into a plan. First, we'll need to initialize OpenTofu with "open tofu init". The way we do that is by running "docker [compose](../../Glossary/Framework/Jetpack%20Compose.md) run --rm open tofu init". This will [Fetch](../../Skills/Web%20Development/Fetch.md) code for the provider that we need and perform other configurations. What OpenTofu is doing here is fetching the AWS provider that makes all of those resources work, as well as the random provider, which was used for the random string resource that we saw before. You'll know that OpenTofu has finished initializing when you see this green message over here saying that OpenTofu has been successfully initialized. So now that OpenTofu is initialized, we can generate the deployment plan with "open tofu plan". So let's clear our screen and go ahead and run that. So this error is basically telling us that we don't have any ways to authenticate with AWS. So we can fix that by looking at our Docker Compose file. So let's take a look. We can see in this environment property that there are four environment variables that OpenTofu will be using to
>
> **[1:39](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/reviewing-the-opentofu-plan-24077624?u=76281980&t=99)** connect it with AWS. That will be the access AWS Access Key ID, the AWS Secret Access Key, the AWS Session Token and the AWS Region. We configured these environment variables earlier when we configured AWS. So if you haven't seen the "Configuring AWS" video from earlier, go ahead and check out that video to configure these environment variables and then come right back. Before we continue, let's get out of our Docker Compose file and make sure that we've exported our environment variables for AWS. We can check that by running "export grep -i aws". You can also run "ENV grep -i aws" to get the same set of information. So as you can see in both snapshots, we have AWS Access ID to find, AWS Region, AWS Secret Access Key, and AWS Session Token. These four variables need to be defined before moving on. Now that we define those environment variables, let's give our OpenTofu plan command another shot. Super! We have a plan. Let's go through it real quick. OpenTofu will create everything with a plus sign preceding it. Because this is the first time that we're generating a plan, everything has a plus sign before it and everything will be created as a result. Anything with a minus sign before it will be destroyed or removed by OpenTofu. Anything with a plus/minus sign like you see below is considered a tainted resource, and OpenTofu will destroy and
>
> **[3:15](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/reviewing-the-opentofu-plan-24077624?u=76281980&t=195)** recreate that object to "untaint it". Now that our plan looks good, it's time to apply it. We'll do that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (3), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **CLI Commands:** aws (16), docker (3), make (2), grep (2), rm (1)
> **Env Vars:** aws (14), hcl (1), env (1)
> **Cross-References:** in the next (1)
> **Prerequisites:** configure (1)

#### [Applying the OpenTofu plan](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/applying-the-opentofu-plan-24077623?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/applying-the-opentofu-plan-24077623?u=76281980&t=0)** In the last video, we learned how to use the OpenTofu plan command to see a preview of the resources that OpenTofu will create from our OpenTofu configuration. In our case, we saw how OpenTofu will create an AWS S3 bucket for Explore California and upload all of the website's assets into it. Let's learn how to make our plan a reality. This step is really easy. All we need to do here is clear our screen and run "docker [compose](../../Glossary/Framework/Jetpack%20Compose.md) run --rm opentofu apply". That's it. After doing this, you'll see the plan that we created before once again. This time, however, you'll be asked whether you want to confirm your changes. Since we already know that everything looks good here, I'm going to go ahead and type in "yes" to confirm them. Once I hit Enter, we are off to the races. We'll see a lot of stuff happening here and it won't take very long. That was blazing fast. Just for curiosity, let's run "opentofu plan" again to see what differences we get this time. We can see here that we're getting a lot more output than we did before. We can also see that towards the bottom, that surely enough nothing has changed. How does OpenTofu know this? This brings us to one of OpenTofu's best features. OpenTofu doesn't just create infrastructure for you. It also tracks changes to that infrastructure. It uses something called a state file to do this. Let me show you what I mean.
>
> **[1:32](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/applying-the-opentofu-plan-24077623?u=76281980&t=92)** Our OpenTofu configuration lives in this "Infra" folder, right? Well, if we use "ls" to list the contents of that folder, we can see that there's a little bit more stuff in here. Specifically, we can see a [Terraform](../../Skills/DevOps/Terraform.md) .tf state file listed. This is OpenTofu's database of our infrastructure. Everything that we created when we ran "opentofu apply" gets persisted into here. It's just a plain text file. So we could even run "cat" against it to see what all's in it. I hope that while I'm scrolling upwards, you see the problem with this. This is OpenTofu's plain text database of all of your infrastructure. There's probably stuff in your infrastructure that you rather not have laying around like this. We're also working within a [Git](../../Skills/Software%20Development/Git.md) repository, so someone could easily commit and push this file into GitLab, which we can confirm by running "git status" and then running "git add -infra" and then running "git status" once again. If we were to push this on to GitLab, even though our GitLab instance is private, all of the Git commands that we were using before would work on a public developer platform as well. So if we were to push this up to something like the internet version of GitLab, we could leak all of our infrastructure to the entire world. There are bots on the internet that are constantly scanning Git repos like this to find AWS credentials, IP addresses, and other valuable information to gain access into all sorts of stuff.
>
> **[3:06](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/applying-the-opentofu-plan-24077623?u=76281980&t=186)** This attack happens all of the time. Another problem that might happen is contention. So what if we are applying this plan and some other colleague decides to change our Terraform infrastructure? If they apply those changes, then we're going to have a conflict. One of us isn't going to be able to apply infrastructure anymore. Fortunately, OpenTofu has a feature to solve this exact problem: remote state. Remote state configures OpenTofu to store the state file elsewhere. OpenTofu does this through backends. You can think of backends as secure places to store your state files. Some remote state backends also supports something called "locking", which prevents the collision problem that I was talking about before. It uses a technique also called "locking" to prevent the "multiple rider problem" that I mentioned before. Now, we won't cover remote states in this course, but check out the Terraform courses on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning to learn more about that. Alright, back to hacking. So once again we don't want to commit any of these super secret things. So what we're going to do is we're going to use this command up here called "git restore --staged" to stage these changes. So let's go ahead and run that now, "git restore --staged infra", to remove that directory from our staging area. Confirm that it's removed with "git status". And then we're going to create a new file called "gitignore" which gets users to ignore these changes.
>
> **[4:41](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/applying-the-opentofu-plan-24077623?u=76281980&t=281)** Any files or directories that get added to the ".gitignore" file will not be tracked by Git. This is useful for sensitive data like our state file, since we can have them live inside of our codebase, but not live inside of GitLab or something like this. So to add those files to the getignore, what we're going to do is we're going to type "echo" and then we're going to type "infra terraform.tfstate". And then we're going to use two right angle brackets like this. And then we're going to type "gitignore" at the end of that. You also saw that there's a bunch of that Terraform stuff in here that we don't want to apply. So let's go ahead and run "echo infra /.terraform" and add that to the gitignore as well. So after I do that when I run "git status" you should see that there's two entries now. We should see that there's a ".gitignore" and an "infra" directory. So if I add both using "git add .gitignore" and "infra" and I run "git status", you can see that there is now just one file remaining that we would probably not want to commit, this terraform.lock file. We can do the exact same thing that we did before. We can use "git restore --staged infra/.terraform .lock.hcl" to remove it. Then we can just add that to our gitignore by typing in "echo infra/. terraform. lock.hcl >> .gitignore".
>
> **[6:13](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/applying-the-opentofu-plan-24077623?u=76281980&t=373)** And then if I run "git status" one more time, we can see that our Git ignores both staged, as well as not staged. The reason for this is because even though we've staged the gitignore file earlier, there were some changes made to it that haven't been staged yet. Remember, Git tracks individual changes, not entire files. So it's very possible that you can have files that are both staged to be committed as well as in a not-staged state. So let's go ahead and stage those changes. And then we can also see that the terraform.locl.hcl file is also gone. If I run "git status" again we can confirm that once again. Earlier we talked about using outputs to get information from our resources. Specifically, we talked about a output called WEBSITE _URL. How do we actually get that output? Well that's very easy. All we have to do is type "docker compose run --rm opentofu output". This will show us all of the outputs that OpenTofu knows about from the resources that we've created. So we only want this website URL output, and we don't need all this other junk like website URL equals and these double quotes. So to remove that what I'm going to do is I'm going to rerun this command. I'm going to add "-raw" to the end of it. And then I'm going to add "website_url". And there we go. We just got back the URL of the bucket. So what happens if I actually go to this URL in a browser?
>
> **[7:50](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/applying-the-opentofu-plan-24077623?u=76281980&t=470)** So let me actually copy this. Open a web browser, paste it into the address bar and hit Enter. If I do that we have Explore California on the internet! So it's time for the final boss. Does this work with the end-to-end test that we wrote before? Let's find out in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (18), [Terraform](../../Skills/DevOps/Terraform.md) (10), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** git (18), terraform (10), aws (2), docker (2), rm (2)
> **Env Vars:** url (4), aws (2), website (1)
> **Tools:** gitlab (5)
> **Cross-References:** we talked about (2), in the last (1), in the next (1)
> **File Paths:** terraform.lock (1)
> **Code Identifiers:** website_url (1)
> **UI Navigation:** go to (1)

#### [Running your integration test](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/running-your-integration-test-24082451?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/running-your-integration-test-24082451?u=76281980&t=0)** Now that Explore California is in AWS and we have a website URL to test against, and now that we confirmed that that URL works, let's run our integration test. First, let's run that output command again just in case you don't have it. Since this doesn't export any new lines and the result looks a little bit hard to read, let's go ahead and clear our screen. Run that command again, but add an echo to the end of it by adding ":echo", like this. This will make the result a lot cleaner. Now let's run our integration test from earlier to see what happens. As you can see, we get an error. Specifically, we get the error that we configured earlier telling us to set the website URL. So let's use the export command to do that. So I'm going to go ahead and type this command again. And then I'm going to copy this result. Then in this new line here I'm going to add "export website _url" equals that. But before I hit Enter I want to make sure that I have http:// in the beginning of that address. Let's hit enter and let's rerun our tests. Passing test! Our website is alive! And our tests are passing! So there it is! Explore California is up! You did it! Congratulations. You did a great job.
>
> **[1:32](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/running-your-integration-test-24082451?u=76281980&t=92)** So now that everything's working, let's burn it all down! In the next video, we'll learn how to delete resources managed by [Terraform](../../Skills/DevOps/Terraform.md). Onward!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Terraform](../../Skills/DevOps/Terraform.md) (1)
> **CLI Commands:** make (2), aws (1), terraform (1)
> **Env Vars:** url (3), aws (1)
> **Cross-References:** in the next (1)

#### [Destroying the website from AWS S3](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/destroying-the-website-from-aws-s3-24081430?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/destroying-the-website-from-aws-s3-24081430?u=76281980&t=0)** We confirmed that our website is up and that our integration tests are passing against it. With that knowledge, let's save some money and resources and remove everything from S3. Just like every other OpenTofu command that we've used so far, deleting resources is straightforward. All we have to do is run "docker [compose](../../Glossary/Framework/Jetpack%20Compose.md) run --rm opentofu destroy". This is literally the opposite of apply. It queries everything in your state file, compares that against what's in your infrastructure, and deletes what hasn't already been deleted. Just like plan, we're going to be asked if you want to confirm this action. Let's type yes to continue and hit enter. And just like plan, [Terraform](../../Skills/DevOps/Terraform.md) will go through all of the resources and delete everything at lightning speed. If we run our output command from earlier, we can see that OpenTofu has no idea about it. If we ran our integration tests afterwards, they would fail due to website URL not being configured. So there we have it. We took Explore California's website, learn how to run it within a Docker container, wrote unit and integration tests to prove that the website works like it should, and wrote OpenTofu configuration to upload the website into AWS. You should be really proud of yourself. You learned a lot of concepts to get this far, but we're not quite done yet. We're going to learn one more concept that will take what we've learned to the next level. In the next chapter, we're going to learn about the practice of [continuous delivery](../../Skills/Software%20Development/Continuous%20Delivery%20(CD).md). We'll learn about [continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md), CI, and continuous
>
> **[1:34](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/destroying-the-website-from-aws-s3-24081430?u=76281980&t=94)** deployment, CD, and how they differ. We'll then use GitLab CI to create a workflow that bundles everything we've learned from this course together into one cohesive pipeline. Finally, we'll create a GitLab CI runner, in Docker of course, and use it to run our pipeline automatically. See you there!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1), [Terraform](../../Skills/DevOps/Terraform.md) (1), [Continuous delivery](../../Skills/Software%20Development/Continuous%20Delivery%20(CD).md) (1), [Continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md) (1)
> **CLI Commands:** docker (3), rm (1), terraform (1), aws (1), cd (1)
> **Analogies:** just like (3)
> **Env Vars:** url (1), aws (1)
> **Tools:** gitlab (2)
> **Cross-References:** in the next (1)


### 7. CI/CD as Code

[↑ Back to Table of Contents](#table-of-contents)

#### [Shipping fast with CI/CD](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/shipping-fast-with-ci-cd?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/shipping-fast-with-ci-cd?u=76281980&t=0)** Congratulations on getting this far. You're doing great. Explore California is looking really good. We have unit tests that automatically validate, explore California's functionality, OpenTofu configuration that deploys it into AWS S3, and integration tests that wrap everything together by checking that Explore's reachable from the wild, wild internet. However, we've had to run lots of commands to get to this point. It would be a real pain if our fellow engineers had to do this by hand every time they wanted to release new stuff. It would also be a pain for our test engineers, QA, designers and other colleagues to validate that Explore California works as it should, for the major changes to the website, since day two, we need to check these things by hand. This is where CI/CD helps us in a big way. Let's learn more about it. You'll often hear CI and CD discussed in the same sentence. Many times you'll even hear it said as one [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md). Just don't say "CIS-D". I don't think that's cool yet. And that also means something completely different. Jokes aside, it is important to know that CI and CD are actually two separate concepts that are very closely related to each other. CI stands for [continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md). This concept focuses on the practice of constantly bringing changes to our source code together, safely and securely. The words safely and securely are really important here. When you're working with source code that many people are contributing changes to, it's really easy for someone to introduce changes that break everything,
>
> **[1:34](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/shipping-fast-with-ci-cd?u=76281980&t=94)** or worse, allow people to break into everything. This is why us [DevOps](../../Topics/DevOps.md) fanatics love talking about testing, testing, and more testing. Testing goes a long way towards preventing broken builds or insecure software from getting into your projects. CD stands for continuous deployment, not [continuous delivery](../../Skills/Software%20Development/Continuous%20Delivery%20(CD).md) as you might sometimes hear out in the wild, though we will talk about that soon. Continuous deployment is all about shipping software somewhere where people can use it as automatically as possible. For us, we're doing that by using [Terraform](../../Skills/DevOps/Terraform.md) to ship Explore into AWS S3. For some teams, like those deploying software into industrial equipment or [IoT](../../Glossary/Concept/IoT.md) devices, this can get really, really complicated. Difficulty aside, the biggest benefit that CD brings to bear is the comfort in knowing that teams can update their software whenever they want. So if CI/CD isn't one thing, then why are they always discussed together? Well, if you think about it, being able to build, test and validate the security of changes to your software and deploy those changes out to customers is a really awesome way to [react](../../Skills/Web%20Development/React.js.md) to your users' needs really quickly. However, you can't deploy changes quickly without knowing that your software is functional and secure, and being able to vet and secure your software continuously doesn't do you any good if you have to deploy those changes manually. This is why CI and CD are often discussed together. In fact, these concepts, when put together, can also be expressed as continuous delivery. The name makes a lot of sense here.
>
> **[3:08](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/shipping-fast-with-ci-cd?u=76281980&t=188)** Being able to continuously vet, secure, and deploy changes to your software means that you can also continuously deliver new software to users, ideally as quickly as you'd like. As you can probably imagine, automation is a big, big part of what makes CI/CD work. While you can do CI/CD with a few well-designed shell scripts and duct tape, there's lots of great tools that make it really easy to get started with this concept. As we discussed earlier, GitLab has a great tool built in called GitLab CI. In this chapter, we're going to use it to put everything we've done together into a chain of commands or a pipeline that will automatically test and deploy Explore California into S3. Let's get to work.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Continuous delivery](../../Skills/Software%20Development/Continuous%20Delivery%20(CD).md) (2), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [Continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md) (1), [DevOps](../../Topics/DevOps.md) (1), [Terraform](../../Skills/DevOps/Terraform.md) (1)
> **CLI Commands:** cd (9), aws (2), terraform (1), make (1)
> **Definitions:** stands for (2), is a  (2), means that (1)
> **Env Vars:** aws (2), cis (1)
> **Tools:** gitlab (2)
> **Exercise Files:** source code (2)
> **Cross-References:** we discussed (1)
> **Analogies:** imagine (1)

#### [Introducing GitLab CI](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/introducing-gitlab-ci-24075621?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/introducing-gitlab-ci-24075621?u=76281980&t=0)** As we mentioned earlier, we'll be using GitLab CI as our [continuous delivery](../../Skills/Software%20Development/Continuous%20Delivery%20(CD).md) automation platform. Let's learn more about it. GitLab CI is a tool that makes it easy to build CI/CD pipelines that take software like Explore California into production. If you've ever used [GitHub](../../Skills/Software%20Development/GitHub.md) Actions, CircleCI or [Jenkins](../../Skills/DevOps/Jenkins.md), you'll feel right at home with GitLab CI. There are three components that make GitLab CI work: the GitLab CI YAML file, to describe this, your laptop over here; the GitLab server and GitLab CI runners. You can see how they're related to each other with this image. The GitLab CI .YAML file, represented by our laptop here, tells GitLab CI what jobs it needs to run in order to build, test, and deploy your application. Like shown here, each job is expressed with the command or shell script, as we'll see soon. Jobs are then organized into stages, and GitLab CI will execute each stage in the order that it's written within the file. GitLab CI runners are machines that run jobs from GitLab CI YAML files and report the results back to GitLab. What makes a runner a runner is having the GitLab CI runner agent installed. As a result, runners can, well, run on just about anything: [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md), containers, [Kubernetes](../../Skills/DevOps/Kubernetes.md) pods, Raspberry Pis, smart toasters... Whatever you can think of, a GitLab CI runner can probably run in it. Since we're running GitLab entirely in our own machine with containers, we'll be using another container to host our runner. And then finally there's the GitLab server. Well, it's a GitLab server. I don't know what else to say about it.
>
> **[1:34](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/introducing-gitlab-ci-24075621?u=76281980&t=94)** So there you have it. The fundamentals of GitLab CI. Click on the link below to learn more about all of the things you can do with GitLab CI. With that out of the way, let's create our first GitLab CI .YAML file.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Continuous delivery](../../Skills/Software%20Development/Continuous%20Delivery%20(CD).md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1), [Jenkins](../../Skills/DevOps/Jenkins.md) (1), [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) (1), [Kubernetes](../../Skills/DevOps/Kubernetes.md) (1)
> **Tools:** gitlab (21), github (1)
> **Env Vars:** yaml (4)
> **CLI Commands:** cd (1), make (1)
> **Cross-References:** we mentioned (1)
> **UI Navigation:** click on (1)
> **Definitions:** is a  (1)

#### [Writing our first Gitlab CI workflow](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-our-first-gitlab-ci-workflow-24080521?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-our-first-gitlab-ci-workflow-24080521?u=76281980&t=0)** Now that we know more about GitLab CI and how it works, let's create a simple GitLab CI workflow for Explore California. As always, before going any further, make sure that you're in our Explore California website working directory as shown here, or run "cd ~/explore- california-website" to pop right back into it. The first thing that we're going to do is create a file called ".gitlab-ci.yml". We're going to use the touch tool to do that. But you can create a new file in your editor if you'd like. Now that it's created, let's go ahead and open it. This is where we'll define a list of the activities that we want to do to get Explore California tested and deployed into S3. This is going to be a list of stuff, we can call these activities anything we want. I'm going to call them "test", "deploy" and "teardown". Next we're going to add a "variables" section like this. Here we can define environment variables that every job in our CI/CD pipeline will use. Now this gets a little interesting for us. We've been using Docker [compose](../../Glossary/Framework/Jetpack%20Compose.md) throughout building, testing and deploying Explore California. However, GitLab and the GitLab CI runner, which we'll cover shortly, also run in Docker. How will our GitLab containers be able to recreate the containers within Compose that we've been using this whole time? Enter Docker in Docker. Docker in Docker is, well, Docker in Docker. This is a technique that will allow us to create Docker containers from within Docker containers.
>
> **[1:32](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-our-first-gitlab-ci-workflow-24080521?u=76281980&t=92)** There are two variants of this technique. Docker through Docker allows a container to talk to Docker's container runtime on your real machine, and instruct it to create or delete containers. If you've been using [Windows](../../Glossary/Service/Windows.md) or Mac throughout this course, because your instance of Docker runs in a tiny [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) VM, you've actually sort of been using this technique this whole tim. Through Docker in Docker, on the other hand, it creates a Docker container that can run its own container runtime. This is an advanced technique that we won't cover here. However, if you're interested in the ins and outs of that technique, check out my "[Docker Essential Training](Docker%20Essential%20Training.md)" course where I go much deeper into this. Back to our GitLab CI YAML. All of that was a long way of saying that we are going to add a variable here that connects our containerized GitLab CI runner to our container runtime. That variable is called "DOCKER_HOST" and it's going to look something like this. Again, if you're interested in learning why we're using "var/ run/docker" as the value for Docker_Host, check out "Docker Essentials Training." We talk all about it there. Now, you might be tempted to add your AWS credentials to this variable section. I wouldn't do that. This file gets added to your repo in plain text. Doing this would basically give the world access to your AWS account. And that, my friends, would be very, very bad. Fortunately, we can add secret variables through the GitLab UI, and we'll learn how to do that in the next video. Moving on. Let's define our first job under this. We'll call this "unit-test".
>
> **[3:05](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-our-first-gitlab-ci-workflow-24080521?u=76281980&t=185)** Underneath this, we'll type in, with two spaces, "stage: test". This assigns this unit test job to this test stage that's up here. Next we're going to add a script keyword that tells GitLab CI what the stage is going to do. Since we're running unit tests we're going to add the command that we've been using thus far to do this, which is just "docker compose run --rm unit-tests". That's it. After that we're going to add an "after_script" property. This tells GitLab CI that we want to run a script after this job finishes, regardless of whether it succeeds or failed. We're going to have this set to "docker compose stop [Selenium](../../Skills/Software%20Development/Selenium.md) website". This ensures that we have a clean instance of Selenium after every CI/CD run. This command won't always work since Selenium and website might not be running. But the nice thing about after_script is that this script failing won't cause our job to fail if our unit tests were successful. Cool. One down, two to go. So next, we're going to create a stage called "deploy". And we're going to have it be part of the deploy stage. And then we are going to run our OpenTofu command that applies our infrastructure. So that's going to be "docker compose run --rm opentofu apply". And then we're going to add a flag called "auto-approve". This will configure OpenTofu to basically automatically say yes to the plan that gets generated.
>
> **[4:40](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-our-first-gitlab-ci-workflow-24080521?u=76281980&t=280)** And I feel comfortable doing this because we saw what the plan looks like from scratch, and we're destroying everything after our test run. So it's always going to be a blank slate. After that, I'm going to add an after_script, and we're going to make sure that Selenium and the website services are stopped. Excellent. After our after_script, we're going to add a "before_script" because as you saw earlier, OpenTofu needs to initialize before we can do anything with it. So in this before_script we're going to go ahead and run "docker compose run --rm opentofu init". Beautiful. Let's go ahead and copy our deploy step here and paste it underneath. And then let's go ahead and rename "deploy" to "destroy". Let's set the stage for "destroy" and "teardown". And then let's replace "apply" with "destroy". And then after this we'll add one more stage for our integration test. Let's go ahead and call it integration tests. And we want to make sure that our integration tests run after our infrastructure is deployed. Because if they were to run at the same time, then there's a high chance that our integration tests will fail, waiting for our infrastructure to get built out. So to avoid this, let's go ahead and add a "dependencies" key here. And then let's add the deploy stage here as a dependency. This way this will always run after the deploy stage.
>
> **[6:17](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-our-first-gitlab-ci-workflow-24080521?u=76281980&t=377)** Let's go ahead and put the stage in the test stage. And then let's go ahead and add a slightly more complicated script here. Because remember we need to export this website URL environment variable in order for our tests to work. So what we're going to do is basically the same thing we did earlier. We're going to have "export website_url", equals, and double quotes, "http://"... And then as you saw, we can get the value of the URL for our S3 bucket in one line by running "docker compose run --rm opentofu output -raw website_url", and then we close all of that to make it whole. Then underneath that we're going to run our integration test by doing "docker compose run --rm integration tests". And just like our unit tests, we are going to have an after_script here that will ensure that our Selenium and website services are stopped. So let's go ahead and add those. And then after our after_script, we're going to add a before_script that ensures that Selenium and website are started because our unit tests might tear down those services, which would cause our integration tests to fail. So let's go ahead and add that by running "docker compose up -d selenium" and "website". Beautiful. Alright,
>
> **[7:48](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/writing-our-first-gitlab-ci-workflow-24080521?u=76281980&t=468)** let's go ahead and save that. Alright folks, we are done. So let's go ahead and quit our editor and then commit our changes. However, hold off on pushing it because there's some configurations we need to make to our GitLab runner before we can make this work. So I'm going to go ahead and add a simple commit here. Let's say "Added a GitLab CI pipeline", ".gitlab-ci.yml", and we're going to hold it here. So in the next video we'll wire up our pipeline with our super secret AWS credentials. So stay tuned!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (9), [Selenium](../../Skills/Software%20Development/Selenium.md) (7), [Windows](../../Glossary/Service/Windows.md) (1), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1)
> **CLI Commands:** docker (27), make (6), rm (5), cd (3), aws (3)
> **Tools:** gitlab (14)
> **Code Identifiers:** after_script (6), before_script (3), website_url (2)
> **Env Vars:** aws (3), url (2), yaml (1), docker_host (1)
> **Cross-References:** in the next (2), as you saw (2)
> **Definitions:** is a  (1), is an  (1), is called (1)
> **File Paths:** gitlab-ci.yml (2)

#### [Configuring sensitive variables](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-sensitive-variables-24080520?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-sensitive-variables-24080520?u=76281980&t=0)** As we discussed in the last video, there are some variables that are too sensitive to add to our gitlab-ci.yml file that we can safely define through the UI. Let's learn how to do that now. To get started. Open your web browser and visit our GitLab server at gitlab.[example.com](https://example.com). Click on Explore California website. Then hover over to Settings and then click on CI/CD in the submenu. Click on Expand next to variables, and then scroll down. This is where we're going to define our AWS credentials. Let's head back into the terminal to grab them. Let's go ahead and run that "assume" role command from earlier to get a fresh set of credentials that we can use. Nice. So just like before this will give us our access key, secret key and session token in [JSON](../../Skills/Web%20Development/JSON.md) form. Let's define our access key. First copy the access key shown here. Then head back into the browser. Click on Add Variable, next to CI/CD variables. Scroll down to Key and then type in AWS. And as you can see, GitLab was really nice to already give you a couple of defaults. So we're going to select AWS Access Key ID here and then paste in the value. You can also check Mask Variable so that it doesn't show up in job logs when you're looking at previous runs. Let's click on Add Variable here. We're going to do the same thing for the secret key and the session token.
>
> **[1:32](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-sensitive-variables-24080520?u=76281980&t=92)** And GitLab was also very nice by keeping this window open, or this tab open. So all we need to do is go back to our JSON, copy our secret access key. And then go back here, Add Secret Access Key, paste the access key, then Add Variable. Finally, let's get our session token. Over here. Go back into GitLab CI and Add AWS Session Token. Paste it in. Add Variable. Done. Finally we're going to add another variable here called "ACTUAL_PWD". So you've probably seen a couple of references to this environment variable every time we've gone through the docker [compose](../../Glossary/Framework/Jetpack%20Compose.md) manifest. I can finally answer why that is. This variable works around the limitation with the Docker in Docker approach, by forcing Docker Compose to use the directory that our Explore California source code is actually located in, instead of the directory that GitLab CI will create for us when it runs our jobs. So what we're going to do here is we're going to go back into our terminal, type "PWD" to get the full path to our Explore California website. Here. We're going to copy this. Then we're going to go back into the variable UI and then paste it in. Then we're going to click on Add Variable so that we can persist the change. Brilliant! Our next job —no pun intended— is to create a containerized
>
> **[3:07](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/configuring-sensitive-variables-24080520?u=76281980&t=187)** GitLab CI runner that will run our jobs. We will learn how to do that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (2), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (2)
> **CLI Commands:** aws (4), docker (4), cd (2)
> **Tools:** gitlab (8), terminal (2)
> **Env Vars:** aws (4), json (2), actual_pwd (1), pwd (1)
> **UI Navigation:** click on (6), scroll down (2)
> **Cross-References:** we discussed (1), in the last (1), go back to (1), in the next (1)
> **File Paths:** gitlab-ci.yml (1)
> **URLs:** [example.com](https://example.com) (1)

#### [Creating our first Gitlab CI runner](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-our-first-gitlab-ci-runner-24077622?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-our-first-gitlab-ci-runner-24077622?u=76281980&t=0)** It's time to create a containerized GitLab CI runner to run our CI/CD jobs. We will do this in two steps. First, we'll use the GitLab UI to register a new runner that's assigned to our project. Afterwards, we'll hop into our terminal to connect our GitLab runner within [Compose](../../Glossary/Framework/Jetpack%20Compose.md) to GitLab. Let's get to it! First, make sure that you're within the Explore California website project by going to gitlab.[example.com](https://example.com)/ explorecalifornia/website. From here hover over Settings, then click on CI/CD. From here, click the Expand button next to Runners, then click on New Project Runner. Let's configure our runner now. First, leave [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) as the operating system selected. In the Tags section here, you can add tags that allow you to exclude this runner to specific kinds of jobs. This is useful in lots of situations, like having Linux runners that build no jobs, and [Windows](../../Glossary/Service/Windows.md) runners that build .Net or Windows-specific jobs. Since this is the only runner that we'll create for Explore California, we can leave this blank and instead we'll check Run Untagged Jobs so that this runner will respond to any job that GitLab sends it. Next, give your runner a description over here by typing something like "Explore California's default runner." This can be anything you want though. Finally, scroll down and click Create Runner. Now that our runner is registered, GitLab will give us a command to
>
> **[1:35](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-our-first-gitlab-ci-runner-24077622?u=76281980&t=95)** run to finish the registration. The command contains a token that's essentially a password to authenticate the runner to GitLab. So let's go ahead and click on this copy button to copy that, and then head on into our terminal. Let's clear our screen if you have anything on there. Before we paste that command in, let's open the Docker Compose manifest again and let's search for "gitlab-runner" down here. So this is an example of running GitLab runner within Docker Compose. So as you can see it's using GitLab's gitlab-runner image, it's using Alpine as its Linux operating system, and as you can see, this container is using several different volumes and has several different environment variables to configure it. Fortunately, you don't really need to know any of this to continue with our journey here. However, if you're interested in learning more, feel free to click on the link below to go to GitLabs's documentation where they explain how this all works within Docker. Now that we've seen that, let's go ahead and exit. And then let's type in "docker compose run --rm" and let's go ahead and paste in that command from GitLab. Once you've pasted that in, go ahead and hit Enter. You'll first be asked to confirm that the GitLab instance you want to connect to is indeed gitlab.[example.com](https://example.com). Since it is, hit Enter here. Next, it's asking you if you want to name the runner. By default, it uses the system's host name here, which is just the container's ID.
>
> **[3:06](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-our-first-gitlab-ci-runner-24077622?u=76281980&t=186)** If you're okay with that, hit Enter. If you're like me and you want some flair, enter a name like "Explorer California Runner". Then hit Enter. Next, you'll be asked about the kind of executor you want to use. GitLab supports lots of different kinds of runners, as you can see in this list here. Since we're going to be cool and do Docker in Docker, type "docker", then hit Enter to continue. Next, you'll be asked to provide the default image to use for your containerized runner. This is useful if you want a runner that has lots of tools pre-installed to maximize compatibility with lots of different CI/CD pipelines. Since all of our steps run as Compose services, we're going to use a Docker image that will come with the Docker and Docker Compose programs built in. Fortunately, Docker has an official image that provides just that. So let's go ahead and put that in now. I'm going to type "docker: 26.0.0-[CLI](../../Skills/Network%20and%20System%20Administration/CLI.md)-alpine3.19".
>
> **[4:04](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/creating-our-first-gitlab-ci-runner-24077622?u=76281980&t=244)** Now, my memory is horrible. I only remembered this because I tested everything beforehand. If you're curious about how I found this image, you can go into the Docker Hub at hub.[doc.com](https://doc.com) and then you can search for "Docker CLI". That'll bring you to the Docker image down here, which you can click on. And then you can click on Tags here and then filter tags by ones that say "CLI" in it. Now similarly to the OpenTofu situation that we experienced earlier, you might want to increase the version that we're using of the CLI image. You can find all of the list of versions here. So for example, we could use 26.12 or 26.1 or 26. Or we can use the latest by just typing in "CLI". If you don't particularly care, let's go back into our terminal and continue. So that's it. That's all we need to enter. So let's go ahead and hit Enter. And we can see that the runner has been registered. So let's confirm that our runner and GitLab are talking to each other. Head back into your browser. Let's get rid of this, and then click on the Runners page down here. You should see the new runner that you created, and you should see that it has a green light next to it. If it does, you're finally ready to run our CI/CD pipeline, which we'll do in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (6), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (5), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (3), [Windows](../../Glossary/Service/Windows.md) (2)
> **CLI Commands:** docker (15), cd (4), make (1), rm (1), find (1)
> **Tools:** gitlab (17), terminal (3)
> **UI Navigation:** click on (7), scroll down (1), open the (1), go to (1)
> **Env Vars:** cli (4)
> **URLs:** [example.com](https://example.com) (2), [doc.com](https://doc.com) (1)
> **Versions:** 26.0.0 (1), 26.12 (1), 26.1 (1)
> **Prerequisites:** configure (2)

#### [Putting it all together](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/putting-it-all-together-24075619?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/putting-it-all-together-24075619?u=76281980&t=0)** We've finally arrived, everyone, it's time to see our CI/CD pipeline fly. Now remember that commit we made earlier that I said to hold off on pushing? It's time. Push it. Once you're done, head back into the browser and visit gitlab.[example.com](https://example.com)/ explorecalifornia/website, if you're not already there. You should see a teeny tiny progress circle just above the code button. If you do, then congratulations, your CI/CD pipeline is running. Click on it to see it progress. Here's our sample pipeline. This is a really great way of seeing how everything fits together. All of the steps you're seeing here will run sequentially. So the pipeline will start with the unit test that we ran earlier. Then it will deploy our website into AWS. Then our integration tests will run after that. And finally everything will get torn down at the end. Now you might see errors happen in the pipeline the first time you try to run this. Let's take a look at why this might happen. So if I click on the deploy step, we can see that OpenTofu is failing for the same reason that we saw earlier. It's failing because the AWS credentials that it has have expired. To fix it, we're going to do the same thing we did when we added them into the pipeline the first time. We're going to scroll down on the left, click on Settings, then click on CI/CD. Then we're going to click on the Expand button next to Variables. And then we're going to just simply refresh all of the AWS variables that are on our screen. So if you recall from earlier, the way that we did that is by
>
> **[1:36](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/putting-it-all-together-24075619?u=76281980&t=96)** running this really long sts command. If this is new to you, or if you haven't seen this before, check out my "Configuring AWS" video from earlier where we go into how to get here in more detail. So I'm going to click Enter to refresh my credentials. And you should get a [JSON](../../Skills/Web%20Development/JSON.md) blob on your screen like this. So now what I'm going to do is copy each credential, like our access key ID, and simply edit the variables that I'm looking to replace, paste in the replacement and then save our changes. You can hit the Escape key to close out the pane.
>
> **[2:24](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/putting-it-all-together-24075619?u=76281980&t=144)** Now that our AWS credentials have been updated,
>
> **[2:33](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/putting-it-all-together-24075619?u=76281980&t=153)** we can simply go back to the pipeline by clicking the Back button twice and then clicking the Retry button up here. This will retry any of the stages that have failed. As you'll see in a moment, the stages that have passed, like our test stage over here will remain green so they won't run again. Now that we've updated our AWS credentials, we can see that the deploy stage has now passed and our integration tests should pass right after that. Now there are two ways of getting logs from each stage. The first way is by clicking on the job that you're interested in, like our unit test job over here. By doing this, it will show you the logs from that stage on the next page, like we can see on the screen. Another way that we can see the logs from these jobs is from within Docker itself. Let's head over to our terminal to see what I mean. Let's run "docker ps" here. You'll notice that there are runner containers here alongside our GitLab containers. This is Docker in Docker in action. The Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md) program within our GitLab runner uses the Docker host environment variable to tell our instance of Docker to create the same containers that we created by hand earlier. As a result, the containers that our runner created will live alongside our other containers like we're seeing here. What that means for us is that we can simply copy the container ID of the stage that we're interested in, like this, type in "docker logs", paste in that ID and hit Enter. As you can see, doing this now failed. The reason for this is because you're only able to see logs from jobs this way while containers are running.
>
> **[4:06](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/putting-it-all-together-24075619?u=76281980&t=246)** Since the stage finished before we could see its logs, the container we tried to get logs from no longer exists. This technique is helpful if you happen to have access to a GitLab runner like we do now, and are dealing with a stage that stuck somewhere. You can even use "docker exec" against these containers for some just-in-time troubleshooting. So to see this actually in action, let's go back to our pipeline over here and let's rerun the unit test stage here, since this doesn't actually deploy anything. So as we can see it's now queued up to run. If I refresh my page it's running. So now I'm going to quickly go to "docker ps", look for any runner containers over here, clear my screen, type "docker logs", paste in that ID and now we can see logs from that container. So our pipeline is now done and everything is green. We can confirm that everything worked as we expected by doing a quick once over at the logs from each stage. So I'm clicking the deploy logs, we can see our deploy logs over here. Clicking the Back button brings me back. Same with integration tests, we can see that we have our solve period over here, which indicates that our test has passed. And then if I click on the "destroy" step, we can see that everything has been removed. We did it, y'all! Explore California has successfully been [DevOps](../../Topics/DevOps.md)'d! Great work.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1), [DevOps](../../Topics/DevOps.md) (1)
> **CLI Commands:** docker (11), aws (6), cd (3)
> **UI Navigation:** click on (6), scroll down (1), go to (1)
> **Env Vars:** aws (6), json (1)
> **Tools:** gitlab (4), terminal (1)
> **Cross-References:** go back to (2)
> **URLs:** [example.com](https://example.com) (1)
> **Definitions:** is a  (1)

#### [Challenge: Change the website title](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/challenge-change-the-website-title-24077625?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/challenge-change-the-website-title-24077625?u=76281980&t=5)** Alright. Here's a challenge that takes everything that we've learned in this course and applies it all together. This challenge has two parts. In the first part, I would like you to change the title of the website in the index.htm file within the website directory. Your solution must have unit and integration tests accompany it, and both of those tests must pass. In the second part of this challenge, I'd like you to add a test stage called "check" to GitLab CI that uses the "ls" command that we've been using throughout this course to display the contents of your [Git](../../Skills/Software%20Development/Git.md) repository before running unit tests. I would also like you to change the unit test stage to depend on this "check" stage. And finally, you should be able to see the stage when you click on the little progress circle within the GitLab CI project. Good luck! I hope you enjoy this challenge.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (1)
> **CLI Commands:** ls (1), git (1)
> **Tools:** gitlab (2)
> **UI Navigation:** click on (1)

#### [Solution: Change the website title](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/solution-change-the-website-title-24081426?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/solution-change-the-website-title-24081426?u=76281980&t=5)** All right, folks, I hope you had a lot of fun doing that challenge. I sure had a great time putting it together. Let's learn how to solve this challenge. The first part of this challenge, how do you update the title of the Explore California website? It also had you write tests to cover this change, both unit and integration or end-to-end tests. Now we could go directly into the code and update the title of the page. However, it's always great to add a test before you make that change. We call this [Test-Driven Development](../../Skills/Software%20Development/Test-Driven%20Development.md) and here's an example of how you do that. So what we're going to do is we're going to start with the unit test. So let's open up "spec/ unit/page_spec.rb". Once you're in here, scroll down to the bottom and add a new test within the describe block called "It should show the Explore California title". If you named it something different, that is totally fine. The next challenge is how to actually change our test to check the title. Fortunately, we already wrote an example of this in our integration test, so if we open it up, we can see down here that we have the perfect thing that we need to do this. So let's go ahead and copy this and paste it into our unit test. We'll save our changes and then I'll close this pane. So now we need to test the change that we want to make. So you can name your new page title anything you want. I'm just going to go with "Hello, and welcome to Explore California."
>
> **[1:39](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/solution-change-the-website-title-24081426?u=76281980&t=99)** Save that change. And then we're going to go ahead and exit our editor and then run "docker [compose](../../Glossary/Framework/Jetpack%20Compose.md) run --rm unit-tests". Now if you ran this command like this and you got a passing test, that should not have happened! Because remember, you need to build the container image before running these tests, since our Dockerfile is copying our tests into the container's image. So we need to make sure that we run "--build" first. And there we go. The first thing you should have seen if you went this route is a failure saying that we expected to see "Hello, and welcome to Explore California," but we got "Welcome to Explore California" instead. So let's go ahead and change this by going into the website /index.htm file. So here's our title. This is what we want to change. So let's go ahead and change it to "Hello, and welcome to Explore California!" Saving our changes, quitting our editor and then let's go ahead and run our test again. Remember, you need to add "--build" in order for your tests to be copied into the container image. Now we got a separate failure. Why? Because I added an exclamation mark when it shouldn't have been one. So let's go ahead and remove that. I'm going to write "quit", rebuild and rerun my test. And there we go. Now you can see that you have two dots instead of one, that dot's not lonely anymore.
>
> **[3:11](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/solution-change-the-website-title-24081426?u=76281980&t=191)** That means that both of our tests have passed, and now we can move on to updating our integration test. So let's go ahead and do that now. So we're literally going to do the same thing here. And replace this title with "Hello, and welcome to Explore California." Saving our changes here. Now we need to run our integration test. So the way we're going to do that is by first deploying our changes into S3. Because remember the integration test is an end-to-end test. So if I were to run this just now... Oops. If I were to run this without doing anything, I would get this very sad failure or something like it. So let's go ahead and apply our changes. So I'm going to go ahead and use "opentofu apply". And it looks like I need to refresh my account. Maybe you ran into this while you were solving this challenge. So to fix this, all we need to do is run our "sts assume-role" command again. Like this. Once we have this, we can go ahead and export each of these fields to update our credentials. So let's go ahead and do that now.
>
> **[4:40](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/solution-change-the-website-title-24081426?u=76281980&t=280)** Great. Once you do that go ahead and run "opentofu apply" again. And upon doing this, it should show you a plan. If the plan looks good, go ahead and say yes. And this is going to go ahead and deploy our website. There we go. And once we deploy our website, if we want our integration tests again, we'll still get this error. Why? Because remember we need to update the "website_url" environment variable. The "website_url" environment variable is probably still the one that we used during the course, so we need to update this. So how are we going to update this? We're going to use "opentofu output", like this. We can use the same trick that we used when we created the GitLab CI pipeline by just doing an "export website_url=S". And then once we do that, if we run our integration test yet one more time, we get this really gnarly-looking error. Now this can happen if you forget to add the scheme or the https stuff before the website URL. So what we're going to do is run that export command again, add "http" to the beginning of it, like this, and then run our test again.
>
> **[6:13](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/solution-change-the-website-title-24081426?u=76281980&t=373)** Once we do that, we finally get a test that looks kind of like what we expect. So it looks like we changed the title because that's what we got. But our expected value did not change. So we need to update our integration test. So let's go ahead and copy this. Remember, just like our unit tests we need to copy our tests into the container image. So we need to run "build" for this also. So let's go ahead and add that here. And once we do that... Oops, looks like I need to put that in the right place. Let's fix that. There we go. Once we do that, we get a passing integration test. This brings us to the second part of this challenge: updating our GitLab CI pipeline. So, in the challenge... Let's actually open that pipeline first. So, in the challenge we were asked to provide a stage called "check" that the unit test stage depends on that simply lists the contents of our [Git](../../Skills/Software%20Development/Git.md) repository. So there are a couple of things that we were testing here. The first thing is creating that new job. So let's go ahead and create the "check" job and then add it to the test stage. We were also testing dependencies. So if you see down here, in our integration test, we have an example of that. So all we needed to do is copy that, paste it underneath "stage" and then set that to "unit test", because we want this to run before our unit test stage runs. We also want to list the contents of our Git repository. So we can do that pretty easily by running "ls ." in our script command.
>
> **[7:50](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/solution-change-the-website-title-24081426?u=76281980&t=470)** And then that's it. We don't need to include a before_script or after_script because this isn't really doing very much. So now we're going to write our changes and quit our editor. And then finally, in order to make our GitLab CI pipeline go, we need to commit all of these changes. The last thing that we were testing is the Git workflow, the "git status" to check our changes, "git add" to add the new changes that we made, "git commit" to commit those changes, "git pull rebase" to make sure that we're getting all of the changes that our other developers may have made during this time, and "git push". If you skipped "git pull rebase" during this challenge, that's fine too, because we're working by ourselves here. But it's important to note that when you're working in a collaborative environment, running "git pull rebase" is going to become important. So let's go ahead and run "git status" to see what's changed. We have a lot of stuff here. So let's go ahead and add our "spec" folder, our website and gitlab-ci.yml. Then we're going to use "git commit". And your commit message could look something like "Added code to solve this challenge!" And maybe a description saying "We just changed the title," or something like that. You could have also used the "-m" variant to add a single-line version of the commit, like "Added code to solve our challenge." That is totally fine as well. After you make the change, "git pull rebase" to capture changes from upstream, and then run "git push" to push our changes.
>
> **[9:25](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/solution-change-the-website-title-24081426?u=76281980&t=565)** So once we do that, if we go into a browser and go to gitlab.[example.com](https://example.com), click on Website, we click on the progress circle over here which will bring us to our pipeline, and you can see the "check" stage over here. Now to confirm that it's actually listing the contents of this repository, we need to click on it and look at the logs, which you can see here. Now if you got an error message like this within your pipeline, where it's telling you that the provider credentials are incorrect, that just means that the credentials expired within our pipeline. So what you could have done to solve this is simply go into Settings, CI/CD, Variables, Expand and, then, update the AWS Access Key, AWS Secret Access Key, and AWS Session Token. So let's go ahead and do those now. And then once you do that, click on Website to go back to your repository. Click on the X and then Retry. If you refresh the page you should see the failed stages retry. And as you can see here, the logs suggest that we were able to run the "opentofu apply", which means that our credentials have been updated. And so after a few minutes or even sooner, our integration tests should pass. And then after a while, it should destroy all of the stuff that you uploaded to S3. That's the challenge.
>
> **[10:57](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/solution-change-the-website-title-24081426?u=76281980&t=657)** I hope you had a lot of fun solving it. I had a lot of fun creating it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (14), [Test-Driven Development](../../Skills/Software%20Development/Test-Driven%20Development.md) (1), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1)
> **CLI Commands:** git (14), make (6), aws (3), docker (1), rm (1)
> **UI Navigation:** click on (5), scroll down (1), go to (1)
> **Code Identifiers:** website_url (3), page_spec (1), before_script (1), after_script (1)
> **Definitions:** means that (3), we call this (1), is a  (1), is an  (1)
> **Tools:** gitlab (5)
> **Env Vars:** aws (3), url (1)
> **File Paths:** unit/page_spec.rb (1), gitlab-ci.yml (1)


### 8. Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/next-steps-24082454?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/next-steps-24082454?u=76281980&t=0)** So we've taken our awesome Explore California website and turbocharged it with [DevOps](../../Topics/DevOps.md) principles and tooling. We're now tracking changes to Explore California with [Git](../../Skills/Software%20Development/Git.md) so that we can always keep our work safe. We can also now run our website locally with Docker, test it locally with RSpec, Capybara and [Selenium](../../Skills/Software%20Development/Selenium.md), add automated CI/CD with code on GitLab CI and deploy it onto AWS with OpenTofu. We've gone a long way, but what's next? As it happens, quite a lot. While this course gave us the building blocks to maintain and deploy Explore California, there's a lot that we can do to go even further. Here are some next steps that we can pursue in making Explore California the easiest and fastest website to deploy and maintain out there. We saw how creating containers with Docker supercharged Explore California's ability to ship software fast. However, when you have thousands of containers running in production, things can get complex fast. That's where [Kubernetes](../../Skills/DevOps/Kubernetes.md) comes in. Kubernetes is a container orchestrator that specializes in making it easy to run very short-lived containers at scale with minimal headaches. Like Docker [Compose](../../Glossary/Framework/Jetpack%20Compose.md), you create a manifest describing your application and what it needs to run, including other containers and other dependencies like load balancing and sensitive secrets. However, unlike Compose, Kubernetes handles creating, maintaining, updating, scaling, and removing those containers for you no matter where in the world your app is running. Next, you can learn more about container security.
>
> **[1:32](https://www.linkedin.com/learning/devops-foundations-your-first-project-24355651/next-steps-24082454?u=76281980&t=92)** The reason for this is because our containers run as the root user by default. Additionally, the Docker container runtime runs as root as well. This makes it very easy to start containers, but can become very dangerous if left unchecked. If someone got into the container running Explore, it would be very easy for them to do real damage on the system it's running in. Learning more about container security is a great next step in preventing this. From using the "user" keyword within Dockerfiles to exploring rootless container runtimes like Podman, there are lots of things we can do here to prevent container escapes and other common container-related security vulnerabilities. Finally, when we started this course, we mentioned how many developers and systems administrators will not know that things are awry with their platforms until it's too late. Fortunately, there is an entire practice that focuses on this specific problem. Observability is the methodology of analyzing and studying the behaviors of your system to improve its reliability. Like CI/CD, many people use monitoring and observability interchangeably. However, observability goes beyond the logs and metrics that monitoring tools are great at collecting. There are many observability tools available that can help you use this data to automatically respond to alerts and keep your infrastructure reliable, no matter what. I hope you enjoyed our Explore California adventure as much as I did. Happy coding!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kubernetes](../../Skills/DevOps/Kubernetes.md) (3), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (2), [DevOps](../../Topics/DevOps.md) (1), [Git](../../Skills/Software%20Development/Git.md) (1), [Selenium](../../Skills/Software%20Development/Selenium.md) (1)
> **CLI Commands:** docker (4), cd (2), git (1), aws (1)
> **Definitions:** is a  (2), is an  (1)
> **Env Vars:** aws (1)
> **Cross-References:** we mentioned (1)
> **Tools:** gitlab (1)


## Instructor

- [Carlos Nunez](../../Instructors/DevOps/Carlos%20Nunez.md)

## Resources

- Exercise files available

## Skills Covered

- DevOps

## Path Context

### In [Getting Started with DevOps](../../Paths/DevOps/Learning%20Paths/Getting%20Started%20with%20DevOps.md)
← [DevOps Foundations- Monitoring and Observability](DevOps%20Foundations-%20Monitoring%20and%20Observability.md) | **5 of 5**

## Appears In

- [Getting Started with DevOps](../../Paths/DevOps/Learning%20Paths/Getting%20Started%20with%20DevOps.md)

## Related Courses

_Courses sharing skills:_

- [Leveraging Google Cloud Developer and DevOps Tools](../Cloud%20Computing/Leveraging%20Google%20Cloud%20Developer%20and%20DevOps%20Tools.md) — DevOps
- [DevOps Foundations- Monitoring and Observability](DevOps%20Foundations-%20Monitoring%20and%20Observability.md) — DevOps
- [Advanced Data Engineering with Snowflake](../Cloud%20Computing/Advanced%20Data%20Engineering%20with%20Snowflake.md) — DevOps
- [Automated Data Governance in Practice](../Data%20Science/Automated%20Data%20Governance%20in%20Practice.md) — DevOps
- [Putting ITIL® into Practice- DevOps for ITIL® Practitioners](Putting%20ITIL%C2%AE%20into%20Practice-%20DevOps%20for%20ITIL%C2%AE%20Practitioners.md) — DevOps

---

[↑ Back to top](#)