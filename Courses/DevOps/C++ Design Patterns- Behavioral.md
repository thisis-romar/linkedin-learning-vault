---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: c-plus-plus-design-patterns-behavioral
url: "https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral"
duration_minutes: 209
duration: 3h 29m
level: Intermediate
updated: 6/9/2021
learners: 29992
skills:
  - Software Design Patterns
  - C++
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQF-T1GpCdCroQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1622849290891?e=2147483647&amp;v=beta&amp;t=XPPas-HZ6qijtjMWsh3ZmwdAnBOCra5dMGd9zPyoPgw"
linkedin_topic: DevOps
learning_paths:
  - '[Master C++](../../Paths/DevOps/Learning%20Paths/Master%20C%2B%2B.md)'
prev_courses:
  - '[Test-Driven Development in C++](Test-Driven%20Development%20in%20C%2B%2B.md)'
next_courses:
  - '[C++ Design Patterns- Creational](C%2B%2B%20Design%20Patterns-%20Creational.md)'
path_nav: '[{"path":"Master C++","position":4,"total":6,"prev":"Test-Driven Development in C++","next":"C++ Design Patterns- Creational"}]'
path_count: 1
tags:
  - course
  - topic/devops
  - topic/software-development
  - skill/software-design-patterns
  - skill/c
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/DevOps/C%2B%2B%20Design%20Patterns-%20Behavioral.md)

![C++ Design Patterns: Behavioral](https://media.licdn.com/dms/image/v2/C560DAQF-T1GpCdCroQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1622849290891?e=2147483647&amp;v=beta&amp;t=XPPas-HZ6qijtjMWsh3ZmwdAnBOCra5dMGd9zPyoPgw)

# C++ Design Patterns: Behavioral

> Programmers spend much of their time solving problems, yet they may wind up solving the same problems over and over again. In C++, design patterns can help programmers save their valuable time with classes of programming problems that have similar solutions. Once developers learn these patterns, they can write software more efficiently and make more deliberate software designs. In this course, ins

> [LinkedIn Learning](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral) | 3h 29m | Intermediate | 30K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Behavioral patterns improve software design](#behavioral-patterns-improve-software-design)
  - [What you should know](#what-you-should-know)
- [**1. Introduction to Design Patterns**](#1-introduction-to-design-patterns) (1 videos)
  - [What are design patterns?](#what-are-design-patterns)
- [**2. Behavioral Patterns Group 1**](#2-behavioral-patterns-group-1) (15 videos)
  - [The Chain of Responsibility pattern](#the-chain-of-responsibility-pattern)
  - [Implementing the Chain of Responsibility pattern](#implementing-the-chain-of-responsibility-pattern)
  - [Create Chain of Responsibility handlers](#create-chain-of-responsibility-handlers)
  - [The Command pattern](#the-command-pattern)
  - [Implementing the Command pattern](#implementing-the-command-pattern)
  - [Increasing code reuse with commands](#increasing-code-reuse-with-commands)
  - [The Mediator pattern](#the-mediator-pattern)
  - [Implementing the Mediator pattern](#implementing-the-mediator-pattern)
  - [Using the Mediator from inside other classes](#using-the-mediator-from-inside-other-classes)
  - [The Observer pattern](#the-observer-pattern)
  - [Creating publishers](#creating-publishers)
  - [Creating subscribers](#creating-subscribers)
  - [Project: Create a group-chat program](#project-create-a-group-chat-program)
  - [Challenge: Apply the Command pattern](#challenge-apply-the-command-pattern)
  - [Solution: Apply the Command pattern](#solution-apply-the-command-pattern)
- [**3. Behavioral Patterns Group 2: Strategies, States, and Interpretations**](#3-behavioral-patterns-group-2-strategies-states-and-interpretations) (18 videos)
  - [The Interpreter pattern](#the-interpreter-pattern)
  - [Implementing the Interpreter pattern](#implementing-the-interpreter-pattern)
  - [Applying the Interpreter pattern](#applying-the-interpreter-pattern)
  - [The State pattern](#the-state-pattern)
  - [Implementing the State pattern](#implementing-the-state-pattern)
  - [Applying the State pattern](#applying-the-state-pattern)
  - [The Strategy pattern](#the-strategy-pattern)
  - [Creating strategies](#creating-strategies)
  - [Rewriting classes to use strategies](#rewriting-classes-to-use-strategies)
  - [The Template Method pattern](#the-template-method-pattern)
  - [Creating template methods](#creating-template-methods)
  - [Modifying templates using subclasses](#modifying-templates-using-subclasses)
  - [The Visitor pattern](#the-visitor-pattern)
  - [Implementing visitors](#implementing-visitors)
  - [Allowing classes to accept visitors](#allowing-classes-to-accept-visitors)
  - [Project: Create a greeting-card generator program](#project-create-a-greeting-card-generator-program)
  - [Challenge: Modifying the card generator](#challenge-modifying-the-card-generator)
  - [Solution: Modifying the card generator](#solution-modifying-the-card-generator)
- [**4. Behavioral Patterns Group 3**](#4-behavioral-patterns-group-3) (12 videos)
  - [The Iterator pattern](#the-iterator-pattern)
  - [Implementing the Iterator pattern](#implementing-the-iterator-pattern)
  - [Using iterators](#using-iterators)
  - [The Memento pattern](#the-memento-pattern)
  - [Creating a Memento class](#creating-a-memento-class)
  - [Using Memento classes](#using-memento-classes)
  - [The Null Object pattern](#the-null-object-pattern)
  - [Creating Null Object classes](#creating-null-object-classes)
  - [Setting null objects as the default](#setting-null-objects-as-the-default)
  - [Project: Create an instant replay program](#project-create-an-instant-replay-program)
  - [Challenge: Apply the Null Object pattern](#challenge-apply-the-null-object-pattern)
  - [Solution: Apply the Null Object pattern](#solution-apply-the-null-object-pattern)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Behavioral patterns improve software design](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/behavioral-patterns-improve-software-design?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/behavioral-patterns-improve-software-design?u=76281980&t=0)** - [Shaun] It's been said that object oriented programming without a basic knowledge of [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) is like having a fully stocked refrigerator but no idea how to cook. The basic raw materials are there, right? Concepts like classes, interfaces, inheritance, polymorphism, et cetera. But when you try to actually combine them into a useful hole, the result is, more often than not, a mess. In this course, I'll show you how to improve your programming skills by learning 12 incredibly useful behavioral design patterns. As the course progresses, you'll gain an in-depth knowledge of how each of these patterns works. You'll learn the benefits and drawbacks of each one. And finally, you'll get hands-on experience with them through a variety of real world examples. I'm Shaun Wassell. While I've been working with these design patterns for quite a while now, I remember when I first learned them, how much of a struggle it was at first to apply them effectively. And that's why I made this course, to help you learn and master these patterns as quickly as possible so that you can take your programming skills to the next level. And with all that said, let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) (3)
> **Speakers:** - [shaun] (1)

#### [What you should know](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/what-you-should-know?u=76281980&t=0)** - [Instructor] To get the most out of this course there were a few things that it would be helpful for you to know. First of all it would be very helpful to have a basic knowledge of C++ and ideally also a basic knowledge of how [object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) in C++ works. If you haven't already worked with these things I'd highly recommend you take a look at one of the C++ essentials courses in our library first and then come back to this one. Since we won't be covering any of the very basics of either C++plus plus or object-oriented programming in this course. It would also be helpful for you to have some experience with representing object-oriented hierarchies using Unified Modeling Language or UML as it's usually called. UML isn't very complicated to learn and chances are that you'll be fine in this course even without any prior knowledge of it. But if you find yourself needing a brief primer feel free to check out one of our object-oriented programming basics courses in the library. I also highly recommend that you follow along with me as I write code but at the same time have included the start and finish state for all the code that I write in this course in the exercise files for your reference. So, basically this course is intended for C++ developers who want to learn the ins and outs of [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) and how they can help us write better more maintainable code. If that sounds like something you're interested in then you've definitely come to the right place.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) (3), [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) (1)
> **Env Vars:** uml (2)
> **CLI Commands:** find (1)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)


### 1. Introduction to Design Patterns

[↑ Back to Table of Contents](#table-of-contents)

#### [What are design patterns?](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/what-are-design-patterns?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/what-are-design-patterns?u=76281980&t=0)** - [Instructor] So the first question that you probably have, which we've already touched on briefly in the intro for this course, is what exactly are [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) and why should we use them? Well, the answer to this question is that design patterns are solutions to some very common problems that we run into in [object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md). You may have noticed that when using an object-oriented approach to many programming problems, there are certain harmful patterns, or anti-patterns, as they're commonly called, that tend to pop up in our code. These anti-patterns tend to occur fairly regularly and they can get out of hand very quickly and negatively influence the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our code base if we're not careful. So design patterns then are the positive analogs to each of these anti-patterns. Think about it like this. In real life, when you're confronted with some sort of situation, let's say the person in front of you in line at the grocery store drops a $20 bill and neither they nor anyone else around you notices. In situations like these, there are two main ways to respond. One, you let the person know they dropped the money, or two, you wait for them to leave and pocket the money yourself. Now I hope that in that situation your natural instinct would be to do the right thing and let the person in front of you know they dropped the $20 bill. But regardless, in a similar way, with programming problems that we're confronted with, there are a right way and a wrong way to go about solving them, right? The wrong way here would be the anti-pattern that I referred to before, and the right way would be to use the correct design pattern that we'll learn about in this course. Of course, unlike general philosophies
>
> **[1:32](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/what-are-design-patterns?u=76281980&t=92)** for how to interact with other humans, most of us didn't grow up learning about programming design patterns and that's the gap that this course will aim to fill. So then, design patterns are specific organizational strategies we can use in our programs to ensure that our code remains easy to modify and maintain for the foreseeable future. In this course, we're going to learn about 12 so-called behavioral design patterns, that is, design patterns that deal mainly with how the classes we write in our code bases interact with each other. These patterns will help us maximize the effectiveness of the classes we write and avoid polluting our code bases with code that isn't maintainable.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) (7), [Object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Speakers:** - [instructor] (1)


### 2. Behavioral Patterns Group 1

[↑ Back to Table of Contents](#table-of-contents)

#### [The Chain of Responsibility pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-chain-of-responsibility-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-chain-of-responsibility-pattern?u=76281980&t=0)** - OK, let's take a look at our very first pattern, which is The Chain of Responsibility Pattern. The situation where this pattern can help us, will usually look something like this. Let's say that you're building an application. And somewhere in that application, you have several different pieces of criteria that are checked sequentially. So as an example, your application might need to implement different levels of permissions. So it might need to check up a user's authenticated and then check if they're a paid user, and then check if their plan includes the page they're trying to access. And so on. Or as another example, you might need to validate a string that the user has input. If that string is a password, right, you'd need to check if the passwords long enough, if the password includes a number, letter, and symbol, then it would need to check if the password is one that the users used before. So on, so forth, right. Now, in both those cases, the naive approached implementation would be to use a series of nested if statements, to make sure that requests are handled correctly. So you'd start off by checking if the length of a string is less than six, then you'd move on and check to see if the password contains a capital letter, to see if it contains a lowercase letter, see if it contains a symbol, so on and so forth. Now this series of if statements works, but it's definitely not ideal. You see the main problem here is reusability. If you wanted to implement a similar scenario, but with a slightly different series of checks. So for example, if you wanted to validate an email instead of a password, what you'd have to do, is you'd have to simply copy and paste any of the relevant checks into whatever new piece of code you're writing. And copying and pasting, as we all know, is usually not the best sign.
>
> **[1:34](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-chain-of-responsibility-pattern?u=76281980&t=94)** So here's the basic idea, of The Chain of Responsibility Pattern. Instead of having a big cluster of sequential if-then statements that you have to copy bits and pieces have to replicate functionality in different pieces of your application. What if we express this situation as a chain of objects? Right? So each of these objects would only worry about one particular situation. And if the object can handle that situation, for example, if the password isn't long enough, it'll simply handle it accordingly, right? Maybe by displaying an error message or returning an error message, something like that. Otherwise, it will simply pass the situation on to the next item in the chain. We'll see what this looks like, very shortly in code. So first though, let's take a look at what this situation would look like in UML. All right, well, it's generally going to look something like this. First part is that we create a Handler Interface and Handler doesn't actually have to be the name of the Interface, but you know, we're just going to call it that here. And, this Handler Interface is going to have two methods. The first method is going to be for setting the next Handler that the current Handler will pass the situation on to if it can handle it. And the second method, will be a method for actually handling it situation. So once we have that interface, the next thing we're going to do, is we're going to create concrete classes that actually implement this interface. From password examples I gave earlier, we might have a password too short Handler, a password characters Handler, a password already used Handler.
>
> **[3:07](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-chain-of-responsibility-pattern?u=76281980&t=187)** And so on. Each of which is going to check to see if it can handle the given situation. And if not, it'll simply call the Handle method of whatever the next handler in line is. And once we've created that handler interface and the implementations of it, the client code is where we'll actually set up the chain of these handlers, right? We'll create each of them. And then we'll arrange them in a chain by calling Set Next on each of them. And finally, when a situation comes along, we'll simply call the Handle method on the first Handler in the chain. And if that handler doesn't have anything to say about the situation, it'll call Handle on the next Handler in the chain. And so on, until one of the handlers either can handle the situation or until there are no more handlers in the chain. So those are the basics of The Chain of Responsibility Pattern.

> [!info]- Semantic Content
>
> **Analogies:** for example (2)
> **CLI Commands:** make (1)
> **Env Vars:** uml (1)
> **Definitions:** is a  (1)
> **Prerequisites:** set up (1)
> **Speakers:** - ok (1)

#### [Implementing the Chain of Responsibility pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-chain-of-responsibility-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-chain-of-responsibility-pattern?u=76281980&t=0)** - [Instructor] Now that we've learned the basics of the chain of responsibility pattern. Let's take a look at a concrete example of what this pattern might look like in code. So I mentioned previously that the chain of responsibility pattern could be used to implement string validation. And that's the example we're going to be looking at here. More specifically, let's say that a user is trying to create an account for our application and we therefore have three strings we need to validate, right. The first one is the user's name. The second is their email address. And the third is their password. Each of these strings is going to have a different set of criteria, obviously that it's required to meet. So for the email address, for example. We're obviously going to need to make sure that what the user has entered is actually an email address. And for the password we'll need to make sure that it's the right length, that the user hasn't entered it before, et cetera. And these are in addition to any conditions that are shared between each of the strings. In our case here the only thing we'll be checking to make sure that the strings aren't empty, right. So in other words, we'll want to make sure that the user has actually entered a value. So let's take a look at what this might look like in code. And since I'm on a Mac, I'm going to be using X code here. But if you're on [Windows](../../Glossary/Service/Windows.md), feel free to use Visual Studio or whatever your preferred editor is. And if you're on a Mac, the steps for setting up this project are going to be a little different. So the first thing you're going to want to do is go to file new and click on workspace here. We're going to create a new workspace basically for each of the examples we make. I've just found that it's easier to keep everything
>
> **[1:32](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-chain-of-responsibility-pattern?u=76281980&t=92)** organized and separate that way. So, let's just call this one chain of responsibility and we'll click on save. And that should create our new workspace for us. Now if you want to add the exercise files that I've provided for this course to this workspace, the way you're going to do that is you're going to right click over in this pane here and go to add files to chain of responsibility. And then you're going to find wherever you put the exercise files. And that'll basically open it up here. And then you'll just have to drag those into the actual project. Okay. So, we have the workspace I'll set up. The next thing we have to do is actually create a project inside the workspace. That's just the way that X code organizes things. So, let's go again to file new and project. And that's going to bring up this little window here. We're going to go to Mac OS. Click on command line tool, go to next. And we're just going to give it the same name that we gave our workspace. We're just going to call it chain of responsibility. Okay. And for the language we're going to want to select C++. And for the organization identifier just put in whatever you want there. It's not really that important. All right. So once we input those things we're going to click on next and it's going to ask us where we want to actually save this. All right, so for me that's just going to be in here. I'm going to create a new folder
>
> **[3:08](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-chain-of-responsibility-pattern?u=76281980&t=188)** that'll call C++ behavioral. Click on create. And you're going to want to select this chain of responsibility workspace in this ad to drop down and that'll set it for both add to and group. All right. So once you've done that click on create. And that should create your project for us. And you'll see that you now have this main.CPP file which contains some basic starter code. Okay, so the first thing we're going to do is define the actual handler interface, which we're going to call string validator. So we'll say, class string validator. And then we're going to define some public methods here. We're going to define a virtual distractor just because of how we're going to be extending this class. So we'll say virtual string validator like that. We're going to define a virtual method called set next here. That will allow us to set the next handler in the chain. And that's going to return a string validator and I'll show you why that is a little later on. And we're going to call it set next. And they'll say string validator and we're going to pass a pointer to the next validator in the chain. And this is going to be just a pure virtual method right now. And the last thing here is we're going to give it a method called validate. This is going to be the method that we actually call that starts off the validation chain. And for that we're going to say virtual string validate.
>
> **[4:39](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-chain-of-responsibility-pattern?u=76281980&t=279)** And this is going to take the string that we want to validate as an argument. And the idea here is that this method is going to return some sort of error message or a message saying success depending on how the validation went. So now that we've got our string validator which is the main interface for all of our handlers. The next thing we're going to do is define a base validator that contains all the logic that our handlers are going to have in common. So we're going to call this base validator. And we'll say class base validator. Is going to implement our string validator. And we're going to give it a protected property and that property is going to be called next. And that will basically point to the next validator in the chain. And that will be a no pointer to begin with. And after that, it'll have a few public methods. The first one is just going to be a virtual distractor. And this one is just going to delete the next one in the chain. That'll make it a lot easier to clean up our chain at the end of our program. And next up we're going to define the set next method. So we'll say string validator, set next. And again, that's going to take a string validator pointer as an argument. Which we'll call next validator. It's going to be an override. And what it'll do is this is going to set next equal to next validator. And it's going to return the next validator in the chain. Again, I'll show you why this is later on.
>
> **[6:16](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-chain-of-responsibility-pattern?u=76281980&t=376)** Okay, so that's our set next method. After that we're going to define a generic version of our validate method. This is just going to contain the logic that all of our handlers are going to have in common for the validation. Here's what it's going to look like. We're going to say virtual string validate. It's going to take a string as an argument called test string. And what it's going to do, it's going to be an override too, by the way. What it's going to do is it's going to check if there's a next validator in the chain. And if there is what it's going to do is simply return this next validate called on the same string. All right, so this will basically pass the responsibility to the next handler in the chain. And as you'll see our sub-classes of this base validator are going to call this base validate method. Okay. And otherwise, if there isn't a next one, that means we've reached the end of the chain and we should return a message indicating the validation was successful. So we're just going to say return success like that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Windows](../../Glossary/Service/Windows.md) (1)
> **UI Navigation:** click on (6), go to (4)
> **CLI Commands:** make (6), find (1)
> **Exercise Files:** exercise files (2), starter code (1)
> **Tools:** visual studio (1), command line (1)
> **Definitions:** in other words (1), is a  (1)
> **Prerequisites:** required to (1), set up (1)
> **File Paths:** main.cpp (1)

#### [Create Chain of Responsibility handlers](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/create-chain-of-responsibility-handlers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/create-chain-of-responsibility-handlers?u=76281980&t=0)** - [Instructor] All right, so that's our base validator. The next thing we're going to do is start implementing actual handlers, right, concrete handlers that we're going to use. And the first one we're going to create, we're only going to manually create one and then what I'm going to do is actually have you copy and paste the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of them since it takes quite a while to actually write these things out. And the first one we're going to create is a not empty validator. This is basically going to make sure that the string that the user has entered, right, in our scenario is not empty. So we'll call it class NotEmptyValidator, it's going extend our base validator class that we just created. And for the public methods here, we're going to create a constructor, not that we really needed to do that but just for the heck of it, and then after that, we're going to say string validate, and this is where we're going to write the actual validation logic. Okay, so the sole responsibility of our NotEmptyValidator here is to check and see if a string is empty, and if not then it's going to simply pass the responsibility on to whatever the next handler in the chain is. So what we're going to do just for a little bit of visibility, is we're just going to print out something to the console that says, "checking if empty," this will help us see how it's actually working behind the scenes when we run our code. And then we're going to check if the string is empty. So we're going to say, if testString.empty,
>
> **[1:36](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/create-chain-of-responsibility-handlers?u=76281980&t=96)** then what we want to do is return an error string indicating what's wrong with our test string. So we're going to say return, please enter a value, all right? And otherwise, right, if this if-statement doesn't execute, then that means that are not empty validator, doesn't need to worry about this situation anymore, it can simply pass responsibility down the chain. And the way we're going to do that since we've implemented this base validator class is by saying return base validator, and we're going to call our base validators validate method on the testString, which again, we'll take care of passing that on to the next one in the chain for us. Okay, so that's what our validators are all going to look like basically. And again, I mentioned that typing these things out takes quite a long time. There are about three more that I wanted to add. So what I'm going to have you do is just copy and paste those from the exercise files, from the end state of the exercise files, you're going to copy length validator, alright, you're going to put that under NotEmptyValidator that's going to look like this, it basically just make sure that a string is longer than a certain length. After that you're going to copy and paste the rejects validator, what that does is make sure that a string matches a certain regular expression, right? So it makes sure that it's an email or something. So we're going to paste that under the length validator. And when you do that, you're going to have to go up to the top
>
> **[3:12](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/create-chain-of-responsibility-handlers?u=76281980&t=192)** and include rejects, okay, that will give us the functions that we need to use here. There we go. And after that, you're going to copy and paste both the history validator and to the function above it, which is called in array. And what the history validator does is it allows us to specify what the previous entries for that string have been, right? So if we're checking for passwords that would allow us to check if a user has used that password before, and this little inner a helper function that we defined it just checks to see if a given value is in a vector of strings, right? Which we called an array here. Maybe not the best name, but it works. So those are our four validators, right? We have are not empty validator, our length validator, our rejects validator and our history validator. So the way we're going to assemble them is going to be like this. And again, I'm going to have you copy and paste these from the exercise files since it's quite a bit to type out and I'll explain it afterward. So what you're going to do is copy and paste the contents of main inside of the exercise files. There we go. And what's going on here is we're creating a new base validator called email validator, and this is going to serve as sort of the starting validator for our chain. It's not really going to do anything, it's more just for kind of the naming semantics here.
>
> **[4:46](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/create-chain-of-responsibility-handlers?u=76281980&t=286)** So we're creating our email validator chain and what we're doing then is we're setting the next ones in the chain to check, first of all, if it's not empty, and second of all if it matches the email regular expression which you don't have to know by heart, is that thing right here, all right? So once we've created our chain which only uses two of the validators we saw earlier, we're going to check the emails by calling email validator.validate on several different strings, right? So we're calling it an empty string for calling it on a string that is not actually an email address and finally, we're calling it on something that is an email address. Okay so let's run this code and see what happens. And there we go. So we see what's happening is when the input is empty, we see that we get this string printed out, checking if empty, that is from our not empty validator and we see the value that it returns, which says please enter a value, okay? So in other words, it only got to the not empty validator, in the chain and that returned a string, right? So that said, okay, I'll handle that and return something so we didn't need to further in the chain. With the second one here, what we did as we passed in a string that was not actually an email address, and so we see that it checks if empty, right, it's not empty. So it passes that on to the next handler in the chain that says check rejects match. And that one actually catches it this time
>
> **[6:21](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/create-chain-of-responsibility-handlers?u=76281980&t=381)** and return a string that says the value entered does not match the proper format for an email address. And last but not least, we pass in an actual email address, we see that it checks if it's empty, it checks if it matches the regular expression, and last but not least it returns success, since none of the other handlers could handle it, right? So that's how it works. And one last thing that I wanted to point out is the reason up here in our validator that we had set next returning a string validate or pointer is so that we could do this cool sort of chaining thing here when we're creating our chain of responsibility, right? So we can say email validator set next, set next. If we wanted to add more to the chain we could just continue saying set next, set next, et cetera. Okay, so this was how we checked an email, and my challenge for you now, which you can just do on your own time is to incorporate the other two validators that we defined, right, the history validator and the length validator to create a password validator chain, just like our email validator chain that we create here. So again, feel free to do that on your own time but this here is basically demonstrated how we can implement the chain of responsibility pattern in C++.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Definitions:** is a  (1), means that (1), is called (1), is an  (1), in other words (1)
> **Exercise Files:** exercise files (4)
> **CLI Commands:** make (3)
> **Code Identifiers:** teststring (2)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [The Command pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-command-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-command-pattern?u=76281980&t=0)** - [Instructor] Okay, it's time for our second design pattern which is called the command pattern. The command pattern can help us greatly reduce coupling between different classes in our application as well as make certain pieces of functionality much more reusable as we'll see. And it does this by expressing the different events that can occur in our application as their own classes. And we'll look at the details of this shortly. So in order to get a better idea of how the command pattern works, let's take a look at an example where this pattern might be helpful. So in most applications that we write any changes that we want to make usually take place by one piece of code directly calling another piece of code. So as an example let's say that we're building a drawing application of some sort, and that this application has a clear canvas button that deletes all the lines, colors, et cetera, from the screen. Now this clear canvas button would probably perform its action by having a reference to the editor and making a direct call to the editor's editor.clearAll method or whatever we choose to call it. And that might call something like strokes equals empty array, something like that right? However we choose to actually store the actual shapes in our editor. And this way of organizing things works but it's not really the best way of doing things. The main problem with this kind of setup is that it couples pieces of our code together pretty uncomfortably, right? It [Forms](../../Skills/Web%20Development/Forms.md) tight coupling between different pieces of code since our buttons know about our canvas. So what is the command pattern then and how can it help us
>
> **[1:35](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-command-pattern?u=76281980&t=95)** with the problem that I just outlined? Well the basic idea of the pattern is this. Instead of the changes in our programs taking place as concrete method calls, we express each change as its own command object with an execute method and member variables that contain all the parameters for the command. So in the case of our drawing application that we just looked at, we might have a draw a line command, a change color command a delete object command and so on right? So you might be wondering what all that is going to look like in UML. Generally it'll look something like this. We're going to create a command interface with an execute method as I said that all its subclasses have to implement for themselves. And once we've done that for each event that needs to occur in our application, right, for example, a user clicking on the clear all button in the painting application we mentioned earlier. We create a class that implements this command interface. And notice here that the commands subclasses can also contain any parameters that they need as member variables. So for example, a draw line command would probably contain the coordinates that define the starting and ending point of the line. So these classes will also generally contain references to any other objects that they need to execute. And I've called this target here in the UML. In the case of our drawing application this would probably be the drawing editor canvas, whatever you want to call it. So once we gave our command interface and the appropriate implementations, these implementations are then used by other parts of our application instead of directly
>
> **[3:10](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-command-pattern?u=76281980&t=190)** calling methods on some other class. And what this does is it reduces coupling between our classes and it really increases the reusability of the different pieces of functionality in our application. So for example where our clear canvas button would normally have directly called canvas.clearAll, what we would do is we would instead give our button a command member variable that would contain the command that should be executed when the button is clicked. And as I mentioned before the command is what's going to contain the reference to the canvas and what ultimately is going to call canvas.clearAll or whatever our method is. We'll see what this all looks like very shortly when we go through a coding example. And that's the basic structure of the pattern.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Forms](../../Skills/Web%20Development/Forms.md) (1)
> **Code Identifiers:** clearall (3)
> **Analogies:** for example (3)
> **CLI Commands:** make (2)
> **Env Vars:** uml (2)
> **Cross-References:** we mentioned (1)
> **Definitions:** is called (1)
> **Prerequisites:** setup (1)

#### [Implementing the Command pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-command-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-command-pattern?u=76281980&t=0)** - [Instructor] Okay. So now that we've learned the basics of the command pattern, let's take a look at a concrete example of what this pattern might look like in code. The example we're going to be looking at here is the idea that I mentioned previously of using the command pattern to make buttons in an interface reusable. Let's see what that looks like. So, first of all, if you take a look at the starting state of the exercise files which is what we have here, you're going to see that we have a piece of code without the command pattern. More specifically, what we have is, we have this button class here and in order to create different buttons that do different things, we've created subclasses of that button class. Each of which does its own very specific thing and contains all of its own logic. And the problem with this is that if we wanted to duplicate the functionality of one of these buttons, say the triangle button, we'd have to simply copy and paste all of this code into a new subclass. And additionally, the other problem is that each of these buttons contains its own reference to the canvas, which means that we're pretty much limited to using these buttons in this exact situation, right? In an application with a canvas. So let's see how we can prevent these things using the command pattern. The first thing we're going to do is create a basic command interface. So we'll do that up at the top here. We're going to say, "class," "Command," "public." And then we're going to say, "virtual." And we're just going to create a virtual destructor here for the way that we're going to be extending this class. And then we're going to define a virtual method called execute,
>
> **[1:38](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-command-pattern?u=76281980&t=98)** which will actually perform whatever logic we want the command to perform. All right, you'll see what this looks like shortly. So now that we've created our command interface here, the next thing we're going to do is create a different action for each of the actions that our buttons are currently performing, right? Say, adding a shape or clearing the canvas. And here's what those are going to look like. We're going to say, "class" and we're going to define an add shape command class And this is going to implement the command interface we just defined. And what this command is going to do is, it's going to have a string with the name of the shape we want to add. And it's also going to contain a reference to the canvas. So you see that it's going to be our commands instead of our buttons themselves that will contain references to the canvas or whatever other classes are in the environment that these buttons are in. So now that we've defined those member variables, the next thing we're going to do is define some public methods. We're going to define the constructor, add shape command. It's going to take a string reference as an argument and it's going to take a canvas pointer as its other argument. Okay. And then we're just going to say, "shapeName," "shapeName," and "canvas," "canvas." Okay. So next up, we're going to implement the execute method that we defined on our interface and what that's going to look like for our add shape command
>
> **[3:13](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-command-pattern?u=76281980&t=193)** is we're just going to call, "canvas addShape" with the shape name that we passed to the constructor of our add shape command class. Okay. So that's the add shape command to the only other command that we're going to define here is going to be the clear canvas command. All right. So we're going to say, "class ClearCommand," and we'll say, "public Command." it's going to have a pointer to the canvas that it's going to make the changes to. It's going to have a constructor which is just going to take the canvas pointer and set it. So, "canvas," "canvas." And then for the execute method, what we're going to do is simply call, "canvas," "clearAll." Okay. So it's our clear command and add shape command that are actually performing these actions, again, instead of the buttons that we've defined.

> [!info]- Semantic Content
>
> **Code Identifiers:** shapename (2), addshape (1), clearall (1)
> **CLI Commands:** make (2)
> **Exercise Files:** exercise files (1)
> **Definitions:** means that (1)
> **Speakers:** - [instructor] (1)

#### [Increasing code reuse with commands](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/increasing-code-reuse-with-commands?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/increasing-code-reuse-with-commands?u=76281980&t=0)** - So now that we've got the two commands for our application, what we're going to do is delete our button subclasses and convert our button class. Right, our button superclass here, over to use the command interface. All right, so let's delete our add triangle button, add square button, all of these other button subclasses. Okay, and we're going to change our button superclass here a little bit. First of all, we're going to add a command pointer as a member variable to our button. So we're going to say command command. This is going to be the command that the button will actually perform. And then we're going to create a constructor that will actually set this member variable. We're going to say button, command, command, and set that member variable, like this command command. Okay, and then we can remove this virtual destructor and we can also remove the virtual from this click method and implement it, right, since we'll only need one button class from now on. Now, all this click method is going to do is simply call the execute method of our command, and it'll basically let the command take everything from there, right, it'll let it execute all of its logic. And that should be all the modifications we need to make for now. So what we're going to do is we're going to copy and paste the test code over from the end state of the example code, right? It's just a bunch of stuff that we don't really want to have to type out right now. And that's going to look like what we see here.
>
> **[1:37](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/increasing-code-reuse-with-commands?u=76281980&t=97)** Okay, so we're going to copy and paste over the contents of the main function, as well as the vector-to-string helper function that you'll find right above it. That basically just makes it easier to see the results. So once we've done that, if you look at what's going on in the code here, we're basically creating a new canvas, and then we're creating our add triangle, add square button, and clear buttons. And the way we're doing that is those are all instances of the single button class in our application now, and we're simply passing a different command to each of those buttons we're creating, right? So our add triangle button is simply an instance of the button class with an add shape command passed to it in the constructor, right? And likewise for our add square button and clear button. So if we run our application now, we should see that it works and let's see what the output looks like. So we're going to see that as we go through and simulate clicks on our buttons, those actually add shapes to the canvas just like we'd expect. And the beauty of this again, is that our buttons no longer have to know about the canvas, right? If you look up at where we define the buttons you'll see that it's only the commands themselves that have any knowledge of the canvas. And what this means is that we could use our buttons in a wide variety of applications, not just ones with that same canvas class. And we could also reuse the functionality of different commands if we needed to, right? So if we needed to have say a button and also some kind
>
> **[3:12](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/increasing-code-reuse-with-commands?u=76281980&t=192)** of drop down menu item that performed the same command, we could simply create a single command and assign that to both of those interface elements. So that's what the command pattern looks like in C++. Hopefully you see the benefits there.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1), find (1)
> **Analogies:** just like (1)
> **Speakers:** - so (1)

#### [The Mediator pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-mediator-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-mediator-pattern?u=76281980&t=0)** - [Instructor] Okay, let's take a look at our next design pattern, which is called the mediator pattern. The mediator pattern is generally used when we have a number of classes that are all interdependent on one another in complex ways. When this happens, it can very often lead to complex and tightly coupled code, and using the mediator pattern can help us simplify and keep better track of our dependencies, as well as reduce this tight coupling. So let's take a look at an example of this. Let's say that you're creating an onboarding flow for new users of an application. Now, as most good onboarding flows do, yours needs to take into account the user's previous answers and adjust what they see accordingly. So for example, if the user enters that they're currently employed, maybe this is a piece of tax software or something, what you'll need to do then is show them the appropriate interface elements for entering their employment data. And if they entered that they're married, let's say, you'll then need to display the correct elements so that they can enter their spouse's information as well. Now, the difficulty here comes about because you generally end up relying on the elements themselves to handle all these dependencies and corresponding logic. And what this means is that the marital status text box, in this case, would have to directly know about the spouse info form and likewise with the employment information and any other piece of the onboarding flow that relies on some other part of the interface for its behavior. Now, what this leads to is almost always a very complex web of dependencies that all but destroys the reusability of any individual element.
>
> **[1:34](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-mediator-pattern?u=76281980&t=94)** Since ultimately all of the elements become tightly coupled to all the other elements in the interface. In other words, none of the classes that you use to define your interface will be directly reusable without bringing all the other classes along with it. And note that even though I'm using just a user interface as an example here, this is also true for non-visual things. Basically any other group of classes where there's a tight web of dependencies in between them. So that's the problem. Here's how the mediator pattern goes about solving it. What the mediator pattern does is, instead of having all of the classes depend randomly on other classes, as in the onboarding example we just talked about, what we do is we define all of the relationships between these different classes in a single, what we call mediator class. And the idea then is that whenever some event occurs in one of the classes or elements, instead of directly calling one of the other classes and having a reference to that class, it's simply notifies the mediator class which takes care of making any necessary changes. That might sound a little bit confusing. We'll see exactly what this looks like in the UML diagram and in the code. So let's take a look at the UML diagram first. The first thing we generally do is we create a mediator interface, which contains a single method. And this is usually called something like notify or mediate. And this is the method that the different classes that the mediator is mediating between
>
> **[3:07](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-mediator-pattern?u=76281980&t=187)** are going to call in order to let the mediator know that some change has been made or some event has occurred. And what we do once we've created this interface is we usually create a concrete version of the mediator. This will generally take the form of some sort of container or user interface, something like that. And this concrete mediator will usually contain all of the classes that it mediates between as member variables. So if we have a user data form and employment data form and a spouse data form, the container, the mediator for those elements is going to contain an instance of each one of those as a member variable. And again, whatever one of these mediated classes needs to let the media to know something's up, it's just going to call the mediator's mediate method usually passing some sort of arguments. We'll see what this looks like very shortly. And inside that mediate method, the mediator will then use this information to make whatever changes are necessary and manage the relationships between all of the classes that it's mediating. And those are the basics of the mediator pattern.

> [!info]- Semantic Content
>
> **Definitions:** is called (1), is a  (1), in other words (1)
> **Env Vars:** uml (2)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Warnings:** note that (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Implementing the Mediator pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-mediator-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-mediator-pattern?u=76281980&t=0)** - [Announcer] Now that we've learned about the basics of the Mediator pattern, let's take a look at a concrete example of what this pattern might look like in code. The example we're going to be looking at here is the idea that I mentioned previously, using the Mediator pattern to reduce the dependencies between different pieces of something like a user interface. All right, so let's take a look at that. First of all, the starter code for this video, which you can get in the exercise files, if you take a look at that, you'll see that we have this interface element parent class that really just provides some basic functionality for keeping track of whether or not a given element is hidden, okay? And other than that, we have several subclasses of this interface element class, we've got a button element, we've got a text box, we've got a check box, we've got a submit button, we've got a name text box. Now the main problem here, as I mentioned earlier, is that these bottom level interface elements, like this NameTextBox for example, all know about each other which means that they're tightly coupled and therefore, not at all reusable. So in other words, most of these classes, if we wanted to reuse them in a different context with a different set of other classes, what we have to do is simply copy and paste the logic from inside them and create new subclasses with different dependencies, right? Like if we wanted to reuse this NameTextBox in a situation where there is no submitButton, we'd have to actually create a separate subclass for that situation. So let's take a look at how to improve this situation using the Mediator pattern that we talked about. The first thing we're going to do is up here at the top,
>
> **[1:33](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-mediator-pattern?u=76281980&t=93)** we're going to define a Mediator interface, that's just going to look like this, we're going to say class Mediator, public, we're going to have a virtual void method called mediate and it's going to take a string reference as an argument, right, and we'll just call this event, you'll see why in a minute, and this is just going to be a peer virtual method. So that's the Mediator interface, now concrete implementations of this Mediator interface will more often than not, take the form of some sort of container or manager, right? In our case, our concrete implementation here is going to be the user interface that contains all of these different interface elements. So why don't we create that down at the bottom here. We're going to call this class UserInterface and it's going to implement the Mediator interface. And just as a side note here, we are calling this UserInterface, but that is not the interface as in like an object oriented interface, okay? Just wanted to make sure that was clear. So this user interface is going to have private member variables for each of the different elements that are going to be in the user interface. So we're going to have a TextBox, right, we're going to call that nameTextBox. We're going to have a CheckBox, called isMarried checkbox, and note here that this Mediator set up is going to allow us to remove the specific subclasses, right, like nameTextBox and isMarried checkbox,
>
> **[3:10](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-mediator-pattern?u=76281980&t=190)** those kind of things, you'll see exactly how that works in just a minute as well. We're going to have the spouses name text box, so we're going to say TextBox, spousesNameTextbox and we're going to have a ButtonElement called submitButton. Okay, so the next thing we're going to do is define some of the methods for this user interface class. First one's going to be the constructor, called the UserInterface. We're just going to initialize each of these here. We're going to say nameTextBox equals new TextBox and we're going to pass in the name and whether or not this is visible here. So we're going to say TextBox, we're going to call this Name textbox and we're going to pass true for is visible. Next up we have the isMarried checkbox. We're going to say equals new CheckBox. This is going to be the isMarried checkbox, and we're going to pass true for visible as well. Then we're going to have the spouses name TextBox and that's going to be a new TextBox here, which we'll call Spouses name textbox and that is not going to be visible at the beginning, right, because we're not going to want to show that remember until this isMarried checkbox is checked, right, and then we're also going to have the submit button, which is going to be a new ButtonElement called Submit button
>
> **[4:49](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-mediator-pattern?u=76281980&t=289)** and the initial visibility for this one is going to be false, okay? Oops, I misspelled user interface there. And buttonElement here is still just an abstract class, we'll come back to that in a minute and fix that. The other thing we have to do in our user interface is in the destructor, we have to actually delete these elements, so we're going to say userInterface, delete nameTextBox, delete isMarriedCheckBox, delete spousesNameTextBox and delete submitButton. And then we come to the really interesting part of the Mediator pattern, which is the mediate method. This is the method that contains the actual logic on how the different pieces of our interface are going to relate to each other, okay? So basically, what we're going to do is we're going to move all the logic that was contained in the specific subclasses into this mediate method. So we're going to say void mediate and it's going to take a string reference as an argument. It's going to be marked as override. And inside here, why don't we start off by printing a message that says something like Mediating event, just to see what event it is that our Mediator is currently dealing with, all right, that'll be more for our own visibility
>
> **[6:22](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-mediator-pattern?u=76281980&t=382)** then for anything else. And after that, here's where we get to the logic, okay? Basically, what we're going to do is we're going to have each of our elements call our Mediator whenever something happens with a string specifying what exactly it was that occurred, all right? Now, we're just using a string in this case, note that you could incorporate the command pattern into this as well if you wanted to, instead of just using a string. So basically what's going to happen is, for example, when the isMarried checkbox is clicked, our Mediator is going to get called with the event string that's something like isMarriedCheckBox is checked or something like that. We'll implement that in the checkbox and text box and button element classes in just a minute here, but first let's just define the logic. So we're basically just going to be checking here what the event string is. Right, so we're going to say, if the event is that the name text box is empty, which is one of the cases that the name text box itself is currently handling, then what we're going to do is we're going to hide the submit button. So we're going to say, submitButton, setVisibility to false, all right, so that's one situation that our Mediator is going to handle. Then we're going to say else if event equals and we'll have another events that's name text box is not empty.
>
> **[7:57](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-mediator-pattern?u=76281980&t=477)** In that case, we would want to set the visibility of the submit button to true. And so on, so forth for all of the other pieces of logic in our application, which I'm actually just going to copy and paste for the sake of time here from the finished state of the code. So let's just, there we go. All right, so we have the is married checkbox is checked, we're setting the visibility to true, if it's unchecked, we're setting it to false, and so on, so forth. And if the submit button's clicked, we're going to print submitted. We're probably going to want to have one last thing here just in case we get an event that we don't know how to handle, where we'll say something like, Unrecognized event, okay? And we're also going to copy and paste from the finished code just because it's a lot of repetitive typing, methods for getting the different elements so that we can do things like click on them, that kind of thing.

> [!info]- Semantic Content
>
> **Code Identifiers:** ismarried (6), submitbutton (4), nametextbox (4), spousesnametextbox (2), ismarriedcheckbox (2)
> **UI Navigation:** checkbox (10), click on (1)
> **Exercise Files:** starter code (1), exercise files (1)
> **Definitions:** means that (1), in other words (1)
> **Analogies:** for example (2)
> **CLI Commands:** make (1)
> **Cross-References:** we talked about (1)
> **Warnings:** note that (1)

#### [Using the Mediator from inside other classes](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-the-mediator-from-inside-other-classes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-the-mediator-from-inside-other-classes?u=76281980&t=0)** - [Instructor] Okay, so now that we've moved the logic for our different interfaced elements into the mediator, what we can do is we can delete all these specific subclasses, right? So we can delete spouses name text box, name text box, submit button, et cetera, so that we're only left with these button text box and checkbox elements. And the next thing we're going to do is we're going to have all of our interface elements have a reference to the mediator that they're currently contained inside of. All right, so that's going to look like this. We're going to add it to the interface element superclass here. So we're going to say, mediator, mediator. We're going to add that to the constructor, mediator, mediator, mediator, mediator.
>
> **[0:52](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-the-mediator-from-inside-other-classes?u=76281980&t=52)** And the mediator is protected so the subclasses will have access to it. So now we just need to add the mediator to all of the constructors for these other classes as well. I'm just going to copy and paste it to make it easier.
>
> **[1:14](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-the-mediator-from-inside-other-classes?u=76281980&t=74)** Mediator, add the mediator here. And we're going to add the mediator to where we call the interface element constructor as well.
>
> **[1:33](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-the-mediator-from-inside-other-classes?u=76281980&t=93)** And there we go. And now that we've added the mediator to all of these elements, we need to actually have the mediator pass a reference to itself, to the constructors here. So we just need to add this as the final argument to all of these, these, these, these, and these.
>
> **[1:53](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-the-mediator-from-inside-other-classes?u=76281980&t=113)** And then we need to make all of these elements actually call the mediator with a given event string when something happens. So for our button element, for example, what that's going to look like is we're going to implement this click method here. And we're just going to say, when it's clicked we want mediator, mediate and call that with the name of the element plus the string clicked. So if our name is submit button, for example, it's going to call our mediator with the event string submit button clicked. Okay, we need to do the same thing for our text box. Our text box, the only events that that's going to care about is whether or not it's empty. So what we're going to do is inside this change text method, we're going to check to see if the new value is empty. We're going to say ifnewvalue.empty
>
> **[2:56](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-the-mediator-from-inside-other-classes?u=76281980&t=176)** we're going to call the mediator's mediate method with the name plus empty. So, here's married checkbox . Empty for example, otherwise, if it's not empty we're going to call the mediate method with an event string saying name plus not empty. And note that this string thing is just one way of doing it. It's probably not the best way of doing it, you might want to have an E-number or something. If you were going to really do this in a bigger program to avoid typos. But for our case here, it's going to work just fine. Okay, and last but not least we're going to have our checkbox, for which we're basically going to do the same thing as up here. We're just going to copy that if statement. And paste it here, and we're going to say, if is checked. We're going to change this to is checked. Otherwise we're going to change this to is unchecked. Okay, and that should be all we need to do. The last thing that we're going to do here is, let's write the client code for using this mediator. All we're going to have to do for this one, is we're going to say, user interface, UI equals new user interface. And we'll delete that down here at the end. And I accidentally deleted the return zero thing. And then what we're going to do is we're going to print out a description of all the elements.
>
> **[4:28](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-the-mediator-from-inside-other-classes?u=76281980&t=268)** Remember that this interface element thing has a get description method that we can call that simply tells us whether or not any of the elements is visible. That'll just show us the results of the actions we're going to take here. So we're just going to say interface element create an array of interface elements here. And we're going to use the methods that we created earlier to get references to the get name text box the get is married checkbox, get spouse's name text box and the get submit button. Okay. And then we're just going to print out all of these, we're going to say for{auto element in elements} we're going to say, see out element, get description with a new line. Okay. So that'll print out the initial state of all our elements. If we run this now, we'll see that it starts off our name text boxes visible, are is married check boxes visible. And our spouse's name text box and submit button are not visible because of the state of these other two elements. So the next thing we're going to do is we're going to check that is married checkbox. So we're going to say UI get is married checkbox, set is checked to true. And then we're going to print out all our elements again. So we might want to add another new line here
>
> **[6:03](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-the-mediator-from-inside-other-classes?u=76281980&t=363)** just to keep them separate. So if we run this again now, we'll see that after checking the is married checkbox our spouse's name text box is now visible. And if we were to change the text in the name text box, which I'll just leave as something for you to do, and print out all our stuff again, we would see that the submit button would become visible as well. So again, the point of all of this is that the mediator pattern allows us to put all of the dependencies, put all of the logic of how different pieces of something like an interface work together into a single class so that the elements themselves, the classes themselves don't actually have to know about the other ones. So that really goes a long way toward decoupling our code.

> [!info]- Semantic Content
>
> **UI Navigation:** checkbox (7)
> **Analogies:** for example (3)
> **CLI Commands:** make (2)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [The Observer pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-observer-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-observer-pattern?u=76281980&t=0)** - [Instructor] Okay, onto our next design pattern. The pattern we're going to be looking at here is called the observer pattern. Now you may have already heard of or even used this pattern before. It's used to help control the flow of events in our program in such a way that only the parts of our code that care about certain events are notified of their occurrence. So let's look at a situation where the observer pattern could be useful to us. Let's say that we're building a chat application or some other similar program, now obviously, when one user sends a message, only a very small subset of the users of our application should actually receive that message. Now the problem is that without this pattern, right, without the observer pattern, the only options for us to implement this sort of behavior would be: A, for our user objects to keep checking back periodically to see if anything has changed, or B: For the event to notify all of the objects in our program and let them decide whether or not they care about it. Now obviously, neither of these is ideal. So here's the basic idea of the observer pattern. In the observer pattern, we represent our program as a series of different publishers and subscribers, and the way it works is that a subscriber can then subscribe to any publisher, and while the subscriber is subscribed to that publisher, it'll be notified of every event that is published by that publisher, right? So in the case of our group chat application, that would mean that users could subscribe to or unsubscribe from the messages of a given person or group by joining or leaving that group,
>
> **[1:34](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-observer-pattern?u=76281980&t=94)** and while they're a member of that group, they'll see all the messages sent by other group members. So that might sound a little confusing. It's actually much simpler than it first appears. So let's take a look at what the UML looks like. So the first thing we're going to have is a subscriber interface with some sort of method, it's usually called notify, and this method will generally take some sort of information as an argument. In the case of our group messaging app, for example, that might just be a message object containing the text, time and center information for the new message that was just posted in the group, and what we then do is we extend this interface for every class that needs to listen to events from some publisher of the application. Right, so we might have a chat group user and maybe a chat group admin, both of which would receive notifications from that group when they're subscribed to it, and the last thing we do is we create different publisher objects, and we generally won't need to create an interface here, although, there are cases where that might be helpful, and the thing publishers have in common is that they all have a subscribe and unsubscribe method that subscriber objects can call to start or stop listening to events that the publisher publishes, and we'll see what this looks like in more detail in the code.

> [!info]- Semantic Content
>
> **Definitions:** is called (1), is a  (1)
> **Env Vars:** uml (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Creating publishers](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-publishers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-publishers?u=76281980&t=0)** - [Instructor] Okay, now that we've learned the basics of the observer pattern, we're going to take a look at an example of what this pattern is going to look like in code. And for the example we're going to be looking at here, we're going to be creating users and chat groups and allowing users to subscribe and unsubscribe from different chat groups using the observer pattern. So this should all be pretty straightforward, here's what it's going to look like. We're going to start off by defining our subscriber and publisher interfaces. And those are going to look like this. We're going to start off with our publisher, we're going to say class publisher. And our publisher interface is going to contain the two main methods that we talked about previously, which are subscribe and unsubscribe which allows subscribers to control what messages they receive from what publishers. So the first one is going to be a pure virtual subscribe method. So we're going to say virtual void subscribe, and it's going to take a pointer to a subscriber. Now we haven't defined the subscriber interface yet, but we will do that shortly. And, this method of course, is going to add the subscriber to the publishers list of subscribers. And we're also going to have an unsubscribed methods, so we can just copy paste that, like that, and that will unsubscribe this subscriber from his publisher. And we're going to add one last method to this, and that method is going to be called publish,
>
> **[1:34](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-publishers?u=76281980&t=94)** this is just what we're going to be using in our client code to actually tell our publisher to publish some sort of message. So we're going to say virtual avoid publish, and we're going to pass a string reference, that is going to be the message we want to send to all of the subscribers for this publisher. All right, so that's our publisher interface, the next thing we're going to define is the subscriber interface. So let's say class subscriber, and this subscriber interface is going to have two main methods, the first one is going to be a notify method that the publisher can call to tell it that there's been some kind of update, right? Or some new message. And the second one is going to be just a basic method for getting the name of the subscriber. I'll show you why we need to do that shortly. So let's define our virtual void notify method. This method is going to take the publishers name as an argument, and it's also going to take the message as an argument that the publisher is publishing, okay? And that's going to be a pure virtual method. And the second one here, it's going to return a string and it's going to be called getName. Okay, so now that we have our subscriber and publisher interfaces, we now just need to define our chat group class and a chat user class that can subscribe to it.
>
> **[3:09](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-publishers?u=76281980&t=189)** Here's what those two are going to look like. We'll start off with our chat group class. We're going to say class chat group, and that's going to implement our publisher interface. And this is going to have two member variables, one is going to be the name of the group, right? So we'll say groupName, and the other one is going to be a vector containing pointers to all of the subscribers that are currently subscribed to this chat group, all right? So it's going to be a vector of subscribers. We'll call that subscribers. And one thing to note here is that if we were going to make several different kinds of groups in our program, we'd probably just want to have one sort of base group class that contained all of the logic for dealing with subscribers and publishing things, but since we're only creating one chat group class here, we're not going to bother with that extra layer of class hierarchy. All right, so the public methods for this chat group now are going to be the constructor to which we're just going to pass the name, and that's just going to set the groupName to name. Oops, and it looks like we also have to include vector up at the top here. And then we're going to implement the three methods that we define in our publisher interface up here, I'm just going to copy and paste those.
>
> **[4:42](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-publishers?u=76281980&t=282)** The first ones subscribe is going to look like this, what we're going to do is we're just going to say this subscribers.push_back, and we're just going to add that subscriber pointer to our subscribers vector of our chat group, all right? This is how our chat group is keeping track of all of its subscribers. So that's the subscribed method, pretty straightforward. The unsubscribed method is going to be a little trickier, what it's going to look like is this. We're going to use the vectors erase methods, so we're going to save subscribers.erase, and we're basically just going to tell it standard remove_if, and we're going to tell it to remove it if the name of the subscriber that was passed as an argument is equal to the name of the subscriber that we're currently looking at in this vector. Now you may notice that that would remove two subscribers if they both had the same name, right? If their name was both Bob, for example, and that's just something that we're going to have to deal with for now. If we were going to do this normally, we'd probably want to have our subscribers each have a unique ID of some sort and then maybe change this method to get unique ID, something like that. But anyway, we're just going to use the name for now, so we're going to say standard remove_if, and we're going to say subscribers.begin, subscribers.end, and we're going to use the square brackets here and say subscriber,
>
> **[6:18](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-publishers?u=76281980&t=378)** subscriber s and we're going to say return subscriber getName is equal to subscriber getName. Then we just need to add one more argument here, which is subscribers.end. Okay. There we go. All right, and once again, all we're doing here is we're just removing all of these subscribers from our subscribers vector here, whose name matches the subscriber that was passed in through the argument. Oops, and it looks like I put the parentheses in the wrong place. There we go. All right, so that is the unsubscribed method, the last method of our chat group that we're going to implement here is the publish method. Oh, and one more thing is that we need to remove virtual for all of these and add override, forgot about that when I copied and pasted it. Override, override, override. And for publish, all we're going to do is loop through all of our subscribers and call subscriber notify, okay? So we're going to say for subscriber in subscribers, what we're going to do is simply say subscriber, notify, and pass the groupName and the message to that subscriber.

> [!info]- Semantic Content
>
> **Code Identifiers:** getname (3), groupname (3), remove_if (2), push_back (1)
> **CLI Commands:** make (1)
> **Cross-References:** we talked about (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Creating subscribers](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-subscribers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-subscribers?u=76281980&t=0)** - [Instructor] So that is our ChatGroup class. The last class that we have to define here before we can write our Client Code is the ChatUser class. This is going to be pretty straight forward. We're just going to say class ChatUser which implements the subscriber interface. We're going to give it a member variable called username, and then we're going to define its public methods. We're going to give it a constructor that allows us to define this username. So that's going to look like this. Username, username. And after that, we're going to implement the notify and to getName methods. Okay. So we're going to say, first, void notify, and that's going to take the publisher name and the message that's being published. So that here's what that'll look like here. PublisherName and string message. And what that's going to do, for now we'll just have our chat users simply print out the publisherName and the message to show that everything's working. So we're going to say, see out userName, and we'll say received a new message from publisherName.
>
> **[1:29](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-subscribers?u=76281980&t=89)** And then we'll print the message as well as a new line. And that's all we're going to do for notify. Obviously in a more complex application, you'd be doing something a little bit more involved here, but, you know, just to demonstrate this pattern, that's where we're going to stop. And now we just need to define the getName method that we defined in our interface. Oops! And let's mark this as override as well. There we go. All Right. And we're going to say, string getName, and we're just going to simply return the userName. Like that. Okay. So now that we have our ChatGroup and ChatUser classes defined, let's write some client code that demonstrates how we would actually use these things. So we start off by creating our users. We're going to say ChatUser, user1, and we'll say new ChatUser. Give our user a name. We'll call this user Jim. And we'll do the same for a few more ChatUsers. All right. So we'll have user2, user3. We'll call user2, Barb, and we'll call user3 three, Hannah, or something like that. You can pick whatever names you want. That's not important. So now that we have our three users, we're going to create some chat groups. We'll just create two basic chat groups. We'll call them group1, it was a new ChatGroup. Let's say this ChatGroup is the gardening group.
>
> **[3:07](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-subscribers?u=76281980&t=187)** Something like that. Okay. Gardening group and we'll have another group that's something like, we'll call it group2, and we'll make this group the dog-lovers group. Why not? Okay. And now what we need to do, is just have some of these users subscribe to some of these ChatGroups. Okay. So what we're going to do, is we just need to say group1 subscribe and pass user1,, and we'll pass user2 to it as well. Let's say group1 subscribed user2, and we'll do the same thing for group2. We'll say group2 subscribe and we'll have user2 and user3 subscribed to it.
>
> **[4:01](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-subscribers?u=76281980&t=241)** And then we just need to delete all of these things down at the bottom here. Delete all our users. User2 and three, and we'll delete our groups as well.
>
> **[4:21](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-subscribers?u=76281980&t=261)** So if we run our code now... So it looks like I missed something I just need to add equals zero here. I forgot to add that. Hopefully you caught that. Oh, and one last thing I almost forgot, we need to actually publish some messages to our groups. So let's say group1 publish, and we'll say something like, special sale on gardening supplies, right? The usual things you get in chat groups. And we'll say group2 publish, we'll say something like, "It's national dog day, everyone." Perfect. All right. So let's run our code. And we will see that when we run our code, each of our subscribers prints out the messages they received. So Jim and Barb both received messages from the gardening group, and Barb and Hannah received messages from the dog-lovers group. So that is the observer pattern. And the beauty of this again, is that it allows us to really control the communication between different classes in our program.

> [!info]- Semantic Content
>
> **Code Identifiers:** getname (3), publishername (2), username (2)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Project: Create a group-chat program](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-group-chat-program?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-group-chat-program?u=76281980&t=0)** - [Instructor] Okay, so far, we've seen four [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) as well as some individual examples of each one. And what we're going to do now is take a look at an example that combines some of these patterns. Now, we won't be doing all of them here since that would take us a good bit of time to write a program that would be large enough to need it. What we're actually going to do is start off mixing two of the design patterns together, and then we're going to incorporate the third pattern in a challenge and solution video. So the program we're going to be writing here is an extension of the chat group application that we saw in our observer pattern example, and we're going to start off by mixing this example together with the chain of responsibility example that we saw earlier as well. The end result is going to be a chat group program that validates messages and sends those messages to the subscribers of different groups using the chain of responsibility. So here is what this is going to look like. We're going to start off by copying and pasting the code that we wrote in the observer pattern example. All right, so just all the classes, no need for the client code here, and we're going to paste that into our main file in our new project here, so we have our subscriber publisher chat group and chat user, and we're pretty much going to leave this exactly the way we wrote it, no changes here yet. The next thing we're going to do is we're going to copy and paste our code from our chain of responsibility examples. And you can find both of these by the way in the starter code for the exercise files.
>
> **[1:34](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-group-chat-program?u=76281980&t=94)** And for this one, we're just going to copy the length validator the not empty validator, the base validator and the string validator, and we're going to copy those into our new project here, all right. And the first thing we're going to do here is we're going to change the name of string validator to handler, since this interface is going to be doing more than just validating strings, as you'll see, and to change that name, we're just going to do find and replace never going to change StringValidator, we're going to change the name of that to Handler. Make it more generic. So we'll replace all the occurrences of that looks like those are all correct, and we're also going to change the name of BaseValidator to BaseHandler, all right so let's replace that as well, BaseValidator, change that to BaseHandler. All right so we're going to replace that and there we go. And one last name change I'm going to make is we're going to change validate here, the name of this method, we're going to change that to handle. All right, so replace, replace, replace replace, replace, replace, replace and that's it. Okay, so the next thing we're going to do is our handlers handle method, in addition to taking a string is also going to take a chat group. And what this allows us to do is it allow our handlers have access to a chat group, right?
>
> **[3:07](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-group-chat-program?u=76281980&t=187)** So our handlers will be able to do things like post messages in the chat, that kind of thing. You'll see what I mean soon. So we're going to do the same things here, we're going to add ChatGroup, group and PassGroup here,
>
> **[3:30](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-group-chat-program?u=76281980&t=210)** we're going to add ChatGroup group and pass it here. And the same thing in our length validator ChatGroup group, and pass it here. Okay, and once we've done that the next thing we're going to do is add another handler for our chain of responsibility and the job of this handler is going to be to simply publish a message to the given group if the responsibility is passed to it all right? So this is going to be at the end of the chain when we actually assemble our responsibility chain. All right, so we're going to call this class PostmessageHandler, all right and it too is going to extend the BaseHandler, and here's what it's handle method is going to look like. We're going to say handle, ChatGroup, group and string like this, and we'll call our string message here since this is going to be a string that the user wants to post to the group, and what this is going to do now is it's just going to call group publish with that string, and it's going to return a string, right? Since this handle method is still expecting us to return some sort of string, it's going to return the string that says message sent, right? So that's all pretty straight forward. The next thing we have to do now that we have our post message handler defined,
>
> **[5:03](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-group-chat-program?u=76281980&t=303)** is we're going to assemble our handler chain. So the test code for this is going to look very similar to our chat group test codes. So let's just copy and paste some of that over to here. And there are going to be a few differences. The first difference is that instead of just directly calling group publish, we're going to actually construct a chain here, all right so we're going to say, Handler sendMessageChain is what we'll call it, and we'll just create a new BaseHandler which doesn't do anything, this is kind of a nicer way to work with our chain, and then we're going to say, sendMessagChain, and we're going to say setNext, and we're going to set the first handler in the chain to a Not EmptyValidator, and then we're going to set the next one in the chain to a LengthValidator, and we'll just say our messages have to be longer than two characters although generally that's not a restriction that you see in chat apps. And the last thing we're going to do is set the final handler to this post message handler. And remember that if control gets to this post message handler, it will simply post the specified message to the specified group okay? So we have our send message chain, the next thing I'm going to do is copy and paste some of the tests code from the finished state of this video, and we'll just make that a lot easier for us. And the last thing we have to do is delete
>
> **[6:39](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-group-chat-program?u=76281980&t=399)** our send message chain at the end of all of this and we should be good now. So let's run our code and see what happens. And we'll see that as we try and send these four different messages to different groups, we're going to see that some of the handlers are going to print things out, right? So it's checking if it's empty, and what we get back is please enter a value, right? That's when we try and post an empty message, when we try and send a message, that's too short we get the string back saying please enter a value longer than two, and when we send valid messages, we're going to see that it goes all the way through the chain. And then we're going to see that the actual chat users that belong to that group log out that they received the message in question. All right and that is an example of how we could combine the observer pattern, and the chain of responsibility pattern.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) (2)
> **CLI Commands:** make (3), find (2)
> **Code Identifiers:** sendmessagechain (1), sendmessagchain (1), setnext (1)
> **Definitions:** is an  (2)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Apply the Command pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/challenge-apply-the-command-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/challenge-apply-the-command-pattern?u=76281980&t=5)** - [Instructor] Okay, now that we've seen an example of how the observer and chain of responsibility pattern can fit together. It's time for a challenge. Your challenge is going to be to incorporate the command pattern into the code that we wrote previously. So to be more specific, you may have noticed that all our handlers know about the chat group class which creates some unnecessary coupling between those two classes. In order to reduce this coupling, what we can do is we can modify our handlers so that instead of taking a chat group and a string as arguments, they actually take a command as an argument. All right. So we'll be replacing these two things with a single command. And this command then is what will actually contain the reference to the chat group as well as the string that the user wants to post there. So this challenge should take you about 10 to 15 minutes. Once you've finished it, feel free to move on to the next video where I'll show you how I would go about solving this.

> [!info]- Semantic Content
>
> **Speakers:** - [instructor] (1)

#### [Solution: Apply the Command pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-command-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-command-pattern?u=76281980&t=0)** - [Instructor] Okay, hopefully you've given the challenge a try. Now, let's take a look at one possible solution for incorporating the command pattern into this code. So the first thing that I did was to create a message command interface as well as an implementation of that interface for SendMessageCommands. All right, so down here I said class MessageCommand. And just like we created our command interface in the corresponding video, what I did was I just gave this a virtual destructor,
>
> **[0:46](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-command-pattern?u=76281980&t=46)** a virtual execute method, and a virtual getMessage method, okay.
>
> **[1:01](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-command-pattern?u=76281980&t=61)** And once I defined that interface, the next thing that I did was created an implementation of that, right, called SendMessageCommand. This command is meant to represent, of course, when a user wants to send a new message to a given chat group. And the SendMessageCommand, which has to implement the MessageCommand, here we go, public MessageCommand. This is going to have two member variables. The first one is going to be ChatGroup chatGroup and the second one is going to be a string. So the chat group is the group that whoever's trying to send this message is trying to send it to and the message is the actual text that they're trying to post to the group. All right, so now that we have those things, we're going to define a constructor, which is just going to look like this, ChatGroup chatGroup and string message and we're going to say chatGroup chatGroup and message message. All right, and the next thing we need to do is define the getMessage and execute methods in this class. So we're going to start off with the getMessage method. So we're going to say standard string getMessage and simply return the message to member variable. And we're going to say void execute, and for that we're simply going to call chatGroup
>
> **[2:36](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-command-pattern?u=76281980&t=156)** publish message. All right, and both of these are going to be overrides. And that's all we need to do for our SendMessageCommand class. After that, all we had to do was change the chain of responsibility classes to use this MessageCommand interface instead of the ChatGroup and string that they'd been using before. All right, so in our Handler, we changed this from a ChatGroup and string to a MessageCommand, say MessageCommand command and likewise with these other ones as well, change handle to MessageCommand command, pass command through. And we're just going to have to do that for the NotEmpty and the LengthValidators as well,
>
> **[3:38](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-command-pattern?u=76281980&t=218)** pass command, and for the LengthValidator.
>
> **[3:54](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-command-pattern?u=76281980&t=234)** And last, but not least, I forgot about this one, the PostMessageHandler is going to need to change as well. All right, MessageCommand command, okay. And there are a few more changes we're going to have to make. Notably, now we're no longer just dealing with a string as an argument, but we actually have to get the message text from the command. And the way that we're going to do that here is instead of saying teststring.empty, we're going to say command getMessage.empty. And likewise, here we're going to say command getMessage
>
> **[4:35](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-command-pattern?u=76281980&t=275)** and here we're going to say publish command getMessage.
>
> **[4:45](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-command-pattern?u=76281980&t=285)** All right, and then we just need to define some new messages here. And I'm going to copy and paste those again from the final state of this code. All right, so we have our emptyMessage, tooShortMessage and we just need to take those now and pass them to the corresponding sendMessageChain handle method. All right, so instead of passing a group in a string, we're going to pass the emptyMessage command. Instead of a group in a string here, we're going to pass the tooShortMessage command. We're going to pass the sayHelloToGroup1 command here and sayHelloToGroup2 command here. Okay, and then we're going to delete those message pointers as well down here. And let's run our code. Oops, it looks like we missed something. Oh, actually what I did here was not the correct thing, what we want to do instead is simply call execute on this command here, right, so we don't even have to worry about the group or the actual message at all in our PostMessageHandler. We're just going to say command execute, just like that, and return "Message Sent!" Okay, so now let's run our code, see if it works.
>
> **[6:05](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-command-pattern?u=76281980&t=365)** And sure enough, we're going to see that we get the same output as before except now we're using the command pattern instead of all our validators and handlers having to know about the different classes outside of their context. And that's about it. Just keep in mind that this is only one possible way of doing things so don't worry too much if your solution looked a little different than mine.

> [!info]- Semantic Content
>
> **Code Identifiers:** getmessage (7), chatgroup (5), emptymessage (2), tooshortmessage (2), sendmessagechain (1)
> **Analogies:** just like (2)
> **CLI Commands:** make (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 3. Behavioral Patterns Group 2: Strategies, States, and Interpretations

[↑ Back to Table of Contents](#table-of-contents)

#### [The Interpreter pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-interpreter-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-interpreter-pattern?u=76281980&t=0)** - [Tutor] Okay, onto our next design pattern which is the Interpreter Pattern. Now the Interpreter Pattern is generally used in situations where either because it makes our job easier, or because it's a necessity. We need the software, we're writing to be able to understand a language that it wouldn't normally have the capability of understanding. Okay, so you might have a hard time coming up with examples of this, but there are actually a lot of situations where this pattern can be applied. A few examples of this might include if our program needs to be able to understand and execute mathematical expressions, input as strings by the user, right? So if we're writing a calculator program for example. It might also be used if our program needs to have some kind of capacity to understand human languages such as English or Spanish. And this might be the case if we wanted to implement some sort of text-based interface for the user to interact with our program. Right, so the idea there would be that they could type in something like "delete all unverified users" and our program would be able to understand that. And just one last example of this would be if we needed to decode some kind of system such as roman numerals and parse those into machine readable numbers. So in all those situations that we just talked about, we can generally use the Interpreter Pattern to define what's referred to as a grammar that our program can then use to transform things that it doesn't understand such as basic English expressions or roman numerals into things that it does understand. Now you're probably wondering what on earth this pattern could look like.
>
> **[1:32](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-interpreter-pattern?u=76281980&t=92)** Well, it's actually deceptively simple. We generally have an Expression Interface which is then implemented by two different types of Expressions, Terminal and Non-Terminal Expressions. And the way this works is that the logic inside these expression implementations, defines what's called a grammar for the language we're defining, and what it does is it recursively breaks down whatever expression we're trying to parse, until it reaches terminal expressions in every part. And then what it does is it uses the non-terminal expressions to combine these terminal expressions until it reaches a final something, right? Whatever it is that we're trying to turn the language that we give it into. So from the example that we see here, we might have a mathematical equation represented as a string, and we break it down into a series of expressions where the numbers are the terminal expressions and the operators are the non-terminal expressions. Okay, and then what our expressions contain logic for doing, is using those non-terminal expressions for combining the actual terminal expressions until we get our final result. Now this might seem pretty complex and it definitely can be depending on the language that we're trying to define, but with things like mathematical expressions the rules are generally pretty straightforward. And we'll see how this works in code as well.

> [!info]- Semantic Content
>
> **Tools:** terminal (9)
> **Analogies:** such as (3), for example (1)
> **Cross-References:** coming up (1)
> **Speakers:** - [tutor] (1)

#### [Implementing the Interpreter pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-interpreter-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-interpreter-pattern?u=76281980&t=0)** - [Instructor] Okay. So we've learned the basics now of the Interpreter pattern. So we're going to look at a concrete example of what this is going to look like in our code. And for the example we're going to be looking at here, we're going to be creating a very simple program that will use the Interpreter pattern to express simple mathematical equations. And don't worry; this is going to be pretty painless. I promise. So the first thing we're going to do here is create our expression interface. This will be what we use to define the grammar that I talked about earlier. Okay, so we're going to create a very simple interface here called Expression. And it's going to have a single method, which is going to be a pure virtual method called Evaluate. And we're returning an integer here because the end value of any mathematical expression is going to be a number, right? And we could have this be float or double as well, but just for simplicity, I'm using int. So now that we have our expression interface, here's what we're going to do: We're going to define some subclasses that will allow us to define and evaluate mathematical expressions. So that's going to look something like this. We're going to say class and we're going to create an implementation of this expression interface called OperationExpression. And we're going to say public Expression, and we're going to give this class three member variables. The first one is going to be a string. That's going to be the symbol of the operator expressed as a string, right? So that'll be a plus sign, a minus sign, multiplication sign, et cetera. We're going to say operatorSymbol.
>
> **[1:35](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-interpreter-pattern?u=76281980&t=95)** And then we're going to have pointers to two different expressions. The first one is going to be called the leftHandSide, and the second one is going to be called the rightHandSide. And the reason we've chose those names, this operation expression is going to represent an expression like this, right? Five plus 10, where the left-hand side is this, and the right-hand side is this. Now, in mathematical expressions, too, the right-hand or left-hand side might itself be another expression, right? So you could have a plus expression that adds together five and another operation expression. Okay. So that's what left-hand side and right-hand side are. You'll see how we use all of this very shortly. So now we have to implement the constructor, which is going to look like this. We're just going to have OperationExpression. That's going to take a string as an argument, and two expressions as arguments, which we'll have his left-hand side and right-hand side. And we're going to say operatorSymbol, operatorSymbol, leftHandSide, left-hand side, and rightHandSide, right-hand side. All right.
>
> **[3:07](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-interpreter-pattern?u=76281980&t=187)** There's our constructor. Oops. Mistyped that. All right. The only other thing we have to implement now is the evaluate method of our operation expression. And that's going to look like this. We're going to say int evaluate. It's going to be an override. And what we're basically going to do here is we're going to check to see what operation this operator symbol is. All right, so we're going to say something like if operatorSymbol is equal to a plus sign string, in that case what we're going to do is we're going to return the left-hand side evaluated plus the right-hand side evaluated. This might seem a little silly right now, but I'll show you how this actually works when we get to the client code. And then we're going to say else if operatorSymbol is equal to the minus sign, what we're going to do is say return leftHandSide, evaluate minus rightHandSide, evaluate. And if neither of those is the case, what we're going to do is we're just going to say C out, we're going to print something like Unrecognized operator. We'll print out the operatorSymbol just so we know what it is. And then we'll say return zero, or return negative one, or return negative 99;
>
> **[4:40](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-interpreter-pattern?u=76281980&t=280)** whatever you want to do there. All right, so that is our operation expression. Now we could add as many of these kinds of things as we want, right? If we wanted to come up with our own operator symbols and have them do weird things in here, we could definitely do that. But for now, we'll just leave it at that. So that's our operation expression. The other thing that we're going to define here is our number expression. So we're going to say class NumberExpression. The number expression will be what's called a terminal expression, whereas the operation expression will be what's called a non-terminal expression, right? Because it continues evaluating down the sort of grammar tree, if you will. All right, so our number expression is going to look like this. It's going to implement our expression interface and it's going to have a single-member variable called numberString. This will just be the number that it represents, represented as a string. We're going to have a constructor that allows us to pass that in. numberString, and then we'll have numberString, numberString. Okay. And then the evaluate method of this is going to be very simple. All we're going to do is say return, string to integer, numberString. Okay, so it's going to return the numerical value of whatever that number string is. Oops, and I misspelled evaluate here.
>
> **[6:15](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-interpreter-pattern?u=76281980&t=375)** There we go.

> [!info]- Semantic Content
>
> **Code Identifiers:** operatorsymbol (6), numberstring (5), lefthandside (3), righthandside (3)
> **Tools:** terminal (2)
> **Speakers:** - [instructor] (1)

#### [Applying the Interpreter pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/applying-the-interpreter-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/applying-the-interpreter-pattern?u=76281980&t=0)** - OK, so you might be wondering now, how do we actually use this Operation Expression and Number Expression classes? Well, here's what that's going to look like in code. We're going to start off by defining some Number Expressions, right? So we might say something like Number Expression five equals new Number Expression. Like that. Number Expression, we'll do something like, seven equals new Number Expression seven. And then we'll have an Operation Expression, which we'll call five plus seven, which will be an Operation Expression we'll pass in. Plus as the Operator String. And then for the other two arguments, the left hand side and the right-hand side, we'll pass in the two Number Expressions that we have here. All right, so we'll have five and seven as the other two arguments. So now what we can do, is we can print out the result of evaluating this Operation Expression by just saying, C out. And then five plus seven is and then we're going to say five plus seven evaluate. And we'll add a new line on there as well. And then just for cleanup, we'll just delete those things.
>
> **[1:39](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/applying-the-interpreter-pattern?u=76281980&t=99)** Perfect. So if we run our code now, we're going to see that a prints out five plus seven is 12. OK. So if we wanted to make this a little more complicated, we could in fact have, let's define ourselves another Number Expression. All right, we'll say something like 13 equals new Number Expression 13. And then we can define another more complex Operation Expression, that will combine say a Number Expression with an Operation Expression, let's say, right. So we could say, we'll just call this one complexOp or something like that. And we'll say new Operation Expression. And for this one, we'll say minus and for the left hand side, we'll have our 13 Number Expression here. So we'll say 13. And for the right hand side, we'll have our other Operation Expression here. Five plus seven. Right, our Operation Expression doesn't care whether the left or right-hand side are Operation Expressions or Number Expressions. It doesn't really distinguish between the two of them, which is the nice part about this pattern. So now, if we say something like, I'll just write it out using numbers, we'll say 13 minus five plus seven equals and then we'll do complexOp, evaluate.
>
> **[3:11](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/applying-the-interpreter-pattern?u=76281980&t=191)** And if we run our code again, we're going to see that we get the final answer. Now you might be wondering, how this sort of thing that we just did here might be useful, after all you might be thinking, we could have just executed this expression here directly in the code and that would have worked perfectly well for us. Well, first of all, we wouldn't normally write out all these expressions manually, like we did here. OK. For that, we usually have some sort of parser that takes a string and creates these expressions automatically. Right, so what you would do, is you would literally pass in this as a string or you could pass in something like this as a string, if you had different expression logic and it would actually parse that into these expressions, and then it would call evaluate on the top level expression. Hopefully that makes sense. So, the other nice part about this is that it gives us a good deal of flexibility, since we're able to define every aspect of our grammar now. So if we wanted to make this more of a verbal math, type grammar, for example, what we could do is we could change the plus and minus signs here, to plus and minus. And we could actually go down here and change this to plus and minus. And if we ran this code again, it would give us the exact same result, right? So this would be a good starting point for parsing more verbal math like this. Obviously, then we would have to go into the Number Expression
>
> **[4:43](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/applying-the-interpreter-pattern?u=76281980&t=283)** and have it be able to actually parse numbers in written form. But I'll leave that to you as an exercise. Anyway, hopefully this has been a helpful introduction to the Interpreter pattern. This one is a bit trickier than the other patterns we've seen before, but it definitely has some very interesting possibilities.

> [!info]- Semantic Content
>
> **CLI Commands:** make (2)
> **Code Identifiers:** complexop (2)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - ok (1)

#### [The State pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-state-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-state-pattern?u=76281980&t=0)** - [Instructor] Okay, the next pattern we're going to look at is called the state pattern. This pattern can be used when we need a readable and reusable way to change the behavior of an object that can be in different states and needs to behave differently depending on which state it's in. As an example of this, let's imagine that you're creating a program for tracking and providing information about online orders on an eCommerce site. Now the thing is that the information we display about each order is going to depend on the state that the order is in. For example, is the order in transit? Has it been delivered yet? Has a return been requested on the order and so on so forth? So each of these situations constitutes a different possible state that a given online order can be in and the behavior of the order will therefore change depending on which of these states the order is currently in. Now if we were to naively implement this situation, it would probably end up looking something like this. Right, our order class would have a state member variable, most likely a string or an enum, and everywhere that some sort of behavior is expected, we'd have a big if statement or switch statement, which would look at the state member variable and change the behavior depending on what the value was, and this kind of thing works, but it's definitely not ideal. In general, it's very hard to reuse logic in this kind of situation without simply copying and pasting the code, which as I've said before, is generally not a good sign. So here's how the state pattern
>
> **[1:33](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-state-pattern?u=76281980&t=93)** can help us in this kind of situation. As an alternative to this gigantic if then statement in all of our functions, what we do is we actually define the specific behavior for each of the different states inside its own class, and each of these state classes then takes care of executing the state dependent logic. So a UML diagram for this situation would look something like this. We would simply define a state interface and this would have a number of different methods. The exact number of methods will depend on your use case, but generally, the state interface should only contain methods that are common to all of the states, and once we have that state interface, we then represent each possible state as its own subclass of that and define the desired behavior for that state inside that subclass. And lastly, once we've split out our different state behavior into a variety of classes, the class that needs to actually behave differently for those states in the case that we were talking about beforehand, the order class, is then going to contain an instance of one of the implementations of this state interface as a member variable, and it's going to call its methods whenever it needs to do something without actually worrying about the state that it's in. Right, our order is simply going to say current state, do something or current state, do something else, and that's going to call the corresponding method on whatever the current state of the order is.

> [!info]- Semantic Content
>
> **Analogies:** imagine (1), for example (1)
> **Code Identifiers:** ecommerce (1)
> **Env Vars:** uml (1)
> **Definitions:** is called (1)
> **Speakers:** - [instructor] (1)

#### [Implementing the State pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-state-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-state-pattern?u=76281980&t=0)** - Okay, so now that we've learned the basics of the state pattern, let's take a look at a concrete example of what this pattern might look like in code. For the example we're going to be looking at here, we're going to be using the situation we discussed earlier about allowing [products](../../Skills/Software%20Development/Microsoft%20Products.md) on an [E-Commerce](../../Skills/Web%20Development/E-Commerce.md) site to be in several different states and behave differently accordingly. So if we take a look at our starter code here, this is from the exercise files. This is what code without the state pattern usually ends up looking like. As you can see, we have this purchase class with a state member variable, which is just a string. And then based off the value of that variable, it'll behave differently whenever we want to do anything in the class, right? Whenever we want to do something like print out a description or go to the next state or if we had more methods, we would see this exact same pattern repeated over and over again for each of them. So let's see what this would look like if we were to use the state pattern instead to really handle the differences in logic, depending on state. What we're going to do is we're going to start off by defining a state interface. And that's going to look like this. We're going to say class state And it's going to have two methods. The first one is going to be a virtual string method called get description. Generally our state interfaces are going to have the same methods as the class that's going to be using them. Our purchase class has a get description method and a go to next state method. Our purchase class has a get description and a go
>
> **[1:34](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-state-pattern?u=76281980&t=94)** to next state method. So our state interface here is going to have a get description method, and it'll have something slightly similar to the go to next state method. It will have simply a get next state method. You'll see how we use that later on. And both of those are pure virtual methods. Okay, and now that we have that state interface, the next thing we're going to do is define implementations of this interface for each of the different states that a given purchase can be in. Our purchases here have three different states: purchased, in transit, and delivered. We're going to create a new implementation of this state interface for each of those. So we're going to have class, purchased state. Public state. We're going to have an in-transit state. And we're going to have a delivered state. And each of these states is going to have a pointer to whatever the next state is. And then we're going to define the constructor here, which is going to allow us to define the next state. So we'll say next state, next state. And next, we're going to have our get description method, which is basically just going to return the string
>
> **[3:11](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-state-pattern?u=76281980&t=191)** down here from this if statement. All right, so what we're going to do is we're just going to copy this, put it in here and say return, will be shipping soon. And we can add something else to there too. We'll say something like current state: purchased.
>
> **[3:36](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-state-pattern?u=76281980&t=216)** And now that we've defined our get description method, the next thing we're going to define is just the get next state method. We just simply going to return the next state pointer that we have here. Just going to say return next state. Okay. And for our other two classes here, our in transit state and delivered state, it's going to be pretty much the same thing. I'm going to copy and paste all of this with some slight differences. And you're going to notice that this next state and get next state thing are going to be the same in all of these. So what you might want to do, I'm not going to do it here, but what you might want to do is move that up into this state interface, so it would make it more of an abstract class that contains the common functionality between all these states. But for now, we'll just leave it like it is. So we're going to just change this now to in transit state, change this to delivered state, and we're going to change these things too. So we'll say in transit and copy the string from down here. All right, and for delivered. There we go. We'll paste that there and change this to delivered.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1), [E-Commerce](../../Skills/Web%20Development/E-Commerce.md) (1)
> **UI Navigation:** go to (3)
> **Exercise Files:** starter code (1), exercise files (1)
> **CLI Commands:** make (1)
> **Cross-References:** we discussed (1)
> **Analogies:** similar to (1)
> **Speakers:** - okay (1)

#### [Applying the State pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/applying-the-state-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/applying-the-state-pattern?u=76281980&t=0)** - [Narrator] Okay, so we have our three different states. The last thing we need to do now is modify our purchase class so that it defers all of its state dependent logic to one of these three implementations. All right, so here's what that's going to look like. We're first going to change this from a string to a pointer, to a state. All right, so we're going to say state, current state. For the starting state, we're going to add another argument here. So we'll say state, initial state and we'll say current state, initial state. And then what we're going to do is inside our purchases get description method, for simply going to return the result of calling get description on whatever our current state is, right? Our purchase doesn't actually know what its current state is and it also doesn't care. All it cares about is deferring this logic to whatever the current state is. And the same thing here for go to next state. What we're going to do is we're simply going to delete all these things. We're going to say current state equals current state get next state. And we only want to do that if the current state has a next state. So we're probably going to want to add that onto all of these here. Just initialize those as null pointers, forgot to do that at the beginning there. Null pointer, all right.
>
> **[1:35](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/applying-the-state-pattern?u=76281980&t=95)** So we're going to check if the current state has an next state, if it does, we're going to go to it. Otherwise, we're going to log out something like, no more states. Okay, and that's all we need to do for our purchase and the different state implementations. Let's take a look now at what the client code is going to look like. Really, it's going to be fairly simple. We're just going to create all of the different states. We're going to start off in reverse order. So we're going to say delivered state, delivered state equals new delivered state. And we'll just pass in a null pointer for that. Then we're going to have an in-transit state which is going to look like this. We're going to say in transit state, in transit state equals new in transit state. And the next state for that is going to be the delivered state. There we go, and then we're going to have our purchased state. So purchase state, purchase state equals new purchase state. And the next state for that is going to be the in-transit state. And this is just one way of doing it. Obviously there's probably easier ways to set this all up but just to make it explicit what exactly it is we're doing. So now, now that we have those three states,
>
> **[3:10](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/applying-the-state-pattern?u=76281980&t=190)** all we're going to do is we're going to create a new purchase and we're going to pass in the initial state for that purchase. So we're going to say purchase equals new purchase. We'll call that shoes maybe, just it needs a product name argument there. And for the initial state, we're going to pass in the purchase state. So our purchase is going to start off in the purchased state and then what we're going to do, and you can just copy and paste this from the finished exercise files, just to test this off, we're going to go to the next state. We're going to print the description, we're going to go to the next state. We're going to print the description, and it looks like I missed one other thing. We just want to print out the initial description, just so we can see how our purchase behaves in all of its different states. And we'll also want to delete all the pointers we created here. Delete our delivered state, delete our in transit state. Delete our purchased state and delete our purchase. Okay, so let's run our program and see what happens. All right, so we see that the three different things that we're printing, even though we're calling purchase get description on all of them, it behaves differently depending on the state that are purchase currently in, right. And instead of polluting our purchase class with all of the different branching state logic, what we're doing here is we're deferring that actual logic
>
> **[4:44](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/applying-the-state-pattern?u=76281980&t=284)** to each of the different state implementations that we have here.

> [!info]- Semantic Content
>
> **UI Navigation:** go to (4)
> **CLI Commands:** make (1)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [narrator] (1)

#### [The Strategy pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-strategy-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-strategy-pattern?u=76281980&t=0)** - [Instructor] Okay, the next pattern we're going to look at here is called the Strategy Pattern. The Strategy Pattern is used to simplify our code in situations where there are several or many possible ways of performing some calculation or producing some result. So an example of this might be if we were writing a program that had to sort a lot of different things. Now, as many of you know, while some sorting [Algorithms](../../Skills/Software%20Development/Algorithms.md) are generally considered more efficient than others, which one computes the result the fastest can often vary depending on certain situations. So let's say that we wanted to take that into account and have our program use a different sorting algorithm depending on certain criteria. For example, if our array is quite small, we'd probably want to use something like insertion sort, for other situations we might want to use quick sort or radix sort and so on. Now, even though we want to be able to use different sorting algorithms in different circumstances, we definitely don't want the implementation logic for all of these algorithms to be contained in one single place in our code wrapped in if statements that decide which one to use, right? As with most of the other examples that I've given so far in this course, this would not only lead to a mess, but would likely make our code very difficult to reuse without copying and pasting. So here's the basic idea of the Strategy Pattern. Instead of writing all the different ways of doing things in the same place with if statements, we break each different way of doing something out into a class of its own, right? We call these strategies. In more concrete terms, what we're going to do
>
> **[1:33](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-strategy-pattern?u=76281980&t=93)** is we're going to define some sort of strategy interface with a method for actually executing that strategy. So in the case of the sorting example that I gave before, we would have some sort of sorting strategy interface with a sort method. And what we would do then is we would have all of the different strategies for sorting separated out into their own implementations of this interface, right? Separated out into their own classes. And once we have our sorting strategies, we'll then have the client code represented as its own class and this class will contain a sorting strategy as a member variable as well as a method or constructor that allows us to set whatever sorting strategy we need outside that class. And then what happens is whenever this class needs to perform its task, right, so whenever our client needs to sort something, it'll call the sort method on its current strategy member variable. So that's basically what the pattern looks like. We'll see this in more detail in our coding example.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Algorithms](../../Skills/Software%20Development/Algorithms.md) (3)
> **CLI Commands:** make (1)
> **Definitions:** is called (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Creating strategies](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-strategies?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-strategies?u=76281980&t=0)** - [Narrator] All right, so now that we've learned the basics of the strategy pattern, let's take a look at an example of what it might look like in code. Now previously, we discussed the example of using the strategy pattern to choose between different sorting [Algorithms](../../Skills/Software%20Development/Algorithms.md) and that's absolutely possible to implement. But for our example here, we're going to do something just a little bit simpler. We're going to use the strategy pattern to allow us to specify different greetings for people in our program. So let me show you what I mean by that. If you look at the starter code for this video, which we have here, and you can find it in the exercise files, you'll see the situation that the strategy pattern is meant to help us with. So specifically we have this person interface with a single greet method. And then what we've had to do is define a new subclass of this interface for each different person that greets people in a different way. Right? So we have a business person that says, "Good morning, how do you do?" That's how business people talk, of course. We have a normal person that says, "Hi, how are you?" We have a cool person that says, "Hey, what's up?" That's how cool people talk, as you well know. But then the problem is that we come to this politician class, right? But we see here that the politician and the business person have the same way of greeting. And assuming that the politician and business person classes are different in some other key ways, right? Maybe they have different member variables and different methods, et cetera. We really have no other choice than to just duplicate the code inside the greet method. So how can we use
>
> **[1:32](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-strategies?u=76281980&t=92)** the strategy pattern to fix this? We're going to start off by saying "class GreetingStrategy" and what we can actually do, I'm just realizing, is this person class has the exact method that we need our greeting strategy to have. Right, so our greeting strategy is going to have a single virtual method called "greet" that different implementations of this strategy are going to implement. So here's what I mean by that. Instead of defining a person subclass for each of the different greetings, right? Instead of using subclasses to change the behavior of different people, what we're going to do instead is rename these things as strategies. All right, so we're going to say "class FormalGreetingStrategy" and that is a subclass of our greeting strategy. We're going to have this one be a "NormalGreetingStrategy." Oops, and that's going to be just "GreetingStrategy." And we'll have an "InformalGreetingStrategy" here. "InformalGreetingStrategy," "GreetingStrategy."

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1)
> **Exercise Files:** starter code (1), exercise files (1)
> **CLI Commands:** find (1)
> **Cross-References:** we discussed (1)
> **Definitions:** is a  (1)
> **Speakers:** - [narrator] (1)

#### [Rewriting classes to use strategies](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/rewriting-classes-to-use-strategies?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/rewriting-classes-to-use-strategies?u=76281980&t=0)** - [Instructor] And then what this allows us to do is it allows us to have a single person class. Alright, so let's define that here. We're going to say class person and this person class is going to have a greeting strategy as a member variable. And what this allows us to do is use a single person class and simply pass in different greeting strategies depending on how the person is expected to behave. Alright, so in our case here we're going to say greeting strategy, greeting strategy. We're going to allow ourselves to pass in the greeting strategy in the constructor here. We're going to say person greeting strategy greeting strategy and we'll say greeting strategy, greeting strategy. Alright, so that's the constructor. We're going to also have a destructor just because we'll want to be able to pass in sort of an anonymously defined object. So we'll say person and we'll just have this delete the greeting strategy. Alright. And it's just going to give us a little warning here. If you want to make that go away, you can just go up here and add a virtual destructor to our greeting strategy.
>
> **[1:28](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/rewriting-classes-to-use-strategies?u=76281980&t=88)** And then we're going to add a greet method to our person and all our person is going to do inside this method is simply call the greet method of its corresponding greeting strategy. So it's just going to say greeting strategy, greet name. And once we've got all of this logic setup here's what it's going to actually look like to use these classes we've created. Alright, so we're going to create our people. Let's say person, business person. Going to pass in a new formal greeting strategy to the constructor. So we can just say this, a fact, just use a reference. I'm just going to say normal person. And remember that we used to have to have separate subclasses for these things. Now we're able to just create them like this all using the person class. Alright, so normal person, we're going to give them a normal greeting strategy. The cool person is going to have a informal greeting strategy, and, the politician, here's where the code reuse comes into place is going to reuse this formal greeting strategy. Alright. Okay, so now if you want to copy and paste the test code from the final state of the exercise files that's going to look like this.
>
> **[3:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/rewriting-classes-to-use-strategies?u=76281980&t=180)** If you want to type that out instead be my guest, and let's run this and see what it does. Alright, so we're going to see that it prints out the business person says, "Good morning, Shaun, how do you do?" The normal person says, "Hi Shaun, how are you?" The cool person says, politician says, and each one behaves differently depending on the strategy that we passed in to the constructor, alright? So that's the powerful advantage of the strategy pattern. If we have two different people that greet in the same way we can simply reuse the same strategy instead of having to duplicate all of that logic over again. And obviously this is a pretty simple example of what we just saw here, but the concept here applies just the same to much more complex situations.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Exercise Files:** exercise files (1)
> **Definitions:** is a  (1)
> **Warnings:** warning (1)
> **Prerequisites:** setup (1)
> **Speakers:** - [instructor] (1)

#### [The Template Method pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-template-method-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-template-method-pattern?u=76281980&t=0)** - [Announcer] Okay, our next assigned pattern is called the Template Method Pattern. The goal of this pattern is to give us a clean way to represent processes that involve multiple steps where each of those steps can involve some sort of variation depending on the situation. Generally you'll know that you should use this pattern when you find yourself writing a large chunk of code over and over again for several different situations where each different version of this code just has slight variations or tweaks here and there. An example of this would be if you were writing a program that's meant to load, format and consolidate data from a variety of sources. So you might be loading data from a [JSON](../../Skills/Web%20Development/JSON.md) API, a database, text files, maybe even scraping web pages or something like that, and each of these different situations is going to involve pretty much the same steps, right? We're going to load the raw data, we're going to parse that data, maybe we have to format that data, we then have to filter it and finally, we store that in our database, something like that. Now the tricky part is that each of these different situations is going to involve slight variations in each step from all the other situations, for example, during the load step, the JSON API might require us to make a network request, the database will require us to connect and query some sort of database and the text file loader will require us to open a text file, right, and so on, so forth. Now, quite often when programmers try to implement
>
> **[1:33](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-template-method-pattern?u=76281980&t=93)** this sort of situation, what they do is they write each situation as it's own huge lump of code. For example, they might have one function for loading, parsing, formatting, filtering and storing the JSON data, and likewise for the database data, the text file data, the web scraping data, et cetera. And that is where the Template Method Pattern steps in to help us out. Basically, in order to incorporate the Template Method into our code, what we're going to have to do is break our code down into logical steps that each different situation is going to contain and then we'll express each different variation of those steps as it's own class, that'll implement some of the steps in a slightly different way. You'll see what I mean in just a minute here. So in UML this is going to look something like this. We start off by creating an abstract class that has a method for each step, right? So in the example that I described earlier, we'd probably create an abstract data pipeline class, and the steps for that would be load, parse, format, filter and store, and obviously there might be a few others, but the point here is that these methods are logically shared by all the different variations of this process, and each of these methods can either be abstract themselves, right, where subclasses are required to override them, or they can provide some sort of default implementation that subclasses can simply keep as is. We'll see what this looks like again when we get to the coding example. And the last method that this data pipeline
>
> **[3:06](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-template-method-pattern?u=76281980&t=186)** abstract class is going to contain is some sort of public method that'll take care of calling each of these different step methods in sequence, right, usually we'll call this execute or something like that. So anyway, once we have that abstract class, what we do then is we extend that class for each different situation that requires a variation on the process that we're implementing. Right, so these classes can then override each method that requires a variation in their particular case. So a JSON data pipeline subclass would provide special logic for loading data from the API in it's load method, as would the file data pipeline, database data pipeline and so on, in addition to whatever other methods are required for their particular use cases. And finally, whenever we want any of these subclasses to do it's thing, we simply call the execute method that it inherited from the abstract data pipeline class and everything will be performed just as we want it to in that order of steps.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (4)
> **Env Vars:** json (4), api (3), uml (1)
> **Exercise Files:** template (3)
> **CLI Commands:** find (1), make (1)
> **Analogies:** for example (2)
> **Definitions:** is called (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [announcer] (1)

#### [Creating template methods](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-template-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-template-methods?u=76281980&t=0)** - Okay, now that we've learned the basics of the template method pattern, let's take a look at a concrete example of what this pattern is going to look like in our code. For our example here, we're going to be looking at the situation of writing a program that generates greeting cards for people. So since you're all programmers, I'm going to assume that those of you who regularly write greeting cards for friends or family members have noticed that greeting cards generally follow an extremely predictable format, right? You're going to have the introduction, which is something like, dear Sean, then you have the body of it, right, so that'll be something like happy birthday. Right, blah, blah blah. And then you'll have the sign-off, something like, you know, sincerely Sean, right. Writing myself a birthday card. So this is actually a very good example of a situation where we could use the template method pattern. All right. So let's jump right in and start off by defining a top level greeting card template class. All right, so we're going to create a class here called greeting card template. And this template here is going to contain the most generic implementations for each section of a greeting card, as well as logic for stringing all of these parts together. Let me show you what I mean by that. We're going to create, first of all, some protected methods. These will only be accessible to subclasses of our greeting card template. And these are going to be the different so-called template methods of our greeting card generator. And these are going to represent
>
> **[1:32](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-template-methods?u=76281980&t=92)** the different sections of our greeting cards. So we're going to have our first method. These are all going to return strings, by the way. You'll see exactly how this works in just a second. Our first method's going to be called intro, and that's going to take a string as an argument, and that's going to be the name of the person that we're writing the card to. Okay, so that's going to generate the intro, right, it's going to say dear so-and-so, or hey so-and-so, or hello so-and-so, whatever you want the top of the card to say. That's going to be your intro. And for our most generic implementation here, we're just going to say dear, and add the two string onto that. And then end with a comma and a new line character. All right. So that's the intro. The next part of our greeting card is going to be the actual body of the greeting card. For this one we'll just call it occasion, right, it'll be where we say, you know, happy new year, happy birthday, whatever we're writing the card for. Right, that's not going to to need to take any arguments. And it's just going to return something in its most generic form, right, a very generic greeting card will say something like just writing to say hi. Hope all is well with you. All right? And lastly, it's going to to be a very short greeting card, we're going to end with the closing, right, so we're going to have another method called closing that contains the name of the person that the card is from. So for that, we're going to do the exact same thing here.
>
> **[3:06](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-template-methods?u=76281980&t=186)** We're going to have the name of the person it's from, and we're going to return something like, sincerely is a pretty generic sign-off. We're going to have sincerely, new line, and then we're going to add the name of the person it's from, and another new line to end the card. Okay. So those are our three template methods of our greeting card template. The other thing that our top level greeting card template class is going to contain is a method that we can call that will sort of tie all of these template methods together, right, whether that's calling each one of them in sequence, whether it's combining the results of all of them. Whatever the case, we're going to create a public method. And that method is going to be called generate. All right, and it'll take the to and from arguments. So we'll have a string to, and a string from. And what this is going to do is it's going to string together the results of these three template methods. So we're just going to say return intro, and we're going to pass the to argument through to that one. We're going to have the occasion, and we're going to have the closing, and we're going to pass the from name through to that. All right.

> [!info]- Semantic Content
>
> **Exercise Files:** template (12)
> **Definitions:** is a  (2)
> **Speakers:** - okay (1)

#### [Modifying templates using subclasses](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/modifying-templates-using-subclasses?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/modifying-templates-using-subclasses?u=76281980&t=0)** - So that is our greeting card template. The next thing we're going to do, right? Here's where the power of this pattern comes in in order to make modifications to the basic implementation. For example, if we wanted to make a birthday card generator class, we only have to override the methods of this greeting card template that are going to be different. So if we wanted to create, let's say class, and we'll say a birthday card template, and we'll make that extend the greeting card template class. All we have to do now to change the occasion is give this class its own implementation of this occasion method. All right? So we're just going to say string occasion override. And we're going to return a different string for the occasion. So we're going to say something like happy birthday. Hope you have a wonderful day and lots of cake. What a nice birthday card. All right. So that's our birthday card template. Why don't we, while we're at it, just make one more class. Let's make a New Year's card template. I'm not sure how common New Year's cards are but while we're at it, why not? All right? So we're going to say New Year's card template. This is going to extend the greeting card template as well, and actually we just need to label this as public, both of these. And for this one, maybe we want to override both the intro and the occasion, right?
>
> **[1:32](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/modifying-templates-using-subclasses?u=76281980&t=92)** So in that case what we would do is we would say string intro. This one takes a string as an argument and maybe it will return something like the person's name, plus and maybe it just has a bunch of exclamation points after it, right? Very enthusiastic New Year's card. And after that obviously, we want to change the occasion here as well. So we could do that by defining a new occasion method. Let's just mark these as override, right? So occasion override. And for this one, we'll return something like happy New Year's, bunch of exclamation points. See you at the gym tomorrow. All right. So we now have two different templates that only overrides certain methods and notice that these classes themselves, the birthday card template and the New Year's card template, don't have to override all of these methods. And they also don't have to worry about how the methods are actually strung together either, right? So that's all taken care of by the generate method on this greeting card template class. So the way we would actually use these templates that we just created would be something like this. We're going to create a greeting card template. We'll call it G C T. We're going to create a birthday card template. We'll call that birthday card template
>
> **[3:07](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/modifying-templates-using-subclasses?u=76281980&t=187)** and a New Year's card template, which we'll call New Year's card template. Perfect. So now the next thing we're going to do is we're going to copy and paste the code from the finished exercise files, just to save time on typing. It's just going to look like this. And if we run our code now, we're going to see that it generates three different cards for us, each of which has some of the strings from the greeting card template class, as well as its own special overrides of those different methods. Oops, and let's just go in and put some new lines after these, just to make it look a little better. Here we go. And if we run it again, we'll see that that looks really good. And one more thing to keep in mind is that even though we just used the topic of greeting cards in this example for simplicity's sake, this kind of thing can apply to many different situations, right? Basically anywhere that you have a series of steps where each of the steps is a little different in certain situations, right? Just like what we had here is when you'll want to use the template method pattern.

> [!info]- Semantic Content
>
> **Exercise Files:** template (18), exercise files (1)
> **CLI Commands:** make (6)
> **Analogies:** for example (1), just like (1)
> **Definitions:** is a  (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - so (1)

#### [The Visitor pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-visitor-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-visitor-pattern?u=76281980&t=0)** - [Instructor] All right, our next pattern is called the Visitor pattern. The Visitor pattern is used when we need to add some sort of similar functionality to many different classes. And this functionality doesn't really make sense inside the classes themselves. An example of this might be, if we had a bunch of classes and we needed a way to save instances of each class to a database let's say. Now the naive way to do that would be to add a writeToDatabase method to each of the classes in question. And while this approach works, it has the tendency to really add a lot of clutter to our classes, right? In this situation, may be a single extra write to database method isn't so bad. But what about if we also need to be able to export instances of this class to a text file, or to a [JSON](../../Skills/Web%20Development/JSON.md) string, or some other format, right? Ultimately, all of these extra methods, none of which really has anything to do with the actual functioning of the classes themselves, these methods just end up adding a lot of extra code to our classes, and that's where the Visitor pattern can help us. The basic idea of the Visitor pattern is like this. Instead of adding logic for doing whatever it is to each of the classes themselves, we separate this functionality out into a separate class or classes that take care of it themselves. Now, the way that this works architecturally is that we create a Visitor interface as well as classes that implement it. This interface generally contains one method for each class that we would have had to otherwise add an ugly method to, right? So in the case of writing to a database,
>
> **[1:35](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-visitor-pattern?u=76281980&t=95)** we'd have a database Visitor interface with methods that took care of the actual logic for writing given instances of different classes to a database, and likewise for exporting to a file or for exporting to JSON. We would have a different visitor for each of those different cases. And once we've done that, the last thing we need to do is we add a tiny trivial method to all of our original classes, right? And this method is usually called something like accept. And what this method does is it just calls the corresponding method on the visitor instance that it's called with, and usually passes itself as an argument or some kind of data from itself as an argument. We'll see what this looks like in the code in just a minute here. So the main benefit of this pattern then is that where we would have normally had to add two, or three, or 10 different methods to all of our classes, right, write to database, export to file, export to JSON, et cetera. We now only have to add a single accept method to all those classes, and the extraneous logic gets put into its own classes, right? The ExportToJSONVisitor, the WriteToDatabaseVisitor, et cetera. And this not only keeps our classes free of extraneous methods, but it also groups together that code in a more logical way.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (3)
> **Env Vars:** json (3)
> **CLI Commands:** make (1)
> **Code Identifiers:** writetodatabase (1)
> **Definitions:** is called (1)
> **Speakers:** - [instructor] (1)

#### [Implementing visitors](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-visitors?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-visitors?u=76281980&t=0)** - [Instructor] Alright, so we've seen the basics of the visitor pattern let's take a look now at a concrete example of what this pattern is going to look like in our code. So for the example that we have here we're going to be using the situation we discussed earlier where we have several classes and I just picked several fairly random kinds of things. A person class, we have a landmark class and we have a car class just to show that this pattern can be used even when the objects we're using it with don't really have any relationship to each other. So let's say that we want to be able to do things to all of these classes, like write instances of them to a database or export them to a text file, that kind of stuff, right, so remember we said that the usual way that this is implemented is simply by adding a method for each of these things to the classes themselves, right, so inside person we'd have a write to database method we'd have a write to text file method blah, blah, blah and we'd have the same thing in landmark and the same thing in car as well. And these methods can really start to clutter up our classes since they don't really have anything to do with the way these classes themselves work, right, it's more of an extraneous kind of function. So what we can do instead is we can use the visitor pattern and here's what that's going to look like. We're going to start off by defining a visitor interface with methods for each of the classes that we want to add a given piece of functionality to. Alright, we'll see exactly what I mean by that in just a minute here. So let's define our visitor interface, that's going to be called visitor And we're going to define one public method
>
> **[1:36](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-visitors?u=76281980&t=96)** for each of the classes here. So the first one, we're going to say virtual void and we're going to call it handle person, alright and this one is going to take the person's name and age member variables as arguments. So we're just going to say string name and age and we're going to define this as a pure virtual method And we're going to do the same thing for landmarks and for cars so we're going to have handle landmark this is going to take the name and the city name of the landmark, name, city name, okay, it looks like I typed that in wrong let's fix that, there we go. And lastly, we're going to have a handle car method as well so we're just going to change this to handle car change this to make and model, okay. So that's what our visitor interface is going to look like. Once we've defined that interface, we can now define implementations of it that will contain the actual logic. So for writing these classes this person landmark in car class to a database that would look something like this we'd have class call it something like database visitor And we'd say that it extends the visitor class, alright. And then we're going to define all of these methods for our database visitor class so I'm just going to copy and paste that
>
> **[3:11](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-visitors?u=76281980&t=191)** remove virtual from these and we're going to start off with our handle person for our handle person, all we're going to do for now, right, because we don't have an actual database to write to we're just going to print out writing person to database and we'll put their name and their age in here. Alright, name and age just like that and we'll do pretty much the same thing here for our landmark and car classes, just like that and like that. And we'll change this to writing landmark to database say name and city name and for the car, we're going to say writing car to database and we'll say make and model. And the point here is really just to show that we have very different functionality, well, I mean, it's not that different here but it can be as different as we need it to be for each of these methods. So we have our database visitor class now, if we wanted to also be able to do things like write our objects here to a text file or a PDF or something like that, we could do that like this, this isn't going to be too different from the database visitor class, because again, we're not actually writing things
>
> **[4:42](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-visitors?u=76281980&t=282)** to files or to [Databases](../../Skills/Software%20Development/Databases.md), but we'll just change this now to say writing person to file, writing landmark to file, and writing car to file.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **CLI Commands:** make (2)
> **Analogies:** just like (2)
> **Env Vars:** pdf (1)
> **Cross-References:** we discussed (1)
> **Speakers:** - [instructor] (1)

#### [Allowing classes to accept visitors](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/allowing-classes-to-accept-visitors?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/allowing-classes-to-accept-visitors?u=76281980&t=0)** - [Instructor] And now that we have our visitor classes the way that our person and landmark and car whatever classes we have, are actually going to allow us access to their data, is by providing a single method that we can pass one of these visitor objects to. And when we do that, these classes will call their corresponding method on the visitor. So as an example here, the person class will provide an accept method. Right. And that'll have a pointer to the visitor that's trying to visit, and we'll simply call visitorhandlePerson. And we'll pass the person's name and age in. Okay. And we'll do the exact same thing for the landmark and car classes, except, we're going to, instead of having handlePerson, we're going to say handleLandmark and that's going to have the name and cityName. And for our car, that's going to be handleCar with the make and model. Okay. And just to reiterate also this accept method is the only method we're ever going to have to actually add to these classes, right? 'Cause now that we've added it. All that we have to do if we want to add some sort of extraneous functionality, like writing to a database, writing to a text file, writing to an [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) file, whatever we want to do. All we have to do is express that using this visitor interface. All right.
>
> **[1:36](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/allowing-classes-to-accept-visitors?u=76281980&t=96)** So we don't have to add hundreds of specialized functions to each of our classes to implement that kind of functionality. So the way we're going to use these now, is like this. We'll create a person. Person's name will be [John](../../Glossary/Tool/John%20the%20Ripper.md). 40 years old. We'll create a landmark. Right. That'll be the Eiffel tower in Paris, and we'll create a car. All right. So Car car, and we'll do a Chevy Camaro. All right and now that we have those two things, we're going to create two visitors. One is going to be a database visitor, and one's going to be a text file visitor. So we're going to say DatabaseVisitor, and we'll just abbreviate that dbv = new DatabaseVisitor, and TextFileVisitor, we'll abbreviate that tfv, and we'll say new TextFileVisitor. And then we just need to call, the corresponding accept method on each of these objects that we just created here. And pass it, one of these visitors, and it'll do its thing, right? So person.accept, dbv, we'll write the person to a database Right, landmark.accept. We'll write the landmark to a database if we pass the database visitor to it. And likewise, car.accept, we'll pass the database visitor to it. All right. And we can do the exact same
>
> **[3:09](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/allowing-classes-to-accept-visitors?u=76281980&t=189)** thing with the text file visitor. Right. So if we change this now to text file visitor and we just need to make sure to delete our visitors here. Now, if we run our code. We're going to see that everything works just like we want it to. All right. So we're going to see writing person to database, writing landmark to database, blah, blah, blah. And that is that. And again, the beauty of this pattern is that we can add a lot more visitor implementations if we want to, for different functionality without having to add anything to the person landmark or car classes themselves. All we needed to do those was add this accept method and we can basically accept any visitor we want to now.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1), [John](../../Glossary/Tool/John%20the%20Ripper.md) (1)
> **Code Identifiers:** visitorhandleperson (1), handleperson (1), handlelandmark (1), cityname (1), handlecar (1)
> **CLI Commands:** make (2)
> **Analogies:** just like (1)
> **Best Practices:** make sure to (1)
> **Speakers:** - [instructor] (1)

#### [Project: Create a greeting-card generator program](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-greeting-card-generator-program?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-greeting-card-generator-program?u=76281980&t=0)** - [instructor] Okay so far we've seen five more [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) as well as some individual examples of each one. And what we're going to do now is just like in the previous chapter, we're going to take a look at an example that combines some of these patterns. And just like last time we're not going to be combining all of them here. Since again, that would take us a good bit of time to write a program that would be large enough to need it. But what we're going to do is we're going to just mix two of the design patterns together and then we'll have a challenge solution video where there'll be challenged with making a few changes to the code that we ended up writing. So the program that we're going to be writing here is going to be an extension of the greeting card generator program that we first saw in the template method pattern example. And we're going to mix this example together with the visitor pattern that we saw earlier. And the end result is going to be a greeting card generator program that can generate greeting cards for a number of people. So here's what all this is going to look like. We're going to start off by copying and pasting the code that we wrote in our template method pattern example. All right so everything except the main function we're just going to copy. And paste right here. Okay so we have our greeting card template, birthday card template, new year's card template et cetera. And then we're going to head over to our visitor pattern example, and you can find the finished states for these two pieces of code in their respective videos by the way. And what we're going to do is we're going to copy the visitor interface that we created and we'll paste that here.
>
> **[1:33](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-greeting-card-generator-program?u=76281980&t=93)** And we're also going to copy the person class and put that down here. All right and we're just going to make a few changes here. Really, the only change we need to make is we're just going to delete the landmark and car ones since we're not going to need those for this example. And the next thing we're going to do here is we're going to change our greeting card template class a little bit. So if we scroll up here, what we're going to do basically is instead of expecting the client code to pass in the, to and from strings directly, when it calls generate what we're going to do is we're going to add the visitor pattern to this greeting card template class so that it can get information from the person class directly via the visitor pattern that we saw earlier. So what we're going to do is we're going to start off by making this greeting card template class implement the visitor interface and let's move the visitor class and stuff up to the top. (keyboard chattering) There we go. And the next thing we're going to do is we're going to make the greeting card template actually have the from string here as a member variable. All right. So when we basically create a greeting card template it'll already have the person's name that it's from As part of the class. Okay so we're going to say string from, and then down here in public, we're going to add a constructor. I'm going to say greeting card template.
>
> **[3:08](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-greeting-card-generator-program?u=76281980&t=188)** It's going to take a string as an argument and that's going to be our from And then we're going to have to declare a destructor as well just going to say greeting card template. (keyboard chattering) And there we go. And the next thing we're going to do here is we're going to change this generate method. All right. So it's going to go from returning a string to just being a void. All right so it's just going to do something. We'll go back to this in the challenge and solution video how to turn it back so that it actually returns a string but for now, we're just going to make it do something. It'll be much easier to incorporate the visitor pattern right now, if we just do it this way. All right. So we're going to change this method a little bit instead of just calling it generate, we're going to say generate card for, and instead of passing in to and from, or just going to pass in a person pointer. Alright. And what this is going to do now. And what this is going to do now, is instead of just calling return intro occasion closing we're going to actually have our greeting card template class, visit the person class. All right so we're going to say person except and pass this as the visitor okay. And we're going to cut this out of here for now. We'll actually paste it in this other method we're going to put here. And this other method is going to be the handle person method that we have to add in order to make our greeting
>
> **[4:41](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-greeting-card-generator-program?u=76281980&t=281)** card template actually implement this visitor interface. All right so we're going to say void handle person, (keyboard chattering) and here's where we're going to take the person's name and age right? Cause that's the signature that we're going after here. So actually I'm just going to copy that. Will save me a little typing. And inside here, what we're going to do is we're simply going to print something out. So We're just going to say, see out, and we'll say something along the lines of sending this card to, and then we'll put the person's name. We'll put some new lines and here is where we're going to put the intro with the name, the occasion , and the closing with the from member variable that we have up at the top here. And now that we've done all that modification to our greeting card template, right? We've incorporated the visitor pattern into that. One last thing we're going to do is actually create a greeting card generator class that will sort of abstract away a lot of this logic for us. So what this is going to look like we're just going to say class greeting card generator. (keyboard chattering) All right. And this is going to have a template here, right? We're going to actually be able to specify different templates. So we're going to say greeting cards template.
>
> **[6:15](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-greeting-card-generator-program?u=76281980&t=375)** We'll just call it temp. And then we're going to have a vector of people. And these are, are going to be the people that we want to generate greeting cards for okay. So what we're going to do after that we're going to add a method called send greeting cards Which is basically just going to loop through all of the people in this vector here, generate a greeting card for them. And we're just going to see this thing here, printed to the console. All right. And before that we just need to include vector up at the top there. And looping through all our people is just going to look like this. We're going to say auto person, people. (keyboard chattering) And then we're going to say template generate card for, and just pass that person like that. Okay. So that's send greeting cards method. We're going to add two more methods here. The first one is going to be an add person method for adding people to this vector here. We're just going to say, add person. It's going to look like this. And we're going to say people pushback person. All right the other one is going to be a set template method. And we're going to actually be able to use this to switch out the templates that our greeting card generator is using right? So we'll be able to have one greeting card generator and we'll be able to switch out
>
> **[7:47](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-greeting-card-generator-program?u=76281980&t=467)** whether we want it to generate birthday cards new year's cards, whatever kind of cards it is we're trying to generate. So this is going to take a greeting card template as an argument. And it's basically just going to set the template to that new template there all right. Pretty straight forward. So now that we have that we just need to have the client code. And just for the sake of time here I'm going to just copy and paste that test code from the finished state of the exercise files. And I'll explain it before we go on oops and it looks like we actually forgot to do something with the birthday card and new year's card templates. We just forgot to update the constructor. So our birthday card template here we're just going to add a constructor. That's going to look like this. It just needs to take a from argument (keyboard chattering) and it will send that up to the greeting card template, (keyboard chattering) All right and the same thing for our new year's card template here oops and we're actually going to want both of these in public Almost miss that. We're going to add public paste that there and then we'll do the same here, public paste it. Changes to new year's card template. Okay and that should do it.
>
> **[9:19](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-a-greeting-card-generator-program?u=76281980&t=559)** So what we're doing here is we're basically just creating three people. We're creating a greeting card generator. We're adding several people to it. And then what we're doing is we're setting the template for the generator, right? So basically when we set its template to a birthday card template what it'll do is when we actually call generator send greeting cards, it will use that birthday card template to generate those cards. And likewise, when we set it to a new year's card template doing it this way it's sort of a mix of the template pattern and the strategy pattern that we learned about if you want to think about it that way. Okay so let's run this code now And we're going to see that it'll print out several different cards. All right so we're generating birthday cards here and regenerating new year's cards.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) (2)
> **Exercise Files:** template (32), exercise files (1)
> **CLI Commands:** make (5), find (1)
> **Cross-References:** previous chapter (1), go back to (1)
> **Analogies:** just like (2)
> **UI Navigation:** scroll up (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Modifying the card generator](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/challenge-modifying-the-card-generator?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/challenge-modifying-the-card-generator?u=76281980&t=5)** - [Instructor] All right, now that we've seen an example of how the template method and visitor patterns can fit together. And remember I said that this example kind of included the strategy pattern as well. Now that we've seen all that it's time for a challenge. Your challenge here is going to be to make a few changes to this code that we wrote previously. More specifically, notice that the way we have our code written here, our visitor implementation, that is our greeting card template class here. Takes care of all the card generating and sending itself inside this handle person method. And this is fine, but it's very likely that there will be situations where instead of directly sending the card content as soon as it's generated, we'll want to actually be able to access these generated strings from elsewhere in the program. Right? We'll want to be able to access the text of the cards elsewhere than just in the console. Well, as it happens, that's more or less your challenge here. To modify the code we wrote here so that instead of our greeting card generator class simply calling template generate card for, for all the people, it actually returns a vector containing a string for each of the birthday cards it generated. Right? So we'll be doing something like this where we just create a vector of strings. And we'll call this cards. And each time we go through here we're going to want to push the result onto these cards. And then finally return that from this method. All right.
>
> **[1:37](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/challenge-modifying-the-card-generator?u=76281980&t=97)** I'll remove that for now but that's the basic idea. So this challenge should take you about 10 minutes tops. And once you've finished it, feel free to move onto the next video where I'll show you how I would go about solving this.

> [!info]- Semantic Content
>
> **Exercise Files:** template (3)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Modifying the card generator](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-modifying-the-card-generator?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-modifying-the-card-generator?u=76281980&t=0)** - [Instructor] Okay, hopefully you've given the challenge a try. Now let's take a look at one possible solution. The first thing I did here, was to change the return type and name of our method for generating cards. Okay, so if we go up here to handle person, we're going to need to change this from a void method to one that returns a string, okay. So we're going to change that to virtual standard string handle person. All right, and the next thing we have to do is actually change the accept method as well. So instead of just being a void method, it'll return a string as well. This will help us sort of pass the resulting string for the greeting card up through the classes, okay? And we're going to change this also to return. All right, so this is returning a string. This is returning that string from this method. And the next thing we're going to do here is we're going to change this handle person method. So we're going to just change this so that it returns a string as well. All right, so instead of writing this to C out, what we're going to to do is we're simply going to say return, intro, plus occasion, plus closing. And after that, we're going to head down to our greeting card generator class and make a few changes here. So, first of all instead of naming this method send greeting cards, we're going to change it to create greeting cards to make it a little more obvious
>
> **[1:35](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-modifying-the-card-generator?u=76281980&t=95)** that we're returning some strings. And this is going to return a vector of strings, all right. So just like I said in the challenge video, the next thing we're going to do here is we're going to create just an empty vector. And we'll call that cards. And then we're just going to push the result of calling generate card for onto this card's vector. All right, so we're going to say cards, push back, generate card for person. And then let's return our cards. Now, this is still complaining because what we need to do is go back up to our greeting card template and change the return type of our generate card for method to string and actually return the result of calling person accept this, all right? And that should get rid of that warning. And then again, I'm just going to copy and paste the test code from the final state of the code for this video. And there it is. So you can see instead of just letting our generator generate and just print them to the console, we're actually getting the result that we get from Collin Creek greeting cards and printing it out to the console ourselves, okay? So in other words, our client code now has access to those things. So let's run our code. And we'll see that it prints out the greeting cards for all of our people here.

> [!info]- Semantic Content
>
> **CLI Commands:** make (2)
> **Exercise Files:** template (1)
> **Definitions:** in other words (1)
> **Analogies:** just like (1)
> **Warnings:** warning (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 4. Behavioral Patterns Group 3

[↑ Back to Table of Contents](#table-of-contents)

#### [The Iterator pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-iterator-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-iterator-pattern?u=76281980&t=0)** - [Instructor] Okay, the next pattern we're going to look at is called the iterator pattern. The iterator pattern is a way that we can allow ourselves to work with different collection [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) in an orderly way, right. Think of arrays, linked lists, graphs, trees and so on without caring about the underlying organization of that data. The fact is, that different data structures can make it pretty difficult for us when all we want to do is simply iterate through all the items that they contain, right. In other words, there are a lot of situations where we only care about getting the next or the previous item in a collection. Now the problem is this. Depending on the type of data structure we're using, the logic for simply getting the next or previous item in the collection can actually get quite complex. So think about if our collection was represented using a tree structure. In this situation, not only would there be multiple different ways of iterating through it, for example, we could use either a depth-first or a breadth-first traversal, but the logic for doing these things can also be very complex, right. More often than not this leads us to incorporating collection specific logic into our business logic which is, generally, a pretty bad thing. So here's the basic idea of the iterator pattern. We abstract away the internal organization of our different data structures, and simply provide a single iterator interface that other parts of our program can use to iterate through our collections simply by calling next, without caring about the way that our collections are actually organized, right, without caring
>
> **[1:32](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-iterator-pattern?u=76281980&t=92)** that they're a tree, or a list, or a linked list, something like that. So here's what this'll look like in a UML diagram. As I said, we generally start off by creating an iterator interface that the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the app can use to iterate through different structures, and this interface usually provides next and is finished methods, and sometimes the previous method as well, although that's usually more of a convenience than an actual necessity. And once we have this interface the other parts of our app can then use these methods to iterate through any collection, regardless of how the collection is actually organized behind the scenes. And the way that we do this is by creating different implementations of this iterator interface for each different method of traversal that we want for all of our various collections. So for example, we might have a DepthFirstTreeIterator, a BreadthFirstTreeIterator, for iterating through elements of a tree, we might have a ListIterator, for iterating through a list, and so on, so forth, right. And notice, here, that a single type of collection can have several different kinds of iterators, right, depending on, again, the order in which we want to iterate through a given collection. And another thing to note about these iterators is that each of them keeps track of where it's currently at in iterating through the collection, and that makes it possible for us to have several different iterators that are all iterating through the same collection at the same time, but are at different spots in the collection. And there's one last piece to this pattern,
>
> **[3:05](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-iterator-pattern?u=76281980&t=185)** and that is that the iterators for a given collection are usually created by the collections themselves. So our tree collection might have, you know, like getDepthFirstIterator, again, BreadthFirstIterator, it might have those methods that return those respective iterators.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (3), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Definitions:** is called (1), is a  (1), in other words (1)
> **Analogies:** for example (2)
> **CLI Commands:** make (1)
> **Code Identifiers:** getdepthfirstiterator (1)
> **Env Vars:** uml (1)
> **Speakers:** - [instructor] (1)

#### [Implementing the Iterator pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-iterator-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-iterator-pattern?u=76281980&t=0)** - [Lecturer] Okay. So we've seen the basics of the iterator pattern. Now it's time for a coding example. For this example we're just going to use the very simple case of using iterators to allow ourselves to iterate through a series of elements in different orders. Alright. So let's start off here. There's no starting code for this one, really except for just the main method. So let's start off by defining an iterator interface. We're going to define more of a specific iterator here. We're going to define a number iterator. Alright and this is going to allow us to iterate through collections of integers. Alright. So it's going to look like this. It's going to have a virtual distructer to it just because of how we're going to be extending it. We're going to add a next method to it. Right? Remember that the client code will be able to call this next method to get whatever the next item is in the iteration. And finally, we're going to add a Boolean function called is finished Which will return whether or not there are more items in the area. Okay. So that's our number iterator interface and note here that it would be perfectly possible to make this a generic class so that it would work with different data types. Right? So we could just call it iterator in that case but just for the sake of simplicity here, we're just going to use integers. So let's define now some concrete implementations of this number iterator interface. Specifically, what we're going to do here is we're going to define two different iterators one for iterating
>
> **[1:35](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-iterator-pattern?u=76281980&t=95)** through a vector of numbers forwards and one for iterating through a vector of numbers backward. Here's what that is going to look like. We're going to start off by defining our forwards iterator, public number iterator And we'll have a backwards iterator too. Backwards iterator. Public number iterator. Add semi-colons after both of those. And the forwards iterator is going to look like this. We're going to have a current position member variable which keeps track of where in the iteration we currently are. And then we're going to have a reference to a vector of integers and we'll call that numbers. Okay. So what we're going to do and now is implement the next in its finished methods from the number iterator interface. And that's going to look something like this. We're going to say int next. And what we're going to do here is we're going to say int current equals numbers.at current position.
>
> **[2:48](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-iterator-pattern?u=76281980&t=168)** Alright, we're going to increment the current position. And lastly, we're going to return the current element that we got here. Alright. And another thing we have to do is define a constructor here. And the constructor is just going to take a vector reference like this called numbers. and it's going to initialize the current position to zero and set numbers to numbers.
>
> **[3:24](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/implementing-the-iterator-pattern?u=76281980&t=204)** Oops. So we have to include the vector here. There we go. Alright. So we have our next method, which basically just steps through the vector one by one. So the other thing that we have to define now is the is finished method. So we'll say Boule is finished, override and we're simply going to return for this, whether or not the current position is greater than or equal to numbers.size. And that is our forwards iterator. So if we wanted to make a backwards iterator it's actually going to look pretty similar, but the logic is going to be slightly different for doing so. Alright. So really the only thing we have to change here. Well, first of all, we need to change the constructor so that it matches the name and the other thing that we have to change, we just have to change numbers.at current position, to numbers.at numbers.size minus current position minus one. And that will give us the element that we're looking for in a backwards fashion. Alright. So as current position is incremented we'll go backward through the vector.

> [!info]- Semantic Content
>
> **CLI Commands:** make (2)
> **Speakers:** - [lecturer] (1)

#### [Using iterators](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-iterators?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-iterators?u=76281980&t=0)** - [Narrator] So that's our backwards iterator. Pretty simple. Now the last thing we're going to do is define A class that will actually create these iterators for us. So we'll call that class number collection, right, and it's just going to have a vector of integers called numbers. And it's going to have a few methods the first one is going to be the constructor that we'll use to create a new number collection. That's going to look like this and that's going to initialize numbers to numbers. And the next thing we're going to do is define some methods for creating forwards and backwards iterators from this number collection. That's going to look something like this. We're going to say number iterator. We'll call this one, get forwards iterator. We'll say return new forwards iterator. And we'll pass our numbers as an argument there. And the other thing is we're going to return a backwards iterator and we'll call this, get backwards iterator. And we're going to return a new backwards iterator from that with numbers as an argument. Okay, and that's all we need to do to use these classes now. What this is going to look like is we're going to start off by creating our vector of numbers.
>
> **[1:38](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-iterators?u=76281980&t=98)** All right, we'll just use one, two, three, four, five, six, seven for that. And under that, we're going to create a new number collection. We'll call it NC and we're going to pass our numbers vector to that as an argument. And next step, we're going to create our forwards and backwards iterators. So we're going to say number iterator we'll call this fi equals nc.getforwardsiterator and we'll create a backwards iterator by saying number iterator. We'll call it BI for backwards iterator and we'll say nc.getbackwardsiterator. And now that we have those what I'm going to do is just copy and paste the test code from the finished state of the exercise files. So all that's going to do is just loop through the vectors, print out what they contain, et cetera. And then at the bottom here, what we're going to do is just delete our iterators that we created so delete fi, delete bi. All right, and let's run our code. Oops it looks like I forgot an equal zero somewhere. There we go. That needs to say equal zero. That should say it equals zero. Let's try it again. And there we go.
>
> **[3:12](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-iterators?u=76281980&t=192)** So we see that the output here is exactly what we'd expect except now the logic for actually iterating through the collections in different ways is contained inside its own class. Right? Because normally what we would have had to do if we wanted to iterate through our numbers collection here would be to actually have the current position variable here as well as all that logic contained in the client code which can get very messy as you might I'd imagine. And the same thing that we did for the forward iterator and the backwards iterator would be true if we wanted to create another iterator for doing something like iterating through our elements in a random order. And that might be a fun sort of challenge project for you to work on in your own time if you're interested.

> [!info]- Semantic Content
>
> **Exercise Files:** exercise files (1)
> **Analogies:** imagine (1)
> **Speakers:** - [narrator] (1)

#### [The Memento pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-memento-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-memento-pattern?u=76281980&t=0)** - [Instructor] Okay. It's time to take a look at our next pattern which is called the memento pattern. The memento pattern is used when we need a way to prevent most parts of our program from modifying some saved object until we say so. As an example of this, earlier in the course when we looked at the command pattern, we used the example of a hypothetical drawing program. Now, if we wanted to give users the ability to undo their actions in a program like this, what we'd need is some way of freeze-drying the different states that the canvas had been in, in order to allow us to restore those states. And we'd want to prevent other objects in the program from tampering with these saved states, right? Think about how frustrating it would be if you clicked Undo and the state that it went back to was a little different from the one you actually had before. So the memento pattern is used as a way to temporarily freeze data in our programs and then unfreeze it later on when we need to actually use it again. Essentially what the memento pattern does is it makes sure that certain pieces of data in our programs are only modified when they're supposed to be modified. So that's the basic goal of the pattern. Here's a more concrete description of how this pattern works using a UML diagram. First of all, we're going to create a memento interface that acts as a sort of snapshot, right? This interface allows other parts of the program to get basic information about the snapshot. Such as a name and a time for the snapshot. Although these things aren't even a necessity. But what the memento interface does not allow
>
> **[1:34](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-memento-pattern?u=76281980&t=94)** is for any other part of the program to make changes to the data it contains. So once we have this memento interface, the client code then needs to know how to save or freeze data into a memento. As well as how to unfreeze the data that it contains. So if we're implementing a drawing program, whenever some sort of change is made to the canvas, right? If the user draws a shape, for example, the program will generally freeze the previous state of the canvas into a memento object, right? This will usually be done with some sort of create memento method or save method. And then if the user wanted to undo that action, what the program would do would be to unfreeze that memento and restore the previous state to the canvas, right? Where you could actually change it again. And this would be done here using some sort of restore state method of our canvas state class which we would then pass a memento as an argument. And lastly, the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the program, the history mechanism, for example, would only interact with these canvas state mementos through the memento interface we created. Right? So it wouldn't be able to make any kind of changes to those snapshots that we've taken of our state.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Analogies:** for example (2), such as (1)
> **CLI Commands:** make (2)
> **Env Vars:** uml (1)
> **Cross-References:** earlier in (1)
> **Definitions:** is called (1)
> **Speakers:** - [instructor] (1)

#### [Creating a Memento class](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-a-memento-class?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-a-memento-class?u=76281980&t=0)** - Now that we've learned the basics of the memento pattern. Let's take a look at an example of what this pattern is going to look like in code. For this, we're going to be looking at the example that I mentioned earlier of implementing undo functionality for a certain theoretical drawing program, using the memento pattern to save the canvases state at different points in time. So here's what this is going to look like. For our starter code, we just have a basic canvas class that keeps track of the different shapes we've added. This is pretty much the same starting code we used for our command pattern example back near the beginning of the course. What we're going to do now is we're going to start off by creating a canvas memento class. And the memento class is going to look like this. We're going to say class canvas memento. And then we're going to designate this canvas class as a friend class. That means that only the canvas class will have access to the private members of our canvas memento class. And you'll see why that's important in just a minute here. So we're going to say friend class canvas. And that means we're going to have to forward declare canvas up here. So we'll say class Canvas, there we go. OK and, the next thing we're going to do is give this a constant vector of strings, which is going to contain the shapes, right? And we designate this as constant so that nobody can actually change it, right? That's kind of the purpose of a memento, is that we don't want there to be any way for classes other than the one that actually works with these things,
>
> **[1:35](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-a-memento-class?u=76281980&t=95)** to be able to modify it. And that shapes is a private variable, which means that only the canvas class now will have access to it. So for public, all we need to do now is to find a constructor. We're going to say, vector string. I'm going to call it shapes. And that's going to initialize shapes as shapes. Oops and we should call this canvas memento, shouldn't we? And one more thing. There we go.

> [!info]- Semantic Content
>
> **Definitions:** means that (2), is a  (1)
> **CLI Commands:** find (1)
> **Exercise Files:** starter code (1)
> **Speakers:** - now (1)

#### [Using Memento classes](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-memento-classes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-memento-classes?u=76281980&t=0)** - [Instructor] So that's our canvas memento. Pretty straightforward. So the only other thing we have to do now to implement undo functionality in our canvas, is add a vector of CanvasMementos to our Canvas class, as well as an undo method that will use the mementos to restore the old state to the canvas. Here's what I mean by that. We're going to first add a vector here of CanvasMemento pointers of CanvasMemento pointers and we're going to call that oldStates, and then inside our addShape method, we're going to actually incorporate this Memento thing here by saying oldStates.push_back. by saying oldStates.push_back. We're going to add a new element to our oldStates, and we're going to create a new CanvasMemento with the shapes that are currently in the Canvas' shapes member variable here. All right, and now that we've done that, we're going to add an undo method to our Canvas. We're going to say void undo, We're going to say void undo, and we'll say CanvasMemento previousState = oldStates .back. and we'll say CanvasMemento previousState = oldStates .back. We're going to say oldStates.pop_back We're going to say oldStates.pop_back to delete that state that we just undid, and then we're going to say shapes = previousState. Oops, that should be previousState, not previousStates.
>
> **[1:35](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-memento-classes?u=76281980&t=95)** We're going to say previousState, shapes, and then we're going to say delete previousState. Okay. So what we're doing here is we're just looking one back in the CanvasMemento vector that we have here, and using that memento to restore the old state and using that memento to restore the old state to our canvas. Okay, and one more thing that we're going to do here, is we're going to add a destructor that will just delete all of the old states when our Canvas is deleted. We're going to say destructor Canvas, and then inside here, we're going to just loop through all of our pointers. I'm going to say auto p oldStates. I'm going to say auto p oldStates. Delete P and then we'll call oldStates.clear as well. Delete P and then we'll call oldStates.clear as well. All right, so now we move on to the client code, and for the client code, I highly recommend that you just copy and paste that from the finished state of the exercise files, because it's a little bit lengthy, and we're going to paste it here and it'll look just like that, and let's remove this "Hello, World" thing as well, and let's run our program and see what it does. Okay, so we see that our program works just like we'd expect it to, right? We add a bunch of shapes. We print out all the shapes. We call canvas undo, which goes back one step, right? So that's what we're seeing here, and then we print it out,
>
> **[3:12](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/using-memento-classes?u=76281980&t=192)** and then we add some more shapes and call undo, and we see the result of those actions here, and once again, the importance of this memento pattern is that it just gives us a nice, secure way to store the old states and data to store the old states and data that we don't want to be modified.

> [!info]- Semantic Content
>
> **Code Identifiers:** oldstates (12), previousstate (6), push_back (2), pop_back (2), addshape (1)
> **Analogies:** just like (2)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)

#### [The Null Object pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-null-object-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-null-object-pattern?u=76281980&t=0)** - [Instructor] Okay, let's move on to the final behavioral design pattern of this course, the Null Object pattern. The Null Object pattern is used when we need to define some sort of default behavior for an object so that other objects can interact with it without worrying about whether it exists or not. Okay, so let's imagine for a minute that we're writing some sort of class that implements some sort of process, right? And we want to allow whatever code is using this class to write the progress of this process to a variety of different places, right? So maybe we want to write the results to the console, maybe we want to write it to a file, to an API, or so on. Or if they didn't care about the results, they could simply specify no logger at all. So at this point, the program would probably look something like this. We might have a Logger interface with a log method that our code could call to write some result to whatever the client's chosen destination is. And what we do then is we'd have a number of subclasses that would implement this interface, right? So we might have a ConsoleLogger class, a FileLogger, an APILogger class, et cetera. So far so good, right? The client code can simply provide an instance of any of these classes depending on where it wants the results to be logged to. The issue here though is that as we stated before there's a possibility that the user might not want to specify any logger here at all, which would mean that the logger would be NULL. And what this means is that everywhere we want to call logger write, we'd have to wrap it in an if statement
>
> **[1:34](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/the-null-object-pattern?u=76281980&t=94)** and check to make sure that logger isn't NULL, which obviously is a bit of a drag. So in general, this situation occurs whenever it's possible that an object doesn't exist, right? If we're expecting that the object might be NULL, this logic generally makes its way into the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our logic for interacting with that object. And the result of this is that you have at least one, you know, if object check somewhere in each piece of code that interacts with it. So that's the issue. Here's the basic idea of the Null Object pattern. Basically, instead of having null checks all over our client code, what we do is we define what's called a Null Object, which provides the same methods and so on, but essentially does nothing, or implements some sort of safe default behavior. So in the case of our Logger example, what we do is we would define a NullLogger, which would simply do nothing when it's write method or log method, or whatever we end up calling it was called. And this would allow the client code to interact with it like normal without worrying about whether the object is actually there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Env Vars:** null (3), api (1)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Analogies:** imagine (1)
> **Speakers:** - [instructor] (1)

#### [Creating Null Object classes](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-null-object-classes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-null-object-classes?u=76281980&t=0)** - [Instructor] All right, now that we've learned the basics of the null object pattern, let's take a look at an example of what this pattern might look like in code. For this we're going to use the example I talked about earlier of allowing the client code to specify different ways of logging data. So if we look at the starter code for this video, we're going to see that we have this lager interface here, as well as these different implementations of it, and then we have this SomeTask class, right? Very creative name, that in theory would perform some sort of work and write the progress of that work to whatever type of logger the user specifies when they create the task. Now, the problem here is that the user might not want to specify any logger, so they just call the default constructor of our SomeTask class, which would mean that the logger would be a no pointer. And we therefore have to check for this whenever we call our loggers log method as you can see here. All right, and this isn't too bad but it is a bit of a pain. So what we can do instead, using the null object pattern, is we can define a null logger that simply does nothing, right? We have our console logger that logs to a console. We have our file logger, we have our API logger. We're going to just define a null logger that does nothing. And here's what this null longer is going to look like. It's actually going to be very simple, we're just going to define a log method. It's going to look exactly like the other methods on our other logs, except for the body, the body of this method is just going to be empty, okay?
>
> **[1:40](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/creating-null-object-classes?u=76281980&t=100)** So basically, just to find something that we can call that doesn't really do anything in this case. There will be cases where you'll want your null class to actually do something, we'll see some examples of that shortly, but in this case, it's simply does nothing.

> [!info]- Semantic Content
>
> **CLI Commands:** find (1)
> **Env Vars:** api (1)
> **Exercise Files:** starter code (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Setting null objects as the default](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/setting-null-objects-as-the-default?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/setting-null-objects-as-the-default?u=76281980&t=0)** - [Instructor] All right. So now that we have our NullLogger, what we need to do is modify our SomeTask to make the NullLogger the default value for this logger member variable. All right, so all we really need to do here is create a SomeTask constructor with no arguments, and inside here we're just going to say logger equals new NullLogger. All right. And now that we have that, we don't have to actually check if our logger exists every time, because it will always exist provided that the client code doesn't do something like pass in a null pointer for the logger. All right. So we're just going to remove our if statements and save it. And for our clients code what we're going to do is just create different loggers. So we're going to say SomeTask, task one. And for this one, we're just going to pass a ConsoleLogger . All right, we're going to have SomeTask, we'll call this task two. And to this one, we'll pass a new file logger. And our FileLogger class expects us to pass in a file name here. So we'll just say something like logs.txt, and then we'll have our SomeTask task three, which we'll use an API logger, and it'll send it to something like logging [site.com/api/logs](https://site.com/api/logs) something like that.
>
> **[1:32](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/setting-null-objects-as-the-default?u=76281980&t=92)** Normally this would be where we put the address of our [REST](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) API. And last but not least, we're going to define a task without a logger which will be task four. And for this one we'll simply define it like that. So now if we call task.execute on all of these. Right? So if we say task1.execute, task2.execute, task3.execute, and task4.execute, and run our code,
>
> **[2:12](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/setting-null-objects-as-the-default?u=76281980&t=132)** we're going to see that we get stuff logged out to the console. Well, all of these are logged out to the console. Although in the real world we'd have our FileLogger actually write this stuff to a file instead of the console, but just for the sake of demonstration, this is what I did. So we see that the ConsoleLogger is working, the FileLogger is working. The API log is working. Oops, we didn't delete this hello world thing here. And we don't see anything printed out for the NullLogger because that's exactly the point. All right. So that's how the Null pattern works. We're going to see another example of this shortly, just of how the null object pattern works in a different context. And in that case, this methods will actually do something without doing something. You'll see what I mean when we get there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Env Vars:** api (3), rest (1)
> **File Paths:** logs.txt (1)
> **CLI Commands:** make (1)
> **URLs:** [site.com](https://site.com) (1)
> **API Endpoints:** /api/logs (1)
> **Speakers:** - [instructor] (1)

#### [Project: Create an instant replay program](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-an-instant-replay-program?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-an-instant-replay-program?u=76281980&t=0)** - [Instructor] Okay, well, we just learned about three more [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) and we also saw some individual examples of each of them. What we're going to do now, just like in the other sections, is take a look at an example that combines some of them. And we're not going to be doing all of them here, again, since that would take us a lot of time in order to actually come to the point where our program would be the size that needs it, but what we're going to do is we're going to start off, again, mixing two of the design patterns together, and then we're going to incorporate the third pattern in a challenge solution video. So the program we're going to be writing here is going to be an extension of the drawing program that we saw when we talked about the memento pattern. And we're going to start off by mixing this example together with the iterator pattern that we saw. And the end result is going to be a program that will allow users to do a sort of instant replay of the changes that have been made to a given canvas in our drawing program. So in other words, we stored up the history of a canvas. Wouldn't it be nice if we could use that and actually just have a feature in our program where we could watch those shapes being drawn over again? So that's the idea anyway, let's see how to implement that here. So we're going to start off actually by copying and pasting a lot of this code from our memento pattern example, and you can find this in the finished exercise files for that video if you don't have it on hand. We're going to copy that and paste it here. And now that we have that, we're going to make a few changes to this code. The first thing we're going to do is we're going to add a canvas iterator interface. So here's what that's going to look like.
>
> **[1:36](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-an-instant-replay-program?u=76281980&t=96)** Just up at the top here, we're going to say class CanvasIterator. And it's going to have a few public methods. The first one is just going to be a virtual destructor that we're going to need because of how we're going to be extending this class. And then we're going to have a next method and an isFinished method, just like we saw in the examples we looked at with the iterator pattern. So we're going to say virtual and this is actually going to return a canvas memento and this pattern is going to be called next. Here we go. And we're going to have a virtual boolean isFinished. We got to move the declaration for CanvasMemento up above that. So now that we have our canvas iterator interface, let's define an implementation of that and we'll call it forward iterator. So we're going to say class ForwardsIterator public CanvasIterator, and what we're going to be using this class for is for allowing ourselves to replay the changes that a user has made to a given canvas. And to save ourselves a little bit of time, what we're going to do is go over to the finished code from the iterator pattern, which you can find in the exercise files. And we're just going to copy the logic from our ForwardsIterator there. We're going to have to make a few changes to it, but this is the basic logic that we're going to be using here. Just the same.
>
> **[3:08](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-an-instant-replay-program?u=76281980&t=188)** So the first change is that instead of having a vector of integers, this is going to be a vector of canvas memento pointers, and we'll call this history. And likewise, oops, it looks like I called it ForwardsIterator, so let's change that to ForwardsIterator. And then we need to change this int here to a canvas memento pointer as well, change that named history, and change this to history, history. And then to make it match the interface here, we're going to change it from int next to CanvasMemento pointer next. We're going to change this to history and we're going to change this to history as well. Okay. And this just needs to be a canvas memento pointer as well. And the next thing we're going to do is this: our canvas class is currently taking care of all of its own history management, so what we're going to do actually is we're going to break this history, tracking out into its own class. And what this will allow us to do is to sort of take the history from one canvas and plug it into another class. And as you'll see that'll allow us to create the canvas replay class that we're talking about. So here's what this is going to look like. We're going to create a new class called history, and this is just going to be an interface. And it's going to have two methods. One is going to be a virtual void called addState, that'll just allow us to push whatever changes we've made
>
> **[4:43](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-an-instant-replay-program?u=76281980&t=283)** to a canvas into this history here, and this is going to take CanvasMemento called newState. And under this, we're going to have a virtual CanvasMemento method called undo. What this is going to do is it's going to return a canvas memento representing the previous state of the canvas. So we have our history interface. Now let's create an implementation of it. This implementation is going to be called canvas history, so class CanvasHistory, I'll say public History. And this is going to contain a vector of canvas mementos, which is going to be the old states that we've saved to the history, so we'll say oldStates like that. And then, for the public methods, the first one we're going to define is just a destructor for deleting all of the old states when the program is over or when the canvas history is destroyed. So we're going to say CanvasHistory, then we're going to loop through all the pointers in our states and delete them, and then we'll call oldStates clear, and that's our destructor. Next step, we have our addState method, which is going to look like this: CanvasMemento newState, I'm going to say override, and all we're going to do in here is just say oldStates.push newState. And then for the undo method,
>
> **[6:22](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-an-instant-replay-program?u=76281980&t=382)** we're going to say CanvasMemento undo override, and we're going to push the old states back. So we're going to say oldStates.pop back. We're going to get the last state in our vector, which will be the previous state of the canvas. So we're going to say CanvasMemento previousState equals oldStates.back, and then we're just going to simply return that previous state. And the last thing we're going to do is define a method called get forward iterator. So we'll say ForwardIterator, getForwardIterator, and we'll say return new ForwardIterator oldStates. Oh, that should be ForwardsIterator and ForwardsIterator. And now that we've implemented this canvas history, what we're going to do next is we're going to revector our canvas class to actually use this history class instead of its inbuilt history class that it already has. So what we're going to do, we're going to remove our oldStates thing here and replace that with a history pointer. And then for our constructor, what we're going to allow ourselves to do is pass a history instance into this class. So we're going to say history history, and we can remove this destructor here,
>
> **[7:56](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-an-instant-replay-program?u=76281980&t=476)** since we don't have our old states to delete here, we're doing that in our history class up above. And then for our add shape method, we're going to do something a little different. We're going to push the new shape onto our shapes vector, and then after that, we're going to say history addState, and we're going to add a new canvas memento to that, containing the current state of our canvas. And then here for our previous state, instead of saying oldStates.back, we're going to say history undo like that, We don't have to worry about our old states here, either, and we're just going to set the shapes to the shapes of the previous state. And we're actually going to remove this previous state thing since we don't actually need to delete it ourselves. And for clear all, we want to actually do the same thing as we did in addShape, after we clear our shapes, we're going to call history addState with the new state, and we'll leave getShapes as is. Now that we've revectored our canvas class, the last thing we're going to do is we're actually going to create this canvas replay class that will be responsible for replaying the history of another class. So as it happens because of the work we've done with all our other classes, this is actually going to be very simple. Here's what it's going to look like. We're going to say class, we'll call this ReplayCanvas. It's going to have a vector of strings
>
> **[9:28](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-an-instant-replay-program?u=76281980&t=568)** just like a normal canvas called shapes. It's going to have a forward iterator called historyIterator. It's going to have a constructor that allows us to pass the history from another canvas into it. So we're going to say CanvasHistory history, and what we're going to do is we're going to get the iterator from this canvas history and set it on our member variable here. So we're going to say historyIterator equals history getForwardsIterator. After that, we're going to say void replay. This will be the method that we call to actually trigger the canvas replay. And all this is going to do is just say, so while historyIterator is finished, we're going to get a pointer to the next state. So CanvasMemento nextState equals historyIterator next,
>
> **[10:34](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-an-instant-replay-program?u=76281980&t=634)** we're going to say shapes equals nextState shapes. We're going to print out the shapes, so we'll say, cout and something like the shapes are now, and then we'll loop through all our shapes and print those out. So we'll say auto shape shapes cout, and we'll just print the shape, as well as a comma after that. And then we'll print a new line just to keep things cleaner. So that's our replay method. And we're going to have to add this replay canvas class as a friend class to our canvas memento. So we're going to say friend class ReplayCanvas, we're going to have to just add a forward declaration for replay canvas up here, and a semicolon after our class, and then for the client code, I'm just going to copy and paste that as I've usually done from the finished state of the exercise files for this video. Feel free to do the same. And it's going to look like this. We're going to create a new canvas history. We're going to create a new canvas with that history, right? We pass that into the constructor. And then we're going to add a few shapes to the canvas. We're going to watch the replay. We're going to create a replay canvas with the same history that we passed to our canvas here. And then we're going to call replay canvas replay. And that should print out each of the individual states from our canvas.
>
> **[12:08](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/project-create-an-instant-replay-program?u=76281980&t=728)** So let's run this code and see what happens. Okay, so there we have it. We see watching replay, we see the shapes are now rhombus, shapes are now rhombus, triangle, rhombus, triangle, square, rhombus, triangle, square, circle. So it's stepping through each state in the history of our canvas. So hopefully you've enjoyed this example. Again, it combined the iterator pattern with the memento pattern.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) (2)
> **Code Identifiers:** oldstates (8), addstate (4), historyiterator (4), newstate (3), isfinished (2)
> **CLI Commands:** make (3), find (2)
> **Exercise Files:** exercise files (3)
> **Analogies:** just like (3)
> **Cross-References:** we talked about (1)
> **Definitions:** in other words (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Apply the Null Object pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/challenge-apply-the-null-object-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/challenge-apply-the-null-object-pattern?u=76281980&t=0)** - [Instructor] Okay, so now that we've seen an example of how the iterator and momento patterns can fit together, it's time for a challenge. Your challenge is going to be to incorporate the null object pattern into this code alongside the other patterns. Basically, what we want to do is allow the client code to pass in a null object version of this history interface that we created here and pass that into our canvas class in such a way that it doesn't actually do anything, but it also doesn't disturb the normal functioning of our canvas class, right? We should still be able to call all of our methods and it should still work without causing any errors or anything like that. Now the main rule here is that you're not allowed to modify any of the existing classes that we have here. You're only allowed to modify the client code and add another null class, right? So it's going to look something like this. We'll just say class, null history, public history. You're only allowed to edit this class. So this challenge should take you about 10 to 15 minutes. Once you've finished it, feel free to move on to the next video where I'll show you how I would go about solving this.

> [!info]- Semantic Content
>
> **Cross-References:** next video (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Apply the Null Object pattern](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-null-object-pattern?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-null-object-pattern?u=76281980&t=0)** - [Instructor] Okay, hopefully you've given the challenge a try. Now let's take a look at one possible solution. So basically what I did here with the null history, was based on the idea that in order for a null history to really be believable and not disturb the normal functioning of the canvas, it has to store some sort of canvas memento so that when we call history undo in our canvas, nothing will actually happen, right? So if our null history thing just returned a null pointer for this, that would definitely disturb the normal functioning of our canvas. Let me show you what I mean by that. Basically, in order to make the null history thing work, what I did was I gave it a canvas memento member variable called state. And basically what this null history class is going to do, is when the canvas class calls add state, it's simply going to set whatever canvas memento we pass there to this member variable, and whenever we call undo, it's just going to return this member variable. That might not make complete sense yet, so let me show you what that's going to look like. First, let's have our constructor, and inside our constructor we're actually going to set the initial value of this state to just an empty canvas memento. Just like that. And then for our add state method, that's going to look something like this. It's going to take a canvas memento pointer as an argument, new state,
>
> **[1:34](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-null-object-pattern?u=76281980&t=94)** and it's simply going to set this state variable to that new state, all right? So state equals new state. And then for our undo method, we're going to say canvas memento, undo. And we're going to say return state, like that, all right? So we're simply just going to return whatever the last state was that the canvas added to our history. And that's really all we need to do to implement a null history in this state, since from the point of view of the canvas, this null history will actually do nothing, all right? So let's go down and do the client code here, and we're just going to change this canvas history thing to null history. Null history. And we're passing that into the canvas, so the canvas is now using this null history as its history. All right? And we're adding some shapes here. And instead of doing the replay, what I'm going to do again is just copy and paste the finished state of the code from this video. And that's going to look like this. So basically what we're doing here is we're just printing out all the shapes, we're trying to call canvas undo, and we're printing out the shapes again to see what happens there, all right? So if we run our code, we're going to see that after adding all our shapes here, we have them all inside the canvas, we're trying to call canvas undo, and we see that after we've called that,
>
> **[3:10](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/solution-apply-the-null-object-pattern?u=76281980&t=190)** we still have the same shapes. So essentially this null history from the point of view of our canvas is doing nothing. And one last thing I want to point out here is that the null object pattern doesn't necessarily mean that the class actually does nothing as we've seen here, right? We've seen an example with our logger, where the null object class is literally just going to have an implementation of a method with nobody whatsoever, but that's not always the case, right? As we've seen here, sometimes we have to put a little bit of thought into our null objects to make it look like they're doing nothing, when actually they're doing a little bit of something behind the scenes.

> [!info]- Semantic Content
>
> **CLI Commands:** make (3)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-plus-plus-design-patterns-behavioral/next-steps?u=76281980&t=0)** - [Shaun] Well, we've come to the end of this course and we've covered quite a few topics. We covered the 12 different behavioral [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) in C++. We learned how to apply each of them and we saw several examples that incorporated these patterns. So, now that you've completed this course you might be wondering where to go next? Well, there are several things that I would recommend for you. First and foremost, I'd recommend that you immediately start practicing everything you've learned here in your own day-to-day programming. This will allow you to apply your knowledge to solve your own problems which, obviously, is a very important thing. And besides that there are also other courses in the library that would complement this one nicely. In particular, Emma Chu Stone's course on creational design patterns in C++ would be a fantastic next step for you to expand your object oriented programming skills, if you haven't seen it already. Another possible next step too, if you're looking for a good book on the topic would be a book called Design Patterns: Elements of Reusable Object-Oriented Software. This book is where most of the design patterns that we've learned about in this course originated. So, if you want an even more thorough treatment of this topic, be sure to check that book out. Well, thank you so much for watching. If you want to get in touch with me, please don't hesitate to connect with me on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md). I'm on LinkedIn quite often and would love to hear any questions or comments from you. Or if you just want to say hi, that's okay too. Well, thanks again, and I hope you enjoyed the course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) (4), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2)
> **Cross-References:** we covered (1)
> **Definitions:** is a  (1)
> **Speakers:** - [shaun] (1)


## Instructor

- [Shaun Wassell](../../Instructors/Software%20Development/Shaun%20Wassell.md)

## Resources

- Exercise files available

## Skills Covered

- Software Design Patterns
- C++

## Path Context

### In [Master C++](../../Paths/DevOps/Learning%20Paths/Master%20C%2B%2B.md)
← [Test-Driven Development in C++](Test-Driven%20Development%20in%20C%2B%2B.md) | **4 of 6** | [C++ Design Patterns- Creational](C%2B%2B%20Design%20Patterns-%20Creational.md) →

## Appears In

- [Master C++](../../Paths/DevOps/Learning%20Paths/Master%20C%2B%2B.md)

## Related Courses

_Courses sharing skills:_

- [C++ Design Patterns- Structural](../Data%20Science/C%2B%2B%20Design%20Patterns-%20Structural.md) — Software Design Patterns, C++
- [Nail Your C++ Interview](../Software%20Development/Nail%20Your%20C%2B%2B%20Interview.md) — C++
- [Web Servers and APIs using C++](../Software%20Development/Web%20Servers%20and%20APIs%20using%20C%2B%2B.md) — C++
- [Learning C++](../Software%20Development/Learning%20C%2B%2B.md) — C++
- [Introducing Functional Programming in C++](../Software%20Development/Introducing%20Functional%20Programming%20in%20C%2B%2B.md) — C++

---

[↑ Back to top](#)