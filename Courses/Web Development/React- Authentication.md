---
type: course
cssclasses:
  - lle-course
  - lle-level-advanced
slug: react-authentication-25660373
url: "https://www.linkedin.com/learning/react-authentication-25660373"
duration_minutes: 255
duration: 4h 15m
level: Advanced
updated: 5/9/2025
learners: 56445
skills:
  - React.js
  - Authentication Systems
exercise_files: true
github: "https://github.com/LinkedInLearning/react-authentication-6035050"
thumbnail: "https://media.licdn.com/dms/image/v2/D4E0DAQEKpKB3OkEGkw/learning-public-crop_675_1200/B4EZZAfXucGYAc-/0/1744838687924?e=2147483647&amp;v=beta&amp;t=hP9daVuR0FW7rOnO0vVLWrnbFVvwRIKD6qB52Ksy-_U"
linkedin_topic: Web Development
learning_paths:
  - '[Explore React.js Development](../../Paths/Software%20Development/Learning%20Paths/Explore%20React.js%20Development.md)'
  - '[Explore App Development with the MERN Stack](../../Paths/Database%20Management/Learning%20Paths/Explore%20App%20Development%20with%20the%20MERN%20Stack.md)'
  - '[Improve Your React Skills](../../Paths/Web%20Development/Learning%20Paths/Improve%20Your%20React%20Skills.md)'
prev_courses:
  - '[Building Modern Projects with React](Building%20Modern%20Projects%20with%20React.md)'
  - '[React Essential Training](React%20Essential%20Training.md)'
  - '[React- Testing and Debugging](React-%20Testing%20and%20Debugging.md)'
next_courses:
  - '[React- Server-Side Rendering](../Software%20Development/React-%20Server-Side%20Rendering.md)'
  - '[Node Js Essential Training](Node%20Js%20Essential%20Training.md)'
  - '[React- Software Architecture](React-%20Software%20Architecture.md)'
path_nav: '[{"path":"Explore React.js Development","position":7,"total":8,"prev":"Building Modern Projects with React","next":"React- Server-Side Rendering"},{"path":"Explore App Development with the MERN Stack","position":2,"total":13,"prev":"React Essential Training","next":"Node Js Essential Training"},{"path":"Improve Your React Skills","position":6,"total":9,"prev":"React- Testing and Debugging","next":"React- Software Architecture"}]'
path_count: 3
tags:
  - course
  - topic/software-development
  - topic/web-development
  - topic/database-management
  - skill/react-js
  - skill/authentication-systems
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Web%20Development/React-%20Authentication.md)

![React: Authentication](https://media.licdn.com/dms/image/v2/D4E0DAQEKpKB3OkEGkw/learning-public-crop_675_1200/B4EZZAfXucGYAc-/0/1744838687924?e=2147483647&amp;v=beta&amp;t=hP9daVuR0FW7rOnO0vVLWrnbFVvwRIKD6qB52Ksy-_U)

# React: Authentication

> User authentication is a critical part of virtually every website, yet few web developers know all the details behind how it works and how to use it to provide a good user experience. In this course, instructor Shaun Wassell dives into the details behind user authentication in the context of React and how to incorporate user authentication into React applications. Shaun begins with some definition

> [LinkedIn Learning](https://www.linkedin.com/learning/react-authentication-25660373) | 4h 15m | Advanced | 56K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Secure authentication practices in React](#secure-authentication-practices-in-react)
  - [The structure of this course](#the-structure-of-this-course)
- [**1. User Authentication Basics**](#1-user-authentication-basics) (16 videos)
  - [What is user authentication?](#what-is-user-authentication)
  - [Basic project setup](#basic-project-setup)
  - [Creating private React routes](#creating-private-react-routes)
  - [JSON Web Token basics](#json-web-token-basics)
  - [Solution: Working with JSON Web Tokens](#solution-working-with-json-web-tokens)
  - [Adding a sign-up route to the server](#adding-a-sign-up-route-to-the-server)
  - [Generating JSON Web Tokens](#generating-json-web-tokens)
  - [Adding a log-in route to the server](#adding-a-log-in-route-to-the-server)
  - [Adding JWTs to the front-end](#adding-jwts-to-the-front-end)
  - [Parsing JWT data](#parsing-jwt-data)
  - [Adding JWTs to sign-up and log-in pages](#adding-jwts-to-sign-up-and-log-in-pages)
  - [Adding JWTs to the user info page](#adding-jwts-to-the-user-info-page)
  - [Adding an "update user" route](#adding-an-update-user-route)
  - [Adding update functionality to the front-end](#adding-update-functionality-to-the-front-end)
  - [State management for tokens](#state-management-for-tokens)
  - [Adding log-out functionality](#adding-log-out-functionality)
- [**2. Email Verification**](#2-email-verification) (8 videos)
  - [The basic email verification process](#the-basic-email-verification-process)
  - [Setting up an email provider](#setting-up-an-email-provider)
  - [Creating an email provider API key](#creating-an-email-provider-api-key)
  - [Sending emails from Node](#sending-emails-from-node)
  - [Integrating verification tokens](#integrating-verification-tokens)
  - [Adding a "please verify email" page](#adding-a-please-verify-email-page)
  - [Adding a verify email route](#adding-a-verify-email-route)
  - [Creating a verification landing page](#creating-a-verification-landing-page)
- [**3. Resetting Passwords**](#3-resetting-passwords) (5 videos)
  - [The basic password reset process](#the-basic-password-reset-process)
  - [Adding a "forgot password" page](#adding-a-forgot-password-page)
  - [Sending a "reset password" email](#sending-a-reset-password-email)
  - [Creating a "reset password" landing page](#creating-a-reset-password-landing-page)
  - [Adding a "reset password" server endpoint](#adding-a-reset-password-server-endpoint)
- [**4. OAuth**](#4-oauth) (8 videos)
  - [OAuth basics](#oauth-basics)
  - [Setting up OAuth on Google](#setting-up-oauth-on-google)
  - [Adding an OAuth URL route](#adding-an-oauth-url-route)
  - [Creating a "sign-in with Google" button](#creating-a-sign-in-with-google-button)
  - [Adding an OAuth callback route](#adding-an-oauth-callback-route)
  - [Loading user info from Google](#loading-user-info-from-google)
  - [Creating new users from OAuth data](#creating-new-users-from-oauth-data)
  - [Handling successful OAuth authentication](#handling-successful-oauth-authentication)
- [**5. Prebuilt Auth Options**](#5-prebuilt-auth-options) (6 videos)
  - [Why prebuilt auth options?](#why-prebuilt-auth-options)
  - [Authentication with Auth0](#authentication-with-auth0)
  - [Authentication with Firebase](#authentication-with-firebase)
  - [Authentication with NextAuth.js](#authentication-with-nextauthjs)
  - [Authentication with Clerk](#authentication-with-clerk)
  - [Authentication with AWS](#authentication-with-aws)
- [**6. Best Practices**](#6-best-practices) (7 videos)
  - [Using HTTPS and SSL](#using-https-and-ssl)
  - [Salting and peppering passwords](#salting-and-peppering-passwords)
  - [Solution: Adding salt and pepper](#solution-adding-salt-and-pepper)
  - [Never trust the front-end](#never-trust-the-front-end)
  - [Authentication error handling](#authentication-error-handling)
  - [Catching & handling errors](#catching-handling-errors)
  - [Securing your database](#securing-your-database)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps in your React journey](#next-steps-in-your-react-journey)
- [**Appendix**](#appendix) (1 videos)
  - [A tour of Coderpad](#a-tour-of-coderpad)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Secure authentication practices in React](https://www.linkedin.com/learning/react-authentication-25660373/secure-authentication-practices-in-react?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/secure-authentication-practices-in-react?u=76281980&t=0)** - If you've ever seen on the news about how some website was hacked and exposed millions of users' personal information, you're well aware of what the consequences can look like when user authentication isn't done correctly. So with all this in mind, my question to you is what are you waiting for? Let's get your knowledge of user authentication to where you and I both know it needs to be. I'm Shaun Wassell. I'm a senior software developer and tech educator. In this course, you're going to go from a user authentication novice to an expert, and learn virtually all of the most important aspects of user authentication in [React](../../Skills/Web%20Development/React.js.md). Let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (1)
> **Speakers:** - if (1)

#### [The structure of this course](https://www.linkedin.com/learning/react-authentication-25660373/the-structure-of-this-course?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/the-structure-of-this-course?u=76281980&t=0)** - [Instructor] Alright, so in order to help you understand how this course is going to work in a little bit more detail, what I'd like to do is right up front give you the basic overall structure that we're going to be following in this course. Obviously, authentication is a pretty deep topic, so there's lots of things that we're going to be talking about here, and it's important for you to understand how they're going to fit together. So really, first of all, there's two main goals of this course. The first is for you to understand and apply authentication with full-stack [React](../../Skills/Web%20Development/React.js.md), right? So basically at the end of this course, you should be able to confidently add basic authentication to a full-stack React app, which is something that will amaze interviewers, trust me. And the second one here is we're going to develop sort of like a full-stack authentication template that you can use so that in the future when you want to create other applications and know that you're going to need authentication for those, you can just copy this template and not have to set up everything all over again. All right, so those are the two main goals. Let's talk about the basic structure as I said. The first thing we're going to do is talk about the fundamentals of user authentication. So we're going to talk about what user authentication is in the first place, as well as some other key concepts behind user auth. After that, this is a very important part, so do not skip this part. At least, if you want to follow along, I wouldn't recommend skipping it. Project setup, this is where we're going to talk about the basic structure of the project itself
>
> **[1:33](https://www.linkedin.com/learning/react-authentication-25660373/the-structure-of-this-course?u=76281980&t=93)** and how all the pieces fit together. So if you skip this, you might just find yourself a little bit confused as to what all the different pieces of code, what all the files are in the project. After we do that, we're going to take a look at how to add basic user authentication to a full-stack MERN app, right? Now, this is going to be a slightly simplified MERN app. In fact, it's going to be like a MERN app without the M, so an ERN app. Basically what I've done is instead of using [MongoDB](../../Skills/Software%20Development/MongoDB.md), which really has just in the past, complicated things a little bit, I'm just going to use a very simple file-based [JSON](../../Skills/Web%20Development/JSON.md) database in order to store all of our data. This will make things a lot easier as we go through the course, as you'll see. And this is going to include things like how to sign a user up, very important thing there, how to let them create an account. We'll also take a look at, once the user is signed up, how do we log them in and out? We'll take a look at how to create private routes in a React application as well as on the backend. And finally, we'll take a look at the basics of using JSON web [Tokens](../../Skills/Web%20Development/Tokens.md), which really form the beating heart, so to speak, of a lot of modern authentication. All right, and after that, after we've learned all of the basics, we're going to go through a few more very important and I think very beneficial parts of the course. The first is email verification. So we'll take a look at not only how to send emails, but also how to incorporate all of the necessary logic of the email verification flow, right?
>
> **[3:07](https://www.linkedin.com/learning/react-authentication-25660373/the-structure-of-this-course?u=76281980&t=187)** To make sure that a user has signed up with a valid email. We'll also take a look at the flow for resetting passwords and how to implement that. And we'll also take a look at a few other authentication options, right? So now that you know how to implement your own authentication flow, what are some other possibilities that you could use? All right, and last but not least, we're going to take a look at a few best practices for authentication in React. And as I said, with the project setup, it would not be wise to skip this one. There are lots of very interesting techniques in this last section that you'll really want to be familiar with in order to make sure that your users have the maximum security that they deserve. And well, that is the basic structure of the course. So now you know what to expect.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (4), [JSON](../../Skills/Web%20Development/JSON.md) (2), [MongoDB](../../Skills/Software%20Development/MongoDB.md) (1), [Tokens](../../Skills/Web%20Development/Tokens.md) (1)
> **Env Vars:** mern (3), json (2), ern (1)
> **CLI Commands:** make (3), find (1)
> **Prerequisites:** setup (2), set up (1)
> **Exercise Files:** template (2)
> **Definitions:** is a  (2)
> **Speakers:** - [instructor] (1)


### 1. User Authentication Basics

[↑ Back to Table of Contents](#table-of-contents)

#### [What is user authentication?](https://www.linkedin.com/learning/react-authentication-25660373/what-is-user-authentication?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/what-is-user-authentication?u=76281980&t=0)** - [Instructor] Alright, well, let's start off our user authentication journey by learning the very basics. And for that, the first question we're going to look at is, what exactly is user authentication? Well put simply, when users use our applications, user authentication is how we know that they are who they say they are. Now, this is a very interesting and often very tricky topic, and to understand the reason that this is, think about when you interact with someone in real life face to face, right? When you're talking with a friend or a family member, for example, you know that it's them because of the way they look, their voice, their mannerisms, and so on. But interacting with people over the internet is quite different from this, right? Some of you may know someone who's an identical twin, and you may have had this experience when those two people are in the same room, wearing similar clothes, and you have a hard time telling who's who. And sometimes they may even prank you into thinking that one is the other, right? So let's take that a step further. Imagine that everyone on earth is identical, they all look identical, have the same voice, have the same mannerisms, et cetera. You'd have a pretty hard time ever really determining who's who, wouldn't you? And that's actually not a bad metaphor for what it's like to interact with people over the internet as a website, right? What I mean by that is that from the point of view of our website, all of our users are essentially identical, right? Sure, they might have different IP addresses, different browsers, et cetera.
>
> **[1:32](https://www.linkedin.com/learning/react-authentication-25660373/what-is-user-authentication?u=76281980&t=92)** But those pieces of information are shaky at best for identifying our users. At the end of the day, if we were to ask any given user who are you, they could basically answer however they like, right? And we wouldn't really be able to know if they're telling the truth or not, at least not without some clever techniques. And that's where the art and science of user authentication comes in. So once again, our definition of user authentication is that user authentication is simply the technique of verifying that users of our site are who they say they are. So that's what it is on a conceptual level. But the next question is, how exactly does it work? Well, broadly speaking, there are three main strategies or tests that our sites can use to make sure that our users are who they say they are. And we'll call these the knowledge test or knowledge based authentication, the ownership test or ownership based authentication, and finally, the biological test or biological authentication. So let's go over each of these and talk about how they work and what makes them special. The first one here is knowledge-based authentication. And currently, this is probably the most commonly used form of user authentication. It involves making our users prove who they are based on the fact that they know some key piece of information. And this key piece of information is more often than not a password or a PIN. In other words, when the user wants to claim that they're say, [John](../../Glossary/Tool/John%20the%20Ripper.md) Doe, our site will say, okay, well if you're the real John Doe, you'll know John doe's password, what is it?
>
> **[3:07](https://www.linkedin.com/learning/react-authentication-25660373/what-is-user-authentication?u=76281980&t=187)** And if they know it, we'll believe that they are the real John Doe. If they don't know it, then we won't believe them. And this knowledge based authentication can also be something like security questions where we say, okay, if you're John Doe, then what's your mother's maiden name, right? Sure, you've seen that one before. Now obviously, knowledge based authentication has its fair share of problems. Probably the biggest problem is that it's highly reliant on the user actually coming up with a good password. And while we can try and enforce this by forcing the user to use capital letters, numbers, symbols, et cetera, many users still insist on putting things like abc123 or their name or something completely guessable as their password. And in the case of security questions such as someone's mother's maiden name, the street you grew up on or things like that, these answers are generally pretty easy to find within 15 seconds or less on [Google](../../Glossary/Service/Google.md) if you have a specific target in mind. So the next broad grouping of authentication strategies is what we call ownership based authentication strategies. And these involve requiring the user to prove that they have something that only the real user should have. Now, this could be an email address where we send them a code to make sure that they actually own and have access to that email address, or it could be a cell phone where we send a code to their phone number to prove that they have the phone or it could be something like an OTP fob or an OTP app, which displays a code that refreshes automatically every 20 to 30 seconds
>
> **[4:41](https://www.linkedin.com/learning/react-authentication-25660373/what-is-user-authentication?u=76281980&t=281)** to make sure that it can't be guessed by brute force. Now, in general, authentication by ownership is pretty secure, but this too does have its fair share of problems. First of all, some ownership methods such as sending a code to your email address, for example, rely indirectly on knowledge based strategies, right? Since someone other than you can access your email account if they're able to guess your password. Another risk of this kind of authentication is that in the case of a phone or OTP fob, they could either be physically stolen or as many of you and myself have experienced in the past, they can also be lost, which puts the user themselves in a bit of a difficult situation since they can no longer log into anything. Now because knowledge based and ownership based authentication both have their potential vulnerabilities, what many sites recommend or require is that you use something called two-factor authentication, which usually involves a combination of knowledge and ownership. In other words, you both have to know John doe's password and have access to John Doe's phone in order to impersonate John Doe, which makes this a lot more difficult. All right, and lastly, we have the biological strategies for verifying identity, which you might be able to consider a subset of ownership strategies, I suppose. Basically, the biological methods of user authentication rely on certain biological traits such as face recognition, fingerprints, or eye scans, which are usually pretty hard for hackers to fake,
>
> **[6:19](https://www.linkedin.com/learning/react-authentication-25660373/what-is-user-authentication?u=76281980&t=379)** and certainly one would think harder for users to lose. Now, we're not going to be going into much detail on biological authentication in this course, but I wanted to bring it up anyway. This is obviously a very important method of user authentication in the real world, such as getting into your phone by using your fingerprint or your face. And so I just wanted to mention this as another option in some cases. And those are the three main methods of user authentication.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [John](../../Glossary/Tool/John%20the%20Ripper.md) (8), [Google](../../Glossary/Service/Google.md) (1)
> **Analogies:** such as (4), for example (2), imagine (1), it's like (1)
> **CLI Commands:** make (3), find (1)
> **Env Vars:** otp (3), pin (1)
> **Definitions:** in other words (2), is a  (1)
> **Cross-References:** coming up (1)
> **Speakers:** - [instructor] (1)

#### [Basic project setup](https://www.linkedin.com/learning/react-authentication-25660373/basic-project-setup?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/basic-project-setup?u=76281980&t=0)** - [Instructor] All right. Well, now that we've seen the basics of user authentication, let's get started actually seeing what this looks like in a full stack [React](../../Skills/Web%20Development/React.js.md) application. Now, in this video, we're going to take a look at how to set up the project that I've created for you and make sure that everything works for you. So if you check out the exercise files at the beginning state of this video, you should see that you have this project that has a number of things in it. You can ignore most of it really, except for the frontend and backend folders. So the frontend folder, this is a React app that's been created using the Vite library, sometimes pronounced Vite, V-I-T-E, is what the library is. And that basically just sets up a fully functional and very fast React app for you. So that's what you see in the frontend. In the backend, we have a simple [Node.js](../../Skills/Web%20Development/Node.js.md) and express server, right? There's not really a whole lot going on there yet. We will come in here and add some endpoints a little bit later on as we go through our authentication stuff. And if you take a look at this db.js thing, this is how we're going to work with [Databases](../../Skills/Software%20Development/Databases.md) right now. I was thinking of adding [MongoDB](../../Skills/Software%20Development/MongoDB.md) to this course, but ultimately that was just too much setup and too much extra information besides the core information, which is what we're going to be going through here, right? The core information about how authentication works, things like that. So essentially the way that this database works is it just stores all of our data in a [JSON](../../Skills/Web%20Development/JSON.md) object, a [JavaScript](../../Skills/Software%20Development/JavaScript.md) object that is,
>
> **[1:38](https://www.linkedin.com/learning/react-authentication-25660373/basic-project-setup?u=76281980&t=98)** and then when we call this save db function, it's going to write that object to a JSON file. So this will allow us to have some degree of persistence in our application, but it will also greatly simplify the interactions we have with our database and allow us to focus on the core topic of this course. All right, so that's the basics of the frontend and backend. In order to get this started, all you're going to need to do here is, as I said, check out the exercise files for this course. And you're probably going to want to have two separate terminals open here. So you can open those just by going up here, going to terminal, and doing new terminal. The first one you're going to want to have open in the frontend folder, right? And the first thing you're going to want to do here is run [npm](../../Skills/Web%20Development/npm.md) install, that will install all of the dependencies for you. I don't need to do that because I already have them installed. And then you can run the React app by saying npm run dev. And then you're going to open up a new terminal, which you can also do by clicking this little plus button. And you are going to go into the backend directory there, run the same thing, npm install, which will install all of the dependencies. And then you can run that simply by saying node server.js, and that will run that server for you. So you should see both of these running here, and that should be all we really need to do for setup. There's a few other things we might do a little bit later on, such as opening up these ports, but I'll discuss that when we get there. So anyway, that is the basics
>
> **[3:10](https://www.linkedin.com/learning/react-authentication-25660373/basic-project-setup?u=76281980&t=190)** of getting this project set up if you want to follow along. And well, now that we've done this, we can get started seeing how user authentication applies in a full stack React app.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (5), [npm](../../Skills/Web%20Development/npm.md) (3), [JSON](../../Skills/Web%20Development/JSON.md) (2), [Node.js](../../Skills/Web%20Development/Node.js.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **Prerequisites:** install (4), set up (2), setup (2)
> **CLI Commands:** npm (3), node (2), make (1)
> **File Paths:** node.js (1), db.js (1), server.js (1)
> **Tools:** terminal (3)
> **Env Vars:** json (2)
> **Exercise Files:** exercise files (2)
> **Definitions:** is a  (1), we call this (1)

#### [Creating private React routes](https://www.linkedin.com/learning/react-authentication-25660373/creating-private-react-routes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/creating-private-react-routes?u=76281980&t=0)** - [Instructor] Alright, so now that we've got our example project set up, let's take a look at the first thing that we're going to need to do here. And that is we're going to need to create sort of like our own type of [react](../../Skills/Web%20Development/React.js.md) route that will automatically prevent the user from visiting certain pages when they're not authenticated. So you may have noticed that there are these pages that I've created in the starter project, this login page, and this signup page. And there's also this user info page. And basically we only want the user to be able to visit the user info page, for example, when they're logged in, right? Otherwise it wouldn't make any sense because there's no user to display info for. And similarly on the other side, we don't want them to be able to see the login page and signup page when they're already logged in. So in order to do this, what we're going to do is we're actually going to create sort of like this private route component. And if you go into app.jsx, you'll see where the [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) is set up, right? We have three routes, one for each of those pages. So we have a login page route, a signup page route, and a user info page route, which is just the home route, right? So essentially what we want to do here is we want to create another component that we can sort of like replace this route with, and we want to be able to say something like private route path, blah, blah, blah element, right? All of those things are still going to be intact, but then we also want to be able to redirect the user to some URL if the user isn't logged in, let's say.
>
> **[1:36](https://www.linkedin.com/learning/react-authentication-25660373/creating-private-react-routes?u=76281980&t=96)** So here's what this is going to look like. I'm just going to sort of set this up in theory in the app component here, and once we've added all of this, then we'll actually go in and create this component for ourselves. So here's what this is going to look like. We're going to have this redirect, well here, we'll call it redirectPath. And in this case we might want to redirect the user to let's say the login path, alright? And then we're also going to have a prop called something like isAllowed. And this is just going to be either true or false depending on whether the user should be allowed to see this page, right? So eventually we'll actually come in and replace this with whether the user is currently authenticated or not. But right now, just for testing purposes, we'll say false. So again, what we want to happen is if this is allowed thing is false, then when the user tries to visit this path here, it should redirect them to this redirectPath instead of showing this element. That's kind of how this is going to work here. And let's also just kind of reorganize this to make it a little bit easier to tell what's going on and avoid that horizontal scroll bar. And now that we've done this, let's actually create this thing. So here's what this will look like. Inside our source directory, we're going to create a new file, we'll call it, of course, PrivateRoute.jsx. And now that we've done that, here's what this is going to look like. First of all, we're going to need a few things from React Router Dom that's already installed in the front end project here.
>
> **[3:10](https://www.linkedin.com/learning/react-authentication-25660373/creating-private-react-routes?u=76281980&t=190)** So we shouldn't need to install it. And we're going to say import Navigate, right? This is something that we can actually use to redirect the user as you'll see. And then we will also have this outlet thing. You'll see what that looks like in just a minute, and we're importing both of those from React Router Dom. So the next thing, we're going to just specify the basic component structure. So we're going to say const PrivateRoute equals, and here's going to be the props. We're going to have the isAllowed prop, and we'll also have the redirectPath prop, and we'll just leave both of those without any kind of default. You could add a default if there was one that made sense, right? If you wanted to make the redirect path automatically log in, or if you wanted to make isAllowed automatically false, feel free to go ahead and do that, but I'm just going to leave that up to you. And then what we're going to do is we're going to return here different things depending on what the value of this isAllowed prop is. So if the user should be allowed to visit that page, then what we're going to want to do is we're going to want to return whatever that page should be. Now, that's what this outlet thing is for. So we're going to say return outlet, but only if isAllowed is true. So we'll just use a turnery operator here. So we'll say return isAllowed Outlet. And if isAllowed is false, this is where we're going to return this navigate thing. Basically, this is a declarative component version of the navigate function that we can get
>
> **[4:47](https://www.linkedin.com/learning/react-authentication-25660373/creating-private-react-routes?u=76281980&t=287)** from the use Navigate hook in React Router Dom. We'll take a look at that a little bit later, if you don't know what that is. But essentially when this thing is displayed, it will automatically redirect the user to whatever URL we set. So we can say Navigate to equals, and we're going to pass that redirect path thing right on through. And then we'll say replace, right? That basically tells this Navigate component to replace whatever the current path is with this new path here. And that should be all we really need to do there. So let's make sure we export this thing. We're just going to say export default, PrivateRoute. You can either export that as a named export or as the default as I'm doing here. And then we'll go back to app.jsx. We're going to import this thing. And actually, let's make that a named export just to make it more consistent with everything else and really only for that reason. So we will say export cons private route, and now we can say import private route. That one was just completed for me by my IDE. And there's actually one last thing that we have to do here in order to get this to work because the later versions of React Router don't like having anything but this specific route component inside this routes component here. What we have to actually do is we have to create a route that will display this private route. It's a little bit of a pain, so just, you know, bear with me here. We're going to say Route and then this one's going to say element equals private route. And then we're going to keep the redirect path and isAllowed.
>
> **[6:21](https://www.linkedin.com/learning/react-authentication-25660373/creating-private-react-routes?u=76281980&t=381)** So I'm actually just going to cut those and put those up here like so, and we'll just rearrange that a little bit. There we go. And now that we've done that, we're going to put inside this the original route. So that's going to be where we have this path and element. So we're going to say route, and we're going to paste those things that we had there. So path and element, and those are going to go inside here like so. And then we just have to close off this private route component and add another curly brace and triangle bracket there. And sorry about this, we need to just remove that private route. We're going to move this route into there. And there we go. So that hopefully that makes sense there. What we just did so far. We're just going to remove all of the extra sort of artifacts from before, and this is how we get that private route thing in there. So essentially, we have this set up as a nested route. Don't worry too much about the details here. This is just what we have to do in order to get this to work. And now, what you'll see is if we set isAllowed to false and open up our application in our browser, and by the way, you can do that here by opening up your terminal, going to ports, and then clicking on this little world icon here, open in browser, that will open it up there for you. And sure enough, what you see is that automatically redirects you to slash Log in. I have this other one here that I need to close, but the cool thing is that if we change isAllowed to true now and try and navigate
>
> **[7:55](https://www.linkedin.com/learning/react-authentication-25660373/creating-private-react-routes?u=76281980&t=475)** to the regular page, all right, we'll just remove that like so, sure enough, we're allowed to do that because our private route just acts as an outlet in that case. So anyway, that is the basics of creating a private route. We'll be using this route once we add more extensive user authentication to our app.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (5), [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) (1)
> **Code Identifiers:** isallowed (10), redirectpath (3)
> **CLI Commands:** make (7)
> **Prerequisites:** set up (3), install (1)
> **File Paths:** app.jsx (2), privateroute.jsx (1)
> **Env Vars:** url (2), ide (1)
> **Cross-References:** go back to (1)
> **Tools:** terminal (1)

#### [JSON Web Token basics](https://www.linkedin.com/learning/react-authentication-25660373/json-web-token-basics?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/json-web-token-basics?u=76281980&t=0)** - [Instructor] Okay, so at this point, we've got the basic framework for adding authentication to our app set up, right? We've set up our project and we've created that useful private route component. So now that we have all of those things, we're ready to start adding real user authentication techniques to our app. Now, we talked earlier about the three main classes of user authentication. That is knowledge-based, ownership-based and biological-based authentication. And in this course, we're going to be focusing mainly on knowledge-based authentication, right? That is the techniques that we're going to be using here will be focused on verifying users using only passwords, although there are, of course, plenty of ways to go beyond this. Now, in order to understand exactly how this password-based authentication thing is going to work in our application, it's very important that we talk about something called [JSON](../../Skills/Web%20Development/JSON.md) web [Tokens](../../Skills/Web%20Development/Tokens.md). JSON web tokens, which are commonly abbreviated as JWTs, and sometimes that's pronounced jots, although in this course, I'll just be calling them JWTs, these JWTs form the beating heart of most modern website authentications. So let's take a look at exactly what JWTs are and how they work. First of all, what are JSON web tokens? Well, JSON web tokens basically are unique strings that we give different users of our site after they've logged in. And the idea is that whenever a user wants to interact with our site in some way, right? For example, if they want to update their information
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/json-web-token-basics?u=76281980&t=94)** in our database, instead of having to send their password to our server every time, what they can do is they can simply send us this string instead, right? They can send us this JSON token. Now, JWTs aren't just any string, they're actually an encoded JSON object that contains information about the user. So it might contain things, such as the user's ID, the email address, their basic information, et cetera. Basically anything that will make it easier for our app to decide what to show them and what they're allowed to do. So the next question is, what exactly do JWTs look like? They actually don't look like what you see here. Instead, what we see here is an example of what a JWT might look like. And they're usually quite a bit longer than this one that we see here. I've just shortened this so that we can see it all on one screen and for the sake of simplicity, right? So JWTs, as you can see here, have three separate parts separated by periods. They have a header, a payload and a signature. And each of these sections is an encoded JSON object, that's by the way, encoded in Base64, which is something that we'll have to keep in mind when working with them on our front and backend, as you'll see. So here's what each of these parts is meant to do. The header, basically this tells us the type and assigning algorithm that are used for the token. So this could be something like HS256,
>
> **[3:10](https://www.linkedin.com/learning/react-authentication-25660373/json-web-token-basics?u=76281980&t=190)** that's the most common one to use. And next up, the payload, this is the actual data that the JWT contains. So for example, this could be the user's profile information, or at least part of the user's profile information, as we'll see. Now, the pieces of data in the payload are referred to as claims. And we're going to see how all of this works very shortly. And finally, at the end of the JSON web token, we have the signature. Now, this is where things get interesting and where the actual usefulness of JWTs becomes apparent. You see, the signature of a JWT is used to prove the authenticity of the data that the JWT contains in its other parts. Now, this is actually created by combining the header and payload, and signing those using the specified algorithm and a secret key, which we'll define ourselves on the server. If all of this sounds complicated, you'll see exactly how this works very shortly. But basically what this means is that anyone who has the token can see the information that it contains, unless it's encrypted, of course, we'll take a look at that later as well. But they cannot change the data in the token unless they have the private key, which usually will not be the case unless we're working with this on our server, right? The server should be the only place that has access to this private key. So now that we know the basics of what exactly a JWT is,
>
> **[4:44](https://www.linkedin.com/learning/react-authentication-25660373/json-web-token-basics?u=76281980&t=284)** let's briefly walk through the steps of how JWTs are used in full-stack applications. And this flow is what we'll be implementing throughout the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of this section. So, you know, just keep that in mind. Don't worry about the details quite yet. Step one here is that the user logs in successfully to our application. Now, this will mean that they send along their username and password to our server, and our server will check those. And if the user has sent along the correct password for that username, then the user is considered fully logged in. So if a user is able to log in, right, if they provide the correct password, the next thing our server is going to do is it's going to use a secret key to generate a token containing the user's information. Now, again, only the server knows the secret key, so only the server knows how to sign the token correctly and create that last part of the token, okay? Step three now is that the server will send this token back to the user, followed by step four, which is where the user's browser will hold on to this token, usually by storing it somewhere like in cookies, or session storage, or local storage, all right? And obviously each of these methods of storing the token has its own benefits and drawbacks security-wise. We'll take a look at that a little bit later. But anyway, the next step here, step five, is that whenever the user wants to make a privileged request, right? So for example, maybe the user
>
> **[6:17](https://www.linkedin.com/learning/react-authentication-25660373/json-web-token-basics?u=76281980&t=377)** wants to make a request to an endpoint where they can load their private information or modify their private information in our database. Whenever they want to do something like that, they're going to send this JWT along with their request. And finally, step six is that the server is going to use that secret key again to verify that the token that the client sent is legitimate and hasn't been tampered with. So in other words, it makes sure that the client hasn't tried to make themselves look like another user, by let's say changing the ID property in the token, okay? So that's the basic process of using JWTs. Before we move on, there are a few more things that I want to mention about JWTs. The first thing I want to point out is that when the user sends a JWT to the server, it will usually send the token in a request header that looks like what you see here, right? It will be an authorization header that will usually say like Bearer with a space,, and then it will have the actual token string in there. This is just something you'll need to know when we're parsing the token on the backend, okay? And the next thing that you should know here is that tokens are credentials and they need to be treated with care. So you're never going to want to give anyone else access to your JWT tokens, right? You're never going to want to display them in a public place. And as a developer, you're obviously going to want to make sure that the users' JWTs are as secure as possible.
>
> **[7:52](https://www.linkedin.com/learning/react-authentication-25660373/json-web-token-basics?u=76281980&t=472)** Now, the next thing that I want to point out here is the difference between signing tokens and encrypting tokens. Unfortunately, these two concepts do seem fairly similar to each other at first glance, but there's actually a very big difference between them. You see, signing tokens, this proves that the information is intact and hasn't been tampered with, right? This is something that the server does. This proves that the server is the one that generated the token, since the server is the only one who has access to the secret key that can be used to do this. Now, this is very different from encrypting tokens, since encrypting actually hides the information from third parties that the tokens contain, right? And JWTs are not encrypted by default, so do not put secret information in JWTs unless they're encrypted or unless you're using HTTPS, which is something that we'll talk about later in the course as well. Now, the final thing I want to talk about here is some of the benefits and drawbacks of JWTs. So some of the benefits of JWTs, really the two main benefits are that, one, they're stateless. So in other words, all of the information that JWTs contain is contained inside the token itself. And what this means is that it doesn't require our backend to actively keep track of who's logged in, all right? So in our database, we don't have to set properties on users saying, you know, "This user is logged in,"
>
> **[9:27](https://www.linkedin.com/learning/react-authentication-25660373/json-web-token-basics?u=76281980&t=567)** or, "Logged in equals false," things like that. Now, the other benefit of JWTs is that they use JSON, which means that it's generally a very compact representation compared to some of the other options like XML, and it's also more secure as well, since signing JSON is easier and generally less vulnerable than signing XML. All right, so those are two of the main benefits of JWTs, let's talk about some of the drawbacks. The first drawback is that, because of the way tokens work, they'll generally remain valid until they expire or until the private key on our server is changed. So if a user's account gets hacked, for example, there's just not really a good way to kick the hacker out, even after the user changes their password, since the hacker will still have that JSON web token. And this also means that it's hard to block specific users, at least not with making our JWT flow stateful by having a database of things like blacklisted IDs, for example. And as we'll see later on, this also means that logging out is achieved by simply deleting the token from the browser. Since, again, there's no way to say that a user is logged in or logged out in our database. We'll see, of course, how all of this works a little bit later. So that's the first main drawback. The second main drawback of JWTs is that the standard itself, right, the JSON web token standard, doesn't define a process for refreshing tokens. So essentially what this means
>
> **[10:59](https://www.linkedin.com/learning/react-authentication-25660373/json-web-token-basics?u=76281980&t=659)** is that when the token expires, the user will have to re-authenticate. So they'll have to log in every two days or something like that, depending on how long we set the expiry date on our server, if we set one at all, right? So anyway, those are the two main drawbacks of JWTs, and that's pretty much all I wanted to talk about with these things before we get started. So now that we've talked about these, we can start incorporating them into our [React](../../Skills/Web%20Development/React.js.md) app.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Tokens](../../Skills/Web%20Development/Tokens.md) (13), [JSON](../../Skills/Web%20Development/JSON.md) (12), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [React](../../Skills/Web%20Development/React.js.md) (1)
> **Env Vars:** json (12), jwt (9), xml (2), hs256 (1), https (1)
> **Definitions:** means that (3), is an  (2), in other words (2)
> **Analogies:** for example (5), such as (1), similar to (1)
> **CLI Commands:** make (5)
> **Prerequisites:** set up (2), you'll need (1)
> **Cross-References:** later in (1)
> **Warnings:** keep in mind (1)

#### [Solution: Working with JSON Web Tokens](https://www.linkedin.com/learning/react-authentication-25660373/solution-working-with-json-web-tokens?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/solution-working-with-json-web-tokens?u=76281980&t=0)** - [Instructor] All right, well hopefully you gave this challenge a try. So what we're going to do here is take a look at the solution. So really all we needed to do here was implement this decode JWT payload function so that it took a JWT in the format that we learned about, and displayed the contents right, the decoded contents of the payload portion of the JWT. So here's what this looked like. The first thing we had to do, of course, was get the payload portion, and since JWTs are essentially made up of three parts, the header, the payload, and then the signature, we just had to split it by that period symbol and get the middle one. So here's what this is going to look like. We're going to say constant parts equals JWT split, and we're going to split it on the period character. And then we're going to get the middle part by saying constant payload equals parts and that's going to be index one. Alright, so at this point we have the middle part of it, but that's in an encoded base 64 URL string. So what we're going to do here is we're going to have to say, in order to convert this to true base 64, as was said in the instructions, we're going to have to say const base 64 payload equals, and we're just going to say payload dot replace. And we're going to replace all dashes with pluses and we're also going to replace all underscore characters
>
> **[1:38](https://www.linkedin.com/learning/react-authentication-25660373/solution-working-with-json-web-tokens?u=76281980&t=98)** with slashes. Okay, so we'll say replace underscore with slash and that should be all we need to do there. So now that we have this, we're just going to take the [JSON](../../Skills/Web%20Development/JSON.md) payload and we're going to decode it using A to B. So here's what this is going to look like. We're going to say return JSON dot parse, and inside here we're going to say decode URI component. And to this we're going to pass A to B, just like we did before with the base 64 string, right base 64 payload, that is. And that should be all we need to do. So let's test this out. What we're going to do is take a JWT and one was provided down here for us. So let's just copy that like so. And we're going to paste that in there and click decode and sure enough we see that that gives us username, [John](../../Glossary/Tool/John%20the%20Ripper.md) Doe, IAT, right? That's the expiration that was put into there in case we want to use that. And that is the solution to this challenge. So hopefully you figured that out and congratulations if you did.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (2), [John](../../Glossary/Tool/John%20the%20Ripper.md) (1)
> **Env Vars:** jwt (5), json (2), url (1), uri (1), iat (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Adding a sign-up route to the server](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-sign-up-route-to-the-server?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-sign-up-route-to-the-server?u=76281980&t=0)** - [Instructor] All right, so now that we know a little bit more about JWTs and how they work, let's get started integrating them into our full Stack [React](../../Skills/Web%20Development/React.js.md) applications. So we can close our front end files for now because we're going to be jumping into our backend. And really the first thing we're going to do here is we're going to open up server js and add our first route. Now, just as a warning here, our server is rapidly going to get a little bit too big to comfortably fit inside this file. So we'll probably be breaking it out into several files at some point. But for now, we'll just get started here by creating a signup route. So here's what this is going to look like. We're going to create a post route, so we'll say app post. This basically just means that the front end will have to send a post request instead of a get request to sign up, which is, you know, kind of what's typically expected in this case. And for the path we're going to make the path/API/signup. Okay. So now that we've done that, here's what we're going to do. We're going to add a handler, which will take the request and response objects, and this is going to be async as well because there are going to be some asynchronous things going on in this handler as you can probably imagine. So here's what this is going to look like. This route is what the signup page on the front end is going to send a request to when the user enters in their information
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-sign-up-route-to-the-server?u=76281980&t=94)** and you know, wants to create a new account. So here's what this is going to look like. We're going to say const email and password. Those are the two pieces of information the user is going to be sending along and we're going to be getting that from the request body. So here's what that will look like. We're going to say equals request body. Make sure you do have this express [JSON](../../Skills/Web%20Development/JSON.md) thing up above. That's what we'll actually parse the body and make it available here just in case you are trying to type that out yourself. And now that we've done that, the next thing we're going to do is we're going to basically check whether or not there's already a user in our database with this email. So here's what this will look like. Our database, again, let's just take a look at it once more. We'll open up the file here just to make sure we're on the same page. We can basically directly import our database here, as you can see, right? It's part of our module exports and we'll be able to make changes to it. In particular, we've already set this user's empty array thing inside of here. So really all we have to do is search through our user's array to see if there's already a user with that email address, right? So this is just going to be plain old [JavaScript](../../Skills/Software%20Development/JavaScript.md). We don't have to make [MongoDB](../../Skills/Software%20Development/MongoDB.md) queries or anything. That's why I wanted to keep it simpler like this. So here's what this will look like. First of all, let's import the database. We'll say const DB = require and then that's going to be slash DB, right? That file. And now we're going to say matching users equals,
>
> **[3:10](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-sign-up-route-to-the-server?u=76281980&t=190)** and we're going to say DB users. And we're going to search for by saying do find any user. And here I'm going to say matching user like so any user whose email, right? So user email is equal to the email address that the current request is trying to sign up with. All right? So this will find if there are any matching users, right? If there is, there will be an object in this matching user variable. And by the way, let's say const matching user, if there isn't, this will simply be undefined. So we can check this now by saying if matching user, well in this case we don't want to allow the user to proceed. So we're just going to say return response, do send status, and we're going to send a 4 0 9 status basically indicating that there's already a user with that email in our database. All right? So that's what we're doing here. And in fact, I'll leave a comment on these two things here just to make sure this is clear what we're doing. So we'll say something like, make sure there is no user with the email already in the database. All right? And now we move on, right? So here's what we're going to need to do. We're going to need to insert the user's email and password into the database, and this is very important here, you do not want to store the user's password as plain text. And here's why.
>
> **[4:42](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-sign-up-route-to-the-server?u=76281980&t=282)** Occasionally a hacker will gain access to the database and if there's a bunch of users, plain text password sitting in there, right? If the password or if the hacker rather can just see ABC 123, blah, blah, blah. If that's the user's password, then they now know because they can also see the user's email what that user's password is likely to be on a lot of other sites. And they're probably going to have a script ready to just try that you email and password combination everywhere they can. So what we're going to do instead is we're going to encrypt the user's password before we store it in the database and that will basically sort of scramble the password so that it doesn't look anything like what the user's actual password is. So even if the hacker gains access to the database, it will be much more difficult for them to figure out what the user's real password is. Okay? So here's what this will look like. We're going to say const password hash, right? That's what this is called. Here is hashing. And we're going to use a library called B crypt by saying const B crypt equals require B crypt. We are going to have to install this thing here. All right, so you're going to open up a new terminal here and you're going to run, well first of all, we'll change directories into our backend. We're going to run [npm](../../Skills/Web%20Development/npm.md) install B crypt now and that should install that package for us. I'll just close that while that runs. All right, now that we've done that, here's what we're going to do. We're going to say await B crypt.hash and we're going to pass in our password.
>
> **[6:15](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-sign-up-route-to-the-server?u=76281980&t=375)** And then the second thing here is the number of rounds it's going to use to hash our password. And basically the higher the number you put here, the more difficult it's going to be for hackers to crack that should they get access to your database, but also the more computationally intensive it is on your server as well. All right, so anyway, besides that, now that we've hashed the user's password, we're going to create some other information about the user that will store in our database. So here's what this is going to look like. We'll say const starting info. This is just going to be some blank fields that the user can fill out a little bit later in the application. So we'll have hair color and that's just going to be an empty string here. We'll have favorite food and we will have an empty string for that. And then we'll have bio, right, like a biographical paragraph or two that the user can write. And that'll start off as an empty string too. And now what we're going to do is we're just going to insert this user into the database. So we're just going to say DB users push and we're going to push a new user into the database like this by saying email. We're going to have the user's password hash saved specifically as the name password hash, right? With that key so that we remember we'll have info that'll be the starting info we just created. And finally we'll have is verified and we're going to set this to false. This is basically going to be set to true when the user verifies their email. So that should be all we need to do there.
>
> **[7:50](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-sign-up-route-to-the-server?u=76281980&t=470)** The only other thing I suppose is we should probably create some sort of unique ID for this user and we can do that by using the UUID package. So let's install that one as well. We'll open that terminal up again and say NPM install UUID. And now what we should be able to do with this is just import that. So we'll say const and then we'll say uuidv4, which we're actually going to rename here. We'll say v4: uuidv4. And we're going to say equals require UUID. And now that we've done that, what we're going to do is we're going to go down to here and generate a new ID for the user. So here's what that will look like is we'll say right after we hash that password, we'll say constant user ID equals, and actually here, we'll just call this ID equals uuidv4 like this, and we'll add that to the user like this. All right? And the last thing we need to do here is we're going to want to send back something to the user that'll probably involve this ID. But for now what we'll do, just so that we can verify that this server is working is we're going to say something like, you know, response JSON, and we'll just send back the ID that we generated. Okay? And we also have to call DB save. So, right, we need to actually import this save DB function here. And I am writing sync just to make it a little bit easier.
>
> **[9:24](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-sign-up-route-to-the-server?u=76281980&t=564)** You could also change this to fs.write file, you know, if you're using promises or something like that, and make this an async function for now, it doesn't really matter, it's an easy thing to change. So I'll just leave it the way it is and we'll need to import that. So we'll say DB and save DB. And then we just need to go down here. And after we're done, we're going to call save DB like that. And that should be all we need to do. So let's test this endpoint out right now. We're going to need to restart our server by going into our server terminal, then stopping our server, and then we're going to rerun it with nodeserver.js. You can use something like node demon in order to make this work. But now in order to test this, we're going to need to make our port visibility public. So just right click on this, go down to port visibility and say Public, make sure you do that for 3000. Oops, I did that to the wrong one. Doesn't really matter. We'll change this to port visibility public as well for port 3000. And now we're going to open up a tool called Postman. Feel free to download this if you don't already have it. It's a great full stack tool that allows you to test out your server on the backend without having to actually, you know, create the front end functionality first. So we're just going to create a new request here. This is going to be a post request to whatever that URL is. So we'll go back here, copy this URL for port 3000, right? Just click that little clipboard icon and then we'll go back over to Postman.
>
> **[10:58](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-sign-up-route-to-the-server?u=76281980&t=658)** We're going to paste that. And this is of course going to be /API/sign-up. That's that new endpoint we created. And for the body, this is where we're going to include the email and password. So let's just create something real quick. We're going to say this is going to be JSON and we'll say email. That'll be, I'm just going to use sean@[gmail.com](https://gmail.com). Obviously not a real email address. And then we're going to say password. And obviously this isn't going to be a real password, but this will be ABC123! And if we click send, what we hope to see here is sure enough, we get back this response with the ID of the user that was created. And if we go back to our server, now, what we should see is that there is this DB.JSON file, this is where we can see all of the data that's saved in our database. So that's really nice feature about, you know, using this kind of database, at least for demonstration purposes, is we can immediately see if what we did worked. And you can see the password hash here, which is quite a bit different than ABC123. So anyway, that is how to create a simple server endpoint that allows user to sign up. So the next thing we're going to need to do is actually add JSON web [Tokens](../../Skills/Web%20Development/Tokens.md) to this so that we can send that back to the user once they've signed up.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (5), [npm](../../Skills/Web%20Development/npm.md) (2), [React](../../Skills/Web%20Development/React.js.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [MongoDB](../../Skills/Software%20Development/MongoDB.md) (1)
> **CLI Commands:** make (13), find (2), npm (2), node (1)
> **Env Vars:** json (5), uuid (3), api (2), npm (2), url (2)
> **Tools:** terminal (3), postman (2)
> **Prerequisites:** install (5)
> **File Paths:** nodeserver.js (1), db.json (1)
> **Ports:** port 3000 (2)
> **Cross-References:** later in (1), go back to (1)

#### [Generating JSON Web Tokens](https://www.linkedin.com/learning/react-authentication-25660373/generating-json-web-tokens?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/generating-json-web-tokens?u=76281980&t=0)** - [Narrator] All right, so now that we've created our signup route on our server, we need to actually add jsonwebtokens into this. Now, there are a lot of different packages that you can use for doing this, but the one that we're going to use is the most straightforward, and that is jsonwebtoken. So just like we did with the other packages, we're going to need to install this one. So you can actually just open up, well here, we'll open up the same one that we had. We're going to say [npm](../../Skills/Web%20Development/npm.md) install jsonwebtoken, all one [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md), no dashes, nothing. And that will install that for us. So now that we've done that, here's how we add this to our signup route. First of all, we'll just add this in as an import. We'll say const jwt = require jsonwebtoken. All right, now that we've done that, let's actually scroll down to where we're going to need to generate that token. So, this is going to be after we've inserted the user into the database, right? So, we've already added the user to the database, and called saveDb. We'll just kind of, you know, put those a little closer to each other so they don't accidentally get separated. And now, before we respond to the user, and by the way, we're going to be responding in a slightly different way than what we're doing here, we're going to actually create a jsonwebtoken, and we're going to do that by saying, here's where we're going to use that library, jwt.sign, and we're going to pass all of the data that we want to be in that jsonwebtoken. Now, you can decide how much data you want
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/generating-json-web-tokens?u=76281980&t=94)** in that payload, just by the data that you pass here. All right, so in this case, what we're going to do is we're going to include everything except the password hash. We don't want the password hash in there, because that's really just meant for the [Databases](../../Skills/Software%20Development/Databases.md) (indistinct) right? So, here's what we're going to do. We're going to say id, all right, that's the ID we generated. We're going to have the user's email, we're going to have the user's info, which is the starting info, once again, that we created up there. And then we're going to have isVerified: false. And that should be all we need to do. Now, the way this works is this jwt.sign method also takes something called a jwt SECRET, right? This is a secret key that I mentioned in a previous video, and we're going to set that in the environment by saying process.env.jwt_SECRET. Now, what you actually call this thing, it doesn't really matter, but I'll show you in just a minute how we can actually set this in our environment. This is the SECRET key that our server's going to use to actually sign that jsonwebtoken. And next, we're going to actually set an expiresIn date. So, we'll say expiresIn: and we'll say two days. You can set that to whatever you want. This is an additional thing that the JWT library adds on, right? This ability to add an expiration to the web token. And last but not least, we're going to have to add a callback. This is going to be called with any errors
>
> **[3:08](https://www.linkedin.com/learning/react-authentication-25660373/generating-json-web-tokens?u=76281980&t=188)** that occur while signing, and with the token itself, and this is not a promise, this isn't something, I guess you could convert it to a promise and then use async if you wanted to, but I'm just going to put everything inside this callback because it's a little bit easier in this case. So now that we have the token, well, first of all, let's make sure there's no error. We'll say if there's an error, we'll say return response.status. We'll send like a 500 status code along with that error, like so. Otherwise, what we're going to do is we're just going to respond with the token and we're not going to include the ID. Here's why. Because the ID is going to be inside the token itself. So the front end, if the front end is wondering what the ID of the user is, if they want to like load the user's information or something like that, they'll be able to get that directly from the token itself, right? Because we're adding that in there. So we're just going to say response.[JSON](../../Skills/Web%20Development/JSON.md), and we're going to send back the token, like so. All right, so that should be all we need to do. Let's test these changes by, first of all, restarting our server. So we'll go into our backend, stop it and restart it. Let's make sure that that didn't mess up the ports here. It might have. Nope, it looks like it's still set to public. So, let's go back to Postman and test this out. We're just going to send, well, I guess we do need a new email here. So I'm just going to say shaun2@[gmail.com](https://gmail.com), because right now it will actually give us an error, since we stored the first email in our database. So we'll say Send, and what we hope to see back...
>
> **[4:43](https://www.linkedin.com/learning/react-authentication-25660373/generating-json-web-tokens?u=76281980&t=283)** And oops, it looks like that didn't work because we got a 500 Internal Server Error, and I believe that's because we didn't set a JWT secret. So, let's actually stop our server again. And all you need to do, this depends on your operating system, but on a Mac, you can just say export JWT_SECRET= and then you can put whatever SECRET you want. Now, keep this actually secret, if you're planning on using it in production. I'm just going to do abc123, like that. That will be our secret. Not a good secret at all. So please don't use this in production, but that will be all right for our purposes now. So let's just hit Enter there. And now, let's try running our server again. So there we go, node server.js, and if we go back and try Postman again, we should be able to, well, here we're going to need to do Shaun3 there, because you know Shaun2 is now in the database as well. So we'll just say Send. And sure enough, we see that we get back this token, which is quite long, right? That's definitely a lot longer than what I showed you in the slide. But this actually contains all of that data that we added to the jsonwebtoken, right? So, all of the data that we added right here, this is all in there in base64 encoding. So anyway, that is how to generate jsonwebtokens and send them back to the user in our signup route.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [npm](../../Skills/Web%20Development/npm.md) (1), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Env Vars:** secret (3), jwt (2), jwt_secret (1)
> **CLI Commands:** make (2), npm (1), node (1)
> **Code Identifiers:** expiresin (2), savedb (1), isverified (1)
> **File Paths:** process.env (1), response.json (1), server.js (1)
> **Prerequisites:** install (3)
> **Cross-References:** previous video (1), go back to (1)
> **Tools:** postman (2)

#### [Adding a log-in route to the server](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-log-in-route-to-the-server?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-log-in-route-to-the-server?u=76281980&t=0)** - [Instructor] All right, so now that we've created a signup route, let's do the same thing but with a login route. So the login route's going to look pretty similar to the signup route here. The main difference is that instead of creating the new user, like we did up here, we're just going to be loading that user out of the database and checking to make sure that their password is correct. So here's what this is going to look like. All right, we'll leave the signup route up at the top because that's the first part of the flow generally. So here's what this will look like. We'll say app.post, we're going to have this be a post route again, and the path for this is going to be /api/login. And then for the handler, we're going to say request and response. And we'll start off here by getting the email and password off the request body, just like in the signup route, all right? And the next thing we're going to do is we're going to load the user from the database. So all we need to do for that is say const user equals, and then we will say, db.users.find, we're going to find the user whose email matches the email in the request. And if there's no user matching that, right, what we're going to do, we're going to say, if not user, then what we'll want to do is we're just going to say return response.send status, and we're going to send back a 401 status like so. And otherwise what we're going to do is we're going to check whether the user's password is correct. So we'll say const password is correct equals,
>
> **[1:36](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-log-in-route-to-the-server?u=76281980&t=96)** and we're going to use the bcrypt library here. So we'll say await bcrypt.compare. And we're going to compare the password that was included in the request with the user's password hash, right? That's how we do this here. We can just pass in the password and then user.password hash and that will compare those for us. And we do need to make this callback async here in order for this to work. There we go. So now, right now that we know whether the user is logged in or not, we can check and say if password is correct. Well, in that case what we want to do is we want to load the user and send back a [JSON](../../Skills/Web%20Development/JSON.md) web token with the user's information in it. Otherwise we'll do this one first because this is shorter. We're just going to say response.send status and we'll say 401. All right, cool. So inside password is correct. Now we're just going to say jwt.sign. And in fact, this is going to be almost identical to what we had before. So let's just, we're just going to go back up into our signup route, take this jwt.sign thing, copy that, and we will paste that down here. There we go. And now what we can do is, well, first of all, we will need to get the id, email info, all that kind of stuff from the user. So let's just make that a little bit easier here by saying const ID, email, there we go. Info. All right. That's just going to be the info there. So we don't need starting info anymore. And then we will have is verified
>
> **[3:13](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-log-in-route-to-the-server?u=76281980&t=193)** and we're going to send that back to the user by saying equals. And this is going to be equals user, right? The one that we loaded from the database. And then this just needs to be, is verified down here. And that is pretty much all we need to do. So let's give this a try. Now what we're going to try and do is, first of all, we'll need to rerun our server of course. So we'll just stop it and restart it like so. And then what we'll do is we'll go over to Postman and we're going to change this to log in. Let's see, it's always a little bit difficult to remember whether we said login as one [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) or log dash in. I'm actually going to add a dash there and restart the server, you know, just to keep it consistent since we did the same thing for signup. So now we'll go back and say log-in and we'll try and log in with really any of these Seans, right? We can do sean@[gmail.com](https://gmail.com) and we'll try and log in with Abc123, which is the password we used and what we hope to see is something that looks a lot like what we had down here. All right, so let's give this a try. We're going to click send and sure enough we see that we get that token sent back to us, which is exactly what we wanted, all right? So anyway, that is how to add a login route to our server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **CLI Commands:** make (3), find (2)
> **URLs:** [gmail.com](https://gmail.com) (1)
> **API Endpoints:** /api/login (1)
> **Env Vars:** json (1)
> **Tools:** postman (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Adding JWTs to the front-end](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-front-end?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-front-end?u=76281980&t=0)** - [Instructor] All right, so now that we've added [JSON](../../Skills/Web%20Development/JSON.md) web [Tokens](../../Skills/Web%20Development/Tokens.md) and a few routes to the server, the next thing that we're going to do here is we're going to start implementing JWTs on the front end, right? Or integrating them with the front end that we have. Now, the easiest way to go about this is going to be if we open up our front-end folder here to add two custom hooks that will take care of the functionality of, you know, basically getting the data out of the JSON web token if we have one. And then parsing the user from that. So here's what this is going to look like. I'm going to create a new file here. The first new file is going to be called useToken.js, and then I'm going to create another new file, and this is going to be called useUser.js. Now here's how this is going to work. useUser is actually going to use the useToken custom hook. And really useToken is going to be more of a general purpose thing for dealing with the JSON web token that we have in the browser. So here's what this is going to look like. We're going to start off with useToken, of course, as I said, and we're going to start off by importing the useState hook from [React](../../Skills/Web%20Development/React.js.md). All right? And once we've got that, we're going to export this new useToken custom hook. And that's going to look like this. We're just going to say const [token, setTokenInternal]. We're going to call it that
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-front-end?u=76281980&t=94)** because this is going to be used only internally in this custom hook. We're going to actually add a set token function that the client code will be able to use. You'll see why that is in just a minute here, of course. But now we're just going to say = useState. And the next question that we need to answer here is where we actually want to store the token, right? That we get back from the server. So in other words, once the user logs in on our front end, how are we going to basically make sure that that token persists when the user visits other pages, closes our site, things like that. Well, the easiest way to do this though, not necessarily, the best way as you'll see, is to simply store the token in local storage. So what we're going to do here is we're just going to say return and then we're going to say localStorage.getItem, and we're going to store the token under the key token. All right? Pretty straightforward. So that is going to be the initial state for this token state variable is whatever happens to be in local storage. Now, if there's nothing in local storage, then that's just going to be undefined, right? And that's perfectly fine. That will indicate that we don't have any token currently because no user is logged in. That's what that will mean to us. Cool. So the next thing we're going to do is we're going to define a setToken function here that's going to take care of this. We're going to say = newToken. And this is just going to modify local storage and modify the token state variable at the same time.
>
> **[3:12](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-front-end?u=76281980&t=192)** That'll make sure that those two things are kept in sync. All right? So here's what this will look like. We're going to say localStorage.setItem, and we're going to set the token key here to whatever that new token should be. And notice here that because newToken is going to be a string, we don't actually need to like stringify this by using JSON.stringify(), just something I wanted to point out. 'Cause local storage can only store strings. And then we're going to call setTokenInternal to that newToken as well. So that's why we defined this as a slightly different type of state variable or state variable modifying function. And now that we have this set token thing, all we need to do is return setToken. Well token the state variable that is, and the setToken function that we created. And that is our useToken custom hook. So the next thing that we're going to do is we're going to go into this useUser hook and actually use our new useToken custom hook to parse and then basically make the user available to other parts of our application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (4), [Tokens](../../Skills/Web%20Development/Tokens.md) (1), [React](../../Skills/Web%20Development/React.js.md) (1)
> **Code Identifiers:** usetoken (7), useuser (3), settoken (3), newtoken (3), usestate (2)
> **Env Vars:** json (4)
> **CLI Commands:** make (3)
> **File Paths:** usetoken.js (1), useuser.js (1)
> **Definitions:** in other words (1)
> **Speakers:** - [instructor] (1)

#### [Parsing JWT data](https://www.linkedin.com/learning/react-authentication-25660373/parsing-jwt-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/parsing-jwt-data?u=76281980&t=0)** - [Instructor] All right, so we completed this useToken custom hook, let's go over and implement our useUser hook now, which is, again, going to use our use token custom hook. So we'll start off here by importing the useState and useEffect hooks. We're going to need both of these from [React](../../Skills/Web%20Development/React.js.md). And then we'll also import the useToken custom hook that we just created there. All right, and now that we've done that, the next thing we're going to do is define our hook. So we'll say export const useUser. And this one here is going to look like this. We're going to start off by getting our token from the useToken custom hook. And notice that we're not even using the set token function that we defined there. That's okay, since that'll be waiting around if we ever need to do so, all right? And the next thing that we're going to do is we're going to create this sort of internal function called getPayloadFromToken. So this is going to be where we actually take that token and parse the data that it contains. Here's what this is going to look like. We're going to say const getPayloadFromToken equals, and this is going to be token, and then we will say const encodedPayload. So I mentioned earlier that by default [Tokens](../../Skills/Web%20Development/Tokens.md) are Base64 encoded, and what this means is we need to basically decode those, right? We need to decode that data in order to get it into the form that we want it. I'll show you what that looks like in a minute,
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/parsing-jwt-data?u=76281980&t=94)** but here's what that'll look like first, we're going to say token.split. And since the token, remember, consists of three parts, right, the header, a payload and a signature, we're going to need to pull out the payload, which contains the user that we want. So here's what that looks like. We're going to say token.split, we're going to split it by that period character, which remember is what separates those three parts, and then we're going to get index 1, which is going to be the middle one there, all right? And finally, we just need to decode that by saying return atob, this is just a built-in function that will decode a string, and we're going to say encodedPayload. And then we're going to parse this into an actual [Java](../../Skills/Software%20Development/Java.md) Script object by saying [JSON](../../Skills/Web%20Development/JSON.md).parse, all right? That makes sure that that is converted into an actual JSON object that we can work with, all right? So now that we've got that function, here's what this is going to look like. We're basically going to have to use this useEffect hook here to watch the token. And whenever that thing changes, we're basically going to have to call this getPayloadFromToken function and update everything, all right? It just helps us keep the token and the user state in sync with one another. So here's what this is going to look like. We have to first of all define this user state. So we'll say user setUser equals, and we're going to use this use state variable again with this callback that will calculate the initial state for us. And for that, we'll just say, if there is no token, we just want to return null, right,
>
> **[3:08](https://www.linkedin.com/learning/react-authentication-25660373/parsing-jwt-data?u=76281980&t=188)** because there's no currently logged-in user. That's how we'll communicate that to the component that's using this, right? Otherwise, we're going to say return getPayloadFromToken, and actually pass that token to it like so. And that's all we need to do there. So finally, this is where we're going to use this useEffect hook. And we're going to watch, here, I'm going to add that last argument there, just so that I don't forget it, we're going to watch the token for when it changes, right? Remember that that will call this callback function whenever the value inside here changes? And now we just need to say, if there's no token, we want to set the user, right. call setUser null, basically recreating what we did up here, but by calling setUser and using user here. Otherwise, what we want to do is we want to say setUser to getPayloadFromToken called on the token. And that is all we have to do, except of course, return the user, and that will make it available to whatever pages, components, et cetera, need to use that user that's contained inside the JSON web token. So that is how to implement a useUser hook that uses that useToken custom hook that we created previously.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (3), [React](../../Skills/Web%20Development/React.js.md) (1), [Tokens](../../Skills/Web%20Development/Tokens.md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** getpayloadfromtoken (5), usetoken (4), setuser (4), useuser (3), useeffect (3)
> **Env Vars:** json (2)
> **CLI Commands:** make (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor] (1)

#### [Adding JWTs to sign-up and log-in pages](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-sign-up-and-log-in-pages?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-sign-up-and-log-in-pages?u=76281980&t=0)** - [Instructor] Alright, so now that we have the useToken and useUser custom hooks, let's integrate those into our pages, and we'll start off here with the login page and the signup page. Now, both of these pages are going to use the useToken custom hook because neither one of these pages is really going to need to care too much about the user, because they're not going to display the user or anything, right? Really all they need to know is whether the user is currently logged in, and when the user does log in, we're going to redirect the user to that user info page. So the user info page is what's going to use that useUser custom hook, but these ones are going to use useToken. So here's what this will look like. We'll start off by importing useToken. All right, there we go, and that didn't get imported for me automatically for some reason, but we'll just reference that like so. And the other thing that we're going to need here is we're going to need to import a library called axios from axios. So this library is very commonly used in [React](../../Skills/Web%20Development/React.js.md) to make network requests, and this is what we're going to use to actually load data from the server, right? And actually make requests from the front end to our login and sign up endpoints. So now that we have that, and actually we need to install Axios, and by the way, one thing you may have noticed is that [Codespaces](../../Skills/Software%20Development/Codespaces.md) can tend to shut down if you leave it for a little while, and that will basically get rid of your terminals that you have open. So if that happens to you, you're just going to need to open up a new terminal and make sure you're in the front end directory, alright?
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-sign-up-and-log-in-pages?u=76281980&t=94)** And then you're going to run [npm](../../Skills/Web%20Development/npm.md) install axios like that, okay? So that will install the Axios library for you. And now that we have that, we can go into our login page and actually use these two things here. So first of all, what we're going to do is inside onLogInClicked, right, where we have this little alert that says log in not implemented yet, we're going to replace this and actually implement it. So here's what this will look like. We're going to use Axios here to basically take the values that are currently in the email and password inputs, right? That's these two state variables here, and you can see how they're connected down here if you're curious, and we're going to send that along to the server. The server's going to check to make sure that the password that was entered was actually correct for that email. And if all goes well, the server will send us back a token, which we can call setToken on this useToken custom hook to make that happen. So here's what this will look like, and actually while we're at it, let's add this useToken custom hook here by saying const token and setToken to useToken, like so.
>
> **[2:44](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-sign-up-and-log-in-pages?u=76281980&t=164)** And now we're going to go back down to onLogInClicked, we're going to say const response = await axios.post. We'll send that to /api/login and then we're going to have email equal to emailValue. That's that state variable that mirrors whatever's in the email input, and we're going to have password, which is going to be passwordValue, and that will be the password input. Cool. So again, if all goes well, we're going to get back that token in the response, and I'm going to ignore the error case here right now. So you would want to put this inside a try-catch block in case something goes wrong with that request, but just to keep things simple and straightforward, we're going to say const token = oops, there we go, response.data and now we're going to say setToken to that token that we got back, and then we're going to say navigate, and we're going to navigate to the home route, which is going to be where that user info page is displayed. So that is really all we need to do for logging the user in. Let's do this same thing over on the signup page. Basically, it's going to be almost identical to what we just did here, so we can actually just copy and paste a lot of this stuff. So if I drag this over here, there we go. We can add Axios and useToken to the signup page. We'll add this token state variable to the signup page as well, like so, right? Let's adjust the indentation there to make sure that that's all alike, and then we will take this stuff down here,
>
> **[4:18](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-sign-up-and-log-in-pages?u=76281980&t=258)** and OnSignUpClicked is going to be a little bit more in depth than what we had before, just because we're going to want to actually check to make sure that like the password values are the same, right, passwordValue and confirmPasswordValue, but that's really it. So let's just combine these there and we'll open up the signup page again, and this one, first of all, is going to make a request to api/sign-up. And actually, let me just double check that server route there that we had. Okay, so sign-up and log-in. I almost made that mistake. So let's actually change this onSignUp. That's going to be sign-up, and on login, that's going to be log-in, okay? You got to be careful about those things. That's an easy mistake to make. Alright, and now that we've done that, that should be all we need to do. We'll actually come back and handle what happens if the password values don't match up here in a future video when we see how to work with this setErrorMessage thing that we have in both our login page and our signup page. So anyway, that should be all we really need to do in order for our signup page and login pages to work. There's not really going to be a whole lot to see yet because we need to integrate this with the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our application, but that should be all the setup we need. So we might come back here and make a few final adjustments before it'll work once we've put the rest of the stuff in place, but that's all we need for now.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [React](../../Skills/Web%20Development/React.js.md) (1), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (1), [npm](../../Skills/Web%20Development/npm.md) (1)
> **Code Identifiers:** usetoken (8), settoken (3), useuser (2), onloginclicked (2), passwordvalue (2)
> **CLI Commands:** make (10), npm (1)
> **Prerequisites:** install (3), setup (1)
> **API Endpoints:** /api/login (1)
> **Tools:** terminal (1)
> **UI Navigation:** navigate to (1)
> **Warnings:** be careful (1)

#### [Adding JWTs to the user info page](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-user-info-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-user-info-page?u=76281980&t=0)** - [Instructor] Alright, so now that we've added this functionality to the signup page and login page, the next thing we're going to need to do in order to see this in action is actually add them to the user info page. And here's what this is going to look like. We're basically going to need to use the useUser custom hook that we created to load the user and basically pre-populate these values here, right? Favorite food, hair, color, bio, stuff that the user can edit with those values. So here's what this is going to look like. We're going to start off here, of course, by importing axios from axios. We might not use that right away, but that'll be around for when we need it. Then we'll say import useUser from the useUser hook that we created, alright? useUser. And then we're going to import use token as well. Now, we might not use this one yet, but this one will be around for when we want to do stuff like allow the user to log out. So just keep your eye on those. I'm just putting these here for convenience so that I don't forget to import them a little bit later. So now that we have those, here's what this is going to look like. At the top of the user info page, the first thing that we're going to do is we're going to get the current user. We know at this point that there should be one, right? So we'll say user equals useUser, because you know, with the private route that we created earlier, the user shouldn't even be able to visit this page unless they're logged in. So there should be a user at this point. So now that we have that, we're going to need to populate,
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-user-info-page?u=76281980&t=94)** as I said, these state variables down here. And here's what this is going to look like. We're going to start off, actually right after we get the user I suppose, would be a good place to do this. We're going to say const, we're going to get ID, email and info, right? Remember that info, you know, in the database. Let's actually open that up here. Db.js, right, db.[JSON](../../Skills/Web%20Development/JSON.md). Remember that info contains those values, right? So hair color, favorite food, bio, those are all blank right now. If we want to, we can actually come in here and edit those so that that's something a little bit more informative to look at. I'll leave that up just for now. So we're going to say const ID, email and info. And here's what this is going to look like. We're going to say = user, and then use this info thing to populate these. So here's what that'll look like. We're going to say info.favoriteFood or empty string, right? This will just provide a backup in case that's not equal to anything. We'll say info.hairColor for this one or empty string. And then we'll say for bio, info.bio or empty string there. And let's actually bring this together a little bit more. I'm going to move this navigate thing down here below, just so that that's hanging around for when we need that and that all this stuff is kind of grouped together a little bit more. And now that we've done that, the last thing we're going to implement in this video is we're going to go down here to this reset values function. And basically what this one is going to do is when the user goes
>
> **[3:05](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-user-info-page?u=76281980&t=185)** to the user info page and tries to edit their info. So let's say they changed their hair color or something like that, we want them to be able to reset it to whatever the original values were, instead of saving those changes. So here's what that's going to look like. We're going to just remove this, you know, not implemented yet alert. And we're going to say set favorite food. We're going to set that to info.favoriteFood, right? That'll reset it to whatever it was before, regardless of what changes they make to the values in these text inputs here, right? And then we'll just do the same thing for the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of them. So we'll say set hair color to info.hairColor. And last but not least, we're going to have set bio to info.bio, and that is all we need to do there. So let's test this out. We'll actually go into db.json and we'll try logging in with sean@[gmail.com](https://gmail.com). Alright, so for hair color, we'll just say brown. For favorite food, we'll say pizza. And for bio we'll say I like coding and also pizza. Alright, cool. So we're going to load this one and we should see if all goes well, these values displayed in the user info page. But first, if your code space is timed out, you are going to need to run your front end and backend again. So here's what this is going to look like in backend. I'm going to say cd into backend, and we'll run this one by saying node server.js. Alright, there we go. And now for the front end,
>
> **[4:39](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-user-info-page?u=76281980&t=279)** we're going to go into the front end and say [npm](../../Skills/Web%20Development/npm.md) run dev, just to run that [react](../../Skills/Web%20Development/React.js.md) app again. We'll also need to go into ports here. And what we're going to need to do is make the backend public, right? So we'll say port visibility public. You do need to do that if your code space is time's out and you have to rerun it. So we'll make that public and that should make it a lot easier. Now, there might again be a few more bits of [Fine Tuning](../../Skills/Software%20Development/Fine%20Tuning.md) that we're going to need to do to get this to work, but let's just try it out. So let's open up our front end in another browser here. Okay, we should be logged out when we open this up in the first place. So hopefully this opens up and we can see our app and oops, it looks like that didn't work. And I think that that is because we need to actually go back into our app component, right? Remember that in our app component, this is where we have our routes defined. And right now we just have is allowed set to true for the user info page. So we need to actually link that up to whether or not the user is logged in. And the way that we're going to do this is we're just going to add the useUser custom hook to the app component. So we'll say import useUser from useUser, and then inside here we're going to say const user = useUser. And then we just need to our user be the condition on whether or not this should be allowed or not. So we're going to say private route and then we'll say is allowed, alright? And this condition is going to be equal to the Boolean value of the user. So in other words, if user is null, then this is going to evaluate to false.
>
> **[6:14](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-user-info-page?u=76281980&t=374)** If user is not null, then this is going to evaluate to true, and that will really nicely either allow or redirect the user from the user info page. So let's try this again, now that we added that, we're going to go back to here and sure enough, we see that that automatically brings us back to the login page, right? If we try and delete that and go to the homepage, that's going to automatically redirect us, which is exactly what we want. Now, before we log in and check that all this works, there are a few other pieces of setup here that I'm going to walk you through. They're pretty small, but definitely very important. The first thing here is you're going to want to open up the vite config file and you'll see something that looks kind of like this here, but you're going to want to make it look exactly like this, right? So change origin true, leave that if there are any other properties, delete those besides this target thing here, this target thing you're going to want to replace with the URL of your server. And you can get that by opening up your terminal, going to ports, and then looking for the server port. Make sure it is the server port and not the front end, right? And you're going to just copy that URL by clicking this little copy local address thing, and you're going to paste it in that string. Now, the last thing here, well, there's two more things actually. The first thing is if we go back to our server, you're going to want to make sure that you have this JWT secret thing set, right? If you're working in code spaces, the unfortunate thing here is that it will reset any environment variables that you set in the terminal when your code space shuts down
>
> **[7:51](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-user-info-page?u=76281980&t=471)** after a period of inactivity. Pretty inconvenient, but all you need to do is reset that to the same thing that you had before. So you're going to say export, and that's going to be JWT_SECRET=abc123. If you can't remember what the last one you did was, what you're going to need to do is delete your database.json file because you're not going to be able to unhash that password if you don't know the JWT secret, right? That's really the point of this thing is that if a hacker doesn't have that, they won't be able to directly decrypt these password hashes in the database and they won't be able to find out what the user's passwords are. So let's just set that to what we had before, alright. And then we'll rerun our server here with node server.js. And the last thing here is when we used navigate inside our login page and our signup page, we forgot something and that something is we need to say replace, and that needs to be set to true, right? That will replace the current URL with this path here. Otherwise, what that does is that just sticks this path onto the end of whatever the current path is. So let's do that same thing over here in the signup page. We'll just say navigate and add replace true, just so that we don't forget this later. And now we should be able to test this out. So let's go into our login page here. I'm going to try logging in with sean@[gmail.com](https://gmail.com). The password was abc123!
>
> **[9:25](https://www.linkedin.com/learning/react-authentication-25660373/adding-jwts-to-the-user-info-page?u=76281980&t=565)** And if we click login in, and sure enough, we see that that brings us to the user info page but now the exciting part is that the user info page is populated with that data that we put into the database, right? So if we go back and take a look at db.json here, right? We see that those values are what's being displayed inside of here, and that's exactly what we wanted to see. So anyway, that shows that the hooks that we added to the user info page are working.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (4), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [npm](../../Skills/Web%20Development/npm.md) (1), [React](../../Skills/Web%20Development/React.js.md) (1), [Fine Tuning](../../Skills/Software%20Development/Fine%20Tuning.md) (1)
> **Code Identifiers:** useuser (9), favoritefood (2), haircolor (2)
> **CLI Commands:** make (7), node (2), cd (1), npm (1), find (1)
> **File Paths:** db.json (3), server.js (2), db.js (1), database.json (1)
> **Env Vars:** url (3), jwt (2), jwt_secret (1)
> **URLs:** [gmail.com](https://gmail.com) (2)
> **Cross-References:** go back to (2)
> **Tools:** terminal (2)

#### [Adding an "update user" route](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-update-user-route?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-update-user-route?u=76281980&t=0)** - [Instructor] All right, well now that we have our user info page at least partially connected to the server, the next thing that we're going to do is we're going to add functionality for allowing the user to edit these values. Now the reason why this is such a big thing here is because now that we have this token in place, we'll be able to have our server actually make sure that the user who, you know, just logged in is the only one who's able to edit this information, right? Because otherwise, you know, what really anybody could do is send a request to the right endpoint on the server and edit really any other user information in this case, and that's obviously not something that we want people to be able to do, is edit other users' information. So here's what this is going to look like. We're going to open up our backend again. All right, I'm just going to start sort of like with all of our editors closed, just to keep things clean, We're going to open up server.js, and we're going to add a new endpoint here for updating the user's information. Now here's what this is going to look like. I'm going to just scroll down beyond the login route, and we're going to say app.put, all right? So put requests are the usual type of request that's used for updating users or information on the server like this, and we're going to make the URL look something like this. We're going to say /api/users and then we're going to use the user's unique ID that we defined up here in the signup route. Where is it here?
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-update-user-route?u=76281980&t=94)** There it is, when we created that user's new ID. We're going to use that to identify the user that we want to update, and this is also how we'll make sure, by the way, that that user is actually the one that is making the request. So let's scroll back down, and here's what this is going to look like. We're going to have our request and response like usual in there, and then we're going to start off by getting the authorization header from the request headers. Here's what that's going to look like. We're going to say const {authorization} We'll see what this looks like from the client side in a little bit. = req.headers; And now that we've done that, we're going to say const and get the user ID that was in the path by saying req.params; So again, we now know what user we want to update, and this authorization header, that's going to contain the token that we'll use to tell whether it's actually this user or not. So here's what this will look like. We're going to say, if (!authorization), right? So in other words, if there was no authorization header included, then something's wrong, and we should send back a 401 status code saying that there was no authorization header that was sent. So we'll say return res.status(401) and we'll say .[JSON](../../Skills/Web%20Development/JSON.md) and just include a message here, right? This provides a little bit more context for why it's a 401 error. All right, so we'll say no authorization header sent. And otherwise, right, we can go down here,
>
> **[3:09](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-update-user-route?u=76281980&t=189)** and we're going to get the token from this authorization header. Now I mentioned when we first talked about JSON Web [Tokens](../../Skills/Web%20Development/Tokens.md) that they would look a little bit like this, right? They would actually have this [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) Bearer, and then they would have the JSON token after that, right, looking something like that. So what we need to do essentially is just get this part of that token, this part of that header that is, and here's how we do that. Pretty easy. It's just regular [JavaScript](../../Skills/Software%20Development/JavaScript.md). We're going to say const token = authorization.split. We're going to split that on the space character, and we're going to get the second part of that, right, index 1, and that'll give us the token. Cool, and now that we've done that, the next thing that we're going to do is we're going to verify that this token was actually a legitimate token, because otherwise what the user could have done, and this is where that last part of the token comes in, right, the signature. What the user could have done is they could have gone into their JSON Web Token and changed their ID or, you know, changed other parts about the information that was contained in that token, and they can really make themselves look like whoever they want, right? So by verifying the token, we can make sure that the token they're giving us and the information inside it is actually information that we ourselves created and gave to them. So here's what this will look like. We just need to use that JSON Web Token, right, the JWT Library, and that has a function called .verify.
>
> **[4:44](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-update-user-route?u=76281980&t=284)** And all we have to do here is pass that token along with the JWT secret, so just like when we created the JWT, we're going to say process.env.JWT_SECRET, and then we're going to have an async callback here, and this is going to have error and decoded. This will be the decoded information in the JSON Web Token. This function takes care of that for us so we don't have to do it on the server here like we did on the frontend with that A-to-B function. And inside here now, we're going to check if there was an error, right? If there was an error, then that usually is going to mean that the token, you know, we weren't able to verify the token. So we're just going to say return res.status. We're going to send a 401 here as well, but the reason is going to be different. So we'll say message, and this will be Unable to verify token. All right, and now that we've done that, we're going to say const {id} => decoded; And now we can make sure that this ID that was in the JSON Web Token is the same ID as the user that we're trying to update here, right? And again, we know that this ID is intact because we verified the token. So unless the user, right, who's making this request has our JWT_Secret, which that's a very bad situation, and most of the time, ideally, that will not be the case, right? Unless the user has that, they won't be able to make any changes to this, so we can trust in this now. And what we're going to do here is we're going to say,
>
> **[6:17](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-update-user-route?u=76281980&t=377)** if (id !== userId), then we're going to say return and we're going to say res.status and send back a 403 status code, which means that basically the user who's making the request isn't authorized to do what they're trying to do. So in this case, they're trying to update another user, and so we'll say .json, and we'll send back message, and we'll say, Not allowed to update that userId, all right? And that's all we need to do there. So now, right, assuming, of course, that everything has gone well, that the JSON Web Token was verified, that it's the right user who's trying to do this, now we can actually allow the user to update the data, and here's what that's going to look like. We're going to start off by getting the updates from the request body. Here's what this will look like. We're going to say const updates = and then what we're going to do is we're going to basically just, well, here, first of all, we'll start off by getting the user from the body, right? The request body should contain the updates that we want to apply to this user. So here's what that'll look like. We're going to say const, and that's going to be favoriteFood, all right, and hairColor, and bio, and we're going to say = req.body So now that we have those, we're going to want the updates here to be basically those things. And the reason that we're doing this separately, right,
>
> **[7:49](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-update-user-route?u=76281980&t=469)** so we'll say updates = { favoriteFood, hairColor, bio } And again, the reason that we're not just taking the request body blindly is because we don't want users to be able to include extra properties in here, right? We don't want users to be able to just like include whatever information they want in that request body. We want to make sure that we only take favorite food, hair color, and bio from there. So that's what that's going to look like there, and now that we have those updates, what we're going to do is we're going to change them in the database by saying, right, we already have the database, so we'll say db.users First of all, we'll have to find that user, so we'll say const user = and then we're going to say db.users.find We're going to find that user by the ID. And actually this might be something that we want to put up here. So we'll say, you know, just to make sure that that user actually exists in the first place. We'll say const user = db.users.find I think we'll put this after the authorization header there. All right, so we'll say it right there. and then we're going to find the user whose ID is equal to that userId property in the request parameters. And if the user doesn't exist, we're going to send back a 404 status code. So we'll say, if (!user) then we'll say res.sendStatus(404); All right? Basically the user wasn't found, and we'll also say return so that that prevents everything else from executing after that. All right, so we are almost done here. The last thing we need to do is apply
>
> **[9:22](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-update-user-route?u=76281980&t=562)** these updates to the user. So we can do that just by saying user.favoriteFood = updates.favoriteFood All right? And one possibility here is that the favoriteFood will be undefined, or hairColor or bio. These might be left out of the request body, so we want to make sure in that case that the original value, right, whatever the original value in user info was, and actually we need to change that to user.info.favoriteFood, right? We want to make sure that that remains intact. So we'll say or user.info.favoriteFood. And we're basically just going to do the same thing with the other properties. There are probably shorter or more concise ways of doing this, but I'm just going to do it this way because it's a little bit more clear. So we'll say user.info.hairColor = updates.hairColor and user.info.hairColor as the backup. Okay? And last but not least, we have bio. So we're going to say user.info.bio = updates.bio and then user.info.bio, right, the original value as the backup. All right, and once we've done that, we're going to call saveDb(); just to save those updates to that JSON file, and now what we're going to do is we're going to generate a new JSON Web Token and send it back to the client so that they can have the updated token and display the updated data. So what this will look like is we're going to say jwt.sign, and actually this is going to be much faster if I just take it
>
> **[10:56](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-update-user-route?u=76281980&t=656)** from what we had in one of these other ones here. You could probably create some sort of helper function here, but I'll leave that up to you, and I'm just going to put that right inside of here. And basically we just need to make sure that the new user info is what we're sending. So here's what that'll look like. We already have the user's ID, and, in fact, we can just take that user that we have, right? The updated user there, we can just say jwt.sign. We can get everything from that user, right, that you could just include the user here, but since we already have the object syntax there, I'll leave it like that. We have the JWT_SECRET, expires in two days, and then we have, you know, if there's an error, we just send back a 500 status code, and we respond with that token if there's not. And that should be all we really need to do there for that endpoint.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (11), [Tokens](../../Skills/Web%20Development/Tokens.md) (1), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Code Identifiers:** favoritefood (7), haircolor (6), userid (3), sendstatus (1), savedb (1)
> **CLI Commands:** make (12), find (5)
> **Env Vars:** json (9), jwt (3), jwt_secret (2), url (1)
> **Definitions:** in other words (1), essentially is (1), means that (1)
> **File Paths:** server.js (1), process.env (1)
> **Analogies:** just like (2)
> **API Endpoints:** /api/users (1)

#### [Adding update functionality to the front-end](https://www.linkedin.com/learning/react-authentication-25660373/adding-update-functionality-to-the-front-end?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-update-functionality-to-the-front-end?u=76281980&t=0)** - [Instructor] Okay, so now that we've added that update user server endpoint, let's go into our user info page, which I have open here, and we're going to implement this save changes function, which is going to be what gets called when the user clicks save changes after making some changes to these inputs, right? So anyway, let's go back to here and what we're going to do, first of all, we'll start off by deleting this stuff here, and then we're going to make a request to that server endpoint that we just created. So here's what this is going to look like. We're going to put this inside a try-catch block just because that'll make it safer if something happens. So we'll say catch error. And then inside the tri block here, we're going to say const response equals await Axios dot put right, I imported that up above already. And then we're going to, for the path, we're going to say slash API, and I'm using back ticks here so that we can insert the user's ID, we'll say slash API slash users slash and then we're going to insert the ID, right? Remember that we're getting this here from the user up above, right? So we're using our use user custom hook, we're getting the ID email info from that, and that's what we're using down here. So now that we've done that, we're going to include as updates, the favorite food value, right? Whatever the current value in that input is, the hair color value and the bio value. And that should be all we need there
>
> **[1:33](https://www.linkedin.com/learning/react-authentication-25660373/adding-update-functionality-to-the-front-end?u=76281980&t=93)** except for some headers. Now you can add headers in Axios by just adding another object to the arguments. And we're going to say headers. And here's where we're going to add our authorization header. We're going to say authorization, and this is going to be bearer, right? That's that first part of it there just by convention. And we're going to put the token into there as well. Now, we don't currently have the token, as you may have noticed, so we're just going to get that using this use token custom hook that we created. So we'll just say underneath here, const token, oops, that should look like this. Token as well as a set token we're going to want, because again, the server's going to send back an updated token for us, and we're going to say equals use token like so. Alright, so going back down to our save changes function. After we've sent this request and gotten the response, the response, remember, should have the new updated token in it. So we'll say const token, we'll call this new token so that it doesn't interfere with our state variable there. Equals response dot data, and we're going to call set token with that new token like this. All right and finally we're going to show a success message, right? I automated all of this already in this component so that it will automatically hide the success or error messages after three seconds. So all you need to do is just call set show success message. We're going to set that to true here.
>
> **[3:07](https://www.linkedin.com/learning/react-authentication-25660373/adding-update-functionality-to-the-front-end?u=76281980&t=187)** If there's an error, what we're going to do is we're just going to call set error message or set show error message rather, and set that to true there. And that should be all we need to do. All right, so let's give this a try. We're going to go back to our application running here. Make sure to hit refresh just to make sure you have the most up-to-date version. Sometimes this code spaces set up can lag behind a little bit when you make changes for some reason. So we'll try changing my favorite food to, well, we'll change it to pasta, alright? And then we'll change hair color. Maybe I dye my hair green. And for bio we'll say I like coding. And now of course we have to change this to pasta. So let's click save changes now. And if all goes well, oh, it looks like that didn't work. It says uh-oh, something went wrong, couldn't save changes, let's open up the inspector window to see why that was. You can go to network here and you can actually see what's going on, although you may have to click that again to see what's going on. And it looks like we got a 404 error, and I actually believe that this is because we didn't restart our server after making those changes. So just go in here, stop your server and restart it so that those changes will take effect. Again, you could use something like Nodemon in order to make that happen automatically. But honestly, we haven't really been making too many changes to our server here, so I thought I would just kind of skip over that part. All right, so let's try this again. And what you should be able to do here is click save changes and it looks like we successfully saved our data. So now if you hit refresh here,
>
> **[4:41](https://www.linkedin.com/learning/react-authentication-25660373/adding-update-functionality-to-the-front-end?u=76281980&t=281)** you should see that that data is persisted. Alright, so that's how to add that to the user info page.

> [!info]- Semantic Content
>
> **CLI Commands:** make (6)
> **Env Vars:** api (2)
> **Cross-References:** go back to (2)
> **Prerequisites:** make sure you have (1), set up (1)
> **UI Navigation:** go to (1)
> **Best Practices:** make sure to (1)
> **Speakers:** - [instructor] (1)

#### [State management for tokens](https://www.linkedin.com/learning/react-authentication-25660373/state-management-for-tokens?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/state-management-for-tokens?u=76281980&t=0)** - [Instructor] All right, so at this point, we've added a lot of functionality to our application and really hooked up the front and back-end and integrated them as far as, you know, this whole [JSON](../../Skills/Web%20Development/JSON.md) web token thing goes. But there's one thing that you may have discovered doesn't really work very well and it's time to fix that and that is that right now, the way that we have this useToken custom hook implemented and subsequently the useUser hook, which uses the useToken hook, it just doesn't really work because right now we just have a single local useState variable. And what this means is if we use this useToken custom hook in multiple places, it's actually creating independent state variables for the token, right? So you might have noticed if you kind of, like, go through different parts of the application, that this tends to be a little bit out of sync, so here's what we're going to do. We're going to rethink the way that this works and we're going to integrate context into the management of the token here. Now you could use something like Redux instead. There's lots of other state management options. Context is just the easiest way to get started here. So what I'm going to do is I'm going to go into the front-end source directory. We're going to create a new file and we're going to call this thing TokenContext.jsx. All right, now, it doesn't really matter how much you know about context here. Obviously it's helpful if you do know about context, but the idea behind context
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/state-management-for-tokens?u=76281980&t=94)** is that when we have something like this token where we want the state to be the same throughout the entire application, right, we don't want different versions of this token state in different components, what we can do is we can simply define a context that will manage that single-state variable. All right, so if you've worked with Redux before, think about context as just a very small Redux store. Anyway, here's what this is going to look like. We're going to start off by saying import { useState, createContex }. All right, There we go. And we're going to import that from '[react](../../Skills/Web%20Development/React.js.md)', and we're going to start off here by exporting a new bit of context called TokenContext, all right? And we're going to say = createContext. And the initial value for this thing is just going to be undefined, right? So we won't pass anything as the argument there. And now what we're going to do is we're going to export something else called TokenProvider. And this is essentially what's going to define where different versions of this state will exist, right? Different versions of this context. Now we're only going to have one, so what you'll see is that we'll put this TokenProvider up in main.jsx so that it wraps our entire application. Okay, we'll get there in just a minute. But first, what this is going to look like, it's just going to take children. And essentially the whole thing here, the whole implementation for TokenProvider is going to be the logic that we used to have inside useToken here. So I'm going to copy the main body
>
> **[3:08](https://www.linkedin.com/learning/react-authentication-25660373/state-management-for-tokens?u=76281980&t=188)** minus the return statement of useToken, and we're going to bring that over into TokenContext. And now what we're going to do is we're simply going to return the TokenContext provider. So <TokenContext.Provider in triangle braces, and we're going to say value= and we're going to make it accessible to everybody, right? We're just going to take the token and setToken parts of the state and make those available to any component inside of here if they use the useContext hook, don't worry if that doesn't make sense yet. You'll see what this looks like in just a minute. And then we're just going to put all of the children inside of that provider. So this is really just for convenience, right? And now that we've done this, let's go into main.jsx, all right? That's just the entry point for our React application here. We're going to import that provider we just created. So TokenProvider from TokenContext.jsx, you don't need that .jsx on there either. I'm not sure why that was added. And we're just going to say TokenProvider and put the app component inside of that. So what this means is that the app component and everything that it contains will have access to that single-state variable called Token. So here's what this is going to look like now. What we're going to do is we're going to go back over to our useToken custom hook and rewrite it so that it simply uses that context that's available to it. So here's what that'll look like. We no longer need useState,
>
> **[4:40](https://www.linkedin.com/learning/react-authentication-25660373/state-management-for-tokens?u=76281980&t=280)** we can actually change that to useContext. And now we can simply say that useToken is just sort of like an alias, if you will, for useContext called on the TokenContext thing that we created there. And that was just automatically imported for me as well. And that's really all that we need to do there to fix that. So let's just test this out again, and you'll probably notice that a few things work a little bit better than they did before. And by the way, we don't need to directly change anything with useUser because now useToken, right, this custom hook in here is going to be using that context. So things will be much more direct now. All right, so let's go back to our application here. We'll hit refresh. Here, let's actually log out. I'm going to open up the inspector window. You might notice some things are different in there. That's because I tested those to make sure that they worked correctly. So I changed my favorite food to pasta. Just ignore that for now. We're going to go into Application and basically delete this token. And now if we hit refresh, we should see the login page. So let's try logging in again. We'll say shaun@[gmail.com](https://gmail.com). The password's going to be abc123 again or whatever you made it for yourself. We'll log in and that'll bring us right here. And now, right, if we change this to, like, something else. So we'll change this actually to broccoli, definitely not my favorite food, but hey, we'll just go with it for the example. So Broccoli. Broccoli. And if we click Save Changes, we should see uh-oh, looks like something's going wrong there. And this actually has to do with a quirk with [Codespaces](../../Skills/Software%20Development/Codespaces.md). In order to make that work, right,
>
> **[6:14](https://www.linkedin.com/learning/react-authentication-25660373/state-management-for-tokens?u=76281980&t=374)** if you were to inspect that, you would see that you're getting a 401 error, which is a little bit confusing because that error actually isn't coming from our server, all right? You don't need to worry about the details, just know that in order to get this to work, you have to go into Ports and make the front-end port visibility public as well. Again, that's just a quirk with Codespaces. You shouldn't need to worry about that. If you're not using Codespaces, it's a pretty easy fix. So let's just refresh this and we should notice that everything works a little bit better now. So let's try broccoli. We'll try, "I like coding and also broccoli." And if we click Save Changes, sure enough, that will work successfully. And if we refresh here, all right, there we go, we'll see that sure enough, that shows broccoli and that's been persisted. And you can also see that in the database as well. So anyway, that is pretty much all we need to do for the front-end. The last thing we're going to do in the next video is we're going to see how we can implement this Log Out button, which currently just says, "Log out functionality is not implemented yet." All right, so that's what we'll take a look at next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Codespaces](../../Skills/Software%20Development/Codespaces.md) (3), [React](../../Skills/Web%20Development/React.js.md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Code Identifiers:** usetoken (8), usestate (3), usecontext (3), useuser (2), createcontex (1)
> **CLI Commands:** make (6)
> **File Paths:** tokencontext.jsx (2), main.jsx (2)
> **Cross-References:** go back to (1), in the next (1)
> **URLs:** [gmail.com](https://gmail.com) (1)
> **Env Vars:** json (1)
> **Definitions:** is a  (1)

#### [Adding log-out functionality](https://www.linkedin.com/learning/react-authentication-25660373/adding-log-out-functionality?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-log-out-functionality?u=76281980&t=0)** - [Instructor] All right, so the last thing that we have to do here, as I said, is implement some logout functionality. So let's go into our UserInfoPage and we can also close most of these here too. So we'll just close all of those like so, and open our UserInfoPage back up. And now all we have to do is go back down to the logOut function that's already in here and basically just rewrite it so that it logs the user out. Now this is pretty easy to do. All we really have to do is remove, and oops, that's the wrong one there. All we really need to do is remove the token from localStorage and send the user back to the login route, right? We're just going to automatically redirect the user there. So here's what this is going to look like. All we really have to do is call setToken here. And we're going to set that to undefined, right? That's going to remove it from the state anyway. And we might also want to open up a TokenContext here and basically add a case for that. So we'll say, if not newToken, in other words, if it's being removed, what we'll do is we'll just say return localStorage.removeItem('token'). And that should be all we need to do there. So now that we've done that, let's just reroute the user back to the login page by saying navigate and we're going to navigate them back to the log-in page. We don't need that replace true thing because this is just going to add log-in onto the end of the current path, which is the home route. So here's what this is going to look like.
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/adding-log-out-functionality?u=76281980&t=94)** We're just going to go back to our application, which is running here, and hit Refresh to make sure we have the most up-to-date version. And now if we click Log Out, what we're going to see is sure enough, it takes us back to the Log In page and if we inspect the local storage now, if we go into our application, we're going to see that there's nothing in there. So that is how you can log the user out. And by the way, since we haven't actually tested out the Sign Up page yet, as far as I know, let's do that now. First of all, in case you tested this out with a lot of different numbers, you're going to want to make sure that you're only using an email that you haven't used yet. So let's go into our backend db.[JSON](../../Skills/Web%20Development/JSON.md), and we see Shaun1, Shaun2, Shaun3. Let me actually remove these other ones because we don't need those anymore. We'll leave shaun@[gmail.com](https://gmail.com). All right, we'll save that. And now what we're going to do is we're going to try signing up. So let's actually restart our server so that that takes effect. We'll just go into our backend, restart it like so. And now if we go back to our app, and we'll just hit Refresh here just for good luck, what we're going to do is we're going to sign up with another email. I'm going to use shaun2@[gmail.com](https://gmail.com). For the password here, I'm going to say abc123 exclamation point, and the same thing for the confirm password. And now if we click Sign Up, if all goes well, sure enough, that will take us to this blank user info page where we can enter in our user info. All right, so for favorite food, maybe we'll say something like chicken. For hair color, maybe we'll say something like black.
>
> **[3:08](https://www.linkedin.com/learning/react-authentication-25660373/adding-log-out-functionality?u=76281980&t=188)** And for bio, we'll say something like, "Hello, this is just a filler bio." All right, something like that. And if we click Save Changes, that should work for this new user. And just to double check, if you go back and take a look at db.json now, you should see that new profile in there with the correct info that we just entered. And just for good measure, let's log out, and sure enough, that brings us back to the Log In page. So at this point, we have a fully functional if very simple application that allows us to log in, edit info, and log out, as well as sign up. So congratulations on getting this far.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (2)
> **Code Identifiers:** localstorage (2), logout (1), settoken (1), newtoken (1), removeitem (1)
> **File Paths:** db.json (2)
> **CLI Commands:** make (2)
> **URLs:** [gmail.com](https://gmail.com) (2)
> **Cross-References:** go back to (2)
> **Definitions:** in other words (1)
> **Speakers:** - [instructor] (1)


### 2. Email Verification

[↑ Back to Table of Contents](#table-of-contents)

#### [The basic email verification process](https://www.linkedin.com/learning/react-authentication-25660373/the-basic-email-verification-process?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/the-basic-email-verification-process?u=76281980&t=0)** - [Instructor] At this point in our application, we have a working authentication flow that allows users to both sign up and log into our application. And it also uses JWTs to keep our users logged in and make sure users can only access their own data. Now the next thing that we're going to look at adding to our application is an email verification flow. You see, at the point their application is currently at, users can create accounts with any email address they want, regardless of whether or not they actually own that email address. And that's obviously not ideal because we need a valid email address to communicate with our users and do things like allow them to reset their password, which we'll look at later in the course. And obviously, we also don't want users to be able to use each other's emails to create accounts. So what we're going to do is implement our own full stack email verification flow, basically, from scratch. But the first thing you might be wondering is, how exactly does this whole flow work? Well, just to make sure we're on the same page, let's walk through the basic email verification flow step by step. Now, the email verification process begins when a user creates a new account on our application. At this point, they've entered their email and password, and this information will then be sent to the signup endpoint on our server, which creates a new user account and then stores the user's information in the database, right? We've seen this so far. Now, here's where email verification comes in. In addition to the email
>
> **[1:32](https://www.linkedin.com/learning/react-authentication-25660373/the-basic-email-verification-process?u=76281980&t=92)** and password that the user created their account with, our server is going to create a randomly generated verification hash that the user can't see, and this will be used to prove that the user actually has access to that email address. So once we've generated that verification hash, we're going to send an email to the email address that the user provided, and this email will contain a link with the verification hash that the user can click. And next, if the user actually owns that email address, they'll be able to access this link, which will send them to a special page in our application that takes this verification hash and sends it to the server to see if it's the hash that we actually generated for the user. And of course, if it is, we're going to show the user a message saying that their email was successfully verified, at which point, the user's account will be marked as verified in the database, and they'll be able to access all of our application's features. Well, those are the basics of email verification. Obviously, there are many different variations of this flow that we could implement, and if you're familiar with any of those, feel free to implement them yourself once we walk through implementing this one here.

> [!info]- Semantic Content
>
> **CLI Commands:** make (2)
> **Cross-References:** later in (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Setting up an email provider](https://www.linkedin.com/learning/react-authentication-25660373/setting-up-an-email-provider?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/setting-up-an-email-provider?u=76281980&t=0)** - [Instructor] Well, now that we talked about what the basic email flow is going to look like, let's get started with seeing how we can send emails from a [Node.js](../../Skills/Web%20Development/Node.js.md) server, right? That's going to be a key part of this whole flow. So we're going to be using SendGrid here, which is really just an easy tool to get started with. So you're going to need to create an account here. I won't walk you through that. It's pretty easy. And what you're going to want to do here is go to settings and then sender authentication. Now this might also be available in your dashboard here. All right, so you should see something like this once you log in. And if you click create Sender identity, that'll take you to the same spot. But really the idea here is that you want to create some sort of email address that you're going to be sending these emails from. Now, ideally, if this is for a business, you're going to have some sort of like contact at something, you know, [xyzcompany.com](https://xyzcompany.com), something like that. But right now, I'm just going to use my own information here as well as a fake email that I've set up for this course. So from name, I'm just going to use my own name here. From email address, this is going to be the email address. You do have to have an account for this because there will be a little bit of setup with that. So I'm going to do shaun.[LinkedIn](../../Skills/Web%20Development/LinkedIn.md).learning@[gmail.com](https://gmail.com). Okay? Again, you are going to have to set up an email address for this, unless you want to use your own personal email address just for testing, which you can also do. And then for reply to, for this,
>
> **[1:33](https://www.linkedin.com/learning/react-authentication-25660373/setting-up-an-email-provider?u=76281980&t=93)** we're going to have to basically put in either the same email address or some other email address, right? This just tells where users to send their emails if they have questions or if they encounter problems. It's just a required field, so I'm going to use that same email address as what we put in there. There we go. For company address, I'm just going to do a fake address here. Alright, that is a required field. Again, if you're planning on actually using this for professional purposes, you'll want to put your real address in there, your real company address in there. But again, just for testing purposes, I'm putting in this address you see here. And finally, I'm going to just say something like Shaun's Email for the nickname. This would just be basically a memo for reminding you which one's used for which. If you have multiple email addresses here. So let's click create, and that should tell you that the sender has been created. And in order to verify this, we're going to have to check our inbox. So a little bit ironic that we have to verify our email address in order to verify our user's email addresses, right? In order to set this whole flow up. So I'm going to close this and I'm actually going to open this inbox up, right? This is the one that I set up just for this purpose. Let's just hit refresh and see if that shows up. And sure enough, please verify your Single Sender. We're going to say verify Single Sender like so. And that should take us back to here. Alright? And you should see that it says sender verified. So really this is going to look very similar to what we did previously, and that should be all the setup that we have to do for our SendGrid account.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Node.js](../../Skills/Web%20Development/Node.js.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Prerequisites:** set up (2), setup (2)
> **URLs:** [xyzcompany.com](https://xyzcompany.com) (1), [gmail.com](https://gmail.com) (1)
> **File Paths:** node.js (1)
> **CLI Commands:** node (1)
> **Cross-References:** we talked about (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)

#### [Creating an email provider API key](https://www.linkedin.com/learning/react-authentication-25660373/creating-an-email-provider-api-key?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/creating-an-email-provider-api-key?u=76281980&t=0)** - [Instructor] All right, well now that we have the basic account set up for SendGrid, we're going to need to get an API key, which is what our application will use in order to let SendGrid know that it's our application, right? This is how most APIs track things like billing, usage, things like that. So what you're going to want to do here is click on this Email API link, and then go to Integration Guide. And you're going to click on the Choose button here for Web API, and then you're going to click Choose for [Node.js](../../Skills/Web%20Development/Node.js.md), right? This will contain some really nicely structured instructions for getting your app set up. We are going to do things a little bit differently, so you'll just have to bear with me here. And the first thing we need to do here is we're going to create an API key. And let's just call this something like, you know, we'll say [React](../../Skills/Web%20Development/React.js.md) Authentication Key, all right? You can call it whatever you want. If you're designing a specific app, you can use the name of that app there, and we're going to click Create Key here. And you can't see this because it's blurred out, but this thing here is the secret key that we're going to use to access the API. Now, what you're going to want to do here is what you see down below, and this is basically set up your environment so that that key is available. Now, I wouldn't recommend using the way that they recommend here. What I would recommend doing instead if you're working with [Codespaces](../../Skills/Software%20Development/Codespaces.md), is going to your [GitHub](../../Skills/Software%20Development/GitHub.md) account settings, right?
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/creating-an-email-provider-api-key?u=76281980&t=94)** So you can do that by going to your account and then clicking on Settings, and then go to Codespaces under that. And as you can see, I've been using the JWT_SECRET here under Codespace user secrets, just because that makes it much easier to persist those things more long term. And you can add a new secret here. And basically what we'll do is we'll call that SENDGRID_API_KEY. And then we're going to paste the value of that inside of there. And last but not least, we just need to select the repositories that we want this to be accessible from. So I'm going to look for this one here. You're going to need to look for whatever one you're working with here, and that's going to be React authentication for me. If you've created a fork or something like that, you're going to need to select that one. And then we're going to click Add Secret. Okay? Great. So now that we've added that secret, what we're going to do next is we're going to go back to Codespaces. All right? And as you can see, mine restarted, which is why it's so nice to have those things in the GitHub secrets as you just saw. And that's going to open this back up. And what you should be able to do once your Codespace reactivates, I believe you will have to restart it in order for those secrets to take effect if you haven't already. What you can do in order to test and make sure that those are accessible is you can just open up a terminal here and you should be able to say echo along with a dollar sign and the name of the secret you want to test.
>
> **[3:07](https://www.linkedin.com/learning/react-authentication-25660373/creating-an-email-provider-api-key?u=76281980&t=187)** And, oops, let me just go back to that there. So if I say echo $SENDGRID_API_KEY, you should be able to see that there. And if you decided to do the JWT secret thing here as well, which I would recommend, you can echo that and you should see that one there too. All right, so the last thing here is we're going to change directories into our backend and we're going to install the SendGrid mail library into here. So we're going to say [npm](../../Skills/Web%20Development/npm.md) install, this is going to be @sendgrid/mail, that's just the NPM package name, and that is the library that we're going to use in order to send emails. So that should be all we need to do. The next thing we're going to do is take a look at how to send emails from a Node.js server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Codespaces](../../Skills/Software%20Development/Codespaces.md) (3), [Node.js](../../Skills/Web%20Development/Node.js.md) (2), [React](../../Skills/Web%20Development/React.js.md) (2), [GitHub](../../Skills/Software%20Development/GitHub.md) (2), [npm](../../Skills/Web%20Development/npm.md) (2)
> **Env Vars:** api (5), sendgrid_api_key (2), jwt_secret (1), jwt (1), npm (1)
> **CLI Commands:** node (2), npm (2), make (1)
> **UI Navigation:** click on (2), go to (2), select the (1)
> **Prerequisites:** set up (3), install (2)
> **Tools:** github (2), terminal (1)
> **File Paths:** node.js (2)
> **Cross-References:** go back to (2)

#### [Sending emails from Node](https://www.linkedin.com/learning/react-authentication-25660373/sending-emails-from-node?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/sending-emails-from-node?u=76281980&t=0)** - [Instructor] All right, well now that we've got our API key and we also have installed the SendGrid library into our [npm](../../Skills/Web%20Development/npm.md) project, the next thing that we're going to do is we're going to send our first email. Now you can see that there's some sample code here, and you can feel free to copy that and kind of edit it to your liking. But we're actually going to do this a little bit differently. And I'm going to just select this. I've integrated the code above, and click Next: Verify integration. And this allows you to test it. And basically this is going to show you whether it's working or not. So this is a very important step here, of course. So let's go back to our editor, and what we're going to do is we're going to create a function that will basically take care of sending emails for us, and then we'll create a test file that will test that function. So here's what this will look like. Inside back-end, we're going to create a new file here and we'll call this something like sendEmail.js. And what we're going to do here is we're going to start off by importing that library that we installed. So we'll say import sendgrid from, and then that's @sendgrid. Slash rather, sendgrid/mail. And the next thing we need to do is we need to set the API key, right? SendGrid isn't automatically going to pick up on our API key in our environment. So we're going to have to say sendgrid.setApiKey, and then that's going to be process.env.SENDGRID_API_KEY, right? Whatever you called it, right?
>
> **[1:31](https://www.linkedin.com/learning/react-authentication-25660373/sending-emails-from-node?u=76281980&t=91)** I called mine that when I set that in [GitHub](../../Skills/Software%20Development/GitHub.md). So the next thing here, it's going to be pretty straightforward, we're just going to say export const sendEmail. And this one's just going to kind of mirror the sendgrid.send function, but it'll make it a little bit more robust if something about these SendGrid API changes. So what we're going to do here is we're just going to say to, from, subject, text, and [HTML](../../Skills/Web%20Development/HTML.md), right? The different parts of the emails that we're going to send, and then we'll say constant message. This is going to have all of those things sort of bundled together. So we'll have to, from, subject, text, and html, just like we had up above. And then what we're going to do is we're going to pass that to sendgrid.send with that new message that we just created. Now, it might seem a little bit like overkill, and oops, I forgot equal sign there. As I was saying, it might seem like a little bit of overkill to, you know, basically destructure this and then explicitly create this new object there. It's really just so that we can see what's going on and to make it easier to do things like set default values later on if we want to do that. So now that we've created this sendEmail function, let's create a test file. We'll just call this one testSendEmail.js. And what we're going to do here is we're going to import that function we just created. So import sendEmail from sendEmail. And now what we're going to do is we're just going to call sendEmail with some data.
>
> **[3:06](https://www.linkedin.com/learning/react-authentication-25660373/sending-emails-from-node?u=76281980&t=186)** So we're going to start off by saying to. And here's a little trick that I'm going to give you when you're testing out email flows like this, instead of having to create two or more email addresses that you, you know, can send emails back and forth to, what you can actually do is you can say, shaun.[LinkedIn](../../Skills/Web%20Development/LinkedIn.md).learning+test1@[gmail.com](https://gmail.com), all right? Now notice that I added this little +test1 thing. Basically, because of the way that email addresses work, you can add plus and then pretty much anything after that. And this will act as sort of like a label for emails later on. All right, so here's what this is going to look like. This basically allows us to send an email from the same email address, so shaun.linkedin.learning@[gmail.com](https://gmail.com). But basically it allows us to send it to the same email address while having a little bit of differentiation there, and seeing what the to and from addresses actually are. And oops, I forgot the colon there. So let's add that. And then we're going to add the subject. So we'll just say subject and we'll say, does this work? And then for the text we're going to say, if you're reading this, got to put that little backslash in there, so that we can have that single quote in a single quote string. We'll say, if you're reading this, then yes. All right? And that's pretty much all we need to do. Now you can add HTML in there. That is an option. And basically what that'll do is if you put some HTML like this, right?
>
> **[4:39](https://www.linkedin.com/learning/react-authentication-25660373/sending-emails-from-node?u=76281980&t=279)** So you can say something like h1, hello, right? That will actually be rendered if HTML is enabled in the user's email inbox, right? Usually that's not a good idea to add if you can get by with just regular text, because email tends to be overused and misused in many cases in marketing emails, stuff like that. So many inboxes will actually penalize you if you use HTML and it will often tend to label it as marketing material or promotional emails. So anyway, let's try this out. We're just going to say sendEmail, and then we'll say .then, right? This is a promise, you can use async and await with this, but just because this is a test file, we're going to use promises. We'll say something like then, and we'll say console.log. And then we'll say something like email sent. Hopefully we see this message. Otherwise, if something goes wrong, we're going to say .catch. And if there's an error, we're just going to log out that error so we can see what went wrong. All right, and that's pretty much all we need to do. Let's just move this .then thing down a bit and let's run this code to see if it works. So we're just going to open up a terminal, make sure you're in the back-end, right? So say cd back-end. And now we're just going to say node testSendEmail.js, and hopefully this works. And oops, I forgot that we're not using an ES6 module here, so we just have to change this now to const sendEmail equals require sendEmail, right? This is on the [Node.js](../../Skills/Web%20Development/Node.js.md) server,
>
> **[6:13](https://www.linkedin.com/learning/react-authentication-25660373/sending-emails-from-node?u=76281980&t=373)** and we haven't set that up with import export syntax. So we're just going to use that. And for sendEmail, we'll also change this to module. Well here, we'll just say const sendEmail rather. And then we'll say module.exports equals, and then we'll have sendEmail in there, all right? And we also need to change this here, so we'll need to say const sendgrid equals require sendgrid. Oops, there we go, /mail. And let's try that one more time. So we're going to run this, and sure enough, we see that it says email sent. So what we hope to see now is first of all, if we go into SendGrid, we're going to hope to see that it worked, right? So you should see it worked here. If you click on that button, that just means that you successfully sent an email to somebody. And if you go back to your email account here, the one that I was using, right? I'm just going to go into the inbox and if we hit Refresh, and sure enough, we see the email shows up, right? So anyway, that is the basics of sending an email through Node.js using SendGrid. So we're going to be using this to implement that email verification flow that we discussed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (6), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2), [Node.js](../../Skills/Web%20Development/Node.js.md) (2), [npm](../../Skills/Web%20Development/npm.md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Code Identifiers:** sendemail (12), testsendemail (2), setapikey (1)
> **Env Vars:** api (4), html (4), npm (1), sendgrid_api_key (1), es6 (1)
> **CLI Commands:** make (3), node (3), npm (1), cd (1)
> **File Paths:** testsendemail.js (2), node.js (2), sendemail.js (1), process.env (1)
> **Definitions:** is a  (3), is an  (1)
> **Cross-References:** go back to (2), we discussed (1)
> **URLs:** [gmail.com](https://gmail.com) (2)

#### [Integrating verification tokens](https://www.linkedin.com/learning/react-authentication-25660373/integrating-verification-tokens?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/integrating-verification-tokens?u=76281980&t=0)** - [Instructor] All right, so now that we're sending emails, the next thing that we're going to do is we're going to start integrating this idea of verifying a user's email address into our application. Now, a few things I want to point out is if you're starting off with the starting code in the exercise files, you're going to want to replace these emails here with whatever emails you set up in the previous step, right? Don't try and send these with the email addresses that I have here because it just won't work. And the other thing you'll notice from here on is that I have a slightly different email address here than what I showed you previously. I just had some problems with my account with that old email address. Actually, after I stopped recording, I had some problems with my account and I just had to set up a new one so that I could implement all of this and show you how to do it. So those are the only two things I wanted to mention there. Let's jump in now and what we're going to do is we're going to open up our server, right? So go into server.js and we're going to integrate this email verification flow into this signup route first. So the first thing that we're going to do here is we're going to use this uuidv4 package, right, this uuid package that we installed in order to generate user IDs. We're going to actually use this to generate a verification string that users will have to go to the email address to find. All right, you'll see how this works in much more detail in a minute. Let's start off though by scrolling down into the signup route, and underneath where we generate
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/integrating-verification-tokens?u=76281980&t=94)** the password hash and the id, we're going to generate the verification string as well. So here's what that'll look like. We're going to say const verificationString equals uuidv4. All right, so just using that same package to do this. There are other packages you can use for either one of these cases, but this is just a nice sort of dual purpose use case here. And now that we have the verification string, the next thing we're going to want to do is add that on to the user that we add to the database, right? So we're going to add that into here, verificationString, and we're going to leave isVerified as false. This is when we're going to start using this property here because this is how we'll keep track of whether or not a user has actually verified their email address. All right, so that should be all we need to do there, at least as far as creating the user in the database. The next thing is before we create that [JSON](../../Skills/Web%20Development/JSON.md) web token and sign it and send it back to the user, we're going to send an email that incorporates this verification string into a link that the user will be able to click to verify their email. So here's what that's going to look like. We're going to say try, right? We'll have this in a try catch block here because there is a possibility that the send email function will run into an error, and then we'll add catch e, and here we'll just have console.log(e) and we'll say response.sendStatus and send back a 500 status code. And now that we have that,
>
> **[3:09](https://www.linkedin.com/learning/react-authentication-25660373/integrating-verification-tokens?u=76281980&t=189)** let's try calling the sendEmail function that was just imported for me automatically up at the top. And we're going to say to, and this is going to be email, right? The email that the user signed up with. So that's how we'll know where to send that. And from this one, we're just going to hard code to whatever we put in the testSendEmail thing, right? That sender that we set up there. So we'll try this one, we'll copy that, paste that in here, and the next one here is going to be the subject. Here's where we get to decide on the exact wording we want to use for this email. So we'll say something like, Please, oops, let's put that in quotes there, "Please verify your email." And after, that we're going to say text. And I'm going to put this in back ticks here just so I can put that in a slightly longer string than that. All right, and we'll say, "Thanks for signing up." We'll say, "To verify your email, please click here." And for this, here's what we're going to have to do. We're going to have to use the URL of the frontend that we're running. Now, yours is going to be different than mine here, so you will have to make sure to change this, but here's what this will look like. First of all, we'll insert the verification string that we generated, so verificationString like so. And now that we have that, we're going to need to actually run our frontend again, right? Because my codespace shut down again, so let's just create two new terminals like so. And up here, we'll say cd front-end and we'll run this thing by saying [npm](../../Skills/Web%20Development/npm.md) run dev.
>
> **[4:45](https://www.linkedin.com/learning/react-authentication-25660373/integrating-verification-tokens?u=76281980&t=285)** And then when that opens up in the ports here, you should be able to get that. In fact, it looks like the server URL and the frontend URL were already there. We can just copy this and I'm going to go back into server.js and we're going to paste that right here. And that's going to be the verification string. We're also going to add this other segment before that that says verify email. We're going to have to come in and actually create a route for that in the next video as you'll see. All right, so that is all we need to do there. Again, you can kind of adjust this because it's got multiple lines there. That's why I put the back ticks around it because this is a pretty long string to just include like that. So now that we're sending the email, that's really all we have to do. And let's add await here because that does take, you know, that is an asynchronous operation, of course. All right, and that really should be all we need to do there. So the last thing that we're going to do is we're going to test this thing just to make sure that it sends the right email. So let's run our frontend and backend once again. We're going to go into our next terminal here, change directories into the backend if you don't already have that running and we're going to run that with node server.js. And, of course, we'll need to change both of these to public again, so change port visibility to public, and port visibility here is going to be public as well. There we go. And now that we've done that, let's open this thing back up. We'll open up our frontend, all right, by clicking that little globe icon.
>
> **[6:17](https://www.linkedin.com/learning/react-authentication-25660373/integrating-verification-tokens?u=76281980&t=377)** We're going to say continue, in case you run into that, and we're going to sign up here because again, that's the route that we just added that verification to, so we'll try signing up here with whatever email it is you want it to send to. And you can always add those plus labels after the email, right, the first segment of the email if you want to actually be able to create multiple users on the same site. So we're going to say sw_linkedin_learning, that's my email right here. And then we'll say plustest1@[gmail.com](https://gmail.com). All right, and then for the password, I'm just going to say abc123. Same thing here, abc123. Obviously, you'd want your users to have a more elaborate password than that. But for testing purposes, this makes it easier. So let's click Sign Up here. And what we should see is that'll take us to our user info page, but what we're going to want to do is go to our email and check to make sure that we received it. Now, you might receive this in your spam, so you can always check spam here if you're not receiving any emails. Sometimes it does take a second or two. And sure enough, what we see is we received this email. Now, again, please check your spam because it might be in there. You can always move it to the inbox if you want to. And you'll see that the URL that's right here isn't exactly the URL that we saw earlier, right? That's because what SendGrid does is it actually replaces it with its own URL so that you can track when people are clicking specific links in your emails, right? It helps for marketing purposes, things like that.
>
> **[7:51](https://www.linkedin.com/learning/react-authentication-25660373/integrating-verification-tokens?u=76281980&t=471)** But anyway, this link will actually forward you to the address that we put in there. So the next thing that we're going to want to do is figure out how we want to manage that in our app.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1), [npm](../../Skills/Web%20Development/npm.md) (1)
> **Code Identifiers:** verificationstring (3), isverified (1), sendstatus (1), sendemail (1), testsendemail (1)
> **CLI Commands:** make (3), find (1), cd (1), npm (1), node (1)
> **Env Vars:** url (6), json (1)
> **File Paths:** server.js (3)
> **Definitions:** is a  (2), is an  (1)
> **Prerequisites:** set up (3)
> **UI Navigation:** go to (2)

#### [Adding a "please verify email" page](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-please-verify-email-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-please-verify-email-page?u=76281980&t=0)** - [Instructor] Okay, so now that we've integrated that link into our server, the next thing that we're going to do is we're going to take a step back and go back to after the user signs up, right? Currently when the user signs up, they're sent straight to the homepage, right? That is the user info page. And we probably want to tell them that they need to verify their email before we do that. So as you can see, if you check out the starting state for the exercise files here, you'll see that I added this "PleaseVerifyEmailPage." Basically the only purpose of this thing is that it will display this message down here, telling the user that we've sent a verification link to the email address. And after three seconds, it will automatically navigate to the home route. All right? And you can see if you go to the app component that have added a route for this one where we display the PleaseVerifyEmailPage on the path, please verify. So the last thing we need to do here, and I've left this up to you just so that you would see this new flow sort of taking place, is on the signup page we just need to make sure that when the user signs up, we send them to the PleaseVerifyPage instead of the home route. All right, so here's what we're going to do. We're just going to say Navigate to please verify. You can copy and paste that to make sure that it's the same thing there, but there we go. And now that we have that, that should be all we need to do. So let's test this thing out. Make sure your front end and back end are running. And let's log out here. That'll send us back to the login page.
>
> **[1:32](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-please-verify-email-page?u=76281980&t=92)** So let's try this signup page again, and we're going to try here. We'll do the other one here that we set up. So sw.[LinkedIn](../../Skills/Web%20Development/LinkedIn.md).learning, and we'll do test2@[gmail.com](https://gmail.com), right, because we already did test1. And then for the password, you can choose whatever you like there. And then let's click sign up and if all goes well, sure enough, it says, "Thanks for Signing Up," sends us to that PleaseVerifyEmailPage and then automatically redirects us to the UserInfoPage.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** make (3)
> **UI Navigation:** go to (1), navigate to (1)
> **URLs:** [gmail.com](https://gmail.com) (1)
> **Cross-References:** go back to (1)
> **Exercise Files:** exercise files (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Adding a verify email route](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-verify-email-route?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-verify-email-route?u=76281980&t=0)** - [Narrator] All right, so now that we have that please verify email page, let's go to our server side and we're going to add a route that will allow us to verify the user's email. Now really all that we're going to do here is we're going to have the front end, right? We already put that link, if we scroll down here. We already put that link inside this email that we're sending to the user. And as you can see, this is going to take them to the front end of our application. So essentially what we're going to do, and we're going to do this in the next video, is we're going to create a page that will basically be sitting at this route and that page, once the user goes there, is going to take the verification string that's in the URL and send it to the endpoint that we're going to create in this video. So we're creating the endpoint first to verify user's email. So let's scroll down and what we're going to do is we're going to add a new route here. We'll call this app.put, right, since this is a update endpoint, and we're going to say /api/verify-email. Okay, and inside this thing we're going to have our request and response. This is going to have to be an async method here. So we'll just say async like so. And then inside here we'll say const and we'll just expect that the front end is going to send a verification string property in the request body. So we'll say verificationString, and then we'll say equals, oops, there we go, req.body. And now that we've done that, we're going to find the user in the DB with that verification string.
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-verify-email-route?u=76281980&t=94)** So this is going to be a little bit different than what we've done before. We're going to say user or const user, rather, equals db.users. And then we're going to say .find and find the user, as I said with the verificationString, right? That's what we set up here. If we go up to the signup route just to show you and make sure you understand what's going on, right? Remember that we added this verificationString in there when we create the new user. So let's scroll back down. And what we're going to do here is we're going to check if that verificationString is equal to the verificationString from the request body. And now what we're going to do is we're going to check and if there is no user, right, so if there's no user with that verificationString, then what we're going to do is we're going to send back a 401 status code. So we'll say response and here we'll say return, that is, res.status(401).[JSON](../../Skills/Web%20Development/JSON.md) and then we'll just send a message back so they know what's going on saying the email verification code is incorrect and that should be all we need to do there. So the next thing here of course, is if that does match a user, then we want to set that user's is verified property to true. So here's what that'll look like. We're just going to say user.isVerified = true. That's all we have to do there. And now we're going to send the user an updated JWT. So here's what that'll look like. We're going to say const.
>
> **[3:06](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-verify-email-route?u=76281980&t=186)** We'll get the user's ID, their email, their info, and their verificationString, as well as isVerified here from that user object. And actually, we don't even need this verificationString. In fact, the user probably shouldn't have that. There's no reason for them to have it. And now that we've done that, here's what that will look like. We're going to say jwt.sign, we're going to sign the id, email, isVerified, and info into that JSON web token with our process.env, oops, let me try that again. That should be a comma right there. We're going to say process.env.JWT_SECRET. And now we'll add that expiresIn: '2d', just like we've been doing. And last but not least, we're going to have error and token. And here's what that'll look like. We're just going to say if there's an error, pretty much the same as what we did up above. In fact, we can just take this up here if we want to. We'll just copy that and paste that down in there. That's just going to save us a little bit of time. And now that's all we should need to do. So that is our verify email route. We're going to test this thing when we actually create the corresponding front end page.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (2)
> **Code Identifiers:** verificationstring (8), isverified (3), expiresin (1)
> **CLI Commands:** find (3), make (1)
> **Env Vars:** url (1), jwt (1), json (1), jwt_secret (1)
> **UI Navigation:** scroll down (2), go to (1)
> **File Paths:** process.env (2)
> **API Endpoints:** /api/verify-email (1)
> **Cross-References:** in the next (1)

#### [Creating a verification landing page](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-verification-landing-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-verification-landing-page?u=76281980&t=0)** - [Instructor] Alright, so now that we have that verify email route on the server, the last thing we have to do here is hook things up on the front end. So as you can see, I've done most of the work here for you. We have this email verification landing page thing that's set up. And basically what this does is, as you can see here, it pulls the verification string out of the parameters, right? I'll show you that in just a minute here. So it pulls the verification string out of the route parameters, and then it sends that to the server, right, to that route that we just created there. And depending on what happens, right, depending on if there's an error or not, it sets this is loading thing to false and this is success thing to either true or false. And down here in the JSX, you can see that there's really three possibilities that it'll display. The first thing is a loading message showing that it's currently trying to verify the user's verification string. And then if it's successful, it will give us an email verification success and stay there. If it's a failure, it's going to give us email verification fail. And you can see both of those here. Basically, they're the same thing, just with a slightly different message depending on whether or not the verification was successful. Alright, so the last thing I did here is I added the email verification landing page to a new route here under slash verify email. That should be the same one that we have in the email that we sent out. So let's test this thing out, right? Let's first of all, make sure that our front end and back end are running and
>
> **[1:33](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-verification-landing-page?u=76281980&t=93)** that the ports are both set to public. And then we should be able to go to our application here. We're going to log out because we're going to want to sign in here obviously. So we'll say sign up. And then we're going to do that same SW [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) learning, and I'm going to say +test3 here @[gmail.com](https://gmail.com). And now we're going to say ABC123 and ABC123. You can put in whatever password you want and we'll click sign up. And now we're going to see that same thing. And if we go back to our email here, we should see, all right, that sure enough we have this "please verify your email," email. Now this is not the original one. This is actually for test two as you can see here. So what we're actually going to do is we're going to go here and we can actually log out. And then if we go back to this, we're going to see that that will route us to that page, right? So it should take us to verify email slash, and then it's got that number. It looks like that didn't work for some reason, so let's check out why. And oh, that's because I actually added the wrong URL here, so I forgot to add the route parameter thing. And that's just fine because I wanted to talk about this anyway, right? So we should call this verification string. That's string does have to match the string that we have inside the landing page here. So verification string, make sure you're spelling that exactly the same. That's an easy set of words to misspell, right? And now this should work. So let's go back to here. And oops, it looks like something went wrong there. And I believe that that is actually just because we didn't restart the server.
>
> **[3:06](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-verification-landing-page?u=76281980&t=186)** All right, so let's go back here. And since we've made changes to the server, we obviously need to restart that and that should be good. Let's just make sure that that's still on public. Yep, both of them are still on public, so we'll try this again. All right, we're going to just close that tab there and go back to this link here. And there we go, we see success. You saw that loading message there right before, and now if we click go to homepage, sure enough, that takes us to the user info page. And you can see also that the act of verifying your email, if we go to application actually logs the user in, right? It sets that token that we send back. Now, that's not something you have to do if you don't want to log your user in when they verify their email and you want them to actually have to log in again, as many sites do. Then just don't send back the [JSON](../../Skills/Web%20Development/JSON.md) web token when you do that. So anyway, at this point we have a pretty complete email verification flow, so congratulations for walking through this with me.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Env Vars:** abc123 (2), jsx (1), url (1), json (1)
> **Cross-References:** go back to (4)
> **CLI Commands:** make (3)
> **UI Navigation:** go to (3)
> **URLs:** [gmail.com](https://gmail.com) (1)
> **Definitions:** is a  (1)
> **Prerequisites:** set up (1)


### 3. Resetting Passwords

[↑ Back to Table of Contents](#table-of-contents)

#### [The basic password reset process](https://www.linkedin.com/learning/react-authentication-25660373/the-basic-password-reset-process?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/the-basic-password-reset-process?u=76281980&t=0)** - [Instructor] All right, now that we've got the email verification process working for our application, the next thing we're going to do is move on to implementing another very important auth-related feature of any website, and that is allowing users to reset their passwords. Now, I'm sure it's happened to all of us, that we forget our password for a given website, and in those situations, it's very important that websites provide us with a backup method for resetting our password. Now, the process for doing this is actually very similar to the email verification process, so let's just go over it here and make sure we understand the basic steps before we implement it. So the first step here is that the user clicks the Forgot password button on the login page, and this will then take them to a page where they have to enter their email address. And the next step is that the user enters their email into that page and clicks Submit, which will send this email address to a server endpoint. And this server endpoint, basically what that does, is it checks to see if a user with this given email address even exists, right? If it does, the server generates a random verification hash, and it will send a link with that hash to the user's email. Again, this is very similar to the way the initial email verification flow works. Now, when the user clicks the link in the email, they're going to be taken to a reset password landing page that will ask them to enter their new password. Once they've done this,
>
> **[1:33](https://www.linkedin.com/learning/react-authentication-25660373/the-basic-password-reset-process?u=76281980&t=93)** the new password that they've entered will be sent to another server endpoint, which checks to make sure their verification hash matches. And if it does, it will replace the user's old password hash in the database with a hash created from the new password that they've provided. And that's really it. Once the user has reset their password, all they need to do is log into our app now with that new password. Well, that's the basic password reset process, so let's get started implementing it.

> [!info]- Semantic Content
>
> **CLI Commands:** make (2)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)

#### [Adding a "forgot password" page](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-forgot-password-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-forgot-password-page?u=76281980&t=0)** - [Speaker] Alright, so now that we know the basic password reset process and how that's going to all fit together, let's get started here. And I've taken the liberty of providing you here with this forgot password page. It's a pretty straightforward page. There wasn't really anything new here, which is why I just created it and gave it to you, right? It just contains a single email address input that the user can enter their email address into. And when they click that, right when they click the button after entering their email address, that's going to send a request to this endpoint on the server that we're going to have to create ourselves. All right? And once that happens, it's going to display a little success message and it will automatically navigate the user back to the login page, which they'll be needing to use once they reset their password. So that's pretty much all we need to do there. I added another route for this already. So all that's left for you to do is check out the starting state for the code here and make sure it works for you. So what you should be able to do here, running the version of the app that's found in the exercise files for this video is you should be able to finally click on this, forgot your password link, and that'll take you to this little page, which is the component that I just showed you. And you can enter in your email here and then click send reset link. And as I said, that will send a request to our server, which is where we're going to go next.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **UI Navigation:** click on (1)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [speaker] (1)

#### [Sending a "reset password" email](https://www.linkedin.com/learning/react-authentication-25660373/sending-a-reset-password-email?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/sending-a-reset-password-email?u=76281980&t=0)** - [Instructor] All right, so now that we have that forgot password page, the next thing that we're going to do is we're going to create the route that that page is going to make the request to. So here's what this is going to look like. We're going to start off here by going down below verify email, and this server file is getting pretty long, so if you want to break this up into multiple files, feel free to go ahead and do that. But just to keep everything in one place and to avoid having to refactor everything, I'm going to leave them all in this file. So we'll start off here by saying app.put. Right, we're just going to use a put route for this. Doesn't really matter which one you use, but we'll just do app.put, and then this is going to be /api/forgot-password. And we're going to need a way to include the user's email in this and the easiest way to do that is just to add that as a URL parameter, like so. Okay, so for the handler here, which, as with all of the other ones, is going to be async, what we're going to do is we're going to start off by getting that email. So we'll say const email equals, there it is, req.perams. And then once we've done that, we're going to want to find the user with that email and generate the password reset code and basically set it on that user, which will allow us to look them up later when they click that link. If that sounds complicated, don't worry too much about it. You'll see this in just a minute what this looks like. So we'll start off here by getting the user, all right, and we're going to need to just do what we did basically up in verify email,
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/sending-a-reset-password-email?u=76281980&t=94)** but with this email address instead of some kind of code. So we'll say const user equals. This is going to be db.users.find. We're going to find the user whose email address is equal to that email there. And now that we've done that, here's what this is going to look like. We are going to generate a password reset code using that UUID package. So we'll say const passwordResetCode equals uuidv4, just like we generated the user's ID and the email verification string. And now that we've done that, let's go down to here and we're going to say user.passwordResetCode. All right, we'll just use that same name there that we had, equals passwordResetCode. And now that we've set that on the user, we're going to send an email to that user with the same kind of link as we had in the verify email endpoint. So here's what that will look like. We're going to say try, we're going to use await sendEmail like so. And inside here we're going to send that to the user's email, of course. We'll say that it's from whatever our original, you know, sender was. So I'm actually just going to go up here and get that from one of the places where we send an email just to make sure there's no typos. All right, I'll copy that and paste that here. And now that we've done that, we're going to say subject. This is going to be Password Reset. And finally we're going to say text, and inside here,
>
> **[3:09](https://www.linkedin.com/learning/react-authentication-25660373/sending-a-reset-password-email?u=76281980&t=189)** and I'm just going to use back ticks here again so that we can actually insert that. We're going to say to reset your password, click this link. As we saw, SendGrid is going to replace that link with their own so that we can see when users have actually clicked on these things. You can always turn that off by the way too, but it's easiest to just keep it on. And for this, we're going to need to get the front end link again. So we'll go into here, copy the front end URL, and we'll go back and paste that like so. And now we're going to say /reset-password and that's going to be slash and this is going to be the passwordResetCode that we generated. All right, so now that we've done that, the last thing we need to do is decide what we want to do if an error occurs. So we'll say catch (e), and in here we're going to say console.log. We'll just log that out for now and we'll send back a status code. So sendStatus(500) indicating that something went wrong there. And otherwise, right, if everything goes well, we're going to send back a status code of 200, which we'll do inside here. So we'll say response.sendStatus(200). And that should be all we need to do there in order to allow the user to reset their password. Well, at least the first part of this process anyway. So let's test this out. What we're going to do is enter in an email to forgot password. And actually since we made a change to the server, we are going to want to restart that server. All right, so we'll go into backend, restart the server like so, node server.js. And now that we've done this,
>
> **[4:44](https://www.linkedin.com/learning/react-authentication-25660373/sending-a-reset-password-email?u=76281980&t=284)** what we're going to do is we're going to say, can enter in whatever you want here. Let's just say that sw.[LinkedIn](../../Skills/Web%20Development/LinkedIn.md).learning+test3 has forgotten his password. That's me, and then we'll say @[gmail.com](https://gmail.com). And if we click Send Reset Link, what we'll hope to see here is this message saying success. And if we check our email here, again, you may have to go into Spam and it might also take a second there, but you see that it says password reset, and this is what we're going to want to click on in order to take the user to the next step in the process. And let's just move this out of Spam, right? It's not spam, we know who that is that's sending it. All right, so we'll move it into the inbox and we see that that's now there. So the next step in the process is going to be creating a landing page that clicking on this link will take you to.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Code Identifiers:** passwordresetcode (4), sendstatus (2), sendemail (1)
> **CLI Commands:** find (3), make (2), node (1)
> **Env Vars:** url (2), uuid (1)
> **File Paths:** server.js (1)
> **URLs:** [gmail.com](https://gmail.com) (1)
> **API Endpoints:** /api/forgot-password (1)
> **Cross-References:** as we saw (1)

#### [Creating a "reset password" landing page](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-reset-password-landing-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-reset-password-landing-page?u=76281980&t=0)** - [Instructor] All right, so the next step in this process is a password reset landing page. Basically, this is what we want the user to go to when they click that link that we have in the password reset email, right? So when they click this, this is obviously changed by send grid, so that they can track when users click these things, as I've mentioned. But ultimately it's going to take them back to a route. And this page here, which I've included in the beginning state of the exercise files, is what we want the user to see. Now this works in a very similar way to what we saw with the email verification flow, where it's got this page, but it's also got like a success and a failure state. And ultimately what the page is going to do is it's going to load the password reset code, right? That's what we set in the email that we sent out. It's going to load that and then send a request to the server using that parameter as part of the URL. So we're going to need to go back and actually add that to the server. But other than that, it's pretty straightforward. As you can see, it's got this form here that allows us to enter the same password twice, right? This is the set password value and set confirm password value input. And once you've entered that form, that's when this request is going to be made and it will display either the password reset fail, or success depending on what the result of that operation was. Alright, so feel free to look through this page and the other components that it displays.
>
> **[1:35](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-reset-password-landing-page?u=76281980&t=95)** I'll leave that up to you. And the last thing to know is that the app component here has this route added. That's reset password. Password reset code that might look familiar to you from the URL that we put into the email that we sent out for resetting a password. And that's where the user is going to land when they click that link. So that's just going to display the password reset landing page. So once you've checked out these changes and added them to your application, you can just copy these over if you want to, what you should be able to do here, and make sure that your front end and backend are running before you do this, you should be able to click this link, and it'll take you to the application. And you should see a form that looks just like that, where you know you can go in here and enter whatever new password you want. And as long as you enter the same thing here in this next input, you'll be able to click that reset password button. And that is ultimately going to make the request to the server. And that's the next thing that we have to do is create that server endpoint.

> [!info]- Semantic Content
>
> **CLI Commands:** make (2)
> **Env Vars:** url (2)
> **Exercise Files:** exercise files (1)
> **Definitions:** is a  (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Adding a "reset password" server endpoint](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-reset-password-server-endpoint?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-reset-password-server-endpoint?u=76281980&t=0)** - [Instructor] All right, so the last step in this whole reset password flow is to create the endpoint that will actually reset the password once the user enters in their new password into those two inputs. So we're going to go down to the bottom here, and we'll add a new put route, we've been having a lot of those in this application so far. And we're going to say for the path /api/users, and we're going to use the passwordResetCode. All right, so we'll say passwordResetCode like this, just like we did from the front-end, and /reset-password as the... Oops, let's try that again. As the path. And then for the callback here, what we're going to do is we're going to start off by getting that passwordResetCode from the path, so we'll say const { passwordResetCode }, and then we're going to say = req.parameters. Then, down here, we're going to say const { newPassword }, this is what we're sending along with that request from the front-end. And that's going to be in the req.body, as we saw in the page. All right, and then, what we need to do is we need to find the user whose passwordResetCode matches this one here. So here's what that's going to look like. We're going to say const user = db.users, rather, .find, and we're going to find the user whose passwordResetCode, as I said, matches the passwordResetCode from above. And now that we have that, well, first of all, you know, you can put in that whole, like, you know,
>
> **[1:33](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-reset-password-server-endpoint?u=76281980&t=93)** if user doesn't exist thing there if you want to, I'm going to just do that real quick. So we'll say res.sendStatus, we'll send a status of 404 not found. As in the user with that passwordResetCode wasn't found. And, otherwise, what we're going to do is we're going to change that user's password by generating a new hash. So we'll say const newPasswordHash = await bcrypt, right, we're using that library from before, .hash, we'll hash the newPassword that they sent along with 10 iterations, same as what we had before. And last thing here we need to do is we're going to update that user by saying user.passwordHash, that's going to be equal to the newPasswordHash. And the last thing we need to do here is we need to get rid of that passwordResetCode so that the user can't just always use that to reset their password, right? So we're going to say user.passwordResetCode, and we're just going to say delete in front of that, like so, and that will get rid of that. So, now, we just need to save the database by saying saveDb, like so, and then we'll just send back a status code of 200 to indicate to the front-end that everything went well. So here's what that'll look like. We're going to say res.sendStatus, and we're going to send a 200 status code there back to the front-end. And that should be all we need to do. So let's give this thing a try. We're going to restart our server, of course,
>
> **[3:06](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-reset-password-server-endpoint?u=76281980&t=186)** so we'll open up the back-end. We're going to stop that and restart it like that. And now, what we're going to do, let's make sure that our ports are still both public. Okay, looks good. All right, so now what we need to do is test this out, and we're going to go back to our application here, all right? We'll actually go back to the plain old Log In page, so that we can reset our password from there. We're going to have to start this flow all over again, because of some changes that we've made. All right, so let's click on Forgot your password?, and I'm going to enter in here, we'll enter in sw.[LinkedIn](../../Skills/Web%20Development/LinkedIn.md).learning, and we'll use test3 again. We already sent that, but when we sent that, we actually didn't have the logic fully in place for adding that to the database. So we'll just add that again and say @[gmail.com](https://gmail.com), we're going to click Send Reset Link. And what we hope to see now, and you can double-check this inside your code now, if we open up db.[JSON](../../Skills/Web%20Development/JSON.md), we should see with test3 that this now has a password reset string. And, actually, it looks like it doesn't. So maybe what we forgot here, if we go to the whole forgot password email thing, I think what we forgot... Yep, sure enough, we forgot to call saveDb in here in order to make that happen. So let's just add that, and we're going to restart our server again. So we'll stop that and restart it like so. All right, so let's give that a try again. What we're going to do is go back to our front-end, go back to Forgot your password?, and try that same flow again. So we'll just enter in that same email, test3@[gmail.com](https://gmail.com),
>
> **[4:41](https://www.linkedin.com/learning/react-authentication-25660373/adding-a-reset-password-server-endpoint?u=76281980&t=281)** and now, we'll click Send Reset Link. And while that's working, let's go back here and double-check that. So open up db.json again, and what we should see is, sure enough, it now has this passwordResetCode. So good example of how a little thing, one line that you forget, can make a big difference in how your app works. Now that we have that though, let's go and look for that email in our inbox. And it looks like that hasn't arrived yet, so we're just going to have to wait for that. All right, and there it is. We're going to want to take the second one here, because that is the last one that we sent. So let's try clicking this, and if all goes well, we should see that that gives us the Reset Password page. So we'll enter in a new password here. Now, we didn't add any logic to check whether this is actually a different password from before. That's a pretty easy thing to check, you just need to compare the passwordHash with the newPasswordHash, I'll leave that up to you though, and we'll click Reset Password now. And, sure enough, we see the Success message. So now let's go back to the Log In page and try logging in with that new password. So linkedIn.learning, we'll do test3 again, @[gmail.com](https://gmail.com), we'll enter in our new password, whatever you chose for that. And if we click Log In, sure enough, that comes up with our user info page. All right, so that is the final step in the password reset process. So congratulations for adding this to the app.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (2)
> **Code Identifiers:** passwordresetcode (11), newpasswordhash (3), newpassword (2), sendstatus (2), passwordhash (2)
> **CLI Commands:** find (3), make (3)
> **Cross-References:** go back to (5), as we saw (1)
> **URLs:** [gmail.com](https://gmail.com) (3)
> **File Paths:** db.json (2)
> **UI Navigation:** click on (1), go to (1)
> **API Endpoints:** /api/users (1)


### 4. OAuth

[↑ Back to Table of Contents](#table-of-contents)

#### [OAuth basics](https://www.linkedin.com/learning/react-authentication-25660373/oauth-basics?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/oauth-basics?u=76281980&t=0)** - [Instructor] So far, we've seen how to add authentication to our app using [JSON](../../Skills/Web%20Development/JSON.md) web [Tokens](../../Skills/Web%20Development/Tokens.md). And we've also seen how to implement email verification and password reset functionality. So the next thing that we're going to take a look at here is how to add something called OAuth to our applications. Now, put simply, OAuth is when we allow our users to log into our site with credentials from some other site, right? Put another way, they can use another site's credentials instead of credentials, like a email and password that they've created specifically for our site. Now, probably the most well-known case of this is the login with [Google](../../Glossary/Service/Google.md), or login with [Facebook](../../Glossary/Service/Facebook.md), or login with [GitHub](../../Skills/Software%20Development/GitHub.md) buttons that you see on many sites. In this course, we're going to just be seeing how to add a login with Google Button to our site, but the concepts that we'll be covering here will be very similar for all OAuth providers. Now, before we get started, let's take a look at what OAuth is and how exactly it works. And first of all, when we say OAuth, we actually mean OAuth 2.0. OAuth 1.0, which is the original version of OAuth, is now deprecated and it just shouldn't be used anymore. I just wanted to clarify that point for you. All right, so really OAuth has three main parts, right? There are, in other words, three parties that are involved in logging in with OAuth. The first one is the user, which is the person who's trying to log into your app. Now, the second one here is your app itself. This is the site that you're going to build, right?
>
> **[1:35](https://www.linkedin.com/learning/react-authentication-25660373/oauth-basics?u=76281980&t=95)** The site that we built with the user info in our case here. And the third party, this is where OAuth is significantly different than what we've seen so far. The third party is something called the service provider, and this is the third party app such as Google or [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) or Facebook that your app wants to allow the user to log in with. So the basic process for logging in with OAuth is going to look like this. Step one is that we'll need to generate a special URL that we can redirect the user to, right? This will be a URL on the service provider's site. So on Google's site, for example. And it will contain information about what we're asking the user for access to. So for example, do we just want basic profile information? Do we want to be able to make changes to their account on the service provider? Things like that. And that's the kind of thing that we'll be specifying in this URL. So the second step is that once we've generated this URL, if the user clicks on the login with Google or login with Facebook, or login with LinkedIn button, we're going to send them to that URL. And this will display a page asking them usually which account they want to use, and also asking if they want to give our app access to the information that we've requested. Now, if they say yes to this, then the service provider will redirect them back to our site with a special code that our site can use to load the user's information. And after that, our site will actually send that code back
>
> **[3:09](https://www.linkedin.com/learning/react-authentication-25660373/oauth-basics?u=76281980&t=189)** to the service provider, and the service provider will send that information back to our site. And finally, once we've gotten access to the user's info, our site can use that information however we want. So for example, we could create a new account in our database from that user's information, or if the user already has an account with that email in our database, we're going to decide how to combine the service provider's info with the info that we have. And that's really all there is to it. So without further ado, let's see how to actually add all of this into our full Stack [React](../../Skills/Web%20Development/React.js.md) application that we've built.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google](../../Glossary/Service/Google.md) (5), [Facebook](../../Glossary/Service/Facebook.md) (3), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (1), [Tokens](../../Skills/Web%20Development/Tokens.md) (1)
> **Env Vars:** url (5), json (1)
> **Analogies:** for example (3), such as (1)
> **Versions:** 2.0 (1), 1.0 (1)
> **CLI Commands:** make (1)
> **Tools:** github (1)
> **Definitions:** in other words (1)
> **Speakers:** - [instructor] (1)

#### [Setting up OAuth on Google](https://www.linkedin.com/learning/react-authentication-25660373/setting-up-oauth-on-google?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/setting-up-oauth-on-google?u=76281980&t=0)** - [Instructor] All right, so now that we've learned the basics of how OAuth works, we're going to take a look at an example of how to incorporate OAuth into our application. Now, as I said, there are many different OAuth providers that we could choose from, but we're going to be using [Google](../../Glossary/Service/Google.md) since it's by far the most popular. So we're going to be adding a login with Google button to the login page for our app. And in order to do this, our first step, as you can see in the screen here, is going to be to set up a project in Google so that Google will allow us to use OAuth for our application, all right? Basically, this is just where we'll tell Google about our project, what data we want to access on our users, stuff like that. So let's start by going to [console.cloud](https://console.cloud).[google.com](https://google.com). That should take you to a page that looks like this. You will have to sign in with your Google account in order to get here. So you may need to create an account, just do whatever you need to do there. It's a pretty straightforward process. And once you're logged into the GCP console, that is [Google Cloud Platform](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) Console, you're going to click on this little dropdown up here. Mine just says my first project. I have lots of other projects in here, of course, but you're going to click on this and this will list all of your projects if you've worked with [Google Cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) before. If you don't see anything there, that's fine as well. What you're really looking for is this new project thing up in the top right hand corner. So we're going to click on that, and we're going to come up with a name for our project here. So let's call it something, like [react](../../Skills/Web%20Development/React.js.md) authentication.
>
> **[1:35](https://www.linkedin.com/learning/react-authentication-25660373/setting-up-oauth-on-google?u=76281980&t=95)** And now that we have that, what we're going to do, we're just going to leave no organization selected there. We're going to click create, and that will create our new project for us, right? You can see this little notifications thing there. Once that finishes, once you see that little green check mark, you're just going to click select project. And now that you're in here, what you're going to want to do is add Google OAuth to this project, right? Basically, Google Cloud just has a pretty large collection of tools and APIs that you can enable for any given project. And so that's what we're going to do here. All right, so what you're going to do is you're going to go up to this search bar here and search for OAuth, and you're going to select OAuth consent screen. Basically what this does is, it allows us to configure the screen that the user will see when they're logging into our app. All right? So we'll click get started here. It says Google auth platform not configured yet. We'll click get started. It's going to ask us for a little bit of information about our app. So the first thing here is going to be the app name. And for that, we're just going to say something like, react off app. And then the next thing here is going to be, the user support email. And you're just going to select, whatever email you logged in with, there. This is just the email that users would be given if they need support. So if you're setting this up for a company and planning on using this professionally, you're definitely going to want to have some sort of like custom configured company email for this. You're not going to want to put your personal email, like I'm doing here.
>
> **[3:06](https://www.linkedin.com/learning/react-authentication-25660373/setting-up-oauth-on-google?u=76281980&t=186)** And then you can click next. And this is going to ask us about our audience, right? So in other words, do we only want to allow internal people in our organization to log in, right? So if you have like a custom domain name in your email address, like at [linkedin.com](https://linkedin.com) for example, that's what that would give us here. But since we want anybody to be able to log into our app, we're going to select external there, and then we'll go to next. Next thing here is going to be contact information. This will be basically email addresses that Google will use to contact you about any changes to the project, things like that. So I'm just going to put in that custom email address that we had before. So there we go. You can put in, really, whatever email you normally want to receive information through, and then we'll click next. And finally, we just need to agree to the user data policy and click continue. And now if we click create, that's going to set up our project with OAuth. All right, so the next thing we have to do is we're going to go into data access, and we need to tell Google what pieces of data we're planning on collecting from users in the first place. So here's what this will look like. We're going to say, we're going to click on add or remove scopes. And over here, you can see a list of things that you can access on the user. For now, we're just going to say user info dot email, and user info dot profile. And you can see the description over here, such as personal info that you've made publicly available, as well as email address. So we're just going to select those two there and click update. And now that we've done that,
>
> **[4:42](https://www.linkedin.com/learning/react-authentication-25660373/setting-up-oauth-on-google?u=76281980&t=282)** we're going to go into clients, all right? And we're going to create a new OAuth client. Again, this is basically just telling Google about the exact app that we're going to be using so that it knows that a request coming from that URL is our app. So we're going to click create client here. We're going to say that it is a web application, all right? We'll just leave the name as web client one. That's perfectly fine. And for authorized [JavaScript](../../Skills/Software%20Development/JavaScript.md) Origins, what we're going to do here is we're going to add the URL from our front end. Okay, so here's what we'll have to do. Let's go into ports. All right, so we're just going to copy our front end address, and we're going to paste that into there, okay? And oops, we do need to remove that trailing slash there. It doesn't like that for some reason. And now for authorized redirect URIs, what we're going to do is we're going to say add URI, and we're going to add the address of our server. So go into here and copy your server address. All right, we're going to paste that there. And for the path, we're going to add a path that we'll actually go into our server and implement a little bit later. So here's what this is going to look like. We're going to say slash off, slash Google, slash callback. All right? This is just a path that we're going to add that Google will actually make a request to when a user has successfully authenticated, and that should be all we need to do there. So let's click create.
>
> **[6:13](https://www.linkedin.com/learning/react-authentication-25660373/setting-up-oauth-on-google?u=76281980&t=373)** And what this is going to give us is this client ID, as well as the client's secret. Now, what you're going to want to do is set these as environment variables in your code space. Or if you're not using code spaces, just set them locally. So I'm going to do that right now. I'll copy the client ID and go into code spaces, and we're going to add this secret as Google client ID. All right. And then we'll paste the client ID in here and select the repositories that we want to allow it. That's just going to be whatever repo, you're currently working in. Again, assuming that you're working with code spaces. And then we're going to click add secret, and then we just have to do the same thing with the client secret there. So let's go into here. We're going to copy the client secret. We're going to say new secret, and then we'll call this one google client secret, and we'll paste that in there. And then we just have to select that same repo as before, and click add secret. Cool. So that should be all we have to do in order to set up our application with Google. So let's just click okay, and we have set up Google with OAuth. There may be one or two more things that we need to come back here and modify or tweak, but we'll get to those when we've actually started implementing this in our application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google](../../Glossary/Service/Google.md) (18), [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) (2), [React](../../Skills/Web%20Development/React.js.md) (2), [Google cloud platform](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **UI Navigation:** click on (4), dropdown (1), go to (1), select the (1)
> **Prerequisites:** set up (4), configure (1)
> **Env Vars:** url (2), gcp (1), uri (1)
> **URLs:** [console.cloud](https://console.cloud) (1), [google.com](https://google.com) (1), [linkedin.com](https://linkedin.com) (1)
> **Definitions:** in other words (1), is a  (1)
> **Analogies:** for example (1), such as (1)
> **CLI Commands:** make (1)

#### [Adding an OAuth URL route](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-oauth-url-route?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-oauth-url-route?u=76281980&t=0)** - [Instructor] All right, so now that we've set up Oauth in [Google Cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md), the next thing that we're going to do is create a route whose purpose will be to give us the URL that the user should be redirected to when they click the Login with [Google](../../Glossary/Service/Google.md) button. This might not make sense quite yet, but just bear with me here. It's pretty straightforward. All right, so to get started here, we're going to create a new file and this is going to be sort of like a utility file that will contain functions that will make it easier to create this route that we're talking about. So we're going to call this something like googleOauthutil.js, and inside here what we're going to do is we're going to start off by importing, by saying const { google } = require('googleapis') and we will have to install this into our project, so let's actually do that. And once again, if you're using [Codespaces](../../Skills/Software%20Development/Codespaces.md) and the code space has shut down recently and you don't have the front and backend running, what we're going to do, we're going to need to change directories into our backend here. And then we're going to say [npm](../../Skills/Web%20Development/npm.md) install Google APIs and we'll install that into our project like so. Okay, so once that finishes, the next thing that we're going to do is we're going to export something here. We'll just say const oauthClient. And we're going to say equals new google.auth.oauth2. And what this is going to look like is we're going to say process.env.GOOGLE_CLIENT_ID.
>
> **[1:37](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-oauth-url-route?u=76281980&t=97)** And we're going to use the client secret here as well, right? This is basically just setting up our application here so that it has all the information it needs right in the form of the ID in secret to actually interact with Google and allow users to log in with it. And the last thing we're going to do here is we're going to have our redirect URL. This is going to be the one that we put into the Google console. So here's what this is going to look like. We're just going to go back to our ports, copy the backend URL, all right? And we're just going to paste that in here and we're going to add that same path that we had before, which is /auth/google/callback. And that should be all we really need to do there for the OAuth client. So now that we have this OAuth client thing, we're going to use this to generate the URL that the user should be redirected to when they click that login with Google button, right? You're probably familiar with this. When you click that button, it usually opens a new tab or something like that that will allow the user to log in. And here's what this is going to look like. We're going to create a function called getGoogleOauthURL. And what this is going to do is it's just going to use this OAuth client thing that we created up here to generate that URL. First though, we're going to define these scopes that we want to be able to access. Now these scopes here do have to be in the scopes that we selected in the Google Cloud console, right? So you may remember that when we went to data access here
>
> **[3:11](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-oauth-url-route?u=76281980&t=191)** and said Add or remove scopes, we selected user info email and user info profile. Maybe I forgot to save those. I don't know why those weren't there this time, but anyway, they're back there now. So we can basically only use those. Now you can see that this scope thing here has this little like what looks like a URL or something. You can just copy that. And basically what you need to do is just go back into here and we're going to add that onto the end of this URL, which is [https://www.googleapis.com](https://www.googleapis.com)/, and then you can paste that thing there. Okay, so we're going to do the same thing with the profile. Let's go back to here and we'll copy that, auth user info profile and we'll go back to here. I'm just going to paste that in there and then actually take the URL from before that and paste it there. And that's all we should need to do like that. And now that we have these scopes, we're just going to say return oauthClient.generateAuthUrl, and we're going to say access type. This is going to be offline, right? This allows offline access, obviously. And then we're going to say prompt. That's going to be consent that will ask the user for consent for our app to access the information we're requesting. And finally, we're going to say scope. And this is going to be the scopes that we defined up above. So that's all we really need to do there. Now let's just export the get Google OAuth URL function by saying module.exports equals getGoogleOauthUrl.
>
> **[4:48](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-oauth-url-route?u=76281980&t=288)** And now that we have this, the next thing that we're going to do is go over to our server and we're going to create the route that this thing will actually be used by. Now, this is going to actually be a very simple route. Here's what this will look like. We're going to open up server.js and we'll scroll down to the bottom here. Okay, we'll just add our new route down here. And we're going to say app.get, right? So this is going to be a get route instead of a put route. And we're going to say /auth/google/url. And let's also put /API at the beginning of that one too. And then for the request handler, we're just going to say request response. And we're going to use that function we just created to get the URL that we're going to send back. So we'll say const url equals getGoogleOauthUrl. And last but not least, we're going to say res.status 200. And we're going to send back that URL as part of the request body here. So that should be all we need to do. Let's just try running our server just to make sure that all of that is working so far. So let's just go back to our terminal. It looks like Google APIs was installed correctly, so we're just going to say node server.js. And if all goes well, we should see server running on Port 3000, okay? So the next thing that we're going to need to do is go into our front end and basically just add that button that's going to allow the user to access this route.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google](../../Glossary/Service/Google.md) (11), [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) (2), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (1), [npm](../../Skills/Web%20Development/npm.md) (1)
> **Env Vars:** url (11), npm (1), google_client_id (1), api (1)
> **Code Identifiers:** getgoogleoauthurl (3), oauthclient (2), googleoauthutil (1), generateauthurl (1)
> **CLI Commands:** make (3), npm (1), node (1)
> **File Paths:** server.js (2), googleoauthutil.js (1), process.env (1)
> **Cross-References:** go back to (4)
> **Prerequisites:** install (3), set up (1)
> **URLs:** [https://www.googleapis.com](https://www.googleapis.com) (1)

#### [Creating a "sign-in with Google" button](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-sign-in-with-google-button?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-sign-in-with-google-button?u=76281980&t=0)** - [Instructor] Alright, so the next step in our OAuth process here is to add that login with [Google](../../Glossary/Service/Google.md) button to our login page. And there's going to be a few steps to this. The first one is going to be that we need to create a new state variable here that will keep track of that URL that we load from the server. Now you might wonder, right, if we open this server back up here, you might wonder why we had to go to the trouble of creating this extra server endpoint whose purpose is basically just to get the URL that we want to redirect the user to, right? Why couldn't we have just put this on our front end? Well, the reason why we couldn't just put this on our front end, it's a very important reason. So let's just open the GoogleOAuthUtil file back up. The reason is this CLIENT_SECRET and CLIENT_ID, right? These are things that we want to keep secret, and the fact is that if you put these on the front end, they're not secret. I'll go through this in a lot more detail near the end of the course, but basically every line of code that you write on the front end is accessible by the users who are using your site. So if we were to put our secret in here, right, our OAuth client secret in the front end, then that would mean that everyone accessing our site would be able to see that and that would not be a good thing. So that's why we had to go through that trouble. But here's what we're going to do. We're going to say const googleOauthUrl, and setGoogleOauthUrl, and then we're going to say equals useState and we'll just set that to an empty string at first.
>
> **[1:35](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-sign-in-with-google-button?u=76281980&t=95)** And the next thing that we need to do is we're going to scroll down here and we're going to have to load the OAuth URL from this endpoint that we just created when this login page is first displayed. So we're just going to add in a useEffect hook here, right? And that was just automatically imported for me up at the top, I believe. So we're going to add this and we're going to want to load this only at the very beginning of the app, right when the user first visits this page. So we'll put an empty array at the end of that. And then what we're going to do is we're going to create this loadOauthUrl async function, which is going to look like this async. And then inside here we're going to say try. And this will be const response = await axios.get and we're going to make this request to /api/auth/google/url. (keyboard clicks) All right. And once we get the response, we know that that URL is going to be inside there. So we'll just say const url = response.data. And last but not least, we're going to call setGoogleOauthUrl with that URL, and we'll catch any errors that occur here. We'll say catch e. And for now, this isn't a recommended practice here, but we're just going to log that out to the console so that we can use that in development, right? In the real world, what you'd want to do is maybe disable that button or something if something goes wrong or display an error message. But we'll just ignore that for now. Alright, and now we just need
>
> **[3:08](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-sign-in-with-google-button?u=76281980&t=188)** to call this loadOauthUrl thing and that will kick off the process of loading that from the backend. So let's see. Let's scroll down here. And really the last thing here is we're going to add that button to this login page. And what that's going to look like is right underneath this signup button. We're going to create a login with Google button. We'll just say log in with Google. And there are guidelines for this. If you want to do this professionally and actually use this in a public site, there are guidelines that Google wants you to follow, like displaying their logo, different color schemes, things like that. I'm just going to ignore those for now to keep this as simple as possible. But if you are going to use this professionally, please read through those. And now what we're going to do is we're going to say that this button should be disabled as long as there's no GoogleOauthUrl. So that's going to look like this. We'll say disabled equals not googleOauthUrl. Alright? So as long as that thing's not there, this button will be sort of shaded out and disabled. And on click for this button, we're going to want to say window.location.href = googleOauthUrl. We're doing this here, right, with window location href instead of the use navigate thing, because we're switching out the entire URL. We're not just going to another path in our application. All right, so that should be all we need to do there. Let's test this thing out now.
>
> **[4:41](https://www.linkedin.com/learning/react-authentication-25660373/creating-a-sign-in-with-google-button?u=76281980&t=281)** And what we should be able to do, first of all, we'll need to run our front end of course. So our backend is already running. Let's just restart that just in case. All right. And then our front end, we're going to say cd front-end and run [npm](../../Skills/Web%20Development/npm.md) run dev. And now we're going to need to go back to ports. And again, if your code space was shut down or paused, you're going to need to set the port visibility here back to public. All right. So we'll set that to public there as well. Both of these should be public. And now we're going to go back to our app. Let's actually open that up again, 'cause I closed it. And here's what this is going to look like. We're going to have to say continue there, and we should see. Here, we'll log out so that we can see our login page. Sure enough, we see that that was populated with the login with Google button, right? It was disabled for a second there, and now it's clickable, which means that we have that URL. So let's just click this button, and sure enough, what we'll see is this screen, this is the consent screen or the OAuth login screen where you can select the email that you want to log in with. And this is the one that we're going to log in with, but we can't do that yet because currently our server doesn't have an endpoint where Google can let us know that a user has logged in successfully. So that's the next thing that we're going to be building here.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google](../../Glossary/Service/Google.md) (7), [npm](../../Skills/Web%20Development/npm.md) (1)
> **Code Identifiers:** googleoauthurl (3), setgoogleoauthurl (2), loadoauthurl (2), usestate (1), useeffect (1)
> **Env Vars:** url (7), client_secret (1), client_id (1)
> **UI Navigation:** scroll down (2), go to (1), open the (1), select the (1)
> **CLI Commands:** make (1), cd (1), npm (1)
> **Cross-References:** go back to (2)
> **API Endpoints:** /api/auth/google/url (1)
> **Definitions:** means that (1)

#### [Adding an OAuth callback route](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-oauth-callback-route?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-oauth-callback-route?u=76281980&t=0)** - [Instructor] All right, so now that we've got this consent screen that's being displayed to the user, and by the way, if you're getting some sort of like authentication error or something like that instead of this screen here, make sure that you restart your Codespace once you've added those client secrets from [GitHub](../../Skills/Software%20Development/GitHub.md) because I ran into that problem as well, right? Basically it just didn't have the client ID and client secret that are needed for this URL. So anyway, that's just something I wanted to mention there. All right, so what we're going to need to do here is open our server back up and actually I have that right there. And what we're going to do here is we're going to add another route that will be that callback route that we added in the [Google Cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) console. So here's what this is going to look like. We're going to say app.get, and this one here is going to be /auth/[Google](../../Glossary/Service/Google.md)/callback. Now we don't need that API thing here because the call for this isn't going to be coming from our front end, right? It's going to be coming from Google. So here's what this will look like. We're going to say Async request and response for the callback function here for this route. And inside here we're going to first of all need to get the code from the query parameters. So we'll say request.query. And this code is basically something that Google is going to give us that will allow us to get more information about the users, such as their email address and other profile information. So here's what this will look like. We're going to need to create a few other functions in order to do this, but for now
>
> **[1:33](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-oauth-callback-route?u=76281980&t=93)** we'll just kind of like type this out and let it be a placeholder. We'll say Const Oauth user info equals, and for now, we're just going to have that be an empty object. Again, this is where we're going to be using this code to actually load it from Google. So this is just a placeholder for now. And then what we're going to do is we're going to actually create a new user in our database from the info that we get back from Google. So here's what that'll look like. We're going to say const createdUser equals, and for now we're just going to have that be an empty object as well. We will come back and implement that shortly. And really the last thing that we're going to need to do is take this created user that we just made and send back a JWT with that user's info. So we're still going to be using JWTs here in order to manage our authentication. The difference here is that instead of managing the user's password and stuff like that, we're letting Google OAuth take care of all of that for us. So here's what we're going to do. We're just going to say JWT.sign. We will actually want to get things like the ID, the is verified, which is going to be true since it's already been verified by Google, and then the user's email and info, and we're going to get that from the user that we created. Okay? And then we're going to sign that. So we'll say ID is verified. Then we'll do email and info, and we're going to say Process.env.JWT_secret, just like we've done in the past.
>
> **[3:06](https://www.linkedin.com/learning/react-authentication-25660373/adding-an-oauth-callback-route?u=76281980&t=186)** And now for the callback here, we're going to say error and token. And that's going to be the same as what we've seen before as well. All right, so we're just going to say if there's an error, we'll say return response.sendStatus. We'll send a 500 status code there. Otherwise we're going to say response.redirect, and we're going to redirect the user back to the login page. So what this is going to look like, we're actually going to need the URL of our front end here. So let's open that up. We're going to copy the front end URL like this, and then we'll come back to here, and we'll paste it like so and add /login. And we want that token there because that's how we're going to pass it over to the front end. So let's actually use back ticks here so that we can insert that easily, and then we'll say token equals token. You'll see how we grab onto that from the login page shortly. All right, so that's pretty much all we need to do for this route setup. So the next thing we're going to do is go in and actually implement these functions here that will take care of loading the user's info and creating a new user in our database.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google](../../Glossary/Service/Google.md) (7), [GitHub](../../Skills/Software%20Development/GitHub.md) (1), [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) (1)
> **Env Vars:** url (3), jwt (2), api (1)
> **Analogies:** such as (1), kind of like (1), just like (1)
> **Code Identifiers:** createduser (1), sendstatus (1)
> **File Paths:** process.env (1)
> **CLI Commands:** make (1)
> **Tools:** github (1)
> **Prerequisites:** setup (1)

#### [Loading user info from Google](https://www.linkedin.com/learning/react-authentication-25660373/loading-user-info-from-google?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/loading-user-info-from-google?u=76281980&t=0)** - [Instructor] All right, so now that we've added this callback route, we need to implement some functions for this oauthUserInfo thing and creating or updating a user in our database from the user that just logged in, if that's necessary. So here's what that's going to look like. We're going to create a new file here that will contain some utility functions. This is just going to be called something like [Google](../../Glossary/Service/Google.md), well, in fact, we can put it inside this googleOauthUtil file if that's easier, just to keep all of this stuff together. And the first function that we're going to create here will be called something like getGoogleUser. Now, what this is going to do is it's going to take a code, we'll just say async (code) like this, and it's going to use that code to load the user's info, right? This is a secret code here that Google gives us to prove that a user has logged into our site and, well, basically agreed that we can use their data. All right, so here's what this is going to look like. We're going to say const { [Tokens](../../Skills/Web%20Development/Tokens.md) } and we're going to say = await oauthClient, so we're using that thing that we created up here as well, .getToken, and we're going to pass that code, right? So basically we're trading this code that Google gave us for a token that will prove that we have access to those parts of the user's information. And now we're going to say const response =, and we're actually going to use Axios on the backend here to make this request. So we will have to install that in just a minute. We'll just say axios.get,
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/loading-user-info-from-google?u=76281980&t=94)** and we're going to, well, the URL here, here's what this is going to look like. We're actually going to say, and I'm just going to copy and paste this here because it's a little bit long, but it's going to be HTTPS, feel free to just type all this out here, make sure it all matches. And for the access token here, actually let's put this in backticks. And the access token, that's going to actually be tokens.accessToken that we include there. And for the headers, we need to include another token from this tokens thing that we got. Don't worry if this is a little bit confusing, this is just how OAuth works with Google. Every platform has a slightly different way that they handle all this stuff, so these are really just the small details here. So we're going to say headers, and this is going to be Authorization, very similar to what we did on our front end when making requests to our backend. But now our backend is making requests to Google. And we're going to say, in backticks again, Bearer. And this is going to be tokens.id_token, and that should be all we need to do there. Alright, so now that we have this response, we're just going to return response.data and that will contain the information that the user has agreed to give us. So again, their email, stuff like that. Now that we have this getGoogleUser function, we're going to export that one as well. And what we're going to do is we're going to go into our server and replace this empty object
>
> **[3:09](https://www.linkedin.com/learning/react-authentication-25660373/loading-user-info-from-google?u=76281980&t=189)** here with getGoogleUser. And that was just automatically imported for me up at the top. And we're going to pass our code that we got from the query parameter through to that. Alright, so in the next video, now that we've done this, we're going to see how we can create a new user in our database using the info that we just got from Google.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google](../../Glossary/Service/Google.md) (6), [Tokens](../../Skills/Web%20Development/Tokens.md) (4)
> **Code Identifiers:** getgoogleuser (3), oauthuserinfo (1), googleoauthutil (1), oauthclient (1), gettoken (1)
> **CLI Commands:** make (2)
> **Env Vars:** url (1), https (1)
> **Definitions:** is a  (2)
> **Cross-References:** in the next (1)
> **Analogies:** similar to (1)
> **Prerequisites:** install (1)

#### [Creating new users from OAuth data](https://www.linkedin.com/learning/react-authentication-25660373/creating-new-users-from-oauth-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/creating-new-users-from-oauth-data?u=76281980&t=0)** - [Speaker] Alright, so now that we're loading the user's info from [Google](../../Glossary/Service/Google.md), the last function that we're going to create here that should complete our callback URL here is going to be a function for basically taking that user. And there's really two possibilities here, right? One is that the user just logged in with Google OAuth and they've never logged into our site before in any other way, right? And the second possibility here is that they've already logged in with like their email and password and now they're logging in and sort of like linking their account by logging in with Google. So really we just have to take those two possibilities into account here. Here's what that's going to look like. We're going to define our function here called Update or Create User from OAuth. Kind of a complicated name there, but it pretty much describes the different possibilities here. And we're just going to pass the OAuth user info here as an argument. And what we're going to do, first of all, is we're going to get three pieces of information from that. One is going to be the ID. Now this is different, right? This ID is going to be different than the kind of ID that we use. This is going to be the ID that Google uses internally. So we're going to say ID, and we'll rename that to Google ID then we'll say verified email. This indicates whether the user has verified their email with Google and we're going to rename that to is verified 'cause we're basically just going to set that to whatever Google has. In other words, if Google has already verified that email,
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/creating-new-users-from-oauth-data?u=76281980&t=94)** then that's good enough for us, right? And finally, we're going to say email and we'll just leave that as email. We don't need to rename that and we'll say equals OAuth user info, okay? So we're just getting those three properties from that and renaming them accordingly. And now we're going to need access to the database. So we'll just say const db equals require and we're going to load our database file there and that should be all we need to do. Let me just make sure that I did that correctly in server.js. Yep, alright, sure enough, that looks like the same thing. And we'll also want savedb there as well. Cool. So let's scroll back down here. And the first thing that we're going to want to do is figure out whether there's already a user with that email in our application. So we're going to say const existing user equals, and we're going to say db.users.find and find a user with the corresponding email. So user.email equals email. And if there's an existing user, right, we'll say if existing user, then what we'll do is we're just going to want to update the user in the database with any additional information that we have here, right? So we'll want to update this is verified thing for example. And we'll also add this Google ID thing onto the user so that we know that that user has a linked Google account. So here's what that'll look like. We'll say db.user.googleid, we'll say equals googleid.
>
> **[3:08](https://www.linkedin.com/learning/react-authentication-25660373/creating-new-users-from-oauth-data?u=76281980&t=188)** And then we'll say db.user.isVerified. We're going to set this to either the is verified property from Google, right? In other words, if this is true, we want db.user.isVerified to be true. Oh, and sorry, that shouldn't be db.user. This should be existing user. Okay, there we go. And we'll do existing user down here as well. So existinguser.isVerified. That's going to be either the is verified thing up here or, right? If one of these is true, that's good enough for us. If existing user is already verified, then we'll want that to be true because of that as well. And that's pretty much all we need to do there. So now what we're going to do is if there isn't an existing user already, we're going to want to insert a new one by saying db.users.push and here's what the new user's going to look like. The new user's going to have an email as well as a Google ID, and we'll put isVerified in there directly and then we'll have info and set that as basically just an empty object there. All right. And actually to make this easier to send this user back, what we're going to do is we're going to say new user, right? Const newUser equals and then what we'll do is we'll say db.users.push newUser. And in either one of these cases we'll want to return that corresponding user.
>
> **[4:40](https://www.linkedin.com/learning/react-authentication-25660373/creating-new-users-from-oauth-data?u=76281980&t=280)** So in this existing user case, we'll say return existingUser. And in this new user case, we'll say return newUser. And then after all this is done, right, we'll have to do that in two separate places here. We're going to say saveDb so that that result is persisted there. So we'll say saveDb here as well. And that should be all we need to do. So let's make sure that we export this thing now, right? We'll export, what do we call it here? Update or create user from OAuth. And now if we go back to server.js, we're just going to go back down to where we had that empty object. All right, there we go. And we're going to say created user equals, and we'll say instead of that empty object, update or create user from OAuth, and we're going to pass that OAuth user info through and that should be all we really need to do there. So the last thing that we're going to need to do is basically bring this all together by going back to the front-end and seeing how we can handle a successful OAuth authentication when we redirect the user back to the login page here.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google](../../Glossary/Service/Google.md) (12)
> **Code Identifiers:** isverified (4), newuser (3), savedb (2), existinguser (1)
> **CLI Commands:** make (3), find (2)
> **File Paths:** server.js (2)
> **Definitions:** in other words (2)
> **Env Vars:** url (1)
> **Cross-References:** go back to (1)
> **Analogies:** for example (1)

#### [Handling successful OAuth authentication](https://www.linkedin.com/learning/react-authentication-25660373/handling-successful-oauth-authentication?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/handling-successful-oauth-authentication?u=76281980&t=0)** - [Shaun] Okay, so the last step in this whole process, as I said, is to modify our login page because after our user finishes going through this callback here, right? After this finishes, it's going to redirect our user to the login page, but it's going to set this token query parameter. And so essentially what we want our login page to do is check to see whether that's there. And if it is, then to log the user in. So here's what this is going to look like. We're going to go to the login page. And the first thing we're going to do is get the value of that token query parameter if it's there. And here's what this is going to look like. We're going to say, underneath all of this stuff here, we'll start off by saying const. And we're going to use the use location hook, so we'll say const location equals useLocation like so. And now what we're going to do here is we're going to say const queryParams equals new URLSearchParams. And we're going to pass location.search to that. And the last thing here is we just need to get the token out of there by saying const token equals queryParams.get('token'). And actually let's call this something a little bit different here. What we're actually going to call this, oauthToken. So we'll change that there, oauthToken. And now that we have that, let's go down here. And above the last use effect hook that we created,
>
> **[1:35](https://www.linkedin.com/learning/react-authentication-25660373/handling-successful-oauth-authentication?u=76281980&t=95)** which is the one that loads that URL, we're going to use the useEffect hook to basically check this oauthToken thing and redirect the user based on that. So here's what that'll look like. We're going to say useEffect. This is only going to be called in a few different circumstances. The first is if the oauthToken itself changes, so we'll put that as the first dependency. Then we'll have setToken here as well as navigate 'cause we're going to be using all three of those things. And now inside here we're just going to say, if there's an oauthToken, then what that means is the user was redirected here after logging in from [Google](../../Glossary/Service/Google.md). And we're going to want to just set that token. So we'll say setToken to oauthToken. And we're going to say navigate. And we're going to send the user to the home route with replace equal to true. And that is all we need to do there. So one last thing just to kind of highlight that we're actually storing the user's data here in emails, we're going to open up the user info page. We haven't seen this one in a while. And we're going to actually display the user's email in this title, all right? So you've probably wondered why the title up here just has this blank. We're going to display that email in there just by saying info for email. And that should be all we really need to do. Except there is actually one little bug on the server side. And that is we need to add the await keyword to both of these functions here just to make sure
>
> **[3:10](https://www.linkedin.com/learning/react-authentication-25660373/handling-successful-oauth-authentication?u=76281980&t=190)** that we're not getting a promise back and trying to destructure that. That's something that can cause some pretty tricky bugs to track down, so let's just restart our server to make sure that that has those changes. We'll go back to here. And we'll say node server.js like that. And now, we're going to just try this again. So if we go back to our [React](../../Skills/Web%20Development/React.js.md) app, we should be able to log in with Google. And I'm going to click on this one here. And if all goes well, what we should see is. There we go, that it takes us to the user info page. And we have this sw.[LinkedIn](../../Skills/Web%20Development/LinkedIn.md).learning@[gmail.com](https://gmail.com) email in there. And if you check your database, right? If we open up db.[JSON](../../Skills/Web%20Development/JSON.md) here. What you should see is, sure enough, there is now a new user in there for that email. So that's the last thing that we have to do there, right? We now have a fully functional oauth flow that allows our users to log in with Google.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google](../../Glossary/Service/Google.md) (3), [React](../../Skills/Web%20Development/React.js.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Code Identifiers:** oauthtoken (6), queryparams (2), useeffect (2), settoken (2), uselocation (1)
> **CLI Commands:** make (2), node (1)
> **File Paths:** server.js (1), db.json (1)
> **Cross-References:** go back to (2)
> **UI Navigation:** go to (1), click on (1)
> **URLs:** [gmail.com](https://gmail.com) (1)
> **Env Vars:** url (1)


### 5. Prebuilt Auth Options

[↑ Back to Table of Contents](#table-of-contents)

#### [Why prebuilt auth options?](https://www.linkedin.com/learning/react-authentication-25660373/why-prebuilt-auth-options?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/why-prebuilt-auth-options?u=76281980&t=0)** - [Instructor] All right, so throughout this course, we've seen how to implement pretty much everything ourselves for the most part. So the next thing that we're going to do is we're going to take a look at a few prebuilt options that you can use to quickly and easily add user authentication to your application. Now, the downside of a lot of these, of course, is that they change quite frequently. And so the ones that you see here in this course may be a little bit different, right? As we'll see in a minute, they may have changed names, or the basic integration process might be a little bit different. So it's a tricky thing to really talk about these in a static course like this. But the good news for most of these is you'll see is that they have really good documentation that walks you through step by step how to add it to your app. So, what I'm going to do here is I'm going to talk about a few of the major players in this prebuilt authentication space and help you choose which one's right for you should you decide to go down this path. Now, that brings us to the next question, and that is: Why would you want to use prebuilt authentication in the first place, right? Now that we've gone through learning about a large number of different aspects of the authentication process, why would you, you know, turn to another company that's going to do that all for you? Well, there's a few reasons why you might want to do this. Really, the first one has to do with security. Right now, even if we understand all of the basic security concerns behind using user authentication in our applications, the fact is that for most of us, actively managing this security and making sure our app stays secure over time just isn't a realistic pursuit, right?
>
> **[1:35](https://www.linkedin.com/learning/react-authentication-25660373/why-prebuilt-auth-options?u=76281980&t=95)** That's really something that you have to have a fairly sizable team behind you to do. Another reason here is reliability. So, a lot of the things that we've seen how to implement work great when we're running them locally or just in a basic test setup. But the fact is that in the real world, things can get pretty messy. And this also requires that you have a separate reliability team behind you in many cases that's going to make sure that everything about your authentication flow continues to work day in, day out, right? So this is another reason why many people turn to prebuilt auth is that this is something that these companies take care of for you. A third thing is rapid implementation. So, everything that we've seen so far has taken quite a bit of time to really figure out and sort out the details for. With a lot of these prebuilt auth providers, it's really just a few lines of code that you can add to your app. And that's it, right? You can have user authentication in your application relatively quickly. Now, besides this, reduced complexity. So, in many cases, since you are no longer the one with prebuilt auth who's having to manage the database, having to manage the emails, things like that, it can really make it much simpler to add user authentication to your application, as well as just kind of keep it running smoothly. And finally, the last thing here is [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md). So, with prebuilt authentication, you know, you might be just starting out and only have a handful of users on your site,
>
> **[3:10](https://www.linkedin.com/learning/react-authentication-25660373/why-prebuilt-auth-options?u=76281980&t=190)** but then a month from now, you might have thousands and thousands of users on your site. Now, this can be very tricky to scale up sometimes, depending on how you've implemented things, but if you use prebuilt authentication providers, usually this is something that just sort of happens automatically, which is really nice for when your application becomes really popular. So these are some of the main reasons why you might want to use prebuilt authentication. But let's take a look at a few downsides. Really, there are three main downsides here. The first one is that with most of these providers, there's an associated cost. Now, the pricing strategies of these different providers vary quite a bit. It can sometimes depend on how many users you have, sometimes on how much usage you're getting, right, how many users are logging in, things like that. But overall, there is going to be an associated cost here that you'll have to pay. Now, one thing that I'll say for this is if you compare this against the cost of hiring a team, right? Let's say your own employees to take care of the authentication for you, in many cases, the cost of a prebuilt auth provider will seem very, very small indeed. But nevertheless, it is something to think about. The second thing here is that with many of these prebuilt auth providers, there is a risk of vendor lock-in. Now, what's vendor lock-in? Vendor lock-in is when you're not careful, and your application becomes so dependent on a specific vendor's implementation that it becomes very hard to, say, switch vendors.
>
> **[4:46](https://www.linkedin.com/learning/react-authentication-25660373/why-prebuilt-auth-options?u=76281980&t=286)** Or if the vendor goes out of business, it becomes very difficult to, you know, go through and remove all of that vendor's specific code from your application. This is a big risk with really any kind of technology. And this can be true of [Databases](../../Skills/Software%20Development/Databases.md), different libraries, email providers, as we've seen, things like that. And last but not least, ultimately, with prebuilt authentication providers, you're just going to have less customization and control over how your application works in this respect than if you had implemented everything yourself. Now, you might see this as a good thing in certain situations, right? The fact that it's just kind of prebuilt, there's not really a whole lot of thought you have to put into it. That can ultimately end up being a good thing for a lot of people. But, if you have some very specific authentication needs, then what you might find is just that prebuilt auth providers don't quite fit into what you had in mind. So anyway, those are some of the benefits and downsides of prebuilt auth. So the next thing that we're going to do is briefly take a look at some of the most popular auth providers.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **CLI Commands:** make (2), find (1)
> **Definitions:** is a  (2)
> **Prerequisites:** setup (1)
> **Speakers:** - [instructor] (1)

#### [Authentication with Auth0](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-auth0?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-auth0?u=76281980&t=0)** - [Instructor] Alright, so the first prebuilt auth provider option that is available to you, at least the first one that we're going to talk about here, is one called Auth0. Now, this might be a little bit confusing seeing as though there's something else in the space called Oauth, many people you'll hear them pronounce this Auth-O, it's called Auth0. But anyway, there are a few key benefits to Auth0. First of all, though, I wanted to show you what the site looks like. So feel free to just take a look at this, look through their documentation if you're interested in getting started with this. And, you know, feel free to give it a try to see if it fits in with what you need. I'm not going to be doing that here, but I will be going into some of the key features that makes this one unique as well as just reasons why you might want to use this provider. So the first thing here is that, for the most part, when working with Auth0, integrating this into your application is really very easy, right? The libraries and stuff that they provide for [React](../../Skills/Web%20Development/React.js.md) and [Node.js](../../Skills/Web%20Development/Node.js.md) really just make it easy to get started, and, especially now that you're aware of how all of this stuff works behind the scenes, it can really be a very straightforward option for you. Now, the second thing here is that Auth0 provides support for a wide variety of different providers. So, you know, if you want to allow users to log into your site through social media logins like [Facebook](../../Glossary/Service/Facebook.md), [Google](../../Glossary/Service/Google.md), and so on, if you want to do password lists authentication, where basically temporary password is sent to the user's email address,
>
> **[1:35](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-auth0?u=76281980&t=95)** or if you want to do things like enterprise login, you can do those with Auth0. It makes it very easy to do. And besides that, it does have some advanced security features. So you can have like multifactor authentication where it sends a code to, say, your cell phone built in. It protects you from hackers who will try and, you know, hack users' passwords by brute force, as well as through a wide variety of other strategies. And finally, Auth0 has a customizable UI where basically it will actually host your login pages for you if you want it to. And those are fully customizable. So essentially instead of adding that to your React application, you can create your own login page that users will be redirected to that's managed and hosted by Auth0. So anyway, those are some of the key features of Auth0. Again, if you want to try this out, just head over to their website here and read through their documentation. It's pretty easy to get started with.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (2), [Node.js](../../Skills/Web%20Development/Node.js.md) (1), [Facebook](../../Glossary/Service/Facebook.md) (1), [Google](../../Glossary/Service/Google.md) (1)
> **CLI Commands:** node (1), make (1)
> **File Paths:** node.js (1)
> **Prerequisites:** getting started (1)
> **Speakers:** - [instructor] (1)

#### [Authentication with Firebase](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-firebase?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-firebase?u=76281980&t=0)** - [Instructor] Alright, so the next auth provider that we're going to take a look at here is [Firebase](../../Skills/Web%20Development/Firebase.md). This one is provided by [Google](../../Glossary/Service/Google.md) and is integrated in many ways with [Google Cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md). So if you're already using Google Cloud or maybe even Firebase, which is a somewhat more user friendly version of Google Cloud, this can be a great option because it's really just pretty straightforward to get started with. So let's talk about some of the benefits of Firebase. First of all, just like with Auth0, setting this thing up is really pretty quick and easy, right? You really don't have to do a lot of configuration and really it is best for small to medium-sized applications. You'll find that when your application starts getting lots and lots of users, it can rapidly become a little bit more difficult to manage. So that's just something to keep in mind there. Firebase authentication also has extensive support for a lot of different providers. So obviously this includes the most common ones, like Google, [Facebook](../../Glossary/Service/Facebook.md), Twitter, [GitHub](../../Skills/Software%20Development/GitHub.md). But it also includes things like phone authentication. You can do email, password, sign in with this one, so you could actually replace part of what we implemented earlier with this, and there's lots of other providers there as well, that literally all you have to do is just enable them and provide a button that the user can click and Firebase Auth will take care of the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md). Alright, and besides that, one thing that's really nice about Firebase Auth that makes it easy to integrate into [React](../../Skills/Web%20Development/React.js.md) applications
>
> **[1:33](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-firebase?u=76281980&t=93)** is that Firebase provides real time listeners for whenever a user's off state changes. And this makes it really easy to just create something like a custom hook where you can immediately know when the user logs out and adjust everything accordingly. And finally, one of the really nice things about Firebase Authentication is that it's free tier is very generous. I personally have never reached the point in my personal projects where I've had to start paying for Firebase Authentication. It's really a very budget friendly tool for, you know, smaller companies, small teams, and of course personal projects. So anyway, that is Firebase Authentication. Again, if you want to check this out, feel free to head over to their website. They have lots of details about the different things you can do on this. And really, if you're interested in knowing more, just walk through some of the tutorials they have on their site.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Firebase](../../Skills/Web%20Development/Firebase.md) (10), [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) (3), [Google](../../Glossary/Service/Google.md) (2), [Facebook](../../Glossary/Service/Facebook.md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **CLI Commands:** find (1)
> **Tools:** github (1)
> **Definitions:** is a  (1)
> **Analogies:** just like (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [Authentication with NextAuth.js](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-nextauth-js?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-nextauth-js?u=76281980&t=0)** - [Instructor] All right, the next prebuilt auth provider we're going to take a look at is one called NextAuth.js. And the first thing to know about this one is, while we've seen in this course how to add authentication to a regular [React](../../Skills/Web%20Development/React.js.md) [Node.js](../../Skills/Web%20Development/Node.js.md) app, in many cases, and I'm assuming that this is true for many learners out there, what you'll want to do is add authentication to a [Next.js](../../Skills/Web%20Development/Next.js.md) app, and that's what NextAuth.js really provides us with an easy way to do. So, hopefully that sets the stage a little bit for this one. And that's really the main benefit of this provider is that it integrates pretty seamlessly with Next.js apps, right? It's specifically designed to be used with Next.js as part of that ecosystem. Now, in addition to this, NextAuth.js is also open source and extensible. So, unlike the other ones that we've talked about so far, where it's a closed system and really, in many cases you'll have to pay once you get past a certain point, with NextAuth.js, it's open source, meaning that you can start using it for free and also customize it as much as you want. Okay, so this includes things like customizing authentication providers, managing sessions, right? So, managing how a user's credentials are maintained once they've logged in. And also, integrating with [Databases](../../Skills/Software%20Development/Databases.md). So if you wanted to integrate with [SQL](../../Skills/Data%20Science/SQL.md) or [MongoDB](../../Skills/Software%20Development/MongoDB.md) or really any other option, NextAuth.js makes that pretty easy as well.
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-nextauth-js?u=76281980&t=94)** And just like the other ones that we've seen, it also has support for a lot of different providers. So you can do OAuth with this. You can do a credentials-based login like we saw at the beginning of the course. You can also do email or passwordless authentication too, so there's a lot of options available for you there. And finally, it has built-in support for JWTs, right, [JSON](../../Skills/Web%20Development/JSON.md) web [Tokens](../../Skills/Web%20Development/Tokens.md), which we learned all about earlier. And what this means is that you can really manage your sessions very easily with NextAuth.js by integrating them with the concepts that we learned earlier. And in case you're wondering what NextAuth.js' website looks like, here it is here. It might look a little bit different at this point, but feel free to look through the documentation. There's lots of tutorials on here as well. And ultimately, that'll just help you know if you have a Next.js application, whether this is a good option for you.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Next.js](../../Skills/Web%20Development/Next.js.md) (4), [React](../../Skills/Web%20Development/React.js.md) (1), [Node.js](../../Skills/Web%20Development/Node.js.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1), [SQL](../../Skills/Data%20Science/SQL.md) (1)
> **File Paths:** nextauth.js (7), next.js (4), node.js (1)
> **Env Vars:** sql (1), json (1)
> **CLI Commands:** node (1)
> **Documentation:** the documentation (1)
> **Definitions:** is a  (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Authentication with Clerk](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-clerk?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-clerk?u=76281980&t=0)** - [Instructor] Alright, well now that we've talked about NextAuth.js, let's talk about another option that is also specific to well, not necessarily [Next.js](../../Skills/Web%20Development/Next.js.md), but definitely specific to the idea of front-end frameworks and creating web applications. But definitely specific to the world of things like front-end and [JavaScript](../../Skills/Software%20Development/JavaScript.md) and frameworks and Full-Stack applications. And this is something called Clerk, now the interesting thing about Clerk is and if you take a look at their website here, you can see this is it actually provides you with a number of pre-built components that you can add to your site, just like you can add any other kind of [React](../../Skills/Web%20Development/React.js.md) component. And you can actually work on the design of these things. So you can see those all right here. In the Clerk dashboard, instead of having to manipulate those in React in our case, in code. So it's also a hosted solution, so you won't have to worry about doing any pf that self hosting like you would if you did NextAuth.js, but ultimately it's just a really great platform very easy you get started with. So let's talk about a few of the key characteristics of it. As I already said, one of the key features of this library for React developers is that it includes these out of the box React components that are ultimately designed for creating a very smooth and easy to use user interface and [user experience](../../Skills/Web%20Development/User%20Experience%20(UX).md). Besides that it also includes other features
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-clerk?u=76281980&t=94)** like [Multi-factor Authentication](../../Skills/Software%20Development/Multi-factor%20Authentication.md). So you can enable this without having to do a whole lot of set up which is great. Session management this includes things like fine-grained control and active device management. You can read more about what that is if you're curious, but it's a very interesting feature that's available to you. And last but not least, it's developer friendly. As I said, it's designed specifically for React. There are bindings that you can use in order to use it with other libraries like [Vue](../../Skills/Web%20Development/Vue.js.md) and you can also use it with Next.js, too. So if you have a Next.js app it's really the same kind of process for adding Clerk to the app as if it's just regular React app like what we already have. And it's just got a lot of different APIs and very clear documentation that ultimately make it just a really easy tool to get started with and work with long term.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (6), [Next.js](../../Skills/Web%20Development/Next.js.md) (3), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [User experience](../../Skills/Web%20Development/User%20Experience%20(UX).md) (1), [Multi-factor Authentication](../../Skills/Software%20Development/Multi-factor%20Authentication.md) (1)
> **File Paths:** next.js (3), nextauth.js (2)
> **CLI Commands:** make (1)
> **Analogies:** just like (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Authentication with AWS](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-aws?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-aws?u=76281980&t=0)** - [Instructor] Alright, well the last prebuilt authentication option that we're going to talk about here is AWS, [Amazon Web Services](../../Skills/Cloud%20Computing/Amazon%20Web%20Services%20(AWS).md). And currently it has a specific name. Currently it is called, if you go over to here, AWS Amplify Authentication. But I've kind of left that out of the slides here just because they tend to change those names quite a lot. So it used to be called Cognito. It's gone through a few other names as well, I believe. But anyway, it's really just a great platform if you're okay with a little bit more configuration, and especially if you're already familiar with Amazon Web Services and working with those. It can be a really great option for you. So let's talk about why we might want to use AWS authentication services. First of all, it's fully managed, right? So by using these things called user pools, and there's a lot of other tools there as well, all of that stuff, the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the details are automatically managed for you, which is pretty cool. Another thing here is that if you're already using the Amazon Web Services ecosystem, right? If you're already hosting your application on AWS or using some of its other capabilities, it can easily be tied in with other services, right? And this includes also things like Lambda, DynamoDB for [Databases](../../Skills/Software%20Development/Databases.md), API gateway, things like that. Now, it's no secret that AWS is very commonly used by larger corporations, and that is due to the fact that it's very scalable and also very secure. So, a lot of these things are just automatic,
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/authentication-with-aws?u=76281980&t=94)** and this includes the authentication aspect of it, right? And it also includes a lot of the security features that these other options that we've looked at have, such as multifactor authentication. And last but not least, AWS Amplify does come with prebuilt components, but just like the previous option that we saw, it also allows you to customize those and also customize the entire authentication flow. So if you want to create like an onboarding flow, things like that, you can do that as well with this tool. All right, so anyway, that is the pre-built AWS authentication option and some of the things that make it different and some reasons why you might want to use it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Amazon web services](../../Skills/Cloud%20Computing/Amazon%20Web%20Services%20(AWS).md) (3), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **CLI Commands:** aws (7), make (1)
> **Env Vars:** aws (7), api (1)
> **Analogies:** such as (1), just like (1)
> **Definitions:** is called (1)
> **Speakers:** - [instructor] (1)


### 6. Best Practices

[↑ Back to Table of Contents](#table-of-contents)

#### [Using HTTPS and SSL](https://www.linkedin.com/learning/react-authentication-25660373/using-https-and-ssl?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/using-https-and-ssl?u=76281980&t=0)** - [Instructor] Okay, at this point in the course, we've seen quite a few different aspects of user authentication in the context of full-stack [React](../../Skills/Web%20Development/React.js.md), and now, it's time to talk about a few so-called best practices when incorporating authentication into a React application. These are things that we haven't covered directly in the course, but nevertheless, they're very important to keep in mind, since a lot of times, they can make the difference between your user's data being secure and your user's data being easily accessible by hackers. Now, the first of these best practices that we're going to take a look at is that in all your web applications that deal with sensitive data, you need to use HTTPSS, SSL, TLS. Colloquially, these are all names for the same basic idea, so let's talk about that here. If you go to sites like [Google](../../Glossary/Service/Google.md) or [Facebook](../../Glossary/Service/Facebook.md) or [LinkedIn](../../Skills/Web%20Development/LinkedIn.md), and obviously, many others as well, you'll see that, up in the left hand corner of the page, the browser displays this little lock icon, and if you look at the full URL, you'll see that it begins with HTTPS instead of just HTTP, and that's because these sites use SSL, which stands for Secure Sockets Layer, and TLS, which stands for Transport Layer Security, to ensure that their users' communications remain secure, and you don't have to remember what these stand for exactly. It's only important that you understand the basic concepts that we're covering here. Now, to understand why this is important, let's think back to much earlier in the course
>
> **[1:33](https://www.linkedin.com/learning/react-authentication-25660373/using-https-and-ssl?u=76281980&t=93)** when we talked about how, when interacting with some other party via the internet, it's very hard to be certain who exactly it is that we're interacting with. A user on our site might say, "Yeah, I'm [John](../../Glossary/Tool/John%20the%20Ripper.md) Doe, let me access my bank account," but in order to know for sure that this is actually the case, that this is the real John Doe, we have to use some authentication techniques, as we've seen. So while ensuring that our users are who they say they are is important, it's also important that your users can make sure that you are who you say you are and that no one else besides your site can see the requests that they're making, and that's where HTTPS or SSL or TLS or whatever name you want to use for it comes in. So let's take a look at the first concern, proving to your users that you are who you say you are, in other words, making sure your users know that they're actually dealing with your site and dealing only with your site. Now, we've already seen that, in order for users to log in or create an account on a site, they're sending us their password. Now, if a user thought that they were logging into our site, but were actually logging into an imposter site, basically a site that looks like our site, but that was put up by a hacker, they'd be sending their password to that imposter who could immediately turn around and use their password to log into our real site, and there it is, that user's account has been hacked, and this is why it's always very important when you're logging into a site to A,
>
> **[3:06](https://www.linkedin.com/learning/react-authentication-25660373/using-https-and-ssl?u=76281980&t=186)** make sure that you typed the URL correctly, in other words, make sure you're not logging into [google.com](https://google.com) with two Ls or three Os or some other misspelling. There are hackers who actually buy these misspelled domain names and put up a site that looks identical to the real site for this reason, and B, you want to make sure that you always see this little lock icon, which means that the site is using HTTPS and that the certificate for that is valid. Most of the time, your browser will double-check this for you automatically. All right, so that's the first concern of HTTPS. The second concern is making sure that a third party can't intercept and read a user's communications with the site that they're accessing. You see, without HTTPS, it's actually fairly easy for hackers to read these kinds of communications. One example of this is that, in public places like airports, for example, a hacker can simply set up their own [Wi-Fi](../../Glossary/Standard/Wi-Fi.md) hotspot, and they can call it whatever they want, usually something like "free airport Wi-Fi," and then when they connect to this, the hacker will be able to see all the communications that users have with the sites they visit. Now, this is only a problem if the sites that the users are accessing through this malicious hotspot aren't using HTTPS, because what HTTPS does is it encrypts all the communications between the user and the site, so even if a hacker can see those raw requests, they won't actually be able to read them once they're encrypted. So now that we know what HTTPS does for our users, you're probably wondering how it works exactly.
>
> **[4:40](https://www.linkedin.com/learning/react-authentication-25660373/using-https-and-ssl?u=76281980&t=280)** Well, the in-depth details of HTTPS or SSL or TLS are a little bit beyond the scope of this course, but the basic concept is this. When you're creating a site and you want to use HTTPS to keep your user's information secure, you're basically going to register your site with something called a certificate authority, which is a well-known company that can vouch for the fact that your site is who it says it is, and the certificate authority will then give you an SSL certificate, which basically says that the certificate authority is vouching for your identity, and this certificate is signed in a similar way to a JWT so that users' browsers can be sure that the certificate was actually created by this trustworthy certificate authority and not by the site itself, for example. Now, the good news with HTTPS, SSL, TLS, whatever, is that it's generally provided for free from your web host, and if your web host doesn't provide you one for free or wants to charge you for HTTPS, I'd recommend that A, maybe you switch web hosts, or B, you can usually get a free certificate from sites like Let's Encrypt, but the point is that generally, you don't have to pay for these certificates, and they're usually something that just sort of happens automatically. In other words, you usually don't have to manage the implementation details yourself when you're building your site.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (2), [Google](../../Glossary/Service/Google.md) (2), [John](../../Glossary/Tool/John%20the%20Ripper.md) (2), [Wi-Fi](../../Glossary/Standard/Wi-Fi.md) (2), [Facebook](../../Glossary/Service/Facebook.md) (1)
> **Env Vars:** https (12), ssl (6), tls (5), url (2), httpss (1)
> **Definitions:** in other words (3), stands for (2), means that (1), is a  (1)
> **CLI Commands:** make (5), ls (1)
> **Cross-References:** earlier in (1), we talked about (1)
> **Analogies:** for example (2)
> **URLs:** [google.com](https://google.com) (1)
> **UI Navigation:** go to (1)

#### [Salting and peppering passwords](https://www.linkedin.com/learning/react-authentication-25660373/salting-and-peppering-passwords?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/salting-and-peppering-passwords?u=76281980&t=0)** - [Instructor] Okay, the next best practice we're going to talk about here is the practice of salting and peppering passwords. Now, these rather humorous terms, they refer to a fairly simple, but very effective, technique for making the password hashes that we store in our database even more secure. So we'll start off with salting here. Put simply, salting passwords means that before hashing and storing the user's password, we generate a random string of characters and then add that onto the beginning or the end of the user's password, right? And we then hash this entire string and store the user's password salt in our database alongside the password hash that we generated. So that's salting. Peppering passwords is a very similar process, but instead of randomly generating a unique string for each user as we do with salting, we simply have a secret string that's known only to our server that we append or prepend to the password, along with the salt, before we encrypt it, right? So in other words, the salt will be different for each user, and the pepper will be the same for all users. And just to clarify, these two techniques are usually used alongside each other to maximize their effectiveness. Now, at first glance, it might not be quite clear how these techniques of salting and peppering passwords actually make our users' passwords more secure. So in order to understand this a little bit better, let's imagine a situation where the unthinkable happens and a hacker gains access to our database. Well, first of all, the possibility
>
> **[1:32](https://www.linkedin.com/learning/react-authentication-25660373/salting-and-peppering-passwords?u=76281980&t=92)** of this situation occurring is the main reason why we've hashed our passwords in the first place instead of storing them as plain text, right? But salting and peppering adds an extra layer of difficulty over this for hackers. With hashing alone, you see, there are techniques that hackers can use to actually figure out users' passwords, even after they've been hashed, right? And these techniques are usually based on the fact that at least some of our users probably have the same very guessable strings, such as, you know, abc123 or 123456 or some [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) like password as their password. And what hackers can do then is they can usually identify them directly from their hashes, because they'll see that more than one user has the same hash for their password. They can usually be pretty sure that, in cases like that, the users have used one of those very easily guessable passwords. So by adding salt, which is different for all users, to the password before creating the hash, we can basically mask that fact and make it harder for the hacker to get at that. And adding pepper is just another layer over that where they would need to have access to the server as well in order to figure out what string is being used for the pepper before we generate that hash. So that is the concept of salting and peppering when it comes to users' passwords. And that is how they help.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **CLI Commands:** make (2)
> **Definitions:** is a  (1), in other words (1)
> **Analogies:** imagine (1), such as (1)
> **Best Practices:** best practice (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Adding salt and pepper](https://www.linkedin.com/learning/react-authentication-25660373/solution-adding-salt-and-pepper?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/solution-adding-salt-and-pepper?u=76281980&t=0)** - All right, well hopefully you gave this challenge a try. So let's take a look at the solution. So what I did here is I started off, obviously the salt and pepper password function. I started off from scratch. And really all we had to do here was take the pepper and the salt and kind of decide where we wanted to put them, or how we wanted to add them to this password. So really the easiest thing you could have done here was you could have said, you know, return, and then we could have generated the salt. So we'll do that on a line above that. So I said, "const salt =" and then we'll just say, "generateRandomSalt." Use this one down here. So "generateRandomSalt" like so. And now that we have the salt, what we can do is we can just return that salt, obviously. And for the password we're going to have to say, well, here, we'll say "result," just 'cause that was what we said in the instructions there. And we could just add the password, plus the salt, plus the pepper. And this is just one possible order, right? And here, let's actually add the pepper in the uppercase letters that it's written in up there. And as I said, this is just one possible order. You could do the salt before the password, and then the pepper after that. You could do the pepper and the salt before the password. You can really do it in any order that you want. And that is actually something pretty interesting, because that's just another thing that makes it more difficult for hackers to figure out, right?
>
> **[1:33](https://www.linkedin.com/learning/react-authentication-25660373/solution-adding-salt-and-pepper?u=76281980&t=93)** They don't know what the order of the salt and pepper is, even if they're somehow able to figure out what the salt is, they won't know whether it comes before the password or after the password, or before the pepper or after the pepper. And again, that just makes it much more difficult for them. So let's just take a look at this. Let's take our simple Abc123 password. We're going to click Generate Modified Password. And as you can see, this is what the password becomes before it's encrypted. So the likelihood that a hacker could guess this here and generate a hash and find that it matches is much smaller than if we were to just do Abc123 without the extra salt and pepper in it. So hopefully you figured out this challenge. Congratulations if you did.

> [!info]- Semantic Content
>
> **Code Identifiers:** generaterandomsalt (2)
> **CLI Commands:** find (1)
> **Speakers:** - all (1)
> **Non-Speech:** (upbeat music) (1)

#### [Never trust the front-end](https://www.linkedin.com/learning/react-authentication-25660373/never-trust-the-front-end?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/never-trust-the-front-end?u=76281980&t=0)** - [Instructor] All right, let's move on to our third best practice, which is something that I always make sure to tell people when they're first starting off with full stack development, and that is to never trust the front end. So what do I mean by this exactly? Well, when I say "never trust the front end," I mean that when you're building a full stack application and you want to enforce some kind of security measure, it's absolutely critical that this protective logic is implemented on the server and does not rely on the client-side logic alone. So just to give you some examples of this, and then we'll jump over to our application, where there's a pretty big security flaw that I wanted to show you in the context of this best practice. Some examples of this would be to make sure that users can only view their own data, right? So if you have a site with multiple users and you want to keep their data separated so that each user can only view the data that belongs to them, you need to make sure, as we did over here. All right, so let's just find where we did this here. If we take a look at the update user endpoint, remember that what we did is we checked to make sure that the ID that we were trying to update was equal to the ID that was in the [JSON](../../Skills/Web%20Development/JSON.md) web token, right? If we had only done this on the client-side, the user could easily get around that. All right, another example of this would be to make sure that users can't store arbitrary fields in our database. That's why we've gone through the trouble here, right? Every time that we allow the user
>
> **[1:32](https://www.linkedin.com/learning/react-authentication-25660373/never-trust-the-front-end?u=76281980&t=92)** to do something like update, that's why we've gone through the trouble of explicitly destructuring each individual part of that request body, is because we don't want the user to be able to just add extra information, extra properties, into that request body that would be saved in our database without our knowledge, okay? That's another thing that we have to implement on the backend 'cause who knows what the client-side is going to do, right? A user could easily just use Postman, like we've been doing for testing, to basically reverse engineer our server endpoints and send whatever requests they want to it. And the last thing here is when you want to do things like make sure that only paid subscribers can access certain content, if you have that content loaded into the front end but let's say you try and hide it by putting maybe a faded out background in front of it or something like that, then anyone who's familiar with things like CSS or who knows how to look at the requests and find the data inside of them will still be able to access that content, right? The front end... Front end measures are not going to stop them from doing this. So once again, all of these need to be implemented on the backend if you really care about enforcing them for each and every user. So to show you an example of this in more detail, what we're going to do is we're going to use Postman here to send a request to basically hack our server for something that we didn't enforce there, all right? So let's take a look at the update endpoint in a little bit more detail,
>
> **[3:04](https://www.linkedin.com/learning/react-authentication-25660373/never-trust-the-front-end?u=76281980&t=184)** and one thing you'll notice is that even though we went through the trouble of modifying this verify email endpoint so that we could actually tell whether a user was... Whether the email that a user gave us was real or not, we didn't actually do anything about that in the update endpoint, right? There's nowhere here where we actually check to make sure that the user's email is verified. And here's what that would look like in the backend. This is the right way to do it. What we're going to do is we're going to say... And we can just go down here to jwt.verified. We can just say "if," and then we'll have to get the "is verified" property out of decoded, so we'll say "is verified" like so. And then what we'll do is we'll say, "if not is verified," then we'll just want to do the same kind of thing as what we did up here but with maybe a different message. So we'll say "return response status 403," and then we'll say something like, "email must be verified." So in this case, no matter what we do on the front end, and even if we put together a request in Postman to try and get around this feature, it won't work because it's implemented on the server. However, if we had not done this, right? If we had left this out and basically just tried to implement this on the user info page. So if we were to say something like... For save changes, if we were to say "disabled equals," and then check whether
>
> **[4:36](https://www.linkedin.com/learning/react-authentication-25660373/never-trust-the-front-end?u=76281980&t=276)** the user is verified or not, right? So we'll say something like "disabled equals not is verified." That would be pretty easy to get around because, again, the user could basically not even use our front end and just send a request to the update endpoint from a tool like Postman, okay? So I'm going to change these back here. Well, I'm going to change this one back and I'll leave this one on the server. I'm going to un-comment that because that is important there. So anyway, that is the basic idea of not trusting the front end. Make sure you do this. And you might even want to look around our app here to make sure that there's no other places where we need to do this as well. But I'll leave that up to you.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **CLI Commands:** make (9), find (2)
> **Tools:** postman (4)
> **Best Practices:** best practice (2), always make sure (1)
> **Env Vars:** json (1), css (1)
> **Speakers:** - [instructor] (1)

#### [Authentication error handling](https://www.linkedin.com/learning/react-authentication-25660373/authentication-error-handling?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/react-authentication-25660373/authentication-error-handling?u=76281980&t=1)** - [Instructor] All right, so the best practice here, which again is probably pretty obvious, is that if something goes wrong in our application, we usually want to let our users know, right? If they've put in the wrong password, when logging in for example, we want to tell them that. And if they try and view a page that they're not authorized to see, we want to tell them that as well, and so on, right? Now, that being said, we also want to avoid telling our client too much about server-side errors that occur. And one thing that this means, is that in general, we don't want to just send the client the entire error object that our server caught in the "try...catch" block. Since this can usually contain some information that hackers could use to find vulnerabilities in our site, right? Sending this error back to the client can actually provide malicious users with a little too much information about our tech stack, such as what database we're using. And in rare cases, it can even expose things like the IP address of our database, which is generally not a good thing for hackers to know. And in cases where we've been really careless, we may even be sending back sensitive information such as a user's password hash or other information, right? For example, if we send back an error that occurred while we were hashing a user's password or comparing it, for example. Now you may have noticed that we did something a little contradictory when we set up our signup route. That is in our signup route, we actually do return a different status code if the user already exists with that email address. And this is where a little dilemma comes in,
>
> **[1:33](https://www.linkedin.com/learning/react-authentication-25660373/authentication-error-handling?u=76281980&t=93)** and that is, do we maximize [user experience](../../Skills/Web%20Development/User%20Experience%20(UX).md) at the expense of a little bit of safety? Or do we maximize safety with the possibility of frustrating our users, right? For example, if a user keeps trying to sign up with an email address that there's already an account for, because they don't know, right, because we're not telling them. Now, you'll notice that different websites tend to make different choices with regards to this. But in a nutshell, our goal here with errors is to make sure that our site is as safe as possible, while also making sure that our users actually know somewhat what's going on in our application, and are able to adapt accordingly. So it's really about striking a balance between safety and user experience in certain cases.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [User experience](../../Skills/Web%20Development/User%20Experience%20(UX).md) (2)
> **Analogies:** for example (4), such as (1)
> **CLI Commands:** make (2), find (1)
> **Best Practices:** best practice (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Catching & handling errors](https://www.linkedin.com/learning/react-authentication-25660373/catching-handling-errors?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/react-authentication-25660373/catching-handling-errors?u=76281980&t=1)** - [Instructor] Okay, so now that we've learned a few best practices related to error handling, let's go through our application and do just a little bit of cleanup as far as handling errors. And this should be pretty straightforward, all right? All I'm going to do is I'm going to go into the server, and I'm going to change the login route so that when the user enters in a password that's incorrect, we're not just going to send the entire error back, right? This exposes unnecessary data to the client side if something goes wrong on our server, and often can inadvertently reveal things about the technologies we're using behind the scenes as I said. So one thing that we can do here is instead of sending back the error like that, we can just send back like a status code like this, and that will make it a little bit, you know, a little bit less obvious what's going on. And also at the same time, a little harder to troubleshoot, which is why you'll often see things like that in development. But oftentimes people will forget to change that back during production, right, when it's actually set to be released. Now on the front end side of things, we're not really handling errors very well here either. So what I'd like to do is show you how we might handle errors in this on login clicked function, and you can go through some of the other pages and do this there as well. So, one obvious possibility here when the user tries to log in is that they put in the wrong password, right? So what we could do here, because this Axios.Post thing will throw an error if it doesn't receive a response
>
> **[1:36](https://www.linkedin.com/learning/react-authentication-25660373/catching-handling-errors?u=76281980&t=96)** with a 200 or 300 status code, we can actually just wrap this whole thing in a try-catch block. All right, so we'll just put that inside of there like so. And inside the catch block here, what we'll do is we'll just get the error, and we'll set that as a variable. I've already set this up for you in previous, you know, in the starting state of this code, we can actually set this error message here, and that will automatically show up if you scroll down to the JSX, above the form on this page. So let's give this a try. We're just going to say set error message, and we're going to set that to E.message, okay? And notice that I already have some logic in here too that will automatically hide that after a few seconds, right? All right, so let's go back and take a look at our application. Let's just hit refresh there, and make sure it's running if you already have it. And we're going to log out, and now we're going to try logging in as one of our users, but we're not going to use the correct password. So just put in basically any email that will get you this error. And then we'll say something like, blah, blah, blah. And if we click log in now, what we'll see is that sure enough, it says, "Request failed" with status code 401. Now you can customize this error message just by putting in your own string there. If there's an error. Right, you could say something like "Incorrect email or password," but I'll leave that one up to you as well. So anyway, that is the basics of handling errors, both on the back end and on the front end. So I hope this was helpful for you.

> [!info]- Semantic Content
>
> **CLI Commands:** make (2)
> **Env Vars:** jsx (1)
> **UI Navigation:** scroll down (1)
> **Warnings:** troubleshoot (1)
> **Speakers:** - [instructor] (1)

#### [Securing your database](https://www.linkedin.com/learning/react-authentication-25660373/securing-your-database?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/react-authentication-25660373/securing-your-database?u=76281980&t=1)** - [Narrator] Okay, well, we've learned quite a few best practices for off applications. And before we wrap this course up, there's one last best practice that I want to make you aware of. And this is one that most people don't even think about until it's too late, and that is securing your database. Now, in this course, we've used just a basic [JSON](../../Skills/Web%20Development/JSON.md) based database that is by no means meant for production. Chances are in production, you'll probably use something like [MongoDB](../../Skills/Software%20Development/MongoDB.md) or [SQL](../../Skills/Data%20Science/SQL.md), some sort of SQL database in order to store your data. And if you're going to publish your application and host it somewhere, what you'd probably end up doing is creating an instance of MongoDB somewhere most likely on the cloud. And this could mean potentially exposing our database to people who want to hack it and steal our data. Now, in most cases, whatever database provider we're using, right, if it's MongoDB, for example, will usually provide a basic security setup for you, right? MongoDB Atlas does this by default, for example. Now, in most cases, whatever database provider you're using will provide a basic security setup for you by default, right? MongoDB Atlas, just as an example, does this by default. However, if you're just setting up your own server from scratch and adding MongoDB to it, keep in mind that the default setup for MongoDB under many circumstances is actually to have most [Forms](../../Skills/Web%20Development/Forms.md) of security turned off. And the same may be true
>
> **[1:34](https://www.linkedin.com/learning/react-authentication-25660373/securing-your-database?u=76281980&t=94)** for whatever other database type you might be using. Now for instructions and details on how to secure a Mongo database, I highly recommend that you take a look at some of the MongoDB courses in this library. There are plenty of them that talk about this topic, and they're going to do a much more thorough job of explaining the details than we have room for in this course. And there's other, you know, there's the same thing for other database providers as well. So ultimately, the moral here is that whatever database you end up using, just make sure that you follow all of the appropriate security measures, which can usually be found somewhere in the database's documentation, and of course, in this library.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [MongoDB](../../Skills/Software%20Development/MongoDB.md) (8), [SQL](../../Skills/Data%20Science/SQL.md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (1), [Forms](../../Skills/Web%20Development/Forms.md) (1)
> **CLI Commands:** make (2), mongo (1)
> **Env Vars:** sql (2), json (1)
> **Prerequisites:** setup (3)
> **Analogies:** for example (2)
> **Best Practices:** best practice (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [narrator] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps in your React journey](https://www.linkedin.com/learning/react-authentication-25660373/next-steps-in-your-react-journey?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/next-steps-in-your-react-journey?u=76281980&t=0)** - [Shaun] All right, well, congratulations on making it to the end of this course. We have covered quite a few concepts related to user authentication in the context of [React](../../Skills/Web%20Development/React.js.md), and hopefully this really helps you going forward to make your applications more secure. So before I go on to some next steps, I just want to take a moment to personally thank you for watching through this course. Please feel free to send me a connection request on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) and I will be sure to accept it. It's always a great way to keep in touch and hear about my upcoming courses. Now, if you're looking for a few things to watch after this, I would highly recommend searching the LinkedIn library for a number of different courses. I have one course on Full-Stack React. And now that you've learned how to add basic user authentication to a React app, I would recommend watching my course on Full-Stack React and trying to add your own authentication flow to it. That would be a great way to solidify what you've learned here. In addition to that, there's a course on the React ecosystem that I have. You can find that by searching for React Modern Projects and looking for the course with my name next to it. And finally, there is a very popular course about React [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) that would really take your development skills to another level when combined with what we learned in this course. So feel free to search the LinkedIn Learning library for these. The titles might not be exactly what you see here, but they are great next steps for you. So anyway, thank you once again for watching this course,
>
> **[1:36](https://www.linkedin.com/learning/react-authentication-25660373/next-steps-in-your-react-journey?u=76281980&t=96)** and I hope to see you in another.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (7), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (3), [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) (1)
> **CLI Commands:** make (1), find (1)
> **Definitions:** is a  (1)
> **Speakers:** - [shaun] (1)


### Appendix

[↑ Back to Table of Contents](#table-of-contents)

#### [A tour of Coderpad](https://www.linkedin.com/learning/react-authentication-25660373/a-tour-of-coderpad?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-authentication-25660373/a-tour-of-coderpad?u=76281980&t=0)** - [Instructor] Now, before we get started, you might have noticed that there are a number of CoderPad challenges in this course, and that's what you see here. So, basically, CoderPad challenges are just interactive challenges where you'll be asked to complete or modify the code in this window here, and there's a number of other tools that are provided to help you out with this, right? Now, since these are going to be [React](../../Skills/Web%20Development/React.js.md)-related challenges, you'll see over here on the left hand side, the results of the code that you've written in this answer block, and over here on the right hand side, underneath the answer block, this is some automated test code that will check to see whether your answer that you've created is correct or not. Now, you can run this test code just by clicking Test my code down here, and if something isn't complete or if something isn't correct, you'll see that the test will fail there, right? So, what you ideally are going to see after you've written your answer is that the corresponding tests will pass. All right, and finally, up here in the top left hand corner, that's where the instructions are. That just gives you a lot more details about the challenge. So, anyway, that's pretty much all that you should need when you run into a CoderPad challenge, so hopefully you enjoy these. Let's get started with the course now.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [Shaun Wassell](../../Instructors/Software%20Development/Shaun%20Wassell.md)

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/react-authentication-6035050)

## Skills Covered

- React.js
- Authentication Systems

## Path Context

### In [Explore React.js Development](../../Paths/Software%20Development/Learning%20Paths/Explore%20React.js%20Development.md)
← [Building Modern Projects with React](Building%20Modern%20Projects%20with%20React.md) | **7 of 8** | [React- Server-Side Rendering](../Software%20Development/React-%20Server-Side%20Rendering.md) →

### In [Explore App Development with the MERN Stack](../../Paths/Database%20Management/Learning%20Paths/Explore%20App%20Development%20with%20the%20MERN%20Stack.md)
← [React Essential Training](React%20Essential%20Training.md) | **2 of 13** | [Node Js Essential Training](Node%20Js%20Essential%20Training.md) →

### In [Improve Your React Skills](../../Paths/Web%20Development/Learning%20Paths/Improve%20Your%20React%20Skills.md)
← [React- Testing and Debugging](React-%20Testing%20and%20Debugging.md) | **6 of 9** | [React- Software Architecture](React-%20Software%20Architecture.md) →

## Appears In

- [Explore React.js Development](../../Paths/Software%20Development/Learning%20Paths/Explore%20React.js%20Development.md)
- [Explore App Development with the MERN Stack](../../Paths/Database%20Management/Learning%20Paths/Explore%20App%20Development%20with%20the%20MERN%20Stack.md)
- [Improve Your React Skills](../../Paths/Web%20Development/Learning%20Paths/Improve%20Your%20React%20Skills.md)

## Related Courses

_Courses sharing skills:_

- [React- Cloud-Powered Apps with Firebase](React-%20Cloud-Powered%20Apps%20with%20Firebase.md) — React.js
- [Building a GraphQL Project with React.js](Building%20a%20GraphQL%20Project%20with%20React.js.md) — React.js
- [React- Software Architecture](React-%20Software%20Architecture.md) — React.js
- [React- Testing and Debugging](React-%20Testing%20and%20Debugging.md) — React.js
- [React- Building Styles with CSS Modules](React-%20Building%20Styles%20with%20CSS%20Modules.md) — React.js

---

[↑ Back to top](#)