---
type: course
cssclasses:
  - lle-course
slug: tls-for-beginners-securing-network-communications
url: "https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications"
duration_minutes: 183
duration: 3h 3m
exercise_files: false
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQHxXQM9HsuLfg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1715892606980?e=2147483647&amp;v=beta&amp;t=12Mu0DYuPdr366ciLJd1tGoiO2RY-Qsh_jDVuMakpTw"
linkedin_topic: Cybersecurity
learning_paths:
  - '[[Become an IT Security Specialist]]'
  - '[[Cybersecurity Fundamentals]]'
prev_courses:
  - '[[IT Security Foundations- Network Security]]'
  - '[[Introduction to Identity and Access Management]]'
next_courses:
  - '[[Cybersecurity with Cloud Computing]]'
  - '[[Cybersecurity with Cloud Computing]]'
path_nav: '[{"path":"Become an IT Security Specialist","position":5,"total":12,"prev":"IT Security Foundations- Network Security","next":"Cybersecurity with Cloud Computing"},{"path":"Cybersecurity Fundamentals","position":10,"total":12,"prev":"Introduction to Identity and Access Management","next":"Cybersecurity with Cloud Computing"}]'
path_count: 2
tags:
  - course
  - topic/security
  - topic/cloud-computing
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Cybersecurity/TLS%20for%20Beginners-%20Securing%20Network%20Communications.md)

![TLS for Beginners: Securing Network Communications](https://media.licdn.com/dms/image/v2/D560DAQHxXQM9HsuLfg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1715892606980?e=2147483647&amp;v=beta&amp;t=12Mu0DYuPdr366ciLJd1tGoiO2RY-Qsh_jDVuMakpTw)

# TLS for Beginners: Securing Network Communications

> Transport Layer Security (TLS) provides the backbone of securing Intranet and Internet network communications through encryption and authentication. In this course, Daniel Lachance—consultant, trainer, and IT evangelist—guides you through acquiring the skills necessary to plan, implement and maintain a Public Key Infrastructure (PKI) ecosystem that supports TLS security certificates. Learn how to 

> [LinkedIn Learning](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications) | 3h 3m
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Learn to secure your network communications](#learn-to-secure-your-network-communications)
  - [What you should know](#what-you-should-know)
- [**1. PKI Overview**](#1-pki-overview) (8 videos)
  - [Cryptography](#cryptography)
  - [Hashing and digital signatures](#hashing-and-digital-signatures)
  - [Public key infrastructure](#public-key-infrastructure)
  - [Certificate authorities](#certificate-authorities)
  - [Certificates](#certificates)
  - [Certificate lifecycle management](#certificate-lifecycle-management)
  - [Transport layer security](#transport-layer-security)
  - [Analyze a tls packet capture](#analyze-a-tls-packet-capture)
- [**2. Certificate Authorities**](#2-certificate-authorities) (4 videos)
  - [Configure a Microsoft AD CS certificate authority](#configure-a-microsoft-ad-cs-certificate-authority)
  - [Configure Microsoft AD CS certificate templates](#configure-microsoft-ad-cs-certificate-templates)
  - [Configure a Linux OpenSSL certificate authority](#configure-a-linux-openssl-certificate-authority)
  - [Configure an AWS Certificate Manager CA](#configure-an-aws-certificate-manager-ca)
- [**3. Acquiring Certificates**](#3-acquiring-certificates) (5 videos)
  - [Acquire a certificate using Microsoft AD CS](#acquire-a-certificate-using-microsoft-ad-cs)
  - [Acquire a web server certificate using OpenSSL](#acquire-a-web-server-certificate-using-openssl)
  - [Acquire a public CA certificate](#acquire-a-public-ca-certificate)
  - [Install private CA certificates](#install-private-ca-certificates)
  - [Generate certificates using Microsoft Azure](#generate-certificates-using-microsoft-azure)
- [**4. PKI Certificate Usage**](#4-pki-certificate-usage) (6 videos)
  - [Configure a Windows website certificate](#configure-a-windows-website-certificate)
  - [Configure a Linux website certificate](#configure-a-linux-website-certificate)
  - [Configure web brower client certificate authentication](#configure-web-brower-client-certificate-authentication)
  - [Configure VPN certificate authentication](#configure-vpn-certificate-authentication)
  - [Configure a network printer certificate](#configure-a-network-printer-certificate)
  - [Acquire a web app certificate using Microsoft Azure](#acquire-a-web-app-certificate-using-microsoft-azure)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Learn to secure your network communications](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/learn-to-secure-your-network-communications?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/learn-to-secure-your-network-communications?u=76281980&t=0)** - [Dan] This course exposes you to PKI and TLS configurations that can mitigate [[Network Security]] threats. You can follow along with demonstrations that configure a PKI to enable secure TLS communications for various platforms. Hi, I'm Dan Lachance, join me in this course, TLS for Beginners: Securing Network Communications. Let's use PKI and TLS to harden your IT computing environment.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Network Security]] (1)
> **Env Vars:** tls (4), pki (3)
> **Prerequisites:** configure (1)
> **Speakers:** - [dan] (1)

#### [What you should know](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/what-you-should-know?u=76281980&t=0)** - [Narrator] What kind of prerequisite knowledge should you have when you come into the TLS for Beginners Securing Network Communications course? Well, for starters, we should have a bit of knowledge related to the TCP/IP protocol suite because we will be referring to things like IP addresses and DNS names. We should have a general sense of how to navigate through the [[Microsoft]] [[Windows Server]] operating system environment. We'll be working both in the GUI, but we'll also be working at the command line. Nothing too extravagant, but we should know how to move around the Windows server environment, as well as the [[Linux]] operating system environment. Now in Linux, we're going to be doing everything exclusively from the command line. We are not going to be relying on any kind of GUI window manager in Linux, but do we have to be a command line expert? Certainly not, just a general sense of how to move around in Linux and what it's about. The same goes for web browsers and web server configurations. Whether you're familiar with using the Safari, the Firefox, [[Google]] Chrome, Microsoft Edge web browser, really doesn't matter, we just have to have a general sense of the purpose of a web browser and that some of its settings can be configured such as configuring trusted route certification authorities. On the web server side, we're going to be working with the Microsoft Internet Information Services or IIS Web Server, specifically focusing on the security aspect of it, as well as the Apache web server on the Linux platform.
>
> **[1:34](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/what-you-should-know?u=76281980&t=94)** We should have a basic understanding of what the purpose of [[Amazon Web Services (AWS)|Amazon Web Services]] or AWS is, as well as [[Microsoft Azure]]. These are [[Cloud Computing]] environments where we can deploy things like [[Virtual Machines]], virtual networks, but the focus really here is working with PKI certificates in the cloud hosted environment. Our course lab environment will consist of a number of items including a Microsoft Windows Server 2022 [[Active Directory]] domain controller. The domain is called domain1.local and it's already configured. We will be installing an IIS Web server stack on that [[Windows]] host, and we will also be installing and configuring the certificate authority role to support a private internal PKI environment. We'll also be using an [[Ubuntu]] Linux host so that we can work with OpenSSL to set up a certificate authority and also to install and manage an Apache web server. We'll also be using a network printer in this course where we manage network printer certificates that are used to secure the remote management connection between a web browser and the network printer. There are plenty of network printer and [[Wi-Fi|wifi]] router and [[IoT]] device simulators available out there on the internet if you don't have a network printer to actually follow along with. In Amazon Web services or AWS, we'll be using it primarily for certificate manager where we can establish
>
> **[3:10](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/what-you-should-know?u=76281980&t=190)** a private certification authority or CA. In the Microsoft Azure Cloud, we'll be using it primarily for a key vault with a certificate in it that's used to secure an [[Microsoft Azure|Azure]] hosted web application. We'll also use it to configure a point to site or otherwise called a client to site VPN. So this gives you a sense of the general knowledge base that you should have already entering this course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (6), [[Microsoft]] (4), [[Windows Server]] (3), [[Amazon Web Services (AWS)|Amazon web services]] (2), [[Microsoft Azure]] (2)
> **Env Vars:** gui (2), iis (2), aws (2), pki (2), tls (1)
> **Tools:** command line (3), safari (1), firefox (1)
> **CLI Commands:** apache (2), aws (2)
> **Prerequisites:** prerequisite (1), set up (1), install (1), configure (1)
> **Definitions:** is called (1)
> **Analogies:** such as (1)
> **Speakers:** - [narrator] (1)


### 1. PKI Overview

[↑ Back to Table of Contents](#table-of-contents)

#### [Cryptography](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/cryptography?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/cryptography?u=76281980&t=0)** - [Instructor] In order to fully understand Transport Layer Security, TLS, we first have to have an understanding of how [[Cryptography]] fits into that picture. When we talk about cryptography, we're talking about using keys when it comes to encryption, and those keys can be stored in public key infrastructure or a PKI certificate. Keys can be stored or burned into a smart card or a common access card, a CAC. Of course, this is about the size of a credit card and can be used to authenticate to a computer or perhaps to gain access to a facility. Keys can also be stored within a file on a device, in trusted platform module or TPM firmware that is available in most modern computing devices. It could also be part of a token device that a user might use to authenticate to a VPN. And cryptographic keys might also be stored in a [[Hardware]] security module, an HSM. HSMs are dedicated security appliances that are designed to focus on cryptographic operations, including secure key storage. The general encryption process starts with plain text or origin data that gets fed into an encryption algorithm. Next, a key is used in conjunction with the encryption algorithm to result in ciphertext. That's the scrambled data. In order to decrypt that ciphertext, to reverse the process to reveal the original data, you need to have the correct decryption key.
>
> **[1:36](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/cryptography?u=76281980&t=96)** Cryptography is used in many places. It's used to encrypt mobile device storage. It can be used to encrypt files on any type of operating system. It can be used to encrypt network traffic, which is really going to be our focus when it comes to TLS, or Transport Layer Security. Cryptography can be used for file hashing to determine if files can be trusted and haven't been tampered with. And cryptography is also used with [[Cryptocurrency]] [[Blockchain]] transactions to assure that those transactions are valid and haven't been tampered with. In cryptography, we have this notion of symmetric encryption, which uses a single or secret key. So, this secret key is used to both encrypt and decrypt. All parties that need to encrypt and decrypt data would have to have that same key, and so we need a secure way to get that key to those parties, and it also must be stored in a secured manner. Symmetric encryption [[Algorithms]] include AES, that's the US government standard, the Advanced Encryption Standard. But there are other algorithms like RC4, triple DES or 3DES, which is set to be deprecated by the end of 2023 officially, although some [[Microsoft Products|products]] will still be in use beyond that, and the Blowfish algorithm. Each of these have their own respective maximum key size. Now, we don't have to memorize these specific algorithms and their key sizes, but they will pop up occasionally
>
> **[3:10](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/cryptography?u=76281980&t=190)** with some products when you are determining what you want to use to secure network transmissions. Now we have the notion of asymmetric encryption. This uses two mathematically related keys, a public key, which can be shared with anybody, and a private key, which must be kept private to the owner of the key. This is used often with PKI certificates. Normally, the public key is embedded in the PKI certificate, but the private key is stored externally, but can be associated with the certificate. So, with asymmetric encryption, we have a different set of algorithms, such as RSA. RSA is the standard that's used with Transport Layer Security, or TLS. But there are other algorithms like the Diffie-Hellman key exchange algorithm, ELGamal, and ECC, and they have their maximum key size. You'll often see ECC used on mobile computing devices as keys can be very small, yet still very powerful. Smaller keys means less computing power required. Here we have an example of asymmetric email encryption where the sender would encrypt a message, but they would need to have the public key of the recipient. In turn, the recipient when they receive the message to decrypt it would use their related private key to decrypt the message.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Cryptography]] (6), [[Algorithms]] (5), [[Microsoft Products|Products]] (2), [[Hardware]] (1), [[Cryptocurrency]] (1)
> **Env Vars:** tls (3), pki (3), rsa (2), ecc (2), cac (1)
> **Tools:** notion (2)
> **Analogies:** picture (1), such as (1)
> **Prerequisites:** you need to have (1)
> **Speakers:** - [instructor] (1)

#### [Hashing and digital signatures](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/hashing-and-digital-signatures?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/hashing-and-digital-signatures?u=76281980&t=0)** - [Instructor] An important part of transport layer security besides encryption is hashing and digital signatures. Let's talk about how this can be used to secure communications or also to secure files to make sure they haven't been tampered with. Hashing uses a one-way cryptographic algorithm that results in a unique value. Now, that unique value is based on the data fed into the one-way algorithm, whether it's a network transmission or a file stored on storage media. So this resultant unique value is called the hash, or sometimes it's referred to as the message digest. It's all the same thing. Now, what is this used for? This can be used to verify the integrity of the network message or a file, or machine boot up settings, or it could be secure password storage on a [[Linux]] host. It's used with [[Blockchain]] transaction integrity. Now, how does that work? Why does it get used to verify integrity? In our little screenshot example, we are on a machine running a [[Powershell]] cmdlet called Get-FileHash. We are then specifying in the current directory a JPEG image, the file is called doggie.jpg. What happens is we get resultant output telling us the hashing algorithm that was used, so in this case, secure hashing algorithm or SHA256, and we have a unique resultant hash. Now that resultant hash is based on the state of the doggie.jpg file at this point in time.
>
> **[1:36](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/hashing-and-digital-signatures?u=76281980&t=96)** If anything at all changes in that file, the next time we generate the hash from that file, the hash will be different, and so we will know that the file has changed. There are numerous hashing [[Algorithms]] out there. SHA stands for secure hashing algorithm and SHA-2, which is often referred also to as SHA-256, that represents the number of bits and also SHA-512, again, 512 bits. These are common hashing algorithms that are used all over the place, including by Bitcoin. You've also got SHA-3, you've also got RIPEMD. RIPEMD replaces an older obsolete hashing algorithm that was called MD-5 or Message Digest version five. There are other hashing algorithms beyond this, but we should have a general awareness. Just like with encryption, having multiple algorithms available, there are also multiple hashing algorithms available out there. Now let's focus on the digital signature. We aren't talking about having a signature pad and using a pen on a screen to sign your name. Instead, what we're talking about here is a digital signature at the cryptographic level. This is used to provide data authentication so that we know data comes from, who it says it came from over the network. It's also used to provide integrity, so we know that the transmission hasn't been tampered with, and it also supports something called non-repudiation,
>
> **[3:11](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/hashing-and-digital-signatures?u=76281980&t=191)** which means that the sender cannot deny having sent that transmission. We'll talk about why that is shortly but for now, it's just because they have to use their private key to generate the signature. However, digital signatures do not provide data confidentiality, which really means encryption. This is a separate discussion than that, although you can use encryption and digital signatures together for the utmost in security with network transmissions. So digital signatures most certainly are used with TLS, with transport layer security. You can have digital signatures available within an application like an email program, if you choose to digitally sign an outgoing message. You can digitally sign a script, so it's trusted to run on a machine. Device drivers are commonly digitally signed so that they are trusted by the operating system. So we encrypt hash values using a private key that is what the digital signature is. So we have a hashing algorithm resulting in a unique message digest, that digest gets encrypted with the sender's private key. Then on the other end, the signature is verified by the recipient of that network message with the matching public key of the sender. So the private and the public key pair, in this case we're referring to, belong to the sender of the network transmission. Pictured on the screen, we have an example of an email program where the user can choose to encrypt and, or digitally sign the email message.
>
> **[4:48](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/hashing-and-digital-signatures?u=76281980&t=288)** It can also be set as a default. So in the settings within most email programs, users would have the option to automatically encrypt the contents of the message and also add digital signatures. Bear in mind that when you encrypt a message over the network, you need the public key of the recipient because you encrypt with that key, they decrypt with their related private key. But digital signatures are created using the sender's private key and are verified with the sender's public key on the other end of the transmission. So the email digital signature process then, begins with step one, with the sender of a network transmission choosing to digitally sign a message. Doesn't have to be email, it could be just a web browser making a secure HTTPS connection to a web server. In step two, a hash of the message data is generated. In step three, the sender's private key is used to encrypt the message hash, that is the digital signature. In step four, the message and the digital signature are then sent over the network to the recipient. In step five, the recipient uses the sender public key to decrypt the signature, which then yields the hash value. And finally, in step six, the recipient uses the same hashing function used to generate the original hash in step two, to compute the hash value of the send data.
>
> **[6:23](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/hashing-and-digital-signatures?u=76281980&t=383)** In step seven, if the decrypted hash and the computed hash are equal, then the message is determined to be authentic.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Algorithms]] (5), [[Linux]] (1), [[Blockchain]] (1), [[Powershell]] (1)
> **Env Vars:** sha (5), ripemd (2), jpeg (1), sha256 (1), tls (1)
> **Definitions:** is called (2), is a  (2), stands for (1), means that (1)
> **CLI Commands:** make (1)
> **Tools:** powershell (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Public key infrastructure](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/public-key-infrastructure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/public-key-infrastructure?u=76281980&t=0)** - [Instructor] The public key infrastructure, otherwise called PKI, is an important part of how transport layer security or TLS works. Let's define why that's so. Let's start first of all by defining a public key infrastructure. What it is is a hierarchy of digital security certificates that can be used for many things like encrypting email, encrypting files, encrypting web server transactions, digitally signing network messages, to name but just a few options. So certificates always contain, among other things, public keys. To have a PKI certificate, you first have to generate a public and a private key pair. So the public key part is stored in the certificate. The private key is not stored in the certificate, but can be associated with the certificate. So this is what we deal with when it comes to PKI and the keys. Now, keys can also be stored on a smart card, just like an entire PKI certificate could be part of a smart card, but the hierarchy consists of a number of components, the first of which is a certificate authority, otherwise called a CA. The CA is responsible for issuing certificates, for renewing them prior to their expiration date. It also can revoke certificates, such as if we have a lost user smartphone and there was a PKI certificate perhaps on it for authenticating to a VPN. By revoking a certificate,
>
> **[1:32](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/public-key-infrastructure?u=76281980&t=92)** it is not trusted and can't be used. The CA also maintains the certificate revocation list or CRL and should be taken offline when not used. Now, specifically, we're talking about the root CA at the top of the hierarchy. It can be used initially to establish the PKI hierarchy, but it should be taken offline afterwards for security reasons because if the root CA at the top of the hierarchy is compromised, so is everything under it. And that's why we have the concept of a registration authority or an RA, which is sometimes also called a subordinate CA. It's the same thing. So the RA and the subordinate CA are the same thing, but they differ from the root CA. So these can be used to issue and manage certificates. You might have a number of different registration authorities for different departments in a large organization, different projects, maybe different regions around the world that have different technician teams. We also then have other components like the certificate revocation list, which we've mentioned, or CRL. And you can also use the online certificate status protocol or OCSP to validate that certificates are valid and haven't been revoked before they are used. Then we have certificate templates, which, of course, as the name implies, serve as blueprints that are used when issuing certificates. So you might have a certificate template that you use when issuing certificates for users
>
> **[3:05](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/public-key-infrastructure?u=76281980&t=185)** to be used for mail encryption, which would be different than a certificate template used for [[Web Servers]] for PKI certificates to enable HTTPS bindings. And, of course, the last PKI component is the digital or security certificate itself. In it, it will contain the subject name, like the fully qualified DNS domain name of the website or for a user, in that case, it would be the user email address. It would have the digital signature of the certificate authority that issued the certificate. It would have expiry information, whether it expires in one year, two year, five years. That's defined in the certificate template. And, of course, we know the certificate would contain the public key. Let's visualize the hierarchy. So in a single-tier PKI hierarchy, at the top, we would have the certificate authority. Many times you'll see that listed as being the root CA, under which it could directly issue certificates to users, devices, and applications. So users might need certificates for encrypting files or email. Devices might use certificates for authenticating to a VPN. Apps might use it to authenticate perhaps to a centralized identity provider. But in this case, we don't have registration or subordinate authorities in a single-tier high hierarchy. So, again, we have the certificate authority, which we'll call the root CA, at the top of the hierarchy,
>
> **[4:37](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/public-key-infrastructure?u=76281980&t=277)** but instead of it issuing certificates directly, in this example, it issues certificates and establishes two registration authorities. It could be one, it could be two, it could be 100, it doesn't matter how many, but in this particular case, it is the registration or the subordinate CAs that issue the certificates for users, devices, and applications. Now, this is a classic case of where the root CA should be taken offline. If one of the registration authorities got compromised through a security breach, only everything under it would be compromised, not the entire PKI hierarchy. So this gives us a sense then of what the PKI hierarchy consists of.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Web Servers]] (1)
> **Env Vars:** pki (11), vpn (2), crl (2), tls (1), ocsp (1)
> **Definitions:** is a  (3), is an  (1)
> **Exercise Files:** template (3)
> **Analogies:** just like (1), such as (1)
> **Speakers:** - [instructor] (1)

#### [Certificate authorities](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-authorities?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-authorities?u=76281980&t=0)** - [Instructor] We already know that one important part of the PKI hierarchy is the certificate authority, otherwise called a CA. So let's focus on that for a few minutes. When it comes to a certificate or a registration authority, this is the component that is responsible for issuing PKI certificates. You can call them security certificates, digital certificates, X.509 certificates. It doesn't matter, it's all the same. But the CA issues the certificate. It can renew the certificate before it expires. That can be a manual process. It can also be automated. And also, the CA can revoke certificates that should no longer be trusted. Maybe the certificate's been compromised because a user mobile phone has been stolen. So a CA has a longer validity period than a standard issued certificate that it will issue to a user, a device, or an app. Where a user certificate, for example, depending on the certificate template, might be valid for two years, then a certificate authority certificate would be valid, normally, for 10 years. These things can be changed, but these are just standard examples. Now, the CA can also publish the list of revoked certificates. That's called a certificate revocation list or, for short, technicians often just call it a CRL. CAs can also issue certificates to subordinate registration authorities or subordinate CAs, and those registration authorities would, in turn, issue entity certificates
>
> **[1:32](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-authorities?u=76281980&t=92)** instead of the top-level root CA doing it itself. It doesn't matter how you do it. Every option is valid. Depends on the scenario and what your organization needs to do. However, bear in mind that a compromised top-level certificate authority means that all subordinate certificates are compromised. So if the root-level CA is compromised, the entire hierarchy is compromised also. But if only one registration authority is compromised, then only its subordinate certificates that it has issued are considered to be compromised. We've already mentioned a multitier hierarchy that would have the root certificate authority at the top of the hierarchy, under which we would have registration authorities, shown here as Registration Authority 1 and Registration Authority 2, perhaps for different departments or different technical admin teams that would issue certificates. So ultimately, each registration authority issues certificates to be used for users, devices, or applications. Now, this hierarchy is important because it helps us explain what the PKI chain of trust is. So the first thing we have to understand is that, for all of this to work, certificate authorities must be trusted authorities. Just like in order for a driver's license or a passport to be trusted, we have to trust the issuing government department. So the CA needs to be trusted. Otherwise, PKI is meaningless.
>
> **[3:06](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-authorities?u=76281980&t=186)** So that's why we have the notion of a CA trusted root certificate, which also contains a public key that gets installed on user devices, whether it's a [[Linux]] [[Windows Server]], a [[Windows]] laptop, whether it's a an Apple iPhone, it doesn't matter. Those devices need to have trusted root certificates for trusted CAs installed. Now, what this means then is that the device will then trust certificates issued from that CA. So when you look at most modern operating systems, regardless of the type of device it's on, there is a standard list of publicly trusted certificate authorities. So when you connect, for example, to do online [[Banking]] using your device, that certificate is trusted for that website because it was issued by a trusted certificate authority that your device trusts. Now, this also means that the trust is established through digital signatures. Every certificate contains the digital signature of the issuing CA. For instance, your bank website will use a PKI certificate so that you can connect over HTTPS. And that bank web server certificate will contain the digital signature of whatever CA issued it. So digital signatures are created by the CA using its private key, and that signature is verified, for example, by connecting devices using the CA's related public key. Because when you install
>
> **[4:41](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-authorities?u=76281980&t=281)** a trusted root certificate on a device, you're installing the public key of that certificate authority. Let's visualize it here. We've got a screenshot of a Windows computer where we're viewing trusted root certification authorities. Now, what you'll have on devices, as we've mentioned, are standard public trusted CAs. So you can also create your own internal self-signed CA within your organization, where you control the entire PKI hierarchy. You can do that, but the thing to bear in mind is that, by default, devices will not trust certificates that are issued by that self-signed CA. So what this means then is that we'd have to install the CA trusted root certificate for the self-signed CA on all of our internal devices so that we could establish a chain of trust. In other words, so that our internal devices would trust certificates issued by our internal self-signed CA.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (2), [[Linux]] (1), [[Windows Server]] (1), [[Banking]] (1)
> **Env Vars:** pki (6), crl (1), https (1)
> **Analogies:** for example (3), just like (1), for instance (1)
> **Definitions:** means that (2), is a  (1), in other words (1)
> **Prerequisites:** install (2)
> **Code Identifiers:** iphone (1)
> **Tools:** notion (1)
> **Exercise Files:** template (1)

#### [Certificates](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificates?u=76281980&t=0)** - [Instructor] Let's take a few minutes to talk about digital certificates. These are an important part of TLS, because as an example, if you want to use TLS to secure web server communications over HTTPS you need a digital certificate issued to that web server. So let's focus on the certificate for a few moments here. A PKI certificate is also often called a digital certificate, a security certificate, an X.509 certificate. All of those names are synonymous. You might even see some interface tools refer to it as an SSL certificate even though the secure sockets layer or SSL protocol is deprecated. So PKI certificates, as we know, are issued by certification authorities to either users or devices or even applications. And certificates have a specific use, whether it's for file encryption, whether it's for securing network traffic using TLS, whether it's for digital code signing so that would be of interest to software developers. Whatever the case is, whether it's one or a combination of these or other uses, that usage is defined clearly in the certificate template which is used to issue the certificate. Now the certificate itself can be stored in a multitude of ways. It could be stored in a file on a device. And if that certificate is linked to a private key, then the private key part definitely needs to be protected, for example, by adding a pass phrase.
>
> **[1:33](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificates?u=76281980&t=93)** But certificates can also be on smart cards. For example, your users might insert a smart card into a laptop that has a built-in smart card reader in order to send secured messages or to authenticate to a VPN. Also, certificates can also be stored in firmware that's designed to do that, whether it's TPM on an individual device or a standalone HSM appliance. So the items in a PKI certificate will include things such as a version number. This would be the version number of, for example, the template that was used to generate that certificate. Every certificate gets assigned a unique serial number. This is important because if that certificate is revoked in the future, it's the serial number that gets tracked in the revocation list. The certificate will also contain the digital signature of the issuing CA, as well as the algorithm that was used. That would be a hashing algorithm. What's interesting about this, remember, is that this establishes the PKI chain of trust. Because if we trust the CA, then we're going to trust whatever certificates it issues and digitally signs. Certificates also contain a validity period because they expire eventually, much like a passport or a driver's license does. Whether it's one year, two year, five years, that comes from the certificate template used to issue the certificate. As we've mentioned,
>
> **[3:07](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificates?u=76281980&t=187)** also the certificate usage details are included in the certificate, such as if it's designed to be used on a [[Windows]] machine with encrypting file system or it's designed to be used for securing communications for a web server. Of course, the subject name is embedded within the certificate, whether that is a URL or an FQDN, a fully qualified domain name for a website. Maybe it's a user email address. Also, the public key is part of the PKI certificate, but not the private. The private key is not stored within the certificate. Then we have the notion of wild card certificates. Here in our screenshot, we've got a subject alternative name or a SAN here for [digicert.com](https://digicert.com), www.[cert.com](https://cert.com). However, when we select that, we have a number of other entries like admin.[digicert.com](https://digicert.com), [digicert.com](https://digicert.com), content.[digicert.com](https://digicert.com), and so on. This is an example of a wild card certificate. So a wild card certificate can be used for multiple DNS second level domains via the subject alternative name or the SAN field. The certificate is issued to the top level DNS domain, so it can be used by everything that uses that name. So for example, if we've got a wild card certificate that is issued to [fakecorp.com](https://fakecorp.com), then that same certificate could be used for canada.[fakecorp.com](https://fakecorp.com),
>
> **[4:41](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificates?u=76281980&t=281)** and also perhaps for another website that's named europe.[fakecorp.com](https://fakecorp.com). The benefit here is we don't have to issue and manage individual unique certificates for each unique DNS name or web server in this example. Although it's not limited to only web server usage. We've alluded to the certificate revocation list or the curl, the CRL as it's called. We know that the revocation list is published by the CA and that it is a list of revoked certificate serial numbers. We've also mentioned that revocation reasons could include a lost or a compromised user device that had one or more PKI certificates on it. Or if an employee leaves the organization, then we might terminate any certificates issued to them in the sense of revoking them so they cannot be used. They're no longer considered valid, but this only works properly in your PKI environment if, for example, apps that are used for secured connections would retrieve the CRL before using those certificates. That could be an app on a client-side device as well. So in other words, the CRL has to be checked in order for this to be useful in your hierarchy at all. We have variations of this, such as the online certificate status protocol, otherwise called OCSP. OCSP consists of a client component on connecting devices
>
> **[6:15](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificates?u=76281980&t=375)** and a server-side component called the responder, the OCSP responder. This is different than a curl in that instead of downloading the entire list of all revoked certificate serial numbers, in this case, we can query the status of a specific single certificate. So unlike the curl, we don't have to download the entire list. That also leads us into OCSP stapling. So what happens with this is that the certificate owner can check the CA for the status of the certificate periodically. So in other words, a web server that has been issued a TLS certificate, of course, we would probably just call that a digital certificate, can check the CA to make sure that the certificate has not been revoked. Clients that connect to, in this example, the secured website can receive OCSP status for that website. And so what this means then with stapling is that clients don't have to query the OCSP responder all of the time for website certificate status because they already have that status known and they don't have to keep checking over and over again.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (1)
> **Env Vars:** pki (7), ocsp (6), tls (4), dns (3), crl (3)
> **URLs:** [digicert.com](https://digicert.com) (4), [fakecorp.com](https://fakecorp.com) (3), [cert.com](https://cert.com) (1)
> **Analogies:** for example (5), such as (3)
> **Definitions:** is a  (2), in other words (2), is an  (1)
> **CLI Commands:** curl (3), make (1)
> **Exercise Files:** template (3), download the (1)
> **Tools:** notion (1)

#### [Certificate lifecycle management](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-lifecycle-management?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-lifecycle-management?u=76281980&t=0)** - [Narrator] Working with TLS to secure network communications means that we have to have a solid understanding of the PKI certificate lifecycle. If, for example, we have an expired certificate used by a web server, client connections will no longer work correctly. Clients will get warnings, for example, in a web browser that state that the certificate is not valid. Naturally, we want to avoid this. Let's talk about the lifecycle. The first phase of the certificate lifecycle is the request to get a certificate, followed by the issuance of the certificate from a certificate authority. The certificate is then used during its lifetime, such as for a web server or for email encryption by a user, whatever the case might be. The certificate then potentially might be revoked, such as if it were on a smart card that's used for VPN authentication and the user has lost the card. Then, the certificate can be renewed prior to when it is expiring so that it can continue to be used, or otherwise, the certificate just expires, for example, after two years, which means it no longer can be used. Let's go through each of the phases, starting with the certificate signing request, which is really often just sorted down to CSR. In order to have a certificate, you need to have a unique public and private key pair. This needs to be generated first, and if you're setting up your certificate signing requests manually, for example, at the command line using a tool like OpenSSL, then you would generate the key pair first.
>
> **[1:35](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-lifecycle-management?u=76281980&t=95)** But these days, most front-end GUIs do this for you in the background. Now, the public key can be shared with anybody, but the private key needs to be kept private, and often, if it's stored as a file on a machine, it's password-protected. So the CSR is generated next, which includes a unique public key commonly in a format called PKCS #10 format, although there are other formats. PKCS just stands for Public Key [[Cryptography]] Standards. So the CSR then is sent to the certificate authority for verification and signing so that you get issued the certificate. So the CSR then is interesting because this can be done in a variety of ways. It could be done at the command line if you have a self-certified CA that you're going to be using, or it might be part of what you paste into a web form if you're getting a certificate that you want digitally signed by a publicly trusted CA. The certificate signing request will look something like what we have here in our screenshot. Notice that we have a Begin New Certificate Request at the top. The bottom, we have the end new certificate request, and then we have what looks like scrambled data in between, and that will include the public key. However, there are additional items required when you create a CSR, depending on who you're getting the from, such as locale information, city, state, country, the subject Fully Qualified Domain Name, or FQDN. So if you are creating a CSR to get a certificate
>
> **[3:11](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-lifecycle-management?u=76281980&t=191)** for a website, then you have to make sure that the subject FQDN is set to the name that people would be using to connect to the site, for instance, they have to match. So www.[fakesite.com](https://fakesite.com), as an example. Or if it's for a user certificate, then it would contain the user email address, and again, it must be spelled correctly for the user to use it with their mail account. Now that's the request for a certificate. Once the certificate gets issued, this could be something that is a manual process or it could be automated, but once the certificate is issued, it can then be used. What's important to know is that it might require an administrator administrative approval before the certificate is actually issued, or the process could be completely automated. So for new devices such as computers joining a [[Microsoft]] [[Active Directory]] domain, or smartphone connecting to a mobile device management or MDM system, if those devices are already trusted, then the entire process of requesting and issuing a certificate can be completely automated. So the certificate then would be stored in the device trusted certificate store, or it could be stored on other media such as a smart card. Here we have a screenshot example of the simple certificate enrollment protocol, otherwise called SCEP, S-C-E-P. In this case, this screenshot is from Microsoft Intune, which is a mobile device management or MDM solution,
>
> **[4:44](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-lifecycle-management?u=76281980&t=284)** where on the left we can select the platform, what's shown here is [[Android]] Enterprise. On the right, we can specify the certificate type. Here it's shown as a user certificate, where the subject name format uses the user common name. We can also specify the validity period. Here it's set to two years, how the key will be used, the key size and bits, the hashing algorithm that will be used, because remember, a certificate contains the digital signature of the certificate authority. Now, the next phase in the PKI certificate lifecycle is the usage of the certificate, which is to find in the certificate details. Also, it could be email encryption and signing. It could be file system encryption, perhaps the certificate usage is specified as for code signing for software developers or script writers. The next phase of the lifecycle potentially would be revocation. I say potentially, because if a certificate is never considered to be compromised, there would be no reason to revoke it, or if a user is not leaving the organization, again, there might not be a reason to revoke certificate. So employees leaving the organization or a compromised device would be a good reason to add the certificate to the CRL, the Certificate Revocation List, that would be the certificate serial number. And that can be revoked and then acquired by other apps or connecting devices by downloading the CRL or by requesting the validity
>
> **[6:20](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-lifecycle-management?u=76281980&t=380)** of that certificate using OCSP. So revoke certificates to make it simple or simply not trusted. Before the certificate expires, it can be renewed. So what happens is that we have a timeframe which will vary depending on the issuer. So for example, if we have a two year certificate before it expires, prior to that expiry occurring, we can have a manual or an automated process to renew the certificate, perhaps for another two years so it can continue to be used. So public certificate authorities will notify subscribers via email normally that their certificate is going to expire, so they have a chance to renew it, because once it expires, it has to be completely done again from the beginning, from the certificate signing request. Here we have a screenshot in [[Windows]] of an individual certificate. Notice that we have the validity period. So we have an since then of when the certificate will expire. We also have in our screenshot the issuer, in this case it's Root Cert, and who it was issued to, in this case, it just says client cert. So after a certificate expires, it can't be used. Normally there's a 90-day notification prior to expiring, but that will vary. For websites with expired PKI certificates, user web browsers will provide a message to the user that that certificate or the site is not to be trusted because it's not valid.
>
> **[7:54](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/certificate-lifecycle-management?u=76281980&t=474)** In this case, it has expired.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft]] (2), [[Cryptography]] (1), [[Active Directory]] (1), [[Android]] (1), [[Windows]] (1)
> **Env Vars:** csr (6), pki (3), pkcs (2), fqdn (2), mdm (2)
> **Analogies:** for example (5), such as (5), for instance (1)
> **CLI Commands:** make (2), find (1)
> **Definitions:** is a  (2), means that (1)
> **Tools:** command line (2)
> **URLs:** [fakesite.com](https://fakesite.com) (1)
> **UI Navigation:** select the (1)

#### [Transport layer security](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/transport-layer-security?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/transport-layer-security?u=76281980&t=0)** - [Instructor] Transport Layer security or TLS. This is a [[Network Security]] protocol suite, and we usually use it when securing connections through VPNs, perhaps through connections that we want to enable for a web server securely using HTTPS. But there are many other uses of TLS beyond just those examples. First, let's start by talking about the old secure sockets layer or SSL network security protocol suite. This was developed by Netscape way back in the early 1990s, and there were numerous versions. There were SSL versions, one, two, and three variants. However, none of these should be used. SSL should never be used. It should be disabled on clients and servers because there are many known security vulnerabilities. So it has been superseded by TLS, Transport Layer security. So we know that TLS was introduced to supersede SSL. That happened way back in 1999. And of course, over time we have various versions of TLS such as versions 1.0 through to 1.3, which is the current version as of this recording. It was introduced back in August, 2018, but there is a version 1.4 on the horizon. So as security professionals, we need to always keep up to date with any new enhancements or new versions of security related items that we should be using. So TLS uses PKI certificates and related keys,
>
> **[1:37](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/transport-layer-security?u=76281980&t=97)** public and private key pairs to secure network communications. How does it do this? It does it through encryption, which is confidentiality, so encrypting and decrypting communications over the network, as well as digitally signing transmissions over the network. Digitally signing uses hashing. So this provides authentication of messages over the network so that we can be assured that if it looks like a message came from a specific web server hosting a web app, it really did, it's not been forged. TLS also provides integrity, which means that we can [[Representational State Transfer (REST)|rest]] assured that the transmission was not tampered with since it's been sent. It also means we have non-repudiation where the digital signer of a network message cannot refute having sent it because the digital signatures created with the private key and only the owner of the key has access to it. Let's step through the TLS handshake. Let's assume for our example that we have a client web browser connecting to a web server that's been configured with the PKI certificate so it's using HTTPS. In step one, the client sends a hello message to the server. In step two, the server responds and includes its PKI certificate, which also includes its public key, not its private key. Step three, the client verifies server certificate validity. That would include things like checking that the certificate hasn't expired, that the subject name or any of the alternative names
>
> **[3:12](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/transport-layer-security?u=76281980&t=192)** in the certificate match what's being connected to over the network. In step four, the client will then generate a random secret, and that will be encrypted with the server public key because that was provided back in step two. And after that happens, the client will send that encrypted random secret to the server. In step five, the server can use its related private key to decrypt that client random secret. So now the server knows what the client random secret is. Using that random secret, the client and the server can go through a series of calculations to come up with a unique session key. Now, this session key is important because it is used to encrypt transactions between the client and the server from that point forward. Of course, there are also digital signatures that are occurring as well. So it secures the communication channel then by going through these steps initially to come up with a unique session key. Session keys might also time out after a period of inactivity. If you've ever done online [[Banking]] and you've been signed in and perhaps stepped away for five or 10 minutes, you might notice when you come back you have to re-authenticate to the banking web app. That, of course, would generate a new unique session key to be used for encryption and decryption. But TLS can also be used with virtual private networks or VPNs. A VPN in this diagram is allowing the remote client computer
>
> **[4:47](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/transport-layer-security?u=76281980&t=287)** that could be a user working from home or working at a customer site. It would allow them to connect over the internet using an encrypted secured VPN tunnel to a remote private network elsewhere. So that might be an [[Microsoft Office|office]] network where there are office computers and resources that the remote client computer needs access to. Well, what's interesting is we could have this set up as a TLS VPN formerly called an SSL VPN. These types of VPNs are firewall friendly. They don't use strange port numbers that you would have to open up in firewalls. They use TCP Port 443. Now, you might recognize that of course as being used by web service configured for HTTPS. So TLS can be used in this manner, which implies the use of PKI certificates. But the thing we should also think about is on client devices like web browsers, we can determine which versions of SSL or TLS should be used. We know that SSL should never be used, and in this screenshot it's not turned on. SSL version 3.0. But also we know that TLS versions change over time. Technically, we should not be using TLS versions 1, 1.1 or 1.2. The current version that we should be using that is currently trusted is 1.3, but that's on the client side. Depending on the type of server environment you're using, you can also check to see which SSL or TLS versions are supported.
>
> **[6:21](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/transport-layer-security?u=76281980&t=381)** Here on [[Linux]], I've issued the open SSL ciphers dash V command. Dash V is for verbose output. Notice here that we've got some TLS versions shown as being version 1.2. Now this is going to be important if you're configuring, for example, an Apache web server on Linux, you can determine which of these security modules are enabled. So of course, we don't want to use SSL, and ideally we will only be using TLS version 1.3.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Network Security]] (2), [[Banking]] (2), [[Microsoft Office|Office]] (2), [[Linux]] (2), [[Representational State Transfer (REST)|Rest]] (1)
> **Env Vars:** tls (17), ssl (11), pki (4), vpn (4), https (3)
> **Versions:** version 1 (3), 1.3 (2), 1.0 (1), version 3 (1), 1.1 (1)
> **Definitions:** is a  (2), means that (1)
> **Analogies:** such as (1), for example (1)
> **CLI Commands:** apache (1)
> **Ports:** port 443 (1)
> **Prerequisites:** set up (1)

#### [Analyze a tls packet capture](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/analyze-a-tls-packet-capture?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/analyze-a-tls-packet-capture?u=76281980&t=0)** - [Instructor] In this video, we're going to take a look at what happens when a client communicates with a server using TLS, Transport Layer Security. So we're going to analyze a TLS packet capture. To do that, we're going to use the free [[Wireshark]] tool. Wireshark is a free network analyzer tool. You can capture network traffic, and in the case of saved packet captures from before, you can open them up and analyze transmissions. You can get Wireshark for free at [wireshark.org](https://wireshark.org), and it runs on a variety of different operating system platforms. So I've opened up Wireshark on my local computer, and I've also opened up using File/Open a packet capture file that I took some time ago. Now, you can capture network traffic for the network you're connected to, whether it's a wireless network, whether it's a wired network. But in the case of a wired network, let's say, with ethernet switches, the nature of an ethernet switch is such that, you know, when you're plugged into a switch port, you only see traffic that you're sending or receiving. You'll see network broadcasts and multicasts, but you're not going to see individual communications between other devices, not by default, unless you configure the switch port otherwise. So, you have to think about that type of network connection, which determines the scope of visibility for what your packet capture will see. At any rate, here I've opened a packet capture,
>
> **[1:34](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/analyze-a-tls-packet-capture?u=76281980&t=94)** and notice the presence of some TLS traffic. Now, some of this here is showing us TLSv1.2. Ideally, we know we really should be using TLS version 1.3. We've also got some of that traffic. We can make life easy here by filtering what we're viewing up at the top in the bar. For example, if I type in just simply TLS and press Enter, all we're reviewing is TLS traffic. Okay, well, that makes things a little bit easier, and I think I'll go to TLS version 1.3. We shouldn't be using older protocols that are known to have security issues. What I'm interested in is the exchange, let's say, between a client, that might be a web browser, and a web server or some kind of a web app or web service. So I'm going to select here a specific packet, which is labeled as being a Client Hello packet. Now, this is for TLS version 1.3, and in this case, it happens to be for the www.[hbo.com](https://hbo.com) site. Could be any site, could be online [[Banking]], could be a private CA-issued certificate for an internal web app. It doesn't make a difference, the TLS handshake doesn't change. So what we have here is the source IP address of the client, that looks like a private IP address, 192.168.2.11, and then a public IP address out here for the website. So in the Client Hello packet, having that selected at the top, the headers of that packet are shown below, such as the IP header, the Internet Protocol Version 4 header,
>
> **[3:07](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/analyze-a-tls-packet-capture?u=76281980&t=187)** which, among other things, contains things like the source and destination IP. It's a TCP transmission, so transmission control protocol, where the destination port number is 443. Of course, an HTTPS connection, which uses the TLS [[Network Security]] protocol suite in conjunction with the certificate installed on the server to encrypt the connection. Next, we then have the transport layer security, or of course, the TLS header. So what the client is going to do here in this initial Client Hello handshake is it's going to send a list of cipher suites that it supports. So TLS using the advanced encryption standard algorithm, AES-128, we've mentioned that already, or AES-256, and all these other variations of what it supports. Now, at the end of the day, most servers are configured to only support certain specific encryption [[Algorithms]] or cipher suites. So, the server might not be configured to use SSL or TLS version 1.2. We'll see that information when we look at the Server Hello transmission. If I click on that packet, down in the middle here, what it's going to do is specify the cipher that will be used. So of course, that means it'll tell the client, well, I will use a specific encryption type of algorithm, for example, it might use TLS AES-128 with SHA-256 for signing.
>
> **[4:42](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/analyze-a-tls-packet-capture?u=76281980&t=282)** That's the signing or hashing algorithm. So we've got some of that being done here. At the end of the day, as we go further down, a session key, a unique session key will be used to actually encrypt all of the traffic between those two devices for the duration of that session. Here at Wireshark, if I go to View, and let's say, I go to Packet Details, turn that off, what we are viewing here is the hexadecimal on the left and the ASCII variation on the right of the payload of that packet, the actual data, whether it's encrypted or not. Now, once it's encrypted with the session key through TLS, it just looks like jumbled up data. Can't really make heads or tails out of what's going on. And that's precisely the point. So this gives us a general idea then of the communication that is used for the TLS handshake between a client and a server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Wireshark]] (6), [[Banking]] (1), [[Network Security]] (1), [[Algorithms]] (1)
> **Env Vars:** tls (16), aes (3), tcp (1), https (1), ssl (1)
> **Versions:** version 1 (4), 192.168.2 (1), version 4 (1)
> **UI Navigation:** go to (3), click on (1)
> **CLI Commands:** make (3)
> **Analogies:** for example (2), such as (1)
> **URLs:** [wireshark.org](https://wireshark.org) (1), [hbo.com](https://hbo.com) (1)
> **Definitions:** is a  (1)


### 2. Certificate Authorities

[↑ Back to Table of Contents](#table-of-contents)

#### [Configure a Microsoft AD CS certificate authority](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-microsoft-ad-cs-certificate-authority?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-microsoft-ad-cs-certificate-authority?u=76281980&t=0)** - [Instructor] Let's take a few minutes to see how we go about installing the [[Microsoft]] [[Windows Server]] Certification Authority Server role. What we want to do here is we want to be able to establish our own private CA within our organization where we control the entire PKI hierarchy. There are times you may want to do that if you want to be able to completely control the issuance of certificates for services that are used internally, like line of business apps and setting up certificates for VPNs. Just remember, as always, that when you work with a private CA and have certificates issued from it, you need to make sure that devices that will partake in the PKI environment have the trusted route certificate for the CA installed on them so that they trust certificates issued by that CA. We're going to be taking a look at how to do that specifically a little bit later. For now, let's get a [[Windows]] private CA up and running. So in my Windows Server environment, I'm going to go into the Start menu and fire up Server Manager. I've decided I want to use the GUI to set this up. You don't have to do that. You can do this from the command line, such as using [[Powershell]] commandlets, but before we actually do that, in the Start menu on this server, if I go to Windows Administrative Tools, I can elect to start the [[Active Directory]] Users and Computers tool. This server is an Active Directory Domain controller, so the Active Directory Domain has already been established. Let's just examine that for a moment.
>
> **[1:36](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-microsoft-ad-cs-certificate-authority?u=76281980&t=96)** In the left hand navigator here in Active Directory Users and Computers, my domain name is shown as being domain1.local. Now, you don't need an Active Directory Domain in order to install a Windows Server certification authority, but it does give you additional options, as we'll see shortly. So we're just pointing out that we do have an Active Directory Domain. This server is joined to it. Actually, it's a domain controller. So back here in Server Manager, I'm going to click add Roles and Features. In the wizard here, I'm going to keep clicking the next button until I get to the select server roles screen. Now I want to make sure I turn on Active Directory Certificate Services, which is often shortened down to ADCS. So I'll turn on that check mark. I'll click next. I'm not interested in anything else, so I'll keep clicking next until I get to the select role services screen. So there are some subset options for a certification authority that we can work with. So I definitely want the certification authority for sure, but then we can determine if we want the certificate enrollment web service, basically a small website that people could connect you to request certificates. Do we want the online responder for checking the validity of certificates? I don't want any of these additional services. I just want certification authority, so I'll leave that checked on and I'll click next, and I'll click install.
>
> **[3:08](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-microsoft-ad-cs-certificate-authority?u=76281980&t=188)** So all we're doing is installing this. Of course, we're going to have to configure it once the installation has completed. Okay, the installation has completed, but the configuration needs to be done. I'm going to click close, and I'll click the little flag icon with a yield symbol in the upper right here in Server Manager, and I'm going to choose Configure Active Directory Certificate Services. So it's got my domain information and my account, my admin account filled in. I'll choose next. We want to configure the certification authority. That's the only thing we've installed, so I'll turn on the check mark for that and I'll click next. Now, this is where, because we have an Active Directory Domain, we have the option of configuring what's called an Enterprise CA, which is integrated with Microsoft Active Directory. You don't have to do that. So if you've got a standalone server with no Active Directory at all, you could set up a standalone CA. You could still do that here now, but I want this integrated with my Active Directory Domain. The reason I want that is because for machines joined to the Active Directory Domain, it really makes it easy for them to request certificates through the Active Directory Implied Trust, because that machine is already a part of the domain. Again, this might work well for an organization that uses an Active Directory Domain, but it's not the only option. If you set up a standalone CA, how would you have different users requesting certificates? They could do it through the website, the enrollment website option that we mentioned previously,
>
> **[4:44](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-microsoft-ad-cs-certificate-authority?u=76281980&t=284)** but we're not doing that, so Enterprise CA it is. I'll click next. It asks, would you like to set up a root CA, the very top of the PKI hierarchy? Because it's the first time we're doing this, that is what we are going with. You do have the option of setting up a subordinate CA, but you have to have already set up a root CA. And remember, subordinate CAs are also called registration authorities, RAs, and you might do that in a larger organization because you might have a different subordinate CA for each admin team, so they can control their own certificates, or maybe for different business units, different projects, different regions, anything along those lines. So I'm going to set up a route CA, so I'm going to click next. I'm going to create a new private key. Well, isn't that what you would always want to do? Remember, you need a public and a private key pair for this to work. You might use an existing private key if for some reason you are reinstalling a server maybe that failed and you've backed up the private key for the CA that it used to host. So create a new private key. I'll click next. We can then specify cryptographic options. It's going to be an RSA 2048-bit public and private key. Also, we can specify the hashing algorithm for signing certificates issued by this CA, because we know that's how it works. SHA256 is fine. I'll click next. Then we've got the common name for the CA.
>
> **[6:17](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-microsoft-ad-cs-certificate-authority?u=76281980&t=377)** I'm going to call it MyOrgCAWindows, and I'll click next.
>
> **[6:28](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-microsoft-ad-cs-certificate-authority?u=76281980&t=388)** The validity for the certificate, that would be for the CA, is set here by default to five years. You can change that to be whatever you want. I'm going to leave it at five years, and I'll click next. I'll accept the default for the database storage location. On the summary screen, I am happy with my selections, so I'm going to go ahead and click configure to get this up and running. The configuration has succeeded just like that. So if I click close, I'm left here in Server Manager, I can either go to the tools menu here in Server Manager to open up the Certification Authority tool, but I can also, from the Start menu on this host, open up the Certification Authority tool there. It doesn't matter how you do it, but when you do go into that, this is where it will show you your private CA. So it's shown over here on the left, MyOrgCAWindows, and if I expand it, I've got folders for revoked certificates, issued certificates, pending requests. You might configure a certificate template perhaps that requires administrator approval before issuing a certificate to a user, so it would show up as a pending request here until it's approved. You've got failed requests, and because we have an enterprise CA, remember that's only available because this machine is connected to an Active Directory Domain. Well, because of that, we have this additional option of certificate templates. If you have a standalone private CA set up in Windows,
>
> **[8:02](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-microsoft-ad-cs-certificate-authority?u=76281980&t=482)** you don't get the certificate templates option. These, of course, are blueprints where we can configure options for the certificates based on these templates, but we'll get into that stuff later. At this point, we have successfully established a root CA on Windows.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Active Directory]] (16), [[Windows]] (4), [[Windows Server]] (3), [[Microsoft]] (2), [[Powershell]] (1)
> **Prerequisites:** configure (6), set up (6), install (2)
> **Env Vars:** pki (3), gui (1), adcs (1), rsa (1), sha256 (1)
> **CLI Commands:** make (2)
> **Tools:** command line (1), powershell (1)
> **UI Navigation:** go to (2)
> **Analogies:** such as (1), just like (1)
> **Cross-References:** we mentioned (1)

#### [Configure Microsoft AD CS certificate templates](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-microsoft-ad-cs-certificate-templates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-microsoft-ad-cs-certificate-templates?u=76281980&t=0)** - [Instructor] In this demo, we're going to be customizing certificate templates. Now, we know that a certificate template as part of the PKI hierarchy is used to present the instructions for issuing certificates. It's a blueprint. So let's go ahead and take a look at how we can do that specifically with [[Microsoft]] [[Active Directory]] Certificate Services. So here on my [[Windows Server]], I'm going to start by opening up the Start menu, and I'm going to go down under [[Windows]] Administrative Tools. I have already established a private CA within my organization here on my Windows machine. And so from the Start menu, I'm going to start the Certification Authority tool. When I configured my private CA, which is shown here on the left as being called MyOrgCAWindows, I configured this as an enterprise CA. That means it's integrated with Microsoft Active Directory because this server is a domain controller. You don't have to do that, but by virtue of having done that, I get the option of working with certificate templates, which I'll click over on the left. On the right, I have some standard certificate templates that are made available automatically when I install this product. So for a domain controller server, for a web server, for a computer, perhaps client authentication to a VPN or to an IPSec configured environment. We've also got user certificates for encrypting file system or for secure email and all that stuff.
>
> **[1:35](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-microsoft-ad-cs-certificate-templates?u=76281980&t=95)** So we've got a bunch of built-in certificates, but there are many more that are shown here, and you can customize them. That's where we're going with this. So to do that, I'm going to right-click on Certificate Templates over on the left, and I'm going to choose Manage. When I do that, it's going to open up a new window. There's a new tool. Now we're in the Certificate Templates console where we have an alphabetical listing of all of the available templates. Notice there are many more shown here than were available to be used to issue certificates. That's normal. That's how this works out of the box. Now, what I can do is I can customize certificates if I really want to, specifically certificate templates, I should say. So let's say I want to have a customized web server template. I can double-click on the Web Server certificate template here and browse through the options, and that's great, but I want to do my own thing. I want to have my own settings in a web server certificate that's used when I issue web server certificates through this private CA. So what I'll do is right-click on that built-in Web Server certificate template, and I will choose Duplicate Template. It automatically opens up the properties of the new template. And so if I were to go take a look, for instance, at General, I can give it a name. I'm going to call this Custom Web Server Template. Now, there are many options I can specify here that will be used when issuing a web server certificate from this template, like the validity period.
>
> **[3:09](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-microsoft-ad-cs-certificate-templates?u=76281980&t=189)** Here, it's set to two years. So instead of a two-year default validity period for web server certificates, perhaps in our organization, we only want them valid for one year, so I can change that validity period here. I've also got a number of other options. Subject name is one of them. What I can do here is determine what the subject name within the certificate will be. For a web server that needs to be the server name. So if people will connect to www.[site.com](https://site.com), that name has to be in the certificate. So we can have that supplied in the request when the certificate is requested, or we can build it from Active Directory information, where we can choose, for example, the DNS name. But in this case, I'm going to have it supplied in the request. So anytime a technician requests a web service certificate, they're going to have to supply the subject name. We can also specify other options, like issuance requirements, such as requiring certificate manager approval, so it will show up as a pending request until an admin goes in and approves it. So we could do that type of thing as well. If I go under [[Cryptography]], we can specify the maximum key size. If we want to change those options, if I go to Security over here, this is related to the Microsoft Active Directory domain environment, which users or computers are allowed to request certificates.
>
> **[4:43](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-microsoft-ad-cs-certificate-templates?u=76281980&t=283)** So for example, Authenticated Users is selected here and down below, anyone that's an Authenticated User in Windows is allowed to read this template. However, we can also determine who should be allowed to enroll to request a certificate. So I'm going to select, for example, my admin account, instructorazure07, and I'm going to turn on Enroll down below as well. So if you want to be able to get a certificate issued from this template, you have to have at least the Enroll allow permission. Auto enrollment is a separate configuration, so we're not going into that, but we're going to make sure that allow Enroll is set, and that's for my admin account, which I will use when requesting a server certificate. So beyond many other options, this is the type of thing that we would configure within that custom certificate template. So it shows up here, Custom Web Server Template. If I close the Certificate Templates console, we're back in our standard Certification Authority tool. However, even if I try to refresh my list of available certificates, I don't see the custom web server, and that's because what you now need to do on the left is right-click on Certificate Templates. You must choose New, Certificate, Template to Issue, then select your Custom Web Server Template. Now it will show up as being available here. There it is, Custom Web Server Template.
>
> **[6:18](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-microsoft-ad-cs-certificate-templates?u=76281980&t=378)** So at this point, we've got a custom certificate template. In another demo, we'll take a look at how to request a certificate based on that template.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Active Directory]] (4), [[Microsoft]] (3), [[Windows]] (3), [[Windows Server]] (1), [[Cryptography]] (1)
> **Exercise Files:** template (17)
> **UI Navigation:** right-click (3), double-click (1), go to (1)
> **Analogies:** for example (3), for instance (1), such as (1)
> **Env Vars:** pki (1), vpn (1), dns (1)
> **Definitions:** is a  (2)
> **Prerequisites:** install (1), configure (1)
> **CLI Commands:** make (1)

#### [Configure a Linux OpenSSL certificate authority](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-openssl-certificate-authority?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-openssl-certificate-authority?u=76281980&t=0)** - [Instructor] We've talked about how a certificate authority, especially a root certificate authority, or CA, constitutes the top of the PKI hierarchy. Now, what if we want to establish our own PKI hierarchy on the [[Linux]] platform? What are the options? Well, one option is to use OpenSSL. So we're going to be using OpenSSL to establish a Linux certificate authority where we create our own PKI certificates. But we'll get to the creation of certificates later. In this video, the focus is going to be to get the private CA configured. The first thing I'm going to do here in Linux is verify that I have OpenSSL installed. With some Linux distributions, it's automatically there. So I'm going to run sudo as a prefix because I'm going to be running privileged commands. And one way to check this out is simply to try to install it, sudo apt install openssl, all one [[Microsoft Word|word]] together. It's telling me that it's already installed and that it's already the newest version. So having done that, what I can do is I can begin by generating a private key. We know that when you're working with PKI certificates, you need a public and a private key pair. So let me start with the private key. I'll do that at the command line here by running sudo openssl genrsa, to generate an RSA key. Now I'm going to encrypt this with -aes256 - out. I want to call this file CAPrivateKey.key.
>
> **[1:42](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-openssl-certificate-authority?u=76281980&t=102)** You can call the file anything you want, it doesn't have to end with .key. And that file will be in the current directory, that's where it will be created. And the last parameter here is simply 2048, 2048 bits in the key. Okay, I'm going to press Enter. Now, because I'm choosing to encrypt it with a AES-256, of course, I'm being asked for a passphrase because it is a private key file, and we know that those need to be protected unlike public keys, so I'll enter a passphrase and it asks me to verify it. So I'll enter the same thing in here a second time. And if I clear the screen and type ls, we've now got a file here called CAPrivateKey.key. The other file I have here, otherinfo.ext, that file was already there, it's used for something different unrelated to what we're doing right now. If I run the sudo cat command, the cat command allows me to view the contents of text files. And if I specify my CAPrivateKey.key file, notice that what it's giving me back is the encrypted private key. Okay. So that's the first order of business. We need to have a private key as well as a public key if we want to work with a CA, if we want to establish a CA. So the next thing we're going to do is actually configure or establish the private CA. The command I'll use for that is sudo openssl req for request - new -x509,
>
> **[3:20](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-openssl-certificate-authority?u=76281980&t=200)** remember that PKI certificates are also called X.509 certificates, -key. Now I have to supply the private key, and I'm going to reference my CAPrivateKey.key file. I want to use -sha256, secure hashing algorithm 256 for signing. I want this to be good for 365 days, so I'll specify that with -days, that's when the certificate will expire, and -out. I want the file to be called MyPrivateCA.pem. So I'm going to go ahead and press Enter. Because we're referring to the PrivateKey file, we're going to have to enter in the passphrase that we specified when we established it. So I'm just going to go ahead and enter that because private keys need to be protected. When you establish a root certificate authority, you will be prompted for things like I'm being prompted for here, like the country name, the two letter code. So I'll put in CA for Canada. The state or province, let's say Ontario. The locality or city, I'll put in Toronto. The organization name, maybe DemoTest. The organizational unit, maybe IT. The common name. Okay, well, the common name of what? In this case it's going to be the common name of the certificate authority. So I'll call it MyPrivateCA. Email address. Maybe I'll just put in user@[demotest.com](https://demotest.com). And we've now established our private CA.
>
> **[4:54](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-openssl-certificate-authority?u=76281980&t=294)** We've got a file here called MyPrivateCA.pem. So this represents the certificate, including the public key for our CA. Why don't we run sudo cat against that file? So now what we're having is the certificate being shown when we run the cat command against that file, this is what it's supposed to look like. Okay. So now that we've got that done, the next thing that we might be interested in doing is figuring out how to install this trusted root certificate on a Linux host. We've already seen how we can install a trusted root certificate in [[Windows]]. But let's do it here in Linux, because if you're going to establish a private CA and then issue certificates from it, we have to bear in mind that by default, devices will not trust those signed certificates. They only will if they trust the signer, the CA. And so here's what we're going to do to get this going. First of all, I'm going to copy, I'm going to run sudo cp copy the PrivateCA file, MyPrivateCA.pem, my certificate, to /usr/local/share/ca-certificates.
>
> **[6:14](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-openssl-certificate-authority?u=76281980&t=374)** So I want to copy it into this directory. This is the local Linux certificate store. Now, there are a lot of different variations or distributions of Linux. So specifically, depending on what version of Linux or distribution that you're using and which packages are installed will determine exactly where this file should go. But this is the standard. So let's go ahead and press Enter. Of course, if you're using a GUI on top of Linux, there are ways to do it in there as well. Next thing I need to do is run sudo update-ca-certificates. That's all one command, each word separated with a dash. All right. So now it's updated our list of certificates. We can even do the following, sudo ls/ where did we put that? Usr/local/share/ca-certificates. And there is MyPrivateCA.pem. Now, if you don't have access to commands such as the one we used to update the CA certificates, depending on your distribution of Linux, you might have to install a package such as sudo apt install. It's called ca-certificates. Now we have it installed, naturally, otherwise that command would not have worked. But if you need to do that, you can. Okay, so at this point we have established a private root CA on our Linux host, and we've even installed the trusted root certificate on the host as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (12), [[Microsoft Word|Word]] (2), [[Windows]] (1)
> **CLI Commands:** sudo (10), cat (4), apt (2), ls (2), cp (1)
> **Env Vars:** pki (5), rsa (1), aes (1), gui (1)
> **Prerequisites:** install (6), configure (1)
> **Analogies:** such as (2)
> **URLs:** [demotest.com](https://demotest.com) (1)
> **Tools:** command line (1)
> **Definitions:** is a  (1)

#### [Configure an AWS Certificate Manager CA](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-an-aws-certificate-manager-ca?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-an-aws-certificate-manager-ca?u=76281980&t=0)** - [Instructor] In this demonstration, I'm going to be installing a private CA, so a private certificate authority, but I'm not going to do that on-premises. I'm going to be doing it in the [[Amazon Web Services (AWS)|Amazon Web services]], or AWS Cloud. Remember, you might want to install your own private CA if you would like full entire control and configuration flexibility for the entire PKI hierarchy. But remember, the downside is that when you start issuing certificates, say for websites from your private CA, there are no devices that will trust that certificate, at least not by default. But we'll get into that a little bit later. Let's get started here and let's establish a private CA. So here in the AWS Management console, I'm going to search for the [[Microsoft Word|word]] certificate up at the top. The service I'm interested in here is called Certificate Manager. When I select that, I can then choose to go to the AWS private CA view, where if there are existing CAs, they will be shown. But what I want to do here is create a private CA. So I'm going to choose create a private CA. From the mode options dropdown list, it can be a standard or general purpose CA, but if you just want to do it for short-lived certificates with the validity of up to seven days, you can go ahead and do that. I don't want that, so I'll leave it on general purpose. We can specify that we want to establish the root CA at the very top of the hierarchy. But if you've already got a root CA created, under it you could choose to create
>
> **[1:33](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-an-aws-certificate-manager-ca?u=76281980&t=93)** a subordinate private CA, maybe for a specific business unit, for a project, for a region, whatever the case might be. But here I want to route CA. And so down below, I'll have to specify an organization name, an organizational unit, which could map to a department, for instance, a country name, a state or a province name, a locality name, and a common name. So I'm going to go ahead and fill out those details now. Once I've filled out those items down below, we've got the key algorithm for generating the public and private key pair for this CA set to RSA 2048. That's fine. Down below I can determine if I want to use a certificate revocation list, or a CRL, for certificate revocation options, or I could use OCSP. I'm not going to enable either of those. And down below at the very bottom, I just have to acknowledge that I'm going to be charged for this. And so if you're testing this in the AWS Cloud, test it quickly. But then when you're finished with it, remove the configuration so that you're not incurring charges unnecessarily when you're not actually using something. Okay, I'm going to click to create CA button. Now it's pretty quick. It immediately states your CA was requested successfully. Okay, so the status then shows as pending certificate. What is it waiting for? It's waiting for me to go under the Actions button in the upper right,
>
> **[3:07](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-an-aws-certificate-manager-ca?u=76281980&t=187)** and it's waiting for me to choose install CA certificate. So I can see it's valid for a period of time. And also I can, down below, specify the signing algorithm. It's set to SHA256 RSA. Bear in mind that when you issue certificates, the CA adds its digital signature to that certificate, which helps establish the chain of trust. I'm okay with that option, so I'm going to choose confirm and install. And now, instead of pending, the status of this CA is that it's active. So when I go to my private certificate authorities view, if I click on that on the left, then we've got our new active CA shown here. If I click on it to open up that item, then we have the details. So the CA type is shown as a root CA. We have the creation and the expiration for the CA. We have the key as well as the signing [[Algorithms]] shown here. Down below, the subject details. So for the locality, in this case, the city name of Toronto. If I click on the CA certificate tab, I also have the certificate for this route CA, and I can even choose to export it to a file. Because remember, if you use the CA, let's say, to issue a certificate to secure a web application, well, anyone connecting to that web app will get a message that it can't be trusted because their device will not trust the issuer of the certificate unless you install the trusted route certificate on those devices.
>
> **[4:43](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-an-aws-certificate-manager-ca?u=76281980&t=283)** So in any case, we have now established our own private CA in the Amazon Web Services Cloud.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon Web Services (AWS)|Amazon web services]] (2), [[Microsoft Word|Word]] (1), [[Algorithms]] (1)
> **Env Vars:** aws (4), rsa (2), pki (1), crl (1), ocsp (1)
> **UI Navigation:** click on (3), go to (2), dropdown (1)
> **CLI Commands:** aws (4)
> **Prerequisites:** install (4)
> **Definitions:** is called (1)
> **Analogies:** for instance (1)
> **Speakers:** - [instructor] (1)


### 3. Acquiring Certificates

[↑ Back to Table of Contents](#table-of-contents)

#### [Acquire a certificate using Microsoft AD CS](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-certificate-using-microsoft-ad-cs?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-certificate-using-microsoft-ad-cs?u=76281980&t=0)** - [Instructor] In this demonstration, we're going to be focusing on the acquisition of a certificate using the [[Windows]] Cert Manager tool. Acquiring certificates can be done in many different ways, depending on whether you're going to a private CA or a public CA. However, here in Windows, let's take a look at our environment. On my [[Windows Server]], if I go to the Start menu, under Windows Administrative Tools, I can select [[Active Directory]] Users and Computers. The server I'm using is a domain controller. Now, it doesn't have to be, but if it's a computer joined to a [[Microsoft]] Active Directory domain, it makes it very easy to request certificates through that connection to the domain. So I'm going to close down Active Directory Users and Computers. We're just establishing that this machine is part of a domain. What I would like to do is open up a command prompt on this host. Now, within the command prompt, what I'm going to be doing is starting the MMC, or Microsoft Management Console, tool. In here, I can go to File, Add/Remove Snap-ins. And if I want to manage certificates, then I can choose Certificates. So I'll select that and I'll click Add. I'm interested in certificates related to the computer I'm sitting at doing this. So not the user account I'm signed in with. So, Computer Account, Next. It's going to be for the local computer, not a remote computer over the network, but this one I am at.
>
> **[1:34](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-certificate-using-microsoft-ad-cs?u=76281980&t=94)** So Local Computer, Finish, OK. If I were to expand the left-hand navigator, under Certificates, under Personal, Certificates, I would have a reference to any existing certificates related to the local computer. Now, in this particular case, this is a server where I have established a private certification authority. It's called MyOrgCAWindows. So that's why I've got a certificate there shown. Now also, I've got a certificate issued to this server, that's automatic. So I've got a private CA integrated with Windows, so active directory. And so this server was issued a certificate. If I click to open that up, it was issued to this server's DNS name by my private CA. And the certificate has a validity period here of one year. So that happened automatically. But what I want to do in this case is I want to use this tool we're in, the Certificate Manager plugin tool, and I want to request a server certificate, a web server certificate. So I'm going to do that by right-clicking on Certificates in the left-hand navigator. I'll choose All Tasks. Request New Certificate. I'll then click Next. I'm going to use the Active Directory Enrollment Policy because the server I'm sitting at is joined to an active directory domain. So if I have a private CA
>
> **[3:10](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-certificate-using-microsoft-ad-cs?u=76281980&t=190)** that's integrated with Active Directory, and there are templates that I have access to, they will show up here. When I click Next, here are the templates that show up. Now in this case, I've got some built-in templates, Directory, Email Replication, Domain Controller, Domain Controller Authentication, and Kerberos Authentication. They're showing as being available. But if I go into my Start menu on this server, remember, this server happens to be where the private CA is hosted. It doesn't have to be, but that's what this configuration is. If I go to the Certification Authority tool on this host, there it is, MyOrgCAWindows, that's my private CA. If I expand that on the left, and click Certificate Templates, I have a custom web server template shown here. But when I go to request a certificate, it's not showing up here. Well, that's usually a security permissions issue. So what I'm going to do is go back into my Certification Authority tool, and we're going to fix the security settings for that template so that it does show up and is available. So I'm going to right-click on Certificate Templates and choose Manage. That opens up the Certificate Templates console, and I've got my custom web server template here. If I double click on it to open up its settings, I can then go into the security for this particular template. So I'll click the Security tab. For authenticated users,
>
> **[4:42](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-certificate-using-microsoft-ad-cs?u=76281980&t=282)** which would include user accounts and computers, like computers joined to the domain. And that's the issue we're having. Our computer, because we're looking for a computer certificate, doesn't have the ability to enroll using this certificate template. So for authenticated users, or I could add a computer group, or add that individual computer account here, but in this case, I'm going to select Authenticated Users and make sure that they can read this template and enroll, using it to get a certificate. I'll click OK. Let's go back into our cert manager tool. I'm just going to cancel out of that screen and let's step through the motions again. I'm going to right-click Certificates on the left, 'cause I want to request a web server certificate. I'll choose All Tasks. Request New Certificate. Next, Next. Notice now, the Custom Web Server Template shows up, but it does say, "More information is required to enroll for this certificate. Click here to configure settings." Okay, apparently what it needs here is the subject name. This template is set up so that I have to supply the subject name here. So we could specify, for example, the full distinguished name, the common name, all of these other items. So what I want to do here is specify the common name. And the common name here is going to be www.mysite.local.
>
> **[6:16](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-certificate-using-microsoft-ad-cs?u=76281980&t=376)** I'll also add the alternative name, which is going to be a DNS name, www.mysite.local. So the name that is embedded within the certificate here, the DNS name in this particular case, this needs to be what people are connecting to in order for the certificate to be trusted. If they're connecting to a different name, yet this name is embedded in the certificate, that is a problem. So I'm going to add the common name and I'm going to add the alternative name, and I'll click OK. So we've got the Custom Web Server Template selected with the check mark. Now I can choose Enroll. The status is showing as Succeeded. So let's check our work, what's the result here? The result is that when we view the personal certificates for the local computer here, where we requested the certificate, the certificate was issued, www.mysite.local, from MyOrgCAWindows, that's the private CA. If we double-click on that certificate, it has a validity period that will last for one year. That will vary depending on the template used. That comes from the template. Let's go check the template for a second. If we go into our Custom Web Server Template, notice that the validity period, indeed, here under the General tab in the template properties, is in fact set to a one year timeframe. Also, notice here that when it comes to things like issuance requirements,
>
> **[7:49](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-certificate-using-microsoft-ad-cs?u=76281980&t=469)** it does not require manager approval. That's why it happened so quickly. It was pretty much immediate. If we go to the subject name tab, in this template it requires the requester to supply the name in the request, which is what we had to do. Let's close out of the Certificate Templates console here. If we were, from the private CA standpoint, to check issued certificates, so this is the Issued Certificates folder for my private CA, notice that we've got the requester name shown here, which of course is the server, and then we've got the template that was used to issue a certificate, which includes our Custom Web Server Template.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Active Directory]] (7), [[Windows]] (4), [[Microsoft]] (2), [[Windows Server]] (1)
> **Exercise Files:** template (17)
> **UI Navigation:** go to (5), right-click (2), click on (1), double-click (1)
> **Env Vars:** dns (3), mmc (1)
> **Definitions:** is a  (3)
> **CLI Commands:** snap (1), make (1)
> **Tools:** command prompt (2)
> **Prerequisites:** configure (1), set up (1)

#### [Acquire a web server certificate using OpenSSL](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=0)** - [Instructor] Okay, in this demonstration what I will be doing is acquiring a web server certificate using OpenSSL on [[Linux]]. The first thing we should do is establish what we already have. What I mean by that is here in Linux I'm going to type ls. I am currently in the cert$ subdirectory. This is a subdirectory I have created on my machine, in which I've got some certificate files and private key files. I have a private CA called MyPrivateCA, and I've got the .pem file here. That's the private CA certificate. Its related private key is in a file here called CAPrivateKey.key. Now we know that in PKI, if you want to get a web server certificate because you want to use TLS to secure the connection to that server, you need a certificate. And so before you can have a certificate, it needs to be issued from a certificate authority. And that's what we've got here in MyPrivateCA. Okay, so I need to generate a private key and I want to encrypt it in a file for the server. We already have the CA private key. I want to do it for the web server certificate. So I'm going to start with the sudo command prefix as the commands I will be issuing are elevated types of commands. I need to have root permissions but I'm not signed in as root so therefore I'm using sudo; sudo openssl genrsa, to generate an RSA private key,
>
> **[1:35](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=95)** - aes256, that's what I want to encrypted with,
>
> **[1:38](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=98)** - out. I would like the file in the current directory to be called www.demotest.world.key. The file name can be whatever you deem as appropriate. And 2048 bits in the key, that's the last parameter. Enter! Of course, I'm being asked to enter the pass phrase because we need to do that to protect private key files, in this case for a web server certificate that we eventually will acquire. Okay, so I'll enter that in here. Excellent. If we do an ls, we now have our web server private key file. But we need more than that. Because what we need to do to acquire a certificate is we need to create a certificate signing request, a CSR. There are many tools that do this for you automatically in the background while generating the private key but here we're seeing each individual step one at a time at the command line. Which is not a bad thing when it comes to learning the details about how certificates work in the PKI hierarchy. So the way I will create these certificate signing requests is with sudo openssl req, for request,
>
> **[2:55](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=175)** - new -key, and I'm going to specify my key file, the private key file, it's called www.demotest.world.key. That's what we created in the previous command. But what I want to result in now with -out is a file that contains the certificate signing request. So I will call that file www.demotest.world.csr. It's a good file name. Now it's going to ask me for the pass phrase for the private key since I referenced the private key file. So I'm going to go ahead and enter that in correctly. Press Enter. It's asking me for the details. When you make a certificate signing request, whether you're doing it at the command line for a private CA like we are, or if you're trying to get a certificate from a publicly trusted CA out on the internet, one way or another, whether it's a web form or whether you generate the CSR the way we are and then provide that to the certificate authority, either way, you're going to have to specify details like the Country Name (2 two letter code). So I'll specify that. I'll specify some locality information. In this case, the province of Ontario in Canada, the city of Toronto, the organization name, I'll fill in. Also the organizational unit, for example, the IT department. Now for the common name in this particular case, I need the FQDN, the fully qualified domain name, the DNS name of what I want to issue the certificate to. Which is going to be www.demotest.world.
>
> **[4:34](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=274)** That's what people will be connecting to, for instance, if it's a public-facing website that users interact with. The whole reason I'm doing this is because I would like to enable an HTTPS binding on a web server. Well, you need a certificate to do that. And the name in the certificate has to match what it's being connected to. So I have to be careful about what I enter here. It has to be correct. All right, and then email address for a responsible user. I'll just fill in a fictitious one and I'll press Enter. Okay, then it says, "Please enter the following extra attributes to be sent with your certificate request." It wants a challenge password. Okay, I'll enter in a challenge password. Depending on which certificate authority you're doing, they would have to know that in order to use the CSR. We could put in an optional company name. I'm not going to do that. And if I clear the screen and type ls, what we now have is a certificate signing request, a CSR file. Now that we have the certificate signing request, what would happen is the certificate authority, that entity, they would normally do their due diligence to make sure that the information appears to be correct, that it's a legitimate company. Maybe require some confirmation through email, perhaps even through a phone call. If you are doing this on the internet and you are opting for an extended validation type of certificate, then the CA will have to do a lot of due diligence,
>
> **[6:09](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=369)** again to make sure that the company name exists and is registered, for example. But here it's all a private CA internally, so none of that has to happen. But what we need to do now, because our goal is to have a server certificate, so we can use TLS to secure the connection, we need to actually request the certificate from the CSR, the certificate signing request file. Okay, let's do it. Now just before we do that, I just want to reference a file I have here called otherinfo.ext. The file can be called anything, that's not important, but what's inside it is important. So I'm going to run sudo cat and we're going to take a look at that file. There's not very much in here. So we've got a couple of directives such as authorityKeyIdentifier, we've got basicConstraints, keyUsage, such as using keys for digital signatures, nonRepudiation, keyEncipherment and so on. But what we're really interested in in this file is the subjectAltName. You can have multiple names. Down below, I have an [alt_names] section. The heading of that section is enclosed in square brackets. That's the format this file must be in for us to actually generate the certificate using OpenSSL. So, I've got the first name here, www.demotest.world. You can have more than one name here but this name of course is really what needs to match what's being connected to.
>
> **[7:42](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=462)** Otherwise, if people are connecting to a website for instance and the name doesn't match, the name they're connecting to doesn't match what's here, then they're going to get a security warning message in their web browser. Okay, so that file exists because we're going to reference it here on the command line in just a moment. So our command will be sudo openssl x509
>
> **[8:07](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=487)** - req, for request,
>
> **[8:10](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=490)** - in. Now for the file I specify for the -in parameter, this is going to be the certificate signing request. So www.demotest.world.csr, in our case. Then I have to tell it the CA I want to use to digitally sign and issue the certificate. You might have multiple private CAs. So -CA MyPrivateCA.pem, that's the file,
>
> **[8:37](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=517)** - CAkey. You need to reference the CA private key file to digitally sign a certificate that is issued from that CA. So, because we're referring to a private key file here, we're going to be prompted for the pass phrase for it. Of that, we can be sure. Then -CAcreateserial. Because the CA can issue many certificates and each certificate gets a unique serial number, the CA needs to track or should track which serial numbers it has already used. So that's why I'm using -CAcreateserial -extfile. There's the otherinfo.ext file which contains the subject alternative names, the DNS names, in our certificate or that will be in our certificate,
>
> **[9:25](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=565)** - out. The name of the certificate file I want to create is www.demotest.world.pem, or if you want to call it CRT for certificate, it doesn't matter,
>
> **[9:37](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=577)** - days 365. I want this server certificate to expire in one year. So the certificate would have to be renewed before that happens. Otherwise it will no longer be able to be used and you'll have to go through the whole certificate signing request process again. I'll press Enter. All right, of course enter the pass phrase for the CAPrivateKey. So I'll go ahead and enter that in hopefully correctly. And it looks like it worked. Now we can verify this. If I do an ls in the current directory, we now have our server, web server certificate file. It's called www.demotest.world.pem. If it's not already done, I can install an Apache web server on this Linux host with sudo apt install apache2. I've already got it installed so I don't have to install it. Now what I want to do at this point is I want to edit the website config file to tell it to use my PKI certificate, the one we just generated. So I'll use sudo nano, nano is a built-in text editor. And I'm going to refer to /etc/apache2/sites-enabled/000-default.conf.
>
> **[11:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=660)** That's just the config file for the default website. I've already edited this so that the virtual host is set to use port 443, an HTTPS connection. Instead of port 80, which is only HTTP. We want HTTPS, we want to use TLS to secure that connection. The other things I've done here is added a few directives. Even though it says SSL, really, we're using TLS. So SSLEngine is on the SSLCertificateFile. I am specifying my web server certificate. Also, I'm specifying my web server certificate private key file and also the private CA certificate file. All right, so that's what I've specified here in this config file. So I'll press Ctrl X to get out. And I want to make sure that the web server's running so I can run sudo service apache2 status. And it's active and running, as shown here. So that means that we've got an active web server that is using our certificate. Let's test out the connection to it from a web browser. Actually, there is one thing we should check before we do that. And that would be to make sure that our Apache web server running on Linux is not using SSL, but instead is only using TLS. Ideally TLS version 1.3. For that, we can use the nano command to open up /etc/apache2/mods-available/ssl.conf. So what I want to do then is get down to the section,
>
> **[12:38](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=758)** let's see here, where we can determine what is supported on the server. Here we are, SSLProtocol all. So that means all SSL protocols are on. But after that, we have -SSLv3. In other words, we don't want to support SSLv3. You could change this. For instance, what you could do is say, okay, what I want to do is I want to disable all, so -all +TLSv1.3. I only want to support TLS version 1.3 for all the virtual hosts on the web server. Interesting! Ctrl X. To save the modified buffer? Yes, Enter. I've now written to that file with that change. Okay, so just to be safe, and you should do this whenever you make those types of changes, you could of course restart the web server. I can do that with sudo service apache2 restart. Now it wants me to enter the pass phrase for the private key, the web server private key. So I'll enter in that pass phrase. And after that, the web server will be up and running using the certificate. Now let's go to the browser and test the connection over HTTPS. Okay, so from another computer, it happens to be a [[Windows]] computer, I've used the [[Microsoft]] Edge web browser to navigate to [https://www.demotest.world](https://www.demotest.world).
>
> **[14:13](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-server-certificate-using-openssl?u=76281980&t=853)** Now it says it's not secure. Well, why would that be? It's because I don't have the trusted root certificate installed for the CA that issued that certificate. We already know how to do that in Windows, so we're not going to focus on that. I do want to click on "Not secure." And let's see, let's click on, "Your connection to this site isn't secure." I'll click the little certificate icon. Indeed, here we go. We've got our demotest.world certificate, which is being used by our web server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (4), [[Windows]] (2), [[Microsoft]] (1)
> **Env Vars:** tls (7), csr (5), https (4), pki (3), ssl (3)
> **CLI Commands:** sudo (10), make (6), ls (4), apache (2), cat (1)
> **Speakers:** - out (2), - [instructor] (1), - aes256 (1), - new (1), - req (1)
> **Code Identifiers:** authoritykeyidentifier (1), basicconstraints (1), keyusage (1), nonrepudiation (1), keyencipherment (1)
> **Analogies:** for instance (3), for example (2), such as (2)
> **Definitions:** is a  (4), means that (1), in other words (1)
> **Tools:** command line (3)

#### [Acquire a public CA certificate](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-public-ca-certificate?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-public-ca-certificate?u=76281980&t=0)** - [Instructor] In this video, we're going to examine what it would take to acquire a web server certificate from a public certification authority. Now, there are many public CAs out there, and the benefit of getting a web server certificate from them as opposed to a private internal CA, is that it will automatically be trusted by devices. So, for example, if you are setting up a public-facing web application or website, that means anyone in the world might connect to it, in order for them to trust the site or for their devices to trust your site, you're going to need to have a certificate issued from a public CA, because you can't really install the trusted root certificate for your private CA on everyone's computer around the world. That's just crazy and doesn't make any sense at all. So, there are plenty of times this will be important. So, in this example, I happen to be using GoDaddy, so, I'm looking at the SSL certificate options. Now, we also have other choices here, like managed SSL. Managed services mean that everything is taken care of by the company, in this case GoDaddy. So, the issuance of the certificate, the installation of it on your server, making sure it's renewed before it expires, all the stuff that we would normally do as IT technicians working with PKI and TLS. So, I don't want that. We could also opt for a wild card certificate. Now, instead of having a separate web server certificate to secure communications,
>
> **[1:33](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-public-ca-certificate?u=76281980&t=93)** let's say for www.demotest.world and also sales.demotest.world, that would be two certificates or more if you have more names and more servers. So, what we can do is instead acquire a wild card certificate that could be used for everything under demotest.world. However, that doesn't really apply here. So, I'm going to go back to my SSL certificate options and just scroll down. Another aspect of acquiring a certificate from a public CA is how much validation they do on you to make sure that you or your organization are legitimate and should be given a certificate. And that's why we have options here for domain validation versus organizational validation or extended validation and the prices change accordingly. In the description, we're being told that different levels of validation might be required, higher levels of validation for organization, so that user devices will trust the web application or the website. So, what I would then do is click Add to Cart and select the appropriate options for the number of websites to protect, whether I want a wild card certificate, the term, whether the certificate is valid for one year, three years, whether I want the organization, in this case, GoDaddy, to install the certificate. However, down below we get a detailed explanation of how this works.
>
> **[3:05](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-public-ca-certificate?u=76281980&t=185)** So, it says five steps to consider if you manually install your SSL certificate. Let's assume we would. So, you would go into step one, request the SSL, which is what we're doing. That really should say request the certificate, but it's fine. So, you specify your domain name and the type of web server that's hosting the site. So, whether it's an IIS or an NGINX or an Apache web server, whatever it is, and then your DNS domain name. Now, once you do that in step two, GoDaddy and pretty much all public CAs will need you to prove, to verify that you control the domain. For example, by asking you to create specific types of DNS records in the zone. Once you do that, then you will have proven, of course, that you own that domain. Then you'll be able to download SSL certificates. You'll be able to install them on your server and then redirect HTTP if you wish to HTTPS. So, if people try to connect over HTTP, it'll just automatically redirect them. The steps on how to do that will vary depending on the web server you're using and the operating system platform it's on. So, once you've completed this, you will have a publicly issued web server certificate that will be trusted by all devices around the world.

> [!info]- Semantic Content
>
> **Env Vars:** ssl (6), dns (2), http (2), pki (1), tls (1)
> **CLI Commands:** make (2), nginx (1), apache (1)
> **Prerequisites:** install (4)
> **UI Navigation:** scroll down (1), select the (1)
> **Analogies:** for example (2)
> **Cross-References:** go back to (1)
> **Speakers:** - [instructor] (1)

#### [Install private CA certificates](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/install-private-ca-certificates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/install-private-ca-certificates?u=76281980&t=0)** - [Instructor] In this demonstration, we're going to take a look at how to make sure client devices trust certificates issued by a private CA. The problem here is that when you decide to set up a private CA, it means you're setting up your own entire PKI hierarchy, let's say within your organization, and from that CA, and you might even configure subordinate CAs, whatever the case is, you are issuing certificates from your private PKI hierarchy. Now, devices will not trust those certificates by default, so we have to figure out how to make sure that they do trust it. The way that we're going to do that is first begin by looking at what we have in place. I am sitting at the server where that private CA is hosted. That's not necessary for this, but from a demonstration purpose, perspective, we're going to go take a look at it here so we know what all the moving parts are. On my server, I'm going to go to the Start menu and from [[Windows]] Administrative Tools, I'm going to go into Certification Authority. On the left, here's my private CA. It's called MyOrgCAWindows. I'm going to right-click on that CA and choose Properties. Here, I can choose to view the certificate for the CA. So, it was issued to the CA by that same CA. This is a self-signed CA certificate, and it's got a validity period shown down below. So it, like any certificate, will eventually expire.
>
> **[1:35](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/install-private-ca-certificates?u=76281980&t=95)** If I go to the Details tab, of course, I can read the details about the items in the certificate, like the signing algorithm because it will sign certificates that it issues, the public key details and whatnot. But what I really want to do here is I want to click Copy to File. In other words, I want to export the Trusted Root Certificate for this CA. That's going to include the public key, not the private key. I need this because I need that file to install it on client devices so that they trust certificates that this CA issues. It's part of establishing the chain of trust. As we've said before, if you trust the CA issuer of certificates, then by extension, you trust all the certificates that it issues. I'm going to choose Copy the File. That starts the Certificate Export wizard. I'll click Next. You have a number of formats you can use for the exported file. I'll just go with the standard DER encoded binary X.509. I'll click Next. File name. Well, why don't we put this, let's say, in our Downloads folder, and I'm going to call it PrivateCA_TrustedRoot_Cert.
>
> **[2:52](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/install-private-ca-certificates?u=76281980&t=172)** It can be called anything you want it to be called, and you're going to need to make sure that this file is somehow installed on all client devices. Whether you put this on a shared folder on the network and they go and install it manually, we'll see how to do that in a moment, or whether you have an automated solution such as through Windows Group Policy in an [[Active Directory]] domain to get that installed. Whatever the case is, this file needs to be installed on client devices, so they trust certificates issued from the private CA. I'll click Save. I'll choose Next, Finish. The export was successful. So what do we do now? Well, if we were to manually go to a client device, when I say client device, I mean, technically it could be a server OS, it's a client, meaning that it needs to make a connection, let's say, to a secured website using a certificate issued from the private CA. If I go into my Downloads folder, there's the PrivateCA_TrustedRoot_Cert. By double clicking that we can then choose, on the Windows platform, to Install Certificate. This is how we do it manually. Now, we can do it for the Current User, for the Local Machine, regardless of who signs in. That's what I want. I'll click Next. Okay, it will automatically select the appropriate certificate store, that's fine. Next, and Finish. Okay. The import was successful. This computer will now trust certificates issued by the private CA.
>
> **[4:25](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/install-private-ca-certificates?u=76281980&t=265)** Let's explore that a little bit just for fun. Let's say on this client computer, and again, I'm calling it the quote, unquote client computer for this part of the demo. Let's go into the web browser. In this case, [[Microsoft]] Edge. Doesn't have to be Edge. Because from within here, this is one way that we can take a look at the Trusted Certification Authorities that this machine will trust. So I'll click the three dots context button up at the top here, and I'm going to go down and take a look at the Settings here in Microsoft Edge. In the left-hand navigator here, I'll choose [[Privacy]], search, and services, because over on the right, if I scroll down, I will eventually come across, under the Security section, Manage certificates. Okay, I'll click on that. And I'm interested in Trusted Root Certification Authorities. Here we have an alphabetical list of CAs that are trusted by this machine. Most of these, of course, are public CAs so that any certificates issued, let's say, from DigiCert, will be trusted on this machine. But what about our private CA? Let's press the letter M to jump to Ms, and as we go further down, there it is. There's the MyOrgCAWindows certificate. That's the one that we exported and then double clicked to install. When we did that, it added it here. Because this certificate is here, I'll just double click to open it up here, it means that any certificates issued
>
> **[5:59](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/install-private-ca-certificates?u=76281980&t=359)** by this CA will be trusted on this machine. Whether the certificate is going to be for VPN authentication, for web server authentication, for whatever it's being used for, it will be trusted now.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (3), [[Microsoft]] (2), [[Active Directory]] (1), [[Privacy]] (1)
> **UI Navigation:** go to (3), right-click (1), scroll down (1), click on (1)
> **Prerequisites:** install (4), set up (1), configure (1)
> **CLI Commands:** make (4)
> **Env Vars:** pki (2), der (1), vpn (1)
> **Definitions:** is a  (1), in other words (1), means that (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Generate certificates using Microsoft Azure](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/generate-certificates-using-microsoft-azure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/generate-certificates-using-microsoft-azure?u=76281980&t=0)** - [Instructor] There are a number of different ways that we can acquire a PKI certificate. In this particular demo, we're going to take a look at how to generate certificates using [[Microsoft Azure]]. Now in the Microsoft Azure Cloud, we can use the GUI portal administrative tool. I've already signed in to portal.[azure.com](https://azure.com). The first thing we need to consider is if we want to generate certificates here in the [[Microsoft Azure|Azure]] Cloud, we can do that quite easily, but we're going to need a key vault first. Now a key vault is a resource that you create in your Azure subscription, and it serves as a centralized cloud-based secure storage location for secrets such as certificates. You can have a variety of different services then that reach into the key vault to use that certificate, whether that certificate is used for VPN authentication, whether it's used to secure HTTPS connections to a web server, whatever the case is. So let's start by creating a key vault, and then we'll generate a PKI certificate in the key vault. So I'm going to start here in the Azure Portal by clicking create a resource. What do I need to create here, a certificate? No. First I need to create a key vault, within which I will then create the certificate. So I'm going to choose key vault and I will select the key vault service offering from [[Microsoft]], and I'll click create. When you create a resource, when you create an item in your Azure subscription
>
> **[1:35](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/generate-certificates-using-microsoft-azure?u=76281980&t=95)** in the cloud, you specify a resource group to create it in. A resource group is nothing more than as it implies by its name, it's a way to group cloud resources that are related together. So for instance, if this key vault will be used for a project we're creating called Project A, and it's going to involve [[Virtual Machines]] and websites and all that, we could create a resource group called Project A and then create all of our cloud stuff for Project A in that resource group. You don't have to do it that way, but that's what I'm going to do, create new. I want to create a resource group called Project A. And the key vault here will be called kveast123456.
>
> **[2:25](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/generate-certificates-using-microsoft-azure?u=76281980&t=145)** So it needs to be a unique key vault name. So that's going to be created in the Project A resource group. East US region. Standard is fine. I'm not going to change anything else here in terms of the soft delete, so how long to retain things like deleted vaults, I can reduce that, for example, down to seven days, whatever the case might be. Once I've specified those details, I can then click review and create. It'll validate my selection here. We'll give it a moment. It looks good, no error messages. Let's create the vault. So I'm going to click the create button to do that. So we're creating a key vault, which is simply a resource or an object in our Azure subscription. Now once that's done, within it, I can configure secrets like certificates. You can have more than one key vault if you really want to, for example, for different admin teams, different projects, different regions. But in this case, we're just going to have this single key vault. So the deployment is in progress. Now it's complete. So from here, we can now choose to go to resource, in this case, to go into the properties of the key vault. So we are placed now in the properties of our key vault. In the left hand navigator, if I go down to certificates, the problem is it says this operation is not allowed by RBAC. That stands for Role-Based Access Control. In other words, you don't have permissions to do this despite the fact that you just created the key vault. It's not a problem.
>
> **[3:57](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/generate-certificates-using-microsoft-azure?u=76281980&t=237)** If I were to take a look down under settings on the left under access configuration, the permissions model here is set to Azure Role-Based Access Control. That's kind of what that message was implying. So to solve the issue, let's get right to it. I'm going to scroll up and on the left, still in the properties of the key vault, I'm going to click access control IAM. This is where you set permissions to this resource to, in this case, the key vault. So I'll click access control IAM, on the right, I'm going to click add, add role assignment. I have to give myself permissions to manage this vault, or at least to work with certificates. So I'm going to filter the list of roles by the [[Microsoft Word|word]] vault. I want to make sure I have full access to everything in the vault, not just certificates. So I'm going to grant myself the Key Vault Administrator role. I'll select that. I'll click next. For user, group, or service principle, I will choose select members. And what I want to do here is select my account. Now once I've selected my account here, I'll just click select. I can then choose review and assign twice, and that should be it. So if I go to role assignments on the right to check my work, I've got a role assignment here. If we go further down through the list, we've got all the different types of roles that would apply. But the Key Vault Administrator role is showing here having been assigned to my account at this resource, which is the key vault.
>
> **[5:29](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/generate-certificates-using-microsoft-azure?u=76281980&t=329)** Now if I go to the certificates view, we no longer get the message, we have the appropriate permissions. So let's get to the point. We want to create a certificate here. I'm going to choose the generate import button, because from the method of certificate creation dropdown, notice you could actually import an existing certificate file if you've got it, but I don't. So I'm going to choose generate. The certificate here is going to be called MyCert1. It's going to be a self-signed certificate here in Azure. But if you've pre-configured it already, you can actually use what's called an integrated CA. You would have had to have configured another CA that would integrate here. It could even be a public CA. However, here I'm just going to go with the self-signed certificate. The subject name will be CN or common name equals www.mysite.local.
>
> **[6:27](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/generate-certificates-using-microsoft-azure?u=76281980&t=387)** The validity period here for the certificate will be 12 months or one year. For DNS names, I'm just going to go in there and I'm going to type in the same name, www.mysite.local. This has to match the website that it's going to be used for. Very important. Once I've added that DNS name, I'll click OK in that screen, and then I'll click create. In the certificates view, I can click the generate or import button. From the method of certificate creation dropdown, I can choose to import the certificate if I've already got a certificate file, but I don't. So I'm going to choose generate. For the certificate name, I'm going to call it MyCert. It's going to be a self-signed certificate, but I could choose from an integrated or non-integrated CA. That's a separate configuration, and you might even have a public CA linked to this, but in this case, I'm just going to leave it as a self-signed certificate. The subject name is going to be CN or common name equals demotest.world. This is an external DNS domain name that I actually own, so we'll be using that later when we want to create an HTTPS binding for an Azure host web app. For now, we're just setting this up here. I'm going to click on the DNS names link here, because I want to add the same thing, demotest.world. You have to make sure
>
> **[7:59](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/generate-certificates-using-microsoft-azure?u=76281980&t=479)** that whatever is in DNS name here matches what people will be connecting to, such as maybe through a web browser when they type in a URL, the name they connect to has to match the name in the certificate. I'll click OK over on the right and then I'll click create. So after a moment, the certificate will show up here. There it is, MyCert. Currently, the status is disabled. You just have to give it a minute or two. We can keep clicking the refresh button here. After a moment, it will be an available certificate. And after clicking refresh a recent time, now my search shows up as being an enabled certificate. So we've now created a certificate within an Azure key vault that can be used by a variety of different services.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (10), [[Microsoft Azure]] (2), [[Microsoft]] (1), [[Virtual Machines]] (1), [[Microsoft Word|Word]] (1)
> **Env Vars:** dns (5), pki (2), https (2), iam (2), gui (1)
> **UI Navigation:** go to (3), dropdown (2), select the (1), scroll up (1), click on (1)
> **Analogies:** such as (2), for example (2), for instance (1)
> **Definitions:** is a  (1), stands for (1), in other words (1), is an  (1)
> **CLI Commands:** make (2)
> **URLs:** [azure.com](https://azure.com) (1)
> **Tools:** azure portal (1)


### 4. PKI Certificate Usage

[↑ Back to Table of Contents](#table-of-contents)

#### [Configure a Windows website certificate](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-windows-web-site-certificate?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-windows-web-site-certificate?u=76281980&t=0)** - [Instructor] Let's take a look at how we would configure an HTTPS binding for a [[Microsoft]] IIS web server. HTTPS, of course, allows us to have a secured connection between any other device connecting to the web server, so that means encryption, it means authentication of those network messages. But in order for that to work, we need to have a certificate set up for that web server, and we have to configure an HTTPS binding to use that certificate. Let's take a look at how this works. The first thing we have to do here on our [[Windows Server]] is make sure we've got a web server stack installed. From the Start menu on my Windows server, I'm going to go into Server Manager. You can use the Server Manager GUI as a method of installing server components, like a web server. You don't have to do it this way, but that's how I'm going to do it here. So, here in Server Manager, I'll click the Add roles and features link. I'll just keep clicking Next until I get to the point where I'm asked to select the server roles I want to install on this local server. I'm going to scroll down and in the W's I'm going to turn on the check mark to the left of Web Server IIS. IIS is Internet Information Services, that's Microsoft's standard web server stack. It asks if I want to install the management tools. Of course, that sounds good. I'll click Add Features and I'll click Next. There's nothing I want to add on the Features screen, so I'll just continue clicking Next.
>
> **[1:34](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-windows-web-site-certificate?u=76281980&t=94)** I'll keep going through this and I'll just click Install. There are no special web server components I want, so I've just accepted the defaults. Now, the default also means that we will have a default sample website, but not over HTTPS, just over HTTP. Okay, so the feature installation has completed, let's click Close. What that means is either from the Tools menu, I can select Internet Information Services IIS Manager to manage the website. I'll just close out a Server Manager. I also could do the same thing by going to the Start menu and under [[Windows]] Administrative Tools in the I's, I'll find the exact same thing. Internet Information Services IIS Manager, so I'm going to go ahead and launch that. In the left hand navigator, I've got my server name. If I expand that, underneath it I can expand Sites and I will have a default website. Okay, well, let's examine what that looks like. One way to do that is, of course, to connect to this web server using a web browser. So, I can do that in a number of ways. I'm going to go and start Microsoft Edge on this host. And what I want to do in Edge is enter the local loop back IP, 127.0.0.1, so I'm pointing to myself here on this host. And here we're presented with the standard Windows Server Internet Information Services webpage. That is working, however, it's working over TCP Port 80 and it's only an HTTP connection, it's not HTTPS.
>
> **[3:12](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-windows-web-site-certificate?u=76281980&t=192)** If we were to go back to IIS Manager and right click on the Default Web Site, we can then choose Edit Bindings. Here we've got an HTTP binding, as we know, for Port 80. What we want to do is add a binding for HTTPS. But before we do that, let's open up a Command Prompt here, so CMD, where I'm going to run MMC, the Microsoft Management Console. There are a number of ways to get into certificates, this is only but one way, but I want to view the certificate store on the local machine for the machine, not for user certificates. So, here in MMC, I'll go to the File menu and choose Add/Remove Snap-in. I'm going to choose Certificates, Add. It's going to be for the computer account, Next, the local computer I'm at right now, so Finish, OK. If I expand Certificates in the left hand navigator and go down under Personal, Certificates, we've got certificate here for www.mysite.local. This is a certificate that was issued by my private CA in Windows, which is called MyOrgCAWindows. The certificate is valid for one year. So, before that one year expiry, technicians need to make sure they provide a new updated certificate for the web server, or they could renew the existing one before it expires because after the certificate expires,
>
> **[4:45](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-windows-web-site-certificate?u=76281980&t=285)** users will get messages when they connect to this website stating that it's not to be trusted. It's also important to note that connecting to the website must occur using the same name embedded in the certificate, which in this case is www.mysite.local. Now, you might wonder, how is it that attackers go about compromising certificates? Well, one obvious answer is if an attacker can get into a machine, if they can compromise a machine where they could get this certificate and export it, including with its private key, that's one way they could use that certificate for nefarious purposes. But another possibility is malicious actors registering a DNS domain and adding records as if they were another organization, or what happens often using nomenclature that is very similar, but not quite spelt exactly correctly. Imagine, for example, attackers registering a DNS domain name back in the day for [google.com](https://google.com), but only with one o because some people typed that in incorrectly, and then registering a certificate for it. So, that's another way that hackers might go about trying to compromise this. But in this case, we've got a valid certificate on this machine, so I'm going to close out of that. Not going to save it. Let's get back into the IIS Manager where we were reviewing the site bindings for the website. We're going to click the Add button and we will add an HTTPS binding for all IP addresses.
>
> **[6:20](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-windows-web-site-certificate?u=76281980&t=380)** The default port number over TCP for HTTPS is 443, so I'm going to accept that. What I need to do at this point is I need to go down under SSL certificate, which in and of itself is not quite great nomenclature as SSL is deprecated, and it's really just a certificate. Whether you use it for SSL or something totally different is fine. So, what I'm going to do from the dropdown list is choose www.mysite.local. That's the certificate that we viewed a few minutes ago that's available on this host. I'll click OK and I'll choose Close. So, we now have an HTTPS binding, we shouldn't have to restart the website or anything like that. Let's go test it out in our web browser. So, first of all, I'm going to run [https://www](https://www)., and it was called, mysite.local. Now, of course, that name has to be resolvable to the correct IP address for that to work properly. So, whether you are resolving that DNS name through a DNS entry, whether you're doing it through a host's file, whatever the case is, it's mapped to my IP on this server. And notice now I'm able to make an https connection to mysite.local. So, what's interesting is we're not being prompted with any security issues related to the certificate or anything like that. So, if I were to click on the little padlock, in this case, in Microsoft Edge
>
> **[7:53](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-windows-web-site-certificate?u=76281980&t=473)** to the left of the URL line, it says that the connection is secure. This site has a valid certificate issued by a trusted authority. If it's not a trusted authority, it could be because it's a privately issued certificate from a private CA and you don't have the private CA trusted root certificate installed on this device.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft]] (5), [[Windows Server]] (3), [[Windows]] (2), [[Google]] (1)
> **Env Vars:** https (9), iis (7), dns (4), http (3), ssl (3)
> **UI Navigation:** click on (2), select the (1), scroll down (1), go to (1), dropdown (1)
> **Prerequisites:** install (3), configure (2), set up (1)
> **CLI Commands:** make (3), find (1), snap (1)
> **URLs:** [google.com](https://google.com) (1), [https://www](https://www) (1)
> **Ports:** port 80 (2)
> **Definitions:** is a  (1), is called (1)

#### [Configure a Linux website certificate](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-web-site-certificate?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-web-site-certificate?u=76281980&t=0)** - [Instructor] In this demo, we're going to be configuring a [[Linux]] web server certificate. We've already acquired the certificate. We already have a certificate to use for HTTPS. What we need to do is tell the web server that we want to use that certificate to make sure that we have that HTTPS connection, which of course uses TLS as the [[Network Security]] protocol suite to secure the connection. So to get started here in Linux, I'm going to install the Apache Web server. Sudo apt install apache2. If it's already installed, which it is here, it will tell me that it's already there and it'll tell me the version. So that's fine. We've got the Apache web server installed. We also want to make sure the web server is up and running. We can do that with this sudo service, in this case, apache2 status. So it shows here that it is in fact active and running. Otherwise, we could modify our last command and just change status to start. Of course, you can also stop the web server that way, or even restart it if you want to pick up a new configuration change. Now, the next thing we want to do on this web server, now that we know it's there and running, is we want to make sure that it's set to use TLS. So the way that we're going to do that is to open up a config file. I'm going to run sudo nano. Nano is a built-in text editor in some Linux distributions. In this case for [[Ubuntu]], it's there.
>
> **[1:35](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-web-site-certificate?u=76281980&t=95)** What I want to do is reference a file in slash etc slash apache2 slash mods dash available, and the file I want to open up is called funnily enough, ssl.conf as opposed to tls.conf. So a lot of the old ssl stuff still persists today, even though ssl should not be used. What I'm looking for within this file is the set of protocols that will be used. So what I'm looking for is the directive here called SSLProtocol. Now, I've already configured this. What I've done is specified minus all. What that does is it turns off all SSL and all TLS support. After that, I've got a space, then I have plus TLSv1.3. What I'm specifying here is I want to add, that's the plus sign, support for that protocol. So in other words, my web server will only talk using TLS version 1.3. So connecting clients are going to have to support that, otherwise there will not be a connection. Okay, so that's already done. If it's not already set that way I can easily just edit the file and save it, but it's already done. So I'll control X to get out of there. The next thing I need to do is I need to make sure that my web server is set to use my web server certificate. If I type ls for the current directory I'm in on my server,
>
> **[3:10](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-web-site-certificate?u=76281980&t=190)** which is cert, that's a subdirectory I created previously, and I also acquired a web server certificate from a private CA previously. My web server certificate is called www.demotest.world.pem. I want to use that on my web server to secure the connection with HTTPS. So I'm going to use the sudo nano command, nano being the text editor again to open up slash etc slash apache2 slash sites-enabled slash 000-default.conf. Because in here, the first thing I want to do, and I've already changed it, is I want to change the virtual host section at the top from port 80, which is an HTTP binding to port 443, which is by default the port used for HTTPS. You can change it, but that's what I'm going to go with the norm. Then further down below, there's some directives here I haven't changed like the document route where the webpages are served up from. However, I do have some SSL directives here. Again, SSL still persists even though we know to use TLS. At any rate, SSL engine on, SSL certificate file for the server here is in slash cert, and it's the file we just mentioned. It's called www.demotest.world.pem. That's the web server certificate file.
>
> **[4:44](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-web-site-certificate?u=76281980&t=284)** Now, the key file matches that, it's the private key for the web server, so it's stored separately from the certificate. And then we've got the SSL CA certificate file, which is a private ca I've created, I have that file here. It's called MyPrivateCA.pem. Now because we've got the private key file for the web server here, and private key files normally have a passphrase, it means when you start or restart the web server, it's going to prompt for that passphrase. Now, if you really want to, you can use the open SSL command line to remove passphrases from private key files. However, for this example, I'm just going to leave it as it is. So for instance, if I clear the screen and run sudo service apache2 restart, to put new changes into effect, sure enough, it's asking me for my passphrase for my server key. So I'll go ahead and enter in that passphrase and then I'll press Enter. And of course we can verify that the web server is running. I'll just use the up arrow key to go up through previous commands to where we were looking at the status of the Apache 2 Daemon or Apache web server, and it's active and running. Okay, so why don't we go to a client web browser machine, and why don't we try to make an HTTPS connection to this host? Okay, so from a [[Windows]] computer in a web browser,
>
> **[6:20](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-web-site-certificate?u=76281980&t=380)** I've connected to [https://www.demotest.world](https://www.demotest.world), and I did get the default Apache2 webpage. However, of course, it's stating in my browser that this is not a secured site. So I've navigated to it anyway, but if I click not secure up in the address bar and click on the message, it says, "Your connection to this site isn't secure." And then if I click on the little certificate icon, I can see that this certificate that the web server is using was issued to www.demotest.world. Well, I'm connecting to that name, so that's good, but it was issued by MyPrivateCA. Oh, okay. Well, the problem is my computer doesn't trust that private CA. We know what to do about that. We just have to install the CA certificate, basically the trusted root certificate. If we trust the CA, we will then trust certificates it issued. So how do we do that? Well, here in our [[Microsoft]] Edge browser, we know we can go into our settings by clicking the three dot button, go down to select settings on the left. I then want to go into [[Privacy]] search and services on the right. I'm just going to scroll down until I see the managed certificate section, which I'll click on. I want to go to Trusted Route certification authorities. Let's just check it out. If I go to the M's, do we have MyPrivateCA? No, we don't. Well, we would need to have
>
> **[7:52](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-web-site-certificate?u=76281980&t=472)** that trusted route public key certificate, so we could install it on this machine, and I'm going to do it manually. I do have the file on this machine, so I'm going to choose import. I'm going to choose next, and I'm going to browse to my downloads where I've got MyPrivateCA.cer, certificate file. That file might also end with dot pem. Many different formats of the file can be used, and really it's just a file extension here. So in this case, I'm going to open that file, choose Next. I want install the certificate in the Trusted Route certification authority section. So I'll just proceed through and finish and it says, "Are you sure you trust MyPrivateCA? You might want to contact them because if you trust them, you're going to trust every certificate that they issue." So from a security perspective, if an attacker can somehow fool someone into doing this, into installing what we're going to call a malicious CA certificate, then that means that that machine will trust certificates issued by that CA. So this is something that attackers will often try to do in a variety of different ways to try to get this installed on client devices. In this case, we know that we trust it because we created it, no solicited us and asked us to do it. We are doing it. So do you want to install this certificate? Yes. Okay, import was successful, Close. So if we just kind of go back into that same section into
>
> **[9:26](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-linux-web-site-certificate?u=76281980&t=566)** that same HTTP certificates into Trusted Route Certification authorities and down into the M's, notice the presence now of MyPrivateCA in the Trusted Route certification authority store in this machine. Perfect. Let's close out of that. Now, if we just try to refresh our connection to that website, it might take a moment before it actually lets us back in and isn't using old cache information. So I'm going to close the web browser and fire it up again, and we're going to connect to the exact same URL. And now notice this time we are connected to the same URL, but we no longer have a message about security or not trusting certificates. So if I click on the little padlock up in the address bar to the left of the URL, it says the connection is secure and I can view the certificate. It's the same certificate for the web server issued by the same private CAs just now my machine trusts it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (3), [[Network Security]] (1), [[Ubuntu]] (1), [[Windows]] (1), [[Microsoft]] (1)
> **Env Vars:** ssl (7), https (5), tls (5), url (3), http (2)
> **CLI Commands:** make (5), sudo (5), apache (4), apt (1), ls (1)
> **UI Navigation:** click on (4), go to (3), scroll down (1)
> **Definitions:** is a  (2), is called (2), in other words (1), is an  (1), means that (1)
> **Prerequisites:** install (6)
> **Ports:** port 80 (1), port 443 (1)
> **URLs:** [https://www.demotest.world](https://www.demotest.world) (1)

#### [Configure web brower client certificate authentication](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-web-brower-client-certificate-authentication?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-web-brower-client-certificate-authentication?u=76281980&t=0)** - [Instructor] HTTPS client certificate authentication. What is this? We already know that we can install a PKI certificate on a web server, configure an HTTPS binding for it, and of course, that secures and encrypts the connection between client devices that are connecting to the web server and the web server itself. Compared to HTTP, everything's encrypted. The messages are authenticated, but we could take it a step further with client certificates. But before we get too far ahead of ourself, let's explore here a little bit about our configuration for our web server, here on my [[Windows Server]]. I'm going to go to the start menu because in the start menu, I want to go to [[Windows]] Administrative Tools, and I'd like to go into the IIS manager tool that lets me manage the [[Microsoft]] Web server installed on this host, in the left hand navigator I'll drill down under the server name and I'll drill down under websites. So we've got a standard default website here for our web server. If I were to right click on it, I can go down to Edit Bindings. I've got the standard HTTP Port 80 binding. So that's not a secured connection at all, but I've already got a connection here configured for HTTPS over port 443. And if I select that and click Edit, I've got an SSL certificate selected there. We know that really it's just a PKI certificate. SSL shouldn't be there, but that's okay.
>
> **[1:35](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-web-brower-client-certificate-authentication?u=76281980&t=95)** What I want to do here is view that certificate. It looks like it was issued to www.mysite.local. So that name has to match what people are connecting to for the certificate to be considered valid. Not to mention the certificate cannot have expired, but it was issued by a private CA called MyOrgCAWindows. Okay, got it. If we examine the SSL settings for this website, so the default website still selected on the left, if I double click SSL Settings on the right, we don't require SSL connectivity to this site. So HTTP connections would still be allowed and we are ignoring client certificates. One thing at a time, let's require SSL and I'll click Apply. There's no good reason to ever allow HTTP connectivity anymore. So SSL or using the certificate really for TLS is what we want turned on. It's been put into effect. Changes have been successfully saved. I want to try to connect to this web app, this website in a browser using only HTTP. So from my start menu, I'm going to fire up the Microsoft Edge web browser, not that it has to be that web browser, of course, and I'm going to run HTTP, just [HTTP://www.mysite.local](https://HTTP://www.mysite.local). So of course, that DNS name
>
> **[3:10](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-web-brower-client-certificate-authentication?u=76281980&t=190)** has to resolve to the IP address of our server. Okay, we're getting a 403.4 forbidden error. "The page you are trying to access is secured with," well, here it is again, SSL, it's TLS, it's not SSL, but that's okay. Let's change it. So let's add the prefix for the protocol here in the address line to [https://www.mysite.local](https://www.mysite.local). Everything's beautiful. Okay, so we're connected. We see the webpage. If I click the little padlock icon here in Microsoft Edge, it says the connection is secure. Life is beautiful. Let's take it a step further by requiring our client certificates. So back into the IIS Manager Tool. What I want to do, we're still in the SSL Settings, is I want to not accept, I want to require client certificates. Note however, that that option's only available if you require SSL up above, otherwise it's grayed out. Requires SSL, require client certificates Apply. Alright, so now let's test connectivity. Now, be careful with refreshing your browser and whatnot. It's usually best to close out of the browser. In some cases some people have found that rebooting the system works best, but sometimes browsers will cache information. At any rate, I'm going to restart the Microsoft Edge browser and we'll do the basic thing first, meaning we'll just connect to https, [https://www.mysite.local](https://www.mysite.local).
>
> **[4:49](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-web-brower-client-certificate-authentication?u=76281980&t=289)** Okay, looks like we're connected to the site. Let's just do a refresh. But if we actually try to refresh now it says it can't reach the page. If we were to go down to a different browser such as the older Internet Explorer browser, we would get this type of message. So the browser really will determine what kind of a message you get. This one's more meaningful though. It's giving us HTTP error 403.7 forbidden. "The page you are attempting to access requires you a browser to have a client certificate that the web server recognizes." Let's focus on the web server recognizes part. So what that means is, "Does the web server trust any client certificates?" Here's the answer. If we go back into the IIS Manager Tool, right click on the Default Website, edit the bindings, select the HTTPS binding, Edit it, and if we go down and view the certificate, alright, here we go. So MyOrgCAWindows is the CA that issued the service certificate. So any certificates that MyOrgCAWindows issues to clients will be trusted by the web server. It's the PKI chain of trust. If you trust the certificate authority or the creator or issuer of certificates, well then by extension you trust certificates that it issues. Okay, so having done that, let's see what's going on here. Back to the current issue, we cannot connect to the website and in Microsoft Edge,
>
> **[6:24](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-web-brower-client-certificate-authentication?u=76281980&t=384)** you don't quite get the same HTTP type of error message, but it's saying it can't reach the page. So in other words, we can't connect. If I click the padlock icon, it does say that the connection is secure. So the site itself has a valid certificate used by a trusted authority. Well, that's fine server side, but we still can't reach the page because we don't have a client certificate that the web server trusts. So from my command prompt on this machine, I will run MMC, the Microsoft Management Console because I want to manage certificates on this host. Now I'm doing it on the same server. In reality, you would be configuring client certificates on other devices that are trying to securely connect to this website, whether it's tablet, a smartphone, a laptop, whatever it is, and it could be any operating system. Doesn't matter if it's an iPhone, [[Android]] phone, the Mac OS, [[Linux]] OS, whatever. So what I'm going to do here in the case of Windows is go to File, Add/Remove Snap In, Certificates. I want to manage the certificates on this machine for users. So my current user account that I'm logged in with, okay, Finish. If I were to expand Certificates - Current User and then Personal, Certificates on the left, it would reveal any certificates issued to the user currently signed in to this machine on different operating system platforms like Linux and the Mac OS, and on a smartphone. The steps will vary and the tools will be different,
>
> **[7:58](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-web-brower-client-certificate-authentication?u=76281980&t=478)** but the concept remains exactly the same. What I don't have here is a client certificate that can be used for client authentication. I've got a client certificate here, all right. If I double click to open it up, it's a self signed certificate issued by myself to myself for file recovery. Well, that's not going to cut it. No wonder we can't authenticate to the website that requires client certificates. Now this Windows computer is joined to an [[Active Directory]] domain, so it makes it very easy to request client certificates for authentication. For example, what I can do here is right click Certificates on the left. I can choose All Tasks, Request New Certificate. Now, depending on the solution you are using for certificates, you might go to a specific website, for a public ca to get trusted client certificates. If you've got your own internal private CA, you might go to a website that's been made available internally to request a certificate. There are many different ways you can accomplish this, but here at Windows, this is what I'm going to do. I'll click Next, Active Directory Enrollment Policy, Next. Okay, here we go. I want a User certificate because if I click Details over on the left to expand it, I can have a certificate for many purposes, including Client Authentication. That's what we need here. So I'm going to choose Enroll, Succeeded. We now have a new certificate, and if we take a look carefully, it was issued by MyOrgCAWindows.
>
> **[9:32](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-web-brower-client-certificate-authentication?u=76281980&t=572)** That's the same ca that issued the web server certificate. It issued me a certificate, so therefore the web server's going to trust it. Let's go back into the web browser and we couldn't reach the page before. Let's just see what happens if we refresh. Well, no such luck here. Let's close the web browser and open it back up and let's give it a try again. So I'll go into Microsoft Edge, and once again, we will try to connect to HTTPS and our URL in this case is going to be www.mysite.local. So now we are successfully authenticated to that website. Not only does the web server use a certificate to encrypt and secure communications to it, but to take it a step further, it also requires connecting devices to have a valid, trusted client certificate.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft]] (7), [[Windows]] (4), [[Linux]] (2), [[Active Directory]] (2), [[Windows Server]] (1)
> **Env Vars:** ssl (12), http (9), https (5), pki (3), iis (3)
> **UI Navigation:** go to (5), click on (2), select the (1)
> **URLs:** [https://www.mysite.local](https://www.mysite.local) (2), [http://www.mysite.local](http://www.mysite.local) (1)
> **Ports:** port 80 (1), port 443 (1)
> **Versions:** 403.4 (1), 403.7 (1)
> **Definitions:** in other words (1), is a  (1)
> **Analogies:** such as (1), for example (1)

#### [Configure VPN certificate authentication](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-vpn-certificate-authentication?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-vpn-certificate-authentication?u=76281980&t=0)** - [Instructor] We know that PKI certificates are commonly used with [[Web Servers]] to encrypt communications with connections to and from that web server. But certificates can also be used for things not directly related to TLS, like file system encryption, and even authenticating to a VPN. In the case of a VPN the certificate is also going to be important if you're using an SSL or a TLS type of VPN that uses standard port 443 for VPN connections. So, we're going to configure a VPN, a cloud-based VPN, in the [[Microsoft Azure]] Cloud that uses client certificate authentication. Here in the Microsoft Azure portal I'm going to begin by opening up my left-hand navigator and clicking All resources. This will show me all of the items, all of the resources deployed in my [[Microsoft Azure|Azure]] subscription. I've already deployed something called a virtual network gateway. Think of that as being the representation of a VPN device in the Azure Cloud. So, it's kind of like having a VPN [[Hardware]] device that you plug into an empty switch port on a [[Local Area Network (LAN)|local area network]], so that's kind of what we've got here defined already. It's called eastvpn1. I'm going to click on it to open up its properties because what we're going to do now is configure client to site VPN settings. We do that in the properties of the virtual network gateway here
>
> **[1:34](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-vpn-certificate-authentication?u=76281980&t=94)** by going down on the left and selecting Point-to-site configuration. It says point-to-site is not configured, so we're going to click the Configure now link. We need to specify an address pool that will be used to assign IP addresses to VPN clients, so it doesn't have to match any preexisting network you might have deployed in the cloud or anything like that. So, I'm going to specify 192.168.3.0/24. So, the /24 tells me that the first 24 bits from the left are assigned to the network address, which in this case is 192.168.3. So, all VPN clients that authenticate will have an additional IP address added on their device, which will start with 192.168.3. For the tunnel type we have a bunch of options here. I'm interested in an SSL, or really a TLS type of VPN, so I'm going to go with the standard OpenVPN SSL selection here. For the authentication type I'm going to choose Azure certificate, that also will require an Azure user account within the Microsoft Azure [[Microsoft Entra ID|Entra ID]] Tenant. Let's just explore that for a moment. If I were to duplicate my web browser page here, just so we can keep our place in the first one, in the left-hand navigator I can go to my [[Microsoft Entra ID]] Tenant. Think of that as being a directory service that's loosely similar to an [[Active Directory]] domain on premises where you can go and view your user accounts.
>
> **[3:10](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-vpn-certificate-authentication?u=76281980&t=190)** So, we would have to have a user account here in addition to a certificate to authenticate to the VPN. So, we've got Azure certificate. Down below, what this configuration requires is for me to give a name to a root certificate. This is the trusted root certificate, the public certificate data from a certificate authority that I need to paste in the next field. Where do I get that from? Well, if you're working with public CA certificates you can easily acquire the public certificate data from your public CA and paste it in here. And then of course, your client devices that want to connect to the VPN, they would have to have a certificate installed issued from that CA. But it could also be a private CA. It doesn't have to be public. On my local computer I've started the [[Powershell]] ISE. I'm running [[Windows]]. ISE stands for Integrated Scripting Environment. It's kind of a GUI color-coded environment for me working with PowerShell. In it I've paste a number of statements that will create my own private root CA and a certificate issued from it. So, in lines three through to six I am using PowerShell to create a root CA. You don't have to create a new one for this, you could use an existing one. But in this case this is what I'm doing. So, I'm creating a variable called $cert to store the new root certificate information. The root certificate will be called MyRootCert, and it will show up in the local certificate store
>
> **[4:46](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-vpn-certificate-authentication?u=76281980&t=286)** on this Windows computer. We'll check it after we're done. In lines 10 through to 14 I am using that $cert variable from above, which represents our root certificate, to create or issue a client certificate, which I am naming MyClientCert. It too will show up in the local certificate store on this Windows machine. So, let's go ahead and click the Run button up at the top, Run Script, or you could press F5 to execute these statements. So, there are no error messages, so it looks good. Let's open up MMC, the [[Microsoft]] Management Console, on this machine to verify that we have MyRootCert and MyClientCert. Here in MMC I will go to File, Add/Remove Snap-in, Certificates, Add, and I want to look at the local user account certificates, that would be for the user I'm currently signed in as right now. So, Finish, OK, so Certificates-Current User, I'm going to expand that in the left-hand navigator. And I'll go down under Personal, and then I'll click Certificates. Notice the presence of MyRootCert and MyClientCert. If I double-click to open up the properties of MyClientCert, it was issued to MyClientCert by MyRootCert. And we've got the validity period shown here. We know that PKI certificates don't last forever, and eventually they expire. It also says that there is a private key that corresponds to this certificate.
>
> **[6:20](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-vpn-certificate-authentication?u=76281980&t=380)** Okay, so the client certificate then is what I need installed on my client device where I want to establish a connection to the VPN, whether it's a smartphone, a laptop, tablet, whatever it is. But what we need to do here is go into the RootCert, I'm going to right-click on it and choose All Tasks, Export. What I need to do is make sure I export the public key. So, I don't want to export the private key so Next, I'm going to choose Base-64 encoded, that's the format I want to use, Next. Then I'll specify a final name and path where I want the exported root certificate, the public key certificate, for my private CA to be exported to.
>
> **[7:16](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-vpn-certificate-authentication?u=76281980&t=436)** Once I've specified that I'll continue through the Wizard, and it says the export was successful. I want to go open up the exported file that represents that certificate. I've opened up that file in Notepad, and what I want to do is highlight everything between Begin Certificate and End Certificate. And I'll press Control + C to copy it to my Windows clipboard. And so, back here in the Azure portal I'm going to paste that copied information into the public certificate data field. So, this is important because what we're telling our VPN config in the cloud is you can trust certificates issued by this root certificate, and that will allow certificate authentication to the VPN, of course, in addition to a username and password. Okay, so I'm going to go ahead and click the Save button here at the top of our configuration to save this. Okay, once that configuration is saved notice that we can click the Download VPN client button here in the portal to download a simplified configuration installer based on the configuration of this virtual network gateway. So, that's going to let you download a ZIP file that you can then extract, which I've done here. And here, for example, you could go into OpenVPN, you might install the OpenVPN VPN client software, this is free software, and then use this configuration file to make the configuration to the VPN.
>
> **[8:50](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-vpn-certificate-authentication?u=76281980&t=530)** You would need that. You have to connect to the VPN using its public IP, and you also have to have the client certificate, which we have on this machine, to complete that connection. So, I've downloaded and installed the latest OpenVPN Connect client. You can search in your favorite web browser search engine for OpenVPN Connect, and you'll find the download link very quickly. From here I could then specify File, and this is where I would browse for and select the OpenVPN file for our VPN configuration that we downloaded and extracted. When I do that it automatically populates details like the profile name, the server host name, all of that stuff is filled in automatically. At this point we would then be able to click Connect and authenticate with a username-password, and of course, the present PKI certificate on this client device.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (6), [[Windows]] (4), [[Microsoft Azure]] (3), [[Powershell]] (3), [[Web Servers]] (1)
> **Env Vars:** vpn (22), pki (3), tls (3), ssl (3), ise (2)
> **Tools:** powershell (3), azure portal (2)
> **CLI Commands:** make (2), snap (1), find (1)
> **UI Navigation:** click on (1), go to (1), double-click (1), right-click (1)
> **Prerequisites:** configure (3), install (1)
> **Versions:** 192.168.3 (3)
> **Analogies:** kind of like (1), similar to (1), for example (1)

#### [Configure a network printer certificate](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-network-printer-certificate?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-network-printer-certificate?u=76281980&t=0)** - [Presenter] These days, we have a lot of different devices on the network that might exist with a built-in web server stack, even built into the firmware when it comes to [[IoT]] devices or as in our example, a network based printer. Here in my web browser, I'm connecting to the DNS name of a printer on my [[Local Area Network (LAN)|local area network]]. You can connect to the IP address, it doesn't matter. But what I'm trying to do here is to connect over HTTPS and it's saying it's not secure. It says "your connection is not private." Now at this point, we know enough to realize that what that means is that yes, there is a certificate installed on that device, in this case a network printer, but our machine doesn't trust it. Why would that be? Most likely because either the certificate has expired or perhaps it was issued by a CA that our machine doesn't trust, or maybe we're connecting to a name here, printer1.local and that's not embedded as an alternative name in the certificate. If I click where it says not secure here in my browser, it says the certificate is not valid. Well that's not good. If we click on that, what do we have here for details? We have a certificate issued to something called HP4DA3FE, presumably an HP network printing device, but it was issued by itself and it's got quite a long validity time period. So, 20 years in this case.
>
> **[1:34](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-network-printer-certificate?u=76281980&t=94)** So what's the problem here? The problem is it was issued by itself. Why should our machine trust it? Anyone can issue their own certificate. So this is good. This is PKI and TLS working together as they should to warn us. Now what I'm going to do in this case is I'm going to continue anyways. I know the printer, it's on my local area network. I'm the one that made it available. So I'm going to click advanced down below here and I'm going to choose proceed to printer1.local unsafe. I know it's safe. Now I've already got an authenticated connection here. You might be prompted when you connect to different IoT devices or network printers, whatever it is, you might be prompted to authenticate with admin credentials, and of course you should be using a strong password for that. In addition to using HTTPS. What I really want to do here is install a certificate of my own choosing on this embedded web server. So it's embedded in the firmware of the printer. Okay, so I'm going to click on network in the menu here. Naturally, depending on the type of device you're connecting to in your browser and what its nature is and what version it is will determine what the interface looks like and where you click. It's going to be different. However, what I want to do here on the left is scroll down and take a look at my advanced settings because here is where I can view and make changes potentially to the certificates used by this device, this network printer. So I'm being prompted to sign in. So let me just enter the credentials to continue. Okay, so here we've got some information
>
> **[3:11](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-network-printer-certificate?u=76281980&t=191)** about our printer certificate. It's already installed and we can view it. This is the same thing, the same type of information that was presented to us in the web browser when it said this was not a safe site because it was issued by itself; that's the issuer to itself; that's the subject. Yes, self-signed certificates are normally not trusted. Okay, so if we wanted to, we could trust this certificate basically by installing the public key certificate on our device. But I'll just click okay. What we could also do is install a different certificate. Now down below we could also install a certificate authority certificate down below. Maybe if we have a private CA in our organization or from a public CA, if we want our printer to be able to authenticate to email servers. For example, if someone scans in a document that wants it emailed to their email account, or if you are using 802.1x devices on your network for authenticating connections to the network, like 802.1x compliant [[Wi-Fi|wifi]] routers or ethernet switches or VPN appliances. We're not doing anything like that. So I'm interested in printer certificate where I can click configure. Here we have a number of options. We could create a new self-signed certificate. Well, the only time that might be useful is if the existing one has expired. It still won't be trusted unless you've added the trusted route certificate, the public key to your local device and it would be trusted,
>
> **[4:46](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-network-printer-certificate?u=76281980&t=286)** otherwise it won't be. We can also choose to create a certificate signing request. Here it's just called a certificate request, but it's the same thing, CSR. What we're doing here is generating a certificate signing request that we then supply to a certificate authority or a CA and it would then generate a certificate for us. That might even be a public CA. You could choose to install a certificate that you already have. So it says the certificate though must have been created using the last certificate request generated by this printer. In other words, you would first create the certificate request. Then after you get the certificate from the CA and maybe download it to the computer you're running this web browser on, you could choose to install the certificate from there. You can also choose to import a certificate and a private key that you already have, one that's already been generated. Now that might be something again from a public CA, you already have it, so you don't need to generate a request. It might be from a private CA, whatever the case might be. In this particular example here in MMC where I'm managing certificates, I have a certificate called MyPrinterCert. If I open it up, it's got a certificate validity date and it was issued by something called MyRootCert, which is a private CA. It was issued to MyPrinterCert, okay. So as long as my machine trusts MyRootCert by extension,
>
> **[6:23](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-network-printer-certificate?u=76281980&t=383)** it will trust MyPrinterCert. I want to configure my printer with MyPrinterCert. So I want my own certificate that is trusted. Bear in mind that within the time span of one year, 'cause that's the default here, I'm going to have to make sure I renew that certificate, after which I would have to replace it with a new one. So someone has to be tracking this. It's very important. It could be as simple and old school as adding an entry on your calendar before the expiration date as a reminder. Okay, I'll click on, okay, here. What I'm going to do is right click on MyPrinterCert, all tasks, export. I'm going to export it so I can then import it to the printer firmware. So I'll choose next. I do actually want to export the private key. Remember that with the TLS exchange over HTTPS, that the server will give its public key to connecting clients. But then when clients send encrypted messages to generate the session key, the server decrypts those messages with its related private key. We need the private key here. So I'll choose next and I'll accept the default format. And the next thing I'll do is specify a password. Whenever you're storing a private key in a file, you must use a complex password. Otherwise people would only have to get a hold of that file to be able to use it as that entity. For the encryption, it's got TripleDES-SHA1.
>
> **[7:59](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-network-printer-certificate?u=76281980&t=479)** Eh, TripleDES is really not something we should be using. Instead, we should be using its successor, AES. So I'm going to choose AES256 for encryption and then SHA 256 for signing and I'll click next. Okay, now I have to specify the location and name of the file to store this exported certificate in which will include the private key. After I finished the export, I've now got an exported printer certificate file that includes the private key. So back here in my printer configuration, I want to import a certificate in private key now that I've got a file containing that, I'll click next and then I'm going to click choose a file and select it from my local file system. Alright, once I've specified the file, I have to enter the password for it because remember we saved it with the password because it contains a private key. Okay, I'm going to click the finish button and just like that the changes have been updated successfully. I'll click okay. So now if we click on the view button to view the installed printer certificate, it is our new printer certificate that we've imported from the file system. So as long as we've got within the certificate, the name embedded that we are connecting to, such as printer1.local, we can now have connectivity over HTTPS that is trusted so that we no longer get
>
> **[9:34](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/configure-a-network-printer-certificate?u=76281980&t=574)** those untrusted messages.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[IoT]] (2), [[Local Area Network (LAN)|Local area network]] (2), [[Wi-Fi|Wifi]] (1)
> **Env Vars:** https (4), tls (2), dns (1), hp4da3fe (1), pki (1)
> **Prerequisites:** install (5), configure (2)
> **UI Navigation:** click on (4), in the menu (1), scroll down (1)
> **Definitions:** is a  (2), in other words (1)
> **Analogies:** for example (1), just like (1), such as (1)
> **CLI Commands:** make (2)
> **Speakers:** - [presenter] (1)

#### [Acquire a web app certificate using Microsoft Azure](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-app-certificate-using-microsoft-azure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-app-certificate-using-microsoft-azure?u=76281980&t=0)** - [Instructor] Securing network communications to a web app means using TLS, Transport Layer Security because we know that we get the benefits of encryption. We also know that we have message authentication for all network transmissions, so we have all those benefits, but in order to use TLS with a web app, you need to have a certificate, so what we're going to be doing here in the [[Microsoft Azure]] Cloud is enabling an HTTPS connection to a Microsoft Azure hosted web application. The first thing we should do is kind of take an inventory of what's currently in place before we dive into it and try to solve the problem, so think for just a moment. If I'm using the Microsoft Azure Cloud, and I want to use a certificate to secure a web app, what do I need to have in place? Well, this is not a Microsoft Azure course, but the reality is [[Cloud Computing]] is a big deal these days. You need a key vault. Let's start by looking at our key vault. I'm going to go to the All resources view here in the Microsoft Azure portal, and I've got a key vault deployed. I'll click on it to open it up. In the left-hand navigator, I want to go down and click on Certificates. Here, I have a certificate that I've already created in this key vault, and it's called MyCert. If I click to open up the details of that certificate, in its properties, if I scroll down, I can see that the subject name
>
> **[1:32](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-app-certificate-using-microsoft-azure?u=76281980&t=92)** is a common name of demotest.world. Why is that important? Because the name inside the certificate must match what people are connecting to. Doesn't have to be people, I suppose. It could be another network service somewhere reaching out to your web app to get something, but whatever the case is, for HTTPS to work properly with the certificate, the name has to match, so we've got a subject alternative name here. You could have more than one. It needs to match. Okay, so demotest.world. Got it. Now, what I want to do in [[Microsoft Azure|Azure]] is take a look at my App Services. In other words, hosted web apps. Okay, so I've got one here. It's up and running. If I click on it to open up that web app's properties, in the overview, it's called demowebappeast123, but it's using the [azurewebsites.net](https://azurewebsites.net) DNS suffix That's what happens automatically in Azure. That's not going to work because it's got to match the name in the certificate, so what I want to do here in the left-hand navigator in the properties of the web app is I want to go down to something called Custom domains. I need to add my custom domain name that will match what is in my certificate. The reality is if this is a public-facing web app in Azure for your customers, let's say, you would want it to reflect your custom DNS domain name, which normally reflects your government agency or your organization name, so I'm going to click Add custom domain, and what I want to do is specify here that the domain provider
>
> **[3:07](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-app-certificate-using-microsoft-azure?u=76281980&t=187)** is going to be all other domain services, meaning I don't want to register a new domain name here through Azure, and I'm going to add the TLS certificate later, so what's the domain name called? Well, this is where we have to make sure it matches what we were looking at in the key vault. You might recall that that is demotest.world, so we can use just a domain name like this. We don't have to have a www in front of it. We can, but it's not necessary. Here's the thing: Microsoft Azure is going to say, "All right, to prove that you own demotest.world, "we would like you to go into whatever tool you use "to manage your demotest.world DNS domain "and add an A record with these details "and add a TXT record with these details." Okay, so I'm going to go ahead and get started in my external DNS domain tool solution for this. I'm actually using this DNS domain name through GoDaddy, so I'm going to go there, so I've signed into my GoDaddy account. It could be any external DNS registrar that you use. It makes no difference. The point is I'm here where I can add records to my domain, so I'm going to add a new record. The first thing that [[Microsoft]] asked me to do was to add an A record, so I'm going to go ahead and add an A record with the details that were specified in the Microsoft Azure portal. I'll click Save once I've entered the values, but I want to add my second DNS TXT record, so if we take a look here,
>
> **[4:40](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-app-certificate-using-microsoft-azure?u=76281980&t=280)** let's just close some of these little messages, if we take a look further down, indeed, we have now got our A record added according to Microsoft's instructions. I'll click Add New Record. Now we're going to add the TXT record according to the details supplied by Microsoft, and I'll save that DNS record. Okay, the A record and the TXT record have been added, so if I flip back to the Azure portal, where we were adding our custom domain, and we saw the instructions for adding those two records, what we now want to do is we want to click the validate button. That means that it's going to go out and check that we've added those records, and if we were able to add those records with those specific values, we must own the demotest.world DNS domain, so I'll click Validate. After a moment, we get check marks. Excellent. We are good to go, so now what we would do is click Add. When we click Add, it will then add our custom domain name here in our web app, although it says No binding, so what we have to do is add a binding. I'm going to click Add binding. Now, what we can do is we can specify the source for our certificate, which is going to be to import it from a key vault, so at this point, I would choose Select key vault certificate. I would specify the Key vault name, and there's our certificate in that vault called MyCert, so I'm going to go ahead and click on Select. That brings me to this screen to add the TLS binding
>
> **[6:14](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/acquire-a-web-app-certificate-using-microsoft-azure?u=76281980&t=374)** where I will click Validate, and then I will click Add. Okay, and now our demotest.world custom DNS domain name is showing as having a secured connection because we imported the certificate from the key vault, so if I navigate to a web browser and connect to either demotest.world, or if you have DNS entries for things like www.demotest.world, when you make the connection over HTTPS, it will make the connection, but your browser will say the connection is not private. This is okay, and it's expected. This is happening because we have a self-signed PKI certificate that is not trusted by the browser. Of course, we now know that we can install the trusted root certificate for that certificate issuer so that we wouldn't get this type of message, and when you're dealing with self-signed certificates and using those certificates to secure internal line of business apps, for instance, it's important to install that trusted root certificate.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure]] (7), [[Microsoft Azure|Azure]] (5), [[Microsoft]] (3), [[Cloud Computing]] (1)
> **Env Vars:** dns (11), tls (4), txt (4), https (3), pki (1)
> **UI Navigation:** click on (4), go to (1), scroll down (1), navigate to (1)
> **Definitions:** is a  (3), in other words (1), means that (1)
> **CLI Commands:** make (3)
> **Tools:** azure portal (3)
> **Prerequisites:** install (2), you need to have (1)
> **URLs:** [azurewebsites.net](https://azurewebsites.net) (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/next-steps?u=76281980&t=0)** - [Instructor] Okay, now that we've completed the TLS for Beginners Securing Network Communications course, where do you move on from here? What are the next steps? There are a number of recommendations I would offer. One of those would be to think about other tools like [[Microsoft]] [[Active Directory]] Group Policy if your environment uses Microsoft Active Directory. Group Policy contains thousands of settings where you can centrally configure those settings to be applied to some or all devices joined to the domain. So I would focus on TLS and certificate options available there as an example, using Group Policy as a way to automatically install a Trusted Root Certificate on client devices for a private CA. And then definitely experiment with additional certificate templates and their settings. Depending on the tool you're using will determine if you can do that. But if, for example, you're using Active Directory Certificate Services integrated with Active Directory, you will have the Certificate Templates management console available. So that's something else to think about. By all means, consider working with other PKI [[Microsoft Products|products]]. There are plenty out there, whether you're working with private or public CA's, whether it's a mobile device management or MDM tool, with certificate options, whether it's a [[Configuration Management]] tool or suite to be used in the enterprise, it will normally have options available for either generating, managing, or deploying certificates.
>
> **[1:36](https://www.linkedin.com/learning/tls-for-beginners-securing-network-communications/next-steps?u=76281980&t=96)** We should also think about when it comes to certificate deployment options for automating the deployment of certificates to mobile devices, or maybe to allow the auto-enrollment of computers joined to an active directory domain. And finally, because our focus here was on transport layer security or TLS, this is a [[Network Security]] protocol suite. It changes over time. We need to keep up-to-date with things that are changing with TLS. One thing to watch out for would be version 1.4 when it becomes publicly available. We should also subscribe to various security feeds that track any vulnerabilities related to TLS. You can search in your favorite web browser search engine for TLS News, TLS vulnerabilities to subscribe to those types of feeds.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Active Directory]] (5), [[Microsoft]] (2), [[Microsoft Products|Products]] (1), [[Configuration Management]] (1), [[Network Security]] (1)
> **Env Vars:** tls (7), pki (1), mdm (1)
> **Prerequisites:** configure (1), install (1)
> **Versions:** version 1 (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Warnings:** watch out (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [[Daniel Lachance]]

## Path Context

### In [[Become an IT Security Specialist]]
← [[IT Security Foundations- Network Security]] | **5 of 12** | [[Cybersecurity with Cloud Computing]] →

### In [[Cybersecurity Fundamentals]]
← [[Introduction to Identity and Access Management]] | **10 of 12** | [[Cybersecurity with Cloud Computing]] →

## Appears In

- [[Become an IT Security Specialist]]
- [[Cybersecurity Fundamentals]]

---

[↑ Back to top](#)