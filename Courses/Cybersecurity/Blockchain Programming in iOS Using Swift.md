---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: blockchain-programming-in-ios-using-swift
url: "https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift"
duration_minutes: 135
duration: 2h 15m
level: Intermediate
updated: 10/25/2018
learners: 5294
skills:
  - Blockchain Architecture
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C4E0DAQFY4lEYIqLylw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1568669971786?e=2147483647&amp;v=beta&amp;t=kUXzB_ZVnUizTLMa96s2x31NPjsgRbIMoQBI4zjWkgE"
linkedin_topic: Cybersecurity
learning_paths:
  - '[Become a Blockchain Developer](../../Paths/Cybersecurity/Learning%20Paths/Become%20a%20Blockchain%20Developer.md)'
prev_courses:
  - '[Blockchain- Learning Solidity](Blockchain-%20Learning%20Solidity.md)'
path_nav: '[{"path":"Become a Blockchain Developer","position":14,"total":14,"prev":"Blockchain- Learning Solidity","next":null}]'
path_count: 1
tags:
  - course
  - topic/security
  - topic/data-science
  - skill/blockchain-architecture
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Cybersecurity/Blockchain%20Programming%20in%20iOS%20Using%20Swift.md)

![Blockchain Programming in iOS Using Swift](https://media.licdn.com/dms/image/v2/C4E0DAQFY4lEYIqLylw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1568669971786?e=2147483647&amp;v=beta&amp;t=kUXzB_ZVnUizTLMa96s2x31NPjsgRbIMoQBI4zjWkgE)

# Blockchain Programming in iOS Using Swift

> Blockchain enables decentralized encryption of sensitive data. Once the domain of the dark web, the power of the blockchain can now be harnessed by everyday developers. This training course explains how to implement blockchain-based storage and encryption in the iOS framework using Swift. Instructor Mohammad Azam begins with the very basics, explaining all the details related to blockchain technol

> [LinkedIn Learning](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift) | 2h 15m | Intermediate | 5K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Welcome to Blockchain Programming in iOS**](#welcome-to-blockchain-programming-in-ios) (2 videos)
  - [Introduction](#introduction)
  - [What you should know](#what-you-should-know)
- [**1. Understand Blockchain Technology**](#1-understand-blockchain-technology) (6 videos)
  - [What is cryptocurrency?](#what-is-cryptocurrency)
  - [What is a centralized ledger?](#what-is-a-centralized-ledger)
  - [What is a distributed ledger?](#what-is-a-distributed-ledger)
  - [Public and private keys](#public-and-private-keys)
  - [What is mining?](#what-is-mining)
  - [The longest chain](#the-longest-chain)
- [**2. Implementing the Blockchain Core Engine**](#2-implementing-the-blockchain-core-engine) (5 videos)
  - [Developing blockchain models](#developing-blockchain-models)
  - [Generating keys for blocks](#generating-keys-for-blocks)
  - [Initializing the blockchain](#initializing-the-blockchain)
  - [Proof of work](#proof-of-work)
  - [Adding blocks to the blockchain](#adding-blocks-to-the-blockchain)
- [**3. Smart Contracts**](#3-smart-contracts) (2 videos)
  - [Understanding smart contracts](#understanding-smart-contracts)
  - [Implementing smart contracts](#implementing-smart-contracts)
- [**4. Implementing Blockchain Web API**](#4-implementing-blockchain-web-api) (5 videos)
  - [Prerequisites](#prerequisites)
  - [Setting up a server-side Swift project using Vapor](#setting-up-a-server-side-swift-project-using-vapor)
  - [Setting up a controller and services](#setting-up-a-controller-and-services)
  - [Returning the blockchain](#returning-the-blockchain)
  - [Mining a block](#mining-a-block)
- [**5. Decentralizing Blockchain**](#5-decentralizing-blockchain) (3 videos)
  - [Creating node models](#creating-node-models)
  - [Mining on nodes](#mining-on-nodes)
  - [Resolving conflicts](#resolving-conflicts)

### Welcome to Blockchain Programming in iOS

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/introduction?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/introduction?u=76281980&t=0)** - Hello and welcome back. Before we dive into the [Blockchain](../../Skills/Data%20Science/Blockchain.md) technology I'd like to give you a small introduction. My name is Mohammad Azam and I've been doing iOS since 2010. I worked as a lead mobile developer for many Fortune 500 companies. Apart from that, I do have dozens of apps on the App Store include Vegetable Gardening and Vegetable Tree which were featured by Apple. I have been teaching on Udemy for at least three to four years and I have a lot of courses on Udemy including the best-selling courses On Mastering ARKit and also Core ML for iOS. I also do a lot of teaching engagements in the iOS community. Actually, in April I'll be talking at the Blockchain Dev Conference in San Francisco. So if you're in San Francisco, stop by and say hi. Apart from that, I have spoken at Indie Dev Start 360|iDev, MS Coder in Mexico City and a lot more. I work full-time as a full-time instructor for Digital Crafts, which is bootcamp company. So basically, I go and teach people, that's my full-time job. Before joining that, I was a lead instructor for Highland Yard where I was teaching iOS as well as full-stack [Web Development](../../Topics/Web%20Development.md). So, I have a lot of teaching background.
>
> **[1:35](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/introduction?u=76281980&t=95)** I really hope that you enjoy this course and if you do have any questions don't hesitate to contact me and I will be more than happy to assist you further in your quest for learning Blockchain Technology in iOS Using [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md). Thank you so much and I really hope that you enjoy the course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (3), [Web Development](../../Topics/Web%20Development.md) (1), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Code Identifiers:** ios (5), idev (1)
> **Speakers:** - hello (1)

#### [What you should know](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:02](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-you-should-know?u=76281980&t=2)** - [Instructor] Before we go into the core, the actual implementation and learning about how to implement the core engine, as well as the Web API, I think it'd be very, very important to know that what you should know about before going into the course. You should have some idea of the iOS app architecture. This is a not a beginner level course. This course does not teach you iOS [Application Development](../../Skills/Software%20Development/Application%20Development.md). This is focused entirely on how you can implement [Blockchain](../../Skills/Data%20Science/Blockchain.md) using iOS, using [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md). Familiarity with the Swift language is also necessary. I would actually even say that intermediate to advanced level of Swift is required to go further in this course, to actually go through the course. We will be using a server side Swift framework, and I will be using Vapor framework, which is extremely popular in the Swift community, in the iOS community. Knowledge of the Vapor framework, knowledge of the server side Swift framework is also required for you to learn about the blockchain programming and how this course is progressed. If you don't have any knowledge of the server side Swift framework, then you can still continue with the course, keeping in mind that this course is not about teaching you the server side Swift framework using Vapor, but this is focused entirely on the blockchain technology and how you can implement blockchain
>
> **[1:38](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-you-should-know?u=76281980&t=98)** using iOS and using the Swift language. With that said, let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (9), [Blockchain](../../Skills/Data%20Science/Blockchain.md) (4), [Application Development](../../Skills/Software%20Development/Application%20Development.md) (1)
> **Code Identifiers:** ios (5)
> **Env Vars:** api (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 1. Understand Blockchain Technology

[↑ Back to Table of Contents](#table-of-contents)

#### [What is cryptocurrency?](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-cryptocurrency?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-cryptocurrency?u=76281980&t=0)** - [Instructor] Before we get into [Blockchain](../../Skills/Data%20Science/Blockchain.md), it is very important to understand cryptocurrencies. What exactly is [Cryptocurrency](../../Skills/Data%20Science/Cryptocurrency.md)? You might have heard of different cryptocurrencies, like Bitcoin, Ether, and even Ripple, but there are a lot more cryptocurrencies out there, Waves, Nexus, Dash, Litecoin, and so on. Cryptocurrencies are basically digital currencies, meaning, unlike the dollar bill, which you can actually hold in your hand, a physical object, cryptocurrencies are all digital, meaning, it's just bits and bits and ones and zeroes. Now, cryptocurrencies are also decentralized currencies, meaning, you can simply send them over the internet to anyone in any part of the world. No one controls cryptocurrencies. So it's independent and there is no one who is controlling or generating these currencies. Cryptocurrencies and especially Bitcoin, which is the most famous cryptocurrency, was originally developed by someone named Satoshi Nakamoto. Now, we don't really know if this is an individual person or a group of people, we don't know, and no one knows.
>
> **[1:35](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-cryptocurrency?u=76281980&t=95)** And maybe it's a good idea now that no one knows at this point. Satoshi Nakamoto developed and published a whitepaper on a Bitcoin cryptocurrency, which is a peer-to-peer electronic cash system. There are 21 million bitcoins available, meaning, that after 21 million, well, there's won't be any. That's the limit of bitcoins, 21 million bitcoins. If you want to buy bitcoin, then you can go to different digital exchanges like Coinbase, and go ahead and sign up and buy bitcoins and other digital currencies, including ether, as well as litecoin. We're not really interested in Bitcoin cryptocurrency, but it's very, very important to understand that the technology being used behind the bitcoin is called blockchain. And that is what the course is all about. Now, let's go ahead and look at how the blockchain technology works and how it can be used in different aspects of our life.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Cryptocurrency](../../Skills/Data%20Science/Cryptocurrency.md) (4), [Blockchain](../../Skills/Data%20Science/Blockchain.md) (3)
> **Definitions:** is an  (1), is a  (1), is called (1)
> **Speakers:** - [instructor] (1)

#### [What is a centralized ledger?](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-a-centralized-ledger?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-a-centralized-ledger?u=76281980&t=1)** - [Instructor] Let's say that [John](../../Glossary/Tool/John%20the%20Ripper.md) needs to send some money to Mary. And let's also assume that John and Mary are not neighbors but they live quite far apart, maybe thousands of miles apart. Now the current way of sending the money from John to Mary includes a middleman. Now that middleman can be a bank. John is going to send the money to the bank. The bank is going to verify that Mary actually exists and then the bank is going to send the money to Mary. Now John may be sending $20 and Mary is going to get $18. You might be wondering what happened to those $2. Well that's the cut that the bank is going to charge for the transaction fees. Not only the high transaction fees are the problem with this approach, but also the amount of time the money went from John to Mary. It actually might take three to five business days and that's just too late. Apart from the money, the transaction fee, the three to five business days now you have a central place, which is a bank, and if there are bad people like hackers they only have to hack one bank or one central place to get all the information. Once these hackers hack the bank, or that particular central ledger, they can get access to all the accounts,
>
> **[1:35](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-a-centralized-ledger?u=76281980&t=95)** all the Social Security identification, their addresses, first name, last name, bank account number, [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) numbers and so on. And they can just take the money out of bank and John's account and Mary's account and transfer it to some offshore account. This is not a fantasy world scenario or some sort of a scenario that cannot happen because this has happened a lot of times. We have a lot of examples where the hackers have hacked the central location, the central ledger, and simply taken out the money out of the bank from different people's account and transferred it to their own accounts which cannot be traced. So this is the main idea of a central ledger. Now let's go ahead and see what a distributed ledger looks like.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [John](../../Glossary/Tool/John%20the%20Ripper.md) (7), [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [What is a distributed ledger?](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-a-distributed-ledger?u=76281980)

> [!transcript]- Transcript
>
> **[0:02](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-a-distributed-ledger?u=76281980&t=2)** - [Instructor] Let's say that [John](../../Glossary/Tool/John%20the%20Ripper.md) needs to send some money to Mary. And once again, we will assume that they are not next door neighbors, but they live thousands of miles apart. In this case, we are not really going to use a middleman, or a central ledger approach, which we used the last time. Instead of that, we will use different nodes. Now, you can think of these nodes as different servers which people are running. These are the volunteers, they are running these nodes. Each node has a copy of a ledger, which can record a transaction. So if John is going to say that, hey, I want to send $20 to Mary, all of the ledgers on different nodes, and these can be thousands of nodes, gets updated with that particular transaction, saying that John wants to send $20 to Mary. Now, this is not only limited to John, because a different person, like Steve, can come in. And he say, you know what, send $5 to John. And all the ledgers get updated, saying that sent $5 to John. The same way Mary can also say, send $15 to Steve. Once again, all the different ledgers on all the different nodes, and there can be thousands of different nodes, get updated. Now, what happens if Steve is not a good person,
>
> **[1:39](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-a-distributed-ledger?u=76281980&t=99)** and Steve is like, I want to change something in one of the ledgers, so he hacks one of those nodes, the server, and changes a transaction and say send, instead of $15 from Mary to Steve, to $115.
>
> **[2:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-a-distributed-ledger?u=76281980&t=120)** Now, the great thing about these nodes is that they are on a peer-to-peer network, so all of these nodes are actually connected to each other. And when one node makes a change, it is validated against all the other nodes. So all the other nodes are going to verify that their ledger is the updated one. And now you can see that there are three nodes which say something different than the one top-right node, which say it's a completely different thing. So we will ignore that node, we will ignore that particular ledger or that transaction, because something wrong with that. All the three nodes have a different copy of the transaction, and one of those nodes has a different copy. Since three in a democracy will win over one, we will keep the copy which does three have, because that might be the correct copy. Now of course, I'm making this super simple. A lot of different things go on behind the scene. A ledger is not really displaying you the name of the people who are sending money from and to. And that is exactly what we are going to learn in the next lecture. We're going to learn that what the ledger looks like, and how the ledger does not really include the name of the people, but actually the ID of the people that is generated and assigned to them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [John](../../Glossary/Tool/John%20the%20Ripper.md) (6)
> **CLI Commands:** node (4)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Public and private keys](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/public-and-private-keys?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/public-and-private-keys?u=76281980&t=0)** - [Narrator] Let's go ahead and talk about public and private keys. We already know that the ledger consists of transactions from the users. Now these transactions can be send $20 from [John](../../Glossary/Tool/John%20the%20Ripper.md) to Mary, send $15 from Mary to Steve, and so on. Each of these transaction is signed by the person who is approving these transaction like John, Steve, and Mary. These signatures are called digital signatures. In order to create a digital signature you must have a private key as well as a public key. Now a public key as well as a private key is a 256 hash keys. That means that these bits, zeros and ones, can run up to 256 characters. When you need to sign a message of a transaction, you use the formula. You pass in the message which is the actual transaction and you sign it with your own private key which creates a signature. Now remember not to share your private key with anyone. That's why it's called a private key so keep it safe. Now if you need to verify that it is a legitimate signature what you need is a message, the signature, and the public key and then you can verify whether that particular transaction was correct or not.
>
> **[1:37](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/public-and-private-keys?u=76281980&t=97)** So in the ledger, if we have a transaction from Mary say, sending $15 from Mary to Steve, what happens if Steve is not a good person and although Steve can not fake Mary's digital signature, what if Steve duplicates it? That's where the transaction IDs come into play. Each of the key and message also consist of the transaction ID which makes it unique. Now you might be thinking is digital signature secure? Well digital signature is created using a cryptographic function which is called SHA256. That's 2 to the power of 256. Let's take a look at the very simple example. We're not gonna go into the details of SHA256, how it actually works, but let's consider this is Blackbox. If you pass something to that hash function, let's say Mary, it's gonna create 256 lines of ones and zeros. Now although it looks very random but they are not random. And the reason I'm saying that it's not random, is that if you pass Mary again it's gonna produce the same combinations of ones and zeros. But if you change just a little bit from Mary to Barry and pass it to the SHA256 has function,
>
> **[3:14](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/public-and-private-keys?u=76281980&t=194)** it's gonna create a completely different hash. You see that the hash of Mary is very different from Barry. That's 2 to the power of 256. That's particularly like saying that if I select one dot from the screen, and I selected 256 times, then you have to pick the same exact dot 256 times. So the [Probability](../../Skills/Data%20Science/Probability.md) of doing that is well close to none. We already know that the ledger is handled and is contained by different nodes. And as a mentioned previously, that these nodes are not like four or five, but these can be thousands and thousands of nodes. So which ledger should we pick? Which is the correct one? There are so many nodes, keeping track of all these transaction, one of them we have to pick. That is the leading node. And that is exactly what we are gonna learn in our next lecture, the concept of proof of work. So stay tuned for that. Thank you so much

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [John](../../Glossary/Tool/John%20the%20Ripper.md) (2), [Probability](../../Skills/Data%20Science/Probability.md) (1)
> **Definitions:** is a  (3), means that (1), is called (1)
> **Env Vars:** sha256 (3)
> **CLI Commands:** node (1)
> **Speakers:** - [narrator] (1)

#### [What is mining?](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-mining?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-mining?u=76281980&t=0)** - [Instructor] Hi and welcome back. Now in this lecture we'll be talking about mining. What exactly is mining, and what are the different concepts behind mining? We already know that we have different servers, which are known as nodes, and each node has a copy of the ledger. Now, let's remove the ledger and talk about the transaction first because this is how it actually happens. A transaction comes to a node, which can be any transactions, like send $20 from [John](../../Glossary/Tool/John%20the%20Ripper.md) to Mary, send $5 from Steve to John, and so on. Whenever a transaction comes, each of these nodes they start doing a very, very complicated computation. Now, that computation is known as Proof of Work. Now, what exactly is proof of work, is that they have to find the hash that let's say begins with 30 zeros. Now I'm just taking that number 30 as an example. The number of zeros that they have to find in the prefix of the hash it keeps on changing, so it might be 30 right now. It might be 60, 47, 52, it keeps going up as the time goes by, so it gets more and more complicated. Just to give you an idea, if you try to find out, and we'll see later on,
>
> **[1:33](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-mining?u=76281980&t=93)** a hash with the four zeros instead of the 30, your computer is gonna take a very, very, very long time. So finding 30, finding 60 zeros, that special hash is pretty complicated. Basically it's not really possible to do it any other way. You just have to kind of guess. A guess is depends on the transaction, so let's say if I'm saying send $5 from Mary to Steve, so that will be the transaction. But what exactly is a nonce? Well as you have already learned, that each single thing, transaction, or a letter, will create a very different hash. It's not random because if you pass in the same string it's gonna produce the same hash. So what we do is we take the transaction, and we increment the nonce. It's just a incrementing number. And basically, this is where our search begins. So we take the transaction, which let's say is send $5 from Mary to Steve, and then we add one to it. And then we check, have we achieved 30 zeros in the front, at the start of the hash? And you can see that we have not. Well, let's go ahead and try out nonce 2. And you can still see that we haven't really achieved that. And this process will go on forever and ever and ever until you reach the number that can produce
>
> **[3:13](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-mining?u=76281980&t=193)** 30 zeros in the front or 60 zeros, whichever complexity you're working with. And maybe you'll be able to find it. And that particular large number, if you increment it and you add the transaction to it, you will be able to find the nonce. I mean, the hash. Whoever node, whoever server, finds that particular hash, a special hash that is containing 30 first zeros, well that gets a ledger, which is also known as a block, and we'll be referring it to a block from now on. And then that particular node will be allowed to add transactions to that block. So as you can see the block on the bottom left was able to find the special hash, which began with 30 zeros, so it was given a block and then all the transactions that were waiting were added to this block. But now at this point, you might be wondering that, why would a server, why would a node do all these computations? I mean, that looks like very complicated. It might take a while, and it might use a lot of electricity also. And you are correct. It does actually use a lot of electricity. So why are these nodes trying to find that special hash? The special hash that begins with 30 zeros. Well that's because every time they find it
>
> **[4:48](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-mining?u=76281980&t=288)** they get a reward. Initially they started getting 25 bitcoins, but every 4 years it's been cutting down to half, so right now in 2018, if they find the special hash what begins with 30 zeros then they'll get a reward of 6.25 bitcoins, which depending on the time of the day that you look at the bitcoin value that's a pretty nice bonus. Once you have found the hash, a block is generated for you, and you put all the transactions in that block, and then that block becomes part of other blocks in a chain. And hence it's called a [Blockchain](../../Skills/Data%20Science/Blockchain.md). Once it is added to the chain, all the different nodes, and there can be thousands of different nodes, they get an updated copy of the blockchain. And now all these nodes, this might mean thousands and thousands, they have the latest copy of the blockchain, which contains all those transactions which are added in the block that was found before. And then the whole process kind of starts again. Some other transaction comes in and the nodes start again in their quest to find the hash that begins with 30 zeros. At this point, you might be wondering, "What if I can go back and just modify one of the blocks in the block chain?"
>
> **[6:23](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-mining?u=76281980&t=383)** Well remember when I said that the hash is computed by using a transaction plus the nonce, which is just an integer being updated to different values, a counter, but there's one more thing to it which is the hash of the next block will also contain the hash of the previous block. So let's go ahead and see it and example it. So if this is a block 0, which contains three transactions, and there's the hash with 30 zeros. If I want to find the block number one, then I will have to have a transaction plus nonce plus the previousHash. The transaction in this case are the transaction from the block number one. We will have to find the nonce on that, so let's just say the nonce is this huge number again, and then we have to also add in the hash of the previous block, which is this one from block number zero. And then when we do that we create a new hash that begins with a 30 zeros, of course that depends on the value of the nonce because that is how we are changing and just looking for that special 30 zeros hash value. But that is going to allow you to create the hash of the next block, which in this case is block number one. Now, what about if Steve
>
> **[7:58](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-mining?u=76281980&t=478)** on block number zero or John in this case, actually, on block number zero, wants to change one of the transactions, and instead of sending $5 from Steve to John, John is going to change it and say, "Go ahead and send $15 from Steve to John." Well remember when I said that even if you change one single letter the hash value will be completely different? So that hash is invalid, and if that hash is invalid, the next block is also invalid. The only way to get the value of the block number zero is to, well, mine again. And not only you have to mine the block number zero, but you will have to mine the block number one. And block number two, and three, and four, and five, and six, and seven, and hundred, and 200, and thousand, and so on. And even if you are able to mine, which is not possible, but let's just say that even if you are able to mine, we're only talking about one server, one node, and there are thousands of them. And all the other nodes are going to point to the one that is incorrect, and they're gonna say, "Hey, I think the blockchain copy that you are using is incorrect," because in the end it's just a democracy. There are lot more nodes that have a different ledger,
>
> **[9:31](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/what-is-mining?u=76281980&t=571)** that have a different copy of the block chain, and only one of those has been compromised on a server. In order to hack the whole blockchain system, you'd have to change or hack or alter 51% of the nodes, so 51% of the servers. That's a lot. And then you have to mine them all. So it becomes pretty much impossible to change the blockchain. So blockchain is immutable, and it cannot really be changed. Once anything is written into the blockchain it is there permanently. And now at this point, I think you already know everything you need to know. The concepts related to the blockchain, the mining, the public/private keys, and I think the next step in this journey is to just, let's get started to implement the code and see how it, all of this will look like in code. So if you're ready, let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (9), [John](../../Glossary/Tool/John%20the%20Ripper.md) (6)
> **CLI Commands:** find (12), node (6)
> **Definitions:** known as (3), is a  (2)
> **Code Identifiers:** previoushash (1)
> **Versions:** 6.25 (1)
> **Speakers:** - [instructor] (1)

#### [The longest chain](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/the-longest-chain?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/the-longest-chain?u=76281980&t=0)** - [Instructor] Hello and welcome back. Now, I know that you are really eager to get started, but I just have one more small thing that I need to cover before we can jump into the implementation of the [Blockchain](../../Skills/Data%20Science/Blockchain.md) in iOS. And this lecture is about the longest chain. Let's actually see what longest chain is all about. We already know that the blockchain is just a connected view of the blocks, so you have different blocks and they are connected together using the previous hash of the previous block, along with the transactions, along with the index, the nonce, and a bunch of other stuff. When all these blocks are connected together, they form a blockchain. But let's talk about the different nodes. So once the block is actually found and it has been validated by these nodes, that block then becomes part of the blockchain. But what happens if there is some evil node, the node that has been hacked and that is also creating, that's also finding that secret hash value, hence generating the block, what will then happen? Well, in those scenarios, the tree is going to diverge and it's going to form two different branches. One is going to be pointing to the wrong block, and one is going to be pointing to the correct one
>
> **[1:34](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/the-longest-chain?u=76281980&t=94)** that has been validated by other nodes. And at this time, the other nodes are going to validate and going to create another block that is going to add it to the blockchain. But so does the evil node, the hacked node. It's going to create another block and going to add it to the blockchain. Now obviously, I am making this so simple because it's not really easy to create and to win and to find that secret hash with 30 zeroes or 60 zeroes again and again. So it's the very, the [Probability](../../Skills/Data%20Science/Probability.md) of that hacked node, the evil node, to keep finding that hash again and again, is extremely low. Well, let's just assume that it's able to keep up at the start, but then all these nodes validate and they find another block and they add to the blockchain. Same does the evil node, it adds to the blockchain. But then, eventually the evil node is going to lose because all of those nodes, combined together, are creating more blocks and adding to the blockchain. Now, if you're wondering that which path to take, you will always go with the longest add. So you will ignore, you will cross out, like I have, you will cross out anything that you have listened from that evil node, and you will always take the longest node, which in this case is the block node,
>
> **[3:10](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/the-longest-chain?u=76281980&t=190)** or the blockchain node, which is of the nodes that have validated it correctly. And that's the concept of the longest chain.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (10), [Probability](../../Skills/Data%20Science/Probability.md) (1)
> **CLI Commands:** node (12), find (2)
> **Code Identifiers:** ios (1)
> **Speakers:** - [instructor] (1)


### 2. Implementing the Blockchain Core Engine

[↑ Back to Table of Contents](#table-of-contents)

#### [Developing blockchain models](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/developing-blockchain-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/developing-blockchain-models?u=76281980&t=0)** - [Instructor] In this lecture, we will be implementing the models that will be responsible for creating our [Blockchain](../../Skills/Data%20Science/Blockchain.md). So we'll be looking at the transaction model, the blockchain model, as well as a block model. Now, I'm going to create all of these things in [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) Playgrounds for macOS. Now, why am I using macOS and not iOS? Well, there are some helpful tools as well as APIs that we can use to create the SH, sha, basically, SHA-256 hash, which is available in macOS, but we'll have to do a little bit extra to do it on iOS, which we'll see later on. So let's go ahead and start [Xcode](../../Skills/Mobile%20Development/Xcode.md), and go ahead and say New, Playground. And you can see that you can also create a playground for macOS, so that's what I'm gonna select, instead of the iOS, so go ahead and select macOS, and then just select a blank, empty playground. I'm going to call this Blockchain, and I'm going to put it in a lecture. Let's go ahead and, over here. Okay, so there we go. So we have a playground macOS,
>
> **[1:37](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/developing-blockchain-models?u=76281980&t=97)** and currently it doesn't really have anything. So we have to create classes that corresponds to the transaction, that corresponds to a block, and that corresponds to the blockchain. Let's start with transaction. So Transaction class is responsible for, well, transactions. So we can have a property, which is, transaction is coming from. We can have a property to, which is where the transaction is actually going. And we can have a mount, which will represent what is the amount of the transaction. Now, let's go ahead and also create the initializer, which will consist of these fields. So from, to, and also the amount, which in this case is double. Now we can actually initialize our properties from the past in variables, from the initializer, to is to, and amount is amount. Okay, so this is our first model, a basic, simple model representing a transaction. Now, the next model that you're gonna create will represent a block. And if you remember from the past lectures, a block can have multiple transactions. So we'll create a block class called Block.
>
> **[3:14](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/developing-blockchain-models?u=76281980&t=194)** And we will start with a couple of different things. We still start with the index of the block. Now, the index of the block is the position of the block in the blockchain. So if it is the first block in the blockchain, the index will be zero, if it is the 10th block in the blockchain, it will be, index will be according to that. The other one is the previous hash, which, we're gonna initialize it to empty for right now. So the block one will have, the block one hash will be created using the previous hash, plus a transaction, plus a nonce, plus the index of block zero, and so on, which we're gonna see later on. And this is the actual hash of the block. And finally, the nonce, which is an integer also. Now, some of these things are actually not initialized at this point, so let's go ahead and create an initializer. So init, and go ahead and initialize at least nonce to be zero. Everything else should be okay because we have initialized the index, the previous hash, the hash, well, we'll get to it when we get to it, and then nonce. The hash will be created, you know, when we run the proof algorithm, and which will create the hash value for it.
>
> **[4:55](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/developing-blockchain-models?u=76281980&t=295)** So what else is missing? We can also have the transactions in the block. So let's go ahead and add the transactions, private (set) var transactions. As you remember, a single block can have multiple transactions, so that's why the transactions is an array, or a list.
>
> **[5:24](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/developing-blockchain-models?u=76281980&t=324)** There we go, now we can actually talk about the actual blockchain that we want to implement. So let's go ahead and implement the class for the actual blockchain, Blockchain. Now, what will the class have, the blockchain class? Well, a blockchain class definitely have a list of blocks, so let's go ahead and create a list of blocks. And initially, I'm just gonna say that the list of blocks is an empty array, 'cause it doesn't really have any blocks. For now, we are going to leave it at this particular point because, in order to add the block in the blockchain, we will have to find the magical hash. And in order to find the magical hash, we will have to run the proof of our algorithm, which we're gonna see later on when we reach that particular point. Okay, so this will at least give you an idea of what we will be doing later on. We will be dealing with a transaction, we will be dealing with a block, and we will be dealing with the blockchain. Right now, we have simply created a very skeleton-like blueprint architecture of the models that we will be using in our application.
>
> **[6:58](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/developing-blockchain-models?u=76281980&t=418)** We still have to do a lot of work to get the blocks with the transactions into the blockchain, and that is exactly what our mission is. Let's continue that in the next lecture, thank you so much.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (15), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1), [Xcode](../../Skills/Mobile%20Development/Xcode.md) (1)
> **Code Identifiers:** macos (6), ios (3)
> **Definitions:** is an  (3)
> **CLI Commands:** find (2)
> **Env Vars:** sha (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Generating keys for blocks](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/generating-keys-for-blocks?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/generating-keys-for-blocks?u=76281980&t=0)** - Now that we have all our models set up correctly, it's time to generate the key which will be used to generate the hash. Okay, and if you remember from our earlier lectures, the key is basically the transactions, plus the hash of the previous one, plus the nonce, and plus the index. and plus the index. and plus the index. We can also add the index to it, which represents the position of the block inside the [Blockchain](../../Skills/Data%20Science/Blockchain.md). So let's go ahead and create a key. We're going to start with creating a key property which only has a getter. So what can we actually return? So return, string, self, dot, index so we can convert the index to a string. That is not a big deal. We can convert it, the previous hash in the string, which is already a string. And we can also convert the nons to a string using the string function. Or what about the transactions? I mean I can't simply say something like this because it won't really make any sense. Transactions is a list of transaction object. Let's actually move this properly over here. So how can we convert the transactions into a string
>
> **[1:37](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/generating-keys-for-blocks?u=76281980&t=97)** so we can add it to all of these different properties to form a key? And on that key, using that key, the hash we create, or start generating, new hash is the SHA-256. new hash is the SHA-256. Now this is not gonna work because self dot transactions is a transaction array. So we can't convert the string- So we can't convert the string- uh, the array of transaction objects into a string. We have to find a different way. How about if we can actually convert these transactions into [JSON](../../Skills/Web%20Development/JSON.md) and then we add the JSON string over here. The easiest way to convert the transaction object to JSON is to use [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) 4 feature, is to use Swift 4 feature, which is called a Codable. So we will just say that this is Codable. Codable means that it can be finalized into JSON objects. Now, when we have done that, we can actually move on to actually converting our transactions, which is an array of transaction objects, into a JSON string. So let's start with first converting it into a transactionsData. We're gonna use the JSON importer which is also
>
> **[3:10](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/generating-keys-for-blocks?u=76281980&t=190)** part of Swift 4. And we're gonna encode the value, which can be encodable, which in this case is transactions. And this is going to give us the transactions data which is of type data. But we can't really add data to a string. So let's go ahead and convert it to a string. Transactions, JSON, string. Transactions, JSON, string. And then we're gonna use the string initializer passing in the data, which in this case is transactionsData. And we're gonna use encoding, which in this case is utf8. This is going to convert our transactions data, which is coming from the self dot transactions, which is a list of transaction objects, and finally is gonna give us the transactions string which we can try to add it to our transactions which we can try to add it to our transactions who found the key. Now sometimes you will see that- Let's see actually, what's going on over here. I don't think it should- Transactions screen, okay. Uh, let's go over here. Actually we don't even need that bar. Let's close it. Might have to unwrap it, so that might be causing the issue. So now we have created a key which depends
>
> **[4:43](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/generating-keys-for-blocks?u=76281980&t=283)** on the index of the block, the previous hash of the block, the previous hash of the block, the nonce value, as well as a transaction in the JSON format. So if you want to see how it will look like, we are going to have to add somehow a transaction so that we can work on it. Let's see if we can even use a block, create an instance, and see if it can actually give us some sort of uh, idea of what our queue look like. So if I right now say block one dot key, you can see that the block one dot key kinda starts like this. But we don't really have any transactions. Now we can go ahead and add a function to add our transactions, so let's go ahead and do that. Add transaction, and we're gonna pass in a transaction which is of type transaction. And when we're gonna get that we're simply going to add it, or append it, to the array. Alright? But now, we have to actually create a transaction before we can do that. So let's go ahead and create a transaction. Transaction from- Let's say it is Mary who is sending money to Steve of amount of 20 dollars.
>
> **[6:18](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/generating-keys-for-blocks?u=76281980&t=378)** Once we've created the block we can actually go ahead and add the transaction, so let's go ahead and say add transaction. And I was hoping that it would print it out- Here we go! Actually it took a while. But now this is a key that has been generated for that particular block one. Now this is not the hash value because the hash value that we're looking for should start with, like, well it has to be a hash value, not a string like this, first of all. And it will, let's say, start with like three zeros or something. Alright. But at this point we have actually created the code which will return us a key which can be used to create a hash which we're going to see later on. So stay tuned, don't go anywhere, it's gonna get really cool.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (9), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3), [Blockchain](../../Skills/Data%20Science/Blockchain.md) (1)
> **Env Vars:** json (9), sha (2)
> **Definitions:** is a  (4), is called (1), means that (1), is an  (1)
> **CLI Commands:** make (1), find (1)
> **Code Identifiers:** transactionsdata (2)
> **Best Practices:** the key is (1)
> **Prerequisites:** set up (1)
> **Speakers:** - now (1)

#### [Initializing the blockchain](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/initializing-the-blockchain?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/initializing-the-blockchain?u=76281980&t=0)** - [Instructor] Okay, so let's continue with our quest of building the core framework for our [Blockchain](../../Skills/Data%20Science/Blockchain.md) API. Right now we have a Blockchain class. We also have a block class which represents single, or each block in the Blockchain, and we also have a transaction class which represents many transactions that can be added to the block. And these transactions can be: Mary is sending twenty dollars to [John](../../Glossary/Tool/John%20the%20Ripper.md), Steve is sending ten dollars to Mary and so on. Now one thing to note about the Blockchain is that the first block in the Blockchain is called the Genesis Block. And that block contains the rewards for the miners. So when you create a Blockchain it will have the Genesis Block, which contains a reward, of course. So let's go ahead and create our Genesis Block. Which it's going to serve as the first block at index zero inside the Blockchain. Once we have that, we need to create the Blockchain, but right now the Blockchain, well, doesn't really have any Initializers, so let's go ahead and create one. The Initializer is gonna take in the Genesis Block, which is off time block, and it's gonna call a matter called, addBlock, where we're gonna just pass in the Genesis Block.
>
> **[1:39](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/initializing-the-blockchain?u=76281980&t=99)** Now there is no matter called addBlock. So, we have to implement that. So let's go ahead and implement our addBlock method. We're gonna use the addBlock, we're gonna get the block, and now we can start adding the block to the Blockchain. But first we need to check if this is the first block we are adding, or if this is not. So, we'll check that if this is the, if it is empty, then this means that we are adding the first block into the Blockchain. But what will be the previous hash if this is the first block? For the sake of simplicity, we are going to assign the previous block, or the previous hash, at like, all zeros. But we still need to compute the actual hash of the Genesis Block, or the first block. So, we are gonna call our generate hash function. Don't worry too much because this function does not really exist at this point. So, let's comment it out for the time being. Alright, so now if we create our Genesis Block, we can go ahead and also create our Blockchain. The Blockchain is dependent that we pass in the Genesis Block, so, let's go ahead and do that. And now our Blockchain is initialized with one block. Make sure to also add the block into the Blockchain.
>
> **[3:16](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/initializing-the-blockchain?u=76281980&t=196)** Which we haven't really done that over here. So, lets go ahead and say: blocks dot append the new block, which in this case is: block. Now, you might be wondering, what does the generate hash function look like? So, let's go ahead and implement the generate hash function, which is responsible for creating the hash for that particular block. And in this case, it is the Genesis Block. So, generate the hash, for the block, and it's a return, us,
>
> **[3:59](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/initializing-the-blockchain?u=76281980&t=239)** the string, which will represent the hash value.
>
> **[4:08](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/initializing-the-blockchain?u=76281980&t=248)** Now, Var hash equals to block dot key, remember the key that we setup? We get a combination of transaction, plus nouns, plus index, and so on. That's gonna come into play. And, S-H-A-1 Hash. Now, this function obviously does not exist, and it definitely does not exist on the string, which is the type for the key. Don't worry too much about the Sha1Hash function because we have actually copied and pasted it. So, Let's we go ahead and do that. I'm gonna go right at the end, maybe, over here, and copy/paste that method. I wouldn't worry that much about this method at this point. Okay? This method, which is the Sha1Hash uses some of the built-in libraries in the Mac OS framework, or the Mac OS system, to create the hash. Now, if you are using [JavaScript](../../Skills/Software%20Development/JavaScript.md) or [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), there might be some other functions in those languages that you can use. So, now we have the hash, and we can return the hash.
>
> **[5:42](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/initializing-the-blockchain?u=76281980&t=342)** Okay? And we will, him, also append the block that we have passed in, into the block's array. Let's go ahead, and also call block dot hash, which is going to generate a hash. And on the right-hand side you can see that it has actually generated a hash for the value that you are passing. Which depends on the transaction, the, right now it only depends on the transaction, and let's see, it depends on the transaction, the index, the previous hash, and the nouns. But this hash is, well, it's not unique. It doesn't take that much time to create this kind of a hash. So, I cannot really reward you for creating this kind of hash. I mean, if this hash was starting with three zeros, then I would be like, okay yeah, it looks like you have put some work in it. That's called, The Proof of Work. But creating hashes like these, are very easy, as you can see. And they don't have anything special about these hashes. So we need to see that, how we can implement the proof of work, where the hash is going to be beginning with two, or three, or even four zeros. And then you will notice that how much time it takes to generate that kind of a hash. We're gonna check it out in the next lecture,
>
> **[7:15](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/initializing-the-blockchain?u=76281980&t=435)** so stay tuned for more. Thank you, so much.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (15), [John](../../Glossary/Tool/John%20the%20Ripper.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **Code Identifiers:** addblock (4)
> **CLI Commands:** make (1), python (1)
> **Definitions:** is called (1), means that (1)
> **Env Vars:** api (1)
> **Cross-References:** in the next (1)
> **Best Practices:** make sure to (1)
> **Prerequisites:** setup (1)

#### [Proof of work](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/proof-of-work?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/proof-of-work?u=76281980&t=0)** - [Instructor] Okay, so let's now go ahead and see how we can create a proof of work which is going to generate a special kind of a hash that is beginning with, let's start with two zeros. We're gonna go into the generate hash function and we're gonna run a while loop and this loop is gonna run for a very long time until the prefix of the hash it generates begins with two zeros. If it's not two zeros, then we're simply going to increase the nonce by one.
>
> **[0:40](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/proof-of-work?u=76281980&t=40)** And we're gonna give the hash equals two block.key.shahash which is going to give you something different because now the nonce has been increased. Let's go ahead and also print it out.
>
> **[1:01](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/proof-of-work?u=76281980&t=61)** Okay. So there we go. You can actually see that it's trying to find a hash, a unique hash that begins with at least two zeros. And this eventually is able to find it. There we go, right at the end. Zero zero seven three see line b, so on. This is also printed over here. Now as you can see it took few seconds. Now if I was finding a hash that begins with I don't know, like maybe six zeros or seven zeros, then it might have taken a very, very, very long time. Right? So right now our code works if we pass in the genesis block, which we are doing it over here. But it doesn't really do anything if we don't pass in the genesis block, if we are just trying to add a new block. So we need to see how we can do that. Okay, so at this point we have implemented the add block function, which actually creates a genesis block. But we don't have any ability to create the next block, all right? And once again the genesis block is going to hold all the rewards in terms of bitcoins. So those rewards will be given to the minors for doing these really complicated computations. But we need a way to create a next block where
>
> **[2:37](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/proof-of-work?u=76281980&t=157)** the normal transactions can get entered. So I'm gonna create a function called get next block where you can actually pass in the transactions or you can even pass in transactions as an array if you want to. So let's go ahead and do that, and it's going to give you a block. Okay? But the first thing we need is, we need to create kind of an empty block. So we're going to create an empty block, just a placeholder. We're going to also go ahead and add all those transactions to the block so that we can actually use it to find the key, so we are going to say over here transactions not for each just using some shortcut and we're gonna say transaction in and then we can call them block.addtransaction and we're gonna add some transaction to the block. All right, we do need to get a previous block also because the key will be dependent on the hash of the previous block. So, let's go ahead and create a function called get previous block. Now this function does not really exist, so we have to go in and implement this function. So, let's go ahead and do that, get previous block. And we are simply going to return self.blocks. Self.blocks.count minus one. I think you can even use self.blocks.last if you want to. Once we have the previous block,
>
> **[4:14](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/proof-of-work?u=76281980&t=254)** we will set the index of the block that we are trying to do, which in this case is self.blocks.count. And of course to get previous block is going to returning block, so let's go ahead and update that on line number 77. Once we have the index, we are also going to set the previous hash, which will be contained in the previous block.hash. And finally we have to create our own hash so we're gonna say generate hash for this block. And finally, when you're done, go ahead and return the block. Okay? Now currently it's generating the hash for two blocks. One is a genesis block. Well, that's the only one that it's doing at this point. So let's go ahead and do that, so it's creating those. But so, for first things first, we need to get a transaction so let's go ahead and create a transaction. Transaction and we're gonna say from Mary to let's say [John](../../Glossary/Tool/John%20the%20Ripper.md) for $10. We can also say [Blockchain](../../Skills/Data%20Science/Blockchain.md).getnextblock and we can pass the transaction or transactions to get the next block. So this will be our first block. All right, and now you will see that it's going to create two different hashes.
>
> **[5:49](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/proof-of-work?u=76281980&t=349)** One for the genesis block and one for the next block, which is block number one. Actually, it might be a good idea if I go ahead and place a line over here so that you will know that when the calculation for one of the block ended, and when the calculation for the next block started. So now we have to wait because now the search has begun. To create the hash that will consist of two zeros we have to do this search two times. One, to create the genesis block, and one to create a normal block. So let me actually scroll up over here. I want to see if I can show you, here we go. So that's the line, the divider. That means all the above calculation are being done for the genesis block, and you can see that the last one contains two zeros, meaning it was able to find the genesis block. It was able to create it and return it to you. All the calculation after this horizontal line is for the block number one, which contains all the transactions. And you will see at the end it was able to create that particular hash that begins with two zeros. Once again, the genesis block is the index zero block, and it is created by the blockchain itself, and it consists of the rewards and the bitcoins and all those kind of rewards, the financial rewards for the minors.
>
> **[7:25](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/proof-of-work?u=76281980&t=445)** That's what it contains. The actual transactions are added from block one and then two and three and forward, and so on, and then it can go on for hundreds and hundreds of blocks, they are joined together. And this is pretty much it, this is at least a basic overview of how the blockchain actually works. And if you understand this concept of blockchain, you can take this concept and put it on a Web API and you can invoke it from a browser. And this is exactly what we're going to do in our next section. I hope you are ready for it, because it's gonna be really exciting. Thank you very much, and stay tuned for more.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (4), [John](../../Glossary/Tool/John%20the%20Ripper.md) (1)
> **CLI Commands:** find (4)
> **Env Vars:** api (1)
> **UI Navigation:** scroll up (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Adding blocks to the blockchain](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/adding-blocks-to-the-blockchain?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/adding-blocks-to-the-blockchain?u=76281980&t=0)** - [Instructor] Hello and welcome back. In our excitement for the last lecture, we totally forgot to add the block to the [Blockchain](../../Skills/Data%20Science/Blockchain.md). So if I go ahead and print out the block, so lets say, blockchain.numberofblocks.count, you will see that instead of showing us two blocks, it's going to tell us that there's only one block in the blockchain. Which is obviously the genesis block, which is necessary for the blockchain to be initialize. We'll see that the block that we received, or mined, is not in the blockchain, because the getNextBlock function never really adds the block to the blockchain. You will see that it simply creates a block, and it returns you the block without adding it to the blockchain. So what we have to do is finally call the blockchain.add function, so addBlock, passing in the block. Now the addBlock function, in this case, is a private function, so let's go over here and make it non-private, so that we can call it, and that's pretty much it! That one line of code, and also making the addBlock function public and not private, is going to allow us
>
> **[1:36](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/adding-blocks-to-the-blockchain?u=76281980&t=96)** to add the block to the blockchain, and now when we print out the number of blocks in the blockchain, you will see that the blocks are actually two, right over here, instead of one. So make sure that you alter the chain. Make sure that you update your code with this change. Because this is going to allow you to add the block in the blockchain. Thank you very much and stay tuned for more!

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (11)
> **Code Identifiers:** addblock (3), getnextblock (1)
> **CLI Commands:** make (3)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 3. Smart Contracts

[↑ Back to Table of Contents](#table-of-contents)

#### [Understanding smart contracts](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/understanding-smart-contracts?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/understanding-smart-contracts?u=76281980&t=0)** - [Instructor] Hello and welcome back. In this lecture, we'll be talking about what exactly are [Smart Contracts](../../Skills/Data%20Science/Smart%20Contracts.md), and how smart contracts can be applied to the real world applications. The concept of smart contract was first introduced by the [Ethereum](../../Skills/Data%20Science/Ethereum.md) [Blockchain](../../Skills/Data%20Science/Blockchain.md) app platform. Now, Ethereum is a company behind the ether [Cryptocurrency](../../Skills/Data%20Science/Cryptocurrency.md), which is the second most popular cryptocurrency, right behind the bitcoin. We already know what the blockchain is, it's just different blocks being connected together, forming a chain, and each block can contain a transactions, which can be a transaction about financial terms, or transaction about insurance, kind of transaction about medical records, and so on. A smart contract is just a piece of code that lives and resides on the blockchain itself. Hm, that sounds confusing, right? So let's take an example of car insurance. Let's say that we have a blockchain of car insurance company which manages all the records for the different people who get the car insurance, in this case, the car insurance for [John](../../Glossary/Tool/John%20the%20Ripper.md) insurance record that we will be talking about. And all of this blockchain is called, let's say, insurance blockchain. Each record that we see consists of the name, the driver license number, and number of tickets.
>
> **[1:38](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/understanding-smart-contracts?u=76281980&t=98)** Let's say, after a couple of months, maybe six months or so, it's time to renew the insurance. Then the insurance blockchain is going to communicate with the driver license [office](../../Skills/Web%20Development/Microsoft%20Office.md), or the record holder, and is going to get the updated information from the driver license records for that particular user. For John, unfortunately, he got two tickets, which was updated, which was sent back to the blockchain using the smart contract. And then we added a new record to the blockchain for the insurance record, which is saying that now, John has two more tickets. So you can see, when John started out, he had zero tickets. But after some period of time, maybe six months, our blockchain communicated with some third party service, in this case, the driver license record service, and updated the blockchain using the smart contracts and updated the blockchain, reflecting that John now has two tickets. So most probably, his insurance is gonna go up. Blockchain is not only on one node, but blockchain, smart contract is not only on one node, but smart contract is actually run on each of the nodes.
>
> **[3:13](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/understanding-smart-contracts?u=76281980&t=193)** That's why all of the nodes will always be in sync of what is going on and why John insurance rate just climbed up a couple of hundred dollars because he got two tickets. When a new node joins into the network, it gets the full ledger as well as all the smart contracts. And all the smart contracts are ran again and verified again from scratch. Now that we have enough information about smart contracts, which is just a piece of code running on the blockchain, let's go ahead and see how we can implement smart contract in [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) using iOS.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (15), [Smart Contracts](../../Skills/Data%20Science/Smart%20Contracts.md) (6), [John](../../Glossary/Tool/John%20the%20Ripper.md) (6), [Ethereum](../../Skills/Data%20Science/Ethereum.md) (2), [Cryptocurrency](../../Skills/Data%20Science/Cryptocurrency.md) (2)
> **CLI Commands:** node (3)
> **Definitions:** is a  (1), is called (1)
> **Code Identifiers:** ios (1)
> **Speakers:** - [instructor] (1)

#### [Implementing smart contracts](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/implementing-smart-contracts?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/implementing-smart-contracts?u=76281980&t=0)** - [Narrator] Hello and welcome back. Now that you understand what exactly are [Smart Contracts](../../Skills/Data%20Science/Smart%20Contracts.md), let's go ahead and implement a very simple smart contract that is going to allow us to apply transaction fees based on the type of transaction that we're doing. Currently, our transaction class doesn't even have anything called fees or transaction type, we're going to add that later. Let's go, first of all, and create an enum which is going to allow us to find out what kind of a transaction we are doing. So transaction type, we can have different kind of transaction, we'll just focus on domestic transaction, as well as the international transaction. Okay? Now let's go ahead and then create a variable over here to hold these transaction types, so transaction type is transaction type. And also, I'm gonna add one more variable which will be representing the fees associated with the transactions. I'm gonna go ahead an update the initializer to add one more field so that it can take transaction type. So that means when we are creating a transaction, we do have to specify what kind of transaction it is. Make sure to set the transaction inside the constructor, so transaction type is transaction type. Okay? One thing you will notice is that the transaction type is not really decodable or encodable,
>
> **[1:35](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/implementing-smart-contracts?u=76281980&t=95)** so let's go ahead and create it as string, and it will be codable. Hopefully that will resolve our issues. Okay. So at least that part is done, now we have to create some sort of a smart contract. Instead of creating a smart contract class, we are first going to start with creating the protocol which will define the smart contract. We will call our smart contract, simply, smart contract, and it'll consist of one single function which is applied to the transaction. So all the other smart contracts that we're going to be programming and implementing are going to be using the smart contract protocol. So let's go ahead and create a smart contract and call it transaction type smart contract. This is obviously going to inherit from the smart contract. Now we need to implement the apply function, but that's the only requirement of the smart contract protocol. Inside the apply function, we're gonna start with defining some sort of a fees that is associated with each transaction. And now we can check that if the transaction type is either domestic or transaction type is international. If the transaction type is domestic, then we're gonna apply a different fee structure which is 0.02, meaning two percent.
>
> **[3:10](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/implementing-smart-contracts?u=76281980&t=190)** Other case, if the transaction type is international, then we're going to apply little bit more fees for international transfers, like 0.05. Once the fees have been applied, we can simply go ahead and set the transaction fees to the value of the fees that we have applied. Instead of applying the fees amount which is 0.02, we're obviously going to apply the dollar amount of the fees that was actually charged. Just like if you're buying an airline ticket and it will show you these taxes and all the different airlines' fees in dollars, that's the same idea we are using over here. Finally, we are going to reduce the amount of that whole transaction, which will consist of the fees. Then we'll subtract the fees from the transaction. Okay? Alright, so now at this point, we are ready to display our transactions. One thing that I would like to do is I would like to make our [Blockchain](../../Skills/Data%20Science/Blockchain.md) codable so that I can display to you in a [JSON](../../Skills/Web%20Development/JSON.md) format. So let's go ahead and create or use codable. As soon as I make it codable, all the other classes or objects which are used inside the blockchain class must also be codable. So let's go ahead and create codable for our block and also for our transaction. In our block, in our blockchain,
>
> **[4:47](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/implementing-smart-contracts?u=76281980&t=287)** we will declare the smart contracts. So let's go ahead and create a smart contract or a list of smart contracts. It is not necessary that you only have one smart contract, I mean you can have an array of smart contracts. In this case, our smart contract is called transaction type smart contract. But we don't really want to apply JSON to this, I mean we don't really want this to be part of the JSON display or to encode and decode. So we're gonna use a [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) for features. Basically, we are going to create an enum, which is going to allow us to define what different properties are going to be part of the codable and the encodable protocol. In this case, the blockchain only consist of the block's array that we wanted to impart, so I'm just gonna say blocks. This means that when it is time to encode or decode the blockchain, we are not going to be using, or the smart contract property is not going to be part of that encoding and decoding protocols. Alright, so at this point, we are pretty much ready, let's go ahead and I'm gonna uncommon this code, and I'm gonna make that I am passing some sort of a transaction type. So transaction, type, and I'm gonna call it domestic. And instead of this, let's go ahead and use JSON encoder
>
> **[6:23](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/implementing-smart-contracts?u=76281980&t=383)** so we can actually encode our blockchain and we can see what exactly are we getting. So data equals to something, and blockchain string, or blockchain JSON string, equals to string, passing in the data, and passing in the encoding. This is just so that I can actually show you what exactly the transaction looks like after it has been decoded and all that stuff. There we go. Let's see if the playground is gonna catch up with what we did, and hopefully, it will eventually, and when it runs, you will see that it's running actually 522 times. And it's going through, it's not really display, oh, I think we might have, no it is, it should've been displaying something, but it's currently not displaying anything right now. Let's go ahead and see what's going on. Everything looks okay so it most probably is the playground which is acting little bit weird, and we can actually stop it and start it again to see if it works out. No, that's okay. Let me copy paste the code again and see if that helps the playground to catch up. Okay, so now you can see that it's trying to find out the secret hash, which consists of, I believe, two zeros,
>
> **[7:59](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/implementing-smart-contracts?u=76281980&t=479)** and once it is able to find the hash, it's going to create the block and the transaction to the block, and then run the smart contract on each of these transaction and then basically update the block. So here we go. Let's go ahead and see what's going on over here. You can see at the end that we have the blocks, and the part that is interested for us is the transactions. So you can see that the transaction amount is not 10 dollars, but the transaction amount came out to be 9.8. The transaction type is domestic, and to [John](../../Glossary/Tool/John%20the%20Ripper.md), the fee is 0.2 from Mary. So from Mary to John, it's a transaction, and the fees that was charged was 0.2, and that's what's done by the smart contract. If I go ahead and change this to international transaction, and hopefully playground will eventually catch up, then you will see that the fee that will charged on the transaction from Mary to John on the amount of 10 dollars, will be more than two dollars which will be 0.5 will be the fees. And you can see that 9.5 is the total amount that has been transferred from Mary to John. So using smart contracts that live on the blockchain, you can program some logic that will run each time a block is being inserted on the transactions and so on. And smart contracts are really really important
>
> **[9:33](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/implementing-smart-contracts?u=76281980&t=573)** because using the smart contracts, as we have learnt, we can update a lot of different things based on it. It's kind of like an if this then that, so if this condition is true then do this. I will cover more of the smart contracts later on when I add some additional sections where we will go into some contracts working with the Oracle, which means a web service to get some other information. But for right now, you can think of smart contracts are just basically a piece of code that runs on the blockchain. Alright, so I hope you liked this video, thank you so much.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Smart Contracts](../../Skills/Data%20Science/Smart%20Contracts.md) (10), [Blockchain](../../Skills/Data%20Science/Blockchain.md) (10), [JSON](../../Skills/Web%20Development/JSON.md) (5), [John](../../Glossary/Tool/John%20the%20Ripper.md) (4), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Versions:** 0.02 (2), 0.2 (2), 0.05 (1), 9.8 (1), 0.5 (1)
> **CLI Commands:** make (4), find (3)
> **Env Vars:** json (5)
> **Definitions:** is called (1), means that (1)
> **Analogies:** just like (1), kind of like (1)
> **Best Practices:** make sure to (1)
> **Speakers:** - [narrator] (1)


### 4. Implementing Blockchain Web API

[↑ Back to Table of Contents](#table-of-contents)

#### [Prerequisites](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/prerequisites?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/prerequisites?u=76281980&t=0)** - [Instructor] Alright, so, before taking our [Blockchain](../../Skills/Data%20Science/Blockchain.md) to the cloud and exposing it as a web API, let's cover the prerequisites of this section. I will be using Vapor 3, which is the latest one right now, to implement the blockchain for the web API, or to put in the blockchain and exposing it as web API. So, a little bit of knowledge of Vapor framework is definitely gonna help you. Now, this course is not really about the Vapor framework, it is about blockchain, so I won't really go deep into what exactly is Vapor. I do have other courses, as you can see, which covers the Vapor framework, including Mastering Server Side [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) Using Vapor 3. So, if you do want to dive into the Vapor 3 framework, your best course would be this one. So, first of all, make sure that you do have Vapor 3 installed, and on the Vapor website, they give you a lot of instructions of how you can make sure and install Vapor. You can see that you have to have Brew installed, and once you have Brew installed, you will simply say brew install vapor/tap/vapor. And then this will show you that you have Vapor installed. If I do go to my command line right now, and let me stop that, and I will say, vapor --version. And you can see that I actually do have the toolbox for Vapor 3.1.7 installed, and I can also run the Vapor
>
> **[1:36](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/prerequisites?u=76281980&t=96)** help command, which is vapor --help to get all the stuff connected to Vapor. So, this is going to at least prove that I do have Vapor installed. If you don't have Vapor installed, then simply go to the Vapor website and make sure that you do have Vapor 3 installed, because I will be using Vapor 3 to create the web API.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (4), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **CLI Commands:** make (3), brew (3)
> **Env Vars:** api (4)
> **UI Navigation:** go to (2)
> **Prerequisites:** install (2)
> **Versions:** 3.1.7 (1)
> **Tools:** command line (1)
> **Speakers:** - [instructor] (1)

#### [Setting up a server-side Swift project using Vapor](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/setting-up-a-server-side-swift-project-using-vapor?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/setting-up-a-server-side-swift-project-using-vapor?u=76281980&t=0)** - [Instructor] Okay so hopefully at this point you have already installed the Vapor 3 framework. So now let's go ahead and create your first Vapor project. So I'm gonna run vapor new and give it some sort of a name for your project, all right? So I'm just gonna say over here you know, [Blockchain](../../Skills/Data%20Science/Blockchain.md) web api. But obviously you can name it anything that you like. So since I already have Vapor installed, it's gonna go ahead and create that project for me. So if I go ahead and look at the directory structure or list all the files you can see a special folder basically that was created by Vapor which is the name of the project that we just did is created. So let's go ahead and jump into the particular folder and you'll see different files and sources folder and public folder and packaged or server sectra inside that particular folder. But one thing you will notice is that you currently don't have any [Xcode](../../Skills/Mobile%20Development/Xcode.md) Project. So if you don't have Xcode how are you even going to open this in Xcode? Well, you can simply use a command vapor xcode which is going to create you the Xcode Project. Now this command depending on if you're downloading dependencies or not, it can take a little bit of time. So, you know, we're gonna come back when this command is actually finished processing. All right, so Vapor Xcode command has completed processing
>
> **[1:34](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/setting-up-a-server-side-swift-project-using-vapor?u=76281980&t=94)** and basically it's saying that do you want to open an Xcode. And I'm gonna say go head and open it now. For you, you should say yes, open an Xcode. I'm gonna say no over here but the reason that I'm saying no is that I have multiple copies of Xcode and it will open up in Xcode 10. I don't want to open it up in Xcode 10, I want to open it up in Xcode 9.4 or 9.3. All right, so what I'm gonna do is I'm gonna explicitly open it up in Xcode 9.3 so right click and open it up in Xcode 9.3. Now if you only do have 9.3 or 9.4 installed, you will say yes and it will open up obviously in whatever the Xcode it will find in your machine. I have multiple copies of Xcode so that's why I have to select that okay I need to open it up explicitly or manually. So here's our project. The first thing you should do is click on this target and make sure that the target is selected as run. All right, that's the first thing. And also, I'm not gonna run this server on my device, I'm gonna run it on the Mac, so let's go ahead and select the Mac. And that's pretty much it. The final thing you need to do is to go ahead and build your project. Now since we are building the project first time, it can take a little bit of time to build this project. All right, so we're gonna come back when the build is completed, and then I'm gonna show you how you can launch your first Web API written in Vapor.
>
> **[3:10](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/setting-up-a-server-side-swift-project-using-vapor?u=76281980&t=190)** All right, so now that the build is finished, go ahead and open up the app folder and go to the routes. Now there is already a bunch of code written in the routes or [swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) file. I haven't written any of this code, this code comes by default when you create a brand new Vapor project. I think it's good because it gives you a route called hello, meaning if your Vapor server is running you can simply go to that route and it should return hello world. Kind of like an indication that it is working correctly. At this point go ahead click on the run scheme, go to edit scheme and make sure when you click on the run, executable is also set to run. All right, this is very, very important. Close it, and now we can just simply press a play button to start our Vapor server. And you can see on the console that the Vapor server has started on the board localhost:8080, so I can simply copy that board and I can go to a brand new URL and the route that is available to us is hello and when I go to hello you can see that hello world is being displayed. So this kind of proves that at least a Vapor server is now running, right? And in the next lecture we will be introducing that how we can set up our blockchain on the server, all right? So this will be really, really cool so stay tuned for that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Xcode](../../Skills/Mobile%20Development/Xcode.md) (16), [Blockchain](../../Skills/Data%20Science/Blockchain.md) (2), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **UI Navigation:** go to (5), click on (2), select the (1)
> **Versions:** 9.3 (4), 9.4 (2)
> **CLI Commands:** make (2), find (1)
> **Env Vars:** api (1), url (1)
> **URLs:** [localhost:8080](https://localhost:8080) (1)
> **Ports:** :8080 (1)
> **Cross-References:** in the next (1)

#### [Setting up a controller and services](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/setting-up-a-controller-and-services?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/setting-up-a-controller-and-services?u=76281980&t=0)** - [Teacher] Okay, so now that we have confirmed that our route for "Hello, world!" is working, or any other route if we build one, it's working, we can go ahead and implement all the routes right over here in the routes.[swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) file. But it's not really recommended that you do this way, because this particular file can grow up to be very big, and it will turn into a really bad, dirty spaghetti code. So let's go ahead and organize the record in a form of controller and services. So instead of writing all the code of our block chain over here, what I'm gonna do is I'm gonna add a brand New File, which will serve as a [Blockchain](../../Skills/Data%20Science/Blockchain.md) controller. So all the different actions or routes that's gonna happen will be going inside the block chain controller. So BlockchainController, make sure that it is added to the "App" Target as you can see, go ahead and add it, there we go. The first thing I wanna add is import the Vapor, and now I can create a class called BlockchainController. Now inside the BlockchinController class, what I can do is I can create some sort of a function. Alright? So I'm gonna say "greet", obviously, we don't really need a greet function but I think we are just getting started, first of all we need to see that can we even invoke a route which is inside a BlockchaiController, so this is a good practice, see something works, and then we can move forward. It's gonna take in the Request, and what should we return?
>
> **[1:37](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/setting-up-a-controller-and-services?u=76281980&t=97)** Well if it's a greet, then we can return a String, right? Now we certainly can return String, but in Vapor three, it's more about Future, for returning Futures, meaning we can say something like this. Now what exactly is Future? Since this course is not really about Vapor, so I'm gonna just give you a high level definition of Future. Future is basically saying it will be an asynchronous response meaning that this particular response that we are about to send, it will be sent later on, alright? So you're not going to be blocking any calls, When you request something, it will be returned immediately and then the result is going to be evaluated. So, now if I try to return "Welcome to Blockchain", you will see that it cannot compliance because we are returning or we are trying to return a string but the expected type is Future of String, so we can't really do that, so let's go ahead and convert it to String. So Future.map, there is a function called map in Vapor 3 that will allow you to return as strongly type object, which in this case what we are trying to return is a String, so nothing in the Request over here, and in the call back, over here you have to specify what type of object you are returning, or what is a type of the return, type basically
>
> **[3:11](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/setting-up-a-controller-and-services?u=76281980&t=191)** which in this case is String, and now we can go ahead and return over here, "welcome to Blockchain". Now obviously if you need more learning for the Vapor 3, then I have a separate course for that, that is for Vapor 3, alright? So now let's go ahead and build that, and you'll see that it builds successfully. Right now, our routes, file, doesn't have any idea about the Controller we just built, so let's first go ahead and remove all of these code, though we won't be needing this, and what I'm gonna do is I'm gonna create a blockchainController object, and go ahead and create a BlockchainController, now I can simply say router.get and some sort of a route, so I'm gonna say api/greet, so if I go to that particular route, it was use the blockchainController.greet function,
>
> **[4:14](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/setting-up-a-controller-and-services?u=76281980&t=254)** that's it, right? So, let's go ahead and first of all build it. Okay, so that's hooked up and now we can go ahead and run our application. If the server is running on 8080 port, I can go to 8080 port, so localhost.8080/hello, and oops, so now it's not hello, it is, well, we can actually change that if you want, so let's go ahead and change that to hello, make sure it's hello, we are gonna build it, and now you can go ahead and run it. And now if I go to hello, and if I refresh, it will run this and invoke it from the blockchainControllers as we just built. Great, so this means that all of our actions will be now inside the blockchainController, which is really good because we are not polluting our routes.file. Let's go ahead and create another folder inside the App folder, and this time we're gonna create a folder for Services, now Services will include the blockchain service which will be responsible for adding things, adding blocks with the blockchain and in transactions, and, you know, different things you're trying to do to the blockchain. So I'm gonna go ahead and add a new File, there we go, and I will call this the "BlockchainService". There will be nothing in the file, we're just gonna create an actual class which is called the BlockchainService class,
>
> **[5:50](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/setting-up-a-controller-and-services?u=76281980&t=350)** and that's pretty much it. In order to use a block chain service, it will be input from the block chain controller, right now the block chain controller doesn't even know about the service, so what we should do is we can have some sort of a way to create a property, and then that we can use, so I'm gonna say private (set) var blockchainService, which is BlockchainService. Alright? Now I can go ahead and make it kind of like this, so that it will be initialized when you call it from the controller. Or since we are using the blockchain controller init, so we can now also go ahead and initialize this. So I can say self.blockchainService = BlockchainService() Alright? So this way when you create an instant of blockchain controller, your blockchain service is already initialized at that point and it can be then used in the controller actions. So, great! So we were able to create our blockchain controller and the blockchain service, obviously there is nothing really going on in the blockchain controller and blcokchain service, but that is for the next lectures, where we will actually create the blockchain at blocks in transaction to the blockchain and then return the blockchain to the user, so stay tuned for that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (15), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Code Identifiers:** blockchaincontroller (3), blockchainservice (2), blockchaincontrollers (1)
> **Definitions:** is a  (4), means that (1), is called (1)
> **CLI Commands:** make (3)
> **UI Navigation:** go to (3)
> **File Paths:** routes.swift (1)
> **Analogies:** kind of like (1)
> **Best Practices:** recommended (1)

#### [Returning the blockchain](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/returning-the-blockchain?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/returning-the-blockchain?u=76281980&t=0)** - [Instructor] Okay, so in the last lecture we set up our [Blockchain](../../Skills/Data%20Science/Blockchain.md) controller as well as a blockchain service, and now we need to use our blockchain controller to return us the blockchain. But the first question is: How do we even get access to the blockchain? Remember, we were typing a lot of code in [Xcode](../../Skills/Mobile%20Development/Xcode.md) playgrounds. So I have added all of that code, which basically presents a transaction, the block, as well as a blockchain, into Models.[swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) file. So this is all the code that we have already been done. I just literally copy/pasted it over here. All right, so now we can go ahead and work on how will we return the blockchain. And returning the blockchain is a duty for BlockchainService, so let's go ahead and first of all, create a property for the blockchain. Private (set) var blockchain, which will be off-pipe blockchain. All right, now what we're gonna do is inside the initialization matter, and so inside the init matter, we are going to assign or, basically create a blockchain, so Blockchain, and then go ahead from the genesisBlock. We're gonna pass in a block, appears to be an empty block which will serve as a genesis block. Finally, we can create a function called getBlockchain, which will be responsible for returning you a blockchain, and we can simply then return the blockchain itself.
>
> **[1:34](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/returning-the-blockchain?u=76281980&t=94)** So that's great. I mean, the blockchain service is basically returning you the actual blockchain. All right, so now the question is: How can we go to our controller and tell our controller to return us the blockchain? All right, so now we can jump into our BlockchainController and implement a route, or a action, that will return us the blockchain. So I'm gonna start with Function getBlockchain. It's gonna take in the request on check. And over here, we can return a future with the blockchain, something like this. Right, but in our case, it has already been resolved, meaning our service is already returning a blockchain. So we can simply say Blockchain over here if you want to, and we can simply use our blockchain service and get blockchain to return that particular blockchain. If your blockchain service was doing some sort of, anything in its processing, then we would have returned a future over here, but right now, it's not. If you go and look at the blockchain service for a blockchain, it simply returns the blockchain. All right? Okay, so this particular action or route has been created, or a function for BlockchainController. I was gonna copy that, and we are going to reduce for that function right over here in our router. So router.get, and we need to provide a particular pathway
>
> **[3:08](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/returning-the-blockchain?u=76281980&t=188)** which should go to look at the blockchain, which is this. And now I can say blockchainController.getBlockchain. So getBlockchain. Now if I try to build this, it's gonna give me some errors. The reason that it's giving you errors is that whenever you're returning a particular type, which in this case is blockchain, it must conform to content. So if you go and look at our types that we have, it is Transaction type, but the content it use is codable. So let's go ahead and add Content. Now content belongs in Vapor class. Let's have Vapor featured, so let's ahead and add the Vapor, and make sure all of them are using Content. So if I go ahead and find out all of them, you can even Command-click on that, and jumping to the definition, so you're gonna see that this particular protocol, Content, is using the Codable protocol. So just by using Content protocol, you will automatically start using Codable, which will make sure that all your classes, which in this case is Transaction, Block, and Blockchain, can be converted to [JSON](../../Skills/Web%20Development/JSON.md) easily. Now the other thing that you need to do is you need to make sure that these, all classes are final, meaning you cannot really inherit from these classes. All right, there we go. And now let's go ahead and build it.
>
> **[4:44](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/returning-the-blockchain?u=76281980&t=284)** And go ahead and run it. And now you can see our blockchain in action. You can see all of this code sort of lets you leave the hashes being printed. And now if I go to a particular route, which, let's copy this route, and I go to that route, and the route for the blockchain is actually Blockchain, and go ahead and make a request, you can see our initial blockchain. Now I'm using Postman over here. This application is Postman, but you can use any application you like. But the good thing is that we can actually see our blockchain, with one block, and that block contains only messing with the genesis block, so it's nothing really going on in this block. But the great thing is that we were able to take our blockchain to the cloud, and we have exposed our blockchain on the cloud. Now in the next section, we'll be going over how we can add protections, and how we can add blocks to our blockchains. So stay tuned for that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (39), [Xcode](../../Skills/Mobile%20Development/Xcode.md) (1), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Code Identifiers:** getblockchain (4), genesisblock (1), blockchaincontroller (1)
> **CLI Commands:** make (4), find (1)
> **UI Navigation:** go to (4), click on (1)
> **Cross-References:** in the last (1), in the next (1)
> **Tools:** postman (2)
> **Prerequisites:** set up (1), initialization (1)
> **File Paths:** models.swift (1)

#### [Mining a block](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-a-block?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-a-block?u=76281980&t=0)** - [Instructor] In the last lecture, you learned that how you can return a [Blockchain](../../Skills/Data%20Science/Blockchain.md) in [JSON](../../Skills/Web%20Development/JSON.md) format from our Web API implemented in Vapor. But let's see how we can actually mine a new block. For mining a new block, I'm gonna start with our routes. Right now we have a route for hello, which is kind of useless, you can delete that. And we have a route for a blockchain, which returns, well, the blockchain. But we don't really have any route where we can post a new transaction. So let's go ahead and create that route. Whoops. Router.post in this case because we will be posting a route. And this time, what we're gonna do is we're going to post a route, hold on a second, whoops, router.post and we're gonna post a particular route which will post to a server which will match on map to a certain class. So which class is gonna map to, whatever we are sending the key value pair? Well, the transaction class, because that is what we are sending. So all the key value pairs in JSON that we will be sending later on, it will go ahead, it will map to the transaction class. Let's go to the next one. The next one is that at what URL. So the URL will be mine. And what are you gonna use for request? Well, since we are writing our nice code in a blockchain controller,
>
> **[1:34](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-a-block?u=76281980&t=94)** so we're gonna use blockchaincontroller.mine. Obviously this particular mine function does not really exist but we still have to implement it. So this is really important, and this is a really cool feature of Vapor 3, is that you can pause JSON and it can match the key value pairs to this particular class and it can automatically populate it, which is really cool so I don't have to do anything in order to populate JSON data into a class or a transaction class. So let's go to the blockchain controller and implement the mind function. I'm gonna start with function mine. The first parameter will be the request and the next one, I'm just gonna call it transaction with transaction. And this is automatically passed through the route over here, since we are seeing it, that it will map to the transaction class. So that particular instance is already populated and it will be passed to us over here. This is going to return a block which we will return later. The block will actually be created from the blockchain service, so I'm gonna call blockchainservice.getnextblock. And I'm going to pass in the transactions as a transaction or rate. So we already have passing a transaction, so there we go, don't forget to return it from here because we will return a block. All right, so that's a new block that's being mined.
>
> **[3:10](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-a-block?u=76281980&t=190)** Now once again the blocks in service does not really have any function called get next block, so let's go ahead and jump over to the blockchain service and go ahead and implement this function. This is gonna take in a transaction or transactions, in our case we just have one transaction. But in reality I believe you will have way more transactions, depending on the size of the block. All right, great. So now we have implemented a function but we still need to create that next block and also add the block to the blockchain. So let's go ahead and do that. Let block equals two, self.blockchain.getnextblock and passing in the transactions, which in this case is just transaction. Once we get the block we are going to and go ahead and add the block to the blockchain. And finally, obviously, return the block. And that's it, let's go ahead and see what's going on over here. Maybe I misspelled transactions, not transaction. All right, so our function is ready and let's check out our routes. Our routes are ready. We're gonna post JSON at a route called mine. So let's go ahead and run this and we are going to test it out using Postman. Now you will see that when we run the app it automatically starts creating our blockchain
>
> **[4:43](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-a-block?u=76281980&t=283)** with one genesis block. I can copy this URL and let's move over here, request it with a GET request and go ahead and check out the blockchain itself. You'll see that we do have a blockchain and the only block in the blockchain right now is the genesis block, which is automatically added once you create a blockchain. So let's go ahead and add or create a new tab and we're gonna select POST because we will be mining a block. So, this URL slash mine, go ahead and set the headers because we will be sending in JSON so go ahead and set the headers of content type and set it to replication JSON. Now in order to send the JSON, I'm gonna go to body and I'm gonna select raw and now I can type my JSON over here. Make sure that this is in JSON format. The first scheme I'm gonna pass in is from, so I'm gonna say Mary and then to, I'm gonna say Alex and the amount of the transaction, so I can say over here maybe $100. Now this is not something that I'm randomly typing. All of these key value pairs, especially the keys, are going to match against the properties of the transaction, and you can see the name of the properties are exactly the same-from, to and amount. And this is one of the features of Vapor 3 which will automatically match the properties of whatever I'm seeing over here,
>
> **[6:20](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-a-block?u=76281980&t=380)** which is specifically, which is a transaction of self, and is going to create a transaction object for me, I can name it anything I want, but it will create a transaction object based on the JSON post key value pair that you're passing. All right, so let's go ahead and send a request and you can actually see mining operation is taking place now. We are trying to mine, we were able to find a special hash that begins with two zeros. So if it was able to mine the block successfully, then that means our blockchain is gonna be request a blockchain, that particular block should be added. And you can see that that particular block has been added with one transaction and that transaction is Alex to Mary for $100. Let's go ahead and add another transaction and this time this transaction is between Steve and Bryan and the transaction is for $500. And let's go ahead and send it. We'll go back again, you can see that it's trying to find that special hash that begins with two zeros. Once it is found, it has found a block and has mined the block, and it will add the transaction into the block. Now let's go ahead and go to the URL and submit a request to blockchain and there we go, you can see the other blockchains now, getting bigger and bigger. And that's pretty much it. Congratulations for creating your blockchain and adding blocks to your blockchain,
>
> **[7:55](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-a-block?u=76281980&t=475)** adding transactions to your blockchain. Now this section already existed by the way, it was using Vapor 2 but now I have updated from scratch to Vapor 3. Now I would be really, really, really grateful if you can go online on the course and rate and review the course, your rating is super important to me and it helps me update the material much faster, to add more courses and to update with new content. So thank you so much for watching this and stay tuned for more updates. Thank you so much.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (21), [JSON](../../Skills/Web%20Development/JSON.md) (11)
> **Env Vars:** json (11), url (5), api (1), post (1)
> **UI Navigation:** go to (4)
> **CLI Commands:** find (2), make (1)
> **API Endpoints:** get  (1), post
 (1)
> **Definitions:** is a  (2)
> **Cross-References:** in the last (1)
> **Tools:** postman (1)


### 5. Decentralizing Blockchain

[↑ Back to Table of Contents](#table-of-contents)

#### [Creating node models](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/creating-node-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/creating-node-models?u=76281980&t=0)** - [Instructor] In the last section, you learned how you can take your [Blockchain](../../Skills/Data%20Science/Blockchain.md) to the Cloud by using the Vapor framework. But even though we were able to take it to the Cloud, it was a more of a centralized server that was running. And the whole point of blockchain is to run as decentralized. So now in this section we're going to see how we can make this a decentralized application. Now please note that I'm not going to be hosting this into actual multiple servers on the public environment, I'm just going to show you how you can do it by running Vapor on the different ports. But obviously, in your actual application, you have to deploy it on different servers, and then you will have to have the ability for people to connect to those servers and run their own blockchain, so that it can grow, and grow, and grow, and grow. Unless you have a private blockchain and you have, maybe, 100 servers that are running. And they are doing mining and the proof of work, etc. For this to work, we first have to define some sort of a model that is going to present our blockchain node. So let's go to the models file and start typing our model class. Now the starting point of this application is the ending point of the last section. So if you just go to the start folder and open up the project, the x core project, this is what you will see.
>
> **[1:33](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/creating-node-models?u=76281980&t=93)** I haven't really edited any code at this particular point. So make sure, if you want to follow along you can definitely open up the start folder and then open up the x code, which will take you to where we left off. But if you do want to see the last, like the ending result of all of this then go ahead and obviously open the n folder. Okay, so let's get started. The first thing we're gonna do is create a class. We can name the class anything. I'm just gonna call this blockchain node. And this is gonna represent a single node in the blockchain. The only property this blockchain node has is a address property. And this address property represents the local host and the board number that local host is running. So in order to create the blockchain you do have to provide the address. So let's go ahead and create the initializer and simply assign the address property to the past and address for the initializer. We also need to go to our blockchain class and update the blockchain, so the blockchain will know about the different nodes. So a blockchain can be running on several nodes, so let's go ahead and create a property that is going to hold nodes. And this will be an array of blockchain node. So let's go in and call it blockchain node. And I can go ahead and initialize it blockchain node.
>
> **[3:07](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/creating-node-models?u=76281980&t=187)** And initialize it as an empty array. Obviously we are using [swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md), and swift can infer the types. I don't really have to define this. Well even for the block I don't have to. But I actually like doing that, but anyways. Now we need a function, so that we can actually register the nodes. So let's go ahead and do that. So I'm gonna go ahead and create a function, called register a node or register nodes. Because we are just going to register different nodes. You're gonna pass in the nodes, basically which will be of type blockchain node. And we're gonna return, also, the blockchain node. The node that you just registered. You don't really have to, but I'm just gonna return the same node that we just register back. This way I can do self and nodes and append and I can append a contents of a sequence, which in this case is nodes. And finally, go ahead and return self dot nodes. So once you register the nodes, it's going to give you an array of the nodes that have been registered. You don't have to return it but we're just gonna return the nodes over here. Great, let's go ahead and build an application to see that everything, at least compiles. So that is done in the blockchain. We do have to also update our controller file as well as a route file, to allow us to register a node.
>
> **[4:46](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/creating-node-models?u=76281980&t=286)** Currently, as you can see, there is absolutely, no route that allows me to register a particular node. So let's go ahead and create a route. So I'm gonna call it router dot post. And now I have to be careful because I have to pass in the type of where or what type that I'm passing in. So I can say that I will be passing you a blockchain node array. So blockchain node dot self. And the path of that, where we would go is let's say, nodes dot register. And the controller that you're gonna use, or the function of the controller, is blockchain controller dot register nodes. Obviously this function does not really exist if you try to build your application. It is going to complain that, "What are you talking about? "I don't really know anything about register nodes." So now let's see that how we can implement that particle function. Let's go the the blockchain controller. And right over here, above the mine function we're going to add another function called register nodes. The first parameter is the request that we're gonna pass in, that's a given. And nodes, which will be a blockchain node. So blockchain and node. And then we will go ahead and also return an array of blockchain node.
>
> **[6:21](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/creating-node-models?u=76281980&t=381)** Now the blockchain controller doesn't really do any registration, it actually passes that registration to the blockchain service. And we have been using blockchain service throughout our course. So let's go ahead and call it blockchain service dot register nodes, passing in the nodes. And the nodes will be passed. Once again, if you try to build the application, it's gonna complain that, "Well, there is no "register nodes on the blockchain service." So now we need to find out how we can add register nodes on the blockchain service. Let's go to the blockchain service. And as you can see, there is no register nodes. So let's go ahead and pass that. We're doing that because we don't really want all of this stuff to go inside our controller. So now we can say blockchain node. And once again, and yes, you can say that they are delegating are moving forward in this way. And that's true, they are, I mean the blockchain controller is delegating to the blockchain service, which is delegating to the blockchain. Which is simply going to insert the nodes. So blockchain dot register nodes and let's go ahead and pass in nodes. And there we go. So now, at least we will be able to register the nodes. Great, right? So now we have all of our stuff ready. Let's go ahead and in Postman, and give it a try. All right, so let's go ahead and run this.
>
> **[7:56](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/creating-node-models?u=76281980&t=476)** And once it is running, what I'm going to do is I'm gonna launch Postman. And we'll make sure that it is running. There we go, so the mining operation has already started for the Genesis block. And once it stops, here we go. We can now go to the local host eight zero eight zero to register our node. So let's move to the Postman. We're gonna copy this URL. But this is not really the URL for registering the nodes. I believe the URL is nodes slash register. But always make sure whether you are a list. So if you go back to the routes, you can see that the URL is slash nodes slash register. And that is why you have to send in an array of blockchain nodes to register, so let's go ahead and do that. First chain this to post, because we're actually posting. The header, since we are sending in the [JSON](../../Skills/Web%20Development/JSON.md) with send the content type, to application JSON. I can go to the body, select raw. And now I'm ready to go ahead and post some sort of a JSON. Now if you remember correctly, we are actually allowing it to post as an array of addresses. Array of nodes that you want to register. So let's go ahead and create an array. And then each array has an element. And that element has an address.
>
> **[9:29](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/creating-node-models?u=76281980&t=569)** And that's literally it. I mean that is what a blockchain node is, it's just a list of addresses. So let's go ahead and add that address eight zero eight zero. That's the first node I'm registering. And the second node that I'm registering will have a different address. So, eight zero nine zero. Let's go ahead and send this. And when I send this, you will see that it actually sends me back the same exact thing because that is where, that is exactly what we're returning. Now let's do one more thing. So I'm gonna go over here, stop the server. And going to the routes, I'm gonna create another route, which will just return me all the nodes that are registered. So router dot get, and I can say, if I go to nodes, "Just give me all the nodes "that are registered." And use blockchain controller dot get nodes. Once again, there is no function called get nodes. But we have to implement that function. So now I'm gonna go to the blockchain controller and implement that particular function. Function which is get nodes, it's just gonna take in the request that is already passed in. And it's going to return us the blockchain node. The reason we are building this blockchain or get nodes function is that if we just want to get the nodes,
>
> **[11:02](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/creating-node-models?u=76281980&t=662)** want to see the nodes, we wouldn't be dependent on registering the nodes to see all the nodes. We just want a list of the nodes. So that's why we have created a separate function called get nodes. Now let's go ahead and call our service dot get nodes, which also does not exist. Let's go to blockchain service and see how we can return the nodes. Function get nodes. I don't think it's gonna take anything. And then let's go ahead and it does return something, which is blockchain node. And then simply, self dot blockchain dot nodes. Just gonna build that, great. Let's go ahead and run the application again. And it's gonna start mining operation again. And it will allow us to register our nodes again. So let's see if it's finished, there we go. Then we'll go back and register the same exact nodes again, which is eight zero eight zero eight zero nine zero. Each node over here represents a different server that is running. Maybe it's doing a mining operation. Maybe it's just waiting for some transactions. But we are using for mining operations. So these are all mining nodes. Let's go ahead and send that. And this is what it returns. Now I can simply copy this URL and open a new one and remove the register parts. If I just want to access the nodes and there we go. So our nodes registration is complete.
>
> **[12:38](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/creating-node-models?u=76281980&t=758)** We are able to send in the address. And we are able to register the nodes. So in the next lectures, you're gonna see that how we can start the server on different nodes and start mining operation, even, on different nodes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (47), [JSON](../../Skills/Web%20Development/JSON.md) (3), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2)
> **CLI Commands:** node (27), make (4), find (1)
> **UI Navigation:** go to (9), open the (1)
> **Env Vars:** url (5), json (3)
> **Cross-References:** in the last (1), go back to (1), in the next (1)
> **Tools:** postman (3)
> **Warnings:** note that (1), be careful (1)
> **Definitions:** is a  (1)

#### [Mining on nodes](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-on-nodes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-on-nodes?u=76281980&t=0)** - Now that we have registered our nodes on different ports, let's see how we can get started on mining our nodes on different ports. The first thing you need to do is go into the project folder and run vapor build. Now this might take a little bit of time to build, so, make sure that the build is completed before starting the vapor server on a particular node. Once the build is completed, you can simply run, vapor run--port= number of the port that you want to run the vapor server on. I'm gonna go ahead and type 8080 as a port number. Let's go ahead and run it. You'll see that as soon as you run on port number 8080, it's going to start creating those hashes to create and to add the genesis block to the block`chain. You can see all of the hashes are done and our server is now running on local host 8080. I can simply copy the URL and switch to a Postman and go ahead and request the [Blockchain](../../Skills/Data%20Science/Blockchain.md). You can see when I request the blockchain, I get the blockchain with only one node. This is the genesis block that is inside the blockchain. I can go back, open up a new window by command T
>
> **[1:37](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-on-nodes?u=76281980&t=97)** and then run the server on a completely separate node which in this cases can be 8090. So now I have run the vapor server on two different nodes, 8080 and 8090. Just like when I was running it on 8080, when the server actually starts, it's going to start creating the genesis block. And once the genesis block is completed, it is going to add it to the blockchain. So let me go ahead and copy that and I can go ahead and open up a new window in our Postman and request the blockchain. You'll see that you will also have a genesis block on 8090. So, now we have two servers running, one is on the 8080 and the other one is on 8090. You can also perform different actions on different ports. Actions like mining a block. Let's go ahead and see how we can actually mine a block in our blockchain. In order to mine the block, I'm just gonna copy the URL and open up a new window, paste it and instead of requesting a blockchain, I'm simply going to say mine. I'm also gonna set it to post because now we need to post the information that will be contained in the block. And this information is transaction. So, let's first go ahead and set the content type to be application [JSON](../../Skills/Web%20Development/JSON.md) because we will be sending
>
> **[3:12](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-on-nodes?u=76281980&t=192)** it in the form of json. I'm gonna select raw because I will be typing in json by hand. There are three properties that we need to define from, I can state it from [John](../../Glossary/Tool/John%20the%20Ripper.md), to which I can say over here, maybe, the name Mary and the amount that is going from John to Mary we can say $100. Let's go ahead and send it. You'll see that as soon as you send it, the blockchains start working and it's going to try and find that particular secret hash on board number 8080 where I'm trying to mine a block. Once a block has been mined, I'm going to add the transaction, the only transaction that we have into the block and then at the end it's going to be added to the blockchain. You can see that now, we have previous hash and the new hash generated. Lets go back to the first tab and request the blockchain on port number 8080. This time when you request the blockchain on port number 8080, you will notice that now you will have a much bigger blockchain because one of the transactions has been added to the previous block onto the new block. This one is called the transactions. Its an array and it contains the transaction that we just added. If you go to the port number 8090 and request a blockchain, you will notice that the blockchain
>
> **[4:45](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-on-nodes?u=76281980&t=285)** on the server 8090 is not changed. Its the same exact blockchain, it has no effect and it doesn't even really know about the 8080 blockchain.
>
> **[5:01](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-on-nodes?u=76281980&t=301)** Now, in a blockchain environment in a decentralized environment, what you want to do is you want to start resolving your blockchain. This means that the blockchain which is smaller, which in this case is 8090, will get the blocks
>
> **[5:23](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/mining-on-nodes?u=76281980&t=323)** from the 8080 blockchain or the longer blockchain. This is called conflict resolution and usually the conflict resolution works this way. The blockchain that is much longer, much bigger wins and the shorter blockchains adopt the longer blockchains. Obviously, there is much more complexity behind it because you have to look for a blockchain that if it's getting longer and there are no irregularities and also other nodes that are running on 7000, 3000 also have the same bigger blockchain. But, right now we are only running it on 8080 and 8090. So, our task is to create an action that is going to resolve the blockchains between two different ports which is 8080 and 8090. After the resolution, you will notice that the blockchain on 8090 is updateD and is going to be using the block chain on 8080 because that blockchain has more blocks in it. And the conflict resolution is what we are gonna do next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (26), [JSON](../../Skills/Web%20Development/JSON.md) (3), [John](../../Glossary/Tool/John%20the%20Ripper.md) (2)
> **CLI Commands:** node (3), make (1), find (1)
> **Definitions:** is called (2), means that (1)
> **Env Vars:** url (2)
> **Tools:** postman (2)
> **UI Navigation:** switch to (1), go to (1)
> **Code Identifiers:** updated (1)
> **Cross-References:** go back to (1)

#### [Resolving conflicts](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/resolving-conflicts?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/resolving-conflicts?u=76281980&t=0)** - [Mohammad] In the last lecture we learned that how we can register nodes, and also start mining on different nodes. Now we're gonna learn that how we can resolve conflicts between different nodes, so let's get started. I'm gonna jump into the [Blockchain](../../Skills/Data%20Science/Blockchain.md) service and create a function which will be responsible for resolving the conflicts between blockchains existing on different nodes. I've called this function, resolve, and this is going to take in a completion which will return us a blockchain, the resolved blockchain in this case. Let's go ahead and also mark this as escaping because we will be sending the blockchain back, so let's call it, escaping. The first thing we need to do is we need to get all the different nodes, so these are all the nodes on the servers on which the blockchain is running, and in the previous lectures we have already shown that how you can register different nodes. Now I'm gonna go ahead and iterate through all the nodes. For each node I'm gonna go ahead and get the URL, so let's go ahead and get the URL which can be a string and we can simply say that that string can be registered as a address, so node.address and we will get the blockchain. In this case we are assuming that the address
>
> **[1:37](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/resolving-conflicts?u=76281980&t=97)** consists of the whole thing, meaning, localhost 8080, localhost 8090 or any other port numbers. Go ahead and also unwrap this so we'll get the actual URL object. Now we can use the URL session class to go ahead and create a request. We're gonna pass in the URL, and for the completion handler since this is the last argument, we're going to use the trailing closures. We're not interested in the response or the error in this case, so we're just gonna leave that out. Make sure to call resume because if you don't call resume nothing's gonna happen. That is one of the common mistakes that people do and can end up with hours and hours of debugging, so make sure you call resume. You're to go ahead and unwrap the data, now the data that we're going to receive from the blockchain route will be an actual blockchain, so let's go ahead and convert it using the [JSON](../../Skills/Web%20Development/JSON.md) decoder, so JSONDecoder.decode and we're gonna decode the blockchain itself, so it'll be Blockchain.self, and from the data which we are already passed to. At this point we can go ahead and compare the blockchain which we have over here versus the blockchain which is running on this particular node
>
> **[3:10](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/resolving-conflicts?u=76281980&t=190)** so if the blockchain which is currently on this node is greater than the blockchain which we have just received, then this means that we can simply call the blockchain or the completion, passing in self.blockchain, basically the bigger blockchain. Else we have to do something about it, this means that the blockchain running on the current node is not larger than the blockchain that we have received from the server by using our URL session request. And if it's not we'll go ahead and assign it, so blockchain equals to the blockchain that we received from that particular node. And go ahead and finally call the completion handler and passing in the blockchain. Let's go ahead and build that, and that worked fine. Now this is a very special kind of a function resolve because it does asynchronous requests inside the function. So now let's go back to our blockchain controller and create the resolve function over here. I'm gonna start with creating resolve which is going to take in a request, and it is going to return you the future and blockchain.
>
> **[4:39](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/resolving-conflicts?u=76281980&t=279)** Now we're gonna go ahead and create a promise, now this is a little bit different and the reason we're doing that is that inside our service call that is a asynchronous request, so we have to wait for it so that we can return it. So the promise itself is going to return instantly, but it will be evaluated once we've got the blockchain itself. Request.eventLoop.newPromise Once we have the promise, we can go ahead and call blockchainService.resolve this is going to give us a completion handler and the completion handler will consist of the blockchain. We can also use the trailing closure as I'm showing you over here, and call promise.succeed and finally sending in dollar zero. Let's go ahead and first build that, it's going to return the promise so let's go ahead and return promise.futureResult and let's go ahead and build it again. Alright that's pretty much it, the only thing remaining to do is to register our resolve in the routes. So I'm gonna jump into the routes.[swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) file and I'm gonna go ahead and register this particular route. This part will be called resolve, and it's going to use the blockchainController.resolve. Let's go ahead and build it again, and now we're ready to test out our conflict resolution on a particular blockchain. So let's go ahead and run two different servers,
>
> **[6:14](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/resolving-conflicts?u=76281980&t=374)** one on port number 8080 and one on port number 8090. Alright so let's start the server, I'm gonna first server I'm gonna start is from within the [Xcode](../../Skills/Mobile%20Development/Xcode.md), and this server is going to run on the default port which I will leave it as 8080. But the second server that I'm going to start will be from the terminal, and I can simply go ahead and say vapor run - - port equals to 8090. So let's get started with both the servers. And you can see in the background that when I started the server 8080 it already created the blockchain and the genesis block, and the same is going to happen for 8090. Let's go ahead and check out the block for 8080 so I'm gonna go over here and 8080 let's check out the blockchain. And you can see that we have a genesis block, we don't really have any nodes attached to it, so it's a good idea to maybe register some nodes, some servers, so let's go ahead and see how we can do that, there we go, POST, and we're gonna POST it on 8080, alright so we registered two nodes, 8080 by itself and 8090. I'm gonna do the same exact thing on 8090 so the other server 8090 also has an idea of the surrounding nodes. Let's go ahead on 8090 and check out the blockchain, alright so nothing really there,
>
> **[7:49](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/resolving-conflicts?u=76281980&t=469)** it's just the genesis block that has been created. So let's go ahead and mine something so on 8090 I'm gonna go ahead and mine one block. And if you go over here and see, the mining has started and the mining is now completed. Now let's go back and check out the blockchain on 8090. Definitely we can see that our transaction is added to the block. Now remember and keep in mind that this is on port number 8090 node. If you check on 8080, you'll see that it does not have that particular transaction and it does not have that particular block, the only block it has is the genesis block. Now at this point 8080 is much smaller blockchain as compared to 8090 which already contains one block, so on 8080 I'm gonna go ahead and say resolve, and go ahead and run it. Okay, and now I'm gonna go ahead and check out the blockchain of 8080, and look at that, what's just happened, we did the conflict resolution and we were able to update the 8080 blockchain with 8090 blockchain. So if I go to 8090 now, you'll see both the blockchains are similar or exactly the same because now they have been updated. So all the transactions, all the blocks
>
> **[9:24](https://www.linkedin.com/learning/blockchain-programming-in-ios-using-swift/resolving-conflicts?u=76281980&t=564)** have been copied over to 8080 and now the conflict has been resolved and the larger blockchain which was on 8090 wins and 8080 must conform to that particular blockchain. So this is how you perform the conflict resolution in a blockchain using Swift and Vapor.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Blockchain](../../Skills/Data%20Science/Blockchain.md) (38), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (1), [Xcode](../../Skills/Mobile%20Development/Xcode.md) (1)
> **CLI Commands:** node (7), make (2)
> **Env Vars:** url (6), post (2), json (1)
> **Code Identifiers:** eventloop (1), newpromise (1), blockchainservice (1), futureresult (1), blockchaincontroller (1)
> **Definitions:** is a  (3), means that (2)
> **Cross-References:** in the last (1), go back to (1)
> **File Paths:** routes.swift (1)
> **API Endpoints:** post  (1)


## Instructor

- [Mohammad Azam](../../Instructors/Cybersecurity/Mohammad%20Azam.md)

## Resources

- Exercise files available

## Skills Covered

- Blockchain Architecture

## Path Context

### In [Become a Blockchain Developer](../../Paths/Cybersecurity/Learning%20Paths/Become%20a%20Blockchain%20Developer.md)
← [Blockchain- Learning Solidity](Blockchain-%20Learning%20Solidity.md) | **14 of 14**

## Appears In

- [Become a Blockchain Developer](../../Paths/Cybersecurity/Learning%20Paths/Become%20a%20Blockchain%20Developer.md)

## Related Courses

_Courses sharing skills:_

- [Blockchain for Developers- Hyperledger Fabric on Azure](Blockchain%20for%20Developers-%20Hyperledger%20Fabric%20on%20Azure.md) — Blockchain Architecture

---

[↑ Back to top](#)