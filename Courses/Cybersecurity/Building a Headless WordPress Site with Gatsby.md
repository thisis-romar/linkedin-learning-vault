---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: building-a-headless-wordpress-site-with-gatsby-15801541
url: "https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541"
duration_minutes: 151
duration: 2h 31m
level: Intermediate
updated: 3/23/2022
learners: 5709
skills:
  - Wordpress Development
  - GatsbyJS
exercise_files: true
github: "https://github.com/LinkedInLearning/headless-wordpress-gatsby-3022691"
thumbnail: "https://media.licdn.com/dms/image/v2/C4E0DAQHfCK_-CVNrTw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1647886030952?e=2147483647&amp;v=beta&amp;t=9ZTxNHE0CK1_q_WHOnJbnP-kUEVpp221YMw6JYpalI4"
linkedin_topic: Cybersecurity
learning_paths:
  - '[[Advance Your Skills in WordPress]]'
prev_courses:
  - '[[WordPress- REST API]]'
path_nav: '[{"path":"Advance Your Skills in WordPress","position":8,"total":8,"prev":"WordPress- REST API","next":null}]'
path_count: 1
tags:
  - course
  - topic/security
  - topic/web-development
  - skill/wordpress-development
  - skill/gatsbyjs
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Cybersecurity/Building%20a%20Headless%20WordPress%20Site%20with%20Gatsby.md)

![Building a Headless WordPress Site with Gatsby](https://media.licdn.com/dms/image/v2/C4E0DAQHfCK_-CVNrTw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1647886030952?e=2147483647&amp;v=beta&amp;t=9ZTxNHE0CK1_q_WHOnJbnP-kUEVpp221YMw6JYpalI4)

# Building a Headless WordPress Site with Gatsby

> View Morten's LinkedIn NewsletterWordPress, the powerful publishing platform, powers much of the web. Gatsby allows developers to build performance-focused, modern websites and applications with React. In this course, discover how to combine this dynamic duo to get a web publishing powerhouse. Join instructor Morten Rand-Hendriksen as he explains how to use WordPress to populate a Gatsby site. He 

> [LinkedIn Learning](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541) | 2h 31m | Intermediate | 6K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [WordPress and Gatsby working hand in hand](#wordpress-and-gatsby-working-hand-in-hand)
  - [What will be covered in this course](#what-will-be-covered-in-this-course)
- [**1. Setting Up**](#1-setting-up) (3 videos)
  - [Set up WordPress to work with Gatsby](#set-up-wordpress-to-work-with-gatsby)
  - [Set up a Gatsby site using a starter](#set-up-a-gatsby-site-using-a-starter)
  - [Use the exercise files on GitHub](#use-the-exercise-files-on-github)
- [**2. WordPress as a Gatsby Data Resource**](#2-wordpress-as-a-gatsby-data-resource) (2 videos)
  - [Explore the WordPress GraphQL tree](#explore-the-wordpress-graphql-tree)
  - [Use WordPress site data in Gatsby](#use-wordpress-site-data-in-gatsby)
- [**3. Working with Pages**](#3-working-with-pages) (6 videos)
  - [Query pages in GraphQL](#query-pages-in-graphql)
  - [Generate pages automatically](#generate-pages-automatically)
  - [Create a page template](#create-a-page-template)
  - [Work with featured images](#work-with-featured-images)
  - [Challenge: Add metadata to the page template](#challenge-add-metadata-to-the-page-template)
  - [Solution: Add metadata to the page template](#solution-add-metadata-to-the-page-template)
- [**4. Custom Menus**](#4-custom-menus) (6 videos)
  - [Query custom menus in GraphQL](#query-custom-menus-in-graphql)
  - [How WordPress manages menu items](#how-wordpress-manages-menu-items)
  - [Create a flat menu in Gatsby](#create-a-flat-menu-in-gatsby)
  - [Create a nested menu in Gatsby](#create-a-nested-menu-in-gatsby)
  - [Challenge: Create a footer menu](#challenge-create-a-footer-menu)
  - [Solution: Create a footer menu](#solution-create-a-footer-menu)
- [**5. Working with Posts**](#5-working-with-posts) (7 videos)
  - [Query posts in GraphQL](#query-posts-in-graphql)
  - [Generate posts automatically](#generate-posts-automatically)
  - [Add category and tag info](#add-category-and-tag-info)
  - [Create an index template](#create-an-index-template)
  - [Generate index pages automatically](#generate-index-pages-automatically)
  - [Challenge: Create a post navigation component](#challenge-create-a-post-navigation-component)
  - [Solution: Create a post navigation component](#solution-create-a-post-navigation-component)
- [**6. Categories and Tags**](#6-categories-and-tags) (5 videos)
  - [Query categories and tags](#query-categories-and-tags)
  - [Generate category indexes automatically](#generate-category-indexes-automatically)
  - [Create a category index template](#create-a-category-index-template)
  - [Challenge: Create tag indexes](#challenge-create-tag-indexes)
  - [Solution: Create tag indexes](#solution-create-tag-indexes)
- [**7. Custom Post Types and Taxonomies**](#7-custom-post-types-and-taxonomies) (2 videos)
  - [Add custom post types and taxonomies](#add-custom-post-types-and-taxonomies)
  - [Query custom post types and taxonomies in GraphQL](#query-custom-post-types-and-taxonomies-in-graphql)
- [**8. Considerations When Using WordPress and Gatsby**](#8-considerations-when-using-wordpress-and-gatsby) (1 videos)
  - [What to do about Blocks (Gutenberg)](#what-to-do-about-blocks-gutenberg)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [WordPress and Gatsby working hand in hand](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/wordpress-and-gatsby-working-hand-in-hand?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/wordpress-and-gatsby-working-hand-in-hand?u=76281980&t=0)** - [Morten] The modern web runs on [[JavaScript]], and JavaScript frameworks like [[React.js|React]] are quickly evolving our understanding of what websites and applications are, and how they work. While [[WordPress]] forged the way for self-publishing online and today powers a sizable portion of the web, It's becoming increasingly long in the tooth. Enter Gatsby, a react-based site building framework that consumes data through [[GraphQL]] and outputs blazing fast modern web applications. This course picks up where the course Learning Gatsby leaves off, and assumes you're already familiar with both Gatsby and WordPress. Gatsby makes it easy to build modern websites and applications with React, and WordPress makes it easy to write and manage content for the web. Combining the two gives you a modern web publishing power house, and I'm here to show you how to get it right. So let's get cracking.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[React.js|React]] (3), [[WordPress]] (3), [[JavaScript]] (2), [[GraphQL]] (1)
> **Speakers:** - [morten] (1)

#### [What will be covered in this course](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/what-will-be-covered-in-this-course?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/what-will-be-covered-in-this-course?u=76281980&t=0)** - [Instructor] As we get started I want you to know what to expect from this course. So here's a short breakdown of what will be covered and what you need to know before you start. In this course we'll take a deep dive into how to use [[WordPress]] as a data resource for Gatsby. We're not simply building a Gatsby front end to a WordPress site. We're laying the foundations for WordPress to become one of many data sources for a Gatsby site. We'll start by giving WordPress a [[GraphQL]] interface for modern data access and management and explore the vast data sets made available from WordPress to Gatsby through GraphQL. Then we'll turn WordPress into a data source for a Gatsby site and we'll look at how to generate content and pages from WordPress data, including pages, menus, posts, indexes, tag and category archives and custom post types and taxonomies. And finally, we'll briefly discuss what to do about WordPress blocks, AKA Gutenberg blocks. This course assumes you're already familiar with how to build a Gatsby site and how to manage data from various GraphQL sources. So if this is the first time you're building a Gatsby site I strongly recommend watching the course Learning Gatsby first as this content is prerequisite to understand what's happening in this course you're watching right now. I won't be covering any Gatsby basics in this course. You also need an in-depth knowledge on how to build and run a WordPress site including how to create pages, posts and navigation menus. All of this and more is covered in our course, [[WordPress Essential Training]].

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (10), [[GraphQL]] (3)
> **Env Vars:** aka (1)
> **Prerequisites:** prerequisite (1)
> **Speakers:** - [instructor] (1)


### 1. Setting Up

[↑ Back to Table of Contents](#table-of-contents)

#### [Set up WordPress to work with Gatsby](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/set-up-wordpress-to-work-with-gatsby?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/set-up-wordpress-to-work-with-gatsby?u=76281980&t=0)** - [Instructor] The goal and purpose of this course is to explore how we can use [[WordPress]] as a data source for Gatsby. So it's self-evident, we need a WordPress site with some content to work with. If you're following along with the exercise files for this course, I recommend setting up a WordPress site locally on your computer and use this URL for the WordPress site mywpsite.local. Here's why. If you're using the exercise files and you use a different URL for your WordPress site, you'll have to go in and manually change that URL every time you check out a new branch in the exercise files for each movie. But if you use this URL, you'll be able to just work with the exercise files throughout. So it's a lot easier that way. I'm using local to run my local WordPress site on my computer. You can use any tool. Just make sure you set the URL to mywpsite.local to make everything work properly with the exercise files. Once you're done with the course and you want to create your own WordPress and Gatsby site, you can use any tool you want and any site you want. I recommend you always keep a local version of the site on your computer so you have full control over your WordPress sites and your Gatsby site as you're developing. Then once you're ready to launch your site to the world, you can either move the WordPress site online to a live host on the web, or you can connect Gatsby to a live host on the web that already has a WordPress site and pull content from there. Now, once you have a WordPress site set up on a computer, you need some content on that WordPress site. Here you can write your own content if you want to or if you just need some dummy content, you can go grab the Theme Unit Test data from the WordPress codex.
>
> **[1:33](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/set-up-wordpress-to-work-with-gatsby?u=76281980&t=93)** If you go to the page, you can download the Theme Unit Test right here, then go to WordPress, go to tools and import and import the Theme Unit Test data in. The good thing about using the Theme Unit Test data is it gives you all the types of content WordPress might want to display to Gatsby. So if I jump to the front-end here, you'll see I have a fairly built out menu. I have posts and pages and images and categories and tags and types, everything that WordPress might want to send to Gatsby. So it's a good idea to use this data to test your Gatsby site against. All right, with WordPress setup and some content to work with, we need to do one more thing. We need to install two plugins. These plugins allow Gatsby to talk to WordPress and both of these plugins are available from the WordPress plugin directory, meaning you can install them directly from within WordPress. Go to plugins, add new, and the first plugin you want to install is called WPGraphQL. This plugin adds a [[GraphQL]] interface to WordPress. That way, Gatsby can talk directly to GraphQL and pull data from WordPress. Now, when you activate WPGraphQL, you get a new icon up here in the WordPress toolbar called Graphical IDE. Click on that and you'll go to a graphical representation or interface for GraphQL in WordPress. Now, we're not going to use this particular interface in the [[Representational State Transfer (REST)|rest]] of the course because this is the GraphQL implementation for WordPress itself.
>
> **[3:05](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/set-up-wordpress-to-work-with-gatsby?u=76281980&t=185)** Gatsby uses a custom implementation of GraphQL just for Gatsby, and that's done using a second plugin. So again, go to plugins, add new, and this time search for WPGatsby. This plugin does exactly what it sounds like. It makes it easy for Gatsby and WordPress to talk to each other. Once you install and activate this plugin, WPGatsby will sit on top of WPGraphQL and create a Gatsby-specific GraphQL interface for WordPress that's much easier for WordPress to work with and for you to understand as you're working through the of data inside of WordPress. The final step we need to do now is just make sure this interface works. For that, we'll go to the front-end of the site, so just mywpsite.local and then type in /graphql. When you run this in Firefox like I'm doing, Firefox recognizes that this return is in the [[JSON]] format so it gives you a JSON interface to read the data. If you're doing this in Chrome, you'll just get the text without any formatting, but it's the same text. And what you notice right away is the very first thing it says is errors. GraphQL request must include at least one of these two parameters query or queryid. So even though this looks like an error, it actually tells us that the WPGraphQL and the WPGatsby are working properly and providing a GraphQL interface for us. This is what we want from Gatsby. And when we have this, we can start building out Gatsby and pull data from WordPress.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (30), [[GraphQL]] (9), [[JSON]] (2), [[Representational State Transfer (REST)|Rest]] (1)
> **Env Vars:** url (5), json (2), ide (1)
> **UI Navigation:** go to (7), click on (1)
> **Exercise Files:** exercise files (5), download the (1)
> **Prerequisites:** install (4), set up (1), setup (1)
> **CLI Commands:** make (3)
> **Tools:** firefox (2)
> **Definitions:** is called (1)

#### [Set up a Gatsby site using a starter](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/set-up-a-gatsby-site-using-a-starter?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/set-up-a-gatsby-site-using-a-starter?u=76281980&t=0)** - [Instructor] Working with Gatsby and [[WordPress]], you can go down one of two paths. The first path is to use Gatsby as a decoupled headless front-end to WordPress. That means you're basically using Gatsby to create the front-end of WordPress. This way, anything that happens in WordPress also happens in your Gatsby site, and only what happens in WordPress happens in your Gatsby site. That way you can take full advantage of all the features that Gatsby and [[React.js|React]] have to offer on the front-end of your WordPress site, but still manage all your content and work with your WordPress site the way you would in any other WordPress site. If you want to go down this path, you can pick up a Gatsby Starter for WordPress, and it's available directly from Gatsby. Go to [gatsbyjs.com/starters](https://gatsbyjs.com/starters), and then I just type in WordPress, and you'll find whatever Gatsby Starters for WordPress are available. Just install it, run it, link it up to your WordPress site, and you're good to go. But this path is fairly limiting. It also doesn't take full advantage of what Gatsby has to offer. The whole point of Gatsby is what's known as the content [[Mesh]], where we pull data from different sources and then use Gatsby to collate all that data and present it to the user in one cohesive site. That's the path we'll go down in this course. And for that, we don't need a WordPress Gatsby starter. We actually don't need a starter at all. We're going to do this from scratch. It all starts in your code editor. I've chosen VS Code. Here I've navigated to an empty folder, and I'm going to open my terminal. From terminal, I'll simply type in gatsby new. Now I can do this because I've already installed
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/set-up-a-gatsby-site-using-a-starter?u=76281980&t=94)** the Gatsby [[CLI]]. If you haven't done that before, and if you haven't worked with Gatsby before, I recommend you go watch the "Learning Gatsby" course to get familiar with how Gatsby works before you jump into this. All right, so I'll run gatsby new. This triggers a wizard of sorts that walks me through a bunch of questions and lets me set up my site. So down here at the bottom it says, "What would you like to call your site?" I'll call this one Gatsby and WordPress, okay. "What would you like like to name the folder "where your site will be created?" So the wizard will create a new folder under the current folder I'm logged into. So I'll just call it Gatsby and WordPress, like this. Then it asks, "Will you be using a CMS?" And in this case I will. So I'll navigate down to WordPress, and select Yes. "Would you like to an install a styling system?" So here you can choose between Emotion, PostCSS, Sass, styled-components, and Theme UI. I'll say PostCSS. "Would you like to install additional features "with other plug-ins?" Let's see here. [[Google]] Analytics, no. Responsive images, yes. So I'll hit Space bar to mark that. Page meta tags with React Helmet, yes. Automatic site map, yes. Generate a manifest file, yes. Markdown and MDX support, yes, and Done. And now it says, "Configure the WordPress plugin." And if you look at the very bottom, this should be the full URL to your [[GraphQL]] Endpoint set up by WPGraphQL. Remember how we tested that in the previous movies?
>
> **[3:07](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/set-up-a-gatsby-site-using-a-starter?u=76281980&t=187)** So we have mywpsite.local/graphQL. This is the URL it's looking for. So I'll copy that and paste it in, [http://mywpsite.local/graphql](http://mywpsite.local/graphql), Return. Now it lists all the configurations, and says, "Shall we do this?" I say, "Yes." And now Gatsby New will go on to the internet, pull down Gatsby itself, all of these plugins, all dependencies, and do the full install. So this will take a while because there's a lot of data that's being downloaded and installed into your computer. Once it's done, you'll have a fully functioning Gatsby site already connected to WordPress and ready for your development. All right, the installation is done. Now in the instructions it says, "Now you can navigate cd gatsby-and-wordPress, and then [[npm]] run and develop. What I do when I work with Gatsby is the second I've installed Gatsby, I actually reopen VS Code in that folder so I don't have to navigate in through the terminal. So let's do that first. I'll go to File, Open folder, and then I'll open it directly inside WPGatsby here and Gatsby and WordPress. That way my new Gatsby project is at the root. From here, I can boot up the Gatsby development process and check to make sure everything is working properly. To do that, in terminal I simply type out gatsby develop and hit Return. Now the Gatsby develop process starts up, and an entire new Gatsby site is built out with connection to WordPress, meaning Gatsby will now go and try to pull content from WordPress. You can actually see it here in the terminal
>
> **[4:40](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/set-up-a-gatsby-site-using-a-starter?u=76281980&t=280)** if I scroll up a bit. You see, gatsby source WordPress. It's pulling in content type, menu item, category, comment, menu, post feature, page, taxonomy, user, et cetera, et cetera. So you can see Gatsby is going into GraphQL, pulling data out of my active WordPress site and putting it into Gatsby. Now, of course, right now, nothing really happens to that data because we don't have any templates so we're not really building anything, but Gatsby has access to all that information. And that means when I go to this URL here, local host 8,000 underscore underscore underscore GraphQL, so I'll hit Command and click to get to that. We have access to GraphiQL, and on the Explorer side here, if I [[Zoom]] in a bit, you see, we have all these entries that say "allWP." So this is all the WordPress content that Gatsby has been able to pull out of WordPress. That means Gatsby is talking to WordPress through GraphQL, and we're able to treat the data from WordPress just like any other data source inside Gatsby. This is where the true site development begins.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (28), [[GraphQL]] (6), [[React.js|React]] (2), [[GatsbyJS]] (1), [[Mesh]] (1)
> **Tools:** terminal (5), vs code (2)
> **Prerequisites:** install (4), set up (2), configure (1)
> **Env Vars:** url (3), cli (1), cms (1), mdx (1)
> **CLI Commands:** find (1), cd (1), npm (1), make (1)
> **UI Navigation:** go to (3), scroll up (1)
> **Code Identifiers:** graphql (1), wordpress (1), allwp (1)
> **URLs:** [gatsbyjs.com](https://gatsbyjs.com) (1), [http://mywpsite.local/graphql](http://mywpsite.local/graphql) (1)

#### [Use the exercise files on GitHub](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/use-the-exercise-files-on-github?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/use-the-exercise-files-on-github?u=76281980&t=0)** - [Instructor] Now that you know how to boot up a Gatsby project from scratch, let me show you how to use the exercise files for this course so you can follow along with me 'cause it's a bit different. The exercise files for the course are in a [[GitHub]] repo. Here you'll find branches for each movie and the branches are named for the chapter and movie number. So if you're watching chapter number two, movie number two, you're looking for 02_02. You'll also notice that for many movies there are two branches, one with the suffix B and one with the suffix E. In this case the B is the beginning state of the code. The E is the end state of the code. So if you want to code along with me, you check out the B branch and then code along with me. And then you can compare your code to my code in the E branch later on. You'll also notice that for many movies like this one, 03_01, there's only a B branch. That means I'm not changing any code in that movie. So you can just open up the branch and look at the code and follow along with me. For everything to work properly and for you to be able to follow along, you need to bring this GitHub repository into your computer and work with it as your Gatsby project. To do so, start by copying the uri for the GitHub project. Then go to VS Code and navigate to whatever folder you want to place the project inside. So here I've navigated to a folder called wpgatsby. It doesn't matter if there's something else in the folder 'cause you're going to create a new folder for your project. Just make sure you're not dumping a new project into an existing Gatsby project. So you should see a tree like this where there may be other folders but there's no actual project going on. From here we'll run Gatsby new. So we'll say gatsby new,
>
> **[1:33](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/use-the-exercise-files-on-github?u=76281980&t=93)** then name the folder we want to place the new project into. So I'll call it exfiles and then paste in the uri for the GitHub repository with the exercise files. Run this command and now Gatsby new will do the same thing it did before. So it'll set up a new Gatsby project for you, pull down all the dependencies and make everything work together except it'll use the exercise files GitHub repository as the baseline. So that way you get all of my code into the project along with all the goodness of Gatsby. Now you can navigate into the exfiles folder and here you'll see the entire Gatsby project. My preference at this point is to reopen VS Code in this exfiles folder so that I'm working inside the folder at all times and I don't have to navigate into it. So I'm going to do just that. I'll go file, open folder and navigate a wpgatsby and exfiles. Doing this means when I open terminal, I'm already in the exfiles folder and I don't have to navigate into it. All right, these are the exercise files we'll be working with throughout the entire course. This however is the main branch of the repository but we need access to old sub branches so you can go in and see the code at different stages of the process. That requires adding the exercise for GitHub repo as an upstream source and then fetching all the different branches from that repository. Here's how to do that. First I'll go into terminal and make sure I'm sitting in the exfiles folder. You can see that right here. Then I'll type in [[Git]] remote add upstream and paste in the uri for the GitHub repository again.
>
> **[3:06](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/use-the-exercise-files-on-github?u=76281980&t=186)** When I run this command, I'm telling my local version of the code that hey, the original version of the code is sitting on GitHub and I want you to connect to it and be able to pull data down from it. Once I've done this, I can run another command, git [[Fetch]] upstream. This will pull down every single branch from the GitHub repository on the web. And that means from this point onwards, I can now check out any of these branches. So for example, if I want to check out say branch 06_01b, I can simply say git checkout 06_01b, hit return. A new branch is created in my local repository. You noticed the code changed and now you're working with the exact same code I had when I started the movie 06_01. That's it. Now you have the exercise file set up on your computer and you're ready to start working along with me.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (9), [[Git]] (3), [[Fetch]] (1)
> **Tools:** github (9), vs code (2), terminal (2)
> **Exercise Files:** exercise files (5), github repo (2), exercise file (1)
> **CLI Commands:** make (3), git (3), find (1)
> **UI Navigation:** go to (1), navigate to (1)
> **Prerequisites:** set up (2)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)


### 2. WordPress as a Gatsby Data Resource

[↑ Back to Table of Contents](#table-of-contents)

#### [Explore the WordPress GraphQL tree](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/explore-the-wordpress-graphql-tree?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/explore-the-wordpress-graphql-tree?u=76281980&t=0)** - [Instructor] Working with [[WordPress]] in Gatsby is no different from working with any other [[GraphQL]] data source. We send requests to GraphQL. GraphQL returns the data to us and then we do whatever we want with it inside of Gatsby. The difference is now that data is generated by WordPress and we can take advantage of all the data creation features in WordPress by creating posts and pages and categories and tags and custom pulse types and custom fields and custom menus, anything we want inside WordPress and then pull that data into Gatsby and manipulate it to display it on the front end. This entire process starts by running Gatsby develops. So inside my Gatsby site I'll open my terminal and type in gatsby develop. As Gatsby boots up it now queries WordPress through GraphQL for all available data. All that data is pulled into Gatsby and made available as a data source. And we can now explore that data source using GraphiQL. When Gatsby develop has fully booted up, you get two links, one to local host, which is the live Gatsby site, another to localhost:8000/__graphql. And this is what we want to look at. From here you can hit Command or Control + click to open the URL. It'll open in your browser. And here we have full access to all the data GraphQL has available to us through Gatsby. Under the Explorer, you can see all of that data. And what you see right away is we have two different sets of data from WordPress. You have all these nodes, prefixed allWp and then further down
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/explore-the-wordpress-graphql-tree?u=76281980&t=94)** you have another set of nodes, prefixed wp. The difference here is allWP gives you access to all the information. So for example, all your pages or all your posts or all your categories, whereas down here in the wp for prefix, you get one item, a singular item. So one category or one comment or one page or one post. So let's see what type of data we can pull out of GraphQL right now. For example, can we get the name of the site? So this name here, MYWPSITE is developed that's defined inside WordPress. To do that, we'll go into the singular item here. So I'll say wp and here I have a bunch of things. So let's see. How about general settings? So under general settings, I have title. Let's just try that. Run the query and see what we get. Title mywpsite. Oh, so this is the title for the site. All right. Can I get something else? Can I maybe get the description? Over here the description is just another WordPress site. If I run the query, we get just another WordPress site. Cool. I can also get the URL for the site. Here it is, mywpsite.local. So here you see the power of GraphQL right away. We can go in and query specific data from the WordPress database and every piece of data that sits inside the WordPress database is now available to us through GraphQL. All right. So let's clear this query and look for something else. Let's say I want a list of all the posts on the site. For that I can go into allWp posts
>
> **[3:11](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/explore-the-wordpress-graphql-tree?u=76281980&t=191)** then I need to look for the actual posts. So I'll say, give me all the nodes and we need to some information from each of the posts. So let's see, we want the post title which would be down here and maybe the slug, let's see no, the uri to the post. So that would be the, uri you would use to access the post. What else do we want? Maybe the date that it was created. Okay. So let's run that query and see what we get. So now we get an array of nodes. Each of them is a singular post. So here we have the first post called hello title. I don't know, hello world. And then we have block-button post and block-cover and block-image and so on. And here you see there's a huge list of all available posts. All right. But what if I want to get a single post? If we go back and look at the data we have for each of these nodes you'll quickly see we have two different types of IDs. We have this one called databaseId. I'll take data away just to reduce the amount of data we have here. And then scrolling further down we also have another one just called id. So what is the difference between databaseId and id? Run this query and you see the databaseId is a number. So number one for the first post then number 1785 for the second post, 1784. So these are the database ID numbers inside the WordPress database. The other field, the one called id is the ID GraphQL has given to each of these items. So that's an autogenerated ID generated by GraphQL to help us find items if we don't have a databaseId.
>
> **[4:49](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/explore-the-wordpress-graphql-tree?u=76281980&t=289)** So that means if you want to query a single post you can either query it using the databaseId or you can use the id itself. And in this course, we'll do both. But the good rule of thumb here anytime you're acquiring some WordPress content you should use the WordPress information so we'll use the databaseId. So let's say we want just this one post here, the one with a databaseId 1785. I'll copy that number. Then I'll clear out my query here by just closing allWpPost and scroll down wpPost. So this is a singular post. Then I want to find just one post. So I'll go to the databaseId entry here and set it equal to and then paste in my number. Now I'm saying, give me only the post with this ID here, databaseId equals 1785. Then I want some information from this post. So I want the, let's see, the author name probably. So the author node and then the nice name, the username, no nickname or nice name. And then I want the dates and I want the title here. Run this query. Now we get a single post. He has no, this author has no nice name, okay? So we can go back up and say, what if we do first name instead? Do we get something then? Hm, oh, interesting. Oh yeah, right. This is the theme unit test data.
>
> **[6:21](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/explore-the-wordpress-graphql-tree?u=76281980&t=381)** That's why we get weird names. But here you can see we are querying a single post and we're getting all the information from that single post. It gets even more interesting though, because you know that in WordPress you'll have posts that have categories or tags attached to them. So how do you know which categories or tags are attached to a post? Well, if we go and inspect the data here, so I'll collapse author and then we can go down to categories. You'll notice when I open categories we get a new series of nodes. Inside these nodes we now have a list of all the available categories. So here we can say for each of the nodes, I want the name of the category and the link to the category. Run the query again nd now we get a list. So here it says that this post is filed under the category called block. And the link is to /category/block. If we go and look at the actual post, it's this block button one here, it's under the category block and it's also tagged content and something in Greek. So we can go back up here and say, do we have tags anywhere? Let's see tags do the same thing. We look for all the nodes. And then for each of the nodes we want the name and the link. Run the query again. We now get a list of all the tags. So here we have name content and something Greek and then /tag/content. So that means any data that sits in WordPress is available to us through GraphQL. And it's just a matter of creating the query in GraphiQL, pull out the data we want
>
> **[7:56](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/explore-the-wordpress-graphql-tree?u=76281980&t=476)** and then parse it in [[JavaScript]] inside Gatsby.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (16), [[GraphQL]] (11), [[JavaScript]] (1)
> **Code Identifiers:** databaseid (9), allwp (3), allwppost (1), wppost (1)
> **CLI Commands:** find (2), node (1)
> **Env Vars:** url (2), mywpsite (1)
> **UI Navigation:** open the (1), scroll down (1), go to (1)
> **Definitions:** is a  (3)
> **Analogies:** for example (2)
> **URLs:** [localhost:8000](https://localhost:8000) (1)

#### [Use WordPress site data in Gatsby](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/use-wordpress-site-data-in-gatsby?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/use-wordpress-site-data-in-gatsby?u=76281980&t=0)** - [Instructor] The quickest path to understanding how we can work with [[WordPress]] data through [[GraphQL]] is to see a really simple bare bones example. And that's what we'll do in this movie. If you boot up your Gatsby site right now and go to localhost:8000, you'll get this page. And at the very top, it says Gatsby and WordPress Demo Starter. This is the current site title, but this is being generated by Gatsby through information that's sitting inside Gatsby. What I want to be able to do is go to WordPress and change the site title in WordPress and then have that new site title reflected on the Gatsby site. For that to happen, we first need to figure out how the site title is getting to the Gatsby page to begin with. Now, the title is actually defined here in gatsby-config.js at the very top. There's this siteMetadata section. And here we have a defaultTitle that says Gatsby and WordPress Demo Starter, which is the same thing we're seeing at the top here. So that's the data source we're currently working with. That's an internal data source for Gatsby. Now we want to pull it from WordPress instead. And to do that, need to figure out where we should place this data. So the site title is normally placed in the header. Meaning if I go to the source folder inside Gatsby, go to components, I should be able to find a header. And here we have it Header.js. So at the very top of Header.js, it says const Header. So this is the header component and it imports this property siteTitle, and then down here, it use the siteTitle to output. So that means this is where the site title is being displayed,
>
> **[1:32](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/use-wordpress-site-data-in-gatsby?u=76281980&t=92)** but this is not where we're getting the site title from. Now, header is called by Layout.js. And if we go into Layout.js, you can see here, we're calling the Header component. And here, if we scroll down, this is where the Header component is used. And this is where the siteTitle is displayed. So the siteTitle property is being passed into the component here. And it's currently populated with data.site.siteMetadata.defaultTitle, which is what you saw in gatsby-config. Now, this data is being queried using a static query in GraphQL, right here at the very top. It's a SiteTitleQuery, then it's querie's site, siteMetadata, and defaultTitle. And that's what you're seeing down here. This is a great way of having a fallback. So you can say, if something goes wrong on the WordPress end, the site can still have a title and will pull it directly from Gatsby. But it's also not what we want. We want to be able to display the WordPress site title. For that, we need to go to GraphQL. And you'll remember, we already did this once, but I'll do it again. I'll go into wp, then say, I want general settings, so here. And then from generalSettings, I want the title. So this should gimme the title. Yes, it does. mywpsite. So this is the title I want. That means this is the query I want. wp, generalSettings, and title. Just for simplicity sake, I've already added that query in. So you can see, we are running multiple queries here. One for the internal data inside Gatsby and one for WordPress. So wp, generalSettings, title. That also means I should be able to display
>
> **[3:05](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/use-wordpress-site-data-in-gatsby?u=76281980&t=185)** that data down here. So what I'll do is set up a conditional statement that says if we have the WordPress data, then use the WordPress data. Otherwise, use the default title. So in front of data.site.siteMetadata.defaultTitle, I'll put in two pipes. And then in front of that, I'll say data.wp.generalSettings.title. That's wp.generalSettings.title. Data being the object that contains all this information. So now, if we get a result from WordPress, that's the title that's displayed. Otherwise, we display the default title. Save the file. Gatsby will now try to regenerate the content. You can see that down here. There's a bunch of stuff that's happened. And if I go back to the frontend, the site title is now mywpsite. Very cool. All right. So what happens if I go into WordPress and try to change the site title. So I'll go in here, go to Settings, General. Here we have the site titles. I'll call it My WordPress and Gatsby Site. Update that by saving the changes, go back to the homepage, reload the homepage, and look at that. My WordPress and Gatsby Site. So now, the site title is being generated by WordPress, then sent via GraphQL into my Gatsby site, and because I'm running gatsby develop right now, it's automatically updated in the browser.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (16), [[GraphQL]] (4)
> **Code Identifiers:** generalsettings (5), sitemetadata (4), defaulttitle (4), sitetitle (4)
> **UI Navigation:** go to (6), scroll down (1)
> **File Paths:** header.js (2), layout.js (2), gatsby-config.js (1)
> **Cross-References:** go back to (2)
> **Definitions:** is called (1), is a  (1)
> **CLI Commands:** find (1)
> **URLs:** [localhost:8000](https://localhost:8000) (1)


### 3. Working with Pages

[↑ Back to Table of Contents](#table-of-contents)

#### [Query pages in GraphQL](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-pages-in-graphql?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-pages-in-graphql?u=76281980&t=0)** - [Instructor] [[WordPress]] has two main types of content: pages and posts. They're separate in the WordPress backend and they behave slightly differently. Pages are meant to be static pages of content. That would be things like the about page or the contact page on a website or anything that doesn't really change. A good rule of thumb here is if you're going to put something on the main menu and you can click on it to go directly to that item, it's usually a page, it's not a post. The idea is when you make pages, you're making something sort of permanent that goes on the site forever, or at least is unlikely to change. Whereas a post is something more ephemeral, like a news item or a blog post or an article. When we work with Gatsby in WordPress, we can choose how we want to handle both posts and pages and they can be handled completely separately, completely independently. It's a good idea to stick with the formatting that WordPress already imposes on this content and use pages as static or relatively unchanging content and posts for more news type content or blog posts or whatever you want to call it. In this chapter, I want to focus on pages to show you how you can create pages in WordPress and then get them to display in Gatsby and use all the features of pages in WordPress in your Gatsby site. We start, as we always do, in graphical. This is our interface for all the data that comes out of WordPress. And since we're now talking about pages, we're either dealing with all WP page or just WP page, depending on whether we want all pages or just a single page. So let's look at all WP page
>
> **[1:32](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-pages-in-graphql?u=76281980&t=92)** and see what information we can get out of it. I'll open all WP page. Here we can choose how we want to organize the pages. So we can go for the edges. If you open edges, you'll see we have the node itself and then we have the next and previous. This makes a lot of sense if you're working with posts and we will use these features later, but it doesn't really make any sense in pages because pages are not posted in a chronological order and the order in which they were created isn't relevant. So we're going to ignore edges. You can also create custom groups of pages if you want to and this may be important if you have these parent/child relationships between pages, but we're not going to do that here. Instead we'll just go directly to the nodes. So these are the pages themselves. For each of the pages, we now have a ton of data. We have the author, then we have the author database ID, we have the children of the pages if the page is a parent and has children, we have comments for the pages, we have the date and the database ID, and all the other information that's stored in the WordPress database. So if we're looking for a page, we probably want the title, we probably want the URL, and we also want some of the content. So here, I'll say let's just do content and see what happens. So we'll run this query and here on the output, we get all WP page nodes and then we get the first page called sample page with the URI/sample-page and then this is the content of that page. Then we get the next page, which is in Greek. So here we have some Greek letters. We have a very complicated URL
>
> **[3:06](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-pages-in-graphql?u=76281980&t=186)** because this is a Greek letter URI. Then we have null content, so there's nothing inside this post. And then we can keep scrolling down and you see we have a full list of all the pages with all the content. Well, this is useful if we want a list of pages, but we very rarely want a list of pages. Instead, we want to find a specific page. And this is what's really neat about [[GraphQL]]. You can query for anything. So for example, if I want to get this page and I know what it's called, I know that the title is page markup and formatting, I can actually query for the title. So instead of going into all WP page, I'll go down here to just WP page to get a singular page. And instead of querying for the database ID that we did before, I'll query for the title. So I'll go down here to title and I'll say equal to and then just paste in the title. Now we have page markup and formatting. Then, I want the same information as before. So I want the content, I want the title, and I want the URI. Run that query and now I just get that single page. Now notice I didn't use the database ID to get this information. Instead, I use the title of the page. And that means if you know any content within any item, you are actually able to query for that item as long as there aren't two things that have the same name. So using the title like this is risky because you may have multiple things, but in the case, for example, with pages
>
> **[4:40](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-pages-in-graphql?u=76281980&t=280)** where you're unlikely to have multiple pages with the same name, you can, if you want to, query for the page name instead of query for the page ID.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (8), [[GraphQL]] (1)
> **Env Vars:** uri (3), url (2)
> **CLI Commands:** make (2), node (1), find (1)
> **Definitions:** is a  (2)
> **Analogies:** for example (2)
> **UI Navigation:** click on (1)
> **Best Practices:** rule of thumb (1)
> **Speakers:** - [instructor] (1)

#### [Generate pages automatically](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-pages-automatically?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-pages-automatically?u=76281980&t=0)** - [Instructor] Knowing I can gain access to all the pages on the [[WordPress]] site using [[GraphQL]], I now want to set up Gatsby to auto-generate a page for each of the WordPress pages. That way, if I add a new page on WordPress, it'll automatically appears in my Gatsby site as well. In doing so, I want to preserve as much of the WordPress functionality as possible, and that includes the permalink for the page itself, and also the overall structure and content of the page. I start this with a very basic query to give me all the pages in WordPress, all the nodes within those pages, and then I want the URI that is the WordPress-generated permalink and also the database ID so I can query each individual page. This gives me a list of all the available pages. Now I somehow have to tell Gatsby, "Hey, here's a list of all the available pages from WordPress. Can you please go grab each of the pages and then generate a new page for that inside Gatsby for me?" To do this, I need to write a node script. This is done in a file called gatsby-node. A node script allows Gatsby to perform some function over and over again, kind of outside of Gatsby and then instruct Gatsby on what to do with the output of that function. And that's what we need to do here. Tell Gatsby that here's a bunch of data from WordPress I want you to generate a page based on this list of pages. Now, when we make changes to a node script, we have to turn off gatsby develop because gatsby develop kind of gets stuck in these changes since gatsby-node sits sort of on the outside of the Gatsby functionality. So I'll go into my terminal here
>
> **[1:33](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-pages-automatically?u=76281980&t=93)** and hit Control + C to just terminate gatsby develop. Then I'll open up gatsby-node and we can take a look at what's here. Just to save a lot of time, I've already written out the script, and it's already available in the exercise files for this movie. It's just been commented out. So now I can uncomment the comments, and that way you can see what's going on. It's all sitting here under this comment called Generate pages. So if I just highlight all the text here and hit Command or Control + \ then it's uncommented. All right. At the very top, we invoke createPages and run an async function on it that pulls out actions, graphql, and a reporter. Then from actions, we pull out this method here, createPage. And the createPage method is surprise, surprise, the function that actually creates pages inside Gatsby. Then we pass a query to GraphQL and that query is allWpPage, nodes, databaseId and uri. That's the same query you saw in GraphiQL. This is the query that will give us an array of all the available pages from WordPress. If there are no errors, then we go down to this createPage function. So what I've done here is say, create a new constant called pages, that is the query result for data, pageQuery and nodes. So that will give us this list of each of the nodes from allWpPage. Then we set up a forEach loop on pages that generates a new page per item in that array. So here we have createPage,
>
> **[3:05](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-pages-automatically?u=76281980&t=185)** then we grab the path for the page, that would be the page.uri, that's URI WordPress generates. Then we use a component inside Gatsby to actually generate the page. We'll look at that in a second. And then we pass context. This is data we can pass from node to the template to use later. And the context we're passing is the database ID. That way we can tell the page template, "Okay, here's the ID for a page. Please go query the content of the page and generate the actual page." So that's gatsby-node. I'll save this file now that I've uncommented this section. And gatsby-node calls this file here, /src/templates/page.js that I've also added. So I'll go into src, templates and page.js. This is a bare bones template right now 'cause I wanted to show you how it works before we build it out. So what's happening here is we start off at the very bottom by sending a query to GraphQL. This query receives the database ID. This is that context we passed. The database ID that we got from our GraphQL query up here. Then it says, "Okay, based on this database query, we go to wpPage, databaseId equal to this databaseId we just got passed. And then pull out two items, the title and the content. Then we pass this data up to the component, which sits here. It receives the data. And then we create a new page that just sets up an article, sets page.title, and then page.content. You'll also notice I'm using this crazy parameter here, dangerouslySetInnerHTML.
>
> **[4:38](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-pages-automatically?u=76281980&t=278)** This is because the page content that WordPress is passing is pure [[HTML]]. And in the [[React.js|React]] world, you should never, ever, under any circumstances, display pure unsanitized HTML, except for this circumstance where we already know that WordPress has sanitized that data for us and we're simply redisplaying what WordPress says is secure. But this will not work, unless you set up dangerouslySetInnerHTML It's a quirk when you're working with two different systems that are both trying to protect you, they will sometimes try to protect you against themselves. That's what's happening here. All right, if we now run, gatsby develop again, gatsby-node we'll bring out a list of all the available pages and this page template will then generate all the pages for us. So let's see if it works. Open terminal, just clear it here and then say gatsby develop. Now because I've made changes to gatsby-node, Gatsby will likely say, "Oh, I need to clear my cache and do everything all over again." So it'll go on refetch everything from WordPress then regenerate all the content and generate our new pages so that we can see them on the front end. So this might take a little while to finish. Once the build is done, we can go back to the live version of our site and try to find the pages. And this is kind of interesting because now we have pages, but there's no navigation, there's no tools here for us to actually find the pages. But there's a trick. If you just go and put in some garbage URL that goes nowhere, you'll get an error page.
>
> **[6:12](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-pages-automatically?u=76281980&t=372)** And the error page will give you a list of all the pages that are available on the site. So you see all the pages that were generated. So here we have the Front Page. We have the About Page. Let's see, we have Page B. We have, let's see, the Greek page with Greek content. All the pages that were generated from WordPress now exists inside our Gatsby site. They're just not readily available. And also, because I'm not using a proper template, they just look like plain old HTML pages. There's no layout, there's no design, there's no header, nothing. But what we're seeing is, thanks to gatsby-node and this simple page template, we're now auto-generating pages based on what is available inside WordPress.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (16), [[GraphQL]] (5), [[HTML]] (3), [[React.js|React]] (1)
> **CLI Commands:** node (12), find (2), make (1)
> **Code Identifiers:** createpage (4), databaseid (3), allwppage (2), dangerouslysetinnerhtml (2), createpages (1)
> **Exercise Files:** template (6), exercise files (1)
> **Env Vars:** html (3), uri (2), url (1)
> **Prerequisites:** set up (3)
> **File Paths:** src/templates/page.js (1), page.js (1)
> **Tools:** terminal (2)

#### [Create a page template](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-page-template?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-page-template?u=76281980&t=0)** - [Instructor] At this point, Gatsby is auto generating a new Gatsby page for every [[WordPress]] page, which is great. However, as in this example, the pages that are being generated are plain [[HTML]] documents. There's no header, there's no style, there's no formatting. So we have some more work to do here. On the front page of the Gatsby site, there's already a stylistic template applied to the site. And I want to carry that over into my page templates as well. I want the header, the footer, and these margins on the side that prevent the content from spanning the full view port with. So let's see how we can move these stylistic elements over from the Gatsby page to the auto generated WordPress pages. This is one of the great things about working in Gatsby or any other template based system. Once you have the data connections in place and you're able to get the data you want onto the right page and in the right context, you can start building out the display of that content in any way you want. So what we'll do here is borrow some components from an existing page that we know work and place them into the new page template. The page in question is a static Gatsby page and those are always found under the source folder and a folder called pages. I'm looking for the index page. And if I scroll down here, you see in the component, we have two components that are being used, the layout component and the SEO component. Just for reference, in Gatsby, you typically use a layout component to wrap around all content and templates. The layout component is what calls in the header, the footer, the sidebar, the actual content,
>
> **[1:33](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-page-template?u=76281980&t=93)** and anything else that goes on the page. It's just a good wrapper to place around everything. So to use the layout component and the SEO component in our page, we need to first import them. We do that by copying these two import calls at the top here so I'll just paste them in. They automatically get grayed out in VS Code because we're not using them yet. So they're just sitting there being imported. Next, we need to actually use them. So I'll copy the call to layout here at the top, and then go to the return in my page component and paste them in. I need to close the layout component. I do that at the bottom here. And now we have valid HTML. Then we can take a look at the output. So now we are wrapping everything in the layout component and I'm just going to make a guess that that will give us the header and the footer. Then we also have this SEO component which populates the head tag inside the HTML document. And right now it has a bunch of properties defined. Title, description, image, and path name. These are all optional, and we can either let them be the default or we can change them into something else. So in this case, I want to change the title and we already have the page title down here. So I'll just put in the page title. That's coming from Graph QL. I'm going to remove the description. We don't need it for this particular template. And I also want the path name. Now I don't have the path name yet but if I go and change my Graph QL query, I can put in URI and that will give me the path name and then I can call it in up here. So I'll again, say page dot and then this time, URI, and that should give us the path name.
>
> **[3:09](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-page-template?u=76281980&t=189)** Finally, we have a Boolean indicating whether this is an article or not. Meaning, is this an index of articles or is this a singular item? And since this is a singular item, I'll uncomment this so it becomes an article. This is again for SEO purposes. Now I can save page dot JS. If I open my console, you'll see all the pages are now regenerated. And if I jump back to my browser, we now have the header, the margins on the right and left, and at the very bottom, the footer. So now all the stylistic components have been added in thanks to the layout component. That's cool. But what if I want to apply some custom CSS just to this page template? Well, in Gatsby and in [[React.js|React]], there's several approaches to this. The one I like the most is CSS modules because it allows me to write standard CSS documents and then append them to individual templates. And that's what we'll do here. We already have an example of CSS modules in the index page. You can see it right here. It says import asterisk as styles from and then a CSS module. So I'll copy that line and paste it in and then change the call here. So import all the styles as the name styles from a file. So in this case, I'll call it page dot module dot CSS. Then in the templates folder, I'll make a new file. Call it page dot module dot CSS. And in page dot module dot CSS, I'll just create a simple rule.
>
> **[4:42](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-page-template?u=76281980&t=282)** I'll say, article. Notice, this is a class, and sets the background color to pink. That's just so we can see what's happening. All right, now I can show you how this works. So what we're doing is saying import anything inside the page module's file as styles and then we can pull each individual class out and use them in our markup. So to use the markup, I say class name, this will transform into just class, is just in [[JavaScript]], the [[Microsoft Word|word]] class is a keyword that's protected. So we need to say class name anytime we refer to class in JavaScript. It's just a JS idiosyncrasy. Then curly bracket styles. That's the container that holds all of our styles. And then article, that's the style I just created. Save that, jump back in the browser, and look, now it has a pink background. Now of course, this is not the look I'm aiming for but it shows you in a very simple and practical way how we can use CSS modules to inject custom CSS into individual templates.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[HTML]] (3), [[WordPress]] (2), [[JavaScript]] (2), [[React.js|React]] (1), [[Microsoft Word|Word]] (1)
> **Env Vars:** css (10), seo (4), html (3), uri (2)
> **Exercise Files:** template (5)
> **Definitions:** is a  (4), is an  (1)
> **CLI Commands:** make (2)
> **UI Navigation:** scroll down (1), go to (1)
> **Tools:** vs code (1)
> **Speakers:** - [instructor] (1)

#### [Work with featured images](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/work-with-featured-images?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/work-with-featured-images?u=76281980&t=0)** - [Instructor] One of the many features [[WordPress]] offers for both pages and posts is the ability to add a featured image. That's an image that's appended to the post that can be pulled in out of context and displayed wherever you want. That's exactly what I've done here for this page. It's called Page Markup And Formatting, and I've added a giant featured image at the top. Now I want it to somehow transport this featured image over into my Gatsby page. Right now, the Gatsby page has no featured image. I somehow need to grab that image and place it into this template. And I have to do it conditionally because I don't know if each page will have a featured image or not and I don't want to populate the page with empty markup. This is an opportunity to look at one of the really interesting things about how Gatsby handles WordPress content. You see, when you run Gatsby Develop, everything, including all media assets like images, is imported and cached in the Gatsby project. That means we can treat any image or other media asset from WordPress as a proper Gatsby media asset. We don't have to reference them and pull them in from WordPress. They're actually sitting inside our Gatsby project and we can use Gatsby's image handling to process those images. For that, we use this plugin here, gatsby-plugin-image. This plugin was installed when we originally set up WordPress and Gatsby. You may remember in the wizard it asked, "Do you want to set up responsive images?" If you say yes to that, you get gatsby-plugin-image and all its associated plugins installed automatically. You can see them if you go to package.[[JSON]]. Here we have gatsby-plugin-image.
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/work-with-featured-images?u=76281980&t=94)** We also have gatsby-plugin-sharp and we have gatsby-transformer-sharp. And these are the three plugins that will allow us to treat the images as native Gatsby objects. Okay. So how do we do this? In gatsby-plugin-image, there are actually two different types of images. We have static images and dynamic images. Dynamic images are used any time we don't know anything about the image when it's coming in, which is exactly what we have here. We know there may be images. We don't know how they're going to be presented and we don't have any control over them. So let's look at the dynamic images example. We need to do the two standard things. First, send a query through [[GraphQL]] and then display the image in a component. So here is an example of how you would do that. You send a query to the blog post and then you say for the avatar, grab the childImageSharp and then the gatsbyImageData from the image and then you can set up properties about how you want that image displayed. For example, the width, how you want the placeholder to be displayed, and also the format. All right, so let's do just that. The first thing I need to do is find my page. And for that, I'll go and say allWpPage and then just query all the pages for database ID and title. That will give list of everything. And I should be able to find my page sitting down here. Page markup and formatting and ID is 1134. So I'll copy 1134, then I'll close AllWpPage and instead look for just wpPage. In wpPage, I am going to filter for the database ID. It would be equal to what I just picked up, 1134. Then for the page, I need to find the featured image.
>
> **[3:11](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/work-with-featured-images?u=76281980&t=191)** If I scroll down and look at the nodes here, there is a node called featuredImage and inside we have the full node. That's because in WordPress, each image that's uploaded to WordPress becomes its own post with all the information you would get from a post. So among other things, we have the alt text for the image, which I need, and then we have the actual file. So that's sitting here in the localFile. Now, you can think back to what we saw in the code example. I want childImageSharp, I want gatsbyImageData. And here you have all the configuration options you may want to use. So I can set the width. I'll set that to 1360. I can set the placeholder. So we'll set that to blurred. I can also set the height, if I want to, the JPEG options. In this case, I just want to set formats to auto. And that gives me the complete query for the featured image. So I'll copy that query out and go to my query here in the page template and just paste it in. Then I'm actually going to make one small change here. I'll change the format to an array and say AUTO comma WEBP comma AVIF, so I'm using all the modern formats if the browser supports them. All right, now that we have the query, we have the data we need, then we need to figure out how to display it. And that's also described here. To display the image, we import some content from gatsby-plugin-image. In particular, we import GatsbyImage, this is a component. And also the method getImage, which pulls image information out of the return from GraphQL. So I'll copy that, paste it in at the very top here.
>
> **[4:49](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/work-with-featured-images?u=76281980&t=289)** Then we need to actually use the component and get image. Here's a code example of that. So I'll place that where I want to image to appear inside the article. Now we have the GatsbyImage component calling for an image and then some alt text. This is where we use that other thing, the getImage method. So we apply the getImage method to the image data. Looks like this. So I'll say getImage, parentheses. And then inside the parentheses, I will now say page, that's our current page data, featuredImage, node, and localFile. That's the item that contains the image information and getImage will now pull out the actual image and display it in the component. Then I also need to set up my alt text, so I'll just copy this string here. Say page.featuredImage.node and altText, so that we get the alt text and save it so that it's easier to read. And then, because I know that this is going to display weird, I'm also going to apply a figure to wrap around it and set the class name for the figure to styles and then featimg. Close my figure and paste it in here. Then finally, I need to go to page.module and set up my styles so it'll look like this. And here I want to set the margin-block-start to 3rem,
>
> **[6:29](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/work-with-featured-images?u=76281980&t=389)** margin-inline-end to -2rem, and margin-inline-start to -2rem, just to line things up because it's now an article. All right. If we go back to the browser now and look at the page, you'll see here we have the featured image and it's working properly. However, this markup assumes the page always has a featured image and that's not always the case. So we need to add a conditional statement around this to make sure it only displays if there is a featured image, otherwise we'll get errors. So I'll set up a conditional statement here and say page.featuredImage and and, and then parentheses. What I'm saying here is if page.featuredImage is true, meaning we have a featured image, then do whatever is inside the parentheses. And what's inside the parentheses is this figure. So we'll paste in the figure. Now we have a conditional statement that will display a figure with a featured image inside when there is a featured image and nothing when there's no featured image.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (7), [[GraphQL]] (2), [[JSON]] (1)
> **Code Identifiers:** featuredimage (5), getimage (5), childimagesharp (2), gatsbyimagedata (2), wppage (2)
> **CLI Commands:** node (4), find (3), make (2)
> **Definitions:** is a  (4), is an  (2)
> **Prerequisites:** set up (6)
> **Env Vars:** jpeg (1), auto (1), webp (1), avif (1)
> **UI Navigation:** go to (3), scroll down (1)
> **Exercise Files:** template (2)

#### [Challenge: Add metadata to the page template](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/challenge-add-metadata-to-the-page-template?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/challenge-add-metadata-to-the-page-template?u=76281980&t=0)** (upbeat electronic music)
>
> **[0:08](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/challenge-add-metadata-to-the-page-template?u=76281980&t=8)** - [Instructor] Time for your first challenge. In this challenge, I want you to add [[Metadata]] to the page template. Starting with the exercise files for this movie, I want you to extend the [[GraphQL]] query to include the date and the author name. And then I want you to display that date and author name below the title in the template. This challenge should take you no more than 10 minutes. Once the 10 minutes are up, or once you've finished the challenge, go to the next movie and see how I solve the same challenge.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Metadata]] (1), [[GraphQL]] (1)
> **Exercise Files:** template (2), exercise files (1)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat electronic music) (1)

#### [Solution: Add metadata to the page template](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-add-metadata-to-the-page-template?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-add-metadata-to-the-page-template?u=76281980&t=0)** - [Narrator] Welcome back. Let me show you how I solve this challenge. My goal is to display the page author and publishing date down here, directly below the page title. Now, anytime I'm working with data, I always start in GraphiQL. That way I can dig through the [[GraphQL]] tree and find the information I'm looking for first, so I know I have a solid query to use. In this case, I'm looking for two pieces of data, and the first one is the author. So if I go into the page, there's the single page. We're just filtering for the current page we're working with. So wpPage, and then go look for the author node. You'll see, inside here, I have a bunch of information about the author. So what I want in this case is just the name of the author. So, I'll say author, node, name. That gives me the name of the author. Then I also need the publishing date. So here, there's an option called dates. You'll notice there's actually two, there's date and then there's date GMT. So if you want the date justified for Greenwich Mean Time or Global Medium Time, then that's an option but I actually want a date, so I'll just say date. Now I have a query, so I'll test it. And here you see, we have an author name, Theme Buster, and we have a date. Now this is not a human readable date right now. Well, it is but it's not what I want to display but I can use [[JavaScript]] to convert this into a nice looking date. So the query is correct, and I'll copy out the new pieces of my query here.
>
> **[1:33](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-add-metadata-to-the-page-template?u=76281980&t=93)** Then I'll go into my page template and down here we already have title, content, uri and featured image. So I'll just paste these in before there. So I'll say author and date. All right, now I need to use this data somewhere. So first I need to figure out where I want to place it. So here we have the page title and that means I can place it directly after the page title. Then inside here, I'll start by setting up some just plain text. I'll say by and then the author name. So that would be page.author.node.name. Right? Page, author, node, name. Okay, that should be easy enough. And then dot, so punctuation mark, and then published on. Now I want a space here, but because I'm going to break the line and then write some JavaScript, there's a chance that JavaScript will truncate it. So I'll actually put in a space like this. That way I ensure there is going to be a space between published on and the date. And the reason I'm doing that, is I now need to use JavaScript to convert that very long weird date string into a human readable date. And I'll do that by using a new date object. So I'll say new, date, then I'll grab our date, so that would be page.date. That's the date object we got from GraphQL. Then I'll use a method on the date object called toLocaleDateString. And here I need to define my locale. So that would be en-US 'cause I'm in Canada,
>
> **[3:08](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-add-metadata-to-the-page-template?u=76281980&t=188)** well, America, something like that. And then inside I can pass properties and options toLocaleDateString. So I'll say month:long. So that is the whole month name, then day:numeric and finally year:numeric.
>
> **[3:40](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-add-metadata-to-the-page-template?u=76281980&t=220)** All right. Now I'll save page.js, jump back to my browser and here directly under the title, it now says by Theme Buster published on March 15th, 2013. That means everything is working properly and I've completed my challenge.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (4), [[GraphQL]] (2)
> **CLI Commands:** node (4), find (1)
> **Code Identifiers:** tolocaledatestring (2), wppage (1)
> **File Paths:** page.js (1)
> **Env Vars:** gmt (1)
> **Exercise Files:** template (1)
> **Speakers:** - [narrator] (1)
> **Non-Speech:** (upbeat music) (1)


### 4. Custom Menus

[↑ Back to Table of Contents](#table-of-contents)

#### [Query custom menus in GraphQL](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-custom-menus-in-graphql?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-custom-menus-in-graphql?u=76281980&t=0)** - [Instructor] Now that our Gatsby site has pages, we need to give the user some way of getting to those pages because right now, unless they either know the URL to the page or they go to the 404 page and get a list of everything on the site, there's really no way to navigate to anything on our site. The easiest way of creating links to anything within a site is to create a navigation menu. And in [[WordPress]], we have have a really powerful tool for creating as many navigation menus as we like. You find it in the back end of WordPress under appearance and menus. From here, you can create as many menus as you like. And under each menu, you can set up as many menu items as you like. They can be in hierarchical order. You can reorganize them by just dragging and dropping the menu items and you can link to anything, either inside WordPress, so posts, pages, categories, tags, et cetera or outside of WordPress using custom links. If you have a link, you can add a link to a custom menu inside WordPress. Now in WordPress, each of these menus is assigned to a menu location. So on the front end, you would typically have maybe one or two menu locations in the current active theme I have. We have a primary menu location that goes at the top and we also have a secondary menu location that goes somewhere else in the site. This depends on the current theme you're using and this will become important in a few minutes. But first the key question, can we use these custom menus from WordPress in our Gatsby site? The answer is yes. And like with everything else in Gatsby, we start going to GraphiQL to see if we can query the information. In GraphiQL, if we look at the Explorer, you'll see we have two entries here: allWpMenu and allWpMenuItem. This first one, allWpMenu, points at the menu containers.
>
> **[1:36](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-custom-menus-in-graphql?u=76281980&t=96)** So if you create four different menus inside WordPress, you can query those four different menus but that only gives you the menu itself, not what goes inside. So in most cases, we will be looking at allWpMenu item cause we're looking for the actual menu items, the links that we are going to display. So let's see what happens if we query allWpMenu item. I'll open it up, go to nodes and then get some information. So let's get the let's see database ID for each menu item, maybe the parent's database ID if they are in some sort of hierarchical order and also the URL and the label. The label is what you see as the menu text. So let's run this query and as a result we get nothing. Okay, this is an idiosyncrasy in WordPress. You see, we have a set of menus defined. In fact, in the theme unit test data, there's a bunch of different menus defined. But because none of these menus are assigned to the front end, meaning assigned to a theme location, they're all private to WordPress. You can see them if you log into the backend of WordPress but they do not exist outside of the backend. To make any of these menus public, we have to assign them to a theme location. That can be done either by going into the menu itself, going down to the bottom to display location and select a location here or you can go to the managed locations option and then assign a menu for each location. And this is important because if in your Gatsby site you want to have let's say three menus, you then need to find a theme that has three menu locations so that you can actually display them
>
> **[3:10](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-custom-menus-in-graphql?u=76281980&t=190)** on the front end of WordPress and they become available to Gatsby. In our example, I'll set all pages as a primary menu and the social menu as the secondary menus just so that we get a bunch of menu items. Now I'll save this change in WordPress, go back to GraphiQL and you would expect the menu items to appear now that I've run GraphiQL again, right? But when I do, again nothing happens. So what's happening now? This has to do with how Gatsby talks to WordPress and what WordPress tells Gatsby about what's going on. In this particular circumstance, the change I made in WordPress is not being monitored by Gatsby. So even though I changed the status of these menus, Gatsby doesn't know so it can't update anything. In fact, to get this change to be validated inside Gatsby, I need to first stop Gatsby develop then run Gatsby clean to clear out the entire cache and delete everything Gatsby knows about WordPress and then run Gatsby develop again. Now, when Gatsby goes and re-queries all the data from WordPress, it'll discover, oh there's a change about the menus. I better map out all the menu items. You can actually see it here. Fetched 23 menu items. And from there, we can now query the menu items. This is actually something you need to do every time you make a change to the display of the menus. So now you can go and remap the menus in WordPress and that will take effect. But if you want to say make a menu private again or make another menu public, you have to go back, stop Gatsby develop, run Gatsby clean
>
> **[4:44](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-custom-menus-in-graphql?u=76281980&t=284)** and then rerun this entire process. All right, now that Gatsby knows about the menu items, we can run the query again and we get a list of menu items. Cool right? Okay, the problem now is we get all the menu items regardless of which menu they belong to and you'll notice that they're in one stacked order. So there's no nesting going on here. They're just all listed together. We'll talk about the nesting part later on in the course. For now, we'll talk about how to get the menu items from just one menu. What we need to do here is filter the allWpMenuItem based on which menu each it sits in. And we can do that using the filter option in GraphiQL Under allWpMenuItem, we go to filter. Then we choose what we want to filter for. In this case, we want to filter based on the menu, that is the menu container. Then under here, we want to grab the node, so each of the menus and say we want to filter based on the slug. The slug is the name of the menu itself. So what is the slug of the menu? In WordPress, the slug of the menu is the name of the menu itself with just dashes where the spaces are. So if for example I want to filter for social menu, meaning social-menu, the slug should be equal to social-menu. Run this query again and over here, I now only get the menu items from the social-menu. That means I can now query all the menu items that are public from WordPress and I can filter based on which menu they are sitting under.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (21)
> **CLI Commands:** make (4), find (2), node (1)
> **Code Identifiers:** allwpmenu (4), allwpmenuitem (3)
> **UI Navigation:** go to (4), navigate to (1)
> **Env Vars:** url (2)
> **Cross-References:** go back to (1)
> **Definitions:** is an  (1)
> **Analogies:** for example (1)

#### [How WordPress manages menu items](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/how-wordpress-manages-menu-items?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/how-wordpress-manages-menu-items?u=76281980&t=0)** - [Narrator] Before we start working with these [[WordPress]] menus in Gatsby, Let me briefly explain how WordPress itself handles the menus. When we look at a menu like this one called All Pages, you can see there are hierarchical relationships between the different menu items. We have the All The Tests menu item, and then underneath we have Page Alignment, and these other items. And you can manage this by simply grabbing an item and pulling it to the side. You can see here now I made three levels and you can pull them in and out, and then you can change the order. What's actually happening when I do this, in the database is there's an ID attached to each of the menu items, and then there's a possible parent ID attached to each menu item. So when I create these hierarchical relationships what I'm really doing is just adding a number to the parent ID field for each menu item. This is important when we start querying the menus from Gatsby through [[GraphQL]] because when we send a query to say, All Pages, what we get is a flat structure. All the menu items are just listed, one after the other in one response, we don't get nested hierarchies here. There is no array, within array, within array. It's just one big array with all menu items. So if we want to preserve the hierarchy, we need to go find out which menu item is a parent of which other menu item. And we can do that using these two fields here. Database ID, which is the ID for the current menu item, and parent database ID, which is the ID for the parent item. if there is hierarchical structure. What you see on the list I currently have here
>
> **[1:33](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/how-wordpress-manages-menu-items?u=76281980&t=93)** is these items that have parent database ID zero are first level on the menu. So they are the roots level of the menu. But if we scroll down, here we have one item, with the ID 1770 it has parents database ID zero, and then below we have a series of items where parent database ID is set to 1770. Meaning these items are the children of this item up here. However, like I said, in the structure we're getting from WordPress, they're all sitting in one array. It's a flat array. There is no nesting going on. So if we want to create the nesting, we have to run a function to convert this data into nested data. We will cover this later on in the course, but it's important to know upfront, because it explains how we query data, and also showcases how we need to work with the data we get from WordPress, to alter it into what we need for our purposes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (4), [[GraphQL]] (1)
> **Definitions:** is a  (2)
> **CLI Commands:** find (1)
> **UI Navigation:** scroll down (1)
> **Prerequisites:** before we start (1)
> **Speakers:** - [narrator] (1)

#### [Create a flat menu in Gatsby](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-flat-menu-in-gatsby?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-flat-menu-in-gatsby?u=76281980&t=0)** - [Instructor] Let's start by building a flat, single-level menu that will go across the top here in the layout. That menu will be based off this menu here inside [[WordPress]] that actually is a hierarchical menu with subitems, so I somehow need to filter out those subitems for now and then maybe add them in later on. Also note, I've added one custom link to my menu. It just points to [[LinkedIn]] Learning. This is to show you how we can co-mingle links to the internal site with links from external sources into one single menu. The first step is to figure out how to filter out these menu items so I only get the main menu items the first level of the menu. You see right now when I'm sending this query here, I'm getting all the menu items, including the child items. The key here is to filter not only based on the name of the menu itself but also based on the parentDatabaseId of the menu items. So here, I'll go in and say I want to filter based on parentDatabaseId, and I want to set that equal to 0. It's a little hard to see over here, but what we can see is we're filtering based on menu, node, slug, equals all pages, and then also parentDatabaseId equals 0. If I run this query now, you'll see that this huge list of menu items is reduced to a list of just six or seven menu items which is much better. This is what I want to work with. All right, so now that I have a query that works that's giving me the results I want, I need to carry that query over into some component inside my Gatsby site. Just to save some time,
>
> **[1:32](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-flat-menu-in-gatsby?u=76281980&t=92)** I already built out this component for you, and you can find it in the Exercise Files. It sits here under the components folder. It's called MainNav. There's also a MainNav module for CSS for this component with some basic styling. Let's look at the MainNav component. Off the top here, in the component itself, you'll see I'm using a static query. This static query is different from the regular query we were using when we were building out the pages because this is a component, and components can't use page queries. You'll also notice, because we're using a static query, the query is sitting inside the component, whereas when we were doing a page query, it was sitting on the outside of the component. This is a Gatsby-specific thing. If you want to learn more about it, just go check out the Learning Gatsby course, and it's all explained. The query in question is the same query we had in graphical. We are filtering the result based on the all-pages menu. Then we are also filtering out only the menu items that have the parentDatabaseId set to 0. And I'm using the sort here just to sort the order based on the field order in ascending order. Then for each of the menu items, I'm pulling out two pieces of data: the label which is the name, the text that's displayed in the menu, and the path which is the URL. Now, notice I'm remapping the label to the [[Microsoft Word|word]] title here. That's just so that it's easier to understand in the code because I find that the term label sounds like a label appended to something, but it's actually the title that is in the menu item. So this is just a remapping done in [[GraphQL]]. It doesn't actually change the data. It just changes the property name of the data. Then once we have the results, we place it in a constant.
>
> **[3:06](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-flat-menu-in-gatsby?u=76281980&t=186)** We grab wpMenu item and then allWpMenuItems and nodes. This gives us the array of all the menu items. Then we output in the return a nav with a className mainnav. Inside the nav, there's a ul. And then we used a map function to map through all the menu items, grabbing each menu item, giving it an index number, and then returning an li with the index as the key. This is a requirement from [[React.js|React]] so that we can target individual items later. And then we use this component here, UniversalLink, to grab the menuItem.path, that is the link, and also the menuItem.title, the text that goes inside the menu item. And finally, there's this piece here: activeClassName equals current-page. This is a really fancy function inside the Gatsby Link component that automatically applies a className to the link if we are on the page the link is pointing at. All you need to do is add this in: activeClassName, current-page. And when you are on the page that a menu item is pointing to, you'll get a class called current-page appended to that link which is really fancy and really easy to work with. The question you probably have right now is what is UniversalLink? That's a new component I've added to the utilities folder. It's found here. This component is a standard component that's often used if we have links and we don't know if they are internal links to the Gatsby site or external links to some other source. So the component I just borrowed from someone else's work, it basically just goes in and says if the link is internal to the Gatsby site, then I'll put a standard Link component
>
> **[4:40](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-flat-menu-in-gatsby?u=76281980&t=280)** with all the information. If, on the other hand, if it's pointing somewhere else on the web, then I'll put a standard a with an href that points to the same link. This allows us to manage both internal links and external links. Okay, so this is the MainNav component that does all the work. Now we need to see how we can display the MainNav component on the front end. That would be done somewhere in the site. So I'm just going to pull it, place it in the header. Here, all I need to do is import the MainNav from MainNav and then place it somewhere within my layout. So I'll go down to the bottom of the return here and place it here. Now, the MainNav component is self-contained. This does all the work so I don't need to do any settings or change anything. I'll just save this file, jump to my browser, and I might have to reload the browser page, and here is the menu, flat-level menu based on the structure that's in WordPress. And if I click on any of these links, I'm automatically taken to the internal page for that link. And if I click on LinkedIn Learning, I'm taken outside to LinkedIn Learning. So now everything is working properly. And this menu here is now controlled from WordPress, meaning if I go in and change the layout here, I may have to go and restart gatsby develop, but once I do, those changes will also take effect inside my Gatsby site.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (3), [[LinkedIn]] (3), [[Microsoft Word|Word]] (1), [[GraphQL]] (1), [[React.js|React]] (1)
> **Code Identifiers:** parentdatabaseid (4), classname (2), menuitem (2), activeclassname (2), wpmenu (1)
> **Definitions:** is a  (6)
> **UI Navigation:** in the menu (2), click on (2)
> **CLI Commands:** find (2), node (1)
> **Env Vars:** css (1), url (1)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)

#### [Create a nested menu in Gatsby](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-nested-menu-in-gatsby?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-nested-menu-in-gatsby?u=76281980&t=0)** - [Instructor] Single-level flat menus are easy to work with, but they're not always what we need. In many cases, we need a hierarchical menu either as a dropdown or just a straight up hierarchical list so that we can show the relationships between different menu items. Now, [[WordPress]] makes it easy to create hierarchical menus. As you've seen inside the menu builder, you can simply add a menu item and then pull it aside to make it a hierarchical menu item that is a child of another menu item. And you can do this in as many levels as you want. So in this example, we have three separate levels. Now we need to somehow translate this hierarchical information into Gatsby. That's challenging, because as you've seen, when you query the menu items in Gatsby through [[GraphQL]], all you get is a single-level array. There is no hierarchies here. Instead, we have these parentDatabaseId entries on some of the items that point to a previous item. And that's what's telling us there's a hierarchy going on here. So to output the content in Gatsby, we need to somehow parse this data and reformat this flat array into a multidimensional array, a hierarchical array, so that we can see the relationships. The good news is this is a known problem with WordPress and working in Gatsby, so there is a solution. The people who built WPGraphQL created a function that takes a flat array and turns it into a hierarchical array based on this information. The function takes three values: the idKey that you can pass in as any key, the parentKey which you can pass in as any other key and then the childrenKey which is the name that you apply
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-nested-menu-in-gatsby?u=76281980&t=94)** to the new arrays that are created. In the exercise files for this movie, I've added that function in as a new component in our project, and it sits under the utils folder as the component FlatListToHierarchical. This is pretty much exactly the same quote example. I've made some very, very minute changes just to make it work properly. And as you can see, it's grabbing an idKey, a parentKey and a childrenKey, and then saying for each item, is your parentId zero? If so, do nothing. If it has any other value, then creates this hierarchical array and sort the items under. This is just some really fancy [[JavaScript]] that basically turns a flat array into hierarchical array, just like the name suggests. Now that we have the component, we can use it in MainNav. So let me show you what's going on here. Again, I've updated the example a little bit to add in the new functionality, so we don't have to write out all the code by hand. The first thing I've changed is up here in the query. I've taken out the filter that said only give me the first level, 'cause we now want the whole level. And I also added in the parentId in my query so that I not only have the current ID for the current item, but also its parentId. Now you'll notice that I'm querying id and parentId, not databaseId and parentDatabaseId. It doesn't matter. You can do either or, just be consistent. So either do id and parentId or databaseId and parentDatabaseId. It'll work exactly the same way. This produces our flat array. And I'm outputting that down here in the console log just so we can see that there is a flat array. Then I create a new constant called headerMenu. This constant is filled with FlatListToHierarchical,
>
> **[3:11](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-nested-menu-in-gatsby?u=76281980&t=191)** my component and I'm passing in this flat array. In addition, I'm also telling FlatListToHierarchical what I want to use as data here. And I want to use the id as the idKey. I want the child menus to be called children. And I want the parentKey to be parentId. So now the flat array goes into the component and comes back out again as a hierarchical array. And I capture that here by console logging out the headerMenu constant. This headerMenu constant is now our hierarchical array. We can take a look at this in the browser in the console right now. So I'll go into my post, Inspect, open a console. And here, if I reload to just clean it up, you'll see we have two arrays. We have the raw data. This is the array with 19 items. This is the flat array. And then below, we have another one called headerMenu. This one only has eight entries. That's because the top level is just eight items. And if we go into a sub item here, you'll see this one for example has children. And the array here is five, so there are five children inside. And if there were further sub menus, each of these items could have its own set of sub menus, also called children, et cetera, et cetera. So now we have a hierarchical array to work with. Then we need to somehow output that data up here in the menu. And that's done using this new component called MenuLoop. So what I've done here in the return is I've simplified our return by saying create the nav as before, then check, does headerMenu have items in it? So is the headerMenu length more than zero? If so, run MenuLoop on headerMenu.
>
> **[4:45](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-nested-menu-in-gatsby?u=76281980&t=285)** MenuLoop is a component that sits all the way up here at the top. And inside MenuLoop, we set up our unordered list. And then we do what we did before, which is iterate through each the item to say, for each item, create a new list item. Then for each list item, we give it a key. And we also query to see, if the menuItem's children is longer than zero, then also add has-submenu, otherwise do nothing. Then we set up the UniversalLink for the item, and we close our list item, and then we close our unordered list. But what about the sub items? Well, this is where that kicks in, and this is also why I've created a separate component for it. We are going to make this component recursive so that it uses itself to generate new menu items. Let me show you. If we have new menu items, we want those new menu items to appear as a new unordered list inside the current list item, right? That's how we get the valid [[HTML]]. So what I'll do here is say, if we have menuItem.children.length is bigger than zero, meaning this current list item has children, then I want to recursively call MenuLoop again. So I'll say MenuLoop, so that's the same component we are currently inside, and set menuItems equal to the current menuItem.children
>
> **[6:13](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-nested-menu-in-gatsby?u=76281980&t=373)** and close MenuLoop. So what happens now is, if there are children, we go back and start a new MenuLoop that outputs a new unordered list that maps through all the items within that new unordered list, outputs to content, does the same check, are there children here? If so, boot up MenuLoop again. And we get this nice recursive loop using this single component that will output our hierarchical lists for us. Save this. Check this in the browser. And you'll now see we have a properly structured, multilevel HTML list. Here we have all the top level items. And if there are sub items, they are listed under the correct item. That way we have our multidimensional menu, and everything is working properly. Now, granted, it doesn't look great right now. There's some CSS missing. But the overall functionality works, and that's the whole point.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (2), [[HTML]] (2), [[GraphQL]] (1), [[JavaScript]] (1)
> **Code Identifiers:** headermenu (7), parentid (6), parentdatabaseid (3), idkey (3), parentkey (3)
> **Definitions:** is a  (6)
> **CLI Commands:** make (3)
> **Env Vars:** html (2), css (1)
> **UI Navigation:** dropdown (1), in the menu (1)
> **Analogies:** just like (1), for example (1)
> **Prerequisites:** set up (2)

#### [Challenge: Create a footer menu](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/challenge-create-a-footer-menu?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/challenge-create-a-footer-menu?u=76281980&t=0)** (upbeat intro music)
>
> **[0:08](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/challenge-create-a-footer-menu?u=76281980&t=8)** - [Narrator] Time for another challenge. In this challenge, I want you to create a footer menu based on one of the menus in your [[WordPress]] site. To start off, take the exercise files for this movie and augment the header menu so it only displays the top-level menu items. You don't need the drop-downs, just the top-level. Next, create a footer menu component. And in that footer menu component, create a [[GraphQL]] query to display all the menu items for one of the menus in a hierarchical bullet list in the footer. Take 10 minutes to solve this challenge. Once the 10 minutes are up, or you solve the challenge, go to the next movie and see how I solved it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (1), [[GraphQL]] (1)
> **UI Navigation:** go to (1)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [narrator] (1)
> **Non-Speech:** (upbeat intro music) (1)

#### [Solution: Create a footer menu](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-footer-menu?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-footer-menu?u=76281980&t=0)** (upbeat electronic music)
>
> **[0:07](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-footer-menu?u=76281980&t=7)** - [Instructor] Welcome back. Let me show you how I solve this challenge. Actually first, let me just clean up this header menu 'cause it takes up a lot of space right now and I'm not going to spend the time to add the CSS and [[JavaScript]] necessary to make a proper responsive, accessible dropdown menu in this course. So I'm just going to reduce this down to a single level menu again. The easiest way to do that is just to modify our [[GraphQL]] query. That way, we retain all the code we wrote and we just choose whether or not we want to display the sub items or not. You'll remember previously I added a filter for the parent database ID and set it equal to zero so I'll just add that back in here, parentDatabaseId: { eq: 0 }.
>
> **[0:56](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-footer-menu?u=76281980&t=56)** That way we only get the top level menu items and we're back to a flat structure menu. All right, now for the challenge. We want to add a navigation menu in the footer that displays all the links. Anytime I try to create something new in any project, I try to be as lazy as possible because in many cases, if you've already built something, you can probably just copy that thing and make some small modifications to it to make it do what you want in the new instance and that's exactly what's going to happen here. So I already have MainNav and MainNav is fairly generic in how it works. It's just outputting a nav with an unordered list inside and then some hierarchical output. So I should be able to use pretty much the same code to output my footer menu as well. Let's try that. So I'll grab MainNav here and copy it and then just paste it back in again so I get a new file called MainNav copy. Then I'll rename that one FooterNav because my naming convention is extremely literal. Now that I have FooterNav.js, I'm going to close MainNav so I don't accidentally start working on it. Here inside FooterNav, I need to make some naming changes. So we have the name of the component, MainNav here, and if I hit shift + command + L or shift + control + L, I can then change any instance or any occurrence of that name in the entire file. So I'll change it to FooterNav. So now we have a new component called FooterNav. In addition, for the FooterNav, I don't want to have a separate style sheet so I'm going to remove the reference to the style sheet here
>
> **[2:32](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-footer-menu?u=76281980&t=152)** and also up at the top because you now see it's grayed out since we're not using it, I'll take that out. And then finally, I'll take out this parent database reference because I actually want to see all the menu items. That's all I'm going to do. That is the totality of my new component. In fact, after doing this, I'm standing here looking at it thinking I should probably have made a more generic menu component and then just pass in the queries I want to do. But that's for later. The whole point here is to show you once you do something once, you can probably just repeat the same thing over and over and over again, which is what I'm doing. So we have the FooterNav component. Now we need to use it somewhere and the obvious place to use this is in the footer. So I'll go to the footer and import the component, import FooterNav from "./FooterNav". And then we need to use it and here we have a footer and then a div with some information so we should probably use it right before the footer. So we'll put in the FooterNav here. Save Footer.js, jump back in the browser. You may have to reload it and when you scroll to the bottom, look at that. Here's the full hierarchical menu. It looks kind of strange though. It's center aligned. That's not what I want. So we need to add some CSS. So we could of course, create a new style sheet for this but since all I want to do is left align the text, I'm going to perform a minor [[Web Standards]] crime here and just inject some inline CSS. So I'll do that through JavaScript, style equals, two curly brackets
>
> **[4:07](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-footer-menu?u=76281980&t=247)** and then say text align, colon left. That adds the inline style to the menu itself. And look at that. We now have a left aligned menu. Very cool. Now just a caveat, if you saw this and thought, hey, but we have logical properties now. Can't you use logical properties instead? Technically you can. Or you should be able to do it. So if I put in inline start here, it should work, technically, but it doesn't work in reality because the parser that is running through all my content doesn't yet support inline start which is why I end up using left instead. So that will change in the future but for now, it's not supported and it will be treated as incorrect code and not output. So therefore, now we have it using old CSS. That's it. That's the solution to this challenge.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2), [[GraphQL]] (1), [[Web Standards]] (1)
> **CLI Commands:** make (4)
> **Env Vars:** css (4)
> **File Paths:** footernav.js (1), footer.js (1)
> **UI Navigation:** dropdown (1), go to (1)
> **Code Identifiers:** parentdatabaseid (1)
> **Warnings:** caveat (1)
> **Speakers:** - [instructor] (1)


### 5. Working with Posts

[↑ Back to Table of Contents](#table-of-contents)

#### [Query posts in GraphQL](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-posts-in-graphql?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-posts-in-graphql?u=76281980&t=0)** - [Narrator] Posts are the main content type in [[WordPress]]. Usually when you go to a WordPress site, you're looking at the post, or the post index from that WordPress site. And when you use WordPress data in Gatsby there's a good chance you'll want to use posts and all of their features in your Gatsby site as well. So if I go through a post here, let's say this one, Template: Sticky, I get the post title, I get the post content, there's navigation below here to the previous post, or the next post. And I can also see which categories and tags are applied to this post. All of this information is information I may want to use in my Gatsby site as well. In this chapter and in the next chapter, we'll look at how to make all of this work. As you made your way through this course, I hope you've started noticing a pattern in how the [[GraphQL]] data is set up for WordPress. Every time we're looking for a new content type we always have the same structure. So for example, we have All WP Page, and we have the singular WP Page. We had all WP menu and all WP menu item, and then below we have WP menu and WP menu item. And the same thing goes for all other content types, including posts. So here we have all WP post and further down we have WP post, which gives us either the list of all available posts, or a singular post based on the ID. So let's take a look at the post object we can get from GraphQL. If I open all WP post and go down to nodes, I get all the information about each post, and I mean all the information. From here, we not only get the database ID, the dates and the title,
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-posts-in-graphql?u=76281980&t=94)** we also get the contents, the URI, the comments, the categories, the author information, the tags, and any other information associated with this post. When I run a query, all nodes, I get a list of all available posts on the site. And as you can see, there's quite a lot of posts here to choose from. And I can now structure my data to pull out exactly the information I want, and only the information I want. But on the WordPress site I showed you, it's very common in a WordPress site to be able to navigate to the previous or next post. If you go through the list of information here you'll quickly notice there is no information about the previous or next post. So how is that done? The first thing you may think of is, "Well, you just grab the database ID for the current post and you either subtract, or add one, and then you should get the previous or next post." But that's not always the case, because the database stores all information, posts, pages, menu items, images, anything else, so you don't really know what the previous or next database ID is going to be. Instead, WordPress needs to tell you what is the previous or next post. It doesn't do that in the nodes, but it does give you this information in GraphQL if you go to edges instead. Take a look at this. If I open edges, you'll see in addition to the node, which is the current post, we also have next and previous. And if I open next and previous, you'll see inside here sits the full post object for the previous or next post, the exact same information, and all of the information for this post. That means if you use the edges entry point you can not only display the information
>
> **[3:08](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-posts-in-graphql?u=76281980&t=188)** for the current post, but also provide information from the previous and next post, that could be either navigation, or it could be actual content like the featured image, the title synopsis, the full text, whatever you want. This gives you the ability to create navigation between posts without having to go through a menu, or go through a category or something else. And like I said, all the information is contained within this post object. You can go in and find the author, and the link to the author archive. You can go in and find the categories, every single category and all the information about each category you can go in and find the tags, the featured image, anything else you want, it's all stored here. So moving forward, when we are working with indexes, we'll be working with edges, because this gives us access not only to the current post, but also to the next and previous posts. But if we're working with individual posts, we'll still be working with WP posts. In this chapter we'll look at how to auto-generate pages for each of the posts in your WordPress site, how to add categories and tags through these individual posts, how to create index templates for posts, so that you can get a list of all available posts and then navigate into individual posts. And I'll give you a challenge on how to create post navigation for each individual post. Then in the next chapter, we'll look at how to create category and tag indexes, so you can actually navigate through the content in different ways on the site. All right, let's get cracking.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (9), [[GraphQL]] (3)
> **CLI Commands:** find (3), make (1), node (1)
> **Cross-References:** in the next (2)
> **UI Navigation:** go to (2)
> **Env Vars:** uri (1)
> **Exercise Files:** template (1)
> **Analogies:** for example (1)
> **Prerequisites:** set up (1)

#### [Generate posts automatically](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-posts-automatically?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-posts-automatically?u=76281980&t=0)** - [Instructor] In the context of Gatsby, [[WordPress]] is just another [[GraphQL]] data resource. And that means once we have a model for pulling one type of data and we wanted to pull a different type of data that looks pretty much the same, we can use that same model again. Case in point, earlier in the course, we set up functionality to pull out all the pages from the WordPress site and generate new pages in the Gatsby site. Now we want to do the same thing with posts, and the process is pretty much identical. In the exercise files for this course, I've made some small changes to the page.js file, and then I copied the file and created a new file called post.js, which is pretty much identical. The only real difference is I've changed the name of the component from page to post. I've also changed every reference to the data from page to post. And if we scroll down to the bottom, you'll see here, instead of querying wpPage, I'm querying wpPost. All the data that's being pulled out is the same as the data we've worked with before, and the only other main difference is I've changed the style sheet we were referring to earlier from page.module.css to single.module.css, and expanded in a bit, so we now have a little more style, and I've used one single style sheet to apply to both pages and post. Everything else is exactly the same as it was before. Now we need to use this template to generate new posts, and that's done using a GraphQL query. The query is a little bit different from what we had with pages, because posts are structured differently. So here I'm querying allWpPost, and then I'm sorting the result based on the date, and I'm ordering it in descending order. That way we get the latest post first,
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-posts-automatically?u=76281980&t=94)** and then all the posts are listed in reverse chronological order. This is what you expect anytime you're working with blog posts. Then, instead of querying the nodes directly, I'm querying the edges. That way, later on when we add navigation to the previous and next post, we have that information available inside the edges. You'll remember here we have the next and previous posts. Alright, to start this process, I'll copy this entire query, and then go to Gatsby-node. Here, we already have a page query fragment, so I'll create a new fragment for posts, and call it postQuery. Then I'll paste in my query here. That means we have all the data we need. Then we set up the function to generate the pages. So here I'll copy the existing function for generating pages, paste in the copy, and then change all the references from page to post.
>
> **[2:30](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-posts-automatically?u=76281980&t=150)** Once I'm finished that process, I need to change some of the functionality here. You'll remember we are now querying the edges, not the nodes. So that means we need to look for queryResults, data, postQuery, and edges. That also means down here in the createPage, it's not enough to refer to the post, 'cause the post now holds the edges and I need the nodes inside each of these edges. So that means I need to say the path is post.node.uri. Same down here, post.node.databaseId. And there's one more thing I need to do. If we go back and look at the results from this query, you'll notice the URI for the posts starts with a slash and then is the post name. Now, that means, since we already generated pages, there is a small chance that a page and a post might end up with the same URI, and that will cause a collision inside Gatsby and it will not work. So we need to put a prefix in front of the posts to make sure that there's never a collision between pages and posts. The easiest way to do that is to create a string here, and then turn post node into a template literal. So I'll say the string should be posts, and then we put in the template literal, and that way each of the URIs will be posts and then /hello-world, or /block-image, or slash whatever the post URI is. That way there's a clear separation between pages and posts. Okay, this is all we have to do to generate the posts. I can save [[Node.js]], go into the terminal and stop Gatsby develop
>
> **[4:05](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-posts-automatically?u=76281980&t=245)** because we've now made changes to node.js. Just for safety, I'm also going to clear all the Gatsby cache at this point. So I'll say Gatsby clean. Once that process is done, I'll rerun Gatsby develop. Now, because I cleared out the cache, this may take a while, depending on how big the site is and because Gatsby's now generating a whole slew of new pages. Once the Gatsby site is spun up we can now jump back to the browser, and, like with pages, we currently don't have links to any of these posts, so the easiest way to get to the posts is to just enter a garbage URL. This will give us a list of all available pages in the Gatsby site. So here we have all our regular pages, and then below we now have a new list of all the posts. And here you also see why I use the WordPress theme unit test data. It gives me all types of data that I can then experiment with. So for example, here we have posts/markup-[[HTML]]-tags-and-formatting, where I can check that all the HTML tags and formatting are styled in correctly. If I go searching for image, I can also find image posts. So here's a posts called block-image, and if I go there, you can see images. And there are all sorts of other data that you can test. But what's important is now Gatsby is generating a page for every single post, and they're all placed under this posts folder.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (3), [[GraphQL]] (2), [[Node.js]] (2), [[HTML]] (2)
> **CLI Commands:** node (6), make (1), find (1)
> **Code Identifiers:** postquery (2), wppage (1), wppost (1), allwppost (1), queryresults (1)
> **File Paths:** node.js (2), page.js (1), post.js (1), page.module.css (1), single.module.css (1)
> **Env Vars:** uri (3), url (1), html (1)
> **Exercise Files:** template (3), exercise files (1)
> **UI Navigation:** scroll down (1), go to (1)
> **Definitions:** is a  (2)

#### [Add category and tag info](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/add-category-and-tag-info?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/add-category-and-tag-info?u=76281980&t=0)** - [Instructor] One of the key differences between posts and pages is posts are organized using categories and tags. The post edge case, many categories from the [[WordPress]] theme unit test data, serves as a great example of this. By applying categories and tags to posts, a user can come to a single post like this one and see what categories and tags it's organized under then click on any one of the these categories or tags to go to an index for all posts under that category or tag and see related content. Now, right now, none of this information is being displayed in our post template. So we need to change the template to carry over that information. Category and tag information for each post is available through [[GraphQL]], we just have to ask for it. So if we go to grab a single post like this one, 1152 which is educates many categories, in addition to requesting the standard information, like the title and database ID and the content, we can also scroll down and find other information like categories. Here, there's a separate set of categories with a separate set of nodes. And from each of these nodes, we can then pull, for instance, the link and the name. I can do the same for tags as well, scroll down and find tags. Again, under nodes, request the link and the name. When I run this query you'll see this post is associated with a lot of categories and a couple of tags. So in addition to our title and database ID, we now have a categories object with a nodes array containing all of the categories associated with this post. And inside each node, we have a link and a name we can use. Same thing for tags, scroll all the way to the bottom. And you'll find a tags object with a nodes array with each of the tags.
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/add-category-and-tag-info?u=76281980&t=94)** Now we can carry this information over into our template using the query and then display it on the front end. To do that, I'll copy out the query. So categories and tags. Find the query inside post.JS. And here I'm going to add the categories and tags directly under the author. You can place it anywhere you want it doesn't matter for how you're working with the data, is just structurally easier to look at this way because of this giant featureImage thing at the bottom. Now we have the categories and tags and we can iterate through the data to pull the data we want. Now of course, the challenge is both categories and tags. There are many of them. So we need to map through this object and output each individual category in each individual tag. So let me show you how that's done using the tags first. The first step is to figure out where to place it. So I'll put it at the very end here right before the end of the article. I'll set up a new div, inside this div I'll just put out some texts, I'll say tagged colon and then to make sure there's an actual space afterwards, I'll put a space here like that. Then I need to grab the post object, the tags and the nodes inside that post object, and then map through each of them in turn to output the link and the name. So I'll set up curly brackets and say post.tags.nodes. So that brings us into each node. And then I need to loop through those nodes, and for that, I'll use the map method. Inside the map method, I will grab each tag and I'll also apply an index name to each tag so that I can reference them in [[React.js|react]] later if I want to.
>
> **[3:06](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/add-category-and-tag-info?u=76281980&t=186)** And I'll set up an arrow function Inside the arrow function we're now working with a single tag and we already know we have two pieces of information for a tag, link and name so we can use the link component in Gatsby to output an actual link. And to do that, I first need to add the link component. So I'll import a link component from Gatsby. Then I can use it down here. So I'll say here, link, the link component takes two values. First key. This is so react is able to find element again later if you want to do some sort of transform on it and it's mandatory for this to work. So I'll set the key to the index. Then it takes a two property and that's the target of the link. So since we have our tag object, the individual tag, we can just say link and this will give us the link to that tag. Then we need to output what's going to go on the frontend, the text inside the link and that will be the name. So again, tag.name. All right, let's save this and go to the browser and see what it looks like. Down here, we now have tagged, categoriesedge.case. All right, there's no space between each of these items. Okay, so we need to add space between these items. Now, there are different ways of doing this. I found a really clever way of doing this by reading a bunch of tutorials online. So in place of this parentheses here that contains our function, I'm going to put in a square bracket that makes it an array. Then in the array, I'll provide two values. First, a function that puts a comma between each item and then the actual output. So it looks like this, I say index.
>
> **[4:42](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/add-category-and-tag-info?u=76281980&t=282)** So that gives us the current index of the item, that will be either zero or one, or two, or three, or however many items we have. And as long as the index is larger than zero, meaning from the second item onwards, then output comma space. That way for the first item that has index number zero, we just output the link as is. For the second item, which has index one, so it's bigger than zero, we output comma space and then the item. Save this. Back in the browser and look at that. Now we have categories comma edge case. Very cool, right? All right, so it works for tags. Then we need to do the same thing with categories. I'll to save you the trouble of watching me do this exact same thing a second time, I've already created a component for it. It sits right here, It's called catlist. It does the exact same thing just with categories instead, it receives the post object as its property and then parses all the data based on that. So now all we need to do is import this component into our post template, and then we're able to use it. So pulse.JS at the very top. Here, I'll import a component. So import catlist from components catlist. Then I can use the catlist anywhere I want. So I will place the catlist let's see right before h1 here. So I'll say catlist and the property is called postObject. And then inside postObject, I'll just pass the entire post. So that's all the data and that data will include the category data
>
> **[6:15](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/add-category-and-tag-info?u=76281980&t=375)** and that's what will be parsed out, okay? So now we have the catlist component in place. I'll save this one more time. The pages get rerendered and look at that. We now have the categories at the top here. Now of course, clicking on any of these categories takes you nowhere because we haven't built the category indexes yet but the functionality is there and we're getting the correct information in the template.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[React.js|React]] (2), [[WordPress]] (1), [[GraphQL]] (1)
> **CLI Commands:** find (5), node (2), make (1)
> **UI Navigation:** go to (3), scroll down (2), click on (1)
> **Exercise Files:** template (5)
> **Code Identifiers:** postobject (2), featureimage (1)
> **Prerequisites:** set up (3)
> **File Paths:** post.js (1), pulse.js (1)
> **Definitions:** is called (1)

#### [Create an index template](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-an-index-template?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-an-index-template?u=76281980&t=0)** - [Narrator] We've successfully migrated each post from the [[WordPress]] site to the Gatsby site. Now we need to create some form of interface for the user to be able to navigate through the posts themselves. Now in a WordPress site, this is traditionally done by having indexes. So when you go to the blog page of a WordPress site you get a reverse chronological index showing a number of posts, usually 10 or 12. And then you navigate down to the bottom of this list to go to the previous post or the older posts or the newer posts or maybe even a paginated index to find the posts that were published in the past. We want to duplicate this behavior in Gatsby. The challenge here is we're working with static pages. So that means we need to generate new static pages, one for each 10 or 12 or 14 or 4 or whatever number you want to display of posts in an index and then create these static index pages individually, hopefully automatically, so we don't have to manually put all this stuff in. To do this, we'll use some additional features inside all WP posts in [[GraphQL]]. In particular, we want to use these two fields at the top here called limit and skip. So let me explain those. Limit allows us to say how many items we want returned. So if we set the limit to say 10, then you only get 10 returns, instead of all available returns. That would give us the first 10 posts based on our order. However, if we want to be able to skip to the next 10 we then need an additional feature, and that is skip. So if you set the skip value to zero, it means gimme the first 10 items. If you set the skip value to 10, you're saying gimme the next 10 items.
>
> **[1:33](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-an-index-template?u=76281980&t=93)** And if you set it to 20, you're giving the next 10 items after the 21st items and so on and so forth. So if you automate the input of the skip value and set the limit you're able to create multiple different pages showing different sets of pulls based on these two values. This is the feature we're going to use when we auto-generate the pages. But before we can do that, we need to set up the page templates and get all the functionality working. *So, in the exercise files for this movie I've added two new files. The first one is found under templates. It's called posts index. As the name suggests, this is the index page for posts. This is a built out version of the post template. So it has a little more functionality in it since we're iterating through several different posts, and we also need to include some links and other stuff to make it work. If you scroll down to the bottom and look at the query, you'll see the query is the same query we've been using before. We're querying the same amount of data and outputting the same amount of data. But up here at the top you're seeing in addition to sorting it by the date we're also using the skip and limit functions. Now these values for skip and limit are variables that are passed in from node. And this is done using a function we'll build in the next movie. So just for now, assume that skip and limit will be populated, and that's how we get the correct posts. Then, scrolling up to the top, you'll notice there are a couple of differences here. First of all, we're passing in the page context 'cause we need some information about the current page we're working on, then down here because we are displaying a list of posts instead of a single post,
>
> **[3:07](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-an-index-template?u=76281980&t=187)** we have to map through the array of posts that comes to us. So here we have posts, all the posts and then map through each post. Scrolling further down, you'll also notice that the featured image has a link wrapped around it. So when you click on the featured image, you go to the individual post and the title has been changed from an h1 to an h2. And then, there's also a link wrapping around the title, so if you click on the title, you go to the post. Finally, scrolling down a little further, you'll notice instead of using the content for the post displaying the entire content of the post, I'm using the excerpt of the post. So that is just a short version. This is a stylistic option I've made for the index page. Of course, you can choose to display the whole content if you want to. In fact, you can display whatever you want on the index page, and this template allows you to display whatever you want. Then, if you scroll all the way to the bottom you find this new component called pagination and this is where page context comes in. So the page context is information about where we are in the stack. So what is going to be the previous page and the next page in the index. The pagination component is the other file I've added. It sits here. It receives the page context, which includes information about the previous page and the next page, and then it outputs a simple navigation section that has a link to the previous page and a link to the next page, or newer posts and older posts. So that's all the pieces that fit together to output these index pages. In the next movie, we'll look at how to auto-generate the index pages.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (3), [[GraphQL]] (1)
> **UI Navigation:** go to (4), click on (2), scroll down (1)
> **CLI Commands:** find (2), make (1), node (1)
> **Exercise Files:** template (2), exercise files (1)
> **Cross-References:** in the next (2)
> **Definitions:** is a  (2)
> **Prerequisites:** set up (1)
> **Speakers:** - [narrator] (1)

#### [Generate index pages automatically](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-index-pages-automatically?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-index-pages-automatically?u=76281980&t=0)** - [Instructor] Writing the scripts necessary to auto-generate paginated index pages in Gatsby would be a lot of work, but fortunately, this process is so common, there's a plugin for Gatsby to do all of this work for us. That plugin is aptly named, Awesome Pagination for Gatsby because it's pretty awesome. So let's use this plugin to generate the paginated index pages for us. The first thing we need to do is install it into our Gatsby project. So I'll open my terminal, terminate any process that's currently running, and type in [[npm]] install, save Gatsby awesome pagination. While that install process is running, we can set up the plugin in Gatsby node. To do that, we start by importing the plugin. Now, the code example here can't actually be used in our project because this is a node project and I'm using ES6. So instead of the code example, I'm going to set up a new constant, and then I'll destructure the data that comes out from Gatsby Awesome Pagination to grab only the paginate function. So I'll say paginate equals require, and Gatsby awesome pagination. Now, we have the paginate function available to us, and I'm going to use that paginate function all the way at the bottom of my file. There's an example of how this function works further down in the documentation for the plugin. So in the example, we first generate the blog post. So this will be an array of posts. And then we use the paginate function to create a page, then grab the blog post items,
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-index-pages-automatically?u=76281980&t=94)** set the number of items we want per page, give them a page prefix, and point at some template to generate the content. That's exactly what I want to do here. So I'll just copy this piece of the code out, and paste it down here directly below where we generated the posts. Okay, so we want to paginate the blog posts. And if you remember, earlier we created a new post query for all WP posts. So we already have an object that contains all the available posts on the site. That object has been turned into a constant here. So we're saying, constant post, and then query results for data post query and edges. So this posts constant holds the information we need. So I'll create pages based on the items in posts. That's the constant that was created earlier. Then I decide how many items I want per page. And just to create more pages, I'll say four. The path prefix will be the URL we go to, to go to this index. And since I've already defined posts as the prefix for the individual posts, I should use that same prefix here. So posts. And finally, I need to point to the component we'll use to resolve each of the pages. That will be the component we're using as the template, which is this one here, posts-index. So I'll say ./src/templates/posts-index.js.
>
> **[2:59](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-index-pages-automatically?u=76281980&t=179)** Okay, save this. Go back in terminal and run Gatsby develop. And while this process runs, let me explain what happens now. The paginate function is really smart and does a lot of things. So it creates new pages based on the information we've given. So now, it'll create a new page for every four posts we have and using post slash posts, and then post slash two, three, four for however many pages are generated. Now, this paginate function will then pass information over to the template we're using. That information is captured here. So we're capturing the skip and limit information that's generated from this value, items per page. So skip and limit will be populated by the function we just installed, and then populated into the query. So we're getting the right number of posts for each individual page that's generated. And it also passes over something called page context. And within this page context object sits two important pieces of information, previous page path, and next page path. And that's what's used inside this pagination component to output those links to the previous or next post. So all of this work is done by the plugin automatically, and we don't have to wire it all together. We simply provide the components necessary, and all the work is done for us. If you want to know more about exactly how this is all wired together, it's all documented in the plugin information. So here we have an example of the skip and limits components. And here we have an example of that navigation from the previous or next index. So the plugin solves the problems for us, and if there are no errors in my code right now,
>
> **[4:33](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-index-pages-automatically?u=76281980&t=273)** we should be able to go to our local host slash posts and see the index. Look at that, posts index. Here's the first post, the second post, the third post, the fourth post, and then older posts. Navigate to that. Keep going through the content. If I click on any of these, I go into the individual post.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[npm]] (1)
> **Prerequisites:** install (3), set up (2)
> **UI Navigation:** go to (2), navigate to (1), click on (1)
> **CLI Commands:** node (2), npm (1)
> **Exercise Files:** template (3)
> **Env Vars:** es6 (1), url (1)
> **Tools:** terminal (2)
> **File Paths:** src/templates/posts-index.js (1)

#### [Challenge: Create a post navigation component](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/challenge-create-a-post-navigation-component?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/challenge-create-a-post-navigation-component?u=76281980&t=0)** - [Instructor] Up for another challenge? Here, I want you to really test what you've learned so far by creating a post-navigation component. This is a component that sits at the bottom of each individual post page to say, navigate to the previous or next post. In this challenge, you need to first augment the [[GraphQL]] query to get the previous and next post objects. This query might not be in the post's template. It might sit somewhere else. Then you need to get the title and URL for the previous and next post, create a component to display the previous and next post navigation, and then put that component below all the other content in the single post template. Here's a challenge tip. The post query in Gatsby-[[Node.js]] is querying allWpPost.edges. This is a vital piece of information to solve this challenge. This challenge is more difficult than the previous challenges, so give yourself up to 20 minutes to solve it. Once the 20 minutes are up, or you've solved the challenge, go to the next movie and see how I solve the same challenge.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GraphQL]] (1), [[Node.js]] (1)
> **UI Navigation:** navigate to (1), go to (1)
> **Exercise Files:** template (2)
> **Definitions:** is a  (2)
> **File Paths:** gatsby-node.js (1)
> **CLI Commands:** node (1)
> **Code Identifiers:** allwppost (1)
> **Env Vars:** url (1)

#### [Solution: Create a post navigation component](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-post-navigation-component?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-post-navigation-component?u=76281980&t=0)** (bright music)
>
> **[0:07](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-post-navigation-component?u=76281980&t=7)** - [Instructor] Welcome back. Let me walk you through how I solve this challenge. In the exercise files for this movie, I've added all the necessary code so you can compare your solution to mine. And here I'll walk you through all the steps and show you how everything is wired together. The end result is what you see in my browser right now. When I go into a single post, I have navigation at the bottom saying previous post and next post. And if I click on either of those navigation items, I'm going to the post in question. As you can see, each of the navigation items will have the text previous or next post follow by the title of the post in question and the link pointing to that post. The key to solving this challenge is figuring out where to get the information about what the previous and next post is. And I've alluded to this in the past. If we use the edges point on all WP post, we not only get the current note, the current post, but we also get the next and previous post. So if I go in here and say next, and then go look for database ID and URI, you'll see database ID and URI, and then scroll all the way down and find previous and do the same thing, database ID and URI and then run the query for each of the posts. We now have the note itself followed by the next and on the previous post. In this result, it's very important to notice that if the previous post is nothing, meaning you're looking at the first post, or the next post is nothing, meaning you're looking at the last post, the value of the other one will be null. This will become essential to our solution.
>
> **[1:42](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-post-navigation-component?u=76281980&t=102)** All right. So how do we use this data? Well, the first step is to go into Gatsby node and alter our all WP post query because now we're not only looking for the current node but we're also looking for the next and previous nodes. So as you can see in my post query, I'm now querying the node and next and previous, and for next and previous, all I'm gathering is the database ID. Once I have the database ID, I'll be able to later on, query that database ID for more information. Now we need to do something with that data. And that happens down here where we create the pages. So under create page, we have this section called context where we can pass information from node into the template. And before we just passed the database ID for a current post, now we're also going to pass the database ID for the next and previous post. And remember how I said, if there is no post, we get a return of null. Well, if we try to pass the next ID and just say post next and post next equals null, node will stop working. That's an error. So to fix that problem, I've set up this conditional query here that says if post next has a value, then pass post next database ID. On the other hand, if it has no value, then pass the value null, so that everything doesn't fail. So this will pass either an ID or null, depending on whether or not there's a previous or next post available. This information is then captured in post JS down here in the query. I've extended the query to receive not only the database ID for the current post, but also the next ID and the previous ID.
>
> **[3:14](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-post-navigation-component?u=76281980&t=194)** And look closely here and you'll see the database ID for the current post has bang at the end of it. It says the result is an integer and a bang means it has to be an integer. Whereas the next ID and the previous ID do not have that bag at the end because it could either be a number or it could be null. And that's important because if you put that bang here and you pass in null, then the query will fail. Next ID and previous ID are then used in a new fragment at the bottom here. So we have two new fragments, next post, that goes to WP post for the database ID of next ID and then pulls out the title and URI. And then for previous post, again, go to WP post, pull out the previous ID and title and URI. This is one way of solving the problem. You could also do this in Gatsby Node but I find it cleaner to do it in post. Once we have these two pieces of data, so previous and next post, we then use it up here in the output section. I've created a new component called post nav and I'm passing in data previous post and data next post. So that is the data that contains title and URI for the previous and next post. Post nav sits over here. It's a new component. It receives previous post and next post. And then it takes the information from these two and outputs it. So for each of them, we output a link to either the previous post URI with the previous post title or the next post URI with the next post title. And I've wrapped them both in conditional statements saying only if we actually have a previous post, are we going to output this link.
>
> **[4:46](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-a-post-navigation-component?u=76281980&t=286)** And only if we have a next post, are we going to output this link. So again, there might be a null value. I create a null value to pass it through the system to ensure that we don't output garbage content. And also to ensure that we don't get any errors. So send a query from Gatsby Node, pass the query data over to post and post passes the data over to post nav. And that's how we get the navigation. Run this in the browser. You'll see this navigation at the bottom and it'll work across all your posts.

> [!info]- Semantic Content
>
> **CLI Commands:** node (7), find (2)
> **Env Vars:** uri (8)
> **UI Navigation:** click on (1), go to (1)
> **Exercise Files:** exercise files (1), template (1)
> **Definitions:** is an  (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright music) (1)


### 6. Categories and Tags

[↑ Back to Table of Contents](#table-of-contents)

#### [Query categories and tags](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-categories-and-tags?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-categories-and-tags?u=76281980&t=0)** - [Instructor] All right. At this point, we have pages, we have custom menus, we have posts, and we have post indexes, but there's still some pieces missing. If I go to one of my posts and then click on one of the category links here, I get to the 404 page. That's because we have yet to generate the category index pages. But that's a bit challenging because unlike the index page where we just say give me all the posts and then list them in reverse chronological order, for the categories, we need to generate a separate category index page, or maybe a set of category index pages per category. So we somehow have to query the database and say, "Hey, can you give me all the posts that are under this Grandchild Category, and then make an index based on just that category?" And also keep in mind that different posts may be filed under multiple different categories. So how do we do all this? Well, the answer, as with pretty much everything else, is [[GraphQL]] solves this problem for us if we just know where to look. So let's do a query for a category. I'll go and say allWpCategory that gives me every single category. Then give me the nodes. And here I want the databaseID for the category, the name of the category, and the link to the category. So run this query and we get a list of all the available categories on the site with the databaseID, the name, and the link. All right, that's great, but it doesn't really solve the problem of posts, which posts belong under each category, or does it? Look closely here, and under the category option
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-categories-and-tags?u=76281980&t=94)** in GraphQL, there's one called posts. And if I open this one and open the nodes, here we have a post object. And from here, we can then say, give me the databaseID for the post, the link to the post, and the title to the post, and run the query again. And watch what happens. Now, for each of my categories, so aciform, we have a list of posts, and these will be all the posts that are stored under this category. So here there's just one. Down here, there's just one. But if we scroll further down, you'll find that some of these categories have a lot of posts under them. So this one, databaseID 6 called block has a series of posts. And that means we can run a query on the category with that databaseID 6 and say, make an index based on all the posts that are inside this category. And all the posts will be directly available to us. And because we are working with GraphQL, when I say the post is directly available to us, I mean the entire post object is available to us. So we can go in here and pull out whatever information we want about that post. The featured image, the author name, the other categories, the title, whatever we want, it's all available to us. So in this way, GraphQL is kind of like a nested doll where the same item can be queried from multiple different locations. So this is how categories work. Tags work the exact same way. The only difference is, instead of requesting allWpCategory, we just request allWpTag. Run the query again.
>
> **[3:08](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-categories-and-tags?u=76281980&t=188)** We get the exact same kind of result, except this time it's for tags. This is what we're going to do in the [[Representational State Transfer (REST)|rest]] of this chapter. Build out indexes for every single category and every single tag.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GraphQL]] (4), [[Representational State Transfer (REST)|Rest]] (1)
> **Code Identifiers:** databaseid (5), allwpcategory (2), allwptag (1)
> **CLI Commands:** make (2), find (1)
> **UI Navigation:** go to (1), click on (1), open the (1)
> **Analogies:** kind of like (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [Generate category indexes automatically](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-category-indexes-automatically?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-category-indexes-automatically?u=76281980&t=0)** - [Instructor] Creating category indexes works much the same as when we created an index for all the posts. The main difference is we're now working with a series of indexes instead of just one giant index. We need one index per category. So that means we need to create a loop to say for every available category, go count out the number of posts in that category and then create index pages for that category, then move on to the next category and do the same thing. The key to getting this to work is to send the right query to [[GraphQL]], and this is what that query looks like. We'll go to allWpCategory to get all our categories. Each of these categories is captured in a node. And for that node, so each category, we get the databaseId, the uri, and the name of the category. Then, for each of these nodes, so each of the categories, we also request the posts, the nodes, and then some value, in this case, databaseId. It's not important exactly what data we're pulling out for this node, because we're not going to use that data. The only reason we're requesting it is we need to know how many posts are under this particular category. Okay, so let's set all this up. I'll start by copying out this query and going to gatsby-[[Node.js]]. Here, we already have two fragments set up, pageQuery, which we use to query pages, and postQuery for posts. So now we're going to make a third fragment for catQuery and set that equal to the query I just set up. So now we have all the categories as nodes, and inside each node, we have all the posts. Then we need to generate the index pages,
>
> **[1:32](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-category-indexes-automatically?u=76281980&t=92)** and we can do that by copying the existing paginate function we created for the general index pages. So I'll copy this code in its entirety, paste it in below, then change the description, Create your paginated category indexes. And now we need to do some thinking because unlike the original pagination function, where we had a list of posts, and we just created index pages for it, here, we have a list of categories. And then for each category, we have a list of posts. So we need to create a loop here to say grab each category, then run the pagination function on that category, then go to the next category and run the pagination function again. So let's take a look at the data we have right now. In our query, we have this catQuery that returns a series of nodes, an array of nodes, and each of those is a category. So if we go down here, I'll say const to create a new constant called categories and set that constant equal to the query result for the catQuery. So queryResult.data, so that's all the data, .catQuery, that's our fragment, .nodes. This will return an array of all the available categories. Then, I can take that array and map through it, so we get each category, and then pull out the posts from each category, so categories.map. Inside the map, we'll set up categories, category, so that's the individual category, and an arrow function.
>
> **[3:06](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-category-indexes-automatically?u=76281980&t=186)** And inside the arrow function, I'll now paste this pagination function. So that way, we are looping through each category, grabbing each category in turn, and then running pagination on that category. Okay, now I can set up the pagination function. So we are creating a page. Then the items we are working with now will be category, that's the current category, .posts.node. Remember, in our query here, posts, nodes, and then the databaseId, that's why we're asking for this value. We're not actually going to use it, but now we have a list of nodes. I still want four items per page. As for the pathPrefix, I now need to have a path prefix that matches the category name. And that's why I queried for the uri to the individual category nodes, 'cause then I already have a uri that's generated by [[WordPress]], and I can use that uri to match it. So down here, instead of saying posts, which is a string, I'll say category.uri. Now we need to point at a template. We haven't built that template yet, but we're going to in the next movie. So for now, I'll just say categories here. And then finally, I want to pass some additional information to that template. Specifically, I want to pass the category ID so that we can run a query on the data in the template. And I also want to pass the category name, so we can display the name outside of the query. To do that, I can use the context property
>
> **[4:40](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/generate-category-indexes-automatically?u=76281980&t=280)** inside the paginate function. Context allows me to pass on an object with whatever information I want. So in this case, I want to pass on catId, set that to category.databaseId, and I want to pass on catName and set that to category.name. Okay, save that. Don't run gatsby develop yet because we're missing this category's template. So if you run it right now, everything will collapse. But this is the functionality that will eventually allow us to autogenerate these posts.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GraphQL]] (1), [[Node.js]] (1), [[WordPress]] (1)
> **Code Identifiers:** databaseid (4), catquery (4), allwpcategory (1), pagequery (1), postquery (1)
> **CLI Commands:** node (6), make (1)
> **Exercise Files:** template (5)
> **Prerequisites:** set up (4)
> **UI Navigation:** go to (2)
> **Definitions:** is a  (2)
> **File Paths:** gatsby-node.js (1)

#### [Create a category index template](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-category-index-template?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-category-index-template?u=76281980&t=0)** - [Instructor] The function to generate page-native category index pages is in place. So now we need to build the category template, so we can actually output it to Gatsby and generate the pages themselves. For this, I'll use my favorite development technique again, and it's still called being lazy. I already have an index template in my project. It's this one here, posts-index.js. And because I'm now outputting a different kind of index, but I want the same kind of information, and also because I want consistency between the different index pages, I'm going to make a copy of this file and then just make some slight changes to it to make it output category information instead of all post information. So I'll copy this and then just paste it back into the same folder and rename this file categories so that it matches with what we're calling in Gatsby Node here, templates/categories.js. Then I'll close posts-index, so I don't accidentally make any changes. Now I need to first update the component name, so I'll go Shift + Control or Command + L to change all instances and call this one CategoryIndex. Then, I'll scroll down to my query. Now, here, I need to modify my query so that instead of querying all posts, I'm only querying the posts that belong in a specific category. Now, we already know the ID for that specific category because I passed it in through context here, and that means we can use it in my query. But first we need to capture this catId value. That's done by going up here in the top of the query
>
> **[1:36](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-category-index-template?u=76281980&t=96)** and setting up a new variable. That's done by going to the query and setting up a new variable, so $ and then the name of the variable. And this variable will be an integer. And I want it to be always an integer, so I'll put a bang at the end. Now we have the catId variable, which is the value we passed over from here, the databaseId for the current category. And that means we can filter based on it. How do we do that? Well, if you go to allWpPost and open filter, you can go to categories and nodes and element match and databaseId and set it equal to whatever you want. And it looks like this. So I'll copy all this out here, everything in between the parentheses, and then I'll paste it in directly after limit. So we are passing sort, skip, limit, comma, and then filter. And I'll save this, and then it'll get nice and structured, so you can read it. So we're sorting based on date. We skip and limit based on the values that are passed in from the pagination function. And then we filter based on catId. So here I'll grab catId and place it over here in place of 10. So now we're saying give me only the posts that belong to this category, and the category matches the databaseId, catId. All right, the [[Representational State Transfer (REST)|rest]] of it stays the same because we have all the information we need. Now we need to change the output, and that's done up in the component itself. The first thing I want to do is change the name of the page, 'cause right now it says Posts, but we need it to say instead Category and then the category name. So I'll change this to say Category, and then I need the category name.
>
> **[3:11](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-category-index-template?u=76281980&t=191)** Now, again, I'm passing that over in the context here. Remember, category name is passed over as the value called catName. So that page context is already inside my component, right here, so I can destructure it and say constant and then say catName = pageContext. And that way, I have the catName. And then I can just place the catName here, so catName, and it'll output Category: and then the name of the current category. I also want to do this up in the Seo title. So here, I'll set up template literal, and then say Category: $ and then catName. Everything else in this template stays the same because the template is still outputting in the same way that it was before. So the only changes really are the title here and to make sure the query only gives us the posts we're looking for. Now I can save that, make sure that everything is in place, and run gatsby develop. Build is complete, go back to the browser. Here, we have have our post with many categories, and now I can go to any one of these categories, let's say category disinfection, and get a list of all the posts under that category. Now, this category and many of the categories in the theme unit test data only have one post, but I know there's a category called block. So if I go to category/block, you'll see here, we have a bunch of posts. And at the bottom, we have navigation.
>
> **[4:44](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/create-a-category-index-template?u=76281980&t=284)** So I can now click on older posts and get to, oh, that didn't work. Why is that? Look at the URL. You see it says block//2? This is an interesting problem. Let me show you what's going on here. So if we go back to Gatsby Node and look closely at the documentation, it says here pathPrefix creates pages like blog, so /blog, no slash, but this category.uri outputs a URL that ends with a forward slash. So I need to get rid of that forward slash. The good news is that's really easy to do. All I need to do is go in and just slice it off. So I'll say slice 0 and -1. So that just takes the whole string and cuts off the last letter, which in this case is the errant forward slash, save Gatsby Node. Of course, this means I need to restart gatsby develop. So I'll stop and restart gatsby develop and wait for the whole set to be regenerated one more time. Once that's done, I can go back to my browser, reload this page, and then I'll go back to the block page that I know worked, scroll to the bottom, find older posts, click, and look at that, block/2. Everything is now working properly, and we have category indexes for every single category.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Representational State Transfer (REST)|Rest]] (1)
> **Code Identifiers:** catname (6), catid (5), databaseid (3), allwppost (1), pagecontext (1)
> **CLI Commands:** make (6), node (3), find (1)
> **UI Navigation:** go to (4), scroll down (1), click on (1)
> **Exercise Files:** template (5)
> **Cross-References:** go back to (4)
> **File Paths:** posts-index.js (1), templates/categories.js (1)
> **Env Vars:** url (2)

#### [Challenge: Create tag indexes](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/challenge-create-tag-indexes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/challenge-create-tag-indexes?u=76281980&t=0)** (bright music)
>
> **[0:08](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/challenge-create-tag-indexes?u=76281980&t=8)** - [Instructor] All right, time for one final challenge. Here, I want you to create a tag index for your site. First, create a [[GraphQL]] query to retrieve all the tags and their posts. Then build a function to output paginated tag index pages for all the tags. And finally, build a template to display these paginated tag index pages. In this challenge, it's very important to remember my primary development tool, being lazy, because a lot of this work has already been done for you. You may just need to copy some existing work and just change a couple of phrases to solve this challenge. For this reason, the lazy part, give yourself only 10 minutes to solve this challenge. Once the 10 minutes are up or you solve the challenge, go to the next movie and see how I solved the same challenge.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GraphQL]] (1)
> **UI Navigation:** go to (1)
> **Exercise Files:** template (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright music) (1)

#### [Solution: Create tag indexes](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-tag-indexes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-tag-indexes?u=76281980&t=0)** - [Instructor] Welcome back. Hopefully, you are successful in solving this challenge. Now, let me show you how I solve the same challenge. The key here is we already solved this challenge previously. We already built the category indexes, and the tag indexes are exactly the same. The only difference is we're querying tags instead of categories. Let me show you what I mean. In GraphiQL, I've set up the query we're using to pull out all the categories and all the posts inside them. We're querying allWpCategory and then providing a bunch of information. Now, if I just change this query from allWpCategory to allWpTag, and run the exact same query, we get the exact same type of data in the exact same structure. Meaning we can take the original code we brought for categories, duplicate it, and just make it point at tags instead, and the challenge is solved. So let's do just that. Start off by stopping gatsby develop 'cause we don't need it to run right now. Then, I'll go into gatsby-node and find my catQuery. It's right here. I'll copy the entire catQuery, and just paste it back in below, and rename it to tagQuery. Then, I'll query allWpTag, instead of category. And that gives me all the data I need. Then, I'll scroll to the bottom and find where I created the paginated category pages, and just copy that entire function. Again, paste it in below and change the naming. So paginated tag indexes.
>
> **[1:33](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-tag-indexes?u=76281980&t=93)** And here, I need to change every reference to categories or category to tag instead. So make sure you change all of them. I keep forgetting to change some of them and then everything breaks. And there's a key point here. Don't use the Shift + Command + L function here because then you'll also change the function above, and you don't want to do that. So here, I'm just going to do this manually. tags, tagQuery, tags, tag,
>
> **[2:06](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-tag-indexes?u=76281980&t=126)** and here tag, here tag, here tag, tagID, tagName, tag, and tag.
>
> **[2:23](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-tag-indexes?u=76281980&t=143)** So now, if I highlight category up here, there shouldn't be any, well, there's that one. Let's make that tag indexes and that's it. So now, there's no reference to categories, everything is tags here. Good. Then, I need to build a template for this. And since I already have the categories template, I can simply copy the categories template. Paste it back into the same folder. Then rename this template, tags, so that it matches with the name I referenced here. And then inside the tags, I am going to rename the component to TagIndex. Then I'll rename any place I use catName. So again, Shift + Command or Control + L to tagName. And there it says category here. So I need to change that to tag. And down here to tag. And then scroll to the bottom, find my query. Here, catID needs to be changed to tagID. And we are filtering for tags, not categories. So that should actually be it. So I'll save this and boot up gatsby develop again. Everything worked correctly. That means I can go back to my posts. Here, I'll go look for that old post that we've been looking at many times before, the one that's called Edge Case: Many Categories. Because next to that one is one called Edge Case: Many Tags. And here we have a bunch of tags. So now we can go in and check to make sure that the tag indexes work. And look, they do.
>
> **[3:54](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/solution-create-tag-indexes?u=76281980&t=234)** Everything is working the way it's supposed to. We even have navigation, if that's necessary. And we now have both category indexes, tag indexes, and post indexes.

> [!info]- Semantic Content
>
> **Code Identifiers:** allwpcategory (2), allwptag (2), catquery (2), tagquery (2), tagid (2)
> **CLI Commands:** make (4), find (3), node (1)
> **Exercise Files:** template (4)
> **Cross-References:** go back to (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 7. Custom Post Types and Taxonomies

[↑ Back to Table of Contents](#table-of-contents)

#### [Add custom post types and taxonomies](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/add-custom-post-types-and-taxonomies?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/add-custom-post-types-and-taxonomies?u=76281980&t=0)** - [Instructor] In advanced [[Wordpress Development]], it's common to extend the functionality of [[WordPress]] by adding new Custom Post Types, and custom taxonomies, and custom fields, and custom [[Forms]], and eCommerce features, and a lot of other things. And all of this data can be exposed through the [[GraphQL]] API as long as we tell the GraphQL API that the data exists in the first place. In this chapter, I want to briefly touch on how this works so you can see how, once you get it working, it works the same way as all the other data inside WordPress. In this example, I've created a new Custom Post Type called Books and populated with three books. The Book's Custom Post Type has two custom taxonomies called Genre and Writers, and for each book we can apply as many Genres and as many Writers as we want. I've set this all up using the popular Custom Post Type UI plugin. This plugin allows you to set up a Custom Post Type internally in WordPress without writing any code. The output of this plugin is exactly the same as if you wrote a custom plugin for adding your Custom Post Types. So, in the end, the data looks the same, it's just a question of whether you build it inside WordPress or you build it in code on the outside. But here's the thing. If I go to GraphiQL now and scroll through the Explorer, you'll see, even though I have a new Custom Post Type and two new custom taxonomies, they're not featured here in the list under the Explorer. There's no way for me to use GraphiQL to gain access to any of this data yet. That's because, even though the Custom Post Types exist, we haven't told WordPress that we want to expose them to the GraphQL interface yet.
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/add-custom-post-types-and-taxonomies?u=76281980&t=94)** If you've created your Custom Post Types and taxonomies from scratch in your own code, you can easily expose them to GraphQL by simply setting the show_in_graphql option to true and then providing the graphql_single_name and graphql_plural_name. This is very similar to how you would expose content to the [[Representational State Transfer (REST)|REST]] API inside WordPress. If, on the other hand, you're using a plugin like the Custom Post Type UI plugin, there's a good chance that this feature's already built-in or that there's a second plugin you can install to add this feature on. In the case of Custom Post Types UI, they've now added in this feature. So if I go in to edit my existing Post Types here, you'll see this is the Books Post Type, and if I scroll all the way to the bottom of this page you'll see, right here, the Custom Post Types UI plugin is interacting with the WPGraphQL plugin to give us this option, Show in GraphQL. So I'll set that to True, then I get to choose the GraphQL Single Name and Plural Names. These are what's going to be featured in the Explorer in GraphiQL. I'll just leave them at default and click Save Post Type. Then I'll do the same with my taxonomies. Go into each taxonomy, first Genres, scroll to the bottom, Show in GraphQL, set to True, and accept the default names, Save. And then Writers, same thing, scroll to the bottom, set to True, accept the default names, Save Taxonomy. So now the data is exposed through GraphQL, but I still can't see it in my Explorer because we have to get Gatsby to update all its data. So I'll stop gatsby develop, then run gatsby clean.
>
> **[3:12](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/add-custom-post-types-and-taxonomies?u=76281980&t=192)** I do this anytime I change the data inside WordPress to ensure we get a fresh new set of data. And then, finally, I'll run gatsby develop again, and this will pull in all the new data including my new Book Post Type and the new taxonomies. That will make them available inside the GraphQL interface. When the process is complete, and I go back to GraphiQL, you'll see I now have new entries, allWpBook for all books. Scrolling further down, wpBook for the single books, allWpGenre, and allWpWriter, and wpGenre and wpWriter for the two taxonomies. That means I now have the same type of access to this content as I did to any other content on the site. I can go into allWpBook, go to the edges, go to the nodes, see all the same information we saw for Posts, and I can do the same thing for our new custom taxonomies. New content Types inside WordPress, new content Types in GraphQL, meaning new content Types in Gatsby.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GraphQL]] (10), [[WordPress]] (8), [[Wordpress Development]] (1), [[Forms]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **Code Identifiers:** allwpbook (2), ecommerce (1), show_in_graphql (1), graphql_single_name (1), graphql_plural_name (1)
> **Env Vars:** api (3), rest (1)
> **UI Navigation:** go to (3)
> **Prerequisites:** set up (1), install (1)
> **CLI Commands:** make (1)
> **Cross-References:** go back to (1)
> **Analogies:** similar to (1)

#### [Query custom post types and taxonomies in GraphQL](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-custom-post-types-and-taxonomies-in-graphql?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-custom-post-types-and-taxonomies-in-graphql?u=76281980&t=0)** - [Instructor] You can probably guess what I'm about to say now because I've repeated it several times already. In the context of Gatsby, [[WordPress]] is just another [[GraphQL]] data source. And the content that comes from WordPress is structured and formatted in a specific way to make it easy and consistent to access. Earlier in the course, we queried posts and pages in detail and now that we've created a new custom post type, that custom post type matches the content model of those posts and pages. Let me show you what I mean. Here we have a standard query for allWpPost. We go into the edges, get the node, and then look for the database ID and the title for the post and out we get a list of all the available posts on the site. We can go into the Explorer, find allWpBook and make the exact same query, edges node databaseId and title. When we run this query we first get a list of all the posts with structured data. Then scrolling far enough down, we get a list of all the books with the same structured data. And here you see the data structure is identical between the two. That's because when you build a new custom post type you're saying, hey, take the existing custom post type and build a new version of it. And then you can toggle on or off all the features within it. So you can say should this custom post type have a featured image or not? Should it have categories or not? Should it have tags or not? Should it have comments? And all of these things dictate what the data is when it gets stored and what data is available to us
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/query-custom-post-types-and-taxonomies-in-graphql?u=76281980&t=94)** through GraphQL. But the structure of the data matches what we've worked with previously. And that means once you have a new custom post type and new taxonomies, creating pages for the custom post type, creating indexes for the custom post type, creating pages and indexes for those taxonomies works exactly the same way it did with posts, pages and categories and tags. The only thing that changes is the type of data you're querying. So instead of creating an index for all posts by querying allWpPost, you can make an index for all books by querying allWpBook. The [[Representational State Transfer (REST)|rest]] of the code stays almost identical depending on the data that's available. And that is the core premise of all the work we've been doing so far. Once you understand how to pull data from WordPress into GraphQL and then from GraphQL into Gatsby, the consistency of the data models allows us to do the same operation on pretty much any kind of data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GraphQL]] (4), [[WordPress]] (3), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** make (3), node (2), find (1)
> **Code Identifiers:** allwppost (2), allwpbook (2), databaseid (1)
> **Cross-References:** earlier in (1)
> **UI Navigation:** toggle (1)
> **Speakers:** - [instructor] (1)


### 8. Considerations When Using WordPress and Gatsby

[↑ Back to Table of Contents](#table-of-contents)

#### [What to do about Blocks (Gutenberg)](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/what-to-do-about-blocks-gutenberg?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/what-to-do-about-blocks-gutenberg?u=76281980&t=0)** - [Instructor] Working with [[WordPress]] content in Gatsby, we have to address the block-sized elephant in the room, the block editor, also known as Gutenberg. When we create content in WordPress, blog posts, pages, custom post types or anything else, we're using the block editor and the block editor allows the creator to impose stylistic choices onto each individual block of content within that overall content. You can see an example of this in this post, block category common. If we scroll down here, we have a piece of text that's right aligned. Then we have a piece of text that has a drop cap, then a central line piece of text. We have some bigger texts, some much bigger texts, some texts with a colored background. There's some layout blocks being used here to create a grid of images. The list goes on. All this block information is stored in the [[HTML]] for each individual post or page and you can see that on the back end of WordPress by switching the editor from visual editor to code editor. Here, each block starts and ends with a comment that specifies what block it is and some of these comments will have additional stylistic information. For example, align right, which translates into a class, has text align right. Here, the size. So drop cap true and font size medium translates to has drop cap, has medium font size. And if we scroll further down, if there's custom sizing for example, we get style [[Typography]], font size 46 which translates to style font size 46. Now here's the thing. When this content is queried from Gatsby, we don't get these comments. Instead, we just get the rendered HTML. So here you see the text itself. You see the classes, has text align right.
>
> **[1:34](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/what-to-do-about-blocks-gutenberg?u=76281980&t=94)** You see the inline styling, for example, down here, font size 46 pixels, but much of the information has been lost. There is no information telling you, hey, this is a block. This is a type of block and this is what should happen to that block. As a result, when we look at the same post in Gatsby, the styling is mainly lost and we don't have these features that we expect. This italic paragraph is not right aligned. This paragraph is not centered. The text spacing here is all wrong. This piece of text is not bigger than the [[Representational State Transfer (REST)|rest]] of the text. Scrolling through, you'll see things are not looking the way you expect. Bottom line, the things you do in the WordPress block editor do not automatically carry over into Gatsby, mainly because of the weird way WordPress makes us this style and block data into the HTML content. So how do we deal with this? The unfortunate answer is there's no clean, straightforward solution here. That's mainly because WordPress doesn't have a proper API for integrating with blocks. And while that may be coming sometime in the future, it is not here yet and work that is ongoing has not born any kind of meaningful fruit yet. So we need to find a different solution. Now one possible solution is the work that's being done in WP [[GraphQL]] to extended tools to include a block editor. That means when you query a post or a page or something that has blocks, you'd be able to also query the blocks individually, effectively get an array of all the available blocks in the current post and then you can render out the properties, the styles or any other information for each of those blocks, then apply a separate component to each of them and render it all out, inside your Gatsby application.
>
> **[3:07](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/what-to-do-about-blocks-gutenberg?u=76281980&t=187)** But this adds an enormous level of complexity because you're no longer working with a content blob. You're working with individual blocks that all have to be treated separately for things to work. So while this might alleviate some of the pain, it'll also add a lot of complexity to your work. So yeah, even with this WP GraphQL block editor, you have to be very aware of these limitations, use blocks in a conservative way and avoid creating situations for the Gatsby front end. That's it. There is one thing you can do to, if not alleviate, then at least reduce this friction. WordPress ships with default CSS for blocks and you can import that CSS into your Gatsby site so the expected styling applied to blocks carries over. Here's how you do that. That CSS sits inside a giant [[JavaScript]] library called @wordpress/block-library. This is actually a library for building Gutenberg blocks or building WordPress blocks and our purposes are not that at all, but it contains the default CSS we need to use. And because Gatsby renders all the content before we publish it, it's okay to download this giant library and just use a small part of it. So that's what we're going to do. I'll copy out the name of the library. Then I'll stop my build process. And here, I'll say [[npm]] install --save and then paste in wordpress/block-library. This is the giant library so it'll take a long time to install. I'm just going to close my terminal in between. Now that we have the library installed, we're going to use it in the layout component.
>
> **[4:40](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/what-to-do-about-blocks-gutenberg?u=76281980&t=280)** And all we're doing here is importing a style sheet. So where I'm already importing some styles here, I'll say import and then @wordpress/block-library/build-style/style.css.
>
> **[4:59](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/what-to-do-about-blocks-gutenberg?u=76281980&t=299)** Save that, reopen the terminal and run Gatsby Develop. This new style library we're using includes all these custom styles that WordPress has created like has text align right. And that means when we render this out in Gatsby again, some of this stylistic information is now translated properly. This paragraph is right aligned. We have a drop cap. This paragraph is centered. Some of the text is bigger than it was before but you can see there are still major gaps in the styling here that we need to address separately. So you can import the WordPress styles. That gives you a starting point, but you still have to go through and test all the default components to make sure everything is working properly and that the blocks and how they are presented in the WordPress backend, translate to how they're presented in your Gatsby front end.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (14), [[HTML]] (3), [[GraphQL]] (2), [[Typography]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **Env Vars:** css (4), html (3), api (1)
> **CLI Commands:** find (1), npm (1), make (1)
> **Definitions:** is a  (2), known as (1)
> **Analogies:** for example (3)
> **Tools:** terminal (2)
> **Prerequisites:** install (2)
> **File Paths:** wordpress/block-library/build-style/style.css (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/building-a-headless-wordpress-site-with-gatsby-15801541/next-steps?u=76281980&t=0)** - [Morten] That's it, you reached the end of this course, and the beginning of your own journey with Gatsby and [[WordPress]]. Before you go, here are a few important websites and resources to keep in mind as you get started. First, for information on Gatsby, including starters, plug-ins, documentation, webinars, conferences, community, anything Gatsby related, check out [gatsbyjs.com](https://gatsbyjs.com). For information of about WP [[GraphQL]], including in-depth tutorials, documentation, and explanations on how to use and extend this interface, check out [wpgraphql.com](https://wpgraphql.com). It may also be a good idea to join the WPGraphQL [[Slack]] channel to connect with other developers using WordPress and Gatsby in combination. For information about how to use Gatsby source WordPress, check out the dedicated tutorials at the plugin page and in the plugin repository. And finally, because Gatsby is built on [[React.js|React]], it can do anything React can do. To learn more about React, we have a long list of courses in our library. All you have to do is search for React and get started learning. I love hearing from you about how you use what you've learned in my courses. You can find me on Twitter @mor10, 'cause that's my name. And I'm all also on [[LinkedIn]] sharing content information and useful links all the time. Now, all that's left for me to say is (indistinct). Go build some amazing sites using WordPress and Gatsby, and I'll see you in the next course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (4), [[React.js|React]] (4), [[GatsbyJS]] (1), [[GraphQL]] (1), [[Slack]] (1)
> **CLI Commands:** find (1), go build (1)
> **URLs:** [gatsbyjs.com](https://gatsbyjs.com) (1), [wpgraphql.com](https://wpgraphql.com) (1)
> **Cross-References:** in the next (1)
> **Tools:** slack (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [morten] (1)


## Instructor

- [[Morten Rand-Hendriksen]]

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/headless-wordpress-gatsby-3022691)

## Skills Covered

- Wordpress Development
- GatsbyJS

## Path Context

### In [[Advance Your Skills in WordPress]]
← [[WordPress- REST API]] | **8 of 8**

## Appears In

- [[Advance Your Skills in WordPress]]

## Related Courses

_Courses sharing skills:_

- [[WordPress- Staging a Site]] — Wordpress Development
- [[Creating Block Patterns in WordPress]] — Wordpress Development
- [[Building a WordPress Membership Site with Ecommerce]] — Wordpress Development
- [[WordPress- Everything about Plugins]] — Wordpress Development
- [[WordPress Essential Training]] — Wordpress Development

---

[↑ Back to top](#)