---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: powershell-for-microsoft-365-administration
url: "https://www.linkedin.com/learning/powershell-for-microsoft-365-administration"
duration_minutes: 249
duration: 4h 9m
level: Intermediate
updated: 4/15/2025
learners: 46092
skills:
  - Office 365 Administration
  - Powershell
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQEuh7ybpgsnNg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1639075040486?e=2147483647&amp;v=beta&amp;t=eGWEUoCTqR6GJ6_yv7gMA82FJrhEUZRAR21qpbgSC1A"
linkedin_topic: Network and System Administration
learning_paths:
  - '[[Powershell- Automate IT Tasks and Microsoft 365 Administration]]'
prev_courses:
  - '[[PowerShell- Automating IT Administration]]'
path_nav: '[{"path":"Powershell- Automate IT Tasks and Microsoft 365 Administration","position":3,"total":3,"prev":"PowerShell- Automating IT Administration","next":null}]'
path_count: 1
tags:
  - course
  - topic/network-and-system-administration
  - skill/office-365-administration
  - skill/powershell
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Network%20and%20System%20Administration/PowerShell%20for%20Microsoft%20365%20Administration.md)

![PowerShell for Microsoft 365 Administration](https://media.licdn.com/dms/image/v2/C560DAQEuh7ybpgsnNg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1639075040486?e=2147483647&amp;v=beta&amp;t=eGWEUoCTqR6GJ6_yv7gMA82FJrhEUZRAR21qpbgSC1A)

# PowerShell for Microsoft 365 Administration

> As a task automation and configuration management program, PowerShell is useful for a variety of applications. Knowing how to create reusable, deployable components in PowerShell optimizes your functionality as well as your performance, especially compared to in-app administration centers, which are helpful but often inefficient. In this course, instructor Liam Cleary shows you how to use PowerShe

> [LinkedIn Learning](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration) | 4h 9m | Intermediate | 46K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [PowerShell for Microsoft 365](#powershell-for-microsoft-365)
  - [What you should know](#what-you-should-know)
- [**1. Connecting to Microsoft 365 Using PowerShell**](#1-connecting-to-microsoft-365-using-powershell) (9 videos)
  - [Why use PowerShell to manage Microsoft 365?](#why-use-powershell-to-manage-microsoft-365)
  - [Overview of PowerShell syntax](#overview-of-powershell-syntax)
  - [How to connect to Azure Active Directory](#how-to-connect-to-azure-active-directory)
  - [How to connect SharePoint Online and OneDrive for Business](#how-to-connect-sharepoint-online-and-onedrive-for-business)
  - [How to connect to Exchange Online](#how-to-connect-to-exchange-online)
  - [How to connect to Microsoft Teams](#how-to-connect-to-microsoft-teams)
  - [How to connect for security and compliance](#how-to-connect-for-security-and-compliance)
  - [Connecting to Microsoft 365 services](#connecting-to-microsoft-365-services)
  - [The Microsoft PnP PowerShell commands](#the-microsoft-pnp-powershell-commands)
- [**2. Managing SharePoint Online and OneDrive for Business**](#2-managing-sharepoint-online-and-onedrive-for-business) (7 videos)
  - [Connect to SharePoint Online and OneDrive for Business](#connect-to-sharepoint-online-and-onedrive-for-business)
  - [How to create, update, and delete site collections](#how-to-create-update-and-delete-site-collections)
  - [How to create, update, and delete sites](#how-to-create-update-and-delete-sites)
  - [How to create, update, and delete lists and libraries](#how-to-create-update-and-delete-lists-and-libraries)
  - [How to manage users and groups within SharePoint Online](#how-to-manage-users-and-groups-within-sharepoint-online)
  - [How to manage OneDrive for Business using PowerShell](#how-to-manage-onedrive-for-business-using-powershell)
  - [How to manage external sharing capabilities](#how-to-manage-external-sharing-capabilities)
- [**3. Managing Exchange Online**](#3-managing-exchange-online) (3 videos)
  - [How to connect to Exchange Online](#how-to-connect-to-exchange-online)
  - [How to create, update, delete, and manage mailboxes](#how-to-create-update-delete-and-manage-mailboxes)
  - [How to manage exchange online permissions](#how-to-manage-exchange-online-permissions)
- [**4. Managing Microsoft Teams**](#4-managing-microsoft-teams) (6 videos)
  - [How to connect to Microsoft Teams with PowerShell](#how-to-connect-to-microsoft-teams-with-powershell)
  - [How to create, update, and delete teams](#how-to-create-update-and-delete-teams)
  - [How to manage channels](#how-to-manage-channels)
  - [How to provision policies in Teams](#how-to-provision-policies-in-teams)
  - [How to create templates in Teams](#how-to-create-templates-in-teams)
  - [Modifying core configuration](#modifying-core-configuration)
- [**5. Managing Security and Compliance**](#5-managing-security-and-compliance) (8 videos)
  - [Explain the security and compliance features](#explain-the-security-and-compliance-features)
  - [Security and compliance demo](#security-and-compliance-demo)
  - [How to manage Safe Links, Documents, and Attachments](#how-to-manage-safe-links-documents-and-attachments)
  - [How to create alerts and activity alerts](#how-to-create-alerts-and-activity-alerts)
  - [How to create data loss prevention policies](#how-to-create-data-loss-prevention-policies)
  - [How to create sensitivity labels](#how-to-create-sensitivity-labels)
  - [How to create sensitivity policies](#how-to-create-sensitivity-policies)
  - [How to review audit logs](#how-to-review-audit-logs)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [PowerShell for Microsoft 365](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/powershell-for-microsoft-365?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/powershell-for-microsoft-365?u=76281980&t=0)** - [Liam] Hi, I'm Liam and I'd like to welcome you to the course. This course is going to take you on a journey to understand firstly the core [[Powershell]] modules and commands available within [[Microsoft 365]], secondly, how to connect to each service, third, the available commands, and then lastly, how to manage parts of each service, including creation of policies and configuration. We will walk through the out-of-the box capabilities. Our focus will be performing standard day-to-day tasks for managing the tenant. By the end of this course, you will have a greater understanding of how to manage your Microsoft 365 tenant using PowerShell. So join me for the [[Representational State Transfer (REST)|rest]] of this course to understand how to manage Microsoft 365 using PowerShell.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (3), [[Microsoft 365]] (3), [[Representational State Transfer (REST)|Rest]] (1)
> **Tools:** powershell (3)
> **Speakers:** - [liam] (1)

#### [What you should know](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/what-you-should-know?u=76281980&t=0)** - [Instructor] Using [[Powershell]] to manage [[Microsoft 365]] is an area that all organizations often struggle with. Knowing the modules to utilize, which commands to use, and then how to cross services are critical to effective management. To be successful with PowerShell, you simply need some basic knowledge and skills of managing Microsoft 365 and deployments, along with some experience scripting using basic PowerShell. If you do not have any experience with PowerShell within Microsoft 365, no need to worry, the course will give you an overview of where to start. This course will start by helping you understand the available modules, commands, and how to use them. The course will also step through common tasks, configurations, and policy creation for Microsoft 365.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (4), [[Microsoft 365]] (4)
> **Tools:** powershell (4)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)


### 1. Connecting to Microsoft 365 Using PowerShell

[↑ Back to Table of Contents](#table-of-contents)

#### [Why use PowerShell to manage Microsoft 365?](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/why-use-powershell-to-manage-microsoft-365?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/why-use-powershell-to-manage-microsoft-365?u=76281980&t=0)** - [Instructor] So why use [[Powershell]] to manage [[Microsoft 365]]? Well, PowerShell for Microsoft 365 is a powerful management tool that compliments the admin center. For example, you can use PowerShell automation to easily manage multiple user accounts, licenses, and also to create reports. There are six core reasons to consider when trying to use Microsoft 365 PowerShell for managing and configuring the tenant. The first one is that you can retrieve information that's not often available directly within the admin center. You're also able to utilize features that are only available within PowerShell. You can manage and perform bulk operations, such as updating all of the accounts. You can perform queries and filter any of the data that's available within the admin center. We can export any of the content and logs. And then, really, we can manage and configure multiple services in one single interface. For example, right now, to manage Exchange Online, we have to go to the Exchange admin center, to manage [[Microsoft Teams]], we go to Microsoft Teams, to manage [[SharePoint]], we go to the SharePoint admin center, whereas in PowerShell, we can use a single session, connect to everything and then manage everything that we need to. So let's talk about the first one, which is retrieving information. The Microsoft 365 admin center displays most helpful information that we can kind of find, it lets me know information about the users,
>
> **[1:35](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/why-use-powershell-to-manage-microsoft-365?u=76281980&t=95)** the licenses, the services, et cetera. It doesn't show all the possible information though, that Microsoft 365 stores about user licensing, mailboxes and sites. An example of this would be trying to view licensed features based on a specific location. So we would need to utilize PowerShell to be able to identify some of that information. Our next one is the PowerShell-only features. The Microsoft 365 admin center provides access to everyday, practical administrative tasks that apply to most [[Microsoft Office|Office]] 365 tenants and environments. The admin center's purpose is so that you, as the administrator, can carry out the most common management tasks. PowerShell obviously is the first place where most of the new features get deployed. So you can often execute PowerShell and run new features or enable new components or even update configuration that is not available with inside the admin center. Our next one is the bulk operations. The visual interface of the Microsoft 365 admin center is really valuable when you're executing single operations, so updating an account, updating a mailbox, updating a site. However, when you need to change multiple values or specifically selected large sets of items, so all of the users, all of the mailboxes,
>
> **[3:11](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/why-use-powershell-to-manage-microsoft-365?u=76281980&t=191)** the Microsoft 365 admin center is not always the best. An example of this would be let's say we wish to remove one of our users, Adele, from all of the SharePoint sites that she's been added to. Now there's no option for that within the admin interface, this would definitely have to be a PowerShell action. Next is the ability to perform queries and filters. Now the admin center provides several ways to filter data to locate targeted subsets of information, so for example, display a list of mailboxes within New York, so you can filter that out. However, there are limitations on what you can do as far as that filtering would go. So another example would be displaying a list of all mailboxes within either New York or London. The interface just doesn't have that level of filtering and querying. This is another reason to utilize PowerShell. Then of course, we can export content and the logs. So the Microsoft 365 admin center lets you view lists of data. Not every list of information or data is exportable to you, as the administrator. The admin center doesn't provide a way to print the displayed lists either, so for example, if you wanted to export a list of users with all of the specified fields that are associated to the user, you cannot do that within the admin interface, you actually have to use PowerShell. Then of course, managing multiple services. The components that make up Microsoft 365 work together.
>
> **[4:46](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/why-use-powershell-to-manage-microsoft-365?u=76281980&t=286)** Every day admin tasks often cross those services. For example, if you wanted to modify user properties, including mailbox specifics, then you're managing a user account in [[Microsoft Entra ID|Azure Active Directory]] and then the mailbox that's associated to it. PowerShell can provide access to service specific information, which can then combine with other values, so instead of having to access multiple admin centers, you're able to access a single interface or session, which is connected to all of the services.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (14), [[Microsoft 365]] (10), [[SharePoint]] (3), [[Microsoft Teams]] (2), [[Microsoft Office|Office]] (1)
> **Tools:** powershell (14)
> **Analogies:** for example (5), such as (1)
> **UI Navigation:** go to (3)
> **CLI Commands:** find (1), make (1)
> **Definitions:** is a  (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [Overview of PowerShell syntax](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/overview-of-powershell-syntax?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/overview-of-powershell-syntax?u=76281980&t=0)** - [Narrator] So let's review [[Powershell]] Syntax. There were three steps to the PowerShell process within [[Microsoft 365]]. The first is to download the required PowerShell modules locally, making them available for connecting to the cloud. The installed dash module command-let gets one or more modules from an online repository. The command-let validates the modules and copies the folders to the installation location on your machine. Install modules are not automatically imported making them unavailable to you after the installation. You can filter which module is installed based on the minimum, maximum and exact versions of a specified module. The Import-Module command-let adds one or more modules to the current PowerShell session. A module is a package that contains members that can be used in PowerShell. Members include command-lets, providers, scripts, functions, variables, and other tools and files. After a module is imported you can then use any of the module members in the current session. By default, the Import-Module imports all modules and members that it exports. But you can use the alias function command-let and valuable parameter to restrict which ones you need to utilize. The Import-Module command only imports into the current PowerShell session. To import the module into every session
>
> **[1:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/overview-of-powershell-syntax?u=76281980&t=94)** you need to utilize the Import-Module command within your PowerShell profile. In order to use any of the Microsoft 365 modules, you first need to install and then import them into the current PowerShell. In the example here, we are installing the module for [[Microsoft Entra ID|Azure Active Directory]], and then we are importing that module to make it available to us. The second piece is to connect to the required cloud service using the supported approaches provided by the imported and installed module. There are three options we can use when connecting to Microsoft 365. Firstly, the installed and imported PowerShell modules provide commands for connecting to the chosen service. PowerShell supports session connections, which stream the required PowerShell commands directly into the current PowerShell window or session. Lastly, we can use the [[Microsoft]] Graph PowerShell commands which called the [[Representational State Transfer (REST)|rest]] API commands to perform various tasks as needed. In order for us to connect to Microsoft 365, we can utilize standard connectivity. So connect dash [[Microsoft Entra ID|Azure AD]] for connecting to [[Active Directory]] in the cloud, or connect dash exchange online. Each connection command has a different name. We can also credentials when trying to connect. So for example, we can populate a credential variable with our username and password
>
> **[3:07](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/overview-of-powershell-syntax?u=76281980&t=187)** and then pass those values directly to the method that we wish to use. So connecting to exchange or [[Microsoft Teams]] or [[SharePoint]] Online. We can also connect using the session option instead, this is an older version that doesn't support some of the [[Multi-factor Authentication]], but from here we can pass credentials and then we can create what's called a New PowerShell Session, which then creates a basic auth connection to the cloud. Once that's done, we then import the session that we've created directly into our existing PowerShell. At that point, any PowerShell commands that we need are streamed into the current session. We can also then utilize the Microsoft Graph, which is the newer model that's available to us, where we install and import a specific module. So I'm going to say install module. Then I can import the module. And then this time around when we connect, we use specific scopes. So the scopes are API permissions available to us within app registrations. Lastly, once we've made the connections, we can then execute the required commands through the PowerShell session directly to the cloud. Now, in order to utilize PowerShell, it's important to understand that PowerShell uses a verb noun naming system. Most of the command names consist of a standard verb hyphenated with a specific noun. Some examples could be get dash service, new dash service,
>
> **[4:44](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/overview-of-powershell-syntax?u=76281980&t=284)** restart a service or even suspend dash service. Viewing the corresponding commands for a verb is achieved by using the dash verb or dash noun property. Microsoft 365 is often limited to a core set of verb Noun combinations. The most common verbs are add, get, new, remove, select, set, update, and connect. A pipeline is a series of commands connected by pipeline operators. Each pipeline operator sends the results of the preceding command to the next command. The outputs of the first command can be sent for processing as input to the second command. The output can be sent to yet another command. The result is a complex command chain or pipeline that is composed of a series of simple commands. For example, we can retrieve all Azure AD users then pipe the results for sorting or even selecting of specific fields. In the example here, we're retrieving all of the Azure AD uses and then sorting by user principal name. We could also then say, get Azure AD user, which retrieves all of the users sort by user principal name and then only select the specific fields. Variables are a core building block of PowerShell. When connecting to Microsoft 365, it helps to populate variables with the results from the commands executed.
>
> **[6:18](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/overview-of-powershell-syntax?u=76281980&t=378)** Variables can contain multiple results or singular values. Each variable is reusable and available during the current session. They can also be defined within reusable scripts, which either require manual or automatic population depending on the purpose of the script. So in this example, I have a variable called user, which is populated with a static value or the user variable is being populated from the output of a command. We can then load the variables with existing data if we need to as well by creating some kind of results, populating those and then iterating through the users one by one. Once the PowerShell modules are installed and imported into the current session, you can review the list of commands that are available. All commands, filtered commands and specific commands can be retrieved. So in this example here, we're using get dash command, which is a standard PowerShell command-let and filtering to Azure AD. I can also get specifics about a command-let by typing the names such as connect dash Azure AD. I can also then get either the types such as verb or nouns. And then I can also retrieve by specific commands to say get me the values that are inside the Azure AD. Now we can also execute standard commands so I can retrieve an Azure AD user for example, and then use a filter option. I can retrieve a specific SharePoint site for example,
>
> **[7:52](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/overview-of-powershell-syntax?u=76281980&t=472)** notice the syntax is slightly different. And then of course I could create something new by using the various command-lets. The key here is that Microsoft 365 in its PowerShell modules provides all the capabilities that you would need to manage Microsoft 365.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (23), [[Microsoft 365]] (8), [[Microsoft Entra ID|Azure ad]] (8), [[Microsoft]] (2), [[SharePoint]] (2)
> **Tools:** powershell (23)
> **Analogies:** for example (4), such as (2)
> **Definitions:** is a  (4), is an  (1)
> **Prerequisites:** install (4)
> **Env Vars:** api (2)
> **CLI Commands:** make (1)
> **UI Navigation:** select the (1)

#### [How to connect to Azure Active Directory](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-azure-active-directory?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-azure-active-directory?u=76281980&t=0)** - [Instructor] So how do we connect to [[Microsoft Entra ID|Azure Active Directory]]? Well, first off, there are some core prerequisites. The first is that we need to launch [[Powershell]] on our machine as a local administrator. We then need to install the [[Microsoft Entra ID|Azure AD]] module, then import the module into our current session. And then most importantly, the account that we using needs to be a [[Microsoft 365]] administrator account or set of credentials. There are two different modules that can be used to manage Azure AD for Microsoft 365. The first is based in [[Windows]] PowerShell. And this is version 1 of the module for Azure Active Directory. It's also known as the MSOnline module or [[Microsoft]] Online module. And the commandlets themselves are prefixed with Msol at the beginning of the name. The second one is the Azure Active Directory Graph module, as it's referred to. And this one here is what's called version 2 of the Azure Active Directory module itself. And the commandlets include as AzureAD in their name. There are three common ways of connecting to Microsoft 365. The first one is to utilize a browser login. So that means that directly from the PowerShell window, a browser will load. We could also populate a credential object with a username and a password. Or we could use what's called a service principle, which is created within Azure Active Directory.
>
> **[1:35](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-azure-active-directory?u=76281980&t=95)** Now, if we wanted to connect using the version 1 module, then we would first install the module, which is the MSOnline module. And then we would import the module directly into the PowerShell session that we are in. We then need to create the credential object that needs to be utilized. This will then prompt us for the username and the password. And then we can use what's called Connect-MsolService, which is connect Microsoft Online service, and we pass the credential object. Now, if we wanted to use version 2 of the module, then we'll now install the module called Azure AD. There is also a preview version which is called Azure AD Preview. We can then do import module of that same module. We can then pass the same credential object or create a new one. And this time around, we use Connect-AzureAD and then pass the credential object to it. Our other option was to utilize the Microsoft Graph PowerShell, where we install the module Microsoft.Graph. We then import the module. Now I will let you know here that the importing of the module can take some time, because it's not just a single module that's being installed, but a host of different modules. And then of course, here, we then connect to the Microsoft Graph. And the way we connect here is by using Scopes, and it will launch a browser where we can authenticate. Then we consent to the Scopes or the Scope permissions, and then we're able to access whichever service it is
>
> **[3:12](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-azure-active-directory?u=76281980&t=192)** that we've identified with the specific set of permissions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Entra ID|Azure active directory]] (5), [[Powershell]] (5), [[Microsoft]] (5), [[Microsoft Entra ID|Azure ad]] (4), [[Microsoft 365]] (3)
> **Tools:** powershell (5)
> **Versions:** version 1 (2), version 2 (2)
> **Prerequisites:** install (4)
> **Definitions:** known as (1), means that (1), is called (1)
> **Speakers:** - [instructor] (1)

#### [How to connect SharePoint Online and OneDrive for Business](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-sharepoint-online-and-onedrive-for-business?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-sharepoint-online-and-onedrive-for-business?u=76281980&t=0)** - [Instructor] So how do we connect to [[SharePoint]] Online and [[Microsoft OneDrive|OneDrive]] for Business? Well, first off, same prerequisites. We need to launch [[Powershell]] as an administrator. We then install the SharePoint Online module, and then we import that module directly into the current session. And then the account that we're using needs to be a [[Microsoft 365]] administrator. However, if we're just going to manage SharePoint, it could actually be just a SharePoint administrator. The SharePoint Online PowerShell module provides the management for OneDrive for Business and SharePoint Online. From an installation perspective, the installable version can be downloaded from the [[Microsoft]] link, or we can use the Install-Module option to actually install that module directly into the PowerShell session. So two different approaches, depending on what you support within your environment. Now to install the SharePoint Online module, we run Install-Module, and the name of the module is Microsoft.Online.SharePoint.PowerShell. We then import that module in and it will make all of the commandlets available. Now to connect to SharePoint Online once we have it installed and imported, we use Connect-SPOService, Very similar to the other previous, So Connect [[Microsoft Entra ID|Azure AD]], Connect Teams, et cetera. And this time we pass the URL of the admin center that we wish to connect to. The admin center is normally whatever the tenant domain is -admin.[sharepoint.com](https://sharepoint.com).
>
> **[1:36](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-sharepoint-online-and-onedrive-for-business?u=76281980&t=96)** We can also connect to a specific site collection that isn't the admin one, too. So instead of using Connect-SPOService, we can use Get-SPOSite. Now just be aware that when we connect, we do need to have called Connect-SPOService first, before we can execute any other commands. You need to have that connection to SharePoint Online or whatever service it is you're utilizing. Now to connect to OneDrive for Business, you'll notice it's exactly the same. We obviously first connect to the administrator endpoint, which is obviously Connect-SPOService and then tenant-admin. And then to retrieve the OneDrive for Business site collections, we use the same command which we use to retrieve a normal site collection, but we filter it by using a couple of different things. We use a property called IncludePersonalSite and set that to true. That will add the personal locations into the list that Get-SPOSite can retrieve because by default it will only retrieve regular site collections, and then we can filter it to the path because all OneDrive sites are something -my.[sharepoint.com/personal](https://sharepoint.com/personal)/ whatever the email address would be. What we can also do then is utilize that to get a specific for a user. So if I know it's domain-my slash slash user, then I can retrieve that individual OneDrive for Business site. Now we can utilize the Microsoft Graph again
>
> **[3:11](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-sharepoint-online-and-onedrive-for-business?u=76281980&t=191)** so we can install the module and import the module. And the key to being able to utilize the Microsoft Graph is to set the right permissions. So our scopes for this, you'll see the core object is Sites and Files, not users and groups and contacts. Users and groups and contacts would be Azure AD, sites and files are OneDrive and SharePoint.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SharePoint]] (13), [[Microsoft OneDrive|Onedrive]] (7), [[Powershell]] (4), [[Microsoft]] (4), [[Microsoft Entra ID|Azure ad]] (2)
> **Prerequisites:** install (6), you need to have (1)
> **Tools:** powershell (4)
> **URLs:** [sharepoint.com](https://sharepoint.com) (2)
> **CLI Commands:** make (1)
> **Env Vars:** url (1)
> **Analogies:** similar to (1)
> **Warnings:** be aware (1)

#### [How to connect to Exchange Online](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-exchange-online-14407607?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-exchange-online-14407607?u=76281980&t=0)** - [Instructor] So, how do we connect to Exchange Online? Well, I have prerequisites here. We first launched [[Powershell]] as an administrator, and then we install either the Exchange Online module, or we connect using the session-based approach. We can then import the module or the session stuff into the current PowerShell session. And of course, our account must be a [[Microsoft 365]] administrator of some description. It could be the global admin, or it could be an exchange administrator. Now to connect to Exchange Online, we have two call options. The first is the PowerShell remote session. This is the one that doesn't support modern authentication. It doesn't support multifactor authentication, and there's no requirement to install a PowerShell module. The Exchange Online module does support modern authentication. Does support multifactor authentication, but you do need to install the Exchange Online Management PowerShell module to utilize that. Now, if we were connecting using a PowerShell session, we first got to initiate some credential object to grab the username password, and then we create a new PowerShell session. You'll notice that the configuration name for this is [[Microsoft]].exchange with a URI that points to [[Microsoft Outlook|Outlook]] and [[Microsoft Office|Office]] 365. Then we pass credentials, and then that session will be initiated. And there'll be a bunch of redirects that take place you can see in the PowerShell session, and then the session will be created. The session itself is then imported into the current PowerShell window.
>
> **[1:35](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-exchange-online-14407607?u=76281980&t=95)** Now, up until you run that last command that import the PS session, you don't have access to any of the Exchange Online commandlets. That last command physically tells the remote Exchange Online servers to push down the PowerShell commands to make them available to you. Now, if we wanted to use the modern approach, we can install Exchange Online Management module. We can then import that module directly into the PowerShell window that we're in. From that point, we can then start to connect by using Connect-ExchangeOnline, and we can then pass various options. Now what's nice about the module is it supports the modern multifactor authentication approach. So you'll see, I'm simply passing a user principal name, and it will go ahead, and launch a browser, and let me go through the authentication flow. So if my organization is using multifactor in a web browser, then it will go through that flow, and then let me have a session in the Powershell window. However, I can use it without the modern authentication, it will do backwards compatibility. So I can patch a credential object into that same exchange, online connection commandlet. So we get the best of both worlds. 'Cause there are oftentimes where you are working in an environment that doesn't support multifactor, and so you still need to be able to connect without being forced through that approach. Our obviously last option here is to utilize the Microsoft Graph again so we can install the module, and import the module.
>
> **[3:08](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-exchange-online-14407607?u=76281980&t=188)** And then as before, our scopes will change. So you can see I'm using mail read, write mail, read, write, shared mailbox, settings, et cetera. By defining the scopes or the API permissions as mail, when we get to the consent option, we consent to the permissions to the mail, and then there are commandlets that are available based on the scopes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (12), [[Microsoft]] (2), [[Microsoft 365]] (1), [[Microsoft Outlook|Outlook]] (1), [[Microsoft Office|Office]] (1)
> **Tools:** powershell (12)
> **Prerequisites:** install (5)
> **Env Vars:** uri (1), api (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [How to connect to Microsoft Teams](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-microsoft-teams?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-microsoft-teams?u=76281980&t=0)** - [Instructor] So how do we connect to [[Microsoft Teams]]? Well, for our prerequisites, we launch [[Powershell]] as an administrator again. This time we installed the Teams online module, and then we import that module into the current session. And then of course our account must be a [[Microsoft 365]] administrator, such as a global admin, or it could be a Teams administrator. Now the Microsoft Teams PowerShell module is a set of cmdlets for managing Teams directly from the PowerShell command line. You can install the Microsoft Teams PowerShell module, either using the PowerShell Gallery, or you can also manually download it and install it if needed. It provides management for all aspects of the Teams administration, including users, teams, policies, and configuration. And it also supports authentication mechanisms such as access [[Tokens]], as well as credentials. Now, to install the Microsoft Teams module, we first run Install-Module, and the name of it is called MicrosoftTeams. We can then import the module. And then if needed, we can also update the module as newer versions are available. And this is the same for any of the other modules that you wish to utilize for connecting to Microsoft 365. Now to connect to Microsoft Teams, we can use the modern authentication approach by using Connect-MicrosoftTeams, which will just launch the browser and we can authenticate. We also have backwards compatibility by passing a Get-Credential object were we able to connect, and we can also connect
>
> **[1:33](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-microsoft-teams?u=76281980&t=93)** to different types of Microsoft Teams. So if you happen to work in a government or a military team, then there's an extra parameter available to us. And you'll find this as available also in the other modules where you can specify the team's environment name, and if it's a GCC or a GCC high or a DOD, you can utilize the same modern authentication, but you're connecting to any one of those entry points. We can also utilize the [[Microsoft]] Graph again. So we install and import the module as before. And notice the difference here in the scopes, our API permissions. Now there are more of these, but as an idea, anything that's Team, TeamsTabs, TeamSettings, TeamsApp, TeamMember, we can add those as scopes and then during the consent we'll authorize that. And then there are a series of commands available to us specifically for Microsoft Teams. Now, I suppose the key here is with the Microsoft Graph, is that whatever the scopes are will determine if the PowerShell cmdlets that are provided will actually function.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Teams]] (7), [[Powershell]] (6), [[Microsoft 365]] (2), [[Microsoft]] (2), [[Tokens]] (1)
> **Tools:** powershell (6), command line (1)
> **Prerequisites:** install (5)
> **Env Vars:** gcc (2), dod (1), api (1)
> **Definitions:** is a  (1), is called (1)
> **Analogies:** such as (2)
> **CLI Commands:** find (1)
> **Speakers:** - [instructor] (1)

#### [How to connect for security and compliance](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-for-security-and-compliance?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-for-security-and-compliance?u=76281980&t=0)** - [Instructor] So how do we connect for managing security and compliance? Well, our prerequisites: we launched [[Powershell]] as an administrator as before, and then we install and import the Exchange Online module, or we create the session. This means that security and compliance components are inside the [[Microsoft]] Exchange components. And the account we're using must be [[Microsoft 365]] administrator or some credential that matches any one of the admin roles. There were three options for connecting to the security compliance features within Microsoft 365. The first is to use basic authentication, so PowerShell Session Approach. Then we have the Exchange version one module, and then we have the Exchange version two module. From a basic authentication perspective, no PowerShell module is needed. The connection is purely a PowerShell session. The Powershell modules are not available until you've initiated the session. And then the commands are not available until the session has been updated or you use the import PSSession object. And this utilizes the new PowerShell session object. Now from an exchange version one module, there's no support for PowerShell Core, so as in cross-platform PowerShell: macOS, [[Linux]], and [[Windows]]. It can be installed via the download from the Exchange admin center. It does support multifactor and federated authentication, and you can utilize this same set of modules to manage Exchange Online,
>
> **[1:33](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-for-security-and-compliance?u=76281980&t=93)** as well as the Security and Compliance features. Now, the Exchange version two module supports multifactor modern, and credential authentication. They support for Exchange Online, Security and Compliance, and the standalone Exchange Online Protection. You install that module using PowerShell, and it is a cross-platform module, with support for macOS, Linux, and Windows. Now from a basic authentication connection, we've looked at this previously, you'll notice a slight adjustment here, where we were retrieving the Get Credential object. We still create in the configuration in the PowerShell session called Microsoft.Exchange, but our URL is a little bit different. We're now connecting to ps.compliance.protection.[[Microsoft Outlook|outlook]]. So we connect into the compliance components. Once we have connected, we do the same import as we did before. Now, the difference here is when we connect. We don't connect using Exchange Online or anything like that, we use what's called connect-IPPSSession, which will specifically create a security and compliance connection, not a regular Exchange connection. If we're using the version one module, once we've installed it, we can simply say, connect the session. If I'm connecting to a government or a military security and compliance one, then you can see we have some different connection endpoints. So we using the PS compliance one, you'll notice the difference in the URL.
>
> **[3:07](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-for-security-and-compliance?u=76281980&t=187)** This is connecting to one in Germany, so outlook.de, and then you've got the authorization endpoint. So anything in the gov cloud is .us instead of it being .com. Now, if we're using the version two module, we first need to install the module and then import the module, and then we get to use the same commandlet again, so Connect-IPPS session, and we can either pass it with the user principal name for modern authentication, or we can actually go ahead and pass a credential for backwards compatibility. So the most important thing to note here is that it doesn't matter which approach we use. It's the exact same commandlet for connecting. So how do you know if it worked? Well, during the initial connection, you should see a progress bar that displays, and it will run through the list of commands that are being imported into the current session. All the Security and Compliance PowerShell commands will then be injected into the current session. And of course, if you execute a command, such as Get-RoleGroup, it should return a value. Now we can utilize the Microsoft Graph again, instead of using the Standard Security and Compliance PowerShell modules, we can do this by installing the module, importing the module. And then once again, we set our scopes to what we'd need them to be. So instead of being users, or mail, or teams, et cetera, we use security actions, policy, eDiscovery, et cetera. Once these have been done and we pass the consent,
>
> **[4:41](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-for-security-and-compliance?u=76281980&t=281)** then the PowerShell commands available for these categories will then execute as expected.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (13), [[Microsoft]] (3), [[Microsoft 365]] (2), [[Linux]] (2), [[Windows]] (2)
> **Tools:** powershell (13)
> **Code Identifiers:** macos (2), ediscovery (1)
> **Env Vars:** url (2), ipps (1)
> **Definitions:** is a  (2), means that (1)
> **Prerequisites:** install (3)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Connecting to Microsoft 365 services](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/connecting-to-microsoft-365-services?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/connecting-to-microsoft-365-services?u=76281980&t=0)** - [Instructor] So, how do we connect to multiple [[Microsoft 365]] services? Well, normally, Microsoft 365 administration involves multiple services. You often start by managing a simple user account, but then you'll need to administer licensing, maybe [[SharePoint]] permissions, and then maybe Exchange permissions and mailboxes. Now there is no single module that allows connection to all of the Microsoft 365 services. And of course, you will need to create multiple connections to manage all of those different supporting services. Now, there are three core steps for connecting to multiple services. The first is to install and import the required [[Powershell]] modules. Next, we define any variables that are to be re-used through the process. And then lastly, we actually connect to each of the services. So firstly, we need to import the modules. So if I was trying to connect to multiple services, I would import the module for [[Microsoft Entra ID|Azure AD]], [[Microsoft]] Online, Microsoft Online SharePoint PowerShell, Exchange Online and [[Microsoft Teams]]. They would be the core set that I would import. We then need to go through the process of installing those modules, once we have them imported. Remember, once you import, that doesn't mean they are currently available in the same session. Next we need to define any variables. So as an example, it could be the organization, so this would be the tenant prefix, so if your tenant prefix was Contoso, then it would be Contoso in that variable.
>
> **[1:33](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/connecting-to-microsoft-365-services?u=76281980&t=93)** Then of course, my admin user account, for this example, I'm going to be using a credential object, so I'm going to say username is this, and then my credential object is whatever I create with that username and password, so three re-usable variables. What I then want to do is start connecting to the various applications. So firstly, I'm going to connect to [[Microsoft Entra ID|Azure Active Directory]]. I'm going to pass my credential object, so connect to Azure AD with the credential object. I'm then going to connect to SharePoint Online with the same credential object with my variable for the tenant domain. I'm then going to connect to Exchange Online and pass the values, or I could pass the credential. I'm going to then connect to Security and Compliance and I'm going to connect to Microsoft Teams. So we can install the modules, import the modules, and then simply reuse those and connect to all of the services in one go, which means it takes a bit of time to connect, but it then means that we have access to all of the services from a single PowerShell session, which you cannot do within the admin interfaces without bouncing between the various browsers that you would have open for the admin centers.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft 365]] (3), [[SharePoint]] (3), [[Powershell]] (3), [[Microsoft Entra ID|Azure ad]] (2), [[Microsoft]] (2)
> **Tools:** powershell (3)
> **Prerequisites:** install (2), you'll need (1)
> **Definitions:** means that (1)
> **Speakers:** - [instructor] (1)

#### [The Microsoft PnP PowerShell commands](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/the-microsoft-pnp-powershell-commands?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/the-microsoft-pnp-powershell-commands?u=76281980&t=0)** - [Instructor] Now let's understand the [[Microsoft]] PnP [[Powershell]] commands. So the first question is, what is PnP PowerShell? Well, PnP or patterns and practices is a cross-platform PowerShell module. It provides over 500 commandlets that can work within your [[Microsoft 365]] environment. However, the commands are specifically focused on [[SharePoint]] Online, [[Microsoft Teams]], Microsoft Planner and Microsoft Flow. So you won't be using this module to do all of your administration. Now, why would you use the PnP PowerShell modules? Well, first off, it enhances the existing modules that are there. It doesn't supersede any modules. Instead, it offers enhancements and improvements. The PnP PowerShell module is managed by Microsoft with support from the wider Microsoft community. And it provides best practice approaches for managing PowerShell. If I wish to use the PnP PowerShell, as before, we're going to install a module, and this is called PnP.PowerShell. I can then import that module in. And then we have to enable tenant access by using what's called Register-PnPManagementShellAccess. And what this will do is force a consent so that the relative permissions, for example, like the Microsoft Graph ones that we've been looking at, are then set. At that point, we can then say Connect PnPOnline. And for example, I'm passing in a SharePoint URL.
>
> **[1:33](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/the-microsoft-pnp-powershell-commands?u=76281980&t=93)** And then I have a connection to it. And then all of the PowerShell commandlets that are available in the PnP become available to us. So the PnP PowerShell is a subset of commands that are helpful and can enhance the way that you would do your PowerShell management.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (11), [[Microsoft]] (6), [[SharePoint]] (2), [[Microsoft 365]] (1), [[Microsoft Teams]] (1)
> **Tools:** powershell (11)
> **Definitions:** is a  (2), is called (1)
> **Analogies:** for example (2)
> **Env Vars:** url (1)
> **Best Practices:** best practice (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)


### 2. Managing SharePoint Online and OneDrive for Business

[↑ Back to Table of Contents](#table-of-contents)

#### [Connect to SharePoint Online and OneDrive for Business](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/connect-to-sharepoint-online-and-onedrive-for-business?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/connect-to-sharepoint-online-and-onedrive-for-business?u=76281980&t=0)** - In order for us to connect to [[SharePoint]] Online, and [[Microsoft OneDrive|OneDrive]] for business, utilizing [[Powershell]] we first need to import a specific module, or at least install the module and then import it into the current session, so we can actually make a connection out. The first thing we have to do though, is check whether we actually have the module installed, and import it into our current session. So, I can say Get-Module and you'll see this will return the list of modules that I have available to me in the current session. So, clearly there's nothing that says SharePoint in the name, so that means the module isn't available to me. To add the module, we first need to say Install-Module, we can pass a name to it at this point which will be [[Microsoft]].Online.SharePoint.PowerShell. Now, not every module is named as long as this one, but the SharePoint one is a little bit longer than the [[Representational State Transfer (REST)|rest]]. So, I'm going to say install module, this will then prompt me because it's going to try and connect to the PowerShell gallery, and then I can choose the yes option to install. And what you should see is the blue bar suddenly flicks up, tells me it's installing, and then it should just install it and then complete. Now, if we're looking at other types of PowerShell modules, some of these can take a while to install and download because of the size of the files. Okay, so now we have installed the module. Let me go back to my Get-Module command and enter, you'll notice I don't get the SharePoint one, but I do get things like PowerShellGet,
>
> **[1:33](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/connect-to-sharepoint-online-and-onedrive-for-business?u=76281980&t=93)** and I get some management and utility ones and [[Package Management]], so some of these were there before, but now we've added to it because we ran the installer option. Now, the first thing we have to do to utilize it is import that module directly into the session that we're in. So, that's one of the keys about PowerShell is you have to include the modules in the session that you'll utilizing, otherwise the commands are not available, so let me just do this. So, import the module, and of course it'll come back and complain and sometimes it does, and it will complain about that being unapproved verbs. Now, if you remember how PowerShell works, it utilizes a verb noun scenario, so things like add, get, new, remove, et cetera. There will be some commands that don't follow that model. So, now that we have that I can say, Get-Module, and sure enough we should see that top one, Microsoft.Online.SharePoint.PowerShell, and then you'll see the exported commands. Now, what I can do here is I can say, well, I want to get a module I could say by name. So, let's do Microsoft.Online.SharePoint.PowerShell,
>
> **[2:46](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/connect-to-sharepoint-online-and-onedrive-for-business?u=76281980&t=166)** I could do this and it will return me the same information. I could then do this and just say Select, expand the property which is called ExportedCommands, and just do enter. And then sure enough what you'll see now is a list of all of the commands that are available to me now that the module is installed. So, fairly straightforward so far. Now, of course, once we have it installed, what we then need to do is to look at how to connect to SharePoint Online and OneDrive, and that's done by utilizing a couple of different approaches. The first one is to actually utilize a credential object which is what we'll do, so we'll create a variable called user. And what we'll do is put our [[Microsoft Office|office]] 365 user account directly in as the account we wish to utilize, so I'm going to paste that into here. You'll notice two things about this. The first thing is this is the admin account that I'm using, and you'll see it's got the prefix for the domain. So, I'm going to create another variable here called tenant, and what I'll do is I'll just select this option and then I'll paste that into there, so that gives me the prefix of the domain for the tenant because when we utilize SharePoint Online, predominantly if we're doing administration functions, we connect him to the admin URL. Now, the admin URL of a SharePoint admin center is normally the tenant-admin.[SharePoint.com](https://SharePoint.com). Whereas site collections will be tenant.[SharePoint.com](https://SharePoint.com)/ and then whatever the path might be afterwards.
>
> **[4:23](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/connect-to-sharepoint-online-and-onedrive-for-business?u=76281980&t=263)** So, we have a user and then we have a tenant that we wish to utilize. Now, the command to utilize for this is called Get-SPOService, and the Connect-SPOService is the command that we utilize by passing in these various options. So, the first thing I'm going to do here is I need to create some kind of connection, so I'm going to get rid of connect here. The first thing I want to do is I wannna create a credential object because the connect one supports not only the standard passing in credentials, but also the browser as well. So, let's start with the first one, so I'm going to say, Get-Credential, I'll say UserName, and then I'll pass my user, and then there's also a message box, and I'll just say Message. Normally you would say something like, please type your username and password. So, I'm going to press enter and you'll see it pre-populates my account. I'm actually just going to copy my password here, and then paste it in and click okay. Now, what did that create? Well, if I click creds, you'll see it created a username and a SecureString, so a key value pair which is my credentials. Once I have that now I can then utilize that as part of that other command for connecting. So, if I go here and say, Connect-SPOService, you can see, I need to pass in a URL, so remember, we're going to use [https://$tenant](https://$tenant), which is my variable -admin.[sharepoint.com](https://sharepoint.com). What I can then do is choose the credential object
>
> **[6:01](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/connect-to-sharepoint-online-and-onedrive-for-business?u=76281980&t=361)** and say creds and press enter. Now, you'll notice it doesn't return any values back, so how do you know whether it's connected? Well, the easiest way is to just utilize an existing command, like Get-SPOSite, press enter and sure enough if it returns values from this list of site collections, then of course you are connected. Now, we can also disconnect as well a specific session, so I can say Disconnect-SPOService if I just clear that, and then I go back to Get-SPOSite, you'll see that that's not going to work because I have no valid connection. So, what happens if you're trying to connect and you have to go through either a third party authentication mechanism from [[Microsoft Entra ID|Azure AD]], or maybe [[Multi-factor Authentication]] 'cause that creds object doesn't function that way. Now, what we can do here is we can go back to our list, get rid of the credential options and just simply pass in the URL. Now, if I click enter here, now what's going to happen is it will launch a browser window that I can then enter credentials into. There you can see that we've got that browser window available to me, so let me just copy my credentials, I'm going to paste these into here, so same thing again, we have the password and click sign in, same thing again. How do we know if it's functioned, or let's do Get-SPOSite and sure enough it has to. Now, of course it was the same process for me because I wasn't utilizing multi-factor authentication
>
> **[7:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/connect-to-sharepoint-online-and-onedrive-for-business?u=76281980&t=454)** for that, but if I did the browser one would have supported that, whereas the other one would not. So, that's how we install the module, import the module, and then make a connection to SharePoint Online and OneDrive for business.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SharePoint]] (14), [[Powershell]] (8), [[Microsoft OneDrive|Onedrive]] (3), [[Microsoft]] (3), [[Multi-factor Authentication]] (2)
> **Tools:** powershell (8)
> **Prerequisites:** install (7)
> **URLs:** [sharepoint.com](https://sharepoint.com) (3), [https://$tenant](https://$tenant) (1)
> **Env Vars:** url (4)
> **Definitions:** is a  (2), is called (2)
> **Cross-References:** go back to (3)
> **CLI Commands:** make (2)

#### [How to create, update, and delete site collections](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-site-collections?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-site-collections?u=76281980&t=0)** - [Instructor] So now that we have a connection, the next thing we want to do is look at how we can manage site collections. Now, if you think in the [[SharePoint]] world, we have the admin center itself, we then have site collections, which are the boundary of the sites, then we have sub sites, then we have lists and libraries and folders, et cetera. So the first thing to manage predominantly will be site collections. So, first off, let's have a look at how we get a list of the site collections. And you can see we're going to use that same command that we used for testing, which was get SPO site. And there's a property called detailed. Now what we're also going to do is actually go in and say, format table, and then I'm going to auto size that table to make it fit. So let's just press enter. Now, of course, what you'll see is it creates a table structure and it says URL and owner, and starts to break them all down. But you can see that we get a list of all of the site collections and there's a whole bunch of them here. Now, of course, you'll see that there's one that's called my that's the one drive for business one. And then you'll see the [[Representational State Transfer (REST)|rest]] of them are just regular site collections. So the my is the parent one for one drive for business sites. Now what's interesting is we don't really know if any of these site collections are connected to [[Microsoft Teams]], for example. So what we can do is when we connect, we can also do what's called a limit and we can say, I want to limit it to everything and then we get to utilize some where syntax. So I want to identify site collections,
>
> **[1:37](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-site-collections?u=76281980&t=97)** for example, that are teams connected. So we can say .IsTeamsConnected and I'll say, equals dollar true and then close that out. So our statement is really straightforward. It's going get me all of the SharePoint site collections with no limits whatsoever where the property is teams connected is equal to true. And so I'll press enter here and you'll see that big list is now suddenly condensed down to just the site collections that have a team connected to it. Now we could go a little bit further and say, well, maybe some of those site collections are connected to groups instead, so we can use the same syntax again. So get SPO site, I'm going to limit it to all of them again and then there's also a property called group ID defined, and I'm going to say true, and then press enter. And you'll see this time around that list is a little bit bigger, which means that these are site collections that have been connected to an [[Microsoft Office|Office]] 365 group. They are not necessarily the same as Microsoft Teams. So when you're managing site collections, just be aware you may be having to identify, now can you do that in the admin interface? Well, of course you can, you can kind of have a look at the template that's been used. You can then look at whether it has what's called group owners or regular, but of course you can't necessarily identify which one of those have teams connected to them. So hence we utilize the kind of the basic properties that are available.
>
> **[3:10](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-site-collections?u=76281980&t=190)** Now, of course we have all of these site collections and we can retrieve individual ones if we needed to, we could say, get SPO site and then I can iterate through the various properties. You can see there's a whole load of them here, but the most important one is obviously I can go through and get the identity, or I could just pass a URL to a specific one. So if I say, get SPO site, let's say I want to use the operations one. I'm going to copy that. Then I can go back to my get SPO site paste the URL, and then what it will do is it just brings back specific information. If I needed to view more details about the one, then I can pipe it out and I could say select star, for example. And then this will give me all of the properties that are available to that site collection. So remember that when we retrieve an object in [[Powershell]], we can get more details by changing the select statement that comes at the end of it. Okay. Now, one of the most common tasks that we would do is obviously create specific site collections. So the first thing we'll do here is define an owner of the new site collection that I wish to create. So I'm just going to use my standard admin account here, paste that in there, so that's owner, then I'm going to create a storage quota, which is going to be the size that I want to allow for that specific site. I can then go in and say, I want to specify the URL of that site, whatever that URL is going to be.
>
> **[4:45](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-site-collections?u=76281980&t=285)** So that was my admin account. So let me just go and get my HTTPS, I'll then copy my [[Microsoft 365|M365]] remember it's not on [[Microsoft]] it's [sharepoint.com/sites/PowerShell](https://sharepoint.com/sites/PowerShell). So we'll call it that, once I've got my URL, then of course I can get my template that I wish to utilize. Now, of course, it's interesting and obviously important to understand what the team site templates are, what the names of the templates are, whether it's a team site, whether it's a teams channel, whether it's a group, whether it's something else. So what you'll need to do is identify that list of what it would be. I'm actually just going to go ahead and I will create a team site so we can use a basic one that isn't connected to a group. So we'll use STS#3. And then of course, I'm going to define a variable for the title of the site. I'm going to call it PowerShell. Okay. So I've created my basic variables. So the owner, the storage, the URL, the template I wish to use, and the title. Now as with most things in PowerShell, it's going to follow a similar model. So where was you were using get SPO, we're now going to say new SPO site. And of course, it's going to say, well, who's the owner going to be? And I'm going to say, well, I actually want to use my owner variable. It's going to say, well, what's the storage quota?
>
> **[6:20](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-site-collections?u=76281980&t=380)** I'm going to use my variable for that. So storage quota is going to say, well, what's the property URL? Dollar URL. It will then say, what's the template that we wish to utilize? I'm going to use my variable template. I can then say the title. And then I'm going to use that title. And then I have a property which is always really powerful to use, which is called no wait, which means it just goes ahead and creates that site. So I'm going to press enter. It's going to go ahead and complete. Now it reckons it's completed that straight away. So how do we tell? Well, let's do get SPO site and just drop the list down and we can scroll through the list of sites that have been created. And if we just look here, we're looking for one called PowerShell and sure enough, there's the PowerShell site that's been created. So it was really, really fast doing it in there using PowerShell. And really because we utilize the no wait option as well. Now, of course, what happens if we needed to change what that would be? Maybe we want to update the title or maybe we want to change the owner, whatever that would be. So what we can do here is create a variable called site. I'm going to say, get SPO site and I'm going to pass that URL that we created. If I just do dollar's site here, you can see, it gives me the details. Remember if we do the pipe option and do select star, you'll see all the values. And if we scroll up and down here, you can see we've got the URL, we've got the details. Is it active? And so my down here, there will be a display title. So it'll say PowerShell.
>
> **[7:53](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-site-collections?u=76281980&t=473)** So let's say we wanted to update that specific. So we can say update SPO site. Now notice what's happening here. There's no update. This is the key when we're looking at some of the verb nouns we often don't get an update, but what we always have is a set SPO site. So just be aware, it's not as simple as well I'm going to update it. You would think he could put update, but you actually have to use the [[Microsoft Word|word]] set. So I'm going to say identity and I can use my variable here. So I'm going to say the URL of that site, and then what I'm going to do a site title. And instead of it saying PowerShell, I'm going to say PowerShell site and press enter. So this is now going to go to that site. So let me go back to my get SPO site here and look through the properties. Now remember we can do the pipe and then select star, or if we already know the property, we can just do this and sure enough, the value of the site has now been updated to match what it was that we typed in the first place. So fairly straightforward to create and to update. If I needed to remove a site collection, I could do that by utilizing this member identity. And I'm going to say site.Url and then of course I do have the no wait option. So I could say remove, I'm going to say yes, and then this will go ahead.
>
> **[9:24](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-site-collections?u=76281980&t=564)** And to confirm that it's been removed, I can say, get SPO site. And if we scroll up, you'll see that that PowerShell one has now been removed. So it's really simple to create, update and delete site collections by utilizing standard PowerShell command lets.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (12), [[SharePoint]] (3), [[Microsoft Teams]] (2), [[Representational State Transfer (REST)|Rest]] (1), [[Microsoft Office|Office]] (1)
> **Env Vars:** spo (13), url (12), https (1), m365 (1), sts (1)
> **Tools:** powershell (12)
> **Exercise Files:** template (5)
> **Definitions:** is a  (2), means that (1), is called (1)
> **UI Navigation:** scroll up (2), go to (1)
> **Analogies:** for example (3)
> **Cross-References:** go back to (2)

#### [How to create, update, and delete sites](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-sites?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-sites?u=76281980&t=0)** - [Instructor] Now SubSites are the other sites or the smaller containers that reside inside a [[SharePoint]] site collection. So imagine the SharePoint site collection is the parent container and then SubSites are smaller containers. SubSites can have their own document libraries, their own lists, as well as permissions et cetera. Now the downside is in the [[Powershell]] modules, there's not really many commands outside of some of the PnP commands which we'll talk about later that can work with SubSites. So what we have to do is look at creating specific functions as self. Now what we're going to do is walk through the idea of creating a specific function. So I'm going to create a new function called SubSite and I'm going to to require a WebURL and a credential. So this is just standard PowerShell. This isn't anything to do with [[Microsoft Office|Office]] 365. This is just me creating a function because there's no functions that exist for retrieving SubSites. Now what's important here, is it's utilizing what's called the CSOM. So what we can actually do here is say New-Object. And then what we're able to do is to create a object utilizing CSOM. So you'll see we utilizing [[Microsoft]] SharePoint client context, with the URL that we pass into it. What we can then do is pass credentials to that in a specific format, which is going to be a SharePoint online set of credentials. So you can see I've got my context variable,
>
> **[1:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-sites?u=76281980&t=94)** I'm passing credentials, and then you'll see I'm using the username and password, which is sent from this object here. Okay that's our first piece. We actually have to create some kind of connection because we don't have a straight PowerShell command, we actually have to construct it ourselves. What I'm then going to do is create a variable called Web, and I'm going to use the current context of the connection, which is going to be whatever the URL is. And I'm going to say go and get the current web. Now the web in PowerShell command lets in SharePoint, or at least in CSOM is a SubSite. Now because we using CSOM nothing is available to us until we actually load the object. So I'm going to say well actually I want you to go and now load into the current context, that specific web, what I can then do is then say context dot and of course I have to execute the query now. So you notice it's a little different than using a standard PowerShell command, but what's nice about trying to write CSOM is that you get to understand what the existing PowerShell commands are already doing. So I'm now going to execute that query. And then from the function, I just want to return a web object and then I'm going to close that function out. So now I have the function written, so let's just review. I created a new function that requires a URL and a credential. It's then going to create a context for the specific URL by passing in the credentials, then it will go and grab the web object, load it into here, execute the query,
>
> **[3:09](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-sites?u=76281980&t=189)** and then return it to me. So what I should be able to do now, is say web equals get SPO and you'll see now my SubSite function is available. And when I look through the properties it'll say WebURL. So there's our first one. So I need to have a specific URL and then I can then pass in a credentials object subject. So the first thing we have to do is obviously get at WebURL. So let me do get SPOSite just so I can choose a specific site we want to use. So I'm going to say let me do global sales here. So I'm going to use that one. So let me just create a SiteURL variable so I can use this going forward, pasting global sales. Okay, so now dollar web equals, and I'm going to say get SPOSubSite. My WebURL is going to be SiteURL variable. So I'm using the global sales site collection, which means it's going to look inside that site collection for any SubSites. And then from a credential perspective, I already have a creds object. I'm going to press Enter. So this is now going to go and connect, and did it return anything? Well let's go to web and show enough it did return. It returned the global sales route site, which has returned me the web or the list the SubSite that makes up the site collection. So think about the logic here
>
> **[4:41](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-sites?u=76281980&t=281)** when you spin up a site collection you get an entry point, you then provision a site into the site collection, which effectively is the same as a SubSite except it's the root level. So now I have a web object I can then go through and pass a URL to any of those if I needed to. And it would return me the details of that specific web. Now the good thing is it means I can then pass a URL to a specific one. If my SiteURL had a SubSite called project one underneath global sales, then I could pass that as the URL and it would retrieve information. Whereas the existing command let's get SPOSite only retrieve the value at the global sales level. So that's great. So we were able to kind of do that. Now what about if I wanted to create a specific SubSite in that too? Now of course we have to go back to the same idea again, of passing a specific value. So I'm going to go ahead and copy some information here into the window to create a specific function. Now it's going to ask me to paste, which is okay so this is my full function. So new SPOSubSite same thing again I'm creating it using CSOM. It's going to ask me for a SiteURL. It's going to ask me for the SubSiteURL. So what I want to call it underneath, it's going to ask me for credentials. It's going to create the same context as before, but then we're going to create this web client object, where we're going to start creating information for a web. And then you can see we're going to specify the template
>
> **[6:15](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-sites?u=76281980&t=375)** like we did with the site collection, choose whether to inherit permissions, the title we want to call it, what the URL is going to be. Add the language so 1033 is standard for here, at least in the US for English. And then of course I'm going to say, go to the current context to the web that I'm in and then add this new web and then execute. So that gives me my new function. So you can see if I do New-SPOSubSite, I can loop through the various properties. So that means my functions now available. So what do we need to create? Well the first thing we have to create is a series of variables that we wish to pass to it. So the first thing is we have the SiteURL, so I don't need to worry about that. But what I'm going to do here is say SubSiteURL. And then I want to choose what that SubSiteURL is going to actually look like. Like what's the value that needs to go in there. Now if we go back over here and get rid of our values that we just typed, if we just go to SiteURL, you can say that I've got this URL right here. So let me just copy this. So let's create my new variable. So SubSiteURL and then I'm going to paste the URL and then I'm going to call it PowerShell. Okay so we have a URL and we'll come back and visit this in a second. So we have that. So now what I can do is say New-SPOSubSite, it's then going to say well what's the first property, which is going to be my SiteURL.
>
> **[7:50](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-sites?u=76281980&t=470)** So I'm going to say SiteURL which is global sales. I can then say SubSiteURL which is SubSiteURL. And then of course from a credentials perspective, we're just using the creds object. Like so and now I'm going to press Enter. Now of course what happened here? well it says there was an exception calling something, it can't add something, and that's because the SubSiteURL value is really just the name that we want it to be called. So I'm going to come all the way back here. I'm going to get rid of all of this here, repopulate my SubSiteURL, and then rerun that and sure enough it now goes ahead and tries to create it. So when it's asking for a URL for creating, it's not actually creating, ignore the push notifications feature. That's okay but you'll see that the site itself. So if I say Get-SPOSite, so let's go back and look through our list here. You can see if we scroll through some way in here should be a PowerShell one. Remember did it create the PowerShell one? Or no it created it in the global sales, but we don't see that PowerShell one anywhere. We just see the root level. So we go back to our first command that we created web Get-SPOSubSite WebURL So let me go and get my global sales
>
> **[9:24](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-sites?u=76281980&t=564)** paste it here. Remember our first one what's it expecting? It's expecting the URL now I know I created it called PowerShell, and then I'm going to say credentials, dollar creds and press Enter. And if I may say web, you can see it's now returned that specific URL because the SPO one doesn't do it. So it's as simple as that. We actually have to create our own functions to be able to make adjustments and changes to SubSites themselves. The same goes for updating as well as removing as well. Now of course if I wanted to create an update function, I'm going to go ahead and copy again and then paste into this window here. So let me just paste like so the function this time is called Update-SPOSubSite. It's going to ask for the URL, the credentials, and in this instance I want it to update the title and the description. So let me just press Enter here. So this way around I can say Update-SPOSubSite. The WebURL is going to be the WebURL that we just utilized. Now of course we can go into here and say SiteURL, and then I could say PowerShell. So I can say like so, so we could actually do this because the SiteURL, isn't going to be that full path. It's just going to be the path up to there. So I've got my WebURL . And then of course
>
> **[10:56](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-sites?u=76281980&t=656)** it's going to ask for the credentials object. So dollar creds and then it's going to say well what do you want to call as the title? I'm going to say PowerShell SubSite. And then of course I'm asking it to update the description, PowerShell SubSite, and then I can go ahead and update that site. So if I go back to my previous one where it says get the web object, like so, and then do dollar web, we get all of those values. If I scroll through the properties here, then you'll see that my title here says PowerShell, and there should be description around here somewhere that will then be updated because remember when we update items in SharePoint we actually have to wait for the updates to be applied. So they don't always appear immediately. You actually have to wait for those values to be returned back to you. But the update is the same as the create and the delete. Now talking about delete, how would we remove sets of SubSites? Well we can do that simply by creating another function. So I can go into functions here. I'm going to copy it and drag it into this window here. So let me just choose clear and then paste. This function is a little bit similar. It's called Remove-SPOSubSite. It requires the WebURL and the credentials. And then what it will do is it loads into the current context. Loads the web object, gets all of the SubSites folder and then iterates all of those SubSites
>
> **[12:30](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-sites?u=76281980&t=750)** and then deletes the objects that are there. So for example in our global sales, when executing this it would remove the PowerShell one that I already created.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (16), [[SharePoint]] (6), [[Microsoft Office|Office]] (1), [[Microsoft]] (1)
> **Env Vars:** url (16), csom (6), spo (2)
> **Tools:** powershell (16)
> **Definitions:** is a  (3), is called (1)
> **Cross-References:** go back to (3)
> **UI Navigation:** go to (3)
> **Analogies:** imagine (1), for example (1)
> **CLI Commands:** make (1)

#### [How to create, update, and delete lists and libraries](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-lists-and-libraries?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-lists-and-libraries?u=76281980&t=0)** - Now, when working with the site collections and the subsites, one of the areas of focus will often be lists and libraries. And of course, as with some of the features that we've seen so far in the out-of-the-box capabilities, you don't get the commands that are required to manage specific things like lists and libraries. So what we can do here is we can repeat the same process we just went through by creating a specific function. So I'm actually just going to paste in here, the code for one of the functions which is called "Get-SPOList". So this is utilizing the CSOM objects, again. Now, obviously as before, it's going to require a web URL and a credential object and then it will connect to the object or the URL that we specify and effectively retrieve the lists and load them for me. So, if we look into our web object from before, we still have our URL which is the "Global Sales" and the [[Powershell]] subsite. So, what we can do here is say lists and then we can use a new function. So "Get-SPOLists," I can say web URL, which is what I'm tryna get to, which is going to be the current web and then I'll say, "URL" and then the credentials object, which will be my creds one and I'm going to press enter. So this is now connecting to the URL, which is as subsite of PowerShell. So if I do lists here, you're going to see this long one. So I'm going to say, "Format table." So it should go that way.
>
> **[1:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-lists-and-libraries?u=76281980&t=94)** And you'll see, we've got some values that are being pulled out here. So I'll tell you what we'll do, the downside to some of the formatting is that you don't always get to see what's going on. So I'm going to say, "Select title." So we can just see that. And so it lists the libraries inside that subsite, which is the default. So pages, assets, page gallery, documents and composed looks. So that shows me that it's connected to the specific option and you can see that we're utilizing a variable and then piping out the values to how we want them to get. Now of course, that's great, we can just retrieve them. We can also create functions to retrieve items if we need to as well. But what about if we want to create specific libraries and lists as part of, you know, provisioning type processes. So, I'm going to come back and copy another piece here. This will be another function called "New-SPOList" and what we'll do is we'll paste it here and you'll see it's very similar to the previous one which was the Get-Command but this time around, we're creating new list creation information, we're giving it a title, the template to use and then we're going to add it and then we're going to set the list description, and then we're going to update. So, fairly painless but in order for it to work, we obviously have to set a few variables. So, the first thing obviously is the URL which we already have, but then we need to specify a list name. So let's call this PowerShell as well. What we'll then do is we'll set the list description that we want to do
>
> **[3:06](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-lists-and-libraries?u=76281980&t=186)** so I can say, "List created using PowerShell." And then of course, I've got my list template that I wish to create. So, we can obviously choose the template that we want to utilize. Now, the list templates themselves, they have unique identifiers as well. So, we can specify and say, "I want it to be a custom list or I want it to be, you know, a custom one that I created or use one of the existing list template IDs that might exist already." So, we need to specify what that is going to be. Now, those are normally specified as numbers or like the team site templates where you're able to use like STS#0 or something else, you can specify those as a specific value. So, let's choose a specific one that we want to utilize. So, let's create a calendar. So, a calendar is actually has an ID of 106 so we'll utilize that. There are lists available online to give you those numbers. Okay, so we have our list name, our list description and then we also have a template. I'm actually going to update the list description to get rid of the capital I here. And there we go. And then what I'll do is I'm going to say, "New-SPOList." Now, what's it going to ask us for?
>
> **[4:39](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-lists-and-libraries?u=76281980&t=279)** Well, the web URL. So I'm going to say, "Web.URL." It's going to ask me for the name which I'm going to use as my list name. It's going to ask me for a description which is going to be my list description. It will then ask me for the template which is my unique template for the list. So list template, like so. And then our last one is obviously going to be credentials. So dollar creds. So, what this will do now is connect to that site and provision a specific list. Now, how do we know it's been created? Well, we could browse to the user interface or we could just go back through our previous commands here and I'm going to say, "Get SPOLists" from that site. I'm going to say, "Lists, select title." And sure enough, you can see that there's one now called PowerShell that's been created which is now the template based on the template that we chose. So really, really straightforward to be able to create a specific [[SharePoint]] list and you might wonder, "Well, why do we need to go to the hassle of that?" Well, think of a provisioning process that we're going through and building site collections, then we're building subsites, then we want to provision specific sites with lists and libraries that are specific to that one. Now, of course, it's great that we can go ahead and create those, but what about creating items that would exist within the specific list itself or being able to retrieve items
>
> **[6:13](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-lists-and-libraries?u=76281980&t=373)** that exist within a specific list or setting permissions? Those are options that we have available to us as well. And we can also add columns to the specific lists as well. Now there are various things that we can do, obviously, as you just mentioned, what about if you wanted to just remove a specific list or we can use CSOM again, so I can come into here and I'm going to paste a new function and the function here is "Remove SPO List Item" for example. So, let's say the list has items in them. I can then go through and retrieve those list items and then remove them or I could add new list items. So I'm going to save this and then what we'll do is actually go to the list and just add a couple of items to that list. So, here we are in the list and obviously what we can do is we can view the items that are here. So I can say, "Events" and you can view calendar view, et cetera. I could just say, "New event," click event, this will pop the window up here. I'm going to call it test one and we'll call it test one. We'll leave it as it is then we'll just click save. So there's my test and I'll do another one. We'll just call it test two, test two. And let's say we just change that to a different time and click save. Okay, so we have two items in the list. What we can now do is we treat those values utilizing PowerShell. So, let's go back to the PowerShell window. So, you're back in the PowerShell. So let me just clear the screen so we have some space. And what we can then do is actually look at
>
> **[7:46](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-lists-and-libraries?u=76281980&t=466)** how we would remove those SPOListItems. Now, of course, we created the function ready. So the web URL is "$web.URL". The name that we're trying to get is the list name. So I'm going to say, "$list name," 'cause we already have a list name. And then of course we have credentials and I'll pass in my creds object. And we press enter. Now what this will do is go ahead and connect it to the list and remove the list items that are in that specific list. So, let's go back to the browser and just check it. So we're back here, let me just refresh and you'll see those items have now been removed automatically. So, not only can we create lists and libraries, we can also add to them, we can also remove from them as well as actually removing the specific libraries and lists themselves.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (8), [[SharePoint]] (1)
> **Env Vars:** url (11), csom (2), sts (1), spo (1)
> **Exercise Files:** template (10)
> **Tools:** powershell (8)
> **Cross-References:** go back to (2)
> **UI Navigation:** go to (2)
> **Analogies:** similar to (1), for example (1)
> **Definitions:** is called (1)

#### [How to manage users and groups within SharePoint Online](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-users-and-groups-within-sharepoint-online?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-users-and-groups-within-sharepoint-online?u=76281980&t=0)** - [Instructor] Now, one of the other areas of focus within [[SharePoint]] Online and [[Microsoft OneDrive|OneDrive]] for Business for that, is management of users and groups. Now the nice thing is that the functions that we need are available to us here. So if I just double-check what my URL is going to be, that's going to be my [[Powershell]] site. What I'm actually going to do is just update that to be the global sales one so we should be able to use site URL. So we'll use that one instead. What we can do here is we can use a standard function that's available to us called Get-SPOUser, which is going to retrieve a specific user or users. And what I can do is pass a site URL. So I'm going to say site URL and what you'll see is it gives me the list of all of the users that belong to that specific site. You'll also see some of the groups potentially that will be here. And normally some of the system ones like in the [[Microsoft]] tenants, we use a provisioning user, but you can see that listed here as well as the two groups that are there by default called everyone and then everyone except external users. So I can retrieve kind of a list of the users, et cetera. Now, what it can do is retrieve a report of information. So that is one thing that's really, really important is the ability for you to run a report that will basically say, I want a list of who's an admin, who's not an admin, et cetera. So we can use what's called
>
> **[1:32](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-users-and-groups-within-sharepoint-online?u=76281980&t=92)** the Get-SPOUser on one again. I'm going to pass in the site for the specific site that I want to use, 'cause of course you may want to run this report on a parse like collection basis. So I'm going to say site URL and then I'm going to pipe that value out and say select object and then I want to get the login name. There's a property called is site admin, any groups that they might belong to and then the user type for the user. And then to make it all look pretty, I'm going to format it as a table, I'm going to wrap all of the columns and I'm going to auto size it, so I'm going to press enter here. Now 'cause it doesn't look that great really here, just purely based on the fact that you're in a small window. But you could export that into [[HTML]] or you could put it as a CSV, whatever it would be. But what we can see here, is we can see the list of the user, so the login, if they are a site admin. So most of these are going to be false. You'll see me though and there's admin. And then you'll see the groups that I might belong to. So global sales members, global sales owners. So remember in SharePoint it creates those standard security groups that are available. So owners, members, contributors, viewers, et cetera. So when we're managing permissions, we can obviously add users to specific groups if we needed to. So let's take a quick look here. So you can see we've got most of these are not admins and they're just members of global sales, global sales owners, we've got people that are not members.
>
> **[3:05](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-users-and-groups-within-sharepoint-online?u=76281980&t=185)** You know, we've got users that are just in here with the standard permissions. And then you've got me, who's there as an admin. So let's have a look at my account. So my admin account here is a member of global sales members and global sales owners. Now what about if I wanted to add an account into that that wasn't a member of that specific one. Let's say wanting to add, you know, Dave, let's say, as a member of a specific owner, or I wanted to remove a specific user or I needed to add new users. Then of course what we can do is start to make adjustments utilizing PowerShell because the functions do exist. So first off, let's just clear the screen. And what we'll do is we'll just take a copy of this account here and then I'll clear everything. What I can do here is, I can say add SPOUser, which is there. It's going to ask me for a specific site, so I'm going to say site URL. I can then say login name, which is the specific user that we wish to utilize. So I'm just going to put it in quotes here because I just copied the actual name itself. And then I get to specify what the group would be. Now in SharePoint obviously, the groups are called by the name of the specific site collection that you might be in.
>
> **[4:37](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-users-and-groups-within-sharepoint-online?u=76281980&t=277)** So from a permissions perspective, what you end up with, you get site owners, site visitors, you get a whole mix of different ones that are there. So what we can do, is we can take a copy of one of the group names that would be existing. So one of the ones that we didn't see listed in the sites is called global sales visitors. So we have the global sales owners and the global sales members, but not visitors. So I'm going to go into here and I can say add the user to that specific group. Okay. So, we just add that. So now let's just loop back through the previous commands and I'm going to say get SPOUser. And then if we now look at Pradeep's account here, sure enough, he's got global sales visitor added to his list. So we can add or remove as needed moving those users in and out. Now, of course I can remove them from the group. So if we choose the opposite way, so I'm actually just going to say and tell you what let's go here. And the add command, what we'll do is we'll say remove SPOUser instead, 'cause that one exists. The site option is going to be the site URL. The login name is going to be that Pradeep option. Let me get Pradeep's email address so we can actually go through and put that in there 'cause I haven't created a user object. So, I obviously need to go through
>
> **[6:09](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-users-and-groups-within-sharepoint-online?u=76281980&t=369)** and just copy his email address so I know what to do. So let me just copy the address and paste it in here. So I'm going to copy his address, login name, Pradeep. And then for the group option, we obviously need to go through and put that other group that we just had in there, which was the global visitors, the global sales visitors role. So let me quotes and paste that into there and then enter. Okay, so we did that. Now let's go back and run that Get-SPOUser. Go back to Pradeep and sure enough, I've now been removed or Pradeep's been removed from that role. So we can add or remove users very comfortably. The last thing we can do is we can also convert people into being site collection administrators. So I can say set SPOUser. Remember we talked about set commands. We've then got the site options, so site URL. I can then say login name. And of course it's going to be that Pradeep account that we wish to utilize. So I can go in and just copy his email address again. So let me do that here. And copy and then I'll paste it into here, Pradeep. And then at this point, what we can then do is say is site collection admin, and then I can say true. So let me set him. Now of course, what actually happened, we don't see the values here. So let's go back to my get SPO and this time you'll see that
>
> **[7:40](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-users-and-groups-within-sharepoint-online?u=76281980&t=460)** Pradeep's value for this column, which if we scroll all the way back up, is the site collection admin one. So we can very quickly create or update an account to be a site collection admin or remove them as needed. So if we were to clear that for example and run that same command and this time say false, and then go back to the get command, you'll see he's now been removed. So managing permissions, adding users to the existing groups or even creating groups is really straightforward using the out of the box (indistinct).

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SharePoint]] (3), [[Powershell]] (2), [[Microsoft OneDrive|Onedrive]] (1), [[Microsoft]] (1), [[HTML]] (1)
> **Env Vars:** url (8), html (1), csv (1), spo (1)
> **Cross-References:** go back to (3), we talked about (1)
> **CLI Commands:** make (2)
> **Tools:** powershell (2)
> **Definitions:** is a  (1), is called (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [How to manage OneDrive for Business using PowerShell](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-onedrive-for-business-using-powershell?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-onedrive-for-business-using-powershell?u=76281980&t=0)** - [Speaker] Now so far, we've focused on just connecting to [[SharePoint]] online, and not really touched on [[Microsoft OneDrive|OneDrive]] for business. And that's because OneDrive for business sites, are really just part of SharePoint online. Now in order for us to actually see the actual personal sites, we actually need to do something a little bit different. So we're going to use Get SPO Sites, and then we have an option called Include Personal Site, which we set to true, which is a flag. And I can press Enter. Now of course what this will do, is go to the current SharePoint online site, go and look for all of the site collections, retrieve the list of site collections, and this time we'll include any of the personal sites that are created. And you can see that suddenly a whole bunch of them. So if we go back up here, you can see intermingled with them is my.sharepoint, and then personal, and then the name of the individual email address or the user. So OneDrive sites are really just SharePoint site collections, just under a specific place. Now of course, it's really hard to view the set of just the personal ones. So we can actually tweak this a little bit here. We can say, get SPO site, I'm going to limit it to all. I'm going to include the personal one and make sure that's set to true. And then what we can do is use what's called filter. So in the filter option, we can write almost like sequel type queries to identify what it is we're looking for. So you can see I'm doing where URL is like
>
> **[1:37](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-onedrive-for-business-using-powershell?u=76281980&t=97)** my [sharepoint.com/personal](https://sharepoint.com/personal)/ like so.
>
> **[1:47](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-onedrive-for-business-using-powershell?u=76281980&t=107)** So, we're basically saying filter out the list to show me just my ones. So I'm going to press Enter, and sure enough it takes -[mysharepoint.com/personal](https://mysharepoint.com/personal) and just shows those. So these are my OneDrive for business sites. They are manageable in the same way as they would be if they were just a regular SharePoint site collection. And if I wanted to retrieve a specific user, then of course I would need to know this whole format. Because if you notice here, the email address is not an email address. They've replaced various characters with other values. So I'm going to do clear here for a second, and what we're going to do is create a new variable, I'm going to call it login. It will be similar to the user one, but we'll just want to create something different. And I'm going to put my admin one right here. Now in order for me to make it fit in the way that I want to, I'm going to say updated login is the variable name. And what I'm going to do is say login.replace, because I need to replace some specific parameters. So whenever we have an @ sign, it gets replaced with, you see, an underscore in that specific one, and then I'm going to do another replace and we'll do this, and this time I'm going to replace the period with the same syntax. So this should be, look at the login, and what should happen is the @ and the dot and the dot, should get replaced with underscores
>
> **[3:22](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-onedrive-for-business-using-powershell?u=76281980&t=202)** to give us that format that we were looking for. So I'm going to press Enter, and then I can say updated login, and you'll see, shouldn't it look like the format of the URLs that we were looking at previously. Whereas if you just double check that and go back to SPO, you can see it should be admin, underscore, tenant, underscore, on [[Microsoft]], underscore. So we can double check to make sure, and sure enough we have the right format name. So that's just formatting the values. What I can then do is use the same Get SPO Site. I'm going to say, I want to get a specific single one. I can say SPO site limit to all of them. If we don't include the all, then some of them don't come back because it's all about paging values. I can then say, well I have to include the personal sites. Otherwise I can't even query them. And then my filter is going to change a little bit, like we did before URL dash like, and then we can say, -my.[sharepoint.com/personal](https://sharepoint.com/personal)/
>
> **[4:35](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-onedrive-for-business-using-powershell?u=76281980&t=275)** and then you can see my updated login property. So that should give us a format that we're looking for now. So let me enter that, and sure enough, it comes back with a single option, but no values. So let's just pipe the value out and say select.star and Enter, and then we'll get all of the values for this specific site collection which is the my site or the OneDrive for business site. So you can see all the properties and values. And just like any other site collection. We have everything from what it's connected to, storage quotas, who the owner would be, etc. So there's lots of properties that we can update as we kind of go through all the information. So of course what we can do, is we can retrieve things like the story. So instead of me doing select star, I could say, well actually I want to see the URL, I also want to say the storage usage current, and who the owner is. So we get to populate the various fields with the values. And if they have values, then they will return those values to us. And one of the things that you often do as part of managing them, is you often want to add a site collection. Because of course, if you are the admin, you can't manage another persons OneDrive for business site, because you are not the owner. So normally as an administrator, it often becomes your job to go through
>
> **[6:09](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-onedrive-for-business-using-powershell?u=76281980&t=369)** and somehow get added to the list of sites that are all there. So what we're going to do is actually create a new variable, I'm going to call it sites, get SPO site. I'm going to set the limit as we did before too all. And I'm going to include all the personal sites because we need to query those. And then I'm going to put that filter back on, to just say everything that's URL like, and then I'll do the -my.[sharepoint.com/personal](https://sharepoint.com/personal)/ okay? So this should give us the full list of the personal sites. So I'll just press Enter. We'll just make sure that's got values, sure enough it does. Okay? What we then want to do is we want to loop each of these, and make an adjustment to basically say, I would like my account as the admin to be a site collection administrator for each of those. Now luckily as with most things, we can use standard capabilities for that. So we can say set SPO user, which we've used before, remember to create a site collection admin or add to a group, this time when we use the site URL, we can say site.url, which is going to be this object, which is going to be the one that we're looping down through the sites object. So I can say site URL, I can say login name, and I'm going to say user, because that my admin account, and then we can use the same syntax we had before,
>
> **[7:44](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-onedrive-for-business-using-powershell?u=76281980&t=464)** so Is Site Collection Admin is true, and then I'm going to close that out. Now sure enough it's going to all the way through you can see, it's basically hitting every single My Site, and adding me as an owner for that site name. A simple task that can be done to ensure that you as an administrator, have access to a specific site collection.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SharePoint]] (9), [[Microsoft OneDrive|Onedrive]] (6), [[Microsoft]] (1)
> **Env Vars:** spo (7), url (6)
> **CLI Commands:** make (5)
> **URLs:** [sharepoint.com](https://sharepoint.com) (3), [mysharepoint.com](https://mysharepoint.com) (1)
> **Analogies:** similar to (1), just like (1)
> **Cross-References:** go back to (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)

#### [How to manage external sharing capabilities](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-external-sharing-capabilities?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-external-sharing-capabilities?u=76281980&t=0)** - [Instructor] And one of the last areas of focus really for [[SharePoint]] Online and [[Microsoft OneDrive|OneDrive]] for business is really around external sharing. Now, one of the options that we have here is obviously to utilize external users. So we have a function called Get-SPOExternalUsers, which will obviously retrieve any external users that have been added to the tenant. Now, of course, that configuration is really around whether you allow external sharing, which email addresses do you wish to use, et cetera. It's fairly straightforward, though, to allow that type of external sharing, and those accounts obviously become valid when you want to obviously share something in SharePoint Online, whether you want to allow something in Teams or OneDrive for Business or something else. So what we can do is obviously add external users, and depending on the policy that's defined, we'll determine whether users are able to share externally. The good thing is, is that obviously we can do this either by utilizing [[Powershell]] and retrieving them, or we can allow end users to configure the permissions. So let's go and have a look in the SharePoint site first and see what the external sharing configuration looks like. So we're in the SharePoint admin center, and if we just expand Policies and click Sharing, then we can see what configuration is enabled. So for example, you can see that we have SharePoint and OneDrive are set to anyone,
>
> **[1:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-external-sharing-capabilities?u=76281980&t=94)** which means that any user could potentially come in and share the specific site or file with whoever they want to. The changing of this permission dictates whether external sharing is available, whether OneDrive has the access to it, or whether the SharePoint does and vice versa, but the one drive permissions can't be higher than the SharePoint ones. So based on that configuration for external sharing will determine whether or not you can add external users. So it's important to understand that we can obviously go through and add specific users, invite them into the various applications as we need to. You can add users at the domain level, so at [[Microsoft Entra ID|Azure Active Directory]], if needed, you can add them into various other locations at the services, you can add them into specific documents if needed, you could also add them to folders if needed, you could just add them into various locations. So it's important as an administrator to be able to understand how to manage those specific accounts and check if you have external accounts. So let's go back to the PowerShell window. So back in the PowerShell window, and you can see that we already have a command here called Get-SPOExternalUser. And you'll notice that there's no external users at the moment. And that's because nobody has been, obviously, allowed to access the specific site to get in as an external user,
>
> **[3:05](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-external-sharing-capabilities?u=76281980&t=185)** and so SharePoint doesn't have any uses to kind of display. So what we can do here is we could add some by using the user interface, or we could just add them into Azure Active Directory as guest accounts. So what we'll do is we'll add one into SharePoint and then we'll rerun this PowerShell command here to see if that's available. So let's go to our specific site we've been using. So we'll go to our Global Sales, click into the Global Sales site, and then we'll choose share and then I can type in an email address of an account. And of course, notice it comes back and it says, "Your organizations policies don't allow you to share with these users, go to External Sharing and enable it." So that's important to understand that those permissions are hierarchical in nature. And so depending on the configuration at the highest level, will then determine whether or not you can actually share with other individuals outside of the organization. And they reside at the SharePoint level, they reside at the organization level, they reside within the Teams interface, they reside all over the place. So of course, whenever you make an adjustment as well, you obviously have to go through and determine whether or not you can do that. So if I go to admin center here, and if I click Policies that reside here, you can see that the external sharing for the global sales site can't be shared. So I'm going to click Edit,
>
> **[4:38](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-external-sharing-capabilities?u=76281980&t=278)** and I'm going to choose Anyone and click Save. So I'm just going to update the policy here so we can actually go through and share. Refresh the page, and then going to click Share. I'm going to type my external account like so, and it will say she's outside the organization, that's fine. I can then choose the permission, so give Edit control and choose Share. So that's an external user that I've now added. That person will receive an invitation and they'll be able to access the site. So what does it look like in PowerShell when we retrieve it now? So let's go back to that console window, okay? So back here, let's go back and say Get-SPOExternalUser. And sure enough, now that account resides and appears as a valid account, because it's now an external user. Now, the interesting thing is that command does not retrieve external users that reside in Azure Active Directory that don't have access to SharePoint. That command only retrieves external users that have been added directly to SharePoint. Now, it's important you notice about the sharing status that I kind of went through. So let's go back here and we'll use Get-SPOSite, which we've used in the past, we use Identity, and then I want to use the URL. So I'm going to say site URL, which is the URL of the site that I'm trying to get to. And then what I can see is DisableSharingForNonOwnerStatus. So I'm going to press Enter, and you'll see what it does is it brings back some information about the site. And that property, DisableSharingForNonOwnersStatus
>
> **[6:15](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-external-sharing-capabilities?u=76281980&t=375)** is a value that we can retrieve and save. So I'll tell you what we'll do, let's get rid of that one and we'll say, Select *. Oops, I chopped off the end of my L for site URL. So let's do that again, okay? And you'll see, as we go down here, you'll see it has various options and you can see SharingDomain, allowed sharing, SharingBlocking, LimitAccess. You can see that these properties are defined directly against. And most importantly is the SharingCapability, which is ExternalUserAndGuestSharing. That's the configuration that's currently set. Now, of course, if we wanted to add external users, we could do that with PowerShell, but if I wanted to just retrieve that configuration, so let's say I wanted to Get-SPOSite and I really just wanted to have a quick look at what the configuration is across the board. for all of my site collections, I can do a Select-Object and then say SharingCapability like so, and show enough what you'll get is the URL of the site. and then whether or not it's been enabled or disabled, 'cause one of the things that you can't do in the user interface is kind of see that very easily. So we can definitely use PowerShell to be able to go through and then determine whether it's on or off. We can also iterate each of the site collections if needed, or we could turn something off. So you'll see, for example here, I've got GuestSharing is configured for my, 'cause you can see like the [[Representational State Transfer (REST)|rest]] of the guest [[Microsoft Word|word]].
>
> **[7:49](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-external-sharing-capabilities?u=76281980&t=469)** So in my GlobalSales, this was the one that we enabled it on, but let's say we suddenly realized that we don't actually want that to be configured that way, so let me just clear the screen here. What I can actually do here is say Set-SPOSite. I'm going to use my identity of the site URL that I wish to utilize. And then we have a property called SharingCapability and I can specify what that would be. So I'm going to say Disabled and Enter. Now, what that means is it's going to change what that value is. So I'm going to go back to here and then go back to my GlobalSales and now you'll see it's been disabled. So I don't have to go into the user interface. It's a one line command and I can update whether or not it's been configured. Now, if I wanted to change it the other way, then of course, I could change it back to a couple of different things. So we have some specific options. So I'm just going to copy my PowerShell and paste it into the window. So let me just copy and paste here. I'm copying set SPO command. Let me just clear. And this is being copied from the training files and I click paste. And you'll say that I'm nice setting it to ExternalUserSharingOnly, which means it's for authenticated users. Now, what does that do to my list? Well, if I go back to my GlobalSales, you can see it's been adjusted now. I could also then change that again with a different command, so let me just clear this here, and we'll use this option,
>
> **[9:23](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-external-sharing-capabilities?u=76281980&t=563)** paste that into here, and this will be an ExternalUserAndGuestSharing. So what does that look like when we look at it here? You'll see global sales now adjusts to be that option as well. So you have the flexibility of being able to do that as and when you need to.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SharePoint]] (12), [[Powershell]] (8), [[Microsoft OneDrive|Onedrive]] (4), [[Microsoft Entra ID|Azure active directory]] (3), [[Representational State Transfer (REST)|Rest]] (1)
> **Tools:** powershell (8)
> **Env Vars:** url (6), spo (1)
> **Cross-References:** go back to (5)
> **UI Navigation:** go to (4)
> **Definitions:** means that (1), is a  (1)
> **Analogies:** for example (2)
> **CLI Commands:** make (1)


### 3. Managing Exchange Online

[↑ Back to Table of Contents](#table-of-contents)

#### [How to connect to Exchange Online](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-exchange-online?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-exchange-online?u=76281980&t=0)** - [Instructor] Now in order for us to manage Exchange Online, we first need to install the module. And this can be done by first checking, which modules we actually have loaded. So as you can see, I don't have an exchange module loaded. So to do this, we first need to install the module if we don't have it installed already. And it's called Exchange Online Management, I'm going to press enter here now. I should already have this installed in my machine so it shouldn't take long to prepare itself. But if you haven't installed this for the first time, it will take some time to download, it might prompt you for installing something from the [[Powershell]] gallery. So just accept the defaults. Now, if I just do get module again, you'll notice it's loaded some extra ones, but it hasn't loaded my Exchange Online. And that's because you need to load that in as well. So I'm going to say import-module Exchange Online Management, and we'll import that module. Now, when I've run get module, you can see that it's listed there as one of the modules that is available. Now, another approach that we have is to actually not use Exchange Online. We could also use the [[Microsoft]] Graph. Now this one is a little bit different. We're going to do the same thing as before. So install module, Microsoft.Graph as this one is called and we'll do enter, now, of course, I already have this installed on the machine, so it shouldn't prompt me, but it is the first time the same as Exchange, it'll ask you to confirm the repository.
>
> **[1:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-exchange-online?u=76281980&t=94)** I'm then going to say, import module, and then Microsoft.Graph. Now I'm not going to press anything yet because when I do this, this is going to default to the version 1.0 end point of the graph. Now of course I like using the beta endpoint, just because it's a bit more cutting edge. So what we can do before we do the import, I'm just going to clear my screen. And I'm going to say select MgProfile, which is a command to set the profile for the graph connector. And I'm going to say, let's use the beta endpoint instead. Now I can import the module and the module will be connected to the beta version of the partial cmdlets. So I'm going to say import module. Now, the Microsoft Graph one, isn't just importing Microsoft Graph, It's importing all of the modules that are part of the Microsoft Graph PowerShell SDK. So there's about 25 of these that will be imported. So this can take some time to load, so you might want to do this and then walk away and kind of leave it as it is. I'm just going to actually kill this one here, I don't need to load it. But now that we have it loaded, one of the things that we want to do is obviously now connect to my specific tenant, to actually see whether we can make a connection, we can connect to Exchange Online, we can manage et cetera. Now this is done by first, let us create a user variable, which will contain the user name that we wish to utilize. So that'll be a kind of admin account
>
> **[3:11](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-exchange-online?u=76281980&t=191)** that we'll be utilizing before. So let me just grab the details for this one. Let me just paste that into here, so my admin account. And then what I'm going to do here is create a credential object using the standard get credential. And I'm going to pre-populate the username by using the user variable. And then it's going to require a quick message, so I'm going to say, add password. Now of course, if I was using PowerShell 7, it would require the message, if I'm using regular [[Windows]] PowerShell, I don't need to. But either way, when a press enter here, it pops up the window little dialog box, and then I can paste in the password. And if I simply say creds, you can see I've got my security object now. At this point, I can say connect Exchange Online and I can pass the credential object like so, and then I can press enter. And the first thing it does, it gives us a little table of the types of commands that have been updated. So for example, get recipient or get mailbox, and then the new, faster equivalent versions. And then it connects to Exchange Online. So let me just clear some space here. To check it's working, I can say, get mailbox for example, and if it connects and returns mailboxes, then we know that we connected directly to Exchange Online. Now I can also disconnect from Exchange Online, like so I'm going to say all, and that will kill my existing PowerShell session that was created. So if I now choose get mailbox, you'll see it comes back
>
> **[4:47](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-exchange-online?u=76281980&t=287)** and say get mailbox isn't recognized. And that's because the module's been kind of still loaded, but the connection is an active, so the commands aren't available. We also have the ability to connect to Exchange Online simply by just pressing enter. And then at this point when you press enter, it will launch a specific browser window, or another PowerShell window utilizing Explorer. From here we can pick the account that we wish to log in with. Of course, we're going to grab a password at this point and put that in. And then if we had anything like multifactor authentication that needed to be completed, you know, maybe it's [[Microsoft Azure|Azure]] multifactor or something else, we can then complete that process, and then we're able to connect. Now let me just clear my screen here. And to double check that we have a connection, we can just choose get mailbox. You'll see it's taken some time, and then we turned some values. So we can connect to Exchange Online either using a credential object, or we can actually go through and fulfill the browser requirements depending on how we want to connect to the environment. Now the last option that we have for connecting to Exchange Online is of course to use the Microsoft Graph. So we can say connect MgGraph, and then the way that we connect to the graph is by defining the scopes or the sets of permissions that we would like to access. So for example, one of the first permissions is email. We could then do Mail.ReadWrite. We could also do maybe
>
> **[6:23](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-exchange-online?u=76281980&t=383)** Contacts.ReadWrite. So we have these various permissions that are available within [[Microsoft Entra ID|Azure active directory]]. And I when press enter here, it's going to say welcome to the graph because I already have an existing connection, but if it didn't, it would ask me to consent or authenticate, and then it would say welcome to the Microsoft Graph. So now I have a connection I can for example say, MgUser mail folder, for example, or child folder or tab through, the various different types of messages that all available or folders, or topics, or whatever it would be that I need to manage Exchange Online. So there are lots and lots of PowerShell commands that are available inside what's called Get-Mg, and we're able to manage the graph components that connect to Exchange Online. An easy example of this would be to say, Get-MgUser mail folder and then press enter. And then of course, this will go ahead and try and connect. And of course we didn't pass any properties, but what we could do is utilize the user. Now, of course it comes back and it says, oh, we're looking for a specific type of object that needs to come back, so let's have a look at my user. So my user has my unique ID here. So it's looking for the user ID. So what we can do is we could say, Get-MgUser mail folder.
>
> **[8:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-exchange-online?u=76281980&t=480)** And then if we tab through the various options, you can see that we can do searching, we can scape, we can do user ID, et cetera. So if I say user and go in, you'll see it comes back and it will say, hey, there's some items in here. Now, of course, those don't make any sense, but we could say select, you know, format as a list, for example, or we could say format as a table, or we could say just select star, and you'll see it starts to show the items in there. Now just be aware as with all PowerShell, some of the values don't return in the right format, some of them are in Jason format, et cetera. but that's as simple as how we can connect. And as easy as it is to actually retrieve some basic values to ensure that we actually have connected.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (8), [[Microsoft]] (8), [[Windows]] (1), [[Microsoft Azure|Azure]] (1), [[Microsoft Entra ID|Azure active directory]] (1)
> **Tools:** powershell (8)
> **Analogies:** for example (6)
> **Definitions:** is a  (2), is called (1), is an  (1)
> **Prerequisites:** install (3)
> **CLI Commands:** make (2)
> **Env Vars:** sdk (1)
> **Versions:** version 1 (1)

#### [How to create, update, delete, and manage mailboxes](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-delete-and-manage-mailboxes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-delete-and-manage-mailboxes?u=76281980&t=0)** - [Instructor] My managing exchange mailboxes is fairly straightforward. Using the new commands for example, we can say, Get-EXOMailbox. And the first thing we can do is learn about what's called the result size, which is going to allow us to return all of the mailboxes, and the Get-EXOMailbox is obviously a faster option. Now, obviously when it returns back, we get this kind of weird structure where it just renders everything. So what we can do is do a select object. And then for example, we could say, I want name, let's have a quick look here. We can say, just copy this value. So primary SMTP, we can paste this here and then I can do Enter. And then of course we get a different view. We get the name of the user and the primary SMTP. So when we bring information back, we don't actually have to stick with the view that's there, but the EXO mailbox one will return all of the values about a specific mailbox. Now, if we wish to retrieve specifics around a user mailbox, so let me just take Pradeep's email address here. The first thing I'm going to do is say user equals, and then I'm actually just going to use that email address here. Let me just clear some space here. And then what I can do is say, Get-EXOMailbox. And this time we're going to use the identity parameter. And then I can just say user, and this will retrieve that list of information that we saw, but it will be just Pradeep's information, nothing else. So fairly straightforward once we have this as an object.
>
> **[1:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-delete-and-manage-mailboxes?u=76281980&t=94)** So if I go back and declare a variable here and call it mailbox and do get, let me just clear some space here. Then what I can do is say mailbox dot, and you can see I can tab through the various parameters or I suppose, properties that are available. So I could say a mailbox alias. And of course, if we tab through the other options, for example, display name, or we can go back and keep tabbing through distinguished name, email addresses, you'll see the email addresses that are associated to the specific account. So it's an easy way of retrieving specific information. Now what about if we were trying to find different types of mailboxes? So what I mean by that is, let's say we're trying to identify any of the shared mailboxes. So the first thing I'm going to do is set the result size to be unlimited, because that's what we're looking for. And then what I can do is use a property called recipient type details. And then I'm able to say shared mailbox as the value. Now, I don't have any shared mailboxes, so that's good. But if I had a shared mailbox, then that mailbox would then be listed here, just like I had done if I get rid of this here and said, Get-EXOMailbox and it would return the values. So shared mailboxes if we wish to use them, not many people use them anymore, but you can, and you can retrieve information. Now, the next thing that you would often want to identify is maybe [[Statistics]] about a specific mailbox.
>
> **[3:10](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-delete-and-manage-mailboxes?u=76281980&t=190)** So there's a command called Get-EXOMailboxStatistics that will allow me to pass an identifier, so of a specific user. And then if I just press Enter here, it's going to go ahead and retrieve any statistics about the mailbox. And this is perfect because this allows me to get a quick snapshot view of the mailbox that's there. Now, what we can do is if I go back here and say, get here, let me go all the way back to get mailbox. I can start to join mailbox retrieval to statistics because rarely do you want to get statistics for a single mailbox. You probably want to retrieve it for all of the mailboxes in one go. So what we can do is we can go into here. I'm going to set the results size to be unlimited 'cause I want all the mailboxes. And then as we've done in the past, we can pipe the value of what we've just done. So this will retrieve all of the mailboxes. Then I can pass that into the statistics option, and then I can pass that value out. So select object, and I'll just choose display name, which is the name of the field here. And then maybe I'll pick the total item size and we'll just copy that one here. So what this will do is combine the two commands together to say, go and get all the mailboxes. Then when you've got them all, take each one of them and throw them into this command
>
> **[4:44](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-delete-and-manage-mailboxes?u=76281980&t=284)** and then get the values. So I'm going to press Enter here, and we'll wait for this to retrieve the mailboxes, and then execute mailbox statistics against each one, and then it should return the display name and the total item size. And sure enough it's still going. But you can see, we now have a simple report that says, this is the mailbox, and this is how much has been used as far as storage would go inside the mailboxes. So an easy way to run some kind of report if we needed to. Now, what we can do also is go through and retrieve other information about the specific mailboxes. So let's say we were trying to identify things such as, if it's an inactive mailbox, so once again, we're going to use the Get-EXOMailbox. We're going to set the result size to be unlimited because this is going to be the format that we wish to use. I'm actually just going to pipe this out and then use the tilde option here. So I can write in the next line. I'm then going to utilize a ForEach-Object loop to basically say, I want to loop through each item. And then I'm going to say, get the mailbox statistics again. And then the identity of the mailbox that I wished it. 'Cause if we're using a for each loop object, we need to pass an identity to that. So then we can use an object called the dollar underscore, which is the item that's being looped at the moment. I'm going to say user principal name.
>
> **[6:18](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-delete-and-manage-mailboxes?u=76281980&t=378)** So once I've got that user principal name, then of course, I then want to retrieve some specific details. So I'm going to say, get the user one. I'm going to pipe the value out. I'm going to tilde it again so I can select object. Now you can see what the tilde does. It just lets me kind of flow to the different lines. I'm going to do last log on time. And I'll say, last user action time, and then we'll close that down. So what we have is retrieval the mailboxes and then loop that command looking for that information. So I'm going to press Enter here. So this will go through each one. And notice what's happening, it's rendering all the mailboxes, but I don't actually have any values. So let me just stop that one from executing for a second. So what's the problem? Well, this is one of the things that's different about some of the commands is that some of the EXO commands don't retrieve the same values that the regular commands would utilize. So let's go back into here. And what we'll do is we'll do the same thing again, but this time I'm going to go through and remove the EXO option. So we're going to use the slower out of the box commands that come with the [[Powershell]]. And then we're going to go across to here again, and Enter. So notice what happens now, we'll wait for this to load. It should retrieve all the mailboxes, get all the details, and notice the values come back now. So just be aware there is a difference between
>
> **[7:53](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-delete-and-manage-mailboxes?u=76281980&t=473)** using the EXO commands and using the older commands. Sometimes the older commands are better. So that's just one way to identify inactive mailboxes. And that's done by just viewing when they logged in last. Now, once we've created the mailbox, there may be an instance where we would need to update a mailbox. So first off we want to check that we have our current user. I'm going to go and retrieve my mailbox. So Get-EXOMailbox with an identity of the user that we just created. I can just double-check that by typing in, get mailbox, the actual mailbox variable and sure enough, it gives me all the details. What I can then do is utilize a command called Set-Mailbox, which requires specific ID. So I can say identity. And then I can say mailbox, and I could say mail ID. And then at this point we can kind of loop through all of the values that we're able to update for a specific mailbox. You can see as I keep tabbing all the way through there's lots and lots and lots of these. Now of course, there are some, if I say display name, that we can update. So right now my display name is PowerShell dash user. So I'm going to say PowerShell user updated, like so, and then I'm going to press Enter. Now what this will do is check them out box itself and force an update to that mailbox. So how do we check if it's correct? Well, let's go back and retrieve the mailbox
>
> **[9:27](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-delete-and-manage-mailboxes?u=76281980&t=567)** and then I can use my variable to come back and say, get the display name. Now notice what happened here. The display name still says PowerShell user. It didn't update my display name to be what it should be. So let's go back to my mailbox and press enter. And if we scroll through, you can see the display name is still sitting here. Now some of the PowerShell commands don't enforce updates immediately. Or they will take the values that you type, but not necessarily update the values. Or they take a specific amount of time to get those values. So dollar mailbox, again, keep going here, and you can see now it's updated. So you can see there was a short space of time where it didn't actually update. So just be aware that you may make an update, check it, it didn't take, you might execute the PowerShell again and again and again, and actually you just need to wait for that command to be completed. Now, the ultimate thing here is what about if we needed to remove the mailbox? Well, that's as simple as saying remove mailbox. We can pass in the identity of the mailbox itself. So mailbox dot ID, and then I can just press Enter here. And this is going to go ahead and connect to the mailbox that we've already got by the ID that's there. It will then prompt me and say, okay, we're going to remove the mailbox and the associated account that was created. I'm going to press A, to do yes to all. Otherwise it might prompt me multiple times
>
> **[11:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-delete-and-manage-mailboxes?u=76281980&t=660)** and then I'll connect (indistinct). What we can then do a say, get mailbox. Actually, if we use the EXO one, it will be quicker. So mailbox, press Enter, and then scroll down. We know that we've got Pradeep here. If we scroll up here, we have Scott Patti, and notice that my PowerShell one has now been removed. So some execution commands will be immediate and some will have to wait. So as we are managing the mailboxes for creating, updating and deleting mailboxes, it's a fairly straightforward approach. Now what we can do is also kind of find what happened to the mailbox. So what about if we delete a mailbox and then realize that we actually needed the mailbox to kind of be put back? Well, luckily what we have is called a command called Undo-SoftDeletedMailbox, which is the original piece that goes first. I'm going to pass in my user object, which is the user. So my PowerShell dot user. I've then got a property called [[Windows]] Live ID, which is my user. So I'll talk with Dave before we do that, let's just make sure that our variables are correct. Okay, perfect. So my user and my user variables being used can work for both of these properties. So let me just say, undo again, soft deleted mailbox, dollar user, will then say Windows Live ID dollar user 'cause it's in the correct format. And then I'm going to say password
>
> **[12:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-delete-and-manage-mailboxes?u=76281980&t=754)** because what we need to do is obviously reset the password when it gets brought back in again. So secure string, string dollar, password as plain text, and then I'll force that in. Okay, and I'll press Enter. Now, what this is going to do is try and find that soft deleted mailbox, which was the one that we just deleted by the user ID and the Windows Live, and then bring it back in which will return the Windows Live or the [[Microsoft Azure|Azure]] ID account, and the mailbox back to where it was with the new password or the original password that we originally had and put that back to wherever it is. So let's give this a second to complete. It shouldn't take too long. However, it can take a little bit of time depending on the size of the mailbox that we're trying to restore. And there we go. It says it's now restored. So when I say Get-EXOMailbox now, I should be able to say my Pradeep one, and then back is my PowerShell one. So as you can see, we can create them, we can update them, we can delete them, and then we can actually restore them if we need to as well, all utilizing PowerShell.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (10), [[Statistics]] (7), [[Windows]] (4), [[Microsoft Azure|Azure]] (1)
> **Tools:** powershell (10)
> **Env Vars:** exo (5), smtp (2)
> **CLI Commands:** find (3), make (2)
> **Analogies:** for example (3), just like (1), such as (1)
> **Cross-References:** in the next (1), go back to (1)
> **UI Navigation:** scroll down (1), scroll up (1)
> **Definitions:** is a  (1), is called (1)

#### [How to manage exchange online permissions](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-exchange-online-permissions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-exchange-online-permissions?u=76281980&t=0)** - [Instructor] So how do we manage Exchange Online permissions? Well, first off in Exchange Online, the permissions that you grant to administrators and users are based on management roles. A management role defines the set of tasks that an administrator or a user can perform. When a management role is assigned to an administrator or user, that person is granted the permission provided by the admin management role. Now, there are two types of management roles. The first is administrative roles, and the second is an end user role. The admin roles, these roles contain permissions that can be assigned to administrators or specialist users. End user roles are assigned the role assignment policies enable end users to manage aspects of their own mailbox and distribution groups. Management roles give permissions to perform tasks to administrators and users by making [[Powershell]] commandlets available to those who are assigned in those specific roles. Management roles give permissions to perform tasks in Exchange Online, but you need an easy way to assign them to administrators and users instead of independently assigning them. Exchange Online provides you with two different ways of making assignments. The first is a role group, role groups enable you to grant permission to administrators and some of those specialists or power users. Role assignment policies enable you to grant permission to end users to change settings on their own mailbox
>
> **[1:36](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-exchange-online-permissions?u=76281980&t=96)** or the distribution groups that they might own. When you create a user in [[Microsoft 365]] or [[Microsoft Office|Office]] 365, you can choose whether to assign various admin roles such as the global administrator role, the service administrator role, password administrator, and so on. Some, but not all of these roles grant the user administrative permissions in Exchange Online. In Microsoft 365 or just Office 365, when you add a user to either the global administrator or the password administrator roles, the user is granted the rights provided by the respective Exchange Online role group, other Microsoft 365 or Office 365 groups and roles don't have corresponding Exchange Online role groups that match the same. So for example, we can see the global administrator in 365 matches the organization management role within Exchange Online, whereas billing administrator does not have a corresponding group, whereas password administrator matches help desk administrator in exchange and so on. Now for mailbox permissions, you can use Exchange Online PowerShell to assign permissions to a mailbox or group so that other users can access that mailbox using three different types of permissions. The first would be full access permission, Send As, so we can send emails as that person or Send On Behalf of that person. Full access allows the delegate
>
> **[3:12](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-exchange-online-permissions?u=76281980&t=192)** or the user to open the mailbox and view and add and remove the contents of the mailbox. It doesn't allow the person to send messages from that mailbox. If you assign the full access permission to a mailbox that's hidden from the address list, the user won't be able to open the mailbox. By default, discovery mailboxes are hidden from the address list. The Send As permission allows the user to send messages as if they came directly from the mailbox or group. There's no indication that the message was sent by someone else. It doesn't allow the user to read the contents of the mailbox either. If you assign the send us permission to a mailbox that's hidden from address lists, that delegate won't be able to send messages from the mailbox. The Send On Behalf permission allows the user to send messages from the mailbox or the group. The from address of these messages clearly shows that the message was sent by a different user. However, replies to these messages are sent to the mailbox or group, not to the user that sent the message. This permission obviously doesn't allow users to read the contents of the mailbox. Now to add or remove full access permission, we can utilize a PowerShell command called add mailbox permission, where we specify the identity, the user, and then the permission. We can also then remove that mailbox permission also by using a similar PowerShell commandlet.
>
> **[4:47](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-exchange-online-permissions?u=76281980&t=287)** If we wish to add Send As, you can see, we now utilize a different command called add recipient permission where we specify who the identity mailbox is, and then the trustee, and then the access. We can also remove that same permission by using the remove recipient permission. If we wish to Send On Behalf permission, then we can use the set mailbox command to select the actual mailbox. So in this case, it's Noah's mailbox and there's a property called Grant Send On Behalf To and then whatever that user might be, we can also set that same permission on distribution groups or other types of groups within Microsoft 365 and Exchange Online. Now, one of the most important things that we often want to do is review all of the permissions that are available. Now to get a full summary of all of the user's permissions, we can use the get mailbox command joined with the get mailbox permission. So in this example here, it's retrieving all of the mailboxes iterating through every single mailbox looking for the mailbox permissions, and then selecting the specific access rights and viewing that back to us on the screen. And of course we could also export that out directly into a CSV or a log file for obviously offline access. So utilize in PowerShell, we can very easily, not only just manage permissions, but we can also generate reports of the permissions that have been set.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (5), [[Microsoft 365]] (4), [[Microsoft Office|Office]] (3)
> **Tools:** powershell (5)
> **UI Navigation:** open the (2), select the (1)
> **Definitions:** is an  (1), is a  (1)
> **Analogies:** such as (1), for example (1)
> **Env Vars:** csv (1)
> **Speakers:** - [instructor] (1)


### 4. Managing Microsoft Teams

[↑ Back to Table of Contents](#table-of-contents)

#### [How to connect to Microsoft Teams with PowerShell](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-microsoft-teams-with-powershell?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-microsoft-teams-with-powershell?u=76281980&t=0)** - [Instructor] Now, as is consistent with managing any part of [[Microsoft 365]], you first need to install specific modules to make things work. And that is no different when it comes to [[Microsoft Teams]]. So first off, if we do get module, now you can see that we start into include lots of [[Powershell]] modules. As you can see the [[Microsoft]] Graph ones that have been loaded and our exchange one. Microsoft Teams is exactly the same. So the first thing we have to do is to install the module. And if I just say name and this one's called MicrosoftTeams, and I'm going to press enter here. Now, the same thing will happen. This time I haven't installed it. I'm just going to say yes, and this will prompt me to say, are you sure you want to do this and do this? And then it will come back and say, okay, hold on a minute, you already have a version of it installed. Do you wish to install a newer version, and do you wish to run it side by side? So what I'm actually going to do is instead of doing Install-Module, I can actually just... let me clear the screen here. I can just import the module because it's already installed. I don't need to update the module. The version I have is sufficient for what we need. So Import-Module MicrosoftTeams. So because it's already installed on the machine, now, I suppose, I know I've gone over this a few times, but when you install the module, it puts it onto the physical machine. When you import the module, it makes it visible and available inside the current PowerShell session that you happen to be in.
>
> **[1:32](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-microsoft-teams-with-powershell?u=76281980&t=92)** So this is now importing the MicrosoftTeams module into my existing session so that I can then start to utilize any of those commands. Now, as before, there're lots of times where the commands can take some time to load, it's now finished, but it can take some time to load depending on the size of the module. So if I now say, Get-Module, you'll see that we've now got MicrosoftTeams that's now loaded for us here. So let me just clear that. Now, of course, how do we connect to Microsoft Teams? Well, first off, let me just check my user objects. So right now my user object is set to the previous user account. I'm just going to adjust this here. So user, and we'll make it the admin account. And then if I do my creds object, you can see that's still valid, so we can still utilize that as needed. So what we can do is do the same thing we did before and say, Connect-MicrosoftTeams this time, pass a credential and use that creds object that we already created. This is now going to go and try and communicate to MicrosoftTeams, and then should we turn back, and sure enough, there we go. Now, notice it gives you a different returning value. Sometimes what you end up with is nothing. So when you connect to certain types of services, you get no response. Others will give you kind of a verbose kind of connection, or you'll end it with something like this. Now, in fact, what we talking about there, if I say verbose, when we do the connection, you can see it only really adds an extra piece
>
> **[3:07](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-microsoft-teams-with-powershell?u=76281980&t=187)** and that's down to the commands themselves. So let me just clear that. So I'm now connected to MicrosoftTeams. Now, I can also disconnect from MicrosoftTeams also, and then I can say, Connect-MicrosoftTeams, but this time we can connect to MicrosoftTeams and I can force a browser prompt this time by not passing a credential object. So I'm going to select my admin account here. This is now going to go ahead and reconnect me. So remember, the browser option is if you need to pass some kind of multifactor authentication, or something else in the authentication pipeline. So that gives me my core connection. Now, we also touched on the idea of using the Microsoft Graph to connect. So MgGraph, and remember we talked about the scopes. Now, the nice thing here is that there's a whole set of scopes that are available to us for connecting to Microsoft Teams. So for example, if I say, TeamSettings.Readwrite.All or maybe I want to manage the TeamsApp configuration, then I can do that too. So I can actually go through and connect. It's going to launch me the browser. I'm going to select the account, it's going to prompt me for consent. I can choose accept, and then it'll connect to the graph. And then I'm able to start utilizing some of those Teams commands if I wanted to kind of manage the Microsoft Teams infrastructure
>
> **[4:40](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-connect-to-microsoft-teams-with-powershell?u=76281980&t=280)** by utilizing the PowerShell graph instead. Now, not all of the kind of commands that you would expect to do in the admin interface exist in the graph. But what we can do, just to kind of prove that we can connect to things is do Get-MgTeam. Now, notice it's asking me for a TeamId, and that's because I haven't passed one yet. So maybe we could say, Get-MgGroupTeam. Notice, it asked me for a GroupId. So you can see where we go in with this. You have to kind of play with them and say MgGroup. So this is now going to retrieve any of the groups that are connected for Teams, then I can grab the ID of the group, then I can pass it into the Team, et cetera. So for example, if I grab this one and then say GroupTeam -GroupId, pass that one and you enter, it's now going to go and tie those two together. So now I get the GroupTeam, and I can start managing the Team. So that's just one way of how you would be connecting and ensuring that you had a connection to Microsoft Teams.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Teams]] (6), [[Powershell]] (3), [[Microsoft]] (2), [[Microsoft 365]] (1)
> **Prerequisites:** install (5)
> **Tools:** powershell (3)
> **CLI Commands:** make (2)
> **Analogies:** for example (2)
> **Cross-References:** we talked about (1)
> **UI Navigation:** select the (1)
> **Speakers:** - [instructor] (1)

#### [How to create, update, and delete teams](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-teams?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-teams?u=76281980&t=0)** - At the first administration task is really around managing teams themselves. So, I'm going to choose Get-team, and Get-team retrieves me a list of the [[Microsoft Teams]]. So, in my team's tenant, you can say that I have, one two, three, four, five, six, seven, eight, teams. You can see whether they're public or private and then the Mail Nicknames associated to them. Now, if I wanted to retrieve a specific team, so, let's say I want to retrieve my U.S Sales one, so, I'm going to use that, I can put the name of the team in there, I can say, Get-team, Display name, and team, and then do the same thing, and it will retrieve information about the team. Now, as you saw before, in other commands, it puts it as kind of this weird table, but if I wanted to, I could say select the specific fields, or I could just say, format table, or format list. If do format list, then what we get to see is all of the properties that are associated to a specific team. So, at that point, we can then start to perform updates if needed. Now, one of our first tasks normally, is to actually create specific teams. So, I'm going to call this one, [[Powershell]] team, that's going to be the team name. Let me just copy that, 'cause I'm going to use that again, like so. We'll do team description, like so, so, same name, same description,
>
> **[1:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-teams?u=76281980&t=94)** and then I get to say team visibility and I can set that to be a specific visibility, so, let's just say public. Once we have that, then I can obviously go ahead and create a new team. So, I can say New Team, Display Name, and we'll take our team name. I then also have the description property, which we've defined. So, team description, and then you'll notice our last one is visibility, and I can use my team visibility and press enter. So, now, what that's going to do is create me a new team. Now, what's the construct of a team? Well, it's going to be the [[Microsoft]] team's components, it's the [[Microsoft Office|Office]] 365 Group, it's the [[SharePoint]] Online site, it's the [[Microsoft Entra ID|Azure AD]] Group, et cetera. So, all of these components get spun up as part of the new team command. Now, when that's being created, obviously, it can take some time, it's not a quick process to create a team. It's even the same in the user interface. So, when you go into the Admin Center and create a new team, it's obviously going to create a team construct and then it will provision all of the other pieces over time after the first piece has been created and then continues to create them after that. So, I'll give this a moment to complete. Okay, so, it's returned. So, we now have our team. So I'm just going to say, Get-team, we'll just retrieve a new list of teams, and sure enough there's my PowerShell Team that's been created.
>
> **[3:07](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-teams?u=76281980&t=187)** So, a fairly straightforward process, we just passed the various properties. Now, what we can also do is obviously, update teams as well, because there may be times where you need to modify a specific team. So, we've got our three variable still. So, what we can do here, is I'm going to say, Dollar Team, I'm going to retrieve that specific team, and I'll use my Display Name variable that we already have. So, team name like so. Let's just make sure that we've retrieved that, so, team, sure enough we have. At this point, I can then use this Set-Team commands to update specific things. So, for example, if I wanted to, you know, use the Set command, it's actually going to require a group ID. Now, how do we get the group ID? Well, the good thing is that when we retrieve a Microsoft Team, there's a property called GroupId. So, we can pass that in, and then at this point, let's say, I wanted to change my Display Name, so, instead of it being PowerShell Team, I'm going to say, PowerShell Team updated, so, that will be my new Display Name. I'm going to press enter. So, this is now going to connect to the group. You can see that it returns a Display Name, but we don't know if it's been updated yet. So, let's go back to my Get-Team, and you'll see it still says PowerShell Team, it doesn't have the specific name update.
>
> **[4:41](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-teams?u=76281980&t=281)** So, what does that mean? Okay, now it's moved. Notice it moved places? And that's because it's got the dots, which means it's now been updated. So, let me go here, and what we'll do is just, PowerShell Team updated, and enter, Dollar Team, and sure enough, it's now updated. So, we can update them as we need to. Now, if you remember, there were other properties that we could utilize, and so, if I do format list here, and that we could modify on a team by team basis. So, you can see we've got, whether you can allow Giphys, whether you've got stickers, memes, et cetera. These are all properties that we can update for a specific team. And this is done utilizing that Set-Team command also. Now, instead of using Set-Team, we can actually go one step further, and what you'll see, is we have a different command that is available to us, and we can do Set-Team, archived, channel, picture, app, targeted. You can see, we can kind of loop backwards and forwards through various properties. Or we can just say, Set-Team, do the GroupId, so, Dollar team dot GroupId, and then we can tab through the various properties. Now, at least we've got Giphy ratings, allow stickers and memes. So, for example, let me go into stickers and memes and I'll say Dollar true.
>
> **[6:16](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-update-and-delete-teams?u=76281980&t=376)** I can then do Display Name again, I could say visibility, let's go through to allow GIPHYs, and let's say, I wanted to make that false, and I can then press enter. So, this will now go ahead and perform an update on that specific team. So, let me retrieve the team again, and team, and we want to do format list. Now, what you can see, is we've got Giphys in our false and stickers and memes in our true. And we could have updated any of the other options also, such as allow creation of channels, and deletion of channels, et cetera. So, we have the ability to manage those specific types of things against a specific team. Now, not only do we have the ability to obviously, create an update, we also have the ability to remove specific teams as well. And this is done by using the Remove-Team command, and you'll notice that everything is really related to the GroupId of the object. So, I'm going to say, GroupId, Dollar Team, which is the one that we created, and then GroupId, and I can say, remove the team. Now, if I now do Get-Team, you'll see that that PowerShell one still exists. So, if I keep saying, Get-Team, eventually... Okay, it's moving around, you can see it's up here now, and now it's gone. So, it does take a little bit of time, obviously, that was nice and quick, but sometimes it can take a long time for that to be deleted. So, we can create, we can update, and we can also delete specific teams as required.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (7), [[Microsoft]] (2), [[Microsoft Teams]] (1), [[Microsoft Office|Office]] (1), [[SharePoint]] (1)
> **Tools:** powershell (7)
> **Analogies:** for example (2), picture (1), such as (1)
> **CLI Commands:** make (2)
> **Cross-References:** as you saw (1), go back to (1)
> **UI Navigation:** select the (1)
> **Speakers:** - at (1)

#### [How to manage channels](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-channels?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-channels?u=76281980&t=0)** - [Instructor] Now when you're using [[Microsoft Teams]], one of the features that's quite common is channels within the teams, public or private channels. And depending on the types of channel that you create will determine the functionality that's available to it. So for example, if you are using a private channel, then of course, you're trying to restrict who can be in that specific channel. Now we can manage those directly with [[Powershell]] too. The first thing that we need to do though is, be able to retrieve the actual team that we kind of want to create. So let me first go into here and we should already have our team object. So team dot display name. Okay, so that's a previous one, which means I need to obviously clear that. So I'm going to say team name equals PowerShell team and we'll just say, get team, display name, let me just make sure this works and dollar team name like so. Okay, so it is called PowerShell team. Okay, perfect just wanted to make sure that was the right value before we start trying to connect to it. Okay, so let's take that last command we ran, which was the get team and what we'll do is populate a variable called team again, and just double check, that's got a value. Okay, sure enough it does, so let's clear them. Okay, so how do we get team channels? Well, once we retrieved the team object, we can then go in and start connecting to team channel. Now you'll notice like lots of things in teams,
>
> **[1:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-channels?u=76281980&t=94)** it's a group ID that you pass. So I'm going to say team, group ID and press enter. Now this will go through and retrieve the original channel that's in there, every default team has a standard channel called general. So it's retrieved my general channel. So what does that look like? Well, let's populate a variable first and we'll call this one channel and then I can say channel and let's do format list. So this gives me an easier view. So what we can see is we've got the idea of it, which is a unique identifier, the display name and the description, which has been passed and then of course the membership type that's been configured. So a standard one means it's effectively a public channel. Now what we can do is obviously try and identify specific channel types if we needed to. So if we go back to our get kind of channel, if I do this, you'll see you've got membership type and there's private shared, standard private shared standard. So when I choose standard, it retrieves that but if I go back and say private, you'll notice it's not going to return any values cause I have no private channels that are in there. Now when we create the channels, obviously we can associate specific individuals as members of those specific ones. So if just double-check we have our channel, our channel is here.
>
> **[3:07](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-channels?u=76281980&t=187)** What we can do is utilize a team channel user command and from here, I can pass in the group ID of the team, so group ID. I can then go through and pass in a display name if I wanted to, or I can say role an owner. Now, that's not just what happened here. The display name simply doesn't really make any sense when you think about it, but actually it's the display name that we need to pass, which is the channel display name, not the display name of the team. So I can say the display name and then of course I've got my channel and I can then say, display name and then do enter. So this is now going to go and connect to it and come back and say, get me the teams channel user that's been associated to this. So let's make this a little bit easier to view, so select, not select, we'll just do format list and what we should not get is, for this specific channel, so the general channel in my team, the admin account has the role of owner and that's the only person that's in there as a specific owner. So we can obviously define and view the permissions that are there. Now, of course, what's our most important task at managing teams, well, once we have the teams in place, we obviously might want to create channels. So I'm going to create a new variable called channel name and in here, I'll call it PowerShell discussions and click enter.
>
> **[4:44](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-channels?u=76281980&t=284)** Now I already have my team, which is the PowerShell team, so I can actually just create new team channel, same thing as always, it's going to need the group ID. So team dot group ID. At this point, it's also going to need a display name, which we just created called channel name and then we pick the membership type and for this one, I'm actually going to choose the private option, which I created as a private channel, so I'm going to click enter here. Okay, so now it's created a private one, which means if we say, get team channel in the group ID, team dot group ID and press enter, what we should now get is both of those channels. So we should get the display name for the general one which was created and we should also get the private one that was created as well. Now, of course we can create as many of those as we need to, but we can also update them, so let's say when I created that, I created it as a private one and actually that's not what I want it to do. So what we can also do here is change things around, we have, so let me just clear some space here. We can also use what's called set team channel. Same thing again, it's going to require that team dot group ID and at this point, if we tab through,
>
> **[6:15](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-channels?u=76281980&t=375)** we've got current display names, description, there's only a couple of things that we can update. So I wanted to update, you know, the membership type, but if we tab through, there's no property for us to update, so be aware that some of the PowerShell commands themselves don't actually have the properties that you would wish to update. So if I say current display name, I'm going to say channel name, and then you can see we have a property called new display name. So let's say I just wanted to call it PowerShell instead of discussions. like so, I can press enter. So that's now going to force an update against the channel, I can then go back to my get team channel and this will retrieve my newly renamed display name of the channel itself. So a very simple process. We can create teams create channels and then of course we can also start to remove channels if we needed to. So I can use remove team channel, like so. Same again, it's going to be the group ID that's required, dollar team, group ID and then I can do the display name, which in this instance is now called PowerShell and press enter. And now it says the channel is not available. It's not there for me to delete, it says there's no channel that has that specific name. So what does that mean? Well, remember there's a timing issue as well
>
> **[7:49](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-channels?u=76281980&t=469)** when it comes to moving, deleting, renaming, et cetera. Just because we rename it in the user interface doesn't mean it renamed everything under the covers. So in my team group, I have a channel and the channel is called PowerShell. If I do enter, it still comes back and says, channel is not found. So just be aware, we have to give it some time and then we're able to execute that delete. And there we have it, it's now completed. You'll see that it removed it, it didn't give me the error this time and so remember that's just a timing thing, especially as we're trying to create them, update them and delete them and remove them all at the same time, you sometimes just have to wait. So in normal administration, you wouldn't be doing everything all in one go. So that's as simple as it is to obviously view specific teams or also go through add view channels, add channels, update channels, and also delete channels.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (9), [[Microsoft Teams]] (1)
> **Tools:** powershell (9)
> **CLI Commands:** make (4)
> **Definitions:** is called (2), is a  (1)
> **Cross-References:** go back to (2)
> **Warnings:** be aware (2)
> **Analogies:** for example (1)
> **Prerequisites:** before we start (1)

#### [How to provision policies in Teams](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-provision-policies-in-teams?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-provision-policies-in-teams?u=76281980&t=0)** - Now, one of the most important tasks, when it comes to [[Microsoft Teams]], outside of creating them and provisioning them, is also the ability to manage things like policies, to be able to identify, you know, specific configuration changes that need to be made to control what individual users can do. So, first off, let's have a look at how we can get a specific policy. Now, you'll notice that we're using a different type of command. It's not just, Get-Team. They have what's called Cs in front of it. These are [[Powershell]] commands that come directly from the Skype for Business type PowerShell commands. So, it's going to be Get-CsTeams, and then meeting policies, messaging policies, et cetera. So, if I just do Get-CsTeams Meeting Policy, what you'll see is it lists a whole bunch of information, directly to the screen. And this is all of the properties for each of the policies that have been created. So, if we scroll a bit further here, you've got one called Kiosk. We scrolled a bit further, should be another one here that's called, Default. And another one up here, which will be Restricted Anonymous Recording, and then another one. So, we have these types of policies. They're not just Meeting Policies. So, if I do Messaging Policy, this will go back and give me one that's related to Messaging, which is, you know, can we use images? Can we use GIPHYs, and all that kind of stuff? We also might have policies that relate to Calling.
>
> **[1:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-provision-policies-in-teams?u=76281980&t=94)** So, if you're allowing Calling, within teams, so get the Calling Policies. This will go back and say, you know, "Can we forward to voicemail?" et cetera, things like that. So, we have a series of policies that all available. One of those policies, if we go back here, and just type Channel Policy is a small one that basically says, who can create channels. And we have some kind of Default, and Global type capabilities. So you'll see, Allow Sharing, Allow Shared Channel, Allow Private Creation, et cetera. Now, of course, when we're creating these types of policies, obviously out of the box, there are Global Defaults, that you can modify. But often as an organization, you need to create your own types of policies. So, how does that process work? Well, the good news is, just like we use the Get capabilities, we can also utilize the Messaging kind of policies and Phone Policies, and everything else. And they all have what's called New. So, for example, New-CsTeams Messaging Policy, and then I'm going to press Enter, and it's going to complain, because it needs a whole bunch of different things. So, I'm not going to fill those in yet, but we have a PowerShell commandment for that. So, first off let's create a messaging policy. So, we'll first create a variable called, messaging policy name, and we'll call it, PowerShell Messaging Policy. We're going to go all out with the names. Okay. And then what I'm going to do is, I'm actually going to copy and paste this,
>
> **[3:07](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-provision-policies-in-teams?u=76281980&t=187)** from the example file into the window, just because there's too much information to kind of, go for. Okay. So, what we can see here, is we using New-CsTeams Messaging Policy. We first give it the Identity, which is the name that we wish to give the policy. And then we have various properties. So for example, in Messaging, which is where we would communicate and chat, do we wish to allow URL previews? Do we wish to own it, to delete the messages? Can the user chat? Can they use GIPHYs? Can they use stickers and memes, et cetera, and some capabilities. So we can create policies, that will control what end users can actually do. So, I just pressed Enter, and this will create my policy. So, I now have this policy defined and created, which means I can then start to assign those to individual users. That's the purpose of the policy. Now, once you've created one, what about if we realize, for example, that actually I wanted to allow GIPHYs, and I wanted to allow memes. So, actually I got that kind of wrong. So, what I want to do here is I want to actually go and get, and kind of update, so I can use SET commands. So, let's do Messaging, cause that's what we created before. Messaging Policy. It's going to to ask for an Identity, and we can use that same variable that we just created. So, the policy name, and I'll just put that down to the next line, and I can then say, Allow, and you'll see, we can loop through everything.
>
> **[4:40](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-provision-policies-in-teams?u=76281980&t=280)** So, I'm going to say, actually, I want to update that, to be True. And then I also want to allow memes, and I'll set that to True, as well. And then my last one, I'm going to allow stickers, and we'll say True, and then I'll press Enter. Okay, so what did that do? Well, that's supposed to have updated. So, I've got my Messaging Policy. So we can say, Get-CsTeams Messaging Policy, and I can do Identity and say messaging policy name. And then let's go back up to here, you can see that my Allow GIPHY is no set to True, my Allow Memes is set to True, and my Allow Stickers are now set to True. So, not only can we create them, but we can update them as we needed to, as well. Now, of course, outside of that. What about if we create them, and then maybe we don't want to keep that policy? Well luckily, we have a Remove-CsTeams Messaging Policy command, which just takes the Identity where I can say, messaging policy name, and just press Enter. Now, that will go ahead and remove. So, when I now run, Get-CsTeams Messaging Policy, like so, they should just return us back to my Edustudent, my Faculty, my Default, and my Global, and my custom one that I created has now been removed. So, as you can see, it's fairly straightforward,
>
> **[6:12](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-provision-policies-in-teams?u=76281980&t=372)** using the standard commands. Be aware that they are not the regular Teams commands. And if you're trying to look up the documentation online, you need to look into the Skype for Business documentation, to identify these types of commands.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (4), [[Microsoft Teams]] (1)
> **Tools:** powershell (4)
> **Analogies:** for example (3), just like (1)
> **Env Vars:** url (1), set (1), giphy (1)
> **Documentation:** the documentation (1)
> **Definitions:** is a  (1)
> **Warnings:** be aware (1)
> **Speakers:** - now (1)

#### [How to create templates in Teams](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-templates-in-teams?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-templates-in-teams?u=76281980&t=0)** - [Instructor] And one of the features available within [[Microsoft Teams]] is the ability to create Teams Templates, which means that when you create a Microsoft Teams, you can select from an existing template. And luckily, there's a command which is called Get-CsTeamTemplateList, which will display the current templates that are there. You'll notice they have a funny kind of reference. It's called an OdataID and they referenced various things like managing a project, help desk, healthcare, et cetera. Now, the good news is that you can actually go ahead and create your own templates as well if needed. But let's first look at what's inside an existing template. So let's take this one, which is healthcare hospital and we'll just clear this here, we'll need OData object, so I'm going to create a new variable called ODataID, which is going to contain that path that we just saw and then at this point, I'm going to say, get CS Team Template, and then I can pass an ID to that. It's actually going to be the ODataID like so, and press enter. Now you notice what it looks like, is a kind of a [[JSON]] structure, and that would be correct. Everything that's contained within the template, is stored in a JSON template. So if we just keep scrolling here, you'll see details about the name and description, which channels are being created, whether it's private or public. We can scroll further down, there'll be list of applications that are available. Things like do we allow giphy's and images in the chats,
>
> **[1:35](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-templates-in-teams?u=76281980&t=95)** things like that, list of applications and then some various information towards the button. So we've got this idea of creating templates based on specific values. Now, to create them, it's a bit more complicated, so we have two different trails of thought here. The first one is to, well, if we could take the existing JSON, and then we could use that as the basis, we could perhaps modify the JSON and then do it that way. That's one way but I actually like to construct things using just PoweShell from scratch. So I'm going to paste some variables here from the exercise file. We'll just click paste and you'll see that I've already created a series of variables for the name, the description, any applications, I'm using planner and then some channels. So we have a [[Powershell]] channel and an Errors channel, and then there's a Locale also. So fairly straightforward, now the complicated piece comes when we try to construct the template itself. So I'm going to paste this again in from the exercise file, it's easier to see this way. So what we're able to do is create a new object, which is called a Team's Template. You can see this object sitting right here. Then, we can take all of the variables that we've created and put them in the various places that are required. So display name, would be template name, short description, description ID, et cetera. So we can see all of these values, I'm going to press enter here. Now, what does that create in our template? Well, if I press enter,
>
> **[3:08](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-templates-in-teams?u=76281980&t=188)** you'll notice it crates me the JSON type object that we saw when we reviewed an existing one that was there. Once we have that, so let's just clear this, we can say new CS Team Template. I'm going to specify my locale, which is going to be the variable I created called locale and then my body of this template is going to be that variable that I just populated, so I'm going to press enter here. Now sure enough it went through and it created an object. It created my new PowerShell Template, the most important thing is this piece here. It created a specific unique identifier for my template. So what does that look like? Well, let's just do get Team Template list, press enter and you should see at the top there, is that one that I created. So let's grab those details and then we'll say, get CS Team Template and we'll pass in an ODataID and this time we'll paste that new one that was created and press enter and sure enough, you can see all of the values that have been associated. You'll see PowerShell Template, the channel's called PowerShell and PowerShell, et cetera. So now we have that template created, so of course we can then either update the template or we can remove items in the template too. It's all about utilizing the Odata path to kind of choose what to update. Now we could download the specific files here,
>
> **[4:42](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-templates-in-teams?u=76281980&t=282)** the JSON again, and obviously push it back up again if we needed to, to say, we'll take this update and apply the updates. The other option is that we can go through and modify, so if I got back to my command from earlier, which is this one, I can modify this option here to be adjusted so that it would then reflect the new values. So we've got our template that already existed. If I wanted to, for example, you can see I'm just browsing through my structure. I could go through here and say true instead and I could go all the way up here to my, is favorite by default and change that one to false. So I've just swapped those values around, so press enter. So I have my new template object, and then this time around, what we're going to do is, I'm just going to go back and get my C Template list here. And I need to copy my OData object because I'm going to need that for the update. I'm then going to go in and say, update CS Team Template. The first property that's required is Odata, so I can paste that in and then we simply need to pass in the body option again, which will be my template, and I can then click update. So now that's forced an update into my template, so let's review back through my commands to where we did the get and sure enough when we go into the get now,
>
> **[6:15](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-templates-in-teams?u=76281980&t=375)** we can scroll through and look for our updates and you'll see there that it's favorite is now true here and this one is now false. So we can update at any point in those templates. Now, what about if we realize that we don't actually want that specific template anymore. Then that's as simple as making sure, so I'm going to say, ODataID. I'm going to paste that one that we've got, and then we'd have a command called CS Team Template and it's just going to take my OData object, OData ID like so, and I can remove. So now when I do my Team Template list, you'll notice that that Team's Template is now gone. So the great news is that we can define them, download the JSON, modify if we need to and upload them using the existing JSON, or we can just construct it our self from a series of variables. But once we define the templates, they become available to anybody who has access to create new teams.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (8), [[Powershell]] (5), [[Microsoft Teams]] (2)
> **Exercise Files:** template (29), exercise file (2), download the (2)
> **Env Vars:** json (8)
> **Tools:** powershell (5)
> **Definitions:** is called (2), means that (1), is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Modifying core configuration](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/modifying-core-configuration?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/modifying-core-configuration?u=76281980&t=0)** - [Instructor] Now, one of the last things to look at here, is just general core configuration. So first off, you'll notice that we actually have, what's called the "Get-CsOnlineCapabilities," and I can say, "Get-CsOnlineUser," which is going to connect to [[Microsoft Teams]], and try and identify, those users. And of course, it's just so much information that comes down. So what we have to do here, is Select-Object, and then of course, we may want to kind of, identify specific properties, that we wish to bring down. 'Cause you can see this so much information. When we actually go through and say, get me the Cs-User, It gives us everything that would be available for teams and also for Skype for business. So I'm going to say, "UserPrincipalName," and just do this. And this will give me a list of all the user principal names, that are, online users that have access to, Skype for business and teams type capabilities. Now, what we can do, is obviously we can retrieve specific user accounts if we need to. So I'm going to pick my AllanD account here, and I'm then going to say, "Get-CsOnlineUser," and I'm going to pass in my identity, for that specific user, and just press enter. So now that retrieved all of the properties that are available, for, specific users that have teams access. Now, if we just go back to here, and say, "Get-TeamUser," okay,
>
> **[1:32](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/modifying-core-configuration?u=76281980&t=92)** it's going to ask for a GroupId. It doesn't ask me for a user. So it's one thing to be aware of, that, when we're looking at the CsOnlineUser, that's a user that has access to teams. Whereas a TeamUser, is somebody that's associated to a specific group. Now we can also retrieve things, such as well, a standard kind of query that you would do, as a end user would be, well, actually, I'm trying to find out, if my users, are associated to meeting policies. So let's go into, "TeamMeetingPolicy." So let's have a look at the list of the names that are here. So we have meeting policies. So let me just get identity, just because, I want to know, "Select Identity." Okay, so we have global, all restricted et cetera. So let's have a look for the global one. So what I want to do, is run a query, that says, "go and get me all of the users, but filtering out, to those that have been associated to the global policy." So, the first thing I'm going to do is, specify a variable called meeting policy, and I'll call it "Global." What I'm then going to do, is create, what's, a filter, and filter in [[Powershell]] are a little bit different. So I'm going to filter what's called, "TeamsMeetingPolicy." This is a property, that's associated to a specific account. And then of course, I'm going to say "equal to something." Now I'm not going to hard code the value, because I want to use a variable.
>
> **[3:05](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/modifying-core-configuration?u=76281980&t=185)** So I'm going to do something like this, where I'm able to, then say, "-f," and do "$meetingpolicy." So what does that filter look like? Well, let's have a look here, You can see it basically fills in the value, and it's dynamic. So, I'm looking for, any user, that has a teams meeting policy, equal to "Global." Now what's nice here, let me just clear it and start back at the top. It's I can say, "Get-CsOnlineUser," and then there's a filter option available to us, that just says "$filter." And this will go ahead and start to identify it. Now it comes back and says it can't bind, the filter globally is not user policy. And that's perfect, because it's just a, core, general one, that was created out of the box. So, how do we, filter, by specific policies? Well, let's go and say, "Get-CsTeamsMeetingPolicy," you'll see the names. And then of course we can go through and update them and say, "well, let's not have that." We could just use "Kiosk" for example. Now, what does it mean by, it's not a user one that's there? Well, if we scroll through the various properties, you can see, there's a few properties here, but there's nothing that, designates that it's a user one. a user one, is one that was created, by you, as an admin and is available for assigning. Say, if I go back, and change my filter a little bit, let's say my filter is, "Kiosk," go back to my, filter option, likes so, $,
>
> **[4:39](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/modifying-core-configuration?u=76281980&t=279)** let's just make sure it filters right, which is "Kiosk" now, and then I'm going to say filter. It's now going to come back with no values, because there's no users that are associated to that specific kiosk one. So it's actually quite a common request where you want to know which users have obviously associated to a policy. Now, some of the things that you might want to actually do, if I just clear this, is use, what's called, "Get-CsTenant," which we'll go and retrieve, information about this [[Microsoft 365]] tenant, and anything that was associated. So for example, "AssignedPlan," what might be there, et cetera. So some of the standard, ways of doing this, let me just kind of clear this out here. I'm going to pay some values in. You can see I'm getting my tenant value. And, as I go through and get display name, it's my Contoso one. You can see the domain that's supported. You can see that we're in the effective upgrade mode of "TeamsOnly." And, I've got the "ProvisionedAsTeams", teams flag set. Now, of course, if we wanted to, kind of move, from a previous version. So let's say that we were still, running, in Skype for business mode. Then of course we can come through and force an upgrade to, our entire tenant. And that's done by utilizing some of the grant commands, that, are available to us. Now, outside of that, there is a set of, commands, that will manage, things like the tenant federation configuration. So for example, what are the allowed domains?
>
> **[6:13](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/modifying-core-configuration?u=76281980&t=373)** What are the blocked domains? Are we allowing public users, teen consumers, et cetera. So we have some high-level tenant, configuration options, that are available to us, that we can adjust if needed as well. So some of these values are there. So, if we just do "Set-CsTenantFederationConfiguration," you'll see that I can kind of loop through, the various options. So if I wanted to, go through and say, "AllowTeamsConsumer," I could say "$false," click enter, then go back and say, get the configuration, And you'll see it now says it's false. I can go back and say, "true," and that will update that value back again. So you do have the flexibility of adjusting some values if needed. Now, we can also get values, and configuration, about the teams, client configuration, too. So I can say "TeamsClientConfiguration." This has a set of properties also, So, everything within, teams, has various objects and various properties. So for example, in the teams client, you'll see when we go to files, that there's all these various kind of integration pieces, that we can set, and change as needed. So for example, I could say, "Set-CsTeamsClientConfiguration," and I could say, "Allow..." Let me, change this to be a bit more restrictive,
>
> **[7:47](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/modifying-core-configuration?u=76281980&t=467)** I could say, "-AllowGuestUser $true," I could then say, "-AllowGoogleDrive" and I could say that would be "$true," And then I can press enter here. And that will then update, the specific configuration. So if I go back to here, and get configuration, you'll see [[Google Drive]] is now true. And we should have a guest options is now set for true, which, if I change that the other way around, and don't false, and then we trade those values, you should then see that Google Drive is now false. So this will affect the client itself.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Google Drive]] (2), [[Microsoft Teams]] (1), [[Powershell]] (1), [[Microsoft 365]] (1)
> **Analogies:** for example (5), such as (1)
> **Cross-References:** go back to (3)
> **CLI Commands:** find (1), make (1)
> **Definitions:** is a  (2)
> **Tools:** powershell (1)
> **UI Navigation:** go to (1)
> **Warnings:** be aware (1)


### 5. Managing Security and Compliance

[↑ Back to Table of Contents](#table-of-contents)

#### [Explain the security and compliance features](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/explain-the-security-and-compliance-features?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/explain-the-security-and-compliance-features?u=76281980&t=0)** - [Instructor] In order for us to understand security and compliance, we first need to understand the security categories that exist within [[Microsoft 365]]. Microsoft 365 security solutions will help you as an organization work securely from anywhere, with all the standard tools that you're used to. There are four security pillars that are identity and access management, threat protection, information protection, and [[Security Management]]. Identity and access management is about protecting user identities, and controlling access to resources based on a specific risk level. Threat protection is about protecting against advanced threats, and then being able to recover from those attacks. Information protection is ensuring documents and files and emails are accessed and viewed only by authorized users. And then of course, security management is really around gaining visibility, and control over all of the security within Microsoft 365. Now within Microsoft 365, there are core security and compliance features. Some of these are auditing, identity protection and governance, data loss prevention, insider risk management, [[Microsoft Defender|Defender]] for Identity, records management, privileged access management. The list goes on for the types of security and compliance features. However, not all of these features have a rich [[Powershell]] command interface. So only some of these are able to be configured utilizing PowerShell, or at least utilizing modules that already exist, or the [[Microsoft]] Graph.
>
> **[1:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/explain-the-security-and-compliance-features?u=76281980&t=94)** Now, one of the first areas is really focused on the security of the content, and some protections, and there were four kind of buckets to this. The first one is data loss prevention. This is about detecting sensitive content as it's used, and shared within the organization, in the cloud, on devices, and then of course tries to protect from accidental deletion or data loss. Information governance is about managing the content life cycle for importing, storing, and classifying business critical data. Information protection allows you to discover, classify, and protect sensitive and business critical content through its life cycle. And then records management uses intelligent classification to automate, and then apply retention schedules for regulatory, legal, and business critical records within the organization. We can also utilize what's referred to as communication compliance and insider risk management. Communication compliance minimizes communication risks by helping you as the IT admin and organization to automatically capture inappropriate messages, investigate policy violations, and then take steps to minimize any of the harm between employees, and sharing of content. Insider risk management is about detecting risky activities across the organization. So you can quickly identify, investigate, and act on those insider risks and threats. And then our last kind of bucket of components is audit.
>
> **[3:10](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/explain-the-security-and-compliance-features?u=76281980&t=190)** So records are retained within a telemetry platform within Microsoft 365. So every user and admin activity is recorded within the organization. And you as an admin have the ability to search the audit log, and investigate a comprehensive list of activities across all the locations and services. We could also perform data subject requests, which are part of the GDPR capabilities, but more importantly, we can manage eDiscovery core and advanced eDiscovery. eDiscovery core searches across content to identify, preserve, and export data in response to legal discovery requests in eDiscovery cases, whereas advanced eDiscovery builds on top of that, by providing intelligent analytics, and machine learning to help you analyze that data even further. So the three most common categories where you as an organization will utilize PowerShell for security compliance is data loss prevention, sensitivity labels and policies, and then lastly for searching the audit log. These are the three most common areas where PowerShell is utilized. PowerShell is utilized for creation of the data loss prevention policies, as well as viewing any of the false positives or actual alerts. Sensitivity labels and policies can be created, applied, changed, updated, both on premises and in the cloud utilizing PowerShell. And then auditing, just because the telemetry platform is so big that it's often easier to just stream the data down in PowerShell,
>
> **[4:44](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/explain-the-security-and-compliance-features?u=76281980&t=284)** and search it offline, or synchronize it into another application if needed. So these are the three core areas where you can utilize PowerShell. Now, the other areas do have PowerShell commandlets, but these are the most common.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (9), [[Microsoft 365]] (5), [[Security Management]] (2), [[Microsoft Defender|Defender]] (1), [[Microsoft]] (1)
> **Tools:** powershell (9)
> **Code Identifiers:** ediscovery (5)
> **Env Vars:** gdpr (1)
> **Speakers:** - [instructor] (1)

#### [Security and compliance demo](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/security-and-compliance-demo?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/security-and-compliance-demo?u=76281980&t=0)** - [Instructor] Now when managing security compliance within [[Microsoft 365]] there is a couple of different ways of connecting purely based on supportability. So the most up-to-date version is to utilize the [[Microsoft]] Exchange Online [[Powershell]] module, which we already imported. However, one of the first options is to utilize the standard PowerShell session object, which I'm going to populate here a specific user. So we use my admin user and I'm also then going to create my creds object. I know I probably already have one, but that's okay. We can do get credential, username and we'll say, user press enter. It's going to ask me to put a message in, because obviously I forgot to type it so I can say add password. And then my box appears and I'm going to select this value for the password and paste in. You can see my add password option here, click okay. Okay, clear that, and then creds. So I've got my secure string option. Now I'm just going to paste what is required for the connection here. And what you'll see is the session approach utilizes a standard PowerShell commandment code, New-PSSession, which is used for remoting to other machines. In this instance, the configuration name is Microsoft Exchange with a URI that points to ps.compliance.protection.[outlook.com](https://outlook.com). And then we pass the creds and we get a connection.
>
> **[1:35](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/security-and-compliance-demo?u=76281980&t=95)** So I'm going to press enter here. This is going to go ahead, it changes the connection URI to be whatever the specific server is and then if I just choose session here, it creates me a remote session to the server. Now what we actually have to do here is import that specific session that we have the remote session. So I'm going to say import PSSession session, and I'll just use a disabled name check-in just because it's going to be faster and click import. Now, what you can see is it's loading all of the commands directly into the same window, and you'll see the module is now loaded, but it's not called Exchange Online, it's called that unique name. Say, if I say, "Get module" and load it, you can say that I have these kind of temp Vons that have been loaded here, which has come from that command. So once we have that, then I can start to connect to Exchange Online or the security and compliance components, because it's the same capabilities. The difference being that the way we connect is by utilizing a different command. So we use what's called connect IPPSSession. So this is going to be the same, whether we use the Exchange Online module, or we use the session-based MODULE, which we just looked at. So I'm actually just going to press clear on here. And I'm going to say, get the PSSession first, PSSession. And you'll see, I've got a PSSession here.
>
> **[3:10](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/security-and-compliance-demo?u=76281980&t=190)** So I've got two, five and six. So I'm going to say remove PSSession. And if I look for the ID here, so I'm going to say two, five, and six. And then if I say PSSession, this time they'll be no sessions. So now what we can do instead is say connect IPPSSession, cause it's already loaded. Cause I have it from the Exchange Online option and I can then just press connect here. Now what this will do is this will launch me to the browser. I can then select my account, wait for it to connect now to it gives you a message very similar by saying it's redirecting and doing whatever, and now I'm connected, but it doesn't return any values whatsoever. So we don't actually know if we are connected. While we do we can execute obviously a specific command if we needed to, to see if we are connected. So for example, if I wanted to get specific information, so let's say, "Get activity alert" for example, now it didn't return anything, but it did execute as a command. So we know that we have a connection at least to the tenant. So we can connect either using the session approach or we can connect using this approach. Or our last one would be that we use the Microsoft Graph. We pass all of our scopes in
>
> **[4:45](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/security-and-compliance-demo?u=76281980&t=285)** and our scopes are a bit more complicated this time. So I'm actually just going to copy and paste the list of scopes that we wish to utilize. So let me just go here and paste and you'll see that we've got security actions, security events, policies, and e-discovery. So the difference is when we connect with the Microsoft Graph, we obviously have to specifically choose the permissions from the Microsoft Graph to connect. I'm not going to connect this way, but that's how we would do it. So now that we have a connection, we're ready to execute any of the commands that we need to modify security and compliance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft]] (5), [[Powershell]] (3), [[Microsoft 365]] (1), [[Microsoft Outlook|Outlook]] (1)
> **Env Vars:** uri (2), module (1)
> **Tools:** powershell (3)
> **Analogies:** for example (2)
> **URLs:** [outlook.com](https://outlook.com) (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [How to manage Safe Links, Documents, and Attachments](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-safe-links-documents-and-attachments?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-safe-links-documents-and-attachments?u=76281980&t=0)** - [Instructor] Now, one of the first areas of focus is really around the ATP type components. Now, what we've got here is we can use Get-ATP, and we have these ATP type. So it could be policy, it could be something else. Now, first off, I just need to make a connection to the environment. So what we'll do for this instance is for the purposes of this, I'm just going to use a session-based control. So I'm going to paste all this into here, like so. This will connect me. We'll wait for the inputs into the existing session. So it has all the properties that I need and the commands. Notice it comes back and complains because I already kind of had it in there once. You can use what's called allow clobber, which means that my command would change to something like this. And when I do a low clobber, it basically comes in and will connect. And there we go, you can see it's connected now. What we can then do is say, get module, just to make sure that we have them connected. And sure enough, I've got connections into here. Now, what I also have to do is I have to say whether it's loaded all of my commands that I'm looking for. So my first one here is Get-AtpPolicy, but notice it doesn't resolve. I'm not able to access the Get-AtpPolicy components because I need to first as well, create IPPSS session to that one. And I'll use my creds object and just press enter. So I now have a session connection
>
> **[1:33](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-safe-links-documents-and-attachments?u=76281980&t=93)** and I have a connection using the exchange one. Now, what does this look like? Well, let's have a look at the get module. Now notice what it's doing. It's just creating the same thing. So this is an interesting point to make, it doesn't matter how we connect, what you end up with is the same kind of modules connected. Doesn't matter whether it's the exchange online module, whether it's a connect IPPSS session module, or whether it's a new session. What we also need in order to be able to successfully manage is an exchange online one too, because some of the security and compliance options reside in exchange online. So we'll make a connection to exchange online, which means we'll have a security and compliance connection and an exchange online connection also. Now, if we say, get module and go back here, now you can see that it's loaded something separately here, and you'll see that I've got things that have ATP in the name. So let me see if I can do that and sure enough. It now starts to resolve my ATP type policies as well. So let's clear this and let's start there. So to look at the ATP policy, we're going to say, Get-AtpPolicy for [[Microsoft Office|office]] 365. And if I scroll up here, you can see that I have a specific policy. Lots of the settings are configured for false, which means if I wanted to retrieve some specific values. So let me go over here because often the standard view isn't
>
> **[3:10](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-safe-links-documents-and-attachments?u=76281980&t=190)** great. So let's just paste from the exercise file here. You can see that my enable safe links, my web access, they're all set to false with odd ones being set for true. So what we need to look at is, well, how do we kind of adjust those? Can we adjust those? Can we update them as needed as well? So the AtpPolicy options say, if I say policy set-AtpPolicy, then you'll see that I've got all of those properties that are available to us. So allow, click through enable safe links for office 365 clients. So let's say I wanted to put that as true. I could do this, set that value for that policy. Okay. If I then go back to looping and getting the original policy like so dollar policy, if we go back to the values that we just looked at, which was for clients, you can say that it's now true, so we can update these policies at any point. So that's kind of a general advanced threat protection policy. That is that. There is also different options available to us, such as SafeAttachmentPolicy, which in my talent is not configured whatsoever. I can say, Get-SafeLinksPolicy and let me go here as well. And you'll notice that there's no safe links policy now in your talent, you may have them, but in the default tenant, they don't exist. So how do we actually go through and create them? Well,
>
> **[4:44](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-safe-links-documents-and-attachments?u=76281980&t=284)** first thing is let's create a name for a safeattachmentpolicy, so safeattachmentpolicyname, and we'll call it [[Powershell]], Safe Attachments. So we have a policy name. I then need to create what's called a policyrule instead. So that cause a bit of policy. So have safeattachment policy attachments rule. Okay. So we have a rule name. I then need the email, which is going to be my user account. Cause I already have that one. And then I'm going to set my send to variable and I'll just use that as the user. So it's going to send it to me either way. What we can then do is say policy equals New-SafeAttachmentPolicy. And then we go to the next line here. So I have space name, safeattachmentpolicyname. Then I can go through and say, well, firstly, I'd like to enable that. I also wish to enable redirect, which means that if there's redirects for the attachments and inside the policy, it will update that. And then I'm going to say redirect address, and I'm going to send it to my email. So I'm going to click okay here. So that's going to create me the new safe attachment policy. So if I go up to my first command that we executed and say, get the policy, sure enough It's there with some basic parameters that have been set. Now, when we create a policy,
>
> **[6:19](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-safe-links-documents-and-attachments?u=76281980&t=379)** we need to go through and create a rule for that also say, if I say New-SafeAttachmentRule, and then go to the next line, my name for this rule will be dollarsafeattachmentrulename. I can then go through and say safe attachment policy, which is the policy that I just created. And then who I want to send that to send to member of send to, oops, let me go back here. I need to do policy.Name. There we go. Okay. So now we have a rule that's been added to our policy. The same exact process works for either safe links policies, where we're able to go through and set those values as well as any of the safe attachments policies. Now, of course, let's say that we liked our policy. So let me go back to my get attachment policy here, but let's say that we wanted to, you know, perhaps update that a little bit to change what I want it to do So what I can then do is say Set-SafeAttachmentPolicy. I've got my identity, which is going to be my safeattachmentpolicyname. What I'm then going to do is go to the next line here I have got my redirect, which I'd specify as true. So we'll keep that one there and then I'm going to change my action and I want to make it so it's dynamic delivery.
>
> **[7:59](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-safe-links-documents-and-attachments?u=76281980&t=479)** So what that means is where my attachments get put into my email, that they will get scanned and then removed and then reattached later on. So we can go back to here and then I'm going to say, redirect address Dollar. And then I can just say, use it. And then we'll update that attachment. So the attachment policy gets updated to now reflect dynamic delivery instead. So what we can then do is I can say, well, go and get me that policy. And I'm going to say format list, which will give me all of the properties. And then you can see the properties where we've tried to update. So action, you can see is dynamic delivery. So those values have been updated now by setting and updating using those update commands. Now of course, our last option here for any of the policy types that we can kind of look at here, whether it's document links or safe links. So safe links are just the same. If we go through the process of creating a safe links policy, then we're going to utilize New-safeLinksPolicy. And then we pass the same and name. Then we pass a, put, some values in here is enabled, is the one that I'm after is enabled and obviously dollar true. Once that's been created, then we'll say New-SafeLinksPolicy and actually get rid of policy. We're going to say rule. And then we do the same process that we did for the attachments.
>
> **[9:31](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-manage-safe-links-documents-and-attachments?u=76281980&t=571)** So we have document protections that exist at the top level for ATP. Then of course we have attachment ones and then we have actual link ones that we can create as well and create and manage utilizing PowerShell.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Office|Office]] (2), [[Powershell]] (2)
> **Env Vars:** atp (7), ippss (2)
> **CLI Commands:** make (5)
> **Cross-References:** go back to (4)
> **UI Navigation:** go to (3), scroll up (1)
> **Definitions:** means that (2), is an  (2)
> **Tools:** powershell (2)
> **Code Identifiers:** safelinkspolicy (1)

#### [How to create alerts and activity alerts](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-alerts-and-activity-alerts?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-alerts-and-activity-alerts?u=76281980&t=0)** - [Instructor] As part of managing the security and compliance within [[Microsoft 365]], we need to be able to review alerts such as activity alerts or protection alerts. So, being able to identify the threats. Now, in order for us to do this there were two commandlets that we can actually have a look at. And the first one is ActivityAlert. And this, if I just press Enter, I don't have anything in my tenant because I have no rules that have been defined. So an ActivityAlert, which will create shortly is some activity took place. So for example, let's say I had tried to share a file or, invite somebody into the tenant. That would be a type of ActivityAlert. The other option is what's called a ProtectionAlert. Now I do have some of these listed, so we'll leave that go in. But there was some ProtectionAlerts that come in and you can see they are malicious types of URLs and things like that. These are threat ones. These are specifically for security and compliance. So an example of this would be creating an alert policy that triggers when someone in the organization deletes something, let's say a content search or something else, you could then be notified of that one. So those two commands, the get commands, the get activity alert and the get protection alert would allow us to obviously retrieve information. Now, what I can do is using any of those I'm also able to view any of those alert details. So if we just create a variable called alert name,
>
> **[1:37](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-alerts-and-activity-alerts?u=76281980&t=97)** and what I'll then do is just kind of scroll up a little bit here, and you can see that we've got some with the name and et cetera. So I'm going to say a potentially malicious URL click was detected. That's a long name. And I'm just going to paste that into there. And that becomes my alert name. Let me just clear some space here. And I'm going to say, Get-ProtectionAlert, and then the identity is going to be the alert name. And I can press Enter. Now, of course it retrieves all of the information about that specific ProtectionAlert. So you can see it's got some details here. Who is it going to notify? So the tenant admins, what type of threat category it sits in, as well as, the details that you want to return to the end-user or the admin. So the comment here is we have detected blah, blah, blah. So you get that information kind of listed here in the actual alert itself. Now, it's great that we can kind of view those alerts. We can also see those in the tenant directly. Now what about if we wanted to create a specific alert a kind of for activity. So be notified of something. So let's just create some variables here. So I'm going to say alertname, and we'll call it external sharing alert. So that's going to be the name that we wish to choose. I'm then going to say the operation that I would wish to utilize. I'm actually just going to copy and paste to my operation here. Just a piece of text. So this is going to be looking for sharing invitations that were created. I'm also then going to specify the user
>
> **[3:11](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-alerts-and-activity-alerts?u=76281980&t=191)** that I wish to notify at this point. So who's my user account that I wish to send the email to, for example. So let's just stick with that admin account for now. So I'll just paste my admin account in. Then of course I can start to filter if I want it to the specific users. So I could say this specific user, or just leave it blank here. And then I can also put a description. It's always helpful when creating any type of policies within Microsoft 365, that you kind of have a description. So (murmurs) this can view and understand the logic for you creating that type of policy. So I've got my various details. I'm not going to filter it to a specific individual. And then what we can do is say New-ActivityAlert. I'm going to go to the next line here to give me some space, and then we're going to start tying everything together. So name, it's going to be alert name. Then I'm going to say, operation will be the operation itself. I can say notify user. You can see that as you name the variables, the right name, it kind of makes sense as you go all the way down. So I'm going to say description and $description. So I have my information created and I can now press Enter and sure enough it goes ahead and creates me an activity alert. You can see the name, External Sharing Alert. You can see some configuration of it was created by me. It's full auditing. You can see it's an activity and it's this specific activity that we're targeting.
>
> **[4:45](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-alerts-and-activity-alerts?u=76281980&t=285)** Now, what does that look like? So if I say, Get-ActivityAlert, notice it now returns that activity alert. Because the interesting thing is we're not actually viewing the alerts, we're viewing the rules or the policies that we've created to retrieve that. Now that's an activity alert. Now the creation of a protection alert is a little bit different. So protection alert is obviously one that sits squarely inside security and compliance. So if I go back and create me a new alert name, and this time we'll call it a Search Deleted. So we're going to call it that because you want to capture, when somebody tries to delete something, as far as a content search would go. Then of course, I need to have an operation of some description. And the operation is called SearchRemoved. So fairly straightforward there. Then of course, I'm going to have my notifyuser. And actually what I can just do is just populate it back with the same value, because I already have that address configured. Now a new type, which is available inside the actual protection alerts is the threat type. And this is an activity which is the threat type that we're trying to capture. And then lastly, we've got what's called the aggregation type. Now the aggregation type is an option where it brings everything together. So we have alerts are triggered for every occurrence or they are triggered based on volume
>
> **[6:19](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-alerts-and-activity-alerts?u=76281980&t=379)** or they are triggered when it reaches an unusual level. Now, I'm going to just aggregationtype. And I'm just going to put it as none, which is the default. So, basically I want it to happen every time it identifies the action, then just to go ahead and set in. Otherwise I could pick the other option, like simple aggregation, et cetera. So I've got my options name. Now I'm going to say New-ProtectionAlert, go to the next line. And the same thing as before, we're going to go through the variables here, alert name, I'm then going to go to category. And for my category, I didn't create a variable for this, but that's okay. We can actually just go through and type the values too. So it'll accept text values. So one of the categories is Others. And so we'll put it as that one. We can also go and start populating all of the other options. And that's not the one I wanted. Notify user, $notifyuser. I've got my ThreatType, $threatType. Then my Operation. I've got my description, but I don't necessarily need to have that one. But the most important one is my AggregationType and I can do my aggregationtype, and I can press Enter here. And this will now create me the new protection alerts. So if I scroll up, it's called Search Deleted. You can see the name here. And if I scroll up here,
>
> **[7:50](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-alerts-and-activity-alerts?u=76281980&t=470)** you can say it's an activity and it's auditable. And it's going to send me a message here. And the operation is SearchRemoved. So, creating those alert options allows me to be able to capture and retrieve in the view, all of those alerts that might have taken place. Now of course, when we want to retrieve all of that, these alerts are then viewable inside the actual logging mechanism inside Microsoft 365.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft 365]] (3)
> **UI Navigation:** scroll up (3), go to (3)
> **Definitions:** is an  (2), is a  (1), is called (1)
> **Analogies:** for example (2), such as (1)
> **Code Identifiers:** threattype (1)
> **Env Vars:** url (1)
> **Speakers:** - [instructor] (1)

#### [How to create data loss prevention policies](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-data-loss-prevention-policies?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-data-loss-prevention-policies?u=76281980&t=0)** - [Instructor] One of the main security controls within [[Microsoft Office|Office]] 365 and [[Microsoft 365]] is data loss prevention policies. We are able to retrieve any of the DLP compliance policies by simply running, get DLP compliance policy. Now, of course, all it gives me is some basic information. However, if I pipe this value out and then maybe do select star, which we can do as a value, you'll see it returns all of the items for every single one. Now, though, that's helpful, it's a bit hard to kind of view. So I'm going to take the name here and what I can then do is actually just filter out the values by let's just create a variable here, we'll put the name in, and then I'll just say, get DLP compliance policy, I'll do identity and I'll say, use the DLP name and then what we'll do is just format that as a list like so. And of course it gives me that same information, but it's just for the one policy, not both of them. Now of course, what you can see from here is what the policy does. So this policy is specifically for [[SharePoint]] online and it has a few other bits of configuration, but nothing spectacular and there's no extra queries, no nothing it's just a standard. Now what's interesting though, is it just shows me the policy details. What about the rules that make up the actual policy? So let's say get DLP compliance rule, and then we'll be able to go to policy
>
> **[1:34](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-data-loss-prevention-policies?u=76281980&t=94)** and then of course I can just use my DLP name again, and this will now go ahead and show me that there's a rule in there called Project Olivine DLP policy rule. So now, I want to be able to get information about that specific rule. So if I go back over here, I could say dollar rule equals, that will populate the rule for me and then I can pipe that out as we did before and say format list, and then I'm able to get the details about the specific rule that makes up that specific policy. And you can see I'm still scrolling and there's lots and lots of values here. So if we keep going and going and going, you can see all of these properties are available, which you would normally just update in the user interface are available to us directly within a [[Powershell]]. So we're able to manage the DLP policy and the rules. So obviously that's just retrieving the information that's there. Now we also have the ability to perform specific types of requests. So we could go through and, you know, view a specific data loss prevention report, for example, so once we have policies in place, so let me just come into here, let me just clear this out. What I'm able to do here is say, get DLP detections or well detections report and press enter and of course, there's no detections available to us, but if I had detections, you'll notice that I got actions, source, page and end date,
>
> **[3:07](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-data-loss-prevention-policies?u=76281980&t=187)** the compliance policy, aggregation, et cetera, there's various options available to us. So the detections report can simply just take a start and an end date depending on the information that it's retrieved. Now, I can also obviously view the same information if I wanted to pass the specific compliance policy. So let's say we run that same command, detection report, I can then go through and say, what I would like to do is run that with a specific policy. So let me go here and then of course, I can say my DLP name and go ahead and it would execute. Now, obviously these are just brand new out of the box, so there's no content that's there, but the get DLP detections report will return any of the values that's identified when those items have actually been created. Now, what about if we wanted to not just view them, but we wanted to start creating our own data loss prevention policies? Well, the first thing we have to focus on is the types of things that we can identify. And this is done by first understanding that DLP utilizes sensitive information types. So if I just press enter here, you can see there's a whole host of different sensitive information types. These are provisioned by [[Microsoft]] in the tenant and match different countries, versions of driving licenses, tax numbers, passport numbers, et cetera. And then we have these other ones called Project Obsidian, et cetera, which are labels that we can create.
>
> **[4:40](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-data-loss-prevention-policies?u=76281980&t=280)** So these specific sensitive information types are available to us, so that we can then create a DLP policy that tries to identify content based on that. So how do we create a new DLP policy? Well, the good news is, that there's a simple command within exchange PowerShell, which is what we connected to called new DLP policy. So if I just clear my screen here, say new DLP policy, and we'll go to the next line. What it's going to require at this point is a name and we could create variables if we needed to, but I'm going to call this DLP policy test. We can then go in and choose what the template should look like and the template is the specific name of the sensitive information type. Now, luckily this is one of them, which is PII data, so we can just use that as the template instead. And then of course at that point, I'm able to create a new DLP policy. So this will now go ahead and create the policy. Now, as before, remember a policy comes in two parts, you have a policy and you have a rule. So right now this is an empty shell, it's just created an exchange DLP policy without any rules whatsoever. It will just be looking for the sensitive information type. That's one way of creating them and those are specifically targeted for exchange. Now, if we're looking at pure security and compliance,
>
> **[6:16](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-data-loss-prevention-policies?u=76281980&t=376)** then we're going to use a different command, which is similar to the one that we first looked at, which is going to be new DLP compliance policy. So that one's created, but that's only for exchange. So if I was going to create one that will be for Microsoft 365 in general, then of course, I'm going to say new DLP policy. I'm actually going to populate a variable at this point because I'll need to use that policy name to be able to create the actual rule that goes with that. So once we have our kind of basis created, what we can then do is start to apply various things to it. So for example, in here, it's going to require a name, it's going to require, so let's just go here and say name and we'll call this DLP compliance policy test, put a space back here. Okay, so that's my name. It's also then can have any other kind of sets of information. So if I go back to my policy, you'll say it's created it and it was enabled, but it has no other properties associated to it. So that's helpful, but doesn't really do anything. So how do we update one while we can say, set DLP compliance policy, it's going to say well, it can say as we tab through, there's lots and lots of them here, so I'm going to go back to my identity here and in my identity option, I'm going to specify what that compliance policy
>
> **[7:52](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-data-loss-prevention-policies?u=76281980&t=472)** is going to be. So it can be the name or it can be whatever the policy is, so I'm going to say policy dot, and let's say name, we'll go to the next line. At this point, I can start adding various sets information. So if we just kind of tab, so one drive shared, remove third party, remove teams confirm, remove, remove one drive shared, add third, add exchange location, so add teams location, force priority. You can see we've got this idea of removing and adding various places. So exceptions and updating at an end point at a SharePoint location. And then for the SharePoint location, we might actually want to add a specific URL for a SharePoint site. So for this example, we'll just use one of the SharePoint sites. Let me go to the next line, so I've added a SharePoint location and what I can then do is specify the mode that we wish to execute in. You may wonder what the mode actually be. Well, the mode itself is the mode of the policy, which will either be enabled, disabled, test with notifications or test without notifications. So this is how we implement. So I'm going to to say test without notifications and press enter and that's going to update my DLP policy. So how do we get our DLP policy? Well remember we can say, get DLP compliance policy, if I just press enter now, you can say I've got my policy here.
>
> **[9:25](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-data-loss-prevention-policies?u=76281980&t=565)** So I'm going to go back and just say, get DLP policy, identity, paste the name in, press enter and of course we can say, select star and it will give me all the details again. If we scroll upwards, you'll see that my SharePoint location is configured for that Contoso brand site and the mode that we sent is test without notifications. So that helps me create the actual policy. Now you can add rules to that now. So right now it's just a default one that says, this is a policy with a location and it's going to be test mode, but there's no rules defined in there at all. So let's go to here, new DLP compliance rule, which is what we want to create. Let's go down to the next line. I'm going to give it a name. Now of course, it's just put in here and we'll call it test rule like so, and then the next one, we can go in and say, well, what is the policy it's associated to? So I'm going to say policy dot name and then the next one is what we want to do. So I'm going to say content, let's do contains sensitive information, and this is done by populating a kind of a hash table of values. So I'm going to say name equals and then of course, I just need to know what my sensitive information type is actually going to be. So remember we looked at using a specific sensory information type, such as PII data or something else.
>
> **[11:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-data-loss-prevention-policies?u=76281980&t=660)** So we can obviously go in and start adding what that would be. So, let's say we wanted to use the same one that we utilize before, or maybe something smaller. So let's use credit card number, for example, as one of the options. So now we've got that, we can go back and then what I'd like to do is update a value that would say block access, and then I'm going to say true, and then press enter. So this is now adding my rules, if I scroll up to the rule you can see, DLP compliance policies test rule and as we scroll up, you'll see all of those properties that we had before when we looked at the last rule are all listed out and you can see, this is my policy rule test. You can scroll all the way down, there's various properties updated. So once we've created those, then we're able to utilize those and we can obviously manage them either in the UI or directly here. Now, what about if you needed to actually get rid of a DLP policy. Well fairly straightforward, we can use remove dash DLP compliance policy, passing the identity, and we'll say policy dot name and enter. And it's going to ask me, I'm going to say yes, and it's going to go ahead and remove that policy. So not only can we add the policies which are just an exchange using new DLP policy, we can add compliance policies and we can add rules to both of them and we can also remove them as needed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SharePoint]] (7), [[Microsoft 365]] (2), [[Powershell]] (2), [[Microsoft Office|Office]] (1), [[Microsoft]] (1)
> **Env Vars:** dlp (32), pii (2), url (1)
> **UI Navigation:** go to (5), scroll up (2)
> **Analogies:** for example (3), similar to (1), such as (1)
> **Exercise Files:** template (3)
> **Definitions:** is a  (2), is an  (1)
> **Cross-References:** go back to (2)
> **Tools:** powershell (2)

#### [How to create sensitivity labels](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-sensitivity-labels?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-sensitivity-labels?u=76281980&t=0)** - Along with Data Loss Prevention Policies and Sensitive Information Types, we have what's called Sensitivity Labels. Now, labels are simply retrievable by using what's called Get-Label, which we'll go ahead and look at the system and retrieve labels that are allowed to be associated to content. So for example, files and emails. So when I did a quick search here using Get-Label, I can retrieve the list of all of the labels that are available. And some of these are Out-of-the-Box [[Microsoft]] ones that are there. Now if I wanted to view specific details about a label, then I could simply come in here and say, let's do Highly Confidential. I'm going to go into there. "Highly Confidential" as the name. And then of course I can say, Get-Label. My Identity will be the labelname. And then we can just pipe all of those values out into a list like normal. And sure enough, it goes that funny kind of structure that you expect to see. But as we go through here, you can see this is my label. It talks about why it's so important, and it's a sensitivity type. And what type of data it's looking for. You've got local options, label options. You can see they're all squashed up into these funny kind of blocks. Almost look like [[JSON]]. And this is because when you create the labels, everything is stored in kind of hash table JSON objects that are then saved, and you can retrieve them later. So, it can make it a bit complicated when you're trying to create them utilizing [[Powershell]]. Now, what we can do is when we created a label, we're also able to get label policies
>
> **[1:35](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-sensitivity-labels?u=76281980&t=95)** because remember a label doesn't really do anything unless it's associated to a policy. So if I go into here, you can see I've got policy. So, what I'm going to do here is I'm going to find a quick value that I'm looking for here. So, we've got the type and I'm looking for specific field, identity. We'll do Name. So, let's do Select Name. So, you can see that I have two specific policies. We have the global sensitivity label policy, and then the Project Obsidian label. Now, of course the label is associated to the policy. The policy is what makes the label valid and can be utilized. Now, of course, to create a specific label, it just requires a couple of different things. So, let's first go in and create a label name. So, I'm going to go in and set my label as "Test Label." I then also can put a label description in and I'll call this "Test Label" again. So, we have a description and a label this time, and I can just create a label like that. It doesn't have any other values associated to it. But of course, when we create them, we obviously want to configure lots and lots of different information around text colors, font sizes, et cetera. Whatever you want to do. Do you want to put a watermark on it and things like that. But for now, we'll just create New-Label, like so.
>
> **[3:11](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-sensitivity-labels?u=76281980&t=191)** Go to the next line. I'll say Name. Labelname. Next line down. And it'll be... I'll choose DisplayName. And we'll use the labeldescription, and press Enter. Okay. Now, notice what happens. If there's a property that we haven't passed, it'll prompt it for us. So, that's good. So, I can go here and say -Tooltip, and I'm going to say "Test." And press Enter. And then it populates that value for me. So if you're ever stuck, as you're creating things like in PowerShell, and you're not sure, then you can come into here, run it, and see what properties obviously come back. And obviously at that point, then you would go back and change what's being done. Now, of course, if we create the label, we may want to associate specific things. So, I created a label that doesn't really do anything. It just has a name and a description and kind of a tool tip that pops up. So, what I've got here is the information. So, what I'm going to do is just retrieve this label again, and you'll see I have an identity. And what I'll do is I'll just say labelname. Let's just make sure it retrieved my label. And sure enough, I have a variable populated. Now to update a specific label, we can actually use the Set-Label option. All we have to do is retrieve the identity of the one that we're looking for.
>
> **[4:42](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-sensitivity-labels?u=76281980&t=282)** So I'm going to say label.Id, because that's the one I want to update. And then I'm deciding what I would like to update. And I think I'll use the LabelActions. So what I'm going to do here is I'm actually just going to Copy and Paste from the exercise file here. And I'll paste that into there and click Paste. And then what you'll see is it's using that funny JSON type format or what's really a hash table, and I've got a type which is content marking. It's going to add me a header with some settings. I'm going to be using font size 10. It's going to put the [[Microsoft Word|word]] "Header." It's going to be text, and it's going to be a specific color. So, what I can then do is press Enter. And then that means my label has now been updated. So, let's go back to here and retrieve that label. And this time we can go Select *, and we'll get all of the values. And if we just scroll up here, remember we saw all of those blocks of text. And you'll see that my LabelActions, which is what I just created, is now configured and entered here, exactly the same as if it would have been, if I'd created it in the beginning. So, we can obviously update the values as we go through, and we can change it to say, well, I want to use this type of font or this type of update or whatever it would be. Now, of course, right now, all it has is the colors and some basic key text values, but we can update those at any point. So, maybe I wanted to update the layout of it. So, maybe I want it to be a diagonal format.
>
> **[6:19](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-sensitivity-labels?u=76281980&t=379)** So, what I can do instead is do the same thing. So if we go back to Set-Label, I'm going to just remove all of my label actions that we put in place. And then I'll copy from the excise file again, the label actions. So in my LabelActions, I have an update to make. We'll paste it over here. And what you'll see this time is I've got my font size, my font color. I've got "disabled" as a "Value." And then I've got this. I've got this "layout," which I didn't have before, which was "Diagonal." And so now what that's doing is it's going to say, this is the text I want to put in. "Watermark," and I want it diagonal across it. So, my label is now updated.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (3), [[Powershell]] (2), [[Microsoft]] (1), [[Microsoft Word|Word]] (1)
> **CLI Commands:** make (3), find (1)
> **Env Vars:** json (3)
> **Cross-References:** go back to (2)
> **Tools:** powershell (2)
> **UI Navigation:** go to (1), scroll up (1)
> **Exercise Files:** exercise file (1)
> **Analogies:** for example (1)

#### [How to create sensitivity policies](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-sensitivity-policies?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-sensitivity-policies?u=76281980&t=0)** - [Tutor] Now, once you have the label ready, then of course, you need to create a specific policy. Now, policy makes the label valid so it can be utilized by [[Microsoft Office|office]] applications and other services. Now, this is done by first retrieving the label, which we already have. So let me just double check, I can say label. And then what we can do here is say new label policy. I can then go down to what I want to call it. And so I can say Test Label Policy and then the labels that I wish to associate. So I'm going to say label 'cause you might have more than one label. And at that point, notice what happens. It comes back and says, well, hold on a minute. I don't know what to do with that variable label 'cause that label is just the details of a specific policy. So that's when we have to understand the format of what's required to create the actual label policy. And not every single thing is just a string value. A lot of the time you need to populate the labels. And in this instance, they are multi-value options. So if I just go back to my label option, you can see it has too much information. It's trying to populate everything. So what I'm going to do here is just say label name and press Enter. And sure enough, it now updates. So when you're creating objects and populating them, you might need to actually just use the various properties, not the entire object itself.
>
> **[1:35](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-sensitivity-policies?u=76281980&t=95)** Now, of course, we can update the policies too. So for example, I've got my policy that's been created now. So if we just go here, you can see I've got my Test Label Policy. If I just copy that information here, if I scroll up here, you'll see it's got various sets of information associated. You can see my test label is associated. Now, what about if I wanted to update that policy to add other things into it. So maybe I wanted to make it so it wasn't mandatory in [[Microsoft Outlook|Outlook]] or maybe I wanted to enforce justification if someone tried to downgrade the label on a document. So the same as we did before, we can this time use set-labelpolicy instead of set-label. I can then go to the next line. And I'm going to go in and say identity. And of course, I've already got my policy. So I'm going to retrieve that with my identification. I'm then going to add an exchange location. And to make life easier, I'm just going to say all of exchange. Maybe I'll go in and say, add [[Microsoft OneDrive|OneDrive]] location, and I'll say all, and move down. And then what I can do here is I can populate settings. Now, settings are another chunk of text, which is in that funny [[JSON]] kind of hash table format. I'm going to paste this in here. And so here's some values. It's going to say, this is my default label, this is going to be mandatory.
>
> **[3:08](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-sensitivity-policies?u=76281980&t=188)** And I'm going to require downgrading. I'm actually just going to remove this line. I don't want to put a default label. That's okay. So we can get rid of that. And you can see what we're doing here. We're just constructing the right values so that we can then update that. Now, of course, notice it came back and it said, okay, the policy can't be found, which is this domain. DLP Compliance Policy Test wasn't found. So that means when it's trying to identify, it doesn't know what to do with that specific policy. So once again, let's go back to policy. Notice, you're probably going to have to use policy name. So let's go back up here. And if I go from ID to name and do that, you'll see now it come back and it said, make sure you type the policy name correctly. And it says the policy name wasn't found. So as we're trying to identify things, sometimes the information doesn't appear because we just created it. Sometimes we have to give it a few minutes to come through. Or sometimes it just won't identify the specific label policy because we are not using the correct value. But in this instance, we'll have to wait for that policy to be valid because we just created it. It doesn't mean it's activated right away. So let's try this again. There we go. I told you we had to wait a few minutes and then it updated. So my label policy has now been updated, which means what I can then do a say, policy get label policy. And I'll use the existing variable that I've got,
>
> **[4:41](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-create-sensitivity-policies?u=76281980&t=281)** which is the name. And if I say policy and scroll up here now, what we should now see is that some of those property values, which are listed all through here have now been updated to match what that would be. And some of the information is stored in kind of these funny brackets to make sure. So you can see in the settings, in this section, there's that piece of code that we actually put in. So when we're creating the sensitivity labels, we can obviously create them in the user interface or in [[Powershell]]. We can update them in both. We can then create policies to push them out as well as then update them as we need to. Updating the labels and the policies. And of course, in the labels option, we can have more than one label as needed. So PowerShell is a great way to be able to manage those policies and to update them, but the key is to make sure that this information is configured in the right way. Otherwise, it won't accept the values. It has to be done in a hash table format.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (2), [[Microsoft Office|Office]] (1), [[Microsoft Outlook|Outlook]] (1), [[Microsoft OneDrive|Onedrive]] (1), [[JSON]] (1)
> **CLI Commands:** make (5)
> **UI Navigation:** scroll up (2), go to (1)
> **Env Vars:** json (1), dlp (1)
> **Cross-References:** go back to (2)
> **Tools:** powershell (2)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)

#### [How to review audit logs](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-review-audit-logs?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-review-audit-logs?u=76281980&t=0)** - [Instructor] One of the tasks for managing [[Microsoft 365]] is obviously the ability to go back and view in the audit logs and the audit trails to identify what's taken place, for example, when an administrator made a change or whether an end user did something that they shouldn't have. Now, there are various locations that we can go to for this one. The first one would be the admin audit log search, or maybe the first one would be the mailbox kind of search instead. Now the mailbox search is a powerful tool. It lets me go in and do a mailbox audit log search. When I press enter, it's going to ask me for a series of values, but that's the command that we actually need to utilize. So what I can do is just copy and paste from the exercise file here 'cause there's quite a few properties that need to be updated. And you can see from here that I'm going to perform a search, looking for admin or delegated access. I'm going to focus on the [[Microsoft]] CDX account which is my admin account. I'm looking for admin or delegation on the log-on types with a range of dates. I can say that I've got my dates a little bit wrong here. I need to update here, otherwise it'll complain. Then I've got my mail recipient. Now, when we execute this command, it's not going to return any results to me, it will email me or email the status mail recipients, a copy of those. Then of course, I've got some Boolean values which are, do you want to check external access or we're looking for things that have attachments, and then of course, I'd like to show all the details possible. Now when you press enter, it looks like it returns values,
>
> **[1:35](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-review-audit-logs?u=76281980&t=95)** but what it actually does, it just returns the fact that it kicked off the search. There's the name of the search with my query. And then you'll just have to wait for it to kind of email results back to you if and when it matches. Now, there are other commands that are available too. So not only do we have the new admin audit log search one, but also the mailbox one which you've looked at, we can also search the admin audit log as well, by looking for specific values. Say if I type search dash admin audit log and press enter, you can see what it's got here is it brings back a series of values of things it took place, and of course our most recent one is us creating the labels. And so that shows up inside there. So if I copy set label policy, I can go back to my search, and then from here, I could say, you can say commandlets and I can paste the commandlet and press enter. And sure enough, it'll retrieve that specific instance. Now, of course, we did create some other bits and pieces, so let me go back to here and say new label. And of course it returns back all the execution of new label. So this allows me to kind of retrieve values and say, "Well, who was it that originally executed that [[Powershell]] command against Microsoft 365, for example." Now, of course, we also have the most common one, which is actually searching the unified log. So the unified log is the one that's part of the core audit capabilities. So what we can do first is I'm going to specify a start date.
>
> **[3:12](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-review-audit-logs?u=76281980&t=192)** So let's do 10/01/2021. I'll then also do an end date, which just make it short and sweet 10/10/21, and enter, I can then use search UnifiedAuditLog, and I'm just going to pass a start date and also an end date to that. And then I'll press enter. Now this one's going to go crazy and suddenly start capturing. I'm going to stop it from executing, but it's going to go and capture everything that's been logged in the audit log and taken place that you would see if you went into the admin kind of center and wanted to have a look. Now, what we can do is we can start to go through and say, "Well, what about if I wanted to look at a specific instance?" So let me grab the identity of this one here. What I can then do a search the UnifiedAuditLog, get rid of all of this. And if I start tabbing through the various items, you can say I can type session IDs, all kinds of different things, users, but if I go back to kind of the beginning end date object IDs, I could paste a value. Now watch what happens. It goes through, it still comes back and asks me for an end date. So I'm going to go here and just put an end date. It's going to ask me for a start date, 10/01/2021, and then it's going to go ahead and try and identify. Now, why didn't it find anything? Because the object ID doesn't refer to the actual entry that's been retrieved.
>
> **[4:46](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-review-audit-logs?u=76281980&t=286)** Now to retrieve the specific entries, we would need to know what the identity of the object is. This object ID needs to be the object ID that we're kind of looking for. So if it was a [[Microsoft Word|word]] document that had an object ID of 2700, let's say, that's what that value would be. But searching the audit log, we can also filter it down to specific things like types of records and things that we're trying to identify. So let me just clear this here, go back to our search, the unified log with our start dates, and then at this point I can start to add in record types, things that I want to kind of identify. So let me just choose RecordType PM, and then I'm going to use [[SharePoint]] file operation. I can press enter. This will now search for everything that was SharePoint online file. For example, if we scroll up, you can see instantly this is my all test cases document, and if I scroll up, the operation is file accessed, and the record type SharePoint file operation, and all the results will match them. So we can filter those results out really quickly. Now that's all helpful that we can retrieve things in the mailbox, things in the audit log, but what about if we wanted to retrieve things about, uses that have logged in? Well locally, what we're able to do here is connect to [[Microsoft Entra ID|Azure Active Directory]]. So I'm going to say, get [[Microsoft Entra ID|Azure AD]] audit signing logs and press enter, and of course straight away, it's going to say,
>
> **[6:18](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-review-audit-logs?u=76281980&t=378)** "Well, actually you need to connect to Azure AD." As do credentials, I still have my creds object. We'll just connect to Azure AD here, and we'll do the same thing again and do sign-in logs. And then this will go ahead and connect to Azure Active Directory and then start to bring down and we'll stop it there 'cause there's way too many. And it just keeps going and going and going. There we go. So that brings back specific ones that are there. It brings back literally everything that's in there. Now, how do we kind of filter those out? Well, we can luckily pass a filter object that can help us, kind of break down and say, "Well, actually I don't need to get all of the values, I'm just looking for specific things." So let me clear this out and we'll do a simple one here. So what we can do is say filter, and then I'm going to say, UserPricipalName equals, and then I can put in a user principal name, which in this case we'll just use our admin account and then I can press enter. And what this will do here, is this we'll go ahead and search. I'm going to stop that again 'cause it has too much information that brings back, but it will go ahead and bring back all the results, and if we start scrolling through the various properties, we should see user principal name, which is my admin account that's been listed here. So we can not only retrieve stuff from the regular audit log, the admin log, the mailbox,
>
> **[7:51](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/how-to-review-audit-logs?u=76281980&t=471)** we can get them from Azure AD, then of course we haven't even touched on that, you can get them from [[Microsoft Teams]] or from all the other locations too. But you can easily retrieve values from any of the audit logs by just using PowerShell.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Entra ID|Azure ad]] (4), [[SharePoint]] (3), [[Microsoft 365]] (2), [[Powershell]] (2), [[Microsoft Entra ID|Azure active directory]] (2)
> **Cross-References:** go back to (4)
> **UI Navigation:** scroll up (2), go to (1)
> **Analogies:** for example (3)
> **CLI Commands:** make (1), find (1)
> **Tools:** powershell (2)
> **Env Vars:** cdx (1)
> **Exercise Files:** exercise file (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/powershell-for-microsoft-365-administration/next-steps?u=76281980&t=0)** - [Liam] I hope you've enjoyed this course as much as I've enjoyed being able to put it together for you and take you through the basics of using [[Powershell]] to manage [[Microsoft 365]]. I recommend that your next steps are download each of the PowerShell modules for each of the services, review the basic commands that are available, then perform some basic tasks using those PowerShell commands, and then lastly, start to learn about the [[Microsoft]] Graph PowerShell SDK, or [[Software Development]] kit, and the commands, as they will be some of the replacement going forward. Regardless of where you go from here with PowerShell for Microsoft 365, I encourage you to dig deeper and deeper into how it can benefit you. With Microsoft 365, the next logical step is to begin using some of the more advanced capabilities with PowerShell and bundle those with parts of [[Microsoft Azure|Azure]]. Most of all, enjoy the process of managing your Microsoft 365 tenant using PowerShell. It is a core part of what I do each day and has played a big role in my overall IT career, especially working with organizations, both on premises and within the cloud. I wish you the best of luck as you utilize PowerShell for Microsoft 365.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Powershell]] (8), [[Microsoft 365]] (5), [[Microsoft]] (1), [[Software Development]] (1), [[Microsoft Azure|Azure]] (1)
> **Tools:** powershell (8)
> **Env Vars:** sdk (1)
> **Definitions:** is a  (1)
> **Speakers:** - [liam] (1)


## Instructor

- [[Liam Cleary]]

## Resources

- Exercise files available

## Skills Covered

- Office 365 Administration
- Powershell

## Path Context

### In [[Powershell- Automate IT Tasks and Microsoft 365 Administration]]
← [[PowerShell- Automating IT Administration]] | **3 of 3**

## Appears In

- [[Powershell- Automate IT Tasks and Microsoft 365 Administration]]

## Related Courses

_Courses sharing skills:_

- [[Microsoft 365 Teams Administrator Associate Cert Prep (MS-700)- Managing Collaboration Tools]] — Office 365 Administration
- [[Microsoft 365 for Administrators- Supporting Users]] — Office 365 Administration
- [[Learning PowerShell]] — Powershell
- [[PowerShell- Automating IT Administration]] — Powershell
- [[Powershell 7 Essential Training]] — Powershell

---

[↑ Back to top](#)