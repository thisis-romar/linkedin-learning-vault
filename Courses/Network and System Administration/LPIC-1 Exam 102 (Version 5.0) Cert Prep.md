---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: lpic-1-exam-102-version-5-0-cert-prep
url: "https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep"
duration_minutes: 322
duration: 5h 22m
level: Intermediate
updated: 3/16/2020
learners: 2523
skills:
  - Linux System Administration
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C4D0DAQGZeN1PtFQy8A/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1584040236526?e=2147483647&amp;v=beta&amp;t=SbPeUTqLt28MGeJdb8ImfS6qDhWYAXx_euuH6zBqyEI"
linkedin_topic: Network and System Administration
learning_paths:
  - '[[Prepare for the Linux Professional Institute LPIC-1 (101-500 and 102-500) Exams]]'
prev_courses:
  - '[[Lpic 1 Exam 101 Version 5 0 Cert Prep]]'
path_nav: '[{"path":"Prepare for the Linux Professional Institute LPIC-1 (101-500 and 102-500) Exams","position":2,"total":2,"prev":"Lpic 1 Exam 101 Version 5 0 Cert Prep","next":null}]'
path_count: 1
tags:
  - course
  - topic/network-and-system-administration
  - skill/linux-system-administration
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Network%20and%20System%20Administration/LPIC-1%20Exam%20102%20(Version%205.0)%20Cert%20Prep.md)

![LPIC-1 Exam 102 (Version 5.0) Cert Prep](https://media.licdn.com/dms/image/v2/C4D0DAQGZeN1PtFQy8A/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1584040236526?e=2147483647&amp;v=beta&amp;t=SbPeUTqLt28MGeJdb8ImfS6qDhWYAXx_euuH6zBqyEI)

# LPIC-1 Exam 102 (Version 5.0) Cert Prep

> Earning the LPIC-1 certification—a distribution-neutral Linux certification—shows potential employers that you have the real-world skills needed to excel as a Linux administrator. In this course, Grant McWilliams covers what you need to know to ace LPIC-1 exam 102, version 5.0. Grant reviews topics that correspond to each exam objective. Learn about shells and shell scripting; managing user and gr

> [LinkedIn Learning](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep) | 5h 22m | Intermediate | 3K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (5 videos)
  - [Welcome](#welcome)
  - [What you should know](#what-you-should-know)
  - [About the LPIC 1 exam](#about-the-lpic-1-exam)
  - [Install CentOS 7](#install-centos-7)
  - [Centos 7 Lab setup](#centos-7-lab-setup)
- [**1. Customize and Use the Shell Environment**](#1-customize-and-use-the-shell-environment) (5 videos)
  - [Set environment variables](#set-environment-variables)
  - [Bash functions for frequent tasks](#bash-functions-for-frequent-tasks)
  - [Bash aliases as shortcuts](#bash-aliases-as-shortcuts)
  - [Set command search path](#set-command-search-path)
  - [Maintain skeleton directories](#maintain-skeleton-directories)
- [**2. Write Simple Scripts**](#2-write-simple-scripts) (10 videos)
  - [Introduction to Bash](#introduction-to-bash)
  - [What is a shell script?](#what-is-a-shell-script)
  - [Execute compound commands](#execute-compound-commands)
  - [Use Command and Variable substitution](#use-command-and-variable-substitution)
  - [Conditional flow with if, then if](#conditional-flow-with-if-then-if)
  - [Conditional flow with case](#conditional-flow-with-case)
  - [Iterate with for loops](#iterate-with-for-loops)
  - [Iterate with while loops](#iterate-with-while-loops)
  - [Using return codes](#using-return-codes)
  - [Create a monitor script](#create-a-monitor-script)
- [**3. Install and Configure X11**](#3-install-and-configure-x11) (6 videos)
  - [X11 architecture](#x11-architecture)
  - [About desktop environments](#about-desktop-environments)
  - [Assistive technologies](#assistive-technologies)
  - [Override specific aspects of Xorg configuration](#override-specific-aspects-of-xorg-configuration)
  - [About remote desktop protocols](#about-remote-desktop-protocols)
  - [Display applications on remote X servers](#display-applications-on-remote-x-servers)
- [**4. Manage User and Group Accounts**](#4-manage-user-and-group-accounts) (7 videos)
  - [Add and removing users](#add-and-removing-users)
  - [Modify users](#modify-users)
  - [Manage groups](#manage-groups)
  - [Manage special purpose and limited accounts](#manage-special-purpose-and-limited-accounts)
  - [Awareness of shadow passwords](#awareness-of-shadow-passwords)
  - [Change default password policies](#change-default-password-policies)
  - [Individual user account settings](#individual-user-account-settings)
- [**5. Automate System Administration Tasks by Scheduling Jobs**](#5-automate-system-administration-tasks-by-scheduling-jobs) (4 videos)
  - [Reccuring user jobs using cron](#reccuring-user-jobs-using-cron)
  - [Reccuring system jobs using cron](#reccuring-system-jobs-using-cron)
  - [Configure user access to cron and at services](#configure-user-access-to-cron-and-at-services)
  - [Systemd timer units](#systemd-timer-units)
- [**6. Localization and System Time**](#6-localization-and-system-time) (7 videos)
  - [Set the system date, time, and timezone](#set-the-system-date-time-and-timezone)
  - [Configure locale settings and environment variables](#configure-locale-settings-and-environment-variables)
  - [Install and start chrony](#install-and-start-chrony)
  - [Configure chrony server](#configure-chrony-server)
  - [Configure chrony client](#configure-chrony-client)
  - [Use cryonyc](#use-cryonyc)
  - [Using ntpd for serving time](#using-ntpd-for-serving-time)
- [**7. System Logging**](#7-system-logging) (8 videos)
  - [Locate and interpret system log files](#locate-and-interpret-system-log-files)
  - [Read the system journal](#read-the-system-journal)
  - [Configuration of logrotate](#configuration-of-logrotate)
  - [Filter journal data by criteria](#filter-journal-data-by-criteria)
  - [Rsyslog actions](#rsyslog-actions)
  - [Clear old systemd journal data](#clear-old-systemd-journal-data)
  - [Retrieve systemd journal data from a rescue system](#retrieve-systemd-journal-data-from-a-rescue-system)
  - [About syslog, rsyslog, and syslog-ng](#about-syslog-rsyslog-and-syslog-ng)
- [**8. Mail Transfer Agent (MTA) Basics**](#8-mail-transfer-agent-mta-basics) (8 videos)
  - [About email](#about-email)
  - [How email works](#how-email-works)
  - [Knowledge of common MTA programs](#knowledge-of-common-mta-programs)
  - [Install Postfix](#install-postfix)
  - [Manage access to Postfix](#manage-access-to-postfix)
  - [Configure a local mail server](#configure-a-local-mail-server)
  - [Create email aliases](#create-email-aliases)
  - [Forward mail as a user](#forward-mail-as-a-user)
- [**9. Manage Printers and Printing**](#9-manage-printers-and-printing) (3 videos)
  - [Configure local and remote printers with CUPS](#configure-local-and-remote-printers-with-cups)
  - [Troubleshoot general printing problems](#troubleshoot-general-printing-problems)
  - [Manage user print jobs](#manage-user-print-jobs)
- [**10. Fundamentals of Internet Protocols**](#10-fundamentals-of-internet-protocols) (5 videos)
  - [About private and public "dotted quad" IP addresses](#about-private-and-public-dotted-quad-ip-addresses)
  - [Network masks and CIDR notation](#network-masks-and-cidr-notation)
  - [Common TCP and UDP ports and services](#common-tcp-and-udp-ports-and-services)
  - [Differences and major features of UDP, TCP, and ICMP](#differences-and-major-features-of-udp-tcp-and-icmp)
  - [Knowledge of the major differences between IPv4 and IPv6](#knowledge-of-the-major-differences-between-ipv4-and-ipv6)
- [**11. Persistent Network Configuration**](#11-persistent-network-configuration) (5 videos)
  - [Gather network information](#gather-network-information)
  - [Manual network interface configuration](#manual-network-interface-configuration)
  - [Configure networking using Network Manager](#configure-networking-using-network-manager)
  - [Configure networking using the GUI](#configure-networking-using-the-gui)
  - [Awareness of systemd-networkd](#awareness-of-systemd-networkd)
- [**12. Basic Network Troubleshooting**](#12-basic-network-troubleshooting) (4 videos)
  - [About routing](#about-routing)
  - [View the routing table](#view-the-routing-table)
  - [Manually configure routing using iproute2](#manually-configure-routing-using-iproute2)
  - [Debug problems associated with the network configuration](#debug-problems-associated-with-the-network-configuration)
- [**13. Configure Client-Side DNS**](#13-configure-client-side-dns) (5 videos)
  - [Query remote DNS servers](#query-remote-dns-servers)
  - [Configure local name resolution with remote DNS servers](#configure-local-name-resolution-with-remote-dns-servers)
  - [Modify the order in which name resolution is done](#modify-the-order-in-which-name-resolution-is-done)
  - [Debug errors related to name resolution](#debug-errors-related-to-name-resolution)
  - [About systemd-resolved](#about-systemd-resolved)
- [**14. Perform Security Administration Tasks**](#14-perform-security-administration-tasks) (6 videos)
  - [Use Nmap and netstat to audit network services](#use-nmap-and-netstat-to-audit-network-services)
  - [Set up limits on users](#set-up-limits-on-users)
  - [Determine which users have logged in](#determine-which-users-have-logged-in)
  - [Basic sudo configuration and usage](#basic-sudo-configuration-and-usage)
  - [Find suid/sgid files](#find-suidsgid-files)
  - [About TCP wrappers](#about-tcp-wrappers)
- [**15. Securing Data with Encryption**](#15-securing-data-with-encryption) (5 videos)
  - [Perform basic OpenSSH 2 client configuration](#perform-basic-openssh-2-client-configuration)
  - [About OpenSSH 2 server host keys](#about-openssh-2-server-host-keys)
  - [Perform basic GnuPG configuration and revocation](#perform-basic-gnupg-configuration-and-revocation)
  - [Use GPG to encrypt, decrypt, sign, and verify files](#use-gpg-to-encrypt-decrypt-sign-and-verify-files)
  - [SSH port tunnels](#ssh-port-tunnels)
- [**Conclusion**](#conclusion) (1 videos)
  - [Taking the exam](#taking-the-exam)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Welcome](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/welcome?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/welcome?u=76281980&t=0)** - [Instructor] Possessing credentials as well-known as the [[Linux]] Professional Institute's LPIC-1 certification will help secure a position in the IT industry as a Linux administrator. The LPIC-1 certification is a popular distribution neutral certification. Prospective employers want assurance that you can do the job at hand, and the LPIC-1 certification goes a long way towards proving your abilities. This course covers all of the objectives of the new LPIC-1 version five exam 102 in a brief certification prep format. All chapters include assessment questions concerning the same material to help you assess your own knowledge. This course ends with tips and tricks on taking the exam to help you prepare. My name is Grant McWilliams, college professor and Linux enthusiast. In the past 20 years, I've trained many people to be Linux-certified, and that experience is accessible to you. I'm sure you're excited to get started and I'm excited to teach you, so let's get to it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (4)
> **Env Vars:** lpic (4)
> **Definitions:** known as (1), is a  (1)
> **Speakers:** - [instructor] (1)

#### [What you should know](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/what-you-should-know?u=76281980&t=0)** - [Instructor] For this course we'll be using CentOS 7 Enterprise [[Linux]], which is a clone of Red Hat Enterprise Linux 7 as well as [[Ubuntu]] 19.04 running in VirtualBox [[Virtual Machines]]. In this course, when I refer to Enterprise Linux 7, I mean Red Hat Enterprise Linux 7 or CentOS 7 Enterprise Linux. Anything we do on one will work on the other. When I refer to Ubuntu, I really mean Debian-based distributions. This could include Debian and Linux Mint. For this course we'll install Linux in VirtualBox virtual machines. There are versions of VirtualBox for [[Windows]], macOS, Solaris, and various Linux distributions. For this course, I'll be running VirtualBox on a Linux host. In order to run virtual machines for this course, you'll need a computer with an Intel or AMD 64-bit CPU with virtualization support. Intel CPUs with virtualization support will be advertised as having VT-x and AMD calls it AMD-V. You will also need a 64-bit host OS. If you're using Linux, this is automatic. If you're using Windows, make sure you have a 64-bit version of that OS. If your OS is 64-bit, then you'll want to download the 64-bit version of VirtualBox as well. For this course, you may also want to have high speed internet for downloading ISO images and doing Linux operating system software updates. You will also want to have 32 to 50 gigabytes of free hard drive space minimum. This will provide enough space for two CentOS 7 Linux guest VMs and one Ubuntu 19.04 guest VM. If you have more free space available, it can give you more flexibility for creating additional guest VMs or giving your VMs
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/what-you-should-know?u=76281980&t=93)** larger virtual hard drives. To run more than one VM at a time, you'll need to have at least four gigabytes of free system memory if you're Linux as your host OS. If you're using Windows as a host OS, I recommend eight gigabytes of RAM minimum. Notice that these [[Hardware]] requirements are not that large. Most computers built in the last 10 years will work fine.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (13), [[Ubuntu]] (3), [[Virtual Machines]] (3), [[Windows]] (3), [[Hardware]] (1)
> **Env Vars:** amd (3), cpu (1), iso (1), ram (1)
> **Prerequisites:** you'll need (2), install (1), make sure you have (1)
> **Versions:** 19.04 (2)
> **CLI Commands:** make (1)
> **Code Identifiers:** macos (1)
> **Cross-References:** in the last (1)
> **Exercise Files:** download the (1)

#### [About the LPIC 1 exam](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-the-lpic-1-exam?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-the-lpic-1-exam?u=76281980&t=0)** - [Narrator] The [[Linux]] Professional Institute created the LPIC-1 certification in 1999, about 10 years after the first Linux kernal was released as an entry level, distribution agnostic certification. In 2010, the Linux Professional Institute teamed up with CompTIA and created a combined set of exams. This resulted in two exams, LXO-103 and LXO-104. Exam takers received both certifications if they passed these exams. These exams were retired in October of 2019, so we won't focus on them in this course. The new LPIC-1 version 5 goes back to being an independent certification offered by the Linux Professional Institute, which validates skills of IT professionals. This certification is not product based and covers tools and technologies from many different distributions including Red Hat, Debian, and SUSE. And unlike the previous certification, it's not tied to the CompTIA Linux+ certification. This course covers the new LPIC-1 version 5.0, 102 exam, which includes shell scripting, user interfaces, and graphical desktop environments, as well as accessibility. For administrative tasks, it covers how to manage user and group accounts, scheduled tasks, localization, and managing system time. It goes on to include system logging, and mail transport agents, as well as managing printers. The 102 exam also covers general networking concepts, such as IP version 6, subnetting, and configuring Linux networks manually, as well as student network manager. It covers networking and DNS troubleshooting
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-the-lpic-1-exam?u=76281980&t=94)** and ends with host system security and encryption. This course covers exam 102 only. We also have a course in the course library that covers LPIC version 5.0 exam 101. So be sure to check that out. The LPIC-1 exam 102 has 60 questions, and you have 90 minutes to complete them. The exam costs $200 U.S. dollars and the exam can be taken at many Pearson [[Vue.js|VUE]] testing centers around the world. The certification is valid for five years. The exam is, or will be, available in English, German, Japanese Portuguese, Chinese, Spanish, Italian and French. Not all languages are available at exam launch time. Check the LPI website for more information and available languages. When you arrive at the Pearson VUE testing center, two [[Forms]] of ID will be required and no personal items will be allowed in the testing room. You must arrive at the testing center 15 minutes early, and if you're 17 years or younger you can take the test, but you need to have an adult with proper ID with you. Please keep in mind that the course you are currently taking is a certification prep course, and covers the objectives of the exam. It does not provide in-depth training for each objective. For more in-depth LPIC certification training, search for my Linux system administrator courses in the course library. There are separate courses covering disk systems, networking, scripting, and so on. In this course, we will be using CentOS 7 Enterprise Linux for all exam objectives. You could however use [[Ubuntu]] or another distribution to learn the objectives
>
> **[3:06](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-the-lpic-1-exam?u=76281980&t=186)** of this certification exam as the only thing distribution specific on the LPIC-1 version 5.0 102 exam is the commands you'll use to solve socket packages and the package names themselves. If you're using another distribution, just use then installation programs for that distribution to install the equivalent packages.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (8), [[Vue.js|Vue]] (2), [[Forms]] (1), [[Ubuntu]] (1)
> **Env Vars:** lpic (7), lxo (2), vue (2), suse (1), dns (1)
> **Versions:** version 5 (4), version 6 (1)
> **Prerequisites:** you need to have (1), install (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [narrator] (1)

#### [Install CentOS 7](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/install-centos-7?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/install-centos-7?u=76281980&t=0)** - [Instructor] For most of this course, we'll be using a CentOS 7 Enterprise [[Linux]] Virtual Machine running in VirtualBox. I won't walk through every step of the installation but instead we'll focus on the important parts. Here's a short checklist of what we need to do to install successfully. Number one, be sure you have virtualization turned on in your BIOS/UEFI. Also, be sure your host OS is 64-bit. Now, install VirtualBox in your host OS. To do so, go to [VirtualBox.org](https://VirtualBox.org) and click on the Download VirtualBox 6 icon. I'll be using Linux on my host OS, but [[Windows]], MacOS X and Solaris will also work. If you're using Linux, you may be able to install VirtualBox through the package manager instead. If that's an option. I had it recommended. Second, install VirtualBox extension pack on the host by downloading it from the same webpage. Once it's downloaded, open it and VirtualBox should install the extension. This extension provides faster USB devices, disk encryption and meta virtual network cards. Next, download the CentOS 7 DVD ISO image. You can download it by going to [CentOS.org](https://CentOS.org). As of this recording, CentOS 8 is newly available. However, since the exam covers technologies included in CentOS 7, we'll use that version instead. To download an older version such as CentOS 7, you want to click on the alternative downloads link. Now, scroll down to version 7 and click on mirrors. Next, click on a mirror and then find the .ISO file. If you click this file, it will download with HTTP. You also have the option of downloading a torrent file, which should start a torrent client.
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/install-centos-7?u=76281980&t=94)** Using BitTorrent may be faster and will put less load on the servers. Once these are downloaded, start VirtualBox and create a new VM by clicking on New. For the name, I'll start typing in CentOS and it'll fill in the other fields for me. I'll then change the name to rhhost1. This is nice shortcut, saving time. Now click through the [[Representational State Transfer (REST)|rest]] of the dialogs by taking the defaults. After the VM has been created, click on Settings to optimize the VM. Click on System, and change the memory to two gigabytes. It isn't required to have this much, but the GUI does run nicer. Click on Processor to add more CPUs if you have them. Now click on Acceleration to change the hypervisor. For me, KVM is the best choice. If you're using a Windows host, you may want to choose Hyper-V. Take note that default is a good choice as well and has the highest chance of working. Now click on Display and click on Enable 3D Acceleration. Leave the Display Type as VMSVGA for now, so the install goes smoothly. I've had problems with the other choices during install with some distributions in the past. Later, feel free to change it to VBoxSVGA which may provide additional speed up. Click on OK. And then click on Start. Now choose your downloaded CentOS 7 ISO image and click on Open. Now click on Start again. Highlight Install CentOS 7 and hit Enter. For the install, I will skip to the important parts. Just pause the video until your installation catches up.
>
> **[3:10](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/install-centos-7?u=76281980&t=190)** During the install, set your time and timezone. And for software selection, choose Server with GUI. And then click on Done. This will give us a nice base to install with the graphical interface. Be patient as the installation GUI may be slow. Also, click on Installation Destination, and immediately click Done. The default automatic partition configuration is fine for what we are doing. Then click on Begin Installation. Once it starts installing, give root a nice strong password. CentOS will warn you if the password is weak. Then click on Done and click on User Creation. We're going to create a user here. The user name isn't that important but making the user an administrator is. During this course I'll be using several different users, but all of them will have administrator privileges through sudo. I will create a user name grant now and check the Make this user an administrator box. This step is necessary for future lessons. If you create other users later, make sure you add them to the group named wheel which gives them admin privileges using the sudo command. That is essentially what this checkbox does. Once your installation is done, then click on Reboot. Now accept the license agreement. Click on Finish Configuration, and then log in. After you've logged in,
>
> **[4:46](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/install-centos-7?u=76281980&t=286)** click through all of the post installation dialog boxes. Now CentOS 7 is installed and we can proceed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (3), [[Windows]] (2), [[Representational State Transfer (REST)|Rest]] (1)
> **UI Navigation:** click on (21), go to (1), scroll down (1), checkbox (1)
> **Env Vars:** iso (3), gui (3), bios (1), uefi (1), usb (1)
> **Prerequisites:** install (11), required to (1)
> **CLI Commands:** sudo (2), make (2), find (1)
> **URLs:** [virtualbox.org](https://virtualbox.org) (1), [centos.org](https://centos.org) (1)
> **Versions:** version 7 (1)
> **Exercise Files:** download the (1)

#### [Centos 7 Lab setup](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/centos-7-lab-setup?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/centos-7-lab-setup?u=76281980&t=0)** - [Narrator] For the best experience, you'll want install VitrualBox guest editions in your CentOS 7vm. This will allow you to do fullscreen mode and have a seamless mouse experience. First, make sure your network is turned on by going to your top right-hand menu in your rhhost1 VM. Click on Wired Off and then click on Wired Settings. Then click the slider button next to your wired connection. This activates the network connection but does not make it persistent. To make it persistent, click on the gear icon next to the slider button. Now click on Connect Automatically and click on Apply. Now you can close this window. Now let's update all software packages by opening a terminal and then typing in 'sudu yum update -y' and hit enter, and then enter your password if prompted. This process should have updated all packages including kernels. Now reboot by typing in 'sudu reboot' and hit enter. Do not skip this step, or installations of the guest editions will likely fail. After your VM reboots, log in. Once logged in, open a terminal and type in 'sudu yum group install -y "Development tool"'
>
> **[1:22](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/centos-7-lab-setup?u=76281980&t=82)** and hit Enter, and enter your password if prompted. This installs the [[Software Development]] tools. After they're done, we'll install the kernel headers. Now let's install the kernel headers. Type in 'clear' and then type in 'sudu yum install -y kernel-devel' and hit enter.
>
> **[1:46](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/centos-7-lab-setup?u=76281980&t=106)** Once that's done, you can close the terminal window. Next, insert the virtual guest edition CD using the virtual box devices menu. You may have to press your Host key to release your mouse pointer. Select the devices menu and then select Insert Guest Edition CD Image. Vitrual box will probably have to download the guest edition's ISO image. It may also fail, it often does. Click on Download and then click on Download again. If it fails, click on Cancel All, click on OK, and then go back up to your Devices menu, click on Insert Guest Edition CD Image again, and then click on Download again, and this time, click on the link. This will open Firefox, and Firefox will download the ISO image. Click on Save File and then click on OK. Choose your location and then click on Save. Once that's done, close Firefox. Go back to your virtual machine and Cancel the download. Now go back to your drive menu, click on Optical Drives, then click on Choose Disk Image and then choose your disk image. Click on Vbox guest editions and click on Open. When the dialogue box appears, click on Run and enter your password. This process will take a few minutes as it has to compile a kernel module. It may even look like it failed, but be patient and wait until you get your prompt back. Now you should have full screen mode, faster networking, better graphics modes, seamless mouse mode, and copy & paste between the host and VM.
>
> **[3:18](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/centos-7-lab-setup?u=76281980&t=198)** You can close this window now. Shut down the VM by going to the top right-hand menu and clicking on the power button and then clicking on Power Off. Now let's configure the network. Make sure the VM is selected and then click on settings, then click on Network. We're going to change the Attached to NAT to Attached to Bridged Adapter. Make sure you choose your physical network adapter in your host. Mine is enp0s25. And then click on OK. Make sure you do this for both VMs. Now let's make a clone of this VM. This method takes a golden image and makes a clone of it, and is a valid way of creating a new VM. Right click on the VM and then select Clone. Change the name of the clone to rhhost2. Click on Linked Clone to save space and then for Mac Address Policy, change it to Generate new Mac addresses for all network adapters, and then select Keep Disk Names. Now click on Clone. When it's done, boot each VM to ensure everything works. You may also want to change the host name so you can tell the VMs apart. The very last thing we'll do is snapshot the VMs. You can snapshot any state by pressing the host key, which on my OS is the right Control key + the T key. I'll name my snapshot Base Install. And then click on OK. You will want to do this with both of your VMs. Now, whenever you want to revert back to this fresh state, you can just close the VM
>
> **[4:49](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/centos-7-lab-setup?u=76281980&t=289)** and select Power Off the Machine and click Restore Current Snapshot 'Base Install' and then click OK. This allows you to practice the course exercises more than once. And now we have out CentOS7 lab setup and we can proceed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Software Development]] (1)
> **UI Navigation:** click on (27), select the (1)
> **CLI Commands:** make (7), yum (3), cd (3)
> **Prerequisites:** install (7), configure (1), setup (1)
> **Tools:** terminal (3), firefox (3)
> **Env Vars:** iso (2), nat (1)
> **Cross-References:** go back to (2)
> **Exercise Files:** download the (1)


### 1. Customize and Use the Shell Environment

[↑ Back to Table of Contents](#table-of-contents)

#### [Set environment variables](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-environment-variables?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-environment-variables?u=76281980&t=0)** - [Instructor] There are two types of variables accessible in a shell session. Environmental variables are variables that are defined for the current shell and are inherited by any child processes or child shells. Shell variables are variables that are contained exclusively within the shell in which they were defined. They are often used to keep track of data like the current working directory. To see the environmental variables in a terminal type in print end and hit enter. If you want to get the value of an individual variable you can specify it. Type in printenv. And for instance, type in capital SHELL and hit enter. Another way is to use the echo command to print the value by typing in echo space dollar sign capital s-h-e-l-l, and hit enter. To see the shell variables we use the set command. Type in set and hit enter. By default the set command shows shell functions as well as variables which can be quite long. To change the behavior of set to posix mode which doesn't show functions use the dash o posix option. Type in set space dash o space posix and hit enter. Now type in set again and let's pipe it to less. Here we can see all the shell variables without the shell functions. To show the difference between environmental variables and shell variables, let's set a variable in our shell. Press Q to quit and then type in capital VAR equals capital TEST and hit enter.
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-environment-variables?u=76281980&t=95)** Now let's grep the output of set to view it. Type in set space pipe space grep space capital V-A-R and hit enter. And there it is. Now let's grep the output of printinf. Type in printenv space pipe space grep space capital VAR and hit enter and we can see that our variable is not a part of the environment. To turn our shell variable into an environmental variable we can just export it. Let's export the variable we just set. Type in export space capital V-A-R and hit enter. Verify with printenv. Type in printenv space pipe space grep space capital V-A-R and hit enter. And we can see that our variable's now a part of the environment. It isn't persistent since we didn't save it in a file. If we rebooted it would disappear. To unset environmental variables we can use the export command again. Type in export space dash n space capital V-A-R and hit enter. We can verify this by typing in printenv space pipe space grep space capital V-A-R and hit enter. We can see that the variable is no longer part of the environment. To unset shell variables we can use the unset command. Type in unset space capital V-A-R and hit enter. Earlier we changed a shell option by turning on posix mode by typing in set space dash O space posix. We can view all shell options by typing in set
>
> **[3:12](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-environment-variables?u=76281980&t=192)** space dash o and hit enter. To unset any shell option use plus O. For instance, to unset posix mode type in set space plus o space posix and hit enter. Then we can verify this again by typing in set space dash o. We can change the value of environment and shell variables by setting them. We can also change batch options using the set command. However, these values only survive for the current login session. To make them persistent, we need to add them to one of the bash startup files. Bash stores its configuration settings in multiple startup files. Different files are processed depending on how the shell starts. When we log into [[Linux]] by typing in a password, Linux first executes the /etc/profile which in turn executes scripts in /etc/profile.d. These two items are the system-wide file and directories holding our environment and shell variables. Then bash executes the ._profile in the user's home directory which holds environment and shell variables specific to the user. The local .bash_profile in turn executes the local .bashrc file which holds aliases and bash functions. In CentOS the local .bashrc also executes the system wide /etc/bashrc file which holds system wide aliases and functions. When that is complete it returns control
>
> **[4:45](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-environment-variables?u=76281980&t=285)** back to the local .bashrc file in the user's home directory. The local .bashrc file holds aliases and functions specific to the user. Once all of this is done the user logs in. If the user is already logged in and executes a shell script, it's executed in a shell, but it's not a login shell. Also, if a user's logged into the GUI and starts a shell, it's not a login shell. Generally speaking a login shell requires entering a password or providing a login key. For non-login shells, only the user's .bashrc gets run. Again, on CentOS the local .bashrc file executes the global /etc/bashrc file which holds system wide aliases and functions. Then it returns control back to the local .bashrc file again and finishes processing it. This file should contain aliases and functions specific to the user. Once this is all done, the shell starts. In summary the various bash profile files are for environmental and shell variables useful for interactive logins. If you want to set a variable for all users, set it in either the /etc/profile or a script in /etc/profile.d. If you want to set a variable specific to a user, change it in the user's .bash_profile. If you want to add aliases or functions for all users, add them to the /etc/bashrc.
>
> **[6:20](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-environment-variables?u=76281980&t=380)** If you want to add aliases or functions specific to a user, add them to the user's .bashrc. It's important to note that when you add aliases or functions to the user's .bashrc file be sure to add them to the end of the file after .bashrc has been called to override system wide aliases and functions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (2)
> **CLI Commands:** grep (6), make (1)
> **Tools:** bash (5), terminal (1)
> **Env Vars:** var (2), shell (1), test (1), gui (1)
> **Code Identifiers:** bash_profile (2)
> **Analogies:** for instance (2)
> **Speakers:** - [instructor] (1)

#### [Bash functions for frequent tasks](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/bash-functions-for-frequent-tasks?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/bash-functions-for-frequent-tasks?u=76281980&t=0)** - [Presenter] If we do the same task often we can add it to a shell startup script as a shortcut, so it is available when the shell opens. These shortcuts can be in the form of a shell function or Command alias. Let's talk about shell functions. Note that the order of execution in bash is first bash aliases. And then bash functions followed by bash built-in commands that are built into the bash shell. And lastly, the commands that reside on the disk. If we type in a command without providing a path to it, the shell will search for it in the directories listed in the path variable. If you want to force a certain command to run, provide the relative path or the absolute path on the command line. For bash functions and aliases, it's important to know which files to add them to. There are many different shell startup scripts that get executed depending on how the shell we're using was started. The shell could be an interactive login shell, a non interactive login shell and interactive non login shell, or a non interactive non login shell. Take note that different distributions may change the name of the scripts and or the order which they're executed in. As such, use this video as a source of information on where to look for the startup scripts more than an exact reference. A login shell, is a shell that we log into the system with interactively or it could be a non interactive login shell if it started with the dash dash login option. Either way, they're still both login shells. One we interact with and the other we don't. In either case, Bash reads and executes the commands and /etc profile first, and then ~/.bash_profile ~/bash_login
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/bash-functions-for-frequent-tasks?u=76281980&t=95)** and lastly ~/.profile, which are all in the user's home directory and specific to that user. Depending on the distribution, one or more of these scripts may spawn other scripts. In the case of Enterprise [[Linux]] seven, the /etc/profile also executes all scripts in the /etc/profile.ddirectory that end in .sh, as well as the /etc/profile.d/sh.local script. If you want to create a shell function that would be available to all users, you would want to create a file in the /etc/profile.ddirectory and name the file with a .sh ending. After the etc profile has run, the .bash_profile in the user's home directory is read and executed. With Enterprise Linux seven this runs the .bashrc file and the user's home directory as well. The users .bashrc then includes the contents of the global/etc/bashrc file. It does this by sourcing the file. Sourcing a file runs the contents of the file in the current shell. The dot command is a synonym of the source command. Sourcing a script is different than running it because it includes the commands in the current environment like they were in the current script as opposed to spawning a new shell with a new environment. For other distributions, this process may be carried out differently. For instance, Obuntu reads the etc profile which executes all scripts then /etc/profile.d just like the Enterprise Linux seven. It then executes /etc/bash.bashrc. It executes the .profile script in the user's home directory as well.
>
> **[3:07](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/bash-functions-for-frequent-tasks?u=76281980&t=187)** It doesn't matter what your distribution does, since you can follow the execution order by viewing the /etc/profile first. And then viewing each file it reads and executes, and continuing to do the same for each of those files. For an interactive non login shell, such as you'd get if you ran the bash command after you've logged in, bash will read and execute the .bashrc file in the user's home directory first, which then sources the /etc/bashrc file and skips the [[Representational State Transfer (REST)|rest]]. If the shell is a non interactive non login shell, such as would get if he ran a script with bash then it only reads the value of the bash_env variable. And if it lists a file, it reads and executes that. Before we add our own function. Let's take a look at the functions already active. In a terminal type in set and hit enter. All of these functions are available to you if you want to use them. Notice that most function names start with an underscore or two. This tells us that these are system functions. Now let's add our own. To do so load your .bashrc file into vi by typing in clear and then typing in vi + space + ~/.bashrc and hit enter. You'll notice that the last line of the file instructs us where to add our functions and aliases. Go into insert mode by pressing the I key and then go to the bottom of the file. Now let's add a function. Let's call this function mkcd, it will make a directory and then cd into it. Type in mkcd + left parentheses + right parentheses + space + left curly bracket
>
> **[4:39](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/bash-functions-for-frequent-tasks?u=76281980&t=279)** new line tab + mkdir + space + dash + p + space + $ + 1 new line: tab + cd + space + dollar sign + one new line: right curly bracket. In this example, the argument we provide for mkcd is placed in the dollar sign one positional variable and our function calls the mkdir -p command to create the directory and its parent directories if necessary. Then the next line calls the cd command to change into it. Now let's save and exit this file by pressing escape + colon + x + exclamation mark and hitting Enter. Because the .bashrc script gets sourced when we log in, this new function will not be active yet. We can either close our shell or source the file. lets source it, type in source + space + tilde + slash + .bashrc and hit Enter. And then let's test it by typing in set, we can see that our new function is available. We could have typed dot + space + tilde + slash + .bashrc as the dot command and the source command are the same. I like the source command because it's less ambiguous and easier to troubleshoot. Now let's try our function. Type in clear. Then type in mkcd + space + testdir and hit enter. We can see from our prompt that we have created a directory and changed into it. If you don't know if something is a command, built-in, bash function or alias, use the type command. Type in: type + space + dash + a + space + mkcd and hit enter. The output shows us it's a function.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (3), [[Representational State Transfer (REST)|Rest]] (1)
> **Tools:** bash (12), command line (1), terminal (1)
> **CLI Commands:** cd (3), mkdir (2), make (1)
> **Code Identifiers:** bash_profile (2), bash_login (1), bash_env (1)
> **Definitions:** is a  (4)
> **Analogies:** such as (2), for instance (1), just like (1)
> **Warnings:** note that (2), troubleshoot (1)
> **UI Navigation:** go to (1)

#### [Bash aliases as shortcuts](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/bash-aliases-as-shortcuts?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/bash-aliases-as-shortcuts?u=76281980&t=0)** - [Instructor] An easy way to create a shortcut to a command is by using an alias. Shell aliases get executed before anything else, so you can override other commands with them. Let's take a look at the Bash execution order. First, Bash aliases are run and then Bash functions. And next, Bash built-in commands that are built into the Bash shell itself. And lastly, the commands on-disk. Now, let's take a look at the current aliases. In a terminal, type in alias and hit enter. From this, we can see we have an alias named ll. This explains why we can type in ll and it will do a long list with colors. Now, let's add our own alias. To do so, load your .bashrc file into vi by typing in vi ~/.bashrc and hit enter. Now, go into insert mode by pressing the I key and go to the bottom of the file. An alias creates a shortcut for any command string. To exemplify how to create an alias, we will create a simple one that will speed up how we navigate the file system. Normally, to change into a parent directory, we type in cd .. Normally, to change into parent directory, we type in cd .. We could shorten this to .. To do so, type in alias ..="cd .."
>
> **[1:20](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/bash-aliases-as-shortcuts?u=76281980&t=80)** Now, when we type in .. it should execute cd, space.. , making navigating the directory structure faster. Note that this is a pretty simple alias that may make our workflow faster, but I often use aliases for creating shortcuts for really long command strings. For instance, r is sinking an entire directory to a remote server using specific options. This is not something I want to remember every time so I created an alias for it. Now let's save and exit this file by pressing escape + colon + x + exclamation mark and hitting Enter. Because the .bash RC script gets sourced only when we start a shell, these functions and aliases will not be active yet. We can either close our shell or source the file. Let's source it, type in source ~/.bashrc and hit enter. Now let's try our alias, type in .. and hit enter. We can see that this moved us up to the parent directory. If we have an alias with the same name as a command, and we don't want to execute the alias, we can prefix the command with the backslash. Let's look at our aliases again. Type in clear and then type an alias, and hit enter. We have an alias named ls to force the non aliased ls to run type in /ls and hit enter. This will run the non aliased ls and will display the listing without color. However, this doesn't guarantee that a command will run as there may be a function or built in named ls as well. If we want to force the execution of a command on disk, we can prefix it with the [[Microsoft Word|word]] command. For instance, type in command ls and hit enter.
>
> **[2:55](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/bash-aliases-as-shortcuts?u=76281980&t=175)** This will run a command on the disk named ls not a function or alias. If you don't know if something is a command or alias, use the type command. Enter type - a ls and hit enter. Here we can see we have an alias named ls and two on-disk commands.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **CLI Commands:** ls (9), cd (4), make (1)
> **Tools:** bash (6), terminal (1)
> **Definitions:** is a  (2)
> **Analogies:** for instance (2)
> **UI Navigation:** go to (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Set command search path](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-command-search-path?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-command-search-path?u=76281980&t=0)** - [Instructor] Bash has some pre-specified locations to look for commands in. These locations are stored in a variable called path. When you type in a command, Bash will look for the command in each of the directories in the path variable and execute the first one it finds. This means if you have the same command in more than one directory, the one that it finds first will get executed. We can look at the path variable using the echo command. In the terminal type in echo space dollar sign capital PATH and hit Enter. Each directory is delimited by a colon and Bash looks from the left to the right. This variable will have different contents depending on the user and the users can add directories to their copy as well. If you want to search for command in the path without using the more complex find or locate commands, just use the which command. Type in which space and then the command. In this case I'll use tar and hit Enter. This will tell you which directory tar is in if it's in your path. If you want to get more data including the man pages for the command use the whereis command type in whereis space tar and hit Enter. To add a directory to the path variable you need to put it in one of your shell startup scripts. Let's create a directory called scripts and add it to our path variable. Type in clear, and then type in mkdir space scripts and hit Enter. Now it's added to our dot Bash RC file. Type in vi space tilde slash dot bash RC, and hit Enter. Go into insert mode by pressing the I key and go down to the bottom and we're going to add two lines. Type in capital PATH equals dollar sign capital PATH colon tilde slash scripts.
>
> **[1:36](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-command-search-path?u=76281980&t=96)** Then hit Enter and type in export space capital PATH. Note that we're assigning the path variable with the current contents of the path variable plus our scripts directory. Also note that we didn't include the dollar sign when exporting the variable. Now Save and Exit by pressing escape X exclamation mark and hitting Enter. Note the script hasn't run yet. So you'll need to log out and back in again. A quicker solution for our current session is to source to file. Type in source space tilde slash dot bash RC and hit Enter. This will process the file immediately. Let's take the path variable again. Type in echo space dollar sign capital PATH and hit Enter. And we can see our scripts directory at the end, you will definitely want to log out and back in to make sure it works on a new shell as well. If you're an administrator and you want to add a directory to every users path variable, you'll need to do it at a system level. This means adding it to the etc profile or even better adding a script to the slash etc slash profile dot d directory. Let's do that. Type in clear and then type in sudo space vi space slash etc slash profile dot d slash scripts dot sh. This file doesn't exist yet but vi will create it. We can name it anything we want, but make sure that it has a dot sh extension. Hit Enter. Enter your password if prompted. Go into insert mode, and exactly the same lines as we did before. Capital PATH equals dollar sign capital PATH colon, tilde slash scripts, then hit enter
>
> **[3:10](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-command-search-path?u=76281980&t=190)** and type in export space capital PATH, and then save and exit by pressing escape colon x, exclamation mark, and hitting Enter. Because we're using the total shortcut for the user's home directory, it should work for every user. Although if a new user wants to take advantage of this, they'll need to create the scripts directory in their home.

> [!info]- Semantic Content
>
> **CLI Commands:** tar (3), make (2), find (1), mkdir (1), sudo (1)
> **Env Vars:** path (8)
> **Tools:** bash (6), terminal (1)
> **Warnings:** note that (2)
> **Prerequisites:** you'll need (2)
> **Speakers:** - [instructor] (1)

#### [Maintain skeleton directories](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/maintain-skeleton-directories?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/maintain-skeleton-directories?u=76281980&t=0)** - When a new user is created, their startup files are copied from the slash etc slash skel directory. This is called the Skeleton directory. We can specify a different skeleton directory by using the dash k option when we create a new user. Note that this is only for the initial creation of a user. If you change the files in the etc skel directory after the user is created, it won't update the existing users. Let's take a look at the etc skel directory. Type in cd space slash etc slash S-K-E-L and hit enter. Now let's do a long list showing hidden files. type in ls space dash la and hit enter again. We can see the shell startup files as well as a hidden .mozilla directory. We can add any files we want here and they will be copied to the new user's home directory automatically. These could be desktop settings, shell aliases, or even configuration files for various programs. Let's copy our modified .bashrc file into the skeleton directory and then create a new user. Type in clear and then type in sudo space cp space slash home slash your username. In my case let's Grant slash .bashrc space dot for the destination which is the current directory and hit enter. Now enter your password. This will copy my .bashrc file into my etc skel directory, use the path to your .bashrc file here. Now let's verify type in cat space .bashrc and hit enter. We can see that this is the modified bashrc file. Let's create a new user named newuser. Type in sudo, space user add space new user and hit enter.
>
> **[1:36](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/maintain-skeleton-directories?u=76281980&t=96)** Now list of files in the home directory of new user including hidden files. Type in sudo space ls space dash la space slash home slash new user and hit enter. We can see a .bashrc file but we don't know if it's the modified one. Let's make sure it's the right one by catting it. Type in sudo space cat space slash home slash new user slash .bashrc and hit enter. From this we can tell it's the modified .bashrc file. This file will get copied to each new user's home directory when we create a new user.

> [!info]- Semantic Content
>
> **CLI Commands:** sudo (4), ls (2), cat (2), cd (1), cp (1)
> **Definitions:** is called (1)
> **Warnings:** note that (1)
> **Speakers:** - when (1)


### 2. Write Simple Scripts

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction to Bash](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/introduction-to-bash?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/introduction-to-bash?u=76281980&t=0)** - Bash is the Default shell on Enterprise [[Linux]], and as such, is the default vehicle for Shell Scripts. As of this recording, the version of Bash that comes with Enterprise Linux seven, is four dot two. Older versions of Linux may target an older version of Bash, or even call the lowest common denominator, The Bourne Shell. Every Shell script specifies the shell it wants to be executed with. Although this can be overridden on the command line. Bash as a version four dot two, has the following features. If then else if conditionals, Case statements, Conditional Tests for, file ownership, file permissions, numeric equivalence, text equivalence, file type is a file, file type is a directory, file type is a block device or character device, file is readable, writable or executable, a file exists or a directory exists. For Loops, C-Style For Loops, While Until Loops with conditions. Brace Expansion, Arithmetic Expansion, Command Substitution, Integer Math, Mathematical Conditionals, Indexed Arrays, Associative Arrays, Extended Globs, and Expanded Regular Expressions. Other features include; functions, substring operations, parameter substitution, increment and decrement operators, debug traps, positional arguments, sub shells, and lastly co-processes. Bash is good for automating tasks,
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/introduction-to-bash?u=76281980&t=95)** and interacting with the operating system. There are jobs like running multiple processes asynchronously, and waiting for all processes to finish before moving on, that are very simple in Bash, but complex and other languages. There are times when you shouldn't use Bash, for instance, Bash does not handle complex data types like XML or [[JSON]], nor can it handle floating point math internally, and relies on external commands for this. Bash, if written poorly, is difficult to troubleshoot, as it's loosely typed and often runs code, even if it doesn't work. Also, there's no object orientation in Bash.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (3), [[JSON]] (1)
> **Tools:** bash (10), command line (1)
> **Definitions:** is a  (3)
> **Env Vars:** xml (1), json (1)
> **Analogies:** for instance (1)
> **Warnings:** troubleshoot (1)
> **Speakers:** - bash (1)

#### [What is a shell script?](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/what-is-a-shell-script?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/what-is-a-shell-script?u=76281980&t=0)** - [Instructor] The beauty of shell scripting is that we don't need a complex development environment to get started. To make a shell scrip that acts like a system command, we do need to do a few things. First, we create a text file in our favorite text editor. In a terminal, type in VI space tilde slash script.sh. We've named it script.sh so that VI will do syntax highlighting for us. Scripts don't have to be named like this but it makes it nicer for text editors. Syntax highlighting makes debugging easier. Hit enter. For the next step we need to edit the text file and make the first line point to the shell interpreter. We can do this by pointing at it directly to the shell interpreter by using the absolute path. Go into insert mode by pressing the I key, then add hash bang, slash bin, slash bash. In this case we're specifying an absolute path to the bash interpreter executable. We can also use a second form. Hash bang slash usr slash bin slash env space bash.
>
> **[1:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/what-is-a-shell-script?u=76281980&t=60)** In this case, we use the env command to search the system path for the bash interpreter, and execute it there. The advantage is that it can be anywhere in the path and it will still work. Now let's add one line of text to our script. Type in echo space double quote, this is a shell script, double quote, and save and exit by pressing escape, colon, X, exclamation mark and hitting enter. After we've added code and saved our script, we need to change the permissions and make it executable. To do so type in chmod space U plus X space script.sh, and hit enter. This step allows us to execute our script as if it were a command. The last thing we would do to make it seamless is to put our command in the system path. In Enterprise [[Linux]] seven we have a directive set aside for this, it just hasn't been created yet. Let's take a look at the contents of the path variable. Type in echo space dollar sign, uppercase P-A-T-H and hit enter. At the end of my path it says slash home slash grant slash bin. When a user logs in a directory is added to the system path specifically for that user. If you're using another distribution, you may not have this in your path variable. If not, you will need to add it. Now let's create this directory. Type in mkdir space tilde slash bin and hit enter. Lastly we'll move our script into the bin directory. Type in MV space script.sh space tilde slash bin and hit enter. Now we can execute any script in this directory just by calling it's name without the path.
>
> **[2:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/what-is-a-shell-script?u=76281980&t=153)** For instance, type in script.sh and hit enter. This will run it as if it were a system command. You can name your scripts without the .sh extension to make them seem even more like system commands, or even create symbolic links to them without the extension. Note that none of these steps are actually required to run text files as shell scripts. You can create a text file and provide it as an argument to bash and it will still run. For instance, type in clear, then type in echo space single quote, echo space this is a shell script, single quote space greater than, space tilde slash bin slash script2 with no extension and hit enter. Now let's cat this file. Type in cat space tilde, slash bin slash script2, and hit enter. This new script only has one line in it that reads echo this is a shell script. It does not have a .sh extension, it does not have execute permissions, and it does not have the first line pointing to a shell interpreter. Now let's run it by providing it as an argument to bash. Type in bash space tilde slash bin slash script2 and hit enter, and it does run without the items I just talked about, however I do recommend setting execute permissions and adding the first line pointing to the interpreter as it allows our shell scripts to mimic system commands.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1)
> **CLI Commands:** make (5), cat (2), chmod (1), mkdir (1), mv (1)
> **Tools:** bash (7), terminal (1)
> **File Paths:** script.sh (5)
> **Definitions:** is a  (3)
> **Analogies:** for instance (2)
> **Warnings:** note that (1)
> **Prerequisites:** required to (1)

#### [Execute compound commands](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/execute-compound-commands?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/execute-compound-commands?u=76281980&t=0)** - [Narrator] A simple command is any of the commands you type in on a regular basis. For instance, ls or grep root /etc/psswd. These are simple commands. When we refer to compound commands, things get more complex. A compound command, is a couple of commands that are grouped in some way and are all executed in close succession. We have a couple of ways of doing this, we can specify a second command that runs after the completion of our first command, type clear and then type in echo "hi" ; echo "there" and hit enter. This will run the first echo command and when it completes, it runs the second echo command. Note that the second echo command runs no matter where the first command succeeds or not. This is really the same thing as typing in the first command, hitting enter and typing in the second command and hitting enter again. It just queues up the command so we don't have to wait. If you only want the second command to run, if the first command succeeds, you can use a double ampersand symbols. For instance, in your home directory type in mkdir newfolder ampersand ampersand, space cd space New Folder. In this case, we only changed a new folder if its creation was successful. In our case it was you can see this in our prompt. Now let's go to the root directory and try the same command again, type in cd space slash and then type in clear. Now bring your line back and hit enter. In this case, the second command doesn't run
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/execute-compound-commands?u=76281980&t=95)** because the first wasn't successful since we couldn't make a directory and slash due to a lack of permissions. If you want to execute a second command only if the first command fails, then use a double pipe symbols. While we're still in slash. Let's try to create a directory again. This time typing clear and then type in MK dir space, new folder, space pipe pipe space echo space double quote directory Creation failed, double quote and hit enter. Now only when the Mk dir command fails does the echo run. Now let's combine both operators have been clear and then type in MK dir space new folder to space ampersand ampersand, space CD new folder to space pipe pipe, bass echo space, double quote, directory
>
> **[2:30](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/execute-compound-commands?u=76281980&t=150)** creation failed double quote. In this example it attempts to make new folder to and if successful it CDs into it. If it's not successful, it echoes the error message, hit enter. Because we're still in slash and can't create a folder, we should have gotten an error message. Now let's go back to our home directory, type in CD and hit enter. Type in clear and then bring your line back and hit Enter again. This time we should see a prompt change as the director was created and we changed into it. These are not the only ways of creating compound commands. If you want to execute multiple commands and have them run inside a current shell, we'll use the curly braces. To demonstrate this, let's use the echo command twice, type in clear and then type in left curly bracket, space echo space, double quote, hi, double quote, space semi colon space echo space double quote there double quote, space, semi colon, right curly bracket and hit enter. The output of this is similar to what we did earlier. You might be thinking this is doing exactly the same thing, but let me explain why it isn't. Let's redirect our first compound command example to a file type in echo space double quote, Hi, double quote space semicolon space echo space double quote there. double quote space greater than Hello dot txt and hit enter. Note that hi ended up on the screen. Now let's view the text file. Type in cat space Hello dot txt and hit enter Only the words there is in the file,
>
> **[4:02](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/execute-compound-commands?u=76281980&t=242)** only the second echo got redirected to the text file. Now let's use curly braces, bring your line back and add in the left curly brace. And then after there, add in a semicolon and a right curly brace, and then hit enter again. Now we'll view the txt file again, type in cat space Hello dot txt and hit enter. We can see that both lines are in this file, the curly braces force bash to process all commands inside the braces as one command. This would allow us to pipe or redirect the output. Now let's do the same thing but replace the curly braces with parenthesis, bring your line back again and change the curly braces to a left parentheses and a right parentheses and hit Enter again. And once again we'll view the text file we get the same thing as using curly braces. The difference between curly braces and parentheses is that the latter runs the commands on a sub shell. The curly braces run the commands in the same shell. Since the commands are running in a sub shell, they'll have different variable scope. To show this type in clear. Then we'll type in a couple of different commands, a equals zero semicolon. And for our second group, left parentheses, a equals 10, semi colon space echo space double quote, I N equals dollars sign a double quote, right parentheses, semi colon. For our last group, echo space, double quote, out, equals dollar sign a double quote. In the first command a is assigned to zero,
>
> **[5:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/execute-compound-commands?u=76281980&t=335)** then inside the sub shell is assigned to 10. Then we echo inside the sub shell, and again outside the sub shell, hit enter. You can see from the output that these are actually two variables named a one in the main shell and one in the sub shell and they hold different values. If you want to have separate variable space, use parentheses for a sub show. If you just want to group commands that execute as one thing use the curly braces

> [!info]- Semantic Content
>
> **CLI Commands:** cd (4), make (2), cat (2), ls (1), grep (1)
> **Analogies:** for instance (2), similar to (1)
> **Warnings:** note that (2)
> **Cross-References:** go back to (1)
> **Tools:** bash (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)
> **Speakers:** - [narrator] (1)

#### [Use Command and Variable substitution](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-command-and-variable-substitution?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-command-and-variable-substitution?u=76281980&t=0)** - [Instructor] We can insert a variable into a text string or command line and have the variable value substituted for the variable name. For instance, in a terminal type, echo space, double quote, my space name, space is space dollar sign, capital U,S,E,R,
>
> **[0:21](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-command-and-variable-substitution?u=76281980&t=21)** double quote and hit enter. This outputs the text My name is followed by the value in the user variable. As we've seen elsewhere in this course, we need to be careful with quoting. In order to have variable substitution, we need to use double quotes. This echo command is pretty basic. Now let's use a command to process the value of a variable. To set this up, let's create a couple of variables. In a terminal type in pdir, equals double quote slash tmp slash files slash today, double quote and hit enter. Let's create a second variable called fname. fname equals double quote, report, double quote and hit enter. variable names can be uppercase or lowercase. Most system variables are uppercase. Now let's use these variables in a command type in mkdir space, dash p space dollar sign pdir and hit enter. Mkdir dash makes the directory and any parent directories if needed. Let's verify this with ls dash d, type in ls space dash d space dollar sign pdir and hit enter. We need the dash d option so ls knows to list the directory and not the contents. Now let's use touch to create a file using variables. Type in touch space dollar sign pdir slash, dollar sign fname and hit enter. This expands out to touch space slash tmp slash files slash today slash report.
>
> **[1:57](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-command-and-variable-substitution?u=76281980&t=117)** verify this with ls type in ls space dash l space dollar sign pdir slash dollar sign fname and hit enter. This shows that the slash tmp slash files slash today slash report file exists. Things get a bit trickier when you add additional text. Let's say I wanted to create a file called report undisclosed January. I may type in touch space, dollar sign pdir slash, dollar sign fname underscore January, and then I would verify it with ls dollar sign pdir slash, dollar sign fname underscore January and hit enter. Not only did this not list our file named report underscore January, it listed the parent directory. The reason for this is that we thought we were adding underscore January to the value of the variable and bash added it to the variable name. It thinks we wanted to create a file with the path slash tmp slash files slash today followed by the value of the variable called dollar sign fname underscore January. The problem is, this variable doesn't exist yet so it's empty. In our case we need to preserve the variables name. To preserve the variables name we can place curly braces around it, type in touch space, dollar sign pdir slash, dollar sign left curly bracket, fname, right curly bracket, underscore January and hit enter.
>
> **[3:30](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-command-and-variable-substitution?u=76281980&t=210)** Now let's verify this with ls dash l type in ls dash l space, dollar sign pdir slash dollar sign left curly bracket fname, right curly bracket underscore January and hit enter. And we see that it created the file. Placing quotes around our strings and braces around variable names makes bash variables much more reliable. For instance, typing in ls space dash l space, double quote dollar sign left curly bracket pdir right curly bracket slash, dollar sign left curly bracket, fname, right curly bracket underscore January double quote ends up being very reliable. We can also insert commands into our strings and have them executed using command substitution. When the command is executed, the standard output of the command is inserted in its place. Let's type in clear to clear our screen. And then I'll give you an example. Type in echo space, double quote. My Space name space is space dollar sign left parentheses, Who am I one [[Microsoft Word|word]], right parentheses, double quote and hit enter. This is a very simple example that runs the Who am I command first, and then substitutes the output in place of the command. When we do command substitution, you can either surround the command with backticks or dollar sign and parentheses as we've done, An example using backticks would be echo space,
>
> **[5:04](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-command-and-variable-substitution?u=76281980&t=304)** double quote, my Space namespace is space backtick who am I backtick, double quote. This does the same thing as the dollar sign parentheses, but it isn't as easy to nest. To do nesting with dollar sign and parentheses, type in echo space, double quote, permissions for find are space, dollar sign, left parentheses, ls space dash l, space, dollar sign, left parentheses, which space find right parentheses, right parentheses, double quote, hit enter. Let's analyze what is happening here. The output of which find shows which directory the find command resides which is slash bin slash find. That output is then inserted into the outer command substitution and executed. The outer command substitution uses the ls command to get the permissions of the find command, which is then inserted into the whole line and executed again, resulting in a sentence consisting of a string of characters followed by the permissions for find. You can nest the command substitution easily with dollar sign and parentheses combination.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **CLI Commands:** ls (11), find (7), mkdir (2)
> **Tools:** terminal (2), bash (2), command line (1)
> **Analogies:** for instance (2)
> **Definitions:** is a  (1)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)

#### [Conditional flow with if, then if](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/conditional-flow-with-if-then-if?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/conditional-flow-with-if-then-if?u=76281980&t=0)** - [ Instructor] Conditionals in Bash are fairly similar to other languages. The syntax looks like this. It can be shortened a bit by placing the then on the same line as the if, separated by a semicolon. If we want to include an else section, we can. We can also check for more than one condition. Note that it checks the condition each time it does an if or elif. This can be slow for a lot of conditions. The condition can be a test of some sort, or it can be the successful execution of a command. This is one thing that separates Bash from other languages. Bash is very tightly integrated with the operating system. In this example, we search etc password for the [[Microsoft Word|word]] root using grep. If it's found in grep, will return a zero to the if conditional and the first code block will run. If grep doesn't find root, then it will return a non-zero number and the second code block will execute. We can negate this result as well. To run the first code block if the command doesn't succeed, use an exclamation point before the command. This is good for error messages. For instance, we could attempt to copy a file, if it fails, then output a message on the screen. We will cover the actual test we can do as an if conditional in another video. But let's talk for a moment about single square brackets versus double square brackets. Single square brackets are POSIX compliant. They work with older shells including Bourne, and are commands that then test the condition. The first single square bracket is really the built in test command. The second square bracket is an argument to the first. You can use the /usr/bin/test command and get the same results.
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/conditional-flow-with-if-then-if?u=76281980&t=95)** With single square brackets, file name expansion and word splitting happening. This may not be what you want. Parameter expansion also happens. Ampersand ampersand, pipe pipe, less than and greater name operators, get interpreted by the shell, since a square bracket test isn't a statement but rather a command. The shell interprets these special characters for its own use. Lastly, quoting is required to preserve variable values. The double square brackets are not commands but rather built into the Bash shell as keywords. They're not POSIX compliant, and are specific to bash and ksh. They do not work with older shells. No filename expansion happens between brackets, this may be good. No word splitting happens between brackets, this is usually good. Parameter expansion does happen between brackets as well as command substitution. They support ampersand ampersand, pipe pipe, less than and greater name operators. These operators are used for Boolean logic to do more complex tests as well as text order comparisons. They also support automatic arithmetic evaluation of octal and hexadecimal. In addition, they support extended regular expression matches. This is really the killer function. Regex conditions bring a great deal of power to bash conditionals. Lastly, quoting is not required. Although I still quote out of habit, you can get away with not quoting with double square brackets as the area between the brackets is protected. I recommend in almost every case to use double square brackets. There are more weird conditions where single brackets break than we have time to go into here.
>
> **[3:07](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/conditional-flow-with-if-then-if?u=76281980&t=187)** Double brackets are more reliable, and consistent, and are the future in most cases.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (3)
> **Tools:** bash (6)
> **CLI Commands:** grep (3), find (1)
> **Env Vars:** posix (2)
> **Analogies:** similar to (1), for instance (1)
> **Definitions:** is an  (1)
> **Warnings:** note that (1)
> **Prerequisites:** required to (1)

#### [Conditional flow with case](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/conditional-flow-with-case?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/conditional-flow-with-case?u=76281980&t=0)** - [Instructor] Generally, we use if conditionals and Bash. However, in cases where we're matching more than one pattern with an if, then else-if conditional block it may be more efficient to use a case statement. The case statement evaluates the condition one time and acts accordingly. This statement evaluates the age variable. If the value is one through nine numerically, then it executes the Action List to the right of the parentheses until the double semicolons which is the Action List Terminator. The characters to the left of the parenthesis make up a glob. You can do pattern matching in the same manner as you would on the command line using wildcards, character sets, and character classes. However, you cannot use regular expressions which is unfortunate. The equivalent if conditional would evaluate age up to four times to do the same thing so this case statement would be much faster. The last condition which is asterisk catches everything that doesn't match above. This would be similar to the else portion of an if then else conditional block. Also, the last condition doesn't need to be terminated by double semicolons as a statement ends anyway. The default behavior of a case statement with double semicolons as Action List Terminators is to execute only the list associated with the first matching pattern and then the case statement ends. For example, if AGE=5 then both the first and second patterns would match but only the first Action List would get executed because the case would end up to the first match. However, in Bash 4, there are two new Action List Terminators double semicolon Ampersand and semicolon Ampersand I've changed the example to reflect this. The double semicolon Ampersand Action List Terminator
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/conditional-flow-with-case?u=76281980&t=95)** is similar to double semicolons but instead of exiting after the first magic continues to process matches. If it finds a second pattern that match it executes that Action List as well. Now, if AGE=5, the first pattern would match and the Action List would get executed since the Action List Terminator is double semicolon Ampersand then the case statement would continue on and match against the second pattern and the second Action List would get executed as well. This semicolon Ampersand Action List Terminator automatically executes the next list without evaluating its pattern. So if age was between 10 and 19, the third pattern would match and the Action List for that match would run but because we have the semicolon Ampersand Action List Terminator the next Action List would also be executed.

> [!info]- Semantic Content
>
> **Tools:** bash (2), command line (1)
> **Analogies:** similar to (2), for example (1)
> **Env Vars:** age (2)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Iterate with for loops](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/iterate-with-for-loops?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/iterate-with-for-loops?u=76281980&t=0)** - [Narrator] Whenever you want to loop through a finite list of items, you may want to use a for loop. The syntax for the for loop looks like this. We start with a list of items to loop through, as we iterate through them, they're assigned to a variable. In this example, the variables name is item. Inside the loop, we do something with the contents of the variable. The list for the for loop can come from any number of sources. It can be a static list that we create such as one, two, three, four, five, or a list of names. If your looking for a list of sequential numbers, we can create a dynamically. In the past, we'd use a sequence command for this. And we'd use command substitution to get it to run. However, it's better to have Bash create the list using expansion. This way we're not spawning a new shell and executing commands. It's more reliable and it's faster. There are times though when we will use command substitution to create the list. However, be very careful because the for loop uses the IFS or internal field separator to [[Microsoft Word|word]] split. The IFS by default splits on blank spaces so if a feel name has a space in it, your loop will break. You can change the IFS temporarily while you're looping. When you're done, you will want to reset the IFS to what it was before or other commands will break. This does work, but is a bit messy. Perhaps a better option will be to use while read which we'll talk about in a later video. Another possibility is to loop through the values of an array. Note that we're not putting quotes around the list. This is because quoting it will result in there only being one item in the list.
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/iterate-with-for-loops?u=76281980&t=94)** In this example, we're looping through items in an indexed array. Since the array in indexed, it would be better to loop through the index instead. Let me explain what this does. First we create the array. Then we get the number of array items using the hash array left square bracket at right square bracket syntax. Now we do math, do subtract one since the array count started from one and we will be looping starting from zero. Next we use a sequence command to create our list from to number of array items minus one. Now each iteration, that index number's assigned to the variable named 'i' and we use that to access the array item specifically. Earlier I mentioned that we don't use a sequence command anymore because Bash has parameter expansion. You may be wondering why we can't this example a lot simpler by using parameter expansion. The problem is that parameter expansion happens before math and variable expansion. Our only choice is to run a sub shell with the sequence command. It is possible to get tricky with the eval command to have Bash create the sequence, but then we defeat the purpose of trying to make it simple.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **Env Vars:** ifs (4)
> **Tools:** bash (3)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)
> **Warnings:** note that (1)
> **Speakers:** - [narrator] (1)

#### [Iterate with while loops](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/iterate-with-while-loops?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/iterate-with-while-loops?u=76281980&t=0)** - [Instructor] The for loop is designed to loop through a finite number of items and when it gets to the end, it exits. It is possible to have a condition inside the for loop that will break out of the loop even before it runs out of items. A better idea is to use a loop with a condition built in. For this we have the while loop and until loops. The only difference is that the while loop will iterate while a condition is true and the until loop will iterate until the condition is true. I usually use a while loop over the until loop. Let's take a look at the syntax of the while loop. The condition inside the single square brackets follows the same rules as an if conditional inside of single square brackets, which means you can match on wildcards, but not regular expressions. The simplest while loop we can make is the infinite loop by replacing the condition with the [[Microsoft Word|word]] true. This loop will keep iterating forever, unless the user kills the script or the condition inside of the if condition double square brackets is true and the break command runs. By nesting an if conditional inside of a while true loop, we can take advantage of the if conditional's double square brackets in regular expressions. Let's take a look at a while loop with a condition. The variable I is set to zero, the loop continues as long as the value of I is less than four. Each time the loop iterates the value of I is increased. Once it reaches four, the loop exits. One major advantage of using a while read loop over a for loop is that it handles different types of text better. This is the example that we used earlier in this course. Any text file provided on the command line will be read by the while read loop and it will handle blank spaces just fine.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **CLI Commands:** make (1)
> **Cross-References:** earlier in (1)
> **Tools:** command line (1)
> **Speakers:** - [instructor] (1)

#### [Using return codes](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-return-codes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-return-codes?u=76281980&t=0)** - [Instructor] Every Bash command or function returns a code when run through signal to the OS whether the process was successful or not. If the operation succeeds, it will return a zero return code. If it fails, it will return a nonzero value from one to 255. The reason that zero is success and one through 255 are failures is that our process can fail for many reasons. Using many numbers for failures allows Bash to give more granular output for troubleshooting reasons. The return code is assigned to the question mark variable. We check the value just like we would any variable. Let's see how it works. Let's go to a terminal and type in ls space slash etc slash passwd and hit Enter. Now read the question mark variable echo do an echo space dollar sign question mark and hit Enter. Since our ls command was successful, the return code is zero. Now let's read a file that does not exist. Type in cat space slash etc slash password. Make sure you spell out password. This file doesn't exist, and hit Enter. Now list the return code again, by bringing your line back. we can see that the return code is one as it failed. Now let's list the same file that doesn't exist using ls, type an ls space slash etc slash password, and hit Enter and list the return code again, this time we get a return code of two. The reason we got two different codes when trying to access a file that doesn't exist is we used two different commands which had different codes. Now let's type a command that does not exist, type in catfile, one [[Microsoft Word|word]] and hit Enter
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-return-codes?u=76281980&t=94)** and list the return code again. This time the value is 127. Now check the return code once more. This time is zero because the act of checking the status actually change the return value. The echo command succeeded so its return value was put in the question mark variable replacing the previous one. In scripts, you want to check the return code right away and maybe even store it in a variable that will not change. There is a fairly standard return code for Bash commands. Error code one is a catch all for general errors. Error code is a misuse of shell built-ins. Error code 126 is a command that cannot be executed. This may be for various reasons, including SC [[Linux]] permission denials. Code 127 is command not found which we just saw. Code 128, invalid argument to the exit statement. Code 128 plus n is a fatal error signal where n is the signal. Code 130 shows a script that was terminated by Control + C and code 255 or higher as an exit status out of range error. Many man pages tell you what the return codes are. For instance, let's check the man page for the ls command. Type in clear and then type in man space ls and hit Enter. Now go clear to the bottom. Here we see that ls will use several codes depending not all man pages list this, so be aware. Press Q to quit. Now let's make a script that uses the return code in a conditional. We'll call the script check dot sh and I'm going to make it in the bin directory my user's home directory. To do so, type in vi space tilde slash bin slash check dot sh and hit Enter.
>
> **[3:09](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-return-codes?u=76281980&t=189)** Go into insert mode by pressing the I key and then add hash bin slash bin slash bash on the top line. Now for our code, type in cat space database dot txt space ampersand greater than space slash dev, base slash dev slash null, I'm redirecting all output from the cat command two slash dev slash null. So it will not be displayed. I am most interested in the return value for this exercise. For our conditional type in if space double square bracket, space, dollar sign question mark, that's the variable space dash eq space, zero space, double right square bracket, space semi colon, then I hit Enter, and tab, have an echo space double quote, able to read the database, double quote, and hit Enter type in else, hit Enter and then tab again. Echo, space double quote, unable to read the database double quote, space greater than ampersand two the redirect at the end sends the text to standard error instead of standard out so we can redirect error output from our script to a different log new line fi. This script attempts to cat the database at txt file and then uses the return code to determine if it was successful or not. If the return code is zero, it prints able to read the database on the screen. If the return code is not zero, it prints unable to read the database to standard error. Note that if you're checking more than one value, you might want to use a case statement instead of multiple if conditions. If you only want to check the success,
>
> **[4:43](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-return-codes?u=76281980&t=283)** we can make this shorter let's merge the cat database dot txt line and the if conditional line. Go up to the cat database line and prepend it with if and then go to the end of the line and add semi colon then that will delete the next two lines. Now the if conditional runs the cat command and automatically acts on the return value, if it's successful, the first condition runs and if it's not the second runs. The code is cleaner to read and simpler to write, but takes away some of the granularity for processing return codes. You can also process return codes with while and until loops as they manage conditions. Function that we have right can also have return values. Let's put our code in a function and return success or fail values. Prepend the code with check underscore database left parentheses right parentheses, new line left curly bracket. Now go to your first echo statement. Add a new line after it add return space zero. Now go to your second echo line and add a new line add a new line saying return space one then go to the bottom of the code block and add a right curly bracket. Now for the calling conditional type in if space exclamation mark space check underscore database space semi colon then New line, tab, exit space one new line fi. These changes call the check database function using an if conditional just like if it were command. If it's successful to check database function returns zero. If it fails, it returns one back to the calling condition.
>
> **[6:16](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-return-codes?u=76281980&t=376)** Our calling condition only checks for the success of the function if it has not successful it exits with the value of one. In summary, if you want to return a value from a function, use the return keyword. If you want to return a value from a script, use the exit keyword.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1), [[Linux]] (1)
> **CLI Commands:** ls (7), cat (7), make (4)
> **Tools:** bash (4), terminal (1)
> **UI Navigation:** go to (5)
> **Definitions:** is a  (5)
> **Analogies:** just like (2), for instance (1)
> **Warnings:** be aware (1), note that (1)
> **Documentation:** man page (1)

#### [Create a monitor script](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/create-a-monitor-script?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/create-a-monitor-script?u=76281980&t=0)** - [Narrator] Now, we're going to put together a lot of things we've talked about by creating a script that monitors failed logins, and if the number is higher than a threshold, we'll email the administrator. This script is in the exercise files directory for this chapter. You can either load that script into VI to look at it or create it as I discuss the details. If you wanted the script to run automatically every day, you could create a cron job for it, place it in the etc/cron.daily directory, or even create systemd.timer and service units for it. To run it you'll need to elevate privileges using sudo. We'll start by discussing the first ten lines of the script, and then we'll follow up with lines 14 to the end. If you load this into VI you can turn on line numbering by pressing escape, colon, and typing set, space, number. On the first line we have our hash, bang, /bin/bash line to call the correct interpreter. On line three we get yesterday's date. Notice at the first argument, dash, dash, date equals yesterday, specifies the date in question, and then the second argument determines how to format the output. This'll be in month, day, year, format with the year being four digits, and each unit delimited by hyphens. On line four we specify which law we'll be parsing, and on line five we set the threshold of how many failed logins we're going to allow in one day before emailing the administrator. Line seven is the email address we send the report to every day. Line eight is the email address of the admin user that we only email if we've broken the threshold set above. Line nine is the report email subject line, which includes the date being analyzed. Line 10 specifies the email message file that we'll be adding text to later in the script.
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/create-a-monitor-script?u=76281980&t=95)** Lines 12 and 13 set the failed login counters that we'll use later. For line 14 we'll get the date again with the month and day, but without the year delimited by a space and not hyphens. We're doing this because we need it to match the format that the log file uses as it will be part of our search criteria. On line 15 we search the log file for authentication failures. The line has to start with a date and can be followed by any number of characters, but must include the words authentication failure. We're doing a case insensitive search and counting the number of results. On line 17 we do a similar search, but add EUID equals zero to the end. This will show a root login failures. Lines 19 through 21 add lines of text to the report including the date, the total number of failed logins, as well as the number of failed root logins. Lines 23 through 25 check to see how many failed root logins happened during the day. If it's higher than the threshold, it emails the report to the admin user. The last line emails the report to the other email address no matter how many results there were. This is not a perfect script, as I've tried to keep it short, but I'll leave it up to you to modify it and make it better. This same type of script could monitor anything. For instance, we could monitor how much space a user's home directory is taking up, their network data usage, their CPU usuage, or even monitor the firewall for certain types of packets. It could be adapted for many different situations.

> [!info]- Semantic Content
>
> **CLI Commands:** sudo (1), make (1)
> **Env Vars:** euid (1), cpu (1)
> **Cross-References:** later in (1)
> **Tools:** bash (1)
> **Exercise Files:** exercise files (1)
> **Analogies:** for instance (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [narrator] (1)


### 3. Install and Configure X11

[↑ Back to Table of Contents](#table-of-contents)

#### [X11 architecture](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/x11-architecture?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/x11-architecture?u=76281980&t=0)** - UNIC systems historically have used the X Windowing System to provide the constructs for graphical interface. X supports screens, drawing and moving of [[Windows]], as well as input devices such as mice and keyboards. The X Windowing System was created by MIT in 1984. Version 11 of the protocol was released in 1987 and has been at that version ever since. A free version of X was released in 1991, called XFree86. In 2004, XFree86 distributed code that conflicted with a GPL license, so it was forked into [X.org](https://X.org). Most [[Linux]] distributions switched to [X.org](https://X.org), and the last version of XFree86 was released in 2008. The development of XFree86 has stopped. When I talk about X in this video, assume I'm talking about the X Windowing System, XFree86, or [X.org](https://X.org). The architecture for all is the same. X Windows is network-aware by default. A service called the X server exists where the video card and input [[Hardware]] is. This means the X server is usually on the local work station. It handles input from the keyboard and mouse and outputs to the video card. The graphical applications that run are X clients. By default, the X clients talk through the local network with the X server, using the X protocol. Because the architecture is built from a client-server model, it's easy to have remote graphical applications. But where the client and server is in this situation is the opposite of what you might think. If I run an application such as Firefox on a remote host, the graphical interface
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/x11-architecture?u=76281980&t=93)** comes back to me on the local machine, but the application is running on the remote CPU. Remember that the server is where the input and video hardware is, so the X server is local to me, and the client application is remote. This is the opposite of most client-server relationships. X has some problems, though. It must remain backwards compatible, so the system has gotten quite complex in the last 40 years with multiple different rendering engines, driver systems, and blobs of code all over the place. As such, it's hard to make fast. Also, our needs have changed. 3D applications, such as games, have proven more difficult with X than with other systems. X was great for servers, but as desktop computers have gotten more common, we've needed a new system. The leading replacement for X is Wayland. Development for Wayland started in 2010. The first release was in 2012. Adoption has been slower than expected, but most distributions offer Wayland now. Wayland differs from X in that the architecture is simpler. There's no monolithic server in the middle. It doesn't handle drawing. It supports direct rendering by the client. [[Memory Management]], command scheduling and mode setting are in the kernel. Other functions are pushed into libraries, leaving Wayland much smaller and lighter. For backwards compatibility, Wayland supports having an X server as a client. And lastly, Wayland doesn't currently have networking ability, although it's been worked on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (2), [[Hardware]] (2), [[Linux]] (1), [[Memory Management]] (1)
> **Env Vars:** unic (1), mit (1), gpl (1), cpu (1)
> **URLs:** [x.org](https://x.org) (3)
> **Analogies:** such as (3)
> **CLI Commands:** make (1)
> **Versions:** version 11 (1)
> **Cross-References:** in the last (1)
> **Tools:** firefox (1)

#### [About desktop environments](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-desktop-environments?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-desktop-environments?u=76281980&t=0)** - [Instructor] With Linus, the graphical interface is just a set of applications running on the OS. This means we can change out the graphical interface and use a different one if we wish. On [[Linux]] a system called X [[Windows]] provides the ability to have a GUI including mouse pointer, keyboard and screens. On top of X-Windows will be a GUI Toolkit. Usually GTK or QT. The window manager and desktop environment use the toolkit to create the windows, launchers, toolbars and menus. It's also important to note that all applications work on all desktops. Choosing one desktop doesn't exclude using applications from another. However, applications for KDE might not look native on GNOME desktop and vice versa. There are many reasons why different desktop environments exist. Perhaps one desktop environment uses less memory or another is designed for a touch interface. It's important for us to find the desktop environment that fits our needs. Desktops may also include extra applications including [[Microsoft Word|word]] processors, spreadsheets, application launchers, configuration tools, and more. In this course, we will be using CentOS 7 Linux. The only officially supported desktop are GNOME 3 and KDE 4.10. Even though only two desktops are officially supported, we can install other desktop environments by adding third party software repositories. Let's begin by talking about GNOME 3, as its the default desktop on CentOS 7. GNOME 3 is part of the GNU Project, is stable, and has a fairly unique way of working. Overview mode was introduced with GNOME 3
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-desktop-environments?u=76281980&t=95)** and is a paradigm shift in workflow. GNOME doesn't even include an application menu by default, and instead relies on going to overview mode, and typing in the application's name. It does have an icon dock, for starting common applications. GNOME applications use the GDK3 windowing tool kit. The other officially supported desktop is KDE Plasma Workspaces, which is a more Windows like desktop environment with a traditional application menu. Some call KDE the most powerful desktop on Linux, and for multiple reasons. It has a semantics search solution and activities. Activities allow a user to switch between configurations quickly. KDE uses the QT toolkit, spelled Q-T, setting KDE applications a part from most other desktops. The next few desktop environments are not officially supported in CentOS, and as such we need to add third party software repositories to install them. MATE may be the most popular third party desktop for CentOS 7. This is because it is essentially GNOME 2. Some users didn't like the drastic changes made in GNOME 3, so they forked the old code and renamed it MATE. MATE is a simpler desktop with a traditional workflow. MATE uses the GTK2 toolkit. One of the problems with MATE is that it uses the older GTK2 libraries. Some developers were concerned that these libraries will become obsolete so they set out to recreate GNOME 2 using newer GTK3 libraries from GNOME 3. The name of this new desktop is Cinnamon.
>
> **[3:10](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-desktop-environments?u=76281980&t=190)** It was created by the Linux Mint Distribution to provide a modern alternative to GNOME 3 but with a more traditional workflow including an application dock and application menu. The last desktop environment we'll look at is Xfce. Xfce has a traditional workflow with an application menu. It's main claim to fame is that it uses less memory and CPU cycles than the other desktops that have more features.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (4), [[Windows]] (4), [[Microsoft Word|Word]] (1)
> **Env Vars:** gnome (12), kde (6), mate (5), gui (2), gtk2 (2)
> **Definitions:** is a  (3)
> **Prerequisites:** install (2)
> **CLI Commands:** find (1)
> **Versions:** 4.10 (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Assistive technologies](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/assistive-technologies?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/assistive-technologies?u=76281980&t=0)** - [Narrator] Most modern operating systems, including [[Linux]], support assistive technologies for users with hearing, speech, vision, or mobility impairments. Assistive technologies include applications for keyboard navigation, on-screen visual keyboards, screen readers, speech synthesizers, as well as visual and audio alerts. For those wanting easier to read and or higher contrast text we can change to a higher contrast view of the desktop, and or one with larger text. To do so, go to applications, system tools, and then settings. Now click on universal access. This dialog has many things we can change. Click on high contrast to switch themes. This makes all of the shading go away and even changes the icons of the desktop. We can also click on large text to make the fonts everywhere much larger. You may notice that an icon now appears in the title bar. Click on it, and there will be a menu of various assistive technologies, which can be turned on or off. You can also have this menu visible at all times, by turning it on with this dialog. If the pointer is hard to see, we can increase its size too. Click on [[Cursor]] size default, and click on a different size cursor. When done, choose the original cursor, close the dialog, and turn off high contrast, and large text. [[Zoom]] has some additional settings. To see them, click on it. In the resulting window, we can change how the system will zoom the screen. Click the on off slider button, and you should immediately be placed into zoom mode. Click it again to turn it off. We can change which portion of the screen is zoomed as well.
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/assistive-technologies?u=76281980&t=95)** Change screen part to top half, and then click on the on off slider again. We can see only the top half is being zoomed. Click the slider again to turn it off. The other two tabs involve turning on crosshairs for the cursor or color effects, both of which only work along with zoom. You can close this dialog now. Below zoom is the screen reader. The screen reader speaks the words on the screen wherever the mouse focuses. Turn it on, and click on some text or select a menu and it will read it to you. Under screen reader is sound keys. You can click on it, and in the resulting dialog you can turn it on. This will beep if the num lock or caps lock keys are pressed. For the hearing impaired, we can have a flash the window or screen on error. Click on visual alerts under hearing. Turn it on, and test it by clicking on test flash. And then turn it off, and close the dialog. GNOME includes an on-screen keyboard that you can use with a mouse. Click the screen keyboard slider to turn it on. It will look like nothing happened. Now go to the applications menu, click on activities overview, and click on the search box, and you'll see that the keyboard comes up. To make it go away, click on the down error on the right. Remember to turn it off now, or it may surprise you later. Normally when you hold a key down, it repeats the character until you let up. If you can't lift your finger fast enough, it will print more than one character. We can manage this by clicking repeat keys, where we can choose the delay and speed of repeat.
>
> **[3:09](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/assistive-technologies?u=76281980&t=189)** Close this dialog. Normally when you hold a key down, it repeats the character until you let up. If you can't lift your finger fast enough, it will print more than one character. We can manage this by clicking repeat keys, where we can choose the delay and speed of the repeat. Now click typing assist. Here we can manage sticky keys, slow keys, and bounce keys. Sticky keys help users who need to press multiple keys at the same time. For instance, to switch to a terminal we may need to press control, alt, and a function key all at the same time. Or to terminate a process we need to press control and C at the same time. Sticky keys allow the key to be locked in a state so a user can press control and then C one at a time for the same effect. Slow keys helps users who may have a problem accidentally pressing keys. It adds a delay between when the key is pressed, and when the action is accepted. Lastly, bounce keys adds a small delay between key presses when the same key is pressed more than once, ignoring multiple repeat presses. Now close this dialog. The next item down is mouse keys. This allows you to use the directional keys to move the pointer instead of the mouse. Now click on click assist. This will allow you to configure a double click just by holding down the mouse button. It also allows you to click by hovering the mouse over an item. Now close the dialog. The last thing in the universal access settings window is the double click delay. This tests the time between clicks to determine what is a double click, and what are two simultaneous single clicks.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Zoom]] (5), [[Linux]] (1)
> **UI Navigation:** click on (15), go to (2), switch to (1)
> **CLI Commands:** make (2)
> **Best Practices:** remember to (1), the key is (1)
> **Env Vars:** gnome (1)
> **Tools:** terminal (1)
> **Definitions:** is a  (1)
> **Analogies:** for instance (1)

#### [Override specific aspects of Xorg configuration](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/override-specific-aspects-of-xorg-configuration?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/override-specific-aspects-of-xorg-configuration?u=76281980&t=0)** - [Instructor] In the past with x386 we had to configure X [[Windows]] manually by editing the configuration file. When Xorg for was released it's ability to auto-configure the graphical interface and input settings improved and, in many cases, we no longer have a configuration file at all. However, we can still manually configure it if you wish, by creating a /etc/x11/xorg.conf file. There are multiple configuration sections in this file to handle different settings, different sections are then tied together to represent one complete configuration of devices. The file section is used to specify path names required by the server. The ServerFlags action is used to specify some global Xorg server options. The module section is used to specify which Xorg server module should be loaded. This section is ignored when the Xorg server is built in static form. The type of modules normally loaded in this section are Xorg server extension modules. The extension section, is used to specify which X11 protocol extensions should be enabled or disabled. The config file may have multiple InputDevice sections. Recent acts serve as employee HAL or udev back ins for input device enumeration and input hot plugging. If hot plugging is enabled, input device sections using mouse, keyboard, and VMMouse drivers will be ignored. If hot plugging is disabled we'd manually configure our mice and keyboards here. The config file may have multiple input class sections. The sections are used to provide configurations
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/override-specific-aspects-of-xorg-configuration?u=76281980&t=93)** for entire classes of input devices as opposed to setting a specific device. We can match on [[Microsoft Products|products]], vendors, drivers, layouts, et cetera. The config file may have multiple device sections. There must be at least one for the video card being used. The identifier entry for the section specifies a unique name for this graphic device. The driver's section specifies the name of the driver to use. A device section is considered active if it is referenced by an active screen section which we'll get to in a moment. The config file may have multiple monitor sections, which describe the capabilities of our video monitors including resolution and scan frequencies. The config file may have multiple mode sections. These sections provide a way of defining sets of video modes independently of the monitor sections. The config file may have multiple screen sections there must be at least one for the screen being used. A screen represents the binding of a graphics device and a monitor. A screen section is considered active if it's referenced by an active ServerLayout section. A ServerLayout represents the binding of one or more screens, and one or more input devices to form a complete configuration. To create the ServerLayout we'd have a device with an identifier and driver. We'll combine the device with a monitor to form a screen. We'll also have an input device which specifies a mouse and keyboard. We'll then create a complete configuration by combining the input device with a screen into the ServerLayout. Although we can create
>
> **[3:05](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/override-specific-aspects-of-xorg-configuration?u=76281980&t=185)** an entire Xorg configuration file manually, it's not really recommended. Any section that we leave out of the config will be auto-configured by Xorg. A better practice, is to override parts of the config by placing pieces of the configuration in /etc/x11/xorg.conf.d Let's go to that directory and list it. In a terminal type in cd/etc/ upper case X11/xorg.conf.d/ and hit enter. Type in LS. We see a filename 00-keyboard.conf. Let's take a look at it with cat, type in cat space 00-keyboard.conf and hit enter. We see that as defining an input class and specifying the keyboard layout as "US" we could override this by editing this file. We can override any of the Xorg settings using this same method.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (1), [[Microsoft Products|Products]] (1)
> **CLI Commands:** cat (2), cd (1), ls (1)
> **Prerequisites:** configure (4)
> **Env Vars:** x11 (2), hal (1)
> **Tools:** terminal (1)
> **UI Navigation:** go to (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [instructor] (1)

#### [About remote desktop protocols](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-remote-desktop-protocols?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-remote-desktop-protocols?u=76281980&t=0)** - [Instructor] [[Linux]] operating systems are designed with remote access built in. Most often that is via the SSH command line. However, there are protocols to allow remote GUI access, as well. X [[Windows]] is the de facto standard for remote desktop on Unix and Linux systems plus there is VNC, or Virtual Network Computing, RDP, or Remote Desktop Protocol, a protocol created by [[Microsoft]] and a new protocol named SPICE. In most cases remote GUI access is accomplished by using an application on the client which connects to a service on the Remote Host via a remote desktop protocol. The Remote Host updates the display on the Local Host. The Local Host then sends keyboard or mouse events back across the network to the Remote Host. The Remote Host updates the Framebuffer based on the results of the mouse and keyboard actions. X Windows, the legacy GUI system used by Linux, supports remote desktop by default. X Windows is the de facto standard for graphical desktops on Linux although Wayland is replacing it. With X Windows is a client which resides where the application is running and a server where the video card and input devices are. However, it only works when X Windows is installed on both a client and server which in most cases is a Linux or Unix client connecting to a Linux or Unix server. In a local, non-remote environment, the client and server are on the same host. The X Client communicates with the X Server to display its desktop and handle input devices. In a networked environment the Client moves to the Remote Host and the server resides on the local one where the video card and input devices are.
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-remote-desktop-protocols?u=76281980&t=93)** This may seem opposite of what you'd expect as servers are usually remote to us. The X Protocol is fairly efficient. Because X supports drawing objects, not just bitmap data it supports objects such as screens, windows, pointers and mouse and keyboard input. It supports object-based screen updates. It can do whole remote desktops or just export single applications. For security it can be tunneled through SSH easily. NX, a third-party extension, provides extra features that improve X including automatic encryption provided by SSH, bitmap compression, X Windows messages compression, session management and local data caching. The end result is greatly improved speed and usability and X does not, however, support 3D desktops like GNOME 3 or [[Unity]] which calls into question its future in Linux. For systems that don't support X Windows let's take a closer look at other remote desktop protocols such as VNC, RDP and SPICE. VNC, or Virtual Network Computing, was created at the Olivetti & Oracle Research Lab. In 2002 it was bought by AT&T and the lab was shut down. The developers left and started RealVNC. VNC uses the concept of a remote Frame Buffer. It transmits the keyboard or mouse events from one computer to another relaying the graphic screen updates back in the other direction over the network. It essentially transmits a photo of the screen. As such, it's simple to implement from a programming standpoint. The default RFB protocol is not secure
>
> **[3:06](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-remote-desktop-protocols?u=76281980&t=186)** and transmits passwords in plain text. Also the base protocol isn't very fast and is fairly bandwidth heavy. Other versions of VNC have been released to address some of these issues like UltraVNC and the commercial version of RealVNC which support encryption. TightVNC recommends using SSH or VPN to tunnel all traffic instead of including encryption. TightVNC also includes extensions to compress the graphics data lessening bandwidth load and improving speed. VNC is platform-dependent. There are clients and servers for many GUI-based operating systems and for [[Java]]. Popular uses for this technology include remote [[Technical Support]] and accessing files remotely. With VNC each connection will be to a different VNC server port. If we had two users who want access to the desktops remotely we need to start two servers on the Remote Host running on two different network ports, one for each user. RDP, or Remote Desktop Protocol, is a proprietary protocol developed by Microsoft for doing remote desktop. RDP Version 10 supports 32-bit color space, 128-bit encryption, audio and file system redirection, printer, serial, and parallel port redirection, USB port redirection, shared clipboard between the local and remote systems, seamless windows in that you can send a window across instead of the entire desktop, multiple monitor support and virtualized GPU support. RDP clients exist for many different operating systems besides Windows including Linux, Unix, macOS, iOS, [[Android]] and others.
>
> **[4:41](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-remote-desktop-protocols?u=76281980&t=281)** RDP used to be called Terminal Services for Windows and is now called Remote Desktop Connection. RDP servers exist for Windows operating systems, Unix, macOS. There's also an open-source RDP server called xrdp for open-source operating systems. SPICE is the Simple Protocol for Independent Computing Environments. SPICE is a remote desktop display system built for virtual environments which allows users to view a computing desktop environment remotely. In 2007 Qumranet developed SPICE as a closed-source protocol. In 2008 Red Hat purchased Qumranet and open-sourced it. It supports TLS tunnels for security and various types of authentication including RSA and SASL which enables other OS-supported types like Kerberos, is used in the Qemu and KVM virtualization stack. And there's XSPICE for standalone remote desktop situations.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (12), [[Linux]] (8), [[Microsoft]] (2), [[Unity]] (1), [[Java]] (1)
> **Env Vars:** vnc (8), rdp (8), spice (5), ssh (4), gui (4)
> **CLI Commands:** ssh (4)
> **Definitions:** is a  (4)
> **Code Identifiers:** macos (2), ios (1)
> **Tools:** command line (1), terminal (1)
> **Analogies:** such as (2)
> **Versions:** version 10 (1)

#### [Display applications on remote X servers](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/display-applications-on-remote-x-servers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/display-applications-on-remote-x-servers?u=76281980&t=0)** - [Instructor] It's fairly easy to utilize remote desktop using X [[Windows]] on [[Linux]]. For this exercise, you'll need to have both of your CentOS VM's booted up. In my case rhhost1 will be the local host, and rhhost2 to will be the remote host. Make sure you changed your VM network settings over to bridge in the CentOS 7 lab setup video, at the beginning of this course, or this will likely not work. For remote desktop using X Windows, the system on which the application is being run, must be configured to allow ssh access. Remember that the application runs on the remote CPU and it's Gooey is brought back for a localhost. Before we can run an application remotely, we need to check the remote host ssh [[Server Configuration]] file. This will allow us to log into the remote host, then start a Gooey application and have the graphical interface, come back through the SSH tunnel. There are some settings that need to be in place, to allow this. To check, log into your rhhost2 VM, Open a terminal and then load the SSH config file into an editor to do so type in sudo space vi space slash etc slash ssh slash sshd underscore config and hit enter. Type in your password of prompted. Now verify that the X11 forwarding item is set to yes. Search for it by typing in forward slash, capital X eleven, capital F and hit enter. If there's a set to no, you'll want to change it now. Also, take note of the next line which says X eleven display offset which is set to ten. We'll look at the effect of this later. On mine, it's commented out with the value included.
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/display-applications-on-remote-x-servers?u=76281980&t=94)** This means that it's the default value for the setting. If you've changed the file, save and exit by pressing escape colon X and hitting enter. Now let's restart the SSHD service. Type in sudo space systems ctl, space restart space sshd and hit enter. Also while we're here, let's check our IP address. Type in clear and then type in IP space eddr and hit enter. I can see that my IP address is 192.168.2.248. Take note of the address and then go to your rhhost1 VM. To run single applications on rhhost2 and have the graphical interface come back to rhhost1, all I have to do is SSH into rhhost2 with a dash capital X option. This tells SSH to forward the applications Gooey interface back through the tunnel. In the terminal, type in ssh space dash capital X space and the IP address of your rhhost2 VM. Minus 192.168.2.248. Type in yes to accept the fingerprint, and then login. Now to test which display applications will run on, check the display variable. Type in clear then type in echo space dollar sign, upper case DISPLAY and hit enter. It should say localhost colon 10.0. local host the host name of the computer, where the X server runs, which is the local computer. Hen is a sequence number, it can be varied if there are multiple displays
>
> **[3:07](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/display-applications-on-remote-x-servers?u=76281980&t=187)** connected to one computer. This is the offset number we noted in the configuration file. And zero is the screen number. Now run Gooey application by typing in its name. For instance gnome dash system dash monitor, and hit enter. The gnome system monitor is running on rhhost2, but the Gooey is on rhhost, this would allow us the crhhost2 system resource usage graphically. With a bit more work, it's possible to bring the entire desktop back to rhhost1.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (2), [[Linux]] (1), [[Server Configuration]] (1)
> **CLI Commands:** ssh (8), sudo (2), make (1)
> **Env Vars:** ssh (4), cpu (1), x11 (1), sshd (1), display (1)
> **Versions:** 192.168.2 (2), 10.0 (1)
> **Tools:** terminal (2)
> **Definitions:** means that (1), is a  (1)
> **Prerequisites:** you'll need (1), setup (1)
> **UI Navigation:** go to (1)


### 4. Manage User and Group Accounts

[↑ Back to Table of Contents](#table-of-contents)

#### [Add and removing users](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/add-and-removing-users?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/add-and-removing-users?u=76281980&t=0)** - [Instructor] It's good to know which files to edit in order to manage user account settings on [[Linux]]. User accounts are stored in /etc/password, passwords and account aging are stored in /etc/shadow, and other account defaults are stored in /etc/login.defs and /etc/default/useradd. This last file also specifies our skeleton directory. The skeleton directory contains the files that are copied to each new user's home directory automatically. We'll observe this in a moment. The process of creating users in Linux is pretty straightforward. Let's go to our terminal and run useradd. Type in sudo useradd and hit Enter. Here we see the many options for useradd, including specifying the user's home directory, user ID, primary group ID, supplemental groups, and shell. Thanks to the stored defaults, we don't have to specify any options to create a user. Any options that we leave out are taken from the system defaults. Now, let's create a new user named bob. Type in clear, and then type in sudo useradd bob and hit Enter. We can verify that this user exists by viewing the etc password file with cat. Type in cat /etc/passwd and hit Enter. The very last line is the account information for the new user named bob. The fourth column is the user's primary group ID. In this case, bob's ID is 1001. We can cross-reference this number in the etc group file. Let's cat /etc/group. Type in cat /etc/group and hit Enter. We see that bob's primary group is also named bob. In Red Hat-based distributions,
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/add-and-removing-users?u=76281980&t=94)** the primary group is created automatically. The last thing we'll do is view the password and aging information in the etc shadow file. We'll need to elevate privileges using sudo for this, as this is where the hashed passwords are stored. Type in sudo cat /etc/shadow and hit Enter. The last line, again, belongs to the new user named bob. We can see that the second column, where the password should be, has two exclamation points. This means the password hasn't been set yet, and thus, bob can't log in. Let's set the password using the password command and view the etc shadow file again. Type in clear, and then type in sudo passwd bob and hit Enter, and then enter bob's password twice. And once that's done, view the shadow file again. Type in sudo cat /etc/shadow and hit Enter. We now see that the bob user has an encoded password in the second field. Now, let's take a look at /home for home directories. Type in clear, and then type in ls /home and hit Enter. We see the bob user's home directory is present. Type in sudo ls -la /home/bob and hit Enter. The files we see were copied from the etc skel directory when we created the user. These are the skeleton files. Deleting users is pretty straightforward. If you'd like to delete the users but keep their home directories and their files, type in sudo userdel bob and hit Enter. However, if you want to get rid of the bob account and bob's home directory, we'd want to type in sudo userdel -r bob. We can cat the etc password file
>
> **[3:09](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/add-and-removing-users?u=76281980&t=189)** to verify the account is gone. Type in clear, and then type in cat /etc/passwd and hit Enter, and we don't see it in the list. If we wanted to verify that bob's home directory has been removed, we're going to type in clear, and then type in ls /home and hit Enter. In our case, it has not, because we didn't provide the -r option. So let's manually delete it. Type in sudo rm -Rf /home/bob and hit Enter, and that should take care of it. Now, let's add a new user that we'll use later in the course. This new user's name will be user1, and we'll also give them a password and place them in the wheel group, so they have admin privileges. Type in clear, then type in sudo useradd user1 and hit Enter. Now, give user1 a password. Type in sudo passwd user1 and hit Enter. Now type in the password twice. Once you've typed in the password, add user1 to the wheel group. Type in sudo usermod -a for append, uppercase G for a supplemental group, wheel user1, and hit Enter. Now user1 should be able to use sudo with admin privileges. Now, let's verify that user1 is in the wheel group. Type in cat /etc/group and hit Enter. Scroll up until you get to the wheel group. You can see that user1 is listed. To view more useradd options, check the man page by typing in man useradd. Be sure to add user1 on rhos2 as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (2)
> **CLI Commands:** sudo (14), cat (9), ls (3), rm (1)
> **UI Navigation:** go to (1), scroll up (1)
> **Cross-References:** later in (1)
> **Documentation:** man page (1)
> **Tools:** terminal (1)
> **Speakers:** - [instructor] (1)

#### [Modify users](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/modify-users?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/modify-users?u=76281980&t=0)** - [Narrator] In order to change a user's account settings after they've been created, we'll need to use the user mod command. Most of the SIN text for user mod is identical to that of user add. Let's discuss a few of user mods more common options. Dash D lets you change the user's home directory. We might use this if we manually move their home directory and need to update etsy password. Dash G and dash U options allow us to change the primary group ID as well as the user's user ID. Dash capital G is for adding users to supplemental groups. Note that this is a complete list and will over ride their current list of groups unless we use the next option, dash A. Dash A and combination with dash capital G allows you to append people to supplemental groups. Dash L allows us to change the user's login name. Dash capital L and dash capital U allow us to lock and unlock users' accounts. User mod also has the ability to move a user's home directory using the dash M option. And lastly, dash S allows us to specify the default login shelf or the user. User mod has a few more options for expiring accounts and setting the inactive period on accounts. However, I used a change command for these things. You can watch the video in this course on individual user account settings for more information. In a terminal create a username Sally using the user add command. Type in sudo space useradd space Sally
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/modify-users?u=76281980&t=95)** and hit enter. Type in your password and hit enter again. Now that we have our new user, let's set our password. Type in sudo space P-A-S-S-W-D space Sally and hit enter. Now enter her password twice and hit enter again. We can verify this work by viewing the etsy shadow file. Type in sudo space cat space slash E-T-C slash shadow and hit enter. We can see that the Sally account exists and in the second field she has an encoded password. Now let's start modifying her settings. We'll start by adding her to the audio group. Type in sudo space usermod space dash A space dash capital G space audio space Sally. The dash A is for appending and the dash capital G followed by audio is the audio group. Hit enter and we'll verify by viewing etsy group. Type in cat space slash E-T-C slash group and hit enter. We'll need to scroll up to the audio group and ensure that Sally's name is in the last column. We can also lock and unlock accounts using user mod. Type in sudo space usermod dash capital L space Sally and hit enter. We can verify that her account is locked by viewing the etsy shadow file again. Type in sudo space cat space slash E-T-C slash shadow and hit enter.
>
> **[3:12](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/modify-users?u=76281980&t=192)** We'll notice that the very first character of her encoded password is now an exclamation point. Let's go ahead unlock her account by typing sudo space usermod space dash capital U space Sally and hit enter. Now verify that the exclamation point is gone by viewing etsy shadow again. Type in sudo space cat space slash E-T-C slash shadow and hit enter. Then we can see that the exclamation point is now gone. The last thing we'll do is restrict the Sally user so she can't login. If we use the user mod dash capital L option, it just locks the password. If Sally was logging in through as a sage, and had working S-S-H keys, should could still login. To keep her from logging in at all we can change her shell to slash sbin slash no login. Type in sudo space usermod space dash S space slash sbin slash no login space Sally. What we're doing here is we're changing her shell to slash sbin slash no login. We can verify this by catting etsy password. Type in cat space slash E-T-C slash P-A-S-S-W-D and hit enter. If we look at the last column, we'll see that her shell is slash sbin slash no login. To reverse the process, just change it back to slash bin slash bash. By typing in sudo space usermod dash S space slash bin slash bash space Sally and hit enter.
>
> **[4:49](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/modify-users?u=76281980&t=289)** For more information on the user mod command you might want to check out the man page by typing in man space usermod and hitting enter.

> [!info]- Semantic Content
>
> **CLI Commands:** sudo (10), cat (5)
> **Tools:** bash (2), terminal (1)
> **Env Vars:** sin (1)
> **Cross-References:** in the last (1)
> **Documentation:** man page (1)
> **UI Navigation:** scroll up (1)
> **Definitions:** is a  (1)
> **Warnings:** note that (1)

#### [Manage groups](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-groups?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-groups?u=76281980&t=0)** - [Instructor] Groups are an important part of the [[Linux]] [[User Management]] system. However, groups have one major limitation, they can't be nested, so you can't have a group within a group. To create a group we use groupadd. Groupadd has a few options, but the most common is dash g for specifying the group ID. Let's create a group called accounting with the group ID of 1050. In a terminal type in sudo space groupadd space dash g space 1050 space accounting and hit Enter. Type in your password and hit Enter again. We can verify this by viewing the Etsy group file. Type in cat space slash etc slash group and hit Enter. We see the last line has our accounting group and the group ID is 1050. If later we want to modify attributes of our group, we can with groupmod. For instance, if we want to change the group ID, we would type in sudo space groupmod space dash g space 1051 space accounting. This will change the group ID of the accounting group to 1051. If accounting were the primary group for any users, their accounts would be automatically updated to reflect the changed group ID. To place users in our new group we can use a g password command. My user name is grant, so to place my user in the accounting group, I would type, sudo space gpasswd space dash a space grant space accounting,
>
> **[1:40](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-groups?u=76281980&t=100)** and hit Enter. Specifying a user for our group is a very group-centric way of thinking about the problem. We can also be more user-centric and modify the user so they belong to a group. To do this, we would type in sudo space usermod space dash a space dash capital G space accounting space grant, and hit Enter. Remember that if you want to add the accounting group to a user's current list of groups, you have to include the dash lowercase a option. To delete a group, we'll use the groupdel command. Type in sudo space groupdel space accounting, and hit Enter. Verify that the group has gone by viewing Etsy group, type in cat space slash etc slash group and hit Enter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1), [[User Management]] (1)
> **CLI Commands:** sudo (5), cat (2)
> **Tools:** terminal (1)
> **Definitions:** is a  (1)
> **Analogies:** for instance (1)
> **Speakers:** - [instructor] (1)

#### [Manage special purpose and limited accounts](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-special-purpose-and-limited-accounts?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-special-purpose-and-limited-accounts?u=76281980&t=0)** - [Instructor] Most accounts in [[Linux]] are the same in that the user accounts with user IDs higher than 500 or 1000 depending on how the system was set up. You can see this configuration setting by viewing the /etc/login.defs file. In the terminal, type in cat /etc/login.defs and hit Enter. On CentOS, the starting UID for regular users is 1000. This is common across most current distributions. All user accounts with IDs less than 1000 are system accounts and often, they're not login accounts, meaning the users can run processes and own files, but they cannot log in to a shell. We can see this by looking at the /etc/password file. Type in cat /etc/password and hit Enter. If we look at the last column for each user, we can see which ones are login users because the shell will be /bin/bash or another login shell. If the account is not a login user, it will have /sbin/nologin or /bin/false. We could also have login users that can only run one custom application by placing that application in the last column using user mod. When the user logs in, that application will run automatically. Keep in mind that it's more complex than it seems as the /etc Linux security policy needs to be modified to allow custom applications to run. There are also third-party restricted shells that can be installed and used such as rsh, rbash, bash with a --restrcted option, rksh, and even ksh -r. However, they are not foolproof and we won't be covering them here. For elevated privileges, we have a special account
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-special-purpose-and-limited-accounts?u=76281980&t=94)** in many Linux distributions named wheel. It itself doesn't give us special privileges, but it's often listed as the group that admins are in. Let's take a look at the /etc/security/access.conf file using less. Type in less /etc/security/access.conf and hit Enter. Now search for wheel by pressing the forward slash and typing wheel. Here we see some special privileges for anyone in the wheel group, including the ability to shut the machine down. Non-local logins to the wheel account are also restricted. Press Q to quit. Let's also look at the sudo configuration. Type in sudo vIsudo and hit Enter, and type in your password if prompted. Once again, search for wheel, press your forward slash, and type in wheel. We can see that users in the wheel group can run all commands with privilege escalation. Below this, we can see how we'd set up special sudo privileges for other users and groups as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (3)
> **CLI Commands:** sudo (3), cat (2)
> **Tools:** bash (2), terminal (1)
> **Prerequisites:** set up (2)
> **Code Identifiers:** visudo (1)
> **Env Vars:** uid (1)
> **Cross-References:** in the last (1)
> **Analogies:** such as (1)

#### [Awareness of shadow passwords](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/awareness-of-shadow-passwords?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/awareness-of-shadow-passwords?u=76281980&t=0)** - [Instructor] In most systems passwords are encoded using a one way hash such as MD5 or SHA. A hash function takes a string of characters of any length as input and produces an encoded string of characters a fixed length, which acts as a kind of signature for the data provided. One way hashes are not reversible and as such, you cannot get the plain text from the hash. Note that for these slides, I've used an MD5 hash due to its shorter length. Most modern [[Linux]] systems use a much longer and more secure SHA512 hash even though a one way hash cannot be reversed, it can be cracked by taking words from a dictionary one at a time, hashing them and comparing the output to the hashed password to see if they match. Linux has additional protection against dictionary attacks called a salt. A salt is a set of random characters that are appended to the password before it's hashed. The salt is stored in the password files with the hash. A hacker who doesn't have access to the salt, wouldn't be able to gas the correct password. The Portion of the hashed password to the left of the dollar sign is the salt. The portion to the right of the dollar sign is the hash. By using a different, random salt for each password, it makes dictionary attacks all but impossible. In order for a hacker to attempt to crack the passwords on Linux, they need to have read access to the salts and hashed passwords. In a system that doesn't use a shadow suite, the encoded user and group passwords are stored in /etc/password and /etc/group. Both of these files are world readable.
>
> **[1:32](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/awareness-of-shadow-passwords?u=76281980&t=92)** Meaning every user in the system can read them and possibly try to crack the hash passwords. The shadow suite removes encoded passwords from etc password and etc group by placing them in etc shadow and etc gshadow, neither of the shadow files are readable by users and as such are not easy to crack. Note that root is the super user. So root can read both of these files. Most Linux distributions these days have the shadow suite installed by default. If it's not installed, we can install it by opening a terminal by going to applications, favorites, terminal. I'm going to make my terminal full screen and [[Zoom]] the font. Now in the terminal, type in Sudo, space, yum install, space, shadow, dash, utils, and hit enter type in your password, and say yes to install. Commands and shadow utils, also let you convert a password from the shadow suite back to the old way, using the P-W-UNCONV and group unconv commands. I can't stress enough that I don't recommend running a system without the shadow suite, but for this exercise, we're going to convert from the shadow suite and to it. Let's investigate the shadow suite by viewing the etc password file using cat. Type in cat, space, slash, etc, slash, P-A-S-S-W-D and hit enter. You'll notice that the second column has an X. This means that the shadow suite is present. Now let's unconvert the etc shadow file so that the encoded passwords are moved to etc password. We'll need to elevate privileges using Sudo.
>
> **[3:06](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/awareness-of-shadow-passwords?u=76281980&t=186)** Type in Sudo space P-W-U-N-C-O-N-V Password unconvert and hit enter. Now let's cat the etc password file again by typing in cat, space, slash, etc, slash, P-A-S-S-W-D and hit enter. We should now see the encoded password in the second field. Before we unconvert etc gshadow, let's create a new group. We'll do this by typing in Sudo, space, groupadd, space, test group and hit enter. Now let's give it a group password by typing in Sudo, G-P-A-S-S-W-D, space, test group and hit enter. Now enter the password twice. Now let's look in the etc gshadow file using cat and elevated privileges using Sudo. Type in Sudo,space,cat,slash, etc, slash, gshadow and hit enter. You'll see that in the second field of the gshadow file we have an encoded password. Now that we have a group password we can use the group unconv command to unconvert it. Type in Sudo, space, G-R-P-U-N-C-O-N-V, hit enter. Now let's look at the etc group file again using cat. Type in cat, slash, etc, slash, group, and hit enter. We can see that we're no longer using the shadow suite in that we have an encoded password in the etc group file. Now we're running without using shadow files at all. I can't think of too many cases
>
> **[4:39](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/awareness-of-shadow-passwords?u=76281980&t=279)** where we'd want to unconvert our shadow files. However, we may want to go the other direction and convert a less secure system to use the shadow suite in order to do this, we'll use two different commands. To convert the password file we'll type in Sudo, space, P-W-C-O-N-V and hit enter. We'll convert the group file by typing Sudo, space, G-R-P-C-O-N-V and hit enter. To verify that we're using the shadow suite again, view the etc password file using cat. Type in cat, space, slash, etc, slash, P-A-S-S-W-D and hit enter. If we have an X in the second field we know that our passwords are once again stored in slash etc slash shadow. We can also check the etc group file by typing in cat, space, slash, etc, slash, group and hitting enter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (4), [[Zoom]] (1)
> **CLI Commands:** cat (11), sudo (10), make (1), yum (1)
> **Env Vars:** md5 (2), sha (1), sha512 (1), unconv (1)
> **Tools:** terminal (4)
> **Prerequisites:** install (3)
> **Definitions:** is a  (1), means that (1)
> **Warnings:** note that (2)
> **Analogies:** such as (1)

#### [Change default password policies](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/change-default-password-policies?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/change-default-password-policies?u=76281980&t=0)** - [Instructor] It's not uncommon for systems to get hacked due to weak passwords. Administrators can tighten up password policies to require a minimum number of characters and character classes. [[Linux]] has a credit system that affects the minimum allowed characters for a password. Credits are given if a user uses characters from different classes like lowercase, upper case, digits, and other characters. If our minimum password length is nine, and we include a lowercase letter, then Linux will allow an eight character password such as tqbfjotl. If our minimum password length is nine and we include at least one lower case and one uppercase letter, then Linux will allow a seven character password, such as Tqbfjot. If our minimum password length is nine, and we include at least one lowercase letter, one uppercase letter, and one digit, then it will accept a password that's six characters long, such as Tq8fjo. The password policy configuration contains settings for unique characters, minimum characters, digits, uppercase characters, lowercase, and other, which includes punctuation. It also has facilities for the minimum number of different character classes to require repeatable characters and whether to check the comment field of the /etc/password file for similarities to users' passwords. Difok is the number of characters
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/change-default-password-policies?u=76281980&t=93)** that must be unique in the new password. Minlen is the minimum length for the new password. Note that credits will subtract from this number. If you're using credits, you will want to add one to your desired minimum. Dcredit is the maximum credit for using digits. A password can be one character shorter if the user includes a digit. If you want to require a certain number of digits, place a negative number here, such as -1. Ucredit is the maximum credit for using uppercase characters. Use a negative number here to require uppercase characters. Lcredit is the maximum credit for using lowercase characters. Use a negative number here to require lowercase characters. Ocredit is the maximum credit for using other characters, such as punctuation. Using a negative number here will also require are other characters. A -1 would require one other character and a -2 would require two, and so on. Minclass defines the number of different character classes required. You can require characters from all four classes by placing a four here. Maxrepeat allows us to restrict the number of identical consecutive characters. Disable this check by setting it to zero. If you want to keep a user from having multiple digits or letters next to each other, we can set maxclassrepeat. Disable this check by setting it to zero. Change gecoscheck to one if you want to check for words
>
> **[3:05](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/change-default-password-policies?u=76281980&t=185)** from the comment field in the /etc/password file longer than three characters in the user's password. It will find both forward and reverse words. Let's go look at the pwquality.conf file in Linux. In a terminal, type in cat /etc/security/pwquality.conf,
>
> **[3:30](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/change-default-password-policies?u=76281980&t=210)** and hit enter. If you want to change any of these settings, you can use VI or any other text editor to edit them. You'll need to elevate your privileges using Sudo to save it, though. Once you've made changes to the system password policy, you may want to force users to change their password on the next login. To do this, we'll use the passwd command to expire the password. Type in sudo passwd --expire, space, and then the user name. In my case, the user name is Grant. And hit enter. Type in your password and hit enter again. You can also use the chage command covered in the password aging section of this course. Type in sudo chage -d 0, space, the user name. Again, in my case, it's Grant. And hit enter. Now when this user logs in again, they'll be prompted to change their password.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (4)
> **CLI Commands:** sudo (3), find (1), cat (1)
> **Analogies:** such as (5)
> **Tools:** terminal (1)
> **Warnings:** note that (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Individual user account settings](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/individual-user-account-settings?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/individual-user-account-settings?u=76281980&t=0)** - [Instructor] In another video of this course we saw how to change global user account settings by editing the /Etsy/login.deskfile. This only affects new users created after the change. If we need to change existing user accounts we have to use various other commands. You could edit the /Etsy/shadowfile for most of these settings, but it's not recommended as it's easy to misconfigure a user this way. The main command we'll use to change user account settings, is chage, or change age. Chage is used to change password and account aging information for users. Chage -d, changes the number of days since January 1st, 1970, when the password was last changed. Setting this to zero means the password hasn't been changed and forces the password change on next login.
>
> **[0:50](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/individual-user-account-settings?u=76281980&t=50)** - E, sets the date that the user's account will expire.
>
> **[0:54](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/individual-user-account-settings?u=76281980&t=54)** - I, sets the number of days of inactivity after password expiration, before the account is automatically locked.
>
> **[1:03](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/individual-user-account-settings?u=76281980&t=63)** - m, sets the minimum number of days before a password changes. Setting this to zero allows users to change their password at any time.
>
> **[1:14](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/individual-user-account-settings?u=76281980&t=74)** - M, is the maximum number of days during which a password is valid.
>
> **[1:19](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/individual-user-account-settings?u=76281980&t=79)** - W, is the number of days of warning before a password change is required. And lastly, -l, lists account aging information. This is the only command option that can be run by any user. Let's go to the terminal now and modify some account aging information. Now let's get the account information by first typing in, cat /etc/passwd. Now I only have one account with a user ID of 1000 or higher, which means a regular user account. My username is Grant. Let's get the account aging information for this user. Chage -l username, which in my case, is Grant. which in my case is grant here. Here we can see a listing of this account's aging information. If we want to force a user to enter their password at the next login, we can use the password command to expire their account. Type in, sudo passwd --expire , and then the user account name. In my case, Grant. Type in your password again, and hit Enter. Another way to expire a password is to use the chage command. Type in, sudo chage -d 0 , and then the user account which in my case, is Grant, and hit Enter. We can verify this by typing in, chage -l , and then user name and hitting Enter. You'll notice that my password must be changed.
>
> **[2:52](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/individual-user-account-settings?u=76281980&t=172)** So before going further, I'm going to go ahead and log out by clicking on my top right hand menu, clicking on my name, and then clicking on Log Out. Then I'll click on Log Out again. I'm going to try to log in normally with my old password, and you'll see that it's going to prompt me to change my password. I need to type in my old password again, and then I'll type in my new password, which I'm setting right here. Type it in a second time, and hit Enter. Once I'm logged in, I'll open up another terminal by going to Applications, Favorites, Terminal, make it full-screen and bump my font size. We're going to verify my aging information by typing in, chage -l , and then my username, which is Grant. You can see that now, my last password change was January 16, 2017. You may also notice that we have both account expiration and password expiration dates. When the password expires the user needs to reset their password. The user may still be able to log in using SSH keys, but when the account expires, it can no longer be logged in at all. Password expiration is to create a moving target for hackers. If users have to change their password every so often it makes it harder to exploit. Account expiration is to close accounts. First, I'm going to change the account expiration date for the user. Let's type in, sudo chage -E , and then I'm going to put a date in there, of 2017-11-10 , and then my username,
>
> **[4:30](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/individual-user-account-settings?u=76281980&t=270)** which is Grant, and hit Enter. I'll need to type in my new password again for sudo. Chage -l , and my username, which is Grant. We can now see that the account expires on November 10th, 2017. Now I'm going to change the password expiration to 90 days using the -M option. Type in, sudo chage -M 90 , and then the username, which in my case, is Grant, and verify by typing in chage -l , username, Enter. Note that this changes the password expiration date and the maximum number of days between password changes. Typically, if the password is expired, users are forced to change it during the next login. You can configure it so that when a user doesn't log in for a specific amount of time, after a password has expired, the account will be automatically locked. To change the number of days the account can be inactive before it's locked to 10, we'll use the -I option. Type in, sudo chage -I 10 , and the the username. Hit Enter. We'll verify this by typing in chage -l , the username. Now that we've done all of this we may want to disable all password aging. To do so, type in, sudo chage -I -1 -m 0 -M 99999 -E -1 the username,
>
> **[6:18](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/individual-user-account-settings?u=76281980&t=378)** which in my case, is Grant, and hit Enter. The -I -1 will set the password inactive to never. The -M 0, will set the minimum number of days between password changes to zero, so the user can change their password at any time. The -M 99999, will set the maximum number of days between password changes to 99999, or roughly 274 years. The -E -1, will set account expires to never. This will disable the password expiration of a user. We can verify this by typing in, chage -l , and the username, and hit Enter.

> [!info]- Semantic Content
>
> **CLI Commands:** sudo (7), cat (1), make (1), ssh (1)
> **Speakers:** - m (2), - [instructor] (1), - e (1), - i (1), - w (1)
> **Tools:** terminal (3)
> **UI Navigation:** go to (1), click on (1)
> **Warnings:** warning (1), note that (1)
> **Env Vars:** ssh (1)
> **Best Practices:** recommended (1)
> **Prerequisites:** configure (1)


### 5. Automate System Administration Tasks by Scheduling Jobs

[↑ Back to Table of Contents](#table-of-contents)

#### [Reccuring user jobs using cron](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/reccuring-user-jobs-using-cron?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/reccuring-user-jobs-using-cron?u=76281980&t=0)** - [Instructor] Systemd manages system services in CentOS 7. It also manages a lot of other objects like devices, system timers, and targets. The systemd equivalent to run levels. Systemd objects are called units and for each unit there's a unit file for configuration. For this course, we're only concerned with service units and service unit files. The command that systemd uses to manage these units is systemctl. To look at our service unit files type into a terminal, systemctl list-unit-files -at service
>
> **[0:40](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/reccuring-user-jobs-using-cron?u=76281980&t=40)** and hit enter. By default, list-unit-files -t service will show all enabled service unit files. Enabled, meaning services that are configured to startup automatically. By adding -a, it will show both enabled and disabled service unit files. Notice that the systemctl subcommand is list-unit-files. This means we're just looking at the unit files and their status. We're not yet looking at the running status of services. The output will be in two columns. The first column is the service name. The second column is the service state which can be either enabled, disabled, or static. Enabled means it will start automatically at boot. Disabled means it will not start automatically. Static means that service is not enabled and has no provisions to be enabled. Some services are not meant to start automatically. Press Q to quit. Let's get more information about enabled services. Type in systemctl list-units -at service and hit enter. By default, list-units -t service shows enabled running services. By including the -a, it will also show enabled non-running services. If you disable and stop a service, it will no longer show up here. The output has five columns. The first column is the service name.
>
> **[2:13](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/reccuring-user-jobs-using-cron?u=76281980&t=133)** The second column shows if the unit file was loaded. The third column, or active column, shows the general state of the service. The fourth column, or subcolumn, shows us more detailed information about its state including whether it's running or not. And the last column shows the description. Press Q to quit. We can filter by the state of the service. For instance, if we wanted to only list services that were active and currently running, we can type in, systemctl list-units -t service --state -running
>
> **[2:53](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/reccuring-user-jobs-using-cron?u=76281980&t=173)** and hit enter. You'll notice the naming convention of the service files. In this case, rsyslog.service. All service files end with .service. Press Q to quit. You can view any of these unit files to get an idea how they work. I'm going to choose the rsyslog.service. Type in clear and then type in, systemctl cat rsyslog and hit enter. Notice the service name was rsyslog.service but the command worked without the extension. What we see here is the unit file, which determines the service dependencies, what command it executes, and what the service should do if the command fails. We can also get the status of a single service, as well. Type in clear again and hit enter. Type in, systemctl status rsyslog and hit enter. This gives us the status of the service, when it started, what the process ID number is, and some log messages.

> [!info]- Semantic Content
>
> **CLI Commands:** systemctl (7), cat (1)
> **Tools:** terminal (1)
> **Definitions:** means that (1)
> **Analogies:** for instance (1)
> **Speakers:** - [instructor] (1)

#### [Reccuring system jobs using cron](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/reccuring-system-jobs-using-cron?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/reccuring-system-jobs-using-cron?u=76281980&t=0)** - [Narrator] Systems services are processes started by the OS and they're managed by the systemctl command. With systemctl, we can start, stop and restart services. Let's look at all unit files in a terminal, type in systemctl list-unit-files -at service
>
> **[0:22](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/reccuring-system-jobs-using-cron?u=76281980&t=22)** and hit Enter. This will show which services are enabled and disabled. Let's pick the atd.service since it's enabled. Let's stop the atd.service using systemctl. You'll need to elevate your privileges using sudo. Press Q to quit and then type in sudo systemctl stop atd and hit Enter. Type in your password and hit Enter again. Now let's check the status of the atd service. Type in systemctl status atd and hit Enter. We can see that the atd service is enabled but it's not currently running. We can start the atd service back up using the systemctl command. We'll need to elevate privileges using sudo systemctl start atd and hit Enter. Then verify with systemctl status. Type in systemctl status atd and hit Enter. We can now see that the service is still enabled but it's also running. To restart a service, we use systemctl restart. Type in sudo systemctl restart atd and hit Enter. If we just want to know if a service is running or not, we can use a systemctl is-active command. Type in systemctl is-active atd
>
> **[1:51](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/reccuring-system-jobs-using-cron?u=76281980&t=111)** and hit Enter. If you want to restrict a service from running either automatically or manually, use systemctl mask. Type in sudo systemctl mask atd and hit Enter. Now let's try to start it. Type in sudo systemctl start atd and hit Enter. We can see that we've been denied. We can unmask it the same way. Type in sudo systemctl unmask atd and hit Enter.

> [!info]- Semantic Content
>
> **CLI Commands:** systemctl (18), sudo (7)
> **Tools:** terminal (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [narrator] (1)

#### [Configure user access to cron and at services](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-user-access-to-cron-and-at-services?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-user-access-to-cron-and-at-services?u=76281980&t=0)** - When a [[Linux]] system boots up, certain services are run automatically by system D. We can enable services so they start automatically, or disable them so they don't. To see what services are enabled or disabled, type into a terminal systemctl, space, list-unit-files, space -at, space, service, and hit enter. Let's choose a service that's enabled, and disable it. I'm going to choose atd.service. Press Q to quit, and then type in sudo space systemctl space disable space atd, and hit enter. Type in your password, then hit enter again. We can press the up arrow key twice to bring back our list unit files line and hit enter. We can now see that our atd.service is disabled. Press Q to quit. We can also use a simpler command. Type in systemctl space is-enabled space atd, and hit enter.
>
> **[1:06](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-user-access-to-cron-and-at-services?u=76281980&t=66)** To re-enable atd, we'll use the systemctl command again. Type in sudo space systemctl space enable space atd and hit enter. And verify with systemctl space is hyphen enabled space atd and hit enter. We could also check the status of the service by typing in systemctl space status space atd and hit enter. This shows us the status of the process and some logging information.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1)
> **CLI Commands:** systemctl (7), sudo (2)
> **Tools:** terminal (1)
> **Speakers:** - when (1)

#### [Systemd timer units](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/systemd-timer-units?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/systemd-timer-units?u=76281980&t=0)** - [Instructor] Systemd allows you to define timer units. Systemd timers are a powerful alternative to cron jobs. There are two different types of timer units: [[Real-Time]] timers and monotonic timers. Real-time timers activate on calendar events similar to cron jobs. There started based on a date and time value. We can set a timer to run every year on December 31st at midnight that says, "Happy New Year," or we could set a timer to run every Sunday that does a full backup of our system. Monotonic timers activate after a time span relative to a starting point. For instance, if a timer is to be activated five minutes after the boot process or 30 seconds after we log in, we'd used a monotonic timer. The main benefits of using timers over cron jobs comes from each job having its own service file. Also, jobs can be started independently of their timer making them easier to debug. Jobs can be configured to run in their own environment. Jobs can be attached to cgroups. Jobs can have dependencies on other systemd units, such as network. We can have job that only runs if the network is active. And jobs are logged in the systemd journal. Cron jobs, however, are easier to create as they're often one-liners, and have built-in support for emailing based on job failure. This functionality however can be simulated with systemd timers. Having units are named with a .timer extension and have a matching .service file. We could have a backup.timer and a backup.service where the timer activates the service. This service then could run a script of any name, such as backup.sh.
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/systemd-timer-units?u=76281980&t=94)** We can list our timers using the systemctl command. In a terminal, type in systemctl list-timers and hit enter. The output shows when the timer last ran and how long before it runs again. It also tells the timer unit name and the service unit that it activates. An example service file would look like this. This time would be named backup.service. This service runs the /root/bin/sysbackup.sh script. This would be something we created to do the work. We could also have an application here. The timer that activates this service would look like this. The type of timer is OnCalendar, which is set to activate everyday at 2:00 a.m. This means it's a real-time calendar. The timer unit that it activates is backup.service. We can see that we can specify dates, in addition to times, using the OnCalendar keyword. Using OnCalendar, we're creating a real-time calendar. We have other keywords that we can use besides OnCalendar that will allow us to create monotonic timers. Remember, monotonic timers are specific to a starting point. OnActiveSec is relative to the moment the timer's activated. OnBootSec is relative to when the machine booted up. OnStartupSec is relative to when systemd was started. OnUnitActiveSec is relative to when the unit the timer is activating was last activated. This references the service unit. This allows us to start counting from when the service unit started. OnUnitInactiveSec is relative to when the unit the timer is activating was last de-activated.
>
> **[3:08](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/systemd-timer-units?u=76281980&t=188)** This references the service unit again. This allows us to start counting from when the service unit stopped. RandomizedDelaySec allows us to specify a random amount of time in which it should activate the service. This allows us to spread the load out. This has been a problem with cron jobs in the past. A monotonic timer would look like this. This monotonic timer uses the OnBootSec keyboard and specifies 15 minutes. It also specifies weekly, so it only runs once a week.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Real-Time]] (4)
> **Analogies:** such as (2), similar to (1), for instance (1)
> **File Paths:** backup.sh (1), root/bin/sysbackup.sh (1)
> **CLI Commands:** systemctl (2)
> **Ports:** :00 (1)
> **Tools:** terminal (1)
> **Speakers:** - [instructor] (1)


### 6. Localization and System Time

[↑ Back to Table of Contents](#table-of-contents)

#### [Set the system date, time, and timezone](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-the-system-date-time-and-timezone?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-the-system-date-time-and-timezone?u=76281980&t=0)** - [Instructor] Setting the date and time from the GUI is fairly easy. Go to applications, system tools, settings, and click on date and time. This interface is pretty simple, but it allows you to change the date, time and timezone as well as have them automatically updated. You can also choose a 12 or 24 hour clock. Our computers get their time information from various sources. Every computer has a real time clock or RTC that keeps time when the computer is off. When the computer is turned on, the OS reads the real time clock and then keeps the time from that point on. If we set up Network Time Protocol or NTP, our computer will contact a time server somewhere on the internet and adjust the time accordingly. This method is the most accurate but requires a network connection. To change the date and time settings from the command line, We'll want to open a terminal. So close date and time and then go to applications, favorites, terminal. I'm going to make my terminal fullscreen and bump my font size. The command we use on CentOS seven to manage our date and time is timedatectl. Type in timedatectl and hit enter. This will show us our local time for our timezone, Universal Time, Real Time Clock, the time of our PC [[Hardware]] clock, our current timezone, whether automatic time synchronization is on or off, and whether we're in daylight savings time. Before we set our timezone we might want to get a list of all time zones. Type in timedatectl list-timezones and hit enter.
>
> **[1:37](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-the-system-date-time-and-timezone?u=76281980&t=97)** Now there's over 400 timezones here, we can scroll through them, or we can be more specific, Press Q to quit, type in timedatectl space list dash timezones, and let's pipe it to grep and search for America with a capital A and hit enter. This is a much shorter list. I'm on the west coast so I'm going to choose America slash Vancouver. To set our timezone we'll type in timedatectl set-timezone, space, America slash Vancouver, and the capital letters matter, hit enter. We can verify this by typing in timedatectl and hitting Enter. If you want to manually set the time we'll use the same command but with a set-time option, type in timedatectl set dash time space, we wannna put in the hour for instance, 23 colon 26 for the minute colon and then 00 for the seconds, hit enter. We can verify this by typing in timedatectl again. If the time didn't change, you'll want to just run the command a second time and then verify again. To set the date, use the exact same command with the same option but follow it with a date, timedatectl set-time and then the date is in the format of year, dash, month,
>
> **[3:11](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-the-system-date-time-and-timezone?u=76281980&t=191)** dash, and then the day. So timedatectl space set dash time space 2019 dash 09 dash 20. If you wanted to set the time and date at the same time, you can, timedatectl set dash time, and inside a single quote, put in the date 2019 dash 09 dash 20 space and then a time so 12:20 colon 00 single quote and hit enter. It may be desirable to have our system update its own time from an NTP server on the network. To set this up, we'll use timedatectl again with the set dash NTP Boolean timedatectl set dash ntp space true, hit enter. We'll want to verify this by typing in timedatectl and hitting Enter. We can see that NTP enabled is on but NTP synchronized is still no. The reason for this is we need to restart the NTP client, sudo systemctl space restart, systemd dash, timedated, hit Enter, and type in your password. Now let's verify again using timedatectl. Hit enter. We can see that NTP enabled equals yes, and NTP synchronized equals yes.
>
> **[4:45](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-the-system-date-time-and-timezone?u=76281980&t=285)** If when you check the NTP enabled line it says n slash A, then your NTP client is probably not installed, install it using yum, sudo yum install chrony, hit enter Now mine is already installed but if it wasn't, I'd say yes to all installation prompts and then type in timedatectl space set dash ntp space true and it would be turned on. Now we have our time and date set to update automatically

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hardware]] (1)
> **Env Vars:** ntp (10), gui (1), rtc (1)
> **CLI Commands:** sudo (2), yum (2), make (1), grep (1), systemctl (1)
> **Tools:** terminal (3), command line (1)
> **UI Navigation:** go to (2), click on (1)
> **Prerequisites:** install (2), set up (1)
> **Ports:** :20 (1)
> **Definitions:** is a  (1)

#### [Configure locale settings and environment variables](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-locale-settings-and-environment-variables?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-locale-settings-and-environment-variables?u=76281980&t=0)** - [Instructor] Setting the locale from the GUI is fairly easy. Go to Applications, System Tools, Settings, and click on Region and Language. Let's talk about locale settings for a moment. The process of changing locale settings from the command line on the [[Linux]] OS that uses System D is very similar to changing time and date settings, except we're using the localectl command. Let's close the Region and Language dialog and open the Terminal. Application's favorites and click on Terminal. I'm going to make my Terminal fullscreen and [[Zoom]] my font. Type in localectl and hit Enter. Localetcl shows us our language and keyboard keymap. To list our languages, use list locales. Type in localectl list dash locales and hit Enter. Now there's over 800 locales so we can narrow it down using grep. Press Q to quit. Localectl space list dash locales, and let's pipe this to grep. And let's find lines that begin with en. So we're going to type in grep space caret en and hit Enter. The locale I'm going to change to is en underscore US dot utf8. So the whole line should say, localectl, space, set dash locale, space, LANG equals en underscore US dot utf, hit Enter. We can verify this by typing in localectl again.
>
> **[1:36](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-locale-settings-and-environment-variables?u=76281980&t=96)** To list other keymaps, use the list dash keymaps option. Localesctl list dash keymaps, hit Enter. Again, there's over 500 keymaps here so we'll use grep again to narrow it down. So the line should say localectl space list dash keymaps, space pipe, space grep space caret US, and hit Enter. Now we've narrowed it down substantially. I'm going to change my keymap to US. Localectl set dash keymap, space US and hit Enter. Now we've been able to change our locale and our keymap. Another tool that's very useful concerning date and time is the date command. Type in date, and it'll show you the current date and time. By default, the date command displays the local date and time. If we want to display Universal Time, we can pass the dash dash utc option. Type in date dash dash utc and hit Enter. If you're using a virtual machine like me, don't be alarmed if your date and time isn't correct. It all depends on how the date and time is getting passed to the virtual machine from VirtualBox. If you want to change the output format of the date command, you can with other options. For instance, if we wanted to show the month by abbreviation followed by the day in long year, I'm going to type in clear to clear my screen. And I'm going to type in date, the plus sign, double quote, percent, h, space, percent, d, space, percent, capital Y,
>
> **[3:16](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-locale-settings-and-environment-variables?u=76281980&t=196)** and then another double quote and hit Enter. The syntax is date plus, and then inside double quotes, the parameters necessary. If you wanted to, check the man page for other options. Here's another example. Date, plus, double quote, percent, s, double quote and hit Enter. This is the number of seconds from January 1st 1970. We would do this whenever we wanted to calculate time such as in a script. We would get the current time in seconds, run the operation, get the time again and subtract the difference. Date can also take the number of seconds and output a date in whatever format we want. As an example, let's take the number of seconds in last operation and find the date for it. Type in date, space, dash dash date, equals, and then inside single quotes, the @ symbol, and the number that was the result of the command previous. I'll paste that in there, another single quote, and hit Enter, and it returns a date back. This comes in very handy if you ever want to calculate dates. For instance, let's say it's February 26th and we want to calculate 10 days in the future. You could type in date, space, dash dash date, space, inside single quotes, plus 10 days, another single quote and hit Enter. And it'll calculate 10 days for you. It also accepts more abstract wording, date, dash dash date, single quote, next Thursday,
>
> **[4:55](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-locale-settings-and-environment-variables?u=76281980&t=295)** single quote, and hit Enter. There's a lot more that the date command can do so check out the man page for more options. There's one more time and date tool I'd like to show. Cal, hit Enter. Cal shows you a calendar. It seems silly at first, but it's actually quite useful. If you wanted to show three months, we can type in cal dash three and hit Enter, and it shows us three months. Note that this is different than typing in cal space three, which is the year number three. You can show the calendar for any year. For instance, cal 1752 and hit Enter. Notice anything strange about September. I'll leave that up to the viewer to research it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1), [[Zoom]] (1)
> **CLI Commands:** grep (5), find (2), make (1)
> **Tools:** terminal (3), command line (1)
> **UI Navigation:** click on (2), go to (1), open the (1)
> **Analogies:** for instance (3), such as (1)
> **Env Vars:** gui (1), lang (1)
> **Documentation:** man page (2)
> **Warnings:** note that (1)

#### [Install and start chrony](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/install-and-start-chrony?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/install-and-start-chrony?u=76281980&t=0)** - [Instructor] The Chrony server and client should already be installed by default but to be sure open a terminal and type sudo space yum space install space dash Y space chrony and hit Enter. Type in your password if prompted. This will not hurt anything if it's already installed. After making sure Chrony is installed we need to make sure the service is running. To check the status, let's use system CTL. Type in clear and then type in sudo space systemctl space status space chronyd and hit Enter. This shows our Chronyd server status. If yours is not running you'll want to start it by typing in sudo space systemctl space start space chronyd and hit Enter. And to make it start automatically at boot, we'd enable it. Type in sudo space systemctl space enable space Chronyd
>
> **[1:03](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/install-and-start-chrony?u=76281980&t=63)** and hit Enter. Again this won't hurt anything if it's already running and persistent, now Chronyd will start when the system boots up. The Chrony package includes the Chronyd ntp time server. Chronyd by default only accepts time queries from the local client. The chronyd server can serve up time information to clients as well as allow a client to connect to it to control it, those are configurable independently. The chronyd server by default allows connections using local Unix domain sockets. It can optionally be configured to allow connections using IP version four or IP version six. The chronyd server which we've already discussed and a command line client called chronyc. Chronyc can be used to query the local time server and also can be configured to query other remote servers if needed. If our chronyd server is configured properly, we can also use chronyc to control our time server remotely.

> [!info]- Semantic Content
>
> **CLI Commands:** sudo (4), systemctl (3), make (2), yum (1)
> **Tools:** terminal (1), command line (1)
> **Env Vars:** ctl (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)

#### [Configure chrony server](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-chrony-server?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-chrony-server?u=76281980&t=0)** - [Instructor] To configure our Chrony server to accept local network requests, we'll need to know our network IP address. Make sure you're logged into your rhhost1 VM and then in a terminal, type in ifconfig. You can also use ip space addr as well, but I find the output is easier to read on the legacy ifconfig command. I can see that my IP address is 192.168.1.69 and my network mask is 255.255.255.0. This makes my network address 192.168.1.0. We need to remember this for later. For this exercise, we'll configure chronyd to accept network time requests from clients in the local network. For this, we'll configure chronyd, and then open a hole in the firewall. To configure Chrony as an NTP time server, we'll want to edit the /etc/chrony.conf file. Let's do this with Vim. Type in clear and then type in sudo space vim space slash etc slash chrony dot conf and hit Enter. Type in your password if prompted. Comments start with a hash, percent, or exclamation point. The first non-commented lines may be the server lines or pool lines. If the keyword is pool, then the line specifies a pool of NTP servers as a time source. This pool name will resolve to multiple servers, which may change over time. At startup, chronyd will get a list of sources from the pool. If a source is unavailable,
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-chrony-server?u=76281980&t=93)** it will attempt to replace it with another. In my case, I don't have any pool lines, but I have multiple server lines. The server keyword here specifies a specific server instead of a pool. Using the server keyword, we could direct all NTP client requests on the local host to a specific time server. The next line is the location of the drift file. This file stores the rate in which the system clock gains or loses in relation to real time. This is stored to allow Chrony to adjust a clock before a reliable source is established. You should not have to do anything with this configuration line. The next line starts with makestep and determines if Chrony can step the time if the time was too far off to adjust via speeding up or slowing down the clock frequency. My line says make step space one dot zero space three. This would allow stepping the clock if the adjustment is more than one second off, but only in the first three clock adjustments. Now let's jump down to the min sources line. This is the number of NTP sources needed before adjusting the local clock. The default is one, but if we want to make our local time more reliable, we may require more than one source. These sources would have to agree before a local clock is adjusted. The allow line is what turns our Chrony installation into a time server for other hosts on the network. It's commented out by default. Let's uncomment this line and change it to our local network address. For mine, I'll make it allow space 192.168.1.0/24,
>
> **[3:08](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-chrony-server?u=76281980&t=188)** as my local network address is 192.168.1.0 and my subnet mask is 255.255.255.0, or 24 bits. We can also allow chronyc to control chronyd by using the cmdallow keyword in the same manner, which we will not cover in this course. The local line allows us to use Chrony to serve up time to our local network without being connected to a time server. This is useful if we have an isolated network. If we wanted to specify a pure host to synchronize time to, we'd use the peer keyword followed by the IP address of the peer. We would also add the peer keyword to the other host configuration file pointing back to this host so both hosts would synchronize with each other. This is an RHCE exam item. Note that we can specify a server, a pool, a peer, and even local in one file. Chrony will fall back to peer and local if the remote time sources aren't available. To save and exit, press Escape colon X exclamation point and then hit Enter. Now we need to allow external NTP requests to our service by opening a port in the firewall. Type in sudo space firewall dash cmd space dash dash permanent space dash dash zone equals public space dash dash add dash service space ntp and hit Enter.
>
> **[4:40](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-chrony-server?u=76281980&t=280)** Type in your password if prompted. In most installations, the public zone is default, so you wouldn't have to add it here. However, if we had a firewalld zone for our internal network and one for our public network and we only wanted to allow queries on the internal network, we'd add the service to that zone only. To enable the changes we've made to the firewall, let's reload the firewall configuration. Type in sudo space firewall dash cmd space dash dash reload and hit Enter. And then to verify our service has been added to the zone, type in sudo space firewall dash cmd space dash dash list dash all and hit Enter. Here we can see that NTP is allowed in our public zone. Now restart the Chrony service. Type in sudo space systemctl space restart space chronyd and hit Enter. Your time server should be ready to serve up time information to clients now.

> [!info]- Semantic Content
>
> **CLI Commands:** sudo (5), make (4), find (1), systemctl (1)
> **Env Vars:** ntp (6), rhce (1)
> **Versions:** 192.168.1 (4), 255.255.255 (2)
> **Prerequisites:** configure (4)
> **Tools:** vim (2), terminal (1)
> **Definitions:** is an  (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Configure chrony client](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-chrony-client?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-chrony-client?u=76281980&t=0)** - [Narrator] Now that we have our rhhost1 VM configured to be an NTP time server, let's go to our second VM and configure it to use our time server. Log in to rhhost2 and open a terminal. Now let's edit the /etc/chrony.conf file using VIM. Type in sudo space VIM space /etc/chrony.conf and hit enter. Enter your password if prompted. Go into insert mode by pressing the insert key or the "I" key. Comment out any existing pool and server lines and add one server line to point to our time server on rhhost1. In my case, it will be server space 192.168.1.69 space iburst. Use the IP address of your rhhost1 VM in your file. Now, say this by typing in "escape:x!" and hitting enter. Now restart the chronyd service. Type in sudo space systemctl space restart space chronyd and hit enter. Let's insure our rhhost2 VM is configured to use NTP by using the timed ACTL command. Type in sudo space timedatectl space set-ntp space true, and hit enter. To test that our system is synchronizing with our time server,
>
> **[1:32](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-chrony-client?u=76281980&t=92)** type in sudo space chronyc space sources space -v. Chronyc is the client command that comes with chrony. The -v option tells us what each column is for. The first character on the left says this host is a server, and the second character is an asterisk showing it's synchronized.

> [!info]- Semantic Content
>
> **CLI Commands:** sudo (4), systemctl (1)
> **Env Vars:** ntp (2), vim (2), actl (1)
> **Tools:** vim (2), terminal (1)
> **Definitions:** is a  (1), is an  (1)
> **Versions:** 192.168.1 (1)
> **UI Navigation:** go to (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [narrator] (1)

#### [Use cryonyc](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-cryonyc?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-cryonyc?u=76281980&t=0)** - [Instructor] Now that we have a time server and client configured, let's use the chronyc command. In my rhhost1 VM, I'm going to type into a terminal sudo space chronyc space tracking and hit Enter. Type in your password if prompted. The reference ID is the computer in which Chrony is currently synchronized to. The reference time is when the time was last processed from the source. The system time may be different from the actual time. Chrony doesn't change the time, but rather speeds up or slows down the system clock until the two are synchronized, so there's no jump in time. The frequency is the amount the system clock would be wrong if Chrony wasn't correcting it. Let's check the Chrony sources by typing in clear and then type in sudo space chronyc space sources space dash v and hit Enter. The dash v option provides more caption information. The sources subcommand shows information about current time sources. The leftmost character under the capital M is the source mode. It will be a caret if the source is a server, an equal sign if it's a peer, and a hash symbol if it's the local clock. The second character shows the state of the source. It will be an asterisk if it's synchronized. The [[Representational State Transfer (REST)|rest]] of the columns provide valuable source information. Refer to the verbose output on the screen for more information. If you want drift and offset information for sources, use the sourcestats subcommand. Type in clear and then type in sudo space chronyc space sourcestats and hit Enter. If the cmdallow option is provided
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-cryonyc?u=76281980&t=95)** in the time service chrony.conf file, we can allow chronyc to control our time server. Note that any configuration change through chronyc is not persistent. As such, we will not be covering the cmdallow option in this course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** sudo (3)
> **Tools:** terminal (1)
> **Definitions:** is a  (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Using ntpd for serving time](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-ntpd-for-serving-time?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-ntpd-for-serving-time?u=76281980&t=0)** - The default NTP server for CentOS is ChronyD, however there are times where you may find yourself administering NTPD. Let's install NDBD and use some of the tools that come with it. First, let's stop ChronyD if it's running and disable it. I'm logged in as user1 but you can use any user you wish, as long as it is in the wheel group so you have sudo admin privileges. Type in 'sudo systemctl stop chronyd' and hit enter, and then to keep it from starting automatically bring your line back and change stop to disable and hit enter again. Now let's install NTBD, type into terminal 'sudo yum install -y ntp' and hit enter. Once that is done type in 'clear'. The next step is to allow NTP traffic through the firewall. Type in 'sudo firewall-cmd --permanent --add-service=ntp'
>
> **[1:02](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-ntpd-for-serving-time?u=76281980&t=62)** and hit enter. Now we need to reload the firewall rules, type in 'sudo firewall-cmd --reload' and hit enter. At this point we have the NTP server installed, it's running, and the NTP traffic is allowed through the firewall. So let's look at the configuration. Type in 'sudo vi /etc/ntp.conf' and hit enter. We can see a lot of the same settings in the ntp.conf file that are in the chrony.conf file. For instance, we can see a reference to the drift file at the top. If you want to allow hosts on your network to synchronize time with this server you need to loosen up the restrict lanes. For instance if my local network was 192.168.2.0 I'd modify the restrict by un-commenting it, and changing the network to 192.168.2.0. The nomodify and notrap statements at the end ensure that your clients are not allowed to configure the server, or be used as peers for time sync. Now go down one line to where it says 'Use public servers from the pool.[ntp.org](https://ntp.org) project.'. A lot of services use NTP to synchronize the system clock with a trusted external source. This source can be an atomic clock, a GPS receiver, or another server that already uses NTP. The NTP pool project is a worldwide cluster of time servers providing easy access to reliable time. You can go to www.[ntppool.org](https://ntppool.org) with a web browser and choose NTP pool servers near you and then modify the lines in this config if you wish. The included settings however, are fine.
>
> **[2:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-ntpd-for-serving-time?u=76281980&t=155)** If you want to query any of these NTP pool servers we can use the NTP date command with the '-qu' options followed by the NTP server address. To do so, open another terminal tab and then type in 'ntpdate -qu 0.centos.pool.[ntp.org](https://ntp.org)',
>
> **[2:55](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-ntpd-for-serving-time?u=76281980&t=175)** that was one of the servers in the file, and hit enter. From this output, we can see some information about the NTP pool servers, close this tab and go back to your first terminal. To save this file press ESC+':x!' and hit enter. Now let's start the service, type in clear then type in 'sudo systemctl start ntpd.service'.
>
> **[3:22](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/using-ntpd-for-serving-time?u=76281980&t=202)** If you wanted to make it persistent we would bring our line back and change start to enable so it will start on reboot. Now let's test our ntp server by querying it, type in 'ntpq -p' and hit enter. The remote column shows the hostname of the ntp servers, the refid column shows the source of the servers. For Stratum 1 servers it should be GPS, PPS, acdesk, or PTB. In my case it is PPS or a generic polls per second server. For Stratum 2 and higher it will show the IP address of the server. The Stratum level is in the third column, following this is the poll delay and jitter numbers, the lower the value the better. Now we have a working NTPD server set up, which we can practice using NTPQ and NTPD with.

> [!info]- Semantic Content
>
> **Env Vars:** ntp (12), ntpd (3), gps (2), pps (2), ndbd (1)
> **CLI Commands:** sudo (7), systemctl (2), find (1), yum (1), make (1)
> **Prerequisites:** install (3), configure (1), set up (1)
> **URLs:** [ntp.org](https://ntp.org) (2), [ntppool.org](https://ntppool.org) (1)
> **Tools:** terminal (3)
> **Versions:** 192.168.2 (2)
> **Analogies:** for instance (2)
> **Cross-References:** go back to (1)


### 7. System Logging

[↑ Back to Table of Contents](#table-of-contents)

#### [Locate and interpret system log files](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/locate-and-interpret-system-log-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/locate-and-interpret-system-log-files?u=76281980&t=0)** - Log files, are files that contain messages about the system. These messages would pertain to the kernel services or applications. There are different logs for different tasks. For instance, there's a log that keeps track of failed logins. There's another log, Just for cron jobs and so on. Most system D based operating systems have two login systems Rsyslog log and journald. Rsyslog is compatible sysklogd and handles persistent logs. Rsyslog either logs to text files in the local machine or it logs across the network to a remote logging server using either TCP or UDP protocols. Journald however, is part of system dean is not persistent by default, so the journal logs don't survive a reboot. Since the journal is binary and it's only stored in memory, it's very fast to write to and very fast for us to search, to ensure Rsyslog is running. We can use systemctl, hyphen sudo space systemctl space start space rsyslog and hit enter. Type in your password if prompted. We can also make the service start at boot by bringing the line back and changing start to enable and hitting enter again. In order to read the logs provided by our assist log, we need to be able to find them. The configuration file for our rsyslog is slash etc slash rsyslog.conf let's look at this file using less with line numbering turned on. Type in less space dash capital N slash etc slash our rsyslog.conf and hit enter. What we're looking for here starts about line 46
>
> **[1:32](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/locate-and-interpret-system-log-files?u=76281980&t=92)** so if necessary, scroll down until you see it. These are the rules of where each type of data will be logged to. We'll look at the authpriv rule on line 57 as an example. There are two columns in a rule. The selector on the left and the action on the right, the selector, is divided up into facility on the left and priority on the right in this example, all authentication messages with priority of critical and higher are logged to slash var slash log slash secure lines 52 through 54 are important log anything of level info or higher. The facility is a wildcard matching everything and the priority level is info following. Following this is negation for mail, authpriv and cron. The priority on these three is known, meaning it will ignore messages of these types. The file that these messages will be written to is slash var slash log slash messages. Let's open another terminal tab and look at this log file. Type in sudo space cat space slash var slash log slash messages and hit enter. Type in your password if prompted. .This can be a very long file as most all system messages in it. This is the goto file for troubleshooting and enterprise [[Linux]]. What I find useful in parsing this file since they can be so big is using the inverted search and grab it. You may have noticed that we have a great deal of systemd messages. If we wanted to ignore these while looking for something else, we could an invert to search with grep. Type in sudo space, grab space dash V for invert space, single quote, systemd colon, single quote,
>
> **[3:05](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/locate-and-interpret-system-log-files?u=76281980&t=185)** space slash var slash log slash messages and hit enter. Now we see all lines excluding those containing the [[Microsoft Word|word]] systemd from this data we can get a better idea of what is happening. If you want to remove more unwanted error messages, you can pipe this output back into grep again and filter out some more with the dash v option. Another useful tool for monitoring a log file is a tail commands follow option. Type in clear and then type in sudo space tail space dash f space slash var slash log slash messages and hit enter. The dash f option will follow the end of the file. As new data gets added to it, it will get displayed. Let's leave tail following the log file and go back to our first terminal tab. Line 57 specify as private as indication information. Here's where we'd see login information including failures. Go down to line 73, lines 73 specifies the boot messages we'd view slash var slash log slash boot dot log for these, rsyslog also provides the ability to write to log files on a remote server across the network. This topic is beyond the scope of this course, but you can see the examples at the bottom of the config file. Logs by default in enterprise Linux are rotated based on a configuration file. We won't go into log rotation here, but you may want to know a cron job named log rotate in slash etc slash cron daily, log rotate can be configured in the slash etc slash log rotate.conf file. There's one last thing of interest concerning logs. If you ever want to write a message to the system log file,
>
> **[4:39](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/locate-and-interpret-system-log-files?u=76281980&t=279)** you can use the logger command for instance, type in clear and then type in logger space double quote Linux is so worth it. Double quote and hit enter. Now flip over to your other terminal tab and look for our message. It should be at the bottom.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (3), [[Microsoft Word|Word]] (1)
> **CLI Commands:** sudo (4), systemctl (2), find (2), grep (2), make (1)
> **Tools:** terminal (3)
> **Env Vars:** tcp (1), udp (1)
> **Definitions:** is a  (2)
> **Analogies:** for instance (2)
> **Cross-References:** go back to (1)
> **UI Navigation:** scroll down (1)

#### [Read the system journal](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/read-the-system-journal?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/read-the-system-journal?u=76281980&t=0)** - [Narrator] The second method of logging data in Enterprise [[Linux]] is a service called journald which is part of systemd. The journal is a binary file stored in slash var slash run which is a virtual file system in RAM. Everything in this file system disappears on reboot, thus the journald by default is not persistent. Because it's in RAM, it also has a fixed size based on the amount of memory the system has. The data in the journal is structured and indexed so it can be accessed very quickly. If persistence is necessary, Journald can be made persistent, or even configured to forward journal data to our syslog. This gives us the ability to even send journal data to a remote log server. To view the journal entries, we'll use journalctl. In a terminal, type in journalctl, and hit enter. Typing journalctl without any arguments gives us all of journal entries. If you wanted to be a bit more specific and only view journal entries from the kernel, we can specify the dash k option. Press q, bring your line back and add a dash k to the end. We can also specify the command we want to show journal entries for. For instance, to show journal entries for the cron service, press q, then type in clear, and then type in journalctl space slash sbin slash crond and hit enter. This will only show journal entries for crond. We can also specify by systemd unit as well. Press q, type in clear, and then type in journal ctl, space dash u for unit space crond, which is the systemd unit name, and hit enter again.
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/read-the-system-journal?u=76281980&t=93)** In a previous video, we followed the messages log file by using tail dash f. We can't use tail dash f to follow the journal since it's binary, but we can use journalctl space dash f. Type in clear. Type in journal ctl space dash f and hit enter. This follows the journal just like tail dash f does for regular log files. To make the journal persistent so it survives a reboot, we just need to make a directory and restart the service. Press control c to stop journalctl, and then type in clear. Now type in Sudo space nkdir space slash var slash log slash journal. This'll be the directory the journal is in. Hit enter. Now restart the journald service. Type in Sudo space systemctl space restart space systemd dash journal d, and hit enter. And now let's look in the directory we just made. Type in ls space dash l space slash var slash log slash journal and hit enter. We should see a directory with a random looking name. This is a unique identifier of our system that was created at OS installation. Now that the journal survives reboots, we can show all events for a different number of reboots. Since we haven't rebooted yet, let's do it now. Type in reboot. After your system boots up, log in and open a terminal. To show entries from our previous reboot, use the dash b option. Type into a terminal journalctl space dash b space dash one and hit enter. We should see the journal entries from the previous session. We can also specify date ranges with journalctl.
>
> **[3:07](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/read-the-system-journal?u=76281980&t=187)** We can use the dash dash since and dash dash until keywords to specify ranges of dates and times to show. There are even some natural language keywords to make searches easier such as yesterday and one hour ago. In the past, this would have been accomplished by clever grep searches.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1)
> **CLI Commands:** journalctl (8), make (3), sudo (2), systemctl (1), ls (1)
> **Definitions:** is a  (4)
> **Tools:** terminal (3)
> **Analogies:** for instance (1), just like (1), such as (1)
> **Env Vars:** ram (2)
> **Cross-References:** previous video (1)
> **Speakers:** - [narrator] (1)

#### [Configuration of logrotate](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configuration-of-logrotate?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configuration-of-logrotate?u=76281980&t=0)** - [Instructor] In a previous video we looked at the Rsyslog configuration file and learned about rules made up of filters and actions. The filter determines which items in a log file to act upon. It is made up of a facility and a priority. An action is what Rsyslog will do. The facility is the subsystem that produces the system message. Facilities can be one of: kern, user, mail, daemon, auth, syslog, lpr, news, uucp, cron, authpriv, ftp, and local0 - local7. The second half of the filter is the priority. Priorities can be one of: Debug, Info, Notice, Warning, Err, Crit, Alert, and Emerg. When a priority is listed it shows all messages with this priority and higher. In this list Debug is the lowest priority and Emerg being the highest. For instance, if we set the priority to Warning it will also show Err, Crit, Alert, and Emerg. If I wanted to specify one specific priority I would use the equals sign before it. For instance, mail.=crit This would only show critical mail entries, not alerts or emergencies. If we want to exclude a certain priority we can use an exclamation point. For instance, mail.!crit If we wanted to include all priorities we can use an asterisk to select all. Rsyslog also supports property-based filters. A property-based filter lets you filter messages based on a property such as text inside the message or the host it came from.
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configuration-of-logrotate?u=76281980&t=94)** For instance, :msg, contains, "openvpn"
>
> **[1:42](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configuration-of-logrotate?u=76281980&t=102)** This would filter any messages that contain the [[Microsoft Word|word]] openvpn. As well as contains we can also based on: Isequal, Startswith, Regex, Eregex, which is for extended regular expressions, and Isempty. For instance, we could filter out hosting by typing in :hostname, isequal, "server1"
>
> **[2:09](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configuration-of-logrotate?u=76281980&t=129)** We can also do expression-based filters. For instance, this example filters messages by the openvpn program which are logged to /var/log/openvpn.log Also if the message contains the word init it will get saved in /var/log/init.log If it doesn't contain init then it's saved in /var/log/noinit.log

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (2)
> **Analogies:** for instance (6), such as (1)
> **Warnings:** warning (2)
> **Cross-References:** previous video (1)
> **Speakers:** - [instructor] (1)

#### [Filter journal data by criteria](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/filter-journal-data-by-criteria?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/filter-journal-data-by-criteria?u=76281980&t=0)** - [Instructor] Logs need to be rotated so they don't get too large. The configuration file for log rotate is /etc/logrotate.conf. Type into a terminal sudo space les space -N space /etc/logrotate.conf and hit Enter. We can see that the very top uncommented line is weekly. This is the log rotate schedule. The next uncommented line says rotate space four. This is how many weeks worth of logs to keep. Line number nine instructs to create new log files after the old ones have been rotated. This is good to have turned on because some services will only log to a file that already exists. Line number 12 says to use a date in the suffix of the rotated file name. Line 15 will compress the log files if uncommented. The last line we'll look at is line number 18. Line 18 includes all configuration files and /etc/logrotate.d. If we wanted to create a log rotation scheme for specific filters, you could create a config file here. Press Q to quit and then let's look at logrotate.d directory. Type in cd space /etc/logrotate.d and hit Enter. Then type in ls. Here we have logrotate files for cups printing, libvirtd, samba, and yum.

> [!info]- Semantic Content
>
> **CLI Commands:** sudo (1), cd (1), ls (1), yum (1)
> **Tools:** terminal (1)
> **Speakers:** - [instructor] (1)

#### [Rsyslog actions](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/rsyslog-actions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/rsyslog-actions?u=76281980&t=0)** - [Instructor] For every filter, there's an action. The action for a log filter usually involves saving the log messages to a file. For instance, in the case of cron* /var/log/cron.log, all cron messages are saved to the var.log/cron.log file. As this line is written, this file is synchronized on every write. This can create a lot of overhead if there are a lot of writes to the log. To keep from syncing the file system every time the log is written, we can precede the path with a dash. Be aware that the data's held in RAM since it's not read to the disk right away. This opens the possibility that you may lose data if the server shuts down unexpectedly. Log save paths can either be static as we saw in our previous example or dynamic. With a dynamic path, we can create a template and then use that template name, prefixed with a question mark, in the rule. The template for this example might look like this. It will create a new log file for each message based on the timestamp of the message. When creating templates, we can access properties of the message. A property could be the text of the message, the host name, message priority, and so on. To see the entire list of message priorities, view the main page for rsslog.conf and search for available properties. A simple example would be %msg%, which would be the entire message text. Inside of the properties, we can also specify a character range. To specify the first 10 characters of the message, we'd use %msg:1:10%. After the range, we can include a property option. This is used to transform the text or format it. A great example might be lowercase.
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/rsyslog-actions?u=76281980&t=93)** Here's an example template that logs a message a severity, facility, timestamp, host name, message text, and a new line.

> [!info]- Semantic Content
>
> **Exercise Files:** template (4)
> **Ports:** :10 (1)
> **Env Vars:** ram (1)
> **Cross-References:** as we saw (1)
> **Analogies:** for instance (1)
> **Warnings:** be aware (1)
> **Speakers:** - [instructor] (1)

#### [Clear old systemd journal data](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/clear-old-systemd-journal-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/clear-old-systemd-journal-data?u=76281980&t=0)** - [Instructor] By default, the systemd journal is not persistent and only runs in RAM. However, as soon as you make it persistent, the journals can stack up, consuming large amounts of disk space. In a previous video, we made our journal persistent by creating the /var/log/journal directory and restarting the systemd-journald service. We haven't been using our system with a persistent journal for very long, so our journals won't be very large. However, let's take a look. Type in sudo space journalctl space dash dash disk dash usage and hit Enter. This shows we're only using 16 megabytes of space. On a production system, this may end up being gigabytes or more. You can also use the du command to get the size of the journal directory. Type in sudo space du space dash sh for summary and human readable space slash var slash log slash journal and hit Enter. Note the du command only shows the amount of disk space used by the archived journal files, not the current journal in RAM, which dash dash disk dash usage shows. Depending on if you have archived journals or not, you may see a difference in the output of these two commands. There are a couple of ways of clearing out journal files that you don't need. One method is to delete them based on how old they are. Let's check the age of our files. Type in ls space dash l space slash var slash log slash journal slash asterisk and hit Enter. This shows my oldest journal file is from January 5. This is also my current journal file since I have no archived journals. Again, in a production system,
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/clear-old-systemd-journal-data?u=76281980&t=93)** you may have journals for weeks or months taking up space. If we have archived journals, we can delete them based on age. If you wanted to clean up journals from more than a month ago, we type in sudo space journalctl space dash dash vacuum dash time equals 30d. This is going to vacuum up journal files older than 30 days. Obviously, you could set this to a much shorter timeframe, such as seven days. Another way we can manage old journal files is to delete them when they reach a certain size. To delete journal files when they reach one gigabyte, type in sudo space journalctl space dash dash vacuum dash size equals one capital G. This will delete any file larger than one gigabyte. You can also delete all but a certain number of journal files. If we wanted to delete all but two journal files, we'd type in sudo space journalctl space dash dash vacuum dash files equals two. There are multiple ways of slimming down our journal files. Use the one best suited for your situation.

> [!info]- Semantic Content
>
> **CLI Commands:** sudo (5), journalctl (4), make (1), ls (1)
> **Env Vars:** ram (2)
> **Cross-References:** previous video (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Retrieve systemd journal data from a rescue system](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/retrieve-systemd-journal-data-from-a-rescue-system?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/retrieve-systemd-journal-data-from-a-rescue-system?u=76281980&t=0)** - [Instructor] There are times where a server does crash we need to read the data from our recovered drive. For normal logging with our syslog or other equivalents, it's easy. We just mount the recovered drive into our file system and use grep to find what we want. This works because the syslog's messages are text files. However, the systemd-journald is binary so we can't do this. In order for us to access journals from a recovered system, they need be persistent. You make your journal persistent by creating a /var/log/journal directory. After restarting the system D dash journal D service, it creates a directory in /var/log/journal named with the UID or universally Unique Identification Number. This is different for your system. When you use journalctl it automatically reads the journal at /var/log/journal. And then, a directory named with your UID. Inside that directory, can be one or more journals depending on other factors like the number of users. If we restore a journal from a recovered system with a different UID, then journalctl won't read it automatically. To get journalctl to read journal files in a different directory, we can use the dash dash directory option. Let's say we've inserted the recovered drive into our system, and matter of the root file system has /media/recovered. This means the recovered system journal directory path would be at /media/recovered/var/log/journal. To use this directory for journalctl operations type, sudo space journalctl space --directory= and then the path to the recovered journal directory.
>
> **[1:36](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/retrieve-systemd-journal-data-from-a-rescue-system?u=76281980&t=96)** To get journalctl to read a different file in any location, we can specify it by path. For instance, if we wanted to read the journal at /var/log/journal/recovered.journal we'd type in sudo space journalctl space --file= and then the path to the recovered journal file, and it will read it. Another option is to place the recovered system's entire EUID directory in /var/log/journal. And the use the -m option to merge all logs in the journal directory. To do so, type in sudo space journalctl space -m. This treats all journals in the journal directory as one, allowing us to read and search through them.

> [!info]- Semantic Content
>
> **CLI Commands:** journalctl (8), sudo (3), grep (1), find (1), make (1)
> **Env Vars:** uid (3), euid (1)
> **Analogies:** for instance (1)
> **Speakers:** - [instructor] (1)

#### [About syslog, rsyslog, and syslog-ng](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-syslog-rsyslog-and-syslog-ng?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-syslog-rsyslog-and-syslog-ng?u=76281980&t=0)** - [Instructor] Syslog, otherwise known as sysklogd, came into being in 1980. At that time, it could log single lines to a log file and specify the facility or service that the message is about, plus a log level and a message. It could log to remote servers over UDP protocol, but did not have congestion control. The syslog protocol became the de facto standard in the [[Linux]] and Unix world. Syslog had many limitations, so it needed to be replaced. 24 years after syslog came out, the rsyslog project was started, and is backwards compatible with sysklogd. Rsyslog supports timestamps with millisecond granularity and time zone information. It also supports the addition of the name of relays in the host fields to make it possible to track the path a given message has traversed. It supports reliable transport using TCP, supports GSS-API and TLS for security, and logging directly into various database engines. It also supports RELP, or the reliable event logging protocol, which has a back channel and communicates back to the source to let it know if the message arrived. It has support for buffered operation modes where messages are buffered locally if the receiver is not ready. Plus, complete input/output support for the systemd journal and content-based filtering, as well as rich filtering capabilities. In short, rsyslog is a modernized and more powerful version of sysklogd. In 2007, rsyslog became the default in Fedora. Fedora was the first distribution to move to rsyslog. From 2009 on, openSUSE has used rsyslog
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-syslog-rsyslog-and-syslog-ng?u=76281980&t=95)** as well as Red Hat Enterprise Linux 5 and newer. It's also the default in both Debian and [[Ubuntu]]. It's important to note that rsyslog is backwards compatible with sysklogd making it an easy transition for anyone who already knows how to administer sysklogd. Syslog-ng, however, is a new stack written from the ground up starting in 1998. It still uses the syslog protocol, but has extended it just like rsyslog has. Like rsyslog, it features content-based filtering, rich filtering capabilities, flexible configuration options, and adds important features to syslog, like using TCP for transport. Syslog-ng comes in an open-source version, but also a proprietary version as well, and it can log directly into a database. In addition, it has the ability to format log messages using Unix shell-like variable expansion. It can use this variable expansion when naming files covering multiple destination files with a single statement. It has the ability to send log messages to local applications, support for message flow control and network transport, and it can rewrite portions of the syslog message with set and substitute primitives. It can classify incoming log messages and at the same time extract structured information from unstructured syslog messages. It has generic name-value support. Each message is just a set of name-value pairs, which can be used to store extra information. It has the ability to process structured message formats transmitted over syslog. For instance, it can extract columns from CSV formatted lines.
>
> **[3:08](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-syslog-rsyslog-and-syslog-ng?u=76281980&t=188)** This means it can handle multi-line messages. And lastly, the ability to correlate multiple incoming messages to form a more complex correlated event. In short, syslog-ng is a very powerful system logger. It can handle logs and multi-line structured formats sent over reliable connections. Most distributions, however, do not use it and it doesn't currently support systemd's journal.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (2), [[Ubuntu]] (1)
> **Env Vars:** tcp (2), udp (1), gss (1), api (1), tls (1)
> **Definitions:** is a  (3), known as (1)
> **Analogies:** just like (1), for instance (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** opensuse (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)


### 8. Mail Transfer Agent (MTA) Basics

[↑ Back to Table of Contents](#table-of-contents)

#### [About email](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-email?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-email?u=76281980&t=0)** - Email was the killer application that drove the creation of the internet as we know it. It's still the most popular application on the internet, although now we have many other ways of communicating. Electronic mail works in a similar way to snail mail, in that we send letters or messages from one person to another which are delivered by multiple carriers between the two. Let's cover some terms associated with email systems. With email, the protocol that sends from one email server to another is SMTP, or Simple Mail Transfer Protocol. The SMTP Protocol runs on IP for host addressing and uses port number 25. Just as a traditional letter has a sender and destination address, an email has a sender's ID and a domain, along with a destination email address. It also carries a subject line and the message itself. The email system compromises of a mail user agent or MUA, which is the program that sends the mail from the user's computer to a mail transport agent or MTA. A mail transport agent or MTA is responsible for delivering the email from the sending mail server to the ending mail server. A mail submission agent or MSA is responsible for accepting new mail for the MTA. This functionality may be included in the MTA or MUA software applications or it may be a separate program. A mail delivery agent or MDA is responsible for delivering incoming messages to the mail spool. This also may be integrated in the MTA or it may be a separate program. We've already discussed the SMTP protocol used by mail user agents to send mail to MTAs,
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-email?u=76281980&t=95)** it's also used between MTAs. However, for downloading mail from the MTA, there are two common protocols used by the mail user agent. The first is Post [[Microsoft Office|Office]] Protocol or POP. The last version of POP was POP3 and it uses port number 110. By default, POP deletes the email from the server when you download it. Although there are some POP clients that will optionally leave the mail there. It's also possible to use a secure version of POP called POP3s, which uses SSL and port number 995. The other protocol for downloading email is Internet Mail Access Protocol or IMAP. IMAP is more powerful than POP in that it's easier to access mail using multiple systems, and works better over slow connections. Because IMAP only downloads a copy of the email, a user can log into a different host and retrieve their email again. A smart host or mail relay is an MTA configured to deliver mail for another system. The other system may not have a caustic connection to the internet, so it delivers mail to the relay, which then sends it on. A mail queue is a directory where mail is stored while it's waiting to be processed. And lastly, a mailbox is a place where user mail is stored. A mailbox is created for each user.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Office|Office]] (1)
> **Env Vars:** mta (7), pop (6), smtp (3), imap (3), mua (2)
> **Definitions:** is a  (2), is an  (1)
> **Speakers:** - email (1)

#### [How email works](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/how-email-works?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/how-email-works?u=76281980&t=0)** - [Instructor] It helps to visualize how email systems work. Let's go through the process of an email going from one user to another. The mail is composed and then sent on by the mail user agent using SMTP. The mail submission agent that resides on the mail server accepts the message and adds it to the mail queue for further processing. The mail transport agent on the mail server then transports that email to another mail server on the network or internet using the SMTP protocol. The MTA receives the mail, and the mail delivery agent stores it in a mail spool, waiting for the mail user agent to retrieve it. Lastly, the MTA authenticates the mail user agent, which downloads the mail message using either POP3 or IMAP protocols.

> [!info]- Semantic Content
>
> **Env Vars:** smtp (2), mta (2), pop3 (1), imap (1)
> **Speakers:** - [instructor] (1)

#### [Knowledge of common MTA programs](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/knowledge-of-common-mta-programs?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/knowledge-of-common-mta-programs?u=76281980&t=0)** A Mail Transport Agent or MTA is a program which transports email between mail servers. There are a lot of MTA's available for [[Linux]]. In this video, we'll discuss a few. The old standby and first killer application for the internet is Sendmail. Sendmail came out in 1983, and by 1996, had 80% of the MTA market share. As recently as 2005, Sendmail had fallen to 35%, and as of this recording, was down to 4%. Sendmail is big and difficult to configure, and complex to secure. There aren't many Sendmail servers of production these days. Postfix came out in 1998, about the time that Sendmail had near total dominance of the MTA market. It was created by IBM as a drop-in replacement for Sendmail. And was created in response to Sendmail's security and configuration issues. It has easy to understand configuration files, it supports multiple domains, encryption, and so on. Postfix is not monolithic, like Sendmail and has multiple independent binaries to spread the load. Postfix currently has about 30% of the MTA market share, and is the default for most Linux distributions. Qmail was released in 1995 and is a secure, reliable, and efficient MTA. It was was also developed as a replacement for Sendmail due to security issues and configuration complexity. Qmail's claim to fame is that it has no known security issues. It has not been updated in many years, for it lives on packaged in indie mail and open sourced mail server platforms. Qmail is public domain and has negligible market share. The last alternative MTA that we'll discuss is Exim.
>
> **[1:37](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/knowledge-of-common-mta-programs?u=76281980&t=97)** Exim was created at the University of Cambridge and first released in 1995. Exim is the most popular MTA in the world and commands 57% of the market. Exim follows the Sendmail design philosophy, with one large binary handling all functions of the MTA. It has also had a long history of security vulnerabilities, however, it has many configuration options and abilities that other MTA's don't have, and is easy to configure. Thus, contributing to it's popularity. Because of how popular Sendmail was, almost every MTA has a Sendmail emulation layer. So it can function in place of Sendamil to maintain backwards compatibility. Often, you can type Sendmail or mail-q on the command line of a system that doesn't have Sendmail installed and they will run. Usually, there are different commands serving the same functions as the originals. Since we'll be moving into a networked environment and we'll be using both VM's now, let's change their host names to make it easier to tell them apart. Log in to your rhs1 vm, type in sudo hostname ctl set-hostname rhhost1.[localnet.com](https://localnet.com)
>
> **[2:48](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/knowledge-of-common-mta-programs?u=76281980&t=168)** and hit enter. Type in your password as prompted. You'll have to close your terminal and open it again to see the results. Also, log in to you rhhost2 vm, and set it's hostname to rhhost2.[localnet.com](https://localnet.com). Now, when we're on the terminal, we can identify the host we're logged into easier.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (2)
> **Env Vars:** mta (11), ibm (1)
> **Tools:** terminal (2), command line (1)
> **CLI Commands:** make (1), sudo (1)
> **URLs:** [localnet.com](https://localnet.com) (2)
> **Definitions:** is a  (2)
> **Prerequisites:** configure (2)

#### [Install Postfix](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/install-postfix?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/install-postfix?u=76281980&t=0)** - [Instructor] To get started with Postfix, we'll want to install it first. Just to make sure that Sendmail is not installed, we'll also attempt to uninstall it. Be sure you're logged in to your hrhost1 VM as user1, and have a terminal open. In the terminal type in sudo space yum space remove space sendmail, and hit enter. Type in your password if prompted. If Sendmail is installed, it will remove it now. Otherwise it will provide a warning message and return you to your prompt. Now let's install Postfix. Type in sudo space yum space install, space dash Y space postfix, and hit enter. If it's already installed, we can proceed with the next command. Now let's make Postfix the default MTA. Type in clear. Type in sudo space alternatives, space dash dash set, space MTA, space slash USR, slash sbin, slash sendmail.postfix, and hit enter. Now Postfix is installed and ready.

> [!info]- Semantic Content
>
> **CLI Commands:** sudo (3), make (2), yum (2)
> **Env Vars:** mta (2), usr (1)
> **Prerequisites:** install (3)
> **Tools:** terminal (2)
> **Warnings:** warning (1)
> **Speakers:** - [instructor] (1)

#### [Manage access to Postfix](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-access-to-postfix?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-access-to-postfix?u=76281980&t=0)** - [Narrator] In this chapter we'll look at various Postfix configuration files. We'll start by looking at the access file. We can find this in the etc Postfix with the other configuration files. In a terminal type in "less /etc/postfix/access". And hit Enter. Surprisingly, the entire Postfix access main pages included in the configuration file commented out. Let's zip down to the email section by typing "/EMAIL" in uppercase. Here we see the format of specifying access rules by email addresses. We can use patterns as well specifying just the "tld" or the "domain". Now let's take a look at the host section by typing in "/HOST " in uppercase. Here we see a similar section outlining the patterns for host names which is a bit more complex because we can also match an IP addresses and sider networks. Now let's go to the Accept Action section. Type "/ACCEPT in uppercase. There's only one result for Accept. OK, as is should be. Just below this we have the Reject section. We can reject with different codes and messages here. Just below that we have the Other Action section. Which includes Discard. Which is interesting. It rejects the message. But tells a sending server accepted it. We can also use regular expressions that match. Let's go there by typing "/REGULAR in uppercase. This section explains how to use regular expressions. Which we won't go in to any further. Just know it's here. Now let's look at some examples. Type in /EXAMPLE in uppercase. The two examples allow mail from the host
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-access-to-postfix?u=76281980&t=95)** at IP address 1.2.3.4 but denies all other hosts on that network. This could also be '"www.[example.com](https://example.com) ok" for accept. Or, "www.[example.com](https://example.com) reject". Also know that before anything in this file is used you need to convert it to a Postfix database file using the Postmap command. If you don't you're access control won't take effect. Press "q" to quit less. Now type in "sudo postmap /etc/postfix/access". And hit Enter. Type in your password if prompted. If it's successful there will be no message. We can however verify by typing in "ls /etc/postfix" and hit Enter. We see that we have an "access.db" file now. This is the file we just created.

> [!info]- Semantic Content
>
> **Env Vars:** email (1), host (1), accept (1), regular (1), example (1)
> **CLI Commands:** find (1), sudo (1), ls (1)
> **URLs:** [example.com](https://example.com) (2)
> **Versions:** 1.2.3 (1)
> **Tools:** terminal (1)
> **UI Navigation:** go to (1)
> **Speakers:** - [narrator] (1)

#### [Configure a local mail server](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-a-local-mail-server?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-a-local-mail-server?u=76281980&t=0)** - [Instructor] To configure a local mail server, we'll want to start by editing the etsy Postfix main dot c-f file using VI. On your rhhost1 VM open a terminal and type in sudo, space, v-I, space, slash, e-t-c, slash, Postfix, slash, main dot c-f and hit enter. Enter your password if prompted. The first parameter we'll edit is my host name. Press forward slash and type in myhostname and hit enter. Go into insert mode and change the value to rhhost1.[localnet.com](https://localnet.com). And make sure it's uncommented. And then go down to my domain and change that to [localnet.com](https://localnet.com) and also make sure it's uncommented. Now scroll down again until you see my origin. We can change this to rhhost1.[localnet.com](https://localnet.com) or we can use dollar sign myhostname which contains the same value. As such, we just need to uncomment the line that includes myhostname. Next, let's double check which network interfaces Postfix will answer on. Scroll down again until you see inet interfaces. We need to make sure this says all. You may have to comment out the current line and uncomment the line that says all or just edit any line directly. Now let's edit my destination. Press escape and then forward slash and type in mydestination. Press n to go to the next instance. Make sure this includes myhostname, local.mydomain, localhost, and mydomain. We will comment out the default line and uncomment the one that includes mydomain. Now press escape and type forward slash again. Type mynetworks and hit enter.
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-a-local-mail-server?u=76281980&t=95)** You can press n multiple times to step through the instances of the [[Microsoft Word|word]] mynetworks or just scroll down. We need to change the values to 192.168.1.0 slash 24 comma space 127.0.0.0 slash 8. If your local network is not 192.168.1, then insert the correct one here. The last thing we'll edit is the DNS look ups. This line is not currently in the file so we'll need to go to the n and add it. Go to the n by pressing escape, capital G, capital A, and hit enter. This will take you to the n of the file and put you in insert mode. Now add disable, underscore, dns, underscore, lookups, space, equals, space, yes. This instructs Postfix to use the get address info system library routine which also looks in Now save and exit by pressing escape, colon, x, exclamation mark, and hitting enter. Let's check the Postfix configuration file now using Postfix check. Type in sudo space Postfix space check and hit enter. If there are syntax errors, you'll want to fix them now. Now let's allow SMTP traffic through the firewall. Type in clear and type in sudo space, firewall, dash c-m-d, space, dash dash permanent, space, dash, dash, add, dash service, space s-m-t-p and hit enter. Now let's reload the firewall rules. Type in sudo space, firewall, dash c-m-d, space
>
> **[3:08](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-a-local-mail-server?u=76281980&t=188)** dash dash reload and hit enter. Now let's verify that Postfix is running and we'll start on reboot. Type in clear again then type in sudo, space, system c-t-l, space, enable space Postfix and hit enter. And we'll restart this service now that we've changed the configuration file. So bring your line back and change enable to restart. And lastly, let's check the status. Bring your line back again and change restart to status. If everything looks good, we can move on. Now we need to make Postfix the default mail transport agent. Type in clear and then type in sudo space, alternatives, space dash dash set, space m-t-a, space, slash u-s-r, slash, s-bin, slash snmail dot Postfix and hit enter. Now let's create a second user to sn email on. Type in sudo space useradd space user2 and hit enter. Now let's set a password for user2. Type is sudo space p-a-s-s-w-d space user2. Now set a password for user2. Now, let's create an email for user2. Type in clear and then type in date, space, Pipe, space, mail, space, dash s, space, double quote, This is a local test double quote. user2 and hit enter. Now we'll switch over to user2 and check that user's email. Type in s-u, space, hyphen, space, user2 and hit enter. Now enter user2's password.
>
> **[4:42](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-a-local-mail-server?u=76281980&t=282)** To check user2's email on the command line, type in mail and hit enter. We can see that we have an email. To read it, press the number of the email. In our case, it's the number one so press number one and hit enter. We can see the email came from rhhost1.[localnet.com](https://localnet.com). When done, press q to quit. Keep in mind that to troubleshoot this process you can also open a terminal and tail the var log mail log file for messages. Also, now that we have a working mail server, let's create a new snapshot. Press control t and let's call this Working Mail Server and hit enter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **CLI Commands:** sudo (8), make (5)
> **UI Navigation:** scroll down (3), go to (3)
> **URLs:** [localnet.com](https://localnet.com) (4)
> **Versions:** 192.168.1 (2), 127.0.0 (1)
> **Tools:** terminal (2), command line (1)
> **Env Vars:** dns (1), smtp (1)
> **Warnings:** keep in mind (1), troubleshoot (1)

#### [Create email aliases](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/create-email-aliases?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/create-email-aliases?u=76281980&t=0)** - [Instructor] The "Aliases" file is a file that forwards email from one recipient to another local recipient. Unlike the other files we've looked at, this file has non-commented working lines. So let's take a look at it. On your Rhhost1 VM in a terminal, type in, "less /etc/aliases" and hit "Enter". Notice the path to this file is different than the others, as it's an Etsy and not an Etsy post fix. The format is pretty simple. Any mail sent to the recipient on the left, gets forwarded to the recipient on the right. Notice that there are a lot of service names. Any mail sent to the names of these services, will get forwarded to the root account. So the root user will see them. Notice at the very bottom of the file, we can also decide gets the emails to root. Since it's not a good idea for someone to be logged in as root, we can have all emails to root be forwarded to a user account. In our case, maybe we'd want them sent to the user named "user1". Another interesting use is that we could forward emails for accounts that don't exist. Let's say a user no longer works for a company, but there still may be email coming in for that person. We can forward that email to a current user, that took over his or her job. For instance, "Bob user1". In this case Bob no longer works for the company, so we have his email forwarded to user1, who took over his job. Another possibility is to create an email account for a group of users. Say, support. Then forward that email to each person doing that job. In this case, "support user1 user2 user3". Any email sent to support will get forwarded to user1, user2 and user3. Before this file can be used, we need to convert it to an indexed database file.
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/create-email-aliases?u=76281980&t=94)** But we don't use the postmap command for it. We have a special command just for the Aliases file, called "newaliases". Type in "sudo newaliases" and hit "Enter". And the aliases.db file will be created. We can verify by typing in, " ls /etc/aliases* " and hit "Enter". Because this file is in the "/etc", instead of "/etc/postfix" we want to be more specific in listing just the alias's files. So we use the asterisk.

> [!info]- Semantic Content
>
> **CLI Commands:** sudo (1), ls (1)
> **Tools:** terminal (1)
> **Definitions:** is a  (1)
> **Analogies:** for instance (1)
> **Speakers:** - [instructor] (1)

#### [Forward mail as a user](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/forward-mail-as-a-user?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/forward-mail-as-a-user?u=76281980&t=0)** - [Instructor] System wide email aliases are stored in the /etc/aliases file and must be managed by the system administrator. This file must be compiled by the newaliases command to create an aliases database before it can be used. We can do this just by running the newaliases command without options. Host space looks into its configuration files to figure out where the aliases file is. However, regular users can forward email of their own by using a .forward file in their home directory. This is their individual email forwarding file. As such, the user doesn't have to be root to configure it. By using a .forward file, the user can forward to a local user or even an outside email address. Take note that this file needs to be readable by the user. The easiest way to ensure this is to create it as that user. This is the simple as creating a file named .forward with a text editor, including anything that we would've put in the right hand side of the aliases file. For instance, if user one wanted all of their email forward to their personal account on Gmail, they'd just type that address into their .forward file. When mail is destined for the user, the MTA looks at the .forward file to see if any additional processing is necessary. If there's a forward in that file, the MTA will send the mail on in the same manner as if it had been in the aliases file. Unlike the aliases file, this does not need to be compiled and will be consulted each time email comes in for that user.

> [!info]- Semantic Content
>
> **Env Vars:** mta (2)
> **Analogies:** for instance (1)
> **Warnings:** note that (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)


### 9. Manage Printers and Printing

[↑ Back to Table of Contents](#table-of-contents)

#### [Configure local and remote printers with CUPS](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-local-and-remote-printers-with-cups?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-local-and-remote-printers-with-cups?u=76281980&t=0)** - [Instructor] Early computers used line printers which printed one line at a time. The printers were slow, so there needed to be a print queue or spool to line up print jobs. The print queue or spool would line up print jobs from multiple computers and give them to the printer as the printer was available. Initially [[Linux]] used BSD's printing system, which comprised of a service called LPD, or Line Printer Daemon. There were clients such as LPR that would send print jobs to the print server using the LPD protocol. When graphics printing came out, there needed to be a way to tell the printer how to print graphical images. So the PostScript language was created. Users would create a graphical document in either black and white or color which would be saved as a PostScript file. PostScript is a programming language that describes the page. The computer would send this PostScript file to the printer. High end printers had PostScript engines in them called RIPS or raster image processors. That would take the code and run it just the same as we would with bash or [[Python (Programming Language)|Python]] code on a computer. The code would run and output the file in the printer's native language and the printer would print the page. Cheaper printers were sold without raster image processors. And as such, they needed computers that had software raster image processors built in. The software would save the graphic output as a PostScript file. The raster image processor would process the PostScript file into the printers own language, and then it would send that process data to the printer to be printed. This piece of software that converts the PostScript file to the printers language is called a print filter. The print system used on modern Linux distributions is CUPS, originally short for common Unix printing system, but changed a CUPS over legal issues
>
> **[1:32](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-local-and-remote-printers-with-cups?u=76281980&t=92)** concerning the name UNIX, and it's late caps use the LPD protocol but switched to IPP or Internet Printing Protocol. IPP supports printing to local and remote printers access control as indication and encryption. 10 years later, Apple boughtCUPS from the developer and hired him. When you send a print job to a CUPS printer, it goes to the scheduler which sends the job to the filter which converts the print job into the format the printer understands. The filter then passes the data to the backend which sends the data to a physical device or network connection. For physical connections we have serial parallels USB and PDF backends. In addition, there are network backends for IPP, Jet direct for HP printers, LPD for Line Printer Daemon compatible printers, and SMB protocol for [[Windows]] print spools. Through the CUPS LPD helper application, the CUPS scheduler can accept LPD job requests as well. The scheduler also has a graphical web interface. There are different ways of configuring CUPS. You can edit the configuration file at /etc/cups/cupsd.conf use the GNOME Print Manager or use the CUPS Web GUI. In this video, we'll use the Web GUI because it works on every distribution that uses CUPS. First, before we use the CUPS Web GUI we need to change the config to allow the users in the wheel group admin access. In the terminal we'll open the cups-file that confer with VI. We'll need to elevate privileges to edit this file. So type in sudo vi /etc/cups-files.conf and hit Enter. Enter your password if prompted.
>
> **[3:09](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-local-and-remote-printers-with-cups?u=76281980&t=189)** What we're looking for here is this system group line. The default is SystemGroup, space, sys, space, root. Let's add the wheel group to the end of the line, since our admin users are in it. Go into insert mode by pressing the I key and add space wheel. In some distributions this is already done for you. Now Save and Exit by pressing Escape colon x, exclamation mark and hitting Enter. The next thing we have to do is restart the CUPS service. type in sudo, space, systemctl, space, restart, space, CUPS, and hit enter. To access the CUPS configuration web GUI, open a web browser and in the address bar type in localhost, colon, 631, and hit enter. To add a printer click on adding printers and classes. And then click on Add Printer and enter your username and password. You can have FireFox save it if you wish. The next thing is to select one of the network printer types. If you have a real printer to add, and you know the type, you can do that now, I'm going to fake it. This will allow me to send print jobs to it, and they'll sit in the queue allowing me time to manipulate them with other tools. For my fake printer, I'll click on internet printing protocol IPP, and then click on Continue. The next pane will allow me to enter the URL to the printer on the network. For the purpose of this exercise, I'm going to use ibp, colon, slash, slash, 192, dot, 168, dot, two, dot, 100, slash IPP. In my case, there's nothing at this IP address. Again, I'm not trying to make a queue
>
> **[4:41](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-local-and-remote-printers-with-cups?u=76281980&t=281)** that works I actually want the opposite. If you're setting up a real print queue, you'd want to put in the real information here. I'll click on Continue. I'm going to name my printer bogus printer one with no spaces. If I click the Share this printer checkbox, other clients could print to it through this host. This host would be the principal for those computers. I'll leave this unchecked to keep our setup simple, click on continue. Now it's asking me to select the type of printer. If this were a real printer, it may query it and choose the driver automatically. But since I've given it a printer that is impossible to query, I'll just choose hp. Click on continue. For the model, I'll choose the printer at the top, we'll pretend it's an HP 910 and click on Add Printer. The last page asked for print options. Just click on set default options. We now have a printer setup. To make the printer default, we can either click on bogus printer one or wait, or wait for the timeout and then click on the administration menu. In this menu, you'll see an item named set as server default, click that. Now our new printer is the system default. The last thing we'll do is install a package of print filters called Foomatic. Go back to your terminal and type in sudo, space, yum, space, install, space, dash y, space, foomatic, dash, filters and hit enter. This filter converts PostScript to raster and we'll need it for our bogus printer. The exercises in this course will work fine without Foomatic but you may get error messages

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (2), [[Python (Programming Language)|Python]] (1), [[Windows]] (1)
> **Env Vars:** cups (12), lpd (6), ipp (5), gui (4), bsd (1)
> **UI Navigation:** click on (11), open the (1), checkbox (1), select the (1)
> **CLI Commands:** sudo (3), make (2), python (1), systemctl (1), yum (1)
> **Tools:** terminal (2), bash (1), firefox (1)
> **Prerequisites:** setup (2), install (2)
> **Definitions:** is a  (1), is called (1), short for (1)
> **Code Identifiers:** boughtcups (1)

#### [Troubleshoot general printing problems](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/troubleshoot-general-printing-problems?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/troubleshoot-general-printing-problems?u=76281980&t=0)** - [Instructor] In a network environment where multiple users on multiple hosts may be directing print jobs to one central print queue, it might get complex. To help troubleshoot, we can inspect and manipulate print queues using some very basic commands. To get a list of print queues known to the system, type into a terminal, lpstat space dash a, and hit Enter. Dash a shows printer's accepting status. We can see my BogusPrinter1 listed. Now I used the dash p option to show the printer's status. Bring your line back and change a to p, and hit Enter. If it gives you an error that it can't find foomatic dash rip, and you've installed the foomatic dash filters package, then you need to restart cups. Type in sudo space systemctl space restart space cups, and hit Enter, then bring your line back and run it again to see if the message is gone. Now let's use the dash s to show the printers and classes. Bring your line back again, change dash p to dash s and hit Enter. We should see our BogusPrinter1 with its location information. We can also use the cups webGUI to see some information as well.

> [!info]- Semantic Content
>
> **CLI Commands:** find (1), sudo (1), systemctl (1)
> **Code Identifiers:** webgui (1)
> **Tools:** terminal (1)
> **Warnings:** troubleshoot (1)
> **Speakers:** - [instructor] (1)

#### [Manage user print jobs](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-user-print-jobs?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-user-print-jobs?u=76281980&t=0)** - [Narrator] On occasion we have to check on user print jobs. This exercise we'll need some print jobs. Click on Applications then go down to Activities Overview. And click on Printers. This is the Gnome Printer GUI, this tool is best used for simple print setup, as it doesn't have all the power of the CUPS GUI. Click on the gear icon next to our bogus printer. And then click on Printing Options. In the resulting dialogue in the top left, we have a Test Page button. This prints a test page, click it several times. Now close this window. We can see from the GUI that we have three jobs in the BogusPrinter1 queue. Now close this GUI. The first thing we'll do on the command line is see if any print jobs are queued currently. For this we'll use the lpq command with the all option. Type in lpq space dash A and hit enter. If you want to specify which printer to query, use the dash capital P option, followed by the printer name. We see from this output that we have three print jobs in the queue. We can see they're owned by user1 and what their print job numbers are. If you run lpq with pseudo you will get all print queues. A user can only manipulate their own print jobs, but Root can manipulate any job. If you want to remove a print job from the queue, we can use the lprm command. Let's remove a print job. We can see in the output of lpq what the print job numbers are. Now to remove a print job, specify it by a job number. Let's remove the first job by typing in lprm space one and hit enter. And verify by bringing back your lpq line and hitting enter again. Notice that the first job is now gone. If you need to alter other aspects of the print job,
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manage-user-print-jobs?u=76281980&t=93)** you can use the lp command. For instance, if you wanted to change the print priority or the number of copies. Dash i specifies the job number, dash n is the number of copies, and dash p is the priority of the print job. If we have a print queue that is not working, with jobs waiting to print, we may want to move those jobs to another queue. We can use the lp move command for this. First, we get a list of our print queues using lpstat space dash S. Then we get a list of our print jobs with lpq space dash A. Now that we have the names of the print queues and the jobs we can move the second job from BogusPrinter1 to BogusPrinter2 by typing in lpmove BogusPrinter1 dash two space BogusPrinter2. This would be move job number two from BogusPrinter1 to the BogusPrinter2 print queue. To print text from the command line we can use the lpr command. First, let's create a file to print. Type in clear then type in echo space double quote very small print file double quote space greater than space printfile.txt and hit enter. Now to print it, type in lpr space printfile.txt and hit enter again. We can verify this with lpq. Type in lpq space dash A and hit enter. Now our print job number four is printfile.txt

> [!info]- Semantic Content
>
> **Env Vars:** gui (4), cups (1)
> **UI Navigation:** click on (4)
> **File Paths:** printfile.txt (3)
> **Tools:** command line (2)
> **Analogies:** for instance (1)
> **Prerequisites:** setup (1)
> **Speakers:** - [narrator] (1)


### 10. Fundamentals of Internet Protocols

[↑ Back to Table of Contents](#table-of-contents)

#### [About private and public "dotted quad" IP addresses](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-private-and-public-dotted-quad-ip-addresses?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-private-and-public-dotted-quad-ip-addresses?u=76281980&t=0)** - [Instructor] Each computer or device on a TCP/IP network will have an IP address. This could be an IP version four address or an IP version six address. We won't go into details about the differences in this video, but just know that every device has an address of either type, or both. IP version four has a 32 bit address space, meaning there are roughly four billion IP version four addresses. However, there are 18 million addresses reserved for private space, and 270 million addresses reserved for multicast networks. Each IP version four address comprises of four dotted quad octets. They're called octets because they're made up of eight bits each. Four octets of eight bits makes 32 bits total. These octets are delimited by dots. Since an octet is an eight bit number, its values range from zero to 255 in decimal notation. There are several ranges of private IP addresses. These are 192.168.0.0 to 192.168.255.255, which comprises 65,000 IP addresses. 172.16.0.0 to 172.31.255.255 which comprises of about one million IP addresses 10.0.0.0 to 10.255.255.255, which comprises about 16 million addresses. These are private address ranges and are not routable across the internet. This means they're free for anyone to use as long as these addresses are translated to public addresses before the data enters the public Internet. We usually do this with NAT or network address translation. There are some other address blocks
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-private-and-public-dotted-quad-ip-addresses?u=76281980&t=93)** that we can't use for public networking. The address range of 169.254.0.0 to 169.254.255.255 is only used for link local addressing, meaning they're used when an IP address can't be assigned automatically. Every host also has non-routable addresses called loopback addresses. Those addresses are from 127.0.0.0 - 127.255.255.255 These addresses are only used inside of our computer and are never routed over a network. Any packets received on the loopback address must be dropped because our host should never receive one outside its own network interface. Addresses ending in zero or 255 are also special. The dot zero addresses define the network or sub network and addresses ending in .255 are for the broadcast address. The broadcast address is one address that lets data be sent to every host on the network. Generally, we are given a public IP address from our internet service provider, which is assigned to our router. Behind the router we use one of these private address ranges for our local hosts. When a local host wants to communicate with the outside public world, their address is translated to the public address. Return traffic is then translated back to the private address. This lets us get by with fewer public addresses, as most computers and internet cafes, private homes, and business offices are using private addresses behind NATs.

> [!info]- Semantic Content
>
> **Versions:** 192.168.0 (1), 192.168.255 (1), 172.16.0 (1), 172.31.255 (1), 10.0.0 (1)
> **Env Vars:** tcp (1), nat (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Network masks and CIDR notation](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/network-masks-and-cidr-notation?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/network-masks-and-cidr-notation?u=76281980&t=0)** - With IPv4 addresses, there are four octets of eight bits each, making up a 32-bit number. In decimal, this ranges from 0.0.0.0 to 255.255.255.255 It's a bit clumsy to work with in decimal, so let's convert it to binary. The numeric range in binary is 00000000.00000000.00000000.00000000 to 11111111.11111111.11111111.11111111 This might not look better, but it'll make more sense when we start breaking up networks into sub networks. Initially, the network portion of an IP address started with a zero as its higher-order bit and used seven bits of the first octet for the network address. This means we had seven bits left for networks, which gave us 127 networks. The [[Representational State Transfer (REST)|rest]] of the IP address space was used for hosts. Since we have 24 bits for hosts, we have around 16 million hosts. This was fine when we only had a few companies needing very large networks. But it became clear that we need more networks and they didn't have to be this big. In 1981 the address range was divided into classes. The original networks were now Class A networks. The next class would be Class B. Class B use 16 bits for the network portion and 16 bits for the host portion. It used the binary number 10 as its high-order bits, which left 14 bits for network addresses. This means we have 16,384 Class B networks and 65,536 hosts on each. We don't need to reserve bits on the host portion, so we get the full 16 bits worth. The third class is Class C. Class C uses 24 bits for the network portion
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/network-masks-and-cidr-notation?u=76281980&t=94)** and eight bits for hosts. It uses the binary number 110 as its higher-order bits. This leaves 21 bits left for networks or roughly two million. It uses eight bits for host or 256 in decimal. Keep in mind that for each network the lowest number is the network address and the highest is the broadcast. So we need to subtract two from each of these host numbers. The Class D networks are reserved for multicasting. The Class E networks are defined as experimental. We have another number called the network mask, which is used to determine how much of the address spaces is for networks and how much is for hosts. For Class A, the network portion is eight bits. If we put ones in every spot that the network occupies our network mask in binary would be 11111111.00000000.00000000.00000000 or 255.0.0.0 in decimal. For Class B networks it would be 11111111.11111111.00000000.00000000 or 255.255.0.0 For Class C it would be three octets of ones and then one octet of zeros or in decimal 255.255.255.0 There may be times where you don't want all of your hosts in the same network, but you are only given one network. In this case, you can subnet or divide your network. This means that you take one bit from the host section and add it to the network section to double the number of networks to two and half the number of hosts on each network. In this case, our network mask in binary would look like this. If we convert the binary number one, followed by seven zeros, into decimal, we end up with 128.
>
> **[3:08](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/network-masks-and-cidr-notation?u=76281980&t=188)** So our network mask in decimal would be 255.255.255.128. We'd be using the one extra bit for our network addresses, which gives us two networks and seven bits for host, which gives us 128 hosts each. Once again, keep in mind that the lowest host number is the network address and the highest is the broadcast. Since we have two networks, we're now using four numbers for networks and broadcast addresses. Let's say we apply this to the 192.168.2.0 network. Normally, 192.168.2.0 would be the network address and 192.168.2.255 would be the broadcast. Since we have now divided this into two networks, the first network's addresses would go from 192.168.2.0 to 192.168.2.127 Whereas zero being the network address and 127 the broadcast. Usable IPs would go from one through 126. The second network would comprise of 192.168.2.128 to 192.168.2.255 With 128 being the network address and 255 the broadcast. Usable IPs would number from 129 to 254. The subnet mask can be written in two different [[Forms]]. As dotted-quad notation or CIDR, which is short for classless inter-domain [[Routing]]. To convert to dotted notation, let's look at our last octet. If we subnet, we're stealing one bit from the host and putting it in the network section. So, we need to convert to decimal, 11111111.11111111.11111111.10000000 This binary number converted to decimal is
>
> **[4:45](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/network-masks-and-cidr-notation?u=76281980&t=285)** 255.255.255.128 because the one followed by seven zeros converted to decimal is 128. This is our network mask in dotted-quad notation. With CIDR, we just append the number of bits in the network mask to the IP address. So, ours would be /25. For instance, our IP and subnet mask could be 192.168.2.100/25 Simple, right? If you do a slash 26 subnet, you now have four networks with 64 hosts and each and so on. CIDR is very easy to work with. I recommend that you think in CIDR and then convert back to dotted notation whenever you need.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Representational State Transfer (REST)|Rest]] (1), [[Forms]] (1), [[Routing]] (1)
> **Versions:** 192.168.2 (8), 255.255.255 (4), 11111111.11111111.11111111 (2), 0.0.0 (1), 00000000.00000000.00000000 (1)
> **Env Vars:** cidr (4)
> **Definitions:** defined as (1), means that (1), short for (1)
> **Warnings:** keep in mind (2)
> **CLI Commands:** make (1)
> **Analogies:** for instance (1)
> **Speakers:** - with (1)

#### [Common TCP and UDP ports and services](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/common-tcp-and-udp-ports-and-services?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/common-tcp-and-udp-ports-and-services?u=76281980&t=0)** - [Instructor] Each host has an IP address, used to route traffic throughout the network. There needs to be an addressing scheme, inside of our computers to determine, what happens to that traffic, once it arrives at our host and there is. We use a system of ports that directs traffic, through certain servers as listening on them. For instance a web server on a host, with the IP address 192.168.2.100. If a host is trying to access this web server, they'd send an http request message to 192.168.2.100. This traffic would be directed, using network routers to this IP address. Once it arrives at the local network stack, it will be directed to the service listening on port 80. In our example, the web server. The network socket for this combination would be, 192.168.2.100 colon 80, as port 80 is the default port for [[Web Servers]]. Every packet has a source address, and a source port, a destination address and a destination port. Many destination ports are defined by IANA, a government standards organization. IANA stands for the Internet Assigned Numbers Authority. The ports are represented by a 16 bit number, meaning there are roughly 65, 000 of them. The lower 1024 ports are called, the Well-Known Ports, and are reserved for, common communication protocols. For instance, ports 20 and 21 are used for FTP, port 22 is used for Secure Shell, port 23 is used for telnet, port 25 is used for SMTP mail, port 53 is used for DNS, and port 80 is used for web servers. If you get tired of trying to remember these ports,
>
> **[1:32](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/common-tcp-and-udp-ports-and-services?u=76281980&t=92)** you can just view the /etc/services file, in any [[Linux]] distribution. Let's do that now. In a terminal type in, less space /etc/services and hit enter. Here we can scroll through this file. The first column is the port name, the second column is the port number, and transport protocol, the third column is the description of the protocol, and, or service. Many commands in Linux will display the service name, or protocol instead of the port number. To override this you may be able to pass the command, A-N option to display the port number instead. Those commands consult this file, to translate to the port name.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Web Servers]] (2), [[Linux]] (2)
> **Ports:** port 80 (3), port 22 (1), port 23 (1), port 25 (1), port 53 (1)
> **Env Vars:** iana (2), ftp (1), smtp (1), dns (1)
> **Versions:** 192.168.2 (3)
> **Analogies:** for instance (2)
> **Tools:** terminal (1)
> **Definitions:** stands for (1)
> **Speakers:** - [instructor] (1)

#### [Differences and major features of UDP, TCP, and ICMP](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/differences-and-major-features-of-udp-tcp-and-icmp?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/differences-and-major-features-of-udp-tcp-and-icmp?u=76281980&t=0)** - [Instructor] With TCP/IP, there are different transport protocols. These are TCP and UDP. Both TCP and UDP are used to transport payloads of data. Both use [[IP Addressing]], and are forwarded from host to host until they get to their destination. You can think of TCP and UDP as the trucks that carry packages to destination addresses, although they are different in how they do it. TCP, or Transmission Control Protocol, is a connection-oriented protocol, meaning it is considered reliable. It has a three-way handshake to insure that there is reliable communication between hosts. TCP provides acknowledgments after successful delivery of packets, and automatic re-sends in the case of missing data. TCP also cares about the ordering of the packets, so it reassembles the packets at the destination, in order to preserve the integrity of the data. It uses error detection and error recovery. There are checksums on each packet. If the packet arrives damaged, it's resent. UDP or User Datagram Protocol on the other hand is not connection-oriented, nor does it care about the sequence of data. With UDP, there's no tracking of messages and no way of knowing if the packet was delivered or not. UDP does pay attention to checksums on the data, but if it finds a damaged packet, it just discards it, so it uses error detection, but not error recovery. This might seem like a crazy idea, but think for a moment about streaming movies. If a frame gets corrupted, do you want the movie to stop while that corrupted frame is re-sent? Probably not. It's better to drop it and keep the movie going. There's a third low-level protocol called ICMP, or Internet Control Message Protocol. ICMP is an error-reporting protocol
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/differences-and-major-features-of-udp-tcp-and-icmp?u=76281980&t=94)** that network devices use to generate error messages. For instance, if a packet has not been delivered due to a network problem, we'll see an ICMP message. All network devices have the capability to send, receive, and process ICMP messages. It's important to note that ICMP is not a transport protocol like TCP or UDP, and doesn't carry data between hosts. ICMP is not just a protocol for network devices, it can also be used by network admins to troubleshoot network connections using diagnostic tools, such as ping and traceroute. In summary, TCP is used when reliability is more important than speed. UDP is used for speed, and when reliability isn't an issue. And ICMP is used to relay error messages and troubleshoot networks.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[IP Addressing]] (1)
> **Env Vars:** tcp (9), udp (8), icmp (7)
> **Warnings:** troubleshoot (2), note that (1)
> **Definitions:** is a  (1), is an  (1)
> **Analogies:** for instance (1), such as (1)
> **Speakers:** - [instructor] (1)

#### [Knowledge of the major differences between IPv4 and IPv6](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/knowledge-of-the-major-differences-between-ipv4-and-ipv6?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/knowledge-of-the-major-differences-between-ipv4-and-ipv6?u=76281980&t=0)** - [Instructor] In the late 1990s, we thought we would be transitioning to IP version six as it had become a draft standard. It remained a draft standard for nearly 20 years and only as of 2017 became a full standard. The world is still mostly running on IP version four. One of the issues with IP version four is that it has a limited number of possible IP addresses. Although 4.3 billion seems like a lot, we probably exhausted that a long time ago. Most internal networks use network address translation to make one public IP address appear as many, possibly thousands. This has gotten us by so far. However, IP version six has many advantages, including 340 trillion trillion trillion addresses. That's not really a number but the real number of 340 undecillion may not mean a lot to anyone. IP version six also has other advantages over IP version four, including stateless address autoconfiguration, network renumbering, and router announcements when changing connectivity providers. It places packet fragmentation into the endpoints and also [[Network Security]] was a design requirement. You may be asking why we haven't moved to IP version six already if it's so much better. Part of the issue may be that people keep doing what they know. Another thing may be that the addresses look like this, which aren't very readable. Rules exist to improve the situation though. All leading zeroes in a group can be deleted. This shortens the address quite a lot. We can also remove successive groups of zeroes, thus collapsing the address again. This results in an even shorter address. This is much easier to use and possibly remember
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/knowledge-of-the-major-differences-between-ipv4-and-ipv6?u=76281980&t=95)** but still not that user-friendly. [[Linux]] has had IP version six turned on by default for at least a decade or more. It's good to take advantage of that capability if possible.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Network Security]] (1), [[Linux]] (1)
> **CLI Commands:** make (1)
> **Versions:** 4.3 (1)
> **Speakers:** - [instructor] (1)


### 11. Persistent Network Configuration

[↑ Back to Table of Contents](#table-of-contents)

#### [Gather network information](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/gather-network-information?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/gather-network-information?u=76281980&t=0)** - [Instructor] Traditionally, [[Linux]] used the net tools commands including ifconfig, netstat, arp route and iptunnel. These commands have mostly been superceeded by the IP route T- suite of tools including the IP and SS commands as well as others. I find the syntax of the legacy tools easier to remember, but in time they may no longer be available so it's important to migrate to IP route two. We will install the net tools package in this course so we have access to the legacy commands as well as the replacements which are installed by default. You can do this on any of your three [[Virtual Machines]] or all of them. I'm going to use ARP Host one. To do so, log into our ARP Host one and in a terminal type in sudo space yum space install space dash Y space net dash tools and hit enter. Type in your password if prompted when it's done installing, make sure IP route two is installed as well. In enterprise Linux seven IP route two is called IP route. Bring your line back and change the net tools to IP route and hit enter. To get IP address and network mask information using legacy tools, we'll type in clear and then type in ifconfig. From this output we can see the IP version four and IP version six addresses for all network devices. These lines of prefix was the words I net and I net six. To show information for a specific network interface, we can provide it as an argument, type in clear and then type in ifconfig space ETH two ETH two being the name of my network card. If your network card is named something different enter that.
>
> **[1:32](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/gather-network-information?u=76281980&t=92)** We can also see the network mask prefixed by the [[Microsoft Word|word]] net mask. It's interesting to note that you can give ifconfig the network mask insider form, but it prints it back in dot notation. To get IP and network mask information using the newer IP tool type in clear and then type in, IP space ADDR and hit enter. This shows a lot of useful information in a very cryptic form. IP shows the IP address for each network interface as well as the net mask insider form. If you wanted to show network information for only one device, the syntax is slightly more complex than ifconfig. Type in clear and then type in IP space, ADDR space show space, show, space ETH2 and hit enter. This will narrow the output to only the ETH two I nterface. Perhaps the output of ifconfig is more readable than that of IP, but that same output is harder to parse and shell scripts. Using the newer IP command, we can provide the route sub command and get the default gateway. Contrary to what I said earlier about the IB command being harder to read, I think the output of IP route is more readable than the legacy route command. Let's try it. Type in clear and type in IP space route and hit enter. The topline store default gateways by numeric addresses in which network interfaces they use. Now let's look at host names. There are three types of host names, static, transient, and pretty. The static host name is the one stored in etc host name, and can be read by a text viewer. The transient host name is a dynamic host name used by the Colonel. By default, when the system starts,
>
> **[3:03](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/gather-network-information?u=76281980&t=183)** it's set to the static host name, but it can change any time by user or a service such as [[DHCP]] or MDNS. The Pretty host name is a free form UTF8 string of text that is presented to the user. The host name can be up to 64 characters in length, has to be composed of seven bit ASCII, lowercase characters and cannot have spaces or dots. Red Hat recommends both the static and transient host names match the Fully Qualified Domain Name or FQDN used by DNS. Red Hat also recommends that the host name conforms to the format of DNS name labels, even though it's not necessary from a machine perspective. The host name, CTL command enforces that host aims only consist of lowercase a through z, uppercase A through Z. The numbers zero through nine, hyphens, underscores and dots which are used to delimit sections. They cannot, however, begin or end in and die and cannot have two dots immediately next to each other. Here are some examples of valid DNS names. It's worth noting that older systems may not play nice with the underscore character, so may be a good practice to avoid it. To show just a host name, we can use a legacy tool named host name. Type in clear, and then type in host name and hit enter. To get more information, we would want to use the host name, CTL command that comes with system D.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (2), [[Virtual Machines]] (1), [[Microsoft Word|Word]] (1), [[DHCP]] (1)
> **Env Vars:** eth (3), dns (3), arp (2), addr (2), ctl (2)
> **CLI Commands:** find (1), sudo (1), yum (1), make (1)
> **Definitions:** is a  (2), is called (1)
> **Prerequisites:** install (2)
> **Tools:** terminal (1)
> **Analogies:** such as (1)
> **Best Practices:** good practice (1)

#### [Manual network interface configuration](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manual-network-interface-configuration?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manual-network-interface-configuration?u=76281980&t=0)** Network interface configuration files control the interfaces for individual network devices. As the system boots, these files are read and the network interfaces are configured or possibly ignored, depending on the configuration. These files are stored in /etc/sysconfig/network-scripts and named ifcg-name where name is the name of the network interface. For instance, the configuration file for the enp0s3 network interface would be /etc/sysconfig/network-scripts/ifcfg-enp0s3.
>
> **[0:37](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manual-network-interface-configuration?u=76281980&t=37)** The network interface is also named inside of the file as well. Generally it's a good idea to have them match. These scripts are for all network interface configuration including dynamic and manual configs. A dynamic config may have only few lines in it specifying the name of the device and the boot protocol of [[DHCP]]. This example specifies that it should be enabled at boot and uses DHCP for configuration. To create a static interface configuration, we'll change the boot protocol to none. In this case, we're using a 24-bit subnet mask in the prefix line. If we wanted to insert the netmask as dotted notation, we could write it as netmask equals 255.255.255.0. The last line is the statically assigned IP version four address. There are a lot of different options you can add to this file. Let's go over some of the more common ones. Device specifies the network device to configure. IPADDR is for the IP version four address. IPV6ADDR is for the IP version six address. The HWADDR option specifies which network interface to configure if you have more than one. This is used to identify the network interface. The MACADDR option assigns a new MAC-address overwriting the one on the card. BOOTPROTO assigns the boot protocol and can be none for static assignments, bootp for older dynamic assignments, and dhcp for newer dynamic networks. ONBOOT specifies whether the network interface should be enabled at boot. IPV6INIT specifies whether IP version six
>
> **[2:12](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manual-network-interface-configuration?u=76281980&t=132)** should be enabled or not. The next couple need some explanation. The global DNS name server file is /etc/resolv.conf. However, Enterprise [[Linux]] allows interface specific DNS servers using the DNS one equals or DNS two equals options. We would specify the DNS server addresses here. To keep these options interface specific, we need to set PEERDNS to yes. If we change it to no, these DNS servers are then copied to the global/etc/resolv.conf file. Netmask and prefix are both used to set the network mask. Only one is required. If you want to enter the netmask in dotted notation, use NETMASK equals. If you want to use CIDR, then use PREFIX equals. The next two are items are for whether you want network manager controlling the interface, or whether you want non-root users to be able to control it. There are many other options used to tune network interfaces and provide more control. The Red Hat Enterprise Linux six deployment guide actually has more information on these files than the Red Hat Enterprise Linux seven networking guide, so reference that. You might want to note that if you change or edit these configuration files, you'll need to notify network manager. One way to do this is to use nmcli to reload the configuration. In a terminal, you would type nmcli space c for configuration, space reload and hit enter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[DHCP]] (3), [[Linux]] (3)
> **Env Vars:** dns (6), dhcp (2), ipaddr (1), ipv6addr (1), hwaddr (1)
> **Prerequisites:** configure (2), you'll need (1)
> **Versions:** 255.255.255 (1)
> **Tools:** terminal (1)
> **Analogies:** for instance (1)
> **Warnings:** note that (1)

#### [Configure networking using Network Manager](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-networking-using-network-manager?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-networking-using-network-manager?u=76281980&t=0)** - [Narrator] The preferred method of managing network connections on Enterprise [[Linux]] 7, is by way of NetworkManager. If you want to manually control a connection you can keep NetworkManager running and have it ignore that connection. For all other networking needs, NetworkManager works great. NetworkManager comes with four different programs to configure the network, which I've mentioned before. We're going to focus on NMTUI, the NetworkManager Text User Interface, and NMCLI, the NetworkManager Command Line Interface. We installed NMTUI in chapter four, but if for some reason you don't have it, install the NetworkManager-TUI package. Once it's installed, type in NMTUI and hit enter. Once the interface appears, you can navigate with the arrow keys, or press tab to step forward, and shift tab to step back through options. Press enter to select an option, and press space to toggle check boxes. NMTUI is easy to use, but has some limitations such as, it can't edit VPNs or wireless networks using WPA Enterprise. You can always use NMTUI, and as a functionality you desire isn't available, move to other methods. Let's move down to quit, and hit enter. The other tool that we can use from the command line is NMCLI, which is installed by default along with NetworkManager. There are manual pages available for NMCLI, including examples. Let's check out the man page of examples by typing in man NMCLI-examples, and hit enter. We won't read all of these examples as they're long and beyond the scope of this video, but we will go use some examples
>
> **[1:32](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-networking-using-network-manager?u=76281980&t=92)** of how to use NMCLI. It's a good idea to keep this manual page in mind when you're learning NCMLI, as it has both simple and complex examples. Press Q to quit. If you followed the manual network configuration video in this course, and created a manual network interface configuration file, you will need to notify NetworkManager of it. Any time you manually edit a network configuration file you need to notify NetworkManager. To do so, type sudo NCMLI C for configuration, space reload, and hit enter. Type in your password, and hit enter again. This is a shortcut for NMCLI con reload, which is a shortcut for NMCLI connection reload. Most NMCLI options have short and shorter options. Choose the amount of verbosity you feel comfortable with. You can also just reload one configuration file by providing it as an argument. For instance, if I wanted to reload the IFCFG-ENP0S3 file, I would type sudo NMCLI con load space, then the path to the interface file, which is /etc/sysconfig/network-scripts/ifcfg-enp0s3, and hit enter.
>
> **[2:53](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-networking-using-network-manager?u=76281980&t=173)** Note that you can specify multiple files here. If you make changes to these files using NetworkManager tools, such as NMCLI, you don't have to reload the configuration, but you do have to bring the connection down and back up again. Type in clear, and then type in sudo NMCLI dev disconnect space, and then the device name, which in my case is EMP0S3, and hit enter. To bring it back up, we type in sudo NMCLI con up EMP0S3, and hit enter.
>
> **[3:30](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-networking-using-network-manager?u=76281980&t=210)** You can take down and bring up any interface using this method. We've previously covered how to add IP addresses to interfaces in this course, so we won't go over that again, but will focus on more general options. To show all network connections, type in NMCLI con show, and hit enter. You only show active connections, add the --active option. Bring your line back, and add --active to the end, and hit enter. Because I only have two network connections and they're both active, the output ends up being the same. To see NetworkManager status, type in clear, and then type in NCMLI general status, and hit enter. This shows the status of connections and can be quite useful as a quick overview. The last thing we'll cover with NMCLI is it's interactive connection editor. This is different than the commands we have used to edit settings previously. Type in NMCLI con edit, and hit enter. This is an interactive tool that gives you possible options. It's now asking me what type of connection I want to edit. I can choose generic, ethernet, [[Wi-Fi|WiFi]], WiMAX, Bluetooth, and so on, it's very powerful. After I choose a mode, it will give me more options. I will choose ethernet by typing in ethernet, and hit enter. Now I can type in a question mark, or the [[Microsoft Word|word]] help, to get more possible options. I typed in question mark and hit enter. These options would be used to add or remove connections, edit properties, or just show information.
>
> **[5:04](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-networking-using-network-manager?u=76281980&t=304)** Let's type in quit to exit this program. I don't use this mode as I usually know what I want to do ahead of time, but it may be a good tool while you're learning how to use NMCLI.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1), [[Wi-Fi|Wifi]] (1), [[Microsoft Word|Word]] (1)
> **Env Vars:** nmcli (16), nmtui (5), ncmli (3), emp0s3 (2), tui (1)
> **CLI Commands:** sudo (4), make (1)
> **Definitions:** is a  (2), is an  (1)
> **Analogies:** such as (2), for instance (1)
> **Tools:** command line (2)
> **Prerequisites:** configure (1), install (1)
> **Documentation:** man page (1)

#### [Configure networking using the GUI](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-networking-using-the-gui?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-networking-using-the-gui?u=76281980&t=0)** - [Instructor] You can configure most network settings using the GUI but not all. Complex operations require going to the command line or editing files. Let's take a look at the GUI networking tools. Let's go to the applications menu. Then down to System Tools and over to Settings. Now double click on Network. This is the same interface that you'll get if you click the top right-hand menu, choose a network connection, and then click on the Settings item. Here we can click on the gear icon in the bottom right-hand corner of the window to edit settings. Let's click on Security in the left-hand pane. Here we can edit the 802.1x Security Settings. By clicking on Identity we have access to the interface name, Mac address, MTU size and Firewall Zone. Next is the IP Version 4 IP Address section. Here we can choose the method in which we want to configure the connection. Mine is on dynamic using [[DHCP]]. In this mode I can still override the DNS and static route settings. If we switch this over to manual we're basically building an IFCFG configuration file but in the GUI. Here we can add or edit the IP Version 4 address, Netmask and default Gateway. We can also add more IP addresses like we did on the command line earlier in this course by clicking on the plus button. We can also add DNS servers and static routes if we wish. The IP Version 6 section mimics it's IP Version 4 counterpart. The last section, named Reset, is to clear all settings and forget an interface.
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-networking-using-the-gui?u=76281980&t=94)** Let's close this dialogue and go back to the main configuration window. Under the Network Connections we see plus and minus signs. This allows us to add new connections to the ones that Network Manager already recognizes. Click the plus symbol. Depending on your version of Network Manager and your installed software you may be able to connections for VPN, Bond, Team, Bridge and Vland. In my case I only see VPN connections. Let's close this window. The last GUI tool we'll look at is the Network Connection Editor. Open a terminal and type, nm-connection-editor and hit enter. This offers some functionality that's not in Control Center. If we click on the add button we can set up a new connection type. This new connection could be one of Bluetooth, DSL, Infiniband, Mobile Broadband, and other connections that were not listed in the other GUI. This tool gives a lot of power that is normally reserved for the command line. Setting up each one of these connection types is beyond the scope of this course but I recommend checking out the Red Hat Enterprise [[Linux]] 7 Networking Guide on Red Hat's website.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[DHCP]] (1), [[Linux]] (1)
> **Env Vars:** gui (5), dns (2), vpn (2), mtu (1), dhcp (1)
> **UI Navigation:** click on (5), go to (1)
> **Versions:** version 4 (3), version 6 (1)
> **Tools:** command line (3), terminal (1)
> **Prerequisites:** configure (2), set up (1)
> **Cross-References:** earlier in (1), go back to (1)
> **Speakers:** - [instructor] (1)

#### [Awareness of systemd-networkd](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/awareness-of-systemd-networkd?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/awareness-of-systemd-networkd?u=76281980&t=0)** - [Instructor] Networking under [[Linux]] is very powerful, but managed by many different tools. On Red Hat based distributions, we may edit ifcfg files in /ect/sysconfic/network-scripts. On Debian based distributions, we can edit the /ect/network/interfaces file. On SUSE, we'd edit ifcfg files in /ect/sysconfig/network. For dynamic interfaces, we could use WICD or NetworkManager. In recent years, most distributions have moved to NetworkManager, which is still the most powerful dynamic network management stack on Linux. However, that may change in the future as the developers of systemd have created networkd. Systemd-networkd is a system service that manages networks. It detects and configures network devices as they appear, as well as creating virtual network devices. Networkd isn't as powerful as NetworkManager, but currently can manage both static and dynamic connections. Systemd-networks will look for network configuration files in /usr/lib/systemd/network, /run/systemd/netowrk, and lastly /ect/systemd/network in that order. Each configuration found in these directories will have a .network file extension. In each file, there will be a match criteria. Any devices not matched by a .network file are ignored. In this file, there's a section named match, which matches a piece of data such as a network device, name, MAC address, driver, device path, type, SSID, or a property,
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/awareness-of-systemd-networkd?u=76281980&t=93)** such as a vendor name, or the model of the card. The link section is used to configure low level link information such as ARP or multicast. The network section is for configuring the network itself. This is equivalent to the ifcfg configuration files. The route section is for [[Routing]] information. The [[DHCP]] server section is for DHCP server information. The bridge section is for configuring network bridges. And there are more sections that we won't go over here. A simple static network configuration file looks like this. I think you can see from this file that the syntax is clean and easy to read. The match section at the top matches the network device by name. The network section assigns the IP address, netmask, and default gateway. Note that networkd may need to be installed before the configuration directories will appear. In CentOS, if we want to install it, we would type sudo space yum space install space -y space systemd-networkd. Also note that systemd-networkd requires systemd-resolved to manage name resolution. If you want to switch from NetworkManager to networkd, we stop one service and start the other. With this comes the networkctl command to manage our network interfaces.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (2), [[DHCP]] (2), [[Routing]] (1)
> **Env Vars:** dhcp (2), suse (1), wicd (1), mac (1), ssid (1)
> **Analogies:** such as (3)
> **Prerequisites:** install (2), configure (1)
> **CLI Commands:** sudo (1), yum (1)
> **Warnings:** note that (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 12. Basic Network Troubleshooting

[↑ Back to Table of Contents](#table-of-contents)

#### [About routing](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-routing?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-routing?u=76281980&t=0)** - [Instructor] [[Routing]] is the process that allows one system to find the network path to another. Routing is often handled by devices on the network dedicated to routing traffic called routers. Although, any computer or programmable network device may be able to act as a router. Usually a host doesn't have to have complex routing defined as there are rules for where traffic goes depending on where it comes from. Scenario one, if the source and destination hosts are on the same physical network, the traffic is forwarded to the destination directly, usually by the network switch. Scenario two, if the source and destination hosts are not on the same physical network, then all defined routes in the routing table are tried one after another. These routes could be dynamic routes added by our [[DHCP]] server or they could be static routes that we've added manually. If a proper route is found, the packet is forwarded. Scenario three, if a proper route is not found, then the packet is sent to the default gateway, which would be configured either dynamically via DHCP or manually via the system administrator. The default gateway then attempts to find a proper route for the packet and sends it on. If it cannot, it will send it to its default gateway.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Routing]] (5), [[DHCP]] (2)
> **CLI Commands:** find (2)
> **Env Vars:** dhcp (2)
> **Speakers:** - [instructor] (1)

#### [View the routing table](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/view-the-routing-table?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/view-the-routing-table?u=76281980&t=0)** - [Instructor] To view your [[Routing]] table, you can be logged into either of your VMs. I'm logged into my rhhost2 VM. In a terminal type in ip space route and hit Enter. Let's look at the different parts of this output. The left hand column is the destination network address with subnet mask. The [[Microsoft Word|word]] default signifies the default gateway address where traffic is sent if there's no available route. The second section encompasses two words, dev and the name of the device for the network. This could be a physical or virtual network device. The third section also encompasses two words, proto followed by the protocol. The kernel protocol says that the kernel installed the route during auto configuration. These protocols are defined in the /etc/iproute2/rt_protos file. If this section says proto static, it means the route was set by the administrator and overrides the dynamic routing. The next section determines the scope of the destination. The /etc/iproute2/rt_scopes file defines these. The next section is for the source address. For many lines, this will be the last piece of data. However, you may have another section displaying the metric, this would display the cost of using the route or the number of hops. Local networks would be one hop and remote networks more.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Routing]] (2), [[Microsoft Word|Word]] (1)
> **Code Identifiers:** rt_protos (1), rt_scopes (1)
> **Tools:** terminal (1)
> **Speakers:** - [instructor] (1)

#### [Manually configure routing using iproute2](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manually-configure-routing-using-iproute2?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manually-configure-routing-using-iproute2?u=76281980&t=0)** - We can add static routes using the IP route command. We can also add static routes using the legacy route command, although it needs to be installed first. However, if you want your static routes to survive a reboot, you'll need to add them to configuration files in /etc/sysconfig/network-scripts. When we create a network interface configuration file, we name it ifcfg-interface name, such as ifcfg-eth0. For static routes, we name the configuration file route-interface name. For instance, if we wanted to add a static route for the eth0 interface, we'd create a file called route-eth0. To add a route manually using the IP command, we'd type ip route add 192.168.100.0, which is the network address, /24 which is the subnet mask, 192.168.100.1, which is the gateway, dev team0, which is the interface. If you typed in ip route, you would see the results of this line. The legacy route version of this command line is a bit longer. We'd type in route add -net 192.168.100.0 netmask space 255.255.255.0 gw for gateway, space 192.168.100.1 dev team0. However, if you reboot after setting the static route with either of these commands, it will be gone. To make it persistent, we'll want to add it
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/manually-configure-routing-using-iproute2?u=76281980&t=93)** to a configuration file. As such, we'd create a route-team0 file in /etc/sysconfig/network-scripts. Inside this file, we'd add address0 equals 192.168.100.0. Netmask equals 255.255.255.0. Gateway equals 192.168.100.1. The device name is derived from the file name itself. This would be equivalent to a manual IP route example earlier, but it would survive a reboot.

> [!info]- Semantic Content
>
> **Versions:** 192.168.100 (6), 255.255.255 (2)
> **Analogies:** such as (1), for instance (1)
> **CLI Commands:** make (1)
> **Tools:** command line (1)
> **Definitions:** is a  (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - we (1)

#### [Debug problems associated with the network configuration](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/debug-problems-associated-with-the-network-configuration?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/debug-problems-associated-with-the-network-configuration?u=76281980&t=0)** - [Instructor] It's very common to have to troubleshoot network problems. We may configure the network according to the information we have, and if it doesn't work, we need to figure out why. You'll want both of your VMs up and running for this exercise. Remember that we only need about five things to have a working network. An IP address, a network mask, a default gateway, a nameserver if you want name resolution, and of course low-level device configuration. The first thing you might want to do is make sure that your ethernet card is talking to your network. Bad drivers or cables could mean you're in a degraded state. Let's use ethtool for this. My network interface is ENP0S3, so I type in ethtool space ENP0S3 and hit enter. Here we see our speed is good, and that we're running at full duplex. If you don't see this, you'll need to troubleshoot it further. For the IP address, we can either set it manually or use [[DHCP]]. If we're using DHCP, the [[Representational State Transfer (REST)|rest]] of the settings will probably come along automatically if they're configured right on the DHCP server. Whether we use static assignment or dynamic, we need to go over some tools to troubleshoot our configuration. The next thing we'll do to troubleshoot is get our IP address and then ping it. Get your IP address using the IP or if config commands. Type in clear, then type in IP space addr and hit enter. IP is the recommended tool for this, but I still prefer the output of if config for interactive use. I can see my IP address as 192.168.2.236. Type in clear and then ping your IP address by typing in ping, and then your IP address. For me it would be ping space 192.168.2.236. And hit enter.
>
> **[1:36](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/debug-problems-associated-with-the-network-configuration?u=76281980&t=96)** We see that it works. Press control C to quit the ping. We can reach our IP address, so our traffic is able to make it to the switch and back. Now we need to need to ping another host in the local network. For this, we'll use our RH host two VM. The address of mine is 192.168.2.228. If the IP address of your RH host one and RH host two VMs are the same, you probably didn't choose to have new mac addresses assigned when you cloned it. You can check if the mac addresses are the same with the if config command. The quickest solution is to delete the VM and re-clone it; However, if they have different IP addresses, then type in ping space and then your HR host two IP address. Mine is 192.168.2.228. This works as well. Press control C. Remember that if you have two hosts on the same switch, and they're on the same network, then there's no [[Routing]] happening. It's just hosts sending data to a switch, and then the switch is sending it on, depending on the mac address. We can see this list of known hosts. Let's look at it. Type in clear and then type in ARP. This will show the Mac and IP address correlation on our network, or the ARP table. If you don't see anything, ping something, anything. We're only doing that to get something in our ARP table, so it doesn't matter if it's successful or not. Now look at it with a numeric IP addresses instead of names. Bring your line and add space dash N, and hit enter. Now we see the IP addresses. The next step is to ping the default gateway. We can get that setting by using the IP command again. Type in clear and then type in IP space route and hit enter.
>
> **[3:07](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/debug-problems-associated-with-the-network-configuration?u=76281980&t=187)** The line with default in it is our default gateway. We can get the same output by typing route space dash N, or netstat space dash R, for the routing table. Now that we have the default gateway, we can ping it. My default gateway is 192.168.2.1. You will want to ping your default gateway here. Type in ping and then the IP address of your default gateway. If you can ping the default gateway, then you're getting as far as you can go inside your network before the traffic leaves. Press control C. Let's ping something we know is outside of our network, such as [[Google]] DNS server. We don't want to ping a DNS name, as we're trying to keep DNS out of it for now. Type in clear, then type in ping space 8.8.8.8, which is Google's DNS server, and hit enter. Okay, we're getting responses back. Press control C. If you can't ping this, it means there's a problem with the default gateway. Perhaps a firewall doesn't allow our traffic out, or it doesn't let the return traffic back in. Or the IP address we're trying to ping doesn't respond to ping requests. Let's assume we're not getting responses, but we want to know how far our traffic gets. For this, we'll use trace route. Type in clear and type in trace route, space dot 8.8.8.8, and hit enter. Trace route is a tool that measures transit delays of packets across an IP network. We can see from the output of our trace route that there's seven hops between our host and Google's DNS server. For you, there may be more or less, depending on where you are. The left column represents the hop number.
>
> **[4:40](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/debug-problems-associated-with-the-network-configuration?u=76281980&t=280)** This is a sequential count of the number of degrees of separation the host is from yours. The more hops, the more routers between you and it. The second column is a host name, which is a result of a reversed DNS lookup on the host IP address. Then the IP address of the network hop, and the last column is very useful. It gives the round-trip time so the packet to get to the host and back again. We have three different times, because it sends three packets. Sometimes our network works, but just not very well. By using trace route, we can check the amount of time each hop takes. Now let's move on to DNS. Now that we can ping an outside host by IP address, we need to test our DNS servers. Let's ping something by name. Type in clear. Type in ping space [LinkedIn.com](https://LinkedIn.com) and hit enter. If you get a response, then you have proper DNS resolution. Press control C. If not, then you need to troubleshoot the DNS servers. Start by pinging each by IP address and then move on to NS lookup and dig. With either tool, you can target a DNS server to force a resolution to go through it. Check the configured DNS servers first. This will most likely be in slash etc slash resolve.conf, or one of the network interface configuration files. See the network configuration videos in this course for more information. Type in clear and then type in cat space slash etc slash resolv.conf and hit enter. I can see that my DNS server is 192.168.2.1. I can ensure connectivity to it first by pinging it. Type in ping, space, and then enter the IP address of your DNS server here. For me it's 192.168.2.1, and then hit enter.
>
> **[6:16](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/debug-problems-associated-with-the-network-configuration?u=76281980&t=376)** Notice that this does not ensure that the DNS server is working, only that there's a host there, and it's responding to pings. Let's force it to resolve something. Press control C, and then type in clear. Now type in dig space at, and then the IP address of your DNS server. Space, and then the name you want to resolve. I'm going to use [LinkedIn.com](https://LinkedIn.com), and then hit enter. We're now forcing 192.168.2.1 to resolve [LinkedIn.com](https://LinkedIn.com). We can see from the output that we've got an address record back, so that works. You can use the older NS lookup for this as well, but I prefer dig. It's newer and more powerful.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[DHCP]] (3), [[Google]] (3), [[LinkedIn]] (3), [[Routing]] (2), [[Representational State Transfer (REST)|Rest]] (1)
> **Env Vars:** dns (16), dhcp (3), arp (3), enp0s3 (2)
> **Versions:** 192.168.2 (8), 8.8.8 (2)
> **Warnings:** troubleshoot (5)
> **Definitions:** is a  (4)
> **CLI Commands:** make (2), cat (1)
> **URLs:** [linkedin.com](https://linkedin.com) (3)
> **Prerequisites:** configure (1), you'll need (1)


### 13. Configure Client-Side DNS

[↑ Back to Table of Contents](#table-of-contents)

#### [Query remote DNS servers](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/query-remote-dns-servers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/query-remote-dns-servers?u=76281980&t=0)** - [Instructor] We have more than one command to query DNS servers, including the legacy NSLookup. However dig does such a good job I'm going to focus on it. Let's type in dig space [redhat.com](https://redhat.com) and look at the results. In a terminal, type dig space [redhat.com](https://redhat.com) and hit Enter. This shows information on [redhat.com](https://redhat.com). The dig command shows the header which includes the dig command version number and the global options used by dig. It also shows a question section which is the question that sent to the DNS server. Since the default question is an address query it shows that we made an address query. Then comes the answer section which is the response from the DNS query. In our case, it shows the address record. Next is the authority section which displays the DNS name server that has the authority to respond to this query. Lastly is the additional section which displays the IP address of the name server listed in the authority section followed by stats about the query including the amount of time it took. If we want to pair this down and only show the answer section, we have to add some options. Type in clear and then type in dig space [redhat.com](https://redhat.com) space +nocomments space +noquestion space +noauthority space +noadditional space +nostats and hit Enter. You can, of course, choose to hide any of these individually if you wish. An interesting shortcut is to turn off all sections and then turn the answer section back on. Type in dig space [redhat.com](https://redhat.com) space +noall space +answer.
>
> **[1:37](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/query-remote-dns-servers?u=76281980&t=97)** This is a shorter line to type but provides the same output as the long line we typed in previously. By default, dig queries the address record but we can select a different record if we wish. To do this, filter by the record type. For instance, to ask for the mail exchange vector, let's type in clear and then type in dig space [redhat.com](https://redhat.com) space MX space +noall space +answer and hit Enter. If you want the name server address, just replace the MX with NS for name server. To view all records, replace MX with ANY. We'll bring our line back by pressing the up arrow key and then replace MX with the [[Microsoft Word|word]] ANY and hit Enter. If you just want the IP addresses which might be useful in the case of shell scripts, you can use the +short option. Type in clear and then type in dig space [redhat.com](https://redhat.com) space +short and hit Enter. This can be combined with any of the record filters we just discussed. A reverse DNS lookup takes the IP address and finds the DNS name associated with it. We'll need to use the -X option to do a reverse lookup. We'll use the IP address that we got from the previous query. When I did it, I got 209.132.183.105. So to do a reverse query, we'll do dig space -x space 209.132.183.105 space +short and hit Enter.
>
> **[3:11](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/query-remote-dns-servers?u=76281980&t=191)** This shows us the DNS name associated with this IP address using the short form. Another common task is to query a specific DNS server. In the default output, it shows us the DNS server that responded which may not be the one we want to query. To specify a DNS server, precede its IP or name with an at symbol. Type in dig space @ns1.[redhat.com](https://redhat.com) space [redhat.com](https://redhat.com). This will query the ns1.[redhat.com](https://redhat.com) DNS server for the address record for [redhat.com](https://redhat.com). Hit Enter. This is most useful when querying our own DNS service to verify that they're giving the answers that we want. You can also do bulk DNS name queries by making a text file with each DNS name in it on a separate line. For instance, let's make a file called dnsnames.txt in our home directory using vi. Type in clear and then type in vi space dnsnames.txt and hit Enter. In this file, we're going to add three lines by pressing the I key to go into insert mode. And then we're going to type in [redhat.com](https://redhat.com), Enter, [ford.com](https://ford.com), Enter and [centos.com](https://centos.com) and we'll hit Enter again. Save and exit by pressing Escape :x Enter. To use this file, we'll pass the -f option to dig and specify the filename. Because we're getting back answers from multiple sites we'll probably want to add the +noall and +answer options as well. So type in dig space -f
>
> **[4:45](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/query-remote-dns-servers?u=76281980&t=285)** space dnsnames.txt space +noall space +answer and hit Enter. We can specify the type of record as well like we did earlier. If you find yourself typing in the same options to dig every single time, you can store them in a file called .digrc in your home directory. In this file, just add the options such as +noall space +answer on one line and they will be automatically appended when using dig unless you override them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **URLs:** [redhat.com](https://redhat.com) (12), [ford.com](https://ford.com) (1), [centos.com](https://centos.com) (1)
> **Env Vars:** dns (14)
> **File Paths:** dnsnames.txt (3)
> **Analogies:** for instance (2), such as (1)
> **CLI Commands:** make (1), find (1)
> **Versions:** 209.132.183 (2)
> **Definitions:** is an  (1), is a  (1)

#### [Configure local name resolution with remote DNS servers](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-local-name-resolution-with-remote-dns-servers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-local-name-resolution-with-remote-dns-servers?u=76281980&t=0)** - There are two client-side name resolution files that we should be concerned with in Enterprise [[Linux]] 7. Resolve.conf and nsswitch.conf. The purpose of resolv.conf is to provide a list of global DNS servers for our client to query when resolving DNS names. The format of the file is fairly simple. There are two columns. The first column will have either search, domain, nameserver, sortlist or options in it. If it has search, it will be followed by up to six domain names with a total of 256 characters. That will be appended if you try and resolve something incomplete. For instance, if you try to resolving the [[Microsoft Word|word]] test, and the search line included [localnet.com](https://localnet.com), [ford.com](https://ford.com) and [apple.com](https://apple.com), it would try to resolve test.[localnet.com](https://localnet.com), test.[ford.com](https://ford.com), test.[apple.com](https://apple.com) and lastly, test by itself. One of these domains has to be a local domain name. If you do not include a search line, then it defaults to your local domain name. For instance, if my local domain name was [localnet.com](https://localnet.com) and I tried resolving test, my system would resolve test.[localnet.com](https://localnet.com) first and then try to resolve test second. Even if I did not have a search line. If the first column specifies domain, it will pin that name to the end of a short name. Normally this is our local domain name but if we wanted to change it we could. For instance, if we had domain space [otherdomain.com](https://otherdomain.com) and we resolved test, it would try to resolve it to test.[otherdomain.com](https://otherdomain.com). You only need to do this if your local domain is not the same as the [[Representational State Transfer (REST)|rest]] of your network. The keyword we use the most is nameserver.
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/configure-local-name-resolution-with-remote-dns-servers?u=76281980&t=95)** The nameserver keyword is followed by a DNS server IP address to query. For instance, nameserver space 4.2.2.2. You can have up to three nameserver lines and the client will query them in the order they are listed. The sortlist keyword allows us to sort IP addresses in certain networks. For instance, if we had sortlist space one three zero dot one five five dot one six zero slash two five five dot two five five dot two four zero dot zero it would allow addresses returned to be sorted in this network. There can be up to ten network and subnet pairs. The last keyword in the first column is options. The options keyword let's us set DNS client options, such as, querying the names of his round robin to spread the load, a timeout or the number of attempts the client will query the DNS server before giving up. Keep in mind to that if you want to set DNS name servers per network interface, you can do so by adding them into the interface specific configuration files. The slash ETC slash resolve.conf is a global configuration file. Also keep in mind that if you set your DNS server's in the interface specific configuration file, and you also set the PEERDNS directive to yes then those DNS servers will get copied to slash ETC slash resolve.conf automatically.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1), [[Microsoft Word|Word]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **Env Vars:** dns (8), etc (2), peerdns (1)
> **URLs:** [localnet.com](https://localnet.com) (4), [ford.com](https://ford.com) (2), [apple.com](https://apple.com) (2), [otherdomain.com](https://otherdomain.com) (2)
> **Analogies:** for instance (5), such as (1)
> **Warnings:** keep in mind (2)
> **Versions:** 4.2.2 (1)
> **Definitions:** is a  (1)
> **Speakers:** - there (1)

#### [Modify the order in which name resolution is done](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/modify-the-order-in-which-name-resolution-is-done?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/modify-the-order-in-which-name-resolution-is-done?u=76281980&t=0)** - The name service switch, or nsswitch files used by applications to determine sources, to get name service information from. This isn't just for domain name resolution order, but rather for multiple categories. Each category of information, is identified by a database name. The first column of the file is the database name. The remaining columns specify the order, in which to look up the data. The following [[Databases]] are understood. aliases for mail aliases, ethers for Ethernet numbers, group for groups of users, posts for host names and numbers, initgroups, for supplementary group lists, netgroup for network groups, networks for network names and numbers, password for passwords, protocols for network protocols, publickey, for secretkeys, used by NFS, and NIS plus, rpc for remote procedure call numbers, services for network services, and lastly shadow, for shadow passwords. For this course, were mainly interested in the hosts keyword possible options would be files, dns, nis, nisplus. The latter two would use sons, nis, or nisplus, for hosting resolution. The first two, would be for more common situations. The files option specifies the resolve hostnames, used in the local name resolution file etsyhosts. DNS would instruct the client to resolve via DNS. We can use both and then will try them in order left to right, for instances it's common to use hosts colon space file, space DNS, this way it will consult the etsyhost file first and then try to resolve a DNS second This allows us to override DNS with a local etsyhost entry.
>
> **[1:38](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/modify-the-order-in-which-name-resolution-is-done?u=76281980&t=98)** After each option, we can have an optional status and an action inside square brackets. The status can be one of success where no error occurred not found where the lookup succeeded, but the entry was not found, unavail where the services permanently unavailable, or try again where the service is temporarily unavailable. This could mean that the file is locked to the server camp process this mini request this temporary. The action can be one of return, which returns the result right away, continue which moves on to the next method in the list, or merge, which allows the system to move on to the next method, after result and then merges the results. We can use an exclamation mark to negate the status to match anything but what is in the square brackets. In our example, earlier it would return if DNS was not unavailable. Which is the same thing as being available and then not move on to resolving via the etsyhost file.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Databases]] (1)
> **Env Vars:** dns (6), nfs (1), nis (1)
> **Speakers:** - the (1)

#### [Debug errors related to name resolution](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/debug-errors-related-to-name-resolution?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/debug-errors-related-to-name-resolution?u=76281980&t=0)** - [Narrator] It's important to have a working DNS setup. The exam only specifies setting up a caching-only name server, which passes queries under a master server and caches the results. However, there are many things that can happen that keep the client from functioning. First, let's double-check the /etc/nsswitch file. Type in less /etc/nsswitch.conf and hit enter. You'll want to go down to the host line. These entries are used in order of left to right. The first attempt at name resolution will go to local files. This means the /etc/hosts file, which we modified at the beginning of this course. After that, it will attempt to resolve via DNS. Press Q. Now let's take a look at the /etc/hosts file. Any attempt to reach rhhost1, [nolocalnet.com](https://nolocalnet.com), or rhhost1 will go to the IP address in the first column. If this IP address isn't correct, then name resolution will fail. Most of the troubleshooting I've had to do to make the chapters in this course work together has concerned making sure the /etc/hosts file was correct. Keep an eye on it, specifically if you're using dynamic IP addresses. After the /etc/hosts file is consulted and a match is not made, the system will attempt to query the DNS server. Press Q. We keep our global DNS servers in the /etc/resolv.conf file. Let's take a look. We probably want to verify that the name servers listed in this file are reachable and actually name servers. We'll do this with a dig command in a moment. Press Q. Another place where DNS servers are configured is in the network interface specific config files. We'll look at the ifcfg-et80 file. If your network interface is named something different,
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/debug-errors-related-to-name-resolution?u=76281980&t=95)** then specify that instead.

> [!info]- Semantic Content
>
> **Env Vars:** dns (5)
> **UI Navigation:** go to (2)
> **CLI Commands:** make (1)
> **URLs:** [nolocalnet.com](https://nolocalnet.com) (1)
> **Prerequisites:** setup (1)
> **Speakers:** - [narrator] (1)

#### [About systemd-resolved](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-systemd-resolved?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-systemd-resolved?u=76281980&t=0)** - [Instructor] Systemd - Networkd is the new stack to manage network configuration files on [[Linux]]. Along with this comes systemd that's resolvd. Resolvd provides interface specific name resolution. Currently, we manage global name resolution configuration in the /etc/resolv.conf file and per interface configuration in each ifcfg network configuration file. The current system is fairly rigid and not very dynamic. It's not uncommon these days to be connected to more than one dynamic network at a time and each having different name resolution servers. For instance, we may have a VPN connection going through a public [[Wi-Fi]] network. The DNS servers associated with these connections would come and go as the connections would. If DNS resolution isn't handled dynamically per interface, then there may be old DNS entries that are irrelevant to our current environment. For instance, if we disconnect from the VPN but the DNS entries reflect devices on those networks. We choose which [[Forms]] of name resolution we use and in which order in the /etc/nsswitch.conf file. Let's take a look at that. In a terminal typing cat space /etc/nsswitch.conf and hit enter. If necessary, scroll until you see the host line. This is the order in which your system resolves host names. Mine says files DNS and then my host name. This means it consults the local /nc/host file first and then ask DNS to resolve the name and lastly, the NSS - my host name plug-in module which provides host name resolution for the locally configured system host name as returned by the get host name system call.
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-systemd-resolved?u=76281980&t=94)** If you wanted to include the systemd - resolvd service in the action, we'd add the [[Microsoft Word|word]] resolv to this line. We'd have to decide where we want to include it to determine the order. We can also move the /etc/resolv.conf file and then link the /run/systemd/resolve/resolv.conf file in its place. You can also enable resolvd by configuring per interface systemd - networkd configuration files. See another video in this course for more information. To run the service, we need to start it and then enable it to make it persistent.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1), [[Wi-Fi]] (1), [[Forms]] (1), [[Microsoft Word|Word]] (1)
> **Env Vars:** dns (6), vpn (2), nss (1)
> **CLI Commands:** cat (1), make (1)
> **Analogies:** for instance (2)
> **Tools:** terminal (1)
> **Speakers:** - [instructor] (1)


### 14. Perform Security Administration Tasks

[↑ Back to Table of Contents](#table-of-contents)

#### [Use Nmap and netstat to audit network services](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-nmap-and-netstat-to-audit-network-services?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-nmap-and-netstat-to-audit-network-services?u=76281980&t=0)** - It's important when we're securing a system to turn off services that are not being used. If it's not running, it's difficult to exploit. To get the status of services on the [[Linux]] system that uses system d, you can type in sudo systemctl list-units
>
> **[0:17](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-nmap-and-netstat-to-audit-network-services?u=76281980&t=17)** - -type=service and hit enter. From this output, we can see what the status of each service is. If you only want to show active services, you can search by state. Press Q, bring your line back, and add --state=active and hit enter. These are services that were running, are running, or could be running if they're needed. To get a list of services that are running right now, bring your line back and change the state to running. Press Q, bring your line back, and change active to running, and hit enter. Now we can systematically go through this list, which services we want running, and shut down those we don't. To shut down a service, type sudo systemctl stop and then the service name. To keep it from restarting on the next boot, change stop to disable. To keep someone from being able to start it manually, change disable to mask. We should also check to see which services are currently listening to network ports. To do so, we'll use the netstat command. Press Q and then type in clear. Now type in sudo netstat -ltup The -l option shows services listening on network ports, the -t option shows tcp ports, the -u option shows udp ports, and the -p option prints the application name. Hit enter. If you want to show numeric host names and port numbers instead of protocol names, add -n. Then hit enter. With the output from netstat, we can get a better idea of which services are actively accepting connections, and make a decision if we want them shut down or not.
>
> **[1:51](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-nmap-and-netstat-to-audit-network-services?u=76281980&t=111)** We can also use lsof to show us open files associated with the network. To do so, type in clear then type in lsof -I for internet, and hit enter. On my host, I have one. The left column is the command name, then the process ID, the user it's running under, the protocol type, which is IP version four, the transport protocol, which is TCP, and then lastly the name of the file or internet address. We can also check to see which protocols our firewall is allowing in from the outside. To do so, type in clear. Then type in sudo firewall-cmd
>
> **[2:28](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-nmap-and-netstat-to-audit-network-services?u=76281980&t=148)** - -list-services and hit enter. This shows I am allowing three different ports in through the firewall: [[DHCP]], ntp, and ssh. The last thing we'll do is use [[Nmap]] to scan our own computer to see which ports are open. Nmap may not be installed, so let's install it. Type in sudo yum install
>
> **[2:49](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-nmap-and-netstat-to-audit-network-services?u=76281980&t=169)** - y nmap and hit enter. Once it's installed, let's scan our own computer's local host address. Type in clear. Type in sudo nmap -sT 127.0.0.1 and hit enter. The -sT option is to scan tcp ports. If you wanted to scan udp ports, type -sU. It looks like I have four ports open on my loopback address. Some services are configured to listen on loopback, but not on our actual external address. Let's scan that next. Type in clear then type in sudo nmap -sT and your IP address. Mine is 192.168.2.236 and then hit enter again . Now, I only have two outside ports open, ssh and rpcbind. With the information we've gained from these tools, we can audit our system services and the traffic being allowed into our host from the outside. All of these tools have many many more options that what I've covered here, but this should get you started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Nmap]] (5), [[Linux]] (1), [[DHCP]] (1)
> **CLI Commands:** sudo (7), systemctl (2), ssh (2), make (1), yum (1)
> **Code Identifiers:** st (3), su (1)
> **Versions:** 127.0.0 (1), 192.168.2 (1)
> **Prerequisites:** install (2)
> **Speakers:** - it (1), - y (1)
> **Env Vars:** tcp (1)

#### [Set up limits on users](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-up-limits-on-users?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-up-limits-on-users?u=76281980&t=0)** - [[Linux]] has the ability to limit the amount of system resources available to user processes. These limits include the number of open files. The maximum size of user created files. How much memory is used, and more. We can change these limits using the ulimit command, but they revert back to defaults on the next reboot unless we keep them in the etc security [limits.com](https://limits.com) file. Let's take a look at this file. In a terminal, type in vi /etc/security/[limits.com](https://limits.com) and hit enter. Now, scroll to the bottom. In vi, you can go to the bottom by pressing the uppercase g when not in insert mode. Here, we have some example entries. We see that we have four columns. Domain, Type, Item, and Value. Domain can be a user, group, or wildcard. Type can be one of two types, soft or hard. These are the two types of limits that we can have. The soft limit is the one that we use to restrict users. The hard limit has to be set by the admin user, and limits the maximum value for the soft limit. For instance, if the admin set the hard limit of processes to 100,000 then the soft limit restricting users can never be higher than this. Item is the thing we're restricting the limit of. So, there's many different items. For instance, the size of core files. Maximum data size. Maximum file size. The maximum locked-in memory size. The maximum number of open files. Maximum CPU time. The maximum number of processes. And, the maximum number of logins for the user. There are more items that I didn't cover.
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/set-up-limits-on-users?u=76281980&t=95)** You can read about these in the limits.conf man page. The last column is the value that we set. Now, go back to your terminal and press escape, colon, q and hit enter. You can check the current limits by using the ulimit command with a dash a option. Type in ulimit -a and hit enter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1)
> **URLs:** [limits.com](https://limits.com) (2)
> **Tools:** terminal (2)
> **Analogies:** for instance (2)
> **Env Vars:** cpu (1)
> **Cross-References:** go back to (1)
> **Documentation:** man page (1)
> **UI Navigation:** go to (1)

#### [Determine which users have logged in](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/determine-which-users-have-logged-in?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/determine-which-users-have-logged-in?u=76281980&t=0)** - [Instructor] Often an administrator needs to know who is logged in now or even who was logged in in the past. Let's start by talking about finding out who is logged in right now. The simplest command for this purpose is users. Type in users into a terminal and hit Enter. This will list the users logged into the system. It doesn't tell very much information though. The who command gives a bit more. Type in who and hit Enter. We see who is logged in and when they did so, as well as what screen they're on. The next step is to go to the w command, perhaps the shortest command in [[Linux]]. Type in w and hit Enter. This method is my favorite. It shows all the information of the who command, but also what the user's running and how much CPU time it's taking. Notice the header. It shows the current time, how long the system has been running, and how many users are logged in. And the system load averages for the past one, five, and 15 minutes. If you want to see when the users will logged into the system in the past, we use the last command. Type in clear, then type in last and hit Enter. This shows when the users logged in, and if you notice by scrolling up also shows who is logged in currently. As a side point, if you want to see the system shutdowns, add -x. Bring your line back, add space -x, and hit Enter. Lastly, the manual way is to look in the log files themselves. On Red Hat based distributions, authentication logs are located in slash var slash log slash secure. For Debian based systems, it's in slash var slash log slash auth dot log Since we're in a Red Hat based system,
>
> **[1:32](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/determine-which-users-have-logged-in?u=76281980&t=92)** let's take a look at var log secure. We need to elevate privileges for this, and we're going to look for open sessions. Type in clear and then type in sudo space grab space single quote space session space opened single quote space slash var slash log slash secure and hit Enter. Here, we can see a much lower level list of session events.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1)
> **CLI Commands:** sudo (1)
> **Env Vars:** cpu (1)
> **Tools:** terminal (1)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)

#### [Basic sudo configuration and usage](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/basic-sudo-configuration-and-usage?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/basic-sudo-configuration-and-usage?u=76281980&t=0)** - [Narrator] Since the beginning of this course, we've been using Sudo to elevate privileges. We've also introduced the idea of using Su to switch to root, thus elevating privileges as well. With Su, you have to type in the root user's password. In order to do so, you have to know the root user's password. This can be problematic because if we have multiple users that need to elevate to root, then they all need to know the root user's password. If one leaves the company, then the root password has to be changed, and everyone needs to be updated. Switching users keeps you logged in as root for the whole session, increasing the chances of human error. With Sudo, you elevate privileges for one command only. Also by switching to root, there's very little in the way of [[Accountability]]. All log messages say that the root user executed all commands. With Sudo, all command execution is logged individually. When using Su to switch to root, you are all powerful. Sudo is more granular in that an administrator can decide which commands you are allowed to run, as well as which files you have access to. Let's use Sudo to elevate our privileges. In a terminal, type in Sudo space cat space slash etc slash shadow. Sudo will then prompt us for our password, and execute cat with elevated privileges. When cat is done, we'll return to our normal user role. Hit enter, type in your password, and hit enter again.
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/basic-sudo-configuration-and-usage?u=76281980&t=94)** Sudo allows us to elevate privileges for one command. Sudo also caches the user's password for a short period of time for convenience. Sudo has a configuration file that allows an administrator to set up who gets to elevate privileges, and what they get to do. It's granular enough that we can even configure which command options the user can run. Let's look at this configuration file. The tool to edit the configuration file is called Visudo, which locks the Sudo file, and edits it using VI. Let's type in Sudo space Visudo. Once we're done editing, it checks the syntax, and then saves the file. Go ahead and hit enter. We want to scroll all the way to the bottom. This configuration file allows us to group together users, hosts, and even commands, and then create access control rules allowing certain users to run certain commands on certain hosts. Our user doesn't have an access rule in this file, but we do belong to a very special group named wheel. You'll want to note the line on your screen that starts with percent wheel. When Sudo is using operating system groups, we need to prefix the name with a percent symbol. This line gives members of the wheel group permissions to run all commands on all hosts as all users. Let's exit this file by pressing escape colon q exclamation point, and hitting enter. Let's view the slash etc slash group file using less. Type in less space slash etc slash group and hit enter.
>
> **[3:12](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/basic-sudo-configuration-and-usage?u=76281980&t=192)** You'll want to take note of the wheel group and check out the members. The last column on the right is the members that belong to the wheel group. This is how we are allowed to elevate privileges. The quickest way to allow other members to elevate privileges is to add them to this group.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Accountability]] (1)
> **CLI Commands:** sudo (13), cat (3)
> **UI Navigation:** switch to (2)
> **Prerequisites:** set up (1), configure (1)
> **Tools:** terminal (1)
> **Definitions:** is called (1)
> **Speakers:** - [narrator] (1)

#### [Find suid/sgid files](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/find-suid-sgid-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/find-suid-sgid-files?u=76281980&t=0)** - [Instructor] SUID and SGID bits on executable files are special bits for privilege escalation. The SUID bit allows non-user owners to execute commands with privileges of the user-owner. In our terminal, let's do a long list on slash USR, slash BIN slash SU by typing in LS space dash L space slash USR slash BIN slash SU and hit enter. The first thing we notice is that the file is bright red. This tells us something special is happening. Let's take a look at the permissions. The permissions for the user-owner are RWS. Note that we can no longer see the user-owner's execute position. The S in the user-owner's execute position means we have the SUID bit set. If the S is lower case, then execute is set. If the S is upper case, then execute permissions are not set. The case of the letter S is the only way we can tell if the execute permissions are set. When the SUID bit is set and a regular user, such as Bob, executes it, their privileges get elevated to that of the user-owners. In our example, Bob would be executing the SU command as root. This means this command would have all of the power of the root user. To se the SUID bit, we utilize an extra column of numeric permissions. First, we decide what underlying permissions we want on the file. In this case the file has RWX RX RX or seven five five permissions. Then, we decide what extra bits we want. In this case, SUID would just equal to four. Setting these permissions via numeric mode would be chmod space four seven five five
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/find-suid-sgid-files?u=76281980&t=94)** space slash USR slash BIN slash SU. We can also use symbolic method as well by typing chmod space U plus S space slash USR slash BIN slash SU. The SGID bit is very similar to SUID. When we have the SGID bit set, the S resides in the group-owners execute position. For instance, the slash USR slash BIN slash SCREEN command. When a regular user executes a command with the SGID bit set, it runs with the privileges of the group owner of that file. In this case, the screen group. To add SGID bits to a file, we prefix the permissions with a two. For the screen command, the permissions would be two seven five five. So, we'd set SGID with chmod space two seven five five space slash USR slash BIN slash SCREEN. We can also set this with symbolic mode in which we would use chmod space G plus S space slash USR slash BIN slash SCREEN. Because these commands allow privilege escalation without prompting for a password, it may be advantageous to know where in the file system they are for security reasons. We can use the find command to locate SUID and SGID files. Type into a terminal SUDO space find space slash, that's where we're going to look, space dash type space F, we're going to search for files. Space dash perm space dash 4000. You can also type forward slash 4000 and it'll work Hit enter. Type in your password if prompted. This will find all files with an SUID bit set.
>
> **[3:09](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/find-suid-sgid-files?u=76281980&t=189)** You can also use the symbolic method. Type in clear and then type in SUDO space find space slash space dash type, space F, space dash perm, space forward slash U equals S and hit enter. To find files with the SGID bit set, we'll change the line slightly. Type in clear and then type in SUDO space find space slash space dash type space F, space dash perm space dash 2000 and hit enter. We can also use symbolic method here as well. Type in clear. Type in SUDO space find space slash space dash type, space F, dash perm, space, forward slash G equals S and hit enter. This will list all files with the SGID bit set. With symbolic method, we can find both. Bring your line back and add comma U equals S and hit enter. This will list all files with SUID or SGID bit set. Let me show you one last trick. Type in clear and then bring your line back and add space dash exec space LS space dash L space dash dash color equals auto, space left curly bracket, right curly bracket space backslash semicolon and then hit enter. This executes the LS command and applies the dash dash color equals auto option to each file so we can see the permissions and the colors.

> [!info]- Semantic Content
>
> **Env Vars:** suid (10), sgid (10), usr (7), bin (7), sudo (4)
> **CLI Commands:** find (8), chmod (4), sudo (4), ls (3)
> **Analogies:** such as (1), similar to (1), for instance (1)
> **Tools:** terminal (2)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [About TCP wrappers](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-tcp-wrappers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-tcp-wrappers?u=76281980&t=0)** - [Narrator] Firewalls are incredibly important, they limit which packets get to which host; however there are times when you want more protection from within a network application. If a network service is sending and receiving encrypted data, the firewall can't tell if it should allow it or deny it. The services it self has access to the data , and can often make a better decision; one way of doing this is by using TCP Wrappers, note, that denying access with TCP Wrappers should be using conjunction with a firewall, and is not a replacement. TCP Wrappers looks at two files, and determines whether to allow or deny access to the service. This files are in /etc/hosts.allow, and /etc/hosts.deny. A syntax of both of this files is the same . The services column is a coma separate list of services that this rule should apply to you. The clients are coma separate list of hosts or IP addresses affected by the rule. There are also accepted wild cards, which I'll get to in a moment. The options columns are the action or actions that will be carried out such as allow or deny. For matching the clients, we have some built in wild cards. ALL, matches everything, and can be used for clients and services. LOCAL, matches hosts without a period in their fully qualified dominium; such as local hosts. KNOWN, indicates that the host name is known. Be careful with this, as it may cause problems if your DNS service is not accessible. UNKNOWN, is the exactly the opposite of known, and the same precaution apply. Lastly, PARANOID causes a connection to be dropped, if forward and reverse DNS lookups don't match. A guess the IP address using the host name,
>
> **[1:34](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-tcp-wrappers?u=76281980&t=94)** and then guess the host name using the IP. This rules will go in either the allow, or deny file, there's a bit of difference between this two files. the deny file can means lines of rules that deny access to services. Since the default policy is allow, there's no need to allow access in this file, the allow file contains lines of rules that either, allow or deny access. Both files can effectively allow and deny access to services they just do it differently. One strategy is to deny all in the host that deny file, and specifically allow in the host that allow file. This will be the firewall of doing things. Another, would be to only use the deny file to deny specific access. Lastly we could just use the allow file and specify allow and deny rules in the same file. It's up to you, but beware that, if a rule and the host that allow file, allows connection then it's allowed. If a rule and a host at deny file denies a connection, then it's denied, however; if a rule and the host that deny file, denies a connection, and there's also a rule and a host at allow file, that allows it, that connection is allowed. Host that allow overrides host that deny. You can only have one rule peer service in either file. Order is important; if a connection is allowed at the top of the file, and denied at the bottom, it will never get to the bottom. It matches the first instance. If there are no rules matching a service in either file, then the service is granted. Lastly, any changes to the files happen automatically; without restarting services. Lets take a look at the deny file first. In a terminal, typing:
>
> **[3:08](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-tcp-wrappers?u=76281980&t=188)** sudo vi /etc/ hosts.deny, and hit enter. We see some comments, but no rules. But have one line denying everything. Remember that the allow file overrides the deny file. Well deny everything in this file, and then grant access in the allow file. Go in the insert mode by pressing the I key, and then clear to the bottom, type in ALL: ALL; this denies access to all services on all networks, now lets save that, by pressing scape, colon, x, s, exclamation mark, and hitting enter. Now we'll move to the allow file. Be aware that all new connections will be blocked, including those to the SSH service. Something to be aware of if you're doing this to the network. Now lets edit the allow file; typing sudo vi /etc/host.allow and hit enter. Go to insert mode again, and go to the bottom of the file. In this file we can grant access to whichever services we want. Lets talk about some example rules. Lets allow SSHD connections from the one-nine two that want to stay in that one network. You will probably want to add your network here, if its different. Type in SSHD: 192.168.1. Saving next by pressing escape, colon, x, exclamation mark, and hitting enter. If you wanted to allow all services from one host, specify the IP address. We can specify multiple services in one rule as well.
>
> **[4:42](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-tcp-wrappers?u=76281980&t=282)** By separating them with comas; also IV is a local wild card to match local hosts on a network. Troubleshoot your rules; You can use 'tcpdchk', and 'tcpdmatch'. One more thing that I'll mention, is that is possible to spunk commands for a match rule. You'd use either spun or twist to execute a command. For more information on this commands, and the rule definitions; see the man pages for host_options, and host_access. To find the applications at support TCP Wrappers, we'll use LDD. On our HS1 lets check the SSHD binary. In a terminal type ldd /usr/sbin/sshd | grep
>
> **[5:25](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-tcp-wrappers?u=76281980&t=325)** libwrap. So, and hit enter. If you see libwrap as output, then you know this application has TCP wrappers support.

> [!info]- Semantic Content
>
> **Env Vars:** tcp (5), sshd (3), dns (2), local (1), known (1)
> **CLI Commands:** sudo (2), make (1), ssh (1), find (1), grep (1)
> **Warnings:** be aware (2), be careful (1), troubleshoot (1)
> **Code Identifiers:** host_options (1), host_access (1)
> **Tools:** terminal (2)
> **UI Navigation:** go to (2)
> **Definitions:** is a  (2)
> **Analogies:** such as (2)


### 15. Securing Data with Encryption

[↑ Back to Table of Contents](#table-of-contents)

#### [Perform basic OpenSSH 2 client configuration](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/perform-basic-openssh-2-client-configuration?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/perform-basic-openssh-2-client-configuration?u=76281980&t=0)** - SSH has many options, including which ciphers to use, which host a client can connect to and how it operates. However, usually a client configuration uses the defaults and is much easier to work with in the SSH [[Server Configuration]]. There are three ways of passing configuration options to the SSH client. They're processed in the following order: command line options, the per user configuration file stored in the user's home directory. And lastly, the system wide configuration at /etc/ssh/ssh_config For each parameter, the first value will be used. So any options specified on the command line override the configuration files. Any option in the users configuration file overrides the system wide configuration file. Let's start by looking at our system wide configuration file. Later in this chapter, we'll talk about the per user configuration as well. Let's load the system wide configuration file into less. In a terminal type in less -Q /etc/ssh/ssh_config
>
> **[1:02](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/perform-basic-openssh-2-client-configuration?u=76281980&t=62)** and hit enter. I've added the -Q option to quit the error bell in less. Configuration options in these files are stored in key value pairs on individual lines. Any line that starts with a hash symbol is a comment. In the case of the SSH configuration items, it will be the name of the item and the default value. To edit this setting will uncomment the line and change the value. There are not very many uncommented lines. The first one is the host line followed by the pattern of host a client is able to connect to. In my case, it's an asterisk, meaning all hosts. The next line that's uncommented is the GSS API authentication line. The value is set to yes to use the generic security services API. This is an IETF standard for doing strong encrypted authentication in network based applications. It's what we use to use Kerberos with SSH. The next line that's uncommented, is the forward X11, trusted line. This is set to yes. If set to yes, the remote clients will have access to the full original display. If set to no, the remote client will not have access to the entire display, and the security [[Tokens]] will expire after 20 minutes. After this time remote clients will be refused. The last uncommented lines are locale related variables. To see what settings we can modify, let's take a look at the man page for this file. Press Q to quit. Now type man SSH_config and hit enter. This man page explains all possible options including ciphers, optimizations, and generic configuration items.
>
> **[2:36](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/perform-basic-openssh-2-client-configuration?u=76281980&t=156)** Keep this man page in mind when configuring the SSH client as it explains every option.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Server Configuration]] (1), [[Tokens]] (1)
> **Env Vars:** ssh (6), api (2), gss (1), ietf (1), x11 (1)
> **CLI Commands:** ssh (8)
> **Documentation:** man page (3)
> **Tools:** command line (2), terminal (1)
> **Code Identifiers:** ssh_config (2)
> **Definitions:** is a  (1), is an  (1)
> **Cross-References:** later in (1)

#### [About OpenSSH 2 server host keys](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-openssh-2-server-host-keys?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-openssh-2-server-host-keys?u=76281980&t=0)** - There are multiple ways of authenticating using SSH, but the two most common are passwords and private public key pairs. For this exercise, we're going to need both of our VM's booted up, and you'll need the IP address of your RH host 2vm. To set up passwordless logins, I need to generate the SSH key pair using the SSH keygen command in the RH host 1vm. In a terminal, in your RH host 1vm, type in "ssh-keygen" and hit enter. Go ahead and take the defaults when prompted. Since we did not pass at any options, it created RSA private and public keys. Lets verify this by listing the dot SSH hidden directory. Type in clear and then type in "ls -l ~/.ssh" and hit enter. We can see that we have a file called "id_rsa". That is our private key, and we have a file called "id_rsa.pub" which is our public key. Let's view the public key with cat. Type in "cat ~/.ssh/id_rsa.pub" and hit enter. Now let's copy the public key to RH host 2 using the SSH copy id command. Type in "ssh-copy-id user1@" and then your RH host 2 IP address. In my case, it's 192.168.0.21 You want to enter yours instead.
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-openssh-2-server-host-keys?u=76281980&t=93)** Say yes to accept the fingerprint to continue and then enter the remote user's password. To make sure that the key is added to the local SSH agent that manages the keys, we can type in "ssh-add" and hit enter. This is not always necessary, but doesn't hurt anything if you run it. When using SSH copy id, two things happen. First, the local public key is copied across the network to the remote end and stored in the user's authorized keys file. And second, the fingerprint of the remote server is stored in the local user's known host file. Type in "clear" and let's take a look at the local known host file. Type "cat ~/.ssh/known_hosts" and hit enter. This file stores the IP address of the remote servers as well as their matching fingerprints. If the remote server ever changes it's IP address, you'll need to delete it's lane from this file before connecting again. Don't worry about remembering this as the SSH client will refuse to connect and remind you to delete the line. Now that the key is copied, let's try to log in to the remote host. Type in "ssh user1@" and then the IP address of RH host 2. In my case it's 192.168.0.21 and hit enter. We should be able to log in without typing a password. If it prompts you for a password, just go back and do the steps again. I mentioned earlier that the SSH copy id command copied the public key into the authorized keys file on the remote host. Let's look at this file
>
> **[3:06](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/about-openssh-2-server-host-keys?u=76281980&t=186)** while we're still logged into RH host 2. Type in "clear" and then type in "cat ~/.ssh/authorized_keys" and hit enter. This file contains the public key from RH host 1 that we viewed earlier. We can manually append any SSH public key to the authorized keys file using a redirect but it's best to let the SSH copy id command do it as SSH can be picky about file permissions and syntax.

> [!info]- Semantic Content
>
> **CLI Commands:** ssh (20), cat (4), ls (1), make (1)
> **Env Vars:** ssh (12), rsa (1)
> **Code Identifiers:** id_rsa (3), known_hosts (1), authorized_keys (1)
> **Best Practices:** the key is (2), it's best to (1)
> **Prerequisites:** you'll need (2), set up (1)
> **Versions:** 192.168.0 (2)
> **Tools:** terminal (1)
> **Speakers:** - there (1)

#### [Perform basic GnuPG configuration and revocation](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/perform-basic-gnupg-configuration-and-revocation?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/perform-basic-gnupg-configuration-and-revocation?u=76281980&t=0)** - [Instructor] GPG, or GNU [[Privacy]] Guard, is an implementation of the proprietary PGP, or Pretty Good Privacy program. It's primarily used to encrypt emails but can be used to encrypt files as well. GPG utilizes a combination of public and private keys, making it difficult to crack. GPG should be installed but if it is not, you'll want to install the package named gnupg2. We're going to start out by generating some GPG keys. We'll use the gen key option for this. Type into a terminal gpg2 --gen-key and hit Enter. Take the default for the type of key, take the default for the key size, take the default for the amount of time before the key expires. The default is that it does not expire. Enter y, for yes to accept. Now, enter your name, I'm going to use user1 and then hit Enter. Hit Enter for email, hit Enter for comment, and press O to accept. Now, enter a strong password, and reenter it. When it finishes, it shows our public key, the fingerprints, user ID, and sub key. Copy and paste the public key identity we'll need this later You might be wondering where all of this information goes. Type in clear, and then type in ls ~/.gnupg and hit Enter.
>
> **[1:20](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/perform-basic-gnupg-configuration-and-revocation?u=76281980&t=80)** Here we see all of the files that were just created. To list the keys use gpg2 again. Type in gpg2 --list-keys and hit Enter. Here, we can see the keys in our public key ring. The public key we can distribute to other systems and users. They then would encrypt files with it and send them back to us, only we can decrypt the files using our private key. Let's export the public key now. Type in clear and type in gpg2 --export user1 , and then the file name, I'll name it user1.pub. If you want to append it to an email you'll need an ASCII text to do so, add the --armor option. If you don't want to send a public key or add it to an email and text, then maybe you want to store it on a key server on the Internet. If so type in gpg2 --send-keys and then, paste in your public identity. And hit Enter, this will send the key to the public key server at [gnupg.net](https://gnupg.net). You can check the key out by pulling it back down again. Type in gpg2 --keyserver hkp://keys.[gnupg.net](https://gnupg.net) --recv-keys ,
>
> **[2:41](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/perform-basic-gnupg-configuration-and-revocation?u=76281980&t=161)** and then paste in your public key identity again. (mouse clicking) This should retrieve the key, if not hit it a second time and see what happens. Once you've distributed the public key to someone they'll need to import it into their key ring. To do so they'll use a --import option with the path. If someone has stored the key in a current directory you could add it to the key ring by typing in gnupg2 --import the path to the key file. Then, they can verify with the --list-keys option. One last thing we might want to do is to make a revocation certificate so you can nullify your key pair in case you lose a private identity. To create the certificate you'd type gpg2 --gen-revoke user1 enter a reason for the certificate an optional description, and a pass phrase to unlock the secret key for user1. The certificate will be generated and displayed on screen. Copy and paste this into a separate file and store in a secure location for use later with gpg2 --edit-key user1, if needed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Privacy]] (2)
> **Env Vars:** gpg (4), gnu (1), pgp (1), ascii (1)
> **CLI Commands:** ls (1), make (1)
> **URLs:** [gnupg.net](https://gnupg.net) (2)
> **Prerequisites:** install (1), you'll need (1)
> **Tools:** terminal (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Use GPG to encrypt, decrypt, sign, and verify files](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-gpg-to-encrypt-decrypt-sign-and-verify-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-gpg-to-encrypt-decrypt-sign-and-verify-files?u=76281980&t=0)** - We can encrypt and decrypt files with GPG for [[Privacy]]. We can also sign files as well, ensuring they haven't been tampered with. Let's start by encrypting a file. Let's encrypt the etc password file and save it as a new file. To do so, in a terminal type in, gpg2, space, dash, dash, armor, space, dash, dash, recipient, space, user1, space, dash, dash, output, space, passwd.gpg, that'll be our encrypted file. Space, dash, dash, encrypt, space, and then the file we want to encrypt is slash, etc, slash, P-A-S-S-W-D, and hit enter. We didn't have to use the dash, dash, armor option which saves encrypted files as a text file, but this allows us to view it with cat, so let's do that. Type in cat, space, passwd.gpg, we can see that it's encrypted. Now if you want to decrypt this and display the output on the screen, type in clear and type in gpg2, space, dash, dash, decrypt, space, passwd.gpg. If it prompts you for your password, type it in now. If you want to decrypt it in place without displaying it on the screen, just type in clear, and then type in gpg2, space, passwd.gpg, and hit enter. This will restore the original file, and not just display the unencrypted contents. We can also encrypt and sign a file, let's use the etc password file again. Bring your encrypt line back, and add dash, dash, sign to it. Say yes to override the file and hit enter again. That's it, the decryption step is exactly the same.
>
> **[1:36](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/use-gpg-to-encrypt-decrypt-sign-and-verify-files?u=76281980&t=96)** When you decrypt it, GPG will display a message, telling you if the signature is good or not.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Privacy]] (1)
> **CLI Commands:** cat (2)
> **Env Vars:** gpg (2)
> **Tools:** terminal (1)
> **Speakers:** - we (1)

#### [SSH port tunnels](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/ssh-port-tunnels?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/ssh-port-tunnels?u=76281980&t=0)** - [Instructor] We've talked about using different protocols for remote GUI access. If you want step-by-step instructions on how to install and configure programs for remote desktop on [[Linux]], search for my desktops and remote access course in the course library. In that course, I have separate videos for remote desktop protocols. Now, let's talk about remote access using SSH, and some of the other features of that protocol. SSH comes built in to every Linux distribution, although it might not be turned on by default. SSH is a powerful and very underused tool. SSH can provide remote login functionality. SSH can execute commands remotely, even without an interactive login. SSH has the ability to copy files across a network with the SCP command, and it has an interactive interface like FTP, but secure. SSH can act as a generic encrypted tunnel that we can pipe data through, and also has several different port forwarding modes. Local, remote and dynamic, or SOCKS proxy. In this course, we'll focus on tunneling, as well as port forwarding. There are several ways of sending data through an SSH tunnel, first, let's talk about piping it. We can take the output of any command and pipe it into an SSH tunnel easily, although it's important to think about what happens to that data on the other end. For instance, if I wanted to read a file, send it through a tunnel, then redirect that output back to the file system, I could. In this case, the cat command reads the etc password file on the local machine, and then pipes it into the SSH tunnel we just created when we logged in. The command string inside quotes is executed on the remote end, on the remote end,
>
> **[1:35](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/ssh-port-tunnels?u=76281980&t=95)** the cat + Space + hyphen + Space, greater than command reads from standard in and writes through standard out. In this example, we are essentially copying the file. However, it can take standard out from any command, including binary data. SSH also has the ability to redirect traffic through the tunnel by port forwarding. Let's say that you have a remote database server with Mariadb running on it, Mariadb database runs on port 3306. For security reasons, the firewall on the database host does not allow connections on port 3306 from the outside. What we need to do is take local database client traffic, send it across the network using SSH, and then redirect that traffic to the database on the server. The syntax is a bit odd, so let's go over it. First, note that SSH will log in remotely as user one on rhhost2.[localnet.com](https://localnet.com), then it will take port number 3306 on our client machine and forward it through the tunnel and redirect it to 127.0.0.1:3306. It's not obvious by the SSH command string, but this 127.0.0.1 is the local host after it's logged into the remote host. Because it's redirecting data to 127.0.0.1:3306, the data will go to the servers running on rhhost2, that is answering on that port, in our case, Mariadb. This allowed us to go through the open SSH port and redirect non-redirect SSH data through it to another service. It is important to understand that 127.0.0.1 is processed on the remote host
>
> **[3:09](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/ssh-port-tunnels?u=76281980&t=189)** because we can change it to something else known to the remote host, for instance, if we changed 127.0.0.12, ubhost1.[localnet.com](https://localnet.com), and rhhost knew of this host, it would forward the traffic to it. In this sense, SSH is [[Routing]] traffic inside the network for us. We're not limited to one port either, they forward multiple ports, we just include multiple segments. For instance, to forward both ports 3306 and 5901, we just duplicate the forwarding portion and change the ports. SSH can do remote port forwarding as well. This means instead of grabbing the local port and sending it remotely, it grabs the remote port and brings it back to the local host. And it might be hard to imagine when we'd do this, so let me give you a scenario. Let's say we have rhhost one and rhhost two again. What we want to do is SSH from rhhost one directly to rhhost two, however, rhhost two is behind a firewall. When we try to SSH, it blocks our incoming connection. This firewall, however, does not block outgoing SSH connections from rhhost two, so on rhhost two, we SSH out to a host accessible to both rhhost one and rhhost two. In my example, it would be our ubhost one vm. We then use remote port forwarding to grab a port that we know is not being used. I usually choose a very high number port, such as 10,000, and we forward the traffic on that port back through the tunnel to rhhost two. It's important to note that we leave this connection up. Looking at the syntax in the diagram,
>
> **[4:41](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/ssh-port-tunnels?u=76281980&t=281)** we can see that the port of 10,000 is a remote port on ubhost one. Then we forward the traffic to 127.0.0.1:22. That portion is processed by rhhost two as it's own local host address. This means it grabs the remote port 10,000 on ubhost one, and brings it back to rhhost two and delivers it to the local port 22. Now back on rhhost one, we SSH to ubhost one on port 10,000, which we previously set up. This open SSH connection from rhhost two grabs the traffic and brings it back to the tunnel, sneaking past the firewall. I'd like to say right now that you should use this for good and not for evil. In my example, we severed the firewall for a remote login session where we could have applied the database scenario that we did earlier. We could be using the database client accessing a public host that has forwarding traffic inside the public network.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (2), [[Routing]] (1)
> **Env Vars:** ssh (24), gui (1), scp (1), ftp (1), socks (1)
> **CLI Commands:** ssh (24), cat (2)
> **Ports:** port 3306 (2), :3306 (2), :22 (1), port
10 (1), port 22 (1)
> **Versions:** 127.0.0 (6)
> **Analogies:** for instance (3), imagine (1), such as (1)
> **Definitions:** is a  (3)
> **Prerequisites:** install (1), configure (1), set up (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Taking the exam](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/taking-the-exam?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/taking-the-exam?u=76281980&t=0)** - [Instructor] The LPIC-1 version five certification is made up of two exams. For this exam, time management is key. You have roughly 60 questions to accomplish in 90 minutes, but the minutes can go by fast. The exam consists of multiple choice and fill in the blank questions. Complete the easy questions first. If you get hung up on one difficult item and run out of time before getting to the easier items, you may fail the test. The test really does follow the exam objectives. The [[Linux]] Professional Institute has told you what is on the exam, so pay close attention to the objectives. Each LPI exam is ranked on a scale from 200 to 800 with a passing score of 500. Since exam questions vary in difficulty, the number of correct answers required to reach the passing score of 500 will depend on the exact combination of questions on the exam you take. For security purposes, candidates are not always given the same questions as one another for a particular exam. The exam passing score is based on LPI psychometric studies to ensure all exams have a consistent level of difficulty. The number of questions is also tied to the total of the weights for the objectives on the exam. This exam has a total weight count of 60. As such, the exam has 60 questions. For each weighting, there will be one question. For objective weightings, see the exam objectives. As of this recording, the price of the exam was 200 US dollars and there are no prerequisites for taking the exam. Keep in mind that the technologies and commands listed in the objectives in this course may not be exhaustive. LPI reserves the right to change the test as they see fit. Lastly, practice, practice, practice.
>
> **[1:33](https://www.linkedin.com/learning/lpic-1-exam-102-version-5-0-cert-prep/taking-the-exam?u=76281980&t=93)** Walk through this course more than once, complete the assessment questions more than once without using [[Google]] or your notes, and good luck.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1), [[Google]] (1)
> **Env Vars:** lpi (3), lpic (1)
> **Warnings:** keep in mind (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [[Grant McWilliams]]

## Resources

- Exercise files available

## Skills Covered

- Linux System Administration

## Path Context

### In [[Prepare for the Linux Professional Institute LPIC-1 (101-500 and 102-500) Exams]]
← [[Lpic 1 Exam 101 Version 5 0 Cert Prep]] | **2 of 2**

## Appears In

- [[Prepare for the Linux Professional Institute LPIC-1 (101-500 and 102-500) Exams]]

## Related Courses

_Courses sharing skills:_

- [[Lpic 1 Exam 101 Version 5 0 Cert Prep]] — Linux System Administration
- [[Red Hat Certified System Administrator (EX200) Cert Prep- 2 File Access, Storage, and Security]] — Linux System Administration
- [[Ubuntu Linux- User and Group Management]] — Linux System Administration
- [[Ubuntu Linux- Storage Management]] — Linux System Administration
- [[Ubuntu Linux- Operating System Basics]] — Linux System Administration

---

[↑ Back to top](#)