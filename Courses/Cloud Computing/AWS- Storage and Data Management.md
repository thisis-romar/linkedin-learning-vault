---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: aws-storage-and-data-management-17535108
url: "https://www.linkedin.com/learning/aws-storage-and-data-management-17535108"
duration_minutes: 304
duration: 5h 4m
level: Intermediate
updated: 1/5/2023
learners: 9774
skills:
  - Storage Management
  - Data Management
  - Amazon Web Services (AWS)
exercise_files: true
github: "https://github.com/LinkedInLearning/aws-storage-and-data-management-3082086"
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQFOfnWsuTJ7WA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1671825403910?e=2147483647&amp;v=beta&amp;t=0-dg5x7Vwo-_AUu-1EqkZ882Xz9EdIZP8Tvc7YBl_dU"
linkedin_topic: Cloud Computing
learning_paths:
  - '[[Infrastructure Management on AWS- Skills for Administrators]]'
prev_courses:
  - '[[Complete Guide to AWS Security and Compliance Management]]'
path_nav: '[{"path":"Infrastructure Management on AWS- Skills for Administrators","position":5,"total":5,"prev":"Complete Guide to AWS Security and Compliance Management","next":null}]'
path_count: 1
tags:
  - course
  - topic/cloud-computing
  - topic/devops
  - skill/storage-management
  - skill/data-management
  - skill/amazon-web-services-aws
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Cloud%20Computing/AWS-%20Storage%20and%20Data%20Management.md)

![AWS: Storage and Data Management](https://media.licdn.com/dms/image/v2/C560DAQFOfnWsuTJ7WA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1671825403910?e=2147483647&amp;v=beta&amp;t=0-dg5x7Vwo-_AUu-1EqkZ882Xz9EdIZP8Tvc7YBl_dU)

# AWS: Storage and Data Management

> Amazon Web Services (AWS) offers solutions that are ideal for managing data on a sliding scale—from small businesses to big data applications. This course with instructor Brandon Rich teaches system administrators the intermediate-level skills needed to successfully manage data by creating backups, enforcing compliance requirements, and managing the disaster recovery process.

> [LinkedIn Learning](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108) | 5h 4m | Intermediate | 10K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Storage and AWS](#storage-and-aws)
  - [What you should know](#what-you-should-know)
  - [Exercise files](#exercise-files)
- [**1. Overview**](#1-overview) (3 videos)
  - [What is data management?](#what-is-data-management)
  - [Data security in AWS](#data-security-in-aws)
  - [Regulatory compliance and AWS](#regulatory-compliance-and-aws)
- [**2. Object Storage**](#2-object-storage) (11 videos)
  - [AWS S3 basics](#aws-s3-basics)
  - [Durability and availability](#durability-and-availability)
  - [S3 bucket creation](#s3-bucket-creation)
  - [S3 upload and logging](#s3-upload-and-logging)
  - [S3 cross-region replication](#s3-cross-region-replication)
  - [Set up SNS for event notifications](#set-up-sns-for-event-notifications)
  - [Set up S3 for event notifications](#set-up-s3-for-event-notifications)
  - [AWS Glacier and S3 lifecycle rules](#aws-glacier-and-s3-lifecycle-rules)
  - [S3 data lifecycle configuration](#s3-data-lifecycle-configuration)
  - [Versioning in S3](#versioning-in-s3)
  - [S3 teardown](#s3-teardown)
- [**3. File Systems**](#3-file-systems) (10 videos)
  - [Elastic Block Storage](#elastic-block-storage)
  - [Create an instance with multiple EBS volumes](#create-an-instance-with-multiple-ebs-volumes)
  - [Modify the type and size of an EBS volume](#modify-the-type-and-size-of-an-ebs-volume)
  - [Detach and reattach an EBS volume](#detach-and-reattach-an-ebs-volume)
  - [EBS backup, restore, and schedule with CloudWatch](#ebs-backup-restore-and-schedule-with-cloudwatch)
  - [Elastic File System](#elastic-file-system)
  - [EFS demo create EFS](#efs-demo-create-efs)
  - [EFS demo mount EFS](#efs-demo-mount-efs)
  - [EBS and EFS teardown](#ebs-and-efs-teardown)
  - [Amazon FSx](#amazon-fsx)
- [**4. Database Services**](#4-database-services) (9 videos)
  - [RDS](#rds)
  - [Create a MySQL RDS instance](#create-a-mysql-rds-instance)
  - [Connect with MySQL Workbench](#connect-with-mysql-workbench)
  - [Create an Aurora read replica](#create-an-aurora-read-replica)
  - [RDS backup and recovery](#rds-backup-and-recovery)
  - [RDS teardown](#rds-teardown)
  - [DynamoDB](#dynamodb)
  - [Create a DynamoDB table](#create-a-dynamodb-table)
  - [Dynamo teardown](#dynamo-teardown)
- [**5. Getting Data to AWS**](#5-getting-data-to-aws) (20 videos)
  - [Move data to AWS](#move-data-to-aws)
  - [Transfer data using the AWS CLI](#transfer-data-using-the-aws-cli)
  - [Storage Gateway](#storage-gateway)
  - [Configure File Gateway and shares](#configure-file-gateway-and-shares)
  - [Connect to File Gateway via NFS](#connect-to-file-gateway-via-nfs)
  - [File Gateway teardown](#file-gateway-teardown)
  - [Transfer data with AWS Snowball](#transfer-data-with-aws-snowball)
  - [AWS Database Migration Service](#aws-database-migration-service)
  - [DMS demo: Setup steps](#dms-demo-setup-steps)
  - [DMS demo: Create replication instance](#dms-demo-create-replication-instance)
  - [DMS demo: Create and execute task](#dms-demo-create-and-execute-task)
  - [DMS teardown](#dms-teardown)
  - [AWS SFTP service](#aws-sftp-service)
  - [AWS SFTP prerequisites](#aws-sftp-prerequisites)
  - [Create an SFTP endpoint](#create-an-sftp-endpoint)
  - [Send data to S3 using AWS SFTP](#send-data-to-s3-using-aws-sftp)
  - [AWS SFTP teardown](#aws-sftp-teardown)
  - [AWS DataSync](#aws-datasync)
  - [Move data with AWS DataSync](#move-data-with-aws-datasync)
  - [AWS DataSync teardown](#aws-datasync-teardown)
- [**6. Moving Data in AWS**](#6-moving-data-in-aws) (7 videos)
  - [AWS Glue overview](#aws-glue-overview)
  - [AWS Glue: Create an S3 metadata crawler](#aws-glue-create-an-s3-metadata-crawler)
  - [AWS Glue networking](#aws-glue-networking)
  - [AWS Glue: Create a connection to RDS](#aws-glue-create-a-connection-to-rds)
  - [AWS Glue: RDS data crawler](#aws-glue-rds-data-crawler)
  - [AWS Glue: Create and run a job](#aws-glue-create-and-run-a-job)
  - [Glue and course teardown](#glue-and-course-teardown)
- [**7. Backup**](#7-backup) (4 videos)
  - [Understand AWS Backup](#understand-aws-backup)
  - [Create and configure an AWS Backup job](#create-and-configure-an-aws-backup-job)
  - [Restore from backup](#restore-from-backup)
  - [AWS Backup teardown](#aws-backup-teardown)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Storage and AWS](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/storage-and-aws?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/storage-and-aws?u=76281980&t=0)** - [Narrator] Do you need to move a semi truck full of disk storage? Well, if so, AWS has you covered. That's right, AWS Snowball will deliver a truck to your door. But for most enterprise applications, you can get the job done in the cloud. In this course, you'll learn the ins and outs of object storage and lifecycle management in S3, schema migration, backup, and replication of relational data in RDS, experience flexible network file storage with EFS, and even move and transform data with AWS Glue. Hi, I'm Brandon Rich. I'm an enterprise architect at the University of Notre Dame. I've been working in the cloud since 2014 when our organization began a massive migration of services, business processes, and data to the cloud with [[Amazon Web Services (AWS)|Amazon Web Services]]. In the process, we learned a tremendous amount and became a recognized leader in cloud operations for the [[Higher Education]] space. Now, let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon Web Services (AWS)|Amazon web services]] (1), [[Higher Education]] (1)
> **Env Vars:** aws (3), rds (1), efs (1)
> **CLI Commands:** aws (3)
> **Speakers:** - [narrator] (1)

#### [What you should know](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/what-you-should-know?u=76281980&t=0)** - [Instructor] If you're looking to store and manage data in the cloud, you've come to the right place. [[Amazon Web Services (AWS)|Amazon Web Services]] is home to a wide array of options to suit just about any [[Data Storage]] job you can throw at it, from file storage to relational data to [[NoSQL]] style documents and objects. In AWS, you have many options as to the kind of storage available to you. There's object storage supported by S3, Amazon's oldest and most durable service, block storage via Elastic Block Store, the easy way to provision drive capacity for [[Amazon EC2|EC2]] instances, [[Relational Databases]], courtesy of RDS, Amazon's managed relational database service. You have NoSQL courtesy of DynamoDB and [[Data Warehousing]] via Redshift though the last one is outside the scope of this course. When you use AWS storage and [[Data Management]] services not only do you gain access to all these storage types in one place, but you also take advantage of AWS's high level of service and reliability. You get incredible durability particularly with objects stored in S3, high availability, a built-in feature of many AWS storage services, automated backups, automated as with RDS relational databases, or able to be automated as with Dynamo or EBS, managed services where we don't have to worry about running the underlying servers or configuring and maintaining platforms, and seamless encryption at [[Representational State Transfer (REST)|REST]] as seen an S3 EBS and more.
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/what-you-should-know?u=76281980&t=94)** Think of the time, research, and resources that can go into making your own homegrown or on-premises storage achieve these features and you'll start to see why [[Cloud Storage]] particularly in AWS, is so compelling. I do want to give a heads up about which AWS services are not in scope for this series. The data analytics tools such as Elastic Map Reduce, Kinesis, and Quicksight are not discussed. Similarly, AWS's data warehouse, Redshift, is not covered in the course. For a closer look at data analytics and AWS, check out another course in this library called Amazon Web Services for [[Data Science]].

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon Web Services (AWS)|Amazon web services]] (2), [[NoSQL]] (2), [[Relational Databases]] (2), [[Data Storage]] (1), [[Amazon EC2|Ec2]] (1)
> **Env Vars:** aws (8), rds (2), ebs (2), ec2 (1), rest (1)
> **CLI Commands:** aws (8)
> **Analogies:** such as (1)
> **Warnings:** heads up (1)
> **Speakers:** - [instructor] (1)

#### [Exercise files](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/exercise-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/exercise-files?u=76281980&t=0)** - [Narrator] If you check the details for this course, you'll see a link to a [[GitHub]] repository that contains all the files that I'll be referencing throughout the course. Now, it's certainly possible for you to just listen and watch and follow along, but if you want to go along with me and perform the demos yourself these files will come in handy. Chapter two has a few data files that we'll use to interact with S3, as well as a policy template that we'll use to set up an SNS topic. Chapter four has some [[SQL]] code that we'll use to set up data in an RDS database and a [[JSON]] file that we'll use for DynamoDB. Chapter five has a subfolder full of files that we use to demonstrate the S3 [[CLI]] and a policy that we use to set up AWS transfer for SFTP. Finally, chapter six contains some SQL and some [[Python (Programming Language)|Python]] that we'll use while demonstrating AWS Glue. So whether you clone this repo or you're just downloading or referencing files from the web everything you need should be here to follow along with this course. Let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SQL]] (2), [[GitHub]] (1), [[JSON]] (1), [[CLI]] (1), [[Python (Programming Language)|Python]] (1)
> **Env Vars:** sql (2), aws (2), sns (1), rds (1), json (1)
> **CLI Commands:** aws (2), python (1)
> **Prerequisites:** set up (3)
> **Tools:** github (1)
> **Exercise Files:** template (1)
> **Speakers:** - [narrator] (1)


### 1. Overview

[↑ Back to Table of Contents](#table-of-contents)

#### [What is data management?](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/what-is-data-management?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/what-is-data-management?u=76281980&t=0)** - [Instructor] Your business or organization both depends on, and generates data. Product orders, government records, student transcripts, emails, project documents, even chat logs. Though it's no longer paper, that data we create still piles up, and quickly, causing genuine risk for organizations whose operations depend on their data. How do we make sure that we can access the data we need when we need it, while at the same time minimizing storage and maintenance costs? That effort is called Data Lifecycle Management, the act of devising and implementing policies to control data as it flows through your organization. What is the Data Lifecycle? Well, there are a number of definitions out there, but in short, it's a bit like the water cycle. Data progresses from inception to active usage to archive, and from there, either deleted for good or back into active usage. So when we talk about data Lifecycle Management, what we mean is enacting policies to migrate data to different tiers of storage as it progresses through that life cycle. As your data usage goes from frequent to rarely used, it can move from faster storage to slower storage, and thus from more expensive services to cheaper ones. This process can be automated letting data migrate tiers as policies, such as age or access patterns dictate, As we'll later see, S3 has built-in mechanisms for doing just that.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Definitions:** is called (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Data security in AWS](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/data-security-in-aws?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/data-security-in-aws?u=76281980&t=0)** - [Instructor] Key to ensuring your organization's security and compliance in the cloud is what AWS calls the shared responsibility model. The way AWS puts it, a shared responsibility model means that AWS handles the security of the cloud, while the AWS customer handles security in the cloud. What this means is that AWS provides assurances about the security and capabilities of their services and about the physical security of the [[Hardware]] on which your resources live. Meanwhile, you are responsible for understanding how to use these services in an appropriate way to satisfy your obligations. Let's start with security of the cloud. How can we be assured that AWS is doing all they can and should be doing on their side of the shared responsibility model? This might be a good time to look at some of AWS's security protocols. AWS states that their data centers are housed in nondescript buildings. They're secured at both the perimeter and at the facility itself. The security includes video cameras, security personnel, and more. Amazon also states that people wishing to access the data center floor must present multifactor authentication at least twice. This ensures that the people entering the facility are guaranteed to be who they say they are. Anyone who gets this far is also accompanied constantly by security staff. Not only that but AWS background checks all their employees, limits access to only those people
>
> **[1:32](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/data-security-in-aws?u=76281980&t=92)** with a legitimate business need for such access, and logs every visit to a remote location. All this should give you some assurance that no unauthorized person is going to tamper with the hardware running your [[Amazon EC2|EC2]] instance or storing your S3 data. But what about other threats? AWS goes to extreme measures to protect against fire and flood. They build their own power supplies and back everything up with onsite generators. And, of course, their data centers themselves are redundant and geographically separated. The multiple facilities that make up an availability zone are close enough to provide minimal latency for load balanced or distributed workloads but far enough away that a threat to one does not put the other at risk. This is of course why AWS always recommends replicating data and compute across multiple AZs. That's another part of the shared responsibility model. Shared responsibility extends not only to security but to redundancy and availability as well. While some services like EFS handle replication for you, other AWS services leave it to you. If you don't architect your applications and [[Data Storage]] to take advantage of the high availability options AWS offers, you may end up putting your business at risk. That's just a tip of the iceberg as far as AWS's security of the cloud. Within the data centers of AWS, you'll find state-of-the-art advanced networking monitoring, world-class [[Intrusion Detection]], strict change management practices,
>
> **[3:05](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/data-security-in-aws?u=76281980&t=185)** and regular third party audits to ensure the [[Solidity]] of all the above. If you're interested in the details, just search for [[AWS Security]] white paper and you can read pages upon pages of detailed assurances about the security of AWS. Suffice it to say that AWS spends more time, money, people, and effort on security and operations than most organizations could ever dream to match. Consider that when you're wondering if your data will be safer on premises than in the cloud. So, what about security in the cloud? What's your responsibility? Well, it comes down to understanding your requirements, the capabilities of AWS, and how to properly configure each AWS service to match those requirements. For instance, RDS, the relational database service provides an option for replicating across availability zones but you must enable it yourself. If you don't, in the single instance of the RDS database is lost or compromised, AWS will not be able to do anything about it. Similarly, elastic block storage, or EBS, allows your data volumes to persist after host terminates and to be snapshotted and backed up at will. However, if you don't enable those settings to take advantage of those features, their usefulness will be nil in a disaster scenario. S3 is a perfectly safe place to store data but AWS will not stop you if you choose to set a security policy that makes it public. And encryption works the same way. EBS, S3, and EFS all offer seamless server site encryption
>
> **[4:42](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/data-security-in-aws?u=76281980&t=282)** but you have to opt in. Now in some cases, your responsibilities may go beyond what AWS provides. When that happens, you'll need to take additional steps. For instance, if you must meet stricter encryption standards than what is satisfied by the available AWS options, you'll need to perform your own encryption prior to sending data to the cloud. As long as you do this, just like you would on premises, you'll be able to meet your requirements and still enjoy the benefits of AWS storage and compute.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hardware]] (2), [[Amazon EC2|Ec2]] (1), [[Data Storage]] (1), [[Intrusion Detection]] (1), [[Solidity]] (1)
> **Env Vars:** aws (25), efs (2), rds (2), ebs (2), ec2 (1)
> **CLI Commands:** aws (25), make (1), find (1)
> **Analogies:** for instance (2), just like (1)
> **Prerequisites:** you'll need (2), configure (1)
> **Definitions:** means that (1), is a  (1)
> **Speakers:** - [instructor] (1)

#### [Regulatory compliance and AWS](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/regulatory-compliance-and-aws?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/regulatory-compliance-and-aws?u=76281980&t=0)** - Depending on your particular industry, you may be obligated to demonstrate compliance with any number of regulations regarding data movement and storage. A few examples include [[PCI DSS]] for the handling of financial information, HIPAA and HITECH for health information, and FERPA for the handling of educational records. Some IT professionals, and even CIOs may fear that moving data to the cloud, may put their ability to comply with regulations at risk. If that sounds familiar, fear or not. AWS goes to great lengths to make sure that whatever your legal requirements, you can work with AWS to ensure compliance, and take full advantage of all the cloud has to offer. AWS provides detailed guidance regarding many different regulatory frameworks. If you go to aws.[amazon.com/compliance](https://amazon.com/compliance), you can drill down into any number of regulations, and find out more. If we scroll to the bottom of this page, and click the link, 'Compliance Resources,' we'll find a list of white papers that AWS provides, to help you understand how to do compliance in the cloud. For instance, architecting for HIPAA security and compliance on [[Amazon Web Services (AWS)|Amazon Web Services]]. Clicking that opens a pdf. All this content used to be exclusively provided by pdf, but now we see a link here that takes us to a web version of the content. You want to pay particular attention to this section. Each of the documents of this kind, provides details on which AWS services are suitable for keeping that kind of compliant data. It'll even tell you which features
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/regulatory-compliance-and-aws?u=76281980&t=94)** within individual services, you should avoid or specifically configure to stay within the law. In fact, AWS devotes an entire section of its web console to keeping you aware of compliance matters, and security on the could. It's called, AWS Artifact. Here I am at the main console of Amazon Web Services. You access AWS artifact the same way you'd access any AWS service, such as [[Amazon EC2|EC2]] or RDS. It's an entry in the main menu. So I'll click artifact here, and then choose 'View Reports.' Inside, we'll find a list of documentation that AWS makes available related to its own compliance status. You'll find ISO Reports, PCI DSS, SOC reports and more. When you choose to download one of these documents, your copy will be watermarked with a special code identifying you as the original requester. So, don't share your copy. Because of this, I won't demonstrate downloading the documents, but I will give you one [[Microsoft Word|word]] of advice. Use Acrobat Reader to open it. The documents come in a special format, where the main document is a set of terms and conditions, while the actual content you're after, is an attachment to that. The text provides some instructions on how to access the content with Adobe Acrobat Reader. But, if you're using something like preview on the Mac, it may not be clear how to reach the content. Of course, some regulatory frameworks are stricter than others. If you're a government or higher ed institution subject to FITARA, FedRAMP or SRG,
>
> **[3:08](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/regulatory-compliance-and-aws?u=76281980&t=188)** you may want to look at GovCloud. To find out more, we'll go to aws.[amazon.com/govcloudus](https://amazon.com/govcloudus). GovCloud is an entirely distinct region of AWS, that is in many ways identical to other regions, like Virginia's US East One, except that GovCloud is a hundred percent housed within the US, and accessible only by US citizens. GovCloud has a provisional ATO, or authority to operate under the FedRAMP high baseline. These are crucial features for organizations subject to ITAR, or US International Traffic and Arms Regulations. Customers must create and maintain a fully separate account for GovCloud, distinct from any other AWS accounts they might operate, and they must sign a GovCloud specific contract. They must be quoting AWS documents now, "US persons not subject to export restrictions, and who comply with us export control laws and regulations." Organizations must for GovCloud from an existing account, or through an AWS business representative. If your organization is subject to [[Regulatory Compliance]], chances are, you know already a lot about the requirements of your particular responsibilities. Using AWS doesn't change any of that. It just means that there are now layers in the stack that are managed by AWS. It might sound concerning to give control of any part of your critical compliance responsibilities to AWS, but if you take advantage of Amazon's clear documentation, and understand your part in the shared responsibility model, you should feel quite at ease.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PCI DSS]] (2), [[Amazon Web Services (AWS)|Amazon web services]] (2), [[Amazon EC2|Ec2]] (1), [[Microsoft Word|Word]] (1), [[Regulatory Compliance]] (1)
> **Env Vars:** aws (17), pci (2), dss (2), hipaa (2), hitech (1)
> **CLI Commands:** aws (19), find (5), make (1)
> **Definitions:** is a  (2), is an  (2), means that (1)
> **URLs:** [amazon.com](https://amazon.com) (2)
> **UI Navigation:** go to (2)
> **Analogies:** for instance (1), such as (1)
> **Prerequisites:** configure (1)


### 2. Object Storage

[↑ Back to Table of Contents](#table-of-contents)

#### [AWS S3 basics](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-s3-basics?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-s3-basics?u=76281980&t=0)** - [Instructor] Launched way back in March of 2006, S3 or Simple Storage Service is one of Amazon's longest running AWS offerings. Its purpose is to provide easy to use [[Cloud Storage]] that is scalable, highly available, and reliable. Before we start, it's important to know that S3 isn't actually a file system in the traditional sense. Rather, S3 provides what's known as object storage. What distinguishes object storage from file storage? Well, storage devices come in a few flavors having to do with the level at which the storage system manages data. Block storage operates at the level of disk blocks and sectors. This is the lowest level to the [[Hardware]]. Sectors originally referred to actual sectors on the circular magnetic disks and platter based hard drives. In the age of solid state drives, sectors are more of a logical construct but the concept remains the same. Block storage is generic and can be formatted to apply a file system on top of it. File storage is what you're probably most familiar with. A file storage device uses a file system, software that manages files in directory structures sometimes with permission bits that describe who can read, write, and list the files. Examples include FAT32 and NTFS for [[Windows]], HFS for Mac and Ext4 for [[Linux]]-based systems. Object storage is a bit different. An object might be a file from your computer or server's file system
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-s3-basics?u=76281980&t=94)** but once placed into object storage, it's an object data with [[Metadata]] attached. In S3, this metadata often takes the form of tags. The same kind of tags that appear on resources throughout AWS. Objects go into Buckets. A Bucket is called that because you can basically throw anything you want in it. In a real life bucket, there's no structure. There are no shelves. Likewise, an S3 bucket has no native concept of folders or hierarchies. It's all one flat name space for objects inside. Of course, unlike a real bucket, S3 buckets can hold a virtually unlimited amount of objects. S3 can easily scale to hold billions of objects. It's also very important to understand that unlike with a file system, your server can't natively mount object storage. Rather, your host will talk to S3 via restful APIs, through an SDK like the ones for [[Python (Programming Language)|Python]] or [[Java]] or using some kind of abstraction layer. For instance, Ruby applications can use a gem called Fog to abstract S3 and treat it more like a file system. They're also hardware and software gateways that can provide this abstraction for an entire server, such as AWS's file gateway. In the case of file gateway, the middle layer actually does provide an NFS mount, so your instance can take advantage of S3 storage while still talking to a typical file system. Back to Buckets. The name space for Buckets is global which means that each time you create a Bucket
>
> **[3:08](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-s3-basics?u=76281980&t=188)** you'll need to come up with a name that's unique worldwide. Object names within a Bucket can contain letters, digits, or certain special characters, including slashes. Speaking of slashes, I said there's no concept of folders and object storage, and that's true. However, S3 goes a long way toward pretending that there is. Take a look at these two object names. Knowing about the flat name space, you may recognize that these are just object names that happen to share some of the same characters, but S3 will visually represent them as two objects within a files folder. This convenience is a bit confusing when you know the truth but in practice it rarely causes issues. In fact, the folder metaphor runs pretty deep depending on which interface you use. These days, the web console even has a create folder button and it doesn't even let you set key names with embedded slashes. So, what distinguishes S3 from any other object storage? In the next video we'll get into S3's incredible reliability, security, and [[Data Management]] features.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hardware]] (2), [[Metadata]] (2), [[Cloud Storage]] (1), [[Windows]] (1), [[Linux]] (1)
> **Env Vars:** aws (3), fat32 (1), ntfs (1), hfs (1), sdk (1)
> **CLI Commands:** aws (3), python (1), ruby (1)
> **Definitions:** is a  (2), known as (1), is called (1), means that (1)
> **Analogies:** for instance (1), such as (1)
> **Prerequisites:** before we start (1), you'll need (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Durability and availability](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/durability-and-availability?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/durability-and-availability?u=76281980&t=0)** - [Instructor] Let's talk about S3's Vaunted Availability and Durability. Availability refers to the S3 service itself. Will S3 respond when you make a request to store or retrieve an object? Durability refers to S3's ability to keep your data without corrupting it or losing it. So here are the numbers. Objects stored in S3 are 99.99% available in standard storage mode and as for durability, well, I'm not even going to try to say that, but it's 11 nines. AWS claims that with that level of reliability, if you were to store 10,000 objects in S3, you might lose one object every 10 million years. That's a level of certainty that goes well beyond that of any physical storage I've ever maintained myself, no matter how redundant. As for that availability, well, 99% uptime doesn't mean that S3 never goes down, but if it does, the service level agreement clock starts to tick down. If S3 is out for long enough users will start to see refunds on their bill. This actually happened back in early 2017 when a misconfiguration caused S3 to go down for several hours. With so many customers, you can bet that this money back guarantee provides AWS with plenty of motivation to keep their services up and running. Curiously, the wide array of services that suffered concurrent service interruptions, such as [[Slack]], [[Trello]], and many more, only serve to demonstrate how widely used and trusted S3 is as a Cloud [[Data Storage]] provider. During that time,
>
> **[1:32](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/durability-and-availability?u=76281980&t=92)** those companies weren't scrambling to restore service. They knew they could rely on AWS's world-class engineers to keep them informed and correct the problem. AWS offers multiple ways for you to secure your data in S3 with both client side and server side encryption. Once enabled, server side encryption in S3 provides seamless encryption of data at [[Representational State Transfer (REST)|rest]]. At rest means while the data is sitting in S3. Seamless means that the client doesn't even need to know about the encryption in order for it to work. Objects uploaded to S3 get encrypted before they're stored while objects downloaded from S3 get decrypted before they're sent to you. When you create a bucket, you are offered three choices, and you can encrypt with an S3 dedicated key that's specific to your account. It's auto-generated by AWS and shared with no one else. You can encrypt with a key of your own creation using KMS, AWS's Key Management Service. KMS is a service that lets users generate encryption keys and helps with tasks like rotating and expiring them over time. Finally, you can encrypt with your own BYO key, which means client side encryption. In this case, you'll handle encryption and decryption yourself before you pass data off to S3. In both server side methods, S3 performs seamless encryption, meaning you don't have to worry about the details. You just pass data in and out, knowing it's encrypted while at rest. How about encryption in transit?
>
> **[3:04](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/durability-and-availability?u=76281980&t=184)** Requests to and from S3 made through the AWS console are always encrypted via TLS. Requests using the AWS [[CLI]] are too. Any REST request is encrypted too, as long as it's made via HTTPS. If you make REST requests over plain HTTP, your data will not be encrypted in transit. S3 provides multiple different tiers of storage for different use cases. Within a single bucket, individual objects can exist in one of the following, standard storage is the default, highly durable basic S3 storage, as you go down the tiers storage becomes cheaper, but there are trade-offs. Infrequent Access Standard is essentially the same as S3 standard, but with fewer nines of availability guaranteed. It's durability is the same. Next, Infrequent Access One-Zone lets you trade a little more availability for cost. Unlike the first two S3 classes objects stored of this tier, are not replicated across three or more availability zones. They only exist in one, making them potentially vulnerable to an AZ outage, a rare event in itself. Finally, Intelligent-Tiering, watches your data usage patterns to automatically shift individual objects between storage tiers designed for frequent, infrequent, and rarely accessed data. Glacier, once its own distinct service, is the name for S3's multiple tiers of long-term archival storage. These storage classes give you the cheapest possible storage cost as long as your access patterns are infrequent.
>
> **[4:39](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/durability-and-availability?u=76281980&t=279)** Outside of cost, the main difference in the three, is how often and how quickly you can retrieve data. Glacier Instant Retrieval can return data just as fast as standard S3, in milliseconds when data is retrieved once per quarter. You'll be charged more if you need your data more often. Depending on your use case that might make standard and frequent access a better choice. Glacier Flexible Retrieval is an even cheaper option if your access needs are closer to one to two times per year. Finally, Glacier Deep Archive is the cheapest, longest term object storage you can get in AWS. If you have data that you need to retain for years and years, but only need to access maybe once or twice a year, this is the class for you. Retrieval at this tier is the slowest of all, up to 12 hours, but for certain cases, such as compliance related document storage, it's ideal. Each S3 tier has nuances in the cost models, such as the minimum object size and minimum storage duration. So make sure to read the docs at aws.[amazon.com](https://amazon.com), as you make your decision. Now let's dive into the S3 interface and see what we can learn.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Representational State Transfer (REST)|Rest]] (5), [[Slack]] (1), [[Trello]] (1), [[Data Storage]] (1), [[CLI]] (1)
> **Env Vars:** aws (9), kms (2), rest (2), byo (1), tls (1)
> **CLI Commands:** aws (10), make (5), az (1)
> **Definitions:** refers to (2), means that (1), is a  (1), is an  (1)
> **Analogies:** such as (3)
> **Tools:** slack (1), aws console (1)
> **URLs:** [amazon.com](https://amazon.com) (1)
> **Versions:** 99.99 (1)

#### [S3 bucket creation](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-bucket-creation?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-bucket-creation?u=76281980&t=0)** - [Instructor] Let's get started by creating an S3 bucket. Here I am at the main dashboard of [[Amazon Web Services (AWS)|Amazon Web Services]]. And before we go into the S3 specific section, I want to point out this dropdown which for me currently says North Virginia. Here in the top right. If I click it, you'll see a list of all the regions in which AWS operates. This dropdown exists on most screens in Amazon Web services and it's used to determine where the resources that you provision actually get created. When you have a region selected, that'll filter the view in most parts of AWS to show you just resources that exist in that region. So, you could give yourself a shock by working in one region and changing regions and forgetting that you've changed it and wonder where all your resources have gone. So, if that ever happens to you, just look up here and make sure that you're working in the region that you expect to be in. For purposes of this course, the best thing to do is just pick the region that's closest to you geographically, which for me is that North Virginia and stick with it for the duration. Now we'll head into S3. I've recently visited that service, so it's right here in the top under recently visited. I can also click services and see a categorized view of AWS. There's S3 right at the top, or I can search here by typing S3 and selecting the first option. If this is your first time into S3, you'll see a screen that looks like this. We can click Create Bucket, and here we are ready to configure our first bucket. We need to give it a name. So, I'll call this demo primary. And remember, S3 bucket names need to be globally unique
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-bucket-creation?u=76281980&t=95)** so I'm going to append my name here and you should do the same. Now, let's take a look at this region dropdown because it looks a little different. It says global and all the regions are grayed out. S3 is one of the few places in AWS that acts a little differently from the [[Representational State Transfer (REST)|rest]]. It says global because when you look at a list of buckets in S3, it's going to show you buckets from every region. Now, S3 buckets do have a region as you can see right here. We're going to select the region in which to create the bucket. Buckets are region specific. It's just that the S3 part of the console shows you everything at once. There's no filter. That's why it says global. So, choose your region right here. Make it the same one that you chose earlier to stick with for the duration of the course and scroll on down. Under object ownership, you can keep the default recommended choice of ACLs disabled. If you choose the other option, that will let you set individual object level permissions which is not what we are interested in for this demo. We'll keep the default, block all public access. We're going to be interacting with this bucket from and within AWS, so we won't need any public access. Bucket versioning we do want to enable. Then, I'll let S3 keep track of changes to each individual object in the bucket. Under tags, this is optional, but we can add a tag. We get a key value pair input here and this can be whatever we want. So, I'm going to call this key creator and put Brandon so that anybody who looks at this bucket later will see that information and know who created the bucket.
>
> **[3:10](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-bucket-creation?u=76281980&t=190)** You can add more tags if you like. Sometimes these may just be informational for other users of this account, or in some cases, they could be used by third party services or other add-ons that will take some action for you based on tags on resources in your account. Let's move on to encryption. This is disabled by default but you can enable it and you get two options. You can get Amazon S3 managed keys which is the key that's created for your account by Amazon, managed by them, or you can choose KMS keys. If we were to choose the latter, we would need to have already created a KMS key in a different part of AWS. So, we'll just stick with that first option. We don't need advanced settings so we can just click create bucket. Okay, now we have our bucket created and we can click into it. And we can see that we don't have any objects yet. So, there's nothing under objects. We have a couple other tabs, including this properties one that has different options for us. You can see that we have versioning enabled. We can see the tags that we created. And if you scroll down, you can see this server access logging. This is something that I would like to enable. If we click edit and enable that you can see that we're going to be asked to give a different bucket for the logs to go into. So we actually need to back out of here because we don't have another bucket yet. So, hit cancel and you can actually click over here and go to buckets. Then let's just create another bucket by clicking Create Bucket. We'll give it a different name. We'll call it demo logging and again,
>
> **[4:44](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-bucket-creation?u=76281980&t=284)** I'll append my name there just for uniqueness. Keep all the other options at their default and click Create Bucket. By the way, very important on this logging bucket. Don't enable versioning. That's just going to create a whole lot of versions as logs get written to this bucket. So here we have our two buckets, demo primary and demo logging. And we'll click into the primary bucket. Go to properties, head down to server access logging. Click edit, enable and then browse S3 to choose our destination bucket, demo logging. Click Choose Path. And then we could from here, just save changes. But there's one additional thing that I can do. You can see the prompter that says prefix. And remember, that if I use slashes in a prefix, S3 basically treats those like folders. So what this lets me do is say, use one logging bucket to be the logging destination for multiple S3 buckets. And the easiest way to do that is to provide a subfolder for the logs to go into on a per bucket basis. So, we'll create a prefix here called demo-primary. I guess for completeness, I'll go ahead and put the full bucket name there, and then save changes. Let's remember to put a slash at the end of that so it acts like a folder and click save. So with that done, we are ready to add some objects to the bucket and see how this logging works.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon Web Services (AWS)|Amazon web services]] (2), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** aws (6), make (2)
> **Env Vars:** aws (6), kms (2)
> **UI Navigation:** dropdown (3), go to (2), select the (1), scroll down (1)
> **Best Practices:** recommended (1), remember to (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [S3 upload and logging](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-upload-and-logging?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-upload-and-logging?u=76281980&t=0)** - [Instructor] All right, now that we've got our bucket, let's click it and add some files to it. We can see on this screen that we've got a couple of tabs of different perspectives on the bucket that we'll explore in future videos, and we have no objects in the bucket. You can see the count here is zero, and we've got an Upload button, so let's click it so that we can add some things. We'll get the Add Files button, we'll click that. And if you'll look in the exercise files of this course, you'll see Chapter 2 has a few data sets that we can upload. These are obtained from [data.gov](https://data.gov), so just freely available for anybody to pick up. I've got one about Marilyn Farmland here, and we can also click the states.csv file, and go ahead and say Open. This will allow both of these files to be uploaded at once. If we look at properties, we can expand that, and we can see storage class, which you may now be realizing was not something that we selected at the bucket level. So every individual object in a bucket can have a different storage class. If you think back to the slides on object storage and S3, you'll remember some of these. We've got standard by default, with its really good durability and availability. You've got standard IA here, which is infrequent access, so that trades a little bit of that availability for reduced cost. You've got intelligent tiering, which will move your data around to different tiers based on usage and access patterns. And of course you've got the glacier tiers as well. We'll leave these at standard for now and scroll on down.
>
> **[1:36](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-upload-and-logging?u=76281980&t=96)** Server-side encryption, so we already selected this at the bucket level, and that'll take place for all the objects to go into the bucket, so we don't need to do anything here. Check sums are an optional feature that you can turn on on a per object basis that will give you a hash that you can check over time with your reference that you keep to make sure that there's been no degradation in an individual object. That's a pretty rare event, but this is a useful feature for archivists and folks like that. Finally, we've got tags and [[Metadata]]. You'll remember that we set tags at the bucket level to show who created the bucket. We can do the same here. We can add a tag and we could put a creator tag here, or any other type of tag. You know, these are all CSVs. We could say type of CSV. Anything that you might find useful. Metadata we've got here is similar but slightly different. It's the metadata that's actually attached directly to the object. This is different from tags, in that metadata is something that you'll get anytime that you request an object, say from get request through the API or an SDK request for the object, you'll see that metadata. Tags are like tags that exist throughout AWS. These key value pairs that can be pulled up by a dedicated API call that says, what are the tags on this resource? And one of the main differences is that tags, capital T tags, can be used in rules for IM policies, access policies, things like that on a per object basis.
>
> **[3:10](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-upload-and-logging?u=76281980&t=190)** So we'll go ahead for now and just say Upload. And with that we've got some data in the bucket, and we'll go on to the next video where we'll see how we can do some cross region replication with these objects.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Metadata]] (5)
> **Env Vars:** api (2), csv (1), sdk (1), aws (1)
> **CLI Commands:** make (1), find (1), aws (1)
> **File Paths:** states.csv (1)
> **URLs:** [data.gov](https://data.gov) (1)
> **Cross-References:** next video (1)
> **Exercise Files:** exercise files (1)
> **Definitions:** is a  (1)

#### [S3 cross-region replication](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-cross-region-replication?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-cross-region-replication?u=76281980&t=0)** - [Instructor] Another powerful feature of S3 is the ability to replicate automatically across regions. Now, we've already talked about how great the durability and availability of S3 is. So why would we want to replicate across regions? Well, first, it's not impossible for a region to have an outage. There have been a couple of examples of this occurring in the history of AWS, and so it can be a good safeguard to have things not just replicated across availability zones, but also across regions. It's a rare event, but it can occur. And the other reason you might want cross-region replication is because as I said before, you want to choose your region based on proximity to the users or workload that would be accessing the S3 data. So if your workload is actually cross-region, say if you're a global organization, it might make sense to replicate your data across regions for performance purposes. So with that said, let's take a look at how to enable cross-region replication on our demo bucket. Before we do this, we're actually going to need a destination bucket in another region. So to do that, we'll go ahead and click Create Bucket. We'll give a name, we'll call it demo-secondary-region, and then initials just like before to make the name unique. And we need to select an alternate region. I'm going to scroll down here and I'm going to choose Ireland. Beautiful country, good place for our objects to go and we'll go down and choose versioning enabled. Other than that, we'll leave all the options
>
> **[1:32](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-cross-region-replication?u=76281980&t=92)** at default and click Create Bucket. Now that our destination bucket is created in the Ireland region, which you can see here, we need to click back into the primary bucket, which is called Demo Bucket. From here, we'll choose management. Then go down to replication rules and click Create Replication Rule. Now, we need to give this rule a name, so I'll call it replicate-to-Ireland, and its status will be enabled. If we go down, we see the source bucket and some options here to limit or filter the objects that get moved from the source bucket to the destination. So you have the option here if you wanted to say, create different rules for different subsets of your data or just exclude some objects entirely for whatever reason, you can do that. But in this demo, we want everything to move over. So we'll leave things as they are at default. Scrolling down to destination, you want to click browse S3. So we can choose our destination bucket. That's demo-secondary here, and choose Path. All right, that's set. Let's go down to IM role. Okay, so what is this all about? Well, in order to do the replication, AWS needs to act on your behalf. It needs to be able to take the objects out of the source bucket and replicate them across to the other bucket. So as you will see in plenty of places in AWS, the service itself needs to assume a role with permissions. So this is the same kind
>
> **[3:06](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-cross-region-replication?u=76281980&t=186)** of role that you would give to a user. You're just giving it to the S3 service. Now, you've got a couple of options here, including choose from existing IM roles, and if I click on this, I will see there's an option that says Create new role. And because I'm currently logged in as a user that can create IM roles, that is a root user in this case, I can choose this option and this wizard will create that IM role for me behind the scenes. So encryption, we don't need to worry about but destination storage class is interesting. If you click this, you'll find that you can change the storage class that objects get replicated into so they can be something different from what they are in the primary bucket. If your replication was something that you thought maybe wouldn't be used very often, you could put it in a cheaper tier of storage so that you would save a little money that way. Of course, you're always making a choice between cost and your use case. There are a couple of advanced options here that we could touch on. Replication time control is something that gives you additional auditing and logging of your replication. It notifies you if an object fails to be replicated ever within 15 minutes. This can help you achieve some compliance goals with your S3 storage if that's a requirement for you. Delete marker replication is interesting. If your source bucket has versioning enabled, it never actually deletes anything. A delete action on an object is just like a change that gets recorded by the versioning over time. So instead of actually fully getting deleted,
>
> **[4:38](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-cross-region-replication?u=76281980&t=278)** it gets a flag set on it, which S3 calls a delete marker. So the question here is whether that delete marker should get propagated over to the destination bucket. Since we want these buckets to look exactly the same, I'll go ahead and click them. Replica modification sync here is something similar for [[Metadata]] on objects so I'll click that too and choose save. Now, I get a question here whether to replicate existing objects, and we'll say yes because we want to go ahead and move those objects that are already in the bucket. Click submit. Now we see this screen, create batch operations job. All that rule setup leads into this batch job that runs regularly to replicate the data from one region to another. You can see we've got an option here to automatically run the job when it's ready, which we would love for it to do. So we see these results immediately. You can get a completion report out of this job but I'm going to uncheck that because I don't think we need to configure that right now. Let's go ahead and keep the [[Representational State Transfer (REST)|rest]] of things at the default and click save. All right, now you can see that we have this job. If we click this refresh button, we should see the status that it's in. So it's trying to replicate the existing objects. It's in a preparing stage and you can continue to monitor it by clicking this. Now, in the future, this will be an ongoing task, so you won't ever have to kick this off again or sit there and watch it, but it will just happen on its own.
>
> **[6:17](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-cross-region-replication?u=76281980&t=377)** All right, now the job's in Ready state, so it's about to run. Now it's active. And after a few minutes, the job is in complete state. So you see it found two objects, 100% complete. We can click this hamburger menu over here, go back to buckets, then click into the Ireland bucket demo-secondary and see that those objects exist. If we want to take this a step further, we can go to buckets, back to the demo bucket and upload a new file. I'll click add files and choose the Parks_and_Landmarks_in_Detroit.csv file, and click upload. And that's now uploaded to our primary bucket so we can close that. Head back to buckets and into the secondary. And in a few moments, we'll be able to refresh this and see that the object has been replicated automatically to the Ireland region. And now a few minutes later, we see that file too has been replicated to the cross-region destination.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Metadata]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** aws (3), make (2), find (1)
> **Env Vars:** aws (3)
> **UI Navigation:** scroll down (1), click on (1), go to (1)
> **Definitions:** is called (1), is a  (1)
> **Prerequisites:** setup (1), configure (1)
> **File Paths:** parks_and_landmarks_in_detroit.csv (1)
> **Cross-References:** go back to (1)

#### [Set up SNS for event notifications](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/set-up-sns-for-event-notifications?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/set-up-sns-for-event-notifications?u=76281980&t=0)** - [Instructor] Another really powerful capability of S3 is the ability to send notifications to other AWS Services when something happens in the bucket. You can send messages to SNS, the Simple Notification Service; to SQS, the Simple Queue Service; or to Lambda Functions, where you can actually execute code in response to something changing in S3. For this demo, I'd like to set up an SNS topic and show you how to connect it to the S3 bucket to receive notifications via email when certain events occur. So here, on the home console, in the search bar, let's type SNS and choose the first option, Simple Notification Service. SNS is a pub/sub service. It lets you create message topics so that you can blast out notifications to subscribers via email or text messaging. Today, we'll set things up so that we get emails when things change in S3. So here, we want to create a topic, and we'll call it S3DemoNotifications. Click Next Step. You can see here that you have a couple of options for how this message topic works. Standard means at least once message delivery, which means there's a possibility of duplicates, so if that were a problem for your use case, you choose the first in, first out style. For us, standard works just fine. We've got our name. Display name is optional, so we can skip that. In fact, we can just scroll all the way down and click Create Topic without changing any of the defaults.
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/set-up-sns-for-event-notifications?u=76281980&t=95)** Okay, now that the topic is created, we have one more step. With the pub/sub model in SNS, you can have multiple subscribers to the one publisher, that is, the message topic. So right now, we don't have any subscribers. We need to create a subscription to this topic so that we can receive email whenever S3 has a notification for us. I'll click Create Subscription. The protocol here we'll choose will be email, And the address is an address I've created for purposes of receiving the mail. You should put your own address in here if you want to try it out. Click Create Subscription. Okay, now the subscription is created, but before I can start to receive emails, you can see we've got a status here of Pending Confirmation. That means I need to go into my email account and respond to a message that will ask me to confirm the subscription. So I've got this open in another tab here, and you can see I've got subscription confirmation here as an email, and all I need to do is click this link, and that will confirm my subscription, which means now, we can go back into SNS. And if we refresh the page here, we ought to see that the status is updated. So now, we're confirmed. We can click back to the topics list, go into S3DemoNotifications, and we can test things out real quick. Click Publish Message, and we'll give a subject, just something like, "Hello, world,"
>
> **[3:08](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/set-up-sns-for-event-notifications?u=76281980&t=188)** and then some kind of message body here. Just say, "This is a test message." Scroll down, and we'll click Publish Message. So this will allow us to confirm that we are receiving emails from the SNS topic. Look here in my inbox. There we go. "Hello, world," test message. That is all working. But before we can have S3 start to send notifications to this topic, there's one more thing we need to do. We need to set an access policy on the topic that will specifically allow our demo bucket to publish events to it. If you're here in the topic and click Access Policy, you can see the current policy, but it's read only. Head up to the Edit button here, and we'll be able to change it. Expand Access Policy, and you can see that this section is now editable. As for what goes in here, well, if you look in the chapter two section of the exercise files associated with this course, you will see a document called sns_publish_policy_template.[[JSON]]. If you copy this, we can fill in the values that will make this work. So copy, head back in here, erase what is already there, and paste the contents. Now, if you look here, you'll see that we're allowing the S3 service to publish to this SNS topic, and what we need to provide is the unique identifier for the S3 bucket as well as the SNS topic. So from here, we need to open up some new tabs so that we can keep this window open
>
> **[4:43](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/set-up-sns-for-event-notifications?u=76281980&t=283)** but also go [[Fetch]] those identifiers. The first thing to do is go up here to the name, S3DemoNotifications, right click, and choose Open Link in New Tab. That will let us have the details of the topic here, and we can copy the ARN, that unique identifier, head back to the first tab, and paste that over the SNS topic ARN. So erase that, paste that in. Now, here on the second tab, again, I will go type S3 to go to the S3 section. Find our demo bucket. Go to Properties, and copy its ARN. In this case, I can click the Copy button, and that will go into my clipboard. Back here, I'll paste that over S3 bucket ARN, make sure there are no additional spaces, and we're good. I'll go down and hit Save Changes. Okay, now that we've got the SNS topic all set up, in the next video, we'll head back to S3 and configure the event delivery that'll make this all work.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (1), [[Fetch]] (1)
> **Env Vars:** sns (11), arn (4), aws (1), sqs (1)
> **CLI Commands:** make (3), aws (1), find (1)
> **UI Navigation:** go to (2), scroll down (1)
> **Definitions:** is a  (2), is an  (1)
> **Prerequisites:** set up (2), configure (1)
> **File Paths:** sns_publish_policy_template.json (1)
> **Code Identifiers:** sns_publish_policy_template (1)

#### [Set up S3 for event notifications](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/set-up-s3-for-event-notifications?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/set-up-s3-for-event-notifications?u=76281980&t=0)** - [Instructor] Okay, following up from the previous video, we've got our SNS topic all created and ready to receive events from the S3 bucket. So now we just need to go configure that on the bucket itself. That demo bucket is still open here in the second tab and it's open to the Property section which is where we need to be. Scroll on down to Event notifications and click Create event notification. Here we need to think a little bit about what kinds of notifications we want sent from this bucket. For this demo, we'll send an email anytime something is deleted out of the bucket. Maybe we've got important files in there and we want a notification anytime that happens. So we'll call this S3DeletionEventNotification. And we'll scroll down here to Event types. This is where we can select all the types of events that we want to show up in our email whenever they happen. I'm interested in Object removal. If I click Permanently deleted or Delete marker created, that'll cover everything. But there is this all-encompassing section over here if I check it, called All object removal events which encompasses both of these. So that'll be good and I'll scroll on down to the bottom. The destination is an SNS topic, of course, and we need to choose our topic. Here in the dropdown, we see the one we just created, S3DemoNotifications and we can click Save changes. Now, all we need to do test this is to go to our Object section and delete something.
>
> **[1:36](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/set-up-s3-for-event-notifications?u=76281980&t=96)** So I'll select this first one, Maryland_Farmland. Check the box and click the delete button. To confirm this I'll need to type the [[Microsoft Word|word]] delete here, and say delete objects. Okay, that's successful. We can close this and then head back to email to see if we get our notification. And there we have it. So I have full details on what happened. Delete marker created. Remember, this is a version bucket so a full delete didn't happen. We got a delete marker set on that object and I have complete details on what happened here in my email. This is the same kind of information that you could send to a Lambda and take action in any other service in Amazon based on this information. So it's a very powerful feature that can unlock a whole lot of functionality in AWS.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **Env Vars:** sns (2), aws (1)
> **UI Navigation:** scroll down (1), dropdown (1), go to (1)
> **Definitions:** is an  (1), is a  (1)
> **CLI Commands:** aws (1)
> **Cross-References:** previous video (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [AWS Glacier and S3 lifecycle rules](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glacier-and-s3-lifecycle-rules?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glacier-and-s3-lifecycle-rules?u=76281980&t=0)** - [Instructor] Let's talk S3 Storage Tiers and lifecycle policies. First up, we have S3's primary offering S3 Standard. This general purpose tier sports an amazing 11 nines of durability, which in fact all S3 Tiers do. It also has four nines of uptime meaning that on average you might experience one minute of unresponsiveness a week. In practicality, the service is much more reliable than even that. As the advertised numbers take into account the effect of the rare actual service outage where you might accumulate several minutes of downtime at once. To get a sense of the low cost of AWS objects storage, this most expensive S3 Tier, will run you about 2.30 cents per gigabyte per month. With prices falling the more you store. So that's S3 Standard. From here, we'll be making trade-offs, lower cost but along with that, certain concessions In terms of things like uptime and speed of retrieval. Intelligent tiering is a storage class that can move data for you automatically by watching your access patterns. It charges a small amount to monitor your data but then moves objects between frequent, infrequent and archive instant access tiers without your intervention. It's worth noting that tiers, that's a tears used by intelligent tiering are not technically the same as the other S3 Tiers we're discussing in this video. This storage class kind of stands on its own and has its own internal tiers.
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glacier-and-s3-lifecycle-rules?u=76281980&t=94)** You could basically think of this as an easy button for managing data life cycles but if you'd like a little more control over your object storage, you want to keep listening. The next two storage tiers trade a bit of availability for cost. The IA in Standard-IA stands for infrequent access. If you don't need to get to your data all that often, give this tier a try. Do notice that at some point you run into some storage minimums. First, objects that are deleted before 30 days incur a small monetary penalty. Second, for billing purposes, each object is treated as if it were at least 128K. This is no doubt due to some internal quirk of storage allocation happening within AWS but it's something to be aware of. One Zone-IA is just the same, but as the name suggests limited to just one availability zone that drops its uptime to 99.5%. To be clear, when your objects are stored in just one AZ you're subject to losing access in the event of a full AZ outage or losing your files, if the AZ is somehow destroyed. Still, depending on your requirements, it might be a risk you're willing to take to achieve lower costs. For this tier, you're looking at about only 1 cent per gigabyte per month. Glacier is Amazon's long-term data archiving solution. It's designed to store vast quantities of data
>
> **[3:08](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glacier-and-s3-lifecycle-rules?u=76281980&t=188)** as reliably and cheaply as possible. There's literally no limit to the amount of data you can store and a single archive file can be up to 40 terabytes in size. Of course, there's a trade off for this low cost reliability. As the Glacier name implies, to retrieve data you must effectively unfreeze it and depending on the tier you choose, the process is not necessarily immediate. After all the Glacier use cases for archival or long-term storage for things you need to keep for say, compliance purposes but don't need to access quickly or regularly. Glacier used to be a fully distinct service from S3 but eventually AWS simplified things quite a bit by making Glacier into a set of storage tiers of S3. Let's take a look at what those tiers are. First, there's Glacier Instant Retrieval. This is where the prices really start to drop at four tenths of a cent per gigabyte per month. However, you now have a minimum of 90 days that you must keep data stored before retrieving it. Still when you need the files out of here, you can get them instantly. Price drops even more with Glacier Flexible Retrieval. In this case, the flexible party would be you. If you don't mind waiting minutes to hours for your data to become available to re-download this tier is a great option. Now, the per object minimum size changes to 40K. Finally, the longest term cheapest archival storage on AWS, Glacier Deep Archive,
>
> **[4:43](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glacier-and-s3-lifecycle-rules?u=76281980&t=283)** retrieval takes hours and you must keep objects a minimum of 180 days but the price is less than one-tenth of a cent per gigabyte per month. That low cost storage is no doubt attractive, but of course often you won't want to archive data directly to Glacier. Rather, you'll be starting with hot data, data that is frequently accessed. What you'd like to do is migrate it over time as it becomes less frequently used to cheaper long-term storage. S3 Lifecycle rules allow you the control to do exactly that. S3 lifecycle rules can migrate data to cheaper storage tiers in S3. They're based on object age in days. They cannot be applied based on access frequency. If you want that, just use the intelligent tiering class and they will not be applied to objects less than 128K in size, which is minor bummer. There used to be more complex rules about which tiers could transition to which others but AWS now depicts this in their documentation with what they call a waterfall. Take a look here at their docs to see this image as well as more details. For now, let's head into the console to try out configuring some data lifecycle rules on our own.

> [!info]- Semantic Content
>
> **CLI Commands:** aws (5), az (3)
> **Env Vars:** aws (5)
> **Definitions:** is a  (2), stands for (1)
> **Versions:** 2.30 (1), 99.5 (1)
> **Warnings:** be aware (1)
> **Speakers:** - [instructor] (1)

#### [S3 data lifecycle configuration](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-data-lifecycle-configuration?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-data-lifecycle-configuration?u=76281980&t=0)** - [Instructor] Now, let's see how we can configure S3 to manage the data lifecycle of the objects in our bucket. Go ahead and click into your demo-bucket, and then to the Management tab. Here, you see Lifecycle rules, and we'll create a new rule by clicking this button, Create lifecycle rule. On this screen, we have a lot of flexibility as to what we can do, and as to how much logic we can put into a single rule. But we want things to be readable and easy to understand, so we're going to do these things in a few steps. The idea here is that we're going to tell our objects to go to Infrequent Access after 30 days, then go to the Glacier tier after a longer period of time, and finally, after a year, expire and delete completely. So to do that, like I said, we could put it all in one rule, but for readability's sake, I think for better maintainability sometimes it's nice to have them separated. So we're going to start with a rule called MoveToInfrequentAccess. Now, this next section, like we've seen in other parts of S3, asks whether we want to scope this rule down to any subset of objects. We don't want that, so we can leave this all at default, except in this case the default selection is to limit the scope. So we need to select this radio button here that says Apply to all objects in the bucket. S3 gives us a little bit of a warning, and we just need to say yes, we understand what's happening, we know what we're doing. We acknowledge, and move on. All right, here's where all the logic gets defined
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-data-lifecycle-configuration?u=76281980&t=95)** under Lifecycle rule actions. We're going to act on both current and non-current versions of objects in the bucket, because remember, we're dealing with a bucket that we set to be versioned. So it has both current versions and past versions of any edits that we've made over time. So this MoveToInfrequentAccess we're going to make very straightforward. We're just going to apply to every version of every object, so we'll check both of these, Move current versions of objects between storage classes, yes, and Move non-current versions of objects between storage classes, also yes. That results in two configuration boxes down below, which we will now edit. So we're going to move current versions of objects to Standard Infrequent Access after a certain number of days. And in fact, you can see as we're typing that 30 is the minimum. So we're just going to go with that, and that fits nicely. 30 days, you can see we can add more transitions. Yeah, move things to other tiers in this same rule after a different number of days. That's perfectly viable, but we're going to keep things straightforward, so just have the one rule. Non-current versions, we're going to make no distinction here. We're also going to move those to Infrequent Access after the same number of days, 30 days. You can retain some number of past versions here if you want. Just leave this blank, though, and we'll move on. You see a summary of what's going to happen as a result of this rule on the last part of the screen here. Just make sure that looks good, and we'll say Create rule. All right, so that's the first part, MoveToInfrequentAccess.
>
> **[3:12](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-data-lifecycle-configuration?u=76281980&t=192)** Let's click the button to create another rule, Create lifecycle rule. This is going to be MoveToGlacier. Same thing, apply to all objects in the bucket, acknowledge this is what we mean, and we're going to move current versions and move non-current versions, this time not to IA, but to Glacier Flexible Retrieval. This time, we'll move it after 100 days. There's a note here where S3 is warning you about the retrieval cost of objects in Glacier. We know about that, but make sure to read if you want more details, and click acknowledge. Same thing for the non-current versions. Go to Glacier Flexible Retrieval, after 100 days, leave this blank. No need to retain any objects. By the way, if you put a zero in here, you may see an error, so do just leave that blank. Acknowledge the warning down below, read the summary, and click Create rule. Okay, so that's step two of three. We'll create one more. And this is ExpireObjects. We'll apply to all objects in the bucket, acknowledge the warning, and this time we won't select these two first check boxes. Instead, we'll go to Expire current versions, and Permanently delete non-current versions. Again, this is not a very sophisticated rule. You may want to do different things in your bucket, depending on your use case.
>
> **[4:43](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-data-lifecycle-configuration?u=76281980&t=283)** But for current versions after a number of days of object creation, we're going to say 365 there. As for non-current versions of objects, same thing, 365. And we will not retain any past versions. So this will delete everything, current and non-current versions of objects after a year. We'll click Create rule. So with that, we've established a complete data lifecycle policy for the objects in this bucket. After 30 days, they're going to move to the cheaper Infrequent Access tier. After 100 days, they're going to move to Glacier, and then after an entire year, they're going to expire completely. So you can see how from the tools that we've just seen there are a lot of ways that you can develop a very sophisticated [[Data Management]] policy for your organization.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Data Management]] (1)
> **CLI Commands:** make (4)
> **Warnings:** warning (4)
> **UI Navigation:** go to (3)
> **Best Practices:** make sure to (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [Versioning in S3](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/versioning-in-s3?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/versioning-in-s3?u=76281980&t=0)** - [Narrator] If you've been following along with the course your demo bucket has versioning enabled on it. This means that the objects we place inside will retain a history of their versions as they're edited over time. Let's go ahead and click into the demo bucket and we can pick one of the files. How about this parks and landmarks from Detroit? We can open up the file and make a quick change to the data. Why don't we just go ahead and delete this column here? Now, that's a pretty big change, and if that were a mistake this is something that our S3 bucket versioning could save us from because we would have the old version. Let's go ahead and save that and head back to S3. We'll go back to the demo bucket and re-upload the file. That's how you create a new version of anything. You just upload it again, so we'll click upload then add files, and add that CSV and click upload.
>
> **[1:05](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/versioning-in-s3?u=76281980&t=65)** Hit close and we can click back into the file and click this versions tab right here. Now you can see that we have two versions maintained on the file. We have the current version and the past one which we can click and download separately. You can see here we've got the version ID designating that we are looking at a past version and not the current one. So in this way, turning on versioning for an S3 bucket allows you to see back in time and make sure that any accidental object deletion or modification is not a catastrophe because you can always get back to an earlier version of the file.

> [!info]- Semantic Content
>
> **CLI Commands:** make (2)
> **Env Vars:** csv (1)
> **Cross-References:** go back to (1)
> **Definitions:** means that (1)
> **Speakers:** - [narrator] (1)

#### [S3 teardown](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-teardown?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-teardown?u=76281980&t=0)** - [Instructor] Throughout this course, if you're following along, you're going to be building resources in AWS, and each one of these is going to have some cost associated with them. If you look at AWS's documentation, you can see that the free tier that exists can cover the cost of a lot of these resources. If you are a first time subscriber to AWS, there's a lot that you can do in the first 12 months that is free. However, you always want to keep track of the things that you build and delete them whenever you don't need them anymore to make sure you don't encounter any unexpected costs at the end of the month. So that's what we're going to do at the end of every chapter. So in order to take care of what we've built in S3, we need to do a couple of things. First, let's work backwards by starting with the Simple Notification Service. We're going to delete the topic that we use to email. So I'll go into Amazon SNS, which I got from my Frequently Accessed Services, or by typing SNS up here in the menu. I'll go to topics, highlight the checkbox next to S3 Demo Notifications, and click Delete. I'll have to confirm by typing Delete Me in the text box, which is something that happens a lot throughout AWS's graphical interfaces. They'll have you type the [[Microsoft Word|word]] delete or copy and paste, or type out the name of a resource in particular just to make sure that you are doing what you intend to do. So, okay, let's head back to the dashboard by clicking the logo button here,
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/s3-teardown?u=76281980&t=94)** and we can go into the S3 service, either by going to recently visited or by typing. We've got two buckets here from the course. Now, the first thing I want to mention here is that the demo bucket is something I'm going to keep around for using later in the course, and you might want to do the same. So I'm just going to delete the secondary bucket here from the Ireland region. But if you'd like to delete both, it's going to be the exact same procedure. So just repeat that for the primary bucket if you so choose. I'll click the check box here. Now, there are really two ways of doing this. I can click Delete and it will prompt me to empty the bucket first or I can empty the bucket first and then Delete. Let's go ahead and just click Delete and I'll show you what it says. You get a warning here that buckets have to be empty before they can be deleted, and they give you a link to this thing they call the empty bucket configuration. So here, to empty the bucket, I have to confirm by typing permanently delete, you can type it or you can cut and paste it, click empty, and then once this goes green, you can exit. The bucket is empty so you can highlight it and click Delete. Here, I'm going to have to copy and paste the name of the bucket to confirm, and I can click Delete Bucket. All right, from here, like I said, I'm going to keep the primary bucket around for future purposes, but feel free to repeat that process if you want to clean everything up. All right, we'll see you in the next chapter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **CLI Commands:** aws (4), make (2)
> **Env Vars:** aws (4), sns (2)
> **UI Navigation:** in the menu (1), go to (1), checkbox (1)
> **Cross-References:** later in (1), in the next (1)
> **Warnings:** warning (1)
> **Speakers:** - [instructor] (1)


### 3. File Systems

[↑ Back to Table of Contents](#table-of-contents)

#### [Elastic Block Storage](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/elastic-block-storage?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/elastic-block-storage?u=76281980&t=0)** - [Instructor] When you create an [[Amazon EC2|EC2]] instance, you're asking AWS to start a virtual server for you. Although it's sometimes easy to forget, this takes place on actual [[Hardware]], somewhere deep in Amazon's data center facilities for that availability zone. Of course, a virtual machine is simply a way of giving you a virtualized slice of what's on the underlying hardware. A bit of the CPU, some of the RAM, et cetera. So in the beginning, if you needed hard drive space you'd use what's called instant storage, a slice of the hardware's disk drive. Instant storage uses the disk attached to the instance's hardware. It has lowest possible latency, survives reboots, and is suitable for ephemeral storage. It might be disk-based or SSD-based. Now, having said that it does not persist when the instance is stopped, although reboots are fine. It cannot be encrypted and it's not part of a snapshot taken for an AMI. It cannot be detached or backed up on its own and it has a fixed size tied to the instance size. In short, instance storage is good for temporary uses like caching but its overall usefulness is limited. Enter AWS's Elastic Block Store, or EBS. EBS is network-attached storage, meaning that it has a lifecycle separate from the instance to which it's attached. If the instance is stopped or even fully terminated, the EBS volume lives on. This feature alone allows us to unlock the true elasticity
>
> **[1:32](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/elastic-block-storage?u=76281980&t=92)** of AWS Elastic Compute Cloud, or EC2, instances. It means that we can shut down or sleep instances when we don't need them, such as at night or after business hours. Since on-demand EC2 instances are charged hourly, this saves us money, and because EBS stays persistent, we can pick right back up where we left off the next day. Since EBS is network storage, it can then be detached from one instance and attached, even at runtime, to another instance, and the data persists. You can also attach multiple EBS volumes to a single instance, and unlike instance storage EBS can be grown on demand, giving you extra capacity when you need it. Just be aware that you will need to make the operating system aware of any capacity changes. EBS volumes can also be created as encrypted volumes. The encryption is seamless, invisible to you as the user of the attached EC2 instance, but impenetrable to anyone who might gain access to the volume without permission. What about reliability? Hard drives have a notorious failure rate, and with a vast number of hard drives AWS has deployed at any given time, failure is a certainty. The good news is that EBS is very reliable. Within its availability zone, an EBS volume is automatically replicated. This replication gives EBS five nines of availability, meaning you can expect about five minutes of downtime in a year. But we know that availability is not the same as durability, and disks are volatile. AWS advertises that EBS has an annual failure rate
>
> **[3:07](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/elastic-block-storage?u=76281980&t=187)** of between 0.1 and 0.2%, making EBS 20 times more reliable than the typical off-the-shelf disk which carries a failure rate about 4%. Of course, AWS gives you the tools to make sure that even those kinds of failures are not catastrophic. You can snapshot an EBS volume at any time. In fact, you can keep as many snaps as you like, creating them on a regular schedule, whatever satisfies your backup needs. Just be aware that your monthly bill will increase with the number of snapshots you keep in your account. Now, let's take a look at what it takes to create an EBS volume.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (4), [[Hardware]] (4)
> **Env Vars:** ebs (15), aws (6), ec2 (4), cpu (1), ram (1)
> **CLI Commands:** aws (6), make (2)
> **Versions:** 0.1 (1), 0.2 (1)
> **Definitions:** means that (1), is a  (1)
> **Warnings:** be aware (2)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Create an instance with multiple EBS volumes](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-instance-with-multiple-ebs-volumes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-instance-with-multiple-ebs-volumes?u=76281980&t=0)** - [Instructor] In this video, we're going to look at how to create and configure multiple EBS volumes for an Amazon [[Linux]] EC two instance. For that, we'll need to head into the EC two section of AWS, which I've recently visited, so I've got the link right here. Click in, and let's go to the big orange button, launch instance. Click that, and we'll be at the EC two creation wizard. First, we need to give this host a name. We'll call it EBS demo one, one, because we're going to have a second one of these in a few minutes. For Amazon machine image, we'll take the default of Amazon Linux two, and under instance type, we'll also keep the default of T two Micro. Here under key pair, we either need to select or generate an SSH key that we use to log into the machine, so click create new key pair. This SSH key will be used throughout the duration of the course, so give it a name you'll remember. I'll call mine storage demo key. Keep everything else at default, and click create key pair. That will download to my machine, and we'll use it in a moment. Under network settings, you can see we're creating a new security group. It allows SSH traffic from anywhere in the world, and we're getting a warning that that is not particularly secure. I agree with that. In production, you would never want to create a rule that allowed SSH from anywhere in the public, however, for demo purposes, it'll work just fine. All right, configure storage,
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-instance-with-multiple-ebs-volumes?u=76281980&t=95)** because this is a storage course, we want to click the advanced button and see more details. There's one volume on this machine right now. Clicking the triangle will expand it. We can see that storage type is EBS, and it has a device name, dev slash XVDA. You don't need to memorize that, but take a good look, because we'll be seeing this in a moment. We have a snapshot ID, as well as a size in gigabytes of eight gigs. We have a volume type of GP two, and if you click the dropdown, you'll see several options here. GP two and GP three are both the general purpose SSD that is the standard for most EBS use cases, but if you want to learn more, you can click on info and see a sidebar pop up that gives more details. And even more than that, you can click on EBS volume type here at the bottom, and see a lot of information about the different cases for which each of these volume types is suited. GP two and GP three are pretty similar. GP three is newer and is cheaper, and allows you, if you select it, to specify your own IOPS and throughput. GP two scales its performance with volume size. We'll keep it at GP two, the default for now and keep going. Delete on termination is set to yes, which means this volume will go away if the instance that we're creating now is terminated. Encryption is set to no, and we'll keep it that way. Now let's create our second volume.
>
> **[3:09](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-instance-with-multiple-ebs-volumes?u=76281980&t=189)** Click the button that says add new volume. We'll make some different choices on this one. The device name of dev SDB is filled out for us already. Snapshot, we're going to leave blank. Up here, we have a snapshot ID that refers back to the Amazon Linux image that's going to form the basis for this root volume. Down here, with no snapshot selected, it means we're going to get a blank volume with no data on it. The size in gigabytes of eight, we will change to six, just so that later when we're looking at these at the block level, we'll see the difference very easily. The volume type can be GP three, can be GP two. It doesn't really matter, but I'll make them match. Now for delete on termination, I'm going to select no, because that allows us to use the feature of EBS that says that even if this instance terminates, we'll still have the EBS around, and can attach it to a different instance. For encrypted, go ahead and say yes, and you'll be prompted over here to select a KMS key. We've not created one separately, but that's fine, because like with S three, server-side encryption can be done by means of a key that's maintained by the AWS service. It's called AWS/EBS, and if you click it, everything will be fine, and this volume can be encrypted. You won't need to know the key or anything about it. It'll just be a seamless encryption whenever things are read from or written to the disk. Now that puts us at the end of the wizard, and we can choose launch instance.
>
> **[4:44](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-instance-with-multiple-ebs-volumes?u=76281980&t=284)** It'll take a few moments for this instance to spin up. To watch it, click the ID link here. This takes us to our list of EC two instances, and in a moment, you'll see the instance state will change to running. At that point, we can log in. All right, this instance is created. If we click the check box on it, we can see some details down below. Let's copy the public IP address right here, and head on over to the terminal. Before we can use our SSH key to log into the machine, we'll need to apply some different permission bits that it downloaded with. Type CH mod, then four zero zero, and give the path to the key. For me, it's in my downloads folder, and now that that's done, I can use it to SSH to the instance. If you want to know more about how that works, check out one of my other courses, AWS Deploying and Provisioning. All right, we're going to type SSH, then give the public IP dash I flag, and the path to that key. That's going to take the place of a password, and before we execute this, we need to give a username right here before the IP. That user will be EC two dash instance at. There we go, that'll log us in. Now just like anytime we go to a host for the first time, SSH is asking us to accept its fingerprint. We'll say yes and hit enter, and here we are.
>
> **[6:20](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-instance-with-multiple-ebs-volumes?u=76281980&t=380)** We are logged into the machine, and we can check out the storage situation. We're going to use the disk free command, DF dash H, and we'll see a few things. All right, several file systems here, the temp ones are memory-based file systems, so we don't have to worry about those. And we can see one here that is larger than all the others, eight gigabytes in size, XVDA one. Okay, so you should recognize that is similar to XVDA, which was our root block, but it's not exactly the same, so what's going on here? Let's find out by typing LSBLK to list our block storage. Okay, now we can see that XVDA one is a partition of XVDA, that root device, so that's where eight gigabyte drive comes from, and you can see it's mount point is slash, so this is all the storage on the device, XVDA one. There's something else here that says XVDB, and what is that? That is not the name that we expected. We expected to see dev SDB. Well, there is something on this machine called dev slash SDB. Let's take a look at it, and we can see that it's actually a symlink. So when we see XVDB here, that is actually our second volume, this six gigabyte volume and we can recognize that here as well under the LS block command by the six gigabytes,
>
> **[7:54](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-instance-with-multiple-ebs-volumes?u=76281980&t=474)** so we know that this represents our second volume. AWS documentation states that EC two will take those device names and symlink them to other things. So you can see also with dev slash XVDB, that this is also a symlink. It just goes to its own name, so there's no redirection there. All right, so now that we know what's what, how do we actually take advantage of this six gigabyte drive? Well, we're going to need to mount it, and before we can mount it, we're going to need to format it. Let's check out if this drive is formatted or not by using sudo, and then the file command, file dash S. I'm going to point to dev slash, and we'll use this name, XVDB. If we see the [[Microsoft Word|word]] data here, that means this is an unformatted drive. We need to format it using something called MKFS. We use sudo again, then MKFS dash T, and the format type of EXT four, and that same path, dev slash XVDB.
>
> **[9:05](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-instance-with-multiple-ebs-volumes?u=76281980&t=545)** Okay, now that that command has finished its work, we can run the file again, and see much different output. Instead of just seeing the word data, we see details on how the file system is formatted. Okay, great. So let's do DF dash H again to see if anything has changed, and it really hasn't. We can see just the one root path here. Now we need to create a mount point. So a mount point needs a folder, and let's create one. We use sudo again and make a directory with MKDIR, and we'll call it slash EBS dash encrypted, because this is our encrypted volume. So it's just an empty folder, but we're going to use the mount command now to connect it up to that disk that we just formatted. So we'll say sudo mount slash dev slash XVDB slash,
>
> **[10:01](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-instance-with-multiple-ebs-volumes?u=76281980&t=601)** and now the folder name. So we have sudo, mount, the name of the disk, the name of the mount point. Okay, that seems to have done something, so let's go CD into EBS encrypted and look around. Okay, it does look as though there's a file system going on here. With a file system, we should be able to create a file. So let's try to VI hello dot text or something like that. We'll say hello and save the file, and uh-oh, can't open the file for writing, so let's exit out of here, and figure out how to resolve this. So you can see that this volume, this part of the file system is owned by root. We need to change that. We're not logged in as root. We're logged in as EC two user, so if we want to be able to use this file system, we need some ownership over it. We use the CH own command, but we've got to use sudo, because that is a privileged command. CHOWN, I'll say EC two user, colon EC two user to make both owner and group the same thing, dot, which is our current directory, and we'll give the dash R command just for fun to be recursive, even though we don't need it right now. Now if we do LS again, you can see that the ownership has changed, and I should now be able to use VI to open up a hello dot text, put a message in there, write and quit, and save it successfully.
>
> **[11:37](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-instance-with-multiple-ebs-volumes?u=76281980&t=697)** I can open up the contents, I can do LS again, and now that you can see we have an encrypted EBS volume mounted on this EC two instance, usable and writeable.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (3), [[Microsoft Word|Word]] (2)
> **Env Vars:** ebs (11), ssh (8), xvda (6), xvdb (6), aws (5)
> **CLI Commands:** ssh (8), sudo (6), aws (5), make (4), ls (3)
> **UI Navigation:** dropdown (1), click on (1), go to (1), open the (1)
> **Definitions:** is a  (3), is an  (1)
> **Analogies:** just like (1), similar to (1)
> **Prerequisites:** configure (2)
> **Tools:** terminal (1)

#### [Modify the type and size of an EBS volume](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/modify-the-type-and-size-of-an-ebs-volume?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/modify-the-type-and-size-of-an-ebs-volume?u=76281980&t=0)** - [Instructor] In this video, we'll expand the size of the EBS volume we just created and then update the operating system to make that space usable. To do this, we'll need to head back to the AWS Console, specifically into [[Amazon EC2|EC2]] and into our list of running instances, which I can get from clicking here. Click the checkbox next to EBSDemo1. You see the details below and can click the Storage tab to find out more about the storage volumes. You see both of our device names here. And recall that SDB is our secondary EBS at size six. This is the one that we want to resize. To do that, click on its Volume ID here and we'll be taken to another part of the EC2 Console. The Volume section, as you can see highlighted here. If we check the checkbox next to this volume and go over to Actions, we can modify volume. This will allow us to change the volume type if we wish, or in the case of this demo, to change the size from six to 10. Click Modify. We're being told here that this may take a few minutes and that we must extend the file system on our operating system to get this to work. So we say Modify. If you scroll over to the side, you can see that the state of this volume is modifying. Once that's complete, we can head back to the Console.
>
> **[1:37](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/modify-the-type-and-size-of-an-ebs-volume?u=76281980&t=97)** Okay, after a few minutes, our volume is done modifying. You can see the size is up to 10 gigabytes and although it is still in this optimizing state, we can proceed with the demo. Head back to the Console and log into the instance. If you type df -h, you'll see xvdb, which represents the secondary drive right here. And for some reason, it is still at about six gigabytes. What is going on here? Well, as Amazon just told us, we're going to need to modify the operating system to make this work. Let's take a look first at lsblk. Lsblk. Okay, there we see the 10 gigs. They are here. They're just not visible to the operating system. So the command we need is sudo resize2fs for file system and we'll give the name dev/xvdb. Give it a second to run. Now let's run df -h again and we'll see the larger size is now available to us.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (2)
> **Env Vars:** ebs (2), ec2 (2), aws (1), sdb (1)
> **CLI Commands:** make (2), aws (1), find (1), sudo (1)
> **UI Navigation:** checkbox (2), click on (1)
> **Tools:** aws console (1)
> **Speakers:** - [instructor] (1)

#### [Detach and reattach an EBS volume](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/detach-and-reattach-an-ebs-volume?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/detach-and-reattach-an-ebs-volume?u=76281980&t=0)** - [Instructor] In this video I'm going to demonstrate how we can detach an EBS volume from this instance and attach it to another. In order to show this functionality I'm going to need a second [[Amazon EC2|EC2]] instance. Now before we do that, please take note of the availability zone that EBS demo one is in. We want to make our second instance in the exact same AZ, because volumes are unique to an availability zone, we can only detach and reattach within this AZ. For me, it's US East 1A, but it may be different for you. Click launch instances. Now we'll call this EBSDemo2. We'll keep the same AMI, which is Amazon [[Linux]] 2 and the same instance type. For the key pair, instead of creating a new one, we'll use the dropdown and select the one we've already got storage-demo-key. Under network settings, instead of saying that we have no preference for the subnet, we'll click edit and choose the one that's in the availability zone that matches EBSDdemo1. For me, that's right here US East 1A. We'll keep everything else at the default. Allow it to create the ssh from the World Security Group and keep storage at just the one root volume. Click launch instance. Now while this instance is being created, we can click into the ID of the instance and head back to the EC2 console.
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/detach-and-reattach-an-ebs-volume?u=76281980&t=95)** And while this is being created, we can take a step that we need to take on the first instance before we can proceed. So head back to the console. I'm here logged into EBSDemo1. You remember if you use the disc free-h command, that we've got xvdb mounted as ebs encrypted. In order to proceed we're going to have to unmount this block device. So say sudo umount, not to be confused with unmount, that's umount -d, and then the name of that device /dev/xvdb. All right, now we have unmounted the device which we can confirm and then exit. Okay, back on the EC2 console, EBSDemo2 is now up and running. That's great, but before we ssh to it we need to detach the volume from EBSDemo1. Let's clear filters here so we can see that other instance. Here's EBSDemo1, and if we click the checkbox next to it, we can go to the storage tab, scroll down and see that 10 gigabyte volume that we modified earlier. Click the volume ID, and once again we'll go to the volume section. We're looking at just that one volume which we now want to detach from EBSDemo1. Check the checkbox, go to actions and then choose detach volume. Confirm by clicking detach. If you scroll to the side, you'll see that this stays in use
>
> **[3:13](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/detach-and-reattach-an-ebs-volume?u=76281980&t=193)** for a few moments and should become available again. There we go. Now the volume is available to be reattached. Check the checkbox again, go to actions and then attach volume. Now here under instance we can hit the dropdown and attach to EBSDemo2. The new device name you can edit, but we'll take the default, it's called dev/sdf. Click attach volume. If you look at the status now you can see that, that volume is in the attaching state. Let's head back to the console. Now we need to connect to our EBSDemo2, so we're actually going to need to [[Fetch]] that IP address from the EC2 console. Let's head over to instances un-click EBSDemo1, click on EBSDemo2, and copy the public IP address. Now back to the console. Ssh EC2-user@IP address-I, and then refer to your key.
>
> **[4:28](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/detach-and-reattach-an-ebs-volume?u=76281980&t=268)** Say yes to the fingerprint. And now here we are on EBSDemo2. The DF command shows us just the mounted route drive, but if we do lsblk, we'll see that we have xvdf, which is no doubt a sim link for dev/sdf which we saw referred to in the AWS console. And there it is. So we need to do just like we did before. First I want to check by running sudo file -s on dev/sdf.
>
> **[5:07](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/detach-and-reattach-an-ebs-volume?u=76281980&t=307)** Is this volume formatted? It would rather us use not the SIM link, but xvdf so let's try that. There we go. We did not get the [[Microsoft Word|word]] data back. We get all this detail, which shows us that it is already formatted, which is exactly what we'd expect because this is the same EBS volume that we just used on the other instance. All we need to do is create a mount point. So let's do that. We'll create an empty folder. We use sudo, make directory and we'll call this one, /ebs-reattached. That's an empty folder which we can mount using sudo mount /dev/xvdf, and then the name of the mount point. Hit enter and we should be good. Let's cd into ebs-reattached. Okay, I can see the hello.txt file that we had before and open it up. And with that, we have detached an EBS volume from one instance and restored it by attaching it to a completely different instance carrying on with the same data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (5), [[Linux]] (1), [[Fetch]] (1), [[Microsoft Word|Word]] (1)
> **CLI Commands:** sudo (4), ssh (3), make (2), az (2), aws (1)
> **Env Vars:** ec2 (5), ebs (4), ami (1), aws (1), sim (1)
> **UI Navigation:** go to (4), checkbox (3), dropdown (2), select the (1), scroll down (1)
> **File Paths:** hello.txt (1)
> **Tools:** aws console (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [EBS backup, restore, and schedule with CloudWatch](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/ebs-backup-restore-and-schedule-with-cloudwatch?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/ebs-backup-restore-and-schedule-with-cloudwatch?u=76281980&t=0)** - [Instructor] As I mentioned, it's possible to take snapshots of your EBS volumes to ensure that you have backups of your critical data. Let's go into the [[Amazon EC2|EC2]] section and see how this works. All right, here we are looking at EBSDemo2. It's already highlighted. If you go down below and click the Storage tab, we can see details of the two volumes attached to it. We have the 10 gigabyte secondary volume and the eight gigabyte root volume. Let's click the ID of the first one, the root volume. We're taken to the Volume section over here on the side, and we can select and highlight that volume. Then go to Actions and choose Create snapshot. This is how we create a one-off manual snapshot of an EBS. We'll call it EBSDemo2-root-volume-snapshot, something like that, and click Create snapshot. From here, we're taken back to Volumes but you can see the snapshot ID up in the header and click it, and we can see that it's already making progress on taking this snapshot. If you want to refresh the page, you've got the button right here. Okay. Now the snapshot is complete, and it's in the Available state. So what can we do with this? If I go to Actions and click the dropdown, I can see that I can create a brand new EBS volume from the snapshot, or I can create a machine image. Remember, when we created the EC2 instances to begin with? We chose the Amazon Machine Image or AMI, for Amazon [[Linux]]. This is how you create your own AMI's.
>
> **[1:37](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/ebs-backup-restore-and-schedule-with-cloudwatch?u=76281980&t=97)** If I had modified this root volume in some way, say by, especially installing certain packages or configuring it in a way that's important to my business, I can create a new AMI from this snapshot to use as the basis for future builds of EC2 instances. Let's see how that works. Click Create image from snapshot. From this screen, we need to give it a name, and we'll just call it EBSDemoAMI. We'll keep everything else at the default and click Create Image. Now you can see up at the top a confirmation that the AMI is on its way. We'll click that ID of the AMI to go to that section of the console. Now, if you check this box right here, you can see that the AMI is in the Available status, and we have the option to launch an instance from this AMI. So that means just like that, we've got a new AMI that we can use to create instances. Very nice. So not only can we launch instances from this AMI, we can go back to that snapshot and use it to create a new EBS volume just to restore the storage for an instance if we had a problem. So there are a few things that we can do with those snapshots. Now, even better than creating a snapshot manually is to have AWS take them automatically for us. For more of that, please see the final chapter of this video course where we will explore the AWS backup service.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (3), [[Linux]] (1)
> **Env Vars:** ami (9), ebs (4), ec2 (3), aws (2)
> **UI Navigation:** go to (3), dropdown (1)
> **CLI Commands:** aws (2)
> **Cross-References:** go back to (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Elastic File System](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/elastic-file-system?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/elastic-file-system?u=76281980&t=0)** - [Instructor] In order to address the limitations of EBS and provide users with truly flexible storage, AWS introduced an elastic file system. EBS' first claim to fame is that it grows and shrinks in size. Not manually, on demand like EBS, but automatically in response to your usage. You don't even have to update the operating system because EFS always presents, what looks like, many petabytes of storage. EFS is not block storage like EBS. Rather, it's a file system that can be mounted by NFS 4.0 or 4.1 clients. Unfortunately, that does mean that [[Windows]] cannot natively mount EFS. However, you can enable an NFS service on certain versions of Windows that provides an NFS client or use a third party client. Alternatively, you can stand up a native CIFS file system and use another AWS service, FSX for Windows, which we'll explore later. As a network file system, EFS can be mounted by many clients at once, making it well suited for tasks that require shared disk space, such as horizontally scalable web apps or parallelizable [[Data Processing]] jobs. This makes EFS a potential NAS replacement. And unlike EBS, elastic file systems can be mounted from outside AWS. If you're using AWS Direct Connect, you can have an on-premises instance, or anything that can use NFS, mount EFS. This makes EFS a perfectly viable way to get data out of your data center and into the AWS cloud. Before an instance can connect to an EFS file system,
>
> **[1:37](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/elastic-file-system?u=76281980&t=97)** it must have at least one mount target. A mount target is an AWS resource that provides the NFS connection between the client and the scalable storage in EFS. It's like the front door of EFS. You'll make one when you create an EFS instance for the first time. The mount target has a DNS A record that resolves to the IP of the backend file system. This is the domain name that you'll use on the client, say an [[Amazon EC2|EC2]] instance, when you connect to EFS. As is the case with other AWS resources, such as application load bouncers, AWS discourages users from connecting using the IP address directly, as it may change without warning. On the other hand, the domain name of the mount target stays consistent and reliable. You may have at most one mount target within an availability zone. If you have multiple subnets, as is the case in this diagram, you'll choose one to house the mount target and any instance within any subnet of the AZ will be able to resolve it and use it. Now, we have established two connections to the same EFS. These two instances are sharing the backend storage. If the first instance writes, the second one can read and vice versa. One of the most useful features of the mount target is that security groups can be attached directly to it. [[AWS Security]] groups are like portable firewalls that can be attached to different types of AWS resources. With security groups on your mount points, you have maximum control over what instances in your account can mount this file system. Finally, if you want instances in another availability zone
>
> **[3:12](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/elastic-file-system?u=76281980&t=192)** to connect to EFS, you'll need another mount target in that AZ. Here, with two mount targets, we have four instances sharing the EFS volume. Imagine these are all workers in a distributed data processing job or that they're all members of an auto-scaling group that serves a web application. These hosts can all share data seamlessly, with fairly low latency. Speaking of which, users have some control over the performance of EFS. To configure IOPS, you can initialize a file system in one of two modes, general purpose or max IOPS. EFS documentation states that file systems in the max IO mode can scale to higher levels of aggregate throughput and operations per second, with a trade-off of a slightly higher latency for file operations. As for throughput, by default, EFS throughput scales with the amount of data stored on the file system and employs a bursting model. In general, AWS increases your EFS IOPS capacity along with the size of your instance. This can lead to some undesirable situations. If your volume is quite high but your overall dataset size is low, you may find that you're throttled at times using EFS. If you know that you're going to need higher sustained throughput than what bursting provides, you can also use provisioned throughput to pay for a certain baseline of throughput performance. It's all a trade off between cost and functionality. To get the full picture of your options, be sure to consult Amazon's document page, Amazon EFS performance. Even with those special configurations, AWS has made significant improvements
>
> **[4:45](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/elastic-file-system?u=76281980&t=285)** to EFS since its launch. First, they've seamlessly deployed numerous performance enhancements, including one that tripled throughput for reads and another that increased IOPS by nearly five X. Not only that, they've introduced S3-style storage classes to give you even more control over the cost and behavior of your file system. As shown here, there are multiple options for how to deploy. For instance, EFS One Zone trades a bit of durability for significant performance and cost benefit. Even in this mode, EFS data is still replicated in the background to give you better durability than running your own network storage. It's just limited to one availability zone. There are also infrequent access tiers and intelligent tiering, just like with S3. Meanwhile, EFS takes backups that can be replicated out to other AZs, giving you plenty of options to tune your desired price and performance. Now, let's get into the console and see how this works.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (3), [[Data Processing]] (2), [[Amazon EC2|Ec2]] (1), [[AWS Security]] (1)
> **Env Vars:** efs (25), aws (12), ebs (5), nfs (5), iops (4)
> **CLI Commands:** aws (12), az (2), make (1), find (1)
> **Analogies:** such as (2), it's like (1), imagine (1), picture (1), for instance (1)
> **Versions:** 4.0 (1), 4.1 (1)
> **Prerequisites:** you'll need (1), configure (1)
> **Definitions:** is an  (1)
> **Warnings:** warning (1)

#### [EFS demo create EFS](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/efs-demo-create-efs?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/efs-demo-create-efs?u=76281980&t=0)** - [Instructor] In this video, we'll keep using the two [[Amazon EC2|EC2]] instances from the previous EBS lessons to mount an Elastic File System. We'll start by creating a security group that will allow both of these instances to talk to EFS. There are multiple ways to approach using security groups for purposes like this, and you can learn more about them in my other course, "AWS Deployment, Provisioning, and Automation." For this demo, we'll go with something simple, and create a group that allows NFS file system traffic from any host in the entire VPC, in which these instances reside. So for that, we'll need to go find the VPC in this IP range. Let's head into the EC2 section of the console. Click on Instances Running to see a list and there are two demo instances. Click the checkbox next to one to see its details and go to the networking section. Here you see the ID of the VPC where this server lives. Click it and we'll go to the VPC section of AWS. Okay, here we see our VPC and right here on the line, it's CIDR block. A CIDR block represents a range IPs and this one, 172.31.0.0/16 represents all the IP addresses within this VPC. Both of the EC2 instances we've created then are within this range. So if we create a security group that allows NFS traffic from this range, they'll both work.
>
> **[1:33](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/efs-demo-create-efs?u=76281980&t=93)** So I'm going to go ahead and copy this. Close the tab that opened and that'll put us back in the EC2 section. Over here on the side, I'm going to scroll down to security groups. Here we'll click create security group. We're going to call this something descriptive, which will be NFSFromDefaultVPC, and we need to give it a description. So we'll say allow NFS traffic from anywhere within the VPC.
>
> **[2:09](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/efs-demo-create-efs?u=76281980&t=129)** Security groups actually belong to a VPC so that's what this line here indicates. As for inbound rules, let's click add rule, and we'll add our NFS rule. In the dropdown, you can put in any port that you like but there are some presets here, including NFS. Go ahead and click that and it will get preset with the port 2049. As to the source, we can say custom, anywhere or my IP, we'll go with custom. And here we can paste in that CIDR block we just copied. We can leave the description blank since it's pretty self-evident. And there's the description up here. Then can scroll on down. Outbound rules can stay at their default and we'll click create security group. Now, if we apply this security group to our EFS mount points, any instance that's in the VPC should be able to talk to it on port 2049. All right, with that, let's go type EFS in the search bar and head to the Elastic File System part of the console. Click Create file system. There's an extremely simple interface you can click through to create an EFS for the first time, however, we won't get into the details. We'll call this one EFSDemo. It's in our default VPC and standard storage class works, but let's hit Customize. Okay, let's see what kind of options we have.
>
> **[3:45](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/efs-demo-create-efs?u=76281980&t=225)** Storage class is either standard or one zone, which would, of course, put us in just one AZ. Automatic backups are enabled. This uses the AWS Backup service, which we'll talk about later in the course. There's lifecycle management set up by default. As you recall, there are storage tiers in EFS similar to S3, and this rule says that after 30 days, an individual file will move out to infrequent access. That means that you may encounter some additional first byte latency hitting that file but you'll have a much better balance of price and performance for this file system. And you can change this however you like but we'll keep it at default. Performance mode is General Purpose or Max I/O, which will have you pay a little more for increased throughput. Throughput mode can also be bursting based on demand or set up in advance by the use of provisioned throughput. Encryption we'll keep at its default. No tags needed for this, and we'll click next. Now, this is interesting. Mounting targets are the endpoints that our EC2 instances will actually talk to when they want to mount this EFS. So you can see that we've got one in basically every AZ that's part of this VPC. That's nice and that's what we want but we do want to change their security groups. They've all got this default security group and if you go and look that up, you'll find that it is the default VPC security group, which is actually pretty wide open.
>
> **[5:18](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/efs-demo-create-efs?u=76281980&t=318)** What it does is it allows all traffic within the VPC from any resource that also bears this security group. That is an interesting pattern. I like the pattern of creating security groups that trust themselves. That way you can sort of batch different resources with the security group that will allow it to get to things. That's very nice. But in this case, because it allows all ports, it's a little more expansive than I would like. I'm going to go ahead and just X these all out and instead, we'll use the dropdown to choose the NFSFromDefaultVPC security Group. You can see that appears in the background so you kind of have to click off of it to see the effect sometimes. Choose that for all of these mount targets. And now we'll click next. On this screen, you can actually set up some file system policies that will apply to the entire EFS. If you click through these check boxes, you can see a little bit of how these work. You can prevent root access with this one, you can enforce read-only access with this one. These are all editable, so you can go in there and make tweaks or you can write your own. That's a little bit outside of the scope of what we want to do in this demo though. So I'm going to uncheck this and just click next. But beware that you can always in AWS click the learn more buttons that appear frequently to find out details. Click next. The next screen is a summary. If everything looks good to you, go ahead and click create. Our EFS file system is now in creating mode
>
> **[6:55](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/efs-demo-create-efs?u=76281980&t=415)** and in a few moments, it will become available as it has already, very fast. In the next video, we'll see how we can connect to it from our EC2 instances.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (6)
> **Env Vars:** vpc (13), efs (8), ec2 (6), nfs (5), aws (4)
> **CLI Commands:** aws (4), find (3), az (2), make (1)
> **UI Navigation:** go to (2), dropdown (2), click on (1), checkbox (1), scroll down (1)
> **Definitions:** means that (1), is an  (1), is a  (1)
> **Prerequisites:** set up (3)
> **Ports:** port 2049 (2)
> **Cross-References:** later in (1), in the next (1)

#### [EFS demo mount EFS](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/efs-demo-mount-efs?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/efs-demo-mount-efs?u=76281980&t=0)** - [Instructor] Now that we've got our EFS demo created let's see what it can do by mounting to it from two different [[Amazon EC2|EC2]] instances. In order to demonstrate this, I've kept around the two machines from the EBS part of this chapter, EBS demo 1, here with the white background and EBS demo 2. You can see that I'm logged into two distinct machines by the differing IP addresses in the prompt. Now, if we head back to the AWS console and click into EFS demo, you can see details of the file system and the orange attach button. Let's click that. Our default is mount via DNS, which is what we want to do, but we have two choices underneath. We can use the EFS Mount Helper or we can use the NFS client. The first command is much shorter and easier to work with, so I'll want to copy that and use it over here in the terminal. However, before I'm able to use it, there are a few steps. First, we have to make sure that the helper package, Amazon EFS Utils is installed. Type sudo yum install dash y to answer yes automatically to all prompts, Amazon EFS Utils. This will install the special software built and maintained by Amazon that makes mounting EFS easier. And we'll need a mount point. So let's create one using sudo make directory slash EFS. Now with those two steps done
>
> **[1:31](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/efs-demo-mount-efs?u=76281980&t=91)** we should be able to paste our command and with a slight modification, make it work. We want to slash here so that it's an absolute path instead of a relative path. At this point, we should have our mount complete. If you do disk free with DF dash h, you can see that we do have slash EFS mounted and it's mounted to something that's on the network. If you look at the size available, that's eight exabytes. And in fact, if you say DF with no extra arguments on it, you'll see that expanded. Lots of bites available. Of course, this is because EFS will expand as you need it. It basically has infinite space. All right, let's go over to our second terminal and do the same. We'll say sudo yum install dash y Amazon EFS Utils, and we will create a folder for mounting. Paste the same command with a slash modification there. And now we've mounted it here too, so I'll go ahead and CD into it, slash EFS and look around. Okay, nothing yet, but like with the fresh EBS volume we can see that everything is owned by Root Root. So the first thing we want to do is change that to something we can work with. Sudo chown EC2 user colon EC2 user
>
> **[3:05](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/efs-demo-mount-efs?u=76281980&t=185)** dot to represent the current directory. And for good measure a dash R for recursive. Now if wen LS again, we can see that EC2 user is the owner. Let's head back to the first terminal and also CD into slash EFS. When we LS here, we can see that EC2 user is already the owner of this Mount point. That's because they are the same network file system. This one is pointed to the same one as this one. Now, back here, if I create a file, I can save it, see that it has saved, go to the other terminal and also see it here. So with that, you can see how EFS provides a network file system with essentially unlimited storage capacity.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (5)
> **Env Vars:** efs (13), ec2 (5), ebs (4), aws (1), dns (1)
> **CLI Commands:** sudo (4), make (3), yum (2), cd (2), ls (2)
> **Tools:** terminal (4), aws console (1)
> **Prerequisites:** install (3)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)

#### [EBS and EFS teardown](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/ebs-and-efs-teardown?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/ebs-and-efs-teardown?u=76281980&t=0)** - [Instructor] As always, it's important to clean up the resources that we created so we don't incur any unexpected charges from AWS. Let's start with EFS. I'll click into it under my recently visited services. Here's our EFS demo. I'll click the check mark next to it and go up to delete. As you often have to do in AWS, you need to provide some sort of confirmation by typing or copying in a phrase or ID. So we'll put in that ID and then hit confirm. This will delete the EFS and the mountain targets. Now we want to head to the [[Amazon EC2|EC2]] section and get rid of our instances. I'll type EC2 in the search bar and go here. Click instances running, and you have some options here. Later on this course, I'm going to continue to use EBSDemo1 to show things like the file gateway, so you might want to keep it around. I'll keep it around, but if you want to, you can delete both of these instances at once. We'll just click EBSDemo2, go up to instance state, and choose terminate instance. We're given a note down here about deleting EBS volumes. Any ones which do not have delete on termination set to true will persist, and we have at least one volume like that, so we'll need to go to the volume section next. For now, click terminate. This will start our EBS demo machine terminating, and when it's done,
>
> **[1:33](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/ebs-and-efs-teardown?u=76281980&t=93)** we can head to the volume section and take care of those EBS instances. You can see it has disappeared from our list because our instance state filter is on running. If you uncheck that, you can see that it's in the process of shutting down. After a few moments, the instance has transitioned to the terminated state, and we can head over here on the left to volumes. Because I've kept around EBSDemo1, I still have this eight gigabyte volume that's in the in use state. If you chose to delete both instances, that one would've gone away. We had a third one, which was the root volume of EBSDemo2, which has disappeared. What we have left is this 10 gigabyte volume, which is in the available state. That's the EBS volume for which we chose do not delete on termination of the instance. So it's sitting out here able to be attached to another instance if we like. And we want to go ahead and just delete it. So click the checkbox next to that 10 gigabyte volume, go up to actions, and choose delete volume. Okay, with that gone, there are a few other things we still need to clean up. Remember we created an AMI and a snapshot from one of these volumes. We'll start with AMIs. Check the check box next to this one. Go to actions, and then the [[Microsoft Word|word]] is deregister AMI. Go ahead and confirm and we'll head to snapshots. Be aware that you need to delete AMIs
>
> **[3:09](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/ebs-and-efs-teardown?u=76281980&t=189)** before snapshots because there's a dependency there. AMIs depend on the snapshot existing, so you can't delete the snapshot first. Here under snapshots is the snap that we used to create that AMI, and we can go ahead and go up to actions and choose delete snapshot. With that, we've cleaned up everything that we created in the part of the course related to EBS and EFS.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (2), [[Microsoft Word|Word]] (1)
> **Env Vars:** ebs (5), efs (4), ami (3), aws (2), ec2 (2)
> **CLI Commands:** aws (2), snap (1)
> **UI Navigation:** go to (2), checkbox (1)
> **Warnings:** be aware (1)
> **Speakers:** - [instructor] (1)

#### [Amazon FSx](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/amazon-fsx?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/amazon-fsx?u=76281980&t=0)** - [Instructor] Amazon FSx is a multifaceted service where an AWS offers managed versions of several popular file systems. NetApp ONTAP, OpenZFS, Luster, and [[Windows]] File Server. Throughout AWS, managed refers to systems that you could set up for yourself but for which AWS abstracts away the complexity of configuring and administrating them on your own. This makes it easy to stand up sophisticated infrastructure without needing a certification in, for instance Windows File Systems. In fact, FSx is probably the best AWS option for those interested in network attached storage for Windows workloads. Since this course otherwise focuses quite a bit on non Windows storage, I thought we'd take this example and see just what FSx can offer with a few simple clicks. First of all, FSx for Windows behaves just like any Windows File Server file system. Provisioning a SIFs file system and allowing applications and end users to connect via the SMB protocol. File and folder permissions are handled by your own on-premises [[Active Directory]] or by AWS [[Microsoft]] Managed AD. Which is another managed service offered by Amazon. FSx for Windows File System is highly available which is often something that's challenging to implement from scratch. Behind the scenes, Amazon distributes this file system over multiple availability zones, offering high uptime and automatic failover. In addition, FSx can set up a [[Windows Server]] failover cluster. Just the same multi-node HA setup you'd implement
>
> **[1:36](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/amazon-fsx?u=76281980&t=96)** if you were doing this on your own. FSx is cost efficient due to data deduplication and compression options. Amazon claims turning on this feature for the Windows flavor can cut an FSx bill up to 60% for general purpose workloads. Like all AWS managed services, patching is handled for you and can be scheduled to happen at a convenient time. FSx also does automated daily backups and you can schedule even more with the AWS backup service, which we discuss later in this course. Finally, FSx also integrates with other AWS services such as CloudWatch for logging. In all this, you don't need to launch [[Amazon EC2|EC2]] instances, configure nodes, or manage a storage layer. You just launch an FSx, point it to your active directory, and start connecting systems. We won't be demoing this. Setting up active directory is a bit outside the scope of this course. But FSx is a great option if you're looking for Windows Native Storage, ZFS, ONTAP, or Lustre in the cloud.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (9), [[Active Directory]] (3), [[Microsoft]] (1), [[Windows Server]] (1), [[Amazon EC2|Ec2]] (1)
> **Env Vars:** aws (8), ontap (2), smb (1), ec2 (1), zfs (1)
> **CLI Commands:** aws (8), node (1)
> **Definitions:** is a  (3), refers to (1)
> **Prerequisites:** set up (2), setup (1), configure (1)
> **Analogies:** for instance (1), just like (1), such as (1)
> **Cross-References:** later in (1)
> **Speakers:** - [instructor] (1)


### 4. Database Services

[↑ Back to Table of Contents](#table-of-contents)

#### [RDS](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds?u=76281980&t=0)** - [Instructor] AWS's Relational Database Service or RDS can take a lot of work off your hands when it comes to [[Relational Databases]]. Without RDS, deploying a database to AWS can be a rather involved process. You'll provision an [[Amazon EC2|EC2]] instance, attach storage, say using one of the services we've discussed like EBS, EFS, or FSX, and then you'll have to install and configure the database instance yourself. Patching becomes an ongoing concern both for the database and the OS it's installed on. And if you want to read replica or standby for failover, well your database platform may provide tools but you're on your own to implement them. Good news, RDS manages all of this for you. With RDS, you can select one of six different relational database platforms. Oracle, [[MySQL]], [[PostgreSQL]], [[Microsoft SQL Server|SQL Server]], MariaDB and Amazon Aurora. Let's take a brief aside to talk about Aurora. Amazon Aurora is AWS's proprietary database engine. It's compatible with MySQL and PostgreSQL and meant to outperform traditional expensive database engines. AWS advertises that Aurora provides five times the throughput of MySQL and they're positioning Aurora to compete with the likes of Oracle and SQL Server. Aurora is also highly available and durable, automatically replicating itself across three availability zones. It's even available in a serverless flavor that auto scales in response to use rather than needing to be pre-provisioned.
>
> **[1:33](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds?u=76281980&t=93)** Aurora's compatibility with existing engines means that it's an attractive migration target for organizations moving relational databases to the cloud. Back to RDS in general, when you run a database on RDS, AWS handles the compute for you. You never need to remote into an instance to manage your database processes. Of course, that doesn't mean that RDS doesn't have compute resources. Your database needs CPU and RAM to run on. With RDS, the details of the underlying compute resources are abstracted away. Under the covers is this PostgreSQL instance running on [[Ubuntu]], Red Hat or most likely Amazon [[Linux]]? Who cares? As long as it responds on port 5432 when we want connect, we don't need to know. But we do need some control over the provisioning of the compute resource. So in a similar way to EC2, RDS provides us with DB instance classes. The instance classes of RDS mirror the naming conventions of those for EC2. Sample types include standard, memory optimized, and burst capable database instance types. Platform support varies. Some database types only support some instance classes. We can convert a database instance to another instance class whenever we like, but be aware that this will incur a reboot of the database. It's possible to reduce the outage time in a situation like this or perhaps more critically in the case of an unplanned outage. When you select multi-AZ deployment RDS will automatically synchronously replicate data to a standby instance of your database
>
> **[3:08](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds?u=76281980&t=188)** in a neighboring availability zone. Users continue to read from and write to only the primary instance, but if that one goes down, AWS will update the DNS record associated with your RDS instance and it'll resolve to the quite fresh hot standby. Some applications will still need to reconnect but the result will be much less problematic than a full blown outage. AWS claims that this failover usually occurs in under two minutes. AWS uses a variety of methods to achieve this replication. It varies based on the DB platform and its available tools. Just keep in mind that no matter the platform, synchronous replication means that every time you write you must wait for that write to replicate to the standby. If your use case can sustain the additional write latency, multi-AZ is a very attractive failover option. You can fully stop database instances giving you the same flexibility to control uptime and cost that we enjoy with EC2. Just be aware that you can only do this for seven days at a stretch before AWS will restart the instance automatically to ensure that it does not fall behind on required maintenance and patching. Another nice feature of RDS is the ability to create read replicas. Read replicas are copies of your source database that are kept up to date as changes are made in the source. RDS read replicas are kept in sync using the existing asynchronous data transfer options available in MySQL, MariaDB and PostgresSQL meaning those are the only DB platforms currently supported.
>
> **[4:43](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds?u=76281980&t=283)** That transfer is encrypted even when the replica is in another region. And since I mentioned it, yes, your replica can be in another region giving you great high availability on the chance that an entire region goes down. So why create a read replica? Well, one big reason is to reduce load on transactional systems. You can point reporting tasks or other query only workloads to the replica, ensuring your primary system remains responsive. You can create unique instances on the replica if you're using MySQL to enhance its performance without impact in the primary. Read replicas can be promoted to master manually in a scenario where the primary fails. They can be cross region, giving you additional disaster recovery assurances. And finally, your read replicate can use a different instance type than your primary, giving you more control over your cost performance ratio. One last note about replicas. MySQL can actually be replicated to Aurora since they're compatible. So even if you don't start with Aurora, you have an easy way to take advantage of its many performance and storage benefits. Back to management. Don't forget about patching. Both the underlying OS and the database engine need regular updates, some of which may be critical for security. RDS manages that too. We can choose a maintenance window, say, one hour every Sunday at midnight and RDS can automatically apply any OS patches and minor database level updates that are available for our chosen platform. No downtime unless the update does require a reboot. OS patches can be scheduled, taken immediately, or deferred,
>
> **[6:19](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds?u=76281980&t=379)** however, AWS will mark some OS updates as required and will not let them be deferred indefinitely. As for patching the database engine, well, if this capability gives you pause, fear not. AWS has considered that not all database updates are backward compatible or safe for your data. So auto patching is entirely opt-in, does not extend to "major" releases and varies based on the platform. Major updates to the DB engine you'll need to initiate yourself. You'll want to create snapshots of your data and handle any data conversion that needs to take place. Still, auto patching for minor updates can be a great time saver. It should come as no surprise that RDS gives you options as to the underlying storage of your database. Much like instance class the storage option can be changed with minimal downtime. Magnetic is the cheapest and slowest option only available for backward compatibility purposes. General purpose, scales performance with storage size. Provisioned IOPS lets you pay more to specify the performance and size you want. Finally, let's talk backup. In the same way that you can declare a maintenance window for RDS, you can declare a backup window. By default, RDS creates and keeps seven days worth of daily backups for you. But that can go up to 32. Even better, RDS can restore not only any of those daily backups but by replaying transaction logs, it can restore to any point in time within the backup window down to the minute.
>
> **[7:54](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds?u=76281980&t=474)** You should know that anytime you restore an RDS instance you're not restoring in place, RDS will spawn a new RDS instance using the backup point that you request. So if you need to restore some data into your hot copy you'll still need to either manually restore data or reconfigure apps to point to the restored database. Now let's head into the console and try our hand at creating an RDS instance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (6), [[Amazon EC2|Ec2]] (4), [[PostgreSQL]] (3), [[Relational Databases]] (2), [[Microsoft SQL Server|Sql server]] (2)
> **Env Vars:** rds (24), aws (11), ec2 (4), sql (2), ebs (1)
> **CLI Commands:** aws (11), mysql (6), az (2)
> **Definitions:** means that (2), is a  (1)
> **Warnings:** be aware (2), keep in mind (1)
> **Prerequisites:** install (1), configure (1), you'll need (1)
> **Ports:** port 5432 (1)
> **Best Practices:** don't forget (1)

#### [Create a MySQL RDS instance](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-mysql-rds-instance?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-mysql-rds-instance?u=76281980&t=0)** - [Instructor] Let's create a [[MySQL]] database using RDS. But before we do that, I want to visit the [[Amazon EC2|EC2]] section of the console so we can create a security group that will allow us to connect to the database on the appropriate ports. Let's head into EC2. We're going to go down the side here and choose security groups. Here, click create security group. We'll call this rule MySQL access from world. And go down to inbound rules and click add rule. As with most common ports, there is an entry in the dropdown for MySQL. Please start typing, we'll see it. MySQL/Aurora on port 3306. The source can be custom, anywhere or my IP. What we want to do is allow anywhere on IPV4. Now, of course, you wouldn't want to do this in production. In most cases, what I would prefer is to have a database B in a private subnet accessible by other resources within AWS. But for purposes of this demo, we'll make it open to the world and something that we can access from a laptop. Click create security group. Oh, we do have to give it a description. So we'll say, allow my [[SQL]] traffic from anywhere. Now scroll down and click create security group.
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-mysql-rds-instance?u=76281980&t=95)** Okay, so that's ready for us to use in a moment. Now we'll head to the RDS section of AWS. Come to the search bar and type RDS. You'll see this, the relational database service and go ahead and click it. Now we can click create database. All right, on this screen, we have a few options. We're going to go with the standard create rather than the easy so we can see some of the details of what we're choosing. We'll click MySQL. And you can see that we're set with MySQL Community edition and we have a number of version options down below. In this case, I'm going to select version 8.0.23 which is a few minor points behind because I've found in setting up this demo that this is the latest one that is compatible with Aurora. Later on, we're going to create an Aurora read replica from this database, and so we need to select that version. It's possible this may change by the time you're viewing this video. You can always take a look at the AWS documents to be sure. There's also this link here that always shows up which is the known issues or limitations and if you click that, you can see some of the specific details of things that do and don't work on this version of a database running on RDS. It's definitely worth looking at because each database engine has some unique and particular behavior that differs when you're running it on RDS. So some of this is kind of esoteric but it may have a real impact on your workload
>
> **[3:09](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-mysql-rds-instance?u=76281980&t=189)** so be sure to check it out. All right, so we're scrolling down. For templates, we've got production, dev/test or free tier. You can always customize this to your liking but if you choose free tier it's going to make some selections down here for you. You can see we've got options to do multi-AZ deployment or a single DB instance. Of course, a single DB would have no redundancy and no cross AZ capabilities, but for purposes of this demo that'll be all that we need. So we're going to click free tier, and you'll see that down below chosen for you is single DB instance. So we'll scroll on down for the database identifier which will become part of the endpoint URL. We'll call this MySQL RDS demo. Now we need to create a login and I'll call this MySQL_RDS_user. You can have it auto generate password or you can type one in that you have to confirm here. Whatever you do, just make sure that you have the password and can use it in future steps of this video. So I'm going to type one in here and keep going. The instance class can stay at DB.t3.micro, that's fine. Small instance size is all we need for this. Storage can also stay at default. As for connectivity, scroll this into view a little bit and make sure that public access is set to yes.
>
> **[4:45](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-mysql-rds-instance?u=76281980&t=285)** Again, that's not really how we would set up a production database, but that's our purpose for this demo to be able to access it from your own laptop. And to facilitate that, here we select VPC security group in this drop down and look for MySQL access from world. As with many of these security dropdowns in AWS it actually did work behind the dropdown, so just click off that and you can see that it has been selected and you can X out the default security group. All right, keep scrolling down. Password authentication option is fine. We don't need to worry about enhanced monitoring but we do want to expand the additional configuration just for this first box here where I want to put in a name for the database, we'll call it very simply, my database. That's not to be confused with the DB identifier that we gave up above. This is actually the name of the database that will get created inside the MySQL we're about to build. Parameter groups are interesting and we'll look more at those later. There are all kinds of key value pairs that go into a database engine that tell it how to work, change little settings and things. Those are termed parameter groups in RDS and you can take default ones, you can copy them, you can make changes yourself so you can have custom parameter groups. Later on we'll see what it takes to change one of those, but for now we'll leave everything else at the default. Scroll all the way down and click create database. All right, now that this database is being created
>
> **[6:22](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-mysql-rds-instance?u=76281980&t=382)** let's look at how we can connect to it using MySQL Workbench.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (10), [[Amazon EC2|Ec2]] (2), [[SQL]] (1)
> **CLI Commands:** mysql (10), make (5), aws (4), az (2)
> **Env Vars:** rds (7), aws (4), ec2 (2), ipv4 (1), sql (1)
> **UI Navigation:** dropdown (2), scroll down (1)
> **Versions:** version 8 (1), 0.23 (1)
> **Definitions:** is an  (1), is a  (1)
> **Ports:** port 3306 (1)
> **Prerequisites:** set up (1)

#### [Connect with MySQL Workbench](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/connect-with-mysql-workbench?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/connect-with-mysql-workbench?u=76281980&t=0)** - [Instructor] While the RDS instance is being created we can take a moment to go download [[MySQL]] Workbench, the client that we'll use to connect to it. I've searched for that phrase, MySQL Workbench and this was the first hit here at [mysql.com](https://mysql.com) and we can click download now. On the next page you'll be asked to select your operating system and you've got a download button. From here you need to log in or create and then log in with an Oracle web account. Since Oracle purchased MySQL, this is now a step necessary to download this software. I've already done this so I'll leave that for you to pause and continue. But you can see that I've got MySQL Workbench installed here and this is the first screen you see when you open it. All you need to do is click MySQL Connections and the little plus next to that and we'll have an interface that allows us to enter in the details of this connection. Let's head back to the browser. I'll close this tab and I'm back here at AWS. Our RDS instance is in the available state so let's click the name of it to see details of what got created. All right, right here under connectivity and security, the default tab, we have the endpoint, which we can copy. That URL is going to go into MySQL Workbench here under host name, so we can delete that IP address and paste in that URL. Port will stay 3306. Username will be what we created earlier, mysql_rds_user to put in your password, at least for the Mac,
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/connect-with-mysql-workbench?u=76281980&t=94)** I'll click store in keychain and then type in the password that I created earlier. Click okay. Default schema should be my_database and then we'll need to give the connection a name. We'll call this MySQL RDS Demo, before clicking okay, click test Connection to make sure that you don't have any typos or anything like that. And now we see that we do have a successful connection made with MySQL. So this tells us that not only was all of our data entry here correct but also that our security group and our network settings on the RDS instance in AWS are all correct. We have a clear network path from our home machine to the RDS database. So click okay and now we have a box here representing the connection, we can click on that and it will establish a connection to the database. Up here if you click the schemas tab, you'll see that we are in the my_database database, no tables yet which is to be expected, it's a brand new empty database. So we need to give it some data. To do that, click little folder here and navigate to chapter four in the exercise files we're going to open this file called employee_data.[[SQL]], so double click on that. Let's take a look at what we've got here. At the top of the file, you'll see drop table statements. If you run these, it'll allow you to clean up everything that's in the script and run everything from scratch. Then we've got create table statements for departments,
>
> **[3:07](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/connect-with-mysql-workbench?u=76281980&t=187)** employees, projects, and project team members. So we're setting up a basic workplace schema here. Further down, we've got insert statements that put values into those tables, and on line 76, a commit. After that, we've got a couple of SQL queries that will let us confirm the data that we've put into the tables. So let's scroll all the way up to the first create table statement and highlight all the way down through the commit. Click the lightning bolt here and everything will get created as you can see down here below. And then we can highlight this query here to confirm that we've entered data. All right, there you can see the members of our sales team and with that, we have now created an RDS database, logged into it with MySQL Workbench, and created some sample data. In the next video, we'll look it how to create a read replica using Aurora.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (10), [[SQL]] (2)
> **CLI Commands:** mysql (10), aws (2), make (1)
> **Env Vars:** rds (6), aws (2), url (2), sql (1)
> **Code Identifiers:** my_database (2), mysql_rds_user (1), employee_data (1)
> **UI Navigation:** click on (2), navigate to (1)
> **File Paths:** employee_data.sql (1)
> **URLs:** [mysql.com](https://mysql.com) (1)
> **Cross-References:** in the next (1)

#### [Create an Aurora read replica](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-aurora-read-replica?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-aurora-read-replica?u=76281980&t=0)** - [Instructor] I'm here at the RDS Console looking at the details of the [[MySQL]]-rds-demo database we just created. What I'd like to do in this video is create a read replica of this MySQL instance using the Amazon Aurora database type. Go up to Actions, and you'll see Create read replica, and below that, Create Aurora read replica. Go ahead and click it. As this screen loads, we'll start to see some options come up. And we'll choose the latest Aurora version that's compatible with the version of MySQL that we selected. So if you click the dropdown, you'll see references to various MySQL versions, including the one we chose earlier, 8.0.23. So we can select the highest compatible version, which is this, 3.02.1, and then look at DB instance class. You'll find with Aurora that we don't actually have the option to choose some of the smallest classes that we've used before. You won't see smalls or micros on here, but you can choose this db.t4g.medium as one of the smaller cheaper versions. If you're following along with this demo, this instance will not live for very long, so don't worry too much about charges. All right, so moving on, we do not need multi-AZ deployment. For our database instance identifier, we'll call it mysql, maybe aurora read replica. We'll go down to the next section, [[Network Security]], and make sure that publicly accessible is selected. As for availability zone, it doesn't really matter.
>
> **[1:36](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-aurora-read-replica?u=76281980&t=96)** You can leave that at the default. We do need to choose an existing VPC security group so that we can access this instance from outside. So click that and choose the MySQLAccessFromWorld. Remember that Aurora and MySQL both use the same port, 3306, so that security group will work for both. You can also X out the default VPC security group, and then move on from there. Look over the remaining options, you see the port, as we just mentioned. You see the parameter groups are at their defaults. Everything else can stay where it is. Scroll all the way down and choose Create read replica. Now, this step can take a while to complete. Not only are we standing up a new RDS instance with the Aurora database, but AWS also has to set up the replication between the two [[Databases]]. So we'll wait for this to be done, and then we'll see how the replication works. To monitor what's going on, click Databases. And we'll see that we've got an Aurora cluster in the creating state. So what about this cluster? Well, what you'll see in a moment is that we actually end up with two Aurora instances. In past versions of this course, creating an Aurora read replica resulted in just one additional instance. Now the minimum standard deployment of Aurora creates a cluster of two instances: a read/write main instance and its own read replica used for failover. So you might wonder, why don't we just start with an Aurora cluster instead of creating a MySQL RDS
>
> **[3:10](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-aurora-read-replica?u=76281980&t=190)** where we then have to create a read replica and somehow implement failover for that? And I would say that's the point. That is what Amazon is trying to do. With Aurora, they're providing a very attractive value proposition to people who want that kind of managed resilience in a MySQL or [[PostgreSQL|Postgres]]-compatible database. So that would work to just start with Aurora and use that for any MySQL purposes that you have in the future. If you click the refresh button here, you can kind of watch things creating. And we'll come back in a minute to see both of these databases available. Okay, now the read replica is available. And we'll want to see how the replication is working between our original MySQL and this Aurora instance. But before we connect the two instances, I want to point out what's going on here. For Aurora, we see two records here on screen: one designated replica cluster, and the other called writer instance. To illustrate why, let's take a quick look at AWS docs about Aurora. You see here a depiction of a typical Aurora cluster. And the first thing to understand is that every Aurora database you provision, no matter what size, is called a cluster. Part of the reason why, as this image shows, is that Aurora separates compute from storage. There's always a common storage layer, shown here in gray, that's spread across multiple availability zones That exists no matter what. And there's always at least one instance, which is the compute you connect to.
>
> **[4:44](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-aurora-read-replica?u=76281980&t=284)** While we chose single-AZ deployment, the most common Aurora installation involves multiple instances spread across multiple AZs. In that case, you'd have one endpoint like this, that is the read/write endpoint, and any additional endpoint would be an Aurora read replica. In our case, our cluster is a single AZ, meaning we just have the one read/write endpoint. Our cluster is basically this section on the left here and the gray bar showing the storage spread across multiple AZs. Nevertheless, AWS refers to the cluster and even shows URLs for both writer and reader instances separately. The trick is that in our case, these instances are one and the same. Our writer instance and our reader instance are both wrapped up in that one instance that we have provisioned. If you take a look over here, you can see that. We've got a link for the replica cluster and for the writer instance. But if we click in to the cluster, we can get a little bit of an idea of what's going on here. It shows two endpoints, but you can see here the size of this cluster is listed as one instance. This is where things are a little bit confusing if you have chosen not to do multi AZ. They refer to a writer instance and a reader instance as if they're separate things. But in fact, if you look at the domain names that are provided and resolve them with something like nslookup, you'll find they go to the exact same IP address. So just as we're told here, the size of this cluster is just one instance. So now that we've cleared that up,
>
> **[6:20](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-aurora-read-replica?u=76281980&t=380)** let's connect to this Aurora instance and compare it with our MySQL to see how the replication's working. Let's go back in and actually grab one of those domain names using the copy button here, and then we'll head back to MySQL Workbench. I'm here on the home screen, and I'll click the plus button to add a new connection. We'll call it Aurora Replica, and we'll paste over the IP address. It's my host name with that domain name. The username will be just the same as our original MySQL instance because, of course, this is meant to be a perfect replica. mysql_rds_user. For the password, that will again be the same. Click OK. And under default schema, we'll say my_database. Click Test Connection and make sure you've typed everything correctly. And if that works, click OK, and then OK again. Double click Aurora Replica to open it up. All right, so now we've got two tabs here in MySQL Workbench, the Aurora replica here, and the original MySQL RDS database here. We'll click into that first tab, and then highlight the sales query to see our initial state. I'll hit the lightning bolt on that, and we can see the three rows that started out in this database. And we can copy this query, then head back to the Aurora replica and paste it in. Hit the lightning bolt again, and you can see the three rows are here. So the initial migration has been successful. But of course, we want to see an ongoing replication.
>
> **[7:55](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-aurora-read-replica?u=76281980&t=475)** Let's head back to MySQL, and you can see I've typed in a new insert statement here. Insert into employees values 99, which is a high ID number that won't conflict with anything else, my first and last name, and the number one for the sales department. So that should show up in the same query we've been using. Highlight these two rows here, the insert and the commit, hit the lightning bolt. And before we go anywhere, let's check the sales query here in the source database, just to confirm that worked. So we now have four rows here in the source, and we'll head over to Aurora to see the replication. Click the lightning bolt again. So there you have it, four rows and practically instantaneous replication from MySQL to Aurora. Now, let's head back to the AWS Console for a moment. As I said, a read replica can be used for a variety of purposes. It makes a great endpoint for reporting or anything else that really only needs a read-only database. The other thing that we can do in a disaster scenario is back here on the the Databases page, we can highlight the cluster, choose Actions, and go to Promote. What this will do is it will make the read replica become a full-fledged RDS instance of its own. At that point, it will become a normal standalone RDS database that you could redirect traffic to. It'll have all your data that was replicated before the promotion. In fact, if you watch the tear-down video at the end of this chapter, and you should to make sure that you don't incur any unexpected charges, you'll find that this promotion step is necessary before we can fully delete the Aurora cluster.
>
> **[9:30](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-aurora-read-replica?u=76281980&t=570)** Before we get to that, though, let's take a look at backup and restore in RDS for those mistakes that won't be helped by a read replica.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (17), [[Databases]] (4), [[Network Security]] (1), [[PostgreSQL|Postgres]] (1)
> **CLI Commands:** mysql (17), az (4), make (4), aws (4), find (3)
> **Env Vars:** rds (7), aws (4), vpc (2)
> **UI Navigation:** go to (2), dropdown (1), select the (1)
> **Definitions:** is a  (2), is called (1), refers to (1)
> **Code Identifiers:** mysql_rds_user (1), my_database (1)
> **Versions:** 8.0.23 (1), 3.02.1 (1)
> **Tools:** aws console (1)

#### [RDS backup and recovery](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds-backup-and-recovery?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds-backup-and-recovery?u=76281980&t=0)** - [Instructor] Let's imagine that I've just made a terrible mistake and deleted some important data out of the source [[MySQL]] database. We have the read replica, but that stays in sync with the source. So it's really not that much help if I've deleted something important. What am I going to do? Well, remember that when we set up an RDS database, it defaults to keep seven days worth of snapshots taken nightly. So that means I can at least get back to last night. But often, that's not good enough. We need something a little more recent. Fortunately, RDS has our back here. If we have the mysql-rds-demo highlighted and go to Actions, you'll see Restore to point in time. This is a really helpful feature that has genuinely saved me in real life a few times. In a moment, you'll see that we have some options. Let's start with Restore time. The first option is Latest restorable time which is just a few moments ago. And we can also click here and we appear to be able to put in just about any time that we want. How does RDS let us do this? Well, we have the nightly backups but to get something in between, RDS looks at the actual change logs of the database engine. These are the logs that MySQL keeps as you update data all through the day. So it goes up to the most recent backup that we have, say last night, and replays the changes from that log through the day up to the point in time that you're specifying. So it can get really specific on a restore time. If we leave this at latest restorable time and scroll down, you see the [[Representational State Transfer (REST)|rest]] of what we have to fill in here is just to say, what's our new instance going to be?
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds-backup-and-recovery?u=76281980&t=94)** Because remember, RDS doesn't do restorations in place. It creates a new instance for you to log into. So you can see here, we need to give a database identifier. This is called mysql-rds-demo right now, so we can just paste that and append something like, restored so we know what's what. And then we can pretty much leave everything else as is. I mean, we can change instance type, we can change other configuration options on this database but we really just want a replica that goes back to that point in time. So we can go all the way to the bottom and choose Restore to point in time. Now, RDS is creating a new instance based on my previous one at that exact point that I specified. When I need to get the data back, all that I'm going to need to do is create a new connection using MySQL Workbench to go to this new database and extract the important data that I accidentally deleted. I can either from there, manually restore it to the original database, or if I like, I can take all my applications that are connected to the source and reroute them to this new restored database.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (5), [[Representational State Transfer (REST)|Rest]] (1)
> **Env Vars:** rds (6)
> **CLI Commands:** mysql (5)
> **UI Navigation:** go to (2), scroll down (1)
> **Definitions:** is a  (1), is called (1)
> **Analogies:** imagine (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [RDS teardown](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds-teardown?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds-teardown?u=76281980&t=0)** - [Instructor] As usual, it's important to make sure that we clean up the resources we no longer need so that we won't be charged for them. All right, the first thing I'm going to do is go to the [[MySQL]] RDS restored, go up to Actions, and choose Delete. We do not want to retain automated backups, so we'll uncheck this and we'll check the next thing, which is an acknowledgement that this is going to delete everything and no backups will be available. Type delete me to confirm that and click Delete. As for MySQL RDS demo, you can do the exact same steps to that database, or if you want to continue to follow along with this course, keep it around, we'll use it later in the course. For the Aurora cluster, what we need to do is click on the row for the replica cluster, and before we can delete it, we need to go to Actions and choose Promote. This will turn it into its own full-fledged standalone database instead of being a read replica of the MySQL demo. Click Promote Read Replica. This will take a moment to complete, and when it does, you'll see that this no longer says replica cluster. Now it's done and it says regional cluster, which means that we can delete this. Instead of clicking on the cluster, we'll click on the actual instance, the second row, go to actions, and then delete. Uncheck create final snapshot, acknowledge that we don't care, and type Delete Me. Then click Delete, and now the cluster will be on its way to deleting.
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/rds-teardown?u=76281980&t=94)** Before we finish, the last thing you want to check is go over here and expand, and choose Snapshots. I didn't keep these instances running long enough for a snapshot to have taken place overnight. However, if you've been following along over the course of a couple days, you may have, so come in here and check any snapshots that exist, go up to actions and delete those as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (3)
> **CLI Commands:** mysql (3), make (1)
> **UI Navigation:** go to (2), click on (1)
> **Env Vars:** rds (2)
> **Definitions:** is an  (1), means that (1)
> **Cross-References:** later in (1)
> **Speakers:** - [instructor] (1)

#### [DynamoDB](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dynamodb?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dynamodb?u=76281980&t=1)** - [Instructor] DynamoDB is AWS's highly available, scalable [[NoSQL]] database. You might compare it to other NoSQL [[Databases]] like [[MongoDB]]. The main advantage of Dynamo is that like RDS for [[Relational Databases]], Dynamo is a fully managed service. It scales performance on demand, meaning that you can provision and pay for just the level of read-and-write performance you desire. It's also a distributed storage system, meaning its durability and availability is very high. Like S3, it can publish event streams for all its activity. And finally, it's stored on SSD, which gives it very low latency. The primary artifact of DynamoDB is the table. You can almost think of this as equivalent to a bucket in S3, it's not like a table in a relational database, it's a place to store objects. You can have as many of these tables as you want. And within a table, you'll create records which AWS calls items. Items cannot exceed 400 K in size. Items can be one of a few data types. Scalars are single-value types such as strings or booleans. They are multi values, they're like arrays or sets. And documents are complex types that combine the above two and can involve in [[Data Structures]]. Using these types, what you'll end up with is something that is basically [[JSON]]. In this example, first and last are scalar types of string. pilot_of is a string set,
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dynamodb?u=76281980&t=95)** more commonly called an array of strings. Finally, the whole thing is a map object, a conglomeration of the other data types into a structure that contains many key-value pairs as well as substructures like the nested object father. This kind of data model means that Dynamo is an ideal choice for non-relational data. If you need to frequently join records, you may want to look at putting your relational database on RDS or on [[Amazon EC2|EC2]], but if your data load is high volume, non-relational, and under 400 K, Dynamo is going to work really well. Now let's discuss primary keys. Although NoSQL databases are schemaless, each Dynamo table you create should contain the same kind of data. The exact fields of the items might vary, some fields might be optional, for instance, who are just not present on some records, but there needs to be some consistency as to what's inside. So given that when you create a DynamoDB table, the first thing you'll need to do other than give it a name is select a primary key. Think about the format of the data that you want to put in. In the previous example, we had first, last, and pilot of all examples of keys. For the primary key, we want to select one that will uniquely identify a record, meaning it should be present for all items. They should be diverse and evenly distributed enough within the data to be good for physically partitioning the data. And they should serve as effective keys for actually querying the data, which we'll get into. In some cases, you might have an actual ID or a GUID, like an employee ID to serve as a primary key.
>
> **[3:12](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dynamodb?u=76281980&t=192)** When it comes to querying, you have just a few simple choices. You can query on that kind of primary key, or you can query on what's called a sort key, which is a kind of secondary key. You can create custom indices on which to query, these can be comprised of any other field on an object, but they must be maintained, and they take up space. They also often force you into a two-part query model where first you query the index, obtain a primary key, and then use that to query the object you really want. Finally, you can perform a full table scan retrieving all records and filter the results, which can be a slow process. You really want to choose your keys and your indices well so that you can minimize the result set that you've got to filter at the end. So what's a sort key? Sort keys combined with primary keys to act like a composite key that will extend your namespace, allowing the primary key to be repeated. So for a trivial example of that, if the primary key was first name, having last name as a secondary key would let you store both [[John the Ripper|John]] Smith and Sally Smith. Otherwise, you'd get a primary key conflict error. Sort keys don't affect partitioning, so nothing to worry about there. However, sort keys do affect the order in which the record will be stored on disk, which can have implications for query speeds, that's why these sort keys are sometimes called range keys. Indexes in Dynamo allows searching on a field other than the primary key. Any field that's not a primary key, sort key, or index cannot be directly searched.
>
> **[4:47](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dynamodb?u=76281980&t=287)** Also worth noting, indexes become their own records and take up space, so you'll want to think about that. As an example of how these keys and indexes work together, imagine we're storing employee records. If we index start year and have employee ID as a primary, we'll enable a two-step process to drill down to employee records by start year. First, we'll search the start year from the index, grab all the matching employee IDs, and then query the main table for those employee IDs by a primary key. Like S3, DynamoDB is designed for high durability and availability, all your data is synchronously replicated to three different locations within your region. You also have the option to enable cross-region replication, which asynchronously replicates your Dynamo tables to another region. AWS handles this automatically, so if there's a problem in your primary region, you need only to switch your endpoint to the other, and your connected applications can fail over to the backup. Note that there is an additional charge for enabling this feature. One last capability of Dynamo I should mention is its streams feature. Similar to event notifications in S3, streams publish events whenever something changes in a table. You can get notifications for things like inserts, updates, and deletes on records letting downstream systems note that a change occurred as well as providing context, like the JSON that was just edited or deleted, making it possible to chain Dynamo with other services like Lambda or SNS to create some interesting [[Design Patterns]]. With that, let's head to the console
>
> **[6:20](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dynamodb?u=76281980&t=380)** and try our hand at creating and querying a simple DynamoDB table.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[NoSQL]] (3), [[Databases]] (2), [[JSON]] (2), [[MongoDB]] (1), [[Relational Databases]] (1)
> **Env Vars:** aws (3), rds (2), json (2), ssd (1), ec2 (1)
> **Definitions:** is a  (4), is an  (2), means that (1)
> **Analogies:** such as (1), for instance (1), imagine (1), similar to (1)
> **CLI Commands:** aws (3)
> **Code Identifiers:** pilot_of (1)
> **Warnings:** note that (1)
> **Prerequisites:** you'll need (1)

#### [Create a DynamoDB table](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-dynamodb-table?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-dynamodb-table?u=76281980&t=0)** - [Instructor] All right, in this demo we're going to create a DynamoDB table. I've got the service here under recently visited, but of course you can always go up here and type into the search bar and click the first option. Now before we get started, let's go to the console where I have opened the file under chapter four called film_data.[[JSON]]. So this is in the exercise files. If you open that up, you can see that there are a number of Dynamo ready items here that describe different movies such as "Jaws," here, "Memento," "Eternal Sunshine of the Spotless Mind." You can scroll through this and see. Now this is not exactly a JSON file because if it were this would be an array and everything would be in square brackets with commas between it. Instead, what we have are individual items here, and in a moment we'll need to copy and paste some things into the GUI. Now, if you were using DynamoDB in a production setting you would probably be working with an application that you're building, which would mean you'd be using some sort of programming language SDK, say for [[Python (Programming Language)|Python]], and you would interact through that to talk to Dynamo, and your application would be creating these items. So what we'll do is a little bit out of the ordinary from a production scenario, but it'll work pretty well. So back to the console, we are in the Dynamo DB section. Let's click Create table. So we'll call it FilmData. And for our partition or primary key, we will enter, go back and you can look film_title, film underscore title. That's going to be something that appears in every item.
>
> **[1:36](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-dynamodb-table?u=76281980&t=96)** So that makes a good primary key because it's in every item and it's evenly distributed, sort of, alphabetically. As to the sort key, a secondary thing that we might want to query on would be director. So we'll type that in. Keep all these performance configuration options at their default setting, no need for tags, and click Create table. At this point, our table is creating. Should just take a second and we can refresh here to watch its progress. Okay, the table's active now, so we can click its name and go to Explore table items, this button here in the top right. Here we have two sections. We have the Scan and Query section, which we can use in a moment to query our data, and we can collapse it now. And we can see items down here in the bottom. Right now, of course we have no items, so we'll click Create item. Now because we defined that partition key and sort key we are greeted here with inputs for film title and director. So as you can see here in this little tab widget here, we're on the form view of this page, but we can also go to the JSON view. And here in the JSON view we have another toggle which gives us even more information. So you can check and uncheck this toggle that says View DynamoDB JSON. When you're viewing Dynamo DB JSON, this is literally the JSON format that Dynamo stores it's data in. So you can see this looks a little bit different from your typical JSON you might expect. Instead of film title being a key and the title being a value,
>
> **[3:10](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-dynamodb-table?u=76281980&t=190)** film title is a key that leads to a small object here, and we have S as the key and something here ready for us to type in a value. So that's what DynamoDB JSON looks like behind the scenes, and if you're working with its API or [[CLI]], you might need to use this format, but for our purposes we can just untoggle this and we can see something here that looks a lot more like JSON. So what we can do here is go back to the console and highlight one of our films from bracket to bracket, copy it, come over here, delete what's here, and paste in that text. Then we can click Create item. So this is a little bit of a slow process, like I said, not normally the way that you would do it in practicality, but take a moment, go through here, and highlight each of those films, and create some items in the database. Okay, now that we've got all the film titles in here, we can take a look at the Scan/Query section and see a little bit of how we can interact with this data. So in our dropdown here, we have just a table name, and we can choose, oops, you can always expand that again if it collapses, we can go either scan or query the data. So scan is just kind of what it sounds like. It's a full table scan, which means that if you run it, you're going to get all the objects back, and you have the option to filter. So you can filter on any attribute of the data. I could put in here something like film title is equal to and we'll say "Jaws" and run that again
>
> **[4:44](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-dynamodb-table?u=76281980&t=284)** and we're going to get just that response back. But in terms of performance, it's not very good, because we are pulling all the data back on the server side, pulling that all down over the network, and then filtering the results on the client side for just the thing that matches the film title that we wanted. So we can also go to query here and we can search based on the partition key, which is the film title. So I can type something that I know exists like "Star Wars," run that, and get a response back. Of course, the difference is I'm not doing a full table scan. I'm not bringing all the data of the database over a network call. That query is being executed on the server side in AWS. I can also filter on the director here. The sort key can't be queried by itself, but it can be queried as a subset. So, of course, this will work if I type in director George Lucas. By the way, I've got two versions of the movie "Psycho" in here. So if I search that as the partition key, I get both. But then I can specify Alfred Hitchcock to narrow that down a little bit. So if I want to search beyond that and I want it to happen on the server side instead of being a full table scan, then I'm going to need to create an index. So I head back to tables here on the side, click into FilmData, click on Indexes and Create Index. So here we're creating another partition key and sort key because essentially what we are doing
>
> **[6:17](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-dynamodb-table?u=76281980&t=377)** is we are creating a copy of our data, and how extensive a copy of that we get to decide here, but we do need to start with a partition key. So here in our data we have release year on a lot of the objects, but not all of them. That's okay, that's what an index is good for. So we type in release year there, and I'm going to leave sort key off. I'll scrolling down and keep all of the defaults here, and I want to point out attribute projections. This is where we can determine how much of a copy this index actually is. If we say all, then that means every attribute of every data record is going to get copied into this index. We can be more specific and say only certain keys, but for this demo I'll keep it at all, and we'll click Create index. Now that this index is being built, we can go back to the table level, click into FilmData and click Explore table items again. And now under Query, we should have something new in the dropdown. Now it may take a moment for the index to appear, or we may need to refresh the page. And now we can see that the index does exist. We can click on it, and we see we have the same two options, Scan and Query. If I click Query, kind of a quirky thing, it actually skips back to the table option here. But we can reselect Index, and we see we have a different thing that we can search on, this partition key based on release year. So I can type in 1991 and get back "Jurassic Park." And now that query is taking place on the server side
>
> **[7:51](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-a-dynamodb-table?u=76281980&t=471)** with AWS rather than pulling the full table scan down and needing for me to filter it on the client side.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (10), [[Python (Programming Language)|Python]] (1), [[CLI]] (1)
> **Env Vars:** json (9), aws (2), gui (1), sdk (1), api (1)
> **UI Navigation:** go to (4), toggle (2), dropdown (2), click on (2)
> **Definitions:** is a  (3), means that (1)
> **CLI Commands:** aws (2), python (1)
> **Code Identifiers:** film_data (1), film_title (1)
> **Cross-References:** go back to (2)
> **File Paths:** film_data.json (1)

#### [Dynamo teardown](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dynamo-teardown?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dynamo-teardown?u=76281980&t=0)** - [Narrator] Okay, let's clean up our DynamoDB table. This is a pretty simple process. We're here under Dynamo under tables and we'll click the checkbox next to film data. Click delete, type in the [[Microsoft Word|word]] delete. Have this box checked to delete all CloudWatch alarms and no need to create a backup. Click delete table. That's going to take care of the table of the items and the index.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **UI Navigation:** checkbox (1)
> **Definitions:** is a  (1)
> **Speakers:** - [narrator] (1)


### 5. Getting Data to AWS

[↑ Back to Table of Contents](#table-of-contents)

#### [Move data to AWS](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/move-data-to-aws?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/move-data-to-aws?u=76281980&t=0)** - Unless your organization is just getting started, you no doubt have lots of data on premises. And like many organizations, your data is everywhere. It's on servers, local file systems, on a range of file system types and operating systems. It's on network attached storage. It's in [[Relational Databases]] of many different types. Oracle, [[Microsoft]] [[SQL]], [[PostgreSQL|Postgres]], [[MySQL]]. Some of this might even be on staff laptops in the form of text files or CSVs. When we are ready to move to the cloud, how do we get all this data to AWS? We've seen a few ways already. You can always upload data to S3, or add records to Dynamo directly from the AWS gooey. But that's often the slowest way to do things, and certainly the least automated. And recall that if you're using AWS Direct Connect, you can transfer data quickly over the direct Connect pipe to an EFS file system via an NFS mount. But there are many more ways, of course. In this chapter, we'll explore some of those other options. For instance, command line interfaces for services like S3, have commands that can push, update, sync, and manage data in those services. We'll look at that in the next lesson. After that, it's AWS storage gateway, a service that can provide you local NFS mounts that ultimately convey your data over the internet to S3. Next, we'll examine AWS database migration service and schema conversion tool. These AWS features can help you both migrate,
>
> **[1:33](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/move-data-to-aws?u=76281980&t=93)** and if you choose, continuously replicate local [[Databases]] to RDS, S3, and more. You're still subject to the latency of your AWS connection overall, but these tools make the move a lot simpler than a DIY approach. Finally, we'll take a peek at AWS's take on Sneakernet. AWS Snowball. This service lets you ship your data directly to AWS, bypassing the internet entirely. We'll start with command line tools in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Relational Databases]] (1), [[Microsoft]] (1), [[SQL]] (1), [[PostgreSQL|Postgres]] (1), [[MySQL]] (1)
> **Env Vars:** aws (10), nfs (2), sql (1), efs (1), rds (1)
> **CLI Commands:** aws (10), mysql (1), make (1)
> **Cross-References:** in the next (2)
> **Tools:** command line (2)
> **Analogies:** for instance (1)
> **Prerequisites:** getting started (1)
> **Speakers:** - unless (1)

#### [Transfer data using the AWS CLI](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/transfer-data-using-the-aws-cli?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/transfer-data-using-the-aws-cli?u=76281980&t=0)** - [Instructor] In this video, I'm in a terminal to demonstrate how the AWS command line interface can be used to interact with S3 and sync large amounts of files to it on a continuing basis. If you don't have the [[CLI]] installed and configured, check out my other course called AWS Deployment, Provisioning, and Automation. To get started, we'll type aws s3 ls to list our buckets. This will let us know that everything is hooked up right. Here we see the two buckets that I created earlier in the course. So with that, let's type aws s3, and then mb for make bucket. We'll type S3://, and then the name of the new bucket we want to create. I'll call this s3-cli-sync, and then give my initials to ensure uniqueness. With that done, we can go back to the console and observe that the new bucket has been created. Okay, I'm in a folder of the course materials called Ch5/s3-cli-demo, and inside here, you can see that we have one big file, and a subfolder full of smaller files. You can see we have lots of files there that we want to get to S3. Type cd .. to back up one, and make sure you're on the same level as big file. From here, we could use the S3 CLI to copy files individually, but we have a better option. We'll type aws s3 sync, and then a dot for the current folder, and we'll specify the name of the bucket, S3://s3-cli-sync, and then my initials,
>
> **[1:38](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/transfer-data-using-the-aws-cli?u=76281980&t=98)** the fully qualified bucket name. Hitting Enter and executing this will send up all these files on a recursive basis to S3. The big file and the subfolder. We can go back to the console and check out the results. Click here, and you can see we've got everything that we expected, including all of those smaller files. But how do we know that this is a true sync? Well, let's start by adding another file here. We'll call it hello.text. Now we've got a new file, and if we execute the same sync command again, you can see that it only uploads the new file that it didn't already know about. And if we head back to the console and click refresh again, you can see that that's true. So you can see how the sync function is very powerful. If you want to make sure a certain folder or files is always sync to S3, a good way to achieve this would be to set up a recurring crime job to run the sync command on the same folder. It will only incur the bandwidth for newly created files. With this, you can use the CLI to stay in sync with S3.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[CLI]] (6)
> **CLI Commands:** aws (5), make (3), ls (1), cd (1)
> **Env Vars:** cli (3), aws (2)
> **Cross-References:** go back to (2)
> **Tools:** terminal (1), command line (1)
> **Definitions:** is a  (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Storage Gateway](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/storage-gateway?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/storage-gateway?u=76281980&t=0)** - [Instructor] We've discussed S3's incredible durability and availability. However, it can be a bit challenging to integrate with other systems since it doesn't present a traditional file system interface. By default, you must interact with it via restful HTTP calls, AWS [[CLI]] commands, or via the AWS web console. But what if you could make S3 mountable as a file system? That's where AWS Storage Gateway comes in. Storage Gateway is a software appliance you can place in between your data source and S3. Storage Gateway acts as a go-between. Presenting a file system front end to S3 and Glacier Storage. It can be configured to provide file, volume, or tape interfaces. Storage Gateway is smart. It will cash hot data locally so you don't have to go all the way to S3 each time you access frequently used data. And here's where things get interesting. It can be deployed to either [[Amazon EC2|EC2]] or to VMware. Deploying to VMware means that storage gateway can act as a bridge between your existing local data center and the cloud. In a [[Hybrid Cloud]] scenario, you deploy your gateway as a VMware appliance in your local data center. Servers in your data center can now mount a file system on that gateway and seamlessly gain access to [[Cloud Storage]]. You can persist in this mode and maintain a true hybrid cloud, or you can use the gateway as a bridge to AWS, leveraging it as a way to get data to S3 in a one-time migration. Storage Gateway provides a few different ways
>
> **[1:33](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/storage-gateway?u=76281980&t=93)** to present to file system. File Gateway allows the client to mount via NFS or SMB, making it suitable for both [[Linux]] and [[Windows]] workloads. There's Volume Gateway, which uses an iSCSI interface. Tape Gateway presents a virtual tape library or VTL interface. Tape storage is often used for archiving data that is very relaxed requirements for access times. As you may have guessed, this type of gateway uses the Glacier tiers of S3 storage for the most infrequently accessed data. Let's dig into the first mode. As a File Gateway, AWS storage gateway can present an NFS or SMB file system that is backed remotely by an S3 bucket. This file interface can be mounted by any instance that supports these protocols. This can occur entirely in AWS or the gateway can be on premises in VMware, making the file gateway a local bridge to cloud storage. Unlike mounting EFS from outside AWS, this model does not require the use of direct connect. Within a single gateway instance, you can create one or more file shares. These mapped individual buckets and will be your actual NFS mount points. This means that you can potentially serve many different applications and use cases with a single file gateway. You might be wondering how a file gateway handles NFS [[Metadata]] like file permissions. Permission bits and ownership attributes are stored in S3 object metadata. To provide optimal performance,
>
> **[3:06](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/storage-gateway?u=76281980&t=186)** the file gateway pays attention to what data is used most often and keeps it in a local cash. So hot data can be read and written quickly without incurring network latency. Files are synced to S3 behind the scenes while you get your data without lag. File Gateway does come with a few caveats. First of all, AWS makes it clear that although you're technically able to access the backend S3 directly, you should avoid doing so. The behavior of File Gateway is not well defined when you alter the backend S3 storage and may result in errors or lost data. File Gateway does support using buckets of various S3 storage tiers. However, you should read Amazon's documentation on this as certain usage patterns can trigger additional fees. Such as the infrequent access tiers, early deletion fees, or even errors such as when trying to update files that have been moved to Glacier. For our demo, we'll stick with S3 standard to avoid any of these issues. Other things to consider. File Gateway cannot support symbolic or hard links. So if these features are critical to your use case, you'll need to use another solution. Also, since S3 does not support renaming objects directly, File Gateway implements the operation by copying the affected file objects and deleting the old ones. This means that your gateway won't have to send any additional data over the network. But it also means that mass renames, such as renaming a folder containing many files and subfolders, may not be an instantaneous task. Finally, although the NFS protocol will allow connections for more than one client,
>
> **[4:40](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/storage-gateway?u=76281980&t=280)** AWS again recommends that you do not do this and instead maintain a one-to-one connection policy between clients and file shares. This will help avoid possible data corruption. If what you need is an NFS file system that supports multiple clients as a first class use case, you're going to want to look at EFS. However, if your goal is to take advantage of S3's highly durable, relatively cheap storage, while avoiding the complexities of interacting directly with S3 APIs, AWS Storage Gateway is a great way to do just that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hybrid Cloud]] (2), [[Cloud Storage]] (2), [[Metadata]] (2), [[CLI]] (1), [[Amazon EC2|Ec2]] (1)
> **Env Vars:** aws (10), nfs (6), smb (2), efs (2), http (1)
> **CLI Commands:** aws (10), make (1)
> **Definitions:** means that (4), is a  (2), is an  (1)
> **Analogies:** such as (3)
> **Code Identifiers:** iscsi (1)
> **Best Practices:** avoid doing (1)
> **Prerequisites:** you'll need (1)

#### [Configure File Gateway and shares](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/configure-file-gateway-and-shares?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/configure-file-gateway-and-shares?u=76281980&t=0)** - [Instructor] Now we'll create a File Gateway using AWS Storage Gateway. Before we get started, there are two things we need to take care of. First, click into S3. We need to create the bucket. It will be the backend for this gateway. We'll go over here to create bucket and we'll call it something like file-gateway-demo, and I'll give it my initials again to ensure uniqueness. I'll keep everything else at the default and click create bucket. Now we need to create a security group. Head over to the [[Amazon EC2|EC2]] section, and along the left-hand side, go to security groups. There's a one-time step in the File Gateway configuration where AWS needs to access the newly created gateway via port 80. Amazon actually says that as long as this traffic is allowed for my laptop, things will work and it can be turned off after the gateway's initialized. For our demo purposes, we'll just allow port 80 from the world and remove this access during the tear down at the end. But if it were your production instance, you'd want to allow it only as long as it is needed for the configuration. We'll go up to Create Security Group and we'll call this AllowHTTPFromWorld and in our description, allow port 80 HTTP traffic from anywhere. Go down and click Add Rule under Inbound Rules and choose the prefilled HTTP option, which makes Port 80 the default here
>
> **[1:33](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/configure-file-gateway-and-shares?u=76281980&t=93)** and our source will be anywhere, IPv4. That's all we need to do here. So go down and click Create security group. Now we can head into the Storage Gateway section of AWS. We'll type that up here in the search bar and click the first option. From here, click Create Gateway. We're going to give our gateway a name, which will be FileGatewayDemo. Here below we have a few different gateway options, including the default, which we'll choose the S3 File Gateway. That's the one that's backed by S3 Storage. We also have FSX, Tape Gateway and Volume Gateway. Under Platform Options, you see a few things. This is where we choose the platform for the Storage Gateway instance itself. If we are running VMware or [[Microsoft]] Hyper-V for our virtualization in an on-premises data center, we could choose that option because remember, one of the primary use cases of Storage Gateway is to create a bridge from a local data center to the cloud. For the purposes of this demo, I don't have a local data center. I'm just going to be connecting from within AWS, so I'm going to choose [[Amazon EC2]]. Below you see that we've got a button that says Launch instance, and an icon that indicates this will open in another tab, and that's exactly what we'll do. We'll go off in another tab and we'll follow the wizard to create an EC2 that's suitable for the File Gateway. But before we do, let's take a look at some of the instructions below here. We're told that instance type needs to be at least m5.xlarge. Now, that's definitely a more expensive instance type
>
> **[3:06](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/configure-file-gateway-and-shares?u=76281980&t=186)** than we've used before in this course so make sure to watch the tear down at the end of this section to make sure you're not charged any more than you expect. We also need to make sure the instance has a public IP and we need to make sure that it can be accessed through port 2049, which is NFS. We actually already have a security group that allows this from a previous part of the course, so we'll just use that. Finally, pay attention to this section, which specifies that you need to add an extra volume for the cache on this File Gateway with a size of at least 150 gigabytes. AWS recommends we have at least 150 as the basis for the caching disk. They also devote an entire section of the Storage Gateway documentation to the subject of caching. If you search for managing local disks for your AWS Storage Gateway, you can find a lot of information on how to tailor disk cache size to your particular use case. All right, let's click launch instance. We'll be taken to the EC2 wizard where we'll give our instance a name. I'll call it FileGatewayDemoHost. Scroll down and you'll see the AMI has been pre-selected for us. This is the image that Amazon maintains that has the File Gateway software already on it. Keep on scrolling down. Under instance type, we can type in the search bar m5.x to get the m5.xlarge that we need. Select that and move on down. For key pair, we'll do something we haven't done before, which is choose proceed without a key pair.
>
> **[4:39](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/configure-file-gateway-and-shares?u=76281980&t=279)** We will not need to SSH this instance as it's fully managed by Amazon. Under network settings, you can see that auto-assigned public IP is set to enable, so that's good. And here under firewall, click Select existing security group. We're going to choose two. We'll take AllowHTTPFromTheWorld and NFSFromDefaultVPC, which we created earlier. That's been collapsed here under this plus button. So if we click that, we can see that both are applied. Head on down, and here we need to click add new volume under configure storage, and we'll give this 150 gigabytes. That will be our caching volume. All right, with that, we can go ahead and click launch instance. Click the instance ID to see the ongoing creation of the instance, and we can see that this is already in the running state, but we don't want to move on until status checks have passed from initializing to passed. Okay, now that both status checks are passed, we're ready to proceed. Click the checkbox here to see the details of the instance and click this button to copy the public IP address. Now we'll head back to the AWS Storage Gateway. This tab is where we'll continue to configure the instance. Under confirm set up gateway, click I completed all the steps, and click Next. Here we'll choose if this is a publicly accessible endpoint
>
> **[6:13](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/configure-file-gateway-and-shares?u=76281980&t=373)** and its connection type will be IP. Here we're going to paste the IP address that we just generated. Click next. All right, review what we've done so far. Then click next. Now, Amazon is connecting to this instance to complete the setup. We need to wait for it to look for the local disk so we can decide which one is going to be the cache. So when this loads up, we'll select the 150 gigabyte size disk to be the cache and move on. Okay, Amazon has interrogated our disks and has actually figured out which one is the non-root disk, which is the 150 gigabyte, and it's already set to be allocated to cache, so that's all good. That's exactly what we want, and we can move on. Here under CloudWatch log Group, we will simplify things by deactivating logging. We'll say no alarm to CloudWatch. These are all things that you would want to set up in a production instance, of course, but not necessary for the demo. We can also skip tags and then click configure. Something seems to have gone wrong there but it did not recognize that we selected the cache so I reselected it, refreshed it, and reselect it and I'll click configure. Okay, for whatever reason, that seemed to work a second time. Now the gateway is coming up so we've successfully created the Storage Gateway. However, we can't connect to it until we first create an NFS file share, which we'll do in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (3), [[Microsoft]] (1), [[Amazon EC2]] (1)
> **Env Vars:** aws (7), ec2 (4), http (2), nfs (2), fsx (1)
> **CLI Commands:** aws (7), make (4), find (1), ssh (1)
> **Prerequisites:** configure (4), set up (2), setup (1)
> **Ports:** port 80 (4), port 2049 (1)
> **UI Navigation:** go to (1), scroll down (1), checkbox (1), select the (1)
> **Cross-References:** coming up (1), in the next (1)
> **Definitions:** is a  (1)

#### [Connect to File Gateway via NFS](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/connect-to-file-gateway-via-nfs?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/connect-to-file-gateway-via-nfs?u=76281980&t=0)** - [Instructor] Now that the File Gateway has successfully been created we need to do one more thing, create a file share on it. This will create the NFS endpoint to which our [[Amazon EC2|EC2]] instances will connect. It's also the step where we'll tell AWS to use our S3 bucket as the storage backend. Remember though that buckets are associated at the file share level, not with the gateway. A single gateway can provide a file system interface to multiple buckets for different purposes. Let's check the checkbox next to File Gateway and click Create file share. Because we check that checkbox the first dropdown option is pre-selected for us. Under Amazon S3 location we'll go with the default which allows us to connect directly to the bucket without any indirection. Going down a bit we'll paste in the bucket name from the previous lesson. Here we have the option to give a prefix. This would allow us to place all the files saved through this particular share into a subfolder paving the way to reuse this bucket for other purposes, using other prefixes in the future. For this demo, we'll leave it blank. AWS region should match where the S3 bucket is. Our bucket is in US East 1, so that works. File share name, we can edit slightly. I'm going to call this-file-gateway-share, with the initials. And everything else on this screen can stay at default. Click Next. On this screen we're asked to choose a storage class for the objects. Remember, the backend storage is S3 so it makes sense that we would have the option to choose S3 Standard, Intelligent Tiering,
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/connect-to-file-gateway-via-nfs?u=76281980&t=94)** One Zone, Infrequent Access, things like that. For now, we'll keep this at S3 Standard, but it's good to know that you have this option. Encryption, we'll go with S3 Managed Keys and we'll click Next. On this screen we have Access Object. This is an option that lets you specify an IP or a CIDR block to which to restrict file system access. Unlike a security group, this is enforced at the file share level which is why it's part of this configuration wizard. We'll remove this entry for now and skip this step allowing any user with network access to reach this file share. You can see some other options here such as Root Squash, which prevents remote root users from logging in as root on this system. It's a good idea to keep that at default. Permission bits are how the file system will treat files and folders as they're created. We'll keep those at default and click Next. Okay, now we're on the Review page. Scroll all the way down and click Create. At this point we need to give the share a few moments to be created. Right now it's in the updating status. When it's done, we'll be able to mount it to an EC2 instance. Okay, now that the share has become available let's click into the ID and scroll down past the details. Down here we see some mount commands that will become useful in a moment. I'm going to copy this first one for [[Linux]]. Over here on the terminal, I've logged into one of the EC2 instances from earlier in the course.
>
> **[3:07](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/connect-to-file-gateway-via-nfs?u=76281980&t=187)** If you didn't keep one around, that's okay. As long as you have an instance in the same VPC as the Gateway, the security group we used will allow it to mount our file share. Let's start by creating a mount point. I'll say, sudo make directory, and we'll call it file-gateway. Now, before I paste in the command from the previous page I want to prefix that with sudo again, paste, and then backspace over this mount path and give it the path that we just created. Okay, with that, we can do the df -h command and see our mount right down here. In fact, you can even see the name of the S3 bucket that backs up the storage. Much like with EFS we see storage space looks extremely large, like eight exabytes, and if we take off the -h, we'll see an even larger number that struggles to fit on the screen, honestly. All right, so now we can move into the File Gateway folder. And we can see that we have mounted the file system. Let's create something. We'll echo a little message here into hello.txt. And we can see that that file has been created here in the file system. And if we go back to AWS now we can go to the S3 section and take a look in our bucket. Now you'll recall from earlier that we shouldn't touch any of the backend storage but we do want to see what's going on here. We'll go into file-gateway-demo. And there you see it, our hello.txt.
>
> **[4:41](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/connect-to-file-gateway-via-nfs?u=76281980&t=281)** And you can clearly see now that we have mounted an S3 bucket as an NFS file share using File Gateway.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (3), [[Linux]] (1)
> **Env Vars:** ec2 (3), aws (3), nfs (2), cidr (1), vpc (1)
> **CLI Commands:** aws (3), sudo (2), make (1)
> **UI Navigation:** checkbox (2), dropdown (1), scroll down (1), go to (1)
> **File Paths:** hello.txt (2)
> **Cross-References:** earlier in (1), go back to (1)
> **Tools:** terminal (1)
> **Definitions:** is an  (1)

#### [File Gateway teardown](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/file-gateway-teardown?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/file-gateway-teardown?u=76281980&t=0)** - [Narrator] Okay. As with every other section of this course, we're now going to go through and clean up all the resources that we created so that you won't have any unexpected charges on your AWS bill. The first thing we'll do is go into the [[Amazon EC2|EC2]] section. I'm going to delete the EBS demo one instance that we use to mount the file system. I'll highlight it, go up to instant state and choose terminate. We'll need to confirm that and now that's in the terminating state. Okay, before we delete the file gateway instance we need to head back to storage gateway. Click storage gateway. Highlight the file gateway. Go to actions and choose delete gateway. We're getting a warning here. As we do throughout AWS, we'll type the [[Microsoft Word|word]] delete in this case to confirm and click delete. AWS is asking us for some feedback here which is something that doesn't show up all the time. We can choose other here and not giving them any details and just click submit. Okay, our gateway is deleted. Now we can head back to EC2 and delete the instance that's associated with it. Go into running instances, highlight the file gateway. Go up to instant state and choose Terminate instance. This is an important step because you want to make sure that M5 dot X large machine is not running any longer than you need it to be. Finally, let's clean up the last bit
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/file-gateway-teardown?u=76281980&t=94)** by going to S3 and deleting our bucket. In this case, we're looking for the file gateway demo. I'll highlight that and click delete but we'll be stopped here because the bucket is not empty. We'll get this warning here. Buckets must be empty and there's a link for the empty bucket configuration. Follow that link and type the words permanently delete. Click empty and that will remove all the objects from the bucket. Now, if that's done, we can click exit, and once again highlight and choose delete, willing to type or paste in the name of the bucket to confirm and click delete bucket. And with that, we have cleaned up all the resources from the storage gateway section of the course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (2), [[Microsoft Word|Word]] (1)
> **Env Vars:** aws (3), ec2 (2), ebs (1)
> **CLI Commands:** aws (3), make (1)
> **Warnings:** warning (2)
> **UI Navigation:** go to (1)
> **Definitions:** is an  (1)
> **Speakers:** - [narrator] (1)

#### [Transfer data with AWS Snowball](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/transfer-data-with-aws-snowball?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/transfer-data-with-aws-snowball?u=76281980&t=0)** - [Instructor] When it comes to transferring data to the cloud, especially in very large quantities, latency and bandwidth restrictions are a constant concern. There are many points at which our throughput might be limited by the speed of our connection or bottlenecked by low bandwidth network ops. We can alleviate this somewhat by using a technology like AWS Direct Connect, but at a certain size of data load, the bandwidth of the internet just isn't going to cut it. We'd almost be better off loading our data onto a truck and driving it to AWS. In fact, this is true. Given sufficient data quantity and demanding enough time constraints, physically moving hard drives of data is more efficient than moving that same data over the internet. Back in 1981, American Dutch computer scientist Andrew Tanenbaum put it thusly. "Never underestimate the bandwidth of a station wagon full of tapes hurdling down the highway." For an interesting dive into the implications of this statement, take a look at XKCD author Randall Monroe's what-if article on FedEx bandwidth. For just these sorts of situations, AWS provides AWS Snowball. Snowball is a ruggedized device that Amazon ships to your door via, what else, Amazon Prime. Snowball provides a way to get large quantities of data in and out of S3. You can receive data from an S3 bucket in the mail or create an export job wherein you'll load data onto a device and ship it to AWS. To get started, you'll log into the AWS console and create what's called a Snowball job. A job consists of a few things. For example, for an import job,
>
> **[1:33](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/transfer-data-with-aws-snowball?u=76281980&t=93)** you'll specify a shipping address, a destination S3 bucket, and an optional Lambda. This Lambda will be executed for every S3 put performed by the Snowball import job. You can use this to kick off all kinds of processing related to your data import. You'll also select a size for your Snowball. The device can be ordered in 50, 80, and 100 terabyte variants. The 100 terabyte size is exclusive to a Snowball variant called Snowball Edge. Snowball Edge is a true edge computing device. Rather than just storage, it allows you to cluster multiple devices together and actually run compute on them via Lambda functions. So you can not only transfer data, but you can perform work. For this series, we'll focus on the normal 50 or 80 terabyte Snowball transfer device. How about security? If you're going to send terabytes of valuable data through the mail, you want to know it's safe. First of all, Snowball requires that you encrypt the data to be transferred. You must select an 80 risk KMS key management service key when you create your job. This key will be used to encrypt the data while it's still on your own device. Then the encrypted data moves to the Snowball, never even traveling with the key. When it arrives in S3, the same KMS key can be used to decrypt. Second, the enclosure itself is tamper-proof and equipped with something called a trusted platform module that can alert AWS to any unauthorized changes to the [[Hardware]]. After your job is complete, AWS erases your data according to the [[NIST]] standard for media sanitation.
>
> **[3:10](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/transfer-data-with-aws-snowball?u=76281980&t=190)** Finally, you can configure SNS notifications for events that occur during the job's lifecycle. This way, you can get text messages or emails for things like job created, Snowball shipped, Snowball delivered, Snowball back at AWS, job completed, or job canceled. The Snowball will arrive at your location via one or two day shipping, your choice. When it does, you'll connect the device to your network and install the Snowball S3 adapter software on your computer. The software comes for [[Windows]], Mac, and [[Linux]], and provides an S3 compatible endpoint for you to transfer data locally. Remember, you'll have to specify in advance the target S3 bucket and the KMS encryption key that will be used. When you're done, the Kindle-esc e-Ink label on the device updates to the facility that you need to ship to and you simply ship the device back via your carrier of choice. When you get the SMS notification, you'll know your data is securely in S3. So all this is very cool, but when is Snowball really the best method for getting your data to the cloud? AWS has some rules of thumb in their published documentation. It's really a function of your available bandwidth, amount of data to transfer, and your time constraints. They give some examples in this chart. For instance, on a 100 megabit connection, 100 terabytes could take almost four months to transfer. That's assuming you don't run into any connection problems along the way. AWS recommends that if a data transfer over your connection is going to take longer than a week, you're a candidate for Snowball, which is only bound by the transit time of the delivery device.
>
> **[4:45](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/transfer-data-with-aws-snowball?u=76281980&t=285)** Now, if you have a persistent need to move large quantities of data to AWS from an on-premises data center, it would be cost-prohibitive to use Snowball over and over. You're going to want a bigger pipe. Although it's outside the scope of this course, I'd recommend that you do look at AWS Direct Connect, which offers AWS customers a way to tap into high bandwidth networking infrastructure that goes directly to AWS. Another option, of course, is to move your data-generating workloads to AWS. Migrating from a [[Hybrid Cloud]] to a full cloud native solution is definitely going to alleviate many data transfer headaches. There's actually another use case for Snowball, which these days is part of a larger set of devices called the Snow Family as shown here. You can see the specs on AWS's documentation, but they vary in both storage and compute capacity. Why compute? Because the Snow Family devices are not just used for shipping data, they're also used for edge computing. Edge computing refers to scenarios where you need a device that can collect and process data, but that must run in an environment where conditions or connectivity make it impractical to work directly in the cloud. Just one example might be a research team operating in a remote location, collecting data with sensors whose output must be processed and stored locally. This team can use a local Snow Cone or Snowball device to work with that data, even running an AMI directly on the device, and then ship it to S3 later to continue their work in the cloud. So whether you're looking at edge computing or a one-time large scale data transfer, take a look at AWS's family of Snow devices.
>
> **[6:20](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/transfer-data-with-aws-snowball?u=76281980&t=380)** Oh, and if you've got a really huge data set to move, we're talking exabytes, well, AWS has you covered there too. Meet AWS Snowmobile. It's a literal semi-truck, and if you ever end up needing to ship this much data, give it a try, and please take pictures.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hardware]] (1), [[NIST]] (1), [[Windows]] (1), [[Linux]] (1), [[Hybrid Cloud]] (1)
> **Env Vars:** aws (20), kms (3), xkcd (1), nist (1), sns (1)
> **CLI Commands:** aws (20), make (1)
> **Definitions:** is a  (2), refers to (1)
> **Analogies:** for example (1), for instance (1)
> **Prerequisites:** configure (1), install (1)
> **Tools:** aws console (1)
> **Speakers:** - [instructor] (1)

#### [AWS Database Migration Service](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-database-migration-service?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-database-migration-service?u=76281980&t=0)** - [Instructor] Chances are if you're interested in Cloud [[Data Storage]], you're not starting from scratch. You've already got a significant amount of data on-premises in your local data center or co-location and you're wondering how to get it to the Cloud. Maybe you've been running a relational database or two or three for years, and you're ready to let AWS take over the operational details by migrating to RDS. Perhaps you've been running [[MySQL]] and you're interested in the performance enhancements promised by Aurora or maybe you have some new architectural goals and just want to switch database engines. How do you get from here to there? Well, you have a number of do it yourself options. You can use native tools such as Oracle's data pump but often these tools are geared toward homogenous transfers, not for migrating to another platform. You could use your database's export tools to export data and schemas to [[SQL]] scripts and run them, but again changing engines would mean that you're responsible for translating the resulting output into a syntax suitable for your destination. The files could also be quite large at which point you're met with the challenges of getting them to the Cloud in the first place. Finally, you could export your data in some other format such as CSV, but then it's all on you to write import scripts that will work with your destination platform. That's where the database migration service comes in. AWS has essentially abstracted away the details of a number of existing migration tools and created a service that gives you a single interface for moving data from one database to another. DMS can do one time or continuous data migrations to and from a variety of relational
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-database-migration-service?u=76281980&t=94)** and [[NoSQL]] [[Databases]] and can even move data to S3. Let's take a look at how this works. The key component of a database migration service task is the replication instance. The replication instance is an [[Amazon EC2|EC2]] virtual machine provisioned by DMS. The host must be able to access both your source and target databases on the respective service ports. For example, port 5432 for [[PostgreSQL|Postgres]]. The replication instance runs migration tasks, moving relational data from your source database to the target. This process can be a one time event, or you can keep the instance and use it as a persistent replication engine. Just be aware that as long as the replication instance is running, you will incur the cost of that EC2 host, which varies based on the sizing you choose. The two sides of the migration can both be in AWS or they can be local, on-premises databases. The AWS side can be an RDS database, a database running on EC2, a NoSQL database like Mongo or Dynamo, or even S3. This means that you can set up a migration from a local DBMS to a database in AWS, giving you yet another way to move data to the Amazon Cloud. When you're ready to go Cloud native, you can just point your applications to the remote database, decommission your old one on-premises, and terminate the replication instance. Of course, if your target is RDS, you can easily set it up with read replica or multi AZ functionality to keep that high availability you get from having multiple copies of your database.
>
> **[3:09](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-database-migration-service?u=76281980&t=189)** The data stored on the replication instance itself is encrypted with an AWS key management service key of your choosing. It can be one auto generator for DMS or it can be one that you've created on your own. If you need your own SSL certificates, you can also encrypt the traffic to and from the source and target. The other great thing about database migration service is that it not only gives you a way to move data to AWS, it also makes it possible to migrate to a different database engine than the one you're currently using. This is called a heterogeneous migration. AWS provides documentation that makes it clear exactly what conversions are supported. This includes many transactional databases. For instance, you can migrate a [[Microsoft SQL Server|SQL server]] database to Aurora, MySQL, another SQL server or Postgres. Plus you can migrate many different data warehouse platforms into Redshift. AWS also provides a lot of specific detail on the use of each database platform as both a source and a target. Be sure to read the documentation carefully to make sure you understand the nuances of the migration that you're planning for DMS. To summarize, let's look at the key concepts behind database migration service. First, you'll create a replication instance to connect to both databases and execute the actual migration. Second, you'll establish endpoints which is just another way of saying you'll set up the connection information on both sides. Third, you'll create a migration task, which can be set up as either a one time or continuous migration. Finally, within the task, you can set up simple transformations such
>
> **[4:42](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-database-migration-service?u=76281980&t=282)** as filtering out certain tables or schemas. Let's head to the AWS console to try this all out.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Databases]] (5), [[Amazon EC2|Ec2]] (3), [[MySQL]] (2), [[NoSQL]] (2), [[PostgreSQL|Postgres]] (2)
> **Env Vars:** aws (10), dms (4), rds (3), sql (3), ec2 (3)
> **CLI Commands:** aws (10), mysql (2), mongo (1), az (1), make (1)
> **Analogies:** such as (2), for example (1), for instance (1)
> **Prerequisites:** set up (4)
> **Definitions:** is an  (1), means that (1), is called (1)
> **Ports:** port 5432 (1)
> **Documentation:** the documentation (1)

#### [DMS demo: Setup steps](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-setup-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-setup-steps?u=76281980&t=0)** - [Instructor] Okay, in this demo of the AWS Database Migration Service, we're going to use the existing [[MySQL]] RDS database that we created earlier in the course. If you're following along but you don't have that one anymore, feel free to refer back to chapter four. Build it again and pick up right here. What we're going to do is migrate from a MySQL database in RDS to a [[PostgreSQL|Postgres]] database in RDS, and we use Database Migration Service to do it. So that means we need to create the destination database. Before we go into RDS and take that step, we need to go into [[Amazon EC2|EC2]] and create a security group. Scroll down here on the side under the EC2 section to security groups, then click create security group. We'll call this PostgresFromWorld. In the description, something like allow Postgres traffic from anywhere. We'll create a rule. We'll use the dropdown to choose Postgres 'cause it's in the dropdown here, [[PostgreSQL]], port 5432. Our source will be anywhere IPv4. Now again, that's not how I would set up something in production. I would normally have the database in a private subnet and only allow world traffic through say to an application server that can talk to the database. But for our demo, that will work just fine. So click create security group. Okay, now we can type RDS to go into that section of Amazon and we should see in here our preexisting MySQL RDS demo,
>
> **[1:40](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-setup-steps?u=76281980&t=100)** once we click on DB instances. So we've got our source system here still running. Now we're going to go create database and we'll get our target system going. All right, we'll stick with Standard Create and we'll choose Postgres under database engine. For this, I'm going to choose a known good copy here. One that I know works with this demo, 13.4-R1. It's entirely likely the newer ones will work for you as you look at this in the future, but that's what I'm sticking with for now. I'm going to go to the free tier, just like we did with the MySQL, and I'll start providing some of the identifiers here. So we will match the convention that we used with MySQL and we'll call this psql-rds-demo for the database instance identifier and give the username as psql_rds_user. I'll provide a password. Hopefully you still remember the one you used for MySQL and we'll continue on down. Database class of db.ts.micro is just fine. And here under storage, we can leave all those at default. Moving on down to connectivity, we want the default VPC and the default VPC subnet group here. You need to make a change. Public access set to yes. And then under choose existing security group, we'll hit the dropdown here
>
> **[3:13](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-setup-steps?u=76281980&t=193)** and choose our PostgresFromWorld rule. We can also X out the default security group that was chosen for us. We don't have to worry about availability zone and we can scroll all the way down, leaving everything else at default and click create database. Now, while our target is being created, there is one important step that we can take on the MySQL database. As I said, RDS takes care of the provisioning and creating of your database whenever you stand up a new instance. And that means a lot of the configuration parameters that you would normally have to configure on your own are taken care of by RDS. But what if you need to override them? We'll find out momentarily that when we want to set up continuous replication using Database Migration Service that the default parameters included in the MySQL parameters aren't good enough for that. We need to make a kind of change. So we'll click here into mysql-rds-demo. Click on the configuration tab and scroll down a bit. You'll see parameter group, default type, MySQL 8.0. Click it and we'll be whisked away into another part of the RDS console dealing with parameter groups as you can see on the side here. This is a list of all of the key value pairs that you as a MySQL administrator can modify to change the behavior of the database. It's a long list and you can see that AWS has helpfully annotated what they do over here on the side. Now, there are two parameters that we're going to need to change to make continuous replication work for us
>
> **[4:47](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-setup-steps?u=76281980&t=287)** and you might be tempted to click edit parameters but this is not actually going to be effective for us because this is a default pre-built parameter set. And if we try to save that, we'll get an error. So cancel out of that. Instead, go to parameter groups from the breadcrumb here and click Create parameter group. The first thing we're asked is what parameter group to base this on. So we'll build off the MySQL 8.0 set that's pre-selected here, and we'll give our group a name. We'll call it mysql80 so we remember where it's based off of, _replication_group. And this will be description of parameters suitable for continuous replication. Click create. No, our group name needs to contain only letters, digits or hyphens, so those underscores will not work. We'll have to change those to hyphens. Click Create. Now we have a new group right here that we can click into. Exactly the same as the default but now we can edit it. Click edit parameters. All right, the two parameters that we need to change are as follows. Type up here in the search B-I-N-L-O-G, binlog_format. This is defaulted to the value mixed. We need to click the dropdown and change that to row. Next, in the filter, type log_bin_trust.
>
> **[6:23](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-setup-steps?u=76281980&t=383)** And that should be enough to reveal logbin_trust_function_creators. We're going to set this to one. That's not strictly necessary for our replication, but it does allow replication to copy over things like functions and procedures. So we want that to be at the one value. All right, now click save changes. At this point, our parameter group is saved and all we need to do now is apply it to our MySQL database. So go back and click [[Databases]]. Click mysql_rds_demo and go up to modify. On this page, we need to scroll down to where we can change the parameter group. It's down here under additional configuration. Hit the dropdown there and you see the only other MySQL parameter group, which is ours, the replication group. Click that and scroll all the way to the bottom and click Continue. We'll see a summary of our changes. It should be just the parameter group and we have an option: apply during the next maintenance window or the one we want, apply immediately. Click apply immediately and modify DB instance. Now, the MySQL instance will go into a modifying state, which you can monitor by clicking into mysql-rds-demo, going back to configuration and watching the parameter group. This is applying. You can continue to refresh the page until this is done and then move on to the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (16), [[PostgreSQL|Postgres]] (4), [[Amazon EC2|Ec2]] (2), [[PostgreSQL]] (1), [[Databases]] (1)
> **CLI Commands:** mysql (16), make (3), aws (2), psql (1), find (1)
> **Env Vars:** rds (9), aws (2), ec2 (2), vpc (2)
> **UI Navigation:** dropdown (5), scroll down (3), click on (2), go to (2)
> **Code Identifiers:** psql_rds_user (1), binlog_format (1), log_bin_trust (1), logbin_trust_function_creators (1), mysql_rds_demo (1)
> **Versions:** 8.0 (2), 13.4 (1)
> **Prerequisites:** set up (2), configure (1)
> **Cross-References:** earlier in (1), next video (1)

#### [DMS demo: Create replication instance](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-create-replication-instance?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-create-replication-instance?u=76281980&t=0)** - [Instructor] All right, continuing from the last video, we have just applied our custom parameter group to the [[MySQL]] database in RDS, and we were waiting for it to go from the applying state to complete. And what we found is that instead the status has changed to Pending reboot. So some database configuration changes do require a reboot and this it turns out, is one of them. So what we're going to do is we're going to scroll up, we're going to go to actions, still under MySQL RDS and click Reboot, click Confirm and that'll start the RDS instance rebooting. The next thing that we want to do in this demo is establish a connection between the Database Migration Service and both of the [[Databases]]. So we'll need to make sure that this is in the available state before we do that. Now, I do think that we're going to have time to type in Database Migration Service here at the top. Click into that service and start up a few things while this database becomes available again. Because the first thing that we need to do here in Database Migration Service is create replication instance. Let's click that button. This is going to create a host that will act as the go-between, between these two databases. In this example, both of our databases are in AWS but they're publicly accessible. And the way this instance is going to reach them both is through the public internet, through those public IPs that they expose to the world. Now, in your own institution
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-create-replication-instance?u=76281980&t=94)** or organization you might have this database instance reach out to private IPs of both of them because maybe they're not world exposed or maybe one of them is on premises and you are talking to it through the public internet and talking to an RDS database in AWS through its private interface. There are a lot of ways that you could do this, but for our purposes, everything's going to be talking through the public internet. So, we're going to create this replication instance and we'll call it 'Demoreplicationinstance', something very simple. We'll skip the friendly name, give a short description, 'A replication demo', and we can leave the instance class at default along with engine version. We don't need multi AZ so we can choose dev or test workload and we can choose for VPC. The only option here, which is our default VPC. So we can have this be publicly accessible but the instance itself does not actually need to be public. We're not going to SSH to it or anything like that, we're going to configure it from here in the AWS console. So it's going to be able to talk out to the public internet so it can talk to both of these databases, but in itself, we can uncheck that so it's not publicly accessible. Everything else can stay at default, click Create.
>
> **[2:59](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-create-replication-instance?u=76281980&t=179)** - Okay. Demo replication instance is being built there. You can see it's in the creating state. You can also see that it got rid of all my capitalization. So all those woRDS just run together in the name, but that's okay, it's still recognizable. What we need to do at this point is create endpoints. So before I do that, I want to make sure that the RDS database is back online. So I'm going to come up here, I'm going to type RDS and without clicking it, I'm going to right click on RDS and open that in a new tab so we can keep an eye on it. I'll click into my second tab here and just check the status of that MySQL by clicking on DB Instances and it looks like it's back available, so we're good to go. Click on the first tab. Back here in the Database Migration Service, let's click Endpoints. We're going to tell DMS how to connect to MySQL and to [[PostgreSQL|Postgres]]. So let's create an endpoint. This will be our source endpoint, which is going to be the MySQL. So we'll keep that selected. And it says, 'Select RDS instance'. Nice little convenient thing here. We're going to choose 'mysql-rds-demo'. Okay, by doing that, it has pre-filled a few things for us which makes things easier. It's got the endpoint identifier, it's got the source engine and now it wants to know how to access the database. So instead of using AWS Secrets Manager, we are just going to type in our username and password. So click 'Provide access information manually'. Server name is pre-filled here, again,
>
> **[4:32](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-create-replication-instance?u=76281980&t=272)** which is very nice, as is the username. So all we need to do is remember our password that we set. We can keep everything else for this demo at default, and click Create Endpoint. The endpoint is created, but we do want to test it. So we're going to check this check box. We're going to go up to Actions and we're going to click Test Connection. It's going to use our demo replication instance. So before we do this, let's go back to replication instances and make sure that that thing is up and running. It's still in the creating state so I'm going to let this come available before we move on. Okay, now, if that's up and running, we can go back to Endpoints, check the checkbox next to 'mysql-rds-demo'. Go to Actions and choose Test Connection. Click 'Run test', and we'll wait to see whether the replication instance can reach our MySQL RDS. Okay, now, if that says successful, let's go back to Endpoints and we'll create our Target. Click Create Endpoint. This time instead of Source we'll choose Target Endpoint. And again, select RDS DB instance. Choose the Postgres RDS from the dropdown and continue on down below. Change this radio button to provide access information manually. A lot of it's prefilled for us. And type the password from your Postgres database. Under the database name, in this case, unlike with MySQL
>
> **[6:05](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-create-replication-instance?u=76281980&t=365)** we never specified a default database name. We use the one that we know Postgres creates which should be lowercase Postgres, P-O-S-T-G-R-E-S. Scroll all the way down and click Create Endpoint. Now, once again, I'm going to click the checkbox here, go to Actions, and then choose Test connection. Click Run test. And now we've got a successful connection there too. So in the next video, we'll use this pipeline from source to replication instance to target, to sync some data from MySQL to Postgres.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (10), [[PostgreSQL|Postgres]] (6), [[Databases]] (4)
> **CLI Commands:** mysql (10), aws (4), make (3), az (1), ssh (1)
> **Env Vars:** rds (11), aws (4), vpc (2), ssh (1), dms (1)
> **UI Navigation:** go to (3), click on (2), checkbox (2), scroll up (1), dropdown (1)
> **Cross-References:** go back to (3), in the next (1)
> **Speakers:** - [instructor] (1), - okay (1)
> **Code Identifiers:** words (1)
> **Tools:** aws console (1)

#### [DMS demo: Create and execute task](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-create-and-execute-task?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-create-and-execute-task?u=76281980&t=0)** - [Instructor] All right, now that we've got both sides of this connection working, let's head into database migration service and set up the task that will move data from our [[MySQL]] to our [[PostgreSQL|Postgres]]. Click on create database migration task. We'll give a unique identifier here of MySQLToPostgres. Under replication instance we'll choose the one that we created earlier. Scroll down and choose our source database endpoint of mysql-rds-demo and the target of psql-rds-demo. For migration type, we have some choices. We've got migrate existing data which is a one-time migration or migrate existing and replicate ongoing, which is what we want to do. We're given a warning here about the configuration that we have to do through MySQL database to make it work. And we did these changes already to the parameter set earlier in the course, so we're good to go. Scrolling down to task settings and we can leave this all at the default. You can see some of the behavior that's defined including dropping tables on target. There's some settings here related to large binary objects so if your database contains that kind of data, you may want to pay attention to the choices here or click for more info. Scrolling down to table mappings, this is where we'll tell DMS exactly what to move from the source to the target. Click add new selection rule. We'll click choose schema and go to enter schema. That creates a new input here. The percent is a wild card, which means take everything in the database and which is what we want to do. And source table name is also a wild card.
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-create-and-execute-task?u=76281980&t=95)** If we wanted, we could erase this and put in a specific table name like employees but we want everything, so we're going to keep the percent. We also have the action to include or exclude, so you can see how you can start to build some sophisticated rules here that perhaps take the whole database, but exclude just a certain schema or a certain couple of tables, things like that. So we'll keep that as is with the two percents and include. Scroll down and we'll look here under task migration startup, we can choose to automatically start the task on create instead of manually running it later. So we'll keep that as is and click create task. Now at this point, the task is creating and soon it will go into the running state and we ought to see the results of the job. Okay, now the migration task has changed from ready to starting to now load complete, replication ongoing which is what we want. So let's click into the MySQLToPostgres task name and we can click on the tab that says table [[Statistics]] to see what's happened. Now you can tell right away that putting the wild card in for the schema might have taken things a little further than we meant to because not only did we get our schema that we created adhoc in the MySQL database, we also got all of the sort of built in administrative schemas. So you see mysql, performance_schema, I really don't need all of that. And if we were to redo this demo we would specify just the one schema called my_database which we created earlier in the course.
>
> **[3:08](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-create-and-execute-task?u=76281980&t=188)** So when you filter by that you can see all of the tables that we specified in the [[SQL]] that you loaded in, employees, departments, projects, all that. They have been moved. And so now we can take a look at the Postgres database and see what the result looks like on that side. So over here I have installed PG admin, the client to connect to Postgres [[Databases]] and established a connection to that RDS database. So you can see I'm already connected here and if I expand databases, I've got Postgres here. And if I expand schemas, you see all the ones that we just saw moved over including my_database, which I can expand and I can see the tables here, all those that we just saw in the dashboard. If I go to tools and do query tool, I can type in some SQL. So I can say select * from my_database.employees and I can see that the data has indeed come over. So our migration was fully successful but this is also supposed to be an ongoing replication. So let's head back to MySQL Workbench where we are logged in to the source MySQL. And we're going to do one more thing here. We're going to add a new employee. This row right here, Scott Summers, values are 1002 for the primary key. First name, last name, and department one. I'm going to run that as well as the commit which inserts one row over here and now back in the Postgres database,
>
> **[4:42](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-demo-create-and-execute-task?u=76281980&t=282)** I'm going to see if I get a 15th row here. And as you can see, that is some really quick replication by the database migration service between the source, MySQL database and the target Postgres database.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (8), [[PostgreSQL|Postgres]] (6), [[SQL]] (2), [[Databases]] (2), [[Statistics]] (1)
> **CLI Commands:** mysql (8), psql (1), make (1)
> **UI Navigation:** click on (2), scroll down (2), go to (2)
> **Code Identifiers:** my_database (3), performance_schema (1)
> **Env Vars:** sql (2), dms (1), rds (1)
> **Cross-References:** earlier in (2)
> **Definitions:** is a  (2)
> **Warnings:** warning (1)

#### [DMS teardown](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-teardown?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-teardown?u=76281980&t=0)** - [Instructor] Okay, wrapping up from the previous video we're looking at our database migration task and it's time to clean things up. That way we won't see any unexpected charges on our monthly AWS bill. So we're still here looking at the task, the [[MySQL]] to [[PostgreSQL|Postgres]] task, and the first thing we want to do is go up to Actions and choose Stop. That'll stop the replication from running. And if you refresh, you see it goes into a stopping phase and after a moment it will be stopped. Okay, now we can go to Replication Instances and delete this replication instance. These are like [[Amazon EC2|EC2]] instances but they don't show up in the EC2 section of AWS. So you got to make sure to delete it from the database migration service. Check the checkbox, go up to Actions, and choose Delete. Click the button to confirm and we see that we have failed to delete. So this may mean that we still have some tasks running, something that's keeping this thing from being deleted. Let's have a look here. We'll look at Database migration tasks. This one is stopped, but I believe we need to delete it. So let's check that check box. There we go. Go to Delete, delete that. And when that's gone we should be able to go take care of the instance. Okay, the task is gone so let's head back to Replication instances. Check the checkbox again, Actions, and then Delete. Confirm with a button.
>
> **[1:32](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/dms-teardown?u=76281980&t=92)** And now we've got deletion in progress, so that's good. We've still got our end points here. These will not incur any costs. They're just definitions, but you can delete them if you want to by checking both of them, going to Actions, and choosing Delete. Now, the things that we do still have around that will incur cost are over in RDS. Type RDS. Choose the first option here and I'm going to start with our Postgres database. So I'll click DB instances. Check the psql-rds-demo. Go up to Actions and choose Delete. I do not want a final snapshot. Keeping a snapshot around would incur some charges and I don't really care about the data that's in this database, so I'll uncheck that and I will uncheck Retain automated backups which causes a new check box to appear that says "I acknowledge that I will lose all this data," and that is just fine. I'll type, "delete me" to confirm and click Delete. Now the Postgres database is deleting itself and that will be taken care of in just a few minutes. Now, if you'd like, you can go ahead and do the same to the MySQL database, but if you're following along this course, there is still a use for it yet, so you can either keep it around or perhaps even put it to sleep for a little while. But if you're all done, go ahead and delete it. Otherwise keep it and you'll see it again soon.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PostgreSQL|Postgres]] (3), [[MySQL]] (2), [[Amazon EC2|Ec2]] (2)
> **CLI Commands:** aws (2), mysql (2), make (1), psql (1)
> **Env Vars:** aws (2), ec2 (2), rds (2)
> **UI Navigation:** go to (2), checkbox (2)
> **Cross-References:** previous video (1)
> **Best Practices:** make sure to (1)
> **Speakers:** - [instructor] (1)

#### [AWS SFTP service](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-service?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-service?u=76281980&t=0)** - [Instructor] Even with all the great cloud native AWS storage and transfer options we've discussed in this course, you're still going to run into systems, be it with a vendor or in your own backyard that rely on SFTP. AWS transfer for SFTP is a service that acknowledges that SFTP processes still make a lot of the world go round, but provides a way to make those workloads easier to manage. Many IT organizations have plenty of processes that look like this. A vendor generates a daily batch file of data updates, which it sends to an SFTP endpoint owned by the organization. The organization must then implement scheduled jobs, which monitor the SFTP system for new files and processes. In this example, incoming data is parsed and stored in a relational database. There's a definite maintenance overhead to this process. The SFTP endpoint, probably a [[Linux]] server, must be patched regularly and hardened for security. It is after all world exposed so that the vendor can reach it. For each additional job that mirrors this pattern, say another vendor integration, the receiver must either create another SFTP endpoint or configure the existing one to accept another incoming connection. Often this means creating a unique login for each vendor, along with unique destination directories for their files. After all, you don't want different companies sharing the same password or file space, potentially collaborating each other's data or accidentally exposing information. Then there's the question of availability.
>
> **[1:36](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-service?u=76281980&t=96)** Creating a horizontally scalable SFTP endpoint is likely more trouble than you'd prefer to take on for something like this. So instead, you have a single point of failure, the host or instance on which the SFTP service runs. If it goes down, the whole pipeline stops. Finally, consider durability. This SFTP service may be using some remote storage, such as an NFS mount, but it's often just using its own local drive. If it's an [[Amazon EC2|EC2]] instance, perhaps EBS storage. As with availability, the cost and complexity of building the system for high durability is significant, so a failure not only means the pipeline is down, it may mean you've lost data. These are the challenges that AWS transfer for SFTP is meant to solve. With this service, you can spin up managed SFTP endpoints backed by S3 storage. Combining a managed service with an S3 backend means the resulting system is highly available, auto scaled, and extremely durable. In fact, the setup of AWS transfer for SFTP is so closely tied with S3, that's rather useful to think of yourself as configuring an SFTP access point to one or more S3 buckets. You start by creating a bucket and an IM role with an attached policy that can access it. If you wish to restrict users to a certain folder path in the bucket, say because you're going to share this bucket as the backend for multiple vendors, you can go ahead and create that folder. Your role policy can be tailored to allow only this path. An AWS transfer for SFTP can be configured
>
> **[3:11](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-service?u=76281980&t=191)** to make sure that's where the user's files go. We then create an SFTP endpoint and add a user to it. When we add the user, we associate the user with a bucket, a roll for the bucket, and optionally a folder path. Then when the user connects to the SFTP endpoint using an SSH key, they're actually interacting with that bucket via the roll through SFTP. If you add another user to the SFTP endpoint, you can have that person/account use another folder on the same bucket, or you can actually use an entirely different bucket. This leads to some interesting potential [[Design Patterns]]. You can support many different use cases with just one endpoint. Then use bucket and bucket policies to lock things down appropriately. This team of users here can access the endpoint and have their data routed to individual folders on their team bucket. While these cloud vendors get accounts that map to their own distinct buckets, securely separated from the others not only by their own IM policies, but by the boundary of the bucket itself. So, AWS transfer for SFTP allows you to take advantage of S3 in some new and interesting ways. Let's go into the console and see how to build one.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Linux]] (1), [[Amazon EC2|Ec2]] (1), [[Design Patterns]] (1)
> **Env Vars:** sftp (20), aws (6), nfs (1), ec2 (1), ebs (1)
> **CLI Commands:** aws (6), make (3), ssh (1)
> **Prerequisites:** configure (1), setup (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [AWS SFTP prerequisites](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-prerequisites?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-prerequisites?u=76281980&t=0)** - [Instructor] Okay. Our goal is to create an SFTP endpoint using AWS Transfer for SFTP. Before we can do that, we need a few other resources first, starting with the S3 bucket that will serve as the backend storage. Let's go to the search bar, type S3, and then click into that service. On the next page, choose Create Bucket. Go ahead and click that and we'll give the bucket a name. I'll call it Vendor Dropbox Demo and then append my initials to ensure uniqueness. Head on down. We will block public access. We'll leave that option at default. Keep going. Leave everything else as is and click Create Bucket. Okay, now I've got the Vendor Dropbox bucket. I'll click into it. And I'm going to click the Create Folder button. We'll just call this My Folder. And the purpose of this is to be the destination for all the files that will be dropped here by the SFTP user. That way, if we ever wanted to create a different SFTP endpoint using this same bucket we could route those files to a different subfolder and have separation between the two. Okay. The last thing that I'm going to do before we leave this bucket is go to properties and copy the ARN, the resource name of this bucket. This button here will copy the ARN to my clipboard. Okay.
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-prerequisites?u=76281980&t=94)** Now, the next thing that we need to do is head into the IAM section because we need to create a policy that will allow our SFTP user that we're going to create the ability to work with this bucket. Let's go up to the search bar and type IAM for Identity and Access Management. Click the first option there. And we're going to create a policy and then assign that policy to a role. Policies are definitions that grant certain permissions and once you assign those to roles, roles can be assigned to users or services. So, the policy defines what someone can do. It's loaded up into a role, and the role is assigned to a user or service. Click Policies. Okay, we're going to create policy. On this screen, we have a wizard that will walk us through the steps of creating a policy, or we can click this tab and edit [[JSON]] directly. Before we do anything here, click your [[Cursor]] into the JSON, create a few lines here, and paste that ARN of the S3 bucket. That way we've got it handy in just a moment. Now over here on the terminal, I've opened up one of our exercise files. This is in the Chapter 5 subfolder and it's called sftp_demo_policy.json. Copy that whole thing from bracket to bracket. Head back to the editor, delete the existing JSON and paste this in.
>
> **[3:07](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-prerequisites?u=76281980&t=187)** Let's take a look at what's going on here. This JSON defines an IAM policy and it has two statements. You see this here? We've got an array with two statements inside. The first one has an effect of allow which refers to two actions. GetBucketLocation from S3 and ListAllMyBuckets. It's applied to resources, AWS, S3, *, which means that the recipient of this policy will be able to do these two actions on every single bucket in the account. The second statement has the Sid AllowSpecificBucket. The Sid is a bit like a description. It has no actual impact as to the effect of the policy but it works a bit like a code comment. So, we can see here from the Sid that the intention is to allow privileges on one specific bucket, and you do see the effect is Allow and the action is S3*, meaning every possible S3 action will be applied to this resource. So, this is the part of the policy that will let our user do everything it needs to do in terms of writing to our bucket. So, now's the time to copy this ARN. Go up here, fully copy what's in between the two quotes here and overwrite it. We'll do the same on the next line. Making sure to leave the /* at the end so that this policy will allow writing
>
> **[4:39](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-prerequisites?u=76281980&t=279)** to any subfolder of this bucket. Finally, we want to delete the pasted ARN here at the bottom. Get rid of the white space. Check over our JSON to make sure we haven't left any unterminated double quotes or commas out and click Next Tags. Here, we don't need to create any tags, so we'll click Next Review. Let's give this policy a name. We'll call it SFTP Reader Policy. And in the description, we'll say, "Allows Access to our SFTP Backend Bucket." Click Create Policy. Okay. In the next step, we'll create a role which will be the vehicle for this policy. The role can be assigned again to a user or service. Click Create Role. Okay. Our trusted entity type will be an AWS service. In this case, it'll be the transfer service. So, we'll go down here to the dropdown and do a little type ahead, and we can choose Transfer. Click the checkbox next to that, and that's all we need to do on this screen. Click Next. Next, we want to attach the policy that we just created. So up here in the filter, we can start typing SFTP, hit Enter, and there you see it, SFTP Reader Policy. Check the checkbox next to it, then click next. Finally, we'll give the role a name.
>
> **[6:13](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-prerequisites?u=76281980&t=373)** We'll call it SFTPS3AccessRole. And for the description, we can leave what's there, but also say, "Allows SFTP Transfer to access our S3 backend bucket." Review everything below, then click Create Role. We've made our description too long or somehow otherwise violated what can go into the description here. So, I will just backspace over this description, leave what was there to begin with and click Create Role. Okay. Now we have the role, and in the next video, we'll go ahead and create the SFTP endpoint that will use all these pieces.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (6)
> **Env Vars:** sftp (11), arn (5), json (5), aws (3), iam (3)
> **UI Navigation:** go to (2), checkbox (2), dropdown (1)
> **CLI Commands:** aws (3), make (1)
> **Definitions:** refers to (1), means that (1), is a  (1)
> **Cross-References:** in the next (2)
> **File Paths:** sftp_demo_policy.json (1)
> **Code Identifiers:** sftp_demo_policy (1)

#### [Create an SFTP endpoint](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-sftp-endpoint?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-sftp-endpoint?u=76281980&t=0)** - [Instructor] Okay, in this video we're going to create the SFTP endpoint. Let's go up here to the search bar and type SFTP. AWS has expanded the transfer service to include more functionality, so it's now called AWS Transfer Family. Go ahead and click into there. And on this screen, we'll choose Create Server. SFTP is already populated for us, so we'll keep that as is. And you can see some of the other protocols that are now supported by the service. Click next. Our identity provider for this server will be service managed, basically meaning that we will be creating user accounts within the AWS transfer part of the console. This is not to be confused with your root account user or any IAM users that you might create. These users will only have access to the SFTP endpoint and nothing else in your account. If you want to choose one of the other options, you could bring your own identity provider, like Okta or Auth0. Click Next. Our endpoint, in this case, is going to be publicly accessible. That's easiest for the demo, but in a production instance, you might want to go with a VPC hosted and create something that is accessible through a private network interface. Custom host name, we won't worry about. We will just let the AWS transfer service create a domain name for us. Click next. Okay, our domain, the backend can be S3 or EFS. We're going with S3, so click next. CloudWatch logging.
>
> **[1:34](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-sftp-endpoint?u=76281980&t=94)** We'll let the service create a new role for us through this wizard. We can leave this next option as default, as well as the server host key. Skip over tags and manage workflows and just go click next. All right, look everything over and confirm by clicking Create Server. Okay, it's going to take a few moments for the endpoint to come online, and while it's doing that we can go ahead and create a user for this endpoint. Hit the check box right here, then go up to Add user. All right, we're going to give this a name like SFTP Demo User, with underscores there. And for the role, we will choose the SFTP role that we created earlier. So you can start typing here. And you can see SFTP S3 Access Role. That's the one we created. You see the transfer service endpoint is a bit like a conduit that connects this user to the S3 bucket. So that user needs to be able to use this role to talk to S3. All right, we're not going to do any additional scoping of the policy, so that can stay as none. And as to home directory, click the drop down here and we'll choose the bucket first and then type my folder, which was the name of the sub folder that we created in that bucket. This, basically, establishes the user's home directory in the SFTP endpoint that's going to exist
>
> **[3:07](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-sftp-endpoint?u=76281980&t=187)** but this folder needs to be preexisting when we make this choice here, which is why we created it earlier in the course. Okay, SSH public keys. This user is not going to use a username and password. Instead, much like when we log into an [[Amazon EC2|EC2]] instance, it's going to use an SSH key. So unlike with S3, that would generate keys for us, we're going to need to do this on our own. So I'm going to head back to my Mac terminal here and we're going to use a command called ssh dash keygen. You might be familiar with using this for other purposes, such as creating a key for [[GitHub]] access. We'll say ssh keygen dash T RSA, which is the type of key that we want to create, dash B, which is the number of bytes that we want this key to be in size, we'll say 4096 and dash capital C, which expects an email address. Now, this doesn't need to actually be a functioning email address, it's more like a label, so I'll just give it something fake here, Brandon at domain dot com. And hit enter. Now, it's generating my key pair and it asks if I want to save it as a particular file name here, ID RSA, which I don't, but I do want to save it in the same location. So I'll paste that beginning part of the path and give this a different name. We'll call this SFTP Demo Key. Hit enter. Now here, we're asked for a passphrase. Do not put anything here, just hit enter.
>
> **[4:41](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-an-sftp-endpoint?u=76281980&t=281)** And hit enter again to confirm that there's no passphrase. And now ssh keygen has created a key pair for us. Here in this folder, we can do an LS on SFTP, hit tab, and then star and you can see that we've got two files here. SFTP demo key and SFTP demo key dot pub. The first is the private key, the second is the public key. It's that public key that AWS wants us to paste in now. So I'll cat that out, and you can see the contents and I could just copy this with my [[Cursor]], but to make sure that I've got everything and don't miss any characters, I'm going to use the pbcopy command. So I'll cat that out again, but instead of hitting enter I'll do a vertical pipe and then pbcopy, which will place the contents onto my clipboard. Now, I can go back to AWS and paste that in. Okay, I don't think I need any tags here, so I'll go ahead and click add. And now we've got both a user and a server. In the next video, we'll see how we can use these to connect via SFTP.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (1), [[GitHub]] (1)
> **Env Vars:** sftp (13), aws (6), ssh (2), rsa (2), iam (1)
> **CLI Commands:** aws (6), ssh (5), make (2), cat (2), ls (1)
> **Cross-References:** earlier in (1), go back to (1), in the next (1)
> **Tools:** terminal (1), github (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Send data to S3 using AWS SFTP](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/send-data-to-s3-using-aws-sftp?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/send-data-to-s3-using-aws-sftp?u=76281980&t=0)** - [Narrator] Okay. Now that our SFTP endpoint is online, let's try to interact with it with a user that we just created. For that, we're going to need an SFTP client and I've downloaded one called Cyber Duck which you can find at [cyberduck.io](https://cyberduck.io). This client is available for Mac or [[Windows]] so no matter your platform you should be able to follow along if you like. Let's click Open Connection and then make sure that we change this to SFTP. If that's not already the default. We need to type in the URL of the server which we can get back at AWS by clicking the server ID and copying the endpoint right here. This thing that looks like a url and we can paste it right here, keep port at 22. And then for username we'll type SFTP underscore demo underscore user. Now, we won't use a password because as we just said we'll use SSH key authentication. So for the private key, if you saved your key in the right place, you should find SFTP demo key. They're waiting for you. Otherwise, you'll need to navigate to the location where you used SSH key gen to make your public and private key. Just make sure for this, that you choose the private key as that is what is going to get us into the SFTP server. To do that, let's click connect. Okay, I've established a connection here. If this is your first time connecting to the SFTP service you may also be asked to accept a fingerprint much like you do when you first log into an [[Amazon EC2|EC2]] instance. Just click allow and go on through.
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/send-data-to-s3-using-aws-sftp?u=76281980&t=95)** All right. Now here I am at the root of the SFTP endpoint. You can see that I have the bucket name right here the S3 bucket name, followed by my folder which means I'm in the home folder of this SFTP demo user. Now, from here, I can upload, download, delete and do all the file interactions that I might want to do with any SFTP endpoint. And to demonstrate I've got this AWS logo file sitting right here. I'm just going to drag and drop that onto the surface. And with the upload complete, I'm done. To verify if this worked, let's go back to AWS. Go to S3, click the link, find our Dropbox bucket, click into it, then my folder, and there it is, AWSlogo.PNG. So that file made its way all the way through from the SFTP endpoint to the bucket. So you can see how using AWS transfer for SFTP you can create an SFTP endpoint that behaves like any other you've ever used, but that's backed up by the durability and availability of S3.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (1), [[Amazon EC2|Ec2]] (1)
> **Env Vars:** sftp (13), aws (4), ssh (2), url (1), ec2 (1)
> **CLI Commands:** aws (4), find (3), make (3), ssh (2)
> **UI Navigation:** drag and drop (1), go to (1)
> **URLs:** [cyberduck.io](https://cyberduck.io) (1)
> **Cross-References:** go back to (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [narrator] (1)

#### [AWS SFTP teardown](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-teardown?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-teardown?u=76281980&t=0)** - [Instructor] Okay, let's go ahead and clean up the resources we created with the SFTP demo. Click into AWS Transfer Family. We'll start by deleting the SFTP endpoint. Here it is. Click the checkbox next to it. Go up to Actions, and choose Delete. Confirm by typing delete, and click the Delete button. That'll take the endpoint away along with the users that were created underneath it. Next, let's go visit CloudWatch. Click the first result, CloudWatch, and we'll see the logs that were generated by this SFTP activity. Expand logs, and go to Log groups. And you'll see one here for the AWS transfer service along with a few log entries that were created by the activity that we just did. Go up to Actions, and you can Delete log group. Next, we'll go to S3, and we'll delete the bucket associated with the dropbox. Click vendor-dropbox. First hit Empty. Type permanently delete to empty out the bucket. Once that's successful, hit Exit. Highlight again and choose Delete. Copy the bucket name to confirm, paste it in the box, and click Delete bucket. Okay, we have now cleaned up everything associated with our SFTP demo, and that should make sure
>
> **[1:31](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-sftp-teardown?u=76281980&t=91)** that you don't incur any additional charges from that exercise.

> [!info]- Semantic Content
>
> **Env Vars:** sftp (4), aws (2)
> **CLI Commands:** aws (2), make (1)
> **UI Navigation:** go to (2), checkbox (1)
> **Speakers:** - [instructor] (1)

#### [AWS DataSync](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-datasync?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-datasync?u=76281980&t=0)** - [Narrator] Throughout this course, we've talked about a few different ways to send and sync data to the cloud. If you've tried this on your own you know that you can be subject to the whims of the network between your source and destination. It's vital to have a tool that can not only maintain a high level of throughput but also one that's resilient and can handle interruptions along the way. That is the purpose of AWS DataSync. It can take data from locations inside or outside of AWS and get them to the cloud reliably and quickly, pulling from sources like NFS, CIFS, or [[Hadoop]] file systems. It can be run ad hoc or set up on a schedule where it will sync new files as they appear in your source. Your destinations can be any one of the FSx file system types, an S3 bucket or an EFS file system. A common use case would be to get data from a location in your on-premises data center into the cloud. In this case, you'd run an agent VM locally close to the data, for this, Amazon provides images to build the agent in VMware, KVM and HyperV variants. DataSync can also be configured to move data from object storage in other clouds namely [[Microsoft Azure|Azure]] files or GCP storage buckets. Another supported use case is transferring between AWS services, perhaps across regions. This method requires no agent at all. Nice. Since we can try this using only the AWS account we've been using for the course that will be the subject of our DataSync demo. We're going to move data from an EFS to an S3 bucket.
>
> **[1:36](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-datasync?u=76281980&t=96)** To show that I've set two things up in advance, an EFS file system with some data in it and an S3 bucket to act as the destination. That'll be our good old demo primary bucket. Then to move the data requires just two steps. First, we'll define the source and destination locations in the DataSync console. Then we'll create a DataSync task that moves data from EFS to S3. Let's head to the console and try it out.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Hadoop]] (1), [[Microsoft Azure|Azure]] (1)
> **Env Vars:** aws (4), efs (4), nfs (1), cifs (1), kvm (1)
> **CLI Commands:** aws (4)
> **Speakers:** - [narrator] (1)

#### [Move data with AWS DataSync](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/move-data-with-aws-datasync?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/move-data-with-aws-datasync?u=76281980&t=0)** - [Instructor] Okay. Our goal here is to move data from EFS to S3 using AWS DataSync. To show that I've logged into an [[Amazon EC2|EC2]] Instance that have stood up here with an EFS file system mounted. If you want to replicate this, you'll remember that we did this back in Chapter 3 so you can go back and follow those instructions. In here, I've got a little bit of data, and this is replicated from the way that we set up the data for demonstrating S3 [[CLI]] syncs back in the beginning of this chapter, second chapter of Chapter 5. So, you'll see that we've got one large file called big file and a folder with many small files. These are in Chapter 5 of the exercise files but there's also a Ruby script in there that will generate them for you if you would like to do it that way. All right, so we've got our EFS and we've got some data. Our destination is here in S3, our demo bucket, which has a few objects in it already and we'll create a subfolder in the course of syncing data over for all the EFS data to land. All right, let's use the search bar to head to the DataSync section of AWS. To get started here, we choose a use case between on-premises and AWS or between AWS storage services. We're doing the latter, so we'll click that and go right into the wizard. This is going to walk us through a few steps. If you expand the menu over here, you'll see some of the things that we mentioned in the slides about setting up locations or agents,
>
> **[1:33](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/move-data-with-aws-datasync?u=76281980&t=93)** but we'll just walk through it. We'll create a new location for the source. That will be an EFS file system. The region is wherever you set up your EFS. Mine is in us-east-1 so the dropdown will show the EFS I called DataSyncDemo. So for mount path, this is not to be confused with the directory that we use to mount the EFS on our EC2 Instance, so it's not /EFS. I want to just refer to the root of the EFS file system, so that'll be a slash. As for subnets, I'm going to choose one where I know I can reach the EFS from. Remember, if you follow those directions, all of your access points in the EFS should have a security group on them that allows NFS from anywhere in the VPC. And I'm going to choose that security group right here. Before we do that, I'm going to choose in-transit encryption TLS It's a little surprising actually that this would not be a default option. You always want to have your data encrypted in transit when you're making a move like this, especially over the public internet, which we're doing in this demo. The two optional settings here can stay at their default and then we click next. All right, now we are setting up the destination. So, we'll create new location. We'll choose S3. The region where your bucket is. And I'll select the bucket in this dropdown. Storage class is Standard
>
> **[3:07](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/move-data-with-aws-datasync?u=76281980&t=187)** and the folder, we can create here. So, I'm going to call this datasync-demo. For the IAM role, click the Auto Generate button, which I've already done so you can see an IAM role name there. Once it's generated, go ahead and click Next. And now we've got two locations. We'll create the task. We'll call this DataSyncDemo and take a look through the options here. You can see that there is a step here where data sync will verify that the data was transferred correctly. You can see, you can choose here whether to scan the entire source for all files or if you want to drill down into certain locations. We'll keep those at default. I'll not put this on a schedule, but of course you can set up any schedule you'd like, including a custom schedule to sync this data as often as you want. For us, we'll kick it off manually. No tags necessary and we do want the default CloudWatch logging going on here, so keep that at default and click next. All right, scroll down, review your choices and click Create Task. Now, we didn't give this a schedule, so as soon as task status is available, go up here to start and click Start With Defaults. That'll change the task status to running, and if we want we can click See Execution Details here. So, we're in a launching phase now and in a few moments this will be complete. Okay, now we have a success message. So, let's head into S3 and see the result. I'll click into our bucket here
>
> **[4:42](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/move-data-with-aws-datasync?u=76281980&t=282)** and we see the subfolder datasync-demo and all of our files in here. Now, if we had made this into a scheduled data sync job it would continue to run on that schedule and like the sync command and S3's CLI, it would recognize the changes as they're made. It doesn't re-sync all the files. It just looks for things that have changed since the last run. So, whether you are coming from an on-premises file system or from another [[Cloud Storage]] provider, or from another service in AWS, DataSync is a really great way to move files around. It runs things in parallel as much as possible. It's high performing. It handles interruptions and blips in the network. Works very well. Just another great way to get data into the cloud.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (2), [[CLI]] (2), [[Cloud Storage]] (1)
> **Env Vars:** efs (12), aws (5), ec2 (2), cli (2), iam (2)
> **CLI Commands:** aws (5), ruby (1)
> **UI Navigation:** dropdown (2), select the (1), scroll down (1)
> **Prerequisites:** set up (3)
> **Definitions:** is a  (2)
> **Cross-References:** we mentioned (1)
> **Exercise Files:** exercise files (1)

#### [AWS DataSync teardown](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-datasync-teardown?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-datasync-teardown?u=76281980&t=0)** - [Instructor] Okay, picking up from the last video, we'll do our cleanup here. So here in the demo-bucket, you can delete everything from here and then delete the bucket. But this bucket continues to be used in the course, so I'm going to keep it around. And we'll go ahead and check just this check box here, and click Delete to get rid of the datasync-demo folder. I'll confirm by typing, delete, and click the Delete objects button. From here, I'll head back into DataSync. So all I need to do here is highlight the task, go up to Actions, and then Delete, which I can confirm, and then go to Locations, highlight and delete both of those. None of these definitions really incur any charges, but it's good to kind of clean things up. As for the pre-setup steps, I can go in here into [[Amazon EC2|EC2]]. I can delete the instance or terminate the instance is the right phrase for that. I'll highlight, I'll go to Instant state, Terminate, confirm and then I'll also head over and clean up the EFS by going to that section, highlighting and then choosing Delete. I have to copy the name of the file system here, the ID of it, paste it, and click Confirm. And so with that, we will have cleaned up everything that we created with the DataSync demo.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (1)
> **Env Vars:** ec2 (1), efs (1)
> **UI Navigation:** go to (2)
> **Prerequisites:** setup (1)
> **Speakers:** - [instructor] (1)


### 6. Moving Data in AWS

[↑ Back to Table of Contents](#table-of-contents)

#### [AWS Glue overview](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-overview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-overview?u=76281980&t=0)** - [Instructor] AWS Glue is a fully managed serverless [[Data Integration]] service based on [[PySpark]], the [[Python (Programming Language)|Python]] implementation of [[Apache Spark]]. With Glue, you design data flows by connecting sources to targets with transformations in between. The Glue wizard and GUI help you define these jobs which generate PySpark code. If you're already familiar with Python and Apache Spark, you'll be right at home. If not, Glue can get you started by proposing designs for some simple ETL jobs. Because Glue is serverless, although you pay for the resources consumed by your running jobs, you never have to create or manage an [[Amazon EC2|EC2]] instance. Another core feature of Glue is that it maintains a [[Metadata]] repository of your various data schemas, this could be relational table schemas, the format of a delimited file, and more. Although it is sometimes confusing, Glue calls these metadata repositories [[Databases]]. You can define the schema in one of two ways, first, you can manually enter it, this would involve you typing the name of each data column or field, then specifying its type and data width. Alternatively, Glue can search your data sources and discover on its own what data schemas exist. To do this, you must define what's called a crawler. Crawlers can read from S3, RDS, or a [[Java Database Connectivity (JDBC)|JDBC]] source. They can discover table schemas, but do not discover relationships. And they can be scheduled to update themselves over time. With crawlers keeping your metadata up to date,
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-overview?u=76281980&t=95)** mapping source data to destinations becomes fairly straightforward. Keep in mind that existing jobs are not automatically aware when schemas change and may need to be refreshed. Jobs can be triggered on a schedule such as daily or monthly on completion of another job where we chain dependent jobs or on demand. Finally, a few caveats with Glue. Unlike many popular ETL packages, it has no third-party connectors. You're not going to be connecting to Salesforce out of the box with Glue. As I mentioned earlier, when schemas change, you'll need to update the jobs that use them. Chaining dependent jobs is possible, but job chaining is not easy to visualize once it's built. Finally, the wizard and GUI are really only suitable for very simple jobs, after that, you'll be writing Python. With that said, let's get into Glue and see what it can do.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (3), [[Metadata]] (3), [[PySpark]] (2), [[Apache Spark]] (2), [[Data Integration]] (1)
> **Env Vars:** gui (2), etl (2), aws (1), ec2 (1), rds (1)
> **CLI Commands:** python (3), apache (2), aws (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)
> **Warnings:** keep in mind (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [AWS Glue: Create an S3 metadata crawler](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-an-s3-metadata-crawler?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-an-s3-metadata-crawler?u=76281980&t=0)** - [Instructor] In this demo, we're going to create a job using AWS Glue that moves data from a CSV stored in S3 to a table in our [[MySQL]] database. If you didn't keep or create the MySQL demo from earlier in this course, you can do so by reviewing the chapter four video called Create a MySQL RDS Instance. In addition, we're going to need an S3 bucket. If you've still got the demo primary bucket from back in chapter two, that's perfect. Otherwise, take a moment to create a new one with the same default configuration that we used in that chapter. I'm going to go into S3 and into my demo bucket, where I've got a file called states.csv. This exists in the chapter two folder of the course exercise files. And it looks like this. Very simple, we've got the name of the state, the abbreviation, the year it became a state, the capital, a few other bits of information here, and that's it, just a couple of columns, a row for each one of the 50 states. Okay, so make sure that that's in your bucket. And now, let's go to the search bar and type Glue. We'll go here to AWS Glue, and on the next page, instead of clicking Get Started, we'll come over here to Crawlers. Okay, we're going to create a crawler that will look at our S3 bucket and identify the schema of all the files inside. So click Create Crawler. We'll call this DemoCrawler.
>
> **[1:37](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-an-s3-metadata-crawler?u=76281980&t=97)** First, it'll examine the S3 bucket, and then later in this part of the course, it'll look at an RDS database. Go ahead and click Next. We're asked, is the data already mapped? And the answer is not yet, so keep that at default and click Add a data source. Okay, we're choosing S3, which is the default, we can skip the network connection option, and go to the location of S3 data that's in this account, and we could type in the S3 bucket name or we can click Browse. Oka, do not click into the bucket, but rather click the little radial button next to the bucket and click Choose. Okay, if you click off or tab off of that, the error message that you just saw should go away, and you can see below, a couple of options for how the crawler should work. We'll keep that at the default to crawl all sub-folders. Click Add an S3 data source. Okay, click Next, and now we need to create or choose an IAM role that will be used throughout the Glue process. Right now, we don't have one, so we're going to click Create New IAM Role. And we're prompted to add a suffix to an existing name, so I'll just put my initials on there and click Create. Okay, so an IAM role has been created for us and we can go ahead and click Next. Now we need to choose where the output of the crawler should go. That [[Metadata]] is going to end up
>
> **[3:09](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-an-s3-metadata-crawler?u=76281980&t=189)** in something called a database, which is a little bit of confusing terminology, but it's just the location where that schema information will get stored. We don't have that kind of database yet, so we're going to click add database, which opens in a new tab. We'll call it DemoMetadata, leave the other fields blank and click Create database. Oh, we're being told it needs to be lowercase letters, so we'll call it demo_metadata, like this. Click Create database. Okay, we can close this tab now, and back on the original tab, hit the refresh button, which will cause our demo metadata to show up in the dropdown. Now click Next. Okay, review all of the options, scroll down and click Create crawler. After a moment, the new crawler will appear. Check the check box next to it, and we'll go to Run. This process takes a few moments, but what's happening is that the crawler is now inspecting our S3 bucket. It'll look at every file that it encounters and try to take a look at it and understand what its schema is, which it will save in the database that we specified. Okay, now that that has finished and succeeded, we can go over here on the side to [[Databases]], click into demo_metadata, and see that we have two table entries right here in this section. The first is from the parks_and_landmarks_detroit file
>
> **[4:44](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-an-s3-metadata-crawler?u=76281980&t=284)** that was in the S3 bucket alongside states, and the second is the one that we're interested in, states.csv. You can see it's been renamed with an underscore here. Go ahead and click it and we can see what the crawler discovered. Scrolling down, you can see all of the columns have been found, as well as their data types. This is our opportunity to review what has been discovered by the crawler, and if we want to change something like a data type, we could do that here. But this is all looking really good, so it means that we have the metadata for our source file and can now move on to establishing a connection to RDS. That's going to take a few steps, which I will describe in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (3), [[Metadata]] (3), [[Databases]] (1)
> **Env Vars:** rds (3), iam (3), aws (2), csv (1)
> **CLI Commands:** mysql (3), aws (2), make (1)
> **UI Navigation:** go to (3), dropdown (1), scroll down (1)
> **Code Identifiers:** demo_metadata (2), parks_and_landmarks_detroit (1)
> **Cross-References:** earlier in (1), later in (1), in the next (1)
> **File Paths:** states.csv (2)
> **Definitions:** is a  (1), means that (1)

#### [AWS Glue networking](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-networking?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-networking?u=76281980&t=0)** - [Instructor] Our target for this demo is going to be the [[MySQL]] RDS database, but before Glue can connect to it, I need to do a little explaining. Imagine this box here represents all of AWS, not just your resources, but mine, too, and all those belonging to AWS behind the scenes. Now, most of the resources we've created in this course have been built inside our default VPC. A VPC, or virtual private cloud, is like your own data center inside AWS. It's an enclave where your network traffic is private and enclosed, much like your on-premises data center may be. Since VPCs were introduced, AWS has designed most of their services, including RDS, to work within a VPC. You can see our database here inside the VPC. The thing is, some AWS services exist outside of any VPC, and need special steps taken to establish communication to your private resources. S3 is actually one such service, although AWS does an admirable job of hiding that complexity. Normally, you don't have to think about it. Another service that's outside the VPC is Glue. Now, anytime that we set up a Glue job with a connection to a VPC resource like this RDS database, Glue is going to create a network interface inside our VPC that'll be used to run our job. This is basically a little worker node or job runner for the Glue job. So in order to let Glue both talk to our RDS database
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-networking?u=76281980&t=95)** and communicate with S3, which, by the way, it would need to do, even if our source was not S3, we need to do two things. First, we need a security group that lets the worker talk to RDS. This'll take the form of a self-referencing rule. This is a security group that fully trusts any resource bearing that same security group. It doesn't trust, say, an IP address or a CIDR block, it trusts the ID of another security group, in fact, it trusts the ID of itself. We'll attach this security group to the RDS, and later, Glue will use it when it tries to talk to the database so it will be allowed in. The second thing we need to do is give the Glue job a way to talk to S3 from within the VPC. We'll do this by creating something called a VPC endpoint. Specifically, we'll create a gateway endpoint to S3, giving Glue a private channel to the S3 service. Although it is possible for Glue to reach S3 through the public internet, Glue uses the VPC endpoint to keep all job traffic within the AWS network instead of going outside to the public internet. If all this sounds a bit confusing, I don't blame you. It's a fairly big hurdle to usability that one needs to understand so much of the internal networking, not just of their own resources, but of AWS itself to configure Glue jobs properly. Right now, the only real way to understand the necessity of these steps is to take a deep dive into the docs. Still, AWS has put a lot of development time
>
> **[3:08](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-networking?u=76281980&t=188)** into enhancing Glue since it's release several years ago. Here's hoping they improve this service in the future by abstracting away some of these challenging details. Now, let's head back into the console and get Glue talking to RDS.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (1)
> **Env Vars:** vpc (11), aws (9), rds (7), cidr (1)
> **CLI Commands:** aws (9), mysql (1), node (1)
> **Prerequisites:** set up (1), configure (1)
> **Documentation:** the docs (1)
> **Definitions:** is a  (1)
> **Analogies:** imagine (1)
> **Speakers:** - [instructor] (1)

#### [AWS Glue: Create a connection to RDS](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-a-connection-to-rds?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-a-connection-to-rds?u=76281980&t=0)** - [Instructor] All right, let's take the steps necessary to get AWS Glue talking to our RDS database. Remember that I'm assuming that the [[MySQL]] you created in chapter four is still around. If it's not, head back there and look for the video called creating a MySQL RDS database. When you're done, let's head back here into [[Amazon EC2|EC2]]. Along the left hand side, we'll look for security groups. We're going to create the self-referencing rule I mentioned in the last video. Click Create security group. Now, this will have to be done in two steps because after all, we can't reference this rule until the rule exists. So type in a name, we'll call it Glue self-referencing rule and I'll copy paste that into the description. We're not going to do anything else. We won't add inbound or outbound rules. We'll just click Create security group. Now, here on the summary page for that rule, we can click edit inbound rules and now we can reference itself. Click add rule. Under type, choose all TCP. Keep source as custom, and click into the text box with the magnifying glass. We'll type the [[Microsoft Word|word]] Glue, to filter down to this rule and now this rule is referring to itself. It will trust any traffic coming from any resource in AWS that also has this security group attached. Click Save rules. Okay, now that we have the rule, we want to head to the RDS section of AWS. Type RDS and choose that option.
>
> **[1:36](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-a-connection-to-rds?u=76281980&t=96)** We need to attach this security group to our RDS MySQL database. Click on DB instances and click on the name of our MySQL database. Click the Modify button and we'll scroll about halfway down the page here, once it loads. It's right here under connectivity. Hit the dropdown by security groups, type Glue and we'll choose the self-referencing rule. Make sure that appears below, scroll all the way down and click Continue. Here in the summary, make sure to check Apply immediately. That way we won't need to restart the database. Click Modify DB instance. That's going to take a few moments to take effect. Our next step is going to be in the VPC section, so I'll search that and click into VPC. Over here on the side, I'm going to endpoints. Now, Create endpoint. All right, we'll just call this demo endpoint for now. Keep AWS services clicked here and go down and filter by the words S3. We'll choose this middle one. Com dot Amazon AWS dot and then the name of your chosen region dot S3. Okay, there's a global one here and one for Outpost, but we'll choose this one in the middle. Now we have three more options, and the one we want is the type of gateway. So make sure that is checked. Scroll down, choose your default VPC out of the dropdown and scroll down a little more.
>
> **[3:09](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-a-connection-to-rds?u=76281980&t=189)** Very important step here. Make sure to check the checkbox next to the route table that appears so that AWS will modify your routes to use this endpoint. And that should be everything you need, so scroll all the way down and then click Create Endpoint. Now that we've done those two things, we can head back to the Glue service. And on the side we can go to Connections. That'll open up in a new tab. There's a lot going on in this screen, but just scroll past anything that refers to connectors and go down to connections. Click Create Connection and we'll call this one MySQL Connection. Type is going to be Amazon RDS and database engine is MySQL, of course. Down here, we will choose our RDS instance from the dropdown and that pre-fills a few values for us, including the database name of my underscore database, which we created in chapter four. Remember that, as it will come in handy in a few steps. Our username is pre-filled but we'll need to type in our password. Click Create Connection. Now, AWS has been making a lot of enhancements to Glue over the past few months and years, and unfortunately, in so doing, they have lost a pretty useful button. There used to be something that would let you just test the connection from here. Fortunately, we can get back to it. If you type in Glue and go back to the front page, you may see this Legacy pages section. Scroll down and click Connections, legacy. Now, this is just an older user interface, but it does have this nice useful button here.
>
> **[4:44](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-a-connection-to-rds?u=76281980&t=284)** I'm going to click MySQL Connection. Click Test connection. Choose the IAM role that we created earlier and click the Test connection button. Now, when you're watching in the future, it's possible that legacy Pages section will have been fully retired and you'll need to use the newer interface for everything. Hopefully, by that time they'll have reinstated this Test connection button so that you can use it, but if not, no worries. Just use the connection in the crawler set up that we'll do in a moment, and you'll find out at that time whether you need to make any changes to your connection. Just remember you need to have the Glue self-referencing rule attached to your RDS. You need the VPC endpoint set up and you need to have typed in the password correctly. So we've got a successful connection here and in the next video, we'll use this in a crawler to discover our RDS schema.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (7), [[Amazon EC2|Ec2]] (1), [[Microsoft Word|Word]] (1)
> **Env Vars:** rds (9), aws (7), vpc (4), ec2 (1), tcp (1)
> **CLI Commands:** aws (7), mysql (7), make (5), find (1)
> **UI Navigation:** dropdown (3), scroll down (3), click on (2), checkbox (1), go to (1)
> **Prerequisites:** set up (2), you need to have (2), you'll need (1)
> **Cross-References:** in the last (1), go back to (1), in the next (1)
> **Best Practices:** make sure to (2)
> **Definitions:** refers to (1)

#### [AWS Glue: RDS data crawler](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-rds-data-crawler?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-rds-data-crawler?u=76281980&t=0)** - [Instructor] We're picking up right where we left off with Glue, now able to talk to the RDS database. Over here in [[MySQL]] Workbench, I've logged into that database, and you can find this bit of [[SQL]] in the Chapter 6 folder of the exercise files. It's in a file called mysql_states_table.sql. You see, speaking of features that Glue has modified or taken away, one of the things you used to be able to do is set up a job that would create the table in the destination database. Unfortunately, Glue isn't going to allow us that option right now, at least not through its graphical editor. So instead, what we're going to do is create the destination for our data and then have a crawler in glue discover it so that we'll have both sides of the [[Metadata]] defined. In this case, you can see that this table is not going to be exactly like the schema of our CSV file, and that's because we want to show a little bit of transformation happening in this ETL job that we're going to define in Glue. So what will eventually end up doing is mapping state to state name, abbreviation to abbreviation, and then we'll do a little bit of concatenation between the capital and the name of the state so that we can show a very basic transform that changes the data as it moves from S3 to the RDS database. So, to get this table created, we just highlight it and click the lightning bolt, and then we can head on back to AWS. Now we're going to go back to crawlers. We could create an entirely new crawler and database, but we're actually able to use the same one
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-rds-data-crawler?u=76281980&t=95)** that we use to crawl S3. Click into Demo Crawler, scroll down to Data Sources, this tab here, and then click Add a Data Source. Now we'll choose [[Java Database Connectivity (JDBC)|JDBC]], and that's the protocol that Glue is using to talk to the database. As for connection, hit the dropdown, you'll find MySQL Connection. Here in include path, a very important step, type my_database. Now this does rely on you having set up the RDS database just like we did in Chapter 4. I wouldn't recommend using the percent here, or at least in my experience, that has led to errors. But being specific with the schema path here should work for you. Click add a JDBC data source. Okay, now we can click Run Crawler, and to watch the progress, we can click back on crawlers again. Now what's happening here is that the demo crawler will re-scan everything that's in the S3 bucket. Having found nothing changed, it won't make any modifications to those tables. Then it will look at the RDS database in the schema my database, and describe the schema for all of the tables that it encounters there. Okay, a few minutes later, and the crawler is done and has been successful, so let's click over to [[Databases]], into demo metadata. And now we see we have more tables than before. In addition to the two CSV files from S3, we have several records of database tables from our RDS database, including my database states.
>
> **[3:10](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-rds-data-crawler?u=76281980&t=190)** You can see how it's naming things here using the schema_, and then the table name. And there's the discovered schema from RDS. So now we're ready in the next video to define an AWS Glue job to move data from S3 to RDS.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[MySQL]] (2), [[SQL]] (2), [[Metadata]] (2), [[Java Database Connectivity (JDBC)|Jdbc]] (2), [[Databases]] (1)
> **Env Vars:** rds (7), csv (2), aws (2), jdbc (2), sql (1)
> **CLI Commands:** mysql (2), find (2), aws (2), make (1)
> **Code Identifiers:** mysql_states_table (1), my_database (1)
> **Cross-References:** go back to (1), in the next (1)
> **UI Navigation:** scroll down (1), dropdown (1)
> **Prerequisites:** set up (2)
> **File Paths:** mysql_states_table.sql (1)

#### [AWS Glue: Create and run a job](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-and-run-a-job?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-and-run-a-job?u=76281980&t=0)** - [Instructor] Now that we've got Glue talking to both S3 and the RDS database, and having established a [[Metadata]] repository of files in S3 and tables in RDS, let's go into AWS Glue and click on Jobs. This will open up a new UI in a new tab. Under Create job, we've got a few options. The default, which we'll go with, is Visual with a source and target. This will create a template for us, using Glue's drag and drop interface. But you can also see we can start with a blank canvas, or go straight to editing scripts with Spark, [[Python (Programming Language)|Python]], or even a Jupyter Notebook. Our source is S3 and our target will be [[MySQL]]. This will just let Glue know a little bit about what we're doing, so that it can generate some boilerplate code for us. Click Create. Now on this page, you can see that we've got a visual representation of a basic ETL job. You can click on the first node here. This is a source node, and it says S3 bucket. Still, we need to choose the location from either the Data Catalog or S3. So we'll go to the database here, keeping Data Catalog selected, and choose our demo metadata. This will allow us to specify the Table, states csv. Even though it says table, remember, this is a representation of the schema of our CSV file. So you can see if you click Output schema, because we have that metadata, Glue knows about the columns that exist here. Now we can go to the Data target, and choose the other end.
>
> **[1:37](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-and-run-a-job?u=76281980&t=97)** The database here will again be demo metadata and the table will instead be, my database states. Again, we see the output schema matches what the metadata crawler found. In between here, we have ApplyMapping. Now, if you're familiar with other ETL tools that use a drag and drop interface, I think you're going to find this a little bit limited. We don't have the ability to directly map column to column with this interface, knowing about both sides, despite the fact that we have the crawler information and all of the schema for the source and target, we don't really get to connect the dots in the way that you might with other systems. So we do need to know a little bit about what Glue is expecting here. So we have this option Drop, and we can go ahead and click that for all the columns we know we are not going to map over. We're going to leave just state and abbreviation, and we'll deal with the concatenation of capital and state later on, in code. Here we have Source key and Target key, and again, we have to know about both sides of the metadata. If you want to click over to MYSQL, you can review that by clicking Output schema, and see that the column is called state name, instead of just state. So here, under ApplyMapping, we'll replace the target with state name. If we were to run the job just from here, Glue would map over the state and abbreviation into our database table, but we want to do a little bit more. And before we run the job, we need to supply a few details.
>
> **[3:11](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-and-run-a-job?u=76281980&t=191)** Go to Job detail, and give the job a name. We'll just call this Demo Job, and we need to fill in this IAM Role down here, which you will recognize from generating it earlier. Everything else here can stay the same. Now we can click Save, on the job, and again, we could run it from here, but let's do a little bit more. Go to Script, and you can see the [[PySpark]] code that Glue has generated out of this visual template. I will say that the drag and drop interface of Glue is quite limited, and a little bit confusing. It's much better I think, if you let it set up some boilerplate, and then edit the code directly. So to get the most out of Glue, it helps to know a little bit of this code. Let's take a look at what's going on here. We've got some import statements at the top. We've got some setting up of variables, based on the parameters that we configured outside in the GUI. And we've got three kind of sections of code here that represent the three nodes on the graph we were just looking at. We create a dynamic frame out of the demo metadata states.csv metadata. We have Apply Mapping, which has a mapping for state to state name, and abbreviation to abbreviation. And then, at the end, we have a statement here, that writes into the dynamic frame represented by the state's table. So that's the whole job, but if we want to do the concatenation for the capital, we're going to need to go to another exercise file. In the chapter six folder of exercise files,
>
> **[4:47](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-and-run-a-job?u=76281980&t=287)** you will find this: It's called glue states concat.py and there's just a few modifications here. First, we have a new import statement that pulls in a couple of functions that we're going to use, concat, lit, and col. Here, we say temp datasource equals, and we create a new data frame. We're starting with the S3 bucket and we're adding a new column. We'll call it capital concatenated, and then you can see we are concatenating the column capital. We're concatenating a literal, which is a comma and space, and then we're concatenating the state. So it doesn't actually matter whether we use capital in the mapping, even in the visual, or in the code here. Instead, we are creating a new column before we ever get to the point of apply mapping. So we create a new data frame out of that dynamic frame, print Schema just for a little debugging purpose. And then here, I've added a new line to the apply mapping to make sure that capital concatenated maps to the same name column in the destination. And that's it for changes. If you copy this entire script, we can head back to the AWS console and put it in, click Edit script. Now we're warned here that doing this will force us to abandon the drag and drop mode of editing this script. And that's okay, click Confirm. Now we can highlight and delete everything and paste in the modified script. Click Save, and now click Run.
>
> **[6:24](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-glue-create-and-run-a-job?u=76281980&t=384)** Okay, follow the link here to Run Details, and we can see that we are in the running state. If you want to watch any logs while or after this is running, just click here in CloudWatch and that will open up in a new tab. We'll let this run and in a moment we can inspect the results. Okay, the job has now succeeded, so we can head over to the MySQL database and do a select star from states. Now you can see that we've got data populated from the CSV that was in the S3 bucket. We've got state name, we've got abbreviation, and we've got the capital concatenated, just like we wanted. So it took a bit of doing, but you can see how once you've established a set of connections and a metadata repository with Glue, it can be a nice way to kickstart and run Python-based data integrations. Feel free to experiment with the code from here. In the next video, we'll clean everything up.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Metadata]] (9), [[MySQL]] (3), [[Python (Programming Language)|Python]] (2), [[PySpark]] (1)
> **CLI Commands:** mysql (3), aws (2), python (2), node (2), find (2)
> **Env Vars:** rds (2), aws (2), mysql (2), etl (2), csv (2)
> **UI Navigation:** go to (5), drag and drop (4), click on (2)
> **Exercise Files:** template (2), boilerplate (2), exercise file (1), exercise files (1)
> **Definitions:** is a  (3), is called (1)
> **File Paths:** states.csv (1), concat.py (1)
> **Tools:** jupyter (1), aws console (1)

#### [Glue and course teardown](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/glue-and-course-teardown?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/glue-and-course-teardown?u=76281980&t=0)** - [Instructor] Okay, as with everything in this course, we'll clean up after ourselves so you don't incur any unexpected charges. We'll start here at the dashboard of AWS Glue, and we'll go to the Job section. Highlight the Demo Job name by clicking the checkbox. Go up to Actions and choose Delete jobs. Click Delete to confirm. Okay, expand the menu here with the three lines, and go to Glue catalog. And that'll open up in a new tab. Let's go to [[Databases]] first. Check the box next to demo_metadata and hit Delete. Confirm that with the button, and that will take care of all of our [[Metadata]] and the database that enclosed it. Now we can go to Crawlers. Check the checkbox, go Actions, and then Delete crawler. That will take care of the crawlers and their data connections, except for the connection to RDS, which is here under Connections. Another new tab opens. Scroll down, choose mysql_connection. Go to Actions and Delete. Okay, now let's think about the things that we had to do to the RDS database. To start, we'll go to VPC and delete the endpoint. Click Endpoints, click the checkbox next to DemoEndpoint, go to Actions and then Delete VPC Endpoints. We'll type delete to confirm.
>
> **[1:33](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/glue-and-course-teardown?u=76281980&t=93)** Now, here in RDS, we're going to make things simple. Instead of detaching the security group, at this point in the course, we are done with this database and we will fully delete it. You'll want to go ahead and do this for any RDS that you might still have around. Click the checkbox, go to Actions, and then Delete. Here, you'll have to answer a few questions. Say no to final snapshot because we don't need this data and a snapshot could sit around and incur some charges for you. Do not retain automated backups, and do acknowledge that all this data is going away by checking the third box. Type delete me to confirm and click Delete. Okay, at this point you could delete the security groups that we created, the Glue self referencing rule, or go cleanup IAM policies, but none of those things are going to charge you. Okay, from here, we'll go into S3. And you can delete this generated S3 bucket that's owned by Glue by checking the box next to it. First, clicking Empty, and typing permanently delete, or you can cut and paste. This will empty out anything that Glue placed in there, while it was running. And when that's successful, hit Exit, check it again and choose Delete. Copy and paste the name of the bucket to confirm that. You may want to come here and delete your other S3 buckets. But that'll do it for cleaning up resources associated with this Glue demo. In the next set of videos, we will look at AWS backup.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Databases]] (1), [[Metadata]] (1)
> **UI Navigation:** go to (8), checkbox (4), scroll down (1)
> **Env Vars:** rds (4), aws (2), vpc (2), iam (1)
> **CLI Commands:** aws (2), make (1)
> **Code Identifiers:** demo_metadata (1), mysql_connection (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)


### 7. Backup

[↑ Back to Table of Contents](#table-of-contents)

#### [Understand AWS Backup](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/understand-aws-backup?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/understand-aws-backup?u=76281980&t=0)** - Throughout this course, we've explored many ways of using AWS services to store and manage your data. And at each stop, we've looked at how to keep that data backed up. After all, there are many potential threats to your data. Disc barrier is one. While AWS's is high level of advertised durability, facilitated by behind scenes replication and integrity checks, makes this possibility a remote one, remember that not every service has the same level of durability as S3. EBS volumes will beat an off-the-shelf hard drive any day, but they're still subject to failures. Malware is another danger. Any AWS stored data attached to an instance with internet access, is subject to the same malware risk as any PC or laptop. A very real, and very scary possibility these days, is that ransomware could encrypt your data, and hold it hostage until you pay up. You don't want to get stuck having to comply with a demand like that. Finally, there's the most prevalent danger to your data, you, or your users, or your newly released, "we definitely tested all the edge cases, didn't we?" application feature. I'm talking about accidental data loss or data corruption, the kind of thing that's hard to plan for, because it is by nature a surprise. In all these cases, a good backup strategy can turn potential catastrophe into an IT win. So of course, it's important to have backups. That's why with every [[Data Storage]] service we've discussed in this course, we've touched on the subject of creating and restoring backups.
>
> **[1:33](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/understand-aws-backup?u=76281980&t=93)** For better or for worse, the method varies from service to service. EBS has snapshots, Dynamo DB can be backed up to S3. RDS has its own snapshot capabilities, as does Storage Gateway. And at first, EFS didn't even have way to backup. When the service first launched, you were responsible for doing your own backups, say by scripting and scheduling an ETL job, and storing the backups somewhere on your own. So you can see that managing backups in AWS, can mean working with a fairly disjointed set of tools and processes. This scenario is not unlike the state of AWS command line tools was a few years ago. At that time, it was not unusual for individual AWS services to have their own command line tool, which would be installed separately from each other. These interfaces were not always the most consistent, because different development teams created them independently of each other. Eventually, AWS made a considered effort to rectify this situation, leading to the single AWS [[CLI]] we enjoy today. The unified CLI still lets you work with all the many different services that comprise AWS, but it provides a one stop shop for doing so. Similarly, AWS backup is a service that aims to bring consistency to the experience of managing and maintaining backups in AWS. It takes those disparate implementations of backup that I just mentioned, and provides a one-stop shop to manage them. AWS backup establishes a consistent, centralized management interface for backups
>
> **[3:06](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/understand-aws-backup?u=76281980&t=186)** across multiple services. It debuted with support for EBS, RDS, Dynamo, Storage Gateway, and for the first time since that service was released, EFS. With AWS backup, you create backup plans oriented around schedules. And the backup service is based on tagging, letting you create a truly flexible backup strategy for your organization. Let's head to the console and see how it works.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[CLI]] (2), [[Data Storage]] (1)
> **Env Vars:** aws (13), ebs (3), rds (2), efs (2), cli (2)
> **CLI Commands:** aws (13)
> **Tools:** command line (2)
> **Definitions:** is a  (1)
> **Speakers:** - throughout (1)

#### [Create and configure an AWS Backup job](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-and-configure-an-aws-backup-job?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-and-configure-an-aws-backup-job?u=76281980&t=0)** - [Instructor] I'm logged into an [[Amazon EC2|EC2]] host that has an EFS file system mounted. Let's take a look at what's inside. All right, we've got a couple of files here, text files. We've got data in [[JSON]] format. And, oh what's this? We've got very-important-documents. Inside here you can see annual budget, final report. Some things that look like we really wouldn't want to lose them. So to protect ourselves against the risk of accidental deletion, let's go to AWS backup and create a backup plan. Back here to the AWS console. And we're going to type "backup" into the search bar here. And click the first result AWS Backup. Now we'll click Create Backup plan. What we'll do now is define our plan in two steps. The first step is to set up a plan that defines the timing, frequency and retention for the backups that we're going to create. Then we'll take that plan and assign to it resources like the EFS file system that we just looked at. Now we can start with a template and we can choose from some common patterns of backup here. Or, for this demo, we can click Build new plan. We'll call this MyBackupPlan, and scroll down to rule configuration. Our first rule we'll call DailyBackup. For the backup vault we can click to create a new one or we can select an existing one from the dropdown. So there's a default that should be pre-created that we can build. This one here could use some explaining. When I first created the EFS file system it actually gave me the option to establish backups
>
> **[1:35](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-and-configure-an-aws-backup-job?u=76281980&t=95)** using AWS backup right there at the creation point of the EFS. So this is actually a vault for an existing backup plan for this EFS. But just because AWS has given us that convenience over in the EFS part of the system doesn't mean that we can't come in here and create our own custom backup rules for this or any other file system. So we'll go ahead and just choose the default here, and continue on. Backup frequency is already set to daily, but you see you have every option here including Custom cron expression. So if you're familiar with cron you can create a command that gives a very detailed frequency for this backup. If you'd like to enable something similar to what we saw with RDS where you can have a very fine-grain recovery point, going down to say the minute, you can click this to enable continuous backups for point in time recovery. That will incur a little bit of extra cost for you, but it does mean that you have a whole lot of resolution as to where you can target your backups. Could be very nice. We'll leave it unchecked for now and continue on down. The backup window says when will this backup occur? You can see the default is 5:00 AM UTC, or you can click customize backup window and give your own time. We'll keep the default for now. Now we have some transition rules. We can choose to transition to cold storage, never, or after a certain amount of days, weeks, or months. For these daily backups we'll keep them for two months before going to cold storage. As for retention, this gets to when the backups
>
> **[3:10](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-and-configure-an-aws-backup-job?u=76281980&t=190)** will actually be finally fully deleted. So we'll say these daily backups are going to stay around for six months. And you might be thinking, well I want my backups to stay around for longer than that. We're going to supplement this rule with a different rule that takes snapshots at a different interval and keeps those around for longer. Of course, all this configuration means you can tune your backup plan to exactly how you'd like it to work. The other thing to note here is that there needs to be 90 days between cold storage and final expiration due to the limits of S3 Glacier. Because these backups, like many things in AWS, are being stored in the backend by S3. And so the lifecycle policies we're configuring here are really S3 object lifecycle policies. So when you say cold storage, that's transitioning to the S3 Glacier tier and expiration means actual deletion. We don't need to worry about copying to any other region or any of the other options on this page. So click Create plan. Okay, now we're into the second phase of creating the backup plan, which is assigning resources. The resource assignment name we will call EFSDemoAssignment 'cause we're just going to assign the one EFS. And as far as the IM role we can keep that at default. And scroll on down. We can either include all resource types, which is very expansive. Protect all resource types enabled in your account, or we can get specific by checking the second option here. If we select resource types
>
> **[4:41](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-and-configure-an-aws-backup-job?u=76281980&t=281)** you'll see we have different types of resources that we can protect and we can assign multiple different types at one time. These are all just check boxes we could check. And the same frequency and retention model of the backup plan that we created will apply to all these resources. In this case, we'll just go with EFS here. Once we've selected that, we get another option here to choose the actual EFS. And we're asked for an ID, but luckily this dropdown is going to allow us to check those off individually. It will load the file systems that are known to the system and allow us to select those. So we've got our EFS file system established here. If we had a more expansive rule, we could come here and exclude certain things. So we've got a lot of options here as to how we can target the backup to just certain resources. In fact, in this last part here, we could skip entirely the selection of resources based on type and do it based on tags. And tags are a very powerful way in Amazon to focus on certain resources, whether it's for your cost reporting or for integrating with a third party that does things based on tags. That [[Metadata]] that you can put on resources can be really useful and this is one place that you could do that. So we've got our one EFS selected here and now we can go ahead and click Resource selection. That's kind of a funny named button because what you're really doing is confirming the resource selection that we just did. Now because we were able to do the schedule and the resource assignment all in the context
>
> **[6:13](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-and-configure-an-aws-backup-job?u=76281980&t=373)** of that wizard we haven't actually had the opportunity to go back and add any more rules to our backup. So before we're done, I'd like to demonstrate how that works by going here to Backup rules here in the Summary of the entire backup plan. And we can click Add backup rule. So we can give a different rule to supplement the daily. We'll call it MonthlyBackup. Keep the default vault, switch backup frequency to monthly. And we can tweak these or we can leave them at default, but we'll transition to cold storage after nine months. So we'll keep these around longer. And retention we'll say we'll retain it for two years before anything gets finally deleted. Now you can click Add backup rule. So you can see how you can start to layer and build up your backup policies to give you just what you need. Okay, so now we've created this backup plan but we don't really want to wait around in this demo for the schedule to kick in. So what we want to do is go run it manually. We'll go to the dashboard here on the side and click Create on-demand backup. So from here we can select our resource type of EFS. We can choose that file system. And we can choose Create backup now. We'll leave everything else at default and click Create on-demand backup. So you can see that's running and it won't take very long because, as you probably noted before, there's not a lot on the EFS that we're demoing here. Not a lot of data shouldn't take long to back up.
>
> **[7:48](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/create-and-configure-an-aws-backup-job?u=76281980&t=468)** And, alright, it's already completed. Let's head back over to the EC2 host. And we're working along and something very bad is about to happen. Oh dear. We have deleted the very important documents. That is a big problem. But luckily we have the AWS backup ready to save the day in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (2), [[JSON]] (1), [[Metadata]] (1)
> **Env Vars:** efs (13), aws (7), ec2 (2), json (1), rds (1)
> **CLI Commands:** aws (7), go run (1)
> **UI Navigation:** go to (2), dropdown (2), scroll down (1)
> **Ports:** :00 (1)
> **Cross-References:** in the next (1)
> **Tools:** aws console (1)
> **Exercise Files:** template (1)

#### [Restore from backup](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/restore-from-backup?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/restore-from-backup?u=76281980&t=0)** - [Instructor] Okay, so we've just had a problem. We've deleted some important documents out of our EFS file system, and we're here at the AWS Backup dashboard to see what can be done about it. The easiest way to get into our backups is to go to protected resources. Makes you feel very secure, your EFS is protected. You can see here we have two entries. Remember, that one of these was from the automatic backup that was offered to me by the EFS Creation Wizard. The second one is the one that we just created. And you can see I've got a couple of different backups here, including the one that I manually kicked off just a few moments ago. I can click on the radio button next to that and click Restore. Now I have the option for full restore or item level so I could be specific about certain things I want to come out of this restore. I'll keep that at full. And I can either go to a directory in the source file system or a completely new file system. I don't want to have to go to the trouble of mounting a new EFS, so we'll just say restore to a directory in the source file system. The default restore role is fine and we'll click Restore Backup. Now, this process may take a little longer than the actual creation of the backup did. So we'll come back in a few moments when this thing is complete and see what has happened. Okay, now the backup is completed. We've still got this bar at the top that says it may take several hours, but of course, this is dependent, largely, on the size of the backup that's being restored. So we didn't have a lot of data to bring back and it finished very quickly. Let's go back to the [[Amazon EC2|EC2]] Host
>
> **[1:32](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/restore-from-backup?u=76281980&t=92)** and take a look at what we've got. If I do LS now, you can see I don't have my very important documents folder back but what I do have is a new folder that's named for Database Backup Restore. So we'll go ahead and go into there and take a look. And here we have, I'll make a little space here, we have everything. We have File one, two and three, we have the data and there it is, very important documents. So we are back in business with the files that were deleted thanks to AWS Backup.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (1)
> **Env Vars:** efs (4), aws (2), ec2 (1)
> **CLI Commands:** aws (2), ls (1), make (1)
> **UI Navigation:** go to (3), click on (1)
> **Cross-References:** go back to (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [AWS Backup teardown](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-backup-teardown?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-backup-teardown?u=76281980&t=0)** - [Instructor] All right, as with everything, we're going to go back through what we created as a result of this demo and delete things to make sure we don't incur any unexpected charges. So let's go into AWS Backup and we'll go to Backup plans and we'll try to kind of work backwards here. We'll click MyBackupPlan, and the first thing I'm going to do is go delete these rules here. So daily backup, I'll confirm the name, delete it. Same for monthly backup. Oh, it's the last one on here. So it looks like I'm not able to delete that. Same I'm assuming for, no, not so much for resources. We can delete that there. So let's type EFSDemoAssignment and delete that. Now we will go up to the top here, click the Delete button, type MyBackupPlan, and yeah, there we go. Delete the whole thing. Now this backup plan, remember if you happen to have that same as I do, is associated with the EFS. I do not think that we'll be able to delete it until we delete the EFS, but we can try it. Go ahead and copy the name here, paste it in, and sure enough, okay, we're getting a permissions error because it's associated with the EFS system. We'll go into Backup vaults. Now we do have inside the default vault one recovery point. See that's the actual backup that was created. So we can go there, click it, click its checkbox, go to actions, and choose delete. This is also one way that you could get to the Restore menu.
>
> **[1:37](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/aws-backup-teardown?u=76281980&t=97)** So that's submitted for deletion and we'll get deleted here in a moment. Okay, now that's deleted and it should take care of everything that we created for AWS Backup. Meanwhile, I'm going to clean up the other resources that were a part of this demo. Going to [[Amazon EC2|EC2]] here. Click on my running instances and I will terminate the EFS that we logged into, confirm that, and wait for that to go into a terminated state. Okay, now I'm going to go to EFS and finally delete the file system. Here, I call it EFSDemo2, and I'll click Delete. I'll need a copy of the file system ID in there, making sure there's no additional spaces, and click confirm. That's now deleting the file system in addition to the EC2 and all the backups. So with that, we should have cleaned up everything associated with the AWS Backup Demo.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon EC2|Ec2]] (2)
> **Env Vars:** efs (5), aws (3), ec2 (2)
> **CLI Commands:** aws (3), make (1)
> **UI Navigation:** go to (2), checkbox (1), click on (1)
> **Speakers:** - [instructor] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/aws-storage-and-data-management-17535108/next-steps?u=76281980&t=0)** - [Brandon] That does it for this course. We've seen the breadth of what AWS has to offer in terms of storage and [[Data Management]]. To go further, it's hard to find a more in-depth resource than AWS's own documentation at aws.[amazon.com/documentation](https://amazon.com/documentation). Of course, there's plenty more AWS content in the video library of this site. I'd recommend [[Amazon Web Services (AWS)|Amazon Web Services]] Enterprise Security to learn more about IM and Access Control. You may also want to check out [[Apache Spark]] Essential Training to help you better understand AWS Glue. And finally, you could go into one of my other courses Amazon Web Services Deployment, Provisioning, and Automation, to learn how to get your applications to the cloud. Thanks for watching and good luck as your journey to the cloud continues.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Amazon Web Services (AWS)|Amazon web services]] (2), [[Data Management]] (1), [[Apache Spark]] (1)
> **CLI Commands:** aws (5), find (1), apache (1)
> **Env Vars:** aws (4)
> **URLs:** [amazon.com](https://amazon.com) (1)
> **Speakers:** - [brandon] (1)


## Instructor

- [[Brandon Rich]]

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/aws-storage-and-data-management-3082086)

## Skills Covered

- Storage Management
- Data Management
- Amazon Web Services (AWS)

## Path Context

### In [[Infrastructure Management on AWS- Skills for Administrators]]
← [[Complete Guide to AWS Security and Compliance Management]] | **5 of 5**

## Appears In

- [[Infrastructure Management on AWS- Skills for Administrators]]

## Related Courses

_Courses sharing skills:_

- [[Practical Database Design- Implementing Responsible Data Solutions with SQL Querying]] — Data Management
- [[JavaScript- Best Practices for Data]] — Data Management
- [[Advanced Python- Working With Data]] — Data Management
- [[AWS- Monitoring, Logging, and Remediation (2021)]] — Amazon Web Services (AWS)
- [[Complete Guide to AWS Software Deployment]] — Amazon Web Services (AWS)

---

[↑ Back to top](#)