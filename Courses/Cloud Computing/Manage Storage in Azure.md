---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: manage-storage-in-azure
url: "https://www.linkedin.com/learning/manage-storage-in-azure"
duration_minutes: 288
duration: 4h 48m
level: Intermediate
updated: 10/12/2023
learners: 15901
skills:
  - Microsoft Azure
  - Cloud Storage
exercise_files: true
github: "https://github.com/LinkedInLearning/manage-storage-in-azure-4413556"
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQGz9LokEdqyeg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1696813122151?e=2147483647&amp;v=beta&amp;t=c1OT2DujyEF-D8FgCZjziYOopQvCe13wNGwMKAzXFEQ"
linkedin_topic: Cloud Computing
learning_paths:
  - '[Infrastructure Management on Microsoft Azure- Top Skills for IT Professionals](../../Paths/Cloud%20Computing/Learning%20Paths/Infrastructure%20Management%20on%20Microsoft%20Azure-%20Top%20Skills%20for%20IT%20Professionals.md)'
prev_courses:
  - '[Microsoft Azure- Networking Concepts](Microsoft%20Azure-%20Networking%20Concepts.md)'
next_courses:
  - '[Controlling Cost with Azure Management and Billing](Controlling%20Cost%20with%20Azure%20Management%20and%20Billing.md)'
path_nav: '[{"path":"Infrastructure Management on Microsoft Azure- Top Skills for IT Professionals","position":7,"total":8,"prev":"Microsoft Azure- Networking Concepts","next":"Controlling Cost with Azure Management and Billing"}]'
path_count: 1
tags:
  - course
  - topic/cloud-computing
  - skill/microsoft-azure
  - skill/cloud-storage
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Cloud%20Computing/Manage%20Storage%20in%20Azure.md)

![Manage Storage in Azure](https://media.licdn.com/dms/image/v2/D560DAQGz9LokEdqyeg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1696813122151?e=2147483647&amp;v=beta&amp;t=c1OT2DujyEF-D8FgCZjziYOopQvCe13wNGwMKAzXFEQ)

# Manage Storage in Azure

> A strong understanding of cloud storage is vital not only for successfully managing data, but also for optimizing cloud spend. In this course, instructor Nicole Stevens covers important aspects of storage in Azure, including code-related deployments, deploying environments with storage accounts via code and Azure Bicep, along with how to authorize access through SDKs for developers. This demo-focu

> [LinkedIn Learning](https://www.linkedin.com/learning/manage-storage-in-azure) | 4h 48m | Intermediate | 16K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Understanding storage in Azure](#understanding-storage-in-azure)
  - [What you should know](#what-you-should-know)
- [**1. Overview of Azure Storage**](#1-overview-of-azure-storage) (10 videos)
  - [Azure Storage and the storage services](#azure-storage-and-the-storage-services)
  - [Azure Storage types and performance tiers](#azure-storage-types-and-performance-tiers)
  - [Creating a storage account in the Azure portal](#creating-a-storage-account-in-the-azure-portal)
  - [Working with Azure Storage data in the portal](#working-with-azure-storage-data-in-the-portal)
  - [An overview of Azure Table storage and Azure Storage Queues](#an-overview-of-azure-table-storage-and-azure-storage-queues)
  - [Working with Azure Storage data in Azure Storage Explorer: Adding Tables, entities, queues, and messages](#working-with-azure-storage-data-in-azure-storage-explorer-adding-tables-entities-queues-and-messages)
  - [Migrating and transferring data into Azure Storage](#migrating-and-transferring-data-into-azure-storage)
  - [Migrating and transferring data into Azure Storage using AzCopy](#migrating-and-transferring-data-into-azure-storage-using-azcopy)
  - [Hierarchical namespace and SFTP for blobs](#hierarchical-namespace-and-sftp-for-blobs)
  - [Hierarchical namespace and SFTP for blobs in practice](#hierarchical-namespace-and-sftp-for-blobs-in-practice)
- [**2. Redundancy, Disaster Recovery, and Replication**](#2-redundancy-disaster-recovery-and-replication) (5 videos)
  - [High availability and durability for Azure Storage](#high-availability-and-durability-for-azure-storage)
  - [Backing up Azure file shares and operational backup for blobs](#backing-up-azure-file-shares-and-operational-backup-for-blobs)
  - [Backing up Azure file shares and blobs demo](#backing-up-azure-file-shares-and-blobs-demo)
  - [Disaster recovery and failover](#disaster-recovery-and-failover)
  - [Object replication for blob data](#object-replication-for-blob-data)
- [**3. Networking and Infrastructure as Code for Azure Storage**](#3-networking-and-infrastructure-as-code-for-azure-storage) (7 videos)
  - [Storage account firewalls and virtual network access](#storage-account-firewalls-and-virtual-network-access)
  - [Private endpoints for Azure Storage](#private-endpoints-for-azure-storage)
  - [Create and manage a storage account with Azure PowerShell](#create-and-manage-a-storage-account-with-azure-powershell)
  - [Executing Azure PowerShell in scripts with the Cloud Shell code editor](#executing-azure-powershell-in-scripts-with-the-cloud-shell-code-editor)
  - [Create and manage a storage account with the Azure CLI](#create-and-manage-a-storage-account-with-the-azure-cli)
  - [Deploy infrastructure for Azure Storage using Azure Bicep](#deploy-infrastructure-for-azure-storage-using-azure-bicep)
  - [Deploy infrastructure for Azure Storage using Azure Bicep demo](#deploy-infrastructure-for-azure-storage-using-azure-bicep-demo)
- [**4. Authorizing Operations on Azure Storage**](#4-authorizing-operations-on-azure-storage) (7 videos)
  - [The control plane and the data plane](#the-control-plane-and-the-data-plane)
  - [Authorize with shared keys](#authorize-with-shared-keys)
  - [Authorize operations with Entra ID and Azure RBAC](#authorize-operations-with-entra-id-and-azure-rbac)
  - [Authorize operations with Azure AD and Azure ABAC storage](#authorize-operations-with-azure-ad-and-azure-abac-storage)
  - [Accessing storage account data from other Azure services](#accessing-storage-account-data-from-other-azure-services)
  - [Enable passwordless Entra ID–based access to Azure Storage](#enable-passwordless-entra-idbased-access-to-azure-storage)
  - [Shared access signatures and access delegation](#shared-access-signatures-and-access-delegation)
- [**5. Mounting and Synching to Azure File Shares**](#5-mounting-and-synching-to-azure-file-shares) (3 videos)
  - [Setting up Azure File Sync](#setting-up-azure-file-sync)
  - [Mounting a file share to Windows Server](#mounting-a-file-share-to-windows-server)
  - [Mounting a file share to Linux](#mounting-a-file-share-to-linux)
- [**6. Protecting Azure Storage Data**](#6-protecting-azure-storage-data) (6 videos)
  - [Encryption at rest and in transit](#encryption-at-rest-and-in-transit)
  - [Encryption at rest, encryption scopes, and encryption in transit in practice](#encryption-at-rest-encryption-scopes-and-encryption-in-transit-in-practice)
  - [Soft delete and versioning for blobs, containers, and file shares](#soft-delete-and-versioning-for-blobs-containers-and-file-shares)
  - [Change feed for blob data](#change-feed-for-blob-data)
  - [Point-in-time restore for blob data](#point-in-time-restore-for-blob-data)
  - [Immutable storage for business-critical blob data](#immutable-storage-for-business-critical-blob-data)
- [**7. Managing Cost and Monitoring Azure Storage**](#7-managing-cost-and-monitoring-azure-storage) (6 videos)
  - [Billing, reserved capacity, and network routing preference for Azure Storage](#billing-reserved-capacity-and-network-routing-preference-for-azure-storage)
  - [Storage tiers and blob rehydration](#storage-tiers-and-blob-rehydration)
  - [Manage storage tiers for blobs and files](#manage-storage-tiers-for-blobs-and-files)
  - [Lifecycle management policies and rules](#lifecycle-management-policies-and-rules)
  - [Monitoring Azure Storage](#monitoring-azure-storage)
  - [Monitoring Azure Storage with storage insights and workbooks](#monitoring-azure-storage-with-storage-insights-and-workbooks)
- [**Conclusion**](#conclusion) (1 videos)
  - [Learning more about Azure management](#learning-more-about-azure-management)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Understanding storage in Azure](https://www.linkedin.com/learning/manage-storage-in-azure/understanding-storage-in-azure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/understanding-storage-in-azure?u=76281980&t=0)** - By 2025, it is estimated that between 150 to 200 zettabytes of data will be created, with large enterprises tripling their [Unstructured Data](../../Skills/Software%20Development/Unstructured%20Data.md) storage capacity of files and blobs when compared to today. With over 50% of organizations experiencing data breach in the last two years and cloud spend waste estimated at 28%, it is no surprise that the latest State of the Cloud report places managing spend, security, and lack of expertise as the top challenges facing organizations in the cloud. My name is Nicole Stevens and I've been architecting [Data Storage](../../Skills/Data%20Science/Data%20Storage.md) solutions for over 20 years. Join me as we explore managing [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage to help you start building the expertise to overcome these challenges.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Unstructured Data](../../Skills/Software%20Development/Unstructured%20Data.md) (1), [Data Storage](../../Skills/Data%20Science/Data%20Storage.md) (1), [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (1)
> **Cross-References:** in the last (1)
> **Speakers:** - by (1)

#### [What you should know](https://www.linkedin.com/learning/manage-storage-in-azure/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/what-you-should-know?u=76281980&t=0)** - [Instructor] This course starts with beginner level concepts and a walkthrough in the [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) portal to create a storage account. But quickly moves into an intermediate level, therefore, a good understanding of how to navigate around the portal is essential. [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning has some excellent short courses on working with the Azure portal, such as this one, "Navigating the Azure portal." There are also sections of this course that require connecting through the remote desktop to a virtual machine. If this is not something you have attempted before or you need to brush up on this, there are further short courses on LinkedIn Learning, including this one on connecting to [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) in Azure. The companion [GitHub](../../Skills/Software%20Development/GitHub.md) repository to this course contains [Infrastructure as Code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) files to create each environment for you, so that you can follow along live if you wish. Each top level folder within the GitHub repository matches with a chapter, and within each chapter, there is a folder for each environment with a readme file, explaining the commands required to create the environment at the folder level. To match the instructions to the environment, you can click through on the instructions from the Video title table. The commands in the readme have been designed to run in the Azure Cloud Shell, which can be accessed using this button from within the portal. Just drag that up. You'll need to be working from within the Bash shell, and then clone the GitHub repository, which you can do
>
> **[1:37](https://www.linkedin.com/learning/manage-storage-in-azure/what-you-should-know?u=76281980&t=97)** by returning to the root of the repository, clicking on the Code button, copying the SSH command, back into the portal, and using [Git](../../Skills/Software%20Development/Git.md) clone.
>
> **[1:54](https://www.linkedin.com/learning/manage-storage-in-azure/what-you-should-know?u=76281980&t=114)** Once the repository is command, you can use the command from the readmes to create each environment. If you are unfamiliar with Git and GitHub, there are multiple courses available on LinkedIn Learning, including this full course on Git essentials. Use of Git when managing cloud environments is a key skill, so I'd very much recommend checking these out. The walkthroughs for each video are deployed to a resource group. Therefore, if you'd like to keep your costs down, make sure you understand how to delete a resource group, whether that's from the command line, or as I'm sharing here, from within the portal. I would also recommend setting up Visual Studio Code if you would like to follow along live, especially for the Infrastructure as Code sections using Bicep. VS Code is available for all major operating systems. And once installed, I would also recommend installing the Bicep extension, which enables IntelliSense and validation for your Bicep files, and the Azure extension, so that you can explore your subscription from inside VS Code as well as the portal. Throughout the course, we will be exploring the content of our Azure Storage accounts using Azure Storage Explorer. So if you'd like to follow along, I'd recommend installing the Storage Explorer from the link currently being displayed. That's everything you need to know and install to get the best out of this course. So let's get cracking on our storage journey by jumping into the first video, Azure Storage and the storage services.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (9), [GitHub](../../Skills/Software%20Development/GitHub.md) (4), [Git](../../Skills/Software%20Development/Git.md) (4), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (3), [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (2)
> **Tools:** github (4), azure portal (3), vs code (2), bash (1), command line (1)
> **CLI Commands:** git (4), ssh (1), make (1)
> **Definitions:** is a  (2)
> **Prerequisites:** you'll need (1), install (1)
> **Env Vars:** ssh (1)
> **Documentation:** the readme (1)
> **Analogies:** such as (1)


### 1. Overview of Azure Storage

[↑ Back to Table of Contents](#table-of-contents)

#### [Azure Storage and the storage services](https://www.linkedin.com/learning/manage-storage-in-azure/azure-storage-and-the-storage-services?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/azure-storage-and-the-storage-services?u=76281980&t=0)** - [Instructor] When thinking about transitioning an application to the cloud or building from scratch in the cloud, it is highly likely the application will need to persist some data. [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage is the solution in Azure for supporting modern [Data Storage](../../Skills/Data%20Science/Data%20Storage.md) requirements in the cloud. There are many benefits to a developer or administrator when working with Azure Storage. Azure Storage is accessible from anywhere over HTTP or HTTPS. Developers can access data through SDKs in many languages or through the Storage [REST](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) APIs. The Azure Portal and Azure Storage Explorer enable users to visualize and work with data through a UI. Azure Storage is secure when correctly configured. Data is encrypted at rest by default and fine-grained access control can be provided to your data, ensuring only permitted users and systems are granted access. It's scalable with deployment configuration options to match an application or project's data size and performance requirements. Azure Storage is managed by [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md). Therefore, maintenance, updates, and fixing of critical issues are taken care of for you. Azure Storage is highly available with redundancy built in which can be configured to protect from local outage or a natural disaster. In this video series, we will be reviewing each of these benefits and how to configure them.
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/azure-storage-and-the-storage-services?u=76281980&t=94)** Azure Storage is a shared pool of storage. It provides a namespace that can be used to deploy different storage services. Each of these services has been designed to cater for the various [Forms](../../Skills/Web%20Development/Forms.md) that modern data can take, from simple text files to documents and stream videos. Determining which storage service or combination of storage services you need for an application or project requires knowledge of each service's suitability to hold specific types of data. Let's break this down further by looking at each storage service listed on this slide. Blob storage is designed to hold huge amounts of [Unstructured Data](../../Skills/Software%20Development/Unstructured%20Data.md). To facilitate this, blob storage has three types of blobs: block blobs, append blobs, and page blobs. Block blobs are optimized for efficient upload of large amounts of text and binary data, making block blobs perfect for storing images, documents, media, and backup and archive objects. Block blobs can also be used to hold files for static websites. Append blobs are optimized for fast appends as data can only be added to the tail of an append blob. This makes workloads that require logging a perfect use case for an append blob. Page blobs are optimized for random read/write operations of 512-byte pages. This enables virtual disk storage
>
> **[3:05](https://www.linkedin.com/learning/manage-storage-in-azure/azure-storage-and-the-storage-services?u=76281980&t=185)** for [infrastructure as a service](../../Glossary/Concept/IaaS.md), also known as [IaaS](../../Glossary/Concept/IaaS.md), VMs on Azure. However, VHD's page blobs in a storage account are not Microsoft's recommended method for supporting disk storage for [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) in Azure. Azure Managed Disks are the recommended method of disk storage. Azure disks simplify disk management for Azure VMs by managing the storage account for you, abstracting away the storage account and VHD implementation. This means you no longer have to create a storage account to hold your disks. Azure does all this for you. This also means you no longer have to design around storage account limits for multiple disks. Some features of unmanaged disks were not available with managed disks historically. This is no longer the case, but increased capability is available for managed disks. Unmanaged disks are gradually being deprecated. And from the end of September 2025, all VMs with unmanaged disks will be stopped and deallocated. Azure Files is a fully managed file share service in Azure. The shares can be accessed by the network file system and server message block protocols and therefore can be mounted to [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) and [Windows](../../Glossary/Service/Windows.md) servers or macOS clients. REST API access can be direct or through SDKs which are available in multiple languages. Azure Files can also be used to persist state
>
> **[4:40](https://www.linkedin.com/learning/manage-storage-in-azure/azure-storage-and-the-storage-services?u=76281980&t=280)** for resources running containers in Azure such as Azure Container Instances, Azure Container Apps, and Azure [Kubernetes](../../Skills/DevOps/Kubernetes.md) service. A further service to be aware of is Azure NetApp Files which is an enterprise class file storage service in Azure. This is designed for high performance and low latency use cases. NetApp Files are not implemented through storage accounts. So to compare the two services, visit this link in the Azure documentation to Azure Files and NetApp Files. The Azure Queue Storage Service is for messages which have a default time to live of seven days, but can be configured to live indefinitely. Each message can have no more than 64K in size, but the storage queue size is only limited by the capacity limits of a storage account. Azure Queue Storage is often used to offload work to separate application components, decoupling the components, and therefore building in resilience to component failure. Azure Table Storage enables the storage of [NoSQL](../../Skills/Software%20Development/NoSQL.md) entities. These entities can be up to one meg size and are stored as one or more key value pairs. The structure of each entity in the table can be different across each of the entities. Table data is partitioned by its partition key which load balances the data across nodes
>
> **[6:15](https://www.linkedin.com/learning/manage-storage-in-azure/azure-storage-and-the-storage-services?u=76281980&t=375)** and then it is uniquely identified by including its row key. If you don't require complex searching of your data, Azure Table Storage is a fast, low-cost option to other NoSQL resource types in Azure, such as Cosmos DB which also has an implementation of Azure Tables. To compare Azure Tables and Azure Cosmos DB, follow this link to the Azure Table Storage documentation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (36), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (3), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (2), [NoSQL](../../Skills/Software%20Development/NoSQL.md) (2), [Data Storage](../../Skills/Data%20Science/Data%20Storage.md) (1)
> **Env Vars:** rest (2), vhd (2), http (1), https (1), api (1)
> **Definitions:** is a  (3), known as (1), is an  (1)
> **Analogies:** such as (2)
> **Best Practices:** recommended (2)
> **Code Identifiers:** macos (1)
> **Tools:** azure portal (1)
> **Warnings:** be aware (1)

#### [Azure Storage types and performance tiers](https://www.linkedin.com/learning/manage-storage-in-azure/azure-storage-types-and-performance-tiers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/azure-storage-types-and-performance-tiers?u=76281980&t=0)** - [Instructor] There are four types of [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage account available with features to support the different requirements of a workload's persisted data. The first we'll look at is the general purpose storage account. It is recommended by [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) for most Azure storage scenarios. It has built-in support for all the Azure storage services and all the storage redundancy options and we'll explore all these options in a later video. The standard general purpose V2 account has a five petabytes of capacity that can be extended on request to Azure support. There are no limits to the number of blobs, files, shares, queues, entities, or messages that can be used to fill up this capacity. Each storage account can handle up to 20,000 requests per second and standard storage accounts are backed by hard disk or HDD based [Hardware](../../Topics/Hardware.md), which for some workloads, will not deliver a high enough level of performance. Because of this, Azure offers three premium storage account types. These are backed by solid state drives or SSDs. However, the performance of each of these account types does bring in some trade-offs to enable this performance. Let's compare these for block blobs and file shares. Each of the premium storage accounts affixed to a specific service, so for premium block blobs, this is for block blobs only.
>
> **[1:37](https://www.linkedin.com/learning/manage-storage-in-azure/azure-storage-types-and-performance-tiers?u=76281980&t=97)** Also, a premium account has regional redundancy only with a single data center or multiple data centers in one region, and this is due to the latency requirements of a premium block blob account, as premium block blob accounts are optimized for high numbers of transactions with a low latency, enabling hundreds of thousands of requests per second. In some scenarios where a workload continually executes a large number of transactions, a premium block blob storage account may be more cost effective than standard storage. This is due to premium block blob storage having a high storage cost, but lower transaction cost when compared to a standard storage account. There will actually be a crossover point where premium accounts may become more cost effective. To read how you can calculate this for your workloads, please follow this link to the Microsoft documentation on premium block blob cost effectiveness. File shares also have a premium option with similar trade-offs between features, performance, and cost. Azure premium file shares will only allow the creation of shares and files with regional redundancy options only. The five petabyte capacity of a standard account means that multiple shares with large files enabled to the maximum of a hundred terabytes can be housed in the account. However, with a premium file share account,
>
> **[3:13](https://www.linkedin.com/learning/manage-storage-in-azure/azure-storage-types-and-performance-tiers?u=76281980&t=193)** the maximum capacity is a hundred terabytes with the same maximum file share size of 100 terabytes. One of the most important differences here are the billing models. A standard account is pay as you go. You will only be billed for what you use. Therefore, if you create a hundred terabyte share but only store a hundred giga files in it, you will only be charged for that 100 gig plus transaction costs. With a premium file share, you are charged for the full provision file share size so using the same example, you would be billed for the full 100 terabytes. Another key difference between the two is IOPS and throughput, standard file shares have a static IOPS of a thousand unless large files are enabled when it increases to 20,000 per account. But don't forget, the total IOPS for a standard storage account is 20,000 IOPS. So if you need to run multiple standard shares at 20,000 IOPS each, you're going to need multiple standard V2 storage accounts. With premium file shares, the IOPS grows to a hundred thousand with the provision capacity of the file share, along with throughput, which makes premium file shares perfect for smaller data objects. For detailed information on these figures, for planning and Azure files deployment, see this Microsoft Learn article for Azure file [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) and performance targets. There are still two legacy account types, standard V1,
>
> **[4:49](https://www.linkedin.com/learning/manage-storage-in-azure/azure-storage-types-and-performance-tiers?u=76281980&t=289)** which supports blobs, queues, files, and tables and standard Blob Storage for block and append blobs. Historically, standard account types were created with the classic deployment model, which some organizations may still be using. It is also possible in some scenarios that a legacy account may have been more cost effective, however, with classic deployment being deprecated and legacy storage pricing being brought in line with current storage pricing, if you have any legacy types in your organization, it is definitely time to switch. To this point, Microsoft does not recommend using these legacy types and they are no longer available for selection through the standard create storage account blade of the Azure portal.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (9), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (4), [Hardware](../../Topics/Hardware.md) (1), [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) (1)
> **Env Vars:** iops (6), hdd (1)
> **Best Practices:** recommended (1), don't forget (1)
> **Tools:** azure portal (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Creating a storage account in the Azure portal](https://www.linkedin.com/learning/manage-storage-in-azure/creating-a-storage-account-in-the-azure-portal?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/creating-a-storage-account-in-the-azure-portal?u=76281980&t=0)** - [Instructor] The storage account types and services that we have discussed so far are part of the minimum requirements to create a storage account in the Portal. So, we can switch to the [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Portal and run through how to create a storage account with just these essential options. To create a storage account, you can search for the storage account resources at the top of the Portal by clicking into the search bar and typing storage accounts. Storage accounts is now listed in the dropdown under services. If we click on it, we can enter the storage accounts blade. At the top right of the storage accounts blade, we can click on Create, and this will take us to the creation blade for a storage account. The storage account's creation blade now opens at the Basics tab where we can see all of the required values marked with red asterisks. The subscription is a container for creating resources and billing them in Azure. The login I'm using only has access to one subscription, but an organization could have multiple subscriptions, which this login could have access to. Each subscription can also be billed differently. For example, there are enterprise, pay as you go, and trial subscriptions that are available. A subscription contains one or more resource groups, where the resource group represents a logical grouping for the lifecycle of Azure resources.
>
> **[1:36](https://www.linkedin.com/learning/manage-storage-in-azure/creating-a-storage-account-in-the-azure-portal?u=76281980&t=96)** Here, I've created a resource group of 01_03 for all the resources I want to create for this Chapter 1 Video 3 demo. At the end of the demo, the lifecycle of the resource group's resources is complete so I will delete the resource group, which in turn deletes all of its resources. We scroll down, we can see the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the basic options. A storage account name must be globally unique for the whole of the Azure Cloud that you are using. For the majority of us, that is the Azure Public Cloud, but there are currently three other clouds for government, military, and data sovereignty uses. I'm going to give this storage account name ns0103example, and that will be checked on review to check that it is unique. My region has defaulted to UK South as that is the region of the resource group I'm creating this resource in. When thinking about the choice of region for your data, you need to consider local data residency laws and to check whether all the features you require are available for the region. We will cover this throughout the course. Performance is where you choose the storage account type discussed in a previous video. The current selection of standard is the standard general purpose v2 account. If you choose premium, you are now offered the choice of premium account type, which matches the three premium types
>
> **[3:09](https://www.linkedin.com/learning/manage-storage-in-azure/creating-a-storage-account-in-the-azure-portal?u=76281980&t=189)** discussed in a previous video. I want to be able to create blobs, files, queues, and tables in the storage account so I'm going to switch back to standard. The redundancy options will be discussed in a future video so I will leave these at the defaults. All the required options have now been entered and I can validate the selections by clicking Review. If the validation passes, the Create button is enabled and the storage account can be created, so I'll click the Create button to create the account. After a short time, the storage account is created. And if we click on Go to resource, we're taken to the overview blade for the newly created storage account. At the top of the overview blade is a display of all of the options chosen in the Basics tab, subscription, resource group, region, performance, and account kind. And on the Storage Account menu bar under the [Data Storage](../../Skills/Data%20Science/Data%20Storage.md) heading are the menu items for Containers, File shares, Queues, and Tables. This is due to the selection of a standard storage v2 account, which supports all of the services. Now we have a storage account, we can start to look at how to store some objects, and we'll cover that in the next two videos.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (5), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Data Storage](../../Skills/Data%20Science/Data%20Storage.md) (1)
> **UI Navigation:** click on (3), switch to (1), dropdown (1), scroll down (1), go to (1)
> **Cross-References:** previous video (2), in the next (1)
> **Definitions:** is a  (2)
> **Tools:** azure portal (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Working with Azure Storage data in the portal](https://www.linkedin.com/learning/manage-storage-in-azure/working-with-azure-storage-data-in-the-portal?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/working-with-azure-storage-data-in-the-portal?u=76281980&t=0)** - [Instructor] You've created your [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) storage account and are ready to start working with your data within the account, but how should you create a structure for your data to be stored? Each Azure storage service has a set of resources which are used together to create a structure for data to be stored. This structure is also used in some services to secure access to the data. We'll start by looking at Blob Storage. All services start with a storage account with blobs being organized by containers. You can have an unlimited amount of containers, and the containers are often named to organize the types of blobs stored within them. With blobs being stored inside a container and a container holding an unlimited amount of blobs up to the storage account capacity, blob containers cannot contain other containers. The hierarchy is flat, but blob names can contain a virtual directory, which enables grouping of blobs with the same prefix. Each of these components come together to make up the URL, which will access the blob. As this needs to be a valid URL, there are rules for naming of accounts, containers, and blobs. For more information on this, see this link to the naming and referencing documentation for blobs. This is a storage account that was created in the previous demo. To work with blob data in a storage account, you can access Containers on the left-hand menu or Blob service on the Properties tab of the Overview blade. This screen shows the list of blob containers. There is only the $logs container
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/working-with-azure-storage-data-in-the-portal?u=76281980&t=94)** as this is required for functionality that's built into the storage account when it was created, and no other containers have been created as this account is new. So let's click at Container at the top and walk through how to create one. Adding a name, (keyboard clacks) we can see that the naming rules are enforced in the portal, so I actually have to set this to all lowercase. Now blob containers are private by default, requiring a level of authorization to interact with the container and its data. Well, we'll cover this later in the course, so I'll set this container to publicly accessible and click Create, and the container is created. Clicking on the newly created container, a blob can be uploaded by clicking the Upload button, then we can either drag a blob in to this square here to Drag and drop files, or I can browse for files on my local machine, choosing the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) logo and clicking Open and Upload. If I close down the upload pane, the blob has been created and this has created the blob at the container level. If I'd wanted to use a virtual directory, I could click Upload once more, can choose the same blob again, or any other blob to that matter. But I need to go into Advanced and choose Upload to folder, and I'm going to choose media. Scrolling down and clicking Upload,
>
> **[3:11](https://www.linkedin.com/learning/manage-storage-in-azure/working-with-azure-storage-data-in-the-portal?u=76281980&t=191)** closing the upload to blob pane. We now have a media virtual directory, and entering the media directory, the LinkedIn logo blob looks like it's stored in a directory, but this is actually a flat file structure, represented this way in the portal. If we go to the ellipses against the blob and click to view Properties and copy its URL and paste it in, we can see the makeup of the blob URL with the container, the virtual directory, and the blob itself. And hitting Return, because this blob is public, the blob can be downloaded. The resources for Azure files has a slightly different setup. Starting as always with the account is the namespace. One or more file shares can be created and underneath these, one or more directories. Directories can also be nested to create the file system structure that most end users are comfortable using. Files can be uploaded to any directory level, including the share level itself. To access a file by a URL, the format is almost identical to that of blobs, except the path must reflect the directory structure the file is under. For example, look at this quote path as shown from the slide. Going back to the portal and the storage account created in the first demo, you can create Azure file shares by entering the File shares menu on the left-hand side,
>
> **[4:46](https://www.linkedin.com/learning/manage-storage-in-azure/working-with-azure-storage-data-in-the-portal?u=76281980&t=286)** or through the Properties tab, scroll down by entering the File service. This is a fairly new account, so there are no file shares at the moment. You can create one by clicking Add File share at the top left, and we'll need to name the share. Now the same naming rules apply. We cannot use a capital letter, so I'll set it to marketing with a little M. Now you can get more information on naming shares directory and files through the link that's currently being displayed on screen. Now this is a standard v2 account, so the file share automatically sets that it will grow up to five terabytes. I'll click Create and Review and Create, and the share is created. Now the marketing share is created, we have the option to upload a file or add a directory. Now I'm going to build out the marketing example from the slides, so we'll start with adding an assets directory. (keyboard clacks) And we'll just pop over to Browse and I can enter the assets directory. And if you remember that slide, underneath, there was a logo directory too. Can enter the logo directory, and at this point, I'd like to upload my file. So I'll choose the LinkedIn logo that we had before in the blob demonstration and clicking Upload and closing down these panes,
>
> **[6:22](https://www.linkedin.com/learning/manage-storage-in-azure/working-with-azure-storage-data-in-the-portal?u=76281980&t=382)** and into the ellipses and Properties. If we expect the properties and copy the URL and add it into the browser, we can see the makeup of the file path is reflected in the URL with marketing, assets, logo, and the file itself. And hitting Enter, we cannot download this logo and this is because Azure file shares cannot be set to be publicly available like blob containers. If I return to the Overview page of the storage account, (computer mouse clicks) we can also manage data in the portal through the Storage browser. If I enter File shares and the marketing file share, the menu options are almost identical to those shown within the storage service, and then drill through the directories to the files. There is also this option here to copy files between directories. Now this option is also present for blobs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (5), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (3)
> **Env Vars:** url (7)
> **UI Navigation:** drag and drop (1), go to (1), scroll down (1)
> **Definitions:** is a  (3)
> **CLI Commands:** make (1)
> **Cross-References:** later in (1)
> **Analogies:** for example (1)
> **Prerequisites:** setup (1)

#### [An overview of Azure Table storage and Azure Storage Queues](https://www.linkedin.com/learning/manage-storage-in-azure/an-overview-of-azure-table-storage-and-azure-storage-queues?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/an-overview-of-azure-table-storage-and-azure-storage-queues?u=76281980&t=0)** - [Instructor] The [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Portal is a great place to start when creating storage components and working with data in Azure, but there are many other options including Azure Storage Explorer with its desktop client for [Windows](../../Glossary/Service/Windows.md), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md), and macOS. Before we jump into the Data Explorer, there are two final storage services we need to cover, starting with Azure Tables. The structure of Azure Tables starts underneath the storage account with one or more tables that can be created, which in turn hold one or more entities where an entity is a row in a table that has properties which are key value pairs. The first two properties are mandatory. The partition key is used to partition the data across partition servers. Therefore, for optimal balancing of load to the servers, a good partition key will distribute the load. The row key uniquely identifies the entity in the partition and together with the partition key uniquely identifies the entity in the table. Only partition and row keys are indexed. So if you are performing a point read, which is a read which uses just a partition key and row key to [Fetch](../../Skills/Web%20Development/Fetch.md) one row, it will be efficient, but any other type of read will be slow as the table grows. This is one of the disadvantages of Azure Table Storage. On insert or update, the timestamp is added or updated by default. You can then add up to 252 other properties to an entity. And for each entity, the number of properties can differ
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/an-overview-of-azure-table-storage-and-azure-storage-queues?u=76281980&t=94)** and be of different types and names. For further information of the types of properties that can be added and their naming rules, please follow this link to the table service model. With Azure queues, an account can hold unlimited queues and a queue can contain unlimited messages up to the max queue size of 500 terabytes. A message is a string, which can hold up to 64K in text. This means millions of messages can be held in Azure Queue Storage. And Azure Queue doesn't guarantee that messages are delivered in order. But if this is not an issue for your application, Azure queues are a really cost-effective asynchronous messaging service. For information on naming queues and [Metadata](../../Skills/Web%20Development/Metadata.md), please follow the link on this page. Now we'll take a look at working with the data we have discussed in the Azure Storage Explorer. This is the Storage Explorer, and once it loads, you need to connect to the resources to be able to view them either through an [Azure AD](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) account, which enables access to single resources or an entire subscription, or by using further authentication methods which we'll discuss later in this course. I'm going to sign in with my instructors Azure AD account to get access to my subscription. I'll choose the Azure Public Cloud where my account and subscription are held and the login page is loaded and I'll select my identity and it says that I'm now authenticated
>
> **[3:09](https://www.linkedin.com/learning/manage-storage-in-azure/an-overview-of-azure-table-storage-and-azure-storage-queues?u=76281980&t=189)** because I was already logged in in an earlier session so I can close the browser and I'm now signed in and we can see this as my Azure subscription is loaded. Clicking Open Explorer and expanding the subscription, I can navigate through the storage resources that I have access to using this tree view by clicking on the storage accounts. For example, this one was created in the very first demo. And if you'd watched at the bottom left-hand side of the screen, the properties of the storage account were loaded down at the bottom here. Expanding the storage account itself, I have access to all of the Azure Storage services. Drilling in further on the blob containers, we can see the log container that I added and the blobs that were uploaded through the Azure Portal. We can also see the file shares that were added, the marketing share and its directories. And once again, the file that was uploaded to those directories through the Azure Portal. You may have noticed that at each point as I have navigated through, the properties of the resource are shown in the bottom left and the menu items change that allow you to manage that specific resource at the top. It's also possible to right-click on a resource to manage it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (16), [Azure ad](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) (2), [Windows](../../Glossary/Service/Windows.md) (1), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Definitions:** is a  (4)
> **Tools:** azure portal (3)
> **Code Identifiers:** macos (1)
> **Cross-References:** later in (1)
> **UI Navigation:** right-click (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Working with Azure Storage data in Azure Storage Explorer: Adding Tables, entities, queues, and messages](https://www.linkedin.com/learning/manage-storage-in-azure/working-with-azure-storage-data-in-azure-storage-explorer-adding-tables-entities-queues-and-messages?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/working-with-azure-storage-data-in-azure-storage-explorer-adding-tables-entities-queues-and-messages?u=76281980&t=0)** - [Lecturer] Let's explore this experience further by adding a table and an entity. As I've expanded the table service, we can see a list of tables here. These tables are used internally for storage account metrics. I don't want to use these tables, I want to create my own. So I will right click on the table service and choose create table and I'll give it a name of [products](../../Skills/Software%20Development/Microsoft%20Products.md). The screen on the right has now switched to a view of the entities in the table which we haven't got any at the moment. So clicking to Add to create an entity I must add a PartitionKey. In this case, we're going to use countries as a partition and then I must also add a RowKey. So I'm going to use my product code as a RowKey. And carrying on with the example data from the slide deck I'm going to add some more properties to create this entity. So the first one was Cost, which has a type of Double and that was 2.99. The second was a Size of a String of Large. Now I have all my entities. I can click Insert and it is displayed within the table including the two extra properties I added. If I choose to add another entity,
>
> **[1:33](https://www.linkedin.com/learning/manage-storage-in-azure/working-with-azure-storage-data-in-azure-storage-explorer-adding-tables-entities-queues-and-messages?u=76281980&t=93)** I am shown the structure of the last entity that was added. But because this is a no [SQL](../../Skills/Data%20Science/SQL.md) type of data it doesn't have to have the same properties. So I can get rid of the Cost and the Size, add in another PartitionKey and another RowKey of a product number. But this time I'd like to add the Colour. So I'm going to set to Red, and that's all I want for this entity. That's all the properties that I need. So clicking Insert, I can see my two entities listed. But moving to the right, I can see that the properties have been updated with null's place for properties that aren't being used. Now, this is actually quite manual, so note at the top I can import a CSV file if I wish to, which will give me a quick method for processing some data. Working with Queues has a similar workflow. I can right click and create a queue. In fact, I'm going to choose to create two queues. The first one called ready, and the second one called finished. By clicking on a queue, I can choose to add a message within the queue. (keyboard clacking) I've added some text. This text can be up to 64k in size, but as this is Base64 encoded this message can only be 48k in size.
>
> **[3:09](https://www.linkedin.com/learning/manage-storage-in-azure/working-with-azure-storage-data-in-azure-storage-explorer-adding-tables-entities-queues-and-messages?u=76281980&t=189)** Clicking OK, the message is added to the queue. Having a setup like this with two queues is a simple example of how processing can be decoupled using [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage Queues. A compute resort can get the messages from the ready queue, process them, and then place the message on the finish queue to notify that the processing has ended. You could set up an example implementation of this using a Logic app which has triggers that detect inserts of messages into queues. It could then pick up and push the message to the second queue. But as you can see, but as you can see this method of processing could provide a much more powerful decoupled workflow.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1), [SQL](../../Skills/Data%20Science/SQL.md) (1), [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (1)
> **Env Vars:** sql (1), csv (1)
> **Definitions:** is a  (2)
> **Prerequisites:** setup (1), set up (1)
> **Versions:** 2.99 (1)
> **UI Navigation:** click on (1)
> **Speakers:** - [lecturer] (1)

#### [Migrating and transferring data into Azure Storage](https://www.linkedin.com/learning/manage-storage-in-azure/migrating-and-transferring-data-into-azure-storage?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/migrating-and-transferring-data-into-azure-storage?u=76281980&t=0)** - [Instructor] When migrating or transferring data to [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage, there are three key factors that will influence your choice of migration tool or service. Firstly, how much data do you need to transfer? If it's a few gigabytes, then it's likely it can happen online. But if you're looking at terabytes or petabytes of data, then bandwidth is going to come into play. Poor or inconsistent bandwidth is likely to push a migration down the offline route. The third factor is likely to affect your choice of tooling. Is this transfer a one-off migration or something that is scheduled or triggered periodically? The first service we'll look at is aimed at large datasets with little to no bandwidth on offer for the transfer. This is the Data Box family of Data Box Disk, Data Box, and Data Box Heavy. These are all [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) managed services primarily aimed at large data offline migrations. With this service, you order a device suitable for your requirements through the portal and Microsoft deliver you one or more SSDs or a ruggedize device. You then copy your data to these devices and send them back to Microsoft to automatically load into a designated storage account. Although the main driver is likely to be sized when choosing a Data Box, note the usable size is slightly smaller. You may also need the higher levels of encryption
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/migrating-and-transferring-data-into-azure-storage?u=76281980&t=94)** or to load to multiple storage accounts, in which case you will need at least Data Box. With transfer of an increased dataset size, you are likely to require higher transfer speeds, so interface options differ to match this from USB 3.1 speeds to 4 by 40 gigs for QFPS+. The customer managed option for offline data transfer is an import/export job. Here, the customer provides their own drives and must arrange their own method of shipping. The customer uses a Microsoft provided tool which determines the number of drives required, preps the drives, facilitates the data copy, and encrypts the data. The Azure Portal is then used to create a job so the customer knows where to ship the drives to. Import/export supports blobs and files, but has an extra feature where data can be exported from Azure Storage Blobs to on-premises. One excellent use case for this is data recovery from Azure Storage. Along with its offline capability, the Azure Data Box family has Azure Stack Edge and Data Box Gateway for transferring large datasets with high bandwidth to Azure blobs and files. These can include data from on-premises servers transferred as migration, or scheduled and continuous transfer often used for [IoT](../../Glossary/Concept/IoT.md) devices. The Azure Stack Edge device also comes with compute at the edge, enabling pre-processing of data
>
> **[3:07](https://www.linkedin.com/learning/manage-storage-in-azure/migrating-and-transferring-data-into-azure-storage?u=76281980&t=187)** which can mean less data is transferred to Azure. For transferring large datasets where high bandwidth is available, Azure Data Factory and the command line tools AzCopy, Azure [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md), and Azure [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) can be used to transfer data online. Azure Data Factory has connectors to multiple resources, including Azure storage. Its code-free UI runs from the portal where [Data Pipelines](../../Skills/Data%20Science/Data%20Pipelines.md) are designed. These can be triggered and scheduled. Azure Data Factory is a full ETL service and therefore can be costly. But if transformation of data is required before loading, then Data Factory may be the best option. The command line tools support blobs, files, and table storage. As command line tools, the commands can be scripted where they can be scheduled through [Windows](../../Glossary/Service/Windows.md) Task Manager or [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) cron jobs. With fees limited to egress, if your transfer scenario is a one-off or can be scheduled, then tools such as AzCopy are likely to be the most cost effective. When transferring a small dataset which is often an ad hoc operation, the recommended UI tools are the Azure Portal and Azure Storage Explorer which we have already seen in previous demos. We can also use the command line tools and we'll be spending some time with Azure CLI and PowerShell in later videos. So let's have a look at how AzCopy
>
> **[4:40](https://www.linkedin.com/learning/manage-storage-in-azure/migrating-and-transferring-data-into-azure-storage?u=76281980&t=280)** can be used to transfer data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (18), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (4), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (2), [IoT](../../Glossary/Concept/IoT.md) (1)
> **Tools:** command line (4), azure portal (2), powershell (2)
> **Env Vars:** cli (2), usb (1), qfps (1), etl (1)
> **Definitions:** is a  (2), is an  (1)
> **Versions:** 3.1 (1)
> **Analogies:** such as (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [instructor] (1)

#### [Migrating and transferring data into Azure Storage using AzCopy](https://www.linkedin.com/learning/manage-storage-in-azure/migrating-and-transferring-data-into-azure-storage-using-azcopy?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/manage-storage-in-azure/migrating-and-transferring-data-into-azure-storage-using-azcopy?u=76281980&t=1)** - [Instructor] Being a command line tool, we're going to need a terminal to run AzCopy. So I'm currently in [Windows](../../Glossary/Service/Windows.md) [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md), but I could be using Bash in [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) for example as AzCopy runs on Linux, Mac OS, and Windows. So to use AzCopy, firstly you need to authenticate. I'm going to have a quick look at the help to work out how to do this. So azcopy is the executable. Login is the command that I need to know about. I'm just going to pass it the help parameter. I'm scrolling up in the terminal. We can see some examples. Now I have options, to log in interactively, so I would be putting in my identity credentials and scroll down. I could be using a managed identity so this would be good if I was using [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) back compute. And here we have options for using a service principle. Now using a service principle is the recommended way to authenticate when scheduling an AzCopy script. Now I will log in using device code authorization flow which uses my [Azure AD](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) identity to authenticate and returns the identities authorizations. And to do this, I can use azcopy login. If I hit return, it gives me the instructions on how to do this. So I need to enter this code into this browser window, paste the code in, and click next. And it's going to ask me which identity that I want to use which is my Azure instructor identity. And it's double checking that I want to sign in
>
> **[1:36](https://www.linkedin.com/learning/manage-storage-in-azure/migrating-and-transferring-data-into-azure-storage-using-azcopy?u=76281980&t=96)** to Azure Storage AzCopy, which I do. If I click continue, I'm now signed in, and I can close this window which I shall do and return to the terminal. And at the bottom here, we can see in the info it states that login has succeeded. Now for this demo, I'm going to create a new blob container in the example storage account that we created in the first video. So by entering into the help once more, but this time by deleting the command, I can get to the generic help and see all of the commands that are available. Now the first thing I want to do is create a new container. So if I look up at the command for create a container or file share, I can use make. So AzCopy make is the command for creating container. So if I use the same help command, but this time with the make command, I can see the examples for the make command. I'm going to need to use the executable, the command of make, the full URL to the storage account, and also add in the new container name that I require. And this will create the container for me. Now that's quite a long string, so I've created one earlier in my notes, and I shall paste this in. So we have AzCopy make, the URL to my storage account and the new container, and hitting return the terminal states it has successfully created the resource.
>
> **[3:09](https://www.linkedin.com/learning/manage-storage-in-azure/migrating-and-transferring-data-into-azure-storage-using-azcopy?u=76281980&t=189)** Now if I have a look in this directory, I also have a text file, and this is the text file that I'd like to upload into this new container. So if we go back to the main help once more, we're looking for the commands that will upload a file or blob into the container. So this is copy a copy source data to the destination location. Once again, I've got the command stored for this cause it's quite long, but we can paste it in, and I can talk you through it. So we have the executable, the copy command, the file that I would like to upload, and its path which is the local directory. And I'd like to upload that into my storage account to the container I've just created. And I can actually give it a new name if I wanted to, but I'm going to choose the same name as the file which is testupload.txt. Hitting enter on the terminal, we get some output, and we can see that the total number of transfers is one, and the number of file transfers completed is one. So the AzCopy command has taken my test upload.text file and uploaded it into my new container. So switching back to the portal, this is the account we created in the first demo. If I go into containers, I now have 0106 new container which is the one I've just created. And entering that container, we have the test upload.txt file. The most recent addition to the storage migration options
>
> **[4:45](https://www.linkedin.com/learning/manage-storage-in-azure/migrating-and-transferring-data-into-azure-storage-using-azcopy?u=76281980&t=285)** is Azure Storage Mover. It is a fully managed migration service for files and folders where a migration plan is created in the Azure portal, and agents are deployed to an on-premises virtual machine. The agents run the migration jobs, which are also defined and can be tracked together in the portal making it easier to monitor multiple file migrations. Being a fairly new service, currently NFS migration to Bob Storage is only available, but additional sources and targets are on the roadmap along with automatic discovery of sources and provision of targets based on the migration plan.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (5), [Windows](../../Glossary/Service/Windows.md) (2), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (2), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (1), [Azure ad](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) (1)
> **Tools:** terminal (5), command line (1), powershell (1), bash (1), azure portal (1)
> **CLI Commands:** make (6)
> **Env Vars:** url (2), nfs (1)
> **File Paths:** testupload.txt (1), upload.txt (1)
> **Cross-References:** go back to (1)
> **UI Navigation:** scroll down (1)
> **Definitions:** is a  (1)

#### [Hierarchical namespace and SFTP for blobs](https://www.linkedin.com/learning/manage-storage-in-azure/hierarchical-namespace-and-sftp-for-blobs?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/hierarchical-namespace-and-sftp-for-blobs?u=76281980&t=0)** - [Instructor] Historically, the flat structure of blob storage was used for scale. A flat structure object storage such as blob storage could scale linearly, therefore having no impact on data capacity or read/write performance, whereas performance for objects in a hierarchical structure was impaired as the number of objects grew. You have already seen how the blob storage structure in [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) consists of the account and the container, followed by the blob name, and it is within the blob name that the virtual structure is created by embedding slashes to create a virtual path. The problem with the structure comes when you need to perform directory maintenance such as renaming, moving, or deleting directories. Take this example. If I had a million blobs in virtual directory app two and needed to rename it, one million blob copies and one million deletes would have to be performed. This is because for blob storage to operate on a virtual directory, the operation must happen on every file that is part of the virtual directory. By enabling the hierarchical namespace for blobs feature on a storage account, the container at the top level stays the same, but underneath the container, directories can be added to build out a file system that is no longer virtual. Because it is built on the Azure Blob Storage, this hierarchical structure for objects
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/hierarchical-namespace-and-sftp-for-blobs?u=76281980&t=94)** retains its [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) and delivers better throughput and IOPS and faster operations at individual notes. For example, in the structure, if I were to rename app two to HR app, this is one transaction and does not require alteration to all of the underlying blobs. With a hierarchical namespace, security can now be applied at the container, directory, and blob level with [Azure Active Directory](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md). Transaction costs are slightly higher for a hierarchical namespace, and a key consideration is you cannot revert back to a flat namespace once this is enabled. There are also some limitations around encryption, blob protection, and replication features. For more details, visit this blob storage feature supports docs page. You may be thinking, what is the use case here? You may not need to rename virtual directories on mass scale for your applications and you don't require fine-grain security for the blob level. The answer is [Big Data Analytics](../../Skills/Data%20Science/Big%20Data%20Analytics.md). By selecting hierarchical namespace for Azure Blob Storage, the service can also be known as Azure Data Lake Storage Gen2 or ADLS Gen2 for short where a data lake is a centralized storage of structured and unstructured files, which is transferred in using one or more tools. The hierarchical namespace
>
> **[3:06](https://www.linkedin.com/learning/manage-storage-in-azure/hierarchical-namespace-and-sftp-for-blobs?u=76281980&t=186)** also enables [Hadoop](../../Skills/Data%20Science/Hadoop.md) compatible access through the Hadoop Distributed File System where Hadoop is a distributed computing system capable of processing massive amounts of data. In Azure, HDInsight is a Hadoop cluster which is capable of [Batch Processing](../../Skills/Data%20Science/Batch%20Processing.md) massive amounts of data using a tool such as Hive in Hadoop cluster and [Azure Databricks](../../Skills/Data%20Science/Azure%20Databricks.md) also uses the Hadoop Distributed File System to connect to Azure Data Lake storage. It is tools like Hive and Spark which run inside HDInsight and Azure Databricks that require the hierarchical namespace as they often write output to temporary locations and then rename the location at the conclusion of a job. Visualization tools such as [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) can also use the interactive querying features of engines such as Hive to query over massive amounts of data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (6), [Hadoop](../../Skills/Data%20Science/Hadoop.md) (6), [Azure Databricks](../../Skills/Data%20Science/Azure%20Databricks.md) (2), [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) (1), [Azure active directory](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) (1)
> **Analogies:** such as (5), for example (1)
> **Definitions:** is a  (3)
> **Env Vars:** iops (1), adls (1)
> **Speakers:** - [instructor] (1)

#### [Hierarchical namespace and SFTP for blobs in practice](https://www.linkedin.com/learning/manage-storage-in-azure/hierarchical-namespace-and-sftp-for-blobs-in-practice?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/hierarchical-namespace-and-sftp-for-blobs-in-practice?u=76281980&t=0)** - [Instructor] Switching into the [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Portal, when creating a storage account in the portal, the hierarchical namespace checkbox is on the Advanced tab if I scroll down a little bit. Clicking the checkbox enables two more features which depend on the hierarchical namespace, the network file system, or NFS, and SFTP. NFS v3 provides [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) file system compatibility, enabling Linux clients to mount the container into a VM. This feature allows access for legacy applications that cannot access storage through [REST](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) APIs, and is highly suited to high throughput, high scale, and read heavy workloads. Ticking Enable SFTP enables the SSH File Transfer Protocol for this account. On creation, it will be possible to transfer files to the storage account via SSH, which is a protocol enabling secure access between machines. Let's have a look at how SFTP for Azure Blob Storage works. So, we'll go to the resource, and to begin with we're going to need to add a container and a directory. Now, notice the icon next to Containers has changed compared to the blob icon we saw earlier. This is because this is no longer Blob Storage, this is Azure Data Lake Storage. So, I click on a Container and I'll create a container called interface. And within interface,
>
> **[1:33](https://www.linkedin.com/learning/manage-storage-in-azure/hierarchical-namespace-and-sftp-for-blobs-in-practice?u=76281980&t=93)** I will add a directory called incoming. This is going to be my home directory, which will explain in a second. Now that the structure is ready, I need to go back to my storage account and set up SFTP. We need to create local users in the SFTP settings blade as SFTP does not support [Azure AD](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) authentication or authorization. A local user requires a username and then a password. So, I'll add a username and a password which Azure generates or it provides a secure shell public private key pair and Azure stores the public key. SSH is the recommended method of connection. But to keep this demo simple, I'm going to choose password. Clicking Next, access can be granted for one or more containers, so I'll choose the container I added earlier, and I can choose one or more permissions. Again, to keep this simple, I'll choose All. Optionally, I can choose a landing directory. This is where a local user will start from when logged in. So, I'm going to set this to the incoming directory that I created earlier. And notice, the structure must be the container first and then the directory. Clicking Add to add the local user,
>
> **[3:06](https://www.linkedin.com/learning/manage-storage-in-azure/hierarchical-namespace-and-sftp-for-blobs-in-practice?u=76281980&t=186)** a prompt is displayed with the password. As we can see, this is only going to be displayed once so I'll copy this into a notepad for use later. Now we've got everything set up, we just need to test it. So, by copying this connection string and opening a supported SSH client, we can log in. And the host name that I've posted in here is the connection string for the account. And we just need to take the username, which, as you can see, is the name of the account and the local user. And I just need to delete that @ symbol. Ah, it's done it for me. And we need to paste in the password, which I copied earlier, and click Login. And this on the right-hand side is my incoming folder and on the left-hand side is my managing storage directory from my local machine. This is the file I'd like to copy, my test-upload.txt. And I can drag it to the right-hand side and the file has been uploaded. And I can check this by switching back to the Portal, back into my storage account, and we'll have a look in my directory and there is my test-upload.txt. Now, you may be aware that Blob Storage can be configured to stream events such as blob creation to fire processing workflows using Event Grid
>
> **[4:40](https://www.linkedin.com/learning/manage-storage-in-azure/hierarchical-namespace-and-sftp-for-blobs-in-practice?u=76281980&t=280)** and compute such as Azure functions and logic apps. At the time of writing this video, there are issues with events created when a blob is created through SFTP. Once these are ironed out, this could be a powerful mechanism for interfacing with legacy systems that cannot transfer data through APIs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (6), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Azure ad](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) (1)
> **Env Vars:** sftp (7), ssh (4), nfs (2), rest (1)
> **UI Navigation:** checkbox (2), scroll down (1), go to (1), click on (1)
> **CLI Commands:** ssh (4)
> **File Paths:** test-upload.txt (2)
> **Analogies:** such as (2)
> **Prerequisites:** set up (2)
> **Cross-References:** go back to (1)


### 2. Redundancy, Disaster Recovery, and Replication

[↑ Back to Table of Contents](#table-of-contents)

#### [High availability and durability for Azure Storage](https://www.linkedin.com/learning/manage-storage-in-azure/high-availability-and-durability-for-azure-storage?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/high-availability-and-durability-for-azure-storage?u=76281980&t=0)** - [Instructor] When building systems that require persistence of data, it is critical that the data is in an immediately accessible state. Unfortunately, [Hardware](../../Topics/Hardware.md) can fail, software can glitch, and data can become corrupted. Each of these issues will affect data accessibility. To help us understand how component failure may affect access to data, two key objectives are used, durability and availability. Where durability is the protection of data against loss, degradation, and data decay, and availability is whether the storage is accessible. These objectives are both measured as a percentage, but are calculated differently with durability being a combination of the [Probability](../../Skills/Data%20Science/Probability.md) of data loss per year and the time taken to recover from that loss of data per year, and availability as the percentage of uptime over local time in a year. Both are expressed as nines, where five nines would be 99.999%. One or two nines are always before the decimal place, further nines are after the decimal place here. Five nines equates to one object lost per hundred thousand a year for durability, and four nines to 42.6 minutes of downtime per year. Storage redundancy is used to ensure that availability and durability objectives are met by keeping multiple copies of data.
>
> **[1:35](https://www.linkedin.com/learning/manage-storage-in-azure/high-availability-and-durability-for-azure-storage?u=76281980&t=95)** In [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage, at least three copies of data are always kept within a region. Azure has multiple regions each geographically separate, such as West US, East US, or Southeast Asia. Within a region is a group of two or more data centers connected with a low latency network. Each data center has its own power, cooling, and networking. The two diagrams depict the options for storage redundancy in a single region, locally redundant storage and zone redundant storage. Locally redundant storage is the lowest level of redundancy with three copies of data held in a single data center. Writes happen synchronously to the data and failures are protected against at the rack and drive level. Zone redundant storage separates the three copies into separate data centers across availability zones in a region. Writes happen to each zone synchronously and the separation by zone increases the storage durability. This protects the data from data center outage. However, if the region suffers an outage, then the data in both of these scenarios will be inaccessible. If regional outage is a failure your system or application needs to protect against then Azure offers geo-redundancy where the two types of single region storage of LRS and ZRS are asynchronously replicated to a secondary region and stored as locally redundant storage.
>
> **[3:10](https://www.linkedin.com/learning/manage-storage-in-azure/high-availability-and-durability-for-azure-storage?u=76281980&t=190)** This replication is made possible by the secondary region being a regional pair to the first, where a regional pair is within the same geography, but at least 300 miles away where possible. For example, UK West and UK South or East US and West US. These geo-redundancy types have different acronyms of GRS for geo-redundant storage and GZRS for geo-zone-redundant storage. The addition of a secondary region with the copies of data raises the durability to 16 nines as the data is now protected from regional outage. These configurations support failover to the secondary region, and we'll look at this more closely in a future video. With geo-redundant data now copied to a secondary, it is also possible to configure read access in the secondary region. These configurations are known as read access geo-redundant and geo-zone-redundant storage. The addition of the readable secondary increases the SLA on the data reads to 99.99%. To access the read endpoint on the secondary, a suffix of -secondary is required. So, must be taken into consideration if building for application [Resiliency](../../Skills/Software%20Development/Resiliency.md) using the read endpoint. Another consideration is the use of last sync time for objects replicated to the secondary. The replication is asynchronous, so each geo-redundant storage account has a last sync time property which can be queried by the storage client libraries
>
> **[4:44](https://www.linkedin.com/learning/manage-storage-in-azure/high-availability-and-durability-for-azure-storage?u=76281980&t=284)** to see how far behind it may be. There are a couple of trade-offs when deciding which redundancy configuration to choose, and I've switched to the portal to show the first one. This is the create storage account page, you may recall it from the demos in Chapter 1. I'm adding a storage account name and I've also already chosen a region of UK West. The other settings are set to standard performance and GRS. Clicking the dropdown on Redundancy, I would expect to see LRS, ZRS, and GRS, but ZRS is missing. This is because UK West is a region with no availability zones, so zonal redundancy is not available. If I switch my region to West US 3, the zonal options are now available. Also, switching to Premium and looking at the redundancy options once more, geo-redundancy is not available for Premium accounts. So, the choice of region and whether a Premium account is required are the first trade-offs when choosing redundancy. Cost is also a key factor. The more copies of the data, the higher the cost to store it. There are also the asynchronous replication costs for replicating data to the secondary region. Finally, some storage account features are not supported for all redundancy configurations.
>
> **[6:18](https://www.linkedin.com/learning/manage-storage-in-azure/high-availability-and-durability-for-azure-storage?u=76281980&t=378)** For example, there is no page blob support for redundancy beyond LRS and no read endpoint access for table storage.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (3), [Hardware](../../Topics/Hardware.md) (1), [Probability](../../Skills/Data%20Science/Probability.md) (1), [Resiliency](../../Skills/Software%20Development/Resiliency.md) (1)
> **Env Vars:** lrs (3), zrs (3), grs (3), gzrs (1), sla (1)
> **Definitions:** is a  (3), known as (1)
> **Versions:** 99.999 (1), 42.6 (1), 99.99 (1)
> **Analogies:** for example (2), such as (1)
> **UI Navigation:** dropdown (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [Backing up Azure file shares and operational backup for blobs](https://www.linkedin.com/learning/manage-storage-in-azure/backing-up-azure-file-shares-and-operational-backup-for-blobs?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/backing-up-azure-file-shares-and-operational-backup-for-blobs?u=76281980&t=0)** - [Instructor] [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage redundancy provides [Resiliency](../../Skills/Software%20Development/Resiliency.md) against [Hardware](../../Topics/Hardware.md), software, data center, and regional outages, but it doesn't protect the individual files and objects in an account from accidental deletion or corruption. For this, a point-in-time restore mechanism is required so that data can be recovered from a point in time where a previous version existed before the deletion or corruption took place. For Azure files, this is Azure file shares backup which uses Recovery Services vault. This is a management tool that stores recovery points. Within the Recovery Services vault, a backup policy is created with a schedule for backing up a share along with its retention, which can be customized with daily, weekly, monthly, or yearly retention. Configuration of the backup can set a delete lock on the file share and the storage account, which is recommended, and snapshots are taken in line with the schedule, and these are stored locally within the same storage account. So this is a local backup, which is also why a delete lock is recommended to stop the snapshots from being lost. The Recovery Services vault keeps the available snapshots in line with the retention policy. For block blobs, a similar functionality is available through operational backup for Azure blobs. When enabled on a storage account, all block blobs within the storage account are set up with a backup policy within a backup vault, where a backup vault is also a management tool
>
> **[1:35](https://www.linkedin.com/learning/manage-storage-in-azure/backing-up-azure-file-shares-and-operational-backup-for-blobs?u=76281980&t=95)** that stores recovery points like a Recovery Services vault but for the newer workloads within Azure. Configuring the backup policy sets a delete lock on the storage account. Notice there is no schedule in the backup policy. This is because backups are continuous, using a feature of Azure blob storage called blob point-in-time restore. This tracks changes to blobs. Once again, the backup is stored locally within the same storage account as the data. The backup vault manages the blobs in the backup in line with the retention period in the backup policy to a maximum retention of 360 days or equivalent number of complete weeks, which is 51, or months, which is 11.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (6), [Resiliency](../../Skills/Software%20Development/Resiliency.md) (1), [Hardware](../../Topics/Hardware.md) (1)
> **Definitions:** is a  (2)
> **Best Practices:** recommended (2)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Backing up Azure file shares and blobs demo](https://www.linkedin.com/learning/manage-storage-in-azure/backing-up-azure-file-shares-and-blobs-demo?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/backing-up-azure-file-shares-and-blobs-demo?u=76281980&t=0)** - [Instructor] I've switched to the portal to show how to set up [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) file share backup. This storage account has a file share called files backup and within it two directories, one with a picture and one with a work document. To back up this file share I'll search for recovery services vault and select recovery services vaults in the dropdown. This is an old vault from a previous configuration. I'd like to create a new one for this demo. So selecting the rg-02-02 resource group which is my resource group for this group of videos. I'll set the vault name to nas0202vault and I'll leave the location at UK South and move on to the networking tab. Here I can choose how the vault can be accessed either publicly or privately through a private endpoint. I'll leave this as public for the demo and click review and create and then create.
>
> **[1:10](https://www.linkedin.com/learning/manage-storage-in-azure/backing-up-azure-file-shares-and-blobs-demo?u=76281980&t=70)** The recovery services vault has created. So I could go to the new vault to create the backup but I'd like to use the backup center which I'm going to search for at the top as the backup center is a single point to manage all backups. To use the backup center we need to add a backup and choose Azure files from the dropdown list for the data source type. Now I need to choose the vault to backup to. This is the recovery services vault that I just created. So I click this and click select and continue to move on. Now I need to select the storage account which holds the file share. Sometimes this may appear empty as it is still discovering supported resources, which we can see listed at the top here. My storage account that I created for this demo isn't listed, and we can see this call out at the top. So I'm just going to cancel this and wait a few moments. I've waited a few more moments for the storage accounts to be discovered, and this is the storage account for this set of demos in resource group rg-02-02. So I'll select this storage account, and I want to enable the lock on the storage account to protect my snapshots. So clicking okay, the storage account is registered with the vault. It's taken a few moments, but the storage account has now registered
>
> **[2:44](https://www.linkedin.com/learning/manage-storage-in-azure/backing-up-azure-file-shares-and-blobs-demo?u=76281980&t=164)** and I can add the file share or shares that need to be backed up. Files backup is listed so I can select it and click okay. Moving to the policy details, the default policy of a daily backup with 30 days retention can be edited. The schedule frequency can be changed to hourly if required. And next is retention range. We can choose weekly, monthly, and yearly with yearly backup points that can be set all the way out to 10 years, along with the day or week of a backup depending on their restore point. There are limits though. If I change the number of monthly backup points to 200, a warning is shown as the number of restore points exceeds the maximum of 200. So I'll reset it back to 120. Now I'd like to keep all of these at the defaults but I'm going to rename my policy to daily 30 days and click okay to enable the backup.
>
> **[3:58](https://www.linkedin.com/learning/manage-storage-in-azure/backing-up-azure-file-shares-and-blobs-demo?u=76281980&t=238)** Now, while that enables, I'll return to the backup center and set up operational backups for blobs. The start is the same by clicking add backup but this time the resource is Azure blobs. Continuing on, I now need to create a vault and this time it is a backup vault, which I'll create in my same resource group rg-02-02 and enter a vault name of sa0202bv for backup vault.
>
> **[4:39](https://www.linkedin.com/learning/manage-storage-in-azure/backing-up-azure-file-shares-and-blobs-demo?u=76281980&t=279)** I'll leave the vault in UK South and choose local redundancy and click review and create and create to create the backup vault. The vault is created and the name is loaded into the vault selection. So I will click next to move on and create a backup policy. I have no backup policies, so I'll click create new, and I'd like this backup policy to be seven days. So I will name it accordingly and move to create the retention rules. The default rule is 30 days. I'll edit this and change it to seven. Note that this can be days, weeks, or months up to a year as described earlier. Note this warning, the older the recovery point the longer the restore job may take. I'm happy with this, so I'll save the update and create the backup policy. The backup policy is now created and selected so I click next to choose the data store. To choose the storage account I click add and it is the sa0202 storage account that I need to back up and I'll choose to select this one item. The validation process has found an error. The identity I'm logged in as does not have storage account backup contributor for this storage account.
>
> **[6:13](https://www.linkedin.com/learning/manage-storage-in-azure/backing-up-azure-file-shares-and-blobs-demo?u=76281980&t=373)** So I can choose the storage account and assign the missing roles directly from here and I'll do it at the resource scope. The role is now assigned and it's just waiting a few seconds before it automatically revalidates. Automatic revalidation has kicked off. We just need to wait for that to complete. Validation is now successful so I can click next and choose to configure the backup. The backup job is now configured and I can see all the backup jobs by entering backup jobs on the left hand side and I need to change the data source type because I don't have any Azure [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) being backed up. So I can look at files and I can see the files backup that I set up earlier. There are of course limitations to both of these backups such as operational blob backup can only be enabled on general purpose V2 storage accounts. To review these limitations, follow the two [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) Learn document links that are displayed now.
>
> **[7:31](https://www.linkedin.com/learning/manage-storage-in-azure/backing-up-azure-file-shares-and-blobs-demo?u=76281980&t=451)** I've now left the two backup configurations overnight and I'm back in the backup center and the overview page. It states that no jobs have run in the last 24 hours for blobs. This is due to the continuous backup nature of blobs in this configuration so it doesn't actually require a backup job to run. If I change the resource type now to Azure files, it states that one schedule backup has run and this is for the file share that was chosen. A new feature of vaulted backups for blobs and files is currently in preview. It's designed to backup data outside of the local storage account to a backup vault. This will give blob backups retention of up to 10 years and the ability to restore to a different account. Although preview items do not always go to GA, I feel this is certainly one to keep an eye on. With the backups now configured, join me in the next video to look at disaster recovery and failover.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (5), [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) (1), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **UI Navigation:** dropdown (2), go to (2), select the (1)
> **Prerequisites:** set up (3), configure (1)
> **Definitions:** is a  (2), is an  (1)
> **Warnings:** warning (2), note that (1)
> **Cross-References:** in the last (1), in the next (1)
> **Analogies:** picture (1), such as (1)
> **Speakers:** - [instructor] (1)

#### [Disaster recovery and failover](https://www.linkedin.com/learning/manage-storage-in-azure/disaster-recovery-and-failover?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/disaster-recovery-and-failover?u=76281980&t=0)** - [Instructor] If disaster strikes and a major unplanned outage occurs, your use case may require building in [Resiliency](../../Skills/Software%20Development/Resiliency.md) to withstand the outage. To enable such resiliency in [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage, Geo-Redundant Storage accounts support failover to the secondary region. If you recall from the first video in this chapter, Geo-Redundant Storage is asynchronously replicated from the primary to the secondary region, which can also have a read-only endpoint. During operation of a Geo-Redundant account, should the primary endpoint become unavailable, the primary region is also possibly unavailable, and asynchronous replication will cease. If a region is lost due to a major disaster or has been unavailable for a long period of time, then [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) may initiate failover, but customers can also initiate failover in the event of primary endpoint outage. Until failover is complete, no write access is available, but read access is available if the account was RA-GRS or RA-GZRS. On completion of failover, the endpoint DNS has been updated to the new primary and writes can proceed once more. Note, as shown in this diagram, the new primary is locally redundant storage. To start replicating back to the original primary region, you will need to change the new primary to be geo-redundant.
>
> **[1:36](https://www.linkedin.com/learning/manage-storage-in-azure/disaster-recovery-and-failover?u=76281980&t=96)** Because premium accounts do not support geo-redundancy, then only general-purpose v2 is supported for customer-initiated failover. There are also further support considerations, such as disabling operational backup for blobs before initiating failover. I've switched into the portal to show how to manually initiate failover for a storage account. This is the overview page for the storage account that I've created for this demo. Rephrase for this demo. The storage account is set to RA-GRS replication with a primary region of UK South and a secondary at UK West. Now, before failing over I would like to check the last sync time, so that I know the last time data from the primary was guaranteed to be written to the secondary. This will give me an estimate for data loss after failover. This value can be obtained using [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) or the Azure [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md). So I'll open the Cloud Shell, and I'll copy in a command to do this. This Azure CLI command will show in this storage account, which is listed here, the geo replication stats, and I want to see the last sync time. The last sync time for the storage account has now been displayed. We can also see the last sync time in the portal when entering the redundancy pane,
>
> **[3:10](https://www.linkedin.com/learning/manage-storage-in-azure/disaster-recovery-and-failover?u=76281980&t=190)** which actually shows quite a nice graphic of the redundancy setup. And in the prepare for failover section, I can see the last sync time also shown at the top. And there are further notes which also speak about the amount of time it may take, which is typically less than an hour. I'm going to type "yes" to confirm that I would like to failover, and hit Failover to initiate the failover. This account has now failed-over, and is listed as being available at UK West only, and is also locally redundant storage. From here, I could set up geo-replication once more, but notice as this is originally an RA-GRS account, I cannot now choose geo-zone redundancy. I must choose a local redundancy in the primary and the secondary. So that's how to initiate customer-managed failover for a Geo-Redundant Storage account. Join me in the next video where we'll explore a different type of replication for blobs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (3), [Resiliency](../../Skills/Software%20Development/Resiliency.md) (2), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (1)
> **Env Vars:** grs (3), cli (2), gzrs (1), dns (1)
> **Prerequisites:** setup (1), set up (1)
> **Cross-References:** in the next (1)
> **Tools:** powershell (1)
> **UI Navigation:** open the (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Object replication for blob data](https://www.linkedin.com/learning/manage-storage-in-azure/object-replication-for-blob-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/object-replication-for-blob-data?u=76281980&t=0)** - [Instructor] Geo redundancy for a storage account enables [Resiliency](../../Skills/Software%20Development/Resiliency.md) of data for a regional pair. But what if you have a use case that requires replication of data beyond regional pairing or for purposes outside of failover? [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) storage has object replication which is available for block blobs only. Object replication allows block blobs from one container to be asynchronously replicated to another region. Note on this diagram, UK South is not a paired region with East Asia. Object replication can also be to the same region or a different region in separate subscriptions. Also, object replication can be enabled to replicate block blobs to a subscription in a different tenant. The use cases for object replication for blobs are often around disaster recovery and enabling the ability to have a backup of the blob data outside the local storage account. Use cases can also include copying data for processing by workloads outside of the current subscription or tenant along with keeping a copy of the data in an archive. Let's look at how to get this configured in the portal. I've already created this resource group and it contains three storage account, two in UK South and one in East Asia. I'm going to set up replication from the sa0204 prefix storage account to the other two accounts. The sa0204 is the source account, and first of all
>
> **[1:38](https://www.linkedin.com/learning/manage-storage-in-azure/object-replication-for-blob-data?u=76281980&t=98)** I'll check that the prerequisites for object replication are enabled by checking the data replication settings of the account. Scrolling down to tracking, versioning for blobs and blob change feed must be enabled. We will cover the capabilities of both of these later in the course. Returning to the resource group and into the data protection settings of the target accounts and scrolling down to tracking once more. These require versioning for blobs be enabled but not blob change feed. Now that's the pre-reqs checked. Let's have a look at the source data. Entering the containers for the source storage account we can see there are two [JSON](../../Skills/Web%20Development/JSON.md) and text. I'm going to replicate JSON to the same region and text to East Asia. Inside each container, there are a couple of blobs and a blob inside each of the virtual directories. I'm going to use these virtual directories to filter the blobs to be replicated. To set up replication, we go back to the storage account and into object replication, which is under [Data Management](../../Skills/Software%20Development/Data%20Management.md). Firstly, I'd like to look at advanced settings. Here I can choose to turn off cross tenant replication which is a recommendation in the Azure architecture documentation for the security of storage account.
>
> **[3:14](https://www.linkedin.com/learning/manage-storage-in-azure/object-replication-for-blob-data?u=76281980&t=194)** Now to add the first replication rule by clicking on create replication rules. Up to a thousand rules can be added in the portal. If you need more than that you can upload the configuration in a JSON format. I only want to add a couple of rules so I'll be adding them manually. The first rule I want to add is to the same region. So my destination storage account that I would like to choose is sasame. I then choose the JSON source container and I'd like to send this to the JSON same region target container. Note you can choose to copy over everything or just new objects or you can also choose to set a custom target date. I'll choose everything and click save. That's it for this rule. So I'll click create to create the rule. That's my first rule created. For the second rule, I would like to choose the cross region account of sacross. This time, I'd like to choose the text containers, which are in each of the accounts. However, I want to add a filter that will only replicate the blobs in the cross virtual directory so I'm going to choose a filter of cross/ and click save.
>
> **[4:46](https://www.linkedin.com/learning/manage-storage-in-azure/object-replication-for-blob-data?u=76281980&t=286)** I'm also going to leave the copy over to only new objects and click create to create this new rule. And that's the new rule created. Returning to the resource group and looking at the target account in the same region, the container that I chose as a replication container has had all the blobs from the source container copied to it. But if I go up a level and into the text container nothing has been replicated to the text container, which is as I configured. If I return to the resource group and into the cross region storage account and its containers, and into the text container and into cross because it was this virtual directory that I chose to replicate, I'll upload my new blob. Just search for a blob on my machine. The logo file will do nicely and upload this blob. Now going to the target container, which is our cross region storage account into container and text cross region. The blob hasn't appeared yet. Now this isn't unusual so we can actually check the replication status of the blob at source account by going back to the blob and looking at its properties. At the source account that I just created
>
> **[6:25](https://www.linkedin.com/learning/manage-storage-in-azure/object-replication-for-blob-data?u=76281980&t=385)** the object replication policy ID is shown as complete so the blob should now be there. So if I return to the target container and into our cross region virtual directory the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) logo blob file is present. The replication status of a blob is a key property when working with object replication for blobs as there is currently no SLA on how long it would take to replicate to the destination account. This was quite a small file and it still took nearly a minute to come across. We've now explored redundancy, backup, and disaster recovery in this chapter. Come and join me in the next chapter on how to manage networking for storage, and also for how to create storage using [infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md).

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (5), [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (2), [Resiliency](../../Skills/Software%20Development/Resiliency.md) (1), [Data Management](../../Skills/Software%20Development/Data%20Management.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Env Vars:** json (5), sla (1)
> **Cross-References:** later in (1), go back to (1), in the next (1)
> **Definitions:** is a  (2)
> **Prerequisites:** set up (2)
> **Speakers:** - [instructor] (1)


### 3. Networking and Infrastructure as Code for Azure Storage

[↑ Back to Table of Contents](#table-of-contents)

#### [Storage account firewalls and virtual network access](https://www.linkedin.com/learning/manage-storage-in-azure/storage-account-firewalls-and-virtual-network-access?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/storage-account-firewalls-and-virtual-network-access?u=76281980&t=0)** - [Instructor] [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage has multiple network configurations at the storage account level, ranging from public to private, the choice of which will depend on your application or system's scenarios. For example, until this point in the course, all of the storage accounts that I have used in the demos have been set to be enabled from all networks. This means that the endpoint for the storage account is publicly accessible, so any virtual machine from in Azure, or on-premises, or any client anywhere on the Internet, even bad actors, can hit the public endpoint, so long as there is some Internet access from their clients. My scenario for this configuration was that I didn't want to talk through networking too early in this course. But this configuration is unlikely to be secure enough in most scenarios, so Azure Storage accounts have the option to enable access from virtual networks and IP addresses. You can then start adding firewall rules, such as only allow IP address 217.26.25.32, which in the case of this diagram would block access from the virtual machine in Azure, along with the internet client, leaving just machines from the on-premises static IP able to hit the endpoint. However, what if your [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) are in Azure and have no public internet access, or you don't want your traffic travelling over the internet? In this scenario, you can enable traffic
>
> **[1:33](https://www.linkedin.com/learning/manage-storage-in-azure/storage-account-firewalls-and-virtual-network-access?u=76281980&t=93)** from a specific virtual network and subnet through a service endpoint. With a service endpoint, it is the private IP address that is now the source IP, and traffic travels over the Azure backbone. Service endpoints are a secure, direct connection to past services in Azure. Now, there is a possible issue with this setup. The machines on-premises cannot connect to the endpoint by default, even if the network is extended over ExpressRoute. You must add further configuration as there is no native support for on-premises integrations. Switching into the portal, we will look at how to enable access from virtual networks and IP addresses with firewall rules. I have already deployed some resources into this resource group for the video RG0301. There is a virtual machine which is attached to this VNet in UK South. There is also a second VNet in East Asia. I also have a storage account also in UK South. Clicking to view the storage account and entering the networking tab, this storage account is currently accessible from all networks, which means if I enter one of its containers and into its blob properties to copy the blob's URL, and paste it into the top of my browser, I can access the blob and even download it.
>
> **[3:09](https://www.linkedin.com/learning/manage-storage-in-azure/storage-account-firewalls-and-virtual-network-access?u=76281980&t=189)** Navigating back to the networking page for my storage account, if I now choose to enable selected virtual networks, and IP address, and click Save. It sometimes takes a few moments to process, but if I go back to the tab with my blob on and reload the page, I can no longer access it. The firewall rule is preventing access. So, to allow access from my machine specifically, I can add an IP address firewall rule, which can be a single IP address, or a range of IP addresses inside a format. I can just choose here to add my client IP address and click Save, which adds it to the address range. So going back to the blob URL and refreshing, I can now access the blob once more. If you remember, back in our resource group, we also had a virtual machine. I'll switch to this virtual machine now and paste in the blob URL. And as expected, the firewall rule, which is only allowing the IP address from my client, has blocked access. Now, I'd like to add access to this VM through a service endpoint, so that it is connecting from its private IP address
>
> **[4:44](https://www.linkedin.com/learning/manage-storage-in-azure/storage-account-firewalls-and-virtual-network-access?u=76281980&t=284)** over the Azure backbone. So, I'll click on the networking pane within the storage account, and I have to choose to add an existing virtual network, and I will choose the VNet which was Vnet-0301, and the subnet, which is Snet-0301, which is the subnet the virtual machine is attached to. Clicking Enable, note that the portal has detected that the [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md). Storage Service endpoint needs to be enabled for this subnet. So, when I click to enable, the endpoint status switches to enabling, and this will make the subnet known to the service so that its traffic can be allowed through once added. Now that the service endpoint is enabled, I can add the virtual network. And the VNet has been added to the configuration. And now I just need to click Save to configure. The configuration has successfully saved. So, if I switch back to my virtual machine, and if I click to refresh, my blob now loads. Moving back to the portal, there are a couple of other items in here that I would like to look at. If you recall all of the resources in the deployment, there was a second VNet and subnet in East Asia. If I select to add these into the virtual network section of the networking configuration,
>
> **[6:23](https://www.linkedin.com/learning/manage-storage-in-azure/storage-account-firewalls-and-virtual-network-access?u=76281980&t=383)** it now states that a Microsoft Storage.GlobalService endpoint is required. This is a cross-region service endpoint, as East Asia is not in the same region as the storage account, and is also not a paired region of the storage account. Scrolling down a little further, I'm going to leave resource instances to explain in a future video. Next, there are the exceptions. These are for resources in Azure that cannot be added in through network rules, such as Azure Backup and Azure Site Recovery, which may need to access the storage account. For services such as these, a firewall rule allows access to trusted services, which authenticate using strong authentication such as a system-managed identity, and we will cover managed identities later in this course. Finally, there is read access to logs and metrics generated by diagnostics on the storage account. These can be read from anywhere once turned on. But be careful here though, there is information in these logs and metrics that could leave your storage account open to being attacked. That's the end of this video on configuring firewall rules for a storage account public endpoint. In the next video, it's time to look at private endpoints.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (11), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (2), [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) (1)
> **Env Vars:** url (3), rg0301 (1)
> **Definitions:** is a  (3), means that (1)
> **Analogies:** such as (3), for example (1)
> **Cross-References:** go back to (1), later in (1), in the next (1)
> **UI Navigation:** switch to (1), click on (1)
> **Warnings:** note that (1), be careful (1)
> **Prerequisites:** setup (1), configure (1)

#### [Private endpoints for Azure Storage](https://www.linkedin.com/learning/manage-storage-in-azure/private-endpoints-for-azure-storage?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/private-endpoints-for-azure-storage?u=76281980&t=0)** - [Instructor] When deploying a storage account through the portal or through [infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md), the storage account will default to enabling a public endpoint. As we saw in the previous video, access to data can be secured over the [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) backbone by enabling a service endpoint and disabling access to IP addresses external to Azure. However, the storage account endpoint is still exposed to the internet. And in some scenarios, this may not meet compliance requirements. This is where a storage account private endpoint can be deployed. Here is a diagram of the same resources shown previously. By adding a private link to the virtual network, Azure [PaaS](../../Glossary/Concept/PaaS.md) services such as Azure Storage can be accessed through a private endpoint. Therefore, traffic between resources in the VNet and the storage account travel via the private link o n the Azure backbone, removing any exposure to the internet. DNS resolution is used to route the traffic to the private endpoint creating a private DNS zone attached to the VNet by default or by updating DNS configuration if the VNet is using a custom DNS server. Outside the VNet, the storage endpoint URL resolves to the public address and inside the VNet to the private address. A DNS record is required for each primary and secondary storage service to be made private as each has its own endpoint.
>
> **[1:33](https://www.linkedin.com/learning/manage-storage-in-azure/private-endpoints-for-azure-storage?u=76281980&t=93)** Another feature of storage private endpoints is the native support for access from on-premises applications through a VPN or ExpressRoute, and to regionally or globally peered VNets which enables private access to data for a hub and spoke topology. Access to the public endpoint can be disabled removing compliance concerns. I switched to the portal now to show you how to deploy a private endpoint in the portal. This resource visualizer is showing the deployment in this resource group, which has a single VM, a VNet, and this storage account. There are containers in the storage account which contain blobs, and I've already tested that the blobs are accessible by loading one of the URLs in the virtual machine. Jumping back to the portal and into the Networking tab, I'll switch public network access to be disabled and click Save. And switching back to the virtual machine and refreshing the browser, access to the blob has been stopped by disabling the public endpoint. Switching back to the portal once more, I can add a private endpoint through this tab, private endpoint connections, and adding a private endpoint. I'll add a name for my private endpoint
>
> **[3:12](https://www.linkedin.com/learning/manage-storage-in-azure/private-endpoints-for-azure-storage?u=76281980&t=192)** and this automatically fills in the network interface name. A [NIC](../../Glossary/Concept/NIC.md) is required as this is how the endpoint is injected into the VNet. Clicking next, the target subresource is the storage service that requires the endpoint. I am setting up a private endpoint to read blobs so I will select blob in the dropdown. Clicking Next once more, the virtual network where the endpoint will be deployed to is vnet-0302, and I'd like to deploy to the snet-plink which is currently empty. Moving to the DNS tab, the default is to integrate the zone which changes the DNS CNAME resource record for the storage account to an alias in a subdomain with the prefix of private link. This also creates the zone with a record for the private endpoint. We can now move through the tabs to Review and Create to create the endpoint.
>
> **[4:25](https://www.linkedin.com/learning/manage-storage-in-azure/private-endpoints-for-azure-storage?u=76281980&t=265)** The endpoint is now created. So clicking on Go to Resource and into DNS configuration, the NIC that has been created is displayed along with the updated DNS configuration. And if I switch back to my virtual machine and refresh the browser, the blob is accessible once more so the private endpoint is functioning correctly. And that's how to create a private endpoint from within the portal where private endpoints are protecting your data from exfiltration through public endpoints. In the next few videos, we'll build up to creating this exact deployment but using infrastructure as code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (5), [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (2), [NIC](../../Glossary/Concept/NIC.md) (2), [PaaS](../../Glossary/Concept/PaaS.md) (1)
> **Env Vars:** dns (9), nic (2), url (1), vpn (1), cname (1)
> **Cross-References:** as we saw (1), previous video (1), in the next (1)
> **UI Navigation:** dropdown (1), go to (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Create and manage a storage account with Azure PowerShell](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-azure-powershell?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-azure-powershell?u=76281980&t=0)** - [Presenter] Working with resources in [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) through the portal is intuitive and a great way to start when learning. But when starting to work hands-on with Azure every day, it can quickly become apparent that using the portal is not the most efficient method of managing resources. By automating resource management using scripting, you can take a standard approach to complex and time consuming tasks. These scripts can be shared and reused across environments ensuring the same results each time. Or if a script needs to evolve, it can be version controlled. Version controlled scripts can also serve as documentation of a task or an environment. The key factor here is automation reduces human error and therefore saves time and money. [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) is [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md)'s task automation solution and Azure PowerShell is a set of commandlets that are used for managing Azure resources. Azure PowerShell works on [Windows](../../Glossary/Service/Windows.md), macOS and [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) and can be authenticated using multiple methods such as interactive browser based login, service principles and managed identity when executed from compute based in Azure. The command that's in Azure PowerShell call the Azure API with each Azure service usually having its own module. As mentioned, you could use Windows, Linux,
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-azure-powershell?u=76281980&t=94)** or even Mac to run Azure PowerShell commands. But you can also run command line tools right here in the Azure portal using Azure Cloud Shell. Azure Cloud Shell is a browser-based terminal. It runs on a temporary host loaded each time the cloud shell is entered. You can even upload or download data to the Cloud Shell session as its data is persisted on an Azure file share. You also have the option of running a PowerShell or Bash session on the terminal. The cloud shell comes with multiple tools already installed. For example, [Git](../../Skills/Software%20Development/Git.md) for [Version Control](../../Skills/Web%20Development/Version%20Control.md) or [Ansible](../../Skills/Network%20and%20System%20Administration/Ansible.md)
>
> **[2:26](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-azure-powershell?u=76281980&t=146)** which is a configuration tool. And there's also Docker. A Docker is used to build, run, and manage containers in the cloud. And if I copy a command in from my notes, and of course Azure Cloud Shell comes with Azure PowerShell modules already installed. Now we are going to look at creating a storage account within the Azure Cloud Shell using Azure PowerShell Commandlets. So to create a storage account, first you'll need to be authenticated to Azure and this requires the Connect AZ account command lit. So pasting in a further command and listing the examples and scrolling a little further up. You can see the different mechanisms to authenticate that are mentioned in the previous slides. Here we're authenticating with a service principle. This is how to log in interactively or further down, we have examples for using system assigned or managed assigned identities. And I'll explain more about these in the next chapter. Now I'm already authenticated into Azure as I'm already logged in through the Azure portal so I don't have to run this command but I may need to set the context for the commands I would like to run. For example, if I copy in another command from my notes,
>
> **[4:01](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-azure-powershell?u=76281980&t=241)** this command lists all of the subscriptions that are available to me and I could use it to set the subscription that I'd like to use. But I only have one available. So I don't need to set the subscription. To create a storage account, I'll need to create a resource group and I can search for the command using get command which I'll paste into the session once more. And this is listing the commandlets that I can use for a resource group. And if I scroll up a little, we can see the command I need to use is new AZ resource group. And I can use the help pasting in a command once more to look at the examples for this commandlet, just make the window a little bit bigger and scrolling up to the top, I'm going to need a location and a resource group name. So I'm going to set these as PowerShell variables using these two commands that I'll paste in. And now I can use these variables as part of my new AZ resource group command to create my resource group. Now the output that is returned is a formatted version of the JS packet returned by the Azure [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) API. The provisioning state, which is listed here, is succeeded. So the resource group has created successfully.
>
> **[5:36](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-azure-powershell?u=76281980&t=336)** Now to create the storage account. The storage account modules are named around AZ storage so searching for those commands and I'll paste in the command once more, just get command AZ storage. That is a lot of commands. So clearing the page, I can narrow this down to the storage account command by using AZ storage account. So pasting in a slightly different command, forget command AZ storage account. And the command I'm looking for is listed here. New AZ storage account. Now I'm not going to use the get help and the examples this time as there are nearly 20 listed and it's difficult to read. So I'll paste in an example of my own. But first I'll set up some variables for the storage account. And this sets up the storage account name. And I can use this variable in my storage account command. That's my storage account command that's been automatically entered as I've pasted it in. So we have the storage account name, the location I set up earlier, the SKU, which is a geo-redundant storage account with a read endpoint and the kind which is a general purpose V2 storage account. 'Cause this automatically entered the command when I pasted it in,
>
> **[7:08](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-azure-powershell?u=76281980&t=428)** I've also been warned of a breaking change from the API the default for allowing public access. Two blobs will be set to force by default in the future. At the bottom, now the command has executed, the output for the storage account is returned. It is a do net object from which the value shown on the screen are taken. These include the create time, and this says, "The storage account has been created successfully." I can now use the get AZ storage account commandlet to retrieve information about my storage account that has just been created and I can store this into a variable. And then I can use the variable to return details about the storage account. For example, I can list all the variables and properties that I can select from the storage account. And these can all be accessed and viewed. For example, all of the primary endpoints for the storage account. And because this is geo-redundant, all of the secondary endpoints for the storage account I can even see the connection strings. Apologies, I spelled that wrong. And I can even see the connection strings which include a method of authentication called an account key which we'll cover in the next chapter.
>
> **[8:41](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-azure-powershell?u=76281980&t=521)** Now we've finished looking at our storage account. I can even paste in a command which will remove the storage account. And it's just checking if I'm sure I really want to do that, which I am. So I'm going to enter, Yes and the storage account is deleted.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (22), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (9), [Windows](../../Glossary/Service/Windows.md) (2), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (2), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **Tools:** powershell (9), azure portal (2), terminal (2), command line (1), bash (1)
> **CLI Commands:** az (9), docker (2), git (1), ansible (1), make (1)
> **Definitions:** is a  (8)
> **Analogies:** for example (4), such as (1)
> **Env Vars:** api (3), sku (1)
> **Prerequisites:** set up (2), you'll need (1)
> **Cross-References:** in the next (2)

#### [Executing Azure PowerShell in scripts with the Cloud Shell code editor](https://www.linkedin.com/learning/manage-storage-in-azure/executing-azure-powershell-in-scripts-with-the-cloud-shell-code-editor?u=76281980)

#### [Create and manage a storage account with the Azure CLI](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-the-azure-cli?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-the-azure-cli?u=76281980&t=0)** - [Instructor] A second command line language is available to manage resources in [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md), the Azure Command Line Interface or Azure [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md). The structure of a command in Azure CLI differs from that of [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md), which has a very specific verb-noun structure. The CLI commands are split into five parts. The AZ indicates that the command being input is for the Azure CLI. The group denotes a major resource or service in Azure such as storage for Azure Storage or VM for Virtual Machine. Some services also have subgroups that can be worked with using the CLI. Azure Storage has subgroups for the blob, file, queue, and table services. Next is the command, such as delete, list, create, or show. And finally, the parameters that the command needs to execute. These can be flags, options, and arguments. I've switched back to the portal and I've already entered the cloud shell, so that I can start working with Azure CLI. But this time I'm going to switch to the bash shell, which is a [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) or Unix shell. And I can do this by choosing bash here on the left. The cloud shell reloads and mounts my cloud file share drive, the same drive that was mounted to PowerShell in the last video. Which is why, if I list the directories, my repro from my [GitHub](../../Skills/Software%20Development/GitHub.md) repository is still present. Help with the Azure CLI commands
>
> **[1:36](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-the-azure-cli?u=76281980&t=96)** is using the --help command. (keyboard clicking) So, if you already know the service you are looking for, which I did here, group is for resource group, you can type AZ the service name and --help. The help lists the group's commands and its possible subgroups. Looking at the list, the command I'll need is create. I don't need a subgroup for this one, I can continue digging further though with --help. (keyboard clicking) Make this window a little bigger. This has listed the required parameters for the AZ group create of location and name. So, I'll complete my command now with both of these. I'm pasting in a command that I've saved earlier. So, there's the resource group name and the location. And hitting return the group is created, and a [JSON](../../Skills/Web%20Development/JSON.md) response is returned showing the resources properties along with the provision state of succeeded. If you are searching for a command, the Azure CLI contains an AI knowledge base of commands which use the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) find. For example, I can try and find AZ storage. And here a list of common ways to use AZ storage is displayed. The Azure CLI also has AZ Interactive.
>
> **[3:14](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-the-azure-cli?u=76281980&t=194)** This is an interactive environment designed to help learn Azure CLI with auto-completed drop-downs, and examples of how to use each command. Just wait for it to load. It's loaded now. For example, if I type storage and press space, I am given a list of subgroups. I'd like to create an account, so I'll choose Account and space once more, and its available subgroups are shown to me, which include the create command. So, I can type create and press space once more and now the list of available arguments, and switches are displayed. I'm going to add the two required parameters of name, which I'm going to set to SA0304AZCLI, and also, pressing space, it's automatically filled in the resource group. (keyboard clicking) I'm going to set the resource group to RG0304, which is my resource group for this video. So, that's the mandatory parameters entered, so hitting return, it's taking a moment, the storage account has created and if I scroll up, I can see the JSON representation of this storage account. We have the SKU, which was set
>
> **[4:47](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-the-azure-cli?u=76281980&t=287)** to standard geo-redundant storage with a read-only access point, and all of the endpoints are also listed. Now, typing quit will exit out of AZ Interactive. Now, with this being a command line tool, we can, of course, run bash scripts of Azure CLI commands. So, by changing directory into the GitHub repository chapter 3 of this course, and pasting in a command that will show a script file on screen, using the bash cat command. At the top of the script are the variables and these are passed to the AZ storage account create command at the bottom. Pasting in a further command I'm going to execute the script. Fortunately, I haven't got the correct permissions to execute it, so I'll just fix that now, (keyboard clicking) and try and execute the script once more. If we wait a moment, the script starts to run, and the storage account is created. Now, this is also where Azure CLI, and Azure PowerShell differ. So, if I execute the same script once more, the command executes successfully. Now in PowerShell, this would display a resource already exists error.
>
> **[6:23](https://www.linkedin.com/learning/manage-storage-in-azure/create-and-manage-a-storage-account-with-the-azure-cli?u=76281980&t=383)** This is because the Azure CLI is idempotent. It doesn't matter how many times you run the command; you will always receive the same result. In PowerShell, I would need to check the existence of the resource first to check that it didn't exist. So, when should you use the Azure CLI and when should you use PowerShell? Although both run on [Windows](../../Glossary/Service/Windows.md), Mac and Linux, there are some differences, such as shell support as shown in this table, that may sway your choice. But [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md)'s recommendation says, it's up to you. You should take your experience into account. So, if you are from a Windows sysadmin background and have a strong history with PowerShell, then Azure PowerShell is likely to have less of a learning curve.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (19), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (14), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (8), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (2), [GitHub](../../Skills/Software%20Development/GitHub.md) (2)
> **Env Vars:** cli (14), json (2), sa0304azcli (1), rg0304 (1), sku (1)
> **Tools:** powershell (8), bash (4), command line (3), github (2)
> **CLI Commands:** az (8), find (2), make (1), cat (1)
> **Analogies:** such as (2), for example (2)
> **Non-Speech:** (keyboard clicking) (4)
> **UI Navigation:** switch to (1), scroll up (1)
> **Definitions:** is a  (1), is an  (1)

#### [Deploy infrastructure for Azure Storage using Azure Bicep](https://www.linkedin.com/learning/manage-storage-in-azure/deploy-infrastructure-for-azure-storage-using-azure-bicep?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/deploy-infrastructure-for-azure-storage-using-azure-bicep?u=76281980&t=0)** - [Instructor] One of the key features of cloud deployments is the ability to deploy a complete infrastructure using code. This is also known as [infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) or IAC for short. In [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md), this is possible due to the Azure Resource Manager which enables requests to be passed to Azure Services through a single API, regardless of the type of requester as shown in this image from the [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) documentation. The Azure Resource Manager can also take ARM templates to create a resource or multiple resources where an ARM template describes the desired outcome of the resources in a [JSON](../../Skills/Web%20Development/JSON.md) format. For example, if I access this storage account and scroll down on the left hand side and under automation choose to export a template. Then the Azure portal will generate an ARM template that could be exported as infrastructure as code, and used to create this resource once again. In the background, it is the Azure Resource Manager that is generating the template and then passes it to the portal to display. Scrolling down through the JSON definition you will see the properties of this storage account that we have already discussed such as the SKU and network settings. Now, this template only contains a single resource but if I go back to the resource group by going through the overview,
>
> **[1:35](https://www.linkedin.com/learning/manage-storage-in-azure/deploy-infrastructure-for-azure-storage-using-azure-bicep?u=76281980&t=95)** and then scroll down to export template for the resource group and just wait for the template to be generated. There were over 12 resources in this resource group and the template has got a lot more complicated. Now, this format is perfect for the Azure Resource Manager but authoring templates such as this is very demanding for most users. This is why Azure Bicep was created to make the process of authoring complex deployments as infrastructure as code a lot easier. Before taking a look at Azure Bicep this concept of describing the outcome is an important one for infrastructure as code. So far in this chapter we have looked at scripting a deployment. This is known as imperative deployment. You are stating all the steps that must be taken to get your desired outcome. Thinking in terms of creating a chocolate cake you would provide the recipe to follow. This is very different to describing the outcome of a deployment, also known as a declarative deployment. Coming back to the chocolate cake analogy, a declarative deployment would be like stating "I'd like a chocolate cake with icing", but it would leave the recipe to whomever is making the cake. ARM templates and Azure Bicep are declarative approaches to infrastructure as code. On receiving a Bicep file or ARM template, the Azure Resource Manager decides the steps
>
> **[3:08](https://www.linkedin.com/learning/manage-storage-in-azure/deploy-infrastructure-for-azure-storage-using-azure-bicep?u=76281980&t=188)** or compiles its own recipe for the deployment.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (11), [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (5), [JSON](../../Skills/Web%20Development/JSON.md) (2), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **Env Vars:** arm (5), json (2), iac (1), api (1), sku (1)
> **Exercise Files:** template (9)
> **Definitions:** known as (3), is an  (1)
> **Analogies:** such as (2), for example (1)
> **UI Navigation:** scroll down (2)
> **CLI Commands:** make (1)
> **Cross-References:** go back to (1)

#### [Deploy infrastructure for Azure Storage using Azure Bicep demo](https://www.linkedin.com/learning/manage-storage-in-azure/deploy-infrastructure-for-azure-storage-using-azure-bicep-demo?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/deploy-infrastructure-for-azure-storage-using-azure-bicep-demo?u=76281980&t=0)** - [Instructor] So what does a Bicep file look like? This is Visual Studio Code. I'm going to use this to show the excellent authoring experience it provides for Bicep through the Bicep extension. The icon of four squares on the left opens the list of available extensions. I will search for bicep, and install the extension, which is supplied from [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md), closing the extension description, and going to the file view. Now, if I create a file, which this file, I'll call demolive.bicep. By giving the file the bicep extension, the VS code bicep extension will be utilized as I edit the file. Now, clicking space bar, will kick off the Bicep intellisense that's built into the bicep extension. To begin with, I'd like to create a resource. So I'll choose it, and I'll call this resource stg, which is the symbolic name for the storage resource that I'm going to create. And this is how a specific resource is identified in a bicep file. Adding another space, brings up a list of possible resource providers for the resource that I want to create. I am creating a storageAccount, so I type in storage and choose storageAccounts as my provider. Next, is the API version I'd like to use. I'm going to take the latest,
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/deploy-infrastructure-for-azure-storage-using-azure-bicep-demo?u=76281980&t=94)** but others are provided to try and ensure breaking changes to the APIs don't stop the [infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) from working. Typing the space bar once more, I have the choice of setting a definition for a resource or selecting an existing resource. I'm creating a new one, so I'm going to choose the equal sign. Typing space once more, I have the possibility of creating loops, using conditions, or in this case, I just want to choose to set up a template of storageAccount required properties. And these are the required properties for storageAccount when defined in a bicep file. I'll fill in the name as sa0305bicep, and strings must be placed within single quotes, and a location of uksouth. (keyboard clattering) When I get to the name of the sku, pressing space will give me a list of valid skus, which I can pick from. I'll choose Standard_LRS, and this is the same for kind. We have our premium storage types and our general purpose V2 storageAccount. Now notice the yellow line under uksouth, if I hover over it, it shows that best practices are not being followed and that the location should not be hard coded. If I click Quick Fix, it's showing what the Quick fix is,
>
> **[3:06](https://www.linkedin.com/learning/manage-storage-in-azure/deploy-infrastructure-for-azure-storage-using-azure-bicep-demo?u=76281980&t=186)** which is to create a new parameter called location. This is how parameters are coded within a bicep file. The editor has even added a parameter decorator describing the parameter. Parameters can be used as shown or overridden on deployment. This is now ready to deploy at the default scope of resource group. So saving the file, and switching into the Terminal, I'm going to use an [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) command to deploy this bicep file, and I've got a copy of the command in my notes and I'll paste it in. I'll just change that to a little a. (keyboard clattering) So az deployment group create, or create a deployment at the resource group level. So long as the resource group already exists. I'm passing in the template file that I've just created, which is demolive.bicep, and I just want to change these to specific single quotes. Hitting enter, wait a few moments. Bicep is now compiling into adjacent template to send to the resource manager. This is compiled successfully and the deployment is now running. The storageAccount has deployed, and scrolling up, we can see the [JSON](../../Skills/Web%20Development/JSON.md) representation of the storageAccount deployment.
>
> **[4:41](https://www.linkedin.com/learning/manage-storage-in-azure/deploy-infrastructure-for-azure-storage-using-azure-bicep-demo?u=76281980&t=281)** Now, this is a nice example to start with, but the real power of bicep is deploying more complex deployments with multiple interlinked resources. This bicep file creates a storageAccount and a virtual machine, and VNet, and deploys the storageAccount into the VNet using a private link. Before looking at some of the features of this bicep file, I can right click on the file and choose to Open the Bicep Visualizer, which will visualize the deployment for me, and show how all of the resources are linked. Returning to the file, at the top, I have the parameters. You will notice that the admin password is set as secure. This means, it will not be shown at the command line or in the outputs in the portal. You can also set up arrays of allowed parameters, and scrolling down, we have some variables. For example, this is the storage name and it is being set to a unique value based on the resourceGroup id. Scrolling down a little further, we have the definition for the storageAccount, and above it, the network, which you can see is a module, this means it can be reused by multiple bicep files and pass different parameters for each use. Scrolling down a little further, we have the definitions for the PrivateEndpoint, and its private links,
>
> **[6:15](https://www.linkedin.com/learning/manage-storage-in-azure/deploy-infrastructure-for-azure-storage-using-azure-bicep-demo?u=76281980&t=375)** which take a subnetID from the network defined at the top to deploy into. Right at the bottom of the file is the virtual machine, which is once again, a reusable module. I can deploy this from the command line just as before using az deployment create, which I'll paste in the command from my notes. Hitting return, the bicep file is compiled into adjacent format acceptable to the resource manager once more, and I need to add in an admin password. The deployment will now run. And after a moment, the deployment has finished. And if we switch to the portal and into the resource group for this video, which is 0305, we can see all of the resources that were created by that bicep deployment. And if I enter the storage account and go to its networking, the public network is disabled, and the private endpoints have been created. Now, that's just the tip of the iceberg for infrastructure as code deployments with Azure Bicep. But I'm sure you can see how powerful at all this can be. Join me in the next chapter where we'll look at authentication and authorization methods for Azure storage.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (3), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1), [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (1), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Code Identifiers:** storageaccount (9), storageaccounts (1), resourcegroup (1), subnetid (1)
> **Tools:** command line (2), visual studio (1), vs code (1), terminal (1)
> **UI Navigation:** click on (1), open the (1), switch to (1), go to (1)
> **Env Vars:** api (1), cli (1), json (1)
> **Exercise Files:** template (3)
> **Prerequisites:** set up (2), install (1)
> **CLI Commands:** az (2)


### 4. Authorizing Operations on Azure Storage

[↑ Back to Table of Contents](#table-of-contents)

#### [The control plane and the data plane](https://www.linkedin.com/learning/manage-storage-in-azure/the-control-plane-and-the-data-plane?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/the-control-plane-and-the-data-plane?u=76281980&t=0)** - [Instructor] When starting to work with [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage for the very first time it can be really confusing why some operations are authorized and some are not. For example, in the storage account, I have a container, and within this container is a single blob, which I can list and then using the three ellipses at the right hand side I can also view the blob. But if I close this down and enter the cloud shell where you can see I've already logged in to AZ copy and this is with the same identity. If I use the AZ copy command to access the same blob from the same storage account and download it to this destination, if I scroll up a little I get an authorized permission mismatch. But how can this be when I can view it in the portal? Let's take a look at why. When operating with resources in Azure, every interaction you have will either be in the control plane or the data plane, where the control plane enables management of resources in a subscription, and the data plane exposes capabilities of an instance of a resource. For a storage account, management operations, such as creating, updating and deleting a storage account are all managed by the Azure resource manager. And this is at the management.[azure.com](https://azure.com) endpoint.
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/the-control-plane-and-the-data-plane?u=76281980&t=94)** The Azure resource manager applies Azure policy and appropriate management logs and writes to the activity log about each requested activity. It authenticates a request and then hands the request to the appropriate resource provider. In this case, [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md).Storage which carries out the request. If I want to interact with the data within a storage account the request must go to the appropriate endpoint for the resource. For a storage account on the data plane, there is one for each service. An authorization of activities is separate for each service. So how did my identity get access to the blob, given it was not all authorized to view it at the data plane level? When the blob request came in on the portal it hit the instant specific endpoint. But because I was in the portal it was able to retrieve an access key granting access to the data using a control plane permission which has been granted to my identity. When I tried on AZ copy it could not automatically retrieve the same access key as the session was in the cloud shell not in the portal. So unlike the storage accounts in previous videos this storage account within its configuration has public access to blobs turned off. If I enable blob public access which is a control plane activity and save it and return to the container, I can see
>
> **[3:10](https://www.linkedin.com/learning/manage-storage-in-azure/the-control-plane-and-the-data-plane?u=76281980&t=190)** that this container was set to private access on creation. This was the only setting available at the point of creation as blob public access was disabled. But if I select the container now and choose to change its access level I can choose anonymous read access for the blobs inside the container, selecting for both and clicking OK. I can now return to cloud shell and switch to Bash cloud shell. Log back in once more, copying the code into the device login page.
>
> **[4:02](https://www.linkedin.com/learning/manage-storage-in-azure/the-control-plane-and-the-data-plane?u=76281980&t=242)** For the same identity and returning to the cloud shell and pushing the up key to choose the same command the file downloads and I can display its content by using the cap command. Note that we can mix and match access at the container level. If I create a new container, I'll just close the cloud shell so we can see a little bit more on the screen. I can create a new container and I can set its access to private. So I have one where it's anonymous and one that's private. Only Azure blobs has public read access functionality for storage. All other services need authorization to access the operations at their instant specific endpoint. The [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the videos in this chapter will look at the available ways to authorize data operations on those endpoints.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (7), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **UI Navigation:** scroll up (1), go to (1), select the (1), switch to (1)
> **CLI Commands:** az (3)
> **Definitions:** is a  (2)
> **Analogies:** for example (1), such as (1)
> **URLs:** [azure.com](https://azure.com) (1)
> **Tools:** bash (1)
> **Warnings:** note that (1)

#### [Authorize with shared keys](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-with-shared-keys?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-with-shared-keys?u=76281980&t=0)** - When a storage account is created, two 512-bit access keys are also created by default. You have already seen the power of these access keys, which are called shared keys in the previous video, where I can access data within a private container even though the identity I was logged in as wasn't granted specific access to the data. This is because the identity I was logged in as was the storage account owner, so I have permissions to retrieve the shared keys, also known as the access keys. This is something the portal did automatically and then use the key's permissions to access the data. Access keys can be retrieved in the portal through the Access Keys menu or I can go into the Cloud Shell and use [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md), or in this case, [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md). I'll paste in a PowerShell command, and this command gets the storage account key for this storage account. And it's returning the first value because there are two keys, and that has returned the storage account access key. So let's copy this key and then we can use it. This is Azure Storage Explorer, which you may recall us using in the first chapter. We can use the key to attach to a resource,
>
> **[1:36](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-with-shared-keys?u=76281980&t=96)** and the resource that we want to attach to is the storage account or service. And I can choose to use an account name and key, and click Next. I'll add a display name for this connection (keyboard clacking) and the key that we've just copied from that PowerShell command, and then I just need the account name, which I'll copy from my notes. Clicking Next and clicking Connect, a new connection has been successfully added which I can see in the Storage Explorer. This is the one that we've just added, manage storage 0402, and it shows the type of connection here. If I double-click on this attached storage account, the detail for the storage account loads. And using this key's permissions, I can right-click on the Blob Containers and choose to create a new blob container. (keyboard clacking) Or I could go into an already created container and choose to delete a blob. The blob is now listed as deleted. So if I go back to the portal and into the same container, there is no blob3.[JSON](../../Skills/Web%20Development/JSON.md) file. We can also use keys within applications. So if I enter the Cloud Shell once more and go into my repository (keyboard clacking) and open a code view,
>
> **[3:11](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-with-shared-keys?u=76281980&t=191)** I'll just drag this up so it's a little bigger, I have the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) file within the folder for this video. Let's drag it up a little bit more. At the bottom of this Python file, I have a place to add my access key which I will copy from the output of my PowerShell,
>
> **[3:37](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-with-shared-keys?u=76281980&t=217)** and I also have a place for the storage account name which I will also copy from the same PowerShell command. This Python file takes these two values and sets up this object, which we can use to interact with the Azure Blob storage service and then uses the service's in-built methods to list blobs in this given container. The container being, here, testaccountkey. So if I save the file and close the editor, I can run the file just like an application would by using a Python command at the command line. That looks like I've got my slash around the wrong way. I'll just change that and run the command once more, and all of the blobs that were inside that container are listed. As you have seen, keys grant full access, enabling configuration of the storage account, including the account's data. Therefore, access keys can be used maliciously to read, alter, and delete data within the account from any application and client that can use them. So please be careful with your keys. You must protect shared keys, and Azure gives you the following options. If you must use shared keys, ensure your applications read the key from key vault when they're required. This secures the key,
>
> **[5:10](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-with-shared-keys?u=76281980&t=310)** allowing only applications that have been granted access, the ability to read the key. Make sure you rotate the keys periodically to minimize risk if a key is compromised. This is why there are two keys. You can switch applications to using the second key whilst the key originally being used is regenerated. To help with ensuring keys are rotated in a timely manner, you can set an expiration policy which places a banner in the portal when the key is due to be rotated. The expiration policy can be monitored for compliance with Azure policy. You should use logs to monitor key usage for suspicious activity. And if none of the above meet your organization security policies, you can also disable shared key usage. So let's have a look at all of these options in the portal. Here we are back in the portal at the same storage account that we used in the earlier demo. Rotating keys can be achieved through the Access Key pane by clicking on Rotate Key next to the key name. Note if I click Rotate key and regenerate it, the last rotated date will change. We just can't see it here because the key was created and rotated on the same day. We can, of course, rotate keys using Azure CLI and Azure PowerShell.
>
> **[6:43](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-with-shared-keys?u=76281980&t=403)** First of all, we'll retrieve key1 in PowerShell by pasting in the same command that we used earlier in the demo. Now, I'll paste in a second command which will rotate the storage account key for us. And now, if I use the history within PowerShell, the storage account key for key1 has changed. Closing the Cloud Shell, we can see that the option to set a rotation reminder is at the top of the page, where there is a reminder that this will set a banner in the portal and not notified via email or other means. You have the option to be reminded every day to yearly or set a custom duration. We'll leave this at 60 days and hit Save to set the reminder. The rotation reminder is also called an expiration policy. So if I search for Azure policy and enter the Authoring menu for Definitions, we can filter for the Storage Category. I'm going to scroll down for Storage. Keep going a little bit further. There it is. And dragging this across so we can see the names. We can see a policy that states "Storage account keys should not be expired." Viewing the definition, we can see that when applied,
>
> **[8:18](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-with-shared-keys?u=76281980&t=498)** it will audit for non-compliance and therefore would be reported on the Compliance dashboard. An example of which is shown here. Returning to the Definitions once more and filtering for Storage once again, and this time searching for Key, dragging the name column across so we can read them fully, we can see that there's a built-in policy present to prevent shared key access being enabled. Once again, the effect of this policy is to audit for non-compliance. If we want to turn off shared key access for a storage account, we can choose the Configuration pane from within the Storage Account Settings and click Disabled on Allow storage account key access and save it. So if I now try to look at the blobs within the containers of this storage account, I cannot. The portal is no longer able to retrieve the key and grant my identity permission to access the data. And finally, for monitoring, as key access is a control plane activity, it can be monitored in the activity log and therefore from within Azure Monitor. And here we can see each of the listings for where I've listed the storage account keys. That is the end of our walkthrough
>
> **[9:53](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-with-shared-keys?u=76281980&t=593)** using keys for accessing a storage account. If your use case must use shared keys, make sure you protect them. In the next video, we will look at Azure RBAC, the recommended way of accessing data in Azure Storage.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (11), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (8), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (4), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Tools:** powershell (8), command line (1)
> **CLI Commands:** python (4), make (2)
> **Cross-References:** previous video (1), go back to (1), earlier in (1), in the next (1)
> **Env Vars:** cli (2), rbac (1)
> **UI Navigation:** double-click (1), right-click (1), scroll down (1)
> **Definitions:** is a  (2), known as (1)
> **Best Practices:** the key is (1), recommended (1)

#### [Authorize operations with Entra ID and Azure RBAC](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-entra-id-and-azure-rbac?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-entra-id-and-azure-rbac?u=76281980&t=0)** - [Instructor] Throughout this course so far the permissions given to users of the data in each storage account have been over privileged. Granting public access to blobs or using an access key that enables full access to a storage account does not follow the principle of least privilege where an identity should only be granted the specific permissions necessary to perform a task. The principle of least privilege is a best practice security recommendation to ensure that if an identity is compromised, then the blast radius and therefore impact of that breach is minimized. In [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md), the principle of least privilege can be adhered to by utilizing Azure RBAC, or role-based access control which enables fine-grained access management to resources and data. Azure RBAC is built on the Azure resource manager and uses role assignments to grant permissions. So a service principle, whether that's a user, group, or a type of app registration is assigned a role which can be a built-in or custom role. The role is assigned as a scope, then the service principle comes together to create a role assignment. The role assignment at a very basic level says who has access, what they have been granted access to, and what level they have been granted access. It is scope in Azure that defines the level at which a role assignment is defined. There are four levels of scope in Azure,
>
> **[1:36](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-entra-id-and-azure-rbac?u=76281980&t=96)** the management group, subscription, resource group, and the resource, which for this video series would be storage accounts, but it could be any resource. If you are not familiar with management groups they provide a method of managing access policies and compliance above the subscription level. In fact, there is a root management group defined for all [Azure AD](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) tenants, which all management groups sit under. How you define the hierarchy you need using management groups is up to you. They can be simple or complex, but it is likely to mirror your organization structure in some way. When a role assignment is made, it includes all of the items in the scope below it. So here at the resource group level, the role assignment access just covers the resources in the resource group. But if the role assignment is at a higher level, say this top level management group, then access permissions also apply to everything below it. I've switched the portal now so that we can have a look at role definitions and how role assignments can be made through the portal. This storage account in its configuration has access keys disabled, and it also has blob public access disabled. So currently if I go into my containers and try to look at the blobs in the containers,
>
> **[3:09](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-entra-id-and-azure-rbac?u=76281980&t=189)** I have no way of viewing them or listing what's in this container. In the portal, role assignment is performed in the access control or IAM menu. We can view my identity's level of access to this resource, this storage account by clicking on view my access and my identity as inherited the owner role from the subscriptions scope level, which we can see here. Owner is one of the fundamental roles in Azure along with reader, contributor, and access administrator. Clicking on owner, we can see the privileges it grants. There are over 14,000 of them. It's a very powerful role. The actions are set at the control plane and affect Azure Resource Manager operations and data actions are at the data plane and affect data operations at the resources endpoints. Here you can see owner grants no data actions which is why we cannot view the blobs within the containers or the blobs themselves. Returning back to actions, if we search for access key and scroll down into the [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md).Storage provider the list storage account keys is one of the permissions granted to owner, which is why when access keys were enabled, we were able to use the portal to retrieve the key and view the blob data.
>
> **[4:44](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-entra-id-and-azure-rbac?u=76281980&t=284)** Coming out of the my access view, we need to add a role to my identity so that we can view the blob data. So choosing to add a role assignment, we can see the exhaustive list of built-in roles, including at the top, reader, which is one of the fundamental roles. And in the privileged administrator roles we can see the other three, owner, contributor, and user access administrator. We want to grant at the least privilege. So just the ability to read the blob data. So going back to the job function roles, I'm going to search for storage. So I can view the permissions that are available for a storage account. And here we can see the ones for the services, blob, file, queue, and table. I think we want to choose storage blob reader but let's view its actions and data actions just to be sure. At the control plane level this role allows retrieval of containers and at the data plane level, it allows blob read. You can also view the [JSON](../../Skills/Web%20Development/JSON.md) definition for a role definition by clicking JSON at the top as I just did. Notice the actions and data actions have a definition of provider, resource type, and action. You can export the JSON definitions of built-in roles to use as a basis for custom roles by adding
>
> **[6:18](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-entra-id-and-azure-rbac?u=76281980&t=378)** or removing actions and data actions that you may require. There are also the concepts of not data actions and not actions. These are explicit denies. I'm happy that blob data reader grants just the permissions that we need. So I will select it and scrolling back up to the top, move to the members tab. Here I can choose to assign that role to a user, group, service principle, or managed identity. I need to assign the role to my identity. So selecting user and clicking select members I'm going to search for my identity, which is Azure Instructor 14. I'll select it and click select at the bottom. Azure Instructor 14 is now displayed to assign the role to. So clicking review and assign and review and assign once more, the role assignment will be added. Now this takes a little bit of time so I'm just going to wait a while and we'll switch back to it in a second. On navigating back to the container we can now see the blobs and I'll try and view one of the blobs, which I can now also do. And if I try and edit the blob and click to save, well the portal actually hangs
>
> **[7:52](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-entra-id-and-azure-rbac?u=76281980&t=472)** which may be a bug, but we cannot save changes as re permissions were granted and nothing more. This is great for users and groups but what about that [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) script we used in the previous video that was mimicking an application getting access to a storage account. If you recall with a slide on role assignments we can assign a role to an app registration. I've already created an app registration in Azure AD, which was recently renamed to [Microsoft Entra ID](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md). We can see the app registration listed here and I've noted its client secret value along with the tenant ID and the application ID of this app registration. And we require those three values to use within the Python script. So navigating back to the storage account we're going to need to assign a role to the app registration to enable it to read blobs in the container. So I'm going to go back into access control once more. And this time add a role assignment, which once more is going to be storage blob data reader but the member is going to be a service principle. So I'll select the members and this time search for the app registration name, which was spmassprbac. I will selected as a member and assign the role.
>
> **[9:31](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-entra-id-and-azure-rbac?u=76281980&t=571)** The role is now assigned so opening Cloud Shell and switching to Bash. I'm going to need to edit my Python script which is in the folder for this video. Just drag this up a little bit so we can see it a little clearer. And as explained previously we're going to need the tenant ID, application ID and application secret that were copied from the app registration. Now I've got these noted down, so I'll just paste them in. And we're also going to need the storage account name, which is shown at the top of this page. So by using these three values we're going to be able to retrieve a credential that is going to enable us to list the blobs by using that RBAC that we assigned to the app registration. So saving the file and closing the editor. I'm going to run the Python file and our blobs are listed. So that's how to grant just re privileges on a storage account using the fine grain control of Azure RBAC. In the next video, we'll have a look at how to utilize attribute based access control.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (11), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (4), [JSON](../../Skills/Web%20Development/JSON.md) (3), [Azure ad](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) (2), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **Env Vars:** rbac (4), json (3), iam (1)
> **CLI Commands:** python (4)
> **Cross-References:** previous video (1), in the next (1)
> **UI Navigation:** scroll down (1), select the (1)
> **Definitions:** is a  (2)
> **Tools:** bash (1)
> **Best Practices:** best practice (1)

#### [Authorize operations with Azure AD and Azure ABAC storage](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-azure-ad-and-azure-abac-storage?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-azure-ad-and-azure-abac-storage?u=76281980&t=0)** - [Instructor] In the previous video, we explored how [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) role-based access control, or RBAC, enables fine-grained access to a storage account and its data, but there are scenarios where the granularity of access that is possible through RBAC doesn't quite go far enough. For example, in this scenario, the storage account, itopslogging, stores logs for the IT operations team. The storage account contains a sales app container, and within the sales app container are two virtual directories, where server logs for the sales application are stored to the server directory and the workstation directory for workstation logs. There are two user groups that require read access to these directories. Level two support is read access to workstation blobs only, and level three support is access to workstation and server logs. We can grant storage blob data reader to fit the requirement of the level three support group, but there is no RBAC role to suit read access to the workstation virtual folder only. If we continue to add more containers as the IT ops logging requirement grows, how can we ensure each group has just the privileges required for their roles? This is where Azure ABAC comes in, where ABAC stands for attribute-based access control. ABAC extends the RABC functionality with conditions on specific actions,
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-azure-ad-and-azure-abac-storage?u=76281980&t=94)** enabling fine-grained access to resources and reducing the number of role assignments, as the principal itself can become part of the check. The controls can be expressed in terms of projects, organizational units, or [Data Classification](../../Skills/Data%20Science/Data%20Classification.md) levels through attributes such as tags, enabling access rules that will match the meaning for a business. Azure ABAC is evolving. There are currently GA support for blobs, queues, and Data Lake Storage Gen2 with more features and services in Preview. Azure ABAC uses conditions, which are conditional checks that occur on authorization of an action, which in the case of Azure storage is a data action combined with an expression, in terms of the security principal that was accessing it, the resource the request was made to, the parameters sent with the request, and the environment the request was made in. In terms of setting up a condition for the level two support team, the team needs to be able to read blobs in the workstation. So read is the action, the resource is the blob path of workstation, and level two support is the service principal. Note, to use Service Pincipals, you need [Microsoft Entra ID](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) Premium. Now let's have a look how we can set up Azure ABAC in the portal. This storage account has blob public access disabled and a container called Sales App,
>
> **[3:09](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-azure-ad-and-azure-abac-storage?u=76281980&t=189)** which has two virtual folders of server and workstation, the same setup as shown in the slide deck. We're going to set up read access on the workstation virtual directory only and assign this to my logged in identity. So we enter access controller as before and want to start by adding the storage blob reader role, which I'll search for. As soon as I select this role, the portal knows that this role can have conditions applied to it, so the conditions tab is activated. The tenant I am using does not have Microsoft Entra ID Prem One or Two, so I'll be assigning the role to my identity, which I'll search for in select members. And Azure Instructor 14 is the identity that I'm logged into in the portal. Clicking next, this is where the condition can be added using add condition. If you recall the previous slide, the action was read, so we'll add a read action, but we could have also chosen to list blobs. Next, we need to add the expression, which will be evaluated every time a blob is read. The attribute sources are environment, request, and resource, and this is where Service Principal would be displayed,
>
> **[4:44](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-azure-ad-and-azure-abac-storage?u=76281980&t=284)** if we were using Prem One or Two. Choosing Environment, we can take a look at the attributes available to create an expression against. Environment mainly looks at where the request came from. Request allows selection of parameters from the request string of Version and Snapshot, but we need to add an expression for resource, and the resource is the path name. But also note, blob index tags are available. This is where key value pairs can be used to describe data in terms of your organization. Selecting blob path enables the choice of a string operator or a cross product operator. Now, blob path returns a single string, so we're going to use string like and a value of workstation with a wild card. So this will return everything with a path like workstation. If we were going to state that we needed a specific container, we could add another expression, but what we have so far satisfies the requirement. So, I'll click save. The editor format for the condition is now shown, and we'll click review and assign, and this will assign the role to my identity. It takes a little while for the permissions to be correctly applied, but once the condition is applied,
>
> **[6:18](https://www.linkedin.com/learning/manage-storage-in-azure/authorize-operations-with-azure-ad-and-azure-abac-storage?u=76281980&t=378)** if we now navigate back to the container, we can list the underlying virtual directories and their blobs, but choosing to view a blob in the server virtual directory returns an authorization error. But if I go back and choose to view a blob in the workstation virtual directory, it passes the condition check and the blob is displayed. The condition we have set up is working as expected. Now that we have a good understanding of Azure RBAC and ABAC, let's look at managed identities and passwordless access in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (8), [Microsoft Entra ID](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) (2), [Data Classification](../../Skills/Data%20Science/Data%20Classification.md) (1)
> **Env Vars:** abac (7), rbac (4), rabc (1)
> **Prerequisites:** set up (3), setup (1)
> **Cross-References:** previous video (1), in the next (1)
> **Definitions:** stands for (1), is a  (1)
> **Analogies:** for example (1), such as (1)
> **Speakers:** - [instructor] (1)

#### [Accessing storage account data from other Azure services](https://www.linkedin.com/learning/manage-storage-in-azure/accessing-storage-account-data-from-other-azure-services?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/accessing-storage-account-data-from-other-azure-services?u=76281980&t=0)** - [Presenter] There are many paths and IAS services in [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) that could be required to access data in your storage accounts. But how can you ensure that these services get the access they require whilst minimizing your administrative overhead? So far, we've mainly been accessing the data endpoints with a logged in user who has authenticated into the portal or the Azure Storage Explorer. But what if your solution requires access from web apps or function apps, [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md), container-based services or even ingestion into a data pipeline? It is possible that none of these services will use a logged in user's credentials to access data. So how is access granted? We've already seen the first piece of the puzzle when looking at Azure RBAC. A [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) script was executed in Cloud Shell with a credential from an app registration with a client secret. We don't have to worry about securing a password in this case, but the values that make up the service principle with a secret should be protected. We could store them in a key vault but is likely that the same issue would apply. How do we store the credentials to access the key vault? This management of secrets and credentials is a common problem for administrators and developers. This issue can be addressed by using managed identities where the credentials are automatically managed for you in [Microsoft Entra ID](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md), you don't even have access to them. In Azure, there are two types of managed identities,
>
> **[1:39](https://www.linkedin.com/learning/manage-storage-in-azure/accessing-storage-account-data-from-other-azure-services?u=76281980&t=99)** system assigned and user assigned. A system assigned identity is attached to a single resource. It has a lifecycle with the resource. Once the resource is deleted, the identity is automatically deleted too. A user assigned identity is a standalone resource that can be attached to one or more resources enabling multiple resources to share an identity. So how are managed identities useful in our use case of multiple types of compute accessing a storage account? RBAC roles can be assigned to managed identities, enabling the underlying resource to be granted access to work with the storage accounts and any other supported service. This is a page from the [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) documentation and it lists the services that support managed identities for Azure Resources. You can view the docs page at the address shown on the screen. As you can see, this is an exhaustive list which of course includes Azure VMs. Let's switch to the portal and look at how to set up read access to storage from a virtual machine using a managed identity. I've switched into the portal and this is the resource visualizer for the resources deployed for this demo. Now I'm in the portal inside a virtual machine. The virtual machine at the top here is attached to this vnet and inside this vnet, a private endpoint has been deployed
>
> **[3:14](https://www.linkedin.com/learning/manage-storage-in-azure/accessing-storage-account-data-from-other-azure-services?u=76281980&t=194)** which is attached to the storage account, SAO405. And this storage account has had blob public access disabled so that we can access the data in the storage account from the portal. I've granted my identity storage blob data contributor and therefore, as we are accessing the portal from inside the virtual machine, we can read the blob data. But if I copy the URL for this blob from the blobs properties and paste it into the browser, I cannot access the blob. 'Cause this method of access does not have my identities credentials. Let's rectify this by adding a managed identity to the virtual machine that I'm currently logged into. So I'll go back into the resource group and into the configuration of vmwin-0405 and in the left hand menu system is identity. We have the choice of the two identities we described in the slide deck, system assigned and user assigned. We don't need to keep this identity beyond its use with this virtual machine. So we'll stay with the default of system assigned by setting the status to on and clicking Save. The identity has been created.
>
> **[4:48](https://www.linkedin.com/learning/manage-storage-in-azure/accessing-storage-account-data-from-other-azure-services?u=76281980&t=288)** So now we need to assign a role to the identity from the storage account. So navigating back to the storage account in the resource group and back into access control, we'll choose to add a role assignment of Blob Data Reader
>
> **[5:13](https://www.linkedin.com/learning/manage-storage-in-azure/accessing-storage-account-data-from-other-azure-services?u=76281980&t=313)** but this time when clicking Next, we'll choose managed identity to assign the access to and when selecting the members, we choose a system assigned identity and there's the assigned identity, which we've assigned to our virtual machine, so it gets the same name as the resource you've assigned it to. So I select this and click Select and it's added as the member. And now we'll review and assign the role. It takes a few moments for the role to assign. So while this is happening, we'll update the storage account name in the Python script which I need to open up in Visual Studio. It's in chapter four and 0405, which is the folder for the video that we are currently going through. And the file I need to edit is editblobsidentity.py. Just going to move the terminal down a little bit and close this notification. And I'm going to need to copy the storage account name and update this URL. So I'll copy it from the portal and paste it into my Python file. Save the file. Note that our script now sets up the blob service client, client object with an instance of the managed identity credential and uses this to download and output the contents for a given blob to the screen.
>
> **[6:51](https://www.linkedin.com/learning/manage-storage-in-azure/accessing-storage-account-data-from-other-azure-services?u=76281980&t=411)** So if I run my Python file, just drag this up a little bit so we can see the output, identity.pi. We can see the context of the blob output to the screen.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (6), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (4), [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) (1), [Microsoft Entra ID](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) (1), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **Env Vars:** rbac (2), url (2), ias (1), sao405 (1)
> **CLI Commands:** python (4), make (1)
> **Definitions:** is a  (3), is an  (1)
> **Tools:** visual studio (1), terminal (1)
> **Prerequisites:** required to (1), set up (1)
> **File Paths:** editblobsidentity.py (1)
> **Documentation:** the docs (1)

#### [Enable passwordless Entra ID–based access to Azure Storage](https://www.linkedin.com/learning/manage-storage-in-azure/enable-passwordless-entra-id-based-access-to-azure-storage?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/enable-passwordless-entra-id-based-access-to-azure-storage?u=76281980&t=0)** - [Instructor] In this chapter so far, we have looked at how each of the following can be granted access to data: [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Resources, which can have a managed identity; Azure Resources or Non-Azure Resources which cannot have a managed identity; and users through a browser, IDE, or command line. In each of these scenarios where application code may need to be run, it has been seen that a different SDK credential type may be needed for each scenario, and this means three versions of application code for the same job. Now, ideally, IT pros and developers should be using one version of the code. Azure enables this using the default Azure Credential which can be used as a credential type in each of these scenarios. Therefore, you only use one version of the code. The default Azure Credential combined common methods of authentication into a single check as described in this page of the Azure documentation. Making the picture a little bit clearer, the default Azure Credential starts by looking for specific environment variables. Then depending on the environment the application that is calling it is running in, it checks for various identities. Then whether a user has already authenticated into a command line or IDE or will launch an interactive browser, this method is classed as passwordless as in each scenario, apart from the interactive browser, the default Azure Credential is retrieving credentials
>
> **[1:35](https://www.linkedin.com/learning/manage-storage-in-azure/enable-passwordless-entra-id-based-access-to-azure-storage?u=76281980&t=95)** from the environment it is running in. I'm back in our demo environment and back logged into the virtual machine so that we can see this in action in our demo setup. I have the second [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) script from the 04_05 folder. And this time, we're using the credential type of default Azure Credential. And this is going to run on the Azure VM, which has the managed identity. So if I scroll down to the bottom and update the script with the storage account name and save the script, I'm now going to execute it, default_cred.pi. And once again, the blob is read, the default Azure Credential has worked down the credential chain and has fetched the managed identity and used this to authenticate and authorize access to the blob, which has been read and output to the screen. If I now return to the portal and back to our virtual machine, I want to disable the identity so that it's no longer available to default as your credential. Just need to wait a little while for that to execute and it's now disabled. So switching back to Visual Studio Code and I'll execute the Python script again. If I expand the terminal window,
>
> **[3:12](https://www.linkedin.com/learning/manage-storage-in-azure/enable-passwordless-entra-id-based-access-to-azure-storage?u=76281980&t=192)** this time, we can see it failing and we can also see it gradually going through the credential chain to try and find the credentials that it can use to access the blob. So we'll run AZ login to imitate a developer or IT pro working on an application. I'll choose my identity, return back to Visual Studio Code and I'm now logged in and I'm going to run my script once more. The authentication token created by AZ login is retrieved and the blob is read using the role assignment to the identity. I think you can see how powerful default Azure Credential can be when working on applications as a development team and IT pros. So that's all for managed identities and passwordless connections in this video. Join me in the next one where we're going to take a look at [SaaS](../../Glossary/Concept/SaaS.md) [Tokens](../../Skills/Web%20Development/Tokens.md).

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (13), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2), [SaaS](../../Glossary/Concept/SaaS.md) (1), [Tokens](../../Skills/Web%20Development/Tokens.md) (1)
> **CLI Commands:** python (2), az (2), find (1)
> **Tools:** command line (2), visual studio (2), terminal (1)
> **Env Vars:** ide (2), sdk (1)
> **Code Identifiers:** default_cred (1)
> **Cross-References:** in the next (1)
> **UI Navigation:** scroll down (1)
> **Analogies:** picture (1)

#### [Shared access signatures and access delegation](https://www.linkedin.com/learning/manage-storage-in-azure/shared-access-signatures-and-access-delegation?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/shared-access-signatures-and-access-delegation?u=76281980&t=0)** - [Instructor] So far in this chapter, we have explored account key and [Microsoft Entra ID](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) based access to storage. There may be scenarios where account key based access poses too much of a risk, but access via Microsoft Entra ID permissions is not possible. For these scenario, a shared access signature or SAS can be used to grant specific permissions to storage resources. A SAS is secured by the storage account key or with Microsoft Entra ID. For a SAS secured with an account key, access can be granted to table, queue, file, and blob services with a SAS generated at the account level enabling get and set of properties at the service level. A SAS secured with Microsoft Entra ID is called a user delegation SAS, and is the recommended method of generating a SAS as it does not require a storage account key to generate. User delegation SAS grants permissions on blob storage only and are a combination of the Microsoft Entra ID permissions for the credentials used to create the SAS along with permissions chosen for the SAS. Therefore, if the Microsoft Entra ID credentials have blob storage reader and the SAS permission is selected with Read/Write, only the Read permissions would be authorized. Another key feature of SAS is its ability to be time limited as start and end times can be specified for a SAS. This is a powerful feature enabling time bound access to resources,
>
> **[1:33](https://www.linkedin.com/learning/manage-storage-in-azure/shared-access-signatures-and-access-delegation?u=76281980&t=93)** ensuring a client only receives access when specifically requested. There are two [Forms](../../Skills/Web%20Development/Forms.md) of SAS: ad hoc where permissions and expiry are set at the point of SAS creation and stored access policies where permissions and expiry are chosen upfront and stored against a resource container with a resource container being a blob container, file share, table or queue. When a SAS is created using a stored access policy, it inherits the permissions and expiry of the policy. Stored Access policies add protection against a compromised SAS by enabling a SAS to be revoked without regenerating the account keys, which is the mitigation against a compromised SAS secured with an account key. Therefore, only the specific SAS is invalidated and not every SAS that has been secured with the key. A common scenario for SAS usage is from users logged into clients that require access to a storage account you control. We'll use this scenario to see how a SAS works. The client requests a SAS token from the provider which is likely to be compute in [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) running the client libraries. The SAS provider requests a user delegation key for a user delegated SAS token or the storage account key for an account or service SAS. If the credentials being used by the SAS provider have the correct RBAC permissions,
>
> **[3:07](https://www.linkedin.com/learning/manage-storage-in-azure/shared-access-signatures-and-access-delegation?u=76281980&t=187)** the key is returned. The SAS provider uses the key to secure the SAS token which contains the permissions and expiry for the token which is returned to the client. The client then uses the token with the URL to access the data. I've switched to the Azure portal as we're going to explore how keys are created in the portal and use Azure Storage Explorer as the client. We'll start by creating account level SAS which in the portal is achieved in the shared access signature pane. It is confirmed at the top here that account level SAS enable access to all services and we'll untick queue and table which have been ticked for us in allowed services, and allowed resource types is where a single service account SAS and a service SAS differ as we can select service resource types. So we'd also like to access service, container and object where an object would be a blob or a file. I'm going to keep all of these permissions the same, but we could just choose to generate a read SAS. Scrolling further down, here is the expiry and start time. These can have a 15 minute SKU on them, so I'm going to set the start time to around 15 minutes earlier to make sure that this demo is not affected by the SKU, but I could use this to time box the access into the future. We can also limit the SAS to specific IP addresses
>
> **[4:44](https://www.linkedin.com/learning/manage-storage-in-azure/shared-access-signatures-and-access-delegation?u=76281980&t=284)** and protocols. Scrolling further down and hitting generate SAS and connection string, the connection string and SAS are generated. We can't see them here due to security restrictions, so I'll copy the connection string and paste it into notepad. And now we can see the sections that make up the connection string with the SAS. Firstly, we have all of the endpoints listed. Now I'm going to delete the queue endpoint and the table endpoint because we've not granted access to those. Next we can see the version of the service we want to use, the selected services of blob and file, the service resource types of service, container and object. These are all the permissions that were selected. We have the start and expiry times, the protocols, and this final part of this signature is all of these sections secured with the user delegation or account key, depending on which one you choose. Each of the parts to the right of shared access signature make the SAS token. I can now take this connection string and use it to create a connection in Azure Storage Explorer. Choosing to attach a resource and a storage account, I have the choice of using a connection string
>
> **[6:22](https://www.linkedin.com/learning/manage-storage-in-azure/shared-access-signatures-and-access-delegation?u=76281980&t=382)** or a shared access signature. Now I copied a connection string, so we'll leave it at that default and click next. For the display name, I'm going to choose SAS connection string, and I'll paste in the connection string from my notepad and click next. Here we can see that the Storage Explorer has picked up all of the settings that we discussed previously and has also listed the permissions, resource type access and service access. Clicking connect, and the storage account has been attached. Now I know there's a blob container in this account, but ah, I've got an error. So why is this? If I switch back to the portal, we are back at the shared access signature page and right at the top of the page was this banner which you may have noticed earlier. This storage account has authorization with account keys disabled. So even though the SAS is signed with the key, we can't use it to authorize, so we need to switch to the configuration pane and enable storage account key access. But before I save the changes, we'll also enable an upper tier limit for SAS expiry. This is best practice for SAS generation to minimize the effect of a SAS being compromised. We'll set it to two hours. So now SAS keys can allow
>
> **[7:55](https://www.linkedin.com/learning/manage-storage-in-azure/shared-access-signatures-and-access-delegation?u=76281980&t=475)** no more than two hours access at a time. Clicking save and returning to the Storage Explorer and closing the authentication error and refreshing the blob containers. We can now list containers and the blobs with their virtual directories, and it's just queuing up the transfer of the blobs, so we can also access the blobs. These will all allow permissions of our SAS. If I minimize Visual Studio Code, this storage account also has a stored access policy on this file share, but if I right click on it, I cannot access it to generate a SAS as the SAS used to generate this connection does not allow me to do so. So moving down in the Azure Storage Explorer, the connection that uses my Microsoft Entra ID connections which has the correct permissions to manage access policies, and right clicking once more, I now have the option to generate a shared access signature and use the access policy here, read create list. Note the start and expiry times are grayed out as they are inherited from the policy as is read, create and list. Clicking create, the service SAS URL is generated for use along with the SAS token.
>
> **[9:30](https://www.linkedin.com/learning/manage-storage-in-azure/shared-access-signatures-and-access-delegation?u=76281980&t=570)** We've reached the end of this chapter on authenticating to an authorizing access on a storage account where we have explored how to access data in a storage account as a user using a managed identity on Azure compute or through a service principle on an app registration. Join me in the next chapter when we will look at features that are specific to managing Azure file shares.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Microsoft Entra ID](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) (7), [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (7), [Forms](../../Skills/Web%20Development/Forms.md) (1)
> **Env Vars:** sas (51), url (2), sku (2), rbac (1)
> **CLI Commands:** make (3)
> **Best Practices:** recommended (1), the key is (1), best practice (1)
> **Cross-References:** we discussed (1), in the next (1)
> **Tools:** azure portal (1), visual studio (1)
> **UI Navigation:** switch to (1), click on (1)
> **Definitions:** is called (1), is a  (1)


### 5. Mounting and Synching to Azure File Shares

[↑ Back to Table of Contents](#table-of-contents)

#### [Setting up Azure File Sync](https://www.linkedin.com/learning/manage-storage-in-azure/setting-up-azure-file-sync?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/setting-up-azure-file-sync?u=76281980&t=0)** - [Instructor] [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Files enables file system storage within the cloud in Azure. But there are circumstances, particularly around business continuity, migration, and file centralization, where having a set of files on premises and in the cloud is beneficial. This is where Azure File Sync comes in. When installed on an on-premises [Windows Server](../../Skills/Network%20and%20System%20Administration/Windows%20Server.md), Azure File Sync creates a cache of local files with those in the cloud, therefore extending Azure File Storage functionality and creating a hybrid file service where clients can access the files which are stored both on premises and in the cloud. This functionality is enabled by the Azure File Sync agent which is installed on the [Windows](../../Glossary/Service/Windows.md) file servers. If this were to be managed in an [Enterprise Architecture](../../Skills/Software%20Development/Enterprise%20Architecture.md), it may look like this, with the Azure file share network traffic secured to a VNet. The on-premises file server would make a DNS query to the on-premises DNS server. This server would forward it over a VPN tunnel or equivalent to a DNS server within Azure. This server reads the Azure file private DNS and returns it to the file server. The file server then uses this DNS entry to connect to the private endpoint of the Azure File Share storage sync service so that files can be synced.
>
> **[1:35](https://www.linkedin.com/learning/manage-storage-in-azure/setting-up-azure-file-sync?u=76281980&t=95)** It is the storage sync service that is the glue that connects the file server to the Azure File Share and sets up the synchronization service. And we're going to walk through how to set this all up. Now, unfortunately, I don't have an on-premise setup to create a VPN connection to demo that full architecture. so the Windows file share will be in Azure, but we'll still set up private endpoints for the recommended security configuration. Once these are created, we're also going to look at setting up the storage sync service and its private endpoint and private DNS record. So let's jump into the portal and see how we can set this up. Now, I've deployed all of the resources we're going to need for this demo, and this is the resource visualizer of the resource group that I've deployed them into. Let's [Zoom](../../Skills/Software%20Development/Zoom.md) in a little so we can see these a little better. Now, the deployment includes the virtual machine and the private DNS zone that we showed from the demo architecture, and these are at the top of the visualization, and we can see that the virtual machine is attached to vnet-vm-0501. Moving down a little further, the storage account is already being injected into the VNet using this private endpoint here. So before we create the storage sync service to get syncing to this storage account, I'm just going to check that the private DNS that would've been set up for the file service
>
> **[3:10](https://www.linkedin.com/learning/manage-storage-in-azure/setting-up-azure-file-sync?u=76281980&t=190)** in the storage account is being resolved correctly at the virtual machine. To do this, I'm just going to grab the endpoint of our file service, which I can get in the endpoints pane for the storage account. So there's the file service and this one is the endpoint. And I've already RDPed into my virtual machine and I've also already turned off IE enhanced security configuration because part of the steps for this demo require it to be off to log in to set up the storage sync service. So if I open a command prompt, I can use nslookup to check to see how a service endpoint is set up, and that endpoint resolving to 10.0.0.5, which is a private IP address within my VNet. So I'm happy that that DNS setup is correct. Now, this also means if I open Edge within my virtual machine, I just need to click through the setups because this is the first time Edge has been run on this VM. And I'm going to copy in a command from my notes. Now, this command is hitting the file endpoint for a directory called filesync and it's asking to list the directories.
>
> **[4:45](https://www.linkedin.com/learning/manage-storage-in-azure/setting-up-azure-file-sync?u=76281980&t=285)** Now, it's not going to work because I'm going to need some kind of authentication, authorization on here. So I'm just going to grab a SAS token to do this for me. I scroll to find shared access signature and I want a file service and I want to see containers and objects. The [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) I'll leave alone. I'll grab my SAS token and I can paste it on the end of my URL, which is still in my virtual machine. Now, I'm appending this, so I need to change the question mark, which is for parameters, to an ampersand so that it appends the SAS token to the rest of the URL. I'm hitting Return. Now, there's not a lot returned, but that's because I know that there's no directories actually in this storage account. So it said that it's hit my endpoint for the share of filesync, no other directory path, and there are no directories inside the share. So I'm happy that this is all working correctly between the virtual machine and the storage account. So we're ready to set up the sync service. Now, I'm going to need to go back to the portal, and you can search for Azure File Sync at the top of the portal page and it actually comes up within the marketplace. And when it opens, it opens straight into a configuration page. So we're going to have some mandatory fields. So I need a storage sync service name, so afs0501, for my storage sync service.
>
> **[6:21](https://www.linkedin.com/learning/manage-storage-in-azure/setting-up-azure-file-sync?u=76281980&t=381)** I'll keep the region at UK south. I'd like it in the same resource group as the rest of my resources for this demo. So let's click networking. Now, I can choose to allow access from all networks, but we want to set this up with a recommended configuration of private endpoints only. And the portal is showing me that there is no private endpoint configured, so I'm going to need to add one. And it's picked up our resource group and location once more, but I need to name the private endpoint, so pafs0501, and I'm going to need to know where to deploy the private endpoint to, which is our virtual network that the virtual machine and the storage account is attached to in this demo, which is vnet-vm-0501 and its subnet. Scrolling down a little further, yes, we would like a private DNS zone for name resolution for the endpoint. So that's all our parameters set up correctly. So clicking OK. That's everything we need. We can review and create to create the Azure File Sync service. Going to the newly created resource, first I'd like to register my server, and to register the server, the sync agent needs to be downloaded and installed. So I'm going to need to download the sync agent on the virtual machine because that's mimicking our file server. So I've copied the URL from the portal
>
> **[7:58](https://www.linkedin.com/learning/manage-storage-in-azure/setting-up-azure-file-sync?u=76281980&t=478)** and that opens this Azure File Sync Agent download page. So I'll click download. Now, I want to choose the sync agent for my operating system. Now, this server is Windows Server 2022, so I'm going to tick the one for 2022 and hit download. And that's just going to take a few moments. So I can open the file. Just minimize a few of these things down so the screen's a little cleaner. I'm just going to accept all of the defaults as we go through the installation. But I am just going to quickly set that I want the storage sync agent to be updated by Windows Update every Saturday at one AM. So after a few moments, the installation is completed, and clicking finish, our agent is going to load so that we can configure it. So the agent has checked to see if it needs updating, which it doesn't. And now I'm going to need to sign into Azure to get this sync agent set up. And this is why we turned off IE enhanced security configuration, because without this off, I'm not going to be able to sign in to the Azure Cloud environment. So clicking sign in, I'll use my credentials, pop in my password. So now I've got a little walkthrough. I need to choose the Azure subscription
>
> **[9:32](https://www.linkedin.com/learning/manage-storage-in-azure/setting-up-azure-file-sync?u=76281980&t=572)** that my storage sync service is in. So I've only got one, so I'll choose that. And the resource group, which is rgO501, and that's the storage sync service we created a few moments ago. So I'll select that and click register. And the Azure File Sync Agent in the background was registering this virtual machine with the service in Azure. So now we can see the registration is successful and it's also checked that the network connectivity is set correctly. So I'll close the Azure File Sync Agent configuration. I'm just going to set up the folder that will become the base of the file sync from the server to the cloud. So I'll just pop in to see and add a new folder called filesynctest. Now, if I switch back to the portal, and we'll click refresh on the registered servers page, and we can see our server vm-0501 is now registered. So the next step is to set up a sync group. And this points to the Azure file share. So I'll click add sync group and give it a name. And I need to select the storage account, which is sa0501. And the file share that I want to sync to, which is filesync. And once created, the sync group needs to be paired with the server by adding a server endpoint. So we click on the sync group
>
> **[11:07](https://www.linkedin.com/learning/manage-storage-in-azure/setting-up-azure-file-sync?u=76281980&t=667)** and click to add the server endpoint, choose our registered server, and now we need to add that base path. So if you remember, it was c filesynctest. Scrolling down. You can also choose cloud tiering to create a cache of files on the server, but we want full copies of the files, so we'll leave all of this disabled and click create. The endpoint is now created, and first of all, it enters provisioning mode. I'll just close this so we can see it a little easier. And then once it's added, it changes to pending while the setup is completed between the sync service and the virtual machine. You can hit refresh at the top of the page to check to see if the status has changed. After a while, there's a green tick for health and upload to cloud and download to server. So if we switch back to the virtual machine, and I'll add a folder in our base directory and also add a test file.
>
> **[12:23](https://www.linkedin.com/learning/manage-storage-in-azure/setting-up-azure-file-sync?u=76281980&t=743)** Add some text to it and save it. And now switching back to the portal, we want to have a look at our storage account. Into the file share, filesync. But of course, I don't have access to view the file share from this machine, so I need to go back into the virtual machine and into this listing that we did in the URL and hit Return, and this time I can see my new folder, folder1test. So this is hitting the endpoint within the storage account, not this folder within the virtual machine. So I'm happy that the file sync is functioning correctly. So that's how to set up Azure File Sync and with the storage account and file sync service secured with (indistinct) endpoints. In the next video, we're going to set up a file share mount to a [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) VM, so I'll see you there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (23), [Windows](../../Glossary/Service/Windows.md) (3), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (3), [Windows Server](../../Skills/Network%20and%20System%20Administration/Windows%20Server.md) (2), [Enterprise Architecture](../../Skills/Software%20Development/Enterprise%20Architecture.md) (1)
> **Env Vars:** dns (10), url (4), sas (3), vpn (2)
> **Prerequisites:** set up (11), setup (3), configure (1)
> **UI Navigation:** open the (1), select the (1), click on (1)
> **CLI Commands:** make (1), find (1)
> **Cross-References:** go back to (1), in the next (1)
> **Best Practices:** recommended (2)
> **Code Identifiers:** rgo501 (1)

#### [Mounting a file share to Windows Server](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-windows-server?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-windows-server?u=76281980&t=0)** - [Instructor] Using the Mount capabilities of an [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) file share opens up further ways to access your data from compute. File shares can be mounted to many types of compute in Azure, including VMs, Azure App Service, and AKS or Azure [Kubernetes](../../Skills/DevOps/Kubernetes.md) Service Clusters. A file share can be simultaneously mounted to multiple types of the same compute, and to ensure files are not overwritten at the same time Azure files provides the leases and locks functionality. both the SMB and NFS protocols can be mounted and SMB requires port 445 to be open. For the demo, the following resources have already been created and you can of course create these using the bicep files for this chapter from the [GitHub](../../Skills/Software%20Development/GitHub.md) repo. in the demo, first of all, we're going to look at the file share that has been created and then switch to the window server to mount the share using a script. Switching to the portal. A note here, I am accessing the portal through the [Windows](../../Glossary/Service/Windows.md) VM, which is in the same VNAT as the private link to the storage account. And this is so I can access the file share in the portal otherwise I'll be blocked by the networking configuration. And we can see the resources that have been deployed. And this is the storage account. So working through to the file share at the top there is an option to connect and it is this connection that's going to give us
>
> **[1:32](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-windows-server?u=76281980&t=92)** a script to mount the file share on the server. Note at the top there's a warning stating that secure transfer required is enabled for this storage account. So this means that an SMB client of at least 3.0 is required on the server. Now we can go to the [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) Docs to see the supported clients for each server version, and you can access this document by following the URL being shown on the screen now. Here we can see that [Windows Server](../../Skills/Network%20and%20System%20Administration/Windows%20Server.md) 2022 has SMB version 3.1.1 therefore it meets the requirement of SMB 3.0. Go back to the portal. The next item on the connect is the drive letter. This drive letter just needs to be any free drive letter on your server. I can now choose how to authenticate. The file share does not have identity-based authentication enabled through [Active Directory](../../Skills/Network%20and%20System%20Administration/Active%20Directory.md) Entera ID domain services previously known as Azure ADDS, or [Entra ID](../../Skills/Cloud%20Computing/Microsoft%20Entra%20ID.md) Kerberos. Therefore, I'll stick with the storage account key. But please note identity based authentication is the recommended method. Clicking show script and scrolling down a little bit has displayed a [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) script that can be used to mount the drive. So we'll copy the script using the copy to clipboard, and open PowerShell and paste the script in.
>
> **[3:13](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-windows-server?u=76281980&t=193)** The first command sets connectivity over 445 which is available as the NSG for this virtual machine is on the subnet the private endpoint is injected into. The second command creates the share and persists the storage account key in the credential manager. Now, if I open a file explorer and access this PC, files mount is now mounted on drive Z. I can create a folder within the mount and minimizing these windows and going back to the file share and browsing. The new folder is present. So let's add a file
>
> **[4:10](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-windows-server?u=76281980&t=250)** with some text. Minimizing the window once more and into new folder, new file.txt has been created. Now at this point on the Windows VM we could set up some permissions for the folders and files but only if this account was domain joined. If I now go to the server and search for credential manager and go into Windows Credentials this is where the storage account key is stored and persisted in case of reboot. Now note, this is in the user context. If this mount was for an application we would need to create the credential as the system user, which if I switched to this PowerShell script it can be done with a tool such as PsExec. Now these are just some of the ways that an Azure file share can be mounted but you could also use a custom script extension on an Azure VM or a group policy object in Windows AD or an ADMX template through Intune. So that's how to mount an Azure file share to Windows server. Join me in the next video where we'll look at the same routine, but for a [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) based machine.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (9), [Windows](../../Glossary/Service/Windows.md) (5), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (3), [Windows Server](../../Skills/Network%20and%20System%20Administration/Windows%20Server.md) (2), [Kubernetes](../../Skills/DevOps/Kubernetes.md) (1)
> **Env Vars:** smb (5), aks (1), nfs (1), vnat (1), url (1)
> **Versions:** 3.0 (2), version 3 (1), 1.1 (1)
> **Tools:** powershell (3), github (1)
> **Definitions:** is an  (1), means that (1), known as (1)
> **Cross-References:** go back to (1), in the next (1)
> **UI Navigation:** go to (2)
> **Exercise Files:** github repo (1), template (1)

#### [Mounting a file share to Linux](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-linux?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-linux?u=76281980&t=0)** - [Instructor] There are two protocols that can be used to mount an [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) file share to a [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) system, SMB and NFS. You may have heard or read the phrase SMB for [Windows](../../Glossary/Service/Windows.md), NFS for Linux, but why is that? Well, the first one is quite self-explanatory. NFS is only supported on Linux but Linux also supports SMB, so why choose NFS when possible? NFS is a stateless protocol. It enables faster transfer and has a reduced overhead. It is also a native file system to Linux. Therefore, mounting an NFS share is easier. NFS uses less bandwidth and is more efficient and is designed for scale in largely-distributed environments, where you could have many Linux systems all needing to access the same file share. Now, we're going to walk through mounting an NFS share to Linux. So for the demo, I'm going to stick with the file share being injected into a VNet with a private endpoint. But notice, the file share is now a premium file share, which is a requirement for an NFS mount. This file share will be mounted to the [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) server with the [Windows Server](../../Skills/Network%20and%20System%20Administration/Windows%20Server.md) being used to access the portal and to SSH into the Ubuntu server as the security group on the subnet only allows for RDP. I'm now RDP-ed into the Windows server shown in the demo architecture,
>
> **[1:33](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-linux?u=76281980&t=93)** and all of the architecture has already been deployed. And you can also deploy this infrastructure using the Bicep files for folder 05_03 in the [GitHub](../../Skills/Software%20Development/GitHub.md) repository supplied with this course. The portal is open at the resource group the resources were deployed to. And scrolling down, we can get to the storage account which is now premium storage account of kind file storage. And into the file share, we can see that the protocol is NFS. Now, this is the file share that I'm going to mount. And to do this, I'm going to need to SSH into the Linux VM to run some bash commands. So, I'm going to need to know the private IP address of the Linux server, which is 10.0.0.4. So, opening a [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) window, I'll SSH to the Linux box, which is at this IP address. And admin0503 is the username. And now, I just need to add the password. And my SSH session has been created. So, I now need to know the commands to create the mount. So, if I go back to the portal and back to the file share, I can use these commands to mount the share. Now, we've already stated that my Linux distribution is Ubuntu, but in the dropdown box here,
>
> **[3:07](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-linux?u=76281980&t=187)** I can see commands for other distributions. And going back to Ubuntu, I'm just going to paste these commands in one by one so we can see what they do. So, the first command is going to update the package list so that all the package information is up to date. That's going to take a few seconds to run. So, I'll go back and copy the second command. Paste this in. Now, this is installing the package nfs-common, which is required by the mount command. Just leave that to run. And the mount path is stating where on the Linux machine the file share is going to be mounted to. First of all, we need to create the folder that it's going to be mounted to, which is this third command. The installation's still running, so we'll just leave that for a second. The folder is now created. The final command is going to mount the share. Let's paste this in. Just drag that across a little bit more so we can see the full command. Now, the -t states the type of amount as nfs. The first part of the string before the colon is the NFS server, which in this case is the file service endpoint for the storage account. And on the right of the colon is the share. Then, we have the local mount point
>
> **[4:43](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-linux?u=76281980&t=283)** which is the directory I've just created. And next to the options, the version of NFS being used and the security mode, where sys is the local Unix user and group. Note there's no storage account key for authentication and authorization. NFS depends on system security through the private endpoint to the VNet. So hitting Return, the command runs, and I should be able to cd to the mount point
>
> **[5:21](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-linux?u=76281980&t=321)** and into nfsmount, and I can make a directory, cd into that directory, and create a file. There's the file created in our folder. Now, if I reboot and just leave for a couple of moments for the Linux machine to boot back up again, add the password in once more. If I now cd back to the mount point once more and back into nfsmount, the directory in the files have gone, and this is because the mount has not persisted across the boot. I'd need to mount it once more using the same command as before. Now, this isn't ideal as I'd like the mount to persist across reboots. Therefore, I'm going to need to add it to the fstab system file, which hold information about the file system, mount points, and mount options. Now, fstab is held in etc, so let's cd to etc and use nano to edit the file. Now, the form of the line that I'm going to need to add to the file is very similar to the mount line, and I've got a copy of it in my notes. So, it's quite long. I'm going to copy it across.
>
> **[6:54](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-linux?u=76281980&t=414)** Just move the command and paste it in. So once more, we have the endpoint, the share, and the local mount point with the options. I'll save the file, and I've just realized I needed to run nano as sudo, so I'll just need to do this once more. Otherwise, I'm not going to be able to save the file. The file's now saved. So, let's shut down the server once more.
>
> **[7:32](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-linux?u=76281980&t=452)** Just wait for it to reboot. It's not quite ready yet. Let's just try once more. Pop the password back in. Right, got the password right this time, and cd back to the mount point.
>
> **[7:57](https://www.linkedin.com/learning/manage-storage-in-azure/mounting-a-file-share-to-linux?u=76281980&t=477)** And this time, the directory is there and so is the file, so the changes in fstab have been picked up and the share has been mounted on reboot. So, that's the end of this chapter on features that are specific to Azure files. Join me in the next chapter where we'll look at how to protect data inside your storage accounts.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (13), [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) (4), [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (2), [Windows Server](../../Skills/Network%20and%20System%20Administration/Windows%20Server.md) (2), [Windows](../../Glossary/Service/Windows.md) (1)
> **Env Vars:** nfs (13), ssh (4), smb (3), rdp (2)
> **CLI Commands:** cd (5), ssh (4), make (1), sudo (1)
> **Tools:** github (1), bash (1), powershell (1)
> **Cross-References:** go back to (1), in the next (1)
> **Definitions:** is a  (2)
> **Versions:** 10.0.0 (1)
> **UI Navigation:** dropdown (1)


### 6. Protecting Azure Storage Data

[↑ Back to Table of Contents](#table-of-contents)

#### [Encryption at rest and in transit](https://www.linkedin.com/learning/manage-storage-in-azure/encryption-at-rest-and-in-transit?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/encryption-at-rest-and-in-transit?u=76281980&t=0)** - [Instructor] Securing data with encryption protects the data from being viewed or altered by those who should not have access to the data. For [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage, we are primarily concerned with encryption at [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md), which is part of the defense to stop an attacker that may obtain a hard drive but not the keys to decrypt the data on it and therefore must break the defense of the encryption to read it. And also encryption in transit where data is intercepted when moving between devices and networks that has already been encrypted before transmission. For encryption at rest, there are a couple of options. For a client, which is writing to a storage account, by default, all data written to any endpoint in Azure Storage is encrypted with Storage Service Encryption, or [SSE](../../Glossary/Standard/Server-Sent%20Events.md) for short where a [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md)-managed key is used to encrypt the data. This all happens without cost and without code changes. Microsoft manages the whole process for you. In some scenarios around compliance, a Microsoft-managed key may not meet regulatory requirements. Therefore, a customer-managed key which is stored in a key vault, can be used as a key encryption key to wrap the Microsoft-managed key. Therefore, no data can be read back from this account without the customer-managed key. A further option for blob data only is to send the encryption key on the request to the blob endpoint. The key can come from the key vault
>
> **[1:35](https://www.linkedin.com/learning/manage-storage-in-azure/encryption-at-rest-and-in-transit?u=76281980&t=95)** or another secret service and each key can be different for each blob. The blob service then encrypts each blob with the encryption key sent in the request. This is known as encryption on request. There is also an option on creation of the storage account for infrastructure encryption. Infrastructure encryption creates a further encryption layer by encrypting the data at the infrastructure layer which was already encrypted with the service encryption. Therefore, if one of the encryption keys is compromised, the other still protects the data. For encryption in transit, it is recommended that HTTPS is used on the endpoint which will initiate a TLS or transfer layer security handshake. This handshake process enables both the client and server to possess a session key. The client uses the session key to encrypt the data which is transported encrypted to the server, which in this case, is the storage service. If a bad actor is somehow able to intercept your requests, they cannot read the data as it is encrypted with the session key. The storage service then uses the session key to decrypt the data, which is then stored in the account. The same process can be repeated if data needs to be read from the storage account.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (4), [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [Sse](../../Glossary/Standard/Server-Sent%20Events.md) (1)
> **Env Vars:** sse (1), https (1), tls (1)
> **Definitions:** known as (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [instructor] (1)

#### [Encryption at rest, encryption scopes, and encryption in transit in practice](https://www.linkedin.com/learning/manage-storage-in-azure/encryption-at-rest-encryption-scopes-and-encryption-in-transit-in-practice?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/encryption-at-rest-encryption-scopes-and-encryption-in-transit-in-practice?u=76281980&t=0)** - [Instructor] We are now in the portal at the Resource Group for this walkthrough on encryption in [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage. In this key vault are some keys, one of which we would like to use for customer-managed encryption. And this storage account has a system-assigned identity. If we go into the key vault and look at the access control for it, we can see that the identity has been assigned: Key Vault Crypto User. Now, this is because Key Vault Crypto User has the data actions that are needed for the encryption process, which is Wrap Key, Unwrap Key, and Get. Now, to enable my logged in user to select the key and set up the customer-managed encryption, I'm going to need to assign a role. And this role is going to need List Key. If I search for key here, the Key Vault Crypto User Has List Key. So that's using the principle of least privilege. So I'll select my username, which is azure instructor 14, and add it as a member and assign the new role assignment. So now I can return to the storage account, and we can set up customer-managed encryption. So Azure Storage wraps the account encryption key with a customer-managed key taken from Key vault.
>
> **[1:37](https://www.linkedin.com/learning/manage-storage-in-azure/encryption-at-rest-encryption-scopes-and-encryption-in-transit-in-practice?u=76281980&t=97)** So entering Encryption, choose Customer-managed keys, and we're going to select the key from the key vault. Selecting Key vault. This is the key vault that we've just set up the permissions on. And I'm going to use the Key storage encryption key which has already been created inside the key vault. Clicking Select and opening the Advanced section, it is possible to use a key that is not controlled by this tenant. This is for scenarios where customers want to control their keys but you need to keep the data. Clicking Save, and Azure will apply the key. It doesn't replace the [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md)-managed key. It uses the new key to encrypt the Microsoft-managed key. This is called a key encryption key. The customer-managed key is now enabled, and any new data is encrypted automatically with old data retrospectively encrypted in the background. Scrolling up to the top, you can see Infrastructure encryption. And this cannot be enabled as this can only be set on storage account creation. At the top, we also have a tab for Encryption scopes. If you have a scenario where you are storing multiple customers' data in one one storage account, then Encryption scopes is likely to be a useful feature. Encryption scopes enable the use of multiple Microsoft-managed or customer-managed key to encrypt data.
>
> **[3:12](https://www.linkedin.com/learning/manage-storage-in-azure/encryption-at-rest-encryption-scopes-and-encryption-in-transit-in-practice?u=76281980&t=192)** Here I've clicked Add, and we're going to use a Microsoft-managed key and call the scope "customer1." Note, we can also choose to enable infrastructure encryption at this level. So I'm going to choose Customer-managed keys from my key vault, and use customer1 key and click Create. Navigating to the Containers blade, I'm going to choose to create a new container and call it customer1. And opening the Advanced options, we have the option to use the newly created encryption scope. And clicking Create or the data created in the customer1 container will be encrypted with the key from the encryption scope. And the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the data is going to be encrypted using the customer-managed key that we set up first. This makes Encryption scopes a really powerful tool for securing data for multiple customers in one storage account. And it can even be used at the Blob level. If we switch now to the Configuration pane, we have the options for Encryption in transit. Here is the option for Secure transfer required. This controls where the HTTPS is required when accessing the storage account. Note, although HTTPS is recommended, there are configurations for access from [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md)-based clients and Azure files that require just HTTP access.
>
> **[4:48](https://www.linkedin.com/learning/manage-storage-in-azure/encryption-at-rest-encryption-scopes-and-encryption-in-transit-in-practice?u=76281980&t=288)** Scrolling further down, you can choose the minimum TLS version for requests. Here there is a default of 1.0 or greater, and this would've been chosen for backwards compatibility. We've come to the end of our storage data encryption demo. Join me in the rest of the chapter where we're going to look at protecting your data, where we're going to look at protecting your data from deletion and corruption.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (5), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (4), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1)
> **Env Vars:** https (2), http (1), tls (1)
> **Prerequisites:** set up (4)
> **UI Navigation:** select the (2)
> **Definitions:** is called (1), is a  (1)
> **Versions:** 1.0 (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [instructor] (1)

#### [Soft delete and versioning for blobs, containers, and file shares](https://www.linkedin.com/learning/manage-storage-in-azure/soft-delete-and-versioning-for-blobs-containers-and-file-shares?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/soft-delete-and-versioning-for-blobs-containers-and-file-shares?u=76281980&t=0)** - [Presenter] Deletion of data with an accidental or malicious is a key concern of any organization that stores data. And therefore, it's important to understand how to protect data from deletion. This is where the features soft delete for blobs and files and versioning for blobs should be employed as recommended by [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) as part of a strategy to protect data from deletion. For example, here we are back in the portal and I've just realized that the storage account from my walkthrough has been deleted. Now, if the storage account has been deleted in the last 14 days and no other storage account has been created that has the same name, then it may be possible to restore the storage account through Restore at the top of the storage account list. But note this message at the top of the restore pane. Microsoft does not guarantee that the storage account will restore even if it's displayed in this list of restorable accounts at the bottom of the page. And my account is called SA0602 and it's the last one that I want to restore. The recovery process is running and luckily my storage account had only just been deleted and has restored. So refreshing the page, my recovered storage account is listed. Now I don't want that to happen again. So if we go into the storage account and scroll down to locks, I can stop the storage account from being deleted by adding a delete lock.
>
> **[1:36](https://www.linkedin.com/learning/manage-storage-in-azure/soft-delete-and-versioning-for-blobs-containers-and-file-shares?u=76281980&t=96)** Let's give it a name of no delete. And now this lock has been added. A storage account cannot be deleted, whether that's through the portal, the command line, through an SDK or using [infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md). Now scrolling up into the [Data Management](../../Skills/Software%20Development/Data%20Management.md) menu and into data protection, this storage account has no protection enabled. So if I go into a container and delete a blob, the blob is now gone. And this switch here, show deleted blobs does not list the one that's deleted. For this feature to work, we need to enable soft delete for blobs. So if I go back to the data protection menu and tick soft delete for blobs, I can now set the number of days that a deleted blob can be recovered up to. And this is up to the maximum of 365 days. So clicking save and returning to the containers pane, back into the same container and delete the second blob. If I now choose, Show Deleted Blobs, the deleted blob is listed. I can restore this blob by using the ellipses and choosing to undelete it and that's the blob restored. But what happens if I overwrite the blob?
>
> **[3:08](https://www.linkedin.com/learning/manage-storage-in-azure/soft-delete-and-versioning-for-blobs-containers-and-file-shares?u=76281980&t=188)** So I go into its properties and choose to edit it add some new text and save. There's no deleted blob being shown. But if I go into the properties and snapshots, [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) has created a soft deleted snapshot of the blob and it did that in the background at the point in time before the edit was saved. Now, soft delete for blobs only covers the blobs themselves. If I navigate back to the container and choose to delete the container and override the delete lock, the container has been deleted. And if I choose to show deleted containers, the container isn't listed because it wasn't protected. To protect the container from deletion, we go back to the data protection pane and this time we need to enable soft delete for containers which once again, has a default of recovery up to seven days. So saving this change and returning back to the container's listing, I'll delete the final container in here, override the delete lot once more and choose to show deleted containers. And the container is listed and it's the same process to undelete using the right hand side of the container name.
>
> **[4:44](https://www.linkedin.com/learning/manage-storage-in-azure/soft-delete-and-versioning-for-blobs-containers-and-file-shares?u=76281980&t=284)** And there the blob has been successfully undeleted. For soft delete for blobs, there is one further option to mention back in data protection, which is to allow permanent deletion of soft deleted snapshots or versions before the retention period ends. If this is not enabled, snapshots and versions can only be deleted automatically at the end of the retention period. Soft delete for file shares works similarly to that of blob containers and the option can be enabled for all shares on the account and a full share can be restored up to its retention period which has a maximum of 365 days. Microsoft also recommends that on protecting blobs, enabled blob versioning should also be enabled which is found in the data protection pane under the tracking items. Checking enable versioning for blobs, you have the option to keep all versions or to automatically delete versions after a configurable number of days. Saving to keep all versions will enable any version on the blob on this storage account to be accessible and therefore you can restore to a previous version of a blob. However, you do need to think about the cost implications of this as every version you store will contribute to your storage costs. And this of course also includes every soft deleted blob, container, snapshot and file share. We will explore how to limit these costs
>
> **[6:21](https://www.linkedin.com/learning/manage-storage-in-azure/soft-delete-and-versioning-for-blobs-containers-and-file-shares?u=76281980&t=381)** in the next chapter. I've clicked Save on blob version enable. So let's have a look how it works. Navigating back to the containers and into soft version we led it this first blob, add in a small text change and save the change. Clicking on versions, we have a version of this blob with a version ID of the date when the version was created. It is earlier than the current date. By select this blob version and download it, it is the original version and the version ID is how Azure uniquely identifies each version. If we go back to the portal, you can restore any version of the blob by selecting the version and choosing to make current version. Note, the list of versions has increased as the old current version is now the previous version. So going back to overview and editing the blob, the text has reverted back to its original state and if I chose to overwrite the blob, another new version would be created rather than a deleted snapshot. Even though soft delete for blobs is still enabled. And if I go back into data protection and disable soft delete for blobs and click save and go back into the container and show the blob properties once more,
>
> **[8:00](https://www.linkedin.com/learning/manage-storage-in-azure/soft-delete-and-versioning-for-blobs-containers-and-file-shares?u=76281980&t=480)** the versions are still present. So if you need to remove the versions, you have to delete them. That's the end of our look through the recommendations for data protection. To enable container and file share soft delete and blob soft delete and blob versioning which together enable restoration of data that has been deleted or modified. Join me in the next video to look at further options for blob data protection.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (3), [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (2), [Infrastructure as code](../../Skills/Software%20Development/Infrastructure%20as%20code%20(IaC).md) (1), [Data Management](../../Skills/Software%20Development/Data%20Management.md) (1)
> **Cross-References:** go back to (3), in the next (2), in the last (1)
> **Env Vars:** sa0602 (1), sdk (1)
> **Definitions:** is a  (1), is called (1)
> **CLI Commands:** make (1)
> **Tools:** command line (1)
> **UI Navigation:** scroll down (1)
> **Analogies:** for example (1)

#### [Change feed for blob data](https://www.linkedin.com/learning/manage-storage-in-azure/change-feed-for-blob-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/change-feed-for-blob-data?u=76281980&t=0)** - [Instructor] Through our exploration of protecting data in a storage account, we've covered accidental deletion and reverting to previous versions of blobs, but not how to log changes against data. Change feed for blobs creates a transaction log of all changes with each change making a single entry in the log. Each of these changes is in order of change and these are guaranteed to be written in that order. The log is stored as a blob in a storage account and therefore inherits the account's durability, and the log is stored as an immutable append blob and is therefore read only. It cannot be deleted or modified for a given duration. The duration that a log is kept for is set up when the data protection is enabled at the account level. In this storage account, the change feed has been enabled. Scrolling down in the Data protection plane, we can see that logs will be automatically deleted after seven days for this change feed configuration. Now, you can choose to keep all logs, but this is going to be much more costly as each log will contribute to the storage cost of the account. When change feed was enabled, a container was created, $blobchangefeed, and the logs are stored within this container. But before we look at the logs, we're going to need to create some changes to the blocks blobs in this account. So I'll go back to Containers and there's a third container, containerfeed,
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/change-feed-for-blob-data?u=76281980&t=94)** with two blobs. I'm going to edit blob1.txt and create two separate text changes, which should be put into the log as two separate entries. I've saved both of those. And I'm also going to add a [Metadata](../../Skills/Web%20Development/Metadata.md) change to blob1.[JSON](../../Skills/Web%20Development/JSON.md) because blob property changes are also tracked. So here's the metadata and we're going to add a key-value pair, feedCheck, and just a value of 1234. So saving that change and going back into the blob change feed container. Logs are stored in the log virtual directory and split into data buckets for each day. So go through, this is the date of the current day and this is the time that the changes have been added. Now, it can take a couple of minutes for the change feed to be updated. So if you need a feature to trigger off changes in real time, then you should look at [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage events. The thing with Azure Storage events is they're not guaranteed to be ordered. The log of changes is added in Apache Avro format. Apache Avro as a binary format, and note that this blob type is in append blob. So we cannot view these in the portal. To view this file, we'll need to download it. So I'm going to use the ellipses to the right and click Download. Now, I've installed an extension within Visual Studio Code which will allow me to view this file.
>
> **[3:09](https://www.linkedin.com/learning/manage-storage-in-azure/change-feed-for-blob-data?u=76281980&t=189)** So I need to copy the file into VS Code. There we go. Now, I can't read the file in this format as it is, but I can right-click on the file and choose to preview the data on the side, which is going to load up the Avro extension. Closing the two other [Windows](../../Glossary/Service/Windows.md), we can now look at the format for the file. Scrolling across to the right a little, we can see the first two entries where the two blobs were created. Then the two edits, which are listed as utilizing the Put Blob API, which is the request on the blob service for create or modification of a block blob, whereas the blob properties utilize a different API, which generates a blob property's updated event. The Apache Avro format is compact and fast, which is important as a blob change feed is likely to produce a high volume of change events, which is why Apache Avro is designed to be consumed by [Big Data](../../Skills/Data%20Science/Big%20Data.md) processing tools such as [Apache Spark](../../Skills/Data%20Science/Apache%20Spark.md). This is also why the virtual directory is split into buckets, to enable asynchronous processing by such tools, and multiple processing applications can read the changes in parallel. The features of blob change feed bring multiple use cases from audit and compliance to creating metrics and analytics on how your blob data is used. Change feed can also be used for backup, DR, and archiving and to synchronize a cache or index for searching data,
>
> **[4:47](https://www.linkedin.com/learning/manage-storage-in-azure/change-feed-for-blob-data?u=76281980&t=287)** and, finally, as a trigger on change events, which can also be filtered.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Metadata](../../Skills/Web%20Development/Metadata.md) (2), [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (1), [Windows](../../Glossary/Service/Windows.md) (1), [Big Data](../../Skills/Data%20Science/Big%20Data.md) (1)
> **CLI Commands:** apache (5)
> **File Paths:** blob1.txt (1), blob1.json (1)
> **Env Vars:** api (2)
> **Tools:** visual studio (1), vs code (1)
> **Code Identifiers:** feedcheck (1)
> **Cross-References:** go back to (1)
> **UI Navigation:** right-click (1)

#### [Point-in-time restore for blob data](https://www.linkedin.com/learning/manage-storage-in-azure/point-in-time-restore-for-blob-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/point-in-time-restore-for-blob-data?u=76281980&t=0)** - [Instructor] Change feed also enables another data protection feature for block blobs in [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md). This is point in time restore for containers. In the second storage account, which must be a storage v2 general purpose account. Otherwise, I cannot enable point in time restore through the data protection options. Now the setup of the storage account, when I ran the Bicep files, has already enabled it for me and point in time restore for containers has three requirements. That's soft delete for blobs, versioning for blobs, and enable blog change feed. And this is why they're grayed out, because they are required for point in time restore. If I scroll up in the menu and go to containers, I've already created some edits and some deletes in this storage account. In container del one, I have deleted blob2.txt. In container edit one, I have edited both of these blobs. And in container restore I have deleted a blob and edited blob1.[JSON](../../Skills/Web%20Development/JSON.md). Going back to the container listing, we'll choose to restore container edit one and container restore by selecting both of the containers and going up to the dropdown for restore containers. I can choose to restore selected. This gives me the option to restore to a point in time back to when either point in time restore was switched on
>
> **[1:35](https://www.linkedin.com/learning/manage-storage-in-azure/point-in-time-restore-for-blob-data?u=76281980&t=95)** or to the retention point if that has been hit. Now I noted the time it was before I made the changes that we've just been through so I'm going to change this time to that point. And next we have the option to filter the restore to specific blobs in a container using the container name and a full blob name, including the virtual directory. Now, I want to restore for all blobs in these containers. So I'm not going to filter and I'm going to choose yes to proceed and click restore. During the time of restore, the storage account cannot access the blob ranges being restored until the process is complete. Now the time taken for the completion is dependent upon the number of changes that need to be restored. So if your restoration time is beyond 30 days this could be in the hours. Out of the container listing and choose to refresh the portal to make sure it picks up the changes. If I come out of the container listing and back in, there is an error. The point in time restore process is still running. So I cannot access this container. After a short time, the process is complete and entering one of the containers that we chose to restore, we can see that the data is restored as expected. This walkthrough brings us to the end of protecting data against deletion or logical corruption.
>
> **[3:10](https://www.linkedin.com/learning/manage-storage-in-azure/point-in-time-restore-for-blob-data?u=76281980&t=190)** Join me in the next video where we'll look at data protection options designed for regulatory requirements.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **UI Navigation:** scroll up (1), in the menu (1), go to (1), dropdown (1)
> **File Paths:** blob2.txt (1), blob1.json (1)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **Definitions:** is an  (1)
> **Prerequisites:** setup (1)
> **Speakers:** - [instructor] (1)

#### [Immutable storage for business-critical blob data](https://www.linkedin.com/learning/manage-storage-in-azure/immutable-storage-for-business-critical-blob-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/immutable-storage-for-business-critical-blob-data?u=76281980&t=0)** - [Presenter] Business critical data can be data that is essential for the success of a business, where the data is relied on for decision making or data that is governed by data protection laws or regulatory requirements or a combination of the both. Examples of such data are financial, healthcare and personally identifiable data which are all covered by various types of regulations some of which are often regional. Examples of data critical to a business but not necessarily covered by regulation could be operational data used for analytics, audit trails, backups, or archive data or data required for legal proceedings. In each of these cases, it is likely that the data needs to be kept in an unaltered state and for an identified amount of time. This data is written to once and can be read many times, but not altered. This is also called WORM state, Blob storage in [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) can utilize the immutable storage for blobs feature to place blob data into a WORM state. There are two types of immutable storage policies, timed-based retention and legal hold. With both types of immutable blobs, a blob can be created and read, but not modified or deleted. Each type does have subtle differences. Time-based retention requires a specified interval for the blobs or containers to be in a WORM state. This interval is either unlocked for testing
>
> **[1:36](https://www.linkedin.com/learning/manage-storage-in-azure/immutable-storage-for-business-critical-blob-data?u=76281980&t=96)** or locked where it cannot be decreased or removed. This lock enables [Regulatory Compliance](../../Skills/Artificial%20Intelligence%20(AI)/Regulatory%20Compliance.md), For more information on which regulations, see this link to the Azure documentation. Once the time interval is finished, the data can be deleted but not overwritten. For legal hold, the WORM state is held for the length of the legal proceedings with tags on the data being used to identify the blobs that are part of the legal hold. Once a legal hold is removed, the data can be deleted or modified. One of the key differences between the two types is locked time-based retention policies cannot be cleared before the scheduled interval even by administrators. The features of the two types of immutable storage make them suitable for specific protection scenarios. Time-based retention, especially in the lock state, is designed to be used for classified compliance and regulatory data. For example, personally identifiable information and legal hold is for investigations and archival where the time for attention is not known upfront. I've switched into the portal now so that we can explore how the two types are set up. This storage account has blob versioning enabled, and it also has a couple of containers of blob data that we are going to place into a WORM state and with both types of immutable storage for blobs, the policies can be set at the container level
>
> **[3:11](https://www.linkedin.com/learning/manage-storage-in-azure/immutable-storage-for-business-critical-blob-data?u=76281980&t=191)** and the blob version level dependent on the configuration of the data protection in the storage account. We'll set up a time-based retention policy on this container by clicking the ellipses next to it and choosing access policy. We add a policy under a mutable blob storage and choose a policy type of time-based retention. As this is a demo, I'm only going to set the retention period to one day but this can actually be up to 400 years. We need to leave version level immutability unchecked as this is what sets a container level scope for the policy. If protected appended rights are enabled, a pen blobs can be added to and block blobs that are written to using an application that can use put blob calls can add blocks to the blob but it cannot overwrite blocks that are already present. So I'll leave both of these options unchecked and click Save. The immutable storage policy is listed and the scope is listed as container with a state of unlocked. Returning to the container and entering it and trying to delete one of the blobs. I cannot, and the error listed states that the blob cannot be deleted due to the immutability policy. Going back up a level to the container and returning to the policy as it is unlocked, we can edit it and we can extend the retention period
>
> **[4:51](https://www.linkedin.com/learning/manage-storage-in-azure/immutable-storage-for-business-critical-blob-data?u=76281980&t=291)** and we can edit it once more and we can decrease the retention period. I can also choose to delete it because it is unlocked but if we lock the policy, type yes to confirm and back to the ellipses, we can no longer delete the policy. Even though my login has administrative privileges, going back into edit the policy, I can extend the policy and I can do this up to five times at the container scope but if I try to edit once more and decrease the policy, I cannot. Caning this change and going back to the resource group, version level immutability can either be enabled at the account level at the point of creation which if I go into this second storage account and enter the data protection pane, we can see this when scrolling down and here it is enable version level immutability support or we can enable it on a new or existing container which doesn't require this checkbox to be enabled. We could add a policy at the account level which can be overridden at the container level. And note this is for time-based retention only. If version level immutability is enabled at the container level,
>
> **[6:23](https://www.linkedin.com/learning/manage-storage-in-azure/immutable-storage-for-business-critical-blob-data?u=76281980&t=383)** legal hold can only be set at the blob version level, and if I return to the container listing and go to manage its policies and add a policy at the container scope, the only policy available to add is time-based due to the version level immutability switch at the account level. Going back to the first storage account that we used. We're now going to set up a legal hold on this container. The processes the same. Use the ellipses to enter the access policy and add an immutable blob storage policy. And this time the policy type is legal hold. For this to apply to blobs, the blobs in this container will need at least one specific tag to be present. We add case ID so all blobs with the case ID tag will be subject to the legal hold and I'll click Save to apply this tag. So let's add a case ID tag to one of the blobs in the container. Let's go to blob three and scroll down. The case ID tag is already present so now if I try and delete the blob, I cannot, the delete failed due to the immutability policy. And also if I try and edit the blob, the save never updates, again because of the immutability policy.
>
> **[7:58](https://www.linkedin.com/learning/manage-storage-in-azure/immutable-storage-for-business-critical-blob-data?u=76281980&t=478)** For a turn back to the container level and into the access policies, scroll across, I can delete the tag and click to save the policy. This will clear the legal hold so the immutability policy is no longer listed. And returning to the blobs once more, selecting blob three and clicking Delete, the blob can be deleted as the immutability policy has been cleared. We've now reached the conclusion of this chapter on data protection, exploring each of these options for recovery of deleted, overwritten or modified blobs. We've also looked at how to track changes to blobs and restore to different versions, and how to apply immutability policies for legal or regulatory data. Join me in the next chapter where we're going to look at how to manage costs for storage data and how to monitor Azure storage.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (3), [Regulatory Compliance](../../Skills/Artificial%20Intelligence%20(AI)/Regulatory%20Compliance.md) (1)
> **Env Vars:** worm (5)
> **UI Navigation:** go to (2), checkbox (1), scroll down (1)
> **Prerequisites:** set up (3)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)


### 7. Managing Cost and Monitoring Azure Storage

[↑ Back to Table of Contents](#table-of-contents)

#### [Billing, reserved capacity, and network routing preference for Azure Storage](https://www.linkedin.com/learning/manage-storage-in-azure/billing-reserved-capacity-and-network-routing-preference-for-azure-storage?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/billing-reserved-capacity-and-network-routing-preference-for-azure-storage?u=76281980&t=0)** - [Instructor] When managing a storage account for cost, it is important to understand how the services are billed, so that costs can be minimised. Billing on a storage account is a combination of multiple factors. The region the account is created in, whether the account is premium or shared, and the level of redundancy chosen, which determines how many copies there are of the base data. Where the base data is how much data in total is being stored in the storage account. There are costs associated with every read and write operation to the data in the storage account along with a charge for transferring data to another region, which is part of the reason why it is recommended that applications that access data are stored in the same region as the data. These three factors together often make up the core of the cost on a storage account. There are also actions on a storage account that can incur extra cost, and we can see lists of these given in the [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage documentation for avoid billing surprises, which you can access by following this link. For example, enabling soft delete for blob storage and versioning will create extra data that needs to be considered as part of the storage account cost. Or value adds for Azure Files, which can be viewed at this link, explains that including Azure File sync
>
> **[1:33](https://www.linkedin.com/learning/manage-storage-in-azure/billing-reserved-capacity-and-network-routing-preference-for-azure-storage?u=76281980&t=93)** and Azure Backup will both increase the cost of using Azure Files through transaction costs, and additional storage utilization through differential snapshots. These are both being costs that may not be expected when enabling Azure File sync or Azure Backup. The pricing for all storage services can be seen on their pricing pages. For example, this is the pricing for Azure Files, which can be viewed at this link. Scrolling down we can see that premium [Data Storage](../../Skills/Data%20Science/Data%20Storage.md) on SSDs is always the highest, but the costs increase as redundancy increases from ZRS through to GRS, and scrolling down further to the list of transaction prices the cost of transactions is per 10,000 with prices increasing once more depending on the level of redundancy chosen. Moving down slightly to the end of this table there is a small note on outbound data charges. Clicking through on this we can see that data transfer is charged when moving between availability zones, for inter-regional transfer and for internet egress, whether out of the [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) backbone or through an ISP, with the ISP [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) being slightly cheaper. We can plan for upfront storage account costs, taking all billing factors into consideration by using the pricing calculator,
>
> **[3:06](https://www.linkedin.com/learning/manage-storage-in-azure/billing-reserved-capacity-and-network-routing-preference-for-azure-storage?u=76281980&t=186)** which you can access through this link. So, let's price up a monthly cost for our Azure Files service. Choose Azure Files. So, for 1,000 gigabytes of transaction-optimized storage with local redundancy at the East US region, we'll estimate that we have 10 times as many write operations as lists and read transactions. And scrolling down a little further, we'll have one file sync server and that will total the billing for us to around $125 per month and this is the estimated cost. You may have noticed when we were viewing the Azure Files pricing that I skipped reservations which was on this pricing page just above transactions. Reservations are described as helping to lower storage costs by committing to a storage spend in increments of 10 terabytes or 100 terabytes over one or three years. Note also that this is against the cost to store the data only. It does not cover transactions or egress. Switching to the portal, we'll start to purchase a reservation by searching for it and choosing reservations under services, and clicking add. We can see it is possible to create a reservation for blob storage and files
>
> **[4:43](https://www.linkedin.com/learning/manage-storage-in-azure/billing-reserved-capacity-and-network-routing-preference-for-azure-storage?u=76281980&t=283)** along with many other services. But unfortunately, I am unable to create a reservation for this subscription type as I'm not the owner of the subscription, which is a prerequisite. So, here is a screenshot of the reservation blade. Here we can see that a reservation is purchased based on the 10 terabyte or 100 terabyte sizes, with a greater discount being given for purchasing the 100TB sizes. Now, this is a screenshot of a files reservation. If I had chosen a blob reservation, this would be a 100TB and 1PB sizing. With a reservation, you are guaranteeing a cost level over the term. Here we can only see 3 years, but there is also a higher cost option of 1 year, which can be billed up front or in monthly buckets. Once purchased, the reservation will apply across all Azure Files storage at the chosen tier, redundancy, and region up to the reservation size. We'll cover more on tiers in the next two videos. Therefore, if I choose 10TB of hot GRS storage at West US 2, then all file shares that match these choices can have the reduced reservation cost applied up to the 10TB limit, after which standard cost is applied. A reservation is use it or lose it, so if the total does not meet the 10TB size, then you lose the reservation, it is not rolled over.
>
> **[6:18](https://www.linkedin.com/learning/manage-storage-in-azure/billing-reserved-capacity-and-network-routing-preference-for-azure-storage?u=76281980&t=378)** The scope of application must also be set when purchasing the reservation. Where the subscription scope can cover a single subscription, be shared across multiple subscriptions, or more granular at the resource group, or management group level. A reservation isn't locked in for the full term. There is the possibility of switching to a different storage reservation using exchange. For example, if you find the tier you have chosen is not the most efficient to have a reservation against for your application, you can exchange it to a reservation at a new tier. You are refunded the unused portion of your reservation, and this is applied to the new reservation. It is also possible to cancel a reservation, and refunds can be applied for up to $50,000 a year. If you recall the walkthrough of Azure Storage pricing, there is a cost charge for all data leaving the storage account. This page of the Microsoft documentation, which can be accessed by the following link, shows that by default a storage account is created with routing via the Microsoft Global Network, where the client accesses data over the ISP to the local point of presence on the Microsoft Backbone closest to the client. The request travels over the backbone to the region where the data is stored, and the data is retrieved and travels back through the same path. If the point of presence and the location of the storage is in a different region,
>
> **[7:52](https://www.linkedin.com/learning/manage-storage-in-azure/billing-reserved-capacity-and-network-routing-preference-for-azure-storage?u=76281980&t=472)** there is an extra cost associated to transferring the data across the backbone. This can be reduced by using public internet routing for a storage account. For public internet routing, the request is routed over the public internet to the point of presence nearest the data, and the data is transferred back over the same route. This route may be more cost-effective, but there is a trade-off against the high speed, high reliability, and availability of the Microsoft backbone. In this video, we covered how Azure Storage is built, and looked at cost optimization for egress, and also how to use reservations for storage. In the next two videos, we will look at options to help further with storage costs and transaction costs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (14), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (5), [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) (4), [Data Storage](../../Skills/Data%20Science/Data%20Storage.md) (1)
> **Definitions:** is a  (7), is an  (1)
> **Env Vars:** isp (3), grs (2), zrs (1)
> **Cross-References:** in the next (2), we covered (1)
> **Analogies:** for example (3)
> **CLI Commands:** make (1), find (1)
> **Best Practices:** recommended (1)
> **Prerequisites:** prerequisite (1)

#### [Storage tiers and blob rehydration](https://www.linkedin.com/learning/manage-storage-in-azure/storage-tiers-and-blob-rehydration?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/storage-tiers-and-blob-rehydration?u=76281980&t=0)** - [Instructor] The two main components of cost for storing data in a storage account are the cost for storing the data itself and the cost for accessing the data. When we think about data and its frequency of access, data with high transaction rates will be written and read live in applications, whether that's a Web app or perhaps an [IoT](../../Glossary/Concept/IoT.md) or media data stream. There are also types of data that are written to and read from infrequently but are always required to be available for immediate access, such as historical medical records or short-term backups, and then there is data that might be written once and not accessed for a long time, if at all. This data is often kept for regulatory and compliance reasons and for long-term backups. [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) storage for blobs and files sets its cost around how often data is accessed, along with the cost of storing the data itself. For blobs, this is managed through choosing a blob access tier. There are four access tiers for Azure blobs in a Standard V2 storage account which map to the frequency of data access on the previous slide. Hot is for frequently accessed data, cool and cold for infrequently accessed data, and archive for data that is rarely accessed. The hot, cool and cold access tiers are all online. The data within them is all immediately accessible, whereas the archive tier is for data
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/storage-tiers-and-blob-rehydration?u=76281980&t=94)** that can be held offline and can withstand a delay for the data to become available. The cost to transact with the data held in each tier increases from left to right, but the cost to store the data decreases from left to right. Note, these costs are all at the blob level. A default tier can be set for the service itself, but each blob can have a different tier regardless of the container it is stored in. A note of caution before carrying on further. The cold tier is currently in preview, which means it may change or be deleted, but as a highly requested feature, I strongly believe it will go GA. Each of the cooler tiers has a minimum amount of time a blob can be stored for costing purposes. For example, if we moved a blob from the hot tier to the cool tier, the blob will be charged for at least 30 days, even if we move the blob back to the cool tier before the minimum. This is the same if the blob is deleted from the tier. You will always be charged the minimum. The process of moving a blob from archive to one of the other tiers is called rehydration. It can take up to 15 hours for a blob to rehydrate, so it is imperative to build this into your application or process planning. It is possible to rehydrate a blob quicker, but this is at the highest transaction cost. For file shares, there is a service tier which is set either at the share level or the service level. File shares have premium, transaction optimized,
>
> **[3:10](https://www.linkedin.com/learning/manage-storage-in-azure/storage-tiers-and-blob-rehydration?u=76281980&t=190)** hot, and cool storage tiers, where the choice of tiers available is governed by the performance chosen for the account. Premium is only available for a premium file share, and the others are all configurable from a standard file share. Azure files has no concept of an archive tier. All storage tiers are online, and data is immediately accessible. Premium storage is for scenarios that require single-millisecond latency, such as [Databases](../../Skills/Software%20Development/Databases.md) and FSLogix profiles for Azure Virtual Desktop. Transaction optimized is for scenarios where there are still a very high number of transactions but not the latency requirements of a premium share. Hot storage is for team file shares and shares being utilized with Azure File Sync, and cool is for data that could be archived. The cost of transactions increase as you move through the tiers from left to right with the cost for storage dropping, but with premium file shares being a provisioned upfront cost for total amount of data, which includes transactions, there may be a point when utilizing premium shares over transaction optimized may be more cost effective. Let's jump into the portal and look at how to manage tiers for blobs and files. This is a Standard V2 storage account with its default access tier for blobs set to hot. So if we go into the container
>
> **[4:44](https://www.linkedin.com/learning/manage-storage-in-azure/storage-tiers-and-blob-rehydration?u=76281980&t=284)** that's been set up for this walkthrough and choose to upload a blob from my laptop, I choose the blob and click Upload. If I close the upload blob window, we can see that its access tier has been set to hot, and in brackets is the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) inferred, which means it inherited this tier from the blob service default that we saw on the Overview page. If I upload a second blob from the same folder but this time go into the advanced upload settings, we can choose the tier that we'd like to upload it at, and this will set it specifically for this blob. Note there are two lists for hot here. One is inferred. The other is being specifically set for the blob. We'll go into why this is shortly, but for the moment, let's choose cool, and scroll down and upload the blob. The list now shows that two blobs in this storage account have different access tiers. So why did we have two types of hot access tier available? If we navigate back to the Overview page and into the default access tier settings which is in the configuration pane, and scrolling down, if I change the blob access tier to cool and click Save, all applicable blobs that were inferred hot
>
> **[6:20](https://www.linkedin.com/learning/manage-storage-in-azure/storage-tiers-and-blob-rehydration?u=76281980&t=380)** will now be changed to inferred cool. Now note, there is a transaction cost associated with the write operations of moving all of these blobs between tiers, and this only happens for the ones that are inferred. Therefore, blobs that have their tier explicitly set are not affected. So if we go back into the container, blob1.txt, which was hot inferred, is now cool inferred, and this is why there were two hots on the upload. One was to take hot at the blob level itself specifically. The other was to take the inferred tier from the storage account. We've now decided that blob2.[JSON](../../Skills/Web%20Development/JSON.md) can be archived, and in the portal, this is achieved through the ellipses on the blob and then choosing to change the tier. If I choose archive, note a warning is shown stating that rehydration can take several hours. I should also state at this point that the archive tier is not available for zone-redundant storage. Clicking Save and the new access tier is set. This can of course be achieved through the blob [REST](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) API, programmatically with the storage client libraries, or on the command line with Azure [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) and Azure [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md). To rehydrate the blob, we go back to the same menu and choose an online tier. We can also choose the rehydrate priority,
>
> **[7:56](https://www.linkedin.com/learning/manage-storage-in-azure/storage-tiers-and-blob-rehydration?u=76281980&t=476)** standard taking up to 15 hours and high priority for emergency situations, which is guaranteed to less than an hour for blobs up to 10 gig. I'll leave it as standard and click Save and leave the blob to rehydrate. If this process was part of an application, a blob rehydration does create a blob event. This means you could fire an Azure function to alert when the blob was available for read. For file shares, the storage tier is set at the share level. Here we have two shares. One is hot. One is transaction optimized. These tiers were set on creation of the file share by selecting a tier here, but it is also possible to move a share between the storage tiers, and we can do this in the portal by entering the file share and choosing Change tier. Note, [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) recommend that you start with transaction optimized if you are migrating data from an on-premises file server due to the high number of transactions this produces. You can then switch to hot after migration if required. Choosing hot, I'll now click Apply, and this process charges a read transaction at the cost of the old tier and a write transaction at the cost of the new tier, and moving from cool has the added cost of data retrieval. Now, this manual method is fine
>
> **[9:32](https://www.linkedin.com/learning/manage-storage-in-azure/storage-tiers-and-blob-rehydration?u=76281980&t=572)** for a couple of blobs like I've been doing, but if you're managing tens or hundreds of thousands, you'll want to look for a method of automating this process. So join me in the next video when we'll look at how to combat this issue with lifecycle management for blobs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (8), [IoT](../../Glossary/Concept/IoT.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Env Vars:** iot (1), rest (1), api (1), cli (1)
> **Definitions:** is a  (3), is called (1)
> **Analogies:** such as (2), for example (1)
> **File Paths:** blob1.txt (1), blob2.json (1)
> **Cross-References:** go back to (1), in the next (1)
> **Tools:** command line (1), powershell (1)
> **UI Navigation:** scroll down (1), switch to (1)

#### [Manage storage tiers for blobs and files](https://www.linkedin.com/learning/manage-storage-in-azure/manage-storage-tiers-for-blobs-and-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/manage-storage-tiers-for-blobs-and-files?u=76281980&t=0)** - [Instructor] This is a standard V2 storage account with its default access tier for blobs set to hot. So if we go into the container that's been set up for this walkthrough and choose to upload a blob from my laptop. I choose the blob and click upload. If I close the upload blob window, we can see that its access tier has been set to hot and in bracket is the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) inferred, which means it inherited this tier from the blob service default that we saw on the overview page. If I upload a second blob from the same folder, but this time go into the advanced upload settings, we can choose the tier that we'd like to upload it at and this will set it specifically for this blob. Note there are two lists for hot here. One is inferred, the other is being specifically set for the blob. We'll go into why this is shortly, but for the moment, let's choose cool and scroll down and upload the blob. The list now shows that two blobs in this storage account have different access tiers. So why did we have two types of hot access tier available? If we navigate back to the overview page and into the default access tier settings, which is in the configuration pane,
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/manage-storage-tiers-for-blobs-and-files?u=76281980&t=94)** and scrolling down, if I change the blob access tier to cool and click save, all applicable blobs that were inferred hot will now be changed to inferred cool. Now note, there is a transaction costs associated with the right operations of moving all of these blobs between tiers and this only happens for the ones that are inferred, therefore blobs that have their tier explicitly set and not affected. So if we go back into the container, blob1.txt which was hot inferred is now cool inferred. And this is why there were two hots on the upload. One was to take hot at the blob level itself specifically, the other was to take the inferred tier from the storage account. We've now decided that blob2.[JSON](../../Skills/Web%20Development/JSON.md) can be archived and in the portal this is achieved through the ellipses on the blob and then choosing to change the tier. If I choose archive, note a warning is shown stating that re-hydration can take several hours. I should also state at this point that the archive tier is not available for zone redundant storage. Clicking save and the new access tier is set. This can can of course be achieved through the blob [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) API, programmatically with the storage client libraries or on the command line with [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) and Azure [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md).
>
> **[3:09](https://www.linkedin.com/learning/manage-storage-in-azure/manage-storage-tiers-for-blobs-and-files?u=76281980&t=189)** To rehydrate the blob, we go back to the same menu and choose an online tier. We can also choose the rehydrate priority, standard taking up to 15 hours and high priority for emergency situations, which is guaranteed to less than an hour for blobs up to 10 gig. I'll leave it as standard and click save and leave the blob to rehydrate. If this process was part of an application, a blob re-hydration does create a blob event. This means you could fire an Azure function to alert when the blob was available for read. For file shares, the storage tier is set at the share level. Here we have two shares, one is hot, one is transaction optimized. These tiers were set on creation of the file share by selecting the tier here. But it is also possible to move a share between the storage tiers and we can do this in the portal by entering the file share and choosing change tier. Note, [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) recommend that you start with transaction optimized if you are migrating data from an on-premises file server due to the high number of transactions this produces. You can then switch to hot after migration if required. Choosing hot, I'll now click apply. And this process charges a read transaction
>
> **[4:44](https://www.linkedin.com/learning/manage-storage-in-azure/manage-storage-tiers-for-blobs-and-files?u=76281980&t=284)** at the cost of the old tier and a write transaction at the cost of the new tier. And moving from cool has the added cost of data retrieval. Now, this manual method is fine for a couple of blobs like I've been doing, but if you're managing tens or hundreds of thousands, you'll want to look for a method of automating this process. So join me in the next video when we'll look at how to combat this issue with lifecycle management for blobs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (3), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (1)
> **File Paths:** blob1.txt (1), blob2.json (1)
> **Env Vars:** api (1), cli (1)
> **Cross-References:** go back to (1), in the next (1)
> **Tools:** command line (1), powershell (1)
> **UI Navigation:** scroll down (1), switch to (1)
> **Definitions:** is a  (2)
> **Warnings:** warning (1)

#### [Lifecycle management policies and rules](https://www.linkedin.com/learning/manage-storage-in-azure/lifecycle-management-policies-and-rules?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/lifecycle-management-policies-and-rules?u=76281980&t=0)** - [Instructor] When you're managing massive numbers of blobs, transitioning them manually into more cost-effective tiers just isn't practical. It is much more efficient to employ an automated method to transition the data for you. When thinking about the lifecycle of the data you are managing and matching this lifecycle to the graphic from the previous video, it is likely that the age of a blob or versions of a blob will correlate to the access frequency of the blob. The older the blob, the less likely it is to be accessed, and the blob is also likely to age out until the point when the blob or blob version's lifecycle has expired and the blob is deleted. Lifecycle management policies for block blobs and append blobs use the age of the blob and its frequency of access to determine an action on a blob such as transitioning the blob or the blob version to a cooler tier and deleting a blob or blob version. These are the actions of a lifecycle management policy, and filters can be employed based on the blob prefix or blob tags to determine which blobs are affected by the action. Note here that append blobs can only be deleted. Setting an access tier is not possible on an append blob. The age and frequency of access component of a lifecycle management policy is the run condition, which can take into account the number of days after modification, creation, last access, and tier change of a blob where the last access time must be specifically enabled,
>
> **[1:35](https://www.linkedin.com/learning/manage-storage-in-azure/lifecycle-management-policies-and-rules?u=76281980&t=95)** or for a blob version, the days after version creation time. I've switched into the [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) portal to walk through how to set up a lifecycle management policy. This is a standard general-purpose v2 storage account and has two containers. These are logs and media, both of which contain four blobs created nearly a day ago. We're going to set up a lifecycle management policy to place the logs into cool storage a day after created, and then into archive storage. But I'm going to leave the media blobs at the hot access tier as they are to be accessed frequently. In the portal to create a lifecycle management policy, we need to scroll down and click Lifecycle management in the storage account menu. If you recall from the slide before this walkthrough, we need to enable access tracking if we want to use it as a run condition. For this walkthrough, we are just interested in creation date, but I could check this box to enable access tracking if required. We'll choose to Add a rule to our policy, and each rule requires a unique name. (keyboard clacks) And for the walkthrough on this use case, we want to filter to just transition the blobs in the logs container. So we will choose the rule with the scope of limit blobs with filters. When this is selected, a further step for setting the filter is added at the top of the page.
>
> **[3:10](https://www.linkedin.com/learning/manage-storage-in-azure/lifecycle-management-policies-and-rules?u=76281980&t=190)** We now need to choose which blobs to run this rule against. We have the choice of block blobs which can be transitioned through tiers or deleted, and append blobs, which can only be deleted. Our logs are block blobs, so we'll leave this at just block blobs. For blob subtypes, a base blob is the current version, snapshots are for blob snapshots, and versions for previous versions of a blob. Each of these can be moved through different access tiers by lifecycle management and also deleted. Versioning is enabled for blobs in this storage account, but we don't require it for our logs, so we'll leave the selection at base blobs. Clicking Next, we can set up the rules for the base blobs. If we had selected versions of snapshots on the previous tab, then an extra tab for rules would've been added for each. We have the option to set the run condition on the last modified or created time. We want to transition blobs if they were created more than a day ago to cool, so we choose Created, and place one into the More than days ago field. The action we would like to take is to move to cool storage. Then the other condition we need to add, scrolling down a bit further, is to transition to archive storage, which we would like to do after 30 days. Choosing archive storage, note the warning that if the blobs need to be accessible, then this is not recommended.
>
> **[4:46](https://www.linkedin.com/learning/manage-storage-in-azure/lifecycle-management-policies-and-rules?u=76281980&t=286)** Clicking Next, we can add the filter. All filters for a blob prefix must start with a container name. Ours is logs. (keyboard clacks) And if we had a specific virtual directory, we could tag it onto the path here too. We could also add filters for index matches for blob tags here. We just want all blobs in the specified container, so we'll leave this blank and click Add. The lifecycle management policy has been added and is listed. If we click through to the Code View, we can see how the lifecycle management policy is defined in [JSON](../../Skills/Web%20Development/JSON.md). Each has a type of lifecycle and is followed by a definition block, which will contain actions for every blob type chosen. We only chose base blobs and to transition these base blobs to cool, and then the archive tiers. Finally, there is the filter block. This shows we would like to apply the rules to the blobs in the logs container only. Now going back to the Containers and into logs. Lifecycle management policies run once every 24 hours, so we'll break for a while and come back to this shortly because the run condition could fire anytime in the next 24 hours.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Prerequisites:** set up (3)
> **Cross-References:** previous video (1), in the next (1)
> **Env Vars:** json (1)
> **Tools:** azure portal (1)
> **UI Navigation:** scroll down (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)

#### [Monitoring Azure Storage](https://www.linkedin.com/learning/manage-storage-in-azure/monitoring-azure-storage?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/monitoring-azure-storage?u=76281980&t=0)** - [Presenter] When you are looking after data, you need to have mechanisms to monitor how the data is being used, whether the data is always available, and if access to the data is performing as it should. Here's the resource group with the resources for this walkthrough on monitoring [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage. Now, so that we can have a look at these monitoring options, I've created a function app, which contains two functions. The first writes blobs to a storage account every two minutes, and to two different containers. The second lists blobs in one of those containers and downloads the first 10 blobs listed. So these functions are simulating the types of operations on a storage account that can be monitored. In Azure, the Azure monitor collects data from resources into a common data platform with activity logs and metrics being collected by default. Activity logs track operations for the resource which are at the control plane, such as creating, modifying, or deleting a resource. If I increase the time span on this view and click apply, we can view the control plane activities that occurred when this storage account was set up. And here's the create activity. Going back to the storage account overview and then into the monitoring tab and scrolling down, we can see some examples of the types
>
> **[1:36](https://www.linkedin.com/learning/manage-storage-in-azure/monitoring-azure-storage?u=76281980&t=96)** of numerical values that are collected at regular intervals and stored in Azure Monitor metrics, which is a time series database. We have total ingress and total egress which is increasing over the last few days as the blobs are written and read. Scrolling down a little further, we can see there have been some fluctuations in average end-to-end latency. And this chart shows a breakdown of request to the storage account. Drilling into this chart, it takes us into the metrics explorer for the storage account. Here we can design our own charts, changing the name spaces from the account level, for example, to the blob choosing our metrics. I'm going to choose transactions and a summation, and also, we can apply splitting. I'd like to see this by API. Now this chart is showing that requests that are hitting the APIs at the blob endpoint, and we can see it confirms the activity of our Azure function with put blobs, list blobs, and get blobs. These charts can be saved to the Azure portal or to a Grafana workspace as part of a single overview of a system's health. Returning to the storage account and down through the menu to diagnostic settings. This is where it is also possible to recapture resource logs for a storage account. Resource logs log what happened within a resource at the data plane.
>
> **[3:09](https://www.linkedin.com/learning/manage-storage-in-azure/monitoring-azure-storage?u=76281980&t=189)** So in the case of our walkthrough, it's logging the requests that are hitting the blob endpoint from the Azure function. Resource logs are not collected by default in Azure. They must be enabled through diagnostic settings. Here, the diagnostic settings were enabled when the storage account was created by deploying the Bicep files. But these settings can also be applied automatically using Azure policy to enable log and metrics collection at scale. At the blob service, editing the diagnostic setting, we can see that read, write, and delete resource logs are being collected along with transaction metrics which were emitted on every request to the storage account. A diagnostic setting must also have a destination to send the logs or metrics to. Metrics are stored for 93 days which is three months by default. If you need to archive your metrics or transactions against the blobs for longer, then you can archive them to a storage account. We can also choose to stream diagnostic settings to an event hub where other systems or third party tools can be configured to subscribe to specific events. This functionality is particularly useful for activity logs, so we'll go back and have a look at this now. And activity logs can also be sent to event hubs by enabling diagnostic settings for the activity log which is here in export activity logs.
>
> **[4:43](https://www.linkedin.com/learning/manage-storage-in-azure/monitoring-azure-storage?u=76281980&t=283)** If I click add diagnostic setting, we can see that event at a control plane level can be sent to security information and event management systems or SIEM Systems for short. And this would be for further analysis. Activity log diagnostic settings also include service health, so that health events can be tracked. Returning back to the diagnostic settings for the storage account, back into our blob service settings, we can see that some third party tools can be integrated natively with Azure to stream metrics and logs directly without the use in an event hub. And this is selected by choosing center partner solution. Lastly, at the top, we have the option that was already set up on this storage account to send to a log analytics workspace. If I search for log analytics at the top of the portal and choose log analytics workspaces. This is the log analytics workspace. The storage account had been set up to write to. A log analytics enables the collection of analytical data from multiple resources into a single workspace. The data is stored in multiple tables, and if we right click on storage blob logs and choose edit schema, we can see that the table has columns for data specific to the resource being recorded from.
>
> **[6:18](https://www.linkedin.com/learning/manage-storage-in-azure/monitoring-azure-storage?u=76281980&t=378)** For example, a blob service log has account name. And scrolling down, we can also see requested details, such as an app ID from an app registration or a user principal name. A log analytics workspace enables querying using the KQL language against the data. And this is done through the logs menu. I'll just close down the examples page and clear the query plane. For example, I have a query in my notes if I paste this in.
>
> **[6:58](https://www.linkedin.com/learning/manage-storage-in-azure/monitoring-azure-storage?u=76281980&t=418)** Metrics being logged by a storage account. I logged into the Azure Metrics Table, and they have a resource provider of [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) storage. So by using this query and hitting run, I can see the metrics logged in the last 24 hours because this has been set as the time range. If I just close this menu and scroll to the right, we can see the metric name. And these are the metrics which were seen in the overview charts we looked at for the storage account. Now, KQL queries are a powerful troubleshooting tool. For example, if I take the second query from my notes and paste it into the query pane and choose to run the query. This query is showing the top 10 errors in the last three days which is container not found. I've got a third query which I'll paste in, again, at the storage blob blogs table. And this query would enable us to investigate the operations with the biggest latency. I can even choose to draw charts off of these queries and configure the charts. So I'll choose a line chart, so you can see how powerful this tool is. And if we look towards the top of the page,
>
> **[8:32](https://www.linkedin.com/learning/manage-storage-in-azure/monitoring-azure-storage?u=76281980&t=512)** I can choose to create a new alert rule. And this could be against any of the KQL queries. And these can be used to send alerts to support teams or to trigger automated remediation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (11), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **Analogies:** for example (4), such as (2)
> **Env Vars:** kql (3), api (1), siem (1)
> **Prerequisites:** set up (3), you need to have (1), configure (1)
> **Cross-References:** in the last (2)
> **Tools:** azure portal (1)
> **UI Navigation:** click on (1)
> **Definitions:** is a  (1)

#### [Monitoring Azure Storage with storage insights and workbooks](https://www.linkedin.com/learning/manage-storage-in-azure/monitoring-azure-storage-with-storage-insights-and-workbooks?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/monitoring-azure-storage-with-storage-insights-and-workbooks?u=76281980&t=0)** - [Instructor] Returning to the storage account, there are a couple of other menu items that I would like to have a look at, and scrolling down and into Monitoring, the first is Insights. Insights are created by the maintainers of [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Resources. The maintainers build in their expertise on the type of resource, which in this case, is Azure Storage. They'll use this to generate a view of its overall health. Scrolling back up, I can choose to show performance of the storage account, the capacity which is gradually reducing, as every two minutes we are writing blobs to it, and the availability of each of the APIs. This will enable you to start troubleshooting any issues that your service may be experiencing. If I switch into Azure Monitor, it has a storage account insights view and here, we have access to the transactions and minimizing this menu to errors and to capacity. But this time, the view is for multiple storage accounts from the selected subscription. So this is enabling observation at scale. The insights are built up from a series of workbooks. Each of these workbooks corresponds to a tab on our storage accounts Insights page. A workbook uses multiple data sources from Azure to create rich visual reports
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/monitoring-azure-storage-with-storage-insights-and-workbooks?u=76281980&t=94)** for analysis such as storage insights. So this is the storage account overview workbook. And if we click Edit, we could edit the menu options, the tabs, or scrolling down even further, we can choose to edit the actual graphs themselves. We could choose to change the split, the time range and then if you're happy with your changes, you could also choose to save the workbook for later use. We've come to the end of our walkthrough on logging and monitoring options for Azure Storage. Monitoring in Azure is a huge topic so to learn more about monitoring, follow this link to the Azure Monitor documentation. If I scroll down, this is where this excellent graphic comes from on how logs and metrics from Azure Resources can bring insights into your resources, helping you visualize and analyze issues and respond accordingly.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (8)
> **UI Navigation:** scroll down (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Learning more about Azure management](https://www.linkedin.com/learning/manage-storage-in-azure/learning-more-about-azure-management?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/manage-storage-in-azure/learning-more-about-azure-management?u=76281980&t=0)** - [Instructor] Congratulations, you've reached the end of this course on managing [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Storage. We've covered a lot of ground in this course. If you want to know more, where should you go and what should you do? For me, the key to really understanding any resource in Azure is to practice with hands-on environments, which is why the [GitHub](../../Skills/Software%20Development/GitHub.md) repository for this course has Bicep files for creating resources and environments, which enable you to follow along with the walkthroughs in this course and then to explore further. Whilst exploring, you can cross-reference with the readme in the further-reading folder of the repository. and this has links to [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) documentation that was referenced in the making of this course. It also has links to the community Q&A and Azure Storage Technical blog, which often has releases on deep dives into Azure Storage features. Sticking with Azure documentation, the Storage Documentation Hub is split by the storage solution your use case requires. For example, blobs for application storage or data lake for enterprise analytics, or cloud-hosted file shares. And if you want to explore even deeper within the documentation, the Azure Architecture Center for Storage Design has guides for planning a storage deployment and architectures and solution ideas for specific use cases and scenarios. Each of these scenarios has been written by Microsoft Solution Architects
>
> **[1:34](https://www.linkedin.com/learning/manage-storage-in-azure/learning-more-about-azure-management?u=76281980&t=94)** using knowledge gain designing solutions for customers. [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning is of course, an excellent starting point for diving deeper. For example, I'd like to look at Azure Data Lake, so I search for it within the LinkedIn Learning portal. Now, Azure Data Lake is only briefly discussed in this course, and the top result here is a full course on [Azure Data Lake for Developers](Azure%20Data%20Lake%20for%20Developers.md). You may also want to dive deeper into the power of creating repeatable storage configurations using Azure Bicep, which we covered during the infrastructure's code version of this course. And if you're thinking about applying the knowledge gained in this course to gaining a certification, perhaps check out my Azure Solutions Architect AZ-305 Prep Course on designing [Data Storage](../../Skills/Data%20Science/Data%20Storage.md) solutions. Microsoft Learn has an excellent collection of learning paths and modules for Azure Storage. And if you've not used Microsoft Learn before, you can often get hands on in the Azure portal using their lab simulations or Sandpit credits. And this is really useful if you are unable to get credits, or perhaps you may have run out of free credits for Azure, which you can get for 12 months. Azure has a vibrant community full of people who want to help. Reach out through social media, [Slack](../../Skills/Software%20Development/Slack.md) channels, or on LinkedIn, or perhaps find your local user group. I have a lot of choice here in the UK. Please feel free to reach out to me on social media.
>
> **[3:08](https://www.linkedin.com/learning/manage-storage-in-azure/learning-more-about-azure-management?u=76281980&t=188)** I'm active on LinkedIn and across these platforms. It's been a pleasure having you on board for this course. If you have any questions, please drop them in the course Q&A and good luck with your learning.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (15), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (4), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (4), [GitHub](../../Skills/Software%20Development/GitHub.md) (1), [Data Storage](../../Skills/Data%20Science/Data%20Storage.md) (1)
> **Tools:** github (1), azure portal (1), slack (1)
> **CLI Commands:** az (1), find (1)
> **Documentation:** the readme (1), the documentation (1)
> **Analogies:** for example (2)
> **Cross-References:** we covered (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [Nicole Stevens](../../Instructors/Cloud%20Computing/Nicole%20Stevens.md)

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/manage-storage-in-azure-4413556)

## Skills Covered

- Microsoft Azure
- Cloud Storage

## Path Context

### In [Infrastructure Management on Microsoft Azure- Top Skills for IT Professionals](../../Paths/Cloud%20Computing/Learning%20Paths/Infrastructure%20Management%20on%20Microsoft%20Azure-%20Top%20Skills%20for%20IT%20Professionals.md)
← [Microsoft Azure- Networking Concepts](Microsoft%20Azure-%20Networking%20Concepts.md) | **7 of 8** | [Controlling Cost with Azure Management and Billing](Controlling%20Cost%20with%20Azure%20Management%20and%20Billing.md) →

## Appears In

- [Infrastructure Management on Microsoft Azure- Top Skills for IT Professionals](../../Paths/Cloud%20Computing/Learning%20Paths/Infrastructure%20Management%20on%20Microsoft%20Azure-%20Top%20Skills%20for%20IT%20Professionals.md)

## Related Courses

_Courses sharing skills:_

- [Blockchain for Developers- Hyperledger Fabric on Azure](../Cybersecurity/Blockchain%20for%20Developers-%20Hyperledger%20Fabric%20on%20Azure.md) — Microsoft Azure
- [Choosing A Cloud Platform For Developers Aws Azure And Gcp](Choosing%20A%20Cloud%20Platform%20For%20Developers%20Aws%20Azure%20And%20Gcp.md) — Microsoft Azure
- [Google Cloud Data and Storage Foundations](Google%20Cloud%20Data%20and%20Storage%20Foundations.md) — Cloud Storage
- [Azure DevOps for Beginners](Azure%20DevOps%20for%20Beginners.md) — Microsoft Azure
- [Azure Storage Essential Training (2021)](Azure%20Storage%20Essential%20Training%20(2021).md) — Microsoft Azure

---

[↑ Back to top](#)