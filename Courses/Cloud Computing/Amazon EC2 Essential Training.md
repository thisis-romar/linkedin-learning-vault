---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: amazon-ec2-essential-training
url: "https://www.linkedin.com/learning/amazon-ec2-essential-training"
duration_minutes: 267
duration: 4h 27m
level: Intermediate
updated: 6/1/2023
learners: 3708
skills:
  - Amazon EC2
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D4D0DAQGLTV_hboqNbA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1685473977717?e=2147483647&amp;v=beta&amp;t=VfcE9GjMFzjyGbvhcGbNYbVnOQuaRzmkIE28iwwsHiU"
linkedin_topic: Cloud Computing
learning_paths:
  - '[Infrastructure Management on AWS- Skills for Administrators](../../Paths/Cloud%20Computing/Learning%20Paths/Infrastructure%20Management%20on%20AWS-%20Skills%20for%20Administrators.md)'
prev_courses:
  - '[Aws Essential Training For Administrators](Aws%20Essential%20Training%20For%20Administrators.md)'
next_courses:
  - '[AWS- Networking](AWS-%20Networking.md)'
path_nav: '[{"path":"Infrastructure Management on AWS- Skills for Administrators","position":2,"total":5,"prev":"Aws Essential Training For Administrators","next":"AWS- Networking"}]'
path_count: 1
tags:
  - course
  - topic/cloud-computing
  - topic/devops
  - skill/amazon-ec2
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Cloud%20Computing/Amazon%20EC2%20Essential%20Training.md)

![Amazon EC2 Essential Training](https://media.licdn.com/dms/image/v2/D4D0DAQGLTV_hboqNbA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1685473977717?e=2147483647&amp;v=beta&amp;t=VfcE9GjMFzjyGbvhcGbNYbVnOQuaRzmkIE28iwwsHiU)

# Amazon EC2 Essential Training

> Amazon Elastic Compute Cloud (Amazon EC2) provides secure, resizable compute capacity in the cloud, and is one of the most widely used cloud computing services out there today. If you’re a cloud administrator/architect who wants to stay competitive in the job market, it’s more important than ever to know how to leverage Amazon EC2 as more companies move their workloads to the AWS cloud. In this co

> [LinkedIn Learning](https://www.linkedin.com/learning/amazon-ec2-essential-training) | 4h 27m | Intermediate | 4K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (6 videos)
  - [Learning Amazon EC2](#learning-amazon-ec2)
  - [What is Amazon EC2?](#what-is-amazon-ec2)
  - [AWS Free Tier](#aws-free-tier)
  - [AWS regions and availability zones](#aws-regions-and-availability-zones)
  - [AWS Local Zones](#aws-local-zones)
  - [Picking the right region: Best practices](#picking-the-right-region-best-practices)
- [**1. Introduction to Amazon EC2**](#1-introduction-to-amazon-ec2) (17 videos)
  - [What makes up an EC2 instance?](#what-makes-up-an-ec2-instance)
  - [Amazon Machine Image (AMI)](#amazon-machine-image-ami)
  - [How to find a pre-defined AMI](#how-to-find-a-pre-defined-ami)
  - [How to create and restore an AMI](#how-to-create-and-restore-an-ami)
  - [EC2 instance types](#ec2-instance-types)
  - [Key pairs](#key-pairs)
  - [VPC and subnets](#vpc-and-subnets)
  - [Security groups](#security-groups)
  - [Demo: Security groups](#demo-security-groups)
  - [EBS volumes](#ebs-volumes)
  - [Demo: EBS volumes](#demo-ebs-volumes)
  - [Advanced launch options: Part 1](#advanced-launch-options-part-1)
  - [Advanced launch options: Part 2](#advanced-launch-options-part-2)
  - [EC2 user data](#ec2-user-data)
  - [Connect to your EC2 instance](#connect-to-your-ec2-instance)
  - [Navigating the EC2 console](#navigating-the-ec2-console)
  - [Navigating the EC2 dashboard](#navigating-the-ec2-dashboard)
- [**2. EC2 Deep Dive**](#2-ec2-deep-dive) (11 videos)
  - [EBS snapshots](#ebs-snapshots)
  - [Demo: EBS snapshots](#demo-ebs-snapshots)
  - [Instance store](#instance-store)
  - [Elastic IP](#elastic-ip)
  - [Elastic network interface](#elastic-network-interface)
  - [Demo: Elastic network interface](#demo-elastic-network-interface)
  - [Placement groups](#placement-groups)
  - [Instance metadata](#instance-metadata)
  - [Attaching an IAM role](#attaching-an-iam-role)
  - [Launch templates](#launch-templates)
  - [Security groups as source or destination](#security-groups-as-source-or-destination)
- [**3. EC2 Purchasing Options**](#3-ec2-purchasing-options) (8 videos)
  - [Introduction to EC2 purchasing options](#introduction-to-ec2-purchasing-options)
  - [On-demand instances and capacity reservations](#on-demand-instances-and-capacity-reservations)
  - [Spot instances](#spot-instances)
  - [Requesting spot instances](#requesting-spot-instances)
  - [Reserved instances](#reserved-instances)
  - [Savings plans](#savings-plans)
  - [Dedicated instances vs. dedicated hosts](#dedicated-instances-vs-dedicated-hosts)
  - [Launching dedicated instances and dedicated hosts](#launching-dedicated-instances-and-dedicated-hosts)
- [**Conclusion**](#conclusion) (1 videos)
  - [Additional resources](#additional-resources)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Learning Amazon EC2](https://www.linkedin.com/learning/amazon-ec2-essential-training/learning-amazon-ec2?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/learning-amazon-ec2?u=76281980&t=0)** - [Shyam] In this course, I'll help you take the first steps to learning [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md). I'll teach you not only how to launch, manage and scale your own virtual servers in the AWS Cloud but also equip you with a highly valuable skillset that will open up new career opportunities. Hi, I'm Shyam Raj. I'm a technical instructor and industry specialist in [Cloud Computing](../../Topics/Cloud%20Computing.md). Join me in this course to discover how Amazon EC2 can benefit you and your business. Let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) (2), [Cloud Computing](../../Topics/Cloud%20Computing.md) (1)
> **Env Vars:** ec2 (2), aws (1)
> **CLI Commands:** aws (1)
> **Speakers:** - [shyam] (1)

#### [What is Amazon EC2?](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-is-amazon-ec2?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-is-amazon-ec2?u=76281980&t=0)** - [Instructor] Let's begin by understanding what is [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md)? Amazon Elastic Compute Cloud or Amazon EC2 is the compute offering from AWS. It provides on-demand, scalable compute capacity in the form of [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md). Think of virtual machines that you may have installed on your computer. Now, move these virtual machines to the AWS cloud and they are known as [EC2](../../Skills/DevOps/Amazon%20EC2.md) instances. So essentially, EC2 instances are virtual machines that you launch in the AWS cloud. EC2 instances allow various configurations of CPU, memory, storage, and networking capabilities. So when you launch an EC2 instance, you can choose from predefined configurations available for a variety of workloads. For example, certain EC2 instance types are preconfigured for specific workloads such as [Web Servers](../../Skills/Web%20Development/Web%20Servers.md) or database servers, while there are others for applications that require low latency, high CPU, high memory, and other requirements. So when you launch an EC2 instance, you can pick from a predefined configuration or you can also customize the configuration. These instances can be launched in multiple regions. You can choose a region that is closest to you or you could choose the most cost-effective region or the one that helps you with your compliance requirements. The Amazon EC2 service is also referred to as [IaaS](../../Glossary/Concept/IaaS.md) or [infrastructure as a service](../../Glossary/Concept/IaaS.md). When you create a virtual machine using Amazon EC2,
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-is-amazon-ec2?u=76281980&t=95)** you have complete control over the operating system and the applications running on it. You can use the EC2 instance for creating a web server, a database server, or a [Batch Processing](../../Skills/Data%20Science/Batch%20Processing.md) server. Since the infrastructure is in your control, you can decide how you want to use it and it is known as infrastructure as a service. Imagine you want to install a database on an EC2 instance. Since the EC2 instance is an IaaS offering or infrastructure as a service offering, you are responsible for the EC2 instance and for managing the database running on top of it. For use cases where you don't want to manage the underlying EC2 infrastructure, AWS offers managed services such as Amazon RDS, or relational database service, AWS Lambda, AWS Elastic Beanstalk, and others. Another thing to remember is that because the infrastructure is in your control, you are also responsible for its security, like applying operating system patches. Now, let's talk about the key features of Amazon EC2. The first one is low cost. Amazon EC2 instances are launched using shared [Hardware](../../Topics/Hardware.md). In the background, AWS uses physical servers to launch EC2 instances, and the capacity of these physical servers is shared across multiple customers. AWS does this using hypervisors. A hypervisor is a software used to manage virtual machines. Common examples include VMware and VirtualBox.
>
> **[3:09](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-is-amazon-ec2?u=76281980&t=189)** The hypervisor allocates the resources of the physical server, such as the CPU and memory to multiple virtual machines, in this case, EC2 instances. The hypervisor is not visible to the end user. So when you launch an EC2 instance, you only have visibility of the instance itself. You cannot see the underlying physical server or the hypervisor. These EC2 instances that run on the same underlying physical server may belong to different customers, and because multiple customers share the physical resources, the cost is split, making Amazon EC2 an incredibly low-cost offering. The next key feature of Amazon EC2 is that it is quickly scalable both up and down. Let's say you launch an EC2 instance with two vCPUs and two gigs of RAM. Looking at its usage, you realize that you need more capacity to serve your customers. All you need to do is shut down your instance, change the configuration, and reboot. And now, in a matter of a few minutes, you have more capacity. The opposite is true as well. Let's say you start with more capacity and realize that you have over-provisioned. In a matter of minutes, you can scale down the configuration of the instance. Next, you have complete control over the instances. You can control the configuration, the operating system, and any applications you install on it. With Amazon EC2, there is no upfront investment required. You do not need to purchase any hardware to get started.
>
> **[4:43](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-is-amazon-ec2?u=76281980&t=283)** And for usage, it has a pay-as-you-go model, you only pay for what you use. AWS does have other payment models such as reserve instances and savings plans that give you discounts for paying upfront. But to get started with EC2, you do not need them. You can use the pay-as-you-go model and just pay for your consumption. And lastly, Amazon EC2 is available in multiple locations. Your EC2 instances can be launched in any region where AWS provides its services. As you can imagine, these features make Amazon EC2 an attractive compute offering for small customers and large enterprises. In the upcoming videos, we'll look at these features and configuration options in detail.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (18), [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) (11), [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) (6), [Infrastructure as a service](../../Glossary/Concept/IaaS.md) (3), [IaaS](../../Glossary/Concept/IaaS.md) (2)
> **Env Vars:** ec2 (29), aws (10), cpu (3), rds (1), ram (1)
> **CLI Commands:** aws (10), make (1)
> **Analogies:** such as (4), imagine (2), for example (1)
> **Definitions:** known as (2), is an  (1), is a  (1)
> **Prerequisites:** install (2)
> **Code Identifiers:** vcpus (1)
> **Speakers:** - [instructor] (1)

#### [AWS Free Tier](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-free-tier?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-free-tier?u=76281980&t=0)** - [Instructor] Let's now talk about the AWS Free Tier. The AWS free tier provides free hands-on experience to certain AWS services, such as [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md), Amazon S3, and others. For new users, this can be useful, as you can try certain services without being charged. All brand-new AWS accounts have access to the free tier. To create a new free tier account, go to aws.[amazon.com/free](https://amazon.com/free). Once you're here, you can see the different types of offers available with the free tier. For example, there are some AWS services that are available as short-term free trials. Some services are available for free for the first 12 months, and some services are always free and you can see these by scrolling down. For example, if you are a new AWS user, you get 750 hours of [EC2](../../Skills/DevOps/Amazon%20EC2.md) per month for free for the first 12 months. You also get [5G](../../Skills/Software%20Development/5G.md) of storage on Amazon S3 for free for the first 12 months. Scrolling down, you can see some more services. For example, Amazon SageMaker is available as a free trial for the first two months and AWS Lambda provides 1 million free request per month for free always. An important thing to keep in mind is that free usage is calculated each month across all regions and automatically apply to your bill, and another thing to bear in mind is that free usage does not accumulate.
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-free-tier?u=76281980&t=95)** If you are a new AWS user, I recommend checking out the FAQs for answers to common questions about the free tier. From the AWS management console, it is possible to monitor your free tier usage. So once you're in the AWS management console, click your account name at the top right and corner and then navigate to account. Once you're here, click free tier on the left navigation pane. This will show you the free tier services available in your account. Here is the service name, the free tier usage limit, the current usage, forecasted usage, and month to date actual usage percentage and forecasted usage percentage. This shows you free tier usage data for the current month. If you'd like to see free tier usage for the previous months, navigate to the bill section. First, select the required bill, and then scroll down to see the usage by service. For example, you can see here I've used several services in the last month. Expand any service to see its usage details. For example, in the US East/North Virginia region, I have some free tier usage for the key management service, which is a service that allows you to manage encryption keys. So if you want to monitor your free tier usage for the current month, go to the free tier section and for previous months, go to the bill section.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) (1), [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (1), [5G](../../Skills/Software%20Development/5G.md) (1)
> **Env Vars:** aws (10), ec2 (2)
> **CLI Commands:** aws (11)
> **UI Navigation:** go to (3), navigate to (2), select the (1), scroll down (1)
> **Analogies:** for example (5), such as (1)
> **URLs:** [amazon.com](https://amazon.com) (1)
> **Cross-References:** in the last (1)
> **Definitions:** is a  (1)

#### [AWS regions and availability zones](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-regions-and-availability-zones?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-regions-and-availability-zones?u=76281980&t=0)** - [Instructor] Let's talk about AWS regions and availability zones. AWS services are available at multiple locations worldwide and these locations make up regions and availability zones. Let's first talk about a region. A region is a separate geographic area serviced by AWS. So AWS services are available at multiple independent locations worldwide, and each of these independent locations is known as an AWS region. Each region is designed to be completely isolated from other regions. For example, let's say you have two [EC2](../../Skills/DevOps/Amazon%20EC2.md) instances in two different AWS regions. You have an EC2 instance in Ireland, the eu-west-1 region, and you have another EC2 instance in London, the eu-west-2 region. By default, these EC2 instances will not talk to each other, because each region is isolated from other regions. This is by design and it allows the system to be fault-tolerant. This means if one region develops a problem or stops working, other regions will remain unaffected. Each region is represented by a name and a code. For example, the North Virginia region is represented as is-east-1. London is represented as eu-west-2. Mumbai is represented as ap-south-1 and so on. Within these regions, AWS uses physical facilities to provide services to its customers.
>
> **[1:33](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-regions-and-availability-zones?u=76281980&t=93)** These facilities are known as availability zones. Think of these as data centers. An availability zone could be made up of a single data center or multiple data centers. There is no way to tell, as AWS does not reveal this information, but essentially, an availability zone is a facility within a region from where AWS provides its services. Availability zones within a region are isolated but are connected through low-latency links. This is different from the concept of a region. By default, regions are isolated, but availability zones inside a region are connected. This allows you to build highly available infrastructure. If there is a failure in one availability zone, you can quickly fail over to another availability zone. For example, let's say we have a region with two availability zones and inside these availability zones, you have your [Web Servers](../../Skills/Web%20Development/Web%20Servers.md). Servers 1 is the primary web server and it is running in Availability Zone 1 and Server 2 is secondary and it's running in the second availability zone. Server 1 is serving traffic from the internet. If there is a problem with the availability zone, you can quickly switch the traffic to the second availability zone. This is possible because availability zones within a region are connected. When you launch EC2 instances, you launch them inside an availability zone. So when you're in the EC2 console and when you're trying to launch an EC2 instance, you will have the option
>
> **[3:06](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-regions-and-availability-zones?u=76281980&t=186)** to select the availability zone you want to launch in. Every region has at least two availability zones, and again, this is for high availability reasons. An availability zone is represented by a region code followed by a letter. For example, the availability zones in the North Virginia region are represented as us-east-1a, us-east-1b, us-east-1c, and so on because these availability zones belong to the North Virginia region, whose region code is us-east-1. I'm here at a document titled AWS Global Infrastructure. This document shows the different AWS regions on a world map. The blue dots are the AWS regions that are already available and the red ones are the ones that are coming soon. At the time of this recording, there are 31 AWS regions and 99 availability zones. To see more details, navigate to the Regions and Availability Zones section. Here you will see the different AWS regions in different parts of the world. Scrolling down, you can see more details about the regions. For example, the US West Oregon region has four availability zones and it was launched in 2011. Here's another document titled Regions and Zones. Scrolling down this document, you will see a table with a list of all the AWS regions, their region code and their opt-in status.
>
> **[4:42](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-regions-and-availability-zones?u=76281980&t=282)** Any region that has the opt-in status set to not required is already enabled in your AWS account. So these regions are enabled by default in your AWS account and those regions that have the opt-in status set to required need to be enabled before you can use them. Now let's switch to the AWS Management Console and take a look at regions and availability zones. Once you're in the AWS Management Console, you will see the currently selected region on the top right hand corner. To see a list of regions already enabled in your account, click your name and then navigate to the Account section. Once you're here, scroll down to the AWS Region section. Here you will see a list of all the regions enabled by default and you will also see other regions that can be enabled in your AWS account. Notice you do not see availability zones and that's because availability zones are shown when you are launching an EC2 instance. Start by clicking the Services dropdown menu, then navigate to the Compute section and then click EC2. From the EC2 dashboard, click the Launch Instance button and then click Launch Instance. Doing so will invoke the EC2 instance launch wizard. To see the availability zone in which the EC2 instance will be launched, scroll down to the Network section or the Network Setting section and click Edit.
>
> **[6:17](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-regions-and-availability-zones?u=76281980&t=377)** The availability zone can be seen under the Subnet dropdown. So in this dropdown, you can see all the availability zones in the region. For example, us-east-1a, us-east-1b, us-east-1c, and so on. A subnet is an IP address range assigned to an availability zone. So if you launch this EC2 instance, the instance will be launched in the North Virginia region and the availability zone you select from here. Finally, there are two special regions that we need to talk about. The first one is AWS GovCloud and the other one is AWS China. AWS GovCloud is designed to hold sensitive data for US government customers. By default, you do not have access to this region. To gain access, you will need to contact AWS and provide your identity information. Similarly, the AW China region is specifically designed for Chinese customers only. You'll need to contact AWS to get access to this region. Now that you've understood about regions and availability zones, take a moment to try this yourself. Try to enable a region that is not enabled by default. For example, try to enable the Cape Town region or the Zurich region. These are not enabled by default in your account. Enabling a region does not incur charges, so feel free to turn them on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (13), [Web Servers](../../Skills/Web%20Development/Web%20Servers.md) (1)
> **Env Vars:** aws (26), ec2 (13)
> **CLI Commands:** aws (26), make (1)
> **UI Navigation:** navigate to (3), dropdown (3), scroll down (2), select the (1), switch to (1)
> **Definitions:** is a  (4), known as (2), is an  (1)
> **Analogies:** for example (7)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [AWS Local Zones](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-local-zones?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-local-zones?u=76281980&t=0)** - [Instructor] So we understood that availability zones are physical facilities within an AWS region from where AWS provides its services. Local zones are similar to availability zones in that they provide AWS services, but they are located closer to large populations and industry centers. Here is the AWS global infrastructure map that we saw in the last video. It shows AWS regions in different parts of the world. For example, here in North America, AWS has regions on the west coast and the east coast. But as you can see, there could be many other large cities that are not close to an AWS region. Users in these cities could experience high latency when accessing services hosted in these AWS regions. For this purpose, AWS has local zones. A local zone is an extension of an AWS region. It brings core AWS services such as compute, storage, and database close to large population and business centers. It allows low latency access for use cases such as [Live Streaming](../../Skills/Web%20Development/Live%20Streaming.md), [Real-Time](../../Skills/Database%20Management/Real-Time.md) gaming, virtual workstations, and more. It also allows you to comply with state and local data residency requirements in specific sectors such as healthcare, financial services, and government. Every local zone is represented by an AWS region code followed by an identifier that indicates the location of the local zone. For example, the Atlanta local zone is represented as us-east-1-atl-1.
>
> **[1:37](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-local-zones?u=76281980&t=97)** This is because the Atlanta local zone is tied to the North Virginia region, whose region code is us-east-1. Similarly, the Perth local zone is represented as ap-southeast-2-per-1. This is because the Perth local zone is tied to the Sydney region, whose region code is ap-southeast-2. Unlike availability zones, local zones are not enabled by default. So if you'd like to launch an [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance inside a local zone, the local zone first needs to be enabled in your AWS account before you can launch an EC2 instance in it. To enable a local zone, sign in to your AWS management console, and navigate to the EC2 service. You will find this under the Compute section. Once you're on the EC2 dashboard, under the Account attributes section, click Zones. Doing so will show the local zones available within the currently selected region. As you can see, I've already enabled a couple of local zones in this region, Atlanta and Boston. Scrolling down, you can see the other local zones available in the region and their status. For example, if you wanted to enable the Chicago local zone, you'll need to click Manage, change the status to Enabled, and then click Update zone group. Once the local zone has been enabled, you will need to assign it an IP address range, similar to what we saw with availability zones. And that is done from the VPC service.
>
> **[3:11](https://www.linkedin.com/learning/amazon-ec2-essential-training/aws-local-zones?u=76281980&t=191)** Once you assign it an IP address range, it then becomes available to launch an EC2 instance. For example, I've already assigned an IP address range to the Atlanta local zone. Let's take a look at it from the EC2 launch wizard. So back to the EC2 dashboard, I'll click Launch instance, and then click Launch instance again. Then I'll scroll down to the Network settings section. And then click the Edit button. From here, I can use the Subnet dropdown to see the Atlanta local zone, which can be seen over here. And as you can see, it has an IP address range assigned to it. Notice that the other local zones are not available for selection. Even the Boston Local zone, which was enabled, does not show up in this list because I haven't assigned it an IP address range. So there are two steps to use a local zone. First, it must be enabled in your account, and second, you must assign it an IP address range. Now, take a moment to try this yourself. Try to enable the Las Vegas local zone on your AWS account. Here's a tip for you: You will not find it under the North Virginia region. Similar to a region, enabling a local zone does not incur charges. So feel free to turn it on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (7), [Live Streaming](../../Skills/Web%20Development/Live%20Streaming.md) (1), [Real-Time](../../Skills/Database%20Management/Real-Time.md) (1)
> **Env Vars:** aws (15), ec2 (7), vpc (1)
> **CLI Commands:** aws (15), find (2)
> **Analogies:** for example (4), similar to (3), such as (3)
> **UI Navigation:** navigate to (1), scroll down (1), dropdown (1)
> **Cross-References:** in the last (1)
> **Definitions:** is an  (1)
> **Prerequisites:** you'll need (1)

#### [Picking the right region: Best practices](https://www.linkedin.com/learning/amazon-ec2-essential-training/picking-the-right-region-best-practices?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/picking-the-right-region-best-practices?u=76281980&t=0)** - [Instructor] With so many regions available to launch an [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance, a common question is how to pick a region. What are the factors to keep in mind when picking a region for your EC2 instance? Primarily, there are four factors, starting with service availability, pricing, latency, and compliance. Let's talk about the first one, service availability. Not all AWS services are available in all AWS regions, so it could be possible the AWS service you want to use is not available in the region of your choice. You can verify this by looking at the AWS Regional Services document. Here is the document titled "AWS Regional Services" and it contains a list of all AWS services available in different regions. [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) is probably the most popular AWS service and so it is available in all regions, but the same cannot be said for other AWS services, especially the newer ones. So before you pick a region, it is important to verify that the service is available in the region of your choice. The next factor is pricing. The cost of an AWS service is different across regions, so it could be possible that the cost of an EC2 instance in North Virginia is different from that in London. Here is a document titled "Amazon EC2 On-Demand Pricing." This document allows you to look up the price of an EC2 instance in different regions. For example, here, the region is set to US East (Ohio),
>
> **[1:37](https://www.linkedin.com/learning/amazon-ec2-essential-training/picking-the-right-region-best-practices?u=76281980&t=97)** the operating system is set to [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md), and down here we have the different EC2 instance types. The instance type determines the configuration of the instance. Let's use t3.micro as an example. A t3.micro instance provides two vCPUs and one gig of RAM. The cost of a t3.micro instance running a Linux operating system in the Ohio region is $0.0104 per hour. Now let's change the region to London. The cost of the same EC2 instance running a Linux operating system of type t3.micro in the London region is $0.0118 per hour, which is higher than the cost in Ohio. This difference is due to factors such as the cost of real estate, internet, cooling, and electricity. So before you select a region for your EC2 instances, make sure you've checked the price of the instance in that region. The next factor is latency. Latency is the delay that you can expect when you are accessing a service. Consider an example where users are located in Australia and they're trying to access a server in North America. As you can imagine, in this scenario, users can expect a high latency because the packet has to travel a long distance. To reduce the latency, the server can be brought closer to the users. So this is another factor to keep in mind,
>
> **[3:10](https://www.linkedin.com/learning/amazon-ec2-essential-training/picking-the-right-region-best-practices?u=76281980&t=190)** the location of your users. Ideally, you would want to select a region that is closer to your users. Next, we have compliance. Certain businesses and certain data types are bound by data residency regulations. For example, if you are a bank in the United Kingdom, you would want your data to be hosted in the country rather than keeping it outside the country, so this is another factor that could influence your region selection. If you're hosting certain types of data, such as [Financial Data](../../Skills/Software%20Development/Financial%20Data.md) or healthcare data, you may be bound by data residency regulations. Finally, there is another factor to keep in mind which is not region specific but it is about AWS in general, and it is known as service-level agreement. Service-level agreement is a commitment between a vendor and a customer, in this case, between AWS and you or your business. It outlines the level of service you can expect from AWS and it is measured in metrics that vary across different AWS services. Here is a document titled "AWS Service Level Agreements" and it outlines the service level agreement for different AWS services. Let's check the SLA for Amazon EC2. For Amazon EC2, the performance is measured in monthly uptime percentage, and AWS expects to provide more than 99.99% uptime every month. If the uptime percentage is less than 99.99%
>
> **[4:44](https://www.linkedin.com/learning/amazon-ec2-essential-training/picking-the-right-region-best-practices?u=76281980&t=284)** but greater than or equal to 99%, AWS provides 10% of service credits. And similarly, you have different service credits for different tiers. Let's look at another service. Let's take Route 53 as an example. Route 53 is a [DNS Management](../../Skills/Network%20and%20System%20Administration/DNS%20Management.md) service from AWS, and its performance is also measured in monthly uptime percentage. AWS expects to provide 100% uptime for Route 53. If the monthly uptime percentage is less than 100% but greater than or equal to 99.9%, AWS provides 10% of service credits. So before you sign up for an AWS service, it is important to check the service-level agreement. This is an important factor to keep in mind before moving your workloads to AWS.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (7), [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) (4), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (3), [Financial Data](../../Skills/Software%20Development/Financial%20Data.md) (1), [DNS Management](../../Skills/Network%20and%20System%20Administration/DNS%20Management.md) (1)
> **Env Vars:** aws (22), ec2 (11), ram (1), sla (1), dns (1)
> **CLI Commands:** aws (22), make (1)
> **Definitions:** is a  (4), known as (1), is an  (1)
> **Versions:** 99.99 (2), 0.0104 (1), 0.0118 (1), 99.9 (1)
> **Analogies:** for example (2), such as (2), imagine (1)
> **Warnings:** keep in mind (4)
> **Code Identifiers:** vcpus (1)


### 1. Introduction to Amazon EC2

[↑ Back to Table of Contents](#table-of-contents)

#### [What makes up an EC2 instance?](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-makes-up-an-ec2-instance?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-makes-up-an-ec2-instance?u=76281980&t=0)** - [Instructor] Let's Understand What Makes Up an [EC2](../../Skills/DevOps/Amazon%20EC2.md) Instance. In this video, we'll identify the components that make up an EC2 instance, and then in the following videos, we'll understand each of those components in detail. So for now, in this video we'll keep it at a very high level. We'll simply identify the components and the features that make up the instance and then in the following videos we'll understand each of those components and its details. Let's start by navigating to the EC2 service. Once you're on the EC2 console, click the launch instance button and then click Launch Instance. Doing so will invoke the EC2 instance launch wizard. The first thing you can configure here is tags. Tags are key value pairs that you can assign to almost any AWS resource. By default, you can assign a name tag, but if you'd like to add additional tags, you can click here and then add the tag key and tag value. For example, you can add tags to identify the owner, email address, department, cost center and more. Tags are very useful for identifying, filtering and cost allocation purposes. Moving on, the next item you can configure is application and OS images, also known as Amazon machine image or AMI. An AMI is a template that contains information about the operating system and applications to be installed. on the instance. You can select from predefined AMIs
>
> **[1:34](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-makes-up-an-ec2-instance?u=76281980&t=94)** or you can explore more AMIs in the AWS marketplace and community. Every EC2 instance must be associated with an AMI because it is the AMI that contains information about the operating system and the applications that need to be installed on your instance. Over here, you can select the architecture of the AMI. Moving on, the next item you can configure is instance type. The instance type determines the size or the configuration of the instance in terms of vCPU and memory. By default, the T2 micro instance type is selected that provides one vCPU and one gig of ram. There are other options available that provide more or less configuration. For example, if you select a T2 medium instance type, it provides two vCPUs and four gigs of ram. Over here, you can see the price of a T2 medium instance running different operating systems. Next, you have the option to configure a key pair. A key pair is used to log into the EC2 instance. It contains two keys, a public key and a private key and hence, it is known as a key pair. Most EC2 instances will need a key pair for login purposes. Without a key pair, you might not be able to log into your instance. Moving on, you can configure network settings. To see all settings, click the Edit button. The first setting you can configure is the VPC. A VPC is a virtual network
>
> **[3:07](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-makes-up-an-ec2-instance?u=76281980&t=187)** in which you can launch an instance. It corresponds to an IP address range, so essentially a VPC is an IP address range in which you can launch your EC2 instance. A VPC maps to a region. For example, this VPC ending with 1053 belongs to the Oregon region. In the earlier videos, we understood that every region has multiple availability zones. So the VPC IP address range is broken down into smaller IP address ranges known as subnets and every subnet belongs to an availability zone. So in this dropdown, you can select the subnet or in other words, the availability zone. For example, here in Oregon, the availability zones are US West 2a, 2b, 2c, and 2d. And you'll notice every subnet has an IP address range associated with it, and this IP address range belongs to the larger IP address range of the VPC. So essentially a VPC is a large IP address range, which is then broken down into smaller IP address ranges, known as subnets, and each subnet belongs to an availability zone. We'll talk more about VPC and subnets in the upcoming videos. Next, we have the option to configure public IP address. This option here allows you to control whether your EC2 instance will be assigned a public IP address or not. Next, we have security groups; a security group is--
>
> **[4:40](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-makes-up-an-ec2-instance?u=76281980&t=280)** A security group is like a virtual firewall that controls incoming and outgoing traffic for your EC2 instances. To configure a security group, you can define rules and within these rules you can define the type of traffic, protocol, port range and the source or destination of the traffic. Moving on, the next important setting is storage. Over here, you can configure the storage to be associated with your EC2 instance. Every EC2 instance will have at least one volume known as the root volume that contains the operating system. Here is the size and type of the root volume. You can also add additional volumes for your EC2 instance. Moving on, we have some advanced settings, these settings are optional, meaning you do not need to configure them to launch your instance. However, there are some important options that we can talk about. For example, the first one here is request spot instances. Spot instances are a type of EC2 instance that is available at a discounted price. We'll talk more about this in an upcoming video. Next, we have the option to domain join the EC2 instance. This can be useful if you want to join your EC2 instance to a domain like a [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) [Active Directory](../../Skills/Network%20and%20System%20Administration/Active%20Directory.md) domain. The next important setting here is IAM instance profile. By default, your EC2 instance will not have access to other AWS services, even if they belong to your AWS account.
>
> **[6:14](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-makes-up-an-ec2-instance?u=76281980&t=374)** For example, if your EC2 instance wants to access an image sitting in your S3 bucket, by default, the EC2 instance will not be able to do so and this is by design. However, by assigning an IAM instance profile, also known as an IAM role to your EC2 instance, the instance will be able to access the image. Moving on, let's look at some other important settings to be aware of like shut down behavior and stop behavior. This controls what happens to your EC2 instance when you shut it down or when you stop it. Then you have settings for termination protection and stop protection, and you also have a setting to turn on detailed CloudWatch monitoring. CloudWatch is a monitoring service from AWS that monitors the different AWS resources. By default, your EC2 instances are monitored every five minutes, but if you'd like to change that to one minute monitoring, you can turn it on from here. Moving on, there are some other important settings to be aware of, like placement group. By default, when you launch an EC2 instance, you do not have control over how the EC2 instance is placed on the underlying [Hardware](../../Topics/Hardware.md) or the underlying physical server. By using placement groups, you can influence this. A placement group allows you to influence how EC2 instances are placed on the underlying hardware. This is typically done to improve the performance or fault tolerance of the EC2 instance. We'll talk more about this in an upcoming video.
>
> **[7:49](https://www.linkedin.com/learning/amazon-ec2-essential-training/what-makes-up-an-ec2-instance?u=76281980&t=469)** And finally, the other important setting to be aware of is tenancy. The tenancy setting determines whether your EC2 instance will be launched on shared or dedicated hardware. So as you can see, there are so many settings that you can configure when you're launching an EC2 instance. In the upcoming videos, we'll look at each of these settings in detail. Once you've configured your required settings, you can then click the launch instance button to launch your EC2 instance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (30), [Hardware](../../Topics/Hardware.md) (3), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1), [Active Directory](../../Skills/Network%20and%20System%20Administration/Active%20Directory.md) (1)
> **Env Vars:** ec2 (30), vpc (9), aws (6), ami (5), iam (3)
> **Definitions:** known as (5), is a  (4), is an  (1), in other words (1)
> **Prerequisites:** configure (11)
> **CLI Commands:** aws (6), make (2)
> **Analogies:** for example (6)
> **Code Identifiers:** vcpu (2), vcpus (1)
> **UI Navigation:** select the (2), dropdown (1)

#### [Amazon Machine Image (AMI)](https://www.linkedin.com/learning/amazon-ec2-essential-training/amazon-machine-image-ami?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/amazon-machine-image-ami?u=76281980&t=0)** - [Instructor] Let's understand the first component that makes up an [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance, known as Amazon Machine Image or AMI. An AMI is a template that contains information about the operating system and applications that need to be installed on an EC2 instance. The best way to understand an AMI is to think of the operating system CD or DVD that we used in the past. For new installations, we would use a CD or DVD to load the operating system. The operating system would also have some applications pre-installed on it, such as a text editor. Take the operating system CD or DVD and virtualize it, and now we have an AMI. So an AMI contains the information needed to install the operating system and necessary applications on an EC2 instance. You must specify an AMI to launch an EC2 instance because without an AMI, the EC2 service would not know which operating system to install on the instance. So every EC2 instance begins as an AMI. An AMI can be used to launch multiple instances. AMIs can be of two types, predefined AMIs and custom defined AMIs. Predefined AMIs are supplied by Amazon or a verified Amazon partner while custom defined AMIs are created by users. Predefined AMIs are available in the AWS marketplace and they can be easily identified by looking at the green verified provider label. AMIs are regional resources,
>
> **[1:34](https://www.linkedin.com/learning/amazon-ec2-essential-training/amazon-machine-image-ami?u=76281980&t=94)** so it is possible that an AMI available in the North Virginia region is not available in the Ohio, Oregon, or other regions, and that's because AMIs are regional resources. Every AMI has an identifier called as the AMI ID. When you launch an EC2 instance from the AWS management console or the user interface, you do not need to provide the AMI ID. But if you launch an EC2 instance using the APIs or from the command line interface, you will need to provide the AMI ID. An AMI contains information about volumes to attach to the instance when it is launched. Every EC2 instance has at least one volume known as the root volume on which the operating system is installed. An EC2 instance may also have additional volumes depending on the type and the use case for the instance. So the AMI contains information about the volumes to create and attach to the instance when it is launched. Custom AMIs are stored as Amazon Elastic Block Store or Amazon EBS snapshots. Amazon EBS is a storage service from AWS. It provides persistent long-term storage. So custom AMIs, that is AMIs created by users, are stored as EBS snapshots. Predefined AMIs provided by Amazon or its partners do not show up as snapshots. Instead, they show up as images in the marketplace. But if you create an AMI, which is known as a custom AMI,
>
> **[3:09](https://www.linkedin.com/learning/amazon-ec2-essential-training/amazon-machine-image-ami?u=76281980&t=189)** it gets stored as an Amazon EBS snapshot. So here is the lifecycle of a custom AMI. It starts as an EBS snapshot, and when you create an EC2 instance from the AMI, the EBS snapshot gets restored as an Amazon EBS volume, and the volume gets attached to an EC2 instance. We'll talk more about this in the next video, when we create an AMI and launch an instance out of it. AMIs can be backed by EBS volumes or instance store volumes. The difference is in the storage type. EBS volumes provide persistent long-term storage, while instance store volumes provide short-term or temporary storage. If an AMI is backed by an EBS volume, the root device of the EC2 instance will be an Amazon EBS volume that provides persistent long-term storage. By root device, we mean the root volume on which the operating system is installed. If an AMI is backed by an instance store volume, the root device or the root volume of the instance will be an instance store volume which provides temporary storage. So depending on your use case, you may choose an AMI backed by an EBS volume or backed by an instance store volume. AMIs can be shared with other users. You can share AMIs with specific AWS accounts or you can make the AMI public by sharing it with all AWS accounts. Similarly, other users may share their AMIs with your AWS account.
>
> **[4:44](https://www.linkedin.com/learning/amazon-ec2-essential-training/amazon-machine-image-ami?u=76281980&t=284)** An important thing to keep in mind is that there is always a risk with shared AMIs, as Amazon cannot vouch for the security and integrity of these images. So you must perform due diligence before using them and it is recommended to use AMIs published by trusted sources. AMIs can be copied within or across regions, and this can be useful for backup and disaster recovery purposes. If you want to backup an EC2 instance, you can copy the AMI from one region to another, and then use the AMI to create a backup instance. And this is possible because AMIs can be copied. EBS backed AMIs support encryption. Doing so can allow you to meet your security and encryption compliance requirements. So those are the important features and concepts about AMIs. We'll talk more about these and implement some of these in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (14)
> **Env Vars:** ami (27), ec2 (14), ebs (13), aws (6), dvd (3)
> **CLI Commands:** aws (6), cd (3), make (1)
> **Definitions:** known as (2), is a  (2)
> **Cross-References:** in the next (2)
> **Prerequisites:** install (2)
> **Tools:** command line (1)
> **Exercise Files:** template (1)

#### [How to find a pre-defined AMI](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-find-a-pre-defined-ami?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-find-a-pre-defined-ami?u=76281980&t=0)** - [Instructor] Now let's perform some hands-on exercises using AMI. First, let's understand how to select a predefined AMI from the marketplace or from the community. Once you're logged into the [EC2](../../Skills/DevOps/Amazon%20EC2.md) console, click instances on the left navigation pane, and then click the launch instances button. Doing so will invoke the EC2 instance launch wizard, and from here you'll be able to select an AMI. Over here, you can see the commonly used AMIs. And to access the marketplace, click browse more AMIs. From here, navigate to AWS marketplace AMIs, where you'll find AMIs published by AWS and trusted third parties. And as you can see, there are several AMIs to choose from. For example, in this region, there are over 7,400 AMIs in different categories, and the AMIs are listed here. As you can see, some of these AMIs are supplied by AWS while others are supplied by third parties. To know more about an AMI, click the AMI name or click the select button. Here you will see more information about the AMI, such as the overview, hourly pricing, version details, operating system, some more product details, license and pricing information, user information, and support details. So you can select an AMI from the marketplace, or you can also select an AMI from the community.
>
> **[1:36](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-find-a-pre-defined-ami?u=76281980&t=96)** Here you will find AMIs published by developers, and you can see the AMIs as you scroll down. For every AMI, you will see the AMI name, the AMI ID, the platform, publish date, architecture, root device type, which can be EBS or instant store, the AMI owner, virtualization type, and support for ENA, or elastic network adapter, which provides enhanced networking capabilities. So to launch an EC2 instance, you can select a predefined AMI available in the marketplace, or from the community, or you can also create your own custom AMIs. And that's the topic for the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (3)
> **Env Vars:** ami (13), ec2 (3), aws (3), ebs (1), ena (1)
> **CLI Commands:** aws (3), find (2)
> **UI Navigation:** navigate to (1), scroll down (1)
> **Analogies:** for example (1), such as (1)
> **Cross-References:** next video (1)
> **Speakers:** - [instructor] (1)

#### [How to create and restore an AMI](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-create-and-restore-an-ami?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-create-and-restore-an-ami?u=76281980&t=0)** - [Instructor] Now let's learn how to create a custom AMI. We'll start with a [Windows](../../Glossary/Service/Windows.md) [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance and install IIS, or internet information service, on it. IIS will create a web server on your EC2 instance. Then, we'll create an AMI from the instance, so anytime you launch a new EC2 instance from the AMI, IIS should be pre-installed on it. I've already set up my server. Let me show you the steps I followed so you can do the same thing in your lab environment. Once you've logged into the EC2 console, click instances on the left navigation pane, and then click the launch instances button. From here, select a Windows AMI. The exact Windows operating system type does not matter, so feel free to select any Windows AMI. I selected [Windows Server](../../Skills/Network%20and%20System%20Administration/Windows%20Server.md) 2019 Base AMI. Then select an instance type. I selected T2 medium, which provides two virtual CPUs and four gigs of ram. Then create a new key pair or associate an existing key pair. Then create a new security group or associate an existing security group and make sure the security group allows inbound RDP and HTTP. RDP will allow you to log into the EC2 instance and HTTP will allow you to access the web server. In later videos of this course, I talk about creating key pairs and security groups and I also talk about connecting to an EC2 instance. Feel free to check out those videos
>
> **[1:32](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-create-and-restore-an-ami?u=76281980&t=92)** to configure your instance. With the configuration in place, click the launch instance button to launch your EC2 instance and then log into your EC2 instance to configure IIS. Once you've logged into the EC2 instance, click the start menu and then go to server manager. From here, click local server on the left navigation pane. Then click manage and click add roles and features. Doing so will open a wizard. On the first step, select the installation type. In this case, we are performing a role-based or feature-based installation. On the next step, select the server. In this case, we are installing on the local server. And then on the next step, from the available roles, select web server IIS. No configuration changes are required, so click next a few times and let the installation complete. Once the installation has completed, IIS will be installed and your EC2 instance will function as a web server. Let's test it. Back to the EC2 console, select your EC2 instance. Copy the public IP address and access it from a browser. I'm here on a browser tab. I'll paste the public IP address and press enter and we can see the default IIS webpage. So IIS has been installed on this EC2 instance. Now we'll create an AMI from this instance. Let me show you how to do it. Back over here to the EC2 console.
>
> **[3:04](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-create-and-restore-an-ami?u=76281980&t=184)** To create an AMI from this instance, select the instance, click the actions button, go to image and templates, and then click create image. Start by providing an image name or an AMI name. Then provide an image description and then use this option to create an AMI without rebooting the EC2 instance. By default, when you create a new AMI, AWS reboots the instance. This is done to ensure that everything on the instance is stopped and it is in a consistent state during the creation process. Check this box to prevent the reboot. Down here you can see the storage information of this AMI. This AMI will have a root volume of 30 gigs in size. Notice you also have the option to add additional volumes. Next, you can configure tanks. And with all the configuration in place, you can then click the create image button. Doing so will initiate the AMI creation process. To save some time, I've already created the AMI. Let's look at it. Back over here, I'll click AMIs on the left navigation pane. And here is the AMI. We'll look at the information associated with the AMI in a minute, but first, let's launch an instance from this AMI. Select the AMI and then click launch instance from AMI.
>
> **[4:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-create-and-restore-an-ami?u=76281980&t=275)** I'll give this instance a name. I'll call it Win server from AMI. Notice I do not have to select an AMI because it was already selected in the previous step. Next, I'll select an instance type. I'll select a key pair and I'll also select a security group that allows inbound HTTP access. Here I've selected a security group that allows all inbound access, which is okay for a lab environment, but not recommended in a production scenario. Next, we can see the storage volume that will be associated with the EC2 instance. In this case, the root volume will be 30 gigs in size. And with all the configuration in place, I'll click the launch instance button. While the instance is being launched, let's look at the information associated with the AMI. I'll go back to AMIs and select the AMI. Over here we can see details such as the AMI ID, the AMI name, the account owner ID, the status of the AMI, which is available, which means the AMI can be used to launch new EC2 instances, platform details, architecture, source information, creation date, and root device type. Over here we can see the block device mappings. We know that custom AMIs are stored as snapshots, so here is the snapshot ID, the size of the snapshot, 30 gigs in size, and the type which is general purpose GP2. To see the associated snapshot,
>
> **[6:07](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-create-and-restore-an-ami?u=76281980&t=367)** go to snapshots on the left navigation pane. I'll click the refresh button and here we can see the snapshot associated with the AMI. This snapshot will be restored as an EBS volume and then attached to the EC2 instance that we just launched. Let's go back to AMIs and look at some more details. Under the permissions tab, you can see the permissions of this AMI. So this AMI is private and has not been shared with any other AWS accounts. Now let's talk about some of the actions that you can perform on the AMI. First, select the AMI, then click the actions button. The first action you can perform is to copy the AMI. AMI are regional resources and they can be copied from one region to another. It can also be copied to the same region. The purpose of doing this is to change the encryption status of the AMI. This AMI is an unencrypted AMI, so if you wanted to encrypt it, you can copy to the same region and change the encryption status. You can also copy to other regions. Back over here, let's look at some other important actions. With the AMI selected, click the actions button. Here, you can change the AMI permissions, you can share the AMI privately with other AWS accounts or other AWS organizations and OUs, or you can even make the AMI public. Notice here you have the option to add the create volume permission.
>
> **[7:40](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-create-and-restore-an-ami?u=76281980&t=460)** By default when you share an AMI, it can only be used to create a new EC2 instance. With this option turned on, the AMI can also be used to create a new volume. So in addition to using the AMI to create a new instance, it can also be used to create a new volume. Back over here, let's look at some more options. The AMI can also be used to request spot instances. Spot instances are EC2 instances available at discounted pricing. And here you also have the option to manage tags and change the description of the AMI. Now let's look at the EC2 instance we launched from the AMI. I'll go back to the instances section and here is the EC2 instance we launched called Win server from AMI. Now let's verify if IIS is installed on the instance. Select your EC2 instance, copy the public IP address, and try to access it from a web browser. Back over here to the browser, I'll open a new tab and try to access the public IP address. And straightaway we can see the default IIS webpage. We did not make any configuration changes on the instance, and yet IIS was pre-installed out of the box. As can imagine, using custom AMIs can be so useful for dev and test environments where you need preconfigured machines for testing purposes, it can save a lot of time that would otherwise be required for creating the test environment. Now let's go back and review some more information associated with the AMI.
>
> **[9:12](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-create-and-restore-an-ami?u=76281980&t=552)** Back over here, let's quickly look at the volume associated with this EC2 instance. On the left navigation pane, I'll click volumes. The first volume belongs to the EC2 instance that we originally launched and this is the volume associated with the new EC2 instance. And this was restored from the snapshot. Back to AMIs, when you go to the AMI section, by default, you're looking at AMI owned by you. If you'd like to see AMIs shared with you, use the dropdown and set it to private images and if you'd like to see public AMIs, use the dropdown and set it to public images. Finally, let's understand how to delete an AMI. I'll set this back to owned by me. To delete an AMI, first you need to deregister it. So select the AMI, click the actions button and then select deregister AMI. Notice here, AWS gives you a warning that snapshots are not automatically deleted when you deregister an AMI and here is the associated snapshot ID. So first, let's deregister this. The AMI has been deregistered, which means it cannot be used to launch any more EC2 instances. Finally, let's delete the associated snapshots. So we'll go to snapshots, select the snapshot, click actions, delete snapshot, and then delete again. So to delete an AMI, there are two steps we need to perform.
>
> **[10:45](https://www.linkedin.com/learning/amazon-ec2-essential-training/how-to-create-and-restore-an-ami?u=76281980&t=645)** The first step is to deregister the AMI and the second step is to delete the associated snapshot.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (27), [Windows](../../Glossary/Service/Windows.md) (4), [Windows Server](../../Skills/Network%20and%20System%20Administration/Windows%20Server.md) (1)
> **Env Vars:** ami (60), ec2 (27), iis (11), aws (5), http (3)
> **UI Navigation:** select the (8), go to (5), dropdown (2)
> **CLI Commands:** aws (5), make (3)
> **Prerequisites:** configure (3), install (1), set up (1)
> **Cross-References:** go back to (3)
> **Analogies:** such as (1), imagine (1)
> **Definitions:** is an  (1)

#### [EC2 instance types](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-instance-types?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-instance-types?u=76281980&t=0)** - [Instructor] Now let's talk about [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance types. As we've seen in the previous videos, when you launch an EC2 instance, you must provide an instance type. The instance type determines the configuration of your EC2 instance. Instance types offer different compute, memory, and storage capabilities to your EC2 instance. Instance types are grouped into different instance families, such as general purpose, compute optimized, memory optimized, and more. We'll look at these in a minute. You should select an instance type based on the applications you plan to run on the EC2 instance. For example, if your application is compute intensive, you should select a compute optimized instance type. If your application requires a balance of CPU and memory, you should select a general purpose EC2 instance type. Now let's talk about the naming convention of instance types. Here is an example: m6i.large. The first letter, in this case m, represents the instance family. The m instance type belongs to the general purpose instance family. The number after that, in this case six, represents the generation. And the letter after that, in this case i, is the attribute denoting the additional capability. The m6i.large instance type has an intel processor, and that is represented with the i attribute. The [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) large represents the instance size. In this case, it provides two vCPUs and eight gigs of RAM.
>
> **[1:37](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-instance-types?u=76281980&t=97)** Let's look at one more example, c5.9xlarge. The letter c represents the instance family, in this case compute optimized. The number five is the generation. This instance type has no attribute denoting an additional capability. 9xlarge is the instance size. In this case, it provides 36 vCPUs and 72 gigs of RAM. This information can be found in the AWS documentation. Let's take a look at it. Here is a document that provides information about the different instance families, types, and sizes. On the left side, you have the different instance families: general purpose, compute optimized, memory optimized, accelerated computing, storage optimized, HPC optimized. Select any instance family to see more information about it. For example, the general purpose instance family is well-suited for [Web Servers](../../Skills/Web%20Development/Web%20Servers.md) and code repositories. Under the general purpose instance family you have several instance types, the most popular ones being T2, T3, M4, and M5. Selecting an instance type will show its features and the different instance sizes available in that type. For example, we have m5.Large, xlarge, 2xlarge, and so on. And over here you can see their configuration. Scrolling down, you can also see the use cases
>
> **[3:10](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-instance-types?u=76281980&t=190)** for the M5 instance type. I highly recommend checking this document before selecting an instance type for your workload. With so many options available to select an instance type, it is hard to find the right instance type for your workload or application requirement. AWS has a tool in the EC2 Instance Launch Wizard to compare instance types. Let's take a look at it. Once you're in the EC2 Console, click the Launch Instances button. From here, scroll down to the instance type section and click Compare Instance Types. Over here you will find old instance types listed with their configuration and you also have the option to search for specific instance types. For example, let's search for instance types that offer eight vCPUs, 16 gigs of memory, and high network performance. I'll start by setting the filter for vCPUs, and I'll set this to greater than or equal to eight. Next, I'll set the filter for memory, and I'll set this to greater than or equal to 16. Finally, I'll apply the network performance filter and set this to high. As you can see, it is very easy to use this tool to search the required instance type. Even with this comparison tool, it is easy to under provision or over provision. Under provisioning can cause performance issues,
>
> **[4:45](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-instance-types?u=76281980&t=285)** while over provisioning can lead to unnecessary costs. AWS has a tool called Compute Optimizer that analyzes your EC2 usage and provides recommendations about instance types that can best handle the existing workload. To discover how you can optimize your EC2 usage using Compute Optimizer, please visit the course titled AWS Cost and Performance Optimization, available in the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning Library. In this course, I also show tools and techniques that you can use to optimize your EC2 costs and improve performance. Finally, AWS does allow changing the instance type. This can be useful if you feel that your instance is being over or underutilized. There are a few things to be aware of when you're changing the instance type, and these are listed in the AWS documentation, but the most important thing to be aware of is that the instance needs to be stopped before you can change its type. This could mean downtime if your application is running from a single instance. Now let's see how to change the instance type. I'll click Cancel and Cancel one more time. Over here, I have an EC2 instance that is currently in the stopped state, and the current instance type is t3.micro. To change the instance type, select the instance, click the Actions button, go to Instance settings, and then click Change instance type. From here, you can select the desired instance type. Select the required instance type and then click Apply.
>
> **[6:27](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-instance-types?u=76281980&t=387)** And as you can see, the instance type has now changed to t3.large. Now take a moment to try this in your AWS account. First, launch an EC2 instance, then stop it and try to change the instance type.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (13), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [Web Servers](../../Skills/Web%20Development/Web%20Servers.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Env Vars:** ec2 (13), aws (7), ram (2), cpu (1), hpc (1)
> **CLI Commands:** aws (7), find (2)
> **Analogies:** for example (4), such as (1), for instance (1)
> **UI Navigation:** select the (3), scroll down (1), go to (1)
> **Code Identifiers:** vcpus (4)
> **Definitions:** is an  (1), is a  (1)
> **Warnings:** be aware (2)

#### [Key pairs](https://www.linkedin.com/learning/amazon-ec2-essential-training/key-pairs?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/key-pairs?u=76281980&t=0)** - [Instructor] Now it's time to talk about [EC2](../../Skills/DevOps/Amazon%20EC2.md) key pairs. Before we understand how key pairs work, it is important to talk about [Cryptography](../../Skills/Cybersecurity/Cryptography.md). Cryptography is the technique of transforming data so only intended recipients can process it. Here is an example. Let's say person one wants to send their bank account number to person two. Instead of sending the number in plain text, it can be encrypted, in other words, transformed before being sent on the wire. Doing so ensures that any person in the middle who might be able to see the encrypted text cannot make sense of it because they wouldn't have the key required to decrypt it. With this understanding, let's talk about public key cryptography. It uses two mathematically related keys to encrypt and decrypt data. One key is called the public key, while the other one is called the private key, and together, it is a key pair. This type of cryptography is also known as asymmetric cryptography because it uses two keys, one for encryption and the other for decryption. Here is an example. Let's say we have data that needs to be encrypted. We start with the public key. When the public key's applied to the data, the data is transformed. In other words, it is encrypted. To restore the data back to its original form, we need the other key, the private key. When the private key's applied to the encrypted data, it is restored to its original form.
>
> **[1:33](https://www.linkedin.com/learning/amazon-ec2-essential-training/key-pairs?u=76281980&t=93)** With this understanding, now let's talk about EC2 key pairs. When you're launching an EC2 instance, a key pair name can be specified. Bear in mind, it is not mandatory to do so. If you're already aware of the login credentials of the instance, like in the case of a custom AMI you might have created, you can skip the key pair, but if you're using the predefined AMIs provided by AWS, you will need a key pair to obtain the password of the instance. At boot time, the public key content is placed in the instance, and when you're trying to access the instance, a private key must be specified. An important thing to bear in mind is that key pairs are regional resources. So a key pair that you create in one AWS Region will not exist in another Region. Anyone who has access to your private keys can connect to your instances, so it is important to keep your key pairs in a secure place, and there is no way to recover a lost private key. I have personally been in a situation where I lost my key pair used to log into my web server, and as a result, I did not have access to the instance. Luckily, it turned out that my developer had a copy of the key pair, and we were able to log in. So it is important to keep your key pairs in a secure location. For [Windows](../../Glossary/Service/Windows.md) instances, the key pair can be used to decrypt the password, and for [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) instances, the key pair can be used to directly log in using the SSH command.
>
> **[3:07](https://www.linkedin.com/learning/amazon-ec2-essential-training/key-pairs?u=76281980&t=187)** Now let's go to the AWS console and look at key pairs. Once you're in the EC2 console, you can create key pairs from two places. One way to do it is when you're launching an EC2 instance. So when you're launching an EC2 instance, you have all this configuration that you can apply. And down here is the Key pair section. From here, you can create a new key pair. You must provide a key pair name, then select a key pair type, and the private key download format. If you're connecting from a Linux or a Mac OS machine. you will need it in the PEM format, and if you're connecting from a Windows machine, you will need it in the PPK format. So there are three items to configure, a key pair name, the type, and the format, and then click Create key pair. Doing so will download the key pair to your local machine. You can also create a key pair from the Key Pairs section on the left navigation pan. Click Key Pairs and then click Create key pair, and you'll see the same options here. You can also import a key pair that you may have created on your local machine. So if you created a key pair offline on your local machine, and you want to import it into AWS, click the Actions button and then click Import key pair. Provide a key pair name, and point your browser to the key pair file, and then click Import key pair. We understood that key pairs are regional resources. Over here, I have a key pair in the Ohio Region.
>
> **[4:42](https://www.linkedin.com/learning/amazon-ec2-essential-training/key-pairs?u=76281980&t=282)** If I switch my Region, you will notice the same key pair does not exist in the other Region. So here in the Oregon Region, I do not see the same key pair. Now I'll switch back to the Ohio Region, and let's understand how key pairs can be used to log into EC2 instances. I'll click the Instances section on the left navigation pan. For Windows instances, the key pair can be used to obtain the password. Select your Windows instance, click the Connect button and then navigate to RDP client. Here you can see the public DNS name of the EC2 instance, the username, and you have the option to obtain the password. Click Get password, and here, you can see the key pair name associated with this instance. Then click Upload private key file. Point your browser to the private key file, and click Open. Then click Decrypt password. Here is the password required to log into the Windows EC2 instance. For Linux EC2 instances, the key pair can be used to directly log into the instance. Select your Linux instance, click the Connect button, then go to SSH client, and here, you will see the command needed to log into your EC2 instance, and you can see that the key pair name is part of the command. In the later videos of this course, we'll understand how to use the key pairs to log into Windows and Linux EC2 instances.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (12), [Windows](../../Glossary/Service/Windows.md) (6), [Cryptography](../../Skills/Cybersecurity/Cryptography.md) (5), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (5)
> **Env Vars:** ec2 (12), aws (4), ssh (2), ami (1), pem (1)
> **CLI Commands:** aws (4), ssh (2), make (1)
> **Definitions:** is an  (2), in other words (2), is called (2), is a  (1)
> **UI Navigation:** go to (2), navigate to (1)
> **Prerequisites:** required to (1), configure (1)
> **Tools:** aws console (1)
> **Exercise Files:** download the (1)

#### [VPC and subnets](https://www.linkedin.com/learning/amazon-ec2-essential-training/vpc-and-subnets?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/vpc-and-subnets?u=76281980&t=0)** - [Instructor] Now let's talk about an interesting topic, VPCs and subnets. A VPC, also known as a virtual private cloud, is a virtual, logically isolated network of your AWS account. Essentially, it is an IP address range in your AWS account. Within a VPC, you can have subnets. A subnet is a range of IP addresses in your VPC, so simply put, both VPCs and subnets are IP address ranges. A VPC is a larger IP address range and a subnet is a smaller IP address range in your VPC. We use a similar concept in traditional on-premises networks. We use IP address ranges to logically isolate different segments of the network such as departments. The same concept applies in the AWS cloud. In the AWS cloud, we have different regions, and within a region we can have a VPC, and a VPC is simply an IP address range. Within a region you can also have multiple VPCs. For example, in this region, we have two VPCs. VPC A has the IP address range 172.31.0.0/16 and VPC B has the IP address range 10.0.0.0/16. This type of a configuration can be very useful for isolation purposes. For example, you can have your production instances in VPC A and non-production instances in VPC B, and by default, they will not talk to each other
>
> **[1:36](https://www.linkedin.com/learning/amazon-ec2-essential-training/vpc-and-subnets?u=76281980&t=96)** because VPCs are logically isolated. Within a VPC, you can have subnets and these are smaller IP address ranges that belong to the VPC IP address range. For example, in this VPC, we have three subnets, and each of these subnets has an IP address range that belongs to the VPC IP address range. The number you see after the slash is known as the subnet mask. Each of the subnets has the subnet mask of /20 while the VPC has the subnet mask of /16. This can seem a bit confusing especially if you're not from a networking background. The subnet mask /20 seems to be larger than that of the VPC, which has a subnet mask of /16, but with subnet masks, a larger number means a smaller network. This means the subnet mask /20 is smaller than the subnet mask /16 so these subnets have smaller IP address ranges and they belong to the VPC IP address range. There are several subnet calculators available online. Feel free to use them to understand subnetting at a high level. Now let's talk about the default VPC and subnet. If your AWS account was created after 12th April, 2013, it will have a default VPC in each region. The default VPC is configured with subnets in each availability zone, and it also has a route table. Now let's see this from the AWS console.
>
> **[3:13](https://www.linkedin.com/learning/amazon-ec2-essential-training/vpc-and-subnets?u=76281980&t=193)** Once you're in the [EC2](../../Skills/DevOps/Amazon%20EC2.md) console, you can select a VPC when you're launching an EC2 instance. Once you're in the EC2 instance launch wizard scroll down to the Network Settings section. Once you're here, click the Edit button and then you'll be able to select a VPC and a subnet. You can launch the instance in the default VPC which has the IP address range of 172.31.0.0/16 in every region, or you can also launch your instance in a custom VPC. And then you can select a subnet. An important thing to bear in mind is that the VPC belongs to the region and the subnet belongs to the availability zone. For example, here we can see three subnets belonging to three different availability zones and they have an IP address range within the VPC IP address range. Now let's see how to create a VPC. First, click the Services dropdown menu, then navigate to the Networking & Content Delivery section, and then go to the VPC service. From here, you will see the VPCs in the currently selected region. For example, I have two VPCs in this region. One of them is the default VPC, and I know this by looking at the IP address range, and I also have a custom VPC. To create a new VPC, click the Create VPC button and from here, you can provide a VPC name and then provide an IPv4 address range
>
> **[4:48](https://www.linkedin.com/learning/amazon-ec2-essential-training/vpc-and-subnets?u=76281980&t=288)** and an IPv6 address range. For example, I'll provide the IP address range, 10.1.0.0/16, then click Create VPC.
>
> **[5:01](https://www.linkedin.com/learning/amazon-ec2-essential-training/vpc-and-subnets?u=76281980&t=301)** Within a VPC, you can also create subnets and they can be seen from here. As you can see, I have got three subnets, one in each availability zone, and here you can also see the number of IP addresses available in each subnet. To create a new subnet, click the Create subnet button, select your VPC, give your subnet a name, then select the availability zone in which you want to create the subnet, and then provide an IP address range that belongs to the VPC. Then click Create subnet. A VPC also has a route table associated with it. The route table tells AWS how to handle the traffic. For example, here's the route table that belongs to the default VPC and I can see the routes by going to the Routes tab here. As you can see, all traffic belonging to the VPC IP address range is treated as local traffic and any other traffic destined to the internet is sent to the internet gateway. The internet gateway is a router attached to your VPC and it allows you to send traffic to the internet. So as you can see, a VPC has several components. You have the VPC IP address range. You then have subnets, which are smaller IP address ranges that belong to the VPC. Then you also have route tables that tell AWS
>
> **[6:36](https://www.linkedin.com/learning/amazon-ec2-essential-training/vpc-and-subnets?u=76281980&t=396)** where to route traffic, and you also have internet gateways that send traffic to the internet. If you do not have the default VPC in your region, it is really easy to create one. I'll switch to a region that does not have a default VPC. For example, I'm here in the Oregon region and here I do not have the default VPC. To recreate it, simply click the Actions button, and click Create default VPC. Then click Create default VPC again, and your default VPC should be created. As you can see, it uses the same IP address range 172.31.0.0/16. It automatically creates the required subnets in every availability zone, and also creates the internet gateway to send traffic to the internet. Now, take a moment to try this yourself. Try to create a VPC with an IP address range, and then try to create subnets within that VPC.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (3)
> **Env Vars:** vpc (53), aws (8), ec2 (3)
> **Analogies:** for example (8), such as (1)
> **CLI Commands:** aws (8)
> **Definitions:** is a  (5), known as (1), is an  (1)
> **UI Navigation:** scroll down (1), dropdown (1), navigate to (1), go to (1), select the (1)
> **Versions:** 172.31.0 (3), 10.0.0 (1), 10.1.0 (1)
> **Tools:** aws console (1)

#### [Security groups](https://www.linkedin.com/learning/amazon-ec2-essential-training/security-groups?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/security-groups?u=76281980&t=0)** - [Shyam] Now let's talk about an important feature of [EC2](../../Skills/DevOps/Amazon%20EC2.md) instances known as security groups. A security group is like a virtual firewall that controls inbound and outbound traffic for your EC2 instance. Think of it as a wrapper that sits around your EC2 instance and monitors all inbound and outbound traffic. It then allows or denies the traffic based on the configured rules. Security groups allow you to filter traffic based on protocols and port numbers. Security groups have separate rules to control both inbound and outbound traffic. With security groups, you can only configure allow rules. Deny rules cannot be configured. This might seem a bit different if you work with different firewalls that allow you to configure both allow and deny rules, but with security groups, you can only configure allow rules. By default, security groups block all inbound traffic and allow all outbound traffic. So when you create a new security group there is an outbound rule in place that allows all traffic and there is no inbound rule in place which means no inbound traffic is allowed. Security groups are stateful in nature. This is very important to keep in mind as you work with security groups. Because security groups are stateful in nature, if the initial request from your instance is allowed the response traffic is automatically allowed regardless of the inbound rules. Here is an example. Let's say we have an EC2 instance with a security group and the instance is trying to access a server
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/security-groups?u=76281980&t=95)** on the internet. The initial outbound request is allowed by the security group. When the server responds back, the response traffic will be automatically allowed regardless of the inbound rules. This is because security groups are stateful in nature. They keep a track of the connections. The response traffic belongs to an existing connection and so it will be automatically allowed regardless of the inbound rules. Security group rules can be modified anytime. Multiple security groups can be attached to an EC2 instance. When you do so, rules from all security groups are evaluated to decide whether to allow traffic. For example, imagine an EC2 instance that is receiving inbound traffic and has three security groups associated with it. To make a decision on whether to allow traffic, the rules from all security groups will be aggregated and then evaluated. Security groups are easy to configure. We'll explore this in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (6)
> **Env Vars:** ec2 (6)
> **Prerequisites:** configure (4)
> **Definitions:** is an  (2), known as (1)
> **Analogies:** think of it as (1), for example (1), imagine (1)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [shyam] (1)

#### [Demo: Security groups](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-security-groups?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-security-groups?u=76281980&t=0)** - Now let's understand how to configure security groups. You can configure a security group when you're launching an [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance. Once you're logged into the EC2 console, click the launch instances button. Scroll down to the network settings section and click the edit button. From here, you can create a new security group or associate an existing security group. To create a new security group, you must provide a name and description and then configure the inbound rules. When you're creating a security group from here, that is from the EC2 instance Launch Wizard, you can only configure inbound rules. You cannot configure outbound rules from this console. Once the security group has been created, you can then change the outbound rules, but it cannot be configured from this console. Also, bear in mind that the rules you're configuring here are allow rules. We learned this earlier that security groups only support allow rules, not deny rules. So the rules you're adding here are allow rules. To configure an inbound rule, you must specify the traffic type, protocol, port range, and source information. For traffic type, there are pre-configured options that you can select from such as SSH, SMTP, DNS, HTTP, and more. When you select a preconfigured option, the protocol and port range is automatically populated. If you'd like to customize these,
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-security-groups?u=76281980&t=95)** select from one of the custom options. For source type, there are three options available. You can allow traffic from any IP address or custom IP address ranges, or from your public IP address. The security group will be created when you launch your EC2 instance, and it will be automatically associated to your instance. To see the Associated Security group, select your EC2 instance and go to the security tab. Here, you can see the Associated Security group ID, its inbound and outbound rules. You can also create security groups by navigating to security groups on the left navigation pane. From here, click create security group. Over here, you can provide a name, description, select your VPC, add inbound rules, and notice you also have the option to add outbound rules. So when you create a security group from this console, you have the ability to configure the outbound rules. Notice that there is no preconfigured inbound rules. This means, by default, all inbound traffic will be blocked, and there is a preconfigured outbound rule that allows all outbound traffic. Now let's configure the security group to allow inbound HTTP traffic. Start by providing a name and a description, and then select the VPC. In this region, I only have one VPC, so it is pre-selected.
>
> **[3:09](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-security-groups?u=76281980&t=189)** Then click add rule under inbound rules and set the traffic type to HTTP. Notice the protocol and port range is automatically set to TCP port 80. Set the source to any IPV4 address and then click create security group. The security group has been created. If you'd like to make changes, you can edit the outbound rules and the inbound rules. Now let's see how to associate the security group with an EC2 instance. First, navigate to the instances section. Select your EC2 instance. Go to the actions button. Click the actions button. Go to security and then click change security groups. Over here, you will see the associated security groups and to associate a new security group, click here and select the desired security group. Then click add security group and click save. The security group has been associated with the EC2 instance, and we can confirm this by selecting the instance and going to the security tab. And here we can see two security groups associated with the instance. Bear in mind that the same security group can be attached to multiple EC2 instances. Now try this in your AWS account. Create an EC2 instance and attach a security group that allows inbound ICMP traffic or in other words, ping requests.
>
> **[4:44](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-security-groups?u=76281980&t=284)** Test that you are able to ping the instance and then edit your security group. Remove the rule that allows outbound traffic and see if you're able to ping the instance. Finally, remove the inbound rule that allows ICMP traffic and see if you're still able to ping.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (10)
> **Env Vars:** ec2 (10), http (3), vpc (3), icmp (2), ssh (1)
> **Prerequisites:** configure (8)
> **UI Navigation:** go to (3), select the (2), scroll down (1), navigate to (1)
> **CLI Commands:** ssh (1), make (1), aws (1)
> **Definitions:** is a  (1), in other words (1)
> **Ports:** port 80 (1)
> **Analogies:** such as (1)

#### [EBS volumes](https://www.linkedin.com/learning/amazon-ec2-essential-training/ebs-volumes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/ebs-volumes?u=76281980&t=0)** - [Instructor] Another important component of [EC2](../../Skills/DevOps/Amazon%20EC2.md) instances is EBS volumes. EBS volumes, or Elastic Block Store volumes, are a block-level storage for use with EC2 instances. Block storage is a technology where data is stored in blocks of equal sizes. The best way to understand an EBS volume is to think of a hard drive. We know hard drives are attached to computers for storage. Virtualize the hard drive, and now we have an EBS volume. EBS volumes are raw, unformatted storage volumes. This means they do not have a file system installed on them. An EC2 instance can have multiple EBS volumes attached to it. Just like how a computer can have multiple hard drives attached to it, an EC2 instance can have multiple EBS volumes attached to it. An EBS volume can be attached only to one EC2 instance. There is an exception to this and that is the Provisioned IOPS volume type but for all other volume types, an EBS volume can only be attached to a single EC2 instance. EBS volumes are well-suited for use as primary storage for file systems, [Databases](../../Skills/Software%20Development/Databases.md), or applications that require granular updates. To attach an EBS volume to an EC2 instance, they must be in the same availability zone. For example, let's say we have an EC2 instance in the us-east-1a availability zone. To attach an EBS volume,
>
> **[1:33](https://www.linkedin.com/learning/amazon-ec2-essential-training/ebs-volumes?u=76281980&t=93)** it must also be in the same availability zone as the instance. If you have an EBS volume in the us-east-1b availability zone, it cannot be attached to the instance in the 1a availability zone. So, the instance and the EBS volume must be in the same availability zone. EBS volumes can persist independently from the life of an instance. This means the EBS volume can continue to exist even if the EC2 instance has been terminated. Now, let's talk about the types of EBS volumes. There are three types. Solid-state drive volumes, also known as SSD volumes, hard disk drive volumes, also known as HDD volumes, and magnetic volumes. First, let's talk about SSD volumes. These are optimized for transactional workloads requiring frequent read or write operations with small IO size. IO refers to input, output. The performance of these volumes is measured as IOPS, that is, input, output per second. SSD volumes are ideal for use cases such as boot volumes and databases. Boot volumes are the root device volume of an EC2 instance. SSD volumes are further classified into two types. General Purpose SSD and Provisioned IOPS SSD. General Purpose SSD provides a balance of price and performance. It is suitable for most workloads including virtual desktops, databases, and boot volumes. Provisioned IOPS volumes
>
> **[3:08](https://www.linkedin.com/learning/amazon-ec2-essential-training/ebs-volumes?u=76281980&t=188)** are the highest performing EBS volumes. They are suitable for workloads that require low latency such as IO-intensive [NoSQL](../../Skills/Software%20Development/NoSQL.md) and [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md). Provisioned IOPS volumes support multi-attach. It is a feature that allows a single EBS volume to be attached to multiple instances in the same availability zone. Now, let's talk about the second type of EBS volumes, known as HDD volumes. These are optimized for large streaming workloads. The performance of HDD volumes is measured as throughput. Throughput refers to the amount of data that can be transferred in a given timeframe. This is ideal for use cases such as log processing and data warehouses. An important thing to keep in mind is that HDD volumes cannot be used as boot volumes. HDD volumes are classified into two types. Throughput Optimized HDD and Cold HDD. Throughput-optimized volumes are low-cost volumes, and they're suitable for frequently accessed, throughput intensive workloads, so they're a good fit for data warehouses and log processing. Cold HDD volumes are low-cost volumes suitable for large sequential workloads. They are designed to support infrequently accessed data such as [Big Data](../../Skills/Data%20Science/Big%20Data.md) workloads, log processing, and [Windows](../../Glossary/Service/Windows.md) File Servers. The last EBS volume type is magnetic volumes. These are backed by magnetic drives.
>
> **[4:42](https://www.linkedin.com/learning/amazon-ec2-essential-training/ebs-volumes?u=76281980&t=282)** They are suitable for workloads with small, infrequently-accessed data sets. These are also known as previous generation volumes. There are differences between the volume types we discussed in terms of durability, supported size, maximum IOPS, and maximum throughput. I highly recommend reviewing the documentation before selecting a volume type for your EC2 instance. In the next video, we'll understand how to configure EBS volumes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (11), [Databases](../../Skills/Software%20Development/Databases.md) (3), [NoSQL](../../Skills/Software%20Development/NoSQL.md) (1), [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md) (1), [Big Data](../../Skills/Data%20Science/Big%20Data.md) (1)
> **Env Vars:** ebs (22), ec2 (11), hdd (8), ssd (7), iops (6)
> **Definitions:** known as (4), is a  (2), refers to (2), is an  (1)
> **Analogies:** such as (4), just like (1), for example (1)
> **Cross-References:** we discussed (1), in the next (1)
> **Documentation:** the documentation (1)
> **Warnings:** keep in mind (1)
> **Prerequisites:** configure (1)

#### [Demo: EBS volumes](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-volumes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-volumes?u=76281980&t=0)** - [Instructor] Now let's understand how to configure EBS volumes. You can configure an EBS volume when you're launching an [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance. Let's do that. Once you're in the EC2 console, click the Launch Instances button. Scroll down to the storage section to see the root volume configuration. Here you can see the size of the root volume and the type. The size depends on the selected AMI. By default, AWS selects the Amazon [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) AMI which requires a minimum of eight gigs, but the size will change if you change the AMI. For example, if you select a [Windows](../../Glossary/Service/Windows.md) AMI, it requires a minimum of 30 gigs. To see additional configuration options, click Advanced. Then expand the volume configuration. So here you can configure the volume size, type, delete on termination, and encryption status. GP2 is general purpose SSD volume, and SSD volumes have their performance measured in IOPS or input/output per second. So the performance depends on the volume type, and it also depends on the size of the volume. For example, if I change the size to 100 gigs, you will notice the performance change as well. In the last video, we understood that HDD volumes cannot be used as the root volume. So if you select any one of the available HDD volumes called HDD or throughput optimized HDD you will see an error message that it cannot be used as the root volume.
>
> **[1:34](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-volumes?u=76281980&t=94)** I'll set it back to GP2. When you set the Delete on termination configuration to Yes, the EBS volume is deleted when the EC2 instance is terminated. If you'd like to retain the EBS volume, set this to No. Here you can change the encryption status. Notice you also have the option to add additional EBS volumes. Additional volumes can have the type set to HDD. Every volume type has a minimum required size. For this volume type, the minimum required size is 125 gigs. Once you've configured your volumes, click the Launch Instance button. The attached EBS volumes can be seen from the EC2 console. Select your EC2 instance and go to the Storage section to see the attached EBS volumes. This EC2 instance has one EBS volume attached to it. Its size is 30 gigs and it does not have encryption enabled. You can also create EBS volumes by going to the Volume section on the left navigation pane. Here you can see the root volume of the EC2 instance, which is 30 gigs in size. To create additional volumes, click the Create volume button. From here, you can select the volume type and size. SSD volumes have their performance measured in IOPS and HDD volumes have their performance measured in throughput. Next, you can select the availability zone.
>
> **[3:06](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-volumes?u=76281980&t=186)** This is important because the EBS volume must be in the same availability zone as the EC2 instance. Then you have the option to specify the snapshot ID. This can be useful if you want to create an EBS volume by restoring from a snapshot. Then you have the option to turn on encryption. An important thing to bear in mind is that encryption can only be configured when you're creating the EBS volume. You cannot change the encryption status after creating the volume. Now let's create a volume and attach it to an EC2 instance. Select the required volume type. Then configure the volume size. Select the availability zone in which your EC2 instance is running, and then click Create volume. Now the volume is being created. While the volume is being created, let's talk about modifying the size of the EBS volume. The size of EBS volumes can be increased. To do this, select the EBS volume, click Actions, and then click Modify volume. From here, you can change the type, and you can also change the size. The size of EBS volumes can only be increased. For example, I can set this to 40 gigs, but I cannot reduce to 29 gigs. It shows an error message that the size of a volume can only be increased, not decreased. Also notice there is no option to change the encryption status,
>
> **[4:39](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-volumes?u=76281980&t=279)** so the encryption status can only be configured when you're creating the volume. Now, let's go back. This volume is now in an available state, so we can attach it to an EC2 instance. To attach the volume, select it, click the Actions button and then click Attach volume. Select your EC2 instance option, optionally change the device name, and then click Attach volume. Now the volume has been attached to the EC2 instance. Bear in mind that new volumes cannot be used straight away. They need a file system. Let's see how to create one by logging into a Windows EC2 instance. Alright, I'm here on a Windows EC2 instance. I'll first open Explorer, and from here we can see that this EC2 instance only has the C drive available. Even though the additional volume has been attached, it is not available for use, and that's because it needs a file system. So click the start menu and navigate to the Disk Management utility by searching for diskmgmt. Over here we can see both the volumes, the 30 gig volume, which is the root volume or the C drive, and the one gig volume that we attached. This volume is currently offline. To make it online, right click and then click Online. Next, you'll need to initialize the volume. To do this right, click again and select Initialize Disk.
>
> **[6:15](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-volumes?u=76281980&t=375)** Then click OK. Now the volume is online, but the space is unallocated, so we need to create a file system. Right click on the volume and select New Simple Volume. On the next step, select the size you would like to allocate. Then assign a drive letter, and on the next step, select the file system format. Then click Next and click Finish, and now the volume is ready. If we go back to Explorer, we can see that the new volume is available for use. Similarly on Linux EC2 instances, new volumes must be initialized before they can be used. AWS has a document that outlines the steps you need to perform to initialize a new EBS volume. Now try this in your AWS account. Create a new EC2 instance and try to attach an additional volume. Also, try to change the size of the volume. Then, detach the additional volume and try to attach it to another EC2 instance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (19), [Windows](../../Glossary/Service/Windows.md) (3), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (2)
> **Env Vars:** ec2 (19), ebs (17), hdd (6), ami (4), aws (3)
> **UI Navigation:** select the (7), scroll down (1), go to (1), click on (1)
> **Prerequisites:** configure (4), you'll need (1)
> **CLI Commands:** aws (3), make (1)
> **Analogies:** for example (3)
> **Cross-References:** in the last (1), go back to (1)
> **Speakers:** - [instructor] (1)

#### [Advanced launch options: Part 1](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-1?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-1?u=76281980&t=0)** - [Instructor] Now let's talk about the advanced options you can configure when launching an [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance. These options are available in the EC2 instance Launch Wizard. So I'll start by clicking the launch instances button. The first option we'll talk about is auto-assigned public IP. You will find this under the network setting section. Click the edit button and here you have the option to auto-assigned a public IP address to your EC2 instance. You can set this to enable or disable. A public IP address is required to connect to your EC2 instance from the internet. The default value for this option depends on your subnet configuration. So if you'd like to change the default value, you will need to reconfigure your subnet. You can override this value at the time of launching an EC2 instance. Now let's talk about some more advanced options available in the advanced details section. These configuration options are not mandatory meaning you do not need to configure this to launch your instance. The first option is request spot instances. Spot instances are a purchasing option that allow you to request unused EC2 capacity for a cheaper price, so essentially, spot instances or EC2 instances available at a discounted price. We'll talk more about spot instances and how you can request them in a later video in this course. The next option is domain join directory. This option allows you to join your EC2 instance to a directory defined in the AWS directory service.
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-1?u=76281980&t=95)** The AWS directory service allows you to create a [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) [Active Directory](../../Skills/Network%20and%20System%20Administration/Active%20Directory.md) in the AWS cloud. The next option is IAM instance profile. This option allows you to attach an IAM instance profile, also known as an IAM role, to your EC2 instance. By default, the EC2 instance does not have permissions to access other AWS services. By attaching an instance profile, you can provide permissions to the instance to access other services. You can attach an instance profile at the time of launching an instance and you can also do this after the instance has been launched. We'll talk more about this in an upcoming video. The next option is host name type. This setting determines the type of host name that will be assigned to your EC2 instance. Let's talk more about this. The host name type determines if your EC2 instance will have IP name or resource name assigned as the host name. So host names can be of two types, IP name and resource name. Let's talk about IP name. When you set the host name type as IP name, the private IPv4 address of the instance is included in its host name. Here is the format in the us-east-1 region. It is private-ipv4-address.ec2.internal, and here is an example. Ip-private IPv4 address.ec2.internal. In all other AWS regions, it has a different format.
>
> **[3:10](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-1?u=76281980&t=190)** The format is private-ipv4-address.region.compute.internal. For example, IP dash, the private IPv4 address.eu-west-one.compute.internal. When you set the host name type as resource name, the EC2 instance ID is included in the host name. The format in us-east-1 region is ec2-instance-id.ec2.internal. For example, the EC2 instance ID, it has a different format in all other AWS regions. It is ec2-instance-id.region.compute.internal. For example, the EC2 instance-id.eu-west1.comput.internal. The host name type you can select depends on your subnet configuration. For example, if your subnet is IPv4 only or if it is a dual stack subnet, which means it supports both IPv4 four and IPv6, the host name type can be either IP name or resource name. However, if your subnet is configured as IPv6 only, the host name will be resource name. Let's look at this from the EC2 console. Back over here, I'll click cancel and select the first EC2 instance. This EC2 instance has the host name type set to IP name, so the host name is IP dash, the private IP address, dot region, which is us-east-2,
>
> **[4:46](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-1?u=76281980&t=286)** dot compute, dot internal. The second EC2 instance has the host name type set to resource name, so it is instance ID, followed by the region, dot compute, dot internal. Now let's see how the subnet configuration influences your host name type. I'll click launch instances again, scroll down to network settings and click edit. From here, I'll select a VPC, and then I'll select an IPv4 only subnet. This subnet here is IPv4 only. With this subnet selected, when I scroll down, I can configure the host name type as IP name or resource name. Now I'll change the subnet type to IPv6 only. With an IPv6 only subnet, the host name is set to resource name. So to summarize, the host name type can be IP name or resource name. For IPv4 only or dual stack subnets, you can choose from either of the two. For IPv6 only subnets, it is set to resource name. The next option is DNS host name. This setting determines whether your EC2 instances host name will result to an IPv4 address, an IPv6 address or both. And this, again, depends on your subnet configuration. The first option allows DNS request
>
> **[6:22](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-1?u=76281980&t=382)** for the IP name to resolve to the private IPv4 address. The second option allows DNS request for the resource name to resolve to the private IPv4 address. And the third option allows DNS request for the resource name to resolve to the IPv6 address. Right now I have an IPv6 subnet selected, so I'm able to use the third option. If I change the subnet to an IPv4 only subnet, I'll be able to use the first two options. So the DNS host name setting determines whether your EC2 instances host name will resolve to an IPv4 address, an IPv6 address, or both. The next option is instance auto recovery. With this option, your EC2 instance will be automatically recovered if it fails any of the system status checks. An instance becomes unreachable when the underlying physical [Hardware](../../Topics/Hardware.md) fails, or if there is a problem that requires AWS involvement to repair. In such situations, the instance is automatically recovered. The next option is shut down behavior. This setting determines what happens to the EC2 instance when you issue a shutdown command from within the operating system. Will the EC2 instance stop or terminate when you shut down the instance from within the operating system? For example, when you click the start menu in a [Windows](../../Glossary/Service/Windows.md) computer and issue the shutdown command. The next option is about hibernate behavior.
>
> **[7:56](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-1?u=76281980&t=476)** Hibernation causes the state of the EC2 instance to be saved. So when you restart the instance, it is restored to its previous state. This is different from stopping and starting your EC2 instance, which causes the instance to lose its state. With hibernation enabled, your instance will restore to the previous state. An important thing to bear in mind is that the hibernation setting can only be configured at instance launch time. If this option is not enabled here, you will not be able to hibernate your EC2 instance. The next two options are similar, termination protection and stop protection. When termination protection is enabled, the instance cannot be terminated unless protection is disabled. As you can imagine, this option can be very useful for mission critical instances. With stop protection enabled, the instance cannot be stopped unless the protection is disabled. The next option is detailed CloudWatch monitoring. By default, basic monitoring is automatically enabled and it collects usage metrics at five minute intervals. With detailed monitoring, metrics are collected at one minute intervals and there is an additional charge to this. The next option is Elastic GPU. This is related to Amazon Elastic Graphics. Amazon Elastic Graphics provides low-cost graphics acceleration for Windows instances. AWS does have a separate family of instances for graphic intensive workloads,
>
> **[9:29](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-1?u=76281980&t=569)** but they can be quite expensive. Instead, for workloads that only require a small amount of additional graphics acceleration, the Elastic GPU option is a good choice. This option only works with Windows AMIs. So if you try to use this option without a Windows AMI, you will see an error message. I'll set this back to select. The next option is Elastic Inference. This option allows you to add accelerators for [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) inference workloads and these are available in different sizes. You can select the type of accelerator and here, you can see the associated memory. There are some more options available in the screen that you can configure, and we'll talk about these in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (32), [Windows](../../Glossary/Service/Windows.md) (4), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1), [Active Directory](../../Skills/Network%20and%20System%20Administration/Active%20Directory.md) (1), [Hardware](../../Topics/Hardware.md) (1)
> **Env Vars:** ec2 (27), aws (8), dns (5), iam (3), gpu (2)
> **CLI Commands:** aws (8), find (1)
> **Definitions:** is a  (3), is an  (2), known as (1)
> **Analogies:** for example (5), imagine (1)
> **UI Navigation:** select the (2), scroll down (2)
> **Prerequisites:** configure (4)
> **Cross-References:** in the next (1)

#### [Advanced launch options: Part 2](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-2?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-2?u=76281980&t=0)** - [Instructor] Now let's talk about the remaining options available in the advanced details section. Starting with credit specification, the credit specification option only applies to T2, T3, T3a and T4g instances. If you try to launch any other instance type the credit specification option will not be available. So it only applies to these instance types and they're called as burstable performance instances. These instances belong to the general purpose instance family, and they're designed for workloads that do not consistently need high CPU performance. Examples include [Web Servers](../../Skills/Web%20Development/Web%20Servers.md) and small [Databases](../../Skills/Software%20Development/Databases.md) that have a fairly average utilization and do not consistently need high CPU. They're called burstable instances because they provide a baseline level of CPU performance with the ability to burst above the baseline if required. The baseline performance for burstable instances is described in the AWS documentation. Here we have the different instance types and here is their baseline utilization per vCPU, for example, for a t2 micro instance, the baseline performance is 10% per vCPU,. When the instance utilization stays below the baseline, it earns CPU credits per hour. These credits can be used to burst above the baseline if required like when the server experiences heavy utilization so it is CPU credits that determine
>
> **[1:34](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-2?u=76281980&t=94)** if burstable instances are allowed to burst above the baseline CPU level. These instances can be configured in one of two modes, standard and unlimited. When configured in the standard mode, if the instance bursts above the baseline CPU level, it first uses the accrued credits. When it runs out of accrued credits, it gradually comes down to the baseline CPU level and cannot burst above the baseline until it accrues more credits. As you can imagine, there could be situations where this is not ideal. The instance may need more CPU power, but being in the standard mode, it cannot burst above the baseline and for such use cases, you can use the unlimited mode. When configured in the unlimited mode, if the instance bursts above the baseline CPU level, it first uses the accrued credits. When it runs out of accrued credits, it uses surplus credits to burst. In other words, it borrows credits. When its performance falls below the baseline it uses the credits it earns to pay down the surplus it borrowed. This mechanism allows the [EC2](../../Skills/DevOps/Amazon%20EC2.md) service to average the CPU utilization over a 24 hour period. If the 24 hour average is above the baseline, additional charges are applied. By using the unlimited mode, you are assured that your EC2 instance will be able to burst above the baseline even if it runs out of CPU credits. If your application consistently requires high CPU,
>
> **[3:08](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-2?u=76281980&t=188)** it is recommended to use fixed performance instances instead of burstable instances. The next setting is placement group. This setting allows you to influence the placement of instances on the underlying [Hardware](../../Topics/Hardware.md). Normally, when you launch multiple EC2 instances, you are not telling AWS how they should be placed on the underlying hardware, but with placement groups you can tell AWS to place your instances close to each other in a single availability zone to achieve low latency or to spread your instances across the underlying hardware to reduce correlated failures. We'll talk more about this in an upcoming video. The next option is EBS optimized instance. Some instance types allow for dedicated capacity for EBS input output resulting in improved EBS performance. An important thing to be aware of is that EC2 instances and EBS volumes are hosted on distinct underlying hardware. This means they do not share the hardware and there is a connection between EC2 instances and the EBS volumes. Sometimes the connection can become a performance bottleneck. By using an EBS optimized instance, you can get dedicated throughput for your application but one thing to be aware of is that this option is only supported with certain instance types. The next option is capacity reservation. Capacity reservation, just like it sounds,
>
> **[4:41](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-2?u=76281980&t=281)** allows you to reserve EC2 capacity. Sometimes it is possible that AWS may not be able to launch your EC2 instance because there is no available capacity. You will normally not see this behavior when you're launching a few instances, but if you're launching quite a lot of instances, it is possible that AWS may not be able to fulfill your request. Using capacity reservation will ensure that you have access to EC2 capacity. We'll talk more about this in an upcoming video. The next option is tenancy. This option will determine if your instance will be launched on shared or dedicated hardware. There are two options available for dedicated tenancy. Dedicated instances and dedicated hosts. We'll explore the details and differences in an upcoming video. The next option is RAM disk ID, a RAM disk is a virtual disk created in the memory or the RAM of an EC2 instance. By configuring the RAM disk ID, you can specify the type and size of the RAM disk to be used for the instance. If you'd like to use a RAM disk to launch an EC2 instance, it can be specified here. The next option is kernel ID. If you'd like to specify a kernel ID for your EC2 instance, it can be provided here. The next option is Nitro Enclave. This feature allows you to create isolated execution environments called enclaves. Because they're isolated, they can be used
>
> **[6:15](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-2?u=76281980&t=375)** for handling sensitive data such as PII, also known as personally identifiable information such as financial and intellectual property data. This option is only supported with certain instance types. The next option is license configuration. AWS has a tool called License Manager that allows you to manage software licenses for [products](../../Skills/Software%20Development/Microsoft%20Products.md) such as [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) and Oracle. Customers use it to track and control license usage. The license manager configuration can be associated here to manage licenses for software running on your EC2 instance. The next few options relate to EC2 [Metadata](../../Skills/Web%20Development/Metadata.md). EC2 metadata is data about your EC2 instance. It includes information such as AMI ID, instance ID, hostname MAC address, IP address, security groups, and more. Essentially, it is data about your EC2 instance. The metadata accessible option can be used to enable or disable access to the metadata. The metadata is available in two versions, version one and version two, and you can control the version from here. The next option is metadata response hop limit, hop limit only applies if you use metadata version two. Version two uses a token based authentication and it uses session [Tokens](../../Skills/Web%20Development/Tokens.md) that determine how long a session can last. When creating a session token,
>
> **[7:47](https://www.linkedin.com/learning/amazon-ec2-essential-training/advanced-launch-options-part-2?u=76281980&t=467)** the hop limit, also known as TTL, or time to live is set to one by default. Certain use cases may need a higher hop limit value and it can be configured here. Don't worry if any of these doesn't make sense to you right now. We'll talk more about metadata in an upcoming video. The next option is allow tags in metadata. This option determines if your EC2 instance tags will be available in the metadata. The last setting here is user data. It allows you to run custom scripts when an EC2 instance is being launched. You can use it for reasons like installing software or starting a service when an instance is being launched. As you can see, there are so many options to configure your EC2 instance. Now, try some of these in your AWS account. For example, try to set termination protection or stop protection and try to terminate or stop your EC2 instance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (20), [Metadata](../../Skills/Web%20Development/Metadata.md) (10), [Hardware](../../Topics/Hardware.md) (6), [Web Servers](../../Skills/Web%20Development/Web%20Servers.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **Env Vars:** ec2 (20), cpu (13), aws (7), ebs (6), ram (6)
> **CLI Commands:** aws (7), make (1)
> **Analogies:** such as (4), for example (2), imagine (1), just like (1)
> **Definitions:** is a  (2), known as (2), in other words (1)
> **Documentation:** specification (3)
> **Code Identifiers:** vcpu (2)
> **Warnings:** be aware (2)

#### [EC2 user data](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-user-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-user-data?u=76281980&t=1)** - [Instructor] Now let's talk about [EC2](../../Skills/DevOps/Amazon%20EC2.md) user data. User data can be used to run scripts at instance startup. That is when the instance is booting up. It can be used to automate a variety of configuration tasks, and by default it is only executed when the instance is first launched. User data can be used to configure common tasks such as updating packages on the instance, installing a web server at launch time, installing an agent, backing up logs, and updating device configuration. When configuring user data on [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) AMIs, it can be in the form of shell scripts or cloud init directives. Shell scripts are run by the shell, which is the command line interpreter. Cloud Init is a package that allows you to initialize and configure cloud instances, such as EC2 instances. It is an open source package and it is also used to configure [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) of other [Cloud Computing](../../Topics/Cloud%20Computing.md) providers. Cloud Init is commonly preferred for complex configuration involving multiple tasks. An important thing to keep in mind is that user data scripts do not run interactively. So if your script requires user input like providing a yes or no choice it needs to be coded into the script because you cannot interact with the script when it is running on the instance. User data on Linux AMIs is executed as the root user so you do not need to include sudo in your commands. When configuring user data on [Windows](../../Glossary/Service/Windows.md) AMIs, it can be provided as batch commands
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-user-data?u=76281980&t=95)** or as [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) commands. And the user data must be enclosed within a tag. If you're using batch commands the user data must be enclosed in the script tag. And if you're using PowerShell commands the user data must be enclosed in the PowerShell tag. Now let's go to the EC2 console and configure user data. We're going to create two EC2 instances, a Windows and a Linux EC2 instance. And on both the instances we're going to use user data to install a web server. Let's start with the Windows instance. I'll start by providing a name. Next, select a Windows AMI. The exact Windows operating system type does not matter so feel free to select any Windows AMI. Next, select a required instance type. I'm selecting t2.medium which provides two VCPUs and four gigs of RAM. Next, associate a key pair and also associate a security group. Make sure the security group allows inbound http traffic so we can test the web service. Scroll down to the advanced details section and then scroll down to user data. I'm going to use a PowerShell script to install the web server. Because this is a PowerShell script, it is enclosed in the PowerShell tag. The first command Import-Module ServerManager imports the server manager module to manage server roles and features.
>
> **[3:10](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-user-data?u=76281980&t=190)** The next command Add-WindowsFeature Web-Server adds the web server feature to the EC2 instance which installs the necessary components to host a web server on the machine. The next command that is Start-Service, starts the Windows process activation service which is responsible for handling HTTP requests. The next command creates a file called index.[HTML](../../Skills/Web%20Development/HTML.md) in the required path. This is the default directory of the web server and the next command sets the content of the index.html file. As you can see we are using HTML tags such as HTML body and heading one. When the script runs it should install a simple webpage that says "Hello world". Next, I'll launch the EC2 instance. Now let's repeat the same process for a Linux EC2 instance. I'll click launch instances, give it a name. Next, select a Linux AMI. In this case, I'm using the Amazon Linux AMI. Select an instance type, t2.micro should be okay for the Linux instance. Select a key pair. Then select a security group that allows inbound HTTP access. Scroll down to the advanced detail section and then to user data. Here's the user data for the Linux EC2 instance. It starts with the pound symbol followed by an exclamation and the path to the interpreter.
>
> **[4:45](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-user-data?u=76281980&t=285)** The next command that is yum update will update the packages on the Linux instance. We learned earlier that you cannot interact with the script, so any user choices like a yes or no must be coded into the script like you can see here. The next command will install the httpd service on the Linux instance. The next command will write some text to a file called index.html in the route directory of the web server. And as you can see, again we are using HTML tags, HTML body, and heading one. And when the script runs it should install a simple webpage that says "Hello world". The next command will start the httpd service, and the last command chkconfig httpd on will ensure that the service automatically starts when the instance is rebooted. Lastly, I'll click the launch instance button. We'll go back to all instances, and let's copy the public IP address off the Windows instance to check if the web service is running. Copy the public IP address and try to access it from a new browser tab. And we can see the webpage that says "Hello world." Now let's try the same thing for the Linux EC2 instance. Back over here, select your Linux instance. And this time instead of copying the public IP address we'll copy the public DNS name. We should get the same result because the public DNS name should resolve to the public IP address. So copy the public DNS name,
>
> **[6:18](https://www.linkedin.com/learning/amazon-ec2-essential-training/ec2-user-data?u=76281980&t=378)** open a new browser tab, and try to access it. And straightaway we can see the same webpage. So as you can see without logging into the instance we have configured a web server. Back to the EC2 console. If you'd like to see the user data that was supplied to the instance, you can do so by selecting the instance, going to the actions button, instant settings and then click edit user data. And here is the user data we supplied. We cannot edit it because the instance needs to be stopped before you can do so. As you can imagine user data can be so useful to automate instance configuration quickly and in a scalable way. Now, take a moment to try this in your AWS account. Try to use the user data to install a web server on both Windows and Lenox EC2 instances.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (12), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (12), [Windows](../../Glossary/Service/Windows.md) (9), [HTML](../../Skills/Web%20Development/HTML.md) (7), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (6)
> **Env Vars:** ec2 (12), ami (4), html (4), dns (3), http (2)
> **Prerequisites:** install (6), configure (4)
> **Tools:** powershell (6), command line (1)
> **CLI Commands:** sudo (1), make (1), yum (1), aws (1)
> **UI Navigation:** scroll down (3), go to (1)
> **Analogies:** such as (3), imagine (1)
> **File Paths:** index.html (3)

#### [Connect to your EC2 instance](https://www.linkedin.com/learning/amazon-ec2-essential-training/connect-to-your-ec2-instance?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/connect-to-your-ec2-instance?u=76281980&t=0)** - [Instructor] Now let's understand how to connect to an [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance. We're going to connect to two types of EC2 instances: a [Windows](../../Glossary/Service/Windows.md) and a [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) EC2 instance. To follow along this video, you will need to launch two instances: a Windows and a Linux instance. And the Linux instance is running the Amazon Linux AMI. The way you connect to these instances will depend on the type of computer you're connecting from. That is from a Windows computer or from a Linux or Mac computer. First, let's understand how to connect to these from a Windows computer. I'll begin by selecting the Windows EC2 instance and I'll copy the public IP address. Next, I'll switch to a Windows computer. From the Windows computer, you will need to use the Remote Desktop Connection utility. So open the start menu and click Remote Desktop Connection. Paste your public IP address and click connect. The username and password required for this connection is available in the EC2 console. Back over here, to obtain the username and password, select your EC2 instance. Click connect, navigate to RDP client. Click get password, then click upload private key file and point your browser to the private key and then click decrypt password. Here's the password to log into the EC2 instances. Bear in mind, this is the default password. If you log in and change the password, you will need to connect with the new password.
>
> **[1:34](https://www.linkedin.com/learning/amazon-ec2-essential-training/connect-to-your-ec2-instance?u=76281980&t=94)** So here's the password and here's the username. Back to the Windows computer. I'll type the username and password and then press OK. When you try to connect, you will be presented with a certificate from the remote computer and you'll see a warning because the certificate is not issued by a trusted certifying authority. I'll click yes to accept the connection and I'm logged into the Windows EC2 instance. If you're unable to connect, it is likely that your security group does not allow the required inbound access. For Windows EC2 instances, the security group should allow inbound RDP access or Remote Desktop Protocol access. And for Linux EC2 instances, the security group should allow inbound SSH access. So to connect to a Windows EC2 instance from a Windows computer, you'll need to use the Remote Desktop Connection utility. Now let's understand how to connect to a Linux EC2 instance from a Windows computer. I'll begin by closing this connection. To connect to a Linux EC2 instance, you will need to use an SSH client like PuTTY. The private key file that you download from the AWS console is in the PEM format and it cannot be used with PuTTY. To use the private key file with PuTTY, it needs to be in the PPK format. Let's go back to the EC2 console. So back over here, I'll click cancel, and navigate to key pairs on the left navigation pane and click create key pair. And as you can see from here,
>
> **[3:08](https://www.linkedin.com/learning/amazon-ec2-essential-training/connect-to-your-ec2-instance?u=76281980&t=188)** the default download format is .pem. The PEM format cannot be used with PuTTY. To use the private key file with PuTTY, you will need to convert it to PPK. Let's see how to do that. Back over here to the Windows computer, to convert from PEM to PPK format, you will need to use PuTTYgen. PuTTYgen is automatically installed when you install PuTTY. So we'll open up PuTTYgen, click the load button and open your private key file in the PEM format. I'll set the visibility to all files and select the private key in the PEM format. The file has been imported. Click OK and then click save private key. You'll have the option to provide a password. For now, I'll proceed without a password. And now you can save the file in the PPK format. I'll set the visibility to all files. Click my PEM file so it loads the file name and just change the extension to PPK. Then click save, and the private key file is now saved in the PPK format. And we can verify this by going to the File Explorer. Here we have the PEM file and the PPK file. Now we can use this with PuTTY to log into the Linux EC2 instance. Back to the EC2 console, I'll click cancel, click instances on the left navigation pane, select the Linux instance and copy its public IP address. Back over here to the Windows computer, I'll open PuTTY.
>
> **[4:42](https://www.linkedin.com/learning/amazon-ec2-essential-training/connect-to-your-ec2-instance?u=76281980&t=282)** And under the host name or IP address field, I'll paste the public IP address. Next, we need to provide the private key file. So we'll navigate to SSH, Auth, Credentials. And here we can provide the private key file. So click browse, select the private key file in the PPK format. Click open and then click open again to initiate a connection. Now you'll need to provide the username of the Linux instance. For Amazon Linux AMI, the username is ec2-user. If you're connecting to any other Linux AMI, such as [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) or CentOS, the username is different, and you'll find this in the AWS documentation. So provide your username and press enter and you'll be logged into your EC2 instance. Once again, if you're unable to connect, it is likely that your security group does not allow inbound SSH access. Now let's understand how to connect from a Linux or Mac computer. Back over here, let's first connect to the Windows EC2 instance from a Mac computer. The steps remain the same if you try to connect from a Linux computer. Select your EC2 instance and copy the public IP address. To connect to a Windows EC2 instance from a Linux or a Mac computer, you will need a Remote Desktop Connection utility. Here I already have the utility installed, so I'll start by adding a PC and I'll provide the public IP address and add it. To initiate a connection, I'll double click to initiate a connection and now I'll need to provide the username and password.
>
> **[6:16](https://www.linkedin.com/learning/amazon-ec2-essential-training/connect-to-your-ec2-instance?u=76281980&t=376)** So we'll follow the same steps again. Back to the EC2 console. I'll click connect, RDP client, then click get password. Upload the private key file and click decrypt password. Here's the password, I'll copy this. Go back to my Mac computer and provide the username and password and click continue. You'll be presented with a warning that the certificate cannot be verified. I'll click continue and I'm logged into the EC2 instance. So to connect to a Windows EC2 instance from Linux or Mac computer, you need to use a Remote Desktop Connection utility. Now let's understand how to connect to a Linux EC2 instance. Back to the EC2 console. I'll click cancel from here. Select the Linux EC2 instance and copy its public IP address. And to connect to the Linux instance from a Mac computer or from a Linux computer, you'll need to use a terminal. Here I have a terminal. I'm going to use this to log into the Linux EC2 instance. The first step is to use the cd command and navigate to the folder where you have the key pair. In my case, it is in the downloads folder. Now you can use the SSH command to log into the Linux EC2 instance. So the command is ssh, followed by the username, which is ec2-user for Amazon Linux AMI at the public IP address we copied. And now we need to provide the private key file name. And to do that, we'll use the -i switch,
>
> **[7:51](https://www.linkedin.com/learning/amazon-ec2-essential-training/connect-to-your-ec2-instance?u=76281980&t=471)** and then the private key file name and press enter. So once again, the command is ssh ec2-user at the public IP address -i and the private key file name. Press enter. You'll see a message if you want to continue connecting. I'm going to say yes, and it presents an error message. It says the private key file is unprotected, and this is because the permissions on the file are too open. These permissions allow anyone to read the file, which is why the server rejects the connection. We'll need to change the permissions of the file and it's easy to do so. We'll use the command chmod 400 and then the file name and that should fix the problem. When you specify the permissions as 400, only the owner of the file has read permission. Other users have no permissions. So the first digit represents the permissions for the file owner. The second digit represents the permissions for the files group and the third digit represents the permission for everyone else. And four denotes read permission. So with 400, we're setting the permissions on the file as read only just for the owner. And now if we repeat the command, we should be able to log in. So ssh username at public IP address -i and the private key file name. And we're logged into the EC2 instance. You can also log into Linux EC2 instances directly from the EC2 console.
>
> **[9:23](https://www.linkedin.com/learning/amazon-ec2-essential-training/connect-to-your-ec2-instance?u=76281980&t=563)** Back over here, to log in from the EC2 console, select your Linux EC2 instance, click connect, and then go to EC2 Instance Connect. From here, you can enter the username, which is ec2-user, and click connect and you will be connected to your Linux EC2 instance directly from the browser. So as you've seen, the way you connect to an EC2 instance will depend on the type of computer you're connecting from. If you're trying to connect to a Windows computer, you will need to use Remote Desktop Connection utility. But if you're trying to connect to a Linux EC2 instance, you can use PuTTY on Windows or you can use a terminal program on a Linux or Mac computer. And for Linux instances, you can also log in directly from the EC2 console. Now take a moment to try this on your AWS account. Try to log into a Windows and a Ubuntu Linux EC2 instance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (43), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (31), [Windows](../../Glossary/Service/Windows.md) (21), [Ubuntu](../../Skills/Web%20Development/Ubuntu.md) (2)
> **Env Vars:** ec2 (39), ppk (8), pem (7), ssh (5), ami (4)
> **CLI Commands:** ssh (8), aws (3), find (1), cd (1), chmod (1)
> **UI Navigation:** navigate to (4), select the (4), switch to (1), open the (1), go to (1)
> **Tools:** terminal (3), aws console (1)
> **Prerequisites:** you'll need (3), install (1)
> **Cross-References:** go back to (2)
> **Warnings:** warning (2)

#### [Navigating the EC2 console](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-console?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-console?u=76281980&t=0)** - [Instructor] Now let's look at some important information available on the [EC2](../../Skills/DevOps/Amazon%20EC2.md) console, and some of the actions you can perform from here. Once you're in the EC2 console, select a running EC2 instance and navigate to the Details tab. Here, you will find important information, such as the EC2 Instance ID, the public IPv4 address, private IPv4 address, the public and private IPv4 DNS name, the state of the EC2 instance, hostname of the instance, the instance type, in this case, t2.micro, the VPC and subnet IDs, the AMI ID that was used to launch the EC2 instance, the platform of the instance, and the status of stop protection and termination protection, the status of detailed monitoring, which in this case is disabled. Down here, we also have the launch time. So this instance has been running for about 15 minutes, and scrolling down, you will see some more information, such as the key pair associated with this EC2 instance, the credit specification, and the owner of the EC2 instance. Scrolling back to the top, under the Security tab, you will see the security groups associated with the EC2 instance. This instance has two security groups associated with it, and here, we can see the aggregated inbound and outbound rules. Under the Networking tab, you will see the network adapters associated with the instance. This EC2 instance only has a single adapter associated with it, and here, we can see the public and private IPv4 addresses.
>
> **[1:36](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-console?u=76281980&t=96)** Under the Storage section, you will see the volumes associated with the instance. This EC2 instance has two volumes associated with it. The primary volume or the root volume is eight gigs in size, and the additional volume is one gig in size. Under the status checks tab, you will find checks used by AWS to detect problems with the EC2 instance, and there are two types of checks, system status checks and instance status checks. System status checks are used by AWS to monitor the underlying system on which the EC2 instance runs. It detects problems with the underlying [Hardware](../../Topics/Hardware.md) that require AWS involvement to repair. Examples of these problems include loss of power, network connectivity, and software or hardware issues on the physical host on which the EC2 instance is running. Instance status checks monitor the software and network configuration of your individual EC2 instance. Examples of these problems include incorrect network configuration, exhausted memory, and corrupted file system. These problems must be addressed by the user, and these can be done by rebooting the EC2 instance, or by changing the configuration of the instance. These status checks are performed automatically by AWS every minute, and these are built into EC2, so these cannot be disabled or deleted. Under the monitoring tab, you will find graphs that monitor the usage of the EC2 instance in terms of CPU utilization, network usage, disk usage, and in case of burstable instances,
>
> **[3:11](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-console?u=76281980&t=191)** like t2 and t3 instances, you will also see graphs for CPU credit balance and CPU credit usage. At the top here, you do have the option to change the time period for the graphs, and you also have the option to enable detailed monitoring, which monitors your EC2 instances every one minute. By default, basic monitoring is turned on, which monitors your EC2 instances every five minutes. And then you have the Tags section, that shows the tags associated with the EC2 instance. In this case, I have two tags, a name tag, which can be seen here, and another tag for the owner of the EC2 instance. Now let's look at some of the options available at the top. The Connect button will provide information about how you can connect to the EC2 instance. So with the EC2 instance selected, click the Connect button, and under SSH client, you will see the command that you need to use to log in to the EC2 instance. In case of [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) instances, you can also log in directly from the browser using EC2 Instance Connect. We talked about this in a previous video. So you would provide your EC2 instance username and click Connect to connect from the browser. I'll cancel from here, select the EC2 instance, and the Instance state button can be used to change the state of the instance. For example, you can stop the instance, reboot the instance, and terminate the instance. The hibernate option is available only if you enable hibernation at the time of launching the EC2 instance.
>
> **[4:45](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-console?u=76281980&t=285)** Now let's look at some of the options available under the Actions button. So with the EC2 instance selected, click the Actions button. Over here, you have the same option, Connect. You also have the option to View details. These are the same details we saw earlier in this panel at the bottom. You also have the option to change the instance state, and under Instance settings, you can change the options you configured at the time of launching the EC2 instance. For example, you have the option to attach to an Auto Scaling Group. Auto Scaling is a feature of AWS that allows you to build scalable applications in the AWS cloud. If you'd like to know more about this, check the course titled EC2 Load Balancers in the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning Library. Some other options you can configure here include termination protection, stop protection, shutdown behavior, auto recovery behavior, in case of burstable instances, you can change the credit specification, and you also have options to edit user data, allow tags and [Metadata](../../Skills/Web%20Development/Metadata.md), and manage the tags of the EC2 instance. Some of these options are unavailable because they can only be used when the EC2 instance is stopped, and this option here, Change Nitro Enclaves, is only available with certain EC2 instance types. The next option here is Networking. This allows you to perform network related actions, like attaching or detaching network interfaces. You can also manage your IP addresses, and you also have the option to change source or destination check. Source or destination check is automatically turned on
>
> **[6:20](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-console?u=76281980&t=380)** for every EC2 instance, and it ensures that the instance is either the source or the destination of any traffic it receives. This is turned on by default. However, you will need to turn off source or destination check if your instance is running services such as NAT, or network address translation, or firewalls, because those type of instances must be able to send or receive traffic when the source or destination is not itself. So by default, source/destination check is automatically turned on. However, if you're running special services on your EC2 instance, such as NAT or firewall, you can turn it off by using this option. The next option is to manage security. From here, you can change the security groups associated with your EC2 instance, and you can also modify the IAM role, or the IAM policy, and you can also modify the IAM role associated with your EC2 instance. The next option is Image and templates. It allows you to create an image or an AMI from your EC2 instance. You can also create a template from the currently running EC2 instance, and you can launch more instances like the currently running EC2 instance. The last option is Monitor and troubleshoot, and here, you have an important option called Get system log. This option can be very useful for troubleshooting purposes. For example, if your EC2 instance isn't responding, and you need to see what happened on the EC2 instance, you can click Get system log to get a log for your EC2 instance.
>
> **[7:53](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-console?u=76281980&t=473)** As you can imagine, this information can be very useful for troubleshooting purposes. I'll cancel from here and go back to the EC2 console. I'll select the EC2 instance, click Actions, go to Monitor and troubleshoot, and the next option is Get Instance screenshot. This option again can be very useful for troubleshooting purposes, especially if your instance isn't responding and you would like to see the state of the EC2 instance. So this will take a screenshot of the EC2 instance. I'll cancel from here, and go to the Actions menu again. Under Monitor and troubleshoot, you have some more options, like being able to turn on detailed monitoring. If you have any alarms configured on your EC2 instance using the CloudWatch service, you can turn it on from here. If you'd like to get serial console access to the EC2 instance, that can be done from here, and you also have the option to replace the root volume of the EC2 instance. You can also use Fleet Manager. This is a feature of the AWS Systems Manager service that allows you to view the health and perform management tasks on a fleet of EC2 instances. So as you can see, there is a lot of useful information available in the EC2 console. In the next video, we'll explore the EC2 dashboard.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (55), [Hardware](../../Topics/Hardware.md) (2), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [Metadata](../../Skills/Web%20Development/Metadata.md) (1)
> **Env Vars:** ec2 (55), aws (7), cpu (3), iam (3), ami (2)
> **CLI Commands:** aws (7), find (3), ssh (1)
> **Analogies:** such as (4), for example (3), imagine (1)
> **UI Navigation:** select the (2), go to (2), navigate to (1)
> **Cross-References:** we talked about (1), previous video (1), go back to (1), in the next (1)
> **Definitions:** is a  (3)
> **Warnings:** troubleshoot (3)

#### [Navigating the EC2 dashboard](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-dashboard?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-dashboard?u=76281980&t=0)** - [Instructor] Now let's look at important information available on the [EC2](../../Skills/DevOps/Amazon%20EC2.md) dashboard. Once you're in the EC2 console, you can access the EC2 dashboard by clicking the link on the left navigation pane. Here you can see the count of important resource types, such as your running EC2 instances, dedicated hosts, EC2 instances having any status, such as running, stopped, and hibernated, load balancers, security groups, EBS volumes, auto-scaling groups, elastic IPs, key pairs, placement groups, and EBS snapshots. Down here, we have information about the service health in the currently selected region, so the service is operating normally. And down here, we can also see the availability zones. On the top right hand corner, you can see the account attributes, such as the supported platforms, which can be EC2 Classic or EC2 VPC, the default VPC in this region, and some other settings that you can enable at the region or account level. For example, settings for EBS encryption, availability zones and local zones, and EC2 serial console. This option here allows you to manage serial console access. Serial console is a method of accessing the device by using a serial cable and attaching it directly to the device. Because it does not require IP connectivity, since you're using a serial cable, it is commonly used for troubleshooting. The EC2 serial console option you see here is a virtual serial connection. You also have the option
>
> **[1:33](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-dashboard?u=76281980&t=93)** to change the default credit specification. This only applies to burstable EC2 instances, and you also have the option to change your participation in console experiments. Scrolling down, you have some links to documents and some additional information, such as forums, pricing and contact us. On the left hand pane is an important option called EC2 global view. This tool gives you a summarized view of your EC2 resources across all regions. So here under Region Explorer, I can see that I've got 17 enabled regions, 17 subnets in four regions, one EC2 instance in one region, the number of security groups, VPCs and volumes. As you can imagine, without this information, if you wanted to consolidate your resources, you would have to switch between regions. Down here, you also have the resource count per region. So for the different regions, you have the count of EC2 instances, VPCs, subnets, security groups, volumes, and auto-scaling groups. At the top here, you also have the option to perform a global search using the resource ID or tag or region name. And you can also download your resources in a CSV file. Back over here, the event section contains information about events, such as instance reboot, instance start or stop, and instance retirement. You also have the option to view tags
>
> **[3:05](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-dashboard?u=76281980&t=185)** used on your EC2 instances and other resources, and you also have the option to view limits for different types of resources. For example, you can see the name of the limit, the limit type, and the current limit applied on your AWS account. As an example, let's look at VPC limits. So I'll change the category to networking. And down here, we have the option to view the EC2 limit. So in my account, I have a limit of five VPCs in a region. If you'd like to increase your limit, you can click Request Limit Increase, and this will allow you to open a case with support to get the limit increased for that resource type. And you also have the option to calculate VCPU limit. This option allows you to identify the current limit for VCPUs on your AWS account. For example, let's say we have a workload that requires 24 VCPUs, and let's say we need to use T3 instance type for the workload. So we can add the instance type here. Let's say we would like to use T3 2X large. A T3 2X large instance type provides eight VCPUs, and we need three of these instances for 24 VCPUs. And we can see that we are currently in the allowed limit. This means the request can be fulfilled within the currently allowed limit. So as you can see, there is a lot of important information in the EC2 dashboard. Now take a moment to try this yourself. Play around with the different options available
>
> **[4:38](https://www.linkedin.com/learning/amazon-ec2-essential-training/navigating-the-ec2-dashboard?u=76281980&t=278)** in the EC2 dashboard, and also review the limits for different resource types in your AWS account.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (18)
> **Env Vars:** ec2 (18), ebs (3), vpc (3), aws (3), csv (1)
> **Analogies:** such as (5), for example (3), imagine (1)
> **Definitions:** is a  (3), is an  (1)
> **CLI Commands:** aws (3)
> **Documentation:** specification (1)
> **Speakers:** - [instructor] (1)


### 2. EC2 Deep Dive

[↑ Back to Table of Contents](#table-of-contents)

#### [EBS snapshots](https://www.linkedin.com/learning/amazon-ec2-essential-training/ebs-snapshots?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/ebs-snapshots?u=76281980&t=0)** - [Instructor] In this section of the course, we'll deep dive into some interesting concepts starting with EBS Snapshots. We spoke about EBS Snapshots in an earlier video about AMIs. Now, let's explore this in detail. EBS Snapshots are a mechanism to backup data on EBS volumes. EBS Snapshots are stored in a service called Amazon S3. Amazon S3, or Simple Storage Service, is a storage service from AWS. So when you create a snapshot from an EBS volume, that snapshot is stored in Amazon S3, EBS Snapshots can be restored back as EBS volumes. When you restore an EBS Snapshot as an EBS volume, the new volume begins as an exact replica of the original volume that was used to create the snapshot. Snapshots are incremental. This means when you take multiple snapshots of an EBS volume, only the blocks that have changed since the most recent snapshot are saved. Let's understand this in detail with the help of an AWS document. I'm here at an AWS document that explains incremental snapshots. Here we have a volume, which is 10 gigs in size, and we take the first snapshot of this volume known as snapshot A. Because this is the first snapshot, it will be a full snapshot of the EBS volume and it will be 10 gigs in size. Now, let's say after snapshot A was taken, four gigs of data have changed on the volume.
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/ebs-snapshots?u=76281980&t=95)** Now, when you take a snapshot known as snapshot B, it will only contain the data that has changed on the EBS volume. So snapshot B will only be four gigs in size. However, to restore snapshot B into an EBS volume, it will also need the remaining six gigs of data So for the remaining six gigs, it references snapshot A. So snapshot B contains the data that was changed, that is four gigs in size, and for the remaining six gigs it relies on snapshot A. As a result of this mechanism, the size of snapshot B is only four gigs. Now, let's say after snapshot B was taken, two gigs of data were added to the EBS volume. Now, when you take a snapshot that is snapshot C, it will only contain two gigs of data. However, to restore it back to the EBS volume we need the remaining data as well. So for the remaining four gigs, snapshot C references snapshot B, and for the remaining six gigs of data snapshot C references snapshot A. If each of these snapshots were full snapshots, the total storage required would be 10 plus 10 plus 12 gigs which is a total of 32 gigs. With incremental snapshots, the total storage required is 10 plus four plus two gigs, which is a total of 16 gigs. Incremental snapshots save time required to create the snapshot, and they also save storage costs. Snapshots only capture data
>
> **[3:09](https://www.linkedin.com/learning/amazon-ec2-essential-training/ebs-snapshots?u=76281980&t=189)** that has been written to the EBS volume at the time the snapshot was created. This means if there is any data that has been cached by applications or by the operating system, they will not be included in the snapshot. So AWS recommends detaching the EBS volume before taking the snapshot. If it is a root volume, it cannot be detached. So for root volumes, AWS recommends that the instance should be stopped before the snapshot is taken. Snapshots can be copied from one region to another allowing for data migration. Let's look at a use case for this. Let's say we have an EBS volume in US East one, and we want to create a copy of this volume in another regio, EU West one. EBS volumes cannot be copied from one region to another. So instead, we can create a snapshot of the EBS volume in the same region and then copy the snapshot to another region and then restore the snapshot back as an EBS volume. So snapshots can be copied from one region to another allowing you to move EBS volumes to different regions. Snapshots can also be encrypted and snapshots can also be shared with other AWS accounts and also publicly. Users that have access to your snapshot can use it to create their own EBS volumes and the original snapshots will be unaffected. In the next video, we'll put these concepts into practice on the [EC2](../../Skills/DevOps/Amazon%20EC2.md) console console.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (1)
> **Env Vars:** ebs (24), aws (6), ec2 (1)
> **CLI Commands:** aws (6)
> **Definitions:** is a  (4), known as (2)
> **Cross-References:** in the next (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)

#### [Demo: EBS snapshots](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-snapshots?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-snapshots?u=76281980&t=0)** - [Instructor] Now, let's understand how to configure snapshots. Once you've logged into the [EC2](../../Skills/DevOps/Amazon%20EC2.md) console, go to snapshots on the left navigation pane. By default, you will see snapshots owned by you, but you can also use the dropdown to view private and public snapshots. To create a snapshot, you will need an EBS volume. So navigate to the volume section and create two EBS volumes. Both the volumes have the same configuration in terms of type and size. The only difference is their encryption status. The first volume is not encrypted, while the second one is. Now, let's snapshot these. We'll start with the unencrypted volume. Select the volume, click the actions button, and click create snapshot. Here, you'll be able to provide a description for the snapshot. Notice that you're not allowed to change the encryption status. This means if your EBS volume was not encrypted, your snapshot is also not encrypted. Scroll down and click create snapshot. Now, let's repeat the same process for the encrypted EBS volume. So select the volume, click the actions button, and click create snapshot. Notice that the encryption status cannot be changed. The EBS volume was encrypted, so the snapshot is automatically encrypted. So this is an important thing to bear in mind. The encryption status of the snapshot depends on the encryption status of the volume. If the volume is encrypted, the snapshot is automatically encrypted.
>
> **[1:34](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-snapshots?u=76281980&t=94)** If the volume is not encrypted, the snapshot is also not encrypted. Scroll down and click create snapshot. The snapshots have been created. Now, let's take a look at them. Click snapshots on the left navigation pane, and here's the two snapshots we created. I'll assign them name tags based on their encryption status. So I'll select the snapshot, and down here we can see this one is encrypted. So I'll assign a name tag called Encrypted, and I'll call the other one as Unencrypted. In the last video, we understood that snapshots are stored in Amazon S3. Let's take a look at it. Use the services dropdown menu and navigate to the storage section and then go to S3. In the S3 console, you will see your buckets and your objects. But as you can see here, the snapshots are not visible. So this is another important thing to keep in mind that snapshots are not visible in the Amazon S3 console. Even though snapshots are stored in S3, they're not visible on the console. Now, let's go back to EC2. Back over here to snapshots. Now, let's understand how to restore a snapshot back to an EBS volume. We'll begin with the unencrypted snapshot. So select a snapshot, click actions, and then click create volume from snapshot. Notice that you have the option to change the volume type and size, so when you're restoring a snapshot back to a volume, you can change the type and the size.
>
> **[3:09](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-snapshots?u=76281980&t=189)** And if the snapshot was not encrypted, you can turn on encryption when you're restoring back to a volume. You also have the option to select the availability zone to which you want to restore, and then click create volume. Now, let's try the same thing for the encrypted snapshot. So, select the encrypted snapshot, click the actions button, and then click create volume from snapshot. We have the same options. We are allowed to change the volume type and size. We can also provide the desired availability zone, but notice, we cannot change the encryption status. So if the snapshot was encrypted, the restored volume will be automatically encrypted. However, you can change the key used for encryption. Scroll down and click create volume. Now, let's look at some other options available with snapshots. Select a snapshot, click the actions button, and here we have an option called create image from snapshot. This option can be used to create an AMI from a snapshot of a root device volume. The next option is copy snapshot. This option allows you to copy a snapshot to a different region. The next option is modify permissions. By default, a snapshot is private, which means only you are allowed to access it, but you can modify the permissions to share the snapshot with other AWS accounts and also publicly. Bear in mind, only unencrypted snapshots can be shared. For example, if I select the encrypted snapshot, you will notice that the modify permissions option
>
> **[4:43](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-snapshots?u=76281980&t=283)** is grayed out. The next option is manage fast snapshot restore. Normally, when a snapshot is restored to an EBS volume, there is latency when the volume is accessed for the first time. This is because the volume needs to be initialized. Normally this is not a problem, but it can be a concern for certain use cases such as virtual desktop infrastructure, or VDIs, and backup and restore scenarios. With FSR, or fast snapshot restore, enabled, the restored EBS volume is fully initialized at creation. This means there is no latency when trying to access its contents. To use this feature, you'll need to first enable it at the availability zone level. So click on manage fast snapshot restore, and select the availability zones in which you'd like to enable this feature, and click the enable button. Here, you will see a message telling you about the charges for this feature. So this feature is available at an additional cost. Once FSR is enabled at the availability zone level, when you restore a snapshot in that availability zone, it is automatically restored using fast snapshot restore. I'll cancel and close from here, and let's look at some more options. Select the EBS volume, click actions, and the next option is archive snapshot. You can archive a snapshot to move it into a low cost, long-term storage tier. We know that snapshots incur storage charges. By moving it to the archived tier,
>
> **[6:18](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-snapshots?u=76281980&t=378)** you can save on storage costs. This can be ideal for snapshots that do not need to be accessed frequently, like when you need to store snapshots for compliance reasons. To archive a snapshot, click the archive snapshot option, and then click archive snapshot. The archival status of a snapshot can be seen by selecting the snapshot and then going to the storage tier tab. For example, this snapshot has not been archived, so it is in the standard storage tier. Here, I have a snapshot that has been archived, and we can see that the storage tier is archived. Bear in mind, the minimum archive period is 90 days. So if you delete or permanently restore before the 90 days period, you are still billed for the full 90 days. Archived snapshots need to be restored to the standard tier before they can be used to create a volume. To restore an archived snapshot, you'll select the snapshot, click the actions button, and then select restore snapshot from archive. There are two types of restore, permanent and temporary. Permanent restore causes the snapshot to be restored from the archive to the standard tier, and the snapshot is deleted from the archive tier. Temporary restore causes the snapshot to be restored to the standard tier, and a copy remains in the archive tier. The other option available in the actions menu is delete snapshot. By default, when you delete a snapshot, it is deleted forever and cannot be recovered. As you can imagine, this can be a concern
>
> **[7:53](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-ebs-snapshots?u=76281980&t=473)** in certain situations like production environments. However, you can use the Recycle Bin to protect your critical snapshots from accidental deletion. With Recycle Bin, you can create retention rules for deleted snapshots and AMIs. Let's take a look at it. I'll click Recycle Bin, and here we can create a retention rule. To create a retention rule, you'll start by providing a name and a description. Down here, you can select the type of resources you'd like to protect. In our example, we are talking about EBS snapshots. You can apply this to all resources or resources that match a specific tag. we'll apply to all. And then you can select a retention period, which can be a minimum of one day and a maximum of 365 days. The rule type can be unlocked or locked, and then you can click create retention rule. By doing this, any deleted EBS snapshot will be stored in the Recycle Bin for the duration of the retention rule. Now, take a moment to configure EBS snapshots in your AWS account. Create a volume, and then try to snapshot it. Try to restore it and play around with the different options like changing the volume type and size. In the end, be sure to delete unnecessary volumes and snapshots to avoid storage charges.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (2)
> **Env Vars:** ebs (12), ec2 (2), aws (2), fsr (2), ami (1)
> **UI Navigation:** select the (10), scroll down (3), go to (2), dropdown (2), navigate to (1)
> **Analogies:** for example (2), such as (1), imagine (1)
> **Prerequisites:** configure (2), you'll need (1)
> **CLI Commands:** aws (2)
> **Cross-References:** in the last (1), go back to (1)
> **Definitions:** is an  (1)

#### [Instance store](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-store?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-store?u=76281980&t=0)** - [Instructor] In the previous videos we talked about EBS volumes and that they provide persistent storage for [EC2](../../Skills/DevOps/Amazon%20EC2.md) instances. Another type of volume that you can attach to your instances are instance store volumes. Instance store volumes provide temporary block-level storage for your EC2 instances. Unlike EBS volumes, instance store volumes are physically attached to the host computer. So when you launch an EC2 instance and attach an EBS volume, the instance and the volume are on distinct underlying [Hardware](../../Topics/Hardware.md). But with instance store volumes, the volumes are hosted on the same underlying physical host as that of the EC2 instance. And for this reason, instance store volumes provide high-speed, low-latency access to data. So while instance store volumes only provide temporary storage, the data can be accessed at a high-speed and with very low-latency. And this makes it ideal for short-lived data and storage such as buffers, caches, and other temporary content. Instance store volumes for an EC2 instance can only be specified at launch time. So if you want to use instance store volumes, you must include them when you're launching the EC2 instance. Instance store volumes cannot be detached from one instance and attached to another. The data in an instance store volume only persists during the lifetime of its associated instance. This means your data is lost if the instance
>
> **[1:34](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-store?u=76281980&t=94)** is stopped, terminated, or hibernated, and the data is also lost if there is a failure on the underlying disk drive. However, data is not lost if the instance reboots. Due to the temporary nature of the data on the instance store volume, it is not suitable for valuable, long-term data. An important thing to bear in mind is that not all EC2 instance types support instance store volumes. The number and size of available instance store volumes depends on the instance type. Let's talk more about this. I'm here at an AWS document that talks about instance store volumes. Scrolling down this document, you will see all the EC2 instance types that support instance store volumes. For example, an m1.xlarge instance type supports four instance store volumes, each of size 420 gigs. Let's take a look at this from the EC2 console. From the EC2 console, click the Launch instance button, then select an instance type that supports instance store volumes. For example, I'm going to select m1.xlarge. Scrolling down to the storage section we can see the instance store volumes. So this instance type has a root volume of type EBS and it also supports up to four instance store volumes, each of size 420 gigs. Notice that the storage type is ephemeral.
>
> **[3:08](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-store?u=76281980&t=188)** So instance store volumes are labeled as ephemeral0, ephemeral1, and so on. And these volumes can only be attached at launch time. Back over here, now let's talk about the differences between EBS volumes and instance store volumes. EBS volumes provide persistent storage, while instance store volumes provide temporary storage. EBS volumes are located on disks that are on a separate storage system, while instance store volumes are located on disks that are physically attached to the host computer. EBS volumes can persist independently from the EC2 instance, while instance store volumes can only persist during the lifetime of the associated instance. EBS volumes can be attached to an instance at launch time or afterwards. But instance store volumes can only be attached at the time of launching the instance. EBS volumes can be detached from one instance and attached to another, while instance store volumes cannot be detached from an instance and attached to another. And lastly, EBS volumes are ideal for long-term, durable storage, while instance store volumes are ideal for short-lived data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (11), [Hardware](../../Topics/Hardware.md) (1)
> **Env Vars:** ebs (11), ec2 (11), aws (1)
> **Analogies:** for example (2), such as (1)
> **CLI Commands:** aws (1)
> **Cross-References:** we talked about (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Elastic IP](https://www.linkedin.com/learning/amazon-ec2-essential-training/elastic-ip?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/elastic-ip?u=76281980&t=0)** - [Instructor] Now let's talk about elastic IP addresses. We've seen in the previous videos that when you launch an [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance, it automatically gets assigned a public IP address. The public IP address of an instance changes when it is stopped or hibernated and started again. As you can imagine, this could be a problem if you have users or applications connecting to the public IP address, but this problem can be easily overcome by using an elastic IP address. An elastic IP address is a static IPv4 address reachable from the internet. The keyword here is static. So essentially an elastic IP address is a fixed IP address for your EC2 instances. It is associated with your AWS account and can be assigned to any EC2 instance in the region. An elastic IP address is used to ensure that the public IP address of the instance remains fixed. It can also be used to mask the failure of an instance or software by remapping the address to another instance. Here is an example. Let's say you have an EC2 instance running in the US East 1A availability zone. It has an elastic IP address assigned to it, which is used by users on the internet to connect to the instance. Now, let's say there is a failure in the availability zone and the instance needs to be redeployed in US East 1B. The same elastic IP address can be assigned to the new instance in the US East 1B availability zone,
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/elastic-ip?u=76281980&t=95)** and your users can continue to connect with the instance using the same elastic IP address. An important thing to bear in mind is that an hourly charge is imposed if an elastic IP is not associated to a running EC2 instance. This is because there is a global shortage of public IPv4 addresses. So it is important to make sure that elastic IP addresses are judiciously used, and so AWS supplies an hourly charge if the elastic IP is not associated to a running instance. Also, bear in mind that elastic IP addresses can only be used in the allocated region. Now let's go to the EC2 console and see how to configure an elastic IP. I'm here at the EC2 console and I have a running EC2 instance. As you can see, this EC2 instance has a public IPv4 address and it also has a public DNS name that contains the public IP address, and it does not have an elastic IP address. Now let's associate elastic IP address to this instance. So we'll navigate to elastic IPs on the left navigation pane, and we'll need to begin by allocating an IP to the account. So click allocate elastic IP address. We'll choose from Amazon's pool of IPv4 addresses and click allocate. Now an elastic IP address has been allocated to the account, but it is not associated with an instance. So let's do that.
>
> **[3:07](https://www.linkedin.com/learning/amazon-ec2-essential-training/elastic-ip?u=76281980&t=187)** Select your elastic IP address, click the actions button, and then click associate elastic IP address. There's two ways to do this. You can associate the elastic IP address to an instance or to a network interface. My EC2 instance only has a single interface, so I can use any of these options. But if your instance has multiple interfaces, you will need to use the second option that is network interface. So I'll select the instance option, then select the desired instance and the private IP address, and click associate. Now let's see the changes on the instance. So we'll go back to instances and refresh, and I'll select the instance again. And now we can see that the public IP address has changed to the elastic IP address. We can verify this by looking at elastic IP addresses here. Also, note that the DNS name has been updated to include the elastic IP address. Now let's go back to elastic IPs. Notice that there is also the option to update your reverse DNS settings. This option can be used if you're planning to use the elastic IP address to send emails. Reverse DNS configuration is commonly used to avoid emails from being flagged as spam. An elastic IP address can also be transferred to another account. Now let's see how to dissociate and release the elastic IP address. Before the IP can be released back to the pool, it must be dissociated from the instance.
>
> **[4:41](https://www.linkedin.com/learning/amazon-ec2-essential-training/elastic-ip?u=76281980&t=281)** So with the instance selected from the actions menu, select dissociate elastic IP address and then click dissociate again. Once the IP address has been dissociated, it can be released back to Amazon's pool. And to do this, select your elastic IP address, click the actions button, then click release elastic IP addresses, and click release again. Now take a moment to try this in your AWS account. Associate an elastic IP address to an instance, then try to dissociate the elastic IP and associate with another EC2 instance. When you're done, be sure to release the elastic IP address back to the pool to avoid unnecessary charges.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (11)
> **Env Vars:** ec2 (11), dns (4), aws (3)
> **UI Navigation:** select the (3), go to (1), navigate to (1)
> **Definitions:** is a  (4), is an  (1)
> **CLI Commands:** aws (3), make (1)
> **Cross-References:** go back to (2)
> **Analogies:** imagine (1)
> **Warnings:** note that (1)

#### [Elastic network interface](https://www.linkedin.com/learning/amazon-ec2-essential-training/elastic-network-interface?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/elastic-network-interface?u=76281980&t=0)** - [Instructor] Now let's talk about Elastic network interfaces. An Elastic network interface is a logical networking component that represents a virtual network card. When we use a physical computer, it has a physical network card that provides network connectivity. Similarly, [EC2](../../Skills/DevOps/Amazon%20EC2.md) instances have a virtual network card, known as Elastic network interface, and it provides network connectivity. Every instance in a VPC has a default network interface called as the primary interface, and it is represented as eth0. You can attach multiple network interfaces to the same EC2 instance. Let's look at a use case for this. Imagine a VPC with two subnets, a private and a public subnet in the same availability zone. You can have an EC2 instance with two interfaces in two different subnets, an interface in the private subnet and an interface in the public subnet. This configuration is commonly used to separate management traffic from production traffic. The interface in the public subnet has a private and a public IP, and it serves production traffic from the internet, while the interface in the private subnet only has a private IP and it serves management traffic from the corporate data center only. So by assigning multiple interfaces in different subnets, you can separate management traffic from production traffic. Here's another example. Let's say we have an application server with two interfaces, eth0 and eth1. Eth0 seves management traffic,
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/elastic-network-interface?u=76281980&t=95)** while eth1 serves production traffic. Let's say there is a failure on the server, and the application needs to be redeployed on another server. The production traffic can be redirected to App Server 2 simply by attaching eth1 one to the instance. So as you can see, it's easy to redirect traffic simply by reattaching the interface. Now let's talk about the attributes of Elastic network interfaces. Every Elastic network interface has a primary private IPv4 address. It can also have one or more secondary private IPv4 addresses. It can have one Elastic IP per private IPv4 address. It can have one public IPv4 address, which can be the same as the Elastic IP address. It can also have one or more IPv6 addresses. It can also be associated to one or more security groups. It also has a MAC address, which is commonly known as the physical address, and it cannot be changed. It can also have a source or destination check flag, and it is usually enabled to ensure that the instance is the source or destination of any traffic it receives. However, if you're running special services, such as NAT, or network address translation, [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md), or firewalls, source or destination check flag should be disabled on that interface. And finally, every interface also has a description associated with it. An Elastic network interface can be attached
>
> **[3:08](https://www.linkedin.com/learning/amazon-ec2-essential-training/elastic-network-interface?u=76281980&t=188)** to a running or a stopped EC2 instance. An important thing to bear in mind is that the interface and the instance must be in the same availability zone. And the primary interface, which is eth0, cannot be detached from the instance. You can detach secondary or additional interfaces, but the primary interface cannot be detached. Each network interface can have multiple private IPv4 addresses. The maximum private IPv4 addressed per interface and the maximum number of interfaces that can be attached to the instance depend on the instance type. Here is an AWS document that talks about the number of IP addresses per interface, per instance type. And scrolling down the document, you can see the different instance types, the maximum number of interfaces they support, and the number of private IPv4 addresses you can assign per interface. For example, on a t2.micro instance, you can attach two interfaces, and each interface can have two private IPv4 addresses. But look at some of these large instance types. For example, on a t2.xlarge instance, you can attach three interfaces, and each interface can have a maximum of 15 private IPv4 addresses. So that's a lot of private IP addresses that you can assign to the instance. In the next video, we'll understand how to configure Elastic network interfaces.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (4), [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) (1)
> **Env Vars:** ec2 (4), vpc (2), mac (1), nat (1), aws (1)
> **Definitions:** is a  (2), known as (1), is an  (1)
> **Analogies:** for example (2), imagine (1), such as (1)
> **CLI Commands:** aws (1)
> **Cross-References:** in the next (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [Demo: Elastic network interface](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-elastic-network-interface?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-elastic-network-interface?u=76281980&t=0)** - [Instructor] Now let's configure elastic network interfaces. Here in the [EC2](../../Skills/DevOps/Amazon%20EC2.md) console I've got two EC2 instances running, server one and server two, and they're running in different availability zones. Let's begin by reviewing the interface associated with server one. So I'll select server one and go to the networking tab, and down here I can see that it has an elastic network interface and here is the public and private IPv4 address. Now let's assign an additional private IP address to the same interface. To do this, we'll go to network interfaces on the left navigation pane, and here we can see the two interfaces belonging to the two instances, server one and server two. I'll begin by selecting the first interface that belongs to server one. Then click actions and manage IP addresses, expand the interface, and here we can see the existing private and public IP address. To assign an additional IP address, click assign new IP address. You can manually assign an IP address from that subnet range or you can leave it set to auto-assign, in which case AWS will automatically assign an IP address from that range. Scroll down and click save, and then confirm. And now the interface should have two private IP addresses. We can verify this by selecting the interface. And down here under the details tab we can see the primary IPv4 address, and we can also see the secondary private IPv4 address.
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-elastic-network-interface?u=76281980&t=95)** Let's also verify this on the instance. So back to instances, we'll refresh and select server one, and then go to networking and we should see the two private IP addresses, primary and secondary. Now let's create an additional interface and assign it to the instance. What we did now was assigning an additional IP address to the same interface, but now we'll create an additional interface and assign it to the instance. So we'll go back to network interfaces and we'll create a new interface. But before that, we should make a note of the availability zone in which the instance is running because the interface and the instance need to be in the same availability zone. So server one is running in US East 2B. I'll click create network interface, then we need to select a subnet. In the US East 2B availability zone I have two subnets, subnet one and subnet two. You can select any subnet, the important thing is the availability zone. So I'll select the first subnet in that availability zone, US East 2B. Next for private IPv4 address, we can auto-assign or we can manually assign the IP address. The next option is elastic fabric adapter. An elastic fabric adapter is a network device that you can attach to your EC2 instance to accelerate high performance computing, also known as HPC and machine learning applications. Scroll down and then associate
>
> **[3:07](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-elastic-network-interface?u=76281980&t=187)** the required security groups with your interface. For this, you can associate multiple security groups if required and then scroll down and click create network interface. So here's the interface we created. Now let's associate it with the EC2 instance. So select the instance, click the actions button, and then click attach. In the instance dropdown, you'll notice we only see one instance because that is the only instance running in the US East 2B availability zone. The other instance is in a different availability zone, so it does not show in the dropdown. We'll click attach. So the interface has been attached. Now let's check this from the instance. Let's go back to instances on the left navigation pane, and refresh, and select server one networking and scrolling down, we can see there are two interfaces attached to the same EC2 instance. We can also verify this by logging into the EC2 instance. Once you've logged into your EC2 instance, click the network button, and then click network and internet settings, and then click ethernet on the left navigation pane. And here you can see the two interfaces that we attached to the instance. Back over here, now let's attach an elastic IP address to the interface. Scrolling up, we can see that we already have a public IP address. This IP address is associated with the primary interface, (indistinct), and we can see that here.
>
> **[4:43](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-elastic-network-interface?u=76281980&t=283)** Now let's attach an elastic IP address to the second interface that we attached, and it has a private IP address ending with .189. So we'll begin by navigating to elastic IPs on the left navigation pane, and then we'll click allocate elastic IP address. We'll choose Amazon's pool of IPv4 addresses and click allocate. The elastic IPs allocated to the account, now we need to associate it with the instance. So with the elastic IP selected, click the actions button, then click associate elastic IP address. We're going to use the network interface option because the EC2 instance has multiple interfaces associated with it. So select the network interface option, then select the desired interface. This is the additional interface we created, and here is the private IP address ending with .189. Then click associate. The elastic IP has been associated. Now let's verify the changes from the instance. Back over here, I'll refresh and select server one. And now we should see two public IP addresses, the first one ending with .29 associated with the primary interface, and the second one ending with .250 associated with the secondary interface. And we can also verify this by going to the networking tab. And scrolling down, we can see the two interfaces and both the interfaces have a public and a private IP address. Now take a moment to try this in your AWS account.
>
> **[6:18](https://www.linkedin.com/learning/amazon-ec2-essential-training/demo-elastic-network-interface?u=76281980&t=378)** Try to associate multiple interfaces with the EC2 instance, and then assign multiple IP addresses to the interfaces. As a configuration scenario, try to attach two interfaces belonging to two different subnets to the same EC2 instance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (10)
> **Env Vars:** ec2 (10), aws (2), hpc (1)
> **UI Navigation:** select the (4), go to (3), scroll down (3), dropdown (2)
> **CLI Commands:** aws (2), make (1)
> **Cross-References:** go back to (2)
> **Definitions:** is a  (1), known as (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [Placement groups](https://www.linkedin.com/learning/amazon-ec2-essential-training/placement-groups?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/placement-groups?u=76281980&t=0)** - [Instructor] Now, let's talk about an interesting topic: placement groups. Imagine that you're launching multiple [EC2](../../Skills/DevOps/Amazon%20EC2.md) instances. How will AWS deploy these instances on the underlying [Hardware](../../Topics/Hardware.md)? Will all the instances share the same underlying hardware, or will the instances be spread across distinct underlying hardware? By default, when you're launching EC2 instances, you do not have control over how AWS deploys them on the underlying hardware, but you can influence this using placement groups. Placement groups allow you to influence the placement of instances on the underlying hardware. This can be useful for use cases, like achieving low latency or reducing the likelihood of failure. For example, you may want to pack your instances closely together to achieve low latency, or spread them across distinct underlying hardware to reduce the likelihood of failure. By default, the EC2 service attempts to place instances in a way that spreads them across the underlying hardware, but as you can imagine, this may not always be the ideal scenario, especially if you're trying to pack them together to achieve low latency. There are three types of placement groups: cluster, partition, and spread. A cluster placement group is a logical grouping of instances in a single availability zone. All instances in a cluster placement group have access to a high-throughput and a high-bandwidth segment of the network, and for this reason, a cluster placement group
>
> **[1:34](https://www.linkedin.com/learning/amazon-ec2-essential-training/placement-groups?u=76281980&t=94)** is ideal for low-latency and high-throughput applications. For example, high-performance computing or HPC applications that need to process data and perform calculations at high speeds. Cluster placement groups are also recommended when majority of the traffic is between instances in the group, because all instances in a cluster placement group are in a single availability zone. It is important to bear in mind that cluster placement groups are not supported for burstable performance instances, like T2 and T3 instances. Next is partition placement groups. With partition placement groups, instances are grouped into partitions, and each partition is hosted on a separate rack. Each rack has its own network and power source, and no two partitions in a placement group share the same rack. Because instances are hosted on distinct partitions, it reduces the likelihood of correlated hardware failures. It is important to bear in mind that partitions can be spread across multiple availability zones in the same region, and you can have a maximum of seven partitions per availability zone. Partition placement groups are ideal for deploying large distributed workloads, such as [Hadoop](../../Skills/Data%20Science/Hadoop.md), Cassandra, and Kafka, across distinct racks. Next is spread placement groups. With a spread placement group, each instance is placed on distinct underlying hardware. So, the difference between partition
>
> **[3:06](https://www.linkedin.com/learning/amazon-ec2-essential-training/placement-groups?u=76281980&t=186)** and spread placement groups is that with a partition placement group, a group of instances share the same partition, and no two partitions share the same rack, but with spread placement groups, each instance is on a different rack. Each rack has its own network and power source, and it reduces the risk of simultaneous failure, which would occur if instances share the same hardware. Spread placement group is ideal for critical applications where instances must be isolated from each other. Some other key points about placement groups, there is no charge for creating a placement group, placement groups cannot be merged, and an EC2 instance can only belong to one placement group. Now, let's go to the EC2 console and configure placement groups. Before you can launch an EC2 instance in a placement group, you must first configure it. To configure a placement group, navigate to Placement Groups on the left navigation pane. From here, click Create Placement Group. Here, we can see the three types we talked about: cluster, partition, and spread. Let's create a placement group of each type, beginning with cluster placement group. I'll call this "Cluster PG." Set the type as cluster, and click Create Group. I'll repeat the same thing for the remaining two types. Create Placement Group. Let's call this as "Spread Placement Group," and the type is spread.
>
> **[4:39](https://www.linkedin.com/learning/amazon-ec2-essential-training/placement-groups?u=76281980&t=279)** With a spread placement group, you can choose the type of spread, also known as the spread level. Normally, you will use rack. However, if you're using AWS Outposts, you can also set it to host. AWS Outposts allows you to bring AWS services to your on-premises environment or your data center. For this example, we'll set it to rack and click Create Group. And we'll repeat the same thing for the partition placement group. I'll call this as "Partition PG," and set the type to partition. When you're creating a partition placement group, you can define the number of partitions you'd like to create. I'm going to set it to four and click Create Group. Now that we have the placement groups, we can launch EC2 instances on these. Let's try to do this. I'll go to instances on the left navigation pane and click Launch Instances. I'll select an AMI. Then, an instance type. I'll also provide a key pair. I'll select an existing security group. Then, I'll scroll down to advanced details, where I can set the placement group. Here, we can see the three placement groups we created: cluster, partition, and spread. When you select the partition placement group, you can also select the partition on which you would like to launch the instance. For example, if I select the partition placement group, I can see all four partitions,
>
> **[6:12](https://www.linkedin.com/learning/amazon-ec2-essential-training/placement-groups?u=76281980&t=372)** and then, I can select the partition on which the instance should be launched. So, with a partition placement group, you do have visibility into the partition on which the instance will be launched. Once you've configured the required items, click Launch Instance, and then, click View All Instances. The placement group details can be viewed by selecting the instance. So, select your EC2 instance, and under the Details tab, you will find details about the placement group. For example, this EC2 instance is using a partition placement group. Here's the placement group ID and the partition number on which the instance has been launched. So, as we've seen, placement groups can be very useful in influencing how AWS deploys the instances on the underlying hardware.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Hardware](../../Topics/Hardware.md) (11), [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (9), [Hadoop](../../Skills/Data%20Science/Hadoop.md) (1)
> **Env Vars:** ec2 (9), aws (6), hpc (1), ami (1)
> **UI Navigation:** select the (4), go to (2), navigate to (1), scroll down (1)
> **CLI Commands:** aws (6), find (1)
> **Analogies:** for example (4), imagine (2), such as (1)
> **Prerequisites:** configure (3)
> **Definitions:** is a  (1), known as (1)
> **Cross-References:** we talked about (1)

#### [Instance metadata](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-metadata?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-metadata?u=76281980&t=0)** - [Instructor] Let's talk about instance [Metadata](../../Skills/Web%20Development/Metadata.md). Instance metadata is data about your [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance. It contains important attributes of your EC2 instance, such as the ami-id, hostname, instance-id, public-ipv4 address, security groups, and more. To retrieve instance metadata, you will need to access a specific IP address using the HTTP protocol. For IPv4, the URL is [http://169.254.169.254/latest/meta-data](http://169.254.169.254/latest/meta-data).
>
> **[0:38](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-metadata?u=76281980&t=38)** There is a similar URL for IPv6 as well. The IP address used by the metadata service is a special IP address known as the link local address, and it is only accessible by software running on the instance. Software or scripts running on your instance can access the metadata to get information such as hostname, public IP address, MAC address, and more. To access instance metadata, requests can be made in one of two versions. Version one, which is a request response method, and version two, which is a session oriented method. While version one is fully secure, version two adds additional protection in the form of session authentication. By default, you can use either version one or version two, or both. Here is the version one command. It uses the curl utility followed by the URL. Curl is a utility that allows you to interact with HTTP content using the command line. For version two, you will need to use two commands. The first command uses the curl utility to send an HTTP put request for a token. The -H option specifies a token with a duration of 21,600 seconds. When the token is returned, it is stored in a variable called TOKEN in uppercase. The next command passes the token with a specific header value to access the metadata. While both version one and version two are supported, it is possible to enforce the use of version two, and this is described in the AWS documentation.
>
> **[2:14](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-metadata?u=76281980&t=134)** Now let's go to the EC2 console, and see how to access metadata. I'm here at the command line interface of your [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) EC2 instance. You can also access the metadata from a [Windows](../../Glossary/Service/Windows.md) EC2 instance. First, let's try the version one command. I've already tried the command on this machine, so I'm going to repaint it by using the up arrow. The command as we know is curl followed by the URL. When I press enter, I can see all the attributes that I can access. For example, if I wanted to view the ami-id, I can add that keyword to the end of the command. Let's try that. Curl followed by the URL, and towards the end we'll add ami-id. And here is the ami-id of the EC2 instance. Let's try one more. I'll first clear my screen, use the up arrow, and let's try to access the host name. I'll add the hostname keyword to the end and press enter. And here we can see the host name of the EC2 instance. This command can also be used to access the user data. To access the user data, the keyword we need to use is user-data. So the command is curl http, followed by the IP address, /latest/user-data. And here we can see the user data that was supplied to the EC2 instance. If you did not supply any user data, you will see the 404 not found message. This command can also be used to access the identity document. The identity document contains important information,
>
> **[3:48](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-metadata?u=76281980&t=228)** such as the account id, availability zone, and more. To access the identity document, the command is curl, followed by the URL, /latest/dynamic/instance-identity/document.
>
> **[4:08](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-metadata?u=76281980&t=248)** And here we can see the instance-identity document. As you can see, it contains important information, such as the accountId, architecture, availabilityZone, instanceId, type, region, and more. Now let's try to access the instance metadata using version two. I'll first clear the screen. We know that for version two we need to use two commands. The first command is used to request a token, and save it in a variable. I've already tried the command, so I'll repaint it using the up arrow. And here is the command. We begin by setting the variable called token, and then we use the curl utility with the http PUT request for a token, and the TTL value is set to 21,600 seconds. When I press enter, a token is returned and stored in the variable, and the next command will use the token to access the metadata. Here is the second command. As you can see, it uses the token variable, and uses the same URL that we used earlier, [http://169.254.169.254/latest/meta-data](http://169.254.169.254/latest/meta-data). Press enter, and you will see the same attributes. Let's try to access a specific attribute. I'll first clear the screen, and towards the end of the command, let's add mac. This will show the MAC address of the EC2 instance. The information we are looking for is here. This is the MAC address of the EC2 instance. As you can see, this command adds a lot of information, like the IP address you're trying to access,
>
> **[5:43](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-metadata?u=76281980&t=343)** here's the token, and the TTL remaining on the token. If you'd like to hide this information, you can remove the -v switch from the command. Let's try that again. I'll use the up arrow, and remove the -v token, and try again. And straight away we can see the output, the MAC address of the EC2 instance. Now let's go to a Windows EC2 instance and try to access the metadata. From a Windows EC2 instance, you can use the [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) utility to access the instance metadata. I've already opened up PowerShell, and I've already tried the command, so I'll use the up arrow. It's the same command, curl followed by the URL. And when I press enter, I can see all the attributes that I can access. And this is described in the content section. Now let's try to access a specific attribute. I'll first clear the screen, and let's try to access the instance-id. So I'll add instance-id to the end. And under the content section, we can see the instance-id of the instance. You can also access the metadata from a browser. Let's try that. I have a browser open here. When you're trying from the browser, you do not need to use the curl utility. You can use the URL as it is. So http followed by the IP address, /latest/metadata. Press enter, and here's all the attributes. Let's try to access the public IP address of this instance.
>
> **[7:16](https://www.linkedin.com/learning/amazon-ec2-essential-training/instance-metadata?u=76281980&t=436)** So towards the end of the command, we can add public-ipv4, press enter, and here's the public IP address of the instance. So as you've seen, instance metadata can be used to access important attributes of your EC2 instance, such as the IP address, instance-id, security groups, MAC address, availability zone, region, and more.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Metadata](../../Skills/Web%20Development/Metadata.md) (16), [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (14), [Windows](../../Glossary/Service/Windows.md) (3), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (2), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1)
> **Env Vars:** ec2 (14), url (9), mac (5), http (3), ttl (2)
> **CLI Commands:** curl (10), aws (1)
> **Definitions:** is a  (5), known as (1)
> **Analogies:** such as (5), for example (1)
> **Tools:** command line (2), powershell (2)
> **Code Identifiers:** accountid (1), availabilityzone (1), instanceid (1)
> **URLs:** [http://169.254.169.254/latest/meta-data](http://169.254.169.254/latest/meta-data) (2)

#### [Attaching an IAM role](https://www.linkedin.com/learning/amazon-ec2-essential-training/attaching-an-iam-role?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/attaching-an-iam-role?u=76281980&t=0)** - [Instructor] Now let's talk about attaching an IAM role to an [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance. By default, your EC2 instances do not have access to other AWS services. Here is an example. Let's say you have an EC2 instance running in your AWS account, and you also have other AWS services in the same account. For example, you have an S3 bucket with objects in it and you also have an Amazon RDS database. By default, your EC2 instance will not be able to access these objects or these services even if they belong to the same AWS account. Let's verify this from the EC2 console. I'm here at the EC2 console. For this demonstration I'm going to use an Amazon [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) EC2 instance. This EC2 instance has the Amazon Linux AMI. And the reason I'm using this is because it has the AWS command-line tools pre-installed on it. You can use any AMI of your choice, including [Windows](../../Glossary/Service/Windows.md) AMIs, but you may need to install the AWS [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) on it. With Amazon Linux AMI the AWS CLI is pre-installed so I'm going to use this for the demo. Before we go to the terminal of this EC2 instance let's verify that we have a bucket in the S3 service. So I'll use the Services dropdown menu. I'll scroll down to the Storage section and then go to the S3 service. As you can see here I already have a bucket in the S3 service. Now let's try to access this bucket from the EC2 instance. I'm here at the terminal of the EC2 instance. And to access the S3 service
>
> **[1:34](https://www.linkedin.com/learning/amazon-ec2-essential-training/attaching-an-iam-role?u=76281980&t=94)** I'm going to use the command aws s3 ls. You start with the aws keyword, followed by the service, in this case S3, followed by the command you'd like to access, in this case ls, which is for listing the S3 buckets. If you'd like to see other AWS services you can interact with, you can use the command aws help. Scroll down and you will see all the services that you can interact with using the command-line interface. Press Q to exit. So let's use the command aws s3 ls. By the way, if you'd like to see other commands available with the S3 service, you can type in aws S3 help. Scroll down the page using the Space bar and you will be able to see the different commands supported for the S3 service. Q to exit. Now let's try the command aws s3 ls and we can see an error message here that says, "Unable to locate credentials." So the EC2 instance is not able to access the S3 bucket. Now let's understand how to fix this. Back over here, to grant permissions to the EC2 instance there are two options. You can add access keys to the EC2 instance or you can attach an IAM role. Let's talk about adding access keys to the EC2 instance. Access keys are credentials associated with a user account. So you can hardcode these access keys into the EC2 instance to provide permissions to the instance to access another service. But as you can imagine, hard coding access keys
>
> **[3:07](https://www.linkedin.com/learning/amazon-ec2-essential-training/attaching-an-iam-role?u=76281980&t=187)** into the EC2 instance is a [Security Risk](../../Skills/Cybersecurity/Security%20Risk.md) because anyone with access to the EC2 instance can use the keys to access another service. So it is not a recommended security best practice. Also, for better security, the keys must be rotated. Alternatively, to grant permissions to the EC2 instance, you can attach an IAM role. So you can attach IAM roles to EC2 instances to grant them permissions or access to other AWS services. Let's talk about IAM roles. An IAM role is an AWS identity with specific permissions associated to it. It is similar to an IAM user in that it is an identity with permissions. However, unlike an IAM user, which allows you to log into AWS, an IAM role is intended to be assumable. So in simple words, an IAM role is an object within AWS with specific permissions and other objects can assume the role to gain specific permissions. An IAM role does not have long-term credentials such as a password or access keys associated with it. Instead, when a role is assumed it provides temporary security credentials for that session. And this is transparent to the end user. The end user does not see the temporary security credentials that are generated. AWS handles this in the background. So to grant permissions to your EC2 instance you should prefer IAM roles over adding access keys. Going back to the earlier scenario, to grant access
>
> **[4:42](https://www.linkedin.com/learning/amazon-ec2-essential-training/attaching-an-iam-role?u=76281980&t=282)** to this EC2 instance, you can attach an IAM role with the necessary permissions. And when you do so the instance should be able to access the S3 bucket and the Amazon RDS database. Now let's go to the AWS console and see how to configure this. Back over here to the AWS console. To configure an IAM role, we'll need to go to the IAM service. So we'll use the Services dropdown menu and navigate to Security, Identity, and Compliance. And then go to the IAM service. From the IM console you can create both access keys and IAM roles. If you're logged in as the root user or the first user account of your AWS subscription you can create access keys from here by clicking My Security Credentials. Once you're here, scroll down, and from here you can create an access key. If you'd like to create an access key for an IAM user you can go to Users on the left navigation pane, click the required username, go to Security Credentials, and then create the required access keys from here. But we are going to use IAM roles. To create IAM roles click Roles on the left navigation pane and then create a role. The first step is to define the entity type that will be using the role. In this case, an AWS service, that is EC2, will be using the IAM role. Next, we need to provide a use case. In our scenario The EC2 instance wants access to other AWS services so we're going to use the EC2 use case. Click Next.
>
> **[6:18](https://www.linkedin.com/learning/amazon-ec2-essential-training/attaching-an-iam-role?u=76281980&t=378)** On this step, you will need to attach a permissions policy. A permissions policy is a document with permissions attached to it. There are predefined policies managed by AWS and you also have the ability to create a custom policy. For this demonstration we are going to use a predefined policy. So we'll search for S3, and the policy we are looking for is AmazonS3ReadOnlyAccess. We're only trying to list the S3 bucket from the EC2 instance so read-only access should be sufficient. If you'd like to see how the policy is defined click the plus symbol. As you can see this policy provides list permissions on all resources, so this policy should be enough to list the S3 buckets from the EC2 instance. So select the policy, scroll down and click Next. Provide a name for your IAM role. I'm going to call this as EC2S3ReadOnlyAccess. Scroll down and click Create Roll. Now the role has been created. The last step is to attach the role with the EC2 instance. So we'll go back to the EC2 service. Scroll to Instances on the left navigation pane. Select your EC2 instance, click the Actions button then go to Security and click Modify IAM role. From the dropdown select the IAM roll you just created called EC2S3ReadOnlyAccess, then click Update IAM role.
>
> **[7:56](https://www.linkedin.com/learning/amazon-ec2-essential-training/attaching-an-iam-role?u=76281980&t=476)** Now the EC2 instance should have the necessary permissions to list the S3 buckets. Let's verify this. Back over here to the EC2 terminal, let's try to use the same command again. aws s3 ls. And now we can see the S3 buckets in this AWS account. So as you can see the advantage of doing it this way is that access keys are not hard coded into the EC2 instance. By the way, if you did want to configure access keys the command to do this is aws configure. So let's say you had access keys and you wanted to add them to your EC2 instance, you would use the command aws configure, press Enter, and here you can type in your access key ID, your secret access key which is also available from the same console, the default region, and the default output format. And when you press Enter your access keys and your secret access keys, get hard coded into the EC2 instance. But as we now know, this is not a recommended security best practice. To grant permissions to the EC2 instance you should always attach an IAM role. Now, take a moment to try this in your AWS account. Try to access any other AWS service from your EC2 instance. As an example try to list the IAM roles in your AWS account.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (37), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (3), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2), [Windows](../../Glossary/Service/Windows.md) (1), [Security Risk](../../Skills/Cybersecurity/Security%20Risk.md) (1)
> **Env Vars:** ec2 (37), iam (27), aws (23), ami (3), rds (2)
> **CLI Commands:** aws (32), ls (5)
> **UI Navigation:** go to (8), scroll down (6), dropdown (3), select the (2), navigate to (1)
> **Definitions:** is an  (4), is a  (2)
> **Prerequisites:** configure (5), install (1)
> **Tools:** terminal (3), aws console (2)
> **Best Practices:** recommended (2), best practice (2), you should always (1)

#### [Launch templates](https://www.linkedin.com/learning/amazon-ec2-essential-training/launch-templates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/launch-templates?u=76281980&t=0)** - [Instructor] When you're launching an [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance, you are required to provide parameters or configuration information, such as the AMI ID, instance type, key pair, VPC and subnet information, security groups, storage information, and more. And you're required to provide this information each time you launch an EC2 instance. You can save some time by saving this information in a launch template. With launch templates, you can store EC2 launch parameters and use them to launch EC2 instances. A launch template contains configuration information required to launch an EC2 instance. It contains all the information you would configure from the EC2 instance launch wizard, such as the AMI ID, instance type, subnets, security groups, and more. Launch templates can be used to launch instances from the [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) console or the user interface, the AWS SDKs, or [Software Development](../../Topics/Software%20Development.md) kits, and the AWS [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md), or the Command Line Interface. Each template can have one or more numbered versions. When you create a new template, it is Version 1, and every time you edit the template, you are creating a numbered version. Each version can have different parameters, and any version can be set as the default. Each version can have different parameters, and any version can be set as the default. Here's an example. Let's say you create a new launch template. When you create a new template, it is Version 1. Version 1 of this template has information
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/launch-templates?u=76281980&t=95)** such as the AMI ID, the instance type, security group, and key pair. When you edit the template, it creates a new version. That is Version 2. In this case, the difference between Version 1 and Version 2 is that Version 2 has a different security group, and additionally, it also has the subnet configuration. Again, when you edit the template, you create a new version. That is Version 3 In this case, the difference between Version 2 and Version 3 is that Version 3 has a different instance type and subnet configuration. Version 3 has been configured as the default. Even though Version 3 is the default, you can still use Version 1 and Version 2 to launch EC2 instances. Launch templates are immutable, so when you edit a template, a new version is automatically created. A launch template can contain all or some of the parameters to launch an EC2 instance. Parameters contained in the template can be overwritten at launch time. For example, let's say you have a launch template that has the instance type configured as t2.micro. When you use the launch template to launch an EC2 instance, the instance type can be changed. For example, you can change it from t2.micro to t2.medium. Launch templates can also be used to launch instances with Amazon EC2 Auto Scaling. Auto Scaling is an AWS service that allows you to create highly available and fault tolerant applications in the AWS Cloud. If you'd like to learn more about Auto Scaling, check out the course titled EC2 Load Balancers
>
> **[3:09](https://www.linkedin.com/learning/amazon-ec2-essential-training/launch-templates?u=76281980&t=189)** in the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning Library. Now let's go to the EC2 console and configure a launch template. Once you're in the EC2 console, navigate to the Launch Templates section on the left navigation pan. From here, click Create launch template. You'll need to start by providing a name. I'm going to use this launch template to create [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) EC2 instances, so I'll call this template as LinuxServerTemplate. Next, you can provide a description. Check this box if you plan to use this template with an Auto Scaling group. Next, you have the option to tag the template. The tags you define here will be applied to the template itself and not to the EC2 instances launched with the template. If you'd like to tag the EC2 instances, there is a Configuration option towards the bottom, but the tags you define here will only apply to the template. Next is the Source template option. Use this option if you'd like to copy the settings from an existing template. Scrolling down, you will see the exact same settings that you see in the EC2 instance launch wizard. Starting with the AMI information. Bear in mind that all the settings you see here are optional, meaning you may or may not configure them. For this launch template, I'm going to use the Amazon Linux AMI, which is in the Quick Start section. If you'd like to select an AMI from the Marketplace or Community, click the option Browse more AMIs. Scrolling down, I'm going to select an instance type.
>
> **[4:45](https://www.linkedin.com/learning/amazon-ec2-essential-training/launch-templates?u=76281980&t=285)** I'll set this to t2.micro. I'll also provide a key pair name. Then I'll select an existing security group. Scrolling down, we have the Storage section. Here we can see the root volume, which is eight gigs in size, and this setting is inherited from the AMI you select. Notice, you also have the option to add additional volumes. Next, we have Resource tags. The tags you define here will be applied to the EC2 instances launched using this template. Let's add a tag. I've added a tag for the Owner. This tag will be applied to any EC2 instance we launch with this template. Scrolling down, we have the Advanced details section, which contains information such as the IAM instance profile, hostname type, shutdown behavior, hibernate behavior, termination protection, and more. These are the exact same options that we see in the EC2 instance launch wizard. Once you complete your configuration, click Create launch template. The template has been created, and we can see it here on the console. This template only has one version, and it is the default version. To create an EC2 instance using this template, you can select it, click the Actions button, and then click Launch instance from template. But before we do that, let's create another version of this template. So with the template selected, click the Actions button, and then click Modify template, which will create a new version.
>
> **[6:19](https://www.linkedin.com/learning/amazon-ec2-essential-training/launch-templates?u=76281980&t=379)** You will notice here that all the settings have been copied from the original template. We can make any changes over here. For this version, I'm going to change the instance type. I'm going to change this from t2.micro to t2.medium. I'll leave all of the settings the way they are and click Create template version. So a new version has been created, and we can see that here. This template has two versions, and the default version is Version 1. Let's change the default version. Select your template, click the Actions button, and then click Set default version. From here, select the version that you'd like to make default, and then click Set as default version. So now we have two versions of the template, and Version 2 is the default version. Now let's launch an EC2 instance using this template. With the template selected, click the Actions button and then click Launch instance from template. Doing so will invoke the EC2 instance launch wizard, and you will notice all the configuration items are automatically populated from the template. Here, you will see all versions of the template, and you can select any version to launch the EC2 instance. So we can see that the AMI is pre-selected. The instance type, key pair, security group, and all of the configuration items are automatically configured from the launch template. I'm going to click Launch instance, and the instance has been launched. As you can see, storing the information in a launch template simplifies
>
> **[7:53](https://www.linkedin.com/learning/amazon-ec2-essential-training/launch-templates?u=76281980&t=473)** the EC2 instance launch experience. We do not need to configure every parameter. Now let's see the EC2 instance we launched. I'll go back to the EC2 console, click Instances on the left navigation pain, and here is the EC2 instance we launched. Let's view the tags of this EC2 instance. When you create an EC2 instance using your launch template, it will automatically have two tags assigned to it. The first tag is the Template ID, and the second tag is the Template Version. Additionally, we can see one more tag because we added this tag through the launch template. So as you can see, launch templates can save a lot of time that would otherwise be needed to configure the EC2 instance. Now, try this in your AWS account. Create a launch template with the desired settings and then override some of these when you're launching the instance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (30), [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) (2), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (2), [Software Development](../../Topics/Software%20Development.md) (1), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (1)
> **Exercise Files:** template (54)
> **Env Vars:** ec2 (32), ami (8), aws (5), vpc (1), cli (1)
> **Versions:** version 1 (6), version 2 (6), version 3 (5)
> **Prerequisites:** configure (5), required to (3), you'll need (1)
> **CLI Commands:** aws (5), make (2)
> **Analogies:** such as (4), for example (2)
> **UI Navigation:** go to (1), navigate to (1), select the (1)

#### [Security groups as source or destination](https://www.linkedin.com/learning/amazon-ec2-essential-training/security-groups-as-source-or-destination?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/security-groups-as-source-or-destination?u=76281980&t=0)** - [Tutor] In an earlier video, we talked about security groups in that they are like a virtual firewall that control inbound and outbound traffic for [EC2](../../Skills/DevOps/Amazon%20EC2.md) instances. We also learned that we can configure IP addresses as the source or destination of security groups. Interestingly, a security group can also reference another security group as its source or destination. So we can not only configure an IP address as the source or destination, but we can also configure a security group as the source or destination for another security group. Let's understand this with an example. Let's say we have two servers in the US east-1 region, a web server, and a monitoring server. Both these servers are associated with different security groups. The web server needs to allow web traffic from the internet so the inbound rule of the web server security group is configured like this. It allows HTTP traffic on port 80 from all IP addresses. The monitoring server also needs to access the web server on ICMP to perform health checks. The IP address of the monitoring server is 10.1.1.1. To allow this access we can configure an inbound rule that looks like this. It allows ICMP traffic from the source IP address of 10.1.1.1, which is the monitoring server. While this configuration will work if the monitoring server's IP address changes or if you add additional monitoring servers you will need to update the inbound rule. So instead of configuring the inbound rule
>
> **[1:32](https://www.linkedin.com/learning/amazon-ec2-essential-training/security-groups-as-source-or-destination?u=76281980&t=92)** with the IP address of the monitoring server we can configure the security group associated with the monitoring server. So now the inbound rule allows ICMP traffic from the security group associated with the monitoring server. This means the rule will continue to allow access even if the IP address of the monitoring server changes or even if you add additional monitoring servers. A similar configuration can also be applied to outbound rules. Here is an example. Let's say the same web server also needs to send logs to a logging server at 10.1.1.5 on TCP 9999. The logging server is associated with a different security group. To allow this access we can configure an outbound rule that looks like this. It allows custom TCP traffic on TCP 9999 towards the destination 10.1.1.5. While this configuration will work if you change the IP address of the logging server or if you add additional logging servers you will need to update the outbound rule. So instead of configuring the outbound rule with the IP address of the logging server you can add the security group of the logging server as the destination. Now, the IP address of the logging server does not matter. You can change the IP address or you can add additional logging servers and the access should still work. Here's another example, which is a typical scenario in a production environment. A group of [Web Servers](../../Skills/Web%20Development/Web%20Servers.md) need to access a group of database servers on TCP 1433. To allow this access,
>
> **[3:07](https://www.linkedin.com/learning/amazon-ec2-essential-training/security-groups-as-source-or-destination?u=76281980&t=187)** the source of the database server security group inbound rules can be set as the security group associated with the web servers. This means if you add additional web servers in future they should continue to have access to the database servers. Now, let's go to the EC to console and configure this. Here is the configuration scenario we are going to use. We have a web server that needs to allow internet traffic. We have a web server that needs to allow web traffic from the internet, and it also needs to allow ICMP traffic from the monitoring server. Now, let's go to the EC2 console and configure this. I'm here at the EC2 console. I've already configured two servers, a web server and a monitoring server. Let's begin by reviewing the security groups associated with these servers. I'll select the web server first. Go to the security tab and here we can see that the security group associated with the web server is called WebServer SG, and it has two inbound rules. The first rule allows web traffic on port 80 from all IP addresses and the second rule allows ICMP traffic from all IP addresses. The second server, which is called as the monitoring server has a security group associated with it called as the Monitoring SG. So both the servers have been configured with different security groups. This security group allows SSH traffic from all IP addresses, so administrators can use this to log into the monitoring server. The current security group configuration of the web server allows ICMP traffic
>
> **[4:40](https://www.linkedin.com/learning/amazon-ec2-essential-training/security-groups-as-source-or-destination?u=76281980&t=280)** from all IP addresses. We need to update this rule to only allow ICMP traffic from the monitoring server. But before we do that, let's verify the access. I'll first copy the private IP address of the web server and try to ping it from the monitoring server. So here's the private IP address of the web server. I'll now switch to a terminal window. I'm here at a terminal window and I'm logged into the monitoring server. From here, I'll ping the private IP address of the web server. And as you can see, we are receiving responses. So the monitoring server is able to access the web server on ICMP to perform the required health checks. Let's also verify that the web server also allows ICMP from the internet. Back over here to the EC2 console, I'll copy the public IP address of the web server. Back to the terminal, I'll try to ping this from my local computer. This traffic will traverse the internet and we can see that the web server is responding to ICMP request from the internet. Now, we need to change this configuration. We need to modify the security group of the web server so it only allows ICMP traffic from the monitoring server, not from the internet. Back over here to the EC2 console, let's first navigate to security groups on the left navigation pane. Then I'll select the security group associated with the web server, which is called as WebServer SG. I'll navigate to inbound rules and then click edit Inbound rules. Here is the inbound rule that allows ICMP traffic
>
> **[6:14](https://www.linkedin.com/learning/amazon-ec2-essential-training/security-groups-as-source-or-destination?u=76281980&t=374)** from the internet. We need to change the source of this rule to the security group associated with the monitoring server. The existing rule cannot be updated so we have to add a new rule that allows all ICMP traffic from a custom source and will set the source to the security group of the monitoring server which is called MonitoringServer SG. So this rule will only allow ICMP traffic from the monitoring server, and now we can delete the other rule that allows ICMP from everywhere. Then I'll click save rules. Now the security group looks good. It allows HTTP traffic from all IP addresses and ICMP traffic only from the monitoring server. Now let's go to the terminal and verify this. Back over here, I'll first try to ping from the internet and we can see that pings are getting blocked. Now let's try to ping from the monitoring server. I'll switch to this tab here and try to ping the private IP address of the web server and we can see that pings are being allowed. With this configuration, in future, if we add any more monitoring servers they will also be able to ping the web server. As you can see, using a security group as a source or destination for another security group can simplify security group rules and reduce their complexity particularly in large deployments.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (5), [Web Servers](../../Skills/Web%20Development/Web%20Servers.md) (3)
> **Env Vars:** icmp (16), ec2 (5), tcp (4), http (2), ssh (1)
> **UI Navigation:** go to (4), select the (2), switch to (2), navigate to (2)
> **Prerequisites:** configure (8)
> **Definitions:** is called (4), is an  (1), is a  (1)
> **Versions:** 10.1.1 (4)
> **Tools:** terminal (4)
> **Ports:** port 80 (2)


### 3. EC2 Purchasing Options

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction to EC2 purchasing options](https://www.linkedin.com/learning/amazon-ec2-essential-training/introduction-to-ec2-purchasing-options?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/introduction-to-ec2-purchasing-options?u=76281980&t=0)** - [Narrator] Now let's shift gears and talk about [EC2](../../Skills/DevOps/Amazon%20EC2.md) purchasing options. When launching EC2 instances in your AWS account, you have the option to use different purchasing options. These purchasing options allow you to optimize costs and usage based on your needs. There are seven purchasing options in total, starting with on-demand instances, capacity reservations, spot instances, reserved instances, savings plans, dedicated instances, and dedicated hosts. Let's talk about some of the use cases for these purchasing options. On-demand EC2 instances allow you to get started with EC2 without any long-term commitments. With reserved instances, you can reserve capacity with upfront payment in exchange for a billing discount. Spot instances allow you to use spare EC2 capacity available at lower costs. And with dedicated instances and dedicated hosts, you can run your EC2 instances on dedicated [Hardware](../../Topics/Hardware.md). So as you can see, there are different use cases and scenarios, and EC2 purchasing options make these possible. In the following videos, we'll explore each of these purchasing options in detail.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (7), [Hardware](../../Topics/Hardware.md) (1)
> **Env Vars:** ec2 (7), aws (1)
> **CLI Commands:** aws (1), make (1)
> **Speakers:** - [narrator] (1)

#### [On-demand instances and capacity reservations](https://www.linkedin.com/learning/amazon-ec2-essential-training/on-demand-instances-and-capacity-reservations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/on-demand-instances-and-capacity-reservations?u=76281980&t=0)** - [Instructor] Let's explore the first two purchasing options, that is, on-demand instances and capacity reservations. On-demand instances are those that we've been launching all throughout this course. These are the instances you launch from the [EC2](../../Skills/DevOps/Amazon%20EC2.md) Console without any reservations or special pricing. With on-demand instances you pay by the hour or second without any long-term commitments. You have full control over the EC2 instance lifecycle. You can launch, stop, hibernate, reboot, or terminate the instance anytime. On-demand instances are ideal for short-term workloads that cannot be interrupted. With on-demand instances pricing is based on per instance hour. Partial instance-hour is built per second for [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) EC2 instances, [Windows](../../Glossary/Service/Windows.md) instances, Windows with [SQL](../../Skills/Data%20Science/SQL.md) Enterprise, Windows with SQL Standard, and Windows with SQL Web instances. For all other instance types the partial instance-hour is billed as a full hour. Now let's go to an AWS document that explains pricing for on-demand instances. Here is a document titled "[Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) On-Demand Pricing," and this document allows you to look up the prices for on-demand instances. You can do this by specifying the region, operating system, and instance type, and AWS will tell you the hourly price for that on-demand EC2 instance. Bear in mind this is not the only price you pay for EC2 instances. You also pay for data transfer, and it is described here.
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/on-demand-instances-and-capacity-reservations?u=76281980&t=95)** In addition to this, you also pay an hourly price for storage attached to the EC2 instance. While on-demand instances are a great choice for short-term workloads, their availability is not guaranteed. Imagine that you have an event coming up for which you need 100 EC2 instances for a short duration. When you try to launch those instances, it is not guaranteed that there will be available capacity to fulfill your request. For such situations, there is on-demand capacity reservations. On-demand capacity reservations allow you to reserve compute capacity and mitigate the risk of capacity constraints. Capacity is reserved in a specific availability zone for any duration. Capacity reservations are ideal for business-critical workloads that require capacity assurance, for example, if you need compute capacity for an upcoming event. Capacity reservations can be created anytime without requiring a long-term commitment. Billing starts when you provision capacity and stops when you cancel the reservation. You are charged at the equivalent on-demand rate whether you use the capacity or not. And other than that, there are no additional charges. To create a capacity reservation you must specify the availability zone in which to reserve capacity, the number of instances, and instance attributes such as the instance type, tenancy, and platform or operating system. Now let's go to the EC2 Console
>
> **[3:10](https://www.linkedin.com/learning/amazon-ec2-essential-training/on-demand-instances-and-capacity-reservations?u=76281980&t=190)** and see how to create a capacity reservation. From the EC2 Console click Capacity Reservations on the left navigation pane. Then click Create Capacity Reservation. First, you'll need to provide the instance type, the platform, which is the operating system, and then the availability zone. Next, you can provide the tenancy, which is shared or dedicated [Hardware](../../Topics/Hardware.md). If you'd like to put your instances in a placement group, you can define that here. Then you can define the number of instances required for this reservation. And down here you can define when the reservation ends. If you set this to manual, the capacity reservation ends when you manually end it. Or if you'd like it to end at a specific date and time, you can use this option and provide the end date and end time. The next option is instance eligibility. Instance eligibility defines which instances will count towards this reservation. If you set this to open, any instance matching the attributes you define above will count towards this reservation. If you set it to targeted, only instances launched with the reservation ID will count towards the reservation. So when you create the reservation, your reservation ID is generated, which you can specify at the time of launching the EC2 instance. When you finish the configuration, click Create at the bottom to create your capacity reservation. So just to recap, for short-lived workloads, on-demand instances are a great choice because you do not need to enter into any commitments.
>
> **[4:44](https://www.linkedin.com/learning/amazon-ec2-essential-training/on-demand-instances-and-capacity-reservations?u=76281980&t=284)** For short-lived workloads that require guaranteed capacity, on-demand capacity reservations are a good choice because it provides capacity assurance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (10), [Windows](../../Glossary/Service/Windows.md) (4), [SQL](../../Skills/Data%20Science/SQL.md) (3), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1), [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) (1)
> **Env Vars:** ec2 (11), sql (3), aws (2)
> **Analogies:** imagine (1), for example (1), such as (1)
> **CLI Commands:** aws (2)
> **UI Navigation:** go to (2)
> **Cross-References:** coming up (1)
> **Definitions:** is a  (1)
> **Prerequisites:** you'll need (1)

#### [Spot instances](https://www.linkedin.com/learning/amazon-ec2-essential-training/spot-instances?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/spot-instances?u=76281980&t=0)** - [Narrator] The next purchasing option is probably the most interesting one, Spot Instances. Spot Instances are unused [EC2](../../Skills/DevOps/Amazon%20EC2.md) instances available at less than the On-Demand price. Spot Instances are available at steep discounts, allowing you to lower your EC2 usage costs. In some cases, Spot Instances may be available at up to 90% discount compared to On-Demand Instances. Now let's understand how you can request a Spot Instance. AWS sets the hourly price of Spot Instances, and this is known as the Spot price. When you're requesting a Spot Instance, you're required to provide your maximum price per hour. If the maximum price you're willing to pay per hour exceeds the Spot price, the request is fulfilled. Here is an example. Let's say the Spot price for an EC2 instance is $0.80 per hour. This is set by AWS. The maximum price you're willing to pay is $0.65 per hour. In this case, the maximum price does not exceed the Spot price, and so the request will not be fulfilled, meaning the Spot Instance will not be allocated to you. In this case, the maximum price does not exceed the Spot price, so the request will not be fulfilled, meaning the Spot Instance will not be allocated to you. The Spot price changes based on demand and supply. Let's say the Spot price has changed to $0.60 per hour. Now the maximum price exceeds the Spot price, so the request will be fulfilled,
>
> **[1:32](https://www.linkedin.com/learning/amazon-ec2-essential-training/spot-instances?u=76281980&t=92)** and the Spot Instance will be allocated to you. Now let's say the Spot price has changed again, and it is now $0.75 per hour. Now your maximum price no longer exceeds the Spot price, so the Spot Instance will be interrupted, meaning the Spot Instance will either be terminated, stopped, or hibernated, depending on your configuration. An important thing to bear in mind is that with Spot Instances, you always pay the Spot price, which is set by [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md). Even though the request is only fulfilled when your maximum price exceeds the Spot price, you are charged the Spot price, and it is kept at the On-Demand rate. This means you will never pay for a Spot Instance more than the On-Demand rate. To request a Spot Instance, you start by creating a Spot Instance request. In the request, you'll need to include the maximum price you're willing to pay per hour, the desired number of instances, the instance type, and availability zone. When using Spot Instances, one thing to be aware of is interruptions. When Amazon EC2 reclaims a Spot Instance, it is known as a Spot Instance interruption. This can happen if AWS needs to repurpose capacity, that is, use the capacity of Spot Instances for other services, or if the Spot price is higher than your maximum price. In both these situations, the Spot Instance will be interrupted. When Amazon EC2 interrupts a Spot Instance, it either terminates, stops, or hibernates the instance,
>
> **[3:06](https://www.linkedin.com/learning/amazon-ec2-essential-training/spot-instances?u=76281980&t=186)** depending on the configuration. When using Spot Instances, your application should be prepared for an interruption. To help you prepare for interruptions, AWS provides two types of signals. The first one is EC2 instance rebalance recommendation, and the second one is Spot Instance interruption notice. The EC2 instance rebalance recommendation is a signal that notifies you when a Spot Instance is at elevated risk of interruption. A Spot Instance interruption notice is a warning that is issued two minutes before the Spot Instance is interrupted. So when you see the EC2 instance rebalance recommendation, it means there is a good chance that your Spot Instance will be interrupted. The rebalance recommendation allows you to move your workloads to other Spot Instances that are not at an elevated risk of interruption. When you see the Spot Instance interruption notice, you can be sure that your Spot Instance will be interrupted in the next two minutes. Upon interruption, Spot Instances are terminated, stopped, or hibernated, depending on the configuration. When set to stop or hibernate, Spot Instances will automatically resume when capacity is available. Rebalance recommendations and interruption notifications can be captured using two services, Amazon EventBridge and instance [Metadata](../../Skills/Web%20Development/Metadata.md). Amazon EventBridge allows you to capture events occurring in your AWS account and take actions on them. Here is an example of a rebalance recommendation captured using Amazon EventBridge.
>
> **[4:40](https://www.linkedin.com/learning/amazon-ec2-essential-training/spot-instances?u=76281980&t=280)** It contains the event ID and the detail type, which is set to EC2 Instance Rebalance Recommendation. Other details include source, account, time, region, and the affected instance ID. Here is an example of a Spot Instance interruption notice captured using Amazon EventBridge. It contains the ID, the detail type, which is set to EC2 Spot Instance Interruption Warning, source, account, time, region, the affected instance ID, and the instance action, which is terminate. The rebalance recommendation and interruption notice can also be captured using instance metadata. We use the same instance metadata URL that we discussed in an earlier video. The command is curl http followed by the IP address 169.254.169.254 /latest/meta-data/events /recommendations/rebalance. The output of this command is the time at which the rebalance recommendation was generated. Here is an example of the Spot Instance interruption notice captured using the instance metadata. The command is the same, curl http followed by the IP address /latest/meta-data/spot/instance-action. The output of this command is the action, which is terminate, and the time at which the action is scheduled to occur. When you're creating a Spot Instance request,
>
> **[6:14](https://www.linkedin.com/learning/amazon-ec2-essential-training/spot-instances?u=76281980&t=374)** it can be of one of two types. It can be a one-time request or a persistent request. With a one-time request, the request remains active until EC2 launches the Spot Instance or until the request expires or until you cancel the request. After the request is fulfilled, if capacity is no longer available, the Spot Instance is terminated, and the request is closed. With persistent requests, even after the request is fulfilled, the request remains active until it expires or you cancel it. If capacity is no longer available, the Spot Instance is interrupted. If the interruption behavior is set to be stopped or hibernated, it can be resumed. If the interruption behavior is set to be terminated, the Spot Instance request is reopened, and a new Spot Instance is launched. This is because the request type is persistent. Finally, let's talk about the differences between On-Demand and Spot Instances. On-Demand Instances can be launched immediately, while Spot Instances can be launched if capacity is available and if there is an active Spot request. With On-Demand Instances, the hourly price is fixed. With Spot Instances, the hourly price varies based on demand. On-Demand Instances are interrupted by the user, while Spot Instances can be interrupted by the service when capacity is no longer available or when the Spot price exceeds your maximum price On-Demand Instances are suitable for workloads
>
> **[7:48](https://www.linkedin.com/learning/amazon-ec2-essential-training/spot-instances?u=76281980&t=468)** that cannot be interrupted, such as servers and [Databases](../../Skills/Software%20Development/Databases.md), while Spot Instances are suitable for workloads that can be interrupted, like [Batch Processing](../../Skills/Data%20Science/Batch%20Processing.md) and data analytics. So as you've just seen, Spot Instances can provide cost savings if your application can handle interruptions. In the next video, we'll understand how to request a Spot Instance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (9), [Metadata](../../Skills/Web%20Development/Metadata.md) (4), [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) (3), [Databases](../../Skills/Software%20Development/Databases.md) (1), [Batch Processing](../../Skills/Data%20Science/Batch%20Processing.md) (1)
> **Env Vars:** ec2 (12), aws (5), url (1)
> **Definitions:** is an  (5), is a  (3), known as (2)
> **CLI Commands:** aws (5), curl (2)
> **Versions:** 0.80 (1), 0.65 (1), 0.60 (1), 0.75 (1), 169.254.169 (1)
> **Cross-References:** in the next (2), we discussed (1)
> **Warnings:** warning (2), be aware (1)
> **Prerequisites:** required to (1), you'll need (1)

#### [Requesting spot instances](https://www.linkedin.com/learning/amazon-ec2-essential-training/requesting-spot-instances?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/requesting-spot-instances?u=76281980&t=0)** - [Instructor] Now let's understand how to launch a spot instance. There are two ways to request a spot instance. The first option is to click the launch instances button here and then set the instance type to spot instance. The second option is to create a spot request by navigating to spot request on the left navigation pane. First, let's create a spot instance request from here. Begin by clicking the launch instances button. Configure the required items such as the EMI, instance type, key pair and more, and then scroll down to the advanced details section. From here, select the checkbox to request spot instances. Click customize to see the configuration options. The first option is to set the maximum price. This is the maximum price per hour for the spot instance. You can set this to a custom price, or leave it set to no maximum price in which case the maximum price is capped at the on-demand price. Bear in mind that even though you're configuring the maximum price, you will always pay the spot price for your spot instance. And the spot price is defined by AWS. The next option allows you to define the request type which can be a one-time request or a persistent request. Then you have the option to configure the validity of your spot instance request. Configure the desired settings, then click the launch instance button to launch your spot instance. Once the instance has been launched, you will see your spot instance here.
>
> **[1:33](https://www.linkedin.com/learning/amazon-ec2-essential-training/requesting-spot-instances?u=76281980&t=93)** As you can see, I've already launched a spot instance. I know that this is a spot instance because by selecting the instance and going to the detail section, I can see that the life cycle is set to spot. So this is a spot instance. We can also verify this by navigating to spot requests on the left navigation pane. And here is the spot request I created earlier with the associated instance ID. You can also request a spot instance from here by clicking request spot instances. On this screen you have more configuration options beginning with launch parameters. Launch parameters allow you to define the settings of your [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance. You can configure this manually or use a launch template. If you choose to configure this manually, you can define things like the AMI, the key pair, and down here you can define additional settings such as storage information, monitoring, tenancy and security groups. Bear in mind the launch parameters do not include the instance type. This is defined later in the configuration. The other option is to define the launch parameters using your launch template. Select the option, and from the dropdown select your launch template. If your launch template has multiple versions, select the desired version from here. The next option is additional request details. And by default the box to apply defaults is checked. Uncheck the box to see the configuration options.
>
> **[3:07](https://www.linkedin.com/learning/amazon-ec2-essential-training/requesting-spot-instances?u=76281980&t=187)** The first option is IAM fleet role. The IAM role is an AWS-defined role that provides the EC2 service that required permissions to launch and terminate spot instances on your behalf. Next you have the option to set the maximum price. You can leave it set to default in which case it will be kept at the on-demand price, or you can set your custom maximum price. The pricing history tool can be useful to see historical prices. The black line corresponds to the on-demand price, and these lines correspond to the spot prices. As you can see, the spot prices for this instance type are significantly lower than the on-demand price. Back over here, the next option allows you to configure the validity of your spot instance request. Then you have the option to terminate the instances when the request expires. You also have the option to register your spot instances behind a load balancer. The next section allows you to define your target capacity. This can be defined in terms of instances, vCPUs or memory. For this example I'll set the total target capacity as two. The next option allows you to include on-demand based capacity. With this option, you can allocate a portion of the total capacity as on-demand capacity. This strategy can be useful in ensuring that your workloads do not get interrupted. Even if your spot instance gets terminated,
>
> **[4:41](https://www.linkedin.com/learning/amazon-ec2-essential-training/requesting-spot-instances?u=76281980&t=281)** your on-demand instance will continue to run. Bear in mind this option is only available if you choose to use launch templates. I'll scroll down and set the on-demand capacity as one. So we are requesting a total of two instances. Of which one instance will be a spot instance and the other instance will be an on-demand instance. The next option allows you to maintain target capacity. With this option turned on, replacement instances will be launched if your spot instance is terminated due to change in pricing or available capacity. With this option turned on, you can define the interruption behavior. This can be set to terminate, stop or hibernate. The next option is capacity rebalance. We know that the EC2 instance rebalance recommendation signal is emitted when the spot instance is at an elevated risk of interruption. With this option turned on, AWS will attempt to replace such instances before they are interrupted. With this option turned on, you can define the replacement strategy. This can be set to launch only or launch before terminate. When set to launch only, AWS will launch replacement instances, but not terminate the ones that receive the rebalance recommendation signal. When said to launch before terminate, AWS will launch replacement instances and terminate the ones that receive the rebalance recommendation signal. The next option allows you to set
>
> **[6:13](https://www.linkedin.com/learning/amazon-ec2-essential-training/requesting-spot-instances?u=76281980&t=373)** the maximum cost for spot instances. This is the maximum cost for all of your spot instances. The option we saw earlier was the maximum price per spot instance. But this option allows you to set the maximum price for all your spot instances. Next you have the option to define the VPC and the availability zone. In the next section you can define the instance type requirements. You can set your instance type requirement by specifying instance attributes, or by manually selecting the desired instance types. When you select the first option to specify instance attributes, you can define the attributes in terms of vCPUs, memory, or you can select additional attributes such as CPU manufacturer, instance generation, storage and network information. Or you can manually select the instance types. When you select this option, AWS automatically suggests instance types that will result in a strong fleet strength. Fleet strength determines the likelihood of fulfilling your request. As you can see, AWS has suggested different instance types. And these selections result in a strong fleet strength. If you'd like to change the instance types, you can select the desired instance types and delete them, or add additional instance types. Based on your selection, the fleet strength will vary. In this case I have two instance types across three availability zones in this region resulting in a total of six instance pools.
>
> **[7:48](https://www.linkedin.com/learning/amazon-ec2-essential-training/requesting-spot-instances?u=76281980&t=468)** For each instance type you can see the configuration in terms of vCPU and memory, the spot price and savings on the on-demand price. The next section allows you to define the allocation strategy. Here you can define the allocation strategy for spot instances and for on-demand instances. To see all the available options, click show all strategies. The lowest price strategy will result in maximum cost savings, but comes at a high risk of interruption. Capacity optimized has the lowest risk of interruption, but is not the most price optimized. When you select diversified across all pools, your spot instances will be distributed evenly across all pools. Price capacity optimized provides a balance of price and interruption risk. Next we have the option to set the on-demand allocation strategy. This can be set to lowest price which means your on-demand instances will be selected from the lowest price pools. Or you can set to prioritized. Setting this to prioritized will cause instance types to be selected in the priority order. The priority order can be seen over here. To set the priority order, set the strategy to capacity optimized. Then select prioritize instance types. And then define your priority. Scroll to the bottom to see a summary of your selections. In this example we've requested two instances, of which one instance is a spot instance and the other one is an on-demand instance. Here we can see the instance requirements
>
> **[9:21](https://www.linkedin.com/learning/amazon-ec2-essential-training/requesting-spot-instances?u=76281980&t=561)** which have been provided as a launch template named LinuxServerTemplate. The fleet strength is weak because we have selected two instance types across three availability zones resulting in six matching instance pools. The estimated hourly price is $0.042 per hour. And this results in a 70% savings compared to on-demand prices. As you can see, you can be very granular with your spot instance configuration. When you've configured the required items, click the launch button to create your spot instance request. As you can see, you can be very granular with your spot instance configuration. By carefully planning your spot instances, you can have significant savings over on-demand instances.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (3)
> **Env Vars:** aws (7), ec2 (3), iam (2), emi (1), ami (1)
> **UI Navigation:** select the (6), scroll down (2), checkbox (1), dropdown (1)
> **CLI Commands:** aws (7)
> **Prerequisites:** configure (6)
> **Exercise Files:** template (5)
> **Definitions:** is a  (3), is an  (2)
> **Code Identifiers:** vcpus (2), vcpu (1)

#### [Reserved instances](https://www.linkedin.com/learning/amazon-ec2-essential-training/reserved-instances?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/reserved-instances?u=76281980&t=0)** - [Instructor] The next purchasing option is Reserved Instances. Reserved Instances provide significant discounts on [EC2](../../Skills/DevOps/Amazon%20EC2.md) usage when you commit to a one year or three year term. Reserved Instances are not a specific instance type, but rather a billing discount applied to the use of On-Demand instances. With Reserved Instances, you have a term commitment. Reserved Instances can be purchased for a tenure of one year or three years. When Reserved Instances expire at the end of the tenure, you go back to paying on-demand rates. With Reserved Instances, you have three payment options, all upfront, partial upfront, and no upfront. With all upfront, full payment is made at the start of the term with no other costs or additional hourly charges incurred for the remainder of the term regardless of the hours used. With partial upfront, a portion of the cost must be paid upfront and the remaining hours in the term are billed at a discounted hourly rate, regardless of whether the Reserved Instance is being used. As the name suggests, with no upfront, you are not required to make any upfront payment. You are billed a discounted hourly rate for every hour within the term, regardless of whether the Reserved Instance is being used. Generally speaking, you can save more money by making a higher upfront payment for Reserved Instances. The following attributes determine the price of a Reserved Instance. The instance type, region, tenancy and platform. Reserved Instances are available in two offering classes,
>
> **[1:39](https://www.linkedin.com/learning/amazon-ec2-essential-training/reserved-instances?u=76281980&t=99)** standard and convertible. Both standard and convertible offering classes allow certain attributes to be modified. However, if you like the flexibility to exchange instances, you should choose the convertible offering class. The standard offering class does not allow instances to be exchanged. Let's talk about modifying Reserved Instances. Modifying a Reserved Instance allows you to change certain configuration and continue to benefit from the billing discount. Both standard and convertible Reserved Instances can be modified and the following attributes are available for modification, availability zones within the same region, instance size within the same instance family and generation for [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) and Unix instances only, for example, you can change from t2.micro to t2.small instances but you can't change the family, so you can't change from T2 to T3 instance family, and you can also modify the scope. That is you can change from a zonal to a regional Reserved Instance and vice versa. We'll talk about the scope in a couple of minutes. The ability to change the instance type is governed by a normalization factor. Here is an AWS document that describes the normalization factor for different instance sizes. For example, the normalization factor for a t2.micro instance is 0.5 versus while the normalization factor for a t2.medium instance is two. So if you've purchased a t2.medium Reserved Instance, you can change that to four t2.micro Reserved Instances.
>
> **[3:15](https://www.linkedin.com/learning/amazon-ec2-essential-training/reserved-instances?u=76281980&t=195)** This is based on the normalization factor. Now let's talk about exchanging Reserved Instances. Only convertible Reserved Instances can be exchanged. Exchanging allows you to modify these attributes, instance family, operating system and tenancy. So when you're modifying the Reserved Instance, you can only change the instance size, but when you're exchanging, you can change the instance family. So if you like the flexibility to change the instance family, you should consider using convertible Reserved Instances. Now let's talk about the scope of Reserved Instances. This can be either regional or zonal. When you purchase a Reserved Instance for a region, it is known as a regional Reserved Instance. When you purchase a Reserved Instance for a specific availability zone, it is known as a zonal Reserved Instance. The scope does not affect the price of the Reserved Instance. Now let's talk about the differences between regional and zonal Reserved Instances. Regional Reserved Instances do not reserve capacity, while zonal Reserved Instances do reserve capacity in the specified availability zone. So if you'd like capacity reservation, you should opt for zonal Reserved Instances. With regional Reserved Instances, the discount applies to usage in any availability zone in the region. With zonal Reserved Instances, the discount only applies to usage in the specified availability zone. With regional Reserved Instances, the billing discount applies to any instance size within the family.
>
> **[4:49](https://www.linkedin.com/learning/amazon-ec2-essential-training/reserved-instances?u=76281980&t=289)** For zonal Reserved Instances, the billing discount applies to the usage for the specified instance type and size only. So unless you're looking for capacity reservation, you should choose regional Reserved Instances. Now, let's go to the EC2 console, and understand how to purchase a Reserved Instance. From the EC2 console, click Reserved Instances on the left navigation pane, then click Purchase Reserved Instances. From here, you can define the attributes you're interested in and then search for an available Reserved Instance. For example, you can define the platform, the tenancy, which is default for shared and dedicated for dedicated [Hardware](../../Topics/Hardware.md), the offering class, which can be convertible or standard, the instance type, the term, which can be one year or three years, and the payment option, which can be no upfront, partial upfront, or all upfront. After defining new attributes, click the Search button. Doing so will show you all Reserved Instances with the matching attributes in this region. Here you have the seller, which can be AWS or third party. Next you have the term, which can be 12 months or 36 months. Then you have the payment option, which could be all upfront, no upfront or partial upfront. Then you have the effective rate and the upfront price. For all upfront Reserved Instances, you are required to pay the full amount upfront, and for partial upfront, you'll only be paying a portion of the cost upfront. The next column is hourly rate.
>
> **[6:25](https://www.linkedin.com/learning/amazon-ec2-essential-training/reserved-instances?u=76281980&t=385)** For all upfront Reserved Instances, this is set to zero because you're paying the full amount upfront but you will see this for no upfront and partial upfront Reserved Instances. Next, you have the offering class, which can be standard or convertible. Then you have the available quantity, the desired quantity, and the normalization factor, which depends on the selected instance type. The Reserved Instances you're seeing here are regional Reserved Instances. If you'd like to see zonal Reserved Instances, you can toggle the switch here to only show offerings that reserve capacity, in other words, zonal Reserved Instances. And when you turn on this toggle, you will see a new column is added called Availability Zone. Now, when you select a Reserved Instance, it is going to be a zonal Reserved Instance. Set the desired quantity, add it to the cart, and complete the purchase to purchase a Reserved Instance. Reserved Instances are automatically applied to running On-Demand instances if the specifications match. If you have no running On-Demand instances that match the specifications of your Reserved Instance, the Reserved Instance is unused until you launch an instance with the required specifications. While Reserved Instances provide billing discount, you are bound by selections such as the instance family, region, and operating system. And for this reason, AWS recommends using Savings Plans over Reserved Instances to reduce compute costs. And that's the topic for the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (3), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1), [Hardware](../../Topics/Hardware.md) (1)
> **Env Vars:** ec2 (3), aws (3)
> **CLI Commands:** aws (3), make (1)
> **Definitions:** known as (2), is an  (1), in other words (1)
> **Analogies:** for example (3), such as (1)
> **UI Navigation:** toggle (2), go to (1)
> **Cross-References:** go back to (1), next video (1)
> **Prerequisites:** required to (2)

#### [Savings plans](https://www.linkedin.com/learning/amazon-ec2-essential-training/savings-plans?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/savings-plans?u=76281980&t=0)** - [Instructor] Now let's talk about savings plans. Savings plans is a flexible pricing model that offers low prices on [EC2](../../Skills/DevOps/Amazon%20EC2.md) usage in exchange for a commitment of one or three years. This sounds like reserved instances but here is the difference. Savings plans provide lower prices on EC2 instance usage regardless of the instance family, size, operating system, tenancy or the AWS region. As you can see, it is more flexible compared to reserved instances. With savings plans, there are three plan types: compute savings plans, EC2 instance savings plans and SageMaker savings plans. Let's talk about the first one. With compute savings plans, discounted pricing is automatically applied to your EC2 instance usage regardless of the instance family, region, operating system and tenancy. Here is an example. Let's say you are using the C5 instance family in the us-east-1 region. With compute savings plans, you can change the instance family and the region. As an example, you can switch to the M5 instance family in the eu-west-1 region. With EC2 instance savings plans, discounted pricing is automatically applied to your usage regardless of the instance size, operating system and tenancy. However, you must commit to a specific instance family in a chosen region. Here is an example. Let's say you are using a t3.large [Windows](../../Glossary/Service/Windows.md) instance in the us-east-1 region.
>
> **[1:35](https://www.linkedin.com/learning/amazon-ec2-essential-training/savings-plans?u=76281980&t=95)** With the EC2 instance savings plan, you can change the instance size and operating system but not the family and the region. So as an example, you could switch to a t3-medium [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) instance in the same region, that is us-east-1. To purchase a savings plan, you will need to enable Cost Explorer. Cost Explorer is a service that allows you to visualize and manage your AWS costs and usage. Once purchased, savings plans apply automatically to your eligible AWS usage. Here is a document that allows you to look up the prices for savings plans and here are the option we discussed, compute savings plans and EC2 instance savings plans. Here you can define the attributes, such as the region, term length, payment options, operating system and tenancy. And based on the attributes you define, the results are displayed in this table. Here is the instance name or the instance type, the on-demand rate for that instance type, the savings plans rate, the savings compared to on-demand rates, region, operating system and tenancy. Now let's go to the EC2 console and see how to purchase a savings plan. From the EC2 console, click savings plans on the left navigation pane then click purchase savings plans. Doing so will navigate you over to the [Cost Management](../../Skills/Software%20Development/Cost%20Management.md) service and from here, you can purchase a savings plan. Here are the three plan types we discussed:
>
> **[3:08](https://www.linkedin.com/learning/amazon-ec2-essential-training/savings-plans?u=76281980&t=188)** compute savings plans, EC2 instant savings plans and SageMaker savings plans. To purchase a savings plan, you'll first need to define the term, which can be one year or three years, then your hourly commitment in terms of USD then the payment option and down here, you can see the cost based on your selections, you can add this to the card and complete your purchase. If you choose the EC2 instance savings plan, you will need to define the region and the instance family as well. So here you can select the region, then the instance family, then the hourly commitment, payment option and then add to the card and complete the purchase to begin the savings plan usage.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (11), [Windows](../../Glossary/Service/Windows.md) (1), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1), [Cost Management](../../Skills/Software%20Development/Cost%20Management.md) (1)
> **Env Vars:** ec2 (11), aws (3), usd (1)
> **Definitions:** is a  (3), is an  (2)
> **CLI Commands:** aws (3)
> **UI Navigation:** switch to (1), go to (1), select the (1)
> **Cross-References:** we discussed (2)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Dedicated instances vs. dedicated hosts](https://www.linkedin.com/learning/amazon-ec2-essential-training/dedicated-instances-vs-dedicated-hosts?u=76281980)

#### [Launching dedicated instances and dedicated hosts](https://www.linkedin.com/learning/amazon-ec2-essential-training/launching-dedicated-instances-and-dedicated-hosts?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/launching-dedicated-instances-and-dedicated-hosts?u=76281980&t=0)** - [Narrator] Now, let's understand how to launch dedicated instances, and dedicated hosts. To launch a dedicated instance, you would begin just as you normally would by clicking the launch instances button. Configure the required settings such as the AMI, instance type, key pair, security groups and storage and then scroll down to the advanced details section. From here navigate to the tenancy option and set this to dedicated instance. Then click launch instance. The [EC2](../../Skills/DevOps/Amazon%20EC2.md) instance will be launched as a dedicated instance. Now, let's talk about launching instances on a dedicated host. To launch instances on a dedicated host, you will first need to allocate a host to the account. After allocating, the capacity is made available immediately and you can start launching instances on it. Navigate to the dedicated host section on the left navigation pane and then click allocate dedicated host. Begin by providing a name for the dedicated host. Then you have the option to define the instance family. The option you define here specifies the instance family that will be supported by the dedicated host. For example, if I set this to C4, I can use the dedicated host to launch C4 instances only. The next option is instance type. Here you can select a single instance type. For example, if I select C4 large, the dedicated host will only support C4 large instances. Most of the instance families you see
>
> **[1:34](https://www.linkedin.com/learning/amazon-ec2-essential-training/launching-dedicated-instances-and-dedicated-hosts?u=76281980&t=94)** in this list only support a single instance size or type. However, there are certain instance families that support multiple instance types. These include C5, M5, R5, C5n, M5n, and R5n. I'll select any one of these instance families, for example C5. When I select this instance family, I have the option to enable support for multiple instance types. So if I select the C5 instance family, I can launch multiple instance sizes belonging to the C5 family on the same dedicated host. The next option allows you to select an availability zone for your dedicated host. Next, you have the option to define the Outpost ARN, or the Outpost Identifier. AWS Outpost is a service that allows you to deploy AWS in your local on-premises environment, or your data center. The next option is about instance auto placement. Auto placement allows you to specify whether instances are launched onto a specific dedicated host, or any available dedicated host. When the option is enabled, that is auto placement is enabled, EC2 instances will be automatically placed on this dedicated host. When you disable this option, you must use the host ID of this dedicated host when you're launching an EC2 instance. Doing so will launch the EC2 instance on this dedicated host. The next option is about host recovery.
>
> **[3:07](https://www.linkedin.com/learning/amazon-ec2-essential-training/launching-dedicated-instances-and-dedicated-hosts?u=76281980&t=187)** If there is a problem with a dedicated host, AWS will automatically restart your EC2 instances on a new replacement dedicated host. This could be due to loss of network connectivity, system power, or [Hardware](../../Topics/Hardware.md) or software issues on the physical server. The next option is about host maintenance. When the performance of the dedicated host is degraded, a new dedicated host is allocated and your EC2 instances are scheduled to be automatically rebooted, and moved to the new dedicated host. The maintenance is scheduled to occur after 14 days and you have the option to reschedule. Host recovery is triggered when the dedicated host has failed and recovery is immediate. Typically, this means your dedicated host is unreachable. Host maintenance is triggered when the dedicated host has degraded performance and recovery is scheduled. Your dedicated host is still reachable. Next, you have the option to define the required quantity, tag your dedicated host and then click the allocate button. Doing so will allocate a dedicated host to your AWS account. You can then use the dedicated host to launch instances on it. To launch an instance on the dedicated host, click the instances section on the left navigation pane, click the launch instances button, then scroll down to the advanced details section, and then navigate to the tenancy option. From here, set this to dedicated host. When you set this to dedicated host,
>
> **[4:39](https://www.linkedin.com/learning/amazon-ec2-essential-training/launching-dedicated-instances-and-dedicated-hosts?u=76281980&t=279)** you have a few options that you can configure. The first option allows you to define how you will target the dedicated host. You can do this by host ID or by host resource group. If you set this to host ID, you can select the dedicated host on which you would like to launch the EC2 instance. You can also target by host resource group. Host resource group is a collection of dedicated hosts that you manage as a single entity. This allows you to group dedicated hosts by purpose. For example, you could have a group of dedicated hosts for the dev environment, and another for production. This is also commonly done to manage software licenses on dedicated hosts. You can create host resource groups from the AWS License Manager service. The next option allows you to define tenancy affinity. Tenancy affinity establishes a relationship between an EC2 instance and a dedicated host. When you set this to host, an EC2 instance launched on a specific dedicated host will always be restarted on the same host. When you set this to off, AWS tries on a best effort basis to launch the EC2 instance back onto the last dedicated host it ran on. Configure the remaining settings and then click the Launch Instance button to launch this EC2 instance on a dedicated host. So that is about dedicated instances, and dedicated hosts. Bear in mind that both these options have additional charges because your instances will use dedicated hardware. If you're only interested in having your instances
>
> **[6:14](https://www.linkedin.com/learning/amazon-ec2-essential-training/launching-dedicated-instances-and-dedicated-hosts?u=76281980&t=374)** on dedicated hardware, you should consider dedicated instances. But if you'd like visibility into the physical server on which your EC2 instances will be placed, and also the ability to bring your per core, per socket, and per virtual machine licenses, you should consider dedicated hosts.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (12), [Hardware](../../Topics/Hardware.md) (3)
> **Env Vars:** ec2 (12), aws (6), ami (1), arn (1)
> **UI Navigation:** navigate to (3), scroll down (2), select the (2)
> **CLI Commands:** aws (6)
> **Analogies:** for example (4), such as (1)
> **Definitions:** is a  (3)
> **Prerequisites:** configure (3)
> **Speakers:** - [narrator] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Additional resources](https://www.linkedin.com/learning/amazon-ec2-essential-training/additional-resources?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/amazon-ec2-essential-training/additional-resources?u=76281980&t=0)** - [Shyam] Congratulations on completing this course on [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md). By now, you should have gained a solid understanding of how to launch, manage, and scale [EC2](../../Skills/DevOps/Amazon%20EC2.md) instances in the AWS cloud. I hope you found this course insightful and that the skills you acquired will help you in your career. It has been my pleasure to guide you through this course and help you gain a valuable skillset. If you're interested in taking your EC2 knowledge to the next level, I encourage you to check out my course on EC2 load balancers. This is where you'll learn how to optimize your EC2 infrastructure for [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) and high availability and gain a deeper understanding of how load balancers work in the AWS cloud. Thank you again for choosing to learn with me, and I wish you all the best in your future endeavors.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ec2](../../Skills/DevOps/Amazon%20EC2.md) (4), [Amazon EC2](../../Skills/DevOps/Amazon%20EC2.md) (1), [Scalability](../../Skills/Artificial%20Intelligence%20(AI)/Scalability.md) (1)
> **Env Vars:** ec2 (5), aws (2)
> **CLI Commands:** aws (2)
> **Speakers:** - [shyam] (1)


## Instructor

- [Shyam Raj](../../Instructors/Cloud%20Computing/Shyam%20Raj.md)

## Resources

- Exercise files available

## Skills Covered

- Amazon EC2

## Path Context

### In [Infrastructure Management on AWS- Skills for Administrators](../../Paths/Cloud%20Computing/Learning%20Paths/Infrastructure%20Management%20on%20AWS-%20Skills%20for%20Administrators.md)
← [Aws Essential Training For Administrators](Aws%20Essential%20Training%20For%20Administrators.md) | **2 of 5** | [AWS- Networking](AWS-%20Networking.md) →

## Appears In

- [Infrastructure Management on AWS- Skills for Administrators](../../Paths/Cloud%20Computing/Learning%20Paths/Infrastructure%20Management%20on%20AWS-%20Skills%20for%20Administrators.md)

---

[↑ Back to top](#)