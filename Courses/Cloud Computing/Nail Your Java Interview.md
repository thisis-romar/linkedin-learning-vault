---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: nail-your-java-interview-2
url: "https://www.linkedin.com/learning/nail-your-java-interview-2"
duration_minutes: 157
duration: 2h 37m
level: Intermediate
updated: 4/22/2024
learners: 90339
skills:
  - Tech Career Skills
  - Interview Preparation
  - Java
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQF3DaHHmTVoeA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1712877366579?e=2147483647&amp;v=beta&amp;t=5UMCKuQfzn_eJ7S8_6RK82CeNC-K-QRmbSqTgurQVKo"
linkedin_topic: Cloud Computing
learning_paths:
  - '[Building Your Java Skills](../../Paths/Cloud%20Computing/Learning%20Paths/Building%20Your%20Java%20Skills.md)'
prev_courses:
  - '[Continuous Delivery for Cloud Native Java Apps](Continuous%20Delivery%20for%20Cloud%20Native%20Java%20Apps.md)'
path_nav: '[{"path":"Building Your Java Skills","position":6,"total":6,"prev":"Continuous Delivery for Cloud Native Java Apps","next":null}]'
path_count: 1
tags:
  - course
  - topic/cloud-computing
  - topic/software-development
  - skill/tech-career-skills
  - skill/interview-preparation
  - skill/java
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Cloud%20Computing/Nail%20Your%20Java%20Interview.md)

![Nail Your Java Interview](https://media.licdn.com/dms/image/v2/D560DAQF3DaHHmTVoeA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1712877366579?e=2147483647&amp;v=beta&amp;t=5UMCKuQfzn_eJ7S8_6RK82CeNC-K-QRmbSqTgurQVKo)

# Nail Your Java Interview

> Have you lined up an interview for a Java developer position? In this course, review critical Java concepts-and commonly-asked interview questions-to ensure that you approach your Java interviews with confidence. Instructor Kathryn Hodge dives into a discussion of key topics that might appear as questions in an interview, such as the difference between the Java Runtime Environment (JRE) and the Ja

> [LinkedIn Learning](https://www.linkedin.com/learning/nail-your-java-interview-2) | 2h 37m | Intermediate | 90K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (4 videos)
  - [The secret to nailing your Java interview](#the-secret-to-nailing-your-java-interview)
  - [Understand types of interview questions](#understand-types-of-interview-questions)
  - [Get ready for your technical interview](#get-ready-for-your-technical-interview)
  - [How to use CoderPad](#how-to-use-coderpad)
- [**1. Answering Java-Specific Questions**](#1-answering-java-specific-questions) (6 videos)
  - [What's the Java Virtual Machine?](#whats-the-java-virtual-machine)
  - [What's the JDK and JRE?](#whats-the-jdk-and-jre)
  - [What are classes, instances, and constructors?](#what-are-classes-instances-and-constructors)
  - [What's the difference between static and non-static methods?](#whats-the-difference-between-static-and-non-static-methods)
  - [How is scope used in Java?](#how-is-scope-used-in-java)
  - [How do you access modifiers restrict scope?](#how-do-you-access-modifiers-restrict-scope)
- [**2. Leveraging New Java Features**](#2-leveraging-new-java-features) (6 videos)
  - [Understand the impact of Jigsaw](#understand-the-impact-of-jigsaw)
  - [Use optionals to avoid NullPointerExceptions](#use-optionals-to-avoid-nullpointerexceptions)
  - [Create Lambdas to pass functionality as data](#create-lambdas-to-pass-functionality-as-data)
  - [Use the Streams API to process collections](#use-the-streams-api-to-process-collections)
  - [Discuss the pros and cons of local variable type inference](#discuss-the-pros-and-cons-of-local-variable-type-inference)
  - [Leverage new switch expressions](#leverage-new-switch-expressions)
- [**3. Solving Technical Interview Questions: String Manipulation**](#3-solving-technical-interview-questions-string-manipulation) (7 videos)
  - [Concatenate strings with different methods](#concatenate-strings-with-different-methods)
  - [Normalize string input](#normalize-string-input)
  - [Access data from strings](#access-data-from-strings)
  - [Apply string functions in technical interviews](#apply-string-functions-in-technical-interviews)
  - [Understand string equality](#understand-string-equality)
  - [Solution: Developing a Palindrome Checker](#solution-developing-a-palindrome-checker)
  - [Solution: Counting with String Manipulation](#solution-counting-with-string-manipulation)
- [**4. Solving Technical Interview Questions: Arrays and Linked Lists**](#4-solving-technical-interview-questions-arrays-and-linked-lists) (4 videos)
  - [Review arrays for technical interviews](#review-arrays-for-technical-interviews)
  - [Solution: Maximum Product of Two Numbers](#solution-maximum-product-of-two-numbers)
  - [Mastering linked lists for whiteboard coding interviews](#mastering-linked-lists-for-whiteboard-coding-interviews)
  - [Solution: Delete the Middle of a Linked List](#solution-delete-the-middle-of-a-linked-list)
- [**5. Solving Technical Interview Questions: Stacks, Queues, and Hash-Based Structures**](#5-solving-technical-interview-questions-stacks-queues-and-hash-based-structures) (6 videos)
  - [Leveraging stacks as a data structure](#leveraging-stacks-as-a-data-structure)
  - [Using queues in technical interviews](#using-queues-in-technical-interviews)
  - [Mastering hash-based structures](#mastering-hash-based-structures)
  - [Solution: Generate Binary Numbers](#solution-generate-binary-numbers)
  - [Solution: Matching Parentheses](#solution-matching-parentheses)
  - [Solution: Find Most Repeated Word](#solution-find-most-repeated-word)
- [**6. Solving Technical Interview Questions: Abstraction, Encapsulation, Inheritance, and Polymorphism**](#6-solving-technical-interview-questions-abstraction-encapsulation-inheritance-and-polymorphism) (5 videos)
  - [Mastering abstraction for technical interviews](#mastering-abstraction-for-technical-interviews)
  - [Add encapsulation to your programs to impress interviewers](#add-encapsulation-to-your-programs-to-impress-interviewers)
  - [Leveraging inheritance in your technical interview solutions](#leveraging-inheritance-in-your-technical-interview-solutions)
  - [Use polymorphism to reduce complexity in coding interviews](#use-polymorphism-to-reduce-complexity-in-coding-interviews)
  - [Mastering polymorphism with a deeper look at Java](#mastering-polymorphism-with-a-deeper-look-at-java)
- [**Conclusion**](#conclusion) (1 videos)
  - [Good luck in your interview](#good-luck-in-your-interview)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [The secret to nailing your Java interview](https://www.linkedin.com/learning/nail-your-java-interview-2/the-secret-to-nailing-your-java-interview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/the-secret-to-nailing-your-java-interview?u=76281980&t=0)** - [Kathryn] Do you have a technical interview coming up for a [Java](../../Skills/Software%20Development/Java.md) position? Are you overwhelmed by all the concepts you have to study in order to impress your interviewer? If so, I can help you. As software developers and engineers, we have our work cut out for us because in addition to HR interviews, we also have technical interviews. In technical interviews we're asked questions about [Software Development](../../Topics/Software%20Development.md), and we're tested on our knowledge of languages, frameworks, and libraries. It takes practice to get it right. Technical interviews are the most important part of the software development hiring process. No matter how great your communication skills are, you need to be able to answer technical questions. And this course will help you do that. The secret to nailing these technical interviews is not memorizing hundreds of different [Algorithms](../../Skills/Software%20Development/Algorithms.md), rather it's thoroughly understanding software development concepts and being able to recognize algorithmic patterns to implement efficient and maintainable solutions. In a [Java Software Development](../../Skills/Software%20Development/Java%20Software%20Development.md) interview, it's especially important to know about the Java programming language, specifically, what makes it unique and how it works under the hood. You can earn bonus points if you discuss the feasibility of the latest features added in newer versions of Java. Hi, I'm Katherine Hodge, and I'm a Java software developer. I'm excited to help you prep for your Java technical interview and advance your software development career. Join me in my [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning course
>
> **[1:35](https://www.linkedin.com/learning/nail-your-java-interview-2/the-secret-to-nailing-your-java-interview?u=76281980&t=95)** to understand the different tools available in the Java programming language that can help you answer those tough technical interview questions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (6), [Software Development](../../Topics/Software%20Development.md) (4), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1), [Java Software Development](../../Skills/Software%20Development/Java%20Software%20Development.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Speakers:** - [kathryn] (1)

#### [Understand types of interview questions](https://www.linkedin.com/learning/nail-your-java-interview-2/understand-types-of-interview-questions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/understand-types-of-interview-questions?u=76281980&t=0)** - [Narrator] In a [Java](../../Skills/Software%20Development/Java.md) technical interview, there are thousands of different questions you might be asked, but most of these questions fall into a few different categories. These include questions on the Java language, perhaps your past programming projects and experience, and [Software Development](../../Topics/Software%20Development.md) concepts, such as object oriented programming, [Algorithms](../../Skills/Software%20Development/Algorithms.md), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md), and more. However, the questions that are asked in a given technical interview really depend on the company's requirement, their interview process, and the personality of the interviewer. So be sure to get as much information about the interview process as you can from the recruiter so you can be prepared. In a given set of interviews for a role, you might have one interviewer that asks you questions about the Java language, while another that gives you questions about your communication skills or your leadership skills. In some cases, you may get one technical interview question with the whiteboard to write your solution on. This course is designed for you to be able to tackle any question that comes your way. In all of these scenarios, communication is key. You may have the best solution in your mind and be able to write it out in 10 minutes, but if you aren't communicating your thoughts or what you're writing on the whiteboard with the interviewer, you likely won't get the job. With remote jobs becoming more and more relevant, communication is so important. On the job, you aren't expected to go off and develop a given software product all by yourself.
>
> **[1:36](https://www.linkedin.com/learning/nail-your-java-interview-2/understand-types-of-interview-questions?u=76281980&t=96)** You work with a software development team to develop the product, as well as with product managers and other stakeholders. Someone once told me when going into a technical interview, act like you've already gotten the job. And this is your first project you are working on with your coworker. You'll still be nervous because it's your first project but it's a project that you're leading and you need to communicate with your coworker in order to create an efficient solution. Ultimately, being able to talk about your technical skills as well as showcase your personality is key to any interview.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3), [Software Development](../../Topics/Software%20Development.md) (2), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1)
> **Analogies:** such as (1)
> **Speakers:** - [narrator] (1)

#### [Get ready for your technical interview](https://www.linkedin.com/learning/nail-your-java-interview-2/get-ready-for-your-technical-interview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/get-ready-for-your-technical-interview?u=76281980&t=0)** - [Instructor] For technical interviews, you never want to wait for the moment you need a job. You should always be enhancing your skills, building your brand, and expanding your knowledge. This can mean working on personal projects, taking on tougher work at your day job, or reviewing what you know once in awhile. It can even mean running through a few example technical questions every week. A good rule of thumb is to make sure you're adding a line item to your resume at least once every six months. If not, maybe it's time to move to a new role. This could be on a team within the same company or a different company entirely. Attending networking events is a great way to prepare yourself for the communication portion of the interview process. Having an elevator pitch on your experience and your passions is essential. In getting ready for technical interviews, you always want to be ready for any opportunity that comes your way.

> [!info]- Semantic Content
>
> **Best Practices:** you should always (1), rule of thumb (1)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [How to use CoderPad](https://www.linkedin.com/learning/nail-your-java-interview-2/how-to-use-coderpad?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/how-to-use-coderpad?u=76281980&t=0)** - This course includes interactive code challenges that appear when you click on the challenge links in the courses table of contents. Each challenge includes instructions and a couple of code editors you can use to create and test your own solution to the challenge. These challenges are hosted by CoderPad and they appear in the same area of the course page where you watch the courses videos. We recommend using a desktop browser for the best experience, but you can use the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning mobile app if you prefer. The code challenge has four main areas. Instructions, a console for output, a code editor for your answer, and another code editor where you can see how your code is used. You can use these drag handles to allocate space as you'd like. Let's complete this coding challenge. There are comments in the starting code showing you where to put your solution.
>
> **[1:08](https://www.linkedin.com/learning/nail-your-java-interview-2/how-to-use-coderpad?u=76281980&t=68)** Let's run it. To run it, we can use the "test my code" button. In the output, we can see a message indicating whether our code return to correct result. In this case, we're correct and our code returned 19. Now, let's say our code is not successful. We see our answer is incorrect in the output. If you're stuck, you can always ask for help. In our starter code, we have variables for show expected results and show hints. These determine what you see in the output. Let's change them both to true. In the output, we see the expected result as well as, a hint on how to solve the problem. The code editor in the lower right shows how your solution is used. You can change this code to experiment with different test cases. Let's change seven to 21. Now, the largest number in the array is 21, and that's what's expected in the output. When you finished each code challenge, return to the course and click the next video to see my solution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Cross-References:** next video (1)
> **UI Navigation:** click on (1)
> **Exercise Files:** starter code (1)
> **Speakers:** - this (1)


### 1. Answering Java-Specific Questions

[↑ Back to Table of Contents](#table-of-contents)

#### [What's the Java Virtual Machine?](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-java-virtual-machine?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-java-virtual-machine?u=76281980&t=0)** - [Instructor] In an interview you might be asked about the underlying technology that makes a [Java](../../Skills/Software%20Development/Java.md) program work. One of these technologies is the JVM or the Java Virtual Machine. Java was designed for developers to make applications that could be run on any machine that supported Java without having to be rewritten or recompiled by the programmer. This is a develop once or compile once run anywhere ideology. When Java code is compiled it is compiled into Java bytecode. The Java Virtual Machine runs the bytecode and in turn runs your Java program. The JVM can run this same bytecode on any machine that supports the JVM because the Java Virtual Machine is aware of the particularities on the low level of each machine type. Now, in order for the Java virtual Machine to work, it has to be run on a given platform, think Mac or [Windows](../../Glossary/Service/Windows.md), according to the Java Virtual Machine specification. The JVM specification defines an abstract that can be mapped to any platform rather than a specific implementation for a real machine or processor. It defines an abstract for a virtual machine that runs on your computer. This specification describes a set of instructions, a set of registers, and more. Once the specification has been implemented
>
> **[1:33](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-java-virtual-machine?u=76281980&t=93)** for a given platform and the Java virtual machine has been installed as a part of your runtime environment, any Java program can run on that machine. Ultimately, the Java Virtual Machine provides a platform-independent way of executing code by abstracting the differences between operating systems and CPU architectures. With this in mind, no JVM implementation is the same and it tends to not have major effects on your programs because the power of Java comes from its OS and architecture obstruction. In short, the Java Virtual Machine is what allows Java to be run on many different platforms without being recompiled or rewritten.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (17), [Windows](../../Glossary/Service/Windows.md) (1)
> **Env Vars:** jvm (5), cpu (1)
> **Documentation:** specification (4)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [What's the JDK and JRE?](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-jdk-and-jre?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-jdk-and-jre?u=76281980&t=0)** - [Lecturer] The [Java](../../Skills/Software%20Development/Java.md) Runtime Environment and the Java Development Kit are required tools for running and creating Java programs. These are essential components used behind the scenes in developing Java programs, and as such, are often asked about in technical interviews. The JRE, short for Java Runtime Environment, contains the Java Virtual Machine, as well as browser plug-ins for executing Java applets. Without the Java Runtime Environment, you can't run Java programs. This is why you have to install the JRE in order to run a Java program on your computer. The JDK, short for Java Development Kit, is a full-featured [Software Development](../../Topics/Software%20Development.md) kit for Java, including everything from the JRE, as well as compilers, debuggers, and tools to create Java programs. Without the Java Development kit or JDK, you can't build Java programs. The JDK is a necessary tool for Java development. In other words, the JRE is what you need to run Java programs, and the JDK is what you need to develop Java programs. Since the JDK contains the JRE, you can just download the JDK in order to write and run Java software. However, the reverse is not true. The JRE does not contain the JDK. The JRE only lets you run Java programs,
>
> **[1:35](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-jdk-and-jre?u=76281980&t=95)** not build them. On top of the JDK, many developers also download IDEs, or Integrated Development Environments. An IDE is a special tool that helps make writing code easier, with code completion and a user interface to write and run your code. Although it's a nice tool, an IDE is not required to write Java software. However, in order to use an IDE to write your Java code, you must have the JDK already installed because the IDE uses the JDK to compile and run your program.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (22), [Software Development](../../Topics/Software%20Development.md) (1)
> **Env Vars:** jdk (10), jre (7), ide (4)
> **Definitions:** is a  (3), short for (2), in other words (1)
> **Prerequisites:** install (1), required to (1)
> **CLI Commands:** make (1)
> **Exercise Files:** download the (1)
> **Speakers:** - [lecturer] (1)

#### [What are classes, instances, and constructors?](https://www.linkedin.com/learning/nail-your-java-interview-2/what-are-classes-instances-and-constructors?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/what-are-classes-instances-and-constructors?u=76281980&t=0)** - [Instructor] Almost anything in real life can be represented in code. Think of the objects around you: a pencil, a chair, a lamp, a computer. On the most fundamental level, the way we represent these objects and how we use them in code is defined in classes. These classes are really blueprints, blueprints for the objects we want to create in code. For example, let's say we wanted to represent a dog in code. We define a given object in [Java](../../Skills/Software%20Development/Java.md) using attributes and behaviors. Some of the different attributes that define a dog are its breed, its age, and its color. In defining a dog's behavior, something a dog does is bark, so we can define a barking behavior. To represent a dog in code, we would have a Dog class with a String attribute called breed, an int attribute called age, and a String attribute called color. These attributes could also be called fields. We could say the Dog class has a breed field, an age field, and a color field. We could also make the color and dog fields have enum data types instead of String data types. But let's keep this example simple for now. This class would also have a method called bark because a dog can bark. This is a behavior of a dog we want to define.
>
> **[1:35](https://www.linkedin.com/learning/nail-your-java-interview-2/what-are-classes-instances-and-constructors?u=76281980&t=95)** From this class blueprint, we can create several different types of dogs. For example, one dog could be a German Shepherd, three years old and have brown hair. Another dog might be a Golden Retriever, five years old and have yellow hair. We construct these dogs from our Dog class using a constructor. Now, both of these dogs have a breed, age, and color defined by the class blueprint, but the values of these attributes are different, depending on the dog. Each of these dogs can also bark, and they would be able to call the bark method. This makes the German Shepherd dog and the Golden Retriever dog instances of the Dog class. These dogs were created from the Dog class using a constructor, making them instances of the Dog class. Understanding the difference between a class, constructor, and instance is key to succeeding in any Java interview, because they are the most fundamental concepts to Java. In your programming interview, you may be asked a question about one of these topics, or asked to create a small program using these ideas. Almost every piece of code you write will touch a class, instance, or constructor in some way.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3)
> **Analogies:** for example (2)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [What's the difference between static and non-static methods?](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-difference-between-static-and-non-static-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-difference-between-static-and-non-static-methods?u=76281980&t=0)** - [Instructor] The difference between static and non-static methods is very similar to the difference between classes and instances. A static method is a class method and belongs to the class itself. This means you do not need an instance in order to use a static method. A non-static method is an instance method and belongs to each object that is generated from the class. If your method depends on individual characteristics of the object, then the method should be non-static. We call it an instance method because it's accessing things specific to that object or instance. For example, if look at the dog class from the previous lesson each dog generated from the dog class had a breed, age and color. The values of these instance fields were different depending on the dog instance. If we access breed, age or color in a given method then that method has to be a non-static method because it accesses instance variables or non-static variables whose values vary per instance. If we want to use a method without creating an instance, static or class methods are the way to go. However, static class methods can only access static class variables or variables that belong to the class. This means if your method requires the value of one of the instance attributes,
>
> **[1:34](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-difference-between-static-and-non-static-methods?u=76281980&t=94)** you have to define it as a non-static method. Otherwise, you should define your method as static. So you can use it independently from your instances. Static and non-static methods along with static and non-static attributes can sometimes trip you up in interviews. You might be asked to define the term non-static or static or use these methods in a coding example. Let's take a look at how these concepts work in code. Here, we have dice class that represents a single dice. We start off with a static class variable called sides of dice. We make the static because every dice we roll should have the same number of sides. In this case, six. If the sides of dice value ever changes, it will change for all dice in existence because this variable belongs to the class. However, the side of a given dice that is face up will be different depending on the specific dice instance. This means our face value variable will be an instance variable and without the static keyword it's automatically non-static. Moving onto the methods, we make a static method that allows you to change the number of sides on the dice. Again, denoted with the static keyword. We also have an instance method that retrieves the current face value of a particular dice
>
> **[3:08](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-difference-between-static-and-non-static-methods?u=76281980&t=188)** as well as an instance method that allows you to roll a dice. Since these instance methods will be called on by a specific instance or a specific dice, they do not contain the keyword static and are therefore, non-static. In this instance methods, we also use the keyword this to further specify that face value is an instance variable belonging to the instance that called the method. Moving on to our main method, we create some dice objects. First, before we even create our instances we can print out how many sides are on a dice because it's a static variable. We simply access it by the class instead of the instance. To create two dice objects or two dice instances, we use the constructor. Next, we'll roll the dice a few times using the roll instance method. We'll roll the first dice and then the other dice a few times within a for-loop. After all the rolls, we'll retrieve the face value of each dye with the get face value instance method. Let's run the program and see it work.
>
> **[4:32](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-difference-between-static-and-non-static-methods?u=76281980&t=272)** We have outputs in our console showing the different rolls. So this looks good. Let's try changing the number of sides on the dye from six to eight using the static change num sides of dice method. Before our rolls, we'll write dice.changenunumsidesofdice and input eight. Again, with this static method, we access the class and then the method. This will change the number of sides for all our dice instances rather than a specific dice instance this is what we want. Let's run the program again and see if we can roll an eight. And here we are. The changes have been implemented and each dye has eight sides.
>
> **[5:33](https://www.linkedin.com/learning/nail-your-java-interview-2/what-s-the-difference-between-static-and-non-static-methods?u=76281980&t=333)** Now, before we move on, here are a couple things you can't do because of how static and non-static methods are defined. These are a few (indistinct) that might get you in your interview. First, since sides of dice is a static variable you can not use an instance to access it. You'll need to use the class instead. Next, the same reasoning here. Change num sides of dice is static. So you can not use the dice object to access it. You'll need to use the class name instead. Dice.roll also doesn't work because roll is an instance method meaning an instance must call it not a class, same case with dice.facevalue. Face value is an instance variable and cannot be accessed from the dice class. You must create an instance in order to get access to the face value instance attribute. Now, why is all of this important? The difference between static and non-static methods is often asked about in [Java](../../Skills/Software%20Development/Java.md) interviews. Why? It's a concise way to see how familiar an interviewee is with adding functionality to classes and using that functionality with instances and inside other functions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Definitions:** is an  (4), is a  (2)
> **CLI Commands:** make (2)
> **Prerequisites:** you'll need (2)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [How is scope used in Java?](https://www.linkedin.com/learning/nail-your-java-interview-2/how-is-scope-used-in-java?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/how-is-scope-used-in-java?u=76281980&t=0)** - [Lecturer] Depending on the programming language you are working in, the definition of scope can vary. In [Java](../../Skills/Software%20Development/Java.md), there's different scopes for different pieces of code. We've already seen this with classes and instances, but let's see how it works with other parts of your program. First, let's talk about parameters. Parameters are how we can pass information to a method in Java. You often see a method definition with inputs. Each of these inputs is called a parameter, and lives only inside the function itself. Let's take a look at this in action. Here, we have a counter class with a count instance attribute, and an instance method that increments the count of a given counter. In the increment count method, amount is a parameter that holds the value that was passed into the function. It's the amount that we want to increment the count instance variable by. If we try to access the amount variable outside of this function, we will get errors because amounts is a parameter. They are only accessible inside the function they're passed into. Going to the main class, we have a counter object and called the increment count method with the hard-coded argument value, five. Here, C is a local variable created within the main function. It's not attached to any classes or instances,
>
> **[1:34](https://www.linkedin.com/learning/nail-your-java-interview-2/how-is-scope-used-in-java?u=76281980&t=94)** so this means we won't be able to use it outside the body of the main method. Local variables live inside methods, meaning the method they are created in is the only method that has access to them. So what does that make our count instance attribute? Count is an instance variable and is accessible to any of the instance methods within the counter class, as well as the instance itself. That's why it's used by increment count, as well as the counter instance, C. We access count using the counter instance with c.count. It's important to understand how scope works in Java to ensure that the variables and methods written in your solutions are correct.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3)
> **Definitions:** is a  (3), is called (1), is an  (1)
> **CLI Commands:** make (1)
> **Speakers:** - [lecturer] (1)

#### [How do you access modifiers restrict scope?](https://www.linkedin.com/learning/nail-your-java-interview-2/how-do-you-access-modifiers-restrict-scope?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/how-do-you-access-modifiers-restrict-scope?u=76281980&t=0)** - [Instructor] Different access modifiers determine where certain variables and methods can be accessed in your code. In other words, they allow you to restrict the scope of specific functionality in your program. Access modifiers are often asked about in [Java](../../Skills/Software%20Development/Java.md) interviews because they help make your programs more secure and maintainable. The three different access modifiers in Java are private, protected, and public. We use these keywords on pieces of a given class. Private variables and methods are only visible in the class that they live in. Protected variables and methods are visible to the package and all subclasses. Public members are accessible everywhere within the program. If no modifier is provided, it's only visible to the package it lives in. Let's take a look at some code and see how this works. We have a BankAccount class with some examples of variables and methods with access modifiers. Let's walk through each of these and talk about where there'll be accessible throughout the application. First, we have a private String variable called owner. Since it's private, it will only be accessible within the BankAccount class. The same goes for the balance variable. It will only be able to be accessed within this BankAccount class. If I wanted to make these protected instead,
>
> **[1:34](https://www.linkedin.com/learning/nail-your-java-interview-2/how-do-you-access-modifiers-restrict-scope?u=76281980&t=94)** I would just replace private with protected. To retrieve the balance, we have a public getBalance method. Since it's public, this method will be visible everywhere throughout the application. This means any code in this application can use this method to see the current balance of a bank account. This is also true for depositing and withdrawing money with the public deposit and public withdrawal methods. Now, we don't want just any piece of code to be able to set a new balance or transfer money to a new account. So we make the setBalance and transfer methods have no modifier. Another class may need to access these methods, but we don't want every class to be able to access them. A lot of times in your interviews, you'll be asked to code on a whiteboard. You should consider using access modifiers where it makes sense, not only to demonstrate your Java knowledge, but also keep your code maintainable and secure.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3)
> **CLI Commands:** make (3)
> **Code Identifiers:** getbalance (1), setbalance (1)
> **Definitions:** in other words (1)
> **Speakers:** - [instructor] (1)


### 2. Leveraging New Java Features

[↑ Back to Table of Contents](#table-of-contents)

#### [Understand the impact of Jigsaw](https://www.linkedin.com/learning/nail-your-java-interview-2/understand-the-impact-of-jigsaw?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/understand-the-impact-of-jigsaw?u=76281980&t=0)** - [Instructor] Before [Java](../../Skills/Software%20Development/Java.md) nine, a JAR could be considered a unit of modularity. It was a single piece that grouped functionality together and had an explicit interface between other JARs. However, it weakly encapsulated the elements within itself. To fix this, Java nine had an epic called project [Jigsaw](../../Skills/Web%20Development/Jigsaw.md). This introduced modularization and the module system to Java. Project Jigsaw aimed at providing Java developers, with configuration and containment of the technologies built into Java, as well as their own programs. One component that was modularized was the JDK. Instead of using the entire JDK in your application, you can install and use specific components, or specific modules that your application needs. You can scale up or scale down to just the functionality you need from the JDK. This allows you to have a customized JDK for various platforms. From a small computing device, to a dense cloud environment. Furthermore, it enables improved application performance with a lighter artifact. Another important initiative of this umbrella project was encapsulating some of the internal APIs in the JDK. These non-standard APIs, were never meant to be public or maintained. The module system helps encapsulate
>
> **[1:33](https://www.linkedin.com/learning/nail-your-java-interview-2/understand-the-impact-of-jigsaw?u=76281980&t=93)** some of this functionality, while providing reliable configuration through available modules. While this mainly affected internal Java workings, developers can also organize their code into modules as a top level construct. Using module definition files, developers can declare dependencies between modules and allow their JAR to be modularized as well. This makes it easier for developers to construct and maintain libraries as well as larger applications. Jigsaw affected how many of us think of Java artifacts, and how to group components in a Java application. Your interviewer may ask you about Jigsaw. So it's important to understand how it can help improve your applications performance, and scale down your application for smaller devices. If you're interested in learning more about project Jigsaw, and modularization and Java, I encourage you to check out first look Java nine and Java modularization courses, here on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) learning.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (11), [Jigsaw](../../Skills/Web%20Development/Jigsaw.md) (5), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Env Vars:** jdk (5), jar (2)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)

#### [Use optionals to avoid NullPointerExceptions](https://www.linkedin.com/learning/nail-your-java-interview-2/use-optionals-to-avoid-nullpointerexceptions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/use-optionals-to-avoid-nullpointerexceptions?u=76281980&t=0)** - [Instructor] Introduced in [Java](../../Skills/Software%20Development/Java.md) eight, optionals are commonly used in enterprise Java code. They provide a solution for representing certain values that may or may not exist rather than polluting your code with no references. Whenever a piece of data might be null, it leaves room for a NullPointerException to occur. To prevent this, we use Java optionals. An optional is a container object, which may or may not have a given value. To see if the value exists, you can use isPresent. And this will return true, if the value exists, otherwise it'll return false. To retrieve the value, you can use the get method. This will retrieve the value inside the optional or container. Let's take a look at how these work. Here, we have two optionals. The first optional is empty, containing null or nothing. And the second optional contains data. In this case, the string Katheryn. We represent an optional with the optional data type built into Java. And then in the arrow brackets, the data type that the optional may contain. Here is a string. Let's use these optionals. Before we grabbed the data directly, we should make sure it's present. To do that we'll write nameopt.isPresent. This will check if the data exists. If the data is present and it does exist, we'll print out a specific greeting.
>
> **[1:35](https://www.linkedin.com/learning/nail-your-java-interview-2/use-optionals-to-avoid-nullpointerexceptions?u=76281980&t=95)** We'll do system.out.println, hi and grab the data, with nameOpt.get. If the data doesn't exist, we'll print out a generic greeting. In this case, hi. Let's run the program. Since the data exists, we see the specialized greeting in the console, but what if the data did not exist? Let's swap our nameOpt for the empty opt in our if statement. We'll run the program again, and we get hi, the data doesn't exist because the optional is empty. So we get the generic greeting, and no, no pointer exceptions. We can further simplify this by using or else, instead of this if statement, we'll write system.printlnHinameOpt.orElse. And we'll say the empty string, if the data doesn't exist. We'll do the same thing for system.out.printlnHi emptyOps.orElse.
>
> **[2:55](https://www.linkedin.com/learning/nail-your-java-interview-2/use-optionals-to-avoid-nullpointerexceptions?u=76281980&t=175)** Here we avoid the extra if statement, and provide a default value in this case an empty string, if the optional is empty. Let's run the program again. NameOpt has a value so we get, HiKathryn, Empty opt does not have a value, so we just get hi. Optionals can be a great tool to avoid null pointer errors in your code. And they'll prove to be useful to you as you write your solutions and your technical interviews.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (4)
> **Code Identifiers:** ispresent (2), nameopt (2), orelse (2), printlnhinameopt (1), printlnhi (1)
> **Definitions:** is a  (2)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Create Lambdas to pass functionality as data](https://www.linkedin.com/learning/nail-your-java-interview-2/create-lambdas-to-pass-functionality-as-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/create-lambdas-to-pass-functionality-as-data?u=76281980&t=0)** - [Instructor] Lambdas introduce some [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) support in [Java](../../Skills/Software%20Development/Java.md) 8, that allows you to pass functions as data. In the past, whenever we've created a function, we have always had to put it inside of a class. Sometimes, we want to create functions on the fly and not assign them to a class. We want to use them for a single purpose in the moment to calculate something. Lambdas give us a way to do just this. Let's take a look. In this code, we create a student's array list and add a few students. Instead of using a for loop to iterate through each student, we write simpler code that uses a Lambda expression. Let's run it. And here are the students. In this code, we use the forEach method to iterate through all of the students and print each one. In the Lambda function, the value of S becomes a given student name, as the iteration takes place. This is a very common imperative pattern in enterprise code, that reduces the amount of code that needs to be written. This type of Lambda function is called a consumer. It takes inputs, but does not output anything. We can pull out the Lambda function with consumer, string, we'll call this function printItem. It'll take an input N, and we'll print out the results. We'll import consumer,
>
> **[1:39](https://www.linkedin.com/learning/nail-your-java-interview-2/create-lambdas-to-pass-functionality-as-data?u=76281980&t=99)** at the top. And then we can replace this function with printItem, our consumer Lambda function. The string in the brackets represents what input data type the function will take, in this case a string. So N has to be a string. We can run it again and we'll see the students in our console. There are also other Lambda function types. A regular Lambda function has inputs and outputs. Let's try it out. We'll write function it will take N an integer, it will also output an integer, and we have our import at the top here with function dot function. We'll call this Lambda function doubly, it will take in an N and it is an integer, and it will return N times N doubling the input. To use the function, we simply take the function name dot apply input in our input. We'll say eight. So this will output 64. We'll print that out to the console. We also have a predicate Lambda type. A predicate, is a Lambda expression that returns true or false, and there are different types you use depending on what the input is to your predicate. In this case, our input will be an Int. So we'll write IntPredicate, we'll call this is Div By Three. We'll make sure to import the requirements.
>
> **[3:16](https://www.linkedin.com/learning/nail-your-java-interview-2/create-lambdas-to-pass-functionality-as-data?u=76281980&t=196)** It will take an input N, and it will return N mod three equals equals zero. This will return true, if N is divisible by three. To use it, we use the name of the function is Div By Three dot test, input in N input. And this case will say three, and this should return true because three is divisible by three. And we'll print that out to the console. The last Lambda function we have, is a supplier. This is a function that takes no inputs but produces an output. We'll write supplier. The output it will produce is a double, we'll make sure to import the requirements. We'll call it random Num Under 100. This will produce a number that is between zero and 99 inclusive. It will take no inputs, as we said before, to generate this number, we'll do mapped out random times a 100. To retrieve a value, we'll do system dot out dot print LN, used the name of the function and do dot get to generate another number out of this supplier. Let's run the program and see what happens. For doubly dot apply, we get 64, it doubles the number. Three is divisible by three,
>
> **[4:49](https://www.linkedin.com/learning/nail-your-java-interview-2/create-lambdas-to-pass-functionality-as-data?u=76281980&t=289)** so we get true for the predicate and for our supplier we get a number that's under a 100. Although you'll mostly use Lambda functions when iterating through lists or in conditional optionals. This Lambda technology is important because you may get an error about a consumer or supplier while debugging. Understanding the terms, will help you troubleshoot faster.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Definitions:** is a  (5), is called (1), is an  (1)
> **Code Identifiers:** printitem (2), foreach (1)
> **CLI Commands:** make (2)
> **Best Practices:** make sure to (2)
> **Versions:** java 8 (1)
> **Warnings:** troubleshoot (1)
> **Speakers:** - [instructor] (1)

#### [Use the Streams API to process collections](https://www.linkedin.com/learning/nail-your-java-interview-2/use-the-streams-api-to-process-collections?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/use-the-streams-api-to-process-collections?u=76281980&t=0)** - [Narrator] When first learning how to code, you'll often iterate through an array or a list using a for-loop. While this is a perfectly acceptable way to go through elements in a collection, there are more concise ways to iterate through a list using newer [Java](../../Skills/Software%20Development/Java.md) features. The streams API was introduced in Java 8, and it helps you collapse your iteration into more concise readable code. Let's take a look. First, how do we create a stream? A stream is just a sequence of items. To create a stream outright you can use stream.of and then add the contents to your stream. For an array, you can use the array and then use a raised out stream to convert your array into a stream. For lists, you have access to adopt stream method on your list object. This method will return a stream. With our stream ready, we can begin to use streams functionality. Let's say we want to print out each item in the string. Instead of using a for loop, we can use the forEach method. We'll grab our list, shopping list.stream, stream through all the elements and for each element, will do the following: print it out to the console. Notice here we're using a Lambda. We use Lambdas to process the elements in the stream generated from our list. This is one line of code it's much more readable than a for loop.
>
> **[1:35](https://www.linkedin.com/learning/nail-your-java-interview-2/use-the-streams-api-to-process-collections?u=76281980&t=95)** Now, instead of processing each item sequentially, we can also process items in parallel. We can write shoppinglist.parallelstream.forEach and have the same Lambda of system.out.println. We can also use streams to check if a certain condition is met inside of the stream. Let's say we want to know if a given item was on a shopping list, we do use the list, generate the stream and see if any of the items in the stream meet the following condition. In this case is apples on our list. If the condition and the predicate Lambda is true for any of the items, the value returned from the expression will be true. So, if apple's is on our list, the boolean is on list will be true. We can also filter the items in our stream and create a new stream with those filtered items. Let's say we wanted to create a new stream with only the items in a given aisle. We'd create the stream, use .filter and give it to a Lambda to determine what should be kept in the new stream. In this case, the stream items in aisle would have coffee and cherries because both of those start with C. Let's try something with numbers. Let's say we want to double the numbers in a given list. To do this, we'll create a stream and map a Lambda to each element in the stream that doubles the item, then we'll create a new stream with these doubled items.
>
> **[3:13](https://www.linkedin.com/learning/nail-your-java-interview-2/use-the-streams-api-to-process-collections?u=76281980&t=193)** The original list, and the original stream will not be modified. Now, if we wanted to return a list instead of a stream, we can collect the output stream with .collect and convert it into a list. We'd write .collect and use the collector's API to convert it into a list. Ultimately, the streams API is just another way Java is coming closer to an imperative design that makes code more concise. In your interviews, using the streams API instead of a for loop to iterate through and process your lists will show your flexibility as a developer and your knowledge of new Java features.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (4)
> **Env Vars:** api (4)
> **Code Identifiers:** foreach (2)
> **Versions:** java 8 (1)
> **Definitions:** is a  (1)
> **Speakers:** - [narrator] (1)

#### [Discuss the pros and cons of local variable type inference](https://www.linkedin.com/learning/nail-your-java-interview-2/discuss-the-pros-and-cons-of-local-variable-type-inference?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/discuss-the-pros-and-cons-of-local-variable-type-inference?u=76281980&t=0)** - [Instructor] Introduced in [Java](../../Skills/Software%20Development/Java.md) 10, local variable type inference can help collapse some long class names in initializing variables in your code. Local variable type inference means that you'll be able to initialize variables without having to specify the associated type. Let's take a look. In initializing variables, usually we have the data type, the variable name, and the value we want to initialize the variable with. With local type inference, we can replace the data type at the front with the identifier VAR. So we'll take away the list, write VAR, we'll take away double, write VAR. This new syntax reduces the verbosity associated with writing Java code. It's called local variable type inference because we infer the type from the context. For example, when we say VAR total equals 38.23, the compiler will already know the type is double. With this, Java still remains a statically typed language. Once the type is assigned, you cannot change it. The use of local variable type inference is still debated among Java developers. This feature isn't available completely throughout Java. It's restricted to local variables with initializers, which we've seen here, indexes in enhanced for loops, and a few other places. If you used this feature,
>
> **[1:33](https://www.linkedin.com/learning/nail-your-java-interview-2/discuss-the-pros-and-cons-of-local-variable-type-inference?u=76281980&t=93)** you've run the risk of making your code look less uniform. However, VAR is a common idea within many programming languages. So it shouldn't be hard for other developers to pick up and read. If you're working in a code base with lots of long class names, this feature can help make your code more readable with less code to read, as well as reducing the amount of code you need to write. Explicit types often get in the way as required syntax that's not very important to the goal of your program. Writing out these types can clutter your code. The VAR identifier helps eliminate this. Local variable type inference can be very useful, as long as your variable names are specific and there's enough written context in the code that another developer could come in and understand what's going on. In your interviews, you could use this new feature as you code up your white board solution and discuss your opinion on it. This shows the interviewer you are up to date with the new Java features, as well as their implications in a code base.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (6)
> **Env Vars:** var (6)
> **Versions:** java 10 (1), 38.23 (1)
> **Definitions:** means that (1), is a  (1)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Leverage new switch expressions](https://www.linkedin.com/learning/nail-your-java-interview-2/leverage-new-switch-expressions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/leverage-new-switch-expressions?u=76281980&t=0)** - [Instructor] In newer versions of [Java](../../Skills/Software%20Development/Java.md), switch expressions have been enhanced to allow you to set a value directly to a variable. This is an example of a switch statement we're used to. We use the stage variable to determine the value of the string season. If stage is 0, the season is Spring, if stage is 1, the season is Summer, and so on. At the end, we print the value of the season string. It's quite a bit of code to do a simple operation, but with the additions from Java 12 and Java 14, we can simplify this switch statement. We'll still have our int stage 2, but we're able to declare and initialize our season string with our switch statement. Here, we write String season, and it will have the value of what our switch statement returns. For the switch statement, we have our cases. Case 0 maps to Spring, case 1 maps to Summer, and so on. For the default case, we have an invalid stage. Once the season is set, we print it to the console as usual. With this new switch statement, our code is a lot more concise. You likely won't use a switch statement in every technical interview, but when you have to, this tool can definitely come in handy. Something to note, with this new switch statement, the default case is required. It's checked at compile time,
>
> **[1:34](https://www.linkedin.com/learning/nail-your-java-interview-2/leverage-new-switch-expressions?u=76281980&t=94)** and if you do not have a default case, the code will not run. The switch must be exhaustive. In all of these cases, we set the variable directly with a string, but we can actually expand our default case to have a few statements. We'll add some curly brackets and add a print statement. We'll say System.out.println("This season is invalid"), and then we'll yield invalid stage. If the stage is -5 or an invalid value, the default clause is run printing the statement and season will have the value Invalid stage. Usually, you won't be using 0, 1 and 2 in your code, but rather you'll be using enums. Let's see what a switch would look like with enums. Here, we have a Season enum with our seasons, and we have a switch statement that uses the season to determine the weather. We put the Spring and Summer seasons in the same case with a comma, and if either of these is true, the weather gets the value Sunny, For Fall, it's Rainy. For Winter, it's Snowy. And for the default case, we yield invalid weather and print the fact that the season is invalid. At the end, we print out the weather. Let's run it. The season is Spring, so we should get sunny as our weather.
>
> **[3:08](https://www.linkedin.com/learning/nail-your-java-interview-2/leverage-new-switch-expressions?u=76281980&t=188)** And that's what we get in our console. So what's the benefit of these new switches? Switches have been extended so that it can be a statement or an expression. At the same time, they've been simplified and require less overhead code, saving you from confusion and annoying bugs caused by a missing break statement. They also provide some safety by requiring an exhaustive switch with the default case.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3)
> **Versions:** java 12 (1), java 14 (1)
> **Definitions:** is an  (1), is a  (1)
> **Speakers:** - [instructor] (1)


### 3. Solving Technical Interview Questions: String Manipulation

[↑ Back to Table of Contents](#table-of-contents)

#### [Concatenate strings with different methods](https://www.linkedin.com/learning/nail-your-java-interview-2/concatenate-strings-with-different-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/concatenate-strings-with-different-methods?u=76281980&t=0)** - [Instructor] Often in interviews, you'll be asked to manipulate strings in some way, shape or form. The first step to modifying strings, is usually concatenation. The roots of how strings are concatenated or combined, lie in how strings are created. When strings are created they're immutable, you cannot make an already created string longer or shorter. You also can't modify a character that's in an already created string. If you want to do this, you have to create a new string. This means when we're concatenating two strings, we aren't changing their contents, instead, we are reading the values of the two strings and creating a new string leaving the old strings on altered. So how do we concatenate or combine strings? We have a few options. The most common is with the concatenation operator, or the plus sign. In creating the string variable name, the first name and last name variables were not modified. Instead, a new string was created with the first name and the space, and then another new string was created combining the first name space and the last name. This means two strings had to be created in order to make the name string. Again, this happens because strings are immutable once created and cannot be modified. Another option for concatenation, is what the concat string instance method. We can write string concat name,
>
> **[1:38](https://www.linkedin.com/learning/nail-your-java-interview-2/concatenate-strings-with-different-methods?u=76281980&t=98)** firstName.concat with the space, and then we will concat the last name on top of that. This is a little less readable but it essentially does the same thing. The first name and space are combined into one new string, and then this new string is concatenated with the last name string. With both of these concatenation methods, we create an extra string that we immediately throw out. The first name concatenated with the space. To make this more efficient, we can use the string builder class. Since strings are immutable, classes like string builder allow you to create string like objects that are mutable. Instead of creating a bunch of new strings to complete the concatenation, you simply create a string builder object, run the appropriate mutations with the pend, insert and delete methods, and then call two string to create the final string. Here, we did all of our operations on the one string builder instance, instead of creating a bunch of strings that we weren't going to use. There is also another class called string buffer, and it's pretty similar to the string builder class. The difference, it's thread safe because it's methods are synchronized. When you are asked to manipulate or work with strings in a technical interview, you should use a string builder or string buffer object, in order to make your programs more efficient.
>
> **[3:13](https://www.linkedin.com/learning/nail-your-java-interview-2/concatenate-strings-with-different-methods?u=76281980&t=193)** Understanding the background behind why these modifier objects are useful, can help you stand out as a candidate.

> [!info]- Semantic Content
>
> **CLI Commands:** make (4)
> **Code Identifiers:** firstname (1)
> **Definitions:** is a  (1)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)

#### [Normalize string input](https://www.linkedin.com/learning/nail-your-java-interview-2/normalize-string-input?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/normalize-string-input?u=76281980&t=0)** - [Narrator] When developing a solution to a technical interview question, you'll often need to normalize the user's input or the data you're working with. This could mean taking out white space, making sure all the characters are lowercase or alphabetical, or more. As a part of the string data type we get a few methods for free. But you have to know what they are in order to use them in an interview. Let's take a look. In this code we have a few string variables. We've put these in a list, so they're easier to work with. Just looking at these strings, the first thing I really want to do is trim off the white space at the front and the back. Using the list, we can create a stream to access each string individually and print out the trimmed response. We'll write animals.stream.fourEach to access each individual string. We'll run a .trim on it and we'll print out that result to the console, taking away those spaces. We'll save it up, run it, and here we have the trim to strings, no spaces at the front or the back. In this program we are trimming the results, we're trimming each string, printing it out, but then we throw away the result. The animals list remains unchanged. If we wanted to save the results of this trim in a new list, we could use map and .collect with the collector's API.
>
> **[1:38](https://www.linkedin.com/learning/nail-your-java-interview-2/normalize-string-input?u=76281980&t=98)** This will return a new list with the animal strings trend. So we'll have this list, we'll call it trimmed. We'll write animals.stream, .map will map the functionality of the trim and then we'll collect the results with Collectors.toList. The trend list now has our trim to strings, but one of the animal strings will be empty, that nothing string. What if we want to filter it out? We can stream these trend results and filter out that empty string. We'll again return this list, so slowly building up this list of realAnimals. We'll write trimmed.stream. We'll do .filter and we only want to keep the animals that are not empty. So we'll write S, if S is not empty, we want to keep it, and we'll want to collect it into a list and save that for our realAnimals. So we'll use collectors.toList again. To normalize the input even further, we can make all the animal names lowercased. So we'll write list string, We're going to create this new list with normalized animals names. We'll take our realAnimals, we'll stream through the contents. And then we'll map the two lowercase functionality,
>
> **[3:16](https://www.linkedin.com/learning/nail-your-java-interview-2/normalize-string-input?u=76281980&t=196)** S.toLowerCase. We'll collect the results in a collectors.twoList. We've done these optimizations using the [Java](../../Skills/Software%20Development/Java.md) streams API, and the Java collectors API. But another option is to use a for loop to access each string, normalize it and then add it to a new list. Let's run this. And there are the normalized names. Overall, if you need to do many normalizations on your input, I'd suggest separating it out into a new reusable function. If you just need to do one or two normalizations, I'd suggest using the streams API and the collectors API to avoid the verbosity of a for-loop, making your code more readable for your interviewer.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2)
> **Code Identifiers:** realanimals (3), tolist (2), foureach (1), tolowercase (1), twolist (1)
> **Env Vars:** api (5)
> **CLI Commands:** make (1)
> **Speakers:** - [narrator] (1)

#### [Access data from strings](https://www.linkedin.com/learning/nail-your-java-interview-2/access-data-from-strings?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/access-data-from-strings?u=76281980&t=0)** - [Instructor] Now that we know some ways to normalize our input. Let's talk about how we can access the data inside of our string. Much of this functionality is already built in with string instance methods. Most of these methods use indexing for accessing each character stored in the string. Looking at the first variable here, we could say that A is a index zero, P is a index one, P is a index two and so on for the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the string. The last index in the string is the length of the string minus one. So here for apples, since the length is six the last index must be five because we start our counting at zero. To access a character at a given index we can use the strings charAt method. Let's say we want to access the first character. All right, char first character, create a variable. We'll access the string, apples, use charAt, that built in functionality and our input will be zero. Because the first character is a index zero. We'll print this out to the console and verify it's an A.
>
> **[1:20](https://www.linkedin.com/learning/nail-your-java-interview-2/access-data-from-strings?u=76281980&t=80)** And we get A in our console. We can do this for any index we want. However, if the index is out of bounds meaning it's less than zero or greater than the length of the string minus one we will get an index out of bounds exception. Be careful of that as you access characters in your strings. We can also check the index of a specific character. Let's say we want to know the index of the character E in our apple string. All right, apples.indexOf. We want to know the index of E so that's our input. And we'll print out that number that it returns to our console, so that number will be the index of E. We'll run this again and it should be four in our console. If the character is not in the string indexOf would return negative one. What if we wanted to access a sequence of characters in a string or a sub string of a string, we can use the sub string function. All right, apples.substring. And we'll grab the characters between index two and four. We'll save this into a new string called sub and we'll print it out to the console. In this case the sub string function will return a string with the letters P and L or the letters at index two and three.
>
> **[2:53](https://www.linkedin.com/learning/nail-your-java-interview-2/access-data-from-strings?u=76281980&t=173)** This is because sub strings indexing is inclusive of the first index but exclusive the second index. We can also check if a certain sequence of characters is in a string, with the contains function. We could see if apple.contains the app string. If it does this will return true, otherwise false. Let's print this out directly to our console. And we get false. That's because apples has a capital A and this is looking for a lowercase A. If we capitalize this, run our function again, we get true in the console. This is why normalizing your input is so important. These errors can trip you up. We also have the option to convert a string into a character array. We can create a character array called applesArray and run apples.toCharArray to convert apples into a charArray. This allows us to work with our string data in an array format. So instead of using charapp I could write int index equals four system.out.println applesArray and access the fourth entry. This will return the same thing as apples.charAt four as input.
>
> **[4:27](https://www.linkedin.com/learning/nail-your-java-interview-2/access-data-from-strings?u=76281980&t=267)** Let's run this again and we see E in our output, because that's a index four of our apples array. This type of functionality can become really useful in interview questions that require extreme memory constraints or clever indexing of strings.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Code Identifiers:** charat (3), indexof (2), applesarray (2), tochararray (1), chararray (1)
> **Definitions:** is a  (4)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)

#### [Apply string functions in technical interviews](https://www.linkedin.com/learning/nail-your-java-interview-2/apply-string-functions-in-technical-interviews?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/apply-string-functions-in-technical-interviews?u=76281980&t=0)** - [Instructor] Sometimes, you may need to use more advanced string functions to process more complex string data. In this lesson, we'll be manipulating and parsing a string of text. For this given text below, we'll want to find out a few things. How many sentences does it have? How many words, how many characters? There are lots of questions we can ask about this piece of text, along with the various ways to manipulate it. Starting with the first question, let's try to figure out how many sentences this text has. This specific piece of text separates each sentence with a period and a space. If you are given this in an interview you might want to ask a clarifying question like should we expect sentences with question marks or exclamation points, ensuring you have a good definition of what a sentence is will be important for the implementation. In this case, let's say a sentence is defined by a piece of text with a period at the end of it. We'll assume there are no words that will contain periods. Now, how do we implement this? We can use the string split function. Split takes a regular expression and breaks a string around a matches of that regular expression outputting a string array. On the most basic level, split will break a string based on a single character or a single sequence of characters. This means split can break our string
>
> **[1:35](https://www.linkedin.com/learning/nail-your-java-interview-2/apply-string-functions-in-technical-interviews?u=76281980&t=95)** into individual sentences, splitting on the.space sequence in the text. To implement this we'll write text dot split backslash backslash dot space. This will output the string array with our sentences. So we'll save that in a variable. We did use a double backslash because we want to use the literal dot character rather than what the dot means in regular expressions. To figure out the number of sentences in our text, we can just grab the length. We'll do system dot out dot println. The text has, we'll grab our sentences dot length sentences.
>
> **[2:23](https://www.linkedin.com/learning/nail-your-java-interview-2/apply-string-functions-in-technical-interviews?u=76281980&t=143)** Let's run the program and see how many sentences this text has. The text has four sentences. We can do the same thing for words. Words in our case are split by a space or a dash. So we'll do a similar operation. We'll write text dot split space or dash. We'll save this in a different array we'll call it words. The length of this array is how many words are in the text. So we'll print out the length to the user. We'll right system dot out dot println, the text has words dot length words. We'll run the program again and this piece of text has 53 words. While this is the correct [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) count let's take a look at what's in this words array. We'll use arrays dot aslist so we can easily see it in our console. Running the program again, the giant panda, this looks good. Bamboo has a period with it. This is something to consider filtering out in the future. Now finding the number of characters is much more simple. All we need to do is find the length of the text string. Since we're counting every char as a character,
>
> **[3:59](https://www.linkedin.com/learning/nail-your-java-interview-2/apply-string-functions-in-technical-interviews?u=76281980&t=239)** the length of the text is the number of characters in the string. We'll print it out with system dot out dot println, the text has we'll input text dot length grabbing the number of characters and we'll finish it off with characters. Running this again, we have 293 characters in this text and there you have it. With a few string methods you can find out some pretty interesting information about a given piece of text.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **CLI Commands:** find (3)
> **Speakers:** - [instructor] (1)

#### [Understand string equality](https://www.linkedin.com/learning/nail-your-java-interview-2/understand-string-equality?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/understand-string-equality?u=76281980&t=0)** - [Illustrator] The roots of string equality lie in how we create an initialized strings. There are two ways we can create strings. We can create string literals or string objects. The difference between these is how they are stored and referenced in our code. Most of the time, you'll likely create a string literal. That's a series of characters with quotes around them. As string literals, both string A and string B refer to the same string in memory. They are both references to the same literal ABC value within the string constant pool. This matters in the case of string equality, which we'll discuss in a minute. The other way to create a string is with the string object constructor. In this case, instead of creating a string literal, we are creating a string object. Object A and object B do not refer to the same string value because they are created as separate objects with the string constructor. Usually, you'll want to create string literals over string objects, because it gives the compiler a chance to optimize your code. If string A and string B have the same contents, then it's nice to store them in just one place, rather than storing them as separate objects. Now, you might be thinking that's nice, but what does it have to do with string equality? Let's take a look at some equality statements. Running the program, we can see what equals what.
>
> **[1:37](https://www.linkedin.com/learning/nail-your-java-interview-2/understand-string-equality?u=76281980&t=97)** In the printout, literal A equals equals object A, and literal A equals equals object B, both returned false. This is because the variable literal is referencing a different ABC than the object is. Their references are separate, and that's what's checked when we use the double equal sign for equality. However, in the last case, literal A and literal B referenced the same string and therefore their equality returns true. This can become annoying when dealing with more complicated programs with strings, which is why the string instance method .equals was created. This compares the true value of strings rather than references and pointers. Let's try it out. We'll swap the double equal sign for the .equals function. We'll write .equalsobjectA, and the same for the others. Let's run again. Now, all three return true. What happens if the string value is different? We'll create a new string literal, we'll call this string literal C and we'll give it the value C. We'll compare it to our other strings with system.out.println(literalA.equals(literalC))
>
> **[3:11](https://www.linkedin.com/learning/nail-your-java-interview-2/understand-string-equality?u=76281980&t=191)** And we'll also create an object C, so we can see the difference.
>
> **[3:21](https://www.linkedin.com/learning/nail-your-java-interview-2/understand-string-equality?u=76281980&t=201)** LiteralA.equals(objectC) We'll run the program again, and they both returned false. This is because the true string value is different. Now, what does this mean for your interviews? It means you'll likely want to use the .equals method instead of the double equals operator in your solutions. You might use this to compare identifiers between objects, or filter out certain objects without a matching ID. Understanding the difference between literals and objects is also important, because it's a common question asked in interviews.

> [!info]- Semantic Content
>
> **Code Identifiers:** equalsobjecta (1), literala (1), literalc (1), objectc (1)
> **Env Vars:** abc (2)
> **Cross-References:** in the last (1)
> **Speakers:** - [illustrator] (1)

#### [Solution: Developing a Palindrome Checker](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-developing-a-palindrome-checker-23734091?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-developing-a-palindrome-checker-23734091?u=76281980&t=0)** - [Instructor] Let's implement a function that determines if a string is a palindrome. Remember, a palindrome is a [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) or phrase which reads the same backwards as it does forwards. We'll start by normalizing the input and converting it to lowercase. This will ensure that we treat uppercase and lowercase letters as the same. For our implementation, we'll create a reverse string and check if the reverse string is the same as our input string. We'll use a string builder to create that reverse string. Then we'll iterate through the normalized string backwards, appending each character to our reverse string builder. At the end, we'll convert our reverse string builder to be a string, and then compare it with the normalized string. If the strings are equal, then the original string must be a palindrome. Let's run it. Our implementation works as expected. Now, how can we improve this? Well, instead of manually reversing the string, there are string builder functions and functions from Apache StringUtils that'll do this automatically for you. You can mention these to your interviewer, but your interviewer will likely want to see you do the implementation manually to see more of your skills. Another note about this implementation
>
> **[1:32](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-developing-a-palindrome-checker-23734091?u=76281980&t=92)** is that we build the entire reverse string before doing any checks to see if there's a mismatch of characters causing the string to not be a palindrome. Instead of building a reverse string, we could check if at each iteration point the corresponding characters match. For this implementation, we'll use a while loop and create two variables that denote the start and the end. Then, while the start is less than the end, we'll compare the characters at the start and end positions.
>
> **[2:11](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-developing-a-palindrome-checker-23734091?u=76281980&t=131)** If they don't match, the string is not a palindrome. If they do, we can increment and decrement the variables accordingly until they overlap. We'll increase the start and decrement the end. This makes the implementation ideal because we only iterate through half the string. When the two indices meet in the middle, and if each comparison was true, then the function should return true, so let's do that after the while loop. Let's run it. All of our tests still pass. Let's consider one last implementation. Let's say we wanted to use [Java](../../Skills/Software%20Development/Java.md) streams instead of a while loop. We can do this with an in-stream.
>
> **[3:19](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-developing-a-palindrome-checker-23734091?u=76281980&t=199)** This allows us to do a loop, but with the Lambda in less lines. We iterate over half the string, and use a similar theory as we did in our last implementation where we immediately return false, the given index does not meet the condition. Let's see if this works. The tests still pass. Ultimately, it's up to you which implementation you choose, but it's important to consider the pros and cons of your decision.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Definitions:** is a  (2)
> **CLI Commands:** apache (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Counting with String Manipulation](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-counting-with-string-manipulation?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-counting-with-string-manipulation?u=76281980&t=0)** - [Instructor] Let's implement a function that finds the number of vowels in a given string. There are many ways to solve this problem, but we'll walk through two different solutions. For our first implementation, we'll use a variable to keep track of the number of vowels we've seen in the string. We'll call it count. Then we'll iterate through the string incrementing our vowel count each time we see a vowel. In order to know when we've seen a vowel, we'll also want to create a string to keep track of our vowel characters. That's a a, e, i, o, u, and y. Since these vowels are in lowercase, we'll also convert our input to lowercase and we'll trim off any white space from the beginning and the end. Let's iterate through our normalized input. We'll convert our string to a charArray to keep our for loop cleaner. Currently, you can't use a for each loop with strings directly. In the body of our loop, we'll check to see if the current character is in our vowels list. We'll use indexOf, and if this does not return -1, then the character is a vowel and will increment our count. Once we've iterated through the input, we'll know the total vowel count and we can return it from the function. Let's run it.
>
> **[1:33](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-counting-with-string-manipulation?u=76281980&t=93)** All the tests pass. Now let's look at another way we could solve this problem with [Java](../../Skills/Software%20Development/Java.md) Streams. The beginning will be similar. We'll have a vowel string and a normalized input. Then we'll iterate through the input and filter for the vowels. We'll use the same condition as before. Once all the vowels have been found, we'll count them and return the count. Now this function returns along so we can cast it to an int to ensure we return an int from the function. Let's run it. It works. So which implementation is better? The first implementation is more explicit and could be considered easier to read for those not familiar with Streams. The second is more concise and expressive, since it uses the more functional parts of the Java programming language. String manipulation questions are often asked in interviews, and I encourage you to keep practicing with these types of questions in preparing for your interview.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2)
> **Code Identifiers:** chararray (1), indexof (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 4. Solving Technical Interview Questions: Arrays and Linked Lists

[↑ Back to Table of Contents](#table-of-contents)

#### [Review arrays for technical interviews](https://www.linkedin.com/learning/nail-your-java-interview-2/review-arrays-for-technical-interviews?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/review-arrays-for-technical-interviews?u=76281980&t=0)** - [Instructor] In the next part of the course, we'll be looking at various [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) that can help us organize and store our data in different ways. These are often asked in technical interview questions, because they test not only your knowledge of a given programming language, but also how you apply that knowledge to a given problem involving data. One common data structure is called an array. In [Java](../../Skills/Software%20Development/Java.md), an array is a collection of ordered objects of the same data type. Each object in the array can be accessed by an index, starting at index zero, up to the length of the array minus one. Since items in an array are stored in directly adjacent memory locations, an array's size cannot be changed after it's allocated. Like other data structures, there are specific operations you can use to declare, allocate, initialize, add, and remove objects to an array. Let's take a look at some code. First, we declare an array. Here, we have an int array, and we've given it the name nums. This array can only contain ints, but right now the array is empty. Next, we declare another array. The difference, we are also allocating space for five items. We haven't initialized the array with any values, but there are five potential spots where values could live. So what about initializing an array?
>
> **[1:38](https://www.linkedin.com/learning/nail-your-java-interview-2/review-arrays-for-technical-interviews?u=76281980&t=98)** Let's say we own a pet shop, and we have pets that are currently available, and pets that are currently not available. To represent this, we've declared, allocated space for, and initialized, two string arrays, with available and unavailable pets in our pet store. In this case, cat, dog, and fish are available, and bird, rabbit, hamster, and gerbil are unavailable. With our pet arrays created and initialized, we can print out what is in these arrays with arrays.2 string. Let's run the code. We'll run main.main, and we see both the available pets and the unavailable pets in our console. Now let's say we wanted to move one pet from available to unavailable, and another pet from unavailable to available. This would require retrieving, removing, and adding items to both the available and unavailable pet arrays, and essentially swapping one animal for the other. To do this, let's determine which objects at what indices we want to swap. We'll want to swap the fish and the bird pets, so let's create some variables. We'll write int indexOfAvailablePet, and the available pet we want to swap is at index two, that's the index of the fish, and the index of the unavailablePet is at index zero, that's the index of the bird.
>
> **[3:17](https://www.linkedin.com/learning/nail-your-java-interview-2/review-arrays-for-technical-interviews?u=76281980&t=197)** Then we'll want to retrieve the current available and unavailable pets. To do that, we'll need to access each individual array, and use that index to retrieve the pet. So in my availablePets array, I'm going to retrieve the fish using the index of that pet. We'll save the fish in a string called availablePet. To grab the bird, we'll say String unavailablePet, we'll use the unavailable pets array, and we'll use the index of the unavailablePet to get the bird. Now we have access to the bird and fish items. To add the bird to the availablePets array, and delete the fish, we'll access the availablePets array, access the appropriate index of where we want the bird to be, and set its value to the unavailablePet, or to our bird. To add the fish to the unavailablePets array, we'll do something pretty similar. We'll access the unavailablePets array, use the index of the bird of where that is located, so that's the index of the unavailablePet, and we'll set its value to our fish, which is the availablePet. Let's grab these print statements, add them to the bottom, and see what happens. At first, we had our normal available and unavailable pets, but then after the swap, we see fish is swapped with bird,
>
> **[4:54](https://www.linkedin.com/learning/nail-your-java-interview-2/review-arrays-for-technical-interviews?u=76281980&t=294)** and bird is swapped with fish. In this case, we swapped two items, but what happens if you want to add something to an array that's already full without deleting anything? Ultimately, you have to copy everything to a new, bigger array, and then add your item, but this can be time consuming. Another option is to make a huge array, but then you're wasting a bunch of space, and this makes insertion and deletion much more difficult and time consuming. If you have to use arrays, and need to create more space, comparing and contrasting these options with your interviewer is a good idea. Arrays can also be used to implement other data structures. They have N dimensions and less overhead. So if you need to use a collection of items that will not change in size, arrays are the way to go. Understanding the advantages and disadvantages of each data structure can be useful for determining which data structure organizes your data in the best way for your given use case. No data structure is perfect, but some can be perfect for your use case.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (3), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** unavailablepet (5), availablepets (3), availablepet (2), unavailablepets (2), indexofavailablepet (1)
> **Definitions:** is a  (2), is called (1)
> **CLI Commands:** cat (1), make (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Maximum Product of Two Numbers](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-maximum-product-of-two-numbers-23732177?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-maximum-product-of-two-numbers-23732177?u=76281980&t=0)** - [Instructor] Let's implement a function that finds the maximum product of two numbers in an unsorted array. To start, we'll create some variables. One will hold the length of the array. And the other will keep track of the maximum product we've seen so far. Our input is invalid if the length of the array is less than two. If there are not two items to multiply, then the question of maximum product is invalid. There are a variety of ways you can handle this, but in this case, we'll return a sentinel value or a value that's not expected by the algorithm. This can be used later on to drive an exception if needed. Another option is to throw an exception right here instead of returning a value. Both options are valid, and you can discuss them with your interviewer to see which they think would be best. Here, the constraint says to return Integer.MIN_VALUE, so that's what we return here. Validating your input is in fact valid is a key factor in creating a good algorithm. Keeping track of the assumptions you're making about your input is also important in case those assumptions change later on. Now it's time to calculate the maximum product. Since the array is unsorted, we'll need to look at every pairing in the array and keep track of the maximum product. We'll iterate through the entire array.
>
> **[1:34](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-maximum-product-of-two-numbers-23732177?u=76281980&t=94)** Then for each element, we'll iterate through the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the array. For each pairing, we'll calculate the product. If it's greater than the current max, we'll overwrite the max with the new value. At the end, we'll return the max value. Let's run it. Our code works as expected. Often in an interview, you'll be asked about the time complexity of an algorithm. We won't discuss too much about time complexity in this course, but we can say this algorithm runs in O of N squared time. N represents the length of our input, and N squared means our algorithm runs in quadratic time. For every item in the array, we check it with every following item. This makes the time complexity O of N squared. Now, there are ways we could make this more efficient. If we can assume our input will have lots of duplicate values, we could remove those duplicates with a hash set, which we'll discuss later in this course. Then try to calculate the product of the unique items. If the array was sorted, we could calculate the product of the two items at the end of the array because these items would have the highest value. In fact, many sorting [Algorithms](../../Skills/Software%20Development/Algorithms.md) have a time complexity that is less than N squared, so another route is to sort the input and then calculate the product
>
> **[3:06](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-maximum-product-of-two-numbers-23732177?u=76281980&t=186)** of the items at the end of the array. Let's try that. Arrays has a class method called sort, and we can use that to sort the array. This algorithm uses quick sort for primitive values like the ints we have here, and merge sort for referenced values that have a comparator. Since the min values could both be negative, where the multiplication creates a positive value, we'll need to check the product of both minimum values as well as both maximum values. If the negative values are negative enough, their multiplication could be greater than the multiplication of the maximum values. Let's run it. Our code still works. Thinking about time complexity, quick sort's worst case is still N squared, but with the randomized pivot, it often comes to N log N, and could be faster in some cases. Instead of having the input as an int array, you could discuss converting the input to an integer array so that merge sort is run instead. However, this implementation still has the side effect of sorting the original input. Numbers would now be sorted outside of the function. If sorting is useful for your program down the line, that's great, but it's not ideal to have it as a side effect of another function. The way you write your algorithm depends on the requirements, the assumptions you want to make,
>
> **[4:41](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-maximum-product-of-two-numbers-23732177?u=76281980&t=281)** and how it fits into the rest of the program or code base. In a technical interview, you always want to try to get the algorithm as close to O of N time or linear time as possible, where you look at every item in the input just once. Let's consider one more possible answer to this question. The idea behind this answer is to find the potential maximum and minimum values first, and then at the end calculate the product. To do this we'll set up our min and max values and then iterate through the array. For each item, we check if it's greater than our first or second maximum, or if it's less than our first or second minimum.
>
> **[5:52](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-maximum-product-of-two-numbers-23732177?u=76281980&t=352)** When one of these is true, we drop into the if block and set our variables accordingly. The goal is to find the two minimum and two maximum values in the array. We'll use these to calculate the maximum product. Remember, the two minimum values could both be negative, creating a large positive value in their multiplication. This could be larger than the max product. That's why we have to keep track of the minimums as well. Let's run the code. The tests still pass. Here, we've shown a few different solutions to this challenge. While it's nice to create a working algorithm that runs in linear time, often in an interview, it's not about providing the perfect solution. Rather, it's about considering your requirements and assumptions carefully and being able to talk through your thought process.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1)
> **CLI Commands:** make (2), find (2)
> **Env Vars:** min_value (1)
> **Cross-References:** later in (1)
> **Definitions:** is a  (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Mastering linked lists for whiteboard coding interviews](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-linked-lists-for-whiteboard-coding-interviews?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-linked-lists-for-whiteboard-coding-interviews?u=76281980&t=0)** - [Instructor] Depending on the technical interview question, you may want to organize your data in a linked list. A linked list is a linear data structure where elements containing data of the same type are linked using pointers. This allows us to easily modify and retrieve data in a sequential manner. Each element in the linked list is called a node, and it contains data along with the pointer, pointing to the next element in the linked list. If the next pointer is null, then the given node must be the last element in the list. We often call the first item of a linked list the head of the list, and the last item the tail. So how is this different from an array? Since the elements are connected by pointers, the items do not need to be stored at contiguous memory locations. This means insertion and deletion are less expensive. With these pointers, we can also have singly-linked lists or doubly-linked lists. Singly-linked lists have nodes that only have a pointer pointing to the next node. Doubly-linked lists have nodes with pointers to the next node as well as the previous node. Let's take a look at how linked lists work in [Java](../../Skills/Software%20Development/Java.md). In order to use a linked list, we must import Java.util.linkedlist. In this example, I created a linked list called students that represents students from a given class. Each student is represented by a string, the student's name.
>
> **[1:33](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-linked-lists-for-whiteboard-coding-interviews?u=76281980&t=93)** Since this linked list will contain strings, we have string in the arrow brackets. To add specific students to the list, we write the name of the linked list, students.add, the method, and then input the student's name. In this example, we've added Charlie, Sally, Morgan, Taylor, and Jamie. Now, you might be wondering, where do the node and next node references come in? Although the implementation of a linked list is abstracted to us here, the foundation of the data structure relies on nodes and links to the next node. In this example, we imported Java's implementation of a linked list, and that's what we're using here. However, it is a good practice to try creating a linked list from scratch, using a custom node class. Furthermore, understanding the implementation of a linked list can help you understand when it makes sense to use it. As we said earlier, insertion and deletion are less expensive operations for a linked list. If you have a use case that requires these operations frequently, using a linked list might be a good way to go. Let's look at the other operations that come with Java's built-in linked list implementation. Any of these operations are fair game for your interviewer to ask you to implement from scratch, where you create your own node class and implement the given behavior. Let's take a look. We have the option to add to the front or the back of the list using add first and add last, as well as the ability to insert nodes
>
> **[3:08](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-linked-lists-for-whiteboard-coding-interviews?u=76281980&t=188)** at a given position. Here we insert Tara at index two. With our items added, there are many ways to retrieve items from a linked list. Since we declared this linked list with strings in the arrow brackets, we know only strings can be inputted and outputted from this list. We can retrieve the first item, the last item, a student at a given index, or even the index of a given student. We can also retrieve the size of a linked list, and use that in various iteration use cases. So far, we've added items and retrieved items from a linked list. We can also remove items based on placement, the value of the item itself, or by index. We can even search for items in a linked list using the contains method, and empty the list with the clear method. Let's run the program and see what this outputs. Going back to the top of our implementation, we added all of these students. The first thing we print out is the first student. We added Sarah to the front, and that's what we get in our console. We added Hailey to the back, and that's what we get as our last student. For our second student, we get Charlie, and Charlie was the first item we inputted, but then we put Sarah in front, so Charlie makes sense as our second student. If Charlie's at index one, we added Tara at index two,
>
> **[4:44](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-linked-lists-for-whiteboard-coding-interviews?u=76281980&t=284)** Sally's at index three, Morgan must be at index four, and that's what we get in our console. Once we've added all of these students, it comes up to eight students, and that's what we get as the size. Next, we remove a bunch of students. We remove Sarah at the front, Hailey at the back, Jamie, the item at index one, which is Tara, and then we print out the students. We're left with four students, Charlie, Sally, Morgan, and Taylor. This list does contain Taylor, so we have Have taylor? does equal true. We clear the list, and that is what we get back in our console, a cleared list. As we mentioned before, there is a lot of abstraction here. In our code, we don't see the next pointers, and we don't have access to them. We only have access to our data, which in some ways is a benefit. We don't have to worry about maintaining the data structure. We only have to worry about inserting, removing, and searching for our data. Thinking about the benefits of this data structure, linked lists are more efficient at inserting and deleting data because they're not a fixed size. Linked lists are dynamic [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) that can grow and shrink at runtime. Unlike arrays, we don't have to shift elements for insertion or deletion. We just have to update the next and previous references. Other data structures, such as a queue and a stack,
>
> **[6:17](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-linked-lists-for-whiteboard-coding-interviews?u=76281980&t=377)** can also be implemented using a linked list. However, linked lists do have their disadvantages. More memory is required to store elements in a linked list as compared to an array. Search or node traversal is also difficult in a linked list, because each node is connected by a next pointer. For example, if we wanted to access a node at position N, we would have to traverse all of the nodes before it, by following the next pointers. This can take some time. If you're interested in learning more about these data structures, I encourage you to take my [Programming Foundations- Data Structures](../Software%20Development/Programming%20Foundations-%20Data%20Structures.md) course, here on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning, where we dive deeper into these topics.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (4), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (4), [Programming Foundations](../../Skills/Software%20Development/Programming%20Foundations.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** node (13)
> **Definitions:** is a  (4), is called (1)
> **Analogies:** such as (1), for example (1)
> **Cross-References:** we mentioned (1)
> **Best Practices:** good practice (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Delete the Middle of a Linked List](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-delete-the-middle-of-a-linked-list?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-delete-the-middle-of-a-linked-list?u=76281980&t=0)** - [Instructor] Let's implement the sum function. We'll start off by creating a variable called sum to keep track of the sum of the items in the linked list. We'll also want a variable for keeping track of where we are in the list. We'll create a variable called current and set it to the beginning of the list. With our variables set up, it's time to iterate through the list, adding each item's value to the sum. We can do this with a while loop. While the current item in the linked list is not equal to null, we'll want to retrieve its data, add it to the sum, and go to the next item in the list. We can access a node's data with the val field, then we'll add it to the sum. We'll also want to increment the current variable, so we move along in the linked list. We'll set current = current.next. Once we've iterated through the list, we can return the sum.

> [!info]- Semantic Content
>
> **CLI Commands:** node (1)
> **UI Navigation:** go to (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 5. Solving Technical Interview Questions: Stacks, Queues, and Hash-Based Structures

[↑ Back to Table of Contents](#table-of-contents)

#### [Leveraging stacks as a data structure](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-stacks-as-a-data-structure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-stacks-as-a-data-structure?u=76281980&t=0)** - A stack is an ordered list of objects that are inserted and removed following a last in first out policy, a LIFO policy. The first item inserted into the stack will be the last item removed from the stack. And the last item inserted onto the stack will be the first item removed from the stack. For stacks we insert items using a push method, essentially pushing items on the top of the stack. We delete items using a pop method, which pops items off the top of the stack. If you think of a deck of cards, if we put one card down then card two down then card three down card three is last inserted or pushed on top of the stack. Naturally the last card inserted will be drawn first since it's on top. The third card will be removed from the deck first. Since card one was pushed on top of the stack first, it will be removed last because it's at the bottom of the stack. Here, removing it the stack is empty once again. Let's take a look at how something like this might work in [Java](../../Skills/Software%20Development/Java.md). First, we import Java.util.stack so we can use Java's built in implementation of the stack data structure. Then we create a stack using the stack constructor. We also create three strings that represent cards one, two, and three.
>
> **[1:33](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-stacks-as-a-data-structure?u=76281980&t=93)** This is a loose interpretation of a card and it would certainly be better if we had a card class to represent each card with their value and type. However, for this example, let's keep it simple and represent each card with a string. We can insert these cards onto the deck using the push method. Stacks can also be implemented with the arrays or linked lists, but the main difference is the rules of how we insert and remove data are much more strict. We can only push and pop data from the top of the stack or the back of a list rather than both the bottom and the top. Looking at some of the other stack methods, we can use the peak method to see what's on top of the stack without removing anything. We can also find out the size of a stack using the size method. To remove items from a stack we use the pop method, however, you have to be careful. If you try to pop a stack that is empty, you will get errors. To avoid these errors, we should make sure the stack actually has items. To do this we can use the is Empty method on the stack and check if the stack is empty before removing the item. We'll do this with an if statement. We'll say, if the deck of cards is not empty, then we'll remove an item. Once we've removed it, we'll print it out to the console. If we want to iterate through the stack,
>
> **[3:09](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-stacks-as-a-data-structure?u=76281980&t=189)** removing each item, we can change our if to a while loop and the stack would be cleared. We'd remove each item and tell the stack was empty. Let's run our program and see how this stack handles our data. First, we print out the deck of cards we see our Jack of diamonds, our eight of hearts or three of clubs. The three of clubs is on the top of the stack and the size of our stack is three. When we remove items we've removed from the top so first we remove the three of clubs then the eight of hearts, and last the Jack of diamonds. This is how we use stacks to work with our data. But why would you use a stack in your program? Stacks are great when you need to reverse things. For example, let's say you push a string onto a stack, one character at a time, and then you make a string from the members popped off the stack. The resulting string is reversed. Stacks are also good for keeping track of state and the runtime stack is a great example of this. The runtime stack keeps track of what variables you currently have access to and what sub-routine or function you're in. Whenever you get an error, an error message coming from the runtime stack usually appears. And you can retrace your steps from where the error occurred.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3)
> **CLI Commands:** make (2), find (1)
> **Definitions:** is a  (2), is an  (1)
> **Env Vars:** lifo (1)
> **Analogies:** for example (1)
> **Warnings:** be careful (1)
> **Speakers:** - a (1)

#### [Using queues in technical interviews](https://www.linkedin.com/learning/nail-your-java-interview-2/using-queues-in-technical-interviews?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/using-queues-in-technical-interviews?u=76281980&t=0)** - [Instructor] A queue represents an ordered list of objects, just like a list, but its intended use is slightly different. A queue is designed to have elements inserted at the end of the queue, and elements are removed from the beginning of the queue. If you think of a line at a supermarket, the first person in line is always the first person out of line. The last person that joins the line is also the last person out of line. To use queues in [Java](../../Skills/Software%20Development/Java.md), we have the java.util.Queue interface. Since an interface is not concrete, we'll need to instantiate an implementation of the interface in order to use it. In Java, we can choose between a linked list and a priority queue. A linked list is a pretty standard queue implementation, so we're going to use this implementation in our example. Let's take a look at how this works in code. Here, we have an example where we want to process the requests of a series of people in line in order. We import the java.util.Queue interface and then use a LinkedList concrete implementation to create the queue. We create it and save it in a variable called queue. Then we enqueue a series of names, or a series of people, in line using the add method. Let's run this and see what it looks like in our console. We want to see what the queue looks like, so we'll print it out. This printout looks pretty similar
>
> **[1:37](https://www.linkedin.com/learning/nail-your-java-interview-2/using-queues-in-technical-interviews?u=76281980&t=97)** to how our LinkedList looked in the console, and that's because our queue is implemented as a LinkedList. The main difference is that there's more rules for how we can insert data. For queues, the only way we can enqueue or add data is to the back of the list, and we dequeue from the front of the list. LinkedLists provide more freedom with insertion and deletion, but with that freedom comes responsibility. Let's take a look at some more things we can do with queues. If we want to see what's next in the queue without removing it, we can use the peak method. We'll access the queue and run .peak. And we'll print out what's next to the console with System.out.println, next up, and we'll see our next person in line. To remove items, we can use the remove method with queue.remove. To traverse a queue and see all of its items, we can use a while loop and check if the queue is empty before removing the next item. Similar to our stack, we'll say while queue.isEmpty, and we want when it's not empty to remove items. We'll save this item in a string called removed, and then print it out to the console. Let's run the code. First, we print out the entire queue.
>
> **[3:12](https://www.linkedin.com/learning/nail-your-java-interview-2/using-queues-in-technical-interviews?u=76281980&t=192)** The first item in the queue is Sam, and Sam is next up to be removed from the queue or to have their request processed. In the next step, we remove items from the queue, starting with Sam, then Anna, Heidi, Sally, and finally Charlotte. We process the items in the queue in order. Queues are good for storing the order in which something happened or storing the order of things that need to happen. In this case, we stored the order of people in line and processed each of their requests in order. Adding to the back of the queue and removing from the front of the queue takes very little time because the queue has a LinkedList implementation. However, a queue really only shows its advantages when you want to use it in this manner where the first item in is the first item out.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (4)
> **Analogies:** just like (1), similar to (1)
> **Code Identifiers:** isempty (1)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Mastering hash-based structures](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-hash-based-structures?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-hash-based-structures?u=76281980&t=0)** - Hash-based structures are the most commonly used [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) in technical interviews. There are a few Hash-based structures in [Java](../../Skills/Software%20Development/Java.md) including Hashtables, HashSets, and HashMaps. But they're very similar when it comes to their core functionality. There are differences lie in synchronization, usages of Knoll and whether or not duplicate values are allowed. But diving into those differences is out of the scope for this course. Generally, you should use a HashMap for your interviews because it provides the most flexibility by allowing duplicate values and an old key. A HashMap is also not synchronized, which means it has better performance than the synchronized Hashtable, but it's not thread safe. So let's talk about HashMaps. A Hash map consists of key-value pairs and uses a hash function to store and organize its data. A hashing function takes a group of characters called a key and maps them to a hash or a hash value. The hash determines where in the map, the object is stored. This means as long as the user of the Hashtable is aware of the key, retrieving the object is extremely fast. Essentially a HashMap is a data structure with key-value pairs where a hashing function is used to locate the element. For example, you might have the keys Z to associate with the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) Zebra. Zebra would be stored in your Hash structure as a value. And its key would be Z.
>
> **[1:34](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-hash-based-structures?u=76281980&t=94)** This means when you want to retrieve the object, Zebra you simply call the Hash-based structure with the key Z. However, sometimes you can have two keys that hash to the same value. And this is where you have Hash Collisions and this Hash structure. You could also have a word zoo with the same key Z. Here Z would mount both to Zoo and to Zebra. The way we solve for this is for each key we would have a list data structure that holds all of the values associated with that key. Now that we've talked about this in theory let's try working with this in code. Like the other built-in data structures we'll need to import java.util.HashMap in order to use it. In this sample code, we've created a HashMap called wordToNum, using the HashMap constructor. This HashMap will map the letters of a number to the actual numeric value. Here we add FIVE key value pairs into the HashMap using the put() function. The put() function is how we insert items into a HashMap. The keys are of type string and the values are of type integer. To retrieve an element we use the get() method in the input key on the HashMap. Here the input key is the string THREE. So we'll get the appropriate value which is the integer 3 back from the HashMap.
>
> **[3:07](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-hash-based-structures?u=76281980&t=187)** We can also list out the values and keys in a given HashMap. To retrieve the values we use .values for the keys we use .keySet. To remove elements we can use the key. Here we removed the key pair associated with the key FOUR. This will not only remove the string FOUR key, but also it's associated integer 4 value. Let's run this program and see it in action The first thing we do is print out the value associated with the key THREE. That's the integer 3. We see that in the console looking good. Then we print out the values. And so that's 5, 1, 4, 2, 3. Those are all the values in our HashMap. The order is not guaranteed, so this looks good. For the keys we see each key associated with the value FIVE, ONE, FOUR, TWO, THREE all in a word string format. Then we removed the key pair associated with the key FOUR. So that's FOUR the key as well as 4 the value which we see in the console. To prove that it's removed let's paste these print statements below our remove statement. The values in key set should no longer include the key and value 4. Running the program again, FOUR is no longer a key and it's no longer a value. When working with HashMaps. You'll notice data is stored in such a way that searching
>
> **[4:41](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-hash-based-structures?u=76281980&t=281)** for values is much faster than many other data structures. Insertion and deletion is also quick but hash structures do take up more space in memory.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (3), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (3), [Java](../../Skills/Software%20Development/Java.md) (2)
> **Env Vars:** four (6), three (3), five (2), two (1)
> **Code Identifiers:** wordtonum (1), keyset (1)
> **Exercise Files:** sample code (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - hash (1)

#### [Solution: Generate Binary Numbers](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-generate-binary-numbers-23732175?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-generate-binary-numbers-23732175?u=76281980&t=0)** (bright music)
>
> **[0:05](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-generate-binary-numbers-23732175?u=76281980&t=5)** - [Instructor] Let's create a program that returns an array with the first n binary numbers. Here we have a function called generateBinaryNumbers, and it takes an int to n. Given we know we want to return an array of n length, we can create a placeholder variable for it. If our input happens to be 0, our work here is done. We can just return the result array because it'll be an empty array. But now for the core implementation. One way to generate the first n binary numbers is by using a for-loop and converting each number, starting at 1 from the decimal system to the binary system. The Integer class in [Java](../../Skills/Software%20Development/Java.md) has a method called toBinaryString, and we can use that to convert I to a binary string. However, since we want to have an array of n's, we'll need to reparse the string into an Integer. Then we'll add it to our array. We use index i -1 because we start our four loop variable. i at one, but our array starts at 0. This puts our first binary number at the first slot within the array. As the loop continues to iterate, we'll fill all the spots in the array. At the end, we'll return it. Let's run it. This implementation works as expected. The time complexity is O of N login because we loop through
>
> **[1:42](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-generate-binary-numbers-23732175?u=76281980&t=102)** and use Integer.toBinaryString, which has a time complexity of login. Now, if you come up with a solution, your interviewer might say, try implementing it without Integer.toBinaryString. This is where things can get a little more complicated. If we look at the generated numbers, you'll notice a pattern. We add an item to the array. In this case, 1, and then that same item with the 0 appended to it, and then a 1 appended to it. Then look at the second item. After all the items associated with the first item have been added, we add the second item with a 0 appended to it, and then a 1 appended to it. This pattern follows throughout the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the binary numbers. We can leverage this pattern and use a Queue to generate the first n binary numbers in our code. Let's implement this algorithm. Like with the other, we'll start off by creating a result array and checking our base case. With an input greater than one, we can create a Queue that'll help us generate the next item to be printed. Queue and LinkedList come from java.util so we'll import them at the top. The first item we'll add to our queue is 1 because this is the first binary number. Then we'll get into the iteration. If we remember from our pattern,
>
> **[3:15](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-generate-binary-numbers-23732175?u=76281980&t=195)** we add an item to the array, and then add that item with a 0 appended to it and then a 1 appended to it to the queue. That's exactly what we'll do here. We'll remove an item from the queue, save it in a variable, and then add it to the array. For the other items that will be added to the Queue, we'll do a mathematical operation. To create a new number that has the current with the 0 appended to it, we multiply the current by 10. To create a new number that has the current with the 1 appended to it, we multiply the current by 10 and add 1. We add both of these items to the queue because they will be the second and third items or the second and third binary numbers to be added to the array. Now just because we save the next item to be added doesn't mean we actually add it to the array. We only want to add the first n binary numbers, so that's why we use a Queue and our iteration goes till end. Once we've added all of our numbers, we'll return the array. Let's run it. Our code still works. This implementation also runs in linear time with its single for-loop, but it does use a little more space due to its storage with the queue. So which implementation is better? The first implementation with the Integer.toBinaryString is certainly cleaner and easier to understand
>
> **[4:49](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-generate-binary-numbers-23732175?u=76281980&t=289)** because the binary number generation is abstracted to you. It's likely something you'd use in practice in a code base. However, the second implementation shows a deeper understanding of [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) as well as pattern matching, and it's more efficient. Presenting both options to your interviewer and discussing the differences between them is a great way to proceed as it shows your versatility as a developer.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1)
> **Code Identifiers:** tobinarystring (4), generatebinarynumbers (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright music) (1)

#### [Solution: Matching Parentheses](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-matching-parentheses-23730203?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-matching-parentheses-23730203?u=76281980&t=5)** - Let's implement a function that determines if a piece of text has matching parentheses. To start off, we'll create some [Data Structures](../../Skills/Software%20Development/Data%20Structures.md). We'll use a stack to help us keep track of the parentheses symbols as we iterate through the string. We'll also use a map to give us a correspondence between a given open symbol and closed symbol. Since a square bracket can't close an error bracket or vice versa, the opening symbol has to be of the same type as the closing symbol. Next, we'll iterate through the string. We'll grab the current character with S.char at, and we'll save it in a variable called current. We can check if current is an opening symbol using the values in our map. We'll use contains value to check if current is one of these opening symbols. If it is, we'll push it onto the stack. This will help us keep a record that we've seen an opening symbol. when we see the appropriate closing symbol we'll pop this character off the stack to complete the match. Now to check if the current character is a closing symbol, we'll use the keys in our block symbols map. If it's a closing symbol, we should check what's on top of the stack and see if there's a corresponding
>
> **[1:39](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-matching-parentheses-23730203?u=76281980&t=99)** opening symbol to go with it. However, the stack might be empty, so we'll need to account for that. With this code, we're actually doing three different things. We check to see if the stack is empty. We pop off an item from the stack if it's not empty, and then check if the item we popped off is valid. If it is valid, all of our work is done because the corresponding symbol has been popped off the stack. If it is not valid, we should return false. This means we saw an opening symbol, but it was of a different type than the closing symbol. With this implementation I use and and or operations to collapse the nested if statements and make this code a little more readable. But you're welcome to use nested ifs here instead. Now, if the current character is not a block symbol, either opening or closing, we do nothing and continue to iterate through the loop. At the end, we'll return whether or not the stack is empty. If the stack is empty, then every opening symbol has a corresponding closing symbol. If there was ever a mismatch of opening and closing symbols, then the function would've returned earlier in the iteration. If there were more opening symbols than closing symbols, then the stack would not be empty and the function would return false because the opening and closing parentheses did not match.
>
> **[3:15](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-matching-parentheses-23730203?u=76281980&t=195)** Let's run the function and see if it works. Our code works as expected. This is only one way to solve the matching parentheses problem, but there are many other solutions. This solution runs an O of N time and space. If we consider the number of parentheses, symbols as a constant, that makes it fairly efficient. There are other iterations of this problem as well. What if there was only one opening and closing symbol type? Could you make it work in O of one space? Thinking about the different versions of this problem you might receive and how you could solve them can help you prepare for your next technical interview.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1)
> **CLI Commands:** make (2)
> **Definitions:** is an  (1), is a  (1)
> **Cross-References:** earlier in (1)
> **Speakers:** - let (1)

#### [Solution: Find Most Repeated Word](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-find-most-repeated-word-23732176?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-find-most-repeated-word-23732176?u=76281980&t=0)** - [Instructor] Let's create a program that finds the most repeated [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) in a given piece of text. We'll start by normalizing the string with the helper function. This function will do a few data transformations for us. First, we'll convert the text to lowercase and trim off the sides of the input. Then we'll take out the delimiters using the split method. This will convert our input into individual words. Next, we'll have to account for the common words we want to ignore. To filter out these words, we'll use the streams API. We'll use arrays.stream to convert the words into a stream. Then we'll filter and only keep the words that are not a common word and that are not empty. Then we'll collect the common words into a list and return it. We could add a base case that checks for empty text at the top of the function, but it seems unlikely that a lot of users would call this function with an empty input. That's a great thing to discuss with your interviewer. If it's common, it might be worth doing that extra check here. However, with our current implementation, if the input is empty, the list will return empty anyways.
>
> **[1:35](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-find-most-repeated-word-23732176?u=76281980&t=95)** Let's normalize our input. It'll give us the uncommon words. If there are no uncommon words, we'll return the empty string. But if there are uncommon words, we can begin to calculate the most repeated word. To do this, we'll use a hash map. It'll keep track of the words we've seen so far, and how many times we've seen them. In our implementation, we'll loop through the uncommon words, adding them as keys to our hash map. Since we're using a hash map, we'll need to add an import at the top. Now, if a word does not exist in the map, we'll give it the default value zero and increment it by one. If the word does exist in our map, we'll retrieve its value and also increment it by one. It'll replace the word's current value in the map. After iterating through the words, we can find the most repeated word by using collections.max. We'll be using collections and map for this. Let's look at this implementation. Here we retrieve the word count map entry set, which is a set containing each word and its count. Then we compare each entry by its value,
>
> **[3:11](https://www.linkedin.com/learning/nail-your-java-interview-2/solution-find-most-repeated-word-23732176?u=76281980&t=191)** and retrieve the entry with the maximum count. To find the most repeated word, we retrieve the key for this entry and return it from the function. Let's run the code. And it works as expected. The time complexity of this algorithm is O of N, because we iterate through our input a constant number of times. We can also consider the length of the common words in relation to our input, but if our input is very large, the number of common words becomes a constant.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (10)
> **Non-Speech:** (typing) (5), (upbeat music) (1)
> **CLI Commands:** find (2)
> **Env Vars:** api (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 6. Solving Technical Interview Questions: Abstraction, Encapsulation, Inheritance, and Polymorphism

[↑ Back to Table of Contents](#table-of-contents)

#### [Mastering abstraction for technical interviews](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-abstraction-for-technical-interviews?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-abstraction-for-technical-interviews?u=76281980&t=0)** - [Instructor] In this chapter, we'll be looking at the four main principles of [object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md). Since [Java](../../Skills/Software%20Development/Java.md) is an object-oriented language, many of these concepts are commonly used, and they're fair game for your interviewer to ask about. While most technical interview challenges focus on [Data Structures](../../Skills/Software%20Development/Data%20Structures.md), your interviewer may ask for a general example of one of these object-oriented principles. It's important to be prepared to talk about them. The first object-oriented principle we'll be looking at is abstraction. In [Software Development](../../Topics/Software%20Development.md), we use abstraction to hide implementation complexity. This complexity could be in the form of an API, design, or system, and the goal of abstraction is to generalize the features of a given system. If a software product uses abstraction, it should be able to provide a user with example input, output, and a broad description of what the system does, without going into the underlying technical details. Consider a person driving a car. When the person presses on the gas, they'll know it'll increase the speed of the car. However, the person doesn't know how pressing on the accelerator increases the speed mechanically. This is what abstraction is, generalizing complex functionality. Looking at abstraction in Java specifically, we have abstract classes. An abstract class is like a template for a class, where some of the functionality is not implemented yet.
>
> **[1:35](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-abstraction-for-technical-interviews?u=76281980&t=95)** You cannot instantiate an abstract class, but other classes can extend the abstract class and implement the appropriate functionality. One way we might use abstract classes in Java is to process and manage data from various tables in the cloud. Let's say different tables exist, but they manage their data in fairly similar ways. To do this, we can create an AbstractTableManager class and create concrete classes for each type of specific table manager. Looking at some code, we have an abstract table class with a few abstract methods. Some of them are commented out, and we'll get to those in a minute, but let's start with the getTableName method. This method will be implemented by the concrete classes and return the appropriate table name. The operations on a given table will be pretty similar, so we've implemented the putItem method. It doesn't do anything in this example, but in theory, it would access the appropriate table in the cloud and put our data into it. To use this abstract class, we create another class called UserTableManager that is a concrete class. It will be a concrete class that extends the AbstractTableManager class and implements the abstract methods, here, getTableName. This TableManager will store user data. User data is just a plain old Java object
>
> **[3:09](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-abstraction-for-technical-interviews?u=76281980&t=189)** with getters and setters for username and email attributes. The table name will be USERTABLE, and we've written that here for the getTableName implementation. As we add on generic operations to the TableManager class, we'll be able to use them with our UserTableManager, as well as with other classes that extend the TableManager class. Some of the example methods we could add on are operations to retrieve and remove items, as we see here with the commented out methods. This allows us to store table operations for cloud tables in one place, rather than multiple places throughout the code. It's commonly used, but also slightly complicated, so having it accessible in one place is ideal. It's also generic enough of an operation to be used with many different tables, rather than just one table. Speaking of cloud tables, we also have an abstract getDBClient method commented out. Here, we put the responsibility of creating a client to talk to our cloud database on the concrete class, rather than the abstract class. We haven't implemented the full functionality here to provide simplicity on the abstraction idea, but with an abstract class, you can implement functionality in the abstract class or push it out to the concrete classes. Let's look at how we might use this class in theory.
>
> **[4:44](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-abstraction-for-technical-interviews?u=76281980&t=284)** We create a userTableManager object along with UserData. To insert the data, we call putItem on the UserTableManager. We haven't hooked up enough code together for our program to actually insert data into the cloud, but we can see it execute through the putItem method. Let's run it. And there's our insertion into the user data table. Now we can't directly instantiate the AbstractTableManager class itself, because it is abstract. The getTableName method does not have an implementation in the AbstractTableManager, so it cannot be created as an object itself. So why is the AbstractTableManager class useful? One major reason is that the AbstractTableManager class allows us to place the TableManager [Algorithms](../../Skills/Software%20Development/Algorithms.md) in one place and classify them as TableManager operations. As we saw with the UserTableManager, other concrete classes can use these algorithms and not worry about the implementation, since they're already heavily tested with their usages in other concrete classes. These algorithms also don't have to be rewritten every time we want to create a new TableManager. Ultimately, there's a bunch of different ways you could represent cloud tables, data, and TableManagers in your program. But one main goal you should have
>
> **[6:16](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-abstraction-for-technical-interviews?u=76281980&t=376)** in contributing to a code base is writing code that is easy to read and easy to contribute to. With this implementation, if there's ever a new type of table that's created and has the same format as the other tables, a new engineer can reference the concrete TableManager classes. They can also reuse the frequently called but complex table operations and the AbstractTableManager class.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (4), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (3), [Object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) (1), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1), [Software Development](../../Topics/Software%20Development.md) (1)
> **Code Identifiers:** gettablename (4), putitem (3), getdbclient (1), usertablemanager (1)
> **Env Vars:** api (1), usertable (1)
> **Definitions:** is an  (1), is a  (1)
> **Cross-References:** as we saw (1)
> **Exercise Files:** template (1)
> **Speakers:** - [instructor] (1)

#### [Add encapsulation to your programs to impress interviewers](https://www.linkedin.com/learning/nail-your-java-interview-2/add-encapsulation-to-your-programs-to-impress-interviewers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/add-encapsulation-to-your-programs-to-impress-interviewers?u=76281980&t=0)** - [Instructor] Another important object-oriented concept is encapsulation. Encapsulation is about binding an object's state and behaviors together. It's a way of wrapping data and code acting on the data into a single unit. In [Java](../../Skills/Software%20Development/Java.md), this would be a class. To prevent classes from being tightly coupled, we want to keep related data and behaviors together inside of a single unit class. With encapsulation, you can make a class's variables and data hidden from other classes. The data would still be accessible, but only indirectly through specific methods of the class. This is known as data hiding. In Java, we can achieve this by declaring all the fields in a class as private and writing public methods in the class to get and set the values of these attributes. With this, other classes can still access the hidden data, but they can only do so through a public method of the given class. Let's take a look at an example in code. Here, we have a Hogwarts student class with name and house data. HogwartsHOUSE is just an enum with four different Hogwarts houses that a student could be assigned to. The student class generates the house data piece using the hashcode of the provided name in the constructor. The class also provides a few public getter methods to access the data.
>
> **[1:36](https://www.linkedin.com/learning/nail-your-java-interview-2/add-encapsulation-to-your-programs-to-impress-interviewers?u=76281980&t=96)** Since the data pieces are private, the only way to access the data is through the public methods. You might notice there are no setters in this class. We do this so that the Hogwarts student instances become immutable once constructed with the constructor. No changing houses or changing names. This class encapsulates the private data pieces that are inside of it. Now, let's use it. Let's create a Hogwarts student in our main class. In our main class, we have a Hogwarts student named Elizabeth Blackburn that's 11. To access the different data pieces within the student object, we have to use the public methods or the public getters provided in the Hogwarts student class. Here we use getName and getHouse to grab the data because we cannot directly access or modify the data. If we try to access the data directly, we'll get some errors. This is because each piece of data in our class has the private access modifier. The Hogwarts student class encapsulates the data inside of it. We hide the implementation of the name and the house data through the public method. Now, why do this? We use encapsulation so that the user has no idea of the inner implementation of the class and the data it contains. The user doesn't know how the student gets assigned to the house and it doesn't have access to the data directly.
>
> **[3:12](https://www.linkedin.com/learning/nail-your-java-interview-2/add-encapsulation-to-your-programs-to-impress-interviewers?u=76281980&t=192)** The user only knows how to retrieve the data using the getter method. This type of implementation can help keep classes separated. It's important to remember that encapsulation is different from abstraction. Abstraction provides generalization, while encapsulation hides unwanted implementation details from the actual users of an object. We write encapsulated code to improve the reusability of our programs. We want to make it easy to change the code when new requirements come along. What happens if we need to add a new Hogwarts house? Only one class needs to be changed in this case rather than multiple. Encapsulation also makes [Unit Testing](../../Skills/Software%20Development/Unit%20Testing.md) much easier because each piece is less coupled.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2), [Unit Testing](../../Skills/Software%20Development/Unit%20Testing.md) (1)
> **CLI Commands:** make (2)
> **Code Identifiers:** getname (1), gethouse (1)
> **Definitions:** known as (1)
> **Speakers:** - [instructor] (1)

#### [Leveraging inheritance in your technical interview solutions](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-inheritance-in-your-technical-interview-solutions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-inheritance-in-your-technical-interview-solutions?u=76281980&t=0)** - [Instructor] The third foundational object oriented principle is inheritance. Inheritance is the process where one class acquires the fields and methods of another through extension. With inheritance, we can write out the common properties and functionality in one class and have other classes with unique features, all extend the single class, making code more reusable. Instead of writing the same common implementation multiple times, we can write it once. And then whenever a class needs the functionality, extend that class. We've actually already seen inheritance a little bit in the previous lessons. With our user table manager class, we extended the abstract class. Inside abstract table manager, we had a few methods that the user table manager inherited and had access to. Those included, put item, get item, remove item. This is one way to use inheritance. Provide the [Algorithms](../../Skills/Software%20Development/Algorithms.md) in one class, other classes extend that class, and can have access to this algorithms. To understand this concept a little further, let's take a look at another example. Let's say I want to have a class that represents an ArrayList. But the list should only contain non-negative numbers. We want most of the functionality from the ArrayList class but we want to change or override certain components. Let's start by creating a new class
>
> **[1:35](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-inheritance-in-your-technical-interview-solutions?u=76281980&t=95)** called non-negative ArrayList. Non-negative ArrayList. And it's going to extend the ArrayList class. Our list will only contain non-negative numbers or integers. So, we'll put integers as our data type for ArrayList. Then we'll import the class. With this extension, our non-negative ArrayList class has all the functionality from the ArrayList class. In order to make sure the contents of the list are non-negative, we'll need to change the functionality and the constructor as well as the add methods. Let's make a constructor, and its input will be a series of numbers. So non-negative ArrayList. To do a series of numbers in [Java](../../Skills/Software%20Development/Java.md), we just do the data type. In this case, integer dot dot dot. Our input will be called numbers. And it'll ultimately be converted to an array inside of our implementation. We'll want to construct our list with only non-negative numbers. So, we need to filter out anything that's not a non-negative number. To do that, we can use the streams API. We'll say Arrays.Stream pass in our numbers. We'll filter and only keep the numbers where N is greater than or equal zero. That's the definition of non-negative. We'll need to import arrays. We'll collect the results of the filter
>
> **[3:08](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-inheritance-in-your-technical-interview-solutions?u=76281980&t=188)** and we'll put it inside of a list.
>
> **[3:17](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-inheritance-in-your-technical-interview-solutions?u=76281980&t=197)** Now, this creates our non-negative list. But we need to pass it into the super constructor. Super calls the superclass or the parent classes constructor. In this case, that's the ArrayList constructor and that's constructing our list. The ArrayList class takes care of everything else from here. We just give it the valid numbers. We also call the non-negative ArrayList class a subclass, because it's extending our superclass or our ArrayList class. It's extending it, but changing some of the functionality. Great. Now we just need to change this class. So, only non-negative numbers can be added to the list. To change that functionality, we'll need to override certain functions that add items to the list. We'll need to match their methods signatures exactly while modifying their implementation. We can still use the original functionality with the keyword super to access the original ArrayList implementation. Let's see how this works. We'll want to override the functionality of the add method. So, we'll write public add. Matching that signature, it outputs void.
>
> **[4:40](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-inheritance-in-your-technical-interview-solutions?u=76281980&t=280)** And here ,all we do is verify the element is valid for our list, and then add it to the list using the original ArrayList implementation. If the item isn't valid, we simply don't add it to our list. Now, the ArrayList class has quite a few methods to add items to the list. To save some time, I've overwritten them here, but we'll walk through each of these methods. For the second add method, we verify the element is valid. And then again, use the ArrayLists implementation to actually add the item to the list. Next, we have the methods that let you add collections to the list. Here, we filter out the invalid items in the collection and then add it to the list leveraging the ArrayLists implementation. We also modify the set functionality so that only valid elements can be inserted. And for Replace All, we've run the operation immediately and then remove the invalid elements. Let's try this out in our main class. Here, we create a list with negative and non-negative elements. Then we run a few operations on it. We are using the add operations to test our custom functionality. But you could use any of the ArrayList operations on our non-negative ArrayList object, if you wanted to. That's the benefit of inheritance. Let's run this program in debug mode
>
> **[6:14](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-inheritance-in-your-technical-interview-solutions?u=76281980&t=374)** and see inheritance in action. Time to step through the program. First, we construct the list. Notice, in our debugger, the negative numbers are filtered out. We'll step over again, and these should be printed to our console. We only see two, three, and zero. Now, let's try adding some elements. Here, we try adding one. We'll step over. One is not added because negative one is not a non-negative number. We try to add nine. That is a non-negative number. And so, it's added to our ArrayList. For the collection, only 12 and four should be added. And that's what we see in our debugger, 12 and four. Even in the case of our iterators, the appropriate add and set methods will be called. We'll step over, and negative 19 is not added. Finally, the last operation. We feed the Replace All method a consumer that will modify the items in our list. In this case, subtract each item by one. We do have zero as an integer element in our list. So that will be removed after the Replace All functionality takes place. This means we'll only have five items in our list. And that's exactly what happens.
>
> **[7:49](https://www.linkedin.com/learning/nail-your-java-interview-2/leveraging-inheritance-in-your-technical-interview-solutions?u=76281980&t=469)** All of our items were subtracted by one and we are left with five elements in the list. We'll step over again. And we'll see the new list in our console. We see our five items with no negative numbers. Using inheritance allows us to use the same attributes and behaviors across multiple classes without writing additional code. If you ever want to change the behavior, you can override the class and modify it, while keeping all of the other behaviors as we've done with our non-negative ArrayList class. By creating this non-negative ArrayList class, we do not have to do the extra If Checks around the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our code to check if a given number or collection added is a valid non-negative value. This prevents unwanted bugs. This is different from pure abstraction because both the ArrayList class and our non-negative ArrayList class are both concrete classes. Nothing is abstract. The main disadvantage of inheritance is that the superclass and the subclass can become tightly coupled. Meaning, they can't be used independently of each other. Also, the program extends increased effort to jump through all the levels of implementation to get to the appropriate functionality.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Algorithms](../../Skills/Software%20Development/Algorithms.md) (2), [Java](../../Skills/Software%20Development/Java.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** make (2)
> **Definitions:** is a  (2)
> **Env Vars:** api (1)
> **Speakers:** - [instructor] (1)

#### [Use polymorphism to reduce complexity in coding interviews](https://www.linkedin.com/learning/nail-your-java-interview-2/use-polymorphism-to-reduce-complexity-in-coding-interviews?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/use-polymorphism-to-reduce-complexity-in-coding-interviews?u=76281980&t=0)** - [Instructor] Polymorphism is the ability for an object or function to take many [Forms](../../Skills/Web%20Development/Forms.md). Depending on the context and the situation, the form may be different making your code more flexible and reducing complexity. [Java](../../Skills/Software%20Development/Java.md) supports two types of polymorphism, runtime, compile-time. First, we'll be looking at runtime polymorphism and we've already seen it quite a bit in this course even if you didn't realize it. Let's take a look. With our non-negative ArrayList example, we extended the ArrayList class. This makes the non-negative ArrayList be a non-negative ArrayList object as well as an ArrayList object. It takes these two forms. In our main function, we created an ArrayList of this class and added integers to the list. We also have a regular ArrayList and use the same method on it. Let's run it. Here we see that only non-negative items are added to the non-negative ArrayList and all the items are added to the regular ArrayList. When we instantiated our non-negative ArrayList object we made the data type of the variable a non-negative ArrayList. Since non-negative ArrayList is of type ArrayList as well, we could change this to just ArrayList instead. It holds integers, so we add integer inside the arrow brackets.
>
> **[1:33](https://www.linkedin.com/learning/nail-your-java-interview-2/use-polymorphism-to-reduce-complexity-in-coding-interviews?u=76281980&t=93)** So what happens now? Do the negative items still get added? Let's find out. We'll run the program and we get the same output. The non-negative ArrayList still acts like itself with its custom behavior even though it's taking the form of an ArrayList. This is called runtime polymorphism because the behavior of the object is determined at runtime. In our non-negative ArrayList class we override the add functionality with the new implementation. So that's what gets used. Runtime polymorphism is achieved by overriding functionality. This allows us to customize the functionality of an already created class with the new class without modifying the original class' code. It can also be useful if you want to do a similar operation on a series of objects with different implementations. Let's take a look at another example. Let's say I wanted to create a function that adds a random number to a list. I'd like to be able to add a random number to my regular ArrayList as well as my non-negative ArrayList. So I make the input type ArrayList. We can call the add method on our input list because add is in the ArrayList class it's functionality that's accessible to all ArrayLists regardless of the implementation let's call the function with both of our lists and run it.
>
> **[3:07](https://www.linkedin.com/learning/nail-your-java-interview-2/use-polymorphism-to-reduce-complexity-in-coding-interviews?u=76281980&t=187)** We print out the original lists, then we try to add a negative number to our non-negative ArrayList, and we do add a number to our regular ArrayList. However, add random number didn't really work for our non-negative ArrayList implementation. We tried to add a random number, but we weren't successful. This means we need to change up our implementation. We'd like to call add until a random number is actually added to the list. A number is added when the list's size is incremented. Let's create a variable for the list's size in this case size equals list.size and we'll make a loop. We'll continue to add until a random number has actually been added to the list.
>
> **[4:01](https://www.linkedin.com/learning/nail-your-java-interview-2/use-polymorphism-to-reduce-complexity-in-coding-interviews?u=76281980&t=241)** We'll continue to add items until list outsize is greater than the original size. If the dynamic list.size is greater than we must have added an item. It's also interesting to note that when this is used with our non-negative ArrayList class we use the ArrayList's implementation for dot size but we use the custom classes implementation for the add functionality. Similar to our non-negative ArrayList class, I could create a positive ArrayList class and even number ArrayList class and more. And they can all use this functionality to add a random number to their list. That's because we define the addition of a random number by if the size of the list gets bigger. Let's run the code again.
>
> **[4:56](https://www.linkedin.com/learning/nail-your-java-interview-2/use-polymorphism-to-reduce-complexity-in-coding-interviews?u=76281980&t=296)** Here we try to add -84 to the non-negative ArrayList and it doesn't work. So we call it again and add 10 to the non-negative ArrayList. 10 is valid, so it actually gets added. And that's what we see in our output. For the regular ArrayList. Any number can be added, so we directly add 84. For the non-negative ArrayList object, we continue to generate an add numbers until a number is finally added or the size of the list increases. Using an ArrayList as our input data type and defining the addition on the properties of an ArrayList reduces the complexity and makes our code more reusable.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Forms](../../Skills/Web%20Development/Forms.md) (2), [Java](../../Skills/Software%20Development/Java.md) (1)
> **CLI Commands:** make (2), find (1)
> **Definitions:** is called (1)
> **Analogies:** similar to (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Mastering polymorphism with a deeper look at Java](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-polymorphism-with-a-deeper-look-at-java?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-polymorphism-with-a-deeper-look-at-java?u=76281980&t=0)** - [Tutor] In this lesson, we'll take a deeper look at the functionality, built into [Java](../../Skills/Software%20Development/Java.md) to see how it's benefiting from using polymorphism. We'll start with the example from the previous lesson with our non-negative array list. This class took on the form of an array list as well as the form of a non-negative array list, depending on what functionality was called. There are actually even more [Forms](../../Skills/Web%20Development/Forms.md) that a non-negative ArrayList class can take. Let's look at the ArrayList class. Here, we see the ArrayList class extends abstract list and implements a few interfaces. The E and the arrow brackets has to do with the topic called generics. We won't dive into generics here but it's what allows you to create array list with integer string or any data you like. The data type inside the array list doesn't matter, but it does have to be of a single type. Abstract list is an abstract class that would be interesting to look at later if you're looking for more examples of abstract classes, but let's dive into the list interface. An interface is a set of method signatures for, to be implemented functionality. It's kind of like a specification for a set of behavior without the implementation. So like an abstract class, an interface cannot be instantiated. However, unlike an abstract class and interface can only implement static methods. It cannot contain implemented instance methods.
>
> **[1:37](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-polymorphism-with-a-deeper-look-at-java?u=76281980&t=97)** The instance methods can only be specifications that are later implemented by a concrete class. In order to use an abstract class we have another class extended. To use an interface we have another class implemented. Classes that implement an interface must implement all of the unimplemented methods in that interface. In the case of the list interface the ArrayList class must implement size, is empty, contains, iterator and so on. And we see that here. All of the unimplemented methods in the list interface are either implemented here in the ArrayList class or in a class that extends, perhaps abstract list. In addition to implementing these methods the ArrayList class also adds new functionality. It builds up a specific implementation of a list. In this case, a list built out of an array. This means if I go to the main class we can change array list to just list containing integer. We'll need to import the appropriate functionality, but it's again another form for my non-negative array list object to take. Looking at the ArrayList class it could also take the form of cloneable, random access or serializable since it implements all of their interfaces through the ArrayList class.
>
> **[3:13](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-polymorphism-with-a-deeper-look-at-java?u=76281980&t=193)** We could even change this to just cloneable. And we get an error because there's no ad method in the cloneable interface. Functionality is restricted based on the data type specification. Cloneable does not have a specification for an ad method, even though it does exist for my object. This is an important consideration when working with different forms an object can take. Let's switch this back to list and add some more functionality to our non-negative ArrayList class. What if I wanted to construct an instance out of another list class? The list could be a non-negative array list, a linked list, an array list. Any class that implements the list interface. Currently, we only have functionality to create a non-negative array list out of a series of integers or an integer array. Let's create another constructor in our custom class. To do this, we'll write public non-negative array list. And the input will be of type list containing integers. We'll call it list. And import list into our class. This allows us to feed in any type of list to the non-negative array list constructor. On this input I can use any method that's listed in the list interface. One of those methods is stream. So we'll write.stream, we'll filter out the invalid values. We'll construct a new list with collect
>
> **[4:55](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-polymorphism-with-a-deeper-look-at-java?u=76281980&t=295)** and we'll pass this into our super constructor. Now, if we look to the root of where the stream functionality lives it actually lives in the collections interface. The list interface extends the collections interface. Now, since we only require the streams method from our input we can change the data type of our input from list to collection. So instead of list, we'll have a collection of integers. This makes our constructor more reusable because now we can turn any type of collection, rather than just any type of list into a non-negative array list. Let's test this out in our main function. Here we create a hash set in an array list that are both collections. Then we turn them into non-negative array lists. In the printouts we should only see non-negative numbers. And it looks good. Now you may not have realized it but we're using compile time polymorphism in this example. Since we have two constructors in our non-negative ArrayList class, the appropriate constructor used to build the list is determined at compile time, based on the inputs type. For our collection inputs the constructor that takes in a collection object is used. If a series of integers or an array is provided as input
>
> **[6:32](https://www.linkedin.com/learning/nail-your-java-interview-2/mastering-polymorphism-with-a-deeper-look-at-java?u=76281980&t=392)** the other constructor is used. We call this method overloading because we are using the same method name, in this case the constructor with two different sets of parameters. If you want to have multiple ways to manipulate an object with different sets of inputs or outputs, method overloading provides a way for you to do this. For method overloading to work you do have to have the same name for the method. Otherwise the method would not be overloaded. It would just be a completely different method. Instead of having the repeat streaming functionality we can convert the array to a list and call the collections input constructor. This makes our code more maintainable because the main constructor logic is in one place. Taking a step back, the ArrayList class itself also has a few overloaded methods, including add an add all which we override in our non-negative ArrayList class. Overloaded methods can make your code cleaner. They're fast because they're bonded during compile time which is an earlier phase than runtime and they make your code more flexible by providing multiple ways to use similar functionality.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Forms](../../Skills/Web%20Development/Forms.md) (2), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Definitions:** is an  (3), is a  (1), we call this (1)
> **Documentation:** specification (3)
> **CLI Commands:** make (2)
> **UI Navigation:** go to (1)
> **Analogies:** kind of like (1)
> **Speakers:** - [tutor] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Good luck in your interview](https://www.linkedin.com/learning/nail-your-java-interview-2/good-luck-in-your-interview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/nail-your-java-interview-2/good-luck-in-your-interview?u=76281980&t=0)** - [Kathryn] Congratulations. You finished the course. We started out by looking at the foundations of [Interviewing](../../Skills/Data%20Science/Interviewing.md) for [Software Development](../../Topics/Software%20Development.md) roles. Then we dove into some [Java](../../Skills/Software%20Development/Java.md) specific-topics that you might be asked about in your interview. We explored some new Java features that developers are using in the field and learned more about how Java works under the surface. We also reviewed the built-in [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) and mastered some key object-oriented concepts to get you ready for the coding portion of your interview. To top it off, we got some great practice in with a few technical interview challenges and solutions. Thank you for watching this course and good luck in your interview.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3), [Interviewing](../../Skills/Data%20Science/Interviewing.md) (1), [Software Development](../../Topics/Software%20Development.md) (1), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1)
> **Speakers:** - [kathryn] (1)


## Instructor

- [Kathryn Hodge](../../Instructors/Software%20Development/Kathryn%20Hodge.md)

## Resources

- Exercise files available

## Skills Covered

- Tech Career Skills
- Interview Preparation
- Java

## Path Context

### In [Building Your Java Skills](../../Paths/Cloud%20Computing/Learning%20Paths/Building%20Your%20Java%20Skills.md)
← [Continuous Delivery for Cloud Native Java Apps](Continuous%20Delivery%20for%20Cloud%20Native%20Java%20Apps.md) | **6 of 6**

## Appears In

- [Building Your Java Skills](../../Paths/Cloud%20Computing/Learning%20Paths/Building%20Your%20Java%20Skills.md)

## Related Courses

_Courses sharing skills:_

- [Preparing To Get A Job In Data Analytics](../Data%20Science/Preparing%20To%20Get%20A%20Job%20In%20Data%20Analytics.md) — Tech Career Skills, Interview Preparation
- [Nail Your DevOps Interview](Nail%20Your%20DevOps%20Interview.md) — Tech Career Skills, Interview Preparation
- [Nail Your C- Interview](../Software%20Development/Nail%20Your%20C-%20Interview.md) — Tech Career Skills, Interview Preparation
- [Get Ready for Your Coding Interview](Get%20Ready%20for%20Your%20Coding%20Interview.md) — Tech Career Skills, Interview Preparation
- [Nail Your Cybersecurity Interview](Nail%20Your%20Cybersecurity%20Interview.md) — Tech Career Skills, Interview Preparation

---

[↑ Back to top](#)