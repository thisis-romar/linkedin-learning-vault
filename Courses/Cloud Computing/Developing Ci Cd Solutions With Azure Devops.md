---
type: course
cssclasses:
  - lle-course
slug: developing-ci-cd-solutions-with-azure-devops-22123562
url: "https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562"
exercise_files: false
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQEyONPb0Cg_rg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1680204655176?e=2147483647&amp;v=beta&amp;t=yD2ESyqmsN6fzBzSMjnZFYcEO4I2lv0q2kRakWJLA7U"
linkedin_topic: Cloud Computing
learning_paths:
  - '[[Advancing Your Azure Developer Skills- Exploring Complex Application Development]]'
  - '[[Getting Started as an Azure Developer]]'
prev_courses:
  - null
  - '[[Azure DevOps for Beginners]]'
next_courses:
  - '[[Migrating to Azure]]'
  - null
path_nav: '[{"path":"Advancing Your Azure Developer Skills- Exploring Complex Application Development","position":1,"total":7,"prev":null,"next":"Migrating to Azure"},{"path":"Getting Started as an Azure Developer","position":7,"total":7,"prev":"Azure DevOps for Beginners","next":null}]'
path_count: 2
tags:
  - course
  - topic/cloud-computing
  - topic/network-and-system-administration
  - topic/software-development
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Cloud%20Computing/Developing%20Ci%20Cd%20Solutions%20With%20Azure%20Devops.md)

![Developing Ci Cd Solutions With Azure Devops](https://media.licdn.com/dms/image/v2/D560DAQEyONPb0Cg_rg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1680204655176?e=2147483647&amp;v=beta&amp;t=yD2ESyqmsN6fzBzSMjnZFYcEO4I2lv0q2kRakWJLA7U)

# Developing Ci Cd Solutions With Azure Devops

> As a developer, you need to field a constant flow of requests for new features, updates, and bug fixes. By using Azure pipelines, you're able to build your applications and catch bugs early. In this course, Prince Mokut teaches you to configure a fully automated continuous integration and continuous delivery (CI/CD) pipeline in Azure. Prince starts with a high-level overview of Azure pipelines con

> [LinkedIn Learning](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562)
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Why use Azure Pipelines?](#why-use-azure-pipelines)
  - [What you should know](#what-you-should-know)
- [**1. Azure Pipelines**](#1-azure-pipelines) (3 videos)
  - [What really is Azure Pipelines?](#what-really-is-azure-pipelines)
  - [Key concepts of Azure Pipelines](#key-concepts-of-azure-pipelines)
  - [Define pipeline assets in Library](#define-pipeline-assets-in-library)
- [**2. Designing a Build Pipeline**](#2-designing-a-build-pipeline) (6 videos)
  - [Get your source code to Azure Repos](#get-your-source-code-to-azure-repos)
  - [Configure a build pipeline](#configure-a-build-pipeline)
  - [Integrate SonarCloud with the build pipeline](#integrate-sonarcloud-with-the-build-pipeline)
  - [Protect your important branches](#protect-your-important-branches)
  - [Create a task group for build pipelines](#create-a-task-group-for-build-pipelines)
  - [Simulate CI trigger and branching policies](#simulate-ci-trigger-and-branching-policies)
- [**3. Designing a Release Pipeline**](#3-designing-a-release-pipeline) (6 videos)
  - [An overview of a release pipeline](#an-overview-of-a-release-pipeline)
  - [Create web apps in Azure](#create-web-apps-in-azure)
  - [Configure a release pipeline for multistage deployment](#configure-a-release-pipeline-for-multistage-deployment)
  - [Reconfigure your production stage release to deploy to a staging slot](#reconfigure-your-production-stage-release-to-deploy-to-a-staging-slot)
  - [Release pipelines for staging slot deployments](#release-pipelines-for-staging-slot-deployments)
  - [Test release to deployment slots](#test-release-to-deployment-slots)
- [**4. Maximizing Azure Pipelines**](#4-maximizing-azure-pipelines) (1 videos)
  - [Configure notifications for successful deployments](#configure-notifications-for-successful-deployments)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Why use Azure Pipelines?](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/why-use-azure-pipelines?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/why-use-azure-pipelines?u=76281980&t=0)** - [Prince] [[Microsoft Azure|Azure]] Pipelines help with the build, test, and quick delivery of your software solutions to users. In this course, we will learn the concepts of Azure Pipelines and various features that can make your pipeline configuration easier. We will define the pipeline to build and deploy a .NET solution to Azure App Service. We will also integrate SonarCloud to scan our source code for any vulnerabilities and define a branch policy that checks for scan results before allowing modifications to be merged with a protected branch. I am Prince Mokut, a cloud architect and [[DevOps]] engineer with many years of experience designing pipelines for projects written in several programming languages. If you're ready to start building CI/CD workflows for your development team or [[Software Projects]], then come join me in my [[LinkedIn]] Learning course on Developing CI/CD Solutions with Azure Pipelines.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (4), [[DevOps]] (1), [[Software Projects]] (1), [[LinkedIn]] (1)
> **CLI Commands:** cd (2), make (1)
> **Env Vars:** net (1)
> **Exercise Files:** source code (1)
> **Speakers:** - [prince] (1)

#### [What you should know](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/what-you-should-know?u=76281980&t=0)** - [Instructor] Before we get started with this course, there are a few things you need to have. First download [[Git]] to your PC for [[Version Control]] of the source code and Visual Studio Code for making changes to the sample web application we will deploy to [[Microsoft Azure|Azure]] App Service. You also need to sign up on SonarCloud so we can use the code analysis feature. Next, you will need to sign up on Azure Portal and Azure [[DevOps]]. So visit the link displayed on the screen to a register profile.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (3), [[Git]] (1), [[Version Control]] (1), [[DevOps]] (1)
> **Tools:** visual studio (1), azure portal (1)
> **CLI Commands:** git (1)
> **Exercise Files:** source code (1)
> **Prerequisites:** you need to have (1)
> **Speakers:** - [instructor] (1)


### 1. Azure Pipelines

[↑ Back to Table of Contents](#table-of-contents)

#### [What really is Azure Pipelines?](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/what-really-is-azure-pipelines?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/what-really-is-azure-pipelines?u=76281980&t=0)** - [Instructor] Let's imagine your development team is building a new software application for the organization. You'd have several developers collaborating on a large project that could take multiple sprints to be completed. Consider the current toil might be waiting for the ops team to spin up new servers, manually copy compile codes or the software application to multiple environments, and fix up compatibility issues across several environments. How can we reduce toil for the dev team, enabling them to quickly test the application and get feedback? For the purpose of quickly making your software applications accessible to users, [[Microsoft Azure|Azure]] Pipelines provides a quick, easy, and safe way to automate building and deploying your projects. Almost any language or project type is supported with Azure Pipelines, like [[Node.js]], [[Python (Programming Language)|Python]], [[Java]], and even mobile applications. CICD or [[Continuous Integration (CI)|continuous integration]] and [[Continuous Delivery (CD)|continuous delivery]] are combined in Azure Pipelines to build and deliver your code to any target, whether infrastructure in any cloud platform or on-premises. Development teams utilize continuous integration in Azure Pipelines to automatically merge, build, and test their code.
>
> **[1:36](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/what-really-is-azure-pipelines?u=76281980&t=96)** Implementing CI helps to catch bugs early in the development cycle, which makes them less expensive to fix and lowers the possibility of bugs in the production environment. To verify quality, you can design automated testing scripts that can run as part of your CI process. The end product of a CI process is usually a build artifact which is then used by a released pipeline to support frequent or continuous deployments to environments. The build artifact is released to one or more test and production environments using a method known as continuous delivery. This process allows teams to deploy applications faster to several environments in the cloud or on-premises, enabling them to improve the quality of their software applications. When using Azure Pipelines to define your continuous delivery process, you can enable approval gates, check monitoring systems to determine the state of your production environment and integrate your team's change approval board process if you use a software tool to manage change approvals. All these things can be done before an artifact is deployed to any environment. Azure Pipelines provides a limited hours of free pipeline run every month for projects, so you can sign up for [[Azure DevOps Services]],
>
> **[3:13](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/what-really-is-azure-pipelines?u=76281980&t=193)** which is cloud-based, and choose single services like Azure Pipelines, Artifact basic plan, or basic test plans. Or you can run locally in your enterprise server by installing the Azure [[DevOps]] Express tool.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (8), [[Continuous Delivery (CD)|Continuous delivery]] (3), [[Continuous Integration (CI)|Continuous integration]] (2), [[Node.js]] (1), [[Python (Programming Language)|Python]] (1)
> **CLI Commands:** node (1), python (1)
> **File Paths:** node.js (1)
> **Env Vars:** cicd (1)
> **Definitions:** known as (1)
> **Analogies:** imagine (1)
> **Speakers:** - [instructor] (1)

#### [Key concepts of Azure Pipelines](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/key-concepts-of-azure-pipelines?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/key-concepts-of-azure-pipelines?u=76281980&t=0)** - [Instructor] [[Microsoft Azure|Azure]] Pipelines is simply an automation tool that replaces the manual process of building and deploying software applications. But this tool has several components which we will learn in a bit. In Azure [[DevOps]], there is the build and release pipeline. To access Azure DevOps, you can visit dev.[azure.com](https://azure.com) and sign up or sign in with your [[Microsoft Outlook|Outlook]] account. I'm already logged into my DevOps account and I've opened the sample project. To access the build and release pipeline, you can hover your mouse on the Pipelines icon and here you would have the pipelines and releases. If you want to create an automated workflow to build and test your software application, you use Pipelines. So let's click Pipelines and then click on New pipeline. Here you define the [[Continuous Integration (CI)|continuous integration]] process of your application, and if you're defining the pipeline using YAML syntax, then you can also define the continuous deployment process in the single workflow. So for instance, if my code is in Azure Repos and I would like to define my workflow using the YAML syntax, then I would select the Azure Repos [[Git]] as the source of my code.
>
> **[1:33](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/key-concepts-of-azure-pipelines?u=76281980&t=93)** If you don't wish to use YAML, you can select the use classic data option. In the next page, I would select my repository as you may I have more than one repo in this project. Then I'll select the appropriate template that fits my code. For example, [[Node.js]] for building my Node.js application only or Node.js Express Web App to [[Linux]] on Azure. If I want to define all the build, test and deployment workflow in this single pipeline. Choosing the workflow with deployment steps, you see I'll need to select my Azure subscription, click continue and then choose an existing web app in my subscription. Then click the validate and configure button. However, if I select the first option, which should only build my code and produce an artifact, then I don't have to immediately configure the deployment process. You can still manually define the deployment workflow in this YAML file, if you'd like. In this file, there is a trigger set to the default branch, which is the master branch. A trigger is defined to start up the pipeline when an event occurs. For example, run the pipeline when changes are pushed to the repo for a specific branch. A pool is a computing infrastructure where jobs would run
>
> **[3:08](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/key-concepts-of-azure-pipelines?u=76281980&t=188)** and in this case, can be a [[Windows]] or Linux server. A pool is also referred to as an agent, especially if you use the classic data option to define your pipeline. A step is where you define specific tasks or scripts to be executed when the pipeline is triggered. For instance, we have a task here to install Node.js and after that run [[npm]] install and build the code. You can add a set of tasks to test your application, as well as scan the code for vulnerabilities. The difference between a task and a script is that a script uses command line, [[Powershell]] or Bash to execute your custom code, while a task is more or less an abstracted package script. All you have to do in a task is to provide the input parameters. So if I click the last line and press enter and on the top right of the screen, I select Show assistant to search for npm and then select this npm task. Then I can change the command from install to any other command, and then provide the necessary parameters required here if compulsory, and then click the add button. Now comparing the script on line 18 with the task I just added, you see this starts
>
> **[4:44](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/key-concepts-of-azure-pipelines?u=76281980&t=284)** with a task key and the command is just installed. Now if you are defining your build and deployment workflow in the YAML file, you can use a stage two logically separate pipeline jobs. For example, you can have a stage for the build phase, another stage to deploy to dev, QA or production environment. I'll show you how to define a stage outside of a YAML file in releases. Now, once you're done defining your pipeline, you can click the save and run button at the top right corner. A run is simply an execution of a pipeline. Here all the tasks and scripts defined in the pipeline executed one after the other. You can also view the logs to see the progress and fix issues if errors occur during the run. Since our sample workflow only has a build definition and no deployment task, you can define the deployment workflow in releases. It's important to have a task in your YAML file to publish an artifact of the build code, which would then be deployed via the release pipeline. So let's quickly look at the releases. On the left side of the screen, I'll select the pipeline's menu and click releases. Click on the new dropdown
>
> **[6:21](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/key-concepts-of-azure-pipelines?u=76281980&t=381)** and then select new release pipeline. You'll find a number of templates to choose from. I would like to deploy to Azure Web App. So I'll select the Azure App [[Service Deployment]] and click apply. Recall earlier that I mentioned a stage is a boundary of a set of pipeline jobs. In releases, you must have at least one stage so you can name this stage as a dev and you can always rename this to anything later on. You need to select an artifact for your release pipeline to run. To do that, I'll click the add an artifact button. Select the build pipeline and then I can click the Add button. It'll always default to the latest version of the build pipeline. Once it's added, I can then set trigger conditions for this release pipeline. So I'll click the lightning icon and toggle the enable continuous deployment trigger. Doing this will cause my release to run any time there is an updated artifact from this particular build pipeline I selected. You can also set the build branch filters if you want the release pipeline triggered only for artifacts from a specific branch
>
> **[7:55](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/key-concepts-of-azure-pipelines?u=76281980&t=475)** and also enable pull request trigger if you're enforcing branch policy following any branching strategies. So let's quickly close this and then open this stage. In this stage, we have a single job with one task name, Deploy Azure App Service. This is where I'll define the release workflow, provide my Azure subscription web application name among other inputs. When you're done defining your pipeline, the next step is testing to be sure it works as expected. If the test is successful, this means that any time a developer commits and pushes changes to your repository, the build pipeline will be triggered and run. Then create an artifact. The release pipeline is triggered because of these updated artifacts and deploys the new version of your application to its target environment. That's it. Your users instantly use the updated version of the app without noticing any considerable downtime. I so much enjoy designing pipelines because it helps me eliminate application downtime. I want my customers to continue using my application without disruptions from deployments and I also want my development team to quickly build and test the applications. So if you want to be like me
>
> **[9:28](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/key-concepts-of-azure-pipelines?u=76281980&t=568)** and many other DevOps engineers, let's continue learning together.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (12), [[DevOps]] (4), [[Node.js]] (4), [[npm]] (3), [[Linux]] (2)
> **UI Navigation:** select the (7), click on (2), dropdown (1), toggle (1)
> **CLI Commands:** node (4), npm (3), git (1), find (1)
> **Env Vars:** yaml (7)
> **Definitions:** is a  (3), is an  (1), means that (1)
> **Analogies:** for example (3), for instance (2)
> **Prerequisites:** install (3), configure (2)
> **File Paths:** node.js (4)

#### [Define pipeline assets in Library](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/define-pipeline-assets-in-library?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/define-pipeline-assets-in-library?u=76281980&t=0)** - [Instructor] Apart from pipelines and releases, there are other features in [[Microsoft Azure|Azure]] Pipelines that help make your workflow seamless to design. If you expand the menu on the left, you'll see here that we have environments, which would usually be your [[Virtual Machines]] or Azure [[Kubernetes]] cluster where you can deploy your software applications or [[Microservices]]. There is a task group feature where you can define common build and deployment steps templates, then reference the templates in your pipelines. This effectively reduces the time spent to define new pipelines for similar programming languages. Deployment groups are useful if you have a set of application servers with pipeline agents already installed so you can concurrently deploy your applications to the groups. There is also the library feature where you can define variables, like values and secrets to be used across multiple build and release pipelines. You can also upload secure files, like certificates and sign-in keys. Let's click the add variable group button here. And you can provide the name for the variable group. For example, project secrets.
>
> **[1:41](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/define-pipeline-assets-in-library?u=76281980&t=101)** And under variables, let's click the add button and start defining keys and values. For example, I have a DB password as the name. And then in the value column, I'll type my password. If I want this value to be a secret so that it can be viewed by anyone, I can click the lock icon to the right of the value to max this value. Now, once I click the save button at the top and then click the lock button to reveal the value, it's empty. So you can use this variable across pipelines in this project, knowing the value remains the same and it's a secret. This is quite useful if you are defining pipelines for microservices and applications where secrets should not be entered as plain text in your pipeline configuration. There is also an option to link secrets from Azure Key Vault in your Azure subscription. So for example, instead of saving my DB password locally in this variable group, I can create a secret in the Azure Key Vault and link that here in the variable group. Now, if I need to update the database password, I'll simply do that in the key vault.
>
> **[3:17](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/define-pipeline-assets-in-library?u=76281980&t=197)** And this pipeline will retrieve the updated value from the vault during the next pipeline run. With the security control in the library, you can specify who or which groups can read, use, define, or modify the values in a variable group. This provides an additional layout security over assets created in the library. So in your next CIC design for your team, try out the library feature and integrate your key vault.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (5), [[Microservices]] (2), [[Virtual Machines]] (1), [[Kubernetes]] (1)
> **Analogies:** for example (3)
> **CLI Commands:** make (1)
> **Env Vars:** cic (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 2. Designing a Build Pipeline

[↑ Back to Table of Contents](#table-of-contents)

#### [Get your source code to Azure Repos](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/get-your-source-code-to-azure-repos?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/get-your-source-code-to-azure-repos?u=76281980&t=0)** - [Instructor] Now let's take a look at having a source code in the [[Microsoft Azure|Azure]] [[DevOps]] repository. First, we need to create a project on Azure DevOps. So if you're not signed up to Azure DevOps, simply go to dev.[azure.com](https://azure.com) and click the Start Free button to sign up. You would usually need an [[Microsoft Outlook|Outlook]] account to be able to register on Azure DevOps. Once you're signed in, click on the New Project button at the top-right corner of the screen. And then let's enter a project name. For this course, my project name is Student App. You can provide the description if you'd like. We'll leave that blank. I would select public visibility. This is necessary because if I should go SonarCloud page, you can see here it's free for open source projects. So in order to integrate SonarCloud with my project, I need to leave it as open source. Otherwise, I'll have to pay for using SonarCloud. So back in my project, I'll expand the advanced option and make sure my [[Version Control]] is [[Git]]. Then click the create button. Once the project is created, let's head over to the Azure Repos page. So on the left side of the screen, click on Repos. Now we need to import a repository from [[GitHub]].
>
> **[1:36](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/get-your-source-code-to-azure-repos?u=76281980&t=96)** So to do that, I'll click on the Import button and here we see we need to paste the GitHub URL. Let's head over to GitHub at the address displayed on the screen. Ensure that the selected branch is main, and then click the code button towards the right of the screen. And let's click the copy button. Back in my Azure DevOps page, I'll paste the URL in the clone URL field. Then click the Import button and let's wait for this to complete. Awesome. The import was successful. So this is a simple [ASP.NET](https://ASP.NET) Core application that shows the assessment report of all students for a particular course. We will build the CI/CD pipeline to deploy this application to stages, also known as environments, like the dev, test and production environment. We'll be deploying these applications to [[Agile Development|Agile]] Web App Service. You'll see how to use deployment slots to ensure that there is minimal downtime when we are deploying our source code or application to the production environment.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (7), [[DevOps]] (5), [[GitHub]] (3), [[Microsoft Outlook|Outlook]] (1), [[Version Control]] (1)
> **Env Vars:** url (3), asp (1), net (1)
> **UI Navigation:** click on (3), go to (1)
> **CLI Commands:** make (1), git (1), cd (1)
> **Tools:** github (3)
> **URLs:** [azure.com](https://azure.com) (1), [asp.net](https://asp.net) (1)
> **Exercise Files:** source code (2)
> **Definitions:** is a  (1), known as (1)

#### [Configure a build pipeline](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-build-pipeline?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-build-pipeline?u=76281980&t=0)** - [Instructor] It's time for us to create the CI or build pipeline for our sample application. Remember that we need to enable [[Continuous Integration (CI)|continuous integration]] on this pipeline so that anytime changes are committed to the branch, it'll trigger the pipeline to build and test our source code for the linked branch. So let's start by navigating to pipelines on the left menu. Click to open and then click on the create pipeline button. You get a prompt asking where your code is. If you would like to use the YAML syntax option to define your build pipeline, then you can select any of the options that have a YAML tag and represents the location of your source code. You can check [[Microsoft Azure|Azure]] [[DevOps]] documentation to learn more about the advantages of using YAML over classic data. For this course, I'll be using the classic data option. In the next page, you get a prong to select your repository so make sure that the selected repo represents the name of your application and also select your default branch for manual builds. Let's click continue to proceed. A template comes with predefined task. All you have to do is provide custom inputs where necessary and run the pipeline. You can also add custom scripts
>
> **[1:34](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-build-pipeline?u=76281980&t=94)** and tasks like bash commands, et cetera, or even start an empty job without using a template. In that case, you need to define custom task in the empty job in order to build your pipeline. A sample application is an [ASP.net](https://ASP.net) Coreweb application. So let's search for [ASP.net](https://ASP.net) Coreor scroll down. If you don't find this in your list, you can simply use the search field. Click the [ASP.net](https://ASP.net) Core option and then click the apply button. Now this is what our template contains, and at the top here, we have the name of this pipeline. I'll leave this as this, but you can change the name if you want. Then in the tax page we have something called the pipeline settings where you select the agent pull, the agent specification, and even set some parameters that can be used across multiple tasks in your pipeline. So in the agent specification field, click the dropdown and I'll be using [[Ubuntu]] latest since my [ASP.net](https://ASP.net) Core application can run on both [[Windows]] and [[Linux]] OSs. Click the get sources tab and here you can configure some settings for your pipeline like change the repository, change the default branch,
>
> **[3:10](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-build-pipeline?u=76281980&t=190)** and also configure other settings. Would leave this without making any modifications. If you click on the agent's job, you have other settings you can configure here. For example, we have the agent pool and here you see it shows inherit from pipeline. This is because in the pipeline configuration page, we've already selected the agent pool which is Azure pipelines. There are other settings you can configure, but I'll be leaving this as it is now. Now remember that a task is an abstracted package script and simply commands that run to build or test your code. Here we have the rester task. No need to modify the task accepting situations, for example, where you have a custom feed package you want to use to build your application. In that case, you can come here to change the settings and configure appropriately. There are other options like the control options where you can set a condition for this task to run, and we would explore that later on in this course. The next task is the build task which will build a sample application and we won't be making any changes. I want to point out here that we have the path to project already defined with this parameter. Recall earlier in the pipeline settings
>
> **[4:44](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-build-pipeline?u=76281980&t=284)** that there are some parameters already defined here that will be used across multiple task. So if you check this task, the build task, you would notice that you cannot edit this field except you decide to unlink it. I do not have any task scripts defined in my sample application, so I can either disable this task or remove it completely. To do that, just right click on the task and then select the remove selected task option. The .net Core published task publishes my web project to an output which is defined as the build that artifact staging directory. This directory is then published as an artifact that can be used in my release pipeline to deploy this application to any target. Back in the published task, you see some variables for example, build configuration. This appears in the build task as well where we have the arguments configurations set as build configuration. To ensure consistency in your pipeline for repeated values, it's best to use variables. So to do that, you can go to the variables tab and you see a variable named build configuration with the value set as release. This is not a secret, so it's not locked
>
> **[6:18](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-build-pipeline?u=76281980&t=378)** and you can also check this box so that anytime you need to manually trigger the pipeline, you can change the value for the build configuration from release to any value of your choice. If you had defined a variable group as I explained in an earlier video, then you can go to variable groups and link such variable groups here. For example, your Azure Key vault, and once this is linked, you can consume the values in Azure Key Vault inside your pipeline. In the triggers tab, we want a pipeline to automatically run when changes are committed to the repository's main branch. So let's click on the enable continuous integration checkbox and make sure that the selected branch is the main branch. If you want to specify more branches and specific directories in your code to monitor for changes, then you can click the add button here to add additional branches or click the part filter add button to specify a particular path to monitor. You can also schedule the build time for your pipeline by clicking this add button. Select a specific day you would like a pipeline to build specific time, and also check this box so that the pipeline will only be triggered if
>
> **[7:52](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-build-pipeline?u=76281980&t=472)** any changes committed to the branch. You can as well specify additional branches in your branch filter section, but I won't be using this in this course, so I'll delete this.. In the options tab, you can set build number format, click the little information icon beside the build number format and click more information to learn more about this. There are other settings here that are quite self-explanatory and you can enable them for your project if you'd like. Now, in the history page, it shows records of changes that has been made to this pipeline. This is quite useful for audit purposes, but we don't have anything here listed because this is a new pipeline and hasn't been saved yet. So if I click on the save and queue button and then select the save option and click the save button, then now there is a record in the history page. Let's queue a build pipeline to compile the project. So on the far right, click the queue button, and here in the run pipeline pane, you can select the agent pool because this is manually triggered, you can also change the agent specification and the branch. And if you'd like to change your build configuration variable for example, you can click this, click the build configuration,
>
> **[9:27](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-build-pipeline?u=76281980&t=567)** and then change the value here. I won't be doing this, so I'll just go back and then click on run to trigger this pipeline. This is the summary tab that shows the pipeline was manually triggered at the top left here. It shows you the ripple name, the branch name, and the commit number. To view the logs of this currently running pipeline, you can click the agent job and here you can see all the tasks we defined in the pipeline, like the [[Representational State Transfer (REST)|rest]] task, the build published and publish artifact task. You can always view each of these to check for errors and we can see that the build succeeded with zero warnings and zero errors. If we check the published artifact task, we can see that the file was uploaded, and yeah, you'd see here on line 17 that the file upload was successful. So to view the artifact that has been created for this pipeline, you can click the agent job at the top and then click the one artifacts link. Let's expand the drop directory and here we can see a zip file for a source code. Pretty straightforward, right? Now assuming in a team setting where several developers are working on different parts of the project, all they need to do is commit the access code
>
> **[11:02](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-build-pipeline?u=76281980&t=662)** to the feature branch and create a pool request to the main branch, then the pipeline will automatically run and create an artifact like this for this project and this artifact is then used in the release pipeline to deploy to any target environment.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (4), [[Continuous Integration (CI)|Continuous integration]] (2), [[ASP.NET]] (2), [[ASP.NET Core]] (2), [[DevOps]] (1)
> **UI Navigation:** click on (6), select the (4), scroll down (1), dropdown (1), go to (1)
> **Env Vars:** asp (4), yaml (3)
> **Exercise Files:** source code (3), template (3), zip file (1)
> **Definitions:** is an  (2), is a  (2), defined as (1)
> **Analogies:** for example (5)
> **Prerequisites:** configure (5)
> **URLs:** [asp.net](https://asp.net) (4)

#### [Integrate SonarCloud with the build pipeline](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/integrate-sonarcloud-with-the-build-pipeline?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/integrate-sonarcloud-with-the-build-pipeline?u=76281980&t=0)** - [Instructor] Our project pipeline run was successful but something is missing. It's usually important to scan your code early in your development process so that you catch bugs early before it gets to production. With Sonar Cloud, we will see how to enable continuous code scanning and check the results to make sure we have a clean code of our sample application. Sonar Cloud is a cloud service tool used to continuously scan source codes to detect bugs, vulnerabilities, and code smells across project branches and pull requests. You can check out the documentation on the website to learn more about Sonar Cloud. Before we can set up Sonar Cloud in [[Microsoft Azure|Azure]] Pipelines, we need to create a profile on [sonarcloud.io](https://sonarcloud.io). You can sign up using your Azure [[DevOps]] account, [[GitHub]], [[Bitbucket]], or GitLab. I already have an account linked to my Azure DevOps profile so I won't be creating one now but it's pretty straightforward to create a profile if you follow the prompts. Now on the Sonar Cloud page, I'll click the login button, and then select Azure DevOps. Once I'm signed in, I'll click on the plus icon at the top right corner
>
> **[1:33](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/integrate-sonarcloud-with-the-build-pipeline?u=76281980&t=93)** and select analyze new project. If you have an error showing the Azure DevOps access token is not valid, go to the account settings and then click on your organization. Select the administration tab and then select organization settings. Let's scroll down to where we have personal access token. And you see here that the token is not valid so we need to create a personal access token in Azure DevOps and provide the token in this field. To do that, head over to your Azure DevOps page and then at the top right, click on the user settings and click personal access [[Tokens]]. Let's create a new token by clicking the new token button and then provide a name, say Sonar Cloud. To permit Sonar Cloud, we need to grant the read and write permissions. So under code, click the read and write permission and then click the create button. Let's copy the token that has been created and head back to our Sonar Cloud page. Then in the token field, let's paste the token. Click the update button,
>
> **[3:06](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/integrate-sonarcloud-with-the-build-pipeline?u=76281980&t=186)** and once you see a green tick, that means Sonar Cloud is now connected to our Azure DevOps organization. Now let's create a project on Sonar Cloud. At the top right corner, click the plus icon and then select analyze new project. Now we can see a list of repositories. Let's select the student app. Recall that we created the student app as a public project. That's why I can select it here. The [[Representational State Transfer (REST)|rest]] of the projects are locked because they are not public projects. So click the setup button at the right to configure the Sonar Cloud settings. Since we are designing the analysis for our Azure pipelines, let's click with Azure DevOps pipelines. There're a couple of steps we need to take. First, we need to install the Sonar Cloud extension in our Azure DevOps organization. To do that, click the Sonar Cloud extension link. And it will take you to Visual Studio Marketplace. So click the get it free button and it will ask you to select your Azure DevOps organization. Make sure the right organization is selected, then click the install button. Once that is completed, head back to the Sonar Cloud page and let's go to the next step which is to add a new Sonar Cloud service endpoint. To do this, let's copy the token here,
>
> **[4:42](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/integrate-sonarcloud-with-the-build-pipeline?u=76281980&t=282)** and we will go to project settings and select service connections in Azure DevOps page. So I'll close this and head back to the Azure DevOps home page. Then open my student app project, and at the left corner, I'll click project settings. Under pipelines, select service connections and click create service connection button. Let's look for a service connection type of Sonar Cloud. So I'll select Sonar Cloud and then click next. In the next page, I'll paste the token I copied from the Sonar Cloud page and then click the verify button. Verification succeeded. So I can provide a service connection name. I'll call this Sonar Cloud-Student App SVC.
>
> **[5:45](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/integrate-sonarcloud-with-the-build-pipeline?u=76281980&t=345)** You can provide a description. I'll leave that blank and then click verify and save. Now that is completed, let's head back to our Sonar Cloud page. And the next step, you have to choose the option that best describes your build. We are designing this analysis for a .Net Core application, so we'll select .Net. You scroll down, and the next step, we have to set the [[Fetch]] div value to zero. If you defined your pipeline using the YAML syntax, you can go ahead and copy this to your file but I'll show you how to do this with the classic data model. So back in my Azure DevOps page, I'll click on my pipelines page, and then for the pipeline, click on the ellipsis and select a date. Once the pipeline is open, let's click on get sources and then scroll down in the settings page and uncheck shallow fetch. This will allow the Sonar Cloud to analyze your project without any error. So let's head back to our Sonar Cloud page. And the next step involves adding a couple of tasks in our pipeline. So to do that,
>
> **[7:18](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/integrate-sonarcloud-with-the-build-pipeline?u=76281980&t=438)** I'll quickly copy the project key field. Note the Sonar Cloud organization and the project name field. Back in Azure DevOps, we need to add a task to prepare analysis configuration, run code analysis, and then publish quality gate results. You can see for prepare analysis configuration, this task must run before your build step while the run code analysis must run after your build step. So let's head back to our Azure DevOps page and then beside the agent job, click the plus sign and search for Sonar. Let's add these three tasks. Click the add button beside them. And the first one being prepare analysis on Sonar Cloud would run before the build step. So drag and drop before the build step. And here, let's select the service endpoint which is a service connection we created earlier, the organization name and ensure that integrate with MS Build is selected. In the project key field, let's paste the value we copied earlier and then in the project name field, let's type student app. There's nothing else to set up here. Now for the run code analysis,
>
> **[8:52](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/integrate-sonarcloud-with-the-build-pipeline?u=76281980&t=532)** let's drag this just after the build task and there is nothing to configure here. For the publish quality gate results, let's drag and drop after the code analysis. This task is only necessary for the pull request function which will publish the quality gate results in our pipeline. And that's all to do. So every time the build pipeline is triggered, the source code will be scanned for vulnerabilities based on the task we've defined here. Let's save and queue these changes and see if it runs successfully. So at the top, click the save and queue button. Click the save and queue button again. We don't need to make any change here. Then click the save and run button. You can click the agent job to view the logs. And now we can see the rest of tasks, the Sonar Cloud tasks we've added, like prepare analysis, run code analysis and publish the quality gate results. The pipeline run was successful including the Sonar Cloud task. So if you click the run code analysis, you can scroll to the end to see if the analysis was successful and you should find here on line 298, analysis successful. And it says you can find the results
>
> **[10:26](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/integrate-sonarcloud-with-the-build-pipeline?u=76281980&t=626)** at a particular URL. So you can click this URL to go to Sonar Cloud, and here, you would see the results of the analysis. On the Sonar Cloud page, click the main branch and you'll see that the quality gate was not computed. Well, we have one bug here, two code smells, zero security hotspots and vulnerabilities, and 0% of coverage and duplications. So with this result, we can ensure that quality code is only merged to our main branch and pushed to production environment. Before we finish here, we need to define a base code that modified code changes will be scanned against to get a quality gate. This is necessary for every new project. You can see how this shows not computed for quality gate. So to fix this, let's click the set new code definition and select the previous version. This would mean that all code that has changed since the previous version bump is considered a new code and will be scanned against the old one. So select this and it's automatically saved. So this is how to integrate Sonar Cloud with your build pipelines to continuously scan new versions of your code for vulnerabilities and bugs.
>
> **[11:59](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/integrate-sonarcloud-with-the-build-pipeline?u=76281980&t=719)** If I head back to the overview page, and scroll down to select the main branch, then open this bug by clicking its value, you can see here a bug is displayed which says you should add a description to this table in views home index.CS [[HTML]] file. To view this bug, you can click it to open. It shows you where the issue is. So if you scroll up, you see on line 14 that there is a table without a description. You can click on why is this an issue to read the reviews and understand a non-compliant code as well as what a compliant code would look like. You can adopt the suggestions in your code and then recommit the code to your repo. Cool, right? This way, you are sure to push a more secure and bug free code to production systems. You can try to clone your code locally from Azure repos and fix the bug. Then commit and check the results of your new scan to see if there are any changes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (18), [[DevOps]] (15), [[Representational State Transfer (REST)|Rest]] (2), [[Fetch]] (2), [[GitHub]] (1)
> **UI Navigation:** click on (7), select the (5), go to (4), scroll down (4), drag and drop (2)
> **Prerequisites:** set up (2), configure (2), install (2), setup (1)
> **CLI Commands:** make (3), find (2)
> **Env Vars:** url (2), svc (1), yaml (1), html (1)
> **Tools:** github (1), bitbucket (1), gitlab (1), visual studio (1)
> **Definitions:** is a  (3)
> **URLs:** [sonarcloud.io](https://sonarcloud.io) (1)

#### [Protect your important branches](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/protect-your-important-branches?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/protect-your-important-branches?u=76281980&t=0)** - [Instructor] We have our source code in the repo, defined the build pipeline and integrated SonarCloud to scan our code for vulnerabilities, but we need to prevent buggy code from being committed to our main branch, which is the most critical branch of our application. How do we set this up so we can ensure that only high-quality code is pushed to the main branch? Got any ideas? Please share in the comments section of this course. [[Microsoft Azure|Azure]] [[DevOps]] has branch policies where you can define certain configurations to protect important branches. Once this is set up, assuming we protect our main branch in this DevOps project, then direct commits or pushes to the main branch will fail. The only way to update the protected branch would be via a pull request. You can specify that approvers or code reviewers review code changes, and a build must be successful even with a pass for vulnerability scan before changes can be merged with the main branch. Let's quickly set that up. At the left menu of your Azure DevOps under Repos, click on Branches.
>
> **[1:30](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/protect-your-important-branches?u=76281980&t=90)** To the right of the main branch, you see on ellipsis. Click on it and select branch policies. The first thing we need to do is to require a minimum number of reviewers so you can toggle the button and then let's select one since this is a project with just one user. You can also check the allow requesters to approve their own changes so we don't get stuck in the project. There are other settings you can configure, like check for linked work items, assuming you work in an [[Agile Development|agile]] team. You can also check for comment resolution and limit merge types. Let's activate this and then uncheck every other option, except the basic merge. This means that the history in topic branches will be preserved as we merge with the main branch. I want changes that should be merged with my main branch to be validated first. I want it checked for bugs or vulnerabilities, and if there are errors, the merge should fail. You can configure that in the build validation policy. To do that, click the plus sign, and then you have to select the build pipeline, so it's important that your pipeline has run at least once before you set this build policy. You can also select a path filter if you'd like but I want this to monitor the whole branch.
>
> **[3:06](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/protect-your-important-branches?u=76281980&t=186)** The trigger option is set to automatic, which means whenever the search branch is updated, this build policy will be triggered as part of the pipeline run. For the policy requirement, you can select required, which means the build must succeed in order to complete the pull request, or optional, which means a build failure will not block the completion of a pull request. In that case, you'll be merging codes with bugs to the main branch. For the build expiration, I'll select immediately when the main branch is updated so it deletes the build run. Let's type a display name for this policy. I'll call it the student app PR build policy and then click the save button. That's the basic configuration needed to protect an important branch. Remember, we are defining the settings on the main branch and if you'd like to have certain reviewers review code, you can configure that here in the automatically included reviewers. This means that for every pull request that is created, these reviewers will be automatically added as part of the approvers. So to do that, you can click the plus sign, search for a reviewer and then you can make this optional or required.
>
> **[4:41](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/protect-your-important-branches?u=76281980&t=281)** And also check the allow requesters to approve their own changes option. If you're working with two or more people in a team setting, then you may not need to check this option. So let's click the Save button, and that's it for branch policies. A couple of other settings are required after defining the branch policies. Let's enable branch filters to monitor for feature branches and then we will add the condition in our publish artifact task to only publish an artifact when the branch trigger matches the condition. That is when the main branch is running. To do that, let's navigate to open the Pipelines page, click on the ellipsis for this pipeline, and select a date. Let's open the triggers tab at the top and on the branch filters, click the add button and then select the main dropdown. Then in filter my branches, let's type features/*. Then press enter. You can save this by clicking the save and queue button. Then click save so we don't lose our configuration. Next, we need to specify a condition for the published artifact task so that the project is only published for the main branch and not the feature branches.
>
> **[6:15](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/protect-your-important-branches?u=76281980&t=375)** This allows us to use the same pipeline definition with several branches and skip certain tasks if the conditions are false. So to do this, let's navigate to the task page. Select the publish artifact task and then expand the control options. Under run this task, let's click the dropdown and select custom conditions. In there, let's type this condition and succeeded equal variables. Build the sus branch, reps/add/main. This task will check the system variables of a build pipeline agent, and if they build the source branch is main, then it's a match and that published task will run. Otherwise, it's escaped. You can save this by clicking the save and queue button at the top. Click the save button and click the save button again. With this setup, we cannot push changes directly to the main brunch except through a pull request and the merge requires [[Code Review]] from at least one person, as well as a successful build pipeline run. As a developer, you can create a feature branch for new features you're developing and push to the repo in Azure DevOps. Each push will cause this pipeline to run
>
> **[7:48](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/protect-your-important-branches?u=76281980&t=468)** so that we can catch errors early in the process. I've seen situations where a developer would spend weeks developing locally and only adopt remote repo weeks down the line. This usually leads to issues and spending time resolving build pipeline errors. For example, a developer might have been using an older or deprecated version of a package or some vulnerabilities exist in the code and now we can't push to any environment until those issues are resolved. So it's best to adopt [[Continuous Integration (CI)|continuous integration]] early in your [[Software Development]] so you can catch and resolve issues early and always have a quality source code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[DevOps]] (4), [[Microsoft Azure|Azure]] (3), [[Agile Development|Agile]] (1), [[Code Review]] (1), [[Continuous Integration (CI)|Continuous integration]] (1)
> **UI Navigation:** click on (3), select the (3), navigate to (2), open the (2), dropdown (2)
> **Prerequisites:** configure (3), set up (1), setup (1)
> **Definitions:** means that (2), is a  (1)
> **Exercise Files:** source code (2)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Best Practices:** it's best to (1)

#### [Create a task group for build pipelines](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-a-task-group-for-build-pipelines?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-a-task-group-for-build-pipelines?u=76281980&t=0)** - [Instructor] Let's imagine this scenario. You are in charge of designing CICD pipelines for your development teams and there is a standard framework, for example, the net call for the backend, and [[Next.js]] for the front end. If it's quite a busy team, you will likely be defining pipelines a lot of times. Now, how can you reduce the time spent to design a pipeline? By using a task group. A task group is a feature that allows you to have a template of task that can be used in several similar pipelines. Just as you add the task to your pipeline, you can add the task group template and redefine some parameters as variables so that you provide that for each application pipeline you design. Also, with a task group, you can modify a task and that will affect every pipeline using the task group. This saves you time for other important work, assuming you need to just make a quick change on the main task group. Let's create a task group for this build pipeline. First of all, we need to unlink every shared parameter in our pipeline. If that is not the case and we try to create a task group, an arrow is displayed unlink parameters. Let me show you that. So for example, if I select all this task
>
> **[1:34](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-a-task-group-for-build-pipelines?u=76281980&t=94)** by pressing down the control button and then click each of the task, then right click and select create a task group, I have an error, one or most selected task app settings linked to the pipeline parameters. So let's click, okay. So I then select everything, and remember, in the build task, we have a path to project parameter linked to the pipeline settings. Same thing in the restore task where we have the CS proj linked to the pipeline settings. So to do this, I'll go to the pipeline page and then click on link all, then confirm to link the parameters. Let's check each task to know where we will define a variable. In the restore task, there is nothing really to define here. In the so cloud analysis task, let's create a variable for some fields. Not the value of the SonarCloud service endpoint, this might change for different projects or pipelines that will need to reference this parameter. So for this, I'll replace the value with dollar bracket SonarCloud endpoint.
>
> **[3:03](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-a-task-group-for-build-pipelines?u=76281980&t=183)** And then for the organization, I'll type dollar bracket SonarCloud bug.
>
> **[3:14](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-a-task-group-for-build-pipelines?u=76281980&t=194)** Let's copy the project key, and then I'll replace the value with dollar brackets sonar cloud project key. And then for the project's name, which would always change for each pipeline, I'll replace the evaluate dollar bracket SonarCloud project name. There is nothing to define in the other sections of this task, but you can just confirm that. If you check the build task as well, there is nothing to configure here. For the wrong code analysis, there's nothing to configure, the same applies to quality gate results. Publish code, publish the artifacts. So that's it. Now that this is completed, let's press the control key again and select all the task. Then right click and select create task group. Let's provide a name for this task group. I'll call it net core build task and a description, tasker for building [ASP.net](https://ASP.net) Core applications. The category will be build.
>
> **[4:52](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-a-task-group-for-build-pipelines?u=76281980&t=292)** And now in the parameters section, you can see we have default value for build configuration. This can always be changed when you're defining a new pipeline. So we won't be selecting default values for SonarCloud endpoint, the organization project key and project name, so that you can always provide these values when you reference this task group in a new pipeline. So let's click the create button to create the task group. And now the task group is automatically applied to our pipeline with some variables with empty values. So to the right you can see we need to select a SonarCloud endpoint. So click the dropdown and select the service connection we created earlier. For the organization, click the dropdown. We don't find one, so you can just type your SonarCloud organization name. The SonarCloud project key I copied earlier, I'll just paste that here, and the project name is student app. And that's all that needs to be done. Now let's save and cue this to be sure that it runs successfully. So I click the save and cue button, save and run, and then open the agent job to view the logs. We now have a center cloud analysis running.
>
> **[6:28](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-a-task-group-for-build-pipelines?u=76281980&t=388)** You can click this to be sure it's run successfully. And there were no errors in the log which means our task group is working. We see everything is completed and an artifact is created. So the task group works as expected. So if you have a new repository with totally different source code but the .net core application and you have to create a pipeline, all you have to do is navigate to pipelines, click on the new pipeline button, select the use classic data option and make sure you select the right repository and the branch for your manual build, then click continue. Then here, instead of selecting a template, you start with an empty job, and once this page opens, you click on the plus sign beside agent job and search for your task template. Next call, build task. So you select the task and then add it. And now you can define the parameters for your new pipeline like the service endpoint, the SonarCloud organization, the new project key, as well as the project name. And that's it.
>
> **[8:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-a-task-group-for-build-pipelines?u=76281980&t=480)** You've saved a lot of time defining multiple pipelines by just providing these four parameters. To manage your task group, you can navigate to pipelines menu and then select task groups. I won't save any change to that. And here you can see your task group. If you click to open, you can modify the task in this task group and save it by clicking the save button. You can also save the task group as a draft, test it first with a sample application and then publish it. In that way, you would have different versions for your task group which allows for backward compatibility, meaning new applications would use the new version of your task group while existing applications or pipelines will continue to run with the old version. You can click on the history tab to review changes that has been made to the task group. And you can also click on the reference tab to view the list of pipelines that are using this task group. To a export task group, you can click the export button and export this task group to another project. And then you can also set security permissions by clicking the ellipsis, select security, and grant permissions to individual users or groups. I enjoy using task groups
>
> **[9:35](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-a-task-group-for-build-pipelines?u=76281980&t=575)** especially as a help to design pipelines for [[Microservices]]. So consider using task groups when building your next set of pipelines for your development team.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Next.js]] (1), [[ASP.NET Core]] (1), [[Microservices]] (1)
> **UI Navigation:** click on (5), select the (4), dropdown (2), navigate to (2), go to (1)
> **Exercise Files:** template (4), source code (1)
> **CLI Commands:** make (2), find (1)
> **Analogies:** for example (2), imagine (1)
> **Env Vars:** cicd (1), asp (1)
> **Definitions:** is a  (2)
> **Prerequisites:** configure (2)

#### [Simulate CI trigger and branching policies](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/simulate-ci-trigger-and-branching-policies?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/simulate-ci-trigger-and-branching-policies?u=76281980&t=0)** - [Instructor] We now have a working build pipeline, defined by using a task group: enabled [[Continuous Integration (CI)|continuous integration]] on the main and feature branches, and also enabled branching policies to require a pull request with a successful PR build before pulling changes into the main branch. Now, let's simulate a typical situation where a developer needs to modify code in a feature branch and merge with the main branch. Remember, earlier in the scan result of SonarCloud, we have a bug with a suggested solution to add a description to the table. Let's try to fix that locally in Visual Studio Code, and then push to the remote repo. to see what happens when a developer push changes. In your student app project, navigate to repos. on the left menu, and then click on the clone button at the right side of the page. Let's copy this URL. Open a terminal in your system. I have my batch terminal opened already, so I'll navigate to the downloads folder, and here I'll type [[Git]] clone, and then paste the URL I copied. So right click, and paste, then enter. When the clone process is completed, let's navigate to the project folder.
>
> **[1:38](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/simulate-ci-trigger-and-branching-policies?u=76281980&t=98)** So, CD student app, and then here I'll run code dot to open my code in Visual Studio Code. If you don't have Visual Studio Code, you can easily download one via a [[Google]] search, and install it in your PC. Let's wait for the VS Code to load the source code. Then on the lower left corner of the VS Code, you shall see main, which represents the main branch. So, let's create a feature branch to simulate a change to some part of our source code since we cannot directly push to the main branch. So, click the main, and then select create a new branch. Let's name this branch features, slash index, then enter. It could be any name, but I decided to use index. Recall earlier that we define a branch filter for features slash asterisk branches. So, if you check again, we have features slash index branch created. Expand demand source code directory, let's open views, expand home, and then select index dot CSHTML. Let's add that to our SonarCloud page to review the suggestions for fixing the bug.
>
> **[3:14](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/simulate-ci-trigger-and-branching-policies?u=76281980&t=194)** So in my SonarCloud page, if you click on the overview page, scroll down, and click the main branch. Let's click overall code, and then select the bug by clicking the value, open the suggestion, and you see the issue is on line 14. It's asking us to add a description to this table. Now let's check why this is an issue and the suggestion is, the simplest way to do it, is to add a caption element inside the table. So, let's add that to Visual Studio Code. And then after the table at line 14, create a new line, and let's create a caption. And we'll caption these assessments reports for CIS student, 2023. Let's save the change. And now it's time to push it back to our remote repo. in [[Microsoft Azure|Azure]] [[DevOps]]. So, let's open a terminal in VS Code at the top bar you would see the terminal menu, click and then select new terminal. In here, let's type git, add dot, to stage the changes we've made, and type git comment,
>
> **[4:50](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/simulate-ci-trigger-and-branching-policies?u=76281980&t=290)** dash M, which, fixed index CSHTML bug, and then press enter. And finally, to push, let's type git, push, dash, dash, set, dash, upstream, origin feature slash index, then enter. This will push the change to the remote repo. now. Awesome, once that is completed, let's head over to Azure DevOps page. Close this pane, and let's refresh the page. Once the page is refreshed, you should see a notification like this to create a pull request. If you don't see that under the repos. menu at the left, you can click on pull request, and then you should see this, or you can create a new pull request. I'll simply click the create a pull request button, and here you can see the title of the pull request, which was the comments we created in the terminal. You can click the files tab to see the change that was made, and it's highlighted green, which shows assessment report
>
> **[6:24](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/simulate-ci-trigger-and-branching-policies?u=76281980&t=384)** for CIS student, 2023. You can also view the comments tab to see the number of comments for this pull request. Back in our overview page, notice at the top here we are creating a pull request from the feature slash index branch into the main branch. Now, let's scroll down and we can click the create button. Remember the build policy we defined earlier, it's part of the pull request checks. So, first, the pipeline will run to check that there are no bugs in this code before it allows this pull request to be completed. So, you can click the view two checks, and you'd see here, we have required the students have viewed policy, which would cause the pipeline to run fast on this feature branch. Make sure that it runs successfully, there are no bugs, and once it's successful, then we can approve the pull request and complete it. Click the PR build policy to view the logs, click on the agent job. And you would notice here, that the published artifacts will be skipped because this pipeline is running for the feature branch.
>
> **[7:57](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/simulate-ci-trigger-and-branching-policies?u=76281980&t=477)** Awesome, the pipeline runs successfully, and there was no artifact created because this pipeline is running for a feature branch and does not match the main branch condition we set earlier. So, let's go back to our pull request. I'll just navigate to repos. and select pull request, then open the PR, and you'd see that the build policy was successful. So, the second requirement is that a user must approve a pull request. To do that, I'll just click approve at the top here, and that's it. To complete this pull request so that the changes made in the features slash index branch will be reflected in the main branch, let's click the complete button at the top, and here we have the merge type: merge, no fast forward. The [[Representational State Transfer (REST)|rest]] are disabled by policy we defined earlier, and then we can safely delete the feature slash index branch after merging. So, let's click on complete merge, and now the pull request is completed. So, this is how Azure Pipelines helps with continuous integration and scanning of your source code to catch bugs early in the development process.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Git]] (4), [[Microsoft Azure|Azure]] (3), [[Continuous Integration (CI)|Continuous integration]] (2), [[DevOps]] (2), [[Google]] (1)
> **UI Navigation:** click on (5), navigate to (4), scroll down (2), open the (2), select the (1)
> **Tools:** terminal (6), visual studio (4), vs code (3)
> **CLI Commands:** git (4), cd (1), make (1)
> **Env Vars:** url (2), cshtml (2), cis (2)
> **Exercise Files:** source code (4)
> **Cross-References:** earlier in (1), go back to (1)
> **Definitions:** is an  (1)


### 3. Designing a Release Pipeline

[↑ Back to Table of Contents](#table-of-contents)

#### [An overview of a release pipeline](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/an-overview-of-a-release-pipeline?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/an-overview-of-a-release-pipeline?u=76281980&t=0)** - [Instructor] A release pipeline enables teams to more easily deploy build artifacts to any target environment, whether in the cloud or on-premises. You can define an environment in each stage of a release pipeline to deploy updated versions of your software application. This reduces time to deliver new versions to your users, improves productivity of your team, and enable consistency with the use of a deployment workflow. For a release pipeline to work, you must have added a published artifact task in your build pipeline and run your build pipeline at least once. When a new artifact is published, depending on the release pipeline continuous deployment setting, the release pipeline is immediately triggered and all the task or workflow defined in each stage is executed one after the other. To create a release pipeline, you go to pipelines and select releases, then click the new pipeline button. You would need to select a template or start with an empty job or simply select App Service template. A stage is just like an environment. You can have multiple stages in your release pipeline. For example, one stage for development environment, another for testing or QA, and one for production.
>
> **[1:35](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/an-overview-of-a-release-pipeline?u=76281980&t=95)** Each stage comes with pre-deployment configurations, as well as post-deployment configurations. Before you define your stages, you have to select a build artifact. To do that, you click add an artifact button and then select the build pipeline source. You can also choose to deploy latest versions of your application or specific versions. Then click the add button. After selecting an artifact, you can then set up continuous deployment trigger, which means that when a new artifact is published by the build pipeline, the release pipeline will be triggered immediately. After selecting an artifact, certain conditions can be defined for each of your stages. For example, for the dev stage, you can set pre-deployment configurations to check for a particular artifact by branch. You can select the branch name and this would only deploy to this stage if the build artifact was triggered by changes in this main branch. You can also set up pre-deployment approvals whereby certain groups of persons or users must approve a deployment to a particular stage. And then you can also define [[Git]] to evaluate before deployment and invoke a [[Representational State Transfer (REST)|REST]] API
>
> **[3:11](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/an-overview-of-a-release-pipeline?u=76281980&t=191)** to your monitoring tool or query work items or check on our cloud quality Git result to make sure there are no bugs before deploying to a particular stage. There is also the post-deployment approval setting, which might be useful in certain scenarios, for example, after the conclusion of a user acceptance test. The QA lead can approve to indicate that the UAT was successful and it's safe to deploy the same artifact to open environment. To set up the workflow for each stage, you can click the one job, one task link and here you can configure your stage, for example, select the [[Microsoft Azure|Azure]] subscription for this web app and then select the application type, the application service name. You can also configure the agent. For example, change the agent pool to self-hosted agent or use the Azure Pipelines agent. And then in the task pane, you can add additional task to deploy your application and configure as necessary. In the release pipeline, you also have the variables tab where you can define variables for this release pipeline and scope it to a particular stage or make it available to all the stages. You can also make it settable at release time, which means that a user must manually enter the parameters
>
> **[4:49](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/an-overview-of-a-release-pipeline?u=76281980&t=289)** when the pipeline is manually triggered. You can also link variable groups, like the Azure Key Vault from Azure so that your release pipeline can consume secret values while executing. The retention tab is where you define the number of days and releases to maintain for artifacts in each stage. For example, I may want to retain development release artifacts for 10 days, while production artifacts can remain for as long as 20 days. In the options tab, you can provide a description for your pipeline and modify the release name format. You can go to the Integrations tab and check any of these boxes to report deployment status to work. That is if you have work items linked to the repository. You can also report status if you've integrated your Azure [[DevOps]] with [[Jira]], et cetera. And lastly, the History tab is where you would find a report of changes made in the release pipeline. Use this to audit activities related to the pipelines. We will use some of these features while designing a release pipeline for our sample application where you can check out [[Microsoft]] Learn if you'd like to learn more about Azure Pipelines.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (6), [[Git]] (2), [[Representational State Transfer (REST)|Rest]] (1), [[DevOps]] (1), [[Jira]] (1)
> **Analogies:** for example (6), just like (1)
> **CLI Commands:** make (3), git (2), find (1)
> **UI Navigation:** select the (4), go to (2)
> **Prerequisites:** set up (3), configure (3)
> **Env Vars:** rest (1), api (1), uat (1)
> **Exercise Files:** template (2)
> **Tools:** jira (1)

#### [Create web apps in Azure](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-web-apps-in-azure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-web-apps-in-azure?u=76281980&t=0)** - [Instructor] Let's create three [[Microsoft Azure|Azure]] Web Apps for our sample application. Each app will represent an environment that is the development, testing or QA and production. Usually your production environment would be the longest running web app in Azure, while dev and test can be recreated when needed. As a best practice, we will create a staging slot in the production web app so that users in our production environment will not experience any downtime while a deployment is initiated for the environment. If you haven't done so yet, you should register or log into Azure portal via [portalazure.com](https://portalazure.com). Signing up to Azure is pretty straightforward as there are prompts to guide new users. Once you're logged in, in the home page and at the top left corner of the screen, click the create a resource button. Under categories, scroll down and select web and then click the create button for the web app. I'll change to the app subscription and then click create new to create a new resource group. A resource group is like a placeholder for similar cloud resources, and I would use this resource group for my dev and test environment.
>
> **[1:37](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-web-apps-in-azure?u=76281980&t=97)** Let's name it student-dev-rg, and then click OK. Let's provide a name for the web app. The name must be unique, so I'll add a value, like two numbers, and then -dev. Make sure code is selected as the publish option. And since this application is a .NET Core application, I'll select .NET 6 LTS for my runtime stack. Leave [[Windows]] as the operating system for this web app and choose the region closest to your location. This is the dev application and does not necessarily require a lot of compute resources. So to save cost, I'll change the size and switch to a free version of the app service plan. First of all, I need to create a new app service plan. So click the create new button, and in here, I'll type student-dev-rg app service plan -free.
>
> **[2:55](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-web-apps-in-azure?u=76281980&t=175)** Then click OK. Then for the pricing plan, you can click the dropdown and select Free F1 at the top of the list. You can set zone redundancy for a free version. So let's click the next button. If you want to set up [[GitHub]] Actions for this web app, then you can go ahead and do so. Well, I'll leave this as it is and click the Next button. Ensure enable public access is on so that this app can be accessible over the internet. Click Next to go to monitoring. You may enable Application Insights if you've configured your app for insights and want to pay for that. Well, I'll select no. And then click Next. The tags where you can define name and values that you can use to identify a cloud resource. So I'll just click next to review my entries and once I'm sure of the entries, then click the Create button. The web app for the development environment is being deployed now. This environment will host the dev version of our sample application. So anytime a developer pushes changes to the repo and creates a pull request into the main branch, it will automatically deploy to this dev environment
>
> **[4:29](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-web-apps-in-azure?u=76281980&t=269)** and the developer can assess it via the name we defined earlier. Let's quickly create a web app for the test or QA and the production environment. Click on the portal menu at the top left corner of the screen and select create a resource. Select Create button for the web app, select the appropriate subscription, and then the resource group for the test environment. Let's provide a name for the web app called as student app 11 -qa.
>
> **[5:13](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-web-apps-in-azure?u=76281980&t=313)** Make sure it's validated. Code should be selected as the publish option and let's select the runtime stack, which will be the .NET LTS. Leave the operating system as Windows and select the closest region. For the pricing plan, I'll leave it at the App Service plan we created earlier for the dev environment. And then just click Next button just to confirm that the configurations are accurate. Click networking and make sure enable public access is on. Click next to go to monitoring and disable Application Insights. Then let's click the review and create button, and confirm the entries. Then click create button. While the QA app is deploying, let's click the portal menu again and select create a resource. Select the create button for the web app and then choose the appropriate subscription. For the production app, you'll typically have a separate resource group from the dev applications. So click the create new button and let's name this resource group student prod -rg.
>
> **[6:44](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-web-apps-in-azure?u=76281980&t=404)** Click OK. For the web app name, let's type student app 11 and make sure it's validated. Select code for publish setting and for the runtime stack. Let's select .NET 6 LTS. Make sure that Windows is selected as the operating system and choose the closest region. For the production app, let's create a new service plan. Call this student prod -app service and then click OK.
>
> **[7:24](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/create-web-apps-in-azure?u=76281980&t=444)** Now let's click Explore pricing plans. And for the production app, just to save costs for this course, I'll select the basic B1 option. You can see the price here per month and the cost per hour. If you'd like to choose a cheaper version, you can select the D1 option but I'll just go with the basic option and delete this immediately after I'm done testing. So select B1 and click the select button. Now let's go through the same process. Click Next to go to the deployment tab, networking. Make sure Enable Public Access is turned on. Check monitoring and disable Application Insights. And then let's click the review and create button. Confirm your entries, and then click the create button. Awesome. So now that the three environments are ready, we can then define a release pipeline and link each stage to the appropriate environment. As a personal challenge, you can try to configure the release for at least the dev stage before watching the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (4), [[Windows]] (3), [[GitHub]] (1)
> **UI Navigation:** select the (6), go to (3), scroll down (1), dropdown (1), click on (1)
> **Env Vars:** net (4), lts (3)
> **CLI Commands:** make (6)
> **Tools:** azure portal (1), github (1)
> **Prerequisites:** set up (1), configure (1)
> **URLs:** [portalazure.com](https://portalazure.com) (1)
> **Cross-References:** next video (1)

#### [Configure a release pipeline for multistage deployment](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-release-pipeline-for-multistage-deployment?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-release-pipeline-for-multistage-deployment?u=76281980&t=0)** - [Instructor] I hope you were able to configure release pipeline for the dev stage. If not, that's fine. We will do it together now. Back in the [[Microsoft Azure|Azure]] device page, let's navigate to releases. So go to pipelines and select releases. Click on the new pipeline button. Just as in the build pipeline, there are a number of templates and releases for deploying applications of various frameworks and to different targets. We want to deploy to Azure web app, so let's click the apply button for Azure app [[Service Deployment]]. Let's rename this stage to development. Close the site pane and underneath the development stage you'll see the add button. Click to add for the QA stage, and let's select apply. Rename the stage to QA and let's close this pane. Under the QA stage, let's click the add button again for production. Click the apply button beside app service deployment and rename the stage to production. Then close the pane. Recall earlier we added a text to our build pipeline to publish an artifact so we can deploy that artifact
>
> **[1:36](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-release-pipeline-for-multistage-deployment?u=76281980&t=96)** via our release pipeline. To select the build artifact, click on the add an artifact button at the left side of the screen. And then let's select the build pipeline we defined earlier. So click the dropdown and select student app CI. We typically want to always deploy the latest version where you can choose an older version anytime you decide to manually create a release. Click the app button and click the continuous deployment icon, this little lightning icon, and let's toggle to enable continuous deployment. This means that anytime a new build artifact is created, the release pipeline will be triggered automatically. You can also specify branches in addition to this trigger so that the release is only triggered for artifacts created from specific branches. Let's choose the main branch by clicking the add button on the build branch filters and then select the main branch. Let's close this pane. If you do not wish to enable continuous deployment, but rather schedule a time for releases to be triggered, you can use the schedule set features to configure this. You can enable this and then select the days and time you'd prefer the pipeline to be triggered. This is quite useful for teams
>
> **[3:12](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-release-pipeline-for-multistage-deployment?u=76281980&t=192)** with critical applications that cannot be updated just anytime. All right, let's define each of the stages, starting with the dev stage. Before that, let's disable the schedule trigger and close the pane. Click on the one job one task link for development stage. In the stage settings page, we will connect to our Azure subscription. If you are logged in with the same account to both Azure [[DevOps]] and Azure Portal, you should see your subscription listed here in the Azure subscription field. Otherwise, you can click on the manage link at the top and then select new service connection to create a service connection. Select Azure resource manager, then scroll down and click next. Leave service principle automatic as the authentication method, and then click next. You select the appropriate subscription here and then you can decide to scope this to a particular resource group or leave it blank so that it covers all the resources group in this subscription. Then provide a service connection name. I won't be saving these since I already have a service connection linked to my subscription, therefore I'll close this. And back in my release configuration for the dev stage, I'll click the subscription list and select the app appropriate subscription.
>
> **[4:51](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-release-pipeline-for-multistage-deployment?u=76281980&t=291)** Then click the authorized button. Once authorized, remember, the web app we deployed is with [[Windows]] operating system. So let's leave the app type as web app on Windows, and then let's select the app service name. So click the dropdown, and this is the development stage, so we'll select the studentsapp-dev. Let's look at the deploy task. You see here some of the settings we configured in the stage page are linked here. If you have other settings you need to define, you can expand the categories here and define them as needed. We are done configuring the dev stage, so let's go ahead and check out the QA stage. To do that, place your mouse cursor over the task tab and select QA. Let's select the appropriate subscription. So select the first one since we've already authorized this subscription in the dev stage. Make sure the app type is web app on Windows. And for the QA stage, we need to select the studentapp-QA. You can click the deploy task to review the settings, but let's head over to the production stage. So go to the task tab and select production. Select the upper price subscription.
>
> **[6:31](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-release-pipeline-for-multistage-deployment?u=76281980&t=391)** Make sure the app type is web app on Windows, and then let's select the app service name for this stage. Let's modify the pipeline name to something descriptive. So I'll select this and type studentsapp-cd-pipeline and click the save button.
>
> **[6:59](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-release-pipeline-for-multistage-deployment?u=76281980&t=419)** You can provide a comment and then click okay. We don't have variables to define for our code and you can explore that later, but let's manually deploy the students app to all environments. First, let's check the build pipeline run to confirm the latest version that created a build artifact. To do that, click on pipelines and then select the build pipeline. And we can see the number here, 126.6. So back in the release page, click on create release. If you want to manually deploy to a particular stage, you can click the stage to change from automated to manual. You will have to click on a deploy button for that particular stage. However, I don't need it to be manual for now. So let's confirm the appropriate build artifact is selected, which is 126.6. You can provide a release description and then click the create button. To view the logs, click the release link at the top green bar and then let's wait for this stage to start deploying. Now click the logs to view. The agent first initializes job and then downloads the artifact from a build pipeline, and then attempts to deploy the build artifact
>
> **[8:35](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-release-pipeline-for-multistage-deployment?u=76281980&t=515)** to the Azure app service. Now the app is deployed to the dev environment. Let's check out the QA stage. At the top, change the stage from development to QA, and you can see it's preparing an agent for the job and should start running the logs now. The same process as defined in the development stage is (indistinct) for the QA web app. First, it initialized the job, downloads the build artifacts, and then deploys to the QA web app. Awesome. The QA stage was successful. Now let's change to the production stage and check the logs. The stage is preparing an agent for the job. It initializes the job, downloads the artifacts from the build pipeline, and then deploys to the production web app. Let's wait for this to be completed. It might take a moment. Awesome. All three stages deployed successfully to the linked Azure web app. Now let's go to Azure portal and try to assess the URL of each environment. So head over to your Azure portal and click on the portal menu. Then select app services. If your web app isn't showing in the list, make sure that the filter is accurate.
>
> **[10:10](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-a-release-pipeline-for-multistage-deployment?u=76281980&t=610)** So let's change this to the learning subscription and apply. And now I would open my student app dev, and then click on the browse button to open the webpage. Awesome, our site is up and served from the dev environment. So going forward, new versions built from the main branch would always be automatically deployed to this environment. This reduces the time it'll take a developer to deploy and test the dev version of an application. You can also verify that your QA and production web app are reachable by going back to your Azure portal and select the QA and production web app, then browse it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (13), [[Windows]] (4), [[Service Deployment]] (2), [[DevOps]] (1), cursor (1)
> **UI Navigation:** select the (15), click on (9), go to (3), dropdown (2), navigate to (1)
> **CLI Commands:** make (3), cd (1)
> **Tools:** azure portal (4)
> **Versions:** 126.6 (2)
> **Prerequisites:** configure (2)
> **Env Vars:** url (1)
> **Definitions:** means that (1)

#### [Reconfigure your production stage release to deploy to a staging slot](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/reconfigure-your-production-stage-release-to-deploy-to-a-staging-slot?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/reconfigure-your-production-stage-release-to-deploy-to-a-staging-slot?u=76281980&t=0)** - Now there is one problem we need to fix. Notice how a create release action or a new build artifact would cause the release to deploy to all environments without any confirmation especially to the QA and production environment. This should not be the case because users may be testing new features in the QA environment or a larger user base would be using your production app and deploying new versions this way could break the app causing undesirable effects. We have to configure approvals so that deployments to non-development environments are first approved before the deployment begins. To do that, let's head back to our [[Microsoft Azure|Azure]] [[DevOps]] project and navigate to releases. Select the student pipeline and click the edits button. Let's enable pre-deployment approval for the QA stage. Click on the pre-deployment conditions and scroll down to enable pre-deployment approvals. In the approvers field search for and select your name. If you have additional approvers to add, you can go ahead and search them. You can also add valid user groups, for example,
>
> **[1:34](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/reconfigure-your-production-stage-release-to-deploy-to-a-staging-slot?u=76281980&t=94)** user groups of the QA team. This way, the QA team would control what gets deployed to the QA environment and add the appropriate time they need a new version of the application for testing. Once you have more than one entry, you can select an approval order. The first option being that every approver must approve a deployment to the QA stage in any order. The second option requires the first approver confirms the release before the next approver or you can choose the last option which means any single approver can approve a deployment. Use the timeout settings to cancel a release if it's not approved after the set timeframe. Let's change this to 30 minutes and I'll delete this group. You have approval policies like denying a user that triggered a release from approving it. This is a good policy for better control. You also have revalidate the approver's identity which will prompt for login details and the last option to skip approval if previous stage was approved by the same person. There are scenarios where you'd want to have a standalone stage such that it's not triggered after a stage, but after a new artifact is available. To do that, use the trigger option at the top
>
> **[3:11](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/reconfigure-your-production-stage-release-to-deploy-to-a-staging-slot?u=76281980&t=191)** and you can select after release. This means that while your new artifacts will trigger a release to the development stage, it'll also be queued for the QA stage. I'll change this back to after stage. Let's assume the policy for a QA team requires that every successful app testing must be followed by a member of the QA team approving a post-deployment in the QA stage. You can do this in the post-deployment conditions so click on the post-deployment conditions and then you can enable post-deployment approvals and select either the QA team or the QA lead as part of the approvals. I won't be setting this up here but it's something you can explore in your next CI/CD project. Let's disable this and close the pin. Let's quickly set up pre-deployment approvals for the production environment. So click the conditions button for the approval stage and then scroll down to enable pre-deployment approvals and then search for and select your name. I'll change the time-out to 30 minutes. Close the setting screen and click the save button. You can provide a comment, configure pre-deployment approvals for QA and prod stage.
>
> **[4:49](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/reconfigure-your-production-stage-release-to-deploy-to-a-staging-slot?u=76281980&t=289)** Then click the okay button. Now let's test this. Click the create release button at the top right and then let's click the create button. Click the release to in the green bar and let's wait for this to start running. When the dev stage deployment is successful, there is an approved button in the QA stage. If you are not logged into DevOps at the time this runs, you can always check your email for an approval notification. You can approve this and then click on the approve button. To see what happens to the QA stage, after the 30 minutes timeout, try to trigger the release pipeline again, but without approving the QA stage. After that, recall earlier that for the production environment, we need to minimize service disruption or ensure application availability even during deployment of new versions of a web application. As a challenge, modify the production stage in the release pipeline and configure tasks to enable deployments to a staging slot. Remember to create a staging slot in your production web app first so you can reference that in your pipeline.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[DevOps]] (2), [[Microsoft Azure|Azure]] (1)
> **UI Navigation:** click on (3), scroll down (2), navigate to (1), select the (1)
> **Prerequisites:** configure (3), set up (1)
> **Definitions:** is a  (1), means that (1), is an  (1)
> **CLI Commands:** cd (1)
> **Analogies:** for example (1)
> **Best Practices:** remember to (1)
> **Speakers:** - now (1)

#### [Release pipelines for staging slot deployments](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/release-pipelines-for-staging-slot-deployments?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/release-pipelines-for-staging-slot-deployments?u=76281980&t=0)** - I hope you found out what happens if a deployment to the QA stage is not approved before it times out. For me I didn't approve the production stage, and it was automatically rejected. Now if you need to redeploy a particular version to a particular stage, you can always go to releases, select the release, and then click on redeploy for a particular stage. You would see the artifact that will be deployed, the version and the branch. Click on deploy, and then it'll request for approval. Then just click on the approve button. You can provide a comment if you'd like and then click approve. So this will start deploying. You can view the logs if you'd like, but while this is deploying, let's solve the challenge from the previous video. So rather than deploy directly to the production web app, we want to deploy a new version to a staging slot to minimize service disruptions. So we will have a task in our production stage to deploy to the staging and swap the staging slot with the production slot. Before we can do that, let's go create a staging slot in our production web app.
>
> **[1:34](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/release-pipelines-for-staging-slot-deployments?u=76281980&t=94)** So go to your [[Microsoft Azure|Azure]] portal, and if you are not on your production web app, simply click the menu button and then select app services. Make sure the appropriate subscription is selected then click apply. Then you can now select your production web app. In the production web app under deployment category, let's click on deployment slots. I don't have a deployment slot because I was attempting to save costs. So let's upgrade our service plan to a standard tier. So click the upgrade button, and in this specification page, click on production, and then click see additional options. Then select S one and click the apply button. We can quickly delete these after testing the pipeline, and we'll only pay for the hours it's been used. Let's close this page and then in the deployment slots page for the production app, click the refresh button, and we now have a slot available for the production web app. Let's add a new slot. To do that, click the add slot button at the top, and let's provide a name for this slot. Let's call it staging. And underneath the name field,
>
> **[3:07](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/release-pipelines-for-staging-slot-deployments?u=76281980&t=187)** you can see the URL for the staging slot. You can decide to clone settings from the production app or create new settings for the staging slot. Let's clone new settings. So click the dropdown and select the student app. Then click add. Once the slot is created, let's close this, and then back in my Azure [[DevOps]] portal Navigate To release this. Make sure the student app pipeline is selected and then click the edit button. There are a number of possible configurations you can use. One is modifying the production stage to deploy to the staging slot of your web app. You'll select the resource group and the slot from the dropdown list. And then after every successful deployment to the staging slot, you have to manually add over to the app service in Azure portal and click the swap button to make the staging production, and the production becomes the staging. That's quite a cumbersome task, but this is useful for maybe unstable applications that require close monitoring when it's being deployed. Another option is to add an additional stage between the QA and the production
>
> **[4:40](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/release-pipelines-for-staging-slot-deployments?u=76281980&t=280)** and then add a task to deploy to the staging environment. You can always add an approval gate so that deployments to staging are first approved. But for this class, I will simply add a task to my production slot to swap the staging for production after a successful deployment to the staging slot. So to do that, let's open the one job, one task link for production stage and then click the add button beside the agent. And let's search for swap. Select the Azure app service manage, and click the add button. Now let's select the Deploy Azure app service task and then scroll down and enable deploy to slot or app service environment. Click on the dropdown to select the resource group, and then by default it selects the production. So let's change this to staging, and that's it. So let's go to the swap slots task and select the appropriate Azure subscription. Make sure the action is swap slots and select the app service name. So we'll select the production app service and choose the appropriate resource group
>
> **[6:17](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/release-pipelines-for-staging-slot-deployments?u=76281980&t=377)** and then select the swap slot, in this case staging. So in this case, we want to swap staging with production. Make sure the swap with production checkbox is checked, but if you have other slots you wish to be the target then you can uncheck this option and specify the target slots for swap. And that's it. So when the release workflow gets to the production stage, it'll deploy your web app first to the staging slot as defined in the first task, and then swap staging slots with production as defined in the second task. Let's click the save button to save this. You can provide a comment and then click okay.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (6), [[DevOps]] (1)
> **UI Navigation:** select the (10), click on (6), go to (3), dropdown (3), navigate to (1)
> **CLI Commands:** make (5)
> **Tools:** azure portal (2)
> **Env Vars:** url (1)
> **Cross-References:** previous video (1)
> **Documentation:** specification (1)
> **Speakers:** - i (1)

#### [Test release to deployment slots](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/test-release-to-deployment-slots?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/test-release-to-deployment-slots?u=76281980&t=0)** - Now that we are done defining the swap slot task for the production stage of our release pipeline, let's quickly test our pipeline. First, let's browse to the staging slot URL, to see what we find in the page. So, head back to the [[Microsoft Azure|Azure]] portal page and then make sure you're in the staging app service and click the browse button. And we see here the service is unavailable. This is because we are yet to deploy any application to the staging slot. However, if you check the production slot URL you can see the website is displayed. Now let's imagine a scenario where, a developer has been asked to modify this application to include a serial number column, for better [[User Experience (UX)|user experience]]. To do this, let's head back to our Visual Studio Code project. And before we make any change, let's click on the branch menu, and then select the main branch. You can see here, the change we made to our feature index branch is not yet pulled into our local repository. So to fix this, let's click on terminal, at the top. And click new terminal. Now, once the terminal is opened, we need to pull changes from the remote repository, so type "get pool" and then enter.
>
> **[1:36](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/test-release-to-deployment-slots?u=76281980&t=96)** And now we have the change in our local repository. Let's create a new branch so that we can make the change and add a serial number column to our project. So at the lower left corner of the screen, click the branch menu, and select create new branch. Let's call this branch features slash serial number. And then press enter. Now let's close these terminal. And then after line 17, let's create a new line. And then add "TH" and then let's type serial, S slash N shot for serial number. And then where we have the T body, let's create a new line and then paste this function. And then after reaching our elemental and line 30 let's create a new line and then paste dysfunction. This will loop and increment, the serial number value from one to, whatever number of items we have in the table. Let's quickly save this so you can press CTRL S to save. Now let's push this code to the remote repository. At the top, click on the terminal menu, and select new terminal. To stage the change, let's type GI add dot,
>
> **[3:12](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/test-release-to-deployment-slots?u=76281980&t=192)** and enter. And to commit, get commit dash M, and you provide a comment. At that serial number. Press enter. And then to push, let's type "get push" dash, dash, set, dash, upstream, origin, and then the name of the branch which is features, slash, serial number. Enter. And it's has pushed successfully to the remotes repository. So let's go to create a pool requests from this feature branch to the main branch in Azure [[DevOps]] page. So back in my Azure DevOps page, I'll navigate to records, and you should see a notification that you updated the serial number feature branch some minutes ago. Let's click create a pool request button. And here you can see the three lines we added to this file. Back in the overview page let's try to create the pool request. So click on create, and you should see the student app, PR bill policy is triggered. so we'll have to wait a while for this to be completed.
>
> **[4:45](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/test-release-to-deployment-slots?u=76281980&t=285)** This will trigger the pipeline to run where it'll build the code and then try to scan the code using the SNA cloud task. We defined earlier. The build policy was successful, and you can view the two checks that was run. And you can see here we have the required policy, which is the student app pool request build policy, as well as the quality gate from the sonar cloud report. So let's close this and approve the pool request. To do that, click the approved button at the top. And now we can complete this pool request can be pulled into the main branch so click on the complete button at the top, and let's leave the delete feature branch after Managing option checked, and then click complete merge. And that's it. So if we go to the pipelines at the left, and check the build pipeline. It should be triggered for the main branch. This is automatic meaning, anytime a developer make changes and pushes to a feature branch, then create a pool request, which is then approved. The main branch will trigger the pipeline to run and then we'll see how the release pipeline would run. You can click on this pipeline to view the logs. And then select agent job.
>
> **[6:22](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/test-release-to-deployment-slots?u=76281980&t=382)** This time around an artifact will be created because the main branch triggered this pipeline. The build for the main branch was successful and we have an artifact created. So let's navigate to release this and then check what happens to a dev stage. And we see here, it's already triggered for the dev stage. To view this progress, let's click on release four and you can click on the logs, to view the progress for the development stage. As usual, the latest artifacts is downloaded and then it's now deploying to the Azure Azure App Service. This Is successful, so let's go to our, Azure Azure portal. And then open our app services. Let's filter based on the lending sub, subscription. And then click apply. And now I want to view my dev euro. So I'll open the dev web app and then, click on the browse button. Cool! And I'll see the serial number column in my webpage. So let's go back to Azure portal, and check for the QA web app. So select the QA. And then click on the browse button. We still have the old app because the serial number is not displayed here, and that is because we've not approved the deployment
>
> **[7:57](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/test-release-to-deployment-slots?u=76281980&t=477)** to the QA environment. To do that, let's quickly go back to Azure DevOps page and select the pipeline. And then let's click on the, approved button for the QA stage. You can add a comment, and then click approve. Let's click on the view logs to monitor the progress. The release to the QS stage is successful so let's go back to uh, QA web app, and then refresh the page. And now we see a serial number here. Awesome! So let's assume, QA team is satisfied with the changes made and it's time to push to the production environment. I'll just add over over to the release page on Azure DevOps. Select the release pipeline and let's close this. Then under the production stage, I'll click the approved button. and then click approved button again. You can view the logs by clicking on the view logs button and then let's wait a while for this to run. As usual, the initialized job tax would run fast, and then the latest artifact is downloaded. Then this will deploy the Azure app service to the staging SLT,
>
> **[9:32](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/test-release-to-deployment-slots?u=76281980&t=572)** and once it's done deploy into the staging SLT, it'll then swap the SLTs. The tasks include warming up the SLT's and then swapping up service SLT's, staging, and production. Once the production stage deployment is successful let's try to browse our production URL. So I'll navigate to the website and then click the refresh button. And now we see the serial number in the production app. If we go to the staging SLT's, and then refresh. We now see our web app, but the old version without any serial number column. So this is how C I C D pipelines, help eliminate deployment issues for your development team. Quick, easy, and reliable workflow that runs the same for every change, that needs to make it to all your linked environments. Assuming we need to roll back the latest change for the production environment. I can go back to my Azure portal, and click on the production web app, then select deployment slots and then use the swap button gear to swap between production and staging. Or back in my DevOps portal I can go back and select a previous release. Select release three, for example.
>
> **[11:10](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/test-release-to-deployment-slots?u=76281980&t=670)** And then try to redeploy for a production stage. This will still require an approval, and you can get the necessary approval and redeploy old versions of your release. Once this is completed, you can still browse the URL, and you see that the previous version, without the serial number, will be deployed to the production slot.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (12), [[DevOps]] (5), [[User Experience (UX)|User experience]] (1)
> **UI Navigation:** click on (13), select the (4), go to (4), navigate to (3), open the (1)
> **Env Vars:** slt (5), url (4), ctrl (1), sna (1)
> **Tools:** terminal (6), azure portal (4), visual studio (1)
> **CLI Commands:** make (5), find (1)
> **Cross-References:** go back to (4)
> **Analogies:** imagine (1), for example (1)
> **Speakers:** - now (1)


### 4. Maximizing Azure Pipelines

[↑ Back to Table of Contents](#table-of-contents)

#### [Configure notifications for successful deployments](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-notifications-for-successful-deployments?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-notifications-for-successful-deployments?u=76281980&t=0)** - [Instructor] You may have noticed that you received notifications for release approvals, timeouts, or even a fill deployment but no notification is sent for successful deployment of artifacts to environment. Let's learn how to set up notifications for release pipelines that have successfully deployed artifacts to linked environments. There are two ways to set up notifications: at the [[DevOps]] organization level, and in each project. To set up notifications at the organization level, click Organization Settings. Under General, click on global notifications. You can review some default notifications, like some for build and code. If you scroll down, you should find some notifications for release when manual intervention is pending and even when deployment approval is pending. Now, to create a new notification, let's click on subscribers at the top and let's search for a group project collection valid users. Then click Search. Make sure that your organization name is shown and then project collection valid users. Click to select this group and then here we have some notifications. Let's set up the notification for a successful deployment.
>
> **[1:36](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-notifications-for-successful-deployments?u=76281980&t=96)** To do that, click on the new subscription button at the top and under Category, select Release, and then select a deployment is completed template. Click the next button, and here we see a description for this new subscription. We have the subscriber selected and the deliver to would be members of project collection, valid users by roles. So you can specify the roles to limit who receives the notification for a successful deployment In a typical setting, business stakeholders who are part of your DevOps project don't need this notification from release, so you can filter persons by roles to receive specific notifications. Click on the roles dropdown and we have the deployment requested for stage owner, release requested by and approved by. I'll just uncheck deployments requested for and then close this list. We also need to specify any team project so that this notification can work for future projects that will be created and even existing projects in the organization. Now let's set up a filter criteria. Under the field, click on the dropdown and select deployment status. Make sure the operator is the equal sign and then the value should be succeeded.
>
> **[3:12](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-notifications-for-successful-deployments?u=76281980&t=192)** And that's it. Let's click the finish button, and if you check under the release category, you can find a deployment is completed. Subscription has been created. Now let's quickly test this. Let's go back to the [[Microsoft Azure|Azure]] DevOps home page and then select the student app project. Let's go to pipelines and select releases. We will create an auto release for the development stage while the QA and production would be changed to manual. Make sure you selected the pipeline and click create release button at the top right. Then let's change this stage, the QA and production to manual so it doesn't trigger, and then let's click the create button. Now you can click the release six at the top and the development stage should start running soon. Awesome, once it starts running, you can click the logs to view that. The deployment to development stage is successful and now you can check your email to confirm if you got a notification. Nice, I have email from Azure DevOps showing a deployment to development succeeded with the release description, who requested the release, the branch is deployed to and as well as the artifact version.
>
> **[4:48](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/configure-notifications-for-successful-deployments?u=76281980&t=288)** Cool, right? If you want to create this via the project settings, all you have to do is go to the project setting page, click on notifications and select a new subscription to follow the same process as we configured earlier in the global notification.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[DevOps]] (4), [[Microsoft Azure|Azure]] (2)
> **UI Navigation:** click on (6), dropdown (2), scroll down (1), select the (1), go to (1)
> **CLI Commands:** make (3), find (2)
> **Prerequisites:** set up (5)
> **Cross-References:** go back to (1), earlier in (1)
> **Exercise Files:** template (1)
> **Speakers:** - [instructor] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/next-steps?u=76281980&t=0)** - [Prince] CI/CD solutions With [[Microsoft Azure|Azure]] [[DevOps]] is a great tool to eliminate deployment toil and enable faster build, test, and delivery of your software applications to your users. I enjoy creating pipelines for new projects while continuously improving existing pipelines with the help of a task group template to accommodate changes in software applications. As you prepare to start your journey with configuring Azure build and release pipelines for your next project, there are many additional resources to help you along the way. Check out courses on Azure DevOps in the [[LinkedIn]] Learning Library as well as documentations or tutorials on [[Microsoft]] Learn. If you are already using [[GitHub]] as your source control and would like to learn how to integrate GitHub with Azure DevOps to manage your CI/CD workflow, then you can learn from my course on integrating Azure DevOps with GitHub here in the library. As a reminder, we learned about the concept of Azure Pipelines, how to configure a pipeline and integrate SonarCloud to scan the source code for vulnerabilities. We also found out how to protect an important branch, the main branch, so that only approved changes are merged from other branches. Then, new commits to the main branch triggers a build, produces an artifact,
>
> **[1:35](https://www.linkedin.com/learning/developing-ci-cd-solutions-with-azure-devops-22123562/next-steps?u=76281980&t=95)** and the release pipeline deploys the artifacts to the linked environment. And lastly, we learned how to minimize service disruption by deploying to a staging slot and swapping that with production in Azure. I encourage you to explore using YAML syntax in configuring your CI/CD pipelines and learn more about branching strategies like the trunk-based development, GitFlow, or GitHub flow. Whatever stage you are in your CI/CD journey, I hope this course has made it easier to understand the concepts and be able to build CI/CD solutions with Azure DevOps for your next set of [[Software Projects]]. Thank you for watching this course, and I wish you luck in your CI/CD journey.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Azure|Azure]] (8), [[DevOps]] (5), [[GitHub]] (4), [[LinkedIn]] (1), [[Microsoft]] (1)
> **CLI Commands:** cd (6)
> **Tools:** github (4)
> **Exercise Files:** template (1), source code (1)
> **Env Vars:** yaml (1)
> **Definitions:** is a  (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [prince] (1)


## Instructor

- [[Prince Mokut]]

## Path Context

### In [[Advancing Your Azure Developer Skills- Exploring Complex Application Development]]
**1 of 7** | [[Migrating to Azure]] →

### In [[Getting Started as an Azure Developer]]
← [[Azure DevOps for Beginners]] | **7 of 7**

## Appears In

- [[Advancing Your Azure Developer Skills- Exploring Complex Application Development]]
- [[Getting Started as an Azure Developer]]

---

[↑ Back to top](#)