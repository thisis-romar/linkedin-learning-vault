---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: kotlin-multiplatform-development
url: "https://www.linkedin.com/learning/kotlin-multiplatform-development"
duration_minutes: 153
duration: 2h 33m
level: Intermediate
updated: 5/21/2025
learners: 3069
skills:
  - Kotlin
  - Cross-platform Development
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D4E0DAQGy6ak9gCh6EA/learning-public-crop_675_1200/B4EZbaHVwdGcAc-/0/1747416079622?e=2147483647&amp;v=beta&amp;t=hzdneffla9nn0g9EWrfoBcVeXyiV5XJ_1OLHuW2k5qE"
linkedin_topic: Mobile Development
learning_paths:
  - '[Kotlin Professional Certificate by JetBrains](../../Paths/Mobile%20Development/Professional%20Certificates/Kotlin%20Professional%20Certificate%20by%20JetBrains.md)'
prev_courses:
  - '[Kotlin Essential Training- Object-Oriented and Async Code](Kotlin%20Essential%20Training-%20Object-Oriented%20and%20Async%20Code.md)'
next_courses:
  - '[Exploring Ktor with Kotlin Multiplatform and Compose](Exploring%20Ktor%20with%20Kotlin%20Multiplatform%20and%20Compose.md)'
path_nav: '[{"path":"Kotlin Professional Certificate by JetBrains","position":3,"total":4,"prev":"Kotlin Essential Training- Object-Oriented and Async Code","next":"Exploring Ktor with Kotlin Multiplatform and Compose"}]'
path_count: 1
tags:
  - course
  - topic/mobile-development
  - topic/software-development
  - skill/kotlin
  - skill/cross-platform-development
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Mobile%20Development/Kotlin%20Multiplatform%20Development.md)

![Kotlin Multiplatform Development](https://media.licdn.com/dms/image/v2/D4E0DAQGy6ak9gCh6EA/learning-public-crop_675_1200/B4EZbaHVwdGcAc-/0/1747416079622?e=2147483647&amp;v=beta&amp;t=hzdneffla9nn0g9EWrfoBcVeXyiV5XJ_1OLHuW2k5qE)

# Kotlin Multiplatform Development

> Master cross-platform development with Kotlin by creating sophisticated applications that run seamlessly across Android, iOS, web, and desktop. In this comprehensive course, instructor Colin Lee empowers you to make smart architectural choices between Compose Multiplatform's shared UI approach and platform-specific interfaces like SwiftUI, balancing code reuse with native feel. Learn how to create

> [LinkedIn Learning](https://www.linkedin.com/learning/kotlin-multiplatform-development) | 2h 33m | Intermediate | 3K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Kotlin multiplatform development](#kotlin-multiplatform-development)
  - [What you should know](#what-you-should-know)
- [**1. Creating Our First Multiplatform App**](#1-creating-our-first-multiplatform-app) (7 videos)
  - [The problem Kotlin Multiplatform solves](#the-problem-kotlin-multiplatform-solves)
  - [Comparison with alternatives](#comparison-with-alternatives)
  - [Supported platforms and performance](#supported-platforms-and-performance)
  - [Just what the KDoctor ordered](#just-what-the-kdoctor-ordered)
  - [Android studio for KMP](#android-studio-for-kmp)
  - [Xcode for KMP](#xcode-for-kmp)
  - [Crafting a template with the Kotlin Multiplatform wizard](#crafting-a-template-with-the-kotlin-multiplatform-wizard)
- [**2. Combining Native with Multiplatform**](#2-combining-native-with-multiplatform) (6 videos)
  - [Building our first multiplatform app](#building-our-first-multiplatform-app)
  - [Whether to use Compose Multiplatform](#whether-to-use-compose-multiplatform)
  - [Expect and actual](#expect-and-actual)
  - [An actual example](#an-actual-example)
  - [Accessing operating system methods](#accessing-operating-system-methods)
  - [Multiplatform project structure](#multiplatform-project-structure)
- [**3. KMP and Swift**](#3-kmp-and-swift) (3 videos)
  - [Using Kotlin from Swift code](#using-kotlin-from-swift-code)
  - [Making it easier with Swift export](#making-it-easier-with-swift-export)
  - [KMP together with SwiftUI](#kmp-together-with-swiftui)
- [**4. Architecture**](#4-architecture) (2 videos)
  - [Recommended architecture with MVVM](#recommended-architecture-with-mvvm)
  - [Google ViewModel](#google-viewmodel)
- [**5. Sharing Network**](#5-sharing-network) (4 videos)
  - [Adding Ktor to your project](#adding-ktor-to-your-project)
  - [Modeling with Kotlinx-serialization](#modeling-with-kotlinx-serialization)
  - [Making network requests](#making-network-requests)
  - [Apollo GraphQL](#apollo-graphql)
- [**6. Sharing Database**](#6-sharing-database) (4 videos)
  - [Adding SQLDelight to your project](#adding-sqldelight-to-your-project)
  - [Setting up your database and adding objects](#setting-up-your-database-and-adding-objects)
  - [Creating queries](#creating-queries)
  - [Going with the flow](#going-with-the-flow)
- [**7. Quality of Life**](#7-quality-of-life) (5 videos)
  - [Kermit for logging](#kermit-for-logging)
  - [Managing resources](#managing-resources)
  - [KOIN for dependency injection](#koin-for-dependency-injection)
  - [Storing values in multiplatform-settings](#storing-values-in-multiplatform-settings)
  - [Dates in Kotlinx-datetime](#dates-in-kotlinx-datetime)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Kotlin multiplatform development](https://www.linkedin.com/learning/kotlin-multiplatform-development/kotlin-multiplatform-development?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/kotlin-multiplatform-development?u=76281980&t=0)** - Are you ready to explore the magic of [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md)? In this course, I'll provide you with a broad overview of the topic. For example, we will explore many libraries that make it possible for you to write an app that runs across many web, desktop, and mobile platforms. But these solutions will require hardly any platform specific code. Hi, I'm Colin Lee. I'm an experienced mobile engineer at an AI startup. I launched a [Kotlin](../../Skills/Software%20Development/Kotlin.md) user group even before [Google](../../Glossary/Service/Google.md) announced official support for Kotlin and [Android](../../Glossary/Sdk/Android.md). So let's go. I'm excited to share with you the magic of Kotlin Multiplatform.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (2), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (2), [Google](../../Glossary/Service/Google.md) (1), [Android](../../Glossary/Sdk/Android.md) (1)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Speakers:** - are (1)

#### [What you should know](https://www.linkedin.com/learning/kotlin-multiplatform-development/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/what-you-should-know?u=76281980&t=0)** - [Instructor] Here's what you should know. Since this course is not going to cover the basics, you may want to consider an intro to [Kotlin](../../Skills/Software%20Development/Kotlin.md). You could simply read the Kotlin tour on the official website. Just go to [kotlinlang.org](https://kotlinlang.org) and click on "Get Started." The official Kotlin tour is short, sweet, and won't ask you to rate it five stars at the end. However, you shouldn't need too much preparation. If you've used a similar language with strong types, like [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md), Kotlin will feel quite familiar. You will also need to know how to install software on your computer and how to use a terminal or command line in your operating system.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kotlin](../../Skills/Software%20Development/Kotlin.md) (4), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Tools:** terminal (1), command line (1)
> **UI Navigation:** go to (1), click on (1)
> **URLs:** [kotlinlang.org](https://kotlinlang.org) (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)


### 1. Creating Our First Multiplatform App

[↑ Back to Table of Contents](#table-of-contents)

#### [The problem Kotlin Multiplatform solves](https://www.linkedin.com/learning/kotlin-multiplatform-development/the-problem-kotlin-multiplatform-solves?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/the-problem-kotlin-multiplatform-solves?u=76281980&t=0)** - [Instructor] Why are we discussing [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md)? Cost complexity and trade-offs make it the best solution for many apps. Let me explain. Native apps have traditionally provided the best experience for users. What is a native app? A native app uses the best language and user interface libraries to run natively on your system. By using methods supported by your operating system vendor, your app is more likely to look like an app that belongs on that platform and to work in a way that is more intuitive to users of that operating system. Writing a traditional native app across iOS, [Android](../../Glossary/Sdk/Android.md), MacOs, [Windows](../../Glossary/Service/Windows.md), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md), and web requires a large team of highly paid developers communicating closely with one another. It is a very expensive and it's challenging to write one app that looks beautiful and runs nicely across all of these devices. Typically, one developer lacks the specific knowledge of languages and frameworks and the time to port one app to all of these different systems. Also, each platform ends up with unique bugs based upon the different development teams working on each platforms app. Enter hybrid frameworks. These were intended to solve the difficulty in knowing all of these systems, and to produce a native app across all of them. Most hybrid frameworks substitute calls in one language,
>
> **[1:37](https://www.linkedin.com/learning/kotlin-multiplatform-development/the-problem-kotlin-multiplatform-solves?u=76281980&t=97)** like [JavaScript](../../Skills/Software%20Development/JavaScript.md) or Dart, with user interface calls in the native language. While hybrid frameworks seem convenient and they would seem to solve the difficulty of producing an app across a wide spectrum of platforms, they have several key downsides. Debugging is challenging since multiple languages are involved, and the programmer may not understand the native code. The languages used tend to have poor performance, like JavaScript, or may be less known outside of hyper development, such as Dart. Apps which require careful handling of device [Hardware](../../Topics/Hardware.md), like camera, Bluetooth, and others, may be more challenging to write and to maintain. Kotlin Multiplatform handles most of the downsides of hybrid frameworks admirably. What is Kotlin Multiplatform? In 2017, the [Kotlin](../../Skills/Software%20Development/Kotlin.md) language team at JetBrains added support to allow running software written in Kotlin across a wide variety of different hardware and software types. They added support to allow writing apps for the [Java](../../Skills/Software%20Development/Java.md) Virtual Machine, or JVM, native on the LLVM, and web. However, each platform can still benefit from custom per-platform code in some instances. So they added a mechanism to the core Kotlin language to allow running a different function or method depending upon the underlying platform. That announcement was very popular and exciting,
>
> **[3:13](https://www.linkedin.com/learning/kotlin-multiplatform-development/the-problem-kotlin-multiplatform-solves?u=76281980&t=193)** but it was still difficult to write software because you then had to write slightly different code to accommodate all the differences between platforms. This all changed with the development of Kotlin Multiplatform libraries. The complexity and cost of developing high-quality apps for many platforms has made native development a challenge. Meanwhile, hybrid frameworks often fall short. Kotlin Multiplatform changed the game by offering a method to share logic while still embracing the unique strengths of each platform.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (5), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (3), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [Hardware](../../Topics/Hardware.md) (2), [Android](../../Glossary/Sdk/Android.md) (1)
> **Env Vars:** jvm (1), llvm (1)
> **Definitions:** is a  (2)
> **CLI Commands:** make (1)
> **Code Identifiers:** ios (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Comparison with alternatives](https://www.linkedin.com/learning/kotlin-multiplatform-development/comparison-with-alternatives?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/comparison-with-alternatives?u=76281980&t=0)** - [Instructor] What if you could code native apps for every device without giving up on the performance, the flexibility, or the unique look and feel of each platform. This is what [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md) or [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) offers. So how does KMP compare to hybrid alternatives? Today, most software is composed of libraries. Since 2017, many [Kotlin](../../Skills/Software%20Development/Kotlin.md) software libraries have been written for Kotlin Multiplatform. This means you might be able to write a simple app without writing any code specific to the operating system or at [Hardware](../../Topics/Hardware.md). In addition, you can mix and match Kotlin with other languages and incorporate shared business logic into native user interfaces. For example, Kotlin Interoperates with Objective-C and [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) Code for writing apps for Apple devices like iOS, macOS, watchOS, and visionOS. Your software teams can decide how much they want to share code to deliver software faster, or how much they would prefer to have native user interfaces that are easy to use and which native developers will feel more comfortable working with. Since all of your code is actually running in the best native target for your operating system, Kotlin Multiplatform is also very fast and offers native performance. Many companies have adopted KMP or Kotlin Multiplatform, and have found it to perform very well for their apps.
>
> **[1:34](https://www.linkedin.com/learning/kotlin-multiplatform-development/comparison-with-alternatives?u=76281980&t=94)** Here is a handful of companies that use KMP and have nice things to say about it. I personally use KMP to revitalize the apps that we wrote at Meetup. There are a lot of common misconceptions about KMP. Here are a few. Some assume it's simply another hybrid framework like [React Native](../../Skills/Mobile%20Development/React%20Native.md). While there is a user interface framework available called [Compose](../../Glossary/Framework/Jetpack%20Compose.md) [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md), KMP can be used with entirely native UI as well. Also, KMP works across a very wide spectrum of platforms and is not limited to mobile or even client apps. Many case studies are also very complex apps and they benefit extensively from the way that Kotlin Multiplatform allows sharing of logic. Since KMP uses the best native code generator for most platforms, it is typically comparable in performance to native. As I found during my Meetup project work, native developers can quickly start contributing code to KP projects if there's someone to support the team with Kotlin Multiplatform knowledge. Finally, there is a large, robust community centered on KP mostly due to [Android](../../Glossary/Sdk/Android.md) apps, which also share code with iOS. That community is very competent and quite often does not limit their libraries exclusively to mobile. Kotlin Multiplatform stands apart from hybrid frameworks. By combining the performance and the flexibility of Native,
>
> **[3:09](https://www.linkedin.com/learning/kotlin-multiplatform-development/comparison-with-alternatives?u=76281980&t=189)** with the efficiency of shared business logic, KP allows infinite mixing and matching to find the most efficient solution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kmp](../../Glossary/Framework/Kotlin%20Multiplatform.md) (9), [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (7), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (3), [Hardware](../../Topics/Hardware.md) (1), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Env Vars:** kmp (9)
> **Code Identifiers:** ios (2), macos (1), watchos (1), visionos (1)
> **Definitions:** is a  (3)
> **CLI Commands:** find (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Supported platforms and performance](https://www.linkedin.com/learning/kotlin-multiplatform-development/supported-platforms-and-performance?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/supported-platforms-and-performance?u=76281980&t=0)** - [Instructor] What app would you build if you knew it could work on every device and operating system? [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) supports nearly all operating systems and [Hardware](../../Topics/Hardware.md) in wide use today; this includes mobile, desktop, web, and server. In order to support this wide range of hardware and operating systems, [Kotlin](../../Skills/Software%20Development/Kotlin.md) uses four code generators: their [Java](../../Skills/Software%20Development/Java.md) Virtual Machine or JVM; Native or LLVM; [JavaScript](../../Skills/Software%20Development/JavaScript.md), and WebAssembly. These four code generators allow Kotlin to target most modern devices and operating systems: [Android](../../Glossary/Sdk/Android.md), iOS, [Windows](../../Glossary/Service/Windows.md), Mac OS, [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md), web, WebAssembly, server-side backend web, watchOS, tvOS, even Raspberry Pi, or any device that can run Java, C, or JavaScript. Kotlin is known to interoperate quite nicely with a good number of alternative programming languages. The best are like Java, Objective-C, [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md). It's decent at other JVM languages, C/C++, [Rust](../../Skills/Software%20Development/Rust%20(Programming%20Language).md), and JavaScript; and only okay maybe at like Go, [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), and any other language with C bindings. Based on Kotlin's four code generators, there are also numerous options to choose in terms of user interface frameworks. Here's a small sampling: [Jetpack Compose](../../Glossary/Framework/Jetpack%20Compose.md), SwiftUI, [React](../../Skills/Web%20Development/React.js.md),
>
> **[1:36](https://www.linkedin.com/learning/kotlin-multiplatform-development/supported-platforms-and-performance?u=76281980&t=96)** any JavaScript framework, Flutter, [Angular](../../Skills/Web%20Development/Angular.md), Spring. So how easy is it to use Kotlin on these platforms? What are the best-case platforms that are the easiest to use today? I would say the easiest is Android and iOS with [Compose](../../Glossary/Framework/Jetpack%20Compose.md) and SwiftUI, or Jetpack Compose, Windows, Mac OS, and Linux with Compose Desktop, and back-end web with [Ktor](../../Skills/Software%20Development/Ktor.md) and Spring. Harder might be Kotlin with React and JavaScript frameworks or WebAssembly, which is still fairly young. And hardest would be all the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the platforms. Compose [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) is a technology which uses the Jetpack Compose Android user interface library to create shared user interface code across nearly all supported devices. There are also numerous software libraries supporting with this framework, which function decently across many of the devices. As you have learned in this section, Kotlin Multiplatform offers exceptional support across a wide range of platforms. It also supports interoperability with many other languages as well as user interface frameworks. By using Kotlin, you should be able to target nearly any system once you have the right skills.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kotlin](../../Skills/Software%20Development/Kotlin.md) (7), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (5), [Java](../../Skills/Software%20Development/Java.md) (3), [Android](../../Glossary/Sdk/Android.md) (3), [Jetpack Compose](../../Glossary/Framework/Jetpack%20Compose.md) (3)
> **Code Identifiers:** ios (2), watchos (1), tvos (1)
> **Env Vars:** jvm (2), llvm (1)
> **CLI Commands:** python (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Just what the KDoctor ordered](https://www.linkedin.com/learning/kotlin-multiplatform-development/just-what-the-kdoctor-ordered?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/just-what-the-kdoctor-ordered?u=76281980&t=0)** - [Instructor] Before we get started programming, it's important to ask if our system is ready. There could be a number of things required to build and run a [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) app. To make sure that we're ready, we're going to take a trip to the doctor, specifically KDoctor. Now KDoctor is only for macOS. However, you won't generally be able to develop software for iOS or macOS unless you have a macOS device for development. You will still be able to use Kotlin Multiplatform and take this course, however, even if you can't develop for Apple devices. To get started installing KDoctor, you will need Homebrew. If you don't already have it installed, you'll find the steps to install it at brew.sh. I will walk through this with you. Here I'm copying the URL and I'm going to go into a command line terminal and paste this content here into the terminal. After you run the bash command to install Homebrew, it will walk you through the process. Here I have to put in my sudo password and just follow along in the interactive terminal. With Homebrew installed, we simply need to run the brew install kdoctor. First I'm going to run through the steps here to add it to my path. Just copy and paste and then brew install kdoctor. Once installation is completed, we can then run KDoctor
>
> **[1:39](https://www.linkedin.com/learning/kotlin-multiplatform-development/just-what-the-kdoctor-ordered?u=76281980&t=99)** and it will run us through the steps needed to have a fully working Kotlin Multiplatform environment. As you can see, it can't find [Java](../../Skills/Software%20Development/Java.md) or the [Xcode](../../Skills/Mobile%20Development/Xcode.md) command line tools. It's using system Ruby, which is not good and does not have CocoaPods. So keep running KDoctor until you no longer see required steps to perform. If you're not developing on a macOS device, but are using [Windows](../../Glossary/Service/Windows.md) or [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) instead, we have a number of manual steps you're going to need to perform. First, install [Android](../../Glossary/Sdk/Android.md) Studio from the official website. This will give you all the Android SDK tools, as well as Android Studio. Second, make sure JAVA_HOME is set in your environment to working Java development kit. Android Studio provides one, so try running Java from the command line and ensure that it runs without errors. There we go, java -version. And if it doesn't run correctly, make sure you add the correct Java development kit to your path. Third, install the Kotlin Multiplatform plugin in your Android Studio. So you go to Settings, Plugins, and Marketplace, and then you would search for it. I already have it installed here as you can see, but just make sure that it's installed and fully updated. Whether you're using macOS, Windows or Linux, following the outline setup process will ensure that you have the tools and the environment ready
>
> **[3:14](https://www.linkedin.com/learning/kotlin-multiplatform-development/just-what-the-kdoctor-ordered?u=76281980&t=194)** to build and to run your projects with confidence.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (5), [Android](../../Glossary/Sdk/Android.md) (5), [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (4), [Windows](../../Glossary/Service/Windows.md) (2), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (2)
> **CLI Commands:** make (4), brew (3), find (2), sudo (1), ruby (1)
> **Tools:** command line (3), terminal (3), bash (1)
> **Prerequisites:** install (6), setup (1)
> **Code Identifiers:** macos (5), ios (1)
> **Env Vars:** url (1), sdk (1), java_home (1)
> **File Paths:** brew.sh (1)
> **UI Navigation:** go to (1)

#### [Android studio for KMP](https://www.linkedin.com/learning/kotlin-multiplatform-development/android-studio-for-kmp?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/android-studio-for-kmp?u=76281980&t=0)** - [Instructor] For working with [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md), a number of integrated development environments or IDE options are supported. For this course, we will use [Android](../../Glossary/Sdk/Android.md) Studio for most of our development. We will also use [Xcode](../../Skills/Mobile%20Development/Xcode.md), which we'll cover in a later video. A lot of improvements have been coming to Android Studio recently related to Kotlin Multiplatform. As a result, you may want to consider using a beta or canary version of the IDE. For this course, I recommend using Android Studio Meerkat, Narwhal, or later. Starting with Meerkat, Android Studio has the capability to add shared module to your Android app projects for you. Beginning with Narwhal, the IDE also has the capability to show [compose](../../Glossary/Framework/Jetpack%20Compose.md) multi-platform previews. Let's take a look at an example project that I created. First of all, you're going to generally want to switch from the Android view to the project view. This is generally true with Kotlin Multiplatform projects in Android Studio. There are a couple of plugins you should consider which will improve your development experience. First, there is a compose [multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) project, which is included in most recent versions of Android Studio. This adds some useful functionality to make compose multiplatform easier to work with. So you can see this is [Jetpack Compose](../../Glossary/Framework/Jetpack%20Compose.md). It's not the same one.
>
> **[1:31](https://www.linkedin.com/learning/kotlin-multiplatform-development/android-studio-for-kmp?u=76281980&t=91)** We want to go to this one, Compose Multiplatform IDE support and this will come with, I believe, Narwhal and later. Also, you're going to want the Kotlin Multiplatform plugin, which I have right here, which will help as well. This should allow you to run your iOS app directly from Android Studio. If it doesn't show up automatically, you can still add iOS or desktop targets or any other by editing the run configurations. If you want to run your app directly from Android studio for iOS, desktop, or web assembly, you may find you have to add a new run configuration manually. Sometimes the iOS configuration appears automatically if you already have the Kotlin Multiplatform plugin installed. If you want to run the desktop app, you should follow these steps. First, go to the dropdown at the top of the IDE where you would select Android or iOS run configurations, you select Edit Configurations and then press the plus button. Choose application, name your run configuration, select a recent [Java](../../Skills/Software%20Development/Java.md) version. Here I'm going to choose the JetBrains Runtime that came with Android Studio. Select the desktop main module, and then we need to select a main class here. You tap this icon in the right corner, the document icon, and it searches and finds a main class. It will not find one if you haven't created it.
>
> **[3:06](https://www.linkedin.com/learning/kotlin-multiplatform-development/android-studio-for-kmp?u=76281980&t=186)** So usually select the first option, press OK, and run the desktop app. Okay, I had to update some [Gradle](../../Glossary/Tool/Gradle.md) versions, but now I should correctly run the desktop version. You'll notice I made changes to both the Gradle Libs versions 2 ML file, as well as the Gradle wrapper properties file in order to update the versions of the project. When I'm here and there's underlined text, you can put your mouse over, click, and then you just hit option enter and it'll update to the latest version, assuming that version works correctly. Okay, here we go. I'm just going to press run and it's going to run, and as you can see, it's correctly loading the version here. So it'll show to you that it's coming from the desktop app. Here, it's Java and the version of Java that is running. So you can see that I managed to get the desktop configuration to work. Web assembly or WASM JS in this case is more experimental. We won't dive too deeply into this course due to more limited multi-platform library support today, but you can still run it from Android Studio. Again, you go to the dropdown for run configurations, choose edit configuration, add a new one with Gradle as the type, here I'll put in web and this part, you have to type exactly the right thing.
>
> **[4:41](https://www.linkedin.com/learning/kotlin-multiplatform-development/android-studio-for-kmp?u=76281980&t=281)** I put down here WASM JS browser development run minus T. Exactly that. And now if I hit okay and I try running it, this should load my web browser and show a web assembly version of the project running. As you can see here, it's running and showing WASM JS. It's in my Safari browser, so everything's working. I can also show you how to create this iOS app configuration if it does not create it for you. Let me give this a go. Do I press minus? Say iOS application and we'll type in iOS here. You have to choose the project file in the folder and here it is. Then you have to wait for it, then choose the project scheme, and finally the configuration and the target. So you're picking which type of simulator you want it to run on, and it says before launch, build iOS application. All this stuff should be fine. So I'm going to hit okay and now I can run it for iOS. Now you're going to have to make sure you run X code before you do this, most likely because you have to accept the license terms or X code will not allow it to run the simulator. It's now installing the simulator and here it is starting up the simulator.
>
> **[6:15](https://www.linkedin.com/learning/kotlin-multiplatform-development/android-studio-for-kmp?u=76281980&t=375)** It's going to then immediately run the app in our new iPhone simulator. It's also possible to choose devices as targets, and as you can see, I click here. It's got Hello iOS. This is coming from the iOS specific code that's right here. So now that we know how to run code against each platform, we should look into the features that Empower Kotlin Multiplatform development in Android studio. First, we need to get our minds into the gutter. That's the IDEs gutter, that is. The gutter is the area just to the left of the main editor window. Let's look at platform KT in the common main code. You'll notice an A with a diamond surrounding it in the gutter. If you click on it, it allows you to travel quickly to the different actual fun. Get platform calls for each platform. If you tap on the E with the diamond surrounding in the gutter, then you'll return to the Expect fund, so it will cover actual and expect a little more in a later video. Let's say we want to delete one of the actual functions. You'll notice that the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) expect gets a red underline if we do that. If you tap on it and hit option return to do the IDE Quick fix, it'll give you the option to create the missing function just like that. The same thing implies to the interface. Here you can tap on the eye to get to the implementations
>
> **[7:54](https://www.linkedin.com/learning/kotlin-multiplatform-development/android-studio-for-kmp?u=76281980&t=474)** of the interface, and if you tap on the eye again, it will take you back. If you want to go backwards and forwards on a Mac, you can do command option left arrow and right arrow, just like that. You can also do cross platform refactoring, so let's try to rename the Expect Fund, get platform function right here. Simply right click the name of the function, choose refactor, and then rename. If you're using a track pad, you can also right click by tapping with two fingers. Now, if we type the new name of the function and you'll see that all the actual funds have also been renamed, as you can see, all of these are now the same. The same goes for interfaces across the platform boundary, except that you may need to check check boxes in the dialogue to ensure they're renamed properly, and then you can select all and then hit okay. The same goes for refactoring to change the signature of the Expect Fund function. You'll find that most refactorings simply work across these platform boundaries as long as you use a [Kotlin](../../Skills/Software%20Development/Kotlin.md) multi-platform aware IDE like Android Studio. So here I can change the signature and just add something new to it, and there I've added a string and you'll see each of the functions now has a string added to it.
>
> **[9:27](https://www.linkedin.com/learning/kotlin-multiplatform-development/android-studio-for-kmp?u=76281980&t=567)** So there you go. Android Studio is definitely aware and will empower your [Cross-platform Development](../../Skills/Mobile%20Development/Cross-platform%20Development.md). I recommend it for now until they get IntelliJ working with all of these new features. It's probably your best option.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (16), [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (6), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (4), [Gradle](../../Glossary/Tool/Gradle.md) (4), [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (3)
> **Code Identifiers:** ios (12), iphone (1)
> **Env Vars:** ide (7), wasm (3)
> **UI Navigation:** go to (3), dropdown (2), select the (2), click on (1)
> **CLI Commands:** find (3), make (2)
> **Tools:** safari (1), intellij (1)
> **Analogies:** just like (2)
> **Definitions:** is a  (1)

#### [Xcode for KMP](https://www.linkedin.com/learning/kotlin-multiplatform-development/xcode-for-kmp?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/xcode-for-kmp?u=76281980&t=0)** - [Instructor] If you're building an iOS app with [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md), you should know how to use [Xcode](../../Skills/Mobile%20Development/Xcode.md) to empower multi-platform and development with [Kotlin](../../Skills/Software%20Development/Kotlin.md). You might be asking yourself, "If I have spent all of this energy building an app using both native iOS languages like [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) and Objective-C, will I still be able to debug my app in Xcode?" The answer is yes. Touchlab produced an excellent library that makes it possible to debug your KMP apps using Xcode across the Kotlin, Swift, Objective-C boundaries. Installing the plugin is not difficult on a Mac. You first make sure you have the homebrew package manager installed, which I do, so it's actually BrewMan's version. And if it's not installed, go to brew.sh and run the one line command there to get started. Next run brew install xcode-kotlin. When that's done, simply run xcode-kotlin install.
>
> **[1:10](https://www.linkedin.com/learning/kotlin-multiplatform-development/xcode-for-kmp?u=76281980&t=70)** I'll show you, xcode-kotlin install. It's a yes, attempt to shut it down because you want it to restart, and now it's installation complete, it's now running with Xcode. At this point, you should be up and running. Whenever you upgrade Xcode, it's important to run xcode-kotlin with the sync command instead of install. Again, to make sure that you keep your tools up-to-date and working with the latest version. Now that you have the plugin installed, you can now add references to your Kotlin code to your iOS project inside of Xcode. I can open my project in Xcode. Be sure to set the option so it doesn't copy the files, instead references them in place. Also, don't add them to a target since that will attempt to package them redundantly. So I will go here, I would say Add Files to iOS app, and I can go to composeApp, src, commonMain. I think that's correct, src like that. Hit Add. Do not copy, reference them in place, and do not add them to targets. You do want to create groups. So now you can actually see all of your common Kotlin source inside of Xcode, and it will be able to handle it with syntax highlighting and everything working here,
>
> **[2:44](https://www.linkedin.com/learning/kotlin-multiplatform-development/xcode-for-kmp?u=76281980&t=164)** as you can see. All of these Kotlin files are now searchable from inside of Xcode. Now we can set break points in our Kotlin code and debug both Kotlin and Swift code from within Xcode. When it comes time to actually run your project from Xcode, it should be pretty simple. Chances are that if you have a simulator already installed and selected when you open your project, you can simply press the Run button. You might need to first select the correct target at the top, only if there's more than one. Right here. However, oftentimes the only setup required is to set a team for signing purposes under your target's signing capabilities tab, which you click here on the project, you then would go here, select the target, which is the app, and then click Signing Capabilities. And here you would add a team. And this is going to require signing into your Apple account and all of that. But right now it's actually installing and working. I recommend creating a team and getting everything working. Here it's launching and it's going to fire up this iPhone 16 Pro simulator right here. Which will go to the main screen and then install the app, and finally, I can click, and it's working. I'll see you in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Xcode](../../Skills/Mobile%20Development/Xcode.md) (15), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (11), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3), [Kmp](../../Glossary/Framework/Kotlin%20Multiplatform.md) (2)
> **Code Identifiers:** ios (4), composeapp (1), commonmain (1), iphone (1)
> **Prerequisites:** install (5), make sure you have (1), setup (1)
> **CLI Commands:** make (2), brew (2)
> **UI Navigation:** go to (3), select the (1)
> **Env Vars:** kmp (2)
> **File Paths:** brew.sh (1)
> **Cross-References:** in the next (1)

#### [Crafting a template with the Kotlin Multiplatform wizard](https://www.linkedin.com/learning/kotlin-multiplatform-development/crafting-a-template-with-kotlin-multiplatform-wizard?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/crafting-a-template-with-kotlin-multiplatform-wizard?u=76281980&t=0)** - [Instructor] To write our first code, we should start from a template. "Why not just get started writing?" you might ask? There is a lot of custom setup to select which platforms to support and the directory structure required is quite complex to create it all from memory. So, to avoid creating all of this boilerplate, first, let's go to the URL [kmp](../../Glossary/Framework/Kotlin%20Multiplatform.md).[jetbrains.com](https://jetbrains.com). After that, we can name our project with a title and a project ID. Normally, a project ID is in reverse DNS notation. So if our project is named hello, and our internet domain is [example.com](https://example.com), we would make the ID com.example.hello. In this case, it's com.callingtheshots.project. Next, let's select which platforms we would like to use. The template supports [Android](../../Glossary/Sdk/Android.md), iOS, Desktop, Web, and Server. Let's choose all except server for now. You'll notice that the iOS platform lets you decide whether or not to share the UI using [Compose](../../Glossary/Framework/Jetpack%20Compose.md) [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md). Let's leave that checked for now. Finally, let's press the DOWNLOAD button. We now have a functioning app, a very basic app that runs across all of these platforms and we can decompress it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kmp](../../Glossary/Framework/Kotlin%20Multiplatform.md) (1), [Android](../../Glossary/Sdk/Android.md) (1), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1), [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (1)
> **Env Vars:** url (1), dns (1), download (1)
> **Exercise Files:** template (2), boilerplate (1)
> **Code Identifiers:** ios (2)
> **URLs:** [jetbrains.com](https://jetbrains.com) (1), [example.com](https://example.com) (1)
> **CLI Commands:** make (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)


### 2. Combining Native with Multiplatform

[↑ Back to Table of Contents](#table-of-contents)

#### [Building our first multiplatform app](https://www.linkedin.com/learning/kotlin-multiplatform-development/building-our-first-multiplatform-app?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/building-our-first-multiplatform-app?u=76281980&t=0)** - [Narrator] In this video, we'll take a hands-on approach to building your first [multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) app, guiding you through the process of using the custom template that you just downloaded. By the end of this session, you'll have a working app running on multiple platforms and a clear understanding of how to make impactful changes to its functionality. First, let's open the template that you just downloaded. You'll need to uncompress the zip file into a directory. In most cases, you can double-click the file in the file manager of your operating system, and this will start the process. Next, we're going to open the project from within [Android](../../Glossary/Sdk/Android.md) Studio. If needed, we may be required to allow permission to let it build the project. Then we want it to [Gradle](../../Glossary/Tool/Gradle.md) sync and choose the project view instead of Android, which is what we always use for [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) development. First, I'm going to go into the Gradle directory and update some versions. This should be 8.11.1. It's the current today, and we'll update these to the current latest, 8.9.1. If you hit Option + Enter, you can change to the latest version of each and then Gradle sync and wait for it. If you press the elephant button up here, that will sync your project with the Gradle files. It will also, if you've changed the Gradle files at all
>
> **[1:35](https://www.linkedin.com/learning/kotlin-multiplatform-development/building-our-first-multiplatform-app?u=76281980&t=95)** for a sync now option with the button at the top, and this is fine at the bottom there. Now we are going to add any missing run configurations. I'll go to edit configurations, add, application, we call it Desktop, select the [Java](../../Skills/Software%20Development/Java.md) version, desktopMain, the main class, and hit OK. And then we'll add the web assembly version, which is Gradle. We call this Web, and this has to be exactly this, wasmJSBrowserDevelopment, which is the branch we're running, then Run -t, just like that, and hit OK. Now one by one, we can run each of these targets. You try to run them all at once, you may run into some problems, so we're going to wait until the emulator starts to start the next one. As you can see, I've got Android running here. Close that now. Here's JVM. Says Hello, Java. This is running in Mac OS right now. I am going to then run web. Here's web running, and now I can actually run iOS last. And there's iOS. Okay, I'm now running all four projects side by side.
>
> **[3:08](https://www.linkedin.com/learning/kotlin-multiplatform-development/building-our-first-multiplatform-app?u=76281980&t=188)** Now you may notice I ran into some problems earlier on trying to build and run all four of them at the same time. It does seem that there is a issue in Android Studio where it doesn't seem to want to execute all four at once. You may need to try potentially stopping a task to start another and see if that helps. But right now I have all four running and I can now demonstrate how you would add some changes to your project. So after going through the steps for each platform, we should have our project running on Mac OS, an Android emulator, an iOS simulator, and a web browser. If your device is not running Mac OS, you can instead run only on Android, web, and your desktop operating system. Let's make a small change to our project and observe that the results have changed in each. As you can see, I now have all four running at once. I have made a small change and changed the text to read Hello [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning! in the shared code, and it's reflected in all four. You've now seen how to set up, run, and customize your first Kotlin Multiplatform app using the custom template. By following our steps, you successfully built a very simple project for multiple platforms. Additionally, you made a small change and observed its impact across all of the platforms, reinforcing the power of this shared code. With this setup complete, you're ready to dive deeper into Kotlin Multiplatform development and we're going to explore some more advanced features.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (6), [Gradle](../../Glossary/Tool/Gradle.md) (6), [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (3), [Java](../../Skills/Software%20Development/Java.md) (2), [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (1)
> **Code Identifiers:** ios (3), desktopmain (1), wasmjsbrowserdevelopment (1)
> **UI Navigation:** open the (2), double-click (1), go to (1), select the (1)
> **Exercise Files:** template (3), zip file (1)
> **Prerequisites:** you'll need (1), required to (1), set up (1), setup (1)
> **CLI Commands:** make (2)
> **Versions:** 8.11.1 (1), 8.9.1 (1)
> **Definitions:** we call this (1), is a  (1)

#### [Whether to use Compose Multiplatform](https://www.linkedin.com/learning/kotlin-multiplatform-development/whether-to-use-compose-multiplatform?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/whether-to-use-compose-multiplatform?u=76281980&t=0)** - [Instructor] Let's look at what [Compose](../../Glossary/Framework/Jetpack%20Compose.md) [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) offers, its benefits, and its limitations, and how to decide if it's the right choice for your project. Compose Multiplatform has its origins with the user interface framework [Jetpack Compose](../../Glossary/Framework/Jetpack%20Compose.md). Jetpack Compose was written by [Google](../../Glossary/Service/Google.md) for [Android](../../Glossary/Sdk/Android.md) developers. It shared some of the same main developers who originally wrote [React](../../Skills/Web%20Development/React.js.md). I feel Compose has a wonderful developer experience for writing user interfaces in a declarative manner. It's fast, and it's easy to work with. Compose Multiplatform is a user interface library written by JetBrains, based upon Jetpack Compose for Android. They figured out a neat trick to treat every platform as if it was an Android screen, simply by porting the Android graphics library Skia to each of these platforms. They call it Skiko for Skia [Kotlin](../../Skills/Software%20Development/Kotlin.md). While it's obvious that writing all of your user interface code once may save you a lot of time, there are reasons why this may not be a good idea for you. Using the same UI in all apps may mean that your Android app looks identical to your iOS app, and your desktop and web apps. This may seem favorable to some, but it also means that the controls might not look like the native controls for that platform.
>
> **[1:34](https://www.linkedin.com/learning/kotlin-multiplatform-development/whether-to-use-compose-multiplatform?u=76281980&t=94)** This might cause some confusion, as icons and appearances are not the same as users would normally expect. You might find that some animations behave differently or require custom tweaking. You can get around some issues with the special [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md) libraries, but a Compose Multiplatform app still may not appear the same on iOS, for example, as if you had written it using SwiftUI. Also, if you work for a larger company, you might have existing development teams and investments into the native user interface libraries. In this case, it might make sense to reuse existing user interface code and to only replace the code sections with shared Kotlin code when it makes sense. A lot of larger companies share business logic between Android and iOS apps with [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md), while still using Jetpack Compose on Android and SwiftUI on iOS. Here's some examples. Compose Multiplatform can offer a huge savings in cost, time, and administrative overhead. However, you must consider if a seamless native [user experience](../../Skills/Web%20Development/User%20Experience%20(UX).md) may be more important to your organization than those savings. Kotlin Multiplatform has the ability to empower both hybrid Compose and per-platform native user experiences, You can mix and match to your heart's content.
>
> **[3:09](https://www.linkedin.com/learning/kotlin-multiplatform-development/whether-to-use-compose-multiplatform?u=76281980&t=189)** Using your new skills in KMP, the choice is ultimately yours.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (7), [Android](../../Glossary/Sdk/Android.md) (7), [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (5), [Jetpack Compose](../../Glossary/Framework/Jetpack%20Compose.md) (4), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (2)
> **Code Identifiers:** ios (4)
> **CLI Commands:** find (1), make (1)
> **Env Vars:** kmp (2)
> **Definitions:** is a  (1), means that (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Expect and actual](https://www.linkedin.com/learning/kotlin-multiplatform-development/expect-and-actual?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/expect-and-actual?u=76281980&t=0)** - [Instructor] So far we've used a template to modify existing code to work on multiple platforms. However, we haven't really explained how [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) works. Under the hood, there are two special [Kotlin](../../Skills/Software%20Development/Kotlin.md) language modifiers, which were added to the core Kotlin language back when [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md) was added to Kotlin in 2017. These are expect and actual. When we write expect in Kotlin, we're telling the compiler that we want to create an interface. However, unlike interfaces in most languages, a Kotlin expect interface is expected to have an actual implementation for each platform. When we write actual in Kotlin, we're telling the compiler that this is the actual implementation of the interface we told the compiler to expect earlier. The compiler can then look at the project directory structure to determine which platform this actual implementation serves. Now, as I said, expect and actual both create and satisfy an interface. However, they're also more powerful than interfaces or protocols in most languages. In fact, there are at least seven different language constructs that can be used with expect and actual in Kotlin. You can use expect and actual with functions, properties, objects, type aliases, enums,
>
> **[1:36](https://www.linkedin.com/learning/kotlin-multiplatform-development/expect-and-actual?u=76281980&t=96)** annotations, and now even classes, which is a new beta feature in the language. Expect and actual classes should become stable soon, but may change slightly before they're considered final. This is what an experimental feature means in the Kotlin world. Doesn't mean that they're not usable yet, just means it might change a little bit. You don't need to know how to use all of these different types of expect and actual to begin using Kotlin Multiplatform. In fact, you can do most things with only expect and actual functions and properties. All of the others are just there simply to make Kotlin Multiplatform more convenient. Not only can you use expect and actual with functions and properties, but you can make the return types different for each actual statement. You can return an interface that has a different implementation for each platform. The main point I want to get across is that expect and actual are very powerful keywords in Kotlin. You can use them to provide platform specific implementations of a great number of things. These built-in language features are a big reason why Kotlin is possibly the most powerful multi-platform language today.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kotlin](../../Skills/Software%20Development/Kotlin.md) (10), [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (3), [Kmp](../../Glossary/Framework/Kotlin%20Multiplatform.md) (1)
> **CLI Commands:** make (2)
> **Env Vars:** kmp (1)
> **Exercise Files:** template (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [An actual example](https://www.linkedin.com/learning/kotlin-multiplatform-development/an-actual-example?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/an-actual-example?u=76281980&t=0)** - [Instructor] In the last segment, I gave you an overview of the expect and actual keywords in [Kotlin](../../Skills/Software%20Development/Kotlin.md), but now it's time for a brief demonstration. I will show you an example of using different implementations for each platform for Kotlin property and function. First open your template project again in the IDE. You'll need to wait for it to fully sync before every feature works as expected. For starters, let's try a property. Let's open the template project and go to the shared folder. Typically, this code sits under commonMain slash kotlin for your app or library module. You need to use the Project view, not the [Android](../../Glossary/Sdk/Android.md). So here we have my shared code for my project. You'll notice the existing code here already has some expect and actual functions. Let's delete the existing Greeting.kt code and start over. Delete all but the package statement. Here we're going to add expect val_world: String and expect fun platformFunction. As you type these lines, your IDE will likely underline the names in red. Don't worry about this yet. This is just here to remind you that you haven't yet written the actual implementations of these functions. If you click the red underlines and hit Option + Enter,
>
> **[1:33](https://www.linkedin.com/learning/kotlin-multiplatform-development/an-actual-example?u=76281980&t=93)** there's an option to add the missing actual declarations, and you can then select each one that you want to support and press OK. And I can then do the same thing here with the other one. Add missing, Desktop, Android, iOS, and WebAssembly. Now you select all the platforms you want the implementations for on that quick fix screen. You can choose iOS main instead of the individual check boxes for each of the different iOS targets, for example, this works because [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md) uses a hierarchical approach, so you're not forced to write multiple implementations when it does not make any sense. After you click through to this screen, the IDE will create files for each platform and will leave the implementation as TODO, Not yet implemented. TODO is a built-in Kotlin function that will crash your app with that TODO message if you try to run it without writing the necessary code. This crash is deliberate to remind you to finish your code before checking it in and sending it off to production or to a client. You can also find all TODOs in your code using the TODO tool window in most JetBrains IDEs. So that would go Tool [Windows](../../Glossary/Service/Windows.md) and then look for TODO, and it found four TODOs, as you can see. Let's define the actual val greeting to four different values depending upon the platform.
>
> **[3:08](https://www.linkedin.com/learning/kotlin-multiplatform-development/an-actual-example?u=76281980&t=188)** Let's go to Android here for world, we're going to say, "Let's do the robot, Android!" We're going to go to desktop, going to say, "Let's click around, desktop!" And iOS will do, "Let's take a bite of the apple, iOS!" Finally, go to WebAssembly, we're going to say, "Let's rock the web, WASM!" We're going to have to change some code here. So if I go to shared code, we need to change this greeting here from Greeting.great to the new property called world. You'll see it turn purple. If I run this on any of the platforms, it should work. I run for Android here. If I click here, it says, [Compose](../../Glossary/Framework/Jetpack%20Compose.md): Let's do the robot, Android! So, and likewise, if I pick any other one of these, let's do web. We should see the web version. "Let's rock the web ,WASM!" There we go. Compose: Let's rock the web, WASM!
>
> **[4:40](https://www.linkedin.com/learning/kotlin-multiplatform-development/an-actual-example?u=76281980&t=280)** So there we go. We've implemented an expect and actual val returning a string. So as you can see, expect val is used for setting a value such that the result could be different per platform and calculated differently. What if we simply wanted to run a function that works differently for each platform? You could use a side effect of expect val, but that will be unexpected and awkward. So instead, we're going to use expect and actual fun in this case. You should expect fun in the next video, (laughs) pun intended, we're going to start calling platform-specific APIs directly from Kotlin. We'll make our app move around in unique ways for each individual operating system.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (6), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (5), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (2), [Kmp](../../Glossary/Framework/Kotlin%20Multiplatform.md) (1), [Windows](../../Glossary/Service/Windows.md) (1)
> **Env Vars:** todo (5), ide (3), wasm (3), kmp (1)
> **Code Identifiers:** ios (5), commonmain (1), val_world (1), platformfunction (1)
> **UI Navigation:** go to (5), open the (1)
> **CLI Commands:** make (2), find (1)
> **Cross-References:** in the last (1), in the next (1)
> **Exercise Files:** template (2)
> **File Paths:** greeting.kt (1)

#### [Accessing operating system methods](https://www.linkedin.com/learning/kotlin-multiplatform-development/accessing-operating-system-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/accessing-operating-system-methods?u=76281980&t=0)** - [Instructor] In this video, we're going to call methods or functions that are specific for each operating system, but we're going to make that call from shared code. You see here, I have created a platform function with expect fun, and I gave it the composable annotation. This makes it a [Compose](../../Glossary/Framework/Jetpack%20Compose.md) [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) function, so then we can actually make this call from Compose code and also take advantage of some of the Compose methods. So we're going to use this expect fun to create a function interface that we satisfy with a different [Kotlin](../../Skills/Software%20Development/Kotlin.md) function for each platform, so let's take a look at [Android](../../Glossary/Sdk/Android.md) here. Starting with Android, we create a sequence of toasts, so toast is a system message that briefly appears over the top of our app, covering everything underneath it. In this case, we define our platform func to show two different messages with the Toast.makeText method, which only Android supports, passing in the Android context that we get from the composable, so LocalContext.current is the way, in [Jetpack Compose](../../Glossary/Framework/Jetpack%20Compose.md), that you can get Android's context. Here, we're passing into a launched effect, which allows you to use coroutines to run normal code inside of Compose rather than view code, and so here, we're in a coroutine, looping forever while true, meaning we continue this loop until the end of time unless this is no longer visible, which we'll show, and Toast.makeText is going to spit these two messages out
>
> **[1:37](https://www.linkedin.com/learning/kotlin-multiplatform-development/accessing-operating-system-methods?u=76281980&t=97)** with a two second delay between each of them, so this is going to show the two messages on repeat for Android. So let's take a look at iOS. For iOS, we changed the background color of our app's root window controller, as you can see here. Take the key window. We're using the UI application method, which is built into iOS, into the operating system, and shared application with key window, rootViewController.view, and from that, we can set the background color back and forth every two seconds. This code will cycle back and forth between a cyan color and a yellow color background. Next, we go to desktop. For desktop, we're going to use Compose Multiplatform method that only works on desktop. We're going to create a dialog window on the screen where the message in dialog for the title cycles between two different messages. Again, we're using the launched effect with while true. Every two seconds, we change it between the two marquee slogans. Finally, we go to WebAssembly. Again, WebAssembly, we're going to use a platform access to the browser's [JavaScript](../../Skills/Software%20Development/JavaScript.md) to the document.title that allow us to change the title of the webpage. I'm going to show this in Chrome, because if I show it in Safari, it doesn't show you the title, so we're going to go back and forth every two seconds
>
> **[3:10](https://www.linkedin.com/learning/kotlin-multiplatform-development/accessing-operating-system-methods?u=76281980&t=190)** between the two messages. As you can see, using [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md), you can make calls to specific messages for each operating system on each platform while keeping the main majority of your codebase still shared and still reusable, and I'm going to demonstrate that quickly here. So let's take a look at Android. Here we go. You see here that this is the messages going back and forth every two seconds. If I close it so the view is no longer visible, these will stop. There we go. Okay, so that's Android. iOS. See, the colors are going to change in the background here, back and forth, and then we're going to check out web. I'm going to open this instead in Chrome, and you'll notice the title changing right there. Every two seconds, good, and finally, we're going to test desktop, and the title of the window's changing there. So we've just demonstrated how we can run the platform functions of the operating system directly from actual functions and then calling it from shared code. This approach not only simplifies [Cross-platform Development](../../Skills/Mobile%20Development/Cross-platform%20Development.md), but it also ensures that platform-specific features integrate cleanly into your app. Kotlin Multiplatform enables you to write maintainable code while leveraging the unique capabilities of each platform.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (8), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (5), [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (2), [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (2), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (1)
> **Code Identifiers:** ios (4), maketext (2), rootviewcontroller (1)
> **CLI Commands:** make (3)
> **UI Navigation:** go to (2)
> **Tools:** safari (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Multiplatform project structure](https://www.linkedin.com/learning/kotlin-multiplatform-development/multiplatform-project-structure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/multiplatform-project-structure?u=76281980&t=0)** - [Instructor] Now that we've looked at a bit of [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) code, we should familiarize ourselves with the project structure that makes it all possible. Let's take a look at our template and identify each location where the different code goes for each platform and for shared code segments. The template keeps all of its [Kotlin](../../Skills/Software%20Development/Kotlin.md) code within a single module named composeApp. All of its Kotlin code lives under composeApp/source in different folders with special names. Share code always lives under a folder named Common Main. Under this folder, there's a folder for composed resources and for Kotlin code. Resources folders are used to store images, translatable strings, plurals fonts, raw files, that sort of thing. The Kotlin folder contains the actual shared code with the keywords and code references to software libraries that provide shared code implementations in Kotlin Multiplatform. In addition to the shared code folder, common Main, we'll have another folder for each platform or closely related group of platforms. For example, iOS here is a group of platforms. It includes ARM 64 for most actual iOS devices, and ARM 64 simulator, or x64 simulator for simulator
>
> **[1:36](https://www.linkedin.com/learning/kotlin-multiplatform-development/multiplatform-project-structure?u=76281980&t=96)** is on Intel devices. Because iOS is actually a group of platforms, it sometimes makes sense to share code across all of iOS, and sometimes it makes sense to make different code for each architecture. You might want to use the more specific architectures if you're calling code that only works in that architecture. Likewise, we can share code for all JVM platforms that use methods specific to the JVM, or we can create code under [Android](../../Glossary/Sdk/Android.md) and desktop that are more specific than the JVM. Each time we create both expect and actual implementations, we can decide for ourselves as a programmer which level is most appropriate to create our actual code implementation at. Otherwise, each specific platform has its own code in directory name platform main. For example, desktop is Desktop Main. Android is Android Main. iOS is iOS Main for all iOS platforms, device, and simulator on all architectures. But you could get more specific like iOS simulator, ARM 64 main or less specific like Apple Main, which also supports Watch OS and Vision OS. Web has wasmjsmain, which supports writing both web assembly as well as [JavaScript](../../Skills/Software%20Development/JavaScript.md) code from Kotlin. You might wonder how Kotlin knows which platforms you're supporting.
>
> **[3:09](https://www.linkedin.com/learning/kotlin-multiplatform-development/multiplatform-project-structure?u=76281980&t=189)** All of these settings are defined within the standard build system called [Gradle](../../Glossary/Tool/Gradle.md). You can also try Amper instead of Gradle, but that build system is still very new and less tested with complex configurations. In Gradle, you would look in the main module directory for a file called Build Gradle.kts. Within the Kotlin block, right here, you'll see multiple targets defined. Ours has wasmjs, Android Target, Desktop, which in this case is a JVM target and iOS target. We also define a desktop target by naming our separate JVM Target Desktop, as you can see here. So you've seen that you can place your code in different folders for each module to support different platforms. This doesn't really demonstrate how useful Kotlin Multiplatform is though. What really makes [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md) exciting is the plethora of useful libraries that provide both the expect and the actual code for us to use. By using these libraries, we can access the network, store data in [Databases](../../Skills/Software%20Development/Databases.md), key platform settings and key value stores, manage dates and times, handle [Dependency Injection](../../Skills/Web%20Development/Dependency%20Injection.md) across platforms, and provide resources for all platforms. In the coming videos, I will demonstrate some of these libraries that provide all of the code for each platform for you,
>
> **[4:44](https://www.linkedin.com/learning/kotlin-multiplatform-development/multiplatform-project-structure?u=76281980&t=284)** and I will show you how they will make your work much easier.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kotlin](../../Skills/Software%20Development/Kotlin.md) (7), [Android](../../Glossary/Sdk/Android.md) (4), [Gradle](../../Glossary/Tool/Gradle.md) (4), [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (3), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Code Identifiers:** ios (9), composeapp (2)
> **Env Vars:** jvm (5), arm (3), kmp (1)
> **CLI Commands:** make (2)
> **Exercise Files:** template (2)
> **Definitions:** is a  (2)
> **Analogies:** for example (2)
> **Speakers:** - [instructor] (1)


### 3. KMP and Swift

[↑ Back to Table of Contents](#table-of-contents)

#### [Using Kotlin from Swift code](https://www.linkedin.com/learning/kotlin-multiplatform-development/using-kotlin-from-swift-code?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/using-kotlin-from-swift-code?u=76281980&t=0)** - [Instructor] The [Kotlin](../../Skills/Software%20Development/Kotlin.md) native code generator was designed to interoperate seamlessly with Objective C code, Apple's original language of choice. However, most developers who write code today for Apple devices use the [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) programming language, which offers more modern features than Objective C. This video demonstrates how you can call Kotlin functions from your Swift code, and how you can handle Kotlin types from return variables. Kotlin has top level functions and properties that can be called from anywhere. These get mapped to Objective C and to Swift by using the Kotlin file name. For example, a function, called Expect Fund platform function, and a file called Greeting KT would be called, greetingkt.platform function in Swift. You can simply call the function in Swift by passing any appropriate arguments. So let's try creating a new platform function here that does not depend on [compose](../../Glossary/Framework/Jetpack%20Compose.md) multi-platform or [Jetpack Compose](../../Glossary/Framework/Jetpack%20Compose.md). So now we have to implement it for all of our platforms. It's already in web assembly. And here for iOS, I'm going to implement it. I'll put it on the IO dispatcher so that it can run without blocking the main thread. Copy the same code here,
>
> **[1:43](https://www.linkedin.com/learning/kotlin-multiplatform-development/using-kotlin-from-swift-code?u=76281980&t=103)** and then here, we'll launch this to the main dispatcher
>
> **[1:51](https://www.linkedin.com/learning/kotlin-multiplatform-development/using-kotlin-from-swift-code?u=76281980&t=111)** so that we can use the main thread, which is required to make UI updates on [Android](../../Glossary/Sdk/Android.md) or iOS.
>
> **[2:06](https://www.linkedin.com/learning/kotlin-multiplatform-development/using-kotlin-from-swift-code?u=76281980&t=126)** There we go. So now we have this new call platform func, and we're going to go over to [Xcode](../../Skills/Mobile%20Development/Xcode.md) and add it to our Swift code. First. We should probably remove this from our shared code. I've done this right here. I made it commented out, and then I go over to Xcode. So just to show you here, if I start typing, greeting.kt, it auto completes it, and if I put platform func, should find that as well. Looks like it hasn't built yet, so I might need to temporarily hold off and build. I'd say, greeting_ioskt.platformfunc. And now it found it. Now you're going to see that when it builds and runs, the simulator is going to show the background changing between teal and yellow again. If I remove this, run it again, it won't do that. So you've just demonstrated how to directly call our code from the Swift code, for our Kotlin code. You'll notice that we can make calls directly from Swift to Kotlin. While this is impressive and looks easy, the hard part is usually designing the types of return values, such that it's easy to map from Kotlin types to Swift types. If you want to learn how to do anything specific in Swift from Kotlin code,
>
> **[3:40](https://www.linkedin.com/learning/kotlin-multiplatform-development/using-kotlin-from-swift-code?u=76281980&t=220)** I recommend looking up the language construct you're trying to use within the Kotlin Swift interopedia. It can answer any questions about what is supported out of the box and how best to use it. For example, Kotlin seal classes and Swift's enum classes are often compared and they work quite similarly. However, Kotlin seal classes aren't neatly translated into Swift's enums simply because Kotlin code is interpreted as Objective C, which has no similarly advanced concept. However, there are a couple easier ways to use Kotlin from Swift. First, a consulting company, called Touch Lab, created a third party library called Skie. This library inspired an official Kotlin, multi-platform support library, called Swift Export. These solutions provide easier translations between Kotlin and Swift, such as Seal classes and co-routines. We'll dig deeper into this, in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kotlin](../../Skills/Software%20Development/Kotlin.md) (16), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (16), [Xcode](../../Skills/Mobile%20Development/Xcode.md) (2), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1), [Jetpack Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1)
> **CLI Commands:** make (2), find (1)
> **Code Identifiers:** ios (2), greeting_ioskt (1)
> **Analogies:** for example (2), such as (1)
> **File Paths:** greeting.kt (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Making it easier with Swift export](https://www.linkedin.com/learning/kotlin-multiplatform-development/making-it-easier-with-swift-export?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/making-it-easier-with-swift-export?u=76281980&t=0)** - [Instructor] In our previous video, we called our [Kotlin](../../Skills/Software%20Development/Kotlin.md) code directly from [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md). However, there are some small issues that may become annoying to Swift developers having to use our Kotlin code. We discuss Kotlin Sealed classes as one example of a type that will be difficult to work with in traditional [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) code. In this video, we'll discuss the solutions to make [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md) code much easier to integrate for Swift developers. After KMP came out and developers discovered some minor difficulties with certain aspects of the integration between Kotlin and Swift, a KMP consulting company called Touchlab realized that there could be a commercial opportunity in making it all easier. In fact, this is the same company that wrote much of the documentation for KMP. They wrote a product called Touchlab SKIE, S-K-I-E, which used compiler plugins to rewrite the code interface to be much easier to use along with Swift code. This would help developers use Sealed classes, as Swift enums and Kotlin coroutine code from either combine or async/await. After a while, they decided to open source this product, making a once costly library available for free. And today, you can use it for your own code. However, even though Touchlab SKIE can solve these problems for you, and it's a very viable solution, the core Kotlin team used the SKIE library as inspiration to create an official library. Even though it's still young and could change in the coming months,
>
> **[1:32](https://www.linkedin.com/learning/kotlin-multiplatform-development/making-it-easier-with-swift-export?u=76281980&t=92)** I recommend learning to work with the new official Swift Export Library for KMP. While we could work with our existing project template, it seems Swift Export and [Compose](../../Glossary/Framework/Jetpack%20Compose.md) [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) aren't compatible just yet. So, let's open a special Swift Export sample code instead, and I'll explain it. You'll need to use Kotlin version 2.1.0 or newer as I have here in the [Gradle](../../Glossary/Tool/Gradle.md)/libs.versions.toml file. You'll also need to set experimental setting in the gradle.properties file, kotlin.experimental.swift-export.enabled=true. This setting will eventually become default, but we're adding it here today to say that we want to use the new feature. even though some details may change. Experimental language features in Kotlin don't mean that they're broken or that they're going to blow up. We're signing onto the idea that any app that we write today may require code changes one day if we upgrade the version of Kotlin, and if the feature is implemented differently in the final version of that language feature. After doing that, we're going to need to add a swiftExport block to the project/shared/build.gradle.kts file for the shared code. Here we have the swiftExport Block. Just be sure to use the quick fix when you create it to import ExperimentalSwiftExportDsl OptIn or none of this will work when we try to build. Also, make sure that the module name
>
> **[3:06](https://www.linkedin.com/learning/kotlin-multiplatform-development/making-it-easier-with-swift-export?u=76281980&t=186)** matches its old name, Shared in this case, and the flattenPackage structure should mimic your project structure. Here it's com.[GitHub](../../Skills/Software%20Development/GitHub.md).jetbrrains.swiftexport. At this point, you should gradle sync. You can press the Elephant or the Sync Now button if that appears, and it should reimport the Gradle script and pick up any changes you've made. Finally, you may want to open the Project X code and verify that the correct build phase is getting used. This is so that Swift Export gets called instead of exporting our shared code framework to Objective-C. Now let's look at the actual Kotlin code usages and X code. If we open ContentView.Swift and command click on useClassFromA here, we'll dive into the shared code framework. I have to wait for it to build first. There it is, useClassFromA. As you can see here, the exported framework is written in Swift instead of Objective-C as it usually would. From here, we can add any custom code we want, including using Sealed classes that convert to enums from Kotlin to Swift. Also, I'm confident that a solution will be found to allow using Composed Multiplatform code eventually within Swift Export modules. Until that time, we can still use Swift Export as of today to provide business logic to our application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (16), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (12), [Kmp](../../Glossary/Framework/Kotlin%20Multiplatform.md) (5), [Gradle](../../Glossary/Tool/Gradle.md) (5), [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (2)
> **Env Vars:** kmp (5), skie (3)
> **Code Identifiers:** swiftexport (2), useclassfroma (2), flattenpackage (1)
> **File Paths:** gradle/libs.versions.toml (1), kotlin.experimental.swift (1), project/shared/build.gradle (1), contentview.swift (1)
> **CLI Commands:** make (2)
> **Versions:** version 2 (1), 1.0 (1)
> **UI Navigation:** open the (1), click on (1)
> **Exercise Files:** template (1), sample code (1)

#### [KMP together with SwiftUI](https://www.linkedin.com/learning/kotlin-multiplatform-development/kmp-together-with-swiftui?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/kmp-together-with-swiftui?u=76281980&t=0)** - [Instructor] In the last video, we learned one new way to make the integration easier between [Kotlin](../../Skills/Software%20Development/Kotlin.md) and [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md). In this new video, we'll use Kotlin together with Swift UI. Swift UI is Apple's recommended user interface framework. It's also declarative like [Compose](../../Glossary/Framework/Jetpack%20Compose.md) and has a lot of the same nice usability features. Why would you want to work with Swift UI even when Compose multi-platform is available? Maybe your company already has a lot of highly paid Swift UI developers for Apple [products](../../Skills/Software%20Development/Microsoft%20Products.md), and they want to produce the cleanest user interface integration that they can, even if it costs more or has to be redone for an [Android](../../Glossary/Sdk/Android.md) Compose app. Maybe you want to use existing Swift UI views that were custom designed for the problem that you're tackling. Whatever the case is, there's plenty of support for using Kotlin together with Swift UI. Here's a webpage that JetBrains provides that tells you how to use the two of them together. Explains in good detail. It's a little bit painful, but it can be done. You can use Swift UI inside of Compose multi-platform. However, Touch Lab has actually provided their own support library to make it easier to integrate Swift UI from within Compose. It's called Compose Swift Bridge, and you can check out the sample here. I had to make some little tweaks to it. So you should check out the actual sample code we're providing to you in order to make sure that the [Java](../../Skills/Software%20Development/Java.md) tool chain is set up correctly so that you can run it. This is a very big project. It may take a long time to build,
>
> **[1:32](https://www.linkedin.com/learning/kotlin-multiplatform-development/kmp-together-with-swiftui?u=76281980&t=92)** but it very nicely demonstrates how easy it can be to include Swift UI views from within your Compose multi-platform code. Here we can take a look at this map view right here, and you can see there's this ExpectSwiftView annotation. This is put there for use by the Compose Swift Bridge Library, which then goes through as a plugin and will replace this stuff with a version that will work to call Swift UI from inside of Compose very easily. They actually have several different versions. They get ut to work with Skie, S-K-I-E, as well as other methods of integrating your views. Here I have the project running with everything in Compose on Android, and over here we have both a UI kit as well as a Swift UI view here for the map view. And this is getting integrated, this native map view, into this shared code here. So since I have the same function, new title, I can paste in actual coordinates of where [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) is. I can go ahead and do the same thing over here with the Android code as well. And you'll notice everything works just fine even though I'm using both Compose as well as Swift UI at the same time. So there you go. The title isn't showing up, but that's because it's a different type of view, and it's not showing titles at this moment. So this is the [Google](../../Glossary/Service/Google.md) Maps from Android, and this is a different map view from Swift UI that's highly custom for iOS. That's Apple Maps, this is Google Maps.
>
> **[3:07](https://www.linkedin.com/learning/kotlin-multiplatform-development/kmp-together-with-swiftui?u=76281980&t=187)** It's all just working natively in exactly the way you'd want it to work.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (16), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (11), [Android](../../Glossary/Sdk/Android.md) (4), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (3), [Google](../../Glossary/Service/Google.md) (2)
> **CLI Commands:** make (4)
> **Definitions:** is a  (2)
> **Code Identifiers:** ios (1)
> **Cross-References:** in the last (1)
> **Exercise Files:** sample code (1)
> **Best Practices:** recommended (1)
> **Prerequisites:** set up (1)


### 4. Architecture

[↑ Back to Table of Contents](#table-of-contents)

#### [Recommended architecture with MVVM](https://www.linkedin.com/learning/kotlin-multiplatform-development/recommended-architecture-with-mvvm?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/recommended-architecture-with-mvvm?u=76281980&t=0)** - [Presenter] Why is [Software Architecture](../../Skills/Web%20Development/Software%20Architecture.md) important? I often joke, "Never argue with an app developer. We're always changing our views." Architecture is important because the user interfaces in our app are constantly changing, and it's vital to keep them separated from other layers for a variety of reasons. Ever since [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) introduced the Model-View-ViewModel or MVVM architecture in 2005, it has consistently been one of the most popular app architectures. [Google](../../Glossary/Service/Google.md) recommends it for [Android](../../Glossary/Sdk/Android.md) apps. Most iOS apps also implement it. We should talk about architecture and why it matters, but I don't intend to go over all of the options in this course. MVVM makes special sense for [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) development in part because Google publishes opinionated [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md) library implementations of this architecture for both Android and iOS apps, and even desktop and others, but it is not the only option. For this section of the course, a key question you might have is why is software architecture important? A common answer everyone uses is it makes testing your code easier. By separating the view from the view model and the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of your data, it's easier to verify that each step is producing the correct output that you would expect. However, there is another reason specific to [multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) development. Structuring your application
>
> **[1:32](https://www.linkedin.com/learning/kotlin-multiplatform-development/recommended-architecture-with-mvvm?u=76281980&t=92)** will make it easier to verify your code's behavior after you decide which layers should be shared and which should be customized for each platform. You want to be able to verify that all platforms produce the correct answer at each layer regardless of the underlying library code. Most modern software architectures exist primarily to separate the view or the user interfaces from the logic code. This is important because views can change very often whenever the design changes, but the underlying logic generally does not change very much, and the logic result should be tested. MVVM creates a simple binding from the model data to the VM model to the view that works very well with modern declarative user interface libraries like [Compose](../../Glossary/Framework/Jetpack%20Compose.md) and SwiftUI. Many useful support libraries have been written to make MVVM easier to implement in Kotlin Multiplatform apps. It is also important to note that using a view model makes it easier to change decisions later about how to implement your user interface. Until a Compose Multiplatform came out, most Kotlin Multiplatform apps only use shared code up to the view model layer. By having a clear separation between layers, it makes it easier to later change your decisions about which code is shared at which layer and which code is instead unique and customized to each platform.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (3), [Software Architecture](../../Skills/Web%20Development/Software%20Architecture.md) (2), [Google](../../Glossary/Service/Google.md) (2), [Android](../../Glossary/Sdk/Android.md) (2), [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (2)
> **Env Vars:** mvvm (4), kmp (1)
> **CLI Commands:** make (2)
> **Code Identifiers:** ios (2)
> **Speakers:** - [presenter] (1)

#### [Google ViewModel](https://www.linkedin.com/learning/kotlin-multiplatform-development/google-s-viewmodel?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/google-s-viewmodel?u=76281980&t=0)** - [Instructor] To support [compose](../../Glossary/Framework/Jetpack%20Compose.md) multi-platform, [Google](../../Glossary/Service/Google.md) offers the common view model library as part of its Jetpack series of libraries. In this video, we'll use Google's common view model to share code across platforms. First, we're going to go into our project, which I have here, and we have to paste in the [Gradle](../../Glossary/Tool/Gradle.md) dependency for lifecycle view model compose into the dependency section for our app. As you can see here, I already have it added to my libs versions two ml file. This is the version number, and here, I use the version number further down right here, and referencing it here. That's the one. You have to correctly spell all of this. We're going to put a link in so that you can see the documentation for how to add this. It's right here. You're basically doing the same thing here, except we're just using the Gradle version catalogs to define it, right here. So we've added the lifecycle view model compose, as well as androidx-lifecycle-runtime-compose, and androidx-lifecycle-viewmodel-[Android](../../Glossary/Sdk/Android.md), okay? And then we need to add these to our common build, Gradle KTS file. So we come down here, you can see, Android has nothing special added, but we've added all these lifecycle, lifecycle view model, lifecycle runtime compose, lifecycle view model compose, all of them to the common main shared dependencies. Didn't have to touch desktop, iOS,
>
> **[1:34](https://www.linkedin.com/learning/kotlin-multiplatform-development/google-s-viewmodel?u=76281980&t=94)** or Android, just the common section. And that takes care of that. Now, we should start to create a common view model for a composed multi-platform project. So if I go in here, I did create it already, but we can talk about it, if you're doing this for your own projects, don't call it main view model, probably. I mean, it's up to you. I recommend choosing a descriptive file name appropriate to whatever screen or user interface level you're writing for. We start by declaring a new class extending from view model, which is defined as androidx-lifecycle-viewmodel, even though it's a common shared code version of the Android view model. To use best practices, we create a UI state. This is a data class typically, and we would expose the UI state with a public version of the private variable. You're using the as state flow function. So we have a mutable state flow and a immutable state flow here. This creates a stream of updates which, each change we make can be easily observed by our composed code. Because we used a mutable state flow that we kept as private, it can only be changed by methods inside of our view model. This is important, but we can observe a public version instead that is not modifiable. This is helpful for encapsulation to prevent external code from becoming dependent
>
> **[3:09](https://www.linkedin.com/learning/kotlin-multiplatform-development/google-s-viewmodel?u=76281980&t=189)** upon the inner workings of the view model never changing. Because let's be honest, our change code is constantly changing, we're always changing our views. Now let's modify our app KT code here to depend upon our new common view model. You'll see we have a view model passed into our app, and this is being created by a default value. This is a default variable. So this function here is a builder that creates a view model, and we have to do this special version of the view model function because in Android, we can use injection and have it handle it automatically. But when we're doing this in any other platform, we don't have all of the components that we need so that this block here has to define our view model. You could possibly inject it into this block, but this here has to be where it gets defined for a common view model. After we've added it here, we can then make use of it. Here, we're using the UI state collective state. Remember, UI state, we created as the public, non-modifiable UI state for our view model. And the collective state function is very important because it converts our data class here into a state's data class. So now, state is one of the core ideas behind compose, state allows us to automatically observe changes as they occur and update our UI in real time.
>
> **[4:44](https://www.linkedin.com/learning/kotlin-multiplatform-development/google-s-viewmodel?u=76281980&t=284)** Okay? Finally, we want to replace the code, updating the show content variable here. So we have this view model toggle showing content function that I defined, and this will update the UI state. The UI state update function here that we're calling on our state flow allows us to atomically update the value of our mutable state flow. And here, we're using the copy function. This allows us to change one variable without touching any other part. We don't have to define everything all over again. We can just say copy it, change this one parameter and nothing else. And so this will autonomically make the update in a way that won't break when we're working on multiple thread. And then this will present the greeting if I go back here. When we tap on the button, it will then change the value of the show content variable here to then make this visible. We're not even using this anymore, so I can get rid of that. So we present our greeting using state value show content, and then it uses state value greeting to reflect the actual greeting. So state flow must be unwrapped with the value call, as you see here, to obtain its current value. Now we can run our app and see that the functionality has not changed, but now we're providing it with a view model that leads to better architecture and better testability. There it is.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (7), [Android](../../Glossary/Sdk/Android.md) (5), [Gradle](../../Glossary/Tool/Gradle.md) (3), [Google](../../Glossary/Service/Google.md) (2)
> **CLI Commands:** make (4)
> **Definitions:** is a  (3), defined as (1)
> **Code Identifiers:** ios (1)
> **Env Vars:** kts (1)
> **Documentation:** the documentation (1)
> **UI Navigation:** toggle (1)
> **Speakers:** - [instructor] (1)


### 5. Sharing Network

[↑ Back to Table of Contents](#table-of-contents)

#### [Adding Ktor to your project](https://www.linkedin.com/learning/kotlin-multiplatform-development/adding-ktor-to-your-project?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/adding-ktor-to-your-project?u=76281980&t=0)** - [Instructor] These days, there's hardly a single app that doesn't need to talk to a network. Even apps that used to only work offline nowadays use the network to check for updates or to send crash reports or analytics data. As a result, you'll very likely need a solution for talking to clients or servers over the internet. Fortunately, the company that originally made [Kotlin](../../Skills/Software%20Development/Kotlin.md) also builds a multi-platform networking library that works for you as both a client and a server. It's called [Ktor](../../Skills/Software%20Development/Ktor.md), K-T-O-R, and it supports the HTTP [REST](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) protocol that is most popular on the internet. We're going to make a good number of changes to our example project in this section. These changes will make it possible to handle network requests and responses from Kotlin multi-platform code using the Ktor library. Let's start from the example project for the section. I'll describe each of the changes that I made in order. First, we're going to make changes to our [Gradle](../../Glossary/Tool/Gradle.md) configuration for the app. Again, Gradle is the primary build system Kotlin uses for most apps. You need to tell Gradle about which library dependencies to include in your app. You start by updating the version catalog first, which I have open here, and then you can add the libraries from the version catalog to the build.gradle.kts file. In order to use Ktor to make REST requests and to parse the results
>
> **[1:31](https://www.linkedin.com/learning/kotlin-multiplatform-development/adding-ktor-to-your-project?u=76281980&t=91)** with the Kotlinx serialization library, we're going to need to add five libraries. You can see I added the version here for Ktor, and I have five libraries down below here. These five libraries are: ktor-client-core, ktor-client-okhttp, which is a very popular JVM HTTP library, ktor-client-darwin, which Darwin is a Apple reference, ktor-client-content-negotiation, and then, finally, ktor-serialization-[JSON](../../Skills/Web%20Development/JSON.md). We need all of these. And then we add as well a new plugin for Kotlinx serialization. The plugin will help serialize our types for us to and from JSON, in this case. We'll also need to provide this build plugin. We need to provide the version catalog with the details it needs to access these libraries. We do so by providing a full module version or name here, as well as a version reference to Ktor. You can see they're sharing the same version here. If there were any reason why you would want a different version of any one of these, you would need to make a new version reference or just cut this off and then put the version in here. Either way. Because they're all the same version number, I'm referencing Ktor five times here. The easy way to find these module names and version numbers is to search for these libraries on [GitHub](../../Skills/Software%20Development/GitHub.md) and to copy the module name from either the README file on the front page of the library or from its docs.
>
> **[3:09](https://www.linkedin.com/learning/kotlin-multiplatform-development/adding-ktor-to-your-project?u=76281980&t=189)** So we've already entered the names and version numbers here for the five libraries in one plugin. So let's focus on the changes now for the build.gradle.kts file. Again, this is under composeApp. It's specific to our app. Here we're looking at the build.gradle.kts file, and we see that the plugins, the very last one here, we've added our Kotlinx serialization plugin. This has to be at the top of the file. You cannot put plugins later down. It's dependent on the order, and so this matters. These are also dependent on order, but, generally speaking, they seem to not mind being in the wrong order unless it's something that's very, very integral, like Kotlin, for example. Usually, I just add new third-party libraries to the end of the plugin block. As we come down here, under androidMain, we've added the one OkHttp library, which provides our [Android](../../Glossary/Sdk/Android.md) implementation. Down here under commonMain, for our share code, I added three libraries: ktor.client.core, ktor.client.content.negotiation, and ktor.serialization.json, okay? Finally, for iOS, we add the Darwin Library. And for desktopMain because we're using JVM for desktop, I'm using the OkHttp library that works for both Android and for JVM generally. There we go. So next we want to make sure that we have the necessary permissions to reach the internet.
>
> **[4:41](https://www.linkedin.com/learning/kotlin-multiplatform-development/adding-ktor-to-your-project?u=76281980&t=281)** This is important, especially for Android, because an Android app will not function without the necessary permission. Here you can see I added the uses-permission: android:name="android.permission.INTERNET." If you don't put that there, nothing will work, and that is just normal on Android. At this point, we're ready to set up our network models.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Ktor](../../Skills/Software%20Development/Ktor.md) (14), [Android](../../Glossary/Sdk/Android.md) (7), [Gradle](../../Glossary/Tool/Gradle.md) (6), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (4), [JSON](../../Skills/Web%20Development/JSON.md) (3)
> **Env Vars:** jvm (3), http (2), rest (2), json (1), readme (1)
> **CLI Commands:** make (6), find (1)
> **Code Identifiers:** composeapp (1), androidmain (1), commonmain (1), ios (1), desktopmain (1)
> **File Paths:** build.gradle (3), ktor.serialization.json (1)
> **Definitions:** is a  (2)
> **Documentation:** the readme (1)
> **Tools:** github (1)

#### [Modeling with Kotlinx-serialization](https://www.linkedin.com/learning/kotlin-multiplatform-development/modeling-with-kotlinx-serialization?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/modeling-with-kotlinx-serialization?u=76281980&t=0)** - [Presenter] So after setting up our network client, we now need to define the models. To do so, we need to make them serializable. You'll remember earlier that we set up a build plugin for kotlinx serialization, as well as ctor libraries for [JSON](../../Skills/Web%20Development/JSON.md) serialization and deserialization. I think I'll show you here again. This is it, as well as at the top here. It needs both a plugin and the dependency. So what is [Kotlin](../../Skills/Software%20Development/Kotlin.md) Externalization? This is an add-on library for Kotlin provided by JetBrains, for the purpose of converting data between formats through the process of serialization and deserialization. Here, we're going to send Kotlin data classes to our networking library, ctor, which our serialization library will then convert into json to send over the wire. When we receive a response back over the internet, come back as json. We'll want that JSON deserialized back into our Kotlin data classes. Kotlinx serialization generates most of the code for us to do this across [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md) platforms. I use the Hacker News API to provide a simple example, since it does not require us to provide any authentication. However, since their json API is polymorphic, we have to use a sealed interface to define our models. I'll pull that up here very fast. Sorry, models here.
>
> **[1:33](https://www.linkedin.com/learning/kotlin-multiplatform-development/modeling-with-kotlinx-serialization?u=76281980&t=93)** We use the Serializable annotations as well as the serial name, and we have to have this null response to handle situations where, for whatever reason, what comes back is not of type story. As you see here, we have this customized serializer, which I wouldn't need otherwise, except that the API has the capability to return multiple types of objects in a polymorphic way as a generic data type. So we had to do some complicated stuff here. For our code example, we're only concerned with the type of story. We had to set a serial name for a type to match it by name specifically. Normally, this would be fine without it, but because of the case difference I had to do this. We also created a special json content polymorphic serializer, which then handles the different types that are allowed for an item response from the Hacker News API. We then have to mark every one of these types here, once, twice, three times, as serializable in order for the Kotlinx serialization build plugin to generate the necessary code to handle serializing them. You'll notice our data class for story has all the json attributes here, which are returned in the top stories HTTP response. If we set the isLenient setting on our json for ctor library, right here, then we don't need to include every single one of them.
>
> **[3:07](https://www.linkedin.com/learning/kotlin-multiplatform-development/modeling-with-kotlinx-serialization?u=76281980&t=187)** We can get away with deserializing to a much simpler data class that does not need to handle every attribute returned by the API. This can be very useful if your API is adding new attributes regularly, which might otherwise crash your app when you're not handling them. You can be inconsistent as long as you have this set here. Unless they change the names, for example. It can also be helpful to reduce the memory requirements here for storing data that we don't use. Like, for example, if we don't need descendants or kids or any of this to display our UI, we could just remove it, and this would all be none the wiser as long as we can pass both ways with their story objects. You've learned how to serialize kotlin data classes now, to send them over the internet as json, and to handle the json response as a data class. This library, Kotlinx serialization, also handles other formats besides json, which you can read about on the project webpage.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (11), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (5), [Kmp](../../Glossary/Framework/Kotlin%20Multiplatform.md) (1)
> **Env Vars:** api (6), json (1), kmp (1), http (1)
> **Analogies:** for example (2)
> **CLI Commands:** make (1)
> **Code Identifiers:** islenient (1)
> **Definitions:** is an  (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [presenter] (1)

#### [Making network requests](https://www.linkedin.com/learning/kotlin-multiplatform-development/making-network-requests?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/making-network-requests?u=76281980&t=0)** - [Instructor] Now that we know a bit about serialization, we want to set up our API. We need to define an HTTP client. Then we want to create the functions which use that client to send requests and handle the responses. So let's take a look at this news API. Now, typically we'll want to create our client using [Dependency Injection](../../Skills/Web%20Development/Dependency%20Injection.md), but I wanted to keep this example very simple and easy to read. At the top of the news API, you'll notice that we created a client variable. We gave it the capability to handle [JSON](../../Skills/Web%20Development/JSON.md) serialization by installing the content negotiation module with the JSON block here. We are pretty printing and is lenient. These are totally optional, but again, as lenient means you don't have to have every single attribute for it to work necessarily unless it's there required. And pretty print means that it's going to add all the extra white space to make it easier to read. After creating our models, we can write methods to send HTTP requests and to handle responses. Here I wrote a getTopStories method. This is going to perform http get request. As you see here, get, client.get. It's referencing this client and we're calling client.get with a URL string. It performs an HTTP get request against the Hacker News API to get a list of the top stories by their numeric IDs. You see we return list of long
>
> **[1:33](https://www.linkedin.com/learning/kotlin-multiplatform-development/making-network-requests?u=76281980&t=93)** and grabbing from the body from this URL. Then we take the first 10 IDs returns, storiesIDs.take10. This is a [Kotlin](../../Skills/Software%20Development/Kotlin.md) stream API here. Only take the ones that are not null 'cause we don't want ones that are not stories, if there were any. And finally, return the details of each of the 10 stories as a list, which we get from this URL. So we're actually making 11 requests here and getting the bodies and returning them as a list of story. Next, let's take a look at our view model. We made a couple of updates. First, we of added a news API, which is something we normally, again, provide via dependency injection. Second, we've added a suspend function, which can run on a background thread or in case like a Kotlin co-routine on a background thread. The suspend function then calls the news API and updates our UI state with the result. Again, we're using this update method to atomically update the contents of our UI state. And here we're calling this news API. You see here in the gutter, we have this call that tells you it's a suspend function call, which means that this is going to suspend execution and it's going to come back later. Keep waiting until it gets past it. But meanwhile, other code can still run. This is a function of Kotlin co-routines to have suspend functions like this. So this will wait here again, jumping in and out of the function, allowing things
>
> **[3:06](https://www.linkedin.com/learning/kotlin-multiplatform-development/making-network-requests?u=76281980&t=186)** to run while it gets the top stories from the internet. Prints the results of just the titles, and then update our UI state and showing this content and adding the top stories to the top stories list in the UI state. Now we should take a look at App.kt and our changes to our composed multi-platform UI. You'll notice we've added a co-routine scope here, which allows us to run the code in the background. So then we come down here. When you press the button, it launches a co-routine from this default scope and runs our view model code in the background. So we'll use the default dispatcher. I would actually normally recommend using the IO dispatcher for anything that's internet-based, like network request, but you can get away with this a lot of the time. Finally, we update the animated visibility section down here to show a lazy column containing the titles of the Top 10 Hacker News stories, as you see here, instead of what we previously showed, which is just a static image and some text from our shared code. So I'm going to run it here on desktop, and I click that, it spits out the current top 10 Hacker News stories. Likewise, I should be able to do this on other platforms. Here's an [Android](../../Glossary/Sdk/Android.md) and iOS. Now in practice, you're going to want to provide some kind
>
> **[4:42](https://www.linkedin.com/learning/kotlin-multiplatform-development/making-network-requests?u=76281980&t=282)** of error handling for this, possibly retry. This is just a very quick, dirty example. And as you can see, it's now working on all of the platforms, we had Android, iOS, and desktop. What you'll notice though from this code is that we did not have to define any platform specific code other than providing a different client library for each one of the platforms in our [Gradle](../../Glossary/Tool/Gradle.md) configuration. It's pretty cool, huh?

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kotlin](../../Skills/Software%20Development/Kotlin.md) (3), [Dependency Injection](../../Skills/Web%20Development/Dependency%20Injection.md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (2), [Android](../../Glossary/Sdk/Android.md) (2), [Gradle](../../Glossary/Tool/Gradle.md) (1)
> **Env Vars:** api (8), http (3), url (3), json (2)
> **Code Identifiers:** ios (2), gettopstories (1), storiesids (1)
> **Definitions:** is a  (2), means that (1)
> **File Paths:** app.kt (1)
> **CLI Commands:** make (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Apollo GraphQL](https://www.linkedin.com/learning/kotlin-multiplatform-development/apollo-graphql?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/apollo-graphql?u=76281980&t=0)** - [Instructor] While most of the internet is based upon [REST](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md), another protocol has also grown. [GraphQL](../../Skills/Web%20Development/GraphQL.md) was popularized by [Facebook](../../Glossary/Service/Facebook.md) for managing complex APIs, which would normally be split between numerous [Microservices](../../Skills/Software%20Development/Microservices.md). The solution makes it much easier as a client to interact with only one endpoint using customized requests rather than making many requests to multiple microservice hosts for the same thing. In [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md), the Apollo GraphQL Library has excellent support and it's easy to use. We're going to integrate the Apollo GraphQL [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md) client into our app for accessing the [GitHub](../../Skills/Software%20Development/GitHub.md) API. First, we're going to add the library build plugin dependency to our [Gradle](../../Glossary/Tool/Gradle.md) version catalog. We open libs.versions.toml. This is in our Gradle folder. We then add a versions entry for Apollo, a libraries reference for the runtime, and finally, a plugins entry for the build plugin. Next we jump over to our app's build.gradle.kts file, which is under composeApp, and we add a plugin under the plugins block at the top of the file, the runtime under a commonMain.dependencies, and finally, an Apollo block. Here we set the service name, set a package name. This affects the naming of the generated files.
>
> **[1:36](https://www.linkedin.com/learning/kotlin-multiplatform-development/apollo-graphql?u=76281980&t=96)** This affects the package that classes are generated in. And finally, we set the endpoint for our Apollo service, as well as the schema file, where it's located. At this point, we need to download the schema for our graph. We need to go to this URL to download the public schema for GitHub. The schema defines what queries and mutations are available for us to perform requests against. We can use it to assemble our custom requests and mutations. So now we have to copy the schema file we've downloaded to src, commonMain/graphql. And it's very important we have to rename it, so the file extension is graphqls. This is the new schema extension. After you've done this and you've put it in place and you rebuild your app, then Apollo will generate code under commonMain that will allow you to access the schema from both shared and platform-specific code. At this point, we should start writing our queries. You can put your queries and mutations in the same directory as a schema file, or you can put in a subdirectory. I recommend using a subdirectory. This helps you find your queries later as well as your mutations by the feature that they're associated with. The only difference between a query mutation is that a query returns data from the API without changing any, whereas a mutation is meant to change the data remotely.
>
> **[3:09](https://www.linkedin.com/learning/kotlin-multiplatform-development/apollo-graphql?u=76281980&t=189)** It's like the difference between a GET request and a POST or a PUT request when you're using REST. And this is what a query looks like. If you haven't used GraphQL, you can look all this stuff up online and learn how to create your own. But I've created one here to search the top repos, grab the first 10, sort them by the number of stars they have. So this is going to give me the 10 most popular repositories on GitHub by a number of stars that are also public. Before you're able to test and use your queries, though, you will need to authenticate to the GitHub GraphQL API. This is going to require two steps. First, you need to create your personal access token for the GitHub API. Then you need to create an authentication interceptor, which I did here for Apollo, and then apply it. This is a temporary key, it'll go away. To create the token, simply go to this URL and make sure that you grant it the appropriate public repo permission. The HDP interceptor has an example in our sample code under MainViewModel. Here we're adding both an authorization interceptor and a logging intercept as well. Two different interceptors in the list, and we're creating our Apollo client here and pressing the server URL here for it. Normally you would inject this via [Dependency Injection](../../Skills/Web%20Development/Dependency%20Injection.md). So now we can write this function here to show our top repos. You'll notice it's a suspend function, which is important in [Kotlin](../../Skills/Software%20Development/Kotlin.md)
>
> **[4:41](https://www.linkedin.com/learning/kotlin-multiplatform-development/apollo-graphql?u=76281980&t=281)** because the suspending function can be run on a Kotlin coroutine on another thread. This allows us to run a background request while our app does not have its UI frozen, which would be very bad, especially in say, a mobile app, for example. In a true MVVM app, you would want to go one level deeper than this and actually call this function from a repository class from the view model, rather than just calling directly from the view model. So also, we should set up our Apollo client, which we do here, and we set the server URL and we add the interceptor. And then finally, we call the query, which we execute, and we grab this data object here, dataAssertNoErrors.search. That will return the list of the top repos. And then finally, we update the _uiState. Again, this is an atomic operation. It's going to change the _uiState with the copy function where we can update just two properties without changing anything else. And we can now show the app running as expected on each platform. You can see, I just click that and wait. And here, this is the top 10 GitHub repos on GitHub sorted by stars. Here's it working on [Android](../../Glossary/Sdk/Android.md). And finally here we have it running on iOS. So we've learned how to communicate easily across the internet using both REST and GraphQL models
>
> **[6:16](https://www.linkedin.com/learning/kotlin-multiplatform-development/apollo-graphql?u=76281980&t=376)** by writing only shared code. I hope you can quickly realize the power this provides to you as a programmer.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [GraphQL](../../Skills/Web%20Development/GraphQL.md) (7), [GitHub](../../Skills/Software%20Development/GitHub.md) (7), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (3), [Gradle](../../Glossary/Tool/Gradle.md) (3), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (2)
> **Env Vars:** api (4), url (4), rest (3), kmp (1), post (1)
> **Tools:** github (7)
> **Code Identifiers:** commonmain (3), composeapp (1), dataassertnoerrors (1), ios (1)
> **API Endpoints:** get  (1), post  (1), put  (1)
> **Exercise Files:** download the (2), sample code (1)
> **File Paths:** libs.versions.toml (1), build.gradle (1)
> **CLI Commands:** find (1), make (1)


### 6. Sharing Database

[↑ Back to Table of Contents](#table-of-contents)

#### [Adding SQLDelight to your project](https://www.linkedin.com/learning/kotlin-multiplatform-development/adding-sqldelight-to-your-project?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/adding-sqldelight-to-your-project?u=76281980&t=0)** - [Narrator] Now that we know how to write shared network code, the next logical step is to figure out how to store that information. We'd like a single database that works across all platforms, which makes it simple to store and search for information. We can use this as a network cache, as storage for data created by users, or in any way we'd like. For [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md), the most popular and flexible option is SQLDelight, a [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md) library written by the same engineers who built the Cash App. Let's start by adding SQLDelight, to our existing project. First, we need to add the plugin and database drivers for each platform. We do this in the [Gradle](../../Glossary/Tool/Gradle.md) libs.versions.toml file. Each of these database drivers provides everything you need to both query and write data to an SQLite database on each supported platform using KMP code. So, let's look at the libs.versions.toml file. We'll add one plugin and three database drivers: [Android](../../Glossary/Sdk/Android.md), native for iOS, and JVM for desktop. Here we put the version in. We add the Android driver, the coroutine extensions for working on background threads, or that sort of thing, a native driver and JVM driver. Finally, we add a plugin at the bottom. Then we go over to our composeApp, buildgradle.kts file again.
>
> **[1:34](https://www.linkedin.com/learning/kotlin-multiplatform-development/adding-sqldelight-to-your-project?u=76281980&t=94)** We add the plugin at the top. We add the Android driver under androidMain.dependencies. We add the coroutine extensions under commonMain.dependencies. We're actually adding [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) datetime and Apollo datetime. It's useful because we needed it for the [GraphQL](../../Skills/Web%20Development/GraphQL.md), but we also need date time to handle the date and time types in our database. For iosMain, we add the native driver. And then in desktopMain, we're adding the JVM driver. All of that right here. Okay? And then we come down here, we create a SQLDelight block with [Databases](../../Skills/Software%20Development/Databases.md). I'm creating one called cashDB, and I'm sending a package name here. So, next we want to create the queries that we're going to need to use. So, here we have both TopRepos for [GitHub](../../Skills/Software%20Development/GitHub.md), and we have TopStorys for Hacker News. So, the TopRepos, we're going to create a table here. This is all [SQL](../../Skills/Data%20Science/SQL.md) code. If you're not familiar, you can look online and learn how to write SQL. And then we have created a selectALL function, an upsert function, upsertFullRepo, and a delete function. SQLight does actually have a native upsert function in newer versions, but because not every platform supports the newest versions out of the box, we're going to simplify here by using INSERT OR REPLACE instead.
>
> **[3:09](https://www.linkedin.com/learning/kotlin-multiplatform-development/adding-sqldelight-to-your-project?u=76281980&t=189)** Likewise, we're doing the same thing with TopStory. So we have a selectAll, which will select star from TopStory, and order them. And then two upsert functions, and a delete function. I'm demonstrating here with the two different upsert functions, that you can either allow the user to pass in every value like this, or you can have functions where these are pre-specified, or you can have one where it simply takes all of the values allowed for that database entry, and it's a single question mark that fills all of them in for you. Very simple. So, all of this is just SQL code, and it's going to be used to generate source code that we can use from Kotlin Multiplatform. So, then we can go over to create the actual functions. If I go into my common code here, here I have a expect fun createDriver function, and then a createDatabase function. So, my createDatabase function is what I would call from common code. It's using the createDriver function then to return the CacheDB database. At that point then we have to, of course, define the actual functions as you see here. For desktop, we use the JdbcSqlightDriver that we've imported right here. Then we have iOS. We have a native SQLight driver. We just named it test.db for the database file name. And finally, on Android,
>
> **[4:41](https://www.linkedin.com/learning/kotlin-multiplatform-development/adding-sqldelight-to-your-project?u=76281980&t=281)** we have to do something a little special because Android requires context. Context is a very powerful object in Android. You do not want to leak this object because it uses a lot of memory. Here we are referencing our app object. I had to create a special app object for our project to provide the application context, as you can see here. This is all special for Android. If you're not familiar with context, it's something you always just need. I prefer not doing this, and instead using [Dependency Injection](../../Skills/Web%20Development/Dependency%20Injection.md) here if you can. But here we've created our AndroidSqlightDriver. This will allow Android to access the database. Now that we've added all the necessary dependencies as well as methods to set up the database drivers, we will need to define any queries that we want to provide. SQLDelight provides some basic type safety that makes it easier to write these, and I'll cover that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (8), [SQL](../../Skills/Data%20Science/SQL.md) (3), [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (2), [Kmp](../../Glossary/Framework/Kotlin%20Multiplatform.md) (2), [Gradle](../../Glossary/Tool/Gradle.md) (1)
> **Code Identifiers:** ios (2), selectall (2), createdriver (2), createdatabase (2), composeapp (1)
> **Env Vars:** jvm (3), sql (3), kmp (2), insert (1), replace (1)
> **File Paths:** libs.versions.toml (2)
> **Cross-References:** in the next (1)
> **Tools:** github (1)
> **Exercise Files:** source code (1)
> **Definitions:** is a  (1)

#### [Setting up your database and adding objects](https://www.linkedin.com/learning/kotlin-multiplatform-development/setting-up-your-database-and-adding-objects?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/setting-up-your-database-and-adding-objects?u=76281980&t=0)** - [Instructor] So now that we have [SQL](../../Skills/Data%20Science/SQL.md) Delight set up in our code base, we need to make sure that the queries are defined for our database. We took a brief look at these in the last video. I just wanted to go into a little more detail. So we had to create these two .SQ files within the SQL Delight folder in common made. Each file defines the SQL commands to create the database Table and Fields. Plus it defines any queries like select or SQL methods to update the database, like inserts, updates, replaces, or if supported upserts. Take a look at the files I created here. When we create the table, we set the field types and their constraints. The first field is almost always the ID Field, ID Fields are special in [Databases](../../Skills/Software%20Development/Databases.md). Nearly everything gets an ID because it's the only way to link or join data between objects reliably. In general, IDs are never allowed to be null, and they almost always have the auto increment keyword. That means you can pass null to create an object and it'll automatically set it to the next ID. After that, we set the names and types for each field in that table. String types are text, lungs are integer, doubles are real. Binary types are blob. If you want special support for float into short types. You can also add special type adapter libraries for SQL Delight when you'd prefer not to do manual type conversions.
>
> **[1:34](https://www.linkedin.com/learning/kotlin-multiplatform-development/setting-up-your-database-and-adding-objects?u=76281980&t=94)** All these types can also have the special constraint, not null. Not null, of course, means that there must always be a value passed for this variable or parameter. It cannot be null. So this is useful in [Kotlin](../../Skills/Software%20Development/Kotlin.md) because then you don't have the question mark and have to deal with null safety and optionals. You can create custom queries against database by using the select keyword. You can add new items to the database with insert and you can update existing items. With update, you can then delete from an item from the database with delete, and this is taking an ID so that you can choose which one to delete. You can use upsert to either insert or replace an item if it already exists. If you're having trouble filling out the SQL statements for your models, I recommend looking up examples on the internet. SQL can get quite complicated, especially for many to many relationships or complex joins between objects. Once you've got the SQL right here in these two files, SQL Delight will generate the code that takes to make type safe calls to your database. SQL Delight also supports transactions, which is a way of performing multiple SQL operations where if anyone fails, then all of it rolls back. In the next video, we're going to take a look at using the generated queries from this code, from these .SQ files in your project.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [SQL](../../Skills/Data%20Science/SQL.md) (11), [Databases](../../Skills/Software%20Development/Databases.md) (1), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (1)
> **Env Vars:** sql (11)
> **CLI Commands:** make (2)
> **Cross-References:** in the last (1), in the next (1)
> **Definitions:** means that (1), is a  (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Creating queries](https://www.linkedin.com/learning/kotlin-multiplatform-development/creating-queries?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/creating-queries?u=76281980&t=0)** - [Instructor] Earlier we added database models for our top story and top repo types. These SQ light files provide [SQL](../../Skills/Data%20Science/SQL.md) delight with all the generated type safe code for queries that we'll be able to use in our project. Now we're going to use that generated code. The first thing that you should do is either rebuild your project or run the generate SQL delight interface [Gradle](../../Glossary/Tool/Gradle.md) task. This is going to generate the code to make queries against our new database models. And after we've run it, now that we have the generated code, we can add code to our view model to add the data to our database and to query it. So normally in a complex app, it's not unusual to have a repository class that will handle the complex caching strategy between your network and your database, and also, which isolates that complexity from your view model. You will also need a reference to your database class in the view model. One would normally inject that with [Dependency Injection](../../Skills/Web%20Development/Dependency%20Injection.md), but we haven't covered that yet. So for here now we're just going to add it directly here. Let's add two methods for each of our models, top stories and top repos. For each, we're going to want a query method to return each item in the database and an insert method, which will attempt to upsert the new data from the network. Beware that upserts referred to an SQ light data method that may be newer, may not be available in the default database on each platform.
>
> **[1:34](https://www.linkedin.com/learning/kotlin-multiplatform-development/creating-queries?u=76281980&t=94)** As a result, we're using insert update instead, which is a little bit less efficient, but does the job. For each of these methods, this inserts top stories and database, insert fake story and top repos from database. We're going to access the new classes available from our database class to directly call generated [Kotlin](../../Skills/Software%20Development/Kotlin.md) methods. These classes and methods are named based upon the file names and the query names, like topRepoQueries.selectall, topStoryQueries.upsert. Here we have top story queries, upsert and topStoryQueries.selectAll. These are all based upon the methods we created here. So to upsert the new stories from the network into the database here, we call database.topStoryQueries.upsert for each story and to query the top stories from each database, we need database.topStories.selectAll. We've learned how to insert values into our database and how to make queries against it. In the next video, we'll learn how you can use Kotlin Coroutine flows to actually watch the database values in real time and [react](../../Skills/Web%20Development/React.js.md) to them as they update in our database.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [SQL](../../Skills/Data%20Science/SQL.md) (2), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (2), [Gradle](../../Glossary/Tool/Gradle.md) (1), [Dependency Injection](../../Skills/Web%20Development/Dependency%20Injection.md) (1), [React](../../Skills/Web%20Development/React.js.md) (1)
> **Code Identifiers:** topstoryqueries (3), selectall (2), toprepoqueries (1), topstories (1)
> **CLI Commands:** make (2)
> **Env Vars:** sql (2)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Going with the flow](https://www.linkedin.com/learning/kotlin-multiplatform-development/going-with-the-flow?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/going-with-the-flow?u=76281980&t=0)** - [Instructor] If you've used reactive coding methods, you'll find [Kotlin](../../Skills/Software%20Development/Kotlin.md) coroutine flows immediately familiar. Much like observables and subjects in Rx flows offer a means of watching a value as a stream of values that changes over time while we observe it. In the case of database programming flows allow us a mean to watch values in our database and to [react](../../Skills/Web%20Development/React.js.md) immediately in the user interface whenever any value changes. [SQL](../../Skills/Data%20Science/SQL.md) Delight offers the possibility to query our database in a manner that observes the stream of updated values as a Kotlin flow. To do this, we first need a new library. We added it already here, but that is SQL Delight coroutines, extensions. So we go into this Libs versions 2 ML file in the [Gradle](../../Glossary/Tool/Gradle.md) directory and add this right? If we haven't already, we can then add it to the apps, build Gradle KTS file again under [compose](../../Glossary/Framework/Jetpack%20Compose.md) app build Gradle KTS, and here we go into Common Maine and we add Libs SQL light, coroutines extensions. At this point, we can simply Gradle sync to make sure we have the library there that's either hit sync now or press the elephant. And then we use the new function as flow, which allows us to convert our one-time database results instead into a flow of results changing over time.
>
> **[1:35](https://www.linkedin.com/learning/kotlin-multiplatform-development/going-with-the-flow?u=76281980&t=95)** Instead of one list of top stories, we get a series of lists of top stories, each one representing the new values in our database. Now we can add changes directly to our database and then watch as they're immediately applied and added to the user interface. So we're going to add a new text field and a button to insert a fake story into our Hacker News top stories with a very high score so that it appears at the top. Then we'll add an order bias score descending to the top story thing here. When we do our select, the order by score descending and that will make certain that our new fake story appears on top. And then when we add our fake story, this will prove that we can write to our user interface and instantly track the changes and even animate them as they appear in our view. So I'm going to do that right now. So here I have click here to get some top stories from the internet, and here I can add a fake story
>
> **[2:51](https://www.linkedin.com/learning/kotlin-multiplatform-development/going-with-the-flow?u=76281980&t=171)** and then it appeared in real time, as you can see because this down here is watching the database in real time as it changes. So as you can see, it's not too difficult to add a database to your multi-platform app and you hardly need to write any platform specific code to use it. You only have to learn a little bit of SQL and Kotlin and suddenly you can use a database anywhere. Here it is in [Android](../../Glossary/Sdk/Android.md). There we go. Note that if you're building for iOS, you might have to make one special change. I had to go in here, open the project, you click this line right here at the top, top row, and then target, build settings, I'll type in linker, go to linking general other linker flags and add in SQL light three with a plus sign, minus L-S-Q-L-I-T-E-E three. If you add that, then you're not going to get any weird linker errors, which you might get otherwise with SQL Delight. And now we can actually run our project with iOS. So I'm going to get some top HN stories and insert my own fake story. There we go. And as you can see, it added in real time, same way it works with Android and with desktop. So I can do it here on Android
>
> **[4:31](https://www.linkedin.com/learning/kotlin-multiplatform-development/going-with-the-flow?u=76281980&t=271)** and it added it right there, see? So make sure you remember to add your linker flag if you run into a bunch of linking errors.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [SQL](../../Skills/Data%20Science/SQL.md) (6), [Gradle](../../Glossary/Tool/Gradle.md) (4), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (3), [Android](../../Glossary/Sdk/Android.md) (3), [React](../../Skills/Web%20Development/React.js.md) (1)
> **Env Vars:** sql (6), kts (2)
> **CLI Commands:** make (4), find (1)
> **Code Identifiers:** ios (2)
> **UI Navigation:** open the (1), go to (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)


### 7. Quality of Life

[↑ Back to Table of Contents](#table-of-contents)

#### [Kermit for logging](https://www.linkedin.com/learning/kotlin-multiplatform-development/kermit-for-logging?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/kermit-for-logging?u=76281980&t=0)** - [Instructor] Those of us who maintain apps for a living know that logging is important. It's often how we discover what's broken or what happened behind the scenes to results in an outcome. However, we don't want all of our debugging logs dumped to the console in a production app, for a lot of reasons. Instead, if we intend to diagnose customer bugs and issues, we need them sent to a crash reporting solution in the cloud. Touch Lab's Kermit Library provides the solution for our apps. Named after the pun, Kermit the Log, Kermit can hide or strip debug logs in production and then send those logs instead to a variety of different crash reporting solutions from a [Kotlin](../../Skills/Software%20Development/Kotlin.md) multi-platform app. For crash reporting solutions, Kermit has support for [Google](../../Glossary/Service/Google.md)'s Crash Lytics, Bug Snag and Sentry. You simply add the correct library dependency to your project and you'll be able to integrate logging with these solutions. Let's start as usual by adding the libraries to the Libs version to ML file in the [Gradle](../../Glossary/Tool/Gradle.md) folder. So first, we need to get Kermit, the latest version right now. Then we need to add the library dependency for both Kermit and Kermit Crashlytics. Here we go. And that's it as far as the- Those versions to a ml file is concerned.
>
> **[1:34](https://www.linkedin.com/learning/kotlin-multiplatform-development/kermit-for-logging?u=76281980&t=94)** Now we have to go back as usual to [compose](../../Glossary/Framework/Jetpack%20Compose.md) apps, build Gradle KTS file. Here we have to add our Kermit dependencies. So you'll notice I have to add it here under [Android](../../Glossary/Sdk/Android.md) main, under iOS main, but not under desktop, as there's no desktop support right now for Crashlytics in Kermit. I'm going to just use Kermit in the common main folder however here so that you can see that the actual logging support to hide the logs in production will work across the board for any platform. So if I sync now, that will then bring in our new dependencies. Actually using Kermit is simple. All we have to do is just add calls to logger star methods throughout our code to then send messages and stack traces to our logging methods. To add logging to your crash supporting solution, simply call logger dot set log writer with the name of the appropriate log writer. Here's some examples here. We added some logger messages directly into our main vm model, right here. Every time we add something to the database, we add log messages that will show only in debug, but not in production. In production, it will go instead to our crash reporting solution. I'm going to then run my code right here. Cuz I'm running in debug
>
> **[3:08](https://www.linkedin.com/learning/kotlin-multiplatform-development/kermit-for-logging?u=76281980&t=188)** we expect to see the logs spit out right here. As you can see, I have info top stories insert in a database right here and that is coming from my logger. The line of code right here. And if I were to add a top story, then we end up with another line there. So these log messages, we can sprinkle 'em throughout our code and the production version of our app will not show these messages to somebody if they run it with a tool like Log Cat, for example. But in production, it will also send them the message to our crash reporting solution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Gradle](../../Glossary/Tool/Gradle.md) (2), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (1), [Google](../../Glossary/Service/Google.md) (1), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1), [Android](../../Glossary/Sdk/Android.md) (1)
> **CLI Commands:** cat (1)
> **Code Identifiers:** ios (1)
> **Env Vars:** kts (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Managing resources](https://www.linkedin.com/learning/kotlin-multiplatform-development/managing-resources?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/managing-resources?u=76281980&t=0)** - [Instructor] When it comes to developing an app across many different platforms, one simple area to reduce overhead is to share different types of resources between all of the apps. We can share translated strings for different languages, including plain strings, strings with templates, plurals, string arrays. We can include fonts to share as well as image and file assets. We can also create different directories for alternate versions of resources depending upon qualifiers. For example, you can have different strings for different languages or use different images depending on day versus night mode theming. You would normally need to add the [compose](../../Glossary/Framework/Jetpack%20Compose.md) component resources dependency, but we've actually been using it all along. You'll notice that it's included as part of compose with the Compose [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) plugin. Let's start by adding the Comic Sans TrueType font. What better way to troll all the font purists out there? So we will simply add the font file to commonMain, composeResources, font folder, and we put this TrueType font in there, which everyone will recognize from its infamy, and at that point, we can then create a compose FontFamily based upon that file, so if I go over here to Fonts, I have created a composable function that creates a FontFamily based on the Comic Sans font, and this will work on every platform that we have here,
>
> **[1:37](https://www.linkedin.com/learning/kotlin-multiplatform-development/managing-resources?u=76281980&t=97)** so finally, we can change the text of one of our buttons in our app to use the edgy new FontFamily, so here we have the insert_story button to add a fake story, which will now be in Comic Sans format, so if I go here and run it, you see I now have a Comic Sans Insert a fake story, and the same thing would happen if I were to run it on [Android](../../Glossary/Sdk/Android.md), for example. As you can see, the font has changed here to Comic Sans on Android, and the same would apply for iOS, so the original app we started from already included a vector image here of the compose-multiplatform logo in XML image drawable format. The vector drawable on Android is a format very similar to SVG, or Scalable Vector Graphics. However, because Android doesn't natively support SVGs, exactly, we can support all of the platforms for everything that compose-multiplatform supports if we use a vector-drawable format, like Android supports. We can also use rasterized image formats like JPEG, PNG, bitmap, WebP, all of these work, but will work best if you provide different images for each qualifier directory so that you can support screens with different pixel densities. Otherwise, you'll make blurry images that don't look very good. Perhaps the most useful aspect of sharing resources for multiplatform [Kotlin](../../Skills/Software%20Development/Kotlin.md) projects is to share translations.
>
> **[3:12](https://www.linkedin.com/learning/kotlin-multiplatform-development/managing-resources?u=76281980&t=192)** Here we have a strings file where we have English text, and then we have another one with everything translated by AI into Spanish. Hopefully, these are fairly accurate. I'm not worried if they're not because this is a example. In order to provide translations, you need to create a default strings file for your default language first. We're using English as our default right now, so after you've created this, then you can create alternate directories for each language and locale using the resource qualifiers in the directory name. In this case, es is español, so we have our Spanish translations, so here we're translating our app into Spanish. We need to create this values directory here, and then the values-es and populate both of those, so you copy the strings.xml file here, and you change all these values within the string tags into Spanish. Be aware that, like, if you use AI to do translations, that it does make mistakes, but in this case, it's just good enough for testing, and at this point, we can actually run our app in Spanish, for example, and it should just work. As you can see here, the application has been translated into Spanish based upon the strings we put in values-es/strings.xml. All of these are correctly shown here, and the app still works.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (6), [Android](../../Glossary/Sdk/Android.md) (5), [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (4), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (1)
> **Code Identifiers:** commonmain (1), composeresources (1), insert_story (1), ios (1)
> **Env Vars:** xml (1), svg (1), jpeg (1), png (1)
> **Analogies:** for example (3), similar to (1)
> **File Paths:** strings.xml (1), values-es/strings.xml (1)
> **CLI Commands:** make (2)
> **Definitions:** is a  (2)
> **Warnings:** be aware (1)

#### [KOIN for dependency injection](https://www.linkedin.com/learning/kotlin-multiplatform-development/koin-for-dependency-injection?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/koin-for-dependency-injection?u=76281980&t=0)** - [Instructor] [Dependency Injection](../../Skills/Web%20Development/Dependency%20Injection.md) is very popular among experienced software developers. It offers improvements to the decoupling, testability, and reusability of software components. For [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md), the most popular dependency injection solution of choice is called Koin, with a K. It has a simple, easy to use DSL, domain specific language, and can be used with annotations to simplify adding it to your classes. Let's start by adding Koin to our project. Then we should add dependency injection to improve our existing code. As usual, we should add the actual library dependencies to the project. If we go to [Gradle](../../Glossary/Tool/Gradle.md) folder, libs.versions.toml, we add Koin version 4.0.4 is the current one right now, maybe newer when you look at it. And then we add three actual libraries, koin-[Android](../../Glossary/Sdk/Android.md), specific to Android, koin-[compose](../../Glossary/Framework/Jetpack%20Compose.md)-viewmodel, for injecting view models, and koin-core. We don't end up needing a plugin for this. So when we go over here, again, we don't need a plugin. We add koin-android under androidMain.dependencies. We add koin-core and koin-composed-viewmodel under the commonMain.dependencies, and that's it there, and there's nothing else to do in that file.
>
> **[1:37](https://www.linkedin.com/learning/kotlin-multiplatform-development/koin-for-dependency-injection?u=76281980&t=97)** Next, we need to add the code to initialize Koin on startup. We need to run the startKoin command for each platform with all the modules and the logging configured. When producing multi-platform projects, there's a common Koin pattern where the project will have a list of modules including a common module and a platform module. By defining the platform module in shared code as expect val platformModule, as we do here, we can define it differently for each platform. First, let's identify common code modules to add to the common module. We can add the MainViewModel with viewModelOf, the HTTP client, the NewsApi, the [SQL](../../Skills/Data%20Science/SQL.md) driver, and our database. Every one of these can be added as a singleton, except for the view model, using the singleOf method. The view model has a special viewModelOf method to use instead, because it has to be injected differently. Now that we have our common modules to find in a module, to identify any platform specific modules to add, at this point, there are none. Every library that we've used in this project has shared code provided for each platform, but it's good practice to have these modules ready. So we're going to create an empty platform module for each platform. Here's desktopMain,
>
> **[3:11](https://www.linkedin.com/learning/kotlin-multiplatform-development/koin-for-dependency-injection?u=76281980&t=191)** iOS, and Android. At this point, we should wire up the Koin initialization. Android needs special configuration due to Android's context, so we'll need to create a custom Android application class and wire up its onCreate in order to start Koin. We'll first create an AndroidApp class extending from Android's application class as we do here. Then after overriding the onCreate here as I do, we also have to add this name parameter to our application. This ensures that our app is started up using this new custom application. Here, we run startKoin. We pass in the androidLogger class. We pass androidContext with the application context. And we pass modules of appModule, which as you remember, is a list of the common module with everything in it and the platform module with everything in it. And like I said, we have to make sure that we define our app here. Otherwise, none of this code is going to get used when we start our app. Now we have to do the same kind of thing for iOS. However, we have a initKoin class in shared code here. This can be reused for both iOS and desktop, basically for all platforms that don't need anything special as far as context or the Android logger.
>
> **[4:46](https://www.linkedin.com/learning/kotlin-multiplatform-development/koin-for-dependency-injection?u=76281980&t=286)** Here, we just set the printLogger for everything debug and above, and we pass the appModule again. Now, if we go over to iOSApp.[swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md), I'm going to wire it this way, I'm going to add a UIApplicationDelegateAdaptor, and create an AppDelegate, so that I can then import ComposeApp. ComposeApp here is our XCFramework, our shared library that I created, I'll show you. Right here, we created ComposeApp as the baseName, and we made it static. You can do this a lot of ways, but here we have all of our iOS logic for shared code bound up into a shared framework called ComposeApp. So here we import ComposeApp, and then that makes available the KoinSharedKt.doInitKoin method. This looks really ugly, but this is a builtin method of the UI application delegate. So we are just adding a new function call to something builtin that needs to be overridden. Anytime our application starts up, we initialize Koin. Now we need to wire up each of the places where we should be injecting our classes instead of constructing them on the fly. Remember in NewsApi, if go there, we were originally creating our client on the fly, but now we actually create our client here with the client KT method that does this.
>
> **[6:23](https://www.linkedin.com/learning/kotlin-multiplatform-development/koin-for-dependency-injection?u=76281980&t=383)** So we can say singleOf client, and it'll reference this, and we no longer need to do it on the fly here. We can just say, by inject. And this is important, NewsApi needs to subclass KoinComponent. This makes this inject method function. In MainViewModel, we should be injecting our NewsApi by inject as well. This makes sure it's the same instance, because we use singleOf, remember? And the database, we were using this createDatabase call. Now we can just say by inject. As you can see here, we call createDatabase like this. We no longer need to have all the extra junk. We can simply have createDriver and createDatabase as singles here, and all of the magic happens in MainViewModel. It just finds it by doing CacheDB by inject. Again, I add KoinComponent here to our MainViewModel so that we can inject to it. Finally, we need to do one more thing. To make everything work in compose, and to be able to inject into compose, get rid of that, we add a KoinContext surrounding the top level of the entire compose app. This then allows me to inject things with things like KoinViewModel or the call to get or call to inject. Remember, I used to have to define my view model here in the constructor with a default value.
>
> **[7:58](https://www.linkedin.com/learning/kotlin-multiplatform-development/koin-for-dependency-injection?u=76281980&t=478)** Now I can just inject it, and it's going to always use the right view model. This right here, and that's all coming again from this. At this point, the app should now run on all platforms and we're now properly using dependency injection to share singletons instead of passing in a brand new instance each time that could cause us terrible bugs down the road. This should make it more testable and much easier to use.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (9), [Dependency Injection](../../Skills/Web%20Development/Dependency%20Injection.md) (4), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (4), [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (1), [Gradle](../../Glossary/Tool/Gradle.md) (1)
> **Code Identifiers:** ios (4), singleof (3), createdatabase (3), startkoin (2), viewmodelof (2)
> **CLI Commands:** make (3), find (1)
> **Env Vars:** dsl (1), http (1), sql (1)
> **Definitions:** is a  (2), is called (1)
> **File Paths:** libs.versions.toml (1), iosapp.swift (1)
> **Versions:** version 4 (1), 0.4 (1)
> **UI Navigation:** go to (1)

#### [Storing values in multiplatform-settings](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980&t=0)** - [Instructor] We've talked about how to persist data by storing it into a database with custom [SQL](../../Skills/Data%20Science/SQL.md) code. However, many times we want to persist values and apps, we don't need a complex database. A simple key value store is enough for most purposes, like app settings or preferences, for these purposes, there's a very useful [KMP](../../Glossary/Framework/Kotlin%20Multiplatform.md) library called multi-platform settings. It's a common pattern in apps to have an onboarding screen that you only ever show once. You may have app settings like choosing whether to show dark mode or light mode. In these cases, it often doesn't make sense to store the values in a database with all of the overhead of making special database tables and queries. This is why settings exist, like user defaults on iOS or shared preferences or data store on [Android](../../Glossary/Sdk/Android.md). Additionally, you might want to encrypt certain settings if they contain sensitive values like your OAuth [Tokens](../../Skills/Web%20Development/Tokens.md) or your encryption keys. Safe encryption and storage of these values at [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) demands safe and effective solutions like key chain storage on iOS or encrypted shared preferences on Android. You'll be happy to know that multi-platform settings helps you manage both cases so that you can securely store both encrypted and plain text settings across multiple platforms. Note that if you don't need encrypted preferences, the KMP integration is far easier using the no ARG dependency for multi-platform settings.
>
> **[1:36](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980&t=96)** However, I'm going to show you the tricks needed to set up your own custom factories and use coins named dependencies to inject two different versions of the same type, both encrypted and unencrypted settings into the same single settings repository class. So let's first go to Libs.versions, toml in the [Gradle](../../Glossary/Tool/Gradle.md) folder as we always do. We need to add the multi-platform settings dependency, which I have the version here, and I have the module here. You're also going to need Android X security crypto KTX, which I have the version here. And here I have the module for the library. So the latter library for Android is technically deprecated by [Google](../../Glossary/Service/Google.md), but it still remains the quickest way to create encrypted preferences on Android. Data store should hopefully have something soon. Until then, this is how you do it. For desktop, we have one more solution, which is we have the encryption for desktop credential storage. I have a version here and I have a version here. It's from [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) and it's for the JVM. So with those three libraries, then we should be set. So we can go over to the build.gradle KTS in the composeApp folder, and we have to add these libraries just as we did before. There's nothing to add up here for plugins. As we go down here we see Android security Crypto has been added to androidMain,
>
> **[3:10](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980&t=190)** and we've added multi-platform settings to common Main because it's our shared code. iOS has a special case because it uses the built-in key chain settings from multi-platform settings. We do not need to add a special iOS main dependency because it's built into the shared code. On Desktop Main, we add the credential storage JBM, and we don't need to do anything down here. That's all taken care of. Just need to add all the libraries under dependencies and we're good. So now we're going to add a settings repository class to our shared code. I created one down here. I made it a coin component, which again, this allows you to inject into the class. And here we're doing a special trick with coin injection. We're injecting name dependencies. Now, if you have more than one way of injecting something of the same type, you need to name them because there's no other way to tell them apart. So here we're injecting them by name, and if you look at the coin shared stuff, you see settings repository here. But what you don't see is how we're getting plain text encrypted. So for each individual platform, if I look at the coin shared platform module, we have a factory for settings that creates it by name, encrypted and plain text. So for Android, we have encrypted settings factory, which this get here is a way of saying that we want coin to inject into it.
>
> **[4:44](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980&t=284)** And what's happening here is this is looking for encrypted shared preferences that we're automatically going to pass into the encrypted settings factory. Likewise here we set up shared preferences. I create a plain text database for shared preferences and then pass it into these shared preferences settings, which is built into multi-platform settings. By the way, encrypted settings factory is something I created here. Basically it's still using shared preferences settings, but because encrypted shared preferences actually creates an instance of shared preferences, they're both the same type. Both the encrypted and the unencrypted version are using shared preferences. The difference here is how I created this with a master key that goes into your key chain in a way that all of this is secure. This code down here is going to make sure that our shared preferences and when you write them that they're encrypted. Okay, going down to desktop, we had to do some very special stuff. Here I have this provide encrypted preferences settings function that I got from open source. You can check it out in the sample code, but this takes care of all of that using the Microsoft credential storage. And for plain text, we just use a regular preferences settings with [Java](../../Skills/Software%20Development/Java.md) preferences, which are very common. Here for iOS, we're doing key chain settings, which again,
>
> **[6:18](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980&t=378)** this is built into multi-platform settings and it's user default settings, which is just how preferences or settings are normally done on iOS. And we pass in the standard user defaults. This is a platform function of iOS. So now that we've got all of this [Dependency Injection](../../Skills/Web%20Development/Dependency%20Injection.md) set up, each of them by name, then the logic will work in our injection to these settings repository. And this is shared code. So all of this here will allow us to put the scene onboarding Boolean in there in an unencrypted format. Because I'm using plain text settings, it'll allow us to put the last name of the fake story text so we can store the field value for next time and it will store our auth token. This isn't very useful right now because I'm hard coding the auth token, but if you were getting a token from a server from REST, this would be a great way to store it to keep it safe from prying eyes. And then a finally, a clear function for both settings, instances that just calls duck clear on each and this will wipe them all out. So now that I've created this settings repository, I can inject it as well. If I go to app.kt here again, we have the coin context. That means we can inject into the top level composed class. And here we're using coin inject to inject the settings repository directly into our composable function, our coin context. The fake story text here where I think you'd want to say, remember, mutable state of fake story text
>
> **[7:55](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980&t=475)** and then dismissed onboarding. This is just false, but what we're going to do is when the user dismisses onboarding or sees onboarding for the first time, we're automatically going to set it to true in the settings.
>
> **[8:14](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980&t=494)** Yeah, here I'm setting the fake story text when we press the button to insert a fake story that then saves it. And down here we're using settings.clear. If we haven't dismissed onboarding and the value is false for has shown onboarding, here we have a launch effect that is going to set, has shown onboarding to true. Launch effect is again, just a way of using a co-routine to run some code in our composable. Instead of having to put views here, we can just run code. This is a little confusing, the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) unit. When you put unit into launch effect, you're saying that I only ever want this to run once, not every time some value changes. So you could put a variable in here and then every time that variable changes, it runs agaain. This is a way of saying I only want it to run one time. We've added all of our preferences, both encrypted and unencrypted, and now if I run it, clear sittings and run it again, you didn't see the message because I already ran it, but here is the message that I've created that only appears one time. If I run the app again, it's not going to appear. And at this point I can type in something. This is a fake story and if I go back, run it again, it should still say that. There we go. So we have our shared preferences being stored in the Android app and jump over to, let's do desktop next.
>
> **[9:53](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980&t=593)** As you can see, desktop is the onetime message. And I can say, this is a fake story, insert. And then if I rerun it, expect to see the same thing. There we go. That's desktop. And here is an iOS. So an iOS, we have the one time message. Let's say this is a fake story, insert a fake story.
>
> **[10:24](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980&t=624)** Run it again. Stop and rerun. And it didn't show the one time message and it kept the text. So all three of them now are storing settings or preferences into a key value store, storing encrypted stuff in encrypted settings and non-encrypted stuff in unencrypted settings. Now let me just prove that this worked. I'm going to go over to device manager and here I can bring up the device explorer like this bigger go to data, data, find our app, the list, and then shared preferences. If you look at the plain text, this is seen onboarding, true, fake story, this is a fake story. Encrypted XML, you'll notice has all of this. If I go out to a running device here for a second and make some requests, expect this to crash. So don't worry 'cause I didn't put a API key in there. Okay, if I look at this now, it's opened up. I'm go device explorer. Close that. You can look at the encrypted settings and now you can see there's a new encrypted setting. This one has the key name encrypted as well as the value encrypted. You can see it's a symmetric cipher, but yeah, you can't read any of this. This is hopeless if you're a hacker. So you would have to get it the key, which is stored in the key chain. And so this is secure on Android. On iOS, it's a little bit harder to get at.
>
> **[11:57](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980&t=717)** If you're using the simulator, you go to window, devices and simulators. If it's a device, you can just download the entire container. If it's a simulator, you need to get this identifier and go to a very specific directory, which is going to be a pain. And I'm not going to say it's going to be quick or easy. You can look this stuff up online. The folder you want is going to be under the hidden directory, the library, under your home directory, it's going to be library developer, core simulator devices, and then the identifier you got here with data containers, data application, the app id, library preferences, and then the applications bundle id.plist. So if you can find it, you can hunt for it with the terminal's fine command, looking for .plist files. This file contains your unencrypted settings, I can show you one right now. So this is an example where it's stored, seen onboarding with the setting. It's hard to read, but it's there. You can see the settings are being stored. That's the unencrypted one. The encrypted one, you're going to have to get even more fancy for. I have the database browser for SQLite, and here's the directory with the device id. It's going to be long and hard to get at. You need to look in the GenP folder, once you find the right database. And then go to this AGRP column, find your team ID and the full bundle ID here. And then you go to the data column and tap into that.
>
> **[13:32](https://www.linkedin.com/learning/kotlin-multiplatform-development/storing-values-in-multiplatform-settings?u=76281980&t=812)** And you can look over here and see that there are SF Cipher texts, authentication codes hidden here that you can't read. So just to show you, iOS has a hidden too. I'm not going to bother going into desktop at this point, but you can also verify that that works correctly. At this point, congratulations, you can now handle storing both settings and sensitive [Data Storage](../../Skills/Data%20Science/Data%20Storage.md), both from [Kotlin](../../Skills/Software%20Development/Kotlin.md) multi-platform across multiple platforms. This is again, super useful. I hope you can make great use of it in your apps.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (9), [Kmp](../../Glossary/Framework/Kotlin%20Multiplatform.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [Gradle](../../Glossary/Tool/Gradle.md) (2), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (2)
> **Code Identifiers:** ios (11), composeapp (1), androidmain (1)
> **Env Vars:** kmp (2), sql (1), arg (1), ktx (1), jvm (1)
> **Definitions:** is a  (8), is an  (2)
> **CLI Commands:** make (4), find (4)
> **UI Navigation:** go to (6)
> **Prerequisites:** set up (3)
> **File Paths:** build.gradle (1), app.kt (1)

#### [Dates in Kotlinx-datetime](https://www.linkedin.com/learning/kotlin-multiplatform-development/dates-in-kotlinx-datetime?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/dates-in-kotlinx-datetime?u=76281980&t=0)** - [Instructor] Dates are hard. There's a joke that engineers, much like [Microsoft Excel](../../Skills/Data%20Science/Microsoft%20Excel.md), both struggle to know when something is a date, but also the very nature of dates and times is unbelievably complex. Most regular people don't understand this, but there are so many examples. To get an idea, I recommend Googling, "Falsehoods engineers believe about time." Once you get basic date handling working, you then start to deal with time zones, and then fractional time zones, and once time zones seem all right, you then have to deal with daylight savings time, even sometimes fractional daylight savings time within the same time zone. Once you've got your time working right, you'll be very lucky if you never have to deal with historical times or dates before the laws change what the times and dates mean. The point of this is all very simple. Never ever roll your own date and time handling logic, unless you absolutely have to. It's foolish and is only ever going to make you very, very sad, especially when you have to support your code and the serious mistakes that it causes. It's also recommended to rely upon the very best available date and time libraries available. In [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md), there's a special library for this, called Kotlinx-datetime. So we're going to add code to perform date math and to show the date in different internationalized formats. Without the library we have access to, this task would be hard and you wouldn't know
>
> **[1:34](https://www.linkedin.com/learning/kotlin-multiplatform-development/dates-in-kotlinx-datetime?u=76281980&t=94)** whether your code was dangerous and broken until somebody traveled to a new time zone using your app, or maybe they found daylight savings time caused the time to roll back. And it wouldn't show the month and days swapped in a way people would expect in their region. In America, we prefer the month before the day, while in Europe and much of the world, prefers the day before the month. If you show these backwards, it can cause great confusion. I will note to you that this library is a work in progress and that it cannot implement certain low cal dependent things that we take for granted in the [Java](../../Skills/Software%20Development/Java.md) date, time libraries, like 12 hour time being handled automatically when you're using your date formats. This is because a number of the [Kotlin](../../Skills/Software%20Development/Kotlin.md) platforms do not yet have access to the rather essential concept of locales. So ISO date format will always work with this library, but it isn't ideal for a lot of use cases. You can also then manually construct your own custom date formats, as needed, by handling each section of the date individually in your common code. So for this, let's start, as usual, by making sure we have the necessary dependencies. We need Kotlinx-datetime. We've got the version here, and the library here. You may already have it added from previous exercises because we had dates within our return values from some of these network APIs,
>
> **[3:06](https://www.linkedin.com/learning/kotlin-multiplatform-development/dates-in-kotlinx-datetime?u=76281980&t=186)** we added in the Apollo date, time support section. Now let's add dates to our top hacker news stories. So we need to update topstory.sq under SQLDelights, under Common Main. So here we have a time, integer not null, and we added a new method that should allow us to update the dates. We're using the upsert method here, this one, which has insert or replace in the top story. And when we put a new time in here, it's going to populate and replace the existing entry. So using this, we can go to main view model, and we can create a new function. Add four hours. So here we're taking advantage of the date math capabilities of our Kotlinx-datetime library. So we're doing a select on a particular ID in top story queries. So I think I had to add that. You'll see there's a select star where ID equals question mark that allows you to select not all of the stories, but just the one. Here, we're doing select execute as one, which then returns a single top story, based on the ID. Here we do instant from epoch second, so this is taking the time, which is stored, in this case, in seconds since 1970, and it's adding four hours to the time, and then returning epoch seconds again.
>
> **[4:42](https://www.linkedin.com/learning/kotlin-multiplatform-development/dates-in-kotlinx-datetime?u=76281980&t=282)** So then finally, we do database top story queries, upsert. This is again, going to do an insert or replace. You could do an update instead, which would be more recommended, in my opinion, but we're going to replace here, and it's going to replace every single one of those with the new time inserted. So we have top story ID, top story type, top story by, all of these are the same except for the time. This will change everything, assuming that it all happens atomically on one thread. So if I run the code here, I run desktop first. Now I have all these hacker news stories with the date and time, which as you can see, if I go look at app Ducket, I changed this as well. The logic to show date and time. Here we go. So I create a by Unicode pattern, create this date format, and I say instant from epoch seconds, step format, date format. And then this allows me to show the date and time. Now keep in mind, when you're trying to do date formats with this library, it doesn't handle 12 hour time, and that kind of sucks. It's a work in progress, and when the locales are supported, you'll be able to do all the things you can do at the Java Library. Going back to our thing here, every time I press one of these, you can see the time increases four hours and the date will increase when I go over the end of the day. And this button was added for each story, so you can mess with it.
>
> **[6:14](https://www.linkedin.com/learning/kotlin-multiplatform-development/dates-in-kotlinx-datetime?u=76281980&t=374)** There's still ordered by score, so you're not going to change the order by doing this, but you can at least see that the date math is correct. Okay? We can do the same thing now for [Android](../../Glossary/Sdk/Android.md). And we can do it for iOS. Okay, here we go. As you can see, dates roll over, and date math is correctly handled. So I hope this helps you. Hopefully they soon figure out a way to support locales across more platforms. That would be very nice so that you're not forced to do JVM-specific solutions or having to roll your own date and time formats based upon what is supported. I hope that was helpful.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2), [Microsoft Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1), [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (1), [Kotlin](../../Skills/Software%20Development/Kotlin.md) (1), [Android](../../Glossary/Sdk/Android.md) (1)
> **Env Vars:** iso (1), jvm (1)
> **Definitions:** is a  (2)
> **Best Practices:** recommended (2)
> **CLI Commands:** make (1)
> **Code Identifiers:** ios (1)
> **UI Navigation:** go to (1)
> **Warnings:** keep in mind (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/kotlin-multiplatform-development/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-multiplatform-development/next-steps?u=76281980&t=0)** - Congratulations on completing this epic journey into the wild world of [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md). I hope you found this course very helpful as your first step down the road to mastery. To continue on your adventure, I recommend watching the free YouTube videos from recent [Kotlin](../../Skills/Software%20Development/Kotlin.md) conferences. These have outstanding content from top engineers at innovative companies. To learn more about the Kotlin language, there are a ton of resources. Consider the book "Kotlin in Action", which goes into great depth, or just build something useful to you, which is always a great way to learn. You can also follow me on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md), where I often share content related to the newest innovations in both Kotlin and [Compose](../../Glossary/Framework/Jetpack%20Compose.md) [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md). Follow me for tips, hot takes, and posts that'll make your manager think you're researching something. Thank you for joining me. I'm sure you're excited to use all that you've learned. Remember, always celebrate responsibly. Friends don't let friends push straight to production.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Kotlin](../../Skills/Software%20Development/Kotlin.md) (4), [Kotlin Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1), [Multiplatform](../../Glossary/Framework/Kotlin%20Multiplatform.md) (1)
> **CLI Commands:** make (1)
> **Speakers:** - congratulations (1)


## Instructor

- [Colin Lee](../../Instructors/Mobile%20Development/Colin%20Lee.md)

## Resources

- Exercise files available

## Skills Covered

- Kotlin
- Cross-platform Development

## Path Context

### In [Kotlin Professional Certificate by JetBrains](../../Paths/Mobile%20Development/Professional%20Certificates/Kotlin%20Professional%20Certificate%20by%20JetBrains.md)
← [Kotlin Essential Training- Object-Oriented and Async Code](Kotlin%20Essential%20Training-%20Object-Oriented%20and%20Async%20Code.md) | **3 of 4** | [Exploring Ktor with Kotlin Multiplatform and Compose](Exploring%20Ktor%20with%20Kotlin%20Multiplatform%20and%20Compose.md) →

## Part of

- [Kotlin Professional Certificate by JetBrains](../../Paths/Mobile%20Development/Professional%20Certificates/Kotlin%20Professional%20Certificate%20by%20JetBrains.md)

## Appears In

- [Kotlin Professional Certificate by JetBrains](../../Paths/Mobile%20Development/Professional%20Certificates/Kotlin%20Professional%20Certificate%20by%20JetBrains.md)

## Related Courses

_Courses sharing skills:_

- [Exploring Ktor with Kotlin Multiplatform and Compose](Exploring%20Ktor%20with%20Kotlin%20Multiplatform%20and%20Compose.md) — Kotlin
- [Kotlin Essential Training- Object-Oriented and Async Code](Kotlin%20Essential%20Training-%20Object-Oriented%20and%20Async%20Code.md) — Kotlin
- [Kotlin Essential Training Functions Collections And I O](Kotlin%20Essential%20Training%20Functions%20Collections%20And%20I%20O.md) — Kotlin
- [React Native Ecosystem and Workflow](React%20Native%20Ecosystem%20and%20Workflow.md) — Cross-platform Development
- [C- Cross-Platform Development](../DevOps/C-%20Cross-Platform%20Development.md) — Cross-platform Development

---

[↑ Back to top](#)