---
type: course
cssclasses:
  - lle-course
  - lle-level-advanced
slug: create-a-crm-mobile-application-with-react-native-23458867
url: "https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867"
duration_minutes: 170
duration: 2h 50m
level: Advanced
updated: 2/28/2024
learners: 18638
skills:
  - Mobile Application Development
  - React Native
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQHnKiYx-QGRdg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1708996526383?e=2147483647&amp;v=beta&amp;t=RVD3RlZgu14-Sz0_TSe3LZ8Axfu6X7DiOHXEhWZPVKw"
linkedin_topic: Mobile Development
learning_paths:
  - '[Become a React Native Developer](../../Paths/Mobile%20Development/Learning%20Paths/Become%20a%20React%20Native%20Developer.md)'
prev_courses:
  - '[From React to React Native](From%20React%20to%20React%20Native.md)'
next_courses:
  - '[React Native Ecosystem and Workflow](React%20Native%20Ecosystem%20and%20Workflow.md)'
path_nav: '[{"path":"Become a React Native Developer","position":3,"total":5,"prev":"From React to React Native","next":"React Native Ecosystem and Workflow"}]'
path_count: 1
tags:
  - course
  - topic/mobile-development
  - skill/mobile-application-development
  - skill/react-native
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Mobile%20Development/Create%20a%20CRM%20Mobile%20Application%20with%20React%20Native.md)

![Create a CRM Mobile Application with React Native](https://media.licdn.com/dms/image/v2/D560DAQHnKiYx-QGRdg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1708996526383?e=2147483647&amp;v=beta&amp;t=RVD3RlZgu14-Sz0_TSe3LZ8Axfu6X7DiOHXEhWZPVKw)

# Create a CRM Mobile Application with React Native

> React Native lets web developers apply their skills to native apps for iOS and Android. In this course, Emmanuel Henri shows how to take a design spec created in a previous course—Prototype a CRM Mobile App with Framer—and convert it into a real CRM application, with a clean interface, smooth performance, and natural cross-platform adaptability. Learn how to set up a project, establish a strong fo

> [LinkedIn Learning](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867) | 2h 50m | Advanced | 19K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [CRM application with React Native](#crm-application-with-react-native)
  - [Course prerequisites](#course-prerequisites)
- [**1. Project Setup and Backend**](#1-project-setup-and-backend) (4 videos)
  - [Initialize React Native project](#initialize-react-native-project)
  - [Project folder structure](#project-folder-structure)
  - [Explore material kit and vector icons](#explore-material-kit-and-vector-icons)
  - [Node and express backend setup](#node-and-express-backend-setup)
- [**2. Redux and Main Views**](#2-redux-and-main-views) (7 videos)
  - [Get started with Redux](#get-started-with-redux)
  - [React Native actions](#react-native-actions)
  - [Create tab menu](#create-tab-menu)
  - [Tab navigation](#tab-navigation)
  - [Create the people list view](#create-the-people-list-view)
  - [Code the people item view](#code-the-people-item-view)
  - [Add new components](#add-new-components)
- [**3. Complex Component**](#3-complex-component) (4 videos)
  - [Setting up Redux for a detail view](#setting-up-redux-for-a-detail-view)
  - [CSS details](#css-details)
  - [Coding the scroll on the detail view](#coding-the-scroll-on-the-detail-view)
  - [Finalize detail view](#finalize-detail-view)
- [**4. CRUD Operations**](#4-crud-operations) (6 videos)
  - [Create a new add person form](#create-a-new-add-person-form)
  - [Functions for add person action](#functions-for-add-person-action)
  - [Update person Redux reducer](#update-person-redux-reducer)
  - [Update person view](#update-person-view)
  - [Connect your list view to backend](#connect-your-list-view-to-backend)
  - [Testing and debugging Redux for editing](#testing-and-debugging-redux-for-editing)
- [**5. Company View and Final Touches**](#5-company-view-and-final-touches) (2 videos)
  - [Code the company list query](#code-the-company-list-query)
  - [Create company item and list view](#create-company-item-and-list-view)
- [**Conclusion**](#conclusion) (1 videos)
  - [Building your own CRM application with React Native](#building-your-own-crm-application-with-react-native)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [CRM application with React Native](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/crm-application-with-react-native?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/crm-application-with-react-native?u=76281980&t=0)** - [Mani] If you've built any applications with [React](../../Skills/Web%20Development/React.js.md), you've probably heard about [React Native](../../Skills/Mobile%20Development/React%20Native.md), the framework that allows you to build native iOS and [Android](../../Glossary/Sdk/Android.md) apps. In this course, we'll build a CRM application using React Native. We'll first set you up with everything you need to get going, and then get the backend set up and start it. Next, we'll build several views while managing our states with Redox, and finally, continue to create our application with full CRUD capabilities. In the end, like most of my projects, we'll code a small algorithm with Lodash to remodel our data and create a company list view. Hi, I'm Mani Henri. I'm a React and React Native developer. I'm here to support you on this journey [Learning React Native](Learning%20React%20Native.md) while building an amazing CRM application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (4), [React](../../Skills/Web%20Development/React.js.md) (2), [Android](../../Glossary/Sdk/Android.md) (1)
> **Env Vars:** crm (2), crud (1)
> **Code Identifiers:** ios (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [mani] (1)

#### [Course prerequisites](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/course-prerequisites?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/course-prerequisites?u=76281980&t=0)** - [Instructor] As this course is the creation of a [React Native](../../Skills/Mobile%20Development/React%20Native.md) application, some knowledge of [React](../../Skills/Web%20Development/React.js.md) is a must. If creating React components, using props, or the state, and importing modules are concepts that aren't familiar to you, I'd suggest you take a basic React course. Also, experience with [JavaScript](../../Skills/Software%20Development/JavaScript.md) is a must for this course, as this is the basis of a React programmer. You can take this course on a Mac or [Windows](../../Glossary/Service/Windows.md). I've delivered this course on both operating systems, so feel free to use whichever you prefer. Last but not least, being familiar with terminal commands and [npm](../../Skills/Web%20Development/npm.md) isn't a must, but definitely will help you when we use these tools.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (4), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [Windows](../../Glossary/Service/Windows.md) (1), [npm](../../Skills/Web%20Development/npm.md) (1)
> **Definitions:** is a  (2)
> **CLI Commands:** npm (1)
> **Tools:** terminal (1)
> **Speakers:** - [instructor] (1)


### 1. Project Setup and Backend

[↑ Back to Table of Contents](#table-of-contents)

#### [Initialize React Native project](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/initialize-react-native-project?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/initialize-react-native-project?u=76281980&t=0)** - [Instructor] Okay, so we're going to go and get started by creating a new [React Native](../../Skills/Mobile%20Development/React%20Native.md) project. And the way to go about doing this, if you want to follow along on the instructions from [reactnative.dev](https://reactnative.dev), you can go ahead and click on Get Started right away. And what it's going to show you are all the steps that you need to go through to create a new project. In the past, we used a React Native [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) Quickstart. It's a bit more complex and there's a lot of things that you need to install, but this is the way to go if you want to have specific development operating systems and specific target OS that you want to do. Especially if you're using [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md), you want to go and click on Linux and find out how you can actually use [Android](../../Glossary/Sdk/Android.md) and Linux. The same thing with Android and [Windows](../../Glossary/Service/Windows.md). iOS is not supported either on Linux or Windows. There's ways to counter that, but I wouldn't recommend it. If you really want to develop for iOS, go for macOS and then go ahead and install whatever you need to do this. This is the long way. There's a lot of things that you need to install to make it work this way. I would recommend, and this is what we're going to use in this course, is the Expo Go Quickstart and it goes very quickly. You can create a project and then open it up in your simulator as soon as you're ready. The requirements for it is that you have a simulator installed on your system. So if you're planning on going to Android as the target OS, then make sure you install Android Studio.
>
> **[1:36](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/initialize-react-native-project?u=76281980&t=96)** Make sure you install the requirements to use Android Studio. The same with iOS. If you are planning on going on iOS as the final target OS, go and make sure that you have [Xcode](../../Skills/Mobile%20Development/Xcode.md) installed, simulator installed, and all the requirements for these systems. And you can use the quick start here by jumping to the specific target OS and development OS and follow these instructions to actually install everything you need for your system. In most cases, you need all of these things anyways to finish up on a specific system. But to get started with the actual application, the React Native application, it goes much faster this way. So I have all the requirements on my system. I'm going to open up my simulator. I'm using iOS and basically right now, I have iPhone 14 Pro Max as the targeted operating system and the device. And what I could do is basically minimize the instructions here and then what we're going to need to do is open up a terminal to create the new application, which we're going to see on our desktop shortly. I'm going to create it on the desktop. So I'm going to open up my terminal, like so, and make sure that I am on my desktop. So I'm going to do CD desktop. So I just did an ls to see where I was. I'm going on a desktop and then, yep, I'm on the desktop now. So what I'm going to do is go ahead
>
> **[3:08](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/initialize-react-native-project?u=76281980&t=188)** and on my desktop, create a new application, CRM application, with the instructions here. And we're going to need to use the NPX command, which is basically the same as [npm](../../Skills/Web%20Development/npm.md). The other difference between NPX and NPM is that it doesn't need to install the libraries or the packages prior to do the NPM command. So what it does with NPX, it automates the actual installation of the libraries you need in order to create the new project. So I'm going to go and create expo app and call this application CRM. I'm building it on this desktop, like so. So while this is actually creating all of this, I'm going to check, I have the folder here, we're all good to go and wait for the installation slash creation of the project to finish. So now that I have the application or project created, I can CD into CRM like so. And now I am inside of the application, where I have all the assets for this specific application. So now what we're going to do is start the application and then run it on our simulator and make one small change in my tool of choice, which is VS code, where you can use any editor you want to make the code changes and see it change in the simulator. So let's go ahead and do this.
>
> **[4:43](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/initialize-react-native-project?u=76281980&t=283)** The command, as you can see here, is NPX expo start, as such. So now I've got this and you can use this to actually install expo on your actual physical iPhone and basically test it on the iPhone as well, which is super cool when you want to see on the actual physical device how it actually runs. Now I'm just going to run it on the simulator so I can do I for iOS. If you are on Android, you do A. And let's go ahead, I'm going to do I, and now you're going to see my simulator here run the application. Go and do this, open up the application. And as you can see, this is the Expo app. So there's nothing in this application yet. So if you want to see it live, I'm going to close this window, I'm going to minimize this for now. I may use the terminal directly in VS code. And what I'm going to do is drop the folder inside of VS code so we can see our code here and make the proper change. And you need to go into the App.js file. And then I'm going to close the explorer so I can see my simulator here as well. Have as much screen as possible, like this. Put my simulator here, so I can see it while I'm doing development, like so. This is cool. Yeah, that's good. And as you can see right now, I have this here.
>
> **[6:18](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/initialize-react-native-project?u=76281980&t=378)** Open up in App.js to start working on your app. This is the same thing that we have here. So let's go ahead and change this and you'll see the change happen live. And if I save this, I'm going to see the change directly in the application here. This is super cool. The other thing that you need to always keep in mind of is that you can also open the debugger directly from here. So if I press J here, I'm going to see the debugger and you have all the stuff that you need for the debugger here. All right, so now that we have a project started, we can move on. In the next video, we'll get familiar with the folder structure and the files and take a look at the schema.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (6), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (4), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (3), [npm](../../Skills/Web%20Development/npm.md) (3), [Windows](../../Glossary/Service/Windows.md) (2)
> **CLI Commands:** make (8), npx (4), npm (3), cd (2), find (1)
> **Env Vars:** npx (4), crm (3), npm (3), cli (1)
> **Code Identifiers:** ios (6), iphone (3), macos (1)
> **Prerequisites:** install (8)
> **Tools:** terminal (3), vs code (3)
> **UI Navigation:** click on (2), open the (1)
> **File Paths:** app.js (2)

#### [Project folder structure](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/project-folder-structure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/project-folder-structure?u=76281980&t=0)** - [Instructor] Let's take a look at the project structure and what are the files included with an Expo build. Note to self, if you've ever done [React Native](../../Skills/Mobile%20Development/React%20Native.md), the [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) approach, you have more files here, and you have like the iOS and [Android](../../Glossary/Sdk/Android.md) files. And if you want to write specific codes to these specific files for targeting these target OS, it's in the folder structure here. But when you do an Expo version, it doesn't have any of that. Expo actually supports not only Android and iOS but also the web, so you can leverage this to create a build after that either for web, for iOS, or Android. So, this is the beauty of working with Expo. Right now, this is basically a React Native project supported by Expo. But once you're done with this, if you want to do a build, specifically to all these different target OS, then you create a build and we're going to take a look at this later on in this course. So right now, what we're going to do is just explore what Expo offers for the file folder structure, and it actually is much more simplified than the other approach. So right now, you should have an Expo document here, which basically keeps track of where you've actually worked with in terms of devices. So right now, we just did an iOS simulator, and this is why there's nothing here. And if you read the README.md, it's going to tell you that you don't have to pay much attention to this unless you want to have more information,
>
> **[1:34](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/project-folder-structure?u=76281980&t=94)** and you do not commit this file or folder to [GitHub](../../Skills/Software%20Development/GitHub.md), and it's in the .gitignore here. So, this is the Expo. Then, the assets are basically all the image files that are used in the application. So once we start putting buttons and stuff like that, this is where we're going to put them. The node_modules are all the libraries and packages that are installed with this application, and you can see a replication of what they are. So we have [react](../../Skills/Web%20Development/React.js.md)-native, we have react, expo-status-bar and expo, and you have @babel/core for the devDependencies, and these are the scripts that we can use with this one. And basically what we're doing is npx expo start, and you can be specific when you start it to go to iOS or to go to the web here. So this is what I was talking about here, so if we go into our terminal that we've opened. Because it's already opened here, I'm going to stop the server from this terminal and do it from VS Code instead. And I'm going to bring my terminal either by clicking here, New Terminal, or Control + Grave, which I usually use as the shortcut here. And do, let's say for example, ios. So npx expo start
>
> **[2:55](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/project-folder-structure?u=76281980&t=175)** - -ios, it's going to literally open my server and go straight to iOS and open up my application here. So let's say I reopen the application, this is fine. This is from my previous build. And again, if we go back to the App.js file and make another change, you'll see that change live in here. So, Hello you, save. It should update automatically. If it was from the server connection prior, you can restart it here, no problem, or you can just basically reload the app by doing R, like so. And then it's going to reload the app, and Hello you. So if you have any issues where the application doesn't automatically change the text, you can do an R in here and it will basically rebundle and send it to the application. And now, it's working, okay? So, this is for the App.js file. Well, this is the entry point for your application here, so let me just close this for a second and make this full. And this is the file where you have basically your first view, which we'll explore later on what are these views and these components that you're going to use with React Native. And you have a stylesheet already created. This is why you don't have an external stylesheet, it's created within the file, and we're going to keep that structure throughout the course as well. So, we have a nice stylesheet inside of the component
>
> **[4:29](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/project-folder-structure?u=76281980&t=269)** and keep it modulated for that specific file. We have the app.[JSON](../../Skills/Web%20Development/JSON.md) file here where you can actually put more information about your application. So the version, what is the orientation, where are the icon, and so much more that we'll explore later on. If you have specific things that you want to do to the web, Android, or iOS, you can enter these things here as well. And if you want more information about the app.json file, you can explore it on the documentation at [expo.com](https://expo.com) and you'll know all the details about that. We're not going to do this in this course. The babel is the config for babel, and then the package.json, we explored already. So this is pretty much the file structure for a brand new project created with Expo as opposed to the CLI approach, which actually minimizes the number of files and makes it that much faster to create or work with your project and application. So, this was the file structure. Let's move on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (4), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (3), [JSON](../../Skills/Web%20Development/JSON.md) (3), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2), [React](../../Skills/Web%20Development/React.js.md) (2)
> **Code Identifiers:** ios (7), node_modules (1), devdependencies (1)
> **File Paths:** app.js (2), app.json (2), readme.md (1), package.json (1)
> **Tools:** terminal (4), github (1), vs code (1)
> **CLI Commands:** npx (2), make (2)
> **Env Vars:** cli (2), readme (1)
> **Documentation:** the readme (1), the documentation (1)
> **UI Navigation:** go to (2)

#### [Explore material kit and vector icons](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/explore-material-kit-and-vector-icons?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/explore-material-kit-and-vector-icons?u=76281980&t=0)** - [Instructor] This project will use a few libraries to make our application follow material design styles. For example, we'll set up material kit, a library full of great pre-built components, based on material design. We'll also explore a vector icon library that is pre-installed with Expo, so we can have icons we can use whenever we need them. With those two libraries, we should have everything we need to build great views very quickly. So let's jump to your console core command prompt for [Windows](../../Glossary/Service/Windows.md) to add those. So the first thing we need to do, if you want to follow along with the instructions, you can go to the webpage where we have the information about the vector icons. So you can go to docs.[expo.dev](https://expo.dev), forward slash guides, forward slash icons. In the past, we needed to install this library separately, but now it's included directly inside of Expo. So if you look at this here, all we have to do at this point is import ionicons here from Expo Vector. So if you want to use ionicons, obviously, but if you want to have more information about the icons, you can click here on this link and take a look at what are the icons available. So you have several that you can take a look at. So for example, if you want an arrow, then you have all these designs that you can take a look at and so on and so forth. So you can look for specific icons and add them as you please. So, the way it works is basically, you import ionicons from @Expo/vector, icons, and then you can use them directly inside of a view. So we can do that later on,
>
> **[1:34](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/explore-material-kit-and-vector-icons?u=76281980&t=94)** but I just wanted to mention to you that it is pre-install with Expo and therefore, we don't need to install anything else. For the material design, we need to install this library, [react](../../Skills/Web%20Development/React.js.md)-native-material. So the way to go about it, this one is fairly simple. You go all the way down here and all you have to do, is do [npm](../../Skills/Web%20Development/npm.md) install@react-native-material/core. So I'm literally going to copy this command here and go into VS code, open up the terminal, shut down our server here, and just to get some space here, I'm going to remove the Explorer. And all I have to do is paste this particular command here and I'm going to install the react-native-material core. So with this, if we go back to the documentation, we would be able to add a button like this. So import button from react-native, and then add it to the application here, like so. So if I show you what I just did and we start our application again, like so, now what could do is do R, to reload the application. So it rebuilds it. It's very important to do that from time to time, just to kind of refresh the build on this simulator. So now that we have that, what we could do is, in this particular view here, we can import the button as such. And then in the text or somewhere in here, let's just go back to documentation,
>
> **[3:06](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/explore-material-kit-and-vector-icons?u=76281980&t=186)** and basically grab this button here like so. We're going to copy and paste that button here. Just add it in here, save. Now that we added it, it would show up on the simulator. So we're not going to use that for now. I just wanted to give you an example. We're going to do that later. And remove that here. So you have an idea of how this works, but we're going to do multiple items as we build our components here. I just wanted to give you an example. So now that we have a kit we can use to build our views plus a great icon library, let's move on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (4), [Windows](../../Glossary/Service/Windows.md) (1), [npm](../../Skills/Web%20Development/npm.md) (1)
> **Prerequisites:** install (6), set up (1)
> **Tools:** command prompt (1), vs code (1), terminal (1)
> **CLI Commands:** make (1), npm (1)
> **Cross-References:** go back to (2)
> **UI Navigation:** go to (2)
> **Analogies:** for example (2)
> **URLs:** [expo.dev](https://expo.dev) (1)

#### [Node and express backend setup](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/node-and-express-backend-setup?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/node-and-express-backend-setup?u=76281980&t=0)** - [Instructor] Now let's take a look at the backend we'll use for our [React Native](../../Skills/Mobile%20Development/React%20Native.md) application. For mobile applications, you have multiple choices you can use for backend. If you prefer using your own, you can quickly get started with one such as the Node Express API we'll leverage from another one of my courses. Or you can leverage cloud options such as [Google](../../Glossary/Service/Google.md)'s [Firebase](../../Skills/Web%20Development/Firebase.md). In this case, I have one prebuilt for you and let's set it up. So the first thing you need to do is have [MongoDB](../../Skills/Software%20Development/MongoDB.md) install, the Community Edition. So the way to get to that, you go to [mongodb.com/try/download/community](https://mongodb.com/try/download/community). And when you get to that page, what you need to do is go down here and follow the instructions. There's a simple install if you have Homebrew where you do brew install mongodb-atlas and then atlas setup. Or you can go into the further instructions and download it from here, there's more options. So just follow the instructions to install it on your system. Once MongoDB is up and running, then you can go to the next step. So pause the video if you don't have MongoDB installed on your system and then unpause when you're ready. All right, so once you have MongoDB installed, what you need to do is go ahead and close this. I'm just going to minimize code here for a second. And then you go into the exercise files. And in the exercise files, what you have is resources here. In the resources, what you need to do is grab these files here and make sure that you are showing hidden files,
>
> **[1:34](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/node-and-express-backend-setup?u=76281980&t=94)** so you can see babelrc. And once you have that, you can copy all these files and then create your backend. So I'm going to create a new folder, call this backend, copy the files from here, close this, go into the backend, and then paste the files here, and then drag and drop the backend into VS Code to install it. So just as a quick overview, this is the backend that I've built for you. You have controllers in here, you have the model, you have the routes, and all the latest and greatest for babelrc. So let's go ahead and install this. So I'm going to bring the terminal, so control grab E or you can grab it from here like so. And the first thing you need to do is do a [npm](../../Skills/Web%20Development/npm.md) install or i. Once you have everything installed, what you could do is do npm start like so. And that's all you need to do. Your backend should be saying your server is running on port 3000. If you're seeing errors, if you're seeing something that is not connecting to database or something like that, that means that you haven't in installed MongoDB properly. So if you see that, go back to the instructions I just showed you and then restart your server with npm start. Once you see this, you should be able to go to local host 3000 and you should see this: Node and express server is running on port 3000. So if you see this,
>
> **[3:07](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/node-and-express-backend-setup?u=76281980&t=187)** that means everything is working properly. I will minimize this so this is running in the background while we continue working on our React Native application. So I'm just going to close this and bring the CRM back here. Perfect, so this is how you install the server or the backend that I included in this course. If you're running into any problems, read the console and it's going to tell you exactly what happened. If you're seeing the cannot connect to the database, make sure that you take a look at it. One of the things that you can do also if you have Postman, is open Postman, go to this particular URL here, so local host 3000 or this address and do a send as a command. You might not see anything right now because you haven't posted anything on the database, but if you see an empty array as a return data, then you're good. If you see errors, that means that your server needs to be installed properly or you need to restart the process that we've just done. One way or another, that's another way of checking if you have your server set up properly. All right, so let's move on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [MongoDB](../../Skills/Software%20Development/MongoDB.md) (7), [npm](../../Skills/Web%20Development/npm.md) (3), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (2), [Google](../../Glossary/Service/Google.md) (1), [Firebase](../../Skills/Web%20Development/Firebase.md) (1)
> **Prerequisites:** install (8), setup (1), set up (1)
> **CLI Commands:** npm (3), node (2), make (2), brew (1)
> **UI Navigation:** go to (4), drag and drop (1)
> **Tools:** postman (2), vs code (1), terminal (1)
> **Env Vars:** api (1), crm (1), url (1)
> **Ports:** port 3000 (2)
> **Exercise Files:** exercise files (2)


### 2. Redux and Main Views

[↑ Back to Table of Contents](#table-of-contents)

#### [Get started with Redux](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/get-started-with-redux?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/get-started-with-redux?u=76281980&t=0)** - [Instructor] Redux is a great tool to manage your state and we'll get started on setting it up. I'll make sure to cover the code we're introducing as we create it. But if you need a more in-depth introduction to Redux, do a search in our library for courses related to it. On the second note, I am not using the new approach to Redux due to some middleware that is already deprecated. And personally I prefer it this way, as it allows me to see the logic through and through. However, if you prefer using the new code or another state machine like MobX, feel free to follow along this course and then update the code to whichever framework you desire. Okay, so the first thing we're going to do is turn off the application here and install the dependencies that we need to continue. So the first thing I'm going to do is install Redux, then I'm going to install [React](../../Skills/Web%20Development/React.js.md) Redux, Redux thunk. And while we're in here, I'm going to also install lodash for later. So let's go ahead and install these libraries. Alright, so what I'm going to do before is make sure I maximize my screen, and I'm going to close the terminal while we're doing code. So the first thing we're going to need to do is open the App.js if you don't have this file already open, and we're going to create our store. So basically our store is where your state is stored. It's as simple as that. So, in Redux you have the reducers that takes any actions and reduces the data to what it needs to be next.
>
> **[1:35](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/get-started-with-redux?u=76281980&t=95)** So let's say for example, you have a state of alignment that is at center, and as you click on a specific button, it calls an action that says, for example, left align. And what it will do is go into the reducer and change your state where this particular element is going to be left-align as opposed to center. So then what we need to pass here is basically the value from the state that would be dynamically changed. So that's pretty much how Redux works. So let's go ahead and set it up. So the first thing I'm going to do is import provider from React Redux, and then I'm going to import two elements from Redux, the first one being Create Store, and then I'm going to also import Apply Malware like so. So one thing to note, Create Store says that it's going to be deprecated, it's okay, you can still use it, with the legacy approach to Redux. Again, if you want to update it, you go ahead and take a look at the updated code, but I like to use it this way. After that, what I'm going to do is import thunk from Redux Thunk. Perfect. So now what we need to do is wrap the provider on top of our containers here. So what I'm going to do is change this particular element here and remove it.
>
> **[3:10](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/get-started-with-redux?u=76281980&t=190)** Actually, we're not going to use the view 'cause we'll have other elements later on. And I'm going to wrap inside of these particular elements, the provider like so. And then we're going to pass the store that we'll create in the second right here. But right now, there's no value because we haven't created the store yet. So let me just go ahead and do that. So the provider actually provides the store inside of our application and wraps it right here. All right, so now that we have the provider here, we need to create the store. So what I'm going to do is create the store right here. So I'm going to create a variable called store, and I'm going to use Create Store like so. So what we're going to do eventually, we're going to create reducers that will pass right here. So what I'm going to do for now is place the reducers here and apply Middleware, and pass thunk to it like so. So now we have all this set up, but the problem is if we save this, our application's going to crash 'cause we don't have our reducers yet. So what I'm going to do is create a quick file that will have our reducers export them and then have them load here. So let's go ahead and do that. So I'm going to create a file right here. And inside of Source, I'm going to create a folder that I will call Reducers.
>
> **[4:47](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/get-started-with-redux?u=76281980&t=287)** And inside of the Reducers folder, I'm going to create People Reducer dot JS. And inside the people reducer, I'm going to create an export, which will be default, with state equals initial state. And then action. And then this will be basically returning our state, which will be exported as reducers. We'll do them later. And just save this as is and save this as is. There's a big chance that the application will still crash 'cause we still have to recreate a whole bunch of things to make our reducers work. But this is what we're going to do for now. And then import our reducers from reducers here. People reducer, like so. Now the next thing I want to do that we're going to start using inside of this course is a Component folder inside of the Source folder. So we're going to create that right away, and then create a source folder, not inside this guy.
>
> **[6:19](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/get-started-with-redux?u=76281980&t=379)** And what we're going to do now inside of the Source folder, we're going to create a folder called Images. And we're also going to drop the reducers inside of the source here, like so. So now inside of our source we have images and reducers, and we'll keep working on our application in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (2)
> **Prerequisites:** install (5), set up (1)
> **CLI Commands:** make (3)
> **UI Navigation:** open the (1), click on (1)
> **Definitions:** is a  (2)
> **Analogies:** for example (2)
> **File Paths:** app.js (1)
> **Cross-References:** in the next (1)

#### [React Native actions](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/react-native-actions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/react-native-actions?u=76281980&t=0)** - [Instructor] So let's continue working on the initial setup for Redux. The one thing that we need to do now is to make sure that we can get some data inside of our application, otherwise our application's not going to show anything and it will break. So right now, if you run the application, you're going to get errors like this. So, what we want to do is get to bare state where we have data inside of our application and we can load some people inside of our CRM. So what we're going to do first is go back to our reducers right here and start working on this. So the first thing we're going to need to do is create the initial state. So let's go ahead and do that: initialState. And our initial state will include an object with a bunch of stuff that we're going to add to it as we have actions and our reducers are reducing that state with a new state. And this is why I love the legacy way of doing Redux, because the logic is there, the actions tells you what's going to happen next, and it's very, very logical in its approach. So the first thing I'm going to need is a people array. And this is going to basically include all the people that we have inside of our database and then be able to list this into a people view. And therefore, I have also data for detailed view. Next, we need to have a few things that I want to keep in our state that we're going to use as specific actions are acted upon with the application. So one of them is detailView.
>
> **[1:35](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/react-native-actions?u=76281980&t=95)** So, whenever the detailView is true, it's going to show the detail view as opposed to the list view. Also, I want to have a personSelected state where we're going to have it at null for now. So whenever somebody is selected, then we're going to see this person in particular. And then we need a few things that we're going to use as we have a form. So, we're going to have a form inside of our application where we enter the data for adding new contacts inside of our application. This is where we're going to use the next several items to fill with that new information, send it to the actual database, persist the information there, and then basically clear the state with empty information. So, the first thing we need to do is: firstName, and let's go ahead and copy this, we're going to do a few of those, like this. Then, lastName, then we're going to need phone, we're going to need email, company, and you can add anything you want on there. This is basically the stuff that I'm going to use in the application, but if you want to hold anything else for a specific CRM, then you can add your own things in here. And we're also going to set it up when we do the actions, how we send this, and the form itself. So you can update it with new fields if you want
>
> **[3:11](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/react-native-actions?u=76281980&t=191)** inside of the form that we're going to use. We're going to need to hold an ID. So when we need to use that is basically, let's say for example, you're selecting a specific person, you need to hold, in the state, that information. And the last one is toUpdate. So, this is something that we're going to use with the UI. So when this is true, it's going to show a specific screen. All right, so this is our initial state. What we're going to do now is include the first reducer inside of our reducers. So what I'm going to do is create a switch statement, like so, an action type will passed to our switch, and then based on the action type, we're going to select something that is happening with our reducers, and therefore at that point, update the state with that specific data. So, in this case here, we have what we're going to call our INITIAL_FETCH, so when we [Fetch](../../Skills/Web%20Development/Fetch.md) our database. And what we're going to do with that is return whatever the state is and then add, in people, the action.payload. So this way, basically, when we click on a specific action that calls the INITIAL_FETCH, it's going to return the states, so wherever it is in the states. So, if we have data in any of these elements,
>
> **[4:45](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/react-native-actions?u=76281980&t=285)** it's going to return that state, but also update people here with what we fetched directly from the database. All right, so we have our first action. And in the switch statement, you always need to have a default, right? So, let's go ahead and do our default. And our default is going to be just return the state. Perfect, so we need to enter this because otherwise it's going to return an error when it runs through that switch statement if we don't have a default. Now the next thing we need to do is go inside of our src and create our actions. So inside of our src, I'm going to create a folder here that I'm going to call "actions." And inside of that folder, and you can split them if you want, and then make sure that your modular imports are done properly, but what I'm going to do is simply do an index file inside of the actions and use that as basically the file where I hold all the actions. So I'm going to do index.js, and then what we're going to do is export our first action. So, the first action is going to be called loadInitialContacts. And basically, actions are functions. So what we're doing when we call this particular action here, we're basically calling data, or in this case here, whatever other actions that we're going to put in there.
>
> **[6:19](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/react-native-actions?u=76281980&t=379)** So delete contact, update contacts, so on, so forth. And then what we're doing, the result of that action is going to update our state here in our reducers. So that basically is what's happening. So, we're going to write that function with this in mind. So, what we're doing here is return, whatever what's going to happen here, we're passing a dispatch. And basically dispatch is a function that we can use to dispatch specific elements. So what we're going to do here is first, we're going to fetch our database, so the database we set up earlier, and the URL is 'http://', and I'm going to use that address. You can use localhost as well, it works, but I like to input the specific address, port 3000, and it's contact, this is what we created. If you are curious about any of this, you can go into the backend right here, in the Routes, and then see that this is the route that returns a contact when we get something here. So, if you're curious, you can browse through all the code here. This is another course of mine where I built the database. All right, so let's continue. So once we fetch this, what we're going to do is do a .then, and we're going to use the response from that fetch to return the response.[JSON](../../Skills/Web%20Development/JSON.md).
>
> **[7:58](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/react-native-actions?u=76281980&t=478)** So basically what happens here is whatever is the response from the server, we're returning it to that function, and therefore we're going to be able to update our state with the data from the server. So once this is done, let's close this here, we're going to do another command. So, then, what we're going to do is dispatch the data that we just pulled from the database and select our reducer. So the reducer that we just created is INITIAL_FETCH, right? And what we're going to do is, to that INITIAL_FETCH, we're going to pass, as the payload, the data, like so, as simple as that. So basically the data is going to be now loaded into our state as the payload. And if you go back to the reducer, basically the reducer is going to update our state with the payload from our fetch here. And then the only thing I want to do is catch an error. If there's anything that goes wrong, at least we see it in the console, and pass the error just to make sure we understand what's happening. Okay, perfect. So now what we've done is basically created the first action here that will fetch the data from the server and then inside of our reducers,
>
> **[9:33](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/react-native-actions?u=76281980&t=573)** pass that data into our state, and therefore return our state for the data. So at that point, we'll be able to use that data to create the other views and not have any errors. All right, so now that we've created our initial actions and reducers, let's move on to the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (6), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Code Identifiers:** detailview (2), initialstate (1), personselected (1), firstname (1), lastname (1)
> **Env Vars:** initial_fetch (4), crm (2), url (1)
> **CLI Commands:** make (3)
> **Cross-References:** go back to (2), next video (1)
> **Definitions:** is a  (2), we call this (1)
> **File Paths:** index.js (1), response.json (1)
> **Prerequisites:** setup (1), set up (1)

#### [Create tab menu](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-tab-menu?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-tab-menu?u=76281980&t=0)** - [Instructor] Okay, so now what we're going to do is create the tab navigation at the bottom of our application. Again, if you run the application right now, it's still going to be a fee because we're not using anything to load the information that we stored in our state. So what we're going to do is actually work on the tab at the bottom and then create the people list view on top and start loading things back then. So up until we actually finish these things, you're still going to have errors. So let's go ahead and get started. So the first thing I'm going to do is go inside of the terminal here, and I'm going to clear that up here and then do an [npm](../../Skills/Web%20Development/npm.md) install. We need a few things to continue working with the views and have icons and things like that. So we're going to use a few of those libraries. So the first thing I want to do is install [React Native](../../Skills/Mobile%20Development/React%20Native.md) material forward slash core. Then I want to again do [react](../../Skills/Web%20Development/React.js.md) navigation, and this is the navigational stuff that we're going to use momentarily. I'm going to use the material bottom tabs to maintain the look of the application. And then again, react navigation native, we need that.
>
> **[1:31](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-tab-menu?u=76281980&t=91)** Then there's a few libraries that we need. React native paper, react native safe area context,
>
> **[1:48](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-tab-menu?u=76281980&t=108)** react native screens, and finally react native vector icons. So we should be good here. If you made a typo, just take a look at the messages in the console log. It's going to give you a good idea of what happened, if anything. Okay, so now we're going to go in the app.js. I'm just going to close this for a second and we're going to start working on our navigation right here. What I want to do is inside of our provider, I want to have the navigation right here. So I'm going to remove this altogether and the next time we actually run the application, we have data in it. It's going to pass that data and basically have the tabs at the bottom. So what I'm going to do is import the navigation tabs that I just installed. Let me just close this for having a little bit more estate for now. Okay, so what I'm going to do is import navigation container from react navigation native. And I'm also going to import a component that I'm going to create momentarily. So right now it's going to tell you guys that there's an error, but I'm going to create this navigation component momentarily. So it's going to be from source, from components, navigation like so. And then what we're going to do right here inside
>
> **[3:25](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-tab-menu?u=76281980&t=205)** of our provider, we're going to have the navigation container. And inside the navigation container, we're going to have the component that will create shortly like so. With all this, you should be good for this particular area. So now we need to create this new component. So now in our components folder, we're going to create the navigation component. So let's go ahead and call this navigation.js, X out. And then we're going to create our component that will be our navigation tool right here. So let's go ahead and do that. So we need to import a few things. So let's go and create that screen. Create material bottom tab navigator. So that's the first one that we need. And let me just close this again. We're also going to need an icon from react native vector icons, and we're going to go specifically to evil icons. We're going to use that shortly. Now we're going to import other components that we don't have yet. So we'll create a quick component in the next video to make sure that we have these components when we load our application and it's not running into a problem, but what we're going to do is create a component
>
> **[4:58](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-tab-menu?u=76281980&t=298)** that we're going to call people list from, and it's going to be inside of the component here. People list, don't have that yet, so it's going to scream if you start the application. We're also going to import company list from company list. And finally we're going to import add person from add person, like so.
>
> **[5:32](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-tab-menu?u=76281980&t=332)** So when we have all these components loaded, we can start creating our navigation. Let's go ahead and do that. So I'm going to create a tab that will have create material bottom tab like so. And then we're going to create our navigation component. And if you like the shorter way of doing things so for example, if you want to export this component directly from here, you can do that. Some people actually do that, but I tend to do the opposite. Again, this is just a personal style. If you have your own way of working, it's going to be the same at the end of the day. And then I would do something like export default navigation like so, and then I can start working on my component here, like so. Okay, so the first thing we're going to do is create a tab dot navigator, and inside of that tab navigator, we're going to have our screens. So just for the sake of time, I'm going to continue this particular code in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (6), [React](../../Skills/Web%20Development/React.js.md) (3), [npm](../../Skills/Web%20Development/npm.md) (1)
> **File Paths:** app.js (1), navigation.js (1)
> **CLI Commands:** npm (1), make (1)
> **Cross-References:** in the next (2)
> **Prerequisites:** install (2)
> **Env Vars:** npm (1)
> **Tools:** terminal (1)
> **Analogies:** for example (1)

#### [Tab navigation](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/tab-navigation?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/tab-navigation?u=76281980&t=0)** - [Instructor] Let's continue doing the tab navigation here. So the first thing I'm going to do is add some props for the tab navigator. So if you don't know, tab navigator comes with a lot of options and basically props that you can use. For example, the one that I'm going to use first is what is the initial route? So when the application loads, what is the route that will be loaded on the screen from the tab? So it's going to be people. Another prop is active color, and that's the color that you want of the tab itself. So this is a color that I'm using throughout this application. Feel free to select whatever color you want. Then the bar style basically allows us to add more styles to the bar itself. One of them is the background color. So just as a note, this is the active color, so basically the color when you select a particular tab. The background color is the color of the tab itself. So we're going to go for this color here. Okay, so these are the options for the tab navigator. So the tab navigator comes with basically a component that you can use, which is the tab screen, and this basically allows you to add the different tabs inside of the tab navigator. So let's go ahead and do that. And the tab screen also has some options. So what we're going to do first for the tab screen is what is the name of that tab? So the first one is people, and what is the component you're going to load
>
> **[1:35](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/tab-navigation?u=76281980&t=95)** by this particular tab? And in this case, it's PeopleList. And then if we have any options for a tab, what you do is you can go ahead and use one of the props that's called options. And for those, you have several things you can do. So for example, what is the tab bar label? So that's the actual label. Even though we're loading the PeopleList component and the screen name is people, we still need to put a label here. The other one that I'm going to do is use an icon. And this is why we imported this icon here. So what we're going to do is basically go ahead and do a function that will return an icon for this particular tab like so. And inside of that bar, we're going to load icon name user, and we're going to put a size for it at 30 and then a color. And basically the color will be the color that will be passed through the icon like so. Okay, so now we have our first tab screen. What we're going to do to speed this up, we're going to basically copy and paste the entire code that we just written and we'll create the other tabs the same way. So the second one is add, which uses the AddPerson component. And then for the tar label, it's going to be add.
>
> **[3:15](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/tab-navigation?u=76281980&t=195)** And we're going to be using the plus icon from the library we loaded, same size, same color. And again, we're going to do this one more time for the CompanyList. And this is going to be company, which will use the CompanyList component. For this, we're going to use the label company. And for the actual icon itself, it's going to be the archive icon. And again, if you want to have more details, just go and research [React Native](../../Skills/Mobile%20Development/React%20Native.md) vector icons, and then you can take a look at all the lists of the icons that are possible with this particular library. And then there are sublibraries that you can load like what we just done here, so EvilIcons, and then use whatever icon that are part of that library. Then the next thing we need to do, we save this. I'm going to add a line here. And then what we'll do is go in our list of components and add the People component, the CompanyList component and the AddPeople component. Right here, we're just going to create a temporary component here for each one of them so we can load our components whenever we click a tab, especially the first one, which is PeopleList. So this one we're going to import [React](../../Skills/Web%20Development/React.js.md) component from react, and then we're going to import also View, StyleSheet,
>
> **[4:54](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/tab-navigation?u=76281980&t=294)** and Flatlist from react-native. Okay, so then what we're going to do is create a class component, which we'll call PeopleList extends Component, then create a render return, and then just create a simple view, Hello. Export default PeopleList. Okay, so what we're going to do very quickly just to have a base component that we can load for each in the navigation, we're going to copy all this very quickly, create components. We're going to create the AddPerson.js, and we're going to create the CompanyList.js. And for each one of them, we're going to paste this, quickly switch the PeopleList to this one is Company List, save it and do the same for here for AddPerson. I'm going to change the PeopleList to AddPerson. So we have unique components for each one of them. And we can start the application and run it. And we're going to do this in the next video.
>
> **[6:26](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/tab-navigation?u=76281980&t=386)** So let's go ahead and move on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (3), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1)
> **File Paths:** addperson.js (1), companylist.js (1)
> **Analogies:** for example (2)
> **CLI Commands:** tar (1)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Create the people list view](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-the-people-list-view?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-the-people-list-view?u=76281980&t=0)** - [Instructor] Okay so what we're going to work on next is to make sure that first of all, we can load the application and if there's any issues, then we fix 'em right away. The other thing is we're going to focus on the PeopleList component. So we're still going to break our application by adding more code, but as we move along, things are going to start moving into place. So let's go ahead and start the application first, see if there's any issues at the forefront. And I'm going to do [npm](../../Skills/Web%20Development/npm.md) start. Select iOS. And we have some issues here. So texturing must be rendered in text. Okay, so something is wrong here. At the top here, located in PeopleList. So let's take a look at that. So what we're going to do first, let's go into PeopleList. Yeah, okay. So what we're going to do here is just switch this to a text. There you go. And make sure it's also loaded here, like so. Good. Perfect. And we're going to copy and paste these in each one of the list that we've created here. Perfect. And AddPerson. Okay. First thing, we're good to go. So what I'm going to do now is just load the PeopleList
>
> **[1:34](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-the-people-list-view?u=76281980&t=94)** and do the full application here. So we may actually get rid of the text there. So the first thing we're going to do is import a few things. So I'm going to go ahead and undo the text thing here 'cause we're going to change that anyways. It's just for the others not to get any problems after that. Okay, so we're going to import, connect from [React](../../Skills/Web%20Development/React.js.md) Redux. We're going to also import PeopleItem, which we'll work on shortly. So basically if you want to see the architecture of the application, we're going from PeopleList to PeopleItem, and then we're going to have detailView, which we're going to show a lot more information about that person. So this is the way we're basically going to build it from now on. So PeopleItem. And what we should do, is quickly add a component, call it PeopleItem, like so, .js, copy and paste from CompanyList. Go in PeopleItem, boom. Quickly switch these two to PeopleItem, like so. So we have something right here. Then we're going to do import, PeopleDetail. So the detailed view from PeopleDetail, and we're going to do the exact same thing
>
> **[3:06](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-the-people-list-view?u=76281980&t=186)** that we just did, very quickly. So we have something here. Boom. PeopleDetail.js, like so. Paste, select here, select here. PeopleDetail. We're building basically our architecture of our applications right now by having simple components and then we'll change the whole thing as we work along. Okay, so we've done that here. Now I'm going to come back here, so now I can actually load this and then import loadInitialContacts,
>
> **[3:48](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-the-people-list-view?u=76281980&t=228)** from actions. And while I'm thinking about it, we need to make a few changes in our directory. And I'm going to do this directly inside of the app. So I'm going to go right here, like so. And what I'm going to do is make sure that my components are inside source, here, and I want to make sure that my actions are in source, so we're good with that. Images should be in source too, and then the reducers. So we're all good. And if we start the application, we may have some issues where it's loaded, but as far as I'm concerned, I'm pretty sure we have all this well done. If not, then it's going to show us very quickly. Let's make sure that we are loading it from here. PeopleList, CompanyList, Navigation. We're all good. Okay. All right, let's continue working on this one. Now what we need to do is create the style sheet, so we're going to go ahead and do that. Styles equals StyleSheet.create. So we're creating a style sheet for this particular page. And then we're going to add styles for the container. And it's going to be paddingTop. I'm going to put 80 pixels.
>
> **[5:22](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-the-people-list-view?u=76281980&t=322)** Okay, so this is done. Now I'm going to go inside of my class here and I'm going to create first, a component. And basically when the component has mounted, we want to load our initial contacts. And then what we're going to do, inside of our render here, we're going to render a flat list. And inside of a flat list, we're going to have a few things. So a flat list component from [React Native](../../Skills/Mobile%20Development/React%20Native.md), basically takes a few things. The first thing it takes as a prop is what is the data that we're actually loading? And we're going to do some Redux right after that, towards the end of that file, because what we need to do is connect Redux or the store to this component, so we actually have access to the props.people, inside of our state. So we're going to do that. This is what we have to set up, so it loads the props.people. Then we're going to render an item and for each item in the array, so basically people is an array, and the Flatlist is a flat list of elements. We need to have for every single elements that we're going to have inside of the flat list, we're going to render one item. So this is what it takes as a prop. So item,
>
> **[6:56](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-the-people-list-view?u=76281980&t=416)** and this item is PeopleItem. And it takes a prop that will be PeopleItem. So basically every single item inside of our array is going to return this PeopleItem component, which we quickly created. Just forgot this here. And it needs to also have a key. So we need to extract from the list what exactly is the key. So for every item in a list, there needs to be a key. So what is going to be that key? So that's basically what we're setting up right now. And we're going to use the index of that item to string, like so. Simple operation to render a key for every item here. Okay. So now we need to map the store to this particular component. We need to map state to props, takes a state, and then it will return the people from the state.people and detailView. This is something we're going to use later because we need to understand when we click on a specific action, it's going to put detailView as true,
>
> **[8:30](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-the-people-list-view?u=76281980&t=510)** and if it's true, then show the detailed view as opposed to the flat list on the screen. And this is what we're mapping to it, like so. Okay. So basically what this does here is for the people props and the detailView prop, we're mapping the state.people and the state.detailView. So what we have in the store needs to be mapped. Now in order to connect all this, we need to run connect from the Redux library and have map state to props. And it's going to take it from loadInitialContacts, right? And then we export PeopleList. So these are all the arguments that this function connect takes. So with this, we should be able to connect the store to this specific view here. Perfect. So now that we've done the PeopleList component, we need to move on to the next one on the list. Let's move on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [npm](../../Skills/Web%20Development/npm.md) (1), [React](../../Skills/Web%20Development/React.js.md) (1), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1)
> **Code Identifiers:** detailview (5), loadinitialcontacts (2), ios (1), paddingtop (1)
> **CLI Commands:** make (6), npm (1)
> **Definitions:** is an  (1), is a  (1)
> **File Paths:** peopledetail.js (1)
> **UI Navigation:** click on (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Code the people item view](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-people-item-view?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-people-item-view?u=76281980&t=0)** - [Instructor] Okay, so now we'll focus on the PeopleItem view. So when we run the list, we're going to have PeopleItem. And inside of the PeopleItem, we're going to have the component that will be repeated multiple times. So let's go ahead and create that. So what I'm going to do is import a few things first. So in the [React Native](../../Skills/Mobile%20Development/React%20Native.md) view, I'm going to import Text, keep View, keep StyleSheet, also we'll remove Flatlist, replace it with Image, and TouchableWithoutFeedback. So let's go ahead and find that one. Perfect. So once we have those, let me just close this, we should be good to go. And now we're ready to import other things. We're also going to Connect redux in here and we're going to import again icons that we'll select from [react](../../Skills/Web%20Development/React.js.md)-native-vector-icons/EvilIcons.
>
> **[1:04](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-people-item-view?u=76281980&t=64)** And then we're going to import all of our actions as actions from actions. Perfect. Okay, we're going to create a style sheet, StyleSheet.create. And we're going to create a few here. So the first one, I want to create a title that will have a few things here. So we're going to have a top that will have minus 60 pixels, a left that will have 100 pixels. And again, I did all these in my initial project a while ago, so I'm using pretty much verbatim most of it from the initial project. Then what we're going to do, we're also going to create styles for a class that's called Image in which we'll have a height of 100. Then we're going to go and have also an action class. And inside that action class, we'll have a top of minus 30. We'll have a background color that will be black color, that will be white. And font size, that will be at 24. And once we start the application, you're going to see all these styles in action. If there's anything you want to change,
>
> **[2:37](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-people-item-view?u=76281980&t=157)** play with it as you start the application. Then the icon is going to have a few styles too. Position absolute. Top 15. Left zero. Color white. Background color. And we're going to go with something very specific on this one, RGBA 255, 255, 255, 0.
>
> **[3:14](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-people-item-view?u=76281980&t=194)** Just to show you options that we have when we create style sheets. Okay, so that's our style sheet. Now what we need to do is recreate a little bit of this. We're not going to do a class, we're going to do a functional component here. So we're going to do this instead, PeopleItem, we're passing props and we're going to return. The first component we're going to use is the TouchableWithoutFeedback, which will have an onPress function that will run props.selectPerson. So we'll create that action once we go back to redux and extend on the actions that we already have. What we'll do is basically take the actual person of this item, so the idea of that item, and then it will return and push to the detail view of the information from that particular item or particular person in the list, so props.people. Okay, so then what we're going to do, we're going to create a view, and inside of the view, we're going to have a few things. We're going to have first the image component, then we're going to have an icon component, and we're going to put some details in here in a second. And then we're going to have two text component,
>
> **[4:49](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-people-item-view?u=76281980&t=289)** one here and one there like so. Perfect, so now we're going to need to put a few things. So for the image itself, the image takes two props that I need. First of all, it takes a source. And what is the source? It requires from my images background.jpg.
>
> **[5:16](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-people-item-view?u=76281980&t=316)** And we're going to drop these images shortly inside of our images folder. The second prop it takes is any style. So yes, in this case, we have some styles that are applied to the image, styles.image. Perfect. Now the icon, we need a name, right? And the name is going to be user. Then we're going to have a size of 100. And then some styles, and they're going to come from styles.icon. And finally, for the text, we're going to apply first of all styles here, which will be taking styles.title, and then style styles.action. And basically, the title will be props.people.firstName.
>
> **[6:22](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-people-item-view?u=76281980&t=382)** Again, we're going to do the exact same thing, props.people.LastName. And then in the text here, which company comes from, so people.company. And finally, we need to do what we've done previously, making sure that we connect our component through redux. Okay, so let's save this. Just one thing that I realized here. A little typo. Perfect. Save that. Perfect. Now that we've done the PeopleItem component, we're going to move to other components that are left in our list. Let's move on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1), [React](../../Skills/Web%20Development/React.js.md) (1)
> **Code Identifiers:** onpress (1), selectperson (1), firstname (1)
> **CLI Commands:** find (1)
> **Env Vars:** rgba (1)
> **Cross-References:** go back to (1)
> **Speakers:** - [instructor] (1)

#### [Add new components](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/add-new-components?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/add-new-components?u=76281980&t=0)** - [Instructor] We're going to complete the remaining components needed on this video, plus add all the images required from the exercise files. Right now your application in the simulator is giving you errors and this is normal. We haven't quite finished integrating most of the elements needed to make the application work properly. So bear with me as we continue on our journey. So let's get to it. Okay, so the first thing that we're going to do is add the remaining components and add the images in the image folder. So what I'm going to do is minimize first VS Code here. And as you can see, there's errors and this is completely normal. So just for the sake of not having any errors, let's just close the application for now. So I'm going to go in the exercise files. And in the exercise files you have a folder called Images. So grab all the images here. And the only thing you don't need in the image folder is the PDF file and the afdesign here, which are basically the original design files that I've used to create these. And I'm going to copy them and then go inside of the CRM here. And in the Sources folder, you have the Images folder, and I'm going to drop the images right here, like so. Perfect. So we got everything we need. We can go back and grab our application here, and we're going to add more components. So the first one that I want to add is the Companyitem.js. And let's go ahead and grab code from the AddPerson. And while I'm here, I'm seeing something here.
>
> **[1:36](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/add-new-components?u=76281980&t=96)** This shouldn't be asking for component with a a low cap. And let me just check to make sure that I didn't make that mistake somewhere else. I did, so let's remove that just to remove that error. Navigation doesn't seem to have it. Component here in the PeopleDetail. Make sure you save your file as you go along. Let's remove that here. And finally on PeopleList, remove that. This is a typo. I probably was writing down component, and then all of a sudden I wrote it again and it auto completed for me. So, okay, so now that I have this, let's grab the code from AddPerson, copy. Go inside of CompanyItem, paste it, change AddPerson to CompanyItem, like so.
>
> **[2:35](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/add-new-components?u=76281980&t=155)** And make sure we also just create a View instead of text while we're here. And just remove this so we don't run into any errors. And I'm going to copy that, paste it in the other component that I created just to fix that little problem that might be an issue. And here we're good. Yeah, let's paste it here as well. And quickly check over your components just to make sure you don't have any of this. View, we're good. Here we're good. Here we're good. Okay, perfect. So now I'm going to continue. Copy all this. Close the others. Create a new component here. And this one I'm going to call UpdatePerson.js. Paste this. Update here. So UpdatePerson. Perfect, we're all good. And then finally, do the exact same thing for a component that we're going to call DetailView, which will work in detail in the next chapter 'cause there's a lot of code in this specific component here. So I'm going to do this. Again update DetailView, like so. And there you go.
>
> **[4:08](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/add-new-components?u=76281980&t=248)** One more thing I want to do before we leave this video. I want to complete the code for PeopleList. This is one that I really want to complete now because we have more things that we need in here. So the way I'm going to do this is first I'm going to create a method that I'm going to call renderInitialView. And basically this function or method as part of our class, what it's going to do is, when the state returns in DetailView true, we're going to basically run the PeopleDetail view here. And if it's off, we're going to return the Flatlist. So that will allow us to click on a specific item inside of our list, and if it's clicked, then it's going to update our state with DetailView equals True. And then it's going to render the PeopleView as opposed to the Flatlist. So let's go ahead and do this. If (this.props.detailView === true)
>
> **[5:20](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/add-new-components?u=76281980&t=320)** then return <PeopleDetail />, like so. So we want to make sure that we have PeopleDetail imported here, so we're good. Perfect, else { return, and then we're going to return the Flatlist here. So I'm going to highlight line 33 to 37, cut it, return, paste it here, like so. Perfect. And then, in the render method, basically what we're going to do is first have a view and we're going to add styles, and basically those styles from container. And if you remember, we did those here, earlier. Perfect. And then we're going to use the method that we just created, so renderInitialView(), like so. So basically this function will be returned inside of the PeopleList. So if DetailView is false, it's going to return the Flatlist of people. If it's true, then we're going to have the PeopleDetail. So this is basically what I wanted to do in this video, so let's move on.

> [!info]- Semantic Content
>
> **CLI Commands:** make (7)
> **Code Identifiers:** renderinitialview (2), detailview (1)
> **Exercise Files:** exercise files (3)
> **File Paths:** companyitem.js (1), updateperson.js (1)
> **Env Vars:** pdf (1), crm (1)
> **Cross-References:** in the next (1)
> **Tools:** vs code (1)
> **UI Navigation:** click on (1)


### 3. Complex Component

[↑ Back to Table of Contents](#table-of-contents)

#### [Setting up Redux for a detail view](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/setting-up-redux-for-a-detail-view?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/setting-up-redux-for-a-detail-view?u=76281980&t=0)** - [Instructor] In this chapter, we're going to start working on a detailed and complex component such as the detail view. The first thing we're going to do is set up redux for the detail view, and then we're going to keep working on the detail view itself, the component, until we're done with it. So let's go ahead and get started. So the first thing we're going to do is go back to our actions here. And we're going to add a few actions. 'cause right now, all we have is just the load initial contact, so we need two more actions specific to the detail view. So one of them is called select person. And this one basically will take a people ID. And then return inside a redux a type, which we'll call selected person. And we'll need the select ID inside of our actions, which is basically the people. And I just made a mistake here, just a little typo that could screw us in the future, like so. And I want to make sure that this is a function, perfect. Alright, so basically if we click, and we select the action selected person, and we have the people idea, we're going to be able to return inside of our state a person as selected, and we're going to have the people idea with that.
>
> **[1:35](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/setting-up-redux-for-a-detail-view?u=76281980&t=95)** Now the next one that we need to work on is called non-selected. Which basically means that we don't have anything selected. And this will return inside of our state. Type, none, selected. As such. Perfect. So these are the actions and then we need to work on a reducer. So let's go ahead and bring the reducer here. So, when the action of a selected person is clicked, we're going to pass it inside of our reducer here. And what I'm going to do, actually, I'm going to do it after the initial [Fetch](../../Skills/Web%20Development/Fetch.md). We're going to have a case. Which will be selected person, remember we just created that action, so if the action is selected person, we're going to return our state, our state. And, in the detail view, we're going to put it as true. So remember, in the previous video we did in the component here, in people list. So in the people list here, if the actual state says that detail view is true, then return the people detail, right? This component. Otherwise return the flat list. Well this is where we're going to do this.
>
> **[3:08](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/setting-up-redux-for-a-detail-view?u=76281980&t=188)** So if the action is the selected person, we're going to pass the action as selected person, and we're going to have a people ID inside of this particular action. And now we're passing this inside of our reducer. So, the detail view's going to be as true. And then person selected, we're going to have the action with the select idea. So, this here. So basically this will update the state with a people ID here inside of our key here, select ID. With that, with the people detail here, we're going to pass it in people detail and it's going to grab whatever is in the state as the selected person selected here. And therefore, we'll have all the details inside of the detail view. More on this as we work on the detail view. So, we're going to finish this here. So, we have also another case, if you remember correctly, that other case is non-selected, right? And what happens when we have that? We return. Again, the state. Detail view is now going to be false. And the person selected will be no, so nobody.
>
> **[4:49](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/setting-up-redux-for-a-detail-view?u=76281980&t=289)** Otherwise, we have the default case here. Alright, so we can save this. What I'm going to do next is work on the detail view a little bit, because we have a lot of code in the detail view, so I'm going to close these here. And I'm going to open the detail view. So, for the detail view, first and foremost, we have to load a few things here in [React Native](../../Skills/Mobile%20Development/React%20Native.md), we have style sheet, which is good, we have, instead of flat list, we're going to have the scroll view. We're going to need text, we're going to need image, and we're going to need touchable opacity. Like so. We're also going to need a few things, so let's go ahead and import everything that we need, and then we'll move on to the next video with the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the code, 'cause we have a lot of code in this particular view. It's a complex one, this is where we have all the details of the person, or the contact, so, there's a lot of things that we need to add here, so, [react](../../Skills/Web%20Development/React.js.md) redux. We need also to import evil icon. From react native vector icons. And then we're going to select evil icons. We also need material icon. From, again, the same guy is here. So let's copy and paste this.
>
> **[6:28](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/setting-up-redux-for-a-detail-view?u=76281980&t=388)** And instead of evil icons, it's going to be material icons. Next we're going to also import simple icon, there's a lot of icons that I'm using in this particular component. And simple line icons. And finally, we're going to import all our actions, which we're going to use at some point here. There you go. And while we're here, the last thing that I'm going to create just before we move on to the next video is the styles, 'cause we're going to work on the styles here. Style sheet, dot create. And we're just going to create the first one, so there's something in here, otherwise it's going to return an error, even though our application, it's still not going to work for a little bit. But I'd like to just add something here, so, title one's going to be top minus 75. Left 100, and font size is going to be 24, like so.
>
> **[7:41](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/setting-up-redux-for-a-detail-view?u=76281980&t=461)** Okay, and then we'll continue working on this in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (2), [Fetch](../../Skills/Web%20Development/Fetch.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [React](../../Skills/Web%20Development/React.js.md) (1)
> **Cross-References:** next video (2), go back to (1), previous video (1), in the next (1)
> **Definitions:** is called (2), is a  (1), basically means (1)
> **UI Navigation:** select the (1), open the (1)
> **CLI Commands:** make (1)
> **Analogies:** such as (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [CSS details](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/css-details?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/css-details?u=76281980&t=0)** - [Instructor] So let's continue working on the detailed view and we're going to continue the CSS in this video. So what I'm going to do is keep working on the CSS and basically all these styles are going to be applied once we start working on our scroll view and Touchables and so on, so forth. So let's go ahead and we're going to get started with title2 and most of these are going to be very similar. So let's go ahead and copy and paste. And the only thing is going to be different is the top. Okay, let's keep going. Next one is going to be an image and the height of the image is going to be 100. And then we're going to have background color that will be transparent. The next one is going to be closeIcon and the position is going to be absolute. Next one is going to be top 10, right 5. And again, these are all positional elements and also colors. So if there's anything that you want to change from what I'm doing here, please feel free to do so. These are styles, so these are not going to have a huge impact on the application except the actual look. So let's go ahead and continue. The icon is going to be position absolute like so. And then top 15
>
> **[1:37](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/css-details?u=76281980&t=97)** left 0, color is going to be white. Background color is going to be an RGBA value, which takes four parameters, 255, 255, 255 and 0. And the reason why I'm doing this, I could have just simply done the actual name of the color, I wanted to show you a different alternative for the colors. Okay, the next one is the text area. And the text area will have a flex direction of row. Then we're going to have padding left 20, padding top, we're going to have 5. And the reason why I said this is because the next one has a 10. So what I'm going to do is copy this very similar code and I'm going to call this final text. Flex direction is row padding left 20 and padding top is going to be 10 instead of five. Text icons plural, by the way, it's going to have a specific color of #26a. Again, I'm showing you another option for colors. This is the X value.
>
> **[3:11](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/css-details?u=76281980&t=191)** Then we're going to have action area. So basically just to understand what's happening here, we're creating a style sheet with this function here Create. And this style sheet is going to be applied whenever we add the styles inside of our views. In the next following videos, we're going to work on adding the components or the views inside of that component and add the actual styles to it. So there's a lot of styles that we need to add because this is a big component with a lot of stuff in it. So action area, padding top takes 10, flex direction is row as well. Justify content will be space around. And once you see how this component looks, you can come in and change the styles if there's anything that you don't like. But these are styles that I chose based on how it feels when you open up the application. And again, you can choose your own edit icon has its own styles, basically just color #26, a6e4. Sections, we have a few things. So what I'm going to do is copy from this one here
>
> **[4:47](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/css-details?u=76281980&t=287)** 'cause there's a lot of stuff that's coming from the same. And then paste it. So this is the one that I've copied here. Text area and pasted here. So sections, the flex direction is row, the padding left is 10, the padding top is 10 as well. And then we only have to add a width of 100. We have a few things left and then we're done for the CSS and the style sheet. The delete icon's going to have a color of, again, we're using a hex value. So #e9a 69a Now I'm going to paste again the way I've done for the sections. Make sure we add the class first, edit delete area, and then paste. So the first one is the same. Padding left, we don't need it. Padding bottom is going to be 10. And then I need justify content is space around. Align item is center. Background color, we're going to go for an RGBA value again, rgba.
>
> **[6:25](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/css-details?u=76281980&t=385)** And this one is 211, 211, 211, and 0.3 for the alpha. Then margin bottom 10, margin top, 20. And we have one last class to create action image width of 100 and height of 100 as well. Perfect. So now we have all of our styles created. We're ready to get rolling with the actual component itself. Let's save this and move on to the next video.

> [!info]- Semantic Content
>
> **Env Vars:** css (3), rgba (2)
> **Cross-References:** in the next (1), next video (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** closeicon (1)
> **Versions:** 0.3 (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Coding the scroll on the detail view](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/coding-the-scroll-on-the-detail-view?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/coding-the-scroll-on-the-detail-view?u=76281980&t=0)** - [Instructor] Okay, so now we'll continue working on the detail view. One of the things that we're going to do first is add an action here. So basically this particular action update test will allow us to update the contact whenever we want to. So basically when we click on any of the contacts, we are going to go into the screen where we can update the view itself. So let's go ahead update contact this.props.person. So we're passing the actual person in here. So we're going to create this update contact action when we get to the actual CRUD section in the next chapter. So what we're going to do is pass this particular person into that action or function and then go to the screen where we update the contact with all the information from that particular person. Okay, so now we're going to go inside of our view here. So I'm going to use the view here and get started on creating our full view. The first thing I'm going to do here is pass a style. So we have a style that's called container, and we're going to use that. And then inside of that style is where we're going to have our scroll view. And what I want to also add in here as a prop is show vertical scroll indicator as false. So basically we don't want to have the scroll thingy
>
> **[1:38](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/coding-the-scroll-on-the-detail-view?u=76281980&t=98)** on the side of that particular scroll view. The first thing we're going to do after that is add an image. And that image needs a source, right? And that source requires a particular image. And that image is one of the images that we put inside of the image folder. And this one is the background. And then we're going to add style to that image. And it's going to come from styles.image, which was the one that we created in the last video. Then this is where we're going to start using the icons. So this one is EvilIcon, and it has a name as one of the props that it takes. And we're going to pass user, it needs a size too. So for the size, we're going to do 100. And finally we're going to pass styles. And we're going to use Styles.Icon like so. What I'm going to do next is basically copy this and paste it 'cause we're going to have another icon, but this time the icon's going to be simple icon, and the name of that icon is close. We're going to use it as a 30 for the style and do close icon for the styles. Again, if you want to know what we've done for that one,
>
> **[3:11](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/coding-the-scroll-on-the-detail-view?u=76281980&t=191)** you can check. Close Icon is here, and these are the styles for that particular icon. So that's what we worked on on the last video, and now we're going to add all these components and implement all the styles that we have for each one of them. Okay, oh, one thing that we forgot to put in here is onPress, something's going to happen. And onPress we're going to use one of the actions. And that action is this.prop.noneSelected which we created. So this way when we press this close icon, it's going to send an action to Redux basically saying that, noneSelected, and this is how we're going to change the view from detail view to the list. Okay, so then we're going to do a text component, which will have a style. And this one is going to be styles.title1. And what we're going to pass here is this.props.person.firstName. And also we're going to repeat this. So if you want, you can quickly copy, make sure you have a space, and do the last name here. So first name, last name is going to be here like so, okay? Perfect, now we can copy this if we want,
>
> **[4:49](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/coding-the-scroll-on-the-detail-view?u=76281980&t=289)** or you can simply type what is going to happen next. And for the style, we're going to use style.title2.
>
> **[5:02](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/coding-the-scroll-on-the-detail-view?u=76281980&t=302)** Instead of doing the name, we're going to do this.props.person.company, like so. Okay, now we're going to do another view right below here that will apply styles to it. And this time it's going to be styles.text area. And what we're going to do next is basically add a whole bunch of things that are all going to be the same. So we can redo this section, copy and paste, and just change the name or something else. So we're going to do the phone first. So let's go ahead and do that. So first we're going to have material icon, and if you want again to copy any of these lines, and paste it here like so. And then the name is going to be phone. The size is going to be 40. And then change this to text icons, like so. Then what we're going to do is do a text, so you can copy this one if you want. Paste it here. The style is going to be final text like so. And then this.props.person.phone is going to be what goes in here.
>
> **[6:36](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/coding-the-scroll-on-the-detail-view?u=76281980&t=396)** Now everything else is going to be very similar to this and not a whole lot of change. So let's go and copy this here and paste it. And then what we're going to change on this one is instead of phone, it's going to say email here, text icons, it's going to be the same, final text is going to be the same. And then all we change is email here. So you can see what we're going to do a couple of times here. It's going to be very simple, and it's going to go quickly. So we can paste again. And instead of email, we're going to do assignment. And here it's going to be project. So basically what we're doing is adding all the different elements that are part of our person. So if you look back, and let's go into the reducers. If you look back here, our initial state has all these things here. First name, last name, phone, email, company, project, notes. All of these are going to be displayed on the detail view. So this is where, and I'm going to close the explorer. This is where we're repeating a lot of these views with the different things here. So in case of this one, it's project, and icon is assignment. So we're going to do this again. And on this one we're going to use the mode edit as the icon. And for the actual prop itself, what we're going to do is notes like so.
>
> **[8:11](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/coding-the-scroll-on-the-detail-view?u=76281980&t=491)** Okay, so now after we're done with this, I would recommend you save, and we'll keep working on this particular component in the next video.

> [!info]- Semantic Content
>
> **Code Identifiers:** onpress (2), noneselected (2), firstname (1)
> **Cross-References:** in the next (2), in the last (1)
> **UI Navigation:** click on (1), go to (1)
> **CLI Commands:** make (1)
> **Env Vars:** crud (1)
> **Analogies:** similar to (1)
> **Prerequisites:** make sure you have (1)
> **Speakers:** - [instructor] (1)

#### [Finalize detail view](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/finalize-detail-view?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/finalize-detail-view?u=76281980&t=0)** - [Instructor] Okay, so let's continue working on the detail view. We still have a lot of code to write, so let's go ahead and get started. The next thing that we need to do is a view. So let's go ahead and create a view. And this view is going to have a style of styles dot edit, delete area. And inside of that view, basically this area is going to be where we can delete or update specific items. So what we need at this point is to use the touchable opacity button, and it's going to have the styles apply to it. Sections. And let's close that out. But inside of the touchable opacity, there are a few things that we need to do, and we're going to need to do an on press. And that on press is going to create the update test. This is where we're going to leverage the function that we just created above. So let's go ahead and do this, and we're going to return this dot update test. So basically when we touch that specific area, which is here, whatever is inside of this view is going to be touchable and will basically call this function here. So let's go ahead and do that. So what we're going to do is leverage against
>
> **[1:34](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/finalize-detail-view?u=76281980&t=94)** some of these things here. So the icon, and the text, and we're going to put it inside of the touchable opacity. So I'm going to do this and do this too. And the specific material icon that we're going to use is the auto renew. The size is going to be 40, and we're going to call the edit icon like so. And then for the text itself, we're going to do final text again. And instead of passing a value, we're going to type the name edit. And again, we're going to redo all this code all over again for the delete function. So copy all this, paste it all again, and the styles are going to be the same. And for this one, instead of using this update test, I'm going to go ahead and do props dot delete contact, which we'll pass. And inside of that prop, we're going to pass this dot prop dot person, dot underscore ID. And let me just finish the button. Delete forever is the name of the icon. We're going to still do 40, edit icon is the same, and then for the style, instead of edit,
>
> **[3:06](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/finalize-detail-view?u=76281980&t=186)** we're going to type delete. So this section here, this is going to be a section where you can either edit the specific contact or delete the contact. So you have all the details of the contact above. And then here you have two buttons that allow you to either edit the contact or delete the contact. And we're going to put the function delete contact, and the function update contact, the one that we use here inside of our CRD videos later on in that chapter. So just FYI. Okay, so now let's continue. We're going to create another view. And style for this one is going to be actions area. And inside of it we're going to have also a touchable opacity. And inside of this one, we're going to have an image. And that image is going to have a source which require an image that we drop earlier. So images, and then the specific image is called dot png.
>
> **[4:18](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/finalize-detail-view?u=76281980&t=258)** And then for the style, we're going to apply action image. And this is why it takes a lot of videos to get through that specific detail view component, because there's so much happening on that particular component. So for example, we have the details for the contact, and then we have actions that we can click on. It's important to take the time to do it properly. Okay, so let's go and copy and paste this again. And on this one, the image that we're calling is going to be email. So you're starting to see what's happening here. So we have three touchable opacity. We could have added a non-press or an action for this, but it's a bit beyond, I guess, the scope of this course. It's basically the same thing as these particular functions that we're pressing here. These are important because we're actually updating the contact, we're deleting the contact. But if you want to call email or what's going to be the next one, SMS, the contact, these are functions that are beyond what we're teaching here. So you can always figure out what these are and how you can call into specific elements inside of the phone to call, email, and so on, so forth. But this is not what we're teaching here. I just wanted to add the image, but we're not going to do the actual functions around these buttons. Okay? So we're doing it one more time and this one is SMS. And it's the same style, same everything.
>
> **[5:55](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/finalize-detail-view?u=76281980&t=355)** And then what we need to do is one last view. So what we're going to do is get outside of that view, right here, just before the scroll view, and create one last view with style again. Styles dot action area. And these are the texts that are going to go below the images, FYI. So we're going to do call, and repeat, and copy and paste for these. Three times. One is email and one is SMS. Okay, so we are officially done with the actual component side of things. Now what we're going to do is basically connect redux to this particular component. So the way to do this, again, we need to create a map, state to props, so we have access to this. Passes the state. And then we return for the props that we have inside of this particular component. One the person selected. And one quick note to update, state that update to update. So these are two elements that will basically update the state with these elements here.
>
> **[7:29](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/finalize-detail-view?u=76281980&t=449)** So two updates. If we click on the edit button, that means that we're into an update state, and this will generate different screens and allow us to update the contact. And finally what we're going to do is instead of doing the export default here, we're going to connect map state to props, and the actions to this particular component, which is detail view, like so. Okay. We are officially done with entering all the elements that we need inside of this particular view. Let's move on.

> [!info]- Semantic Content
>
> **Env Vars:** sms (3), fyi (2), crd (1)
> **Definitions:** is a  (1), is called (1), means that (1)
> **UI Navigation:** click on (2)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)


### 4. CRUD Operations

[↑ Back to Table of Contents](#table-of-contents)

#### [Create a new add person form](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-a-new-add-person-form?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-a-new-add-person-form?u=76281980&t=0)** - [Instructor] In this chapter, we'll focus on adding all the proper CRUD operations with our actions, reducers, and then finalize them with the components, so let's get started, so what we're going to do first is go inside of our index here, and we're going to start adding a few actions, so what I'm going to do, right after the noneSelected and before the loadInitialContacts. It doesn't really matter where in the file you put this, but for me, it's create and then load, and this is what I'm going to do, so first, we're going to create an action that's going to be called createNewContact, and these are going to take a few things, so let's go ahead and add the elements that we're going to need. The first one is, obviously, we're going to need a firstName passed to create a new contact, lastName, and let me just close the Explorer so we have more screen estate. Then we're going to pass the phone, the email, company, project, and then notes, and then, inside of this particular function or action, we're going to return, and we're going to pass dispatch 'cause we're going to need it. We're going to return whatever is inside of that action or function, so there's a few things that are going to happen. First thing we need to [Fetch](../../Skills/Web%20Development/Fetch.md),
>
> **[1:33](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-a-new-add-person-form?u=76281980&t=93)** and the back end is, again, the 127. You can also do a localhost if you want, 0.1. It's the same. Port 3000, and this is our API end, and what we're doing here is a POST method, so we need to add that, 'POST', and in the body of that POST, we're going to have whatever is returned here, so basically, this function will create a new contact, so we're fetching the back end with the method of POST, and then, in the body, we're going to pass whatever we got from the form inside of the add person, which we're going to work on in the next video, so let's go ahead and add all the things that we need. Basically, you need to use this function here, so [JSON](../../Skills/Web%20Development/JSON.md).stringify, so it creates our JSON that we're going to need, and pass the firstName, and then, what I'm going to do, just to make this faster, I'm going to copy and paste lastName. We're going to paste again, and what I'm going to do this time is just do option-click on both. I could have done that on the last one too. Do phone, paste again, option-click on the two,
>
> **[3:08](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-a-new-add-person-form?u=76281980&t=188)** and then, here is going to be email, paste again, option-click, company, paste, option-click, project, and finally, we're going to do the same for notes. Okay, once you have that, you want to make sure also you pass the headers in this particular call to your APIs, so the headers is 'Accept': 'application/json', 'Content-Type'. If you're familiar with API calls, this is familiar territory for you. Okay, so once we make the call, we pass the new item inside of our back end. What we need to do, and we're going to use .then for that. We're going to do a dispatch of our reducers, so basically, the dispatch is going to pass to our reducer this item here, where we're going to create a 'NEW_CONTACT', and we're going to create the 'NEW_CONTACT' reducer shortly. Then we need to do something else, so we're also going to need to dispatch loadInitialContacts right after that, and the reason why we're doing this
>
> **[4:41](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-a-new-add-person-form?u=76281980&t=281)** is to make sure that our contacts, once we add a new contacts, we are calling onto the action that will loadInitialContacts and therefore, make sure that we update our list of contacts inside of our application, and finally, if there's any error, catch the error and put it into the console what is this error. All right, so now we create a new action. Save this to make sure. Let's go ahead and go into our reducers and work on our reducer. I'm still going to close my Explorer just to have more screen estate, and what I'm going to do, again, here, right after noneSelected, I'm going to build the next case. The first case is a form update that we're going to need and use in a future video when we update a contact, but I'm still going to do it now 'cause it's part of creating and updating a new contact, so basically, we're going to reproduce the state and then update the action.payload.prop with the action.payload.value, and this is basically updating, inside of our state, these elements here. As we work on the form by adding a firstName, lastName, and so on and so forth, this particular reducer here is going to change the values in here
>
> **[6:15](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-a-new-add-person-form?u=76281980&t=375)** with what the form is basically adding, and then we're going to use that form to post it to the API at the end of that call, so basically, do the action that we have just created here. Create a new contact. We're going to pass all the information that we added to the form, so that's why this form is important here, 'kay? So the next one that we need is a case where we create the 'NEW_CONTACT', the one that we had inside of our createNewContact action, and this will return the state with all the elements that we had on the other side, so if you want to avoid typing a lot of things, what we could do is copy this, paste it here, simply remove this on all of them, like so, and sometimes, if I'm doing things a bit too fast, you can pause the video, take a look at what I'm doing, and then, what I'm going to do here is option-click on all these, like so, and replace them with simply this, and then delete, like that, 'kay? Once we have that case, we also need to have a case where we add a new person, so let's go ahead and do that.
>
> **[7:48](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-a-new-add-person-form?u=76281980&t=468)** These are all the reducers that are going to be involved in adding a new person, adding a contact, and this is why we're creating all of them now. Returns the state and then returns all the action.newPerson. Once we do the form on the add new person component, all of these specific reducers here are going to make a lot more sense, so just save it for now, and let's move on to the form in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (3), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Code Identifiers:** loadinitialcontacts (3), firstname (3), lastname (3), noneselected (2), createnewcontact (2)
> **Env Vars:** post (4), api (3), new_contact (3), json (2), crud (1)
> **CLI Commands:** make (7)
> **UI Navigation:** click on (3)
> **Definitions:** is a  (3)
> **Cross-References:** in the next (2)
> **Ports:** port 3000 (1)

#### [Functions for add person action](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/functions-for-add-person-action?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/functions-for-add-person-action?u=76281980&t=0)** - [Instructor] Okay, so now let's work on the add new person component. So that's all we have right now, so we have a lot of code to write to make this work. So the first thing I'm going to do is start by making sure that I have everything I need in my imports. I need ScrollView here and I also need Text, so let's go ahead and add those. Then what I'm going to need is a few things. So I'm going to need to import connect from, not http2, but from [react](../../Skills/Web%20Development/React.js.md)-redux like so. I'm also going to need to import Button and a TextInput from @react-native-material/core like so. And finally I'm going to need to import all of our actions because we're going to need to use a few of 'em here, and that's the easier way, from '../actions'; like so. Now the next thing we're going to do is create our style sheets. So like we've done in the past, create styles = StyleSheet.create. And then we're going to add a few styles here. One of them is the form. The second one is fieldStyles.
>
> **[1:35](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/functions-for-add-person-action?u=76281980&t=95)** And then finally, addButton. All right, so the style that we're going to insert here, one is flex: 1, paddingTop is going to be 50, paddingLeft is going to be 10. And again, as we run this application shortly, you're going to see the styles that then you can go back here and change them to your liking. These are basically my personal preferences. paddingRight. And this one was left and not right. So paddingRight, 20 as well. And then, justifyContent: 'space-between', like so. Then for this one, we're going to have a height of 70. color, we're going to add a color here. So it's going to be #f47100. And then we're going to go ahead and go into the addButton. And this is going to be a simple style of marginTop at 20. Okay, so inside of our Component, our class Component here, we're going to add first a function. And this function will allow us to add a new person inside of our database.
>
> **[3:07](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/functions-for-add-person-action?u=76281980&t=187)** So onAddPress, and this is how we're going to call it, what we're going to do is create a variable first before passing anything with all the elements that we have in our actions. So if you want to eliminate some of the code, we should have it somewhere here like so. So what I'm going to do is simply copy and paste these guys here. So this is from the actions, and the action that we just created a video ago, createNewContact, I basically copied these guys here and I'm going to paste them here like so. And then what I'm going to do is create this.props with these. And let me just close the Explorer to get a little bit more space. Then this.props, what we're going to do is leverage the action that we just created, createNewContact, right? And then again, pass all the elements that we just created as a variable. So these guys, this.props. And then finally, we're going to use our navigation to get back to the People, navigate to the People screen like so. So if I explain this very shortly, once we AddPress, we're going to create all these elements here as a variable
>
> **[4:45](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/functions-for-add-person-action?u=76281980&t=285)** and pass the this.props from it. So basically all of these are going to be available then to be passed to our new action here and create a new contact with these elements. And then at the end, we're going to navigate back to the People screen. This is how it works in here. All right, so now we're ready to start working on our stuff here. So the first thing we're going to do is create a ScrollView. So let's go ahead and do this, ScrollView. And inside the ScrollView, we're going to have, again, what we did the last time, showsVerticalScrollingIndicator. We're going to still say false for that. And then inside the ScrollView, we're going to create a new View that will have the styles for the form, so style={styles.form} like so. And then inside we're going to create the Text inputs. So the first thing, we're going to create a Text input, and this one will add a new contact. So we're going to have a text at the top of our form that says Add a new contact. And then we're going to have all the Text inputs or the [Forms](../../Skills/Web%20Development/Forms.md). So we're going to create one, then copy it and just paste it again for all the other elements that we need to add inside of our form. So therefore, these guys here. All right, so let's get started. So TextInput, and let me just close it out.
>
> **[6:20](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/functions-for-add-person-action?u=76281980&t=380)** And then inside of it, we're going to have a label, right? And the label is going to be First name for this one. Then we're going to insert styles for this guy, the styles that we've defined, so styles.fieldStyles, right? Then we're going to have a value, so this.props.firstName like so. Then onChangeText, so whenever we add text, we're going to update the value of our prop, which basically then will be passed into our action here. So once we have some value inside of that prop firstName, then once we click or tap onAddPress, we're going to pass all the values that we've created inside of our form, then pass it into the action and so on and so forth. Okay, so let's finish this, this.props.formUpdate with the prop: 'firstName', value. Perfect. And FYI, as you type this, if you have any typos or anything that doesn't work or the application doesn't work out, once we start running it, then what you could do is look into the exercise files to find my code and see what I've done. Okay, so now we need to repeat the same thing for last name
>
> **[7:55](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/functions-for-add-person-action?u=76281980&t=475)** and all the elements that we have in our form. So let's continue, TextInput. This is going to be Last name. And then I change this here. And Option + Click here and change this to lastName. Do this again. Now this is Phone number. And then click here, and then Option + Click here, and what you do is change this to phone. And continue down the list. If you want to challenge yourself, just pause the video and basically do the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the form on your own. See if you can get it yourself. All right, this time is going to be Email. Then same thing here, same thing here, email. Paste, and then if you want, as a reminder, take a look at here. firstName, lastName, phone, email, then it's company, projects, and notes, so let's continue,
>
> **[9:06](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/functions-for-add-person-action?u=76281980&t=546)** company. Then let's do this for the project.
>
> **[9:22](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/functions-for-add-person-action?u=76281980&t=562)** And then finally, Notes. Okay, so now that we have all this, the next step is to add the button that will call the actual add onAddPress function that we have here, right here. So to call this, we need the button. So let's go ahead and create the button. So the first thing we're going to do for this particular area, we're going to again insert some styles in a View, so style, and this one is going to be styles.addButton like so, boom. And then add a button right here. So the Button will have a title of Add, color of #4db6ac. And then onPress, we're going to basically use this.onAddPress. Now let me just go here. And then we're going to make sure that we bind this particular function to this class here. Okay, so finally, before we actually end this particular video, we need to connect, again, Redux. So we're going to do, as usual, mapStateToProps,
>
> **[10:57](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/functions-for-add-person-action?u=76281980&t=657)** pass the state, and then we're going to basically create all of the stuff that we need in here. So what I'm going to do is simply do a copy of all this here, paste it down right here like so. And then pass it to state as opposed to props like we did up there. And then return these guys. And just remove the constant here for the second one, like so. And finally, here on the export, we need to also connect. So let's do this, connect(mapStateToProps) the actions, and this component. Okay, so that was a lot of code. So if you got the gist of all this, we have a form to which have a prop of all these elements here. So this form has each prop that we need. Once we hit the button here, it's going to call this onAddPress function that is right up here and pass all the elements that we have in our form, map it to the props, and then send it to the action createNewContact, create a new contact, and then navigate back to People. So in short, this will create a new contact with the action that we set in the previous video
>
> **[12:32](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/functions-for-add-person-action?u=76281980&t=752)** and then call the reducer to change the state to its proper state. All right, so let's move on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (2), [Forms](../../Skills/Web%20Development/Forms.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Code Identifiers:** onaddpress (5), firstname (4), addbutton (3), createnewcontact (3), fieldstyles (2)
> **CLI Commands:** make (2), find (1)
> **Env Vars:** fyi (1)
> **Cross-References:** previous video (1)
> **UI Navigation:** navigate to (1)
> **Exercise Files:** exercise files (1)
> **Definitions:** is a  (1)

#### [Update person Redux reducer](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/update-person-redux-reducer?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/update-person-redux-reducer?u=76281980&t=0)** - [Instructor] In this video, we're going to add all the code that we need for Redux for the update person component. So what we're going to need to do first is go inside of the actions here and then we're going to add a few actions based on what we need for updating a person. So what I'm going to do is first go below the create new contact right here and go ahead and create the action, update, contact. So what we're going to pass is a person, and then what we're going to do is return this action, which is going to be called update contact like so, and the payload is going to be the person. Perfect, now we also need to create an action for saving the contact. So basically whenever we update a contact, we need to save it as well. So what we're going to pass in here is going to be similar to what we're passing when we're creating a new contact. So let's go ahead and copy some of the code that we have right here. So first thing I'm going to copy is all this here and bring it down here and update, save contact. But I'm going to also add the ID. So underscore, ID, the reason why we're passing ID is to make sure we identify which contact we're going
>
> **[1:35](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/update-person-redux-reducer?u=76281980&t=95)** to save or update. All right, so once we have that, a lot of the same code will apply from the one here. So what I'm going to do is copy all this, all the way down to here to line 41, copy that, go inside of my save contact and paste it. And then we're just going to change what we need to change. So the first thing we need to do is change the endpoint we're passing to. In order to update the contact, we need to pass the ID in here. So underscore, ID, like so, it's not a post, but a put method. And then everything else is going to be remotely the same. So all of these elements here are going to be the same. If you look at all the headers are going to be the same, the dispatch are going to be remotely the same except here. So the type is not new contact, but save contact, capital letters. And then we're also going to load initial contacts once more just to make sure that we have all of our contacts listed inside of our view. Alright, so this is good for the actions. Now what we need to do is go inside the reducers and open that file. I'm going to close this and then what I'm going to do somewhere around add person, I'm going to create two cases.
>
> **[3:09](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/update-person-redux-reducer?u=76281980&t=189)** So one is going to be the update underscore contact. And before this becomes a problem, let's do a lowercase on this one. And then I'm going to return and let's see if I have something that has all the elements that I need. I have some of it here. So what I'm going to do is copy and paste it in the update contact. Before I return any of this, I need to return the state clearly. I'm also going to add another one to update to true. And then in all these elements here, and I'm going to also add ID before I forget it, I'm going to change some of these things. Okay, so instead of having nothing here, what I'm going to do is on all these guys, I'm going to option click to select all these guys. 'Cause there's going to be a lot of repetitive codes. So I don't like to write things twice if I can shorten it. So this is going to become action, dot payload. And let's just do the first one. So this one is going to be first name, second one's going to be last name, phone, email, company, project, and finally notes. And I forgot to do this one here, that's okay.
>
> **[4:44](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/update-person-redux-reducer?u=76281980&t=284)** We'll just copy and paste right here. And then change this to underscore ID. Okay, so update contact is done, so let's save it. Let's create the next case. Case, which is save contact, right? And then on this one I'm going to return this date, make to update false this time, make also detailed view false.
>
> **[5:21](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/update-person-redux-reducer?u=76281980&t=321)** And just to explain this, when we save the contact, we're updating these elements inside of the state. To update means detail view, false. That means we're going out of the detailed view. If this was true, the detailed view would show right? And then we're going to do all these guys. Empty, so copy line 47 through 53, paste it here. And then at the end we're also going to add underscore ID and comma, and that completes our safe contact. One other thing I just want to make sure to check is that I have underscore ID here. Yeah, I do, okay, good. Okay, so now we've added all the actions and the reducers that we need for the update form view. We're going to work next on the component itself. Let's move on.

> [!info]- Semantic Content
>
> **CLI Commands:** make (5)
> **Definitions:** is a  (1)
> **Analogies:** similar to (1)
> **Best Practices:** make sure to (1)
> **Speakers:** - [instructor] (1)

#### [Update person view](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/update-person-view?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/update-person-view?u=76281980&t=0)** - [Narrator] Okay, so now we're going to work on the UpdatePerson view and the component. And the cool thing about this particular component is that we've written a lot of the code already on the AddPerson. So what we're going to do is primarily copy all of the code from the AddPerson view, all of it like so, and then put it in the UpdatePerson and then change what we need to change. Basically select all. So I did Command A on Mac and Ctrl A on [Windows](../../Glossary/Service/Windows.md), and then I'm going to paste it like so. And then I'm going to make all the changes that I need to make. So the first thing that I need to change is in here, the actual name of the class itself. We're going to call this UpdatePerson and make sure that at the end, we change it accordingly here. Perfect. Now the second thing that we need to change is this one here. The function is not on AddPress, but on UpdatePress. And we're going to change it to where the button is too.
>
> **[1:19](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/update-person-view?u=76281980&t=79)** And if you want to make sure that you're writing the right thing here and not have to do any typos because the function's already created, you can simply do this .UpdatePerson and then .bind(this), like so. Okay, now let me close this to have more screen estate. All right, let's go back up and change a few things here. Instead of CreateNewContact, we need to now not only pass the underscore ID on both here, let me just do option click underscore id. I'm just going to remove the extra space here. And then instead of CreateNewContact, it's SaveContact.
>
> **[2:19](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/update-person-view?u=76281980&t=139)** So save this and most of the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) is pretty much the same. So if we look at it, we have our styles that we've created. We have the function that we created for specifically updating a contact and we have our scroll view, which is pretty much the same as the other one. Now here we have this.props/firstName. And basically we have the values that would be passed directly from the id. So you should have pretty much stuff showing up on the field here and then onPress, and then mapStateToProps. The only thing that we need to add here is again, the id. So I'm going to do option click on both and do underscore id. So I have access to this and everything else is the same. And then here, actions, everything seems to be working here. So now you've completed adding a person, updating a person. Now we're going to focus on finalizing listing people so we can actually see them and start running our application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Windows](../../Glossary/Service/Windows.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** make (4)
> **Code Identifiers:** firstname (1), onpress (1), mapstatetoprops (1)
> **UI Navigation:** click on (1)
> **Speakers:** - [narrator] (1)

#### [Connect your list view to backend](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/connect-your-list-view-to-backend?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/connect-your-list-view-to-backend?u=76281980&t=0)** - [Instructor] Okay, so this is where we're going to connect all the pieces that we need so we can actually see the application running. So as we update the code, we're going to keep an eye on our simulator, because this is going to work eventually. Right now we have an error that I just noticed, Flatlist doesn't exist. Clearly it doesn't, because FlatList takes a capital L. So let's go ahead and fix that right away. Right here, the component in question, I'm going to save that, and lo and behold, I actually have here a list that shows up. The other thing I want to fix here is mapStateToProps, just to make sure we're spelling this right, like so, save that. Perfect. Now, if you see the screen and you don't see anything in here, it's normal. It's because you don't have anything in your database yet. So you have two choices. Either you add it through this screen and we could do that. So let's go Manny. So there's a little issue here that we're going to fix, but if you want to dismiss that and go to People and see something, you can add it directly through the application that I call Postman. So if you're familiar with Postman, what Postman does, it allows you to add through your backend specific things. So contact, POST, if you remember correctly, we could do first name, last name, and so on and so forth. So you can add a few things
>
> **[1:33](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/connect-your-list-view-to-backend?u=76281980&t=93)** and then send it to your backend. And it's going to add people. So if you want to go ahead and do that, you can do that too, or add it through the form. But clearly there is an issue with the app form, which we'll debug next. So what I'm going to do first is finalize everything that I need. So I did this here so I can close that. And then I'm going to go in PeopleDetail and make a few changes here. It's going to be very quick here. So first thing I'm going to do is remove these guys here. I don't need those. Then I need to import a few things. So I'm going to import { connect } from '[react](../../Skills/Web%20Development/React.js.md)-redux'.
>
> **[2:16](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/connect-your-list-view-to-backend?u=76281980&t=136)** We're going to import DetailView From './DetailView'. We're going to import UpdatePerson from './UpdatePerson' here and import all the actions so we have access to all of them from '../actions'. Perfect. So now in the component itself, we're going to have first a function that will be renderDetails. And basically this function will again check, like, what we've done in the past. We're going to check if this.props.update, not .update, but .toUpdate. If it's true, then return <UpdatePerson />. So basically when this particular element, in our state or this particular prop, toUpdate is true, then return this view, else, return <DetailView />. And then in our return statement here, all we have to do is use the function we just created, {this.renderDetails()}. And this is a function. Let's make sure we use this properly. I just need to create the view first
>
> **[3:52](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/connect-your-list-view-to-backend?u=76281980&t=232)** and just insert this inside the <View>. Perfect. And then what we're going to do is, again, map our props. So mapStateToProps = state =>
>
> **[4:13](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/connect-your-list-view-to-backend?u=76281980&t=253)** and then return toUpdate: state.toUpdate.
>
> **[4:22](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/connect-your-list-view-to-backend?u=76281980&t=262)** And finally, again, like we've done in the past, connect(mapStateToProps, actions)
>
> **[4:35](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/connect-your-list-view-to-backend?u=76281980&t=275)** and then bind PeopleDetails to the export. Okay, we're going to save this, and now we have all of our views. Now what we're going to do is quickly debug this. So we have a few things that we need to debug. Most likely, there's going to be a few screens that are not going to work. So Company is not going to work properly or not show anything yet, because we haven't finished those. People, if we click here, we have some elements that we need to debug. We're going to take a look at that in the following video so we know that we have this. So let's dismiss this and let's bring back our view here so we have an issue when we add a new contact. And we're going to take a look at that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (1)
> **Code Identifiers:** toupdate (4), mapstatetoprops (3), renderdetails (2)
> **CLI Commands:** make (3)
> **Tools:** postman (3)
> **Definitions:** is an  (1), is a  (1)
> **Env Vars:** post (1)
> **Cross-References:** in the next (1)
> **UI Navigation:** go to (1)

#### [Testing and debugging Redux for editing](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/testing-and-debugging-redux-for-editing?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/testing-and-debugging-redux-for-editing?u=76281980&t=0)** - [Instructor] Okay, so now what we're going to do is add a few actions and a reducer to be able to remove ailments in our applications. We're also going to fix and debug a few things. So the first thing I'm going to do right away and get it out of the way is I made a little typo here. I don't know if you caught that. If you don't have this error, it means that you've done the right thing and I didn't. So basically, what I need to do first is fix this, and it should get rid of that PropType error. And then the reason why we have an error when we type elements in this form is that we don't have the right action inside of our actions to the form update. We have the reducer, well, we don't have the action, so we need to add that. All right, so what we're going to do first is go into the actions and start by adding this one here. So I'm going to do an export cons and do formUpdate, and then what is the prop and the value. We're basically passing those two parameters. Then we're going to return for the specific action a type, which will be FORM_UPDATE, which we were calling in this particular element here, but didn't have the proper action for it. So that's why we have an error here. And then the payload is going to be the prop and the value. And we're using that in two of our [Forms](../../Skills/Web%20Development/Forms.md), the update and the addNewContact.
>
> **[1:35](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/testing-and-debugging-redux-for-editing?u=76281980&t=95)** So you would've probably ran into more issues inside the update as well if you went into the edit mode. Okay, so we have that one done. We're going to save. And then we're going to do another one, and I'd like to do it a little bit lower here after the update and save, since this is a delete, and I'll keep it above loadInitial action since this one is the one we're always using. And I'm going to do export const deleteContact. We're going to pass an id as a parameter, then we're going to return. And I think I'm going to copy some of the code that we have somewhere else. I think I'm going to copy saveContact here and just delete everything that I don't need. Yeah, it's going to be faster this way. Okay, so basically what I don't need here, okay, so let's fix our errors. Dispatch, this is all good, [Fetch](../../Skills/Web%20Development/Fetch.md), all good, the id, all good, and the method is delete. So what I'm going to do is shorten that one. So let's do this. We don't need a body in this one. And I'm going to do this method: 'DELETE', like so.
>
> **[3:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/testing-and-debugging-redux-for-editing?u=76281980&t=180)** we don't need the headers. We can go straight to then, like so, then remove this.
>
> **[3:10](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/testing-and-debugging-redux-for-editing?u=76281980&t=190)** Again, if I'm going too fast, just pause the video. So basically, what I've done is leverage the previous code with the fetch. We're passing an idea as we need it for the delete method. Then make sure you don't have anything after the parenthesis. And then we do the then, we're doing a dispatch, and the dispatch is going to be DELETE_CONTACT. And then we're also doing a dispatch of loading initial contacts and we're also catching if there's an error. And let's make sure that what you're passing is written the same way as this, okay? And just to make sure when you type this here that you're passing the id that's here. You just simply do this and pass the id, like so, save, okay? Then what we need is to go in the reducers and add the case for delete a contact. So I'm going to go all the way down, just before the default, and do a case for DELETE_CONTACT, like so,
>
> **[4:29](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/testing-and-debugging-redux-for-editing?u=76281980&t=269)** return ...state, detailView is going to be false.
>
> **[4:39](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/testing-and-debugging-redux-for-editing?u=76281980&t=279)** Again, we don't need the detailView when we're deleting. And personSelected is going to be null. So we're clearing the person that we're selecting from that action and the reducer. Okay, so now we can test this, see if there's any bugs. Let's bring our terminal. Let's do our complete refresh by doing R. Now you should see this, click. You have all the details here. Everything should look fine and dandy like this. If it doesn't, that's your CSS. If there's something missing, you got an error, take a look at the error or take a look at my code. Then clicking here should actually turn it back. Yep, there you go. Now we have this. If we want to add a person, we go ahead and click on Add, and I'm going to start typing. Now the form should work. So let's take a look. Manny, yep, it's working. Manny Henri, 555555555, me@[p.com](https://p.com), then company, [LinkedIn](../../Skills/Web%20Development/LinkedIn.md), project, Bla, Bla, then Add. And we have the new person. So if we click on the second one, I have two with the same name. Let's go. And this one, let's click on Edit. And now we have a little issue. So we have, "Cannot bind of undefined." Okay, so let's take a look here. Let me just close this for a second.
>
> **[6:14](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/testing-and-debugging-redux-for-editing?u=76281980&t=374)** So we clicked on Edit, and UpdatePerson is where the error is. So let's go ahead and click UpdatePerson. Let's take a look at what we're having as an issue here. UpdatePerson doesn't seem to be catching that one. So let's see. And it's not UpdatePerson, but on UpdatePress. That's the right function because that was the function that we actually created here. So that was my bad. Okay, so now we're good. Let's go back, click on Add, and we have this on UpdatePress, UpdatePerson. We have some issues here too. So let's take a look at this. Let's make sure we have all the elements. What is the error? It says, "this.props.navigation to people." Okay, so here's where we have an issue. And this, we don't need. So let's remove that one because this is not part of the add, okay? All right, let's go back here. Let's click again on one of the names. Let's click on Edit. Let's just change to Manuel and then click on Add. And it hasn't refreshed it. So, interesting. Okay, so if I do, this is the add new contact. So again, we need to change this. So let's go back to the people. Let's click on Add again.
>
> **[7:48](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/testing-and-debugging-redux-for-editing?u=76281980&t=468)** And let's click on this one, click on Edit. So there's a few things I want to change. So let's again go into, first, here, it should be Update contact, like so. Let's make sure we have all the elements in place here 'cause we copied a lot of code for this one. So we need to make sure that we're doing this right. Notes, boom, boom, boom. Okay, so let's go ahead and check the actions to make sure that everything's dandy and there. So let's go into the actions here. And what I'm going to do is close the explorer, close the UpdatePerson, and saveContact is here. So saveContact, the id is passed, but for some reason, it doesn't take it up inside of the fetch here. And I know exactly why. It's because this is not a template strings. It's a regular string. So let's go ahead and do that. Let's make sure to transform this to a template string. Now it should take it. Yes. Okay. So that means that I made the same mistake in the other fetch here where I need the id, right here. See, it doesn't take it. Okay, so let's go and change it to template string. Little details but that makes a world difference in programming. Okay, so let's see if everything is back to normal. I'm just going to change the name to Manuel
>
> **[9:23](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/testing-and-debugging-redux-for-editing?u=76281980&t=563)** as opposed to Manny, and Add. And Manuel Henri. So this is all good. Now we also added the delete in this video, so let's test this. If I click on Delete, it's deleted. We're all good. Okay, so now we've completed all of our debugging. The application works with the people, adding new people, editing new people, deleting new people. Now we're going to focus on the company views in the next chapter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (4), [Forms](../../Skills/Web%20Development/Forms.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Code Identifiers:** savecontact (3), detailview (2), formupdate (1), addnewcontact (1), loadinitial (1)
> **UI Navigation:** click on (10)
> **CLI Commands:** make (8)
> **Env Vars:** delete_contact (2), form_update (1), delete (1), css (1)
> **Exercise Files:** template (3)
> **Definitions:** means that (2), is a  (1)
> **Cross-References:** go back to (1), in the next (1)


### 5. Company View and Final Touches

[↑ Back to Table of Contents](#table-of-contents)

#### [Code the company list query](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-company-list-query?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-company-list-query?u=76281980&t=0)** - [Instructor] Okay, so in this chapter we're going to create a view that will be a company view that will give us a list of the companies with the people that work there. So if we have two people that work at [LinkedIn](../../Skills/Web%20Development/LinkedIn.md), we're going to have one LinkedIn list with two people inside of it. So what we're going to do is create a second name, just Gabe Bla, phone number six, six, six, six, six, six. Email gabe@[whatever.com](https://whatever.com). Company, LinkedIn. Let's make sure it's the same company. Boom, boom, boom, boom, okay. Add this one. So we have two guys and let's edit this one to make sure it has the same name and company LinkedIn, add. And we have two companies with the same name. Okay, perfect. So now what we need to do is in the actions, clean the lodash, 'cause we don't need lodash here, we're actually going to do it in the other file. So what I'm going to do now is bring the company list first and then in the next video we're going to do company item and finish it with a beautiful view for companies. So let's go ahead and work on this one now. And the first thing I'm going to do is change a few things here. Actually, I'm going to import connect from [react](../../Skills/Web%20Development/React.js.md) redux.
>
> **[1:29](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-company-list-query?u=76281980&t=89)** Then I'm going to import this from lodash. And the reason why I'm going to do lodash is, basically I'm going to write a quick algorithm that will allow me to pull all the people from contacts and put them together in a company view. So, and then I'm going to import company item from company item, like so. Okay, now in the render, before I go and do the render, I'm going to do styles, I'm going to need styles from this guy too. Stylesheet.create. And now I'm going to do simply a container that will have flex of one and padding top of 80. So basically, give some room in between all the elements of the iPhone and the actual view. Okay, and then in the view itself, we're going to do all this, return. And I'm going to first leverage my view and insert the container, the style, styles.container. And then I'm going to create a flat list. And again, let's make sure we type flat list properly, 'cause that was our big issue the last time. Close that component.
>
> **[3:02](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-company-list-query?u=76281980&t=182)** And then we're going to, first for the data, we're going to use this.props.companies. We're going to render item and we're leveraging a lot of the same code that we've done for render people, but I'm going to type it this time. So item, and for each item, we're going to use the company item and companies as the actual prop that it takes, item.
>
> **[3:42](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-company-list-query?u=76281980&t=222)** And make sure you close out your component before that curly brace here. 'Cause this is part of the function render item. And you want to make sure that your component starts here and it closes before that curly brace. Just a quick note, 'cause I made that mistake before and I looked for that error for a while. I was like, where is this mistake? And sometimes it's the simple typos like this, that make your life a living hell for a couple of hours or a couple of minutes because you're like, I can't see what I've done wrong. We've all been there as developers. Okay, so now I'm using a lot of the same syntax for basically using the key extractor as the item itself. So whatever is the item and the index will basically create a key, 'cause every component inside of React, when there are lists, they need a key. Okay, so this is pretty much done for this particular render method, now we need to create the algorithm for our little list, and this is how we're going to do it. We're going to do it in the map state to props here, map state to props, past the state. And then we're going to first create a variable called people. And for that, we're going to pass state.people. So we have our array of people.
>
> **[5:16](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-company-list-query?u=76281980&t=316)** And then what we're going to do is create a prop called companies. And as you must remember, we're actually passing it to every item here. So the companies is here, right? So we're going to create companies, and this is how we're going to basically create this particular variable. So we're going to leverage lodash and chain using people.
>
> **[5:46](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-company-list-query?u=76281980&t=346)** And then, we're going to group by company. You're probably going to start seeing where this is going. And then what we're going to do is map... Value and key... and return, company, key, and then the names with the value.
>
> **[6:12](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/code-the-company-list-query?u=76281980&t=372)** So basically what we're doing, we're going through the list of all the people, trying to find which companies are the same. So, chain them and group them by company. And then what we're going to do is map that with a company, which will be a key and names value. So there's probably going to be an array of people as the names. And then finally, what we're going to do is return the companies, which will have, at that point, a list of people inside of it. And that's basically the algorithm to map all the names for a company and have that company list all the names. And then finally, what we're going to do is do like we're usually doing in redox, is make sure that we connect map state to props with company lists. Save that. Now we may run into some issues here because right now we don't have all the elements, but I'm going to take a look at this bug just to make sure, flat list. Yeah, we did load this the wrong way this time. So let's do this, now we don't have a problem anymore. If we go here, we may have a call stack error because right now we don't have all the elements completed. So we're going to complete this in the next video. And if there's anything that we did wrong, a typo somewhere, we'll debug that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (4), [React](../../Skills/Web%20Development/React.js.md) (2)
> **CLI Commands:** make (8), find (1)
> **Cross-References:** in the next (3)
> **Code Identifiers:** iphone (1)
> **URLs:** [whatever.com](https://whatever.com) (1)
> **Speakers:** - [instructor] (1)

#### [Create company item and list view](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-company-item-and-list-view?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-company-item-and-list-view?u=76281980&t=0)** - [Instructor] Okay, so let's finalize our code with the CompanyItem, which we'll be able to load into CompanyList once we have this one done. So, the first thing I'm going to do is change here. Remove the Component 'cause we're not going to need it. And then, what I'm going to do is add Image to that list here. And finally, import Icon from '[react](../../Skills/Web%20Development/React.js.md)-native- vector-icons/Material Oops. Let's make sure that forward slash material icon, that's why it wasn't showing. It's because I had a little issue here. Okay, now we'll create the style sheet as usual, styles, stylesheet.create, and we're going to start by doing title top -55, left 150 and font size 24. Then we're going to do each class, which we'll do height of 100, left of zero
>
> **[1:36](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-company-item-and-list-view?u=76281980&t=96)** padding top 5. Then what we're going to do is do action top minus 25. Background color, we're going to do black color white. So basically the background's going to be black and the text's going to be white. And this is going to be for the list of people. Padding bottom, we're going to do 5, padding top we're also going to do 5. And padding left, we're going to do 10. And the final element is going to be the icon position absolute top 10, left 10, color white. And background color, we're going to do an RGBA 255, 255, 255 is zero. Okay, so now inside of company items, we're going to change this to something else. We're not going to do a class component on this one. We're going to do a functional component.
>
> **[3:12](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-company-item-and-list-view?u=76281980&t=192)** Company item takes props 'cause we don't need it. It's a lot more code, a lot more complex things that happens in the background with React. We don't need it. So we're just going to do a functional one on this one. Let's do a view. We're going to do an image and this image will take a few things. So the first thing is the source, right? So the source will require image, background.jpeg, style styles.image, right? So we have that style. Now we need to do an icon next. So we're going to do that. Remember we load in an icon. So we're going to do that, icon, which we'll close out right away. And then we need to do a few things. So the name of the icon is going to be business, the size will be 100. And again, we're going to do style styles.icon And then finally we're going to do text, which will repeat all of the particular people within the company. So this is where we're going to do that. Style styles.title, I'm actually going to name the company first. So pass props.companies
>
> **[4:48](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-company-item-and-list-view?u=76281980&t=288)** .company and then we're going to do all the people inside of this particular company. So props.companies .name.map So we're mapping every person that is inside of that particular company. And we're going to return inside of that return we're we're going to have text, name, first name, and then name.lastName So every person that is going to be on that array, we're going to return the first name and the last name. So we're basically going through every name that sits inside the company. Now the only thing I need to do here is add a key. So that's very important in React. So name._id And then I'm also going to add the style that we just wrote. Styles.action And, okay, so here's something that was imported by mistake. Let's make sure that it's not flat list. Should be flatList, but I don't think we're using it. No, we're not using it here. So let me remove that one. Perfect. Now we can save. Okay, so what is property text doesn't exist.
>
> **[6:26](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/create-company-item-and-list-view?u=76281980&t=386)** Okay, let's go ahead, take a look at this. We forgot to import text, so let's make sure we do. There you go. Save it. We're back. I'm just going to reload and rebundle. It doesn't seem to have any issues. Let's go here. And what do we have now? Company item cannot read Property Map of Undefined. Okay, so let's go and take a look at this particular thing here. Names, that's a problem I had name singular, so now it should work. There you go. So now we have two names inside of the company, [LinkedIn](../../Skills/Web%20Development/LinkedIn.md). So, if I add another person, let's add another person in another company. Let's say my company, I'll go Fields, Project, whatever, whatever. Manuelo Henrieto. I'm just making fun of my own name, fyi. Okay, number, email doesn't really matter. We didn't put any catch on this one. Add. Okay, so that completes the application. I hope you had as much fun as I did. Play with the application, update the styles if you want to. This is basically the styles that I've put in in there. And then if you want to add more to this application, feel free to do so.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (3), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** make (3)
> **Code Identifiers:** lastname (1), flatlist (1)
> **Env Vars:** rgba (1)
> **Speakers:** - [instructor] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Building your own CRM application with React Native](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/building-your-own-crm-application-with-react-native?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/create-a-crm-mobile-application-with-react-native-23458867/building-your-own-crm-application-with-react-native?u=76281980&t=0)** - [Narrator] I hope you had as much fun as I did building this CRM application. Now that we have a full function of the application with crowd abilities, there are a few areas I suggest to continue building your skills or explore how you could improve this application. For example, you could expand the project data by adding multiple projects per contact, details per project or even assign projects to companies instead of people. Next, you could take a similar approach by adding multiple phone numbers and emails per contact. Then you could add type checking with either PropTypes or flow, which adds further type assertion and multiple ways to prevent bug and bad code in your application. Adding more tests to your app would also help in improving your development workflow. Finally, pushing your [Mobile Development](../../Topics/Mobile%20Development.md) skills further by taking any of the iOS or [Android](../../Glossary/Sdk/Android.md) courses available on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning is a great next step. As all our mobile projects end in these environments, being familiar with the development languages and tools is a must to be able to release your application. Thanks very much for taking my course, and I'll see you in a bit.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Mobile Development](../../Topics/Mobile%20Development.md) (1), [Android](../../Glossary/Sdk/Android.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Definitions:** is a  (2)
> **Code Identifiers:** ios (1)
> **Env Vars:** crm (1)
> **Analogies:** for example (1)
> **Speakers:** - [narrator] (1)


## Instructor

- [Emmanuel Henri](../../Instructors/Web%20Development/Emmanuel%20Henri.md)

## Resources

- Exercise files available

## Skills Covered

- Mobile Application Development
- React Native

## Path Context

### In [Become a React Native Developer](../../Paths/Mobile%20Development/Learning%20Paths/Become%20a%20React%20Native%20Developer.md)
← [From React to React Native](From%20React%20to%20React%20Native.md) | **3 of 5** | [React Native Ecosystem and Workflow](React%20Native%20Ecosystem%20and%20Workflow.md) →

## Appears In

- [Become a React Native Developer](../../Paths/Mobile%20Development/Learning%20Paths/Become%20a%20React%20Native%20Developer.md)

## Related Courses

_Courses sharing skills:_

- [Learning React Native](Learning%20React%20Native.md) — React Native
- [React Native Ecosystem and Workflow](React%20Native%20Ecosystem%20and%20Workflow.md) — React Native
- [From React to React Native](From%20React%20to%20React%20Native.md) — React Native
- [React Native Essential Training](React%20Native%20Essential%20Training.md) — React Native

---

[↑ Back to top](#)