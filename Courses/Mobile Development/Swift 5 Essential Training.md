---
type: course
cssclasses:
  - lle-course
  - lle-level-beginner
slug: swift-5-essential-training
url: "https://www.linkedin.com/learning/swift-5-essential-training"
level: Beginner
updated: 2/2/2022
learners: 1968
skills:
  - Swift (Programming Language)
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQEks0fJDAIWMw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1568843493234?e=2147483647&amp;v=beta&amp;t=Ey2BOGas6-6UdMEFUO7hHuHTHqGxudAPCc2i_SAC6Mk"
linkedin_topic: Mobile Development
learning_paths:
  - '[Get Started with iOS App Development](../../Paths/Mobile%20Development/Learning%20Paths/Get%20Started%20with%20iOS%20App%20Development.md)'
  - '[Master Swift](../../Paths/Mobile%20Development/Learning%20Paths/Master%20Swift.md)'
prev_courses:
  - null
  - '[Programming For Non Programmers Ios 17 And Swift 5](Programming%20For%20Non%20Programmers%20Ios%2017%20And%20Swift%205.md)'
next_courses:
  - '[Xcode 15 Essential Training](Xcode%2015%20Essential%20Training.md)'
  - '[Swift- Delegations and Data Sources](Swift-%20Delegations%20and%20Data%20Sources.md)'
path_nav: '[{"path":"Get Started with iOS App Development","position":1,"total":4,"prev":null,"next":"Xcode 15 Essential Training"},{"path":"Master Swift","position":2,"total":4,"prev":"Programming For Non Programmers Ios 17 And Swift 5","next":"Swift- Delegations and Data Sources"}]'
path_count: 2
tags:
  - course
  - topic/mobile-development
  - topic/software-development
  - skill/swift-programming-language
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Mobile%20Development/Swift%205%20Essential%20Training.md)

![Swift 5 Essential Training](https://media.licdn.com/dms/image/v2/C560DAQEks0fJDAIWMw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1568843493234?e=2147483647&amp;v=beta&amp;t=Ey2BOGas6-6UdMEFUO7hHuHTHqGxudAPCc2i_SAC6Mk)

# Swift 5 Essential Training

> Swift is the programming language for the Apple ecosystem—iOS, macOS, watchOS, and tvOS—and it’s a particularly expressive, fast, safe, and fun language that’s suitable for new and experienced programmers alike. In this course, instructor Harrison Ferrone helps you gain hands-on experience with Swift 5, and prepares you to start developing your own apps and games using this popular language. Follo

> [LinkedIn Learning](https://www.linkedin.com/learning/swift-5-essential-training) | Beginner | 2K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Begin an adventure with Swift 5](#begin-an-adventure-with-swift-5)
  - [Quest accepted: What you should know](#quest-accepted-what-you-should-know)
- [**1. Getting Started**](#1-getting-started) (3 videos)
  - [Swift retrospective](#swift-retrospective)
  - [Getting Xcode](#getting-xcode)
  - [Reviewing the exercise files](#reviewing-the-exercise-files)
- [**2. Getting to Know Your Data**](#2-getting-to-know-your-data) (13 videos)
  - [The quest begins: Variables](#the-quest-begins-variables)
  - [Variables and constants](#variables-and-constants)
  - [Type safety and inference](#type-safety-and-inference)
  - [Logging and commenting](#logging-and-commenting)
  - [Swift operators](#swift-operators)
  - [Understanding strings](#understanding-strings)
  - [Working with strings](#working-with-strings)
  - [Type conversions](#type-conversions)
  - [Booleans and logical operators](#booleans-and-logical-operators)
  - [Introducing optionals](#introducing-optionals)
  - [Accessing documentation](#accessing-documentation)
  - [Challenge: Player stats](#challenge-player-stats)
  - [Solution: Player stats](#solution-player-stats)
- [**3. Working with Collections**](#3-working-with-collections) (10 videos)
  - [Gathering inventory with collections](#gathering-inventory-with-collections)
  - [Swift arrays](#swift-arrays)
  - [Core array methods](#core-array-methods)
  - [Swift dictionaries](#swift-dictionaries)
  - [Core dictionary methods](#core-dictionary-methods)
  - [Working with sets](#working-with-sets)
  - [Core set methods](#core-set-methods)
  - [Swift tuples](#swift-tuples)
  - [Challenge: Merchant shop](#challenge-merchant-shop)
  - [Solution: Merchant shop](#solution-merchant-shop)
- [**4. Application Control Flow**](#4-application-control-flow) (9 videos)
  - [Controlling your destiny](#controlling-your-destiny)
  - [The if statement](#the-if-statement)
  - [Unwrapping optionals](#unwrapping-optionals)
  - [Using for-in loops](#using-for-in-loops)
  - [While loops](#while-loops)
  - [The switch statement](#the-switch-statement)
  - [The guard statement](#the-guard-statement)
  - [Challenge: Game logic](#challenge-game-logic)
  - [Solution: Game logic](#solution-game-logic)
- [**5. The Wide World of Functions**](#5-the-wide-world-of-functions) (10 videos)
  - [Leveling the playing field](#leveling-the-playing-field)
  - [Basic functions](#basic-functions)
  - [Overloading functions](#overloading-functions)
  - [Complex functions](#complex-functions)
  - [Function types](#function-types)
  - [Understanding closures](#understanding-closures)
  - [Using closures](#using-closures)
  - [Type aliasing](#type-aliasing)
  - [Challenge: Battlegrounds](#challenge-battlegrounds)
  - [Solution: Battlegrounds](#solution-battlegrounds)
- [**6. Classes, Structs, and Beyond**](#6-classes-structs-and-beyond) (9 videos)
  - [Populating the game world](#populating-the-game-world)
  - [Value vs. reference types](#value-vs-reference-types)
  - [Basic Swift classes](#basic-swift-classes)
  - [Access modifiers and properties](#access-modifiers-and-properties)
  - [Subclassing in Swift](#subclassing-in-swift)
  - [Basic Swift structs](#basic-swift-structs)
  - [Chaining optionals](#chaining-optionals)
  - [Challenge: Inventory system](#challenge-inventory-system)
  - [Solution: Inventory system](#solution-inventory-system)
- [**7. Leveling Up with Enums, Protocols, and Errors**](#7-leveling-up-with-enums-protocols-and-errors) (9 videos)
  - [Final stage, final boss](#final-stage-final-boss)
  - [Swift enumerations](#swift-enumerations)
  - [Raw and associated values](#raw-and-associated-values)
  - [Introducing protocols](#introducing-protocols)
  - [Using extensions](#using-extensions)
  - [Throwing errors](#throwing-errors)
  - [Handling errors](#handling-errors)
  - [Challenge: Battle actions](#challenge-battle-actions)
  - [Solution: Battle actions](#solution-battle-actions)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Begin an adventure with Swift 5](https://www.linkedin.com/learning/swift-5-essential-training/begin-an-adventure-with-swift-5-14524901?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/begin-an-adventure-with-swift-5-14524901?u=76281980&t=0)** - Programming should be an adventure, and one of the newest and most exciting languages out there is [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md). It's fast, flexible, and safe, and version 5.0 is the best iteration we've had yet. Hi, I'm Harrison Ferrone, and I'd like to welcome you to Swift 5 Essential Training. Throughout this course, we'll be exploring the core language features and capabilities of Swift from the ground up. We'll start with common data types, conditional logic, and methods. And keep going until we're comfortable creating classes, structs, protocols, and much, much more. Now, we have to crawl before we can walk, so the examples we'll be working with aren't part of an actual iOS application. Instead, we'll be learning SwiftSyntax and common use cases through examples that pull from gaming, even though we won't be making a functioning game. Let's put it this way, I'd rather you have fun with the code you'll be writing than force you to write "hello world" or your name 100,000 times. Whether you're looking to develop an app idea you've had for years or a new one you've just thought of, you'll leave this course equipped with the right fundamentals for the job. It's dangerous out there alone, so keep your Swift skills equipped.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (4)
> **Code Identifiers:** ios (1)
> **Versions:** version 5 (1)
> **Speakers:** - programming (1)

#### [Quest accepted: What you should know](https://www.linkedin.com/learning/swift-5-essential-training/quest-accepted-what-you-should-know-14531790?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/quest-accepted-what-you-should-know-14531790?u=76281980&t=0)** - [Instructor] Before we get started, there are a few assumptions that we should clear up. First, even though this course was designed specifically for students with no [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) experience, it's not a programming fundamentals course. You should be completely comfortable with concepts like variables, methods and classes, preferably in an object oriented context before diving in. If you're brand new to programming or you need a basic refresher, check out [Programming Foundations Fundamentals](../Software%20Development/Programming%20Foundations%20Fundamentals.md) from our course library, it'll get you up to speed on everything you'll need to know. Second, since we're starting from scratch in Swift, it doesn't make sense to jump right into an [Xcode](../../Skills/Mobile%20Development/Xcode.md) app project without understanding the language. We have to walk before we run after all. Instead, we'll do all of our work in the X code playgrounds I've already prepared for you. Think of these as sandboxes that you can play around in without having to worry about the added information, and techniques that go into making an iOS app. If you are familiar with a previous version of Swift or you're just looking for what's new in Swift 5, feel free to bounce around to the topics you're interested in.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (4), [Programming Foundations](../../Skills/Software%20Development/Programming%20Foundations.md) (1), [Xcode](../../Skills/Mobile%20Development/Xcode.md) (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** ios (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)


### 1. Getting Started

[↑ Back to Table of Contents](#table-of-contents)

#### [Swift retrospective](https://www.linkedin.com/learning/swift-5-essential-training/swift-retrospective-14527899?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/swift-retrospective-14527899?u=76281980&t=0)** - [Instructor] At its core, [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) is a compiled programming language that works with, rather than replaces, the Objective-C modules and frameworks that Apple has developed over the years. Since its release, developers have had the ability to create projects with Objective-C and Swift code side by side. So the changeover has largely been a smooth gradient rather than an abrupt handover. Prior to 2014, iOS applications were written entirely in Objective-C, but that all changed when Swift 1.0 was released with [Xcode](../../Skills/Mobile%20Development/Xcode.md) 6. A month later, Swift 1.1 was released with Xcode 6.1, followed in early 2015 by Swift 1.2 and Xcode 6.3. Swift 2.0 was released later that same year, but with two big changes. Version 2.0 was not only a huge upgrade to the previous version of the language, but Swift 2.2 was open sourced and continues to be developed and released under the Apache 2.0 license today. Swift 3 dropped at the 2016 Worldwide Developers Conference, with Swift 4 following the same pattern and releasing in Fall 2017. Now we did have to wait a bit longer than usual for a new Swift release, but Apple came through and put out version 5.0 in early 2019.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (10), [Xcode](../../Skills/Mobile%20Development/Xcode.md) (3)
> **Versions:** 2.0 (2), 1.0 (1), 1.1 (1), 6.1 (1), 1.2 (1)
> **CLI Commands:** apache (1)
> **Code Identifiers:** ios (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Getting Xcode](https://www.linkedin.com/learning/swift-5-essential-training/getting-xcode-14525889?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/getting-xcode-14525889?u=76281980&t=0)** - [Instructor] Before writing any [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) code, we'll need to install [Xcode](../../Skills/Mobile%20Development/Xcode.md) on our local machines, which you can find by searching the app store app. Swift 5 came out with Xcode 10.2, so any later versions you may be working with are still compatible with the course. Go ahead and hit get or update your already installed version, grab a cup of coffee and wait for the download to complete. After that's done, go ahead and unzip the file and drag Xcode into the applications folder before booting it up. This is an important step as moving the program later on can cause problems. Once Xcode is safely in your applications folder, go ahead and boot it up. Right away you'll see three options in the dashboard. Start a playground, create a new project, or clone an existing project. Now an Xcode playground is exactly what it sounds like a place to test and play with code in a Sandbox environment. We'll be working exclusively in playgrounds for this course so that the results of our code are immediately displayed without having to build and run an entire application. As far as new projects go, there are quite a few options to choose from. You can start a new project from the first launch menu, like we have here, or by going to file, new, project. New projects can be for iOS, watchOS, tvOS, macOS and cross-platform. Clicking on any of the platforms
>
> **[1:33](https://www.linkedin.com/learning/swift-5-essential-training/getting-xcode-14525889?u=76281980&t=93)** is going to display their base applications, templates, and frameworks, all of which provide a solid starting point for any of your needs. Cloning a repository is as easy as registering your preferred source control account, and linking a repo. But we won't be using this feature or starting any new projects in this course. It's just important that you at least know what your options are, and where to find them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Xcode](../../Skills/Mobile%20Development/Xcode.md) (5), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2)
> **Code Identifiers:** ios (1), watchos (1), tvos (1), macos (1)
> **CLI Commands:** find (2), unzip (1)
> **Versions:** 10.2 (1)
> **Definitions:** is an  (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)

#### [Reviewing the exercise files](https://www.linkedin.com/learning/swift-5-essential-training/reviewing-the-exercise-files-14526924?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/reviewing-the-exercise-files-14526924?u=76281980&t=0)** - [Instructor] I've provided all the playgrounds that you'll need for this course in the exercise files. So go ahead and download them, and open them up. Inside, you'll see a folder named AdventureSwift_Starter, which has a playground file for each chapter of the course. And it's a good idea to create a copy of the starter project on your desktop, so you always have a clean copy of the starter project on hand. At the beginning of each chapter, you're going to want to go into your working folder and open up the corresponding chapter playground. Let's open up the chapter two variable basics playground and take a look at the editor, and what we need to know to get started. Now, playgrounds are pretty versatile and offer a lot of functionality, but the only things you need to know right now are how to access the different areas of the editor, and execute the code we'll be writing. By default, playgrounds are set up to run manually when you press the play button icon next to a line number. This is going to execute all previous lines. However, this can get annoying when you're learning a new language. So if your machine can handle the extra processing load, go ahead and click on the little arrow next to the play button at the bottom of the debug area here. And we'll say automatically run. I have set up all the playgrounds to automatically run, but in case one of them is off, go ahead and toggle that back. Up in the top right-hand corner, you'll see three groups of buttons, but we're only concerned with the three on the far-right with the different page icons. Hitting the farthest on the left
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/reviewing-the-exercise-files-14526924?u=76281980&t=94)** is going to expand or hide the navigator where you can see each playground file and the overall structure. Every chapter playground has its own page detailing the topic objectives of each video, as well as a section to fill in code we'll write together. At the bottom of each page, there are next and previous buttons for flipping back and forth like a book. By the end of the course, the goal is for you to have a solid [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) reference guide that you've created yourself and can refer back to anytime. If we hit the middle page icon with the bar on the bottom, this will expand or hide the debug area, which is where all printed output shows up. However, it's not always necessary to manually print information to the debug console, as the gray area running along the right side of the main text area is going to display your variables and computations every time the playground runs. Now, it is empty right now, but as soon as we start writing our first lines of Swift, that'll populate. Finally, the page icon with the bar on the right controls the Inspector window, which gives you access to specific information about a given file. Now we're not going to use this, but if this pops up or you accidentally hit it, just hide it again. All of the texts in the playgrounds I've prepped for you are written in markup, which I used to format the non-code text. If you happen to open a playground and see a file that looks like this, go to the Editor menu and select Show Rendered Markup.
>
> **[3:10](https://www.linkedin.com/learning/swift-5-essential-training/reviewing-the-exercise-files-14526924?u=76281980&t=190)** If you accidentally delete any of that, you can just command + Z to undo, and it'll pop right back. Each chapter's also going to have a separate challenge playground for you to follow along with. If we open up the chapter two challenge, you'll see that it includes two files, a starter and a final. You should try the challenges on your own in the starter file, and follow along with my solutions in final. All the tasks and objectives that I'll ask you to fulfill are written out right here. Lastly, we've got a final version of each playground in the chapter folders that has all the code we'll be creating in the course. With that, we're ready to get going on our Swift adventure.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3)
> **Exercise Files:** starter project (2), exercise files (1), starter file (1)
> **UI Navigation:** click on (1), toggle (1), go to (1)
> **Prerequisites:** set up (2), you'll need (1)
> **Documentation:** reference guide (1)
> **Speakers:** - [instructor] (1)


### 2. Getting to Know Your Data

[↑ Back to Table of Contents](#table-of-contents)

#### [The quest begins: Variables](https://www.linkedin.com/learning/swift-5-essential-training/the-quest-begins-variables-14530749?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/the-quest-begins-variables-14530749?u=76281980&t=0)** - If you think about a programming language in physical terms, variables and types are it's building blocks, it's atoms. They're the foundation and come in different shapes and sizes according to their functions. In this chapter, we're going to work with basic variable types common to all languages, including strings, integers, and Booleans. We'll also be introducing optional types, which are unique to [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md). These allow us to gracefully handle variables with nil values without bringing our code to a crashing stop. We've got a lot to cover, so let's start out on our Swift adventure.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2)
> **Speakers:** - if (1)

#### [Variables and constants](https://www.linkedin.com/learning/swift-5-essential-training/variables-and-constants-14529808?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/variables-and-constants-14529808?u=76281980&t=0)** - [Instructor] All right, if you have the Chapter 2 Playground from the AdventureSwift working folder on screen, click on the variables and constants page. If not, go ahead and open that up, and let's talk about variables. So in [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md), you have two primary options for declaring variables, depending on how you plan to use them. Variables that need to be changed after they're created start with the VAR keyword, which tells the Swift compiler the value can be modified. Now to see this in action, let's complete our first objective and declare a changeable variable to store a player's health and assign it a value of 90. Start with the VAR keyword. We'll call this currentHealth. We just use the equals symbol to assign it a value. Now, Swift does encourage the use of camel case for names. So we have the first letter of our variable name lowercased, and then all following first letters are in caps. You'll also notice that we don't use any kind of semi-colon or line ending symbol, because Swift doesn't make us do that. It's very clean and very efficient. Now that's about as basic a variable as we can get, but in many cases, you'll want a variable that has a set unchangeable value. We can do that by declaring a constant with the let keyword, followed by the variable's name and value assignment. So we'll call this maxHealth, since this will never change,
>
> **[1:32](https://www.linkedin.com/learning/swift-5-essential-training/variables-and-constants-14529808?u=76281980&t=92)** and give it a value of 100. Just to make sure we understand the difference here, let's try changing the value of currentHealth and maxHealth to different numbers. Like most programming languages, variables are accessed by name, so we'll say currentHealth, and we'll say he's on his last legs, so he's at 13. Now that's fine, you'll see the inspector that currentHealth is now equal to 13. Now, if we change maxHealth to 110, we're going to get an error. If you click on the little stop sign icon, this will expand it, and it says "Cannot assign the value: maxHealth is a 'let' constant." So we knew that, but now we're just checking. I'm going to comment this line out with two forward slashes, so you have it for reference later. For those of you that prefer a more compact code style, Swift allows multiple variables to be declared on a single line, with the caveat that they all have to be of the same type. We'll get into types in the next video, but for now let's declare three integer values all at once by saying VAR, and we're going to call this inventory items, and we're going to set this to five, and you separate each variable with a comma. So now we can just name a new variable called equipmentSlots. We'll set this to three, and we'll declare one more, currentGold = 42. With no errors in the console, we're ready to talk about type safety and inference.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (5)
> **Code Identifiers:** currenthealth (4), maxhealth (4), equipmentslots (1), currentgold (1)
> **Env Vars:** var (3)
> **CLI Commands:** make (2)
> **UI Navigation:** click on (2)
> **Cross-References:** in the next (1)
> **Warnings:** caveat (1)
> **Speakers:** - [instructor] (1)

#### [Type safety and inference](https://www.linkedin.com/learning/swift-5-essential-training/type-safety-and-inference-14527900?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/type-safety-and-inference-14527900?u=76281980&t=0)** - [Instructor] I've already switched to the next playground page called type safety and inference, so if you're not there already, go ahead and select that and close out the navigator to give us a little bit more room. The first thing you really need to know about [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) is that it's a type safe language. Meaning that every variable you declare has to have a specific type and a value that matches that type. When you assign an initial value to a variable, the Swift compiler is really smart it infers its type without you having to specify it. So if we take our current health example from the last video, the compiler correctly infers that current health is an integer value and that's great. However, variables won't always be declared with initial values, in which case you'll need to explicitly include its type. For example, let's declare a new variable, we're going to call this character name, but instead of using the equals operator to assign it a value, we're going to use a colon and type in the string class. Now, this is what's called a type annotation, which essentially tells the compiler that character name can only hold string type values. If we try and assign character name a integer or a Boolean or something like that, we're going to get an error, because the compiler is going to be watching out for tight mismatches. Here, we can see, cannot assign value of type int to type string. To assign a string value we just need to call character name
>
> **[1:38](https://www.linkedin.com/learning/swift-5-essential-training/type-safety-and-inference-14527900?u=76281980&t=98)** equals and use an open and close set of double quotations. Now, anything inside here, is going to count as the string text, I'm going to call this King Krull for our character name. And we're not going to get into types too heavily right now, but Swift does support the fundamentals like floats and doubles for floating point numbers, bulls for true and false values, and collection types like arrays in dictionaries. If you really like your code extra readable and explicit, you can combine type annotations and initial values when you create your new variables. But to be clear, the compiler doesn't need both. So we could say var max health of type int equals a hundred. It's overkill, but if that's your thing, go for it. Since we've already seen multiple variable declarations on the same line, it's good to know that we can also specify their types with a single annotation, as long as we add it to the very end of the variable. So let's say var inventory items again, comma equipment slots, comma, current gold, and here at the very end we'll add a colon, declare all three of these as an integer, and that'll do it. Just to note here before we move on, we'll be declaring most of our variables we work with as vars or mutable values, but the syntax is the exact same for let constants.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3)
> **Definitions:** is an  (1)
> **Analogies:** for example (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Logging and commenting](https://www.linkedin.com/learning/swift-5-essential-training/logging-and-commenting-14527903?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/logging-and-commenting-14527903?u=76281980&t=0)** - [Instructor] Now that we know how to actually create variables, it's important to take a quick look at how to comment and debug the examples we'll be working with. And we've already seen a single line comment. That's two forward slashes followed by any kind of text that you want the compiler to ignore. So I'm going to say I'm a single-line comment. A multi-line comment starts with a single forward slash and an asterisk symbol and is closed out by the reverse. So an asterisk symbol and a forward slash. Now, anything on any line that goes between here is commented out. Now, these can even be nested comments, so I'm a nested comment, as long as it's within our asterisk and slashing brackets. As far as logging specific output to the debug area, the print function is the way we're going to do that. So let's create a simple variable. I'm going to call this antiHelloWorld. Again, we're going to assign it a string value that just says, we're not here. Functions in [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) are called by name, just like variables. So to display the value of antiHelloWorld, we can just say print and the IntelliSense is going to come up and give us that function and we can just feed it antiHelloWorld. Once the playground executes, you can see our strings show up in the console
>
> **[1:32](https://www.linkedin.com/learning/swift-5-essential-training/logging-and-commenting-14527903?u=76281980&t=92)** at the bottom of the editor, as well as in the inspector on the right. If you go to the inspector and mouse over the gray box, you'll see two options show up. There's a little I icon, which is a quick look, or you can click the gray box and that'll put the display output right underneath the function call. So whichever debug method floats your boat, that's the way you can do it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Code Identifiers:** antihelloworld (3)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Swift operators](https://www.linkedin.com/learning/swift-5-essential-training/swift-operators-14526928?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/swift-operators-14526928?u=76281980&t=0)** - [Instructor] A programming language isn't really complete without a way to operate or modify your variables. And [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) is really no different. Now let's start off with a basic math operation and go from there. So I'm going to declare a new variable. I'm going to call this usingMath in real life, and I'm going to assign this an actual mathematical expression. I'll say 5 + 4 - 3 / 2 * 1.
>
> **[0:31](https://www.linkedin.com/learning/swift-5-essential-training/swift-operators-14526928?u=76281980&t=31)** Now you've undoubtedly seen these type of arithmetic operators before. And Swift follows the conventional order of operations, evaluating parentheses first, then exponents, multiplication, division, addition, and finally subtraction. You can see that in action by adding a pair of parentheses around the 4 - 3 operation, which is going to give us a different result than 8. It's actually going to evaluate to 5. Next up, we should look at compound assignment operators, which are used as a shorthand replacement for any of the math operations. Let's say currentHealth again, and I'm going to set this to 45 this time. And on the next line, I want currentHealth to be multiplied by, let's say, 5. So we could say currentHealth = currentHealth * 5. That's the longhand way to do it. The shorthand or Swifty version is to use the asterisk and an equal sign and give it the value we want to multiply it by. Now this syntax works for every arithmetic operator, so you can see I commented them out for you here if you need to reference them later. Now the final operation we're going to look at in this video is called the Modulo. It's written with the percent symbol and returns the remainder of two numbers being divided. So for instance if we say var experienceOverflow, we're going to set this to 100 % 90.
>
> **[2:10](https://www.linkedin.com/learning/swift-5-essential-training/swift-operators-14526928?u=76281980&t=130)** Since 100 divided by 90 is 1, there's a remainder of 10 left, and that's actually what experienceOverflow is going to store for us. Now that's all the operators that really make sense to go through by hand with our current Swift skills, but we will cover the remaining comparison, logical, and range operations when we get to the topic of [Control Flow](../../Skills/Software%20Development/Control%20Flow.md). If you want to jump the gun or you're just itching to go, I have commented out the comparison, logical, and range syntax for you here at the bottom of the playground if you want to get started early.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3), [Control Flow](../../Skills/Software%20Development/Control%20Flow.md) (1)
> **Code Identifiers:** currenthealth (4), experienceoverflow (2), usingmath (1)
> **CLI Commands:** make (1)
> **Definitions:** is called (1)
> **Analogies:** for instance (1)
> **Speakers:** - [instructor] (1)

#### [Understanding strings](https://www.linkedin.com/learning/swift-5-essential-training/understanding-strings-14530754?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/understanding-strings-14530754?u=76281980&t=0)** - [Instructor] The string class is going to be a constant building block in any [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) application you put together. So we're going to spend a little time understanding its basic features. And the string is literally a collection of characters strung together that can be manipulated, mutated and accessed in various ways. There are also Unicode compliant in Swift and can handle storing special characters. Now let's run through what we already know. We can create string variables without a value by giving them a name. So I'm going to say activeQuest and the string type annotation. Here activeQuest is declared, but not initialized, meaning that the compiler is going to throw us an error if we try and print it out at this point. Now to get around this when you need to initialize a string variable with some kind of empty value, we can just assign it an open and closed set of quotes. And this is largely a style point, but it can be useful to have empty strings or other variable types when displaying missing data or something like that in your app. Finally, we can assign an actual string to our activeQuest variable. I'm going to call this quest, Retrieving the Cart. Why not. Quick note here when you assign an actual string or any kind of type really, to a variable, it's called a literal value.
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/understanding-strings-14530754?u=76281980&t=94)** So that is the difference between using a literal value, like Retrieving the Cart, and using activeQuest, which is the variable that's holding the literal value. If you hear me refer to a literal type, like an integer literal, string literal, et cetera, that's what I'm talking about. The difference between the actual value and the variable that's storing that value. Now one of the great things about Swift is how easy it makes traditional programming operations. For instance, if we want to add or concatenate two strings together, we can just use the plus operator. Just declare a new variable. We're going to call this questDifficulty and assign this a string literal. I'm going to call that Nightmare. I'm going to make a third variable. I'm going to call this questInfo, and this is going to be equal to activeQuest. Here we use the plus operator to append a string, and we're just going to give this the arrow plus questDifficulty. Notice that I included the blank spaces in the middle string to make sure that this reads correctly, 'cause the compiler, if it's missing, is just going to add on these strings without spaces. And that doesn't look right. If you want to trim down the syntax, 'cause this can get kind of bulky,
>
> **[3:07](https://www.linkedin.com/learning/swift-5-essential-training/understanding-strings-14530754?u=76281980&t=187)** you can use the compound operator with plus equals. If we reference questInfo, and say plus equals a new string literal. I'm just going to add an exclamation point. This literally adds that additional character onto the end of questInfo. And having all these plus symbols or plus equals operators really does bloat string code. So it's usually a better idea to use string interpolation when you're constructing patchwork text. String interpolation is just a fancy way of describing a string literal that has other values inserted into it. So let's do a final example here and print out an interpolated string. This is going to be a constant. I'm just going to call this questInfo_Interpolated. And I'm going to write out the string without any insertions first, I'm going to say, I'm not sure you're ready to yet, it's level. Now I know that doesn't read grammatically correct. So let's fix this. Now the syntax to insert a string into another string is a backslash, open parentheses, close parentheses. Now anything that goes inside here will be interpolated or inserted, so we can use variables or literal values. But right now we already have questDifficulty and activeQuest, so I'm going to say, I'm not sure you're ready for
>
> **[4:42](https://www.linkedin.com/learning/swift-5-essential-training/understanding-strings-14530754?u=76281980&t=282)** activeQuest yet it's, and I'm going to put in the questDifficulty here, level. Now our entire string reads, I'm not sure you're ready for Retrieving the Cart yet. It's Nightmare level. I want to stress that this kind of interpolation is not limited to string variables. You can put in numbers, you can put in booleans, you can put in anything that can be translated into a text representation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3)
> **Code Identifiers:** activequest (7), questdifficulty (4), questinfo (3)
> **CLI Commands:** make (2)
> **Definitions:** is a  (1)
> **Analogies:** for instance (1)
> **Speakers:** - [instructor] (1)

#### [Working with strings](https://www.linkedin.com/learning/swift-5-essential-training/working-with-strings-14529807?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/working-with-strings-14529807?u=76281980&t=0)** - [Instructor] Let's keep going with strings and start working with the functionality that [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) already has built into the class. First though, let's declare a new test variable that we can work with. I'm going to call this dialogue. I'm going to give this a string literal. Just says the Innkeeper's Haven. Let's get some data on the dialogue variable by printing out how many characters it has and whether it's empty or not. So we can access any properties or methods on a class using the dot notation like in most programming languages. So we can say dialogue.count and dialogue is empty. And these are going to kick back 21 and false respectively. So 21 characters in dialogue at the current time and it's not empty. These are super useful when dealing with form fields or other logic that depends on a string's context. But counting is empty are available on almost all Swift classes. So use them accordingly. We can also query whether a string contains a specific character or string of characters using the contains method. So we can say dialogue.contains. I'm just going to look for that capital S character. And this is going to kick us back a true or false value. Right now it's false because contains is case sensitive. So if we give it a lower-class S, it's going to return true. Now sometimes the concatenate operator
>
> **[1:35](https://www.linkedin.com/learning/swift-5-essential-training/working-with-strings-14529807?u=76281980&t=95)** or string interpolation isn't going to cut it when you're manipulating a string value, which is where append and insert come in. Append is just a more explicit way of adding a new string onto the end of an existing one, but works on the same principle as the plus operator. If we type in dialogue.append, we'll see that there are a variety of options like most classes in Swift. We're just going to append contents of string. And here I'm just going to say a comma, weary travelers, exclamation point. Now if we wanted to insert or remove characters or strings at a specific point in our dialogue variable, we could do that with the insert and remove functions. All we need is a position or what's called an index. Our collections in Swift are zero indexed meaning that they started zero and increment up. We take a look at our dialogue string right now. The character at position zero is T, one is H, two is E and so on. However, getting into constructing string indexes at this point in our Swift adventure would be more confusing than helpful. For special topics like this, the docs have you well covered. That being said, there are a few simpler remove methods that we can use.
>
> **[3:07](https://www.linkedin.com/learning/swift-5-essential-training/working-with-strings-14529807?u=76281980&t=187)** For instance, dialogue.removeLast will take off the last character and return it to us. Let's just print out dialogue just to make sure we're seeing this in action. Now, the exclamation mark is gone. We can use dialogue.removeFirst to take off the lowercase T and we can even use dialogue.removeAll to entirely clear it out. Now it's just an empty string again. The last method we're going to look at here is split, which is helpful for going through a string and basically looking up any character we specify and returning us the string before it and the string after it. So since our dialogue string has a handy comma, almost in the middle, we're going to split on that. Now there are a lot of options again with split. We want the first one with a separator, and we're just going to look for a single character, which is the comma. Now to get this to actually work, we need to comment out our removeALL call because that just emptied out the string. And here we can see that the split method has essentially kicked us back an array. And we haven't talked about arrays, but they're collections of values. So in this case, we have two values. He Innkeeper's Haven is one which was on the front end of the comma and weary travelers, which was on the end.
>
> **[4:42](https://www.linkedin.com/learning/swift-5-essential-training/working-with-strings-14529807?u=76281980&t=282)** Like we've seen all of these functions and really all Swift classes in general, come with different variations. If you're looking for a specific functionality, look to the docs as the Swift team may have already provided what you're looking for. It's also a good idea to familiarize yourself with core class methods, like string or integer classes that do a lot of the, I would say more common functions or operations that you'll be using day to day.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (7)
> **Code Identifiers:** removeall (2), removelast (1), removefirst (1)
> **Documentation:** the docs (2)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Analogies:** for instance (1)
> **Speakers:** - [instructor] (1)

#### [Type conversions](https://www.linkedin.com/learning/swift-5-essential-training/type-conversions-14530753?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/type-conversions-14530753?u=76281980&t=0)** - [Instructor] Now that we've got some strings and numbers under our belts, let's do some work with conversions. Now the [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) compiler will do the majority of the work when you're doing anything mathematical. But when it comes to converting between types, well, that takes a little extra effort. To start with, let's declare a test variable here, we're just going to call this currentGold_Double. I'm going to give this a literal value of 5.832. Now, if we were working with only Doubles, we wouldn't have to do any extra syntax, compiler will take care of everything. But, let's say we want to convert currentGold Double to an integer. Well, we can say var currentGold_Integer equals, actually before that, let's give it a explicit type annotation enforcing that we only want this variable to hold Int, and just say currentGold Double, and see what happens. Now we've got an error, as expected, cannot convert value of type Double to type Int. So how do we actually fix this? Well, with explicit conversions, you're going to wrap the variable you want to convert in parentheses, and then at the very beginning, you're going to add the type you want it to be converted to. Now in the right-hand inspector you'll see that currentGold Integer is now five, instead of 5.832.
>
> **[1:37](https://www.linkedin.com/learning/swift-5-essential-training/type-conversions-14530753?u=76281980&t=97)** Because the compiler has shaved off the decimal values during the conversion. And this is called Loss of Precision, which is going to happen anytime you take something like a decimal or a floating point number, and try and squeeze it into a smaller type, which is an Int that can't hold decimals. You'll also notice that when it shaves off those decimal values, it's not rounding, it's not going up to six because we're above 5.5 or something like that, which does happen in other languages. It's just going to simply chop it off. Our numbers aren't the only type Swift is going to let us explicitly convert. For instance, we could say var currentGold_String, and do away with the type annotation, and we'll just say String(currentGold_ Double). And you can see in the inspector on the right, that it just transformed 5.832 into a string literal. No problem whatsoever, now that's a lot harder in some other languages, so be thankful. Now conversions are great, but one thing you need to be aware of going forward is how the compiler is going to deal with operations between variables of different types. So what do I mean by that? Well, let's say we want to add two variables together of different types. So we'll say var bankDeposit equals 37, which is an integer, plus 5.892, which is a double.
>
> **[3:11](https://www.linkedin.com/learning/swift-5-essential-training/type-conversions-14530753?u=76281980&t=191)** Because we're using number literals here, the compiler is just going to infer the result to be a Double by default, it's going to take the most accurate result, it doesn't want to chop anything off or lose precision. However, if we want to add two variables of different types together, we're going to have to explicitly tell the compiler what we want the result to be. So let's say var bankDeposit_Explicit, and let's try and add our currentGold Double plus currentGold Int. All right, again, we're going to get a tight mismatch error of sorts, binary operator '+' cannot be applied to Doubles and Ints. Essentially what this is telling us, or the compiler is trying to say, is that it doesn't know what we want bankDeposit_Explicit to be. Now I want this to retain its precision, so I'm going to actually do an explicit conversion right here in the expression. I'm just going to say I want the currentGold Integer to be converted to a Double, and then added to currentGold Double. Now what we've done is told the compiler that we want to keep all the precision of our Doubles. So our result is 10.832, and bankDeposit Explicit is inferred as a Double. Keep in mind here that the compiler is really smart in Swift,
>
> **[4:44](https://www.linkedin.com/learning/swift-5-essential-training/type-conversions-14530753?u=76281980&t=284)** and it's always going to deliver the correct result type. But that might not be the type you want or need at the end of an operation. So, always be sure that you're essentially giving the compiler enough information.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3)
> **Code Identifiers:** currentgold (7), bankdeposit (2)
> **Versions:** 5.832 (3), 5.5 (1), 5.892 (1), 10.832 (1)
> **Definitions:** is an  (2), is called (1), is a  (1)
> **Warnings:** be aware (1), keep in mind (1)
> **Analogies:** for instance (1)
> **Speakers:** - [instructor] (1)

#### [Booleans and logical operators](https://www.linkedin.com/learning/swift-5-essential-training/booleans-and-logical-operators?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/booleans-and-logical-operators?u=76281980&t=0)** - [Instructor] Boolean variables can hold either a true or false value, and are declared using the Bool keyword. And these are very handy for setting up flags or pairing with comparison operators to do logic checks. They also follow the same type inference rules as we've seen, so you can declare them with initial values or without, depending on what you need. Let's start off by declaring a new variable. We're going to call this isActive. And just to be clear, I'm going to give this a type annotation type Bool, and set it to false. That's all there really is to declaring booleans so let's practice with logical operators. When you need to reverse or toggle the state of a boolean value or expression, the not operator is the way to go. So we can say isActive = !isActive. Now you'll hear this referred to as the bang operator, as well as the NOT, which essentially just sets the value to its opposite. This is going to become second nature to you when you start writing applications that require states to be toggled back and forth, so just keep it in mind. When there's more than one boolean value to evaluate, you'll need to use two logical operators, the AND which is written with two ampersands, or the OR, which is written with two pipes. Let's declare another test variable.
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/booleans-and-logical-operators?u=76281980&t=94)** We're going to call this canMove. We're also going to set this to false, and let type inference do its thing. And we're going to evaluate both of these. So we're going to say var playerControl = canMove && isActive. When we use the AND operator, both values have to be true in order to return a true value. Otherwise it's false. So right now isActive is true, but canMove is false. If we trained canMove to true, playerControl will be true. However, if we replace the AND or the double ampersands with the OR operator, which is written with two vertical pipes, Player Controller is going to evaluate if either canMove or isActive is true. So both of these are true. Therefore playerControl is true, but if we change canMove back to false, playerControl is still going to be true. Only needs one or the other.

> [!info]- Semantic Content
>
> **Code Identifiers:** isactive (6), canmove (6), playercontrol (4)
> **UI Navigation:** toggle (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Introducing optionals](https://www.linkedin.com/learning/swift-5-essential-training/introducing-optionals-14527895?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/introducing-optionals-14527895?u=76281980&t=0)** - [Instructor] If you're coming from a C-style language, the concept of objects having nil values shouldn't be news, but in [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md), any variable type can hold a nil value as long as it's declared as an optional. Optionals tell the compiler that the variable is either storing a value or storing nothing, which is useful when you need placeholders for potentially unknown values without crashing your code. I'm going to declare two optional variables here. They're declared just like any other. So we'll say var, and I call this itemGathered, give it a string type, and to mark it as an optional, we're going to add a question mark. I'll do the same thing and name a new variable, isExchangeable, and this is going to be an optional boolean. You can see in the inspector that both of these, because they lack initial values, are stored as nil, which is fine. That's not to say that we can't have initial values. So let's add pickaxe to itemGathered. Now, if we try and print out itemGathered, we're going to see something interesting. It's not actually going to print out the value or the string pickaxe. It's going to print out the optionally wrapped pickaxe. Now this is Swift's way of telling us that we need to gracefully and safely unwrap the string value before getting to it. We'll also get a warning
>
> **[1:33](https://www.linkedin.com/learning/swift-5-essential-training/introducing-optionals-14527895?u=76281980&t=93)** if you click on the yellow triangle at the top of the editor, that our expression is implicitly coerced from string optional to any, which is a very confusing way of saying that itemGathered has not been unwrapped or addressed as an optional. What we can do for optional values is unwrap them in two ways, using optional binding or force unwrapping. We're going to get to optional binding later on when we talk about [Control Flow](../../Skills/Software%20Development/Control%20Flow.md), so for now let's force unwrap itemGathered by adding an exclamation mark. Now the actual string pickaxe is printed out, and it's no longer an option. Now be careful with force unwrapping because essentially what you're doing is telling the compiler that you're 100% sure the optional is not nil, and to immediately unwrap it without checking. If we tried to print out and force unwrap isExchangeable, we're going to get a crash because it's nil, and we basically overrode the compiler and took away that safety measure that it's implementing for us. So let's comment that out, just so we can keep going with our program. Now from my point of view, forced unwrapping sort of defeats the purpose of having optionals in the first place. They exist specifically to save you from having to remember if a certain variable is nil or not. So don't form a habit of force unwrapping. To sum up,
>
> **[3:04](https://www.linkedin.com/learning/swift-5-essential-training/introducing-optionals-14527895?u=76281980&t=184)** optionals either store a value or nothing at all. They're compatible with variables or constants, and can be declared with any type.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2), [Control Flow](../../Skills/Software%20Development/Control%20Flow.md) (1)
> **Code Identifiers:** itemgathered (5), isexchangeable (2)
> **Warnings:** warning (1), be careful (1)
> **UI Navigation:** click on (1)
> **Definitions:** is a  (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Accessing documentation](https://www.linkedin.com/learning/swift-5-essential-training/accessing-documentation-14528849?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/accessing-documentation-14528849?u=76281980&t=0)** - [Instructor] One of the great things about developing iOS apps is the detailed resources Apple provides to the development community. If you have a developer account, you can access the documentation online, but it's usually more efficient to jump directly into a specific topic from the editor. You can access detailed documentation for any class by highlighting its type, going up to the Help menu, selecting Search Documentation for Selected Text. Now this is going to show what [Xcode](../../Skills/Mobile%20Development/Xcode.md) thinks you're looking for. In this case, yes, we're looking for the string class. And this will give you a rundown of all its uses, its features, really everything you need to know from right inside the editor. However, opening up a brand new Xcode window is kind of troublesome, and it's not really efficient when you're really deep into a coding problem. So if you highlight string class again, you can hit Command and right click, which will open up the Actions menu. And we can either jump to the definition, which is going to take you back to where we just were, or you can hit Show Quick Help, which just pops up all that information right here without any new [Windows](../../Glossary/Service/Windows.md). When learning a new language, it's always good to know where to get help, so make constant use of these docs as you're continuing your [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) journey.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Xcode](../../Skills/Mobile%20Development/Xcode.md) (2), [Windows](../../Glossary/Service/Windows.md) (1), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** ios (1)
> **Documentation:** the documentation (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Player stats](https://www.linkedin.com/learning/swift-5-essential-training/challenge-player-stats-14526917?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/challenge-player-stats-14526917?u=76281980&t=0)** (upbeat electronic music)
>
> **[0:06](https://www.linkedin.com/learning/swift-5-essential-training/challenge-player-stats-14526917?u=76281980&t=6)** - Now, I've got my first challenge for you. We're going to focus on creating player stats, different variable types, performing basic operations on them, and printing them out. I've chosen to leave out optionals from this challenge because I haven't shown you the correct way to handle them yet, and I don't want you to get into any force unwrapping bad habits. Feel free to take this challenge beyond what I've laid out if you feel inspired. These challenges are just a baseline. They're not a ceiling. Now this shouldn't take you more than 5 to 10 minutes, but if you're feeling stuck, refer back to the playground examples, or jump into the next video and follow along with my solution. Happy coding.

> [!info]- Semantic Content
>
> **Cross-References:** next video (1)
> **Speakers:** - now (1)
> **Non-Speech:** (upbeat electronic music) (1)

#### [Solution: Player stats](https://www.linkedin.com/learning/swift-5-essential-training/solution-player-stats-14528851?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/solution-player-stats-14528851?u=76281980&t=0)** (bright upbeat music)
>
> **[0:06](https://www.linkedin.com/learning/swift-5-essential-training/solution-player-stats-14528851?u=76281980&t=6)** - [Instructor] All right, I'm going to jump into my solution to the chapter challenge. You can follow along or see the full code in the final playground page. For the first task, we want to create two string variables and assign them values. I'm going to say var characterName, Harrison, and var weaponClassification is going to be hands and fists.
>
> **[0:39](https://www.linkedin.com/learning/swift-5-essential-training/solution-player-stats-14528851?u=76281980&t=39)** Our second task is to use the plus equals operator to add a nickname string onto characterName. So, I'll say characterName plus equals space the Stealthy.
>
> **[0:55](https://www.linkedin.com/learning/swift-5-essential-training/solution-player-stats-14528851?u=76281980&t=55)** Third task we want to create a variable called currentMana, and assign it a decimal value. I'm going to set mine to 75.9. Next, we want to create another variable called manaPercent, and we're going to assign it currentMana divided by 100. The catch is that we want to explicitly convert it to a string. So, I'm going to start out. I'm just going to upfront declare it as a string, and inside the parentheses, I'm going to take currentMana and divide it by 100. For task number five we're going to create another variable called debug stats, and we're going to use string interpolation to print out our information in sort of a creative way. So, we'll say var debugStats equals, and I'm going to use string interpolation here. I'm going to print out my characterName, and I'm going to use the backslash N to create a line break. So, I'm actually going to have two of these. I'm going to say prefers. I'm going to use my weaponClassification. One more line break, current mana, and I'm going to insert my mana percentage.
>
> **[2:38](https://www.linkedin.com/learning/swift-5-essential-training/solution-player-stats-14528851?u=76281980&t=158)** For task number six we're going to create two booleans, and assign them some values. So, I'm going to say var questAccepted is going to be false, but canQuest is going to be true. Task number seven is going to see us using the and operator to evaluate if questAccepted and canQuest are both true. We want to store this in a variable. So, I'm going to call the variable questStatus. I'm going to say questAccepted and canQuest.
>
> **[3:20](https://www.linkedin.com/learning/swift-5-essential-training/solution-player-stats-14528851?u=76281980&t=200)** For our last task, we're going to add an interpolated string that includes our questStatus onto debugStats. The caveat here is that we need to use the append method. So, we'll say debugStats.append contents of sequence, and I'm going to give this a line break right off the bat. Say questing, and I'm going to use the questStatus variable.
>
> **[3:53](https://www.linkedin.com/learning/swift-5-essential-training/solution-player-stats-14528851?u=76281980&t=233)** Go ahead and print this out, and let's see our work. I'm going to expand the debug area a little bit, just so we can see all this, but it looks pretty good. Harrison, the Stealthy. I've got my weapon classification, my current mana percentage, and I am currently not able to quest.

> [!info]- Semantic Content
>
> **Code Identifiers:** charactername (4), currentmana (3), debugstats (3), questaccepted (3), canquest (3)
> **Versions:** 75.9 (1)
> **Warnings:** caveat (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright upbeat music) (1)


### 3. Working with Collections

[↑ Back to Table of Contents](#table-of-contents)

#### [Gathering inventory with collections](https://www.linkedin.com/learning/swift-5-essential-training/gathering-inventory-with-collections-14525882?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/gathering-inventory-with-collections-14525882?u=76281980&t=0)** - Now, that we've got variables, and types under our belts, we can explore collections. These special types are going to let us store data that basic types can't, like a list of high scores or the items and pricing of a shop's inventory. We'll also be looking at different ways to access and modify specific collection elements, as well as the common operations they come with right out of the box.

> [!info]- Semantic Content
>
> **Speakers:** - now (1)

#### [Swift arrays](https://www.linkedin.com/learning/swift-5-essential-training/swift-arrays-14524893?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/swift-arrays-14524893?u=76281980&t=0)** - [Instructor] With the basic [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) types out of the way, we can dive into our next playground, which is chapter three and our first collection type, arrays. Now, arrays work like ordered lists, meaning that each element or value in the collection is organized by the order it was added. So you'll see these used when data needs to be in a specific sequence. Let's start by declaring an array without an initial value, and I'm going to call this, level Difficulty. Now the array syntax or type annotation is two brackets and inside the brackets, the type of value we're going to store. So in this case, this is going to be an array of strings. In cases where you need an empty initialized array, just like we did with the empty string, you can assign it a pair of empty brackets. That'll let the compiler know that it's an empty array, but it has been initialized. This is the preferred style you'll see in the Swift docks and most community projects. But just to be thorough, you can also declare arrays with some added syntax. So we'll just say this is called extraSyntax1, going to use the array keyword with an open arrow, and a closed arrow, and the string type inside. I'll declare an empty array using this syntax, we use Array, open arrow, string, closed arrow and a set of parentheses.
>
> **[1:33](https://www.linkedin.com/learning/swift-5-essential-training/swift-arrays-14524893?u=76281980&t=93)** That'll also initialize it. The compiler treats these variations the exact same. So again, it's a style choice. Swift types often have different syntax approaches, but going forward, I'll be sticking to the concise style of level difficulty. Now let's add in some string values to level difficulty. I'm going to say our first element is going to be Easy separated by a comma. Second element is moderate. Third is veteran, and fourth is nightmare. I'm going to leave the type annotation here for the example, but type inference does work with collections. Like with strings, we can get the "is empty" and "count properties" of arrays by using the dot notation. So we'll say level difficulty.count and level difficulty is empty. And we have four elements, so that's what's going to return from count and we are not empty. Each array item is given an index value that you can use to access and or modify its value by using what's called subscripts syntax. Arrays are zero-indexed, again just like strings, meaning that our first item in level difficulty easy is at index zero. So for instance, if we want to access the last item in level difficulty, we'll call this most difficult. We can assign it level difficulty, open and close brackets,
>
> **[3:13](https://www.linkedin.com/learning/swift-5-essential-training/swift-arrays-14524893?u=76281980&t=193)** and the index of three. The open and closed brackets here are the subscripts syntax. So what we're saying is please query level difficulty at item three, which is nightmare. We can also use subscripts to change an item's value. So if we want to change nightmare, which is at index three to some other string, we can do that. We'll call this utter ridiculousness. Now nightmare has been overridden and utter ridiculousness is now inside level difficulty. Now the thing to be careful of with arrays is that if you try and reference an index that doesn't exist, the compiler's going to throw you a nasty out of range exception. So let's try that just for fun and try and query level difficulty at index four. Well that doesn't exist, there are only three items. Now you might be thinking if we can't dynamically add items to the array, what real good is it? It's not going to help us a lot in our Swift apps. In the next video, we'll talk about exactly that and how to dynamically modify the contents of an array after it's created, which is the more common use case.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (4)
> **Analogies:** just like (2), for instance (1)
> **Code Identifiers:** extrasyntax1 (1)
> **Cross-References:** in the next (1)
> **Definitions:** is called (1)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)

#### [Core array methods](https://www.linkedin.com/learning/swift-5-essential-training/core-array-methods-14526918?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/core-array-methods-14526918?u=76281980&t=0)** - [Instructor] The next step with arrays is learning how to dynamically manipulate them. Now, they come with several handy methods right out of the box, including ones we've already seen, like append, insert, remove, and sort, which is going to be a new one. Let's start by creating a new array. We're going to call this characterClasses. I'm going to use the concise syntax and let the compiler infer the type. So this is going to be an array of strings again. Ranger, Paladin, and Druid.
>
> **[0:41](https://www.linkedin.com/learning/swift-5-essential-training/core-array-methods-14526918?u=76281980&t=41)** And down at the third comment, I'm going to add a print statement so that we can see these change sort of in real time and know what's going on. To add a new item to the end of the array, we're going to say characterClasses.append newElement. Type String. And we're going to add a new class called Gunslinger. You can also use the += compound operator the exact same way. So we'll say characterClasses +=. This time we're going to add Healer, and Berserker. So you can see that you can add more than one item at a time. Again, like with strings, we can insert and remove items from an array based on their index. So this is where the dynamic modification comes in. So we'll insert a new element. Character class is going to be Beast Master. And we'll put it at index two. You'll see that when our new item goes in, all the following ones get bumped up one index. So nothing gets overridden. They just get moved. Removal actually works the same way. We'll say characterClasses.remove at, and all we have to do is supply an index. So we'll take out Paladin, and now we just have Ranger, Beast Master, Druid, Gunslinger, Healer, and Berserker. We can also adjust the order of an array
>
> **[2:17](https://www.linkedin.com/learning/swift-5-essential-training/core-array-methods-14526918?u=76281980&t=137)** by using either the reverse to modify index order directly or reversed to leave the original array as it is. So let's do reverse first. And we'll be able to see that now the exact items are in a reversed order. The same is true of alphabetical sorting. [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) makes that super easy with the sort method. And now we will see that it is alphabetical. B, B, D, G, H, R. Perfect. And like I mentioned, both of these functions are actually performing the operation on our characterClasses array, which isn't always ideal because you want to keep a clean copy of your data. So let's take a look at the other method, reversed and sorted, that return a copy. So we'll say reversedClasses = characterClasses.reversed.
>
> **[3:31](https://www.linkedin.com/learning/swift-5-essential-training/core-array-methods-14526918?u=76281980&t=211)** Can also do the same thing with sort. So we'll say sortedClasses characterClass.sorted. And checking for a specific item is just as easy as checking a string for a character thanks to the contain method. Contains is case-sensitive, and you have to be aware of your spelling. So if we look for gunslinger with a lowercase G, we're going to get false. But if we change that to a capital G, we'll get true. Perfect. So far here, we've only done the basics of dynamically modifying arrays, but what happens when we create 2D or jagged arrays? That is, arrays that store arrays as their values. Well, let's take a practical approach here. We're going to take a new variable called skillTree, and I'm going to add an annotation just to be super clear about what we're doing. So inside our array are going to be arrays of strings for their values. Now, you'll see 2D or jagged arrays declared like this on multiple lines so that the syntax is more readable here when you give it initial values. For the first item, it's going to be an array itself. Say Attack+ for the first item. Barrage and Heavy Hitter.
>
> **[5:08](https://www.linkedin.com/learning/swift-5-essential-training/core-array-methods-14526918?u=76281980&t=308)** Since that entire array is a single element, we separate them with a comma just like we did before. For the second array, say Guard+, Evasion, and Run. Now, jagged arrays aren't required to store arrays of the same length. It just turned out this way for the example. As long as the inner arrays always store the same type, compiler is perfectly happy. And to access nested array values, we'll need to what's called chain subscripts together to get down into the data. So for instance, if we wanted to create a variable to store the second value in the first inner array, we could say attackTreeSkill = skillTree at item zero. And inside item zero, we want the third item. If we display this now by clicking the gray box in the inspector, we'll see that we correctly get Heavy Hitter. When you're dealing with nested collection types like this, it's helped to have a mental model about how to access the data. So what skillTree and the chained syntax here, we're essentially say access the entire item at index zero, which is an array. Then take the item at the second index, which is Heavy Hitter.
>
> **[6:43](https://www.linkedin.com/learning/swift-5-essential-training/core-array-methods-14526918?u=76281980&t=403)** Hopefully that helps you visualize how to drill into your data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Code Identifiers:** characterclasses (6), skilltree (3), newelement (1), reversedclasses (1), sortedclasses (1)
> **Definitions:** is a  (1), is an  (1)
> **Analogies:** just like (1), for instance (1)
> **Warnings:** be aware (1)
> **Speakers:** - [instructor] (1)

#### [Swift dictionaries](https://www.linkedin.com/learning/swift-5-essential-training/swift-dictionaries-14527901?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/swift-dictionaries-14527901?u=76281980&t=0)** - [Instructor] Like arrays, dictionaries are collection types, but instead of holding single values accessed by indexes, they hold key value pairs. Now in these key value pairs, all the keys need to be of the same type and all values need to be of the same type. Now, it's important to know that dictionary items are unordered, so their values are accessed by their associated keys not in sequence. Let's do the same thing we did with arrays and start off with a simple uninitialized dictionary. We're going to call this blacksmithShop and the syntax for a dictionary-type annotation is two brackets. The first type is going to be the type of key that we're going to store separated by a colon, a space, and the value type. So in plain English, we're saying here that blacksmithShop is going to be a dictionary of string keys and int values. To create or initialize an empty dictionary, we use the open and close brackets and a semi-colon. That's all it takes. And we can also give these initial values. So for our first key, I'm going to say an item is bottle and it costs 10. Going to I have a shield in here that costs 15 and an ocarina, which costs 1,000. Again, since dictionaries store key value pairs,
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/swift-dictionaries-14527901?u=76281980&t=94)** it's important to visualize each element as, essentially, an item with a key property and a value property. And dictionaries do have the is empty and count properties available, but since we've used those so much, we're just going to jump into more important things like accessing their values. Again, we're going to use subscript syntax. So I'm going to try and grab the shield price. I'm going to name this variable shield price and I'm going to say, blacksmithShop subscript. Now, what do we actually put in here? Well, it's not an index. It's the actual key that we use to look up our item. Now, shield price is equal to 15 because we've gone in and queried the shield key. Accessing a key value pair like this returns an optional value, which we've already talked about. That means that our blacksmithShop at key shield could be nothing. It could not exist. And we'll address that and how to gracefully handle that situation, again, when we get to [Control Flow](../../Skills/Software%20Development/Control%20Flow.md) and optional binding. We can also use subscripts with dictionaries to modify values, just like with arrays. So we can say blacksmithShop, and I'm going to change the number or the price of the bottle to 11. And that's it.
>
> **[3:06](https://www.linkedin.com/learning/swift-5-essential-training/swift-dictionaries-14527901?u=76281980&t=186)** Now, if you try and update a non-existent item, [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) is going to automatically add it to your dictionary, which is, I have to say, a really useful feature when you don't know or don't want to check each item. So we'll say blacksmithShop. I call this a towel because you always need to bring your towel and it's going to be a cost of one. Let's print this out so we can see it a little clearer. And there we go, towel is in there with bottle, ocarina, and shield, and again, these are unordered. They will appear in different orders depending on how the compiler executes. Now, at this point, you might be asking yourself, how do we access or modify keys or if there's a way to list out all the keys and values without having to essentially iterate or loop through the dictionary. The short answer is that keys are not modifiable. If you misspelled or need to change a key, you'll have to completely remove it and add a new one. However, accessing batch lists of keys and values is easy with the dictionary class's properties. So let's capture those right now. We'll say a constant variable called all keys is going to be equal to blacksmithShop.keys. Let's also do all values and use the values property. Now, if we use our quick jump, hold down command and click on keys, we can see that it's actually going to give us
>
> **[4:41](https://www.linkedin.com/learning/swift-5-essential-training/swift-dictionaries-14527901?u=76281980&t=281)** a dictionary, and that's not what we want. We want this to be an array of keys. In order to actually cast our blacksmithShop.keys into an array, which is what we want, we're going to explicitly convert it just like we've already done. We'll close blacksmithShop.keys in parentheses, and add a type annotation to the beginning. Now we want this to be an array of strings so we can use the shorthand two brackets with the string class inside. Now you'll see the difference here if you go to the inspector and display both all keys and all values. All keys is perfectly readable, whereas all values is not. Let's do the same thing here and finish off. These are integers for our values. We're going to specify that it's an array of ints, and there we go. We have all our keys and all our values.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Control Flow](../../Skills/Software%20Development/Control%20Flow.md) (1), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Code Identifiers:** blacksmithshop (9)
> **UI Navigation:** click on (1), go to (1)
> **Analogies:** just like (2)
> **Definitions:** means that (1)
> **Speakers:** - [instructor] (1)

#### [Core dictionary methods](https://www.linkedin.com/learning/swift-5-essential-training/core-dictionary-methods-14524900?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/core-dictionary-methods-14524900?u=76281980&t=0)** - [Instructor] The next logical step with dictionaries is learning how to update, remove, and otherwise modify their values. Let's create a new dictionary to work with. I'm going to call this player stats, and this is going to be string keys and int values just like last time. I'm also going to give it some initial values, so I'm going to say HP is that 100, attack is at 35, and gold is at 29.
>
> **[0:37](https://www.linkedin.com/learning/swift-5-essential-training/core-dictionary-methods-14524900?u=76281980&t=37)** I'm also going to add a print statement down in our caching and removing items comment, say print playerStats. Perfect. In some cases you may want to store an overwritten value when updating a key value pair, which isn't possible with subscripts. For that we need to use the update value method. So we're going to capture the overwritten value in a variable called old value. We're going to say player stats, update value. So let's declare a new variable called old value, and this is going to store our overwritten value from player stats that we're gona update right now. So we'll say playerstats.updatevalue, and we're going to give a new value of 30 for key attack. Now you can see in the debug area, that attack is set at 30 and old value stores 35, which was the previous value. A note here that update value is going to create a new key value pair if the one reference doesn't exist, just like with subscripts. You can also update a dictionary with a another dictionary variable for when you have more than one item to modify. For instance, we could set player stats to a dictionary literal and we'll say, this is going to be key evasion for value of 12 and MP for 55.
>
> **[2:15](https://www.linkedin.com/learning/swift-5-essential-training/core-dictionary-methods-14524900?u=76281980&t=135)** Now you have to be careful with this approach because it's going to completely override the existing dictionary elements with the new ones, not merge them. So you can see that debugged in our print statement that we only have MP and evasion. I'm going to comment that out just so we have our original three items and let's remove gold. Now, this is probably the simplest operation. We say, player stats, we can use subscript to reference the key and we just set it to no. Now player stats only has attack and HP. Again, you may want to store the old removed values during this operation. So we can use a similar approach. We'll say var removed value. Say player stats, remove value for key. Let's remove gold again just to keep this consistent. So we have the same outcome here, but now remove value holds 29. I'm going to copy and paste our last two lines of code down into the caching, removing items comment, just to be organized. And we're going to start talking about nested dictionaries before moving on. For this example, we're going to create a new dictionary, we're going to call this quest board. Now, for this one, I'm going to let the compiler in for our types, from the initial values. So for the keys, they're going to be strings. So, first quest is going to be fetched gemstones.
>
> **[3:53](https://www.linkedin.com/learning/swift-5-essential-training/core-dictionary-methods-14524900?u=76281980&t=233)** Now here's where it can get a little complicated. The value for each key is going to be a dictionary. Indenting like this really again, helps visualize what data you're actually writing. So the dictionary inside [Fetch](../../Skills/Web%20Development/Fetch.md) gemstones is going to be keys of strings and values of strings.
>
> **[4:23](https://www.linkedin.com/learning/swift-5-essential-training/core-dictionary-methods-14524900?u=76281980&t=263)** And we're going to add two items in the fetch gemstones objective and secret. Now that is just the first item in quest board. So if we go out to our nested bracket, we can add a comma to separate it and add a new item. We're going to call this one defeat big boss. That's the key. And for the value again, it's going to be dictionary in itself. We're going to have an objective again, beat the boss and a secret, win with 50% health. Now let's go in and access the objective for the fetch gemstones quest, which is this item right here. And this is going to require us to use back-to-back subscripts, just like we did with arrays, but we're going to use the keys as lookups instead of indexes. So we'll say var gemstone objective is going to be our quest board. And our first key we're going to look up is fetch gemstones to get into that nested dictionary. And then right back to back with that, we're going to look for the objective key. Now, we're going to get a bunch of errors here
>
> **[5:59](https://www.linkedin.com/learning/swift-5-essential-training/core-dictionary-methods-14524900?u=76281980&t=359)** because this subscript could very well give us a nil value and we haven't captured that or unwrap that in an optional. Now the correct way to do this is with optional binding, but we haven't gotten there yet. So we're going to allow the compiler to fix this for us just as a stop gap right now, and use a question mark right after the fetch gemstones subscript. Essentially what this does is tell the compiler to break the return chain if any of the keys don't exist and immediately return a nil value. So, because we do objectively know that there is a fetch gemstones key and that that nested dictionary has an objective, we get back retrieve five gemstones. If we change objective to a lowercase O, we're going to get nil. Again, dealing with nested collection types can be a bit of a headache, but if you use the correct syntax and spacing to make things easier and more visual, it'll go a lot easier.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (6)
> **Analogies:** just like (3), for instance (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** playerstats (1)
> **Definitions:** is a  (1)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)

#### [Working with sets](https://www.linkedin.com/learning/swift-5-essential-training/working-with-sets-14529815?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/working-with-sets-14529815?u=76281980&t=0)** - [Tutor] Sets are a great collection type for situations when you need to make sure values are only present once and don't necessarily care about the order of those unique values. Let's go down to our first comment, create a new variable. I call this activeQuests. And for the type annotation, we need to use the set type <string>. Now, what this is saying is that we want a set of strings to be stored in activeQuests. Now, to give this some initial values we use the exact same syntax as arrays. So I'm going to put in a few quest names here. [Fetch](../../Skills/Web%20Development/Fetch.md) gemstones, and then Big Boss, The Undertaker, and granny needs firewood.
>
> **[0:58](https://www.linkedin.com/learning/swift-5-essential-training/working-with-sets-14529815?u=76281980&t=58)** I'm going to go down to our second comment here and just print this out so we can see what's going on when we actually perform operations with the set. There we go. A type inference with sets is a bit different than with other types we've seen. The compiler can correctly infer from our initial values that we want to set of strings. However, we always need to include the set type to tell the compiler that this is not an array. Now, inserting and removing items is really efficient with sets because we don't have to worry about indexes or referencing key value pairs like we did with dictionaries. So to insert something we can just say activeQuests, insert, new member. And I'm going to use the new member of type string. I'm just going to say this new quest is called only the strong. The same goes for removing. We can use the remove function. And actually, we don't want to use the index 'cause that just over-complicates things with sets. We'll use remove string member. And we'll just take out The Undertaker. There we go. Only the strong is inserted on the end. And The Undertaker is removed. We can also check if a unique value exists in activeQuest, just like in our other collections with the contains method. And again, this is case and spelling-sensitive. So be careful here.
>
> **[2:31](https://www.linkedin.com/learning/swift-5-essential-training/working-with-sets-14529815?u=76281980&t=151)** And since all-4-one isn't in activeQuests right now, which going to return false. Lastly, we can sort, depending on what you need with your collection data. Now, sorting a collection can be simple as we've seen or complex, depending on what you need to do with your data. But the sorted method can at least alphabetize our set easily. So we can say activeQuests sorted. Here in the auto-complete it's a good time to point out that all the collections we've used sorted or reversed functions on can also take predicates, which are essentially additional rules for how the operation should be performed. So that's what this second sorted function with the complicated syntax is. It's still a bit early to talk about those, but we'll cover them later on when we get to closures. So I'm going to actually use sorted for now. If we click the show gray square in the inspector on the right, we can see that they're now alphabetized. One last note here. Sets can only store values that are hashable. Meaning they're able to provide a hash value of themselves, which is why some of these functions have the type hashhable. Now, all of [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md)'s basic types are already hashhable by default. So you don't really have to worry about it at this point in your Swift skills. However,
>
> **[4:03](https://www.linkedin.com/learning/swift-5-essential-training/working-with-sets-14529815?u=76281980&t=243)** if you want to store your own custom types in the future, they'll need to adopt what's called the hashable protocol. Now, more to come on protocols later.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Code Identifiers:** activequests (5), activequest (1)
> **Definitions:** is a  (1), is called (1)
> **CLI Commands:** make (1)
> **Analogies:** just like (1)
> **Warnings:** be careful (1)
> **Speakers:** - [tutor] (1)

#### [Core set methods](https://www.linkedin.com/learning/swift-5-essential-training/core-set-methods-14527898?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/core-set-methods-14527898?u=76281980&t=0)** - [Instructor] Because sets only store unique values, there are a number of useful operations that we can perform with them, that would require a lot of hard coded logic if we were using something like an array or dictionary type. I've already copied over the activeQuests set from the last playground, so we can get right to work. Now, all the set operations we're going to use in this example need a second set to work against. So we're going to create a new variable here. We're going to call this completedQuests, type Set, let type inference do its thing. We're going to add some initial values. We're going to say, Big Boss, All-4-One, and The Hereafter. Perfect. Now the four fundamental set operations are intersection, difference, union, and subtraction, all of which pretty much do exactly what they sound like. So if we wanted to return a new set containing all the common values between both sets, we could declare a new variable to store that result called commonQuests. And here we can say activeQuests at the intersection of another string set. Here, we can just feed this completedQuests. Since Big Boss is the only value in both, that's what's going to be returned and stored in commonQuests. We can use the symmetricDifference to return a new set
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/core-set-methods-14527898?u=76281980&t=94)** with all values that are not in both. So the exact opposite of intersection. So we'll say, var differentQuests. It's going to equal activeQuests, symmetricDifference. And for the other sequence, again, we're going to use completedQuests. If you pop open the display from the inspector, you can see that differentQuests now has a really long list of all the different values between both sets, which is everything but Big Boss. Now, the union operation creates a new set composed of all values of both sets, regardless of what they are, or if they're present in each other. So we'll say, var allQuests equals activeQuests, union of.
>
> **[2:30](https://www.linkedin.com/learning/swift-5-essential-training/core-set-methods-14527898?u=76281980&t=150)** We pop up in the gray box view on the inspector. We can see that we have a scrolling list now, 'cause there's so many, and all of our sets are combined. Finally, subtracting two sets, returns a new set minus the values from a second set. So we can say, clippedQuests, it's going to equal activeQuests, subtracting another sequence. For that, again, we'll use completedQuests. Perfect. For situations where you want to dynamically perform any of these operations directly on a set, instead of returning to modified version, you can use the formIntersection, formSymmetricDifference formUnion, and subtracting methods respectively. They do the exact same thing. Now there's more functionality in sets that you can make use of, including subsets, super sets and determining set membership and equality. And those are outside the scope of this essential course, but, again, are well laid out in the docs.

> [!info]- Semantic Content
>
> **Code Identifiers:** activequests (5), completedquests (4), commonquests (2), symmetricdifference (2), differentquests (2)
> **CLI Commands:** make (1)
> **Documentation:** the docs (1)
> **UI Navigation:** open the (1)
> **Speakers:** - [instructor] (1)

#### [Swift tuples](https://www.linkedin.com/learning/swift-5-essential-training/swift-tuples-14528854?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/swift-tuples-14528854?u=76281980&t=0)** - [Instructor] In [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md), tuples can take a group of values and store them as one compound value. Now, this sounds like an array, I know, but tuples have the ability to store values of different types in a single variable. So you can mix and match as you need. For this example, we're going to create a tuple to store Attack data. So let's start out with our first variable. We're going to call this uppercutAttack, and for the type annotation, we use an open and closed parentheses. Now, inside here, we need to declare the types and in what order they'll be stored. Our uppercutAttack is going to have a name which is a string, a damage value that's an integer and a rechargeable property that's a Boolean. We could also use type inference by assigning some literal values to our uppercutAttack. So we could say this is going to be called uppercut Smash, and then we would add in the damage value, I'm going to say this we'll do 25 and it is rechargeable. To access each value individually, we use dot notation and select the index of the value we want. So uppercutAttack is going to have indexes of zero, one and two because at zero index, just like our other collections. So our value at index zero is going to be the name, uppercut Smash, uppercutAttack at index one is going to be our damage
>
> **[1:36](https://www.linkedin.com/learning/swift-5-essential-training/swift-tuples-14528854?u=76281980&t=96)** and uppercut at index two is our true rechargeable property. Now this is fine when you know what values the tuples is storing, because you're looking right at it. But for clarity you'll want to decompose or unpack, as Swift calls it, each tuple value into its own named variable, which is much, much more efficient. To unpack uppercutAttack, we need to declare a new variable. But instead of having a variable name, we're going to have three inside a pair of parentheses. Now you'll notice that we need to match this with the type signature or the type annotation that we've already declared. So we'll say, var, you want the variable name to be attack, damage and rechargeable. And once we assign this to uppercutAttack, the Swift compiler is going to do all the parsing for us. So instead of calling the indexes like we did before, we can just say attack, damage and rechargeable. And the tuple we've defined as it is now is okay for [Prototyping](../../Skills/Software%20Development/Prototyping.md). But when you're writing production Swift, you'll want to name your tuple variables for clarity. And we can do this by either adding individual value names to the type annotation or directly to the assigned literal value, whatever style really makes more sense to you. So I'm going to show you both ways. We're going to declare a new tuple.
>
> **[3:11](https://www.linkedin.com/learning/swift-5-essential-training/swift-tuples-14528854?u=76281980&t=191)** This is going to be shieldStomp, and we're going to use the type annotation approach here. We'll say name string type, damage int and rechargeable Bool. So again, we're just modifying our type annotation to add names, whereas with uppercutAttack, we just define the types. Let's do the literal values now. We'll say this is going to be a planetSmash attack. And here we can say name, planet smash, damage, 45, rechargeable and we'll say it's true for this one. Either of these approaches is going to let you get the individual values by name instead of by index or by manually unpacking the tuple. So this is closer to a regular variable or collection that we've defined so far. Note here, that because we didn't initialize shieldStomp, you still have to do that just like any other variable. So we could say shieldStomp.damage, it's going to equal a hundred. Or we could just print out or reference the planetSmash.rechargeable. I'm going to copy out our shieldStomp declaration, just pop that into the last comment here
>
> **[4:47](https://www.linkedin.com/learning/swift-5-essential-training/swift-tuples-14528854?u=76281980&t=287)** for organization and reference. And we're really all set with tuples. And these are really great for returning values from functions or storing temporary groups of values. However, it's tempting as it may be in the future to use tuples to create and store complex [Data Structures](../../Skills/Software%20Development/Data%20Structures.md). It's still a better idea to use classes instructs for that as we'll see later on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (4), [Prototyping](../../Skills/Software%20Development/Prototyping.md) (1), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1)
> **Code Identifiers:** uppercutattack (8), shieldstomp (4), planetsmash (2)
> **Definitions:** is a  (1), defined as (1)
> **Analogies:** just like (2)
> **UI Navigation:** select the (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Merchant shop](https://www.linkedin.com/learning/swift-5-essential-training/challenge-merchant-shop-14530752?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/challenge-merchant-shop-14530752?u=76281980&t=0)** (upbeat bouncy music)
>
> **[0:06](https://www.linkedin.com/learning/swift-5-essential-training/challenge-merchant-shop-14530752?u=76281980&t=6)** - Okay, it's time for another code challenge, so get ready. We're going to hit on all the collection types we've worked with in this chapter to build up our own little merchant shop, or at least the data behind one. I'll be asking you to reference and modify shop data so you can get some hands-on experience. It shouldn't take you more than 10 minutes, but if you get stuck, head over to the next video for my solution. Have fun and happy coding.

> [!info]- Semantic Content
>
> **Cross-References:** next video (1)
> **Speakers:** - okay (1)
> **Non-Speech:** (upbeat bouncy music) (1)

#### [Solution: Merchant shop](https://www.linkedin.com/learning/swift-5-essential-training/solution-merchant-shop-14524897?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/solution-merchant-shop-14524897?u=76281980&t=0)** - [Narrator] All right, let's get into our merchant shop challenge here with our first task. This is going to be to create two collections. The first is going to be an array of strings. The second is going to be a dictionary, with keys that match our array items. And their values are going to be the item prices. Let's start out and call our new variable shopitemsArray. We'll give this literal values, let's say we can get a Stone Shield, a Bronze Hammer or a Gold [Helm](../../Glossary/Tool/Helm.md). For the dictionary, I'm going to call this shopItemsDictionary. And again I'm going to use the, values in our array for our keys. So Stone Shield is going to be priced at 15, Bronze Hammer is going to be 25, Gold helm is going to be 35 and here we want one more value that's not in shopItemsArray so that we can check for it later. I'm going to call this Diamond Longsword, and this is going to be at 100, it's the most expensive. Our second task is to use the contains method to check if shopItemsArray has an item we want. In this case we're going to store the result.
>
> **[1:32](https://www.linkedin.com/learning/swift-5-essential-training/solution-merchant-shop-14524897?u=76281980&t=92)** We're going to call this longswordInStock, and I want to look up if the shopItemsArray has a Diamond Longsword in it.
>
> **[1:52](https://www.linkedin.com/learning/swift-5-essential-training/solution-merchant-shop-14524897?u=76281980&t=112)** Now this is going to return false because it's not there yet. So in task number three, we're going to use the insert method to add it. So we'll say shopItemsArray, insert new element, Diamond Longsword at index 2. Lets just make sure this works Stone Shield, Bronze Hammer, Diamond Longsword. You know what I'm going to put that on the very end and switch the index to 3 Perfect. For task number four, we're going to create a variable called selectedItem and assign it an item from shopItemsArray using subscript and indexes. So we'll say shopItemsArray at index 2, why not, we'll grab the Gold Helm. For task number five, we're going to use our selectedItem as a key and look up its price in our shopItemsDictionary. So we're going to say var itemPrice. Actually, let's be a little bit more specific, selectedItemPrice equal shopItemsDictionary for key selectedItem. Now in the inspector we see that the price is 35, which is correct. For the remaining tasks, we're going to be working with sets and as we know from our previous video, we need two of them to work with. So let's set out one set called fullArmor
>
> **[3:29](https://www.linkedin.com/learning/swift-5-essential-training/solution-merchant-shop-14524897?u=76281980&t=209)** of type set, and we're going to give it some initial values, Diamond Helm, Diamond Armor, Diamond Greaves, and Diamond Braces. Our second set is going to be called currentArmor, and this needs to have some values that are different than fullArmor so that we can perform the operations. I'm going to say I only have a Diamond Helm and a Diamond Braces equipped. For task number seven, we're going to use the subtract method to find out which items were missing for a complete diamond set. I'm going to say var missingPieces equals fullArmor subtracting from currentArmor. Now if we take a look, missingPieces were missing Diamond Armor and Diamond Greaves, which is correct. For the last task, we're going to create a tuple, assign it some named variables and give it some initial values. We're going to say var armorPiece is going to be equal to a named variable called Diamond Braces, a cost which is going to be an integer and canEquip which is going to be a Boolean and it'll be true right now.
>
> **[5:03](https://www.linkedin.com/learning/swift-5-essential-training/solution-merchant-shop-14524897?u=76281980&t=303)** Just to make sure this all works, we're going to say armorPiece canEquip and armorPiece.cost Perfect. All our values are coming out the way they should and can be referenced.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Helm](../../Glossary/Tool/Helm.md) (5)
> **Code Identifiers:** shopitemsarray (7), shopitemsdictionary (3), selecteditem (3), fullarmor (3), armorpiece (3)
> **CLI Commands:** make (2), find (1)
> **Cross-References:** previous video (1)
> **Speakers:** - [narrator] (1)
> **Non-Speech:** (upbeat music) (1)


### 4. Application Control Flow

[↑ Back to Table of Contents](#table-of-contents)

#### [Controlling your destiny](https://www.linkedin.com/learning/swift-5-essential-training/controlling-your-destiny-14531795?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/controlling-your-destiny-14531795?u=76281980&t=0)** - Making decisions using code really gives you that feeling of controlling how your application works. Handling optional types correctly, laying out different adventure paths for your character to take, even computing whether they're still alive at all, comes down to [Control Flow](../../Skills/Software%20Development/Control%20Flow.md). If statements, for loops, switches paired with defensive programming are all going to be the foundation of every creative feature you develop. So let's dive in.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Control Flow](../../Skills/Software%20Development/Control%20Flow.md) (1)
> **Speakers:** - making (1)

#### [The if statement](https://www.linkedin.com/learning/swift-5-essential-training/the-if-statement-14529811?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/the-if-statement-14529811?u=76281980&t=0)** - We're going to start our exploration of [Control Flow](../../Skills/Software%20Development/Control%20Flow.md) with the if statement, which is the simplest way to execute conditional code in your applications. If statements are best suited to handling [Decision Trees](../../Skills/Artificial%20Intelligence%20(AI)/Decision%20Trees.md) with three branching options or less. If you find yourself with an if-else statement extending beyond a few conditions, it's probably time to use a switch, which we'll talk about later in this section. Let's start by recreating some test variables from our very first video. We'll say currentHealth = 95 and maxHealth = 100. Now let's check for the condition where currentHealth is greater than or equal to maxHealth. So we use the if keyword followed by an expression we want to evaluate. So we'll say, "currentHealth >= maxHealth", open and closed curly braces. Inside the body of the statement, we'll just print out, "HP is at maximum." You'll notice we used a new operator here which is part of the comparison family. Now these operators check greater, less, equal, or not equal conditions, all of which I've commented out for your reference here. Now what we're saying here with the if statement is that if our expression evaluates to true, then execute whatever commands we have inside the curly braces.
>
> **[1:35](https://www.linkedin.com/learning/swift-5-essential-training/the-if-statement-14529811?u=76281980&t=95)** However, we see that because our currentHealth isn't equal to our greater than maxHealth right now, nothing is being printed out, which means we need to define more cases to handle those different situations. We can do that by adding an else-if statement and we tack that on right after the curly brace of the if statement. So we'll say "else if currentHealth <= 50" a new pair of open and closed curly braces, and for this case, we're going to print out, "Grab some health soon!" Now, finally, if neither of those conditions are met, we can handle the fall through with a catch-all else statement. Again, we just tack this on to the end of the last closed curly brace, give it its own set of open and closed braces and print out, "HP is between 50 and 100." On our debug area we're seeing else statement printout, but if we change current health up here to something different, so say 45, we're going to get a different one, "Grab some health soon!" If it's at 100, we're going to get, "HP is at maximum." Else-if statements also let us evaluate multiple expressions using the AND and OR operators that we've already seen. So for quick reference, we can say, "if currentHealth > 90" or "currentHealth <= maxHealth"
>
> **[3:13](https://www.linkedin.com/learning/swift-5-essential-training/the-if-statement-14529811?u=76281980&t=193)** open and close curly braces, And we'll just print out, "You're doing just fine." Perfect. A note here, if you're coming from another language where you need open and close parentheses around each expression, [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) will allow you to do that with no problem, it's just not the Swift approved syntax, it sort of clutters up the code. But if that makes you feel more comfortable or at ease, go ahead.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2), [Control Flow](../../Skills/Software%20Development/Control%20Flow.md) (1), [Decision Trees](../../Skills/Artificial%20Intelligence%20(AI)/Decision%20Trees.md) (1)
> **Code Identifiers:** currenthealth (7), maxhealth (5)
> **CLI Commands:** find (1)
> **Cross-References:** later in (1)
> **Speakers:** - we (1)

#### [Unwrapping optionals](https://www.linkedin.com/learning/swift-5-essential-training/unwrapping-optionals-14530750?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/unwrapping-optionals-14530750?u=76281980&t=0)** - [Instructor] Now that we've seen if statement syntax, we can talk about handling optionals the correct way with optional binding. Essentially, we use a variation of the if statement to safely unwrap the optional, whether it's a value or nil, and then store it locally for use. I've already created a few optional values that we've used in earlier sections, so that we can get right to work here. For our first example, we're going to unwrap item gathered. So here we use the if keyword and then let, and here we have to declare the name of the locally unwrapped optional. So we'll call that item, and it's going to be equal to item gathered, followed by an open and closed curly braces. Now in plain English, we're saying, if item gathered is not nil, unwrap the value and assign it to item, which we can then use in the body of the if statement. So we could print out an interpolated string here, you found an item. I'm going to say that again. The unwrapped optional value item is only accessible within the if statement. Trying to reference it anywhere else will come up as a blank in auto-complete. And because item gathered is nil right now, nothing's going to execute, and our program won't crash like it did with forced unwrapping. Unwrapping optionals like this is going to become second nature as you continue with [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md).
>
> **[1:36](https://www.linkedin.com/learning/swift-5-essential-training/unwrapping-optionals-14530750?u=76281980&t=96)** And we can add an else statement to easily handle the nil value. And we can just print out, sorry, no item found.
>
> **[1:50](https://www.linkedin.com/learning/swift-5-essential-training/unwrapping-optionals-14530750?u=76281980&t=110)** And just to make sure we're unwrapping item gathered correctly, I'm going to set it to, let's say Diamond Longsword again. Why not? And we'll see that you found a Diamond Longsword prints out and is unwrapped correctly. Now in cases where multiple optionals need to be unwrapped at the same time, you can use a shorthand or a single line syntax. For example, let's unwrap the is shop open, and query for a dictionary value at the same time. So we'll say, if let shop open equals is shop open, which is our optional boolean. And here, we just add a comma to separate our multiple optionals that we're unwrapping this line. We don't need a new if keyword. We can go right into let searched item. We want that to be equal to blacksmith shop for key shield. Open and closed curly braces. Now, if both of these are found or are not nil, we're going to print out we're open. Shop open. And we have a searched item in stock.
>
> **[3:17](https://www.linkedin.com/learning/swift-5-essential-training/unwrapping-optionals-14530750?u=76281980&t=197)** If neither or only one of these optionals are nil, we're going to print out, sorry, either we're not open or don't have your item. I'm going to harp on this again. The gotcha with unwrapping multiple optionals in the same line is that they both need to be non nil values. So this is sort of like having the and operator in an if statement. They both have to be true. Right now, is shop open is true. And there is a shield in the blacksmith's shop dictionary. However, if we go up to is shop open and delete its initial value, we're going to get the sorry, either we're not open or don't have your item debug log. This syntax is applicable to our previous example with optionals, including the two dimensional arrays and dictionaries we worked with. So I've copied our quest directory up at the top of the playground already with the same values we used earlier. So let's try an unwrap one of our objectives safely this time. So we'll say, if let [Fetch](../../Skills/Web%20Development/Fetch.md) gems equals quest directory, and here we need to use the chain syntax for our subscript. So we'll say we want to look up the fetch gemstones dictionary. Add the question mark to denote that it's an optional. It might not exist.
>
> **[4:49](https://www.linkedin.com/learning/swift-5-essential-training/unwrapping-optionals-14530750?u=76281980&t=289)** But if it does, to query the objective within. Now, if we find this quest, I'm going to print out active quest object is our fetch gems local variable. Else. We're just going to print out that quest is no longer available. Perfect. Now we've got active quest object retrieve five gemstones.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (3), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **CLI Commands:** make (1), find (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Warnings:** gotcha (1)
> **Speakers:** - [instructor] (1)

#### [Using for-in loops](https://www.linkedin.com/learning/swift-5-essential-training/using-for-in-loops-14531792?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/using-for-in-loops-14531792?u=76281980&t=0)** - [Instructor] For-in statements are used to iterate or loop through collections like arrays, dictionary key-value pairs, ranges, even the characters in a string. I've already set up some test variables to get things going. So let's take the string first and iterate through each of its characters. We start with the for keyword to declare the loop, then assign a local variable to capture each iterated value. So I'm going to call this for stringCharacter, then we use the in keyword and specify what collection we want to loop through, in this case, playerGreeting. Follow this up by an open and closed curly braces, where we can perform some kind of operation on the locally stored string character variable. Well, in this case, I'm just going to print out stringCharacter. Now, in English, we've essentially said loop through each value in the playerGreeting string, store the value in stringCharacter for that loop, and print it out each time. We can see in the debug log that each individual character has been printed out on its own line. You'll also notice that we didn't have to specify a type for stringCharacter because [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) is going to infer the correct type from playerGreeting. Now, the same syntax applies for an array. So let's loop through our armor types, capture each one locally in a variable called armor, and just print them out.
>
> **[1:40](https://www.linkedin.com/learning/swift-5-essential-training/using-for-in-loops-14531792?u=76281980&t=100)** Again, Swift infers the type correctly, and we get a similar debug log with Heavy Plate, Hunters Gear, and Mage Robes printed out. Dictionaries are a bit different, since they hold key value pairs instead of single values. But the syntax is the same. For instance, if we wanted to loop through all the keys by themselves, we could say for weaponKey in weapons.keys, open and closed curly braces, and we'll print out each weaponKey. Perfect, now we've got Longsword, Dagger, and Mace. Now, the same goes for values. If we wanted to change this, we could say we want to store the weapon values in weapon.values. Now we'll get the prices, 75, 25 and 150. This isn't really efficient to do the keys and values separately, since most of the time, you'll want them both to be able to display the correct data. So we can say for, use an open and closed parentheses, and specify a local variable for the key. So we can say this will be weapon and the value, which is damage, in weapons. Now, let's print these out. We'll just say weapon, and this'll have the damage. Now in our debug log, we have all
>
> **[3:15](https://www.linkedin.com/learning/swift-5-essential-training/using-for-in-loops-14531792?u=76281980&t=195)** of the dictionary values and keys, right where we need them together. So Dagger is 25, Mace is 75, Longsword is 150. To wrap things up here with the for loop, we're going to work with range operators, which we only briefly touched on. Now, there are three kinds of ranges available. They're called closed, half-open, and one-sided. It's easier to show these rather than tell. So we'll say for, and again, we need a local variable. I'm going to call this indexNumber, in 1, followed by three periods and 10. Now, what we're saying in English is that for every number or index number in 1 through 10, do something. So we'll just print out each index number. Now we're going to get 1 through 10 printed out in our debug log, and this is called a closed range, meaning it takes into account the first number of our range, which is 1, and our last one, which is 10, and includes those plus everything in between. For one-sided ranges, we can declare a starting or ending point of the range. And Swift runs the loop, as long as there are values remaining in the specified direction. So we can say for armor in armorTypes, subscript 0 followed by three periods. Now, what we've said here is for every armor in armorTypes, starting at its first item, which is index 0,
>
> **[4:50](https://www.linkedin.com/learning/swift-5-essential-training/using-for-in-loops-14531792?u=76281980&t=290)** and going 'til there's no more. So we can print out armor. Again, we're going to get the same output 'cause we're doing the same thing we did before when we just printed out armor in armorTypes. You want to be careful with one-sided ranges because they can cause infinite loops if the value you're iterating over doesn't have a set number of items. So if we had just said for indexNumber in 1... without the 10, that for loop would iterate forever from 1 to a never-ending infinite number, and [Xcode](../../Skills/Mobile%20Development/Xcode.md) would crash. The last kind of range operator we can use is called a half-open range, which are written a little differently, basically allowing you to specify the last value is left out. So we'll say for indexNumber in 1, and for half-open ranges, we only use two periods and a left arrow, and we'll say up to 10. And here, we're just going to print out our index number again. This time, we're going to get 1 through 9, with 10 left out. Now, we can also use one-sided ranges here. And let's say, again, using our armorTypes, we'll say for armor in armorTypes from the start to the second index,
>
> **[6:27](https://www.linkedin.com/learning/swift-5-essential-training/using-for-in-loops-14531792?u=76281980&t=387)** we're going to print out our armor. Now we're just going to get index 0 and index 1, which is Heavy Plate and Hunters Gear. Our third item, which is Mage Robes, is going to be dropped off. Now, you have to be careful here too, because we could conceivably give an index number that exceeds the number of items in your collection, and that's going to generate a range error. Now, it's safer to use the collection's count property as the upper end in this kind of situation. So we can change this, instead of using the index 2, we can say we're going to loop through from the beginning of our armor types until our armorTypes.count. This is going to make this safe, and we'll never get an out of range exception.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3), [Xcode](../../Skills/Mobile%20Development/Xcode.md) (1)
> **Code Identifiers:** armortypes (6), stringcharacter (4), playergreeting (3), indexnumber (3), weaponkey (2)
> **Definitions:** is called (2)
> **Warnings:** be careful (2)
> **CLI Commands:** make (1)
> **Analogies:** for instance (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [While loops](https://www.linkedin.com/learning/swift-5-essential-training/while-loops-14528859?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/while-loops-14528859?u=76281980&t=0)** - [Instructor] Now, WHILE loops are exactly what they sound like. While some defined condition is true, do something. That's really that simple. There are two types of WHILE loops, the standard one and the repeat one. For cases when you don't know the number of times you'll need to perform some action, like with a foreign loop, the standard loop is going to be the way to go. So let's start by creating a player variable. We'll call this playerHealth, and we'll set it to five. Now we're going to say, as long as player health is greater than zero, we're going to print out something. So we use the while keyword to declare the loop. We give it an expression, just like with the four. So we'll say while player health is greater than zero, open and closed curly braces. Now the one thing we need to be careful of with WHILE loops is that we have to manually decrease our expression variable so that it's not an infinite loop. So at the very first line of our WHILE loop body, we're going to say player loop -= 1, and we'll just print out, "Still kicking". Now you can see in the debug and in the inspector that our expression, playerHealth greater than zero, is going to be evaluated at the beginning of each loop. So five statements print out. On the sixth, playerHealth is no longer greater than zero, so the WHILE loop finishes.
>
> **[1:35](https://www.linkedin.com/learning/swift-5-essential-training/while-loops-14528859?u=76281980&t=95)** Now, repeat WHILE loops are essentially the reverse. The expression is checked at the end of each pass, instead of at the beginning. So let's create the same thing, and we'll use the repeat keyword. Here, we use open and closed curly braces for the loop body. Again, we have to manage the increment or decrement of the value we're evaluating. So we'll say playerHealth -= 1. We'll print HP at, and we'll just say playerHealth. And after the repeat open and close curly braces, we're going to add the while keyword, and the expression. So playerHealth is greater than zero. We're going to need to comment out our previous WHILE loop because that has already put our player health down to zero. So in order to get our repeat count working correctly, let's give that a rerun and see our output. There we go. HP at 4, 3, 2, 1, 0. Both of these WHILE loop approaches are useful, but you have to pay attention to the behavior you want when choosing between them for a specific approach.

> [!info]- Semantic Content
>
> **Env Vars:** while (8)
> **Code Identifiers:** playerhealth (6)
> **Analogies:** just like (1)
> **Warnings:** be careful (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [The switch statement](https://www.linkedin.com/learning/swift-5-essential-training/the-switch-statement-14530757?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/the-switch-statement-14530757?u=76281980&t=0)** - [Instructor] Switch statements take values that we define and run them against possible matching patterns. Each match can have a different block of code, which is called a case, that is going to execute when a match is made. I've got a few test variables again for us to work with. So let's start by checking what value the initial variable is storing. We start out with the switch keyword and the X code editor is going to handily print out or set up our blueprint for the switch statement. And what this is really saying is that we're going to switch on a value for a number of cases, and we can define as many as we want. And we end it with a default, which is going to catch any fall through or any cases that we don't explicitly define. Let's see that in action, it's a bit easier to visualize. We'll say we're going to switch on initial, the case or the first case we're going to declare is if initial is holding a capital H. If it is, we're going to use a colon, which declares the case executable block, anything that goes after this colon is going to be printed out or executed if initial matches the capital H character. So we're going to print out, I'm guessing Harold, and we can define as many cases as we want as I mentioned, as long as we use the case keyword and a new pattern. So this time I'm going to check if initial is holding a capital A, followed by a colon
>
> **[1:35](https://www.linkedin.com/learning/swift-5-essential-training/the-switch-statement-14530757?u=76281980&t=95)** and a new print statement, maybe Alita. Now for the default, this is going to execute if initial is neither a capital H nor a capital A, I'm going to print out, I've got nothing. Should be noted that the code blocks in between case statements are not limited to one line. You can use as much space and put as much logic in there as you need. This is just a really simple example. And one thing to be aware of with switch statements is that they need to be exhaustive. So if we left out the default statement here, we're going to get an error. Switch must be exhaustive. This is saying that we're only really defining two cases and initial could have an infinite amount of cases. So we need to have that default class there. I'm going to put the default case back in and we should be good to go with the debug statement, printing out, I'm guessing Harold. If we changed initial to capital A, we'll get maybe a Alita. And if we change it to anything else, we'll get I've got nothing. A note here, we're not going to go through the syntax of switching on all value types that we've encountered so far, because it's largely the same. But you should know that cases can also be defined using ranges and multiple values, which we're going to look at next. If you look at our test variables, we've got HP and MP,
>
> **[3:10](https://www.linkedin.com/learning/swift-5-essential-training/the-switch-statement-14530757?u=76281980&t=190)** which are both integers. So we're going to do a switch that's going to evaluate both of them at the same time for each case. So again, we'll say switch and for the value we're going to need open and close parentheses, if you have more than one value to evaluate. And we'll say MP and HP. Now for the pattern, it's the same thing. We want to match, let's say if MP is 15 and HP is 10. We'll print out, great job, why not. Now for the second case, let's use a range. We'll say if MP is between one and 15 or 20 up to, but not including 25. Print out, ranges are the best. Finally, we can't forget our default. So we'll print again, I've got nothing. Now, since our HP and MP values don't match any of our cases, except our default, that's kind of boring. So I'm going to change HP to 22 and M P to five, so that we get our middle case statement. Ranges are the best, all right, so that's working. Looking at our switch now, it's easy to see how useful it might be to locally store and use our switch variables
>
> **[4:46](https://www.linkedin.com/learning/swift-5-essential-training/the-switch-statement-14530757?u=76281980&t=286)** inside each switch case. Right now we cannot access or reference MP and HP inside our print statements. We can access them as they are, but we can't capture them. However, switch statements do allow something called value binding. So let's create a new case. We're going to try this out. We're going to say case, and we're going to declare a local MP variable and a local HP variable. Here, we're just going to print out our local MP and local HP.
>
> **[5:28](https://www.linkedin.com/learning/swift-5-essential-training/the-switch-statement-14530757?u=76281980&t=328)** And this isn't actually going to do anything because we only bound the values, but we haven't set up an actual expression. So here we need a new keyword called where. Now we can specify a match pattern for our switch so we'll say where, local MP, plus local HP is greater than 20. Now here's an interesting thing about switch cases. The moment a match pattern is made essentially the switch statement stops. So right now our ranges are the best case is printing out. And even though our new value binding case is true and it should print out, the switch isn't even getting to that. So you have to be careful with the order of your cases because they could stop before your condition that you actually want to check for is met. So let's change HP to 30, and MP to 40, just so we're out of both of our first cases and we've printed out 40 and 30. I know that was a lot of different syntax to take in all at once, but it's important to understand how extensible and flexible switch statements really are. Now we'll get to how to pair them with something called an enum type or an enumeration type later, because that's really where they shine. So more on that coming up.

> [!info]- Semantic Content
>
> **Warnings:** be aware (1), be careful (1)
> **Prerequisites:** set up (2)
> **Cross-References:** coming up (1)
> **Definitions:** is called (1)
> **Speakers:** - [instructor] (1)

#### [The guard statement](https://www.linkedin.com/learning/swift-5-essential-training/the-guard-statement-14528848?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/the-guard-statement-14528848?u=76281980&t=0)** - [Instructor] The guard statement in [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) acts like a really strict version of an if-else statement, allowing us to be much more explicit when making sure our code only executes in certain situations. Now you might be thinking, well, we could just do this with an if, an else-if, an else, and so on. And while that's true, the guard statement is just more concise with how it handles failing conditions. I've already set up a shop items dictionary, and a current gold integer value for us to work with. So let's loop through our shop items, but hold off any logic until we get a guard statement set up. So we're going to use our for syntax that we've already learned, and we're going to locally store the item and the price in shop items. And that's all we're going to do right now. Now to determine if we can afford certain items, we're going to use a guard statement to check our current gold against each item in the dictionary. So the guard statement naturally uses the guard keyword followed by an expression. So we're going to say guard current gold is greater than equal to the price of the current item being looped over. And if that's not true, we're going to add the else block and print out you can't afford the, and we'll insert or interpolate the item.
>
> **[1:38](https://www.linkedin.com/learning/swift-5-essential-training/the-guard-statement-14528848?u=76281980&t=98)** And you'll notice that the guard statement is a little different than the if here, because there's no if body, there's only an else, which is exactly how it's supposed to work. So we're saying we can only get past this line of code if current gold is greater than or equal to the individual item price. At this point, the compiler is throwing us an error saying that the body of the guard statement can't fall through to any following code. And that's true. It's also suggesting that we either use a return or throw keyword to fix this. Now the return keyword just tells the compiler to exit out of the current loop entirely, which isn't what we want. While the throw keyword is an error handling feature that we'll cover later. For this situation, we're going to use the continue keyword, which is going to stop the current loop we're in, but still continue to the next one instead of exiting out entirely. And we get one debug log, you can't afford the Excalibur. So what's happened is we've looped through magic wand and our guard statement past 'cause we have more than 10, iron [Helm](../../Glossary/Tool/Helm.md), we have more than five, but Excalibur, sadly, we are far short. Now if our guard gets passed, let's add another print statement just so we can make this a little more clear. We'll say, go ahead. The item is yours for price.
>
> **[3:14](https://www.linkedin.com/learning/swift-5-essential-training/the-guard-statement-14528848?u=76281980&t=194)** Perfect. Go ahead. The iron helm is yours for five, magic wand for 10, but you can't afford the Excalibur.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Helm](../../Glossary/Tool/Helm.md) (2), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Definitions:** is a  (1), is an  (1)
> **Prerequisites:** set up (2)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Game logic](https://www.linkedin.com/learning/swift-5-essential-training/challenge-game-logic-14528850?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/challenge-game-logic-14528850?u=76281980&t=0)** - All right, congratulations on unlocking another chapter challenge. For this one, you're going to be tasked with creating some simple game logic, with the loops and conditional logic we've learned so far. You're also going to be unwrapping a few optional values and using the switch and guard statements to keep your code safe. This one might take a bit longer than the previous challenges, somewhere between 10 and 15 minutes, but if you're stuck, go ahead and head over to the next video for my solution. Happy coding.

> [!info]- Semantic Content
>
> **Cross-References:** next video (1)
> **Speakers:** - all (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Game logic](https://www.linkedin.com/learning/swift-5-essential-training/solution-game-logic-14531793?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/solution-game-logic-14531793?u=76281980&t=0)** (bright upbeat music)
>
> **[0:06](https://www.linkedin.com/learning/swift-5-essential-training/solution-game-logic-14531793?u=76281980&t=6)** - [Instructor] All right, let's dive into our Game Logic challenge and tackle the first task of creating two optionals for us to work with. I'm going to declare my first optional variable called this lefthandWeapon. It's going to be an optional string, and I am going to give it an initial value. I'm also going to create a righthandWeapon, optional string with no initial value, so it's storing nil. For the second task, I'm going to unwrap both of these using the single line, shorthand syntax. I'm going to say, if let leftWeapon equals lefthandWeapon
>
> **[0:55](https://www.linkedin.com/learning/swift-5-essential-training/solution-game-logic-14531793?u=76281980&t=55)** and let rightWeapon equals righthandWeapon. If both of these unwrap successfully, I'm just going to print out an interpolated string, looks like, your leftWeapon and rightWeapon
>
> **[1:19](https://www.linkedin.com/learning/swift-5-essential-training/solution-game-logic-14531793?u=76281980&t=79)** are an even match for me. Else if one or the other of these optionals is nil, I'm going to print out, I didn't bring enough [Hardware](../../Topics/Hardware.md) for this. And there we go. Since righthandWeapon is nil, we're going to get our else print out. For our third task, we're going to create a dictionary of names and experience points. And we're going to loop through those in the next task. So we'll say, var playerExp we'll give this a few initial key value pairs. Let's say, I have 233, Steven has 489
>
> **[2:12](https://www.linkedin.com/learning/swift-5-essential-training/solution-game-logic-14531793?u=76281980&t=132)** and Bob the Noob has 0, poor Bob. Task four is going to be to use a foreign loop to capture our keys and values and playerExp. I notice there's an extra T on there, so I'm going to delete that. And start the for loop, use our parentheses syntax so we can capture the keys and values locally. I'm going to name this, name and exp in playerExp. Open and closed curly braces. And for task five, I'm going to move the comment in here Because it's to add a guard statement. So we'll do that next. We'll say guard exp, and we want to make sure that all our players at least have one exp to continue, otherwise they're in trouble. So we'll say guard exp is greater than zero, else if any of our players don't have more than one experience points, I'm going to print out, name of the player you need some serious help. Don't forget to use the continue keyword here so that we pop out of this iteration of the loop, but we keep going with the other players. I'm going to move comment number six into our for loop
>
> **[3:53](https://www.linkedin.com/learning/swift-5-essential-training/solution-game-logic-14531793?u=76281980&t=233)** because we're going to use a switch statement here to check the exp value of each player. We're going to say switch on exp and for the first pattern, we're going to check if it's 32. We're just going to print out, looking good. For our second case, we're going to define a range. So if the player exp is between 201 and 500, we're going to print out, getting better every day. For case number three, we're going to capture the local exp and check that it's above 500. So we'll say case, open parentheses, let localExp, where localExp is greater than 500.
>
> **[4:56](https://www.linkedin.com/learning/swift-5-essential-training/solution-game-logic-14531793?u=76281980&t=296)** On this case, we're going to print, time to level up. And we can't forget our default. So we're going to just print out, corrupted data found.
>
> **[5:16](https://www.linkedin.com/learning/swift-5-essential-training/solution-game-logic-14531793?u=76281980&t=316)** and we can see in our debug log, that the first 2 players get through and they get the... Getting better every day, switch case. And Bob the Noob, gets stopped by the guard statement and needs some serious help, which is exactly what we expected.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Hardware](../../Topics/Hardware.md) (1)
> **Code Identifiers:** righthandweapon (3), playerexp (3), lefthandweapon (2), leftweapon (2), rightweapon (2)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright upbeat music) (1)


### 5. The Wide World of Functions

[↑ Back to Table of Contents](#table-of-contents)

#### [Leveling the playing field](https://www.linkedin.com/learning/swift-5-essential-training/leveling-the-playing-field-14528852?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/leveling-the-playing-field-14528852?u=76281980&t=0)** - Our [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) adventure so far has gone from variables and types to collections and decision logic, but the functions we've been using are still a hundred percent defined by Swift. In order to start creating our own executable chunks of logic, we need to learn how to declare functions. Adding parameters, return types, and closures to that mix opens up a whole new level of programming possibilities, a level that includes declaring our own custom types. So let the quest begin.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2)
> **Speakers:** - our (1)

#### [Basic functions](https://www.linkedin.com/learning/swift-5-essential-training/basic-functions-14528861?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/basic-functions-14528861?u=76281980&t=0)** - [Instructor] We're going to start this section off with a look at the syntax of writing basic [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) functions as well as how to define parameters and return types. For our first simple example, let's create a function named findNearestAlly and just have it print out a message. So we start with the func keyword, followed by the name in camel case, findNearestAlly. We close that off with an open and closed pair of parentheses, which is where any parameters are going to go in when we have them, and an open-close set of curly braces, which is going to define the function body. Since we're just going to print out a string here, I'm just going to say searching. To call a function we just use its name, followed by a pair of parentheses to capture any arguments that we want to input. Since findNearestAlly doesn't have any, that's all it takes. And we can add on a return type to our functions by using a dash right arrow and defining the value type we want to send back. So in this case, let's send back a string. I really want to reiterate that we're only using print statements in most of our examples to keep things simple, but you can absolutely add in very complex logic, multiple lines, there's really no limit to that. So don't take that as a hard and fast rule that it can only be one line. And we're going to get an error here,
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/basic-functions-14528861?u=76281980&t=94)** because we're missing the return keyword. So anytime we have a return value type assigned in our function, we have to actually return something. So we'll say return and we'll just give it a string literal. Now we can capture our return value in a variable, just like anything else. So we'll say var ally now equals findNearestAlly. And in inspector you can see that ally is now storing Argus. For the last piece of the syntax puzzle here, let's include an integer parameter in the function that we can use inside the function body. So like tumbles, these parameters need a variable name that we can use and a type. So we'll say findNearestAlly is going to take in a parameter called level of type int. And I'm also going to change the print statement just so we can use that locally. Searching for ally above level and we'll use the level parameter. Again, we're going to get an error here because we're missing an argument for the level parameter. If we go back and delete the parentheses and then restart or reopen them, auto-complete is going to suggest that we need to pass in the level parameter. I'm going to say level five. And we can see the printed message in the debug area
>
> **[3:06](https://www.linkedin.com/learning/swift-5-essential-training/basic-functions-14528861?u=76281980&t=186)** referencing the input. And it's still storing the return string, Argus. That's really all there is to the basics of function syntax. You can mix and match functions with either parameters, return types, multiples of either, or really anything that you need in your application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Code Identifiers:** findnearestally (5)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Overloading functions](https://www.linkedin.com/learning/swift-5-essential-training/overloading-functions-14525885?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/overloading-functions-14525885?u=76281980&t=0)** - [Instructor] [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) will let us have multiple functions with the same name, as long as their function type or signature is different. Defining multiple functions with the same name is called function overloading, and it's very useful in cases where you need constant functionality with dependent or computed results. Let's make a new base function, and we'll call this attackEnemy. And we're just going to have it print out a message, with no parameters and no return type.
>
> **[0:40](https://www.linkedin.com/learning/swift-5-essential-training/overloading-functions-14525885?u=76281980&t=40)** Now let's write an overloaded version of attackEnemy. So we still say the same thing, func attackEnemy. But this time we're going to have one parameter. It's going to be called damage of type Int. And we're going to have a different print statement here. It's going to say attacking for the damage passed in. To make things even more interesting, let's create one last overloaded function. Again, it's called attackEnemy. This one is going to have two input parameters, damage of type Double, and weapon of type String, and a return type of Bool. And inside here, we're going to declare a local variable to hold our return value. So we'll just say attackSuccess. I'm just going to default it to true. I'm going to have a print statement here, attacking enemy for damage with the weapon parameter. And we can't forget to actually return a Boolean value. In this case, we can just return the attackSuccess variable. Now, [Xcode](../../Skills/Mobile%20Development/Xcode.md)'s going to give us a warning here. Since we didn't actually change attackSuccess, there's no point in leaving it as a var, so we'll change it to a let constant. Now, if we call each of these functions in turn,
>
> **[2:16](https://www.linkedin.com/learning/swift-5-essential-training/overloading-functions-14525885?u=76281980&t=136)** we're going to see our different outputs. So attackEnemy, or the base attackEnemy, it's going to print out "Executing default attack". The first overloaded function is going to take in damage. Let's say this takes in 34, and we get the "Attacking for 34" debug log. And finally, we're going to create another variable called enemyDefeated to store our return value from the last overloaded attackEnemy, and for the damage, I'm going to say 65.988 with a weapon, Godly Glaive. Now we go attacking enemy for 65.988 with the Godly Glaive. With this, we're in a good position to start talking about some more complex functions and eventually closures.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1), [Xcode](../../Skills/Mobile%20Development/Xcode.md) (1)
> **Code Identifiers:** attackenemy (7), attacksuccess (3), enemydefeated (1)
> **CLI Commands:** make (2)
> **Versions:** 65.988 (2)
> **Definitions:** is called (1)
> **Warnings:** warning (1)
> **Speakers:** - [instructor] (1)

#### [Complex functions](https://www.linkedin.com/learning/swift-5-essential-training/complex-functions-14530751?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/complex-functions-14530751?u=76281980&t=0)** - [Instructor] Functions in [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) can go from simple to complex very quickly. When we start adding in multiple return types, optionals, and even default values. Let's revisit optionals here and add a new function. I'm going to call this setupArenaMatch, with no input parameters, but it is going to return an optional boolean. I'm going to return false here by default, but that can also now hold true or nil. Being the swift programmers that we are, we're going to use our trustee if let statement to unwrap the optional boolean. So we'll say, if let initSuccess, and we just call setupArenaMatch, open and close curly braces and we'll just print out Level initialized and our correctly unwrapped optional. If there's a nil value, we'll handle that too, with else and we'll print out, "Something went terribly wrong." Perfect, now our level initialized is false. We see that in the debug, but we can also change our return value in setupArenaMatch to nil, and the code doesn't crash, and we're perfectly safe. Let's create an overloaded version of setupArenaMatch. Let's say func setupArenaMatch. And this time we're going to have one input parameter
>
> **[1:36](https://www.linkedin.com/learning/swift-5-essential-training/complex-functions-14530751?u=76281980&t=96)** called levelName, type string, and we're going to return what's basically a tuple or multiple return values. I here we use the same tuple syntax, we'll name the return value. So I'm going to say success of type Bool. And it's also going to have a secret item of type string. I'm going to add a print statement here, and print out the level name we're going to pass in. And I'm going to return a very shorthand tuple. You can of course be more explicit if that's your thing, I'm going to say we initialize this to true. And our secret item is going to always be a Minotaur's Head. And the cool thing about returning tuples or using tuples as returned values is that swift is going to do a lot of the heavy unpacking for us. So let's declare a variable called levelDetails, set it to setupArenaMatch that takes in a levelName. And I'm going to call this Poison Flats that we're trying to initialize. And now level details has been correctly inferred as a tuple with a success and a secret item property. (keyboard typing) Lastly, let's create a new function with default values defined right along with the input parameters. I'm going to say func setupDefaultMatch.
>
> **[3:13](https://www.linkedin.com/learning/swift-5-essential-training/complex-functions-14530751?u=76281980&t=193)** For the parameters, again, I'm going to use levelName of type string and opponents of type Int with no return type at this time. Now the set, the actual default values, we add an assigned initial value right in the parameter listing. So we'll say the default value for the level name is always going to be Fire Marshes, and for opponents, we're always going to have three opponents. I'm just going to add another interpolated string here. Arena match will take place in the whatever levelName in this case Fire Marshes by default, between our opponents parameter. (keyboard clicking) If we start calling this method, auto-complete is going to recognize that we included default parameter values, and give us two versions of the function to run. So let's choose the default version first, which has no parameters. And we'll see that debug log print out that our arena is the Fire Marshes between three players. However, just because we specified default values in the function doesn't mean that we can't override them. We can easily say setupDefaultMatch, give it Poisoned Flats and 10 opponents. This approach can be really helpful
>
> **[4:47](https://www.linkedin.com/learning/swift-5-essential-training/complex-functions-14530751?u=76281980&t=287)** if you need a baseline value or values in your functions, but still want the option of modifying them dynamically. And before we continue, I want to call out the fact that all these variations of functions can be combined in any way that you need. They're not limited to the examples here. It's just easier to learn if we separate out the syntax one at a time.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3)
> **Code Identifiers:** setuparenamatch (6), levelname (4), setupdefaultmatch (2), initsuccess (1), leveldetails (1)
> **Non-Speech:** (keyboard typing) (1), (keyboard clicking) (1)
> **Speakers:** - [instructor] (1)

#### [Function types](https://www.linkedin.com/learning/swift-5-essential-training/function-types-14525886?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/function-types-14525886?u=76281980&t=0)** - Every function in [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) has a type or unique signature that can be used like any other type we've seen so far. This can come in handy when you need to pass functions as parameters into other functions, or even use them as return types. Let's start off by declaring a new function. We're going to call this compute bonus damage. This is going to take in one parameter called base damage of type Int. And it's also going to kick back a returned integer. Now the logic in here is pretty simple, we're just going to always return base damage times four. Now for compute bonus damage, the type signature of the function is written like this, open and close parentheses with Int in the middle returns an Int. Knowing the function signature allows us to treat it like a type now that we know how to declare it. So let's write a new function, this is going to be called deal damage. It's going to take in a base damage of type Int, and a bonus damage parameter. But this time, instead of just a type like inter string, it's going to take in our function type signature. So Int inside parentheses returns an Int. Just to drive this home where matching, or declaring this bonus damage parameter to match the function signature of compute bonus damage.
>
> **[1:38](https://www.linkedin.com/learning/swift-5-essential-training/function-types-14525886?u=76281980&t=98)** Now inside here, since our bonus damage property here is kicking back in Int, we're going to capture that in a constant called bonus, and we can now use bonus damage where the parameter as a function. So it takes in our base damage, and we'll kick it back to bonus. Let's also print out an interpolated string, we'll say base damage, is base damage. Add a line break bonus damage is going to be bonus. Now we can call deal damage, pass in an integer. I'm going to say our base damage is 55. And for the bonus damage, we can pass in compute bonus damage as a function. Notice that we don't have to use any parentheses after compute bonus damage because we're treating it as a variable, and not as the function call itself. Now the same approach applies to using functions as return types. Now, in our output, we can see that base damage is 55 and bonus damage is 220. Which means that when we passed in compute bonus damage as a parameter, it was executed as a function inside deal damage. Now you might be thinking, why don't we just do all of this right inside the deal damage function itself?
>
> **[3:12](https://www.linkedin.com/learning/swift-5-essential-training/function-types-14525886?u=76281980&t=192)** The answer is you absolutely could, but separating out code like this will keep things cleaner, more maintainable, and largely more modular in the long run. So make good use of it when you can.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **CLI Commands:** make (1)
> **Definitions:** means that (1)
> **Speakers:** - every (1)

#### [Understanding closures](https://www.linkedin.com/learning/swift-5-essential-training/understanding-closures-14524899?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/understanding-closures-14524899?u=76281980&t=0)** - [Instructor] Closures in [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) act like blocks or Lambda expressions in other programming languages, allowing for a more concise way to write, store, and return function like operations. This is one of those things that's easier to understand in the code itself. So let's declare a variable. We're going to say this is closure, and for the type annotation, we're going to give it a closure type. The base or empty closure is two parentheses with a return type of the same. This should look familiar as it's basically a function signature. Since closures are a type in themselves, we can assign initial values. All we need is open and closed pair of curly braces, which stands in for the function's body. Now, here I've explicitly typed closure as a closure type, but the compiler can infer that from the curly braces if that's more your style. We can also see in the inspector that closure holds a function signature as its value, which should tell you that closures can be used as parameters, return types, or variables like anything else. Now, an empty closure doesn't do as much good. So let's revisit our function types example, declare a new closure to compute bonus damage. Say var_computeBonusDamage is going to be a closure of type int that returns an int.
>
> **[1:37](https://www.linkedin.com/learning/swift-5-essential-training/understanding-closures-14524899?u=76281980&t=97)** To initialize the closure, we need a pair of curly braces. And usually you'll see these on multiple lines like this to declare the body. We also need to explicitly capture the closures parameter. So in this case, it's going to be an integer. And you can choose the name of this, I'm going to say base of type int. And you need to match the function signature. So it's going to kick back an int, and we're going to use the in keyword. Now I know that's a lot of weird looking syntax, so let's break it down. Our closure or compute bonus damage variable is defined as taking in an int parameter and kicking one back as a return value. So inside the body of the closure, we have to mimic that signature. The in keyword marks the start of the closure statement body. So anything that goes after that is going to be executed when the closure is called. If we cut out the type annotation now, the compiler is going to be able to correctly infer that compute bonus damage is a closure that takes in an int and returns one as well. And we're getting an error here because I haven't actually specified a return value. So let's copy what we did from the last video and always the base damage times four. To call the closure, we use its name, computeBonusDamage, but since it's holding what's essentially
>
> **[3:11](https://www.linkedin.com/learning/swift-5-essential-training/understanding-closures-14524899?u=76281980&t=191)** a function as a value, we add the open and closed parentheses to execute the body. And we're also going to get an error here because it does take in an argument. So let's specify that the base damage is 25. In the inspector we can see that it's returned 100, which is our base damage multiplied by four. You also see that this actually runs twice. We have 100 showing in the inspector where we actually declared compute bonus damage, and where we called the closure. Now this is because when we call computeBonusDamage, it's actually referencing the compute bonus damage variable. And we haven't gotten to reference in value types just yet. So keep this in the back of your head for later. And there are a lot of different shorthands and tricks to writing closures in Swift with the least possible characters. But the one I use the most is the following. You can strip out the parentheses and the named value and type, and just say base in. This is just the right amount of explicit readability and implicit type inference for me personally. But I'm going to leave you to explore the other options as they're totally dependent on your programming style and the compiler could care less which one you use. You just remember as long as you have either a type annotation or an explicitly assigned closure, you're good to go.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2)
> **Definitions:** defined as (1), is called (1), is a  (1)
> **Code Identifiers:** computebonusdamage (2)
> **Speakers:** - [instructor] (1)

#### [Using closures](https://www.linkedin.com/learning/swift-5-essential-training/using-closures-14527902?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/using-closures-14527902?u=76281980&t=0)** - [Instructor] Now that we've got basic closure syntax covered, we can start working with them as parameters in functions we've already used, like sorted. For this example, we're going to create a new variable and we're going to sort our high scores array from lowest to highest. I'm going to say VAR, ascending sort, and this is going to capture our sorted array. And we're just going to say high scores dot sorted. Now you can see in the auto-complete that one of the overloaded methods accepts a closure with two integer parameters and returns a Boolean. Now don't worry about the throws and rethread is keywords we'll get to those when we talk about error handling. I'm going to select this option and I'm going to hit enter so that auto-complete can set up the closure for us leaving the local parameter names and return value for us to specify. I'm going to keep this sort of as the long hand, just for clarity. I'm going to call the first captured value, first value. And the second value, second value. The sorted closure works like this. It's going to take the first number in the array and then the next number and do some kind of comparison that we specify. And then it's going to keep looping through each high score until it gets to the end. So what we can do here is create the ascending order from lowest to highest by returning true, if the first value is less than the second value.
>
> **[1:38](https://www.linkedin.com/learning/swift-5-essential-training/using-closures-14527902?u=76281980&t=98)** If we print out ascending sort, we should see that it goes from zero all the way to 3412. And using closures like this to perform operations, that sort or map come in handy quite a bit, but it's also useful to be able to use custom defined closures in your own methods. Let's recreate a simple function here that accepts a closure as a parameter. So we're going to call this function active members. We're going to call the closure parameter completion, and it's going to take in an array of strings and kickback back out void, which is no return type. Now, when a function takes in a closure like this, the closure parameters and statement body are going to be available when the function is called, just like the sorted method allowed us to write in our predicate when we sorted the first value and the second value. Now, in order for that to happen, we actually need to call the closure parameter inside active members and pass it a string array to close over. So we'll call completion. And since a closure is basically a function, we add the open and close parentheses and for our parameter, we're just going to get it party members, which I've declared at the top of the file for us to use. And let's call active members and see how this actually works.
>
> **[3:11](https://www.linkedin.com/learning/swift-5-essential-training/using-closures-14527902?u=76281980&t=191)** All right, so we've got the same sort of syntax that sorted has now. And we're going to capture the string array. We're just going to call it members. And we're just going to loop through these with a for-loop. So for name in members, print out an interpolated string, and we'll just say name is active. There we go, in our debug log, we've got Harrison is active. Steven is active, and Bob, the Noob is active. At this point, you might be a little confused as to why we would even use this approach instead of an actual function for a parameter like we did earlier. The answer is that we had to define the logic we wanted the function to have before we used it. With closures, we can defer that data operation logic until we actually call the method. Now this approach lets us store access and operate on data outside of its declared scope. So in our case, we could declare the party members array and the active members function in one class and call it in a completely different one. The bonus here is that we're essentially passing the data inside active members to the closure, which we can access from any other class. Now that's only one way of passing data and it's one way traffic, but we can sort of do this in reverse, still using closures. So let's make an overloaded version of active members.
>
> **[4:50](https://www.linkedin.com/learning/swift-5-essential-training/using-closures-14527902?u=76281980&t=290)** We're going to call this active members again, and we're going to pass in a parameter of type string array. This is going to be called members and we're going to actually return a closure. Our closure return is going to take in no parameters, it's going to return void or nothing. Inside, we're going to declare a completion closure, and we're going to use a type annotation to make sure we know what we're doing. So this completion variable is going to hold a closure that takes in nothing and returns void, just like our return type. And we're going to give it an initial value. Here, we're just going to do the same thing, as we did an active members, we're going to loop through every name in members. We're going to print it out. And finally, we're going to return our completion variable. And we didn't actually mutate completion, so we're going to change that to a let constant. The debug functionality that we've just created is now stored in the completion variable as a closure and returned, so we can capture it like any other value type. We'll declare a new variable, and this will be closure return. It's going to be equal to are overloaded active members.
>
> **[6:30](https://www.linkedin.com/learning/swift-5-essential-training/using-closures-14527902?u=76281980&t=390)** And for the members array again, we're just going to use party members. Now here's a gotcha, I run into a lot when people ask about closures and storing them as values, cause we're not actually seeing a new debug statement, it's still the old ones. So why is this happening? Well, take a closer look at our actual code. Closure return, the variable is storing a closure, which is a shorthand function. Now in order for a function to execute, it has to be called and we haven't actually done that yet. So now our closure return can be treated exactly like a function. We can call it and get a new printout statement. And there we go. We have the is active statement, but we also have the new three lines where we have Harrison is present, Steven is present, and Bob, the Noob is present. I've gone through the returning a closure example because you should know that it's possible. However, they are used rarely in this kind of situation. It's much more common to need them as input parameters.

> [!info]- Semantic Content
>
> **CLI Commands:** make (2)
> **Definitions:** is called (1), is a  (1)
> **Analogies:** just like (2)
> **Env Vars:** var (1)
> **Warnings:** gotcha (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Type aliasing](https://www.linkedin.com/learning/swift-5-essential-training/type-aliasing-14524898?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/type-aliasing-14524898?u=76281980&t=0)** - [Instructor] Type aliasing is a great tool for combining complex or unwieldy custom values into clearer defined types. For this example, we're going to look at creating a type alias for a tuple that we've already used, and for a closure. Now, let's revisit our attack tuple, but this time we're going to declare it as its own custom type. So, we start with the typealias keyword, and if you hit Enter, autocomplete is going to set this up for us. So, we need to give our type a name. I'm going to say these are all going to be called AttackTuple, and for the definition I'm going to say, name is going to equal to string, damage int, rechargeable bool. And what we said here is that whenever AttackTuple is used as a type, the compiler is going to infer that it's a tuple of a string, int, and bool. When you're using type aliases or defining them, as we have here, you'll see that we use the equal sign, but it's followed by a type signature, and this is the only real case where this is going to happen because you're defining the type, not assigning it an initial value. Now, AttackTuple can be used as a regular type. So, we'll declare a new variable here called sunStrike of type AttackTuple. Let's actually initialize it here. We'll say its name is Sun Strike, damage is 45, and it is not rechargeable.
>
> **[1:36](https://www.linkedin.com/learning/swift-5-essential-training/type-aliasing-14524898?u=76281980&t=96)** We can also use this as a input parameter type and a return type. Let's declare another function that's just going to take in an AttackTuple, and return a modified version. So, we'll say func levelUpAttack. It's going to take in a baseAttack parameter of AttackTuple, and it's going to return an AttackTuple. Pretty simple. Inside, we're going to create a new variable for our return value. We'll just call this increasedAttack of type AttackTuple, even though type inference will work here correctly. This is just to be explicit, and for the name we will keep the baseAttack name. Damage is going to go up, so we'll say baseAttack damage plus 10, and baseAttack rechargeable. Actually, we're going to change that to true. Just to give it a little bit more modification. Now, all we have to do is return increasedAttack, and we've got a function that uses AttackTuple as a parameter and a return type. Let's just call this and say, var poweredAttack. It's going to be equal to levelUpAttack, and we want to level up sunStrike. Now, if we pop the view open from the inspector we'll see that sunStrike is still the name. The damage has been upgraded to 55,
>
> **[3:11](https://www.linkedin.com/learning/swift-5-essential-training/type-aliasing-14524898?u=76281980&t=191)** and rechargeable is now true. Now, for the closure, let's use our typealias keyword again. We're going to call this new type an ArrayClosure, and the type signature is going to be an array of strings and it kicks back no return or void. Now, instead of this long completion signature here in activeMembers from the last video, we can just specify that we're going to pass in an ArrayClosure. Our code is going to execute exactly the same, but now activeMembers is very explicit and it's very clear on what's happening without having to reference a specific type or closure signature in the function.

> [!info]- Semantic Content
>
> **Code Identifiers:** baseattack (4), sunstrike (3), levelupattack (2), increasedattack (2), activemembers (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Battlegrounds](https://www.linkedin.com/learning/swift-5-essential-training/challenge-battlegrounds?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/challenge-battlegrounds?u=76281980&t=0)** (upbeat bouncy music)
>
> **[0:06](https://www.linkedin.com/learning/swift-5-essential-training/challenge-battlegrounds?u=76281980&t=6)** - Here we are again at another chapter challenge. And this time we'll be laying out some basic battle logic with our newfound function, syntax. Your tasks are going to include basic and overloaded functions, as well as creating your own custom type using a type alias. This one should be pretty fun since we have a lot of tools at our disposal right now, so get creative with it. This should take you around 10 minutes, but as always, if you get stuck, head over to the next video, follow along with my solution. Happy coding.

> [!info]- Semantic Content
>
> **Cross-References:** next video (1)
> **Speakers:** - here (1)
> **Non-Speech:** (upbeat bouncy music) (1)

#### [Solution: Battlegrounds](https://www.linkedin.com/learning/swift-5-essential-training/solution-battlegrounds?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/solution-battlegrounds?u=76281980&t=0)** (soft music)
>
> **[0:06](https://www.linkedin.com/learning/swift-5-essential-training/solution-battlegrounds?u=76281980&t=6)** - [Instructor] All right, let's jump into our battlegrounds challenge and start working with functions, closures, and type aliases. For our first task, we're going to create a type alias called attack, and we're going to name values for name and damage. So we'll say typealias attack, specify a name of type string, damage of type Int. With our second task, we're going to write a function called attackEnemy. It's going to take an integer and just print out a creative interpolated string. Let's say attackEnemy. It's going to take in a damage parameter type Int, and I'm going to print out Enemy hit for the damage parameter, damage. I'm going to add a line break there just so we get a clean printout. For task number three, I'm going to create an overloaded version of attackEnemy. So we'll say func attackEnemy again, but this time we are going to have the input parameter be of type attack from our type alias. And this is going to return a string value. I'm going to write the return with a string literal instead of a variable. It's just a little quicker, but here we can just say, you attacked with, we're going to use our attack parameter.
>
> **[1:43](https://www.linkedin.com/learning/swift-5-essential-training/solution-battlegrounds?u=76281980&t=103)** We can use the.name property, add a line break, Enemy hit for attack.damage, damage and a new line break.
>
> **[2:02](https://www.linkedin.com/learning/swift-5-essential-training/solution-battlegrounds?u=76281980&t=122)** And with those two functions defined, our fourth task is to use both of them. I'm going to use our base one first and just say, attackEnemy with the damage parameter. I'm going to say, we'll attack for 90. There we go, we get enemy hit for 90 damage. For the overloaded function, I'm going to store the return string in a variable I'm going to call this message, attackEnemy, and let's see, I'm going to create an attack type right in line here. So this is going to say, power slam for a hit count of 55.
>
> **[2:48](https://www.linkedin.com/learning/swift-5-essential-training/solution-battlegrounds?u=76281980&t=168)** We print out the message here. We get you attacked with power slam, enemy hit for 55 damage. All right, so both of those are working as expected. For task number five, we're going to create a typealias closure like we did in the last video and it's going to take an array of attacks and return void. So let's say typealias. I'm going to call this new type AttackClosure. And it's going to take in an array of attack types and return void or nothing. I'm also going to declare a test array of a bunch of attacks so we can work with them in task six and seven. So I'm going to say playerActions, it's going to equal an array of attacks. I'm going to create these all in line here just for efficiency. Let's say, Mist Bomb. It's going to be 45, Rangers Eye, it's going to be for 15 and Fists of Fury for five. Our six task is going to be to write a function that uses a closure parameter of type AttackClosure, and just closes over some data that we pass. I'm going to call this func fetchPlayerAttacks, and name the parameter closure,
>
> **[4:26](https://www.linkedin.com/learning/swift-5-essential-training/solution-battlegrounds?u=76281980&t=266)** and it's going to be of type attack closure. I'm going to remember that when we use closures as parameters, we need to call them inside the function method. So we'll say closure and we'll pass this in our playerActions array. For our final task, we're going to call [Fetch](../../Skills/Web%20Development/Fetch.md) players and we're just going to loop through the closed over player actions. So we'll say fetchPlayerAttacks, hit enter for the closure Auto-complete. I'm going to capture the closure data as attack options and use a for-loop. So let's say for, name, damage, I'm going to use a for-loop here. And since we're getting back an array of attacks, we can unpack them as a tuple. So we'll say for name and damage in attackOptions, print out name, will hit for damage if cast, perfect.
>
> **[5:41](https://www.linkedin.com/learning/swift-5-essential-training/solution-battlegrounds?u=76281980&t=341)** If we go down to our debug log, we'll see that Mist Bomb is going to hit for 45, Rangers Eye for 15 and Fists of Fury for five.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Code Identifiers:** attackenemy (6), playeractions (2), fetchplayerattacks (2), attackoptions (1)
> **Cross-References:** in the last (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (soft music) (1)


### 6. Classes, Structs, and Beyond

[↑ Back to Table of Contents](#table-of-contents)

#### [Populating the game world](https://www.linkedin.com/learning/swift-5-essential-training/populating-the-game-world-14526925?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/populating-the-game-world-14526925?u=76281980&t=0)** - At this point, we've covered the separate pieces of the [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) language that make up a basic program, but not how to combine them into coherent, usable objects. Up to now we've been using playgrounds, but in a real application we'll want classes and other data models that we can define and control. Mastering [Data Modeling](../../Skills/Data%20Science/Data%20Modeling.md) brings our Swift adventure one step closer to the finish line. So, let's get to it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2), [Data Modeling](../../Skills/Data%20Science/Data%20Modeling.md) (1)
> **CLI Commands:** make (1)
> **Speakers:** - at (1)

#### [Value vs. reference types](https://www.linkedin.com/learning/swift-5-essential-training/value-vs-reference-types-14531787?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/value-vs-reference-types-14531787?u=76281980&t=0)** - [Instructor] In [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md), there are two kinds of data types, references types and value types. Under the hood, all Swift basic data types are implemented as structures, which are value types. These include strings, characters, arrays, dictionaries, booleans, integers and floating-point numbers. Enumerations are also value types, and since Swift implements optionals as enums, they're also value types by association. Don't worry too much about memorizing all this right now. We'll get into how each of these works individually. As far as reference types go, functions, classes and closures are going to fall into that category. The main point to focus on when thinking about value versus reference types is how they're passed or assigned. Let's look at an illustrated example for how value types work. Imagine we're playing a video game and we've just walked into a shop. There are items on all the shelves and along the walls. We see a cool sword and we want to buy it. Since the sword represents a value type in this example, we aren't actually going to buy the sword hanging on the wall. Instead, the shopkeeper is going to give us our own sword, which is an identical copy of the one we pointed at. Now, this is how value types are passed, by copy. Anything we do to the sword we buy doesn't affect the one on the shop wall in any way. We can lose ours. We can break it. We can leave it in a ditch. The sword on the wall remains unaffected. When we refer to the sword on the wall
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/value-vs-reference-types-14531787?u=76281980&t=94)** versus the sword that I just bought, they are two separate objects. As the name suggests, reference types are passed by reference. Let's revisit our game example again. Let's say I have a player avatar representing a reference type in this example. That exists in the game world. Anytime someone interacts with my avatar, they're interacting with my specific individual object. If I want to go to another stage of the game, my avatar isn't copied to that level. Instead, a reference to my avatar is passed to that level. Now, anything that happens to my avatar anywhere in the game will permanently affect it since it's not a copy, but the real thing. This is what we mean by a reference type. Now, when you're choosing between making an object a struct or a class, ask yourself if you need a small encapsulated data object or something that can use inheritance and can be a reference rather than a copied value. I'd highly suggest referring to the Apple Doc's list of appropriate use cases for each. It's really helpful when the decision seems like it's a toss-up.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3)
> **UI Navigation:** go to (1)
> **Definitions:** is an  (1)
> **Analogies:** imagine (1)
> **Speakers:** - [instructor] (1)

#### [Basic Swift classes](https://www.linkedin.com/learning/swift-5-essential-training/basic-swift-classes-14528843?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/basic-swift-classes-14528843?u=76281980&t=0)** - [Narrator] Even though [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) has an entire library of built-in classes and structs, it's important to know how to create your own. For the next few videos we're going to explore the class and struct syntax, how to add properties, methods, initializers, and finally, how to subclass. Let's start off by defining a class called Adventurer. All we need to do for that is use the class keyword, give it a name, and an open-close pair of curly braces. Any properties and methods defined inside this class declaration are going to belong to specific instances of an adventurer that we create. If you're coming from a language that refers to instances as objects or class objects, that's perfectly fine. Just be aware that we're talking about the same idea. All our adventurers should have a name. So we'll declare a variable called name of type string. They should also have a constant maxHealth of type int. And I'm also going to give them an optional value called specialMove. This is going to be an optional string. Any instance properties like these can be declared with or without initial values based on the situation, it's up to you. They follow the same rules inside classes that we've already seen in previous examples. Now, in order to actually create an instance of our adventurer, we need what's called an initializer function. The purpose of this is to set all non-optional values
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/basic-swift-classes-14528843?u=76281980&t=94)** when an adventurer instance is created. So for this, we use the init keyword and if we hit Enter, auto-complete is going to give us the basic setup. Now initializers are functions so we declare the parameters we want to accept, and these need to match our non-optional values. So we need a string and we need an int, and we'll call the int maxHP. Now inside the init function, the only job this should have is to set our adventurer properties. So we'll say self.name is going to equal the init name parameter, and self.maxHealth is going to be equal to the maxHP passed in. This initializer function is commonly referred to as the designated init, but in many cases you'll need more flexibility, which is where convenience initializers come in. These are essentially overloaded init functions with their own unique signatures. You can have as many as you want, but they do all need to delegate to the designated init at some point in their execution. That sounds more difficult than it really is so let's see that in practice. We're going to declare a convenience init with the convenience keyword, followed by init, and the parameters we want to take in. For this one, we want an option that only requires a name for us to create an adventurer. Our job inside the convenience init
>
> **[3:08](https://www.linkedin.com/learning/swift-5-essential-training/basic-swift-classes-14528843?u=76281980&t=188)** is to pass up the name parameter into the designated initializer. And we can do this by using the self keyword, which is going to reference the adventurer class. We'll say self init. And for name, we'll pass it name. And for maxHP, we're going to say these adventurers always start with 100. If we go outside our class curly braces, we can declare instances of our adventurers. So I'm going to say var player1 is equal to Adventurer. I'm going to use our designated init first. I'm going to say Harrison, maxHP is 99. I'm also going to declare player2 and use our convenience init so we can see how this works. And all we need here is a name. Now, if you expand the adventurer, quick lookup in the inspector, you can see that player1 is Harrison 99 and has no special move, so it's nil. Steven is Steven, 100, and also nil for the special move. Since classes can have their own functions, let's add one into adventurer. That's just going to print out our property so it makes a little easier to debug. We're going to say funk printStats with no parameters and no return type. We're just going to use the print function here
>
> **[4:41](https://www.linkedin.com/learning/swift-5-essential-training/basic-swift-classes-14528843?u=76281980&t=281)** to debug an interpolated string. So I'm going to say Character is going to be self.name. Max health is going to be self.maxHealth. And we can say player1.printStats and player2.printStats. Perfect. And to really drive home how reference types work, let's declare one last player variable. We're going to call this defaultPlayer, and we're going to assign it to player1. I'm actually going to cut and copy the defaultPlayer, put it up before our printStats so we can see this change. Since both adventurer instances are classes, classes are reference types, if we change a property in defaultPlayer or player1, both of the instances are going to reflect that change. So let's say defaultPlayer.name equals Bob the Noob. You'll see in the debug log that our first character, which is player1 is now Bob the Noob with a max health of 99. The Harrison string was overridden. We can also change player1.name. We can change it back and this will affect both the defaultPlayer and player1 instances.
>
> **[6:15](https://www.linkedin.com/learning/swift-5-essential-training/basic-swift-classes-14528843?u=76281980&t=375)** Be careful with this behavior when deciding to store a reference instead of a value type as it can lead to unintended data changes throughout your entire app.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Code Identifiers:** defaultplayer (5), maxhp (4), printstats (4), maxhealth (3), specialmove (1)
> **Definitions:** refers to (1), is a  (1)
> **Warnings:** be aware (1), be careful (1)
> **Prerequisites:** setup (1), you'll need (1)
> **Speakers:** - [narrator] (1)

#### [Access modifiers and properties](https://www.linkedin.com/learning/swift-5-essential-training/access-modifiers-and-properties-14528846?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/access-modifiers-and-properties-14528846?u=76281980&t=0)** - [Narrator] Like most programming languages, [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) allows us to limit or control how our classes, structs, properties and other entities are accessed based on the module or file they're in. Swift defines individual modules as frameworks or applications made up of source files that act as a single unit of code distribution. In plain English, each of our playground files is its own module as is any single target app you make in the future. Getting into apps with multiple build targets or frameworks is way outside the scope of this course and won't come up until later in your Swift journey. Now, there are four levels of access in Swift. We've got public, internal, file private, and private. Public entities can be freely accessed from inside their source file, as well as any separate module that imports the parent module. Internal entities can be accessed by any source file within their defining module, but nowhere else. Every entity in Swift is set as internal by default, which is why we haven't had to explicitly set an access level so far. File private entities can only be accessed inside their defining module. Lastly, private variables can only be accessed inside their declaration scope.
>
> **[1:35](https://www.linkedin.com/learning/swift-5-essential-training/access-modifiers-and-properties-14528846?u=76281980&t=95)** So if we changed our name to private, it's only going to be accessible inside the Adventurer class. Nowhere else. There are variations on how access control works with sub-classing, [Unit Testing](../../Skills/Software%20Development/Unit%20Testing.md) and protocols, but that's an area you'll need to explore after you're comfortable with the Swift basics. Now I have copied our entire Adventurer class here so that we can look at some additional kinds of properties that it can store. First up are computed properties, which come in two variations, read only and read write. Let's start with a read only property. We're going to call this healthLost and have it return a computed int value.
>
> **[2:25](https://www.linkedin.com/learning/swift-5-essential-training/access-modifiers-and-properties-14528846?u=76281980&t=145)** You'll notice that this syntax is a mix between a variable declaration and a function, which is actually where computed variables sort of sit. So here we can say return maxHealth minus. Well, let's say 50 for now. Every time we call healthLost, it's going to return the result of that subtraction. However, that's all it's going to be able to do. We can't actually assign a new value to healthLost or change it in any way. If we do need a property that can be retrieved and assigned with some kind of added computed logic, we can declare it with explicit get and set events, which makes it a read and write property. For this example let's add a file private variable. We're going to call this health. It's going to be an integer. And we're going to leave it undefined. Since this is a non-optional value, the compiler is complaining that our initializer doesn't actually set it. So we'll say self.health. We're also going to just start that out as our maxHP. Perfect. This is a common scenario where we have a private variable that needs some sort of public interface. You might know this as a backing field. So what we can do is add a new variable underneath our file private health. I'm going to call it Health with a capital H.
>
> **[3:58](https://www.linkedin.com/learning/swift-5-essential-training/access-modifiers-and-properties-14528846?u=76281980&t=238)** It's going to be of type int, and it's going to be a computed property. This time though, we're going to use the get keyword, and this is its own event function. And for get we're always going to return our file private health. So anytime capital H Health is queried, our lowercase H is going to be returned. Now, anytime we try and assign capital H Health a new variable, our set event is going to fire. And this is where we want an added bit of logic. So we're going to say if new value is less than or equal to 100, assign our private health to the new value. This newValue keyword, although it's not being highlighted, is used by Swift to locally store the value we're trying to assign to the computed property. So for example, if we tried to change health to 25, newValue would be 25 and the if statement would evaluate. Before we move on, we're going to change the healthLost to use our new health property so this is always going to be an accurate computation of how much health we've lost. We're also going to change the print stats message to be a little more explicit. And we're going to say Health is going to be equal to self.health divided by self.maxHealth.
>
> **[5:39](https://www.linkedin.com/learning/swift-5-essential-training/access-modifiers-and-properties-14528846?u=76281980&t=339)** There we go. So now our debug logs have changed. Our health is now 99 out of 99 for Harrison and 100 out of 100 for Steven. Lastly, we need to talk about type properties. These are going to belong to a specific type, in this case our Adventurer class, rather than the specific instances of adventurers that we've created. Type properties come in two flavors, static and class. We're going to use the static keyword here, declare it as a variable, and call it maxActivePlayers. Set that equal to 10. Static types cannot be overridden in a subclass. However, if we need a variable to be overridable in a subclass, which we'll get to in the next video, we can declare it as a class variable. I'm going to say class var credo of type string. And class variables can only be computed properties. So I'm going to always return "Defend the helpless!"
>
> **[6:55](https://www.linkedin.com/learning/swift-5-essential-training/access-modifiers-and-properties-14528846?u=76281980&t=415)** To drive this home, let's see if we can access either of these from our player one instance. If you hit the dot notation, you'll see that they do not show up. And it's because they belong to the Adventurer class itself. So we have credo here and Adventurer.maxPlayers. These are going to be the same across the entire class wherever they're referenced. These are particularly useful when you need to store variables that are essentially global to a class or struct.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (7), [Unit Testing](../../Skills/Software%20Development/Unit%20Testing.md) (1)
> **Code Identifiers:** healthlost (4), maxhealth (2), newvalue (2), maxhp (1), maxactiveplayers (1)
> **Definitions:** is a  (3)
> **Cross-References:** later in (1), in the next (1)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [narrator] (1)

#### [Subclassing in Swift](https://www.linkedin.com/learning/swift-5-essential-training/subclassing-in-swift-14529814?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/subclassing-in-swift-14529814?u=76281980&t=0)** - [Instructor] A great way to expand the flexibility of your custom classes, without having to reinvent the wheel, is to create subclasses that share functionality with their parent. For this example, we're going to create a subclass of Adventurer, we're going to call it Ranger and change some of its inherit properties. Under the subclass comment, and declare a new class, name it Ranger, and a subclass we just add a colon and the name of the parent class we want. (keyboard clicking) Depending on the access level of the parent, a subclass is going to have access to its properties, methods and other entities. In this case Ranger is going to inherit name, special move, maxHealth and our computer properties. So there's no need to duplicate that code. However, if we want Ranger to have unique properties that the Adventurer parent class doesn't have, we can declare them. So we'll say var classAdvantage, and this is going to be a string. This goes for methods as well. Just understand that anything declared in the subclass is not accessible to the parent. Inheritance is a one-way street and it trickles down from top to bottom. A Ranger still is a class so we need a designated initializer. we'll say init, (keyboard clicking) we want a name of type string, and an advantage of type string. Again we have to set all non-optional properties.
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/subclassing-in-swift-14529814?u=76281980&t=94)** So we'll say self.classAdvantage is going to equal advantage. Now here we have the option of manually reinitializing all our parent properties. But the more efficient way to do that is to pass the responsibility up to the parent classes designated init. Now we can use a new keyword here It's called Super. Super references Adventurer in this case. But it'll reference whatever parent class you're using. So now we have access to the adventurer init, and we'll pass it up into name and maxHP. So pass name and we'll say Rangers have more XP at 150. It's really important to note this order of operations when you're Subclassing in [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md). Any properties on our subclass needs to be initialized before we call the parent classes init function not after. The last thing we're going to do here is talk about overriding properties and methods. Since we declared credo as a class of variable, we can make sure that this is not the same for Ranger as it is for Adventurer. We use the override keyword here, (keyboard clicking) and then we declare credo the exact same way as an Adventurer. So class var credo (keyboard clicking) of type string, remember it has to be a computer property because it's a class property, and we'll return "To the King." (keyboard clicking)
>
> **[3:10](https://www.linkedin.com/learning/swift-5-essential-training/subclassing-in-swift-14529814?u=76281980&t=190)** Now if we print out the Ranger.credo, we will get a different value. As far as methods go, these are also overridable. So let's have Ranger print out a different debug log when we call printStats. Again we use the override keyword, func and the same name and auto-complete is going to know that we have that. And we'll just print out a new interpolated string. Here we'll say self.name:Ranger, (keyboard clicking) Advantage, (keyboard clicking) self.classAdvantage that's what we name that. let's declare a new Ranger. We're going to call this Aragorn. (keyboard clicking) It's going to be a Ranger class, (keyboard clicking) and advantage is going to be stealth. If we call printStats on our new Ranger, we're going to see that the debug messages now show up differently depending on the class we use. But, they still follow the same basic blueprint.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Non-Speech:** (keyboard clicking) (9)
> **Code Identifiers:** classadvantage (3), printstats (2), maxhealth (1), maxhp (1)
> **Definitions:** is a  (2)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Basic Swift structs](https://www.linkedin.com/learning/swift-5-essential-training/basic-swift-structs-14524894?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/basic-swift-structs-14524894?u=76281980&t=0)** - Structs share similar syntax and functionality with classes, but they're useful for different situations. While classes are well-suited to complex [Data Structures](../../Skills/Software%20Development/Data%20Structures.md), structs are better suited to modeling simple objects that don't need inheritance or reference capabilities. Let's start off by declaring a new struct using the struct keyword, the name it level, give it a set of open and closed curly braces. Now, other than swapping out that new keyword, the syntax for controlling access, adding properties methods, and other entities to structs remains the same as with classes. So for instance, we could add a constant called level ID of type int, a variable called level objectives, which is going to hold a string array and let's do a read only computed property called level description of type string. And this is always going to return an interpolated string that says level ID and we'll use self dot level ID. One important difference with structs is that they do come with a default initializer already configured. So below our struct declaration, I'm going to declare a new variable. This is just going to be a test string array of objectives that we can use going forward.
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/basic-swift-structs-14524894?u=76281980&t=94)** I'm going to say find the lost cat, collect all gemstones, and defeat the big boss. I'm going to create a new level. I'm just going to call this level one. And if we use the level struct name and one parentheses, we'll see that our default initializer has already been set up for us. I'm going to assign this level ID one and our objectives array to level objectives. If you do need to customize an initializer you can absolutely do that, and it's the same syntax. We use init and we're going to want to take in an ID of type int and an array of strings called objectives. Now we have to set all our non-optional properties against, we'll say self dot level ID equals ID and self dot level objectives equals objectives. Because we've chosen to define our own initializer the default or what's called a member wise initializer that was originally set up for us is no longer available, so you have to choose either the default configuration for struct or your own init. I'm going to get rid of this error, just delete the level initializing code and use the custom one we just defined.
>
> **[3:09](https://www.linkedin.com/learning/swift-5-essential-training/basic-swift-structs-14524894?u=76281980&t=189)** I'd suggest using the default initializer with structs whenever possible, just because it cuts down on the amount of manual coding you have to do. Defining more than one initializer is also possible with structs. You just don't have to include the convenience keyword. To define a function that performs some kind of operation is the same syntax is with classes, provided that it doesn't attempt to modify any of the struct's properties directly. For instance, let's have a simple function here. We're going to call this query objectives. It's going to have no parameters and no return type. And it's just going to loop through and print out our level objectives. Say for objective in level objectives, print out the objective. No big difference there. But what if we needed to add a function that modified our level objectives property by removing a completed item? So let's say func, complete objective. This is going to take in an index parameter of type int, so we know how to look up the array item that we want to remove. And we'll just say level objectives remove at index. Now this error is very important. Cannot use mutating member on immutable value. Self is immutable.
>
> **[4:44](https://www.linkedin.com/learning/swift-5-essential-training/basic-swift-structs-14524894?u=76281980&t=284)** We didn't have to worry about the safety of modifying member properties in classes because their reference types and self is always mutable by default. Since structs are value types and passed around by creating copies of themselves. We have to add the mutating keyword to give our let's say explicit permission for the function to perform this kind of action. Let's drive the value versus reference type concept home one more time. We're going to declare a new variable called default level. I'm going to assign it to level one. Now let's take level one, complete the first objective at index zero and print out its objectives. Perfect. Now we have just collect all gemstones and defeat the big boss. What we'd expect to see if this was a class was that default level also changed, but if we query our objectives from default level, it's still going to have all three. And that's because when we assigned default level to level one, it's actually assigning a copy, not the level one instance itself. Again, you want to keep this difference between structs and classes in mind when your modeling your application data in [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md).

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **CLI Commands:** find (1), cat (1)
> **Analogies:** for instance (2)
> **Prerequisites:** set up (2)
> **Speakers:** - structs (1)

#### [Chaining optionals](https://www.linkedin.com/learning/swift-5-essential-training/chaining-optionals-14531798?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/chaining-optionals-14531798?u=76281980&t=0)** - [Instructor] Optional chaining allows you to unwrap an optional type that has optional properties of its own. It's handy to think of these optional chains in a very literal sense, like a chain link. If all the links are present and strong, the chain remains unbroken, but if one link fails, the chain breaks apart. I've set up two structs for us in this playground, an Item and an Owner. Each item has a description and an optional owner property, while the Owner struct just has a name. Let's start off by creating an item. I'm going to call this rareDagger, and I'm going to use the Item struct's default initializer. For the description, just going to say "A unique dagger of unknown origin." I'm going to set the previous owner to nil for now. In order to get the name of the previous owner, if there is one, we create an optional chain by saying if let, and if there is a unwrapped optional, I'm going to call that owner. Here, I'm going to query rareDagger.previousOwner?.name.
>
> **[1:17](https://www.linkedin.com/learning/swift-5-essential-training/chaining-optionals-14531798?u=76281980&t=77)** The added question mark here is telling the compiler to return nil if previous owner is nil, and not to bother with trying to get the name property of something that doesn't exist. So if we get a value back, I'm going to print out "This item used to be owned by \(owner)." Else, let's print out, "Looks like this items history is unknown..."
>
> **[1:53](https://www.linkedin.com/learning/swift-5-essential-training/chaining-optionals-14531798?u=76281980&t=113)** Now there's no real limit to how many optionals you can chain together like this. It just depends on the nested optionals and your [Data Structures](../../Skills/Software%20Development/Data%20Structures.md). However, it's important to understand that even though the name property isn't an optional in the struct, using optional chaining automatically returns it as an optional value. Think of it this way. Optional chaining is always going to return an optional, regardless of how the actual property you're looking for is declared. Now, if we create an Owner, and we'll call this daggerOwner, we'll use the Owner struct's default initializer, and we'll say the name of the previous owner was "The Great Thief." We can assign daggerOwner to rareDagger.previousOwner and see our different debug log print out,
>
> **[2:51](https://www.linkedin.com/learning/swift-5-essential-training/chaining-optionals-14531798?u=76281980&t=171)** "This item used to be owned by The Great Thief." Now the principle of optional chaining works for both retrieving and assigning values. For instance, let's comment out setting daggerOwner as the item's previous owner, and try to set the previous owner's name manually. So we'll say rareDagger.previousOwner?.name equals "The Old Merchant." You'll see in the inspector that this is returning a nil value, because again, the chain breaks at previousOwner and therefore the name can't be assigned a value if its parent's struct doesn't even exist. However, if we uncomment the line above, we'll see that this works just fine. "This item used to be owned by The Old Merchant." Calling methods through an optional chain works much the same way in that no matter what the return type is, the optional chain is always going to wrap it as an optional. Let's see this in action by adding a simple function to the Owner struct. I'm going to call this returnOwnerInfo. No parameters, but it is going to return a string.
>
> **[4:13](https://www.linkedin.com/learning/swift-5-essential-training/chaining-optionals-14531798?u=76281980&t=253)** I'm going to a return interpolated string with the owner's "\(name) is the original owner!"
>
> **[4:24](https://www.linkedin.com/learning/swift-5-essential-training/chaining-optionals-14531798?u=76281980&t=264)** Actually, we're going to say is the current owner. That's more accurate. We're going to comment out the owner assignment line once again before using this new function. And we're going to use if let ownerInfo exists, it's going to be equal to rareDagger.previousOwner?.returnOwnerInfo(). If we do find owner info, since it's already going to print something else for us, we'll just say "Owner found!" Else, we'll print out "No owner in our records..."
>
> **[5:13](https://www.linkedin.com/learning/swift-5-essential-training/chaining-optionals-14531798?u=76281980&t=313)** Let's uncomment rareDagger.previousOwner assignment once again. We see this item used to be owned by the old merchant, owner found. To finish off optional chaining, let's revisit the example from way back in chapter three where we were trying to retrieve nested dictionary values. Now at the time, we didn't really talk about why the optional chaining worked, because we didn't know enough about optionals, but let's try it again with our new information. I've copied the nested dictionary at the top of the file. So we can say if let gemstoneObjective equals questDirectory, here we have to use chain subscript, ["[Fetch](../../Skills/Web%20Development/Fetch.md) Gemstones"]?["Objective"].
>
> **[6:06](https://www.linkedin.com/learning/swift-5-essential-training/chaining-optionals-14531798?u=76281980&t=366)** If that's found, we're going to print out the gemstoneObjective. Perfect, that shows up because it exists and we're good to go. And we can see that optional chaining through subscripts works exactly the same as with nested class or struct properties. While optionals can be a tricky thing to understand, the rules at least are consistent across the [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) language. Get comfortable with them and optionals will be your new best friends.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1), [Fetch](../../Skills/Web%20Development/Fetch.md) (1), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Code Identifiers:** raredagger (6), previousowner (6), daggerowner (3), returnownerinfo (2), gemstoneobjective (2)
> **CLI Commands:** find (1)
> **Definitions:** is a  (1)
> **Analogies:** for instance (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Inventory system](https://www.linkedin.com/learning/swift-5-essential-training/challenge-inventory-system-14527892?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/challenge-inventory-system-14527892?u=76281980&t=0)** (cheerful music)
>
> **[0:05](https://www.linkedin.com/learning/swift-5-essential-training/challenge-inventory-system-14527892?u=76281980&t=5)** - Oh man, I've been waiting to get to this challenge the entire course. This is the one, the defining moment where we put all of our [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) knowledge to the test and create a simple but functional inventory system. You're going to have to use classes, structs, and optional chaining for this one, folks, so get ready. You should be spending about 10 to 15 minutes on this one. So give it a shot on your own, then jump to the next video for my solution. Quest accepted.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Cross-References:** next video (1)
> **Speakers:** - oh (1)
> **Non-Speech:** (cheerful music) (1)

#### [Solution: Inventory system](https://www.linkedin.com/learning/swift-5-essential-training/solution-inventory-system-14525881?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/solution-inventory-system-14525881?u=76281980&t=0)** - [Instructor] All right, let's get into our inventory management challenge with the first task of creating a class called item. I'm going to say class Item, and we need two properties here, a name and a price.
>
> **[0:31](https://www.linkedin.com/learning/swift-5-essential-training/solution-inventory-system-14525881?u=76281980&t=31)** Going to move the second comment into our class because we need a default initializer for this task. So I'm going to say init name: String price: Int, and assign the instance properties here in the body of the init method, self.name = name, self.price = price. For our third task, we need to create a struct called BonusEffect, and it's only going to have one property called bonus. Let's say struct BonusEffect var bonus of type Integer. Our fourth task is to add a optional BonusEffect to our Item class. And I'm going to call this var secret of type BonusEffect, and it's going to be an optional. For task number five we need to create a class called Inventory, and it's going to have a property that's an array of Items, so it'll be able to track what it's storing. Say class Inventory var storedItems: of type array of Item.
>
> **[2:04](https://www.linkedin.com/learning/swift-5-essential-training/solution-inventory-system-14525881?u=76281980&t=124)** Task number six is going to be to declare the inventories default initializer. In this case, we only needed to take in one parameter, and it's going to be an array of Items. In here we just say self.storedItems = items. I spelled items incorrectly, there we go. All right, now for the fun part. Task number seven is going to be creating a BonusEffect and two items that we can work with. I'm going to say partyWideEffect, and this is going to be a new BonusEffect, and for the input parameter, I'm just going to put 15. I'm also going to declare a potion Item, its name is going to be "Healing Potion", price is going to be 35. Also going to create an antidote, "Antidote" for the name, price is going to be 15. Now you can choose to add the BonusEffect or the partyWideEffect to either the potion or the antidote. I noticed that I missed a T there. I'm going to add it to the potion. I'm going to say potion.secret = partyWideEffect. Now, for our last task, we're going to create an instance of our inventory. We're going to initialize it with our potion and antidote
>
> **[3:39](https://www.linkedin.com/learning/swift-5-essential-training/solution-inventory-system-14525881?u=76281980&t=219)** and unwrap, or optionally chain, the potion's secret variable. So let's take that step-by-step. We'll say var inventory, it's going to be an instance of our inventory class, and it's going to take in an array, and I'm going to do that in line here of our potion and our antidote, just two items. I'm going to say if let itemPower is = inventory, with a lowercase I, .storedItems. I'm going to access potion at index one using subscript, and then I'm going to access its secret. Now here, because secret is an optional, we have to add the question mark, and then we can get to the bonus value. Now if it does find an itemPower, let's say "Healing potion has a bonus effect of,
>
> **[4:39](https://www.linkedin.com/learning/swift-5-essential-training/solution-inventory-system-14525881?u=76281980&t=279)** and use the unwrapped optional for itemPower", else I'm going to print out "The item queried has no secret effect..."
>
> **[4:55](https://www.linkedin.com/learning/swift-5-essential-training/solution-inventory-system-14525881?u=76281980&t=295)** Perfect, Healing potion has a bonus effect of 15, but if we comment out one of our lines from task number seven, we're going to get The item queried has no secret effect. Everything works great. Let's get on to the last module of the course.

> [!info]- Semantic Content
>
> **Code Identifiers:** storeditems (3), partywideeffect (3), itempower (3)
> **CLI Commands:** find (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 7. Leveling Up with Enums, Protocols, and Errors

[↑ Back to Table of Contents](#table-of-contents)

#### [Final stage, final boss](https://www.linkedin.com/learning/swift-5-essential-training/final-stage-final-boss-14530766?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/final-stage-final-boss-14530766?u=76281980&t=0)** - This chapter marks the end of our [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) adventure, at least for now. The topics we'll be covering are your stepping stones to the more intermediate and advanced features that Swift offers. This is going to include a special kind of collection, called an enumeration, as well as protocols, extensions and efficiently handling errors. By the end of this chapter, I really hope you'll be as excited as I was after learning the Swift essentials, and just curious for what other hidden secrets the language has to offer.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3)
> **Speakers:** - this (1)

#### [Swift enumerations](https://www.linkedin.com/learning/swift-5-essential-training/swift-enumerations-14530756?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/swift-enumerations-14530756?u=76281980&t=0)** - [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) enumerations allow you to put related values together into a custom type. Like switch statements, each value in an enum is considered a different case, which is one of the reasons they go so well together. To declare a new enum, we use the enum keyword followed by the name we want it to have. So I'm going to call this enum GameState, and open and close the body with a pair of curly braces. To declare each case individually, we use the case keyword followed by its name. So we want GameState to have three cases, Completed, Initializing, and LoadingData. You can also shorten this if you prefer sort of an abbreviated version. We could say GameState, open and closed curly braces on the same line, and only use one case keyword. Now this is exactly like when we declared multiple variables on the same line. I am going to comment this out because we will get a naming conflict with two enums, but that's there for your reference. The compiler is now going to recognize GameState as its own type, which means we can define variables with it, just like anything else. So we'll say var currentState. To assign a value, we just type in the name of the enum, which is GameState, and use dot notation to select one of the cases. So I'm going to say Completed. I can also print this out
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/swift-enumerations-14530756?u=76281980&t=94)** and just say "Current state is" currentState. You'll see that the name of the case is automatically translated by Swift into a string, so we get "Current state is Completed". Now that covers the basics of how to declare and use enumerations, but we still want to look at how they work specifically with switch statements. Let's do another example and switch on the currentState value. Now for the pattern we can say GameState.Completed, and GameState.Initializing and so on, but Swift does allow us to have a more short, concise version, and just use the .Completed. It's already inferring that current state is an enum. For the Completed state, just going to say "Completed processing all tasks". Remember, our switch statement needs to be exhaustive, so we have to do all three cases. So for Initializing, I'm going to say "Still initializing data". And for case LoadingData, say "Player data correctly unpacked".
>
> **[2:53](https://www.linkedin.com/learning/swift-5-essential-training/swift-enumerations-14530756?u=76281980&t=173)** Now technically, a switch statement being used with an enum doesn't need a default case like we've seen before, because enums only allow for a constant number of options, and they're all known. There's no unknown. However, if we needed to add cases to our enum in the future, a switch statement without a default would break and would throw up a nasty error. Now to avoid having to manually update a switch statement that's using an enum, we can add @unknown default case, and we'll print out "Unknown game state detected".
>
> **[3:35](https://www.linkedin.com/learning/swift-5-essential-training/swift-enumerations-14530756?u=76281980&t=215)** This is going to catch any undefined cases, just like a normal default, but it's more explicit and safe done this way. Now we are going to get a warning because our default isn't going to be executed right now. But if we add another case to GameState, we'll say Restarting, this will take care of that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3)
> **Code Identifiers:** currentstate (3)
> **Analogies:** just like (2)
> **Definitions:** is an  (1)
> **Warnings:** warning (1)
> **Speakers:** - swift (1)

#### [Raw and associated values](https://www.linkedin.com/learning/swift-5-essential-training/raw-and-associated-values?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/raw-and-associated-values?u=76281980&t=0)** - [Instructor] Enumerations can be expanded to include raw values of sequential or unique values or associated values which capture case specific parameters. Let's declare a new enum here. I'm going to call this non playable characters. We're going to have three cases here, a villager, a blacksmith, and a merchant. And we can specify a raw value type by adding a colon after the enum name. And we want this to be a string type, so we'll just add the string class. [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) is going to take care of setting string and integer raw values for us by default. So if we create a new NPC, we'll just say VAR blacksmith equals non playable characters blacksmith. Now, if we print out the raw value of blacksmith, we'll just get it string representation. If we change our raw value type to an integer, we can see that blacksmith is going to be an integer of one, or raw value of one. Since Enums, like everything else we've seen, are zero indexed. Well, that's not much fun. So let's switch back to a string type and we're actually going to define our own raw values that are unique to each case. And we can do that by setting a unique or literal string. So for villager, and I forgot the R,
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/raw-and-associated-values?u=76281980&t=94)** we're going to say common, not much useful info there. For the blacksmith, we're going to say one per village, will have quest information. And for merchant say no limit per village, will make you cool stuff. Now, when the debug log runs, we'll see that the raw value of blacksmith is now this unique string, instead of a default representation of the enum case name. Now raw values have a lot of uses, but you might've guessed that having them all be of the same type can be limiting. And that's where associated values come in, which let you store values of different types for each case. For this example, I'm going to create another enum. I'm going to call this player state, and I'm going to have the first case be alive. Second is KO, third is unknown. For our KO case, it'd be useful to store the level we want to restart at and the unknown to store an error string, which is something that we can accomplish with raw values. We can however add an associated value to any of our cases by just specifying the name and type that we want. So for KO, I'm going to name the associated value level of type integer. And for unknown, I'm going to call it debug error of type string.
>
> **[3:09](https://www.linkedin.com/learning/swift-5-essential-training/raw-and-associated-values?u=76281980&t=189)** You'll notice that this looks a lot like passing parameters to a function, and you should continue to think of it like that because it'll help with your understanding of what's going to happen next. And before we add another switch statement to test this out, I should probably tell you the best part about a numeration types. They can have properties and methods of their own. Now this feature is perfect for creating class like behavior for situations with finite options. So inside the enum declaration, we're going to add a new function. We're going to call this evaluate case, no parameters, and no return type. Since this function is inside player state, we can have the enum sort of switch on itself using the self keyword. Now for the pattern we could say player state and do this long hand, but I'm just going to use again, the dot notation, let's say for alive and print out, still kicking. For KO, and here we need to locally capture the associated value. And we can do that by either declaring a VAR or a let, so here I'm going to say let, and I'm going to call this local variable restart level. Now we can use restart level inside our case block, which is honestly very useful. So let's say print, sorry, Back to restart level for you.
>
> **[4:49](https://www.linkedin.com/learning/swift-5-essential-training/raw-and-associated-values?u=76281980&t=289)** I'm actually going to add in, so sorry, back to level restart for you, there we go. And for case unknown, I'm going to name this message. And I'm just going to print that out directly 'cause it's already a string. For the default, we can't forget. We'll just say unknown state encountered. Now with our player state class, we can say player state KO, give it a level of one and call the evaluate case function. On the debug log we'll see, sorry, back to level one for you, as our associated value was captured, passed into our cases and used in our KO case statement. Now, this is perfect. Swift enums are often under utilized because of their inflexibility in other languages. So be sure to take advantage of them in Swift.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (3)
> **Env Vars:** var (2), npc (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Introducing protocols](https://www.linkedin.com/learning/swift-5-essential-training/introducing-protocols-14526935?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/introducing-protocols-14526935?u=76281980&t=0)** - [Instructor] We did a lot of work with classes and subclassing in the last chapter. But there's one rather glaring issue with that approach that you might've noticed. Classes are limited to single inheritance and structs, well they can't subclass at all. So how do we extract and share common functionality without these limitations? Well on [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md), the answer is protocols, which are essentially blueprints, light classes, but they can be adopted by a class or struct independently. Let's start off by declaring a protocol and we just use the protocol keyword and we name it. I'm going to call this a collectible protocol. You give it open and close curly braces. Now, if you go to community projects or forums, you'll see protocols labeled like this as action verbs. And that's sort of just a style thing, but the community has adopted it. And protocols themselves can only have property and function declarations. They can't actually have any stored values or implementations. That is up to the adopting class or struct to implement on a case by case basis. However, we do want all collectible objects to have a name. So we'll say we want var name of type string. And we also want them to have prices of type int. Even though protocol variables can't store values, they still need to have explicit getters or getter and setter events. We've already seen these with computed properties, so let's give our name just a getter so it's a read only,
>
> **[1:36](https://www.linkedin.com/learning/swift-5-essential-training/introducing-protocols-14526935?u=76281980&t=96)** and our price is going to be a read write. So that's with a get and the set. Now that our compiler is happy again, I'm going to add a function declaration here, and I'm going to call this collect. No input parameters and it's going to kick back a bool. You'll notice here that we don't declare a function body of any kind. It's just the pure signature of the function. The adopting class as responsible for implementing it. Now, if you're wondering, well, what if we want to initialize a protocol just like we do a class? Well, we can do that too. We can add an initializer here. I'm going to say with name of type string and starting price of type int. Again, just the function signature, no body at all. Now adopting a protocol is incredibly easy. It's just like subclassing. We're going to take a class example here, but again, this works for structs. And we'll call the class item, colon and we can use collectible, open and closed curly braces, and that's all it takes. And we will get an error saying type does not conform to the protocol collectible. And do we want to add stubs? Well, yes, we do [Xcode](../../Skills/Mobile%20Development/Xcode.md). Go ahead and fix that for us. Now automatically our name, price, init and collect are all stubbed in.
>
> **[3:14](https://www.linkedin.com/learning/swift-5-essential-training/introducing-protocols-14526935?u=76281980&t=194)** And we do have some errors though because as I've sort of hit you on the head with multiple times, the protocol doesn't actually do anything. It's just a blueprint. So in our init, we still need to set our non-optional values. So we'll say self.name = withName. Self.price = startingPrice. And for our collect function, we're going to print out item collected and always return true for this, just for the example. A few things to note here before we move on. First, variables and functions enforced by a protocol are written the exact same way. Second, if you're using an init function in your protocol, it's going to be required in the adopting class, which is why the compiler added the required keyword in front of the init. Now we can create an instance of item just like we have before with a class. So we'll say let potion = Item. I'm going to call this a high potion. Starting price of 35. There's no upper limit on how many protocols an object can adopt, which essentially lets you build up classes or structs or even enums from different functional pieces. This is a huge step in efficiency over an object oriented, single inheritance approach as different objects adopting the same protocol can all have their own implementations while following the same blueprint structure. Let's go back up to our collectible protocol
>
> **[4:50](https://www.linkedin.com/learning/swift-5-essential-training/introducing-protocols-14526935?u=76281980&t=290)** and I'll show you how easy this is. We'll say a new protocol is called usable. There's just going to be one function called use. To adopt multiple protocols, we'll go to our class and after collectible, we'll add a comma and usable. And again, we're going to get an error because we don't implement the use function. So before that pops up, I'm just going to write this in here. And auto-complete does recognize that we have the use function from our protocol. So here I'm just going to print item used. Let's go down to our potion and call collect and use just to make sure this is working. We should see our debug logs in the console. And there we go. Item collected, item used. Now protocol oriented programming in Swift is a huge subject in itself and way outside our course scope. But I would encourage you to explore its potential for creating modular and flexible application architectures.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2), [Xcode](../../Skills/Mobile%20Development/Xcode.md) (1)
> **Definitions:** is a  (2), is called (1)
> **Analogies:** just like (3)
> **Code Identifiers:** withname (1), startingprice (1)
> **UI Navigation:** go to (2)
> **CLI Commands:** make (1)
> **Cross-References:** in the last (1)
> **Speakers:** - [instructor] (1)

#### [Using extensions](https://www.linkedin.com/learning/swift-5-essential-training/using-extensions-14529813?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/using-extensions-14529813?u=76281980&t=0)** - [instructor] Now that we've seen what protocols can do, we can take a look at extending their functionality. First off, any class, struct, protocol or even an enum can be extended, regardless of whether you created it or it's part of the [swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) language. Now second, extensions can either be used to add default implementations of an existing feature, or new computed properties and functions. Let's keep working with the collectable protocol and we're going to extend it. All we do is add the extension keyword, the name of the protocol or class that we're extending, and open and closed curly braces. Let's add in a computer property that's always going to return a collectibles price times 10. So we'll say var priceIncrease type Int. It's always going to return self.price times 10. And we have access to our price variable because we're extending the collectible protocol. The self keyword here refers to whatever object is being extended. So in this case, we have access to the price variable from our collectible protocol. We can also add in a convenience initializer and pass the heavy lifting up the chain to the protocols required init. Now, these don't need the convenience keyword. So we can say a init, just with a name and we'll just call self.init, select our collectible required initializer,
>
> **[1:38](https://www.linkedin.com/learning/swift-5-essential-training/using-extensions-14529813?u=76281980&t=98)** pass up name and by default, all items using this convenience init, will start with a price of a 100. Now this accomplishes the same thing as convenience or subclass initializers we worked with earlier. So we could create another item with a set price of a hundred, and we'll call this an antidote. Let's have item and just with a name, and now if we print out antidote.price or debug it in the right-hand inspector, we'll see that it's a 100. And last thing, let's go back up to the collectible extension and we're going to add in a default implementation of the collect function. So we name it exactly the same, func collect, no parameters, returns a bool and we'll say print out default item couldn't be collected. And we're always going to return false here for this. Now, extending functions like this isn't strictly necessary since the point of protocols, is to give the conforming object the chance to define its own implementation. However, it is a good idea when you need some baseline functionality like adding default values to function parameters. To see this in action, we can go to our item class and we can actually comment out, it's collect implementation. And once that runs, we'll see the default debug inside a console.
>
> **[3:17](https://www.linkedin.com/learning/swift-5-essential-training/using-extensions-14529813?u=76281980&t=197)** And there we go. And one last thing before we go, extending swift classes, is the same as doing it to our custom protocol. So let's add some of our own little symbol functionality to the string class. We'll say extension. We want the string class to be extended, open and close curly braces. I'm going to add sort of a simple function here called fancyDebug. No inputs or return values. And I'm always going to print out when this function is called, this string has, and I'm going to reference whatever string I'm using. So we'll use self account property characters. Now any string has access to this fancyDebug function. So we can say antidote.name.fancyDebug. And we'll see that antidote has eight characters. To reiterate classes, strucks, enums and protocols, can all be extended with computer variables, functions and initializers. But these extensions aren't a requirement. As we've seen, it's perfectly fine to have protocols or classes without extensions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2)
> **Code Identifiers:** fancydebug (3), priceincrease (1)
> **Definitions:** refers to (1), is a  (1), is called (1)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)

#### [Throwing errors](https://www.linkedin.com/learning/swift-5-essential-training/throwing-errors-14528847?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/throwing-errors-14528847?u=76281980&t=0)** - [Instructor] Optionals are great for handling values that might be nil while your code is running, but they won't account for situations where you need the program to send out and interpret an error. [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) makes throwing and responding to errors almost second nature, allowing you to customize how your code responds to unexpected actions. Enums are the most commonly used type when you're handling errors because they have a fixed number of options. So let's declare a new enum. We're going to call this DataError. It's going to have two cases, EmptyPath and InvalidPath. For this example, we're going to use this DataError enum to mock up fetching some kind of player data. And even though it doesn't exist, we can still get the topic across here. And we're going to have to have DataError adopt the error protocol so the compiler knows that our new type is compatible with error handling. So we'll say colon after DataError, Error. The error protocol doesn't actually have anything in it, it's just used as a kind of flag to mark types we want to be recognized as an error. Now we can create a function that essentially throws out errors into our code under specific situations. For this example, we're going to mock up a function called load data to check a file path. So let's say loadData. It's going to take in a parameter called path, type string, and it's not going to have a return type.
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/throwing-errors-14528847?u=76281980&t=94)** To mark a function as throwable, or that is able to throw an error, we add the throws keyword after the parameters. Just a note here that a function marked with throws can still have a return value, it's not a replacement. You just add the return value as normal right after throws. Now, we've already seen the guard statement in action, so this is a perfect place to use two of them as gatekeepers to determine if and when an error should be thrown. First off, we'll check that the path parameter contains a forward slash character. So we'll say guard path contains forward slash and if it doesn't, instead of just having a printout, we're going to use the throw keyword and throw out one of our enum cases. Now this is going to be DataError.InvalidPath. Remember that if a guard statement fails, the entire function is exited, so what this is doing is saying if we detect that path contains, or doesn't contain, a forward slash character, throw our error and then exit loadData without going any further. And we're also going to check if the path is not empty. So we say guard, we use the bang operator or the is not, path is empty, else we're going to throw our second type of error, DataError, EmptyPath. With that, our loadData function is set up to defensively handle two use cases where a file path might not be configured correctly or present at all.
>
> **[3:10](https://www.linkedin.com/learning/swift-5-essential-training/throwing-errors-14528847?u=76281980&t=190)** Now we just need to figure out how and when to handle them when we call the loadData function itself, which we'll talk about next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Code Identifiers:** loaddata (4)
> **Definitions:** is a  (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Handling errors](https://www.linkedin.com/learning/swift-5-essential-training/handling-errors-14526926?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/handling-errors-14526926?u=76281980&t=0)** - [Instructor] We've already mentioned that the idea behind throwing and handling errors is delegation. And we've covered where we want to send out errors from in our program, but not where and how they need to be addressed. So let's go down to our test code here, this is from the last video. Let's create a file path that we can work with going forward. So we'll say playerDataPath =/example/data.txt, why not? Now we can call load data, and pass in our new variable, but we need to prefix the function call with a new keyword, try. So we'll say try loadData path, playerDataPath. The try keyword attempts to call a function that throws an error, sort of like the if let statement tries to query an optional value and unwrap it. Now using the try keyword by itself only works right now because our path is not throwing any errors. But if we go up and delete the forward slashes in player data path, we'll see that this is a different case. There we go, an error was thrown and was not caught. What we need in this particular situation is a new kind of statement called a Do-Catch. Let's take our try loadData and copy that. I'm going to use the do keyword, open and closed curly braces.
>
> **[1:34](https://www.linkedin.com/learning/swift-5-essential-training/handling-errors-14526926?u=76281980&t=94)** I'm going to paste back in our try loadData. Now we're still going to get an error here because we haven't handled the error properly. And that's where the second half of the statement comes in, the catch keyword. Now each catch essentially works like a switch case, it has its own executable body of code. However, we have to define which error or what errors, we're actually trying to catch. Here we have the option of defining each error case with its own catch body, just like a switch statement and cases. But for now, let's just determine if the error is one of our own or something else. So after the catch we can use the is keyword, and our DataError type. And what this is saying is catch all errors that are of our data error enum. So let's just print out invalid or empty path detected just to cover both cases, and do catch statements have to be exhaustive, just like switches. So we add a catchall catch at the bottom with no expression or checking, and we'll just say unknown error detected. Perfect, now in our debug log we see that our catch statement where we're detecting, if it's of type data, error is firing and that's great. Let's go up and make sure that our player data path
>
> **[3:11](https://www.linkedin.com/learning/swift-5-essential-training/handling-errors-14526926?u=76281980&t=191)** has those forward slashes in it. And we'll add a print statement underneath our try so that if it's successful, we'll just say data [Fetch](../../Skills/Web%20Development/Fetch.md) successful. And now that our path variable is valid, we should see that in our debug. Perfect. Now in the last video, I did mention that we can throw errors and return values from the same function. So let's see how that works. I'm going to go up to low data and I'm going to add in an optional boolean return value after the throws keyword. And if our path passes our two guard statements, I'm just always going to return true for this example.
>
> **[4:00](https://www.linkedin.com/learning/swift-5-essential-training/handling-errors-14526926?u=76281980&t=240)** Now we don't actually have to store or unwrap our return value just like with a normal function. But if we want to, we need to use our if let, I'm going to call our unwrapped optional return if there is one, data loaded, I'm going to say try load data again from our player data path. And if there is one present, I'm just going to print out a different string, data fetch went just fine. Now there's one gotcha here. Since our function can throw an error and return a value, we need to add a question mark after the try keyword. this is going to tell the compiler, then if an error is thrown, the return value is nil. Otherwise it's an optional and unwraps normally. And before we get to the chapter challenge, there's one more thing we need to know about handling errors and that's how to propagate or pass them to other functions. I'm going to create a new function. I'm going to call this propagate data error. It's going to take no parameters, but it is going to be able to throw errors, open and closed curly braces. From inside here, I'm going to call try load data again, player data path. From inside here, I'm just going to call try loadData with our player data path again. And that's going to take any errors generated by loadData and delegate them for propagate data error to throw.
>
> **[5:37](https://www.linkedin.com/learning/swift-5-essential-training/handling-errors-14526926?u=76281980&t=337)** Now let's use a do catch statement again, to call propagate data error this time, which is going to kick off the whole error chain. I'm going to do some of the repeated code here because I want to show you how to declare a catch statement for each individual Enum error that we have. So inside do, I'm going to say try to propagate data error. And if that succeeds propagated data fetch successful.
>
> **[6:09](https://www.linkedin.com/learning/swift-5-essential-training/handling-errors-14526926?u=76281980&t=369)** For our first catch, we're going to say catch data error for empty path. And we're going to print out empty path detected. Also going to add a catch for data error, invalid path. Print out invalid path detected. And finally we need our default catch, which again is going to print out, unknown error. You can see in the debug area that all three of our approaches have printed out different messages, which means that we've handled them differently, but the result is essentially the same. A final note here using catch statements for each individual error enum is perfectly compatible with other throwing functions. It's not just for the propagating error approach. You can mix and match as you want. This was just easier to break it out as an example.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (3)
> **Code Identifiers:** loaddata (5), playerdatapath (2)
> **Definitions:** is a  (2), means that (1)
> **Analogies:** just like (3)
> **File Paths:** example/data.txt (1)
> **CLI Commands:** make (1)
> **Cross-References:** in the last (1)
> **Warnings:** gotcha (1)

#### [Challenge: Battle actions](https://www.linkedin.com/learning/swift-5-essential-training/challenge-battle-actions-14528845?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/challenge-battle-actions-14528845?u=76281980&t=0)** - This last chapter challenge is going to be our biggest boss fight yet, putting our skills with enumerations, protocols, extensions, and air handling to the test. Don't worry, I have faith in your ability to think critically about everything we've learned throughout the course. Remember, it's all about putting small, separate pieces together to form a coherent structure, from atoms to molecules to an entire physical body. This should take you about 10 minutes, so get after it. I'll see you in the next video with my solution. Happy coding.

> [!info]- Semantic Content
>
> **Cross-References:** in the next (1)
> **Speakers:** - this (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Battle actions](https://www.linkedin.com/learning/swift-5-essential-training/solution-battle-actions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/solution-battle-actions?u=76281980&t=0)** - [Instructor] All right, let's dive into our last challenge solution here and handle some errors. Our first task is going to be to create an enum that adopts the error protocol and has three cases. I'm going to say enum, I'm going to call this action error, It's going to adopt the air protocol, and for the first case, I'm going to say insufficient MP, second case out of range and third case unknown error. The second task we need to create a function, that's going to take in an integer and a double and return an optional Boolean. I'm going to say func attack enemy, parameter name is going to be MP for the first one and distance as a double. I'm also going to kick back an optional Boolean. And task three is pretty simple, we're going to mark attack enemy as a throw bool function with the throws keyword. For task number four, we need to add two guard statements to make sure that our MP and distance parameters meet certain conditions. So I'm going to guard against MP, always needed to be over 10, and if that's not the case,
>
> **[1:32](https://www.linkedin.com/learning/swift-5-essential-training/solution-battle-actions?u=76281980&t=92)** I'm going to throw action error, insufficient MP. I'm also going to say guard distance always needs to be less than 5.89 or else we're out of range. And I'm going to throw action error out of range. Task number five is also an easy win. We're just going to return true, if both our guard statements are past. Task six is to use a do-catch statement to call attack enemy, but handle each error case individually. Here we can say, do we'll use our try keyword, so we'll try to attack our enemy, let's say we have 15MP and our distance is five. And if that's true, we're going to say attack landed. Perfect. Now we are going to catch our individual errors, so we'll say catch action error, insufficient MP.
>
> **[2:43](https://www.linkedin.com/learning/swift-5-essential-training/solution-battle-actions?u=76281980&t=163)** And in here, I'm just going to print out sorry, you don't have enough MP for that one. I'm going to catch the action error out of range and print out the enemy is too far away to hit.
>
> **[3:06](https://www.linkedin.com/learning/swift-5-essential-training/solution-battle-actions?u=76281980&t=186)** For the action error unknown, I'm going to print out, not sure what happened there boss. Remember our do-catch statements need to be exhaustive, so I'm going to add a default catch at the end and printout undefined error detected. Now, if we change around our MP, we'll give it nine, we should see, sorry you don't have enough MP for that. If we give it back enough but we change our distance to be 10 units away, the enemy is too far away to hit. Our last task is going to be to unwrap the optional bullion that is returned from attack enemy. So I'll say if let attack success equals try and we need our question mark, in case an error is thrown, we'll get a nil value back, say attack enemy for 55, we have 55 MP, but we are close enough. Actually, we need to be close enough for both. And there we go. We're going to print out attack success and use our unwrapped optional. Here we go, attack landed, attack success. Feel free to play around with both values in our attack enemy calls to explore all the different error messages.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Versions:** 5.89 (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/swift-5-essential-training/next-steps-14526916?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/swift-5-essential-training/next-steps-14526916?u=76281980&t=0)** - [Harrison] Thanks for joining me on this trip through the basics of the [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) 5 language. You're now ready to embark on your app or game making journey. If you're looking for the next step in that journey, I'd highly suggest poking around our iOS App Development series, as there are tons of courses on the language itself, and on specific core components. If you're looking for a specific place to start after you've finished up here, check out our series of courses on creating your first iOS app. As always, and for the last time, happy coding.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (1)
> **Code Identifiers:** ios (2)
> **Speakers:** - [harrison] (1)


## Instructor

- [Harrison Ferrone](../../Instructors/DevOps/Harrison%20Ferrone.md)

## Resources

- Exercise files available

## Skills Covered

- Swift (Programming Language)

## Path Context

### In [Get Started with iOS App Development](../../Paths/Mobile%20Development/Learning%20Paths/Get%20Started%20with%20iOS%20App%20Development.md)
**1 of 4** | [Xcode 15 Essential Training](Xcode%2015%20Essential%20Training.md) →

### In [Master Swift](../../Paths/Mobile%20Development/Learning%20Paths/Master%20Swift.md)
← [Programming For Non Programmers Ios 17 And Swift 5](Programming%20For%20Non%20Programmers%20Ios%2017%20And%20Swift%205.md) | **2 of 4** | [Swift- Delegations and Data Sources](Swift-%20Delegations%20and%20Data%20Sources.md) →

## Appears In

- [Get Started with iOS App Development](../../Paths/Mobile%20Development/Learning%20Paths/Get%20Started%20with%20iOS%20App%20Development.md)
- [Master Swift](../../Paths/Mobile%20Development/Learning%20Paths/Master%20Swift.md)

## Related Courses

_Courses sharing skills:_

- [Swift 5- Protocol-Oriented Programming](Swift%205-%20Protocol-Oriented%20Programming.md) — Swift (Programming Language)
- [Swift- Delegations and Data Sources](Swift-%20Delegations%20and%20Data%20Sources.md) — Swift (Programming Language)
- [Programming For Non Programmers Ios 17 And Swift 5](Programming%20For%20Non%20Programmers%20Ios%2017%20And%20Swift%205.md) — Swift (Programming Language)

---

[↑ Back to top](#)