---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: react-native-essential-training
url: "https://www.linkedin.com/learning/react-native-essential-training"
level: Intermediate
updated: 11/10/2020
learners: 18638
skills:
  - React Native
exercise_files: false
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQGKXGNugAV8Ag/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1605024546102?e=2147483647&amp;v=beta&amp;t=sT7pQrM4VbNoMCmTrtyurh_WoGeP76aHq06n5A-S4Vk"
linkedin_topic: Mobile Development
learning_paths:
  - '[Become a React Native Developer](../../Paths/Mobile%20Development/Learning%20Paths/Become%20a%20React%20Native%20Developer.md)'
next_courses:
  - '[From React to React Native](From%20React%20to%20React%20Native.md)'
path_nav: '[{"path":"Become a React Native Developer","position":1,"total":5,"prev":null,"next":"From React to React Native"}]'
path_count: 1
tags:
  - course
  - topic/mobile-development
  - skill/react-native
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Mobile%20Development/React%20Native%20Essential%20Training.md)

![React Native Essential Training](https://media.licdn.com/dms/image/v2/C560DAQGKXGNugAV8Ag/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1605024546102?e=2147483647&amp;v=beta&amp;t=sT7pQrM4VbNoMCmTrtyurh_WoGeP76aHq06n5A-S4Vk)

# React Native Essential Training

> React Native allows you to build native mobile apps using JavaScript and React. In this project-based course, learn how to use React Native to build production-ready, native mobile apps on both iOS and Android. To help acquaint you with the fundamentals of React Native, instructor Samer Buna takes a hands-on approach, showing how to build applications from scratch. He first walks through how to bu

> [LinkedIn Learning](https://www.linkedin.com/learning/react-native-essential-training) | Intermediate | 19K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Working with React Native](#working-with-react-native)
  - [Is this course for you?](#is-this-course-for-you)
  - [Course overview](#course-overview)
- [**1. Getting Started**](#1-getting-started) (2 videos)
  - [Why React Native?](#why-react-native)
  - [Requirements and setup](#requirements-and-setup)
- [**2. React Native Fundamentals**](#2-react-native-fundamentals) (5 videos)
  - [Hello React Native with Expo](#hello-react-native-with-expo)
  - [Hello React Native on iOS](#hello-react-native-on-ios)
  - [Hello React Native on Android](#hello-react-native-on-android)
  - [Styling React Native components](#styling-react-native-components)
  - [Exploring components and APIs](#exploring-components-and-apis)
- [**3. Building a Simple Game App**](#3-building-a-simple-game-app) (15 videos)
  - [Introduction and preview](#introduction-and-preview)
  - [Initializing the TargetSum app](#initializing-the-targetsum-app)
  - [Configuring ESLint](#configuring-eslint)
  - [The game component with a random target](#the-game-component-with-a-random-target)
  - [Generating the random play numbers](#generating-the-random-play-numbers)
  - [Styling the random play numbers](#styling-the-random-play-numbers)
  - [Using TouchableOpacity for press events](#using-touchableopacity-for-press-events)
  - [Changing UI based on selected numbers](#changing-ui-based-on-selected-numbers)
  - [Adding shared behavior to numbers](#adding-shared-behavior-to-numbers)
  - [Using React DevTools with React Native](#using-react-devtools-with-react-native)
  - [Computing dynamic values where needed](#computing-dynamic-values-where-needed)
  - [Changing UI based on game status](#changing-ui-based-on-game-status)
  - [Working with timers](#working-with-timers)
  - [Caching to optimize computable values](#caching-to-optimize-computable-values)
  - [Remounting a component to reset it](#remounting-a-component-to-reset-it)
- [**4. Building a Data-Driven App**](#4-building-a-data-driven-app) (9 videos)
  - [Introduction and preview](#introduction-and-preview)
  - [Initializing the Bakesale app](#initializing-the-bakesale-app)
  - [Using the Fetch API](#using-the-fetch-api)
  - [Using FlatList with API data](#using-flatlist-with-api-data)
  - [Working with images](#working-with-images)
  - [Navigating to different views with state](#navigating-to-different-views-with-state)
  - [Fetching more API data for a view](#fetching-more-api-data-for-a-view)
  - [Implementing a back link](#implementing-a-back-link)
  - [Using TextInput to implement search](#using-textinput-to-implement-search)
- [**5. Animation and Gesture Control**](#5-animation-and-gesture-control) (9 videos)
  - [Preparing for swiping](#preparing-for-swiping)
  - [The Animated and Easing APIs](#the-animated-and-easing-apis)
  - [Creating an animation loop](#creating-an-animation-loop)
  - [Considering the device dimensions](#considering-the-device-dimensions)
  - [The PanResponder API](#the-panresponder-api)
  - [Preparing the PanHandlers](#preparing-the-panhandlers)
  - [Swiping left and right](#swiping-left-and-right)
  - [Handling swipe gestures](#handling-swipe-gestures)
  - [Opening URLs](#opening-urls)
- [**6. Deploying**](#6-deploying) (5 videos)
  - [Test first](#test-first)
  - [Testing on Android](#testing-on-android)
  - [Changing app logo and splash screen](#changing-app-logo-and-splash-screen)
  - [Using TestFlight with internal testers](#using-testflight-with-internal-testers)
  - [Generating signed APK for Android](#generating-signed-apk-for-android)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Working with React Native](https://www.linkedin.com/learning/react-native-essential-training/working-with-react-native?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/working-with-react-native?u=76281980&t=0)** - [Samer] Hello, welcome to the [React Native](../../Skills/Mobile%20Development/React%20Native.md) Essential Training course. My name is Samer Buna. I work at [Agile](../../Skills/DevOps/Agile%20Development.md) Labs where we're creating educational content for web and mobile developers with a focus on an interactive and adaptive approach. You can check some of the work I do over at [jscomplete.com](https://jscomplete.com). In this relatively short course, we will build two practical mobile applications with the React Native framework and its supporting tools. The first application will be a simple math game and the other application will be a more featured application that works with an API and we'll make use of more advanced features of the React Native framework. React Native enables software developers to build rich native mobile applications on both iOS and [Android](../../Glossary/Sdk/Android.md) using just [JavaScript](../../Skills/Software%20Development/JavaScript.md). It's a very popular framework and the list of who uses it out there is impressive and has big names like [Facebook](../../Glossary/Service/Facebook.md), [Instagram](../../Glossary/Service/Instagram.md), Uber, and jsComplete. Okay, the last one is not big but the signal here is that the knowledge I'll be sharing in this course is practical and real. I am not a fan of abstract teaching examples that are removed from practical realities. And this is why the applications we're going to build in this course will be real and practical. We'll start with an easy application, which is a simple game I created for my kids. This is just to get our feet wet and get some confidence in React Native. Then we'll do the deep dive and build a more featured data-driven application that has navigation, asynchronous calls, animation, touch gestures, and many more features. We will test our apps on the iOS simulator, the Android emulator, and with TestFlight,
>
> **[1:33](https://www.linkedin.com/learning/react-native-essential-training/working-with-react-native?u=76281980&t=93)** which is basically an iOS beta testing medium the apps creator can use to publish new applications for beta testing through an invite-only system. I'll also show you how to use the Expo tools to build and test your React Native apps. If you're on a [Windows](../../Glossary/Service/Windows.md) machine, you're going to love this Expo framework as it will enable you to build tests and even publish iOS apps without needing [Xcode](../../Skills/Mobile%20Development/Xcode.md). It's also the fastest way to get started with React Native anyway and that's why we'll start with it. This course is mostly live coding and not scripted. I explain concepts when we first need them and I run into problems while coding and I show you how to debug and solve these problems. Solving problems is how I personally learn. To get the most value out of this course, you should build the apps with me on your own and don't just watch me. In fact, you should try to build slightly different apps so that you run into other problems and learn from those. I'll be happy answer any questions that you might have on the jsComplete Help [Slack](../../Skills/Software%20Development/Slack.md) channel, which you can invite yourself to at [jscomplete.com/help](https://jscomplete.com/help). I'm also active on Twitter so if you have a tweet-sized question, you can certainly mention me on it. I try to be not too slow and not too fast but you can always control the play speed if you want. We will be doing a lot of things in this course. Some will be easy and others will be hard. Remember that you can pause, skip, and replay when needed. To focus on the important concepts of the course, in a few videos I do the styling needed off-camera. I encourage you to do your own styling but if you want it
>
> **[3:08](https://www.linkedin.com/learning/react-native-essential-training/working-with-react-native?u=76281980&t=188)** to sync with the styles that I do off-camera, the course has a [GitHub](../../Skills/Software%20Development/GitHub.md) repository with [Git](../../Skills/Software%20Development/Git.md) tags for every video which you can use to sync your progress with mine.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (7), [Android](../../Glossary/Sdk/Android.md) (2), [Agile](../../Skills/DevOps/Agile%20Development.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [Facebook](../../Glossary/Service/Facebook.md) (1)
> **Code Identifiers:** ios (4), jscomplete (2)
> **CLI Commands:** make (1), git (1)
> **URLs:** [jscomplete.com](https://jscomplete.com) (2)
> **Tools:** slack (1), github (1)
> **Env Vars:** api (1)
> **Definitions:** is a  (1)
> **Speakers:** - [samer] (1)

#### [Is this course for you?](https://www.linkedin.com/learning/react-native-essential-training/is-this-course-for-you?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/is-this-course-for-you?u=76281980&t=0)** - [Teacher] Before we begin, let's talk a little bit about the level of this course and what it covers. This course will have a pure focus on the [React Native](../../Skills/Mobile%20Development/React%20Native.md) framework. It will not teach you [React](../../Skills/Web%20Development/React.js.md) itself. You need a good knowledge of React and [JavaScript](../../Skills/Software%20Development/JavaScript.md) first. If you have not learned React yet, you should probably do that first. I don't mean to be a gatekeeper here, but I don't want you to waste your time on this course if you're not ready for it. When it comes to learning JavaScript and React, you have a lot of options. There are many courses on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning that you can start with. You can also check out the many books and video courses I have over at [jscomplete.com](https://jscomplete.com). If you're new to React, to start with this complete introduction to React lesson. This one does not assume any prior knowledge of the library. Taking this course without a fair understanding of the React Library itself will only confuse you, and you will probably get a bad first experience with React Native. Let me give you a specific example here. In this course, I use [React Hooks](../../Skills/Web%20Development/React%20Hooks.md) like useState and useEffect and many other methods in the React API. I will not be explaining these concepts before using them. I simply expect you to know them. Similarly, I'll often use modern JavaScript syntax, like async/await, for example. And I do not explain these either. If you're not familiar with things like import/export, [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md)/spread syntax, timers, promises, and async/await, I recommend that you go through this article first where I explain all these concepts and much more. However, prior knowledge in React Native itself
>
> **[1:34](https://www.linkedin.com/learning/react-native-essential-training/is-this-course-for-you?u=76281980&t=94)** is not required. When I use something from the React Native API, I'll certainly explain that first. In the next video, I'll do an overview of all the course chapters and what will be covered in each.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (8), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (4), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [React Hooks](../../Skills/Web%20Development/React%20Hooks.md) (1)
> **Code Identifiers:** usestate (1), useeffect (1)
> **Env Vars:** api (2)
> **URLs:** [jscomplete.com](https://jscomplete.com) (1)
> **Cross-References:** in the next (1)
> **Analogies:** for example (1)
> **Speakers:** - [teacher] (1)

#### [Course overview](https://www.linkedin.com/learning/react-native-essential-training/course-overview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/course-overview?u=76281980&t=0)** - [Instructor] The first part of this course will be an introduction to [React Native](../../Skills/Mobile%20Development/React%20Native.md). We'll start with the advantages and disadvantages of the framework and then I'll show you what you need to do to set up a React Native development environment for both iOS and [Android](../../Glossary/Sdk/Android.md). The second part is going to be the Hello World example of React Native. We'll cover the basics of the platform and the environment that it creates for us. We'll do an example on iOS and another on Android. In that part, we'll also talk a little bit about styling in React Native and I'll show you the RN tester project that you can use to practically explore and experiment with all the components and APIs of React Native. Then we'll get real and build the simple math game app. We'll take it one step at a time and make small increments as we go. We'll learn how to work with the React Native UI elements like view and text and how to interact with the app and communicate through components. We'll also work with random numbers and timers. In addition, we'll optimize a few things with caching, I'll show you a trick to reset the component by remounting it instead of resetting its data. By the end of that part, we'll have a complete game that a user can play, win, lose, and play again. In the fourth part, we'll start on the data-driven app. We'll start the whole thing with raw data coming from the API and design our app around it. We'll learn how to use the [Fetch](../../Skills/Web%20Development/Fetch.md) API and more React Native UI elements like FlatList, image, text inputs, how to navigate between views, and how to fetch more data on demand. We'll also see how to do some animation and gestures. We'll learn about the animated and easing APIs,
>
> **[1:36](https://www.linkedin.com/learning/react-native-essential-training/course-overview?u=76281980&t=96)** how to create an animation loop, like this, and how to make the animation work on any device size. We'll learn about the PanResponder API and how to use it to create a swipe left and right gestures. And we'll also learn how to open external links for the React Native app. In the final part, we'll test our data-driven application on Android and I'll show you how to deploy it on TestFlight. I'll show you how to change the app logo and splash screens and I'll walk you through the process of generating a signed API for the [Google](../../Glossary/Service/Google.md) Play Store.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (8), [Android](../../Glossary/Sdk/Android.md) (3), [Fetch](../../Skills/Web%20Development/Fetch.md) (2), [Google](../../Glossary/Service/Google.md) (1)
> **Env Vars:** api (4)
> **CLI Commands:** make (2)
> **Code Identifiers:** ios (2)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)


### 1. Getting Started

[↑ Back to Table of Contents](#table-of-contents)

#### [Why React Native?](https://www.linkedin.com/learning/react-native-essential-training/why-react-native?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/why-react-native?u=76281980&t=0)** - [Instructor] [React Native](../../Skills/Mobile%20Development/React%20Native.md) is a [JavaScript](../../Skills/Software%20Development/JavaScript.md) framework that allows us to develop native mobile applications for both iOS and [Android](../../Glossary/Sdk/Android.md) without having to write code in either [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) or [Java](../../Skills/Software%20Development/Java.md). React Native is powered by [React](../../Skills/Web%20Development/React.js.md), so in React Native applications, we write code using JavaScript with JSX and React Native will bridge our code into native land for both iOS and Android. We can use the same APIs, we use for regular React applications in a React Native application. But there are a few differences that you should be aware of, and we're going to cover these differences in this course. A React Native application will render using real native mobile UI components not Webviews, and it can access native platform features like the camera, address book, contacts and the user's location, all with the JavaScript interface that React Native provides. There are many reasons to consider a React Native over its alternatives, but I'm going to briefly mention the most important ones. If you're already sold on the advantages of React Native feel free to skip this video. Rendering mobile applications using cross-platform frameworks like Cordova or Ionic, which uses a combination of JavaScript, [HTML](../../Skills/Web%20Development/HTML.md) and CSS, typically render using webviews. The performance of these webview based applications is usually not very good when compared with native applications. Additionally, not all the platforms features and APIs can be accessed using webview based application. The [user experience](../../Skills/Web%20Development/User%20Experience%20(UX).md) overall on a webview based mobile application feels a little off.
>
> **[1:34](https://www.linkedin.com/learning/react-native-essential-training/why-react-native?u=76281980&t=94)** [Facebook](../../Glossary/Service/Facebook.md) actually started their mobile apps using webviews up until 2012 or so. The webview based Facebook application was famous for its slowness and bad overall user experience. React Native on the other hand, does not use webview at all, it acts like a bridge between the code that we write in JavaScript and the native platform it works with. It translates the UI components that we write in React into real native mobile UI elements. We also get all the benefits of the React Library itself, when we develop React Native applications, from the simple but powerful concept of components to the nature of reactive updates that happen automatically when the states of those components change. A coder who can develop React applications can be easily trained to develop iOS apps and Android apps using React Native, removing the need to hire specialized developers on target platforms. Rotating coders among projects becomes more feasible and that inturns improves the knowledge sharing among the team. React Native provides an excellent developer experience over normal mobile applications, this is mostly because of the great set of developer tools that comes built in with the framework. Since it's just JavaScript, all those tools will feel familiar to the web developer, for example, we can debug React Native applications using Chrome Dev tools, we can use any text editor to see the changes we do, all we need is a refresh exactly like in a web application, the build step is not needed as it is when writing normal mobile applications.
>
> **[3:09](https://www.linkedin.com/learning/react-native-essential-training/why-react-native?u=76281980&t=189)** Additionally, pushing updates to our already published applications can be easier, updates can actually be pushed directly without the App Store review cycle. The code written for iOS applications in React Native can be mostly shared with an Android application, we'll see an example of that as we build an application that gets compiled to both target platforms and most of the code will be shared on those platforms, in fact, the code will be shared by default and when we need to write code for a single platform, we branch off into a special file to do that. Of course, React Native is not perfect, it comes with many disadvantages as well, let me mention a few important ones. React Native is still a bit slower than purely native, there is a communication overhead between the JavaScript thread and the Native UI thread which happens over the React Native bridge. Optimizing React Native applications is essentially optimizing this bridge communication. React Native does not provide complete solutions in JavaScript land, you would still need developers on your team that can work with Swift and Java and fill the gap where needed. React Native is also a fast paced project with frequent breaking changes, so if you're developing a React Native project, you'll need to follow the changes and probably update your project as frequently as you can to deal with smaller changes. In the next video, I'll go over what's needed to develop applications with React Native for both iOS and Android. If you've already built your first React Native app, you can probably skip this next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (24), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (8), [Android](../../Glossary/Sdk/Android.md) (5), [React](../../Skills/Web%20Development/React.js.md) (5), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2)
> **Code Identifiers:** ios (5)
> **Env Vars:** jsx (1), html (1), css (1)
> **Definitions:** is a  (3)
> **Cross-References:** in the next (1), next video (1)
> **Analogies:** for example (1)
> **Warnings:** be aware (1)
> **Prerequisites:** you'll need (1)

#### [Requirements and setup](https://www.linkedin.com/learning/react-native-essential-training/requirements-and-setup?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/react-native-essential-training/requirements-and-setup?u=76281980&t=1)** - [Instructor] The fastest way to get started with [React Native](../../Skills/Mobile%20Development/React%20Native.md) is with Expo. Expo is a framework and platform with many tools and services built around React Native. We'll go over an example of how to use Expo in the next video, but you don't really need any tools besides the Expo [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) to use it. However, in this video, I'm going to go over the instructions to fully set up a Mac machine for React Native development with [Xcode](../../Skills/Mobile%20Development/Xcode.md) and [Android](../../Glossary/Sdk/Android.md) Studio. If you're not on a Mac, you can skip all these instructions and just use Expo to follow this course. You can develop Android applications natively on [Windows](../../Glossary/Service/Windows.md) or [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md), but I strongly recommend using Expo as that'll enable you to develop and test your React Native applications for both Android and iOS. In any case, the instructions to set up a React Native environment change frequently. And the best way to get them done is to follow this environment setup section in the official documentation, and that's what I'm about to do. I am using the current latest version of Mac OS and chances are you'll be using a different version. But I recommend that you update to the latest Mac OS version available, before starting with this course. Things change fast in the React Native ecosystem and being on the latest software helps minimize the issues you might run into. This includes the OS itself, and all the tools we'll go through right now starting with Xcode, which you need to install if you don't have. Here we go download is complete. Let's open up Xcode, accept the license agreement, and it will take some time at first install some components,
>
> **[1:35](https://www.linkedin.com/learning/react-native-essential-training/requirements-and-setup?u=76281980&t=95)** so let that finish. And you should see this Welcome to Xcode screen. Another thing that you need from Xcode is the command line developer tools. So usually this should be installed already, but to make sure you can use Xcode dash, select dash dash install, and you'll either get a prompt to install the command line tools, or you'll get this message that they are already installed. Let's go back to the environment setup section in the React Native documentation and follow the instructions here. So I'll follow the instructions to install the iOS target on Mac OS, you need node and Watchman. And the easiest way to install these on Mac is using Homebrew. So if you don't have Homebrew, get that first, it's just one command that you copy into your terminal and install it here. If you already have Homebrew, make sure it's up to date with the command brew update. And then you need to install both node and Watchman. Watchman is a recommended tool to have when working with React Native, it's not required, but React Native uses it to watch changes in the file system. So this is done, you should now have node, let's check the version. So the node I'm using is 1218, which is the latest as of this recording. So make sure your version of node is recent as well. And also [npm](../../Skills/Web%20Development/npm.md), let me check NPM. So NPM is at 6.14.6, I usually like to NPM install dash GNPM to get the latest NPM version, which is now 6.14.8,
>
> **[3:13](https://www.linkedin.com/learning/react-native-essential-training/requirements-and-setup?u=76281980&t=193)** as you can see, so this did upgrade MPM. Okay, we have node, we have NPM, we have Watchman, so let's proceed, we did Xcode in the Mac store. And the command line tools were installed with that, you can confirm that running Xcode again. And bringing up the Xcode preferences menu and in locations, check the command line tools location here. Also check on components here. And let's install the latest simulator as well. So you can download the 13.5 simulator and this will take some time as you can see, it's big as well. So while this is installing, let's continue with the documentation, we need CocoaPods which is a Ruby program, and Ruby comes by default with Mac OS. So we can use the Ruby gem command which is similar to NPM and node, it installs packages from the internet. So we'll go ahead and sudo gem install CocoaPods. Very good, so this German all of its dependencies are installed, and this is all for the environment setup for the iOS requirement on Mac OS. Let's now go over setting up a development environment for Mac OS, but for the Android target. So same thing you need node and Watchman, we have those and we need the [Java](../../Skills/Software%20Development/Java.md) Development Kit, which we can install using the brew cask install command in here, paste that in the terminal, brew cask install the open JDK. And there we go successfully installed.
>
> **[4:50](https://www.linkedin.com/learning/react-native-essential-training/requirements-and-setup?u=76281980&t=290)** And furthermore, for Android development, you need Android Studio. So download and install the latest Android Studio and this is just a Mac package, so let's download. And as you can see, the current version of Android Studio is 4.0.1. But any version after that should be okay. Yes to that, we don't need to import any settings. And let me go through the setup wizard real quick. Let's do custom in here. Next, I like dark theme. Make sure this performance checkbox is checked. And we also need the Android virtual device, click Next, click next a few times, and click Finish. And this will download and install the Android emulator. So let that finish. And if you get this system extension blocked by Intel Corporation abs, make sure to unblock it. So you have to unlock this and click Allow on the Intel Corporation apps. There you go, the download is done. And you'll get to this screen. Go ahead and click on configure and pick the SDK Manager. And the latest right now is 10R API level 30, but I think what we need is API level 29. And you can double check in the documentation. It should mention here somewhere what Android version is required, as of the React Native version that you're using. So we need Android 10 Q and we need the SDK platform 29, and we need Intel x86 and [Google](../../Glossary/Service/Google.md) API's.
>
> **[6:26](https://www.linkedin.com/learning/react-native-essential-training/requirements-and-setup?u=76281980&t=386)** So you can do show package details. And we need Android SDK platform 29, Intel x86 Atom 64 Image and Google API's Intel system image as well. Click Apply confirming, you'll need to accept the licenses and next to download. Another thing we need to do in Android Studio is to create a device emulator and to get to the emulator manager start a new Android Studio project. And it can just be an empty project. And as you can see, we don't have any devices here. And to create new device, click on this AVD Manager icon. And the default configured device here is the Android 10 plus, and that's not the target that we're using for React Native. So I'm going to go ahead and remove this device. And we'll create a new virtual device. And this time, we'll pick the right target. You can choose any device definition here, I'm just going to keep it on the default pixel two. And right here is the release name. So download the release associated with the target that we configured in the Android Studio. And that right now is Android 10 API level 29. And again, check the documentation to make sure which current Android release is required for the current version of the React Native that you're using. When this is done, click Next.
>
> **[8:01](https://www.linkedin.com/learning/react-native-essential-training/requirements-and-setup?u=76281980&t=481)** And then this next screen, you can customize a few things about the device, I'm just going to keep everything as is and click Finish. And now we see the device with the right target in here. I'll go ahead and test that. And you should see the Android emulator in here with its menu of things to do. And the final thing we need for Android is to configure the Android home environment variable in one of these files, which depends on what shell you're using and how its configured. In my case, I need the bash profile. So we can just edit this bash profile file. And if it doesn't exist, you can just create it and copy these commands here and paste them in here. And these set the Android home variable first and then it appends to the path, the directories where the Android binaries exist. So if we save and quit this file, and open up a new terminal, you should see tools like ADB, for example, which is the Android debug bridge. That's handy when you need to connect an actual Android device to your Mac. And that set for the Android requirements. And after that you can use the React Native CLI command to create Android applications. You also need an iOS developer account and you need to enroll in the [Apple Developer](../../Skills/Mobile%20Development/Apple%20Developer.md) program if you want to push your apps to the App Store. You can enroll as an individual or as an organization, and membership is currently priced at 99 US dollars.
>
> **[9:37](https://www.linkedin.com/learning/react-native-essential-training/requirements-and-setup?u=76281980&t=577)** But again, you only need this if you plan to push your apps to the App Store. You'll also need Google's Play Store account which requires a one time registration fee. And with your developer account, you can push your applications to the Google Play Store. And that's it, we're ready to start the Hello React Native project now and test it on both iOS and Android.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (29), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (14), [Xcode](../../Skills/Mobile%20Development/Xcode.md) (9), [npm](../../Skills/Web%20Development/npm.md) (7), [Google](../../Glossary/Service/Google.md) (4)
> **CLI Commands:** node (8), npm (7), make (6), brew (3), ruby (3)
> **Prerequisites:** install (16), setup (4), set up (2), configure (2), you'll need (1)
> **Env Vars:** npm (7), api (5), sdk (3), cli (2), gnpm (1)
> **Tools:** command line (4), terminal (3), bash (2), atom (1)
> **Code Identifiers:** ios (5)
> **Versions:** 6.14.6 (1), 6.14.8 (1), 13.5 (1), 4.0.1 (1)
> **Documentation:** the documentation (3)


### 2. React Native Fundamentals

[↑ Back to Table of Contents](#table-of-contents)

#### [Hello React Native with Expo](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-with-expo?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-with-expo?u=76281980&t=0)** - [Instructor] We are ready to create our first [React Native](../../Skills/Mobile%20Development/React%20Native.md) app. And I'll show you how to do that using the ExpoPlatform, which will allow you to follow this course on any operating system including [Windows](../../Glossary/Service/Windows.md), if that's what you're using. You can actually build simple React Native apps right in your browser using the Expo Snack tool, which gives you an in-browser editor, complete with a file tree and syntax highlighting. And the tool is integrated with other Expo tools like the iOS and [Android](../../Glossary/Sdk/Android.md) preview. So, here is how this example is going to look on iOS. Tap to play. And you get the preview right here. This iOS preview is the code that's here. You can test things out by changing the text here, for example, "Hello, Expo Snack." And there you go. You change something, the preview will auto-refresh. And you can even install [JavaScript](../../Skills/Software%20Development/JavaScript.md) modules from [npm](../../Skills/Web%20Development/npm.md) and use them here as well. Also, you can develop your React Native applications here and test them on your own devices. All you need to do is download the Expo Client mobile app on your phone, which is available on both iOS and Android. And then you just scan this QR code to launch this example app on your phone. How cool is that? If you're on a Windows machine, this is probably the easiest option for you to follow this course. If you want to take things one level up, you can run an Expo project locally using the Expo [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) node package. And this is something you
>
> **[1:32](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-with-expo?u=76281980&t=92)** can npm install, -g for global, expo-cli. So this is same command you would use on your Windows-based node installation as well. And then to create an export React Native project, you use the command, "Expo init," and then give it the name of the project, which will also be the name of the folder created in the working directory. Let's name this project, "Hello Expo." So the command will ask you, "What kind of workflow would you like for your project," and you have some options here. This is mostly to pick either regular or TypeScript flows, and whether you need a managed workflow or not. So let's pick the managed blank workflow. And it's currently installing all the dependencies for this project. So this will take some time. When it's done, as you see in instructions here, we can just cd into the newly-created directory. And to run things we do npm start. And once the project is started, it will open up this screen, which has useful shortcuts to run the applications either directly in simulators or on your own device as well. So I'm going to go ahead and run on an iOS simulator. So as you can see, this open up an iPhone simulator. And it will take some time to load up. And you should get the Open in "Expo" dialog here. And you'll see in the logs here how it's bundling.
>
> **[3:06](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-with-expo?u=76281980&t=186)** And when it's done, you'll get the application with this Command D menu. And this here is the default screen for the Expo app. So let me go back to the terminal here. And I'm going to open up a new tab on this. And then launch your code editor here. I use VS Code, So I'll open up VS code in here. And the entry point here is app.js and this one will have the text that you see on the screen. So we can change that and save, and it should be auto refreshed in your simulator. You can also test this application in a web browser. And this will open up the same application. And it's also connected to the VS code that we have here. We save and it will auto-reload.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (4), [Windows](../../Glossary/Service/Windows.md) (3), [npm](../../Skills/Web%20Development/npm.md) (3), [Android](../../Glossary/Sdk/Android.md) (2), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2)
> **CLI Commands:** npm (3), node (2), cd (1)
> **Code Identifiers:** ios (5), iphone (1)
> **Tools:** vs code (3), terminal (1)
> **Env Vars:** npm (1), cli (1)
> **Prerequisites:** install (2)
> **File Paths:** app.js (1)
> **Cross-References:** go back to (1)

#### [Hello React Native on iOS](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-on-ios?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-on-ios?u=76281980&t=1)** - [Instructor] To use [React Native](../../Skills/Mobile%20Development/React%20Native.md) for iOS without expo, you can use this command. npx [React](../../Skills/Web%20Development/React.js.md)-native init, and then the name of your applications. And I'm not running this command yet. This command will download the temporary copy of the React Native [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) and then use it to initialize a React Native project. However, since we're going to be using the React Native command many times, let's just install it permanently with [npm](../../Skills/Web%20Development/npm.md) installed-global React-native. The only downside to this method is that when React Native releases a new version, you need to update this local of the CLI if you want to use the new version. While the NPX way will always use the latest available released version, but download it every time. When this package is installed, it will make the React Native command available globally like this. So let's go ahead and use it. We do React-native init, the name of the project. So we'll do HelloRN here. And this command is going to take some time. It does a few things. Just like expo, it'll create a directory on this level with the same name that we pass through it, and it will then install the packages required for React Native to work. When the command is done, it will give you some instructions on how to run the application for both iOS and [Android](../../Glossary/Sdk/Android.md). We'll test it for iOS first and then we'll do Android. So you can run it for iOS, either directly using the React Native command run iOS, or you can open it up in X code and run it from X code as well.
>
> **[1:33](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-on-ios?u=76281980&t=93)** So let me go to HelloRN and run the React Native run iOS command. So this will launch up the iPhone simulator. And once it building, it will run the React Native application directly on the simulator without an expo client. At some point, while this project is building, it will open up another terminal for the Metro bundler, and then it will launch the application on the simulator. And this is the screen that you get. Which you can edit in the project. So in here, let's open up a code editor on this directory and the entry point again, is App.js. And as you can see, the default application comes fully configured with things like [ESLint](../../Skills/Software%20Development/ESLint.md), Prettier, Babbel, Metro bundler, and Flow as well, which is a static type checker, just like TypeScript. This part here is the type of the app function. And let me just remove it in here. We won't be covering flow or TypeScript in this course, but they add great value to any project. Once you get comfortable with React Native's components and APIs, take a look at either flow or TypeScript and learn about what they bring to the table. And this is all the code that you see on the screen here. So you can change anything in here and just like expo, things will auto refresh. So here's step one that you see on the screen so we can change things and save, and it should be auto refreshed in here.
>
> **[3:07](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-on-ios?u=76281980&t=187)** And there is also a command + D screens. D is for debug. This is the debug menu in React Native. Or if you're testing on an actual device, you can just shake the device to open it up. So this is the debug menu, which has a few handy things here. You can, for example, disabled, the fast refresh that happens on save. And if you do that, then you'll need to reload every time you save. You can change the configuration for the bundler and the inspector is handy as well. This allows you to inspect a few things. For example, here, you can inspect elements. You can just tap on an element to see what is going on with that element. You can see its box model, you can see some of the styles. And this inspector display is permanent. So when you're done with it, you can hide it again. But the more important line here is really the debug line. If you click it, React Native will open up your browser dev tools, and you can do a few things here. You can, for example, reload the app from here if you need to. And of course you can do everything the developer tools allow you to do. For example, we can just come in here and console log a message and save, and this message will show up in the open developer tools. So it's probably a very good idea to always be in this mode and always keep your eye on the developer tools as you're building your application. And you can use Command + R to reload this application, and this is a regular iPhone application. You can, for example, kill it with a swipe up just like you kill any other application and you see it on the screen right now. You can click it to run it again. And down here, there are some cool links
>
> **[4:39](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-on-ios?u=76281980&t=279)** to learn the basics of React Native. These are links to the official React Native documentations. For example, if you click on styles, it will open up the style section of the React Native documentation in here. Let's explore the direct restructure here a little bit further. Starting with package.[JSON](../../Skills/Web%20Development/JSON.md). And you can see right here what dependencies this project needs. And it's just React and React Native. The notice that there is no React Dom in here, we're not building for the Dom, we're building for React Native. React Native also comes packaged with Jest. So you can use just to write your tests and things are configured with Babbel and the Metro React Native bundler. The entry point for the whole project is actually this index.js file. Which here imports the app component that we saw, the main component in this example, and it registered this component with the app registry package from React Native. So this is equivalent to the ReactDom.render method that renders a component into the Dom. This app registry registered the component into the React Native flow and make it show up in the simulator. And there's also app.json where you can organize any variables that you need in your application. Then app.js is the main component and this component has a few cool examples of things you can do with React Native. So right out of the box, we get so much value.
>
> **[6:13](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-on-ios?u=76281980&t=373)** We're simply writing [JavaScript](../../Skills/Software%20Development/JavaScript.md) and React, and we're seeing our application in a simulator on an iPhone. And most importantly, we didn't need to do a build step for the application between changes. Things just work. And you have options to control how they work. We also get to debug this application as if it was a web application with the powerful Chrome dev tools. All of these are huge ones for working with React Native. In the next video, we'll test the same React application on Android.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (24), [React](../../Skills/Web%20Development/React.js.md) (7), [Android](../../Glossary/Sdk/Android.md) (3), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (2)
> **Analogies:** for example (6), just like (4)
> **Code Identifiers:** ios (6), iphone (3)
> **File Paths:** app.js (2), package.json (1), index.js (1), app.json (1)
> **CLI Commands:** npx (2), make (2), npm (1)
> **Env Vars:** cli (2), npx (1)
> **Prerequisites:** install (2), you'll need (1)
> **UI Navigation:** go to (1), click on (1)

#### [Hello React Native on Android](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-on-android?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-on-android?u=76281980&t=1)** - [Instructor] To run the project for [Android](../../Glossary/Sdk/Android.md), the command is [react](../../Skills/Web%20Development/React.js.md)-native run-android. And this will launch up the Android emulator and run the Metro Bundler again for Android. And when Metro is done bundling, the emulator will load the same application that we're working with. So, again, we can come in here and change anything we want. We change this back to One spelled out, and it will auto-reload in Android as well. And I want you to notice one thing. The Debug instructions here are different. So in here, you press Cmd or Ctrl + M to get to the Debug menu. And you can open up the debugger, and you'll see the same console.log message as well. The interesting thing is that, if you remember, the instructions on iOS were to press Command + D. And the same application rendered a different message for the Android application, and I want to point your attention to that. So this would be in the DebugInstruction component, which is coming from react-native/Libraries. Now, what I'm going to do is I'll go to this place under node_modules and find this DebugInstructions component. So here is the react-native package, and we need Libraries, and we need the NewAppScreen, and under components, we'll find the DebugInstructions component. And here is the part that I'd like you to notice.
>
> **[1:36](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-on-android?u=76281980&t=96)** This component is written using the Platform.select method. So Platform is something that you also import from react-native. And its select method, you give it an object with ios, and you can also use android, or you can use default, which will work for Android in this case. So what this does is it will detect the platform and use the properties here to decide which value to use for that platform. So on ios, it will use this component, and on android and all the other platforms, it will use this different component that has different instructions. So this is one way to do something differently for iOS and Android. Another platform customization that we can do is using the filename itself. So if we rename App.js to App.ios.js, and if we duplicate this file and make another copy, App.android.js, then what [React Native](../../Skills/Mobile%20Development/React%20Native.md) is going to do is that it will use App.android.js for the Android platform and use the App.ios.js for the iOS platform. So, for example, let's remove the header here in App.android.js and save, and you'll notice that the header is gone in here. But if we run the same application on iOS, the header will still be there because it's in App.ios.js. Let's actually test that, react-native run-ios. The simulator is running.
>
> **[3:10](https://www.linkedin.com/learning/react-native-essential-training/hello-react-native-on-android?u=76281980&t=190)** And as you can see, the header is still there, while it is not there in the Android build because it's using a different file here. So this is one other strategy to have different components for different platforms, but the code usually can be mostly shared between platforms. And you would something like Platform.select to just customize a small part of the code. And you can also use the Platform.select to customize the styling of your component, which is often necessary because the layout of Android and iOS are different. So I'm going to delete the Android file and go back to a regular, just one file in here. So you can actually keep both simulators open and test your changes in both simulator at once. And this is especially easy on the Expo platform as well. But in this course, I'm going to test everything on iOS and test things on Android after we're done with major milestones. In the next video, we'll get a basic introduction on using styles with React Native.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (18), [React](../../Skills/Web%20Development/React.js.md) (5), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (2)
> **File Paths:** app.ios.js (3), app.android.js (3), app.js (1)
> **Code Identifiers:** ios (6), node_modules (1)
> **CLI Commands:** find (2), make (1)
> **Cross-References:** go back to (1), in the next (1)
> **UI Navigation:** go to (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Styling React Native components](https://www.linkedin.com/learning/react-native-essential-training/styling-react-native-components?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/styling-react-native-components?u=76281980&t=0)** - [Instructor] Let's talk briefly about using CSS with [React Native](../../Skills/Mobile%20Development/React%20Native.md), which is a bit different than what we're used to on the web. While [React](../../Skills/Web%20Development/React.js.md) itself does not have any restrictions on how to use CSS, React Native enforces a standardized approach for styling. You can't use all CSS properties in React Native projects. You can only use the subset that is supported by React Native, which relies mostly on Flexbox for layout and focus on simplicity overall. When we write CSS for a web project, we have to deal with how CSS support varies among different browsers. This is not an issue with React Native CSS subset, although sometimes, you'll need to conditionally use or not use a CSS property based on the platform or give that property different values based on the platform. Since React Native is just a [JavaScript](../../Skills/Software%20Development/JavaScript.md) framework without a browser that can parse CSS, React Natives way to use CSS properties is using the JavaScript API. This means using JavaScript objects instead of CSS strings and just like using JavaScript to represent [HTML](../../Skills/Web%20Development/HTML.md), using JavaScript to represent CSS allows for a much richer experience and greater control. This CSS in JavaScript thing is supported in React itself but React Native requires it and enhances it. So let's work through some examples. But before we do that, let me simplify this example here. So let me clean up the component and let's just keep one view component and one text component and let's remove everything else.
>
> **[1:40](https://www.linkedin.com/learning/react-native-essential-training/styling-react-native-components?u=76281980&t=100)** And we can save to format. This is Prettier that just auto formatted this component for me. And to have it integrated in your editor, you would install an extension. So I have the Prettier code extension here for VS code. And then you would configure your editor to auto format when you save. And you can also see how [ESLint](../../Skills/Software%20Development/ESLint.md) is working and given us warnings that we now have unused variables. So we can clean these variables as well and auto format. I'm going to actually delete all of these new screen components and not use them. For the styles properties here, we only need the section container and the section title, everything else is gone. And in here, we don't have colors anymore. So let's just remove this and keep it default. So a few things to notice right away, there is a margin top. If we don't have this margin top, then the text would overlap with the controls that the iPhone here is using. So this is something that you would need to take care of. First, give it some margin. I usually do 40 in here, and there's also some horizontal padding in here. Now notice how padding horizontal is not something standard in CSS properties. This is an example of a CSS property that's part of React Native enhancements to this styling. And there are other enhancements that are React related.
>
> **[3:15](https://www.linkedin.com/learning/react-native-essential-training/styling-react-native-components?u=76281980&t=195)** For example, you can give this styles property an array of values. We can, for example, add an object here and say something like make the color red. This means, first apply the section title style object then apply this other object to set the color to red. And because the color red style came last, it will override any color styling that may come before it. So if section title here had a color blue property, the color red object will override that because it was included after the section title object. You'll notice how ESLint is immediately complaining that this is an inline style and it is recommended that you don't use inline styles and that you would use a styles object created through the stylesheet.create method. So let me tell you why. If you make a typo in here, and you save, you get a clear exception in here that you are trying to use an invalid style property. So that's a good signal. While if you make a typo in this object, the application will render correctly was just a warning that you're using an invalid style key. So that's one of the differences how using a stylesheet.create based object is a little bit more strict and more clear in its dealing with CSS properties. All right, so let me get rid of this extra object here and just keep step one as a blue text and just keep one item in this array. You can get rid of the area
>
> **[4:49](https://www.linkedin.com/learning/react-native-essential-training/styling-react-native-components?u=76281980&t=289)** but I'm going to keep it here for the next example. And in fact, let's go back to having step two and step three so that we learn about the Flex layout. And let me rename this to just container and rename section title to just title just for simplicity. And also to add some visibility into what we're going to do next, ;et me give the container a different background color. So background color, and let's go with orange. So that's our container and it's not taking the whole screen. It's just sizing itself to the content that's in it. If we add the magic Flex one property, that tells the container to expand and use all the available space in the layout in the default direction, which is vertical. And not only we can control the container itself, we can control the items. So for example, we can say justify content to be center which means align all the items in the container to be centered in the Flex direction, which again is vertical. We can also align the items to be centered as well but this align items property, we'll align them in the opposite direction for the Flex direction, which is vertical. So this will align them horizontally. So that means this padding horizontal here is no longer needed. So there are a few values here for justify. For example the default is Flex start and this will put the content
>
> **[6:21](https://www.linkedin.com/learning/react-native-essential-training/styling-react-native-components?u=76281980&t=381)** at the beginning of the Flex direction. There is also a Flex end. If you need to put it at the end of the Flex direction. You can also use space between which puts as much space between the content as possible. There's also a space around which justifies the content by putting equal spaces around them. There is a lot of Flexibility in positioning things around using this Flex layout. And it's a lot more Flexible than that. You can, for example, change the Flex direction itself to be row instead of the default column. And that would align the content horizontally just like that. And in this case, the justify content and align items values are flipped in terms of the direction that they work on. So the justify content will now work in the row direction and the align items will now work in the inverse direction. So if we want to put the content at the end, we can align items to Flex and in here or Flex start to put it up top. So notice how align items is now working vertically while justify content will work horizontally. So if we put Flex start in here, there will be no more spaces around them horizontally and they will all be justified on the start of the Flex direction, which is row in this case. So let me go back to the default direction and with spaces around and centered as well.
>
> **[7:57](https://www.linkedin.com/learning/react-native-essential-training/styling-react-native-components?u=76281980&t=477)** Now, these text elements are not really Flexed. We can inspect that by giving them background color. So let's go with gray and let me show you how we can use Flex values on these texts children's as well. I'm going to add it here. So I'll just add another object directly here and do Flex one in here. And don't worry about the warning just for the sake of the example. So check it out. The children used their Flex to take all the available space within the container space. To make this a little bit more clear, let me get rid of align items and have the default which is going to take the whole screen as well. And let me give the titles a little bit of margin. So just 10 here as a margin. And here we go. The text elements are taking 1/3 of the screen each and this is happening through the Flex layout. And the reason they're taking one third of the screen is because all of their Flex values are the same. So the container divide their layout equally. If the first Flex child here has a value of two instead of one, while the other ones are going to take one and one, that means the first element is going to be as high as both the second one and third one. So basically the first one is going to take half the screen. And the second and third ones will take one quarter of the screen each. So you can control these Flex values
>
> **[9:31](https://www.linkedin.com/learning/react-native-essential-training/styling-react-native-components?u=76281980&t=571)** to give any height that you want to any element on the screen. And there is no limit to that. I can say that this element has five. This element has four, and this element has three and check out how these elements took that space on the screen. The Flex layout basically sums up all these Flex numbers and then give every child element the portion that it requested. There is a lot more to the Flex model than that. And I encourage you to learn more about it on your own. Check out this Yoga layout site which has many examples and an integrated playground for you to play around with everything about Flex. There's also this fun little game called Flex Froggy which I remember helping me a lot when I was learning about Flex myself. This one is teaching the web model which is slightly different than the React Native model but it covers the basics that are shared between all of them. It's a fun little game to play and I think you're going to like it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (9), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (6), [React](../../Skills/Web%20Development/React.js.md) (4), [ESLint](../../Skills/Software%20Development/ESLint.md) (2), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** css (15), api (1), html (1)
> **Definitions:** is a  (5), is an  (2)
> **Analogies:** for example (5), just like (2)
> **CLI Commands:** make (4)
> **Prerequisites:** you'll need (1), install (1), configure (1)
> **Cross-References:** go back to (2)
> **Warnings:** warning (2)

#### [Exploring components and APIs](https://www.linkedin.com/learning/react-native-essential-training/exploring-components-and-apis?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/exploring-components-and-apis?u=76281980&t=0)** - [Instructor] The [React Native](../../Skills/Mobile%20Development/React%20Native.md) repository on [GitHub](../../Skills/Software%20Development/GitHub.md) under the [Facebook](../../Glossary/Service/Facebook.md) organization here has a few really good reference examples and packages that you should explore. But let me point your attention to one particular package that is very helpful. And you can find that under packages here, and I'm talking about this rn-tester package. This package is basically a demo of all the available React Native views and modules. Let me show you. There are some instructions here that you can do. Basically we clone the React Native repo, all of it. So we just paste the same command to clone the React Native repo. Then we go into the new created directory and run [npm](../../Skills/Web%20Development/npm.md) install in here, and then we need to run npm start here. And keep this running in a terminal. And from another terminal, let's go into packages, and then rn-tester. And in here, we run npm install as well. And we can follow the directions here to install the bundler gem, and then we use the bundler gem to install the dependencies of this project. So we need to gem install bundler, and I need to sudo this command. And then once bundler is installed, we run the bundle install and pod install commands. When the pod installation is complete, take a look at what's in this directory, and there is an xcworkspace file.
>
> **[1:36](https://www.linkedin.com/learning/react-native-essential-training/exploring-components-and-apis?u=76281980&t=96)** This is the file that we need to open in [Xcode](../../Skills/Mobile%20Development/Xcode.md). So we can just do open and that file and to run the RN Tester project, we can just click the run button here. And it will launch this RN Tester project in the simulator. And let me go ahead and open up my code editor on this directory. You are going to love this RN Tester project. It has many examples of a lot of things that you can do with React Native. You can explore how these examples look in iPhone, and you can also run the same project in [Android](../../Glossary/Sdk/Android.md) and see how things are different in Android. You can also explore the source code of all these components and APIs. So let's explore some of the UI components that are presented here in the RN Tester project. Let's take a look at Button. And in here, you'll see multiple examples of how to use the button component in React Native and how to customize its color and layout and how to make it disabled. And you can press those to see the action that they do, which is calling the React Native alert API. And all of these examples are available right here in the source code. So you can do a file search here and look for button, and you'll see this button example right here. And in here, you'll see the four examples that are presented in the RN Tester UI app. So here is the unbutton press function that just uses the alert API, which we are importing from React Native. And you'll see the four examples here in an exported array.
>
> **[3:08](https://www.linkedin.com/learning/react-native-essential-training/exploring-components-and-apis?u=76281980&t=188)** And every one of them has a render function returning the component example here. So this is the simple button example that we see in here. If you need to adjust the color of a button, you can just pass the color property here. If you want to disable the button, we can just pass the disabled attribute. So these are very simple to follow examples, and there are so many of them. So let's explore some more. The one UI element that probably exists in every React Native application is the view element. So search for view and you'll see a few examples here. And what I'm looking for is this one, the basic building block of all UI. So this screen here gives you a lot of examples on how to use the view element. This view element is often compared to the div element in [HTML](../../Skills/Web%20Development/HTML.md). Let's take a look at its source code. So look for view example. And the source code of this example is mostly about styling view elements. You'll see a lot of examples on how to style the view element differently. Another important UI element here is the text element. So unlike HTML on the web, you can't actually have free form of text with the React Native application. You have to wrap any text with a text React Native element. And another element example is the image element. And in here, you'll see examples of how to use the image element and all the attributes that you can use with it
>
> **[4:40](https://www.linkedin.com/learning/react-native-essential-training/exploring-components-and-apis?u=76281980&t=280)** and how to style it. So take a look at all these examples and explore their source code and get yourself familiar with the way they are used in React Native. One thing you'll notice is that there are some components that are platform specific, like for example, Date Picker iOS in here. If you search for iOS, you'll see all the iOS elements. And of course on Android, those will have the Android suffix as well. These components can be only used in their platform, but there are usually better third-party components that provide solutions that can be rendered on all platforms. Right under the components section, you can explore the APIs available to you in React Native. For example, here is the alert platform API, and how to use all of its different options. And you can see the source code for these API examples as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (13), [Android](../../Glossary/Sdk/Android.md) (4), [npm](../../Skills/Web%20Development/npm.md) (3), [HTML](../../Skills/Web%20Development/HTML.md) (2), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Prerequisites:** install (7)
> **CLI Commands:** npm (3), find (1), sudo (1), make (1)
> **Env Vars:** api (4), html (2)
> **Exercise Files:** source code (6)
> **Code Identifiers:** ios (3), iphone (1)
> **Tools:** terminal (2), github (1)
> **Analogies:** for example (2)


### 3. Building a Simple Game App

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction and preview](https://www.linkedin.com/learning/react-native-essential-training/introduction-and-preview-2?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/introduction-and-preview-2?u=76281980&t=0)** - [Presenter] In this chapter, we're going to build this Target Sum Game. So let me quickly explain it to you. You'll get a random number here, 26. And you'll get six random numbers here. And some of these six random numbers, some to the 26 value. The user is challenged to pick the numbers that sum to the target sum. For every game, you get 10 seconds to solve the game. If you don't solve it within 10 seconds, you automatically lose. So let's try to win this game. If you pick the right numbers, you win. You can play again and if you pick the wrong numbers, you lose.

> [!info]- Semantic Content
>
> **Speakers:** - [presenter] (1)

#### [Initializing the TargetSum app](https://www.linkedin.com/learning/react-native-essential-training/initializing-the-targetsum-app?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/initializing-the-targetsum-app?u=76281980&t=0)** - [Instructor] Let's create a brand new [React Native](../../Skills/Mobile%20Development/React%20Native.md) project for our target sum game. We'll start with [React](../../Skills/Web%20Development/React.js.md) init TargetSum. CD TargetSum. And react-native run-ios. When the command is done, we should see the package we're running. We should see the React Native template application running in the simulator. Let me now run Atom here and start changing things. The first thing I want to do is create a new folder under TargetSum. We call this source folder. And inside this source folder we'll build our complete application and put all the React Native components in there. And let's create a new folder under source. Call this folder components. And inside components, I'll create a new file and call this file App. So this is my top level app component. And this is going to be a regular React class component. I'll name this component App, and import it as default. Let's make a very simple app component. We'll need to import a few things from a React Native. Anything you do in the React Native application is part of the React Native library. So let's go ahead and bring in the View component and the Text component. And use those. So here's my view, and inside the view, we'll put a text. And put a placeholder text in there. Okay so this is looking good, I'm exporting this. So in index.ios.js, instead of all this.
>
> **[1:32](https://www.linkedin.com/learning/react-native-essential-training/initializing-the-targetsum-app?u=76281980&t=92)** In fact, except the last line, I'm going to delete everything. And I'm going to import App from the same level here. Source components/App. And I'll just use App here. So I only need the app registry from React Native. So let's import that. Import AppRegistry from react-native. And I'll do the exact same thing in index.[Android](../../Glossary/Sdk/Android.md).js. 'Cause this is the entry level for Android applications as well. Okay so if everything work, the template should go away and I should see the my TargetSum text in there. So Command + R. And it's not working. So could not resolve TargetSum source components App. Did I misspell that? Yes I did. So its components. Missing an N in here. Reload. And you should see the TargetSum container text right here in the corner. But we know that things are working. In fact, let's not have a text at all. Let's just give our container a background color so that we know that we have an empty container to work with. So we'll do a style in here. And although you can use backgroundColor in here directly, it's usually better to do all the styling through the React Native style sheet create function. So instead of that, we'll have styles. say container variable. And this container variable
>
> **[3:04](https://www.linkedin.com/learning/react-native-essential-training/initializing-the-targetsum-app?u=76281980&t=184)** is going to be defined on the style sheet that comes with React Native. So we'll do const styles, equal StyleSheet.create. And then in here we'll give it an object. It's just one argument. An object of all the properties of the styles that we want. So we want a container property. And in here we define the styles. For example, background color. And let's just go with a simple gray color. And give the whole view a flex of one so that it stretches across the whole screen. And let's go ahead and test that. Now we have a gray background color for an empty container in this simple React application. Now the advantage to using StyleSheet.create versus doing this directly with [JavaScript](../../Skills/Software%20Development/JavaScript.md) object. Is that if you come here and you do a wrong CSS property. Let's for example say that you did FLX instead of flex. Just an honest mistake. But if you do this through regular JavaScript object, this will probably be ignored. While in React Native, you will actually get a very clear error that FLX is not a valid style sheet property. And it gives you the list of all the valid style sheet properties at that point. So if we don't use StyleSheet.create, say that we do that directly through an object. Let's see the difference. We don't really get a very clear error that we used an invalid style sheet property. Although React Native still gives you a warning here. But I like to see an error if I use something wrong in the styles that I'm doing.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (11), [React](../../Skills/Web%20Development/React.js.md) (5), [Android](../../Glossary/Sdk/Android.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2)
> **Env Vars:** flx (2), css (1)
> **File Paths:** index.ios.js (1), index.android.js (1)
> **CLI Commands:** cd (1), make (1)
> **Exercise Files:** template (2)
> **Analogies:** for example (2)
> **Code Identifiers:** backgroundcolor (1)
> **Tools:** atom (1)

#### [Configuring ESLint](https://www.linkedin.com/learning/react-native-essential-training/configuring-eslint?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/configuring-eslint?u=76281980&t=0)** - [Instructor] The first order of business is to bring in some [ESLint](../../Skills/Software%20Development/ESLint.md) configuration. If I forget a comma here or actually leave something that is not used, I'd like ESLint to be configured to tell me about these problems when I save this file. Right now, there is no ESLint configured. So you can install ESLint and bring in any configuration you like. I like to use my own, slightly modified recommended configuration for ESLint, it's part of my DOT files repo as well. So it's .eslintrc.js in here. And this configuration uses the Babel ESLint and the main recommended ESLint rules, plus the recommended [react](../../Skills/Web%20Development/React.js.md) plugin rules and a few customizations. So we'll add this file under TargetSum as .eslintrc.js. And to get this file to work, we need to install a few dependencies. So [npm](../../Skills/Web%20Development/npm.md) install, these are development dependencies, so dash D. We need ESLint itself. I also need Babel-ESLint because my configuration uses that as a parser. And I need ESLint plugin react. So you need all three of these packages if you're using my exact same customized configuration. Otherwise, you might need to install something like ESLint Airbnb configuration file. Let's test, you might need to restart your editor to get ESLint configuration to work, but now, if I go to App.js and save, I should see the problems and ESLint gives me exactly what is wrong.
>
> **[1:33](https://www.linkedin.com/learning/react-native-essential-training/configuring-eslint?u=76281980&t=93)** You'll see the error messages right here in your editor and the linter says, "Text is undefined," so remove that. And it says you forgot a semicolon here, so fix that and make ESLint happy.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [ESLint](../../Skills/Software%20Development/ESLint.md) (14), [React](../../Skills/Web%20Development/React.js.md) (2), [npm](../../Skills/Web%20Development/npm.md) (1)
> **Prerequisites:** install (4)
> **File Paths:** eslintrc.js (2), app.js (1)
> **Best Practices:** recommended (3)
> **CLI Commands:** npm (1), make (1)
> **Env Vars:** dot (1), npm (1)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)

#### [The game component with a random target](https://www.linkedin.com/learning/react-native-essential-training/the-game-component-with-a-random-target?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/the-game-component-with-a-random-target?u=76281980&t=0)** - [Instructor] Let's start on the game logic. First of all, it might be a good idea to have a game component and make the app component, render that game component and put all the logic related to a single game inside the game component. This way we can have the app component reset the game component at any point by just remounting another game component. Which will be handy when we need to replay the game. So I'm going to copy the content of this file, this will be our game component and in the app component, I'll just render this game component. I don't need any children here so I'm going to make it self-closing and we'll import game from the same level here, game digests. It's been a longer need the view or actually the style sheet. I don't need anything from [React Native](../../Skills/Mobile%20Development/React%20Native.md). So, this is just rendering the game component. Again, we'll come back to this app component when we need to and you'll see why this decision is a good one to start with every time you have a case like this. Also, in here, we can have some default. Any properties that we want to pass to the game component can be controlled from within the app component itself. So far if we don't know these properties, we'll come back here once we do. Alright, let's create the game component, Game.JS, this is the exact app component that we started with, but we'll name it game. And what I want to do first in this game component is to specify the target summits of. The initial number that we want to challenge the players
>
> **[1:36](https://www.linkedin.com/learning/react-native-essential-training/the-game-component-with-a-random-target?u=76281980&t=96)** to find a sum for. So let's create a text component in here and inside the text component, I'll just have a placeholder of 42. We need to import text from react native, let's test. So you'll see the 42 appearing here in the corner. Let's actually give it some styles. We'll do style equal styles.target. This is a [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) target dev. I am not a CSS expert, but I'll do some minimal styling in this app just to get things to look decent for now. I'll fast forward to the styles that I came up with here and explain them real quick. I've given the container a padding top of 30, so that we keep out of the way of the phone controls here. I gave the target a bigger font and a different background color, and a little bit of horizontal margin using this margin horizontal property, which is an example of a layout property that is introduced just in react native and it's equivalent to setting both margin left and margin right. So you notice how I get the styling out of the way before I actually start thinking about the logic of this target value. Which is by the way, right now. So instead of 42, we need a dynamic value that is generated for every game. So when I initialize a game, I need a private property for this game with a randomly selected target. So when I need something private to the game instance itself, that's the sign that it should go on the instance of the property directly. For example, we'll call it target,
>
> **[3:10](https://www.linkedin.com/learning/react-native-essential-training/the-game-component-with-a-random-target?u=76281980&t=190)** so this.target. First I don't have a target, so let's go ahead and just generate the random target. So target equal, we'll do math.random and this gives me from zero to 0.999, so let's do a target between 10 and say 50. You don't want to target under 10 because that would be easy to solve. So we'll do 10 plus 40 times math.random and we'll floor this so that we don't have any fractions. And this should actually work. Let's test it. 46 32, 25, excellent. In the next this movie, we'll generate the random numbers that will potentially sum to this target.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (3), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **CLI Commands:** make (2), find (1)
> **Definitions:** is a  (2), is an  (1)
> **File Paths:** game.js (1)
> **Env Vars:** css (1)
> **Versions:** 0.999 (1)
> **Cross-References:** in the next (1)
> **Analogies:** for example (1)

#### [Generating the random play numbers](https://www.linkedin.com/learning/react-native-essential-training/generating-the-random-play-numbers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/generating-the-random-play-numbers?u=76281980&t=0)** - [Instructor] We have a target sum it's now time to generate the actual numbers that the player is going to use to sum up to the target. So before we do that, let me talk about my decision to keep this target div in this top level game component and not create a child component just to represent this target div. This is definitely a style preference. If you want this target device to be contained and you want the style of this target div to be contained, then a child component to represent this target div might be worth it. However, a child component to represent this target, div also introduce some complexities. So it's just something that you need to decide on. I decided to keep this target text in this game component for many reasons. But one of them is that it's the only text of this type, the target text. And the other reason is that a lot of the calculations in this game component are going to depend on the value of this target. So introducing the child component here is going to introduce some data flow complexities in the game, and I'm trying to keep things simple. So now we need to create a text element for every random number that the player is going to use to play. But before we do that, let's think about it. First of all, this number of random numbers, I'll call it random numbers count is something that we probably should control in every game. So for example, I might render a game with six random numbers, and then I might render another game with seven random numbers to make the game a little bit more difficult. So it's definitely something that we should pass to the game
>
> **[1:35](https://www.linkedin.com/learning/react-native-essential-training/generating-the-random-play-numbers?u=76281980&t=95)** from the top level component that controls the game. So let's call this random number count. Let's render our first game with six random numbers. This is the first prop that I am designing for the game component. This means inside the game component, when I use this prop, anywhere in here, I'll use this.props.randomNumberCount, it's going to be six by default, but I can absolutely render a game with a different random number of count. So I'm going to actually go ahead and render this inside the text just for testing this random number count to make sure that I'm seeing the value here. However, since I introduced a new prop for this game component, I should define the type of this prop using prop types. In fact, the recommended [ESLint](../../Skills/Software%20Development/ESLint.md) configuration should warn you if you're using a prop. without defining its type, but I actually turn off this recommended setting. But for this example, let's turn it back on by removing this line. And if you do that and you use any prop type, you should now get this message that random number count is missing in prop type validation. Yes, it is. Let's fix that. Import from prop types. This is the package that we need now, it's now a separate package. We import it from prop-types. This used to be part of [react](../../Skills/Web%20Development/React.js.md), but now it's on separate package. And the prop type definition is a property on the game component, propTypes. This is camel case. And inside the object that represent the prop types, we put a property for every prop
>
> **[3:08](https://www.linkedin.com/learning/react-native-essential-training/generating-the-random-play-numbers?u=76281980&t=188)** that we design in this component. So we have random numbers count, which is prop types.string. And it is absolutely required. We cannot render a game component without random number count. So now we want to take this random number count, which starts at six, and we want to have an array of random numbers so we can render that array inside our game component. And just like we placed the target number on the instance of the component, we can also place this array on the instance of the component. Let's call this array random numbers. So this array is somehow driven by the random numbers count property. To create an array from a number, there is a [JavaScript](../../Skills/Software%20Development/JavaScript.md) trick we can use, array.from and you give this from an object and inside the object, we define the length of the array that you want. So I want the length to be the this.props.randomNumberCount, just like that. This gives me in the rate of six undefined elements. And then I can map this array into actual random numbers here. So for example, this is going to give me a function for every element in the array. And I can use an expression similar to what I did for the target here to create random numbers here. Maybe we'll have these random numbers start from two up to say, maybe 10, let me fix the indentation here. So this will actually work.
>
> **[4:42](https://www.linkedin.com/learning/react-native-essential-training/generating-the-random-play-numbers?u=76281980&t=282)** This will give us an array of random numbers, but we have a logical problem here. We can't just generate any set of random numbers. We need random numbers with the possibility of summing up to target, which is another random number here. So to do that, you might be tempted to start thinking about factoring this target number and somehow figure out some factors that will sum up to that target number and then fill the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the random numbers count array with random numbers. But that's actually a lot harder than it needs to be. In fact, if we just change the order of this operation, if we start with the random numbers and then do the target after that, we could derive the value of the target from the random numbers themselves. Maybe we'll sum three or four or five out of these random numbers and say, this is our target. And this way we would be sure that the random numbers have a possible solution to sum up to the target. So I'm going to do exactly that. I'm going to start with this.random numbers and I'll slice this array 'cause I don't want the whole random numbers. So let's take the first four numbers and sum those. Now, of course the random numbers count is dynamic. So I can't just say, give me four to get the first four numbers. Let's do this.props.randomnumbercount minus two. And I just figured out there is a typo here, so it's props. This.props.randomNumberCount minus two.
>
> **[6:15](https://www.linkedin.com/learning/react-native-essential-training/generating-the-random-play-numbers?u=76281980&t=375)** This would give me an array of the first four numbers. And I can sum that array. So I'll put this on its own line. The easiest way to summon array without introducing any new libraries is just to use a reduced call. And the reduce gives you access to an accumulator in the current element, and you can return the accumulator plus the current element. And you can start from zero, fail safe. You actually don't need it in some case, but in case the array that you sum is empty. For some reason, you need this fail safe so just use it. And now we can test. Instead of putting the random number count, let's output the random numbers themself. Let's actually see if that works. And it did. I am seeing the warning here, invalid prop of time numbers applied to game expected string. Totally. Random number count is not a string. It's a number. That was a very helpful React warning. Don't cover something wrong that we did here in the prop types. Okay, so instead of rendering the random numbers directly here, let's render them each in their own text container. So I'll go ahead and map this array. This will give me a single random number and I'll map that random number into a text by itself. So the map is going to give me the value of each random number, a single number, and I can render it here. Random number. Now, the very helpful, ESLint is telling me that I'm missing a key prop. Remember that every time you have a dynamic list of children that you're rendering in here,
>
> **[7:50](https://www.linkedin.com/learning/react-native-essential-training/generating-the-random-play-numbers?u=76281980&t=470)** you need a unique key to represent each child. Now, I can't use the random number itself because it's not unique. I might pick two same random numbers. But I can use the index of that. The position of that random number in the array. Now in general, using the index is not actually a very good idea. If you are reordering the items or if you're deleting or updating the items, because React is going to be confused when you delete an item and it will reposition the array and bad things will happen. But for our particular case, we are not going to rearrange or delete any items. We are just clicking on those items and we just need to identify them when we click on them. So that is probably okay for this particular case. If you need to delete or reorder things, the solution is to give every number a unique ID. Instead of just the value, you can represent every random number with an object and give that object its unique ID. But I think it would be a case of over-engineering if we did this for this particular app. Okay, so let's test that. And now we can tell if 22 is actually the sum of the first four elements and it is totally the sum of the first four elements. By the way, this four elements here can also be customized. So instead of doing random number count minus two, we can pass a property for example, that is sum number count, but I'll keep it simple. Once we're done with the MVB of this game, there will be a lot of room for improvements, and I'm going to actually challenge you
>
> **[9:22](https://www.linkedin.com/learning/react-native-essential-training/generating-the-random-play-numbers?u=76281980&t=562)** with some of those improvements at the end of the game. So let's go ahead and style these numbers now. I'll do my styling off-camera. If you want to be creative, do your own styling and test your knowledge in the subset of the [React Native](../../Skills/Mobile%20Development/React%20Native.md) styles that you can use. I'll show you the styles that I'm going to come up with in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (3), [ESLint](../../Skills/Software%20Development/ESLint.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1)
> **Analogies:** for example (3), just like (2)
> **Code Identifiers:** randomnumbercount (3), proptypes (1)
> **Definitions:** is a  (3)
> **CLI Commands:** make (2)
> **Best Practices:** recommended (2)
> **Warnings:** warning (2)
> **Env Vars:** mvb (1)

#### [Styling the random play numbers](https://www.linkedin.com/learning/react-native-essential-training/styling-the-random-play-numbers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/styling-the-random-play-numbers?u=76281980&t=0)** - [Instructor] Here are the styles that I ended up with, you can check them out from [Git](../../Skills/Software%20Development/Git.md) tag 3.6. It's not very good, I know, but it will do for now. Hopefully you made a much better looking game, but here's what I did. I had to wrap all these random numbers in their own containers so that I can control its flex property and I named it randomContainer and these guys get just random. And I made some changes here to give the target margin from both sides. And then the randomContainer gets its own flex property but the direction, this time, is row and the flex wrap property is wrapped because I want the numbers to wrap. Justify content is spaces around. For every random number, I just give it a background with some margin, a font size, and a textAlign center. But the key for layout-ing these numbers is here. The flexDirection is row. By default, the flexDirection is column. The flexWrap is wrap, by default, it's not. In fact, you can actually see what would happen if we don't have, for example, wrap. Bad things happen. What would happen if the direction is column? They will stack vertically and since I have a wrap, it will wrap, right? So you need all these three to get to the same layout that I reached in here. Okay, so this will do for now. Now, before we continue, here's one observation to the design so far. If we analyze what we did here, you might be thinking, "Isn't the target always going
>
> **[1:33](https://www.linkedin.com/learning/react-native-essential-training/styling-the-random-play-numbers?u=76281980&t=93)** to be the first four numbers summed?" So the solution to this game will be really easy, just click the first four numbers and you are absolutely correct. It's always going to be the first four numbers. Now, I'm not going to change it now because this problem, if we want to label it problem for now, is actually helpful in testing 'cause I want to start testing and make sure when I click the actual sum the game is won. So once I code all this logic, I can come back and fix this problem and maybe just shuffle the random numbers. The fix is actually really easy. But let's keep it just like that for now and put a to-do here so that we don't forget to do shuffle the random numbers. All right, so in the next video we'll start clicking on these numbers and figure out what needs to happen when we do.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (1)
> **Code Identifiers:** randomcontainer (2), flexdirection (2), textalign (1), flexwrap (1)
> **CLI Commands:** git (1), make (1)
> **Analogies:** for example (1), just like (1)
> **Versions:** 3.6 (1)
> **Cross-References:** in the next (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor] (1)

#### [Using TouchableOpacity for press events](https://www.linkedin.com/learning/react-native-essential-training/using-touchableopacity-for-press-events?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/using-touchableopacity-for-press-events?u=76281980&t=0)** - [Instructor] Our next step is to click these numbers and start to think about what should happen when we do. Now, these numbers are so far presented here with just a text element. And since we're going to click on that text element and that click is potentially going to need the random number value. Since it's going to trigger a computation of the (mumbles) that's our sign that we need a component to represent every clickable number. Whenever you have something that you need to click on and you need the value related to that thing that you click on, it's your sign that you need a component. The other sign that we actually needed a component here is that we are rendering six similar things, they're all random numbers. They share markup, they share style and they're also now going to share behavior. Whenever we click on each of these six, the behavior is basically the same. These are our signs that we need a component here. So let's go ahead and create a component. The new component markup is going to be basically this exact same text without the key, because the key is going to be here. So instead of this text here, what we want to do is we want to render a component let's name this component, random number. And I still need to do the key in here because the dynamics children is here. So the key is the index and this random number of component is going to need it's number, which we can pass through it from the map here as random number. And it probably also needs to use the unique ID that identifies this number,
>
> **[1:35](https://www.linkedin.com/learning/react-native-essential-training/using-touchableopacity-for-press-events?u=76281980&t=95)** but I'll actually leave it until we need it. So I'll just simply do that and then I'll take this markup and go create this new random number component. Let's place this random number component here, so import random number from the same level random number and we'll create random number under the components folder and call this randomnumber.js and this random number can be a simple [react](../../Skills/Web%20Development/React.js.md) class component, call it random number and it returns the exact same markup that we had before except that we don't need the key. And instead of random number here it is this.props.number. And I need to bring a few things including the style. So the styles for the game had the random numbers style which is now part of the random number component. So we'll take that and put it in here. And I'll paste this line as well to create the style sheet and get things to work here. So I got the style and I also need to import text from [React Native](../../Skills/Mobile%20Development/React%20Native.md) and also need the prop types because I just defined the prop on this random number component. I don't need the view so far, I just need prop types and my prop types let's define that, prop types are, we have number which is prototypes.number.isrequired.
>
> **[3:12](https://www.linkedin.com/learning/react-native-essential-training/using-touchableopacity-for-press-events?u=76281980&t=192)** Okay. So what I did so far should be exactly equivalent, right? I didn't change anything, I'm just rendering this random number now through a component instead of directly which made the code for this game component shorter and that's always a good thing. Test and make sure things are working exactly as they did before. And now we can actually test the clicking event or as the terminology in react native, the pressing event. When we press on an element here, what's going to happen? When we tap an element on the phone what's going to happen? So our elements currently is just a text. If I want to handle the pressing event, I need to bring in one of two elements either touchable opacity or a touchable highlight. So touchable opacity and touchable highlights are both wrappers for making views respond to touches on press down. The opacity of the wrapped view is decreased. So these two are the recommended ones. And the difference between them is mostly layout as the touchable highlight component will wrap its child in a new view, which can affect the layout. And also the visible difference is that the touchable capacity component uses animation to decrease the pacity of its child. while the touchable highlight will actually highlight the child. So I'm going to go ahead and try the touchable opacity and see how that works. So we need to import it from the react native. and in here, we need to surround this text with touchable opacity.
>
> **[4:44](https://www.linkedin.com/learning/react-native-essential-training/using-touchableopacity-for-press-events?u=76281980&t=284)** So we'll do a touchable opacity and inside the touchable opacity, we'll do all our texts and then the touchable opacity has an on-press property like that and I can assign this property to be a function that is on the instance of the random number component, I'll call this function handle press. So the on-press here in react native is equivalent to the unclick event in web-based react applications. So we can now define handle press or use class field Centex here and inside the handle press let's just console log this.props.number, make sure that we are seeing every number when we click on it. So to see this console log statement, we need to have the remote JS debugger open. So make sure that you are debugging JS remotely. And if you do, the remote native debugger, [Windows](../../Glossary/Service/Windows.md) could be open and running like that. And now when we press on any number, we should see its value here. Just to make sure that works. So let me actually show you the difference between touchable opacity and touchable highlights. So when we touch an element now, you'll see how the animation changes its opacity. If we used touchable highlight instead of touchable opacity and take a look at the difference, you'll see that the element is actually highlighted when we touch it now. And of course we can style the highlight, but this is just the difference between touchable opacity and touchable highlight. So for our case, touchable opacity would be better.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (4), [React](../../Skills/Web%20Development/React.js.md) (2), [Windows](../../Glossary/Service/Windows.md) (1)
> **UI Navigation:** click on (5)
> **CLI Commands:** make (4)
> **Best Practices:** the key is (2), recommended (1)
> **File Paths:** randomnumber.js (1)
> **Speakers:** - [instructor] (1)

#### [Changing UI based on selected numbers](https://www.linkedin.com/learning/react-native-essential-training/changing-ui-based-on-selected-numbers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/changing-ui-based-on-selected-numbers?u=76281980&t=0)** - [Instructor] Now that we know that we have a way to handle the press of a number, let's think about the logic behind this press. Every time we press a number, we would like to keep a running sum. And once this running sum is equal or greater than 28, we have two cases to handle. But before we actually handle that logic, let's handle the UI logic. So every time I press a number, what should the UI do? And for this particular game, the UI should show that this number is pressed because I can't actually press it again. Once I press it once, the UI should change to the state of that number, and I can't press it again. Since the UI has to change, that means every time I press the number something has to change on the state somewhere because I need [React](../../Skills/Web%20Development/React.js.md) to re-render the view. So your next question becomes, what should we place on the state? And where should we place that on the state? For example, can I place the value of the number on the state? Can I place this six here on the state? While it might work for this case, it won't work for this case because I have randomly selected numbers that are equal. So I can't place the nine or the six. I need to place the unique identifier for that number. And currently we are using the position of that number as the unique identifier of that number. If you remember, in the game component, we used the key index as the unique identifier for every random number. So when I click on this first nine,
>
> **[1:33](https://www.linkedin.com/learning/react-native-essential-training/changing-ui-based-on-selected-numbers?u=76281980&t=93)** I should place the number zero somewhere on the state that says, the number with positional index zero is now selected. Similarly, when I click on 10, I should say the number with positional index five is now selected. And since every time I am pressing on a number I am adding this selected status to a structure, I need to maintain a collection of selected numbers. So I can do that with either an object or an array. Let's just simply use an array for selected numbers. Now, since the game components render all the numbers, it makes sense to place this array of selected numbers here on the state of the game component. So that's exactly what I'm going to do. I'm going to initiate the state object here, and inside that state object, I'll plan on having a selectedNumbers array, which starts as empty array, just like that. And every time I press a number, I'd like to push this pressed number to the selectedNumbers state property. Now I have two distinct tasks to do for this feature. The first task is to push a number to that selected numbers array every time I press the number. And the second task is to make the UI respect the values of selected numbers and change its content based on their values. For example, if those selected numbers array had the number zero in it, that means the first random number has to be styled as selected. And also we can not press it anymore. These two unique tasks, pushing to selectNumbers and making the UI respect the values in selectedNumbers
>
> **[3:08](https://www.linkedin.com/learning/react-native-essential-training/changing-ui-based-on-selected-numbers?u=76281980&t=188)** can actually be implemented separately. And I always like to start with the second task. I always like to start with the data related part of the change. So if I have data here in the selectedNumbers array, I'd like the UI to respect that data. The other task is behavior related. Once I have the UI respect the data, I'll figure out how to make it insert new data to this structure. A way to test that is to start with fake values. So let's start with the first one and the fifth element selected, and we'll make the UI respect that. Once we're done changing the UI to do so, we'll reinitialize this selected numbers as an empty array, and figure out how to do the behavior. Okay, so how should the UI look differently when the number with indexes zero and four are selected. Let's just change the opacity a little bit when we press the number. Sounds good. This means that inside this random number of component, we want to change the opacity of the text here. So let's test that, opacity is, let's go with 0.3, and see how that looks. So this actually look good, let's just do that. So I'm going to take this opacity part, and make it into its own style. So let's call that selected style, and just put the opacity in here. We'll add this selected style, only if the random number is selected.
>
> **[4:41](https://www.linkedin.com/learning/react-native-essential-training/changing-ui-based-on-selected-numbers?u=76281980&t=281)** This means in here, we would like to conditionally add another style. So I'll change this into an array so that I have the option of adding another style. And the other style is only when the number is selected. So I need to figure out if the number is selected. Now to figure out if the number is selected, I need access to the selectedNumbers array that I just placed on the game state. So let's think about that just a little bit. To figure out if the number is selected, we need access to the selectedNumbers array. So the suitable place to actually figure out if the number is selected is right here in the game component. So let's create a new function. Call this function isNumberSelected. And this is a function, and it will basically receive the index to be checked. So let's call this numberIndex, and it will return true or false based on that. So the number is selected if this dot state dot selectedNumbers dot index of the numberIndex, if this is greater than or equal to zero, then the number is selected, right? 'Cause if it's not selected, it's not part of the selectedNumbers array, this will be minus one. So now we can actually compute the isNumberSelected for every number using this function. So I'll go ahead and do exactly that. On a new line, we'll do something like isSelected equal, this dot isNumberSelected, the function that I just wrote, and calling that using the index
>
> **[6:13](https://www.linkedin.com/learning/react-native-essential-training/changing-ui-based-on-selected-numbers?u=76281980&t=373)** for the current random number which is here, just index. And let me format that. So now I have, for every random number I pass a property, which is just a Boolean isSelected. So let's go ahead and define that property. In here, we'll need to define the property isSelected. This is PropTypes dot Boolean dot isRequired. It's always either true or false. And in here, you only include the selected style when the number is selected. We can do something like this dot props dot isSelected, and and styles dot selected. Let me format all that multiple lines. So only include styles dot selected if the isSelected Boolean property is true. If it's false, [React Native](../../Skills/Mobile%20Development/React%20Native.md) is going to just ignore that false. All right, let's try it, and there you go. The number with index zero, and the number with index four are both selected and the other numbers are not. So this is part one of the task, making the UI respect data element that we fake on the state. Now it's time to make the UI behave to add these data elements on the selected numbers. Let's do that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (1), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1)
> **Code Identifiers:** selectednumbers (8), isselected (5), isnumberselected (3), numberindex (2), selectnumbers (1)
> **CLI Commands:** make (5)
> **Analogies:** for example (2), just like (1)
> **UI Navigation:** click on (2)
> **Definitions:** means that (1), is a  (1)
> **Versions:** 0.3 (1)
> **Cross-References:** in the next (1)

#### [Adding shared behavior to numbers](https://www.linkedin.com/learning/react-native-essential-training/adding-shared-behavior-to-numbers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/adding-shared-behavior-to-numbers?u=76281980&t=0)** - [Instructor] Now we need to actually select the number when we press on it. To select a number, we just need to add its index right here in the selected numbers array that belongs to the state of the game component, which means the game component should have a function that does exactly that. So let's call this function select number, and is an arrow function that will modify the state. And to select the number just like is number selected, I need access to the number index to be selected. Again, I can't just select based on the number value, I need the number index. So this function is basically going to set the state. So it will return an object that has the new state. And inside this object, we are going to change the selected numbers array and basically push a new element to that selected numbers array. So I have options here, I can, for example, do something like selected numbers, and do the previous selected numbers and then concat the new number to it, however, I prefer to use the object to spread property. So basically, I'd like to copy the original previous state.selected numbers. So this will copy the original array, and then we just add the number index. Of course, the order doesn't really matter, as long as the number index is inside the selected numbers. So every time we select a number, we want to copy the existing selected numbers, and then append this new number index to the same array and return that. And of course, we do this to state immutable, right?
>
> **[1:33](https://www.linkedin.com/learning/react-native-essential-training/adding-shared-behavior-to-numbers?u=76281980&t=93)** We don't want to mutate the state directly, we want to return a new object to be merged with the state. So we can simplify this set state and use parentheses instead of the other syntax. Let me format that, so this way, it is returning the object directly using the arrow function, shorthand notation, and I don't need the return keyword. And of course, since we are depending on the previous state, it's much safer to use the contract that receives the function rather than the contract that receives an object. Okay, so now that we have a select number function, we need the random number component to invoke this function when we press on it, right? So I need to flow this new behavior to the random number component, just like I flow data to the random number component. Now, you might be tempted to use the same name, say something like select number, equal this dot select number. This will work, however, from the perspective of the random number component, it shouldn't really know that the press event is selecting a number in the game component, because I might actually use the same random number component in a different game that does not select a number, it does something else when you press on it. So from the perspective of the random number component, we should just name this onPress. What should you do when I press on you? As the random number component, it's a lot safer to not know what is going on. This is a little bit more about
>
> **[3:05](https://www.linkedin.com/learning/react-native-essential-training/adding-shared-behavior-to-numbers?u=76281980&t=185)** isolating the responsibilities of every component. In fact, this is selected property is also revealing the logic of the game that a number is selected. So maybe instead of is selected, we do something like is disabled, right? Because if it is selected, in here, if this function is returning true and the number is selected, the random number should just be disabled, it should look disabled, and it should behave disabled. So let's actually change that instead of is selected everywhere here, I'm going to use is disabled. And instead of selected style, we'll name that as disabled. I like this much better than the first version. Naming is actually hard and it does matter. You might think that it doesn't matter here, but it does, because now this component doesn't have any logic, even in names about the state of being selected, or that number is going to be selected when it's pressed. So it's totally generic, even in names. And I might actually make it more generic. So instead of naming it random number, it will be something like principle number or something like this. But I think the name random number is okay in our case, just to have things related to the randomness of the game in general. Okay, so now that we have an onPress function that we pass down to the random number component, we can now use it. So first we define a prop type for this onPress, onPress is PropTypes.function, which is the property funk for prop type.
>
> **[4:39](https://www.linkedin.com/learning/react-native-essential-training/adding-shared-behavior-to-numbers?u=76281980&t=279)** And it's also required, what should I do when I press the component. So when I press on this component, I need to call this new props which is onPress. So basically, instead of console log here, we need to call this.props.onPress and we want to pass the index of the component here. Remember how onPress expect the index of the number that we are executing. However, we are not passing the index of the number to the random number component, we're only doing it as a key. And we can't actually access the key as a prop inside the random number component. So let's also pass the index. And instead of calling it index, I'm going to call it ID. So the ID of this random number is its positional index. So same thing as key but now I have this props, the ID inside the random number that would represent this index. So we need to define this ID. So ID is PropTypes.number.isRequired. And now I am going to pass in here, this props, the ID, which is the unique identifier for this number, and it's basically the position. Now the reason I named that ID instead of just using index is because I might later change the design and actually have an actual ID for every numbers that have this positional index. So from the perspective of the random number component, it receives an ID, an identifier, it doesn't receive an index. So I think this will work.
>
> **[6:13](https://www.linkedin.com/learning/react-native-essential-training/adding-shared-behavior-to-numbers?u=76281980&t=373)** Let's actually test, let's select this one, selected, selected, selected, selected, selected, perfect.

> [!info]- Semantic Content
>
> **Code Identifiers:** onpress (7), isrequired (1)
> **Analogies:** just like (2), for example (1)
> **Definitions:** is an  (1), is a  (1)
> **CLI Commands:** make (1)
> **UI Navigation:** select the (1)
> **Speakers:** - [instructor] (1)

#### [Using React DevTools with React Native](https://www.linkedin.com/learning/react-native-essential-training/using-react-devtools-with-react-native?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/using-react-devtools-with-react-native?u=76281980&t=0)** - [Tutor] So far in our simple application, we can select a number and that number is going to be presented as selected. However, as we are adding these features, we are also introducing problems. For example, we did nothing to say that a number that is selected can't be re selected, right? But we don't have any visibility into these problems. One tool that really helps in adding some visibility to the problems that you have in code is [react](../../Skills/Web%20Development/React.js.md)-devtools. This react-devtools that is hosted on this, [GitHub](../../Skills/Software%20Development/GitHub.md) reveal, give us this visual screen that we can use to interact with the application and see what is going on when we do. In a web based react project, we can just install a browser extension and get this react-devtool to work, but with [React Native](../../Skills/Mobile%20Development/React%20Native.md), we need to use the stand alone version of react-devtools. We can do that by simply installing, globally react-devtools, and as you can tell, react-devtools is actually an electron app. So once the react-devtools package is installed, we fire it up with the command react-devtools, which is now available globally. And you can see immediately how the react-devtools recognized our application, and now we have our application right here with all the random numbers and the state of our game component is in here. So let me show you why we have a problem. We click on the first element,
>
> **[1:33](https://www.linkedin.com/learning/react-native-essential-training/using-react-devtools-with-react-native?u=76281980&t=93)** now we can see the selected element rate. We click on the last element, and it gets added to the selected numbers array, let me click back on the first element, and it is added back. And now we have a duplicate value in the selected numbers array. We basically selected the first element twice. So this should not happen. We need to fix this. Because otherwise the logic of that game is going to be flawed. So as you can see, this tool is really helpful in having the visibility into these problems, and I would go as far as saying that really developing any react applications without this devtool, is counterproductive. This is one of the tools that you have to absolutely, take advantage of. Because otherwise you will simply be moving at a much, much slower pace. So how do we fix this problem? How do we prevent a number from being selected multiple times? You might say that since I have the select number behavior here, and I can compute the is number selected, I can drop in here and have an F statement, that says if the number is already selected, do not select it. However, this feels like the wrong place for this obstruction. Because select number, as I named it, is a function that will just select the number. It's not a function that says select number, if the number is not selected, right? If the name was so, if the name was select the number,
>
> **[3:05](https://www.linkedin.com/learning/react-native-essential-training/using-react-devtools-with-react-native?u=76281980&t=185)** if the number was not selected, then maybe that if logic statement belongs here. The place where I think this logic belongs is inside the random number component. And the random number of component, can absolutely decide on this logic. And the reason is, it's basically part of handling the press for this random number, if this random number is disabled. Note, not is selected, because we named it is disabled. If it's disabled, I should not be invoking the behavior for on press. So, the if statement belongs right here, if this, the props that is disabled, then do nothing. Otherwise, call the actual on press behavior for this number. Let's verify that this solved the problem. Here's my game component, here's the selected numbers array, click the three, click the three again, nothing. Click the five, yes. Index number two was selected, click it again, nothing. The other thing about having this visual confirmation is that, you should not really be testing the code without any visual confirmation. You can definitely do that visual confirmation through console logs, but this tool will make the visual confirmation a lot easier with fast feedback loop and you don't need to add console log statements anywhere in the group.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (11), [GitHub](../../Skills/Software%20Development/GitHub.md) (1), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1)
> **UI Navigation:** click on (2), select the (2)
> **CLI Commands:** make (1)
> **Tools:** github (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Prerequisites:** install (1)
> **Speakers:** - [tutor] (1)

#### [Computing dynamic values where needed](https://www.linkedin.com/learning/react-native-essential-training/computing-dynamic-values-where-needed?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/computing-dynamic-values-where-needed?u=76281980&t=0)** - [Instructor] It's time for us to write code to determine if the game is won or lost. This means that while we're tapping on numbers we want to calculate the sum of the selected numbers. This sum is going to affect the game status. The game status can be one of three values. It can be the currently playing game status or it could be won, or it could be lost. The thing is this game status can be computed so we don't really need to put it on the state. So what I'm going to do is I'm going to create a new function here and call it game status. And the goal of this function is to compute the game status at any point. So the first thing that we need is the sum of selected number. Let's put that in a variable, call that sum selected. We can compute the sum of selected numbers from the selected IDs that we put on the state. Cause every time we select a number here we place its index ID on the state. In fact this selected numbers name might be a bit misleading. We're not placing the actual numbers on the state. We're placing the positional order of each number, which we're using as the ID of that number. So I'm going to actually rename this selected numbers state everywhere into selected IDs instead. I think that makes it a little bit more clear
>
> **[1:33](https://www.linkedin.com/learning/react-native-essential-training/computing-dynamic-values-where-needed?u=76281980&t=93)** that we are selecting the IDs of these numbers so that when we want to compute the sum of selected numbers we start from this dot state dot selected IDs. And then we want to reduce this ray into a sum of the selected numbers that maps to these selected IDs. So let's do that. We reduce, reduce is going to give us an accumulator and the current ID. And we want to start this from zero and every time we want to return the accumulator plus the number that maps to the current index. And we can read that from this dot random numbers for the current index, and that should do the trick. In fact, that's something we should test right away. So let me console log sum selected. And for testing of this, let me just invoke game status every time we render. 'Cause every time we select a number we are going to render that view. Now by the way, console.log is not going to show up here, right? Every time we select something the console.log is not going to show up here. You have to look at it here. So we start with zero two, then I'm going to select 10. And now my sum is 12, three, 15. So this seems to be working great. If you want feedback right here in the simulator, you can actually use console.warned. And if you do that, you would get the warning right here.
>
> **[3:08](https://www.linkedin.com/learning/react-native-essential-training/computing-dynamic-values-where-needed?u=76281980&t=188)** So we start with zero. Now it's eight, 18, 26. So with console.warned, you get immediate feedback here. You don't have to look at the debugger, but I do prefer using log instead and always looking at the debugger. Okay, we have the selected sum which means we can use this value to actually determine the game status because it's one of simple three cases. When the sum selected is less than the target, which is Vista target, you can still play. So I'm going to return playing. When the selected sum is exactly equal to the target then the game has been won, so I'm going to return one. Otherwise, if we hit this point, the game is lost. We can actually document this with an if statement as well. So if sum selected is greater than this.target, again, this if statement is completely optional because that is the case, then we need to return lost. And I don't need to use else statements here because I'm just returning from the function. So every successful if statement is going to short circuit everything else. So now I can capture this game status. Let's do that. And we can simply display it in a text here just for testing. So I'm going to do exactly that, text and render the game status and test that. So the game status is playing.
>
> **[4:44](https://www.linkedin.com/learning/react-native-essential-training/computing-dynamic-values-where-needed?u=76281980&t=284)** If I click 17, 10, two plus five, I'm still playing. When I hit five, I won the game. And if you lose the game, you get lost the game. Okay? So the game status is working okay, for now. Of course, I still have a lot of edge cases to fix. For example, if I win the game, I can also lose the game. So I need to fix that edge case. But before we do that let's make the UI respect the computed game status.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Definitions:** means that (1)
> **Analogies:** for example (1)
> **Warnings:** warning (1)
> **Speakers:** - [instructor] (1)

#### [Changing UI based on game status](https://www.linkedin.com/learning/react-native-essential-training/changing-ui-based-on-game-status?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/changing-ui-based-on-game-status?u=76281980&t=0)** - [Instructor] Let's simply make the target green or red based on the gameStatus. So I can add another style here for the target to set maybe the background color to either green or red based on the gameStatus. So I'll make this into an array so that we can add another style. And in here I'm going to actually use another function to compute the style of this target panel based on the gameStatus. So let's do exactly that. Let's call this function, targetPanelStyle. We can just pass the gameStatus here directly to this targetPanelStyle so that we don't compute it again. Let me format this a little bit. And now we can write targetPanelStyle. So targetPanelStyle is a function and this function is going to receive the gameStatus here. So this is the gameStatus. And based on the gameStatus, it's going to return a style sheet. Well, you know what? I think we can do this with an easier way. Instead of introducing a new function here. Undo, undo, undo. Okay, so instead of all that function logic we can simply add dynamic style here. You can say something like styles and dynamically pick a style based on the gameStatus that we are working with. So let's do something like dynamic STATUS_ the actual gameStatus that we have.
>
> **[1:37](https://www.linkedin.com/learning/react-native-essential-training/changing-ui-based-on-game-status?u=76281980&t=97)** So gameStatus here, this would be either status playing, status won or status lost. And we can just could add style for each one of those. So in here, we'll code these styles. So STATUS PLAYING, this is the default. So we can do backgroundColor, which is basically this guy. So I'm going to move it here. So the target is always the same style except for the status. And just like this status, let's do one status for the won case and another status for the lost case. So when the status is won, let's make this green when the status is lost, let's make it red. Very simple basically, instead of if statements in functions we're using object lookup here, let's test that. So this starts as gray. And when I win the game it's green and when I lose the game, it's red, perfect. Let's now figure out how to stop selecting once the game is over because right now we can continue selecting numbers. So I think we can simply add another condition here. The number is disabled if it's selected or if the gameStatus is over, if the gameStatus basically is not playing. So let's simply do that. If the gameStatus here is not playing we cannot play this right.
>
> **[3:14](https://www.linkedin.com/learning/react-native-essential-training/changing-ui-based-on-game-status?u=76281980&t=194)** Let me just format everything here a little quick and let's test. We can play green. All the numbers are disabled and we can't play, very good. Let's try it the other way. Perfect. So we can only play if the gameStatus is playing. So with this we are actually wrapping up an MVP of this game before move on to another example. This app has plenty of room for enhancements and optimization and I'd like to show you some of those optimizations but to highlight an optimization problem before we solve it, let's introduce one more feature to this game. Let's make the game limited in time. The player has only 10 seconds to solve the challenge. After that, the game should be over automatically. We'll do that in the next video.

> [!info]- Semantic Content
>
> **Code Identifiers:** gamestatus (14), targetpanelstyle (4), backgroundcolor (1)
> **CLI Commands:** make (5)
> **Env Vars:** status_ (1), status (1), playing (1), mvp (1)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Working with timers](https://www.linkedin.com/learning/react-native-essential-training/working-with-timers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/working-with-timers?u=76281980&t=0)** - [Instructor] Implementing a countdown to track the seconds spent playing this game is easy. We can just use a regular interval. The question is do we need to put this information on the state or not? And to answer that, we need to answer another question, which is do we need to display anything in the UI while the timer is ticking? We should probably do that. We should probably display these remaining seconds. Like you start with 10 seconds and then the counter ticks that down every second and we should display the number of remaining seconds in the UI, which means we need to put this number on the state of the game. Let's do that. We'll start with remainingSeconds. I think that's a good name. And we start with 10. Of course, we can make this configurable if we need to, if you want to make the game harder. Just like randomNumberCount, we can make this game receive another property and maybe a good name for this property would be something like initialSeconds. This is a number and it's required. And here instead of hard coding this, we use this.props.initialSeconds and we can pass the initialSeconds down from the App component in here. So initialSeconds are 10. Okay, great. So now we have initialSeconds. What we need to do is when the component mounts, that's where we're supposed to start intervals. We can't start the intervals before the component mounts because the user can only interact with the component when it's completely mounted.
>
> **[1:35](https://www.linkedin.com/learning/react-native-essential-training/working-with-timers?u=76281980&t=95)** So let's do componentDidMount and inside componentDidMount, we'll define this interval. We can use setInterval. And we'll do a function. And we'll do that function every second. Inside that function, we want to basically decrement the remaining sections. So we'll setState inside this function and we'll return remainingSeconds. The previousState.remainingSeconds minus one. And we can display the remainingSeconds somewhere here. For testing, I'm just going to replace this gameStatus Text with the remainingSeconds. So this.state.remainingSeconds. Of course, this will work, however, it has a big problem. So I do have remainingSeconds and they're ticking but I did not stop this timer anywhere, so this timer is going to continue to go down into the minus. So let's go ahead and stop this timer. The best way to stop this timer is to actually clear it using clearInterval, which means we need to capture the ID that this setInterval method gives us. We can place this ID on the component instance itself. So this dot, call it intervalId. And the reason I placed that on the component instance is we need to also clear this timer when the component unmount. So on componentWillUnmount, I also need to clear this timer.
>
> **[3:06](https://www.linkedin.com/learning/react-native-essential-training/working-with-timers?u=76281980&t=186)** I'm not supposed to leave timers behind. So I'll do clearInterval here using this .setIntervalId. And we'll do the exact same condition right here after we set the state. So the best place for this new condition is inside the second argument callback for setState 'cause this callback will be invoked after the setState operation and in here, we can place a condition, a condition that says if this.state.remainingSeconds is exactly zero, I need to stop the timer, which is the exact same statement that we used in componentWillMount. Again, the reason we're placing this here in the second callback of setState is because setState is asynchronous and I want to do this statement exactly after the setState operation is performed. Testing that now. The timer starts from 10 and it continues ticking all the way to zero and when it hits zero, it should be stopped. This seems to be working. So now, when that timer hits zero, if I'm still playing, the game should automatically be over, which means our gameStatus computation has to be a little bit different. There is a condition that is superior to all the other conditions. Basically if this.state.remainingSeconds is zero, I need to return LOST.
>
> **[4:42](https://www.linkedin.com/learning/react-native-essential-training/working-with-timers?u=76281980&t=282)** This conditions happens before all the other conditions. Let's test that. So the gameStatus is playing 'cause I'm not seeing any colors and when the remainingSeconds hits zero, the game will be over automatically. While this code seems to be working okay, we actually have many problems. One problem that you probably identified already, I also need to clear the timer when I win or lose the game manually. So let's say I won the game, right? I need to stop the timer because otherwise it will go down to zero and it will make me lose the game after winning the game. So that's an edge case that we need to handle. But the other bigger problem is now this line 'cause this line is going to be computed every second and the truth is I don't really need to compute it every second. I need to compute the gameStatus on every click or when the remainingSeconds hits zero. So there are fewer conditions to compute the gameStatus. So that's something we could optimize because the computation of gameStatus involves actually computing the sum of an array. Now, it's probably not really a big deal for this particular case but I'd like to show you how to optimize such cases. If you want to optimize the rendering of the gameStatus only when needed, how would you do that? And the answer is you would cache this gameStatus computation and invalidate that cache when needed. Let's do that in the next video.

> [!info]- Semantic Content
>
> **Code Identifiers:** remainingseconds (11), gamestatus (8), setstate (6), initialseconds (5), componentdidmount (2)
> **CLI Commands:** make (4)
> **Definitions:** is a  (2)
> **Env Vars:** lost (1)
> **Cross-References:** in the next (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Caching to optimize computable values](https://www.linkedin.com/learning/react-native-essential-training/caching-to-optimize-computable-values?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/caching-to-optimize-computable-values?u=76281980&t=0)** - [Instructor] In the previous video, we implemented an interval timer to limit the time a user has to play the game. And we identified that the computation of the gameStatus is now not needed every second, but rather when a number is clicked or when the timer hits zero. To optimize this computation, we need to cache it. Let's do that. Instead of this.gameStatus as a function, I'm going to assume the gameStatus is a fixed number that I have cached on the instance. This gameStatus start with playing. So in here I'll introduce a gameStatus instance property. And this one starts with playing. And now I need to change this gameStatus, the cached copy of the computed value. I need to change that when needed. And I can do so in componentWillUpdate. This life cycle method happens right before the render method. When the component is about to be updated. When the state of the component is different, or when the props of the component is different. So I actually conditionally here, I can compute the new gameStatus if there is a need to. For example, I'll have an if statement here that says, if the nextState .selectedIds array does not equal the current state selected IDs. Let's console log this value to test that we need to disable the timer for now.
>
> **[1:37](https://www.linkedin.com/learning/react-native-essential-training/caching-to-optimize-computable-values?u=76281980&t=97)** Because the timer will also trigger the componentWillUpdate. And let's test. Numbers are actually disabled. We did something wrong. Let's figure out what we did wrong. Game status. I'm not actually sure. Let's debug that. What is gameStatus here? Oh the reason we are seeing this problem is that we define gameStatus on the instance twice. Once it was the initial value. And also we still have the function here, gameStatus. So let's call this function instead of gameStatus let's call it calcGameStatus 'cause that's exactly what it's going to do. Let me test now. Seems to be working. Let me clear that. And now remember what we did. In componentWillUpdate I am console logging if the selected IDs for the next state does not equal the selected IDs for the current state. So when we click on the number, we will see true. The previous selected IDs does not equal the next selected IDs when I click the numbers because I am appending an item to the original array. And I am doing that without mutating the original array. So this condition will actually work for our case. If the next state selected IDs does not equal the current state selected IDs. Or the next state, the remaining seconds equals zero. This is the only case
>
> **[3:10](https://www.linkedin.com/learning/react-native-essential-training/caching-to-optimize-computable-values?u=76281980&t=190)** where I need to recompute the game status. For these two cases, we need to compute the gameStatus. Basically this.gameStatus equal this dot calculate the game status one more time. Let me format this code. So this is the cache in play. I have a gameStatus where I cache the value of the game status. And I have a calcGameStatus where I calculate the new game status only when I need to. Only when the selected IDs are different, or when the remaining seconds hit zero. However for this to work, gameStatus has to read from the new state. 'Cause we're no longer computing the game state on render. In here we would still be computing on the previous state. So let's pass the new state here, the next state to calculate gameStatus. And make this gameStatus depend on next state instead of the current state. So we're reading the state in gameStatus twice. Replace that with nextState. And I think that should work. So basically, when there is a condition for us to consider recomputing the gameStatus, compute the gameStatus based on the next state that is coming to this component. Let's test. The timers is on. And it's rendering. And I can go here and win the game. That's normal. And remember at the timer will still hit the last case, but it's working. Every time I actually need the gameStatus to change,
>
> **[4:43](https://www.linkedin.com/learning/react-native-essential-training/caching-to-optimize-computable-values?u=76281980&t=283)** the condition will be invoked and it will change. And you can validate that with a console log statement here in calculate game status. So calcGameStatus. So let's make sure that this calcGameStatus only happen when needs to. So while the timer is ticking here, calculate game status is not actually being invoked. And when the timer hits zero, one calculate gameStatus happens. Alternatively, only when I click that calculate game status happen. So this is a much better way for caching the expensive computation. Of course in our case, it wasn't really that expensive. But it's the thing that you can do to optimize expensive computations in your app. cache them and only invalidate the cache when you need to. This also will allow us to fix the other problem. Basically right here. When I re-invalidate the cache. If this gameStatus is not playing, that's another case where I need to clear the interval. Clear the interval for this.intervalId, right? The minute the gameStatus turns into either won or lost, I want to clear the timer. I don't need the timer to keep running so that when I win the game, the timer stops. One last thing we need to do. Remember that we placed a TODO here somewhere. TODO shuffle the random numbers. 'Cause right now, winning the game's really easy.
>
> **[6:15](https://www.linkedin.com/learning/react-native-essential-training/caching-to-optimize-computable-values?u=76281980&t=375)** You just pick the first four numbers. So we need to shuffle those numbers. So in here, after computing a target, I need to shuffle the random numbers. And the easiest way to shuffle the array is really to use a library like Lodash, which has the shuffle function. But of course I don't need to import the whole Lodash library just to use the shuffle function out of Lodash. I can use Lodash micro modules to import just the shuffle function. So basically we import shuffle from lodash.shuffle. So this will import only the shuffle function out of the Lodash library, which is what I'm interested in. And to use it in here, we can do something like shuffledRandomNumbers. Equal shuffle, the function from Lodash, performed on this.randomNumbers. Just like that. And instead of using randomNumbers, let me look where we're using randomNumbers. So we actually need to change randomNumbers in these two places into shuffledRandomNumbers. So in here, and in here. And let's test. I don't have shuffle. This is what you get when you have an error like that. I need to [npm](../../Skills/Web%20Development/npm.md) install shuffle. So npm install, lodash.shuffle. Try now. All right so 15 does not equal to three plus seven
>
> **[7:49](https://www.linkedin.com/learning/react-native-essential-training/caching-to-optimize-computable-values?u=76281980&t=469)** plus 10 plus one. We'll probably need this, this and that. And I barely hit the time target. But seems like the shuffle is actually working.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [npm](../../Skills/Web%20Development/npm.md) (2)
> **Code Identifiers:** gamestatus (24), calcgamestatus (4), randomnumbers (4), componentwillupdate (3), nextstate (2)
> **CLI Commands:** make (2), npm (2)
> **Definitions:** is a  (4)
> **Env Vars:** todo (2)
> **Analogies:** for example (1), just like (1)
> **Prerequisites:** install (2)
> **Cross-References:** previous video (1)

#### [Remounting a component to reset it](https://www.linkedin.com/learning/react-native-essential-training/remounting-a-component-to-reset-it?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/remounting-a-component-to-reset-it?u=76281980&t=0)** - [Instructor] Let's do one last feature for this game. Let's create a Play Again button once the game is won or lost. We'll start with the markup for that button. Let's place it in this area under the numbers. We can use the Button component from [React Native](../../Skills/Mobile%20Development/React%20Native.md). Here is the documentation for the Button component. We give it an onPress event and a title for that button. Let's do that. We'll place it right here under the View, Button with a title of Play Again. And we'll figure out the onPress next. But let's see how that looks. Well, you can see actually how the onPress is a required property. So let's provide an empty onPress function. Here's the button, it looks decent. So what should this button do? This button should completely reset the game, which means it has to reset the state of the game but also, it means that we have to generate a new target number. We have to generate a new set of random numbers and we also have to reset any timers that we did in the game. So a simple reset for the state is not going to do. We could think about moving all the initialization of the game into its own function and then calling this function once we want to [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) the game. But that's not going to be enough because we have to clear any interval timers that we have. We also have to refire this code
>
> **[1:36](https://www.linkedin.com/learning/react-native-essential-training/remounting-a-component-to-reset-it?u=76281980&t=96)** that initializes a new interval for the new game. So it is a bit of a complicated logic here. If we start thinking about creating the reset right here on the game level, we'll have to reset the state. We'll have to reinitialize everything that we did before the game mounts and we also have to refire the timers. So instead of going down that path, there is an easier way and the easier way is to just unmount the Game component and remount it. If we do that, then everything is going to fire normally. The state is going to be reinitialized. The generations of all the random numbers is going to be fired again and the timers are going to reset normally as well. So how do we do that? It's really simply. We'll make the App component unmount this Game component and remount it. And there is a simple trick to do that in [React](../../Skills/Web%20Development/React.js.md). If we pass this Game component a key property, any key property, React is going to identify that particular game with that key property. If we later change that key property, React is going to think that this is a brand new game and it will actually unmount the previous game and mount a new one. So all we need to do is have some kind of gameId here and just change that gameId every time we want a new game. And the easiest way to do that is to place this gameId on the state of the App component. So we can do something like this.state.gameId.
>
> **[3:10](https://www.linkedin.com/learning/react-native-essential-training/remounting-a-component-to-reset-it?u=76281980&t=190)** And we'll initialize this gameId right here on the state of the App component. So state equal an object and that object has a gameId. We can start the gameId with one. Now, to reset the game, all we need to do is change this gameId into something else. So let's write a resetGame function in here. No arguments for this function. And it will simply set the state. It will change the gameId to be the previosuState.gameId plus one. Very simple. So if we call this resetGame function, React is going to unmount the previous gameId with one and remount a new on with two. So now we need to task this resetGame function reference to the Game component so that it can call it when we hit play again. So from the perspective of the Game component, we can name this feature onPlayAgain. This will be the App component resetGame function. Let me format that a little bit. And here we go. The game has a key, we can increment that key from the App component. The game also has a behavior that it can invoke to reset the game and we named it onPlayAgain. So we'll go to the Game component and inside the Game component, we'll define this onPlayAgain function, which is a PropTypes function
>
> **[4:49](https://www.linkedin.com/learning/react-native-essential-training/remounting-a-component-to-reset-it?u=76281980&t=289)** and it's also required. Now, the rest button, the onPress here can just be this.props.onPlayAgain, just like that. And this should actually work. Let's actually test it real quick. So we've got a game. The timer is decrementing and we can play again. Did you see that? Everything reset right away because React has a new game, a game with a different ID than the previous one. So even if the game is lost like that, Play Again will just reset it. So this is great but we want to show the Play Again only when the game is lost. Not when the status is still playing, so we can do that simply with a conditional here. So I'll put the whole Play Again button in a conditional and we can do something like this.gameStatus is not playing. Show this Play Again button. Format this. And here's the condition. If the game status is not playing, we'll go ahead and show this play button. Let's test that. The game starts and there is no Play Again button. And once the game hit a condition where it's won or lost, the Pay Again button is going to show up and we can play the game again. Testing the won condition. Same thing. If the game is won, we can play again.
>
> **[6:24](https://www.linkedin.com/learning/react-native-essential-training/remounting-a-component-to-reset-it?u=76281980&t=384)** So I think is a good set of features to call this game done and move on to a different example. However, before we do that, I'd like you to think about more features to add to this game. And maybe you can focus on two features. The first feature is when you play again, make the game harder. And you can do that by controlling the parameters that we send to the game. The randomNumberCount, the initialSeconds, all of these are controllable as well. So that's one feature. Make the game harder as it progress. The other feature, keep a score. Whenever the game is won, give the player some kind of score and that score persists between games. So the more games the user win, the higher the score. So you want to think about where to place this score and how to control it. I hope you liked this example. Let me know if you have any questions about what we did or about the challenges. In the next chapter, we'll build a different example and this time, we'll work with data and actually remote data so that we know how to do things asynchronously.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (5), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1)
> **Code Identifiers:** gameid (11), onpress (5), resetgame (4), onplayagain (4), previosustate (1)
> **Definitions:** is a  (6), means that (1), is an  (1), we call this (1)
> **CLI Commands:** make (3)
> **Cross-References:** in the next (1)
> **Documentation:** the documentation (1)
> **UI Navigation:** go to (1)
> **Analogies:** just like (1)


### 4. Building a Data-Driven App

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction and preview](https://www.linkedin.com/learning/react-native-essential-training/introduction-and-preview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/introduction-and-preview?u=76281980&t=0)** - [Narrator] Over the next few chapters, we're going to build the bake sale app. It's a very simple app that lists what we're going to call deals. Every deal has an image, a title, and a price. We can click on a deal to get more information about this deal. We can swipe left and right here to navigate the images. We can click on this button to go to the deal page on [bakesaleforgood.com](https://bakesaleforgood.com). The app also support searching for deals. So let me quickly show you the APIs that we're going to be working with. So they're all hosted a [bakesaleforgood.com/api](https://bakesaleforgood.com/api). We have /deals. This is list of all the deals in the system. We have /deals slash the ID of the deal. This will give us details about a single deal. We have /deals with a search term, and this will perform the server side search and return the list of deals that match the search term.

> [!info]- Semantic Content
>
> **UI Navigation:** click on (2), go to (1)
> **URLs:** [bakesaleforgood.com](https://bakesaleforgood.com) (2)
> **Speakers:** - [narrator] (1)

#### [Initializing the Bakesale app](https://www.linkedin.com/learning/react-native-essential-training/initializing-the-bakesale-app?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/initializing-the-bakesale-app?u=76281980&t=0)** - [Instructor] Let's start the Bakesale app. We'll initialize a new project for that, so we'll start with [React Native](../../Skills/Mobile%20Development/React%20Native.md) init Bakesale. And then we'll CD into Bakesale and run React Native run iOS. Excellent, we have a brand new React Native project, let's open up the editor on this directory. Let's also run the [React](../../Skills/Web%20Development/React.js.md) dev tools. Perfect. We'll start the usual by creating a source folder under the root and under source we'll create a component folder and, in components, we'll create our App.js. App.js is a regular react class component and let's bring in some components from React Native, so I'm going to import view and text from React Native. I'll make the app component render a view and inside the view, we'll render a placeholder text. Now we can go to our index.ios.js, delete everything except the last line and we'll import our app component from dot slash source, components, slash App. We also need app registry from React Native
>
> **[1:35](https://www.linkedin.com/learning/react-native-essential-training/initializing-the-bakesale-app?u=76281980&t=95)** so we'll import that as well. And the component that we want to register is App. Let's make sure this worked and we have big failing here from the App component, excellent. We'll do the exact same thing for index.[Android](../../Glossary/Sdk/Android.md).js so that later we can test our application for Android as well. Let's center Bakesale on the screen. We need to import StyleSheet from React Native and we need to create a styles object for this component, so const styles equal StyleSheet.create and in here we'll define our different styles. We'll need a style for the container, so I'm going to call this container. The container is going to have a flex of one and we need to justify content to center. Let's also alignItems to center as well. For the header, let me just give it a font size of, say, 40. Let's test those out, I'll give the view a style of styles.container and we'll give the text a style of styles.header. Let's test that out, looking good. I'm going to start this app with the remote data itself. In the next video, we'll [Fetch](../../Skills/Web%20Development/Fetch.md) the data
>
> **[3:08](https://www.linkedin.com/learning/react-native-essential-training/initializing-the-bakesale-app?u=76281980&t=188)** from the Bakesale API and start thinking about what to do with it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (7), [React](../../Skills/Web%20Development/React.js.md) (2), [Android](../../Glossary/Sdk/Android.md) (2), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **File Paths:** app.js (2), index.ios.js (1), index.android.js (1)
> **CLI Commands:** make (2), cd (1)
> **Code Identifiers:** ios (1), alignitems (1)
> **Env Vars:** api (1)
> **Cross-References:** in the next (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)

#### [Using the Fetch API](https://www.linkedin.com/learning/react-native-essential-training/using-the-fetch-api?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/using-the-fetch-api?u=76281980&t=0)** - [Instructor] We're ready to [Fetch](../../Skills/Web%20Development/Fetch.md) the data and see what we're working with. I'll create a new file under source and call this file [AJAX](../../Skills/Web%20Development/AJAX.md).JS. And this is the file where we're going to do all the Ajax requests that are to be used by the app. So we'll export a default object here. That is our API. And in this default object, we'll define functions responsible for fetching different types of data. So let's define the first function. We're interested in fetching the initial deals. So I'm going to call this function fetch initial deals. In this function we want to do an Ajax request to the big sale API. We can use the fetch library to do that. The fetch library comes built in [React Native](../../Skills/Mobile%20Development/React%20Native.md) so we don't need to import anything. And you can see the documentation for the fetch API under the react native website. So you do fetch, the default method is get but you can change it if you need to. Once you have the initial response you can convert the initial response to [JSON](../../Skills/Web%20Development/JSON.md), and then you can use that JSON object returned from the server. So I'm going to copy this example and use it in here exactly as is except our API is going to be different and I'll just return the response JSON itself in here. So our API is from bake sale. So I'm going to define a constant here, call this constant API host.
>
> **[1:35](https://www.linkedin.com/learning/react-native-essential-training/using-the-fetch-api?u=76281980&t=95)** This will be HTTPS:[BakeSaleForGood.com](https://BakeSaleForGood.com). And in here I'll use this variable. So I'll go with like API host plus /API/deals. This is the end point that we're interested in. Let's go ahead and test that. In APTA JS, we can use a component did mount call. And in here we can use the fetch initial deals function right away, just like that. Remember that this is an async function. So we have to await on that in here. And this will give me back the list of deals. So I'll put it in a constant, just like that. Since we used await that means we have to make component did mount itself async and for now let's just console log the list of deals. Of course, I need the fetch initial deals function. So I'll import that from up on level Ajax. Let's go ahead and test that. I forgot to make this fetch initial deal async as well so that's required. I also did not make these named exports. So we need to import Ajax itself and call Ajax.fetch initialdeals. Seems like that worked. Let's check the debugger, debug JS remotely. And if you notice here I have in the rate of nine items
>
> **[3:13](https://www.linkedin.com/learning/react-native-essential-training/using-the-fetch-api?u=76281980&t=193)** those are objects that represent the deals. Every deal has a title, a price, which is in cents, a list of media files, a key and a cause. So we can now use this data to render a list of deals. To do that we'll place the deals on the state of the app component. So let's introduce a state here. This state will hold the deals array and it will start as an empty array. And when we have the deals we'll just set the state to account for those deals that just came from the API. So deals just like that. And now we can use the list of deals in here. So let's introduce a new component to represent the list of deals. We'll call it deal list. So create new file under component, call that deallist.js. This is a regular class component. So deal list and I'll bring in view and text and style sheet. This probably should be in my snippet and I'll create a view in here and I'll use a placeholder text as well for now and do something like that deals. Now in app.js I'd like to render this deals list component. So I'll have to import that. So import deal list from same level deal list. So to render the deal list, it's as simple as deal list and we'll give it the list of deals from here
>
> **[4:46](https://www.linkedin.com/learning/react-native-essential-training/using-the-fetch-api?u=76281980&t=286)** because they are hosted on the state of the app component. So this.state.deals, however, I only want to render the deal list when I have a list of deals. I'd like to keep this text while I'm loading the deals. So let's have a conditional here and do something like if I have deals, so if this.state.deals.length is greater than zero,
>
> **[5:14](https://www.linkedin.com/learning/react-native-essential-training/using-the-fetch-api?u=76281980&t=314)** we'll go ahead and render the deal list. Otherwise we'll just render the text, just like that. Let me format and let's test. So the bake sale header should flash and then I should have the deals right here. Of course we're not rendering the deals yet. We'll do that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (10), [AJAX](../../Skills/Web%20Development/AJAX.md) (6), [JSON](../../Skills/Web%20Development/JSON.md) (3), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (2)
> **Env Vars:** api (9), json (3), https (1), apta (1)
> **Analogies:** just like (4)
> **File Paths:** ajax.js (1), deallist.js (1), app.js (1)
> **CLI Commands:** make (3)
> **Definitions:** is an  (1), is a  (1)
> **URLs:** [bakesaleforgood.com](https://bakesaleforgood.com) (1)
> **Cross-References:** in the next (1)

#### [Using FlatList with API data](https://www.linkedin.com/learning/react-native-essential-training/using-flatlist-with-api-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/using-flatlist-with-api-data?u=76281980&t=0)** - [Instructor] When we started this project, I forgot to add [ESLint](../../Skills/Software%20Development/ESLint.md). So I just did that off-camera and I'm going to go ahead and fix the ESLint problems that we have. So we're not using previous state in here. so I can actually simplify this since we're not using the previous state to just that. Looking good. And deal list, we're not using style sheet, but we will. So I'm just going to keep it. I'll create a styles object here from stylesheet.create. And in here, I'll introduce a list property. So this is the main list. I'll just give it a background color for now. Very light gray, just to make sure things are working and I'll give this list to the view. So styles.list, make sure this is working. Perfect. So this is our view right here. So we want to definitely make this view take the full screen. So let's do flex one and maybe we'll give it a width of 100%. And to make this text not overlap with the controls of the iPhone, we'll just give it padding top of say, 50. Excellent. So we have introduced a new property for this component called deals. So we need to define the type for that property. So we'll do static propTypes equal
>
> **[1:37](https://www.linkedin.com/learning/react-native-essential-training/using-flatlist-with-api-data?u=76281980&t=97)** we have deals. And this deal is propTypes.array and it's required. We'll need the prop size package. So we'll import that from prop types and that's a dependency. So [npm](../../Skills/Web%20Development/npm.md) install prop-types. You can work with this deals array right away. We can actually just simply map it to whatever we want to do to render a single deal. So in here, we can do this.props.deals.map. The map is going to give me a single deal. Let's just render a text item for every deal just for testing. So in here we'll do deal.title just like that. And we'll give every text a key just to make [React](../../Skills/Web%20Development/React.js.md) happy. So deal.key. Okay, so this should work. And I can see the titles of the deals right here. The big sale project is just starting. So we only have nine deals, but you can imagine that this list of deals is going to be in the hundreds which means that we need to start scrolling this list. So we need to introduce a new [React Native](../../Skills/Mobile%20Development/React%20Native.md) component to handle that. This component used to be list view but React Native deprecated that in favor of FlaList or SectionList. For this particular case, we need FlatList. So using this FlatList component is easy.
>
> **[3:11](https://www.linkedin.com/learning/react-native-essential-training/using-flatlist-with-api-data?u=76281980&t=191)** You just give it a data property, which is an array. So we can give it our deals array and you give it a render item function which is the function that this FlatList component is going to use to render every item in that array. So let's use this FlatList array just like that. And instead of all this here, we'll just use FlatList. The data that we want to give this FlatList is our deals array which is on the props of this component. And to render every item, we can just render the title for now. We'll need to import FlatList from React Native. And I think we can test that right away. And now I am rendering the list exactly as I did before. But now this list is scrollable. You can test that and see how you can actually now scroll the list. In the next video, we'll take this part and make it into its own component to render a single deal.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (3), [ESLint](../../Skills/Software%20Development/ESLint.md) (2), [npm](../../Skills/Web%20Development/npm.md) (1), [React](../../Skills/Web%20Development/React.js.md) (1)
> **CLI Commands:** make (6), npm (1)
> **Code Identifiers:** proptypes (2), iphone (1)
> **Analogies:** just like (2), imagine (1)
> **Definitions:** means that (1), is an  (1)
> **Cross-References:** in the next (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)

#### [Working with images](https://www.linkedin.com/learning/react-native-essential-training/working-with-images?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/working-with-images?u=76281980&t=0)** - [Instructor] For every item in this flat list, we want to render a new component to represent a single deal. So we'll call this component DealItem and we'll pass it the deal, which is just the item. And we'll go ahead and place this component deal item on at the same level. So DealItem. No longer using texts in here. And we'll now create new item. So dealitem.js. [React](../../Skills/Web%20Development/React.js.md) class component which we named DealItem. So we need to work with the same prop types and [React Native](../../Skills/Mobile%20Development/React%20Native.md) components. So I'll bring those in. DealItem. I probably don't need a flat list in deal items. So it's just view text. We're also going to render an image for every deal item. So we'll go ahead and bring in image just like that. So deal item receives single property so far. So we'll define a static prop types equal the property that this deal item receives is the deal items. So this is prop types dot, let's go with object for now and this as required as well. I can actually make this little bit more specific by using the prop types shape property. But I'm going to simplify this example so that we can go over many other concepts. So every deal is going to be its own view.
>
> **[1:34](https://www.linkedin.com/learning/react-native-essential-training/working-with-images?u=76281980&t=94)** And inside the view, we'll render the title of the deal, the price of the deal and the image that represents the deal. Let's actually start with the image. So the image component in React Native expects a source property and this source property is an actual object. And I can specify many attributes for that object, but the most important one is the URI if you're rendering an image from the internet. What is the URI of that image? And for us, the URI is this.props.deal.media. And let's render the first media item as the image for that deal. Close this image component. And we can actually test that right away. So we have a deal item that renders a deal. Let's test. It doesn't look like it worked. So let's take a look at what we're working with here. So we've got deal list. Every deal has a media, it has an array. So every deal item is a view, has a source. This all looks good to me. So I think this might be a style problem. So let me go ahead and define a style property for this image. So we'll go with style equal styles.image. I will create the styles object. Styles equal stylesheet.create. And we'll go with image.
>
> **[3:07](https://www.linkedin.com/learning/react-native-essential-training/working-with-images?u=76281980&t=187)** So let's give this image a width of 100% and let's give it its height which is coming from the API as 150. So let me format things out, let's test. And there you go. We have images and they are scrollable. Very good. So let's now we're render the other information for every deal. So under the image, we'll render another view and for that view, we'll render multiple texts. So let's render one text for the title of the deal. So this would be deal.title. And I'm going to go ahead and define the deal out of the props for the render so that I don't do this.props.deal every time. So the deal here is destructured out of this.props. And we'll render another text for the price and another text for the name of the cause for this deal. So we have deal.price and we have deal.cause.name. That's the shape of the API. Let's see how that all looks. Perfect. So we have the deal. We have its title, you have its price in cents, and we have its cause. So we need the price to be in dollars. And we'll probably do that in a few screens because right after we're done with this task,
>
> **[4:41](https://www.linkedin.com/learning/react-native-essential-training/working-with-images?u=76281980&t=281)** we're going to tap on every deal to see more information for every deal. So let's create a function. Call this function price display, and pass this deals price to that function. And this function is going to be responsible for displaying the price. So I'll place this function in a different file. So I'll import price display from let's put it up on level. Let's call this file util. So this is all the utility functions that we need in our system. So we'll create this new file util.js right under source. We'll use named exports. So export const price display equal a function. This function is going to receive the price as an argument. So price here. Remember this price is in cents. In fact, let's just name it priceInCents. And this function will return a string that represents the price in dollars. I'm going to actually make it return the dollar symbol as well. So what we need is the price in dollars, which is price in cents divided by 100. So I think this will work. Let's test that out. We already use the price display in the deal item and looks like it's working. Perfect. In the next video, we'll start thinking about what should happen when we press on a deal to get more information about that deal.
>
> **[6:14](https://www.linkedin.com/learning/react-native-essential-training/working-with-images?u=76281980&t=374)** But before we do that, we should probably do some style work to make this list look a little bit better. I'll do that off-camera, try to come up with your own styles and I'll explain the styles that I'll come up with in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (2), [React](../../Skills/Web%20Development/React.js.md) (1)
> **Env Vars:** uri (3), api (2)
> **CLI Commands:** make (3)
> **File Paths:** dealitem.js (1), util.js (1)
> **Cross-References:** in the next (2)
> **Definitions:** is an  (1), is a  (1)
> **Code Identifiers:** priceincents (1)
> **Analogies:** just like (1)

#### [Navigating to different views with state](https://www.linkedin.com/learning/react-native-essential-training/navigating-to-different-views-with-state?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/navigating-to-different-views-with-state?u=76281980&t=0)** - [Instructor] I've made my styles. Here's how my list of deals looks like right now. So I've just added some margins around every deal and make it into a box, made the title of every deal a little bit bigger and bolder and align the cost and the price under the title in one row. You can check the [GitHub](../../Skills/Software%20Development/GitHub.md) repository for this movie to see the list of styles that I've used to do all that. Let's do the important part now and the challenging part. When we tap on a deal, we want to go to a different view that displays more details about every deal. This means we want to define a press event on very deal. And that's going to be for the whole deal. So if I press anywhere inside this box of the deal, it takes me to that deal detail view. So I'm going to start the deal detail view by duplicating this DealItem component. So duplicate DealItem as DealDetail. And the DealDetail will keep it exactly as is at first. We'll just rename the component into DealDetail. And I'll just put a placeholder text item in here to identify this as the details of a single deal. Inside the DealItem component, which is what's displaying this box in here, I want to make this whole thing a touchable component. So instead of View here, I'll use TouchableOpacity and I'll include TouchableOpacity
>
> **[1:35](https://www.linkedin.com/learning/react-native-essential-training/navigating-to-different-views-with-state?u=76281980&t=95)** from [React Native](../../Skills/Mobile%20Development/React%20Native.md), just like that and now, I can define an onPress event on this TouchableOpacity. So what's going to happen when I press on this deal box? Let's go ahead and put that inside a function in here. We'll call it handlePress. Format. And we'll define handlePress right here. handlePress is going to be a function that is going to do something. What is it going to do? We basically want React Native to rerender the view when we touch on a deal and render the DealDetail view, instead of the DealList. So rendering the DealList happen here in the top-level App component. If we have a list of deals, we are rendering the list of deals. Now we have another condition. We're going to have to put something on the state to make [React](../../Skills/Web%20Development/React.js.md) rerender itself and display a different view. So I'll call this currentDealId. And this starts with null. So basically, when I don't have a currentDealId, the view is going to just render the list. If I have a currentDealId, I'll make React render the DealDetail component for that dealId. So we can create a function right here. Call this function setCurrentDeal. This function will receive a dealId and it will change this state to make that dealId as the currentDealId. So we'll do a setState call and we'll just make the currentDealId the dealId
>
> **[3:11](https://www.linkedin.com/learning/react-native-essential-training/navigating-to-different-views-with-state?u=76281980&t=191)** that we are receiving as an argument to this function. You can simplify setState here as well to make it just an object. And now I need to task this setCurrentDeal down and flow it to the DealList component to define it as the behavior for every item press. So I'll call this onItemPress and we'll pass it as this.setCurrentDeal. So now the DealList component receive a nil property. We name that property onItemPress. This is a PropTypes.function.isRequired and we want to flow this behavior down to the DealItem component as well. So we'll call it onPress here and this would be this.props.onItemPress and we format. Perfect. Inside the DealItem component, this receives an onPress function. So it's a PropTypes.function also required and the handlePress is going to invoke this .props.onPress and call it with the ID of the deal, which is available from the API as this.props.deal.key. The ID of every deal is named as key in the API. So now to complete this feature in the App component, we need to add another conditional. And I'll refactor this turnary here to make it more clear. Basically, let's do multiple if statements here. If we have the current deal,
>
> **[4:45](https://www.linkedin.com/learning/react-native-essential-training/navigating-to-different-views-with-state?u=76281980&t=285)** so if this.state.currentDealId, if that exists, we're going to return the DealDetail view for that deal. And we'll need to figure out what to pass this component. But let me continue on these if statements. If I have a list of deals on this state, so if this.state.deals.length is greater than zero, then we'll go ahead and return what we're returning here, which is the DealList. And otherwise, we'll just return the text just like that. I don't need else statements here because I'm returning early from the render component. Okay, so what does this DealDetail component need to render itself? It needs a DealItem, so we'll need to figure out what deal to pass to the DealDetail in this case. So I'll go ahead and define that in a function as well. I'll call this function currentDeal. So currentDeal is going to figure out what deal to pass to the DealDetail component. currentDeal is a function. It doesn't receive anything. It will return the currentDeal that needs to be rendered in the DealDetail component. And it can do that by finding the deal from the list of deals that we have on this state. So it will return. We'll start from this.state.deals and we'll do a find call here. And basically, this find call is going to give me a deal every time
>
> **[6:21](https://www.linkedin.com/learning/react-native-essential-training/navigating-to-different-views-with-state?u=76281980&t=381)** and I would like to return if this deal.key equal to the current deal that we have on the state. So this.state.currentDealId. And if that condition is true, it will return that deal to be rendered in the DealDetail component. I think we can test. Let me just format everything and we need DealDetail. So let's import that. We're importing DealDetail from DealDetail. And let's test. Refresh. So the deals are still rendering, so this if statement is now the active one and when I press on a deal, I'm going to press on this website package deal. This if statement is going to take over and it will render that deal on its own using the new DealDetail component that we're going to modify now. Let me fix the styling a little bit here in DealDetail. We'll go with marginTop. Instead of 12, We'll do 50. This looks okay. So now, we need to make this DealDetail component render more information about this deal. And we'll need an [AJAX](../../Skills/Web%20Development/AJAX.md) request to go [Fetch](../../Skills/Web%20Development/Fetch.md) more information about that deal because the initial list of deals that we fetch doesn't have that information per deal. When we ask the API for information about a single deal, it will give us back the images and the title and the cost and the price,
>
> **[7:53](https://www.linkedin.com/learning/react-native-essential-training/navigating-to-different-views-with-state?u=76281980&t=473)** so all this information is going to come back from the API. But I don't want React to rerender this information. I already have that information. I'd like React to rerender the extra information that we fetch from the server. So to make that clear, in the App component, I'll call this property initialDealData because this is just part of the data that I'd like this component to render. And in the DealDetail, instead of rendering the deal from the props directly, I'll put the initial DealDetail on the state and then I'll make an Ajax call to change that state. So let's introduce a state object. And in here, we'll define the deal. This deal starts out as this.props.initialDealData. initialDealData is now the prop that I receive as the DealDetail component and instead of reading the deal from the props here, we want to read it out of the state, just like that. So now, this DealDetail component is ready to fetch more information for every deal, place that information right on the state and use that to rerender this extra information under the initial information that we have for every deal.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (4), [Fetch](../../Skills/Web%20Development/Fetch.md) (4), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (2), [AJAX](../../Skills/Web%20Development/AJAX.md) (2), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Code Identifiers:** currentdealid (7), onpress (4), handlepress (4), dealid (4), currentdeal (4)
> **CLI Commands:** make (11), find (2)
> **Env Vars:** api (4)
> **Analogies:** just like (3)
> **Definitions:** is a  (2)
> **Tools:** github (1)
> **UI Navigation:** go to (1)

#### [Fetching more API data for a view](https://www.linkedin.com/learning/react-native-essential-training/fetching-more-api-data-for-a-view?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/fetching-more-api-data-for-a-view?u=76281980&t=0)** - [Narrator] We wired the on press event for every deal, and now we can go to a deal detail view. It's now time to [Fetch](../../Skills/Web%20Development/Fetch.md) the extra details from the server. We're going to need another [AJAX](../../Skills/Web%20Development/AJAX.md) function. In Ajax.js, just like the fetch initial deals function, we now need another function. We'll call this function fetch deal detail. And this function is going to need the deal ID 'cause that's how we're going to use the API. We need a deal ID to ask the API server for information on one deal. So the code here is exactly the same, except that we want to do slash here and add the deal ID. And this should work to give us information on a single deal. By the way, I don't need these to be let variables, I'm going to change them all to just const. Whenever you can use const, you should. There's actually an [ESLint](../../Skills/Software%20Development/ESLint.md) rule for that. So if I undo this to let and go to my Eslint file, the rule is to prefer const and we'll do that as an error. So with this rule in this Ajax file, it's going to tell you use const instead of let. This is simply perfect, thank you, excellent. Okay, cool. Now we can test this function, fetch deal detail. So in the deal detailed component,
>
> **[1:33](https://www.linkedin.com/learning/react-native-essential-training/fetching-more-api-data-for-a-view?u=76281980&t=93)** we want to bring in the Ajax actions. So import Ajax from up one level Ajax, and we can fire this new fetch deal detail function when the component mounts, because eventually we want to place the information coming back from the fetch deal detail function on the state of the component. So we need component did mount, and we need to call Ajax.fetch deal detail. And in here pass the ID for the deal, which is on every deal is stored as the key, remember? So in here, we can do this.state.deal.key. This is an Ajax action, so we want to await on it, which means we want to make this into async and this will give me deal detail, so let's name this constant full deal. And we can const a log full deal to see what we're working with, but eventually we want to set the state and change the state deal to be full deal. Previous state is not used, so this can be simplified to the object syntax. Remember that the shared information between the initial deal data and the full deal data is going to stay the same with this set state call. So [react](../../Skills/Web%20Development/React.js.md) is only going to render the new information in here. So let's go ahead and test all that. Refresh, go in here. And now I have the full deal information in here.
>
> **[3:09](https://www.linkedin.com/learning/react-native-essential-training/fetching-more-api-data-for-a-view?u=76281980&t=189)** So we have a cause and charity, we have a description, we have a geo-location in case the deal is only offered in a particular place. And we also have user information, and the user information has an avatar for the user and the name of the user. So let's display at least the description and the user information. So I'll go ahead and do that. Get rid of this console log. And in here, we can now display the user information. So I'll place that in its own view. And in here, we need two things. We need an image component for the avatar of the user, so the source of this image component is URI is deal.user.avatar. Or we're going to need some special style for this image component. And we also need to display the user name. So we'll do something like deal.user.name. We need an equal sign in here, and let's figure out the style for the avatar. So styles.avatar. And if we test this URL that's coming from the avatar right here, just to see what we're working with. So it looks like we have an image that is 60 pixels by 60 pixels. So we'll make that the height and width for the avatar. So in here we'll do avatar is width, 60, height, 60.
>
> **[4:44](https://www.linkedin.com/learning/react-native-essential-training/fetching-more-api-data-for-a-view?u=76281980&t=284)** So I think this should do for the user. Let's also have another view for the description. So in here, we'll do a text and we'll do deal.description. Let's test, click on the deal and cannot read avatar of undefined. So this is happening because the initial data did not have a user property, and we're trying to read the user property on the initial data as well. By the way, this is the type of problem that a type checker tool like flow or TypeScript would detect for you. So you don't have to go actually see the error, the tool is going to tell you you're using.user.avatar, what about the case when.user is null. But I don't want to overwhelm the example, I want to try and keep things simple. The solution here is really simple. We can make this whole view conditional on the existence of the user property on the deal. So I'll just do that. I'll have a conditional here, then do something like deal.user and end this view. Format everything, there you go. So only show this view if there is a deal.user. Let's test, click on a deal and there you go, we've got the author or you've got the description. Before we move on and add another feature to this application, let's style this view a little bit and make it look decent. I'll do that off-camera.
>
> **[6:20](https://www.linkedin.com/learning/react-native-essential-training/fetching-more-api-data-for-a-view?u=76281980&t=380)** Here are the styles that I came up with, you can find them in the [GitHub](../../Skills/Software%20Development/GitHub.md) repository. By the way, if you want to inspect the styles, you can do so using the inspector here. Now, this inspector works in two mode. If the react dev tools is not running, it will show an overlay here and you can click on things to see their styles. For example, click on the description and you'll see what we have here, styles for the description. If the react dev tools is running, then clicking on any element with the inspector is going to highlight that element in the dev tools. And it will also show any styles that element has, right here. So you can see the styles that I added, for example, for this row, I made the flex direction row justify content spaces around a line item center, and then inside this view, I have the view for the information right here and the view for the user right here. And you can see every style applied to that element right here in the react dev tools, and this is really handy. The other feature that I used while coming up with these styles is the hot reloading. Let me show you what that means. So let's say you're in this view and you want to do some changes to the style. So if you only have the live reload, that means when you change something here, [React Native](../../Skills/Mobile%20Development/React%20Native.md) is going to reload the app.
>
> **[7:54](https://www.linkedin.com/learning/react-native-essential-training/fetching-more-api-data-for-a-view?u=76281980&t=474)** So for example, if I want to make the margin horizontal 15 instead of 12, and I save, react native is going to reload the app, but I lost my state, I am back to the main page, not the page where I was on. So that's not ideal. Instead, you can enable hot reloading, and hot reloading is going to reload the app without losing your state. So if I want to set this back to 12 and I save, react native is going to hot reload the app without losing the state, and this is very helpful. However, it doesn't work all the times, so sometimes you have to manually reload the app. But for most cases, it's really helpful when you want, for example, to test things without losing the state of the application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [AJAX](../../Skills/Web%20Development/AJAX.md) (8), [Fetch](../../Skills/Web%20Development/Fetch.md) (7), [React](../../Skills/Web%20Development/React.js.md) (4), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (3), [ESLint](../../Skills/Software%20Development/ESLint.md) (2)
> **CLI Commands:** make (5), find (1)
> **UI Navigation:** click on (4), go to (2)
> **Analogies:** for example (4), just like (1)
> **Env Vars:** api (2), uri (1), url (1)
> **Definitions:** is an  (1), is a  (1)
> **File Paths:** ajax.js (1)
> **Cross-References:** coming up (1)

#### [Implementing a back link](https://www.linkedin.com/learning/react-native-essential-training/implementing-a-back-link?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/implementing-a-back-link?u=76281980&t=0)** - [Instructor] We have a few major features to add to this application before we can call it done. The first thing I'd like to add is a search capability. So basically right now, the app just lists all the available deals, but I'd like to be able to search all the deals in the system. Now, this search is going to be server-side, not client-side. So I'm not just filtering the list that I have on the screen. So let's for example say I have 20 deals displayed on this list view. If I search, I'd like the server to search all the deals available in the system, and give me back a new list to display to the user. So let's start that task. We'll start with the markup. But before we wire this search, there's one thing we should do. When we press on a deal, we go to a new view that represents this deal. There's no way for the user to go back. So let's put a link here, to take the user back to the old deals list. So this link would be in the deal detail component, and I'll put it right here above the image, and I'll make this a custom link. Instead of using a button, I'll just use a TouchableOpacity component, and inside the TouchableOpacity component, I'll use a text, this text would be just "Back". So we need this TouchableOpacity component from [react](../../Skills/Web%20Development/React.js.md)-native, and this TouchableOpacity component needs an on-Press property. So what is going to happen when we press this TouchableOpacity component?
>
> **[1:34](https://www.linkedin.com/learning/react-native-essential-training/implementing-a-back-link?u=76281980&t=94)** Let's think about that. I'll use an empty function here just to render that and I have the Back in here. I need to fix the styles a little bit, but I do see this Back link. When I click on this Back link, what I want to do, is set the currentDealId to null 'cause when I'm on a deal view in here, the currentDealId is not null. If I set the currentDealId to null, then act is going to re-render, and take me back to the full list. So I can use setCurrentDeal, the exact same function here, and just use null in the dealId. But I like to have a more readable method. So I'm going to copy setCurrentDeal. Then I'm going to go with unsetCurrentDeal. This is a function that doesn't take any dealId, and it will just do null here. So this unsetCurrentDeal, is going to be needed by the DealDetail view. So let's pass it there. From the perspective of the DealDetail, we can call this onBack and we'll pass it here as this, the unsetCurrentDeal, as a function reference. Let me format this, perfect. So DealDetail has an InitialDealData and an onBack function. So let's go to the DealDetail now, and we'll define the type for this onBack function, it is a PropTypes that function, and it's required. And now, under the onPress for the TouchableOpacity for the back button, we can use,
>
> **[3:07](https://www.linkedin.com/learning/react-native-essential-training/implementing-a-back-link?u=76281980&t=187)** this.props.onBack, let's test that. Click on here, click back, and that took me back to the main list. Click on here, click back, that took me back to the main list. So let me fix the style for this back link real quick. So instead of having the border here, on the deal itself, I'll just have it on the detail view in here, and let's give the back button some style. So style is, styles.backlink. And as you can see, I have hot reloading, so anything that I do here is going to be reloaded. So, backlink, let's give it a marginBottom of five, take a look at that. And I'll also, I'll change the color into something blue. So we'll go with 22f. Perfect. This looks good.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (1)
> **Code Identifiers:** onback (4), currentdealid (3), unsetcurrentdeal (3), setcurrentdeal (2), dealid (2)
> **UI Navigation:** click on (3), go to (2)
> **Definitions:** is a  (3)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Using TextInput to implement search](https://www.linkedin.com/learning/react-native-essential-training/using-textinput-to-implement-search?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/react-native-essential-training/using-textinput-to-implement-search?u=76281980&t=1)** - [Instructor] So let's now think about the search feature. We'll start with the market. Let's add a search text input in here above all the deals. So that would be in the app component in here. So if I am rendering a deal list, I'd like the system to be able to give me a search feature on that. I don't need the search feature if I'm on a deal detaile view, I just need it when I'm on a deal list view. So I'm going to put this inside view, and I'll add a text input. Now we can add the text input right here directly and start working with that. But this search bar is going to be a controlled component. And in fact, we should debounce the search so that the search fires immediately when we type, but not on every character, and this way we don't have to have a submit search button. So to isolate all this features, we'll put them in their own components. I'm going to call this component search bar and go implement it. So we'll add search bar under components here. This is a search bar dot JS, and this is a [React](../../Skills/Web%20Development/React.js.md) class component search bar. And in here we'll do the text input. In the app component, I need to import this search bar. So import search bar from dot slash search bar, perfect. So in this search bar, we can have the text input from [React Native](../../Skills/Mobile%20Development/React%20Native.md). So I'll go with that. I need to import that from React Native.
>
> **[1:35](https://www.linkedin.com/learning/react-native-essential-training/using-textinput-to-implement-search?u=76281980&t=95)** So import text input, React Native. So in the text input, let's do a few things. Let's first give it a style, so I'll call this styles dot input, and we'll import style sheet from React Native, and we'll create a styles object from that. So styleSheet.create, and let's give some styles for the input text. So let's go with height 40 and test all that. Let me format, and let me refresh. So we have the text input. However, it seems like I lost the deals. So let's figure out what is going on. This looks like a styling problem. Let me try to fix it real quick. This deal list has a padding top in here, and it has a flex one. I'm going to kill that for now. So I see the deal list in here, and the search bar is right here. So let me actually put a placeholder text in here to see what's going on with the search bar. So let's go with place holder. Let's go with something like search all deals, format. So now if you can see, I see the search bar right here, but I need to avoid the phone controls. So let's do that, let's give this style. I'll just go with styles.main in here. And actually I will do the same for the deal detail
>
> **[3:12](https://www.linkedin.com/learning/react-native-essential-training/using-textinput-to-implement-search?u=76281980&t=192)** so that we always have consistent margins from above. So in here we'll have a main, and this main we'll only have margin top of 50, so let's make sure that works, perfect. 50 sounds too much, so we'll go with 30, perfect. Now when I click on a deal, the margin top now is too much, so we'll just get rid of this margin top in here, because now it's coming from the main app. Very good. So now I have a margin top in here, and I have this search all deals text button. Very good. Let's actually give this one too some margin horizontal here, let's go with 12, and this is looking decent. So let's go ahead and capture the search term. And we can do that by making this search bar a controlled component. Basically we place something on the state. Let's call that search term. This search term begins as an empty string and we'll wire this text input with an unchange text event. We'll call this handler this.handleChange. And handle change is simple. Basically it is a function that is going to receive the actual text that the user types. So we'll call that search term, and it will set the state with that search term.
>
> **[4:48](https://www.linkedin.com/learning/react-native-essential-training/using-textinput-to-implement-search?u=76281980&t=288)** So it puts the search term right on the state. So let's test all that, and we can do so with React Dev Tools. So now I have a search bar component right here, and this one has a state. And when I start typing, I see the information right on the state of the search bar component, very good. So let's now think about the actual search operation. We're going to need an [AJAX](../../Skills/Web%20Development/AJAX.md) call. So let's get that out of the way. So let's call this new function fetchDealsSearchResults. And this function is going to depend on a search term. And the API is simple, it is basically slash deals and we specify a search term just like that equal, and then we'll have the search term in here. Everything else is the same. So we'll go to slash API slash deals, give it a search term, and it will give us back some listings from the database that match that search term, very good. So we can now fire this [Fetch](../../Skills/Web%20Development/Fetch.md) search results anywhere we want, so let's think about that. The app component will have a list of the initial deals, and if we have initial deals, it will display those deals. However, if we have search results, we want to display the search results rather than displaying the list of deals. So we'll have to introduce a new array here for the search results of the deals.
>
> **[6:22](https://www.linkedin.com/learning/react-native-essential-training/using-textinput-to-implement-search?u=76281980&t=382)** And the reason we want to display a new array rather than replacing the deals that we depend on, is that if we undo the search, we want to go back to the same view, and we don't need to go back to the server to fetch more deals. So instead of replacing the deals on the state, when we do this search, we'll just introduce a new array, call this array deals from search. This will start as an empty array, and we'll create a function here to do the search basically. So we'll call this function searchDeals. This function is going to call the fetchDealsSearchResults, which is an AJAX operation. So you want to do AJAX dot that will fetch that with a search term. This means the search deals here is going to need to receive the search term as well. And this is an async operation. So we want to await on that, and make search deals itself an async function. And the result of calling this function is going to be the deals from search. So let's put that in a constant deals from search, just like that. And once we have it, we want to just set the state to save the deals from search. So when I call this search deals function, it will go and fetch from the database and then come back and set the results on the state of this component. We also need another function to clear the search. So let's think about that as well.
>
> **[7:56](https://www.linkedin.com/learning/react-native-essential-training/using-textinput-to-implement-search?u=76281980&t=476)** This is a very simple function that would set the state. Basically it will set deals from search back to an empty array. We can actually combine these two functions for simplicity. So I'm going to go ahead and do that. Let's make this deals from search into a variable like that, to start in an empty array, and then we'll do an if statement. If there is a search term, then we want to change the deals from search, deals from search to be that, and then do set state. This way to clear the search, I can just call search deals with an empty search term. Very good. So let's go ahead and pass this search deals into the component that it needs that, which is the search bar. So I'll pass it here with the same name. So search deals is this dot search deals. And inside search bar, we need to define this new property that we just passed to it. So static prop types equal, we've got search deals, which is a propTypes.function.isRequired, and I need prop types. And now what I want to do is, after setting the state for that search term, I'd like to perform the search operation. But remember, I don't want to perform the search operation
>
> **[9:30](https://www.linkedin.com/learning/react-native-essential-training/using-textinput-to-implement-search?u=76281980&t=570)** on every single character I type, so we should debounce this action. To do an operation right after a set state operation, we can use the second argument for set state, which is a callback that React is going to invoke after the set state operation is done. So in here we can debounce the operation for searching. Debounce is something that we can use from Lodash. So I'll go ahead and import from Lodash, we'll import debounce from lodash.debounce. This way, I'm only importing the debounce function and not the whole Lodash library. And I'll create a new function here, call this function debouncedSearchDeals. And this is basically a debounce call for this.props.searchDeals. And we'll debounce it with 300 millisecond delay, just like that. Now in here, I can just call the debounced search deal. So this dot search deals and pass this dot state dot search term, so that the search happens in the main component. So I think we can test all that. However, we need [npm](../../Skills/Web%20Development/npm.md) install lodash dot debounce.
>
> **[10:54](https://www.linkedin.com/learning/react-native-essential-training/using-textinput-to-implement-search?u=76281980&t=654)** And I'll test with the React Dev Tools open. I'm going to highlight the app component to see the deals right now. The deals from search is an empty here. And now let's search. We'll search for yoga, and look at that. I have deals from search has two listing only that matches the yoga search. Very good. Now, all I need to do is, if I have deals from search, display those instead of displaying the original deals array. This is easy, in the app component, we'll create a new variable called this deals to display, and it will basically be this.state.deals from search. If the length of this array is greater than zero, then we'll just place deals from search as the deals to display, otherwise we'll do just deals. So let me format all that. So if we have deals from search on the array, then the deals to display are those deals from search, otherwise we're just displaying the deals. And any here, we'll replace this.state.deals with just deals to display. And check it out, reloading work, now I have deals to display. What happens if I delete this? I see all the deals. Let me test all that one more time. Yoga, two deals, delete all the deals.
>
> **[12:29](https://www.linkedin.com/learning/react-native-essential-training/using-textinput-to-implement-search?u=76281980&t=749)** Website, there you go. And this search is actually case insensitive because the API that we use supports that. One thing that we should also test is the debounce feature. So the expectation is that when I type a [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md), only one network call should be made through the API. And we can validate that using the inspector here, this inspector has a network tab. So go ahead and click that. React Dev Tools has to be closed for this network tab to work. But once it's working, you'll start seeing the network calls. These are [WebSocket](../../Skills/Web%20Development/WebSocket.md) calls, don't worry about those. However, when we type something here in the search, you should see a single hit through the API right here. And when I deleted that search, known work calls were made over the wire, looking good. So I believe we reached the MVP status for this app. However, there are still a lot of improvements that we can do, especially with animation and gesture recognition. So in the next chapter, we will explore those.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (5), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (4), [Fetch](../../Skills/Web%20Development/Fetch.md) (4), [AJAX](../../Skills/Web%20Development/AJAX.md) (3), [npm](../../Skills/Web%20Development/npm.md) (1)
> **Env Vars:** api (5), ajax (3), npm (1), mvp (1)
> **Definitions:** is a  (7), is an  (3)
> **Code Identifiers:** fetchdealssearchresults (2), searchdeals (2), stylesheet (1), handlechange (1), proptypes (1)
> **CLI Commands:** make (3), npm (1)
> **Cross-References:** go back to (2), in the next (1)
> **Analogies:** just like (3)
> **UI Navigation:** click on (1), go to (1)


### 5. Animation and Gesture Control

[↑ Back to Table of Contents](#table-of-contents)

#### [Preparing for swiping](https://www.linkedin.com/learning/react-native-essential-training/preparing-for-swiping?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/preparing-for-swiping?u=76281980&t=0)** - [Tutor] Over the next few videos we're going to implement a few improvements to our app using animation and gesture control. The most important feature is to allow the user to browse through the multiple images that are available on some deals. The bake sale API already exposes an array of images for us. We have so far only used the first image. So let's give users the ability to swipe left and right while they are on the deal detail view to see the different images attached to a deal. We can use [React Native](../../Skills/Mobile%20Development/React%20Native.md) built-in gestures and animations to do that. To prepare for this swiping animation, I'm going to make the deal detail view full screen here, get rid of these borders. So in here, let's get rid of the marginHorizontal and let's get rid of the borders everywhere. See how that looks. This is looking better, but let's give the backLink some marginLeft. Maybe 10? Perfect. So this way when I start swiping the images it is edge-to-edge, which I think is going to look better. In the next video, we'll explore the animated API.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1)
> **Code Identifiers:** marginhorizontal (1), backlink (1), marginleft (1)
> **Env Vars:** api (2)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [tutor] (1)

#### [The Animated and Easing APIs](https://www.linkedin.com/learning/react-native-essential-training/the-animated-and-easing-apis?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/the-animated-and-easing-apis?u=76281980&t=0)** - Let's first understand how to do simple animations without any gestures. Animations give some nice visual polish for your app. It's not a required thing, but it does make the app feel truly Native. Let's think about a simple thing that we can animate in our app. There're many things that could use some animation. For example, we do the transitions between screens without any animations so far. Usually, Native apps use animated transitions. That's one thing we can do here. But I'll go with the simpler example and animate the initial Bakesale header [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) that we displayed while waiting on the initial deals to arrive from the server. Let me actually comment out the [AJAX](../../Skills/Web%20Development/AJAX.md) call right here, refresh, so this is the initial view, so maybe the server gets loaded and that Ajax call to [Fetch](../../Skills/Web%20Development/Fetch.md) the initial fields takes a few seconds. It would be cool to show some kind of animation on the screen here. Let's just bounce the Bakesale word back and forth horizontally between the edges of the screen. I think that would look cool. The Animated library that comes with [React Native](../../Skills/Mobile%20Development/React%20Native.md) has an excellent API to declaratively define powerful on-screen animations. The word declaratively in the previous sentence is important. And it means basically that we don't have to worry about how the animation is carried out under the hood. Which is not an easy thing, especially in React Native because the animation has to happen natively and not not through any state within the [JavaScript](../../Skills/Software%20Development/JavaScript.md) thread. Thankfully, the Animated API will take care of the how, and all we need to specify is the What.
>
> **[1:37](https://www.linkedin.com/learning/react-native-essential-training/the-animated-and-easing-apis?u=76281980&t=97)** What to animate and What motion effects and durations to use The important thing to remember here is that when we animate something with the Animated library, we will not be causing [React](../../Skills/Web%20Development/React.js.md) Render method to be called repeatedly to perform that animation. The animation will happen on the UI thread behind the React Native bridge. Only defining the animation happens in the JavaScript thread. There are a lot of things to learn about the Animated API. We can spend the full course on just that, but let me go over the basics. First, we need a value to animate. For example, in the Bouncing Animation that we are planning to do, the value to animate, would be the X position for the Bakesale word. But the value to animate can be anything, you can animate a color, a size or an angle for example. We also need the value to transition to, so we will be going from one value to another value over some period of time. If for example, the X position starts with 0, we can animate the transition to say, 100, over a period of 1 second, and that would move the word 100 pixels on the screen during that one second. We can also control the speed and acceleration of the animation. This is known as the Easing of the motion and React Native has built in functions to use for many Easing types. To create an animated value, we can use Animated.value() for a single value, or Animated.valueXY(),
>
> **[3:13](https://www.linkedin.com/learning/react-native-essential-training/the-animated-and-easing-apis?u=76281980&t=193)** if we want to animate 2 values together. For our example, we just need to animate a single value. There're 3 main animation types that we can use with Animated. Timing, is the popular one, probably because it's very configurable with the Easing functions. We can only animate these special components here. So if we need to animate an image for example, we would need to replace the regular image component with this Animated.Image component. The Animated API is one of those APIs that you can only understand by example. There are a few very good resources out there about it. On top of that list is an article by my friend, Nader here who is a very experienced React Native developer. This is one of his many articles on Medium that explains React Native concepts pretty well. Check the Animated examples he presented here to get some more confidence about the API. Are you ready to do our own example and see how to use the Animated API to implement the Bouncing Effect for the app loading screen? We are going to do that, in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (6), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (5), [AJAX](../../Skills/Web%20Development/AJAX.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Env Vars:** api (6)
> **Analogies:** for example (5)
> **Definitions:** known as (1), is an  (1), is a  (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** valuexy (1)
> **Cross-References:** in the next (1)
> **Speakers:** - let (1)

#### [Creating an animation loop](https://www.linkedin.com/learning/react-native-essential-training/creating-an-animation-loop?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/react-native-essential-training/creating-an-animation-loop?u=76281980&t=1)** - [Instructor] To accomplish the bouncing animation effect that we're planning, we need to control the exposition of this bakesale [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md). So right now we are rendering this word with a simple text inside of you. You can actually animate both of these components. Usually we animate the containing view. So to animate this view, we need to change the regular component into animated.view, just like that. And of course, I need to import animated from [React Native](../../Skills/Mobile%20Development/React%20Native.md). So this is the very first step, animated from react native. And you would change your component to an animated version of that. Next, you need the value that is to be animated which is the exposition of this word. The value to be animated has to be passed to the style of the animated component, but we need to define it first. Since this is related to the component in here, we can define it on the instance of this component. So let's call it title X position. And since it's just a single value, we can use new Animated.Value, just like that, and we'll make it start from zero. When working with positions, animated.value zero once given to an animated component would represent the position where the component is located currently. And then any value that we transitioned this position to is going to be an offset from that zero initial value. Remember that this position
>
> **[1:32](https://www.linkedin.com/learning/react-native-essential-training/creating-an-animation-loop?u=76281980&t=92)** will not represent an absolute value, but rather a relative one. So let's give this position to the animated component. So in here, we already have the style containers. I'll convert this into an array so that we can add more styles. And the new style that I want to add is basically the exposition. I will use it as the left property for this view. So the left would be this.title X position, just like that. So when we define the animation and the animation starts happening, the animated view component is going to control its left position. And that can happen in any of the two directions. Remember, this current left position is the zero mark which is where the left of the bakesale word is currently at. So when we do the animation, if we reset the left to zero, that would center the bakesale word on the screen, because that is the left where it started. That's the zero marker. Very good. So now we can animate. So let's first test an animation of left from zero to say 100, and I would expect the word to move 100 pixels on the screen. We can do that in component did Mount. We need the word to exist in the UI first. I'm going to first use the spring animation type to show you how that works. So basically we do animated.spring. This method takes two arguments.
>
> **[3:04](https://www.linkedin.com/learning/react-native-essential-training/creating-an-animation-loop?u=76281980&t=184)** The first argument is what to animate. So we want to animate this.title X position. That's the value we're interested in animating. And in the second argument, you give a configuration object. There are plenty of things in the API that you can configure but the important property here is the to value. Where do you want the position to be animated? So let's animate it from zero to 100. So this call defines the animation but it doesn't really start the animation. To start the animation, you can chain a call for start, just like that. So define the animation and start the animation. So we can test this out, refresh and did you notice how the bakesale word springs to 100 of the screen just like that. To make for a balancing effect back and forth we can start a new animation after this one is done and basically animate the position to minus 100, for example. However, the animation calls are all asynchronous, so we can't start the new animation right here after the first animation. We'll have to do it through a callback. The start method exposes this callback and this callback is going to be invoked once the animation is done, once the value reaches 100. So we can basically do another call here to animated.spring and we also need the .start here as well. And we'll animate the value to minus 100
>
> **[4:38](https://www.linkedin.com/learning/react-native-essential-training/creating-an-animation-loop?u=76281980&t=278)** and let's test that out. Refresh. Did you see that? One more time. Excellent. So to accomplish our balancing operation, we can just put that in a loop. We'll put this call here in its own function, call this function animate title. So this is a function and this function will do the animated.spring call. And now since we have a function, we can just make the function call itself. So basically in the callback here, once the first animation is done we can call this dot animate title again and to flip the direction of the animation we can have the animate title except a direction here and this direction would be default to one. So in our case, the first direction would be to the right. And in here, we can flip this direction by saying minus one times the current direction that we have. So this way, every time we flip the direction and we just use the direction here instead of minus one, we can do direction times 100. So the first time we animate, it will animate to 100. When that's done, it will animate to minus 100 and it will go into a loop of doing that. So now on component did mount, we need to start the animation. So this.animate title, and by default, it will start to the right Let's test. Back and forth,
>
> **[6:11](https://www.linkedin.com/learning/react-native-essential-training/creating-an-animation-loop?u=76281980&t=371)** we have animation. So I hope this wasn't too hard. It will get a little bit harder when we combine animation with gesture recognition. In the next video we are going to talk about the screen dimensions and the different easing functions that we can use with timed animation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (8), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (2)
> **Analogies:** just like (5), for example (1)
> **CLI Commands:** make (3)
> **Env Vars:** api (1)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [Considering the device dimensions](https://www.linkedin.com/learning/react-native-essential-training/considering-the-device-dimensions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/considering-the-device-dimensions?u=76281980&t=0)** - [Instructor] We accomplished the desired bouncing animation that we wanted using the spring animation type. But I like it to be faster than that. If you notice the spring animation takes some time to be done and the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) sits on the edge for a while. So if we need more control over that, we can use a different type of animation. We can use the timing animation. Let's check what the default for the timing animation look like just without any configuration. So now it's actually going too fast, but there is no weight on the edge, which is excellent. So we can control this a little bit further. We can change the duration. So instead of making it go really fast, let's make the duration one second. So this means don't move too fast. Take one full second to go from zero to 100. So let's test that out. So now, this is much better. It takes one full second to go to the edge. You see that? Now between zero and 100 [React Native](../../Skills/Mobile%20Development/React%20Native.md) would use some kind of easing function to define how to transition from zero to 100. For example, there is a linear transition. There are some transitions where you can start fast, go slow and fast or start slow, go fast and slow. These are the different easing functions that we have. So let me show you some of those. We'll need to import easing from React Native, and we need to specify an easing here in the animated configuration. Let me format that. And then here we can specify easing as easing dot one of the functions.
>
> **[1:37](https://www.linkedin.com/learning/react-native-essential-training/considering-the-device-dimensions?u=76281980&t=97)** For example, I think the default is just ease. This is what we're using right now. Let's check that out. You can do something like bounce, for example. So this would define a different motion. So now, it is bouncing on the edge as you can see. The simplest easing is linear. So just go from zero to one in a linear fashion, without any movement up and down. And you can actually combine easings if you want to make for complicated motions. I kind of like the default motion, 'cause as you can notice, it goes slow, fast, and then slow. Which I think is appropriate for this particular animation. We hard-coded this hundred pixels on the screen and it's working for us in this particular device. But if we get a bigger device or a smaller device, that hundred might not be appropriate. We can actually see this problem if we rotate the device. We can go to [Hardware](../../Topics/Hardware.md) here and change the orientation to landscape right. And now you can see the problem. The word is not really bouncing between the edges, but rather just 100 pixels on the screen. So how do we fix this problem? How do we make this animation appropriate for any screen size? We can use the dimensions library. React Native has a dimensions library. We can import that, dimensions, just like that. And this dimensions library can be used to read the width of the screen.
>
> **[3:11](https://www.linkedin.com/learning/react-native-essential-training/considering-the-device-dimensions?u=76281980&t=191)** This is how we do it. You do dimensions.get window.width. This will give me the width of the screen. And of course you can read the height of the screen if you need to. So every time we animate the title, we're going to read the width of the screen. Let's store that in a constant. Call this width, just like that. And instead of 100, we can make this number relevant to the width. For example, we can try the width value itself and see how that looks. It's now going beyond the edges of the screen. We actually need half the width. So let's see how that looks. So this is a little bit better but it is still going a little bit beyond the edge of the screen. And this is because we're not accounting for the width of the word itself. So let's try to subtract the value from the words to account for that. Test that, better. But still maybe 150, let's test that. So now it is perfectly going from one edge to the other. And now if we change the orientation even while this animation is happening, the two value is going to adjust itself to the appropriate edges of this particular screen. So this could, will now work on any screen size. So now we can put back the [AJAX](../../Skills/Web%20Development/AJAX.md) fetching and that would make this animation only happening
>
> **[4:45](https://www.linkedin.com/learning/react-native-essential-training/considering-the-device-dimensions?u=76281980&t=285)** while we're loading the initial deets. We have a small problem in this code that we need to solve before moving on. Basically, this is an infinite loop. We did not stop this loop anywhere. So we need an exit condition for this loop. We can see the problem by maybe console logging something here and checking how this is going to be fired indefinitely. So in the developer here, you'll notice that there is a constant log line that is going on and on and on and on. So how do we solve this problem? The animated view was actually unmounted but our code does not account for that because it is animating just a value. However, this [stark](../../Skills/Web%20Development/Stark%20(Accessibility%20Software).md) callback here receives an object and this object has a property named finished. So I'm going to destructure this finished property from this object. And basically, if the animation was successful, the finished property is going to be true. But if the animation was interrupted for any reason, this finished will be false. So I'm mounting the animated component will cause the animation to be interrupted. So all we need to do to stop the infinite loop is an if statement here that basically checks on the finished attribute. If the previous animation was finished successfully, go ahead and start new animation. But if the previous animation did not finish successfully, it will not start a new animation.
>
> **[6:20](https://www.linkedin.com/learning/react-native-essential-training/considering-the-device-dimensions?u=76281980&t=380)** So let me go ahead and format everything. And now we can test. You will not see the console log going on and on because the animation was stopped. So let me clear this console log line. What we need to understand next to implement the swiping feature is how to work with gestures. React Native has a built-in API for recognizing touch gestures. It's called PanResponder. We'll explore that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (4), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (3), [Hardware](../../Topics/Hardware.md) (1), [AJAX](../../Skills/Web%20Development/AJAX.md) (1), [Stark](../../Skills/Web%20Development/Stark%20(Accessibility%20Software).md) (1)
> **Analogies:** for example (4), just like (2), kind of like (1)
> **CLI Commands:** make (5)
> **Definitions:** is a  (3), is an  (2)
> **Env Vars:** ajax (1), api (1)
> **UI Navigation:** go to (2)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [The PanResponder API](https://www.linkedin.com/learning/react-native-essential-training/the-panresponder-api?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/the-panresponder-api?u=76281980&t=0)** - [Instructor] [React Native](../../Skills/Mobile%20Development/React%20Native.md) has a built in gesture recognizing system called the PanResponder. We can use it to detect when the screen is being touched and released, and we can read the accumulated distance between the first touch and the release. So it's not just when the screen is touched, but also where the touch gesture is going and where it ended. So we can easily implement features like drag and drop or swipe left and right with this API. We can also recognize multitouch features like pinch, [Zoom](../../Skills/Software%20Development/Zoom.md), or shrink. Our example will be a simple one. We want to recognize these swipe left and right gesture and do something when they are detected. One thing to understand is that the PanResponder itself does not make any changes to the UI. It is used to only detect fingers on screen movements. This is why the PanResponder is usually used with the Animated API so that we can do actual changes to the UI in response to detected touch gestures. In our example, if we detect a swipe gesture, we want to animate one image out of the screen and animate another image into the screen. We define handlers for a PanResponder object. They are called panhandlers. These are things like onResponderMove, onResponderRelease. These handlers need to be passed to the UI element that need to respond to touch gestures, which is the deal image in our case. The handlers expose two objects.
>
> **[1:33](https://www.linkedin.com/learning/react-native-essential-training/the-panresponder-api?u=76281980&t=93)** Event is the Native touch event, which can give us absolute locations on the screen of where the touching finger is. This is often not very useful. The more useful object is the gesture state, which exposes many properties like move X and Y, and more importantly, DX and Y. The DXY properties hold the accumulated distance of the gesture since the touch started. This is going to be useful in our example to detect this swipe gesture. Let's take a quick look at the PanResponder API. You can create a PanResponder object with the create method, which takes that configuration object that defines the methods. I think these methods are unnecessarily verbose, but that's what we have to work with. You can override any of these methods if you need to. For example, the onPanResponderGrant method can be used to show a visual feedback that a touch gesture has started, so we can, for example, highlight the area that was touched. OnPanResponderTerminate can be used to detect if a gesture was canceled. However, for our example, we'll keep things simple. We need the onStartShouldSetPanResponder to be a function that returns true. This basically activates the responder. We need the onPanResponderMove, which is the core detector of the swipe gesture. When the touching fingers starts moving on the screen, the user basically is swiping in one direction. This handler will give us the distance traveled in that direction. So in our example, when this handler is called,
>
> **[3:08](https://www.linkedin.com/learning/react-native-essential-training/the-panresponder-api?u=76281980&t=188)** we want to move the image that is being swiped with animation. And finally, we need the onPanResponderRelease to decide if the swipe was far enough to actually animate the image out of the screen. I usually go with 40% of the width of the device. If the image is wiped 40% or more in one direction, we will need to continue the animation all the way when the swiping finger is released. We'll use this handler to accomplish that. The RN tester project has an example for the PanResponder API if you want to see it in action. But we're going to do our own example next step by step. We'll do that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1), [Zoom](../../Skills/Software%20Development/Zoom.md) (1)
> **Code Identifiers:** onrespondermove (1), onresponderrelease (1), onpanrespondergrant (1), onstartshouldsetpanresponder (1), onpanrespondermove (1)
> **Env Vars:** api (4), dxy (1)
> **Analogies:** for example (2)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **UI Navigation:** drag and drop (1)
> **Definitions:** is called (1)

#### [Preparing the PanHandlers](https://www.linkedin.com/learning/react-native-essential-training/preparing-the-panhandlers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/preparing-the-panhandlers?u=76281980&t=0)** - [Instructor] The first thing we need to do to implement the swiping of the deal images feature is to make the active image index part of the component state because we want to change the image index and have the image changes. So let's do that first. I'll name it image index. And this starts from zero and in here, instead of hard coding the first element here we'll read the image from this.state.image index. Let me format everything. So this should not really change anything, except now we have the ability of changing the image index and that would change the image. In fact, we could test that from the developer tool. So I'll look for the detail view, go in here, change the image index to one and see the image changes. The next thing we can do is to define the PanResponder. So from [React Native](../../Skills/Mobile%20Development/React%20Native.md) we'll import PanResponder. We'll also import Animated 'cause we know we need to use Animated as well. So we have PanResponder and Animated and we'll create a PanResponder object. I'm going to name it ImagePanResponder. This is a PanResponder.create call and it receives a configuration object. So we place this ImagePanResponder on the component instance itself. This is just a preparation step. We still have to pass the handlers to the image itself but let's include these handlers
>
> **[1:34](https://www.linkedin.com/learning/react-native-essential-training/preparing-the-panhandlers?u=76281980&t=94)** that we are interested in. The first one is the onStartShouldSetPanResponder. So we'll include that in here. This is a function and I just want to make it return true and make this PanResponder active. We also need the onPanResponderMove. The onPanResponderMove is a function that receives the event and the gesture state. And for now let's just console log moving just like that. We also need the onPanResponderRelease. It's also a function that receives the event and the gesture state and let's console log released. Very good. So this PanResponder is now usable. I'll go ahead and pass it to our image in here. What we need to pass is this.imagePanResponder.panhandlers. And this is an object that has all the panhandlers. So we're going to spread this object into this image component. So here's, what's going to happen. I'm going to try and swipe this image. Now we did not do any animation yet. So nothing is going to change on the screen, but we will see the console log lines when I move my cursor on this image and when I release my cursor from that image. So check it moving, moving, moving released. And it will also work on the other direction, moving, moving, moving, released.
>
> **[3:09](https://www.linkedin.com/learning/react-native-essential-training/preparing-the-panhandlers?u=76281980&t=189)** So while the user's finger is moving we get this onPanResponderMove. And this actually gives us the DX in this particular case, that is super important. So let's check that out. Now, when I touch and move my finger to the left, I get the measurement in pixels, as you can see. And it's in the negative when we go left. It will be in the positive when we go right, just like that. So we're ready to start animating this image as we detect this swipe left or right gesture. We'll do that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** cursor (2), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1)
> **Code Identifiers:** onpanrespondermove (3), onstartshouldsetpanresponder (1), onpanresponderrelease (1), imagepanresponder (1)
> **Definitions:** is a  (3), is an  (1)
> **CLI Commands:** make (3)
> **Analogies:** just like (2)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Swiping left and right](https://www.linkedin.com/learning/react-native-essential-training/swiping-left-and-right?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/swiping-left-and-right?u=76281980&t=0)** - [Instructor] While the user is swiping an image, the usual behavior of swiping is that the X position of the image is going to change in the same direction that we are swiping to. To make that happen, we can use the animated library. So I'll basically change the image component here to be an animated image, animated.image and we need to create an animated value. So let's do that right here before the image responder, we'll call this value image X position just like the title X position. So this is going to be a new animated value. It's a single value. We don't need an X and Y and it starts from zero. Now that we have an animated value, we can use it as the left property on the animated image component. So we already have a style for the image. I'll change that into an array, and we'll add a new object here. This object will have the left property to be this .image X position, just like that. So we now have an animated value and it controls the left style property for an animated image. So now all we need to do to make the image actually move is inside the on pan responder move, we need to set the value for this animated value. So we can do something like this.image X position.set value, we can directly use the DX value
>
> **[1:38](https://www.linkedin.com/learning/react-native-essential-training/swiping-left-and-right?u=76281980&t=98)** that is coming from the move gesture. So now, while the finger is moving on this green this image X position animated value is being set to the distance that's measured from the start of the touch. So I think we can test that. Now when I touch and move the image it's position is going to change and I can touch and move it back in here, just like that. So now when we release the gesture on a state like that we should just continue animating the image off the screen and we can do that using the animated library as well. So in here on pan responder release we're going to need some kind of condition. This condition is going to decide whether to animate the image completely off the screen, or to reset the image back to its zero position in case the swipe wasn't long enough. So this condition will definitely depend on the width of the screen. And as you remember, we used the dimensions library to get to the width of the screen. So I'll copy that. We'll probably need the actual width of the screen. So I'm not going to do minus 150 here. And the condition is if the GS.DX, which is the distance, if that is less than, say, minus one times the width times 0.4. So it is all in the negative when we swipe left and this is the first thing that we were going to do. So if the DX is less than 40% of the width in the minus,
>
> **[3:14](https://www.linkedin.com/learning/react-native-essential-training/swiping-left-and-right?u=76281980&t=194)** then that means swipe left which means I have to complete the animation to swipe the image completely out of the screen. And we can do that using the animated library as well. So animated dot I'll go with timing here. We are animating this .image.X position, and we're animating this to a value of minus one times the width of the screen. And this has to be in the negative again, because we are doing swipe left. Let's give this a duration of say 250 milliseconds. We need to import dimensions from [React Native](../../Skills/Mobile%20Development/React%20Native.md). And I think we can test that. So let's go ahead and try, swipe, release. That did not work because I forgot to start the animation. So we need to start the animation, retesting, swipe left and it went off the screen completely. Of course, I don't have an image now so we'll need to do something after the swipe gesture is complete. But before we do that, let's implement the swipe right gesture. The swipe right would be another simple if statement here that will be basically exactly the same as this one, but without the negative sign. If the X is greater than positive width times 0.4 then we want to animate this to a value of width. So let's do this in the same if statement with some math. So let's go with math.absolute value for the DX.
>
> **[4:51](https://www.linkedin.com/learning/react-native-essential-training/swiping-left-and-right?u=76281980&t=291)** If that is greater than the positive width then it's going to be either swipe left or right. We need to decide if the swipe is left or right. And we can use the sign of the X to do that. So I'll put this in a constant named direction and that direction is the sign using math.sign for GS.DX. So if this is minus one that would be a swipe left and plus one for swipe right. This would allow me to do the timing in either direction using direction times width. So for swiping left we want to animate the position all the way to the left. And if we're swiping right, we want to animate it all the way to the right. We can test that. Swipe left still works. And now swipe right is going to work as well. Very good. The swipe detection and animation is working now but once a single swipe is successful we need to update this component to show the next image. And we need to animate the entrance of the next image from the opposite side of where we swiped the previous one. We'll figure that out in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1)
> **Analogies:** just like (3)
> **CLI Commands:** make (2)
> **Versions:** 0.4 (2)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Handling swipe gestures](https://www.linkedin.com/learning/react-native-essential-training/handling-swipe-gestures?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/handling-swipe-gestures?u=76281980&t=0)** - [Instructor] We are swiping left and right, but we now need to handle the swipe. What is going to happen when a swipe is complete? And if you remember, we can do operations after an animation is complete using the callback argument for the start method. So in here, this is the function we're going to handle the swipe. So I'll go ahead and write a new function for handling swipe. We'll call this function handleSwipe, and I'll just call it here. And eventually the handleSwipe is going to be different if we're swiping left and right, but let's start with a simple handle swipe left. We'll define handleSwipe. This is a function, in this function, we want to do two things. First, we want to update the state and change the image index that makes the view change its content, and then we want to animate the same view that is holding the image that we swiped completely to the left, we want to animate it from the right. So let's start with setState, setState is simple. We want to change the image index to be the previous state image index plus one, very simple. This is if we're swiping left, if we're swiping right, we're going to decrement the image index. But let's first implement the swipe left case. After this setState is complete, which we can do inside the callback here, after the setState is complete, we want to animate the next image. So next image animation is going to happen here.
>
> **[1:35](https://www.linkedin.com/learning/react-native-essential-training/handling-swipe-gestures?u=76281980&t=95)** So at this point, the current animated image holds the next image, right? But it was completely swiped to the left. It's left position here is minus the width of the screen. So we want to reset it's position to the right side. We want to reset the left position to the width of the screen so that it goes to the other side of the swipe and then animate it's entrance to the screen. So two things, first, we want to do something like this dot image X position dot set value to the width of the screen. I don't have the width of the screen here, so I'll go ahead and just put this width on the component instance so that we can access it in multiple functions. So it is this dot width now, and in here I can set the value to this dot width, just like that. Once I set the value to this dot width, I'll be ready to animate this image back to the screen from the other direction. So we can use animated here and I'll go with spring action here. So we are animating this image X position, and we want to animate it to a value of zero, which is the center where the image started. So I think this will do. We'll go ahead and start that and we can test. So swipe one image,
>
> **[3:09](https://www.linkedin.com/learning/react-native-essential-training/handling-swipe-gestures?u=76281980&t=189)** there you go. The next image showed up from the other direction of the swipe. Let's see that one more time. There you go. So now we want to make this handle swipe work on both directions. Cause right now it is not going to work on the other direction. In fact, if you get this gray, that means I incremented the image index out of bounds of the actual data that I have. So one more time, if I swipe once I get the new image, I swipe again, I get the third image, and three images are all that I have in this deal. So if I swipe again, I'm going to go to the fourth image which does not exist. So we should actually guard this state with an if statement. We can do an if statement here, and the if statement is going to return early, if I don't need to handle the swipe. This if can be a simple array lookup. So basically if this.state.deal.media, this is the array of images that we have. If the image index here, plus one does not exist, if we're trying to swipe left and we want to increment the image index, and I don't have that data, that means I don't need to handle the swipe. That means I need to return early. So it's this.state.imageIndex, we need to read that from the state. Not only do I need to return, I also need to reset the partial swipe. We can actually use the exact same function here to do that. So reset partial swipe, but this time it will be resetting from the same position.
>
> **[4:46](https://www.linkedin.com/learning/react-native-essential-training/handling-swipe-gestures?u=76281980&t=286)** So let's test all that. Swipe once, good. Swipe twice, good. Now swipe, and you'll see how it got reset and the image index was not incremented. Very good. So now let's handle swipe right. And swipe right is basically going to flip the increment here and here, and it will flip the side of the width. When we go right, we want to animate the next image from left. So let's make handleSwipe receive a variable. I'm going to call it indexDirection, and I'll assume this to be either plus one or minus one. And in here, I'll just add indexDirection instead of plus one, same thing here, add indexDirection, so this could be plus one or minus one, And in here, we want to multiply the width with the indexDirection. So if it's minus one, I'm actually swiping right, and I want the next image to animate from the left. So now this indexDirection should be specified in here. It should be plus one for left swipe, and minus one for right swipe. Because when we swipe left we increment, when we swipe right, we decrement. So it's actually the inverse of this direction. So we'll do that. We'll do minus one times this direction here. I think that should work, let's test. So we can swipe left and we can swipe right. Very good.
>
> **[6:20](https://www.linkedin.com/learning/react-native-essential-training/handling-swipe-gestures?u=76281980&t=380)** I think it's working. But look at that, we have a case that was not handled. So that case is if I swipe less than 40%, what's going to happen? I'm stuck like that. So if I swipe less than 40%, I need to reset the image as well. So we can do that here, basically with the same spring call. And it is the else statement here. If it's not greater than .4, just reset the image back to its zero position. Let's test that. Less than 40%, there you go. Reset. More than 40% swipe out, less than 40%, reset. More than 40%, swipe. Very good. We successfully implemented swipe left and right on the image. Now, how about I challenge you with another swipe? How about implement swipe for the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of this detailed view? So not the image. If, for example, you swipe on a title, or the description of this deal, show that user the next deal that is in the list. It's the exact same code, but instead of incrementing the image, you are picking the next deal in the array of deals. Try to do that on your own.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Code Identifiers:** indexdirection (5), handleswipe (4), setstate (4), imageindex (1)
> **CLI Commands:** make (2)
> **Analogies:** just like (1), for example (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Opening URLs](https://www.linkedin.com/learning/react-native-essential-training/opening-urls?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/opening-urls?u=76281980&t=0)** - [Instructor] The last feature we should add to this app is probably the most important feature: a call to action. A button to purchase a deal. We can do that either through the App Store itself, or with a simple web check-out. Payments made through the App Stores have some significant fees. And since this is a pay for good system, we'll just simply take the user to the web check-out screen. The API for every deal actually has a URL that we can use. This is the same deal page, but on the main website, and that page has a check-out button. So all we need to do is just include a button that takes the user to this URL. We can use the linking library that comes with [React Native](../../Skills/Mobile%20Development/React%20Native.md) to do so. This library can actually be used for both incoming and outgoing app links. But we need it for just an outgoing link. The syntax is simple. We just need to call the open URL API method. Let's do that. I'll include the button to buy a deal right here under the description. So we need the button component from React Native. And in the render method of this component, right under the description, let's include the button. So button, we'll go with title, is buy this deal. And this button needs an on press event. Let's create a new function here to buy the deal. I'll call it open deal URL. Open deal URL is a function, doesn't receive any arguments.
>
> **[1:40](https://www.linkedin.com/learning/react-native-essential-training/opening-urls?u=76281980&t=100)** And it will simply call the Linking Library, which we need to import from React Native as well, so let's do that. And what we need to do on linking is to just open URL, just like that. And the URL that I am interested in opening is the URL attribute that we have on the deal that we have on this date. So basically, this.state.deal.URL, just like that. And I think we can test all this. We have a buy this deal button, and when we click on this button, the user will be taken to the main bake sale for good website, and they can check out this deal from here. So I think we have added enough features for this app. I think it's in a good shape to be deployed to the App Store. So in the next chapter, we are going to do exactly that. But before we do that, we should double-check on our [Android](../../Glossary/Sdk/Android.md) application. And see if any styles need any modification, and see if all the features are working for Android as well. So in the next chapter, we'll check the Android version of this application, and then we'll go ahead and deploy this application on both the App Store and the Play Store.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (3), [Android](../../Glossary/Sdk/Android.md) (3)
> **Env Vars:** url (9), api (2)
> **Cross-References:** in the next (2)
> **Definitions:** is a  (2)
> **Analogies:** just like (2)
> **UI Navigation:** click on (1)
> **Speakers:** - [instructor] (1)


### 6. Deploying

[↑ Back to Table of Contents](#table-of-contents)

#### [Test first](https://www.linkedin.com/learning/react-native-essential-training/test-first?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/react-native-essential-training/test-first?u=76281980&t=1)** - [Instructor] Before we start the process of publishing our app to the App Store and Play Store, we need to test it as much as we can. We've so far been testing the app only on an iPhone 6 Simulator. That is not enough. It's very important that we test the app on actual devices before publishing it. Some problems will only appear on actual devices. The [React Native](../../Skills/Mobile%20Development/React%20Native.md) site has documentation on how to test with an actual device. I'd recommend that you follow this now and test your app on your iPhone if you have one. But testing the app yourself is not enough either. You should also find early testers among family and friends and get their feedback about the UI. You can use Apple's test flight for that. It allows you to beta test with many people by inviting them to your pre-published app. This is a great tool. I'll show you how it works, but first we need to test the app on [Android](../../Glossary/Sdk/Android.md) and the same deal goes there. If you have an Android device, connect it and do your tests there. I keep a few old Android devices just to test things with, but let's just test our app on an Android emulator. We'll do that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (4), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1)
> **Code Identifiers:** iphone (2)
> **CLI Commands:** find (1)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)
> **Prerequisites:** before we start (1)
> **Speakers:** - [instructor] (1)

#### [Testing on Android](https://www.linkedin.com/learning/react-native-essential-training/testing-on-android?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/testing-on-android?u=76281980&t=0)** - [Instructor] To test on [Android](../../Glossary/Sdk/Android.md), we need to have an emulator up and running. So let's do that. The Pixel Emulator here is the one that we used. So let's start that. And once the emulator is up and running, we can go ahead and do, [react](../../Skills/Web%20Development/React.js.md)-native, run-android. The application is loading. And things are working. Let's have the remote debugger open. So Command + M, and debug JS remotely. Very good, I don't see any problems. Let's test going to a single deal. How about the swipe feature. Swiping seems to work, although it is a little bit slow here on the emulator. This could be an emulator problem. So I'll try and test this on an actual device. However, there is an important problem that we did not see on a bigger device when we were testing with iOS is that, this whole view is not scrollable. So I can't actually reach the button here on this smaller device. This is not an Android only problem, but it's more obvious here. So this one is scrollable, and you can see the button here, but the other ones, since the description of the deal is bigger, we are not seeing the button. So let's make this scrollable.
>
> **[1:32](https://www.linkedin.com/learning/react-native-essential-training/testing-on-android?u=76281980&t=92)** All we need to do in the detail detail component is, this top level view here, that hosts the deal detail component, has to be a scroll view instead of just a view. So scroll view, and slash scroll view, which means I need scroll view from react-native as well. Let's test that, hit the R key twice. Now this is scrollable. Very good. Let's also give it some margin down below here so that, the button is not right on the edge. So I'll go ahead and do that. So styles.deal, which we do not have, so let's create deal, and I'll just give it a margin, bottom, of say, 20. Let me test that real quick. Very good. So now we have a scrollable view, and we can buy this deal. Let me go ahead and test that out. So it is going to [http://bakesaleforgood.com](http://bakesaleforgood.com), on this deal, and, a user can buy it. Let's test the search feature. Looks like it's working. However, I don't like that, the keyboard here stayed when the search was performed. So when the search is performed, we should probably get rid of the keyboard. So we can probably do that by just blurring out of the
>
> **[3:08](https://www.linkedin.com/learning/react-native-essential-training/testing-on-android?u=76281980&t=188)** search. So let's try that, in the search bar component, we have the text input here. We'll go ahead and give it a ref attribute. The ref attribute is a function, and this function is going to expose the input element here. And I'll go ahead and put it on the, instance of this component. So this.inputelement equal the input element exposed from this function. So now we can access the text input instance with, this.inputelement. So what I want to do now is, create a new function here. I'll call this function, search deals. So this is the local function that is going to, perform this.props.searchdeals. And after that, it will, blur the text input. So this is going to receive the search term, and pass it along in here. And instead of debouncing this.prompts.searchdeals, we're going to debounce this.searchdeals. And to blur the text input, all we need to do is call this.inputelement. This is the actual UI input element, and we can do .blur on it. So let's go ahead and test all that. (indistinct) Now we can search. When we search, the search is now blurred and I see, the listing. Very good. Here is another problem.
>
> **[4:40](https://www.linkedin.com/learning/react-native-essential-training/testing-on-android?u=76281980&t=280)** So let me show that. So, I typed in a search, the search worked, it blurred out and the keyboard was dismissed. And now I pressed into one of the deals from the search. Perfect. Everything is working. When I go back, the search is still active, and the search term here was cleared out. So we can either not clear out the search term, or reset the search when we click back. I think we should persist the search term, 'cause it would be interesting to keep the state of the search here so that, I browse through multiple deals from the same search. However, this requires that we put the search term on the state of the app component here. So I'm going to call it activesearchterm here. And the reason I named it active is that it is different from the search term that we have on the state of the search bar. This one is the local search term. And in fact, we need to pass the active search term, to the search bar component in here. So in here we'll name it, initialsearchterm, and we'll pass that as this.state.activesearchterm. Let me format. So this way if there is an active search term, and I remount this search bar component, it will have the initial search term property. So we'll go ahead and define that initial search term is, propTypes.string,
>
> **[6:18](https://www.linkedin.com/learning/react-native-essential-training/testing-on-android?u=76281980&t=378)** and it is required. I'm going to use it all the time. So the search term here, doesn't start from an empty string, it starts from this.props.initialsearcterm. Very good. Now to finish this simple feature, when I actually perform the search. So in here, when I perform the search and bring back deals from search, I should also, set the active search term, to be the search term that was passed to this method. So not only I have deals from search, I also have an active search term, that is going to (indistinct) through navigation. The other thing that we need to do here is to specify the value of the text input. So the value of the text input, is going to be connected to this.state.searchterm, just like that, all the time, 'cause this is a controlled component. So, let's test that out now, search for yoga, search is working, click on this list, click back. The search is still there. Now we can change the search to something else. Click here, click back. And if I don't have any search, click here, click back, all is good. So as you can see, I had bugs in the app and I had no idea that they existed. And the only way to find these bugs is to test every possible thing you can think about, and get some other perspective.
>
> **[7:51](https://www.linkedin.com/learning/react-native-essential-training/testing-on-android?u=76281980&t=471)** Usually the person who writes the code is biased, toward the happy cases. You want to get someone else to test your code. And it's much more likely that they are going to find some problems. This is why in the next video, we're going to prepare a production release of our app, push it through an [Apple Developer](../../Skills/Mobile%20Development/Apple%20Developer.md) account, and use test flight to give it to others, to test it. So while testing things out, after changing the main view in the deal detailed view, into a scroll view, I got into this state. Now that we have a scrolling gesture, it seems that it's easy to conflict, the swiping gesture with the scrolling gesture. If you just swipe horizontally it works, but if you swipe and scroll, it doesn't. So there are a few ways to solve this, but the easiest is probably to not have the animated image within the scroll view. So I'm going to change the scroll view, back to just view, and I'll make the detail view right here, into a scrollable view. So this is a scroll view, view/viewinhere. Let me test that out. So now the image is swipeable, but it's not scrollable. You cannot scroll up and down, you can just swipe through. So you can't have the case that we had before, because this is not scrollable now. This is still scrollable.
>
> **[9:25](https://www.linkedin.com/learning/react-native-essential-training/testing-on-android?u=76281980&t=565)** I think I even rather have the title, not within the scrollable area. So I'm going to actually do that as well. I'll have the title in here. Let's see if that is going to affect any of the styling. So now the title is not scrollable. This area is scrollable, just like that. And this wipe is working perfectly. I definitely need to give this app to more people, to test it on real devices because I am pretty sure we have more problems that I am not aware of, because of my biased testing.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (3), [React](../../Skills/Web%20Development/React.js.md) (2), [Apple Developer](../../Skills/Mobile%20Development/Apple%20Developer.md) (1)
> **Definitions:** is a  (4), is an  (2)
> **CLI Commands:** make (2), find (2)
> **Code Identifiers:** ios (1), proptypes (1)
> **UI Navigation:** click on (1), scroll up (1)
> **Analogies:** just like (2)
> **URLs:** [http://bakesaleforgood.com](http://bakesaleforgood.com) (1)
> **Cross-References:** in the next (1)

#### [Changing app logo and splash screen](https://www.linkedin.com/learning/react-native-essential-training/changing-app-logo-and-splash-screen?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/changing-app-logo-and-splash-screen?u=76281980&t=0)** - [Instructor] Before publishing an app there are two things that you'll probably want to do. Give the app a good icon instead of this default one and do something about the splash screen which currently just shows the name of the app and a Powered by [React Native](../../Skills/Mobile%20Development/React%20Native.md) message. You can close the app to reload it with the splash screen. I just double clicked on the home button. And when I double click on the app, the splash screen will splash. So you'll first need to prepare images to use as icons and splash screen if you want to change that as well. Those images should be big. The icon should be a square of at least 1,024 pixels. And this splash screen should be a square of at least 2,500 pixels. The reason you need to start with these big sizes is that you need to provide different sizes of these assets for different screens. So on a big iPad for example, the assets will need to be much bigger. So the Bakesale project has those assets ready for us. We can download them from [bakesaleforgood.com/images/logo.png](https://bakesaleforgood.com/images/logo.png). Go ahead and save that somewhere. I'll save it right here on the desktop, bakesale-logo. There's also /splash.png. We'll save that as well. Bakesale splash. Now the problem is you'll need to use these starting assets to generate many assets with different sizes for the different devices.
>
> **[1:34](https://www.linkedin.com/learning/react-native-essential-training/changing-app-logo-and-splash-screen?u=76281980&t=94)** If you do that manually you are going to be super frustrated with this process. Luckily you don't have to. You can use online services to generate all the images needed and just add them to your [Xcode](../../Skills/Mobile%20Development/Xcode.md) project. For example, you can use the MakeAppIcon service to do that. But there is an even easier way. You can locally generate all the needed icons and do the Xcode needed modifications in one step using this great library, generator-rn-toolbox. So we'll go ahead and do that. You can use this tool to generate many things related to your React Native project, but what we're interested in right now is the icons and splash screen generation. So go ahead and follow the instructions there. You'll need Node and you'll need to [npm](../../Skills/Web%20Development/npm.md) install yo and generator-rn-toolbox. So that's our first step. Make sure that you have a recent version of Node and npm install -g you and generator-rn-toolbox. You also need ImageMagick. ImageMagick is the tool that will be used to generate all these different sizes images. So if you don't have it on Mac, it's as easy as brew install imagemagick. So let's do that as well. And once we have ImageMagick we can use the tool to generate those assets. The commands that you need are documented here. So this is the first one that we can
>
> **[3:07](https://www.linkedin.com/learning/react-native-essential-training/changing-app-logo-and-splash-screen?u=76281980&t=187)** use to generate the icon. So I'm going to copy that part and use it with the icon that I just downloaded from [bakesaleforgood.com](https://bakesaleforgood.com), which I placed under the desktop. So we'll go with bakesale-logo.png. So this command is going to ask you a few questions. The name of the project is Bakesale and you might get this conflict for contents.[JSON](../../Skills/Web%20Development/JSON.md), just hit yes there to overwrite it. And you can see how this application generated all the needed sizes for all the different devices. So this is probably the easiest way to create the app icon set that is needed for Xcode and [Android](../../Glossary/Sdk/Android.md). So now we just react native run ios to test the new icon. And check it out. The app icon is now the Bakesale logo. Very good. Now for the splash screen. If you just want to get rid of the Powered by React Native and keep the default splash screen you can do so by opening Xcode. So just run open ios/bakesale.xcode. This is the Xcode project. If we open up that under the Bakesale folder here, there will be a Launchscreen.xib file. If you click on that, you can see the default launch screen right here. So you can actually just click on
>
> **[4:42](https://www.linkedin.com/learning/react-native-essential-training/changing-app-logo-and-splash-screen?u=76281980&t=282)** Powered by React Native and hit delete to delete it and rebuild your application. And you would have this default splash screen without the Powered by React Native part. But if you want to change the splash screen completely into something different, then you can use this same rn-toolbox tool to generate the splash screens. So let's do that. I'm going to copy this part --splash. And in here, I'll use the splash screen that I downloaded under desktop as well. So bakesale splash.png. The name of the project is Bakesale. And we can override that and that as well. So three files actually were in conflict. Just override them all with a yes. And for the splash screen to take effect you actually need to uninstall the app. So this is important. Long press the app and uninstall it first and then run the react native run ios command to re-install the app with the new splash screen. So the app installing, and I paused the video here on the splash screen so that you can see it. We have a splash screen. If you're using the latest Xcode, I'm currently using Xcode 9, you do need to check on the image assets. There might be one missing icon. If you go to Bakesale here, and go to this images folder and click on the Appicon, you should see all the app icons here filled out.
>
> **[6:19](https://www.linkedin.com/learning/react-native-essential-training/changing-app-logo-and-splash-screen?u=76281980&t=379)** These were all generated by the rn-toolbox tool. However, this last one was not generated. And it's probably because this is a new requirement in Xcode 9. So if you see this one not filled out like that, just drag the logo file that we downloaded into this box, just like that. All the icons here need to be set before we can publish our app, which is what we're going to do in the next video. We're going to build this app, upload it to the app store and test it with a test flight first.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Xcode](../../Skills/Mobile%20Development/Xcode.md) (9), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (7), [npm](../../Skills/Web%20Development/npm.md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (1), [Android](../../Glossary/Sdk/Android.md) (1)
> **Prerequisites:** install (4), you'll need (3)
> **CLI Commands:** node (2), npm (2), make (1), brew (1)
> **UI Navigation:** click on (4), go to (2)
> **Analogies:** for example (2), just like (1)
> **URLs:** [bakesaleforgood.com](https://bakesaleforgood.com) (2)
> **Definitions:** is an  (1), is a  (1)
> **File Paths:** contents.json (1)

#### [Using TestFlight with internal testers](https://www.linkedin.com/learning/react-native-essential-training/using-testflight-with-internal-testers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/using-testflight-with-internal-testers?u=76281980&t=0)** - [Narrator] So to put up an app on the test flight, you need an [Apple Developer](../../Skills/Mobile%20Development/Apple%20Developer.md) account. And this does cost money. Once you have the Developer account approved, you will see this screen where you can control the membership certificate, iTunes Connect and other stuff in here. To put an app on testflight, we need to register a bundle identifier. Now we can do this either through the web developer console here, or through [Xcode](../../Skills/Mobile%20Development/Xcode.md). So I'm going to do it through Xcode. I'll go ahead and open iOS/Bakesale.Xcodeproject. And if you go to the main screen, you'll see the General tab here. And what we're trying to register, is this identifier. And it's by default generated like that, you don't have to use the exact same thing. Usually, it's the domain in reverse. So I'm going to actually do that, I'm going to go with comm.bakesaleforgood.bakesale. So this is now my bundle identifier, and I can go to the console and register it. But we can also register it here in the capabilities tab. If you enable features here, Xcode is going to register the app ID immediately. So let me enable the push notification feature. You need to choose a team here, so we can go ahead and do that, it will ask you to log in with your Apple ID. And once you do, you should see the default team here. So I'll go ahead and select that, so just by enabling this push notification, now the app ID is registered.
>
> **[1:35](https://www.linkedin.com/learning/react-native-essential-training/using-testflight-with-internal-testers?u=76281980&t=95)** And you can go to certificates, IDs and profiles to make sure the app ID was registered successfully. So now that we have an app ID, we need to create the app on the iTunes Connect. Go to iTunes Connect right here, and go to my apps, click on this plus sign, select new app, it's an iOS app, and the name is bakesale, languages English, and here is the bundle ID, the one that I just registered through Xcode. And give the app also some kind of unique identifier here. So I'll go with 00042, create. So it looks like the name bakesale is already used, so we'll pick another one, and you'll need to pick a different unique name as well. So I'm going to go with BakesaleForGood. And you'll get to this screen which has app store information, features, test flight and activity. And what we need is test flight. So if you go to test flight, it will give you an empty screen because we don't have any builds yet. So the next step, is to create a build our app and submit it to the App Store. And we can do both through Xcode. So going back to Xcode, every time you want to do a build, you want to increment the version here. Either increment, the major version minor version number, or the build number. Now this is our first built, so we don't need to increment. We do need to change this target into generic iOS device. And after doing so, we go to product and pick archive.
>
> **[3:12](https://www.linkedin.com/learning/react-native-essential-training/using-testflight-with-internal-testers?u=76281980&t=192)** And the archive option means build release that we can upload to the App Store. This step is probably going to take some time. If you get this screen, just choose always allow, and you'll end up on this screen that lists all the archives that you have on the system. So what you need to do next is to upload to the App Store. Now this is going to ask you a few questions, just stick with the defaults. So click next, automatically manage signing next. And you should end up with a screen like this. Click Upload, and if everything works, you'll get the upload successful screen. If you go back to the iTunes Connect console and refresh, click on iOS, you'll see the bill that we just uploaded and it will be in the processing stage. So this is going to take a few minutes, just give it some time. And you should get an email when it's done. Once you get that email that the app has completed processing, you might see a screen like this. I have a build, but it's missing compliance. This is related to encryption. So just go ahead and click on this and click on provide expert compliance information and just pick No, the app does not use any encryption. And start internal testing. Once you see this ready to test, you are ready to send this application for internal users. And the cool thing is, you don't have to go through the app review process to test it with internal users.
>
> **[4:47](https://www.linkedin.com/learning/react-native-essential-training/using-testflight-with-internal-testers?u=76281980&t=287)** If you want to test it with external users, you have to submit the app for review. So to add internal testers, you want to click here, add iTunes Connect users. And in here you should see the list of your internal testers. I don't have any, so let's go ahead and add a few. So click on this plus and provide the tester information, click Next, and there you go. Kyle is now in the system. And he has been sent a confirmation email as part of this testing team. And after Kyle accept the invitation, we can go to add testers, we can select the testers that we want on this project. We'll go ahead and select myself as well, and click Add. And this will send an invitation for Kyle and myself to test this app using the test flight app on our iPhones. Once you're done testing with the app, and you're ready to publish the app, you want to submit it to the App Store. Go to the App Store tab in here, make sure all the information that is required here is filled out. Go to the prepare for submission. And then here you need to do a few things, you need to add some actual screenshots from the app in here. You need to fill some of this information and select the build before you submit the app, so you can select the build that we just did. And once everything is ready here, you can submit for review.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Xcode](../../Skills/Mobile%20Development/Xcode.md) (6), [Apple Developer](../../Skills/Mobile%20Development/Apple%20Developer.md) (1)
> **UI Navigation:** go to (10), click on (5), select the (3)
> **Code Identifiers:** itunes (5), ios (4), iphones (1)
> **CLI Commands:** make (2)
> **Cross-References:** go back to (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [narrator] (1)

#### [Generating signed APK for Android](https://www.linkedin.com/learning/react-native-essential-training/generating-signed-apk-for-android?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/generating-signed-apk-for-android?u=76281980&t=0)** - [Instructor] To build and release for [Android](../../Glossary/Sdk/Android.md), we need to generate a signing key, and we can use key tool to do that. The [React Native](../../Skills/Mobile%20Development/React%20Native.md) site has a guide article about that. Before we go through this article, let me explain some of the terms you're going to see here. So an APK is basically the Android binary format. This is comparable to IOS's IPA or [Windows](../../Glossary/Service/Windows.md)' exe files. We're going to use the key tool to generate a key store file. This key store file is like your certificate. This file is really important. It identifies that you or anyone you share this file with is authorized to publish a new version of your app. And if you lose your file, you will not be able to publish any new versions of your app. So make sure that you keep this file secure and backed up. We're going to have to work with some [Gradle](../../Glossary/Tool/Gradle.md) variables here, and Gradle is basically the build tool that Android uses. So you'll have to edit some files to basically add the certificate to the build. So let's go ahead and go through these steps one by one. So the first thing, I'm going to copy this key tool command, paste it here, and I'll change a few things. I'm going to call this bake sale key, and they'll give it an alias of bake sale. So bakesale-key.keystore. That would be the name of the generated file. And this command is basically saying, generate this keystore file with this alias and make it valid for 10,000 days.
>
> **[1:37](https://www.linkedin.com/learning/react-native-essential-training/generating-signed-apk-for-android?u=76281980&t=97)** the command is going to prompt for a password, and this password is super important because again, without it, you cannot publish any new versions to your app. So answer the other questions here. Once you're done, you should see the bakesale-key.keystore file in this directory. Now this file does not belong to your search control. So make sure you don't commit that. Next you want to edit this file, and if it doesn't exist, just create it. So I'll atom this file and copy these properties in here into the file and use the names that you used for the file. So we used bakesale-key and just bake sale. And you want to also put the password in here. There is a way to store these passwords in your key chain instead of having them in plain text here. The guide has a link to do that. The next step, you want to go under Android under app and edit the build.gradle file. This is a big file. So what you want to add is you want to add this signing config section. So add it here under the default config. And also you want to add this line under the build types release section. So look for build types, and you'll see a release section in here. So add the signing config, signingconfig.release line under here. Once you're done with these steps,
>
> **[3:11](https://www.linkedin.com/learning/react-native-essential-training/generating-signed-apk-for-android?u=76281980&t=191)** you want to run this command in your app directory, but before you run that command, move the keystore file to under Android app so that it can find it. Now we can run this command, basically CD into Android and run the gradle assemble release command. So you should see a build successful message. And now we have under Android app build outputs/apk, if you list the file under that folder, you'll see the app-release.apk which is the file that you need to use to submit this app to the [Google](../../Glossary/Service/Google.md) play store, to submit that apk to the Google play store. You want to go to Google Play Console, log in with your developer account, click on create application, and we'll give it a title, Bake Sale. And you'll need to fill the information. You need to fill up the description, and you need to upload screenshots and logo. And once you're done doing that, you'll have a screen to upload the signed release apk file that we just generated.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (7), [Gradle](../../Glossary/Tool/Gradle.md) (4), [Google](../../Glossary/Service/Google.md) (3), [React Native](../../Skills/Mobile%20Development/React%20Native.md) (1), [Windows](../../Glossary/Service/Windows.md) (1)
> **CLI Commands:** make (3), find (1), cd (1)
> **Env Vars:** apk (1), ios (1), ipa (1)
> **UI Navigation:** go to (1), click on (1)
> **Definitions:** is a  (2)
> **File Paths:** build.gradle (1)
> **Tools:** atom (1)
> **Prerequisites:** you'll need (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/react-native-essential-training/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/react-native-essential-training/next-steps?u=76281980&t=0)** - [Instructor] Thank you for taking this course, and I hope that you enjoyed it. I know it might have been a little bit challenging in parts, but trust me, it gets easier. You've made a great investment [Learning React Native](Learning%20React%20Native.md), because its popularity is unmatched and there is a big demand for [React Native](../../Skills/Mobile%20Development/React%20Native.md) developers. So what's next? Focus on building as many apps as you can, get yourself in trouble. Get used to see and appreciate the big red screen because the more problems you face, the better is your knowledge. This course did not cover all the React Native APIs. There is a lot more to explore. Get comfortable reading the documentation and using the examples. And finally, working with React Native is no doubt a lot more pleasant than having to deal with [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) and [Java](../../Skills/Software%20Development/Java.md), but do not dismiss the idea of learning Swift and Java. To become a better React Native developer, you need to be comfortable going native when needed. I encourage you to learn the basics of native iOS and [Android](../../Glossary/Sdk/Android.md) development next. That's what I'm doing.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React Native](../../Skills/Mobile%20Development/React%20Native.md) (5), [Swift](../../Skills/Mobile%20Development/Swift%20(Programming%20Language).md) (2), [Java](../../Skills/Software%20Development/Java.md) (2), [Android](../../Glossary/Sdk/Android.md) (1)
> **Definitions:** is a  (2)
> **Code Identifiers:** ios (1)
> **Documentation:** the documentation (1)
> **Speakers:** - [instructor] (1)


## Skills Covered

- React Native

## Path Context

### In [Become a React Native Developer](../../Paths/Mobile%20Development/Learning%20Paths/Become%20a%20React%20Native%20Developer.md)
**1 of 5** | [From React to React Native](From%20React%20to%20React%20Native.md) →

## Appears In

- [Become a React Native Developer](../../Paths/Mobile%20Development/Learning%20Paths/Become%20a%20React%20Native%20Developer.md)

## Related Courses

_Courses sharing skills:_

- [Learning React Native](Learning%20React%20Native.md) — React Native
- [React Native Ecosystem and Workflow](React%20Native%20Ecosystem%20and%20Workflow.md) — React Native
- [Create a CRM Mobile Application with React Native](Create%20a%20CRM%20Mobile%20Application%20with%20React%20Native.md) — React Native
- [From React to React Native](From%20React%20to%20React%20Native.md) — React Native

---

[↑ Back to top](#)