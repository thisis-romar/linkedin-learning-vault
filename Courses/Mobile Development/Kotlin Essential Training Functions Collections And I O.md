---
type: course
cssclasses:
  - lle-course
  - lle-level-beginner
slug: kotlin-essential-training-functions-collections-and-i-o
url: "https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o"
level: Beginner
updated: 4/29/2022
learners: 2135
skills:
  - Kotlin
exercise_files: true
github: "https://github.com/LinkedInLearning/kotlin-essential-training-functions-collections-and-io-3008787"
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQHQxXohmsZEgQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1650999446186?e=2147483647&amp;v=beta&amp;t=012dHBeTDcwco_mvjkWz0aVST5nrbThJnBPW1sO0Ic8"
linkedin_topic: Mobile Development
learning_paths:
  - '[[Kotlin Professional Certificate by JetBrains]]'
next_courses:
  - '[[Kotlin Essential Training- Object-Oriented and Async Code]]'
path_nav: '[{"path":"Kotlin Professional Certificate by JetBrains","position":1,"total":4,"prev":null,"next":"Kotlin Essential Training- Object-Oriented and Async Code"}]'
path_count: 1
tags:
  - course
  - topic/mobile-development
  - topic/software-development
  - skill/kotlin
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Mobile%20Development/Kotlin%20Essential%20Training%20Functions%20Collections%20And%20I%20O.md)

![Kotlin Essential Training Functions Collections And I O](https://media.licdn.com/dms/image/v2/C560DAQHQxXohmsZEgQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1650999446186?e=2147483647&amp;v=beta&amp;t=012dHBeTDcwco_mvjkWz0aVST5nrbThJnBPW1sO0Ic8)

# Kotlin Essential Training Functions Collections And I O

> If you’re looking to get up and running with Kotlin, the powerful programming language from JetBrains, this course gives you the tools you need to start writing command-line programs in a matter of hours. Join instructor Nate Ebel, the author of Mastering Kotlin, as he shows you the ins and out of Kotlin and walks you through its unique selling points for developers—from null safety and concise sy

> [LinkedIn Learning](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o) | Beginner | 2K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Welcome to Kotlin](#welcome-to-kotlin)
  - [What you should know](#what-you-should-know)
  - [Using the exercise files on GitHub](#using-the-exercise-files-on-github)
- [**1. Setting Up Your Kotlin Development Environment**](#1-setting-up-your-kotlin-development-environment) (4 videos)
  - [Introducing IntelliJ IDEA](#introducing-intellij-idea)
  - [Installing IntelliJ IDEA for macOS](#installing-intellij-idea-for-macos)
  - [Installing IntelliJ IDEA for Windows](#installing-intellij-idea-for-windows)
  - [Run your first Kotlin code](#run-your-first-kotlin-code)
- [**2. Common Data Types**](#2-common-data-types) (4 videos)
  - [Mutable and immutable data types](#mutable-and-immutable-data-types)
  - [Numeric data types](#numeric-data-types)
  - [Char and string data types](#char-and-string-data-types)
  - [Nullable types](#nullable-types)
- [**3. Working with Functions**](#3-working-with-functions) (6 videos)
  - [Basic functions](#basic-functions)
  - [Function parameters](#function-parameters)
  - [Functional types](#functional-types)
  - [Higher-order functions](#higher-order-functions)
  - [Challenge: Write a name formatter function](#challenge-write-a-name-formatter-function)
  - [Solution: Write a name formatter function](#solution-write-a-name-formatter-function)
- [**4. Control Flow Structures**](#4-control-flow-structures) (8 videos)
  - [if and when statements](#if-and-when-statements)
  - [Statements vs. expressions](#statements-vs-expressions)
  - [try/catch as control flow](#trycatch-as-control-flow)
  - [Smart casting](#smart-casting)
  - [while loops](#while-loops)
  - [for loops](#for-loops)
  - [Challenge: Replace an if/else expression with a when expression](#challenge-replace-an-ifelse-expression-with-a-when-expression)
  - [Solution: Replace an if/else expression with a when expression](#solution-replace-an-ifelse-expression-with-a-when-expression)
- [**5. Debugging Kotlin Code**](#5-debugging-kotlin-code) (3 videos)
  - [Setting breakpoints](#setting-breakpoints)
  - [Stepping through code](#stepping-through-code)
  - [Evaluate expressions and add watches](#evaluate-expressions-and-add-watches)
- [**6. Working with Collections**](#6-working-with-collections) (7 videos)
  - [Arrays](#arrays)
  - [Lists](#lists)
  - [Sets and maps](#sets-and-maps)
  - [Working with collections](#working-with-collections)
  - [Sequences](#sequences)
  - [Challenge: Filtering a list of data](#challenge-filtering-a-list-of-data)
  - [Solution: Filtering a list of data](#solution-filtering-a-list-of-data)
- [**7. Working with Inputs and Outputs**](#7-working-with-inputs-and-outputs) (5 videos)
  - [Command-line arguments](#command-line-arguments)
  - [Receiving user input](#receiving-user-input)
  - [Read and write to files](#read-and-write-to-files)
  - [Challenge: Parsing a list of data from a file](#challenge-parsing-a-list-of-data-from-a-file)
  - [Solution: Parsing a list of data from a file](#solution-parsing-a-list-of-data-from-a-file)
- [**8. Testing Kotlin Code**](#8-testing-kotlin-code) (2 videos)
  - [Writing JUnit tests](#writing-junit-tests)
  - [Mocking data](#mocking-data)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Welcome to Kotlin](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/welcome-to-kotlin-14800824?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/welcome-to-kotlin-14800824?u=76281980&t=0)** - [Nate] [[Kotlin]] is one of the most popular programming languages for building mobile applications, being the default choice for [[Android]] development. It's no surprise why developers are turning to Kotlin. It's concise, flexible and includes a powerful set of modern language features. With Kotlin, developers can efficiently build robust, scalable applications that run on mobile devices, in the browser and a variety of other targets. I'm going to show you how to start building applications with Kotlin. We'll start with foundational language features, such as variables and functions, explore Kotlin's collection types, and examine the basics of File I/O. My name is Nate Ebel. I've been building mobile applications for over 10 years and have been working with Kotlin since before it's 1.0 release. If you're excited to start learning Kotlin then join me in my [[LinkedIn]] learning course on Kotlin essentials.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (8), [[Android]] (1), [[LinkedIn]] (1)
> **Versions:** 1.0 (1)
> **Analogies:** such as (1)
> **Speakers:** - [nate] (1)

#### [What you should know](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/what-you-should-know-14798820?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/what-you-should-know-14798820?u=76281980&t=0)** - [Instructor] This course is designed to help you learn the essentials of the [[Kotlin]] programming language. To get the most out of it you should already be familiar with writing code using a modern language such as [[Java]], [[Python (Programming Language)|Python]], [[Swift (Programming Language)|Swift]] or [[JavaScript]]. You'll want to be comfortable with object oriented [[Programming Concepts]] and you should know your way around [[GitHub]]. If you feel you should brush up on any of these concepts check out the course library for more resources and come back to this course when you're ready.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (1), [[Java]] (1), [[Python (Programming Language)|Python]] (1), [[Swift (Programming Language)|Swift]] (1), [[JavaScript]] (1)
> **CLI Commands:** python (1)
> **Tools:** github (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Using the exercise files on GitHub](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/using-the-exercise-files-on-github-14803700?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/using-the-exercise-files-on-github-14803700?u=76281980&t=1)** - [Instructor] The exercise files for this course are stored on [[GitHub]]. There are branches for both the start and end states of each video where we work on code. You can find a list of all the branches here in GitHub in the Branch dropdown menu. For example, if you wanted to find the start state for video 03_01, basic functions, you'd change it to the 03_01b branch and the ending state would be in the 03_01e branch.
>
> **[0:37](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/using-the-exercise-files-on-github-14803700?u=76281980&t=37)** There are multiple ways to work with this project in IntelliJ. One simple way is to copy the URL from the Code button and then move over to IntelliJ where we can check that code out into our working machine. Once in IntelliJ, we're going to select the Get from VCS option. VCS in this context stands for [[Version Control]] system. When we click that button, we'll get the Get from Version Control dialogue, where we will then paste in the URL to the repo that we copied from GitHub. In the Directory field, we can select the location that we want to copy the project into. Once you're satisfied with that working directory, go ahead and click Clone to download the project to your machine. Once you've opened the project, you may receive a few dialogues asking if you want to add the files to [[Git]], go ahead and ignore those, but chances are, by the time you download the files, there will be updates to IntelliJ and the associated plugins. If you receive a dialogue asking you to upgrade your dependencies, please do so, then wait project to sync. It typically takes longer when the project loads for the first time. Once the sync is complete, however, you're ready to work with this version of the code. By default, we're running the code from the Main branch. As I mentioned earlier, I've created branches for each video in the course where we modify code. If you'd like to follow along, use the branches popup menu
>
> **[2:08](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/using-the-exercise-files-on-github-14803700?u=76281980&t=128)** in the lower right corner of IntelliJ to change the branch to whatever branch you're looking for. As an example, let's check out the code for the start of the 03_01 video. We will choose it here from the dropdown and we can search for 03_01b. We'll select that option and click Checkout. This will checkout a copy of that branch locally. We'll then rebuild our project to ensure that we're working with the code from this newly selected branch. Once that's complete, we're now ready to work with this version of the code. If you run into any trouble checking out the different branches or with using Git in general, I recommend searching the course library for the "[[Git Essential Training]]" videos.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (3), [[Git]] (3), [[Version Control]] (2)
> **Tools:** intellij (5), github (3)
> **CLI Commands:** git (3), find (2)
> **Env Vars:** url (2), vcs (2)
> **UI Navigation:** dropdown (2), select the (2)
> **Exercise Files:** download the (2), exercise files (1)
> **Definitions:** stands for (1)
> **Analogies:** for example (1)


### 1. Setting Up Your Kotlin Development Environment

[↑ Back to Table of Contents](#table-of-contents)

#### [Introducing IntelliJ IDEA](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/introducing-intellij-idea-14799802?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/introducing-intellij-idea-14799802?u=76281980&t=0)** - [Instructor] Like most programming languages, it's possible to write [[Kotlin]] in a variety of ways, from the command line, your favorite text editor, or other more language focused programs. In this course, we're going to be using a program called IntelliJ Idea or just IntelliJ for short. By using IntelliJ, you'll better understand why it's useful for building Kotlin projects. Kotlin is an IDE or integrated development environment. IDEs combine developer tooling with a graphical interface to streamline the development process. Without a fully featured IDE, we would need to build our Kotlin projects using a combination of different text editors, terminals, and command line tools. And while this is perfectly possible, it's not usually the most efficient way to write modern [[Software Projects]]. IntelliJ has a robust suite of tools for Kotlin development including a fully functional text editor, syntax highlighting, and powerful refactoring capabilities. Why choose to use IntelliJ over your favorite text editor or another IDE? Well that's because IntelliJ is built by JetBrains, the creators of the Kotlin programming language. They are the industry leaders in IDE development with a long track record of terrific tooling for C#, [[Java]] and other popular languages. Because of this, IntelliJ provides a powerful and comprehensive set of functionality
>
> **[1:32](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/introducing-intellij-idea-14799802?u=76281980&t=92)** for building nearly any type of Kotlin project you could want, including mobile, web, or backend projects. IntelliJ comes in two flavors, community edition and ultimate, and both of these variants are available across Mac, [[Windows]] and [[Linux]]. The community edition provides full support for Kotlin development and is free to use and download. The ultimate edition is a paid product which includes additional tools and functionality not specifically related to Kotlin. For this course, the community edition is more than enough, but feel free to use ultimate if you have access. Whether using community or ultimate edition, you'll want to make sure you're using version 2021.3.1 or newer so the UI and menus match as closely as possible. This should ensure that you have no trouble following along.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (9), [[Software Projects]] (1), [[Java]] (1), [[Windows]] (1), [[Linux]] (1)
> **Tools:** intellij (8), command line (2)
> **Env Vars:** ide (4)
> **Versions:** version
2021 (1), 3.1 (1)
> **Definitions:** is an  (1), is a  (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Installing IntelliJ IDEA for macOS](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/installing-intellij-idea-for-macos-14800826?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/installing-intellij-idea-for-macos-14800826?u=76281980&t=0)** - [Instructor] Before we can start writing [[Kotlin]] code with IntelliJ, we'll first need to download and install one of the available versions of the IDE. Let's walk through the IntelliJ installation process for macOS. If developing on a [[Windows]] machine, no worries, we will cover the Windows installation process soon. Whether you want to install the Community or Ultimate Edition, there are two primary ways to download IntelliJ. Directly from the browser or by using JetBrains Toolbox. If you aren't familiar, JetBrains Toolbox is an application for managing the installation and updates for any JetBrains IDE. It's a very helpful tool if you're using IntelliJ, [[Android]] Studio, or any other JetBrains IDE. We won't walk through the installation of JetBrains Toolbox itself in this course. However, if already using jet brain's toolbox, you can install IntelliJ using JetBrains Toolbox by opening JetBrains Toolbox in your machine, scrolling through the list of the available IDEs, until you find one of the available versions of IntelliJ. We see here that both the Ultimate and the Community Edition are available to us. If we click either of these to install, JetBrains Toolbox will take care of managing the installer. And from there, we can then open the IDE and continue on with the installation process. It'll even help us manage that installation in the future if we want to upgrade, downgrade,
>
> **[1:34](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/installing-intellij-idea-for-macos-14800826?u=76281980&t=94)** or even uninstall the application. If you prefer to download IntelliJ from the browser, the best place to start is by searching download IntelliJ from your favorite web browser. We want to access the IntelliJ download site from [jetbrains.com/idea/download](https://jetbrains.com/idea/download). This page should detect your OS version automatically. If we see here, the middle tab is selected for me, indicating that I am on macOS. Now, if you're working on a Mac, you'll want to ensure that you download an installer for the correct CPU architecture, whether that be Intel or Apple Silicon. Once you've selected the correct download option, click Download to start the download of the installer. Once the download is complete, we'll need to locate the download on our development machine. For me, that download is located in my downloads directory where I see the installer is waiting for me. Once I double click on that installer, this will kick off the download process. I'll be prompted to drag the IntelliJ icon into the applications directory, where it'll start copying over the files needed for IntelliJ. Once that's complete, we can open up the applications directory, double click the IntelliJ icon and this will continue on to prompt us to open up the IntelliJ application.
>
> **[3:08](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/installing-intellij-idea-for-macos-14800826?u=76281980&t=188)** And from here, we should be able to follow the wizard until we are greeted with the IntelliJ welcome screen. For this course, you should not need any additional plugins, but feel free to customize the look and feel of IntelliJ to match your personal preferences. Once your IntelliJ installation is complete, you'll be ready to create your first Kotlin project.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (2), [[Windows]] (2), [[Android]] (1)
> **Tools:** intellij (16)
> **Env Vars:** ide (4), cpu (1)
> **Prerequisites:** install (4)
> **Code Identifiers:** macos (2)
> **UI Navigation:** open the (1), click on (1)
> **CLI Commands:** find (1)
> **URLs:** [jetbrains.com](https://jetbrains.com) (1)

#### [Installing IntelliJ IDEA for Windows](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/installing-intellij-idea-for-windows-14799792?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/installing-intellij-idea-for-windows-14799792?u=76281980&t=0)** - Before we can start writing [[Kotlin]] code with IntelliJ, we'll first need to download and install one of the available versions of the IDE. Now, let's go through the IntelliJ installation process for [[Windows]]. Whether you want to install the Community or Ultimate Edition, there are two primary ways to download IntelliJ. Directly from the browser or by using JetBrains Toolbox. If you aren't familiar, JetBrains Toolbox is an application for managing the installation and updates for any JetBrains-based IDE. It's a very helpful tool if you're using IntelliJ, [[Android]] Studio or any other JetBrains IDE. We won't walk through the installation of JetBrains Toolbox itself in this course. However, if already using JetBrains Toolbox, you can use it to install IntelliJ by opening JetBrains Toolbox, scrolling through the available list of IDEs and locating IntelliJ IDEA Community Edition or Ultimate Edition. Once located, you can click the install button and JetBrains Toolbox should manage the install of the latest version for you. It'll even help manage that version in the future if you want to upgrade, downgrade, or even uninstall IntelliJ altogether. If you prefer to download IntelliJ from the browser, the best place to start is by searching download IntelliJ from your favorite web browser. We want to access the IntelliJ download site
>
> **[1:33](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/installing-intellij-idea-for-windows-14799792?u=76281980&t=93)** from [jetbrains.com/idea/download](https://jetbrains.com/idea/download). This page should detect your OS version automatically. In this case, the tab furthest to the left is selected, indicating that Windows is my operating system. Once you've chosen either Ultimate or Community, select the correct download option and click the download button. This should start the download of the installer. Once the download is complete, we'll need to locate the download on our development machine. For me, I will navigate to my Downloads directory. And I will double click on the application installer. If prompted, go ahead and click Allow to allow the program to install. And from here, we can follow the setup instructions for a basic IntelliJ installation on our machine. Once we've clicked through the basic options, the installer should perform its work to install IntelliJ to our Windows installation. Now, this can take several minutes, so grab a cup of coffee or do anything else you need to while we're waiting for your installation. Once the installation is complete, we should have the option to finish the install. And if we click the Run IntelliJ button and click Finish, IntelliJ should load up on our machine for the first time.
>
> **[3:12](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/installing-intellij-idea-for-windows-14799792?u=76281980&t=192)** Now, once IntelliJ is open, we should not need to install any additional plugins. But feel free to customize the look and feel of IntelliJ to match your personal preferences. Now, once your installation is complete, you'll be ready to create your first Kotlin project.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (3), [[Kotlin]] (2), [[Android]] (1)
> **Tools:** intellij (16)
> **Prerequisites:** install (9), setup (1)
> **Env Vars:** ide (3), idea (1)
> **UI Navigation:** select the (1), navigate to (1), click on (1)
> **URLs:** [jetbrains.com](https://jetbrains.com) (1)
> **Definitions:** is an  (1)
> **Speakers:** - before (1)

#### [Run your first Kotlin code](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/run-your-first-kotlin-code-14799786?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/run-your-first-kotlin-code-14799786?u=76281980&t=0)** - [Instructor] To create our first [[Kotlin]] project, open the IntelliJ application. Once open, select New Project. Once the New Project window is open, we want to make sure that the Kotlin option is selected on the left side of the screen. We can create a variety of different Kotlin project types from this view. But for now, we will stick with the basic JVM console application. If you aren't familiar, JVM stands for [[Java]] Virtual Machine and is the most common runtime environment for Kotlin applications. Name your project HelloKotlin. And then in the Location option, select a project location that you're comfortable with. For me, I will navigate to this course's working directory and just update that location. For Build System, go ahead and leave the default option selected. For Project JDK, you'll need to ensure that a valid JDK is selected. JDK stands for Java Development Kit. A JDK provides the virtual machine, the compiler, and other tools needed to build and run JVM applications. JDK versions are correlated with versions of Java, and by extension, Java bytecode. Any JDK version 8.0 or newer should work fine for this course. So if we extend this JDK dropdown, we'll see that it has automatically detected
>
> **[1:34](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/run-your-first-kotlin-code-14799786?u=76281980&t=94)** a JDK version 8.0 on my machine as well as version 15.0. So because both of these are a version 8.0 or above, either one will work fine, I will go ahead and leave version 15.0 selected. If you don't have a version of the JDK installed on your machine, you could click the Download JDK option and download any available version of Oracle OpenJDK. Once you're satisfied with your project settings, go ahead and click next. On this screen, you can further customize the initial configuration of your project. For now, we will leave these settings as is and go ahead and click Finish. After clicking Finish, IntelliJ will create your new Kotlin project and prepare the project by syncing any initial dependencies. This can sometimes take a minute or two, so you may have to be patient while IntelliJ does its thing. Now that the project has finished it's initial sync, let's take a few moments to familiarize ourselves with the interface of IntelliJ. On the left side of the screen is the Project Tool window. This represents our project's file structure, and is where we can locate any build or source files for the project. Notice, if we navigate into our src, main, kotlin directory, we have a single source file named Main.kt. If we come over into the main panel of our screen, we'll see that main.kt is already open.
>
> **[3:10](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/run-your-first-kotlin-code-14799786?u=76281980&t=190)** The .kt file extension is the primary extension for Kotlin files. Within that Main.kt file, we find that we have a main function. This is the primary entry point to our Kotlin program. Finally, the toolbar at the top of the screen provides additional tools and actions, such as the ability to run or debug our project. Let's complete our initial project setup by running our code. We can run our Kotlin program in one of two ways. The easiest is to click the green Play button in the gutter next to the file line numbers. If we click that button and select Run MainKt, IntelliJ will compile our application and any program output will be written to the console. In this case, we see Hello World! And we see any program arguments. In this case, none were passed to the run. Additionally, the other way that we can run our application is by selecting the green Play button from our toolbar. Again, in this case, if we click the Run button, we'll see the same output printed out to the console. The ability to run a main function in this way is actually quite convenient. Any main function in any Kotlin file can be run this way,
>
> **[4:44](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/run-your-first-kotlin-code-14799786?u=76281980&t=284)** making it a very convenient, quick way of running some Kotlin code from within IntelliJ. Now that we've run our first code, we are ready to dive deeper into the essentials of the Kotlin programming language.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (12), [[Java]] (4)
> **Env Vars:** jdk (10), jvm (3)
> **Tools:** intellij (6)
> **Versions:** version 8 (3), version 15 (2)
> **File Paths:** main.kt (3)
> **UI Navigation:** open the (1), navigate to (1), dropdown (1)
> **CLI Commands:** make (1), find (1)
> **Definitions:** stands for (2)


### 2. Common Data Types

[↑ Back to Table of Contents](#table-of-contents)

#### [Mutable and immutable data types](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mutable-and-immutable-data-types-14800822?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mutable-and-immutable-data-types-14800822?u=76281980&t=0)** - [Instructor] The ability to store, update and retrieve values using variables is a core feature of most programming languages. So we're going to start our exploration of [[Kotlin]] by learning how to define both mutable and readonly variables. To create our first variable, we're going to come into our empty main function here, and we going to start by writing val. Val is a keyword denoting a readonly variable. Next, we need to type out the variable name. In this case, we will name our variable, our first variable. Now we can define a variable's type by adding a colon and then the type itself. In this case, we are going to define this as a type Boolean. This indicates that we want to store some Boolean value, essentially true or false. And lastly to finish off our variable, we need to provide a default value in this case. We can do this by adding equals true. Now we have a readonly variable named our first variable that holds the value true. And we can easily validate this by using a print function to print out the value of the variable. So we'll come a couple lines down below our variable and we will invoke the println function by typing println and we will pass our first variable to that function.
>
> **[1:40](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mutable-and-immutable-data-types-14800822?u=76281980&t=100)** Once we've added the call to println, we can run our main function by once again navigating to the green play button in the gutter and clicking Run MainKt. Once that function has been run, we should see true printed out in the output window on the bottom of our screen. Now let's say we need to change the value of our variable after we print it. How would we do that? Well, again we'll hit Enter a couple times and come down below our print statement. And now we're going to try and assign the value of false to our variable. So we'll type our first variable equals false. You should see the IDE showing a red underline underneath the variable name. This indicates a compiler error. If we place our cursor over that line, we should see the error message, val cannot be reassigned. So why are we seeing this error? Well, a central tenant of Kotlin is an explicit type system and Kotlin differentiates between mutable and readonly variables. By defining our variable using val, we made it readonly. By default, val variables must be assigned once and their values will not change. This makes it easier to reason about the behavior and the value of the variable,
>
> **[3:13](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mutable-and-immutable-data-types-14800822?u=76281980&t=193)** but also means we can't store mutable values using a val variable. To define a mutable variable, instead of using val, we can use var. So if we come back up to the definition of our first variable, we can change the val to a var.
>
> **[3:36](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mutable-and-immutable-data-types-14800822?u=76281980&t=216)** Once we've updated the val to var, the compiler error went away. Now, we can add another call to println, and if we run our program again, we can verify that the variable is in fact changing. We first see true printed out, and then we see false printed out. In addition to strong typing, Kotlin supports type inference. What does that mean for us developers? It means we don't always have to explicitly define the type of a variable if the compiler can understand the variable's type from the assigned value. Again, if we go back to the declaration of our first variable, we can remove the Boolean type declaration from our variables in initialization. Now, even though we don't explicitly declare a Boolean type, the Kotlin compiler still understands that our first variable is of type Boolean because we assigned it a Boolean value of true. To compare Boolean values, we can use the equal equals syntax. Let's add two more print statements to demonstrate equality checks. First, we will compare our first variable
>
> **[5:05](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mutable-and-immutable-data-types-14800822?u=76281980&t=305)** to the value of true, and next, we will compare our first variable to the value of false. Before running the code, take a second to think about the output. What do you expect to be printed to the console based on these equality checks? If we now run our code once again, we see true, false, false and true printed out to the console. The first two lines, true and false come from our earlier print statements. And then our two equality checks printed out false and true respectively. This makes sense since we set our first variable to false after the first print statement. As we continue learning about Kotlin, we will be building on these concepts of immutability, type inference and comparison. As a rule of thumb though, Kotlin favors immutability by default. So as we write Kotlin code, we should think about whether or not we really need a value to change before opting into mutable variables.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (8), cursor (1)
> **Definitions:** is a  (2), is an  (1)
> **Env Vars:** ide (1)
> **Cross-References:** go back to (1)
> **Best Practices:** rule of thumb (1)
> **Prerequisites:** initialization (1)
> **Speakers:** - [instructor] (1)

#### [Numeric data types](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/numeric-data-types-14803696?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/numeric-data-types-14803696?u=76281980&t=0)** - [Instructor] Numeric data types are essential for writing almost any application. Let's jump in and explore examples of both integer and floating point data types in [[Kotlin]]. Integer data types represent whole numbers. The easiest example is Int. To create an Int variable, I'll once again use the val keyword, give my variable a name, in this case, aInt, and explicitly define the Int type. And then, I will assign it a value of zero to start. If we want it to be a bit more concise, we could use type inference to define another variable without needing to explicitly define the type of Int. So in this case, I could type, val anotherInt = 0. And in this case, another Int will simply be inferred to be of type Int. In Kotlin, an Int is 32 bits. If you need a smaller data type, we have several other options we can leverage. We could use aByte, which is eight bits,
>
> **[1:22](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/numeric-data-types-14803696?u=76281980&t=82)** or we could use aShort, which is 16 bits.
>
> **[1:31](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/numeric-data-types-14803696?u=76281980&t=91)** If you need a larger data type, you could use aLong, which is 64 bits. By default, integer values that fit within 32 bits will have an inferred type of Int. So, if I type, val inferredInt = 10, this variable will automatically have the type of Int, because it fits within 32 bits. If a value is larger than 32 bits, it will be inferred as long. So in this example, we'll type, inferredLong = some very large number here.
>
> **[2:16](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/numeric-data-types-14803696?u=76281980&t=136)** So in this case, this value will be inferred to be of type long because it's not going to fit into the standard 32-bit integer value. Now, if we wanted to store a smaller value as a long, rather than an Int, without explicitly adding the long type to the variable declaration, we can append an L to the end of our integer literal to signify that it should be treated as long. So in this case, we'll define a variable called anotherLong and assign it to the value of 10L. This signifies that this value should be treated as a long rather than an Int. So far, these examples have all used signed integers. However, if you need an unsigned value, there're unsigned equivalents of each integer type. For example, we can explicitly declare an unsignedInt. Notice, in this case, that the Int type is pre-appended with a U to represent unsigned, and similarly, the integer literal that we assigned to it has a U appended at the end. Additionally, we could use type inference and specify our integer literal as in unsignedLong.
>
> **[3:42](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/numeric-data-types-14803696?u=76281980&t=222)** In this case, we use both a U and an L in our integer literal to signify that we want it to be unsigned and long. Beyond integer types, we also have two floating point types. A 64 bit double, and a 32-bit float. By default, the inferred type of a floating point number will be double. So, if I create another variable here called inferredDouble, and I set it to the value of 5.5, this value will automatically be inferred to be a double. If I want my inferred value to be a float, we can specify that explicitly by adding an f at the end of the floating point value. So in this case, we'll type, val inferredFloat = 5.5f, and in this case, our type will be float. We can easily compare values between numeric types as well. We can use equal equals to check if two values are the same. So, if we do a simple println here, and check if 5 == 4, this will let us know if the value of five equals the value of four.
>
> **[5:21](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/numeric-data-types-14803696?u=76281980&t=321)** We could also use the greater than symbol to check if a value is larger than another. So in this case, if 10 is greater the than one, notice that 10 here is explicitly defined to be a float. And similarly, we could use less than or equal to to check if a value is smaller than or equal to another. In this case, 10.1 less than or equal to 5.2.
>
> **[5:56](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/numeric-data-types-14803696?u=76281980&t=356)** Now, if go ahead and run these comparisons, we should see the outputs of each evaluation, and notice that we see false, true, and false. This should make sense as five is not equal to four, 10 is greater than one, and 10.1 is not less than or equal to 5.2. In all of these examples, we are able to compare values without explicitly worrying about their type. The numeric types will handle any necessary conversion to ensure an accurate comparison is made. It's also easy to convert between numeric types. Most types have available conversion methods to convert from one type to another. For example, to convert a double to an Int, we could do so by using the toInt function. So, I will type, println, so we can print out the value of this conversion, and we'll say, 10.5.toInt. If we run this code, then, we'll see that 10 has been printed out to the console. The conversion from double to Int effectively drops any decimal place and gives us just the whole number value. We could also easily convert from, say, 10.9,
>
> **[7:32](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/numeric-data-types-14803696?u=76281980&t=452)** which will be inferred to be a double, to a float representation of that value, or, as another example, we might convert from an integer five to an unsigned long. While conversion is simple, we must be careful when can converting a large type to a smaller type, as we may lose precision in the process. Let's see what happens when we try to convert 100 million to a byte. To start, I'm just going to define a literal for 100 million using a special syntax that Kotlin provides for defining numeric literals. So, I'ma type, 100, and then, rather than continuing to add more zeros to my value of 100 million here, I'm going to use an underscore to effectively represent where we would put the decimal separator if we were writing this out. So, now I'll add three more zeros, another underscore, three more zeros. This is all representation of 100 million, and makes it a little bit easier to read. And now, if I try to convert this to a byte using the toByte method, the compiler has no problems with this. Now, I'm just going to print this out, and if we look at the output, we will see that zero is the resulting value from this conversion from 100 million to a byte. This is because the resulting byte
>
> **[9:08](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/numeric-data-types-14803696?u=76281980&t=548)** is taken from the least significant bits of the larger long. While converting from a smaller type to a larger one is generally safe, we must be careful we don't lose accuracy when doing the reverse. Throughout this course, we'll keep things simple and use mostly Int and double. But as you go on to develop more complex applications, think about the data you're representing and whether you may be able to represent that data using a more efficient data type.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (3)
> **Code Identifiers:** toint (2), aint (1), anotherint (1), abyte (1), ashort (1)
> **Versions:** 10.1 (2), 5.2 (2), 5.5 (1), 10.5 (1), 10.9 (1)
> **Analogies:** for example (2)
> **Warnings:** be careful (2)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Char and string data types](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/char-and-string-data-types-14804064?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/char-and-string-data-types-14804064?u=76281980&t=0)** - [Instructor] In addition to numeric data types, most programming languages include support for text based types as well, and [[Kotlin]] is no exception. So let's jump in and explore how to work with text in Kotlin. To represent a single character, we can use the char data type. Char variables can be used to store a single alphabet character. Or a single numeric character. We can also use char to store symbols such as an escaped new line. In each of these examples, the character was defined in a single quote literal. This is an important distinction to remember as we eventually work our way into other text-based data types. Now, what if you wanted to combine two char variables together? Is that possible? How would we do it? Let's take a quick look at a couple of options. The first thing we might try is to concatenate two chars together. So we might try that by defining a val and then saying something like concatOption1 equals a literal plus b literal. This doesn't work however, as the plus operator isn't designed to work with chars. And this makes sense based on the next example here. Rather than concatenating two chars,
>
> **[1:35](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/char-and-string-data-types-14804064?u=76281980&t=95)** let's simply define a char literal with two characters. So this time, we'll just define that literal to be a and b within a single, single coded literal. Again, this fails. But this time with a more helpful error message. If we hover over that error message, we'll see that it says too many characters in a character literal ab. The compiler is clearly telling us that a char data type may only store a single character. So to store multiple characters or entire sentences and paragraphs, we need another data type. And that's where strings come into play. String is the go-to data type in Kotlin for storing multiple characters together at once. To declare a string variable, we can use double quotes to define a string literal and assign it to our variable. In this case, we have a string representing the value, Hello, Kotlin! We can define two different types of string literals. The first are escape strings and are what we just saw. Within escape strings, we can use escaped characters such as new line to provide formatting to the screen. For example, if we define a string called escaped, we will be able to insert a new line character between the string, Hello
>
> **[3:11](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/char-and-string-data-types-14804064?u=76281980&t=191)** and World. If we then print out this escaped string, we'll be able to see that we have a new line in between these two words. However, before we do that, we need to comment out our compiler errors here, otherwise our program won't compile and run. So let's go ahead and run our code. And we will see that Hello \n World are broken up into two separate lines. This is because we were able to insert that new line character within our string literal. The second type of string literal is a raw string. These are strings defined using triple quotes. These strings allow for extra flexibility in how they are formatted. So if we write out a raw string here, we can add triple quotes. And then within that, we can use multiline text here with different formatting.
>
> **[4:24](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/char-and-string-data-types-14804064?u=76281980&t=264)** By default here, the IDE is going to try and add this call to trim indent. Go ahead and remove that for now so that we can see the raw output. And once we have our raw string, we'll go ahead and print that out.
>
> **[4:45](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/char-and-string-data-types-14804064?u=76281980&t=285)** And we'll see in the output here that our multiline text is being printed out to the console just like it was defined in our IDE. So all of that white space, all of that formatting, is preserved within this raw string. If we remove the indentation, let's say from our first line, we'll see that that indentation is also removed from the output. We can improve this a bit by adding some type of dilimiter character to the beginning of each line in our raw string and then calling trim margin. So if I come back here to Some, I will add just the pipe character here before a couple of our lines. And then after the trailing triple quotes, I'll add back that call to trimMargin. Once again, I'm going to run this code. And this time, we will see that the places where we added the pipe character are now no longer being printed out to the console with that prepended space. So again, this gives us extra means of formatting these raw strings. We could also customize this further by choosing a different margin character. So rather than a pipe, we could do let's say, two angled brackets here.
>
> **[6:19](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/char-and-string-data-types-14804064?u=76281980&t=379)** And then when we call trimMargin, we can pass in a string literal representing what we want that margin character to be. And now if we rerun the code once again, we'll see we still get that nice trimmed margin formatting, but this time with those custom margin characters. Strings can be easily combined and formatted in Kotlin. To concatenate two strings, we simply use the plus operator. So in this case, I'll concatenate Hello plus Kotlin. If we were to print out this resulting string, we'll see Hello World printed out to the console. We can also concatenate non strings with strings. So in this case, we could create a variable called concatNumber equals a string called The number plus the value of 10. If we print this out to the console, we will see that our output has automatically taken the string representation of that number and concatenated with the first string literal, The number. This is a good time to discuss imutability in Kotlin strings. A string in Kotlin is imutable. Once defined, it can't change. So in these examples, we are actually creating two separate strings, then combining them to create a third. This isn't the most efficient way
>
> **[7:52](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/char-and-string-data-types-14804064?u=76281980&t=472)** to go about this operation. The more Kotlin idiomatic approach to this type of string formatting is to use string templates. Kotlin allows us to create string literals that directly substitute in other values. So for example, let's say we have some numeric literal called aNumber holding the integer value of 10. We could rewrite our numeric concatenation using a string template this time. So we'll create a variable called concatTemplate equals, again, The number. But this time, we'll create a placeholder in the string to represent the number. We can do this placeholder by adding a dollar sign. And then we will reference that aNumber variable. If we print this out to the console...
>
> **[8:57](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/char-and-string-data-types-14804064?u=76281980&t=537)** We'll once again see our expected output. However, this time, we didn't have to create multiple strings to generate this output. This string templating works for more complex expressions as well. If we wanted to, let's say, always add one to the number before printing it out, we could add curly braces after the dollar sign. And within those curly braces in the string literal, we can then exercise arbitrary expressions, meaning we can effectively run code in here. So rather than just always passing the aNumber variable, we could say one plus aNumber. If we then rerun our example, this time we'll see the number 11 printed out to the console. So this gives us a lot of power and flexibility to create formatted strings in a more efficient way. Now, strings are a very common data type in Kotlin development, especially if working in user facing domains such as [[Mobile Development]]. To make strings easier to work with, the Kotlin Standard Library provides many useful functions for us. For example, if we define some variable called someString with the value, Hello there!, we could check whether that is empty or not by saying someString.isEmpty. If we were to print out that result,
>
> **[10:40](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/char-and-string-data-types-14804064?u=76281980&t=640)** you will see false. Which is likely no surprise as the someString variable is not empty. It holds the value of Hello there! If we wanted to check the reverse of this, we could use isNotEmpty. And if we rerun our code, this time we will see our final output is true because it is not empty. If we change the value of our someString variable to be just a couple of blank spaces and rerun our code, this time we see true printed out to the console. Why do you think that is? Well it's because the space is still a valid character even if we can't see it. So in this case, multiple blank characters is still not empty. Alternatively, to isNotEmpty, we could check isNotBlank. This time we see falses printed out because the isNotBlank function checks for white space characters as well. So even if there are a hundred white space characters, isNotBlank is still going to return false. In addition to helper methods like isNotBlank or isEmpty, we can also use the contains function to check if a string contains another string. So we'll create another variable here called
>
> **[12:15](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/char-and-string-data-types-14804064?u=76281980&t=735)** anotherString equals Hello Kotlin World! And if I wanted to check that that string contains Kotlin, I could do so by typing anotherString.contains and passing in the string literal of Kotlin. There are many other really useful helper functions on this string type, and as we proceed through this course, we will use examples of more of them. Char and string are our go-to types for storing any text-based values in Kotlin. In particular, strings provide a great deal of flexibility and utility through different literal types, string templating, and the Kotlin Standard Library. I encourage you to explore on your own and see what other useful string functions you can find.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (17), [[Mobile Development]] (1)
> **Code Identifiers:** anumber (4), somestring (4), isnotblank (4), trimmargin (2), isempty (2)
> **Analogies:** such as (3), for example (3), just like (1)
> **Definitions:** is a  (2), is an  (1)
> **CLI Commands:** make (1), find (1)
> **Env Vars:** ide (2)
> **Exercise Files:** template (1)
> **Speakers:** - [instructor] (1)

#### [Nullable types](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/nullable-types-14797834?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/nullable-types-14797834?u=76281980&t=0)** - [Instructor] What do we do when we have a variable whose value is not available at declaration time? How do you represent the absence of data in [[Kotlin]]? There are a couple of approaches to handling this scenario but the most basic is the use of null values. So let's explore how Kotlin differentiates between null and non-null data types to avoid the dreaded null pointer exception. Null is a special value representing the absence of any real meaningful value. We can assign null as the value of a variable when we don't have a good initial value available to us at declaration time. Let create a new mutable string variable by typing var_aNullableString: String = null.
>
> **[0:56](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/nullable-types-14797834?u=76281980&t=56)** We are attempting to use null as the initial value for this variable. However, the compiler is telling us that this is an error. Why do you think that is? Well, the answer is that Kotlin differentiates between nullable and non-null types and by default, types are non-nullable, meaning they cannot hold a null value by default. This is done in an effort to avoid using null whenever possible, as null can be the source of many errors in our code. If we really want to hold a null value, we can turn any non-null type into a nullable one by simply adding a question mark after the type name. After this update, we're now free to assign both null and non-null values to our variable. Kotlin defaults to non-null types because null has historically been the source of many bugs and headaches in the [[Java]] ecosystem. Most notably, null values can lead to null pointer exceptions when a null value is accessed. So if we're going to use nullable types, we need to be mindful about how we access those values so we don't crash our applications. Let's try to print out the length of our variable here to demonstrate a null pointer exception. We'll start by removing this line where we updated the variable value.
>
> **[2:30](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/nullable-types-14797834?u=76281980&t=150)** And then we'll add a call to println and we'll access the length property of our string by typing .length. Now, if we hover over the dot here, we'll see the compilers giving us an error. And in the verbose language, it says only safe or non-null asserted calls are allowed on a nullable receiver of type nullable string. This error is basically a long way of saying that we're trying to access a possibly null value in an unsafe way. To make this code compile and run safely, there's a few options. The first option and the least safe is to add a double exclamation mark before the property access. Adding double exclamation asserts that a value is not null before accessing it and throws an exception if the value is null. This makes code easier to write, but leads to crashes. If we run this code, we'll get a null pointer exception printed out to the output here. This is because we tried to access the length property on our variable, which is still set to null. If we update the value of our string and print out the value once again, this time we correctly see the output length of that string.
>
> **[4:06](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/nullable-types-14797834?u=76281980&t=246)** Another option for handling null is to use a null safe call. Null safe calls will only access a property or call a method, if the value is non-null. To demonstrate this, let's set our variable back to null. And now, instead of using the double exclamation point, we will replace it with a question mark. What do you think will happen now when we run this code? If you guessed that it would print null, congratulations, you are correct. The null safe call will prevent throwing a null pointer exception in this case and will actually return null instead. So now our code doesn't crash but instead it actually prints the [[Microsoft Word|word]] null out to the console, which maybe isn't as helpful or as interesting as we would like it to be. To improve this, we'll introduce one more operator for working with null and that's the Elvis operator. The Elvis operator allows us to evaluate an expression and return some default value if that expression evaluates to null. So if we place our cursor after the call to .length, we can add a question mark, colon. This is the Elvis operator and now we can provide some default value. In this case, a string saying the value was null. What we've effectively done here is told the program
>
> **[5:44](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/nullable-types-14797834?u=76281980&t=344)** to try and print out the length of our variable but if the variable is null, print out this default string instead. So now if we run our code, we see the value was null printed out to the console. If we change the value of our variable to Kotlin and run it one more time, we see six, the correct length of the Kotlin string. Sometimes we have no choice but to use nullable types and in those situations, we want to be sure to handle them safely. So as you continue on with Kotlin, think carefully when introducing null values.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (7), [[Java]] (1), [[Microsoft Word|Word]] (1), cursor (1)
> **Definitions:** is a  (1), is an  (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)


### 3. Working with Functions

[↑ Back to Table of Contents](#table-of-contents)

#### [Basic functions](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/basic-functions-14802632?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/basic-functions-14802632?u=76281980&t=0)** - [Narrator] How do we write self-contained reusable pieces of code that we can then invoke and reuse throughout our code bases? Well, in [[Kotlin]] we start with functions. In Kotlin functions are first class concepts. We can write and use functions without any kind of enclosing class, struct, or other obstruction. Because of this, functions in Kotlin are incredibly flexible and important. So let's start by building up our understanding of function basics. Here in IntelliJ, we have an example of our first real function. This function is our main function. We've been running this repeatedly. And in this example, it's going to print out the string hello world to the console as we've seen before. Now, let's walk through the key elements of this first function. So first off we have the fun keyword. This represents a function. This is how we know that what we are defining is going to be a function. Then we have the function name, in this case main. We have empty parenthesis. This is where we will eventually put arguments in our functions so we can pass values to them. And then we have the function body which is everything inside of the curly braces. Now let's write our own function to print out a greeting. But before we do, has anyone spotted the second function
>
> **[1:37](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/basic-functions-14802632?u=76281980&t=97)** in our example so far? Println is also a function, and it demonstrates how we can call or invoke a function from within another function. So now we should be ready to create our own greeting function. So again, to create a function, we're going to start by adding the fun keyword. Then we're going to add our function name, in this case, printgreeting. It's going to take no arguments, so we'll add an open and closed parenthesis. And then we will go ahead and add an empty function body using curly braces. Now for the body, we will once again, leverage the println. So I'm going to just copy the println line from our main function and move it inside of printgreeting. And now from within main, we will go ahead and invoke printgreeting instead of using print line directly. Now if we run this code, we should see the same hello world output that we had before. However, this time it's getting there in the console from our printgreeting function that we just wrote. Now, what if we wanted to return a meaningful value from our function? The most basic way is to define a return type for our function, and then return a value from our function body.
>
> **[3:12](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/basic-functions-14802632?u=76281980&t=192)** So let's refactor printgreeting to getgreeting and return a string rather than printing it. So, first off we will change the name from printgreeting to getgreeting. And now we want to explicitly define the return type of this function. To do that, we'll move our cursor after the parenthesis. And similar to defining the type for a variable, we will add a colon, and then we are going to add the string type. So this signifies that getgreeting is now going to return a string. Now within our function body, rather than printing out hello world, we're going to use the return keyword to signify that we're going to return a value from this function. And then we will explicitly return the string hello Kotlin. Now from our main function, rather than printing out to the console directly when calling printgreeting, we will call println and print out the result of calling our getgreeting function. And once again, if we run the code, we'll see hello Kotlin printed out to the console. Now, you might be asking the question, why didn't we have to define a return type for our printgreeting function or for our main function?
>
> **[4:48](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/basic-functions-14802632?u=76281980&t=288)** Well, the answer is because both of those functions are inferred to return type unit. We could rewrite main here to represent this by adding a colon and then the unit type after that colon. Now unit represents a non-meaningful return type. So it can be explicitly defined as the return type like we just saw on our main function. We could also explicitly add a return unit to the end of our main function, but both can be omitted to allow for a more concise code. So you typically wouldn't define a function that returns unit or return unit explicitly. Now, another question you might have is do we always have to explicitly define the return type of a function? And the answer to this is actually no. No type is needed if the return type can be inferred. To demonstrate this we're going to refactor getgreeting once again. So rather than explicitly defining the return type and adding the function body, we can actually just add an equal sign and then provide the value we want to return from our function. So in this case, we've defined it as getgreeting equals hello Kotlin. This is known as a single expression function. And the return type of this will be inferred
>
> **[6:22](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/basic-functions-14802632?u=76281980&t=382)** to be of type string because the right-hand side of the expression here will evaluate to a string. This is very similar to type inference for variables. This works for unit functions as well. If we were to once again type a printgreeting function here, we could then say println, hello Kotlin. And this function would be inferred to return unit. To round out our basic function types, let's take a look at local functions. A local function is a function defined inside of another function. So let's move our getgreeting function inside of our main function. So I'll do that simply by copying and then pasting that function within main. So if we run this code once again, we'll see that it prints hello Kotlin just as it did before. However, this time we can now longer access getgreeting outside of the main function. So if we tried to update printgreeting to print out the result of getgreeting, we'll see that we get a compiler area. It's not accessible in that scope. A local function may only be called from within its enclosing function.
>
> **[7:54](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/basic-functions-14802632?u=76281980&t=474)** So in our case, like we just saw, that means only main may now call getgreeting. Why would you use this? Well, you might use it to organize reused portions of code within a function without exposing that code to other functions within the file or the application. Local functions can scope one function to only be used by another. Does that mean all other functions are open for public use? Well, the answer is kind of. By default, all functions are public. That means they are available to use within any file in the project or any project that depends on our project. If we add the public keyword to the printgreeting function, it won't impact its visibility. And in fact, if we hover over the public keyword, we'll see that the compiler gives us this warning saying redundant visibility modifier. This is because things are public by default. However, if we change it to private, that suggestion by the IDE goes away, and now only the current file can access this printgreeting function. There are additional visibility modifiers as well. In particular, there is an internal modifier that makes the function visible to only the current build module. But we won't need to rely on that during this course. As we discussed, functions are a very important part of the Kotlin language.
>
> **[9:27](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/basic-functions-14802632?u=76281980&t=567)** Take some time to make sure you're comfortable writing basic functions. Try writing your own function to return your name, your favorite number, or some other simple value. Once you've written a few functions, you'll be ready to move on to even more interesting functions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (9), cursor (1)
> **Definitions:** defined as (1), known as (1), is a  (1), is an  (1)
> **Analogies:** similar to (2)
> **CLI Commands:** make (1)
> **Env Vars:** ide (1)
> **Cross-References:** we discussed (1)
> **Tools:** intellij (1)
> **Warnings:** warning (1)

#### [Function parameters](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/function-parameters-14802638?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/function-parameters-14802638?u=76281980&t=0)** - [Narrator] Functions are more interesting and certainly more useful when we can pass values to them that can then be used within the function's implementation. With that in mind let's learn how we can pass argument values to our functions. To pass an argument to a function. We must first define a function parameter. We can do that by specifying a parameter name and a type within the functions parentheses. So let's refactor our getGreeting function to take in the thingToGreet rather than just always saying Hello [[Kotlin]]. So within the parentheses of our getGreeting function we will first define the name of the parameter. In this case, we will call it thingToGreet. And then we will use colon followed by string to indicate that the parameter should be of type string. We will then update our return value to use the past argument value when returning our string. So we will replace Kotlin with dollar sign thingToGreet. And if we remember this syntax by using the dollar sign and then the name of our parameter here we will substitute in whatever argument value is passed to the function in our returned string. And now finally, we must pass a value to getGreeting now when we invoke the function.
>
> **[1:36](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/function-parameters-14802638?u=76281980&t=96)** So if we come down here into our main. If I pass in the string Kotlin and run the code we will see the same hello Kotlin Output that we've seen before. Now, we can also add more than one argument to a function as well. So let's further refactor getGreeting to take in the greeting as well as the thingToGreet. So again, here I'll come into the parentheses. I'm going to hit space once just to give myself a little bit of room. And I'm going to name this new parameter greeting and it will also be of type string. And now I separate my parameters by using a comma. And then once again here now I will replace the hard coded Hello string and use a substitution here with the greeting parameter value. And now I will also update the invocation of getGreeting. And this time I will pass the string hello. And go ahead and run the code one more time. And we'll see again that we have preserved our same Hello Kotlin greeting, but using our new refactored function. Now just before moving on, let's look at what we've done. Notice that the syntax is generally the same here whether we're using one parameter or multiple. The only real difference is we separate the parameters with a comma,
>
> **[3:08](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/function-parameters-14802638?u=76281980&t=188)** and then we have to pass both values when invoking that function. Sometimes a parameter may have a sensible default value. For example maybe our default greeting should always be Hello. For situations like this Kotlin allows us to provide a default argument value for a parameter. Parameters with default argument values can be omitted when invoking the function. Let's update getGreeting to use world as the default thing to greet. So to add a default parameter we're going to come to the parameter definition in our list. And after the type I'm going to add an equals and then the string literal World. Once we do this, we can now remove the passing of Kotlin to our getGreeting function below. And if we run the code, we'll now see that it prints out Hello World, rather than Hello Kotlin. If we give a default value to both parameters then we can invoke getGreeting without passing anything. So we will update getGreeting to have a default for the greeting as well. And I'm just going to refactor this a little bit to make it easier to follow here. I'm just going to split the arguments onto their own lines. And now after the greeting parameter type
>
> **[4:42](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/function-parameters-14802638?u=76281980&t=282)** I will add equals Hello as the default greeting. Now I can remove any argument value being passed to getGreeting, and run this again. And this time we will see Hello World printed out to the console. If I instead passed in Hey, and ran this again. Now I see Hey World. With our current getGreeting function. What would happen if we only wanted to change the value of thingToGreet? We could pass a single string to our function like we just did, but how would the compiler know that we want that value to be associated with thingToGreet rather than greeting? The short answer is that the compiler doesn't know that by default, if we were to pass let's say World to our function here, the output is going to be World World which isn't what we want. To solve this problem Kotlin supports a feature called named arguments. Named arguments let us pass values for specific arguments by referencing the parameter names directly. If we want to use the default value for our greeting but a custom value for the thingToGreet parameter, we can update our code to reference thingToGreet directly. So in to do that,
>
> **[6:17](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/function-parameters-14802638?u=76281980&t=377)** I'll come into the invocation of getGreeting and I will type thingToGreet equals. Now if I run this rather than printing World World it prints Hello World. It now recognizes that the World string that we're passing to it should be assigned to the thingToGreet parameter which means that the default greeting value will be used for the greeting parameter. Using named arguments were also then free to provide argument values in any order we want, as long as they're named. So even though the order of the parameters is greeting thingToGreet, we could use named arguments to change the order. So I could now pass greeting equals hey there. And if we run this again, we'll see Hey There World printed out to the console. This combination of default parameter values and named arguments is really powerful. It can enable us to write a single function that can be invoked in many different ways. Reducing the need for overloaded functions. Like so many things in Kotlin function parameters are flexible and feature rich. By leveraging this functionality we can write more expressive and powerful functions with fewer lines of code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (10)
> **Code Identifiers:** getgreeting (12), thingtogreet (11)
> **CLI Commands:** make (1)
> **Definitions:** means that (1)
> **Analogies:** for example (1)
> **Speakers:** - [narrator] (1)

#### [Functional types](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/functional-types-14803699?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/functional-types-14803699?u=76281980&t=0)** - [Instructor] Functions in [[Kotlin]] don't require any kind of enclosing class. They stand on their own as top-level constructs of the language. This has interesting implications for how we write and organize our Kotlin code. One way in which this is made apparent is by the fact that functions can be used as data types in the same way we might think to use string, int, or any other type. To illustrate this point we'll explore how to define variables with functional types, and how to invoke those function variables. We can define a variable with a functional type. Rather than using an explicitly-named type, such as string, we must describe the function signature for the function type we want to work with. Specifically, we must specify what arguments are needed by the function and what the return type should be. So, here in our Main KT file I'm going to hit Enter a couple times to give myself some space. I'm going to define a new top-level variable here. So, we're going to use var so that we can reassign this value, and we're going to name the variable greetingFunction. And so, now we need to give this variable a type. So, again, we'll use a colon like we have with other variable declarations, and now we want to define a functional type. So, like we said, the first thing we need to specify for this function signature are the parameters. And in this case, we can specify that this function should take no parameters by using open and closed parentheses. And then next, we need to add a dash and the arrow, signifying this arrow symbol here.
>
> **[1:39](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/functional-types-14803699?u=76281980&t=99)** And then we can specify the desired return type, in this case, Unit. So, now greetingFunction is defined to represent the function that takes no parameters and returns Unit. Now, for the moment we're not going to initialize this function. Instead, we're going to come down into our main and demonstrate how we would actually call this function. So, we can invoke our function in a couple of different ways. First, we could invoke it just like a regular function by referencing the name of the variable and open and closed parentheses. Or alternatively, we could use a special invoke method that gets added to any functional type. These two are equivalent, and most of the time you'll likely use the first syntax where you simply invoke the function variable like it was any other named function. Now, like we've already said, in both of these cases we will get an error here because the function has not been initialized. This is because we haven't yet defined what this function should actually reference, or what it should actually do. We've simply defined a variable that will hold a function in the future. To go ahead and initialize our function variable we'll come back up to our line here, and we'll add an equal sign and then open and closed curly braces defining the function body. These curly braces are what's known as a lambda.
>
> **[3:13](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/functional-types-14803699?u=76281980&t=193)** Essentially, it represents some function, some function body that will be executed when this function is called. And in this case, this function body does nothing at the moment. We could change that by adding a call to println, and printing a Hello Kotlin greeting. And now, if we run our code, we will see Hello Kotlin printed out to the console twice. If instead of printing it out twice we print it once and then reassign the function stored by that variable, in this case, we'll assign it a function that will say Hello World. Now, if we print this out we will see that the first function is run when we invoke greetingFunction the first time, and we see Hello Kotlin. Then we reassign the greetingFunction variable using this lambda here that prints Hello World instead. And then when we invoke greetingFunction again, this time we see Hello World printed out to the console. Notice that when we reassign the value we don't need to respecify the arguments or the return type. Those are all already defined in the function signature when we declared the variable. Now, like any other function, we can define functional types that include parameters
>
> **[4:48](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/functional-types-14803699?u=76281980&t=288)** and-or return values. So, let's update greetingFunction to take in a string for the thing to greet. So, first, we will update the parameter list to include the string type, and then we will update our return type here to return a string as opposed to returning Unit. So, now, we want to update the function body here, and instead of printing out a string we're going to return Hello $it. Now, this might look a little bit confusing. What is this it we are referencing here? Well, this it parameter here, or this it variable, is implicitly given name for the parameter passed to the function. Any argument passed will be available as it. However, this isn't always very readable. And instead, we can often override this naming. Within the lambda we can introduce thingToGreet as a named parameter by defining thingToGreet right after the open curly brace, and then specifying the arrow operator again here. So, what this effectively does is renames that string parameter from the implicitly named it
>
> **[6:22](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/functional-types-14803699?u=76281980&t=382)** to this more explicitly named thingToGreet. And so, now, when defining our string literal here we will reference thingToGreet directly. Now, with this in place when we invoke our function we can pass different argument values each time we invoke the function. So, we could pass World the first time, or we could pass let's say Kotlin an additional time here. Now, notice below as well that when we are reassigning our variable here we have a compiler error. And again, this is because this is using the old signature that is returning Unit as opposed to a string. So, to replace this we could simply change it from printing the string to implicitly returning the string. And now notice, in both of these cases when assigning the function variable we're returning a string without an explicit return. This is a special property of lambdas. Within a lambda you don't have to explicitly return the value using the return key [[Microsoft Word|word]]. Whatever the last value is within the lambda will be implicitly return. And the last thing to just notice here before moving on, is that in addition to calling the greetingFunction variable like a regular-named function where we pass in the parameter values, when we call it using invoke we also have to specify the parameter values as well.
>
> **[7:58](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/functional-types-14803699?u=76281980&t=478)** So, any parameters that we add must be passed whether invoking it directly or using the invoke method. So, to just illustrate what we've done here, if we go ahead and run our code one more time we'll see that nothing is actually printed out. And this is because we changed our function to return a string as opposed to printing out a string. So, if we just update a couple invocations here we will wrap these calls in println calls.
>
> **[8:41](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/functional-types-14803699?u=76281980&t=521)** And now, if we run this one more time this time we will see Hello World the first time, Hello Kotlin the second time, and then lastly, Hello World once again. Now, just something interesting to notice here, in our very last invocation when we called the invoke method we passed in World. And when we see the output we actually see Hello World!. So, there's a small, subtle difference here. When we reassign the value of greetingFunction we actually are no longer using the parameter passed into it. If we changed Hello World! to Hello there and rerun, it'll be more noticeable that that second greetingFunction is not using the parameter passed to it. So, to update that we could once again use either the implicit it parameter made available within the lambda. Now, we see Hello World! printed out. And like we did the first time we defined our function value, we could make this a little bit easier to understand by renaming the it parameter as thingToGreet, adding our arrow operator here, and renaming the parameter reference within our function. So, this time if we run this one more time
>
> **[10:20](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/functional-types-14803699?u=76281980&t=620)** we see Hello World!. And if we change this to Kotlin Programming, run it one last time, we'll see Hello Kotlin Programming. So, this is a good means of highlighting the fact that we are reassigning the implementation of that function every time we update the value of the variable. So, if we're going to be using functions in this manner we need to be careful that we implement them consistently and assign them consistently, otherwise we might end up with unexpected results. The ability to store functions as variable values can be quite useful. Imagine allowing a user to remap input handlers to suit their preferences. The variables holding each input handler could be swapped to different functions based on user preferences. This won't be the last time we work with functional types. Functional types are the foundation of another powerful feature of Kotlin, higher-order functions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (10), [[Microsoft Word|Word]] (1)
> **Code Identifiers:** greetingfunction (9), thingtogreet (5)
> **Definitions:** is a  (2), known as (1), is called (1)
> **Analogies:** such as (1), just like (1), imagine (1)
> **CLI Commands:** make (1)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)

#### [Higher-order functions](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/higher-order-functions-14802627?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/higher-order-functions-14802627?u=76281980&t=0)** - [Instructor] Functions can be used as explicitly defined types and functions can take in other typed values as parameters. So by extension, it might come as no surprise that we can pass functions as arguments to other functions. This type of function is known as a higher order function and is an integral part of the flexibility and power of the [[Kotlin]] standard library. To help build our understanding of both functions and the Kotlin standard library, let's dive in and explore how to pass a function to another function. So, in our IDE here, let's define a function that prints out some calculated value. So we'll come above our main function, hit return a couple times to give ourselves some space and we can start by defining a function named printCalculatedValue, and it'll take no parameters to start and we'll just give it an empty function body. Next up, we want this function to take in two integer values. So we will update our parameter list to add value1 of type int and value2 of type int.
>
> **[1:14](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/higher-order-functions-14802627?u=76281980&t=74)** And again, still leaving the function body blank for now. Ultimately, these values are going to be used to calculate some result that will be printed out to the console. Next, we want to define a function argument that will control how the values are calculated. We do this similar to how we would define a functional variable. So within our parameter list, I'll add another comma after value2, and I'm going to first define the name of this functional parameter and I'll just name it calculator for now. And then we need to define the functional type like we would with functional variables. So, this function signature is going to take in two ints and then it is going to return some integer value. You might notice some symmetry here where we are passing two ints into the printCalculatedValue function and then our calculator parameter function is also going to take in two ints. Now that we have our parameter list setup, we can use those parameters to implement the body of our higher order function. So like I mentioned before, we're going to print out some value calculation. So we'll start with a call to println, and then we want it to say the value is, and then now we're going to use a string substitution here, so I'll use dollar sign, open and close curly brace.
>
> **[2:50](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/higher-order-functions-14802627?u=76281980&t=170)** And now within the substitution, we want to make use of the past calculator function to calculate the value and then print that out in our string here. So to do this within our template here, we can use the name of the calculator function and just like we did with function variables, we can pass in value1 and value2 to this calculator function. So now, when printCalculatedValue is invoked, we are going to print out the value is, and then whatever the result of that calculator function might be. This will make much more sense in a moment, once we see how we would actually call this function. Once our function is implemented, we can start using it and this is where it gets fun. In the most straightforward invocation, we need four things. We need to use the name of the function to invoke it and then we must pass in the first and second value. So to start, we'll pass in two and two and then finally we need to pass in a Lambda that matches the function signature that we define in printCalculatedValue. So remember that function signature says that our function argument must take in two int arguments and return an int. So, I will add a comma here. I'm going to add open and closed curly braces for my Lambda. And then within that Lambda,
>
> **[4:23](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/higher-order-functions-14802627?u=76281980&t=263)** I'm just going to rename those parameters as value1, value2 and then I will use the arrow symbol here. And now we can define the calculation. We can actually define the body of this function that we're passing to printCalculatedValue. So in this case, we're going to do value1 plus value2. So this function that we're passing to printCalculatedValue is going to add up the two values passed to printCalculatedValue. And now if we run this code, we see the value is four, print it out to our console. So we passed in two and two and the function that we passed as well, added those two values and then that calculation was used to print out the string the value is four. So that is a pretty basic example but what if we wanted to instead subtract the values rather than adding them? Well, we could invoke this function again but with a different implementation of the Lambda this time. So again, we'll call printCalculatedValue, pass in two and two. And this time within our Lambda, we will say value1 minus value2. And if we run it, this time we see the value as four
>
> **[5:58](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/higher-order-functions-14802627?u=76281980&t=358)** from our first invocation and for the second invocation, we see the value as zero because two minus two is zero. This is the power of higher order functions. Being able to define this one printCalculatedValue function by giving it the flexibility to take in another function that actually determines how the calculation is performed. This makes it very flexible for us to define strategies or how certain pieces of [[Algorithms]] should behave while being able to customize other portions of those algorithms. Now, the current invocations of these functions is a little bit verbose. It requires making sure the Lambda fits nicely within the parentheses when calling printCalculatedValue. While this is very easy for simple functions, it breaks down for more complex ones where we might have more lines of code. It makes it harder to read. This is where trailing Lambda syntax comes into play. Trailing Lambda syntax allows us to move the final Lambda of a parameter list outside the parentheses. Now, what does this mean in practice? Well, if we were to refactor the invocation of printCalculatedValues, what we could do here is actually move the final parentheses after the second integer value we passed in and pass the Lambda outside of those parentheses. So we can see here, the difference
>
> **[7:30](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/higher-order-functions-14802627?u=76281980&t=450)** between the first invocation of printCalculatedValue and the second one. In the second one, the parentheses only include two and two and then the Lambda goes after. In the second one, the Lambda goes within the parentheses. The first version here is trailing Lambda syntax and it makes it feel a little bit more fluent when we're writing out these functions that take a function argument. By applying this trailing Lambda syntax, we moved the function Lambda outside those parentheses which makes it look more like a traditional function body. This often makes the Lambda more readable and easier to understand. Higher order functions are a very common pattern within the Kotlin standard library and within idiomatic Kotlin code in general. As you work through this course or read other Kotlin code, keep an eye open for examples of higher order functions and think about how they support flexible expressive code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (5), [[Algorithms]] (2)
> **Code Identifiers:** printcalculatedvalue (11), printcalculatedvalues (1)
> **Definitions:** is a  (2), known as (1), is an  (1)
> **CLI Commands:** make (2)
> **Env Vars:** ide (1)
> **Exercise Files:** template (1)
> **Analogies:** just like (1)
> **Prerequisites:** setup (1)

#### [Challenge: Write a name formatter function](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/challenge-write-a-name-formatter-function-14804061?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/challenge-write-a-name-formatter-function-14804061?u=76281980&t=0)** - To help reinforce the concepts of functions, functional types and higher order functions, here's a challenge for you. Write a function to print formatted names to the console. This function should take two strings representing first and last name, and it should take a function format parameter that controls how the actual formatting is done. Your function should use the result of the formatting function parameter to print out the formatted name to the console. When testing your code, try calling your function more than once using different format functions each time. This should help highlight how higher order functions can help extract specific decision making out of a function's core algorithm, allowing for more reusable functions.

> [!info]- Semantic Content
>
> **Speakers:** - to (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Write a name formatter function](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-write-a-name-formatter-function-14799784?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-write-a-name-formatter-function-14799784?u=76281980&t=0)** (bright upbeat music)
>
> **[0:05](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-write-a-name-formatter-function-14799784?u=76281980&t=5)** - [Instructor] How did the challenge go? Were you able to write your Name Formatter? There's plenty of ways we could implement such a function. So let's just explore one such implementation of a Name Formatter higher order function. First, I like to start by just stepping out the implementation by defining the function name. So in this case, I'll define my function name as printformattedname. And I'll start with empty parameter list and an empty function body. Then we can start to think about how we want to use this function. We want to format a first and last name so it makes sense to include both a first and last name parameter to our function. And because names are commonly text we'll use string types. So we'll add a first parameter of type string and a last parameter of upstream. Now we're ready for the interesting bit. We want to pass a function that will format our first and last names and then return that formatted value. So what should our formatting parameter type be? Well, I'll start by just adding the name for that formatting function parameter. And then when we think about the parameters, we probably should have two inputs, one for first name and one for last name, and both of these will be of type string. And we want to format these in some way
>
> **[1:40](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-write-a-name-formatter-function-14799784?u=76281980&t=100)** and return a value to print out to the console. In reality, we could return any type we wanted but since we're working with names and console output, a string return type is appropriate. So I'll finish off the function type definition here by indicating that it should return a string. So putting all these pieces together, we're now ready to implement the body of our function. We will use a call to print ln, and we will simply pass in the result of calling the formatter function and passing in first and last to that formatting function. So whenever print formatter name is called, the formatting function passed to it will be invoked passing first and last name to that function. That formatter will return a string and print formatted name will print that string to the console. Let's now test our function by creating two different format functions, and we're going to store them as variable and then invoke our function using each one. So let's start by defining a basic formatter of type function, taking two strings and returning a string.
>
> **[3:06](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-write-a-name-formatter-function-14799784?u=76281980&t=186)** And now we'll define our Lambda and I'll name the parameters first and last, just to be consistent. In our basic formatter, we will return first space last.
>
> **[3:22](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-write-a-name-formatter-function-14799784?u=76281980&t=202)** And now we'll define one more function variable here and we will call this one Fancy formatter. And again, it will take two strings and return a string. And this time again, we'll name the parameters first and last. And for this one's body we want this one to be a little bit more interesting. So this one, we will say first name is $first and last name is $last. So this will give us a much more verbose output to demonstrate. Once our formatter variables are defined, we can use those to call our name formatter function. So we'll come down into main, and we will call print formatted name and I will use my first name, Nate, my last name Ebel, and I will pass in basic formatter. Because we stored basic formatter as a variable representing a function, we can pass it in to print formatted name because the function types match. And if we wanted to demonstrate with the fancy matter again, I can pass in my first and last name. And this time I'll pass in fancy formatter. If we run this code now, we will see that
>
> **[4:58](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-write-a-name-formatter-function-14799784?u=76281980&t=298)** for the first invocation using basic formatter, it simply prints first and last name. And for the second invocation when we passed in fancy formatter, we can see the more verbose output printed to the console. And just to round out the example here, we don't have to predefine the functions that we're going to pass to print formatted name. If we wanted to, we could once again call print formatter name. But rather than passing a function in that we predefined like basic formatter or fancy formatter, we could use trailing Lambda Syntax once again, and we could pass in a function on the fly. So I will name the parameters here first and last. And now when I return the string for this formatting maybe we'll do it in reverse order. So we'll do last, comma first. And if we run this now, we see that our third example successfully prints out last name, comma, first name. So this shows off the power of higher order functions. We defined one print formatted name function and can now use it in a variety of ways to control how that formatting is done without impacting the core logic of printing out that formatted name.

> [!info]- Semantic Content
>
> **Definitions:** is called (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright upbeat music) (1)


### 4. Control Flow Structures

[↑ Back to Table of Contents](#table-of-contents)

#### [if and when statements](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/if-and-when-statements-14802631?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/if-and-when-statements-14802631?u=76281980&t=0)** - [Instructor] How do we define conditional logic in [[Kotlin]]? For this problem, we have two primary tools: if and when, both of which can be used as both a statement or an expression. An expression returns a values, whereas a statement does not. To better understand that this distinction between statement and expression, and to learn more about conditional logic, let's take a look at the basics of both if and when. If in Kotlin behaves pretty much just like any other language. We provide some Boolean expression. In this case, we could say if true and if it evaluates to true, which this always will, some line or block of code will be run. So in this case, we will print out The condition was true if this condition evaluates to true, which it will. So when we print out this code, we see the condition was true printed out to the console. Now, if statements can be written with or without curly braces. If we want to run multiple lines of code in response to some condition, curly braces can help us organize that code and make it more readable. So let's just refactor our if statement here and after the parentheses where the Boolean expression lives, we'll go ahead and add open and closed curly braces.
>
> **[1:36](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/if-and-when-statements-14802631?u=76281980&t=96)** Now, within those curly braces, now we are free to define whatever logic we want. So I could create a variable called message and assign it the value of complex logic here. And then we could go ahead and print that message out. And again, if we run our code, we'll see complex logic here printed out to the console. Within the if, we can perform various logical comparisons or evaluate any arbitrary expression as long as it evaluates to a Boolean value. So again, let's create a variable here. We'll call this someVariable and we'll assign it the value of zero for now. And now within our if, we could check that someVariable is greater than three. And then if it is greater than three, we will print out the value was greater than three. So if we run this, we see nothing because someVariable is in fact not greater than three. If we change someVariable to the value of five, and run again, this time we see the value was greater than three. If we want to run some code
>
> **[3:09](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/if-and-when-statements-14802631?u=76281980&t=189)** if the conditional evaluates to true, and some other code if it doesn't, we can use an else block. So after the curly braces of our if condition, we could add an else and we could print not greater. So if we run with a value of five, we see the value was greater than three. And if we change our variable back to zero and rerun, we see not greater printed out. And just like with the if condition, the else block can also be wrapped in curly braces to make it easier to read and to support adding more complex logic down the line. We don't have to stop at a single if condition either. We can combine multiple ifs together using else if. So we could print out the value was greater than three if someVariable is greater than three. We could also print out using else if someVariable greater than two. We could say the value was greater than two. So now we have three conditions. One thing if the value is greater than three, another if it's greater than two, and an else if neither of those is true. So let's try this again.
>
> **[4:44](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/if-and-when-statements-14802631?u=76281980&t=284)** We'll start with zero. We see not greater printed out. If we change someVariable to one, we still see not greater. If we change it to three, this time we see the value was greater than two because three is not greater than three, so the first condition is not met. However, the second one, someVariable greater than two is met. And finally, if we change someVariable to five and run this one more time, now we see the value is greater than three. And this time, the first if is evaluated to true, which means it's printed out. And notice that even though technically multiple conditions would be true here because five is greater than two and it's greater than three, only one branch will be evaluated. So the first matching Boolean expression is the one that will be used in this if, else if, else block. Aside from if else, the other conditional construct Kotlin provides is that of when. When can be used in a couple of different ways. First off, when can be used as a replacement for if else. So we'll just demonstrate that now by removing the if else statements that we just created and now we're going to use a when instead. So to use a when, we'll start with the when keyword
>
> **[6:17](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/if-and-when-statements-14802631?u=76281980&t=377)** and we'll add open and closed curly braces here. And then we can add different expression values to evaluate, similar to the if else. So we could say when someVariable is greater than three, followed by the arrow operator here. So this will say if someVariable is greater than three, do what is ever on the right-hand side of the arrow. In this case, we will print out the value was greater than three. And then just like before, we'll add another one here that says someVariable greater than two. Println, the value was greater than two. And lastly, we can add else conditions as well, and we could print out not greater. So now again, before we run this, let's just take a moment to review what we've done here. Conditions are supplied on the left side of the arrow and then the code to run if the condition is a true is applied on the right-hand side. And these conditions, just like with if else, are evaluated from top-down and only a single one will be evaluated and run. This is very analogous to switch statements found in languages like C# or [[Java]]. And similar again to if else, we could wrap any of these code blocks in curly braces if we needed more complex logic.
>
> **[7:54](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/if-and-when-statements-14802631?u=76281980&t=474)** So now if I go ahead and run this code a couple times for the value of five, we see the value is greater than three printed to the console. And if we just change someVariable to zero, we'll see not greater printed to the console. The other form of a when statement allows us to pass in an argument directly. So that looks something like this. When someVariable and then we can define our conditions here. Why is this syntax useful? Well, in cases where we might be supplying multiple conditionals, all operating on a single variable, passing the variable into the when allows us to implicitly reference the variable, saving us code. So once we pass an argument value to the when, in this case, someVariable, we can simplify how we write our conditionals. To compare the argument value to specific values, we can simplify specify the target value on the left-hand side of our when conditional. So if we wanted to check if the value was exactly two, we could simply write two with the arrow and then we will print out the value is two in that case. And as with our other examples of if and when, we can provide multiple conditional cases as well. So we could also check if the value was three
>
> **[9:27](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/if-and-when-statements-14802631?u=76281980&t=567)** and if so, print out the value is three. And if we wanted to handle multiple cases in the same way, we could combine them into a single condition using a comma. So if we wanted to treat let's say zero and one the same way, we could do that by putting zero, one and printing out the value was zero or one. When working with numeric types, we can use a when in conjunction with ranges to check if a value is within some range. So if we just look at what we've done here, we've checked if the value is zero or one, two or three, so now let's say we hand some case that handles if the value is four, all the way up to let's say the maximum integer value. So to do this, we might think to do something like greater than or equal to four here. However, this syntax does not work within this from of when because we need to have a value on the left-hand side of that evaluation operator. So instead, we can use the range here. So we could say if someVariable is in, and then we're going to create a range using a special syntax. So to start, we will provide the first value of our range and then two periods and then we define the maximum end
>
> **[11:04](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/if-and-when-statements-14802631?u=76281980&t=664)** of our range. In this case, by referencing Int.MAX_VALUE. So what we've just done here is basically said if someVariable is in the range of four to MAX_VALUE. And notice that the IDE is providing some hints here. It is giving us these little suggestions that say the range of four is less than or equal to the value is less than or equal to Int.MAX_VALUE, basically calling out that four and Int.MAX_VALUE are inclusive of this range. So now we can finish this off by defining what to do if it's in this range, in which case, we will print out the value was greater than three. So now let's run this new version of our when or comment out the old version first. And run our code. And we see the value was zero or one when someVariable equals zero. If we change it to say three, and run again, we see the value is three. And let's say if the value was 100, we see the value was greater than three. So we're able to hit these different expression values, whether they be specific values like a two or a three, a combination of values like zero and one being handled the same way
>
> **[12:37](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/if-and-when-statements-14802631?u=76281980&t=757)** or a range of values as with the final case of four up to max int. If and when are vital tools in our Kotlin development toolbox. We've only just started to cover the functionality they provide and we will continue to build on this foundation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (4), [[Java]] (1)
> **Code Identifiers:** somevariable (18)
> **Analogies:** just like (4), similar to (1), analogous to (1)
> **Env Vars:** max_value (4), ide (1)
> **CLI Commands:** make (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Statements vs. expressions](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/statements-vs-expressions-14800823?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/statements-vs-expressions-14800823?u=76281980&t=0)** - [Instructor] If and when can both be used both as statements and expressions. So how do we know the difference in when should we use one form or the other? To understand the difference and why it's important, we're going to examine the use of if and when as expressions to demonstrate how we can write safer, more concise code. A statement doesn't return any value. An expression, on the other hand, will either have its value returned from a function or assigned to a variable. We can use an if-expression to conditionally assign a variable's name. So let's create a variable called message, and then we can use our equals, and now when we go to assign the value of this variable, rather than just assigning a static constant here, we could use an if-expression to change the value assigned, depending on some variable value. So let's create a variable, var someVariable equals. Let's just say zero for now. So now when we go to assign the value to message, we could say if, someVariable, greater than three, "The value was greater than 3," else, "The value was not greater than 3."
>
> **[1:34](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/statements-vs-expressions-14800823?u=76281980&t=94)** Notice that the syntax here doesn't look too dissimilar from how we've assigned variables or used if in the past. We create our variable as normal and after the equal sign, we define our if-expression. The key difference is that if-expressions must provide a value. So they must always include both if and else. If we tried to remove the else here, we would get a compiler error on the if, saying, "if must have both main and else branches if used as an expression," and the reason for this is that if someVariable is not greater than three, we have to have some backup valuable to assign to message. So I'll just put the else back here, and if we were to just quickly print out the value of message here, we'll see that if someVariable is zero, we see "The value is not greater than 3" printed out. If we change someVariable to five, we see "The value is greater than 3" printed out. In addition to variable assignment, if-expressions can be used when returning a value from a function. So let's to demonstrate this by creating a function called getMessage. It's going to take an input parameter called Int, and it is going to return a string, and so now we could use the return keyword
>
> **[3:12](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/statements-vs-expressions-14800823?u=76281980&t=192)** and use an if-expression to define what value should be returned here. So we could say if, input greater than three, return the value "Greater than 3," else, return the value "Not greater than 3." So again, this is very similar to the previous example, but when we go to return the value, we have to have some return value. So we define our input greater than three condition and then an else to ensure that we always have some fallback value. Now, this syntax here, this use of if-expressions, can be combined with single-expression functions to create a multi-condition function with a single expression. So we could refactor this very easily to a single-expression function by adding the equals after the parameter list and then defining our if-expression directly. So again, now getMessage is a single-expression function, and that single expression is an if-expression with multiple conditional branches. So now let's switch gears, and let's look at when-expressions. So I'm just going to clear out the code in main here for now. Now, similarly to if-expressions, when can also be used as an expression. So I could say val, message equals when. So now within this when, I could reference
>
> **[4:47](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/statements-vs-expressions-14800823?u=76281980&t=287)** some value or some variable and again, define different conditional blocks. So I'll go ahead and, once again, create a variable called someVariable and now within my when, I'll say when someVariable is three, I'll return "The value is 3," and notice here that I have a compiler error over the when, saying, "when-expression must be exhaustive. Add necessary else-branch." So this is the same thing as with the if-expression. When using "when" as an expression, we have to return some value. So in this case, we only have defined a return value if someVariable is three. So to complete this, we need to make this exhaustive by adding an else. In this case, "The value is not 3." Once we've handled that else, or once we've defined conditions that handle all possible values of someVariable, then the when will be exhaustive and the compiler will be happy. To round out our examples of when-expressions, we can use when-expressions when returning from functions, just like we could with if-expressions. So for example, we could define another function here. We'll call this fun, getMessageWithWhen. It'll also take an Int input, and we can use a when-expression to make this
>
> **[6:21](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/statements-vs-expressions-14800823?u=76281980&t=381)** a single-expression function by saying when, input, and now we can define our conditional options again. So we'll say if the value is three, return "Value is 3," else, "Value is not 3." This can be a very useful way to concisely implement a function that needs to map from some input to very specific output cases and to help make sure that we handle all of those possible output cases. Using if and when as expressions allow us to unify assignment or return with the logic that determines the value. This can help us consolidate the conditional logic and helps us enforce imutability by allowing the conditional assignment of read-only values.

> [!info]- Semantic Content
>
> **Code Identifiers:** somevariable (9), getmessage (2), getmessagewithwhen (1)
> **CLI Commands:** make (3)
> **UI Navigation:** go to (3)
> **Analogies:** similar to (1), just like (1), for example (1)
> **Definitions:** is a  (1), is an  (1)
> **Speakers:** - [instructor] (1)

#### [try/catch as control flow](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/try-catch-as-control-flow-14797833?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/try-catch-as-control-flow-14797833?u=76281980&t=0)** - [Instructor] I don't know about you, but sometimes, maybe even most of the time, my code has bugs in it. Sometimes those bugs throw exceptions which may crash our programs. These can stem from things like trying to perform an operation on a null value or trying to divide by zero. There are many ways to work towards preventing these errors and one of which is try catch. We're going to continue our exploration of expressions in [[Kotlin]] by looking at how we can use try catch as an expression to write more concise, error safe code. So let's take a look at an example here. Val message = "the value is ${10 / 0}".
>
> **[0:51](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/try-catch-as-control-flow-14797833?u=76281980&t=51)** What do you think will happen if we run this? If you guessed that our code will crash, you are correct. Dividing by zero throws an arithmetic exception which crashes our application when we run it. Try catch lets us catch these thrown exceptions without crashing and respond to them in some useful way. If we wrap this code in a try catch, so we'll type try with open and closed curly braces, then go ahead and place our variable assignment within that again and then we will add catch. So within catch, we can define a specific error such as an arithmetic exception or in this case, I'll catch any throwable thrown by the code. So in the case of an error, we want to do something with that, so we will print out error was thrown. Now, if we run this code, we don't crash. Instead, we see our error message, error was thrown. There's an issue here though. Handling the error in this way, branches the normal execution of our code. Sometimes a potential error might be expected and recoverable. In cases like that, we can use try catch as an expression to streamline the error handling
>
> **[2:25](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/try-catch-as-control-flow-14797833?u=76281980&t=145)** and reduce the number of logical branches in our code. So let's refactor this to use try catch as an expression to reduce the branching here. So let's delete this for now and we're going to rewrite this using try catch as an expression. So again, I'll use the same message variable name and this time when we go to assign the value, we'll say equals and then we're going to use the try catch here. So within the try, we'll say the value is, and once again, we will perform our invalid divide by zero calculation here. And then now, when we catch the error, we will say error was thrown and below this, we will print out the message. So what we've done here is sort of unified the logical branches in this code. So now we're always going to assign a single value to our message variable. If the division did not throw an exception, then we would see the value is and whatever that successful value was. And in the case where we do get some error, we're always going to get a consistent error message. So now when we go to print out our message, we can be sure that it's always going to have a value, either the successful calculation or the default. And if we run this,
>
> **[4:01](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/try-catch-as-control-flow-14797833?u=76281980&t=241)** we will see that error is thrown is called. And if we just update that calculation, this time we'll just say 10 / 1 instead of zero, this time we see the value is 10 printed out. Now, before we round out this section, I just will highlight the fact that in this case, we are catching any throwable, even if we know that we expect an arithmetic exception. So just to demonstrate that we could catch a more specific exception here, we'll change throwable to ArithmeticException and we will change back our one to a zero so that our exception will be thrown again. And once again, if we run our code, we'll see that error is thrown. Now, if we want to throw some other exception here, we'll just say IllegalStateException, just for fun. Now, if we run our code, we still see an exception being thrown. This is because our catch is no longer catching all exceptions. So if we changed this back to any throwable and reran it, we would once again see error is thrown. If we wanted to still treat ArithmeticException differently, we could also catch multiple exception types here. So we could say error, IllegalStateException and in this case, we could say error was IllegalState.
>
> **[5:41](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/try-catch-as-control-flow-14797833?u=76281980&t=341)** Now, if we run, this time we see the more specific error message printed out. So this is often a good practice to not catch all exceptions and handle them all the same way. Often we want to think about specific exceptions and handle them differently, depending on how we may or may not be able to recover from them. As you write your code, think about what errors you might expect. Are there places where rather than crashing or generically catching the crash, you could provide a sensible default value if one of these errors occurs? If so, you might consider whether you can use a try catch expression to streamline this error [[Control Flow]].

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (1), [[Control Flow]] (1)
> **UI Navigation:** go to (2)
> **Definitions:** is called (1)
> **Analogies:** such as (1)
> **Best Practices:** good practice (1)
> **Speakers:** - [instructor] (1)

#### [Smart casting](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/smart-casting-14804062?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/smart-casting-14804062?u=76281980&t=0)** - [Instructor] Sometimes we may want conditional logic that runs only for certain data types. Perhaps we want to print one message if our variable is an int, and another message is our variable is of type string. In a production code base, having to repeatedly make these kinds of type checks can result in verbose, difficult-to-follow code. To help alleviate this issue, [[Kotlin]] includes the notion of smart casting. To help us understand the power of smart casting, we're going to first look at how to cast a value to a specific type and then we'll see how the Kotlin Compiler can help do this for us in some situations. So if we come into our IDE here, to understand smart casting, we must first understand the concept of casting in general. To cast a value means that we explicitly change the type of the variable or property to some new specific type. So let's say that we have a variable here called aGenericVariable and it is going to be of type Any and we will assign it a value of five. Now, we know this value is always five. So we might want to assign this value to an int variable so it's easier to work with, specifically as an int. So we could say val anIntVariable equals aGenericVariable. Notice, however, that when we do this,
>
> **[1:32](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/smart-casting-14804062?u=76281980&t=92)** we get a compiler error. If we hover over the error, we see type mismatch. Required:int, Found: Any. To make this assignment work, we need to cast the Any to an int. We can make this kind of cast by using the as keyword and then specifying the type that we want to cast it as. So now we're assigning a generic variable as type Int to our anIntVariable. Now we can work with our int variable as the more specific integer type. Now, you might ask yourself what happens if we try casting from incompatible types. Well, let's take a look. Instead of casting the value five to an int, we will change aGenericVariable to be some string value. Now, after changing the value to a string, notice that the code still compiles. However, if we run this code, we'll see that the code crashes due to a thrown ClassCastException. This is happening because we cannot cast string to an int. So now that we have some basic understanding of casting, let's explore the reverse. How can we check the type of a value? Well, let's start by stubbing out a simple function called checkType. checkType will take one parameter called input of type Any.
>
> **[3:13](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/smart-casting-14804062?u=76281980&t=193)** And we'll leave an empty function body to start. Now, let's say we want to print out a specific message if the input argument is a string data type. How do we do this if the parameter type is not specifically of type string? To perform a type check, we can use the is keyword. When combined with a conditional, we can run code only if our input is a string data type. So within our checkType function body, we could say if input is String, println Input is a String. Similarly, we could check if input is not an int, for example. If input not !is Int, println Input is not an Int.
>
> **[4:15](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/smart-casting-14804062?u=76281980&t=255)** So now we'll come back down to our main function and we will go ahead and just remove this second line here that was throwing the exception before. And now we will call checkType. And so we will pass in our GenericVariable to checkType, and we're going to run this code and see what's printed out. So notice when the code prints out that it says input is a string. Input is not an int. So let's just change the value of our GenericVariable here. Let's change it to be five. And run the code again. And this time, we see nothing printed out because it's not a String and it is an Int. So by combining the use of as to cast values from one type to another, or is to check if a value is some type or another, we have a lot of power here to be able to query and manipulate types as needed. However, like we said at the beginning, this can be verbose and error prone. So now we're ready for smart casting. Let's say that if our input value is a String, we want to print the string's length. If we try to get the length of a string before the type check, we won't be able to since the type at that point is still Any. So if we just try to do input.length, that'll fail. That gives us a compiler error
>
> **[5:51](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/smart-casting-14804062?u=76281980&t=351)** because at this point, input is still of type Any. Within the conditional, however, we can access the length property without an issue. So we could update our message here to say input is a String with length, and we can use our template here, and say input.length. And now within this conditional that checks whether input is of type String, we have no problems accessing the length property. How does this work. Why can we access this length property even if the type of input is still technically any? Well, the answer is smart casting. Within our type check, the compiler knows that the type of input is string. So it will automatically cast input to type string wherever it's used within that conditional scope. So effectively, the compiler here is doing the work for us of using that as keyword to cast input to type string. Smart casts work for nullability checks as well. Let's update our function to allow for null input parameters. And our type check to look for nullable strings. So we will add the question mark to our Any parameter. So now we can pass null into this if we wanted. And similarly, we could check if input is of type nullable string.
>
> **[7:28](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/smart-casting-14804062?u=76281980&t=448)** You might notice that we now have an error in our output code. Input might be null, so the compiler tells us that we must now make a null safe call when accessing input.length. Smart casting can help with this scenario as well. Before we print our value, we're going to return from the function if input equals null. Now, notice, as soon as we make that conditional check for null and return from the function early, the compiler now no longer is giving us the error telling us we need to make a null safe call when accessing input.length. The compiler is smart enough to recognize that if the conditional branch is ever reached, it means that input is not null and it will cast it as such for us so that we don't have to do it. The same concept would apply if we were to use an if-else rather than returning early. So let's remove our early return here and then within the conditional that is checking if the input is a nullable string or not, we can say if input equals null, else.
>
> **[8:51](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/smart-casting-14804062?u=76281980&t=531)** So now within the input equals null branch, we can say println Input was a null String
>
> **[9:06](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/smart-casting-14804062?u=76281980&t=546)** and we'll come down here. We'll fix our typo here. And we'll bring this statement up into our else branch. And now as soon as we bring that into the else branch, we again no longer have any type of null safe warning on the call to input.length. So again, the compiler here is smart casting in a couple of ways. First, it's helping us ensure that input is of type string and it's also helping us ensure the input is not null. And to round out this discussion, smart casting works with when statements as well. So we could remove the code in our checkType function here and we're going to redo some of that now with a when. So we could say when input and the first condition we'll check here is whether the value is null. And if it is null, we will print out Input was null. Then next up, we can check if input is String, in which case we will print out Input was a String of length input.length. So again here, when we make our check that input is of type nullable string, we no longer have to do any type of null safe call on input because smart casting will help us ensure that that is going
>
> **[10:39](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/smart-casting-14804062?u=76281980&t=639)** to be a non-null string. And if we go ahead and just run our code one more time, we can verify this. So with an input of five, we see nothing printed out. If we were to pass null to our check input call, we see input was null printed out. And if we pass in a string, in this case, a string saying null, we see input was a string of length four. So all of these different conditional branches are working as expected and in a null safe and type safe way. Smart casts are an incredibly helpful feature of the Kotlin programing language. By automatically inferring and casting types for us, the compiler helps us ensure type safety, while also allowing us to write less code. As we continue through this course, smart casts will crop up from time to time. See if you can notice where they are used.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (3)
> **Code Identifiers:** checktype (6), agenericvariable (3), anintvariable (2)
> **Definitions:** is a  (7), is an  (2), means that (2)
> **CLI Commands:** make (7)
> **Env Vars:** ide (1)
> **Tools:** notion (1)
> **Exercise Files:** template (1)
> **Analogies:** for example (1)

#### [while loops](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/while-loops-14799788?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/while-loops-14799788?u=76281980&t=0)** - [Instructor] In nearly any complex application the need will arise to perform some repeated operation. This could be repeating a task a certain number of times or repeating for every item in some range or a collection. Let's explore one mechanism for this type of repeated operation, the while loop. While loops in [[Kotlin]] behave very similarly to other programming languages. First, we need to define some counter value. So in this case, we'll create a variable called counter and assign it the value of zero to start. Next we set up the looping condition. In this example, we'll set up our loop to run five times by checking whether our counter is less than five or not. So we can do that by using the while keyword and then our condition in this case is going to be, counter less than five, and then within open and close curly braces here we can define what we want to do on each iteration of the loop. If we were to run this code now, our loop would never terminate since we never update the value of our counter variable. So we must be careful to increment the value of the counter within our loop body. This will ensure that eventually the counter becomes equal to five and the loop will terminate. Now that our loop should properly terminate, let's add before the incrementing of the counter,
>
> **[1:34](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/while-loops-14799788?u=76281980&t=94)** a print statement that will print out the value of that counter. So now if we run our code, we should see the values from zero to four printed out to the console. Sometimes we might want to break out of a loop early. If we're within a function, we could possibly just return our desired value to break out of the loop, but that doesn't apply to all situations. For a more general approach, we can use the break keyword to terminate our loop early. We can quickly demonstrate this by breaking out of our loop once our counter is equal to three. Now if we run this, we'll see that we only get the first three values of our loop printed out. As soon as that counter value hits three we break out of the loop and return from our main function. Using break effectively can be a good way to avoid doing unnecessary [[Data Processing]] in our code. The while loops we've been working with so far evaluate their condition before ever executing the loop body. If we need to ensure a loop is run once this form of while loop requires setting up a counter that always evaluates to true at least one time. This isn't overly difficult but it's potentially error prone and semantically doesn't match the intention of a loop that must always run at least once.
>
> **[3:08](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/while-loops-14799788?u=76281980&t=188)** For scenarios like this we can use a do while loop instead of the regular while loop. So let's just go ahead and clear out this existing implementation of our loop and we're going to leave our counter for now. So now instead of starting the loop by defining the when keyword in our looping condition, we instead start using the do keyword and then the body of the loop itself. So in this case again we will print out the value of counter and then we will increment our counter by one. This time using a plus equals operation, as opposed to using the plus plus operation that we used before. This body that we've just defined after do will always run at least once. Lastly, we now need to set up our loop condition. So we get do that by adding while again and we can define the condition we want to check. So let's say we want to run this loop while counter is greater than one. So intuitively here, if counter is starting at zero we might not expect this loop to run. However if we go ahead ahead and run our code we'll see that it runs one time. And this is because in the do while version of a loop, the loop body will always execute at least once. So, in this case we'll start with zero. We'll print zero to the console, increment the counter by one.
>
> **[4:41](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/while-loops-14799788?u=76281980&t=281)** And at that point, one is not great than one, so the loop will terminate. If we update our condition to check that the counter is say less than five, we'll see that our output now matches the basic loop implementation that we saw in our very first example. While loops are a straightforward way to repeat blocks of code times. And like many things in kotlin there are multiple [[Forms]] of a while loop to help fit different types of looping use cases.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (2), [[Data Processing]] (1), [[Forms]] (1)
> **Prerequisites:** set up (3)
> **Best Practices:** avoid doing (1)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)

#### [for loops](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/for-loops-14797841?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/for-loops-14797841?u=76281980&t=0)** - [Instructor] For loops are one of the most common programming language constructs across all languages for repeatedly running a block of code. In [[Kotlin]], we can use four loops in a variety of ways, depending on our specific use case. Let's start off with the basics and explore how to repeat tasks using some simple for loops. In the most basic of for loop, we're typically iterating over some range of values. And in Kotlin, generating a range of values is quite simple using several available functions and operators. Let's start by creating a loop to iterate from zero to five. So we'll start off by using the for keyword. And in this case, we are going to define I as our counter variable, which will automatically be incremented based on the specific range. That range is then going to be defined using the until function. So we'll first say, I in to specify that we want I to be iterated over the values in the upcoming range. And now, we will define that range by saying zero until five. Until is a special type of function called an infix function. We'll dive deeper into how we can write our own infix functions in a little while. For now, it's enough to know that the until function creates a range of values from zero to four. And so now, after we've defined the set of values
>
> **[1:32](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/for-loops-14797841?u=76281980&t=92)** that we will iterate over, we need to define what to do each time it's iterated. So in this case, we will simply print out the value of I. If we run our code, we'll see the values zero through four print out to the console. If we wanted to include five in our range, we could replace the until function call with another syntax, which is to use two periods. This syntax also creates a range, but this time, it is inclusive on both sides. So both zero and five will be include in the range of values that we iterate over. If we run the code, we'll now see that five is print out to the console as well. We could also configure our range to count down if we would like. So we could say, for I in the range of 10 down to zero.
>
> **[2:39](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/for-loops-14797841?u=76281980&t=159)** Now we see all the values from 10 down to zero. Ranges also let us control how to increment our counter by using the step function. So in this case, if we wanted to count from 10 down to zero by three as opposed to the default of one, we could come back inside our parentheses. And after we define the range, we can say step three. So now, what we've done here is defined a loop. We say 10 down to zero by three effectively. And again, step is another infix function here. If we run this, now we'll see 10, seven, four, and one. So again, we're iterating over that same range of values from 10 down to zero, but this time, we're jumping three each time. This only scratches the surface of iterating with for loops in Kotlin. As we continue through this course and learn more about arrays, lists, and other collections, we'll revisit different [[Forms]] of for loop for iterating over these data types.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (3), [[Forms]] (1)
> **Definitions:** is a  (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Replace an if/else expression with a when expression](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/challenge-replace-an-if-else-expression-with-a-when-expression-14803705?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/challenge-replace-an-if-else-expression-with-a-when-expression-14803705?u=76281980&t=0)** - [Instructor] Time for another challenge. This time, we want to get some hands-on practice with using when expressions as a more [[Kotlin]] idiomatic way of writing conditional logic. Examine the provided function, getOutput. This function takes in some input value of type nullable Any and returns an output string to later be printed to the console. This function is implemented as a single-expression function using an if/else expression to provide a return value. If we run this code, we will see various outputs printed out to the console, telling us useful information about all the different input types provided to the getOutput function. Your challenge is to refactor this getOutput function so that instead of an if/else expression as the function body, it uses a when expression. When you're done, the output resulting from running the main function should be unchanged.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (1)
> **Code Identifiers:** getoutput (3)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Replace an if/else expression with a when expression](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-replace-an-if-else-expression-with-a-when-expression-14798822?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-replace-an-if-else-expression-with-a-when-expression-14798822?u=76281980&t=0)** (gentle upbeat music)
>
> **[0:05](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-replace-an-if-else-expression-with-a-when-expression-14798822?u=76281980&t=5)** - [Instructor] How did the challenge go? Did you find when expressions any easier or more difficult to use than if else expressions? Let's walk through one approach for refactoring the given function to a when expression. The easiest way to convert the if expression to a when expression is actually to use the IntelliJ tooling and have the IDE perform the refactor for us. If we move our cursor and click on the if in the if else expression of our getOutput function and then hit Alt + Enter, we'll see that there is an option to replace if with when. If I hit Enter on that, it will actually automatically convert the if else expression to a when expression for us. How does this solution look compared to the one you came up with? While the IDE tooling is the easiest way to refactor the code, it's not the most instructive. So we're going to go ahead and just revert that change and walk through a manual refactoring so we can talk through some of the details. So I'm actually going to start out by commenting out the existing function. (keyboard clicks) We'll save it here as a reference of what we need to build towards, but this will let us build out a new getOutput function without having to worry about changing names
>
> **[1:39](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-replace-an-if-else-expression-with-a-when-expression-14798822?u=76281980&t=99)** or anything like that. Next, I want to set up a new version of the function using a when expression as the single expression function body. So we're just going to stub this out. So again, we'll say fun_getOutput. We'll take in our single input here. (keyboard clicking) We will define our String return type, and this time we'll say equals when (input) and now we have our when expression, but no conditions in it yet. At this point, the compiler is going to complain to us that we must provide an else block. This is because a when expression must provide some value. So we have to ensure that there is at least some fallback value if no conditions are met. So let's go ahead and add our base case to match what was present in the if else version. So we'll say else "Input didn't match target inputs" So now we will have at least some value always returned when getOutput is called. Now that our base case is accounted for, we can go down the list of if else cases and translate them into our when expression. We'll start by adding a condition to check for the number type. We'll do this by saying is Number within that conditional block, we can still use the if else expression from the old code.
>
> **[3:11](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-replace-an-if-else-expression-with-a-when-expression-14798822?u=76281980&t=191)** So we can copy that over. So I will just grab this. I'm going to uncomment it, copy it, comment it again, (keyboard clicking) and add that to our is Number condition. (keyboard clicking) Next up we have the String case. So we'll say is String. And in that case, we will return "Input was a String with ${length input.length}" And notice again here we're accessing input.length. Smart casting is kicking in for us so that we don't have to worry about validating whether input is null or not. And lastly, we can check for null explicitly by adding a null case and returning the string "Input was null" So we'll just kind of compare this to the old version. We've handled the non-Int case, we've handled the non-Int Number case. We copy that code directly over. We've also handled the String case, the null case, and the base case. So now we'll just go ahead and remove that old function. And if we run our main function once again, we should see the same outputs that we expected from the original implementation of getOutput. So what do you think about when expressions?
>
> **[4:50](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-replace-an-if-else-expression-with-a-when-expression-14798822?u=76281980&t=290)** There's no hard and fast rule for when to use a when expression over an if expression. Both can be used to accomplish the same things and it largely comes down to personal preference. However, when expressions do have some additional benefits for exhaustively enumerating close types, which we'll explore soon when we dive into [[Object-Oriented Programming (OOP)|object-oriented programming]] with Colin.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** cursor (1), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (1)
> **Code Identifiers:** getoutput (4)
> **Non-Speech:** (keyboard clicking) (3), (gentle upbeat music) (1)
> **Env Vars:** ide (2)
> **Definitions:** is an  (1), is called (1)
> **CLI Commands:** find (1)
> **Tools:** intellij (1)
> **UI Navigation:** click on (1)


### 5. Debugging Kotlin Code

[↑ Back to Table of Contents](#table-of-contents)

#### [Setting breakpoints](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/setting-breakpoints-14802634?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/setting-breakpoints-14802634?u=76281980&t=0)** - [Instructor] How do we fix our code when something isn't working right? Usually, we need to start by understanding which lines of code might be causing the problem. While we sometimes might intuitively understand the issue, we often find ourselves needing to debug our code line by line to track down those hard-to-squash issues. Thankfully, IntelliJ has a number of built-in tools to help this debugging process. We're going to start building up our debugging toolbox by learning how to set breakpoints in our code and run the bugger to inspect the execution of code at those breakpoints. Now, before we dive too deeply into the tooling, let's make sure we are all on the same page when we say debugging. For me, debugging is anything I can do to help understand why my code isn't working. That might be reading through code line by line, talking through my code with my rubber duck, or using my debugging tools such as Logging or other IDE tooling. That then brings us to the debugger. Most modern IDEs have some form of debugging toolset. These tools are designed to help us find and fix issues in our code. These tools might help us inspect a local database, examine the value of a variable at a specific point of execution in our program or pause the entire execution of the program. One of these common tools are breakpoints.
>
> **[1:36](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/setting-breakpoints-14802634?u=76281980&t=96)** Breakpoints tell the debugger to suspend execution of our program at a specific line of code. When the program is suspended, we can then examine the current state of our application. So let's set our first breakpoint. And before we do, let's go ahead and examine this code that we'll use to test this concept. So we have a loop here iterating from the range of zero up to four. And on each iteration, we're generating a random int and then we are calculating whether that random value is even if or not. And then we will print out whether the value is even or odd. Now, there's a bug in this code that is likely readily available as soon as we run that code. So let's go ahead and run that code now. And if we examine the output here, we see that we have a value ending in five, which is marked as even, a value ending in seven, marked as even, a value ending in six marked as odd, another ending in six, odd, and one ending in eight, which is odd. So as we see this, we can see pretty obviously that our logic for calculating whether the value is even or not is backwards. To help understand what's going on in this code, we're going to set a breakpoint on the line where we define our isEven property. This will let us pause execution of our program after our random number is generated.
>
> **[3:09](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/setting-breakpoints-14802634?u=76281980&t=189)** To set the breakpoint, move your cursor to the left-hand gutter in between the line number and the code, and click in that gutter space. This should result in a red circle icon being added. So we want to place that breakpoint on the line six here where we're calculating our isEven value. If we start our application then with the debugger, when we hit this breakpoint, or any other breakpoint, that line of code in the application will be suspended. To hit our breakpoint then and examine the code, we need to run our code with the debugger attached. The easiest way to do this is to hit the Debug button in the toolbar. Here it is in between the two sort of green play icons. If I hover over it, it says Debug MainKt. Now, you can also find it by going to the menu at the top, go to Run and select Debug.
>
> **[4:21](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/setting-breakpoints-14802634?u=76281980&t=261)** Once the code starts, the debugger tool window has opened down at the bottom of the screen here and it should show the line with our breakpoint being highlighted. So this blue line here is indicating that the code has suspended on that particular line. If we look down at the tool window, we'll then see two main items of interest. We can see a stack trace on the left side of that tool window showing which points in the application have been hit so far. We also see the value of all our in-scope variables on the right hand of that tool window. In this case, we see the value of our counter and we see the current random value. The debugger and breakpoints are extremely valuable tools for [[Kotlin]] developers and there's even more functionality to leverage when debugging our code. We'll see later how we can build on this concept of breakpoints to inspect our code and to learn more information about how our code is behaving.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** cursor (1), [[Kotlin]] (1)
> **CLI Commands:** find (3), make (1)
> **Code Identifiers:** iseven (2)
> **UI Navigation:** in the toolbar (1), go to (1)
> **Env Vars:** ide (1)
> **Tools:** intellij (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Stepping through code](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/stepping-through-code-14802635?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/stepping-through-code-14802635?u=76281980&t=0)** - [Speaker] Okay, once we've stopped execution of our code at a break point, what next? How can we efficiently inspect our code and walk through the execution of our program to find the issue? Let's take a closer look at the debugger tools to understand how to step through code line by line, and how to set conditional break points that only pause execution under specific circumstances. So again, we will use this same loop function here to test out these debugging tools. And again, just as quick review, this code will iterate over a range from zero to four, generate a random number, and should ideally print out correctly whether that number is odd or even. And we also still have a single break point in this code here placed on the line where we determine whether our value is odd or even. If we now run our code with the debugger attached, we'll see that the debugger stops at that break point when calculating whether the value is odd or even. Now, what if we want to pause execution at more than one place in our code? Let's say we want to pause only in the true branch of our win statement. We can set another break point just on that line with the true conditional. Then down in the debug window, along the left-hand side of the menu,
>
> **[1:36](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/stepping-through-code-14802635?u=76281980&t=96)** we can click the green resume program button. This will allow the code to continue running until the next break point is hit. And now we see that that second break point is highlighted in the execution window. By placing break points at specific points throughout our code, you can repeatedly resume execution and hit new break points to better understand the flow of your system. Now, what if we want to examine every line of code in our application? Do we need to set a break point on every single line? Thankfully, the answer is no. Let's clear out our old break points and add a new one at the very first line within our loop. So here where we're generating the random integer. Now, we'll go ahead and run the code again with the debugger. And that first break point should be hit in that first line within the loop. If we want to execute this one single line without setting another break point, we can step over that line by clicking the step over button in the debug tool window. That button lives here towards the top of that tool window next to several arrows. If we look at the first option, this is the step over option. If I click that first icon there, we'll see the execution has run that first line
>
> **[3:12](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/stepping-through-code-14802635?u=76281980&t=192)** of code to generate the next int, and has now paused on the next line, which is determining whether that value is odd or even. By stepping over a line, we allow that line to be executed. Notice how now in our variables window at the bottom, the random variable exists in that variables panel and we can examine the value that was just generated. We can also do more than just step over things. So let's pause our program and now we're going to quickly create a function to extract the logic of generating our random value. So I'll just come above the main function, generate a function called get random int, and I will simply copy and paste the random number generation, and then update the loop to use this new function. Now, let's run the debugger again so that once again we stop at our break point when generating the random integer value. If we click step over again, we will jump to the next line and see what the generated value is. But instead, what if we want to see how that value is generated? So I'm going to go ahead and click resume program again,
>
> **[4:48](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/stepping-through-code-14802635?u=76281980&t=288)** which will keep running until the next time the loop comes back and hits that random number generation. So this time, instead of clicking step over, we want to click step into, which is the second arrow function here in that debug tool menu. By clicking step into, we will follow the call stack and be taken to the body of the get random int function. So effectively, rather than just calling get random int and moving to the next line, we actually move into get random int. We can then use the step out debug option to return back to the line where we were generating the random int. And now from there, we can go ahead and step over once again and see what the random number is. Now, so far, in each of these examples, we've started by running our program with the debugger attached. This then results in our first break point being hit. And we start debugging from there. But what if our bug only occurs under certain circumstances? What if we only want to start debugging once our counter is greater than two? We could simply run the debugger and click resume multiple times until we get to the counter value of two. Or we could set a conditional break point.
>
> **[6:22](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/stepping-through-code-14802635?u=76281980&t=382)** Conditional break points are break points that only pause our application if a certain condition is met. In this case, if our counter is greater than two. So to set a conditional break point, let's start by setting our break point as usual. So in this case, I am going to remove the one on the random number generation and set a break point instead where we determine whether it is odd or even. Next, I'm going to right-click that break point and this brings up this tool window here, which will allow us to define a condition for this break point. So within this condition box here, we can define the conditional logic and we have access to all the variables and properties that are within the same scope of this break point. So in our case, we want the condition to be I greater than or equal to two. So our break point will only stop then as soon as our I counter from the loop is greater than or equal to two. Once we're satisfied with the condition, we can click done. And now, if we run our code again with the debugger, we'll see that our break point is hit, but it's only hit once I equals two. If we click resume again, we'll see I equals three. I equals four.
>
> **[7:56](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/stepping-through-code-14802635?u=76281980&t=476)** And the program terminates. Now, to round out this discussion of conditional break points, we're going to right-click on our break point once again. And we'll see that in this menu here, there are several other options. We could choose to enable or disable the break point by clicking the enabled box. We can also choose whether to suspend all threads when a break point is hit, or just a particular thread. By default, all threads are selected, which makes it easier to debug application in a multi-threaded program. However, there are situations where you might not want that and you could customize that here if needed. These debugging tools give us the flexibility to step in and out of function calls, step over lines of code, and repeatedly pause and run our code under specific conditions so we can understand where things went wrong. Have you ever been in a situation in the past where you wished you could pause and inspect your code? Would any of these tools have helped? Knowing how to efficiently use these tools will make a huge impact on how quickly you can diagnose problems in your code.

> [!info]- Semantic Content
>
> **CLI Commands:** find (1), make (1)
> **UI Navigation:** right-click (2)
> **Speakers:** - [speaker] (1)

#### [Evaluate expressions and add watches](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/evaluate-expressions-and-add-watches-14804065?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/evaluate-expressions-and-add-watches-14804065?u=76281980&t=0)** - As we debug our code we're often inspecting values and calculations to understand if they are working as expected. Sometimes we might have questions that the current code can't answer as it's currently written. In these cases, we might wish that we could quickly perform a calculation or query using the current frame of the application without having to actually make a code change and redeploy. We can address this exact scenario by using the evaluate expression tool and by adding watches. So to demonstrate both of these tools, let's rerun our code to the first break point once again. And then we we are going to step over our code to the point where the random value is generated. And in this case, we've actually already set our break point to that point so we see the random value down here in our debug tool under the variables section. Now, we have a line of code at this point that already checks if the generated random value is even or not. But what if we wanted to check if the value is divisible by three or by five, if the value is divisible by three or by five, do we have to update the code again and redeploy it and then check? Thankfully, no. Thankfully, no. We can use the evaluate expression tool. So in the debug tool window, in the upper toolbar, there should be a little icon
>
> **[1:32](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/evaluate-expressions-and-add-watches-14804065?u=76281980&t=92)** that looks something like a calculator. And as a hover over here, we'll see that it says, evaluate expression. We're going to open up this tool. And now within this, we have access to all the variables and everything else within the scope of the application at this point of suspension in the debugger. And then we can use this tool to calculate the value of random expressions that we might want to evaluate and see how our code might behave under different circumstances. So by default here, we see random is entered in and if we click evaluate, we will see what the current value of random is. If I type random.rem(3)==0 and click evaluate, If I type random.rem(3)==0 and click evaluate, I can see result equals false. So, this suggests that the random value is not divisible by 3. This tool can dramatically speed up debugging by allowing us to evaluate expressions like this and answer questions that might come up as we step through our code. All without needing to actually update the code, redeploy it and get back to this current debugging state. If we find ourselves performing the same evaluations repeatedly, we can turn that expression into what is known as a watch. The watch will become a part of the current debugging session and be updated accordingly anytime a dependent value changes,
>
> **[3:06](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/evaluate-expressions-and-add-watches-14804065?u=76281980&t=186)** it's kind of like creating a virtual variable. So let's take the case of checking whether our random value is divisible by 3 or not. So we can come down into our tool window and in the variable section where it says evaluate expression or add a watch, I'm going to place my cursor and I'm going to type in that same random.rem(3)== 0 expression. random.rem(3)== 0 expression. So again, checking whether the current random value is divisible by 3 or not. But this time, I'm going to come over to the right and where this little plus icon is that says add to watches, I'm going to click this. We see, as soon as I add that to watches, now this expression is showing up in our variable section. And so now, if I resume execution of the code, each time I hit this break point, we'll see that our random value changes and then by extension, our watch changes as well. So this is why I said earlier, it's like a virtual variable. So this lets us continuously see as we run through our code with the debugger whether or not these random values are divisible by 3. So I'll click it again. We'll see this one is not, again, this one is also not. And we'll just click through until the code finishes and that's it. Both of these tools, whether it's evaluating an expression or adding an expression to be a watch and seeing it update every time we hit a break point are incredibly useful.
>
> **[4:43](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/evaluate-expressions-and-add-watches-14804065?u=76281980&t=283)** And it's good to practice with them and to understand when and where to use them during your debugging sessions. However, it's perfectly okay to pick one or two of these debugging tools that we've covered to start with and get more familiar with new ones as you practice and as you need them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** cursor (1)
> **CLI Commands:** make (1), find (1)
> **Analogies:** kind of like (1), it's like (1)
> **Definitions:** known as (1)
> **Speakers:** - as (1)


### 6. Working with Collections

[↑ Back to Table of Contents](#table-of-contents)

#### [Arrays](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/arrays-14800814?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/arrays-14800814?u=76281980&t=0)** - [Instructor] Let's discuss how we store collections of data in [[Kotlin]]. The most basic data structure for this is an array, which behaves very similarly to arrays in other programming languages. Let's take a look at how to create and work with arrays in Kotlin. An array is a collection of values, typically of the same type. Let's say we wanted to store five integer values, we could do so using the arrayOf function. If we passed in five comma separated values here, this will create an array of size five with the elements 1 through 5, the type of the array will be inferred to be array of non-null int. If we replace the last value 5 and instead use null, then the inferred type of the array will be nullable int. The array of function is smart enough to recognize whether we are mixing nullable and non-null values. If we want to create an array of some given size, and initialize it with all null values, we can use array of nulls. So we could say, val nulls = arrayOfNulls, and we want that to be nullable ints, and we say 5 initial values. So calling arrayOfNulls here,
>
> **[1:33](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/arrays-14800814?u=76281980&t=93)** specifying the int type, and passing 5, will create an array of size five with five null values, and it will support us adding int values in the future. So this will let us hold nulls or ints. Alternatively, if we want to initialize an array in some other way, so another way besides specifying every value upfront or making everything null upfront, we can call the array constructor, and provide an initialization Lambda to define how each value should be initialized. So in this case, we'll create a variable here called custom, and we will call the array constructor with 5 as the size of the array and then we will define the open and closed curly braces here to define our Lambda. This Lambda is going to let us initialize the array however we want Within that Lambda, it's going to pass us an integer value representing the index in the array. So for us, with an array of size 5 we will get indexes zero through four, and in this case, we just want to fill up the array with values zero through four, so we will just return the index value itself and that'll be our entire Lambda here. So this will initialize the array with 5 values from zero through four, essentially creating the same array
>
> **[3:05](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/arrays-14800814?u=76281980&t=185)** as we did in our very first example. To access the size of an array, we can use the .size property. So if we created a variable called sizeOfArray, we could set that equal to ints.size, and that would return 5. To access values in array we can use get and set methods on the array. So, if we wanted to get the firstValue we could call ints.get and pass 0. We passed zero because collections in Kotlin including arrays are zero based, meaning the first index in any collection is zero. And similarly, if we wanted to set a value in the array we could say ints.set, we specify the index of the value we want to set and then a comma, and then we pass in the value we want to set. So in this case, I'll pass in 99 and we would set the first value of that array to be 99. Now, one of the fun things about Kotlin is that it provides a lot of syntactic sugar, to make common operations a little bit easier, often trying to make them a little bit more concise or easier to read. Now, one of these is that Kotlin provides an index access syntax for working with arrays
>
> **[4:39](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/arrays-14800814?u=76281980&t=279)** that bypasses the need to call the get or set directly. So instead of saying ints.get 0 to access the first element in array, we could actually used square brackets here and pass in the index that we want. And similarly, rather than saying ints.set 99, we could say ints[0] = 99. So this are equivalent, they're going to use the same code underneath the hood, but it's just a little bit of syntactic sugar that the compiler provides to us to make it a little bit easier to read and quicker to write. If we need an array of some primitive types such as int or short, Kotlin provides additional array types specifically designed for storing values as primitives, rather than storing them as their larger complex object types. Now, why is this helpful? It comes down to the size of each type a primitive int takes up less memory than int the object. Most of the time we work with the complex type, but if we know we will be working with an array of a primitive, and we think that array might be rather large, we can take advantage of this more efficient array. So to create an array of primitive ints, we could use the function intArrayOf, and again we will pass in our same five values.
>
> **[6:17](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/arrays-14800814?u=76281980&t=377)** And similarly, we could create an array of let's say, unsigned shorts, and to do that, we will use ushortArrayOf, and again, pass in five unsigned short values. And here I will just fix my variable typo before heading on. This pattern for creating primitive specific arrays applies not just to int or you ushort, but to really all the other primitive types as well. Now, when we create these arrays, we often then want to process them in some way, and this often looping over an array, iterating over all the elements in that array, and doing something with the values. So to iterate over items in array, we have several options. We can use a basic for loop, so we could say for i in 0, until ints.size, and we will then use that counter i to access each individual value in the array. And if we run this code now, we will see the five values set to our array. So remember we see 99 because we set the first value in our ints array to 99,
>
> **[7:52](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/arrays-14800814?u=76281980&t=472)** then we see 2, 3, 4, and null which again we set as the initial value. Now, alternatively, we can also use a special element syntax that allows to iterate over each item directly without having to manage a counter. So in this example, we would say for element in ints, element is going to be the name of each value in the array. So we can call it element, we could call it index, value, whatever you want. And now within this, rather than having to use a counter to look at the value into the array, we simply say println element, and if we run our code one more time, now we see our list duplicated again in the console output. Now our last option for iterating over an array is a functional option from the Kotlin Standard Library. We can use the, forEach function from the Kotlin Standard Library to operate on each element of the array, or any other collection type for that matter as we'll see elsewhere in the course. So in this case, we would say ints.forEach, and now within this Lambda we get access to every element in the array implicitly using it as we often do within a Lambda,
>
> **[9:31](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/arrays-14800814?u=76281980&t=571)** so we could print out it in this case to print each value, to make this more readable however, we could rename that implicit reference to something like the previous example where we call it element, and once again we would print out element. Now, before moving on, we're just going to comment out these last three examples, just so that we don't confuse ourselves, when we're looking at the at the output from our next samples here. Now, one way in which arrays are commonly encountered in Kotlin, is when writing functions that take a variable number of arguments. Let's say we want to write a function that takes in some greeting, and then prints out a variable number of greetings based on the arguments passed to it. So we will write such a function now, and then play with it a little bit here. So we will call this function, greetThings, it will take in a single greeting of type string, and this will be kind of our basic stub out function. So now, how could we finish this function to allow us to print out a greeting for multiple things? Well, one way in which we could do this, would be to pass an array directly to the function. So we could do something like items as a parameter name,
>
> **[11:05](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/arrays-14800814?u=76281980&t=665)** and it could take in an Array of type Any, and now within the function body we could say for item in items, println, and here we would substitute in our greeting argument value and the item value. Now this would work fine, and in fact, if we come up into our Main function, we could call greetThings, with a greeting of let's say, Hey, and we could create an array of things to greet. In this case, we might say Kotlin and Programming. So this works great, but it requires us to create a new instance of an array anytime we call our function. To simplify this, we can use the varargs modifier. So let's look at how that is implemented first, and then we'll talk a little bit it more about what that means. So down here on our items parameter, which is an Array of Any, we're going to add vararg as a parameter modifier here, and now instead of items being Array of Any, we're going to change it to simply Any. And now notice that the function body here
>
> **[12:38](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/arrays-14800814?u=76281980&t=758)** didn't change at all, we can still iterate over all of the items in that it's parameter, vararg parameters are treated as arrays by the compiler, but when we call our function we can pass the array values directly to the function without having to create a new array first. So up here where we have greetThings, we can pass that array indirectly, if we want, or we could say greetThings, this time we'll say Hello, and this time we can pass in the things that we want to greet separately. So notice in this case, it's much more fluent. We don't have to separate out the greeting and then pass in this separate array that needs to be instantiated. We can pass in all of the arguments very fluently, and the compiler will recognize that everything after the first argument belongs to that array of things to greet. Arrays are a fundamental data type in any programming language, and it's important to have a solid understanding of them before moving on to other more complex [[Data Structures]] in Kotlin.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (11), [[Data Structures]] (1)
> **Code Identifiers:** greetthings (4), arrayofnulls (2), foreach (2), arrayof (1), sizeofarray (1)
> **CLI Commands:** make (4)
> **Definitions:** is an  (2), is a  (2)
> **Analogies:** such as (1)
> **Prerequisites:** initialization (1)
> **Speakers:** - [instructor] (1)

#### [Lists](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/lists-14802628?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/lists-14802628?u=76281980&t=0)** - [Tutor] Lists are a core data structure of many programming languages and [[Kotlin]] is no different. Let's dive into some code and learn to create lists in Kotlin. Along the way, we'll work to understand the difference between mutable and immutable collection types. We can create lists in Kotlin in several ways. The most basic is to use the listOf function. We can call listOf and pass in the items that we want to include in the list. In this case, I'll pass in three strings, Kotlin, code and puppy. ListOf is a generic function allowing us to infer the type of the list items based on whatever we pass in. In this case, this will be inferred to be a list of string. So if we explicitly call this out we could call this stringList and it would have a type list of string just like that. If we wanted to have a listOfInt, we could say val listOfInt equals listOf, and if we pass in all Ints then the type will be inferred to be listOfInt. Similarly to arrays, if we then pass a null value here this would be inferred to be a listOf nullable Int. If we need more complex initialization logic
>
> **[1:34](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/lists-14802628?u=76281980&t=94)** for our first elements of the list, we can use another list builder function that takes a list size and an initialization Lambda. So here, we'll say listOfInt of size five and we'll pass in an initialization Lambda. This is very similar to how we can initialize an array. So within the Lambda, we will get access to the index of that element in the list. I'm going to rename the implicit argument value to index to make it a little bit easier to understand, and then to initialize our values, I want to double the index. So I will say two times index and use that as that initial value in the list. If we assigned this to a variable called doubledValues, and we then say doubledValues.forEach, println, it. If we then print this out, we will see 0, 2, 4, 6, and eight all of the indices doubled. And finally, if we need a simple emptyList of some type, say, as a default placeholder value we can call the emptyList function. So if we wanted a collection called emptyStringList, we could assign it the result of calling emptyList
>
> **[3:14](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/lists-14802628?u=76281980&t=194)** of type string. By specifying the string type explicitly when calling emptyList, our empty stringList variable will be inferred to be a list of string. If we have this type explicitly defined on the variable we actually then don't need it when we call emptyList. So whether it is on the variable type itself or when calling emptyList, we can define the type. We don't have to do it in both places. Accessing list elements is very similar to accessing elements from an array. We can use available getter and an index to look up a value. So here we'll define a variable called firstInt equals listOfInt.get and we'll pass them the index of zero. So we're using the getter on our list passing in the index for the first value, and we should be able to pull out that first integer from our list. Lists also support direct index access using brackets. So if we wanted to get, let's say the second Int, we could a listOfInt and we could use brackets and pass in a one. So under the hood, this will still use the getter but this provides us a little bit more concise syntax to access the elements directly as opposed to having to call that getter.
>
> **[4:48](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/lists-14802628?u=76281980&t=288)** We'll explore some additional functions from the Kotlin Standard Library for querying and accessing elements from a collection a little later on. Now we've seen how to access an element from a list, but what if we want to update an element or even add an element to our list. Let's try adding an Int to our list ofInts. So if we did something like listOfInt.add, we'll see that we get a compiler error here. The IDE can't find any such method add. Similarly, if we tried to use say, direct index access and try to update the first value in that list to a nine, same thing, we also get a compiler error. So what's going on here? Why can't we modify this list? Well, the answer is that Kotlin differentiates between mutable and immutable collection types. By default list is an immutable type. It does not allow for updating of values or for adding and removing of items. To create a list that can be modified, we need to use a mutable list. To create a mutable list, we can use the mutable list of function. So let's create a mutable list of programming languages. So I've got my languages variable, and now I will say mutableListOf, and just like before
>
> **[6:24](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/lists-14802628?u=76281980&t=384)** now I can pass in the items in my list. So I could say Kotlin and [[Java]] to start. So now I have a mutable list of size two. But because this is a mutable list, I can update this however I want. So I could change the first item in my list to be Kotlin with an uppercase K. Notice here we're using direct index access and assigning a value directly. We could also have done languages.set, and passed in zero and the value like this, we can also add an element to the end of our list by saying languages.add and passing in whatever string we want to add to the collection. So in this case, we'll say [[JavaScript]]. Now there are other types of add methods available to us. We don't always have to add to the end of a list. If we wanted to add something to a specific index we could pass in the index so that we one add an update and then pass in the value. So in this case, we've added C++ as the second item in our list, by using that one for our index value. And contrary to adding items we can remove specific elements as well.
>
> **[8:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/lists-14802628?u=76281980&t=480)** So if we type languages.remove, we could pass in the specific string that we want to remove from our collection. Or if we wanted to remove based on a specific index we could say removeAt, and in this case maybe we want to remove the first item in the list, we could pass in an index of zero. Now let's talk a little bit about type compatibility. Mutable list extends list. This means if we want to have some generic list variable here. So we have aList of type list of string, we can assign it the value created by a mutable list function. So in this case, I will create a mutable list of values, dog and cat, and I can assign it to a list variable. Notice though that once we've done this we've masked the underlying mutable list type. We can no longer manipulate our list directly when working with the aList variable. If I tried to say aList.add, we get a compiler error here, again because the variable itself is stored as the immutable type. This is important to remember when deciding whether to use list or mutable list. Often we want our public ABI to be immutable. And so we would choose list.
>
> **[9:34](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/lists-14802628?u=76281980&t=574)** Whereas within the implementation details of our class are function, we might want to manipulate the list, and so we would choose a mutable list. Lists are one of the most common collection types in Kotlin. And thanks to their API design and the Kotlin Standard Library, developers have access to a concise syntax and a great deal of helper functions for working with them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (10), [[Java]] (1), [[JavaScript]] (1)
> **Code Identifiers:** listofint (7), emptylist (6), listof (4), alist (3), stringlist (2)
> **CLI Commands:** make (1), find (1), cat (1)
> **Env Vars:** ide (1), abi (1), api (1)
> **Definitions:** is a  (2), is an  (1)
> **Analogies:** just like (2), similar to (1)
> **Prerequisites:** initialization (3)
> **Speakers:** - [tutor] (1)

#### [Sets and maps](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sets-and-maps-14797842?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sets-and-maps-14797842?u=76281980&t=0)** - [Instructor] Now it's time to introduce two more common [[Data Structures]], sets and maps. Both these data structures are incredibly useful and widely used. We're going to explore how to create and work with both of them. Like with lists, the easiest way to create a set in [[Kotlin]] is to use a builder function. So let's define a variable called languages, and we want this to hold a set of programming language strings. So I will use the setOf function here and then I need to pass in the strings that I want to be a part of this set. So in this case, I'll pass Kotlin, [[Java]] and C++. Oops. Need to make sure that last one is wrapped in quotes. There we go. So we have a set of Kotlin, Java and C++, and because of type inference here, we don't have to explicitly define the type. But if we did, that type would look like this setOf non nullable string. Now, let's remember what is special about sets. They don't allow for duplicate elements. So if we pass Kotlin twice when creating our set, the final set will only contain one instance of the string Kotlin. So if we come back here to our invocation of the setOf function and we pass Kotlin a second time, the languages variable will only have one instance of that Kotlin string. And we could verify this by printing out each element. We can print out each element
>
> **[1:33](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sets-and-maps-14797842?u=76281980&t=93)** in much the same way as we can with lists. So we could use the element for loop syntax here. And we could say for language in languages, println language. Now, if we run this code, we see Kotlin, Java and C++ printed out to the console. And notice we only have one copy of Kotlin, which is exactly what we would expect when using a set. Like all collections in Kotlin, we can access the size of our set by using the size property. And we can check if an element is contained within this set by using the contains function. If we print out the result of this contains invocation, we see true printed out because the string Kotlin is present in our set. If we want to manipulate our set after it's been created, we need to use a mutable set. We can create a mutable set by calling mutableSetOf instead of setOf. So if I come back up here to our declaration of our languages variable, I'll change setOf to mutableSetOf. With a mutable set, we can now add elements using the add method. So if I type languages.add, here we actually see that we have a compiler error. This is just a good reminder of the fact
>
> **[3:09](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sets-and-maps-14797842?u=76281980&t=189)** that just because we call mutableSetOf, it doesn't mean the type of our variable is automatically immutable set. Remember, up here, we explicitly specified that the languages variable should be of type set, as opposed to mutable set. So if we change set to MutableSet, now our add call is recognized by the compiler and we are free to pass in another language string here. So if we come back up here, we could actually simplify this a little bit more by simply removing that explicit type declaration and relying on the compiler's type inference for us. And to mirror the add ability, we can remove from our set as well, using the remove method. And in this case, I will remove that string that we just added. Our next data structure is map. A map allows us to store key value pairs for efficient lookup. Maps and their different implementations are one of the most useful data structures available to us. To create a map, we'll use the mapOf function and create a read only map. So to start, I'm going to create a variable called testScores, and then we will use the mapOf function. So this is going to eventually create our immutable map.
>
> **[4:44](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sets-and-maps-14797842?u=76281980&t=284)** And now we need to pass in key value pairs to this function, and then that will help the compiler infer the type of the map. So the most explicit way to pass in these key value pairs is to create instances of the pair type and pass those to the function. So we can do that by writing pair and then open and close parentheses to invoke the pair constructor. And then we simply pass in the key and the value. So in this case, let's say I pass 123 as a student ID and 91.2 as a test score. And then maybe we want one more pair of ID to score in here. So I'll type pair again. This time the ID will be 456 and this score will be 84.5. So now if I hover over test scores here, we see that its type is inferred to be a map with keys of type int and values of type double. Now, creating a new instance of the pair each time to pass them into the mapOf function is a little verbose. So the Kotlin standard library includes a helper function to more concisely create pairs. We can use this infix function called to, to create a pair associating the value on the left and the right. Now what this looks like is us defining 789 for a student ID again here, then writing to and then defining the test score here.
>
> **[6:23](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sets-and-maps-14797842?u=76281980&t=383)** So effectively what we've done with this invocation is created a new pair and passed in 789 and 79.1 to it. However, to is an infix function. So really what we've done here is called the to function on the value 789 and passed 79.1 to it. We will look in more depth at infix functions later on. Once our map is constructed, we can work with it in several ways. We can use index to look up to access the value for a given key. So if we typed println here and then we said test scores, we used brackets and we pass in a key value here, this will print out the value for key 123. And if we just print that out, we will see 91.2 as the last output in our console. And this does map to the test score that we defined for key 123 in our map. If we use a standard element loop, we can access each key value pair individually and we can get the key or value for that pair. So we can say for record in testScores println score for user $, and now this is where it gets interesting. So record here is actually returned to us
>
> **[7:57](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sets-and-maps-14797842?u=76281980&t=477)** as a pair, effectively, or really it's an entry in the map. So if we type record, it's not going to be the value itself, it's going to be an entry representing the key value pair. So if I want to then get the key for that record, I would say record.key and now we can continue on building my output string here. So we say score for a user. We substitute in the user ID was and then this time we'll say record.value. If we print this out, we will see each of those key value pairs printed out to the console in our formatted string. Having to iterate over each pair and then dereference both the key and the value can be a bit repetitive, so we can make use of destructuring syntax to extract both the key and the value into named variables when defining our loop. So what this looks like, is rather than saying for record in testScores, which will give us a single value that ends up having to hold both the key and the value, we can define some parentheses here, and instead of one named variable, we'll define two, ID and score. So what this is going to do is it's going to assign the key to our variable called ID and it's going to assign value to our variable named score.
>
> **[9:35](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sets-and-maps-14797842?u=76281980&t=575)** So now in our string, rather than having to dereference the element in the loop, we simply replace in the named variables that we just defined. Now, if we print this out, we'll see that same output that we saw before. If we want to check that a map contains a specific key, in this case, a specific student ID, for example, we can use the .containsKey method. So we could say testScores.containsKey and I could pass in a student ID key value here. And similarly, if we wanted to check for the presence of a specific value, we can use the containsValue function and pass in a value that we want to look for within the map. If we wanted to get a collection of all the keys in the map, we could do so by accessing the keys property. This will give us back a set of all of the keys, and then we could print each of those out if we wanted to, for example. So now if we run this, we'll see this last three lines of output here is simply printing out each key available in the map, in this case, each student ID that we've stored. To round out our look at map,
>
> **[11:12](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sets-and-maps-14797842?u=76281980&t=672)** let's talk about mutable map for a minute. If we want to modify our map after it's created, we'll need to use the mutable variant of our builder function. So instead of mapOf, we will use mutableMapOf. Now that we are working with a mutable map, we can add new values using index access syntax. So, we'll write testScores and then we'll use brackets here. And now we can specify a specific key and assign a value to it. So if this key does not exist, we will add the key and specify that value for it. Or we will simply update the value of that key, if it already exists. If we didn't want to use index access syntax, we could use the put method to do the same thing. We can remove a value by saying remove and passing in the key that we want to remove within the map. Or we could explicitly update a value by calling replace and passing in the key that we want to replace and the updated value that we want to use. Now, you should have a basic understanding of how to start working with both set and map, and both of these will come in very handy as you continue to build your Kotlin applications.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (13), [[Data Structures]] (3), [[Java]] (3)
> **Code Identifiers:** setof (5), testscores (5), mapof (4), mutablesetof (3), containskey (2)
> **Versions:** 91.2 (2), 79.1 (2), 84.5 (1)
> **Definitions:** is a  (1), is an  (1), is called (1)
> **CLI Commands:** make (2)
> **Analogies:** for example (2)
> **Speakers:** - [instructor] (1)

#### [Working with collections](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/working-with-collections-14799791?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/working-with-collections-14799791?u=76281980&t=0)** - [Instructor] While collections such as list, set, or map are useful in their own right, the [[Kotlin]] Standard Library provides many useful helper functions that add even greater programming power to these collection types. We're going to explore how to take full advantage of the Kotlin Standard Library as we learn to transform and query common collection types. There are a number of common patterns across collection types that help make the collections easier to learn and work with. Need to create a read only data structure, we can use our listOf, setOf, and mapOf functions. So like we've seen, we could create a readOnlyList by invoking the listOf function. We could create a readOnlySet by invoking a setOf
>
> **[0:55](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/working-with-collections-14799791?u=76281980&t=55)** or readOnlyMap by using the mapOf function.
>
> **[1:07](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/working-with-collections-14799791?u=76281980&t=67)** And like we've seen if we need immutable versions of these common [[Data Structures]], there are builder functions for those as well. If we simply copy these first three examples, we will pace them down here and change these to mutableList. And instead of listOf, we will use the mutableListOf function. Similarly, we can use mutableSetOf to create a mutableSet variable and mutableMapOf to create a mutableMap.
>
> **[1:53](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/working-with-collections-14799791?u=76281980&t=113)** And again, continuing in the pattern of consistency across collections, we can access the size of any of these using the size property. We can check if a collection isEmpty or not by using the same isEmpty or isNotEmpty functions. So in this case, we could check if our, let's say mutableSet.isEmpty by calling isEmpty or if our readOnlyList.isNotEmpty by calling the isNotEmpty function. And we can also iterate over these different collection types using both the item based for loop as well as the forEach function. So we could say for element_in readOnlySet, println element.
>
> **[2:59](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/working-with-collections-14799791?u=76281980&t=179)** Or we could use the, forEach variant and say something like mutableMap.forEach.
>
> **[3:13](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/working-with-collections-14799791?u=76281980&t=193)** In this case we can rename the lambda parameters to key and value. And we could, let say, print out just the value. And if we go ahead and just run this code, we'll see from our readOnlySet, we see the values one, two, and three printed out. And from our immutable map, we see the values A and B printed out to the console. Now, let's say we want to query for elements in a collection and maybe retrieve some of those elements. We could use the first function which is available on several of the common collection types. So for example, if we wanted to get the first element out of our readOnlyList, we could call the first function. And similarly, if we wanted to work with our mutableSet, we could get the first item out of our mutableSet. Maybe we want to take not just the first item but N number of items, we could use the take function for that use case. So we could say mutableLis.take, and let's say we'll just take the first three items, or maybe readOnlySet.take two items. Another really helpful method out of the Kotlin Standard Library for working with conventions is to use the filter function to filter a collection using some predicate.
>
> **[4:49](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/working-with-collections-14799791?u=76281980&t=289)** This will then return a copy of the collection whose values satisfy the boolean condition. So for example, let's say we want to work with our mutableMap here, and we want to filter out any of the key value pairs where the key is less than two. So we could say mutableMap.filter. This filter function is going to give us back each entry from that collection. And remember a map entry includes a key and a value. So we will name that implicit lambda parameter entry. And for our boolean condition, the thing that we will use to to filter out values will say entry.key less than two. For a different filter, maybe in our readOnlyList, we want to filter out values that are not greater than one. So in this case, we could say it greater than one. Now, remember it is the implicit name for the perimeter passed to a lambda. So in this case, it would represent each value in that readOnlyList. And as always, we could change the name of that it parameter to make it a little bit more meaningful and we could name it something like value, and then change our conditional expression to value greater than one. Sometimes we may have one collection of data,
>
> **[6:24](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/working-with-collections-14799791?u=76281980&t=384)** and want to transform that into some other collection. So take this map of programming languages for example. So we're mapping the language name to some integer value. Let's say in this hypothetical scenario, the higher the value, the more popular the programming language. Now, let's say that we want to transform this list into just an alphabetized list of strings representing the languages that score a four or better. For this type of used case, we can combine these different types of collection functions that we've been working with to make this type of transformation very straightforward. First, we can use filter to filter only languages whose value is greater than four. So we can reference languages.filter, it.value greater than or equal to four. Then we could use the map function to turn our collection of key value pairs into a list of strings. So in this case, we'll say map, it.key. So what we've done here is converted our map entries which contain a key and a value and we've simply converted that into simply the key. So after calling the map function here rather than having a map of key to int,
>
> **[7:58](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/working-with-collections-14799791?u=76281980&t=478)** we will have a list of just strings representing those keys. Then we could use the sorted method to sort the strings by their natural sort ordering. And finally, we could print out each of these values by using forEach and calling println and passing in that string. Now, if we go ahead and run this, we will see [[JavaScript]], Kotlin and [[Python (Programming Language)|Python]] here. And if we notice that those are in order, JavaScript having a value of four, Kotlin having a value of five, and Python having an order of six. This type of [[Data Transformation]] leverages these powerful functions to enable developers to write complex code in a very concise, easy to follow syntax. And these functions are written to be generic so they can be used regardless of which type of collection we're working with. Think of a time you've needed to transform a collection of data, would any of these standard library functions have been a helpful? As you continue working with Kotlin, you'll likely find that you turn to the Kotlin Standard Library often.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (7), [[JavaScript]] (2), [[Python (Programming Language)|Python]] (2), [[Data Structures]] (1), [[Data Transformation]] (1)
> **Code Identifiers:** readonlylist (5), readonlyset (4), mutableset (4), mutablemap (4), isempty (4)
> **CLI Commands:** make (3), python (2), find (1)
> **Analogies:** for example (3), such as (1)
> **Best Practices:** the key is (1)
> **Speakers:** - [instructor] (1)

#### [Sequences](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sequences-14802643?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sequences-14802643?u=76281980&t=0)** - The [[Kotlin]] Standard Library provides many helpful functions for working with iterable collections such as List. These functions make complex processing of collections quite easy. However, there are performance considerations to be aware of. Let's start with sequences and, and discuss when to use sequences over other collection types. When we work with iterable collections such as List or Set processing the collection with functions like filter or map create intermediate collections. So let's just imagine that we have a variable here called languages and it is holding a list of programming languages.
>
> **[0:47](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sequences-14802643?u=76281980&t=47)** There we go. So we have a list holding Kotlin C++ and [[Java]]. Now, let's say that we want to do something like a filter where we maybe want to filter out any languages whose length is not greater than four and then maybe we want to map this from the string to let's say simply the length of that string instead. So each of these functions that we're applying to our collection here create an intermediate collection. These intermediate collections are eagerly process meaning the filter or the map is carried out for every element in the collection, before the next function is applied. For small collections like our example here, this might not be a big deal. But for large collections or for collections that are going to undergo a lot filtering or other processing, the eager application of these functions can be computationally wasteful. To help with this scenario, Kotlin introduces the concept of sequences. Sequences are an iterable container providing much the same functionality as iterable which is what backs most of our common collection types but sequences have different performance traits. Rather than eagerly applying a transformation to every item
>
> **[2:22](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sequences-14802643?u=76281980&t=142)** in the iterable container sequences apply all processing steps to each individual item. You could think of sequence as being a depth first approach whereas iterable are more of a breath first approach. So let's take a look at how we could actually create a sequence and there are several ways to create a sequence like with most things in Kotlin. The most common way of creating a sequence is very similar to that of a List. So we could say val_LanguageSequence = sequenceOf
>
> **[3:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sequences-14802643?u=76281980&t=180)** and we could pass in our same collection of language strings. Another option for creating a sequence would be to do something like this: We could say val fromiterable = list of(1, 2, 3)
>
> **[3:22](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sequences-14802643?u=76281980&t=202)** and then once we have a collection, we could use the as sequence function to convert that collection into a sequence. Now, when working with sequences, we have access to many of the same helper functions as we do with our other standard iterables. We could filter out items again in this case any string whose length is not greater than four we could also then map out the values and convert into the length integer as opposed to the language string itself. And we could also let's say take the first two items out of this processed collection. Now, this is where the difference between sequences and iterables gets really interesting. If we were working with an iterable, then in this process here where we're working with language sequence, both the filter and the map would be applied to every element in the original collection before the final call to take is made. With sequences however, each element is transformed and evaluated until a terminal operator is reached. So with an iterable,
>
> **[4:55](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sequences-14802643?u=76281980&t=295)** each filter is applied, then each map call and then finally the take two result is taken. For large collections, this could be potentially very wasteful. Imagine we have a collection of a million items and we only wanted to take two of those items having to do that filter and that map on every item only to then take two is wasting a lot of processing potentially. So for large collections in this type of situation using sequence potentially has significant performance benefits when processing can be avoided for large portions of the collection. So in the example of our language sequence here we're going to perform the filter and the map on each item individually before then coming back to the next item. So we would call filter on the first item, then map on the first item and then take on that item, if it passes our filter. Same thing we would get to the second item we would check if it passes the filter if it does, we would call map on it and then take it and then at that point we would be done. Once we have two items there's no need to process any of the other items in the underlying and collection. So when should you use a sequence as opposed to an iterable? That's a great question. There's not an exact science to it but there are some general guidelines to follow.
>
> **[6:29](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/sequences-14802643?u=76281980&t=389)** If you're working with smaller collections on the order of tens or hundreds then you're probably fine to use iterable. If the size of your collections starts to grow into the thousands or more, you could consider a sequence especially if you're going to do a lot of filtering after applying transformation functions such as map or filter. Don't stress too much about premature optimization of your code though. If you think you might have a good use case for sequences but aren't sure? You could always profile the performance of that code and compare the results to make an informed choice.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (4), [[Java]] (1)
> **Analogies:** such as (3), imagine (2), similar to (1)
> **CLI Commands:** make (2)
> **Code Identifiers:** sequenceof (1)
> **Tools:** terminal (1)
> **Warnings:** be aware (1)
> **Speakers:** - the (1)

#### [Challenge: Filtering a list of data](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/challenge-filtering-a-list-of-data-14799787?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/challenge-filtering-a-list-of-data-14799787?u=76281980&t=0)** - [Instructor] Let's take on another challenge. This time we're going to work with collections processing. So back in IntelliJ, we're going to start by reviewing the collection of the provided data here. We've created a map and stored it in a variable called test scores. Each string value in this map represents a student ID and each double value represents a test score. For this challenge, you're tasked with identifying the three students with the lowest test scores. You're free to use any collection transformation functions you need such as filter, map, et cetera. And here's a hint. You may find it helpful to convert the map to a list to simplify the sorting and filtering of the data. Now within IntelliJ, the scores are defined in testscores.kt, just to help keep them out of the way and easy to find. You'll want to write your code within main kt. And you should be able to access that test scores map without any trouble.

> [!info]- Semantic Content
>
> **CLI Commands:** find (2)
> **Tools:** intellij (2)
> **File Paths:** testscores.kt (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Filtering a list of data](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-filtering-a-list-of-data-14800815?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-filtering-a-list-of-data-14800815?u=76281980&t=0)** - [Instructor] How'd that challenge go? There's honestly a lot of ways we could go about solving this one. So let's just chat through the thought process behind one such solution and you compare your results with this solution. So I'm back here in IntelliJ and I've got my main.kt file where we're going to write the solution, but just to start, I want to take a look at test-scores.kt, and just review our data once again. So we have our test scores variable holding a map of data and we have string values representing the student IDs that we want to ultimately identify. And then we have these test score values. So I'm going to come back over to main.kt. And the first place that I would start is by wanting to filter the data. We ultimately want to find the three lowest scores. So filtering the data will be a must. Now we can't easily filter a map by its values. We could use the toSortedMap function, which I'll just write out here. ToSortedMap would help us filter the keys in the map but it's not the most helpful thing here. So the first step that we could use would be to actually take our test scores variable and convert that to a list.
>
> **[1:35](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-filtering-a-list-of-data-14800815?u=76281980&t=95)** Now the resulting list will include pairs of the student ID and the test score. Now that we have a list of our test data, we can more easily sort the list based on the test scores. For this, we can call the sortedBy function. SortedBy takes a Lambda that we can use to determine what value should be used in the sorting comparison. In our case, we want the test score which we can access as the .second property on each pair. So we will name the implicit Lambda parameter pair and then we will access pair.second. And just to help us identify what we've done here, we'll just say sort by test score. Once we sorted the IDs and test scores, we really don't need the test score anymore. So in this case, we could convert the pairs of ID and score to just a list of IDs. And we'll do that by using the map function. And again, we'll name the Lambda parameter pair. And this time we will say pair.first. So this will map to just the student ID. Now that we have a sorted list of student ID numbers, we want to select the three lowest scores.
>
> **[3:12](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-filtering-a-list-of-data-14800815?u=76281980&t=192)** Since we sorted our collection, based on the score already all we need to do is take the first three elements from the collection. And again, just for our note here, we'll leave a comment saying take three lowest values from the sorted list. And now just to cap things off, we will print out each element in this final collection by using forEach and printing out that string representing the student ID. So now to check our work, we'll print out the result of this code and we see three IDs printed out to the console. So now if we open up our test-scores.kt file, we can just kind of visually check our work here. So if we scroll through this, we see the lowest score here being a 62.1 which has an ID of 385agc. And sure enough, we see that printed out as the first item in our console output. For our second lowest, we have 66.1 which we see printed out as the second item in our output. And the third lowest we have 72.1, which again is printed out to the console. So how did your solution compare to this one? Did you make use of any other functions? Can you think of other ways we might accomplish this same result?
>
> **[4:44](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-filtering-a-list-of-data-14800815?u=76281980&t=284)** As with most things in programming, there's no perfect right or wrong answer. In [[Kotlin]], there's often many ways to accomplish the same task. To help us pick the best solution to our problem, it's helpful to explore the Kotlin standard library and to look for helpful functions that might come in handy next time we need them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (2)
> **File Paths:** main.kt (2), test-scores.kt (2)
> **Code Identifiers:** tosortedmap (1), sortedby (1), foreach (1)
> **Versions:** 62.1 (1), 66.1 (1), 72.1 (1)
> **CLI Commands:** find (1), make (1)
> **Tools:** intellij (1)
> **UI Navigation:** select the (1)
> **Speakers:** - [instructor] (1)


### 7. Working with Inputs and Outputs

[↑ Back to Table of Contents](#table-of-contents)

#### [Command-line arguments](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/command-line-arguments-14797835?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/command-line-arguments-14797835?u=76281980&t=0)** - [Instructor] When we run and deploy our programs, sometimes we might want to control the runtime behavior by passing arguments to the program when it's executed. This makes our programs more flexible and easier to adapt to different situations. We're going to explore how to do this in [[Kotlin]] by passing command line argument to our main function. So we've seen many examples of main functions so far in this course. A basic main function may be defined with no parameters and has to have the name of main, like we see here. And you'll notice that in this form, we have the green play button next to it and if we click that button, our main function will be run and in this case, nothing will actually happen because nothing has been told to happen inside of the main function body. So in this form, the main doesn't allow us to pass and get access to any arguments when the function is called. To support passing command line arguments to our program, we can update our main function to take in an array of strings. So we do this like we would define any other function parameter. So first I'm going to name the parameter args and then it will take in an array of string. Now, it could also take in a vararg parameter, so we could change this to be vararg and we could call this args of type string. And this will work as well,
>
> **[1:39](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/command-line-arguments-14797835?u=76281980&t=99)** because any vararg parameter will be treated as an array of the given type under the hood. So now, each value in our argument's array will represent an individual argument passed when our program is run. And because those arguments are passed in an array, we can treat them as such, so we could access an individual argument. So if we wanted to print out what the first argument was we could do something like this, where we call println. We say, first arg is and then now we could say args and use index access to ask for the first item in the array. Or we could iterate over every item in the array and so we could say something like for arg in our args, println arg. So, in both of these cases, in the first one we're accessing the first element of the args directly, in the second case here, we are just trying to iterate over each passed argument. Now, if we run this code, we actually see an exception printed out to the console here. In particular we see an array index out of bounds exception. Now, why do we think that might be? Well if you guessed that it's because we haven't actually passed any arguments to our main function yet,
>
> **[3:10](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/command-line-arguments-14797835?u=76281980&t=190)** you're correct, congratulations. When working in IntelliJ, we can pass arguments to our main function by editing the run configuration for our application. So to do that there's a couple ways, we could go to the run menu and then navigate down to edit configurations. Or from within IntelliJ's toolbar, we could come up to this dropdown here next to the run button. And in that dropdown button, we should also see edit configurations. So I will just go ahead and select that. And within this new popup there should be a field here called program arguments. So within this, I can enter any command line arguments that I would like to be passed to my main function. So in this case, I'm going to type Kotlin, [[Java]] and Go. Now notice that these are simply separated by a space. So now that we've defined our three parameters, we hit okay. Now if we run our code again, we see first arg is Kotlin printed out and then we see Kotlin, Java and Go each printed out. So this time when we've updated our run configuration, we've passed in those arguments, those arguments are then getting passed to our main function and we can access them through this args array. Now, like we saw the first time we tried to run our code,
>
> **[4:44](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/command-line-arguments-14797835?u=76281980&t=284)** if we're going to possibly take arguments we want to build in error handling. So if our program requires arguments in some cases but none are passed, we could provide some type of useful feedback message to the user and try to prevent any such crash. So, we could come up here to the beginning of our main function and we could check if the arguments array is empty and call println with some useful default message. So we could say, if args.isEmpty print, please pass at least one argument and then we could return from our main function early. So now if we run our code, again we still see the arguments printed out because we're still passing them to the main function. But if we go back to our configurations and we remove those three parameters. Now if we run our code, rather than crashing we see the output, please pass at least one argument. So in this case our program's not doing anything particularly useful, but it's also not crashing, so it's an overall better experience for the end user. By leveraging command line arguments, our programs can be configured to work in different environments and operate on different files or directories. However, by opening up our program
>
> **[6:20](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/command-line-arguments-14797835?u=76281980&t=380)** to take in those external arguments, we also have to account for additional error scenarios like empty arguments or invalid arguments. So to help provide that great end [[User Experience (UX)|user experience]], we want to make sure that we're handling both the happy path, where we get the arguments we want and the error scenarios where might go wrong.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (4), [[Java]] (2), [[User Experience (UX)|User experience]] (1)
> **Tools:** command line (4), intellij (2)
> **UI Navigation:** dropdown (2), go to (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** isempty (1)
> **Cross-References:** go back to (1)
> **Definitions:** is called (1)
> **Speakers:** - [instructor] (1)

#### [Receiving user input](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/receiving-user-input-14799789?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/receiving-user-input-14799789?u=76281980&t=0)** - [Instructor] Receiving user input can make our programs much more dynamic and useful. Let's look at how to receive input from the user and validate that input to ensure it represents a valid file. The primary way of receiving input from the user is by calling readLine. Let's first start by printing a prompt to enter a valid filename. So we could do that by typing, print. Now notice we're using print this time instead of println, this is just a small difference that will allow us to enter input on the same line as the prompt. If you wanted the prompt to be on one line and enter the value on the next line, you could use println here. Now the prompt itself will say, enter a filename and I'll add a colon just as a separator there, and then a space. Now we can then receive input from the user by calling readLine and assigning the entered value to a variable. So in this case, I'll say Val filename equals readLine. And then we could print out the entered value to indicate that we did in fact receive the correct input. So here, I'll type println, you entered $filename,
>
> **[1:29](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/receiving-user-input-14799789?u=76281980&t=89)** there we go. So before we go any further, let's just print this out and test out how this is working so far. So here, we've gotten our prompt to enter a filename and we can see our cursor here. And now I might enter a filename. Maybe I will call this file.kt and I'll hit Enter. And then the program prints out, You entered file.kt and the program finishes. So that's perfect. Everything so far is working as we hoped. Now, once we have our input, we can validate that the input is actually a valid file or not within the project's root directory. To do that, we can create an instance of a file and check whether that file is valid or not. So I'm going to create a new variable here called isValidFile. And then I'm going to sign that the result of creating a new file using the file constructor. When you type file and add the parentheses, you may be prompted to import the file. You may notice here that now at the top of the file I see import [java.io](https://java.io).File. This is going to pull in that file type from the standard set of [[Java]] components which we have full access to via [[Kotlin]]. So to finish off creating my file then
>
> **[3:03](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/receiving-user-input-14799789?u=76281980&t=183)** I want to pass in the filename that was entered by the user. And now to check whether or not that is a valid file, we can access isFile here. So if this is a valid file within the project's root directory, this will return true. Otherwise it will return false. Now the IDE here is actually giving us a helpful warning. If I hover over filename here, it's saying type mismatched. File expects a non-nullable string, but we've actually passed a nullable string. So why is that? Well, it's because readLine is not guaranteed to return us a non-null string. So to fix this, we could come back up here to our call the readLine, and we could use the Elvis operator here. And remember, the Elvis operator is a question mark, colon. And what this will do, is that if the result of readLine returns, no, then we will instead substitute in whatever's on the right hand side of the Elvis operator. So now filename will either be a valid string from readLine or the default empty string. And now if we come back down to creating our file, we see that the warning has gone away because now filename is guaranteed to be non-null. So once we have a file and we know whether or not that file is valid, we can use that information to provide useful feedback
>
> **[4:39](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/receiving-user-input-14799789?u=76281980&t=279)** to the user. So we could do something like if is valid file println, it is a valid file,
>
> **[4:55](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/receiving-user-input-14799789?u=76281980&t=295)** else println it is not a valid file.
>
> **[5:04](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/receiving-user-input-14799789?u=76281980&t=304)** So now if we run our code again, we'll once again be prompted for a filename. So again, I'll just use a placeholder filename here for now, and I'll just call this file.kt and hit Enter and we'll see you entered file.kt, it is not a valid file. So that is actually exactly what we would expect here. Now let's try the happy path. Let's open up our project directory window here and let's just look for the name of a file in our root project directory. So again, the root project here is this one at the very top. If I open this back up, we'll see a number of top level files here. So for example, we could check for the existence of [[Gradle]].properties. So I'm going to run the code again here, and this time when prompted to enter a filename, I will type great gradle.properties and I'll hit Enter, and this time it says that it is a valid file. By prompting a user for input, receiving that input validating it and operating on it, we can build much more interactive applications that adapt to the needs of the individual user.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Java]] (2), [[Gradle]] (2), cursor (1), [[Kotlin]] (1)
> **Code Identifiers:** readline (7), isvalidfile (1), isfile (1)
> **Definitions:** is a  (5)
> **File Paths:** file.kt (4)
> **Warnings:** warning (2)
> **CLI Commands:** make (1)
> **URLs:** [java.io](https://java.io) (1)
> **Env Vars:** ide (1)

#### [Read and write to files](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/read-and-write-to-files-14798817?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/read-and-write-to-files-14798817?u=76281980&t=0)** - [Instructor] How do you read a list of test scores from a file or save processed data to a file for later use? Let's explore how we can work with file inputs and outputs in [[Kotlin]]. Let's start with reading from files. To get started, we'll create a new text file in our root project called scores.txt. So to do that from IntelliJ, I'll open up the project directory window here. And within the root project directory, I'm going to right click, go to new and file. And I'm going to name this file scores.txt and hit enter . In that file, we'll add a list of data representing user IDs and test scores separated by a colon. So I'm just going to paste in some example data here. So here we have seven pairs of ID and test scores. So now back within our main.kt file, we want to read that file in and store it as a variable. So to do so, we will create a variable called scoresFile and then we're going to create an instance of the file. And when prompted by the IDE to import that, we can move our cursor over the file name and hit Alt + Enter and that will import the file type for us.
>
> **[1:37](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/read-and-write-to-files-14798817?u=76281980&t=97)** And now within the file constructor, we want to pass the name of the file. So for us, that will be scores.txt. Now, we have a reference to our scores file. Once we have a valid file, there are a couple of ways to read from that file. The easiest is to call for each line which will read the file and call us back with each mine in that file, while also automatically closing the file for us. So that would look something like this. Where we would reference the scoresFile variable and we would call four each line and then pass a Lambda to that function. To print out each line of that file, we could simply use println. So to start, we will do that like this, println, and we pass in it as the implicit Lambda parameter representing each line of the file. And to just be a little bit more explicit here, we will rename that implicit Lambda perimeter as line. That way it's more clear, that we are iterating over each line in the file and printing out individual lines. So, now if we run our program here, we will see the output of that scores TXT file, printed out to the console.
>
> **[3:10](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/read-and-write-to-files-14798817?u=76281980&t=190)** Now, let's say we want to take those lines that we read in from the file, sort them and write them out to a new file. For that, we could use another useful function called read lines, to go get a list of all lines in the input file and we'll then call to sorted on that list to sort out all those lines. So we'll create a new variable here called sorted lines equals scoresFile.readLines and then we will call sorted on that list. So now that we have this sorted list of lines from that file, we want to write that out. And to write output to a file, we need to set up the file that we want to write to. So we're going to write out to a file called sortedscores.txt. So we'll create a variable to hold this output file. And again, we'll create a new instance. A file passing in the name of that output file, sorted-scores.txt. Now, once we have this output file setup, there are a number of different ways we could go about actually writing to that file. But perhaps the easiest for our use case, is to convert our file to an instance of path. And we can do that by simply calling topath
>
> **[4:46](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/read-and-write-to-files-14798817?u=76281980&t=286)** on that file once it's created. By converting to a path, we open ourselves up to some specific APIs that make writing lines out to a file path quite easy. So once we have that output file variable, storing the path instance pointing to sorted-scores.txt, all we have to do is say, outputFile.writeLines and we can pass in our sorted list of lines that we want to be written out to that file. Now, if we check our route directory here, we'll see at the moment, that there is no sorted-scores.txt file. However, if we run our code, we see our output from printing out each line. But more interestingly, if we open up the project directory viewer again, we now see sorted-scores.txt. And if we open that up, we will see in fact that the scores have been sorted based on the user IDs. We've only touched the surface of file input and output in Kotlin. But with these fundamentals, you have everything you need to start working with file I/O in Kotlin.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (3), cursor (1)
> **File Paths:** sorted-scores.txt (4), scores.txt (3), main.kt (1), sortedscores.txt (1)
> **Code Identifiers:** scoresfile (3), readlines (1), outputfile (1), writelines (1)
> **Env Vars:** ide (1), txt (1)
> **Prerequisites:** set up (1), setup (1)
> **CLI Commands:** make (1)
> **Tools:** intellij (1)
> **UI Navigation:** go to (1)

#### [Challenge: Parsing a list of data from a file](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/challenge-parsing-a-list-of-data-from-a-file-14802633?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/challenge-parsing-a-list-of-data-from-a-file-14802633?u=76281980&t=0)** - [Instructor] Here's the challenge to help reinforce the concepts of user input in File IO. You are tasked with writing a program that helps the teacher find the students with the highest test scores. Within IntelliJ, the test scores will be available within a scores.txt file. Back within IntelliJ, test scores are located within scores.txt and have the following structure, user ID colon score. Each ID score pair is on its own unique line. When you run your program, it should prompt the user to enter the name of the file containing the test scores. The program should then read each line from the file, sort the scores from highest to lowest, pick the three highest scores and write the user ID score pairs to a new file named sorted.txt. Back in IntelliJ. you'll write the code to complete the exercise within main.kt.

> [!info]- Semantic Content
>
> **File Paths:** scores.txt (2), sorted.txt (1), main.kt (1)
> **Tools:** intellij (3)
> **CLI Commands:** find (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Parsing a list of data from a file](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-parsing-a-list-of-data-from-a-file-14802642?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-parsing-a-list-of-data-from-a-file-14802642?u=76281980&t=0)** - Let's take a look at one way which we could solve this challenge to build our test score processing program. To start, let's build the user input To start, let's build the user input so we can read the file name from the user. We'll prompt the user to enter the name of the scores file and then read that input from the user storing it in a variable. So the very first thing we'll do here is to prompt the user to enter the name of the file. So I'll say, enter the name of the file containing scores. So I'll say, enter the name of the file containing scores. And then, we will create a variable called input file name and assign it the value of read line. and assign it the value of read line. And because read line m And because read line might be no,ight be no, we'll use the Elvis operator here and provide a default of empty strength. Next, let's add in a little error handling to prevent against any type of empty input. So we can check if the file name is null or empty and if it is, print a message to the user and end our program. So we'll say, if input file name is null or empty So we'll say, if input file name is null or empty println not a valid file name not a valid file name and return. and return.
>
> **[1:33](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-parsing-a-list-of-data-from-a-file-14802642?u=76281980&t=93)** Now, notice here that with isNullOrEmpty, it's highlighted by the IDE. And it's saying that the call on the not null type may be reduced to simply is empty. Now, the reason for this is that we've already checked the input file name is not null up above when we provided a default value. So we could solve this in a couple ways. We could move our cursor over the is null or empty call, hit alt+enter and then hit enter on the option to change call to is empty. So this will would be one way to simplify things. Or we could continue to use is null or empty and skip the use of the Elvis operator on the read line call. So, either of these will work. In this case, I'm going to go with the basic read line. And then down below, I will check for both the null case and the empty case. At this point, we know that we should have a non-empty string representing the file that we want to read in. So now, we can create a file from the file path and check if that file is valid. So we'll create a variable called inputFile = File So we'll create a variable called inputFile = File will pass in that input file name. We will be sure to import the file type. And now, if input file is not a valid file, And now, if input file is not a valid file, And now, if input file is not a valid file,
>
> **[3:08](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-parsing-a-list-of-data-from-a-file-14802642?u=76281980&t=188)** we'll go ahead and simply return. Next, we want to set up a variable to read in the ID score pairs from the input list. ID score pairs from the input list. So let's say, val rawScores=inputFile.readLines So let's say, val rawScores=inputFile.readLines So let's say, val rawScores=inputFile.readLines So this should read in each individual line. And if we remember from scores.txt, And if we remember from scores.txt, each line then is formatted in a particular way with the ID, separate by the colon and then the actual score. So, to be able to complete our challenge and figure out which scores are the highest, we'll want to parse these scores and split out the values. So I'm going to create a variable called parsedScores So I'm going to create a variable called parsedScores and then we're going to work with the raw scores input here. So we're going to say rawScores and we're going to map each line and we're going to map each line and we're going to convert each streamline into a pair containing the ID and the score. And so to do that, we're going to make use of a couple of functions here. So first, we'll create a variable called elements and we'll assign it the result of saying line.split and we'll use a colon as our delimiter here. So what this is going to do
>
> **[4:42](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-parsing-a-list-of-data-from-a-file-14802642?u=76281980&t=282)** is take in each line that we read in and then split this string based on that colon. Now what we can do is access the different elements Now what we can do is access the different elements of this split array and use that to create our pair of ID to score. So in our map here, we will return the result of saying elements 0 we will return the result of saying elements 0 which will represent the ID to elements 1 to elements 1 which will represent the score. Now that we have access to the actual score values, we can use that data to sort the results and take the highest three. So we'll set up a variable called final scores and that will be equal to parsedScores.sortedByDescending and that will be equal to parsedScores.sortedByDescending and that will be equal to parsedScores.sortedByDescending it.second it.second it.second and then we will take 3. and then we will take 3. and then we will take 3. So, what we've done here is take the parsed scores, we've sorted them in descending order using the second value of each pair. So if we go back to our previous variable there, the second value of each pair should represent the score. So this is going to sort the scores in descending order. And then we take 3 to get the three highest scores.
>
> **[6:17](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-parsing-a-list-of-data-from-a-file-14802642?u=76281980&t=377)** Now, the last thing to do is to write to the file. So to prepare for writing, we will create our output path. And remember, we want that output file to be called sorted.txt. to be called sorted.txt. And we want to work with the path API for writing output lines. So we will convert the file to a path using that two path function. Now, we don't simply want to write out just the scores or just the IDs, we want to write them out in the same format in which we read them in. So, we need to format the final scores into a format a little bit easier to write. So we'll do this by creating a variable called output scores and assigning it the result of finalScores.map. And in our mapping function here, we will use a string template to set up the same string format that we were using when we read them in. So that will be it.first: So that will be it.first: So that will be it.first: followed by it.second. followed by it.second. And now, we can use outputPath.writeLines And now, we can use outputPath.writeLines pass in the output scores. And this should print out the values that we expect to our sorted.txt file. So, if we go ahead and run this code,
>
> **[7:51](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/solution-parsing-a-list-of-data-from-a-file-14802642?u=76281980&t=471)** So, if we go ahead and run this code, we see here that we are prompted to enter the name of the input file. So, that will be scores.txt. So, that will be scores.txt. And our program then finishes. If we look at the project directory here, we now see that we have a sorted txt file. And if we open that up, we will see those highest three scores. We see 98, 91 and 88. We see 98, 91 and 88. If we check that against the input, sure enough, we see 98, 91 and 88 sure enough, we see 98, 91 and 88 as the three highest scores from that input file. So, how was your solution? Was it similar to what we just walked through? Did you find different ways to process the input or right to the file? However you solved the challenge, this type of problem closely mirrors the types of challenges we might face in a production code base. And as we've seen, [[Kotlin]] provides many tools for solving those types of challenges.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** cursor (1), [[Kotlin]] (1)
> **Code Identifiers:** inputfile (5), parsedscores (5), rawscores (4), readlines (3), sortedbydescending (3)
> **File Paths:** scores.txt (4), sorted.txt (3)
> **Prerequisites:** set up (3)
> **CLI Commands:** make (1), find (1)
> **Env Vars:** ide (1), api (1)
> **Cross-References:** go back to (1)
> **Exercise Files:** template (1)


### 8. Testing Kotlin Code

[↑ Back to Table of Contents](#table-of-contents)

#### [Writing JUnit tests](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/writing-junit-tests-14800827?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/writing-junit-tests-14800827?u=76281980&t=0)** - Testing is an important part of writing production quality code with any programming language. We're going to learn to write basic unit tests in [[Kotlin]] using the popular test framework [[JUnit]]. Now, before we can write our first JUnit test, we need to add the necessary dependencies to our project. So within IntelliJ, we'll navigate over to our project viewer, and we want to open up the build.[[Gradle]].kts build script file. Here we want to scroll down until we find the dependencies configuration block. Here we want to make sure that we have added the Kotlin test dependency to our testImplementation class path. This will make testing classes and functions available within our test code. Now, this is likely already in place from when you created your Kotlin project, but if it's not, you want to configure it as you see here. Now, once the test dependency is added, we also want to make sure that our tests are configured to use JUnit by default. So below that you should see a test task configuration block marking each test as use JUnit. This will help us pull in the JUnit testing framework dependencies that we want to work with when defining our unit tests. Once we're sure our builds is configured properly, we can resync our project just to be sure the IDE will recognize these new dependencies.
>
> **[1:34](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/writing-junit-tests-14800827?u=76281980&t=94)** To resync the project can open up the Gradle tool window from the right-hand side of the screen here or navigate to View, Tool [[Windows]], scroll down until you find Gradle, And within that Gradle tool window, in the menu, there should be this Reload All Gradle Projects button. If we click that, it will resync the project and make sure that all needed dependencies are ready to be used. Now we're ready to create our first test class. To do this, we will navigate to our project window on the left side of the screen, and we will navigate to src, test, kotlin. And then we will right-click, select New, Kotlin Class/File. We're going to select to create a new Class, and we're going to name that class RandomNumberGeneratorTest and then hit Enter. This class is where we will write all of our individual unit tests. Our first test is going to be quite basic. So I'll just hit Enter a couple times, come down into our class body. And we're going to write a function called verifyTrueIsAlwaysTrue So for this test, we're going to do what the function name suggests and simply verify that the value true is always equal to itself. And to do this, we will use an assert function
>
> **[3:10](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/writing-junit-tests-14800827?u=76281980&t=190)** and pass the expression true == true. Assertion functions will throw an exception if the past expression evaluates to false. So in this case, if true equals true, our test will pass. Otherwise our test will fail. Now for the final step here, we need to indicate to the JUnit test framework that this function is actually a JUnit test as opposed to a regular function. So for the final step, we will mark this function as an actual test by adding the @Test annotation. Now, once we've written @Test, we will be prompted to pick from a couple different available test types here. We want to make sure that we are selecting the one coming from the org.junit package and hit Enter. You'll know you've done it right if at the top of the file you now see import org.junit.Test. Once the annotation is added, the IDE will now recognize our test and test class as test elements that maybe run invalidated. If we click the little play button on the class name, this will run tests for the entire test class. And down in the tool window that popped up, we see this Test Results drop down here. And if we click this dropdown, nothing happens at the moment because everything was successful. However, as we'll see in a minute, if we had failing tests here,
>
> **[4:42](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/writing-junit-tests-14800827?u=76281980&t=282)** we could use this to drill deeper and see which tests were failing. If we wanted to run just the individual test that we've written, we can click the play button next to that individual test and we will see the same output window pop up. And notice in this case, like we've already saw, our test succeeded because true is always equal to true. If we change this expression to assert that false equals true and we rerun the test here, this time we see failing test results. And in the little test window here at the bottom, we see that we had a failing test within the RandomNumberGeneratorTest class. And then below that nested, we see which specific test failed. And in the test output we can even find which specific line failed. And if we click on that, in the editor it'll actually take us directly to that line making the test easy to locate. When writing tests, we typically follow the same general pattern. We set up the test variables and conditions, call the function or method that we want to test, and finally, we validate the expected and actual values. We've already seen how assert can be used for a trivial test. So let's build on that example as we add additional, more interesting tests. To start, we're going to write a function that returns an integer value
>
> **[6:17](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/writing-junit-tests-14800827?u=76281980&t=377)** between zero and some passed max value. Now we want to simulate how we would do this in a real code base. So we are going to write this test in our main source directory. So we're going to open up Main.kt which lives under src/main/kotlin. And above our main function here, we're going to this new getRandom function. GetRandom will take in an argument called max of type int. And for its implementation, we will generate a random integer value using the Random class from Kotlin.
>
> **[7:04](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/writing-junit-tests-14800827?u=76281980&t=424)** And we will pass our max value to nextInt. Calling Random.nextInt with this max value will generate a random number between zero and whatever that max value is that we passed. Now we'll return to our RandomNumberGeneratorTest class. If our expectation is that this getRandom function returns a positive value between zero and some max value, we can write tests that validate this. So we're going to write a new test function. And this time we will use a special function naming syntax that Kotlin supports specifically for test code. So to start, we will write our @Test annotation, the fun keyword, and now, rather than jumping in and writing our name directly using the same naming patterns that we might typically use for a function, we're going to add backticks. And now within these backticks, we can define our name using human readable text. So we can name this getRandom returns value between 0 and 100. So now this is our test name and so when we see this test name in our output, it becomes very easy to read and understand what to expect from this test. Now to finish implementing this test, we'll create a variable called random and assign it the result of calling getRandom
>
> **[8:39](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/writing-junit-tests-14800827?u=76281980&t=519)** and passing in 100. And finally, we want to assert that that random value is within the range of 0 to 100. So again, we'll call assert and this time we will use the expression random in 0 to 100. So I'll generate a range from 0 to 100 and assert that the value random is within that range. And now we can go ahead and test this by running that function a couple of times. We'll see that the test passed that time. However, because we're dealing with random values here, there could be some inherent uncertainty so we'll just run this a couple more times to verify that each time we run it, it seems to pass. And sure enough, we've run it three times and it passed each time. So this is kind of a positive test. We have some expectation and we are asserting that that expectation is always met. Now, what if we wanted to write a negative test here? Let's say we want to verify that an illegal argument exception is thrown when a negative value is passed to our getRandom function. So this is kind of a test to verify that our function fails in the way we expect. So once again, we'll start off by defining a new test by adding a test annotation. And at this time the function will be named getRandom throws exception
>
> **[10:16](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/writing-junit-tests-14800827?u=76281980&t=616)** when negative value is passed. And now we will call getRandom and we will pass in a value of -1. So if we run this test now, we will see that the test fails by throwing in illegal argument exception. However, in this case, throwing that illegal argument exception is actually what we expect. That's how we think that our function should fail. So in situations like this, we can actually modify our test annotation and indicate to it that we actually expect a particular exception to be thrown. So we could you that by adding parentheses after our test annotation and then we will use named argument syntax here to assign an exception type to the expected argument value here. So we will pass a IllegalArgumentException::class. So this is now telling this test that when it runs, we expect an illegal argument exception to be thrown, and if it isn't, then fail the test. If illegal argument exception is thrown, then treat this test as if it passed, because that's our expected outcome. So now if I run this again,
>
> **[11:51](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/writing-junit-tests-14800827?u=76281980&t=711)** this time we see that our test passed, because that exception was in fact thrown. By writing unit tests for your Kotlin code, you can build greater confidence in that code and be more assure that it will perform the way you expect.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (9), [[JUnit]] (9), [[Gradle]] (5), [[Windows]] (1)
> **Code Identifiers:** getrandom (7), nextint (2), testimplementation (1), verifytrueisalwaystrue (1)
> **UI Navigation:** navigate to (3), scroll down (2), in the menu (1), right-click (1), dropdown (1)
> **CLI Commands:** make (5), find (3)
> **File Paths:** build.gradle (1), main.kt (1)
> **Env Vars:** ide (2)
> **Prerequisites:** configure (1), set up (1)
> **Tools:** intellij (1)

#### [Mocking data](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mocking-data-14799790?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mocking-data-14799790?u=76281980&t=0)** - [Instructor] Sometimes it's not possible to easily set up the environment for a specific test. This could be due to types that are not open for extension, complex dependency graphs, or other reasons. In these situations, it can be helpful to mock out the data and responses that we need to effectively write our test. Mockito is a popular mocking tool for [[Java]] development and works great for [[Kotlin]] code as well. So here's a testing scenario for us. If we open up our code here, we'll see that we have an AnalyticsClient interface, and this interface allows us to track some analytics event. And we also have a ViewModel class. That ViewModel class takes in an instance of an analyticsClient, and it has a public method called submitClicked. And when that button is clicked, it should track an analytics event using that analyticsClient. Now our task is to write a test that validates the tracking of that analytics event when submitClicked is called. So to start, we'll go to our project window, navigate to source, test, kotlin, right-click, select New Kotlin Class or File. And we will create a new test class called ViewModelTest and hit Enter.
>
> **[1:34](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mocking-data-14799790?u=76281980&t=94)** Then within the ViewModelTest class, we will create a new test using our @Test annotation, importing the org.[[JUnit]].Test annotation. And we will name this test trackEvent is called when submitClick is called.
>
> **[2:01](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mocking-data-14799790?u=76281980&t=121)** So here we have our test method, and we want to verify that any time viewModel.submitClick is called, trackEvent is called on the AnalyticsClient interface. Now this is where it gets interesting. We need to create some instance of the AnalyticsClient interface. We could create a class to extend that interface and use that for our test, or we could create an anonymous object expression to create an instance. We'll learn more about both of those strategies later in the course. In both of these cases, we'd have to implement the full interface, but really all we care about is whether or not the track method is called. What it actually does isn't the focus of this test. So this is where mocking can be really helpful. We can mock the object, in this case our AnalyticsClient interface, and we can verify the behavior of that object without actually implementing any of that behavior. So to do this, we need to first pull in the Mockito dependency. We can do this by opening up our build.[[Gradle]].kts file. And within our dependencies configuration block, we need to add the Mockito core dependency to the testImplementation class path. Now here I already have it, it's just commented out. So I'm going to uncomment this. And then I will resync my Gradle project so that that dependency is pulled in and made available to me.
>
> **[3:35](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mocking-data-14799790?u=76281980&t=215)** Now once Mockito has been added to the project and we've resynced, we can return to our test. And now we can call Mockito.mock to create a mocked instance of our AnalyticsClient interface. So I'll create a variable called analytics and assign it Mockito.mock. And that mock method will take in a class type to mock out. So in this case, it'll be AnalyticsClient::class.java. Now when prompted to import the Mockito type, we want to make sure that we import it from org.mockito.Mockito, as we see at the top of the file. Once we have our mocked AnalyticsClient, we can create new instance of our ViewModel. And we can use that mocked instance of the AnalyticsClient to pass to our viewModel constructor. So that is the setup portion of our test. And now we want to exercise the submitClicked method by calling viewModel.submitClicked. And finally, we can call Mockito.verify to assert that analyticsClient.track is called. So we can do this by typing Mockito.verify. We'll pass in analytics as the mock object
>
> **[5:12](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mocking-data-14799790?u=76281980&t=312)** that we want to verify that something happened with. And then we will call track. So what this is saying up to this point is that we want to verify that the track method was called on our mocked analytics variable. And then track takes in some string parameter as part of its API. So we need to tell it what string we should expect here. So we could tell it to expect a particular string like kotlin. This would make our test a little bit less flexible though. So we can leverage another feature coming from Mockito which are matchers. Matchers will help us do pattern matching for different types of expected inputs or outputs. So rather than specifying a specific string here, I could type anyString and then import that matcher from Mockito. So if you look up at the top of our file again here, we see that we've imported the anyString function from org.mockito.ArgumentMatchers. So now what our verification line is doing here is it's going to verify that the track method was called on our mocked interface with any string value. It doesn't matter what it is as long as it's some valid string value. Now if we run this test,
>
> **[6:43](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/mocking-data-14799790?u=76281980&t=403)** we should see that the test passes because the submitClicked method does, in fact, call analyticsClient.track. If we just look at our ViewModel, we can see sure enough analyticsClient.track is called whenever submitClicked is called. If we were to comment out that line of code and rerun our test here, we'll see that this time our test fails. And if we dig into our test input here and look at the failure, we will see this message indicating that analyticsClient.track was expected but was never actually invoked. This indicates to us that our expectation around that interface being used was not in fact met. So this strategy can be a good way to validate that our classes and functions are operating the way we expect. Mocking is a useful tool, but like any tool, it's not always the right tool for the job. Before reaching for a mocking tool, carefully consider whether you can set up your test without it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (4), [[Java]] (2), [[Gradle]] (2), [[JUnit]] (1)
> **Code Identifiers:** analyticsclient (6), submitclicked (6), viewmodel (3), trackevent (2), submitclick (2)
> **Definitions:** is called (9), is a  (2)
> **UI Navigation:** go to (1), navigate to (1), right-click (1)
> **Prerequisites:** set up (2), setup (1)
> **File Paths:** build.gradle (1), class.java (1)
> **CLI Commands:** make (2)
> **Env Vars:** api (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/next-steps-14803698?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/kotlin-essential-training-functions-collections-and-i-o/next-steps-14803698?u=76281980&t=0)** - [Instructor] As we've seen throughout this course, [[Kotlin]] is a powerful and flexible language designed for building modern applications across a variety of domains. Language features such as top-level and higher order functions, default parameter values and data and sealed classes provide first class solutions to some of the most common programming problems. As you continue on with Kotlin, there are many other great resources out there. Be sure to check out Kotlin's "Getting Started" guide, the Kotlin org on [[GitHub]], or work through the Kotlin Koans for more practice. You should now have the tools you need to start building complete applications with Kotlin. For inspiration on where to apply those skills, explore the Kotlin laying website for more information on how to use Kotlin to build mobile, server, browser, and even multi-platform applications. There's plenty more to learn about Kotlin, but hopefully this course has convinced you to try Kotlin for your a next project.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Kotlin]] (10), [[GitHub]] (1)
> **Tools:** github (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)
> **Prerequisites:** getting started (1)
> **Speakers:** - [instructor] (1)


## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/kotlin-essential-training-functions-collections-and-io-3008787)

## Skills Covered

- Kotlin

## Path Context

### In [[Kotlin Professional Certificate by JetBrains]]
**1 of 4** | [[Kotlin Essential Training- Object-Oriented and Async Code]] →

## Part of

- [[Kotlin Professional Certificate by JetBrains]]

## Appears In

- [[Kotlin Professional Certificate by JetBrains]]

## Related Courses

_Courses sharing skills:_

- [[Exploring Ktor with Kotlin Multiplatform and Compose]] — Kotlin
- [[Kotlin Multiplatform Development]] — Kotlin
- [[Kotlin Essential Training- Object-Oriented and Async Code]] — Kotlin

---

[↑ Back to top](#)