---
type: course
cssclasses:
  - lle-course
  - lle-level-advanced
slug: wavelet-analysis-applications-with-wolfram-language
url: "https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language"
duration_minutes: 51
duration: 51m
level: Advanced
updated: 1/10/2024
learners: 1272
skills:
  - Wolfram Language
  - Data Analysis
  - Wavelets
exercise_files: false
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQEpm8u5Dr8Pyg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1704745109030?e=2147483647&amp;v=beta&amp;t=MbOq0DZsQAie-Bck3b8p_ZR-Il0DNlhkN0OYmq3zYOw"
linkedin_topic: Data Science
learning_paths:
  - '[Build Your Wolfram Language Skills](../../Paths/Data%20Science/Learning%20Paths/Build%20Your%20Wolfram%20Language%20Skills.md)'
prev_courses:
  - '[Wavelet Analysis- Concepts with Wolfram Language](Wavelet%20Analysis-%20Concepts%20with%20Wolfram%20Language.md)'
next_courses:
  - '[Modeling Market Prices Using Stochastic Processes with Wolfram Language](Modeling%20Market%20Prices%20Using%20Stochastic%20Processes%20with%20Wolfram%20Language.md)'
path_nav: '[{"path":"Build Your Wolfram Language Skills","position":4,"total":8,"prev":"Wavelet Analysis- Concepts with Wolfram Language","next":"Modeling Market Prices Using Stochastic Processes with Wolfram Language"}]'
path_count: 1
tags:
  - course
  - topic/data-science
  - skill/wolfram-language
  - skill/data-analysis
  - skill/wavelets
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Data%20Science/Wavelet%20Analysis-%20Applications%20with%20Wolfram%20Language.md)

![Wavelet Analysis: Applications with Wolfram Language](https://media.licdn.com/dms/image/v2/D560DAQEpm8u5Dr8Pyg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1704745109030?e=2147483647&amp;v=beta&amp;t=MbOq0DZsQAie-Bck3b8p_ZR-Il0DNlhkN0OYmq3zYOw)

# Wavelet Analysis: Applications with Wolfram Language

> This course presents examples from a variety of wavelet analysis applications in the Wolfram Language, including financial time series, edge detection and denoising of images, thresholding, image and data compression, and image fusion. Familiarity with Fourier transforms and data smoothing methods is recommended for this class. Learn to analyze a time series using wavelets for detecting discontinu

> [LinkedIn Learning](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language) | 51m | Advanced | 1K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (1 videos)
  - [Introduction: Wavelet Analysis](#introduction-wavelet-analysis)
- [**1. Frequency Detection in a Time Series**](#1-frequency-detection-in-a-time-series) (1 videos)
  - [Frequency Detection in a Time Series](#frequency-detection-in-a-time-series)
- [**2. Filtering Frequencies from Time Series**](#2-filtering-frequencies-from-time-series) (1 videos)
  - [Filtering Frequencies from Time Series](#filtering-frequencies-from-time-series)
- [**3. Applying Wavelet Analysis to Finance**](#3-applying-wavelet-analysis-to-finance) (1 videos)
  - [Applying Wavelet Analysis to Finance](#applying-wavelet-analysis-to-finance)
- [**4. Edge Detection on Images**](#4-edge-detection-on-images) (1 videos)
  - [Edge Detection on Images](#edge-detection-on-images)
- [**5. Denoising Images**](#5-denoising-images) (1 videos)
  - [Denoising Images](#denoising-images)
- [**6. Wavelet Thresholding**](#6-wavelet-thresholding) (1 videos)
  - [Wavelet Thresholding](#wavelet-thresholding)
- [**References**](#references) (1 videos)
  - [References](#references)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction: Wavelet Analysis](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/introduction-wavelet-analysis?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/introduction-wavelet-analysis?u=76281980&t=0)** Hello, everyone. Welcome to the seminar on wavelet analysis with the focus on its applications. My name is Paritosh Mokhasi and I'm part of the Wolfram Kernel Technology Group. In order to understand what wavelet analysis does, it's sometimes more prudent to first talk about Fourier analysis. A Fourier transform decomposes signal into its sine and cosine components and converts the time series from a time domain into a frequency domain. The frequency domain indicates which frequencies were present at which instances in time. So for example, if you have a time series given by the signal f(t), the taking a Fourier transform of that through this formula gives us information of what frequencies were present. As an example, let us consider the stationary data, which consists of two signals. One at sine 20 hertz and one at 80 hertz. When you take a Fourier transform of this data, you'll notice that there are two frequencies that show up at 40 and 80 hertz, respectively. Notice that the scale is at two times the frequency. In the above example, the Fourier transform gives us very good information about which frequencies were present in the signal. But what happens when the situation is changed and the signal that you are looking at is a non-stationary signal. So as an example, let us consider a non-stationary signal wherein from 0 to 2,
>
> **[1:35](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/introduction-wavelet-analysis?u=76281980&t=95)** you have a signal which has got a certain frequency content to it, from 2 to 4, there is a certain frequency content, and 4 to 6, a different frequency content. When you do a Fourier transform of this signal, you'll notice that you get three peaks, which is correct. However, you don't get any information about when these peaks occurred at what instance in time. In order to overcome this problem, people generally tend to make use of windowed Fourier transform or short-time Fourier transforms. In a short-time Fourier transform, what we generally do is we window the signal so that it captures only a certain portion of the time series, and on that certain portion of the time series, we perform a Fourier transform. So a windowing on the first from 0 to 2 gives us a one frequency. When you move the window forward to 2 to 4, you get a second frequency. And when you move from 4 to 6, you get a third frequency. So then the obvious question comes up is how should the window be moved, how should the window be scaled, et cetera. In order to avoid all these ambiguity, Fourier transforms have been modified in certain ways, but in order to get a complete understanding about what the signal contains, what kind of information is present in the signal, we can make use of wavelet analysis. So what exactly would you expect to find when you make use of wavelet analysis? So the best way to describe that is through an example.
>
> **[3:09](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/introduction-wavelet-analysis?u=76281980&t=189)** Let us consider this data, which is just a sine function but overlapping the sine function, at some instance in time, you have a very sharp event occurring. Not only is the sharp event occurring at a random location, but the duration for which this event occurs is extremely small. If you were to perform a Fourier transform on this signal with this sharp event in it, you'll notice that the sharp event is completely missed out. This takes just a few seconds and there's the result. And you can see that you have only one peak, and this peak corresponds to the sine function. You see that no other peak is detected by the Fourier analysis. So in essence, it has completely missed out on this short-lived anomaly. In real-world applications, these kind of information can be of critical importance. For example, things like earthquakes, when you're detecting some irregularity in a heartbeat, they occur at a very short instance in time, but they are extremely important in diagnosis. In such cases, if that information is missed by Fourier transform, we might come to wrong conclusions. So now let us look at what happens when you make use of wavelet analysis in order to analyze the same time series signal. In this case, we are going to make use of a continuous wavelet transform using the PaulWavelets. And I-m going to plot the wavelet scalogram. And you'll notice here that the wavelet scalogram gives us a much more detailed picture about what the signal is doing.
>
> **[4:48](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/introduction-wavelet-analysis?u=76281980&t=288)** What you're looking at here on the y-axis are called as scales, and associated with each scale is an octave and a voice. As the octaves become smaller, the wavelet analysis is able to focus on the high-frequency signal or the sharp events that had happened in the signal. So you can see that when the octave was large, it is able to capture just the sine function. But as you move up and the octaves becomes smaller and smaller, then it localizes to the certain features that were present in the signal, and it's able to tell you that at this event, somewhere near 0.3, a certain event took place of certain duration. So this makes it extremely useful to use wavelet analysis. So what exactly is happening behind the scenes? Well, wavelet analysis essentially decomposes a signal into different sets of scales using the following formula. So when you are performing a wavelet analysis, you make use of what is called as a scaling function, denoted by a phi, and a wavelet function, denoted by psi. Associated with each one of these decompositions from the scaling function and wavelet functions, you end up with a set of coefficients. For the scaling functions, we end up with coefficients c, which are called as coarse coefficients, and with the wavelet functions, we end up with what are called as detail coefficients. Wavelet analysis also performs what is called as a multiresolution analysis, wherein you take a signal and you decompose it into coarse coefficients and detail
>
> **[6:21](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/introduction-wavelet-analysis?u=76281980&t=381)** coefficients, and then you take the coarse coefficients and you further perform a decomposition such that you get a level 2 coarse and detail coefficients. And this tree structure leads to what is called as a multiresolution analysis.

> [!info]- Semantic Content
>
> **CLI Commands:** make (6), find (1)
> **Definitions:** is called (3), is a  (2), is an  (1)
> **Analogies:** for example (2), picture (1)
> **Versions:** 0.3 (1)


### 1. Frequency Detection in a Time Series

[↑ Back to Table of Contents](#table-of-contents)

#### [Frequency Detection in a Time Series](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/frequency-detection-in-a-time-series?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/frequency-detection-in-a-time-series?u=76281980&t=0)** So let us now look at certain examples associated with using wavelet analysis. The first one is associated with detecting frequency in a time series using wavelet analysis. Recall that the traditional method that we tend to make use of is Fourier transform, but we are going to show you in this example how you could relate what you see in the wavelet domain to a certain frequency. So as an example, let us start with a single frequency example wherein we are going to take a function. It's a cos(2π) frequency. And we are going to vary the frequency. We'll put all of this inside of a manipulate. We are going to perform a ContinuousWaveletTransform. We are going to make use of the GaborWavelet of order 6. We are going to do a decomposition that consists of eight octaves and four voices. When I evaluate this, you get a manipulate that looks as follows. This is the signal that we are doing the decomposition on, and it consists of a certain frequency, in this case 10 hertz. And you can see that given that this is a stationary signal, which means that the frequency occurs at all instances in time, the wavelet scalogram shows up as a band that stretches across the complete time series. Furthermore, this band is associated with a location which corresponds to a certain scale, and that scale can be related to a frequency. And there is an inverse relationship that exists between frequency and scale. So for example, if I were to move this frequency bar, you see that the frequency
>
> **[1:33](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/frequency-detection-in-a-time-series?u=76281980&t=93)** is increasing. And as the frequency increases, you can see in the wavelet scalogram, the band moves up. And this is a good way of detecting whether you had a high-frequency or a low-frequency content in the signal. Let us look at what happens when you are dealing with two frequencies. In this case, we are going to have two cosine functions of different frequencies. And we are going to superpose these two functions on top of each other and get a composite function. Now, you'll notice that when I perform a wavelet transform on that, you'll get two distinct bands, each band corresponding to a specific frequency. Depending upon how these two frequencies combine with each other, the bands can merge into a single band. Or if you, in certain cases, you might end up having amplitude modulation, in which case you get these dark and light bands in a single row. And so using this information, you can easily find out whether you have multiple frequencies or single frequency. I'm going to go ahead and clear the variables here. And now let us look at a more complicated case wherein we are dealing with multiple frequencies. In this case, here is the data that we are going to generate. It consists of four distinct frequencies occurring at different instances in time.
>
> **[3:07](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/frequency-detection-in-a-time-series?u=76281980&t=187)** So one thing to note here is that we are dealing with multiple frequencies and the signal that we are dealing with is non-stationary signal. When you plot the signal, you notice that it looks as follows, and you can clearly see that at different instances in time, you have different frequencies. We are now going to perform a ContinuousWaveletTransform using the DGaussianWavelet of order 5. We are going to automatically select the number of octaves, but we'll specify that we need 12 voices per octave. The wavelet scalogram in this case looks as follows. Wherein for each subinterval in the time series, you can see that you have certain bands that show up, and as the time progresses, you can see that the bands move up the wavelet scalogram, indicating that higher and higher frequencies were found at the latter stages of the time series. And so this is a very useful way of analyzing time series using [Wavelets](../../Skills/Data%20Science/Wavelets.md). So now that we have talked about extracting frequency information using wavelet analysis, let's do some more analysis on time series. And in this case, we are going to use it for, use wavelets for detecting discontinuities, isolating peaks, and recording certain interesting phenomena. As a first example, I'm going to make use of the Heaviside theta function, which is nothing but a step function. And I'm going to perform a ContinuousWaveletTransform on that.
>
> **[4:39](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/frequency-detection-in-a-time-series?u=76281980&t=279)** So the plot looks as follows. You can see that somewhere in the midpoint, you have a discontinuity. We are going to use wavelet analysis in order to detect this discontinuity. We perform a ContinuousWaveletTransform. And do a wavelet scalogram. And you can clearly see that the wavelet scalogram tells us at what instance in the time series did that discontinuity take place. Notice once again that it is the lower scales that is able to capture when the sharp event took place. At the higher scales, it is unable to figure out anything. That's because at higher scales you are looking at the entire time series, and you might not be able to isolate the signal precisely. But as you move to lower scales, you are able to concentrate on local features. You can make use of complex wavelets if you are interested in analyzing certain oscillatory behavior. In this case, I'm going to construct an oscillatory behavior that's also non-stationary in nature. And this is what the signal that I'm going to analyze looks like. When you perform a ContinuousWaveletTransform on this signal, you get a closed-form expression from which you can extract out several pieces of information. One of the things that you could do is plot the amplitudes of the wavelet coefficients using the wavelets scalogram.
>
> **[6:13](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/frequency-detection-in-a-time-series?u=76281980&t=373)** And as you can see from this, wavelets scalogram, the information that you are getting from this is that at time t = 0, you have some very high frequency component, and as the time progresses, the frequency component goes on decreasing, which is indicated by this curve here. You can also compute the phase of the wavelet coefficients by putting an additional argument into the wavelet scalogram. So you set up automatic as the second argument and arg as the third argument, which is basically, it's going to apply that function to the wavelet coefficients. And this is the result that you get. You can clearly see how the argument behaves for this time series. You can use ContinuousWaveletTransform to detect certain features in the domain. In this case, I'm going to use our familiar example that we saw in a couple of slides before. When you perform a ContinuousWaveletTransform on that, you can clearly see that it's able to isolate when the peak occurred.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Wavelets](../../Skills/Data%20Science/Wavelets.md) (5)
> **CLI Commands:** make (4), find (1)
> **Definitions:** is a  (3), means that (1), is an  (1)
> **UI Navigation:** select the (1)
> **Analogies:** for example (1)
> **Prerequisites:** set up (1)


### 2. Filtering Frequencies from Time Series

[↑ Back to Table of Contents](#table-of-contents)

#### [Filtering Frequencies from Time Series](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/filtering-frequencies-from-time-series?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/filtering-frequencies-from-time-series?u=76281980&t=0)** So now that we have spent enough time looking at detecting frequencies, analyzing how frequencies are related in the wavelet scalogram, let's see how we can actually do some processing on the time series. And one of the most basic processing you can do is filtering frequencies from a time series. So we are going to start off with an example. In this case, we are going to take an example of Sin(x) + Cos(2x) and the ArcSin[Sin[20x]]. That is scaled by a certain number. So we have d1 and d2 and we are going to add them together. And what you see here is a signal which has got obviously some high-frequency component in it. So now the objective for us is to be able to extract the low-frequency component and the high-frequency component from it. Let's see how we do that. In order to do that, we are going to make use of DiscreteWaveletTransform. We're going to make use of a SymletWavelet of order 4, and we are going to perform six levels of decomposition. When you visualize the wavelet coefficients using the WaveletListPlot, they look like this. So right off the bat, the coarse coefficients, which are the ones below, seem to be capturing the lower-order frequencies, while the detail coefficients seem to be capturing the higher-order components. So in order to keep just the low-frequency content, we can then essentially only keep the coarse coefficients. So using WaveletMapIndexed, I can then multiply all the detail
>
> **[1:35](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/filtering-frequencies-from-time-series?u=76281980&t=95)** coefficients with zero and basically eliminate them out. And this results in a modified wavelet transform which when I take the inverse transform of this modified transform, gives me back the following signal. So on the left, you see the data, on the right you see the data that was filtered. So this is the original and this is the filtered data. If you want to extract out the high-frequency component, then all you need to do is ensure that all the coarse coefficients are set to zero, and that can be done using WaveletMapIndexed, but now we apply a pattern saying that anything that ends with a zero, multiply that with zero. And when you take a Fourier... I'm sorry. When you take an inverse wavelet transform of this modified transform, you see that you are able to now extract out the high-component in the time series. So now let's look at filtering certain frequencies, especially when you're dealing with non-stationary time series. So in this case, we are going to make use of a combination of certain sinusoids. And this is what the plot looks like. So you have three bursts of energy. And on the first burst, you have two sinusoidal functions that are present in this domain. When you take a ContinuousWaveletTransform using the GaborWavelet and look at the wavelet scalogram associated with it, you can clearly see the frequency is showing up as the bright spots in the wavelet scalogram.
>
> **[3:10](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/filtering-frequencies-from-time-series?u=76281980&t=190)** So you can see at the first instance in time, you have two frequencies, second instance, you have one frequency, and third, you have another frequency. So now the objective for us is going to be to eliminate or extract out the higher-component frequency from the first instance in time, which corresponds to the 140-hertz component. Now similar to what we had done earlier, this will be equivalent to extracting out the wavelet coefficients and setting those values equal to zero. So we are going to define a vector. It's just a zero vector. And we are going to make use of WaveletMapIndexed to set those coefficients which are four and five and have a certain pattern. And we are going to use that to set it equal to zero. And now when you apply a wavelet scalogram to it, you can see that the higher-order component has been eliminated. You perform an inverse wavelet transform on this new modified wavelet transform, and when you do a ListPlot of that, you can see that the higher-order frequency from the first instance in time has been eliminated. Let's go ahead and clear the variables. I have another example here associated with speech analysis which I will not go into, but I would encourage you to, once you get hold of the notebook, to try it out on your end.

> [!info]- Semantic Content
>
> **CLI Commands:** make (4)
> **Definitions:** is a  (1)
> **Analogies:** similar to (1)


### 3. Applying Wavelet Analysis to Finance

[↑ Back to Table of Contents](#table-of-contents)

#### [Applying Wavelet Analysis to Finance](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/applying-wavelet-analysis-to-finance?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/applying-wavelet-analysis-to-finance?u=76281980&t=0)** So now let us look at how wavelet analysis can be applied in the field of finance. So as an example, we are going to look at the stock price trend of IBM since January 2000 till the current date. So I'm going to extract that information out and I'm going to give it a variable price. When you plot this, you will notice that it's got a certain trend associated with it. But it's got two components to it. One is there is a high-frequency component which basically would account for the short-term market fluctuations. But more importantly, you also have a seasonal trend associated with it. So now our objective is going to be to extract out the short-term market trend and the seasonal trend from this time series. So in order to do that, we are going to perform a DiscreteWaveletTransform using BiorthogonalSplineWavelets. And now let's do some analysis on the wavelet coefficients. The first thing, obviously, we would like to do is to look at what the coefficients look like. And we can make use of WaveletListPlot with an option of plot layout going to common y-axis, which means that all the wavelet coefficients are basically placed side by side, next to each other. The finest one over here is associated with the level [0, 0, 0, 0], and is associated with the coarsest coefficient at the highest level. The next level is... Let's see if we can. There we go.
>
> **[1:31](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/applying-wavelet-analysis-to-finance?u=76281980&t=91)** A [0, 0, 0, 1]. Okay. Now, what we are going to do is extract out certain trends from the financial time series. So we are going to first threshold all the detail coefficients and inverse the time series. When you threshold the detail coefficients, and we are going to threshold it such that anything that's below a value of 100 will be eliminated. And when you plot it, you'll see that this is the original time series. And by eliminating all the components that are below 100 from the detail coefficients, you are able to extract out the seasonal trend in the time series. So technically, now I can take this time series, subtract it with my original time series, and get the high-frequency components from it. Which would also correspond to detrending a time series. So in this case, let's look at the WaveletListPlot. We can remove the trend by removing the coarse coefficients only, which is done by multiplying all the coarse coefficients with a value of zero, so it's basically eliminated. And when you plot it, you can see that this is the inverse transform of just the detail coefficients, which also corresponds to the high-frequency component.

> [!info]- Semantic Content
>
> **Definitions:** is a  (1), means that (1)
> **CLI Commands:** make (1)
> **Env Vars:** ibm (1)


### 4. Edge Detection on Images

[↑ Back to Table of Contents](#table-of-contents)

#### [Edge Detection on Images](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/edge-detection-on-images?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/edge-detection-on-images?u=76281980&t=0)** Now let us shift our focus to the field of [Image Processing](../../Skills/Artificial%20Intelligence%20(AI)/Image%20Processing.md). In the field of image processing, one of the most common things you would tend to do is edge detection. So let's spend a couple of minutes talking about how wavelet analysis can be used to perform edge detection on images. Now, recall that edge detection is in some sense quite similar to detecting events and discontinuities in a time series. And so we are going to use the same principles that we have looked at for detecting discontinuities in order to detect edges in an image. So as an example, we're going to start with detecting the edges of the following image. You're going to perform a DiscreteWaveletTransform on this image using a SymletWavelet, and we are going to perform four levels of decomposition. As usual, we first want to visualize what kind of coefficient, what the coefficients look like. And you can do that using WaveletImagePlot. This is the variable we have given to the transformation, and we apply an image processing component to it, which is basically ImageAdjust so that it adjusts the brightness and contrast of the wavelet coefficients. And this is what the wavelet coefficients look like. So let me go ahead and make this bigger. And you can see that though you can see hints of edges here, they are really not that clear. So now, in order to completely extract out the edges of the original image, what we can do is set the coarse
>
> **[1:35](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/edge-detection-on-images?u=76281980&t=95)** coefficient to zero and reconstruct the entire transformation using only the detail coefficients. We do that in this case. And now when you sharpen it and, first, perform an inverse wavelet transform and then sharpen it, you can see that the edges have been nicely captured. Let's look at another example. This is quite a famous mountain, and we are going to perform a LiftingWaveletTransform, again making use of SymletWavelet. When you look at the wavelet image plot of that, you can see that the detail coefficients, they actually look quite interesting, but it's difficult to figure out which one of the coefficients should be used in order to compute the... To extract the edges. So in this case, we are going to apply the same principle as in our first example wherein we are going to set the coarse coefficients to zero and then perform the reconstruction. And when you compare that with just the wavelet coefficients, this is what the wavelet coefficients look like, this is what the edges look like after we did the transformation. And so this is a very common procedure of making use of [Wavelets](../../Skills/Data%20Science/Wavelets.md) in order for edge detection. Here is a dynamic example that gives you a much more clear picture as to what exactly are the coarse coefficients and the detail coefficients extracting when you perform a wavelet transformation on an image.
>
> **[3:07](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/edge-detection-on-images?u=76281980&t=187)** So in this case, I have a circle with a horizontal line and I can move this horizontal line. And on the right, I have the wavelet coefficients as plotted using WaveletImagePlot. You can see that certain coefficients are able to capture the horizontal line pretty well. And as I move this horizontal line such that it becomes diagonal, now all the coefficients are able to compute it. When it becomes vertical, then a certain pair of coefficients are able to capture it, which you can see is here, here, here, and here. And when I move back, you can see that other coefficients become activated. And this is all due to the fact that you are taking these dyadic [products](../../Skills/Software%20Development/Microsoft%20Products.md) when you're performing the wavelet transform, and that enforces certain features. Finally, let's look at an example wherein we can improve the performance of current image processing functionalities which may or may not be inside of Mathematica, and we can improve the performance using wavelet analysis. So here is a very good image of train tracks. And notice that this train track has several lines associated with them. And we are interested in extracting these lines in a very efficient way. Now, of course, you can make use of Mathematica's built-in function for perspective, using certain perspective transformations and you can get these lines. But if you pre-process this image, you may be able to enhance the performance of the existing function. So in this case, we are going to make use of LiftingWaveletTransform,
>
> **[4:41](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/edge-detection-on-images?u=76281980&t=281)** making use of SymletWavelet, and doing a wavelet decomposition on it. When you look at what the coefficients look like, you can see that now clearly a whole bunch of these coefficients are able to extract the lines really well. So in order to extract the vertical and the diagonal components, we are going to perform an inverse wavelet transform on only certain components of the, only certain coefficients. And we are going to give it an assignment of vertical image. And after we have computed this inverse wavelet transform, we are going to threshold it and get a new image. Here's what the modified image using wavelet transform looks like. And you can see that the lines are much, much more clear now. So if we take this image and now present it to whatever edge detection algorithm that image processing might have, or for detecting lines, then the processing time would be significantly reduced. So in this case, we are interested in extracting the vertical components. And we are going to make use of the Mathematica's built-in function ImageLines. But we are going to provide it with the transform data. When you plot it, take just a few seconds, you can see that based on the transformed data, it's able to capture the lines very accurately. So this kind of preprocessing can go a long way. Let's look at certain neat examples that are associated
>
> **[6:17](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/edge-detection-on-images?u=76281980&t=377)** with wavelet transforms. And one of the most important things that come about due to the wavelet analysis is the idea that you can use it for detecting changes in time series, you can use it for denoising, you can use it for pattern recognition. And most of these can be done because for certain wavelet transforms, especially the stationary wavelet transform, it has a property of translation invariance, which means that if I were to take a certain signal and move it from left to right or right to left and look at the wavelet transform associated with it, you will see that the wavelet transform, especially for the stationary wavelet transform, it moves along with it. So this information allows us to detect events as they are moving in time. We can see if certain signal has a certain motion associated associated with it. If you were to just perform a [Vanilla](../../Skills/Web%20Development/Vanilla.md) discrete wavelet transform, the situation is slightly different in that you will not be able to capture the motion of the original time series. So in that sense, stationary wavelet transforms are really important in several applications. Furthermore, the stationary wavelet transform are also called as undecimated wavelet transform. So to give you an idea of what I mean by that, let's look at this image, which is the Lenna image, we're going to perform a StationaryWaveletTransform on it.
>
> **[7:52](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/edge-detection-on-images?u=76281980&t=472)** And when you look at the dimensions associated with the coefficients, you'll see that the coefficients all have the same dimensions, which is not the case when you perform a DiscreteWaveletTransform wherein a certain amount of down-sampling takes place, which means that as the levels go higher and higher, the dimension of the system is reduced by half. You can extract the components of the transformation, that is, the coefficients using the following syntax, wherein you are going to give it a pattern, and you're going to say that they are all images, and that allows you to directly visualize the coefficients in the form of images. You can, of course, customize your wavelet image plot as follows. In this case, we are going to, for each one of the coefficients, we are going to apply certain amount of image processing to it in terms of ImageAdjust so that we can extract out certain properties. And you can see that for different components, you are able to extract out different characteristics. And you can see for this specific coefficient, which is [0, 0, 1], you are actually able to extract all the edges. In other cases, a certain color is highlighted.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Image Processing](../../Skills/Artificial%20Intelligence%20(AI)/Image%20Processing.md) (6), [Wavelets](../../Skills/Data%20Science/Wavelets.md) (1), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1), [Vanilla](../../Skills/Web%20Development/Vanilla.md) (1)
> **CLI Commands:** make (4)
> **Definitions:** is a  (3), means that (1)
> **Analogies:** similar to (1), picture (1)


### 5. Denoising Images

[↑ Back to Table of Contents](#table-of-contents)

#### [Denoising Images](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/denoising-images?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/denoising-images?u=76281980&t=0)** So keeping with the theme of [Image Processing](../../Skills/Artificial%20Intelligence%20(AI)/Image%20Processing.md), let us now look at how you can use wavelet analysis to denoise images. So we're going to use examples as before in order to bring this point across. So we're going to make use of this picture of an astronaut. And we are going to apply a certain amount of noise onto this image so that it becomes a grainy image. Now we are going to perform a discrete wavelet transform on this grainy image. Before we do further analysis, it's important to have a look at how the image looks like, and this is what the image looks like. It's got some colored noise to it. Now in order to get rid of the noise, we are going to perform thresholding. So we have done the DiscreteWaveletTransform. We apply a wavelet threshold. In this case, we are using the soft method, and we are going to make use of an algorithm associated with the SURE method. And we are going to apply the threshold on certain coefficients. And we are going to evaluate that. And now when you invert this modified wavelet transform and compare it with the original image, you can see that the noise has been, rather than eliminating, I would say that it's been smoothened out. And there on the left is the modified image, on the right is the original noisy image. You can also change the effect of an image. So in this case, we are going to take from our example gallery,
>
> **[1:36](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/denoising-images?u=76281980&t=96)** this image of a rose, and we are going to perform a DiscreteWaveletTransform. And we are going to strongly threshold all the horizontal and vertical detail coefficients only. And the thresholding method we are going to use is a hard threshold. We're going to set a value of 1 for it. Now we modify our wavelet transform. Perform an inverse wavelet transform on that perhaps. But before we do that, we can look at the wavelet image plot of both of them. In the first case, you can see that all the coefficients record some kind of information. After you threshold it, and recall that we are only thresholding the vertical and the detail coefficients. By doing that, only the diagonal components have all the information associated with it. So now when you take the inverse wavelet transform of the first one and the second one, you can see that the first one is able to recover the image as it is. The second one has got some kind of an effect built into it wherein, you know, it's got a more grainy feel to it. One of the most popular examples where [Wavelets](../../Skills/Data%20Science/Wavelets.md) comes into its own is associated with an inverse half-toning problem. So what exactly is half-toning? Well, half-toning is essentially a process wherein you represent an image through a series of dots. So if you look at a certain newspaper, you can see that many of, especially in the old-school newspapers,
>
> **[3:07](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/denoising-images?u=76281980&t=187)** you know, all the images are represented in terms of dots. So we are going to construct an image. Rather, we're going to take an image from our database and we are going to make it grainy. And you can see that here. This has got only dots in it that is used to represent the original image. Let us take this modified image and perform a StationaryWaveletTransform on it using the DaubechiesWavelet. We are going to make use of WaveletMapIndexed. And we are going to filter out the coefficients so that they are smoothened, all the coefficients are smoothened. And we are going to do that only for the coarse coefficients. And when you perform an inverse wavelet transform on that, you can see that the image looks as follows. Which means that it's able to perform an inverse half-toning. So the first one is called half-toning. We have performed an inverse half-toning.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Image Processing](../../Skills/Artificial%20Intelligence%20(AI)/Image%20Processing.md) (1), [Wavelets](../../Skills/Data%20Science/Wavelets.md) (1)
> **CLI Commands:** make (4)
> **Definitions:** means that (1), is called (1)
> **Env Vars:** sure (1)
> **Analogies:** picture (1)


### 6. Wavelet Thresholding

[↑ Back to Table of Contents](#table-of-contents)

#### [Wavelet Thresholding](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980&t=0)** So in the past examples, I have introduced the idea of thresholding wavelet coefficients. So let's look into a little bit more detail as to what exactly does wavelet thresholding entail. And we can do that with the use of an example. We're going to construct a noisy time series signal. You can see that it suggests a sine function with some kind of random noise on it. We'll perform a StationaryWaveletTransform on it using the SymletWavelet. And we're going to perform just an automatic thresholding wherein it's going to just use the default methods that are available and do a thresholding on the coefficients. You can also choose which thresholding methods you want to use and what thresholding values. In this case, you can give additional arguments of Soft and SURE, and that will also perform a certain wavelet thresholding and give you a modified wavelet transform. And when you take an inverse wavelet transform of these two modified transforms, you'll notice that the original looks as follows. By default, the thresholding makes use of the hard, universal option, and you can see that it's able to clear up the noise rather well. If you were to give your own options of Soft and SURE, you can see that in this case also, it's able to get rid of the noise rather well. So you can have a look at the performance of different thresholding methods through the similar example,
>
> **[1:33](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980&t=93)** but now what we're going to do is use several different methods. And for each one of these methods, we can see how the performance is as I increase the noise in the original signal. So in this case, as I increase the noise, you can see that the data is becoming noisier and noisier, but the wavelet threshold is able to pick out the noise rather well and is able to give us the true signal. And you can see that almost all of them do a very good job. The GCV tends to have a little bit more noise. If you change the kind of transformation you are doing from LiftingWaveletTransform to a StationaryWaveletTransform, you can see that almost all of them work rather well. Generalized Cross Validation in some cases gives you more error. You can also do a comparison of the different adaptive thresholding methods using a similar analog that we just saw. The different adaptive thresholding methods are universal level, shear level, and GCV level. And when, when I increase the noise, you can see that adaptively, it's doing a thresholding on all the coefficients and it's able to keep the noise down significantly. Let us now look at another ubiquitous thing associated with [Image Processing](../../Skills/Artificial%20Intelligence%20(AI)/Image%20Processing.md), which is basically taking a data and compressing it while making sure that the amount of information that it loses due to the compression is minimized. Wavelet turns out to be an excellent tool in order to perform data compression and image compression.
>
> **[3:07](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980&t=187)** And the way we do that in wavelet analysis is using what is called as the best basis analysis or best tree analysis. To bring this point forward, let us look at an example. In this case, this is an image of a certain landscape. We are going to perform a DiscreteWaveletPacketTransform. And look at the wavelet image plot associated with it. Now, because this is a rather large image, I had to keep the wavelet image plot a little small so that it didn't overwhelm the screen. But you can see that each one of the vertical and the horizontal coefficients and all the details in the coarse coefficient, each one of them has certain component associated with it. The obvious question is, do we have to retain all this information in order to reconstruct the original signal? Turns out that we don't need all this information. So we can make use of Mathematica's built-in function called WaveletBestBasis, and apply Shannon entropy as our cost function in order to see which one of these coefficients from the transformation can be eliminated. And so that gives us a modified transform. And when you look at the wavelet image plot of this modified transform, you can see clearly that now you don't require all the highest level coarse and the detail coefficients, but rather, a combination of them. And you can see that this process in itself will be associated with a significant amount of image compression. So let's backtrack a little bit and understand the concept of compression using just a simple time series.
>
> **[4:44](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980&t=284)** In this case, I'm going to make use of just a Sin[x +x^2] function. I'm going to perform a WaveletBestBasis. First, I'll take a DiscreteWaveletPacketTransform using the DaubechiesWavelet, and then I'll take a WaveletBestBasis on that transformation that gives me, right off the bat, a modified transformation. I will threshold it to a value such that I'll do a hard thresholding with a value of 0.1. That again gives me back a modified transformation. And then I can count the number of non-zero values that are present inside this modified transformation. And it turns out that there are only 153 non-zero values. So I can basically put this in some kind of a sparse structure and store this information. If you were to just compare it with the original data and see how many values were non-zero, 867. So you are seeing that there is a significant amount of compression, approximately an eight-order, seven-order compression. And when you plot the original and the reconstructed data, the first one is the original data, the second one is the reconstructed data using the WaveletBestBasis and the wavelet thresholding. And you can see that it does a fairly decent job. In the beginning, there are some wiggles, but overall, from a compression point of view, this is doing a fairly good job. You can see the effect of the compression for different
>
> **[6:20](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980&t=380)** threshold values. So in this case, we'll take the exact same signal and we'll perform a DiscreteWaveletPacketTransform using the DaubechiesWavelet. But now we will make the threshold value as a variable. And we'll compute it for different thresholds. And we'll put that in the form of a table. And when you look at the plot of the inverse transform of these thresholded functions, you'll see that as the thresholds change, you can see that it gets a lot more choppy. Okay. Which means that there is a certain value below which, you know, the compressed image is not representing the original signal well. And, of course, if you look at the length of the original data, you have 1,001 points, but for these different thresholded values you're looking at, for the, for a large threshold, you have 110 non-zero values, and for one that's doing a decent job, you have around 353, so you're already getting a really good compression in it. So let's look at an example of performing what would be termed as a lossy image compression. In this case, we are going to take this image of a car in a neighborhood. We're going to take a DiscreteWaveletPacketTransform of that. And we are going to perform certain wavelet thresholding using the hard threshold, and we'll change the threshold values.
>
> **[7:55](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980&t=475)** And when you compare these four different thresholding values, you can see that for each one of these images, you have certain non-zero values. So the one with the least amount of non-zero values, obviously, has a very choppy-looking image. However, when you compare it with the total number of non-zero pixels or the non-zero information that's present in the original data, you see that that number is 49,152. So if you were to even take the case wherein the threshold was 0.02, you are looking at a pretty decent 50 percent approximate compression, and you're getting a very decent result from the lossy compression.
>
> **[8:48](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980&t=528)** So let's look into some more advanced examples from image processing. In this case, we are going to perform what is called as image fusion. So we are going to take, the objective is as follows. We have two images. This is image 1, image 2 is that of a grass and we are going, we are interested in extracting the texture. Remember, the texture, not the color, of the grass and putting that over the first image. And in order to do that, we are going to make use of wavelet transform. So we are going to first initialize the two pictures and then we are going to perform a StationaryWaveletTransform on both these pictures. Now, what we'll do is, before we start some analysis, we'll have a look at what the plots look like. And you can see that for the first one, you have, certain coefficients are able to extract out certain pieces of information. And you can see that the detail coefficients in the second image seem to be contributing to the texture associated with the image. So what we're going to do is we're going to combine all the detail coefficients from both the images. So we're going to, we extract out the detail coefficients. And then we are going to just add them up in some kind of a weighted average way. When we add them up, we get a new set of coefficients. We can append them to our existing transformation.
>
> **[10:23](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980&t=623)** And we can now construct a new discrete wavelet data from the combined [Wavelets](../../Skills/Data%20Science/Wavelets.md) and perform an inverse transformation on that. And you can see that we get an image. That's the original image with the texture of the grass superimposed on it. Here is a slightly simpler example, wherein we are going to make use of a monochrome image in order to generate certain masks. So we have our first image. And we have a monochrome image and we are going to superimpose one over the other so that wherever there is a white spot that those sections in the original image will be highlighted. So we perform a StationaryWaveletTransform on the original image and then we'll perform an ImageMultiply on all the detail coefficients. And we get a modified transformation. And when you perform an inverse transform of that, you'll see that the first case is the one with the mask. You can see that the edges, which were the binary black have been masked, and the one that was white has not been masked. And so there, it all depends on how you want to play around with the detail coefficients on the coarse coefficients, and depending upon how you manipulate them, you can perform different kinds of image processing analysis. Finally, we'll look at some miscellaneous applications associated with wavelet analysis, and the most common one being
>
> **[11:59](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980&t=719)** numerical differentiation. Let's construct a data. Which has got certain trend to it, and also there's a certain amount of noise that is built into it. And now what we're interested in doing is, for this noisy data, we'd like to take the differentiation of the signal. The problem with such a situation, is that when you perform differentiation on such a noisy signal, the original structure of the signal will be completely lost due to the noise. So instead of doing a naive finite difference, or doing some kind of an interpolation and then doing the differentiation on that, it's better to first preprocess the data and then perform the differentiation. So you can directly make use of wavelets in order to do the numerical differentiation. So in this case, we are going to construct a function that performs differentiation on a noisy signal by taking the StationaryWaveletTransform and then take, only applying the differentiation on certain coefficients. So we are going to initialize the function. And when we apply the function to our noisy time series and compare it with the pure function without any noise, you can see that the differentiation is at least able to capture the basic trend of the differentiated function, which is given by the red line. On the other hand, if you were to just naively make use of interpolation and allow Mathematica to compute
>
> **[13:34](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980&t=814)** the differentiation, you'll see that the trend is completely lost. And so you can see the obvious advantage of doing some pre-processing before doing differentiation. You can also make use of integer lifting. Integer lifting is a very interesting concept in wavelet analysis in that it maps integers to integer wavelet coefficients. It doesn't take an integer and convert it into real during the transformation. This becomes really important when you're dealing with images so that, you know, there is no memory inflation when you're doing the wavelet transformation. So let's look at just a very simple example wherein we are going to construct a random data from 0 to 1064 of them. When you take a LiftingWaveletTransform, we can give a method going to integer lifting. And that gives us a discrete wavelet transformation. When you look at what the coefficients look like, you can see that each one of them are integers. So this actually helps in saving memory to the end. Of course, when you take the inverse wavelet transform, because everything is happening at an integer level, the results that come back from the inverse wavelet transform are going to be exact. So the transformations and the math behind it remains the same. It's just the algorithmic implementation that changes that makes it extremely efficient. Finally, we can also look at doing function approximation of a certain function.
>
> **[15:10](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/wavelet-thresholding?u=76281980&t=910)** In this case, you can see the blue line... Sorry. The red line is the function that we are interested in approximating. And we are going to make use of HaarWavelets in order to do it. So let me show you the manipulate that goes behind the scenes. And you can see that I'm going to make LiftingWaveletTransform using the HaarWavelet. And what we are going to do is only look at the largest coefficients and subsequently keep on adding only the largest coefficients. And so right now we only have a horizontal line, but as we move forward, you can see that the lines will eventually merge and start taking the shape of the function we are trying to approximate. And notice that the choppy feel of the approximated function is due to the fact that we are making use of HaarWavelets. And as I move this forward, you can see that the function approximation takes place rather well. But we need to keep on increasing the number of coefficients we are using for the function approximation, because certain regions require more coefficients in order to approximate them, and you can see, by the time we reach 62, it's able to do an excellent job of approximation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Image Processing](../../Skills/Artificial%20Intelligence%20(AI)/Image%20Processing.md) (3), [Wavelets](../../Skills/Data%20Science/Wavelets.md) (2)
> **CLI Commands:** make (10)
> **Definitions:** is a  (6), is called (2), is an  (1), means that (1)
> **Env Vars:** sure (2), gcv (2)
> **Versions:** 0.1 (1), 0.02 (1)
> **Prerequisites:** before we start (1)


### References

[↑ Back to Table of Contents](#table-of-contents)

#### [References](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/references?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/wavelet-analysis-applications-with-wolfram-language/references?u=76281980&t=0)** Finally, I want to bring your attention to a number of references that are available that you can make use of in order to get acquainted with the different applications that wavelet analysis have been used for. Here are a list of certain general references, and also a number of application references that you would find extremely helpful. If you have any questions, please feel free to write to us at training@[wolfram.com](https://wolfram.com), and we'd be more than happy to answer any questions that you might have.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1), find (1)
> **URLs:** [wolfram.com](https://wolfram.com) (1)


## Instructor

- [Wolfram Research](../../Instructors/Data%20Science/Wolfram%20Research.md)

## Skills Covered

- Wolfram Language
- Data Analysis
- Wavelets

## Path Context

### In [Build Your Wolfram Language Skills](../../Paths/Data%20Science/Learning%20Paths/Build%20Your%20Wolfram%20Language%20Skills.md)
← [Wavelet Analysis- Concepts with Wolfram Language](Wavelet%20Analysis-%20Concepts%20with%20Wolfram%20Language.md) | **4 of 8** | [Modeling Market Prices Using Stochastic Processes with Wolfram Language](Modeling%20Market%20Prices%20Using%20Stochastic%20Processes%20with%20Wolfram%20Language.md) →

## Appears In

- [Build Your Wolfram Language Skills](../../Paths/Data%20Science/Learning%20Paths/Build%20Your%20Wolfram%20Language%20Skills.md)

## Related Courses

_Courses sharing skills:_

- [Wavelet Analysis- Concepts with Wolfram Language](Wavelet%20Analysis-%20Concepts%20with%20Wolfram%20Language.md) — Data Analysis, Wolfram Language, Wavelets
- [Quantile and Box-Whisker Plots in the Wolfram Language](Quantile%20and%20Box-Whisker%20Plots%20in%20the%20Wolfram%20Language.md) — Data Analysis, Wolfram Language
- [Intermediate SQL- Data Reporting and Analysis](Intermediate%20SQL-%20Data%20Reporting%20and%20Analysis.md) — Data Analysis
- [Data Analysis- Investigate with SQL](Data%20Analysis-%20Investigate%20with%20SQL.md) — Data Analysis
- [Advanced SQL for Data Scientists](Advanced%20SQL%20for%20Data%20Scientists.md) — Data Analysis

---

[↑ Back to top](#)