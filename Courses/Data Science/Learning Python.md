---
type: course
cssclasses:
  - lle-course
  - lle-level-beginner
slug: learning-python-25309312
url: "https://www.linkedin.com/learning/learning-python-25309312"
duration_minutes: 221
duration: 3h 41m
level: Beginner
updated: 2/21/2025
learners: 2522
skills:
  - Python (Programming Language)
exercise_files: true
github: "https://github.com/LinkedInLearning/learning-python-3980343/codespaces"
thumbnail: "https://media.licdn.com/dms/image/v2/D4E0DAQECotgjIKCbrw/learning-public-crop_675_1200/B4EZT7PgtEGwAc-/0/1739381935225?e=2147483647&amp;v=beta&amp;t=zavFdzEcNtb5C3H8Q58J2_FVW9eqwIM5x_aSr1xHRzQ"
linkedin_topic: Data Science
learning_paths:
  - '[[Getting Started with Python]]'
prev_courses:
  - '[[Python Essential Training]]'
next_courses:
  - '[[Python Object-Oriented Programming]]'
path_nav: '[{"path":"Getting Started with Python","position":2,"total":8,"prev":"Python Essential Training","next":"Python Object-Oriented Programming"}]'
path_count: 1
tags:
  - course
  - topic/data-science
  - topic/software-development
  - skill/python-programming-language
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Data%20Science/Learning%20Python.md)

![Learning Python](https://media.licdn.com/dms/image/v2/D4E0DAQECotgjIKCbrw/learning-public-crop_675_1200/B4EZT7PgtEGwAc-/0/1739381935225?e=2147483647&amp;v=beta&amp;t=zavFdzEcNtb5C3H8Q58J2_FVW9eqwIM5x_aSr1xHRzQ)

# Learning Python

> Python—the popular and highly-readable object-oriented language—is both powerful and relatively easy to learn. Whether you're new to programming or an experienced developer, this course can help you get started with Python. Joe Marini provides an overview of the installation process, basic Python syntax, and an example of how to construct and run a simple Python program. Learn to work with dates a

> [LinkedIn Learning](https://www.linkedin.com/learning/learning-python-25309312) | 3h 41m | Beginner | 3K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Learning Python](#learning-python)
  - [Before you get started](#before-you-get-started)
- [**1. Getting Started**](#1-getting-started) (3 videos)
  - [Using the course resources](#using-the-course-resources)
  - [Installing Python on your computer](#installing-python-on-your-computer)
  - [CoderPad Challenges](#coderpad-challenges)
- [**2. Basics of Python**](#2-basics-of-python) (8 videos)
  - [Building "Hello World"](#building-hello-world)
  - [Variables and simple data types](#variables-and-simple-data-types)
  - [Sequence data types](#sequence-data-types)
  - [Dictionary data types](#dictionary-data-types)
  - [Conditional decisions](#conditional-decisions)
  - [Loops](#loops)
  - [Defining functions](#defining-functions)
  - [Solution: Number counter](#solution-number-counter)
- [**3. Beyond the Basics**](#3-beyond-the-basics) (6 videos)
  - [Built-in functions](#built-in-functions)
  - [Solution: Find the longest string](#solution-find-the-longest-string)
  - [Classes and objects](#classes-and-objects)
  - [Importing and using modules](#importing-and-using-modules)
  - [Using exceptions](#using-exceptions)
  - [Solution: Palindromes](#solution-palindromes)
- [**4. Welcome to Part 2**](#4-welcome-to-part-2) (1 videos)
  - [Applying what you've learned](#applying-what-youve-learned)
- [**5. Using Dates and Times**](#5-using-dates-and-times) (5 videos)
  - [The date, time, and datetime classes](#the-date-time-and-datetime-classes)
  - [Formatting time output](#formatting-time-output)
  - [Using timedelta objects](#using-timedelta-objects)
  - [Working with calendars](#working-with-calendars)
  - [Solution: Dates and times](#solution-dates-and-times)
- [**6. Working with Files**](#6-working-with-files) (5 videos)
  - [Writing files](#writing-files)
  - [Reading files](#reading-files)
  - [Working with the operating system](#working-with-the-operating-system)
  - [Using the filesystem](#using-the-filesystem)
  - [Solution: Files](#solution-files)
- [**7. Working with the Internet**](#7-working-with-the-internet) (3 videos)
  - [Retrieving internet data](#retrieving-internet-data)
  - [Working with JSON data](#working-with-json-data)
  - [Manipulating XML](#manipulating-xml)
- [**Conclusion**](#conclusion) (1 videos)
  - [Conclusion](#conclusion)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Learning Python](https://www.linkedin.com/learning/learning-python-25309312/learning-python?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/learning-python?u=76281980&t=0)** - The [[Python (Programming Language)|Python]] programming language has become one of the most popular languages in the world, and it's not hard to see why this has happened. Python has an easy to learn format, it runs on multiple operating systems, both on the client and the cloud, and has a vast ecosystem of tools and libraries that address a large number of programming scenarios. We'll cover a variety of topics in this course, starting with the basics of the Python language. Then we'll build our first Python program in just a few lines of code before moving on to working with some of Python's built in high level data types, such as dates, times, and files. Hi, I'm Joe Marini, and I've been building software professionally for some of the best known companies in Silicon Valley for more than 35 years. Python is an incredibly flexible programming language that can build cross platform applications that run on the client as well as the cloud. Let's get started learning Python.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (7)
> **CLI Commands:** python (7)
> **Definitions:** is an  (1)
> **Analogies:** such as (1)
> **Speakers:** - the (1)

#### [Before you get started](https://www.linkedin.com/learning/learning-python-25309312/before-you-get-started?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/before-you-get-started?u=76281980&t=0)** - This course is designed to get you up and running with the [[Python (Programming Language)|Python]] programming language. I've designed this course in a way that helps people new to programming to learn this incredibly valuable language, while also being useful to experienced programmers who are picking up Python for the first time. If you are new to programming, you should be able to follow along and complete the course using the examples that I provided. It will be helpful if you already have some coding experience and understand the basic principles of programming, such as using variables, writing statements, and so on. Python has many of the same language features that you will find in other programming languages that you may have already worked with. So having some prior programming knowledge will help, but again, it's not required. However, if you really are completely new to programming and have never written any code before, I would suggest you watch the [[Programming Foundations Fundamentals]] course before taking this one. It does a great job of introducing the basic fundamentals of programming. This course is divided into two parts. In the first part, we'll learn about the Python language itself. We'll see how to write code, run our programs, and use Python's feature set. In the second part, we'll take what we've learned about Python and apply it to some real world examples. We'll see how to use the local file system, work with dates and times, and even access internet-based data sources and APIs. I've designed the examples in this course to work with both traditional developer tools like Visual Studio Code, as well as newer tools, such as [[Google]] Colab, which is a free notebook environment you can use online in your browser without having to install anything at all.
>
> **[1:35](https://www.linkedin.com/learning/learning-python-25309312/before-you-get-started?u=76281980&t=95)** Throughout the course, there will be some programming challenges that will give you opportunities to apply concepts that you've learned to solving real coding problems. And remember, if you're new to all of this, take your time, pause the video, rewind, watch again if you need to. It may take some patience and practice, but by the end of this course, you are going to be a Python programmer.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (7), [[Programming Foundations]] (1), [[Google]] (1)
> **CLI Commands:** python (7), find (1)
> **Tools:** visual studio (1), colab (1)
> **Analogies:** such as (2)
> **Definitions:** is a  (1)
> **Prerequisites:** install (1)
> **Speakers:** - this (1)


### 1. Getting Started

[↑ Back to Table of Contents](#table-of-contents)

#### [Using the course resources](https://www.linkedin.com/learning/learning-python-25309312/using-the-course-resources?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/using-the-course-resources?u=76281980&t=0)** - [Instructor] I've provided a set of exercise files to accompany this course, which you can find in the associated [[GitHub]] repository at the link that you see here. The course content is organized into two separate folders. There's the Finished folder, which contains all of the code examples in their finished state. So you can compare your code against them as you work through the course. The Start folder contains the code examples in their beginning state. And this is the folder I will be working in throughout the course as we build towards the finished state for each example. Now, there are multiple ways that you can choose to work with the exercise files for this course. One way is to work directly within the browser using a GitHub codespace. This repository has been set up with a GitHub codespace, so you can just work directly online with nothing to install. All you need to do is fork a copy of the repository into your own GitHub account, and then fire up a codespace. So if you go here to the Code menu and you click on the [[Codespaces]] tab, you can see that I've already created a codespace. So this is the codespace I will be using, but of course, yours will be named differently. So let me open up the codespace and show you what it looks like. Now, when you start your own codespace, it will take a few moments to spin up, so you just need to be patient while everything gets organized and GitHub does its magic in the background. And then, once it's up and running, you can see that I have a browser-based version
>
> **[1:34](https://www.linkedin.com/learning/learning-python-25309312/using-the-course-resources?u=76281980&t=94)** of Visual Studio Code. And here are the files in their respective folders that you will need for the course. This online version of Visual Studio Code has its own built-in terminal, which you can find here in the View menu. You can see that I can... Whoops, let me get that back there. You can see down here I can view the terminal. Now, the terminal's already open, so I'll just go ahead and type in here. I'm going to make sure that [[Python (Programming Language)|Python]] is installed, so I'll type python --version. You can see I'm using Python 10. You also want to make sure you have the Python extension installed. So if you go to this little icon over here in the sidebar, the one that looks like the boxes. If you click, you can see that I've got the Pylance, Python, and Python Debugger installed. These should already be here for you in the codespace. If they're not, you can install them from the extensions marketplace. Just search for Python up here in the search box, and that's all you'll need to do. The second option for working online without using a GitHub codespace is to use an online notebook like [[Google]] Colab. Google Colab is a free hosted notebook environment that doesn't require any setup at all. Just go to colab.google in your browser, sign in with your Google account, and you are ready to go. If you don't have a Google account, it just takes a few steps to create one. So go ahead and click on New Notebook. And we'll just wait a few moments while that spins up. And then, once the notebook environment loads
>
> **[3:08](https://www.linkedin.com/learning/learning-python-25309312/using-the-course-resources?u=76281980&t=188)** in your browser, you are ready to go, you can give the notebook a name. I'll just call mine Learning Python. And that's pretty much all you need to do. There's no additional setup for Google Colab at all. Now, if you want to use Colab for this course, sometimes you'll just need to either copy and paste the starting point code for some of the examples into the notebook. I've tried to minimize that, but there's going to be some cases where I have some setup code, so you'll have to copy and paste it. Or you can just type the code yourself. So what you're going to do is, if you want to use this notebook, you can just create a code cell for each of the examples. And you see if I hover over here down below the existing code cell, I can add new code cells here. So just add a new code cell for each example. Now, there's also a third option, which is more complicated than the other two, so I only suggest this if you're an experienced programmer and you know what you're doing. If you want to download the files and work locally on your computer, that's easy enough to do. Back here in GitHub, let's go back to the repo. And if I click on the Code menu and click on Local, you can see that I can either clone the repository locally or I can just download a zip file, and then use my favorite code editor to work on them. You will just need to make sure that you have Python installed on your computer in order to use these examples. I'll show how to install Python on your machine in a separate video in case you want to go this route. Any one of these options, GitHub codespace, using a Google Colab notebook,
>
> **[4:42](https://www.linkedin.com/learning/learning-python-25309312/using-the-course-resources?u=76281980&t=282)** or working locally is just fine for following along with the course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (10), [[GitHub]] (8), [[Google]] (7), [[Codespaces]] (1)
> **Tools:** github (8), colab (6), terminal (3), visual studio (2)
> **CLI Commands:** python (10), make (3), find (2)
> **Prerequisites:** install (3), setup (3), set up (1), you'll need (1)
> **UI Navigation:** click on (4), go to (2), in the sidebar (1)
> **Exercise Files:** exercise files (2), download the (1), zip file (1)
> **Cross-References:** go back to (1)
> **Definitions:** is a  (1)

#### [Installing Python on your computer](https://www.linkedin.com/learning/learning-python-25309312/installing-python-on-your-computer?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/installing-python-on-your-computer?u=76281980&t=0)** - [Instructor If you want to complete this course Locally on your computer, you'll need to make sure that Python is properly installed and configured. And I just want to be clear, this step is completely optional for this course. It is entirely possible to complete this entire course online in your browser without installing anything. So, if you want to go that route, you don't need to do this part, and you can skip this video. Okay, if you're still here, I'm assuming that you want to see how to install Python on your computer. Fine, we'll have it your way. Let's go. On my machine, I'm going to open a terminal window, and you can do this on your operating system. Now I'm using Windows, but the process for the Mac is very similar. So I'm going to type the command PY. That's for Windows. For Mac, you're going to type out Python3, and this is also for Linux. If you're on Mac or Linux, type out Python3. For me, I'm going to type PY, and then a space and then --version. And you can see that I'm using Python 3.13 on my machine. Now if you didn't get a result that looks like this, the word Python, followed by a version number, you probably got some kind of error message. Or maybe the version that you have is older than 3.13. That means that you should install the latest version of Python on your computer. So we do that by going to the [python.org](https://python.org) website, which I happen to have open right here. So I'm going to click on the downloads link right here. And that will take me to the downloads page. You can see here that there is a link
>
> **[1:33](https://www.linkedin.com/learning/learning-python-25309312/installing-python-on-your-computer?u=76281980&t=93)** to download the latest version, and it defaults to my version of my operating system. So this is [[Windows]], but you can download it for Mac or for [[Linux]]. Now, I've already done this for my machine, so if you need to do this, go ahead and click the download button and then run the installer when it finishes downloading. It's a very simple process. It should only take a few minutes to complete. And then once you've done that, go back to the terminal, try that test again that we did earlier to make sure that everything worked. So once you've got [[Python (Programming Language)|Python]] installed, you are ready to continue. And remember, you can complete the entire course without installing Python if you want to. You only need to do this if you want to run Python on your local computer.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (9), [[Windows]] (3), [[Linux]] (3), [[Microsoft Word|Word]] (1)
> **CLI Commands:** python (9), make (2), python3 (2)
> **Prerequisites:** install (2), you'll need (1)
> **Versions:** python 3 (1), 3.13 (1)
> **Tools:** terminal (2)
> **Definitions:** means that (1), is a  (1)
> **URLs:** [python.org](https://python.org) (1)
> **Cross-References:** go back to (1)

#### [CoderPad Challenges](https://www.linkedin.com/learning/learning-python-25309312/coderpad-challenges?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/coderpad-challenges?u=76281980&t=0)** - [Narrator] This course includes automated code challenges that appear when you click on the challenge links in the course's table of contents. Each challenge includes instructions and a couple of code editors you can use to create and test your own solution to the challenge. These challenges are hosted by Coder Pad and they appear in the same area of the course page where you watch the courses videos. We recommend using a desktop browser for the best experience with code challenges, but you can use the [[LinkedIn]] Learning mobile app if you prefer. The code challenge has four areas. There are instructions in the top left, a code editor where you will create your answer here in the top right, another code editor where you can see how your code is used in the bottom right and a console for output in the bottom left. You can use these drag handles to allocate space as you like and to get even more horizontal space for the code editors, you can collapse the course's table of contents on the left, which you can see I've already done. Each challenge has instructions that include a description of the challenge and the challenge's parameters, and the desired result. Now depending on the challenge, you'll see some additional information in the instructions such as an explanation of the parameters that your code will be given. Along with some examples of what the expected output might look like. You will create your answer in the top right code editor. There are comments in the starting code showing where
>
> **[1:35](https://www.linkedin.com/learning/learning-python-25309312/coderpad-challenges?u=76281980&t=95)** to put your solution. When you click on test my code down here in the bottom right, you will see a message indicating whether your code returned a correct result. So in this case, the correct answer for finding the largest number in the list is going to be return max of numbers. And I'm going to go ahead and click on test my code. And you can see here in the consult it says, that's right, my code returns 19. Now if your code isn't successful, you can ask for help. Let's go back and change this to the wrong answer. You can see when I click on test my code, oh, I get a problem, the show expected result and show hints variables determine whether you see the expected result and any hints. So I'm going to change both of these to true. Alright, and then I'm going to click test on my code again. And now you can see over here in the console, I am getting the expected result. And there's a little bit of a hint that tells me where to find the right function to be able to use. The code editor in the lower right shows how your solution is used, and in some cases you can change that code to experiment with different test cases. Now, regardless of whether your answer is successful, you will see messages in the console in the lower left. If any messages are too long to fit in that area, you can scroll sideways to see all of the text. And when you've finished each code challenge, you can return to the course's table of contents and click the next video to see my solution so you can compare it with your own.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[LinkedIn]] (1)
> **UI Navigation:** click on (4)
> **CLI Commands:** find (1)
> **Cross-References:** next video (1)
> **Analogies:** such as (1)
> **Speakers:** - [narrator] (1)


### 2. Basics of Python

[↑ Back to Table of Contents](#table-of-contents)

#### [Building "Hello World"](https://www.linkedin.com/learning/learning-python-25309312/building-hello-world?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/building-hello-world?u=76281980&t=0)** - [Instructor] All right, welcome to part one of the course. It is time to start learning and writing some actual [[Python (Programming Language)|Python]] code. So before we fire up our text editor and just start diving in, we're first going to use Python's interpreted mode in the command line interface to see how easy it is to write and run Python. So this mode is called the Python REPL, which stands for read, evaluate, print, loop. Now this might surprise you, but there's a Python Interpreter built into the Python website itself. So let's go to [python.org](https://python.org), which is where I am now, and I'm going to start up the Python Interpreter. Can you guess where it is? Go ahead, guess. Okay, I'll show you where it is. So this little icon right here, if you click on it, you see how it says Launch Interactive Shell? I'm going to click on this, and then right here in our browser, we don't got to install anything. We get a nice Python Interpreter that we can just start working with. So in this mode, I can just simply type some Python code, and it will run right here in the browser immediately. So let's try some things. Let's try 2+2, and you can follow along with me, and you can see that that comes back as 4. So what's happening here is the Python Interpreter is reading what I typed, evaluating the code, printing the result, and then looping back again to the input state. You can see the [[Cursor]] there next to the little three arrows. That's where the term REPL comes from. Read, evaluate, print, and loop.
>
> **[1:34](https://www.linkedin.com/learning/learning-python-25309312/building-hello-world?u=76281980&t=94)** Python is an interpreted language, like some other common ones in use today. For example, like [[JavaScript]] in web browsers, if you've done any [[Web Development]]. The Python Interpreter simply takes each line of code as it comes across and interprets and executes it. And in this case, it's simply taking the numerical expression, 2+2, evaluating it, and coming back with the result of 4. All right, so let's try something a little bit more interesting. Type this along with me. I'm going to say print open parentheses in a quote and then hello world. And then another quote and then a closed parentheses. And then hit the Enter key. And you can see now I've executed the simple Python statement print, which is a built-in Python function along with the string of characters hello world. And we'll learn more about Python functions later in the course. So for now, just follow along with me. All right, let's try something else. I'm going to write name and then a space with an equal sign and another space. And now I'm going to write input and open parentheses and quotes. And we're going to write, what is your name, question mark. And then a space, quotes and closed parenthesis. And when I hit Enter, you can see that now Python is running the input function, which gives me a chance to enter a value. So I'll type my name and then hit Enter.
>
> **[3:09](https://www.linkedin.com/learning/learning-python-25309312/building-hello-world?u=76281980&t=189)** And what I've done now is I've created a variable called name, which contains the value that this input function asked for. And we'll learn more about variables later in this chapter. For now, you can just think of them as little boxes that hold data and that data can change. So if I type name, okay, you can see that I get back the value that the name variable currently contains. So let's use our print function again. So type this along with me. I'm going to print and then open parenthesis quotes, nice to meet you, comma, and then closed quote. Oh actually, sorry, close quote, I got myself stuck. Let's try this again. Print, open parenthesis, quote, nice to meet you, closed quote, then the comma and then name. And when I execute this, you can see the result there, we are combining the nice to meet you string with the variable in our print statement to get this result. So the Python Interpreter in this mode gives us the ability to try out simple ideas before putting them into a real program. All right, now let's type the [[Microsoft Word|word]] exit and open and closed parentheses like this. So we're calling the exit function and that exits us out of the Python Interpreter. Okay, so now that we've seen how to write and execute
>
> **[4:44](https://www.linkedin.com/learning/learning-python-25309312/building-hello-world?u=76281980&t=284)** some very basic Python statements using the REPL, let's use our code editor and write our first Python program using what we've learned so far. So I'm going to demonstrate how to do this first in Visual Studio code and then I'll show how to do it in [[Google]] Colab. So let's go over to [[GitHub]] and you can see I've started up my codespace. And in case you've forgotten how to do that here in the GitHub link, if you go to the Code menu under [[Codespaces]], you'll have to create a new codespace. I've already done that. So I'm starting up my codespace. And when you do this, it might take a moment or two to get started up. So just be patient. And then in the Start folder in chapter two, okay, I'm going to open up the helloworld_start.py file. And let's just close this terminal for now. All right, so let's start off by printing out a hello world message. And you can see that as I type my code, VS Code is helping me out. So I'm going to write Hello World. Let's also have the user input their name. So I'll write input, what is your name? And I'll put a space in there for nice formatting and then I'll print, nice to meet you comma name.
>
> **[6:14](https://www.linkedin.com/learning/learning-python-25309312/building-hello-world?u=76281980&t=374)** All right, so now I need to run my program. And a few minutes ago when we were using the REPL mode, Python was executing each line as I typed it. So now I have a collection of lines of code and I want to run them all at once. So to do this in Visual Studio, what I'm going to do is right click and down here in this menu you can see I've got Run Python and I can choose Run Python in Terminal. I can also do the same thing by right clicking on the file name here in the list. You can see I got this little Run Python file in Terminal option down here. So when I do that, you can see that the terminal opens up, the code runs. I'm going to type in my name and you can see the result there. All right, so far so good. You can also run your programs directly in the terminal if you want to do it that way. So what I can do is I can right click on the chapter two and choose Open in Integrated Terminal. And once I do that, I can just write the command python. And if you're doing this locally on your Mac or [[Windows]] it's different. For Windows, it's just py. For the Mac, it's Python three. Here in the codespace I'm going to write python and then helloworld_start.py, and you can see that the program runs. So I can run it manually if I want to as well. All right, let's go ahead and run the same thing in Colab. So I'm going to switch over to Google Colab and you should already have a notebook created. If you don't, make a new one. And you should have a code cell.
>
> **[7:46](https://www.linkedin.com/learning/learning-python-25309312/building-hello-world?u=76281980&t=466)** If you don't have a code cell for some reason, just click on the + Code button right here and let's do the same thing. And to make this really simple, I'm just going to copy and paste this code. All right. So what I'm going to do now is now that I've got my code, I'm going to run it by clicking on this little play button right here. And this will take a moment for Colab to connect to the back end. You can see down here it's saying it's connecting. All right, so here's the program running, here's my hello world, it's asking me what is my name. I'll type in Joe, and sure enough we see the same result. All right, well congratulations. You have just written a Python program.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (29), [[Google]] (2), [[GitHub]] (2), [[Windows]] (2), [[JavaScript]] (1)
> **CLI Commands:** python (29), make (2)
> **Tools:** terminal (6), colab (4), visual studio (2), github (2), command line (1)
> **UI Navigation:** click on (4), go to (2)
> **Env Vars:** repl (4)
> **Definitions:** is called (1), stands for (1), is an  (1), is a  (1)
> **File Paths:** helloworld_start.py (2)
> **Code Identifiers:** helloworld_start (2)

#### [Variables and simple data types](https://www.linkedin.com/learning/learning-python-25309312/variables-and-simple-data-types?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/variables-and-simple-data-types?u=76281980&t=0)** - [Instructor] Just about every program needs to work with data and information. And in this example we'll explore [[Python (Programming Language)|Python]]'s basic data types and learn how to work with variables. So we saw a quick example of this in the previous video when we created a variable to hold a name value. So here in my code I'm going to open up the file variables_start.py, and if you're using CoLab you can go ahead and copy the starting point code right here into your notebook and follow along there. So what I'm going to do is I'll just do that right now. I'm going to copy these lines and then you can go ahead and create a new code cell just by clicking on plus code here. Or if you hover, you'll see that you'll get these little plus code buttons. So let's just go ahead and paste those in and I'll come back to Colab in just a moment. All right, so there are five major data types in Python. There are numbers such as integers and floating point values with decimal points. There are strings which is a sequence of characters in quotes and there are Boolean values which are either true or false. There are two other major data types that we're going to learn about in a separate video. So I'm not going to cover those here, they're coming later. There are sequence types which are sequences of values and there are dictionaries which map unique keys to specific values. So I've defined some variables here to hold each
>
> **[1:33](https://www.linkedin.com/learning/learning-python-25309312/variables-and-simple-data-types?u=76281980&t=93)** of the basic data types. A variable in Python is basically just like a little box that holds whatever value you put into it. I've given my variables these names and variable names in Python have to follow some rules. They have to start with either a letter or an underscore like this and they can contain letters or numbers, right? So I can do something like this. What they can't do is they can't start with a number, that causes an error. So if I try to name this 1myint, you can see that the code turns red because that's a problem and they are case sensitive. So for example, if I named a variable myint and another one with a capital I like this, those are two different names and two different variables. We can display the value of a variable by using the print function like we did in the previous video. So in that example we use print to display a message. Here we can use it for our variables, so let's try that out. So let's print out myint and let's print out mystr. All right, and then let's run what we have and we'll do that the same way we did in the previous video. I'll just choose run Python in terminal and sure enough you can see the output there. And of course we can try this in CoLab. So let's copy these lines, go to CoLab, paste them in, and let's run. And you can see the result here, so we can operate on data. It's not enough just to declare variables.
>
> **[3:05](https://www.linkedin.com/learning/learning-python-25309312/variables-and-simple-data-types?u=76281980&t=185)** We want to be able to operate on the data and let's try some operations. Oh, by the way, before we do that, you might be wondering what these lines with the green text is and why there's a hash character in front of these. These are called comments. Comments are used by programmers to explain what the code is doing. Now they're not part of your program, they're completely ignored by the Python interpreter. And you will see comments like these in my examples throughout the course that explain what is happening in each example. And they're really useful. They help remind you or other people who work on your code what a specific section of code is doing. So I highly encourage people to use comments in their code. All right, so let's talk about operators on data. So we have standard math operators such as addition, subtraction, multiplication, so on. So let's try some of those. Let's print out myint plus myfloat and let's try the multiplication operator. So that's going to be the star character, myint star, myfloat is multiplying. And let's also try the division operators. That's my end divided by, that's a slash character myfloat. And there are also some operators that you might not be familiar with but are used in programming quite a bit. For example, there's the modulo operator which is represented by the percent sign. So if I write percent,
>
> **[4:40](https://www.linkedin.com/learning/learning-python-25309312/variables-and-simple-data-types?u=76281980&t=280)** so myint percent three. So the modulo operator, this percent sign right here, gives me the remainder of the first number divided by the second number. So if we scroll back up and we see that myint is 10, so 10 divided by three, right? So three goes into 10, three times, which is nine. And then the remainder should be one. So what I'm going to do is comment out each of these previous lines, we don't see them in the output and I'll choose run Python file in terminal, and sure enough you can see here's the result of the addition, the multiplication, the division, and sure enough the modulo operator gives me the remainder value amount. All right? I can also perform operations on data types that aren't numbers. So let's close this terminal. So for example, I can add two strings together. I can write another str equals, this is another string and then I can print out mystr plus another str. I can also multiply a string. I can do print nom space times three.
>
> **[6:10](https://www.linkedin.com/learning/learning-python-25309312/variables-and-simple-data-types?u=76281980&t=370)** And once again, before we try that, I'm going to comment out my previous examples. I'm going to select these lines and I'm going to type control slash, the forward slash, and it's command forward slash if you're on the Mac and that will comment out multiple lines. All right, so let's go ahead and try running this again. So you can see my two strings got added together and then my nom got turned into nom nom nom by multiplying it by three. Now you have to be careful here, you can't add two fundamentally different data types. So if I try to add a string with a number, that doesn't work. So if I write print mystr plus one, that's going to cause an error. Let's try running that again. And you can see that this is what an error looks like in Python. You can see it says, "TypeError, can only concatenate str not int to str." There are two fundamentally types. Now there is a way to fix this and get it to work, but we'll save that for the next chapter. We also have operators that perform logical and comparison operations. So let's try some comparisons. I can write print, now if I want to see if a variable is equal to a specific value, I can use the double equal sign, not just one, that's how you assign the value, I need to do double equals 10. And that's true. I can also check to see if a variable is not equal to something by using the exclamation point equals operator. I can use comparison operators, I can check to see if myint
>
> **[7:49](https://www.linkedin.com/learning/learning-python-25309312/variables-and-simple-data-types?u=76281980&t=469)** is greater than 20. I can check to see if myfloat is less than 10. And there are also operators for greater than or equal to to simply append an equal sign on the end of each of these to check to see if the value is less than or equal to. And once again, I'm going to comment out these lines. Let's go ahead and run this and we can see that yes, myint is equal to 10. Yes, myfloat is not equal to 20, that's true. Myint is not greater than 20, so that's false. And myfloat is not less than ten, so that's false. All right, let's keep on going. We have logical operators, these perform Boolean operations on values. So for example, if I wanted to check to see if myint is greater than five and myfloat was less than a value, I can do that using the logical operator. So I'll write myint is greater than five and myfloat is less than 25.0.
>
> **[9:05](https://www.linkedin.com/learning/learning-python-25309312/variables-and-simple-data-types?u=76281980&t=545)** Now since both of those statements are true, the result should be true. So let's comment these guys out. All right, and let's run this and sure enough, that's true. I can also change this to an or operation. So I can check to see if myint is, let's see, less than five, which is false, right? It's 10 or myfloat is less than 25 and the result is still true, right? Why? Because in the case of an or operator, only one of these has to be true. There's also the not operator, which just flips the result of a Boolean expression. So if I print not, and I'll put these in parentheses just to group them together, all right, let's try that. Now we can see that the result is false because I'm taking the result of this expression, which is true and using the not operator to flip it. All right, we will get more into this as we go through the course, but this should give you some foundational knowledge of how to do operations and data types. It's also worth noting that variables can be re-declared in your code. So for example, the fact that I originally declared myint to be an integer up here, that doesn't really matter. If I really want to, I can do something like this.
>
> **[10:40](https://www.linkedin.com/learning/learning-python-25309312/variables-and-simple-data-types?u=76281980&t=640)** I can write myint equals and now A, B, C, and I'll print it out and let's go ahead and comment that out. All right, so let's go ahead and run this. And you can see that it works, A, B, C gets printed out. So I changed both the value and the type. Right now it's a string and Python is just fine with that. Okay, so that was quite a bit about variables and operators. If you didn't get all of it the first time though, don't worry. We're going to be using these kinds of foundational Python features throughout the course. Once you've used them a few times, they will start to come naturally to you.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (10)
> **CLI Commands:** python (10)
> **Analogies:** for example (5), such as (2), just like (1)
> **Tools:** colab (4), terminal (3)
> **Cross-References:** previous video (3), next chapter (1)
> **Definitions:** is a  (2)
> **File Paths:** variables_start.py (1)
> **Code Identifiers:** variables_start (1)

#### [Sequence data types](https://www.linkedin.com/learning/learning-python-25309312/sequence-data-types?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/sequence-data-types?u=76281980&t=0)** - [Instructor] Right, remember I said that there were five main data types in [[Python (Programming Language)|Python]]. We covered three in a previous video, numbers, strings and booleans. And now we're going to learn about the other two, which are sequences and dictionaries. These are called compound data types because they contain multiple values. In this video, I'm going to focus on sequence types, and I'll cover dictionaries in a separate video. So let's open up sequence_start.py, and if you're using CoLab, just go ahead and make a new code cell if you don't already have one. So a sequence is exactly what its name implies, it is a sequence of values. And one very common type of sequence is a list. A list is defined using square brackets. So I'm going to create a variable named mylist, and then I'm going to type these two square brackets. Now, each value is separated by a comma, and they don't all have to be the same type. So I'll put some numbers in here, and I'll put a string, and then I'll put a floating point number, and then let's put a boolean in here. So lists, just like any other sequence, have a length. So, I can use the len function to calculate the length. So let's print out the length of my list. Alright, so let's try running what we have. And we can see that the list is five-items long. Let's go ahead and try this in CoLab. So I'll copy and paste this in, run.
>
> **[1:34](https://www.linkedin.com/learning/learning-python-25309312/sequence-data-types?u=76281980&t=94)** Okay, and you can see that it works. Alright. So the way that you access each value in a list is by using an index, which is also enclosed in brackets. Now, indexes are zero based. In other words, this first item right here is at index zero, and positive indexes start from the beginning, and negative indexes start from the end. So if I wanted to access this string right here, it's the third item, but it's at index two because it's zero based. So, I would write print(mylist[2]). And I can also index in from the end. So if I print(mylist[-1]), right, that will give me this boolean value right here. I can also change the value at an index in a list by just assigning a new value at that index. So for example, if I wanted to write mylist[0] = 10, we can then print out my list to see how it's changed. Alright, so let's pause here and run what we've got. Okay, so I got the length still. So here's the item at index two, there's the boolean at index minus one. And you can see now that I've changed the first value from a zero into a 10. Let's go ahead and try this in CoLab. And you can either use the same code cell,
>
> **[3:07](https://www.linkedin.com/learning/learning-python-25309312/sequence-data-types?u=76281980&t=187)** or you can make another code cell, right, you can see that the results are the same here. Alright, I'm going to keep on working in the Codespace. If you want to work in CoLab, that's fine, keep on doing that. Alright, we can also add lists to each other using the addition operator. So let's comment out these previous examples. And I'm going to define another list, and I'm going to put some values in there. And then I'm going to write mylist = mylist + another_list. And then, let's print out my list. Alright, and let's try that. And you can see that the values in the second list just get added on to the end of the first list. Oh, by the way, remember we learned about strings in the previous video, I want to point out here that strings are also sequences. So each element in a string is a character. So if I have a string, like mystr, and "This is a string," right, I can access individual characters the same way by using a sequence indexer. So if we comment out this previous one and we run this, you can see that I'm getting the letter i, which is the third letter, but it's at index two. The difference however, is that strings, unlike lists, are immutable.
>
> **[4:42](https://www.linkedin.com/learning/learning-python-25309312/sequence-data-types?u=76281980&t=282)** So you can't change the value of a string once it's created, you have to make a new string if you want to change it. So for example, if I tried to do something like mystr[2] = "Z," right?
>
> **[4:57](https://www.linkedin.com/learning/learning-python-25309312/sequence-data-types?u=76281980&t=297)** If I try to run that, you'll see that results in an error, and it says the "'str' object does not support item assignment," that's because I just can't change it. So, just something to be aware of. Let's continue looking at indexing. So let's go ahead and comment that. So the indexing is pretty flexible. We can use this indexing method to access multiple continuous elements in a sequence by creating what's called a slice. A slice has a starting and an ending value. So if I wanted to get say, three items in the list, these three items right here, I could write a slice that looks like this. So I'll write mylist, and then inside the brackets, I'll have the starting value, which is index one, up through index four. So the first number is the index to start, the second is the index to stop. And that number is not inclusive. So this slice will give me index one, index two, and index three, but not the four. And I can also specify a third value, which is the step amount. Now, normally, the index increments by one, but I can change that. So to skip every other item, I would just step by two. So let's go ahead and try that. And you can see that I'm getting index one and index three, and we're skipping over the second one. Now, slicing values, by the way, are optional.
>
> **[6:30](https://www.linkedin.com/learning/learning-python-25309312/sequence-data-types?u=76281980&t=390)** So I can leave out the first value, and that would be the default to start at zero. I can leave out the four, and then the default value would be to go all the way to the end of the sequence. And of course, you can also use negative numbers in slices to operate starting at the end of the sequence, and I'll let you try that on your own. But let's go ahead and run this updated code, and you'll see that now, I'm getting index zero, and then it's skipping one and getting me two, skipping three, giving me four, right, and so on. And one interesting trick that you can do with this is to reverse the order of a sequence. So for example, if I wrote print(mylist), and then I'm going to use this trick to leave out the starting and ending values, and then do a minus one. Alright, let's go ahead and comment that one out. If I run this, you can see that now the sequence has been reversed. And by the way, that works with strings too. So if you want to reverse a string, you can do this little trick right here with the string. So there's two other types of sequences I want to cover, and they are tuples and sets. Tuples are like lists, but we define them using parentheses instead of brackets. So let's define a tuple. So inside parentheses, I'll write some data values, and then I'll print out mytuple at index one.
>
> **[8:07](https://www.linkedin.com/learning/learning-python-25309312/sequence-data-types?u=76281980&t=487)** Alright, let's go ahead and run that. Okay, and you can see the result there. So I can perform the same kinds of operations on tuples as I can on lists. I can use indexes and slices to access their content, I can use the len function to get their length. The core difference between a list and a tuple is that tuples are immutable, just like strings are. They can't be changed once they are created. So I can't change the value at any of these indexes. Tuples are often used to hold data that isn't going to change, which makes them a little bit more efficient in memory and processing than lists are. And they can be used to do things like return multiple values from a function, and we'll learn more about functions later in the chapter. Sets are another sequence type similar to lists. So let's define a set, and they are defined using curly braces instead of parentheses. So I'll define some values, {1, 2, 3, 2, 4}, and then a string. Then we'll print it out. Sets are an interesting data type because they can only contain unique values. So you can see here, I've defined my set with two instances of the number two. Let's comment out this previous print. But when I run this, you can see that in the output, there's only one instance of the value two. So the other one automatically got filtered out.
>
> **[9:43](https://www.linkedin.com/learning/learning-python-25309312/sequence-data-types?u=76281980&t=583)** So sets enforce uniqueness in their membership. So you can only have one instance of each value. And you can use sets in situations where you want to make sure that there's only one instance of a particular value in the sequence. And it's also important to note that sets, unlike lists and tuples, don't support indexing. So if I try to index into this set by using something like this, if we run this, you can see that I get a type error, and it says "'set' object is not subscriptable," which is just kind of a fancy way of saying, "Hey, you can't use indexes on sets." Alright, one more thing I want to show, and that is the test for membership. You can use the IN operator to see if a value is in a sequence. So let's try that out with each of the sequences we've created so far. So let's check to see if one is in my list, let's check to see if three is in my tuple, and let's check to see if five is in my set. Alright, let's go ahead and run that. And we can see that, sure enough, one is in my list. There is no value three in the tuple, and there is no value five in the set. So sequences are one of the most common foundational data types in Python. They are used in almost every kind of program, and we will be using them
>
> **[11:17](https://www.linkedin.com/learning/learning-python-25309312/sequence-data-types?u=76281980&t=677)** as we progress through the [[Representational State Transfer (REST)|rest]] of the course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (2), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** make (4), python (2)
> **Analogies:** for example (3), just like (2), similar to (1)
> **Definitions:** is a  (4), in other words (1)
> **Cross-References:** previous video (2), we covered (1), later in (1)
> **Tools:** colab (4)
> **Code Identifiers:** sequence_start (1), another_list (1)
> **Warnings:** be aware (1), note that (1)

#### [Dictionary data types](https://www.linkedin.com/learning/learning-python-25309312/dictionary-data-types?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/dictionary-data-types?u=76281980&t=0)** - [Instructor] Let's round out our investigation of data types by considering the dictionary type. Now it's called a dictionary because it works kind of like how a real-world dictionary does. You look up the data inside a [[Python (Programming Language)|Python]] dictionary by using a key-value. Now in a real dictionary, that would be in individual [[Microsoft Word|word]], which you would look up to see what the definition is. And Python dictionaries are similar. They use key-value pairs to associate data with a specific key. So where sequence types, which we saw previously, are indexed by a range of numbers, dictionaries are indexed by a set of unique key-values. Let's go ahead and open up the dict_start file. And, of course, if you're using Colab, that's great, just go ahead and make a new code cell. So let's start by creating a dictionary. And we're going to do this by using curly braces, sort of like with sets, but the difference is that we specify a key and then a value to go along with it, separated by a colon. So let's create a variable called mydict, and I'm going to have these two curly braces. And then let's start putting some keys and values into the dictionary. So I'll have a key, which is a string of "one" associated with a value of 1, and then I'll do the same thing here. I'll make one for two. Then I'll change things up, and I'll have the key be an integer and the data will be a string. And then, let's really go off script.
>
> **[1:33](https://www.linkedin.com/learning/learning-python-25309312/dictionary-data-types?u=76281980&t=93)** Let's make a floating-point value as a key, and then the data will actually be a list. And I'll put three strings in there "four", "point", "five". All right, and then we'll print out mydict. Okay, so a couple of things to be aware of. First, keys have to be immutable data types, like strings or numbers. And each of the keys in a dictionary also has to be unique within the dictionary. Now, in this example, I'm using some strings, an integer, and a floating-point number as the keys. And, in the case of the 4.5 key, the value is actually a list of values. It doesn't have to be a single data element like you see here. In fact, this is fairly common in Python programming to have keys in dictionaries refer to lists or other collection types, like other dictionaries, or sets, or any data element. So let's go ahead and run this code. All right, and you can see in the output, here's our curly braces, we've got each key associated with a value. And let's do the same thing in Colab. I'm going to copy, and we'll paste it in here, and we'll run. And you can see the same result. All right, I'm going to keep on working in the Codespace, you can keep on working in Colab, if that's what you're doing. So to access values in a dictionary, I supply the key as the index.
>
> **[3:07](https://www.linkedin.com/learning/learning-python-25309312/dictionary-data-types?u=76281980&t=187)** So, to get this item right here, I would write mydict. And then in the indexer, I would use the string "one", 'cause that's the key. If I wanted to get the value associated with the 3 key, then I would use mydict and then the value of 3 in here. Okay. So let's go ahead and run that, and let's comment out printing the dictionary. So let's run that. And you can see that, sure enough, the value 1 here is being printed along with the value associated with the "three" key. I can also create new content by creating a new key. So, for example, I can write mydict, and then I'll make a new key called "seven", in this case, it's a string, and I'll just choose a value to go along with that. I'll just put the number 7, and then let's print out the dictionary, and let's comment these two out. All right, let's try running that. And you can see, down here at the end, a new key and a value was added to the dictionary. Now if you try to access a nonexistent key, that results in an error. So let's try accessing a key named "blarg", all right? And let's comment that out. And let's try running.
>
> **[4:40](https://www.linkedin.com/learning/learning-python-25309312/dictionary-data-types?u=76281980&t=280)** Okay. And you can see that I'm getting a KeyError because blarg does not exist. There is no key with that name. So, to avoid that happening, you can use the in operator, and we saw how to do this with sequences. You can use the in operator to see if a key already exists in a dictionary. So let's print out, let's see, let's test to see if the key "two" is in mydict.
>
> **[5:08](https://www.linkedin.com/learning/learning-python-25309312/dictionary-data-types?u=76281980&t=308)** And then let's also test to see if "blarg" is in mydict. And let's comment that out. All right, let's try this. And you can see that, yes, "two" exists, "blarg" does not. And if we scroll up, we can easily verify that the key of "two" actually is there. All right, let's take a look at some other dictionary operations. Comment those out. You can retrieve all of the keys and all of the values in a dictionary by using the appropriately named functions. So, for example, I can print out mydict, and I'm going to call the keys function. And I can do the same thing. On mydict, I'll call the values function. So let's run that. And you can see that, when I do that, I get these lists of values. And it's important to note that these are not technically Python lists. They're a dict_keys and dict_values lists, they're actually special dictionary types. Technically, I mean, they act like lists, you can index into them, you can iterate over them, just like regular lists. I just want to point out that when you see this dict_keys and dict_values, it's because that these lists are specific to dictionary data types. But you can see that, by using keys and values, I can get a list of all the keys, so I know what keys are in my dictionary, and I can also get the values independently.
>
> **[6:41](https://www.linkedin.com/learning/learning-python-25309312/dictionary-data-types?u=76281980&t=401)** So if all I care about is the values, I can iterate over all my values. There's also a way to iterate over the entire contents of a dictionary by using the items function. So I'm going to make a for loop, and then I'm going to write for key, val in mydict.items().
>
> **[7:07](https://www.linkedin.com/learning/learning-python-25309312/dictionary-data-types?u=76281980&t=427)** And this is an example of a tuple. So mydict.items is returning a tuple each time it runs through. Each tuple is going to have two values in it. So let's comment out these two lines. So, in this loop, I'll just simply print out the key and the value. And, remember, we learned about tuples in the previous video. So let's go ahead and run this. And you can see that I'm printing out each key and each value as we go through the dictionary line by line. Okay, so that wraps up our overview of the five main Python data types. And these types, numbers, strings, Booleans, and then sequences, and dictionaries are by far the most common data types that you will encounter when working with Python. And we're going to see these all over the place in the [[Representational State Transfer (REST)|rest]] of the course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (6), [[Microsoft Word|Word]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** python (6), make (5)
> **Code Identifiers:** dict_keys (2), dict_values (2), dict_start (1)
> **Analogies:** for example (2), kind of like (1), just like (1)
> **Tools:** colab (3)
> **Definitions:** is a  (1), is an  (1)
> **Versions:** 4.5 (1)
> **Cross-References:** previous video (1)

#### [Conditional decisions](https://www.linkedin.com/learning/learning-python-25309312/conditional-decisions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/conditional-decisions?u=76281980&t=0)** - [Instructor] One of the most common operations that almost any program has to perform is making decisions. Your programs will often need to compare different values to each other and execute code based upon the logical outcome, and that's where conditional logic comes into play. So here in the editor, I'm going to open up the conditionals_start file. And if you're using Colab, you can just go ahead and copy and paste the starting point code into a notebook cell. And you've probably noticed I've declared two variables on the same line. I didn't show this in the variables video, but you can actually declare multiple variables on the same line, like this. So I'm going to copy this and paste it into Colab. And if you need to make a new code cell, just go ahead and click on + Code. And we'll come back to that in just a moment. All right, in [[Python (Programming Language)|Python]], conditional decisions are handled using the if statement. And I've got my two variables, and they're starting off with the values 10 and 100. So let's add our first conditional statement. I'm going to write, if x < y and then a colon character. Okay, and then I'm going to hit return. And notice when I hit return, that the code gets indented by two spaces. Okay, this is going to become important in a moment. So I'm going to write print("x is less than y"). So I want to point out here that this indentation of the print statement is important. Python uses the white space indentation
>
> **[1:34](https://www.linkedin.com/learning/learning-python-25309312/conditional-decisions?u=76281980&t=94)** to group together sets of statements. In this case, the colon character after the if condition indicates that a set of statements are going to follow, that belong to this if statement. Other programming languages that you might be familiar with ignore white space indentation, but in Python, it's very important to pay attention to this. So for example, if you're used to a language like [[JavaScript]], you might see something like curly braces that group together these statements, like this. Now this is not the way Python works. You have to have the colon and then indentation. It doesn't matter how many spaces there are, it can be two, it can be four, it just matters that all the statements that follow are indented the same, okay? So what's going to happen is, Python will evaluate this statement and if x is less than y, then the print statement will execute. So let's go ahead and run this. And sure enough, because 10 is less than 100, "x is less than y" gets printed. Let's copy this code and paste it in Colab, and let's run it. And sure enough, we get the same result. Okay, so let's try making a couple of changes, all right? So what happens if I make x equal to 1000? And now I run it again, you can see that nothing gets printed, right?
>
> **[3:08](https://www.linkedin.com/learning/learning-python-25309312/conditional-decisions?u=76281980&t=188)** Because x is now not less than y anymore, it's greater than y. So we have to figure out a way to handle the case where the if condition evaluates to false. So let's make that change. On the next line, below the print, unindented, okay? Back at the same level as the if statement I'm going to write else, and then a colon. And then, print("x is greater than y"). Okay, and remember, the indentation of these statements is important, right? So now we have an if condition, and we have an else condition. So when the if condition is true, this block of code will execute. And if it's not, then this block of code will execute. So let's go ahead and run. And sure enough, now we have "x is greater than y." Let's go ahead and add this to our code in Colab. And let's run it, and make this a thousand. And we can see the same result. All right, let's try making the variables exactly equal to each other, all right? Now, let's run it again. And I get, ah, "x is greater than y," but that's not true, they're the same value. And the problem is that this condition right here is evaluating to false. So the else condition is executing even though it doesn't check to make sure that actually x really is greater than y, right?
>
> **[4:43](https://www.linkedin.com/learning/learning-python-25309312/conditional-decisions?u=76281980&t=283)** Let's make one more change to fix that particular problem. In between the if and the else, I'm going to add elif. I'm going to say, elif x==y: I'm going to print("x is the same as y"). All right, so now we have a complete conditional structure. We have if, this condition, else if, this condition. And you can put as many elif keywords as you want. I can have all kinds of else-ifs that will give me a way of chaining more than one condition into a conditional statement. So now we're checking for the special case where x is exactly the same as y, and handling it properly rather than just assuming that because this condition was false, that the catchall else condition is true. So if we run this now, now we get the correct output of "x is the same as y." And of course, let's go ahead and copy this into Colab. Or if you're just working in Colab, you can just type the code yourself. Let's make them the same value. And now we can see the result. So there's one more way of writing conditional statements I want to show you. Sometimes you'll run into situations where you have an if and an else, right? So you've got four or whatever lines of code
>
> **[6:17](https://www.linkedin.com/learning/learning-python-25309312/conditional-decisions?u=76281980&t=377)** intended to do a simple comparison, and that's a bit more verbose than it needs to be. So Python has a construct called a single conditional statement. This statement lets you write common if-else logic all in one line. It's just a more concise way of writing the comparison logic. So let me add the statement and you'll see what I mean. So I'm going to write result, and we declare a variable called result. And result is going to be "x is less than y," and then I'll write if (x < y), and then else "x is greater or equal to y."
>
> **[7:00](https://www.linkedin.com/learning/learning-python-25309312/conditional-decisions?u=76281980&t=420)** And then I'll print the result. Okay, so this code does the work of an if-else construct, but just by using one line. So let's go ahead and run this, and let me comment out the previous examples so we don't crowd the output. Let's go ahead and run. And you can see that "x is greater or equal to y" because they're the same. If I change this to 10, and let's run it, you can see "x is less than y." Okay, it's just a nice concise way of writing a conditional statement instead of having to write out a much more verbose if-else block when the logic is relatively simple.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (6), [[JavaScript]] (1)
> **CLI Commands:** make (7), python (6)
> **Tools:** colab (6)
> **Code Identifiers:** conditionals_start (1)
> **UI Navigation:** click on (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Loops](https://www.linkedin.com/learning/learning-python-25309312/loops?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/loops?u=76281980&t=0)** - [Instructor] Executing a set of code over and over again is a fairly common scenario in programming. So for example, you might want to process all of the contents in a list of values, or you might want to run some code until a certain condition is met. [[Python (Programming Language)|Python]] provides a couple of ways of doing that, which is what we're going to take a look at now. So here in my editor, I'm going to open up loops_start, and of course you can follow along with me if you're using Colab. Just create a new code cell if you need to. In Python, we use a programming construct called a loop in order to execute code multiple times. So let's start by taking a look at the while loop. This kind of loop executes code while a certain condition is true. So you can see I have my variable named x, it's declared to be zero, and I'm going to write while x is less than five, I'm going to print x, and then I'm going to set x equal to the current value of x + 1. So once again, you can see that the code in the while loop is indented to indicate that it belongs to the scope of this while loop. And you can see that if I un-indent the code, we get an error. So if you're writing code and you see an error like this, and if you hover over it, you'll see that it says Expected indented block. That usually means that your code is not indented properly. Okay, so a while loop executes a block of code while a particular condition evaluates to true. So while x is less than five, we're going to print the value of x, and then we're going to increment x by 1.
>
> **[1:34](https://www.linkedin.com/learning/learning-python-25309312/loops?u=76281980&t=94)** So let's go ahead and run this, and sure enough, you can see that x starts out as 0. So it prints 0, then 1, 2, 3, and 4, and then it increments to 5. In this statement right here, the value of x gets to 5. So this condition is no longer true, and so the loop terminates. So this kind of loop using the while keyword is useful for scenarios where you don't necessarily know how many times the loop is going to execute. So let's consider another example. I'm going to declare a variable named answer, and I'm going to use the input statement to ask, "Should I stop?" Then while answer is not equal to "yes", colon, I'm going to print the answer, and then I'm going to get the next answer. And it's always important to make sure, heh, that you have a condition that actually causes the loop to terminate. Otherwise the loop will just continue forever, and you'll have what's called an infinite loop. So let's go ahead and comment this out. Alright, and then let's run our updated code. So in this example, the loop is just going to keep executing until the user enters the [[Microsoft Word|word]] yes. So if I run this, right, and say, "Should I stop?" Alright, no. Should I stop?flarg. Should I stop?yes.
>
> **[3:07](https://www.linkedin.com/learning/learning-python-25309312/loops?u=76281980&t=187)** And you can see that it stops. Alright, let's try the same thing in Colab. Let's run this. Should I stop?no. Should I stop?flarg. Should I stop?yes. And you can see that the loop exits. Okay, so a common scenario for this kind of loop, a while loop, is when you are reading data from a data source, you want to keep reading data until there is no more. But you don't necessarily know how much data there is when the loop starts. So now let's take a look at a different kind of loop. We're going to use a four loop. Four loops in Python work a little bit differently than you might be used to seeing in other languages if you're familiar, for example, with [[JavaScript]] or C or [[Java]], right? They have the concept of an index variable that counts the number of iterations in the loop. Python works a little bit differently. Python for loops are what are called iterators, and we'll learn more about this later. These kinds of loops operate on sets of things, not just numbers. So let's try an example. I'm going to declare a variable named days, and I'm going to create a list of days. So that's going to be Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, and Sunday. Then I'm going to write my loop, I'm going to write for d. So I'm going to create a variable named d in days, and then colon.
>
> **[4:39](https://www.linkedin.com/learning/learning-python-25309312/loops?u=76281980&t=279)** And then I'm going to print d. So in this case, my variable is a list, and my loop is iterating over each element in the days list. So in each iteration, this d variable will be set to the current item that it's looking at at that time through the loop. So if I run this, and let's get rid of this previous example. If I run this, right, you can see that it's printing out, you know, Monday, Tuesday, up through Saturday. For loops are typically used in situations where you have a fairly good idea how many items there are in a particular set of data. Now, I could have used a while loop for this scenario, but it would be more awkward to write that. And the for loop construct is just better suited here. So let's take a look at some ways we can control the behavior of loops using the break and continue statements. And I'm going to use the for loop for this, but everything I'm about to show you also works in while loops as well. So let's go ahead and use the break and continue statement. I'm going to copy this and put it down here, and let's comment out that loop. So I'm going to slightly modify this to use a conditional, which we learned about in a previous video. I'm going to write if d double equals Wednesday, colon,
>
> **[6:05](https://www.linkedin.com/learning/learning-python-25309312/loops?u=76281980&t=365)** and then I'm going to put the break keyword in here, and notice that I'm multiply indenting things, right? So all of these statements belong to the for loop. This break statement belongs to the if statement up here. Alright, so the break statement is used to stop the execution of a loop. In this case, if d is equal to Wednesday, then the break statement executes and the loop terminates. So let's go ahead and run, and sure enough, you can see that that's what happens. We get Monday, Tuesday, we encounter Wednesday, and the loop stops. Alright, so let's take a look at another statement. Let's take a look at the continue statement. What I'm going to do now is change this to say if d is equal to Thursday, instead of writing break, I'm going to write continue. Okay, the continue statement doesn't cause the loop to stop. It just skips the [[Representational State Transfer (REST)|rest]] of the code in the loop this particular time around. Now the rest of the loop, in this case is, you know, this one statement, but if there were a lot more statements here, they would all be skipped for this particular time through the loop. So let's run the updated code, and you can see that what happens is we get Monday, Tuesday, Wednesday, we get to Thursday, and then we skip the print statement, right? The rest of the loop gets skipped and then we have Friday, Saturday, and Sunday. Alright, let's take a look at one more example of using loops. So we're going to go back
>
> **[7:37](https://www.linkedin.com/learning/learning-python-25309312/loops?u=76281980&t=457)** and revisit the loop that we had up here where we were iterating over these days. So let's go ahead and copy and paste this, and I'm going to comment out my previous example. Alright, so I'm going to replace the print statement, and what I'm going to do is, first I'm going to write for i comma d in, and I'm going to use a function called enumerate, and I'm going to enumerate the days list, and then I'm going to print i and d. So the enumerate function is built into Python, and it will iterate over this collection, just like a for loop normally would. But in addition to returning the value of the current item, it also returns a value that is the index of the item in question. And if you remember earlier in the chapter, we talked about i and d together, this is a tuple, right? So this is an example of a tuple being used. So it's going to return two values. So what I'm going to do is print out both the index and the value of the item. So let's go ahead and run this and see what happens. And now you can see I'm getting the index value along with the item, right? So 0 Monday, 1 Tuesday, and so on. So not only do I get the item in the collection, I also get the numerical index of that. So loops are one of the foundational parts of just about any programming language.
>
> **[9:10](https://www.linkedin.com/learning/learning-python-25309312/loops?u=76281980&t=550)** Python is no different, and we'll see a lot more code using loops in the rest of the course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (7), [[Representational State Transfer (REST)|Rest]] (4), [[Microsoft Word|Word]] (1), [[JavaScript]] (1), [[Java]] (1)
> **CLI Commands:** python (7), make (1)
> **Definitions:** is a  (3), means that (1), is an  (1)
> **Cross-References:** previous video (1), earlier in (1), we talked about (1)
> **Analogies:** for example (2), just like (1)
> **Tools:** colab (2)
> **Code Identifiers:** loops_start (1)
> **Speakers:** - [instructor] (1)

#### [Defining functions](https://www.linkedin.com/learning/learning-python-25309312/defining-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/defining-functions?u=76281980&t=0)** - [Narrator] So far, we've been writing [[Python (Programming Language)|Python]] code in individual statements, but most larger programs are organized into reusable blocks of code known as functions. So functions give us a way of organizing our programs so they're more understandable and modular. And that's what we're going to learn about in this example. So here in my editor, I'm going to open up the functions start file. And if you're using [[Google]] CoLab, go ahead and create a code cell and go ahead and copy these three lines and paste them in there as a starting point. Okay, so let's take a look at the code from our very first program from way back in the beginning of the chapter. So here are these three lines, and these probably look familiar to you. Now suppose I wanted to use this code in multiple places in my program. It wouldn't make sense to just copy and paste the code everywhere because if I ever wanted to change how it works, I'd have to go change that code in lots of different places. A function allows me to group this code together and then reuse it throughout my program as just one line of code. So let's convert this into a function. So functions are defined using the def keyword and it's in lowercase, and then I give it a name. I'm going to call this hello_funk, and then an open and close parentheses followed by a colon. And we see that colon. What does that tell us, right? It tells us that we're starting the scope
>
> **[1:33](https://www.linkedin.com/learning/learning-python-25309312/defining-functions?u=76281980&t=93)** of a block of code, which means I have to indent the lines of code that follow that I want to belong to this function. So we saw this with conditional statements. We saw this with the loops that we learned about earlier. We have to do the same thing with functions. So I'm going to indent each one of these lines of code, and you can see the little red squiggle goes away, so it's no longer an error. Okay, so now these three lines of code belong to this function, function names pretty much follow the same rules as variables. So I can have letters and underscores. I can put numbers in here if I want to. I can't start it with a number. If I do that, you see the code turns red, that's a problem. So I'll just leave it like this. Now, this function does not take any parameters. I can't pass any values into the function because parameters would normally go between the parentheses, but we'll learn about that in a few moments. For now, we'll just simply define a function that doesn't take any parameters to it. Alright, so now that the code is inside the function and I've got the code properly indented, I can use the function just by using its name. So below this function and unindented, I'm going to just simply call hello funk with the open and close parentheses. And let's go ahead and copy this code and let's paste it in here to CoLab. And let me [[Zoom]] CoLab up a little bit so we can see it a little bit better.
>
> **[3:06](https://www.linkedin.com/learning/learning-python-25309312/defining-functions?u=76281980&t=186)** So we'll come back to that in a moment. Let's go ahead and run this. So I'm going to run this in the terminal, and you can see that when I invoke the function, right, I'm calling the function here. Now the code inside the function is executing, right? Let's do the same thing in CoLab. I'll click on the run button and sure enough, we get the same result. Alright, so that's how we define a function. Now in this case, the function is not returning any values, which we'll see how to do in a moment. And it's also not taking any parameters. So let's go ahead and add parameters to our function. And the reason that we would want to do this is because it allows us to customize how the function works. So let's add a parameter that customizes the greetings. What I'm going to do is copy this code and I'll paste it down here so we can compare the two results. And I'll just comment out this example. What I'm going to do is define a parameter by putting a name inside the parentheses here, and I'm going to call it greeting. Now, I can define more than one parameter if I want to, I could put, you know, another parameter here, and I can have as many as I want, but this function's going to take one parameter. So what we're going to do is use the greeting parameter in this third print statement. So instead of printing out, nice to meet you every time, we'll simply print out whatever the greeting is.
>
> **[4:42](https://www.linkedin.com/learning/learning-python-25309312/defining-functions?u=76281980&t=282)** And now when I call the function, I have to supply a value to that parameter. So I can pass in a string that says, how are you doing? And then let's call the function again with, hey, what's up? All right, so now let's run this. And you can go ahead and do that in CoLab as well. So I'll put in Joe, right? It says, okay, how are you doing, Joe? And now it's going to run again because now this invocation is happening. I'll put in Joe again. And now it says, hey, what's up? So by adding a parameter, I've been able to customize how the function works. So like I said earlier, functions can also return values. Now, this function doesn't do that. I'm going to define a function that returns a value, and I'll just create a really simple example. What I'm going to do is comment out this previous example using control slash and then I'm going to define a function called cube that will take one parameter. Now to indicate that my function returns a value, I use the return keyword, and then I'm going to return the value that I want to be the result of my function. In this case, my function is going to take the parameter that it's given and multiply it by itself three times, right? 'Cause that's what the definition of a cube is. So then I'll define a variable and I'll assign it to the result of the cube function being called with the value of three.
>
> **[6:17](https://www.linkedin.com/learning/learning-python-25309312/defining-functions?u=76281980&t=377)** And then we'll print the result. Alright, let's go ahead and run that. And you can see that the result is 27. So by using the parameter and by using the return value, we've now created a function that can return different values based upon the parameter it's given. So maybe try it a few times with yourself with some different numbers to see what happens. And let's go ahead and run it in CoLab just to prove that it works. I'll make a new code cell paste the code in here and click run, and you can see the result. Let's do the cube of four. Let's run it. You can see the cube of 4 is 64. Alright, so now we've seen parameters and return values. So let's try another example. Function parameters can be made optional by giving them a default value. So let's go ahead and take our original code from earlier and I'm going to copy this function and I'll paste it down here and then I have to uncomment it. So I'll select it and do control slash again. Alright, what we're going to do is we're going to add another parameter. We're going to add a parameter for the name. And what I'm going to do is I'm going to have name and then the equal sign, and in this case, the value of none. That means no value. So what I'm going to do now is modify the function to see if a value was supplied for the name parameter. What I'm going to do is check using an if statement to see if name is equal to none.
>
> **[7:54](https://www.linkedin.com/learning/learning-python-25309312/defining-functions?u=76281980&t=474)** Then I'm going to ask the user to input their name. If a value is supplied for the name parameter, then this code right here will not execute and we'll just simply print the greeting and the name. So let's go ahead and try this out. So I'll call hello funk with nice to meet you. All right and then for the second parameter, I separate it with a comma and I'll just put in Joe. All right, and let's go comment out the cube example. Okay, let's run this. Now in this case, you see that it just simply prints out nice to meet you, Joe, because the value for name is not none, right? I gave it a string, but if I take off this Joe parameter here, now I'm not supplying a value for the name. So now let's run it again. And now you can see I'm being asked, you can also call Python functions with explicit names for the parameters. So you probably noticed that I'm calling the function with values that are in the same order as the parameters, right? So this string gets passed in for the greeting. This string gets passed in for the value of name, but I can actually do this by explicitly saying which variables or which parameters I want the values to be assigned to.
>
> **[9:28](https://www.linkedin.com/learning/learning-python-25309312/defining-functions?u=76281980&t=568)** So for example, I could simply write greeting equals, and then I can put name equals Joe in the front. And now I'm specifically telling Python like, hey, the name parameter gets this string, the greeting parameter. And this allows me to change the order in which the code is being called. Now, in reality, most people don't do this, but it's nice to be able to do this if you want to make your parameters in a certain order. So if I run this, right, you can see that the result is now the same. Alright, let's look at one final example. We're going to write a function that takes multiple parameters, but we don't know how many parameters are going to be coming in. So let's comment out this example. And what I'm going to do is write a function. So I'm going to use my def keyword and I'm going to call it multi add. And then I'm going to type a star character along with the name of the parameter. I'll call it args, just for historical purposes. So when you put the star in front of this parameter name, it means that there are going to be more than one parameters coming in, but you don't know how many there are in advance. In this case, right? In the previous example, we had one parameter, here we had two parameters, one of which was optional. In this case, we don't know how many there's going to be. So what I'm going to do is have a local variable named result, and I'll initialize that to zero. Then I'm going to use a loop. I'm going to write 4x in args,
>
> **[11:04](https://www.linkedin.com/learning/learning-python-25309312/defining-functions?u=76281980&t=664)** and then I'm going to write result equals result plus x, and then I'll return the result. All right, so what I've done here is I've defined a function called multi add. The star means, hey, I can pass in a variable number of parameters. So now instead of containing just one value, that args parameter is going to contain multiple values in the form of a list. So let's try this out. I'm going to print multi add, and I'm going to call it with some parameters, four or 5, 10, 4. That should be about right, alright. So then now let's go ahead and run this. And you can see that the result is 23. And if I change the code to add another 10 on the end and I run it again, now you can see the result is 33. So you can have a function that takes both regular and variable parameters, but the variable parameter always has to come last. And again, that's because you don't know how many of them there's going to be. So let's modify our example to have a starting value argument. So I'll have a start here, and then I'll have args, and then I'll just simply initialize result to whatever the start amount is. And now I'll add 10 to the start of this list. Okay and I'll run it again. And now you can see the result is 43.
>
> **[12:38](https://www.linkedin.com/learning/learning-python-25309312/defining-functions?u=76281980&t=758)** And if I make the start value zero, you can see it goes back to 33. Python functions are pretty flexible and powerful, but the core reason why you use functions is to group related code together and then customize the data that the code works on by using parameter values that are supplied to the function when it gets called.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (4), [[Google]] (1), [[Zoom]] (1)
> **CLI Commands:** python (4), make (4)
> **Tools:** colab (6), terminal (1)
> **Definitions:** known as (1), means that (1)
> **Code Identifiers:** hello_funk (1)
> **UI Navigation:** click on (1)
> **Analogies:** for example (1)
> **Speakers:** - [narrator] (1)

#### [Solution: Number counter](https://www.linkedin.com/learning/learning-python-25309312/solution-number-counter?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/learning-python-25309312/solution-number-counter?u=76281980&t=5)** - Let's take a look at my solution to the number counter challenge. For this challenge, we needed to count the number of either even or odd numbers in a given list of numbers. So the function that we had to write takes two parameters. There's a string which indicates which types of numbers we're counting and a list which contains the numbers themselves. To solve this challenge, we needed to use an if conditional, a loop, and the modular operator. So my code first checks to see if the function was given a bad value for the rich parameter. If it's not even or odd, then the function returns minus one. Then it initializes the return value of the count numbers function to zero. Then I have a loop that processes each number in the numbers list. So first I check to see what type of number we're counting, and then I check to see what the result of that number, modulo two, is. So if the result of that number modulo two is zero and we're counting even numbers, then I increment the even number count. Otherwise, if it's not zero and we're counting odd numbers, then we have an odd number and I increment the count for the odd numbers. So part of the challenge was that the which parameter is not guaranteed to be either even or odd. So I had to explicitly check for those. So let's go ahead and run our code against the testing code. And if I scroll up a little bit,
>
> **[1:37](https://www.linkedin.com/learning/learning-python-25309312/solution-number-counter?u=76281980&t=97)** you can see that here's the numbers and here are these three answers that we had to provide. So I'm going to click on test my code, and you can see that the even count is four, the odd count is seven, and then the incorrect, which parameter count properly returns minus one. So if I make a change to the code where I don't properly count even numbers, for example, and we test the code again, you can see that that's incorrect. I got the wrong even number count. So let me go back and fix that and click test again and you can see the right answer. Alright, so did you get the answer? How'd you do on this challenge? Remember, it's not important that your code exactly match mine. What's important is that you check out your code versus mine. See where the similarities and differences are. There's always going to be multiple ways to solve any given programming challenge. So take a look at your solution, compare it to mine, and just know the differences.

> [!info]- Semantic Content
>
> **UI Navigation:** scroll up (1), click on (1)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Speakers:** - let (1)


### 3. Beyond the Basics

[↑ Back to Table of Contents](#table-of-contents)

#### [Built-in functions](https://www.linkedin.com/learning/learning-python-25309312/built-in-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/built-in-functions?u=76281980&t=0)** - [Person] In the previous chapter, we learned about the basics of the [[Python (Programming Language)|Python]] language and how to write some relatively simple programs. In this chapter, we're going to move beyond the basics to some of the features that you'll use when writing more comprehensive programs, so let's start by taking a look at some of the features that Python gives you right out of the box. Sometimes, Python is called the "batteries included" language because it has so much great stuff already built in, so here at the homepage, I'm going to hover over the "Documentation" menu and I'm going to click on this button here labeled "Python Doc," and that's going to take me to the documentation page for Python, and I'm going to click on this "Library reference" link right here, and that's going to take me to the section for the Python Standard Library, which is a really big repository of prebuilt code that you can use in your programs, so if I scroll down a little bit, all right, you can see that there's a lot in here, you know, there's a lot of, like, modules, and we'll get to some of these later in the course, but, I mean, look at how long this list is. There's just... There's so much stuff in here, so what I'm going to do is scroll to right about here and you can see that there's a link for built-in functions, so I'm going to click on that, and this page lists all of the built-in functions that Python supports, these are functions that are already written for you and you can just use them right away in your own code. We're going to take a look at some of these and how they are helpful in our programs,
>
> **[1:34](https://www.linkedin.com/learning/learning-python-25309312/built-in-functions?u=76281980&t=94)** so go ahead and keep this page open as we go through the video so you can refer back to it to get more information about each function if you feel like doing so. All right, so the first function that we're going to look at is the "len" function, which we've seen a couple times already, and if I click on it here, it takes you to the documentation, and this is the format of the Python docs, it's kind of complex and nerdy, so I'm not going to make you read through it, but if you want deeper documentation on each of the functions, you can get to them, but let's go ahead and jump over to the code and see how it actually works. All right, so here in chapter three, I'm going to open up the "builtin_start" file, and if you want, of course, if you're using Colab, you can copy and paste the starting point code over to Colab, so I'm just going to go ahead and copy these two lines, make a code cell and paste those in. So let's use the "len" function to calculate the length of each of these variables, and this will be review for some of the things we've already done in the course, so for example, I can print out the length of my string, that will give me the number of characters in the string, I can print out the length of my numbers, that will tell me how many numbers are in this list of numbers, so it works on pretty much every sequence, anything that has a length, you can use the "len" function on, and if we run this, no surprise, you can see that the results are right here, and of course, let's go ahead and copy that code,
>
> **[3:08](https://www.linkedin.com/learning/learning-python-25309312/built-in-functions?u=76281980&t=188)** paste it over here in Colab, and let's run it, and we get the same result. All right, let's keep on going. The next function I want to take a look at is the... Well, there's two of them, there's the "min" and the "max" functions. The "min" and "max" functions will find the largest and smallest value in a sequence, so let's go ahead and try those out. Let's print out the "max" of my string and let's print out the "min" of my numbers, and let's comment out these previous examples, and let's run this. All right, so we can see that the "max" in my string is Z, because that's the last letter in the alphabet, and the "min" of my numbers, not surprisingly, is one. All right, let's keep on going, comment these guys out, and by the way, feel free to keep on... If you're working in Colab, just, you know, as I'm going through this, just do the same code that I'm doing in Colab. So here's the "str" function, the "str" function will return the string representation of an object, and to see why this is important, let's try writing some code, you can see I have a string here named "prefix," which contains the string result, and then, I have the result of five, and remember earlier in the course, we saw this error, I'm going to try to concatenate the "prefix" string with the result, and if I run this, you can see that
>
> **[4:43](https://www.linkedin.com/learning/learning-python-25309312/built-in-functions?u=76281980&t=283)** that results in an error. It says you can only concatenate a "str" with a "str," not an integer, and that's happening because this is not a string, so what I need to do is use the "str" function to turn that integer into a string, and now, when I run it, you can see that that works just fine. You can do this with almost any object, by the way. Let's try it on a floating-point number. "1415," let's do pi, and if I do this again, all right, now, you can see the result there. All right, let's keep on going, comment that out. Another interesting function is the "range" function, it creates a range of values between a starting and an ending value, along with an optional step value. Ranges are often used together with loops in order to execute the loop a specific number of times. Now, we didn't cover this back in the loops section, so I'm going to demonstrate this here, let me give you an example, suppose I wanted to run a loop a certain number of times over a certain range of numbers, I can write "for i in range," and then, I'll have it run over the range from five to 15, and then, I'll just print "i" each time through the loop, so if I run this, okay, you can see that what happens is we get the values printed out starting at five, and then, it goes up through 14, it doesn't print 15,
>
> **[6:19](https://www.linkedin.com/learning/learning-python-25309312/built-in-functions?u=76281980&t=379)** and the reason it doesn't print 15 is because the ending value is outside... It's considered to be outside of the range, it's not included in the range of these numbers, so we go from five to 14, if I wanted 15, I would just simply make this 16 instead. I can also add an optional step value. Now, normally, it increments by one each time, which you can see happening, but I can specify a value like two, and if I run this again, now, you see that the range is incrementing by two each time, so we get five, seven, nine, 11 and 13. You can use the "range" function as a way to index into a sequence of values, so let's do that with our "mystring" variable, and remember, the "mystring" variable is up here, so let's scroll down. Let's write "for i in range" from five, and we're going to combine the "len" function here, so the length of my string, and we'll step by two each time, colon, and we'll print, we'll use the "mystring" variable and we'll index into the string using whatever the value of "i" is, so let me comment that example out, so what we're going to do is, starting at index five, we're going to get each character at the given index, stepping by two along the way, so let's run that,
>
> **[7:57](https://www.linkedin.com/learning/learning-python-25309312/built-in-functions?u=76281980&t=477)** and you can see that starting off at index five, so if we go up here and count, all right, that is zero, one, two, three, four, five, yeah, that's the "u" character, which is correct, so we're getting the "u," right, and then, we step by two, so one, two, then we get the "c" character, which is correct, where you can see it right there, so it's stepping through the string by two spaces each time. The "print" function itself... This is our last example that we'll take a look at. The "print" function is pretty flexible... Let me comment out the previous example there, so you can actually embed the values of variables directly into a string using what are called interpolated strings, they use an "f" character to indicate that they have embedded data, so for example, what I'm going to do is put a letter "f" outside the string, okay, not inside the quotes, once I do that, this becomes what's called an interpolated string, which means that inside the string, I can use these brace characters, these curly braces, to refer to variables, so I'll use the "greeting" variable in here, "you are visitor number," and then, once again, inside these curly braces, I'll embed the value of the "count" variable, so here, I've got these two variables, "greeting" and "count," and this gives me a way of just embedding those values right into the string, all I've got to do is put this little letter "f"
>
> **[9:32](https://www.linkedin.com/learning/learning-python-25309312/built-in-functions?u=76281980&t=572)** outside the string, and that makes it an interpolated string, so now, if I run this, you can see that the values for those variables are being replaced inside the string. All right, so there are plenty of other built-in functions that we didn't get a chance to look at, and if you're feeling up to it, maybe take a moment here, try some of them out, chances are, you'll be using more of these functions as you develop your Python skills.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (9)
> **CLI Commands:** python (9), make (3), find (1)
> **Tools:** colab (5)
> **UI Navigation:** click on (3), scroll down (2)
> **Cross-References:** previous chapter (1), later in (1), earlier in (1)
> **Definitions:** is called (1), is a  (1), means that (1)
> **Documentation:** the documentation (2)
> **Analogies:** for example (2)

#### [Solution: Find the longest string](https://www.linkedin.com/learning/learning-python-25309312/solution-find-the-longest-string?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/solution-find-the-longest-string?u=76281980&t=0)** - [Instructor] For this challenge we needed to find the longest string in a list of strings and then return that length as the result. In order to solve this challenge your code needed to do three things. So first, you have to loop, or iterate over each of the strings in the list, that is passed to the function as a parameter. Next, you have to calculate the length of each string, and then finally, you had to return the length of the longest string. So here, in my solution, my code uses a for loop to iterate over each string and we learned about [[Forms]] back in chapter two. And they use this temporary variable named s. To calculate the length of each string I use the built-in len function which we also learned about earlier, and that returned the length of the string. My code uses this temporary variable named maxlen to hold the length of the longest string that we've seen so far which is initialized to zero. So if the length of the current string is larger than the current value of maxlen we replace the value of maxlen with the current string length and when the loop completes maxlen will contain the length of the longest string on the list and the code just returns that value. So let's go ahead and try this. You can see that this is the test code that your answer will be called with. And if we click on test my code, we can see that the correct answer is returned. Let's go ahead an try adding some more characters
>
> **[1:35](https://www.linkedin.com/learning/learning-python-25309312/solution-find-the-longest-string?u=76281980&t=95)** to the long string, so let's go ahead and add some more characters, test the code again. And now you can see that we are calculating the new longest string length. All right, so that's my solution, how does it compare to yours?

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Forms]] (1)
> **CLI Commands:** find (1)
> **UI Navigation:** click on (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Classes and objects](https://www.linkedin.com/learning/learning-python-25309312/classes-and-objects?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/classes-and-objects?u=76281980&t=0)** - [Instructor] [[Python (Programming Language)|Python]] is an [[Object-Oriented Programming (OOP)|object-oriented programming]] language. And this means that you can create and use classes to create objects that work just like the ones that are built into the language itself. Classes are a really great way of encapsulating functionality that can be kept together and reused as complete modules in other projects. If you think back to earlier in the course when we learned about functions and we learned how functions group related code together, you can think of classes as that kind of concept, but being on like steroids. You can group all kinds of functions and data together. This can be really helpful in keeping larger programs both maintainable and easier to understand. So here in VS Code, I'm going to open up classes_start. And if you're using Colab, just go ahead and create a new code cell to work in. So let's imagine that we're building an application to manage vehicles like cars and motorcycles and so on. I'm going to start by defining a class that will hold some information about vehicles. To do that, I use the class keyword and then I give my class a name, and I'll call it a Vehicle and then a colon. And you know what that colon means? That colon means we're going to start defining some code that belongs to, in this case, a class. So classes are defined using the class keyword and they're given a name. In this case, I've named it Vehicle
>
> **[1:32](https://www.linkedin.com/learning/learning-python-25309312/classes-and-objects?u=76281980&t=92)** to indicate the kind of data it will contain. Next I'm going to add a special method called the init method, and it's called the double underscore method, which is the function that Python calls on your class when the object has been created and it's time to initialize the object's data and set up its state. Now there are several different functions that have these double underscores in them. And to be clear, you don't call these directly. Python will call these functions on your class as it manages the object that you create and use. So remember, we define functions using the def keyword, and I'm going to use the two underscores and I'm going to call it init, and then two underscores again. And the init function takes two arguments. In this case, it always takes an argument called self, well you can call it anything you want, but self is kind of the, you know, accepted standard. And then I'm going to define a parameter called bodystyle, which is what my Vehicle class is going to represent. And again, I'm going to use a colon because now I'm defining the code for this function. Inside this function, I'm going to set a property which is going to be holding some data. It's like a variable and I'm going to call that bodystyle. I'm defining this property on the self object, which is a reference to this class. And I'm going to set it equal to the bodystyle parameter that my function is given, all right?
>
> **[3:07](https://www.linkedin.com/learning/learning-python-25309312/classes-and-objects?u=76281980&t=187)** So now that I've created this class, I can create other classes that inherit data and functions from this class. After all, there's many different kinds of vehicles and different kinds of engines and features. So we wouldn't want to cram all that data into just one class. What we want to do is take the things that are common to other classes, put them in a base class, and then derive other classes that inherit those properties. So let's create another class called Car. And this is going to represent regular passenger cars. Now since this class is going to be derived from the Vehicle class, then inside these parentheses I'm going to type Vehicle, which indicates that now Car automatically gets access to this bodystyle parameter. And then once again, I'm going to define my init function. And this will give the user the opportunity to define the engine type. All right, so inside my initialized function for this Car class, I first have to initialize the properties in the base class by using the super function. So what this does is this says get whatever the super class is, in this case the Vehicle, and I'm just going to call the init function on this. And the bodystyle for this Vehicle is going to be Car.
>
> **[4:44](https://www.linkedin.com/learning/learning-python-25309312/classes-and-objects?u=76281980&t=284)** Now I don't need to pass in a value for the first parameter. Python takes care of that on its own. So you can just, you don't have to worry about that. All I care about is the parameters that I've defined for my class. So now that I've initialized the base class, I can set other properties that are specific to cars. So in this case, I'll define a property called wheels and it's got four wheels. And in this case, I'll just choose to give it four doors. And I'll define a property called engine, and I'll set that to whatever the engine type is that the user passed in. So let's do the same thing for another class that will hold information about motorcycles. So I'll create a class. I'll call it Motorcycle. Once again that derive from Vehicle and I'm going to create my init function. And for the Motorcycle class, we will have two parameters. There will be an enginetype and there will be a hassidecar, hassidecar parameter, which indicates whether it has a sidecar. So once again, I'm going to initialize my parent class, my super class, and I'm going to call that with a bodystyle of Motorcycle. And if hassidecar is true, I'm going to define a property called wheels
>
> **[6:20](https://www.linkedin.com/learning/learning-python-25309312/classes-and-objects?u=76281980&t=380)** and that's going to be three wheels. Otherwise, if it doesn't have a sidecar, then wheels is going to be two. And then I'll define a property called doors, which is zero. And I'll create an enginetype, which is going to be the enginetype that gets passed in. All right, so we have three classes defined, right? We have a Motorcycle, we have a Car, and we have our base class, the Vehicle. So now that we have our classes defined, we can create specific objects of those types. So let's create some cars and a motorcycle. To do that, we use the name of the class along with parameters for the initial function. So I'll create car1, and that's going to be a Car object and the enginetype will be gas. And then I'll create car2, and that's going to be a Car and the enginetype will be electric. And then I'll create a motorcycle, I'll call it mc1. And that's going to be a Motorcycle and that's going to have a gas engine. And I'll set the sidecar to true so that it has a sidecar. So now that we've done that, we can access the properties of each of these classes by using dot notation. So for example, I can print the number of wheels that the motorcycle has by typing mc1 and then .wheels. I can do the same thing with the car. I can print car1.engine. I can print out car2's doors count, all right?
>
> **[7:58](https://www.linkedin.com/learning/learning-python-25309312/classes-and-objects?u=76281980&t=478)** So let's go ahead and run what we have. And you can see sure enough that this motorcycle has three wheels 'cause it has a sidecar. The first car is a gas engine, that's correct. And car2 has four doors. But classes don't just hold data. They also define their own functions that operate on that data. So let's add the ability for each of these vehicles to drive. So in my base class, the Vehicle class, I'm going to define a function called drive. And once again, it always takes the self parameter. And then I'm going to specify the speed that we're driving. And I'll define a new property called mode, and now I'll be in driving mode. And then we'll set the speed property on the object to speed. And then I can have each vehicle specify some driving behavior by doing something called overriding. So what I'm going to do is in the Car class, I'll define the drive method. Now the Car class, because it inherits from Vehicle, it already has a drive function. But I'm going to customize the the behavior by overriding the base classes version. So in this version, once again, I'll have self and speed and I'm going to call the super classes drive method with my speed argument.
>
> **[9:33](https://www.linkedin.com/learning/learning-python-25309312/classes-and-objects?u=76281980&t=573)** And then I'll just put in whatever my own custom behavior for cars is. So I'll print, you know, Driving my, and then I'll print out the enginetype, Car at, and then I'll print out the speed, all right? And then for the Motorcycle, and let's do the same thing. We'll just copy and paste this and we'll put it below there. And in this case, rather than car, it's going to be motorcycle, okay? And then let's call those functions. So I'll call car1.drive and we'll drive at 30, and then car2 is going to drive at 40, and then motorcycle one is going to drive at 50. All right, so let's run the code again. Oh, whoops. Looks like there's no engine type for- Oh, that's because I called it enginetype. So let me fix that. Motorcycles have an enginetype. So let's go ahead and run that. All right, there we go. Now you can see that when I call my drive function on each of these objects, the drive method gets called on the Car and the Motorcycle. So for the Car,
>
> **[11:05](https://www.linkedin.com/learning/learning-python-25309312/classes-and-objects?u=76281980&t=665)** I've got driving my gas and electric; and driving my gas motorcycle at and then the speeds, okay? Now this is a very simple example of how to define classes in Python. And since this is an introductory course, I'm going to just kind of stop here. If you're interested in learning more about object-oriented programming in Python, I would check out the Python Object Oriented Programming course here in the library. It goes into much more detail on how classes work. And by the way, you can get through Python without defining your own classes. I wanted to just show this to you because it is an interesting subject and it is part of Python. In the meantime, think about how you might extend this example. So how would you add the ability to have different kinds of cars? How would you add additional functions like park or reverse? Take some time here to stop and experiment with the code and try out some of those ideas.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (9), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (1)
> **CLI Commands:** python (9)
> **Definitions:** is an  (3), is a  (3), means that (1)
> **Analogies:** just like (1), imagine (1), it's like (1), for example (1)
> **Tools:** vs code (1), colab (1)
> **Code Identifiers:** classes_start (1)
> **Cross-References:** earlier in (1)
> **Prerequisites:** set up (1)

#### [Importing and using modules](https://www.linkedin.com/learning/learning-python-25309312/importing-and-using-modules?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/importing-and-using-modules?u=76281980&t=0)** - [Speaker] One of the most powerful features of the [[Python (Programming Language)|Python]] ecosystem is the large number of library modules of prebuilt code that you can use in your programs. The standard Python installation contains quite a few of these modules, some of which we will learn about later in the course. But before we get there, let's take a look at a simple example that shows how to take advantage of all this code that you don't have to write yourself. So let's open up the modules, start file in our editor, and, of course, in Colab, just create a new code cell for yourself. So here in the Python documentation, let's take a look at the standard library again. So if I scroll down a little bit, okay, here in this section labeled, numeric and mathematical modules, there's a link for the math module. So let's click on that. And that takes me to the math module documentation. And you can see here that there's a lot of math-related functions that I can use in my Python programs. So what I'm going to do is tell Python that my program wants to use the math module. So let's go back to the code. To do that, I need to use the import statement along with the name of the module that I want to use, in this case, it's called math. So once I've done that, suppose I wanted to calculate the square root of a number. Well, if we go back over here into the math module and we scroll down a little bit, okay, here we go.
>
> **[1:35](https://www.linkedin.com/learning/learning-python-25309312/importing-and-using-modules?u=76281980&t=95)** We can see that there's a function for the square root of a number. So what I'm going to do is take advantage of that code. I'm going to write print The square root of 16 is,
>
> **[1:56](https://www.linkedin.com/learning/learning-python-25309312/importing-and-using-modules?u=76281980&t=116)** and now that I've imported the math module, I can write math. And then when I hit dot, you can see that there's a whole bunch of pre-built functions in here. What I'm going to do is use the sqrt function and pass in 16. Alright, so let's go ahead and try running that. So let's run this in the terminal. And you can see the square root of 16 is four. Let's go ahead and copy this over to Colab and let's try running it. And sure enough, the square root of 16 is four. Alright, so now I've imported the math module and I can use it to functions. However, I can also choose to import only specific parts of a module that I want to work with. So for example, if you go back to the math module, you'll see that there are some constants defined down here like pi, and E, and so on. I can import a constant like pi directly. What I'm going to do is right from math import pie. Now I don't need to type the math prefix anymore. If I want to use pi, I can just refer to pi itself. So I'll just write print pi is and then pi. And let's go ahead and comment that one out. Let's run this in the terminal. And sure enough, we can see that pi is, well,
>
> **[3:29](https://www.linkedin.com/learning/learning-python-25309312/importing-and-using-modules?u=76281980&t=209)** the big long value of pi. Alright, let's keep on going. I can also import modules and give them a shorthand name to make them easier to work with. So for example, there's a module called random, which lets me generate random numbers for a variety of reasons. And you can see all the code that's in here. This is the documentation for it. So if I want to work with random numbers, what I'm going to do is import random and then I'll type as r.
>
> **[4:03](https://www.linkedin.com/learning/learning-python-25309312/importing-and-using-modules?u=76281980&t=243)** So now, instead of typing random dot every time I want to use one of the methods or functions inside random, I just need to use this shorthand name that I've given it. So for example, I can now print r. and then I'll call the randint function and I'm going to generate a random integer between 100 and 200. And let's comment that out. Alright, let's run that. And you can see that each time I run it, right, I'm getting a different random number between 100 and 200. And let's go ahead and try that in Colab. So I'll make a new code cell, paste that code in, and let's run it. And you can see each time I run it, the number is different. Alright, so far, so good. Now in addition to the standard library, there are some really good third-party modules that perform all kinds of functions. So for example, let's use the tabulate module. And the tabulate module is a third-party project that prints out tabulated data in a nice format. So you can see here if I scroll down a little bit, there's a whole bunch of like table formats we can use and you can see the different kinds of table formatting for printing out data. But let's go ahead and make use of it in our program. So in my code, what I'm going to do is install it first.
>
> **[5:38](https://www.linkedin.com/learning/learning-python-25309312/importing-and-using-modules?u=76281980&t=338)** Now, you don't need to do this if using Colab because tabulate is already installed for you along with lots of other modules. But here in Visual Studio Code, I'm going to open up my terminal and I'm going to install it using the Python package manager, which is called pip. So I'm going to type pip install tabulate and it's already there. So, it's been installed for me. This is probably, if you're doing this the first time, you'll see that it gets installed. So once it's installed, now we can make use of it and refer to it. So what I'm going to do is in my code, you can see that I already have some sample data here. So I have a list of various product information. There's a header along with some [[Microsoft Products|products]] and prices and quantity. So first, I'm going to write from tabulate import tabulate. Now this may seem a little confusing. It turns out that the class name and the module name are the same. So the same way that the math module contains a, you know, various functions and so on that aren't called math. In the case of tabulate, the actual class is also called tabulate. That's why I'm doing this. So what I'm going to do is create a formatted table. So I'll print the result of the tabulate function and the tabulate function takes the data. And then I'm going to specify that there are headers. So the headers is equal to the first row. And then the table format, I'm going to specify
>
> **[7:15](https://www.linkedin.com/learning/learning-python-25309312/importing-and-using-modules?u=76281980&t=435)** as fancy_grid. Alright, and let's comment out our previous example. Alright, let's go ahead and run our code. And you can see that I'm using the tabulate function to print out this nicely formatted table. Let's go ahead and copy this and let's try it in Colab. So, I'll make a new cell. All right, and let's run this. And you can see I'm getting the same result here. If we look at the documentation again and scroll down, you can see that there's plenty of table formats. So I used fancy_grid. Let's try something else. Let's try double_outline. I'll copy that and let's paste that in here. Alright, so in the [[Representational State Transfer (REST)|rest]] of the course, we're going to be using some other great modules to perform useful work, such as manipulating dates and times and working with files and even processing internet data like XML and [[JSON]]. But before moving on though, I would suggest maybe taking a few moments and trying out some of the other math functions, or maybe try out some of the other features of the tabulate module just to get a feel for what it's like to work with modules. And again, you can find the tabulate documentation at this link and you can find the random and math modules at these links. So take some time here, maybe practice some of these functions before moving on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (6), [[Microsoft Products|Products]] (1), [[Representational State Transfer (REST)|Rest]] (1), [[JSON]] (1)
> **CLI Commands:** python (6), make (5), pip (2), find (2)
> **Tools:** colab (5), terminal (3), visual studio (1)
> **Analogies:** for example (4), such as (1), it's like (1)
> **UI Navigation:** scroll down (4), click on (1)
> **Code Identifiers:** fancy_grid (2), double_outline (1)
> **Cross-References:** go back to (2), later in (1)
> **Prerequisites:** install (3)

#### [Using exceptions](https://www.linkedin.com/learning/learning-python-25309312/using-exceptions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/using-exceptions?u=76281980&t=0)** - [Instructor] Programs run into problems and errors all the time, so it's a good practice to have a plan for handling them when they happen. And in [[Python (Programming Language)|Python]], this is achieved through a mechanism called exception handling. And this is not unique to Python. Many other languages have something like this. So let's look at how it works. I'm going to open up the exceptions_start file, and in CoLab, if you're using CoLab, just make a new code cell. So here in my editor, I'm going to start off by writing some code that is definitely going to cause an error. So I'm going to make a variable called x and I'll assign it to the result of 10 divided by zero. Now that's going to generate an error because obviously dividing by zero is a math error, and when I try to run this, right, sure enough, you can see that we get this zero division error, and it says division by zero, and it shows me the line of code where this happened. And if I do the same thing in CoLab, right, I'm going to get this complaint. Oh, whoops, ZeroDivisionError. So this is a pretty bad experience for users of our program and we can do a much better job of handling errors like this in our code by using exception handling. So let's rewrite this by using exception handling code. So exception handling consists of two main parts. There's the code that I want to run, and then there's the code
>
> **[1:32](https://www.linkedin.com/learning/learning-python-25309312/using-exceptions?u=76281980&t=92)** that handles any problems that might happen. What I'm going to do is I'm going to write the [[Microsoft Word|word]] try and then colon, which means I'm starting a new code block. So I'm going to try to divide these two numbers, then I'm going to use the word except, and then inside the except code block, I'm going to print, "Well that didn't work" and let's go ahead and comment out this example here. Alright, so within the try section is where I write my code that might cause a problem. It's not going to definitely cause a problem, but it might. Python will execute the code as usual, and if anything goes wrong, then the flow of the program immediately transfers to the except section where I can handle the error much more gracefully than having the program just crash. So in this case, the divide by zero error will cause the program to just jump to this print statement. So if I run this with the updated code, you can see that, sure enough, rather than getting that ugly stack trace and crash, we get this message that says, "Well that didn't work." So this is a better experience, but we can improve on this. First, it's not a good practice to just use the word except by itself. You should write except sections that handle specific errors. So let's go ahead and rewrite this in a different part of the code here.
>
> **[3:05](https://www.linkedin.com/learning/learning-python-25309312/using-exceptions?u=76281980&t=185)** So what I'm going to do do is I'm going to have a try section, and I'm going to define a variable called answer, and I'm going to ask the user "What should I divide 10 by?" and then I'm going to declare a variable named num and I'm going to use the int function to convert that string into an in integer number, and then I'll print 10 divided by num. All right, now in this case, the user might enter a zero, which would cause a divide by zero exception, or they might enter some input that isn't a number, which will cause this integer conversion function to fail with a value error exception. So to handle both of these separately, I'm going to use multiple except sections. So the first one will handle the zero division. So I'll write except, and then I'm going to specify the exact name of the exception I want to handle, so this is going to be a ZeroDivisionError as e. I'll just capture that as a variable and I'm not going to use it, but you can use the properties inside the exception variable if you want to. And I'm going to print out, "You can't divide by zero" and then I'm going to have another except section, and this one is going to catch the value error also as e, even though I'm not going to use it, and I'm going to print out
>
> **[4:37](https://www.linkedin.com/learning/learning-python-25309312/using-exceptions?u=76281980&t=277)** "You didn't give me a valid number." And actually you, know what, I changed... I'm actually going to print out e. Let's just see what it looks like. Now there's one more section we can add, and that's called a finally section. This section contains code that will always execute regardless of whether an error occurred, and you can use it to clean up any resources that you've allocated, such as closing any open files that you have or any other system resources that you're using in case anything goes wrong. So I want to just demonstrate that it works. So I'll just simply print "Finally always runs." Alright, so let's go ahead and comment out our previous example, control slash or command slash on the Mac. All right, and then let's run this. Okay, so now my code is running, and it's asking me what should I divide 10 by? Let's enter a real number like two. Alright, so we can see that that works, and the answer of 10 divided by two is in fact five, and you can see that the finally section always runs. Let's run it again. Okay, what should I divide 10 by? Let's enter zero, and you can see now that my ZeroDivisionError exception handle is being triggered. All right, and then finally, let's run it one more time. What should I divide 10 by? Let's enter some gibberish. This is not a number, so I'll print in, I'll put in abc, and it says,
>
> **[6:11](https://www.linkedin.com/learning/learning-python-25309312/using-exceptions?u=76281980&t=371)** now I'm getting my, "You didn't give me a valid number." That's right here, okay? Then I'm printing out the actual exception, and we can see that the exception is invalid literal for int with base 10. So I gave it abc, and this is just a very technical way of saying, "Hey, I couldn't convert abc into a number," and sure enough, "Finally always runs." For those of you running CoLab, let's just go ahead and try that out. I'll copy this, and let's paste it in over here. Let's make a new code cell for this. All right, and let's run this. So let's do two. That works. Zero, right? That causes an exception, and then abc, and you can see the same results. So exceptions make error handling a bit easier by centralizing the code for dealing with those errors in one place and separating it out from the code that actually does the program's work.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (3), [[Microsoft Word|Word]] (3)
> **CLI Commands:** make (4), python (3)
> **Tools:** colab (4)
> **Definitions:** is a  (3)
> **Best Practices:** good practice (2)
> **Code Identifiers:** exceptions_start (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Palindromes](https://www.linkedin.com/learning/learning-python-25309312/solution-palindromes?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/learning-python-25309312/solution-palindromes?u=76281980&t=5)** - [Instructor] All right, this one was a little bit of a tricky one. So let's take a look at my code for this challenge. So in order to determine whether a string is a palindrome, we have to ignore capitalization and we have to also get rid of spaces and punctuation. My code starts by converting the incoming string to lowercase, so we don't have to worry about comparing different letter casing. And this was one of the hints that I provided in the hint section. You could also convert the string to all uppercase as well. It doesn't really matter. Next, I strip off all the spaces and punctuation from the incoming string. So I create a new string, and then I have a for loop that checks each character in the test string. Now I'm using the isalnum function, which is a built-in function for [[Python (Programming Language)|Python]] string objects. And it checks to see if any given character is alphanumeric. In other words, it's A through Z or zero through nine. And again, this was another one of the hints that I gave you in the challenge. So if it is, then I add that character to my new string, otherwise I skip over it. So when this loop completes, this new string will have only the lowercase letters and numbers in it. So it'll have all the punctuation and spaces stripped out. Next, I have to build the reverse version of the string to compare against the original,
>
> **[1:38](https://www.linkedin.com/learning/learning-python-25309312/solution-palindromes?u=76281980&t=98)** because if they're the same, then we know that it's a palindrome. Now, the way that I've solved this here is not necessarily the most efficient way of doing it, but since this is a learning course, I wanted to focus more on understanding the algorithm than being as clever as possible with my code. So I have a variable named reversestr and I have a loop that starts at the end of the test string and works its way backward. So I'm just going to take each character as I come across it and build the reverse string. Now, you could have done this in a much more clever way. You could have done something like reversestr is equal to, let's see, teststr. And you could have used the slicing syntax like this to make the reverse string. That's a lot more clever, but I was going for readability here. So once I have the reverse string, then I can compare it to the original string. Well, I mean not the original string, the new string, the one that I've taken the test string and converted, taken all the space and everything. So if the string with all the punctuation and spacing and such stripped out is equal to my reverse string, well then I know we have a palindrome and I return true, otherwise I return false. So let's go ahead and take a look at the test code. And we've got some strings here that we can test out. So let's test out Madam, I'm Adam, and you can see that it worked. All right, let's go ahead and comment that one out. Let's try some of these other words, let's try race car.
>
> **[3:13](https://www.linkedin.com/learning/learning-python-25309312/solution-palindromes?u=76281980&t=193)** Yep, seems to work, that's good. Let's see, let's try this one here, the a man, a plan, a canal Panama. Yep, sure enough, it works. Okay, well that's my solution. Take a look at my code, compare it to yours, and see where you've got some similarities and differences.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (1)
> **Definitions:** is a  (3), in other words (1)
> **CLI Commands:** python (1), make (1)
> **Speakers:** - [instructor] (1)


### 4. Welcome to Part 2

[↑ Back to Table of Contents](#table-of-contents)

#### [Applying what you've learned](https://www.linkedin.com/learning/learning-python-25309312/applying-what-we-ve-learned?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/applying-what-we-ve-learned?u=76281980&t=0)** - Welcome to Part 2 of the course. Up until now, we've been learning about the basics of the [[Python (Programming Language)|Python]] language and many of its features. We've seen how to use variables, write functions, make use of modules in the standard library, and a lot more. You've now learned enough Python to start writing some real programs that solve real scenarios. In the next few chapters, we'll see how to use Python to work with dates and times, operate on files using the file system, and we'll even see how to retrieve data from the internet and work with data formats like XML and [[JSON]], two of the most common data formats on the web. Let's go, it's time to get started on the fun part.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (3), [[JSON]] (1)
> **CLI Commands:** python (3), make (1)
> **Env Vars:** xml (1), json (1)
> **Cross-References:** in the next (1)
> **Speakers:** - welcome (1)


### 5. Using Dates and Times

[↑ Back to Table of Contents](#table-of-contents)

#### [The date, time, and datetime classes](https://www.linkedin.com/learning/learning-python-25309312/the-date-time-and-datetime-classes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/the-date-time-and-datetime-classes?u=76281980&t=0)** - [Instructor] Just about every programmer has to work with dates and times at some point. In this chapter, we're going to start by learning about some of the [[Python (Programming Language)|Python]] classes that are available for this purpose. So let's open up in chapter five, the Dates Start file and those of you using CoLab can just create a new code cell. So in order to get this rich date and time functionality, we of course have to import the necessary modules and classes from the standard library, and we saw how to do that earlier in the course. So here you can see I've got the from date time import date, and from datetime import datetime classes. So let's go ahead and copy and paste these into our CoLab starting point. So I'm going to make use of the date and datetime classes in this example, both of which are part of the Python standard library. Now, the second import here might be a little confusing. There's actually a class named datetime inside the module named datetime. So to be clear, that's what I'm importing here. I'm importing the datetime class from the datetime module, which happens to have the same name. Let's go ahead and exercise some of the features of these classes. First, let's do something simple. Let's just print out today's date. So I'll make a variable called today and on the date class, I'll call the today function. And then I'm going to print out today's date is,
>
> **[1:35](https://www.linkedin.com/learning/learning-python-25309312/the-date-time-and-datetime-classes?u=76281980&t=95)** and then today. So the today function returns information about the current date and I'm just going to print it out, so let's run that. And sure enough, we can see today's date printed right here. Let's try the same thing in CoLab. So let's copy and paste that. Alright, and we can see that the current date is being printed, so far so good. So for the next example, let's print out the individual components of a particular date object. So remember a little bit earlier in the course, we learned about classes and objects and how different objects can have properties and functions on them. And the date and time classes have various properties that we can make use of. So let's print out the date components and I'm going to print out today.day, today.month, and today.year.
>
> **[2:33](https://www.linkedin.com/learning/learning-python-25309312/the-date-time-and-datetime-classes?u=76281980&t=153)** So I can get the individual day, month, and year among the other values. So let's go ahead and try this out. And we can see that the dates components are, here's the day, here's the month, and here's the year. So the date class also provides some useful properties I can use in other more advanced features of an application. So for example, I can retrieve what's called the weekday number. So let's try that out. And I'm going to just go ahead and comment out these previous examples just to avoid cluttering up the output. So every weekday has a number assigned to it. Zero is Monday up through six, which is Sunday. So I'm going to print today's weekday number, and that's going to be today.weekday. And oops, I don't want to comment out the actual variable that gets my date. Alright, there we go. So if I had some list that I wanted to provide an index variable for that list, which depended on the weekday, I could use the weekday property to index into a list of say abbreviated names. So for example, if I had something like days equals and I'm going to make a list and that's going to have Monday, Tuesday, Wednesday, all the way up through the end of the week.
>
> **[4:14](https://www.linkedin.com/learning/learning-python-25309312/the-date-time-and-datetime-classes?u=76281980&t=254)** Then I can do something like this. I can write, which is a, and then I'm going to print days indexed by today.weekday.
>
> **[4:30](https://www.linkedin.com/learning/learning-python-25309312/the-date-time-and-datetime-classes?u=76281980&t=270)** So this will give me a number zero through six, which will correspond to an index into this list. Let's go ahead and run that. And you can see that today's weekday number is two, so that's a Wednesday. So it's zero, one and two. And you can see that sure enough, that's the result that we're getting. Alright, let's turn our attention to datetime objects. So just like working with dates, I can get time values as well. So using the datetime class instead of the date class, I can now call the now function, and that will gimme the current date as well as the time. So I'll write today = datetime.now, and then let's print out the result of that. So the current date and time is, and we'll print out today and let's go ahead and comment these out. All right, and we can see that now in the output, I have the current date and the current time. So here's the date and here's the current time down to the nearest millisecond, it's pretty accurate. To get the current time, we can use the time portion of the datetime object, and we do that with the time function. So let's try that out. Let's make a variable called T. And I'm going to call on the datetime class. I'm going to call the time function.
>
> **[6:03](https://www.linkedin.com/learning/learning-python-25309312/the-date-time-and-datetime-classes?u=76281980&t=363)** And the argument to that function is the datetime object. So I'll give it datetime.now, and then I'll print out the current time is and then T. All right, so let's go ahead and comment that out and run our updated code. And now we can see I have just the time instead of the date and time. So now that we know the basics of how to retrieve the current date and time, we can use these values in some more advanced scenarios, which we will see as we go through the [[Representational State Transfer (REST)|rest]] of the chapter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (2), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** make (5), python (2)
> **Tools:** colab (3)
> **Analogies:** for example (2), just like (1)
> **Cross-References:** earlier in (2)
> **Speakers:** - [instructor] (1)

#### [Formatting time output](https://www.linkedin.com/learning/learning-python-25309312/formatting-time-output?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/formatting-time-output?u=76281980&t=0)** - [Instructor] All right, now that we've seen how to retrieve basic date and time information in [[Python (Programming Language)|Python]], let's take a look at how to format that information using a set of predefined formatting codes. So in my editor, I'm going to open up the formatting_start example, and here in my example, I've already imported the datetime class. So if you're using CoLab, create a new cell and just go ahead and paste that in. So Python provides a standard set of string formatting codes that you can use in a variety of scenarios. And I'll show the docs for this in a moment. Essentially, the way that it works is each formatting code is a percent sign followed by a letter, and that letter means something in the output string. So let's exercise a few examples and then I'll show you the documentation and a website you can use to try out different formats. So let's start out with a familiar piece of code to get the current date and time we saw in the previous video. So I'm going to have a variable named now and I'm going to call datetime.now. So now that I have the current date and time, I can use some of these formatting codes to nicely format this date and time information however I want it. So the way that this works is to format date and time information into a string, you use what's called the strftime function, and that's available as a function on the datetime object that we get back from this now function. So the way that this works is I'm going to print now and I'm going to call strftime.
>
> **[1:38](https://www.linkedin.com/learning/learning-python-25309312/formatting-time-output?u=76281980&t=98)** And then we're going to give it a string. And that string is going to contain whatever output I want along with these formatting codes. So let me give you a really simple example. Each of these codes serves as a placeholder for the date and time data. So for example, if I wanted to format a string with the full year number, I'd write something like this, I'd write The current year is and then I would type percent. And in this case I would use an uppercase Y, which basically means give me the current year with the four digit century included. So if I run this example really quick, you can see that that's exactly what happens. So the formatting code gets replaced by that piece of the date and time information. So let's try something a little bit more fancy and let's use several codes in the string all at once. So I'm going to write print, once again, now.strftime, and in this case, I'm going to just use codes. So I'll percent, let's see, %a, %d %B, %y.
>
> **[2:58](https://www.linkedin.com/learning/learning-python-25309312/formatting-time-output?u=76281980&t=178)** All right. So this formatting string will give me the, let's see, it's going to be the abbreviated day name followed by the day of the month, then the full month name. And then in the case of the lowercase Y, that's the two digit abbreviated year. All right, so let's try that and let's see what happens. And sure enough, you can see here it is, it's Thursday 16th of January of 25. Python also gives you a way of printing out specific locale information. Before we take a look at that, however, let me quickly show you what the docs are. So if you look inside the Python documentation, this link here will take you right to the list of formatting codes for the strftime function. And in typical, you know, Python docs fashion, it's kind of complex and nerdy, and hard to read and so on. I actually like to use this reference instead. And you can see that this link, it's strfti.me. So they took the function name and just made a .me domain name out of it. And this is much easier to read. You can see an explanation of all the different codes, and you can see how different examples use it. There's a nice little edit box here that you can try out various different formatting codes to see how they work. So this is much preferred, I think, over the built in documentation. So either one of these resources you can try out, and you can see the different codes to put in the string as the placeholder for the formatting of the date and time.
>
> **[4:33](https://www.linkedin.com/learning/learning-python-25309312/formatting-time-output?u=76281980&t=273)** All right, so let's go back and take a look at the locale information. So rather than having to manually figure out the specific locale that your application is being run in and how to print out information using the current localized versions of date and times, there's actually a set of codes that do that for you. So let me enter a few of those and let me comment out the previous example. So what I'm going to do is print out now.strftime. And so the Locale date and time is going to be %c.
>
> **[5:15](https://www.linkedin.com/learning/learning-python-25309312/formatting-time-output?u=76281980&t=315)** And I can get a little bit more granular than that, I can write, for this version, I'll write the locale. So just the date is %x, and then I'll copy and paste that line. And then uppercase %X is the locale time. All right, so let's give that a try. I'm going to run that. All right. And we can see the locale information here. So you can see it's printing out the date and time information appropriately for how I have the locale set up on my computer. Now, if I was running this on a computer in say Europe, then the information you see printed here would probably look a little bit different based upon what that locale settings are. So here I've got, you know, the month first and then the day second. But other countries put the day and then the month. So these control codes, %c, right, lowercase and uppercase %X allow you to use whatever the current locale's appropriate formatting is for dates and times. All right, let's finish up this example with a look at some formatting for time. So just the same way that you can print formatted date information, you can print formatted time information as well. So let's have a couple of examples of that. I'm going to use the strftime, and I'll write the current time is,
>
> **[6:51](https://www.linkedin.com/learning/learning-python-25309312/formatting-time-output?u=76281980&t=411)** so I'll use, let's see, %I:%M:%S.
>
> **[7:03](https://www.linkedin.com/learning/learning-python-25309312/formatting-time-output?u=76281980&t=423)** So that's hours, minutes, and seconds. And then I'll have a space with a %p, which means AM or PM. And you can see that %I and H is 12 versus 24 hour clock. So let's try that one as well. We can try copying, I'll paste this one, and instead of %I, I'll use %H and M and I'll just leave off the seconds, and AM and PM. And let's go ahead and comment out the previous example. Let's go ahead and run this. And you can see that the current time is 12:17:15 AM, and the current time, so you can see that the 24 hour clock and the 12 hour clock, these are the values here. So now if it was afternoon, it would print a 24 hour based value, right? So 13 for one o'clock for example, and that's for the %H case. So if you find yourself needing to print formatted date and time data in a super customized way, Python provides some pretty rich controls for accomplishing this scenario. And of course, you know, you don't need to keep all these codes in your head. Like I said, there's some great documentation on this. So check out the docs and, you know, try out some examples here at this link.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (6)
> **CLI Commands:** python (6), find (1)
> **Documentation:** the docs (3), the documentation (1)
> **Ports:** :17 (1), :15 (1)
> **Definitions:** is a  (1), basically means (1)
> **Analogies:** for example (2)
> **Code Identifiers:** formatting_start (1)
> **Cross-References:** previous video (1)

#### [Using timedelta objects](https://www.linkedin.com/learning/learning-python-25309312/using-timedelta-objects?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/using-timedelta-objects?u=76281980&t=0)** - [Instructor] A common scenario that pops up when working with dates and times involves performing mathematical operations on the dates and times themselves. So for example, given a particular date, you might want to calculate a date in the future or the past relative to that date or the time difference between two dates. And we can use the timedelta class in [[Python (Programming Language)|Python]] to help us with this. And that's what we're going to look at in this example. I've also provided a link to the documentation for timedelta objects right here. You can go look at that on your own time. I'm not going to make you read through the docs. Let's just go ahead and learn by example because that's usually a lot better. So I'm going to open up the timedeltas start example, and you can see that I've imported some packages I'm going to be using. So we've already seen the date and datetime modules. You can see I'm also importing the timedelta class from datetime. Let's go ahead and copy these and let's paste them into a code cell in Colab for those of you using Colab. So a timedelta is basically a span of time. It's not a particular date or a particular time. It's an amount of time. And you can use this class to perform time-based mathematics. So let's take a look at some examples. To construct a basic timedelta, all you need to do is create the timedelta class and pass in the amount of time that you want the Delta to represent. So for example, I'm going to create a timedelta, and the parameters I'm going to give it is,
>
> **[1:36](https://www.linkedin.com/learning/learning-python-25309312/using-timedelta-objects?u=76281980&t=96)** I'm going to say days equals 365 hours is going to be 5 and minutes is going to be 1. So in this case, I'm creating a timedelta, a span of time that represents 365 days, 5 hours and 1 minute. And I'm going to print that out. So let's just go ahead and run this before we do anything else. All right, and you can see 365 days, 5 hours in 1 minute. Let's go ahead and copy. Let's paste this into Colab. Okay, we get the same result. That's great. So let's use today's date as a reference point for the next few operations. And we've already seen how to do that, right? We, I make a variable called now and we already know how to use datetime.now to get the current date and time. So let's go ahead and print today's date. So today is now. All right, so we've got today's date and time. Let's use a timedelta to figure out what today's date will be one year from now. So let's print out in one year it will be. And then I'm going to take now and I'm going to add a timedelta and the days parameter I'm going to set to 365. All right, let's go ahead and try that and let's comment out this first example right there. Okay, let's try running this.
>
> **[3:09](https://www.linkedin.com/learning/learning-python-25309312/using-timedelta-objects?u=76281980&t=189)** All right, so today it's January 16th. In one year it's going to be again January 16th. But you can see that the year has incremented from 25 to 26. Okay, so far so good. Let's keep on going. And in fact, you know what? Let's copy and paste this into Colab. Yep, okay, same result. That's great. Let's try using a timedelta with more than one argument. So I'll write print, in 2 weeks and 3 days it will be, and I'll write now plus timedelta weeks equals 2 and days equals 3. So in this case, I'm constructing a timedelta of 2 weeks and 3 days. Let's try that. All right, so in 2 weeks and 3 days we see what the next date is going to be. All right, so now let's try a calculation in the past and use what we've already learned to format that result as a string. So if you didn't watch the previous video on formatting time output, you should go watch that now to understand what I'm about to do, I'm going to create a variable called T and I'm going to take datetime.now and subtract off a timedelta of 1 week.
>
> **[4:45](https://www.linkedin.com/learning/learning-python-25309312/using-timedelta-objects?u=76281980&t=285)** And then I'm going to create a variable called S. And on that time object, I'm going to call the strftime function. And we learned about this in the previous video. So I'm going to format that time using these formatting codes and you can go watch that video to learn what these codes mean. And I'll print, one week ago it was, and then that string. Okay, let's go ahead and comment out the previous examples. Of course, if you're in Colab, you don't have to do that, just make a new code cell. All right, let's run this. All right? And we can see that one week ago, it was January 9th, all right, seems to have worked. And you can see the formatting codes here, right? So I get the full day name, the full month name, the 2 digit day, and the full year. So by using a timedelta, I calculated times both in the future and in the past. Okay, let's take a look at one more interesting example. Let's comment that out. Let's write a script that's going to calculate how many days it is until the next April Fools' Day. So I'm going to write today equals date.today. So all I care about is the date. I don't want datetime, just the date. Next I'll create a new date that represents April Fools' Day for the current year.
>
> **[6:17](https://www.linkedin.com/learning/learning-python-25309312/using-timedelta-objects?u=76281980&t=377)** So I'll make a variable called afd for April Fools' Day, I'll make a new date object and I'll use today's year property. And April Fools' is on April. That's the fourth month and the first day. So now I'll compare these 2 dates to see if April Fools' Day has already gone by. Now it's January, so it hasn't gone by, but if it has gone by then I need to get the next one. So I'll write if afd. So if April Fools' Day is less than today, I'm going to print and I'm going to use a formatting string for this. So I'll use the F character. I'll write April Fools' Day already went by. And then I'm going to use my formatting here in my interpolated string. I'm going to write today minus afd.days and then days ago.
>
> **[7:20](https://www.linkedin.com/learning/learning-python-25309312/using-timedelta-objects?u=76281980&t=440)** And then what I'll need to do is in my April Fools' Day variable, I'm going to use the replace function to replace the year with today's year plus 1 because if it's already gone by, I've got to get the date for next April Fools' Day. Now I just need to subtract the April Fools' Day date from today to create a timedelta result. So I'll make a variable called time_to_afd equals afd minus today. And then I'll print out and I'll use my interpolating string for this. It's just, and then inside the braces, I'll write time_to_afd.days, and then days until April Fools' Day. All right, so let's go ahead and run this. All right? And since it's January and April Fools' Day has not gone by yet, it's just 75 days until April Fools' Day. But we can, if you want, you can create a fake date representing today and we've seen how to do that. So let's go ahead and change today. I'll just copy and paste this. So rather than making today date.today, I'll just create a new date and I'll have today.year
>
> **[8:54](https://www.linkedin.com/learning/learning-python-25309312/using-timedelta-objects?u=76281980&t=534)** and let's pretend it's November 10th now let's run this again and oh, whoops. All right, there we go. Let's run this again. All right, now we can see that when we're pretending that it's already November, you can see it says April Fools' Day already when by, already when by, (laughs) went by. And might as well fix that as well. April Fools' Day already went by. You know what, let's run it again to get the cleaner output. There we go. This April Fools' Day already went by 223 days ago. It's just 142 days until the next April Fools' Day, all right? So by using timedeltas, you can perform some pretty complex date calculations.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (1)
> **CLI Commands:** make (6), python (1)
> **Tools:** colab (5)
> **Code Identifiers:** time_to_afd (2)
> **Cross-References:** previous video (2)
> **Documentation:** the documentation (1), the docs (1)
> **Analogies:** for example (2)
> **Speakers:** - [instructor] (1)

#### [Working with calendars](https://www.linkedin.com/learning/learning-python-25309312/working-with-calendars?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/working-with-calendars?u=76281980&t=0)** - [Instructor] Let's wrap up this chapter by taking a look at a few examples of how to work with calendars in [[Python (Programming Language)|Python]]. The Python Standard Library provides a couple of useful utilities for working with calendars in both text and [[HTML]] formats. So let's open up the calendars start file, and in co-lab, create an empty code cell. And you can see that I've already imported the calendar module, so let's copy and paste that into co-lab, there we go. So now that I've imported the calendar module, I've got access to all of the classes. Let's start off by creating a plain text calendar. So to do that I'm going to use the text calendar class. So I'll make a variable named C and then using the calendar module, I'll create a text calendar and I want the first weekday to be calendar dot Sunday. And then I'll make a string, actually, let's call it the stir equals and then I'm going to write C dot format month. And then I'm going to just choose a specific month that I want to format. So I'll choose 2026, January, and then I'll just pass in zeros for the next two arguments. Don't worry about those. And then I'm going to print out the str. Alright, the format month method lets me format a particular month into a text string,
>
> **[1:33](https://www.linkedin.com/learning/learning-python-25309312/working-with-calendars?u=76281980&t=93)** and then I'm just printing the results. So let's go ahead and run this. And what I do, you can see in the output that I get this nicely formatted January 2026 calendar. Right, Sunday is the first day and looks like the month starts on a Thursday. Lets change this parameter here to calendar dot Monday instead and let's run. And now you can see that the calendar starts on Monday instead and ends up with Saturday and Sunday. Alright, now let's try the same thing, but now let's use HTML. So I'll create an HTML calendar and I'll use the calendar module. I'll create an HTML calendar, and once again, I'll use Sunday as the first day. I'll set the str. I'll call HC dot format month. Once again, I'll choose January of 2026. And let's print out the str. And let's comment out the previous example. All right, let's run. So now you can see I'm doing the same thing only in this case it's, it's HTML and it's text here, so you can't really read it. But if you were to take this HTML and paste it into an HTML editing program, you would see that you'd have this nicely formatted table. So if you have a need to generate HTML calendars, if you look into this, you can kind of see the data, right?
>
> **[3:07](https://www.linkedin.com/learning/learning-python-25309312/working-with-calendars?u=76281980&t=187)** Let's see if I scroll up, right? You can see that the month is January 2026 and so on. But anyway, this is how you can create some HTML if you need a quick way to generate a calendar. You can also use the calendar class to perform some common operations on dates. So for example, I might have a need to iterate over the days of any given month. So let's try that instead. It's a little bit more useful. So let's go ahead and actually, no, I'm going to uncomment this line to keep the calendar. Alright, so I'm going to write four. I'll use a four loop for this. For I in, and then for the calendar, I'm going to get the inner month days and I'm going to choose a month, so I'll just choose 2026 and I'll choose August of 2026, colon. And then I'm just going to print out. Alright, so this function will return numbers that represent each day in the month. So let's run this and you can see that the way that this works, okay, here in the output, so in August, right, there were 31 days, so there's day number one and day number 31 all the way down here. Now the zeros at the start here indicate that there are days in the week that are not part of this month.
>
> **[4:40](https://www.linkedin.com/learning/learning-python-25309312/working-with-calendars?u=76281980&t=280)** So what that means, for example, is that here we've got, you know, Sunday, Monday, Tuesday, Wednesday, Thursday, right? They're the previous month in July. And so this number one shows up here, that's going to be Sunday, Monday, Tuesday, Wednesday, Thursday, that's going to be a Friday. And then when we get to the end of the month, we can see that there are zeros. That means the [[Representational State Transfer (REST)|rest]] of that week belongs to September. The calendar class also provides some useful utilities for the current locale. So I can loop over the names in the month names and day name properties on the calendar class. So this way I don't have to write custom code in my application that knows what the names of the months are around the world. The system's locale will just give them to me based upon where the user's computer happens to be located. So if I write for name in, calendar dot month, name,
>
> **[5:38](https://www.linkedin.com/learning/learning-python-25309312/working-with-calendars?u=76281980&t=338)** and I can print the name and I can do the same thing for the days. I can write for day in, calendar dot day name, and we'll print out each day, alright? And let's comment out this previous example right here.
>
> **[6:00](https://www.linkedin.com/learning/learning-python-25309312/working-with-calendars?u=76281980&t=360)** Okay? And let's run. Now, obviously here in the US I'm getting the months, you know, January, February, March and so on. If I was in another part of the world, or if my local system here was set to be someplace in Europe, for example, I would get the locale based names and the same thing for the days, right? You see I'm getting Monday, Tuesday, Wednesday, right? So I can get the names of both the months and the days as they would appear in the current locale for where the computer is or for where the system happens to be set to. Okay, let's try one more example. Let's comment that out. So suppose I had a team of people and the team meetings were the first Friday of every month and I wanted to write a script that would print out what those dates would be for the upcoming year. That way I could give that list of days to my team members and they would know what the meeting dates are for the year in advance. What I would need to do is calculate when the first Friday happens in each month and then calculate the date that represents that day. So what I'm going to do is print out, team meetings will be on, and then I'm going to create a loop for month in range and I'm going to start one going up to 13. And obviously there is no 13th month, but remember in the range function, the last number is not inclusive.
>
> **[7:32](https://www.linkedin.com/learning/learning-python-25309312/working-with-calendars?u=76281980&t=452)** So it's going to go from one to 12. So now what I'm going to do is get an array of weeks that represent each one of the months. So I'll make a variable called Cal, and then on the calendar module, I'm going to get the month calendar, and I'll do this for 2026 and the particular month that I'm looking at. So each time through this loop, right, M is going to be a month number, one up through 12, and then I'm using the calendar classes month calendar function to get an array of weeks that represent the days in the given month. So each one of those items in the list is going to be itself, a list of days, and I'm going to calculate where the first Friday falls within the month. So I'm going to define two variables, week one, so that's going to be the first of those lists, and then week two is going to be the second of those weeks. So the first Friday of the month has to be somewhere within the first two weeks. So if week one starts on a Saturday, then the first Friday is going to be within the second week. So I've got these two variables here, right, week one a week two. So then I just need to see which of the two weeks has the first Friday. So to do that, I'm going to use the calendar's Friday constant to index into each of these arrays.
>
> **[9:04](https://www.linkedin.com/learning/learning-python-25309312/working-with-calendars?u=76281980&t=544)** So if the value at week one of the Friday index,
>
> **[9:13](https://www.linkedin.com/learning/learning-python-25309312/working-with-calendars?u=76281980&t=553)** is not equal to zero, then our meet day, I'm going to create a variable called meet day, is going to be week one of calendar Friday. Now if that value is zero, that means that the first Friday falls in the second week. So that in that case, I'll simply set meet day equal to week two of calendar Friday. So remember we saw this earlier, right, when we printed out the values of the month calendar, right? If the day number of a particular day in the week is zero, then that means that that day is part of the previous month. So now I have my meet day variable set to the date of the first Friday and I just need to print out the month name and the day. So let's do that. I'm going to print out and I'm going to use an interpolated string for this. I'll print out the calendar and I'm going to use the month name property for the current month index. And then inside curly braces, I'm going to print out the meet day and that's going to be the actual day number, right? It's going to be, you know, 10th, seventh, whatever. Alright, so let's go ahead and run this. Alright, so you can see we've looped through all the months and now we have a list of the first Fridays of each month. So for January in 2026, it's going to be the second,
>
> **[10:47](https://www.linkedin.com/learning/learning-python-25309312/working-with-calendars?u=76281980&t=647)** and then February it's the sixth, and then down here in August it's the seventh and September it's the fourth. So now I can give this list of dates to my team and they'll know what the actual dates are for the first Friday in each one of the months that we're going to be having our meeting. Such is the power of Python.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[HTML]] (9), [[Python (Programming Language)|Python]] (3), [[Representational State Transfer (REST)|Rest]] (1)
> **Env Vars:** html (9)
> **CLI Commands:** python (3), make (3)
> **Analogies:** for example (3)
> **UI Navigation:** scroll up (1)
> **Definitions:** means that (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Dates and times](https://www.linkedin.com/learning/learning-python-25309312/solution-dates-and-times?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/solution-dates-and-times?u=76281980&t=0)** - [Instructor] All right, how'd you do on this one? This one was a little bit tricky. I made it a little challenging and I gave the hint that there was a particular class in the calendar module that you wanted to maybe look at. The key to solving this challenge was to use the month calendar class, and that's what I listed in the hint section of the challenge instructions. So let's go over my code. So remember that our function is given a year and a month, along with a weekday value from zero to six, which represents a day of the week with zero being Monday and six being Sunday. And our task was to return the number of times that particular weekday appears within the given month. So my code imports the calendar module because we're going to use that, and then I use the month calendar method to get the month for the given year. And what this function does is it returns an array of lists for the specified month, and each list in the array lists a week that's in that month. So each one of these lists contains seven integers, and each index into that particular week corresponds to the integer that represents the day that we are interested in. So for this particular month, right, there is no Monday,
>
> **[1:34](https://www.linkedin.com/learning/learning-python-25309312/solution-dates-and-times?u=76281980&t=94)** there is no Tuesday, but it looks like the first day of the month starts on a Wednesday. And then similarly down here, there is a Monday, Tuesday, Wednesday, but in the last week of this month, there is no Thursday, Friday, or Saturday. So basically, if the value in that index is zero, then that week doesn't contain that day, and this usually happens at the beginning or the end of each month. Alright, so that's what my code is doing. My code looks at each week in this week list, and if the the index for that day in that week is not equal to zero, then I increment the day count because that week contains that day. And then when this loop completes, day count will contain the number of days of that weekday in that month. So let's take a look at our test code. So here in our test code, we are looking at 2025. We're looking at December of 2025, and the test day that we are interested in is zero. So that's a Monday. So if I click on test my code, you can see that sure enough, there are five Mondays in December of 2025. Let's take a look at July of 2027. There's only four Mondays in July of 2027. Let's try a different test day. Let's try Saturday, which is five, right? So let's go ahead and test. And there's five Saturdays in July of 7 27. So this just goes to show that you can sometimes solve what seems to be a thorny problem with a particular class in one of the [[Python (Programming Language)|Python]] standard library modules.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (1)
> **CLI Commands:** python (1)
> **Cross-References:** in the last (1)
> **UI Navigation:** click on (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 6. Working with Files

[↑ Back to Table of Contents](#table-of-contents)

#### [Writing files](https://www.linkedin.com/learning/learning-python-25309312/writing-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/writing-files?u=76281980&t=0)** - [Narrator] [[Python (Programming Language)|Python]] provides built-in methods for working with files and directories. You can open files, write data into them, read the data back in, get information about the files and so on. And that's what we're going to take a look at in this chapter. So let's begin by opening up the writefile_start file here in the editor. If you're using Colab, just go ahead and make a new code cell. Now I don't need to import any modules to work with files because the functions for working with files are just built into the base Python language. So for our first example, let's just write some information to a text file. So the way that I do this is I'm going to create a new variable named sample_file, and then I'm going to call the open function. The open function takes a couple of parameters. The first is the name of the file to operate on, so I'll call that textfile.txt. And then there's the kind of access to the file that you want. So I'm going to open the file for write access. And so I'm going to pass in a w and then a plus sign, which means that Python should create the file if it doesn't already exist. So when the open function returns, and this is one of the built-in functions that we learned about earlier in the course, the result is a reference to a file object that I'm going to store in this variable and that I use that variable to operate on the file going forward. So now that the file is open, let's write some data to it. So I'm just going to write some sample text data. So I'll call on the sample_file object.
>
> **[1:35](https://www.linkedin.com/learning/learning-python-25309312/writing-files?u=76281980&t=95)** I'm going to call the write function and I'll just put in some text. This is some sample text in our sample file. So you can do as many writes as you want. The write function just writes data to the file. I'm just using a single sentence of text, but you can write up a bunch more if you want to. So when you're finished writing your data, the file needs to be closed. And we do that by using the close function. So I'll call close on the file and that's pretty much all there is to it. So to recap, open the file, write some data, and then close it. So let's go ahead and run this and let's see what happens. And we'll give it a second to create the file. And sure enough, you can see down here in the list, the text file shows up. Here's our text data. Let's go ahead and try the same thing over in Colab. So I'm going to copy these lines of code right here, going to paste them in my cell, run, and after a few seconds or so, we should see the file show up. Let's give it a minute. There it is. So we'll double-click on this and sure enough, there's our sample text in our sample file. All right, so far so good. Next, let's try adding additional content to the existing file that we just created. So what I'm going to do is I want to open the file to add text to the end. If I just do the same thing with open and write, that's going to overwrite everything that's already in the file.
>
> **[3:08](https://www.linkedin.com/learning/learning-python-25309312/writing-files?u=76281980&t=188)** And I don't want to do that. I want to add content to the end. So what I'm going to do is copy this original line with the open and I'm going to call this with a+. And then let's write some more data to the file. So I'll copy that line and I'll just change some of the text and then we'll close the file when we're done. And since we've already created the file, I don't need this code anymore, so I'll just comment that out. All right, so in this case, I want to append text to the end. So I use the open function, but this time the write mode is different. Instead of write, it's append. I'm going to write additional data and then we're going to close the file. So let's go ahead and run that. All right, and if we open up textfile.txt, we can see that additional data has now been written into the file. Okay, so that's good. Let's go and try the same thing over in Colab. So I'm just going to copy these lines of code here. And in Colab I'm going to make a new code cell, paste in the new code, and I'm going to run this cell. I don't need to run this one because it's already been created. So let's run this code. All right, and we can see that it's been executed. Let's go ahead and double-click on our textfile and we can see also if we scroll to the right here
>
> **[4:42](https://www.linkedin.com/learning/learning-python-25309312/writing-files?u=76281980&t=282)** that the additional content has now been written into the file. All right, so that's how easy it is to write files. In the next example, we'll see how to read information from an existing file.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (3)
> **CLI Commands:** python (3), make (2)
> **UI Navigation:** open the (3), double-click (2)
> **Tools:** colab (4)
> **Code Identifiers:** sample_file (2), writefile_start (1)
> **File Paths:** textfile.txt (2)
> **Cross-References:** earlier in (1), in the next (1)
> **Definitions:** means that (1), is a  (1)

#### [Reading files](https://www.linkedin.com/learning/learning-python-25309312/reading-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/reading-files?u=76281980&t=0)** - [Instructor] In the previous example, we created a new file with content and now we're going to see how to read the contents of an existing file. So let's begin by opening up the readfile_start file here in the editor. And if you're using CoLab, you can see that I've left the previous code cell in place. Just make a new one down here. So this example is going to use the file that was created during the previous video. So if you haven't completed that example, I suggest you go do it now. There are a couple of ways to read the contents of a file. So first I need to open the file for read access. So similar to the previous example, I'll write my variable name, sample file. And again, I'm going to call the open function on textfile.txt. And in this case, I want read mode. I'm not interested in writing the data, I want to just read the content. Then I'm going to add some code to make sure that the file was open correctly. So I'm going to check using an if statement that if the sample files mode is, in fact, read, then we know that the file was opened correctly. So for the first example, I'll just use the read function to read the entire file contents all at once and then print them out. So because I'm in the if block, I have to indent. So I'll make a variable called contents and I will call sample_file.read. That reads everything, and then I'll print out the contents. Alright, let's go ahead and try that. Sure enough, you can see that that works. This is the text that we created in the previous example.
>
> **[1:35](https://www.linkedin.com/learning/learning-python-25309312/reading-files?u=76281980&t=95)** Let's go ahead and copy this and let's go over to CoLab. Let's paste it into a new cell and let's run this. And sure enough, we can see that that works. We are reading the contents of the file we created in the previous example. Alright, you can also read the contents of a file in a more granular way. I can read the file line by line. So if you have a really large file, sometimes it doesn't make sense to read the whole thing in, especially if all you want is to process a small piece of the content at each time. And for this, we can use the read lines function. So I'm going to comment out the previous call, all right? And we'll comment out that as well. So what I'm going to do is add some new code. I'll make a variable called file lines. And on the sample file, I'm going to call read lines. That will get all the different lines. So I'm going to write for line in file_lines. So I'm creating a for loop that's going to iterate over all of the lines in the file. I'm going to print each line. So in this case I'm going to read the content as an array or a list of individual lines and then print them. So the net effect should be the same that you just saw, only I'm doing it in a slightly different way. So let's run this and sure enough, there's the output. Let's copy this code. And here in our code cell, let's paste that in
>
> **[3:11](https://www.linkedin.com/learning/learning-python-25309312/reading-files?u=76281980&t=191)** and comment out the previous calls and let's run. And you can see that the effect is the same. So just like everything else, well just like almost everything else, [[Python (Programming Language)|Python]] makes working with file content really easy.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (1)
> **CLI Commands:** make (5), python (1)
> **Code Identifiers:** readfile_start (1), sample_file (1), file_lines (1)
> **Analogies:** just like (2), similar to (1)
> **Tools:** colab (2)
> **File Paths:** textfile.txt (1)
> **Cross-References:** previous video (1)
> **UI Navigation:** open the (1)

#### [Working with the operating system](https://www.linkedin.com/learning/learning-python-25309312/working-with-the-operating-system?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/working-with-the-operating-system?u=76281980&t=0)** - [Instructor] Sometimes you need to perform operations with files that go beyond just reading and writing data. So for example, you might need to determine some information about a file, like whether it exists or how big it is, what the path of the file is, whether a given path represents a file or a directory and so on. [[Python (Programming Language)|Python]] provides functions to help you do all of this and more. So let's open up the ospathutils_start file. And of course in Colab, you can just create a new code cell. I've left the previous code cells in place from the previous videos. So go ahead and create a new code cell down here. Let's start with something really simple. Let's print the name of the operating system that we are running on. So I'm going to import the OS module, which is one of the standard library modules that Python comes with. And then I'm going to print os.name. Let's go ahead and run that. And you can see that I'm running on Posix because this is a [[Linux]]-based code base. Let's go ahead and try the same thing in our Colab. So I'm going to import OS and print os.name. Okay, let's run that and you can see I get the same result here. Now this may be different for you. If you're running the example code on your Mac or [[Windows]] or whatever, then the results here may look a little bit different, but you'll see the name
>
> **[1:32](https://www.linkedin.com/learning/learning-python-25309312/working-with-the-operating-system?u=76281980&t=92)** of your operating system when you run this. Let's take a look at some of the path related features. So I'm going to also open and import. So from the operating system module, I'm going to import the path class 'cause I need some path features. So we can check to see if an item exists. So let's print out item exists and then I'm going to call path.exists on the text file that we created in the previous examples. So if you don't have that file, you should probably go back and do the previous exercises in the chapter first. We can also check to see whether it is a file. So let's print out, "Item is a file," and we'll call path.isfile on textfile.txt.
>
> **[2:35](https://www.linkedin.com/learning/learning-python-25309312/working-with-the-operating-system?u=76281980&t=155)** And let's also just for fun, let's print out, "Item is a directory," 'cause we can check that as well and we'll call path.isdir on textfile.txt. So in each of these cases I'm operating on the textfile.txt item. You can see it here in my code space. If you don't have this file, it's probably because you didn't do the previous examples. So go run those, if you don't have this file right here. Same thing for the Colab, it's already there. So let's run our code and now we can see that yes, the item does exist, right? So textfile.txt is in fact there, it is in fact a file. And because it's a file, it is not a directory. All right, so far so good, let's keep on going. Let's get the file's full path information. So I'll print that the item's path is, and then on the path class I'm going to use the real path function to get the realpath to my text file. And we can use the split function to separate the file name from the path. So I'm going to print out item's path and name separately are going to be, I'm going to call path.split and I'm going to call path.realpath on the text file. Okay, let's run our updated code.
>
> **[4:11](https://www.linkedin.com/learning/learning-python-25309312/working-with-the-operating-system?u=76281980&t=251)** All right and we can see that the path here in my code space is right here to the text file. And then here the split function gives us the path by itself and the file name by itself, so that could be useful and let's go ahead and run the same code over in Colab. So let's see, I have to copy and paste the previous two examples. So let's copy that and let's paste that in and let's run. And you can see that we're getting pretty much the same information, does exist. It is a file, it's not a directory. And here is the path in the Colab context. So that's working just fine. Okay, so let's get some information about the file's last modification time. So what I'm going to do is use the path module to get the modification time of the file. And then I'm going to use the time class's ctime function to convert that into a readable time. And we learned about how to use dates and times in a previous chapter. So this will probably look familiar to you. I'm going to import time and from datetime, I'm going to import datetime class. All right, so let's get the modification time. I'll make a variable named to T and I'll call time.ctime and I'm going to use the path class to get the mtime. And you can see that there's other functions, right? There's also get the creation time, right?
>
> **[5:46](https://www.linkedin.com/learning/learning-python-25309312/working-with-the-operating-system?u=76281980&t=346)** So there's the get the access time, last time it was accessed. So I'm just going to get the modification time on my text file and then we're going to print that out. And I'm also going to use the get modification time function. And I'm going to construct a datetime object using the from timestamp function that's built into the datetime class. So what I'm going to do here is print. And on the datetime class I'm going to call from timestamp because the operating system has what's called a timestamp that's on each file and it's not really a readable date, it's basically a big number and that number represents the number of milliseconds from the start of some time period. So we have to convert that into a readable date. So what I'm going to do is get the modification time from the text file and then I'm going to use this fromtimestamp function to create a readable date and time. So let's go ahead and run our updated code. All right, and we can see sure enough that this is the modification time coming back and being passed to time.ctime. And then this output right here is the from timestamp on the datetime class. And of course once again we can run this in Colab to make sure it works there and it does. One more example. This time we're going to be using some date math. And if you watched the chapter on manipulating dates,
>
> **[7:21](https://www.linkedin.com/learning/learning-python-25309312/working-with-the-operating-system?u=76281980&t=441)** this will be a little bit of a review. I'm going to calculate how long ago the item was modified. So I'm going to write td equals, I'm going to call datetime.now, which is the current time. And I'm going to subtract off datetime.fromtimestamp and I'm going to call path.getntime on my text file. So this will give me a time delta. Remember time delta is from the previous chapter. So I'm going to subtract off using date math, the modification time from the current time and that gives me a time delta object that represents the difference between now and when the file was last modified. And of course I have to print out that result. So I'm going to print and I'll use an interpolated string for this. It has been and then in braces I'll put in the TD since the file was modified. Then I'm going to use the total seconds function on that time delta object to get the time difference as total seconds. So I'll also print out, and again I'll use interpreted F string and I'll write or td.total seconds. And that's a function so I have to call it seconds. So let's go ahead and run our updated code.
>
> **[9:02](https://www.linkedin.com/learning/learning-python-25309312/working-with-the-operating-system?u=76281980&t=542)** All right, so we can see that it's been about 20 minutes since the file was last modified or 1200 and almost 34 seconds. So let's go ahead and copy this and run the same thing in Colab and let's run. And we get a very similar output.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (2), [[Linux]] (1), [[Windows]] (1)
> **Tools:** colab (7)
> **CLI Commands:** python (2), make (2), go run (1)
> **Definitions:** is a  (5)
> **File Paths:** textfile.txt (4)
> **Cross-References:** previous chapter (2)
> **Code Identifiers:** ospathutils_start (1)
> **Analogies:** for example (1)

#### [Using the filesystem](https://www.linkedin.com/learning/learning-python-25309312/using-the-filesystem?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/using-the-filesystem?u=76281980&t=0)** - [Presenter] So far in this chapter, we've seen how to create and read files and how to get information about files, but [[Python (Programming Language)|Python]] also provides a set of utilities for manipulating files using the operating systems Shell Utilities. And that's what we're going to look at in this example. So let's start up by opening the Shell Start file here in our editor. And if you're using CoLab, go ahead and create a new code cell and you can see I've left the previous code cells in place from the previous examples. So this example file uses this text file.txt file that's been created in the previous videos. So if you haven't completed those, I suggest you do them now or you could also just create this text file and put some random text in it. So to use these Shell utilities, I first need to import the sh util module into my app. So let's do that along with some other modules we know we're going to be using. So I'm going to import the operating system module and from the operating system module, I'm also going to import the path class and I'm also going to import the shutil module. Alright, so let's start off by doing something pretty simple. Let's make a copy of an existing file. And like I said, this example depends on this file being present, this text file being present. So you can see I've got it here on my list. If it's not there for you, either create it or go do one of the previous examples. It doesn't matter what the content is. So what I'm going to do is use
>
> **[1:33](https://www.linkedin.com/learning/learning-python-25309312/using-the-filesystem?u=76281980&t=93)** the path modules exists function to make sure that the file is there. So I'm going to write if Path.exists on the text file. If we know that the file exists, what I'm going to then do is get the path information for the file using the Path class. I'll create a variable named source and I'll set that to path.realpath for the text file .txt. Alright, so to make a copy, I'm going to use the original path to the file and just put a .bak extension on it to indicate that it's a backup. So I'll make a variable called Destination and that's going to be Source plus .bak. So the source, this is a string and I'm putting this .bak string on the end of it. And then I'll use the copy function of the shutil module to actually copy the file. So I'm going to call shutil.copy and I'll copy the source to the destination. So let's go ahead and run that. And when we run this, sure enough we can see that textfile.txt.bak now appears because we've made a copy of the file, I want to point out here that the copy function, it copies over the files content, but it does not copy over the original files [[Metadata]] such as the creation and modification times and other properties.
>
> **[3:11](https://www.linkedin.com/learning/learning-python-25309312/using-the-filesystem?u=76281980&t=191)** If you want to do that, you can use the copy two function which will copy that data over and I'll let you try that on your own. Alright, let's try something else. Let's try renaming the original file using the rename function. So on the operating system module I'll call rename and I'm going to rename textfile.txt over to newfile.txt. And as you might imagine this just renames the file specified in the first argument to this name in the second argument. So let's go ahead and run our updated code. Alright, so the copy is made and then the file is renamed to newfile.txt. Alright, let's go ahead and make sure that this works over in CoLab as well. So let's copy what we have here all the way down to here. We'll copy, we'll put this in a code cell and we will rename it and when we run, okay, we can see that it executed and if we give it a few seconds, sure enough, yep, here's the copy with the .bak and then the file got renamed. Alright, so everything seems to be working. Alright, let's try something else. Let's try creating a zip archive. The Shell Utilities module is more than just about working with individual files. Let's try creating a zip archive and putting some content into it.
>
> **[4:44](https://www.linkedin.com/learning/learning-python-25309312/using-the-filesystem?u=76281980&t=284)** What we're going to do is use the Shell utilities make archive function in order to create a zip archive that contains the contents of a given directory. So what I'm going to do first is use the path split function to get the path to the directory I want to create the archive out of. And remember we saw this split function in a previous example. So that gives us both the path and the file name. So I'm going to call it path.split on my source, which is textfile.txt. And we actually have to change this code now because textfile.txt doesn't exist anymore. We renamed it. So let's, we have to check to see if newfile.txt exists and we'll do the same thing here. Alright, and since we've already made the copies, let's go ahead and comment out this code. Alright, so now that I've got the path to the directory I want to make the archive out of, I need to call the make archive function, which will take the name of the zip file that I want to create. So let's call on Shutil, I'm going to call make archive, and I want to create a file called archive. It is going to be in the zip format and the contents are going to come from the root dir. Let's go ahead and give this a try. So let's run this, right,
>
> **[6:20](https://www.linkedin.com/learning/learning-python-25309312/using-the-filesystem?u=76281980&t=380)** and sure enough, okay, you can see right there there's the archive zip being created. Let's go ahead and copy this code over and see if it works in CoLab. So I'll copy these two lines and we'll put it down here and we have to remember to rename this new file. And of course we don't need the previous examples anymore. All right, so let's run. All right, looks like we get the little check mark. It looks like things ran. So let's give CoLab a second to update the ui. And sure enough, there is our archive zip being created. Okay, that looks pretty good. So by default, all the contents of the target directory get added to the archive, but I have more control over that if I want to. I can actually add specific items to a zip file. So let's modify our code to do that using a different module. What I'm going to do, let's close that terminal. I'm going to import from the zip file module, I'm going to import the zip file class. Notice the casing difference here, it's capital Z and capital F. What I'm going to do is add some custom code for creating a zip file. So what I'm going to do is I'm going to write with ZipFile, and that's going to take the name
>
> **[7:55](https://www.linkedin.com/learning/learning-python-25309312/using-the-filesystem?u=76281980&t=475)** of the archive on I want to create. So that's going to be testzip.zip. And I'm going to open that in writing mode. So I'm going to write with the zip file as newzip, okay, this creates a context manager, it's called. So now I don't have to keep referring to the various different file utilities. I can just, when I, when this code completes, it'll close the file for me automatically. So what I'm going to do is write newzip.write. And I'm going to add newfile.txt and I'm also going to write textfile.txt.bak.
>
> **[8:45](https://www.linkedin.com/learning/learning-python-25309312/using-the-filesystem?u=76281980&t=525)** So this construct right here, the width keyword lets me create a local scope that simplifies working with objects. It's called a context manager and it takes care of some things that I would otherwise have to handle explicitly. So in this case, I don't need to manually close the zip file when I'm done with it. When these two lines of code finish executing, the context manager that's created by this with statement will automatically close the file for me. So the width keyword will create this zip file object and return it. And I'm naming that object, newzip. And this code creates a new zip file object named testzip, and I'm using the right mode to indicate that I want to write data into it. Alright, so let's go ahead and comment out the previous example. Alright, so now we're checking to see if new file exists. That's all good. Let's run. Alright, and sure enough, there is testzip.zip being created. Let's go ahead and comment. Let's copy, I mean this code. Let's go to CoLab. Let's try the same thing and let's comment out the previous example and let's import the module we need. So from zip file, import, Zipfile. All right, let's run this and we'll give CoLab a couple of seconds because it needs to update the ui.
>
> **[10:18](https://www.linkedin.com/learning/learning-python-25309312/using-the-filesystem?u=76281980&t=618)** Okay, and looks like it worked. Yep, there it is, there's test Zip.zip. So you can see that using the Shell Utilities module of Python gives you really great control over working with file objects. I would suggest maybe taking a few minutes here, look at some of the functions that we didn't cover in the Shell Utilities, and maybe try some of your own experiments.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (2), [[Metadata]] (1)
> **CLI Commands:** make (9), python (2)
> **File Paths:** textfile.txt (5), newfile.txt (4), file.txt (1)
> **Exercise Files:** zip file (10)
> **Tools:** colab (6), terminal (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)
> **Analogies:** imagine (1)

#### [Solution: Files](https://www.linkedin.com/learning/learning-python-25309312/solution-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:04](https://www.linkedin.com/learning/learning-python-25309312/solution-files?u=76281980&t=4)** - Okay, for this challenge, we needed to add up all of the byte sizes of the text files in the depths directory. We can assume that there's a directory called depths at the same level of as our code. So that's where these files are stored. And we had to ignore any files that were not text files. So in my code, I have a variable that keeps track of the total byte count so far, and you can see that I've imported the OS module 'cause I'm going to be using that. Then I have a variable that contains the name of the depths directory. Now I need to get a list of all of the contents of the directory, the steps directory right here. And what I'm going to do is use the listdir function, which is contained within the OS module, which is part of the standard library. And that will give me a list of file names. And those file names are just names. They don't have the directory path in them. So next I have a loop which iterates over each one of the file names in the list that I get back from the listdir function. So I got to do two things. I need to make sure that each one is in fact a file. And I also need to make sure that it ends with the ,txt suffix because we only want to count text files. So the OS path module has a function called isfile. And to make use of isfile, I need to give it the path to the file to check. So that's going to be the depths folder plus the forward slash plus the name of the file.
>
> **[1:40](https://www.linkedin.com/learning/learning-python-25309312/solution-files?u=76281980&t=100)** So I check to make sure that that's a file and not a directory. And then I'm using the endswith function, which is built into [[Python (Programming Language)|Python]] string objects to make sure that the file name ends with .txt. So if both of those conditions are true, then we have a text file. So I need to add its size in bytes to my running total. The OS path module has a function named getsize, which will give me the size of a file in bytes. And again, I have to give it the folder and a slash and the file name. So I get the file size, I add it to my running total, and then when the loop completes, I return the number of total bytes. So let's go ahead and run this code and you can see that sure enough, when I run my code that the size of the text files that are in the directory is 4232 bytes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (1)
> **CLI Commands:** make (5), python (1)
> **Speakers:** - okay (1)


### 7. Working with the Internet

[↑ Back to Table of Contents](#table-of-contents)

#### [Retrieving internet data](https://www.linkedin.com/learning/learning-python-25309312/retrieving-internet-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/retrieving-internet-data?u=76281980&t=0)** - [Instructor] One of the areas where [[Python (Programming Language)|Python]] really shines is in retrieving and working with data from the internet such as [[JSON]] and XML. In this chapter, we'll see how to work with those data types, but first we need to understand how to use Python to access the internet. So let's go ahead and open up the inetdata start example. And of course, if you're using CoLabs, just go ahead and create a new code cell to follow along. So in this first example, we're going to just retrieve data from a server and print the results. So the Python standard library includes a module that lets us make internet requests. It's called URLLib.request. So I'm going to import that module here. This module provides the classes and code that I need to make HTTP requests. So what I'm going to do is create a variable and I'll call that web URL, and I'm going to assign that to the result of calling URLLib.request.URLopen. There we go. And all I have to do is give it the URL I want. So I'm going to call http www.[example.com](https://example.com).
>
> **[1:17](https://www.linkedin.com/learning/learning-python-25309312/retrieving-internet-data?u=76281980&t=77)** And if you're wondering, that actually is a real domain. So if we go to [example.com](https://example.com), you can see that this is just an example domain that the IANA Corporation maintains for using examples. So it's not a real website, it just happens to be owned by the Internet Assigned Numbers Authority, but it is real so we can use it in our example. So we're going to open a connection to that website and this function call returns an HTTP response object. So that's what the web URL will be set to, that type of object. So for the moment, I'm just going to access this page and print out the result code from the response. So the result code is retrieved by calling the get code function on the response that we've created. So I'm going to print the result code, and that's going to be web URL.getcode. So that will be an HTTP result code. So for example, the result code will be 200 if everything's okay, or a 404 if the URL is not found and so on. So let's go ahead and run what we have so far, and sure enough, we can see the result code is 200. That means we are successfully opening that URL. Let's go ahead and copy this and let's run it inside CoLab and sure enough, we can see the same result. So now that we've been able to make a successful connection,
>
> **[2:52](https://www.linkedin.com/learning/learning-python-25309312/retrieving-internet-data?u=76281980&t=172)** let's try to read some data from the URL and print it out. To do that, I need to call the read function on this response object. So once again, I'm going to create a variable named data, and then on the web URL I'm going to call read and then we'll just print the data. So if you recall from earlier in the course, we used a read function to read data contents of local data files. I'm doing pretty much the same thing here. I'm just reading the entire contents of this URL into my data variable and I'm just going to print that out. So if all goes well, this should be the [[HTML]] code for the [example.com](https://example.com) homepage that we just saw. So let's go ahead and run the updated code and sure enough, you can see that I'm getting the HTML for the webpage back and the result code is 200, which means that everything is just fine. Let's go ahead and do the same thing in CoLab, so I'm going to copy and we'll paste and we'll run. And sure enough, here is the HTML coming back from the webpage. Okay, so that is just a quick introduction of how to read data from the internet, and we'll see how to use this a little bit more in depth as we go through the [[Representational State Transfer (REST)|rest]] of the chapter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (3), [[HTML]] (3), [[JSON]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **Env Vars:** url (9), http (3), html (3), json (1), xml (1)
> **CLI Commands:** python (3), make (3)
> **URLs:** [example.com](https://example.com) (3)
> **Tools:** colab (2)
> **Analogies:** such as (1), for example (1)
> **Cross-References:** earlier in (1)
> **UI Navigation:** go to (1)

#### [Working with JSON data](https://www.linkedin.com/learning/learning-python-25309312/working-with-json-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/working-with-json-data?u=76281980&t=0)** - [Speaker] In this example, we're going to build on what we learned in the previous video to see how to use [[Python (Programming Language)|Python]] to connect to a [[Real-Time]] [[JSON]] data feed and process the information. JSON stands for [[JavaScript]] object notation. It is a very common data format. You'll run into this a lot when you're working with data from various different sources on the internet. JSON is essentially a way of representing the data in an object as a string. So let's take a look at a sample JSON data object. The data is represented as key value pairs, and if this looks familiar, that's because it's essentially just like the dictionary objects that we learned about back in chapter two. So in this example, the JSON represents data about a person and you can see the string starts and ends with curly braces. And each field of data has a string as a name, as a key value. And then a data value. In this case, there's a string for the name, an integer for the age, there's a list of strings for the skills and another embedded JSON object or another dictionary for the address information. Now it's beyond the scope of this course to go deep into all the details of what JSON is and how it works. If you're interested in those details, there's a great course here in the library called [[JSON Essential Training]]. Then I suggest you check that out. So let's try a simple example in our code. So what I'm going to do is I'm going to use the useless facts API to [[Fetch]] a random useless fact from this website.
>
> **[1:35](https://www.linkedin.com/learning/learning-python-25309312/working-with-json-data?u=76281980&t=95)** And you can see the link there in the browser bar. There are two HTTP endpoints that I can access. One for random useless facts and one for today's specific fact. So I'm going to copy this path right here and I'm going to put it onto the end of this URL and open this up in the browser. And when I do that, you can see I get some JSON code. Let's go ahead and [[Zoom]] this up so you can read it. So here in this code, you can see that there are various key value pairs. So we have an ID and a value and then a text and value. So this field right here, the text key and the text value is what we are going to be interested in. So let's write the code in our example to pull in the random fact and then extract this text from the JSON. So let's go ahead and jump over to our code and let's open up jsondata_start. And of course, if you're using Colab, just create a new code cell. So I've imported my urllib request module. So what I need to do is open the URL, like we did this in the previous example. So I'll have web URL and I will write urllib.request., and it's going to be urlopen and that's going to be the link to this API. So I'll copy that and paste it in. Okay, so we're going to open this URL
>
> **[3:09](https://www.linkedin.com/learning/learning-python-25309312/working-with-json-data?u=76281980&t=189)** and then just like we did in our previous example, we're going to print out the Result code just to make sure everything's okay. So we'll get the web_url, we'll call getcode. Alright, and then we're going to read the data. So once again, I'll create a variable named data and on the web_url variable, I will call the read function. And then we're going to use print out the data for now. All right, so let's go ahead and run this in the terminal and sure enough, you can see here's the result code. So we've got a result of 200. All right? And here is the actual JSON text coming back. So these are the real bytes coming from the actual website. Let's go ahead and copy this code and run it inside our Colab notebook. And, of course, I have to import urllib.request and it will import the json library for the moment as well. So we'll go ahead and run and you can see once again, it's running. Okay, so now that we have the data coming back, what we need to do is take the raw JSON string that returns from this call and convert it into a live, native Python dictionary object that I can then access just like any other dictionary. So back here in my code space, I'm going to import json.
>
> **[4:46](https://www.linkedin.com/learning/learning-python-25309312/working-with-json-data?u=76281980&t=286)** So the JSON module is a standard library module that lets us work with JSON code. What I'm going to do is instead of printing out the data, let's comment that out. I'm going to make a new variable called theJSON. And then on theJSON module, I'm going to call loads, which means load string. And I'm going to pass in the data string that we got back from the website. So that takes the string of JSON code and parses it into a native Python dictionary object. And now, I can access the data within the object just like I can in any other dictionary. So I'm going to print out theJSON. And remember how we index into dictionaries is by using the key. And remember, the field that we want is the text field. So that will give us our random fact. So let's go ahead and run. All right. And now we can see the random fact, right? If I run this again, we'll get a different random fact. I'll do it one more time. You can see that each random fact is coming back. Alright, let's go ahead and copy this data. Run it in Colab. All right, so now instead of printing the data, we're parsing it into a dictionary. Let's run. All right, let's run again. All right, let's run again. You can see that there's a random fact coming back each time.
>
> **[6:19](https://www.linkedin.com/learning/learning-python-25309312/working-with-json-data?u=76281980&t=379)** So with just a few lines of code, we were able to retrieve data from a remote data source and create a Python object in our code, which we can then access directly.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (17), [[Python (Programming Language)|Python]] (4), [[Real-Time]] (1), [[JavaScript]] (1), [[Fetch]] (1)
> **Env Vars:** json (15), url (4), api (2), http (1)
> **CLI Commands:** python (4), make (2)
> **Code Identifiers:** thejson (3), web_url (2), jsondata_start (1)
> **Tools:** colab (3), terminal (1)
> **Analogies:** just like (4)
> **Definitions:** is a  (2), stands for (1)
> **Cross-References:** previous video (1)

#### [Manipulating XML](https://www.linkedin.com/learning/learning-python-25309312/manipulating-xml?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/manipulating-xml?u=76281980&t=0)** - [Instructor] XML is another very common data format. It's a tag-based language, just like [[HTML]]. And in this example we'll see how to use [[Python (Programming Language)|Python]] to load an XML file and then operate on the file while it's in memory. Now again, it's beyond the scope of this course to explain all of the basic details about XML and how it's used to represent data. There's a great course available here in the library called XML Essential training. I suggest checking out that course if you want to learn more. So I'm going to demonstrate how to work with XML using VS code. And of course, if you're using Colab, that's fine, just go ahead and make a new code cell. What you'll need to do is you'll notice I have a sample XML file here in the [[GitHub]] code space. So there's a couple of ways that you can use this file. One way to do this is to download the example files from GitHub and then upload this XML file into Colab so you can work on it. So if you go to the Colab tab and you click on the little folder over here, you'll see that there's a directory. If you click on this upload button, it'll give you the opportunity to upload the XML file. But honestly, it's probably just easier to just copy and paste the content into a new file. So for example, you just go to the github repo, just copy this XML. So I'm going to copy it, and then here I'm going to just create a new file. So I'm going to click, I'm going to right click and choose New file. And you can see it's called untitled.
>
> **[1:34](https://www.linkedin.com/learning/learning-python-25309312/manipulating-xml?u=76281980&t=94)** So I'm just going to double click that. I'm going to paste in the XML here, and then I'm going to rename this. So actually let me close it and then I'm going to rename this to samplexml.xml. Now the data is in the file system here in Colab, so we can work with it locally. Yeah, don't bother. I mean if you want to upload it, that's fine, but that's what I just did it's probably simpler. So here's what we're going to do, this is the sample file. What I'm going to do is open up the xmlparsing_start file, and this is the code we're going to work on. So here's what we're going to do. First I need to import the module that lets me operate on an XML document, and that is the xml.dom.minidom. So we import that, the XLM file, in this case, I've got it right here at the root level of my code space. If you're working locally on your Mac or [[Windows]], you're probably going to need to move this file into the actual chapter seven, just to make it easier to work on because it'll be a local file path for you there. Just keep in mind, if you're not working on a code space or Colab, if you're working locally, just move it into the folder. Alright, so let's take a look at the XML file itself. So this XML file represents data about a person, and you can see that I've got a root tag named person.
>
> **[3:07](https://www.linkedin.com/learning/learning-python-25309312/manipulating-xml?u=76281980&t=187)** I have a first name, a last name, right, hometown, and then there's a whole bunch of skill tags with name attributes, okay. So what we're going to do is try out some XML operations. So to load the XML file and parse the contents into a Python data structure, I'm going to use the parse function on the XML minidom module. So I'm going to create a variable named doc to hold the XML document. And what I'm going to do is write xml.dom.minidom.parse,
>
> **[3:49](https://www.linkedin.com/learning/learning-python-25309312/manipulating-xml?u=76281980&t=229)** and I'm going to load samplexml.xml. So this will parse the XML file and create an in-memory dom object I can manipulate. Now once we've parsed the document, let's print out the tag name of the root of the document. So I'll print doc. And there are certain standard XML constructs that I have access to once I have an XML document in memory. So I'm going to print out the documents node name. Each tag inside an XML document is also called a node. And then let's also print out the docs first child tag name, right? Now if these property names don't look familiar to you, they are standard properties that are used in the [[Document Object Model (DOM)|document object model]], things like node name and first child and tag name. These are all standard properties of dom elements, whether it's XML or HTML. So let's go ahead and run what I have. So we'll run this in the terminal and we can see that the node name of the document is #document. That's pretty standard. And then the first child, if we look at the actual code itself, the first child is this root tag right here and we can see that its tag name is person. And so that's what we're getting from this operation right here. So we can see that the parse operation worked
>
> **[5:21](https://www.linkedin.com/learning/learning-python-25309312/manipulating-xml?u=76281980&t=321)** and we have XML information in memory. Alright, so far we're off to a good start. Now let's write some code to print out the list of skills that this person has. So to do that we need to access each one of these skills tags and then get the name attribute that's on each one. So to do that I'm going to use the standard dom function. It's called get elements by tag name. I'm going to create a variable named skills, and on my document I'm going to call getElementsByTagName. And the name of the tag is skill, right? Is that correct? Yes, they're called skill tags. That will give me a list of all the skills tag in the document and we can make sure that that works by printing out the skill count and then we'll get the length of the skills array. Now, the reason I have a length property here, and I'm not using the lend function is because this object that comes back from getElementsByTagName is an XML object and it actually has a length property. So that's why I'm using this length property instead of using the length function. Once I've done that, I'm going to iterate over each one of these tags and then print out the value of the name attribute. Alright, so again, let's go back to the XML really quick. You can see each skill has a name attribute, and the name attribute has the content of some skill name. So the way I'm going to do that is
>
> **[6:53](https://www.linkedin.com/learning/learning-python-25309312/manipulating-xml?u=76281980&t=413)** by using this function called get attribute. So for each skill in skills, I'm going to print skill. I'm going to call getAttribute, and I want the name attribute. That's pretty much all we should need to do for that. So let's run this code. Alright, and sure enough, we can see that the skill count is 4 and here are the skills being listed. So that's working fine. Okay, let's go ahead and copy and paste this code over in Colab. So yeah, I've got an empty code cell. So let's just copy and paste this whole thing. And if you've been following along, you've already got this code. So let's go ahead and run this and we see we have the same result. Alright, so everything seems to be working, let's keep on going. So let's try something else. Let's try creating a new XML tag and adding it into the document. So what I'm going to do is to find a variable named new skill. And on the document I'm going to call createElement. So I'm going to create a new tag and that's going to be a skill tag. And then on that new skill tag, now that I've created it, I'm going to call setAttribute and I'm going to set the nameAttribute to some skill. I'm old school, let's make a jQuery skill. Yeah, I can hear you laughing. Alright, so then on my document, I'm going to get the first child, which is the person tag.
>
> **[8:29](https://www.linkedin.com/learning/learning-python-25309312/manipulating-xml?u=76281980&t=509)** And then I'm going to call appendChild to append my new skill. So that is going to put my new skill inside the person tag at the end of the list here. Alright, so let's go ahead and try this out and let's see if it works. Oh, actually, to make sure it works, let's just copy and paste this code down here again to print out all the skills. So we'll see the before and the after. Alright, let's go ahead and run this. Alright, so we can see that the skill count was 4 and here they are. And now the skill count is 5 and we can see that the new one is added down there at the bottom. Alright, let's go ahead and copy this code and let's put it into Colab. Alright, and now let's run and we can see the same result here. So that should give you an idea of how you can use the Python XML module to manipulate XML documents in memory. Now, obviously this is a simple example, but if you have a moment to spare, I would suggest maybe taking some time here to try out some of your own experiments, check out the documentation for the minidom module and use my code as a starting point and just, I dunno, just try some things out. Have some fun.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (3), [[GitHub]] (3), [[HTML]] (2), [[Windows]] (1), [[Document Object Model (DOM)|Document object model]] (1)
> **Env Vars:** xml (28), html (2), xlm (1)
> **CLI Commands:** make (5), node (4), python (3), find (1)
> **Tools:** colab (7), github (3), vs code (1), terminal (1)
> **Code Identifiers:** getelementsbytagname (2), xmlparsing_start (1), getattribute (1), createelement (1), setattribute (1)
> **UI Navigation:** go to (2), click on (2)
> **File Paths:** samplexml.xml (2)
> **Documentation:** the docs (1), the documentation (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Conclusion](https://www.linkedin.com/learning/learning-python-25309312/conclusion?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-python-25309312/conclusion?u=76281980&t=0)** - Well, that brings us to the conclusion of Learning [[Python (Programming Language)|Python]]. I hope you enjoyed learning along with me about the Python language and maybe it's inspired you to continue your programming journey with this great versatile language. If you like what you've seen in this course and you think that Python might be a good fit for projects that you're working on, I can suggest a few courses you can check out next. One of the best features of Python is the rich standard library of built-in modules that provide a wide range of features and capabilities that you can use in your programs. We already used a few of these modules in the course. To learn more about these modules, check out the Python Standard Library Essential Training course. We also saw how to use Python to access information over the internet and process data formats like [[JSON]] and XML. Python has become very popular among data scientists and engineers, and if you're interested in learning more about those subjects, check out the Python working with data and Python [[Data Engineering]] basics courses. In fact, Python has become so popular for working with data that [[Microsoft]] has added it to their popular [[Microsoft Excel|Excel]] spreadsheet program. The course Python in Excel: getting started with data analysis takes a look at how you can use the power of Python in your Excel spreadsheets today. Python is also a very capable, [[Object-Oriented Programming (OOP)|object-oriented programming]] language, and we looked at some of those features in this course. If you want to learn even more about how to build great object-oriented programs, then consider watching the [[Python Object-Oriented Programming]] course. And finally, if you want to take your Python skills to the next level, consider watching the Advanced Python course. Also, be sure to stay up to date with Python
>
> **[1:35](https://www.linkedin.com/learning/learning-python-25309312/conclusion?u=76281980&t=95)** and learn more about the features provided by the language at [python.org](https://python.org). And don't forget to follow me on [[LinkedIn]] where I post all kinds of Python tips. Thanks again for watching, and I'll see you in another one of my courses again soon. Until then, happy coding.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Python (Programming Language)|Python]] (19), [[Microsoft Excel|Excel]] (3), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (2), [[JSON]] (1), [[Data Engineering]] (1)
> **CLI Commands:** python (19)
> **Env Vars:** json (1), xml (1)
> **URLs:** [python.org](https://python.org) (1)
> **Best Practices:** don't forget (1)
> **Prerequisites:** getting started (1)
> **Speakers:** - well (1)


## Instructor

- [[Joe Marini]]

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/learning-python-3980343/codespaces)

## Skills Covered

- Python (Programming Language)

## Path Context

### In [[Getting Started with Python]]
← [[Python Essential Training]] | **2 of 8** | [[Python Object-Oriented Programming]] →

## Appears In

- [[Getting Started with Python]]

## Related Courses

_Courses sharing skills:_

- [[Learning ArcGIS Python Scripting]] — Python (Programming Language)
- [[QGIS and Python for AEC]] — Python (Programming Language)
- [[Python Data Structures- Trees]] — Python (Programming Language)
- [[Using SQL with Python]] — Python (Programming Language)
- [[Python- Working with Predictive Analytics (2019)]] — Python (Programming Language)

---

[↑ Back to top](#)