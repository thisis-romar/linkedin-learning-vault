---
type: course
cssclasses:
  - lle-course
  - lle-level-advanced
slug: hands-on-advanced-python-data-engineering-basics
url: "https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics"
duration_minutes: 116
duration: 1h 56m
level: Advanced
updated: 9/13/2023
learners: 99574
skills:
  - Python (Programming Language)
exercise_files: true
github: "https://github.com/LinkedInLearning/hands-on-advanced-python-2703239"
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQEeGQr9A1OHHQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1692640821642?e=2147483647&amp;v=beta&amp;t=ydIw7jM-9_nheuE8xw2-U2ruB8pz4OZFv5hA7j5dXLw"
linkedin_topic: Data Science
learning_paths:
  - '[Data Engineering Hands-On Practice](../../Paths/Data%20Science/Learning%20Paths/Data%20Engineering%20Hands-On%20Practice.md)'
prev_courses:
  - '[Hands-On Introduction- Data Engineering](Hands-On%20Introduction-%20Data%20Engineering.md)'
next_courses:
  - '[Complete Guide to Python for Data Engineering- From Beginner to Advanced](Complete%20Guide%20to%20Python%20for%20Data%20Engineering-%20From%20Beginner%20to%20Advanced.md)'
path_nav: '[{"path":"Data Engineering Hands-On Practice","position":2,"total":7,"prev":"Hands-On Introduction- Data Engineering","next":"Complete Guide to Python for Data Engineering- From Beginner to Advanced"}]'
path_count: 1
tags:
  - course
  - topic/data-science
  - topic/database-management
  - topic/software-development
  - skill/python-programming-language
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Data%20Science/Hands-On%20Advanced%20Python-%20Data%20Engineering%20Basics.md)

![Hands-On Advanced Python: Data Engineering Basics](https://media.licdn.com/dms/image/v2/D560DAQEeGQr9A1OHHQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1692640821642?e=2147483647&amp;v=beta&amp;t=ydIw7jM-9_nheuE8xw2-U2ruB8pz4OZFv5hA7j5dXLw)

# Hands-On Advanced Python: Data Engineering Basics

> Looking to get up to speed with advanced coding skills in Python? This course was made for you. Join instructor Joe Marini in this hands-on, interactive, skills-first coding course designed for advanced-level Python developers. Explore critical Python coding skills to boost your technical know-how or prepare for an interview to land a new role. This course includes Code Challenges powered by Coder

> [LinkedIn Learning](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics) | 1h 56m | Advanced | 100K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (5 videos)
  - [Hands-on with advanced Python](#hands-on-with-advanced-python)
  - [What you should know](#what-you-should-know)
  - [Getting set up: Codespaces](#getting-set-up-codespaces)
  - [CoderPad challenges](#coderpad-challenges)
  - [The sample weather data set](#the-sample-weather-data-set)
- [**1. Parsing and Exploring Data**](#1-parsing-and-exploring-data) (7 videos)
  - [Loading and parsing JSON data](#loading-and-parsing-json-data)
  - [Basic data operations](#basic-data-operations)
  - [Filtering data](#filtering-data)
  - [Solution: Filtering](#solution-filtering)
  - [Sorting data](#sorting-data)
  - [Transforming data](#transforming-data)
  - [Solution: Transforms](#solution-transforms)
- [**2. Working with Collections**](#2-working-with-collections) (5 videos)
  - [Count values with defaultdict](#count-values-with-defaultdict)
  - [Using the reduce function](#using-the-reduce-function)
  - [Grouping data](#grouping-data)
  - [Working with dates](#working-with-dates)
  - [Solution: Collections](#solution-collections)
- [**3. Math and Statistics**](#3-math-and-statistics) (5 videos)
  - [Random values](#random-values)
  - [Random operations on sequences](#random-operations-on-sequences)
  - [Solution: Random days](#solution-random-days)
  - [Statistical functions](#statistical-functions)
  - [Solution: Detect outliers](#solution-detect-outliers)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Hands-on with advanced Python](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/hands-on-with-advanced-python?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/hands-on-with-advanced-python?u=76281980&t=0)** - Are you a [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) developer who's looking to get more hands-on experience? Great. You've come to the right place. Hi, I'm Joe Marini. I've been building software professionally for some of the best known companies in Silicon Valley for more than 30 years. In this course, I'm going to dispense with all the theory and guide you through a hands-on journey of applying some of the Python concepts I've covered in some of my other courses. Specifically, we're going to be looking at ways to work with data in Python using just the features that are built into the language and the Python standard library. And we'll even see how to use some of the mathematical features of Python, such as working with random numbers and some statistical functions like median, mean and standard deviation. Learning Python [Programming Concepts](../../Skills/Software%20Development/Programming%20Concepts.md) is fun, but it's even more fun when you put the theory aside and just start building real things. Let's jump in and get started on our hands-on Python journey.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (7), [Programming Concepts](../../Skills/Software%20Development/Programming%20Concepts.md) (1)
> **CLI Commands:** python (7)
> **Analogies:** such as (1)
> **Speakers:** - are (1)

#### [What you should know](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/what-you-should-know?u=76281980&t=0)** - [Instructor] Before getting started with this course, there are some things that you should already know and be familiar with. So first, this course is intended for people who are already familiar with the basics of programming in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md). You don't need to be an expert, but this isn't the place to start if you're brand new to Python programming. This course is not going to contain a lot of theory. I'm going to be focusing on hands-on examples of working with data and using a variety of different Python features. If you need to get started learning Python, there are some great resources here in the library for you. I would suggest Learning Python or [Python Essential Training](../Software%20Development/Python%20Essential%20Training.md). If you're absolutely new to programming in general, then you should check out the Programming Foundation series here in the library. Many of the concepts I will be demonstrating in this course are introduced in a couple of other courses: [Advanced Python- Working With Data](../Software%20Development/Advanced%20Python-%20Working%20With%20Data.md) and Advanced Python: Language Features. Might be a good idea to watch those courses first so you have a solid understanding of the Python features that I'll be using. But again, it's not absolutely necessary as long as you're comfortable with not having a complete detailed explanation of each feature as we go. And of course, I'm going to be using Visual Studio Code as my development environment. If you're not familiar with VS Code, then consider watching Visual Studio Code for Python Developers. Again, not absolutely necessary, but it might be helpful if you're new to using Visual Studio Code. That's pretty much all you need to know for this course. So once you feel comfortable with these concepts, you are ready to get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (10)
> **CLI Commands:** python (10)
> **Tools:** visual studio (3), vs code (1)
> **Prerequisites:** getting started (1)
> **Speakers:** - [instructor] (1)

#### [Getting set up: Codespaces](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/getting-set-up-codespaces?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/getting-set-up-codespaces?u=76281980&t=0)** - [Instructor] There are a couple of different ways you can work with the example code in this course. I've set up a [GitHub](../../Skills/Software%20Development/GitHub.md) repository with the examples, and you can find it at this link. The course content is organized into two separate folders. The finished folder contains all of the code examples in their completed state, so you can compare your code against them as you work through the course. The Start folder contains the code examples in their beginning state, and this is the folder I'll be working in throughout the course as we build towards the finished state for each example. If you want to download the examples and work with them locally on your computer, that's easy enough to do. You can clone the repository or download a zip file and then use your favorite code editor to work on them. You'll just need to make sure that you have [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) installed on your computer, at least version 3.10, in order to use the examples. But this repository has also been set up with a GitHub Codespace so you can just work directly online with nothing to install. All you need to do is fork a copy of the repository in your own GitHub account and then fire up a Codespace here in the code menu. Just click on [Codespaces](../../Skills/Software%20Development/Codespaces.md), click on this little plus sign right here, and that will create a Codespace for you and you won't need to install anything. Now you can see that I've already done that and this is the Codespace that I'm going to be using in the course. Either way works fine. If you want to download the files and work locally, that's great, but I'm going to be using the Codespaces feature in this course.
>
> **[1:34](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/getting-set-up-codespaces?u=76281980&t=94)** So let me open the Codespace and show you what it looks like. And it may take a moment when you start up your Codespace, so just be patient, it can take a couple of seconds to get going, alright. So let's make sure this is closed, okay. So when the Codespace starts, you'll see a browser based version of Visual Studio Code, and here in the left hand portion of the screen, you'll see the files that you'll need for the course. There's also a built-in terminal, which can be found in the View menu, so if I go up here and I go to the View menu, I'm going to choose Terminal, or you can type control and then back tick, so when I start up the built-in terminal, I can check to make sure that Python is already installed, so I'll type python --version, and sure enough, you can see that we've got Python 3.10.4 installed. You also want to make sure that you have the Python and Pylance extensions installed. Now VS Code might actually prompt you to do this when it starts up because it will recognize that you're working in a Python workspace. And since this is my own workspace, I've got these extensions already installed. What you'll do is you'll click on this little boxes icon right here and you can see I've got Pylance and Python. You can find them in the marketplace, so if I click on the Python extension here, this is actually the only one you need to install because when you install this, this will install Pylance for you. So if it's not already installed and VS Code doesn't prompt you for it, go ahead over to the extensions marketplace,
>
> **[3:07](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/getting-set-up-codespaces?u=76281980&t=187)** search for this Python extension, it's the one from [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md), it's the one with all the downloads, so that's the one that you want to install. Install this and then you'll be able to work with Python really easily within VS Code. There's one other thing to check quickly to make sure the preferences are set. So what I'm going to do is up here in this menu, I'll go to File, and in Preferences, I'll check Settings, or you can type control comma to bring this up. So once you bring up the settings, do a search on the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) execute, and you'll see that as you do this, there's a preference right here called Execute in File Dir. This should be checked. If it's not, then go ahead and check it and then you can just go ahead and close the settings and that will save them. And that's pretty much all there is to set up.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (10), [GitHub](../../Skills/Software%20Development/GitHub.md) (3), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (2), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **CLI Commands:** python (10), make (5), find (2)
> **Prerequisites:** install (7), set up (3), you'll need (1)
> **Tools:** github (3), terminal (3), vs code (3), visual studio (1)
> **UI Navigation:** click on (4), go to (2), open the (1)
> **Versions:** version 3 (1), python 3 (1), 10.4 (1)
> **Exercise Files:** download the (2), zip file (1)
> **Speakers:** - [instructor] (1)

#### [CoderPad challenges](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/coderpad-challenges?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/coderpad-challenges?u=76281980&t=0)** - [Instructor] This course includes automated code challenges that appear when you click on the challenge links in the course's table of contents. Each challenge includes instructions, and a couple of code editors you can use to create and test your own solution to the challenge. These challenges are hosted by CoderPad, and they appear in the same area of the course page where you watch the course's videos. We recommend using a desktop browser for the best experience with the code challenges, but you can use the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning mobile app if you prefer. The code challenge has four areas. There's the instructions in the top left, a code editor for your answer in the top right, another code editor where you can see how your code is used here in the bottom right, and there's a console for output in the bottom left. You can use these drag handles to allocate space as you like, and you can see I can bring up the console a little bit here. And you can also collapse the course's table of contents on the left to get more space. Each challenge has instructions that include a description of the challenge, and the challenge's parameters, and the desired result. Depending on the challenge, you might see some additional information in the instructions, such as an explanation of the parameters that your code will be given, along with some examples of what the expected output might look like. So you're going to create your answer in the top right code editor, and there are comments showing where to put your solution. When you click the test my code button,
>
> **[1:36](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/coderpad-challenges?u=76281980&t=96)** you'll see a message indicating whether your code returned a correct result. So let's go ahead and put in the right result. So to find the largest number, I'm going to return max, and I'm going to call numbers. All right, and then I'll click test my code. And you can see down here in the console, that I have a successful result, and it shows what my code returned. So if your code isn't successful, let's go ahead and put this back just returning zero, and I'll click test my code again. You can see that now I have the wrong answer, and I can ask for help. So the show expected results and show hints variables up here determine whether you see the expected result and any hints. You can change them each to a value of true to control this output. So let me go ahead and change those, and I'll show you what I mean. All right, and so now I'll click test my code again, and you can see that it says, all right, it's incorrect. Here's what my code returned, and if I scroll down, you'll see that now, it shows what the expected result is, and it's giving me a hint. The code editor in the lower right shows how your code will be executed, and you can change this code to experiment with different test cases. So instead of the large number being 19, I'll make that 25, and let's go back and put the right answer in again. I'll click test my code, and sure enough,
>
> **[3:09](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/coderpad-challenges?u=76281980&t=189)** now you can see I'm getting the new correct answer. Regardless of whether your answer is successful, you'll see messages in the console output, here in the lower left. And if any messages are too long to fit in that area, you can scroll sideways to see all the text, or again, use the drag handles to resize the area to your preference. When you've finished each code challenge, return to the course's table of contents, and click the next video to see my solution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** find (1), make (1)
> **Cross-References:** next video (1)
> **UI Navigation:** scroll down (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [The sample weather data set](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/the-sample-weather-data-set?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/the-sample-weather-data-set?u=76281980&t=0)** - [Instructor] Let's take a look at the dataset that we'll be using for this course. The data is contained in a file named sample-weather-history.[JSON](../../Skills/Web%20Development/JSON.md) and it's located in the top level folder of the example files. So when I open this file, you can see that it's a big array of json data objects, each of which contains some data about the weather of a particular day for a particular location. There's a field for the date as well as for the min and max temperatures for that day, and by the way, these are all in the Fahrenheit scale, followed by the precipitation for that day in inches along with the snowfall and amount of snow already on the ground, again in inches, and the average wind speed for the day in miles per hour. So the dataset is fairly large. You can see that it encompasses about five and a half years of data and the data structure itself is relatively simple. So we're going to be using this weather information throughout the course to perform all kinds of data operations. So just take a few moments to look through the dataset and familiarize yourself with it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (2)
> **File Paths:** sample-weather-history.json (1)
> **Speakers:** - [instructor] (1)


### 1. Parsing and Exploring Data

[↑ Back to Table of Contents](#table-of-contents)

#### [Loading and parsing JSON data](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/loading-and-parsing-json-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/loading-and-parsing-json-data?u=76281980&t=0)** - [Instructor] Let's start off by making sure that we can successfully load the dataset in order to work on it. So to do this, we're going to have to load the [JSON](../../Skills/Web%20Development/JSON.md) file and parse it into a [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) object. And luckily, Python gives us some high-level functions for doing exactly that. So again, if we look at the data file, we can see that it is an array of objects. So it starts with a bracket and it ends with one all the way down at the end. And each element is contained within these curly braces. So first, let's open the file using Python's built-in method for that. So I'm going to open up in chapter one, the parsedata.py file, and I'm going to do this with a context manager and Python's open function, which is used to open files. So the context manager starts with the with keyword and I'm going to write open and then the path to the file and the path to the file, remember, it's up here in the root directory of the examples. So I've got to work my way back up there. So I've got to go up a directory and then up another directory. And then it's going to be sample-weather-history.json.
>
> **[1:19](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/loading-and-parsing-json-data?u=76281980&t=79)** And I'm going to open that in reading mode. So I'll specify r and then I'll write as weatherfile. So now I'll have a variable named weatherfile when I open this. All right, so once the file is open, we can then use the JSON package to load and parse the data. But to do that, I first have to import the JSON module. So I'm going to import json. And then once I've got the JSON module imported, I can use the load function to load the data file. So I'll create a variable named weatherdata and I'm going to call json.load and I'll pass in the weatherfile that I just opened up. All right, so once the data is loaded, we can get some basic information about it to make sure that we did everything correctly. So for example, I can print out the length of the weather data variable and that should tell me how many records I have and it should be a fairly large number because we've got, you know, five and a half years of data in here. And let's also print out the first item in the array that we just loaded. And to do that, I'm going to import the pretty printer. This is a module that makes it easy to print out information. So let's go ahead and pprint. I'm going to call pp because that's the function and I'll print out the very first item
>
> **[2:53](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/loading-and-parsing-json-data?u=76281980&t=173)** in the weatherdata array. Okay, so let's go ahead and run this. So first, I'm going to save it and then there's a couple of ways I can run this code. I can right click on the file name here and I can open it up in the integrated terminal. I'll get back to that in a second. There's an easier way to do this. If I right click right within the source file, down here, I can choose run Python. And this appears once you have the Python extensions installed, and I'll choose run Python in terminal. When I do that, you can see that the terminal comes up and let me give it some more room. There we go. So you can see that it has executed the Python file and sure enough, when we print out the length of the weatherdata array, we get 1,977 records. So almost 2,000 data points. And we also have the first item in the weatherdata. And sure enough, here that is. So we've loaded our JSON correctly and now we have an array of Python objects that we can work on. All right, let me close this. If you want to open up the terminal just directly, you can either right click here and choose open in integrated terminal, or you can open the terminal manually by typing Control + back tick. And when that happens, you get the terminal opened up. If you right click on a folder and choose open in integrated terminal, you'll get a terminal that's already open to that chapter in the terminal. So if I now get a directory listing, you can see that there's my code
>
> **[4:27](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/loading-and-parsing-json-data?u=76281980&t=267)** and I can run it manually just by typing python and then the name of the file I want to run, python parse. There we go, and you can see we get the same result. It's up to you. You don't have to do it this way. Throughout the course, I'll be right clicking and choosing run in terminal just because it's the easiest way to do it. All right, so now that we have our data successfully loaded, let's see if we can figure out how many days of weatherdata we have for each year. So I'm going to create an empty dictionary named years and then I'm going to loop over all of the data that I just loaded and categorize it by year. So for each day in the weatherdata, I'm going to create a key for my dictionary. And that key is going to be the date field. And I'm only going to get the first four characters of the date because again, if you go back and look at the data, you'll see that the first four characters are the year, right? And this is not already sorted by year. You can see that we're jumping around weeks. And if you look through the data, you'll see that it's not in any particular sorted order. So I'm going to get the key. And then if that key already exists in the year's dictionary, then I'm going to increment the count for that key. Otherwise, I'll just initialize that key's value to be one.
>
> **[6:01](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/loading-and-parsing-json-data?u=76281980&t=361)** And then when we're done, I'm going to pretty print the contents of that dictionary and I'll give it a width of five to make it format nicely. Let's go ahead and comment out the previous code so that it doesn't interfere with our output. All right, and now let's run this again. So I'll go ahead and run the Python file in the terminal and sure enough, we can see now that we have the output here of my dictionary. So we've got full datasets for the years 2017 up to 2021. So 365 data points and there's 366 data points in 2020 because that was a leap year and it looks, like we've got about half that data for the year 2022. All right, so at this point, it looks like we're loading and parsing the data just fine. So we can continue with the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the chapter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (12), [JSON](../../Skills/Web%20Development/JSON.md) (8), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** python (12), make (2)
> **Tools:** terminal (12)
> **Env Vars:** json (5)
> **UI Navigation:** open the (2), click on (2)
> **File Paths:** parsedata.py (1), sample-weather-history.json (1)
> **Definitions:** is an  (1), is a  (1)
> **Analogies:** for example (1)

#### [Basic data operations](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/basic-data-operations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/basic-data-operations?u=76281980&t=0)** - [Instructor] So now that we've seen how to load and parse the dataset, let's try some hands-on basic data operations. So for example, let's find out which day had the warmest temperature in the entire dataset. So here in my editor, I'll open up basicdata.py, and you can see that I've already imported the [JSON](../../Skills/Web%20Development/JSON.md) and pretty print modules, and I've already got the code to open the file, and I'll be doing that through the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the course. There's no sense in making you type that each and every time. All right, so to find out the warmest day, we're going to use the max function which returns the largest value within a set of data. So I'll have my warmday variable, and that's going to be the result of max, and I'm going to find the maximum value in the weather data. Now I need to specify which value in the weather information object that I'm interested in. Since we want the warmest day, I'm going to use the tmax field, and I need to use the key parameter of the max function to index into that value. So the way that I'm going to do that is by using a lambda function. So I'll write key equals, and then I'm going to pass an inline lambda function, which takes one argument, X, and I'm going to look at the tmax field. So this function right here, my lambda function, is going to be called over and over again with each element in the weather data. So let's go back and look at the data. So here is tmax, right? And you can see here it's 50, here it's 27, and so on.
>
> **[1:35](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/basic-data-operations?u=76281980&t=95)** And again, these are all Fahrenheit temperatures. So each time through that loop, the X variable is going to be an entire one of these records. So I need to index into that record to get the tmax property, and then I'm going to print, and I'm going to use a format string for this, the warmest day was, and we'll output warmday, and remember, warmday is a full data record, so I'll get the date of that data record, at, and then once again, I'll put in, whoops, got to close that curly brace right there, warmday, and then we'll print out the temperature tmax, and we'll put in the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) degrees. All right, and then let's do the same thing to find the coldest day in the dataset. And in this case we're going to use the min function. So I'm going to copy and paste these two lines, but instead of calling max, I'm going to call min, and instead of warmday, what I want is coldday. So I'll name my variable coldday, and I'll just paste that in here, and of course we're looking for tmin not tmax. All right. Okay, so let's go ahead and run what we have. So I'll just go ahead and run this in the terminal. All right, and you can see that in the output we have the warmest day was, let's see,
>
> **[3:10](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/basic-data-operations?u=76281980&t=190)** it was in July of 2017 at 102 degrees Fahrenheit. And the coldest day was in January, 2018 at four degrees Fahrenheit. All right, so let's try one more basic data operation, and that's to calculate the number of a specific event. So let's go ahead and close this one. So let's figure out how many days in the dataset had snowfall. And remember, each date object has a snow property that lists the number of inches of snow for that day. And there's a variety of ways you could do this. You could loop through all the days and count the ones where the snow field was larger than zero, but in this case I'm going to use what's called a [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) list comprehension, and I cover these in detail in one of the courses I talked about earlier, back in my introduction video, which is Advanced Python Language Features. A list comprehension basically creates a new list in a very concise syntax. So I'm going to end up with a new list, I'm going to call it snowdays. And a list comprehension is basically contained within two brackets. And I'm going to create the comprehension by saying I want to get each day for the days in weatherdata if that particular day has a snow property which is greater than zero. And then I'll just print out the result.
>
> **[4:46](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/basic-data-operations?u=76281980&t=286)** Snow fell on, and then I can just get the length of snowdays
>
> **[4:55](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/basic-data-operations?u=76281980&t=295)** and I'll write days. All right, and just because we have the data, let's also print it out using the pretty print module. So I'll write pprint.pp, and let's just output the snowdays. All right, let's run the updated code. So I'm going to control slash each of these lines, so that we don't have any output that we're not interested in at the moment. And then I'll right-click, and choose Run Python In Terminal. And sure enough, let me scroll back up here, 'cause we've got some output, so we've got snow fell on 15 days, and if we scroll through the output, you'll notice that in each one of these records that's being printed out, we have a snow value that's going to be greater than zero in each case.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [JSON](../../Skills/Web%20Development/JSON.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **CLI Commands:** find (4), python (3)
> **Tools:** terminal (2)
> **UI Navigation:** open the (1), right-click (1)
> **File Paths:** basicdata.py (1)
> **Env Vars:** json (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)

#### [Filtering data](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/filtering-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/filtering-data?u=76281980&t=0)** - [Instructor] It's a very common scenario to only want to work on a subset of a larger dataset. In other words, you want to filter out the values that you're not interested in to be able to focus on the values that you are interested in. In a previous video, I showed one way of accomplishing this when we built a list of all the days that had snowfall in them using a list comprehension. In this example, we'll take a look at using the filter function in order to filter our dataset. So let's start with a simple example, and I'm going to open up the filtering data example here. So we're going to rewrite the list comprehension from that previous example using the filter function instead. So filter takes two arguments. There's the function to use as the filtering operation and the dataset to work on. The function that you're going to use as the callback returns true if the given value should be kept or false if it should be discarded. And this is a simple example, so I'm just going to use an inline lambda function to do this. Alright, so here's what we're going to do. We're going to do the same thing that we did in the last example. So I'm going to write snow days is equal to, and then I'm going to create a new list. So the filter function returns an iterator. So I have to wrap it in a list function in order to actually create the full list. So then I'll call the filter function itself. The first argument is going to be the callback that I want to perform the filtering with. So once again, I'll use a lambda because it's very short, and for each day I'm going to look at the snow property
>
> **[1:36](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/filtering-data?u=76281980&t=96)** and just make sure it's greater than zero. And I'm going to call the filter function on the weather data dataset. And then let's compare the size of the two datasets. So let's go ahead and print out the length of weather data along with the length of snow days. Oops. Alright, and you know what? Let's also just pretty print the resulting snow days. Okay. All right, so let's go ahead and run what we have. All right, so let me scroll back up. So you can see that the two datasets are very different in size, right? 1,977 total days versus 15 for the filtered dataset. And the list as it's being printed out is the same as it was for the previous example where I used a list comprehension. So now let's try something a little bit more complex. We're going to create a subset of the data that contains only summer days that had heavy rainfall, which we will define as at least one inch of rain, which is about two and a half centimeters for you metric folks out there. So let's go ahead and kill this terminal. Alright, so this kind of filtering operation is a little more complex than can fit into a list comprehension with a lambda function while still remaining relatively readable. So what I'm going to do is create an actual function
>
> **[3:12](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/filtering-data?u=76281980&t=192)** to perform the filtering. And this function is going to be called for each weather object in the dataset. So what we need to do is figure out is the given day within the summer months, and we're going to define the summer months as July and August, so I'm going to write summer months is equal to, and I'm going to give it a list. And inside that list I'm going to have two strings,
>
> **[3:42](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/filtering-data?u=76281980&t=222)** - 07- and -08-. Alright? And the reason I'm doing that is if we look at the weather data once again, you can see that the month is contained within the date string and it's surrounded by two dashes. So this will give me a way of making sure that only the summer months are being included in the dataset that I'm getting. So now I have to write the condition that checks to see if the rainfall was heavy and if the day was in the summer months. So if any, I'm going to use the any function here to make sure that a given month is in the days date string
>
> **[4:33](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/filtering-data?u=76281980&t=273)** for M in summer months. So what this statement does is it loops over each of the months I've defined in summer months. That's this loop here, the for M in summer months. Then it makes sure that that substring is in the weather objects date string. And that way I'm filtering out the dates that are not in the summer months, right? So if the month is in summer months and the day's precipitation value is greater than or equal to one inch, then I'm going to return true, because this is in fact a rainy summer day. Otherwise I'm going to return false, because it is not a rainy summer day. Then I'm going to use the filter function just like I normally would. So I'll write summer rain days is equal to, and once again I'll wrap the result in a list and I'll call the filter function. And my filter function is going to call is summer rain day. That's the callback. And I'm going to filter on the weather data. And then once again, let's go ahead and copy these two statements here. So we'll print out the length of the summer rain days result and then we'll pretty print
>
> **[6:15](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/filtering-data?u=76281980&t=375)** the summer rain days itself. All right, so let's go ahead and comment out the previous example and that's control slash to comment out multiple lines all at once. And then let's just run what we have. So I'll run [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) in the terminal. And if we scroll back up, we can see. Alright, so now we have 15 summer days where there was more than one inch of rain, and we have the list of those days. So you can see the precipitation value in each one of these is going to be greater than 1.0. So now we've seen two ways of filtering data. One using a list comprehension and one using the filter function. So I'd like to use list comprehensions when the filtering logic is simple and has maybe one or at the most two conditions. And I'll prefer using the filter function when the filtering logic gets more complex than can just fit into one line of code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **CLI Commands:** make (2), python (1)
> **Definitions:** is a  (2), in other words (1)
> **Cross-References:** previous video (1), in the last (1)
> **Tools:** terminal (2)
> **Speakers:** - [instructor] (1), - 07 (1)
> **Versions:** 1.0 (1)
> **Analogies:** just like (1)

#### [Solution: Filtering](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-filtering?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-filtering?u=76281980&t=0)** - [Instructor] Okay, so hopefully you were able to complete the coding challenge. Let's take a look at my solution and then you can compare it to your own. So for this challenge, we needed to filter the weather data down to a new list that only contained days that were cold and windy and rainy, as defined by the values that we were given, right? Temperature below 45, precipitation above 0.7 inches and the average wind speed above 10 miles per hour. And although I said rainy, I kind of actually meant either rain or snow. So you had to take that into account for your solution. So in my solution, I first import the [JSON](../../Skills/Web%20Development/JSON.md) module and then I open the data file, which as I said in the instructions, was located in the depths folder, and it's the same name as we've been given. So this code should look familiar, because we've been doing this throughout the course so far. And then I created a callback function that will be used by the filter function to filter out the days that don't meet the criteria, and I called that function is cold, windy, rainy day. So this callback function calculates the average temp by adding the Matson MN together and dividing by two, and the total precipitation was the snow plus the rainfall. And so it checks to see if the average temp is less than 45 degrees Fahrenheit, and the total precipitation is more than 0.7 inches, and the wind speed is more than 10 miles per hour. If it meets all those criteria
>
> **[1:33](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-filtering?u=76281980&t=93)** then we return true because it is a cold, windy, rainy day, otherwise we return false. And so once I have that filter function, I just need to perform the operation, and that's what I do here. I make a new list called blustery days, because that's what my mom used to call these, and I wrap the result in a list, because again, filter returns an iterator, and so I want to exhaust the iterator and actually build the list. So the filter function calls is cold, windy rainy day for every day in the weather dataset and then returns the list of blustery days, and you can see down here this is how my code will get called. So when I click test my code, you can see that the result is right and we can scroll through to see each one of those blustery days.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Versions:** 0.7 (2)
> **CLI Commands:** make (1)
> **Env Vars:** json (1)
> **UI Navigation:** open the (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Sorting data](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/sorting-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/sorting-data?u=76281980&t=0)** - [Instructor] It is probably not a surprise to you that sorting is one of the most common data operations, and that's what we're going to take a look at in this video. So let's open up the sortingdata starting point. And you can see here in the starting point code that I've already used the filter function which we saw used in a previous video in the chapter to create a subset of the larger weather data that only includes days on which there was snowfall. So this is going to give us a much more manageable set of data to work with in this example. There's a couple of different ways to sort data in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) using functions that are built into the language that don't require a third-party library like [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) or NumPy. The first is to use the top-level sorted function which takes an iterable object and creates a new sequence of sorted data. So let's try that one first. And I'm going to call that sorted_dataset. And I'm going to call the sorted function, and I'm going to sort the data set, and then I need to give it a key which indexes into the property that is going to be used to sort the information. So in this case, once again, I'll use a lambda because it's nice and short. So I'll have my lambda function which takes one of the days from the weather data, and I'm going to look at the snow property. And just for fun let's use the reverse parameter, and set this to be reversed.
>
> **[1:36](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/sorting-data?u=76281980&t=96)** So it should reverse the sorting order from largest to smallest rather than the default which is the smallest to the largest. So okay, let's go ahead and just run what we have right now. All right, and whoops, I have to actually print out the result, (instructor laughing) would be helpful if I did that so we could see what was going on. So let's go ahead and pretty print the sorted dataset. All right, there we go. Let's try this again. And so when we run the code, you can see if I scroll through the result, let's go up to the top here, that sure enough the data is sorted in descending order by the level of snowfall on that date. So here we have the snow is seven inches, and then we scroll down, we've got 2.5 inches, and 5.9 above that, so okay, we're heading down in the right direction, and then 1.9, and then 1.6, and you get the idea. Okay, so we're sorted in the reverse direction. The second way of sorting is to use the sort function that every list object has on it by default. So let's go ahead and close this terminal. And the difference is that the sort function sorts the given list in place. So sorted returns a new copy of the dataset, but we can also just sort things right in place. So I'll write dataset, which is the variable we have from the original list,
>
> **[3:10](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/sorting-data?u=76281980&t=190)** and I'll just call dataset.sort, okay? And in this case, the key comes first. So I'll write key equals, and once again, I'll have a lambda function that looks at the snowfall, and this time I'll just explicitly set reverse to be false. And then we'll pretty print the result. And like I said, the main difference between these two functions is that while sorted returns a new sequence, the sort function operates on the sequence in place, in other words, it modifies the existing dataset instead of returning a new sorted copy. So let's go ahead and comment out the previous example, and then let's run. All right, and now you can see if I scroll back up, all right, so now we have the snowfall sorted in ascending order. So we're starting off with 0.2 inches, and then we go to, that's another 0.2, and then 0.3, another 0.3, right? You can see that it's getting larger. So at this point you might be wondering if you can sort on more than one data point at a time. And the answer is yes, you can. Let me go ahead and close this terminal. So let's imagine that we wanted to sort on multiple dimensions, first by the amount of snowfall, and then by the average wind speed.
>
> **[4:44](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/sorting-data?u=76281980&t=284)** So for this example, I'm going to use the sorted function, but you can also use the built-in sort function for this if you want. The main difference is to create a two-item tuple that you're going to return from the key function. So let's comment out this previous one, and then let's copy and paste these two lines, and then uncomment them. So I'm going to sort on the dataset, and the key is going to be a lambda, but instead of just looking at the snowfall, I'm going to create a tuple here, and the two-item tuple is going to consist of snow and the average wind speed. And in this case, I'll also just leave off the reverse argument to make this easier to read, and then we'll print out the sorted dataset. So this will result in the data being sorted on snowfall first, and then the average wind speed second. All right, so now we've commented out the other two examples, yes, we have. All right, let's go ahead and run this. Scroll back up to the top. All right, so now we can see that the data is sorted by snowfall first, right? So the first two items, let me make this a little bit taller so we can see more data, and I'll scroll down a little bit. So the first two items both have a snowfall of 0.2, the wind speed is then sorted in ascending order because reverse is false. So you can see that average wind speed here is 3.58,
>
> **[6:19](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/sorting-data?u=76281980&t=379)** and then average wind speed here is 5.59. And then we increase the snowfall amount, so it's no longer 0.2, now it's 0.3, all right? Actually the next two data points are the same, so if you look, they're both 0.3, and then again average wind speed is sorted in ascending order. So that's how you can sort on multiple dimensions using the sort function.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **Versions:** 0.2 (4), 0.3 (4), 2.5 (1), 5.9 (1), 1.9 (1)
> **CLI Commands:** make (2), python (1)
> **UI Navigation:** scroll down (2), go to (1)
> **Tools:** terminal (2)
> **Code Identifiers:** sorted_dataset (1)
> **Cross-References:** previous video (1)
> **Definitions:** in other words (1)

#### [Transforming data](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/transforming-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/transforming-data?u=76281980&t=0)** - [Narrator] There are going to be times when a dataset isn't in the format that you'd like it to be in. And so you need to transform the data to better fit your needs. So one of the ways you can do this is by using the built-in map function. Let's open up our transforming data start code. And let me scroll up a little bit here. Alright, so the map function takes each value in a data set and maps it to a new value, and returns a new sequence of the transformed data. So in this example, we're going to use the map function to change our existing data from imperial units to metric units. And you can see here in my starting point code that I already have a few utility functions that convert imperial units, like temperature, and length, and speed into metric units. And so what I need to do is use the map function. And I'm going to scroll down a little bit here, and you can see I have a callback here called ToMetric. So my map function is going to call this function ToMetric which will convert each data item. So let's go ahead and put that code in. I'll make a new variable called metric_weather, and I'm going to assign that the result of the map function. And the map function takes two arguments. The first is the callback to use, so that's going to be ToMetric, and the data to operate on, so that's weatherdata. And, like in other examples, the map function actually returns an iterator. If I want to create an actual list I'll have to wrap this in a call to the list function. And then after we do that, we're going to pretty print
>
> **[1:35](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/transforming-data?u=76281980&t=95)** the first item from the weatherdata, and we'll do the same thing with the first item in the metric_weather so we can compare them side by side. Alright, so now let's fill out the ToMetric function. And before I do that, I want to just quickly point something out. You probably noticed I imported the copy module up here at the top of my code. The reason I'm doing this is because I want to leave the existing data in the weather data set untouched and create a new data set that has the metric values. So what that means is I can't just simply modify each data item in place because then the original data set would be modified. So the copy module gives me an easy way to create a copy of an existing object in memory. So what I'm going to do is in my ToMetric function I'm going to make a variable called new weather data item. And that's going to be the result of calling the copy modules copy function on the weather data item that's coming in. So this makes me a new copy, and now I can modify the values of the new one without having to worry about affecting the old one. So I'll operate on the tmin temperature, and that's going to be the result of calling the to Celsius function on my weather data items tmin. And I'll do the same thing for tmax. All right.
>
> **[3:07](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/transforming-data?u=76281980&t=187)** And then we have some length, right? So we've got precipitation, and snowfall and such. So we've got precipitation, snowfall, and snow that's on the ground. So I'll make each one of these, SNWD, and we'll copy and paste these properties in. And I'm converting inches to millimeters, so I'm calling the ToMM function that I wrote, and we'll just go ahead and copy and paste those. You can see ToMM is right here. And then the last one is the wind speed. So that is the average wind speed, and that's going to be the result of calling ToKPH. And then I just return the new weather data item. Okay, so we have our ToMetric function, we've got our conversion functions, we've got the map function written. All right, let's go ahead and run the code. And when we run the code, we see, oh no, there's an error. So why is there an error? I'll explain why. So let's go ahead and close this terminal and let me bring it back down to the middle. So let's go to the data. And inside the data let's do a find, and let's look for null. And if you look, you'll see that there are several instances where there are null values in the data instead of valid values.
>
> **[4:42](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/transforming-data?u=76281980&t=282)** And you know, this happens a lot in the real world. Sometimes, you know, data sets just aren't perfect and you need to account for that in your code. So let's go ahead and close this. So what we're going to do is we need to check each data item to see if it's null before we try to operate on it. So in my conversion functions what I'm going to do is put in some just checks. So for example, I'll assign F the value of zero. If the incoming F is none, else, it'll just be F. Okay, and I'll do the same thing with the length function, and then I'll do the same thing with the speed function. So the approach I'm taking here is just substituting a zero value in cases where the data is null. All right, so now we fixed that problem. Let's go ahead and run the example again. Alright, so now we have some valid data and we can see that if we compare the first item in each dataset, you can see that the first item in the weather data is still in imperial and the second one is now in metric. But you can use the map function for more than just converting values. You can actually change the format and the shape of the data itself. So let's go look at an example of that. Let's close the terminal. So let's suppose that I wanted to create a dataset that contained the date field and the average value of the min and max temperatures for a given day.
>
> **[6:16](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/transforming-data?u=76281980&t=376)** So there's two separate things here. First, I want the data as a tuple instead of an object. And since there is no existing data field for the average temperature, I have to calculate it. So let's create a map function statement that would do this. So I'm going to create a variable called tuple_data, and I'm going to call map, right? And the callback function is going to be an inline lambda which will return a tuple, right? So I got to have my parentheses. And it's going to return a tuple of the date. So that's going to be the date field. And the result of calling the average temperature, you can see I have an average temperature function right here that just calculates the average of two temperatures. So I'll call average temp on the tmax and the tmin.
>
> **[7:17](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/transforming-data?u=76281980&t=437)** And then of course that's going to operate on all of my weather data. And then of course I have to wrap this in a list if I want an actual list. Let's go ahead and give us some more room to read here. Alright, and then once we've done that let's print out the first five tuples so that we can see what the data looks like. So I'll use my pretty print function and we'll print out the first five items in the tuples. All right, and let's go back and comment out our previous example. Alright, let's go ahead and run this. Alright, and you can see the result is that now I have a data set that contains tuples instead of objects, and only has the date and the average temperature values. So the map function is a really versatile tool to have in your data operations toolkit right alongside filter and sort.

> [!info]- Semantic Content
>
> **CLI Commands:** make (3), find (1)
> **Code Identifiers:** metric_weather (2), tuple_data (1)
> **UI Navigation:** scroll up (1), scroll down (1), go to (1)
> **Tools:** terminal (2)
> **Env Vars:** snwd (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [narrator] (1)

#### [Solution: Transforms](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-transforms?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-transforms?u=76281980&t=0)** - [Instructor] Alright, so for this challenge, we needed to transform the weather data records from dictionaries with keys and values into a list of tuples that contained only the existing date value and a string that indicated whether the day was cold or warm or hot. So here is how I solved this challenge. First, my code needs to import the [JSON](../../Skills/Web%20Development/JSON.md) module and then open the weather data file. And we've already seen how to do this. So once I've got the weather data loaded and parsed, I'm going to perform the transformation, and I use the map function, which you can see down here, right, and the map function requires a callback to perform the actual mapping from the dictionary format to the tuple format. So my callback function basically takes an average temperature and describes it as a descriptor. So the first line of code calculates the average temperature and then based upon whether that average temperature is below 60 or between 60 and 80 and above 80 assigns a string to describe that day. So this is going to happen for every element that's in the weather dataset. And so, when I'm done, you can see that I'm returning the date and the descriptor, which is what was required by the criteria that we were given. So at the end of this,
>
> **[1:32](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-transforms?u=76281980&t=92)** I have a list that has been transformed from dictionaries with keys and values into tuples with strings. So if we go ahead and click on Test My Code, you can see that that is the right answer and we can scroll through, there's a lot of them, but we can scroll through them and you can see that each one of what was previously a dictionary and, you know, keys and values has now been transformed into a date and a descriptor based upon whether the day was cold or warm or hot.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **UI Navigation:** open the (1), click on (1)
> **Env Vars:** json (1)
> **Speakers:** - [instructor] (1)


### 2. Working with Collections

[↑ Back to Table of Contents](#table-of-contents)

#### [Count values with defaultdict](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/count-values-with-defaultdict?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/count-values-with-defaultdict?u=76281980&t=0)** - [Instructor] Earlier in the course, I used a dictionary to count how many years of weather data we had in the dataset. It's actually pretty fairly common to use dictionaries to keep track of counts and other data values and then map them back to keys. There's a data structure in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md)'s collections module that makes this process a little bit easier called the defaultdict, and I'm going to be using it in this example. So let's go ahead and open up the defaultdict source code file. And I covered the theory behind this collection class in my [Advanced Python- Working With Data](../Software%20Development/Advanced%20Python-%20Working%20With%20Data.md) course. So if you want to learn a little more about the basics, be sure to check that out here in the library. So in order to use this class, I first have to import it from the collections module that is a part of the Python standard library. So from the collections, I'm going to import defaultdict.
>
> **[0:58](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/count-values-with-defaultdict?u=76281980&t=58)** Oh, oops, one too many Ls. Here we go. Now, there's nothing really special about defaultdict compared to a regular dictionary, except that it provides default values, so you don't need to write as much code to check to see if keys already exist in the dictionary. It's a nice little convenience feature. So when you create a defaultdict, you specify what data type you want the default values to be, okay? So for example, to create a dictionary that has integers as the default type, I can specify that in the constructor. So let's go back and rewrite the previous example that we did earlier in the course where we counted how many years of data we have. And so I'm going to have years, and earlier in the course, I just did this, right, and made an empty dictionary. In this case, what I'm going to do is create a defaultdict and I'm going to supply an integer as the argument. So now the default values of my dictionary will be integers if they don't already exist as a key. So now I can just operate on key values even if I haven't already added them to the dictionary. All right, so let's do that. Let's go ahead and rewrite my previous code. So for each day in the weather data, what I'm going to do is the same thing I did before. I'm going to create a key, and the key is going to be based on the date string that is in each one of our data items,
>
> **[2:33](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/count-values-with-defaultdict?u=76281980&t=153)** and just like I did the last time, I'm going to get the first four characters. And just as a reminder, the reason for that is because the first four characters in every date is the year, right? So I'm going to be counting how many data points we have for all the data, and then group them by years. But whereas in the past, I had to check to see if the key was already there in order to know whether to initialize it to one or increment the count. Now I can just do this. I can just simply write years sub key plus equals one. I don't have to do the check like I did before, and then just like last time, I'm going to pre-print the result. And the reason this works is because if there isn't already a value assigned to a particular key in the dictionary, then the value will be assigned the default integer of zero. So unlike earlier, I don't have to first initialize the value to one before I simply start incrementing the count. So let's go ahead and run this. And you can see that the result shows the same value as we saw at the start of the course. We have about 5 1/2 years of data, and the number of data points in each year is the same as we saw previously. Now, you're not limited to using integers as the default values in a default dictionary. So let's create one that uses lists for default values for each key. So let's go ahead and close this terminal. And what I'm going to do now
>
> **[4:05](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/count-values-with-defaultdict?u=76281980&t=245)** is I'm going to write years and months and I'm going to create a defaultdict, and every defaultdict is going to have a list as its default value, and what I'm going to do is comment out my previous example. All right, so now let's do something a little bit different. Now let's write for D in weatherdata. What we're going to do is have the key equal to, and just like previously, we're going to look for the date, but now we're going to take the first, well, the first seven characters, right? This will give me the year, the dash, and the month, all right? And then I'll have years_months for each key. Now remember, this is not an integer now, it's a list, so the value that maps to this key is a list, so now I can do things like .append, and then once I've done that, I'll print out the length of my years_months dictionary, all right? And now let's run the updated code. All right, and now you can see that I've got 65 keys, each representing a year and a month, and the value of that key is a list of days for that month. So I've got 65 individual months of data. So now that we have the data organized into individual months,
>
> **[5:39](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/count-values-with-defaultdict?u=76281980&t=339)** we can ask things like, hey, what were the coldest and warmest days of each month? So let's go back to the code. And now once I've done that, you can see that I have some code here already for calculating the warmest day and the coldest day. So all I need to do is loop over each key in the dictionary and pass each value, which is a list of the days in that month to these two functions. So I'm going to loop over, so for the year_month,
>
> **[6:15](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/count-values-with-defaultdict?u=76281980&t=375)** and the list of days in my years_months items,
>
> **[6:24](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/count-values-with-defaultdict?u=76281980&t=384)** and remember that on a dictionary, the items function returns the key value pair. The key is going to be the year and the month string, and the day list is going to be the list that I created for each one. So now I'm going to print that the warmest day in the year_month was,
>
> **[6:50](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/count-values-with-defaultdict?u=76281980&t=410)** and I'm going to call warmest day, and pass in the daylist, and I'll do the same thing, so I'll copy and paste this, and I'll do the same thing for the coldest day, and instead of calling it warmest_day, I'm going to call coldest_day. Oh, oops, there we go. All right, so my updated code now takes each list, passes it to each one of these two functions, and you can see that inside the warmest day function, I'm using the max function given the month that I've been handed. This is a list of days. So over that list, I'm going to figure out what's the maximum value of, in the case of the warmest day, I want tmax, and in the case of the coldest day, I'm going to use the min function. And again, I'm looking over the list of days for that month and I'm looking at the minimum temperature as the key for the min function. And so I'm going to return a tuple of the date and the coldest temperature, and here I've got the date and the warmest temperature. So now let's run that updated code. And what this should produce is a list of the 65 coldest and warmest days in our dataset. So if we scroll back up, right, you can see that yep, we've got 65 datasets, and so I've got the warmest day in January
>
> **[8:24](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/count-values-with-defaultdict?u=76281980&t=504)** and coldest day in January of 2017, and then we move on to February and then March and then April, and we do that for each one of the years in the dataset, right? So this actually gives me every month of every year, and you can see that because I've returned a tuple from those callbacks, I've got the string and the warmest and coldest temperature in each one of those months.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3)
> **Code Identifiers:** years_months (3), year_month (2), warmest_day (1), coldest_day (1)
> **Cross-References:** earlier in (3), as we saw (2), go back to (1)
> **Definitions:** is a  (6)
> **CLI Commands:** python (3), ls (1)
> **Analogies:** just like (3), for example (1)
> **Best Practices:** the key is (2)
> **Tools:** terminal (1)

#### [Using the reduce function](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/using-the-reduce-function?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/using-the-reduce-function?u=76281980&t=0)** - [Instructor] Sometimes when you're working with a large dataset you want to be able to reduce that dataset down to a single value. And we've already seen some use cases for that right here in this course. So for example, the min and max functions, essentially, just reduce the entire dataset down to a single minimum or maximum value. So for example, we used min and max to find the coldest and warmest days out of the entire dataset. [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) provides a more generic function called "Reduce" that does the same thing using a callback function that your code provides in order to control the reduction algorithm. So let's start with a simple example to see how this works. Let's go ahead and open up the reduce func example. And what I'm going to do first, is import the reduce function and that's in the func tools module. So I'm going to write from functools, import reduce. So let's use this reduce function to calculate how much total snowfall is in the entire dataset. So first I'll write the expression and then I'll explain how it works. Alright? So I'm going to have a variable named total_snowfall and that's going to be equal to the result of a reduced function. And the reduced function takes a couple of things. First, it takes a callback, which in this example I'm just going to use a lambda function because it's a really short expression. The lambda function takes two arguments the accumulator and the current element. And what I'm going to do is either return the accumulator
>
> **[1:39](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/using-the-reduce-function?u=76281980&t=99)** plus the snowfall of the current element, okay? And I'm going to do that across the entire weather data. And I'm going to provide an initial value for the accumulator, which is zero. And then I'm going to print the total snowfall. Alright, so the reduce function takes a few parameters, right? So this first parameter right here is a callback function and I'm just using the lambda, right, 'cause it's pretty simple. So there's two arguments. One is called the accumulator and the second is the current element in the dataset. So this function will be called for every item that's in weather data. The last parameter is the initial starting value for the accumulator, which I'm starting off at zero. And you can think of the accumulator as the value that gets remembered between each time this callback function gets invoked. In this case, we're just adding the value of the snow property of the current element to the existing accumulator value. So this is kind of a fancy way of writing a summing function, right? It's just summing up all the snowfall of the dataset. But before we run this let's also write a slightly more complex version that calculates the total precipitation, right? That's the both rain and snow of the dataset. So I'm going to copy this and I'm going to rename it total_precip.
>
> **[3:16](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/using-the-reduce-function?u=76281980&t=196)** And in this case I'm going to add both the elements of snow.
>
> **[3:24](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/using-the-reduce-function?u=76281980&t=204)** And so I'll copy that, plus the PRCP data. So now I'm adding both the rain and the snow and I'm going to leave the other arguments the same, right? It's the same weather dataset and I'm also starting the accumulator off at zero. And then once again, I'll print out the result. So I'll just copy and paste this. Alright, so let's go ahead and run this. All right, and you can see that the result is 25.38 total inches of snow and 293 point, eh, almost eight inches of total precipitation. Now you might be thinking, okay, well if this is just a glorified version of the sum function then why do I need it? And the answer is that the reduced function can do a lot more than just adding up a total. Remember, the purpose of reduce is to take the total data set and reduce it down to a single value and the algorithm you use is up to you. So let's try something a little more complex. Alright, so let's figure out, what was the warmest day in which it snowed. Alright? And let me give us a little bit more room to work in here. So first let me comment out these previous examples. Alright, so in order to find out the warmest day in which it snowed what I need to do is figure out the highest tmax value for all the days where snow is greater than zero.
>
> **[5:01](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/using-the-reduce-function?u=76281980&t=301)** Now you can write this using some of the more complex algorithm but I'm going to do this using the reduced function because the reduced function was built for situations like this. Alright? I'm also going to use a regular function for the callback called warm_snow_day because it's a little too complex to fit in a lambda function. So I've already declared my function here in the code. And the callback takes two arguments. So the algorithm will check to see if the current element has a snowfall amount and then we look at the tmax value for that element and return the current element if it's tmax temperature is larger than the one that's currently in the accumulator. So here's what that looks like in code. I'm going to return the element, if the elements snow value, right? The snowfall for that day is greater than zero, all right? So if the snowfall for that day is greater than zero we've got to check the temperature. So, and, I'm going to check the tmax temperature. So if that is already greater than what's being passed in the remembered value, the accumulator. Otherwise I'm going to return what's in the accumulator. So each time through my loop, I'm going to be taking a look at the current remembered value and I'm going to have to decide do I replace this with another one, or do I keep the current value that's in the accumulator, right? So I replace it if the element that I'm looking at
>
> **[6:35](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/using-the-reduce-function?u=76281980&t=395)** has snowfall that's greater than zero, so, it snowed that day, and if the maximum temperature for that day is larger than the current maximum temperature I'm looking at, then I know I need to return this new element. Otherwise I just keep the accumulator the way it is. So next, I need to define a starting value for the reduce function in order to give it a starting point to compare against all the other elements. And I've created this notion of a zero date right here. It's got all zeros in the value. So it's just a starting point for the reduce function to start off with. So now all I have to do is call a reduce function. So I'll make a variable call result and I'll call reduce. And my callback is called warm snow day. I'm going to give it the weather data and I'm going to give it my start val. And then once that code has executed I'm going to print out the result. So I'll print the result. Oops. And I'll print out the date value of that result with a temp of, and that's going to be the results. Oops. Tmax and snowfall, and that's going to be the result. Snow property. Alright, so remember I'm reducing the data set of the weather data down to a day.
>
> **[8:07](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/using-the-reduce-function?u=76281980&t=487)** So this result is going to be one of these objects. I'm going to take this entire list of objects and reduce it down to just one of these guys. And that will be the warmest day on which it snowed. All right, so let's go ahead and run our code. And as a result we can see that the warmest day on which it snowed for this particular location was March 24th, 2018. And it got up to about 50 degrees that day but they still managed to have 0.2 inches of snow. So you can see that the reduced function can be pretty powerful. And what makes it so useful is that it gives us a way of remembering values between each iteration of the callback and the callback allows us to provide an algorithm that is as complex or as simple as we need it to be.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **CLI Commands:** find (2), python (1), make (1)
> **Code Identifiers:** total_snowfall (1), total_precip (1), warm_snow_day (1)
> **Definitions:** is called (2), is a  (1)
> **Versions:** 25.38 (1), 0.2 (1)
> **Analogies:** for example (2)
> **Env Vars:** prcp (1)
> **Tools:** notion (1)

#### [Grouping data](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/grouping-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/grouping-data?u=76281980&t=0)** - [Lecturer] In this example, we're going to take a look at how to organize data into groups. And we're going to do this by making use of the group by function that is included in the inner tools module from the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) Standard Library. So let's open up our grouping code and let's go ahead and give ourselves some more space to read. Alright, first, let's start by importing that function from itertools. I'm going to import group by. Alright, so now I can make use of that function. But before we see how to use group by to form data groups, let's take a look at how you would achieve this manually by using a regular dictionary. So you can see I already have some code here that gets all of the weather data for the days in 2022. And then scroll, down a little bit here, I'm using a default dictionary with a list type for each of the default values. And then on this line here I have some code that creates a sorted list of all of the data, and it's sorting by the amount of precipitation. And this is important because the group by function needs to have the data sorted, but we'll get to that in a moment. So basically each key in the dictionary is going to represent the amount of precipitation, and that's how I'm going to create my groups. I'm going to create a group for each precipitation amount and then the dates that have that amount of precipitation. So I loop over each day in the year.
>
> **[1:34](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/grouping-data?u=76281980&t=94)** And then in my dictionary I'm using the key for that precipitation amount. And then because the value for that key is a list, I just append onto that list the date that has that precipitation amount. And then when I'm all done, I print out the length of the precipitation group dictionary, so we know how many groups there are. And then I'm just going to pretty print out the contents of that dictionary. So before we do anything else let's just run what we already have. And when I run this, let me make this a little bit bigger and scroll to the top. So when I run this, you can see that there were 36 total precipitation groups, along with the details of each group. So the largest group is no rainfall at all, and there's a whole bunch of days on which there was no rain. And as we scroll down, you can see that some of the other groups start to come here into focus. So we have the days on which there was 0.01 inches and then the days on which there was 0.02 inches and so on. So what we've effectively done here is group a set of dates for a given year by the amount of rain on those dates. Alright, so now that we're going to do that using a dictionary let's see how to accomplish the same thing using the group by function. So let's go back to the code and close the terminal. So I'm going to comment out this example here so it doesn't interfere with us. But I'm going to leave this line of code in because the group by function needs sorted data like I explained earlier.
>
> **[3:08](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/grouping-data?u=76281980&t=188)** So I'm going to write the statement that uses the function and then I'll explain how it works. So I'm going to have the results in a variable named grouped and then I'll call group by. And that will work on the year data. And then I have to give it a key, and you've probably used to seeing this by now, I'm going to specify the property that we should be grouping the data on and that's going to be a Lambda function, and I'm just going to point it to the precipitation amount, so now it knows how to create the groups. Alright, so the group by function works on an iterable sequence of values and that's this year value. And like other functions we've seen so far we have a key value that specifies how to group the data. So in this case we're using the precip property that represents the amount of rainfall on a given day as the value that the elements in the weather data should be grouped by. Now, the group by function itself creates and returns an iterator. It doesn't actually create a dictionary. It creates an iterator that returns keys and values for each grouping. So we can use those keys and values to create an actual dictionary that we can work with. And one other little feature. The values for each one of those keys, in other words, the group of dates, that itself is going to be an iterator. So what that means is in order to create an actual dictionary containing the grouped data like we did earlier we have to iterate over the results of the group by function and populate the dictionary. I'm not going to leave it just like it is. So to do this, I'm going to embed this whole statement inside a dictionary comprehension.
>
> **[4:43](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/grouping-data?u=76281980&t=283)** And remember, I cover how to write dictionary comprehensions in my other course, Advanced Python Language Features, so if you want to understand a bit more about the theory, you can go look that up. What I'm going to do is put all of this inside curly braces and then I'm going to create a dictionary. So there's going to be a key. And then I have to exhaust the values iterator for each of the dates. And then I have to loop over the results of group by. So it's going to be for K and V in group by. So this loop for K V in group by. So this creates the loop over all the keys and values and the result, and then I'm creating a dictionary with the key which is the precipitation value. And then the V in this argument is going to be the list of dates. But remember that list is an iterator. So I have to use this list function to actually create the real list. And then once I've done that, I can now print out the results like I had them before. So what I'm going to do is print out the length of, oh. I got to put that in curly braces. The length of grouped, and that's going to be the number of total precipitation. Make sure I spell it right groups. And then we can print each group. And I'm going to make it a little easier to read this time around by converting each of the date objects into tuples, which we kind of saw a little bit earlier in the course. So for each key and data pair
>
> **[6:17](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/grouping-data?u=76281980&t=377)** in my new dictionary grouped items, I'm going to print, oops, and print out the precipitation which is going to be the actual key value. And then the number of days in that group is going to be the length of the data. And then the days themselves, this is where I get a little bit fancy. I'm going to actually use the map function to create tuples out of the data rather than having all the objects. So I had to do this before, right? So I'm just going to have my Lambda function map to a tuple.
>
> **[7:06](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/grouping-data?u=76281980&t=426)** And of course I have to enclose that inside a list call because map returns an iterator. All right. And that should be all that I need to do. All right, so let's go ahead and run our result. And now you can see, let's give us some more room here. All right, so same result, right? 36 total precipitation groups. And now you can see here's group zero and the days which are now just mapped to dates, right? And I guess they're not really tuples, they're just dates. And so now we can see precipitation group 0.1 inches, 0.02 inches, and we've got five days in this group and three days in this group. So we have the same results as we did before in a slightly easier to read format. And if we go back to the code, you can see that this one line of code right here replaced this multiple lines of code up here. So by using the group by function, I was able to pretty easily create a dictionary that shows all of my group's data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2)
> **CLI Commands:** make (4), python (2)
> **Versions:** 0.02 (2), 0.01 (1), 0.1 (1)
> **Cross-References:** go back to (2), earlier in (1)
> **Definitions:** is a  (1), in other words (1), is an  (1)
> **Tools:** terminal (1)
> **UI Navigation:** scroll down (1)
> **Analogies:** just like (1)

#### [Working with dates](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/working-with-dates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/working-with-dates?u=76281980&t=0)** - [Instructor] Almost nobody likes having to work with dates and times. And there's a lot of reasons for this. Dealing with time zones and leap years and performing calculations on date and time values, these are not exactly high on the list of most developers' ideas of fun. Now thankfully [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) makes this process a little easier because it includes a module for working with these data types as a part of the standard library. And in this example we're going to use that module to perform some basic date-related operations. Now remember that each item in our dataset contains a date that corresponds to the days' weather information. So let's take a look, and remember, let me scroll the way up to the top here. So we've got this date that's attached to each one of the weather data items for our day. Now we can use this date time module to convert this string into a date object fairly easily. So let's open up our date ops PY code. And in order to operate on dates and times, I'm going to import from date time, I'm going to import the date class. All right? And we can convert the string directly into a date object. Now in this case, since the date string is already in the standard ISO format, we can use the from ISO format function. So I'm going to write a piece of code that says date obj and I'm going to call,
>
> **[1:34](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/working-with-dates?u=76281980&t=94)** on the date class, I'm going to call from ISO format and I'm going to pass in the first item in the weather data and I'm going to get the date property from that item. And then I'll just print out the date object. All right. Now once we have an actual date object in Python, we can get other information such as the day of the week that this date represents. So let's go ahead and print that out. Let's print out date object. And then when I hit the dot, you can see that there's a whole bunch of functions that we can now call on this date object. So I'm going to write weekday, and that's a function actually. So let's run the code we have so far and see what we've got. And you can see that what I run this, I have my date object in ISO format. And that day represents day six, which is a Sunday. And if we look back in the code, right? We can see that zero is equal to Monday and six is equal to Sunday. So January 1st of 2017 was a Sunday. All right? So now we can run more advanced calculations like figuring out what was the warmest weekend day in the dataset. All right? So let's go back to the code. First, I'll define a function that determines whether a given day represents a Saturday or a Sunday
>
> **[3:08](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/working-with-dates?u=76281980&t=188)** because that's the weekend, right? So I'll write a function called is weekend day and that's going to take a day argument. And I'm going to construct the day from that string. So I'll call date dot from ISO format on the date string for the given day. And then I can return whether day dot weekday is equal to five or day dot weekday is equal to six, right? Because five is Saturday, six is Sunday. And now we have a way of checking out whether that given date object represents a weekend day. Then I need to use the filter function, which we saw earlier in the course. That's going to narrow the dataset down to just containing weekdays, all right? So what I'm going to do now is write weekend days is equal to and I'm going to create a list that's going to create a list out of the results of a filter function. That filter function is going to call is weekend day as the filtering algorithm and it's going to operate on the weather data. So once I have a set of weekend days, I can use the max function that we've also already seen previously in the course to figure out what the warmest weekend day was. So I'll write warmest day is equal to max
>
> **[4:45](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/working-with-dates?u=76281980&t=285)** and I'll pass in my weekend days. And the key function is going to be a lambda that takes the max temperature of that day. And then we'll print out date dot from ISO format, the warmest day and we'll get the date property from that day and we'll call str F time because I'm going to format the date a certain way, right? And I'm going to format it using percent A, comma percent D, percent B, percent capital Y. And this basically gives me a way of printing out the date string in a format. So it's going to be the name of the day and the day of the week and so on. So it's a nice output format for the day string, all right? So let's go ahead and run this and we can see that the warmest weekend day was Sunday, the 23rd of July in 2017. One more thing, there's another class in the date time module that is also really useful for performing date-related calculations called time delta. And you can find it in the Python documentation. All right? So this is the documentation for time delta objects. You can read about it on your own time.
>
> **[6:19](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/working-with-dates?u=76281980&t=379)** Let's go ahead and close the terminal. So the time delta class makes it easy to perform date calculations like addition and subtraction and to represent spans of time. So let's try another operation on our data set. We're going to find the coldest day of the year and then calculate the average temperature for the week following that coldest day. Now we already know how to find the coldest day using the min function. And we just saw how to convert the date property to a date object. So I'm going to uncomment that code here. All right, so now we need to look up the data for the next seven days. So first I'll create some variables and I'll create the average temp and I'll initialize that to 0.0. And then the next date is going to start off as the coldest date. So now I'm going to have a loop that runs seven times once for each day. So I don't care what the value is, but I'm going to run this for seven times. What I'm going to do is calculate the next date. So I'm going to add a time delta. And time delta takes an argument named days equals one. And the reason why I'm getting this little squiggle is because I have to remember to go import the time delta from my date module. So I'm going to import time delta here. All right. So I'm calculating the next date is going to be one day
>
> **[7:57](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/working-with-dates?u=76281980&t=477)** from whatever the next date currently is. And it's starting off as the coldest date 'cause we want to look up the seven days following the coldest date. So now we're going to find the weather data object for that date in the list. And we have to do this because the weather data that we have is not currently sorted by anything. So I'm going to look up the next day. I'm going to use the next function to operate on an iterator I'm about to create. So I'm going to create an iterator that looks for each day for day in weather data if the date property of that day is equal to and I'm going to convert the next date to a string. And the next function requires me to pass in a default value. So if the iterator is exhausted, if I get through all the days, then I return none to indicate that the iterator is finished. All right? So this will give me the next day in the set. Then I need to calculate the average for the week. So now that I have my range of seven days, I'm going to calculate that average temp is equal to average temp divided by seven. Oh, actually I have to update my average temp in order to do this, don't I? So the average temp, I'm going to add the average of the min and max temperatures for each one of these days.
>
> **[9:37](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/working-with-dates?u=76281980&t=577)** I can't forget to do that. So as I'm going through this seven day range, I calculate the next date in the dataset, find that day in the actual weather data by comparing the next date to each date, and then add the average temperature for that day to the running total of average temperatures. Then I can calculate the average temperature for the entire week. And I can print out the average temp for the next seven days was average temp. All right. So, let's comment out the previous example. All right, so let's go ahead and comment this and comment that and let's comment this. All right. And now let's run what we have. All right. So, we can now see that the average temp, so the coldest day of the year, right? Was, let's see January 7th and it was four degrees Fahrenheit. And we're getting this output from the code that I uncommented at the beginning of the exercise. So we calculated the coldest day and then we can see that the average temp for the next seven days was 45 degrees Fahrenheit.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3)
> **CLI Commands:** find (5), python (3)
> **Env Vars:** iso (6)
> **Cross-References:** go back to (1), earlier in (1)
> **Versions:** 0.0 (1)
> **Documentation:** the documentation (1)
> **Tools:** terminal (1)
> **Definitions:** is a  (1)

#### [Solution: Collections](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-collections?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-collections?u=76281980&t=0)** - [Instructor] For this challenge, we needed to use the reduce method to find the most miserable day within the entire set of weather data. And the formula that we use to calculate this day takes into account, the wind speed maximum temperature and rainfall for each day, and gives us some combination of a hot, rainy, windy day. So my code to solve this problem uses two additional functions. The first function calculates the misery score for any given day within the dataset using the formula that we were provided as part of the instructions. All right, so that's this function right here, misery score. And one of the things you might have run into when working with this formula was that there were some instances in the dataset where the wind speed was none. And in those cases, I just set the wind speed value to zero. I then use this function in the callback that the reduce function, which is right here, uses to reduce the dataset. So you can see that here, what I'm doing is calling reduce and I'm ranking each day in the weather data. The day rank function takes both the accumulator and the element, which we've seen this before in the reduced function, and it returns the accumulator if the misery score of the accumulator, which is the current day, is greater than or equal to the misery score of the day that we're looking at. Otherwise, it returns the day that we're looking at because the misery score is larger.
>
> **[1:34](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-collections?u=76281980&t=94)** So it's going to run through all the data and calculate the misery score for each day and then just figure out which one is the most miserable day. So that was this function here. And all this function does the misery score function. It just calculates a value based upon the formula that we were told to use. So when I run this code, you can see, that the most miserable day in the data, at least according to this formula, was this day here in April where the max temperature was 82 degrees Fahrenheit. The wind speed was, let's see, 12 and a half miles per hour and there was more than 3.3 inches of rain. That's like what? Almost eight, I guess, centimeters for you metric people. So yeah, that sounds pretty miserable to me. So try playing around with different values for the formula and see how the results are different.

> [!info]- Semantic Content
>
> **CLI Commands:** find (1)
> **Versions:** 3.3 (1)
> **Speakers:** - [instructor] (1)


### 3. Math and Statistics

[↑ Back to Table of Contents](#table-of-contents)

#### [Random values](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/random-values?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/random-values?u=76281980&t=0)** - [Instructor] In this chapter, we're going to take a look at some common statistical operations and we're going to start with random number generation. So let's open up our random numbers example here. So random numbers are used for a variety of scenarios from simulations to machine learning to games and even [Software Testing](../../Skills/Software%20Development/Software%20Testing.md). In [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) one of the ways to generate random values is to use the random module from the standard library. And you can see that I've imported that here. So let's try it out and see what we can do with it. So to generate a random floating point number between zero and one, we can just use the random functions. So I'm going to print out random dot random and that is a function but that's not usually what you want to do. Typically, you want to generate a number that is within a range of values and we can use randint and randrange for that. Alright, so for example if I want to generate a random number within a range I can do random.randint and randint takes two arguments, I can give it 10 and 20 or I can do random.randrange and also give that 10 and 20.
>
> **[1:24](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/random-values?u=76281980&t=84)** The main difference between these two functions is that the randint version will include both endpoints in the range, whereas randrange will exclude the endpoint. So if I wanted to generate a random number between 10 and 20 that included both points as potential values I would use the randint version. If I didn't want to include 20 as a possible value I would use randrange. All right? So let's try the code out. I'm going to run what I have and I'm going to run it and you can see that I've got my floating point number, 0.211 and then I've got my randint and randrange. In this case it made the same number twice. I'll go ahead and run it again, right and you can see that the floating points and the integers are different now. So I run it a few times and you can see that different values are getting generated each time I run the code. So we can use random numbers to pick random elements from our weather data. So let's go back to the code. Here I have some code that builds a list of data points for the summer of 2019 by using the filter function, right? So the filter function is calling this is summer day callback, which makes sure that the given day is within these two summer months, right? July and August for 2019. So let's choose five random days from that summer.
>
> **[3:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/random-values?u=76281980&t=180)** I'm going to have my random days variable and I'm going to initialize that to an empty list. And then I'm going to write a loop. So I'm going to have my loop go over a range of five times and I'm going to write random days.append and I'm going to choose data. So I'm going to have summer 2019 and I'm going to index into that list by calling the random.randrange function and I'm going to pass it the length of the summer 2019 list.
>
> **[3:44](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/random-values?u=76281980&t=224)** So if you only give randrange one value then it's assuming that the first value starts at zero and runs up to this value here. And again, randrange is exclusive of the endpoint so we don't have to worry about overreaching the end of the list here. So what this is going to do is choose five random days and then we can find out, "hey what's the windiest of those five days?" And we already know how to do that, right? So we're going to use the max function on the random days and we're going to give it a key that looks for the average wind speed of that day. Alright, let's make sure that previous examples are commented out. So I'm just going to control slash those lines. Alright, looks pretty good. Okay, let's run this. Alright, and you can see that I've chosen five random days and then I've randomly chosen the windiest of those five random days. So let's go ahead and run this again. That was July 30th, 2019. I run it again and the windiest day was July 12th, I run it again and August 13th this time was the windiest day. So I'm getting a different result each time. Now there's a couple of things I want to point out. First, you can control how random numbers are selected by setting what's called the seed value. So if I use the seed function with the same value
>
> **[5:18](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/random-values?u=76281980&t=318)** then each time I run this I'm going to get the same five days. So let's go ahead and close that. And what I'm going to do is right above my code for random days, I'm going to call random.seed and I'm just give it a number like 10. So now let's go ahead and run this again. All right. And we can see that the windiest day out of the five randomly chosen days was August 12th. So let's run it again and oh look, it's August 12th again and I'll run it again and oh look, it's August 12th again. So this can be useful if you want to run multiple simulations with the same random values chosen each time. The other thing I want to point out is that my code let's go back to the code and kill the terminal. So my code as it's currently written, it doesn't account for choosing the same day more than once, right? It's possible that I can have duplicate days in my list. And in a next video we'll see how to use some of the other functions in the random module to operate on sequences of values like our weather dataset.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Software Testing](../../Skills/Software%20Development/Software%20Testing.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **CLI Commands:** python (1), find (1), make (1)
> **Cross-References:** go back to (2), next video (1)
> **Versions:** 0.211 (1)
> **Tools:** terminal (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Random operations on sequences](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/random-operations-on-sequences?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/random-operations-on-sequences?u=76281980&t=0)** - [Instructor] In a previous video, we saw how to use the random module to generate random numbers in a variety of ways. In this example, we're going to take a look at some of the functions in the random module that are specifically intended to be used with sequences of values. So let's open up our random operations, and give ourselves a little room to work here. So I have some initial code that selects the first 30 days from the weather dataset after it is loaded and parsed by the [JSON](../../Skills/Web%20Development/JSON.md) module. So first, let's see how the shuffle function works. This function is used to shuffle a set of data in place, sort of like how you would shuffle a deck of playing cards. So let's write some code that prints out the first three days of the data, and I'll use the pre-print module for this, and I'll print out the month_data, and we'll choose the first three days, and then we'll print out some dividing line, right? And then we'll call the random shuffle function on the month_data, and it operates in place. It modifies that sequence directly. And then we'll just print the same first three days again, all right? So let's go ahead and run this, and if I make this a little bit larger, all right, so we can see that the first three days are January 1st, 8th, and 16th, and then we shuffle the first 30 days, and then we choose three random days.
>
> **[1:33](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/random-operations-on-sequences?u=76281980&t=93)** So we've got January 1st again, and April 25th, and February 8th. And if I run this again, you can see that, once again, the first three days are the same, but now we've got June, March, and April, okay? So you can see that the first three days never change, but the shuffle function then randomizes the list and chooses three separate random days. All right, so let's go ahead back to our code. So now let's take a look at the choice and choices functions, and as you may have guessed, these functions are used when you want to select one or more values from a given list of values. So let's try these out. I'm going to create a variable called rnd_day and that's going to be the result of calling random.choice, and random.choice takes an iterable sequence. So I'll go ahead and pass in my month data, and then we'll print out the random day, and then we'll print out our dividing line again, and then we'll try something else. We'll call, I'll make a variable called rnd_days, and for this example, I'll use the choices function. So choices also takes my sequence of data and the number of choices I wanted to make. So that number is stored in the K argument, and if I don't give it a value, you can see that it defaults to just choosing one item. So I'll ask it to choose three,
>
> **[3:08](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/random-operations-on-sequences?u=76281980&t=188)** and then we'll print out those random days, all right? And let's go ahead and comment out the first example. Okay, and let's run. And let's open up our terminal here a bit. So you can see that the choice function has selected a single random element, while the choice says function has chosen three elements. And I can run this again, and you can see that the item has changed and the three items have changed. I'll run it one more time, and you can see once again I've got a different choice and a different result for choices, okay? Now again, you need to be careful here and the reason because choice and choices can produce duplicate results. Now, in some cases, that's okay, but if what you're trying to do is build a random sampling of data from the list without duplicates, then you need a way of making sure that your choices don't contain any duplicate values, and for that, we can use the sample function. So let's go ahead and close down this terminal, and let's go back to our code and let's comment this out, and now let's try the sample function. So the sample function does essentially the same thing as the choices function does except that it guarantees that it won't choose something more than once. So I'll call random.sample,
>
> **[4:42](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/random-operations-on-sequences?u=76281980&t=282)** and once again, I'll give it the month data, and K is equal to three, and then we'll just print out those three random days. And now when I run this. Okay, let's make this bigger. All right, so we have three random days, all in April. Let's run it again. All right, three different random days, and you can see each time, there's no duplicates, right? I'm choosing three random days each time, and the sample function is making sure that I'm not getting any duplicate data in my random sample population. So those are some different ways that you can use random numbers to choose items from a collection, depending on whether or not you care about getting duplicate values.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **CLI Commands:** make (4)
> **Code Identifiers:** month_data (2), rnd_day (1), rnd_days (1)
> **Cross-References:** previous video (1), go back to (1)
> **Tools:** terminal (2)
> **Env Vars:** json (1)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Random days](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-random-days?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-random-days?u=76281980&t=0)** - [Instructor] Okay, let's take a look at my solution for this particular challenge. And for this challenge we had to select five non-duplicate random days from a given year and month and return them in a list. So here in my solution, what I do first is I concatenate the year and the month into a single string separated by a dash, because that's the date format that's used by the sample data. And that gives me an easy way of filtering out the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the data and only keeping the days that are in the year and month, which is what the next part of the code does. This is the year, month filter, callback function and this call to filter. So I'm filtering out all of the days that are not within the year and the month that my function is given. So once I have this piece of code executed, I now have a list of eligible days from which I can select five random days. And to do that, I'm using the random sample function to select five days from the year, month data, and then I just return the list of values. And recall from earlier in the chapter, the sample function makes sure that there are no duplicate choices from the dataset. So, scroll back up here a little bit. So when I click on "test my code", you can see that I'm getting the right answer, and my validation code internally is checking to see
>
> **[1:34](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-random-days?u=76281980&t=94)** to make sure that these two lists are different and that they actually match the right expected format. So you can see that the test code gives you a year and a month and then calls select days two times. And in order to get this question correct, one of the things you had to do was return a list that was different from each other. And so you can see that this list of items is different, and so therefore we have correctly solved this challenge by choosing five random days from the list. So how does my solution compare to yours?

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** make (1)
> **Cross-References:** earlier in (1)
> **UI Navigation:** click on (1)
> **Speakers:** - [instructor] (1)

#### [Statistical functions](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/statistical-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/statistical-functions?u=76281980&t=0)** - [Instructor] The [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) Standard Library includes a [Statistics](../../Skills/Data%20Science/Statistics.md) module in which you can find a variety of functions for calculating common statistical data. And we're going to use that module in this example to determine values such as the mean, the median, and standard deviation of certain values in the dataset. So let's open up our statistics functions starting point. And you can see that I've already got some code written that creates a list of weather data only for days that occur during the summer months, June, July, and August. And I do this by looking for the appropriate month numbers in the date string for each item in the dataset, right? So I'm only limiting my data to summer months. And you can also see that I've imported the statistics module up here at the top of my code. So before we do anything else, okay, let's run what we have. And you can see on line 14 right here, I've got a print statement that will print out how many summer days we are considering. So let's run this and we can see that in the output we have data for 460 summer days. So what we're going to do is start by calculating the mean or average values for both the min and max temperatures for the summer days. So let's close this terminal and go back to our code. So I can build a list for each of those using a list comprehension. So I can create a list called max temps, and that's going to be a list comprehension where
>
> **[1:34](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/statistical-functions?u=76281980&t=94)** I get the max temperature for each day in summer months.
>
> **[1:44](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/statistical-functions?u=76281980&t=104)** And I can do the same thing for the min temps. And in that case, I'm looking at T min. All right, so once I have the list of temperatures, I can then use the mean function in the statistics module to perform the calculation. So let's go ahead and do that. I'll print out the max mean, and I'm going to use the walrus operator here to assign the value of statistics dot mean and I'm going to call that on max temps statistics. There we go. If you haven't seen this syntax before, this kind of concatenates, or actually it does two things. First, it calls this function and then takes the return value and actually assigns it to a variable. So I'm declaring a variable here and this syntax is new, relatively new, can't remember. I think it was added in Python, like either three eight or three nine, somewhere around there. But it saves me the step of having to declare a separate variable, and that's because I'm going to use it again later. And I'll do the same thing for the min mean. And I'll call statistics dot mean, and in this case I'm going to use the min temps. And while we're at it, we can find the median value for each of these data sets. And remember, the median is the value where half of the data points are above and half are below that value. It's not the same thing as the mean. Sometimes people get median and mean or average confused. So let's go ahead and do that as well. So I'm going to print out the statistics
>
> **[3:19](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/statistical-functions?u=76281980&t=199)** and I'm going to call the median, oops, median for the max temps, and I'll do the same thing for the min temps. Okay, so let's go ahead and run what we have. All right, so for the 460 summer days that we have, we can see that the max and min average or mean is about 89 degrees Fahrenheit for the max temperatures and about 69 for the min temperatures. And the median values, again, half are above and half are below 90 for the maxes and half are above and half are below 70 for the mins. All right, so far so good. So let's put the statistics package to work a little bit. Now we're going to find the temperatures that are the outliers from our dataset. So let's go ahead and close this terminal. So what we're going to do is use the standard deviation function to find outlier values that are more than two standard deviations above and below the mean for the max and min temperature readings. So first we have to calculate what that level is for both the min and max temperature lists. So I'm going to have a variable called the upper outlier, and that's going to be the maximum mean
>
> **[4:52](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/statistical-functions?u=76281980&t=292)** plus I'm going to call statistics dot and I'm going to call standard deviation on the max temperatures and I'm going to multiply that by two. And just to make it easy to read I'll put this in parentheses. All right. So a temperature above this upper outlier is how we're going to calculate what the outlier is, right? It's going to be the mean plus two standard deviations above the maximum temps. And then we'll do the same thing for the lower outlier. And that's going to be the minimum mean, minus, and then pretty much the same thing here. It's going to be standard deviation. So we'll copy and paste that. Copy and paste. Only in this case we're going to be working on the min temps. All right. And we're going to print out the upper outlier and let's print out the lower outlier. All right, and let's go ahead and comment out our median calculation. So let's go ahead and run the code now that we've updated it to have these outliers. All right. Okay, so we have our means, right? And now we have the outlier values. So a temperature above 100.02 and a temperature below 58.99 is going to be considered an outlier value in our data sets. So let's figure out now which days there are
>
> **[6:27](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/statistical-functions?u=76281980&t=387)** or let's build a list of temperature values that exceed the upper and lower outliers. So I'm going to have a max outliers list and I'm going to build that list again by using a list comprehension. So I'm going to get a temperature for all the temperatures in max temps if that temperature is greater than or equal to the upper outlier. And then I'll do the same thing for the min outliers. And so let's see, I'll have min outliers, and in this case I'm looking at min temps, and I want to make sure that T is less than or equal to the lower outlier boundary. And so now we'll print max outliers, and we'll print the min outliers. All right, so now let's run the updated code. So when we run the code, we can see that we've got two days in which the temperature exceeded the upper outlier boundary and we've got 19 days that were below the lower outlier boundary. So you can see that the statistics module can help with some common types of data calculations just by using what Python already has built in without having to reach for some more complex packages.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Statistics](../../Skills/Data%20Science/Statistics.md) (11), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3)
> **CLI Commands:** find (4), python (3), make (2)
> **Versions:** 100.02 (1), 58.99 (1)
> **Tools:** terminal (2)
> **Cross-References:** go back to (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Detect outliers](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-detect-outliers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-detect-outliers?u=76281980&t=0)** - [Instructor] All right, so this challenge was a little bit of a tricky one, so let's go through it step by step. Our challenge was to find the number of days in all the winter days in the dataset that had an average temperature that was two standard deviations above the mean average temperature for all those winter days. And for the purposes of this challenge, we defined winter days as days in the months of December, January and February for all the years in the weather data. So the first step in this challenge was to narrow down the dataset to include just days in the winter months. So that's what my code here does. Let me, so I'm looking for days that are just within these months. And so I have a list comprehension here that makes sure that each one of the month strings in the winters function matches part of the date for all the days. So this is filters out all of the days that are not within the winter months. And then I'm left with days that are within the winter months. So after that statement executes, the winter months list contains only the days within those months. Next, I need to build a list of all of the average temperatures for the winter days so I can figure out what is the mean value for those temperatures. So I have some code here that creates a list of average temps. So I calculate the average temp for every day in all the winter months. And you can see that that's this little helper function
>
> **[1:34](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/solution-detect-outliers?u=76281980&t=94)** right here that's calculating the average temp. And so once I have that list of average temps, I can then use the [Statistics](../../Skills/Data%20Science/Statistics.md) package to calculate the mean value for those average temps. And that gives me this average mean right here. So now I have that value, I can use the standard deviation function to figure out what the upper boundary temperature is. So temperatures above that boundary are the outliers that we need to count. So here I have my outlier temperature, and that's going to be the average mean plus the two standard deviation of the average temps across the winter months. So once I know the outlier boundary, I can build a list of winter days that exceed that boundary. And once again, I do that with a list comprehension. So after this line executes, you can see that it's looking at every day in winter month and is only including the day in the new list if the average temp for that day is greater than the outlier temperature boundary. And once I have that list calculated, remember the result that we needed to return was not the actual list. What we needed to return was the count or the number of days that were in that list. So now if I click test my code, you can see that the proper answer to return was that there are 15 days in all of the winter days that exceed two standard deviations above the mean for the average temperatures in the winter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Statistics](../../Skills/Data%20Science/Statistics.md) (1)
> **CLI Commands:** find (1)
> **Speakers:** - [instructor] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/hands-on-advanced-python-data-engineering-basics/next-steps?u=76281980&t=0)** - [Narrator] All right, well, it looks like we've reached the end of the course. Congratulations on completing it. I hope that the practical hands-on nature of this course helped you understand how some of the more theoretical concepts, such as [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) and [Algorithms](../../Skills/Software%20Development/Algorithms.md) can be applied to real problems. And I hope it inspired you to try out some of the techniques, programming projects that you are currently working on. Be sure to follow me on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) to find out when I've got new courses coming out and to be notified when I publish my weekly tips on how to use common [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) features. I hope to see you again soon in another one of my courses. Until then, happy coding.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **CLI Commands:** find (1), python (1)
> **Analogies:** such as (1)
> **Speakers:** - [narrator] (1)


## Instructor

- [Joe Marini](../../Instructors/Software%20Development/Joe%20Marini.md)

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/hands-on-advanced-python-2703239)

## Skills Covered

- Python (Programming Language)

## Path Context

### In [Data Engineering Hands-On Practice](../../Paths/Data%20Science/Learning%20Paths/Data%20Engineering%20Hands-On%20Practice.md)
← [Hands-On Introduction- Data Engineering](Hands-On%20Introduction-%20Data%20Engineering.md) | **2 of 7** | [Complete Guide to Python for Data Engineering- From Beginner to Advanced](Complete%20Guide%20to%20Python%20for%20Data%20Engineering-%20From%20Beginner%20to%20Advanced.md) →

## Appears In

- [Data Engineering Hands-On Practice](../../Paths/Data%20Science/Learning%20Paths/Data%20Engineering%20Hands-On%20Practice.md)

## Related Courses

_Courses sharing skills:_

- [Learning ArcGIS Python Scripting](Learning%20ArcGIS%20Python%20Scripting.md) — Python (Programming Language)
- [QGIS and Python for AEC](QGIS%20and%20Python%20for%20AEC.md) — Python (Programming Language)
- [Python Data Structures- Trees](../Database%20Management/Python%20Data%20Structures-%20Trees.md) — Python (Programming Language)
- [Using SQL with Python](Using%20SQL%20with%20Python.md) — Python (Programming Language)
- [Python- Working with Predictive Analytics (2019)](../Software%20Development/Python-%20Working%20with%20Predictive%20Analytics%20(2019).md) — Python (Programming Language)

---

[↑ Back to top](#)