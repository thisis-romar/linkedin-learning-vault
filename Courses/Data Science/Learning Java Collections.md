---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: learning-java-collections
url: "https://www.linkedin.com/learning/learning-java-collections"
duration_minutes: 190
duration: 3h 10m
level: Intermediate
updated: 9/29/2021
learners: 346150
skills:
  - Java Frameworks
  - Java
exercise_files: true
github: "https://github.com/LinkedInLearning/java-collections-2421704"
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQGoBE1qOqZWYg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1632758718519?e=2147483647&amp;v=beta&amp;t=jS6Sttg7CC_Ih1guav7yWTsnm9o81ZF1hfmdgzzhIgI"
linkedin_topic: Data Science
learning_paths:
  - '[Getting Started as a Java Developer](../../Paths/Data%20Science/Learning%20Paths/Getting%20Started%20as%20a%20Java%20Developer.md)'
prev_courses:
  - '[Learning JDBC](Learning%20JDBC.md)'
next_courses:
  - '[Java Memory Management- Values and References](Java%20Memory%20Management-%20Values%20and%20References.md)'
path_nav: '[{"path":"Getting Started as a Java Developer","position":5,"total":10,"prev":"Learning JDBC","next":"Java Memory Management- Values and References"}]'
path_count: 1
tags:
  - course
  - topic/data-science
  - topic/software-development
  - topic/web-development
  - skill/java-frameworks
  - skill/java
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Data%20Science/Learning%20Java%20Collections.md)

![Learning Java Collections](https://media.licdn.com/dms/image/v2/C560DAQGoBE1qOqZWYg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1632758718519?e=2147483647&amp;v=beta&amp;t=jS6Sttg7CC_Ih1guav7yWTsnm9o81ZF1hfmdgzzhIgI)

# Learning Java Collections

> As developers advance past the basics of Java, they must understand how to effectively use Java collections in their daily work. This course offers you a deep dive into the framework and hands-on experience working with it. The Java Collections framework consists of interfaces, implementations, and utilities that together provide standard support for common data structures in the language. Instruc

> [LinkedIn Learning](https://www.linkedin.com/learning/learning-java-collections) | 3h 10m | Intermediate | 346K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Java collections framework](#java-collections-framework)
  - [What you should know](#what-you-should-know)
- [**1. Java Collections Framework**](#1-java-collections-framework) (5 videos)
  - [Solving everyday problems with data](#solving-everyday-problems-with-data)
  - [Data structures](#data-structures)
  - [Collections framework architecture](#collections-framework-architecture)
  - [Working with a collection](#working-with-a-collection)
  - [Beyond the array](#beyond-the-array)
- [**2. The Foundations of Java Collections**](#2-the-foundations-of-java-collections) (10 videos)
  - [Interfaces](#interfaces)
  - [Collection types](#collection-types)
  - [The Collection interface](#the-collection-interface)
  - [Collection methods](#collection-methods)
  - [Creating a collection](#creating-a-collection)
  - [Generic typing](#generic-typing)
  - [Object comparison](#object-comparison)
  - [The equals method](#the-equals-method)
  - [Challenge: Adding and removing collection elements](#challenge-adding-and-removing-collection-elements)
  - [Challenge: Advanced collection problems](#challenge-advanced-collection-problems)
- [**3. Iterating Collections**](#3-iterating-collections) (7 videos)
  - [Iterating collection elements](#iterating-collection-elements)
  - [Accessing collection elements with iterators](#accessing-collection-elements-with-iterators)
  - [Modifying collections while iterating](#modifying-collections-while-iterating)
  - [Accessing collection elements with streams](#accessing-collection-elements-with-streams)
  - [Lambda expressions](#lambda-expressions)
  - [Stream operations](#stream-operations)
  - [Challenge: Iterating collections](#challenge-iterating-collections)
- [**4. Set**](#4-set) (3 videos)
  - [Set](#set)
  - [HashSet](#hashset)
  - [TreeSet](#treeset)
- [**5. List**](#5-list) (4 videos)
  - [List interface](#list-interface)
  - [List implementations](#list-implementations)
  - [ArrayList](#arraylist)
  - [Challenge: Working with Lists](#challenge-working-with-lists)
- [**6. Queue and Deque**](#6-queue-and-deque) (4 videos)
  - [Queue interface](#queue-interface)
  - [Queue implementations](#queue-implementations)
  - [Deque interface](#deque-interface)
  - [Using an ArrayDeque as a stack](#using-an-arraydeque-as-a-stack)
- [**7. Map**](#7-map) (7 videos)
  - [Map interface](#map-interface)
  - [Collection views](#collection-views)
  - [Map implementations](#map-implementations)
  - [How a HashMap works](#how-a-hashmap-works)
  - [Map methods](#map-methods)
  - [Iterating Maps](#iterating-maps)
  - [Challenge: Working with Maps](#challenge-working-with-maps)
- [**8. Collections Utilities**](#8-collections-utilities) (4 videos)
  - [Ordering elements](#ordering-elements)
  - [Sorting collections](#sorting-collections)
  - [Comparators](#comparators)
  - [Finding elements in a collection](#finding-elements-in-a-collection)
- [**Conclusion**](#conclusion) (1 videos)
  - [Course wrap-up](#course-wrap-up)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Java collections framework](https://www.linkedin.com/learning/learning-java-collections/java-collections-framework?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/java-collections-framework?u=76281980&t=0)** - [Kevin] After you learn the fundamentals of [Java](../../Skills/Software%20Development/Java.md), there's no better second step in your learning journey than the Java collections framework. The framework provides [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) like lists, queues, and maps that Java developers work with every day. Having these data structures in your toolbox is critical for coding everything from the most basic to the most advanced Java applications. Hi, I'm Kevin Bowersox. I've built many complex systems with Java throughout my career, and all of them have used Java collections. In this course, I'll teach you about the concepts of the framework, its interfaces, and how to work with them so you can put them into practice and be successful with collections. So if you're ready to level up with Java, join me in my [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning course to learn how Java collections will help you deliver better solutions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (8), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Speakers:** - [kevin] (1)

#### [What you should know](https://www.linkedin.com/learning/learning-java-collections/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/what-you-should-know?u=76281980&t=0)** - [Instructor] Before taking a closer look at the [Java](../../Skills/Software%20Development/Java.md) collections framework, let's talk a bit about who the course is for and what you should know ahead of time. If you're a Java developer who knows the basics, this course is a great next step for advancing your knowledge of the language. It will teach you how to work with the collection framework's interfaces and data structure implementations. These are some of the most heavily-used classes in Java. So as a Java developer, you'll read and write code that uses these [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) constantly. That makes this course a great investment of your time because you'll benefit instantly and throughout the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of your career. There are a few things that you'll need for the course. This course is great for Java developers who want to advance past language fundamentals, like variables, data types, and arrays. However, it shouldn't be your first introduction to Java. So if you've never worked with the language, check out the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning library for an introductory course, like Learning Java. To follow along with the course, you'll need to have JDK 11 and Maven installed on your machine. You can run any operating system that you would like. You'll also need an IDE. I'll be using Eclipse throughout the lessons, but you can follow along with your favorite editor. Just know that you'll need to account for any differences between the two. That's all you need to get started with the course. I'll handle the rest.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (7), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Prerequisites:** you'll need (3)
> **Env Vars:** jdk (1), ide (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 1. Java Collections Framework

[↑ Back to Table of Contents](#table-of-contents)

#### [Solving everyday problems with data](https://www.linkedin.com/learning/learning-java-collections/solving-everyday-problems-with-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/solving-everyday-problems-with-data?u=76281980&t=0)** - [Instructor] Handling data is an important part of coding any application. That shouldn't be a big surprise, because as people solve problems in their daily lives and work, they instinctively organize data. Most applications handle work for people. So, an application's code often arranges data in a similar way. So, to help us better understand collections, let's start by talking about some real life examples of how we organize data. At some point in the workplace, you've probably used a list to plan a small project. In the list, you organize a group of tasks that are needed to complete the project. We can refer to each of the tasks in the list as an element. Each element holds a specific position in the list relative to the others that is designated by a number. The list also has an order which indicates the sequence for completing the tasks. Now lists are a great way to organize data, but what if your project involved a team and you wanted to take it a step further by capturing who's working on each task? You might decide to build a map that captures those assignments. So first, the team members' names are written down. The names serve as the key of the map that will associate with each task. The keys are then associated with or mapped to values, which in this case, indicate the tasks in the project. Each of these key value pairs [Forms](../../Skills/Web%20Development/Forms.md) an entry
>
> **[1:36](https://www.linkedin.com/learning/learning-java-collections/solving-everyday-problems-with-data?u=76281980&t=96)** in our map that we can use to determine who is working on each task. Concepts like lists and maps are two basic tools we use almost daily to organize data when solving problems. In computer science, these are known as [Data Structures](../../Skills/Software%20Development/Data%20Structures.md), and it's very common to find them built into programming languages. In [Java](../../Skills/Software%20Development/Java.md), those data structures are found in the Java Collections Framework, which you're going to learn a lot more about in this course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (2), [Java](../../Skills/Software%20Development/Java.md) (2), [Forms](../../Skills/Web%20Development/Forms.md) (1)
> **Definitions:** is an  (1), known as (1)
> **CLI Commands:** find (1)
> **Speakers:** - [instructor] (1)

#### [Data structures](https://www.linkedin.com/learning/learning-java-collections/data-structures?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/data-structures?u=76281980&t=0)** - [Instructor] So we established that the [Java](../../Skills/Software%20Development/Java.md) collections framework provides [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) for the Java programming language. Now let's talk more about what data structures do. That way you'll know what to expect when you start working with Java collections. At their core, data structures are used to store, organize, manage, and retrieve data. Each data structure has its own unique features that set it apart from the others. But at a high level, these capabilities are provided by a few common operations that are found on every data structure. First, there's operations that allow us to add values into the structure for storage. Some data structures will define the position where a new element is stored. For example, one data structure known as a queue pushes elements into the front of its structure. Other data structures, like a list, let us choose the position where a new element is placed. So we might specify that we want to add a new element as the third item near the middle of its structure. When new elements are added to a data structure, they're stored according to an internally defined organization strategy. This strategy is typically optimized for a particular operation, like retrieving or inserting an element. When working with a linear data structure, its elements are organized to form a sequence. Other data structures based on trees or hashing functions, will use more complicated organizational strategies to store their elements.
>
> **[1:35](https://www.linkedin.com/learning/learning-java-collections/data-structures?u=76281980&t=95)** Once elements are placed inside the structure for storage, we can use other operations to manage the elements if we need to make changes to the structure. For example, we might decide to remove an element. Or we might need to replace an element in the structure. Another common operation is to apply a sorting algorithm that rearranges the data structure's elements. We also need to be able to retrieve elements that were stored in the data structure, which can be achieved using several operations that provide access to the elements. Retrieving an element at a specific position or with a key is known as random access. It normally provides better performance because it does not need to traverse all the elements in the structure to find the target element. If we need to retrieve every element in the data structure, we can use operations or control structures like loops that iterate through each element. Finally, many data structures expose operations that allow us to search for a particular element in the structure, either to remove it, find its position, or to check if it exists. There's a bit more to it than that, but that's a high level look at some common operations found on data structures. Next, we'll start diving into the Java collections framework where you'll find a hierarchy of collection types that support different mixtures of these capabilities.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (8), [Java](../../Skills/Software%20Development/Java.md) (4)
> **CLI Commands:** find (3), make (1)
> **Definitions:** known as (2)
> **Analogies:** for example (2)
> **Speakers:** - [instructor] (1)

#### [Collections framework architecture](https://www.linkedin.com/learning/learning-java-collections/collections-framework-architecture?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/collections-framework-architecture?u=76281980&t=0)** - [Tutor] Now let's shift gears and focus on working with [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) in [Java](../../Skills/Software%20Development/Java.md). So that means we need to start unpacking the collections framework. The framework can be broken down into three sections, interfaces, [Algorithms](../../Skills/Software%20Development/Algorithms.md), and implementations. The interfaces are the foundation of the platform because they are the main abstract types that determine what methods are available on collections, like the list, set, and map. Without implementations, we can't do much with just an interface. So the framework also provides several classes that implement the core interfaces and provide the underlying source code for the data structures. We also find algorithms in the framework that support working with collections. These are provided through static methods that give us functionality for working with collections like sorting and searching. These interconnected sections of the framework work together to provide developers with most of the code they'll need to work with data structures in Java. Now let's talk a little history. Java 1.2 was when the collections framework was first introduced. Prior to that, there were a few classes like hash table and vector that supported working with objects, but they had some concerns about performance and quality. These classes were re-engineered to satisfy collection interfaces and to support generics. But for most situations, you'll want to use the types introduced with the collections framework. The Java community still debates if legacy collection
>
> **[1:35](https://www.linkedin.com/learning/learning-java-collections/collections-framework-architecture?u=76281980&t=95)** should be deprecated. So if you're adding them into new source code, you should have a good reason why. Developers use the collections framework so frequently in Java that many of its benefits are overlooked. The framework contains all of the code a developer needs for working with data structures. So we're not required to build our own data structure implementations, or to design their interfaces. It's provided by the language. Since every Java developer uses the same code for data structures, it's a standard. So you can pick up someone else's code without needing to understand a bunch of custom data structures that they created. The collections framework also was built by some of the brightest engineers and has decades of use in production systems. So you can trust that you're using a high quality data structure that isn't riddled with bugs or performance problems. Because the same collections APIs are used by developers across the language, it naturally creates interoperability across different code bases. If you look at other Java technologies, like, Java EE, Spring, or the Apache projects, you'll find collection scattered across their code. This promotes interoperability amongst them and you're instantly familiar with the parts of the code that use collections. All of these benefits save you a lot of time, effort, and frustration while improving the quality of your code. Learning the architecture and interfaces
>
> **[3:08](https://www.linkedin.com/learning/learning-java-collections/collections-framework-architecture?u=76281980&t=188)** of the collections framework is one of the best ways to level up as a Java developer, because you're going to see them used everywhere in code. So next, we'll step down the level and do some hands-on work with a collection.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (9), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (6), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (2)
> **CLI Commands:** find (2), apache (1)
> **Exercise Files:** source code (2)
> **Versions:** java 1 (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [tutor] (1)

#### [Working with a collection](https://www.linkedin.com/learning/learning-java-collections/working-with-a-collection?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/working-with-a-collection?u=76281980&t=0)** - [Instructor] Let's talk a little bit about what a collection does before taking a look at some code that uses a collection. You can think of a collection as an object that groups other objects together and provide some common operations for working with the group. A collection makes it much easier to write code where you're working with multiple objects, and the number of objects can not be predicted. I'll demonstrate this using a simple example for the Landon Hotel. The Landon Hotel is a fictional hotel that we'll be using for examples and challenges throughout the course. So to kick things off, let's imagine that the Landon Hotel would like to manage the rooms and understand the potential revenue from them being booked. We might model something like a room class. Within that class, we could place fields for the name of the room, its type, its capacity and its daily rate. You'll also notice there's a constructor for the room to create an instance of it and accessors for all of its fields. Then in our application, we might write some logic like this. We might create some rooms. So here you see we have two, Cambridge and Manchester. And then we would pass those rooms into a method that would perform the potential revenue calculation. So you'll notice that I've created that method, and it accepts two parameters of type Room. Inside of the method's body, it takes those rooms and gets their rates and then totals the rates together to calculate the potential revenue.
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/working-with-a-collection?u=76281980&t=93)** Now what would happen if we wanted to add a third room into this calculation? Let's see. I'm going to copy our room, and then I'll switch it over to be a room named Piccadilly. So let's switch up these constructor arguments. I'll make Piccadilly a guest room. It will hold three people, and it's going to cost $125 per night. Okay. Now that we have the third room, we need to include it by passing it as an argument to the method. And that means we'll need to refactor this method signature to accept a third room parameter. Then we'll have to adjust the calculation to account for the third room. So you can see how brittle this code is. And it's not very extensible. If we wanted to add a fourth room, we would have to go through the same refactoring process. Now we can do this much better using a collection to manage all of these objects as a group. So let's go ahead. I'm going to create a collection that holds rooms. I'll give it a name of rooms. And then I'm going to use a static factory method on the list in order to create the collection. Now, instead of passing in these three arguments to our method, I'm just going to replace them with our room's collection. And then I'm going to place those rooms inside of the static factory method
>
> **[3:07](https://www.linkedin.com/learning/learning-java-collections/working-with-a-collection?u=76281980&t=187)** so that they are included in our collection. Now we'll still need to refactor the getPotentialRevenue method in order to accept a single parameter. So let's go ahead. We'll get rid of these three room parameters. And then I'm going to declare another parameter that's of type Collection, and I'll name it rooms. Then we can go ahead and adjust our calculation. So instead of performing this addition, I'm going to take our collection of rooms, and I'll stream it. And then I can use a method named mapToDouble. And I'll get the rate of each room using a lambda expression. And then from there, all I need to do is call the sum method to total the rate of each room in our collection. Okay. That's much easier. You'll notice we're now managing these objects together in a group using our collection. Now let's see how easy it is to add a fourth room into this calculation. So I'm going to copy Piccadilly, and I'll create a new room named Oxford. So let me adjust some of these constructor arguments. We'll make Oxford a suite. It's going to hold five people, and it'll cost $225 a night. Okay, I'm going to save that. So let's go ahead, and I'll first execute this code without Oxford in the collection. You'll notice the potential revenue of the rooms
>
> **[4:40](https://www.linkedin.com/learning/learning-java-collections/working-with-a-collection?u=76281980&t=280)** in our collection is $550. Now, once I add Oxford into the collection, watch what happens. You'll notice that the potential revenue increases to include Oxford which was recently added into our collection. Now here's the great part about this. We didn't need to refactor our getPotentialRevenue method in order to make that happen. All we needed to do was add Oxford into the collection. And because we're managing those objects as a group, it just automatically gets included in our calculation. So you see the flexibility that we get with collections. And this is just a very simple example. We've only scratched the surface. But the benefits of grouping and managing objects in a collection are obvious from this example. As we explore the details of how to use the interfaces and implementations in the framework, you'll learn how to take advantage of their prebuilt methods and the utilities that surround them.

> [!info]- Semantic Content
>
> **CLI Commands:** make (3)
> **Code Identifiers:** getpotentialrevenue (2), maptodouble (1)
> **Definitions:** is a  (1)
> **Analogies:** imagine (1)
> **Speakers:** - [instructor] (1)

#### [Beyond the array](https://www.linkedin.com/learning/learning-java-collections/beyond-the-array?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/beyond-the-array?u=76281980&t=0)** - [Instructor] In our last example, you might've wondered if an array could have been used to group the rooms instead of a collection. That's a valid point, but using collections overcomes a few disadvantages of an array. Let's walk through some examples and I'll show you. Arrays have a fixed size because the space for an array must be allocated for the number of objects that we intend to store. So if I want to declare a new room array, I need to know the number of elements that I'll be storing up front. Then I need to store each room within its index in the array. This can lead to one-off errors that throw index out of bounds exceptions. And if you look closely, you'll notice that we already have one. I wanted to declare an array of size three, but instead, I've declared an array of size two. That would have threw an error. Now, when we work with collections, we don't have these sorts of problems. It's relatively easy to instantiate a collection and add elements into it. You'll notice that I do not have to declare the size of the collection upfront. And if I want to add elements into the collection, I don't have to be concerned about a couple of things. So first, I don't have to worry about specifying the index of the element that I'm adding. I also don't have to worry about the collection filling up and getting an index out of bounds exception. When we work with an array,
>
> **[1:32](https://www.linkedin.com/learning/learning-java-collections/beyond-the-array?u=76281980&t=92)** we don't get this sort of flexibility. You'll notice that our array is currently full. We have three indexes and there's three elements stored within it. If we would like to add a new element into this array, we actually have to make a copy of it. Here's how we do it. First, we declare an array that is slightly larger than our original array. So I'm going to make this an array of size four. Then I'm going to use the array copy method on the system class. I pass in the original array, then I pass in the new array and then I specify the indexes that I'm interested in. Okay, now we have a new array with some space in it. I'm then going to store the new room in the last index within our new array. So I'll just subtract one from its length and then I'm going to store the room into that index. Now, you might get the idea to build your own objects that handle the operations that you'll need to perform on the array. You'll get some reusability with this approach, but at that point, you've started to replicate the features provided by the collections framework. The best case is that you'll be less productive and the worst case is that you'll build a buggy implementation. By using [Java](../../Skills/Software%20Development/Java.md)'s out-of-the-box collections instead of arrays, you'll save time by avoiding these types of situations and you'll write better code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **CLI Commands:** make (2)
> **Cross-References:** in the last (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)


### 2. The Foundations of Java Collections

[↑ Back to Table of Contents](#table-of-contents)

#### [Interfaces](https://www.linkedin.com/learning/learning-java-collections/interfaces?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/interfaces?u=76281980&t=0)** - [Instructor] When working with collections, it's important to have a good understanding of interfaces because they're used heavily by the framework. So let's just make sure you're solid on the concept. That way, you're successful with collections. On the left-hand side of my screen, I have an interface named Contract. I chose that name because the idea of a contract is commonly used to explain an interface. An interface is an abstract type, so that means we can't create an instance of it. And for our purposes right now, we're going to say that it cannot define methods with a body that contain code. On the interface we can define abstract methods. So let's go ahead and add one to the interface named term1. Abstract methods define the roles or terms for a class that wants to implement the interface. Any abstract method on the interface must appear on a class known as an implementation that implements the interface. So on the right-hand side of my screen, you'll notice I have a class named Implementation. If we would like this class to implement the contract interface, all we need to do is type implements, and then the name of the interface, which in this case, is Contract. Now, as soon as we do that, you'll notice the compiler in my IDE start to complain. Let's see what's up.
>
> **[1:36](https://www.linkedin.com/learning/learning-java-collections/interfaces?u=76281980&t=96)** So if we hover over the error, you'll notice that it's complaining because our implementation hasn't defined a method named term1. This method is required because the implementation class implements the contract interface. It's kind of like we're saying the interface, or the contract, is a template for what methods should be found on the implementation class. When the compiler sees the class doesn't match the template, it complains. So let's go ahead and add this method to the class. Just click on add unimplemented methods, and the IDE will automatically add it to the body of your class. Now, inside of this method, I'm just going to print some console output. That way, when we execute this code, we can see what methods invoked. Okay. So now the implementation has satisfied the terms of the contract interface. Let's see what happens if we add a new abstract method. So I'm just going to add another abstract method named term2, and look what happened. Immediately, the compiler complains. You can probably guess why. It's because there is a new method on our contract interface that isn't found on the implementation. So we added a new role or term to the contract, and it's not satisfied. Let's go ahead and satisfy the contract by adding the method. And then once again, I'm just going to add some console output
>
> **[3:09](https://www.linkedin.com/learning/learning-java-collections/interfaces?u=76281980&t=189)** to help us understand the execution of our code. All right, with that in place, we're going to now navigate over to the application class and here, we're going to use our new contract interface and its implementation. So to do that, I'm going to declare a variable of type contract. And then, I'm going to instantiate the implementation and assign it to the contract. This works because the implementation implements the contract interface. And then we can take that object and pass it to anywhere the contract interface is accepted. So you'll notice the method signature for the print terms method has a parameter of type contract. So we can pass our contract object into it. And then inside the body of the method, we can use that interface to invoke term1 and term2 on our object. Okay, let's see what happens. We'll execute this code after we save everything. And you'll notice term1 and term2 are printed to the console. Okay, so we got to cover one final point that's really important. And that's inheritance. Yep, an interface can extend another interface. So I've created another interface named ExtendedContract. And on that interface, I've specified an abstract method named extendedTerm. What we're going to do is have our contract interface
>
> **[4:48](https://www.linkedin.com/learning/learning-java-collections/interfaces?u=76281980&t=288)** extend this new interface. To do that, it's just like inheritance with a class. We just type extends, and then the name of the interface we would like the contract interface to extend. Now what's going to happen is the contract interface is now going to expect its implementations to include the extended term method because of this inheritance relationship. And our old friend, the compiler's back. So you'll notice it's complaining. Probably can guess why. It's because our implementation class, which implements the contract interface, doesn't have the method that the contract interface just inherited from ExtendedContract. Let's go ahead, and we can add that method to the class. And then I'm going to add a little bit more debugging output here, just to help us. All right, so we'll just print this information to the console. And I shortened it up. We just said, extended. Now let's save all of our work. And then we can head over to the application class. And if we take a look at our contract interface, you'll now see the extended term method appear because it's now part of that interface through inheritance. Okay, I'm going to save this and then we can go ahead. We'll execute our code.
>
> **[6:23](https://www.linkedin.com/learning/learning-java-collections/interfaces?u=76281980&t=383)** So when we execute this code, you'll notice that all three methods defined on the contract interface and implemented by the implementation class have been invoked. When working with collections, you'll need to understand how interfaces work to be successful. The use of inheritance between interfaces is a central concept underlying the collections framework that builds relationships between the core interfaces, and defines what methods you'll have available to work with on a collection.

> [!info]- Semantic Content
>
> **Definitions:** is a  (3), is an  (1), known as (1)
> **Env Vars:** ide (2)
> **Exercise Files:** template (2)
> **Analogies:** kind of like (1), just like (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** extendedterm (1)
> **UI Navigation:** click on (1)
> **Prerequisites:** you'll need (1)

#### [Collection types](https://www.linkedin.com/learning/learning-java-collections/collection-types?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/collection-types?u=76281980&t=0)** - [Instructor] Several core interfaces form the foundation that the [Java](../../Skills/Software%20Development/Java.md) collection framework is built upon. Let's explore what they are, their hierarchy, and some patterns for working with them. Here's a look at the top level interfaces where you'll find collections like list, set, and map. These should sound familiar from our earlier discussions. Within this group, inheritance is used to build a hierarchy, so it's common for interfaces to extend other interfaces in the collections framework. The collection interface is the common parent that is extended by other interfaces, like list, queue, and set. That means you'll find some of the same methods appearing on each of them because they inherit from the collection interface. In addition to having these things in common, each interface has its own unique behaviors that defines it. For example, the list and queue maintain an order among the group of elements that they store and the set doesn't allow for duplicate elements. The map interface is still considered a part of the collections framework, but you'll notice that it doesn't inherit from the base collection interface. It [Forms](../../Skills/Web%20Development/Forms.md) its own type hierarchy, but doesn't deviate too far from the patterns that will be used for other collections. We can expand upon this view to include some of the most common concrete classes the framework provides as implementations of these interfaces. These are the types you'll need to instantiate when working with a collection. The most frequently used implementations are ArrayList and HashMap,
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/collection-types?u=76281980&t=93)** so we'll get really familiar with those. If you inspect the implementations closely, you can break them down into two parts: the interface which determines their methods and the implementation style for how the underlying data structure works. The naming conventions used by a collection give us some clues about how it works because they reveal the implementation style and the type of interface. The first part of the name defines the implementation and the last part tells us the interface. So you'll notice ArrayList, HashMap. First the implementation, then the interface. You'll also notice that multiple implementation styles can be combined, LinkedHashSet. It might be easier to understand if we look at some of the most commonly used implementations against a matrix that shows their intersection of the interface and implementation. Here, you can see the whole group on the matrix. At first, it might feel a little intimidating to learn all of these collection types, but don't be intimidated because if you focus on learning the collection interface well, you can quickly learn a large part of the framework. That's because the majority of the collection types inherit from the collection interface.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1), [Forms](../../Skills/Web%20Development/Forms.md) (1)
> **CLI Commands:** find (2)
> **Analogies:** for example (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [The Collection interface](https://www.linkedin.com/learning/learning-java-collections/the-collection-interface?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/the-collection-interface?u=76281980&t=0)** - [Instructor] Let's explore the design of the collection interface and take a glance at its methods. Since the collection interface is at the root of the interface hierarchy, that means the methods on it appear on every type that extends from it. So interfaces and concrete implementations of a list, set, and queue will inherit these common methods from the collection interface. Some collection methods are denoted as being optional, so they might appear on an implementation, but it's possible that a call to one of these methods will throw an unsupported operation exception. The interfaces in the collections framework and the collection interface itself are generic. So when we create a collection, we use a generic type argument to indicate the type of elements that it holds. The argument is declared by placing the type in angle brackets. Since the collection implementations are based on these interfaces, they use a generic type argument too. When instantiating the implementation, we can be more concise by using the diamond operator on the constructor. This operator infers the generic type from the declared variable's generic type. This generic type impacts how we use collection methods because it determines what arguments may be passed into them or what type of object they might return. These are the 21 methods found on the collection interface as of [Java](../../Skills/Software%20Development/Java.md) 11. I've placed them into several groups and added asterisks next to the optional methods. These groups organize the collection methods
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/the-collection-interface?u=76281980&t=93)** by their general behaviors. Some add or remove elements, others iterate or inspect the collection, and one is kind of a catch-all with comparison and array operations. You'll notice that methods on the collection interface make frequent use of generics and that's common throughout the entire framework. So now we have a grasp of the collection interface and the groups of behaviors that it provides. In the next lesson, we'll dive deeper into a few of the most common methods that you'll use.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **CLI Commands:** make (1)
> **Versions:** java 11 (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Collection methods](https://www.linkedin.com/learning/learning-java-collections/collection-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/collection-methods?u=76281980&t=0)** - [Instructor] Let's take a closer look at a few of the most frequently used methods on the collection interface using some visualizations to see their behaviors. Throughout the visualizations, we'll assume that we're working with a collection that has a generic type of string. We'll start with methods that add items into the collection. When you want to insert an element into a collection, you must pass an object that's type matches the collection's generic type. We pass this object into the add method, which places a single element into the collection. The add method returns a boolean that indicates whether the collection changed as a result of the operation. For some implementations, like the set that do not accept duplicates, it's possible the element already exists in the collection. So if we called this method a second time on a set implementation, it wouldn't be added and the method would return false. We'll see this pattern in the return type, which uses the boolean to indicate a change in the collection on other methods as well. Another approach is to insert multiple elements into the collection, using the addAll method. This method accepts a collection of elements to be inserted as its argument. It also uses a boolean return type to indicate whether the collection has changed. Methods on the collection interface that perform operations on the entire collection are known as bulk operations.
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/collection-methods?u=76281980&t=94)** Most of these methods, aside from a few exceptions, accept a collection as their argument. Now let's take a look at how we remove elements from a collection. The first option is to remove an individual element by passing a single object to the remove method. It's return type is a boolean that indicates if the collection has changed. It's possible the collection will not change if we pass an object to the method that doesn't exist in the collection. There is also a bulk operation for removing multiple items from the collection. The removeAll method accepts a collection of elements as an argument for removal from the collection. If we want to remove all items from the collection, we can use another bulk operation named clear. The final method we'll explore for removing collection elements is retainAll. It's a bulk operation that is sort of the inverse of removeAll. It accepts a collection as an argument, then removes all elements of the target collection that are not found within the provided collection. Before going any further, I want to call out that collections are mutable. That means that each of the methods we've seen for adding or removing items changes the underlying collection when invoked. We'll dive deeper into the side effects of mutability later in the course. Now let's move on and see a few methods that help us inspect the collection.
>
> **[3:10](https://www.linkedin.com/learning/learning-java-collections/collection-methods?u=76281980&t=190)** We can use the contains method to determine if a collection includes a particular element. We just pass a single object into the method and it returns a boolean to indicate if it's found inside the collection. A bulk operation also exists for checking if an entire collection of elements is found within the target collection. The containsAll method accepts a collection of elements and returns of boolean to indicate if all elements in the provided collection are found in the target collection. Another method for inspecting a collection is the isEmpty method. As its name implies, it returns a boolean of true if no elements are found in the collection. In our example, the collection contains elements, so false is returned. The final method we'll explore, named size, returns an integer that denotes the number of elements in the collection. The 10 methods we reviewed are the most basic operations you'll use to insert, remove, or inspect collections. Later in the course, we'll talk about how collections are iterated, so you'll have a full understanding of the collection interface.

> [!info]- Semantic Content
>
> **Code Identifiers:** removeall (2), addall (1), retainall (1), containsall (1), isempty (1)
> **Definitions:** known as (1), is a  (1), means that (1)
> **Cross-References:** later in (2)
> **Speakers:** - [instructor] (1)

#### [Creating a collection](https://www.linkedin.com/learning/learning-java-collections/creating-a-collection?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/creating-a-collection?u=76281980&t=0)** - [Instructor] Let's walk through a few code examples that create a collection. Collections can be declared and instantiated like any other object. We specify the type of the collection, provide its identifier, and then instantiate a new collection implementation that will be assigned to the variable. I'm using the collection interface as the variable type. However, if I wanted to be more specific about the type, I could've switched over to use the set interface. So let's make the change and import the set from [Java](../../Skills/Software%20Development/Java.md).util.set. Now, the reason this works is because the hash set implements the set interface. We couldn't do something like using a list as our variable type because it's not compatible with hash set. But let's go ahead, we'll import the list from java.util.list, and then we'll change the implementation over to use the array list. So we'll add the array list constructor, and now we have a list. Now, let's talk about one more thing that you shouldn't do. Sometimes, you'll see people try to instantiate the collection type. The collection type doesn't have any direct implementation, so you can't do something like this. Let's just go ahead and we'll get rid of that code. Now I'm going to switch back over to using a collection as the variable type for our object. Okay so, now we have a collection, and in that collection, I'd like to store strings.
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/creating-a-collection?u=76281980&t=94)** So I'm going to specify the generic type argument on the variable type. And then over here on the constructor, all use the diamond operator to infer the generic type from our variable types Generic. Okay. Now that that's in place, we have our new collection, but it's kind of empty. Let's go ahead and add some elements into this collection. I'm going to add a few of my favorite things. So I love to snowboard, I love [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md), I also love to read, and I'm so excited about snowboarding, I'm going to add that twice. All right, now our collection isn't so empty. Let's go ahead and print this out to the console. Now, I can just pass in the collection to the print LN method, and that's because collections provide their own two-string implementation. So with that code in place, we can go ahead and look at the elements that are found in our collection. So we see the snowboard, Linux books, and the snowboard again. All right, now we're going to take a look at another way that we can create a collection. Each collection implementation has a conversion constructor that accepts another collection as an argument. So we're going to take our collection C and convert it to a set using the conversion constructor. To do this, we're first going to declare a new variable of type collection with a generic type of string. I'm going to call it favorite things.
>
> **[3:08](https://www.linkedin.com/learning/learning-java-collections/creating-a-collection?u=76281980&t=188)** And then, I'm going to use the hash set constructor and pass into that constructor our original collection. Okay, so let's do this. Instead of printing out the original collection, let's go ahead and print out the favorite things collection. I'm going to save it, and then watch what happens. Now that we've switched over to a hash set, we've lost one of the snowboard elements, and that's because a set doesn't contain duplicate elements. Now let's talk about one more best practice. When you're working with a collection, you want to avoid as much as possible doing something like this. So here, you'll notice that I'm using the array list, which is the concrete implementation, as the variable type. In general, you want to use the most abstract type possible. So that's going to be something like the list interface, or you would use the collection interface in this situation. By using the most abstract type possible for the variable type of your object, it allows you to switch the implementation without any consequences. So you'll notice, I just switched over to an array deck and I didn't need to change any of the code that relied on the collection interface. So this is going to make your code a lot higher quality if you keep this point in mind. I'm always on the lookout for it in a [Code Review](../../Skills/Software%20Development/Code%20Review.md).
>
> **[4:42](https://www.linkedin.com/learning/learning-java-collections/creating-a-collection?u=76281980&t=282)** All right, and just to prove it works, we'll go ahead and execute our code again. And there, you can see our output. So, just by a few examples of collections, you can start to see the flexibility that gets provided by the Java collections framework. It's going to be even more apparent when we take a deeper look at generics in our next lesson.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (2), [Code Review](../../Skills/Software%20Development/Code%20Review.md) (1)
> **CLI Commands:** make (2)
> **Best Practices:** best practice (1)
> **Speakers:** - [instructor] (1)

#### [Generic typing](https://www.linkedin.com/learning/learning-java-collections/generic-typing?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/generic-typing?u=76281980&t=0)** - [Instructor] Let's walk through some examples that demonstrate the benefit of using generics with collections. Collections can work with any type of object. So the same type of collection that we use to store strings can also be used to store custom objects like our room. It's not like we need a separate type of collection for the string and another for the room. Like we see in the example, we can use an array list to hold both types. Now collections are only meant to work with objects. So if we insert a primitive like this long, it needs to be autoboxed to its wrapper type to work with the collection. We can see this in action if we stream our collection and then print out the type of its elements. Here I'm just going to get the class of each element in our collection within the stream and print them out. Let's see what happens. You'll notice that our primitive long was autoboxed to its wrapper type [Java](../../Skills/Software%20Development/Java.md).lang.long. Now it's not a good practice, but right now we can add any type of element into our collection. So if I copy this room, I can come up here and add it into the collection. Now our collection is going to hold a string, a long, and a room. Let's say I wanted to work with the room in our stream. That would mean I need to cast the element to a type of room and that would allow me to access the methods on the room type. So here I can do something like get the name of our room. Now obviously this is going to cause a problem
>
> **[1:37](https://www.linkedin.com/learning/learning-java-collections/generic-typing?u=76281980&t=97)** and you'll notice my IDE and the compiler had been trying to warn us about this the whole time. That's because all they know is that this collection store's an object. So when they see us inserting elements into it, they're concerned that we're potentially adding different types of objects. So there's going to be an issue when we execute this code, but let's run it anyway. You'll notice that when executing the code, we get a ClassCastException at runtime and that's because we attempted to cast one of these objects, maybe it was the string or the long, to a type room and it's just not compatible. Prior to Java 5.0 when generics were introduced, this is how we worked with collections. We had to cast every element that we retrieved from the collection to its type and you just kind of hoped there wasn't a different type in there that was going to throw an exception. Now right now our collection is known as a raw type and that's because we haven't provided any type information about the elements it holds. We can do that by using a generic type argument. So here I'll add Room as the generic type argument for our collection and then I'll add the diamond operator on the constructor. Now look what happens immediately. The compiler shifts from giving us warnings to giving us compile time errors and that's because it now has
>
> **[3:10](https://www.linkedin.com/learning/learning-java-collections/generic-typing?u=76281980&t=190)** type information about our collection. It knows that it holds rooms. So when it sees us inserting a type like our string into a collection that holds rooms, it's going to give us a compile time error. So we fail fast and we fail early instead of experiencing this issue at runtime. Now let's go ahead and we can comment out this code because we know it's not going to work and then I'm going to point out one final benefit of using a generic. Because we provided that type information about our collection's elements, the compiler can infer the type of those elements when we retrieve them. So instead of performing this cast, we can just use the element and invoke methods on the generic type of our collection. All right, so with all that in place, let's go ahead and we can execute this code and you'll notice that it prints out the name of our room to the console. When working with collections, you always want to use the generic type and avoid using raw types. This will shift any issues to compile time and it will become much easier to discover and resolve those problems in your code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2)
> **Env Vars:** ide (1)
> **Versions:** java 5 (1)
> **Definitions:** known as (1)
> **Best Practices:** good practice (1)
> **Speakers:** - [instructor] (1)

#### [Object comparison](https://www.linkedin.com/learning/learning-java-collections/object-comparison?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/object-comparison?u=76281980&t=0)** - [Instructor] When working with the [Java](../../Skills/Software%20Development/Java.md) collections framework, several collections and their methods rely heavily upon comparing objects. Just looking at the collection interface, we'll find a few of these methods. They're pretty easy to spot because they accept an object or a collection of objects as their argument. They use this argument to perform an operation like removing an element or checking for its presence in the collection. To perform these operations, they must identify the matching object within the collection they're invoked upon so the method relies upon a test that compares the provided argument against the elements in the collection to find the matching object. In Java, there are two main ways to compare objects. One is identity-based and the other is value-based. When comparing objects by identity, we check to see if two objects have references to the same object in memory. When you use Java's equality operator, this is the type of check that occurs. Comparing objects by value is a test of whether two objects are logically equivalent. It relies upon their field values, instead of their memory addresses to perform this comparison. In Java, we override the equals method on an object to establish the logic for this comparison. A quick way to learn these two concepts is to think about two people who have the same car. Now, that's just how we phrase it.
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/object-comparison?u=76281980&t=93)** They really don't have the same car, but they have two cars with the same make and model. That is a comparison based on characteristics, or values of the car and it's the type of comparison necessary for collection methods, like remove, to function properly. On the other hand, if we referred to two people sharing a single car, that is an identity based comparison. It's normally not what we want when working with collections. These comparison concepts come into play when collection elements must be compared against each other and there's some other factors in Java we have to take into consideration. We're going to unpack those in our next lesson with a few code examples.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (5)
> **CLI Commands:** find (2), make (1)
> **Definitions:** is a  (2), is an  (1)
> **Speakers:** - [instructor] (1)

#### [The equals method](https://www.linkedin.com/learning/learning-java-collections/the-equals-method?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/the-equals-method?u=76281980&t=0)** - [Instructor] Let's dive deeper into how collections rely upon object comparison using a code example based on our car analogy. To help with that, I built a car class that has fields for the make, the model, and the mileage of the car. You'll also find class members like a constructor and a to string method. Now, I've also built a unit test that contains methods which test out the collection methods that we talked about that rely upon comparison. To build the test, I have a setup method that runs before each test. In this method, it creates a collection of cars, and then it creates three cars, a Subaru, a Tesla, and a Honda. Then these three cars are placed in the collection. So essentially, the collections reset with each test. I also added a tear down method that's going to print out the collection after we run the test so that we can see what happen. Now, what we really are concerned with are these three tests. So I've added one test that is going to remove two of the cars so that will leave one remaining and I'll check that via an assertion. The next test will check that our collection contains all three cars. And then the final test is going to remove all of the cars and check that we have an empty collection. Now, if you look at these tests, you'll notice that I vary how I use the car objects. In some cases,
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/the-equals-method?u=76281980&t=94)** I have a reference to the exact car in memory. So for example, on lines 65 and 66, I have the exact instance of the car that we put into the collection. Now, in other areas of the test, I actually instantiate a new car that is logically equivalent using a value-based comparison with a car that's found in our collection. So right now, if I go ahead and I run these unit tests, you're going to see that they fail. And that's because of the way we're performing the comparison. Right now, when the remove method is called, it's using an identity-based comparison as opposed to the value-based comparison we would like to use. The same is true with the other methods that we invoke in these tests and that's why they fail. This is because we haven't overridden the equals method in the car class. Right now, the car class is leveraging the equals implementation that it inherits from object. And you can see it here. It's using the equality operator to perform an identity-based comparison and that's what's causing our test to fail. So let's switch our car class over to use a value-based comparison by overriding the equals method. Now, to do that,
>
> **[3:07](https://www.linkedin.com/learning/learning-java-collections/the-equals-method?u=76281980&t=187)** we can use the capabilities found within the Eclipse IDE. So just right-click within the class, then go to source and then you'll find generate HashCode and equals. Now, you'll notice HashCode is included there too. That's because there's some rules in the [Java](../../Skills/Software%20Development/Java.md) doc that say we must override HashCode when we override equals. We're not going to get into that right now. We'll talk about it when we discuss hashing strategies. Just for now, know it's really important to follow the rule. Okay, so go ahead, click on that option, and this is going to bring up a dialog where we can select the fields that are going to be used in our value-based comparison. Now, when you select these fields, you want to pick fields that don't change frequently. So the make and the model, they are excellent candidates for our equals method, but the mileage, that's going to change daily so we want to omit that in our value-based comparison. Okay, once you have those selected, go ahead and hit generate, and this will generate the HashCode and equals method inside of our car class. Now, I'm going to come down here to the equals implementation that was generated for us. In the Java docs, it says that this method must be reflexive, symmetric, transitive, and consistent. That's a lot. Just know that if you use the IDE's capabilities,
>
> **[4:41](https://www.linkedin.com/learning/learning-java-collections/the-equals-method?u=76281980&t=281)** you don't have to worry about that. But if you're writing your own equals implementation, you should probably take a look at the Java docs and do your homework. Now, inside of this method, I'm actually going to log some information to the console so that we can see the value-based comparison be performed. So I'll cast the object to a car and then I'm going to use the format method on the print stream to print out the make and the model of each of the objects. So there I have my string set up. Now let's pass the arguments into it. First, the make of the current object instance, then the model of the current object instance, and then we'll look at the make of the object we're comparing and the model of the object we're comparing. Okay, so let me just tidy that up and then we're going to save that and now head over to our unit test. I'm going to go ahead right-click, go to run as [JUnit](../../Skills/Software%20Development/JUnit.md) test, and this time, you'll notice using the value-based comparison, our test pass. Let's take a look at the console output. You'll notice that when we run the contains test, the equals method was invoked multiple times to perform the comparison. Okay, let's scroll down here to the remove test. It's kind of easy to see what happened in this one. You'll notice the value-based comparison with equals was invoked multiple times.
>
> **[6:14](https://www.linkedin.com/learning/learning-java-collections/the-equals-method?u=76281980&t=374)** And using that, the collection was able to remove the other two cars and we only have the Tesla remaining. So once we switched from the identity-based comparison to the value-based comparison, that allowed methods on our collection, like remove, contains, and remove all to function properly. And that's why our unit tests were successful.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3), [JUnit](../../Skills/Software%20Development/JUnit.md) (1)
> **CLI Commands:** make (5), find (2)
> **UI Navigation:** right-click (2), go to (2), click on (1), select the (1), scroll down (1)
> **Env Vars:** ide (2)
> **Prerequisites:** setup (1), set up (1)
> **Cross-References:** we talked about (1)
> **Definitions:** is called (1)
> **Analogies:** for example (1)

#### [Challenge: Adding and removing collection elements](https://www.linkedin.com/learning/learning-java-collections/challenge-adding-and-removing-collection-elements?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/challenge-adding-and-removing-collection-elements?u=76281980&t=0)** - [Instructor] Now it's your turn to tackle a programming challenge that will require you to create some collections and use their methods to add and remove a few elements. In this challenge, the Landon Hotel is working on a room management system that uses [Java](../../Skills/Software%20Development/Java.md) collections to manage their inventory of rooms within a service. They've built skeleton methods on a room service class and have provided some comments about what each method should do, but they'll need your help in completing their implementations. To do this, you'll need to add a collection to the class, then instantiate it and then work on each method to complete its implementation. Before you get started, I recommend reviewing the unit tests that were provided to help you validate your work. Once you're done, just run the tests by right-clicking within it and then going to run as [JUnit](../../Skills/Software%20Development/JUnit.md) test. If you can get this bar to turn green, you've got it and your work is done. Now here's one tip before you begin. You should follow the numbering sequence specified in the room service class when completing your work. That's because the unit tests rely upon the order. Okay, go ahead, hit the pause button, give it a try, then continue the video and I'll share one solution to the challenge. You've got this. So how did it go? Were you able to get your test to pass? Let me show you how I approached it.
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/challenge-adding-and-removing-collection-elements?u=76281980&t=94)** I started out by declaring a new field of type collection that had a generic type of room. I named this field inventory. Then within our room service constructor, I took the inventory field and assigned it a new hash set that I instantiated. So I'm using a hash set as my collection implementation. You might have chose to use something like an array list here. That's fine. Now for the next method, I might have approached it a little bit differently than you did. I decided to use a conversion constructor in order to create a copy of our collection. I did this because I wanted to protect the collection from modification. Collections are mutable, so if we just returned our internal collection, somebody could modify the elements within it. But by creating a defensive copy, we protect against this. So that's what I chose to return for this method. For the next method, we needed to take the four parameters and create a room and then insert it into our collection. So first, I invoked the add method on our collection, and then I used the new operator and the room constructor, and then I just passed in all of the parameters that were provided to the method into the constructor. This created the new room instance that would be added to our collection. The next method required us to take an array of rooms and insert the rooms into our collection. So this is a great example
>
> **[3:07](https://www.linkedin.com/learning/learning-java-collections/challenge-adding-and-removing-collection-elements?u=76281980&t=187)** of when we can use a bulk insert operation. So I'm going to invoke the add all method on our collection, and then I need to convert the room array into a collection, so I'll go ahead and use the Arrays.asList method. This allows us to convert an array into a list. So then we have a collection to pass in to add all and all of that collection's elements will be inserted into our inventory collection. Then I moved on to the final method, removeRoom. This method seemed a little bit straightforward. We could just take our inventory collection, invoke remove, and pass in the room that we'd like to remove. But you'll notice if we execute the tests at this point, they're going to fail. Let's see that. That's because we haven't overridden the equals method in our room type and that's going to cause the remove method not to work properly. So let's head over to the room class and we can put that method in place. Once again, I'll just use the assistance provided by our IDE to keep it simple. So just right-click within the class and then go to source, then generate HashCode in equals. And then for the fields on the room that we'll use to perform the value-based comparison, I'm going to use the name and the type. So I'll uncheck capacity and rate. And those two fields will be used
>
> **[4:40](https://www.linkedin.com/learning/learning-java-collections/challenge-adding-and-removing-collection-elements?u=76281980&t=280)** to compare two instances of a room using a value-based comparison. Okay, so I'll generate that code and then we can execute our tests again to see if we pass the challenge. So I'll just right-click then go to run as JUnit test and there you can see we've passed. Excellent. So that completes the first challenge in this chapter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JUnit](../../Skills/Software%20Development/JUnit.md) (2), [Java](../../Skills/Software%20Development/Java.md) (1)
> **UI Navigation:** right-click (2), go to (2)
> **Code Identifiers:** aslist (1), removeroom (1)
> **Prerequisites:** you'll need (1), before you begin (1)
> **Env Vars:** ide (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Challenge: Advanced collection problems](https://www.linkedin.com/learning/learning-java-collections/challenge-advanced-collection-problems?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/challenge-advanced-collection-problems?u=76281980&t=0)** - [Instructor] We're going to do a back-to-back challenge, so you get more experience working with collections. In this challenge, the Landen Hotel has provided three additional methods on the room service class for you to implement. Like last time, they've provided a unit test for you to check your work. And within the body of each method, they've included the instructions for exactly what the method needs to do. The first method, hasRoom, will check if the room provided to this method as an argument, is found within our collection inventory. If it is, it'll return the boolean true. If it's not, it will return the boolean false. The next method, asArray, is going to return an array that contains all of the rooms found in our inventory collection in the order they were added. That last part about order is really important, and you'll want to keep that in mind while you're working on your solution. The final method is getByType. This method is expected to return a new collection containing all the rooms that match the type passed into the method as an argument. So you'll recall that our room class has a type field. You'll want to compare the argument that is provided into this method against that field. And that's going to determine whether or not you return the room in the new collection. Now there's one really important instruction here.
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/challenge-advanced-collection-problems?u=76281980&t=94)** The room inventory collection must not be modified, and the unit test will check for it. So here you might consider using a copy of the collection. Okay. That's the challenge. Go ahead and pause the lesson. Then when you're ready, hit the play button, and I'll share my solution. Hey, welcome back. That was a little bit more challenging, right? Let me show you my solution. So for the first method, hasRoom, I knew that the collection interface had the contains method on it. So I went ahead and invoked the contains method on our inventory collection and passed in the room that was provided to the method. I knew this would return the right results, because we had previously overridden the equals and hashCode methods. So it was just a matter of executing the statement and returning its results in line with the return statement. Okay, let's go ahead, and we'll move on to our next method, asArray. For me, this method was kind of a two-parter. First, I knew I needed to select the right method on the collection interface. So I ultimately went with the asArray method. So I invoked it on the inventory collection. And then I chose the overloaded form of the method that accepts an array. So I declared a new room array and pass that into the method. Now what this does for us is it types the array that is returned by the toArray method. This helps us avoid a cast.
>
> **[3:09](https://www.linkedin.com/learning/learning-java-collections/challenge-advanced-collection-problems?u=76281980&t=189)** Now if we were to execute our unit tests at this point, they would still fail, because we have this requirement to maintain order. If we inspect the implementation that we're using for our collection, you'll notice it's the HashSet. The HashSet doesn't guarantee order. So I had to switch up the implementation that we were using for our collection. I switched over to the linked HashSet that maintains insertion order. So as we add elements into the collection, it's going to hold them in the order they were added. Now you might have chosen to use one of the list implementations that maintains order. That works. So at this point, I think we're good on the tests. But before we run them, let's go finish up our last method, getByType. This method was also a two-parter for me. So the way I started out was by using the removeIf method. I invoked it on our inventory collection. And then I passed in a lambda expression to the removeIf method that check the type of the room and compared it against the provided type and made sure they weren't equal. This would remove all of the rooms from the inventory collection that didn't match the provided type. Now before we go any further, let me talk a little bit about the lambda expression. A lambda is a function that only specifies a parameter and a body.
>
> **[4:41](https://www.linkedin.com/learning/learning-java-collections/challenge-advanced-collection-problems?u=76281980&t=281)** You'll notice it doesn't have a name, and we don't specify the return type. The return type is inferred by the statement that I have highlighted. Between the parameter and the body, we place an arrow token to construct our lambda. All right, so that's a little bit about lambdas. We'll talk about those more later. Let's get back to our problem. Methods like removeIf, remove and add, they're going to modify the underlying collection, and collections are mutable. Our requirements say that the room inventory collection must not be modified. So I needed to find a way to handle this. To do that, I used a conversion constructor. So I declared a collection that had a generic type of room. And then I instantiated a HashSet and passed in the inventory collection to its conversion constructor. This created a new collection implementation that is not linked to the original, so I can modify it without impacting our inventory collection. So to do that, let's just go ahead, and we'll change over from using our inventory collection to the copy. The copy will have the rooms that don't match the type removed by the lambda expression. And then we can just go ahead and return that collection that has been modified. And all this doesn't change the room inventory collection. Okay. It's the big moment. It's time to run the tests.
>
> **[6:15](https://www.linkedin.com/learning/learning-java-collections/challenge-advanced-collection-problems?u=76281980&t=375)** So I'll just right-click within the unit tests, go to Run as, and then I'll select [JUnit](../../Skills/Software%20Development/JUnit.md) test. Hey, it looks like things past. Anytime you're running tests, you want to see green. So that's how I solved the challenge. Maybe you found another way. When working with collections, using the right method to achieve your goals can save you quite a bit of code and build more concise solutions. So anytime you feel like your collections code is getting a little bit cluttered, head over to the [Java](../../Skills/Software%20Development/Java.md) docs and see if you can find a method that does exactly what you want.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JUnit](../../Skills/Software%20Development/JUnit.md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** asarray (3), removeif (3), hasroom (2), getbytype (2), hashcode (1)
> **UI Navigation:** select the (1), right-click (1), go to (1)
> **CLI Commands:** find (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 3. Iterating Collections

[↑ Back to Table of Contents](#table-of-contents)

#### [Iterating collection elements](https://www.linkedin.com/learning/learning-java-collections/iterating-collection-elements?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/iterating-collection-elements?u=76281980&t=0)** - [Instructor] When working with [Java](../../Skills/Software%20Development/Java.md) Collections, it's common to traverse or iterate through each element in the collection, one after the other. Initially, the framework provided an iterator for the task, or a basic loop was used. Then, new versions of Java introduced language features like the enhanced for-each loop, and streams that gave us new approaches for navigating collection elements. At a high level, we can categorize the approaches for traversing a collection into three groups, loops, iterables, and streams. First let's talk about loops. These are the trusted while, do-while, and for loops that we learned when starting out with Java. They can be used to iterate collection implementations that allow positional or reference-based access to the elements that they hold. You'll notice I've placed for-each loops into this group. Using its concise syntax requires the object we are traversing to implement the iterable interface. So it's still a loop, but there's some crossover with iterables. Before discussing iterables and streams, let's talk about the collection interface methods that expose these capabilities. I've divided these methods into two subgroups based upon the interface they originate from. The first set comes from the iterable interface that the collection interface inherits from. It provides a functional style for-each method, and access to iterator and spliterator objects that we'll talk about next.
>
> **[1:32](https://www.linkedin.com/learning/learning-java-collections/iterating-collection-elements?u=76281980&t=92)** In Java 8, the collection interface added methods for obtaining a stream and a parallel stream. Let's look closer at what the iterable interface provides. It defines methods for obtaining an iterator and spliterator, which is used for sequentially accessing a collection's elements. When using an iterator, the has next and next methods are paired with basic loops to step through the collection's elements one at a time in a forward direction. Let's move on to our last category, streams. Streams support a [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) style within Java. When using this style, one or more intermediate operations are applied to a sequence of elements that flow from a source, like a collection, to form a pipeline. A single termination operation completes the pipeline by modifying the elements or returning a result. The operations used in a pipeline often accept Lambda expressions that determine the processing that will be applied to each element. The processing by any operation on a stream does not modify the underlying source collection. However, in some cases, it will return a new collection without modifying the source. In this chapter, we'll look closer at using iterators and streams with collections.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (5), [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) (1)
> **Versions:** java 8 (1)
> **Speakers:** - [instructor] (1)

#### [Accessing collection elements with iterators](https://www.linkedin.com/learning/learning-java-collections/accessing-collection-elements-with-iterators?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/accessing-collection-elements-with-iterators?u=76281980&t=0)** - [Instructor] Let's walk through a few examples involving iterables to see how to traverse a collection. To get us started, I've created a collection of rooms that's backed by an array list implementation. If we want to create an instance of an iterator from this collection, we can declare a variable of type Iterator and give it a generic type of Room. I'll then give it a name and from there we can invoke the iterator method on our collection. This is a factory method that returns a new instance of an iterator. I'm going to use this iterator to print some output to the console. To do that, I'll just invoke the next method on the iterator. Now because the iterator is backed by the collection, we can access collection elements through the next method. Each time we call the next method, it's going to advance one position forward in the collection and return an element. That element is going to be of type Room because our iterator is generically typed. So as I look through the methods that we can invoke on the element returned by the next method, you'll see that we have all the methods from the room type. I'll go ahead and print the name to the console. Now I'm going to do this two more times and each time that we invoke the next method, the iterator's going to advance and return a room. Now watch what happens when we execute this code. The iterator advances through the collection, returns the room and prints the name.
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/accessing-collection-elements-with-iterators?u=76281980&t=94)** So there we can see all of the names of our rooms in the collection. Now one thing that's important to know about this factory method is that it returns a new instance of an iterator. So if I do something like this and I create a second iterator instance, and then in this last line of code I switch over to use the second iterator instance, watch what happens when I execute the code. The first iterator instance prints Piccadilly and Cambridge and then you'll notice our second iterator instance prints Piccadilly. That's because the second iterator instance only tracks its own progress traversing through the collection's elements. It's separate from the first iterator. So through this example, you can see how we get different iterator instances from that factory method. Now this isn't how we typically use an iterator. Normally when using an iterator it's in combination with a while loop. So let me go ahead and create a while loop that uses our iterator to traverse through the collection of rooms. Okay, so there we see it and let's start to dissect this pattern. One of the most important parts is this call to hasnext within the condition of the while loop. Hasnext is a method on the iterator that's going to return a Boolean that indicates if the iterator has more elements to traverse through. If this method returns false,
>
> **[3:06](https://www.linkedin.com/learning/learning-java-collections/accessing-collection-elements-with-iterators?u=76281980&t=186)** we'll fall out of the wild loop. But while it's true, we'll continue iterating through our iterator. And inside of the body of our loop, we can invoke the next method to get each element from the collection. Now my IDE has placed a cast in here that's unnecessary, so I'm going to remove it and then I'll just print out each room's name to the console. All right, let's go ahead and we can execute this code, and there you can see how we can use a while loop and an iterator to traverse through a collection. This was a popular approach for iterating a collection prior to [Java](../../Skills/Software%20Development/Java.md) 5.0 when the enhanced for loop was introduced. This loop, also known as the for-each loop, provides a concise way to iterate through a collection. Let's check it out. I'm just going to type foreach in my IDE, then hit control + space, then enter, and this will cause Eclipse to build out for-each loop for me. This for-each loop iterates through all of the elements in our rooms collection. Now let's break down its syntax. On the right side of the statement, you'll see that we have our collection of rooms and you'll recall that the collection interface extends the iterable interface. Any object that we place on the right side of this statement is going to be iterated through and we need that iterable interface
>
> **[4:40](https://www.linkedin.com/learning/learning-java-collections/accessing-collection-elements-with-iterators?u=76281980&t=280)** to be implemented by that object because it's used to obtain an iterator. And each time that we iterate through our for loop, the next method on that iterator is called to get the next element in our collection. That element is going to be assigned to the variable declared on the left side of this statement. Now because our collection is generically typed, the internal code for the for-each loop can take advantage of that typing and it knows what type to assign each element retrieved from the iterator. Within the body of the loop, we can use this variable. So I'm just going to go ahead and use it to get the name of the room, and we can print it out to the console. Just remember with each iteration of this loop, an iterator is called to get the next element in our collection and that value is assigned to this variable. And we can see that in action if we execute our code because you'll notice that using the for-each loop, we were able to print out the name of each room in our collection. So those are some of the most common patterns involving iterables that we use to traverse a collection. In these patterns, loops play an important role. But as you'll soon see, we can achieve similar results in most cases using a [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) style.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1), [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) (1)
> **Definitions:** is a  (2), is called (2), known as (1)
> **Env Vars:** ide (2)
> **Versions:** java 5 (1)
> **Speakers:** - [instructor] (1)

#### [Modifying collections while iterating](https://www.linkedin.com/learning/learning-java-collections/modifying-collections-while-iterating?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/modifying-collections-while-iterating?u=76281980&t=0)** - [Instructor] When beginning to iterate through collections, most [Java](../../Skills/Software%20Development/Java.md) developers encounter the concurrent modification exception. It's almost like a rite of passage. So I'm going to show you how to avoid that. In our application, I've created a collection of rooms. Then we have some innocent looking code that iterates through the rooms using a for-each loop. Inside of the loop's body, we check to see if a room is pet friendly. Pet friendly is a new Boolean field that I've added to the room type and added accessors for. If the room is pet friendly, we're going to attempt to remove it from the collection of rooms. Now let's go ahead and see what happens when we execute this code. Yup, there it is, the concurrent modification exception. So let's talk a little bit about why that occurred. The concurrent modification exception is thrown when the underlying collection is modified while we're iterating through it. It can be triggered if we attempt to add, remove, or in general change the collection inside of a loop. This exception protects the collection from being changed by another thread while we're iterating through it. However, the exception is still thrown for single-threaded code like ours. Changes to a collection during iteration can have unexpected results at runtime, so that's difficult to troubleshoot. The iterator attempts to protect us from that by failing fast. But what if we still need to remove an element from a collection that we're iterating?
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/modifying-collections-while-iterating?u=76281980&t=94)** Let's take a look at one option. We can collect the elements we'd like to remove in a new collection. So what I'm going to do is create a new collection outside of the for-each loop where we're not iterating. So here we have a collection of type room that's backed by an array list. Inside of the body of our for loop, I can go ahead and switch up line 25. And instead of attempting to remove from the original collection, I'm just going to add the room to the new collection that we created that isn't part of the iteration. Now outside of the for-each loop where we're not iterating, what I can do is take our original collection of rooms and I can invoke the removeAll method on the collection. You'll recall this is a bulk operation that's provided through the collection interface. And if we just pass in the collection of rooms that we gathered during the iteration in a separate collection, we can then remove them all from the original collection. Okay, so let's go ahead and we can see this in action and you'll notice that the rooms Oxford and Victoria were removed from our collection. Those were the pet-friendly rooms. Excellent. Now, there's another way that we can do this using an iterator, so let's take a look at that.
>
> **[3:11](https://www.linkedin.com/learning/learning-java-collections/modifying-collections-while-iterating?u=76281980&t=191)** So first, let's get an iterator for our rooms collection. We can do that by invoking the iterator method on it. Okay, so now that we have our iterator, I'm going to create a while loop that uses the iterator to traverse the collection. This is the pattern that we saw before. And once again, my IDE attempts to perform the cast, so I'm going to remove that. And now inside of our while loop's body, I can just go ahead and grab this conditional statement. So I'll just cut that and paste it here. And then I'm going to go ahead and clean up this code that use the for-each loop. Now, when we're doing the iteration using the iterator, we can just replace line 26 with a call to the iterator's remove method. And this will allow us to safely remove an element from a collection that we're iterating without throwing the concurrent modification exception. All right, okay, so let's go ahead and we'll save this code and then I'll execute it and you'll notice once again, we've removed the pet-friendly rooms. Collections often provide several ways to achieve the same outcome, but sometimes that can get us into trouble like when we modify a collection that we're iterating. So stick to the methods that I showed you or better yet use a stream. I'll show you that next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Definitions:** is a  (2)
> **Code Identifiers:** removeall (1)
> **Env Vars:** ide (1)
> **Warnings:** troubleshoot (1)
> **Speakers:** - [instructor] (1)

#### [Accessing collection elements with streams](https://www.linkedin.com/learning/learning-java-collections/accessing-collection-elements-with-streams?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/accessing-collection-elements-with-streams?u=76281980&t=0)** - [Instructor] The Streams API and Lambdas added to [Java](../../Skills/Software%20Development/Java.md) 8 gave us a concise functional style for writing code. Let me show you through an example. In our application, we have a collection of rooms that I iterate through and check if the room is pet friendly. If it is, I write its name out to the console. This style of code is known as the imperative style. When we use an imperative style, we have to specify exactly how we want the code to behave. So I need to write the code that does the iteration, performs the conditional check and writes the information to the console. When we use a functional style with streams, it's a little bit different. It's more like we tell the code what we want to achieve. Let me show you. So first, think of a stream as a pipeline that our elements flow through but it never stores them like a collection. The elements are always moving forward through the stream. So first, we need a source of elements that can flow through the stream. We can find them in our collection. So on our collection, I'm going to invoke the stream method in order to start the pipeline that the elements in our collection will flow through. Now, on that pipeline, we can add operations, and our first one is going to be an intermediate operation named filter. It allows us to perform a test on the elements flowing through the stream that will determine if they continue on for further processing.
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/accessing-collection-elements-with-streams?u=76281980&t=93)** Now, to perform that test, we use an object known as a predicate. A predicate is a functional interface. So I'm actually going to create an instance of a predicate using an anonymous class. This is kind of a long way but it's going to show you how a collection works. So let's go ahead and import the predicate type. Just hover over the type and then select java.util.function.predicate, and then in order to add the method to the body of the predicate, just hover over the type again, and click on add unimplemented methods. And here we can see the signature of the functional interface. You'll notice that it accepts a parameter of type room. That makes sense. It's the type of element that's flowing through the stream because it's the type of element found in our collection. Now, in the body of this method, what we do is we perform some sort of test on the room. And in this case, we just want to check if it's pet friendly. And it's pretty easy because PetFriendly is a Boolean and we're just going to return the result of the method. That result will determine whether or not the element continues for further processing in the pipeline to the next operation. So let's go ahead and add that operation. We could add another intermediate operation but instead, I'm going to use a terminal operation named forEach. So a terminal operation is used to end the pipeline.
>
> **[3:10](https://www.linkedin.com/learning/learning-java-collections/accessing-collection-elements-with-streams?u=76281980&t=190)** It's either going to return a result or modify the elements that are streamed into it. Now, when we use the forEach method, we need to supply a consumer, which is another functional interface. So I'm going to use an anonymous class so that we can see this as well. All right, so let's go ahead and we'll import the Consumer type, and then we're going to add its functional interface method. And you can see that it just accepts a room as a parameter. I'll go ahead and change that parameter name. And then inside of its body, we're going to print out the name of the room to the console. Now, I'm going to do one more thing here. I'm going to add a little bit of debugging. So I'll use System.out.format and here I can provide a pattern. So we can say that we're testing, and then we'll provide a string and then we'll say that we got a result, and we'll provide a Boolean, and then I'll just put a new line. And now we can pass in the arguments to that pattern. The first one will be the name of the room, and the second one will be the results of our isPetFriendly call. All right, so what we're doing here is just printing the name of the room, and whether or not it's pet friendly. And I added this so that you can see how the stream works. Watch what happens when I execute this code.
>
> **[4:44](https://www.linkedin.com/learning/learning-java-collections/accessing-collection-elements-with-streams?u=76281980&t=284)** You can see the elements flowing through the different operations in the pipeline. So first, the Cambridge room went through the pipeline. And because our predicate returned false, the filter method blocked it from further processing. Now, you can see the next room, Oxford, was pet friendly. So the filter allowed it to continue on for further processing and then our consumer printed out the name of the room to the console. And you can see what happened with the other two rooms as well. One was allowed to process and the other was blocked. So that's how a stream works but I probably haven't sold you on the conciseness of the [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) style. And that's because I used those anonymous classes. So that's where we're going to pick up in our next lesson where we take a look at the lambda expression.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2), [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) (1)
> **Definitions:** known as (2), is a  (2)
> **Code Identifiers:** foreach (2), ispetfriendly (1)
> **Tools:** terminal (2)
> **CLI Commands:** find (1)
> **Env Vars:** api (1)
> **Versions:** java 8 (1)
> **UI Navigation:** click on (1)

#### [Lambda expressions](https://www.linkedin.com/learning/learning-java-collections/lambda-expressions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/lambda-expressions?u=76281980&t=0)** - [Instructor] In our last lesson, we were able to use the functional style to build out a pipeline using our collection, a stream and some operations. Now, it's definitely not as concise as we would like, and that's because we haven't used a lambda expression yet. Let's talk about that. You'll recall that a lambda is a function that only has parameters, an arrow and a body. Anything else, like a method name or a return type is unnecessary. So let's go ahead and convert these functional interfaces over to lambdas. To do that, we can remove everything to the left side of the parameter going into the method on the functional interface. Okay, the next thing we need is an arrow, and I'm just going to overwrite some of this syntax that was left from the previous code. Okay, so now we got our arrow, I'm going to remove this debugging as well. It's really not necessary. And then I'm going to remove everything on the right side of the method body. Okay, watch what happens. Now, all we're left with is the return statement. And that's not necessary for a lambda that doesn't have multiple lines. We can just remove the return statement, and now we get this nice concise lambda expression. Look what that did for our code. Okay, let's do the same thing for the forEach method by switching out the functional interface consumer with a lambda function.
>
> **[1:32](https://www.linkedin.com/learning/learning-java-collections/lambda-expressions?u=76281980&t=92)** You'll recall we're going to get rid of everything to the left of the parameter. Then we're going to replace the syntax with an arrow. Then we're able to remove everything to the right of the method body. Then let's just tidy this up, and look what that does for us. I told you it was concise, and it gets even better. We can replace our lambda with a method reference. A method reference allows us to pass a method into a function. So here I can use the Room type, and then specify that we'd like to invoke the isPetFriendly method, and that will be used as the predicate for our filter operation. Look at that. Okay, let's go ahead, we'll execute it. You can see that we got the same results as last time, except this time, it was only three lines of code, and it very much does say what we want to achieve. We want to stream the elements from our collection of rooms, remove any rooms that are not pet friendly, and then write then out to the console. So we've only scratched the surface of what operations are exposed on the stream interface but now you should understand the pattern of how we work with stream operations, and lambdas to build out a pipeline. Let's take a look at a few more operations next.

> [!info]- Semantic Content
>
> **Code Identifiers:** foreach (1), ispetfriendly (1)
> **Definitions:** is a  (1), is an  (1)
> **Speakers:** - [instructor] (1)

#### [Stream operations](https://www.linkedin.com/learning/learning-java-collections/stream-operations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/stream-operations?u=76281980&t=0)** - [Instructor] So we're going to take a look at a few more stream operations because streams are really important when working with collections, but I can't go into the details on everything because I don't want to lose that focus on collections. So let's just walk through a few examples and then you might want to look at the streams API on your own. So let's imagine that we wanted to gather all of our pet-friendly rooms into a new collection. You might do something like this. You might declare a new collection and then inside of the stream, you're going to pass in that collection and add the rooms into it. So we're doing two things here. We're passing in an external object and we're modifying it within the stream. Now in general, it's okay to work with external objects inside of your stream. It's okay to modify objects inside of your stream, but you should never do both. So there's an operation we can use to handle this. We can use the collect operation. It's a terminal operation that we can use within our pipeline. And the collect operation accepts a collector and we get several collectors on the collector's class via its static methods. And probably the one you'll use the most is toList. So essentially all the elements that flow into the collect operation are going to be gathered into a list and returned by this operation. So all we need to do then is just assign that list that's returned by the operation to our new collection and then we can go ahead and execute the code
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/stream-operations?u=76281980&t=94)** and you'll see that our stream here is going to print those elements in the collection out to the console. Now, if we look at this stream, there's actually a better way we can do this. You'll notice that we are printing out the result of invoking the getName method, because we just want to get the name of the room. What we can actually use another operation to do that, that operation is map. And what the map does is it takes in the elements coming in, so in this case, it's our room, and then we apply a transformation on the element, and that's going to determine what element flows out of the operation and further down the stream for processing. So here, I'm going to accept the room as a parameter into our function that will do the mapping. And then we're going to return a string from this Lambda expression. And that string is what will flow to the next operation. There, all we need to do is use a method reference to go ahead and print out our stream. So I'll use the println method and pass that into the for each operation. And now if we execute our code, look what happens. So we cleaned things up a bit there. Now let's walk through another example. Let's imagine that we wanted to get the total revenue of all the pet friendly rooms. To do that, we can use the rate on the room, which is a double.
>
> **[3:07](https://www.linkedin.com/learning/learning-java-collections/stream-operations?u=76281980&t=187)** We can see it here. Now there's another map operation that allows us to do this really easily and it's mapToDouble. And all we need to do is pass in a method reference that's going to return a double. So here I'll just pass in the getRate method and now we're going to be working with a double stream and this has another operation on it, sum. So let's go ahead and we will sum all of the doubles that are flowing into that operation. And they're going to get totaled and returned as a double. So let's store that in a variable and then we can print that variable to the console. Okay, and you can see we have a total of $450 in potential revenue for our pet friendly rooms. So those are just a few of the methods from the streams API that we can use to work with our collections. This course is about collections, but learning more of the streams API will definitely improve how you work with them. So I'm going to sprinkle in more examples with streams in the remainder of the course and if you'd like, take a deeper look at the streams API.

> [!info]- Semantic Content
>
> **Code Identifiers:** tolist (1), getname (1), maptodouble (1), getrate (1)
> **Env Vars:** api (4)
> **Analogies:** imagine (2)
> **Tools:** terminal (1)
> **Definitions:** is a  (1)
> **Best Practices:** you should never (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Iterating collections](https://www.linkedin.com/learning/learning-java-collections/challenge-iterating-collections?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/challenge-iterating-collections?u=76281980&t=0)** - [Instructor] Now that we've explored a few ways to iterate collections, let's try a programming challenge. In this challenge, the Landon Hotel has asked you to implement three new methods on the RoomService class. The first method applies a discount to the rate of each room. The rate is provided in decimal form as a double argument to the method. You'll need to use this double to reduce the rate assigned to each room. The next method requires you to return a new collection of rooms that contain rooms from the inventory, which have the same or a higher capacity than the argument provided to the method. The final method is similar. It asks you to return a new collection of rooms that match a particular type and are below a provided rate based on the supplied arguments. This challenge uses the same format as the others. There's a comment in each method describing what needs to be achieved, and a unit test is provided for validating your work. So that's the challenge. I recommend trying several approaches for each one, like using a forEach loop, then trying it again with a stream. This will get you more familiar with iterating collections. So if you're up for a challenge, pause the video and work on your solution. When you're finished, resume the video and I'll show you my solution. Hey, welcome back. I'm just going to go ahead and dive right into my solution.
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/challenge-iterating-collections?u=76281980&t=94)** So for the first method, we needed to apply a discount to each room in the collection. So that meant we needed to iterate over them. To do that, I used the forEach method provided by the iterable interface that accepts a consumer, which we can provide as a lambda. So I started out building the lambda by specifying its parameter as r. This is going to be how each room in the collection is passed into the lambda. From here, I could set the rate on the room but I also needed to do a little bit of math. So I got the original rate, and then I multiplied that by one minus the discount that was provided. And using this technique, I was able to reduce the rate of the room. Then I moved on to the next method, getRoomsByCapacity. This method accepted an integer and we needed to take that and go find rooms within our inventory collection that had a capacity that was greater than or equal to what was provided. To tackle this, I used a forEach loop. So let's go ahead and we'll put that into our code. Now, because I went this direction, it meant I needed to create a new collection that would hold all the rooms that matched our criteria. I backed this collection using a HashSet. So I instantiated that with its constructor and then inside of the forEach loop, I needed to check whether or not the room satisfied the condition.
>
> **[3:07](https://www.linkedin.com/learning/learning-java-collections/challenge-iterating-collections?u=76281980&t=187)** So I got the capacity of the room, and then I used the greater than and equal to operator and compared the room's capacity against the requiredCapacity that was provided into the method. From here, if the room satisfied the condition, I could just add it into our new collection, and then I needed to return that new collection with the matching rooms from the method. All right. I was able to move on at that point to provide an implementation for the getRoomByRateAndType method. This method accepted two parameters. One is a double. That was a rate. And the other was a string that's a type. We needed to search our collection inventory for rooms that had a rate lower than what was provided, and that matched the type that was also provided into the method. This is a perfect candidate for a stream, and I also felt guilty for not using it in the last method. So that's how I tackled this one. I got a stream from our collection of rooms, and then I applied two intermediate operations. Both were filter. So the filter I put in place first checked that the rate of the room was lower than the provided rate. And then I could move on and provide the second filter. And this one needed to check that the type of the room was equal to the provided type.
>
> **[4:44](https://www.linkedin.com/learning/learning-java-collections/challenge-iterating-collections?u=76281980&t=284)** Now, from here, I still needed to satisfy the method signature and return a collection of rooms. So this was a perfect situation to use the collect operation. So I specified the collect method and then I used a collector to gather the elements flowing through the stream into a list. Okay, so there you can see my solution. Let's cross our fingers and head over to the unit tests. I'll just right click on the test, go to Run As, [JUnit](../../Skills/Software%20Development/JUnit.md) test, and hey, we passed. Excellent. That still gets me excited every time and it wraps up our latest challenge.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JUnit](../../Skills/Software%20Development/JUnit.md) (1)
> **Code Identifiers:** foreach (4), getroomsbycapacity (1), requiredcapacity (1), getroombyrateandtype (1)
> **UI Navigation:** click on (1), go to (1)
> **Definitions:** is a  (2)
> **CLI Commands:** find (1)
> **Cross-References:** in the last (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)


### 4. Set

[↑ Back to Table of Contents](#table-of-contents)

#### [Set](https://www.linkedin.com/learning/learning-java-collections/set?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/set?u=76281980&t=0)** - [Instructor] Up until this point in the course, we focused mainly on the collection interface and its methods Now, we'll start to examine specific collection interfaces and implementations, starting with a quick look at sets. The main thing that differentiates a set from other collections is that it does not store duplicate elements. This is controlled internally by the collection, which performs a comparison of new elements inserted via the add method or a constructor against those that already reside in the collection. Our deep dive on the collection interface will come in very useful when working with a set. That's because the set only adds two additional methods beyond those found on a collection interface. These two additional methods, of() and copyOf(), were added in later versions of [Java](../../Skills/Software%20Development/Java.md) to help us create immutable sets. If we examine the type hierarchy of a set, you'll see the interfaces that extend it and other set implementations. You'll be very familiar with the basic HashSet implementation because it doesn't contain any additional methods beyond those specified on the set interface. The actual implementation uses a hash map to back the set and elements in it do not retain any order. It's subclass, LinkedHashSet, uses a linked list to order elements, according to when they were inserted. The SortedSet interface is used for sets that iterate through elements in ascending order. And its sub interface, NavigableSet, extends that behavior to allow us to search for elements.
>
> **[1:36](https://www.linkedin.com/learning/learning-java-collections/set?u=76281980&t=96)** When working with the TreeSet implementation of these interfaces elements must implement the comparable interface,, or we need to provide a comparator to determine the ordering. So, that's a high level look at sets. Let's jump into some code in our next lesson.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** copyof (1)
> **Speakers:** - [instructor] (1)

#### [HashSet](https://www.linkedin.com/learning/learning-java-collections/hashset?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/hashset?u=76281980&t=0)** - [Instructor] The set interface is nearly identical to the collection interface that we've studied. However, it does have a few additional methods, and its implementations have some unique behaviors. To see these unique characteristics of a set, let's walk through a few examples. I'm going to start out by creating a set of rooms, and I'm going to use a hash set as the implementation for my set. All right, let's go ahead, import the types, and then I'm going to start adding rooms. I'll first add Piccadilly, and then I'm going to add Oxford. Now, I'm also going to add the same exact instance of Oxford followed by a logically equivalent instance of Oxford named Oxford Duplicate. You can see that object here. If we performed a value-based comparison between these two objects, they would be logically equivalent to each other. Keep that in mind as we walk through the example. Now, I'm also going to use a stream in order to print out our room names. So using a map, I can get the name of each room and then I can use the For Each method to print that to the console. Okay, now, before I execute this code, I want to show you one more thing. In the room class, I've added some console output within our Overridden Equals method. It's just going to tell us more about the comparison so that we can see what's happening with the set internally as we execute this code, so let's check it out.
>
> **[1:36](https://www.linkedin.com/learning/learning-java-collections/hashset?u=76281980&t=96)** You'll notice that the equals method was invoked, and that was for the comparison with the logically equivalent instance of Oxford. For these two objects, it's likely that the equality operator was used, and that's why we don't see equals invoked again. Now you'll notice, we only have one instance of Oxford, and that's because a set doesn't allow duplicates. The other thing I'll call your attention to is the order. The stream didn't iterate through the collection in the order that the elements were inserted, and that's because the hash set doesn't guarantee order. If we wanted to maintain the insertion order of our elements, we would have to switch over to a linked hash set implementation. So, watch what happens when I make that change. Now, you can see Piccadilly, which was inserted first into the set, prints out first. Okay, now let's take a look at some of the new methods that were added to the set interface. I'm going to create a new set named Other Rooms, and then I'm going to grab the set interface, and we're going to use the Of method to create an unmodifiable set. So, this is a set that you can't change. It can be helpful for thread safety or sometimes for performance. You'll notice that the Of method has several overloaded [Forms](../../Skills/Web%20Development/Forms.md) where we can add up to nine elements into the set,
>
> **[3:09](https://www.linkedin.com/learning/learning-java-collections/hashset?u=76281980&t=189)** and there's even a VAR args form of this as well. Let's go ahead and use the overloaded form that accepts two elements. I'll pass Piccadilly into the method, and then I'm also going to pass in Oxford. Now, watch what happens when I try to change our unmodifiable set. If I use the Add method and try to place Victoria into the set, if we execute the code, you'll notice that an exception is thrown. Let's check it out. It's the unsupported operation exception. So on the unmodifiable set, you won't find any support for methods that are going to change the collection. So let's go ahead, we'll comment this out, and then I want to show you another method. So we'll come down here, and I'm going to create a new set of rooms. I'll call this More Rooms, and then we're going to use the Copy Of method on the set interface. This creates an unmodifiable copy of a collection. So let's go ahead and we'll pass in our Rooms collection. And just like the other set that we created, we can't perform any operations like an add or remove that are going to change this set, but let's do this. I'm going to change our stream to use more rooms as its source. So this is our unmodifiable set. And let's first go, we can print out the elements in that collection, and you'll notice we got
>
> **[4:43](https://www.linkedin.com/learning/learning-java-collections/hashset?u=76281980&t=283)** Oxford and Piccadilly. Now I'm going to go ahead and add Victoria into the collection that we copied. Watch what happens now. You'll notice that our unmodifiable set is backed by the first collection that we copied. So, you'll have to be aware of that when working with an unmodifiable set. So, that's a look at some of the recent additions to the set interface and a few of its implementations.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Forms](../../Skills/Web%20Development/Forms.md) (1)
> **CLI Commands:** make (1), find (1)
> **Env Vars:** var (1)
> **Definitions:** is a  (1)
> **Analogies:** just like (1)
> **Warnings:** be aware (1)
> **Speakers:** - [instructor] (1)

#### [TreeSet](https://www.linkedin.com/learning/learning-java-collections/treeset?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/treeset?u=76281980&t=0)** - [Instructor] TreeSet is another set implementation that uses a binary search tree to organize the elements it stores. The relationships between two elements is used to structure the tree that stores data in a TreeSet, so the collection must have a way to compare whether an element is greater than, less than, or equal to another element. To do this, the TreeSet uses a comparator, or elements that implement the comparatable interface, to perform the relational comparison between the elements. Unlike the other collections we've seen, the TreeSet uses this relational comparison to evaluate elements instead of the equals method. This introduces some challenges when working with custom objects in a TreeSet. There can be problems that are unique to your exact situation, so if you're using custom objects make sure you refer to the [Java](../../Skills/Software%20Development/Java.md) docs. Let's walk through an example that uses a TreeSet to store integers. The integer is a great choice for working with the TreeSet because it has a natural sort order. This sort order places integers into an ascending numeric order. We can get started by declaring a variable that uses a NavigableSet as its type. Inside of the set I'm going to store integers, so I'll specify that as its generic type. I'm just going to call this set the numberTree, and then I'm going to use the TreeSet as my set implementation. Now, here, I'm going to pass in this list of numbers, that we've created, into the constructor.
>
> **[1:37](https://www.linkedin.com/learning/learning-java-collections/treeset?u=76281980&t=97)** And then I can go ahead and import the types, and now I have my TreeSet. All right, now let's start out simple and we can stream this TreeSet to the console. So I'll just use the forEach method, within the stream, and that should print everything out. Now, watch what happens as I execute this code. The numbers are placed in ascending order in accordance with the integer sort order. Now, we can do some different things from here. If we wanted to get the set in descending order we can use the descendingSet method to switch it up, and you'll notice that now we have our integers iterated through in descending order as returned by the descendingSet. Another method, headSet, allows us to get all of the integers, in the tree, that are lower than a particular number, so, here, I'm going to get a new set that contains all integers lower than 1,750. There, you can see that set. It also has a counterpart, tailSet, which will give us all numbers higher than a particular number, so now we're going to get a set with numbers greater than 1750. There's also a subSet that allows us to pick a set that is between a range of two numbers, so I'll go ahead and pick all the numbers between 1750 and 2750. Now let's watch what happens there.
>
> **[3:10](https://www.linkedin.com/learning/learning-java-collections/treeset?u=76281980&t=190)** You'll notice that I got everything between the two numbers that I provided. Now there's also methods for getting numbers within the proximity of another number, so, here, I'm going to get the number that is just lower than 750, and that's going to be 500. There's a counterpart method, here, higher that we can use that will give us the next number that is just higher than 750, and that's 1,000. So that's a quick glance at the TreeSet implementation, and we got to see a few of those methods that are available on the NavigableSet.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** descendingset (2), numbertree (1), foreach (1), headset (1), tailset (1)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 5. List

[↑ Back to Table of Contents](#table-of-contents)

#### [List interface](https://www.linkedin.com/learning/learning-java-collections/list-interface?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/list-interface?u=76281980&t=0)** - [Instructor] The List interface and its implementations are the next group of collections that we'll explore. Within this group, we'll find some of the most frequently used classes in [Java](../../Skills/Software%20Development/Java.md), like an ArrayList and a LinkedList. Lists are very helpful because they allow us to store an ordered group of the elements and provide a set of methods for working with those elements by their position in the structure using random access. An elements position is specified by using a zero-based index, which is similar to how we work with an array. On the type hierarchy of the list, there are four basic implementations. Most Java developers consider the Vector and Stack types that came before collections to be obsolete. So we'll only dive deeper into the ArrayList and the LinkedList implementations. In this type hierarchy, you'll notice some crossover between the LinkedList and the hierarchy of the Queue interface. That's because the LinkedList implements the Deque interface. This relationship causes some additional methods to appear on the LinkedList that are not found on the ArrayList. Of the two, the ArrayList implementation is the most frequently used. As its name implies, it is internally backed by an array. This array is created with an initial capacity upon instantiation of the list. If we exceed the initial capacity of the internal array when adding a new element, the ArrayList automatically resizes by creating a new, larger array
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/list-interface?u=76281980&t=93)** that holds the new and existing elements. Operations that add or remove an element in the middle of the array list will automatically adjust the position of the remaining elements in its internal structure. It does this by copying them to their new indexes. This can have performance implications if we need to insert many elements into the middle of a large ArrayList. However, reading or retrieving an item from the ArrayList is relatively fast since we can use random access to get an element by its position. The LinkedList implementation uses a doubly linked list as its internal structure instead of an array. It uses nodes that contain references that link objects together into a sequence that [Forms](../../Skills/Web%20Development/Forms.md) the LinkedList. Each node in the list holds an element, a reference to the next element, and a reference to the previous element. The ends of this internal structure will point to a null reference in memory. This is how we can tell if we're at the end of the list. So when an element is added to the LinkedList, the original references are broken. Then new references are established to point to the new next and new previous elements. The same re-linking process occurs when we remove an element from the LinkedList. In some situations, it provides better performance for these operations. This makes adding elements into a LinkedList relatively fast because we can just re-point the nodes references.
>
> **[3:08](https://www.linkedin.com/learning/learning-java-collections/list-interface?u=76281980&t=188)** However, a LinkedList is not ideal for retrieving an element. That's because we need to traverse all of the references that proceed the element that we want to access. Unfortunately, there's not a silver bullet for choosing a list implementation. It's going to depend on which methods you use the most and the number of elements that are in it. Now, most developers will lean heavily towards the ArrayList and some developers will even question if we need the LinkedList. So here's my advice. Start with an ArrayList and use the List interface as your variable type. Then if you encounter performance issues, you can just change up the implementation later.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2), [Forms](../../Skills/Web%20Development/Forms.md) (1)
> **CLI Commands:** find (1), node (1)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)

#### [List implementations](https://www.linkedin.com/learning/learning-java-collections/list-implementations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/list-implementations?u=76281980&t=0)** - [Instructor The List interface has many methods that deal with an element's position in the list. To help with this, method signatures will often accept an index as an argument, or use the index as the return value. Let's visualize a few of these methods using a list of strings. We'll start out by inserting elements into a list. Using the add method from the collection interface causes an object to be upended at the back of the list. So after this first call, if we call the method again, the element is placed at the second position in the list. You'll also notice that we can add duplicate elements into our list. The List interface also has an overloaded form of the add method that allows us to add an element at a specific position. To do this, it accepts an integer as its first argument, and the object to insert as its second argument. The object is then placed into the list at the index specified by the integer. When calling any method on a list that accepts an index, we'll receive an index out of bounds exception if the index is negative or if it exceeds the size of the list. Another way to insert list elements is using the add all method. It performs a bulk insert operation that inserts elements from a provided collection and a specified position in the list. We can replace an element at a specific position
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/list-implementations?u=76281980&t=93)** using the list set method. So let's replace that letter A in the fifth position, that's ruining our sequence. The method replaces the element at the provided index with the object that is provided as the second argument. Its return value is the element that was in the previous position. In this case, that was the letter A. Let's move on and look at retrieving elements from our list. We can access an element at a specific position by passing an integer to the GET method. This will cause the element at the index to be returned. If we have an object and we need to know where it's positioned in the list, we can pass it to the index of method. This method returns an integer that denotes the index of the object's first occurrence in the list. If the object isn't in the list, the method returns negative one. Let's look at one final method so we can see another way to remove list elements. This remove method is an overloaded form of the remove method found on the collection interface, and it behaves a little bit differently. It accepts an integer that identifies the position of the element that we want to remove. Then, in addition to removing the element at the index, it also returns the element that we're removing. It's a nice feature if you also need to retrieve the element that you're taking out of the list. These methods are very useful when working with a list. And don't forget, the list interface extends the collection interface.
>
> **[3:06](https://www.linkedin.com/learning/learning-java-collections/list-implementations?u=76281980&t=186)** So, you also have those methods to work with too.

> [!info]- Semantic Content
>
> **API Endpoints:** get  (1)
> **Definitions:** is an  (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor (1)

#### [ArrayList](https://www.linkedin.com/learning/learning-java-collections/arraylist?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/arraylist?u=76281980&t=0)** - [Instructor] To help us work with the methods on a list I've added a new guest class to our application. This class is going to store information about the guests of the Landon hotel, so things like their first name and last name. You'll also notice a boolean, loyalty program member on this class. This boolean is used to indicate whether or not a guest is a member of the loyalty program, which can give them some perks. So for example, in a minute, we're going to talk about a check-in list. The list is used to keep track of guests as they arrive at the hotel. So we know the order in which they should be checked in. If a guest is a member of the loyalty program, they are automatically placed ahead of non loyalty program members on the check-in list, just one of the perks. Now, you'll also notice I've added a list of rooms to this class. This list is going to store the rooms that a guest prefers to stay in while at the hotel. Now let's start working on our application. To get started, I'm going to create a list of guests and I'll name it check-in list. And then for the implementation of the list, I'm going to use an arry list. Now, one of the things I'm going to do is pass an integer as an argument into the constructor, let's just say 100. What this does is it declares the array list with a larger initial capacity. By default, the array list will have an initial capacity of 10.
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/arraylist?u=76281980&t=93)** And if we're going to add a lot of items into the array list, it will quickly need to resize. So we can provide this larger initial capacity in order to avoid that resizing, in the event, we know we're going to store a lot of elements. So let's just imagine the Landon Hotel's going to check in a hundred guests today, we might want to go ahead and up that initial capacity. Okay, So we got our check-in list established just in time because two guests have just arrived. [John](../../Glossary/Tool/John%20the%20Ripper.md) has just walked into the lobby and he was followed closely by Maria. So we need to place John in the first position in our check-in list, and then Maria, in the second. Let's go ahead and we can do that using the add method. So I'll just go ahead, pass in John, and then I'll do the same thing for Maria. Okay, and then to see who is at what position in the list, I've built a small utility method that we're going to use to print that information to the console. So there, we can see John at the first position a nd Maria at the second. Now our next guest has just arrived and that's Sonia, she's a member of the loyalty program. So she needs to be placed ahead of John and Maria in the list, let's go ahead and do that. So inside of our class, we can use the overloaded add method on the list interface that accepts an index. And since Maria needs to be at the first position,
>
> **[3:06](https://www.linkedin.com/learning/learning-java-collections/arraylist?u=76281980&t=186)** I'll just provide a zero as the index in our first argument. And then we just need to pass in Sonia, as the second argument, that'll be the element added at that position, and then I'll go ahead and print the list so we can see what's going on. Okay, so now Sonia is at the first position in the list. Now having seen the benefits of the loyalty program, convinces Maria to join. So let's go ahead and add her into the program. I'm going to get Maria from the list, she's in the third position, so that's going to be index two and then I can set her loyalty program member status over to true. Okay, now she's in the program. Let's take a look at what we need to do. So Maria's in the loyalty program, so she needs to be ahead of John in the list. But right after Maria joined, Siri walked into the lobby and Siri's a loyalty program member. So now both Maria and Siri need to be placed ahead of John in the list, in positions two and three respectively. Let's go ahead and do that using a bulk insert operation.
>
> **[4:27](https://www.linkedin.com/learning/learning-java-collections/arraylist?u=76281980&t=267)** Okay, so I'm going to use our check-in list and then I'll invoke the add all method and I'll use the overloaded form that accepts an index. Now we're going to be placing this collection in the list at the second position, so that means we need to specify an index of one. Now we need to pass in the collection that we would like placed at that position. And in the collection, we're going to provide two elements. The first was Maria because she was in the loyalty program prior to Siri's arrival. And then the second will be Siri. So what will happen is at index one or position two, we'll insert Maria. And then after that, we'll insert Siri. We can see what that looks like by just going ahead and printing our list to the console, okay. So we knew Sonia would be in the first position. Then we see Maria as expected followed by Siri and then John who actually arrived first, but is not a loyalty program member is now in the last position in list. Well, kind of, you can see we have an issue here where we never removed Maria from the list, and she's no longer at that spot. Let's go ahead and we can take care of that by removing the last element in the list. So we can specify that index by getting the size of the check-in list and then just subtracting one from it.
>
> **[6:03](https://www.linkedin.com/learning/learning-java-collections/arraylist?u=76281980&t=363)** Okay. Now, if we go ahead, we can print our list again, and then we can see that last element, the duplicate of Maria was removed. Now at this point, John is extremely confused. He has no idea what position in the list he's in, so let's help him out and we'll tell him that. So to do that, I can just use the check-in list and I'll invoke the index of operation on the list. Here I pass in John, and this will return the index that John occupies within the list. Let's go ahead, we can run this code. And there you can see where indicating that John is at the third index in the list. Now, hopefully John knows how programmers count if we tell him he's at the third index, maybe it's best we just tell me it's in the fourth position. So these examples show you how position based methods found on the list, make it easier to manage groups of objects in collections, where we can leverage random access.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [John](../../Glossary/Tool/John%20the%20Ripper.md) (13)
> **Definitions:** is a  (2)
> **Analogies:** for example (1), imagine (1)
> **CLI Commands:** make (1)
> **Cross-References:** in the last (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Working with Lists](https://www.linkedin.com/learning/learning-java-collections/challenge-working-with-lists?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/challenge-working-with-lists?u=76281980&t=0)** - Now it's time to put your new skills to the test in a programming challenge. The Landon Hotel is building a guest service class to help manage guests room preferences and check-in. In the class, you'll find three methods stubs and instructions for completing each method. Unit tests have also been provided like the past challenges to help you validate your work. The first method that you'll need to complete is filter by favorite room. It asks you to build a new collection of guests from the provided list. The new collection should contain guests who've placed the provided room in the first position of their preferred room list. The next method check-in requires you to enter a guest into the check-in list similar to our last lesson. However, you'll notice the list has now been added into the guest service class. For the challenge, the check-in method must add the guests provided as an argument into the check-in list. Guests are added into the list in the order they're passed to the method, with one little caveat, loyalty program members will be placed ahead of other guests that are not in the program. The final method you'll need to complete is swap position. Here you'll trade the places of two guests within the check-in list. To do this, it must check that both guests are in the list prior to doing the swap. Make sure you follow the numbering sequence
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/challenge-working-with-lists?u=76281980&t=94)** and code this method last because it relies upon guests being checked in. So that's the challenge. Just be aware, the second method is a little bit more difficult, but I'm sure you're up for it. So go ahead and pause the video and give it a shot when you're ready, resume the video to see my solution. Hey, welcome back. Are you ready to see my solution? In the first method we were provided with a list of guests and a room. We need to find the guests in the list that had indicated the provided room was their first preference in their preferred room list. Now I thought this was a great candidate for a stream and a filter operation. So I took our lists of guests and I stream them into the filter method. And from here, I used a Lambda expression to create a predicate. In the predicate, I got the preferred room list for each guest and I invoked the index of method on it, passing in the room as an argument. This gave me the position of the room within the preferred room list. I knew that if the room was in the first position, it was their favorite. So I could go ahead and invoke the collect method and use a collector in order to gather these guests into a list. This satisfied the return type of the filter by favorite room method, and my work was complete. The next method check-in was made a little bit more difficult by some edge cases involving the loyalty program.
>
> **[3:09](https://www.linkedin.com/learning/learning-java-collections/challenge-working-with-lists?u=76281980&t=189)** The first thing that I did was I created an if statement that had an else block. Inside of the conditional for the if statement, I checked to see if the guests was a loyalty program member. Now, I also decided to do a check to make sure that the check-in list was not empty. If either of these conditions were false and we dropped into the else block, the logic was fairly simple. We could just go ahead and add the guest into the check-in list. That's because we didn't need to place them in any special position because they weren't a loyalty program member or the check-in list was empty and we just put them in the first position. Now, the logic got much more difficult if the guests were in the loyalty program. To handle this, I created a for loop that iterated through all the guests in the check-in list. Now, one thing I did with the for loop is I took the initialization statement and I moved it outside of the for loops declaration. And this allowed me to access the counter outside of for loop, and you'll see why that was important in a minute. Inside of the for loop, I created another if statement and here I used the check-in list to get each guest with the counter. So I passed the counter in, which was the index, and that got me each guest out of list. And then I just checked to see if they were a loyalty program member. If they were, I just continued the loop
>
> **[4:45](https://www.linkedin.com/learning/learning-java-collections/challenge-working-with-lists?u=76281980&t=285)** because I knew their position in the list was secure and we weren't going to put somebody in front of them. Now, if we iterated through this loop and a guest were not a loyalty program member, that would mean we wanted to put a loyalty program member at their position. So at this point I broke the loop and I knew that I had captured the position at which I broke the loop in the counter. So from there, what I could do was just place the guest into the check-in list at that position designated by the counter. So here you can see I pass in the counter and then I pass in the loyalty program member and they will assume that spot within the list. So that's how I solved the problem with the check-in method. And from there, I moved on to work on the swap position method. This method accepted two guests as arguments, and we needed to exchange their positions within the list. So to do that, the first thing that I did was I got the position of the first guest within the check-in list using the index of method. And then I did the same for the second guest. So I just created another integer position two, and then passed in guests two, into the index of method. Now I wanted to make sure that both guests were in the check-in list.
>
> **[6:18](https://www.linkedin.com/learning/learning-java-collections/challenge-working-with-lists?u=76281980&t=378)** So to do that, I just created an if statement and inside of the if statement, I checked that the position was not equal to negative one. And then I just did that for the second position as well. So inside of the body of this if statement, we could successfully perform the swap because we knew that both guests were in the check-in list, Let's go ahead and we'll work on the swap. We can do that using the set method. So all pass in position one as my index, and then I'll place guests to in position one. And then from there, I'll just do the inverse. I'll pass in position two as the index, and then guess one will be placed in position two. And that successfully completes our swap. All right, let's go ahead, we'll run our tests to make sure everything checks out. So I'll just right click, go to run as J unit test and there we can see our test pass. Now you might've gone about the solution for this challenge in a different way, and that's okay because when you're working with collections, there's many ways to solve a problem.

> [!info]- Semantic Content
>
> **CLI Commands:** make (4), find (2)
> **Prerequisites:** you'll need (2), initialization (1)
> **Warnings:** caveat (1), be aware (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)
> **Analogies:** similar to (1)
> **Speakers:** - now (1)
> **Non-Speech:** (upbeat music) (1)


### 6. Queue and Deque

[↑ Back to Table of Contents](#table-of-contents)

#### [Queue interface](https://www.linkedin.com/learning/learning-java-collections/queue-interface?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/queue-interface?u=76281980&t=0)** - [Instructor] Next, we'll explore the interfaces and implementations related to queues within the [Java](../../Skills/Software%20Development/Java.md) collections framework. If you've ever stood in a line you're familiar with a queue. You enter at the back of the line, which is also known as its tail. Then you wait until you're at the head of the line, or we might say the front of the line before it's your turn. In Java, a queue is a collection that is used to hold elements for processing, sort of like the line we just discussed. Queues typically work the same way. The first item in the queue is the first to be processed, just like the first person in a line is the first to get their turn. We call this first-in, first-out, or FIFO order. Most implementations of a queue are FIFO, but there are some that use a priority ranking or another ordering strategy to arrange their elements for processing. To work with the queue, six methods are included on its interface in addition to those that are inherited by extending the collection interface. Using a visual, we can see how these methods are used to work with the queue. Methods like add or offer are used to add items to the tail of the queue. Operations like pull and remove are performed at the head of the queue. These methods are used to retrieve the next element in the queue for processing and will remove it from the collection. If we want to examine the element at the head of the queue without removing it, we can use the element or peek methods to get a reference to the element. Now, if we actually wanted to remove the element at the head, we would have to use poll or remove,
>
> **[1:35](https://www.linkedin.com/learning/learning-java-collections/queue-interface?u=76281980&t=95)** which we just saw. You might be wondering why there are two methods for each of these operations on the queue. The difference between them is how they deal with a full or an empty queue. There are queues that have a maximum capacity, which are known as a bounded or blocking queue. If we attempt to insert an element into a bounded queue which is full, the add method will throw an exception, while the offer method will return false. A similar situation occurs when the queue is empty. If we try to remove or examine the head element in an empty queue, the methods remove an element will throw an exception, while poll and peek will return null. If we take a look at the type hierarchy for a queue, you'll notice that two of its implementations actually implement the deque interface that extends queue. Of these implementations, ArrayDeque is the most commonly used queue and deck implementation. The LinkedList is not used often, and the PriorityQueue uses a comparitor or elements implementing the comparable interface to determine their position within the queue. So it's one of those queues we mentioned that don't use FIFO order. I should point out that I've only listed types found in the java.util package. There's another group of specialized queue interfaces and types found within the java.util.concurrent package for handling multithreaded scenarios. We'll mainly use the ArrayDeque implementation for working with a deque and a queue. So let's first do some hands-on examples,
>
> **[3:09](https://www.linkedin.com/learning/learning-java-collections/queue-interface?u=76281980&t=189)** working with it as a queue, then we'll take a closer look at the deque interfaces later in the chapter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (4)
> **Definitions:** known as (2), is a  (1), we call this (1)
> **Env Vars:** fifo (3)
> **Cross-References:** we mentioned (1), later in (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Queue implementations](https://www.linkedin.com/learning/learning-java-collections/queue-implementations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/queue-implementations?u=76281980&t=0)** - [Instructor] Let's walk through a few examples to see the methods on the queue in action. To do this, we're going to revisit the check-in list scenario that we used when working with a list. You'll see that this problem is much better suited for a solution that uses a queue. So to start out, I'm going to create a queue that holds guests and I'll name it the checkinQueue. For the queue implementation, I'm going to use an array deque. Okay, so now we have our queue. The first thing that I'm going to do on this queue might seem a little bit odd, but I'm going to attempt to retrieve the element at the head of our empty queue using the remove method. And then from there, I'm just going to print that element or guest to the console. Now watch the behavior here. You'll notice that the method throws an exception because I'm trying to remove the element at the head of an empty queue. Now if I were to switch over this method from remove to poll, we'll get different behavior and you'll notice that it returns null. So that's the difference between some of those behaviors we discussed for the method pairs that insert, remove, and examine elements in the queue. Some throw exceptions while others return a special value like a null or a Boolean. Now let's add some guests into the queue so we can work with it. Now you'll notice we could use the add method and this is one of the methods that will throw an exception
>
> **[1:35](https://www.linkedin.com/learning/learning-java-collections/queue-implementations?u=76281980&t=95)** if we attempt to add too many elements to a bounded queue. And we can see that within the [Java](../../Skills/Software%20Development/Java.md) docs. it's always a good idea to take a peek at the behavior of the method within the IDE so you get the exact one you'd like. Now I don't want to throw an exception, so instead I'm going to use offer in order to add [John](../../Glossary/Tool/John%20the%20Ripper.md) into the queue. Okay, so now we have John in the queue. Let's go ahead and we'll add Bob as well. With both guests in the queue, I'm then going to use a small utility method that I've added to the class that prints out the queue's contents and gives us some information about what guest is in each position in the queue. So let's go ahead. We'll invoke that right after adding our guests into the queue. Then I'm going to go ahead and call that method again after we pull the queue and remove John. Okay, so let's go ahead and we'll execute this code, and you'll notice that both guests were added into the queue. First, John was added at the head of the queue and then Bob was inserted after him. Now when we polled the queue and removed the element at the head, it removed John and then you'll notice that Bob advanced to the head of the queue. So that's the behavior of first-in, first-out order. We inserted John first and he was the first to removed. Okay, let's now add two other guests into our queue. First, I'm going to add Sonia followed by Siri.
>
> **[3:10](https://www.linkedin.com/learning/learning-java-collections/queue-implementations?u=76281980&t=190)** Then I'm going to copy this block of code and modify it to use the peek method. So I'll switch over to guest1 and then instead of polling I'm just going to peek, and then we'll print out guest1. Now let's see what happens here. So you'll notice that the four guests were added into the queue and then we polled one element from the queue and that got us John who was at the head. But then when we peeked we got the element at the head of the queue, but that element remained in the queue. And that's the difference between poll and peek. Poll is going to retrieve and remove the element at the head of the queue while peek is just going to retrieve it for us without removing it. Now throughout this example, I've been using all of the queue methods that do not throw exceptions. We could easily switch over to them. So I'm just going to replace offer with add in my code and then I can close that dialogue, and then we'll switch over from poll to remove and then the equivalent method for peek is element. So these methods would all throw exceptions if we were dealing with a full or an empty queue. Now that's not the case, so we don't see that when we execute them. But aside from the exceptions and the return types, the behaviors are all the same as their counterparts. Now I want to show you one other queue implementation. You'll recall that I mentioned
>
> **[4:42](https://www.linkedin.com/learning/learning-java-collections/queue-implementations?u=76281980&t=282)** not all queues are first-in, first-out order. That's the case when we use the priority queue implementation. This implementation allows us to provide a comparitor that determines the order that elements are stored in the queue. So I'm going to switch over to the priority queue and then we need to build a comparitor for it. So to do that, I'll declare a comparitor and it will have a generic type of guest, and the competitor's going to help us with our loyalty program. So I'm just going to call it programComp. And then in order to create the comparitor, I can use the static comparing method that is found on the comparitor type. From here I can pass in a method reference to the isLoyaltyProgramMember method and then I'm going to reverse the comparitor so that it sorts our guests in descending order. This is going to place loyalty program members ahead of non-loyalty program members. Okay, now once we have the comparitor, we just need to pass it into the constructor of the priority queue. Now watch what happens when I execute the code. You'll notice that our two members of the loyalty program, Sonia and Siri, advance in front of John and Bob in the queue. That's because John and Bob were not part of the program. Using a priority queue and a comparitor is a much simpler approach for achieving the logic
>
> **[6:15](https://www.linkedin.com/learning/learning-java-collections/queue-implementations?u=76281980&t=375)** that we need to handle loyalty program members within our check-in list. I would definitely favor the priority queue over the list solution that we built. So that wraps up our look at the queue interface and a few of its implementations. The key to working with the queue is understanding FIFO order and the subtle differences in the behavior of its methods.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [John](../../Glossary/Tool/John%20the%20Ripper.md) (9), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** checkinqueue (1), programcomp (1), isloyaltyprogrammember (1)
> **Env Vars:** ide (1), fifo (1)
> **Cross-References:** we discussed (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Deque interface](https://www.linkedin.com/learning/learning-java-collections/deque-interface?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/deque-interface?u=76281980&t=0)** - [Instructor] The next collection interface will explore deque extends the queue interface and include some other ways that we can work with the linear collection of elements. The deque interface was introduced in [Java](../../Skills/Software%20Development/Java.md) six, adding a double ended queue to the collections framework. A deque allows elements to be added or removed from both ends of its linear structure. We'll refer to these as the front and the back. However, you might also hear them referred to as the front and rear or the head and tail. Operations on both ends of the deque are performed by using a set of methods with names that are derived from the methods on the queue interface. These methods make the deque very versatile. In the last lesson, we used an array deque as a queue with a first in, first out approach, but it can also handle elements using a last in first out approach that allows it to be used as a stack. In fact, Java has a legacy stack implementation in the Java.util package, but the Java docs recommend using an array deque instead. Let's move on and take a look at some of the interface methods on the deque. To add elements to the front of a deque, the methods add an offer from the queue interface are suffix with the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) first. The suffix first is used to indicate that the operations are performed on the front of the structure. If we attempt to add an element to a bounded deque that is full, these methods will behave the same way as their counterparts found on the queue. So add first will throw an exception,
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/deque-interface?u=76281980&t=93)** and offer first will return false. This naming pattern and the exception behavior is applied across many methods on the deque. For example, inserting elements from the back of the deque is achieved using the add last and offer last methods. So here the suffix last is applied to the method names from the queue interface to specify that operations are performed on the back of the deque. Removing elements from the front of the deque is performed with methods named remove first and pull first. So again, you see the pattern. We take the queue method name and add the suffix first or last depending upon what end of the deque we're working on. If you've picked up the pattern, you might've been able to guess that the names of the methods use to remove elements from the back of the deque are removed last and pull last. If we want to examine the front of the deque, we can use get first or peek first. Here, you'll notice the interface uses the verb get instead of element, which is what we find on the queue. Finally, if you want to examine the element in the back of the deque, we can use the get last or peek last methods. When you place these methods together, you can see the versatility provided by the deque, but that's not where it ends because the deque also has methods that allow it to function as a stack. A stack is another type of data structure that uses last in first out order or lifeboat. It works similar to a stack of dinner plates. We stack plates one on top of the other when storing them.
>
> **[3:09](https://www.linkedin.com/learning/learning-java-collections/deque-interface?u=76281980&t=189)** Then when we need to retrieve a plate, the last plate placed on the stack is the first one removed from it. That's lifeboat order and it's exactly how a stack works. To work with the deque as a stack, we use the methods push, pop, and peek. If we envision the stack as a vertical structure with a top and a bottom, these three operations are performed at its top. Using these operations, we can push an element into the stack for storage, peek at the top element or pop an element to remove it from the top. Now that we've seen the structure of a deque and learned its operations, let's switch over to our next lesson and see some code examples.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (4), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **CLI Commands:** make (1), find (1)
> **Analogies:** for example (1), similar to (1)
> **Cross-References:** in the last (1)
> **Speakers:** - [instructor] (1)

#### [Using an ArrayDeque as a stack](https://www.linkedin.com/learning/learning-java-collections/using-an-arraydeque-as-a-stack?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/using-an-arraydeque-as-a-stack?u=76281980&t=0)** - [Instructor] In [Java](../../Skills/Software%20Development/Java.md), the ArrayDeck is the recommended implementation for when we need a stack, which is one of the most frequent ways that we'll work with the deck. Let's work through a few examples to see how a deck works as a stack. For these examples, let's imagine the Landon Hotel has an older voicemail system that uses a last in first out approach to store messages. So messages will be stored in reverse of the order that they're received. And we'll just say there's no random access. This is similar to how your smartphone stores voicemails. More recent messages appear at the top and older messages at the bottom, except with a smartphone, you can play them in any order. So let's go ahead and we'll build our stack to work with these messages. To do that, I'm going to create a new deck that has a generic type of string. I'll name it messageStack, and I'll use an ArrayDeck implementation. Okay, so now we have our ArrayDeck or our stack. Let's go ahead and we can push messages onto the top of the stack. And I'll keep it simple. I'll just use Message 1. Then I'm going to push three more messages onto the top of the stack. After that, I'm going to use our print method to print out the contents of the stack so we can see what's going on. So I'll just call print and pass in our message stack. And then I'm going to invoke this method after each time we push an element
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/using-an-arraydeque-as-a-stack?u=76281980&t=94)** onto the top of the stack. Okay, let's go ahead, we'll execute this code and take a look at how the stack works. You'll notice that as we push each element onto the top of the stack, it's added at the first position. When we add another element onto the stack, it pushes down all the elements that were added before it and it takes the first position at the top of the stack. Okay, let's move on to our next example and I'm going to clean up this code some. So I'm going to pull each of the calls that push an element on the top of the stack together and then I'm going to remove two of our print statements, then I'm going to pop an element off of the top of the stack. So I'll go ahead invoke the pop method on the deck and then I'll pop another element as well and then I'm just going to take a peek at the next element. Okay, let's go ahead and execute this to see what happened. You'll notice that we push all of our elements into the stack. And then when we start to pop, the first element that we pop from the top of the stack is Message 4 and that's because it was the last message added. When we invoke pop again, Message 3 is at the top of our stack and that's what we retrieve. Now, I just peeked at Message 2 so you'll notice that when we print the contents of our stack, Message 2 remains at its top.
>
> **[3:10](https://www.linkedin.com/learning/learning-java-collections/using-an-arraydeque-as-a-stack?u=76281980&t=190)** So here you can see the last in first out order in action. The last message that we pushed onto the top of our stack is the first one that's removed and then we proceed in that order. Okay, let's walk through one final example to close out the lesson. I'm going to push an element on the top of the stack after we've popped the second element. So I'll name this Message 5 and then I'm also going to print the contents of the stack after we pop the second element. Okay, let's see how this behaves. So once again, we push all four elements onto the stack. Then we pop two of the elements. And now you'll notice we're only left with Message 2 and Message 1 on the stack and Message 2 is at the top. Now, when we push Message 5 onto the stack, you'll notice it takes the first position at the top. And then when we make this call to peek, it retrieves Message 5 from the top of the stack. So that's a look at one way that you'll most frequently work with the ArrayDeck, which is using it as a stack. There are some other situations where you might need to work with both ends of a double-ended queue, like maybe adding some undo functionality to an application. For most of us, those situations will be rare, so I'll leave it to you to test out some of the other methods on the deck interface.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Analogies:** imagine (1), similar to (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** messagestack (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [instructor] (1)


### 7. Map

[↑ Back to Table of Contents](#table-of-contents)

#### [Map interface](https://www.linkedin.com/learning/learning-java-collections/map-interface?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/map-interface?u=76281980&t=0)** - [Instructor] Maps are the last group of collection interfaces and implementations that we'll explore. Early in the course, we learned that a map works by using keys and values. A map is sort of like a dictionary. The [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) represents the key, and the definition is the value. If we want to find the definition of a word in a dictionary, we start our search by looking for the word, because that's the part we know. We use the word to find the part that we need to know, which is the definition. It's a similar concept in [Java](../../Skills/Software%20Development/Java.md). A map stores entries with an association between a key and a value. Just like a dictionary doesn't contain duplicate words, the same is true for a map, and its keys are unique. These characteristics of a map make it fast and efficient at finding values using the key. In the framework, the map interface is unique because it does not extend the collection interface. So we won't find some of the methods that we learned earlier in the course on the map interface. If we inspect the interface, we'll see it contains two generic type parameters, represented as K and V. The first is the type of the key, which can be any type of object. The key is used when looking up and storing values in the map. For the map to work correctly, the key's type must implement the hash code and equals methods. These methods are used by the map to distinguish one key from the other. The second generic type parameter denotes the type of the value stored within the map that is associated with the key.
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/map-interface?u=76281980&t=94)** When working with a map, there are three core methods that are essential to know. The first is the put method. It's used to create or update an entry in the map. It accepts a key and a value as its arguments. If the map does not have an entry for the provided key, the put method will create it, and then null will be returned from the method. But what happens if the key is already associated with a value? In that case, the put method overwrites the pre-existing value with the value provided as an argument to the method. Then the previous value associated with the key is returned by the method. Once you have entries stored in the map, they can be retrieved using the get method. This method accepts a key as an argument, which is used to search the map. If it finds an entry associated with the key, it returns the entry's value. If an entry is not found, the method returns null. To remove an entry, you can call the remove method and pass it a key of the entry you'd like to remove. If an associated entry is found in the map, it will be removed and then returned by the method. So those are some of the key operations you'll need to know on the maps interface. Now let's move on to our next lesson to see another way of working with the map.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (4), [Java](../../Skills/Software%20Development/Java.md) (1)
> **CLI Commands:** find (3), make (1)
> **Cross-References:** earlier in (1)
> **Analogies:** just like (1)
> **Best Practices:** the key is (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Collection views](https://www.linkedin.com/learning/learning-java-collections/collection-views?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/collection-views?u=76281980&t=0)** - [Instructor] Collection Views are another feature provided by the map interface that you'll commonly work with. They are used when you want to obtain parts of the map, like its entries, keys, or values as a collection. It's common to use a collection view when you need to iterate through a map's entries, keys, or values. So you can see in the code, we are getting the keys as a set using the keySet method. Currently, it just returns a set holding the key A. There is a small caveat to be aware of when working with the collection view. The collection view is backed by the map. So any changes to the map will be reflected in the collection view. You'll notice I've added a new entry to the map, and now the collection view contains A and B. So if your map entries change, you'll see the changes reflected in the collection view. If you're not aware of these mechanics, it can lead to some painful runtime issues. These map interface methods provide collection views for a map's entries, keys, and values. I'll draw your attention to the entrySet, which is frequently used to loop through a map's contents. The entrySet returns a set containing objects of type Entry. These provide access to the key and value of each association in the map. Entry is a generic type. It mimics the typing of the map backing the collection. So if a map uses a string for its key and stores integer values, you can expect those types to be returned from the getKey and getValue methods on the entry.
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/collection-views?u=76281980&t=94)** There's quite a bit to learn about working with the map. But they're heavily used in [Java](../../Skills/Software%20Development/Java.md). So we'll go even deeper as we start to explore map implementations.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** entryset (2), keyset (1), getkey (1), getvalue (1)
> **Definitions:** is a  (2)
> **Warnings:** caveat (1), be aware (1)
> **Speakers:** - [instructor] (1)

#### [Map implementations](https://www.linkedin.com/learning/learning-java-collections/map-implementations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/map-implementations?u=76281980&t=0)** - [Instructor] Let's explore the map implementations, so you'll know which one to choose for your situation. In this type hierarchy, we'll find the three most commonly used map implementations. The TreeMap is a map implementation that stores its entries in sorted order within a tree structure. It implements the NavigableMap interface, which extends from SortedMap. Entries in the map are sorted in ascending order based upon their key. This allows for rapid retrieval using methods and behaviors that are similar to what we saw in the tree set. We won't spend much time discussing the TreeMap because most of your code will deal with the HashMap implementation. A HashMap does not provide any guarantees about the order of its objects, but it has a sub-class LinkedHash set that can access elements in both insertion order or the order of their last access. Let's talk a little bit more about how hashing works because it's a key concept for storing objects in a map. Hashing is a technique that turns an object into a numeric value known as its hash value. To hash an object we run it through a hashing function. So, we might take a string like [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) and run it through a hash function to get its hash value. In [Java](../../Skills/Software%20Development/Java.md), the hashCode method is used to produce an object's hash value. Here, I've included the hashCode logic that is used to produce the hash value for a UTF string. Now, it's not important to understand the logic other than realizing it's computing
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/map-implementations?u=76281980&t=93)** a hash value based on the string's value. Most basic types in Java like the string or the wrapper classes for primitives implement the hashCode method. However, if you're using your own custom object type, you'll need to provide an implementation of this method. When we explored equality, you saw how to generate a hashCode and equals method using the eclipse IDE. For most situations, that implementation will be sufficient. If you need to provide your own implementation for hashCode, a few rules apply. First, the hashCode method must consistently produce the same integer, if it is invoked multiple times. The hash value can change, if values used in the object's equals method change. But, in general, you want it to be pretty stable. The second rule states that if two objects are equal, they must produce the same hash value. And the final rule states that two objects that are not equal are permitted to have the same hash value, this is important. So, you'll notice the two strings in our examples produce the same hash value, this is known as a collision. It may seem alarming, however, there are strategies for handling a collision. If you violate these rules, when implementing hashCode and equals the HashMap may have some unexpected behaviors when handling your objects. You'll understand why when we dive deeper into the inner workings of a HashMap in our next lesson.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Code Identifiers:** hashcode (7)
> **Definitions:** is a  (2), known as (2)
> **Env Vars:** utf (1), ide (1)
> **CLI Commands:** find (1)
> **Analogies:** similar to (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [How a HashMap works](https://www.linkedin.com/learning/learning-java-collections/how-a-hashmap-works?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/how-a-hashmap-works?u=76281980&t=0)** - [Instructor] Let's talk in general about how a HashMap uses the hashCode and equals methods when storing a key value pair. Internally, a HashMap contains an array with a default size of 16 that is used to store nodes. In this diagram, I've reduced this down to three indexes to make it easier to view. When the put method accepts a key value pair, the HashMap calls the hashCode method on the key to get its hash value. It then applies logic that hashes the value a second time to build a new hash. This is used to compute the index where the values will be stored in the array. In this index, a new node is created that contains the key, value and derived hash value for the objects placed into the map. This process continues each time the put method is called with a new key value pair. If we have a collision of hash values and objects must be placed in the same index that's okay. The new node is linked together with the existing nodes to build a link list within the array index. So, that is the general logic of how storage works internally within a HashMap. And, admittedly, we skipped a few minor points. Now, let's talk about retrieving a value from a HashMap. When a key is passed to the get method on a HashMap, the map first performs the hashing and index logic to determine the appropriate array index to search for the key's value. So, here, we see the provided key computes to an index of 1, that index currently holds
>
> **[1:35](https://www.linkedin.com/learning/learning-java-collections/how-a-hashmap-works?u=76281980&t=95)** a link list with two nodes. If there were only a single node stored in the index, the search logic could easily return the value from that single node. But since there are multiple nodes stored within the index, the HashMap uses the equals method to compare the provided key with the key stored in the node. In this case, the first node didn't match, so the logic traverses the reference to the next node in the link list. Then, it performs the equals comparison again. This time it evaluates to true causing the value in the node to be returned. So basically the equals method resolves the hashCode collision, and you start to see why implementing the hashCode and equals methods is necessary for working with a map. The internal workings of a HashMap is sometimes assessed in technical interviews so now, you'll be prepared for the question, if it arises, and you'll have a better grasp on how hashing structures function.

> [!info]- Semantic Content
>
> **CLI Commands:** node (8), make (1)
> **Code Identifiers:** hashcode (4)
> **Definitions:** is called (1)
> **Speakers:** - [instructor] (1)

#### [Map methods](https://www.linkedin.com/learning/learning-java-collections/map-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/map-methods?u=76281980&t=0)** - [Instructor] Let's work through a few code examples to get more familiar with the methods on a map. To help us with that, we'll build a HashMap that tracks which room the guests at the London Hotel are staying in. So we'll start out by creating a map that uses a room as its key and stores guests as its values. I'm going to name this map assignments and then I'll use a HashMap implementation for my map. Okay, so now we have the map. Let's go ahead and add the guests into their rooms. So first I'm going to place Maria in Oxford by passing in Oxford as the key and then Maria as the value. Okay, then we can put [John](../../Glossary/Tool/John%20the%20Ripper.md) in his room. It's going to be Piccadilly, so we'll go ahead and use the put method to associate John with Piccadilly. Now at this point, let's retrieve some of the values in the map and print them to the console. So I'll use the print stream and then I'll just say we're getting the guest from Piccadilly. And here we can use the map to retrieve the guest in that room by passing in the key to the get method. So we'll pass in Piccadilly and that should get us John. Now let's do this again for Oxford, but I'm going to do this a little bit differently. Instead of using the exact object instance as my key that I'm passing into the get method, I'm going to create a new logically-equivalent instance of Oxford. So I'll use the new operator in the constructor,
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/map-methods?u=76281980&t=93)** pass in the name Oxford. It's a suite that holds five people and costs $225 a night. Okay, so we're retrieving two values from our map using rooms as the key. In one instance we're using the exact same object instance and in another we're using a logically-equivalent object instance. Let's go ahead and execute this and you'll notice that our console output indicates that Maria is staying in Oxford and John is staying in Piccadilly, so that's correct. Now let's take a look at what happens when you don't override hashcode and equals properly. So in this example, it's the exact same code except for one thing. I'm using a different object as the key of the map and this object does not override the hashcode and equals methods properly. Watch what happens when I execute this code. You'll notice that we're unable to retrieve the value in the map associated with Oxford and that's because we've broken how the map works by not overriding hashcode and equals. Okay, let's finish up with one more example. Imagine that Maria and John would like to switch rooms. Let's see how we can do that. So I'm going to create a new variable guest and then I'm going to use our map and the put method. So first I'm going to put Maria into John's room, Piccadilly.
>
> **[3:07](https://www.linkedin.com/learning/learning-java-collections/map-methods?u=76281980&t=187)** Now I'm not just going to pass Maria in as the value that we would like to associate with Piccadilly. What I'm going to do is use the remove method and then I'll pass in Maria's room as the key which is Oxford. Okay, let's talk a little bit about what this does. First, we're going to remove Maria from her room and you'll recall that the remove method will return the value with the associated key we pass into the method. So this is going to place Maria into Piccadilly. Now you'll also recall that the put method will return the value previously stored within the map entry, so that's going to assign John into our guest variable. Now let's go ahead and we can place John into Maria's room. Now to do that, I'm going to use the putIfAbsent method just to make sure that Maria's out of her room. You'll recall that this method will not overwrite the entry in the map if it already exists. Okay, now to build the association, we pass in Maria's room which was Oxford. And then as our second argument, we pass in John who's assigned to the guest variable. All right, let me go ahead. We'll save our code and we'll execute it. And there you can see that John is now staying in Oxford and Maria's staying in Piccadilly. We successfully completed our switch. So that's a look at several of the methods
>
> **[4:40](https://www.linkedin.com/learning/learning-java-collections/map-methods?u=76281980&t=280)** that insert, update, remove, and retrieve objects from a map.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [John](../../Glossary/Tool/John%20the%20Ripper.md) (10)
> **CLI Commands:** make (1)
> **Code Identifiers:** putifabsent (1)
> **Analogies:** imagine (1)
> **Speakers:** - [instructor] (1)

#### [Iterating Maps](https://www.linkedin.com/learning/learning-java-collections/iterating-maps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/iterating-maps?u=76281980&t=0)** - [Instructor] Traversing each entry in a map can be challenging when first learning about collections. So let me show you how it works. To do that, I'm going to start out by invoking the entrySet method on our map, and I'm actually going to assign this to a variable so you can see the typing of the object returned. Now, as its name implies, the entrySet is a set that contains entries. Now, the entry itself is a generic type. So first, we need to specify the type of the key used for our map and then the type of the values used for our map. All right, so now we have the type of our variable and I'm just going to name it CV for collection view. Now, let's talk about this for one minute. You'll notice that the returned object is a set, so that means it extends the collection interface and it also extends the iterable interface. So we can use this set within the enhanced for-each loop and that's typically how the entrySet is worked with. Let's check that out. So I'm going to go ahead and declare a for loop, and then I'm actually going to copy the type information for our entry because that's what we're going to be iterating through is the entries within that set. So I'll go ahead and place that on the left side of the colon. And then on the right side, we need to invoke the entrySet method again in order to get our entrySet
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/iterating-maps?u=76281980&t=94)** that we're going to iterate through. And then also we need to provide a name for the object that's going to be assigned each entry. Okay, so now inside the body of our loop, we can work with the entry. So let's go ahead and declare a variable of type room and then I'm going to get the key from the entry. Then we can get the guest as well using the getValue method. So now we have the room and the guest from the entry stored into variables. Let's go ahead and we can print these out to the console just to see them. So I'm going to go ahead and specify a pattern to the format method that prints the two strings and a new line character. Then I just need to get the name of the room and pass it in as the first argument to the pattern and then I'll get the guest's first name. Okay, so we can go ahead and execute this code and you'll see that we iterate through the entries in our map. Before we wrap up, there's one more point I'd like to make. The entrySet is a collection. So if you would like, you could also use a stream to iterate through the entries in the entrySet. So that's where we'll end our discussion and exploration of the map.

> [!info]- Semantic Content
>
> **Code Identifiers:** entryset (7), getvalue (1)
> **Definitions:** is a  (4)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Working with Maps](https://www.linkedin.com/learning/learning-java-collections/challenge-working-with-maps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/challenge-working-with-maps?u=76281980&t=0)** - [Instructor] Okay, I hope you're up for the final challenge. In this challenge, the Landon Hotel has created another class named BookingService that manages the booking of guests into rooms in the hotel. They've started work on the class by adding a hash map named bookings. This map will store each room and its associated guests, but they need your help in completing a few methods. The first is the book method. It's provided with a room and a guest that would like to stay in the room. The method will place the guest in the room only if another guest is not staying within it. The return value of the method is a Boolean that indicates if placing the guest in the room was successful. The next method totalRevenue is used to obtain the current amount of revenue using a calculation that sums the rates of all booked rooms. You want to make sure that you complete these methods in the order they're specified. And like other challenges, unit tests have been provided to validate your work once you've finished. So go ahead, pause the video and work on your solution. When you're ready, you can resume the video and I'll walk you through mine. Welcome back. Did you enjoy the challenge? I'm going to dive right in to my solution. For the first method book, we needed to create an association for the provided key and the provided guest within our map, but only if the room was not associated
>
> **[1:34](https://www.linkedin.com/learning/learning-java-collections/challenge-working-with-maps?u=76281980&t=94)** with a guest in the map already. So this was a great spot to use the map's putIfAbsent method. So on our bookings map, I went ahead and invoked it, passing in the room and the guest as arguments. This method will not create an association in the map if one already exists for the key that we provide. So if our room is already associated with a guest in the map, it won't create that association. In that case, it's just going to return the current value associated with the key. Now, if it does create a new association, it returns null. So I can just evaluate the return value of the method against null, which is going to produce a Boolean that I can return from the method to indicate whether or not we booked a guest in a room. So that completed my work for the book method. And I moved on to work on the totalRevenue method. This method required us to get the rate of each room in the booking map and total them in order to determine how much revenue the hotel is making. So to do this, I invoked the keySet method on the map to get a collection view that contained all of the keys within the map. So now I have a collection of the map's keys. And you'll recall that the map's key type is room, so it's a collection of rooms. I took that collection and I streamed it.
>
> **[3:07](https://www.linkedin.com/learning/learning-java-collections/challenge-working-with-maps?u=76281980&t=187)** On the stream, I was able to invoke the mapToDouble method. And then here. I could just pass in a method reference to the room's getRate method. This would produce a double stream and on that double stream, I could invoke the sum method to perform a calculation that totaled all of the rooms' rates and that got me the total revenue. Okay, let's go ahead and we'll test out that solution. So in the unit test, I'm just going to right-click go to run as and [JUnit](../../Skills/Software%20Development/JUnit.md) test. Excellent. It passed. So I hope you enjoyed this challenge working with the map and got to better know some of its methods.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JUnit](../../Skills/Software%20Development/JUnit.md) (1)
> **Code Identifiers:** totalrevenue (2), putifabsent (1), keyset (1), maptodouble (1), getrate (1)
> **UI Navigation:** right-click (1), go to (1)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 8. Collections Utilities

[↑ Back to Table of Contents](#table-of-contents)

#### [Ordering elements](https://www.linkedin.com/learning/learning-java-collections/ordering-elements?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/ordering-elements?u=76281980&t=0)** - [Instructor] When working with collections, the order of elements comes into play quite often, but order isn't always as straightforward as you might think. For example, if I ask you to order these coins, you would likely place them in order by their monetary value as we see in the picture. Things like currency, letters and numbers have a default way they're compared like monetary value, alphabetical order or numeric value. When we perform these default comparisons on a group of objects, it places them into an order known as their natural order. However, there's cases where the natural order isn't what's required. A coin collector might order the coins by their size for storage, or maybe by the year that they were minted. When writing code that must order objects, these same types of considerations appear. The concept of natural order also applies to most common [Java](../../Skills/Software%20Development/Java.md) data types. Dates are naturally ordered chronologically, numbers like int and long use their numeric value and strings are ordered alphabetically. So you might wonder how the natural order of these data types is determined, or how would we specify the natural order for a custom class that we built. In Java, the comparable interface is used to specify the natural order of a data type. Classes that implement the interface define a compare to method that accepts another object as an argument. This argument is evaluated against the current instance.
>
> **[1:36](https://www.linkedin.com/learning/learning-java-collections/ordering-elements?u=76281980&t=96)** The comparison logic built into the type can be used by other code, like a collection or a sort method to determine the natural order for a group of objects with that type. On the interface, you'll notice the compare to method returns an integer that has a special meaning. It indicates the result of the compare to method after the current instance is evaluated against the instance provided to the method as an argument. Let's call the current instance this, and we'll call the object that it's compared against that. When we compare this to that, the result will be one of three outcomes that we're going to demonstrate using some pseudocode. If the current instance or this is greater than the provided instance or that, the compare to method returns any positive number. When the method is used for sorting, it would cause this to be placed after that in the order. If this is less than that, the method returns any negative number. This outcome would cause this to appear before that in the sort order. And finally, if this is equal to that, the method returns zero. Using these values returned by the comparable allows objects of a type to be placed into their natural order. But sometimes we want to use something other than the natural order. For these situations, we can use a comparator. A comparator is another interface that is implemented by a class that compares two objects.
>
> **[3:11](https://www.linkedin.com/learning/learning-java-collections/ordering-elements?u=76281980&t=191)** Unlike the comparable, which performs the comparison inside of the type that you're comparing, this comparison occurs externally in another object. To do that, two objects are passed as arguments into the compare method. This method performs the comparison of the two objects and returns an integer that follows logic similar to the comparable. In Java eight, the comparable interface was updated to include many new static and default methods that can be used for chaining comparators. One trick you can use to remember how a comparator works is to think of it like a balancing scale with two objects. Like the scale, the comparator needs two objects to perform its comparison. Now that we better understand order, let's take a look at how collections are sorted in our next lesson.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3)
> **Analogies:** for example (1), picture (1), compare this to (1), similar to (1), think of it like (1)
> **Definitions:** known as (1)
> **Speakers:** - [instructor] (1)

#### [Sorting collections](https://www.linkedin.com/learning/learning-java-collections/sorting-collections?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/sorting-collections?u=76281980&t=0)** - [Instructor] Let's take a closer look at sorting collections by walking through a few examples that use a natural sort order. We'll start by giving our room class a natural order. The order will sort the rooms in alphabetical order first by their name, and then by their type. To specify a natural order, a type must implement the comparable interface. So let's go ahead and honor room class. We'll implement the interface. The interface accepts a generic type argument. In this case, I'm going to specify that as room, this will allow an instance of a room to be compared against another instance of a room. Now you'll notice the compiler immediately complaints, and that's because we haven't satisfied the comparable interface. So just hover over the room class and then click on add unimplemented methods. And then you'll notice that within your room class, you'll get a CompareTo method. Now here we'll need to provide the logic that performs the comparison between the two rooms. So to start out, I'm going to declare a new integer named result. Our result variable will be assigned the return value of comparing the names of the rooms. So I'm going to get the name of the current room, and then I'm going to invoke it's CompareTo method, which accepts another string. So here you can see I'm leveraging the comparable implementation of the string object. I'm delegating the comparison to another type. Now here, we just need to provide the name
>
> **[1:36](https://www.linkedin.com/learning/learning-java-collections/sorting-collections?u=76281980&t=96)** of the other room that we're comparing against. This is going to result in an integer that's either negative, positive, or zero. Now, if it's anything besides zero, it means the comparison was resolved with our primary sort. So in that case, we can check to see if the result is not equal to zero, and then we can just return the result in this ternary expression. Now, if the result is zero, we can resolve the conflict using a secondary sort. So to do that, we're going to do something similar by getting the type of the room, and then we're going to invoke it's CompareTo method, and then we're going to pass in the type of the other room and this should break the tie. Okay, so we have our CompareTo implementation. Now let's head over to our application class. In this class, I've created a list of rooms and in the list are five rooms. Now one thing I'll point out is that two of the rooms have the same name, but different types. So here you see Oxford as a guest room and Oxford as a suite. Now let's first start out by just executing this code and you can see that the rooms are placed in their insertion order. Now let's go ahead and change that by sorting them using their natural order. To do this, we can use the collections class provided
>
> **[3:14](https://www.linkedin.com/learning/learning-java-collections/sorting-collections?u=76281980&t=194)** by the collections framework. Now don't get this confused with the actual collection interface. It's different. It provides static methods that we can use to work with a collection like a list. So I'm going to go ahead and invoke the sort method and pass in our list of rooms. Now watch what happens. You'll notice that the rooms are placed in alphabetical order. And then when we have a room that has the same name, we order by the type. So that's our natural sort order in action. Now there is an overloaded form of this method. So let's take a look at that. The overloaded form accepts our list and then a comparitor. So I can go ahead and pass in our rooms. And then I can use a comparitor that specifies the natural order. And you'll see that we get the same result. Now in more recent versions of [Java](../../Skills/Software%20Development/Java.md), a sort method was added to the list. So we can do something like this. We can invoke the sort method on our room's list, and then we no longer need to pass in the collection. All we need to provide is a comparitor. And when we do this, we'll get the same results as last time. This is the preferred method for sorting a list since it's been introduced. When specifying the order of elements, be sure to leverage the language features that are provided to you.
>
> **[4:46](https://www.linkedin.com/learning/learning-java-collections/sorting-collections?u=76281980&t=286)** Using the existing comparable implementations and the comparitor static methods will help you write higher quality code that avoids logic errors that can be introduced by custom comparison logic.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Definitions:** is an  (1), is a  (1)
> **UI Navigation:** click on (1)
> **Speakers:** - [instructor] (1)

#### [Comparators](https://www.linkedin.com/learning/learning-java-collections/comparators?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/comparators?u=76281980&t=0)** - [Instructor] If we would like to sort a collection using something besides its element's natural order, we can define our own comparator. So, let's imagine that we wanted to compare our rooms using their daily rate. To do this, we can build a comparator that overrides the natural order of the room. This comparison might be something that we use frequently, so I'm going to create the comparator as a static field inside of the room class. So I'll declare the field with a type of comparator, and then you'll recall that comparators are a generic type, so I'll specify room as its generic type. I'm going to name this the RATE_COMPARATOR, and then we can begin to create our comparator. To do that, I'm going to use the static methods found on the comparator that were added in [Java](../../Skills/Software%20Development/Java.md) 8. The first is the comparing method. This allows us to provide a field on the room that we would like to use for our comparison. So, in this case, I'm going to pass a method reference to the getRate field, so the rate will be used as the sort key. Okay, let's go ahead and we'll save this. And then we can navigate over to our application. Now, you'll notice that we have a list of rooms currently in this application. And if we execute the code, they're being sorted in their natural order. So, Cambridge, Oxford, then the other Oxford room, Piccadilly, and Victoria.
>
> **[1:35](https://www.linkedin.com/learning/learning-java-collections/comparators?u=76281980&t=95)** Now, watch what happens when we shift this code over to use the new comparator that we built. So I'll pass the comparator into the sort method on the list. And then I'll go ahead and execute it. Now, we're sorting in ascending order by the room's daily rate. Now, within this output, you'll notice that we have three rooms that have the same rate. Let's add a secondary sort to our comparator. So I'm going to return to our room class, and then within the class, we can chain a call to the thenComparing method and add another method reference to our secondary sort key. So, I'm just going to use the room name. Let me go ahead and provide that method reference. Then we can return to our application, and we can execute our code. Now you'll notice the rooms are sorted, first by their rate and then by their name. So Victoria is last, but then we still have this conflict with Oxford. So we can head back over to the room class, and chain another call to build a third sort key into our comparator. So I'll just invoke thenComparing once again, and this time I'm going to use the type to break the tie in the sort. Now, back in the application class, before I execute this code, I'm going to invoke another method that's found on the comparator. And that's the reversed method,
>
> **[3:08](https://www.linkedin.com/learning/learning-java-collections/comparators?u=76281980&t=188)** and this just reverses the order in which we sort the elements. So in this case, we're going to get the rates in descending order, and then we will get the name in reverse alphabetical order, followed by the type in reverse alphabetical order. Okay, let's go ahead and check that out. And you notice that we're now ordering in a descending order by the rate. Then we see the names in descending order, and then the types in descending order. In the past two lessons, you may have noticed a pattern. The collections framework provides us with methods for sorting, but they're all designed to rely upon logic that is external to the framework. So when you define that logic, make sure it's reusable, through either a comparable implementation for natural sort order, or a comparator that is used globally throughout your code. Then, you won't repeat yourself all throughout your code base.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** thencomparing (2), getrate (1)
> **CLI Commands:** make (1)
> **Env Vars:** rate_comparator (1)
> **Versions:** java 8 (1)
> **Analogies:** imagine (1)
> **Speakers:** - [instructor] (1)

#### [Finding elements in a collection](https://www.linkedin.com/learning/learning-java-collections/finding-elements-in-a-collection?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/finding-elements-in-a-collection?u=76281980&t=0)** - [Instructor] The collections class contains many static methods that help us work with the collection. In this lesson, we're going to explore a few of them that allow us to find values within a list. So let's start out with the classic binary search. When we use a binary search method in the collections framework, we need our collection to be sorted. So you'll notice that I have a list of rooms and then I've used our rate comparator to sort them. So let's go ahead and on the collections class, we can invoke the binary search method. And you'll notice that there's two of them. The first, we'll use the natural sort order of the collection in order to perform the search. The other one will accept a comparator. Now because we're not using the natural sort order of a room for our collection, we'll need to go with this form of the method. So the first argument's the collection that you would like to search. The second argument is the object that you would like to search for. In this case, I'm just going to look for Cambridge. And then the final argument here is your comparator. So I can pass in our rate comparator. Now this method is going to return an integer that denotes the index where that object is found. So I'm going to go ahead and assign that to a variable called result, and then I'm going to just print it to the console. Okay, so let's go ahead and we can perform our search. So you'll notice that the binary search
>
> **[1:35](https://www.linkedin.com/learning/learning-java-collections/finding-elements-in-a-collection?u=76281980&t=95)** indicated that Cambridge is in index one. And if we take a look at our list, you'll notice that in index one we find Cambridge. So that was a successful binary search. Let's see what happens when a search isn't successful. So I'm going to just copy one of our rooms and then I'm going to paste it in at line 20. And I'm going to build a new room that isn't in our collection. I'll call it New London and then I'll just change the name of the variable. Okay, now I'm going to search for New London inside of our collection with the binary search. Let's see what happens here. So you'll notice that the result is negative. That indicates that the object was not found within the collection. And then it's providing an integer of three. So this is known as the insertion point. It indicates the position in the list where New London would be inserted. And that would be here in position three, where we currently find Oxford. So let's go ahead and we can insert Oxford into our rooms list. To do that, I'm going to use the overloaded add method that accepts a position. And then I'm going to get abstract value of the result. And I'll also need to increment it by one. You'll notice when I do this, we can then insert New London into its proper position
>
> **[3:10](https://www.linkedin.com/learning/learning-java-collections/finding-elements-in-a-collection?u=76281980&t=190)** within the collection. Let's close out the lesson by looking at two more methods on the collections class. These methods are going to help us find the min and the max values within our rooms list. So to do that, we just invoke the methods on the collections class. The first one is min and we pass in our collection and then we pass in a comparator. So I'll pass in the room comparator, and then I'm just going to get the name of the room once we found it. So this is going to get the min value from the collection as determined by our rate comparator. And then to get the max, it's very similar. I'll just change our console output here. And then we'll change our method call to get the max. Okay, let's go ahead. We'll execute the code and see what we got. Okay, so you'll notice that our min room was identified as Piccadilly and it does have the lowest rate of the group. So that's correct according to our rate comparator. And then Victoria was identified as the max, and that's a little bit more difficult because we do have these three rooms that have a rate of $225. But you'll remember that our room comparators secondary sort key was the name. And because Victoria is deemed to be greater
>
> **[4:44](https://www.linkedin.com/learning/learning-java-collections/finding-elements-in-a-collection?u=76281980&t=284)** by that sort key, it appears as the greatest element or the max element in our collection. So that wraps up our review of the collections class and other utilities that we can use to work with collections in the framework.

> [!info]- Semantic Content
>
> **CLI Commands:** find (4)
> **Definitions:** known as (1)
> **Speakers:** - [instructor] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Course wrap-up](https://www.linkedin.com/learning/learning-java-collections/course-wrap-up?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-java-collections/course-wrap-up?u=76281980&t=0)** - [Kevin] Thanks for watching this course on the [Java](../../Skills/Software%20Development/Java.md) collections framework. Before we wrap up, I want to provide you with some ideas on where to go next and give you some information about my other courses. Now that you're familiar with the basic collections, I would recommend looking at the framework's concurrent implementations that handle multi-threaded scenarios. You'll need to be aware of Java concurrency to get the most from these collections. So make sure you're comfortable working with programs that handle more than a single thread. In this course, we've also touched on Java streams, which pair really nicely with collections. If you dive deeper into streams, you'll discover more operations to add to your Java toolbox. Finally, we also highlighted generics in the course. Generics are another section of the language you can explore that will help you better work with collections. If you like my teaching style, I have a few other courses on the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning platform that might interest you. I can teach you about other topics like GitOps, [Microservices](../../Skills/Software%20Development/Microservices.md), APIs, Java, Spring, or [Git](../../Skills/Software%20Development/Git.md) if you're interested. If you want to, tag me when you post your course certificate. I always like to interact with learners taking my courses and celebrate their success. I'm also very active on LinkedIn, so follow me or connect to be notified when I create a new course or share some technical writing. So that's it. For now, congratulations on finishing this course and thanks again for taking it. Now it's time to put your new skills
>
> **[1:33](https://www.linkedin.com/learning/learning-java-collections/course-wrap-up?u=76281980&t=93)** with Java collections into action.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (6), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2), [Microservices](../../Skills/Software%20Development/Microservices.md) (1), [Git](../../Skills/Software%20Development/Git.md) (1)
> **CLI Commands:** make (1), git (1)
> **Warnings:** be aware (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [kevin] (1)


## Instructor

- [Kevin Bowersox](../../Instructors/Software%20Development/Kevin%20Bowersox.md)

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/java-collections-2421704)

## Skills Covered

- Java Frameworks
- Java

## Path Context

### In [Getting Started as a Java Developer](../../Paths/Data%20Science/Learning%20Paths/Getting%20Started%20as%20a%20Java%20Developer.md)
← [Learning JDBC](Learning%20JDBC.md) | **5 of 10** | [Java Memory Management- Values and References](Java%20Memory%20Management-%20Values%20and%20References.md) →

## Appears In

- [Getting Started as a Java Developer](../../Paths/Data%20Science/Learning%20Paths/Getting%20Started%20as%20a%20Java%20Developer.md)

## Related Courses

_Courses sharing skills:_

- [Practice It- Java](../Software%20Development/Practice%20It-%20Java.md) — Java
- [Hands-On Introduction- Java](../Software%20Development/Hands-On%20Introduction-%20Java.md) — Java
- [Learning Java 17](../Software%20Development/Learning%20Java%2017.md) — Java
- [Creating a Book Search Engine from Scratch Using Java and GitHub Copilot](../Artificial%20Intelligence%20(AI)/Creating%20a%20Book%20Search%20Engine%20from%20Scratch%20Using%20Java%20and%20GitHub%20Copilot.md) — Java
- [Advanced Java- Hands-on with Streams, Lambda Expressions, Collections, Generics and More](Advanced%20Java-%20Hands-on%20with%20Streams%2C%20Lambda%20Expressions%2C%20Collections%2C%20Generics%20and%20More.md) — Java

---

[↑ Back to top](#)