---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: database-foundations-application-development
url: "https://www.linkedin.com/learning/database-foundations-application-development"
duration_minutes: 114
duration: 1h 54m
level: Intermediate
updated: 2/21/2025
learners: 65707
skills:
  - Database Development
  - Database Applications
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C4E0DAQF2QDZxqGhZCg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1620230928794?e=2147483647&amp;v=beta&amp;t=w6xKHYfvRQFpzbyQMaRs7mSEDT6-dPAVhwSomXqW_1o"
linkedin_topic: Data Science
learning_paths:
  - '[Database Foundations- From Concepts to Applications](../../Paths/Data%20Science/Learning%20Paths/Database%20Foundations-%20From%20Concepts%20to%20Applications.md)'
  - '[Advance Your Database Administration Skills](../../Paths/Data%20Science/Learning%20Paths/Advance%20Your%20Database%20Administration%20Skills.md)'
prev_courses:
  - '[Database Foundations- Administration](Database%20Foundations-%20Administration.md)'
  - '[Database Foundations- Administration](Database%20Foundations-%20Administration.md)'
next_courses:
  - null
  - '[SQL Server Fundamentals- Master Basic Query Techniques](SQL%20Server%20Fundamentals-%20Master%20Basic%20Query%20Techniques.md)'
path_nav: '[{"path":"Database Foundations- From Concepts to Applications","position":5,"total":5,"prev":"Database Foundations- Administration","next":null},{"path":"Advance Your Database Administration Skills","position":5,"total":11,"prev":"Database Foundations- Administration","next":"SQL Server Fundamentals- Master Basic Query Techniques"}]'
path_count: 2
tags:
  - course
  - topic/data-science
  - topic/database-management
  - topic/software-development
  - skill/database-development
  - skill/database-applications
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Data%20Science/Database%20Foundations-%20Application%20Development.md)

![Database Foundations: Application Development](https://media.licdn.com/dms/image/v2/C4E0DAQF2QDZxqGhZCg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1620230928794?e=2147483647&amp;v=beta&amp;t=w6xKHYfvRQFpzbyQMaRs7mSEDT6-dPAVhwSomXqW_1o)

# Database Foundations: Application Development

> While learning about the individual components of a software development stack like databases, programming languages, and interfaces is important, combining multiple technologies that each contribute a piece to the whole is where all the knowledge really comes together. In this fifth and final course of his Database Foundations series, database expert Adam Wilbert explores the role of relational d

> [LinkedIn Learning](https://www.linkedin.com/learning/database-foundations-application-development) | 1h 54m | Intermediate | 66K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Integrating databases with an application](#integrating-databases-with-an-application)
  - [What you should know](#what-you-should-know)
  - [The exercise files](#the-exercise-files)
- [**1. Application Layers**](#1-application-layers) (4 videos)
  - [Multitier architectures](#multitier-architectures)
  - [Communicate with the data layer](#communicate-with-the-data-layer)
  - [Databases on the web](#databases-on-the-web)
  - [Project overview](#project-overview)
- [**2. PostgreSQL and PHP**](#2-postgresql-and-php) (10 videos)
  - [What is PHP and Apache?](#what-is-php-and-apache)
  - [Create a PHP web server](#create-a-php-web-server)
  - [Connect to the PHP server](#connect-to-the-php-server)
  - [PostgreSQL functions](#postgresql-functions)
  - [Use PHP variables](#use-php-variables)
  - [Use if/else to handle errors](#use-ifelse-to-handle-errors)
  - [Add data to a database](#add-data-to-a-database)
  - [Format SELECT query results](#format-select-query-results)
  - [Filter results with form controls](#filter-results-with-form-controls)
  - [Add data to a table with form controls](#add-data-to-a-table-with-form-controls)
- [**3. SQL Server and ASP.NET Core**](#3-sql-server-and-aspnet-core) (7 videos)
  - [What is ASP.NET Core?](#what-is-aspnet-core)
  - [Create the ASP.NET Core web server with Docker](#create-the-aspnet-core-web-server-with-docker)
  - [Add a database to SQL Server](#add-a-database-to-sql-server)
  - [ASP.NET modularity](#aspnet-modularity)
  - [Model tabular data](#model-tabular-data)
  - [Controller classes](#controller-classes)
  - [URL segments](#url-segments)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Integrating databases with an application](https://www.linkedin.com/learning/database-foundations-application-development/integrating-databases-with-an-application?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/integrating-databases-with-an-application?u=76281980&t=0)** - [Adam] Many technology courses focus on individual components of a [Software Development](../../Topics/Software%20Development.md) stack database, programming language, interface design. The combination of multiple technologies, each contributing a piece to the whole application, is where everything comes together. Hi, I'm Adam Wilbert, a database consultant and trainer with over a decade of experience introducing people to the world of [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md). in this fifth and final course in my [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning Database Foundations series, we're going to take what you've learned about [Databases](../../Skills/Software%20Development/Databases.md) and explore how these building blocks fit together with other technologies. We'll take two different approaches to connecting a database server with a web server and see the components that allow you to build custom interfaces for your data that are accessible from any browser. There's a lot to cover and I'm glad that you're joining me on this journey.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Software Development](../../Topics/Software%20Development.md) (1), [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **Speakers:** - [adam] (1)

#### [What you should know](https://www.linkedin.com/learning/database-foundations-application-development/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/what-you-should-know?u=76281980&t=0)** - [Instructor] In this course, we're going to take a look at a couple of examples of how you can integrate a database server with a web server in order to provide end users a way to interact with a database over the internet. To do this, we're going to add in some new technologies to what we've already learned about [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md) over the previous courses in this series. Namely, we're going to be working with [HTML](../../Skills/Web%20Development/HTML.md), CSS, [PHP](../../Skills/Software%20Development/PHP.md), and [ASP.NET](https://ASP.NET). Now I don't expect you to have any prior experience with any of these languages. We're not going to be writing any code. Instead, we're going to take a tour and read through some working examples to see how all the pieces come together to make your [Databases](../../Skills/Software%20Development/Databases.md) accessible online. To follow along on your own computer, you're going to need to have Docker installed. This will allow us to add [Web Servers](../../Skills/Web%20Development/Web%20Servers.md) to the database servers that we've been working with. You can download Docker for [Windows](../../Glossary/Service/Windows.md), Mac OS or [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) at [docker.com/products/dockerdesktop](https://docker.com/products/dockerdesktop). You'll also need a text editor. You can either use Notepad or Text Edit or any other plain text editor that you prefer. Personally, I'm going to be using Visual Studio Code. I like this text editor because it shares some common design attributes with [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Data Studio that we've already been working with for our [Database Development](../../Skills/Software%20Development/Database%20Development.md). If you'd like, you can download your own copy from code.[visualstudio.com](https://visualstudio.com).

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1), [PHP](../../Skills/Software%20Development/PHP.md) (1), [ASP.NET](../../Skills/Web%20Development/ASP.NET.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **CLI Commands:** docker (3), php (1), make (1)
> **Env Vars:** html (1), css (1), php (1), asp (1), net (1)
> **URLs:** [asp.net](https://asp.net) (1), [docker.com](https://docker.com) (1), [visualstudio.com](https://visualstudio.com) (1)
> **Tools:** visual studio (1)
> **Speakers:** - [instructor] (1)

#### [The exercise files](https://www.linkedin.com/learning/database-foundations-application-development/the-exercise-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/the-exercise-files?u=76281980&t=0)** - [Instructor] The hands-on portion of this course consists of two separate projects. If you want to follow along you can download the necessary files from the course overview page. I've gone ahead and unzipped the files here onto my desktop. Now, inside this folder you're going to find everything that you need for the chapter two project on [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md), and the chapter three project that focuses on [SQL Server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md). Now, at this point, there is nothing more that you need to prepare ahead of time. There's no [Databases](../../Skills/Software%20Development/Databases.md) to install or anything like that. So just download the files, unzip them into a location that's convenient for you, and we'll dive deeper into each of these folders when the appropriate time comes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) (1), [Sql server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **CLI Commands:** find (1), unzip (1)
> **Exercise Files:** download the (2)
> **Env Vars:** sql (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)


### 1. Application Layers

[↑ Back to Table of Contents](#table-of-contents)

#### [Multitier architectures](https://www.linkedin.com/learning/database-foundations-application-development/multitier-architectures?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/multitier-architectures?u=76281980&t=0)** - [Instructor] [Databases](../../Skills/Software%20Development/Databases.md) are a critical component of many different kinds of applications. Understanding how they work is a great first step in the world of [Software Development](../../Topics/Software%20Development.md). But they do represent only a single piece of a completed puzzle. In order to finish off this database foundations series of courses, I thought it would be a good idea to [Zoom](../../Skills/Software%20Development/Zoom.md) out a bit and explore how databases are integrated into the larger picture of [Application Development](../../Skills/Software%20Development/Application%20Development.md). If you've been following this database foundation series since the beginning, well, first of all, congratulations, you've come a long way. You may remember way back in the first course that we discussed the client server model that relational [Database Management](../../Topics/Database%20Management.md) systems use. The database server manages and stores your data, and each user connects to the server using a client. This can either be a graphical interface, such as [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Data Studio that we have been working with, or a command line interface such as [SQL](../../Skills/Data%20Science/SQL.md) Command for working with [SQL Server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) or PSQL for working with [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md). These types of management clients connect you directly with the database server. You send the commands, or queries, that you want it to process and the server responds. This type of interaction is great for developing a database, or for directly manipulating the data, but it's not the way that most people interact with databases. Imagine going to your local library and having to connect
>
> **[1:35](https://www.linkedin.com/learning/database-foundations-application-development/multitier-architectures?u=76281980&t=95)** to their database and write a select query like this. Every time you want to locate the shelf a particular book is on. That would create a very complicated system for all of the library's patrons every time they needed to find a book. Instead, the library has a customized search portal that you can walk up to, enter in your keywords and other search parameters, and the application uses those inputs behind the scenes to retrieve the requested information from the database. The library patron gets the benefit of a custom tailored interface experience and doesn't need to concern themselves with the names of tables or columns or how everything is related. Almost every interaction that you have with a database is funneled through this type of custom built, front end user interface or application. In the world of software development, applications are commonly split into multiple areas of responsibility or layers. You'll hear this referred to as a multi-tier architecture or n-tier architecture. With this design pattern, software developers are able to separate the different components of the application, both logically, in code, and physically across different [Hardware](../../Topics/Hardware.md). The most common multi-tier architecture uses three different layers. A presentation tier, a logic tier and a data tier. The end user accesses the application through the interface in the presentation tier. You might hear this referred to as the application's front end.
>
> **[3:08](https://www.linkedin.com/learning/database-foundations-application-development/multitier-architectures?u=76281980&t=188)** It's the component of the software that makes the application easy to understand and use. In the presentation layer, you'll find interactive buttons and form controls for inputting data and graphics and text that provide navigation hints and organization. It's the component of the software that tells the user what they can do and how to do it. Once the end user selects their options, or inputs their requests, the logic tier takes over. This is the component of the software that sits in the middle and it processes the inputs and outputs. It uses programming constructs like logic gates, if then else blocks, or repeating loops to determine the actions that need to be taken. The logic tier performs any calculations that need to be made as information is moved from the front end to the backend. And finally, we get to the data tier, or the backend, where the information is actually stored. This is your database and it's often called the persistence layer of the application. It allows the data to be stored for future use. The data tier is responsible for saving, managing and retrieving information and passing requested information back through the logic tier and ultimately to the presentation tier, and finally back to the end user. There are many benefits to architecting software using these three tiers. First, since each component is separate it makes it much easier to update individual tiers. For example, you can upgrade the database,
>
> **[4:42](https://www.linkedin.com/learning/database-foundations-application-development/multitier-architectures?u=76281980&t=282)** or swap it over to a completely different platform with minimal impact on the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the application. Second, separate tiers can be scaled differently as the system grows. You can give the database or the logic tiers additional hardware in response to increased loads without having to touch the presentation layer. And finally, the different tiers can have different security contexts, with some being much more locked down and restricted than others. This isolation and compartmentalization of each tier, presentation, logic and data, creates an overall software application that's easier to build and maintain over time. Now we've already spent a lot of time discussing how databases work throughout this database foundation series. So we're not going to be spending too much time talking about them directly in this course. The goal of this course is to help give you a glimpse of what comes next. Your newfound knowledge about databases can open up the door to a very large world of possibilities in software development. At the end of this course, it's my hope that you come away with some insights into where to build your next set of skills and the types of technologies that might be worth pursuing as you continue your learning journey.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Databases](../../Skills/Software%20Development/Databases.md) (5), [Software Development](../../Topics/Software%20Development.md) (3), [Hardware](../../Topics/Hardware.md) (2), [Zoom](../../Skills/Software%20Development/Zoom.md) (1), [Application Development](../../Skills/Software%20Development/Application%20Development.md) (1)
> **Analogies:** such as (2), picture (1), imagine (1), for example (1)
> **CLI Commands:** find (2), psql (1)
> **Env Vars:** sql (2), psql (1)
> **Cross-References:** we discussed (1)
> **Tools:** command line (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Communicate with the data layer](https://www.linkedin.com/learning/database-foundations-application-development/communicate-with-the-data-layer?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/communicate-with-the-data-layer?u=76281980&t=0)** - [Instructor] When transitioning to a three-tier application architecture, we need to shift our thinking a little bit in how we interact with the relational database. In a multi-tier application, the database layer still functions as a server. It accepts connections and processes instructions that are sent to it from a client. This is similar to the way that we've been interacting with our database servers all along when we log in using a management GUI client or a command line interface. The big difference now is that the end user is no longer directly connecting to the database server. Instead, it's the logic tier of the application that becomes the database's client. Everything that we've learned about connecting to a database server still applies though. The logic tier will need to use a valid login account and password. It can have a user account with a specific set of permissions. For instance, maybe the logic tier is only granted select permission on the tables in a specific schema so that it can't access data in other schemas that may be present in the database and it can't modify any data. This helps secure the database and ensures that the logic tiers account can't be used for any nefarious purposes. It may feel strange to think about user accounts that don't represent an actual person, but you'll quickly get used to the idea that an application can be as much of a user as a physical human being. As far as the RDBMS is concerned,
>
> **[1:33](https://www.linkedin.com/learning/database-foundations-application-development/communicate-with-the-data-layer?u=76281980&t=93)** it doesn't really care if it's processing a request coming from a human or from a robot. Since the logic tier is treated as just another client, then it makes sense that it can have access to all of the same database constructs that we've explored. The logic layer can call store procedures and functions for executing repeatable commands within the database server. If multiple rows of data need to be added or modified, those instructions can still get placed inside of a transaction to ensure that consistency and integrity is maintained, and a logic tiers user account can be granted membership to group roles to make them subject to the same security constraints that can be granted and revoked on other users. By funneling all communication to the database server through the logic tier of the application, you gain fine-grained control over the final end [user experience](../../Skills/Web%20Development/User%20Experience%20(UX).md). It gives you the opportunity to build customized interfaces that include buttons and text boxes and formatted lists of content. This arrangement allows everyone to get the most useful information out of the database without them having to know how to write select statements or otherwise be experts in the inner workings of a relational database.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [User experience](../../Skills/Web%20Development/User%20Experience%20(UX).md) (1)
> **Env Vars:** gui (1), rdbms (1)
> **Analogies:** similar to (1), for instance (1)
> **CLI Commands:** make (1)
> **Tools:** command line (1)
> **Speakers:** - [instructor] (1)

#### [Databases on the web](https://www.linkedin.com/learning/database-foundations-application-development/databases-on-the-web?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/databases-on-the-web?u=76281980&t=0)** - [Instructor] [Databases](../../Skills/Software%20Development/Databases.md) are an important component to the functionality of our modern World Wide Web. Online stores use databases for managing inventory and orders, as well as showcasing [products](../../Skills/Software%20Development/Microsoft%20Products.md) on their sites. The [WordPress](../../Skills/Web%20Development/WordPress.md) publishing platform uses [MySQL](../../Skills/Software%20Development/MySQL.md) databases to organize and maintain every article, image and comment that's posted to blogs all over the world. In [Web Development](../../Topics/Web%20Development.md), it's common to think of the components of a site as either being the front end or the back end, or the client side versus server side. This distinction describes where certain actions occur when a user accesses a webpage. When you connect to a website, the web server sends you a series of files. Typically [HTML](../../Skills/Web%20Development/HTML.md), CSS, and [JavaScript](../../Skills/Software%20Development/JavaScript.md), as well as any media files, such as images. These files are interpreted and combined by your web browser and it renders the page that you see on the screen. This makes up the client side of the website, or the presentation layer of our three tiered application. The HTML file contains written content and organization information. CSS files contain styling instructions such as fonts and colors, and JavaScript is a client side scripting language that allows your computer to execute code that affects the pages layout, without having to send additional requests to the web server. Everything else is set to happen on these sites back end, or server-side.
>
> **[1:35](https://www.linkedin.com/learning/database-foundations-application-development/databases-on-the-web?u=76281980&t=95)** Code that needs to run server-side can be written in languages, such as [PHP](../../Skills/Software%20Development/PHP.md), [ASP.NET](https://ASP.NET), Node or Ruby. If the website needs to communicate with a database, that also happens server-side, and those interactions are kept separate from the end user accessing the site. If data is requested from a database then the web server fetches the information, processes it through server-side scripting languages and writes out brand new HTML formatted files that are sent to the end user. At no point is the end user given access to the underlying server-side scripting components that make the communication between the web server and the database server possible. Because web development touches on so many different kinds of technologies, it's no surprise that it requires a very broad set of skills. Some development work will leverage specialists in each area. A back-end developer for the server-side programming, a database developer for creating the data tier, and a front-end developer that's specialized in creating engaging user interfaces. Each of these specialist roles will combine their skills to move a project forward together. Alternatively, you might have heard of someone describing themselves as a full-stack developer. This means that they have experience working with the full range of these technologies and can hook them all together to create a functioning web application all on their own. It's not uncommon for someone to start off as a specialist
>
> **[3:08](https://www.linkedin.com/learning/database-foundations-application-development/databases-on-the-web?u=76281980&t=188)** in one area, and then evolve into a full-stack developer over time as they learn additional skills. In fact, that might be the trajectory that you're on right now. Having progressed through this database foundation series, you're well on your way to specializing in databases. This in turn may open doors in other areas of [Application Development](../../Skills/Software%20Development/Application%20Development.md) as your career progresses.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Databases](../../Skills/Software%20Development/Databases.md) (4), [HTML](../../Skills/Web%20Development/HTML.md) (3), [Web Development](../../Topics/Web%20Development.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1)
> **Env Vars:** html (3), css (2), php (1), asp (1), net (1)
> **CLI Commands:** mysql (1), php (1), node (1), ruby (1), make (1)
> **Analogies:** such as (3)
> **Definitions:** is a  (1), means that (1)
> **URLs:** [asp.net](https://asp.net) (1)
> **Speakers:** - [instructor] (1)

#### [Project overview](https://www.linkedin.com/learning/database-foundations-application-development/project-overview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/project-overview?u=76281980&t=0)** - [Narrator] Now that we've discussed the different tiers that go into a common data-centric application, let me take a moment and introduce the two projects that we'll look at over the next couple of chapters. Throughout this series, we focused on two different relational database platforms, [SQL Server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) and [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md). We've seen that they share many similarities in how they organize your data in tables and schemas and use common structured query language syntax for storing and retrieving information. While they do have the occasional implementation differences, it should be clear that the two platforms are more similar than they are different. In fact, it's safe to say that once you have experience with one relational [Database Management](../../Topics/Database%20Management.md) system, you're probably at least 80% of the way to understanding how to work with every relational database management system that's available today. They all share that much common DNA at their core and the skills that you've developed thus far will transfer to other platforms that you haven't even worked with yet. With those commonalities in mind, we're going to explore how each of these platforms can get integrated into a web application. Thinking about the three-tier approach to [Application Development](../../Skills/Software%20Development/Application%20Development.md), SQL Server and PostgreSQL, we'll provide our data tier. Next, we'll expand our use of Docker in order to pull in additional technologies for the logic tier of our application.
>
> **[1:34](https://www.linkedin.com/learning/database-foundations-application-development/project-overview?u=76281980&t=94)** These technologies will differ depending on the database platform that they're connecting to. For integration with PostgreSQL, we'll use a server-side scripting language called [PHP](../../Skills/Software%20Development/PHP.md). For integration with SQL Server, we'll use a [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) technology called [ASP.NET](https://ASP.NET). In both projects, the presentation tier will make use of standard [HTML](../../Skills/Web%20Development/HTML.md) and CSS that's used everywhere on the internet to create webpages. These components will all get hooked together so that communication can flow from the end user to the database through our custom interfaces. Now, there's a lot going on here and I don't expect you to have any experience writing PHP or [ASP.NET](https://ASP.NET) code or using HTML and CSS. I've given you all of the components that you need to get a working application that passes information between the database server and the end user. The goal here is to simply examine how the pieces fit together. My hope is that these two projects will give you an overview of what this entire process looks like in the real world. There are plenty of courses on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning that'll introduce you to each of these individual components should you be inspired to explore these technologies further.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Sql server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) (3), [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) (3), [Database Management](../../Topics/Database%20Management.md) (2), [PHP](../../Skills/Software%20Development/PHP.md) (2), [ASP.NET](../../Skills/Web%20Development/ASP.NET.md) (2)
> **Env Vars:** sql (3), php (2), asp (2), net (2), html (2)
> **CLI Commands:** php (2), docker (1), make (1)
> **URLs:** [asp.net](https://asp.net) (2)
> **Speakers:** - [narrator] (1)


### 2. PostgreSQL and PHP

[↑ Back to Table of Contents](#table-of-contents)

#### [What is PHP and Apache?](https://www.linkedin.com/learning/database-foundations-application-development/what-is-php-and-apache?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/what-is-php-and-apache?u=76281980&t=0)** - [Narrator] Our first project takes a look at creating a web form that interacts with a [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) database. To do this, we need to create a web server that'll provide the logic and presentation layers of our three-tier application. Our web server needs to do two things. First, it needs to accept connection requests from an end user's browser and then send the relevant files from the server to the client. You know this as hypertext transfer protocol, or HTTP. The most popular open source web server is the Apache HTTP Server and we'll use this software to send files to our browsers that creates the presentation layer of our application. The second thing that we need is support for a server-side scripting language. This will allow us to add programming elements to our application to create the logic tier of the application. For that, we'll rely on a language called [PHP](../../Skills/Software%20Development/PHP.md). PHP is great because it has a tight integration with standard [HTML](../../Skills/Web%20Development/HTML.md) documents. And you can have a single file that includes HTML and PHP code all mixed together. An example of PHP code might look something like this. Here, we have an HTML webpage which includes tags that identify the header and the title of the page, as well as the body content of the page. Inside of the body, we have a small PHP script.
>
> **[1:35](https://www.linkedin.com/learning/database-foundations-application-development/what-is-php-and-apache?u=76281980&t=95)** This script will [Fetch](../../Skills/Web%20Development/Fetch.md) the current date from the server and format it according to the pattern in parentheses. This is done before the final HTML document is sent to the end user and they won't see any of this code when the page renders in their browser. All the end user sees is something like this. It'll say Today is Monday, April 12th, 2021. The PHP code is executed on the web server and only the final output is sent to the client's browser. To make everything work without having to invest in expensive [Hardware](../../Topics/Hardware.md) or complicated network configurations, we're going to add to the system of Docker containers that we've been working with. This will allow us to build a little virtual network that contains both of our servers. We'll start with our PostgreSQL container and specifically use PostgreSQL version 13.2. Then we'll add a second container that has the Apache web server with support for PHP. When each of these are placed inside of a Docker container, both servers can easily communicate with one another. We can then add files to the web server and connect to it using our browser from our local operating system. So that's the plan for what we're going to build. Let's get to it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [PHP](../../Skills/Software%20Development/PHP.md) (7), [HTML](../../Skills/Web%20Development/HTML.md) (4), [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) (3), [Fetch](../../Skills/Web%20Development/Fetch.md) (1), [Hardware](../../Topics/Hardware.md) (1)
> **Env Vars:** php (7), html (4), http (2)
> **CLI Commands:** php (7), apache (2), docker (2), make (1)
> **Versions:** version 13 (1)
> **Speakers:** - [narrator] (1)

#### [Create a PHP web server](https://www.linkedin.com/learning/database-foundations-application-development/create-a-php-web-server?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/create-a-php-web-server?u=76281980&t=0)** - [Instructor] Let's get started building our first server cluster. All of the commands that you'll need to execute can be found inside of the chapter two folder and I've written them out in a file here called postgress_setup.text. Let me just open that up. And you can see all the steps that we're about to move through listed out here, as well as the specific docker commands that we're going to be executing on the command line. So let me close that down and we can get started. Now first, I want to make sure that docker is in fact running. So I'll look down here in my menu bar and I can see the docker icon right there. If you're on Mac OS or [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) you'll see the same icon up at the top of your menu bar when docker is running. Now we're going to execute commands on the command line. So I need to open up my [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) window here on [Windows](../../Glossary/Service/Windows.md). Again, if you're on Mac OS or Linux you're going to use the terminal application for this. Let me open that up and we can get started. Now at first, it's important to know that docker containers are typically kept isolated from one another for security reasons. In order for our web server to be able to talk to the database server we need to create a virtual network for them. And we do that with the command docker network create. And then we named the network. I'm going to call mine PGDB_network. In order for all of these exercises to work, you need to make sure that you're using the exact same names that I'm using. So go ahead and press Enter to execute the command and create the docker network. Now we can create our two server containers. Let's first set up the database server.
>
> **[1:33](https://www.linkedin.com/learning/database-foundations-application-development/create-a-php-web-server?u=76281980&t=93)** For that I'll use the command docker run dash dash name and I'll name it PG_DB_server. Next, we need to add our database server to the network that we just created. So I type in two dashes and network, followed by the name of the network we just created PGDB_network. Then I need dash dash publish. And this will set up our ports. So for this, I'm going to use port 5430 on my local computer, and I'm going to map that to 5432 inside of the docker container. Next I need a dash E followed by Postgress_password in all capital letters. This will set up the password for the Postgress user account. I'm going to use the password, Adam, capital A-D-A-M 123456. Again, typically you'll want to your own password but in order for this exercise to work correctly make sure you using the exact same password that I'm using here. After we type in the password I need dash D and we'll specify the name of the image that we're going to be using in order to create our container. The image that I want is the Postgress image. And I want to use a specific version. So I'll type in a colon and 13.2. I'll press the Enter button. So this tells me that it can't find the image locally. So it's going to download the Postgress image and create my local container
>
> **[3:06](https://www.linkedin.com/learning/database-foundations-application-development/create-a-php-web-server?u=76281980&t=186)** for the Postgress database server. All right, that's all set up. And now we can set up our web server container. To do that we'll run the docker run command again, once again, we'll name the container. So dash dash name, I'll name this container PHP_web_server. Once again, we need to put it in the same network so dash dash network, followed by PGDB_network. Then we'll set our ports with dash dash publish. I want to map port 8080 on my local computer to port 80 inside of the container. So that's 8080 colon 80. Then we need to specify the image so dash D and for this image, we'll use the [PHP](../../Skills/Software%20Development/PHP.md) image colon and a specific version of 8.0.2 dash Apache. I'll press Enter to create that container. Once again, it tells me that it can't find the image locally so it's going to download that from the internet and this will take just a moment to create my container. Okay. So I've got both of my containers set up now. And since both the web server and the database server are in the same network they're free to communicate with each other. And the ports that we've opened up for each server will allow us to connect to each resource from our own local computer.
>
> **[4:38](https://www.linkedin.com/learning/database-foundations-application-development/create-a-php-web-server?u=76281980&t=278)** I'm going to run the CLS command just to clear the screen and get all that off of the screen. And we'll move on to the next step. Next, we need to modify the PHP web server a little bit. By default, the PHP docker container doesn't have what it needs in order to connect to a database server. So we need to add on a Postgress QL extension. To do this I need to log into the web server. We do that with docker EXEC dash IT then the name of the web server, so PHP underscore web server is what we named it. And I'll use the bash shell. I press Enter and that'll move me inside of the container and it puts me inside of the container there at this file path var www dash [HTML](../../Skills/Web%20Development/HTML.md). Now I need to install a little bit of software. To do this we use the command apt-get update and and apt-get install dash Y, lib PQ dash dev, two more ampersands, docker dash PHP dash EXT dash install. Then PGSQL, then PDO, and finally PDO underscore PGSQL. Now this line is a little bit complicated if you've never worked with Linux software installations and you don't really need to concern yourself with what every piece is actually doing. The end result is that we're adding driver support
>
> **[6:13](https://www.linkedin.com/learning/database-foundations-application-development/create-a-php-web-server?u=76281980&t=373)** for making [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) connections with the PHP data object extension or the PDO. So I'm just going to make sure that everything is spelled correctly, and press the Enter key to run through that installation. Now, again, this is going to download some information from the internet. It'll take a few moments to install all of the software. After a few moments you should see this line that says, build complete and everything has been installed and configured on the server. Now, all we need to do is run the command exit to get out of the web server container, that will put us back onto the command line at our main PC. And then I can run docker, restart PHP web server. That'll shut down and restart the web server so that the Postgress extension can be enabled. Then I can type in the CLS command again to clear the screen and that'll return me back up here to the top. Finally, we can now copy all of our files from the exercise files chapter two folder up into the web server. To do that, we use the docker CP command, and then I need the file path to the exercise files. So I'm going to minimize this window here and I'm going to find the chapter two folder, and I'm just going to drag in the PHP web server folder I drag that whole thing over here and drop it onto the PowerShell Window. And that'll put it into the file path to that folder. Now, after that, let's go ahead and make sure this is maximized again. Now after the file path, I'm going to type in a period. It's all going to come back here
>
> **[7:46](https://www.linkedin.com/learning/database-foundations-application-development/create-a-php-web-server?u=76281980&t=466)** right before the closing quotation mark and I'll type in a another backslash and a period. If you're on Mac OS or Linux, you're going to use a forward slash and a period here after the PHP web server folder name. This will copy all of the containing files and folders that are within this root folder there. So we're going to copy all the files from that folder into our PHP web server container. So I'll come here after the quotation mark, type in a space, the name of the container, PHP underscore web server, then a colon and then we want it to go to a specific file path. So I'll type in a colon slash VAR slash www slash HTML and one more forward slash. Press the Enter button that'll upload all those files from the exercise files and the PHP web server folder up into the PHP web server container. Just to verify that all of those files were copied correctly. We can log back into the web server with docker EXCC dash IT, the name of the PHP web server again, PHP underscore web underscore server. And once again, we'll use the bash shell. That'll put us into that HTML folder and I can just type in LS to list out the contents and we should see the index PHP file, the scripts folder the server test folder, style sheet.CSS and the two trees folder. So everything was copied up correctly. You can type in exit. That will put me back onto my local computer and we're done with our terminal or PowerShell window.
>
> **[9:21](https://www.linkedin.com/learning/database-foundations-application-development/create-a-php-web-server?u=76281980&t=561)** Now, if everything worked correctly we should be able to open up our web browser and go to local host, colon 8080. That'll contact the PHP web server that we just set up and you should see this success message. This gives us a little bit of information about the server. It gives me the current date and time in UTC so don't be surprised if this date looks a little bit funny from where you are right now. So this is an UTC time. I have a link here to take a look at some PHP server information. That'll tell me the version of the server that we're working with. And if I scroll down here on this page about halfway down, we should find a section that talks about the PostgreSQL connection. And here we are, we've got the PDO section and it tells us we have PDO driver support for PostgreSQL. And we also have the PDO PGSQL driver listed out right here and we can see that it's enabled. So that is good. Click and close out this tab. And I can also click on the link here to view PostgreSQL information. This will send a simple query to the page and actually this web browser it looks like, it thinks that this is written in Danish which does not make any sense let's go ahead and close that. So it gives me some information about the PostgreSQL server, tells me I'm running PostgreSQL 13.2 running on Debian Linux. I got the name of the default instance there as well as the host name. This is the name of the docker container that we set up as well as the port that we set up. So it looks like everything is working. And with that, our little server cluster is up and running. And we have successfully communicated between the PHP web server
>
> **[10:55](https://www.linkedin.com/learning/database-foundations-application-development/create-a-php-web-server?u=76281980&t=655)** and the PostgreSQL database server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [PHP](../../Skills/Software%20Development/PHP.md) (19), [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) (7), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (5), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (3), [HTML](../../Skills/Web%20Development/HTML.md) (3)
> **CLI Commands:** php (19), docker (17), make (6), find (4), apt (2)
> **Env Vars:** php (19), pdo (6), html (3), pgsql (3), cls (2)
> **Prerequisites:** set up (9), install (4), you'll need (1)
> **Tools:** command line (3), powershell (3), terminal (2), bash (2)
> **UI Navigation:** go to (2), scroll down (1), click on (1)
> **Exercise Files:** exercise files (3), download the (1)
> **Ports:** port 5430 (1), port 8080 (1), port 80 (1)

#### [Connect to the PHP server](https://www.linkedin.com/learning/database-foundations-application-development/connect-to-the-php-server?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/connect-to-the-php-server?u=76281980&t=0)** - [Instructor] Before we dive into reading some [PHP](../../Skills/Software%20Development/PHP.md) code, let's take a quick look at how the exercise files are structured. I'm going to hop over to a terminal window and run the command docker ps -a. This will show me the status of my two Docker containers. I have a web server named php_web_server and we can access it by going to port 8080. We also have a database server named pg_db_server and it can be accessed at port 5430. Now when I open up my web browser and navigate to local host:8080, the file that we see here on the screen is the index.php file that you can find inside of the exercise files. Let's take a look at that folder. Here is our php_web_server folder that we uploaded into the web server inside of Docker, and inside of there are the different files that we're going to be working with. This index.php file is the file that we're currently seeing rendered on the screen back here in the web browser. We can open up this index.php file inside of our text editor. I'm going to be using Visual Studio Code, but you can use whatever text editor you have on your computer. Now the PHP file uses standard [HTML](../../Skills/Web%20Development/HTML.md) tags to organize the page structure and then sprinkles in some PHP scripts throughout the page. On line number eight is where we have the success message. It's written inside of h1 tags, so it gets rendered as a heading one inside of the browser. Below that on line number 10, we have the line that calculates the current date and time. It starts out with an HTML h3 tag,
>
> **[1:37](https://www.linkedin.com/learning/database-foundations-application-development/connect-to-the-php-server?u=76281980&t=97)** so this will be rendered as a heading three, but then we have a PHP tag that opens up right here and closes over here with the question mark and a closing bracket. Inside of the PHP tag, we have a couple of date functions. Here's one that calculates the current date and here's another one that displays the current time. The PHP code will get rewritten by the PHP server when the browser fetches the page. So here in the file, we see the date function that's calculating and formatting the current date and time, but in our web browser, we just see the result of the function. If I switch back into the web browser, we'll see the message here that today is Wednesday, March 17th, 2021. We're not seeing that PHP code, we're just seeing the result of the calculation. Now in this page, we also have three example scripts. I'm going to scroll down here. We have PHP Script One. If I scroll down further, we'll find Script Two and Script Three. We're going to take a look at those in the upcoming movies. Let's just go back up here to Script Number One and take a look at it real quick. Now each of these scripts have a green button down here at the bottom to run each one. These buttons will open up the files that we have in the exercise files folder, so if I switch back over and take a look at that folder structure again, we can go into the scripts folder and here is Script One, Script Two and Script Three. Now you can also open up these in your editor to read the contents. For convenience, though, I've added all of the code from these files so that it displays inside of the web browser inside of these gray contents boxes. This will make things easier to read
>
> **[3:11](https://www.linkedin.com/learning/database-foundations-application-development/connect-to-the-php-server?u=76281980&t=191)** so that we don't need to constantly jump back and forth between the browser and the text editor. Now if you decide to modify any of the exercise files, keep in mind that you'll have to copy them up to the web server again using the docker cp command in order to view the changes here in the browser. So that's how our exercise files are organized. We can now start to explore how the web server communicates with the database server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [PHP](../../Skills/Software%20Development/PHP.md) (12), [HTML](../../Skills/Web%20Development/HTML.md) (2)
> **CLI Commands:** php (12), docker (4), find (2), make (1), cp (1)
> **Env Vars:** php (9), html (2)
> **Exercise Files:** exercise files (5)
> **File Paths:** index.php (3)
> **Code Identifiers:** php_web_server (2), pg_db_server (1)
> **Ports:** port 8080 (1), port 5430 (1), :8080 (1)
> **UI Navigation:** scroll down (2), navigate to (1)

#### [PostgreSQL functions](https://www.linkedin.com/learning/database-foundations-application-development/postgresql-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/postgresql-functions?u=76281980&t=0)** - [Narrator] When we created the [PHP](../../Skills/Software%20Development/PHP.md) server, we had to log in and add support for making [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) database connections. When we did that, we enabled a number of PostgreSQL functions that can be used to send requests from the web server to the database server. The first lines of the Script 1 PHP file includes some standard [HTML](../../Skills/Web%20Development/HTML.md) header lines that'll set up the page. This includes the page's title Query 1 as well as a link to the style sheet. The body of the page is made up entirely of a PHP script. It starts here with the opening php tag and ends here with the closing php tag. This script contains two PostgreSQL functions. The first function is pg_connect. This is where we fill in the login details that will allow the script to reach out to the database server and make the connection. This function takes a number of parameters. The host would be the IP address or the network path of the database server. In our case, we can use the name of our Docker container since we placed the web server and the database server on the same virtual network. So my host is equal to pg_db_server. The port 5432 is PostgreSQL's default port that's used inside of that virtual network. We're going to connect to the default PostgreSQL database and use the default PostgreSQL user account. Finally, we need to specify the password Adam123456,
>
> **[1:36](https://www.linkedin.com/learning/database-foundations-application-development/postgresql-functions?u=76281980&t=96)** which was also established when we created the Docker container. We're using the default PostgreSQL user and password as a matter of convenience here. A more secure approach would be to set up a specific and unique user account for the PHP server to use so it has its own login credentials that we put here in the connection string. This does add a bit of complication though because we would need to ensure that the new user account has been granted all of the required permissions. I want to keep things simplified so we'll stick to the default user account for these examples, which already has administrator level permissions. Now, once a connection is made, we can send a query to the PostgreSQL database server. We do that with the pg_query function. The parameter here is simply the [SQL](../../Skills/Data%20Science/SQL.md) that we want to execute. This example creates a new database named test. So these are our first two PostgreSQL functions, pg_connect and pg_query. There are many other functions that we can use to send communications between the PHP web server and the PostgreSQL database server. I've added a link to the full documentation up here in the description. This lists out all of the PostgreSQL functions that we can make use of when we write PHP scripts. We have a long list of them all right here. For instance, we have a function that'll cancel an asynchronous query or that'll close a PostgreSQL connection. This is a great page to bookmark for future reference
>
> **[3:12](https://www.linkedin.com/learning/database-foundations-application-development/postgresql-functions?u=76281980&t=192)** if you plan on writing your own scripts. Let's go ahead and close this tab and return back to our example. Now, the only other lines in our PHP script are the ones above and below. These simply include the echo command which will print out text into our HTML files. The first one will print out the text Creating a new database named test, and the second one will echo the line Database created. Both of these get wrapped in h3 tags so they'll be formatted by our browser as heading three. Let's go ahead and press the Run button to execute the code. When it runs, we just see those two lines, Creating a new database named test and Database created. We didn't ask for anything to be returned from the PostgreSQL server, so there's no additional information shown on the screen. We can press the Back button to return back to our script. Now, if you press the Run button again, this time, the PHP server is going to try executing the same functions. And this time, you'll see an error message since the test database already exists now. We still get the Creating a new database line as well as the Database created line, so this might be a little bit confusing. Our script could use a little bit of work to better handle the error and we'll tackle that problem in a few minutes, but this is a good start. The fact that this page says that our database already exists confirms that our first execution was successful in creating the database on the PostgreSQL server. So pg_connect and pg_query
>
> **[4:46](https://www.linkedin.com/learning/database-foundations-application-development/postgresql-functions?u=76281980&t=286)** are probably the two most commonly used functions when you want a PHP web server to communicate with a PostgreSQL database server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) (15), [PHP](../../Skills/Software%20Development/PHP.md) (11), [HTML](../../Skills/Web%20Development/HTML.md) (2), [SQL](../../Skills/Data%20Science/SQL.md) (1)
> **CLI Commands:** php (11), make (2), docker (2)
> **Env Vars:** php (9), html (2), sql (1)
> **Code Identifiers:** pg_connect (3), pg_query (3), pg_db_server (1)
> **Definitions:** is a  (2)
> **Prerequisites:** set up (2)
> **Ports:** port 5432 (1)
> **Analogies:** for instance (1)

#### [Use PHP variables](https://www.linkedin.com/learning/database-foundations-application-development/use-php-variables?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/use-php-variables?u=76281980&t=0)** - [Instructor] We can make use of [PHP](../../Skills/Software%20Development/PHP.md) variables to help us reuse elements in our code. PHP variables all start with a dollar sign and then you can give them a unique name. Let's scroll down on our script page here down to script number two and take a look. Now in this example, we're going to add a table to the database that we just created. We need to perform similar tasks as before. First, we're going to connect to the database server and then send it a query, but we can use variables to hold the details of our [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) functions. First, I'm going to create a new variable called DB Connection. The name here starts with the dollar side. That's how I know that it's a PHP variable. Then, I can set its value to the PG Connect function followed by all of these parameters. We're connecting to the same server using the same port and the same username and password as before. The only difference here is that this time we're connecting to our new test database that we just created. So this fills in the value for our DB Connection variable. Next, I'll create a second variable and I'll call it [SQL](../../Skills/Data%20Science/SQL.md) Query. This gets the value of the SQL command that I want to execute. We're going to create a new table called testing and this table will have a single column called ID that'll hold integer values. So far, we've just created the variables and given them their values. We haven't actually sent any commands to the database server yet.
>
> **[1:34](https://www.linkedin.com/learning/database-foundations-application-development/use-php-variables?u=76281980&t=94)** To do that, we can now run the PG Query function. Inside of that, we'll pass it the values from our two variables: the DB Connection variable that we set right there and the SQL Query variable that we set right here. Essentially, what we're saying is to execute this query on this connection. Once that query executes, we can use a new Postgre function to close our connection. This will free resources on the database server. We'll use the PG Close function and supply it the value of a DB Connection variable that we used right up here. This will ensure that the connection that's closed is the same connection that we opened just a moment ago. Let me scroll down on the script and then I'll press the run button to run through all of that code. I get the message that it's creating the table in the test database and that the table was created. Once again, though, if I go back and try and run the script again, this time, I'm going to get an error message. It says that the relation, or what's commonly called a table, already exists. So that's a good sign that it was previously created successfully. Using PHP variables allows you to set up your database connection details and queries ahead of time, and then send all of that to the database server all at once rather than opening a connection and sending the query as two separate requests. This can help speed up communication between the web server and the database server and it makes your code easier to manage.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [PHP](../../Skills/Software%20Development/PHP.md) (4), [SQL](../../Skills/Data%20Science/SQL.md) (3), [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) (1)
> **Env Vars:** php (4), sql (3)
> **CLI Commands:** php (4), make (1)
> **UI Navigation:** scroll down (2)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Use if/else to handle errors](https://www.linkedin.com/learning/database-foundations-application-development/use-if-else-to-handle-errors?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/use-if-else-to-handle-errors?u=76281980&t=0)** - [Instructor] The two scripts that we've created so far haven't responded very well if you try and run the code multiple times. To fix this, we can add in some basic error handling to our script using an if-else programming construct. Let me scroll down on my page here until I get to [PHP](../../Skills/Software%20Development/PHP.md) Script 3. In this example, we're going to drop the database that we just created. This time, we're setting up the database connection variable in exactly the same way. We have the variable named db_connection and it's equal to the pg_connect function with these parameters. The only difference here is that we're connecting back to the [Postgres](../../Skills/Software%20Development/PostgreSQL.md) database again, not the test database. If we connected to the test database, then we wouldn't be able to drop it, since we would have an open connection. I'm also going to set up the sql_query variable again and this time, the syntax of my sql_query will be DROP DATABASE test WITH FORCE. The WITH FORCE modifier will force close any open connections that may still be present and is a new addition to [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) version 13. Now, instead of immediately running this query on this connection like we did in the previous example, I'm going to store the pg_query as another variable that I'm going to call result. The @ symbol before the pg_query function is a PHP notation that'll suppress any error messages that may otherwise try and display if something goes wrong. This allows us to handle the error message in a different way.
>
> **[1:34](https://www.linkedin.com/learning/database-foundations-application-development/use-if-else-to-handle-errors?u=76281980&t=94)** If our DROP DATABASE query is successfully processed, result will be true. If the DROP DATABASE query is unsuccessful, then this result will be false. We can use this information in an if-else block. If the result is true, then we'll print out the message with this echo command that says the database was successfully dropped. If the DROP DATABASE command fails though, the result will be false and the code will jump over to the else section. Here we'll echo the line the query failed with the following error. We can then use a new PostgreSQL function named pg_last_error and give it our current database connection. This'll return the specific error message from PostgreSQL so that we can see exactly what the problem is. When you're ready, let's go ahead and scroll down and click on the Run script_3 button. The first time you do that, you should see that the test database was successfully dropped. This is exactly what we wanted to happen. Let's press the back button and try and run the script again. This time, the else portion of the code is activated and we get a different message. The query failed and we get the error message that the test database does not exist. So now we have three different scripts that modify our Postgres database. One that adds a new database. Then we can add a table to the database and then we can drop the database, which returns everything back to the state
>
> **[3:06](https://www.linkedin.com/learning/database-foundations-application-development/use-if-else-to-handle-errors?u=76281980&t=186)** that the server was in at the very beginning. If you want, you can use [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Data Studio to inspect the server at any point. Let me open up Azure Data Studio and connect to it. On the server sidebar, I'll click on this button here to set up a new connection. We'll connect to a PostgreSQL database. The server will be localhost. We'll use password authentication with the username postgres and the password adam123456. I'll choose the option to remember the password and then click over to the Advanced section where I can type in the port. For this, I'll use port 5430. This is the external port that we're forwarding into the Docker container. Then I can go down here, actually don't need to update ADS right now, so I'm going to go ahead and close that and press the OK button. Then I'll press the Connect button to set up my connection. That'll connect me back into the PostgreSQL server that's running inside of Docker. Now, at this point, I only have the one database, the PostgreSQL database. So I can return to my script and we can run through the steps one more time. We'll scroll up here and run script_1 to create the database. All right, now I can go back. I'll scroll down and run script number two to add the table. Great, let's go ahead and close that. And now I can go into Azure Data Studio, I'll refresh my list of [Databases](../../Skills/Software%20Development/Databases.md) and there is the test database. If I expand the tables folder, we'll see the testing table with its single column.
>
> **[4:41](https://www.linkedin.com/learning/database-foundations-application-development/use-if-else-to-handle-errors?u=76281980&t=281)** So using some PHP scripts, we're able to execute commands by interacting with a basic web form and the web server in turn sends our instructions to the database server for execution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) (6), [PHP](../../Skills/Software%20Development/PHP.md) (3), [Postgres](../../Skills/Software%20Development/PostgreSQL.md) (3), [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (3), [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **Env Vars:** drop (4), database (4), php (3), force (2), ads (1)
> **Code Identifiers:** sql_query (2), pg_query (2), db_connection (1), pg_connect (1), pg_last_error (1)
> **UI Navigation:** scroll down (3), click on (2), scroll up (1)
> **CLI Commands:** php (3), docker (2)
> **Prerequisites:** set up (3)
> **Definitions:** is a  (2)
> **Ports:** port 5430 (1)

#### [Add data to a database](https://www.linkedin.com/learning/database-foundations-application-development/add-data-to-a-database?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/add-data-to-a-database?u=76281980&t=0)** - [Narrator] [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) functions allow us to do more than just connect and send requests to the database server. There are additional functions that allow us to work with information that comes back from the server such as the results of a select query. To see this in action, scroll down on this page all the way to the bottom until you find the link to the Two Trees Olive Oil database. Alternatively, you could just browse to local host:8080/2_trees. This will open up another batch of files that we previously uploaded from the exercise files folder up into our web server. So that we have some interesting data to look at, the first script on this page will set up a sample database. It goes through a couple of different steps. The first thing that it's going to do is create a database. It does that by creating a connection, connection number one, and it uses the pg_connect function with these parameters. Then we'll set up a query that'll create the database two_trees. Next, we'll execute that query on connection number one. And finally, we'll close the connection. So that's the first step. Now that the database is created, we can log into it and create the table. To do this, we need a second connection and I'm going to call it db_connection_2. Once again, it uses the pg_connect function, but this time, we're going to connect to the two_trees database that we set up in the previous lines. Once we're logged into that,
>
> **[1:33](https://www.linkedin.com/learning/database-foundations-application-development/add-data-to-a-database?u=76281980&t=93)** we can create a schema inside of that database with the CREATE SCHEMA inventory line here and then we'll execute the create_schema_query on connection two. Next, we'll create a table. We'll set up another variable called create_table_query. The contents of this variable will be our CREATE TABLE statement and it's going to create a table called [products](../../Skills/Software%20Development/Microsoft%20Products.md) on the inventory schema. This product table will have five different columns: SKU, product name, category ID, size, and price. Once we've set up that query syntax, we can execute that query with the create_table_query syntax right here and we'll execute that query on connection number two. And finally, we can add data into this table. Once again, we'll create one more variable, this time called insert_data_query. The text of this variable is going to be INSERT INTO inventory.products and we'll fill in the values for the five different columns. The values will be this long list here and you can see that we're inserting a lot of rows into this table. So let me go ahead and scroll down on this and you can see there's actually a lot of data there. Finally, we get to the bottom. We'll run the insert_data_query syntax on connection number two. And then at the end of all of that, we'll close connection two. Go ahead and press the CREATE the Two Trees Database button. It'll go through the process and run through all of those steps. It'll create the database, create the schema, create the table, and then add in all the data. Now, we're not going to get any messages back from this
>
> **[3:08](https://www.linkedin.com/learning/database-foundations-application-development/add-data-to-a-database?u=76281980&t=188)** if it's successful, so you'll just see this screen here. Go ahead and press the Back button and the database will be created. Now, if you need to, I also created a second script that'll delete the database. You can access it from the DROP the Two Trees Database button right there if you wanted to go through and reset the server again. As a sanity check, we can verify everything worked correctly by logging in with [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) Data Studio. This step is optional though, but if you do have that connection already created to our database server in Azure Data Studio, you can open it up and right-click on the [Databases](../../Skills/Software%20Development/Databases.md) folder, choose Refresh, there is the Two Trees database. Let's expand tables. There is the table. Here are all of the columns. And if I right-click on the products table and choose select top 1,000, we should see some data returned and there is all the data that was entered with the [PHP](../../Skills/Software%20Development/PHP.md) script. So now we have a database for the Two Trees Olive Oil company with some product data that we can play with.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (3), [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (2), [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1), [PHP](../../Skills/Software%20Development/PHP.md) (1)
> **Env Vars:** create (3), schema (1), table (1), sku (1), insert (1)
> **Code Identifiers:** pg_connect (2), two_trees (2), create_table_query (2), insert_data_query (2), create_schema_query (1)
> **Prerequisites:** set up (5)
> **UI Navigation:** scroll down (2), right-click (2)
> **CLI Commands:** find (1), php (1)
> **SQL:** create table (1), insert into (1)
> **Ports:** :8080 (1)

#### [Format SELECT query results](https://www.linkedin.com/learning/database-foundations-application-development/format-select-query-results?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/format-select-query-results?u=76281980&t=0)** - [Instructor] Data that's returned from a select query, turns into an array of values that we need to process in [PHP](../../Skills/Software%20Development/PHP.md) before it's usable. This will often require a looping routine in order to write out each row of data one row at a time. In the two trees database file, let's scroll down until we get to query number one. This first query in the two trees database will retrieve all of the values from the [products](../../Skills/Software%20Development/Microsoft%20Products.md) table. The procedure starts with these same variables that we've been working with. We'll create a variable called db_connection that contains our connection string, we'll then set up a variable called query_db that contains the [SQL](../../Skills/Data%20Science/SQL.md) that we're going to execute. The query that I'm going to run is a select statement. We'll select the sku, product_name, category_id, size, and price columns from the inventory at our products table. Finally, we have the results variable, it'll hold well, whatever comes back from the server when we execute this query on disconnection. So that's the setup. Now we need to do something with those results in order to read them here in the browser. We'll start with an if else block. This will allow us to handle any errors that may arise. If the result is successfully retrieved, we'll run through all of these lines. If the result fails to retrieve data for any reason, we'll jump down to the else portion and run these two lines. This will allow us to display the error
>
> **[1:33](https://www.linkedin.com/learning/database-foundations-application-development/format-select-query-results?u=76281980&t=93)** that's reported by [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md). If the results are successful though, we'll print out two lines of text. We'll echo the line, the query executed successfully followed by the line, Two Trees Product Information. Now we need to work through the actual data. We do that with a for loop. This will process each row one by one. The first line of the for-loop sets up a counter which determines when to run through the loop again, and when to stop. We start with a counter variable called row and set its initial value to zero. Then as long as the row counter is less than the number of rows contained in the result, we'll run through the loop again. After it's all done, we'll increment the row counter by one. That's what this row plus plus portion means. For each loop, we'll find the value of each column. The pg_fetch_results function we'll get the value from the full results set of the current row in the column called sku. We'll then save that value in a new variable called sku. We do the same thing for the product name column. We'll use the pg_fetch_result function applied to our results set for the current row in the product name column. We'll take whatever value that finds and save that as the product name variable. We'll, then do the same thing for category_id, the size and the price. Once each variable is populated for the first row, we write them out.
>
> **[3:07](https://www.linkedin.com/learning/database-foundations-application-development/format-select-query-results?u=76281980&t=187)** This uses the echo command. It'll create a string of text that includes the sku value followed by a space, the product name, another space, category_id space size, space, and then finally price. At the end it'll add an [HTML](../../Skills/Web%20Development/HTML.md) line break. This will set up the next row of data so that all finishes the first row of data, the loop returns to the top, it increments the counter and runs through the entire statement again for the second row of data from the result set. It'll print that all out, using the echo command and then run through it again for row number three. It'll keep doing this until all of the rows from the results set are processed. To see what this looks like, let's scroll down and find the run query one button. This shows all of the data formatted as a simple list. This is a good start but the presentation leaves a lot to be desired. And this isn't very legible. Instead of outputting the values to a list, we can incorporate them into an HTML table. Let's press the back button, they'll return to our example code and I'll scroll down to query number two. This PHP script starts out exactly the same, we use the same database connection, the same query string and the same results string. In the if block is where things start to change. This is where we'll construct our HTML table. The initial components of the table are built before we start into the loop.
>
> **[4:41](https://www.linkedin.com/learning/database-foundations-application-development/format-select-query-results?u=76281980&t=281)** So we'll echo a new opening table tag and we'll start the first row of data. This will be a header row for the table, so we'll use the tag to denote table header rows and this'll just give us a column header for each column of the table. So we have the sku column, product_name, category_id, size and price column headers. This will clarify what data is in each column. Then we start our for-loop. This portion is exactly the same as before, the difference is what we do with each variable. Instead of echoing them out to a single line, we'll build another table row. Each variable gets written out to its own data cell in that table row. So we'll set up a new row at the beginning, we'll have a table data cell for the sku, another table data cell for the product name, category_id, size and price and then we'll end the row. Each time the loop is processed, another complete row is added to the table. So let's go ahead and scroll down and see what this result looks like. I'll press the run query to button and I get a much better presentation of the data. All of the values get put into a well formatted HTML table and the CSS styling can be applied to make the header row stand out and give us a better alternating row shading that makes the table easy to read. If else, constructs and for loops are just a couple of the many programming techniques that can be applied to your data when you move into an [Application Development](../../Skills/Software%20Development/Application%20Development.md) role. They allow you to process your data in interesting ways
>
> **[6:17](https://www.linkedin.com/learning/database-foundations-application-development/format-select-query-results?u=76281980&t=377)** apply formatting to increase legibility and help create dynamic interactive presentations.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (4), [PHP](../../Skills/Software%20Development/PHP.md) (2), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (2), [SQL](../../Skills/Data%20Science/SQL.md) (1), [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) (1)
> **Code Identifiers:** category_id (5), product_name (2), db_connection (1), query_db (1), pg_fetch_results (1)
> **Env Vars:** html (4), php (2), sql (1), css (1)
> **CLI Commands:** php (2), find (2), make (1)
> **UI Navigation:** scroll down (4), select the (1)
> **Prerequisites:** set up (3), setup (1), before we start (1)
> **Definitions:** is a  (2)
> **Speakers:** - [instructor] (1)

#### [Filter results with form controls](https://www.linkedin.com/learning/database-foundations-application-development/filter-results-with-form-controls?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/filter-results-with-form-controls?u=76281980&t=0)** - We can let our end users interact with the database dynamically by providing some form controls. Form controls we'll collect input from the user and then insert those values into the query commands before they're executed on the database server. I've scrolled down to the third query example in the two trees database. Here, we have an example of a form control called a selection box or a dropdown list. This allows the user to choose from several different options for the types of [products](../../Skills/Software%20Development/Microsoft%20Products.md) that they want to see. The options are number one, olive oils. Number two, flavor infused oils or number three bath and beauty products. Let's select number two, flavor infused oils. Now press the run query three button and we'll see a table that's displayed that only shows the products with a category ID number two. If you go back to the example page and make a different selection, let's choose number three this time. Run the query again, and the resulting table shows me just the products and category ID number three. In order for this to work we need two things. First, we need to collect the input from the end user. Let's look at the code that creates this selection list. In the exercise files of the chapter two folder, go into the [PHP](../../Skills/Software%20Development/PHP.md) web server folder and then into the two trees folder. I'm going to open up the index.PHP file in my code editor. The lines that I want to look at are all the way down on lines 52 through 60.
>
> **[1:35](https://www.linkedin.com/learning/database-foundations-application-development/filter-results-with-form-controls?u=76281980&t=95)** This is where we set up our form controls. The selection box is given a name. In this case, ours is called category ID value. This creates a PHP variable. Each option in the selection list is associated with a value. When the user makes a selection from the three choices the value of their choice is saved to the variable. So if the user selects bath and beauty, the category ID value variable is set to three. When the user selects olive oils, the variable is set to one. This variable and its value are then posted up to the PHP server when the user pushes the submit button. So that's how the browser collects the user input and sends it back to the web server. From here, we need to look at the PHP script to see how the input is actually used. Let's go back to our example code. In order to incorporate user input, we need to slightly modify our approach to sending [SQL](../../Skills/Data%20Science/SQL.md) commands to the database server. We start by creating our connection. I'll set up a variable called DB connection and it will be equal to our PG connect function with these parameters. This part hasn't changed. Next, we need to prepare our query. This uses a new function called PG prepare. This requires three parameters. First, we'll send the query to the connection that we just set up. Next, we'll give it a query name.
>
> **[3:08](https://www.linkedin.com/learning/database-foundations-application-development/filter-results-with-form-controls?u=76281980&t=188)** I'm going to call it my query. Finally, we add in the SQL command that we want to execute. We'll select all five columns from the inventory.products table. In order to filter the results, we'll add in a where clause. We want to filter the results to the only those where the category ID is equal to, well, whatever the end user wanted to see as indicated by their selection and the dropdown list. To fill in the value, we'll use a numbered variable. It starts with dollar sign one. Our query only needs to incorporate a single user input variable. So that's why we're using the number one. This finishes the prepared query and we'll save all of that as result. Finally, we get to the point where we actually complete the where clause and execute the query. We update the result and use the PG execute function. We'll reuse the previously established connection and we'll use the prepared query called my query. The final piece fetches the category ID value variable that was posted by the end user to the PHP server. This will fill in the dollar sign one variable here in the where clause. So when the user selects bath and beauty, the category ID is set to three. If the user selects olive oils, the category ID is set to one. The database server is then able to return the filter data set back to the web server, where it gets saved as our result.
>
> **[4:41](https://www.linkedin.com/learning/database-foundations-application-development/filter-results-with-form-controls?u=76281980&t=281)** After that, we just need to display the results for the end user. The structure of the results doesn't change. It's the same table, the same number of columns. The only thing that's different is the number of rows. So we don't actually need to do anything different with our for-loop. Let me scroll down in the script a little bit. And you'll see that everything here in the if block and the for-loop is exactly the same as what we had in the previous example. It formats all those results into an [HTML](../../Skills/Web%20Development/HTML.md) table. This for-loop will process whatever the results that contains. It doesn't matter if it's one row or a thousand rows. So now we have a dynamic form where the user can make a selection in this dropdown list, press the run button and they'll get a customized view of the data. By using form controls that post values back to the web server. The PHP script can incorporate those values into its code when it communicates with the database server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [PHP](../../Skills/Software%20Development/PHP.md) (7), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (5), [SQL](../../Skills/Data%20Science/SQL.md) (2), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** php (7), sql (2), html (1)
> **CLI Commands:** php (7), make (2)
> **UI Navigation:** dropdown (3), scroll down (1)
> **Prerequisites:** set up (3)
> **Cross-References:** go back to (2)
> **File Paths:** index.php (1)
> **Exercise Files:** exercise files (1)

#### [Add data to a table with form controls](https://www.linkedin.com/learning/database-foundations-application-development/add-data-to-a-table-with-form-controls?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/add-data-to-a-table-with-form-controls?u=76281980&t=0)** - [Instructor] Using form controls, we can allow end users to add new rows of data to the database, without them having to know anything about how [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md) work or how our specific database is structured. Query_4 includes some form controls that accept user input. We have four text boxes and one selection list. Let's add a new row to the database. For the skew I'll fill in SOAP001. The product name is Olive Oil Bath Soap. We'll put it into the Bath and Beauty [products](../../Skills/Software%20Development/Microsoft%20Products.md) category. The size is eight ounces and the price is 599. Let's press the Run Query button and the page returns the message that our row is inserted and it confirms the values that we submitted. And if you go back and then scroll up to query number three, we can run a select query against everything in the bath and beauty products category. And at the bottom, we should see our new product that we just entered. So our form is working. Let's go ahead and go back and take a look at how it works. The process of inserting a row of data into the table uses the same general principles as modifying the where class of the select query. We're using form controls to post values back to the web server. The web server then incorporates those values into a database query. The only difference here is that we're posting multiple values at once.
>
> **[1:34](https://www.linkedin.com/learning/database-foundations-application-development/add-data-to-a-table-with-form-controls?u=76281980&t=94)** Let's go back into the exercise files again and take a look at the code. I'm going to go into the [PHP](../../Skills/Software%20Development/PHP.md) web server folder, two trees, and then open up index.php in my code editor. This time I'm going to scroll down and we'll take a look at lines 72 through 88. Here are our five form controls. Most of them are simple text boxes so they have an input type of text. They all have names though. The first one is the sku_value, the second one is the name_value. Then we have our drop-down list for the category_id_value with the three selections inside of it. Next, we have the text box for the size_value and a text box for the price_value. These all become variables when the user fills them in and they're posted to the web server. Now we can return back to the PHP script to see how they're used. In the script, we're going to set up the same database connection that we've always been using here with the db connection variable. Then we'll prepare our query. The [SQL](../../Skills/Data%20Science/SQL.md) command that we're going to execute is an INSERT_INTO command. So we're going to insert a row into the inventory.products table and we'll supply values for each of the five columns. The values themselves get placeholder variables numbered one through five. This is because we don't yet know what data is going into the table. That completes the prepared query. Finally, we'll fill in the missing values and execute the query with the pg_execute function.
>
> **[3:09](https://www.linkedin.com/learning/database-foundations-application-development/add-data-to-a-table-with-form-controls?u=76281980&t=189)** The variables that were posted by the end user are identified in the same order that they're used in the prepared query. So dollar sign one gets replaced with sku_value. Dollar sign two gets replaced with name_value. That takes all the values coming out of the form and puts them into the insert into command. This completes the query and the database server is able to correctly write everything to a new row in the products table. Now, at this point, the data entry is complete. We don't technically need to return anything to the end user, but it is nice to have some sort of confirmation. The next portion of the code reiterate what just happened and tells the end user that the insert was successful. It creates a few lines that echo out the same values that the user posted through the form and that were incorporated into the query. Of course, the insert command might not be successful, for instance, if the user tries to add a value that violates a table constraint such as a duplicated primary key or a text value when an integer is expected. Should that happen, we also have the if else logic to return an error message. In fact, we can see the error message if we try and enter in the same soap product again. Let me scroll up here where we still have the same values that we filled in a moment ago and I'm just going to try and run this query again. This time [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) complaints that I duplicate primary key value is given. So it can't add the new row. Let me go back, we'll try and change this up a little bit. I'll change this sku to SOAP002,
>
> **[4:45](https://www.linkedin.com/learning/database-foundations-application-development/add-data-to-a-table-with-form-controls?u=76281980&t=285)** but this time instead of a size eight, I'll type in a size large. Once again, I'll run the query and we get a different error message. This time the error says that it was expecting an integer but we supplied a text string. This completes our first project working with PHP and PostgreSQL. You've seen how the servers communicate with each other using functions, how to format tabular data that's returned from select queries and how don't use form controls to post information back to the server so that it can be used to create customized SQL queries. These are all solid building blocks that make up the core foundation of any online web application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (4), [PHP](../../Skills/Software%20Development/PHP.md) (4), [SQL](../../Skills/Data%20Science/SQL.md) (2), [PostgreSQL](../../Skills/Software%20Development/PostgreSQL.md) (2), [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md) (1)
> **Code Identifiers:** sku_value (2), name_value (2), category_id_value (1), size_value (1), price_value (1)
> **Env Vars:** php (3), sql (2), soap001 (1), insert_into (1), soap002 (1)
> **CLI Commands:** php (4), make (1)
> **UI Navigation:** scroll up (2), scroll down (1)
> **Analogies:** for instance (1), such as (1)
> **File Paths:** index.php (1)
> **Exercise Files:** exercise files (1)


### 3. SQL Server and ASP.NET Core

[↑ Back to Table of Contents](#table-of-contents)

#### [What is ASP.NET Core?](https://www.linkedin.com/learning/database-foundations-application-development/what-is-asp-dot-net-core?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/what-is-asp-dot-net-core?u=76281980&t=0)** - [Narrator] It's time to change gears and turn our attention to our second project. This time, we're going to focus on [SQL Server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) and a web server technology called [ASP.NET](https://ASP.NET) Core. While [PHP](../../Skills/Software%20Development/PHP.md) data objects, or PDO, does have support for connecting to SQL Server [Databases](../../Skills/Software%20Development/Databases.md) through specialized drivers and functions, it's more common that SQL Server databases are paired with other [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) technologies. [ASP.NET](https://ASP.NET) Core is a web framework that takes a different approach to the development of web applications. It's open source and unlike the older [ASP.NET](https://ASP.NET) that it replaces, Core is a full rewrite that brings cross-platform support, so it's a solid framework for building web apps no matter what operating system you work on. The [ASP.NET](https://ASP.NET) Core framework takes a very modular approach to creating web apps. You can think of it as a write once, use everywhere philosophy. The three main components, models, views, and controllers, all hook together to create the finished application. This modular design makes testing and development easier, but it does spread functionality out across multiple files that all link and reference each other. Because of this modular design, when developing an [ASP.NET](https://ASP.NET) Core web app, you're almost certainly going to want to use the Visual Studio IDE, or Integrated Development Environment.
>
> **[1:34](https://www.linkedin.com/learning/database-foundations-application-development/what-is-asp-dot-net-core?u=76281980&t=94)** Visual Studio is available for [Windows](../../Glossary/Service/Windows.md) and Mac OS. This will allow you to take advantage of starter projects and templates that help automate the connection of these different components across multiple files. So what is a model, a view, or a controller? The controller class can be thought of as the application's logic layer. It handles requests from the client's browser and works with data from the model. It also determines which views to send back to the client and how to format them. Views make up the presentation layer or the user interface that the client sees. They organize and format data that's passed in from the controllers. And models are representations of the data. They read and store data in the database and enforce the business logic that's placed on the data. So those are the building blocks that make up an [ASP.NET](https://ASP.NET) Core web application. Next, we're going to set up a new server, connect a SQL Server database, and see how all of these components work together.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [ASP.NET Core](../../Skills/Web%20Development/ASP.NET%20Core.md) (5), [Sql server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) (4), [Databases](../../Skills/Software%20Development/Databases.md) (2), [PHP](../../Skills/Software%20Development/PHP.md) (1), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **Env Vars:** asp (6), net (6), sql (4), php (1), pdo (1)
> **URLs:** [asp.net](https://asp.net) (6)
> **CLI Commands:** make (2), php (1)
> **Definitions:** is a  (3)
> **Tools:** visual studio (2)
> **Analogies:** think of it as (1)
> **Prerequisites:** set up (1)

#### [Create the ASP.NET Core web server with Docker](https://www.linkedin.com/learning/database-foundations-application-development/create-the-asp-dot-net-core-web-server-with-docker?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/create-the-asp-dot-net-core-web-server-with-docker?u=76281980&t=0)** - [Instructor] To create our [ASP.NET](https://ASP.NET) Core web app, we already know that we need two components, a web server and a database server. We're going to leverage Docker again to compartmentalize everything and make it easy to manage. But let's take a different approach from what we did with the [PHP](../../Skills/Software%20Development/PHP.md) project. In the first project we created a network then created each server container in that network, we also had to log into the PHP web server to install some additional software drivers. We did all of this by running multiple commands in sequence. Docker has a very useful feature called [compose](../../Glossary/Framework/Jetpack%20Compose.md) that will allow us to combine multiple steps into one. This makes it much easier to set up applications that require multiple containers working together. In the exercise files chapter three folder you'll find the project folder, [ASP.NET](https://ASP.NET). This folder contains all of our project files. Now there's three files that I want to focus on. Docker-compose.yml, the Dockerfile and entrypoint.sh. Let's go ahead and open up all three of these inside of our code editor. First let's review the Dockerfile. This adds up our main working environment. It starts by specifying the image that our container will be built form. In this case the [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md)/dotnet/SDK version 5.0. It then lists out additional commands
>
> **[1:34](https://www.linkedin.com/learning/database-foundations-application-development/create-the-asp-dot-net-core-web-server-with-docker?u=76281980&t=94)** to run on the container to configure it. First, we copy all of the project files into the containers app folder. Then we set the working directory within the container. Next, we run two commands to configure the dotnet server and expose a port. And finally we call another script, the entrypoint.shell script. If you look at that shell script you'll see that it really doesn't contain that much. We have a couple of commands here that are run to start up the dotnet server and the [SQL Server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) container. Finally, it let's review the Docker compose YML file. This one defines the additional services that our application requires. We're setting up two services here a web service and a database service. The web service was already defined in the Dockerfile, so we just needs to know what ports to map from our local computer into the container. In this project, we'll be connecting to the [ASP.NET](https://ASP.NET) web server on port 8,000. The web service depends on the database service and you see that on line number eight, so let's take a look at that service. The database container will use the latest version of the SQL Server 2019 image. We then have the password for the SA account, Adam123456. And the parameter that accepts the end user licensing agreement. Finally, our SQL Server container will be accessible from our local computer on port 1450. So these three files work together to set up
>
> **[3:07](https://www.linkedin.com/learning/database-foundations-application-development/create-the-asp-dot-net-core-web-server-with-docker?u=76281980&t=187)** and configure everything that we need for our application. All we need to do is kick off the process. Let's go ahead and close Visual Studio Code here and we'll take a look inside of the chapter three folder and find the SQL Server setup text file. The commands that you need to run on your terminal are all written out here in this file. But there's really only one step to setting everything up, I'll close this file down and then open up my [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) or terminal application. Then we need to switch into the ASP NET project folder. I'll type it in the CD and actually let me minimize this. And I'll just drag that folder in to my PowerShell window to type in that file path. Okay, that'll switch our context into that folder, the ASP NET folder. Next I'll run Docker dash compose build. This will download the images if you don't have them already and configure all of the services. You'll want to run this command once to compose the Docker images or after making any changes to the apps project files. Since one of the steps is to copy the project files into the web server. So this'll just take a couple of minutes to complete, so we'll just let it do its thing. Once it finishes it'll return you back into your command prompt and I'll just type CLS to clear the screen, return back up to the top. Once the containers are built you can start everything up. To do that we run Docker dash compose up. You'll run this command
>
> **[4:41](https://www.linkedin.com/learning/database-foundations-application-development/create-the-asp-dot-net-core-web-server-with-docker?u=76281980&t=281)** whenever you want to start up the servers. For instance, if you reboot your computer or after shutting down the web server. When you get to this screen that has a couple of informational messages you'll know that everything is up and running. You'll need to leave the terminal window open in order to maintain the servers. When you are done working you'll come back to this window and press Control + C to shut down the servers. And there's a message about that right here on this line. So I'm going to minimize this window and just leave it open and then we can open up our web browser. I'll navigate to local host call in 8,000. That'll connect to our [ASP.NET](https://ASP.NET) Core web server and you should see this page. At this point the web server is up and running and displaying the files from our project. You can use the navigation menu at the top to click to the different pages. For instance, I'll click over here to Hello World. But if you try and go to the customer's page you're going to get a database error. It tells me that I can't connect to the Two Trees database. The SQL Server container is set up but we haven't yet created this database. So we'll tackle that next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (6), [Sql server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) (5), [ASP.NET Core](../../Skills/Web%20Development/ASP.NET%20Core.md) (2), [PHP](../../Skills/Software%20Development/PHP.md) (2), [ASP.NET](../../Skills/Web%20Development/ASP.NET.md) (2)
> **Env Vars:** asp (6), net (6), sql (5), php (2), sdk (1)
> **CLI Commands:** docker (7), dotnet (3), php (2), find (2), make (1)
> **Prerequisites:** configure (4), set up (3), install (1), setup (1), you'll need (1)
> **Tools:** terminal (3), powershell (2), visual studio (1), command prompt (1)
> **URLs:** [asp.net](https://asp.net) (4)
> **File Paths:** docker-compose.yml (1), entrypoint.sh (1)
> **Ports:** port 8 (1), port 1450 (1)

#### [Add a database to SQL Server](https://www.linkedin.com/learning/database-foundations-application-development/add-a-database-to-sql-server?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/add-a-database-to-sql-server?u=76281980&t=0)** - We need to add our two trees database to the [SQL server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) instance. There are two different ways that we can do that using either our graphical management client, [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) data studio or we can use the command line. Let's take a look at both methods. I'm going to minimize the web browser here and take a look inside of the chapter three folder of the exercise files here. We have the two trees.[SQL](../../Skills/Data%20Science/SQL.md) file. Let me open that up inside of my code editor. Now this script is going to go through and create the two trees database. Then create the customer table inside of that database and finally insert some rows of data into the customer table. I'm going to press control A on my keyboard to select everything and then copy that to my clipboard. Let me go ahead and close out of the code editor now. So let's see the graphical user interface method first. I'm going to open up Azure data studio, then we'll create a new connection. So click on this icon here with a little server and the plus icon. That'll open up the connection sidebar. We'll connect to a [Microsoft SQL Server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) connection and the server will be local hosts. We'll use a SQL login, the SA system administrator account with a password Adam 123456. I'll choose the option to remember the password. Let's go ahead and give this a name. I'll call it SQL server [asp.net](https://asp.net). Then we'll click over to the advanced tab. Here we'll put in the port that we set up when we configured the Docker container.
>
> **[1:34](https://www.linkedin.com/learning/database-foundations-application-development/add-a-database-to-sql-server?u=76281980&t=94)** Let me scroll down until I find the port section. And the port that we enabled was 1450. Press okay. And then press the connect button and it should connect into our SQL server instance. At this point, I can right click on the server name here, choose new query. Then I can paste in the text of our script and press the run button to execute it. That'll go through and create the two trees database and fill in all the data. Now I'm not actually going to run this script here because I want to show a different method for doing the same thing. So I'm actually going to close out of this tab and not save any changes. In fact I can close all of these other tabs as well. The other way to do the same thing is on the command line. Let me go ahead and minimize Azure data studio. Now we already have a command line or a [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) window open. That's got our running server inside of it. So I don't want to mess with this window. What I do want to do though is open up a second window. So I'll open up another PowerShell window. Then, we need to review what Docker containers we currently have running. I'll run Docker PS. That shows me I currently have four containers. We have the two for our [PHP](../../Skills/Software%20Development/PHP.md) example running right there. The PHP web server and the PG DB server. The two new ones that we just created are ASP net web one and ASP net DB one. The first one is obviously the web server and the second one is the database server. I need to copy that SQL script into my database server. We can do that with Docker CP.
>
> **[3:08](https://www.linkedin.com/learning/database-foundations-application-development/add-a-database-to-sql-server?u=76281980&t=188)** Then I need the file path to the SQL file. Let me minimize this here. And I'll find the two trees.SQL file and just drag it in. That'll put to the path right here. So we'll say Docker CP the file path to two trees.SQL. Then I want to copy that into my ASP net DB one server. I'm just going to put it at the root folder. So I'll say colon and then a forward slash. Press enter and that'll copy the file into the container. Next, we need to login to the container. We do that with Docker EXEC-IT, the container name ASP net DB one And I'll use the bash shell. That puts me into the container at the root folder. And now I need to run SQL command in order to execute the SQL script on the database server. The SQL command tool can be found at /opt/MSSQL-tools/bin/SQL CMD.
>
> **[4:20](https://www.linkedin.com/learning/database-foundations-application-development/add-a-database-to-sql-server?u=76281980&t=260)** So that locates the tool. Then I just need a few parameters.
>
> **[4:23](https://www.linkedin.com/learning/database-foundations-application-development/add-a-database-to-sql-server?u=76281980&t=263)** - U will specify the user account SA,
>
> **[4:27](https://www.linkedin.com/learning/database-foundations-application-development/add-a-database-to-sql-server?u=76281980&t=267)** - P we'll type in the password. Adam, 123456 with a capital A. Then -I will import a script file. And the file that I want to import is at the root folder. So forward slash and it's two trees.SQL. When you import a script file, it'll automatically run on the SQL server instance. So when I press enter, I get the message that the database context was changed to two trees and that six rows were affected. That's the insert of the data into the customer table. At this point, we're done importing our data. So I can just say exit. That'll remove me from the Docker container. And it puts me back at my root folder. At this point, we're done with this PowerShell window. We'll go ahead and close it down. Now, if you want to verify that the database was in fact created, we can go back into Azure data studio. I still have the server connection here. Let's go ahead and open up the [Databases](../../Skills/Software%20Development/Databases.md) folder. You might need you right click on it and choose refresh. But, there is the two trees database. There is the customer table and I'll right click and choose select top 1000 to see the data. So there's all of our customers that we entered into the table. Now that our database is in place we can return back to our web browser. And I'll refresh this page. It should then successfully make a connection into the SQL server database and reveal my customer data here inside of the web browser.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [SQL](../../Skills/Data%20Science/SQL.md) (11), [Sql server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) (5), [Azure](../../Skills/Cloud%20Computing/Microsoft%20Azure.md) (4), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (3), [PHP](../../Skills/Software%20Development/PHP.md) (2)
> **Env Vars:** sql (17), asp (4), php (2), exec (1), mssql (1)
> **CLI Commands:** docker (7), find (2), php (2), cp (2), make (1)
> **Tools:** command line (3), powershell (3), bash (1)
> **File Paths:** trees.sql (4)
> **UI Navigation:** click on (3), scroll down (1)
> **Speakers:** - we (1), - u (1), - p (1)
> **URLs:** [asp.net](https://asp.net) (1)

#### [ASP.NET modularity](https://www.linkedin.com/learning/database-foundations-application-development/asp-dot-net-modularity?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/asp-dot-net-modularity?u=76281980&t=0)** - [Narrator] We can get a feel for the modularity of an [ASP.NET](https://ASP.NET) Core application by taking a look at a view object. Let's click back to the homepage of our application by clicking on the home link here in the navigation bar. Now, this page has a number of distinct sections. First at the top, we have the page title, which on my computer reads Home Page-Two Trees. At the top of the page, we have a navigation bar with links to the other views of the site. The main portion of the screen includes the welcome line, a message that the server is running, as well as a link to the documentation. At the bottom, we have a common footer that, like the navigation bar at the top, will appear on every page. As we navigate to the different pages of the site, you'll notice that the navigation bar at the top and the footer at the bottom stay the same. The only content that's actually changing when you move from one page to the other is the central portion of the screen and the title that appears up here. With that in mind, let's dig into the project files. I'm going to minimize my browser and go into the exercise files chapter three folder and then go into the [ASP.NET](https://ASP.NET) folder. Here, we have all of the files that combine to make up the site. You can see that there's a lot of them if you start perusing the different folders. The three main folders that we have are the controllers, the models, and the views. Let's go into the views folder and then into the home folder. Here, we'll find an index file. This is the initial page that's shown
>
> **[1:34](https://www.linkedin.com/learning/database-foundations-application-development/asp-dot-net-modularity?u=76281980&t=94)** when we open the site in the browser. Let me right-click and open that in my code editor.
>
> **[1:45](https://www.linkedin.com/learning/database-foundations-application-development/asp-dot-net-modularity?u=76281980&t=105)** Now, you might be surprised at how short it is. At the top, we have a custom title for the page that just reads Home Page. Then we have a few lines that control the central portion of the screen. We have the line for the welcome message, the line that tells me that the server is running, and the line with the link to the documentation. And that's all we have in this file. There's no information about the navigation bar that appears at the top of the page or the footer that appears at the bottom. That's because those elements are shared across all pages in the site. In keeping with the write once use everywhere philosophy, those elements are defined separately and then combined together when the page is accessed by the user. So let's close our text editor here and go back into the views folder. And this time, we'll go into the shared folder. Here, we'll find a file called _Layout. I'm going to right-click on that and open that in the code editor. This file contains all of the common elements. At the top of the page, we have the normal [HTML](../../Skills/Web%20Development/HTML.md) opening tags including the head information. Notice that on this line number six that we have the title, but it's incomplete. It just has the text dash Two Trees. The full title is constructed from the current view's title data, which is defined in the other document. In the body section of the page, we have all of the information that sets up the navigation bar. It's all of these lines here starting on line 11 and coming down towards line 30. Let me scroll down on this page a little bit
>
> **[3:19](https://www.linkedin.com/learning/database-foundations-application-development/asp-dot-net-modularity?u=76281980&t=199)** until I get to line 34. Here, we find this function called RenderBody. This will get replaced with a code from the current view. Finally, at the bottom, we have all of the lines that define the common page footer. So these are all of the pieces for our initial view. Let's go back into the browser. The homepage that we're seeing is made up of code from multiple files on the server. Now, most browsers have an option for viewing the source code in the webpage so let's see what that says. In the Edge browser that I'm using, I can right-click and choose View Page Source. This is the code that my browser actually receives from the web server. On line number six, you'll see that we get the full page title. It says Home Page-Two Trees. We now know that this Home Page portion is the title coming from the index file and the Two Trees portion is defined in the layout file. We then have all of the navigation bar lines. Starting on line 36, we have the three pieces of text coming out of the index file: the welcome message, the message that the server is running, and the link to the documentation. Then we go back to the layout file to get the footer details at the bottom. In the browser, it looks like we have a single complete document, but we know by inspecting the source files that the [ASP.NET](https://ASP.NET) server is piecing everything together from information split across multiple files. So this should give you a taste
>
> **[4:50](https://www.linkedin.com/learning/database-foundations-application-development/asp-dot-net-modularity?u=76281980&t=290)** for how an [ASP.NET](https://ASP.NET) server works and why our project has so many files and folders for what appears to be a simple site. Each file holds a single component of the site: views for presentation, controllers for logic, and models for data. And then the server combines the pieces together whenever they're needed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [ASP.NET](../../Skills/Web%20Development/ASP.NET.md) (3), [ASP.NET Core](../../Skills/Web%20Development/ASP.NET%20Core.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** asp (4), net (4), html (1)
> **UI Navigation:** right-click (3), navigate to (1), open the (1), scroll down (1)
> **CLI Commands:** find (3), make (1)
> **URLs:** [asp.net](https://asp.net) (4)
> **Documentation:** the documentation (3)
> **Exercise Files:** exercise files (1), source code (1)
> **Cross-References:** go back to (1)

#### [Model tabular data](https://www.linkedin.com/learning/database-foundations-application-development/model-tabular-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/model-tabular-data?u=76281980&t=0)** - [Instructor] The modular design of an [asp.net](https://asp.net) site also applies to the database connection, and it controls how data is used throughout the entire site. To see how this works, we need to dig into the project files again. Now a [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) of warning before we get started, this is going to be a bit of a journey through a number of different files. To simplify things, I'm going to open the entire aspnet folder inside of Visual Studio. This will allow me to open and explore the different files without having to go back and forth to the desktop. On the left-hand side of the screen, I get this explorer panel, and I can expand the aspnet folder, and see all the different folders and files that the project contains. Now your text editor may or may not support this kind of project tree view. If it doesn't, that's fine. You can open up each file one at a time. Okay, I'm going to start by closing the Welcome tab. And then I'm going to find the appsettings.[JSON](../../Skills/Web%20Development/JSON.md) file that's in the root aspnet folder. In this file on line 11 is where our initial database connection string is stored. The server we're connecting to is named Db. You might also use an IP address here. This specific name Db is coming from our Docker network. If you open up the Docker-[compose](../../Glossary/Framework/Jetpack%20Compose.md).yaml file, you'll recall that we created two services, one for web and one for Db. So this is where the server name is coming from there
>
> **[1:34](https://www.linkedin.com/learning/database-foundations-application-development/model-tabular-data?u=76281980&t=94)** that we're using in our project. Back in the appsettings.json file, we can read the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the connection string. So we're connecting to the Db server. We're connecting to the database TwoTrees using the user account sa with a password Adam123456. We also have another parameter over here on the far right that just says MultipleActiveResultSets=true. All of this makes up the server connection string. And it's stored as AppDbContext. Next, we need to go to another file. Let's look at the Startup.cs file. It's also in the root aspnet folder. Down on lines 26 and 27 is what we want to look at. This is where we use the connection string. Here, various services are added to the application. We add a database context using the SqlServer connector, as opposed to some other database platform. This is where the connection string AppDbContext is used. So now this creates a connection to the database server, and the TwoTrees database. But we don't yet know anything about the tables that the database contains. For that, we need to go to another file. Let's open up the Data folder. Inside of there, we'll find ApplicationDbContext.cs. This is a class file, and here it outlines what tables are available inside of our database connection. Our database context
>
> **[3:07](https://www.linkedin.com/learning/database-foundations-application-development/model-tabular-data?u=76281980&t=187)** contains just a single table called Customer. On line 14 we're identifying the table, and registering it as an available data set. Okay, now we need to tell the application the columns that this table contains. If you guessed that we're going to another file, you're absolutely correct. Let's head into the Models folder. Here, you'll find a model for the customer table. Let's open that up. In the Customer.cs file, we finally get to a full description of every column in the customer table. The public string for each column is the name of the column as it exists in the [SQL Server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) database. So we have column names customer_id, company, address, city. And if I scroll down we have state and zip. The display name property of each column is how it's presented in any views that reference these data columns. So on the web application, you won't see customer_id, in all lowercase letters, you'll see Customer ID with proper capitalization. We also have some constraints on the data that'll help the application determine if a user is entering valid information when entering or editing values in the web app. In the zip code, for example, it has a maximum length of five characters, and a minimum length of five characters. In other words, the zip code must be exactly five characters long. When a user is entering data through the web form,
>
> **[4:40](https://www.linkedin.com/learning/database-foundations-application-development/model-tabular-data?u=76281980&t=280)** the [asp.net](https://asp.net) server will be able to validate that data without having to send an insert query to the database, and wait for a potential error message to come back if the submitted value doesn't fit the requirements. Other columns have a regular expression pattern that the data must match in order to be validated. For example, the Customer ID must have two capital letters followed by three digits. This helps encode business rules in the logic of the application. We can test this out by going back into our browser. From the main page at localhost:8000, click on the Customer's link. This displays all of the rows from the customer's table In our SQL Server database. You can click the Edit link on the right side of any of these rows. We'll just click on the Edit link for Flavorville. Now we can try and update the data that's in the SQL Server database. Let's come down here to the zip code, and you'll notice if I try and type in additional digits, it won't let you type in more than five. If I try and type in less than five digits, I'll just make this say 503, then click to a different text box, I'll immediately get an error message. This tells me that the zip code must be five characters long. If we try and edit the Customer ID, let's change it to 12345, and click off of it, you'll get a different error message. This tells me that our value doesn't match the required pattern. Okay, I'll admit that this error isn't very clear,
>
> **[6:14](https://www.linkedin.com/learning/database-foundations-application-development/model-tabular-data?u=76281980&t=374)** and it's not going to make much sense to most users, but you get the idea. The [Data Validation](../../Skills/Software%20Development/Data%20Validation.md) is handled immediately by the application right here in the browser before we even get to the point where we're trying to update the database. Because these types of data constraints are defined in the site's data model, we can be more proactive in helping end users enter valid information. And because these kinds of settings are defined a single time in the application, they will apply everywhere the data model is used throughout the entire site.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Sql server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) (3), [ASP.NET](../../Skills/Web%20Development/ASP.NET.md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (2), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1)
> **CLI Commands:** find (3), docker (2), make (2)
> **UI Navigation:** go to (2), click on (2), open the (1), scroll down (1)
> **File Paths:** appsettings.json (2), docker-compose.yaml (1)
> **URLs:** [asp.net](https://asp.net) (2), [localhost:8000](https://localhost:8000) (1)
> **Env Vars:** sql (3)
> **Code Identifiers:** customer_id (2)
> **Definitions:** is a  (1), in other words (1)

#### [Controller classes](https://www.linkedin.com/learning/database-foundations-application-development/controller-classes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/controller-classes?u=76281980&t=0)** - [Instructor] We've talked about views and we've explored the apps data model. Now let's turn our attention to the controller class. That controller tells the database what to do and it creates the bridge between the user interface presented by the views and the data model. In our app, there are five different views that are used to work with the customer data from the two trees database. The main page that we're looking at right now lists out all of the rows that the table contains. This is the index or the default page in the customer view. There's an option on this page to filter the results by a two letter state abbreviation. Let's type in the abbreviation oh for Ohio and press the Filter button. This sends a query to the database using a where class and we see just the two rows from the state of Ohio. To get back to the full list, just press the Filter button again with no input. At the top of the page, there's a link to create a new row in the table. This opens up the create view. The create view includes all the form fields so that the end user can fill in their new values and then press the Create button at the bottom to send the data to the database. Let's press the Back to List button. The third view is for editing a row and we have links for that on every row in the index view, let's go ahead and click on one of them and we'll see what that view looks like. This one is set up identically to the create view. We have the same text boxes, the only difference is that we have a Save button at the bottom instead of Create.
>
> **[1:36](https://www.linkedin.com/learning/database-foundations-application-development/controller-classes?u=76281980&t=96)** We'll go Back to List and we'll take a look at the details view. This view presents the single row of data in a slightly different arrangement. We'll go back to List and we'll check out the last one, the Delete view. This view is arranged like the details screen but there's a delete button that's been added. So those are the five views that we have for working with our data. The index, create, edit, delete, and details views. Let's go into the project files now and see how the connections are made from these five views to the database through the controller. I have the ASP net folder open inside of my code editor, then I'll go into the Views folder, we'll find the folder for customers and here are the five views, create, delete, details, edit and index. Let's start with the index file. The index page displays all of the tabular data. Starting down online number 20, we start building an [HTML](../../Skills/Web%20Development/HTML.md) table. This is very similar to what we did when formatting data in the [PHP](../../Skills/Software%20Development/PHP.md) project. The first row of the table is the header row. This is where we'll list out all the column names. The text that's shown is the display name for each column. We saw where that was coming from in the previous movie. If you go into the models folder and open up the customer model, you'll see that every column in the data model has a unique display name. This is where we're getting
>
> **[3:07](https://www.linkedin.com/learning/database-foundations-application-development/controller-classes?u=76281980&t=187)** the really nicely formatted display names in the table results. Let's close this down and go back to the index file. After the header file down on line number 45, we start a loop. In PHP, we use the four-loop here inside of [asp.net](https://asp.net), it's a foreach loop. This will go through and read each row from the database and create a new HTML table row every time the program goes through the loop. Again, this is exactly the same process that we took with PHP. So that's the index view. It only needs to read information out of the database. So there's no opportunity to send data to the database with this view. The create view on the other hand is kind of the opposite. Let's open that one up. Here, we don't need to read any data out of the database first, we only send values when we enter in a new row. The page is set up as an HTML form, starting online 13. The form we'll use the create action. Each column in the table has its own label that's coming from the column name there as well as an input text box. There's also a [Data Validation](../../Skills/Software%20Development/Data%20Validation.md) line to display the red validation text. Once again, this is very similar to what we saw in the PHP example when working with form controls. At the bottom of this page, all the way down, on line 46, we have the submit button. This will post all of the values that the user entered back to the [asp.net](https://asp.net) server using the create action
>
> **[4:42](https://www.linkedin.com/learning/database-foundations-application-development/controller-classes?u=76281980&t=282)** that was specified back up on line 13. Now there's no code here that tells the server what to do with those values. That's where the controller comes in. In the controller's folder, you'll find the customer's controller file. Let's open that one up. This lists out all of the different things that you can do with the customer data model. This code is written in the C sharp programming language. So I don't expect you to have any familiarity with it but we can still read through portions of it and look for some key words to help understand how it's working. Line 22 starts with a bit of code that says what to do on the index view. Let's scroll down to it. Here is where it's displaying the data from the database for the index view and using the state name in a Whereclass, if one is provided. Since data is only coming out of the database for this view, it's called a GET. There is no post for the index view since we can't use it to send data back to the database. On line number 60, we have the code that defines what to do in the create view. When creating a new row, we don't need to get any information from the database. So line 63 just says, return the view there's nothing else that it needs to do. What we can do on the create view though, is post data to the database. So on line number 66, we start that bit of code. When posting data to the database, it takes all of these supplied values. First checks, if they're valid
>
> **[6:16](https://www.linkedin.com/learning/database-foundations-application-development/controller-classes?u=76281980&t=376)** and then adds them into the customer table. Now we didn't look at the edit view but we can see what that controller does starting on line number 82. It makes sense that the edit view would need to get information from the database first to display all of the original values. So there's a section for getting those values using a specific integer id value. This will pull the details that we're going to edit for the specific row of data. Then after the user makes changes, those new values need to get posted back to the database. And we see that starting on line number 98. Here is where we take all of those inputs and send it back up to the database. So that's a good overview of how the controller class is organized for the two trees customer table. If you want to dig a little bit further, you can find the code for the delete view starting on line 133 and the code for the details view back up on line 42. This one file holds all of the instructions for passing information between the user interface views and the data model.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [PHP](../../Skills/Software%20Development/PHP.md) (4), [HTML](../../Skills/Web%20Development/HTML.md) (3), [ASP.NET](../../Skills/Web%20Development/ASP.NET.md) (2), [Data Validation](../../Skills/Software%20Development/Data%20Validation.md) (1)
> **Env Vars:** php (4), html (3), asp (1)
> **CLI Commands:** php (4), find (3)
> **Cross-References:** go back to (3)
> **URLs:** [asp.net](https://asp.net) (2)
> **UI Navigation:** click on (1), scroll down (1)
> **Prerequisites:** set up (2)
> **Analogies:** similar to (1)

#### [URL segments](https://www.linkedin.com/learning/database-foundations-application-development/url-segments?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/url-segments?u=76281980&t=0)** - [Instructor] One of the neat things about an [asp.net](https://asp.net) web application is that the controller classes encode useful information right into the URL segments of the page. From the Two Trees Customer Data page, click on any of the edit links. The URL in the browser will show Customers/Edit/ and then an ID number of the row that you chose. You can easily navigate to the edit page for other rows in the table simply by changing this ID number here in the address bar. Let me change it to Customers/Edit/6. I'll press enter and that takes me to the edit page for Delish Food. You can also use this method to get to other views. For instance, I'll change it to Customers/Details/3. This takes me to the Customer Detail page for Bread Express. This is great because end users can bookmark and navigate directly to a specific action for any record in the table. This also works for our filter back on the index page. Let's click the back to list link, then I'll filter our list to OH for all of the rows in the state of Ohio. You'll notice that the URL bar now says customers?statename=oh. We can change this to statename=pa for Pennsylvania. This reveals the one customer from the state of Pennsylvania. Now we could bookmark this page
>
> **[1:33](https://www.linkedin.com/learning/database-foundations-application-development/url-segments?u=76281980&t=93)** if we are specifically interested in this filtered list of customers. This mapping of URL segments to the controller class is handled by one of the files in the project. Let's take a look at it. In the aspnet folder, you're going to find a file called Startup.cs. Let's open that up in our text editor. Way down at the bottom on line number 52, you'll find the line that's doing all the work. This shows us the endpoint mappings. The default pattern that we're using shows the controller that's used and it uses the home controller if none is specified, then we have a slash followed by the action that it's going to use. If no action is applied, it'll use the default index action. Finally, we have another slash followed by an ID number for a specific data table row. This means that the URL, Customers/Details/3, mapped to the customer's controller, the details action and the row number three. So that wraps up our exploration of how an [asp.net](https://asp.net) core site works with a [SQL server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) database. When you're done exploring the site and all of the different activities, you can go back into your open [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) window or terminal window and press the control+C keyboard shortcut to shut down all of the servers. You can always start them back up again by coming to the aspnet folder and running docker/[compose](../../Glossary/Framework/Jetpack%20Compose.md) up. It goes without saying that a full exploration of [asp.net](https://asp.net)
>
> **[3:09](https://www.linkedin.com/learning/database-foundations-application-development/url-segments?u=76281980&t=189)** would require some additional background in the C sharp programming language and the Visual Studio IDE. But I hope this has given you some insight into how [Databases](../../Skills/Software%20Development/Databases.md) function in an online web app and gives you some ideas about where you could take your database skills next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [ASP.NET](../../Skills/Web%20Development/ASP.NET.md) (2), [ASP.NET Core](../../Skills/Web%20Development/ASP.NET%20Core.md) (1), [Sql server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) (1), [Powershell](../../Skills/Network%20and%20System%20Administration/Powershell.md) (1), [Compose](../../Glossary/Framework/Jetpack%20Compose.md) (1)
> **Env Vars:** url (5), sql (1), ide (1)
> **CLI Commands:** find (2), docker (1)
> **URLs:** [asp.net](https://asp.net) (3)
> **Tools:** powershell (1), terminal (1), visual studio (1)
> **UI Navigation:** click on (1), navigate to (1)
> **Definitions:** means that (1)
> **Analogies:** for instance (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/database-foundations-application-development/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/database-foundations-application-development/next-steps?u=76281980&t=0)** - [Instructor] Congratulations on reaching the end of this course. And if you joined me through all five courses in this series, then you deserve an extra special round of applause. You've come a long way from your first steps in the world of [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md), all the way up to this introduction to [Application Development](../../Skills/Software%20Development/Application%20Development.md) and integrating [Databases](../../Skills/Software%20Development/Databases.md) with other related technologies. You should now have the confidence to dive straight in to any relational database platform that you want to work with. If this course has piqued your interest in expanding your skills into app development, then I'd encourage you to check out some of the introductory content in the languages that we explored. Learning [PHP](../../Skills/Software%20Development/PHP.md) and [ASP.NET](https://ASP.NET) MVC 5 Essential Training are two great courses to get started with, or if you're feeling ambitious consider tackling the learning path Become a Programmer: Foundations. Once again, I'm Adam Wilbert. I'm glad you've taken this journey with me and I look forward to our next encounter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md) (1), [Application Development](../../Skills/Software%20Development/Application%20Development.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1), [PHP](../../Skills/Software%20Development/PHP.md) (1), [ASP.NET MVC](../../Skills/Web%20Development/ASP.NET%20MVC.md) (1)
> **Env Vars:** php (1), asp (1), net (1), mvc (1)
> **CLI Commands:** php (1)
> **URLs:** [asp.net](https://asp.net) (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [Adam Wilbert](../../Instructors/Database%20Management/Adam%20Wilbert.md)

## Resources

- Exercise files available

## Skills Covered

- Database Development
- Database Applications

## Path Context

### In [Database Foundations- From Concepts to Applications](../../Paths/Data%20Science/Learning%20Paths/Database%20Foundations-%20From%20Concepts%20to%20Applications.md)
← [Database Foundations- Administration](Database%20Foundations-%20Administration.md) | **5 of 5**

### In [Advance Your Database Administration Skills](../../Paths/Data%20Science/Learning%20Paths/Advance%20Your%20Database%20Administration%20Skills.md)
← [Database Foundations- Administration](Database%20Foundations-%20Administration.md) | **5 of 11** | [SQL Server Fundamentals- Master Basic Query Techniques](SQL%20Server%20Fundamentals-%20Master%20Basic%20Query%20Techniques.md) →

## Appears In

- [Database Foundations- From Concepts to Applications](../../Paths/Data%20Science/Learning%20Paths/Database%20Foundations-%20From%20Concepts%20to%20Applications.md)
- [Advance Your Database Administration Skills](../../Paths/Data%20Science/Learning%20Paths/Advance%20Your%20Database%20Administration%20Skills.md)

## Related Courses

_Courses sharing skills:_

- [Advanced Python- Practical Database Examples](../Software%20Development/Advanced%20Python-%20Practical%20Database%20Examples.md) — Database Development
- [Introduction to Couchbase](../Database%20Management/Introduction%20to%20Couchbase.md) — Database Development
- [Vector Databases in Practice- Deep Dive](../Artificial%20Intelligence%20(AI)/Vector%20Databases%20in%20Practice-%20Deep%20Dive.md) — Database Development
- [Databases for Node.js Developers](../Web%20Development/Databases%20for%20Node.js%20Developers.md) — Database Development
- [Programming Foundations Databases](../Software%20Development/Programming%20Foundations%20Databases.md) — Database Development

---

[↑ Back to top](#)