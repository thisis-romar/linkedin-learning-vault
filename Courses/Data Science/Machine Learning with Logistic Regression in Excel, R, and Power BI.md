---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: machine-learning-with-logistic-regression-in-excel-r-and-power-bi
url: "https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi"
duration_minutes: 169
duration: 2h 49m
level: Intermediate
updated: 11/5/2021
learners: 52410
skills:
  - Machine Learning
  - R (Programming Language)
  - Artificial Intelligence (AI)
  - Microsoft Power BI
  - Microsoft Excel
  - Logistic Regression
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQF32uee6WTXTA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1635973062525?e=2147483647&amp;v=beta&amp;t=OfQbNnodDZ0te3kzmywU4sQDH-OGQ3UP1HA2_tdenJ4"
linkedin_topic: Data Science
learning_paths:
  - '[Master Advanced Excel Data & Analytics Skills](../../Paths/Data%20Science/Learning%20Paths/Master%20Advanced%20Excel%20Data%20%26%20Analytics%20Skills.md)'
prev_courses:
  - '[From Excel to SQL](From%20Excel%20to%20SQL.md)'
next_courses:
  - '[Excel- Economic Analysis and Data Analytics](Excel-%20Economic%20Analysis%20and%20Data%20Analytics.md)'
path_nav: '[{"path":"Master Advanced Excel Data & Analytics Skills","position":6,"total":7,"prev":"From Excel to SQL","next":"Excel- Economic Analysis and Data Analytics"}]'
path_count: 1
tags:
  - course
  - topic/data-science
  - skill/machine-learning
  - skill/r-programming-language
  - skill/artificial-intelligence-ai
  - skill/microsoft-power-bi
  - skill/microsoft-excel
  - skill/logistic-regression
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Data%20Science/Machine%20Learning%20with%20Logistic%20Regression%20in%20Excel%2C%20R%2C%20and%20Power%20BI.md)

![Machine Learning with Logistic Regression in Excel, R, and Power BI](https://media.licdn.com/dms/image/v2/C560DAQF32uee6WTXTA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1635973062525?e=2147483647&amp;v=beta&amp;t=OfQbNnodDZ0te3kzmywU4sQDH-OGQ3UP1HA2_tdenJ4)

# Machine Learning with Logistic Regression in Excel, R, and Power BI

> Excel, R, and Power BI are applications universally used in data science and across businesses and organizations around the world. If you’ve spent any time trying to figure out how to better model your data to get useful insights from it that you can act upon, you’ve most likely encountered these applications. In this course, Helen Wall shows how to use Excel, R, and Power BI for logistic regressi

> [LinkedIn Learning](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi) | 2h 49m | Intermediate | 52K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (6 videos)
  - [Apply logistic regressions to solve problems](#apply-logistic-regressions-to-solve-problems)
  - [What you should know](#what-you-should-know)
  - [Introduction to the course project](#introduction-to-the-course-project)
  - [Configuring the Excel Solver Add-in](#configuring-the-excel-solver-add-in)
  - [Working with R](#working-with-r)
  - [Configuring R in Power BI](#configuring-r-in-power-bi)
- [**1. Distributions and Probabilities**](#1-distributions-and-probabilities) (6 videos)
  - [Introducing AI and logistic regression](#introducing-ai-and-logistic-regression)
  - [Differentiating between odds and probabilities](#differentiating-between-odds-and-probabilities)
  - [Differentiating between distributions](#differentiating-between-distributions)
  - [Calculating logs and exponents](#calculating-logs-and-exponents)
  - [Sigmoid curve](#sigmoid-curve)
  - [Utilizing training and testing data sets](#utilizing-training-and-testing-data-sets)
- [**2. Binomial Logistic Regression**](#2-binomial-logistic-regression) (9 videos)
  - [Calculating linear regression](#calculating-linear-regression)
  - [Working with the logit model](#working-with-the-logit-model)
  - [Calculating log likelihood](#calculating-log-likelihood)
  - [Constructing MLE](#constructing-mle)
  - [Solving MLE](#solving-mle)
  - [Predicting outcomes](#predicting-outcomes)
  - [Visualizing logistic regression](#visualizing-logistic-regression)
  - [Challenge: Calculating logistic regression](#challenge-calculating-logistic-regression)
  - [Solution: Calculating logistic regression](#solution-calculating-logistic-regression)
- [**3. Fine-Tuning the Model**](#3-fine-tuning-the-model) (7 videos)
  - [Adding more independent variables](#adding-more-independent-variables)
  - [Transforming variables](#transforming-variables)
  - [Calculating correlations](#calculating-correlations)
  - [Using statistics](#using-statistics)
  - [Configuring confusion tables](#configuring-confusion-tables)
  - [Challenge: Fine-tuning the model](#challenge-fine-tuning-the-model)
  - [Solution: Fine-tuning the model](#solution-fine-tuning-the-model)
- [**4. Multinomial Regression**](#4-multinomial-regression) (5 videos)
  - [Calculating odds for multinomial models](#calculating-odds-for-multinomial-models)
  - [Calculating probabilities for multinomial models](#calculating-probabilities-for-multinomial-models)
  - [Calculating multinomial log likelihoods](#calculating-multinomial-log-likelihoods)
  - [Running MLE](#running-mle)
  - [Making the predictions](#making-the-predictions)
- [**5. Working in Power BI with R**](#5-working-in-power-bi-with-r) (5 videos)
  - [Running R scripts in the Power Query Editor](#running-r-scripts-in-the-power-query-editor)
  - [Running R standard visuals](#running-r-standard-visuals)
  - [Interacting between visual components](#interacting-between-visual-components)
  - [Challenge: Moving into Power BI](#challenge-moving-into-power-bi)
  - [Solution: Moving into Power BI](#solution-moving-into-power-bi)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps with logistic regressions](#next-steps-with-logistic-regressions)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Apply logistic regressions to solve problems](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/apply-logistic-regressions-to-solve-problems?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/apply-logistic-regressions-to-solve-problems?u=76281980&t=0)** - [Helen] You've probably spent a lot of time trying to figure out how to better model your data to get useful insights from it that you can act upon. You can take data with a lot of columns to figure out future predictions based on results in the past. We'll use [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) formulas, the Solver Add-in, and R to model [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) to visualize the model results. We can then use R and [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) to create scalable views that make this model accessible to a much wider group of end users. Hi, I'm Helen Wall and I'm excited to introduce you to one type of algorithm, logistic regression, that you've likely seen modeled in your own data without even necessarily realizing it. Logistic regression models are useful for games, medical research, and fraud detection, and much more. You'll see how to transform the way you view data now into a model that's useful, just by starting to think about the distance and dimensionality of these models. Come join me in this [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) learning course on logistic regression in Excel, R, and Power BI.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (4), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (2), [Power bi](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** make (1)
> **Speakers:** - [helen] (1)

#### [What you should know](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/what-you-should-know?u=76281980&t=0)** - [Instructor] Before you start this course, you should be comfortable working in [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md). You should also be comfortable working with tabular data. I also recommend knowing a bit about [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md), if you're interested in implementing [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) models directly in the application. Logistic regression involves some math. We'll be solving for the variables in a model by using [Calculus](../../Skills/Data%20Science/Calculus.md) to find minimums and maximums. [Linear Algebra](../../Skills/Artificial%20Intelligence%20(AI)/Linear%20Algebra.md) is absolutely not required, but you'll find the concepts in this course more tangible. You don't need to know much about R, but be eager to learn more. If you want to get up to speed on these applications, please check out Excel Essential Training, and [Power Bi Essential Training](Power%20Bi%20Essential%20Training.md) in the library.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (2), [Power bi](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (2), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (1), [Calculus](../../Skills/Data%20Science/Calculus.md) (1), [Linear Algebra](../../Skills/Artificial%20Intelligence%20(AI)/Linear%20Algebra.md) (1)
> **CLI Commands:** find (2)
> **Speakers:** - [instructor] (1)

#### [Introduction to the course project](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/introduction-to-the-course-project?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/introduction-to-the-course-project?u=76281980&t=0)** - [Instructor] In our course project, we're going to work in three different applications, [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md), R, and [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md). In many videos, we'll be working in two of the application simultaneously, to see how they can produce the same results through different steps and processes. For the project in this course, we'll be leveraging college basketball data, which I got from the ESPN website. I took college basketball data for the University of Washington over the last decade. You can see the schedule for the most recent season as of the recording date on the screen here. But you can also navigate to earlier seasons through the dropdown menu under the season header. We can see the results of each game. Let's navigate to an earlier season. We can see the results of each game on this page, but if you want to see the box score, you'll need to click on one of the hyperlinks for the game. This takes us to the game details. We land on the game cast page, but that doesn't contain all the data that we're looking for. Instead, we'll navigate to the box score tab. The box score page displays the details for the game, including the breakdown of stats for the starting players. But for our model, we're interested in the totals for the entire team, which you can see at the bottom of the page. I ran a script to get the data for you, which I saved to a CSV file and the exercise files for those of you with access.
>
> **[1:35](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/introduction-to-the-course-project?u=76281980&t=95)** I encourage you to explore some of the other data on the ESPN website or other websites. If sports data is an area that interests you.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1), [Power bi](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (1)
> **UI Navigation:** navigate to (3), dropdown (1), click on (1)
> **Env Vars:** espn (2), csv (1)
> **Exercise Files:** exercise files (1)
> **Definitions:** is an  (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Configuring the Excel Solver Add-in](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-the-excel-solver-add-in?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-the-excel-solver-add-in?u=76281980&t=0)** - We're going to use [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) to do a substantial part of our [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) modeling. In addition to having Excel on your own computer, you'll also need to install the Solver add-in directly, as well as the data analysis tool pack to run the logistic regression models and analysis within this course. In my own current version of Excel, I access the add-ins through the file menu, and I'll then select options. On the left, we see the add-ins, we select the add-in menu, we see there's a list of add-ins that you can choose to add to Excel. If you already have add-ins in your Excel file, you'll see them in the active section. We don't have any active right now, so we'll select the manage Excel add-in to the bottom. We'll click the go button. We'll then select to add the analysis tool pack and the Solver add-in, and we'll hit okay to confirm these choices. Now we can access the Solver and the data analysis by going to the data ribbon, and over in the top right, we see in the analyze sub-menu options for data analysis and Solver. Solver's a pretty straightforward process, but there may be variations between past versions and also potential future versions. Your version of Excel may look slightly different from mine. If yours does look different and these steps don't work, your best bet is to do an online search
>
> **[1:33](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-the-excel-solver-add-in?u=76281980&t=93)** for the Excel Solver add-in documentation page. [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) includes documentation for our Solver in several places on their website. Once you install them, they'll also appear in all your new and existing Excel files.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (9), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **UI Navigation:** select the (2)
> **Prerequisites:** install (2)
> **Speakers:** - we (1)

#### [Working with R](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/working-with-r?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/working-with-r?u=76281980&t=0)** - [Instructor] In this course we're going to leverage R as part of our [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) modeling process. R is a statistical programming language that lets us run models and create insightful graphics. You can see the Homepage for the R language on the screen. We're going to write our scripts for R directly in [RStudio](../../Skills/Software%20Development/RStudio.md), which is an integrated development environment or IDE for writing and checking code. We can download RStudio directly from this website by selecting the Download link. I'll scroll down a bit, we can see several download options to choose from both Free and Paid for this course we can leverage RStudio Desktop, the first option on the left, for our R modeling, which is a free download. We can simply click on the Download button on the screen, to start the download process. RStudio is a great way to write and test code, but in order to run R directly in [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) desktop models, we need to install it on our computer. If you're planning on uploading your work to a Power BI cloud account, like Power BI Pro, or Power BI Premium, I recommend checking the version currently used from the [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) Documentation you see on the screen. If we scroll down this page, we see that as of the recording date for this course, the Power BI version used for models uploaded to the cloud accounts is version 3.4.4 for R. You can use the same version to follow along, but you can also use a newer version,
>
> **[1:34](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/working-with-r?u=76281980&t=94)** if you check the documentation page and this version number has been updated. If you use a newer version of R will often still work in Power BI, whether that's running the model itself or creating graphics. However some functionalities and libraries or packages may not work, so you want to be careful about using other versions just in case. It's important to note that you can have more than one version of R on your computer, you just need to specify the version for Power BI desktop or for RStudio to connect to. The latest version of R's easy to download from the main R page, but if you want to use older versions, we'll search for them or navigate to this webpage containing a list of the older versions to download on the screen. We can select to download 3.4.4 directly from this website because that's the version we saw that we can use with Power BI, when it's uploaded to the cloud. However, you can still use the latest version in this case 4.1.1, if you would like. For our R modeling we're mostly going to be using the built-in R functions, but we'll also use an external library like mlogit. There are a lot of R libraries available for public download, mlogit is just one of them, but we'll be using this for some of our regression modeling in this course. There are a few different ways to download R libraries to our computer. One way is through the RGui for this particular version of R you see on the screen.
>
> **[3:08](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/working-with-r?u=76281980&t=188)** Note that it can be any version of R, these R console window looks the same for all of them. Once we open the RGui, we can now run a command line, to install the package. We'll type in install.packages, and then we'll put the name of the library within the quotation marks in the screen. In this case, we'll type in "mlogit" close the quotation marks and close the bracket. It's key to type in the command exactly as we see it on the screen, even if we forget the quotation marks or we don't put the S on the end of packages, the R command line won't run. Once we type in this command, we hit Enter to run it. We'll then select our R current location, from the available locations listed, for the CRAN mirrors on the screen. My own closest location is Texas, so I'll select this from the list. You'll want to use the location closest to you though, once we hit OK we see the package installs automatically. Once the package properly installs, we'll see the notifications below to indicate the library has been installed successfully. One other technicality to note when you're using R, is that you'll want to specify the RStudio version the version of R specifically that you want to use directly in this IDE. Because Power BI uses an older version
>
> **[4:44](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/working-with-r?u=76281980&t=284)** of R then you might use in some of your models. If you run into issues with packages perhaps not working, you can change the R version directly in RStudio, by going to the Tools menu, then selecting the option at the bottom for Global Options. We can then change the version used by selecting the Change button to the right of the folder path. You want to select the third radio button down, and then you'll select the particular version you want to use let's say the 64 bit R version of 3.4.4. If you want to use another more recent version of R you can select that from the list as well. We'll confirm our options, and then we will scroll to the bottom of our Global Options menu, and hit OK, once you confirm the R package you wants to use you'll want to close out of RStudio and reopen it, to use the selected version that you just updated it to.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Power bi](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (9), [RStudio](../../Skills/Software%20Development/RStudio.md) (8), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (1), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **UI Navigation:** scroll down (2), select the (2), click on (1), open the (1)
> **Versions:** 3.4.4 (2), version 3 (1), 4.4 (1), 4.1.1 (1)
> **Definitions:** is a  (3), is an  (1)
> **Env Vars:** ide (2), cran (1)
> **Warnings:** note that (2), be careful (1)
> **Prerequisites:** install (3)
> **Tools:** command line (2)

#### [Configuring R in Power BI](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-r-in-power-bi?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-r-in-power-bi?u=76281980&t=0)** - In this course, we're going to leverage [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) to explore how to make [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) models more scalable and accessible. Power BI is a [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) [business intelligence](../../Skills/Data%20Science/Business%20Intelligence%20(BI).md) tool enabling a data driven culture across an organization. I personally think defining it as a communication tool, gets us thinking about how to maximize its impact across the end users. You can learn more about Power BI on Microsoft's homepage you see on the screen. Power BI Pro and also Premium are subscription services that enable us to share analysis with end users, as well as schedule automatic dataset refreshing. Power BI Desktop is a free version you can download to your own computer to develop and design data models and interactive visualizations. For this course we're using Power BI Pro account, to share work and refresh our data. You'll need an email address specific to your organization to sign up for any of these service accounts. But your organization may have passed to take care of this for you. On the Power BI webpage you see on the screen, you can see the available service accounts by scrolling down. We can see these options include Power BI Pro, Power BI Premium and Power BI Mobile. In this course we're mostly going to leverage Power BI Pro as well as, use Power BI Desktop to develop our models. If you don't already have Power BI Desktop
>
> **[1:33](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-r-in-power-bi?u=76281980&t=93)** installed on your computer, the link to download the most recent version is quite easy to find, on the Power BI section of the Microsoft website. Check the limitations of your computer to determine what version you should download, but it's a pretty straightforward process otherwise. You simply click on the link to download Power BI Desktop which is Try Power BI for free, and your computer will automatically start the download process. To use R directly in Power BI Desktop, you'll first need to install the version of R that works with the Power BI cloud accounts. You'll also need to install any libraries or packages necessary to run the model in R. In the video for installing R, in this chapter, you can see how to install the, the version of R we're using and also the libraries. Lastly, you'll need to enable R scripts directly in Power BI Desktop. Once you upload your Power BI Desktop file, denoted with the PBIX extension, the R scripts will run from the Power BI cloud account instead of your own computer. Once you've downloaded R in Power BI Desktop, you can enable scripts for R by going to the options menu in the file section and selecting options and settings, we'll then select options. We'll then select the R scripting option on the left side of the screen.
>
> **[3:06](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-r-in-power-bi?u=76281980&t=186)** We can choose the R directory that we downloaded the version, we want to use to our computer. You can set up a new R home directory by selecting browse and choosing the folder and file paths for your own R directory. Once you've selected this option, you want to confirm it, by selecting okay at the bottom of the screen. Once you select this option, you'll close out of Power BI Desktop completely and reopen it to enable R scripts to run within the application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Power bi](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (23), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (3), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (1), [Business intelligence](../../Skills/Data%20Science/Business%20Intelligence%20(BI).md) (1)
> **Prerequisites:** install (3), you'll need (2), set up (1)
> **CLI Commands:** make (1), find (1)
> **UI Navigation:** click on (1), select the (1)
> **Definitions:** is a  (2)
> **Env Vars:** pbix (1)
> **Exercise Files:** download the (1)
> **Speakers:** - in (1)


### 1. Distributions and Probabilities

[↑ Back to Table of Contents](#table-of-contents)

#### [Introducing AI and logistic regression](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/introducing-ai-and-logistic-regression?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/introducing-ai-and-logistic-regression?u=76281980&t=0)** - [Instructor] [Artificial intelligence](../../Topics/Artificial%20Intelligence%20(AI).md) or AI, as it's often known, uses computer [Algorithms](../../Skills/Software%20Development/Algorithms.md) to replicate tasks normally performed by humans. Examples of these tasks include activities like visual perception, [Speech Recognition](../../Skills/Software%20Development/Speech%20Recognition.md), and [Decision-Making](../../Skills/Data%20Science/Decision-Making.md). [Statistics](../../Skills/Data%20Science/Statistics.md) measures the relationship between input variables and known outcome labels for existing data. In AI algorithms, we want to predict the outcome labels for new data, using the statistics we calculated from the existing data. Machine learning is a type of AI algorithm that uses feedback loops to continuously update the model. Types of machine learning algorithms include classification, regression, clustering, and anomaly detection. Regression modeling lets us solve parameters representing the relationship between the inputs or independent variables and the outcome label, or dependent variable on existing data. We can then use these estimated parameters to predict or forecast outcome labels on new data. There are several different types of regression models, but two of the most common are linear and [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md). [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) shows the relationship between the independent input variable and the dependent variable for the outcome with a straight line, while logistic regression displays this relationship with an S shaped sigmoid curve. The outcome labels or dependent variable in linear regression are continuous numeric values.
>
> **[1:34](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/introducing-ai-and-logistic-regression?u=76281980&t=94)** The outcome labels and logistic regression are also numeric, but they're discrete instead of continuous, which means we'll see whole member predictions instead of decimals, for example. There are three main types of logistic regression, binomial, multinomial and ordinal. Binomial logistic regression is a classic linear log model with two outcome labels, zero and one, for example. Multinomial logistic regression also has discrete outcome labels, but instead of two outcomes, it has three or more potential outcomes, but the order of them doesn't matter. Ordinal logistic regression is another type of logistic regression model with three or more possible outcomes, but order matters in these models. Examples of real world problems that we can apply logistic regression frameworks to include credit scoring, medical testing, and gaming.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (9), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (3), [Statistics](../../Skills/Data%20Science/Statistics.md) (2), [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (2), [Artificial intelligence](../../Topics/Artificial%20Intelligence%20(AI).md) (1)
> **Definitions:** is a  (2)
> **Analogies:** for example (2)
> **Speakers:** - [instructor] (1)

#### [Differentiating between odds and probabilities](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/differentiating-between-odds-and-probabilities?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/differentiating-between-odds-and-probabilities?u=76281980&t=0)** - [Instructor] Sometimes we may use the terms [Statistics](../../Skills/Data%20Science/Statistics.md) and [Probability](../../Skills/Data%20Science/Probability.md) interchangeably under the wider umbrella of mathematics topics. But they're not actually the same. Statistics is when we collect, analyze, interpret, and present findings in our data. Conversely probability is the calculated numerical likelihood of an event occurring. They're not standalone entities though. Within statistics, we can use probability theory to draw conclusions from our data. Let's focus on a probability example. Let's say we have 10 marbles where one of them is orange and the remaining nine are blue. The formula for the probability of a certain event occurring is equal to the number of given outcomes we're trying to measure, divided by the total number of all outcomes. The probability in the marble is orange equals the total number of orange marbles divided by the total number of marbles. This equals 1 divided by 10, or a 10% chance the marble is orange. Conversely, the probability the marble is blue equals the number of blue marbles divided by the same total number of marbles. This equals 9 divided by 10 or a 90% chance the marble is blue. At first glance, odds are sometimes mixed up with probabilities, but they're calculated quite differently. The odds for a given outcome are the probability that a given event occurs divided by the probability this event does not occur. The odds for getting a blue marble equals the probability
>
> **[1:34](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/differentiating-between-odds-and-probabilities?u=76281980&t=94)** the marble is blue, divided by the probability the marble is not blue. In this case, the probability the marble isn't blue is the same as the probability it's orange, but that's not the case if we have more than two colors. The odds of getting a blue marble equal 0.9 divided by 0.1, which equals 9. This means we're 9 times more likely to draw a blue marble than an orange marble. Conversely the odds for drawing an orange marble equals the probability of drawing an orange marble, divided by the probability of drawing a blue marble. This equals 0.1 divided by 0.9 or a 1 out of 9. To convert odds to probability P, we divide the odds by 1 plus the odds. The formula for calculating odds from probability P, equals P divided by 1 minus P.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Probability](../../Skills/Data%20Science/Probability.md) (17), [Statistics](../../Skills/Data%20Science/Statistics.md) (3)
> **Versions:** 0.9 (2), 0.1 (2)
> **Speakers:** - [instructor] (1)

#### [Differentiating between distributions](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/differentiating-between-distributions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/differentiating-between-distributions?u=76281980&t=0)** - [Presenter] Let's say that we have data point observations and we're looking to analyze their distribution for a single field. One way we can do this is through a histogram or density chart. The chart shows the distribution of data points by their value, and the frequency at which they occur. The chart we see looks very much like a Gaussian normal distribution curve, where the most frequently occurring values are in the middle, approximately where we would see the mean. We see the other values distributed around the mean, but not in the same frequency. The goal of this course isn't to become an expert statistician, but rather to understand how to spatially understand the data that goes into [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md). The classic linear logistic regression model has two actual outcomes. Zero or one, for example, which make it a binomial model. The Bernoulli distribution is a commonly used mathematical distribution that says that for a two outcome distribution, there are two probabilities, P and Q, which equals one minus P. Think of this distribution like a coin toss, where the outcomes are either heads or tails. If the coin is evenly weighted, then the mean or the expected outcome for these coin tosses will be 0.5. If the coin is not evenly weighted, we'll see the outcome somewhere between zero and one, but not equal to 0.5. The mean itself is normally distributed,
>
> **[1:34](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/differentiating-between-distributions?u=76281980&t=94)** but the outcomes are not. If we put the Bernoulli distribution on a chart, notice there are two outcomes for the model, but the distribution for the outcomes themselves isn't normally distributed. We see a cluster of values around the [Probability](../../Skills/Data%20Science/Probability.md) of the outcome of zero. Conversely, we see a cluster of values around the probabilities for the outcome of one, hence why the Bernoulli distribution is for a binomial model with two outcomes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2), [Probability](../../Skills/Data%20Science/Probability.md) (1)
> **Versions:** 0.5 (2)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [presenter] (1)

#### [Calculating logs and exponents](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-logs-and-exponents?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-logs-and-exponents?u=76281980&t=0)** - [Instructor] Along with [Statistics](../../Skills/Data%20Science/Statistics.md) and [Probability](../../Skills/Data%20Science/Probability.md), exponents and logarithms are two important concepts in [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) models. I think that even if this isn't a new topic to you, it's well worth reviewing before we start creating our models. Let's say we're solving for the value of X, which equals the base of 10 raised to the third power, which gives us 1,000. Logarithms or logs for short, effectively work as a reverse of exponent. A logarithm lets us solve for the exponent we want to put on the base of a number to get a certain result. For a base 10 logarithm on the number 1,000 the result is three, for example. In logistic regression modeling, we won't be working with base 10 calculations, but instead we'll be working with one of the most important numbers in mathematics, Euler's number or E for short, which is a mathematical constant. It's equal to one plus one divided by the factorial of n as it goes to infinity, which equals roughly 2.71828 or 2.72 if we want to round it to two decimal places. Raising Euler's number to an exponent is the same thing as raising approximately the base of 2.72 to an exponent. For example, Euler's number raised to the second power equals roughly 7.39. Logarithms also work with Euler's number
>
> **[1:37](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-logs-and-exponents?u=76281980&t=97)** and specifically they're called natural logarithms or natural logs for short. The exponent we raised the Euler's number to get a result is the result we get when we take the natural logarithm at this result. Often log and natural log are used interchangeably. So be careful because this often depends on the context of what you're talking about or the programming language we're utilizing. Let's see how this works in [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md). We have some values in the first column for base 10 and Euler's number, in the column next to that, we have the exponent we want to raise the number to, and to the right of that, we have some other columns we're going to fill in with our exponent and logarithm calculations. In the results one column, we'll set the equation equal to the base in column A, raised to the exponent in column B. We see the result is 100 and we can copy the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the formulas down. For the formulas that use Euler's number as the base, we can set up this calculation directly using the Excel exp function which raises Euler's number to the parameter value we pass to it. We'll create a new formula and we'll use the exp function and we'll raise it to the value in column B. We see the formula returns the same result as the column next to it and in a more efficient way.
>
> **[3:11](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-logs-and-exponents?u=76281980&t=191)** Now let's take the log of each of these results and it should return the same values we see in the exponent in column B. Let's start by typing log into the formula bar. We see a list of potential functions pop up. There's a log function at the top where we can specify the base, let's instead use the log 10 function, which specifies 10 as the base. We can hit Tab to select the function and we will reference our results in column C. We see this returns the same results as we see in column B for the exponent, replacing on the base. Let's copy this formula down to the cell below. Now let's calculate the natural log of the results for the last two rows. If we start typing in log, we don't see a specific natural log function pop up. This function doesn't start with a string of letters, log like the base log 10 or of our choosing, instead we'll use the LN function, which is short for natural log and we'll reference the same results in column C. We now see our log results in column E have the same result as the exponents in column B, our calculations for the logistic regression model, will use Euler's numbers to calculate logs and exponents. It's important to note that we can't take the log of a negative number or zero,
>
> **[4:44](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-logs-and-exponents?u=76281980&t=284)** so anything less and/or equal to zero, but the result of taking a log of a number between zero and one is a negative number as we see in our calculations, and this will be an important concept as we build out our logistic regression models.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (4), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (2), [Statistics](../../Skills/Data%20Science/Statistics.md) (1), [Probability](../../Skills/Data%20Science/Probability.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Versions:** 2.72 (2), 2.71828 (1), 7.39 (1)
> **Definitions:** is a  (2), short for (1)
> **Analogies:** for example (2)
> **Warnings:** be careful (1), note that (1)
> **Prerequisites:** before we start (1), set up (1)
> **UI Navigation:** select the (1)
> **Speakers:** - [instructor] (1)

#### [Sigmoid curve](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/sigmoid-curve?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/sigmoid-curve?u=76281980&t=0)** - [Instructor] Regression is one type of model, but there are many different types of regression models, including [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) and [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md), which is the focus of this course. These models have different curves that we can visualize in a two dimensional space to see how they fit. A linear curve in a two dimensional space is a straight line between the independent variable values on the X axis and the dependent variable values on the Y axis. Conversely, a sigmoid curve seen in logistic regression has an S shaped curve in the same two dimensional space. In order to model the relationship between these points in a linear regression, we use the standard formula for calculating a straight line where the dependent variable on the Y axis equals the intercept A plus the slope B multiplied by the X axis values for each data point. Conversely, the sigmoid curve uses the function one divided by the quantity of one plus Euler's number raised to negative one multiplied by the quantity of the intercept A plus the slope B multiplied by the input on the X axis. Lastly, the output for the linear curve can be any real number between negative infinity and infinity because the input X values can be any value between negative infinity and infinity as well. Conversely, the sigmoid curve is between zero and one exclusive, which means the Y outcome
>
> **[1:35](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/sigmoid-curve?u=76281980&t=95)** can get very close to zero and one, but it doesn't actually equal these values. Let's go into [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) to see how the linear and sigmoid curves compare. I've already added starting numbers to the sheet for the independent variable X ranging from negative 10 to 10. I also added some starting parameters for the slope and intercept denoted as B0 and B1, I initially set the intercept to zero and the slope to one, but we'll change these values once we do the calculations. In the Y linear column, let's calculate the linear curve, which equals the intercept plus the slope multiplied by the X value. Our result is the same as the input of X because the slope is one and the intercept is zero. Now let's copy this formula down. We would expect to see the same value in the Y linear column as we do in the X column, but we don't. If we go into one of our formulas and click into it, we see in the calculation references the X value and the proper row, but the slope and the intercept values move down as we copy the formula down. We want these values fixed. So let's go back into the cell at the top and fix our intercept and slope value so they don't move when we copy the formula down. On many computers,
>
> **[3:08](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/sigmoid-curve?u=76281980&t=188)** we can use the F4 key to fix these cells. When we copy the formulas to other cells in the spreadsheet, having down the updated formula makes the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the outcomes in this column so the calculated Y linear values match their counterparts in the X value column. Now let's calculate the values for the Y sigmoid curve. In this column we'll create an equation where we divide one by one, plus the EXP function raised to the negative one, and we'll multiply it by the value in our Y linear column. Now let's visualize both these curves directly through Excel's built-in graphing options. Let's select the first two columns for X and Y linear we'll, then choose to insert and we'll select the top scatterplot option. The scatter plot automatically displays. We see the curve is a straight line with a slope of one, and an intercept of zero. Let's add another scatterplot to the sigmoid curve in the same view. To select two columns of data that aren't directly next to each other. We can hold down the control key as we select both columns, this selects non-contiguous cells in our Excel sheet so we can use them in a chart. We can then add a scatterplot for these data points in the same way we did for the Y linear points. We now see the two curves side by side.
>
> **[4:48](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/sigmoid-curve?u=76281980&t=288)** We see the Y sigmoid curve has an S shaped squeeze between the Y values of zero and one, without exactly equally those numbers. The sigmoid curve intersects the Y axis at 0.5, instead of zero when X equals zero, because we can't have a sigmoid curve that equals zero. This is the inflection point of the sigmoid curve. Conversely, we can see that by changing the intercept value from zero to one, this changes, both the linear and sigmoid curve, but in different ways, the sigmoid curve shifts horizontally while the linear curve shifts vertically. Conversely, let's see what happens if we change the slope value from one to three, this changes the shape of the linear curve, as well as the sigmoid curve, but in different ways, the sigmoid curve now has a steeper curve around the inflection point, but it reaches the results closer to zero and one much sooner than we saw before. These values aren't exactly zero or one, but because of rounding, they often look that way.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (3), [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (2), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **UI Navigation:** select the (2)
> **Definitions:** is a  (2)
> **Env Vars:** exp (1)
> **Versions:** 0.5 (1)
> **Speakers:** - [instructor] (1)

#### [Utilizing training and testing data sets](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/utilizing-training-and-testing-data-sets?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/utilizing-training-and-testing-data-sets?u=76281980&t=0)** - [Professor] One of the most important facets of running machine learning models like [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) is splitting the entire dataset into two pieces. The training and the testing datasets. Typically 80% of the data is training data. And the remaining 20% is testing. Training data sets, let us build models by solving for the model parameters to fit. However, we're not running our final model on the training dataset, the testing data set is what we use to validate the model, in order to best model data in the real world. We want to hide this testing data set away until we're ready to test the model out with it. If we fitted our model perfectly on our training data, all the data points would be mapped perfectly to their actual outcomes. This means if we ran the model on our training data set, the total error loss would be zero because the model would perfectly fit every point. However, [Algorithms](../../Skills/Software%20Development/Algorithms.md) deal with ambiguity. And we want to think about making the model a good fit, but not a perfect one. So we can actually use it in the predictions for our testing data. We want to take the model we built using our training data set and apply these variables and parameters we selected, to run the model on the testing data set to validate it. This means our training data set will have a higher loss error, but the testing data set will have much more reasonable error
>
> **[1:31](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/utilizing-training-and-testing-data-sets?u=76281980&t=91)** because we're not exactly fitting the model, but rather trying to find the best overall approximate fit. There are various ways to split the data into these two separate training and testing data sets. And the case of our model. I took all the data points from the ESPN data and split them randomly into 80, 20 proportions using a randomized variable between one and five as an [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) function. I then sorted a 1/5 of them to the testing data set and the remaining 4/5 the 80% to the training data. I've added these two data sets the training and testing to our binomial model. The key to modeling with this split data set is we don't want to peak the testing data set until we felt our model using the training dataset. There are many ways to split data, but for the purpose of this course, because this isn't necessarily a focus of our logistic regression modeling. I wanted to make it as simple as possible so we could start our modeling process.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1)
> **CLI Commands:** find (1), make (1)
> **Env Vars:** espn (1)
> **Speakers:** - [professor] (1)


### 2. Binomial Logistic Regression

[↑ Back to Table of Contents](#table-of-contents)

#### [Calculating linear regression](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-linear-regression?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-linear-regression?u=76281980&t=0)** - Games like basketball are great examples of [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) models, but how do we know that's the case. In our basketball dataset, I added a new sheet to note at linear X one, where I added the independent variable X one for the points scored in the first column and the dependent variable Y for the game outcome in the second column. Notice the values and the outcome the win-loss column are either one, which means they won or zero which means they lost. We could set win to zero and loss to one. We just need to be consistent with the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the calculations in the model, as per the way we originally classified them. It looks like this is an example of logistic regression, but let's see what this model looks like by running the standard [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) calculations on these numbers. If you look at the formulas in these cells, you can see they referenced the training data set. We don't have to put our models in separate sheets within [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md), but I find doing this keeps my work more organized. Let's start by selecting points scored as the independent variable we'll analyze against the win-loss dependent variable outcome in a two dimensional space. Well, first create a scatter plot. Notice the points are tightly packed into a range of values for the points scored along the X axis, but there's only two outcomes for the y-axis zero and one, and they're clustered together quite closely. We also see an overlap between the points scored
>
> **[1:36](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-linear-regression?u=76281980&t=96)** in the outcome of the game. This occurs because sometimes even if a team scores a lot of points, if they're playing a tougher opponent, this may not be enough points to win the game. If we add a trend line, the scatter plot by selecting the plus icon and selecting the trend line, we see that the line is a linear line through the points. We see it estimates the relationship between the independent variable and the X-axis and the dependent variable on the Y-axis. It's a straight line. It doesn't appear to be a good fit for our model. We want our model to show the relationships between either zero or a one, instead of outside of this range. Now let's calculate the slope for the best fit line for this model to see what it gives us. We'll use the Excel function slope, and we'll then reference our dependent variables, first the known Ys or the known Y. And then we'll reference our known Xs, which is our one independent variable. Let's also calculate the intercept. We use the intercept formula in Excel and we'll reference the same Y and X values that we did for our slope calculation.
>
> **[3:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-linear-regression?u=76281980&t=180)** - We'll make a copy of our scatter plot and put it at the top. If we take a closer look at the trend line compared to the slope and intercept which is calculated, we see the trend line calculates its best fit, using the linear regression slope and intercept values because the linear regression model yields results outside our expected outcome range between zero and one. We know it's not a good fit for our own data. So let's use logistic regression instead. Unfortunately, we can't solve directly using formulas like this for the logistic regression model, but we can model it in Excel, which we'll do using solver.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (4), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (4), [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (3), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** find (1), make (1)
> **Definitions:** is an  (1), is a  (1)
> **Speakers:** - games (1), - we (1)

#### [Working with the logit model](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/working-with-the-logit-model?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/working-with-the-logit-model?u=76281980&t=0)** - To solve [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model, let's start by looking at the logit function, which is a quantile function for logistic regression models that's useful in data transformations. It's the calculated reverse of the standard logistic function associated with the sigmoid curve, because it's the inverse of the [Probability](../../Skills/Data%20Science/Probability.md) function, it's often called the log-odds function. Let's say P represents the calculated probability, logit equals the inverse of the standard logistic function. We define it as the natural log of P divided by one minus P. The logit or odds of our logistic regression model equals the interceptor bias, represented as the B nought value plus the B one value for our first independent variable multiplied by the independent variable plus B two, and so on. We include all the variables we want to calculate in the current iteration of the model, so we can have more than one independent variable. Once we calculate the logit value, we can solve for the probability by setting the logit value equal to the logit formula that uses the probability number. We'll exponentiate both sides of the equation first, which reverses the log on the right side of the equation to solve for the P value. Solving for the probability in our equation represents the predicted outcome for the given inputs in the model that we're solving for. The probability equals one divided by the quantity of one
>
> **[1:36](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/working-with-the-logit-model?u=76281980&t=96)** plus euler's numbers raised to the negative logit value. In [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) lets calculate logit first. In a new sheet I already added to the binomial file titled logistic X one. To make it easier to see the calculations I already set up the X one and Y columns in the same way as the linear model. It's important to note that we want to make sure our dependent variables are zero and one to represent losses or wins instead of W or L, because otherwise the model won't work. If you currently have non-numeric outputs in your dependent variable, you want to convert them first into zeros and ones before running the model. It's important to note that whether you sign them zero or one isn't important, but you want to be consistent with how you label these outcomes. Lets first add our coefficients to the model, which we'll call B zero and B one. And in particular, these are the coefficients for the intercept is B nought and B one is going to be the points scored. I'll just put points for short. We're going to reference these coefficients as we set up the calculation, we'll initially put point zero zero one in both of these coefficient values, don't worry if this seems odd. Assigning initial values to our coefficients, lets us check that we're properly setting up the calculations. Once we run the model using solver,
>
> **[3:09](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/working-with-the-logit-model?u=76281980&t=189)** these coefficient values will update to the values that solve this logistic regression model. Next let's add a new column called logit. The formula for these logit values is equal to B nought plus B one multiplied by our X one value, we'll also fix our J two and J three cells so the parameters, these coefficients stay fixed as we copy the cell's formula down. Next, let's create a new column called ilogit. Now we're going to use the exp function to raise euler's number to negative one multiplied by our logit value. Now let's create a new column called predicted, which we'll use to calculate the probabilities of the outcome labels for this logistic regression model. The (indistinct) value we calculated so far represents the odds for the model. So we want to use the formula to convert odds into probability. We'll take our (indistinct) value and use it in our probability formula by setting equal to one divided by the quantity of one plus our ilogit value. We've now created the formulas to set up for logistic regression model so we can use it to optimize the coefficient parameters when we run solver to optimize our model.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Probability](../../Skills/Data%20Science/Probability.md) (8), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (5), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1)
> **Prerequisites:** set up (3)
> **CLI Commands:** make (2)
> **Warnings:** note that (2)
> **Definitions:** is a  (1)
> **Speakers:** - to (1)

#### [Calculating log likelihood](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-log-likelihood?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-log-likelihood?u=76281980&t=0)** - [Instructor] Before we calculate the log likelihoods for a [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model, let's think about how we would calculate the mean for a binomial distribution. The mean value for a binomial distribution with two outcomes like our model equals the [Probability](../../Skills/Data%20Science/Probability.md), P that an event occurs multiplied by one plus the quantity of one minus P multiplied by zero. The second term of the formula goes to zero so the expected outcome or mean of this model equals the probability P. In order to ultimately optimize the coefficients in our logistic regression model, we first need to calculate the log likelihood for each data point and then sum them up and we'll minimize the distance when we solve the problem. I find the log likelihood function makes the most sense if I separate it into the actual outcome and the alternative outcome terms. In the first term, we multiply the actual outcome by the natural log of probability P for this outcome. We then add the other alternative outcome possibility multiplied by the natural log of the quantity of one minus P or probability. One way to visualize log likelihood is to think of how we would fit a line through the theoretical data points for logistic regression. Let's say we place the sigmoid curve in our logistic regression model through these data points that we want to best fit a line to. We can then calculate the log likelihood values
>
> **[1:35](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-log-likelihood?u=76281980&t=95)** for each of these data points. While these distances don't actually represent the actual distance from each data point to its predicted label on the curve, we can see that points that are farther away from their predicted labels, the longer orange lines have a much higher log likelihood values than those that are very close to the predicted labels. When we optimize our logistic regression model to get the solutions for these coefficients, we ultimately want to minimize the total sum of all these log likelihood distances. When we do these calculations in [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) however, we'll see the log likelihood values don't represent actual distances, but rather relative distances to solve the model with. This sigmoid curve represents the optimized logistic regression model that fits within these 10 data points. One approach that I think makes it easier to understand how to calculate log likelihoods is by adding a few extra columns in our sheet to reference the different components of the log likelihood formula. Let's first add a column in the space C which we'll call alternative Y which means this is the opposite outcome of what we have in our actual outcome. Now let's create a conditional formula that says that if Y is equal to one, we'll set our alternative outcome equal to zero
>
> **[3:08](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-log-likelihood?u=76281980&t=188)** otherwise the outcome will be one. And we'll copy these formulas down. Will then create another column called one minus P. We could write out prediction or call this p-r-e-d to make that shorter. And we'll set this equation equal to one minus the value in our predicted probability column. Now we'll add another column called log likelihood. We'll first multiply the actual outcome in the Y column by the natural log of the predicted value column. We'll then add another term to the formula which multiplies the alternative outcome by the natural log of one minus P or one minus the predicted value we have in the column to the left of it. We now see the log likelihood values for each of the data points in the existing training dataset. Let's sum all these log likelihoods up into a single value at the top of the column. I'm going to note that this is the sum LL then I'm going to highlight this in a blue, 'cause this is going to be important in our model later. We've now set up the formulas and parameters
>
> **[4:43](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-log-likelihood?u=76281980&t=283)** for our logistic regression model in Excel. However, we haven't optimized it because we haven't solved for our coefficients in the model yet. To do so we'll maximize the total log likelihood value to minimize the total loss because it's a negative number. Once we run the solver, we'll have coefficient values that will update the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the formulas we already have in the model.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (7), [Probability](../../Skills/Data%20Science/Probability.md) (5), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** find (1), make (1)
> **Warnings:** note that (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Constructing MLE](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/constructing-mle?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/constructing-mle?u=76281980&t=0)** - Unlike the [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) model where we can solve directly for the intercept and slope values using the least squares method, we can't do the same in the [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model. This means we need to use a methodology called the maximum likelihood estimation, or MLE for short, to solve for these coefficient parameters. The MLE is a probabilistic framework used to estimate the coefficient parameters in the classification models like logistic regression. To solve the logistic regression model using MLE, we want to maximize the sum of the log likelihood values in the existing calculations. Let's visualize what maximizing the MLE looks like. On a two dimensional plane, we can put our coefficients on either axis, but for this example, I'm going to put the bias, B not, on the X axis and the B1 coefficient on the y-axis. Let's say we calculated the log likelihoods for both of these coefficients and summed them up to get a total number. We can place these some totals for the log losses on the two dimensional matrix. One way to visualize which coefficients have the lowest log loss is by adding a color scale to this existing matrix. We see the darker colors as higher values, where we were maximizing the negative sum total, which gives us an idea of this minimized loss total and at which coefficients it occurs. To visualize the log loss function,
>
> **[1:32](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/constructing-mle?u=76281980&t=92)** let's start turning our two dimensional visual, our matrix, into a three-dimensional one by first turning the shape on its side. We can visualize the maximized log total as the highest point in this three-dimensional space at the top of the bowl shape. Where this highest point occurs is where the log loss is the least and there are X and Y coordinates, in this case the B not, and B1 values of the coefficients to solve this model. This isn't an exact way to solve the model, at least the way we see it here, but it lets us visualize what the optimized solution looks like. Now let's replicate this approach in our [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) logistic regression model. I've already created a new sheet called Logistic X1 MLE where we'll approximate our coefficients before we run the model. Notice our total log likelihood is a very negative number. We want to maximize this number so it's the least negative. I've already duplicated this page so we can approximate our calculations in a separate matrix table from the model configurations we've done so far. We can nest the formulas for logit, e logit, and the predicted [Probability](../../Skills/Data%20Science/Probability.md) iteratively inside the log likelihood calculation to save some space on our sheet. But first nest our logit calculation inside our e logit calculation. Next, we'll put our e logit value into our predicted column.
>
> **[3:13](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/constructing-mle?u=76281980&t=193)** And we see that when we drag the formulas down, we get the same result. Finally, let's put the probability formula into our log likelihood calculation.
>
> **[3:32](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/constructing-mle?u=76281980&t=212)** And we're going to take our alternative result. We can use the same alternative result. In this case, what we want to do is we actually want to update this particular calculation. So we'll copy one minus the probability and we'll place it in the second function for the natural log. And then we're going to take our probability and put it in the last term. So when we copy the formulas down, we get the same log likelihood sum at the top, which is exactly what we want to see. Now we can remove the first four columns. I'm just going to delete them all so we know that they're still there, but we've got all our log likelihood calculations in a single column. Now let's copy over our entire column H into the spaces to the right of it. I've already added the coefficient values for B not and B1 in the headers of the columns to the right. Before we copy over the sum, we also want to make sure that we fix the values for X1 and column B in this case, which is Y,
>
> **[5:08](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/constructing-mle?u=76281980&t=308)** so that when we copy them over, they don't all move. So we still reference these same three columns when we copy the formulas over. So now let's copy this entire column and we'll place it in all these columns next to it. There's actually a hundred different parameter coefficient options that I've set up. We want to change our reference so we're using the intercept up and the B1 and the B0 values at the top. So in this case, what we see is that we're keeping these fixed at F2 and F3. So we actually want to update our formulas so that they use H instead. And we want to remove H as fixed column. We're going to remove the dollar sign to do that.
>
> **[6:22](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/constructing-mle?u=76281980&t=382)** So now let's actually remove this column next to it because it doesn't have any parameters at the top. Let's copy this column over again. We want to determine roughly where we think this maximized sum will occur, where the total log loss is minimized by being the least negative. Let's add a summary table to show how these sum of the log likelihoods look compared to each other. Let's put B0 and we'll put B1 in the rows. And then this is going to be negative 10, negative 9, and then so on. There's a few different ways we can calculate these totals, but using the SUMIFS function is a quick way to let us tabulate the numbers in our example. In the value space, we'll use the SUMIFS function to calculate the total log likelihood for each of these inputs.
>
> **[7:39](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/constructing-mle?u=76281980&t=459)** I'm just going put B0 as a placeholder for now and then do the same with B1. So now we want to take our formula and for the B not values, we will set them equal to the column header. And we're going to fix the rows, so we'll put a dollar sign before the 4. And then for our B1 values, we'll fix the columns in that case. So once we fix the cell references, let's copy down the formula and we want to fix this row. We can see that we definitely do have values. They're not zeros. And we'll do the same for other parameter references.
>
> **[8:48](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/constructing-mle?u=76281980&t=528)** Now when we copy the formula down we can see the sum of the log likelihood changes as we go between these different parameters. We'll copy the formulas over for the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the table. Lastly, let's add color formatting to our matrix tables so we can visually see where our optimized solution has its parameter values solved at. We'll first highlight our table, and then we'll go to home and select the conditional formatting options.
>
> **[9:21](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/constructing-mle?u=76281980&t=561)** I'm going to make the darkest value, the highest value, a dark blue. And the lowest value, I'm going to make that almost white. I'm going to go the other way. We see our summary table shows the B not values in the columns and the B1 values in the rows. We're looking for where the log loss for the MLE is the least, which means we're looking for the maximum value of all these coefficient combinations. If we color code the table as we see here, we're looking for the darkest color in this table of possible coefficients. We see that for when B not equals negative 6 and B1 equals 0.09, the total log likelihood sum is maximized, meaning it's the least negative. When we solve our logistic regression model for the bias and independent variable coefficients here represented as B not and B1, we would expect the result to be around this combination of coefficient variables.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (5), [Probability](../../Skills/Data%20Science/Probability.md) (4), [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (1), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Env Vars:** mle (6), sumifs (2)
> **CLI Commands:** make (3)
> **Definitions:** is a  (3)
> **UI Navigation:** go to (1), select the (1)
> **Versions:** 0.09 (1)
> **Cross-References:** in the last (1)
> **Prerequisites:** set up (1)

#### [Solving MLE](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solving-mle?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solving-mle?u=76281980&t=0)** - [Instructor] Unlike the [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) model, we can't solve for the coefficients of the [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model directly. Instead, we need to run the Solver [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) add-in with our existing logit model to determine the coefficients. I recommend configuring the formulas in Excel completely before you run solver to get the optimized solution for this model or any other model in Excel. There are four main components to think about when configuring models in the Excel Solver add-in. The objective is the cell we're trying to optimize in the model. The variable cells are the numbers we're letting the algorithm change for us to optimize the model. The constraints are the limitations we give to the model. And the solving method is the algorithm the solver use In the solver add-in, there are three different types of solving methods. For our problem, we're going to leverage the GRG non-linear solving method. GRG stands for generalized reduced gradient. This algorithm solves for the decision variables, those variable cells in the model, by optimizing the function gradient or slope as the input variables, those variable cells, change. We're going to run our logistic regression model through an R script to compare it to the solver results. We'll use the generalized linear model, or glm, to solve for these parameters. This is a function that's part of the built-in stats function within R.
>
> **[1:33](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solving-mle?u=76281980&t=93)** The parameters for the R glm function for any type of regression model are the dependent variable, the independent variables, the data, and the family type. In our model, we're trying to maximize the log likelihood by having the solver figure out for us which coefficients would solve the model that optimize the total log loss by maximizing it. Let's make our decision variables, the coefficients, in yellow color. Now let's access solver through the data tab of the top ribbon. In the pop-up window, let's configure our models so solver can get a solution for us. We'll first select our blue cell as the check diff we're trying to maximize. Let's make sure that we have the max radio button selected in the objective. Next, let's select the yellow cells as the changing variable cells we want the model to run. In our solving models, we'll select GRG Nonlinear. from the dropdown method for the solving options. Now let's run the solver model. Notice that our results look kind of odd. We see that we've got kind of a skewed predicted numbers. And the other thing is,
>
> **[3:04](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solving-mle?u=76281980&t=184)** if you remember from our matrix calculations, we see the log likelihood looks a bit lower than what it should be. So we'll back into our solver and we'll update the model by unchecking make unconstrained variables non-negative. And we can hit the solve button to run the model again. Once we solve the model, we see the coefficients in the yellow cells have changed so they optimize the total log loss in the blue cell. We've maximized this total. We can also see the calculations, the logit, e logit predicted columns that roll into these predicted [Probability](../../Skills/Data%20Science/Probability.md) and log likelihood values that have been updated since we optimized the model coefficients. The individual log likelihood values tell us how close our prediction is to the actual. By trying to maximize this total log loss, we see how the log likelihoods with the lowest values have predictions close to the actual outcomes, while those with high values are the opposite. Now let's run this model in [RStudio](../../Skills/Software%20Development/RStudio.md). I've already set up the path to import the CSV file containing the dataset, the training data, and I've assigned it to the variable df, which is short for data frame. If you're using the exercise files in this course to run the model on your own,
>
> **[4:37](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solving-mle?u=76281980&t=277)** you'll need to update the folder path in the first part of the file path function so that it references your desktop folder instead of mine. We can display the variable, if we run the entire code, we don't see any results. What we want to do is add another line for df and run it again. Now we see this data frame contains the columns we saw in our Excel file. But notice there are no spaces between the column header names. R replaced them with periods instead of spaces when it imported the data into RStudio. We'll need to remember this when we set up our calculations, that the field names are slightly different from what we saw earlier in Excel. Let's first create a new variable in R called logit, which we're going to assign the glm function to. Now within the glm function, we want to first reference our dependent variable, the win-loss outcome. You can just copy the name of the field from the console below. Now let's put a tilde in our expression, which is this little squiggle line. And we'll put our independent variable after our dependent variable. We'll reference our independent variable for the points scored by copying the field name from the console. You can reference fields in different ways, but this is just the easiest and most straightforward way
>
> **[6:11](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solving-mle?u=76281980&t=371)** to do it for our problem. Next, let's add a comma and we're going to set our data equal to the df variable. We already imported as a CSV file and we set it up as a data frame so we can just set it directly equal to the data. Lastly, we want to add another parameter into the model where we set the family equal to binomial. We'll make sure that parentheses close this expression. And also, binomial goes in quotation marks. Now let's run the model and we can just highlight everything and run it. So notice the model runs, but we don't see an outcome. We've assigned the glm function to the logit variable, but we haven't called the variable so we can see what the results look like. We'll use the summary function on the logit variable to view the results. We can just highlight this single line and run the code. Now let's scroll up and we can compare the parameters. We see that we have -6.19, or roughly -6.2, and close to .09 in our coefficient for the points team scored. Now let's check this against our Excel results. We see the same results in our Excel solver as we saw in our RStudio results.
>
> **[7:46](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solving-mle?u=76281980&t=466)** This is exactly what we want to see so we can build the predictions from the logit variable we just created.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (8), [RStudio](../../Skills/Software%20Development/RStudio.md) (3), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2), [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (1), [Probability](../../Skills/Data%20Science/Probability.md) (1)
> **Env Vars:** grg (3), csv (2)
> **CLI Commands:** make (4)
> **Prerequisites:** set up (2), configure (1), you'll need (1)
> **UI Navigation:** select the (1), dropdown (1), scroll up (1)
> **Definitions:** stands for (1), is a  (1), short for (1)
> **Versions:** 6.19 (1), 6.2 (1)
> **Cross-References:** earlier in (1), as we saw (1)

#### [Predicting outcomes](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/predicting-outcomes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/predicting-outcomes?u=76281980&t=0)** - Once we solve our models for the coefficient parameters. We need to interpret the results for the [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model In [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) model. We already sell for the coefficients as well as the predicted outcomes as the calculated probabilities. But in order to do this in R we need to leverage the predict function, which is another function within the R stats package. The predict function has three input parameters that will pass into it. The Logitech variable, we already calculated using the GLM function, the data frame, which we'll set equal to the new data that we want to add our predicted field to. And the type right now, we have our predicted probabilities as values between zero and one. You want to round the predicted values in both Excel and R to the nearest integer so we can analyze them later for the model fit. We can do this in multiple ways in either application, but for this project, let's use the round function in Excel and the ifelse function in R to round predicted [Probability](../../Skills/Data%20Science/Probability.md) to the closest integer. Let's add another column in our Excel file, and we'll call this predicted rounded. We can use the round function in Excel to reference the predicted outcome. And then we'll select zero as the second parameter of the function, because we want to round it to the closest integer.
>
> **[1:35](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/predicting-outcomes?u=76281980&t=95)** We now see that if the probability is less than 0.5, it rounds down to zero. Well, if it's over 0.5, we see it rounds to one in our studio let's continue to add to our logistic regression model that we've written in the R script. We're going to add a new column to our data frame called "predicted", and we'll use the kerat that points to the left and a dash to assign this predicted field. The outcome of the predict function. We put logit as our first parameter, we set the new data parameter equal to DF, and we set our type equal to response in quotation marks. Let's run this code so we don't see any outcome below. And that's because we want to call the data frame variable again and run it. We see our predicted outcomes appear as the last column in our data frame table. Now let's check this against our Excel model. We see our predicted outcome in the first row matches what we see when we run the GLM and predict function in R.
>
> **[3:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/predicting-outcomes?u=76281980&t=180)** - Let's also round out the predictive outcomes in the data frame as a new field, we'll create a new variable in the data frame called DF and we'll use the dollar sign to assign it to a column, we'll call it, predict zero one. Next we'll assign the ifelse function to this variable. We'll set up the condition so that if DF predicted is greater than 0.5, then we make this outcome one. Otherwise we make it zero. Let's run our code again. We'll call the data frame once more. And we see a zero one values assigned in the final column and these match what we see in our Excel file.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (7), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2), [Probability](../../Skills/Data%20Science/Probability.md) (2)
> **Versions:** 0.5 (3)
> **CLI Commands:** make (2)
> **Env Vars:** glm (2)
> **Speakers:** - once (1), - let (1)
> **Prerequisites:** set up (1)

#### [Visualizing logistic regression](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/visualizing-logistic-regression?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/visualizing-logistic-regression?u=76281980&t=0)** - [Instructor] Once we predict our [Probability](../../Skills/Data%20Science/Probability.md) outcomes for logistic progression model, I find it's immensely helpful to visualize them as graphs as part of the process to get more inside into what the model output tells us. We can construct charts in [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md), and we can also construct them in R. There are thousands of libraries in R and many of them support graphic capabilities, but there are actually built-in graphic commands or functions, we can leverage directly in R without having to load or import any other packages. One example of this is the plot function. Let's first create our scatter plots for the points scored, versus the predicted and actual outcomes in Excel. We can select these three columns, you may hold down the Control key, select our, predicted column, we then insert a scatter plot on the same page. Let's make this a bit smaller, move it over, and I've renamed it. Actuals versus, predicted. You see the actual and predicted values displayed as two separate sets of dots, along the plot in different colors. Notice the actuals are either zero or one, but the predicted outcomes are the shape, of the Sigmoid curve between zero and one. Now let's create the same graphic
>
> **[1:32](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/visualizing-logistic-regression?u=76281980&t=92)** in R using the plot function. Well first input the field we want to put on the X-axis first, which is going to be our points, the team scored, and I'll follow this with a comma, that separates the two fields, and then we'll put our, actual results, df of the, win loss. When we run this code we see the outputs, of 0,1 on the Y-axis and the points scored on the X axis. But say we want to add a line to the scatter plot, we just create it. We can add a line directly on top of the existing scatter plot, using the lines function. Inside this function we'll again put, the points scored, on the X-axis, but on the Y-axis we'll put the predicted. Once we run the curve we see if the plotted points, appear and we also see the line on top of it. The line goes between zero and one but notice it zigzags across the space, where we really want to see a smooth line, across our models so we can use the line to make predictions. To straighten up the sign we need to first sort it by the points scored in the data frame before we graph the scatterplot.
>
> **[3:07](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/visualizing-logistic-regression?u=76281980&t=187)** We're going to reassign our data frame, df, to a variable of the same name, and we'll put the df to reference the original variable in square brackets, and inside of that, we're going to put order, and select the field we want to order the results by, in this case the points scored. We can close off the parentheses and put a comma before the final bracket. Now let's run the entire script again. Now we see a smooth S shaped Sigmoid curve as the line on top of the actual scatterplot points, this shows us the best fit of the line using The Emily Model, as a Sigmoid shape. We see how it returns predictions between zero and one as we were expected to, but as a smooth line for the probabilities of winning based on the point scored. Let's make our plot points orange, by updating, our plot command line. Set, Col for color, equal to, orange in quotation marks, let's also make our plot points a small dot by setting the pch, parameter equals to 20.
>
> **[4:31](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/visualizing-logistic-regression?u=76281980&t=271)** Let's also make the color of the Sigmoid line blue, by setting the col parameter for the color, in the same way. Let's update the title of the visual, by setting the main parameter in the plot function, equal to the name of the title we want the visual to display. Let's call this points scored, versus outcome in quotation marks. Let's rename our X-axis labels so we don't see this df variable, and the dollar sign it looks very cleaner if we rename it, and I'll set this equal to points scored, and this is going to be xlab. And then finally, we want our ylab, let's set this equal to, win or loss. Once we run our updated plot we see an easily interpretable visual representation of our logistic progression model, that looks similar to our output or actually the same as our output in Excel apart from some of the graphics, but perhaps a more scalable approach, for those of you designing models for end-users of your work in R.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (3), [Probability](../../Skills/Data%20Science/Probability.md) (1)
> **CLI Commands:** make (5), find (1)
> **Tools:** command line (1)
> **UI Navigation:** select the (1)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Calculating logistic regression](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/challenge-calculating-logistic-regression?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/challenge-calculating-logistic-regression?u=76281980&t=0)** (playful music)
>
> **[0:05](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/challenge-calculating-logistic-regression?u=76281980&t=5)** - [Instructor] Here's your chance to apply what you just learned about binomial [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) on your own. You're going to use the dataset we're already using for this project to select another independent variable, to run with the same variable as the dependent variable for the outcome of the game. You'll want to think about what variables could potentially work in the model and switch them with the point scored variable to see how they compare. Remember to use numeric inputs in the model to make sure it runs. Also think about whether you want to run the model in either [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) or R or even both, depending on how much you want to deep dive into the model to understand more of the technicalities behind how it runs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (1), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1)
> **CLI Commands:** make (1)
> **Best Practices:** remember to (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (playful music) (1)

#### [Solution: Calculating logistic regression](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-calculating-logistic-regression?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-calculating-logistic-regression?u=76281980&t=0)** - In our challenge, I'm going to use the same data in our script as I did for earlier videos. But I'm going to update it for the challenge. Let's use another column instead of the point scored. So let's see what the name of this column as that we want to use. We'll use the names function, which we'll call on the data frame variable. We now see a list of the column names appear in the console. Let's select rebounds, which just goes by the name rebounds. So I'm going to update the GLM function that is, is assigned to logit, by deleting the points team scored and updating it with rebounds. We can comment out the names code using the hash sign. We can then run this model. Summary will be updated because logit is updated. And we see the results and the coefficients for this model. We can then run the predictions on this model using rebounds instead of points scored. We don't have to update any part of the code in this section because it's already updated in the logit variable that we've already modified. If we add the df variable as another line after the function in which we reorder and reassign the data frame variable based on the points scored, we can see the updated data frame. We want to update the point score to use rebounds. Now let's run this code.
>
> **[1:38](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-calculating-logistic-regression?u=76281980&t=98)** We see the results don't have the same range between zero and one that we saw when we use the points scored as the independent variable. Let's see what this looks like in a graph by updating the plot functions. We'll change the X axis variable from points team score to rebounds for both the plot and line function. Let's also update the X lab parameter, which gives the label for the X axis to rebounds, instead of points scored. It'll also update the main parameter and the plot function to say, rebounds versus outcome, instead of points scored. Now let's run our code. And sometimes if you see these functions are out, we can just run part of it. So we see these margins are too large. And sometimes we see these problems we run into them. In this case, we can see that perhaps rebounds may not be the best model to use in this case. Part of the process of using [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) is experimenting with different independent variables and thinking about how they fit into the model. Let's run our lines command. I notice we still don't see these results. We can look further into the messages that we see in the console, but for now, we can see that perhaps reducing rebounds in the model, isn't really approach that we want to take, and we can further explore other variables or other options
>
> **[3:14](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-calculating-logistic-regression?u=76281980&t=194)** in the future.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (1)
> **Env Vars:** glm (1)
> **Speakers:** - in (1)
> **Non-Speech:** (upbeat music) (1)


### 3. Fine-Tuning the Model

[↑ Back to Table of Contents](#table-of-contents)

#### [Adding more independent variables](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/adding-more-independent-variables?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/adding-more-independent-variables?u=76281980&t=0)** - [Instructor] Not only [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) with a single independent variable lets us visualize the data and predictions in a tangible way. However, more realistically logistic regression models like other models often include more than one variable. In our [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) file, I copied over the model for logistic regression as a single variable to a new sheet called logistic X1 plus X2, plus X3. I have then inserted space for two additional independent variables. For X2, let's set this equal to our home and away variable in our training dataset. And I'll label this home as one and away as zero.
>
> **[0:49](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/adding-more-independent-variables?u=76281980&t=49)** Now let's set our X3 variable. The third independent variable equal to our field goal percentage. And again, we'll get this data from our training dataset.
>
> **[1:06](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/adding-more-independent-variables?u=76281980&t=66)** Now let's add another parameter, B2 and B3 to our coefficients. We'll set all these coefficients equal to 0.0001. Next let's update our calculations to include these new coefficients. We'll take the logic calculation and we'll add the B2 parameter and multiplied this by our X2 variable, and then we'll also add our B3 coefficient and multiply it by our X3 variable. We'll then make sure that we fix the cells that are in column O rather, and we'll copy the formula down. Now let's open the solver to run the model. Our objective is going to remain the same. We're not going to change our variable cells so it references four of them instead of two. So we're still maximizing the sum of the log likelihoods or in other words, we're minimizing the loss. We'll then run our model. Once we've run the model, we see the solver results. If you're not too comfortable using the solver item yet, it can become intimidating. You might be apprehensive to run your model more than once. However, if we run the model again, we should see the same coefficient values
>
> **[2:41](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/adding-more-independent-variables?u=76281980&t=161)** solved for in the yellow cells, because we're not changing anything else about the model. Let's run our model again to see what results we get. After we run the solver again, we see the coefficients do change. One potential reason this occurs is because we're using a gradient method to solve for the coefficients, these decision variables represented in yellow in our model. The algorithm runs along the gradient to determine the maximized total likelihood sum. However, it may not reach this point before it returns the results. After we run the solver, it continues to try to maximize the objective, in this case, our log likelihood sum, until it reaches a result. In this case, we see that it took more than one run of the model to get this result. If we were to run the model again, we would see the same result. Now let's check our results in Excel against the results we see when we run this model to solve for the coefficients in R. We now see four coefficients for the logistic regression model solved instead of two. Let's insert a scatter plot to see what it looks like. We'll choose X1 and then we'll choose our predicted values.
>
> **[4:11](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/adding-more-independent-variables?u=76281980&t=251)** We see the scatter plot looks a bit odd now because we've got more than one variable in the model, but only one of them on the x-axis of the scatter plot against the same dependent variable. Now let's compare what this output looks like to running the model in our studio. We're going to update our GLM function to run the model with more than one independent variable. Right now we only have the points the team scored as the only independent variable in the model. We want to update the model, but first we want to know the fields that we want to update it with. One command that's really helpful for this is using the names function and we'll call the DF variables. So this will get the names of all the columns in the data frame. Let's run this code. So we see a list of our columns. We are going to choose the home and away variable. So we add the plus sign and then we'll paste the name of the home and away variable. Next let's get our field goal percentage. So we'll copy and paste this in the formula after the second independent variables. So we now have three independent variables in the model. Now let's run the model. Let me scroll up. We see our coefficients. The coefficients that the GLM function returns
>
> **[5:47](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/adding-more-independent-variables?u=76281980&t=347)** for our logic model match the same coefficients we see in Excel. I can now run the predictions for this model as well. And let's run the chart to see how it looks. Now we see our chart looks a bit odd because we're not directly analyzing the relationship between the predicted outcome and the points scored, but we're also including two other variables for the home and away games and the field goal percentage. Our chart with three independent variables doesn't really tell us much. We can't visualize the relationship between the independent variable and the dependent variable because there's more than one independent variable. We can check the predictions by running that data frame as another line, and we can then compare these to the output that we see in Excel.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (4), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (4)
> **Env Vars:** glm (2)
> **UI Navigation:** open the (1), scroll up (1)
> **CLI Commands:** make (1)
> **Versions:** 0.0001 (1)
> **Definitions:** in other words (1)
> **Speakers:** - [instructor] (1)

#### [Transforming variables](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/transforming-variables?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/transforming-variables?u=76281980&t=0)** - Another option for working with our variables is to transform them within the model to see how they fit. We call these transformations univariate because we're applying them to a single variable at a time. Examples of transformations we can use in our model include logs, roots, box-cox, and other univariate transformations on the existing variables in the model. We can apply transformations to our independent or dependent variables within the model. But we do want to take a moment to think about what the transformation outcomes will be to make sure we actually changed the fields in a way that's feasible. For example, [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) has actual outcomes of either zero or one, which means that if we were to take the natural log of these two values, we would get zero if we took the natural log of one, and you would get undefined values if we tried to take the log of zero. In our model, the points, which is X1, is a good candidate for a log transformation, because we know we're not going to have any negative values or zeros. So every transformed result will return a defined result as well. I've already copied the binomial logistic regression model with one variable into a new tab in our [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) file that's denoted as logistic X1 transform. We'll take our points scored field and we'll put the LN function around it. And we'll update the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the values in the column.
>
> **[1:40](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/transforming-variables?u=76281980&t=100)** Once we transform our independent variable, let's run the entire model again and solver will use the same configuration except our calculations will use the transform variable instead of variable we used before. We don't have to make any changes in the solver in order to run the model. Once we run the model, we can look at the output and we can use further analysis to see how it fits. We can see how the coefficients change for both the intercept and the points because of the transformation of the variable. Now let's do these changes in [RStudio](../../Skills/Software%20Development/RStudio.md). Let's make sure that our Logit is only using a single variable.
>
> **[2:38](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/transforming-variables?u=76281980&t=158)** We need to first adjust the points scored column and the data frame before we run any part of the model. So before the Logit variable, so we'll add a new line after we create the variable for DF. You're going to copy our points scored variable, and I'm going to override it for this model. So in other words, the original points team scored field will get replaced by this transformed variable. So we're assigning the same column name, the transformed variable, and we'll use the log function to transform this field.
>
> **[3:21](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/transforming-variables?u=76281980&t=201)** Notice that we're using the log function here. And it's important to note that in R, log means the natural log for the number, so we're using Euler's number. Now let's run our model again. We see that the intercept and the points scored, which was transformed, match what we have when we run the model in solver. How do we know this is a good fit? The answer lies in analyzing [Statistics](../../Skills/Data%20Science/Statistics.md) for this model, which we'll do later in this chapter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [RStudio](../../Skills/Software%20Development/RStudio.md) (1), [Statistics](../../Skills/Data%20Science/Statistics.md) (1)
> **Definitions:** is a  (2), means that (1), in other words (1)
> **CLI Commands:** make (3)
> **Cross-References:** later in (1)
> **Analogies:** for example (1)
> **Warnings:** note that (1)
> **Speakers:** - another (1)

#### [Calculating correlations](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-correlations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-correlations?u=76281980&t=0)** - [Instructor] As we add different independent variable fields to [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model. We want to start thinking about whether or not the field should go into the model. We can apply various statistical tests on the overall outcomes of the model, but we want to determine if there are fields we should remove because they are too closely correlated to one another. One way to determine this is by calculating the correlation between various fields in the model. You've likely heard of correlation before in [Statistics](../../Skills/Data%20Science/Statistics.md) courses, we want to remove the inputs for the model that are closely correlated to each other, because this would cause multicollinearity between the inputs if we left the in. Think of correlation along a two-dimensional scatterplot and the points most closely in line up with the linear line, there's a strong correlation between the data points. We see a correlation of one in this example. If there isn't a line that we can draw to represent the strong relationship, then the correlation is around zero or it's non conclusive. We can't quite see a line that goes through these points. A linear line that slips downward shows a strong negative correlation of negative one in this case. We can calculate the correlations between logistic regression model inputs using a correlation table and [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) and R as we'll see later. Let's look at the correlations between the fields for the existing logistic regression model,
>
> **[1:33](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-correlations?u=76281980&t=93)** with three independent variables in Excel. We need to have the data analysis tool pack installed in Excel as an add, and to create a correlation table. If you don't already have it installed, check out the introductory video, where I walked through how to install the Excel add ins. We'll first select the Data Analysis options from the Data tab in the top ribbon. Next we'll select that we want to calculate the correlation. Now we'll select the columns we want to appear in the correlation matrix. In the correlation matrix we want to select numeric columns because correlation calculations won't work if the inputs aren't numeric. We'll select our first three variables X1, X2 and X3 as our input range. We also want to make sure that we check labels in first row. We'll also put our output on a new sheet. Once we confirm our correlation matrix selection, we see it shows the correlation between the three independent variables in this particular logistic regression model. We see it appear on a new page. I'm going to title this Correlation. We don't see correlation values between any of these variables as a particularly high value. This means that we don't have correlation between the input space on our table we see
>
> **[3:09](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-correlations?u=76281980&t=189)** as a result, we can keep these inputs in the model because we know that there's no multicollinearity between them. Now let's create a correlation table in our studio. We're going to leverage the built in core function in R to calculate the correlations between the fields in our data frame variable DF. Once we run the code notice we get an error message in our console in red text, it tells us we're trying to create a correlation matrix with non-numeric values. Let's instead calculate the correlations between just a few fields in the data. We can get the names of our data frame fields by using the names function to call the entire data frame and this will tell us the column names only. Now we're going to take our correlation calculation, and we're going to select just a few fields from the data. We can select the three independent variables in our model by using brackets to reference just a few column names. We'll first insert a comment because we don't have any selected rows, but we do want to select the columns so we'll use this function C to select the column names as effector. Now, within this function we'll insert the column names, which we want to make sure are in quotation marks.
>
> **[4:42](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-correlations?u=76281980&t=282)** Let's copy them from the list below we'll copy the points the team scored, home and away, and the field goals.
>
> **[5:02](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-correlations?u=76281980&t=302)** Once we run this correlation matrix, we'll see that it matches with the results in Excel. As I noted earlier in Excel model, we don't want to remove any of these fields because the correlation between them isn't particularly high or significant in our model. Let's look at the correlation between rebound fields instead to see if this returns different results. We already have the field names when we ran the names command. So now let's copy out our correlation function let's first copy it though. Now let's copy the rebounds field from the list below in the console.
>
> **[6:02](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-correlations?u=76281980&t=362)** We see the correlation between these rebound fields is much higher than those for the independent variables we originally had in the model. Typically, if the correlation between independent variables is higher than points eight, we want to consider removing them from the model. None of these correlations are higher than that number, but some of them are closer to it at around 0.7. If we did want to include rebounds in the model, we would want to consider this correlation matrix to determine whether or not we wanted to include the fields.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (6), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (4), [Statistics](../../Skills/Data%20Science/Statistics.md) (1)
> **UI Navigation:** select the (5)
> **CLI Commands:** make (2)
> **Versions:** 0.7 (1)
> **Cross-References:** earlier in (1)
> **Definitions:** means that (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)

#### [Using statistics](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/using-statistics?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/using-statistics?u=76281980&t=1)** - [Instructor] As we're building our [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model, we want to pay attention to the model [Statistics](../../Skills/Data%20Science/Statistics.md) so we know that if the model we set up is a good fit. There are entire courses in the library that focus on statistics, but understanding the outcomes and the statistics that go along with the logistic regression model can help us develop better models in the future because we're able to put our numbers in the proper context. We can use the logit results return when we solve that maximum likelihood estimate or MLE, and our logistic regression models will further analyze the fit and statistical significance of our model. Let's run just the first three lines of code to see what the outcome is.
>
> **[1:07](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/using-statistics?u=76281980&t=67)** The Deviance Residuals are the difference between our model and the ideal fit for this logistic regression model. We see our numbers seem reasonable and in line with the log likelihood distances we calculated in our [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) model. We've already calculated the estimated coefficients for the logistic regression model using Solver and R. We've already matched the values in the estimate column, but what do the other columns tell us? We see the Z value column, where a value greater than two for the Z values listed means that this independent variable, in this case the points scored and also the intercept, matter in the model. A few examples of statistical test include the Lagrange multiplier test, the likelihood ratio test and the Wald test. The Wald test evaluates statistical constraints on parameters or coefficients. We calculated the model using weighted distances. The column Pr, [Probability](../../Skills/Data%20Science/Probability.md), the coefficient is greater than Z as an also called the Wald test Z value. The closer these values are to zero, the more significant the coefficient is in the model. AIC is an acronym for that Akike information criterion. It's an estimate for the prediction error.
>
> **[2:41](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/using-statistics?u=76281980&t=161)** By estimating the relative amount of information lost by a model compared to others, you can use this number to make a model selection, if we run this model using several different independent variables. If the model loses less information, then it's a higher quality model than a model that loses more information. It's a balanced between the model fit and the model simplicity by dealing with both the risks of over-fitting and under fitting at the same time. If we're choosing between two models, we would tend to to prefer those with lower AIC values. You can select a model with the AIC number, but it's also recommended to check the model residuals and model prediction tests to see if it works. We can calculate the confidence intervals for each coefficient by using another built-in R function, confint, to reference the logit results we already calculated.
>
> **[3:49](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/using-statistics?u=76281980&t=229)** We see there's confidence interval that goes from 2.5% to 97.5%, which contains 95% of the data in the range of coefficients from the model. This tells us that not only do we have an estimate for the coefficients, but here's a range of coefficient values we can potentially expect from running the model. And these values from the low end to the high end will contain 95% of the predictions for the model.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (5), [Statistics](../../Skills/Data%20Science/Statistics.md) (3), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1), [Probability](../../Skills/Data%20Science/Probability.md) (1)
> **Env Vars:** aic (3), mle (1)
> **Versions:** 2.5 (1), 97.5 (1)
> **Definitions:** is a  (1), is an  (1)
> **CLI Commands:** make (1)
> **Best Practices:** recommended (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Configuring confusion tables](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=0)** - [Instructor] Now let's compare the predicted outcomes with the actual outcomes in the [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model. One way to do this is through a confusion matrix, which is a technique for classification [Algorithms](../../Skills/Software%20Development/Algorithms.md) like this that lets us summarize its performance. However, a [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) of caution. A confusion matrix should be on algorithms with roughly an equal number of outcomes in each class, and also in algorithms with only two classes of outcomes, like our binomial logistic regression model. The confusion matrix takes the actual and predicted outcomes for both these outcomes and puts it on a table with four boxes. The confusion table contains true positives, true negatives, false negatives, and false positives. Notice the confusion matrix uses numeric outcomes instead of probabilities, so we want to use the predicted outcomes rounded to the nearest integer, either zero or one, so they match the actual outcomes in the data. It's also important to note that we're going to use the confusion matrix on our testing data instead of the training data we used to set up the model. This is because we're interested in validating our model and measuring the performance instead of developing it. We want to make sure we're not using it as a way to over fit our data, which is why we're not using it on the training data, but instead on the testing data. When we predicted that the outcome would be false and the actual outcome is false,
>
> **[1:34](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=94)** this is called a true negative. Conversely, if we predicted the outcome to be true, one in this case, and predicted the actual outcome to be one, this would be an example of a true positive. Conversely, if the predictions don't match the actuals, then these are what we call false positives and false negatives. False positives predict a positive outcome, but we actually have a negative outcome, while false negatives predict negative outcome, but we'll actually see a positive outcome. In [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md), I've set up a new sheet with confusion at the end of the sheet name to focus on the calculations for the confusion matrix. Notice it looks a lot like the logistic regression models we built using the training data, except for we're going to use the testing data instead of the training data for the predicted outcomes. However, we don't want to solve our model again, but rather use the coefficients we calculated earlier using the training data. You can see I've referenced those parameters in the yellow cells for the coefficients in this table. I've already put the dependent and independent variables in the X1, X2, X3 and Y for the actual outcome of this data. Now we're going to make sure we update our model so we see the predicted column and the predicted rounded column correctly calculated.
>
> **[3:10](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=190)** Our logit formula refers to our X1, X2, and X3 independent variables from the testing data set, but it references the coefficients we calculated earlier using the training data set. Is there validation? With this set up correctly, we can leave these numbers as is, or we could just nest the formulas inside of one another to avoid confusion.
>
> **[3:50](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=230)** Now that we've calculated the predicted [Probability](../../Skills/Data%20Science/Probability.md), let's delete these two columns for the logit and e logit to make our analysis a bit more streamlined. We'll copy the formulas down. So for those of you following along with the Excel file, I just cleaned this up a bit so we could see our predicted values against our actual outcomes using the test data. And then these are the rounded outcomes of those predictions. We can create a confusion matrix using the summits function like we did earlier for estimating the optimized MLE model, but because we've already got several columns with the data we need, let's just create a pivot table as the fastest way to do this. Let's first highlight our columns of data. We'll select insert. And I'm going to put this on the edge of this sheet. So I'm going to put it over here, starting in column L. And this way we don't have to create a new sheet. And we're just trying to make sure that we have our data selected. And in this case, I'm going to select the existing sheet and put it over here starting in column L. Once we've added our predicted outcomes to the row labels, click select to show
>
> **[5:25](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=325)** why this is our actual values in the columns. We can then put the predicted, those probability numbers in the values, we want select to count them instead of summing them. Now, what we're interested in looking at is the percentage of positive results for the actual outcome matches our predictive outcome. We can sum up the cells where the confusion table gives us the true positive and true negative numbers as a calculation, or we can simply select to show. In the Value Field Settings, let's show the values as a percentage of the grand total. Now we can sum up, we'll call these our matches, and we'll sum these up where we've got zero equals zero in cell M3
>
> **[6:27](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=387)** plus N4 is where we have our true positive. So we're adding the true positives and true negatives together. And then we're adding our non-matches, and these are just labels so we know what they mean later. We're going to add these up as M4 plus N3, and these are our false positives and false negatives. So the total for these numbers adds up to 100. One recommended threshold reference for this total, the number that matches between the predicted outcomes and the actual outcomes, is 70% of the data points are correctly classified. In this case, we see 73%. So we can say that our model looks like it's a strong representation of these predicted outcomes. Now let's create a confusion table in R. There are libraries in R that automatically create confusion tables with a [Statistics](../../Skills/Data%20Science/Statistics.md) for us, but we can create a table using the built-in table function in R. Because we're calculating our confusion matrix on the test data instead of the training data, we need to import the testing data into our model as well. Let's rename our DF variable DF1, and we'll rename or replace our DF variable in our logit calculation with DF1 instead. To add our DF2 variable, I'm just going to copy the DF1 and change it to DF2 And now in the name of the file,
>
> **[8:04](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=484)** this is going to be testing instead of training. And we'll just run these, both these lines, to make sure that they both work. Now let's calculate our confusion matrix from the testing data. What we want to do first is make sure that our data frame that we're adding the predicted column to is going to be a DF2 instead of the old DF1. And we'll do the same for the rounding field. So let's run the model and make sure it works. So we see a warning message here. What we want to do is just make sure that we're not running into issues running our code. Because notice that we did, if we go down... this we did get a predicted outcome. I'm going to make sure that I have the labels properly done. In this case is going to be DF2 predicted as not a real field. So let's run our outcome again. We can also check what our outcome looks like by double clicking on variable itself, DF2 in our environment panel in the top right. When we scroll over, we see the results and they look similar to what we saw in Excel. Now let's calculate the confusion matrix
>
> **[9:41](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=581)** from the testing data. The results of the table are the counts of each of the records. So if we use the table command and we put DF2 and then predict 0.1, and run this code, we see it returns a table of the counts for each of these rounded predictions, zero and one. However, we can add a column to the table by adding a comma in our table function, and then referencing the rows. In this case, we want to put our predicted in the rows. So let's put DF2 and let's put our wins and losses in this one, in the first parameter. So this is our actual outcome. Let's make sure that we're referencing the same data frame. Once we run this command, we see a table appears with the counts for each of the categories in the confusion table. However in order to calculate the proportions, we need to use another calculation in the R script. In this case, it's the built-in function, prop.table, which we can put around our existing table calculation. And we'll nest it in parentheses on both sides.
>
> **[11:20](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=680)** Now let's compare our result to what we saw in Excel.
>
> **[11:30](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=690)** Right now, our values don't match. And the reason that we want to check this is we see that we're only using one variable in our GLM function. So let's update our calculation. Let's run the names on our DF1.
>
> **[11:59](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=719)** We want to add these additional fields. We don't need to include the quotation marks. And we'll see an error message if we run into this issue, but it's good to know what to look out for.
>
> **[12:26](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/configuring-confusion-tables?u=76281980&t=746)** And let's run our model again. See we have 22, 27, and 9, 9 and we get the same results in Excel. So we see how there are two different ways to create confusion tables to test the performance of our logistic regression model in either Excel or R.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (6), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (4), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (3), [Probability](../../Skills/Data%20Science/Probability.md) (2), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Env Vars:** df2 (7), df1 (5), mle (1), glm (1)
> **CLI Commands:** make (10)
> **Prerequisites:** set up (3)
> **Definitions:** is a  (1), is called (1)
> **Warnings:** caution (1), warning (1)
> **Versions:** 0.1 (1)
> **UI Navigation:** select the (1)

#### [Challenge: Fine-tuning the model](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/challenge-fine-tuning-the-model?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/challenge-fine-tuning-the-model?u=76281980&t=0)** (playful music)
>
> **[0:05](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/challenge-fine-tuning-the-model?u=76281980&t=5)** - [Instructor] Let's continue now, working on our challenge for [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) modeling. You can pick up where you left off on your own challenge solution, or you can use my own in the exercise files. Your challenge for this chapter is to review the performance of your binary outcome logistic regression model to see what independent variables you should include to best fit the model, test per statistical significance. And when you've perhaps gone too far into overfitting, think about how you can run models in parallel with each other and pick the best one that fits.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (playful music) (1)

#### [Solution: Fine-tuning the model](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-fine-tuning-the-model?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-fine-tuning-the-model?u=76281980&t=0)** - From my own solution, I'm going to create several confusion tables based on separately calculated logit variables that we can compare to one another. Let's first add a ct1 variable in front of our confusion table, that we'll assign the prop table function to. We'll also want to put data.frame around the entire prop table function so it returns it as a variable that we can view as a data frame, once we run this code. We can see ct1 by running it as another line below. We're looking at the results where Var1 equals Var2. In other words, a scenario where the actual matches the prediction. We can look at the frequency, which measures the proportion of the total data that matches this scenario. Now let's update our existing model to remove the field goal percentage from our model. We'll assign our confusion table a new variable. We'll assign it ct1 so we can open them and compare them side-by-side once we run this model. Now let's run the predictions again. Now, if we go to our environment tab on the top right,
>
> **[1:38](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-fine-tuning-the-model?u=76281980&t=98)** we can see a list of the variables. Let's double click on ct1. We see it displayed as a new tab in our window next to this R-script in the top left corner. Now let's open ct2, and then compare our results for ct1. For example, we see that where the predicted outcome of a loss equals the actual outcome of loss has a frequency of about 0.33. We can then add it to the frequency of 0.4, where the predicted outcome was a win, compared to the actual outcome of win. So we've got about 0.73. Conversely, in this ct2 table, where we removed the field goals, we see that we've got a match of about 0.27, for the actual outcome is a loss, and when we predicted that outcome. And then 0.45 for the actual outcome's a win, and we predicted that outcome. So which is slightly lower than what we would see in the other model. But this gives us an idea of how well our data model performs on the testing data.

> [!info]- Semantic Content
>
> **Versions:** 0.33 (1), 0.4 (1), 0.73 (1), 0.27 (1), 0.45 (1)
> **UI Navigation:** go to (1), click on (1)
> **Definitions:** in other words (1), is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - from (1)
> **Non-Speech:** (upbeat music) (1)


### 4. Multinomial Regression

[↑ Back to Table of Contents](#table-of-contents)

#### [Calculating odds for multinomial models](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-odds-for-multinomial-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-odds-for-multinomial-models?u=76281980&t=0)** - Modeling multinomial [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) is quite different from the binomial regression models we've worked with so far, the binomial logistic regression models contain two outcomes, which are binomial models. Examples of these outcome types include true or false, or in this case for our college basketball data, win or loss, which we represent with the discrete values, zero or one. Multinomial regression contains more than two outcomes. But in the case of these models, the order of the outcomes doesn't matter. To see how multinomial logistic regression models work, we're going to work with the fishing data set, which is available directly in R as a built-in data set within the mlogit package. We'll first need to install the packages for mlogit, if you haven't already, by running the commands line, install.packages, which you can do directly in our studio, we're going to put mlogit in quotation marks. And if you haven't installed this yet, you will see it download. Now to see the available data sets within R, we can use the data command and run this line, and we can see the data sets built in, and we can see them specifically for the packages like mlogit we'll see toward the end. If we want to see the packages specific to mlogit, we'll set the parameters inside the parentheses
>
> **[1:36](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-odds-for-multinomial-models?u=76281980&t=96)** to package equals mlogit and run this line of code. We see our fishing data set that we're going to use in our model. We're going to use the mlogit library to run our model. So let's import it at the top of this code. Now let's get our data for the fishing datasets. We'll type in data fishing, and let's say package equals mlogit. Now let's create a new data frame variable called DF, which we will assign our mlogit fishing data set to. If you haven't cleared out the variables you can do so by hitting this little broom icon in the environment section, this will clear out the objects so we don't accidentally use the existing DF variable from the binomial regression. This DF variable will represent our data frame, and we're going to assign the outcome using mlogit.data. So this is the data function from the mlogit package, refer to fishing, and the choice is going to be mode. And we'll see what this means in a second. And the shape is going to be equal to wide. And we'll also see that once we run the data frame, now let's run these lines of code. And now what we're going to do is let's see what the data frame looks like by typing in DF and running this single line of code.
>
> **[3:15](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-odds-for-multinomial-models?u=76281980&t=195)** We see the data frame as the output below, and this is the data frame that we're going to use to create our multinomial logistic regression model. And what this does is the shape of it is determined by how we're going to use the model. We'll understand more of this data set once we start working with it in [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md). In order to see how this fishing dataset works as a multinomial model, I've downloaded it to the exercise files. The fishing sheet of this workbook contains the data. To save some time, I've already set up the model that we're going to work on in the model tab. Notice we've summarized the number of individuals who prefer to fish at the beach, boat, charter, or pier. These outcomes, which are four of them, depends on the independent variable, their income level. To solve this multinomial logistic regression model. We'll first need to determine the outcome we want to use as our baseline. Let's select pier, which means we're going to set up the coefficients for beach, boat, and charter relative to our pier outcome. We'll also set up the intercept for B not, and the income, how efficient as well, B1 which works with our independent variable income. As dummy variables for us to check the calculations
>
> **[4:51](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-odds-for-multinomial-models?u=76281980&t=291)** along the way, I've already put 0.0001 in all the coefficient values in the yellow cells. But for now, we're going to use these coefficients to calculate our odds. Let's first put one in all the rows for pier. We're setting it relative to pier, which means that all the other outcomes, beach, boat, and charter will be measured relative to the outcome for pier. Now let's calculate the odds for each of these alternative scenarios, beach, boat, and charter compared to the pier. We'll first set our logit function, which equals this beach intercept plus the income multiplied by our actual income independent variable. We'll then raise this entire calculation as the exponent of the eulers number. Let's fix the row positions for the coefficients in this formula, but not the columns. Which will let us copy the formulas over. For the independent variable, in this case, for the first row of income, we want to set the column. So we'll fix column A, but we won't fix the rows so we can copy it down. Now let's calculate these odds. Just double check that we've got it properly set up. We're going to come back to work on this model,
>
> **[6:26](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-odds-for-multinomial-models?u=76281980&t=386)** both here in Excel and in R to build out the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our multinomial logistic regression model.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (6), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Prerequisites:** set up (4), install (2)
> **Versions:** 0.0001 (1)
> **Exercise Files:** exercise files (1)
> **Definitions:** means that (1)
> **Speakers:** - modeling (1)

#### [Calculating probabilities for multinomial models](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-probabilities-for-multinomial-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-probabilities-for-multinomial-models?u=76281980&t=0)** - Once we calculate the logit values for a log odds in our multinomial [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model, we can now calculate the probabilities for each of these outcomes for each income level. To do so, we're going to use the formula to convert odds into probabilities. We take the odds for a particular event and divide it by the sum of odds for all the events. In [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md), let's calculate the probabilities using the values of the odds we calculated and the columns next to it. We first calculate the pier [Probability](../../Skills/Data%20Science/Probability.md), which is equal to J3 divided by the sum of all the odds, including pier. Remember that we're calculating dummy values here, and the predicted probabilities will update once we run the model. However, we do want to make sure we set up the formulas correctly in our model first. We'll then fix bottom rows for the sum function. And we can copy these predicted probabilities for each of the outcomes for each of the income levels. We'll copy and paste the formulas. And we see our total predicted probability adds up to one. From here we can calculate the forecasted values for the predicted numbers for beach, boat, charter, and pier. We'll multiply each front of our predicted probabilities by
>
> **[1:35](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-probabilities-for-multinomial-models?u=76281980&t=95)** the total for each income level. In this case it's going to be in column F. We'll fix our F column reference so we can copy over the formula to use it in the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the forecasted values. We can see the totals at the bottom of the forecasted values aren't a whole number, but they're instead a decimal. However, we can see that we've got a total of 182 observations, which matches the total actual observed observations in our existing data that we started with.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Probability](../../Skills/Data%20Science/Probability.md) (2), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (1), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** make (1)
> **Prerequisites:** set up (1)
> **Speakers:** - once (1)

#### [Calculating multinomial log likelihoods](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-multinomial-log-likelihoods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-multinomial-log-likelihoods?u=76281980&t=0)** - To calculate the log likelihood of multinomial regression, we're going to use a formula, similar to what we saw with binomial [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md), except the log likelihood formula, will have a few more terms. In [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md), one function that will really help us out in these calculations is SUMPRODUCT. If we take the units and the unit cost for four different kinds of fruits apples, bananas, pears, and oranges. We can calculate the total cost by multiplying, the units and the unit costs together for each row, and then adding them together to get the final result. The SUMPRODUCT function lets us skip to the total cost in fewer steps without having to write them out in the same way that we did for each product. For our formula we're going to take two and put a raise, although we could take more. We'll take the units array, and the second array is the unit price. Because these arrays are the same size, we would multiply each one of the units by the unit price, except they're not separate calculations, they're all done at the same time within the SUMPRODUCT formula. In order for this equation to work, the arrays in other words, the units and the unit costs are both the same size, they are four rows by one column. It cuts us to the same final calculation, but in a much more efficient way. In Excel let's calculate the likelihood for all the outcomes in the model as a single column
>
> **[1:36](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/calculating-multinomial-log-likelihoods?u=76281980&t=96)** for the log likelihood. In the log likelihood column, let's set up the formula using SUMPRODUCT. We're then going to select all the observed outcomes in the first section of the table. Next we'll reference the probabilities. Because we're taking the natural log of the probabilities, we can actually use the LN function in Excel to reference all the predictive probabilities and take the natural log of each one of them, and use that within the SUMPRODUCT function. You can then copy these formulas down for the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the income levels, and we see that it still refers to the same values. Finally in the blue cell, at the bottom of the log likelihood column, we sum up all the log likelihoods into a single total. In the next video, we'll optimize this total log likelihood by minimizing the log loss, to solve for the coefficients, those yellow cells for the coefficients for each of the outcomes. So we can solve this multinomial logistic regression model.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (3), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Env Vars:** sumproduct (5)
> **Definitions:** in other words (1), refers to (1)
> **Cross-References:** in the next (1)
> **Analogies:** similar to (1)
> **Prerequisites:** set up (1)
> **Speakers:** - to (1)

#### [Running MLE](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-mle?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-mle?u=76281980&t=0)** - [Instructor] Once we configure our multinomial [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model as formulas in [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md), we want to use Solver to figure out the coefficients of the model that maximize the sum of the Log Likelihood. This blue cell is the sum of the Log Likelihood, similar to what we saw with the binomial logistic regression model. We want to maximize this value to make it the least negative, in other words, we're minimizing the total loss. We can do this again through our Solver. For a set objective, we'll choose our blue cell for the total Log Likelihood. We'll maximize this value, and for the changing variable cells, we'll select these yellow cells, so the intercept and the income coefficient for a beach, boat, and charter. We'll make sure that we have the Make Unconstrained Variable Non-Negative, and we'll select the GRG Nonlinear method. Once we confirm these choices, let's run the model to see what Solver gives us.
>
> **[1:14](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-mle?u=76281980&t=74)** We see our Log Likelihood as maximized, and to make sure we have the maximized value, let's run the Solver again, and we see that it updates because there are limitations to using the Solver, so sometimes, they run the Solver again just to make sure that it is indeed maximized as the objective. So now, let's take a look at the coefficients. We see the intercept and income coefficients for beach, boat, and charter as separate coefficients. We're measuring these coefficients for beach, boat, and charter relative to the pier, which we used as our benchmark observe value. We're interested in whether these coefficients are negative or positive. If they're positive, that means that as income rises, they're more likely to take a beach, boat, or charter fishing expedition than to fish on the pier, that they can afford a charter boat, to think of it that way. Now, let's solve our multinomial logistic regression model in R. We're going to add another line, which we'll call logit, and we use the mlogit function, which is from the mlogit library. We'll first set our mode to one, using the tilde, and this means we're keeping it constant. We'll then insert a pipe separator,
>
> **[2:48](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-mle?u=76281980&t=168)** and we'll refer to our income, which is the independent variable in our model. Our data is going to equal our df, so our data frame variable. Lastly, in this mlogit formula, we want to refer to the ref level, in this case, the ref level is pier, which we're comparing the other outcomes to. It doesn't matter which outcome you select as the reference level, but in this case, I just selected pier because it was listed last in the column headers, but that's not necessarily something you have to do, it's a matter of running the model correctly and understanding the context of this regression model outcome. So, run this logit code, and then, to see our outcomes, we'll select summary and refer to logit,
>
> **[3:46](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-mle?u=76281980&t=226)** and we see our coefficients. So, for this package, when we run the mlogit function, we actually see the total of the Log Likelihood. The total of the Log Likelihood is negative 1,477.2. Let's see how this looks in Excel. We see we get the same minimized loss in our total in this blue cell. Now, let's compare the coefficients, and we see that up here, this gets a but confusing to read sometimes if the Console text is a bit um. So, the estimate here is what we want to match our regression outputs, and we see that it does in our Excel file.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (3), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (3)
> **CLI Commands:** make (5)
> **Definitions:** in other words (1), means that (1), is a  (1)
> **Env Vars:** grg (1)
> **Versions:** 477.2 (1)
> **UI Navigation:** select the (1)
> **Analogies:** similar to (1)
> **Prerequisites:** configure (1)

#### [Making the predictions](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/making-the-predictions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/making-the-predictions?u=76281980&t=0)** - [Instructor] In our [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) model, we see the predicted probabilities for the income independent variable for each of the four outcomes for preferred fishing spots. We then see the forecasted values, which take the predicted profitability and multiply it by the total for each of the income levels. We calculated the predicted profitability by using the coefficients in the yellow cells to calculate the logit log odds, and then the predicted [Probability](../../Skills/Data%20Science/Probability.md) outcomes for each income level. In our R code however, we solve for the coefficients, but we haven't yet made any predictions with these coefficient parameters, that match those of our Excel model. One way to combine the predictions with our original data frame, we're going to filter the data frame DF variable, using the subset function, so it only displays the mode values, where the labels are true as a new data frame variable called df1. And we'll use equals-equals to set our condition equal to true. And we'll put true inside single quotes.
>
> **[1:16](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/making-the-predictions?u=76281980&t=76)** We see that we only see true in the column of mode in this data frame. Now we're going to run our predictions by creating another data frame variable called DF two, which will assign the predict function to. We're going to assign the predicted results to this data frame, in a similar way that we did for the binomial model, except we're going to use our multinomial results. We'll use our logit, which represents the multinomial [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) results. And we'll set new data as equal to our DF, this is our data frame. After we run these data frame variables, once we assign them, let's run them by themselves to see what their outcomes look like. We see the second result contains all the predictions. And the first result contains the filtered dataset that we saw in the previous step. We can also check the number of rows in these data frames by using the nrow function, over both these data frame variables. So we want to make sure that these are equal before we combine them. We see that the number of rows in both the variable df1 and df2 contain 1,182 rows.
>
> **[2:52](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/making-the-predictions?u=76281980&t=172)** So when we combine them, their links will match up. To combine these data frames into a single view, we can use the cbind function, which we'll assign to the outcomes variable. So the cbind function will put the two data frames together side-by-side within the same data frame, in this case, a new data frame called outcomes. And this will appear as a data table like we saw the other two DF variables, plus the previous DF variable as well. Let's type Outcomes to see what the results look like. So we see all the outcomes, but there's a lot of rows in this data frame. We can create a summary table to show the results by the income group. It makes it easier to read, similar to the Excel output, we predicted as probabilities. To do this, let's use the aggregate function, and we'll use the cbind function again. And we'll put the data frame outcomes for each of the fields, the beach, charter and pier fields as our column headers. So now let's run this code. So now what we want to do is we want to add another term, which is By.
>
> **[4:26](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/making-the-predictions?u=76281980&t=266)** We'll use this parameter in that we set up after the cbind function, so we close out the cbind function. And then we set up the labels we want to use to group by. So this is the row labels that all the previous column headers are grouping by. And we're going to call this income. And it's equal to the outcomes on the income field. And lastly, we'll set the function equal to Sum, as our third and final term in the aggregate function. Let's run this again. Once we've run these results, we see the summarized outcomes. However, we don't see the predicted column names to the outcome results, in a way that makes it easy to match to our Excel results. We see V1 V2, V3, and V4 instead of beach, boat, charter, and pier. In order to easily match our Excel results, let's use the function to rename the field names accordingly to the variable or those outcome names that we're looking for. We'll use the names function to rename these fields. So we'll say the outcome. And then the names function, we'll call this on the outcome variable. And we'll say the names of the outcomes equals-equals V1. We're going to assign this to
>
> **[6:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/making-the-predictions?u=76281980&t=360)** beach. Let's see what this looks like. Let's first assign this one, I'm going to call this Totals. We're going to assign this table for our aggregated totals. And then let's rename this Totals.
>
> **[6:26](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/making-the-predictions?u=76281980&t=386)** Now let's see what this looks like. So we see we've renamed the first column. So let's just do this for the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the columns as well. So if it's V2, it's going to be boat. And V3. We're just matching them up to the order in which they appear in the aggregate and then the cbind function. So it's going to be charter and then pier. Once we've renamed the fields for our aggregated table summary, we can compare them to our Excel multinomial logistic results. You want to look at the forecasted values, which we can see match what we see in our R code predicted results. And this is exactly what we want to see in our multinomial logistic regression model.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (6), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2), [Probability](../../Skills/Data%20Science/Probability.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Prerequisites:** set up (2)
> **CLI Commands:** make (1)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)


### 5. Working in Power BI with R

[↑ Back to Table of Contents](#table-of-contents)

#### [Running R scripts in the Power Query Editor](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-scripts-in-the-power-query-editor?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-scripts-in-the-power-query-editor?u=76281980&t=0)** - [Instructor] There are two places where we can run R scripts in [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md). We can run them in [Power Query](../../Skills/Data%20Science/Microsoft%20Power%20Query.md) editor, and we can also run them as scripts in a standard R visual. Here's the binomial [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model run with three independent variables. We're going to use this script in Power BI Desktop. We know what the outcome looks like. So first let's set up the R script and the Power Query editor, and then we'll copy and paste this script and modify it in Power BI Desktop. In Power BI Desktop, we can access the R script data connection, by selecting Get Data. Now let's search for R script, or we can find it in the other menu. We'll then select the R script data connection. In this configuration window for the R script, let's copy the R code for the binomial logistic regression model with three independent variables into this space here. I often write the script first in [RStudio](../../Skills/Software%20Development/RStudio.md), as we've already done to make sure it works properly because Power BI and Power Query editor, aren't code testing tools and so any errors we get we'll often have to go into an external application to figure out what's going on. We need to make sure we import the entire script that we need, but it's not going to be all of the script.
>
> **[1:34](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-scripts-in-the-power-query-editor?u=76281980&t=94)** We don't need the plot because we are going to let Power BI do that for us. And we don't need to order that either. Let's remove the names. And we also don't need the logic summary because that's already used in the model. So now it's connecting to the CSV file in my desktop. We hit Okay. We see the script runs. We now see a list of the outputs. In this case, let's select the DF. This is the data frame variable. Let's transform data, just to show what it can do. So we don't have to load all these rows into Power BI Desktop if we don't want to. An example is we can remove some of the information we don't need. I'm going to remove the names here and we can see it adds another step in our Query editor applied steps. Now let's load the data into Power BI Desktop, but let's just actually note this as our R script data.
>
> **[2:54](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-scripts-in-the-power-query-editor?u=76281980&t=174)** It's important to note that this is the training dataset, so in this case, we're just to see how to build the visuals. This isn't necessarily a finalized R model, but it lets you see how you can use R scripts in several ways in Power BI Desktop. Let's add a scatterplot visual, and then we're going to put the predicted outcome on the Y axis, just scroll down so we can see it. So this puts the predicted outcome on the Y axis, and we'll put the point scored. We can put any of the fields on the X axis, but let's put the points scored because that's most, the easiest to identify and tell apart. We also know we've got a scale for it. Let's then add the date as our details. So then we see the scatter plots, those points themselves appear on the chart. However, we see it looks a bit off because we have values that are over one. So we want to change the predicted, so we don't summarize it. And we'll do the same for the points the team scored. Lets actually put field goal on the details. Here we go.
>
> **[4:25](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-scripts-in-the-power-query-editor?u=76281980&t=265)** So we can see some of the details. We don't have, actually have the detail field populated, but we can see now that the range is between zero and one. I'm going to add a Y axis constant line at zero. I'll make it a light gray and a dash line, then I'll add another one as well, at one instead of zero and use the same coloring options. Let's add home and away, to our legend. So we now see two different colors on the scatter plot. Let's change the colors so they stand out a bit more. Let's select to make our, so our home games are blue. Let's make our away games an orange so they're a bit easier to see. You can also add some of the fields to the tool tips. Let's add a field goal as well as the date and the opponent. Let's also add the actual outcome. (indistinct) Now when we hover over these data points, we can see the details behind each one of the games,
>
> **[6:02](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-scripts-in-the-power-query-editor?u=76281980&t=362)** whether it was home and away, how many points they scored. Various details about the game. So right now the field labels look a bit odd because we've used the field names that are imported, which have those periods between some of the field names. I'd like for my analysis to be clean and easy to read. Changing the labels is a quick fix that can make huge impact for end-users. We can update the names by selecting the fields and typing in the label we want to have on the plot. So now we see our tool tip is updated for one. There's a lot of options that we can leverage for formatting the scatter plot, but you can see how importing the R script through the Power Query editor lets us get the data which we can then develop and design using the standard Power BI visuals, like the scatter plot. I'm going to rename this Power Query. So we know that we connected to the R script using the data source and the sheet.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Power bi](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (10), [Power query](../../Skills/Data%20Science/Microsoft%20Power%20Query.md) (5), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2), [RStudio](../../Skills/Software%20Development/RStudio.md) (1)
> **CLI Commands:** make (6), find (1)
> **UI Navigation:** select the (2), scroll down (1)
> **Env Vars:** csv (1)
> **Definitions:** is a  (1)
> **Warnings:** note that (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Running R standard visuals](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-standard-visuals?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-standard-visuals?u=76281980&t=0)** - [Instructor] We can think of setting up the R scripts in [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) editor as a global solution, but that's not the only place we can configure our models within Power BI. We can also explore configuring them locally using the R visual that's available within the standard visualization options. It's important to note that if you're configuring in R visual, your data doesn't necessarily need to come from R, either. You can set up the R visual through any data connection type. Let's set up a CSV file to the same data we used in the R model. Let's select to transform data before we load it. So if we scroll over, we can see that the names of the fields have spaces in them, which we'll account for in our R script when we set the visual. Let's load our data into Power BI after we rename it CSV data. Let's create a new sheet which we'll call R visual, or we'll just call it visual. One of my own preferences when setting up the R visual is to first set it up as a table that I can add the fields to, and then I'll convert it into an R visual. So I'll select the table visual. We'll add the points team scored, not to summarize it, add the win-loss,
>
> **[1:37](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-standard-visuals?u=76281980&t=97)** or hang on. We can swap this out. We want to use the CSV file data instead.
>
> **[1:53](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-standard-visuals?u=76281980&t=113)** So we add the points scored and the actual outcome, the win or loss, to the table. We're not going to include the predicted field because it doesn't exist in the visual yet. We're going to actually run the model directly in the visual. Select not to summarize either of these fields. So we're going to run our logit model in the R visual, instead of in the [Power Query](../../Skills/Data%20Science/Microsoft%20Power%20Query.md) editor, when we used it, when we got the data using R script. So we've made sure that we're not aggregating any data points. Now let's convert this visual into an R script visual. We see, we have message. If we click on this, it says, Do we want to enable the script visuals? We select enable. And now we'll add our code below. We have to select the visual in order for the R script editor to appear. When we created this visual, a script automatically ran that was a preamble that set up the dataset and added the fields to it. Now let's get our R code that produces a line chart on top of a scatter plot from [RStudio](../../Skills/Software%20Development/RStudio.md). I'm going to copy all this code and then edit it in the visual. Once we've copied it into the space, we want to remove some of the fields we don't need. We don't need to include the script for getting the data because the data's already in
>
> **[3:27](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-standard-visuals?u=76281980&t=207)** the visual itself. So can remove that. You also don't need the summary for the logit because that's not actually part of the visual. To render the R script in R visual, we need to run it by hitting the play button in the top right of the script editor. So we see it doesn't run. If we click on this hyperlink, we see we have an error message that says that it could not coerce class function to a data frame. So let's look at our details to figure out what's going on. So we're going to go back into the R code to modify it. Notice there's a preamble script that always runs when we create the visual. You may notice that it uses a dataset variable containing the variables we added to the table. This is the name of our data frame we're running in the R script in Power BI inside this visual, instead of the df name, the data frame variable, that we gave to the variable in RStudio. Power BI automatically assigned it for us, which is why it's automatically set up at the top of the code. We can actually modify the data set name, but let's modify the references below to refer to the dataset instead of df.
>
> **[4:59](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-standard-visuals?u=76281980&t=299)** Now let's run the code and we still see an error message. Let's see what it says on this sample. So it's not able to identify what we're running in the glm model. And the reason for this is that we don't have the names set up correctly for the actual columns. Let's swap out the respective fields from the R script data with our CSV file data and use the names according to what they appear as within this visuals preamble. So let's copy the points the team scored and we'll replace it in the code. And we'll use this little back tick within our script so it properly runs. I'm going to call this the outcome instead of the name it already has.
>
> **[6:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-standard-visuals?u=76281980&t=360)** Once we do that, we want to update the name of the win and loss as well. And the reason we use back ticks is this allows us to have spaces in the names of the fields.
>
> **[6:19](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/running-r-standard-visuals?u=76281980&t=379)** So now let's run our script. Let's see what the message says. So it ran up through where it created the predicted new field that it added to the dataset. See what we need to update here. We need to update our df2 data set. Once we've made these updates, we see that the R script runs and it produces the exact same visual we created earlier in RStudio. If you add a new field to your model, you'll need to update the preamble that contains the assignments for the dataset variable. The visual doesn't actually update with the new field name, but you'll have to add it in manually in the first line of the dataset variable. You can collapse the view so we now have a much clearer view of what the model looks like. However, one thing you may notice is that we hover over the graph, we don't see the tool tips unlike we did in the scatterplot visual. This is because R script visuals don't natively add tool tips like they do in the standard power BI visuals, so we don't see the details as we hover over the line or the scatter plot points.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Power bi](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (6), [RStudio](../../Skills/Software%20Development/RStudio.md) (3), [Power query](../../Skills/Data%20Science/Microsoft%20Power%20Query.md) (1)
> **Prerequisites:** set up (5), configure (1), you'll need (1)
> **Env Vars:** csv (4)
> **UI Navigation:** select the (2), click on (2)
> **Cross-References:** earlier in (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Interacting between visual components](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/interacting-between-visual-components?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/interacting-between-visual-components?u=76281980&t=0)** - [Instructor] We might ask why we would want to use the [Power Query](../../Skills/Data%20Science/Microsoft%20Power%20Query.md) editor versus the R visual to run R scripts. We could even create a model that goes in the database that we can connect to. There's no right answer and what you choose to select to do depends on the situation in the context in which you're trying to use your model and act upon its outputs. Let's say we add slicers to the existing [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) report, which we'll do to the page that contains the R visual. We'll then add the Season Start Year as the field. Let's select to display this as a list, and we can make the items a bit bigger.
>
> **[0:50](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/interacting-between-visual-components?u=76281980&t=50)** Notice that if we select some of the years, it impacts the model output. We see the model output, not only does the line change, but the points displayed, those orange dots change as well. Let's copy this slicer and put it on the Power Query page. Let's select to not sync them for now. So what we see here is that the model changes differently because we ran the R script in the Power Query editor. And so the logit model was run in the query editor it's not impacted by the slicer selections on the page. The predicted outcomes are already determined before they're loaded into the model. The filters only impact what data points are displayed, not how the calculations are done. In the R script visual, the filter not only impacts the data points, but it also impacts the calculations that go into the sigmoid curve. We can see that selecting a smaller season, not only impacts the data points for the actual outcomes, but we see it changes the blue line shape as well. Now let's upload our work to Power BI service. We first want to save it. We'll then select Publish. And this connects to the workspace we're logged into.
>
> **[2:23](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/interacting-between-visual-components?u=76281980&t=143)** Let's select ESPN Analysis. Now let's go into the Power BI service to see what it looks like. Here's our ESPN Analysis workspace, we see the Report and Dataset have been uploaded. We click on the link, we can interact with it in the same way we did for the desktop file. Conversely, we also see the view for the Power Query script updated and uploaded into the Power BI service as well. There are some limitations when leveraging R scripts and Power BI both in the desktop version and here in the Power BI service. You'll want to make sure your R scripts are compatible with R version 3.4.4 or the most recent version that works. If you have questions on this documentation, please review the [Microsoft Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) documentation online for the most recent updates. It's also important to look at the limitations for what you can share. Power BI doesn't let us publicly share R script visuals for example, at least at the time of filming for this course. R visuals and R scripts also may not work with certain workspaces or security configuration. So it's important to research this before deciding to use an R script and share it in the Power BI service.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Power bi](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (8), [Power query](../../Skills/Data%20Science/Microsoft%20Power%20Query.md) (4), [Microsoft Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (1)
> **CLI Commands:** make (2)
> **Env Vars:** espn (2)
> **Versions:** version 3 (1), 4.4 (1)
> **UI Navigation:** click on (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Moving into Power BI](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/challenge-moving-into-power-bi?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/challenge-moving-into-power-bi?u=76281980&t=0)** (bright upbeat music)
>
> **[0:05](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/challenge-moving-into-power-bi?u=76281980&t=5)** - [Instructor] Your challenge for this chapter is to take one of the previous chapters challenges for [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) modeling and bring it into [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md). You can choose to leverage either the binomial, multinomial or ordinal logistic regression models. In Power BI, you can either set up the model in the [Power Query](../../Skills/Data%20Science/Microsoft%20Power%20Query.md) Editor or by running code in the standard R visual.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (2), [Power bi](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (2), [Power query](../../Skills/Data%20Science/Microsoft%20Power%20Query.md) (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright upbeat music) (1)

#### [Solution: Moving into Power BI](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-moving-into-power-bi?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-moving-into-power-bi?u=76281980&t=0)** (upbeat electronic music)
>
> **[0:05](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-moving-into-power-bi?u=76281980&t=5)** - In my solution. I'm going to update the [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) Desktop model to show a different independent variable within the [power query](../../Skills/Data%20Science/Microsoft%20Power%20Query.md) R script. We'll select to open the Query Editor again, we then double click on the Source Step, which opens the R script and a pop-up window again. Let's remove the Field Goal from the GLM function in the second line. Now let's load our data again, to see how it looks, let's remove our filter selection. We now see there are two distinct plots that separate the Home games. In this case which is represented with blue compared to the Away games represented in orange, zero labels. One way we can add interaction in Power BI is by creating a way to select between the models displayed. There are a few ways to do this, but one way is by updating the query again, within the Power Query Editor to model this scenario. This is more of a Power BI trick than anything else, but I think it's a neat one. I'm going to again, select the R script data. And we're going to add a column to our query, which we'll call Variables Include. And I'm going to say this is Home or Away games, and we'll see why in a few steps. Let's also add a conditional column
>
> **[1:44](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-moving-into-power-bi?u=76281980&t=104)** called Home or Away. It says if Home or Away equals one, let's make this Home. And we want to put our alternative result as Away in the Else clause. So now see that the ones are Home and the zeros are Away. Now I'm going to duplicate this query. By right clicking, on the name of it and let's just rename it. We'll leave it with the suffix (2) at the end. And this one's only going to have one independent variable. So we'll want to go into the R script again by double clicking on the Source. And we'll remove the Home or Away from the GLM function in the second line. So this updates are logit variable in the R script. And we'll double click on the Added Custom Step. And this is going to say All Games instead of Home or Away games, because we can't differentiate between the Home or Away in this particular query. We'll also update our conditional column to say that both of the results are All Games and this isn't necessarily the most efficient way to do this, but in the sake of time and because of the data set's not really a big.
>
> **[3:19](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-moving-into-power-bi?u=76281980&t=199)** This is an easy way to do it that's consistent with both of the queries because we're going to combine them as we see in the next step. Now let's combine the two queries together by selecting the R script data, because this already loads into our scatterplot visual. We'll then select the Combine option in the Home dropdown menu. I'm going to select Append Queries. And in this case, I'm going to select the second R scripts data with the (2) suffix on the end of it. And we see this combines the data sets. We scroll down a bit, we'll see the second data set below. We see All Games and then change the data type to text for consistency. Now, let's load our data into Power BI Desktop again. Okay, so we got a message about duplicate value. So. Let's double check we want to actually remove the join between the table here. We don't want our tables to be connected. They're just separate data sources. They're not connected tables. The model. Now let's go back into our Home menu on our Report page and Refresh the data sets. So we now see a third line effectively on our scatterplot visual. And what this represents is all the games.
>
> **[4:53](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-moving-into-power-bi?u=76281980&t=293)** So it's kind of the midpoint between them. So we're going to update our legend to use the new field we added, which is Home or Away instead of the current field. So this is the same results are very similar to them, except we're using something that's a bit more clear in the naming convention. So we see the colors update. So we have a lighter blue line in the middle that represents, all the games and a darker blue line that represents, Away games and an orange line that represents, Home games. So, if we only wants to see one of these views. Let's update our slicer to use the field we just created before we appended the query. So this is going to be the Variables Include. We're going to use that as our field and we'll remove the season start year. Now, when we select one of these choices, if we select all the games, we see a single line in the view as the sigmoid curve. If we select Home or Away Games, we see the line splits into two. So this is a way to interact with a [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) model by effectively letting the end user select which view they want to see. They can see all the games, which means that we don't separate the Home or Away independent variable as part of the model, or we can select to include the Home or Away independent variable in the model.
>
> **[6:26](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/solution-moving-into-power-bi?u=76281980&t=386)** And we see it update it in the scatterplot.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Power bi](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (4), [Power query](../../Skills/Data%20Science/Microsoft%20Power%20Query.md) (2), [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (1)
> **UI Navigation:** click on (2), select the (2), open the (1), dropdown (1), scroll down (1)
> **Definitions:** is an  (1), is a  (1), means that (1)
> **Env Vars:** glm (2)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **Analogies:** similar to (1)
> **Speakers:** - in (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps with logistic regressions](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/next-steps-with-logistic-regressions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/machine-learning-with-logistic-regression-in-excel-r-and-power-bi/next-steps-with-logistic-regressions?u=76281980&t=0)** - [Helen] Now that you've learned about model and [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md), you can build out your knowledge from here. Try out the models on some of the other built-in datasets in R, or find examples of logistic regression you can apply on your own data. Logistic regression is a big topic area. There's a lot more to learn about binomial and multinomial models, as well as starting to explore ordinal models, which I touched on slightly in this course. If you want to learn more about AI and machine learning models, as well as statistical methods, check out some of the other courses in the library on these topics, like [Microsoft Excel](../../Skills/Data%20Science/Microsoft%20Excel.md): Using Solver for Decision Analysis, R for [Data Science](../../Topics/Data%20Science.md): Lunchbreak Lessons, [Power BI- Integrating AI](Power%20BI-%20Integrating%20AI.md) and Machine Learning, Data [Dashboards](../../Skills/Data%20Science/Dashboards.md) in [Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md), and Advanced [Microsoft Power BI](../../Skills/Data%20Science/Microsoft%20Power%20BI.md). I'd love to stay connected, and hope you learn more about logistic regression. Please follow me on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) to see more content on these topics.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Logistic Regression](../../Skills/Software%20Development/Logistic%20Regression.md) (4), [Power bi](../../Skills/Data%20Science/Microsoft%20Power%20BI.md) (2), [Microsoft Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1), [Data Science](../../Topics/Data%20Science.md) (1), [Dashboards](../../Skills/Data%20Science/Dashboards.md) (1)
> **CLI Commands:** find (1)
> **Definitions:** is a  (1)
> **Speakers:** - [helen] (1)


## Instructor

- [Helen Wall](../../Instructors/Data%20Science/Helen%20Wall.md)

## Resources

- Exercise files available

## Skills Covered

- Machine Learning
- R (Programming Language)
- Artificial Intelligence (AI)
- Microsoft Power BI
- Microsoft Excel
- Logistic Regression

## Path Context

### In [Master Advanced Excel Data & Analytics Skills](../../Paths/Data%20Science/Learning%20Paths/Master%20Advanced%20Excel%20Data%20%26%20Analytics%20Skills.md)
← [From Excel to SQL](From%20Excel%20to%20SQL.md) | **6 of 7** | [Excel- Economic Analysis and Data Analytics](Excel-%20Economic%20Analysis%20and%20Data%20Analytics.md) →

## Appears In

- [Master Advanced Excel Data & Analytics Skills](../../Paths/Data%20Science/Learning%20Paths/Master%20Advanced%20Excel%20Data%20%26%20Analytics%20Skills.md)

## Related Courses

_Courses sharing skills:_

- [Advanced Python in Excel- Machine Learning](Advanced%20Python%20in%20Excel-%20Machine%20Learning.md) — Artificial Intelligence (AI), Machine Learning, Microsoft Excel
- [Machine Learning with Python- Logistic Regression](../Artificial%20Intelligence%20(AI)/Machine%20Learning%20with%20Python-%20Logistic%20Regression.md) — Artificial Intelligence (AI), Machine Learning, Logistic Regression
- [Power BI- Integrating AI](Power%20BI-%20Integrating%20AI.md) — Artificial Intelligence (AI), Microsoft Power BI, Machine Learning
- [Complete Guide to Excel Statistics with Copilot](Complete%20Guide%20to%20Excel%20Statistics%20with%20Copilot.md) — Artificial Intelligence (AI), Microsoft Excel
- [Machine Learning and AI Foundations- Clustering and Association](../Artificial%20Intelligence%20(AI)/Machine%20Learning%20and%20AI%20Foundations-%20Clustering%20and%20Association.md) — Artificial Intelligence (AI), Machine Learning

---

[↑ Back to top](#)