---
type: course
cssclasses:
  - lle-course
  - lle-level-advanced
slug: advanced-sql-logical-query-processing-part-2
url: "https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2"
duration_minutes: 127
duration: 2h 7m
level: Advanced
updated: 12/4/2024
learners: 70428
skills:
  - SQL
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C4D0DAQGzhdYQOA6pDw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1600371493000?e=2147483647&amp;v=beta&amp;t=j16eNmCAb2IkLECpQhb4ARcIFu-l0Ds-2Q6YE9xZl0k"
linkedin_topic: Data Science
learning_paths:
  - '[[Master SQL Development]]'
prev_courses:
  - '[[Advanced SQL- Logical Query Processing, Part 1]]'
next_courses:
  - '[[Advanced SQL – Window Functions]]'
path_nav: '[{"path":"Master SQL Development","position":3,"total":8,"prev":"Advanced SQL- Logical Query Processing, Part 1","next":"Advanced SQL – Window Functions"}]'
path_count: 1
tags:
  - course
  - topic/data-science
  - topic/database-management
  - topic/software-development
  - skill/sql
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Data%20Science/Advanced%20SQL-%20Logical%20Query%20Processing%2C%20Part%202.md)

![Advanced SQL: Logical Query Processing, Part 2](https://media.licdn.com/dms/image/v2/C4D0DAQGzhdYQOA6pDw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1600371493000?e=2147483647&amp;v=beta&amp;t=j16eNmCAb2IkLECpQhb4ARcIFu-l0Ds-2Q6YE9xZl0k)

# Advanced SQL: Logical Query Processing, Part 2

> Without an understanding of the underlying concepts behind SQL, developers will encounter a number of challenges that slow down their workflow. This course—the second installment in a series—was designed to help you avoid these common pitfalls and understand SQL's processing limitations. Join instructor Ami Levin as he continues to delve into logical query processing. Learn how expression, table, 

> [LinkedIn Learning](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2) | 2h 7m | Advanced | 70K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Course introduction](#course-introduction)
  - [Agenda](#agenda)
  - [Setting expectations](#setting-expectations)
- [**1. Subqueries and Set Operators**](#1-subqueries-and-set-operators) (4 videos)
  - [Subqueries](#subqueries)
  - [Set operators](#set-operators)
  - [Challenge](#challenge)
  - [Solution](#solution)
- [**2. Advanced Joins**](#2-advanced-joins) (4 videos)
  - [Self and inequality joins](#self-and-inequality-joins)
  - [Lateral joins](#lateral-joins)
  - [Challenge](#challenge)
  - [Solution](#solution)
- [**3. More on Grouping**](#3-more-on-grouping) (4 videos)
  - [Ordered set functions](#ordered-set-functions)
  - [Grouping sets](#grouping-sets)
  - [Challenge](#challenge)
  - [Solution](#solution)
- [**4. Recursions and Cursors**](#4-recursions-and-cursors) (2 videos)
  - [Recursions](#recursions)
  - [The cursors curse](#the-cursors-curse)
- [**Conclusion**](#conclusion) (1 videos)
  - [Conclusion](#conclusion)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Course introduction](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/course-introduction?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/course-introduction?u=76281980&t=1)** (upbeat uplifting music)
>
> **[0:14](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/course-introduction?u=76281980&t=14)** - [Instructor] I recently published a course titled "Advanced [[SQL]] Logical Query Processing." You liked this course so much that you've been asking me in the Q and A section, in the feedback and in personal messages for more. And the truth is, I was planning to do a part two anyway. Leaving good materials from part one on the editing room floor is a huge incentive for people like me to do a sequel. Pun intended. So it's with great pride and joy that I present to you "Advanced SQL Logical Query Processing Part Two." You will learn new techniques and language constructs, subqueries, set operators, lateral joins, grouping sets, recursive WITH clauses, ordered set functions, and more. You will learn how and when to use them, and I'll warn you of the common pitfalls. It'll be educational and fun. Well fun, at least, for me. If you haven't watched part one yet, please go do it now. Go, go, don't worry; I can wait. I'll be here when you come back. I promise. (upbeat uplifting music)

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SQL]] (2)
> **Env Vars:** sql (2)
> **Non-Speech:** (upbeat uplifting music) (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Agenda](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/agenda?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/agenda?u=76281980&t=1)** - [Ami] I suggest you get yourself some coffee, tea, water, soda, scotch, or snacks, whatever floats your boat. We have a long way to go and I need you fully focused and undistracted. The introduction is what we're doing right now. Hi, nice to meet you. I'm Ami. I think we've met before, didn't we? I'll give you three guesses what will be in our first chapter? You got it. Subqueries are the epitome of [[SQL]] composability. We will learn about scalar row and table expression subqueries and I'll show you a neat trick that'll help you remember how correlations work. Set operators are one of my favorite features of SQL, extremely powerful yet easy. And unfortunately, I don't see them used often for reasons that are beyond me. Our second chapter will be about advanced joins. You'll learn about self joins and how they're used to traverse graphs. We'll cover some non-equality predicates and see some practical use cases. Lateral joins are somewhat of an exception to the normal processing operation of join but nothing groundbreaking. After the subquery chapter, they should not be too much of a challenge. Chapter three will blow your mind with a feature called grouping sets, yet another extremely powerful feature that I rarely see used. Ordered set functions are standard aggregate functions
>
> **[1:36](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/agenda?u=76281980&t=96)** with a twist. Their main use is for [[Statistical Analysis]]. So they may not appeal to the less stats-savvy among you. Recursions are considered black magic by many. So we'll break these apart and reveal their essential simplicity and efficiency. I will spend the last section ranting about cursors and how ANSI would have done the world a whole lot of good by banning them from SQL. It will be fun. For dessert, we will briefly review what we've learned. I'll solicit your feedback and give you some advice about which of my other courses to watch. And if you'll be nice, maybe, maybe I'll even recommend some other authors. We'll see about that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SQL]] (3), [[Statistical Analysis]] (1)
> **Env Vars:** sql (3), ansi (1)
> **Speakers:** - [ami] (1)

#### [Setting expectations](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/setting-expectations?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/setting-expectations?u=76281980&t=1)** - [Instructor] So, joking aside, I really meant it when I said you should watch part one first. Even if you watched it just a few weeks ago, but don't feel 100% confident or don't remember everything, it really won't hurt to go through the chapters you found challenging one more time. I'm not going to repeat the materials from part one. And this also includes the introductory chapters about the terminology, the demo database, using the code files, the [[GitHub]] repo and DB fiddle. But we must keep things interesting, so there are a few exceptions. First, I'm going to use my new favorite IDE, [[Microsoft]]'s open source, [[Microsoft Azure|Azure]] Data Studio. I like that it has a minimalist user interface and that it supports both [[Microsoft SQL Server|SQL Server]] and [[PostgreSQL|Postgres]] so I can show you the differences easily side by side. It's an awesome tool, give it a try. In part one, I typed most of the demo queries as I was demoing them. Now, they were relatively short, but now we're going to use more complex and longer queries. So I reserve the right to copy paste the code sections to save valuable time. I will highlight the important sections, but I'm not going to spoon feed you each character. And if you find it harder to follow, feel free to pause the video as frequently as you need and read the code at your own pace. Second, as promised in the introduction video,
>
> **[1:36](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/setting-expectations?u=76281980&t=96)** I have turned the difficulty level up one notch. No pain, no gain. Third, get ready for a bit more of relational theory, not much. In part one, I touched briefly on some aspects of the relational model but now we're going to take it a little bit further and I'm going to give you the broader picture. I'm sure this will help you remember the subtleties much better. The key to success is understanding, not memorizing. So, be patient with the theoretical parts, it will pay off. Forgive me students for I have sinned. In previous courses, I've used the term common table expressions or [[CTE]]. I recently had the pleasure of reading the ANSI [[SQL]] standard for this feature, and was surprised to learn that the term that I've been using all these years simply isn't correct. Although SQL Server, Postgres, and a few major vendors use it, the correct term according to the standard is the [[CTE|with clause]]. So that's what I'm going to use from now on. And this isn't all, but I've spent enough time for the introduction, so we'll cross the remaining bridges when we reach them. Now, let's get down to business.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft SQL Server|Sql server]] (2), [[PostgreSQL|Postgres]] (2), [[GitHub]] (1), [[Microsoft]] (1), [[Microsoft Azure|Azure]] (1)
> **Env Vars:** sql (3), ide (1), cte (1), ansi (1)
> **CLI Commands:** find (1)
> **Tools:** github (1)
> **Exercise Files:** github repo (1)
> **Analogies:** picture (1)
> **Speakers:** - [instructor] (1)


### 1. Subqueries and Set Operators

[↑ Back to Table of Contents](#table-of-contents)

#### [Subqueries](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/subqueries?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/subqueries?u=76281980&t=1)** - [Tutor] Subqueries specify a table expression, a row expression, or a scalar expression. They're all derived from a query expression. In simple terms, it's a query that returns either a table, or a row, or a single value. A subquery may contain any clause except for order by, which is only allowed when used in conjunction with a limit or an offset clause. We'll see an example. Subqueries return a set, and a set has no order. A table subquery may return any number of rows and columns, and I will use the term derived table to refer to table expression subqueries. A row expression subquery may return only one row with one or more column. And lastly, a scalar subquery may return only one row and only one column, and that's why we can treat it as if it was a scalar expression. Trivia fact, a scalar is an element of a field which is used to define a vector space. A quantity described by multiple scalars, such as having both direction and magnitude, is called a vector. Logical query processing works exactly the same way for all types of subqueries. The same as normal queries, with a few exceptions. Each of the three subquery types can be
>
> **[1:36](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/subqueries?u=76281980&t=96)** either correlated or non-correlated. Non-correlated subqueries are standalone queries. They are independent of the query that they are contained in, which is often called their parent or the outer query. Correlated subqueries do not stand alone. They include an expression that references a value from the outer or parent query. Example. This sub query has no dependency or correlation to the parent query from FooBar. It stands by itself and returns the same result for all rows of the outer query. Although average foot will be returned for each row of Foobar, most [[Databases]] will execute it just once and use the result for all rows to improve performance. But once per row or once per statement, it doesn't really matter. You can think of it any way that makes you happy. This sub query is correlated. There where predicate references Foo, a value that does not exist in the scope of the subquery. The result of this subquery depends on the current outer query row value of Foo, so the database must execute it again for each and every row. Well, if databases were a bit smarter,
>
> **[3:09](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/subqueries?u=76281980&t=189)** there could have executed the sub query for each unique value of Foo, but things are not always as simple as they seem. We need to show all adoptions, with their fees, the maximum fee that was ever paid, and the discount of the current fee from the maximum in percent. Select max adoption fee from adoptions finds the maximum fee ever. Now we can take it, put it in parentheses, and use that as an expression subquery inside the outer query that selects all rows from adoptions. These are two independent queries. The outer query select all adoptions, and the sub query always returns the max fee. It also happens to be from the same adoptions table. The maximum fee of $100 is returned for all rows. The discount percent for X from Y is calculated as Y minus X multiplied by a hundred and divided by Y. Now, Y is the max, and it appears twice in the formula, so we must repeat the sub query twice more. Let's execute, and we can see that Patrick got a 42% discount. He paid only $58 instead of a hundred. Diane came in driving a Ferrari and got no discount.
>
> **[4:45](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/subqueries?u=76281980&t=285)** Now we need to calculate the maximum fee for each species instead of overall. We need to correlate each fee to their respective adoption rows. And as promised, here is a cool way to visualize how correlations work. For dogs, we need a where that filters only for dogs. For cats, the literal value needs to be changed to a cat, and the same for rabbits. Now, instead of using static literal filter values, we can use a reference from the outer query so that the value of the filter will dynamically change based on the species for the current row from the outer query. Since both the query and the subquery select from the same table, we must provide unique aliases to distinguish between the species of the outer query, A1 species, and that of the subquery, A2 species. At runtime, A1 species will be replaced with the species from the outer row, and the max from A2 will be picked among the rows only for that same species in the second instance of the table A1. Next we need to show all attributes for people who adopted at least one animal. Persons are identified by their email,
>
> **[6:19](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/subqueries?u=76281980&t=379)** and their attributes are stored in the person's table. Select star from persons reveals we have 120 persons total. Select star from adoptions reveals we have 70 adoptions. You already know how to use an inner join between persons and adoptions to do that filter. Select from persons inner join adoptions on adopter email equals email will give us the correct result. Just don't forget the distinct set quantifier, or you will get the same person multiple times in case they adopted more than one animal. Great, we have 49 adopters in total. Another technique to do that would be to add a filter. Select from persons where email in, select email from adoptions, and here is a triple bonus points mini challenge. Let's execute this query and oops, something is wrong. We get all 120 persons back. How is that possible? This is a sneaky, hard to find, bug. Take more than one minute for this one. Pause the video for a few minutes and see if you can find the bug. If you found it, pat yourself twice on the back, because few people do.
>
> **[7:52](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/subqueries?u=76281980&t=472)** And the easiest way to show it is to execute the subquery by itself. And we get an error that email is an invalid column. There is no email column in adoptions. The email in adoptions is called adopter email. So how come this query ran successfully? When evaluating column references of a sub query, the database first tries to match aliases in the scope of the sub query. If it can't find a match, it will assume that this is a correlation to a column from the outer query, and will try to match it there. What happened here is that when email was not found in adoptions, the email from persons was used. Since the end predicate compares the same email to itself, it will always be true, and all persons are returned. Be very careful with aliases in subqueries. This bug has bitten many smart and experienced developers. The correct subquery is select adopter email from adoptions, and with this one, we now get the correct result of 49 adopters. Now, for another cool predicate called exists. Exists is typically used in the where clause, and is followed by a correlated subquery.
>
> **[9:28](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/subqueries?u=76281980&t=568)** It is evaluated for each row and the rows for which the sub query we turns at least one row will evaluate to true. Nothing prevents us from using a non-correlated subquery in exists, but it doesn't make much sense, since, if we do that, all rows will either evaluate to true or false. For example, where exists select from adoptions where species equal dog will return all 120 persons. The subquery always returns at least one row, since we do have dogs that were adopted, and the predicate will evaluate to true for all rows from the outer query. But if we change dog to elephant, all rows will evaluate to false. Exists only makes sense when it's evaluated per row. So that's why we need the correlation. To find people who adopted animals using exists, we will correlate their emails. That's alias persons as P, adoptions as A, and correlate where A adopter email equals P email. Let's execute, and voila, our 49 adopters show up. One more thing. Some find it confusing that I write a select null inside the exist sub query. The truth is that it doesn't matter. You can write null, star, or your full name.
>
> **[11:04](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/subqueries?u=76281980&t=664)** The select clause doesn't return anything when it's used with exists, it's there just to make the syntax valid. The reason I use nulls is mostly historical. Back in the days when [[Microsoft SQL Server|SQL server]] was still Sybase, this actually had a performance impact, as the expressions were evaluated by the database, even though they weren't going anywhere. It also serves as a visual indicator that the subquery is part of an exists. Old habits die hard.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Databases]] (2), [[Microsoft SQL Server|Sql server]] (1)
> **CLI Commands:** find (5), make (2), cat (1)
> **Definitions:** is a  (4), is an  (2), is called (2)
> **Analogies:** such as (1), for example (1)
> **Env Vars:** sql (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [tutor] (1)

#### [Set operators](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/set-operators?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/set-operators?u=76281980&t=1)** - [Instructor] You've heard me often complain about features of [[SQL]] that are being abused. Set operators are on the opposite side. They seem to be underutilized, and that's a shame, because they're one of the most powerful features of SQL, yet they're very easy to use and understand. The ANSI SQL standard defines three Set operators; UNION, EXCEPT and INTERSECT. Each of these has two variants; ALL and DISTINCT. Many of you have used or at least heard of UNION and UNION ALL, but did you know that UNION is just a shortcut for UNION DISTINCT? And how about EXCEPT or INTERSECT ALL? All means that the result may contain duplicates. The default is DISTINCT, which groups or eliminates duplicates. Side note: The DINSTINCT set quantifier used in the SELECT clause also has an old variant. For SELECT, ALL is the default. And you can try it, SELECT ALL * FROM Foo, and see for yourself. Moreover, the ANSI SQL standard defines an additional MULTISET variant for all Set operators. But as far as I know, MULTISETS are supported only by Oracle, so we're not going to deal with them here.
>
> **[1:34](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/set-operators?u=76281980&t=94)** Sub queries were processed in the context of a parent query. Set operators work differently. They stand between two independent queries that are processed prior to the Set operator itself. Set operators result in a table expression that can be either a Set or a Multiset based on the data, queries and the operator that we use. The result can be used with additional Set operators as a derived table and a FROM clause, or anywhere else where a table expression is allowed. It's important not to confuse Set operators with Joins. Joins combine two table expressions horizontally. The result rows depend on the type of Join, but may contain attributes from one or both sources. This isn't the case with Set operators. Set operators combine two table expressions vertically. The result consists of the matching attributes from both sets, and typically these are the same attributes. Email and breed don't make sense in this context, as they are unrelated attributes. You can't say show me customer emails except for the colors of the rainbow. Well, you can, but you might find yourself being rushed to the ER with a suspected stroke. UNION ALL returns all elements
>
> **[3:07](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/set-operators?u=76281980&t=187)** from the blue and yellow sources. The green result Multiset. Blue is also a Multiset as it contains two airplanes. The result contains both yellow and blue airplanes, and their respective colors represent their origin. UNION DISTINCT or just UNION, eliminates duplicate airplanes. The result contains one airplane, and I painted it black since we can no longer tell if it came from the blue or the yellow set, it came from both. You can think of it as a grouping operation similar to group by, and that will be very close to the truth. INTERSECT returns elements that are common to both sets, and here, only airplanes exist in both the blue and the yellow sources. There is a tricky and somewhat unintuitive aspect to INTERSECT ALL. The ANSI SQL standard defines that when an element exists M times in one source and N times in another, INTERSECT ALL returns the minimum of M and N. Here we have two airplanes in blue and one in yellow, and the minimum of two when one is one, so one is returned. If there were two airplanes in yellow, two would have been returned. But even if there were three in yellow, the result would still have only two.
>
> **[4:44](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/set-operators?u=76281980&t=284)** The intersection is the number of overlapping elements. And the minimum of two and three in this case is still two. The only [[Databases]] that I know of that currently support INTERSECT ALL are [[PostgreSQL]] and MariaDB. So if you're not using either of these, don't worry about it, you only have INTERSECT DISTINCT. INTERSECT DISTINCT or just INTERSECT is supported by most mainstream databases. It eliminates the duplicates and returns just one airplane, regardless if there were two or 2,000 in one or both sets. If an element exists in both sets no matter how many times, the result will have one of it. EXCEPT ALL returns the ship and the spaceship, which exists in the blue set, but not in the yellow. And here too, there is a tricky, somewhat less intuitive aspect. The result of EXCEPT ALL contains one blue airplane as well. The ANSI SQL standard defines that when an element exists M times in one source and N times in the other, EXCEPT ALL returns either M minus N, or zero. If we had two yellow airplanes, the result would have none. Like INTERSECT, the only databases that I know of that currently support EXCEPT ALL
>
> **[6:20](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/set-operators?u=76281980&t=380)** are PostgreSQL and MariaDB. So if you're not using either, don't worry about it, you only have EXCEPT DISTINCT. EXCEPT DISTINCT or just EXCEPT is widely supported and is much more intuitive. It returns one of each of the blue elements that don't exist in yellow. And here, it doesn't matter if there are two, three or 5000 airplanes, it's enough that one exists in yellow for the result not to have any. Even if we had two or 20 blue ships, the result would still contain only one. Out of the three, EXCEPT is the only directional set operator. So if we swap the sets around, yellow EXCEPT DISTINCT blue is only the motorcycle, yellow EXCEPT ALL blue is still the motorcycle. Set operators treat NULLS as not being distinct from each other. This is the same way as grouping treats NULL, not the way where predicates do. And you'll see that this is a huge advantage in some cases, as will be evident in this chapter's challenge. If you don't remember the difference between expressions being equal to each other versus being not distinct from each other, go back and watch part one, chapters three and four again. Here it means that NULLS are treated
>
> **[7:57](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/set-operators?u=76281980&t=477)** just like any known value. A UNION ALL of two NULLS will result in two NULLS. A UNION DISTINCT will result in just one exactly like a known value, and the same is true for both variants of EXCEPT and INTERSECT. Let's see a code example. We need to find animals that were not adopted. The relational operator that corresponds to this request is called an anti-join. [[Microsoft SQL Server|SQL Server]] documentation wrongly calls it an Anti-semi join, and they invented a term that doesn't exist in relational algebra. It's a hybrid between an anti-join and a semi-join. A semi-join represents a join that returns attributes from only one source, and the second source is used as a filter. This represents animals that were not adopted, Anti-semi join doesn't sound very politically correct, and it's doubly so for an erroneous term. SQL offers several ways to perform anti-joins. One technique is to use an OUTER JOIN and a filter only for rows that didn't qualify. Where, name, from adoptions is NULL. Don't forget that with this technique, we need a distinct set quantifier in the select. The key of the adoptions table is name, species,
>
> **[9:35](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/set-operators?u=76281980&t=575)** which represents the animal and the adopter. This means that the same animal can be adopted more than once. It may be adopted, returned, and then adopted again by a different person. The sample data doesn't contain any such animals, but we should always consider the possibility, because if we have any in the future, the same animal would be returned once per adoption. Always validate the schema and the keys. Another technique is to use NOT EXIST. Select name, species, from animals, where, NOT EXIST. Select something, from adoptions, where name equals name and species equals species. Here we don't need DISTINCT. Duplicates are not possible as the sub query will be evaluated only once per animal. So each animal can only be returned either once or not at all. Who needs all these techniques when we can use the magical Set operators? Select name and species from animals, EXCEPT those that appear in adoptions. This returns all animals except those that were adopted. And this is SQL at its best. Simple, concise, efficient and crystal clear. It reads like plain English.
>
> **[11:12](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/set-operators?u=76281980&t=672)** You can't beat it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SQL]] (8), [[Databases]] (3), [[PostgreSQL]] (2), [[Microsoft SQL Server|Sql server]] (1)
> **Env Vars:** except (14), intersect (11), distinct (11), union (10), sql (9)
> **Definitions:** means that (3), is a  (3), is called (1)
> **SQL:** select (3), join (1)
> **CLI Commands:** find (2), make (1)
> **Analogies:** think of it as (1), similar to (1), just like (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor] (1)

#### [Challenge](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/challenge-3?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/challenge-3?u=76281980&t=1)** - [Instructor] Your challenge for this chapter is to show breeds that were never adopted. It is not the animals that were not adopted. It's the breeds that were not adopted. And this may sound like a minor change but trust me, it is not. There is a fundamental logical difference between these two questions. Breed isn't an animals identifier. We have more than one animal of the same breed. Moreover, none breed animals have null for breed, which throws another monkey wrench into the query. A few tips. We have none breed dogs and none breed cats. So remember, you must consider species as well. Breed alone is not enough. I'll give you the answer for the sample data. The only breed that was not adopted is a Turkish Angora cat. This is going to help you a lot identify any wrong solutions. Before you attempt to use set operators, I ask that you try some of the techniques we just used to find animals that were never adopted and see whether or not these techniques work. This should be an instructional exercise on what to do and not to do and how things can easily go wrong. The code file for video two has all the queries. So you can copy them and try to adjust them
>
> **[1:37](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/challenge-3?u=76281980&t=97)** to the new requirement. The code file for this video has all the requirements and the helpful tips. Good luck.

> [!info]- Semantic Content
>
> **CLI Commands:** cat (1), find (1)
> **Definitions:** is a  (2)
> **Speakers:** - [instructor] (1)

#### [Solution](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution?u=76281980&t=0)** - [Instructor] Let's start with a naive approach. I'm going to use the exact same queries that returned the animals that were never adopted and try to just change name to breed. What could go wrong, right? First, the outer join. Change name to breed and execute, nice. I do get the correct breed result, Turkish Angora cat, but it's buried in a pile of wrong ones. Let's see what this query really does. And to do that, I'll follow query execution order. Animals left outer join adoptions on species and name returns all animals and for those that were adopted, we also get their adoption attributes. The ones that were not adopted will have null for all adoption attributes and the where filter keeps only those. We get a list of all animals that were never adopted. So far, same as before. This set of 30 animals now moves on to the select clause, in which we do two things. We select only species and breed attributes and remove duplicates with distinct. And what did we get? All the species and breeds of all animals that were never adopted. Let me add name to the list to make it clearer and execute. Do you see why this isn't the same as breeds
>
> **[1:34](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution?u=76281980&t=94)** who were never adopted? The fact that Angel, who happens to be a non-breed dog wasn't adopted doesn't mean that all non-breed dogs were not adopted. Let's see if that's the case. Select star from animals, inner join adoptions on name and species and yes, here is Abby, a non-breed dog that was adopted by Patrick Hughes on August 30th, 2018. And yet, non-breed dog still shows up in the previous query results because Angel, a different dog, was not adopted. All dogs have four legs, but that doesn't mean that everything with four legs is a dog. So this query won't do. Let's trash it. The query with a not exist suffers from the same issue. Show me the breed and species for animals that weren't adopted isn't the same as show me all breeds that were never adopted. Trash that one too. The not in techniques suffers from a slightly different issue. And I'm going to use the [[PostgreSQL|postgres]] version and use the row constructor just to keep it shorter. I'll replace name with breed and execute. And now, something funny happens. I get back an empty set. Can you see why that is? Pause the video for a minute and try to figure it out.
>
> **[3:12](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution?u=76281980&t=192)** It's enough to have one row with a null breed for the whole not in expression to evaluate to unknown and for all rows to be eliminated. The in predicate can be written as multiple equality predicates. Let's break it down. The pair of expressions, A and one species and breed should be equal to the pair of A and two species and breed. For any two pairs where their species is equal, but the breed is null, this comparison evaluates to unknown. True and unknown is unknown. For any two pairs where the species is different and the breed is null, this comparison evaluates to false, false and unknown is false. If you don't remember these logical evaluations, go back to part one, chapter three, video two. In checks each pair from the outer query against all pairs from the inner query and you can think about it as if it has an or operator between them. The outer pair should be equal to at least one of the inner pairs for the predicate to evaluate to true and for all comparisons to be false, in order to evaluate to false. Now this cannot happen here. The result can never be false because it's enough
>
> **[4:47](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution?u=76281980&t=287)** that we have one unknown and the comparison evaluates to unknown and not unknown is also unknown, so not a single row will qualify. We can work around it by eliminating null breads from the inner query and execute. And this gives me the correct result. Only Turkish Angora cats were never adopted. But do you think this query is correct? Would you use it in your production environment? What do you think will happen if I add another animal? Ferris, the non-breed ferret, for which I know has not been adopted because I haven't put a row in adoptions for it. Will it show up or not? Pause the video for a minute and think about it. Well, the answer is yes. I got you there, didn't I? Ferris is returned as it should. This query also works for non-breed animals who were never adopted. Let's see why. Not in evaluates to true where the comparison is against an empty row expression. And this may sound counterintuitive at first, but if you think about it for a while, it does make sense. For example, the answer to the question, is [[John the Ripper|John]] not one of the persons who are
>
> **[6:23](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution?u=76281980&t=383)** in an empty room is yes. John is not one of these persons. There are no persons in an empty room, so John is not one of them. And I can easily verify this. Select works where one not in select one doesn't return any rows. Adding where false to the subquery returns an empty set and now the works string is returned. And the same thing happened for Ferris. For her row, the inner set is empty and the not in evaluated to true and Ferris was returned. So this query will work. But with all these fancy techniques and mind-bending null logic is completely unwarranted because we have the magic of set operators on our side. The whole trick here was to realize that unlike the previous exercise, we need to access the animal tables twice to solve it with a set operator. The first select returns all species and breeds. And the second one returns this species and breeds who were not adopted. And all we need to do is add an except between them and that's it. This solution doesn't force us to deal with the null,
>
> **[7:59](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution?u=76281980&t=479)** as they are not compared, but they are treated as not being distinct from each other. See how powerful set operators are? Now, I'm sure you'll start using them more in your code. It just works. Simple, concise, efficient and crystal clear. It's [[SQL]] at its best. For the adventurous among you, I have included another solution to this challenge at the bottom of the code file. Now, I don't think it's better than the set operator solution. On the contrary, I like the set operators better, but that solution does enable some interesting query techniques that I plan to cover in a future course that can be used when set operators can not. I'm not going to review that query here, but I recommend that you try and figure it out for yourself. It's pretty cool. And let me know what you think in the course's Q&A section.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[John the Ripper|John]] (3), [[PostgreSQL|Postgres]] (1), [[SQL]] (1)
> **CLI Commands:** make (2), cat (1)
> **Env Vars:** sql (1)
> **Cross-References:** go back to (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)


### 2. Advanced Joins

[↑ Back to Table of Contents](#table-of-contents)

#### [Self and inequality joins](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/self-and-inequality-joins?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/self-and-inequality-joins?u=76281980&t=1)** - [Narrator] The same table expression can be used multiple times in a FROM clause, as long as each instance of it is uniquely aliased. We call it a self join. All the rules we learned regarding join processing hold true for self joins. Each instance of the table expression is completely independent of the other. One of the most common use cases for self joins is querying adjacency list. This is the definition of an adjacency list, go ahead, read it, I'll be waiting. Adjacency lists are commonly used to represent employee hierarchies, bill of materials, post threads and other types of graphs. They are very intuitive, but not necessarily the most efficient or flexible. And don't worry, I'm not going to make you suffer through another employee hierarchy example, I'm sure you've seen a gazillion of these. Practically every book and every article on joins has one. There are other techniques, such as enumerated paths and nested sets that can also be used to represent graphs and [[SQL]]. Let me know in the Q and A section, if you would like to see a course about them, they're really cool. Most joins use equality, qualification predicates. The most common join is between a parent and it's child rows.
>
> **[1:34](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/self-and-inequality-joins?u=76281980&t=94)** Customers with orders, orders with items or animals with adoptions. Non-equality joins are joins that use non-quality qualification operators. There's nothing complex about them if we follow join processing order. Now, despite my earlier promise, a quick review of join order will be very helpful here. Every join begins with a Cartesian product where every element from the blue set is matched with every element from the orange set. Cross joins end here. All other join types proceed to a qualification phase where each row of the Cartesian product is evaluated using the qualification predicate. And this is true, regardless what qualification predicate we use. It can be an equality operator and non equality, or even a constant Boolean expression. If the qualification predicate uses an equality operator, only the row with a two twos qualifies. For a different than operator, all the rows except the previous one qualify, and for a larger than operator, only the row with a blue three and orange two qualifies. These are best explained with a code demo, and I remind you that it's perfectly okay to pause the video as many times as needed if you feel it's going a little too fast for you.
>
> **[3:08](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/self-and-inequality-joins?u=76281980&t=188)** Our task is to report adopters who adopted two animals on the same day. The report should show the adopter, both adopted animals in separate columns and the adoption date. I said two animals, not more than one for a reason. And you can try and solve it for three or more animals, and I would love to see your solution in the Q and A, we're not going to cover it here. Let's begin by joining two instances of the adoption's table, alias them A1 and A2 respectively, and use an equality predicate, where both the adopter email and the adoption date are the same. I'll add an order by just for convenience. We can immediately see a few issues. First, each adoption row is matched with itself, and of course we need to filter these out. So, let's add a predicate, A1 name different than A2 name and execute it. Success. The second challenge, is that we get two rows for each date for the same animals, only where their positions swapped. A row for Gus and Sam, another for Sam and Gus. There is a cool trick to get rid of these duplicate easily. Pause the video for a minute and try to find it. One way is to change the different than operator
>
> **[4:43](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/self-and-inequality-joins?u=76281980&t=283)** to a larger than. Only one of the rows we'll evaluate to true, and the duplicate will be eliminated. There was no requirement to show a specific animal on either of the columns, so name order is as good as any other. But we're not done yet. There's a hidden bug lurking in the solution. Even though it doesn't show with this data, do you think you can spot it? Pause the video once again for a minute, and see if you can spot the lurking bug. Comparing the animal's name between A1 and A2, just isn't enough. An animal's identifier is species and name. And someday, we may have two animals with the same name, but of a different species. Let's try to demonstrate it. I'll add two animals of a different species with the same name. Duplicate sounds like a nice name. Let's get them adopted on the same day, execute the query again, and indeed duplicate doesn't show up. Can you think of a solution, so that duplicate the dog and duplicate the rabbit do show up. Once again, pause the video for a minute and see if you can find a solution. You might have been tempted to use a predicate for either name is larger, or the species is different.
>
> **[6:21](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/self-and-inequality-joins?u=76281980&t=381)** Let's try this, execute, and now duplicate does show up, but this change had an unfortunate side effect. The trick we previously used with A1 name larger than A2 name, no longer works. We get back duplicate rows for adoptions of animals, with a different name and a different species. Sam the cat and Gus the dog, but also Gus the dog and Sam the cat. The OR causes the predicates to evaluate to true for both. If this isn't perfectly clear, you can execute the query without the last predicate, scroll down to Francis rows, and evaluate the predicate for each row in your head. If you're attempted to change the species predicates to larger than, that's not going to help either. As dog is larger than cat, but Sam is larger than Gus. So, if we're going to try this, both are returned. How would you solve it? Once again, pause the video for a minute, and see if you can find a solution. We have three possible conditions for an animal. Either their names are the same, but the species is different, or their species is the same, but the names are different, or both their names and their species are different. And we must account for all three possibilities.
>
> **[7:58](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/self-and-inequality-joins?u=76281980&t=478)** So, let's write these down as individual predicates separated by OR, and execute once again. Now something else has gone wrong. Where did the adoptions of Gus and Sam and Archie and Abby disappear. And for the last time, pause the video for a minute and see if you can find what's wrong. The last predicate is wrong. There is no guarantee that both the name and the species from A1 will be larger than those from A2. Even though Sam is larger than a Gus, cat isn't larger than dog. And the same is true for Archie and Abby. We cannot have both predicates use the larger than operator, and in order to fix this query, we must one of them back to being a different than, and it doesn't matter which one. Let's change it, execute again, and now we get the correct result. If this still doesn't make sense, execute the query without the last predicates and evaluated in your head like before. Can you think of another way to solve this challenge? I invite you to post your suggested solutions in the Q and A section.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SQL]] (1)
> **CLI Commands:** find (4), cat (4), make (2)
> **Env Vars:** sql (1)
> **UI Navigation:** scroll down (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)
> **Speakers:** - [narrator] (1)

#### [Lateral joins](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/lateral-joins?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/lateral-joins?u=76281980&t=0)** - [Instructor] One of the limitation of joins is that each table expression that's being joined must stand alone and be independent of any other. So this query is valid. Each table expression is complete and independent. But if we introduce a correlation between the two, we're going to get an error. F1 Bar is an unknown expression. And the reason is that it does not exist in the context of the sub query. Introducing such correlations could have been very useful for several types of challenges. And the most common one is known as the top end per group challenge. We need to show all animals with their most recent vaccination. Let's start with the animal attributes, names, species, color, and breed, and all that's missing is the last vaccine. I often see developer use correlated expression sub-queries for these types of challenges. Select vaccine from vaccinations. Predicate for the same animal, and add a limit to the result to get only the latest one. By the way, this is the only case when order by is allowed in a sub-query. Following processing order. The entire animals table is evaluated by the outer query and for each animal,
>
> **[1:34](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/lateral-joins?u=76281980&t=94)** the sub-query returns the most recent vaccination. This approach works, but it is limited and inefficient. For those who are not vaccinated like Ace and April the sub-query returns an empty set, which produces an nul expression. The sub-query can only return one expression. So if we want to show the vaccination time as well, we can't just add it to the same sub query. This will return an error. Only one expression can be specified. We have no choice, but to repeat the entire sub query for each additional expression. One expression per sub query. And this can get very ugly very quickly. This approach fails if we need to retrieve not just one, but let's say we want to retrieve the three most recent vaccinations. And if I try it, I'm going to get an error. Sub query returned more than one value. It would be nice if we could prepare our source set in the from clause with each animal already matched with as many vaccination and attributes as we desire. Let's try it. Animals cross join select vaccine and vaccination time. With a predicate for that animal and limit to three rows.
>
> **[3:10](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/lateral-joins?u=76281980&t=190)** Unfortunately, this doesn't work. Standard joins, as we said, must use stand alone table expressions. And the correlation to A name and A species, isn't allowed. But there was a way around it. The ANSI [[SQL]] 99 introduced a feature called lateral derived tables. [[PostgreSQL]], partially support it, SQL I doesn't. Oracle and my SQL do. SQL servers product team, decided it would be a good idea to invent their own proprietary syntax. And they called it cross apply and outer apply. We'll get back to [[Microsoft SQL Server|SQL server]] shortly. but let's start with PostgreSQL. With PostgreSQL, all we need to do to make this query valid is to add the keyword lateral after the join and execute. And now it works like a charm. Lateral processing invokes the sub query for each row of the animal's table and accumulates the result. So you can see for Abby, we got only two rows as she only had two vaccinations. Angel had at least three and the most recent ones are return. Pretty cool, isn't it? And no don't answer. It's a rhetorical question. Note that I'm using a cross join.
>
> **[4:43](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/lateral-joins?u=76281980&t=283)** And that means that animals that were never vaccinated, the sub query returns an empty set for them. And these rows are eliminated from the result. If we want to include animals that were never vaccinated, we must use a left outer lateral join. And here things start to get a little bit funky. Unlike normal joins, there's no logical qualification predicate here. The derive table is evaluated for each animal row. And the correlation predicate is what determines which rows will qualify. In a sense, the correlation is the qualification. To overcome this syntax challenge, we need to use a bullion true expression as the join qualification predicate. And yes, I know this looks weird and confusing, but it will grow on you with practice. This ugly looking syntax does allow us to return Ace, April and Arya, which were never vaccinated. Back to SQL server. Here we can use cross apply to eliminate animals that were never vaccinated. Similar to the cross join and outer apply to designate the animals table as reserved, similar to the outer joint. Unlike the lateral joint syntax, a ply does not require or even allow us
>
> **[6:18](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/lateral-joins?u=76281980&t=378)** to specify a qualification. When I think about it now, maybe the fact that SQL server does not support bullion types at all, is the reason they chose not to use the standard syntax. And now it's time for another challenge.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SQL]] (4), [[PostgreSQL]] (3), [[Microsoft SQL Server|Sql server]] (3)
> **Env Vars:** sql (7), ansi (1)
> **Definitions:** is an  (1), known as (1), means that (1)
> **Analogies:** similar to (2)
> **CLI Commands:** make (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Challenge](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/challenge?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/challenge?u=76281980&t=1)** - [Instructor] Our shelter has been experiencing financial difficulties. The board of directors decided to explore additional revenue sources and came up with the following idea. Instead of spaying and neutering all animals, the shelter should consider responsible breeding of purebred animals. Your challenge is to figure out which animals are breeding candidates. Candidates should be male and female of the same species and breed and you may use any database that you wish. In the next video I'll review my suggested solutions for both [[Microsoft SQL Server|SQL server]] and [[PostgreSQL|Postgres]]. You will find the requirements, expected results and some helpful tips and the code file for this chapter. Good luck.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft SQL Server|Sql server]] (1), [[PostgreSQL|Postgres]] (1)
> **CLI Commands:** find (1)
> **Env Vars:** sql (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Solution](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution-3?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution-3?u=76281980&t=1)** - [Instructor] As always, I'll follow execution order and begin with a FROM clause. Since I need to match two animals, I'm going to need two instances of the Animals table. As a starting point, I'll execute SELECT * FROM Animals AS A1 CROSS JOIN Animals AS A2; and this query returns each animal matched with every other animal. I need to match only animals of the same species and breed, and only purebreds. So, I'll change the CROSS JOIN to an INNER JOIN and add a qualification predicate ON A1.Species = A2.Species AND A1.Breed = A2.Breed; the visual clutter of all these redundant columns from the * is kind of a distraction so let's change it and include only the columns that we need. A1.Species, A1.Breed, A1.Name, AS Male, and A2.Name AS Female. I chose to get species, breed, and male from A1, but it's completely arbitrary. You're welcome to do it the other way around. I'll add an ORDER BY for convenience and execute. I'm not done yet. But let's examine what I have so far. First thing that I notice is that all non-breed animals were eliminated. Do you see why that is? The A1.Breed = A2.Breed predicate caused all animals with a null breed
>
> **[1:35](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution-3?u=76281980&t=95)** to evaluate to unknown. And in this case, it's a desirable effect. There's nothing wrong with leaving it as is. But if you're concerned with future developers getting confused, you're welcome to add a redundant predicate, AND A1.Breed IS NOT NULL or simply add a comment. Another obvious issue is that every animal is matched with itself. If I add the predicate AND A1.Name is different than A2.Name, I solve only half the problem. Pearl no longer shows up, but Nova is matched with Salem, and Salem is matched back with Nova. And since I already committed my aliases for the male to be from A1 and the female from A2, I can add two predicates AND A1.Gender = 'M' AND A2.Gender = 'F'. This is perfectly fine, and if you find this way to be clearer, go ahead and do it. For this solution, I'll make the query slightly shorter and kill two birds with one stone by using the same trick I showed you earlier. A1.Gender &gt; A2.Gender will take care of both as 'M' for male is larger than 'F' for female. A [[Microsoft Word|word]] of caution. If you decide to use this shortcut for strings, make sure your collations use dictionary sort order
>
> **[3:10](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution-3?u=76281980&t=190)** and that the string casing is consistent. If you use a binary sort order or a case sensitive collation, a non-capital 'f' will have a higher sort order than a capital 'M', which is going to fail this query. And with that, the solution is complete.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1)
> **Env Vars:** join (3), cross (2), select (1), inner (1), order (1)
> **SQL:** join (2), select (1), inner join (1), order by (1)
> **CLI Commands:** make (2), find (1)
> **Warnings:** caution (1)
> **Speakers:** - [instructor] (1)


### 3. More on Grouping

[↑ Back to Table of Contents](#table-of-contents)

#### [Ordered set functions](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/ordered-set-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/ordered-set-functions?u=76281980&t=0)** - [Instructor] Most aggregate functions don't rely on any order. MIN, MAX, COUNT, and SUM are inherently order-agnostic. The result will be the same regardless of any order of the individual elements. Ordered set functions are aggregate functions but ones that are effected by element order. They are processed like any other aggregate function except for the order significance. This is the general syntax for ordered set functions. The ANSI [[SQL]] standard defines two types of ordered set functions, hypothetical set functions, and inverse distribution functions. If you watched my window functions course, the hypothetical set functions should look familiar and it's no coincidence. We'll get back to these shortly. Most [[Databases]] support additional proprietary ordered set functions. And one of the most familiar and loved are the string aggregation functions that can concatenate individual string values within a group into a single longer string. These are not defined by the ANSI SQL standard, so each database vendor can choose whatever name it likes. So that's how we ended up with string aggregate in [[Microsoft SQL Server|SQL Server]], GROUP_CONCAT in [[MySQL]] and SQLite, and list aggregate in Oracle.
>
> **[1:34](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/ordered-set-functions?u=76281980&t=94)** Here is a query that groups adoptions by date and filters for groups that have more than one adoption on the same day. Aggregate functions return a single value per group. The SUM function aggregates all individual fees into a single daily total. What if we want to show all animals that were adopted on that day? Obviously, SUM won't work for strings. The animals identifier is name and species. So I'm going to use CONCAT function to create a single string from both the animals identifying attributes. Name and species are expressions from the same row, but there's potentially more than one row per each date group. And to concatenate all rows within a group, we need to use a function like SUM but one that works for strings. And string aggregate is our friend. Unlike SUM, which didn't care about any order, now, the order has significance, hence, it's called an ordered set function (indistinct). We specify the expression to be aggregated, the result of the concatenation of name, the [[Microsoft Word|word]], the, and species, followed by the separator character, comma, and space. Lastly, the sort order is defined in the WITHIN GROUP clause.
>
> **[3:09](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/ordered-set-functions?u=76281980&t=189)** Note that we have two different levels of concatenation here. The CONCAT function worked per row and the string aggregate on top of it for all rows within each date group. A word of caution. If you use SQL server's plus operator to concatenate a null string, the result will be null. CONCAT, on the other hand, converts nulls to empty string. String aggregate like any aggregate function ignores nulls. So for example, if we add breed to the string, we must join to animals, add breed, and alias to avoid ambiguity. Now, if we use the CONCAT function, all animals show up and just the breed will be missing. But if you choose to use the plus operator, the non-breed animals will be missing all together as the string aggregate will ignore them as they are nulls. Disclaimer, if you have little experience with [[Statistical Analysis]] functions, feel free to skip this video. You will find these functions confusing at first, everybody does, so don't feel bad. As I mentioned earlier, if you've watched my window functions course, you should remember, RANK , DENSE_RANK, PERCENT_RANK, and cumulative distributions when they were used
>
> **[4:44](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/ordered-set-functions?u=76281980&t=284)** as window functions. For those of you who don't remember or God forbid, haven't yet watched it, put it in your queue. It will be very helpful to briefly revisit what these functions did as window functions. RANK and DENSE_RANK window functions assigned rank to each row. For example, the RANK window function ordered by name, evaluates the relative rank of each names dictionary sort order. Window functions are evaluated per row. And so we get the relative ranks for all rows, Abby first and Winston last. These are not aggregates. As a grouped aggregate function, this same functionality doesn't make any sense. Here is the same animal set, grouped by species. Name is a non-aggregate expression. And theoretically, and even if this syntax was valid, which it isn't, name wouldn't be valid in the context of a grouped query. Aggregate functions must return one value per group. A rank within a group only makes sense relative to other ranks. So, let's throw away this nonsense. RANK functions make sense as hypothetical set functions. Hypothetical means we ask the database a what if question
>
> **[6:19](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/ordered-set-functions?u=76281980&t=379)** regarding a specific values rank within the group. What would be the rank of the name Ami, if it was in this group. This makes perfect sense as it will return a single value per group. The rank of Ami, if it was one of the elements within the group. So, if Ami were a cat, which isn't far from the truth, what rank would his name get? Ami starts with an A, which happens to be smaller than the current smallest cat name Calvin. So, it would rank number one, yay. If Ami were a dog, which also isn't far from the truth, he would rank number two. Abby starts with an A as well, but her second character B, has a lower dictionary sort order than Ami's second character M. As a rabbit, which is pretty far from the truth this time, Ami would rank number one again. The second type of ordered set functions are called inverse distribution functions. These are the percentile continuous and percentile discreet. Oracle, [[PostgreSQL|Postgres]], and others support these functions as the ANSI SQL standard intendant, but not SQL Server. SQL Server implemented something that resembles inverse distribution functions, but instead of following the standard in implementing these
>
> **[7:57](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/ordered-set-functions?u=76281980&t=477)** as ordered set functions, which they should be, the product team decided it would be a good idea to implement them as if they were window functions. And this doesn't make any sense whatsoever. And every time I need to explain that to a class, I get nervous, irritated, and start to shiver. After not much debate with myself, I have decided unanimously that I am not going to cover SQL Server's bizarre implementation of inverse distribution functions. It will only confuse you. In the code file, you will find links to good articles that attempt to explain it. And I recommend that you take a look at it after watching my window functions course and after you feel comfortable with ordered set functions. In some sense, inverse distribution functions work in a similar manner as a hypothetical set functions, they, too, suggest a hypothetical value evaluated from the group. Unlike the hypothetical set functions, where we provided a value and got back the hypothetical rank, inverse distribution functions do kind of the opposite. We specify the percentile and get back the value that corresponds to it using either a continuous or a discrete model. They don't work for strings. So my [[Microsoft PowerPoint|PowerPoint]] example breaks here, but it will be easier to show what they do in a code anyway.
>
> **[9:34](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/ordered-set-functions?u=76281980&t=574)** We need to rank our animals based on the number of vaccinations they're received. Let's start with a simple grouped query that counts the number of vaccinations per animal, sorted by species and number of vaccinations in descending order, and the animal with the most vaccinations wins. Yeah, I know it's kind of backwards, but stay with me here. Tigger is first of all cats with a record number of six vaccinations, followed by Oscar with three. In the dog's group, Aspen leads with five vaccinations, followed closely by Thor with four. And in the rabbits division, Bon bon is first, followed by Humphrey. So far so good. Let's take this query, wrap it in a [[CTE|WITH clause]], call it Vaccination_Ranking, and embark on our statistical analysis quest. This query is grouped by animal, but that's not going to be relevant. So I'm going to collapse the first query just to avoid distractions. Think of it as the source for the following query. Now, let's select from Vaccination_Ranking and group by species. As you know, after we group by species, we can only refer to the GROUP BY expression, species, and use aggregate functions for all other expressions. Let's add MIN, MAX, and average vaccination of individual animals for each species.
>
> **[11:08](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/ordered-set-functions?u=76281980&t=668)** Postgres average returns along numeric for averages. So let's cast it to a more readable decimal with two digit after the decimal point. Yeah, this is better. And here are the max, min, and average number of vaccinations per animal for each species. Pretty cool, right? The hypothetical set functions come in handy if we need to know what the rank of a hypothetical animal would be. One that had allegedly received X number of vaccinations. So to minimize confusion, I'm not going to show you all four hypothetical set functions, I'll focus just on DENSE_RANK and PERCENT_RANK. And this is an arbitrary choice. The other two, RANK and cumulative distributions are calculated slightly differently, but they were processed exactly the same way. My focus is on how they are processed, not the specific details of each one's algorithm. DENSE_RANK seven WITHIN GROUP ORDER BY number of vaccinations descending, shows that if we had a hypothetical animal that received seven vaccinations, it would have ranked first for all species. Seven is higher than the maximum number of vaccinations for all species. But if we change the seven to a five,
>
> **[12:43](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/ordered-set-functions?u=76281980&t=763)** a dog with five vaccination would still rank first as it would have the same number of vaccinations as Aspen. But for cats and rabbits, it would now rank second, since we have cats and rabbits with more than five vaccinations. PERCENT_RANK works in a similar way, but instead of an integer rank between one and n, it assigns a percentile rank between zero and one. A PERCENT_RANK for a hypothetical animal with seven vaccinations would fall into the top 0%, as seven is higher than the number of vaccinations of any other animal. A PERCENT_RANK with a hypothetical animal with only five vaccinations would fall into the top 8% of all cats. It would still be the top dog of all dogs, pun intended, and at around the top 33% for all rabbits. The inverse distribution functions do the opposite. Instead of supplying a value and getting the rank this value would have gotten, we provide a percentile and these return the value that corresponds to that percentile. The difference between the two functions is that the continuous model interpolates values that may or may not exist in the original group, and the discrete model picks the closest value,
>
> **[14:18](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/ordered-set-functions?u=76281980&t=858)** which has higher than the interpolated one, but only among the existing values within the group. Let's see how it works. For the top percentile zero, both return the maximum number of vaccinations per species. And for one, they return the minimum. For half, they return the value that falls right in the middle of the range, with an equal number of values above and below, also known as the median. Here, they happen to be the same, but the third percentile, inverse distribution for rabbits using a continuous model is four plus change. And you can see that this value was interpolated. There is no rabbit with 4.002 vaccinations. The same percentile using a discrete model returns a six. And this is the closest value from the existing ones that is higher than the one generated by the continuous model. This is Bon bon's number of vaccinations. Well, that wasn't that bad, or was it?

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft SQL Server|Sql server]] (5), [[SQL]] (3), [[Microsoft Word|Word]] (2), [[Statistical Analysis]] (2), [[PostgreSQL|Postgres]] (2)
> **Env Vars:** sql (8), sum (5), rank (5), percent_rank (5), concat (4)
> **CLI Commands:** make (3), find (2), cat (2), mysql (1)
> **Definitions:** is a  (2), is an  (1), known as (1)
> **Analogies:** for example (2), think of it as (1)
> **SQL:** group by (1), order by (1)
> **Warnings:** note that (1), caution (1)
> **Versions:** 4.002 (1)

#### [Grouping sets](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/grouping-sets?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/grouping-sets?u=76281980&t=1)** - [Instructor] A very common data analysis requirement is to present data in multiple aggregation levels. For example, how many animals do we have per species and breed, only per species, and overall? Oftentimes, these aggregation levels are temporal in nature. What's our annual, quarterly, monthly, weekly, daily, hourly, and secondly revenue? There are several ways we can achieve this. We can use subqueries, window functions, and even set operators. But there's an even better way, one that unfortunately I rarely see used. We need to write a query to show the number of annual, monthly, and overall adoptions. Let's start with an Eve approach by writing three separate queries, one that groups by year and month, one that groups only by year, and one that counts all adoptions overall. Typically, when we write queries that group by the whole table, we do so by simply omitting the group by clause. Did you know it's only a shortcut? The correct way is to specify group by with empty parentheses. And soon, you will see why this is very useful. If we try to union these queries and execute, we get an error that states all queries used in set operators
>
> **[1:34](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/grouping-sets?u=76281980&t=94)** must have the same number of columns. Makes sense. For the annual adoption, we need a placeholder for month. And for the grand total, we need one for both month and year. If I try to use a string like all months and all years, we'll encounter another error, a data type mismatch. With set operators, the first query is the one that determines the data types and the aliases. Since month is an integer, it is not compatible with the string all months. And I guess by now, you see where I'm going with this. Year and month are missing information for the higher level aggregates. And the correct placeholder should be null. Remember, I told you that null isn't a value? Null, theoretically, has no inherent data type. Let's use null placeholders and execute, and this time it works. This solution may seem reasonable enough for this simple case, but what if the source data isn't a single table, but a huge combination of complex joins between 50 tables and multiple filter predicates on top? Can you think of a way to shorten the solution? Pause the video for a minute and think about it. [[CTE|With clause]] to the rescue. The aggregation levels we use here are hierarchical.
>
> **[3:11](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/grouping-sets?u=76281980&t=191)** We can encapsulate the query that groups by year and month in a with clause and use that as our starting point. The second aggregate level can be built on top of the first one. Because year and month are in a hierarchy, we can group by year on top of the group by year and month. The same is true for grouping by the entire table. And this syntax may look weird at first, but I promise you it's valid and correct. The union technique is valid, but it's neither efficient nor scalable or convenient. The queries can become very long as more aggregate dimensions are needed. And [[SQL]] offers a much better way to do this, which is called grouping sets. Grouping sets allow us to specify multiple grouping levels and grouping expressions in a single query without the need to repeat anything. And it really puzzles me that very few developers use it, although it's extremely powerful, performs better in most cases, makes the code clearer and more concise, and has been around since the days of ANSI SQL 99, although not all vendors supported it since its inception. Most of my students tell me they've never even heard of it. And I can count on one hand the number of times
>
> **[4:47](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/grouping-sets?u=76281980&t=287)** I saw it used in production code. The syntax can be slightly confusing at first, so let's try to clarify it with the best way that I know, a code example. It would be helpful to think of the standard group by as a special case of grouping sets. Let's copy our three grouped queries and change them to use the grouping sets syntax. All we need to do is add the keyword grouping sets and a set the parentheses around the grouping expressions. The query that counted all adoptions now uses a single grouping set. This is the equivalent of a query without a group by. Same goes for the query that groups by year. For the query that groups by year and month, we need to be more careful. Since it has two grouping expressions, we must make sure they are treated as a single grouping element. And to do that, we must add a second set of parentheses. The outer parentheses are part of the grouping set syntax. Their inner one has a more profound meaning. And to demonstrate it, let's duplicate the query, remove the inner parentheses, execute both, and you can see that the results are different. Only a set of parentheses are missing,
>
> **[6:21](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/grouping-sets?u=76281980&t=381)** and the results are completely different. Look at these results for a minute and see if you can figure out what's going on. The query on the left treats year and month as a single grouping set. This is the equivalent of our original query that did a group by year and month. But without the parentheses, each expression is treated as a separate independent grouping set. And this is the equivalent of two queries, one that groups by year and another that groups by month, and you can think of them as if they are unioned together. And this is the power of grouping sets. Unlike group by and a list of expressions, which always defines a single grouping set, grouping sets allows us to define multiple different grouping sets at once. So to get our monthly, annual, and overall aggregates, we can list them all separated by commas inside the grouping sets clause. And this gives us the grand total, an annual total for each year, and the monthly totals all at once and without repeating anything.
>
> **[7:56](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/grouping-sets?u=76281980&t=476)** Amazing, isn't it? Don't answer. It's a rhetorical question. It gets even better. Grouping sets are not limited to related or hierarchical expressions. For example, group by year of adoption date and by adopter email, piece of cake. Here are the number of animals each adopter has adopted and the annual number of adoptions all at once. One more example, select species, breed, and count(*) from animals, group by grouping sets, species, breed, and overall. The query returns the number of animals per species, per breed, and overall, duh. Can you see the issue with the results? Let me add an order by and execute again, just to make it more obvious. Pause the video for a minute and see if you can spot the issue. We have two rows with null species and null breed, one with a count of 100, and one with a count of 68. One of these rows represents the grand total of all animals, and the other one represents only the non-breed animals of all species. Here, it's not hard to guess which one's which.
>
> **[9:31](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/grouping-sets?u=76281980&t=571)** The null breed came from the source table and represents non-breed animals. The other null came from the empty grouping sets and represents all breeds. So, how can we tell them apart? In this tiny query and dataset, it's really not hard to guess. It won't be as obvious for more complex queries. And what if we need to replace the nulls with the string all, for example? Coalesce species all works, as species doesn't allow nulls to begin with. If there's a null for species, there's no doubt what it means. It can only come from the grouping sets. But for breed, it isn't the case. Both the null for all breeds and the one for non-breeds will be replaced with an all. An all is an all is an all is an all, but don't worry. There is a way. SQL has a special function just for that. The grouping function for an expression returns a one for a row where that expression is an all and zero if it is not. First, let's add it to the select list and execute, and you can see that it returns one for the grand total. And we can now use that to distinguish between the two types of nulls
>
> **[11:06](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/grouping-sets?u=76281980&t=666)** with a conditional expression. Case when grouping breed equals one, then all else breed. And this will maintain the original null for non-breed animals. Pretty neat, right? Don't answer. It's a rhetorical question. I wish we had unlimited time, as there's a whole lot more to grouping sets. And if you would like to see a course dedicated to aggregations, including many more aspects of grouping sets, let me know, you know where. I hope this tiny bite of grouping sets made you hungry for more. I'm going to stop here, or my editor will make sure I'm not around for the next course. Will you ever use the union technique again? Don't answer. It's a rhetorical question.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SQL]] (3), [[CTE|With clause]] (2)
> **Definitions:** is an  (5), is called (1), is a  (1)
> **Env Vars:** sql (3), ansi (1)
> **CLI Commands:** make (3)
> **Analogies:** for example (3)
> **Speakers:** - [instructor] (1)

#### [Challenge](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/challenge-2?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/challenge-2?u=76281980&t=1)** - [Instructor] Your last challenge for this course is to write a query that returns a statistical report about vaccinations. The report should include the total number of vaccinations for several dimensions. How many were done and each year for each species, for each species per year, by each staff member and by each staff member per species. And to make it a bit more interesting, let's throw in the latest vaccination year for each of these groups as well on top of the count. You'll find their requirements, expected results and some helpful tips and the code file, good luck.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1), find (1)
> **Speakers:** - [instructor] (1)

#### [Solution](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution-2?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution-2?u=76281980&t=1)** - [Instructor] Like always, I'll start with the from clause. Obviously I need the vaccinations table and since I need the staff member's first and last name, let's call them vaccinators. I'm going to need to join vaccinations to persons based on email. Let's execute just for good measures and move on. I need to group by multiple dimensions, so guess which feature I'm going to use. From here, it's just a matter of following the required grouping sets as they are spelled out. Empty parentheses will give us the grand total. To get the annual total, I'll add year of vaccination time. To get species, well you guessed it. For the combination of year and species, I need both expressions in parentheses. Next are the vaccinators. Here I hope you did not fall for the trap that I set for you and used first name and last name to identify vaccinators. With this data set, it's pretty obvious that the email addresses consist of first and last name at [animalshelter.com](https://animalshelter.com). So the result is not going to change if I use either email or the actual name. But I can't rely on that. What if someday the shelter hires another person by the same name of Ashley Flores
>
> **[1:36](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution-2?u=76281980&t=96)** and assigns her the email ashleyflores2@[theanimalshelter.com](https://theanimalshelter.com)? This solution must be logically correct and continue to work. I cannot rely on some empirical pattern which happens to be true today, and I've witnessed applications fail miserably with devastating results because of similar assumptions. Remember, always check the schema and look for the primary key. The key for persons is email. Therefore email is the only valid identifying attribute. Next are vaccinator and species. I just need to remember to place both of them in parentheses. Now I'm ready to move on to the select list. I'll start with the obvious expressions. Year of vaccination time, species, email, first name, last name, and count star as the number of vaccinations. These are the grouping expressions and an aggregate count. Here comes the second trap. The requirement asked for the latest vaccination year per group. But we already have vaccination year both as a grouping expression and as a select expression. How does it make sense as an aggregate expression at the same time? Well, it does.
>
> **[3:09](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution-2?u=76281980&t=189)** To show you why, let's switch to a simpler query for a minute. Select species and count from animals group by species. Here, species is the grouping expression and a select expression. Can I put it as an aggregate expression too? Let's try. Add max species and execute. Bam. Works like a charm. Now for this query it's nonsense, of course. With or without max, it's the same species per group. But that's only because this query only groups by one grouping set. My point is that this syntax is legit and there are no logical issues with it. Point proven. Back to our query. Max year vaccination time is a valid expression in this context. And yes, sure. For the annual groups, it won't make much sense and will repeat the same value as the non-aggregate year, but that's not going to be the case for all other grouping sets. Let's try to execute it and oops. Error 8120. Okay, first name is invalid. It's not one of the grouping set and with [[Microsoft SQL Server|SQL Server]], I must add a dummy aggregate to satisfy the syntax root. Execute and now it almost works.
>
> **[4:46](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution-2?u=76281980&t=286)** All that is missing is handling the nulls and to take care of the order by. I'll add a coalesce to replace nulls with the strings from the requirements. Year with all years. Species with all species. Email with all staff, and both first and last name with an empty string. Oh great. A new error. Conversion failed for years to integer. Right, we just saw that before. The year function returns an integer, so replacing it with the string all years doesn't make sense. No problem. I'll explicitly cast it to a VARchar. Year is only four characters but I must make sure that the length will be enough for the replacement string as well. So VARchar 10 will do. Let's execute and whew, no error this time. But hold on. Something is seriously wrong. Look at the rows with a null email. Those are supposed to be the grand totals for all vaccinators. But what's up with the staff member names? Who is Wayne Reyes? I know who Wayne Carter is but we have no Wayne Reyes, and we have both Wanda Myers and Wanda Reyes? I don't remember them either. You're welcome to check the staff table. They don't exist.
>
> **[6:18](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution-2?u=76281980&t=378)** Has SQL Server completely lost its mind? It turns out that now our seemingly harmless dummy aggregates are not so harmless after all. While the database uses a null for expressions that represent a group like email, it doesn't do it for first name and last name. We know that email determines first name and last name but the database doesn't. As far as the database is concerned, these are still valid aggregate expressions and it doesn't care that we only put the max there to overcome the syntax limitation. It doesn't know that we didn't really mean it to be a max. We assumed each group will only have one value for first and last name which would have been true for a group by query. But that's not true for grouping sets. And yes, Wayne Reyes doesn't exist. For the grand total row that treats the entire table as a single group, Wayne happens to be the first name with the largest dictionary sort order value and Reyes happens to be the largest last name. SQL Server gladly retrieves both for us as we requested. The same thing happened with Wanda. In 2017, Myers happened to be the largest last name. But in 2018 it was Reyes.
>
> **[7:54](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/solution-2?u=76281980&t=474)** Wanda was still the largest first name. The coalesce that I added for first name and last name is incorrect. They will never be null as they are defined as not null in the person's table and they are not part of the grouping sets so they cannot be null because of that. What I should do is only return max first name and max last name for rows that represent specific persons' specific emails, not for ones that treat all of them as a group. I could use a conditional expression which will test if email is null as email is the key. However, that may not work in other cases where the grouping set values can be null and the correct way to deal with it is to use the grouping function. When it's zero for email, I can safely return max first name and last name. But when it's one, I know that the row is not for a specific person. But for a group, only these rows will need to return an empty string. Last thing is to deal with the order by. Order by year, species, first name and last name works like a charm. And with that, I conclude this solution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft SQL Server|Sql server]] (3)
> **CLI Commands:** make (4)
> **Env Vars:** sql (3)
> **URLs:** [animalshelter.com](https://animalshelter.com) (1), [theanimalshelter.com](https://theanimalshelter.com) (1)
> **UI Navigation:** switch to (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 4. Recursions and Cursors

[↑ Back to Table of Contents](#table-of-contents)

#### [Recursions](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/recursions?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/recursions?u=76281980&t=1)** - [Instructor] So far we've used many WITH clauses. They helped us break down complex tasks, shorten queries, but there's much more to them than that. WITH clauses pack a powerful and unique ability to process recursions without resorting to any type of imperative solutions. To create a recursive [[CTE|WITH clause]], we must specify our intent using the WITH recursive keyword, the recursive query definition consists of two parts. The first is a standalone, ordinary select query called the anchor. The anchor defines the initial set, the starting point for the recursion. The anchor query is followed by either a union all or a union distinct set operator, which in turn is followed by the recursive query itself. The recursion is indicated by referencing the WITH clause name within the query. This may seem counterintuitive at first, as typically, we cannot reference an object within its own definition. As for the processing, first, the anchor query is evaluated. Here, the full bar table has one row with A and one for foo and bar respectively and this is our anchor. And with that, the anchor query has completed its job. The anchor set moves to the recursive query.
>
> **[1:38](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/recursions?u=76281980&t=98)** The recursive query is evaluated using the anchor set as its source, which produces a new result set. For example, a row with B and two. The original anchor set is now placed in a buffer, a temporary holding place for intermediate results. Then you lead generated results set, B two, takes the place of the original anchor. And now, the recursive query is evaluated once again, but this time using B two as its source data. This in turn results in a third set, the row C three. The previous row, B two, is added to the buffer and the most recent one takes its place. And that's pretty much the whole deal. The process repeats until one of the following conditions are met. Either the recursive query results in an empty set or the maximum recursion depth is reached or the threshold time limit is exceeded, depending on the database that you're using. The buffer now contains the full result set of our recursive WITH clause. Some of the common use cases for recursive WITH are to generate series, concatenate strings and traverse hierarchical data, bill of materials, organizational hierarchies and other graphs.
>
> **[3:13](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/recursions?u=76281980&t=193)** As always, best explained with a code example. Let's begin with generating a series. Before we do it the hard way, let me introduce you to one of the [[PostgreSQL|postgres]] unique and useful functions. Generate series is a set function, a series of rows with expressions of varying types and intervals. Let's generate a row with a date expression for every day of 2019. Select day from generate series, start date and date, interval, alias. Done, piece of cake. [[Microsoft SQL Server|SQL server]] doesn't have an equivalent function, but with it, we get the benefit of practicing recursions. First, let's create the anchor. As static query as a starting point and we'll use the first day of 2019, cast to a date datatype. Remember, the keyword recursive isn't supported in SQL server. We won't be generating any duplicates, so we can use union all. The recursive query uses the anchor and adds one day to it. Every recursion we'll use the previous result until the stop condition is met when the date ad will reach January 1st, 2020. At that point, the where will evaluate to false and empty set will be produced and their recursion will stop.
>
> **[4:47](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/recursions?u=76281980&t=287)** Well, at least we hope. Let's execute and oops, here is the first wall of the default settings. [[SQL]] servers maximum recursion depth is a hundred by default. And since we need to generate 365 rows, we must add a query option hint to allow a recursion depth of 365. In postgres, we can use the same technique. Let's see, here's the postgres version, execute and it generates the same result, great. There is a fundamental difference between SGL server's date add function and the equivalent expression in postgres. Can you see it? I don't mean the syntax, think fundamental. Pause the video for a minute and see if you can spot the fundamental difference. The difference, which we've already mentioned in this course is the interval data type. The arguments for SQL servers date add function are the date part and integer and the date. The date add operation generates an interval under the covers by multiplying the integer by the date part. Only an interval makes sense as an additive argument to a temporal type. Logically, we can't add an integer one to a date. The same reason we can't multiply a string by pie
>
> **[6:25](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/recursions?u=76281980&t=385)** or add two pounds to 30 degrees. There are simply incompatible types. We can add a day, a millisecond or a gazillion years to a date. These are all intervals. Intervals represent a range on the time continuum and integers represent a point on the number's axis. They live in different universes. SQL server doesn't support interval types. Postgres adds day and an interval expression. Clean, elegant, correct. If you're thinking that this is a minor difference and asking yourself, if I lost my mind and why I'm wasting your time, trust me, it's not minor at all. An interval data type can open doors to solving complex temporal logic easily and efficiently. Let's do another example. This time with our old familiar adjacency list. Don't worry, not an employee hierarchy. I promised and I will keep my work. Here we have a table that contains URLs where each one points to another. We can prevent first level sickly references with a check constraint, but not much more. Just so you don't think I'm completely biased, I'll admit that [[Relational Databases]] do not [[Microsoft Excel|excel]], pun intended, in representing complex graph centuries.
>
> **[8:00](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/recursions?u=76281980&t=480)** Let's insert a few rows and select star from web links. You want points to U three and U nine. In turn, U three points to U two, four, five and nine. Well, you get the idea. Our task is to traverse the graph starting with U four. Let's create the crawler WITH clause. And first comes the anchor member. The larger than character will be our symbol for the root node. U four is our starting URL and I'm going to add a level, which starts with the integer zero. The ladder is going to be useful for filtering stop conditions and just for fun, you'll see in a minute, what I mean. We must be careful to cast the datatypes as they are determined by the anchor member for the [[Representational State Transfer (REST)|rest]] of the query. And we often use literal constants as anchor expressions. So remember to specify the data types or it will bite you. Union all we'll do here as well, since we're not going to generate any duplicates. And lastly, the recursive member joins back to the original table and level plus one advances the level counter. We must join back to the original table as the query only sees the output of the previous recursion. This will often be the case, so remember this. Let's execute and it works like a charm.
>
> **[9:36](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/recursions?u=76281980&t=576)** If we want to have a bit more fun, we can use the level indicator with a string function to draw a visual representation of the hierarchy in the result. Zero dashes for level zero, one dash for the first, two for the second. Pretty cool, isn't it? Don't answer, it's a rhetorical question.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PostgreSQL|Postgres]] (5), [[CTE|With clause]] (4), [[Microsoft SQL Server|Sql server]] (3), [[SQL]] (2), [[Relational Databases]] (1)
> **Env Vars:** sql (5), sgl (1), url (1)
> **Definitions:** is a  (5)
> **CLI Commands:** node (1)
> **Analogies:** for example (1)
> **Best Practices:** remember to (1)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)

#### [The cursors curse](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/the-cursors-curse?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/the-cursors-curse?u=76281980&t=0)** - [Instructor] I said once or twice before, that row processing order doesn't pose any significant logical implications. And that for our humble human brains, it is more intuitive to think of it as if it was a row by row operation. That's why I explained the processing of joins, where having and select all as if they were processed row by row. It was mostly for convenience sake. There's no row order until the order by mutilates are set and turns it into a cursor. So any discussion of row order before that isn't even relevant. Until Dr. Codd invented the relational model, data didn't exist. Just kidding. I'm trying to see if you're paying attention. Until Dr. Codd invented the relational model. Data was processed using a paradigm known as sequential access methods, or uncle SAM for short. Sequential access methods are widely used even today in many programming languages. Unfortunately, many [[SQL]] developers are still stuck in this mindset. And the reasons may surprise you. In the early days of computing, sequential access was mandated by available [[Hardware]]. Punched cards and magnetic tapes were linear in nature. Punched cards had to be physically ordered and magnetic tapes were accessed using physical pointers
>
> **[1:35](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/the-cursors-curse?u=76281980&t=95)** to regions on the tape. And this mindset is still prevalent today in many surprising areas. One of them being the use of surrogate keys or enumerators to access rows. Yes, this mindset dates all the way back to the age of spinning magnetic tapes. And developers were so used to this processing paradigm that they found the radically contrasting declarative set based relational model, extremely hard to grasp and accept. Many insisted and still insist on using the old and familiar sequential techniques. Even though they are in sharp contrast to the very essence of the relational model and SQL. And unknowingly, they are throwing away many of its benefits. It is a known human conditioning. We always try and use new technologies exactly the same way we use the previous ones. That's why we still see occasionally, an electric vehicle pull into a gas station. And that's the reason that early computer users tried to use wipe out or tip X for you non-Americans on their monitors to delete errors. They are not stupid. They are just conditioned and we all are. And that my friend, is the main reason that SQL allows us not only to return cursors instead of datasets,
>
> **[3:10](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/the-cursors-curse?u=76281980&t=190)** but also to process them that way. Row by agonizing row, instead of the lovely relations that they are. After the order by clause is processed, the set becomes a cursor. So please don't call it an ordered set. That's a contradiction in terms. Here is a query that returns a cursor and so far, nothing special about this cursor. It can be returned to the client application using the standard APIs. But if that's not bad enough, SQL allows us to create a cursor object on the server using the declare cursor statement. The cursor object doesn't return any data to the client. Well, not yet. Oh, no. Instead we open the cursor object server side , place a pointer to a specific row within the cursor. And we typically start with the first one and then retrieve the values from that row into scalar variables. Yes, you heard me right. Imperative, scalar based logic in SQL. God forbid! These variables can be returned to the client or further processed on the server. And once we are done with the first row, we can [[Fetch]] the next one. And this process repeats until the cursor is closed. And sometimes this is what's left behind.
>
> **[4:45](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/the-cursors-curse?u=76281980&t=285)** Just in case it wasn't clear. I'm not a fan of cursors. I do realize they have their place and can be useful for administrative tasks or in rare cases as a last resort solution. This course is about SQL processing. Cursors are for imperative fans. Therefore I'm not going to do a cursor code demo, nor am I trying to cover the different options that most [[Databases]] offer. There are just way too many of them. But I will draw your attention to an aspect of cursors that many don't consider, but should. And that is isolation and concurrency. Here is McCursy with its first row fetched. Out of the blue, literally another transaction comes along and inserts a new row into Foo Bar. And now McCursy tries to fetch the next row. What do you think? Which row should be fetched? 1.5 Z or two Y. I want ask you to think about it and the answer is it depends. It depends on many things. And first and foremost is the implementation of this specific database of your choice. Let's use this opportunity to show you a section from the ANSI SQL standard that should horrify you.
>
> **[6:19](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/the-cursors-curse?u=76281980&t=379)** Take a minute and read it. The two phrases that should freak you out are implementation defined, and implementation dependent. With [[Microsoft SQL Server|SQL server]], these are called static and dynamic cursors. They too have variants and some funky behavior. [[PostgreSQL]] supports only insensitive cursors. And my SQL supports only as sensitive cursors. Oracle rightfully considers cursors not to be part of SQL, but that of PL=SQL it's procedural sibling. And that's just one aspect. I have hours of horror stories to tell you about cursors, but don't worry. Not now. I have witnessed extremely creative ways of cursor abuse, which deserve a course or maybe even horror movie. Cursors are a Pandora box. I can recommend that you stay away from them if you can. And in most cases you can.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** cursor (12), [[SQL]] (10), [[Fetch]] (2), [[Hardware]] (1), [[Databases]] (1)
> **Env Vars:** sql (11), sam (1), ansi (1)
> **Definitions:** is a  (2), known as (1)
> **Versions:** 1.5 (1)
> **UI Navigation:** open the (1)
> **Analogies:** think of it as (1)
> **Speakers:** - [instructor] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Conclusion](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/conclusion?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/conclusion?u=76281980&t=0)** - [Instructor] Oh, wow, another chapter of our [[SQL]] adventure is ending. We learned about subquery processing and the extremely powerful set operators. Don't worry too much about the subtleties of all versus distinct. You'll likely only use these for unions, which is what most [[Databases]] support anyway. We learned how and when to join tables to themselves, how to use non-equality join qualification predicates, and how lateral joins are processed. I hope you enjoyed the challenge of finding breeding candidates and I kindly ask you again to consider donating to your local animal shelter. We learned how to use grouping sets, and I can tell you, I was blown away by it when I first learned about this feature. I hope you are, too. Many of you will use the string ordered set functions and don't worry if the hypothetical and inverse distribution ones aren't fully clear yet. It takes some time and practice. We moved on to recursive WITH clauses and these also take some time to get used to but can save you from the need to write evil imperative code. And lastly, my favorite feature of SQL: cursors. Nothing much to say about these. Just be careful. If you already have cursors in your database, it might be a good idea to review if they're really necessary,
>
> **[1:33](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/conclusion?u=76281980&t=93)** and you may be better off without them. In case my subtle teasers throughout this course were not enough, I will once again offer my sincere and unbiased recommendation not to miss my "Window Functions" course. It's a challenging course. It's fast paced and includes much more difficult challenges. Now that you've completed both query processing courses, Window Functions are the natural next step to advance your SQL skills. They are amazingly powerful, but they, too, take some effort and practice to master. So, be patient. I have another course on [[LinkedIn]] Learning that received less attention than the [[Representational State Transfer (REST)|rest]] as it is for a specific product and version. This was my first course for LinkedIn Learning, and while it focuses on [[MySQL]] 8.0, it is valuable for other databases, too. The first chapter in that course is about window functions. It's only 30 minutes long, and you can watch it as an introduction before committing to the full "Window Functions" course. The second chapter is about standard and recursive common table expressions or, as we now correctly call them, WITH clauses. And the third is about lateral derived tables. I think they can be great for you to review and practice these features. I also use a different demo database which is always a good practice. LinkedIn offers many more courses for you to choose from,
>
> **[3:09](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/conclusion?u=76281980&t=189)** and I'm not going to mention any specific ones. But I do recommend that you look for those that focus on foundations and theory, not just the syntax. And stay away from those that promise instant results. The zero to hero in two hours kind? You know what I mean. Video courses are great, but I always like to complement my learning with books. For learning foundations, I highly recommend Joe Celko's and Chris Date's books. Joe's books are an easy and fun to read. They flow well, they're spiced with humor, and they cover foundational aspects in a way few authors do. If you haven't read any foundational SQL and relational model books, Joe's books are an excellent choice. Chris Date's books go much deeper into theory and the mathematics of the relational model. They are very deep, very thoughtful, but a harder read. Those of you who prefer this style can start with Chris's books. Others will find it easier to digest after they have some initial understanding of the relational model. And last but not least is my first SQL teacher and mentor, the one and only Itzik Ben-Gan. I owe a lot of what I know today to Itzik. He is to blame for inflicting me with the SQL bug nearly a quarter of a century ago.
>
> **[4:42](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/conclusion?u=76281980&t=282)** Itzik writes exclusively about T-SQL, [[Microsoft SQL Server|SQL Server]]'s dialect, but his books are a fountain of foundational knowledge. They are methodological, deep, yet fun to read. His challenges and quizzes will blow you away. You can't go wrong with either, and even better is if you read all three. Since I haven't solicited feedback enough so far, I'm going to do it again. Unlike my in-person courses, which are not happening for obvious reasons these days, I can't see your reactions or talk to you directly, and I really miss it. Please let me know what you think, what you liked, what you didn't like. It's really important to me. I genuinely care about your experience and your success in the relational data universe. I take your opinions seriously and I use that to improve future courses. The Q and A section is a great place to communicate. And if you look at the Q and A sections of my other courses, you'll see that I answer every single comment. Even just a hi or a thank you makes me happy. So why don't you make me happy? And with that, once again, we say goodbye, but not for long. (upbeat uplifting music) I'm sure we'll get a chance to say another hi soon enough. Until then, my friend, be safe and healthy and don't forget to adopt a shelter animal,
>
> **[6:17](https://www.linkedin.com/learning/advanced-sql-logical-query-processing-part-2/conclusion?u=76281980&t=377)** and there's no better day for that than today. Yours truly, Ami.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[SQL]] (7), [[LinkedIn]] (3), [[Databases]] (2), [[Representational State Transfer (REST)|Rest]] (1), [[MySQL]] (1)
> **Env Vars:** sql (8)
> **CLI Commands:** mysql (1), find (1), make (1)
> **Best Practices:** good practice (1), don't forget (1)
> **Versions:** 8.0 (1)
> **Definitions:** is a  (1)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [[Ami Levin]]

## Resources

- Exercise files available

## Skills Covered

- SQL

## Path Context

### In [[Master SQL Development]]
← [[Advanced SQL- Logical Query Processing, Part 1]] | **3 of 8** | [[Advanced SQL – Window Functions]] →

## Appears In

- [[Master SQL Development]]

## Related Courses

_Courses sharing skills:_

- [[Advanced SQL Project- Design and Manage a Database]] — SQL
- [[Practical Database Design- Implementing Responsible Data Solutions with SQL Querying]] — SQL
- [[SQL Server Fundamentals- Master Basic Query Techniques]] — SQL
- [[Advanced SQL for Data Science- Time Series]] — SQL
- [[Intermediate SQL- Data Reporting and Analysis]] — SQL

---

[↑ Back to top](#)