---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: data-science-foundations-python-scientific-stack-coderpad
url: "https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad"
duration_minutes: 145
duration: 2h 25m
level: Intermediate
updated: 11/9/2022
learners: 44400
skills:
  - Python (Programming Language)
  - Data Science Foundations
  - Data Science
exercise_files: false
github: "https://github.com/LinkedInLearning/data-science-foundations-python-scientific-stack-3084641/codespaces"
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQG2_xrJ2bSZsA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1667582946722?e=2147483647&amp;v=beta&amp;t=jZiEOWL9RZujkXmIVtRAtwNBWlw-7GjmPTRkk1ioGDo"
linkedin_topic: Data Science
learning_paths:
  - '[Advance Your Python Skills for Data Science](../../Paths/Data%20Science/Learning%20Paths/Advance%20Your%20Python%20Skills%20for%20Data%20Science.md)'
prev_courses:
  - '[Python For Data Science And Machine Learning Essential Training Part](../Artificial%20Intelligence%20(AI)/Python%20For%20Data%20Science%20And%20Machine%20Learning%20Essential%20Training%20Part.md)'
next_courses:
  - '[Python Data Analysis](Python%20Data%20Analysis.md)'
path_nav: '[{"path":"Advance Your Python Skills for Data Science","position":3,"total":6,"prev":"Python For Data Science And Machine Learning Essential Training Part","next":"Python Data Analysis"}]'
path_count: 1
tags:
  - course
  - topic/data-science
  - topic/software-development
  - skill/python-programming-language
  - skill/data-science-foundations
  - skill/data-science
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Data%20Science/Data%20Science%20Foundations-%20Python%20Scientific%20Stack%20-CoderPad-.md)

![Data Science Foundations: Python Scientific Stack [CoderPad]](https://media.licdn.com/dms/image/v2/D560DAQG2_xrJ2bSZsA/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1667582946722?e=2147483647&amp;v=beta&amp;t=jZiEOWL9RZujkXmIVtRAtwNBWlw-7GjmPTRkk1ioGDo)

# Data Science Foundations: Python Scientific Stack [CoderPad]

> Join instructor Miki Tebeka as he dives into the Python scientific stack and shows you how to use it to solve problems. Miki covers the major packages used throughout the data science process: numpy, pandas, matplotlib, scikit-learn, and others. He also guides you through how to load data, analyze data, run models, and display results.This course is integrated with GitHub Codespaces, an instant cl

> [LinkedIn Learning](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad) | 2h 25m | Intermediate | 44K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (4 videos)
  - [The Python scientific stack](#the-python-scientific-stack)
  - [What you should know](#what-you-should-know)
  - [Using GitHub Codespaces with this course](#using-github-codespaces-with-this-course)
  - [Setup](#setup)
- [**1. Visual Studio Code**](#1-visual-studio-code) (3 videos)
  - [Use code cells](#use-code-cells)
  - [Extensions to the Python language](#extensions-to-the-python-language)
  - [Understand markdown cells](#understand-markdown-cells)
- [**2. NumPy Basics**](#2-numpy-basics) (9 videos)
  - [NumPy overview](#numpy-overview)
  - [NumPy arrays](#numpy-arrays)
  - [Slicing](#slicing)
  - [Learn boolean indexing](#learn-boolean-indexing)
  - [Understand broadcasting](#understand-broadcasting)
  - [Understand array operations](#understand-array-operations)
  - [Understand ufuncs](#understand-ufuncs)
  - [Challenge: Working with an image](#challenge-working-with-an-image)
  - [Solution: Working with an image](#solution-working-with-an-image)
- [**3. pandas**](#3-pandas) (8 videos)
  - [pandas overview](#pandas-overview)
  - [Loading CSV files](#loading-csv-files)
  - [Parse time](#parse-time)
  - [Access rows and columns](#access-rows-and-columns)
  - [Calculate distance](#calculate-distance)
  - [Display speed box plot](#display-speed-box-plot)
  - [Challenge: Taxi data mean speed](#challenge-taxi-data-mean-speed)
  - [Solution: Taxi data mean speed](#solution-taxi-data-mean-speed)
- [**4. Folium and Geospatial Data**](#4-folium-and-geospatial-data) (5 videos)
  - [Create an initial map](#create-an-initial-map)
  - [Draw a track on map](#draw-a-track-on-map)
  - [Using geospatial data with shapely](#using-geospatial-data-with-shapely)
  - [Challenge: Draw the running track](#challenge-draw-the-running-track)
  - [Solution: Draw the running track](#solution-draw-the-running-track)
- [**5. NYC Taxi Data**](#5-nyc-taxi-data) (8 videos)
  - [Examine data](#examine-data)
  - [Load data from CSV files](#load-data-from-csv-files)
  - [Working with categorical data](#working-with-categorical-data)
  - [Work with data: Hourly trip rides](#work-with-data-hourly-trip-rides)
  - [Work with data: Rides per hour](#work-with-data-rides-per-hour)
  - [Work with data: Weather data](#work-with-data-weather-data)
  - [Challenge: Graphing taxi data](#challenge-graphing-taxi-data)
  - [Solution: Graphing taxi data](#solution-graphing-taxi-data)
- [**6. scikit-learn**](#6-scikit-learn) (8 videos)
  - [scikit-learn introduction](#scikit-learn-introduction)
  - [Linear regression](#linear-regression)
  - [Understand train/test split](#understand-traintest-split)
  - [Preprocess data](#preprocess-data)
  - [Compose pipelines](#compose-pipelines)
  - [Save and load models](#save-and-load-models)
  - [Challenge: Handwritten digits](#challenge-handwritten-digits)
  - [Solution: Handwritten digits](#solution-handwritten-digits)
- [**7. Plotting**](#7-plotting) (9 videos)
  - [Overview of matplotlib](#overview-of-matplotlib)
  - [Use styles](#use-styles)
  - [Customize pandas output](#customize-pandas-output)
  - [Plotting with pandas](#plotting-with-pandas)
  - [Use Matplotlib with pandas](#use-matplotlib-with-pandas)
  - [Tips and tricks](#tips-and-tricks)
  - [Other plotting packages](#other-plotting-packages)
  - [Challenge: Stock data bar charts](#challenge-stock-data-bar-charts)
  - [Solution: Stock data bar charts](#solution-stock-data-bar-charts)
- [**8. Other Packages**](#8-other-packages) (6 videos)
  - [Other packages overview](#other-packages-overview)
  - [Go faster with Numba](#go-faster-with-numba)
  - [Understand deep learning](#understand-deep-learning)
  - [Work with image processing](#work-with-image-processing)
  - [Understand NLP: NLTK](#understand-nlp-nltk)
  - [Working with bigger data](#working-with-bigger-data)
- [**9. Development Process**](#9-development-process) (5 videos)
  - [Development process overview](#development-process-overview)
  - [Understand source control](#understand-source-control)
  - [Learn code review](#learn-code-review)
  - [Testing overview](#testing-overview)
  - [Testing example](#testing-example)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [The Python scientific stack](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/the-python-scientific-stack?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/the-python-scientific-stack?u=76281980&t=0)** - [Miki] [Data Science](../../Topics/Data%20Science.md) is booming, and [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) is a major player there. Hi, I'm Miki Tebeka, and together we'll crunch some data, train some models, create visualizations, and generally have fun with Python and data. We'll solve many exercises while learning. I highly recommend that you follow along and write code. From my experience, there's some magic happening between the fingers and the brain that greatly improves the learning process. Don't be afraid to make mistakes, Python is very forgiving language and recovering from errors is easy. During this course we'll cover tools and techniques that I find useful in my daily work processing data and gaining insights from it. I hope you'll find these tools and techniques beneficial as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [Data Science](../../Topics/Data%20Science.md) (1)
> **CLI Commands:** python (3), find (2), make (1)
> **Definitions:** is a  (1)
> **Speakers:** - [miki] (1)

#### [What you should know](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/what-you-should-know?u=76281980&t=0)** - [Instructor] This course is about [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) and [Data Science](../../Topics/Data%20Science.md). It'll help if you know a bit of both. I'm not going to teach you Python. If you don't know Python, check out our offerings and then come back. I'm not going to teach you the basics of data science either. You can get by with a little math.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [Data Science](../../Topics/Data%20Science.md) (2)
> **CLI Commands:** python (3)
> **Speakers:** - [instructor] (1)

#### [Using GitHub Codespaces with this course](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/using-github-codespaces-with-this-course?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/using-github-codespaces-with-this-course?u=76281980&t=0)** - [Ray] This is Ray Villalobos, senior staff instructor at [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning, and I'm going to show you how to work with LinkedIn Learning courses using [GitHub](../../Skills/Software%20Development/GitHub.md) [Codespaces](../../Skills/Software%20Development/Codespaces.md). Codespaces is a code editor in the cloud with the full power of Visual Studio Code. It allows for real-world hands-on practice that mirrors [Software Development](../../Topics/Software%20Development.md) in the workplace. This course was created using GitHub Codespaces. Using Codespaces, you have everything you need to get going without needing complex installations or build tools. One click and you're ready to go. First, make sure you've signed up for a free GitHub account and that you've logged in. You just see your profile icon right here. When you go to the repository for one of our courses, look for the Code button. Click on Create codespace. The first time you open up a codespace, it might take a few minutes to create the virtual machine. Once it's done, you'll enter the code editor environment. If you're familiar with Visual Studio Code, this is a special version of that editor running on GitHub's servers. The cloud editor's menu lives in a hamburger icon inside the activity bar. The menu will let you see the keyboard shortcuts for your operating system. Let's find the shortcut for the Command Palette. Look in the hamburger menu, under View, and then look for the keyboard shortcut for your operating system. The activity bar has the most common tools you'll need to work with projects.
>
> **[1:32](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/using-github-codespaces-with-this-course?u=76281980&t=92)** You can, for example, show or hide the explorer, do a search and replace for content, manage features of the repository, and much more. By the way, if for any reason you lose the activity bar, you can get it back using the Command Palette. Your course can have one or more extensions pre-installed based on the course you're taking. Those can be found in the Extensions panel. Because this is a browser, it's easy to inadvertently close the browser window and lose the editor. If you do that, don't panic. Codespaces saves everything you're doing on a virtual machine. You can always get the codespace back for this repo by going to the Code menu and finding your codespace right here. Notice that it even remembered that I had opened the Extensions panel. The sample course I'm looking at is what is known as a flat branch repository. You can tell because there's a single branch when you click on the Branch icon on the status bar. You can also tell because there are different folders for each of the videos in the course. Your instructor will let you know what the folder structure is for your course. Forking lets you create your own copy of the repository on your account
>
> **[3:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/using-github-codespaces-with-this-course?u=76281980&t=185)** so that you can keep any changes that you've made, even if you've deleted the codespace. To create your own fork, you can click on the Fork button on the repository. I'm going to hit Create fork. I have my own copy of this repository, and if I want to, I can start a new codespace on that fork. This fork version is almost exactly like the original repository, but it will let you push your own changes. Notice that the URL of the repository is slightly different. Let's go ahead and make a simple change to this file right here.
>
> **[3:54](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/using-github-codespaces-with-this-course?u=76281980&t=234)** I'm going to save it. You'll note that there is an M right here, as well as an asterisk on this branch. In the Source Control panel, you can also see a 1 for the change that we just made. Let's go ahead and try to commit this change. I'm going to hit the Commit button, and I'll ask it to go ahead and stage the changes, and then I'm going to hit this button here to commit this onto my own version of the repo. I'll hit OK. You can also let it go ahead and run [Git](../../Skills/Software%20Development/Git.md) [Fetch](../../Skills/Web%20Development/Fetch.md). That way, it'll automatically sync with your forked repo. Now, that change will be stored in your own version of this repository. Don't worry if you forget to fork a repo and then try to push changes. Codespaces will also ask you if you want to create a fork automatically. Look for additional course-specific tips from the instructor. Now, let's get back to the course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Codespaces](../../Skills/Software%20Development/Codespaces.md) (6), [GitHub](../../Skills/Software%20Development/GitHub.md) (4), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2), [Software Development](../../Topics/Software%20Development.md) (1), [Git](../../Skills/Software%20Development/Git.md) (1)
> **Tools:** github (4), visual studio (2)
> **Definitions:** is a  (3), known as (1), is an  (1)
> **CLI Commands:** make (2), find (1), git (1)
> **UI Navigation:** click on (3), go to (1)
> **Env Vars:** url (1)
> **Analogies:** for example (1)
> **Warnings:** note that (1)

#### [Setup](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/setup?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/setup?u=76281980&t=0)** - [Instructor] This course is using [GitHub](../../Skills/Software%20Development/GitHub.md)'s [Codespaces](../../Skills/Software%20Development/Codespaces.md), which mean everything is already preconfigured for you. To get started, head over to the repository on GitHub. Click on Code, check the Codespaces tab, and click on Create codespace on main. And now you are going to wait a bit. Once you see Visual Studio starting up, wait a bit before you jump into the code. The sign that Visual Studio Code is ready is when you see the "Get started with Jupyter Notebooks" on the right-hand side. And you see that the terminal now is free of output. Now you can go on, close the terminal and these two [Windows](../../Glossary/Service/Windows.md), and you're ready to go. You have a machine with [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), all dependencies installed, and Visual Studio Code ready to run everything. This is going to save you from all the trouble of installing Python and all the scientific applications. But sooner than later, you will need to learn about how to install Python packages with tools such as pip, and also about managing virtual environments. Go and check our offerings on the subject.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [GitHub](../../Skills/Software%20Development/GitHub.md) (2), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (2), [Windows](../../Glossary/Service/Windows.md) (1)
> **Tools:** visual studio (3), github (2), terminal (2), jupyter (1)
> **CLI Commands:** python (3), pip (1)
> **UI Navigation:** click on (2)
> **Analogies:** such as (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)


### 1. Visual Studio Code

[↑ Back to Table of Contents](#table-of-contents)

#### [Use code cells](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/use-code-cells?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/use-code-cells?u=76281980&t=0)** - [Instructor] Writing code when exploring data is a bit different than writing code for applications. You write small pieces of code and you want to see them in action. For that end Visual Studio Code supports code cells. They start with a special comment, so we have a comment and then the two percent signs. And you see now that we have some actions that we can do on the top. You can run the cell either by clicking on one cell or hitting shift and enter. This is going to open an interactive prompt on the right. The first time you run a cell, this might take a bit of time. And here we see the result. Shift and enter will also automatically advance to the next cell, so you can run the next cell again with shift and enter. The interactive prompt can display more than just text. Here we have a piece of code that is going to display the sin of x between minus six and six. If you don't understand the code, don't worry about it. We'll get to it. So shift and enter again. And now we can see also the chart. Let's try and run the code from the command. So I'm going to open a terminal with new terminal, and then [python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) Ch01/01_01/code_cells.py,
>
> **[1:23](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/use-code-cells?u=76281980&t=83)** and nothing is being printed out. The reason is that the interactive prompt is what is known as a REPL: read, eval, print and loop. When you run code from the command line, it only does read and eval. It's not going to print unless you explicitly print. And it's not going to loop unless you do a loop. Let's close the terminal. You can also write code in the interactive terminal without adding it to your code. So let's do 355 divided by 113, and I can hit play to run the code. And this is an approximation of pi. Sometimes you run Command and it takes a lot of time, and you forget to add the result to a variable. If you look at Visual Studio Code, you will see small numbers out here. These are the input and output numbers. So I can do, for example, output number 4 times 10 and run it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **Tools:** terminal (4), visual studio (2), command line (1)
> **Definitions:** is a  (1), known as (1), is an  (1)
> **File Paths:** ch01/01_01/code_cells.py (1)
> **CLI Commands:** python (1)
> **Code Identifiers:** code_cells (1)
> **Env Vars:** repl (1)
> **Analogies:** for example (1)

#### [Extensions to the Python language](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/extensions-to-the-python-language?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/extensions-to-the-python-language?u=76281980&t=0)** - IPython which the Visual Studio Code [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) extension is built on offers extensions to the Python language to make interactive development easier. The first extension is called magics. They start with a percent sign and when I'm going to run the cell, the PWD Magic is going to tell me the present working directory. These extensions are extensions to Python. If you're going to run Python on this file, it is going to complain and the execution will fail. Some cells work on a single line. This is the time magic, and it works on a single line to check how much a sleep of 0.1 seconds takes. But this magic sometimes can work on more than a single line. Starting with 2% signs. These are known as cell magics and I'm going to run this one. It is going to time the whole execution of the cell. Another extension is running shell commands with the bank sign. If I'm going to run this, IPython or the interactive prompt are going to execute the LS command on the Varlog directory and I'm going to see the output. You can do fancier stuff, for example assign dirname to a viable, get the output of ls of this dirname when I'm passing it with a dollar sign as a parameter and now I have files which is like a list in Python. So I can print out how many files I have. Visuals to the code offers more features that will help you become more productive. Do your homework and learn what it has to offer.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (5)
> **CLI Commands:** python (5), ls (2), make (1)
> **Definitions:** is called (1), known as (1)
> **Env Vars:** pwd (1)
> **Versions:** 0.1 (1)
> **Tools:** visual studio (1)
> **Analogies:** for example (1)
> **Speakers:** - ipython (1)

#### [Understand markdown cells](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-markdown-cells?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-markdown-cells?u=76281980&t=0)** - [Speaker] Markdown is a common format to add accomodation with rich text. People even published full books using Markdown in a scientific notation. In Visual Studio Code, you can write Markdown cells by adding the Markdown inside square brackets. When you run the cell, Visual Studio Code is going to render the Markdown as [HTML](../../Skills/Web%20Development/HTML.md). So, we have a header and then a list. Markdown can do more. Here's an example of a link and adding an image. Right, so you have [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md). If you're going to click that, this is going to take us to Python and the Python image. In scientific documentation, you write a lot of math. There is special support for LaTex inside Visual Studio Code markdown cells. You write laTex inside the dollar sign and this is the laTex language if I'm going to run this cell now, you are going to see the equation of Euler identity. If you're not familiar with laTex it's a language for writing scientific paper and it's a bit out of scope of this video. There's a lot of good resource for writing Latex out there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **CLI Commands:** python (3)
> **Code Identifiers:** latex (3)
> **Tools:** visual studio (3)
> **Definitions:** is a  (1)
> **Speakers:** - [speaker] (1)


### 2. NumPy Basics

[↑ Back to Table of Contents](#table-of-contents)

#### [NumPy overview](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/numpy-overview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/numpy-overview?u=76281980&t=0)** - [Speaker] Let's look at one of the building blocks of the scientific [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) stack which is NumPy sometimes pronounce NumPee. NumPy gives us fast homogenous arrays or matrices math functions, [Linear Algebra](../../Skills/Artificial%20Intelligence%20(AI)/Linear%20Algebra.md), randomization, and more. You can ask, why do we need matrices when we have python lists? Python lists are powerful and very fast. However, since they need to accommodate many use cases there's a performance and memory penalty you pay. Let's have a look. So here's the Python integer, and if I'm going to run this one, I'm going to get two to the power of 1000, which is great. but it's going to take a lot of memory. in NumPy, If I'm going to take the N64 and run this cell I'm going to get zero. Because what is known as an overflow, the number cannot fit in a 64 bit integer. In scientific computation, Usually a 64 or 32 bit integer or float can hold all the values you need. under the hood. NumPy uses highly optimized C and Fortran code. Don't look down at Fortran, the Fortran Compiler can sometimes do optimizations that the C compiler can't. especially when dealing with arrays. NumPy offer more than speed, selecting, reshaping multiplication and more. It also provides a lot of scientific functions. All of these functions work both on scalers integers or floats and on arrays. So here we have NumPy sine and you compute the sine of 27.
>
> **[1:35](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/numpy-overview?u=76281980&t=95)** or we can run sine on an array of three elements and get the sine for every element in the array. We import NumPy as np since NumPy has a lot of functions inside. So instead of doing from NumPy import and then a long list we import NumPy as np, and then np dot np dot.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (4), [Linear Algebra](../../Skills/Artificial%20Intelligence%20(AI)/Linear%20Algebra.md) (1)
> **CLI Commands:** python (4)
> **Env Vars:** n64 (1)
> **Definitions:** known as (1)
> **Speakers:** - [speaker] (1)

#### [NumPy arrays](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/numpy-arrays?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/numpy-arrays?u=76281980&t=0)** - [Instructor] Let's start looking into NumPy arrays. So first we input numpy, as np. And now, I'm creating an array of three elements with one, two, and three. Run the cell. And we have an array one, two, and three. For starts, arrays look very much like lists. We can ask for the len, we can get the second element. And arrays are zero-based indexing like the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md). But if you look closer, for example, type of the first element, we see that it's not a Python integer but a numpy.int64. You can ask array what is the data type of the array using the dtype attribute. When you create an array, NumPy will determine the default type for the array for the input. However, you can specify explicitly the data type of the array to be created. This here is saying, dtype=np.int32. And we are going to get an array of int.32. Comparing to int.64, where this takes half the memory, but you need to make sure that all of your values fit inside a 32-bit integer. You can multiply an array by itself. And what you're going to get is an element-wise multiplication. So one times one, two times two, and three times three. I talked about NumPy being fast. Let's have a look. So I'm creating two arrays, each one with 1 million random elements, which are floats. And then, I'm going to use the time matrix to see how much time it takes to multiply them. And that took 6.27 milliseconds. If you'd like to get the dot product
>
> **[1:36](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/numpy-arrays?u=76281980&t=96)** of a matrix, use the @ sign, which is the Python Matrix Multiplication Operator. If you run the cell now, we are going to see 14, which is a dot product of these two arrays. We can move to more dimensions. So here's an array with three rows and three columns. And we can run this one, and now we can see the array. That's a lot of code to write for a small sample matrix. Let's use one of the many utilities we have in NumPy to create arrays. np.arange, it's very much like Python Range, but returns an array. So if you do V equal arange of 12, you are going to get an array from zero to 12. And now, we can use the reshape to reshape the array. So if you're going to run this one, now we're going to get an array with for rows and three columns. So in a single line, we can run mat = np.arange(12).reshape((4, 3)), and then, get our matrix. Can call the dot shape attribute to know what is the current shape or how many rows, columns, and maybe, other dimensions the matrix has. Let's create another matrix by reshaping the original one. Instead of four rows and three columns, three rows and four columns. And now, we're going to change a value in this reshaped matrix. And then, take a look at the original one. And we see that the original matrix is affected as well. When you do a reshape, you get a view of the underlying data. It's not a copy. There are many other methods
>
> **[3:08](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/numpy-arrays?u=76281980&t=188)** and function that work with NumPy arrays. For example, you can transport the matrix by calling the dot T attributes. You'll pick more as you go, but I think this is enough for understanding the basics of NumPy arrays.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (4), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** python (4), make (1)
> **Definitions:** is an  (1), is a  (1)
> **Analogies:** for example (2)
> **Versions:** 6.27 (1)
> **Speakers:** - [instructor] (1)

#### [Slicing](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/slicing?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/slicing?u=76281980&t=0)** - [Instructor] [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) lists support slicing. Here's an example. nums is a list of 1, 2, 3, 4, 5, and we do a slice from 2 to 4. If you're going to run it, you're going to get 3 and 4. We start with the index number 2, index number 3, and up to but not including index number 4. These are half-open ranges. You can do the same for NumPy arrays, so we create a vector with a range and then get a slice of this vector or matrix. Slicing is useful since many times you'd like to take a subset of your data for training, testing, and other tasks. NumPy takes slicing to a whole new level. Let's have a look. Let's create an array with three rows and four columns. When we take the array at location 0, we are going to get the first row. We can also say the second element in the second row, and this is 5, so this one here. We can also do it in another way by saying I want 1, 1 and this will give me the same element. What happens if you want a column? For a column, we're going to use slicing. Say we want all the rows but only the second element for every row, and now we're going to get 1, 5, and 9, which is the first column. Note that we'll get a flat array and not an array of ones. If you want to get the full array of ones, you will need to reshape, so now we get it in a vertical format. You can do slicing on both axes, so all the rows from the second row
>
> **[1:35](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/slicing?u=76281980&t=95)** and the columns from the third column. You can also use slicing to set values, so I'm doing the slicing as before, but now equals 7, and now when I'm looking at the array, I'm seeing that the bottom right is all sevens.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **CLI Commands:** python (1)
> **Definitions:** is a  (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Learn boolean indexing](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/learn-boolean-indexing?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/learn-boolean-indexing?u=76281980&t=0)** - [Instructor] Sometimes you'd like to select parts of an array, not by indices but by some logic, say all the values that are bigger than some threshold. For this we're going to use Boolean indexing, let's see how it works. So we input NumPy and create an array with three elements, let's run it. You can index an array with another array in the same shape containing Boolean values, true or false? So here's the array and I'm adding it an index with true or false, true. And I'm going to run it, I'm going to get only the elements where the index was true. For now this does not seem that helpful, but let's see one more thing and this will make it super useful. If I'm doing array bigger or equal to one, I'm going to get a Boolean array with false, true and true, and now I can combine these two and say array at the location where the array is bigger or equal to one, and I'm getting one and two but not the zero. You can combine these conditions or mask using Boolean operators. However, these are not the normal Boolean operators that we have in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md). You're going to use 1% for and, the vertical bar or and, the Tilda for no. So I have a bigger array now with 10 and I'm saying all the elements that are bigger than two and smaller than seven, note that the parenthesis here is mandatory. And here's an example of negation, so everything that is not bigger than seven. Usually you're going to write it as everything
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/learn-boolean-indexing?u=76281980&t=94)** that is smaller or equal to eight. Let's do a more realistic example, we're going to find outliers using the standard score. First we're going to create our data. So I'm going to create an array of thousand elements with values between zero and 10 with the normal distribution, and then I'm going to add two out outliers at location 33, at location 832. Now I'm going to calculate my mask. So the mask is where the absolute value of the value itself minus the mean, meaning the distance from the mean, is bigger than two times the standard deviation inside the array, and then I'm going to calculate values in the mask, I'm going to see exactly my outliers over there. I can even use this mask to change the values, let's say to the mean of the current array. This is an example for the power of Boolean indexing. You can do a lot of things in very few line. Apart from the cool effect, this is also very fast. In numerical Python, you try hard not to do any follow ups. This method of computation is called vectorization and once you use it, everything runs at the sea or photon level of NumPy.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2)
> **CLI Commands:** python (2), make (1), find (1)
> **Definitions:** is an  (1), is called (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Understand broadcasting](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-broadcasting?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-broadcasting?u=76281980&t=0)** - [Instructor] Let's have a look at the piece of code. We input NumPY as p. We create a range and then add four to it. Let's run this one. We added the scalar to a vector. NumPY does what makes sense, it adds the scaler to every element of the array. This is known as broadcasting. Broadcasting work for other dimensions as well. So we're going to create three to three matrix and then add a vector of three and then matrix plus the vector. And this time we add the vector to every row. We can even do a horizontal vector and a vertical vector and add them together. NumPY will extend these vectors and when we add them, we'll get a matrix of three by three. It'll expand V1 vertically and V2 horizontally to match each other. Here's an image from the astroML site that will help you understand what broadcasting is doing. This is roughly the operations we just did. NumPY tries hard to make broadcasting possible but sometimes it is going to fail. I highly recommend you watch "Losing your Loops" by Jake VanderPlas to understand fully what broadcasting can do for you. Try to follow what Jake is doing in his kNN example. Run the code, tinker with the data, and generally, have fun.

> [!info]- Semantic Content
>
> **Code Identifiers:** astroml (1), knn (1)
> **CLI Commands:** make (1)
> **Definitions:** known as (1)
> **Speakers:** - [instructor] (1)

#### [Understand array operations](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-array-operations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-array-operations?u=76281980&t=0)** - [Instructor] Let's see what an array can offer us. So, "import NumPy as np", and then create an array of three rows and three columns. Let's run it. I'm going to do "v.". You can see a lot of things that can be accessed from the array. Let's take a look at some of these attributes. So, "v.T" returns the transpose of the matrix, "v.any" will return true since there is at least one element in the array which is true. As a reminder, everything in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) is true except the zero value numbers, empty collections, none, and of course, false itself. We also have "v.all", and this one is going to return false. This is because we have one false element at the beginning, which is zero. Any and all are useful functions, and you'll find out you're going to use them in various situations. Know that the truth value of an array is undefined, so if I'm going to "if v:print (ok)" and run the cell, I am going to get an exception. The Zen of Python states that, "In the face of ambiguity, refuse the temptation to guess." Experience shows that it's better to be safe than guess something that would lead to a wrong computation. This is why NumPy refuses to guess the truth value of an array, and you will need to be explicit and state either any or all. NumPy also provides some mathematicals like min, max, mean, SD, sum, prod, and others. Let's take the product of the array,
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-array-operations?u=76281980&t=94)** which is zero because we have zero at the beginning. This method work by default on the whole array, but you can specify which axis you'd like the operation to happen. For example, if you do axis equal one, you'll get the sum of the rows, and if you'll do axis equal zero, you'll get the sum of the columns. This will work with array with more than two dimensions as well. Just provide the right axis. Remember we said that NumPy works hard not to copy anything, but sometimes you do want to copy and mutate an array without affecting the original one. For this, you can use the copy method, so V1 is a copy. We change v1, the first element, to be minus one and then look at the original one and see that nothing has changed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2)
> **CLI Commands:** python (2), find (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Understand ufuncs](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-ufuncs?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-ufuncs?u=76281980&t=0)** - There are many functions defined in numpy, how many, let's see, will input numpy, and then look at how many elements the dir command returned. And you're going to see 603, this might change depending on the numpy version you have. We are not going to cover all of the 600 plus attributes, but as you start working with numpy you'll find the ones useful for you and commit them to memory. However, there's one aspect of numpy functions I like to discuss. So if I'm doing the sine of 90 I'm going to return a number, remember, sine gets its values as radians and not degrees. You might wonder when numpy is defining function that's already defined in the built-in math model, the reason is, that we can take creative vector which is np.arange from minus three to three and one numpy sine on that, and it's going to work, going to get the sine of every element in dir, the built-in math sine will not work. A lot of numpy functions are ufuncs or universal function, it means they work both on scales, regular numbers and arrays. This is very handy and let's us work with the same function regardless of the input type. What happens if you like to write your own function? Do you need to check the type of the input every time? Well, you can, but numpy provides an easier way. Let's have a look. So let's define ReLu, which if N is less than zero return zero, otherwise return N. And if you run it on a scaler it is going to work. However, if you try to run it on our vector,
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-ufuncs?u=76281980&t=94)** this is going to raise an exception, it'll try to do if N smaller than zero which is going to return and numpy array, and the true value of a numpy array is not defined. What you can do is use the np.vectorize, the N at the beginning meaning we're using the np.vectorize as a decorator. If you're not familiar with decorators, we have several courses on the subject, so the same function, but now vectorized. And now when we run it, we can run it on the vector but we can also run it on the number, which is something that is not familiar. This is an array with one value, however, this array behaved just like a number, so if I'm doing our function on minus two and removing seven from it, I'm going to get minus seven. Ufuncs are also NaN aware, which means that if they see NaN, the special floating viral, that means not a number, they will return NaN as well. So we create an element with NaNs and we get the result with the NaN at that location.

> [!info]- Semantic Content
>
> **Definitions:** is an  (1), means that (1)
> **CLI Commands:** find (1)
> **Analogies:** just like (1)
> **Speakers:** - there (1)

#### [Challenge: Working with an image](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-working-with-an-image?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-working-with-an-image?u=76281980&t=5)** - [Instructor] Say we have this image of a flower. We can use Matplotlib to load this image into memory. So, we use the imread method, we create a copy to make it readable, and then I'm going to use plt to show it. If you're going to run the cell, you're going to see the flower image. If you look at the type of the image, you're going to see that it's a numpy array. And when we look at the shape of the array, we see it's 1200x900 on three. These are the number of pixels, so 1200 on 900, and three values for the color, red, green and blue. What I want you to do is draw a blue square around the flower. The top left should be at 190 on 350. The bottom right should be on 680 and 850, and the line width should be five.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Working with an image](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-working-with-an-image?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-working-with-an-image?u=76281980&t=5)** - [Computer User] Let's have a look at my solution. First, we're going to run the code. So we're going to load the image. And we're going to print the type and the shape. Now we're going to look at our code. Let me hide the left side. So we define the top left X and the top left Y, and the bottom right X and the bottom right Y and the width. Also the color, which is blue, right? We have RGB, red, green, blue. Red and green are zero and blue is full. And now we draw the lines using slicing. So from the top left to the top left plus width up to the top left Y to the bottom right Y equal color. This is the top line. Then the bottom line, which is bottom right X. The bottom right X plus width, top left Y, bottom right Y. And then we're doing the vertical lines. So top left X to bottom right X, top left Y to top left Y plus the width. And then finally the right line. Top left X to bottom right X, bottom right Y minus width to bottom right Y. And then we are going to show the image. So let's scroll a bit up and run this cell. And now we see the picture and we have a square around the flower.

> [!info]- Semantic Content
>
> **Env Vars:** rgb (1)
> **Analogies:** picture (1)
> **Speakers:** - [computer (1)


### 3. pandas

[↑ Back to Table of Contents](#table-of-contents)

#### [pandas overview](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/pandas-overview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/pandas-overview?u=76281980&t=0)** - [Instructor] Data in the real world is often composed from values that have mixed types. Some can be numeric: age, for example. Some can be textual: names, for example. Some can be time, like the year of birth, etcetera, etcetera. [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) is a library built for real-world data, data that is messy, incomplete, and from various types. Once pandas was released, it was adopted by the scientific community as the tool for dealing with data. Pandas also excels with working with time-series data. You can easily select subsets of data by time and also change the frequency of the events to suit your needs. You can think of pandas as providing in-memory [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) data sheet, which we call DataFrame, with many, many extra features. Panda's DataFrame can process large amount of data. I've personally worked with millions of rows, and it can draw charts, join other DataFrames, [Pivot Tables](../../Skills/Data%20Science/Pivot%20Tables.md), and more. Apart from being very useful, pandas is one of the best-documented projects out there. Documentation contains many, many code examples. It is also worth reading the book, "Data Analysis with [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md)," which was written by panda's creator, Wes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (5), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1), [Pivot Tables](../../Skills/Data%20Science/Pivot%20Tables.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **Analogies:** for example (2)
> **CLI Commands:** python (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Loading CSV files](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/loading-csv-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/loading-csv-files?u=76281980&t=0)** - [Instructor] Going to have a look at the log of one of my runs a few years back. We'll start by looking at the data, which is in CSV format. [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) is going to load the whole CSV into memory, and it's a good idea to have a quick look at the data before you load some corrupted data or a file that is too big to fit in memory. If you don't have enough memory on your computer, you can spin a machine in the cloud with a lot of memory, do your work, and then tear it down. This approach is usually very cost effective and much easier than using a [Big Data](../../Skills/Data%20Science/Big%20Data.md) platform. If you think your data is too big, remember that you can spin a machine with several terabytes of memory. So here is our data. We have four columns, time, latitude, longitude and height. So let's start. First, let's look at the size of the file on this. So I'm going to use pathlib and then define what is a kilobyte. Define the CSV file and use the start method to get the size and divide it by kilobyte. ST size is in bytes. So let's run this one and I'm going to hide the sidebar. So 42.6 kilobytes. You can do the same thing with the shell command. So we start with the bank sign, telling the IPython or the interactive prompt to run a shell command. And this is an extension to [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md). And we pass the name of the CSV file prefixed by the dollar sign as a variable. Note that this command will work only on [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) or on Mac, not on [Windows](../../Glossary/Service/Windows.md). Now, let's see how many lines we have.
>
> **[1:32](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/loading-csv-files?u=76281980&t=92)** So we do with CSV file open and you should open always file with a with statement. This makes sure that they are closed when you are done with them. So we're going to run it, count the lines and also print the first five lines. And we can do the same with the shell command. First, the head utility to see the first five lines, and then the WC dash L to see how many lines we have in there. Now we can load the data frame into pandas. So we input pandas as PD, and DF is PD read CSV. The variable holding the data frame is traditionally called DF, and we are going to print how many rows are in the data frame. So len of DF. And now we are going to get 740, while WC showed us 741. Pandas is loading the first row as the column names and counting only the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the rows, which are the data rows. Let's have a look at the column. So df.columns, and we see we have time, latitude, longitude and height, which matches what we actually see in the CSV. We can also run the info method to get some information about every column. So the column, how many non values we have, and what is the data type of every column. We see that the first column is an object, which means a string. And then we have floats for latitude, longitude, and height. You can also run the describe method, which will give you information about numerical columns. So only the latitude, longitude, and height.
>
> **[3:06](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/loading-csv-files?u=76281980&t=186)** You'll get the count, the mean, standard deviations and other interesting [Statistics](../../Skills/Data%20Science/Statistics.md).

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (4), [Big Data](../../Skills/Data%20Science/Big%20Data.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1), [Windows](../../Glossary/Service/Windows.md) (1)
> **Env Vars:** csv (7)
> **Definitions:** is an  (2), is a  (1)
> **CLI Commands:** python (1)
> **Versions:** 42.6 (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Parse time](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/parse-time?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/parse-time?u=76281980&t=0)** - [Instructor] Let's load our DataFrame. So we import [pandas](../../Skills/Software%20Development/Pandas%20(Software).md), and we read the CSV, and we display the data types for each column. You can see the latitude, longitude, and height are floats, and the time is an object, which usually means a string in Pandas. In some cases, it's okay for data to be a string, but in our case, you expect some kind of a timestamp. Unlike [JSON](../../Skills/Web%20Development/JSON.md) and other formats, CSV don't have type information embedded with the data, which means that everything comes out as a string. Pandas does a good job at guessing types, but here, it needs our help in parsing the time. There are many ways to write time as a string. Pandas parser know most of the common formats. If you need to write time as a string, do yourself a favor and use a known format, such as RFC 3339. Also, pick a format without spaces in it. And have the year first, so sorting time as a string will work as intended. Let's take a look at the read_csv method. It has many, many arguments. One of these arguments is parse_dates, where you can pass a list of columns to be parsed as times. This is where our initial look at the data came handy. You know, before loading, which column you'd like to parse as time. Let's do this. Going to hide the sidebar. So now we doing pandas with CSV, with the csv_file, and tell it to parse the dates with only a single column, which is a time column. And run the cell.
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/parse-time?u=76281980&t=94)** And now, we see that the time is datetime64[ns], which means a 64-bit timestamp in a nanosecond resolution. On your machine, this might be a little bit different.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (5), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Env Vars:** csv (3), json (1), rfc (1)
> **Code Identifiers:** read_csv (1), parse_dates (1), csv_file (1)
> **Definitions:** is an  (1), means that (1), is a  (1)
> **Documentation:** rfc (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Access rows and columns](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/access-rows-and-columns?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/access-rows-and-columns?u=76281980&t=0)** - Let's access some data. So first we load the data frame and I'm going to hide the side column. To get a column, you can use the square bracket and the name of the column, so DF latitude and I'm getting the whole latitude column. You can also use df.la like an attribute access but I don't recommend using the dot plantation. In some cases, column names have spaces in them and then you can't use the dot plantation anyway, so get in the habit of using square brackets, which always works. You can select more than a single column. So here I'm passing a list with two column names and I'm going to get back at data frame with two columns. If you want a specific value you can first select the column and then the row. So selecting the latitude column and then the first value. Remember that [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) is zero based and not one based. The first item is at zero location. If you want an entire row you can use the dot lock accessor, so dot lock. And now we get the rope. So we see the time, latitude, longitude, and height. Lo can also work with slices as well, so from two to seven. Unlike slicing in Python, which are half open the dot lock in [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) slices from the start to the end, including the end. You can also combine slices and column selection so we're selecting only the latitude and longitude columns. And then getting from two to seven. This time plant is going to use the Python style slicing which is a half open range, meaning you get the first index
>
> **[1:35](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/access-rows-and-columns?u=76281980&t=95)** up to, but not including the last one. And now it's a bit confusing. The best way to make these things sink in, is to practice take some data frames, slice and dice them until you get comfortable with the results and it'll be just fine. Every column in Panas is as serious. One of the differences between seriousness and regular Python lists is that they have a labeled access called an index. All the columns in the data frame share the same index so we can do df.dot index and then run the cell. And this is a range index starting from zero until 740 with steps of one. However, it doesn't have to be this case. Let's create a small example. So we create a data frame. We have some values with number by a range, five rows and two columns. We specify the columns and then we say the index is A, B, C, D, and E. If you're going to run disco code, you see now that the index series A, B, C, D, and E. And now if you're going to use dot lock zero this is going to fail because there's no row labeled zero. But there is a row labeled A, so I can df dot lock A. I can also slice between A and D and Panas can handle it just as well. As a side note, labels don't have to be unique. If they're repeating labels, you will get all the rows with this label. Sometimes you'd like to access the first row regardless of the label. In this case, you can use the IOC accessor, which works by position.
>
> **[3:09](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/access-rows-and-columns?u=76281980&t=189)** So df dot I lock at location zero and this is going to work and bring us the first row. Another kind of index you can have is time-based index. So let's change the data frame index, which currently if we look at it is a range index and we are going to set it to the time and have a look at it. And now it is a daytime index with all of these values. Now, if you're going to run the lock zero we are going to see that it's going to fail because we don't have any role that is labeled with zero. However, I can access a row by time and I can pass the time as a string. and Panas is fine with it. You can even access time index with the time unit. So here we are going to access everything that is at 3:48 and we we're going to get all the roles that fall in this minute. By the way, I'm not jogging at these crazy times. These times are in Newt Sea and I'm at Israel which means the time was actually 6:48 AM. Still early, but not that early.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (4), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **CLI Commands:** python (4), make (1)
> **Definitions:** is a  (5)
> **Ports:** :48 (2)
> **Env Vars:** ioc (1)
> **UI Navigation:** select the (1)
> **Speakers:** - let (1)

#### [Calculate distance](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/calculate-distance?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/calculate-distance?u=76281980&t=0)** - [Instructor] Let's calculate how slow I jog. For speed, we need time and distance. We have latitude and longitude, and we're going to cheat and use Euclidian distance instead of distance on a sphere. So first, we're going to load the data with read_csv. I'm going to hide the side frame. So let's have a look. One latitude in kilometers is about 92 kilometers, and one longitude in kilometers is about 111. It varies depending on where you are on earth, but it's good enough for what we're going to do. So we are going to define a distance function which takes the delta latitude, the lateral longitude, multiply them by the constants, and then use NumPy to get the Euclidian distance. So, we're going to run the cell. There is no output, but now we have the function definition. Let's have a look. So, let's take first longitude, then latitude, and the second one. So we'll take 200 and 201, and we are going to calculate the distance between them, and this is going to work. Now we need to calculate the distance between every row. Remember, we want to avoid for loops as much as possible. Let's use the shift method to avoid looping. Here's an example: So, I'm creating a series of five elements, so zero to four.
>
> **[1:22](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/calculate-distance?u=76281980&t=82)** Now if I'm going to run the shift operator, I'm going to get none as the first one and every element is shifted downward. Shift can also work in the other direction if I give it minus one. So now I have none at the bottom and everyone is shifted up. Now we're going to use shift. So we get a distance, we take the latitude and longitude and the shifted latitude and longitude. If you're going to run this one, we're going to get none again for the first value and then distances which are in kilometers. Let's do a sanity check and sum all of the distances. 4.7 kilometers? That seems about right. Now we'd like to calculate the difference in times. This one, we're going to use the diff method. And if you're going to run it now, we're going to see again, not a time for the first one and then the difference in times: 17 seconds, zero seconds, one second, et cetera. If you look at a stick, we can specify the length of the stick in various ways, right? We can use centimeters, millimeters, nanometers, parsecs, and more. Same goes for time deltas. It's a span of time, and we need to convert it to a number by specifying the unit. So, what we are going to do is times_hour. We are going to take times and divide it by panda's time delta saying one hour. And now we see the numbers as fractions of an hour.
>
> **[2:57](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/calculate-distance?u=76281980&t=177)** Finally, we can calculate the speed. So, we do distance divided by times_hour, and we are going to see the speed. Right? And this speed is in kilometers per hour, and I'm not running 35 kilometers per hour. That's the speed of the fastest man alive, probably. This is an error in the measurement.

> [!info]- Semantic Content
>
> **Code Identifiers:** times_hour (2), read_csv (1)
> **Versions:** 4.7 (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Display speed box plot](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/display-speed-box-plot?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/display-speed-box-plot?u=76281980&t=0)** - [Instructor] Let's run our code, load the data and calculate the speed. Now we can use the describe method of the speed to get some [Statistics](../../Skills/Data%20Science/Statistics.md) about what is the mean standard deviation and other values. I'd like to use box plot for first impression of the data. So let's do that speed.plot.box. If you're going to run this, you're going to see a box plot. A box plot shows the distribution of data. The line in the middle is the mean. Now we have the quintiles and the whiskers are the inter quantile ranges. And all of the dots outside of the whiskers are considered outliers. These quick plots are great when exploring data, but when you present the data, you better have some nicer charts. Don't underestimate nice, it sells. Most of the time when I explore the data I plot basic charts, but when it's time to present I invest some time in making them nicer. It helps the readability but also makes a better impression. There's no end to how much you can fiddle with your charts. Here are some basic things you can do to make your charts nicer. So I'm going to input matplotlib, and I'm going to say that the figure size is 10 over six. This is in inches, but will make the figure bigger. And then I'm using style. Matplotlib has several styles. I like the seaborn-whitegrid and now I'm going to plot again. And now we get slightly bigger chart. There is a difference at look and I have these lines telling me what are the values. It's easier to see with the grid.
>
> **[1:33](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/display-speed-box-plot?u=76281980&t=93)** I can also set some other things. So I have a none here at the bottom. I'd like to remove it so I'm going to say that the name is nothing. And then I'm going to plot and give it a title. Miki's run. And then I'm going to set the Y label with the running speed off. And then here between these dollar sign I'm writing later for showing equations. When I run this one, I get a chart. It has the label, Mickey's run, the none is gone. And here you see running speed and then kilometer over hours.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Statistics](../../Skills/Data%20Science/Statistics.md) (1)
> **CLI Commands:** make (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Taxi data mean speed](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-taxi-data-mean-speed?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-taxi-data-mean-speed?u=76281980&t=5)** - [Instructor] I'm asking you to calculate the mean speed in miles per hour in taxi.parquet. To get the file taxi.parquet, you will need to run the download data [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) script in order to get it, and you're going to use the [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) read parquet instead of read CSV to load the data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **CLI Commands:** python (1)
> **Env Vars:** csv (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Taxi data mean speed](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-taxi-data-mean-speed?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-taxi-data-mean-speed?u=76281980&t=5)** - [Instructor] Let's take it, my solution. As a reminder, you will need to run the download data or copy taxi.parquet from the challenge in order to be able to work. So importing [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) and using the read_parquet to read the file. We have some bad lines in the data. The good lines are the ones that the drop off time is bigger than the pickup time. So I'm going to create a mask, a boolean mask, and then get only the rows that match this mask from the data frame. Now I can do the calculation. So times is the drop of time minus the pickup time. Times in hours is the times divided by delta of one hour, and the speed is the distance, which is in miles, divided by the hour. And now, we can get the mean speed, which is 17 miles per hour in New York.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **Code Identifiers:** read_parquet (1)
> **Speakers:** - [instructor] (1)


### 4. Folium and Geospatial Data

[↑ Back to Table of Contents](#table-of-contents)

#### [Create an initial map](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/create-an-initial-map?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/create-an-initial-map?u=76281980&t=0)** - [Instructor} Folium is a library for creating web-based interactive maps. Under the hood Folium uses leaflet.js [JavaScript](../../Skills/Software%20Development/JavaScript.md) library and leaflet.js uses Openstreetmap to render maps. Let's load the log of one of my runs. So we're going to load track.csv we're going to tell [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) that the time column should be passed as a timestamp, and we also say that the index column is the time. So now if you're going to run this you're going to see that we have only latitude, longitude, and height. The time column is now in the index. So we look at the index the first five rows we're going to see that. Okay, so how are we going to work with volume? We're going to input volume and then get the center of the map which is the mean of the latitude and longitude. We're going to get the center of the map which is the mean of the latitude and longitude. and then we're going to create a map which is folium.map. We say the location is the center and we're going to say the [Zoom](../../Skills/Software%20Development/Zoom.md) level which is 15. I don't have a good rule of thumb for the initial zoom level. Just play with it until you get it right. So let's run this one. And now we have an interactive map, right? We can move it around, zoom in, zoom out, and do everything we do with maps. You can even save the map to an [HTML](../../Skills/Web%20Development/HTML.md) file and then open this file in the browser. If you go back to look at the files we see now that we have track.html, which is an HTML file you can open it in browser. You'll get an interactive map.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Zoom](../../Skills/Software%20Development/Zoom.md) (4), [HTML](../../Skills/Web%20Development/HTML.md) (3), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **File Paths:** leaflet.js (2), track.csv (1), track.html (1)
> **Env Vars:** html (2)
> **Definitions:** is a  (1), is an  (1)
> **Cross-References:** go back to (1)
> **Best Practices:** rule of thumb (1)
> **Speakers:** - [instructor (1)

#### [Draw a track on map](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/draw-a-track-on-map?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/draw-a-track-on-map?u=76281980&t=0)** - [Instructor] Now let's draw a track on the map. So first we're going to load the data frame, and I'm going to hide the sidebar. And now I'm going to use volume, create a map, and then get a location, create a marker on this location, and add this marker to the map, and finally, show the map. Right, when we run this cell now, we see a marker we can tweak around with the marker and style them. For example, we can use a red circle. So I'm going to use now a circle marker instead of a regular marker and say that the color is red. And now we get a circle marker. Another nice thing you can do is add a popup. So we have the circle marker in the location and color is red, and I'm adding a popup with, "Hi there." So if I'm going to run the cell, now I have the marker. But now when I'm clicking on the marker, I'm going to see my popup. There are many styles of markers in volume, and the popup can be [HTML](../../Skills/Web%20Development/HTML.md). Check out the volume documentation for more details. Now that we know how to place one marker on the map, let's throw the entire track on the map. For this I'm going to create a function that will add a marker from a row in the data frame. So I'm going to get a tuple from the latitude and longitude, create a circle marker in the location, which has a radius of five, red color, and the pop-up is going to be the time. So I'm getting it from the row name, which is the index, and I'm going to format it as our column and edit the marker.
>
> **[1:35](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/draw-a-track-on-map?u=76281980&t=95)** Now check it out. Let's take one row first and see that. So I'm calling add marker on the 200th row, we're using the .iloc, and then showing the map. Let's run the cell. And now we see the marker. And if you're going to click on the marker, we're going to see the time. Once we see that the code is working for a single row, we can do it for the whole data frame. So I'm going to create another map and then call apply from the data frame on end marker with access equal one, meaning do it on every row and display the map. This took a while, and the markers overlap. You need less data points. And to do so, we are going to resample data. So I'm going to create a new map. And then I'm going to create a data frame by using resample. And I'm going to say minute frequency and take the mean of every longitude and latitude that falls inside that minute frequency. And now I'm going to apply this ad marker function, but on the minute df. Again, with access equal one. This looks much nicer. This workflow is very typical with visualization. You need to do some iteration until you get a good one. To get a great one, you need to invest more time tweaking it. But for now, I think we're good.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** html (1)
> **UI Navigation:** click on (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Using geospatial data with shapely](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/using-geospatial-data-with-shapely?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/using-geospatial-data-with-shapely?u=76281980&t=0)** - [Instructor] Sometimes we'd like to do more than just draw points on a map. For example, you might like to know if a location, a point, is inside the city limit. Cities are represented as polygon. And the question maps to, is this point inside that polygon? There are several libraries in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) to work with what is called GIS, Geographic Information System and geometry in general. We are going to have a look at one library called Shapely. The two main [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) that we're going to use are a point and a polygon. So from shapely geometry, import point and we are going to create a point and show it. And the interactive prompt is showing the point as, well, a point. But you can ask the point, what is the dot X and the dot Y of the point? Now let's take a look at the Polygon. So input the Polygon and create it. And when we run it, again, the interactive prompt is going to show us a square because this is the Polygon. You can ask Polygon several questions. For example, what is the area? And what is the center of the polygon? You can also ask whether this point is inside the polygon by using the intersects method. So for this point, this is true. Let's take a point, 10, 20, which is outside the polygon, and now it's going to be false. So let's load our tracking data. So we're going to use [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) read CSV from our track. We are going to say parse the time column and use it as the index column. Finally, we're going to resample the data in a minute frequency.
>
> **[1:35](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/using-geospatial-data-with-shapely?u=76281980&t=95)** And now we're going to add a new column to the data frame. This column is going to be a column of points. So we only take the latitude and the longitude columns, apply point to them, and we use the head command to see what we have. So now we can see we have a new column called point and every value inside of it is a shapely point. So let's define our polygon. We're going to take the middle latitude and the maxim latitude and the middle longitude and the maxim longitude. And we're going to create our polygon. Which again is going to look like a square. And now we can say which rows in the data frame are inside that polygon. So we can say data frame point and apply the polygon intersect, which is going to return us a series of Boolean values. So Boolean indexing to find the rows. And we're going to see several rows that are inside the polygon. Let's plot the polygon on the map. This can get a bit tricky since folium expects a series of latitude longitude. While if you ask the polygon for its exterior you're going to get two arrays, one of X's and one of Y's. We are going to do some data shifting here. This is quite common, but lucky for us, Numpy makes it super easy. We're going to use the stack function from Numpy. Let's run Numpy stack on the exterior. And this is almost what we want. We just need to rotate the array by 90 degrees to get the pair of coordinates per row. This is called a transpose
>
> **[3:07](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/using-geospatial-data-with-shapely?u=76281980&t=187)** and it's done with the dot T attribute. Now we have the data in the right shape. It's easy to add it to a map, right? So we're going to create a folium map with the location in the mean with the [Zoom](../../Skills/Software%20Development/Zoom.md) start. And then do the stack to get the points. Add a child with the poly Line from volume and show the map. And now you see we have this polygon on the map. Now let's draw our track using different color for points that are in the polygon or outside. So we're going to define the center, create the map, and then we're going to add the poly Line. And now we have our add marker that's going to get the location from the raw latitude and check if we are inside the polygon using the polygon intersects with the point. And now we create a marker and location. The radius is five. And then the color is going to be yellow if you're in the polygon. Otherwise it's going to be green. And we also add the popup for the time. And add the marker to the map. We call the data frame apply with the add marker function and axis equal one, meaning running it on every row. And let's run the cell. And now you can see the track. All the points outside are green and the one inside are yellow. I think it's pretty cool what you can do with very few lines of code to show map interactive with GIS information inside of them. If you work a lot with GIS information, I suggest you look at the package called GeoPandas,
>
> **[4:40](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/using-geospatial-data-with-shapely?u=76281980&t=280)** which adds GIS data types to data frames.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1), [Zoom](../../Skills/Software%20Development/Zoom.md) (1)
> **Env Vars:** gis (4), csv (1)
> **Definitions:** is called (2), is a  (1)
> **CLI Commands:** python (1), find (1)
> **Analogies:** for example (2)
> **Speakers:** - [instructor] (1)

#### [Challenge: Draw the running track](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-draw-the-running-track?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-draw-the-running-track?u=76281980&t=5)** - [Instructor] Draw the running track from track.csv. Sample the data to a minute interval, and then markers should be blue if the height is below 100 meters, otherwise red.

> [!info]- Semantic Content
>
> **File Paths:** track.csv (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Draw the running track](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-draw-the-running-track?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-draw-the-running-track?u=76281980&t=5)** - [Instructor] So here is my solution. First, we're going to load the dataframe, pass the dates, the index call, and then resample permanent frequency. Next, we're going to create the map, so importing folium, and then creating a map with the location at the mean, and the [Zoom](../../Skills/Software%20Development/Zoom.md) start is 15. And now the add marker function, so getting the location from the latitude and longitude, creating a circle marker at that location with radius five. And now the color, it is blue if the height is less than a hundred, otherwise it's going to be red. And we are going to add a pop up for the time. Finally, we add the marker to the map and then we call the dataframe apply method on add marker with access equal one, meaning walk on every row, and display the map. And now, we can see the track. Most of the dots are below 100 meters. The beginning is a bit higher, so the points are red.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Zoom](../../Skills/Software%20Development/Zoom.md) (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 5. NYC Taxi Data

[↑ Back to Table of Contents](#table-of-contents)

#### [Examine data](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/examine-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/examine-data?u=76281980&t=0)** - [Instructor] We're going to look at some data about taxi rides published by the New York City. Before you can start, you need to run the download data script in the video folder. This will download the taxi.csv file. It might take a little bit, so I suggest that you'll keep this file around and copy it over from chapter to chapter. Let's see if you can safely load this data into memory. So from pathlib we import path, we create the csv file, define what is a megabyte, and then use the stat method of the file to get the size, which is in bytes, and divide it by a megabyte. Let's run this. And it's about 163 megabytes. It is safe to load into memory. Next, we're going to have a look at few of the initial lines and see how many lines there are in total. Initialize the number of lines, open the file, and use enumerate. If the line is one of the first five lines, I'm going to print it out. And always increment the counter. Finally, print out the number of lines. And we're going to run this one, you're going to see we have the vendor id, the pickup and drop off time, how many passenger in the passenger account, the distance, and many more attributes. And we have about a million lines.

> [!info]- Semantic Content
>
> **File Paths:** taxi.csv (1)
> **UI Navigation:** open the (1)
> **Exercise Files:** download the (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Load data from CSV files](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/load-data-from-csv-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/load-data-from-csv-files?u=76281980&t=0)** - [Instructor] Let's load the data from the csv. You need to either run the download csv, or copy over taxi.csv from one of the other chapters. So let's start. Import [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) as pd, and we are going to use pd read csv to load the file. And finally, we're going to print out the length, adding a thousand comma separator for the number of rows. Let's run the cell. And you see we have a thousand rows. Let's have a look at the first one by using the iloc accessor for row number zero. So run the cell, and we're going to see we have a vendor ID, we have the pickup and drop off time, how many passenger, trip distance, and many other attributes. Let's look at the data types. So I'm calling df.dtypes, and this is going to show me the data type per column. We see that the vendor ID is an integer, but the pickup and drop off are objects, which means strings. Let's fix that. We know they are time, so we define the time columns as the pickup and the drop off. And now we use the read csv, and tell pandas to parse dates in these time columns. And now we can look at the data type again. Now we see that both the pickup time and the drop off time are date time as we want them. A lot of time you'll get data which is a bit messy and not formatted properly. This is why it's important to have a look at the data and see that what you load looks okay before doing anymore work.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (2)
> **Definitions:** is an  (1), is a  (1)
> **File Paths:** taxi.csv (1)
> **Speakers:** - [instructor] (1)

#### [Working with categorical data](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/working-with-categorical-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/working-with-categorical-data?u=76281980&t=0)** - Let's load our data. So we import [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) as pd. We define what are the time columns and we use the read CSV method to load the data passing the time columns and looking at the data types. And you can see that the Vendor ID is an integer. Let's see how many unique values are there using the unique method of the column. And we see we have only two values. We'd like to refer them by name. You can access the [Metadata](../../Skills/Web%20Development/Metadata.md) or the scheme of the data. And here we see that Vendor one is Creative and Vendor two is Verifone. So, let's create a map saying one is Creative, two is VeriFone. And now we're going to add a new column, called Vendor which is applying this map on the Vendor ID. And we're going to look at the head to see the first few values. And you see VeriFone and Creative. The Vendor column is a string column. Since there are only two Vendors, it seems wasteful to generate string per row. Let's see how much memory this column takes. So we define a megabyte and then we call the memory usage method on the column with deep=true to calculate the strings divided by megabyte. And you see it takes 67 megabyteS. This kind of data, when there are few possible values that repeat is called Categorical data. Pandas has a special type for Categorical data that is much more space efficient. So, we are going to do
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/working-with-categorical-data?u=76281980&t=94)** data frame Vendor=df Vendor.map Vendors just as before. But now we are going to add S type category and then look at what we have. And you see that it looks like before, VeriFone and Creative but now the data type is a category. Let's see how much space the Vendor column is consuming now. So right now it's a single megabyte versus the 67, as before. You can get the underlying data with the .codes attribute. So if you're going to run this one, we're going to see now, that we get the underlying ones and zeros. Not that these are different from the one and two we had in the data. These are the categories and numbers, not what was in the CSV file. Categories are easy to work. For example, you can compare a category to a string. So let's check how many rows are there, that the vendor is VeriFone. And you see about half of them. There are more things you can do with categories. For example, you can order them. This is useful when you have answers from a survey and you'd like to say that very much is bigger than somewhat read the excellent documentation for more details and examples.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (2), [Metadata](../../Skills/Web%20Development/Metadata.md) (1)
> **Definitions:** is a  (2), is an  (1), is called (1)
> **Env Vars:** csv (2)
> **Analogies:** for example (2)
> **Code Identifiers:** megabytes (1)
> **Speakers:** - let (1)

#### [Work with data: Hourly trip rides](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-hourly-trip-rides?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-hourly-trip-rides?u=76281980&t=0)** - [Instructor} Let's plot how many rides we have on each day. To do this, we're going to use grouping. If you're familiar with [SQL](../../Skills/Data%20Science/SQL.md), you already know about grouping, it's span equivalent of SQL's group by. We probably want to do something like group by the date and count from the rides and group by the date again. If you don't understand SQL, don't worry about it but I highly recommend you learn some SQL. As a data scientist, this is practically a required language. So, first we start by uploading the data frame. Remember to run the download data or copy taxi dot csv. So import [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) as pd define the time column, read it and then set the vendor name from the vendor ID. And let's run this. And now we can look at the times. So going to look at the pickup time. So let's do the head. And we see several times these times have hour, minute, and second component and we are only interested in hours. Let's take a look at the timestamp. So we create a new timestamp from November 1st, 2021 and we look at this timestamp and now we can use the floor method to round it up for days. capital D stands for days. Pandas has several offset aliases You should read the documentation and to know which one to use. We are going to use, as I said, the day which is calendar day frequency. Now let's try flooring up some rows.
>
> **[1:33](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-hourly-trip-rides?u=76281980&t=93)** So pick up daytime, take the head and we do a floor. And this is going to fail. Panda series can hold a lot of types and the floor makes sense only for time data. So how can you access the timestamp object inside this specific series? There is a dot DT attribute that you can use. So we are going to do the same. Pick the column, then go ahead and then dot D and only then floor of D. Let's run this one. And this one is working for us. Now that you can generate the keys, let's group. When you group, you can either specify a column to group by or group by values, which would have the same length as the data frame. Since you're interested in the hour we can use the latter form. So we define the keys and then we group by the keys. You get a group by object. This object waits for you to tell it what to do with every group. Extract some columns, maybe calculate some [Statistics](../../Skills/Data%20Science/Statistics.md). In this case, we'd like to count how many rides are per hour. So we're going to use count. So data frame, group by keys, and then count. We are going to get the counts for every day. So we see first of the month, second of the month, third of the month, and then the same number for every column because we are just counting. So what we're going to do is we're going to extract just one column. We're going to use the vendor ID. So we go by the keys, do the count, take only the vendor ID column and do a bar plot.
>
> **[3:07](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-hourly-trip-rides?u=76281980&t=187)** And now we have a bar plot for every day in the month, how many rides we have. Let's tidy it up. So we are going to run the same group by operation but we're going to change the index name for the day and we're going to call it, the date. And then we're going to create a bar plot with a title of daily access in January 2016. And we tell it we don't want to rotate the axis. We get this a x object, which is the monthly access and we do set y label for the number of rides. Once we run this cell, we have a much nicer task. We have a title, we have labels for the X axis and the Y axis. And we can see the day of the month just below.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [SQL](../../Skills/Data%20Science/SQL.md) (4), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (2), [Statistics](../../Skills/Data%20Science/Statistics.md) (1)
> **Env Vars:** sql (4)
> **Definitions:** stands for (1), is a  (1)
> **Documentation:** the documentation (1)
> **Best Practices:** remember to (1)
> **Speakers:** - [instructor (1)

#### [Work with data: Rides per hour](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-rides-per-hour?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-rides-per-hour?u=76281980&t=0)** - Let's calculate the median hourly rides over all days. First, we'll group the rides by day and hour, and then we'll calculate the median for each hour across all days. To make things more complicated, let's display the results by vendor. Let's take it one step at a time. First, you need to either download the data or copy it over. So you'll have taxi dot csv. And now we're going to load our data frame. I'm going to create two auxiliary columns. First, is the hour column, which is taken from the hour attributes of the datetime. And the second one is the day, again taken from the date attribute. So if you look at them right now we have the day, and then we have the hour in the day. And now we can do a groupby by the vendor, the day, and the hour. And I would like to do a count and I want to extract only a single column, which is the vendor. Oops. The vendor column is not there anymore. What happened is that [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) uses the groupby keys as indices, to the new generated data frame. Let's have a look. We see now that we have what is known a multi-index or hierarchical index. We won't go into these practically because we have a simpler method, and partially because they make my head explodes every time I work with them. What we're going to do is tell pandas not to place the columns from the keys as an index by using as index equal false. So if I'm going to do df groupby and we group by vendor, day, and hour,
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-rides-per-hour?u=76281980&t=94)** and as index equal false. And now we get the count and I'm going to have a look at the columns that were generated. I'm going to run this one we are going to see now that I have the vendor, the day, and the hour as columns in the new data frame. So now I'm going to create a daily data frame from the data frame grouping by with as index equal false and getting the count. And then do another groupby, this time by the hour, to get the median hourly. So we're doing the daily df, groupby, this time only with the vendor and an hour. Again, as index equal false. We are going to get the vendor ID, and get the median. And we can look at the hourly df. Right? So we get the data frame, where the columns are the vendor, the hour, and the vendor id, which is actually the amount of rights. And the index itself is the day. What you'd like to have is the hours in the index, the vendor as a column, and the median value from any column as the values. To reshape the data frame like this you're going to use pivot. If you work with [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) or [SQL](../../Skills/Data%20Science/SQL.md) you probably know or heard about pivot. Let's see, the fancier transpose, where role becomes columns, and the other way around. The documentation on pivoting and reshaping in pandas is excellent with a lot of visual examples. I highly recommend you read it. In our case, I'm going to pivot telling that the columns are the vendor the index in the hour and the values are the vendor id, which holds the count. Then I'm going to do a plot with the bar plot
>
> **[3:10](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-rides-per-hour?u=76281980&t=190)** rotation equals zero. And once we have that, we have our chart. I hope by now we can appreciate the power of pandas. With the very few lines you can slice, dice, and reshape the data, making exploring data super easy.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (4), [Excel](../../Skills/Data%20Science/Microsoft%20Excel.md) (1), [SQL](../../Skills/Data%20Science/SQL.md) (1)
> **CLI Commands:** make (2)
> **Documentation:** the documentation (1)
> **Exercise Files:** download the (1)
> **Speakers:** - let (1)

#### [Work with data: Weather data](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-weather-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-weather-data?u=76281980&t=0)** - [Instructor] Sometimes your data will be in more than one place. Let's add some weather data to our taxi rides and see if temperatures have a correlation with the number of rides. First, we're going to load the weather data, reminding you either to run down data script or copy over the taxi dot CSV file. So input [pandas](../../Skills/Software%20Development/Pandas%20(Software).md), define the time columns with the data, and then add the vendor column. And let's run this out. And now for the weather. You will need to run the download weather script in order to get weather.csv. Let's have a look at the weather CSV file. Right, so we have the station, the date, latitude, longitude, elevation, and much more. We see the date starts around 1948 and we would like the data column to be at timestamp. So, we are going to read the CSV of the weather. Parse dates is the date and we want to set the date as the index column. And we're going to call this WDF for weather data frame. So we see the date as the index, we see the station, the latitude, the longitude, and several other attributes. Let's run describe to have a quick look at the data. Remember, describe only shows numerical values, right? So we have latitude, longitude, snow, and then TMAX, and TMIN. The values of the temperature looks interesting. They don't match either cell use or fahrenheit . To understand what they are, you will need to go to the schema definition. If you read this PDF file, you're going to see that the temperature are 10% in celsius.
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-weather-data?u=76281980&t=94)** Let's convert this to fahrenheit using the convert temperature function from SciPy. So from SciPy concepts, we import convert temperature, and we're going to create a column called temp F for fahrenheit. And we are going to take the maximal, divide it by 10, and then from celsius to fahrenheit, and we are going to look at 10 random roles from the generated column. And now it looks better. Now that you have the data, we can join the two data frames. Pandas has various ways of joining data. Like an [SQL](../../Skills/Data%20Science/SQL.md), there are many kinds of joints. Inner, outer, left. Let's read the commentation to learn more. By default, pandas is going to merge on the index. However, you can specify column or other values to join by. The weather data frame is already indexed by date. So we're going to group by the drives index, the taxi.csv. By the date as well. Ddf for the daily data frame, we're going to group by and use the pickup_daytime.dt.date and get the count. And if you're going to run ahead on this one, we are going to see that we have a date here on the index. And then every column has the same value which is the count. And now we are going to run our merge. So we are going to merge the daily data frame and the weather data frame. And we say on the left index and on the right index. And we are going to do ahead. So now we get the daily data frame with all the rows
>
> **[3:09](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-data-weather-data?u=76281980&t=189)** that has the numbers of the rides. And then on the days that we don't have anything matching we'll get NaN's. And we have the temperature which does match. All that is left is to plot. And we are going to use a scatter plot this time. So JDF plot scatter, the X is the temperature in fahrenheit, and Y is the vendor. And we see that there's not a lot of correlation between the weather and number of rides. No correlation is also a good finding. Note that the data we have here is only for one month. Maybe if you look on a yearly data, you will see more correlation. This might be an interesting exercise for you. Go to the taxi data website, download the files for a year, and then try to see if you find the correlation between the weather and the number of rides.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (3), [SQL](../../Skills/Data%20Science/SQL.md) (1)
> **Env Vars:** csv (3), wdf (1), tmax (1), tmin (1), pdf (1)
> **File Paths:** weather.csv (1), taxi.csv (1)
> **UI Navigation:** go to (2)
> **CLI Commands:** find (1)
> **Code Identifiers:** pickup_daytime (1)
> **Exercise Files:** download the (1)
> **Warnings:** note that (1)

#### [Challenge: Graphing taxi data](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-graphing-taxi-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-graphing-taxi-data?u=76281980&t=5)** - [Narrator] In this challenge, you should load the taxi data, then remove all the rows with either total amount that is less than zero or the passenger count is zero. And then I would like you to create two charts. One is a bar chart of the average tip per passenger count and the second one is a bar chart of the average tip per day. I've included two images which show roughly how the charts should look like.

> [!info]- Semantic Content
>
> **Definitions:** is a  (2)
> **Speakers:** - [narrator] (1)

#### [Solution: Graphing taxi data](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-graphing-taxi-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-graphing-taxi-data?u=76281980&t=5)** - [Teacher] Let's have a look at my solution. First, we're going to load the data frame. So defining time columns, reading the CSV, and telling [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) to pause the time columns. And finally creating a vendor name from the vendor ID. And I'm going to print out how many rows are in the data frame. So we have a million something rows. Now we're going to drop the bad rows. So we define the bad rows as the data frame where the total amount is less or equal to zero, or and this is the vertical sign for or, the passenger count is zero. And then we are going to use the data frame drop method with the index of the bad row and telling it in place equal true meaning change the current data frame. And finally, we're going to print out again the number of rows. So let's run this out. And we see that we have a bit less row, about 500 less of them. Next, for the charts. I'm going to create an axially column, which is the tip percent. And this is the tip amount divided by the total amount times a hundred. And now for the chart of the tip per passenger count. So we're going to go by the passenger count. Get the tip percent and get the mean. And then I'm going to do a bar plot saying the title is tip percent by the passenger count. And finally, I'm going to set the Y label to the tip percent So let's run this one. And for some reason, looks like groups with eight people are doing the biggest tips.
>
> **[1:40](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-graphing-taxi-data?u=76281980&t=100)** Next is the weekday. So I'm going to get the day abbreviation function from the calendar model. And then the day of the week is coming from the pickup daytime column, .dt.weekday. And then by day is we are grouping by the day of the week and then taking the tip percent and mean, and now I can plot. So first I'm going to set the index of by day to day of the week, so it'll be displayed in the resulting chart. And now I'm going to plot, saying, the title is tip percent by day and I'd like a 45 degree rotation. Next I'm going to change generate chart to add the Y label as tip percent and then finally set the X ticks that will show under the X-axis as a day abbreviation instead of the day number. Let's run it. And we see that there's no much different in the percent of tipping throughout the week, maybe Thursdays.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **Env Vars:** csv (1)
> **Speakers:** - [teacher] (1)


### 6. scikit-learn

[↑ Back to Table of Contents](#table-of-contents)

#### [scikit-learn introduction](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/scikit-learn-introduction?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/scikit-learn-introduction?u=76281980&t=0)** - Scikit-learn is a package for machine learning. It implements a lot of [Algorithms](../../Skills/Software%20Development/Algorithms.md) and tasks such as classification, regression, clustering, and more. Apart from providing algorithms Scikit-learn also provides a lot of utilities that help us with the day to day machine learning tasks, such as pre-processing parameter selection, model evaluation, and more. Scikit-learn documentation is one of the best there is in the open source world. There are tutorials, examples reference to articles, and much, much more over there. For example, for people who are new at machine learning there's a choosing an estimator section. You can follow the flow chart until you find the right algorithm for your problem. Scikit-learn is huge and has many parts. The developers of Scikit-learn made the API consistent so you can learn one algorithm and it's pretty easy to use the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of them. We'll cover the interesting parts and learn some guidelines on how you can use and understand Scikit-learn better. As a bonus, it's very easy to get an algorithm created by the [Data Science](../../Topics/Data%20Science.md) team and put it in production as is.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Algorithms](../../Skills/Software%20Development/Algorithms.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Data Science](../../Topics/Data%20Science.md) (1)
> **Analogies:** such as (1), for example (1)
> **CLI Commands:** find (1)
> **Env Vars:** api (1)
> **Definitions:** is a  (1)
> **Speakers:** - scikit (1)

#### [Linear regression](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/linear-regression?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/linear-regression?u=76281980&t=0)** - [Instructor] In regression, we train a model to predict a continuous target such as a price. Scikit-learn comes with several datasets. You can learn on these datasets. Makes it easy to start learning. But you need to remember that they are cleaned up for you. Real world data will be much, much messier. We are going to predict the prices of houses using the California housing dataset. So let's start. So from sklearn.datasets import fetch_California_housing, and run it. Cal_housing equal fetch_California_housing. And let's run the cell. Let's have a look at what we got back. So we'll use the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md)-type function to see what it is. This is called a bunch. All through datasets, you're going to load from sklearn, comes as bunches, and have some common attributes. Bunches behave like Python dictionaries, and this is how we're going to treat them. So we're going to have a look at the keys. And we see we have the data, the target, the frame, target names, feature names, and the description. If you look at the type of the data, this is a NumPy array. And looking at the feature names, we see that every column in this NumPy array is corresponding to one of these names. So the first column is the median income. The second column is the house age. The third one is the average rooms, et cetera, et cetera. If you want to see the whole description, you can print the description. We're going to print it out, and then we see that we get some long description.
>
> **[1:35](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/linear-regression?u=76281980&t=95)** It says the output exceeds the size limit. And you can click on here to see the full thing in a text editors. And now we see some description about California housing dataset, explanation about what is every attribute or feature, and some more references to have a look at. Let's close this one. Let's look at the target. These target are numbers. And these are the prices of the houses in hundreds of thousands of dollars. And this is what we'd like to predict. There are several regression [Algorithms](../../Skills/Software%20Development/Algorithms.md) in Scikit-learn. Let's go with a random forest regressor. So from scikit-learn.ensemble, we import the random forest regressor. We're going to extract the data and the target. Traditionally, these are called X, with a capital X, and Y. And then we are going to create a random forest regressor, and then train it. The training function is called fit. And we're going to get back a regressor. And this is the way that the notebook is rendering it, like some kind of an image. How well did our model do? You can use the score method. So we're going to call the CLF. CLF stands for classifier. Score with Xs and Ys. And we're going to get 0.97 and several digits others. What does this number mean? Is it good or bad? It depends on the algorithm. Each algorithm has a default scoring function that makes sense for it. In our case, we can have a look. So we're going to use the extension that the scientific computation does.
>
> **[3:08](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/linear-regression?u=76281980&t=188)** This is not a valid Python, but it's going to run here. And the question mark at the end is going to show the documentation for the score function. And this says this is the coefficient of determination for the prediction, or sometime known as R-squared. You can go to the Scikit documentation, and see the documentation of R-squared. And in R-squared, one is a perfect score. We are very close to it. Scikit-learn has many other estimators, and you should know the problem and the domain to pick the right estimator for your problem. So what did our model learn? Let's run the built-in DIR command on the classifier. And again, this is a bit too long, so we're going to open it in a text editor. And we see a lot of attribute. The convention in Scikit-learn is that everything that ends with an underscore. A single underscore is something that the estimator learned. For example, if you're going to look at n_features_in, we're going to see that it says eight features. And if you're going to look at the shape of X, which is the data, we see that it is 20,000 rows, but we have eight columns, which are the features. The nice thing about [Decision Trees](../../Skills/Artificial%20Intelligence%20(AI)/Decision%20Trees.md), it's also easy to see the reasoning behind them. Scikit-learn has an export_graphviz, which is exporting the graph into a .file. This .file is the .language from the Graphviz toolkit, which is a generic toolkit for generating graphs.
>
> **[4:43](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/linear-regression?u=76281980&t=283)** It is pretty awesome. I recommend you try it out. This means that you will need .tool or Graphviz installed on your machine. Of course, in code spaces, this is already done for you. So let's do it. First, we're going to call export_graphviz. We're going to take the first estimator output two, tree.dot. We're going to say where it can get the feature_names. Won't show us, just the values. And then I want the tree of up to five. And let's run this one. Going back to our explorer, now we see that we have a tree.dot here. This is the .file for Graphviz. And now we can use the Graphviz utility, dot. I'm saying I want the output to be SVG. The output file name is tree.svg. And the input is tree.dot. So you're going to run this one. Now you see, we have also tree.svg. What I'm going to use is IPython display, and basically display the SVG in the interactive prompt. So let's run the cell. And now we get the picture of how the algorithm is deciding where to go, right or left, and what are the thresholds. Once you train your model, you'd like to use it to make predictions. Let's pick a row, and see what our model predicts. So I'm taking row 17, and then I'm calling predict on this row. And this is going to fail, since the predict method worked on several rows at once. If you look at the shape of our rows, this is just eight or nothing.
>
> **[6:15](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/linear-regression?u=76281980&t=375)** We would like one row of eight features. So we are going to use slicing to get a single row. But this time, the row is going to have a different shape, one over eight. And now we can call predict on this row. And you can see the predicted value is 1.59. Let's look at the target, which is the actual value, and see what it is. So 1.55. Not so bad. This covers most of the basic functionality of Scikit-learn. You call fit to train your model. You call score to validate the model. And you call predict to, well, predict values.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1), [Decision Trees](../../Skills/Artificial%20Intelligence%20(AI)/Decision%20Trees.md) (1)
> **Definitions:** is a  (4), is called (2), stands for (1), known as (1), means that (1)
> **Env Vars:** clf (2), svg (2), dir (1)
> **CLI Commands:** python (3), make (1)
> **Code Identifiers:** export_graphviz (2), n_features_in (1), feature_names (1)
> **Versions:** 0.97 (1), 1.59 (1), 1.55 (1)
> **Analogies:** such as (1), for example (1), picture (1)
> **Documentation:** the documentation (2)

#### [Understand train/test split](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-train-test-split?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-train-test-split?u=76281980&t=0)** - [Instructor] In real life, your model will need to predict values from data it hasn't seen yet. It is common practice to split the data to training and testing parts. This reduces the chances of overfitting which means your model is accurate for data itself but will behave poorly on new data. SecondLearn makes it easy. First, let's slow the data. So we're going to [Fetch](../../Skills/Web%20Development/Fetch.md) the California housing, and then we're going to take the X and the Y from the California housing. And let's look at the shape of the X. And you see we have 20,000 rows and eight features. SecondLearn provides a trained test split function that splits the data into a training and testing. So from model selection, we import the train test split, and then we are going to call the train test split with X and Y. And say, the test size is 0.3, which means 30% and this is going to return four values: the X for the training, the X for the testing, Y for the training, and Y for the testing. And let's look at the X train shape again. And we see it's about 70% of the data. And now we can train our model. So, we take the random forest regressor, fit it only on the X train and the Y train, meaning train it on there. And then we are going to calculate the score on the test data that it hasn't seen yet. And we get the score of 0.8.
>
> **[1:33](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-train-test-split?u=76281980&t=93)** There are several other ways to split data, and you can look at the site documentation for splitter classes and splitter functions, things like K folding with splitter data to K parts, then take one out, and others. Note that sometimes splitting data to test and train randomly might not be a good strategy. For example, in fraud, most of your data will contain valid transactions. There's a good chance that randomly you'll pick only valid samples, and then your model will learn only to say no fraud. Learn about your data, and figure out the best way to split it to a good model.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Versions:** 0.3 (1), 0.8 (1)
> **Analogies:** for example (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Preprocess data](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/preprocess-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/preprocess-data?u=76281980&t=0)** - [Instructor] In most cases, you will need to pre-process the data before you feed it through your model. Some [Algorithms](../../Skills/Software%20Development/Algorithms.md) are sensitives to the range of feature values. Let's look at our data. So, from sklearn.datasets we import the fetch_california_housing, and we're going to load the data. And this time saying as_frame=True, meaning we're going to get the data as a data frame instead of numpy arrays. And then we're going to get the housing data, and call describe on the dataframe. You can see for example that the medium income maximal value is 15. While if you go to the population, the maximal value is more than 35,000. It is a huge difference, and will cause problems for some algorithms. Let's have a look. So I'm going to get a train test split, and split the data to training data and testing data, 30% saved for testing. Let's run this. And now I'm going to import the SVR support vector regressor from the SVM model. Create one, fit it on the training data, and score it on the testing data. Ouch. This is a really bad score. What we're going to use is the scaler from the pre-processing model. This is going to scale all the values. So from sklearn, we import pre-processing, and then X scaled is pre-processing scale of X. Let's see what the scaler did. So we're going to use [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) for that. So we import pandas as pd, and we create a dataframe from the scale data,
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/preprocess-data?u=76281980&t=94)** telling it the column names are the feature names. And then call describe. And we see now, that all the values are between minus one and one. So now we're going to split the data again, calling train test split, but instead of X, we're going to do it on X scale. And Y is not changed with a test size, create a regressor, train it, and then score it. And this is a much, much better score. We have eight features in our original data. We can try and reduce them to four. This is known as dimensionally reduction. One of the most common algorithms is called PCA, which stands for Principle Component Analysis. So importing PCA from sklearn.decomposition, creating a PCA with four components, and then using the fit transform method to transform the X to a different shape. And when we look at the shape of the resulting data, it has the same number of rows, but right now only four columns. There are many other facilities for pre-processing data in SciKit-Learn. It all depends on the algorithm you use, and what's the best data format for it. Sometimes you will do the initial pre-processing in pandas. So you're going to load the data to a dataframe, maybe join it with data from other sources, filling missing values, et cetera, et cetera. And finally, before you move the data into SciKit-Learn, you are going to use the two numpy method to convert the data into a numpy array.
>
> **[3:09](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/preprocess-data?u=76281980&t=189)** And then pass this numpy array into SciKit-Learn, and continue as usual.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Algorithms](../../Skills/Software%20Development/Algorithms.md) (3), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (3)
> **Definitions:** is a  (3), known as (1), is called (1), stands for (1)
> **Env Vars:** pca (3), svr (1), svm (1)
> **Code Identifiers:** fetch_california_housing (1), as_frame (1)
> **UI Navigation:** go to (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Compose pipelines](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/compose-pipelines?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/compose-pipelines?u=76281980&t=0)** - There might be several steps you need to perform before you finally feed your data to your [Algorithms](../../Skills/Software%20Development/Algorithms.md). (indistinct) provides us pipelines which are a way to group together several steps. Let's create one, we'll first scale data, then reduce the number of dimensions, and then use an SVR regressor. So first, let's load the data. So we import California housing and we import the train test split. We're loading the data, getting X and Y, and then splitting it to train and test. And now we're getting PCA from the composition, pipeline for the pipeline, standard scaler from the pre-processing, and SVR from the SVM model. And we can create a pipeline. So pipe is now a pipeline, which have three steps. First step is called scale and it uses a standard scaler. The second one is doing PCA, and it's using four components for the PCA. And the final step is the SVR, which uses an SVR. Once we have this pipe it behaves like every other classifier in cyclic learn. We can call fit to train the data and score to get the data. Pipes remove air prone manual steps from your process. You can combine as many steps as you want. Let's look at the steps we currently have. We can call pipe dot steps. And you see we have three steps. You can change the parameter of one of the steps. We can iterate over the list
>
> **[1:32](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/compose-pipelines?u=76281980&t=92)** of steps until we find the object we want to change the parameter. However, there is an easy way. We can use the get params method. And if you see that we are going to get all the parameters for every step. Each parameter is prefaced by the step name. So PCA double underscore or scale double underscore or SVR double underscore. And we can set a parameter. So for example, if you want to set the SVRC to 0.9, we do set params and then we say SVR underscore underscore C equals 0.9. And we can do that. And set params returns the pipeline. And this is how a pipeline looks inside the interactive prompt. You can also write the pipeline parameters the configuration file and load them, and then run the pipeline.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1)
> **Env Vars:** svr (6), pca (4), svm (1), svrc (1)
> **Versions:** 0.9 (2)
> **Definitions:** is called (1), is an  (1)
> **CLI Commands:** find (1)
> **Analogies:** for example (1)
> **Speakers:** - there (1)

#### [Save and load models](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/save-and-load-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/save-and-load-models?u=76281980&t=0)** - [Narrator] At the end, you'd like your model to run in production. How can you pass a model you've trained and experimented with to production? Scikit-learn makes it easy. [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) has a built in model called Pickle which can store and retrieve almost any Python object. The Pickle Model is Python specific, so you can't save Scikit-learn Pickle Model and have [Java](../../Skills/Software%20Development/Java.md) loaded. But if your production is using Python, let's say in the microservice architecture, this becomes a non issue. Let's have a look. So we have a pipeline. So we import the California housing, the PCA, the train test split, the pipeline, the standard scaler, and the SVR. We load the data, split it to test and train, create a pipeline, train it with the fit method, and then finally look at the score. And let's run this cell. Now we have our model, and we'd like to save it. So I'm going to import Pickle, and then name the output file model.pkl. You can pick any name you want. And then use with open of the output file in WB Mode, which means write and binary data. And this is important as output. We do pickle.dump the pipe to the output. And let's run this cell. If you look at the files, now you see that we have also model.pkl. You can give this file to operations, and now they can use it. Inside the code, they just need to open the file
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/save-and-load-models?u=76281980&t=94)** in read and binary mode, and do pickle.load to get it. And to make sure we got the same thing, I'm going to call the score function, and we see that we got exactly the same score. The same way you save the model to a file, you can send it over the network, store it in a database, or do whatever you like with it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (4), [Java](../../Skills/Software%20Development/Java.md) (1)
> **CLI Commands:** python (4), make (1)
> **Env Vars:** pca (1), svr (1)
> **UI Navigation:** open the (1)
> **Speakers:** - [narrator] (1)

#### [Challenge: Handwritten digits](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-handwritten-digits?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-handwritten-digits?u=76281980&t=5)** - [Computer User] One of the data sets in scikit-learn is handwritten digits. Let's load it. So we import the load digits, and call the load digits extract X and Y. One of the things inside digits is called images. This images is the actual image. So let's take a random image. Let's say image 353. We're going to print it out, and we're going to also use matplotlib to show it. Let's take a digit. So we're going to pick 353. We're going to print Y, which is the label of this digit, and then take the images and print out the current image. Once we run it, we see that the digit is 4, and this is roughly how the image looks like. Kind of look like a 4. If you look at the image shape, it is eight by eight pixels. But if you look at the X shape, you see that the data is flattened. So we have 1700 images, and they are flat vectors of 64, 8 and 8, flatten out. What I'd like you to do, and I'm going to close the prompt. Write a pipeline that will learn to predict digits. Should reduce the number of features to 10 and use K neighbors classifier. Split the data to train and test, and answer what is the score of the pipeline on the test data, and what is the size in kilobyte of the serialized pipeline?

> [!info]- Semantic Content
>
> **Definitions:** is called (1)
> **Speakers:** - [computer (1)

#### [Solution: Handwritten digits](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-handwritten-digits?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-handwritten-digits?u=76281980&t=5)** - [Instructor] Let's look at my solution. So load the data, take a look at an image, look at the shape, and look at the shape of the data. And now, I'm going to create the pipeline. So I'm going to use PCA to reduce the number of features. Train test split the data, the pipeline to create a pipeline, and KNeighborsClassifier. Now I'm going to create a pipeline with PCA and KNeighborsClassifier. And then, I'm going to split the data to testing and training, call fit to train the pipeline on the training data, and print out the score for the test data. The score is 096. To get the size of the data, I'm going to use pickle, define what is a kilobyte, and then I'm using the pickle dumps, which is going to return bytes instead of saving to a file. And I'm going to print out the length of the data, which is in bytes, divided by kilobytes, and we see it's about 154 kilobytes.

> [!info]- Semantic Content
>
> **Env Vars:** pca (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 7. Plotting

[↑ Back to Table of Contents](#table-of-contents)

#### [Overview of matplotlib](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/overview-of-matplotlib?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/overview-of-matplotlib?u=76281980&t=0)** - [Instructor] Displaying charts is an important part of the [Data Science](../../Topics/Data%20Science.md) process and [Storytelling](../../Skills/Web%20Development/Storytelling.md) both in the exploratory state and when presenting reports. So far, we used [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md)' plotting capabilities to display charts. Under the hood, Pandas uses a library called Matplotlib. There are several other plotting libraries in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), but Matplotlib is by far the most used. Matplotlib is very powerful and has many features. However, it can be a bit low level at times. I prefer to use Pandas whenever I can and drop to Matplotlib only when I need to. Pandas abstracts a lot of the low level details, but at times, you will need to work directly with Matplotlib to get optimal results. The way I usually work is start at the gallery and then find a chart that looks like what I want to do. Click on it, and start with the code below tweaking it to my needs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (4), [Data Science](../../Topics/Data%20Science.md) (1), [Storytelling](../../Skills/Web%20Development/Storytelling.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **CLI Commands:** python (1), find (1)
> **UI Navigation:** click on (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Use styles](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/use-styles?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/use-styles?u=76281980&t=0)** - [Instructor] Beauty is in the eyes of the beholder. Let's start with some basic examples and see how you can customize Matplotlib and make it look better. I usually get someone with a good sense of aesthetics to give me an opinion before I publish a report. So, here's our code. We import Matplotlib as plt and numpy. We create linear space between -6 and 6 with a hundred points. The Ys are the sync, which is sign X divided by X, and then we plot the Xs and the Ys. Let's run the cell and this is going to generate a line plot. It looks good, but sometimes we'd like to get a different look. There are several styles available for you to use in Matplotlib. We're going to run plt.style.available and we're going to get a list of several styles that are available. Personally, I like to use the seaborn whitegrid style. So, we're going to do plt.style.use seaborn-whitegrid and then do the same plot again. And now we see a slightly different plot, with grid lines a bit more spaced. Note that once you change the style, it's going to change the styles of all the charts from that point onward. Let's try another style. This is the fivethirtyeight style, and again, we use the style and then plot the same data. Now we see that the line is wider and we still see the grid lines. I encourage you to try out a few styles and see which one you like. You can head over to the gallery and check them out. If none of the styles fits your needs,
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/use-styles?u=76281980&t=94)** you can create your own. This is a bit more involved, but is well documented.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Customize pandas output](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/customize-pandas-output?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/customize-pandas-output?u=76281980&t=0)** - [Instructor] Visual Studio Code can render [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) DataFrame nicely. Let's have a look. So we're going to load the iris dataset from scikit-learn doing load_iris with as frame equal true to get a data frame. And we're going to get the first five rows from the data and look at this data frame. Let's run the cell and you see it rendered nicely. We have headers, we have different colors for even and odd rows, et cetera, et cetera. Let's highlight the maximum value in each column. This can be done with data frame style highlight max. And we run this one. We see that we get the value highlighted. On the last column of the petal width every value is the maximal value, so all of them are highlighted. There are several other built-in styles. For example, instead of highlighting the maximal value you can use a gradient. The higher the value the darker blue the cell will be painted. So let's run this one. And now we see that we get values. Another nice style is displaying a bar on every cell. The bar width is proportional to the value of the cell. So df.style.bar. And now we see the bars. There are several other nice default styles. It is also easy to create your own. Let's say you'd like to color all the cells with a round number in blue. So, I'm doing color blue of value. If rounding the number equal to the number we return color column blue. Otherwise we return none. And we do apply map on our function.
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/customize-pandas-output?u=76281980&t=94)** When we run the code we're going to see only two values colored in blue. You can combine styles by chaining them with the dot operator. Here's another function. This time we're coloring odd numbers. So if integer of the value model two equal one we change the background color to orange and now we do apply map of color the round dot, apply map and then color the odds. And if you run this one, we are going to see some cells have orange background and some cells have both orange background and blue color. Styles can also be applied only to a subset of the columns. So we can do apply map with coloring the odd ones and say subset is the first two columns. If you're going to run this one, you see it affects only the first two columns and the third one is not affected. Apply map works on every element. You can also apply style to a whole column. Let's make the numbers in the top 50% bold. So top 50% of the column. We say is top 50 equal to column bigger or equal to the median of the columns. And then you use the list comprehension to create values per cell. We say the font weight is going to be bold if V, otherwise the empty string meaning no change in style for every value in the top 50. And then we apply this function. Now let's run it. And you see that the top 50 values are now bold. I think you get the general idea. When you generate report and would like to make it easier
>
> **[3:06](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/customize-pandas-output?u=76281980&t=186)** for people to find interesting point in your data frame, use styling. The documentation is as always very good. And there are many more examples there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **CLI Commands:** make (2), find (1)
> **Code Identifiers:** load_iris (1)
> **Documentation:** the documentation (1)
> **Tools:** visual studio (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Plotting with pandas](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/plotting-with-pandas?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/plotting-with-pandas?u=76281980&t=0)** - [Instructor] [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) bases its plotting on matplotlib. It simplifies plotting, and also adds some advanced charts. In order to work, you'll need to run the download data script, which will generate stocks.csv. First, we're going to load this data. So, import pandas as pd, and then read csv of stocks.csv, parsing the date as timestamp, inserting it to the index of the data frame, and then we're going to show the first few lines. And we see that the index has times in it, and in the columns we have the symbol, opening, high, low, close, adjusted close, and volume. This is a very common trading data. Let's have a look at msft price over time. So first, we are going to get only the rows that shows [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md), and then we are going to plot the closing price using the .plot method, which is going to generate a line plot. You can notice that the X axis, which is time based, is rendered nicely by pandas. When you want to look at the distribution of a single feature, it is common to use a histogram. Pandas also offers a KDE, Kernel Density Plot. So you are going to look at the closing price plot, and now .kde. And this is going to generate a density plot. We see that most prices are around the 220 mark. Let's show the monthly volume by stock. So we get the data frame,
>
> **[1:33](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/plotting-with-pandas?u=76281980&t=93)** we do a pivot with the columns of the symbol and the values of the volume. We resample to monthly frequency, and we sum up the volume for month. And finally we do a plot. This time a bar plot. And we say the xticks is a range of 12, and the rotation is zero. When you have very long lines you can break them by putting parenthesis, and then putting one operation per line with the dot. And now we can see the volume per stock. Pandas offers much more advanced charting capabilities. For example, scatter matrix that are great to see the initial correlation between values. The documentation is excellent. Do read it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (5), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **File Paths:** stocks.csv (2)
> **Definitions:** is a  (2)
> **Env Vars:** kde (1)
> **Documentation:** the documentation (1)
> **Analogies:** for example (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Use Matplotlib with pandas](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/use-matplotlib-with-pandas?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/use-matplotlib-with-pandas?u=76281980&t=0)** - [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) does a lot of the heavy lifting when it comes to plotting. But sometimes you'd like to have more control over the generated output. To this end pandas returns the generated matplotlib access. Once you have the access object you can use any method that matplotlib provides. Let's see an example. To run the example, you first need to run the download data.py script which is going to generate "stocks.csv". So we're going to use read CSV to read the CSV pass the dates on the date column and set it to the index of the data frame. And we're going to look at the first few lines. Let's take only the [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) part so query symbol equal msft. And now we want to plot a median of the daily volume. So daily equals msft, we take only the volume column. We do group by, by the index weekday and take the median and finally do a bar plot. For the data exploration stage this chart might be good enough. When you present, you'd like to make it better. So we are going to run the same chart, daily plot bar. This time you're going to add a title "MSFT" and say we don't want rotation on the x-axis. These are the numbers here that are rotated 90 degree. And now we are going to set the tick labels to the days of the week. So Monday, Tuesday through Friday. We're going to set the X label to the weekday We're going to set the X label to the weekday and the Y label to the volume,
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/use-matplotlib-with-pandas?u=76281980&t=94)** and now we can run it. Now we have the same chart but now we have a title we have labeling on the axis and we have the name of the weekday below. matplotlib API is huge it has a lot of power but you'll need to invest some time learning it. Lucky for you that the computation is great. I usually start from the gallery, find something that looks like what I want to do Another way of working with matplotlib and pandas Another way of working with matplotlib and Pandas is to pass the a-x the axis of matplotlib And then pandas is going to render the chart on this provided access. on this provided access.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (5), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **Env Vars:** csv (2), msft (1), api (1)
> **File Paths:** data.py (1), stocks.csv (1)
> **CLI Commands:** make (1), find (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - pandas (1)

#### [Tips and tricks](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/tips-and-tricks?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/tips-and-tricks?u=76281980&t=0)** - [Man] Let's write the function to plot sin between a limit. So, plot sin gets a limit. We create Xs which is a linear space between minus limit to limit with a hundred points. And then we use plt plot with the Xs and NP sin of Xs and we give it the label and we call this function plot sin with six and we get the generated function, but this is a static function. Even if you click on the controls and do zooming and panning it is still going to show you only parts of this chart, and you can change the limit and see the results right away. Jupyter Widgets, or ipy widgets, is the library that does exactly that. It gives you widgets that you can display either in Jupyter notebook or visual to the code, interactive. So we're going to import the interact, and we are going to use it as a decorator to our function. And we're going to set the limit to six. Let's run the cell, and now we see that we have a small widget here. Once we move the widget, the chart will adjust accordingly. However, you see that the limit is not good. We can go below zero, which is not something we want. So, what we're going to use is an integer slider from the widget list. Can set the value, the maximum number, and the minimal number. Right, so we do the IntSlider. We do interact, and now we set limit is an integer slider between zero and twenty. If you're going to run this one, now the limits are going to be between zero and twenty. Right, it start with zero,
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/tips-and-tricks?u=76281980&t=94)** which is probably not what we want. So, let's do, value equal ten, and now when we are going to run the cell the initial value is going to be ten. Ipy widgets comes with a lot of widget you can play with. Go and experiment.

> [!info]- Semantic Content
>
> **Definitions:** is a  (2), is an  (2), we call this (1)
> **Tools:** jupyter (2)
> **UI Navigation:** click on (1)
> **Speakers:** - [man] (1)

#### [Other plotting packages](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/other-plotting-packages?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/other-plotting-packages?u=76281980&t=0)** - [Instructor] Matplotlib is the library for plotting in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), but it's not the only player in town. Matplotlib is old, which means it's working, but it generates static images. Most of the newer plotting libraries, use the browser to get more interactivity. I'm going to share an example with Plotly, and mention some other libraries you might want to have a look at. Plotly is a graphing library that supports several programming languages. Plotly generates interactive plots, for example, you can see here, I can see the tool tips. I can do zooming and panning all within the browser. Let's see an example. I'm going to load the track data, so input [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) as pd, and read csv. Parse the time column and set it to the index. And now I'm going to re-sample the data frame, with a three minute interval with mean and reset the index. And I'm going to show you the first few rows. Right, so we have time as a column again, due to the reset index, the latitude, longitude, and the height. And Plotly works with data frames. So we import plotly.express as PX, and we say we want a bar plot from the data frame. And we say where to get the X and the Y values. What are the column names? And finally show the figure. And now I have an interactive plot, with automated tool tips and I could do zooming and panning, and all the other things you're used to do with charts. You can even write this chart into an [HTML](../../Skills/Web%20Development/HTML.md) file, using fig.write_html.
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/other-plotting-packages?u=76281980&t=94)** And if you're going to look back at our code, now we see we have track.html, which can be opened in the browser, and we show exactly this chart interactively. For most sophisticated reports, look at Plotly's Dash. Let's take a look at some other plotting libraries. The list is long, and you should evaluate, and pick the ones that fit your needs. Seaborn adds a lot of chart types on top of Matplotlib. Bokeh generates interactive plots in the browser. Also supports custom visualization. For example, this is an interactive plot by Bokeh. We have movies, each movie we have the title of the year and the revenue. And we can filter out movies by number of reviews, genre and other properties. Altair uses a concise data language, to generate interactive plots. As rule of thumb, if you're just starting start with Pandas, it'll probably be the easiest path. If you need some interactive plotting, probably go with Plotly. You can also consult the PyViz website, which lists a lot of plotting libraries, and their capabilities.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (2), [HTML](../../Skills/Web%20Development/HTML.md) (2), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **Definitions:** is a  (1), is an  (1)
> **Analogies:** for example (2)
> **File Paths:** track.html (1)
> **CLI Commands:** python (1)
> **Code Identifiers:** write_html (1)
> **Env Vars:** html (1)
> **Best Practices:** rule of thumb (1)

#### [Challenge: Stock data bar charts](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-stock-data-bar-charts?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/challenge-stock-data-bar-charts?u=76281980&t=5)** - [Instructor] For this challenge, you'll need to run the 'download_data' script in order to get stocks.csv. Once you have them, you should load the data and then create a bar chart that has the X as the month, the Y is the median closing price, and each stock should have its own chart. The output should look like this.

> [!info]- Semantic Content
>
> **File Paths:** stocks.csv (1)
> **Code Identifiers:** download_data (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Stock data bar charts](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-stock-data-bar-charts?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/solution-stock-data-bar-charts?u=76281980&t=5)** - [Speaker] Here is my solution. We import [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) as pd and we read the CSV file passing the date column as dates and let's have a look at the first few rows. Now I'm going to create a month column by saying df month equals df date.dt.month and now I'm going to rotate the data with a pivot table. The columns are going to be the symbol. The index is going to be the month. The values are going to be the closing price and the aggregation function for all the values that follow within the same month is going to be the median. Let's have a look at the generated data frame. So we have the stocks, the month and then the values, which are the median and now we are going to generate the plot. So I'm going to take the month abbreviation from calendar and then I'm going to create a bar plot with a title which is median monthly close and no rotation on the X axis. Now I'm going to set the axset labels to the month abbreviation and set the Y label for the closing price. Once I run this, I have this chart with the title, the Y axis, the X axis, and the legend for every symbol.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **Env Vars:** csv (1)
> **Speakers:** - [speaker] (1)


### 8. Other Packages

[↑ Back to Table of Contents](#table-of-contents)

#### [Other packages overview](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/other-packages-overview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/other-packages-overview?u=76281980&t=0)** - [Instructor] People sometime find the world of open source intimidating. There are many packages to choose from, some of them with overlapping capabilities. Which one should you choose? Here are some guidelines I use when evaluating packages. Ask around. The community is helpful and most people are knowledgeable about which packages are good and which are not. Note that people tend to like the shiny new stuff, but if you ask enough people, you will get the right idea. Sites such as Stack Overflow are a great place for asking questions. Most packages are hosted on [GitHub](../../Skills/Software%20Development/GitHub.md). Go over and check the activity, the number of committers, the number of stars, et cetera. Don't be intimidated by the number of issues. Some of them are feature requests and there are many duplicates. In general, packages without issues are ones that nobody uses. After you found the package you like, go and evaluate it. Write some code using real data and see how it behaves.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **CLI Commands:** find (1)
> **Documentation:** stack overflow (1)
> **Tools:** github (1)
> **Analogies:** such as (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Go faster with Numba](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/go-faster-with-numba?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/go-faster-with-numba?u=76281980&t=0)** - [Instructor] NumPy will be fast enough for most of your needs, especially when you avoid for-loops and use vectorized code or ufuncs. However, there are cases when you can't vectorize your code or that you like to squeeze even more performance. The common approach in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) is to write soft, performance-sensitive code in Cython or C and use it from Python. However, before going that route let me show you another approach that is less painful. Numba is a JIT compiler for Python. What a JIT compiler does, it is going to create a specific machine code for a given function. So the first time you decorate a function with the number ngit or JIT decorator it is going to do nothing. The first time you call the function, Numba is going to check, is there a compile code for this function with a couple of integers. If no, it is going to generate a compiled machine code which is highly optimized for that specific type of numbers and then use it. Next time you call a function, it is going to see that the compiled version is already there and use this version. Under the hood, Numba uses the LLVM compiler infrastructure. It is a mature compiler with a lot of optimization built in. Let's have a look. So here's our code. This is polyn. It gets N as an argument, set a total to zero and then for something in range of N,
>
> **[1:33](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/go-faster-with-numba?u=76281980&t=93)** it is going to calculate again and again seven times N squared minus three times N plus 42, and return the total. And we're going to use the timeit module with 10,000 iteration to see how much time it takes. So let's run the cell. And we see that it takes about 155 microseconds. Now let's use Numba, so import Numba and then we decorate the function with numba.jit or numba.njit, and then we define the same function. I just called it with another name but the coding side is exactly the same. And again, we're going to run timeit on the function. So right now we are at 20 microseconds. So let's do the calculation. 155 divided by 20.3. And we see this is about 7.6 times faster just by adding the Numba.jit decorator. Numba shines when we run non vectorized code like a follow in Python. There are options to squeeze even more optimization from the JIT compiler. Do read the documentation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (4)
> **Env Vars:** jit (4), llvm (1)
> **CLI Commands:** python (4)
> **Versions:** 20.3 (1), 7.6 (1)
> **Definitions:** is a  (2)
> **Documentation:** the documentation (1)
> **Speakers:** - [instructor] (1)

#### [Understand deep learning](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-deep-learning?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-deep-learning?u=76281980&t=0)** - [Voice Over] [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) is a hot subject. In its base, Deep learning uses [Neural Networks](../../Skills/Artificial%20Intelligence%20(AI)/Neural%20Networks.md) which has been around for a while. However, it uses some new ways of utilizing [Hardware](../../Topics/Hardware.md) such as GPUs to gain some very impressive results. There are several deep learning libraries available. Lucky for you, most of them are available in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md). You can choose from [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md), [PyTorch](../../Skills/Artificial%20Intelligence%20(AI)/PyTorch.md), MXNet, and others. We're going to work with Keras, which is part of TensorFlow and it's easier to start with. We're going to identify handwritten digits. We use a data set that comes with Scikit-Learn. So from Scikit-Learn data sets we import "load-digits" and the digits is "load-digits" and we are going to run the cell. Let's have a look at the digit at location 37. And this is the image. If you look at the target at the same location we see that this is the number 9. The images are constructed of 8 on 8 images but the data is flattened out to vectors of 64. Each one containing a pixel. We are going to build a network. They're going to have input units, and for us the input units are going to be the 64 pixels. So this one on the left is going to be 64. Then hidden units. And finally the output units, with the [Probability](../../Skills/Data%20Science/Probability.md) for each of them. We are going to guess 1 of 10 digits. So we are going to have 10 output units. First, we're going to split and train the data
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-deep-learning?u=76281980&t=94)** but we'll need to do one extra step. The target is one dimensional. With values from 0 to 9. You will need to transform each value to an array with 10 elements. All of them 0 except the one with the digits. Since this is a common task Keras has the utility to do just that. Right, so if you take "to_categorical" from "tensorflow.keras.utils" and do "to_categorical" from 0, 1, 2, 0, 1 and we are going to run it. And do ignore the warnings on my machine, it is not set up to use GPU. We see now that we get the output as vectors of 3. Each row has 3 elements: either 0, 1, or 2 and 1 only in the place for the right value. So our first value is 0, we have 1 here and 0 is here. The second value is 1, So we have 0 on the first location, 1 in the second location, and 0 at the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md), et cetera, et cetera. So now X is Y of data and Y we're going to convert it to "to_categorical". So we'll get the right shape. And now from Scikit model selections, we're going to do the train test split and split the data to training and testing data. Now we're going to build our model. So from Keras we import sequential and from the layers we import the Dense layer and Activation layer. The input dimension is the shape of the X at location 1 and the output dimension is the shape of Y at location 1. So to create a sequential model, start with a Dense of 128 and the input shape is the input dimension
>
> **[3:09](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-deep-learning?u=76281980&t=189)** in our case 64. Then an Activation layer, then another Dense with an output dimension in our case 10 and then another Activation, this time with "sigmoid". And we're going to compile the model telling it what is the loss, what is the optimizer, and what are the metrics that we are interested in? Let's run this one. And now Keras generated a TensorFlow model. Now we can call fit on the model and we're going to say we want it for 10 epochs. Now that the model is trained, we can call evaluate. This is slightly different than the score method of Scikit-Learn, but roughly the same. So we're going to get the accuracy which is the second parameter. So let's run this cell and we can see that we are at around 0.95 accuracy, not bad. Now let's predict. So we'll get X test, the first three arguments and we see we get an array with probability for each number which is what the neural network clearing. But for us, this is not that helpful. What we're going to do is we're going to call argmax on this input, meaning I want the location of the biggest argument and axis=1 meaning I want to work it on every row. Once we run this one, we're going to see that it gets 5, 0, and 4. And if you're going to do the same thing for the Y test, we're going to see it is actually 5, 0, and 4. We can save our model to disk and load it later. So model.save('digits.h5') We're saving it to a format known as aGF5.
>
> **[4:45](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-deep-learning?u=76281980&t=285)** Later on we can load the model from the disk and run it again, predicting the test. And you see we get the same results.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (4), [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) (3), [Probability](../../Skills/Data%20Science/Probability.md) (2), [Neural Networks](../../Skills/Artificial%20Intelligence%20(AI)/Neural%20Networks.md) (1), [Hardware](../../Topics/Hardware.md) (1)
> **Code Identifiers:** to_categorical (3), agf5 (1)
> **Definitions:** is a  (2), known as (1)
> **CLI Commands:** python (1)
> **Env Vars:** gpu (1)
> **Versions:** 0.95 (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [voice (1)

#### [Work with image processing](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-image-processing?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-image-processing?u=76281980&t=0)** - [Instructor] When it comes to [Image Processing](../../Skills/Artificial%20Intelligence%20(AI)/Image%20Processing.md) you have many options in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md). Let's have a look at the few most common ones. Inside matplotlib you have some utilities to display images. There are some filters in the scipy.ndimage module. Second image has many [Algorithms](../../Skills/Software%20Development/Algorithms.md) for working with images. Pillow can be used to reshape images and paint on them but by far the biggest and most comprehensive library is OpenCV. Open CV is written in C++ and has great bindings to Python. It'll perform most of the tasks you'd like to do on images and then some. Let's have a look. Say we have this image of a sign. By the way, I highly recommend hiking this route. Let's start. So I'm going to import cv2 and then the image is cv2.imread for image read, the sign at CSV, and let's run the cell and we see that we have 1700 on 2300. This is the image size and three and this is for every pixel the colors in it. Let's have a look. So we're going to use matplotlib show the image. Matplotlib does show the image, but the colors seem off. The reason is that OpenCV uses BGR encoding and not RGB. So we can convert using the cv2.cvtColor the image and we tell it cv2.Color_BGR2RBG, and if you're going to show this one
>
> **[1:36](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/work-with-image-processing?u=76281980&t=96)** now it'll have the right color. A common task is to convert images to gray scale and this task can be done again with the cvtColor functions and this time we're going to use Color_BGR2GRAY and going to show the image. This time the image does not look like gray but the reason now is the color map that matplotlib is using. If you're going to tell matplotlib to show the image but use the gray color map, it is going to look just fine. Let's find the edges of the image. We're going to use the Canny algorithm on the gray image and we're going to give it two thresholds, 80 and 150, and again we're going to show the edges image with the gray column map, and now we see that we've detected most of the edges. OpenVC can do much more and there are many examples and tutorials on how to use it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2), [Image Processing](../../Skills/Artificial%20Intelligence%20(AI)/Image%20Processing.md) (1), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1)
> **CLI Commands:** python (2), find (1)
> **Env Vars:** csv (1), bgr (1), rgb (1)
> **Code Identifiers:** cvtcolor (2)
> **Speakers:** - [instructor] (1)

#### [Understand NLP: NLTK](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-nlp-nltk?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-nlp-nltk?u=76281980&t=0)** - NLP stands for [Natural Language Processing](../../Skills/Artificial%20Intelligence%20(AI)/Natural%20Language%20Processing%20(NLP).md). NLP deals both with understanding text, and to some extent generating text. Most of the tasks are in the understanding side but with the rise of personal assistance and the rise of chat bots, text and speech generation is on the rise as well. In [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), we have several libraries that work with text. Scikit-Learn has some text processing capabilities. [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) has been used successfully in NLP, so Keras, [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) and others are options as well. NLTK, which stands for Natural Language Toolkit was originally designed for teaching NLP, and during the time gained a lot of practical tools. And [spaCy](../../Skills/Software%20Development/spaCy.md), which is an industrial strength NLP package with many practical tools built over a nice API. And this is the one we're going to have a look at. Once you install Spacy, you will also need to download a pre-trained model. This is done with Python, -m spacy download, and then the model name. In our case, it's the English one, or the English core Web sm for Small. This is already done for you in the code space so you don't have to run this command. Let's have a look. So we are going to take from Scikit-Learn data sets the [Fetch](../../Skills/Web%20Development/Fetch.md) 20 news groups, and we're going to get just a space one and remove the headers and the footers. We'll take one single text and print it out. And here is a text that we have.
>
> **[1:33](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-nlp-nltk?u=76281980&t=93)** Now we're going to import Spacy and we're going to load the core web small that we downloaded. Then say the doc is NLP of the text and look at the doc. Spacy is going to complain about missing libraries for working with GPUs. You don't have to worry about that. It is still going to work. And if you look the output document looks very much like the original text. But if you look at the type of the document we are going to see that this is something else. This document contains a lot of information about the text. For example, we can list the document sentences and get the first sentence. Splitting text to sentences is surprisingly hard task. We can iterate over the sentences and look at the [Tokens](../../Skills/Web%20Development/Tokens.md). So for every sentence, for every token in the sentence we're going to print the token with a text and the tag of the token, right? And these tags, VB for verb, et cetera is something that you will need to learn about. The convention in Spacy is that attributes without underscore at the end are numeric and one that ends with an underscore like the tag here are textual. Document itself also has entities or ents which are the name entities it can find. So for every entity in the document you print the text and the label of it. And you see that "Army" is an organization "Intelligent" is an organization, and "luck" was a person.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [spaCy](../../Skills/Software%20Development/spaCy.md) (6), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2), [Natural language processing](../../Skills/Artificial%20Intelligence%20(AI)/Natural%20Language%20Processing%20(NLP).md) (1), [Deep Learning](../../Skills/Artificial%20Intelligence%20(AI)/Deep%20Learning.md) (1), [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (1)
> **Env Vars:** nlp (6), nltk (1), api (1)
> **Definitions:** is an  (3), stands for (2), is a  (1)
> **CLI Commands:** python (2), find (1)
> **Analogies:** for example (1)
> **Prerequisites:** install (1)
> **Speakers:** - nlp (1)

#### [Working with bigger data](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/working-with-bigger-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/working-with-bigger-data?u=76281980&t=0)** - [Instructor] [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md), NumPy, and most of the [Data Science](../../Topics/Data%20Science.md) library in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) works best when the data fits in memory. Moving outside a single machine is hard. Try as much as you can to stay inside the limit of one machine. If you lack memory, before you start with a [Big Data](../../Skills/Data%20Science/Big%20Data.md) solution, remember that you can rent a cloud machine with several terabytes of memory. But sometimes you will need to deal with data that is too big to fit in memory. Before you start the painful and expensive route to big data systems, there are some options you can try. Most of the data is stored in [Databases](../../Skills/Software%20Development/Databases.md), and Pandas can read and right to databases. Before we start, you need to run the download data script so you'll have the taxi.csv file in this directory. We're going to use SQLite. SQLite is an embedded database, and it's easy to work with. So what we're going to do is we're going to take the data from CSV, and insert it to a database. This process is known as ETL. ETL stands for extract, transform, and then load. So we're going to import SQLite, Pandas, and tqdm which is a nice library for showing progress bars. We're going to use SQLite to connect to the database. And this is going to create a database if it does not exist. Now we are going to read the CSV, but in chunks. So we're going to say "chunksize=!00_000". Pandas is going to load
>
> **[1:32](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/working-with-bigger-data?u=76281980&t=92)** up to 100,000 lines in memory at once. And this is chunks, which is an iterable. We can iterate over it, so we can do four chunk in tqdm of chunks, which is basically like four chunk in chunks, but it's going to show a progress bar. And then we're going to do to_sql. This is the data frame, to_sql method. The [SQL](../../Skills/Data%20Science/SQL.md) table is called "rides". Using the connection we tell it that we don't want to store data frame "index" in the database, and if the table exists, append the data into it. Let's run this one. And now we are done. If we're going to look at the files again, we are going to see now that we have the taxi.db on the disc. And now we can use SQL to query the data. So, from "SELECT", "passenger_count", and the "COUNT of vendorID as count" from the rides where the passenger_count is bigger than one, group by the passenger_count. And we are going to do with SQL, with this SQL statement and the connection to the database, and show the data frame. And we see the passenger count, and how many rides with these passengers we had. And we have very few rides with big parties. Most databases run on strong machines and have a lot of computation power and storage capacity. It's common practice to use the database to run some filtering and aggregation to make the data small enough to fit in memory
>
> **[3:06](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/working-with-bigger-data?u=76281980&t=186)** and then continue with Pandas from there. Which means you will need to learn SQL, which is another language. But it is well worth the effort. A lot of data out there is in [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md). Apart from SQL, you can also look at the HDF5 format which is designed specifically to store big matrices. If you do need to go outside of a boundary of a single machine, you can have a look at Dask first. Dask provides an API very similar to NumPy and Pandas. We have an array, data frame, a bag, delayed, and futures. But most of the time you're going to work with array or data frame. When you run a calculation, Dask will automatically distribute the work among workers. And these workers can be either threads on processors on the same machine, or by using Dask distributed on a pool of machines. Let's have a look. We import Dask data frame as dd, and now we do dd.read_csv, again, our taxi, and I'm going to help it and say that datatype for the vendorID is float64. And we see that this cell runs very fast comparing to Pandas. This is because Dask didn't actually load the whole data to memory. Now we can do, for example, vendorID value_counts. And we are going to run this one. Again, it is going to be very fast because there's no calculation happening. Dask is just registering, what are the calculations that need to be done?
>
> **[4:39](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/working-with-bigger-data?u=76281980&t=279)** But until we call compute, nothing actually happens. So now let's do value_counts with compute. And now you see it's going to take more time, but it's going to give us a result. The reason Dask works in this format is that we can chain computation, letting Dask decide how to make them parallel and only at the end run them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (7), [SQL](../../Skills/Data%20Science/SQL.md) (6), [Databases](../../Skills/Software%20Development/Databases.md) (3), [Big Data](../../Skills/Data%20Science/Big%20Data.md) (2), [Data Science](../../Topics/Data%20Science.md) (1)
> **Env Vars:** sql (6), csv (2), etl (2), select (1), count (1)
> **Code Identifiers:** passenger_count (3), vendorid (3), to_sql (2), value_counts (2), read_csv (1)
> **Definitions:** is an  (2), known as (1), stands for (1), is a  (1), is called (1)
> **CLI Commands:** make (2), python (1)
> **Analogies:** similar to (1), for example (1)
> **File Paths:** taxi.csv (1)
> **SQL:** select (1)


### 9. Development Process

[↑ Back to Table of Contents](#table-of-contents)

#### [Development process overview](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/development-process-overview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/development-process-overview?u=76281980&t=0)** - [Instructor] We work in teams. Teams allows us to do work that individuals can't do. And in good teams, the whole is much greater than the sum of its parts. However, working in teams means we need to do some adjustments. There are many variants of the development process, and almost every team is a bit unique in the way it works. I'll try to show you some of the things I think strike good balance between helping the team and letting do your thing. Process is sometimes referred to as accidental complexity. It's the things you need to do and not strictly [Data Science](../../Topics/Data%20Science.md), but are essential to get the product out of the door.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Data Science](../../Topics/Data%20Science.md) (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Understand source control](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-source-control?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/understand-source-control?u=76281980&t=0)** - There is a poem going around. "99 little bugs in my code. Take one down, patch it around, 117 bugs in my code." We do make mistakes, and sometimes we need to get back to an earlier version of the code we wrote. This is a job for source control. The other major job of source control is to allow several people to work on the same code. There are many systems for source control, but [Git](../../Skills/Software%20Development/Git.md) is by far the most widely used one. Most IDs have support for Git, and you can run Git command from inside your ID, such as visuals to the code. Learning Git is outside the scope of this course. Do check our offering on the subject. Sites such as [GitHub](../../Skills/Software%20Development/GitHub.md) are offering more than just [Version Control](../../Skills/Web%20Development/Version%20Control.md). They have support for the full software life cycle. One problem that is more specific to scientific application is data files, which tend to be large. It's not a good idea to commit large files into Git. Take a look at Git large file support, known as LFS, or even at DVC, which is built for Machine Learning Projects.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Git](../../Skills/Software%20Development/Git.md) (6), [GitHub](../../Skills/Software%20Development/GitHub.md) (1), [Version Control](../../Skills/Web%20Development/Version%20Control.md) (1)
> **CLI Commands:** git (6), make (1)
> **Definitions:** is a  (2), known as (1)
> **Env Vars:** lfs (1), dvc (1)
> **Analogies:** such as (2)
> **Tools:** github (1)
> **Speakers:** - there (1)

#### [Learn code review](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/learn-code-review?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/learn-code-review?u=76281980&t=0)** - [Instructor] Linus's law states that "given enough eyeballs, all bugs are shallow." It means that the more people look at your code, more bugs are likely to be found. I teach a lot of classes, and my students catch my mistakes every time before I have a chance to find them myself. The process of letting people look at your code and comment on it is called [Code Review](../../Skills/Software%20Development/Code%20Review.md). I highly recommend it will be a central part of your development process. You'd be amazed how many bugs are caught and how many improvements people come up with. There are many ways to do code reviews, from sitting together in a room and going over your code to online tools where people comment on the changes you make to the code. No matter the way you're doing it, it's important to remember that people are commenting on your code and not on you. Some people get very defensive when people comment on their code, and it makes the code review process very painful. Getting everyone to understand you're just trying to make the code better and fix issues and keeping everything technical and professional is very important step in making good code reviews. Some companies have checklists, such as: did you run the tests, is the code easy to understand, are there any new dependencies, and more. Try to think of such a list from your experience, which means from the mistakes you made. I think you'll find it useful. In the world of hosted [Git](../../Skills/Software%20Development/Git.md),
>
> **[1:32](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/learn-code-review?u=76281980&t=92)** it's very common to create a pull request. This means that you work on a feature branch, and once you're happy with your code, you create a pull request that people can comment on. Here's an example from the [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) project. We have a pull request that Jeff submitted, and then they started a discussion about the pull request with comments from various people. And every time you see that the response is a new change to the code, and people can comment that, etcetera, etcetera, until finally, when the code is deemed good enough, it can be merged back to the main branch.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Code Review](../../Skills/Software%20Development/Code%20Review.md) (2), [Git](../../Skills/Software%20Development/Git.md) (1), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **CLI Commands:** find (2), make (2), git (1)
> **Definitions:** means that (2), is called (1), is a  (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Testing overview](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/testing-overview?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/testing-overview?u=76281980&t=0)** - [Instructor] An essential part of your process should be testing. Testing ensures that code behaves the way it's supposed to, and that we didn't break one thing while fixing another or adding a new feature. There are many kinds of tests, unit tests, regression tests, acceptance tests, functional tests, stress tests, fuzzers, and many more. You don't have to be a testing expert. Pick the areas in your code you think are most important and test them. Tests also have a downside. It takes time to write them, and every time we change our code, we also need to change some tests. We say tests add math to your code and make changes more difficult. I'd say the testing is a balance between pain and gain. The gain is how much test you write. The pain is the cost of an error in your code. If you're writing an internal application for your company, bugs, as pleasant as they are, are not that critical. However, if you write software that lives depend on it, or software that will run a device on Mars, bugs are really painful. Note that no matter how much you test, bugs will get into production. You need to be ready with a way to monitor your program for bugs, and a way to quickly fix and ship it to production. The method will vary a lot depending on the system you use. NASA has one of the most strict development process in the world and they still manage
>
> **[1:34](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/testing-overview?u=76281980&t=94)** to ship bugs to outer space. However, NASA will manage to fix bugs on Mars. I'm sure your situation is not that difficult. Another thing you might want to consider is [continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md), or CI system. These systems will monitor your source control, and every time they detect a change to your source control, they will pull the code and run all the tests. This helps avoid the problem of, "I don't care it runs on your machine; we don't sell your machine to customers," meaning it'll find integration problems quickly. When a test fails in continuous integration, the system will alert the team, maybe with a message on a chatroom, maybe with an email. Now the whole team know we have an integration problem and will work or fix it. Continuous integration system also used to run tests that take long time or require complicated set up.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Continuous integration](../../Skills/Software%20Development/Continuous%20Integration%20(CI).md) (3)
> **CLI Commands:** make (1), find (1)
> **Env Vars:** nasa (2)
> **Definitions:** is a  (1)
> **Warnings:** note that (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Testing example](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/testing-example?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/testing-example?u=76281980&t=0)** - [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) has a built in test-with. However, most projects and companies prefer to use pytest, which is more Pythonic and is packed with features. Let's see an example. As soon I have my function, which scales a vector. It gets a vector in an N and return the vector times N. And now we're going to write a test. Test files should either be test underscore something .py or end with _test.py. In the code, we import numpy. We import our scale function and then write a test. We create an array, set the N, and this is the expected result, color code to get the output, And finally call numpy all close on the expected, the output, specifying that nans are equal. And we use the built-in assert to make sure that this is true. Pytest is a discovery based system. It's going to discover all the test files and then inside the test files all the functions that start with test and run them. At first, you need to configure Visual Studio Code to run pytest. You do this by clicking on the hamburger menu, going to view, and then the command palette. Here find Python: Configure Tests, pick pytest, and then say that the Root directory is dot. And give it a minute. Eventually you're going to see either a triangle or a check mark here. And now you can click on it to run the test. You are going to move to the test view. And here you see that we have a single test
>
> **[1:35](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/testing-example?u=76281980&t=95)** inside chapter 9 0 9 0 5, and this test is passing. Pytest has much more to offer such as parameterize tests, fixtures, and more. Apart from testing that your code is correct, you can also run regression tests that make sure that the accuracy of your models is not degrading. Check out my course about testing scientific applications for more.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2)
> **CLI Commands:** python (2), make (2), find (1)
> **Prerequisites:** configure (2)
> **File Paths:** _test.py (1)
> **Tools:** visual studio (1)
> **UI Navigation:** click on (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/data-science-foundations-python-scientific-stack-coderpad/next-steps?u=76281980&t=0)** - [Instructor] Congratulations on reaching the finish line. Now you need to practice. Grab some data and start asking questions. A good place to start is Kaggle, which offers a variety of tasks with many levels. Keep hacking, and stay curious.

> [!info]- Semantic Content
>
> **Speakers:** - [instructor] (1)


## Instructor

- [Miki Tebeka](../../Instructors/Data%20Science/Miki%20Tebeka.md)

## Resources

- [GitHub Repository](https://github.com/LinkedInLearning/data-science-foundations-python-scientific-stack-3084641/codespaces)

## Skills Covered

- Python (Programming Language)
- Data Science Foundations
- Data Science

## Path Context

### In [Advance Your Python Skills for Data Science](../../Paths/Data%20Science/Learning%20Paths/Advance%20Your%20Python%20Skills%20for%20Data%20Science.md)
← [Python For Data Science And Machine Learning Essential Training Part](../Artificial%20Intelligence%20(AI)/Python%20For%20Data%20Science%20And%20Machine%20Learning%20Essential%20Training%20Part.md) | **3 of 6** | [Python Data Analysis](Python%20Data%20Analysis.md) →

## Appears In

- [Advance Your Python Skills for Data Science](../../Paths/Data%20Science/Learning%20Paths/Advance%20Your%20Python%20Skills%20for%20Data%20Science.md)

## Related Courses

_Courses sharing skills:_

- [More Python Tips, Tricks, and Techniques for Data Science](More%20Python%20Tips%2C%20Tricks%2C%20and%20Techniques%20for%20Data%20Science.md) — Python (Programming Language), Data Science
- [Python for Data Science and Machine Learning Essential Training Part 2](../Artificial%20Intelligence%20(AI)/Python%20for%20Data%20Science%20and%20Machine%20Learning%20Essential%20Training%20Part%202.md) — Python (Programming Language), Data Science
- [Python For Data Science And Machine Learning Essential Training Part](../Artificial%20Intelligence%20(AI)/Python%20For%20Data%20Science%20And%20Machine%20Learning%20Essential%20Training%20Part.md) — Python (Programming Language), Data Science
- [Learning ArcGIS Python Scripting](Learning%20ArcGIS%20Python%20Scripting.md) — Python (Programming Language)
- [QGIS and Python for AEC](QGIS%20and%20Python%20for%20AEC.md) — Python (Programming Language)

---

[↑ Back to top](#)