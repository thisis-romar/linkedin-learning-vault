---
type: course
cssclasses:
  - lle-course
  - lle-level-advanced
slug: advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics
url: "https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics"
duration_minutes: 129
duration: 2h 9m
level: Advanced
updated: 9/6/2022
learners: 71762
skills:
  - Snowflake
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQF8cIq-NfphSg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1661965300072?e=2147483647&amp;v=beta&amp;t=5Dla68dldcmCM5Tljl828t3Cv9rxOmlFcJmgl-ZDQEc"
linkedin_topic: Data Science
learning_paths:
  - '[[Master Data Engineering]]'
prev_courses:
  - '[[Data Engineering- dbt for SQL]]'
next_courses:
  - '[[Complete Guide to SQL for Data Engineering- from Beginner to Advanced]]'
path_nav: '[{"path":"Master Data Engineering","position":7,"total":8,"prev":"Data Engineering- dbt for SQL","next":"Complete Guide to SQL for Data Engineering- from Beginner to Advanced"}]'
path_count: 1
tags:
  - course
  - topic/data-science
  - topic/software-development
  - topic/web-development
  - skill/snowflake
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Data%20Science/Advanced%20Snowflake-%20Deep%20Dive%20Cloud%20Data%20Warehousing%20and%20Analytics.md)

![Advanced Snowflake: Deep Dive Cloud Data Warehousing and Analytics](https://media.licdn.com/dms/image/v2/C560DAQF8cIq-NfphSg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1661965300072?e=2147483647&amp;v=beta&amp;t=5Dla68dldcmCM5Tljl828t3Cv9rxOmlFcJmgl-ZDQEc)

# Advanced Snowflake: Deep Dive Cloud Data Warehousing and Analytics

> If you’re familiar with the Snowflake platform but need to learn more, this course can help you supercharge your skills. Instructor Janani Ravi deep dives into architecture and advanced concepts. After a concise introduction, Janani gets started with table structures in Snowflake, covering topics like micropartitioning, clustering, and creating permanent, temporary, and transient tables. She expla

> [LinkedIn Learning](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics) | 2h 9m | Advanced | 72K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Advanced Snowflake](#advanced-snowflake)
  - [Snowflake architecture](#snowflake-architecture)
  - [Getting set up with Snowflake](#getting-set-up-with-snowflake)
- [**1. Table Structures in Snowflake**](#1-table-structures-in-snowflake) (8 videos)
  - [Micropartitioning and clustering](#micropartitioning-and-clustering)
  - [Types of tables in Snowflake](#types-of-tables-in-snowflake)
  - [Creating permanent tables](#creating-permanent-tables)
  - [Creating temporary tables](#creating-temporary-tables)
  - [Creating transient tables](#creating-transient-tables)
  - [Configuring data retention time](#configuring-data-retention-time)
  - [Querying historical data](#querying-historical-data)
  - [Dropping and restoring tables](#dropping-and-restoring-tables)
- [**2. Views in Snowflake**](#2-views-in-snowflake) (6 videos)
  - [Non-materialized, materialized, and secure views](#non-materialized-materialized-and-secure-views)
  - [Creating and populating tables](#creating-and-populating-tables)
  - [Creating and querying views](#creating-and-querying-views)
  - [Recreating views](#recreating-views)
  - [Querying regular views and secure views](#querying-regular-views-and-secure-views)
  - [Creating and querying materialized views](#creating-and-querying-materialized-views)
- [**3. Optimizing Query Performance**](#3-optimizing-query-performance) (8 videos)
  - [Caching, clustering, and search optimization](#caching-clustering-and-search-optimization)
  - [Loading data using COPY INTO](#loading-data-using-copy-into)
  - [Retrieval optimization using cached results](#retrieval-optimization-using-cached-results)
  - [Enabling clustering on tables](#enabling-clustering-on-tables)
  - [Querying clustered tables](#querying-clustered-tables)
  - [Querying clustering information](#querying-clustering-information)
  - [Running point lookup queries on search-optimized tables](#running-point-lookup-queries-on-search-optimized-tables)
  - [Clustering vs. search optimization](#clustering-vs-search-optimization)
- [**4. Working with Semi-structured Data**](#4-working-with-semi-structured-data) (3 videos)
  - [Loading and querying parquet data](#loading-and-querying-parquet-data)
  - [Loading JSON data](#loading-json-data)
  - [Querying JSON data](#querying-json-data)
- [**5. Managing Users and Granting Privileges**](#5-managing-users-and-granting-privileges) (3 videos)
  - [Access control](#access-control)
  - [Creating users and viewing grants](#creating-users-and-viewing-grants)
  - [Granting privileges to roles](#granting-privileges-to-roles)
- [**Conclusion**](#conclusion) (1 videos)
  - [Summary and next steps](#summary-and-next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Advanced Snowflake](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/advanced-snowflake?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/advanced-snowflake?u=76281980&t=0)** - [Janani] Hi, and welcome to this course on advanced [[Snowflake]] on [[LinkedIn]] learning. My name is Janani Ravi, and I'll be your instructor for today. Now, since this is an advanced Snowflake course I assume that you have some basic understanding of how to work with the Snowflake data platform. You have access to a Snowflake account and you know how to work with database schema and Abes in Snowflake. Let's get started by quickly discussing what exactly Snowflake is, how it's architected and what's unique about it. How it helps us process huge data sets and performing analytics with just queries in [[SQL]]. Here is how you can define Snowflake. Manage data platform, [[SaaS|software as a service]]. available on all three major cloud platforms. Let's break this down because every bit of this definition is important. The fact that Snowflake is a managed data platform means that you do not have to install and set up any software or [[Hardware]] before using Snowflake. You can simply access it as a service. All you have to do to work with Snowflake is create an account log in and get up and running. And since the Snowflake is supported on all three major cloud platforms you can work with Snowflake where data actually is. Now, the question is why would you choose to use Snowflake when there are so many [[Big Data]] technologies out there? Well, Snowflake is easy, intuitive and very very straightforward to use
>
> **[1:33](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/advanced-snowflake?u=76281980&t=93)** and allows you to perform large scale data analytics using just SQL queries without the overhead of actually managing your data infrastructure. Snowflake is supported on AWS [[Microsoft Azure|Azure]] and the [[Google Cloud Platform (GCP)|Google Cloud Platform]] which means that you can use a Snowflake with the cloud provider and on the platform of your choice. You can choose to use Snowflake in the region where your data actually lives. Potentially avoiding moving data to a new provider or a new region. Even compared to other big data platforms, Snowflake is a true [[SaaS]] offering very, very easy to use. There is absolutely no cluster provisioning and administration, and there is no overhead of managing any kind of physical or virtual hardware. Snowflake uses its own proprietary SQL engine. There is no software for you to install and manage or configure. Everything is done for you. All of the updates, maintenance, management, [[Performance Tuning]], everything is handled by Snowflake. Snowflake offering is cloud first and cloud only, which means it's a cloud native solution. Snowflake runs only on [[Cloud Infrastructure]] and is supported on AWS Azure and the GCP. You cannot use a Snowflake on your own premises data center even if you want to. [[Data Processing]] in Snowflake uses cloud based [[Virtual Machines]] for both compute as well as query processing. In addition, all of your data is actually stored
>
> **[3:07](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/advanced-snowflake?u=76281980&t=187)** in cloud based storage services. Snowflake's database which is a persistent store of data is actually cloud based storage. Here are the features that Snowflake offers. It's an enterprise analytics software for large scale data processing. Snowflake is capable of processing petabytes of data and is great for the construction of EPL pipelines. Unlike many other big data processing platforms Snowflake does not actually build upon an existing big data framework such as [[Hadoop]] or [[Apache Spark]]. Snowflake in fact runs its own proprietary SQL query engine which makes Snowflake highly performant and very simple and intuitive to use. And finally, you'll find that when you use Snowflake you get hooked onto it because of its innovative architecture which is designed to be cloud-first and cloud-native.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (31), [[SQL]] (4), [[Big Data]] (4), [[Hardware]] (2), [[Microsoft Azure|Azure]] (2)
> **Env Vars:** sql (4), aws (2), gcp (1), epl (1)
> **Definitions:** is a  (3), means that (2), is an  (1)
> **CLI Commands:** aws (2), apache (1), find (1)
> **Prerequisites:** install (2), set up (1), configure (1)
> **Analogies:** such as (1)
> **Speakers:** - [janani] (1)

#### [Snowflake architecture](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/snowflake-architecture?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/snowflake-architecture?u=76281980&t=1)** - [Instructor] So how does [[Snowflake]] enable [[Big Data]] processing at scale? Well, a large part of it is its unique architecture. Snowflake's architecture is a hybrid of traditional shared disc and shared nothing database architectures. I'll explain what I mean in just a bit. Here is a big-picture overview of what the different components in Snowflake look like. Snowflake's architecture is made of three separate layers. At the very bottom, we have the database storage layer. Then we have the query processing layer on top of database storage. And finally, at the very top, we have the [[Cloud Services]] layer. First, let's turn our attention to the database storage layer. This is a central data repository for persistent data that is accessible from all of the processing nodes of the platform. This is where Snowflake's architecture is similar to shared disc architectures. Database is stored on these nodes and compute anywhere can access this data. The data in Snowflake is stored in its own proprietary internal format. And the format is so structured that it allows for queries to be run very, very fast on this data. The format is highly optimized for querying. It's compressed. Data is not stored in the raw format. Instead, it's compressed and then stored. And the format has a columnar structure. The columnar structure ensures that if you select just a few columns from the underlying data, the entire record with all of the columns is not scanned. The storage layer cannot be accessed directly by the user,
>
> **[1:38](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/snowflake-architecture?u=76281980&t=98)** so you can't work with the storage blobs directly. Instead, you access the data stored in storage using [[SQL]] queries. The important thing to note here is that the storage layer is purely used for [[Data Storage]]. All of the query processing is performed in the layer above. This is the query processing layer, and the processing or compute units in this layer are virtual warehouses. The virtual warehouses term is specific to Snowflake. So it's worth an additional bit of explanation. A virtual warehouse is just a cluster of compute resources, resources such as CPU, memory, and temporary storage, which is used to run all of the query processing operations in Snowflake. Virtual warehouses don't actually store data. They do have a cache, though, but their main function is to process queries. Each warehouse is actually a massively parallel, independent cluster with one or more compute nodes. All of the parallelization and optimization of your queries occur within virtual warehouses. Now, you may have more than one virtual warehouse within your Snowflake account. Queries that are executed on different warehouses do not compete for the same resources. They're completely isolated from one another. If you want a single query to run faster, having multiple warehouses will not help. In such situations, you should make your warehouse more powerful. Larger warehouses imply more compute resources
>
> **[3:12](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/snowflake-architecture?u=76281980&t=192)** for your single query, and that results in faster queries. An important feature of Snowflake's architecture is the fact that the compute and storage layers in Snowflake are completely independent. The main advantage of this is that you can scale either compute or storage. You don't need to scale both if you don't need to. For example, if you have a large amount of data, you can scale the data stored by adding additional data storage resources. You don't need to add additional compute if you're not going to be running many queries. On the other hand, you can scale compute performance by adding additional warehouses without affecting storage. So if you're storing just a little bit of data but you're running many queries on the data, well, scale compute, not storage. And finally, on the very top, we have the cloud services layer that work alongside compute and storage. These services, and there are a number of different of them, bring together the different components of Snowflake in order to process queries. The cloud services layer, as you can see, is a collection of services. The services managed in this layer include authentication, infrastructure management, [[Metadata]] management, query parsing optimization, access control, the works. Here is a quick look at the basic object hierarchy within Snowflake. You can see the organization is at the very top. Within an organization, you have accounts. All of your users and roles assigned to users are under this account object. [[Databases]] and virtual warehouses are also provisioned
>
> **[4:49](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/snowflake-architecture?u=76281980&t=289)** within the context of an account. Within a database, you provision the schema, and the schema holds all other data-related objects. A schema in Snowflake is just a logical grouping of database constructs, such as tables, views, pipes, file formats, stages, the works. If you have a database construct, that has to exist within the schema.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (14), [[Cloud Services]] (3), [[Data Storage]] (2), [[Big Data]] (1), [[SQL]] (1)
> **Definitions:** is a  (5)
> **Analogies:** such as (2), picture (1), for example (1)
> **Env Vars:** sql (1), cpu (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Getting set up with Snowflake](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/getting-set-up-with-snowflake?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/getting-set-up-with-snowflake?u=76281980&t=1)** - Here we are in our very first demo of this advanced [[Snowflake]] course I've logged into a Snowflake free trial account, and I'm currently on the Snowflake classic UI. The classic UI is an older user interface but it's more fully featured. Our very first demo on permanent, temporary and transient tables will perform using the classic UI. For the remaining demos in this advanced Snowflake course we'll switch to Snowsight, the new UI from Snowflake. Now I'm working within a Snowflake free trial account. You can get your own free trial by going to this URL that you see here on screen. All of the demos in this course will run on any of the cloud platforms, whether it's [[Microsoft Azure|Azure]], AWS, or GCP. I happen to be using Azure. The Snowflake free trial lasts for about a month and it gives you a number of free credits that you can use to test out Snowflake and it's a great feature. I've run all of the demos in this course on the enterprise edition of Snowflake. The enterprise edition has more features than the basic standard edition and is tailored for large scale organizations and enterprises. Your account should look exactly like this when you log in. Let's take a look at the [[Databases]] and you can see that the only databases that have access to are the ones that Snowflake has created and shared with me. SNOWFLAKE_SAMPLE_DATE and SNOWFLAKE. The SNOWFLAKE_SAMPLE_DATE database has been pre-created for every Snowflake account and contains a number of tables that you can query and test out your Snowflake Commands. Let's head over to Warehouses and you'll find
>
> **[1:36](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/getting-set-up-with-snowflake?u=76281980&t=96)** that one Warehouse has been created by default. This is the COMPUTE_WH, which is a extra small type of Warehouse. It's currently in suspended state. This is the smallest size Warehouse possible on a Snowflake. And this is sufficient for us to run all of the queries in this demo. We don't need a larger Warehouse. Smaller Warehouses consume fewer credits per hour but offer fewer compute resources for processing, but that's sufficient for the purposes of our demo. Now let's head over to Worksheets. Within a Worksheet is where we'll write our queries to create tables and query those tables.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (14), [[Microsoft Azure|Azure]] (2), [[Databases]] (2)
> **Env Vars:** snowflake_sample_date (2), url (1), aws (1), gcp (1), snowflake (1)
> **CLI Commands:** aws (1), find (1)
> **Definitions:** is an  (1), is a  (1)
> **UI Navigation:** switch to (1)
> **Speakers:** - here (1)


### 1. Table Structures in Snowflake

[↑ Back to Table of Contents](#table-of-contents)

#### [Micropartitioning and clustering](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/micropartitioning-and-clustering?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/micropartitioning-and-clustering?u=76281980&t=1)** - [Instructor] In order for [[Snowflake]] to be able to efficiently process large amounts of data, the design of their table structures is quite critical. If you had a huge amount of data in petabytes and you stored this within a table in a single contiguous blob, queries run on such tables will likely to be very, very inefficient. If you just have a single blob of contiguous data, your queries will run slowly because they need to process this huge amount of data and queries will be unable to run in parallel on a contiguous blob. Almost every [[Big Data]] processing platform partitions the data that it stores in tables. Now if this partitioning is static, well that helps with query parallelization but static partitioning has its own limitations. Your data may be skewed. Certain partitions may be very large. Other partitions may be small resulting in slow query performance. In Snowflake, your tables are divided into micro-partitions. These micro-partitions are extremely small partitions automatically managed by Snowflake. In addition to partitioning, Snowflake also performs data clustering which co-locates related records in a table. Let's first understand how micro-partitions in Snowflake work. All of your data in Snowflake is automatically divided into micro-partitions. You just set up the table, micro-partitioning is done for you. You don't need to do anything explicit. A micro-partition is so called because it's small.
>
> **[1:36](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/micropartitioning-and-clustering?u=76281980&t=96)** Each micro-partition contains only about 50 to 500 megabytes of uncompressed data which means in the compressed format, the data will be even smaller. The rows and tables are mapped to individual micro-partitions. And then within each micro-partition, the rows are organized in columnar form. If you have a very large table, that table might contain thousands or even millions of micro-partitions and Snowflake can handle that with ease. Query performance can be improved if you avoid unnecessary scans of micro-partitions while executing queries. This is why Snowflake stores additional [[Metadata]] about the rows in each micro-partition such as the range of values for each column, the number of distinct values and any other property that will help with query optimization. This metadata is used by Snowflake to paralellize queries and to also skip micro-partitions where possible. So what are the benefits of these micro-partitions? Well first, you don't have to do anything. Micro-partitioning is automated. Snowflake takes care of everything for you. Now because these partitions are small, each partition is tiny. This allows for very efficient query pruning and that avoids unnecessary scans of partitions which may not contain the records you need. The column, the storage of your records within a micro-partition, makes your queries more efficient in that only those columns referenced by your query will be scanned.
>
> **[3:07](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/micropartitioning-and-clustering?u=76281980&t=187)** And finally, the data is stored in an efficient, compressed format. In addition to micro-partitions, Snowflake also supports automated clustering of your data. Clustering is the sorting or ordering of data along natural dimensions then storing this data in tables. When you apply clustering to a table, your data with the same clustering key is co-located. One thing to note is that Snowflake tables are partitioned by default, they are not clustered by default. You need to explicitly add clustering to a table by specifying a clustering key. Once your table is clustered, the records with the same value of clustering key are then co-located. This clustering information can then be leveraged by Snowflake to avoid scanning unnecessary micro-partitions for queries. So anytime you have a query that performs a filtering operation or some kind of range filtering using the clustering key, the performance of those queries will be improved. Snowflake maintains a bunch of metadata for your clustered tables such as the total number of micro-partitions for a table. It also keeps track of the number of micro-partitions that contain overlapping values for clustering keys. So records that have the same value in the clustering key column, if they're in different micro-partitions, that is considered overlapping values of clustering keys across micro-partitions. The depth of this overlapping micro-partitions is referred to as clustering depth. One thing to keep in mind is
>
> **[4:41](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/micropartitioning-and-clustering?u=76281980&t=281)** smaller the clustering depth, better the clustering of the table, better the performance of your tables that reference the clustering key.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (14), [[Metadata]] (3), [[Big Data]] (1)
> **Analogies:** such as (2)
> **Warnings:** keep in mind (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Types of tables in Snowflake](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/types-of-tables-in-snowflake?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/types-of-tables-in-snowflake?u=76281980&t=0)** - [Instructor] [[Snowflake]] supports different kinds of tables meant for different use cases. In order to understand how these tables are different, it's important for us to understand how continuous data protection works in Snowflake. Your data can be of three different types stored in different places in Snowflake. We have active data stored within your tables. Time travel data; this is historical data that you can access. And then finally, we have fail-safe data; this is your backup and disaster recovery. Active data is data that you work with on a day-to-day basis: data in your Snowflake tables that is accessible via the queries that you run, any data definition language updates you made, any [[Data Manipulation]] language updates that you make. When you're working with active data, you'll constantly be updating this data. Now, historical data, that is, data from previous time periods, can be accessed at any point in time in Snowflake within a certain predefined period, and this predefined is referred to as the data retention period. Time travel allows you to access the state of your data as it existed at some point in time, and this way you can use time travel to restore data-related objects that may have been deleted. You can use time travel for backups of data from important points in time in the past, and also, time travel can be used to analyze data usage over time. The data retention period that your table supports for time travel is something that you configure on a table. And finally, data stored in fail-safe
>
> **[1:35](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/types-of-tables-in-snowflake?u=76281980&t=95)** is a backup that is separate from time travel. Once your data has moved beyond the time travel retention period, it enters fail-safe. Fail-safe ensures that your data is accessible and recoverable, even in the case of security breaches or system failures. Unlike time travel, fail-safe is not configurable; it's a non-configurable seven-day period during which Snowflake can recover historical data. You enter fail-safe after your time travel period has ended. If you seek to recover data in the fail-safe period, well, that data may take hours or days to recover, and you need to explicitly contact Snowflake. Snowflake supports three different kinds of internal tables. We have permanent tables, temporary tables, and transient tables. These tables differ based on where they're available, their time travel retention period, and their fail-safe period. Let's talk about permanent tables first; those are the easiest to understand. These are any normal tables that you created, and data in those tables will persist till you explicitly drop that table. Permanent tables allow you to configure the time travel retention period. The period can be anywhere from zero to 90 days for all editions other than the Standard Edition of Snowflake. In the Standard Edition of Snowflake, time travel period can only be zero or one. Zero means time travel is disabled. The fail-safe period for permanent tables is seven days. Next we have temporary tables.
>
> **[3:08](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/types-of-tables-in-snowflake?u=76281980&t=188)** Temporary tables are only valid in the session in which they are created; the data is persistent only for the remainder of the current session. The maximum time travel available for temporary tables is just one day, and there is no fail-safe option for temporary tables. In the case of transient tables, they're like permanent tables in that the data in those tables persist till the table is explicitly dropped by the user. However, the maximum time travel available for transient tables is just one day, and there is no fail-safe available. So how should you choose which kind of table you are to use to store your data? Any data that is repeatedly queried, critical data meant for long term use, such data should be stored in permanent tables. If your data is repeatedly queried but it's non-critical and having a backup of that data is less important, such data can be stored in transient tables. And finally, if your data is temporary or transitory data meant for short-term use, store them in temporary tables. One last note before we move on to the demo, Snowflake also supports external tables, so Snowflake can query data stored in external [[Cloud Storage]] outside of Snowflake. Now, this cloud storage could be S3 on Amazon, [[Microsoft Azure|Azure]] Data Lake, or [[Google Cloud Platform (GCP)|Google Cloud]] Storage. With external tables, your data will continue to reside on this external cloud storage service but can be queried from within Snowflake. Here, read queries are allowed;
>
> **[4:41](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/types-of-tables-in-snowflake?u=76281980&t=281)** updates to the data are not allowed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (14), [[Cloud Storage]] (3), [[Data Manipulation]] (1), [[Microsoft Azure|Azure]] (1), [[Google Cloud Platform (GCP)|Google cloud]] (1)
> **Prerequisites:** configure (2)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Creating permanent tables](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-permanent-tables?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-permanent-tables?u=76281980&t=1)** - [Instructor] In this demo that spans the next few videos, we'll see how permanent, temporary, and transient tables work, and understand their different characteristics. Now, when we work within our worksheet, I'm going to give the worksheet a name so that we know exactly where we are. This first worksheet, I'm going to call Production because I'm going to create, you can hypothetically imagine, tables which are meant for production. First, let me go ahead and create a database called ECOMMERCE_DB. You can run this command by clicking on the run button off to the top-left, or by hitting command + enter or control + Enter with your [[Cursor]] positioned over the [[Snowflake]] command you want to run. Once we've created this database, observe on the top-right of the worksheet, we are currently running with role ACCOUNTADMIN. We are using the COMPUTE_WH to run our queries. That's the only warehouse we have. We are working within the ECOMMERCE_DB database in the public schema. Let's make sure we are using the ECOMMERCE_DB based in the command use database. ECOMMERCE_DB, run it. Once the database has been created and that we are currently using that database, you should find the database listed on the left navigation pane of your worksheet. Hit refresh in order to view this database. And you can see that we have the ECOMMERCE_DB there. The database is ready for us to use. Let's now run a query to see which warehouse database and schema we are currently using. Select the current warehouse, current database, and current schema.
>
> **[1:33](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-permanent-tables?u=76281980&t=93)** These are built in-functions available in Snowflake. We are running within the COMPUTE_WH within the ECOMMERCE_DB and the public schema in that database. We are now ready to create our very first table, which is going to be a permanent table. This table is called IN_PRODUCTION. Observe on line seven when I say create or replace table IN_PRODUCTION, I have no qualifier before the name of the table IN_PRODUCTION. This means that this is a permanent table. A permanent table is available across sessions and users till it's explicitly dropped. You can also configure a longer retention period for permanent tables. We won't really worry about configuring and understanding retention periods here. That's for a later demo in this course. Imagine that this table contains mobile phones that this [[E-Commerce]] site sells. And these are mobile phones that are launched in the actual catalog. They are in production. Go ahead and hit command + enter, and you can see that this table has been successfully created. I can tell what type of table this is if I query the INFORMATION_SCHEMA.TABLES available in Snowflake. I've selected the table name, table type, whether the table is transient or not, the retention time for the table, and whether auto-clustering has been turned on from INFORMATION_SCHEMA.TABLES for a table which has a name like IN_PRODUCTION. That's our table right here. Within the query results, notice that the table type is BASE TABLE,
>
> **[3:06](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-permanent-tables?u=76281980&t=186)** which represents a permanent table in Snowflake. You can see that IS_TRANSIENT is also no. The retention time of this particular permanent table is one day, which means that time travel, that is historical data, is available for one day on this table. We'll talk much more about time travel in a later demo. Let's move on and let's now insert some data into this IN_PRODUCTION permanent table. These are the mobile phones that our site is selling right now. That's why they are IN_PRODUCTION Go ahead and hit command + enter or control + enter. And these seven rows have been inserted in our table. Permanent tables are, well, permanent. They can be queried across sessions, and can be queried across users provided the users have access to that table. Let's run a simple select star query, and we are the owners of this table, and we are able to query and view the results of this table. We can also view attributes of the table by running the describe table command. Describe table IN_PRODUCTION will give us all of the columns in this table and the types of the different columns, their default values, and a number of other details.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (4), [[E-Commerce]] (1)
> **Env Vars:** in_production (7), ecommerce_db (6), compute_wh (2), information_schema (2), tables (2)
> **Definitions:** means that (2), is called (1), is a  (1)
> **CLI Commands:** make (1), find (1)
> **Analogies:** imagine (2)
> **UI Navigation:** select the (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [Creating temporary tables](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-temporary-tables?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-temporary-tables?u=76281980&t=1)** - [Instructor] In order to create and work with temporary tables, I'm going to open up a new worksheet. And I can do this by clicking on this plus tab here. Now, every worksheet in [[Snowflake]] is a separate session. And if you remember, temporary tables are tables that are valid only within the session where they've been created. So opening up a new worksheet will allow us to create a temporary table within a different session. I'm going to call this worksheet, testing. We'll create the temporary table within the same database and schema that we were working with before. We'll use the ECOMMERCE_DB and the public schema within that DB. And you can confirm this by looking on the top right. We are in the account admin role, compute warehouse, ECOMMERCE_DB public schema. Now let's create a temporary table. Remember, our temporary table is only valid within the current session, and it is a table that you typically use for testing. That's why we are within the testing worksheet. The create table command is very similar to how we create permanent tables. The only difference on line three, notice I have the keyword temporary before table. This keyword temporary is what makes this table a temporary table, rather than a permanent or a transient table. Let's run this code. This table was successfully created. The table has the same structure as our IN_PRODUCTION table. The IN_TESTING table is what we'll use for testing. Now, if you refresh the left navigation pane, within the test worksheet, you should find within ECOMMERCE_DB and the public schema.
>
> **[1:34](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-temporary-tables?u=76281980&t=94)** You'll find that there are two tables listed and accessible within the session. The first is the IN_PRODUCTION table. It's a permanent table. So it's available across all sessions and across all users who have privileges till the table is explicitly dropped. The IN_TESTING table is a temporary table, and available only within this session within this worksheet. How do we confirm that this is indeed a temporary table? Let's run the same query where we query the table name, table type, whether the table is transient or not, and other details from INFORMATION_SCHEMA.TABLES. The INFORMATION_SCHEMA gives us [[Metadata]] information about Snowflake objects. Our query is for all tables that starts with the IN_ prefix. You can see that on line 18. Let's take a look at the results here. Observe that the IN_TESTING table has table type local temporary. This is different from the IN_PRODUCTION table which has table type equal to base table. That's a permanent table. IN_TESTING is a temporary table, and it's local to this session. Observe that transient is set to null, and it has no retention time. Retention time is by default zero. Let's insert a few records into this testing table. This IN_TESTING table is available in this worksheet for insertion, as well as queries. Now that we've inserted a few records, we can run a simple SELECT * query to query the records available in our temporary local table. And this query ran successfully
>
> **[3:06](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-temporary-tables?u=76281980&t=186)** within the testing worksheet. Our temporary table is accessible here, but what about our permanent table IN_PRODUCTION? Well, that's also accessible within this different session. Remember, permanent tables are accessible across sessions, across users, provided you have the privileges. So when I query the production table, I can see the results here on screen. Let's prove that our IN_TESTING table is indeed local to this session. Switch over to the production worksheet. This is a separate session. Now I'm going to expand the left navigation pane and hit refresh. And you'll observe that within the ECOMMERCE_DB public schema, you will not find that IN_TESTING table. You can only see the production table. You cannot find the testing table in here. Let's be a hundred percent sure of this. I'm going to head over to the editing window and run a query, SELECT * FROM IN_TESTING. You can see this on line 36. When I run this query, I get a compilation error. Object IN_TESTING does not exist or not authorized. It's definitely not an authorization problem, because our current logged in user has created both tables. However, as far as this particular session within the production worksheet is concerned, IN_TESTING, well, it does not exist. That's because it's a temporary table, local to a different session. The testing worksheet has that session. So if you head over to testing, your temporary table will be accessible once again. In fact, let's try cloning the temporary table.
>
> **[4:41](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-temporary-tables?u=76281980&t=281)** Clone will copy over the structure and contents of the IN_TESTING table. And the name of the clone is IN_TESTING_CLONE. Now, when we run this, you'll see an error. And the error is because, we are trying to clone a temporary table to a permanent table. And that's not allowed. You can however, clone a temporary table to a transient table. Well, we haven't studied transient tables yet. We'll come to that next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (2), [[Metadata]] (1)
> **Env Vars:** in_testing (11), ecommerce_db (4), in_production (4), information_schema (2), select (2)
> **Definitions:** is a  (5)
> **CLI Commands:** find (4)
> **SQL:** select (2)
> **Speakers:** - [instructor] (1)

#### [Creating transient tables](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-transient-tables?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-transient-tables?u=76281980&t=1)** - [Instructor] In order to create transient tables and understand how they work, let's open up a new worksheet, that is a new session within our [[Snowflake]] console. I'm going to call this worksheet Beta. Imagine this is a beta testing worksheet. And within this, we'll create transient tables for the purposes of beta testing in our eCommerce site. Now, we'll continue working with the ECOMMERCE_DB. So I have the used database ECOMMERCE_DB run this. This has executed successfully. I'm now ready to create a transient table, and you'll see that the CREATE TABLE command is very similar to the CREATE TABLE commands that we've used earlier. Observe though that on line 3, I've used the keyword TRANSIENT before TABLE. This keyword TRANSIENT is what makes this IN_BETA table a transient table. A transient table is available to all users with the appropriate privileges till their table is explicitly dropped. So they're like permanent tables. They differ from permanent tables in that they do not have a fail safe period. The time travel retention period is a maximum of one day for transient tables, beyond which data deleted is not recoverable using fail safe in Snowflake. Let's see what the characteristics of a transient table are. Let's run are SELECT query on this [[Metadata]] INFORMATION SCHEME TABLES. And you can see that for our transient table, the table type is BASE TABLE. So observe the IN_BETA table on row 1.
>
> **[1:35](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-transient-tables?u=76281980&t=95)** The table type says BASE TABLE, but IS_TRANSIENT is true. So under IS_TRANSIENT, you see YES. And you can see the RETENTION_TIME is 1 by default. Observe that the characteristics of the transient table is very much like our permanent IN_PRODUCTION table, except that IS_TRANSIENT is true. Observe that we cannot see the IN_TESTING table. That is temporary and local to the testing worksheet. Let's populate this transient table with a few beta testing values. Once again, all of these are mobile phones. You can go ahead and query the transient table as well. And this query will also be successful. Remember, transient tables are available to all users that have privileges. Let's switch over to the Production worksheet, which is a different session. And within the Production worksheet, you should be able to view and query the transient table. So a SELECT * FROM IN_BETA will work here. You can see that this ran through successfully. So transient tables are available across sessions. Back to our Beta worksheet, I'm going to try and clone this transient table. So I'm going to create a clone named IN_BETA_CLONE by cloning the contents and structure of the IN_BETA table. Go ahead and run this command, and you can see the error right there on screen. Transient object cannot be cloned to a permanent object. Snowflake does not allow you to create permanent tables
>
> **[3:09](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-transient-tables?u=76281980&t=189)** from transient tables. However, you can create temporary tables from transient tables, and that's what we'll do next. I'm going to create a temporary table called BETA_TO_TESTING by cloning the IN_BETA table. Observe on line 32, I have CREATE OR REPLACE TEMPORARY TABLE, indicating that my clone table is a temporary local one. And this one you can see is successfully created. So you can create temporary tables from transient ones. Before we conclude this demo, let's take a look at what tables are accessible in which worksheet. Now I've switched over to the Production worksheet, and I've hit Refresh on the left navigation pane. Observe that the permanent table, IN_PRODUCTION, and the transient table, IN_BETA, are both available here. None of the temporary tables can be accessed in this session because they were not created in this session. Let's head over to the Testing table. Here, we have created a temporary table. So if you hit Refresh, you'll see that our permanent, transient, and the temporary IN_TESTING table is available within this session. The cloned temporary table, BETA_TO_TESTING, was not created in this session, and so it's not available in this session. And finally, let's head over to our Beta worksheet, and let's hit a Refresh and see what tables are accessible here. Well, of course, the permanent table IN_PRODUCTION is accessible, as is the transient table, IN_BETA, also the temporary table, BETA_TO_TESTING.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (3), [[Metadata]] (1)
> **Env Vars:** in_beta (7), table (6), create (3), is_transient (3), in_production (3)
> **Definitions:** is a  (5)
> **SQL:** create table (2), select (2)
> **Code Identifiers:** ecommerce (1)
> **Analogies:** imagine (1)
> **Speakers:** - [instructor] (1)

#### [Configuring data retention time](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/configuring-data-retention-time?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/configuring-data-retention-time?u=76281980&t=1)** - [Lecturer] In this demo, that'll span the next few videos, we'll work with time travel in [[Snowflake]]. Time Travel and Snowflake gives you access to historical data stored within your table within a certain time period. And this time period is referred to as the retention time period. Now I'm working within my production worksheet. I'm going to run a query to take a look at the [[Metadata]] for my in production table. This is the table that I had created earlier in this course. When you look at this metadata, observe the retention time is set to one. This is the default retention time for a permanent table created in Snowflake. What does this retention time mean? Any changes you make to the data in this table will be backed up for a period of one day. And you can access historical data going back one day. You can actually change this retention time. Once the table has been created you can also specify it at the time of creation. I'm now going to set the retention time in days to be equal to 120 days. And this will give me an error because the retention time for a permanent table can be a maximum of 90 days in Snowflake. And the error gives us very precise instructions as to why this is wrong. In case you do not want the data that you've stored in a Snowflake table to be backed up? Well, you can disable time travel by setting the data retention time to be equal to zero. That's what I've done for my in production table. Now, if I make any changes to this table, I won't have access to historical data.
>
> **[1:34](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/configuring-data-retention-time?u=76281980&t=94)** You should know it's possible to disable time travel, but you may not want to do that because having access to historical data is actually useful. Let's set our data retention time to seven days which means historical data for seven days will be available to us. Why are time travel queries? Let's once again query the metadata for the tables that we've created. And we can use the results of the query to confirm that yes, indeed, our data retention time for the in production table is now seven days, which is within the valid range of zero to 90. Now with transient tables you can choose to disable time travel by setting the data retention time to zero days. That's what I've done here. Or, the maximum data retention time can be the default, one day. If I try to set the data retention time for my in data table to seven days, that will result in an error. That's- transient tables can have a maximum data retention period of one day.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (5), [[Metadata]] (3)
> **CLI Commands:** make (2)
> **Cross-References:** earlier in (1)
> **Speakers:** - [lecturer] (1)

#### [Querying historical data](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-historical-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-historical-data?u=76281980&t=1)** - [Instructor] After having configured a data retention time of seven days for our in production table, let's go ahead and query the data in this table, and take a look at what it looks like. Observe that the first two records are of the brand OPPO, that's a brand of mobile phone. You can see that the [[Google]] Pixel brand is also available here in this table. Another thing to note, is that there are seven records in this table. When querying historical data, it's possible to request for data as the data existed, when a particular query was run by query ID. Select last query ID, will there was the query ID for the last query that we ran, which was the select star from in production. You can see this query ID here at the bottom. Let's go ahead and copy this query ID, you can see that it ends with a 26a, keep that in mind, we'll be using this query ID in our historical query. In the meanwhile, let's delete some data from this table, let's delete those records where brand is equal to OPPO, there were two such records, and when we run the delete command, you can see that two records have been deleted. I'll also update this table, I'm going to set brand is equal to GP, where the brand was originally Google Pixel. So I've shortened Google Pixel to just GP. Now, the latest data in our active table in production will not contain OPPO records, and will have Google Pixel renamed to GP. And when you run the query, you'll see that we have five rows instead of seven,
>
> **[1:33](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-historical-data?u=76281980&t=93)** that's because the two records related to OPPO have been deleted, and the brand Google Pixel is now called GP. This is on row one. You can query historical data using the at, or before function, here line 11, I'm going to select from the in production table, before the statement corresponding to the ID that I have specified was executed. This ID that I have here corresponds to the statement that I executed on line one, that is select star from production. The data in our table before that statement was executed contained OPPO, and you can see that the first two records in our result contain OPPO, and there are a total of seven rows in the result. And if you scroll a bit, you'll see that Google Pixel still has its original brand name, Google Pixel, rather than GP. You can also query historical data based on an offset, relative to the current time. So I want to select star from in production, at a time instance five minutes before the current time. The offset is always a negative value specified in seconds, and it's usually written as 60 seconds, that is a minute, multiplied by the number of minutes. Now I want to see the data as it looked, at five minutes before my current time. Now the updates that I made to the table, where I deleted OPPO and renamed Google Pixel, were about 10 minutes ago. So even five minutes ago, Google Pixel was called GP, and I have just five records, that is five rows,
>
> **[3:09](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-historical-data?u=76281980&t=189)** OPPO is not present. But well, 20 minutes ago? That's a different matter, that's when I hadn't performed any deletes or updates to my table. So when I select star from in production with an offset of 20 minutes, well, you can see OPPO is present here within my query result, and Google Pixel is still called Google Pixel. All of the seven records from the original table were available 20 minutes ago. Now you can also query historical data based on timestamp, and that's something for you to research, and try on your own.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Google]] (11)
> **Env Vars:** oppo (9)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Dropping and restoring tables](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/dropping-and-restoring-tables?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/dropping-and-restoring-tables?u=76281980&t=1)** - [Instructor] If you have Time Travel enabled for a particular table, you can drop that table, and if you're still within the Time Travel retention time, well, you can undrop the table and restore its contents. Let's go ahead and drop the In_Production table. The table was successfully dropped, and we can confirm this by running a Select Star operation. Select Star from In_Production will now give us an error, because that table does not exist anymore. Well, we are still within the seven day retention period for this In_Production table. In fact, we are within a few seconds. I can run Undrop Table In_Production, and the table will be restored. I now confirm that this table has indeed been restored by running a Select Star from In_Production. This should allow us to retrieve the results of this table, and that's indeed the case. The five rows in the active table are now available to us. Let's now change the data retention time for this table and set it to zero, which means we are going to disable Time Travel for this table, the In_Production table. I'll quickly confirm that Time Travel has indeed been disabled. Let query the information_schema.tables and look at the [[Metadata]]. You can see In_Production retention time has now been set to zero, which means we do not have access to historical data for this table. Now, if I were to go ahead and drop this table In_Production, I will not be able to restore its contents. The table data is no longer being backed up by Time Travel.
>
> **[1:38](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/dropping-and-restoring-tables?u=76281980&t=98)** Let's prove this. First, let's prove that this table has indeed been dropped. The Select Star should give you an error, and it does. Let's try and undrop this table. So Undrop Table In_Production, this time this is going to fail. That's because data retention time is zero. Time Travel has been disabled. A dropped table cannot be restored. You can't query historical data from this table as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Metadata]] (1)
> **Code Identifiers:** information_schema (1)
> **Speakers:** - [instructor] (1)


### 2. Views in Snowflake

[↑ Back to Table of Contents](#table-of-contents)

#### [Non-materialized, materialized, and secure views](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/non-materialized-materialized-and-secure-views?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/non-materialized-materialized-and-secure-views?u=76281980&t=1)** - [Instructor] If you analyze data using the same complex query over and over again, it might be useful for you to define a view. Views in [[Snowflake]] are defined by a query, and views allow users to access the result of that query as though the result were in a table. Views can thus be thought of as a named reference to a query, and other queries can reference views exactly like they do tables. Just like you use a table name within a query, you can use a view name instead. Views are very useful because they give you access to different cuts of your data. Views allow you to combine, segregate, or protect data. For example, instead of giving users access to all of the records in a table, you might want to give them access to only a subset of records that you define using a view. Snowflake supports two types of views. We have non-materialized views, or just regular views, and we have materialized views. Let's understand how these are different. We look at the non-materialized view first. A non-materialized view is defined using a query and is just a named definition of a query. In a non-materialized view, the result of running the query associated with the view is not stored anywhere. Each time the view is referenced in another query, the actual query associated with the view is run and the results generated. The performance of a non-materialized view will often be slower than materialized views. A materialized view is defined exactly like a regular view,
>
> **[1:37](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/non-materialized-materialized-and-secure-views?u=76281980&t=97)** but it behaves like a table. A materialized view is also defined using a query. The results of the materialized view are stored in a similar fashion to how our table's contents are stored. So it's in assistance storage somewhere. The implication of this is that you get faster access to the results of the view. So you get faster access to the underlying data, but you have to pay for additional storage costs because the materialized view is actually taking up extra storage, and there are some additional restrictions on materialized views, querying them, working with them, which are beyond the scope of this particular course. Now, if you want Snowflake to provide additional security to your views, you can define both materialized and non-materialized views as secure views. Secure views allow for additional [[Data Privacy]] for the records associated with that view. And because of this, secure views may be less performant than other views because they do not allow Snowflake to perform certain kinds of query optimizations. Views are useful because they make your code more modular and easier to read and maintain. Rather than repeating the same subquery over and over again, you can just define a view for that subquery. Views also allow you to grant access to a subset of a table to your users. Rather than giving them access to all records in a table, you can just give them access to the records that they really need.
>
> **[3:09](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/non-materialized-materialized-and-secure-views?u=76281980&t=189)** And if you choose to use materialized views, these will significantly improve the performance of your queries because they store the subset of data that you're interested in, similar to how table contents are stored. Views in Snowflake have certain limitations. The definition of a view cannot be updated. You have to replace or recreate the view from scratch. If you make changes to the underlying table from which a view is generated, changes to the table structure are not propagated to the views. And finally, views in Snowflake are read-only. You cannot use a view to update the underlying data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (6), [[Data Privacy]] (1)
> **Analogies:** just like (1), for example (1), similar to (1)
> **CLI Commands:** make (2)
> **Speakers:** - [instructor] (1)

#### [Creating and populating tables](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-populating-tables?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-populating-tables?u=76281980&t=1)** - [Instructor] For this demo, and for all of the demos from here on in, we'll no longer work with the classic [[Snowflake]] web interface. You can click on this icon here, off to the top right of your screen, and switch over to the new Snowsight user interface. The Snowsight user interface is far slicker, but as of now, the time of this recording, it does not have the complete functionality that the classic UI does. Within the classic user interface, in order to run queries, you need to create a new worksheet. That's exactly what I'm going to do. And I'm going to rename the worksheet and call it "Views." That's because we are going to be creating and working with views in this worksheet. Within the Snowsight user interface you can specify the database and schema that you want to work with using this dropdown. We'll continue working with the [[E-Commerce]] database and public schema. I'll run a use statement for the e-commerce database as well. Hit command + enter or control + enter. Or you can use the run icon off to the top right of the screen. Within the schema, let's create and populate a few tables that we'll use for our views demos. The first is the customer details table. You can imagine that this table contains information about the customers for our e-commerce site. Customer ID, name, phone number, email address. Customer ID is the primary key for this table. Customer name has a not null constraint. Let's create yet another table. This one will store the [[Microsoft Products|products]] of our e-commerce site. "Product Info" is the name of the table. We have the product ID, name, brand, and price.
>
> **[1:35](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-populating-tables?u=76281980&t=95)** Product ID is the primary key, product name and brand are not null. Let's now create yet another table. This is the "Order Details" table. The order details table links the customer details and the product info table. You can see the prod ID references prod_ID from product info. And cust_ID references cust ID from customer details. This table, you can imagine, contains the orders placed for products on an e-commerce site by our customers. Now that these tables have been created you can run show tables and you can see that the three tables are all three permanent tables. You can see all have a retention time of one. And if you scroll over to the very end you can see that they're not external tables. External tables are tables where the data is hosted outside of Snowflake on the cloud platform's storage service. Like S3 on Amazon, [[Google Cloud Platform (GCP)|Google Cloud]] Storage on the GCP, and Blob Storage on [[Microsoft Azure|Azure]]. Having created these tables we are now ready to populate them. Let's insert some customers into our customer details table. We have Alice, James, Bob, and Mark. Customer C1 one through C4. With this done, lets add some products to our e-commerce catalog. So insert into product info some products from different brands. P1 through P6. The very last field that you see for each record is the price of the product. And finally, let's populate the order details. These are the orders placed
>
> **[3:07](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-populating-tables?u=76281980&t=187)** by our customers for these products.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[E-Commerce]] (6), [[Microsoft Products|Products]] (5), [[Snowflake]] (2), [[Google Cloud Platform (GCP)|Google cloud]] (1), [[Microsoft Azure|Azure]] (1)
> **UI Navigation:** click on (1), dropdown (1)
> **Analogies:** imagine (2)
> **Env Vars:** gcp (1)
> **Speakers:** - [instructor] (1)

#### [Creating and querying views](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-querying-views?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-querying-views?u=76281980&t=1)** - [Instructor] Now that we've set up the customer's [[Microsoft Products|products]] and orders table, you might want to run a query like this one. This query is fairly complicated, but it's a very useful query. Let's first look at lines 3, 4, 5, and 6. Observe that in this query, we are joining three tables together, CUSTOMER_DETAILS, ORDER_DETAILS, and PRODUCT_INFO. CUSTOMER_DETAILS is joined with ORDER_DETAILS based on cust_id. That result is then further joined with PRODUCT_INFO based on prod_id, this is on line 5, and the entire table is ORDERED BY ord_id and then cust_name. Now let's turn our attention to lines 1 and 2, the SELECT clause. We select the ord_id, ord_date, cust_name, prod_name, and the cost of the product, which we get by performing a simple math operation. We multiply the product price by quantity and subtract the discount applied to this product price multiplied by quantity, and that's the cost associated with the product bought by a customer. And now when we run this query, you can see we have a cost column at the very right of our screen. Now it's quite possible that the data analysts on your team often need to run this query over and over again and maybe perform other operations based on the results of this query. Now if each time you were to rerun this query, well, that's going to be pretty painful. In such situations, it makes sense to create a VIEW backed by this query,
>
> **[1:33](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-querying-views?u=76281980&t=93)** and that's exactly what I have done here. On line 8, using CREATE OR REPLACE VIEW, I create a VIEW named ORDER_SUMMARY, and the query associated with that VIEW is present on lines 10 through 15, the same join operation, the three way join operation that we performed earlier. When you create a VIEW using CREATE OR REPLACE VIEW, what is created is a regular non-materialized non-secure VIEW. A VIEW is just a named representation of a query. Now that we have a VIEW, if you want to run the underlying query, we simply call SELECT * FROM ORDER_SUMMARY. When this query is executed, the query associated with the ORDER_SUMMARY VIEW will be executed, and we'll see the same results as we saw earlier. In the case of a regular VIEW, the query associated with that VIEW is rerun each time you access the VIEW, which means that if you change the data in the underlying table, as I'm doing now, I'm inserting some additional orders into the ORDERS_DETAILS table. On line 20, you can see that customer C1, Alice, has bought an additional product P3. Now once you've changed the ORDER_DETAILS table, if you query the ORDER_SUMMARY VIEW, the updated details will be available in the VIEW, so I'm going to do a SELECT * FROM ORDER_SUMMARY once again. And this time when I run this query, the query associated with ORDER_SUMMARY, the join operation, will be rerun, and you can see here at the bottom,
>
> **[3:06](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-querying-views?u=76281980&t=186)** Alice Johnson has bought a third product, a Sony Bravia TV. So when the underlying table changes, the VIEW will also give you updated results.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Products|Products]] (1)
> **Env Vars:** view (16), order_summary (6), order_details (3), select (3), customer_details (2)
> **Code Identifiers:** ord_id (2), cust_name (2), cust_id (1), prod_id (1), ord_date (1)
> **SQL:** select (3)
> **Definitions:** is a  (1), means that (1)
> **Cross-References:** as we saw (1)
> **UI Navigation:** select the (1)
> **Prerequisites:** set up (1)

#### [Recreating views](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/recreating-views?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/recreating-views?u=76281980&t=0)** - [Narrator] In the earlier video, you saw that changes made to the data of the base table are reflected when you query the view. However, changes made to the structure of the base table are not reflected in the view. So let's see exactly that here. I'm going to create yet another view. Once again, this is a regular view called Premium [[Microsoft Products|Products]]. This is backed by the query select star from product info where price is greater than thousand. This view represents products greater than a thousand dollars which are tagged as premium products. Let's query this view, and you can see that there are exactly two products here tagged as premium products which are available based on this view query. Now with this done, I'm now going to change the structure of my table, the product info table. Here is my alter table command. I'm altering the product underscore info table, and I'm going to add an additional column called configuration, which is of type VARCHAR hundred. Now the statement has been executed. The structure of the product info table has changed. Now, if I try to query from the premium products view, this will result in an error. Originally, when this view was first defined, the underlying table had four columns, but we now changed the underlying table to have five columns. In order for this view to be usable, you'll need to recreate the view. That's exactly what I've done on lines 11, 12, and 13. I've invoked the create or replace view command. I've recreating the premium products view
>
> **[1:36](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/recreating-views?u=76281980&t=96)** using the same query, select star from product info, where price is greater than thousand. The view has been recreated. And now if you query from the view, you'll obtain the results that you expect and observe that when you query from the view, you get that extra configuration column from the underlying base table.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Products|Products]] (7)
> **SQL:** varchar (1)
> **Env Vars:** varchar (1)
> **Definitions:** is a  (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [narrator] (1)

#### [Querying regular views and secure views](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-regular-views-and-secure-views?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-regular-views-and-secure-views?u=76281980&t=1)** - So far, the views that we've worked with have been regular views. With regular views the underlying query associated with the views executed each time you query the view. Materialized views are different, but before we understand materialized views let's look at the query profile of a query that we run on a regular view. I have a query here on line 17 and 18. Select star from order summary where broad name like S percent [[Microsoft Products|products]] that start with S. I'm going to run this query. I get the query results. I'm not interested in the query results. Instead, I'm going to click on the three dots that you see over at the bottom, right? This will bring up a little pop up on snow sight which you can use to copy your query ID or view query profile. View query profile is what I'm interested in. Clicking on this. It'll open up a new browser tab and you can see that the query profile gives you a flow chart of the different operations that are run when you execute the query. I'm going to [[Zoom]] out a bit so you can see all of the individual boxes. I'll just adjust the query profile on the screen a little bit. And here you can see at the very bottom, the leaf nodes of the query profile, as it were, are all able scans. You can select each of these leaf nodes and take a look. But each of these correspond to the scan of the three tables on which we ran a joint query in order to create our order summary view. So each of these tables were scanned. Then some kind of filtering operation was performed on each table.
>
> **[1:34](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-regular-views-and-secure-views?u=76281980&t=94)** The boxes about each table scan say filter or join filter. Once we retrieved the results from each of our tables we performed a join operation. And when you select that box you can see that this is a joint between the products and orders table on product ID. You can now select the second join operation just about that. And you can see that this is a join operation which takes a result of the first join and joins that with the customers table. And the attributes, you can see that the joint condition is on customer ID. And then finally some kind of sorting is performed on the results and we get the final result displayed to screen. Now, the thing here is we see a lot of details of what's happening in the underlying query associated with the view. It's possible that this is a view that someone else created and that you are querying. You'll get additional information in the query profile in case of a regular view. But in the case of a secure view you'll see that things are a little different. So let's switch back to our query editor and now create a secure view. So I'm going to use the same query to create a secure view. You can see the command that I run is create or replace secure view. And the name of the view is order summary secure. So the keyword secure on line 20 is what makes this view a secure view. I've associated a comment with the secure view. It simply says this view is a secure version and the query associated with the secure view is select start from order summary.
>
> **[3:08](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-regular-views-and-secure-views?u=76281980&t=188)** Observe that this query underlying the secure view queries another view. Once the secure view has been created, let's now run a query on this secure view. Select star from order summary secure. I'm going to run this query. And once again, I'm going to view the query profile. So click on the three dots here at the bottom right and open up the query profile for the secure view. And observe that here you can see just two boxes within the query profile. That's because the details of the query underlying the secure view is not exposed when you use a secure view. You can see that we have the final result. That's the box on top. And if you click on the box here at the bottom you can see that the only information available to us is that this view is a secure view. The details of the query underlying the secure view is hidden from us. The join operations, the filter operations, all of those details are not exposed to us when we use a secure view. Secure views do not leak the details of the underlying table or its data to queries that use the secure view. This also means that certain optimizations that [[Snowflake]] may be able to perform with regular views are not possible in secure views which means it's possible that secure views are slower than regular views.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Products|Products]] (2), [[Zoom]] (1), [[Snowflake]] (1)
> **Definitions:** is a  (5), means that (1)
> **UI Navigation:** click on (2), select the (1)
> **Speakers:** - so (1)

#### [Creating and querying materialized views](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-querying-materialized-views?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-querying-materialized-views?u=76281980&t=1)** - [Instructor] In this demo, we'll create a view by using one of [[Snowflake]]'s sample tables as the underlying base table. The table that we are working with is in the snowflake sample data database. It's in the schema called TPCH, underscore SF 10 and the table is called LINEITEM. I'm going to create a table called line item in my current database, eCommerce DV, the public schema. And we'll use the data in this table as the basis for our view. Now, the reason I've chosen this particular table is that it's fairly large. It has 1.6 gigabyte worth of data. If you want to see a sample of what this data looks like run a simple select star operation and you can see that this is artificially generated data. So there is lots of data here. You can take a look at all of the columns. The data here is not important. It's important to know that the size of this table is now 1.5 gigs. I'm now going to create two different views using this table. First, we'll create a regular view called high tax view. So I'm going to select all of the records from line item where tax is greater than 5%, greater than 0.05. Now this is a regular view which means that the records associated with this query will not be stored anywhere. Anytime you query the view the underlying table will be queried. So when I run select star from high tax view you'll see that it takes a long time to get the results. You'll find that to get the results from this query, it can take anywhere up to 30 seconds.
>
> **[1:38](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-querying-materialized-views?u=76281980&t=98)** Let's take a look at how long the query execution actually took, and you can see here off to the right, the query duration was 18 seconds and the results of the query had 13.3 million rows. If you want queries on such a view to run faster it's often better to create a materialized view. A materialized view will execute the query associated with it, generate the results and store the results, as though it were a table. Observe line 13, I call create or replaced materialized view, high tax view materialized the use of the keyword materialized before view is what makes this a materialized view rather than a regular view. Now, when you run this, it'll take much longer for the view to be created because all of the results are now stored somewhere as a part of this materialized view. Now let's run the same query on the materialized view select start from high tax view, materialized where tax greater than 0.06. Once again, it takes a while to get the results but it's a tad faster. If you look at the bottom right, and click on the cross, you can see that the query duration was 16 seconds. This is faster than the 18 seconds that it took to run this query on a regular, non-materialized view, and 13.3 million records for return. The query profile of a query run on a materialized view also looks different from that of a regular view. Observe that there are very few boxes.
>
> **[3:13](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-and-querying-materialized-views?u=76281980&t=193)** That's because when we query a materialized view, the materialized view records, which is stored somewhere that is what is scanned. You can see that here in the bottom box, the materialized view scan is what retrieves the records. Those records are filtered and we get the final result. And finally, it's possible for you to secure a materialized view as well. Notice, the create or replace statement on line 19 create or replace secure materialized view, and the name of the view's high tax view, secure materialized. The uses of both keywords secure and materialized makes this a secure materialized view. This is how you create a secure materialized view and querying it is just like querying any other view. Select star, specify the name of the view, and run your query. Remember, queries may run a little slower on secure views as compared to insecure views. If you take a look at the query profile of a secure materialized view you'll find the details are even more sparse. Earlier, there were three boxes, which indicated that a filter operation was performed on the underlying view. And even that is now hidden here.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (2)
> **Versions:** 13.3 (2), 1.6 (1), 1.5 (1), 0.05 (1), 0.06 (1)
> **Definitions:** is called (1), is a  (1), means that (1)
> **CLI Commands:** find (2)
> **Env Vars:** tpch (1), lineitem (1)
> **Code Identifiers:** ecommerce (1)
> **UI Navigation:** click on (1)
> **Analogies:** just like (1)


### 3. Optimizing Query Performance

[↑ Back to Table of Contents](#table-of-contents)

#### [Caching, clustering, and search optimization](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/caching-clustering-and-search-optimization?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/caching-clustering-and-search-optimization?u=76281980&t=1)** - [Instructor] [[Snowflake]] runs queries on huge amounts of data. In order for these queries to run quickly, the queries need to be optimized. And there are several techniques that Snowflake uses for this. We'll discuss and work with three query optimization techniques. The first is caching. Next, we'll discuss clustering. And finally, we'll discuss search optimization. Let's start with caching. It's by far the easiest technique to understand, and it happens automatically. We've already discussed that Snowflake's architecture divides Snowflake into three layers, database storage, where data is persisted, query processing, which comprise of virtual warehouses, and the [[Cloud Services]] layer. So we have the storage layer, the compute layer, and the service layer. The actual data is stored in the storage layer. However, the data is cached in both the compute layer as well as the service layer. The service layer accepts [[SQL]] requests from users, coordinate queries, manages transactions and results, and the service layer has the result cache. This holds the result of every query executed in the past 24 hours. And this cache is available across virtual warehouses and across sessions. The compute layer where the SQL queries are actually processed contains the local disk cache. This is used to cache data used by SQL queries. Whenever data is needed for a given query, it's retrieved from the Remote Disk storage and cached in SSD and memory in the compute layer. The storage layer holds the long term storage for our data,
>
> **[1:38](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/caching-clustering-and-search-optimization?u=76281980&t=98)** refer to as a Remote Disk storage. In order to improve the performance of certain kinds of queries, you can enable clustering on tables. Clustering involves sorting or ordering data along natural dimensions when storing that data in tables. Clustering in Snowflake is completely automated. You designate one or more columns as the clustering key, and this enables clustering. Data is automatically reclustered when it's updated or deleted or changes are made. Let's say you choose country as the clustering key for your data. All of the records belonging to the same country will be co-located in the same micro-partition, allowing very fast retrieval operations on queries which refer to country. If you enable clustering on a table, you will incur additional costs. There are costs for the initial clustering of the table and for maintaining the clustering as the table is updated. Clustering is very useful when you require very fast response times and you don't really care about the cost of clustering. Clustering is used when the improvements that you get for your query offsets the cost of maintaining the clustered table. Clustering improves the performance of queries that filter based on the clustering key. Queries run faster because they can now skip data that does not match the filter predicate. Clustering also gives you
>
> **[3:10](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/caching-clustering-and-search-optimization?u=76281980&t=190)** better column compression for your data. The only configuration that you require when specifying clustering is to correctly define the clustering key. Now clustering improves the performance of equality searches on the clustering key, as well as range searches using the clustering key. Range searches meaning searches which include the greater than and less than operator. And finally, the third optimization technique that we'll discuss is search optimization that has performed using the search optimization service. The objective of search optimization is to improve the performance of selective point lookup queries on tables, queries that use the equality operator. A point lookup query is one that returns either one or a small number of records from a table. Search optimization is a table level property in Snowflake, and users need to explicitly register their tables with the search optimization service. It's not registered by default. When you enable search optimization on a table, Snowflake uses an additional persistent data structure that serves as an optimized search access path for all of the records in your table. This data structure is initially populated when you first enable search optimization, and there on in a maintenance service runs continuously in the background and updates the search optimization data as your table is updated. Search optimization is great if most of your queries are selective point lookup queries, that is, search optimization
>
> **[4:47](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/caching-clustering-and-search-optimization?u=76281980&t=287)** only speeds up quality searches, it does not speed up range searches. But the advantage of search optimization is that the optimization is applied on all columns of supported types in a table. You don't have to pick and choose specific columns to which optimization is applied.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (7), [[SQL]] (3), [[Cloud Services]] (1)
> **Env Vars:** sql (3), ssd (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Loading data using COPY INTO](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-data-using-copy-into?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-data-using-copy-into?u=76281980&t=1)** - [Instructor] Starting from this demo over the next few demos, we looked at some different query optimization techniques that [[Snowflake]] supports. Now in order to run our queries, we need a large dataset. The first thing we'll do is create a new database and able and load in a large dataset. The name of the database is sales_db. Hit Command. Enter to create this database. Go ahead and call use database sales_db. so that we switch over to using the public schema within this database rather than the previous eCommerce DB that we were using. The next thing we'll do is create the table into which we'll load our data. Now, this is a fairly large table with many different columns. I call Create or Replace Table and the name of the table is Iowa Sales. And you can see that we have a large number of columns in here. This is a large dataset that is about five GB in the uncompressed format. Go ahead and hit Shift+Enter and this table has been successfully created. Once this is done, I'm now going to switch over to the terminal window and log in using SnowSQL, Snowflake's command line utility. I already have SnowSQL installed on my machine. If you need instructions on how to install SnowSQL, this URL that you see here on screen should guide you. Whether you have a [[Windows]] machine or Mac OS or [[Linux]], SnowSQL is available for all of these platforms. I'm going to go ahead and run the SnowSQL command -A, specifies the URL for my Snowflake account
>
> **[1:36](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-data-using-copy-into?u=76281980&t=96)** and -use specifies the username. Hit Enter. You'll be prompted for your password, SFI or password, and log into SnowSQL. Observe the prompt. We are using the COMPUTE warehouse, the database and schema have not been specified. The database that we are working with is sales_db and let's use schema public. Public is anyway the default schema that will be used. Next, we'll create a stage. Stages in Snowflake are locations which are used to store data which are then loaded into tables. If the data that's to be loaded is in an external location like say Amazon S3 or [[Microsoft Azure|Azure]]'s blob storage, you'll use an external stage. But here, we're going to use an internal named stage. This named stage is going to be called sales stage and this is what I'm going to use to load in my Iowa sales data. Loading data into this internal Snowflake stage is performed using the quote command. I have the CSV file on my local machine. It's under users/loonycorn/Desktop/AdvancedSnowflake. I put this file into this named sales stage. Go ahead and run this command and you'll see that this operation takes about 10 minutes. That's because of the size of this file. At the end of about eight to 10 minutes, you should get a success message, The CSV file has been successfully compressed and loaded into our Snowflake stage. If you run a list at Sales_Stage command,
>
> **[3:10](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-data-using-copy-into?u=76281980&t=190)** you should see this gzip file is now present in this stage. You can see that this particular file is about 1.2 GB in the compressed gzip format. Now the next step is to copy the data in this stage and load the CSV data into the Iowa_sales table and we'll do this using the copy into command. Observe that I copy into the Iowa_sales table that we had set up previously. And I copy from the Sales_Stage and specifically from the sales.CSV.gzip file. The file format here gives us the format of the CSV file type of CSV, fields are delimited using the comma. We skip the header and fields may be optionally enclosed by the double quotes. In case an error occurs when we load data, we'll continue past the error. Now this operation will take about two to three minutes and at the end, you'll find that the data is partially loaded. There are about three records that are malformed which are not loaded. The [[Representational State Transfer (REST)|rest]] of the data will be loaded.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (6), [[Windows]] (1), [[Linux]] (1), [[Microsoft Azure|Azure]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **Env Vars:** csv (6), url (2), sfi (1), compute (1)
> **Code Identifiers:** sales_db (3), ecommerce (1)
> **Tools:** terminal (1), command line (1)
> **Definitions:** is a  (2)
> **Prerequisites:** install (1), set up (1)
> **File Paths:** sales.csv (1)
> **CLI Commands:** find (1)

#### [Retrieval optimization using cached results](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/retrieval-optimization-using-cached-results?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/retrieval-optimization-using-cached-results?u=76281980&t=0)** - Now that we've successfully loaded data into the Iowa Sales table, be ready to run queries on this table. This table contains records for the sales of different kinds of drinks across stores in Iowa. I'm going to run my very first Select Star from Iowa Sales. Now, because this is a large table, this query will take up to a minute to run. So you'll have to be a little patient. And once you get the results, let's take a look at how long it took for a query to execute. And if you click on the little cross at the bottom right of your screen you'll see that this query took about 44 seconds to run. And a total of 19.7 million rows were returned. Now this was a fairly simple query, no joints, no filtering, nothing, but you'll find that the Query Profile still holds some information for us. So let's open up the Query Profile and you'll find that there are two blocks in here. The bottom most block is the Table Scan where the underlying table was read to retrieve the results. And finally, the block on top is where the result was actually extracted from the table. This query extracted all of the 1.13 GB of compressed data. You can see under [[Statistics]], Bytes written to result is about 1.13 GB. So this was a fairly heavy duty operation. Now let's head back to our Query editor, and let's run this exact same query again. You can simply place your [[Cursor]] on the previous query and rerun it or type in the same query again.
>
> **[1:34](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/retrieval-optimization-using-cached-results?u=76281980&t=94)** This time around, when I hit Shift + Enter, you'll find that the query runs almost instantaneously. Well, at least compared to the previous time and the results are available to us within a few seconds. On the right under Query Details, you can see that the Query duration was just 76 milliseconds in order to return 19.7 million rows. Well, that's pretty amazing. What happened? Well, the Query Results were stored in the cache in the [[Cloud Services]] layer. And you can see from the Query Profile that this Query results were reused. Since [[Snowflake]] had already cached the results of this query previously, Query results were now retrieved from cache. This is Retrieval Optimization. Snowflake by default caches these query results for around 24 hours. These cached results are available across sessions. I'm now going to create a new worksheet which will be a fresh session and you'll find that if you run the same query in this new worksheet, well, the results will be retrieved from cache. Let's run the exact same query Select Star from Iowa Sales. And once you run it, you'll see that the data is available almost immediately. In fact, this query runs even faster than it did before. It takes only about 26 milliseconds to run. These cached results are also available to all other users who have privileges on this table. Now let's head back
>
> **[3:07](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/retrieval-optimization-using-cached-results?u=76281980&t=187)** to our optimizations worksheet and I'll show you how you can disable this caching. If you need to, for any reason. That's a simple ALTER command on the session. ALTER session set use cached result is equal to false will never retrieve data from cached results even if they are available. Now that I've altered the properties of our current session, I'm going to run Select Star from Iowa Sales, once again. And this time the query will take a long time to run because it'll retrieve the data from the underlying storage. You can see because data wasn't retrieved from cached, the query took almost 43 seconds to run. Well, the cached is useful and I'd like to use it. So I'm going to the ALTER Session and set use cached result to true, once again.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (2), [[Statistics]] (1), [[Cloud Services]] (1)
> **CLI Commands:** find (4)
> **Versions:** 19.7 (2), 1.13 (2)
> **Env Vars:** alter (3)
> **UI Navigation:** click on (1)
> **Definitions:** is a  (1)
> **Speakers:** - now (1)

#### [Enabling clustering on tables](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/enabling-clustering-on-tables?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/enabling-clustering-on-tables?u=76281980&t=1)** - [Instructor] The next kind of query optimization that we'll explore in [[Snowflake]] is clustering. Clustering involves the use of a clustering key to co-locate records within a table. Anytime you filter records using the clustering key, those filter operations should run much faster than in an unclustered table. Now let's take a look at our Iowa sales table. We'll look at the [[Metadata]] for this table, and you'll see that the table is not clustered by default. This is a regular permanent table within the database sales underscore DB. And as you scroll over under the clustering key column you'll see a null value indicating that this is an unclustered table. Let's scroll further over to the right and you can see that auto clustering on for this table is set to no. That is automatic. Clustering is not enabled. In the latest versions of Snowflake, clustering is managed for you automatically. The automatic clustering service will constantly scan your table for updates and recluster your data as needed. That is when you have clustering enabled. The next thing I'm going to do here is enable clustering for this table. And this can be done using a simple altered table command. Altered table, Iowa sales, cluster by county. We co-locate records based on the county in which the store is located. Iowa as a state is divided into counties. And this will cluster the records corresponding to the different counties
>
> **[1:34](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/enabling-clustering-on-tables?u=76281980&t=94)** so that the records belonging to the same county will be co-located in the underlying data structure for this table. Let's go ahead and execute this and cluster our table. I've used just a single clustering key here. You can cluster tables by multiple keys as well. I'll run a select star operation once again, and we'll take a look at the metadata associated with this table. And this time around, as you scroll to the right, you'll see that we now have a clustering key. Observe the clustering key is linear clustering by county. And if you scroll further over to the right, you'll find that automatic clustering is now enabled for this table. Anytime you configure a clustering key for a table, auto clustering will be turned on. That's the only kind of clustering supported by Snowflake. Manual clustering, which used to exist earlier, has been deprecated. In order to understand the impact of clustering on our tables, I'm going to turn off caching. I'm going to set use cache result to false. This is so that we can explore the impact of clustering on our queries, isolated from the impact of caching. Now, if you have a table that is already clustered, and you create a clone of that table, the clone table will also be a clustered table. Here I use creator, replace table, and create a clustered clone of the original clustered Iowa sales table. Let's take a look at the metadata associated with Iowa sales cluster.
>
> **[3:08](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/enabling-clustering-on-tables?u=76281980&t=188)** That is our clone table. And you'll see that this table also has a clustering key. The same clustering key, linear by county. So when you clone a table, the clustering associated with the original table is also present in the clone table. But auto clustering is not enabled in the clone. Auto clustering was enabled in the original table, but will not be enabled in the clone. Automated clustering is important only when you plan to update the data that you have in the table. And you want the table to be reclustered when new data is added or existing data is updated. Well, we are not planning to do any of that. We are just planning to see the optimization effects of clustering. So this is totally fine. That's just something to keep in mind. Now, let's see how we can change the clustering state of a table. I'm going to go ahead and drop the clustering on the original Iowa sales table. And I do this by calling alter table, Iowa sales, drop clustering key. So now the original Iowa sales table is no longer clustered. And we can confirm this by querying the metadata for that table. But we have a clustered table called "Iowa Sales Clustered". You can see that this original table has clustering key set back to null, it's no longer clustered. And automated clustering is once again not on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Metadata]] (4), [[Snowflake]] (3)
> **Definitions:** is a  (1), is an  (1)
> **CLI Commands:** find (1)
> **Warnings:** keep in mind (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [Querying clustered tables](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-clustered-tables?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-clustered-tables?u=76281980&t=1)** - [Instructor] Now we have two tables, a clustered table and an unclustered table. Let's run queries on both of these tables. We'll run exact same query on both tables, and we'll see if clustering gives us a little bit of a speed up. Just a heads up that it's possible, that when you run these queries, the speed up is either not very significant, or the speed up doesn't exist at all. That's because the table that we're working with, is still rather small. The effects of clustering are really massive, when you have tables of the order of hundreds of gigs, which is really hard to work with in a demo. Let's run our very first select statement, select star from Iowa Sales, where county is equal to Clinton. It takes a while to generate the results for this query, but you'll find that the query itself runs pretty fast. If you look at the query details, you can see the query duration, is just two point six seconds. Remember the Iowa Sales table is not clustered, and this particular filter operation on the county column, ran in two point six seconds on this unclustered table. Let's now run the same query on our Iowa Sales clustered table. Since the filtering operation is on the cluster key, we would expect that this query runs a little bit faster, and you can see it does. Query duration is two point zero seconds, as opposed to two point six seconds on the unclustered table. Now it's quite possible when you are running this, you don't see much of an improvement, and that's totally fine.
>
> **[1:32](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-clustered-tables?u=76281980&t=92)** Remember the table itself is rather small for clustering to have a massive effect. On a clustered table, if you perform all the by operations using the clustering key, those should be faster as well. Here I have two order by operations. The first order by is on the unclustered table, select star from Iowa Sales order by county. Now, this takes a while to run. I think it's almost 50 seconds. The exact number of seconds is 48. Remember, we do not have caching. If you look at the query details, you see that the query duration was 48 seconds, in order to order and retrieve 19 point seven million rows. Let's run the exact same query on our clustered table, Iowa Sales clustered order by county, and this time around you'll see, that our query runs a wee bit faster. If you look at the query details, you'll see the query executed in 45 seconds. I should tell you though that for this particular data set, it's not that clustering always improves the performance of these queries because this data set, is still rather small. Let's now run a different kind of query. Here the filter operation is on the city. You can see this on line 12 and 15. This means that we're not actually filtering on the clustering key, but we're filtering on a column that is related to the clustering key. Filter queries on columns related to the clustering key column should also be,
>
> **[3:07](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-clustered-tables?u=76281980&t=187)** sped up on clustered tables. Let's run this first query on Iowa Sales, that is our unclustered table. This query runs fairly quickly. It only took about 311 milliseconds, two point six key records, 2600 records. Now I'll run the same query on the clustered table. Filter where city is equal to Adair, and in this particular's case, it seems like this query actually seems to run a little slower on our clustered table, 540 milliseconds as opposed to 311 milliseconds, though I'm not really sure this query performs any worse. I think any difference here is clearly random. If our table had been much larger that it is, filtering by city that is closely associated with county should be sped up on the clustered table.

> [!info]- Semantic Content
>
> **CLI Commands:** find (1)
> **Definitions:** means that (1)
> **Warnings:** heads up (1)
> **Speakers:** - [instructor] (1)

#### [Querying clustering information](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-clustering-information?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-clustering-information?u=76281980&t=1)** - [Instructor] Defining clustering keys for a table only benefits the table if certain conditions are met. Clustering really helps the table if it contains a large number of micro partitions. Clustering is also useful if the queries that you run on the data often use the clustering key as a filter, which means it's also important to pick the right clustering key. Columns which are often used in filters and columns which do not have a very high cardinality. A column with high cardinality is one which has a large number of distinct values. Before actually clustering a table using a certain clustering key, you can get clustering information for that table and that particular key and see whether the key is a good one. For example, here, I'm curious as to whether the date column in my table will give me a good clustered result. Run this query and you can see that this clustering information is present in a [[JSON]] format. You can select the one record in the result and then expand the JSON information off to the right. You can see that the date column is fairly high cardinality. Don't be led by that statement alone, look at the other details here. Look at the average overlaps and the average depth of clustering. Notice that the total number of partitions for this table is equal to 33, and the average overlap is 32. That means there are 32 micro partitions containing values that overlap with each other.
>
> **[1:37](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-clustering-information?u=76281980&t=97)** The average depth here refers to clustering depth, that is the depth of the overlapping micro partitions. Smaller the average depth, better the clustering. And average depth of 33 is not great for our table. So, let's run another query. Let's get clustering information for the city column. Once again, we get this information in the JSON format, let's select the record and expand the JSON and take a look. Once again, this is a high cardinality column. Total number of partitions, 33. Average overlaps is 32 and average depth 33 months again. Again, city does not seem to be a great choice for clustering. Let's try county. This is what we had used to cluster this table in the first place. And if you take a look at the JSON in the result, you can see that it's still a high cardinality column but total number of partitions 33, that remains the same. Average overlaps is just 6.78, and average depth 4.8182. You can see that county is a fairly decent column for us to use to cluster our data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (5)
> **Env Vars:** json (5)
> **Definitions:** is a  (3), refers to (1)
> **Versions:** 6.78 (1), 4.8182 (1)
> **UI Navigation:** select the (2)
> **Analogies:** for example (1)
> **Best Practices:** the key is (1)
> **Speakers:** - [instructor] (1)

#### [Running point lookup queries on search-optimized tables](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/running-point-lookup-queries-on-search-optimized-tables?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/running-point-lookup-queries-on-search-optimized-tables?u=76281980&t=1)** - In this demo, we'll work with the search optimization service which can be used to improve the performance of selective point lookup queries on tables. Search optimization is a table level property and can be enabled for the table as a whole, and it applies to all columns in a table. Now, before we get to search optimization I'm going to change the current session to not use use cached results. USE_CACHED_RESULT I set to false. This is so that we can clearly identify and isolate the improvements in our query performance due to search optimization from any caching related improvements. In order to apply search optimization let's create a brand new table. This table is going to be called IOWA_SALES_OPTIMIZED and we'll populate this table using all of the records from the original IOWA_SALES table. So we create or replace table IOWA_SALES_OPTOMIZED by selecting star from IOWA_SALES. Applying search optimization on any table is associated with some costs. There are storage costs, compute costs and additional maintenance costs. If you want to see what these costs will be for the Iowa sales table, let's run a select on system estimate search optimization costs. We get some [[JSON]] values in the results. Let's expand this and take a look at what these JSON values are. You can see that currently search optimization is not enabled on this table. Search optimization enabled is false. The first set of costs, build costs have to do
>
> **[1:34](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/running-point-lookup-queries-on-search-optimized-tables?u=76281980&t=94)** with actually building this search optimization lookup data structure. You can see how many credits that will consume. For this particular table it'll be roughly 0.02 credits. Let's scroll down a little further. You can see how many terabytes will be consumed for storing the data structure for search optimization. And if you go still further down that's where you'll find the estimate for maintenance costs. Well, since the table is not that old usage patterns are not known, the maintenance costs cannot be computed. Let's take a look at our search optimized table. Show tables like Iowa sales. You can see there are three such tables; the original table, the cluster table that we've created earlier, and the third record is our optimized table that we've created just now. We haven't enabled search optimization just yet. I'm going to scroll over and show you that search optimization has not yet been enabled for this table. You can see that search optimization is off for all three tables. Pay close attention to the third table. That's the one that we are going to be working with. No progress, no number of bites, nothing. The next step is to actually enable search optimization for the table that we've just created. We'll do that with an alter table command line 10 I have alter table Iowa sales optimized add search optimization. These terms "add search optimization" is what enables search optimization for this table. Now we'll confirm that search optimization has
>
> **[3:09](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/running-point-lookup-queries-on-search-optimized-tables?u=76281980&t=189)** indeed been enabled. Show tables like Iowa sales. There are our three tables. Keep track of the third record here. Observe that search optimization is now on on the third record. For any table, it takes some time to build up and populate the data structure that allows search optimization. Now because this is a fairly small table by [[Snowflake]] standards, it has only about one GP of compressed data. Search optimization progress is hundred. That means the entire table has now been indexed. We'll now run a few queries on the original Iowa sales table. We'll then run the same query on our search optimized table and see how they perform. We'll run point look up queries here, select star from Iowa sales, where invoice number is a specific number. This is a point lookup query. We are looking up a particular invoice and you can see that the query duration is 330 milliseconds. This is at the bottom right of your screen. Now let's try the same query on our search optimized table. With search optimization on, point lookup queries on all of the columns of this table should be faster. Let's run this and you can see that the query took 233 milliseconds. A noticeable improvement. Even for a fairly small table, search optimization seems to have had a significant impact. Let's now run a range query. Select star from Iowa sales where sales greater than equal to 500.
>
> **[4:43](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/running-point-lookup-queries-on-search-optimized-tables?u=76281980&t=283)** This query on our regular table takes about 1.9 seconds to run. I'm now going to run the same range query on our search optimized table. Remember that search optimization does not help with range queries, will not improve the performance of range queries. So this select star from Iowa sales optimized where sales greater than equal to 500 ran in about 1.8 seconds, a hundred millisecond improvement. But I think that's purely due to chance. You can try this a couple of times with different range queries, but you'll find that there is no improvement to your range query performance due to search optimization.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (2), [[Snowflake]] (1)
> **Env Vars:** iowa_sales (2), json (2), use_cached_result (1), iowa_sales_optimized (1), iowa_sales_optomized (1)
> **Definitions:** is a  (4)
> **Versions:** 0.02 (1), 1.9 (1), 1.8 (1)
> **CLI Commands:** find (2)
> **Tools:** command line (1)
> **UI Navigation:** scroll down (1)
> **Speakers:** - in (1)

#### [Clustering vs. search optimization](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/clustering-vs-search-optimization?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/clustering-vs-search-optimization?u=76281980&t=1)** - [Tutor] Both search optimization and clustering improve the performance of queries that filter on certain predicates. Search optimization improves the performance of selective point lookup queries on all columns of the table, whereas clustering only applies to certain columns and it improves the performance of both point lookup queries, as well as range queries. In this demo, I'm going to run similar queries on a search optimized table and a clustered table, and we can compare the performance head on. Now, the table that we'll work with is a one from the "SNOWFLAKE_SAMPLE_DATA" database. This is the "Orders" table in the TPCH_SF100 schema. And it's a rather large table. This table holds around 4.3 gigabytes of compressed data. Let's take a look at what this table looks like. You can see that there are a number of different columns. O_CUSTKEY uniquely identifies customers for the different orders. I'll now create a new table which I'll then cluster in just a bit and populate that table with the same data from the Orders table. So the new table is called ORDERS_CLUSTERED. So go ahead and run this code and it'll take several seconds. In fact, several minutes for this new table to be set up and populated. We haven't clustered this table. We'll do that in a little bit. Let's create yet another table. This is going to be called ORDERS_SEARCH_OPTIMIZED and we'll populate it with the same data from the original Orders table.
>
> **[1:33](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/clustering-vs-search-optimization?u=76281980&t=93)** Once again, this is going to take a while to run, about one to two minutes depending on how lucky you are. Well, this table has been created as well, took about a minute or so. Let's now cluster the ORDERS_CLUSTERED table and I'll cluster by the O_CUSTKEY. The unique key that identifies customers in this Orders table. So this first table that we created has been clustered. Let's enable search optimization on the ORDERS_SEARCH_OPTIMIZED table. Add search optimization, and in a few seconds, that's done as well. So we now have a clustered table, a search optimized table, and of course, the original table with no clustering or search optimization. Let's take a look at the properties of these two new tables that we've created. ORDERS_CLUSTERED and ORDERS_SEARCH_OPTIMIZED. I'm going to scroll over to the right, and you can see that the first table is clustered by O_CUSTKEY. And if you scroll further over to the right, you can see that the first table has automatic clustering on, that's to be expected. That is our clustered table, after all. If you look a little over to the right, you'll see that search optimization is on for the second table, off for the first table. And the Orders table is a much larger one than any that we've dealt with before. You can see that search optimization progress is currently zero. When I ran this query and got the results, the table wasn't indexed yet. I'm going to rerun this query, show tables like Orders,
>
> **[3:06](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/clustering-vs-search-optimization?u=76281980&t=186)** and let's scroll over to the right. And hopefully by this time, search optimization is complete. It took a few minutes, but search optimization progress is now at a 100%. We've set up our tables. It's time to run our query. The very first query is going to be on the original table with no clustering and no search optimization. It's a selective point look up query where we look up the orders for a particular customer. O_CUSTKEY set to a particular value 11004176. Let's run this query on the original table. And you can see that this query took about one second to execute and return results. To be precise, this query ran in about 948 milliseconds. That seemed pretty fast to me. Let's see if our clustered table can do better. The exact same query, but this time we'll look up a customer on the clustered table, run the query, and well the query duration is just 414 milliseconds. This is a huge improvement. Definitely not a fluke and definitely due to clustering. Let's now run the same query on the search optimized table. CUSTKEY is same as before 11004176. And once again, you'll find that this query runs much faster. It takes about 852 milliseconds. Now usually point lookup queries on search optimized tables are often faster than point lookup queries on clustered tables. In our case, that doesn't seem to be the case, but you will definitely see an improvement over a table
>
> **[4:41](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/clustering-vs-search-optimization?u=76281980&t=281)** that's not clustered or search optimized. I'll now run a range query and we'll see how this range query performs on the three tables that we'll have. We'll first run the range query on the Orders table. We are looking for customers within a particular ID range, greater than 1104176 and less than 13004176. This query runs for a longish bit of time. It can take up to a minute to run. In my case, it took about 21 seconds. We'll run the exact same query, looking for customers in the exact same range on our ORDERS_CLUSTERED table. Now this query will run much faster. You'll see that there is a significant improvement. The query has run about three seconds faster on our clustered table. Clustering definitely improved the performance of this range query. Let's run the same range query on our SEARCH_OPTIMIZED table. Remember search optimization does not help with range queries, only with selective point lookup queries, but even then this particular query runs faster on the search optimized table. It's 19 seconds to execute this query.

> [!info]- Semantic Content
>
> **Env Vars:** o_custkey (4), orders_clustered (4), orders_search_optimized (3), snowflake_sample_data (1), tpch_sf100 (1)
> **Definitions:** is a  (4), is called (1)
> **Prerequisites:** set up (2)
> **CLI Commands:** find (1)
> **Versions:** 4.3 (1)
> **Speakers:** - [tutor] (1)


### 4. Working with Semi-structured Data

[↑ Back to Table of Contents](#table-of-contents)

#### [Loading and querying parquet data](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-and-querying-parquet-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-and-querying-parquet-data?u=76281980&t=0)** - In this demo, we'll see how we can load semi-structured data into [[Snowflake]]. The first kind of semi-structured data that we work with is a parquet file. This file that I have opened here is actually cities dot parquet. You can see that the parquet file has two fields, continent and country and within the country field, that's actually a complex data structure. We have the name of the country and the cities in the country, which is in the form of an array. This is the parquet file that will load into snowflake and query from snowflake. Now, for this demo, we'll go back to working with snow [[SQL]] because we have to load data. So run this command log into your snowflake account. Make sure you specify your password. You'll be logged into snow SQL. Let's create a new database to work with. Create or replace database company underscore DB. We'll use the public schema that is created by default under this database. Let's now create a table named cities and this table has three columns, continent, country and city. Continent and country are both strings. So that's why they have the varchar type. City is a complex object. It's actually an array. And that's why its type is variant. A type of variant means that that particular field can store any kind of data, even complex data types. Let's go ahead and create the cities table. Our data is originally in the parquet format, so let's go ahead and create a file format specification.
>
> **[1:35](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-and-querying-parquet-data?u=76281980&t=95)** Create a file format parquet underscore format type is parquet. The next step is to create a named internal stage into which will load our parquet data. I'm going to call this stage cities underscore stage and the file format that this stage will expect data loaded to be in is the parquet format. Remember a stage is just a location for data. From there we'll then load the data into tables. Let's list our cities stage and you can see that the stage is initially empty. We haven't put anything into the stage yet. We'll do that right now. So I'm going to load the data from my cities dot parquet file. Using the put command into the cities stage. Executing this command successfully loads our data into the cities stage. We confirm that the data is available within the stage by listing out the contents of the stage. And there you see it, our city dot parquet file. Things look good so far. The last step here is to now copy this data into our cities table. Now for other kind of data, this is how we would write the copy into command. Copy into cities. That's the name of our table from the cities dot parquet file file format is the parquet format, on error continue. Well, this will not work with the parquet format because the parquet file format has just one column of type variant, object or array. The cities table, if you remember, had three columns,
>
> **[3:06](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-and-querying-parquet-data?u=76281980&t=186)** continent, country and city, and so the mapping did not work. How do we go from the single column in our parquet file to the three columns that we have in our table? Well, we run a little query on the parquet file. I want to copy into the cities table from the result of this select query and notice the select query. The select query uses dollar one to identify the single column in the parquet file format. We then extract the individual field values from this single column represented by dollar one. Dollar one , colon continent of type varchar. Dollar one, colon country, colon name. That's a nested field of type varchar and dollar one colon country, colon city, a nested field of type variant. And this will actually work. The individual fields from our single parquet column have been extracted and loaded into our table. If you run a select star from cities here is our data continent Europe, country France and city, a number of cities in France. And then we have number of cities in Greece and Canada. Snowflake supports SQL constructs that allow you to flatten this nested array. Let's say you want to view individual cities as separate records rather than as an array as the data originally is in the table. This is a kind of query that you can run. Take a look at the second line of this query,
>
> **[4:38](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-and-querying-parquet-data?u=76281980&t=278)** lateral, flatten, input, city. This basically extracts the individual elements of the array in the city column and represents the individual elements using C. These flattened records of the city column are then associated with the corresponding records from the continent and country columns and we select continent country and C dot value string as city. This will become clearer when you actually look at the result, observe that we have the continent, country and a flattened list of cities where each city is associated with the right continent and the right country.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (5), [[SQL]] (3)
> **Env Vars:** sql (3)
> **Definitions:** is a  (3)
> **CLI Commands:** make (1)
> **Documentation:** specification (1)
> **Speakers:** - in (1)

#### [Loading JSON data](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-json-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-json-data?u=76281980&t=1)** - [Instructor] In this demo, we'll see how we can load [[JSON]] data into [[Snowflake]], and then later on, we'll query the JSON data that we've loaded. Let's take a look at the actual structure of this JSON data. You can see on line one that this data is an array of JSON objects, you can see the start of the array, the square bracket online one. The curly brace line two denotes the start of the first entity in our JSON array. This entity has a single key called restaurant that you can see on line three, and the value associated with this restaurant key, contains a number of details for different restaurants. These are details that have been passed and extracted from a website in India, named Zomato, which is a food delivery service. For every restaurant we have the attributes of the restaurant, on line four you can see whether the restaurant has online delivery or not, on line five we have the URL for photos from the restaurant. On line seven we have the price range, on line 18 we have the name of the restaurant, et cetera. You can see the data gets fairly complex, on line 29 we have the location field, the value for that field is a nested JSON object with address, city, city ID, ZIP code, et cetera. Let's scroll further down. Till finally, we get to the second entity in the array, on line 103, we have the open curly brace for the second restaurant in the array. You can scroll down further,
>
> **[1:32](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-json-data?u=76281980&t=92)** we have information for about 20 or so restaurants, at the very bottom of this file you can see the closing square brackets, on line 1475. Now that we've understood the JSON data that we are going to be working with, let's head over to SnowSQL, and we'll load this data into a new table. So I'm going to create a new table named Zomato restaurants. This table has exactly one column, named JSON data, that's of type variant, that can hold any kind of data. And that's going to hold JSON data in this case. Now this table has been successfully created. Let's now create a stage called Zomato stage, into which we load our data first. I have the JSON file on my local machine, I'm going to use the put operation to load the data from this file into the Zomato stage. Zomato_data.json is the name of the file. The data seems to have been successfully compressed and loaded, it's fairly small data, so this will happen quickly. Let's list the contents of this Zomato stage, and you should see our gzip file in there. The next step is to load this data into a table, into our Zomato restaurants table. Here is our copy into command that will perform bulk loading for batch data, so copying to Zomato restaurants. From Zomato stage and our JSON gzip file within that stage, the file format is type JSON, and I have an additional format specification,
>
> **[3:05](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/loading-json-data?u=76281980&t=185)** strip outer array equal to true. This will get rid of the outer array in our JSON structure, and every restaurant will be loaded in as a separate row, a separate entity. Now that the data has been loaded, the next step is to query this data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (14), [[Snowflake]] (1)
> **Env Vars:** json (13), url (1), zip (1)
> **Definitions:** is a  (2), is an  (1)
> **File Paths:** zomato_data.json (1)
> **Documentation:** specification (1)
> **UI Navigation:** scroll down (1)
> **Speakers:** - [instructor] (1)

#### [Querying JSON data](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-json-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-json-data?u=76281980&t=1)** - [Instructor] We'll head back to the familiar Snow Site interface in order to run our queries. First, let's do a very simple select star from Zomato restaurants to see what this table looks like. You can see from the result that we have exactly one column named [[JSON]] Data. And within this JSON Data column, every entry is a JSON structure representing a single restaurant. You can see the details of this JSON structure once you select a particular record and look at the JSON structure off to the right. It's possible to access individual fields in this JSON structure in your query. Select JSON data colon restaurant will display the values associated with the restaurant field in each entity. And this is what the values look like. We have a field named R a field named API key, average cost for two, et cetera. Select a particular record and expand this panel off to the right. and you can see the details of the object associated with the restaurant key. Let's access some more fields within this restaurant structure. So select JSON data colon restaurant dot name. Name is a nested field within restaurant and user rating is yet another nested field within restaurant. Observe the values associated within the name field across all restaurants and the value associated with the user rating field. The user rating field is a nested JSON object
>
> **[1:32](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-json-data?u=76281980&t=92)** with a number of sub fields. If you expand the panel on the right you can see the sub fields are aggregate rating, rating color, rating text and votes. I'm going to run another query which will extract the subfields within user rating. So select JSON Data colon restaurant.name JSON Data colon restaurant.user rating.rating text. And we'll get two columns in the result with the name of each restaurant and the text associated with the ratings for the restaurant. If you want to give [[Snowflake]] a hint, you can also indicate the data type of the resulting field. Observe my select statement here. Restaurant.name is of type string. So I say restaurant.name::string rating text is also of type string. location, well, location is an object. If you look at the result, the location object contains address, city, latitude, longitude, and a bunch of other details. Let's look at querying and flattening an array type in the JSON format. I select the restaurant's name and then I select the offers associated with the restaurant. And if you look at the offers, you can see that every offer is an a array of string elements that are different offers applied on the same restaurant. You can see that on certain restaurants, there are absolutely no offers. So you have the empty square brackets. Let's select the particular offer, and you can satisfy yourself that this is often array type and array of strings. What if I wanted to access just the first offer within each array?
>
> **[3:04](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/querying-json-data?u=76281980&t=184)** Well, restaurant.offer square brackets zero will gimme the offer at the first position index position zero. And here you can see the restaurant name, all of the first offers at index position zero. If there are no offers, the value is simply null. Instead of viewing a column in the array format, what if I want to flatten the array? We've seen how to use laterally flatten before. On line 26, observe that I apply lateral flatten on input that is JSON data colon restaurant.offers and I represent the flatten elements using the letter O. Every flattened element is then combined with the name of the restaurant. In my select statement, I select the restaurant name O.value. That is the element in my flattened array is of type string. And I alias that as offer. The result has two columns, all of the restaurant names and all of the offers in the flattened format associated with each of these restaurants.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (13), [[Snowflake]] (1)
> **Env Vars:** json (13), api (1)
> **Definitions:** is a  (3), is an  (2)
> **UI Navigation:** select the (4)
> **Speakers:** - [instructor] (1)


### 5. Managing Users and Granting Privileges

[↑ Back to Table of Contents](#table-of-contents)

#### [Access control](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/access-control?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/access-control?u=76281980&t=1)** - In this video, we'll discuss how access control works in [[Snowflake]] and a few best practices and important considerations for managing secure access to your Snowflake accounts and Snowflake objects. Here is the Snowflake object hierarchy. This object hierarchy is something that we are familiar with. All of these are securable objects in Snowflake which means you can grant privileges and specific kinds of access to roles and users for these individual objects. Snowflake uses a role-based access control. Privileges are grouped into roles, and roles are assigned to users. Roles can also be assigned to other roles, setting up a role hierarchy. Now every account in Snowflake has one user who is designated as the account administrator. The account administrator is granted the system-defined account admin role, and this is an all powerful role. The recommendation is to have each account administrator enable multifactor authentication for login. So log in using the username password, and one other factor like an OPP. You do not want all of your users of Snowflake running as account admin. That does not adhere to the principle of least privilege. The recommendation is that you create custom roles that follow the principle of least privilege when assigning privileges to users. A user must have only those privileges that he or she needs to perform their job.
>
> **[1:35](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/access-control?u=76281980&t=95)** No user should be granted additional extraneous privileges. So only allow users access to securable objects that they need, do not give them blanket access. In Snowflake, privileges are grouped into roles. These roles are then assigned to users, which can then perform operations, granted by the privileges associated with roles. Let's understand this, let's say we have role one. And this role owns two objects. Both of these objects assume are either warehouses or [[Databases]], and this role has certain privileges on these objects. Role one may now choose to grant some or all of these privileges to other roles. Let's say they are roles two and three. Once these roles have been created and have been granted privileges, these roles can be assigned to users, users can then log in and perform the operations accorded by these privileges. Once you're working in Snowflake in the real world, you'll create custom roles for different use cases. Here is the custom role workflow. First you'll actually create the role. Next you'll grant the privileges to the role that you wish the role to have. You'll then grant that role to users. You may then grant that role to other roles, or add the role to a role hierarchy. It's common practice in Snowflake to structure your roles as a hierarchy. A role hierarchy is created by having a hierarchical structure of roles
>
> **[3:08](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/access-control?u=76281980&t=188)** with increasing privileges. Usually the account admin role, which is the all powerful role for your account, is at the top of the hierarchy, and has all of the privileges on an account. A role at the bottom of the hierarchy will have the fewest privileges. Privileges associated with the role are inherited by the parent, which are then further inherited by its parent, and so on, up to the top of the hierarchy. Here is what a role hierarchy might look like. Here, the least powerful role is role three, at the bottom of the hierarchy. The most powerful role is role one, at the top of the hierarchy. So role three has just one privilege. Let's call that privilege C. Role two is a more powerful role that inherits from role three. It inherits privilege C from role three, and it has its own additional privilege, privilege B. And finally, at the top of the hierarchy we have role one. Role one inherits privilege B and C from role two, and it has its own privilege A. Role one can now be assigned to a user, user one, who now has privileges A, B and C.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (11), [[Databases]] (1)
> **Definitions:** is an  (1), is a  (1)
> **Env Vars:** opp (1)
> **Speakers:** - in (1)

#### [Creating users and viewing grants](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-users-and-viewing-grants?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-users-and-viewing-grants?u=76281980&t=1)** - In this demo, we'll see how you can create a new user for your [[Snowflake]] Account, assign a role to that user, and grant specific privileges to that role. So that the user that you have created can perform the operations that you want them to. In all of the demos that we've performed so far, our user has been operating under the account admin role. If you look on the top right, you can see that the current role is account admin. This is the most powerful role in the system. The account administrator is responsible for configuring parameters at the account level, but if you're actually using a Snowflake you want to use the principle of least privileged, and give users only those privileges that they need. Let's go ahead and select the current role. This function will give you the current role under which your user is operating, and you can see that is account admin. If you click on the dropdown off to the top right, you'll see that our current role is account admin. Other pre-created roles are org admin, public security, admin, CIS admin, and user admin. Each of these will have slightly different sets of permissions, with the public role being the least permissive. Since we are operating as an account admin the all powerful role we have the privilege to create new users for our account. So I'm going to call the create user command, and create a new user called test user. The password will be test 1, 2, 3, 4. The comment simply says, this is a test user, must change password, is equal to fault.
>
> **[1:33](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-users-and-viewing-grants?u=76281980&t=93)** So the user does not need to change his or her password, when they first log in. In this demo, will be switching between the current user the account admin, and this test user. The way you know the difference, is that the browser for the account admin uses an incognito window. If you take a look at the top, right, I'm currently operating within an incognito window. Once our test user has been created, we will be able to use this test user to log into the same Snowflake Account. I've opened up a new non incognito browser window. Observe the URL that I have on this browser. This is the same Snowflake Account, and I'm now going to log in as the test user. So I specified test user as the username. The password is the test password that I have specified earlier. Test at 1, 2, 3, 4. I'm now going to sign in, and here you can see that I've managed to sign in successfully. Now we've created a new user, but we haven't really assigned the role to that user. So how did this user manage to sign in? Well, if you take a look at the top left, you can see that the test user has been automatically assigned the redefined public role. This role enables login to Snowflake, and basic object access. Typically any user in addition to the public role should be assigned additional roles for additional privileges. There are some basic operations we can perform as the public role. I'm now going to create a new worksheet
>
> **[3:06](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-users-and-viewing-grants?u=76281980&t=186)** and we'll rename this worksheet. All of these operations would not have been possible if we weren't assigned the public role. There is my test user worksheet. Now I'm going to click on this database option, and you can see that the only database that I have access to is Snowflake Sample Data. If you click on the dropdown at the top right, you can see that we are currently operating using the public role. There are no other roles that we are assigned to. You can see that the public role also does not have access to any warehouses. Well, let's run a select current role. This will confirm that we are indeed operating as the public role. If you want to see what grants and privileges the public role actually has, you can run the command show grants to roll public. Executing this command makes it very clear that we have certain privileges on the Snowflake Sample Data Database, and its underlying schemas. And all of these privileges have been granted to us by the Account Administrator, who assign the public role by default. You can scroll down and see what other privileges and grants this particular user has. You can see that all of them are within the Snowflake Sample Data Database, and there's nothing else. For example, if you were to try to create a database here, this would result in an error, because this role does not have database creation permissions. You can also use the Show Grant's Command with a user to understand what privileges are available to a particular user.
>
> **[4:41](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/creating-users-and-viewing-grants?u=76281980&t=281)** So Show Grants on user tells us that the only privilege this user has is the ownership on the user itself. Granted by the Account Administrator.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (8)
> **UI Navigation:** click on (3), dropdown (2), select the (1), scroll down (1)
> **Env Vars:** cis (1), url (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - in (1)

#### [Granting privileges to roles](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/granting-privileges-to-roles?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/granting-privileges-to-roles?u=76281980&t=1)** - [Tutor] Here I am back in the Incognito browser where I'm logged in as account admin. You can notice my role on the top right. I'm now going to create a new role called BASIC_ROLE and grant some simple permissions to test user via that role. Creating a role is just using the create role command. BASIC_ROLE has been created. Next, I'm going to grant this BASIC_ROLE to our test user. At this point in time, there are no grants or privileges associated with BASIC_ROLE but this role will be assigned or granted to test user. Our ultimate aim is to be able to allow test user to run queries on the Iowa sales table. In order to get there, there are a number of privileges we need to grant test user and we'll grant those privileges via the BASIC_ROLE. The first privilege of course is the ability to use the compute warehouse to run queries. On line 12 here, I've granted the usage privilege on the compute warehouse to the BASIC_ROLE. Anyone assigned the BASIC_ROLE will be able to use this warehouse, won't be able to change any of the details of the warehouse. Let's now switch over to our other browser window, where we are logged in as our test user, you can see that we are on the test user worksheet. I'm going to hit refresh on this browser window and let's see if this user now has access to the warehouse. Click on the dropdown.
>
> **[1:32](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/granting-privileges-to-roles?u=76281980&t=92)** Notice that along with the "Public" role, the user has access to the BASIC_ROLE. Switch the user over to using the BASIC_ROLE. And next to that, you can see that the compute warehouse is now available to the BASIC_ROLE. All the test user has right now is access to this warehouse in order to be able to run queries. The user cannot actually access the Iowa sales table. I'll now switch back to the Incognito window where I'm logged in as an account admin and then grant some more privileges to the BASIC_ROLE. The next privilege I grant is the usage privilege on the database SALES_DB. Users assign the BASIC_ROLE, will be able to use this database but will not be able to modify this database in any way. Just a note here that the account administrator has complete ownership of the sales database and the Iowa sales table. And that's why it's able to grant these privileges to another role. And of course you can even select and query the data as an account administrator, and that will work. This is something you're familiar with. Now that our test user has access to SALES_DB via the BASIC_ROLE, let's switch over to our test user window. This is our test user worksheet. Hit refresh to make sure we have all of the latest access and click on the database dropdown. You can see that we have access to SALES_DB. But when we try to select that database,
>
> **[3:06](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/granting-privileges-to-roles?u=76281980&t=186)** observe that we do not have access to the public schema in that database. We only have default access to the information schema containing [[Metadata]] about this database. We want the test user to be able to query the Iowa sales table in the SALES_DB public schema. This means we have to go back and grant some more privileges to the BASIC_ROLE. In the meanwhile, as a test user, you can try to run a "SELECT * FROM IOWA_SALES," and this is going to fail. We are not authorized to query this table. Let's head back to the Incognito window where we are logged in as an account administrator. We need to grant additional privileges to the BASIC_ROLE. Next is the usage privilege on the schema SALES_DB.PUBLIC. In order to query tables within the public schema, we need usage privileges on the schema that we've just granted to the BASIC_ROLE. We also need select privileges on the SALES_DB.PUBLIC.IOWA_SALES table. And this is the privilege that we grant next to BASIC_ROLE. The select privilege allows the user to perform select operations on the table. Updates, inserts, deletes will not be permitted. So let's switch back to our test user account. This is our non Incognito window and rerun this "SELECT * FROM IOWA_SALES." This time we'll be successful and this user can retrieve the data in this table. The BASIC_ROLE under which this user runs
>
> **[4:42](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/granting-privileges-to-roles?u=76281980&t=282)** has all of the privileges needed to run select queries on this table.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Metadata]] (1)
> **Env Vars:** basic_role (18), sales_db (6), iowa_sales (3), select (2), public (2)
> **UI Navigation:** click on (2), dropdown (2)
> **SQL:** select (2)
> **CLI Commands:** make (1)
> **Speakers:** - [tutor] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Summary and next steps](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/summary-and-next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/summary-and-next-steps?u=76281980&t=1)** - [Instructor] And this brings us to the very end of this course. Let's quickly summarize what we've covered so far and let's look ahead at what your next steps might be. We saw that [[Snowflake]] is a managed [[Big Data]] platform that's cloud first and cloud only. We saw that tables in Snowflake can be permanent, temporary, or transient tables, and you pick one based on your use case. We saw that Snowflake allows you to create non-materialized or materialized views. Materialized views perform better and both of these kinds of views can be secure. We saw that queries in Snowflake are optimized using several techniques. We discussed caching, clustering, and search optimization in some detail. We then saw how we could load and query semi-structured data in Snowflake specifically Parquet data and [[JSON]] data. And we rounded this course off by seeing how Snowflake enables access control using rules. This brings us to the end of this course. If you wish to continue your learning, there are some other interesting courses on [[LinkedIn]] Learning. BigQuery is a big data platform very similar to Snowflake. You can watch Learning BigQuery or Advanced BigQuery in order to see how BigQuery works. Another managed data platform for big data processing which is a competitor to Snowflake is Databricks. If you're interested in learning more about Databricks, [[Azure Spark Databricks Essential Training]] is a great course. I hope you enjoyed this course.
>
> **[1:33](https://www.linkedin.com/learning/advanced-snowflake-deep-dive-cloud-data-warehousing-and-analytics/summary-and-next-steps?u=76281980&t=93)** Thank you for listening. It's time for me to say goodbye.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Snowflake]] (8), [[Big Data]] (3), [[JSON]] (1), [[LinkedIn]] (1), [[Microsoft Azure|Azure]] (1)
> **Definitions:** is a  (4)
> **Env Vars:** json (1)
> **Cross-References:** we discussed (1)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [[Janani Ravi]]

## Resources

- Exercise files available

## Skills Covered

- Snowflake

## Path Context

### In [[Master Data Engineering]]
← [[Data Engineering- dbt for SQL]] | **7 of 8** | [[Complete Guide to SQL for Data Engineering- from Beginner to Advanced]] →

## Appears In

- [[Master Data Engineering]]

## Related Courses

_Courses sharing skills:_

- [[Learning SnowflakeDB]] — Snowflake
- [[Advanced Data Engineering with Snowflake]] — Snowflake
- [[Introduction to Modern Data Engineering with Snowflake]] — Snowflake
- [[Building Generative AI Apps to Talk to Your Data]] — Snowflake
- [[Introduction to Gen AI with Snowflake]] — Snowflake

---

[↑ Back to top](#)