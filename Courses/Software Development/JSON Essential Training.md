---
type: course
cssclasses:
  - lle-course
  - lle-level-beginner
slug: json-essential-training
url: "https://www.linkedin.com/learning/json-essential-training"
duration_minutes: 133
duration: 2h 13m
level: Beginner
updated: 4/19/2023
learners: 400716
skills:
  - JSON
exercise_files: true
github: "https://github.com/LinkedInLearning/json-essential-training-2887317"
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQEXmiSIYftM5Q/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1629482233685?e=2147483647&amp;v=beta&amp;t=LbyNWNKqesH6hUgtM8CxYPr8orwhN_B1w6ERy1mpidY"
linkedin_topic: Software Development
learning_paths:
  - '[Explore Web Development with Vanilla JavaScript](../../Paths/Software%20Development/Learning%20Paths/Explore%20Web%20Development%20with%20Vanilla%20JavaScript.md)'
  - '[Become a JavaScript Developer](../../Paths/Network%20and%20System%20Administration/Learning%20Paths/Become%20a%20JavaScript%20Developer.md)'
prev_courses:
  - '[JavaScript- Modern Browser APIs](JavaScript-%20Modern%20Browser%20APIs.md)'
  - '[Developing for Web Performance](../Web%20Development/Developing%20for%20Web%20Performance.md)'
next_courses:
  - '[JavaScript- Ajax and Fetch](JavaScript-%20Ajax%20and%20Fetch.md)'
  - '[JavaScript- Ajax and Fetch](JavaScript-%20Ajax%20and%20Fetch.md)'
path_nav: '[{"path":"Explore Web Development with Vanilla JavaScript","position":4,"total":8,"prev":"JavaScript- Modern Browser APIs","next":"JavaScript- Ajax and Fetch"},{"path":"Become a JavaScript Developer","position":11,"total":13,"prev":"Developing for Web Performance","next":"JavaScript- Ajax and Fetch"}]'
path_count: 2
tags:
  - course
  - topic/software-development
  - topic/web-development
  - topic/network-and-system-administration
  - skill/json
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/JSON%20Essential%20Training.md)

![JSON Essential Training](https://media.licdn.com/dms/image/v2/C560DAQEXmiSIYftM5Q/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1629482233685?e=2147483647&amp;v=beta&amp;t=LbyNWNKqesH6hUgtM8CxYPr8orwhN_B1w6ERy1mpidY)

# JSON Essential Training

> JSON has become the most common format for data interchange on the web, building from a JavaScript base to a wider set of API platforms. In this course, instructor Sasha Vodnik walks you through using the basic syntax of JSON and applying it in several common implementations to see how you can structure it with schemas and JSON-LD. Sasha begins by making sure you have a thorough understanding of h

> [LinkedIn Learning](https://www.linkedin.com/learning/json-essential-training) | 2h 13m | Beginner | 401K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (4 videos)
  - [The essentials of using JSON](#the-essentials-of-using-json)
  - [What you should know](#what-you-should-know)
  - [Set up your environment](#set-up-your-environment)
  - [Work with the GitHub repo](#work-with-the-github-repo)
- [**1. Understanding JSON**](#1-understanding-json) (7 videos)
  - [What is JSON?](#what-is-json)
  - [Understand objects in programming](#understand-objects-in-programming)
  - [Structure JSON by following the rules](#structure-json-by-following-the-rules)
  - [Build a basic JSON structure](#build-a-basic-json-structure)
  - [Build a nested JSON structure](#build-a-nested-json-structure)
  - [Challenge: Debug a JSON structure and create your own](#challenge-debug-a-json-structure-and-create-your-own)
  - [Solution: Debug a JSON structure and create your own](#solution-debug-a-json-structure-and-create-your-own)
- [**2. Processing JSON Data**](#2-processing-json-data) (5 videos)
  - [Process JSON with language tools and libraries](#process-json-with-language-tools-and-libraries)
  - [Convert JSON to an object or array](#convert-json-to-an-object-or-array)
  - [Convert an object or array to JSON](#convert-an-object-or-array-to-json)
  - [Challenge: Process JSON data](#challenge-process-json-data)
  - [Solution: Process JSON data](#solution-process-json-data)
- [**3. Using JSON Data**](#3-using-json-data) (7 videos)
  - [How to send and receive JSON data](#how-to-send-and-receive-json-data)
  - [Create an HTTP request](#create-an-http-request)
  - [Work with an HTTP response](#work-with-an-http-response)
  - [Convert between JSON and other data formats](#convert-between-json-and-other-data-formats)
  - [Make sense of JSON-P](#make-sense-of-json-p)
  - [Challenge: Examine JSON data used by websites](#challenge-examine-json-data-used-by-websites)
  - [Solution: Examine JSON data used by websites](#solution-examine-json-data-used-by-websites)
- [**4. Applying Techniques for Working with JSON**](#4-applying-techniques-for-working-with-json) (3 videos)
  - [Prevent data reuse](#prevent-data-reuse)
  - [Return readable JSON](#return-readable-json)
  - [Test for an empty object](#test-for-an-empty-object)
- [**5. Working with JSON Schema**](#5-working-with-json-schema) (7 videos)
  - [What is JSON Schema?](#what-is-json-schema)
  - [Create a basic schema with JSON Schema](#create-a-basic-schema-with-json-schema)
  - [Validate JSON data against a schema](#validate-json-data-against-a-schema)
  - [Specify required properties with JSON Schema](#specify-required-properties-with-json-schema)
  - [Work with schema generators](#work-with-schema-generators)
  - [Challenge: Work with JSON Schema](#challenge-work-with-json-schema)
  - [Solution: Work with JSON Schema](#solution-work-with-json-schema)
- [**6. Structuring Data with JSON-LD**](#6-structuring-data-with-json-ld) (7 videos)
  - [What is JSON-LD?](#what-is-json-ld)
  - [Implement predefined key names](#implement-predefined-key-names)
  - [Map a data structure with JSON-LD](#map-a-data-structure-with-json-ld)
  - [Work with JSON-LD generators](#work-with-json-ld-generators)
  - [Add a JSON-LD snippet to a web page](#add-a-json-ld-snippet-to-a-web-page)
  - [Challenge: Structure data with JSON-LD](#challenge-structure-data-with-json-ld)
  - [Solution: Structure data with JSON-LD](#solution-structure-data-with-json-ld)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [The essentials of using JSON](https://www.linkedin.com/learning/json-essential-training/the-essentials-of-using-json?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/the-essentials-of-using-json?u=76281980&t=0)** - You use [JSON](../../Skills/Web%20Development/JSON.md) every day. Behind the scenes, it packages up your email, online purchases, and even today's weather forecast. So any digital device that should have access to that information can understand it. At first glance, it can be a challenge to understand the structure of JSON data, and JSON syntax also relies on several rules that can cause issues in your data if you don't understand them. In this course, we'll walk through the basics of JSON syntax and use cases. I'll show you how to build your own JSON structures and introduce you to situations where JSON's used. We'll also explore how JSON's applied to some programming scenarios, including [Data Security](../../Skills/Data%20Science/Data%20Security.md) and API requests. I designed this hands-on course to help you use JSON data to accomplish tasks from software configuration to [search engine optimization](../../Skills/Data%20Science/Search%20Engine%20Optimization%20(SEO).md) to programming. Let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (8), [Data Security](../../Skills/Data%20Science/Data%20Security.md) (1), [Search engine optimization](../../Skills/Data%20Science/Search%20Engine%20Optimization%20(SEO).md) (1)
> **Env Vars:** json (8), api (1)
> **Speakers:** - you (1)

#### [What you should know](https://www.linkedin.com/learning/json-essential-training/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/what-you-should-know?u=76281980&t=0)** - [Instructor] You don't need to have any particular background for this course. We will touch on some basic programming tasks. So, if you'd like to familiarize yourself in advance, check out a course here in the library on programming fundamentals. We'll also be working with some [JavaScript](../../Skills/Software%20Development/JavaScript.md) code in this course. You don't need to have a background in JavaScript to complete the course, but if you'd like to understand the essentials, a basic JavaScript course would be a great place to start.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3)
> **Speakers:** - [instructor] (1)

#### [Set up your environment](https://www.linkedin.com/learning/json-essential-training/set-up-your-environment?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/set-up-your-environment?u=76281980&t=0)** - [Instructor] To complete the videos in this course, you need two applications, a web browser, and a code editor. You undoubtedly already have a web browser installed on your machine. And any major browser, Chrome, Firefox, or [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) edge is fine for this course. I'll be using Firefox in these videos, which includes a powerful suite of developer tools. A number of great code editors are available, both free apps and paid ones. Any editor that lets you edit and save plain text is fine for this course. So if you have a code editor you like such as Sublime Text or Adam, it's fine to use that. I use Visual Studio Code in these videos, which is a version of Microsoft Visual Studio created specifically for [Web Development](../../Topics/Web%20Development.md). Visual Studio Code is free and has [Windows](../../Glossary/Service/Windows.md), Mac, and [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) releases. The code is available on [GitHub](../../Skills/Software%20Development/GitHub.md) and users can submit issues there as well. I've also installed one extension in Visual Studio Code and that's Live Server by Ritwick Day. This is an HTTP server that you can launch with a single click and that automatically opens the current [HTML](../../Skills/Web%20Development/HTML.md) document in your default browser. This makes testing some of our code in the browser quick and easy to do. If you want to learn more about anything I use or talk about in this course, I encourage you to explore the library for a deeper dive on that topic, now let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (2), [Web Development](../../Topics/Web%20Development.md) (1), [Windows](../../Glossary/Service/Windows.md) (1), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Tools:** visual studio (4), firefox (2), sublime (1), github (1)
> **Env Vars:** http (1), html (1)
> **Definitions:** is a  (1), is an  (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Work with the GitHub repo](https://www.linkedin.com/learning/json-essential-training/work-with-the-github-repo?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/work-with-the-github-repo?u=76281980&t=0)** - [Instructor] All of the files for this course are freely available for download on the course page but if you prefer to work with a [Git](../../Skills/Software%20Development/Git.md) repo instead, you can clone the [GitHub](../../Skills/Software%20Development/GitHub.md) repo at this URL. The repository is built so there are branches for each of the videos in the course. You can use the branch pop-up to switch to a specific branch and take a look at how the code looked for a specific video. For example, the branch title 01_04E would show you the code in chapter one and fourth video in that chapter. If you see a letter B at the end of the name, it means that's how the code looked at the beginning of the video. If you see an E at the end of the branch, that's how the code looked at the end of the video. Note that the files for each video in this course include both the begin and end folder, so just by cloning the entire repo, you'll already have the begin and end state for all the videos. So if you want to use branches, you can, but you'll also have access to all the content you need without using the branches. You may need to install [npm](../../Skills/Web%20Development/npm.md), as well as Git, before you clone this repo. If you want to work with this GitHub repo but you need a little more experience with Git and GitHub, do a search for GitHub in our library. If you're comfortable with GitHub and want to go ahead and clone the course, great, and let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [GitHub](../../Skills/Software%20Development/GitHub.md) (5), [Git](../../Skills/Software%20Development/Git.md) (3), [npm](../../Skills/Web%20Development/npm.md) (1)
> **Tools:** github (5)
> **CLI Commands:** git (3), npm (1)
> **Env Vars:** url (1), npm (1)
> **Exercise Files:** github repo (2)
> **UI Navigation:** switch to (1)
> **Definitions:** means that (1)
> **Analogies:** for example (1)


### 1. Understanding JSON

[↑ Back to Table of Contents](#table-of-contents)

#### [What is JSON?](https://www.linkedin.com/learning/json-essential-training/what-is-json?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/what-is-json?u=76281980&t=0)** - [Instructor] [JSON](../../Skills/Web%20Development/JSON.md) is a popular format used for sharing data online. The name is short for [JavaScript](../../Skills/Software%20Development/JavaScript.md) Object Notation. JSON syntax is based on the JavaScript language, but you don't need to know JavaScript in order to use JSON. In fact, one of the reasons JSON is so popular is that you can use it in conjunction with most any programming language. JSON is widely used in a variety of common data exchange situations, including API requests and responses, and application configuration files. Before JSON was created, extensible markup language or XML, was a popular choice, but JSON quickly grew to eclipse XML, especially on the web because of a few key advantages. One of JSON's advantages is that it's pretty easy for humans to read. For instance, this is one way to encode basic information about the country of Thailand in JSON and in XML. Both of these structures contain the same information, country name, capital, and area. But XML contains more extraneous characters and repeats the data label in both opening and closing tags. This not only creates a visual clutter that interferes with human readability, but it also increases the number of characters required to encode the same data. Meaning that XML data generally requires more bytes. This is a critical difference online because fewer bytes means a large file can be downloaded more quickly and browsers can display website content faster. Human readability and file size are two of the reasons that JSON has come to dominate
>
> **[1:35](https://www.linkedin.com/learning/json-essential-training/what-is-json?u=76281980&t=95)** data encoding on the web today. And that makes JSON an important syntax to understand for a wide range of roles that work with data. From computer programming to software configuration, to data analytics and reporting.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (11), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3)
> **Env Vars:** json (11), xml (5), api (1)
> **Definitions:** is a  (2), short for (1)
> **Analogies:** for instance (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)

#### [Understand objects in programming](https://www.linkedin.com/learning/json-essential-training/understand-objects-in-programming?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/understand-objects-in-programming?u=76281980&t=0)** - [Instructor] The acronym [JSON](../../Skills/Web%20Development/JSON.md) stands for [JavaScript](../../Skills/Software%20Development/JavaScript.md) Object Notation. The JavaScript part of that name is pretty straightforward. JavaScript is the programming language whose syntax [Forms](../../Skills/Web%20Development/Forms.md) the basis of JSON syntax, however, you don't need to know the JavaScript programming language in order to use JSON. The N, for Notation, is pretty self-explanatory. Notation is just another way of saying that this is a way of presenting information with its own format and rules. But what is the O mean, Object? In this case, it's not a common sense of object, meaning just a thing. The [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) Object, in JavaScript Object Notation, references a technical meaning of the word object. In JavaScript, like in other programming languages, an object is a collection of data that's similar to a list. An object contains key value pairs where the key is a label that can describe the data, and the value is the data you're storing. For instance, suppose I'm making a list of my favorite foods. My favorite fruit is a mango, my favorite vegetable is an eggplant and my favorite drink is sparkling water. I can make a list and specify a category and then the favorite thing. This is already most of the way there to creating an object in JavaScript. All I have to do is add the right punctuation, so a JavaScript parser can understand the relationship between the items I'm presenting and I have an object. In a JavaScript object the label for a piece of data is known as a key, the data itself is known as the value. You'll often hear data and its label
>
> **[1:33](https://www.linkedin.com/learning/json-essential-training/understand-objects-in-programming?u=76281980&t=93)** referred to as a key value pair. JSON is based on the way objects are written in JavaScript. JSON uses key value pairs and JavaScript object style punctuation, but with a few of its own rules added. Here's the object I created on the left, and here's how I would specify the same data, using JSON, on the right. Almost identical, with a few differences, but it's not hard to see that JSON syntax is modeled on the notation that JavaScript uses for objects. There's another JavaScript data structure that can be represented as JSON, that's an array. An array is a list of values in order. You don't specify keys in an array, just values, and it uses slightly different punctuation, but under the hood in JavaScript, it's considered a type of object. And, in fact, you can represent an array as JSON as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (14), [JSON](../../Skills/Web%20Development/JSON.md) (9), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (2), [Forms](../../Skills/Web%20Development/Forms.md) (1)
> **Env Vars:** json (9)
> **Definitions:** is a  (5), known as (2), stands for (1), is an  (1)
> **Analogies:** similar to (1), for instance (1)
> **CLI Commands:** make (1)
> **Best Practices:** the key is (1)
> **Speakers:** - [instructor] (1)

#### [Structure JSON by following the rules](https://www.linkedin.com/learning/json-essential-training/structure-json-by-following-the-rules?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/structure-json-by-following-the-rules?u=76281980&t=0)** - [Instructor] The basic structure of both [JavaScript](../../Skills/Software%20Development/JavaScript.md) objects and [JSON](../../Skills/Web%20Development/JSON.md) that stores key value pairs starts and ends with a curly brace. For JavaScript arrays and JSON that stores an ordered list of values, the beginning and ending characters are square brackets. The array format has all the same rules as the object format and objects are subject to a few additional rules. So let's compare a JavaScript object and its JSON representation. In JavaScript, the key doesn't require punctuation around it, but it's okay to specify it in single quotes or in double quotes. In JSON, the key must be surrounded in double quotes. So in JavaScript, I don't need quotes at all on the left, but in JSON I do. In both, the key is followed by a colon. A value in a JavaScript object can store different types of data, including strings, numbers, true false values, and even more complex nested structures like arrays and other objects. In JavaScript, a string value must be enclosed in quotes, but those can be single or double quotes or even backticks in an ES6 environment. In JSON, strings must be enclosed in double quotes, no room for personal style here. In both JavaScript and JSON, you use a comma to separate one key value pair and the next. Numerical values don't need quotes around them in either JavaScript or JSON, but JSON has an additional rule for numbers, they can't start with zero. If this truly is a numerical value and the zero doesn't matter, I can simply represent it
>
> **[1:34](https://www.linkedin.com/learning/json-essential-training/structure-json-by-following-the-rules?u=76281980&t=94)** in JSON as a number with no leading zero. But if the zero is significant, I might need to represent this as a string instead enclosing it in quotes and relying on the system receiving this data to work with it in that format. Likewise, a numerical value in JavaScript can end with a decimal point, but in JSON, this is not allowed. Usually this just means that you need to ensure the decimal is removed. Boolean values, which have a value of either true or false, don't need quotes in either JavaScript or JSON. In a JavaScript object, it's common to put a comma at the end of every key value pair, even if it's the last one in the structure. However, JSON does not support trailing commas. So the final key value pair cannot have a comma after it invalid JSON. And so here I have the same number of key value pairs and on the left in the JavaScript object, I have a comma after each final value, but on the right in my JSON object, I have commas only after the first three when they're separating two different key value pairs, but in the final key value pair, I cannot put a comma at the end. Another feature that's supported in JavaScript objects but not in JSON is comments. You can insert a comment to explain or document your code in JavaScript and it's ignored by parsers when the code runs, but JSON does not support any comments syntax, so you can't include comments invalid JSON. In my JavaScript object here on the left, I can include a comment that says legal approval
>
> **[3:08](https://www.linkedin.com/learning/json-essential-training/structure-json-by-following-the-rules?u=76281980&t=188)** and that's just before the approved true key value pair. So in JavaScript I use those two slashes to say, hey, this line's a comment. But in my JSON, I cannot include that comment. So I can only have the data that I want to include as part of the object. There are a few other technical details of JSON syntax that you might need to understand in specific situations. I think the JSON entry on Wikipedia does a great job presenting the technical details in a readable way. For a more technical reference, check out [json.org](https://json.org). This site created by Doug Crockford who originally proposed the JSON standard. And there are a few variations of JSON that have slightly different rules. For instance, JSON-C is a variant that supports comments. If you're working with a system or team that supports a variant, then it's fine to use that. But keep in mind that for broad support with other apps and systems, the JSON standard is the most broadly supported notation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (26), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (17)
> **Env Vars:** json (25), es6 (1)
> **Definitions:** is a  (2), means that (1)
> **URLs:** [json.org](https://json.org) (1)
> **Analogies:** for instance (1)
> **Best Practices:** the key is (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [Build a basic JSON structure](https://www.linkedin.com/learning/json-essential-training/build-a-basic-json-structure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/build-a-basic-json-structure?u=76281980&t=0)** - [Instructor] [JSON](../../Skills/Web%20Development/JSON.md) is just text. So you can write and save JSON in any plain text editor. However, a code editor like. Visual Studio Code can be really helpful when you're working with JSON, because it includes tools that can point out some errors as you type, and it can also try to help you prevent making others. I'm in the highestFeatures.json file. And I want to create a JSON structure that stores the highest point, highest volcano and highest glacier on earth. Each of these needs a key for the label in addition to the value. So I need to use an object structure and object structure starts and ends with curly braces. So I'll start a new one and notice that when I type the opening curly brace, Visual Studio Code automatically adds the closing one. I love this because it makes it harder for me to forget, to close my braces at the end. Another great feature, if I press enter immediately, while my insertion point is still between the opening and closing braces. The editor moves the closing brace down two lines to the start of its own line. And on the line between the braces, it indents for me, indentation makes code much easier to read, especially when you're working with a more complex nested structure. So having that done for me, saves some work. First, I want to add the highest point. My label is point in double quotes. So I'll add that as the key, then I need a colon. And then the value, which is Mount Everest, the editor color code to the key and value different colors,
>
> **[1:34](https://www.linkedin.com/learning/json-essential-training/build-a-basic-json-structure?u=76281980&t=94)** which makes it easier to read the code and understand the meaning of each part at a glance. I have another key value pair after this one. So I'm going to type a comma and notice with that red squiggly line under the comma and the changes to the red names on the left and on the top that the editor is flagging an error. Now I haven't done anything wrong, but the editor is letting me know that if I stop now, I'd have a trailing comma, which JSON doesn't allow, but I'll press enter, and I'll start entering my next key, which is volcano. The value for volcano is Ojos Del Salado. And notice now that that error flag has disappeared because the comma I typed in the previous line is no longer the end of my JSON content. Now I'm going to need another comma. So I'm going to go through that error highlighting again. So we need a comment at the end of my second line, and then one more feature on a new line and that's glacier, and a colon. And the name is Khumbu Glacier. Now its important that I don't add a comma here. It's the last item in my JSON structure. So I can't include a comma after it. I already have my closing curly brace. So my structure is finished and I'll save it. Now that's an object structure, but what if I want to create a JSON array instead? Well, it's a bit different, but still pretty similar, I'll switch to the highest peaks.json file, an array starts and ends with square brackets.
>
> **[3:08](https://www.linkedin.com/learning/json-essential-training/build-a-basic-json-structure?u=76281980&t=188)** So I'll type that opening square bracket. The closing square bracket is auto completed and I'll press enter. when my pointers between the two of those to get an indented new line between them. Remember that I don't specify keys in an array, just values in order separated by commas. So once again, I'm starting with Mount Everest in quotes and then a comma and on the next line, it's K2 and a comma. And then finally Kangchenjunga, and no comma after this one, because it's the final one. And that's my JSON array.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (11)
> **Env Vars:** json (9)
> **File Paths:** highestfeatures.json (1), peaks.json (1)
> **Tools:** visual studio (2)
> **Code Identifiers:** highestfeatures (1)
> **UI Navigation:** switch to (1)
> **Speakers:** - [instructor] (1)

#### [Build a nested JSON structure](https://www.linkedin.com/learning/json-essential-training/build-a-nested-json-structure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/build-a-nested-json-structure?u=76281980&t=0)** - [Instructor] In real world [JSON](../../Skills/Web%20Development/JSON.md) data, it's common to encounter structures with multiple layers. For instance, an array, in which each element is itself an object, this file contains departure estimates for trains in the bay area, rapid transit system in California, the starting curly brace indicates that the overall structure is an object, but many of the keys have additional objects as their values and some even have arrays as their values. For instance, if I scroll down here, the estimate key contains an array with three objects in it. If the structure feels a little overwhelming, that's okay. It can be challenging to make sense of everything going on here. Especially if you're new to JSON. To get a feel for how all the pieces fit. Let's build out our own nested structure together. In the extremes.JSON file, I want to create a JSON object with two keys, highest peaks and lowest points. The value for each key will be an array, listing the highest peaks and lowest points in order. I'll start by creating the basic structure. My overall structure is an object, so I'll start with curly braces and then I want a highest peaks key, and I want the value to be an array. So I'll start some square brackets and I'll press enter. And for now, I'll just leave those square brackets empty. Now I want another key. So I need to put a comma at the end of the value for the first key
>
> **[1:32](https://www.linkedin.com/learning/json-essential-training/build-a-nested-json-structure?u=76281980&t=92)** and that's the end of the closing square bracket. And then on a new line, I want another key called lowest points. And again, I'll give this an empty array as its value. This is my basic structure. My next step is to fill in the next level of data, which is the array value for each key within the square brackets for highest peaks, I'll add Mount Everest. and then K2, and then Kangchenjunga. And then for lowest points, my values are Dead Sea, Lake Assal, and Turpan Pendi. Now I have a nested structure, which is an object containing arrays as values. Nesting gives JSON a lot of power because any value could itself be another object or array. You may hear JSON referred to as a notation for creating structured data and nested arrays and object are at the heart of those structures.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (6)
> **Env Vars:** json (6)
> **Definitions:** is an  (3)
> **Analogies:** for instance (2)
> **File Paths:** extremes.json (1)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **UI Navigation:** scroll down (1)

#### [Challenge: Debug a JSON structure and create your own](https://www.linkedin.com/learning/json-essential-training/challenge-debug-a-json-structure-and-create-your-own?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/challenge-debug-a-json-structure-and-create-your-own?u=76281980&t=0)** - [Instructor] Ready to try your hand at working with [JSON](../../Skills/Web%20Development/JSON.md). It's time for a challenge. My start code is a JSON file. And at first glance into my editor, it's clear, the code has some challenges. The squiggly underlines all indicate places where the editor suspects my code is written incorrectly. Your task is to fix the errors in this code. There's an issue on each line. And although the editor may give you some help, when you hover the mouse pointer over an error, it's not always going to be specific enough to explain what you need to do to fix it. There's an issue on each line. So use this list of basic JSON syntax rules, along with the editor highlighting to figure out what might help, then try it out. Remember that in debugging fixing one error may change how others are identified for this JSON snippet. Try starting on the first line and working your way down. This ensures you're not trying to fix something that's being affected by an error earlier in the code. For instance, I see the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) item flagged in the first key value pair. When I hover over it, I get a pretty helpful message. Property keys must be double quoted. I've got single quotes here, so I'll change those to double quotes. And now the arrow goes away. One down. If you want an extra challenge, add an additional key value pair to this structure with an array as its value. For instance,
>
> **[1:31](https://www.linkedin.com/learning/json-essential-training/challenge-debug-a-json-structure-and-create-your-own?u=76281980&t=91)** you could create a key called list price history, and store a few numerical prices in an array as its value. This exercise should take you about 10 minutes. When you're done, join me in the next video and I'll walk you through how I approached it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (4), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Env Vars:** json (4)
> **Cross-References:** earlier in (1), in the next (1)
> **Analogies:** for instance (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Debug a JSON structure and create your own](https://www.linkedin.com/learning/json-essential-training/solution-debug-a-json-structure-and-create-your-own?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/solution-debug-a-json-structure-and-create-your-own?u=76281980&t=0)** - [Instructor] So in my stock code I have a lot of errors, but remember, I'm going to start from the top and work my way down. So in the previous video, we already looked at this one, but let's just do it again. We've got this error. It says, "Property keys must be double quoted." Great, I can do that. So I'll replace those single quotes with double quotes. And now that first line is all set. So I'll look at the second line. Again, I've got that squiggle and I'll hover over that. It says, "Value expected." That's not very helpful. But if I look at that and think about my rules and remember that, you know, this is supposed to be a string and strings are supposed to be double quoted as well. And this uses single quotes. So I'll change those single quotes to double quotes. And this is an instance where there was an error and the editor wasn't actually very helpful in telling us what was wrong. It just didn't see that as an actual value, but now we've got that one fixed. So on the next line, if I hover over this issue, "Unexpected end of number." So it's pointing me to the end of the number, seeing there's something weird. And if you refer back to the list of rules, one of the rules is that numbers can't end with a decimal point. So I can just take the decimal out here. Doesn't change the value. It's still 84 and now that error goes away. So moving on line five, we got the error in the value and this says, "Expected comma."
>
> **[1:31](https://www.linkedin.com/learning/json-essential-training/solution-debug-a-json-structure-and-create-your-own?u=76281980&t=91)** Now that's not super helpful. So let's think about this. This is a number and we already corrected an error with a decimal, but there's one other rule in [JSON](../../Skills/Web%20Development/JSON.md) about numbers. And the other rule is that they can't start with a zero, and this one does. So I'm going to go ahead and take that zero off the beginning, called a leading zero in this case. Again, it doesn't change the value. It's still a number. It's still 99.95 and the error goes away. Then in line six, you may be able to spot this one, even before I hover over it. This is one that we saw in earlier videos. But if I check out, it just says, "Trailing comma." So right there, the editor is telling me exactly what's up. It's the last value. So I can't have a trailing comma on it. And now that error goes away. Now I still have one error on the last line. And again, thinking back to the list of rules, you may notice right away what's up here, it's the whole line. And let's see what the editor has to say, exactly that, "Comments are not permitted in JSON." Now I added this here as a reminder that if you wanted to do the challenge work, this would be a place to do it. But first we have to actually take out that comment. And so here we go. This is that code, that JSON content with all of those errors fixed. Now, if I want to go ahead and do that challenge, the challenge was to add a key value pair. That's actually a nested structure. And so to do that, first, I have to add a new key. So I'm going to add a comma after my last existing key. And then my new key,
>
> **[3:06](https://www.linkedin.com/learning/json-essential-training/solution-debug-a-json-structure-and-create-your-own?u=76281980&t=186)** I'm going to call that listPriceHistory, and I'm going to make that value an array in this case. It doesn't matter what, you can put objects within objects. You can put arrays within arrays. Here, I've got an object structure and I'm going to put a nested array within it. So square bracket and press Enter. And then I just want to add a few numerical values for my listPriceHistory. And I'm just making these up. So we'll say, 94.95 and then a comma, and 85.00 and a comma, and 124.95. I'll save that. And so now I have this array, the listPriceHistory array, which contains three values. And that is nested within this overall stock.json structure, which is an object.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (4)
> **Versions:** 99.95 (1), 94.95 (1), 85.00 (1), 124.95 (1)
> **Code Identifiers:** listpricehistory (3)
> **Env Vars:** json (3)
> **Definitions:** is an  (2), is a  (1)
> **File Paths:** stock.json (1)
> **CLI Commands:** make (1)
> **Cross-References:** previous video (1)


### 2. Processing JSON Data

[↑ Back to Table of Contents](#table-of-contents)

#### [Process JSON with language tools and libraries](https://www.linkedin.com/learning/json-essential-training/process-json-with-language-tools-and-libraries?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/process-json-with-language-tools-and-libraries?u=76281980&t=0)** - [Instructor] [JSON](../../Skills/Web%20Development/JSON.md) data is generally shared as a string of characters, but JSON is useful as a notation because it can describe a structure, either an object or an array. So how does a programming language work with a JSON string, and how does it create a JSON string from an object or array? Programming languages need to convert JSON into a structure to use the data, and they need to convert structures from the language into JSON in order to share the data. You may see the terms serialize and deserialize used in reference to working with JSON. Serializing JSON means turning a structure into a JSON string, while deserializing is the reverse, turning a JSON string into a structure that a programming language can work with. Remember that even though JSON is based on the object notation used in [JavaScript](../../Skills/Software%20Development/JavaScript.md), JSON can be used across languages and platforms. Some languages like JavaScript and [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) include utilities right in the language that serialize and deserialize JSON. In other languages like [Java](../../Skills/Software%20Development/Java.md), you need to augment the basic features of the language with a library to manipulate JSON, Whatever language you're working in, you can check documentation or other resources for best practices on working with JSON data most efficiently.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (15), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Env Vars:** json (15)
> **CLI Commands:** python (1)
> **Speakers:** - [instructor] (1)

#### [Convert JSON to an object or array](https://www.linkedin.com/learning/json-essential-training/convert-json-to-an-object-or-array?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/convert-json-to-an-object-or-array?u=76281980&t=0)** - [Instructor] Converting a [JSON](../../Skills/Web%20Development/JSON.md) string to a data structure is pretty straightforward. I'm going to show you what that looks like in [JavaScript](../../Skills/Software%20Development/JavaScript.md) using the built-in JSON.parse method. But in general, no matter what language you're working with, the process of deserializing data involves passing the JSON string to the utility method, which returns the data structure. Remember that JSON can encode either an object or an array, but in JavaScript, as long as your JSON is valid and uses the notation correctly, the utility will understand what data structure is encoded and will return that. You don't need to identify what to expect beforehand. The H+ store populates page content using data from a backend database. This data is provided to the frontend as it JSON string. The items.js file contains a snippet of that JSON string, containing data about two [products](../../Skills/Software%20Development/Microsoft%20Products.md). I'm going to use the JavaScript JSON.parse method to convert this to a data structure. The product variable contains my data. On a live website, this would be the result of an HTTP request using something like [Fetch](../../Skills/Web%20Development/Fetch.md), but for exploration, I've hard coded this in a file. I'll start by creating a new variable called productsParsed. So, that's const productsParsed, and then it needed an equal sign, and I'll set the value to the result of calling JSON.parse, and passing the JSON string in the products variable. So, that's JSON, all caps, dot parse, and then in parens, products. And that's a reference to the product variable
>
> **[1:32](https://www.linkedin.com/learning/json-essential-training/convert-json-to-an-object-or-array?u=76281980&t=92)** above on line one. I'll save those changes, and then it needs to open up products.[HTML](../../Skills/Web%20Development/HTML.md), and I'm going to use my Go Live function for my web server to open that up in Firefox. And then I need to open my browser console. And one way to do that is just to right click and click Inspect. That's going to open up the console. You may see it in a different part of your screen, but that's no big deal. And then just click the Console tab, and you'll have this no matter what browser you're using. And so I can now type the names of variables that are used in my code for this page. And I can see their value here in the console. So, I'm going to start by typing productsParsed and press Enter, and there's the array structure that's no longer a string. I can use this gray arrow to open it up, and I can see, I have a couple objects in there. I can even use those gray arrows, open those up and see all those key value pairs. Now, for comparison, I can also view the original product string here. So, I'll just type product on a new line and press Enter. Oh, no, it's products with an s. And there's the unparsed string. In some browsers, you might instead get a parse data structure here. And in that case, the browser developer tools are doing some extra work behind the scenes and automatically showing you the parsed version of that JSON string. If you wanted to see the raw JSON in a browser that was doing that, you'd need to check the settings for the browser and turn off that option, that parses JSON automatically. But here I can see that that original variable is just a string.
>
> **[3:04](https://www.linkedin.com/learning/json-essential-training/convert-json-to-an-object-or-array?u=76281980&t=184)** I don't have any sort of structure here at all in the console. It's just treating it as a string. So, for this web page, that original products variable is a string and the productsParsed variable that I just created using JSON.parse actually has a data structure that JavaScript can see and work with. In JavaScript, JSON.parse works exactly the same if the JSON data structure is an object. So, back here in my editor, in items.js, I have the item variable down here on line six. This is a JSON string that contains an object. So, down here on line nine, I'm going to create another new variable, and use const and call that itemParsed, and use an equal sign to set the value, and it's going to be JSON.parse, and I'm going to pass in the item variable as the argument, and end with a semi-colon, and I'll save those changes. And then I'll go back to the browser, and in the console, don't need to worry about this particular issue. But instead, I want to look at the variable I just created. So, I'm going to type itemParsed, there it is. And if I press Enter, I've got an object this time, not an array. If I click that gray arrow, I can see the structure and the object has four key value pairs. So, JSON.parse can deal with JSON data no matter which structure it's using. And now that I have my data parsed, I can work with it. So, I can reference properties of this object for instance, using dot notation. For instance, this itemParsed object has a name property.
>
> **[4:40](https://www.linkedin.com/learning/json-essential-training/convert-json-to-an-object-or-array?u=76281980&t=280)** So, I can enter itemParsed.name, and press Enter, and the result is a string, which is the value of the name property of the itemParsed object. So, once the data is parsed, I can do anything I need to do with it in my application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (21), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (6), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (5), [Fetch](../../Skills/Web%20Development/Fetch.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** json (21), http (1)
> **Code Identifiers:** itemparsed (5), productsparsed (4)
> **Definitions:** is a  (3), is an  (1)
> **File Paths:** items.js (2), products.html (1)
> **Analogies:** for instance (2)
> **Cross-References:** go back to (1)
> **Tools:** firefox (1)

#### [Convert an object or array to JSON](https://www.linkedin.com/learning/json-essential-training/convert-an-object-or-array-to-json?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/convert-an-object-or-array-to-json?u=76281980&t=0)** - [Instructor] When you want to transmit data, especially on the web, you generally need to convert it to [JSON](../../Skills/Web%20Development/JSON.md). There are other formats in use. So depending on the system you're sending data to, you may instead need to convert to one of them. Other popular notations for [Data Sharing](../../Skills/Database%20Management/Data%20Sharing.md) include YAML, which stands for yet another markup language and XML or extensible markup language. JSON is common, but by no means universal. So if you find you need to learn how to work with a different notation, check out a course here in the library to help you get started. But JSON is by far the most popular on the web. And whether you have an object or an array that you want a stringify into JSON, the process is pretty straightforward in [JavaScript](../../Skills/Software%20Development/JavaScript.md). The built-in method is pretty self-explanatory, JSON.stringify. In JavaScript or any other language, the process simply involves passing the structure to the utility method, which returns a JSON string. When a user adds items to a shopping cart on the H plus web store and wants to order the frontend needs to stringify the shopping cart object and transmit it to the backend for processing. The cart.js file contains an array with sample shopping cart data. I'm going to use the JavaScript JSON.stringify method to convert this to a JSON string. The cart variable contains my array. On a live website, the object may be stored in a cookie or local storage, but for exploration, I've hard-coded an object in a file. I'll create a new variable down here on line 25, and I'll call that cartJson. And the value is going to be the result of
>
> **[1:33](https://www.linkedin.com/learning/json-essential-training/convert-an-object-or-array-to-json?u=76281980&t=93)** calling JSON.stringify and passing the cart array in the cart variable. I'll save those changes and then open up [products](../../Skills/Software%20Development/Microsoft%20Products.md).[HTML](../../Skills/Web%20Development/HTML.md) and use my web server to check that out in my browser. And I have the browser console open. And so at the prompt I can type cartJson and press enter. And I see the string. Now in some browsers, you may instead see a parsed version of the array here. It's something that browser tools are doing to help you out. So you can go into your settings and try and change that if you want to actually see the string here, but you can also just see on your screen here, that cartJson, the results of the command, we just typed actually produces that JSON string. And if I look at cart instead, I can see that that's displayed as an array. So I definitely changed the structure of that content. JSON.stringify works exactly the same with an object. And to try that out, the cart.js file includes a variable called single down here on line 27, which is an object and contains information on a single product. So down on line 39, I'll create a new variable const and I'll call that singleJson equals and I'll use JSON.stringify and in parens I'll pass in the single variable and end with a semi-colon and I'll save my work there. And back in my browser console I'll type singleJson. And I can see here
>
> **[3:10](https://www.linkedin.com/learning/json-essential-training/convert-an-object-or-array-to-json?u=76281980&t=190)** that I have a JSON string containing that object. So no difference in using JSON.stringify on an array versus using it with an object. Now that I have stringified JSON, I can include it in an HTTP request to the backend and keep the checkout process going for the user. That's super common on the web and JSON's strings are indispensable in exchanging data between browsers and servers and throughout the infrastructure of the internet.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (16), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3), [Data Sharing](../../Skills/Database%20Management/Data%20Sharing.md) (1), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** json (16), yaml (1), xml (1), http (1)
> **Code Identifiers:** cartjson (3), singlejson (2)
> **File Paths:** cart.js (2), products.html (1)
> **Definitions:** stands for (1), is an  (1)
> **CLI Commands:** find (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Process JSON data](https://www.linkedin.com/learning/json-essential-training/challenge-process-json-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/challenge-process-json-data?u=76281980&t=0)** (light music)
>
> **[0:08](https://www.linkedin.com/learning/json-essential-training/challenge-process-json-data?u=76281980&t=8)** - [Instructor] Ready to write code to process [JSON](../../Skills/Web%20Development/JSON.md) data? It's time for a challenge, in this challenge, you'll write [JavaScript](../../Skills/Software%20Development/JavaScript.md) to destringify an object and an array. And you'll also write JavaScript to stringify an object and an array. My start code includes two JavaScript files, blog.js includes a posts, variable containing an array. It also contains an article value containing an object. Your challenge for this file is to write JavaScript code that converts each value to a JSON string. The other JavaScript file, issue.js contains a cue variable containing a JSON string that encodes an array. It also contains an item variable that contains a JSON string that encodes an object, for this file, your challenge is to write JavaScript code that destringifies each value into a data structure that you can work with in JavaScript. This exercise should take you about five minutes. When you're done join me in the next video, and I'll walk you through how I approached it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (7), [JSON](../../Skills/Web%20Development/JSON.md) (4)
> **Env Vars:** json (4)
> **File Paths:** blog.js (1), issue.js (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (light music) (1)

#### [Solution: Process JSON data](https://www.linkedin.com/learning/json-essential-training/solution-process-json-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/solution-process-json-data?u=76281980&t=0)** - [Instructor] My first step is to stringify the posts array. So I have this array, it contains objects and down here on line 17, I'm going to write code that stringifies it. So I'm going to create a variable first and I'm going to make that postsJson, and the value is going to be using [JSON](../../Skills/Web%20Development/JSON.md).stringify and passing it hosts. And so that is going to stringify my posts array. Now, I also want to stringify the article object, and again, it's pretty much the same code. I just need to use a different variable name. So I'm going to use const articleJson, and I'm going to use JSON.stringify, and I'm going to pass it article. So let me go ahead and check just to make sure these worked, I'm going to open up my [HTML](../../Skills/Web%20Development/HTML.md) file for this blog.html, going to open that with my web server. So I've got the page loaded and down in my console, I can check postsJson and I indeed have stringified version of that variable. And I can check articleJson, and I've got a stringified version of that object as well. So that code worked. And the second part here is in the issue.js file.
>
> **[1:36](https://www.linkedin.com/learning/json-essential-training/solution-process-json-data?u=76281980&t=96)** And I just want to do the reverse. I have two variables here and I want to destringify them. I want to parse them so on line 4 I'm going to go ahead and write code that parses the queue variable in line one. And so I'm going to create a const called queueData. And that's going to be JSON.parse, and then I'm going to pass it the variable name queue. And likewise below I have a variable on line six, which is a JSON string containing an object. You can see that curly braces, the first character inside the quote. And so I'm going to create another const called item data on line nine, and the value's going to be JSON.parse, and I'm going to pass it item. I'm going to save that. And then back in my browser, I want to check queueData. And I have an actual array. The browser recognizes this as a data structure, so that's great. It's no longer a JSON string And I want to check item data. That is an object that's recognized by the browser as a data structure rather than JSON string. So it's just using JSON.parse or JSON.stringify to go back and forth between JSON and [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) in [JavaScript](../../Skills/Software%20Development/JavaScript.md). And again, if you're using a different programming language, you're going to want to get familiar with the tools that do the same thing in that programming language.
>
> **[3:09](https://www.linkedin.com/learning/json-essential-training/solution-process-json-data?u=76281980&t=189)** But generally these tools are available and you can easily convert data to JSON and JSON, back to data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (12), [HTML](../../Skills/Web%20Development/HTML.md) (2), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Env Vars:** json (12), html (1)
> **Code Identifiers:** postsjson (2), articlejson (2), queuedata (2)
> **File Paths:** blog.html (1), issue.js (1)
> **CLI Commands:** make (2)
> **Definitions:** is a  (1), is an  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 3. Using JSON Data

[↑ Back to Table of Contents](#table-of-contents)

#### [How to send and receive JSON data](https://www.linkedin.com/learning/json-essential-training/how-to-send-and-receive-json-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/how-to-send-and-receive-json-data?u=76281980&t=0)** - [Instructor] A common use of [JSON](../../Skills/Web%20Development/JSON.md) data is on websites. Many websites you interact with regularly receive and send JSON data in the background in response to your interactions. For instance, as I type a query into the search box, the webpage is sending what I've typed to the back end and receiving back JSON containing possible terms I might be searching for. Then the page displays those possibilities in the menu below the box, which enable me to quickly select my full query. Website data is exchanged using the Hypertext Transfer Protocol, or HTTP for short. When a webpage sends data to the back end, it creates an HTTP request that specifies the web address where it's sending the request, provides details about the request format, and includes data, usually in JSON format. When the receiving system has processed the request and has an answer, it sends a response, which can include its own data. These requests and responses happen all the time for a lot of websites. When I place an order online and get a confirmation message containing my order number, that's generally data that just arrived from the back end system in JSON format. I can view these requests and responses in my browser using the developer tools that are built into all major desktop browsers. I'm using Firefox, but these are available in Chrome and Edge as well. Probably the easiest way to open these if you don't use them all the time is to right click anywhere on the web page and select Inspect. That's going to open up the developer tools.
>
> **[1:34](https://www.linkedin.com/learning/json-essential-training/how-to-send-and-receive-json-data?u=76281980&t=94)** And once I'm there, I want to click Network. That's going to let me look at network requests. After I load a page, all the HTTP requests and responses for the information on that page will be displayed here. I'm going to open [nps.gov](https://nps.gov), (quick typing) which is the webpage for the U.S. National Park Service. Then I'm going to open the menu and I'm going to choose the Events page. Now there are a lot of HTTP requests for this page, but I can filter those a little to avoid getting overwhelmed. HTTP is used for a lot of things on the web, but I'm most interested in XHR requests, which are a common request that returns JSON. In Firefox, I can click the XHR button right here and now only XHR requests are displayed. And there are a few, but the one I'm most interested in is the Event Calendar Service, which is here. Because calendars are updated regularly, it's common for a calendar page to load events as JSON and then add them to the webpage. So I want to check out if that's what's going on here. So I'll click that line in the network list and then over on the right, I'm going to click Response. And I can see right there the data is labeled as JSON. I see the structure, which is an array containing multiple objects, and below that, there's a bunch of data for each object. And each object here is an individual event in the calendar. I can scroll down the webpage and match up the event info
>
> **[3:09](https://www.linkedin.com/learning/json-essential-training/how-to-send-and-receive-json-data?u=76281980&t=189)** with the data I see down in the JSON. These HTTP requests and responses happen all over the web and a lot of the content that you interact with online everyday was provided in an HTTP response as JSON data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (10)
> **Env Vars:** json (10), http (7), xhr (3)
> **UI Navigation:** in the menu (1), open the (1), scroll down (1)
> **Tools:** firefox (2)
> **Definitions:** is an  (2)
> **URLs:** [nps.gov](https://nps.gov) (1)
> **Analogies:** for instance (1)
> **Speakers:** - [instructor] (1)

#### [Create an HTTP request](https://www.linkedin.com/learning/json-essential-training/create-an-http-request?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/create-an-http-request?u=76281980&t=0)** - [Instructor] To get [JSON](../../Skills/Web%20Development/JSON.md) data from a remote source on the web starts with an HTTP request. We're going to work with the h+ sport website, which is a site for a fictitious company that we're using just for this course, the h+ website displays a number of [products](../../Skills/Software%20Development/Microsoft%20Products.md) that the company sells with a name, image, and price for each one. All of this product data on the page is a result of an HTTP request for product data and an HTTP response that contains that product data. To make a request for data, I need to send an HTTP request to a specific web address called an end point. An end point has a program running that receives HTTP requests and generates HTTP responses back. For example, these are the URIs of two end points for the fictitious h+ sport company. These may look like web page URLs, but instead of returning an [HTML](../../Skills/Web%20Development/HTML.md) document to be displayed in your browser, they return JSON data. Now building an HTTP request in a programming language can be a little complex and it's beyond the scope of what we're doing here, but we can get a taste of what goes into an HTTP request and how it works without writing code by simply entering the URL for an endpoint in a web browser and seeing what data we get back. That means that the browser is generating the HTTP request on our behalf. The h+ site supports a product endpoint that can return JSON containing data on h+ products. If I want data on all h+ products, all I have to do is send an HTTP request to this URL. So in my browser address bar,
>
> **[1:35](https://www.linkedin.com/learning/json-essential-training/create-an-http-request?u=76281980&t=95)** I'm going to go ahead and enter that URL. And when I press enter my browser displays the data I get back, which is not a webpage, it's just JSON data. Now Firefox automatically renders this. But the data that came in is actually JSON. Unformatted one long string, but it's much easier to read and understand when the browser automatically parses it for us. And so that's what I'm doing with these buttons up here. We've got the raw data button that shows me the raw JSON, there's a headers button that lets me see some components of the HTTP, but the JSON is really what's useful in understanding what kind of content is here. Keep in mind that although I'm using the browser to view this response data, requests to endpoints for JSON data are actually handled behind the scenes by webpage code. So you don't usually see the raw JSON in the browser window, but once that JSON comes in, the webpage code can work with the data it's received and format it to display it for users like the h+ sport page is doing here.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (10), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (3), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** http (11), json (10), url (3), html (1)
> **Definitions:** is a  (2), means that (1)
> **CLI Commands:** make (1)
> **Tools:** firefox (1)
> **Analogies:** for example (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [Work with an HTTP response](https://www.linkedin.com/learning/json-essential-training/work-with-an-http-response?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/work-with-an-http-response?u=76281980&t=0)** - [Instructor] After a program receives [JSON](../../Skills/Web%20Development/JSON.md) data, it puts it to work, depending on what the program is designed to do, there are all sorts of things it can do with the data such as add it to a database, check it for errors or format it and display it. To get a taste of the process, let's look at a web based example. The start files for this video include web page files for the H Plus Fitness Company. If I open up [products](../../Skills/Software%20Development/Microsoft%20Products.md).[HTML](../../Skills/Web%20Development/HTML.md) and use my web server or view that in the browser, I can see that the structure of the page is pretty bare bones, just the heading, but the page is supposed to display images and information about available products that it retrieves in the server as JSON. The script.js file is set up to fill in the product information. It begins with a variable called raw data, which is one long string of JSON data containing information on several products, in everyday code, this would instead use an actual server request that returns JSON data, but for our purposes, we'll assume that's done and the data has been fetched and stored in this variable. Further on down, the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the file contains code that takes data for each item and adds it to the webpage. The only step that's missing is to parse the JSON into a format that [JavaScript](../../Skills/Software%20Development/JavaScript.md) can work with. JavaScript uses the JSON.parse method to convert a JSON string to a JavaScript object or array. My script.js file includes a comment pointing out where in the file that JSON data needs to be parsed and stored. And that's right after lines three and four, I just need to add a single statement, so on line five,
>
> **[1:33](https://www.linkedin.com/learning/json-essential-training/work-with-an-http-response?u=76281980&t=93)** I'm going to enter const to make a variable and call it data. And I'm going to set its value as JSON.parse and then rawData, which is the variable name of the raw data at the top of the file. The result of parsing that raw data is stored in this data variable. And that variable name is used in the existing code that follows to retrieve information about each product and add it to the web page. So I'll save my work and I'll go back to my browser. And now I see all the products displayed based on the product information that was in the JSON string in my JavaScript file. So we got raw JSON data and then use the utility that's specific to this programming language, JavaScript to convert the data to a format that JavaScript can work with. And then we use the data to do what the program was written for, displaying the data to end users in the web browser window.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (11), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (6), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (4), [HTML](../../Skills/Web%20Development/HTML.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Env Vars:** json (11)
> **File Paths:** script.js (2), products.html (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** rawdata (1)
> **Cross-References:** go back to (1)
> **Analogies:** such as (1)
> **Prerequisites:** set up (1)

#### [Convert between JSON and other data formats](https://www.linkedin.com/learning/json-essential-training/convert-between-json-and-other-data-formats?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/convert-between-json-and-other-data-formats?u=76281980&t=0)** - [Instructor] [JSON](../../Skills/Web%20Development/JSON.md) is not the only data format in wide use, and sometimes, when a program requests data, it's only available in a format other than JSON. Fortunately, converting is pretty straightforward in many situations. There are many data formats available, but aside from JSON, the most common include XML, YAML and CSV. XML stands for extensible markup language. XML is related to [HTML](../../Skills/Web%20Development/HTML.md), which is the markup language used to structure webpages. XML can be customized in ways that HTML can't and it's been widely used for data exchange since before JSON existed. YAML stands for yet another markup language. You got to love it when the name of a language to structure data can make you laugh. YAML was created with the goal of making structured data easy to write and it has fewer rules than other [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) like XML. CSV stands for comma separated values. It's literally characters separated by commas. CSV is often used for exchanging lists or tables. When a data source doesn't offer JSON, but a program expects it, you need to convert it from one format to another. To do this, you commonly use a library, which is a set of computer code that a program can call on to perform specific tasks. Different programming languages use different libraries for data conversion, and often, many libraries are available to choose from. For instance, in [JavaScript](../../Skills/Software%20Development/JavaScript.md), you can use this XML to JSON utility. Now this is an older utility, and on the modern web,
>
> **[1:32](https://www.linkedin.com/learning/json-essential-training/convert-between-json-and-other-data-formats?u=76281980&t=92)** a lot of frameworks are in use that have this conversion capability built in. But for learning purposes, an older conversion tool like this is just the ticket. In my script.js file for this chapter, I have XML data stored in my raw data variable. This would generally come in dynamically by making an HTTP request, but for our purposes, I've included it to make the code less complex. Instead of simply needing to parse the data like I could do with JSON, I first need to convert it. I've saved the XML to JSON.js library file, and I've referenced it within my webpage so I can use its utilities. This is a lot of data. And then, eventually, we get to line two, and here's where I need to add some things. I first need to convert the XML string to a JavaScript structure called a DOM node. This is an intermediate step before I converted. So I'm doing a var peopleDataNode, and that's going to be new DOMParser.parsefromstring rawData text/xml.
>
> **[2:42](https://www.linkedin.com/learning/json-essential-training/convert-between-json-and-other-data-formats?u=76281980&t=162)** Now I've used the documentation for this tool to understand how to write code that uses it. The next thing I need to do to use that tool is actually convert the XML in that DOM node to JSON. So I'm going to create another variable called converted data, and that's going to call XML to JSON and pass it peopleDataNode, and then a comma and two sets of double quotes. Next, I can parse the JSON data for use in JavaScript. So I'm going to create a const called convertedObject, and that's going to be JSON.parse, and I'm going to grab that converted data. Now XML data uses an enclosing structure. So I need to dig into that structure to get the data I want, which is an array. I've looked at the structure, and I know how to reference that array, so I'll add one more statement to reference that array and save the reference with the variable name data. So that'll be const data, and that's converted object.data.product. So I'll save those changes. Then I'm going to open up [products](../../Skills/Software%20Development/Microsoft%20Products.md).html and use my web server to open that up in the browser. And now my product data is populated. That data came in as XML, but I used my library to convert it to JSON, which my program requires in order to display the data on the page. Data conversion sometimes brings challenges.
>
> **[4:16](https://www.linkedin.com/learning/json-essential-training/convert-between-json-and-other-data-formats?u=76281980&t=256)** If data picks up something as simple as an extra space while being passed from one system to another or it's formatted in a way that the receiving system doesn't expect, then the conversion process may require some extra troubleshooting. So it's always best to request data in the format you need if that's available, but it's not always possible to get data in the format your program uses, and converting from one format to another is a standard part of working with data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (13), [HTML](../../Skills/Web%20Development/HTML.md) (3), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1)
> **Env Vars:** json (13), xml (13), yaml (3), csv (3), html (2)
> **Definitions:** stands for (3), is a  (3), is an  (3)
> **CLI Commands:** make (2), node (2)
> **Code Identifiers:** peopledatanode (2), rawdata (1), convertedobject (1)
> **File Paths:** script.js (1), json.js (1), products.html (1)
> **Documentation:** the documentation (1)
> **Analogies:** for instance (1)

#### [Make sense of JSON-P](https://www.linkedin.com/learning/json-essential-training/make-sense-of-json-p?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/make-sense-of-json-p?u=76281980&t=0)** - [Instructor] As you start working with [JSON](../../Skills/Web%20Development/JSON.md), you'll likely start to encounter other terms with the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) JSON in them. That's because JSON is used as a foundation for a few other technologies that rely on structured data. One [Web Development](../../Topics/Web%20Development.md) approach you may encounter is JSON-P, JSON-P stands for JSON with padding. JSON-P is a technique for fetching data for a webpage that's intended to work around some security limits that web browsers impose on web applications. One aspect of web security is based on origins, which you can think of as the web addresses associated with particular web apps. Web browsers prevent scripts from making requests to other origins. This is a same origin policy, and it's intended to prevent malware from injecting a request to another domain that accesses a user' sensitive information, it's a useful limitation. And on the modern [Web Servers](../../Skills/Web%20Development/Web%20Servers.md) have many configuration options that enable them to work with selected other domains they trust. But earlier in the web's evolution, this same origin policy was a blunt tool. JSON-P was developed as a creative workaround. As part of the site's own code a statement is included that adds a script element to the page. Adding elements is totally allowed, even script elements, but the script element is going to load a script that returns data and the data is formatted in such a way that the browser makes it accessible to the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the application. It's a neat trick and some web services continue to support JSON-P, but JSON-P is not as secure as a standard request for data. And there are ways to accomplish everything that JSON-P can
>
> **[1:34](https://www.linkedin.com/learning/json-essential-training/make-sense-of-json-p?u=76281980&t=94)** do while still maintaining security over all the data being transmitted. The most common approach is CORS which includes using the access control allow origin header within your HTTP requests and responses. For this reason it's best to you use these alternatives rather than relying on JSON-P.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (12), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [Web Development](../../Topics/Web%20Development.md) (1), [Web Servers](../../Skills/Web%20Development/Web%20Servers.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Env Vars:** json (12), cors (1), http (1)
> **Definitions:** is a  (2), stands for (1)
> **Cross-References:** earlier in (1)
> **Best Practices:** it's best to (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Examine JSON data used by websites](https://www.linkedin.com/learning/json-essential-training/challenge-examine-json-data-used-by-websites?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/challenge-examine-json-data-used-by-websites?u=76281980&t=0)** (light music)
>
> **[0:08](https://www.linkedin.com/learning/json-essential-training/challenge-examine-json-data-used-by-websites?u=76281980&t=8)** - [Instructor] Ready to explore the [JSON](../../Skills/Web%20Development/JSON.md) data that powers some everyday experiences? It's time for a challenge. The developer tools built into modern browsers let you examine the code and data that are part of websites and applications that you open. For this challenge, pick a website that you use regularly or one you just want to understand better, and you'll use your browser developer tools to look at the data the site is receiving. Remember that you want to be sure to choose the XHR option in your developer tools to filter out other types of requests the browser is making that don't involve data exchange. In Firefox, you click the XHR button on the network tab, in Chrome, you need to first click the filter icon, then click XHR, if you're not sure what site to examine, try the Louvre museum in Paris at louve.fr and open the explore page. This exercise should take you about 10 minutes. When you're done join me in the next video, and I'll walk you through how I approached it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Env Vars:** xhr (3), json (1)
> **Cross-References:** in the next (1)
> **Tools:** firefox (1)
> **UI Navigation:** open the (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (light music) (1)

#### [Solution: Examine JSON data used by websites](https://www.linkedin.com/learning/json-essential-training/solution-examine-json-data-used-by-websites?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/solution-examine-json-data-used-by-websites?u=76281980&t=0)** - [Instructor] I'm going to explore some data on the web, and sometimes you need to actually open up the network tab and start recording and listening for those HTTP requests before you actually open the site, just to make sure you get everything. So I've got a brand new tab here and I'm going to start by opening those developer tools. There are lots of different ways to open the developer tools, but remember, one of the most straight forward is just to right click anywhere on the web page and click inspect on the menu. And that's going to open up your developer tools. I've made mine a little bigger. You can just move the pointer over the top edge. And when it turns into that double-headed arrow, you can stretch that up, make it as big or small as you want. And then I want to go over to the network tab, and I'm not worried about checking on this page, but now that I have this open, I want to go up to my address bar and I'm going to go L O U V R E dot F R. And now as the webpage loads for the first time I'm getting everything, I'm getting all the requests. And so there's all the requests I need to go ahead and deal with their cookie policy. So this includes all of the images, all of the [JavaScript](../../Skills/Software%20Development/JavaScript.md), all of the [HTML](../../Skills/Web%20Development/HTML.md), the fonts, everything that's coming on this page, there's an HTTP request for it. What we want to look at is the XHR requests and that's the data. So I'm going to go over here and click XHR and all I've got right now is a CSS file. So let's keep looking.
>
> **[1:34](https://www.linkedin.com/learning/json-essential-training/solution-examine-json-data-used-by-websites?u=76281980&t=94)** I'm going to try the explore tab, and now I can see that I have one [JSON](../../Skills/Web%20Development/JSON.md) type here. So I'm going to go ahead and click this file. Actually, I don't want to click that link. I'm just going to click this entire line and over here on the right, I can see some information about the request. So these are the HTTP headers HTTP status, and I'm going to go over here to response to actually see what came in. So it's labeled as JSON. And if I start looking through it, I can see first off, everything's surrounded by square brackets. So it's an array. And within the array, we've got curly brackets. So those are objects. So it's an array that's full of objects. And it looks like we've got elements of the menu items that are on the page, got some uncoded content, and that may be just images or icons. And so we can see that some of the things we can see in the JSON are on the page itself. And so this is just one instance where we can actually look at the JSON that's coming in and see that it's rendered on the webpage can also check the what's on tab. And there, I just got another JSON package. So I'll go ahead and check that out. So we can see, we have a little menu here. There's something about news and programming and that's right here.
>
> **[3:07](https://www.linkedin.com/learning/json-essential-training/solution-examine-json-data-used-by-websites?u=76281980&t=187)** And then this image object is actually encoded within the JSON. We've got something on exhibitions and that's right there. So all of this content is coming through as JSON and being rendered into the webpage. So again, when you're exploring the Lourve website and a lot of other websites behind the scenes, JSON is coming in and it's responsible for the content you're seeing. So JSON is super important on the web and you can do this anywhere. You go on any website, you can explore and see. Not every website's going to be based on JSON coming through to the frontend, but a lot of them will.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (10), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** json (10), http (4), xhr (2), html (1), css (1)
> **CLI Commands:** make (2)
> **UI Navigation:** open the (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 4. Applying Techniques for Working with JSON

[↑ Back to Table of Contents](#table-of-contents)

#### [Prevent data reuse](https://www.linkedin.com/learning/json-essential-training/prevent-data-reuse?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/prevent-data-reuse?u=76281980&t=0)** - [Instructor] When an application makes data available, especially sensitive data like financial information, [Data Security](../../Skills/Data%20Science/Data%20Security.md) is an important concern. The [JSON](../../Skills/Web%20Development/JSON.md) data in app provides in response to a request can be formatted in many ways. For instance, you can provide data that parses into an object, but your data can also be in the form of a JSON array or even a function in the case of JSON-P. Because some [Forms](../../Skills/Web%20Development/Forms.md) of JSON data can be evaluated as [JavaScript](../../Skills/Software%20Development/JavaScript.md) by a client, you need to be aware of any security holes opened by different formats and take steps to mitigate them where necessary. Browsers enforce what are known as same-origin policies. One of these policies allows a script to access data from another script only if both scripts share the same origin. The origin includes scheme like HTTP or HTTPS, hostname and port number. So this effectively limits a script from accessing data that comes from another server. Now in some cases, I want to permit [Data Sharing](../../Skills/Database%20Management/Data%20Sharing.md), and there are a number of schemes in use that let us do this. At the same time, I have to recognize any unintentional backdoors and be sure to lock them up. Same-origin policies apply to code in a script. However, an [HTML](../../Skills/Web%20Development/HTML.md) document can load scripts from any origin. Some attackers have taken advantage of this by running malicious code that adds a new script element to an HTML document. This is known as a cross-site script injection attack, sometimes abbreviated XSSI. By hijacking what the browser does after parsing data, malicious code can intercept data
>
> **[1:33](https://www.linkedin.com/learning/json-essential-training/prevent-data-reuse?u=76281980&t=93)** and send it to an attacker. Fortunately, there's an easy way to defend against this. Although a malicious script can add a new script element, it can't control what happens next. When the client downloads the script file, it immediately executes it, so many web services add JavaScript code that generates an error to the start of the JSON data they send. When the data is requested by another script, which can only happen in a script from the same origin, the script can parse the data and strip out the JavaScript code before using the data, but when the data is downloaded directly using an injected script element and the data is immediately executed, the malformed code at the start generates an error and the data is never parsed. One statement that causes an error, and that is commonly used for this purpose is while one. The statement creates a while loop that executes as long as the value one evaluates to true because one is equivalent to true in JavaScript, and because the statement includes no end condition, the statement creates an infinite loop. If the loop is allowed to run, it causes the client to stop responding. Alternatively, the client may have built in logic that recognizes the loop and ends execution of the injected script. Either way, it closes off the possibility that the data object can be parsed and then intercepted by a malicious script. To deliver JSON that's prefixed like this, you have to configure your web service on the backend, but assuming that's already done, let's look at what it takes for your code to parse and then use this data. In the script.js file for this video,
>
> **[3:06](https://www.linkedin.com/learning/json-essential-training/prevent-data-reuse?u=76281980&t=186)** the protected data variable on line one contains data like you might receive from a web service, and this data is prefixed with the while one statement. If I open [products](../../Skills/Software%20Development/Microsoft%20Products.md).html and I go live, and then I open up my console, I can see that there's an error and that the contents of my webpage aren't actually loading. And this is the behavior I want from my raw data, but I don't want to be parsing the data in this form, instead, because I know that my data contains this extra while statement at the start, I can write code to edit that out and work with just the data itself. So I just need to create a function that removes the while statement from the start of the data and then parses the remaining data. So further down here on line six, I'm going to add a function called clean data. I need to start by making that a const. And then the value is going to take a parameter, which is raw data and it's going to return the result of taking that raw data parameter and using the slice method to remove the first nine characters. And those first nine characters are the while statement. So this clean data function is taking my data, removing that while statement, and then returning the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of it, which is just the data. Then down here on line nine, I need to update my existing JSON.parse statement to use that clean data function.
>
> **[4:38](https://www.linkedin.com/learning/json-essential-training/prevent-data-reuse?u=76281980&t=278)** So instead of simply parsing protected data, which has the while statement in it, I want to first call clean data and I want to pass it protected data. And the result here is that the protected data is going to run through the clean data function and the result of that is going to get parsed. And that will be just my data. And I'll save that and I'll go back to my browser. And now I can see, first off, immediately, that the products have loaded on my page, so the data is in use on my webpage. I can also see that my console.log statements, first, I'm getting that entire JSON string logged, and then next, I'm getting just the data logged, which is the array containing all the information without the while statement at the beginning. You can ignore this other error statement down here. That's irrelevant for our purposes, just has to do with an icon that the browser is looking for. So again, because I'm operating from the same origin as the data source, I'm able to run code to modify it, and I can get the data out without being hampered by the error generating statement, and this should help prevent some malicious uses.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (8), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (4), [HTML](../../Skills/Web%20Development/HTML.md) (3), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (2), [Data Security](../../Skills/Data%20Science/Data%20Security.md) (1)
> **Env Vars:** json (8), html (2), http (1), https (1), xssi (1)
> **Definitions:** known as (2), is an  (1)
> **File Paths:** script.js (1), products.html (1)
> **Cross-References:** go back to (1)
> **Analogies:** for instance (1)
> **Warnings:** be aware (1)
> **Prerequisites:** configure (1)

#### [Return readable JSON](https://www.linkedin.com/learning/json-essential-training/return-readable-json?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/return-readable-json?u=76281980&t=0)** - [Instructor] Sometimes it's handy to have a [JSON](../../Skills/Web%20Development/JSON.md) string in human readable format. For instance, when you're encoding a [JavaScript](../../Skills/Software%20Development/JavaScript.md) object or array into JSON, especially a large one, you may run into problems that require debugging. It can be useful to examine the JSON string and identify where things go wrong, but JSON is not made for human readability, so examining the raw JSON won't necessarily do you any good. When I'm converting to a JSON string in JavaScript, I use the JSON.stringify method and pass it just the object or array to be encoded. However, the stringify method in JavaScript takes additional arguments, one of which is designed precisely for this purpose. After the value to be converted to a JSON string, you can optionally pass two additional arguments. The first optional argument is a function that modifies how the conversion happens. The second optional argument lets you specify indentation for levels of hierarchy within the encoded data, such as key names within an object. If you specify a number, then each successive level is indented this number of spaces. You can instead pass the string\t to use a tab. In the start files for this video, script.js contains the raw data variable whose value is a large JavaScript object. And on lines three through six, I'm logging back, parsing it over to a JavaScript object, and then logging it again. But I want to take that parsed data and I want to re-stringify it and I want to use some of this formatting to see if I can make it readable. So here on line 10, I'm going to start by creating a console.log statement,
>
> **[1:34](https://www.linkedin.com/learning/json-essential-training/return-readable-json?u=76281980&t=94)** another one, and I'm going to pass it JSON.stringify, and my parsed object, which is in the variable data. But instead of stopping there, I want to add those indents so I can examine the string in the browser console. So after the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) data, but before that closing paren, I'm going to add a comma, and then I need to add the second argument. Now I don't have a function that I want to use to modify the stringification process, so instead, I'm going to specify null, and then I'm going to add another comma, and then my third argument, and this is the number of spaces to use for indents. I want to use four spaces just to make sure I can see those indents. So I'm going to enter the number four. So I'll save that. I will open up my [products](../../Skills/Software%20Development/Microsoft%20Products.md).[HTML](../../Skills/Web%20Development/HTML.md) file and use my HTTP server to open that in the browser. I'll open my Developer Tools, and in the console, I can see that I have readable content here for my last console.log statement. Now I can tell that I am actually looking at a JSON string because, for one thing, each property name is in quotes, and that's not the case when I'm looking at an object or an array. For instance, here, I can see when I dig into one of these sub-objects that these key values don't have quotes around them, but in a JSON string, the keys and the values have to be quoted when there's strings. So the other thing I noticed here is there's no color coding. This is all black text on a white background in my case. And so, I'm actually looking at a JSON string
>
> **[3:08](https://www.linkedin.com/learning/json-essential-training/return-readable-json?u=76281980&t=188)** that's just been broken up and indented. And with those line breaks and indents, I can easily compare the stringified code with the code for the original object. If I was having a problem, I could compare the two, identify where things were going wrong and then figure out how to solve the problem.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (12), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (5), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** json (12), http (1)
> **Analogies:** for instance (2), such as (1)
> **File Paths:** script.js (1), products.html (1)
> **CLI Commands:** make (2)
> **Definitions:** is a  (2)
> **Speakers:** - [instructor] (1)

#### [Test for an empty object](https://www.linkedin.com/learning/json-essential-training/test-for-an-empty-object?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/test-for-an-empty-object?u=76281980&t=0)** - [Instructor] When you process data returned from a call to a web service, a useful first step is to check whether any data has been returned. A [JSON](../../Skills/Web%20Development/JSON.md) string will generally contain some characters, even if they're just the opening and closing braces for an encoded object or the brackets for an array. So parsing is a common first step. If you're using [JavaScript](../../Skills/Software%20Development/JavaScript.md) though, checking whether the object or array is empty isn't as straightforward as checking for empty data of some other types. Unlike an empty string, for example, which is falsy in JavaScript, an empty object is truthy. This means that a simple logical test, like checking if the value is null cannot distinguish between an empty object and an object with content. In a modern browser, you can use the Object.keys method to generate an array of your object's keys and then check if the length property of the array is zero. I've created a test file containing one empty object called emptyObject, as well as an object containing a key and a value called nonEmptyObject, and additional variables containing data of a few other data types: string, number, array and date. So I'm going to create a new function called testData. And that's going to take a single parameter called data. And I'm going to add the statement if, and then in parens Object.keys parens data.length, triple equals zero.
>
> **[1:38](https://www.linkedin.com/learning/json-essential-training/test-for-an-empty-object?u=76281980&t=98)** Then I'm going to console.log Empty Object, and a colon. And I'm going to return the data. And after the closing curly brace here, I'm going to say else, and I'm going to console.log Not an Empty Object.
>
> **[2:01](https://www.linkedin.com/learning/json-essential-training/test-for-an-empty-object?u=76281980&t=121)** And a colon. And I'm going to return the data. Now, note that this code may behave unpredictably if it's passed data that's not an object. And in particular, a date object would have a zero length here, so it would pass for an empty object and I might skip parsing actual data. Now, to account for this, I'm going to update my condition. So after, oh and I realized I spelled this wrong. It should be Object.keys plural. So after Object.keys data.length equals zero, I'm going to do an and and and I'm going to use data.constructor triple equals Object, and that would be false for a data object because that's constructed from the date constructor. So in theory, this function should be able to take each of my pieces of data and tell me whether they are an empty object or not an empty object. So I'm going to save my work here. I'm going to go ahead, and this is an [HTML](../../Skills/Web%20Development/HTML.md) file. So I'm going to use my HTTP server, open this in my browser. I'm going to open my console. And then I'm going to test my variables. So I'm going to run testData. And I'm going to pass it emptyObject. And it is an empty object. It's been identified as an empty object. And so let me go ahead and do that again. But I'm going to test that against my nonEmptyObject. And that's identified as not an empty object. And let's try with the date variable.
>
> **[3:39](https://www.linkedin.com/learning/json-essential-training/test-for-an-empty-object?u=76281980&t=219)** So that's dateContent. And that's also not an empty object. Now, where things get a bit more complicated is older browser support. And specifically, if we look at the compatibility table for Object.keys, we can see that older versions of some browsers don't support the Object.keys method. For instance, older versions of Internet Explorer. Now, I can write a more robust test using the hasOwnProperty method of the object. And if I look at the compatibility for that method, I can see it supports even older versions of Internet Explorer, as well as a number of these other browsers, like Chrome and Firefox. It goes back to version one. So we have really, really solid support here for any older browsers that might be in my user base. So I'll go back to my function, and I'm going to comment out this code that works in modern browsers, and I'm going to start with a for in statement. So I'm going to do for prop in data. And within the for statement, I'll add an if statement, and I'm going to test whether hasOwnProperty triple equals true. And that's checking for the current property. And if that's the case, I'm going to console.log Not an Empty Object.
>
> **[5:02](https://www.linkedin.com/learning/json-essential-training/test-for-an-empty-object?u=76281980&t=302)** And I will return data. Then I need to account for both empty objects that pass through the for in statement and non object data that skips the for in statement. So after this, I'll create an if-else construction. And this statement is going to check if JSON.stringify data is the same as triple equals JSON.stringify an empty object. And if that's the case, I'm going to console.log Empty Object, and I'm going to return data. Else I'm simply going to console.log Not an Empty Object, and return the data. So I'll save my work here and I'll go back to my browser. And I'm going to run tests again. So I'm going to call that same function. testData, and pass it emptyObject and that is found to be an empty object. And running it with a nonEmptyObject is not an empty object. And testData with a dateContent is not an empty object. And if I run it against the other data types, I'm going to see that those are also not seen
>
> **[6:35](https://www.linkedin.com/learning/json-essential-training/test-for-an-empty-object?u=76281980&t=395)** as empty objects. And so I can use a function like this to test for an empty object. So this is going to be able to tell me whether I have data coming in in response to an API request.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (3), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Code Identifiers:** testdata (4), emptyobject (3), nonemptyobject (3), datecontent (2), hasownproperty (2)
> **Env Vars:** json (3), html (1), http (1), api (1)
> **Definitions:** is an  (2), is a  (1), means that (1)
> **Cross-References:** go back to (2)
> **Analogies:** for example (1), for instance (1)
> **Tools:** firefox (1)
> **Warnings:** note that (1)


### 5. Working with JSON Schema

[↑ Back to Table of Contents](#table-of-contents)

#### [What is JSON Schema?](https://www.linkedin.com/learning/json-essential-training/what-is-json-schema?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/what-is-json-schema?u=76281980&t=0)** - [Instructor] What happens when you make a request for [JSON](../../Skills/Web%20Development/JSON.md) data and the data you receive is different than what you expect? What if the JSON is organized differently than your code expects, or it's simply not a valid response to your request? To identify and deal with situations like these, you can use a schema. A schema is essentially a blueprint for what data should look like. You can create a schema for the data you expect to receive, and then provide that schema to the code that handles the data. Your code can compare the schema to the data and verify either that the data is the type you expected or that there's a problem that needs handling. JSON Schema is a standard for creating schemas for JSON data. It was created by the internet engineering task force. To create a schema using JSON Schema, you create JSON using a specific structure and keywords along with values you specify. You can then write code to validate data against the schema. What kinds of data problems can you identify with JSON Schema? The specification has a number of options, but some of the most common areas include missing required data and incorrect types of data. For instance, imagine if my app was requesting data that included a numerical invoice number and a billing total. Now, imagine that I got this JSON as my response. Well, first off, I noticed that there's an invoiceNum property, but there's no property that's obviously storing the billing total. So, this data isn't exactly what I need, because my code is expecting to work with a billing total. By validating, I can keep from taking the performance hit
>
> **[1:35](https://www.linkedin.com/learning/json-essential-training/what-is-json-schema?u=76281980&t=95)** that comes with processing the data, and then finding the error. Instead, I can identify the error upfront and then perhaps, resubmit my request or report the error. Now, the other thing I noticed about this data is that the invoiceNum value is a string that includes both numbers and letters, but I'm expecting a numerical value. So, even though I have data, it's of the wrong data type. Again, validating the data before processing it, prevents potential errors or unexpected results that might come from trying to perform mathematical operations on a string value. It's also important to be clear about what a schema can't do for you. A schema allows you to describe the general structure of data, but you cannot use a schema to really delve into the meaning of the data itself to decide whether it makes sense. For instance, suppose you have JSON data for something like a shopping cart. That includes a number of items and their prices, along with tax, shipping, and an order total. You can use a schema to verify that the price, tax, shipping, and total values are all numbers. However, a schema does not allow you to verify that. For instance, the total value is equal to the sum of the item prices and the tax and the shipping, or even that the total value is greater than each of its component values. For this kind of validation, you have to write code that works with and compares values.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (9)
> **Env Vars:** json (9)
> **Analogies:** for instance (3), imagine (2)
> **Code Identifiers:** invoicenum (2)
> **Definitions:** is a  (2)
> **CLI Commands:** make (1)
> **Documentation:** specification (1)
> **Speakers:** - [instructor] (1)

#### [Create a basic schema with JSON Schema](https://www.linkedin.com/learning/json-essential-training/create-a-basic-schema-with-json-schema?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/create-a-basic-schema-with-json-schema?u=76281980&t=0)** - [Instructor] I want to create a schema to describe the product data available from the H+ Sport API. To start, I want to look at the data return for a sample request. So, I'm looking at data.js. Now, this is the data I would normally get from an HTTP request to the API. But for the purpose of creating a schema, I've stored the data from a request in my file. Right now, this is [JSON](../../Skills/Web%20Development/JSON.md). It's a string and the editor won't display it for me with line breaks and indents. But I can open product study [HTML](../../Skills/Web%20Development/HTML.md), start up my live server and look at it in the console. So, I'll go ahead and open that HTML file. Start up my HTTP server and then opening up the console. My project includes [JavaScript](../../Skills/Software%20Development/JavaScript.md) code that parses the JSON strings into JavaScript objects and then logs these to the console. And now, I can examine the structure of this data. It's an array and it contains six objects. And each object contains the same five properties. I'm going to build my schema in the schema.js file. A schema is written in JSON, but it's easier to work with it by writing it as an object within the language I'm working in, which is JavaScript. But I'm going to follow the rules of JSON to make conversion easier. So, we'll start by creating a variable. And so, I'll just use VAR and call this product schema. And the overall structure of this is going to use opening and closing braces. The content of the schema is simply a set of keys
>
> **[1:34](https://www.linkedin.com/learning/json-essential-training/create-a-basic-schema-with-json-schema?u=76281980&t=94)** and values, so a JavaScript object. By convention, I start with the dollar schema keyword as the first key. And this makes it clear that this object is a JSON schema, rather than just generic JSON. Remember that JSON requires key names as well as values to be double quoted. So, I have to put double quotes around this. And I'll follow that with a colon and then the value. And this is a special value that's defined in the JSON schema specification. And it's a URI. That's [http://json-schema.org/draft-07/schema](http://json-schema.org/draft-07/schema)#.
>
> **[2:15](https://www.linkedin.com/learning/json-essential-training/create-a-basic-schema-with-json-schema?u=76281980&t=135)** And this value indicates that this schema implements draft seven of JSON schema. And I'll end this line with a comma. And on the next line, I also want to include the dollar ID property. This lets me specify a unique identifier for this schema. By convention, this is in the form of URI as well, but it doesn't need to reference an existing resource. Generally, you want to use a domain you control and specify a unique path. I'm creating a schema for the [products](../../Skills/Software%20Development/Microsoft%20Products.md) displayed on the H+ Sport website. So, I'll use [http://hplussport.com/schemas/product.json](http://hplussport.com/schemas/product.json).
>
> **[3:00](https://www.linkedin.com/learning/json-essential-training/create-a-basic-schema-with-json-schema?u=76281980&t=180)** This isn't a valid URL at this point and that's fine. As long as it's a domain I control, it provides a unique reference for the schema. And if in the future I choose to, I can publish the completed schema at this address. JSON schema also supports a couple additional properties that are optional, but recommended. You use the title property to provide a short summary of the purpose of the schema. So, I'll specify a value of eight plus sport products. And then, you use the description property to give a more detailed idea of the schema's use. So description and I'll say schema for H+ Sport product data. Looking at my data in data.js, I can see that at the top level, it has an array as its value. In my schema, I indicate the data type of the top level JSON content with the type property. So in this case, I'm going to say type and that's going to be a value of array. Then for array data, I specify an item's property. And the value of items is going to be an object. And it's going to specify what kind of data should be found within the top level array. So, the items within that array are objects. And so, I want to use another type keyword. So, the type of data within here is object. So then, I can specify the properties
>
> **[4:34](https://www.linkedin.com/learning/json-essential-training/create-a-basic-schema-with-json-schema?u=76281980&t=274)** of each of those objects using the properties keyword. And properties is also going to be an object and within properties, I can list each property name that is to say each key name that occurs within these objects and specify its data type. I can also give a lot of other information about each one of those properties, but for now, I'm just going to specify the data type. So, my first property is ID and I specify its value as an object, but I only have one key value within that object and that's type and string. And then, comma after that object value. And then, I just need to go through all the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of my objects. And so, I can switch back to my console and just look here. I have ID, which I started with. There's also description, image, image title and name and I can do these in any order. The order doesn't matter within an object. So, I'll just go with the name next and that's going to be a of string. And all the rest of these are going to be string types. So, I can really just copy and paste the code that I've just typed and change the key name. So, we need a description. I need an image_title. And then I need an image. All five of those are strings, but if there were other data types like number, I could specify those here as well. And then, I need to be sure because this is JavaScript,
>
> **[6:06](https://www.linkedin.com/learning/json-essential-training/create-a-basic-schema-with-json-schema?u=76281980&t=366)** I want to end my statement with a semi-colon. Remember that JSON schema code is itself, JSON. I've created this as JavaScript. My editor is able to recognize this as a JavaScript object and verify that the syntax matches what it should match for a JavaScript object. But again, it's important to recognize that this doesn't ensure that the schema I've written provides the validation I expect. That requires testing it against JSON that I know is good and verifying that each good JSON string passes validation with this schema. I also want to create some JSON that has specific errors and test those against the schema and verify that those errors are flagged.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (18), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (8), [HTML](../../Skills/Web%20Development/HTML.md) (2), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2)
> **Env Vars:** json (16), api (2), http (2), html (2), uri (2)
> **File Paths:** data.js (2), schema.js (1), hplussport.com/schemas/product.json (1)
> **URLs:** [http://json-schema.org/draft-07/schema](http://json-schema.org/draft-07/schema) (1), [http://hplussport.com/schemas/product.json](http://hplussport.com/schemas/product.json) (1)
> **Definitions:** is a  (2)
> **CLI Commands:** make (1)
> **Code Identifiers:** image_title (1)
> **Documentation:** specification (1)

#### [Validate JSON data against a schema](https://www.linkedin.com/learning/json-essential-training/validate-json-data-against-a-schema?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/validate-json-data-against-a-schema?u=76281980&t=0)** - [Instructor] The easiest way to validate [JSON](../../Skills/Web%20Development/JSON.md) data against the JSON Schema is to use a validator written for that purpose. Free open source validators exist for a number of languages, including [Java](../../Skills/Software%20Development/Java.md), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), Ruby and [PHP](../../Skills/Software%20Development/PHP.md), as well as browser-based utilities. For the H+ Sport website, I'll use a [JavaScript](../../Skills/Software%20Development/JavaScript.md) validator called Ajv. Ajv is designed for backend use, but I can try the tool out without installing it by using an online instance called a REPL. The left pane shows the JavaScript code that the validator uses. I only need to work with two parts of the code. The schema variable and the test function calls. I'll open the schema.js file for this video. And I want to copy the schema content to the clipboard. Now, I only want the object itself and not the variable declaration or the equal sign. So, I'll click the beginning, click to the end, but not include the semi-colon, and copy that. And then over in the REPL, I want to delete the existing value for schema and paste in my schema. Now, that I've told the REPL what schema I want to validate against, I need to give it some sample data to test. To do that, down here on line 34, I can call the test method and specify the value to test against between parentheses. So, I'm going to start by deleting the existing values between parentheses for the first function call. And I'll just delete the second function call altogether for right now, then I'll go to my data.js file, and I'll copy the value for the rawProductData variable.
>
> **[1:35](https://www.linkedin.com/learning/json-essential-training/validate-json-data-against-a-schema?u=76281980&t=95)** And this is a JSON string containing an array. So, I want everything from the opening quote to the closing quote, but not that closing semi-colon, and not the variable name and equal sign. Copy that to the clipboard. And then over in the REPL, I'll paste that between the parens after the test function call. And now I'm ready to test, so I'm going to scroll down all the way to the bottom and select the Run button. So, this is telling me my data has to be an array and it looks like my error here is that I included the quotes when I wanted to just give it the data instead. So, now I've just pasted in an array rather than a string containing an array, and I'll go ahead and click Run again. And this time I get the message valid. So, my data is valid according to the schema I created, which is exactly what I expect. Now, what happens if there are errors in my data? I would expect that to be invalid and I can test that out. So, I'll go back to my data, I'll introduce a couple errors, and I'll make sure that those get logged as I expect. So, in the code for the first product, Raspberry Mineral Water, I'm going to replace the description value, which my schema specifies has to be a string, and I'm going to replace that with a number. And for that to be a numerical value, I have to take the quotes off. Remember that only strings are quoted in JSON. And then here for the second product, the Strawberry Mineral Water, I'm going to take the quotes off of the ID value.
>
> **[3:09](https://www.linkedin.com/learning/json-essential-training/validate-json-data-against-a-schema?u=76281980&t=189)** So, my schema is expecting a string value for the ID, but I'm going to throw a number at it and see what happens. So, I'll scroll down and I'll click Run again. And this time I get the message invalid followed by a couple specific messages. And the validators pointing out the errors that I introduced exactly. Description must be string, ID must be string. A validator is really helpful in making sure your schema matches the data that you want to work with. And it can also flag errors in data coming in before you try to work with the data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (4), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [Java](../../Skills/Software%20Development/Java.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [PHP](../../Skills/Software%20Development/PHP.md) (1)
> **Env Vars:** json (4), repl (4), php (1)
> **UI Navigation:** scroll down (2), open the (1), go to (1), select the (1)
> **CLI Commands:** python (1), ruby (1), php (1), make (1)
> **File Paths:** schema.js (1), data.js (1)
> **Code Identifiers:** rawproductdata (1)
> **Cross-References:** go back to (1)
> **Definitions:** is a  (1)

#### [Specify required properties with JSON Schema](https://www.linkedin.com/learning/json-essential-training/specify-required-properties-with-json-schema?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/specify-required-properties-with-json-schema?u=76281980&t=0)** - [Instructor] In addition to specifying data types for the properties of [JSON](../../Skills/Web%20Development/JSON.md) data, JSON schema also allows you to specify that one or more properties are required. At this point, my schema specifies a data structure for an array that contains objects and each object contains five properties, ID, name, description, image title and image. What if I want to require that these properties must be included in order for my JSON data to be considered valid? Well, remember that each sub object within the array is itself specified by an object in JSON schema with a type property set to object and a property's property specifying the names of properties it can contain. To indicate that one or more of these properties are required, I simply add another property to the object in my JSON schema. The property must have the name required and its value is an array containing one or more strings as its elements. Each string in the array must correspond to a property defined in the properties object. In my schema.js file, for my items property, I already have a type value of object. And a property's value containing the properties to include with their data types. To specify that all of these properties are required, I add another property after that data structure. because I've got so many nested braces here. I'm going to click the curly brace after the properties key and that's got a box around it in my editor is going to highlight the closing brace for that, which is down here on line 26. So, I'm going to click at the end of line 26. I'm going to enter a comma.
>
> **[1:34](https://www.linkedin.com/learning/json-essential-training/specify-required-properties-with-json-schema?u=76281980&t=94)** And then I can enter another key value pair that's on the same level as the properties key. And the name of this is going to be required and I'm going to set the value of the required property to an array. And in that array, I'm going to add five elements, which are strings containing the names of the required properties. And these can be in any order. So, ID... Name... Description... Image_title... And image. This code specifies that some value is required for each of these five properties within each object for each product. Now, I want to test this. So, I'll save my changes to the schema and I'll copy the value of the product schema variable without the quotes. So, starting at the curly brace after the equal sign. And at the very end, before the semi-colon. And then I want to open up the validator I'm using, which is AJV and I need to swap out my new scheme of value for the scheme of variable. So, we'll select that scheme of variable and paste mine in its place. Then back in my editor, I'm going to open up the data.js file, which contains my data. And I want to copy the value of the raw product data variable without the quotes. So, after that opening apostrophe and all the way down to the end of the line, excluding that closing apostrophe in semi-colon.
>
> **[3:10](https://www.linkedin.com/learning/json-essential-training/specify-required-properties-with-json-schema?u=76281980&t=190)** And I'm going to copy that to clipboard. And then over in AJV, I'm going to scroll down to lines 41 and 42. I'm going to delete that second test function call on line 42. There we go. And within the parentheses for the test function call on line 41, I want to select the contents. And then, I want to paste in what I just copied. And now, I can scroll down and hit run. And after a moment, I get the message valid. All of my required properties are included throughout the data. Now, all of my data currently contains all five properties, but I can test my schema by deleting a few properties here and there. So, for my first item, I'll delete the image key and its value. I seem to have some scrolling behavior going on when I click in here, so I'm just working around that the best I can. So, there's the image key and value and the comma after it. And then, for the next property, I'll just go ahead and delete the ID key and value and the comma after it. And I'll scroll down. And I'll click run. And I get an error. So, it says invalid and it has two messages about specific elements of the array that are missing. And in particular, that there's a required property image and a required property ID, which are exactly the two I deleted. So, I'm really happy to see this error message
>
> **[4:44](https://www.linkedin.com/learning/json-essential-training/specify-required-properties-with-json-schema?u=76281980&t=284)** because it means the schema is catching potential issues that are important to me. And now, in addition to catching potential issues with incorrect data structure, extra data and incorrect data types, my schema also catches missing data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (5)
> **Env Vars:** json (5), ajv (2)
> **UI Navigation:** scroll down (3), select the (1)
> **File Paths:** schema.js (1), data.js (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Work with schema generators](https://www.linkedin.com/learning/json-essential-training/work-with-schema-generators?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/work-with-schema-generators?u=76281980&t=0)** - [Instructor] Building a [JSON](../../Skills/Web%20Development/JSON.md) schema from scratch can be a challenging and tedious process. You can get a leg up on coding the components and the basic structure though by using a schema generator, like the one at [jsonschema.net](https://jsonschema.net). A schema generator allows you to enter an example of valid JSON that you want to validate, and choose some schema options. It then automatically generates a schema that fits your data and any selections you've made. The H+ Sport API can return a number of different categories of data, including data on people. In the code for this video, the data is stored in the data.js file, in the rawPersonData variable, which is on line three. It consists of a single property, cards, containing an array of objects. Each object contains three properties, and two of those properties contain sub-objects as values. So there are a number of levels in this data, and several different properties scattered throughout. Instead of trying to build out a schema from scratch, I'm going to select and copy the JSON from the opening curly brace to the closing one. So again, leaving off the quotes. Going all the way down to the bottom. And I'm going to copy that to the clipboard. When you open the main page of [jsonschema.net](https://jsonschema.net), you may be asked to create an account but there's also an option to just continue as a guest, and that's what I've done here. So the left side of this app is the JSON section, and the right side is the resulting schema. So I'm going to start by selecting the contents of the JSON box and deleting those.
>
> **[1:34](https://www.linkedin.com/learning/json-essential-training/work-with-schema-generators?u=76281980&t=94)** And then I'm going to paste in the JSON I copied from my web page code. Notice that the tool checks for valid JSON and confirms that I'm good to go on that front with the notice just above the JSON box. It says your JSON is valid, and gives me a green check mark. If I click Settings up here, near the top of the left column, I have a number of options. And I want to customize a couple things. First, I'm going to click keyword visibility. And this lets me select whether certain keywords are included in the schema. Examples is selected by default, which can be useful in understanding a schema you didn't create or haven't worked with in a while. But the examples can make the overall structure harder to understand, so for now, I'm going to uncheck that. And then I'll click outside that list to close it. I also want to customize the URI for this schema. So I'll select the value in the absolute URI box, and I'll delete it and then I'll enter the one I've been using, [http://hplussport.com/schemas/person.json](http://hplussport.com/schemas/person.json).
>
> **[2:38](https://www.linkedin.com/learning/json-essential-training/work-with-schema-generators?u=76281980&t=158)** And so this is different than the one I've been using actually. This is specifically for the person data. And then I'll click Save to save my selections. And then at the bottom of the left pane, I'll click Submit. And the tool is generating JSON schema code based on the JSON data I pasted. And now on the right side of the app, the box displays the newly generated schema. And as I examine this code, I notice a couple things. First, it includes a $schema value by default, along with $id, type, title, and description. So these are all [Metadata](../../Skills/Web%20Development/Metadata.md) keywords for the schema. And as I scroll through the data, when I start looking at things, I notice that every property that was included in my data is marked as required. Now, in my case, those properties are all required, and so I'm fine with what's going on here but if any of them weren't required, I could simply remove that extra keyword. Also notice that in addition to the description property for the root structure, all of the other properties in my schema have a description property as well. This can be useful for documentation purposes. At this point, the schema generator has fulfilled its purpose, which is to take care of the bulk of the tedious work and get me most of the way there. So I can copy the code from the schema from the right side of the app, and it actually have a copy to clipboard button. So I don't have to select it first. Then back in my code in my editor, I'm going to open up schema.js and I'm going to add a new variable from my personSchema. So at the bottom, I'll say var personSchema,
>
> **[4:14](https://www.linkedin.com/learning/json-essential-training/work-with-schema-generators?u=76281980&t=254)** and I'll set that equal to the value I just copied. So I'll just paste that in. And I'll add a semicolon at the very end for completeness. And then scrolling up, I'm going to look at the very first meta properties. So on lines 41 and 42, these are just very generic title and description. So for the title, I want to change that value to h+ Sport People. And for description, I want to change that value to schema for h+ Sport person cards. Now that I have a schema for my data, I'll open up a schema validator, just to verify that it works as I expect. And I've been using a live version of Ajv that's hosted online. I'll copy my edited schema, and that's just the value. So everything within the curly braces, including the curly braces. And then over in my validator, I'll paste what I've copied as the value of the schema variable. And then back in my code in data.js, I want to copy the value of the PersonData variable. That's rawPersonData. So that's everything but the quotes. And then scrolling down within my validator, delete the second test function call. And then in the first test function call, I'm going to delete the contents of the parens,
>
> **[5:48](https://www.linkedin.com/learning/json-essential-training/work-with-schema-generators?u=76281980&t=348)** and I'm going to paste my data within the parens. And then I want to scroll down, and click the Run button. And after a moment, I should see a response here. Now, I have an error here, and it looks like the schema validator disagrees with the inclusion of the additional items property at the root level because the object is not an array. So back in my editor, I'll go to schema.js, and I'll go to my personSchema, and down here on line 54, I'm going to go ahead and delete this line. So it's automatically added by the schema generator, but I'm going to save that here. And I will once again copy the value of personSchema. And go over to my validator, and paste that in again. I could just make the changes in the validator but there's a real concern there that I might make the changes there and forget to save them in my file, and then end up with the schema that I'm using that doesn't match the one that actually passed validation. So with my new schema here in place, I'll go down and I'll click Run again. Have to wait a second for that to run. And now I have valid. This is why it's super important to test even a generated schema. Sometimes that includes simply making sure that your schema works for what your schema validator expects. Even with the little change that I needed to make, I have a schema for person data
>
> **[7:22](https://www.linkedin.com/learning/json-essential-training/work-with-schema-generators?u=76281980&t=442)** that took relatively little work to create. So schema generators are a really useful starting point for building schemas.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (12), [Metadata](../../Skills/Web%20Development/Metadata.md) (1)
> **Env Vars:** json (11), uri (2), api (1)
> **Code Identifiers:** personschema (4), rawpersondata (2)
> **File Paths:** data.js (2), schema.js (2), hplussport.com/schemas/person.json (1)
> **UI Navigation:** go to (2), open the (1), select the (1), scroll down (1)
> **CLI Commands:** make (4)
> **URLs:** [jsonschema.net](https://jsonschema.net) (2), [http://hplussport.com/schemas/person.json](http://hplussport.com/schemas/person.json) (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Work with JSON Schema](https://www.linkedin.com/learning/json-essential-training/challenge-work-with-json-schema?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/challenge-work-with-json-schema?u=76281980&t=0)** (light music)
>
> **[0:08](https://www.linkedin.com/learning/json-essential-training/challenge-work-with-json-schema?u=76281980&t=8)** - [Instructor] Ready to try your hand at working with [JSON](../../Skills/Web%20Development/JSON.md) schema? It's time for a challenge. The start code for this video includes JSON data returned from an API that provides weather forecasts. Your challenge is to create a schema that validates data in this structure, it's fine to use a schema generator for this challenge. However you create the schema, customize it with the URI shown here for its ID. Once you've created your schema, save it to the schema.js file as the value of the weather schema variable, also validate it using a schema validator and make any adjustments to the code so it passes validation. This exercise should take you about 15 minutes. When you're done join me in the next video, and I'll walk you through how I approached it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (2)
> **Env Vars:** json (2), api (1), uri (1)
> **File Paths:** schema.js (1)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (light music) (1)

#### [Solution: Work with JSON Schema](https://www.linkedin.com/learning/json-essential-training/solution-work-with-json-schema?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/solution-work-with-json-schema?u=76281980&t=0)** - [Instructor] I want to create a schema for my weather data and just looking at this data, even without it parsed, it's got a pretty complicated structure. So I'd prefer to just use a schema generator. Now, if you want an extra challenge sitting down, parsing this data out and then building out level by level, your own schema is a really great challenge, but I'm going to walk through just building this out, using a generator. So I'm going to start by copying the content between the quotes, take that to the clipboard, and then I'll go over to [jsonschema.net](https://jsonschema.net) and I'll replace the existing [JSON](../../Skills/Web%20Development/JSON.md) with my JSON. And I will go ahead and make a couple changes to the settings. So in keyword visibility, I want to take those examples out just to make the structure easier to read. And I want to change that URI to the one that I specified for this particular schema. So that's [http://explore](http://explore) [california.com/schemas/weather.json](https://california.com/schemas/weather.json).
>
> **[1:13](https://www.linkedin.com/learning/json-essential-training/solution-work-with-json-schema?u=76281980&t=73)** And then we'll go ahead and save that and I will go ahead and submit, and now I have a generated schema on the right. I'm going to go ahead and copy that to the clipboard using that button. And back in my code, I'm going to go to my schema.js file. I'll just take those curly braces out and I'll replace those with the generated schema... pretty big. So I'm going to go ahead and save that. And now I actually want to validate that this generated schema makes sense and that it actually works for what I want to do. So still got that schema on my clipboard. So over in my validator, which is Ajv, I'm going to go to the schema variable. I'm going to select that value and delete it. And I'm going to paste in my schema, and then down near the bottom, I want to take out the value for the second. Actually, I want to take out the entire second test function call, and I want to take out the value of the first test function calls. So I have empty parens. And then, and then back in my files in the data file, I want to go ahead and copy everything between the quotes. And then back in my validator, I'm going to paste that between the parens and then scrolling down, I'm going to click run and see what I get. And in this case looks like I got an error similar to the one that I got in the lesson itself.
>
> **[2:47](https://www.linkedin.com/learning/json-essential-training/solution-work-with-json-schema?u=76281980&t=167)** I have additional items that schema generator threw in there and that the validator is not feeling good about. So I'm going to go back to my schema in schema.js, and I can see I have additional items here on line nine. I'm going to take that out and I'm going to do a search and just make sure, and I have another instance of additional items on line 116. I'm going to take that out as well. And it looks like there's nothing else matching addition here. So there were two instances of that. I took them both out, saved my schema, and now I want to copy just the curly braces and everything between them. I'm going to go back to my validator, scroll up and replace the value of the schema variable. That's down through line 215. So I'll delete that. And then I will paste in what I copied. So this is my updated schema, without those two statements that the validator wasn't happy with before I'll click run again, can take a second for those old results to clear. And this time I get valid. So again, I generated a schema using sample data. Then I validated that schema and I chose to accept the recommendations of the validator,
>
> **[4:20](https://www.linkedin.com/learning/json-essential-training/solution-work-with-json-schema?u=76281980&t=260)** which were different than the recommendations of the generator.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (3)
> **File Paths:** schema.js (2), california.com/schemas/weather.json (1)
> **CLI Commands:** make (3)
> **URLs:** [jsonschema.net](https://jsonschema.net) (1), [http://explore](http://explore) (1), [california.com](https://california.com) (1)
> **Env Vars:** json (2), uri (1)
> **UI Navigation:** go to (2), scroll up (1)
> **Cross-References:** go back to (2)
> **Definitions:** is a  (1)


### 6. Structuring Data with JSON-LD

[↑ Back to Table of Contents](#table-of-contents)

#### [What is JSON-LD?](https://www.linkedin.com/learning/json-essential-training/what-is-json-ld?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/what-is-json-ld?u=76281980&t=0)** - [Instructor] You can create [JSON](../../Skills/Web%20Development/JSON.md) structures and schemas for a wide variety of data, but when you're sharing that data with another organization or receiving data built by someone else, it often takes human intervention to classify the content and decide how to integrate it. You can make it possible to automate tasks like this by adding [Semantic Markup](../../Skills/Web%20Development/Semantic%20Markup.md) to your data. Semantic markup not only organizes the data, but indicates its meaning using a common vocabulary of objects and key names. On the web, a number of semantic systems have been used, including RDFa and microformats. Another popular semantic system based on JSON is known as JSON-LD, which stands for JSON for linking data. JSON-LD follows the rules of JSON, but adds a couple standards to convey the meaning of content. First, it references common vocabularies that are maintained by a variety of organizations, including [schema.org](https://schema.org), [productontology.org](https://productontology.org), and the W3C. Data built on the JSON-LD standard also implements key names based on vocabularies that are references. Together, these two JSON-LD features enable an application to parse data and make decisions about what it contains and how it should be used. For instance, [Google](../../Glossary/Service/Google.md) encourages websites to include JSON-LD in their [HTML](../../Skills/Web%20Development/HTML.md) to provide page content summaries for search results. JSON-LD also enables applications to examine data from multiple sources and identify how they relate to each other. For instance, if I was building an application that requested data about cars for sale
>
> **[1:34](https://www.linkedin.com/learning/json-essential-training/what-is-json-ld?u=76281980&t=94)** from multiple websites and displayed a single searchable list, I might find that the data I received used different key names. This could make it difficult to write code that could reliably recognize when two different cars for sale shared a make, model, and year. Is make the same as brand name, for instance? If both of those information providers used JSON-LD, my application would be able to recognize that the data received is structured based on a common standard and it would be able to correlate values based on their shared key names. This can be especially helpful when retrieving different types of data about a common item from different sources. For instance, if I wanted to compile information on organizations for job seekers, one source might provide a company's location and number of employees while another might give me information on the company's diversity policy and [Ethics](../../Skills/Artificial%20Intelligence%20(AI)/Ethics.md) policy. If both sources provided data structured with JSON-LD, I would be able to recognize the company name as a shared value between both datasets and I could reliably compile all values together into a single merged record. By combining semantic meaning with the widely used JSON data structure, JSON-LD enables data to be combined and used in a variety of new ways.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (14), [Semantic Markup](../../Skills/Web%20Development/Semantic%20Markup.md) (2), [Google](../../Glossary/Service/Google.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1), [Ethics](../../Skills/Artificial%20Intelligence%20(AI)/Ethics.md) (1)
> **Env Vars:** json (14), w3c (1), html (1)
> **CLI Commands:** make (5), find (1)
> **Analogies:** for instance (4)
> **URLs:** [schema.org](https://schema.org) (1), [productontology.org](https://productontology.org) (1)
> **Definitions:** known as (1), stands for (1)
> **Documentation:** w3c (1)
> **Speakers:** - [instructor] (1)

#### [Implement predefined key names](https://www.linkedin.com/learning/json-essential-training/implement-predefined-key-names?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/implement-predefined-key-names?u=76281980&t=0)** - [Instructor] Common vocabularies exist for many popular types of data, and [JSON](../../Skills/Web%20Development/JSON.md)-LD supports tagging data using vocabularies from a variety of sources. To specify a vocabulary for a JSON object, you use the special @context key name. Each vocabulary has its own namespace identifier, which is a URI. This is a string that looks like a URL but may not correspond to an actual live resource on the web. However, the identifier is unique, and can be used programmatically to correlate multiple pieces of JSON-LD data that use a common vocabulary. The start files for this video contain a JSON object containing basic information about the h+ Sport company, and that's stored in the snippet.json file. I want to convert this to a JSON-LD snippet, using a public context. [Schema.org](https://Schema.org) defines contexts for a number of generic types of data. I noticed the thing section here includes an organization section. So I'll select that to explore it. [Schema.org](https://Schema.org) vocabularies can require a little familiarization. There's a lot of information here. In general, [schema.org](https://schema.org) vocabularies start with a list and description of key names that are specific to the category you selected. Here organization. Then they may be followed by a list of other relevant key names from the parent category that are relevant. Here, the organization vocabulary uses keys like name and image from the more generic thing category. To build a schema from scratch, it can be useful to start with a model.
>
> **[1:33](https://www.linkedin.com/learning/json-essential-training/implement-predefined-key-names?u=76281980&t=93)** The JSON-LD Playground is provided by the organization that maintains the JSON-LD standard. And it gives examples for a number of common categories. It doesn't show organization but I'm going to select person. And at the top of the window, I have JSON-LD Input, a model to build on. I see the context is simply [http://schema.org](http://schema.org). Then there's an @type key that indicates the vocabulary within [schema.org](https://schema.org) that's used here. After that, the keys match the names in the [schema.org](https://schema.org) documentation for a person. So I'm going to try this out with my code. First, I'm going to select all of my JSON from snippet.json. I'm going to copy it to the clipboard, and then over in snippet.jsonld, I'm going to paste it in as a starting point. The JSON-LD file extension lets the editor know that it should parse the contents as JSON-LD. On the JSON-LD Playground, in the JSON-LD Input box, I can see at the top that I need a couple special JSON-LD keys. First is @context, which is going to be the same as what's shown for the example. That [schema.org](https://schema.org) URL. So I'm going to insert that at the top. @context: [http://schema.org](http://schema.org). And make sure to include that final slash. Without that, the context value isn't valid. Then I need to put a comma at the end. And the next key I need is type. And that's an @type.
>
> **[3:08](https://www.linkedin.com/learning/json-essential-training/implement-predefined-key-names?u=76281980&t=188)** Now, I'm not defining a person but an organization. And back in my browser, if I go back to the [schema.org](https://schema.org) page for organization, I can see right at the top, it says organization, a [schema.org](https://schema.org) type. So I know I can use the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) organization as my type value here. So I'll go back to my code, and use Organization for my type name. Next, I need to make sure the data I'm providing uses keys defined by the organization type. First, company. So I'll switch over to the organization list. And I can see that's not in the list. But I've spent some time looking at this, and if I actually scroll down to the properties from things section, there's a name key, and that's the same as for the person type. So in my snippet, I can simply replace company with name. Next, I have website. And back in the list of keys for the organization schema type, I can see this isn't listed either but it's a common type of data for both a person and an organization. And so again, if I scroll down, I can see that the URL value is again listed in the thing section for organization. So in my snippet, I'll replace website with url. Next, I have image. And it's important to understand just what this image is. And in this case, it's the company logo. Back on the [schema.org](https://schema.org) page, if I look through the list of keys, I can see that there is a logo value. And its description matches what I have. So back in my code, I'll replace image with logo.
>
> **[4:41](https://www.linkedin.com/learning/json-essential-training/implement-predefined-key-names?u=76281980&t=281)** And finally, I have overview. This is a summary of information about the company, what it does. Again, a pretty common type of data for a number of types of information, not just an organization. And so I'll look at that thing section again, and there's a description key. And that seems to match what I need. So back in my code, I'll replace overview with description. And I'll save that. Now I've converted my organization overview to use the public [schema.org](https://schema.org) organization type, and I've implemented key names for my data that are specified for this type. I could share this data on my website or with other companies, and it would be clear from the information provided here that I'm specifying the company name, URL, logo image and a company description. No need for anyone to guess what this data means.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (16), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Env Vars:** json (14), url (4), uri (1)
> **URLs:** [schema.org](https://schema.org) (10), [http://schema.org](http://schema.org) (2)
> **Definitions:** is a  (4)
> **File Paths:** snippet.json (2)
> **CLI Commands:** make (2)
> **Cross-References:** go back to (2)
> **UI Navigation:** scroll down (2)

#### [Map a data structure with JSON-LD](https://www.linkedin.com/learning/json-essential-training/map-a-data-structure-with-json-ld?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/map-a-data-structure-with-json-ld?u=76281980&t=0)** - [Instructor] Sometimes it's not realistic to change the key names in [JSON](../../Skills/Web%20Development/JSON.md) data that you want to share. For instance, if you want to simply provide data from a large database you maintain, and you already use a set of key names that are different from those in the public context and type that you want to use. For this situation, JSON-LD supports a different type of structure. Instead of renaming your keys, you can include an additional object within your JSON-LD snippet that maps your key names to the key names used in the context and type that your snippet references. I'm going to try this out with the company overview for h+ Sport. I have snippet.json open, which is the data that I want to convert to JSON-LD format. And so I'm going to select that and copy it to the clipboard. And then over in my snippet.jsonld file, I'll paste that in. And now I need to convert my content to use JSON-LD standards. So I know that the first thing I need to do at the top, I want to add an @context keyword, (keyboard clicks) but instead of simply providing a URL, this is where I specify the object that maps my keys to the vocabulary keys. And so the value here is going to be an opening curly brace. In my editor, I can just press enter and I get a blank line and the closing curly brace on a new line. And then after that closing curly brace, I need to make sure that I include a comma to keep my format correct. And I'll save that change. Now within this sub object on line three, the first key that I need to add is vocab. And that's where I'm going to provide the [schema.org](https://schema.org) URL. So that's going to be @vocab,
>
> **[1:36](https://www.linkedin.com/learning/json-essential-training/map-a-data-structure-with-json-ld?u=76281980&t=96)** and the value again is [http://www.schema.org](http://www.schema.org)/ and I need to include that final slash after org. And then a comment at the end of that line. Next, I need to specify the same keys that are in my data, company, website, image, and overview, and for their values, I need to provide the corresponding [schema.org](https://schema.org) key name for each of those pieces of data. I've already looked these up. So, I'll start with company and the value is name and then for website, (keyboard clicks) I know my value that I need to use is URL. Then I have an image key, and that maps to the logo key for the schema that I'm using. And finally overview, which corresponds to description. The object I just created provides a map between my data below and the keys at the URL referenced by the @vocab key. My company key maps to the [schema.org](https://schema.org) name key. My website key maps to the [schema.org](https://schema.org) URL key, and so on. I need to add one more piece here. After the @context object and before my data starts, I want to add the at type key and specify the [schema.org](https://schema.org) type for this data, which is organization. So that's before company I'll add at type. and the value is going to be organization, and a comma at the end of that line. And I'll save this, I'll select it all, and copy it to the clipboard.
>
> **[3:10](https://www.linkedin.com/learning/json-essential-training/map-a-data-structure-with-json-ld?u=76281980&t=190)** And then I'll go to the JSON-LD playground online. And in the input box, I'm going to paste this. This checks for basic syntax errors, and I don't have any, so I'm good. Using this JSON-LD format lets me take data that I already have and indicate how it should be interpreted, but without needing to redo my own internal systems for tracking my data. And that's important, especially when I want to use the data for different purposes. And one set of keys is never going to satisfy all the different uses.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (8)
> **Env Vars:** json (7), url (5)
> **URLs:** [schema.org](https://schema.org) (5), [http://www.schema.org](http://www.schema.org) (1)
> **File Paths:** snippet.json (1)
> **CLI Commands:** make (1)
> **UI Navigation:** go to (1)
> **Analogies:** for instance (1)
> **Speakers:** - [instructor] (1)

#### [Work with JSON-LD generators](https://www.linkedin.com/learning/json-essential-training/work-with-json-ld-generators?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/work-with-json-ld-generators?u=76281980&t=0)** - [JSON](../../Skills/Web%20Development/JSON.md)-LD can be relatively straightforward to write for small snippets, but for larger content or content that relies on more than one vocabulary type automatic JSON-LD generators can save you a lot of time. Generators still rely on you to identify the content that corresponds to each key. But they generally provide an interface for common vocabulary types, where you can enter your data and then they generate the resulting JSON-LD snippet for you. H + Sport is hosting a 5K training run. The event will be listed online, and I want to create a JSON-LD snippet to make it easier for search engines and event aggregators to identify the relevant information and show it to people who might be interested. I haven't created JSON for this event and I'm going to try creating the JSON-LD directly using a generator. I found this JSON-LD generator using a web search, but if you have any trouble opening it, just search for generate JSON-LD in a search engine and identify a tool from the results. This generator supports a number of common categories of information. There's an event option, so I'll select that. On the left I have fields where I can enter my information and on the right is JSON-LD code based on the form. I can see that the event type is already part of the snippet. There's also a nested location array that includes a separate virtual location data type. In the files for this video, I have an empty 5ktraining.jsonld file, where I'm going to put my generated JSON-LD snippet. I also have a 5ktraining.txt file,
>
> **[1:34](https://www.linkedin.com/learning/json-essential-training/work-with-json-ld-generators?u=76281980&t=94)** which contains the text from the flyer. I'll use that to add content to the generator. So first up is the event name. And in my text, I have the event name, 5k training run. So I'll paste that in. Once I paste that in, I can see that over here, the name field is populated with the values pasted in. The description is also part of my flyer. So we'll copy that and paste it. And that's generated over here as well. Now I don't have data that corresponds to all the suggested fields here. For instance, I don't have an image URL. For attendance mode, I have the choices of online, offline, or mixed. And this is definitely going to be an offline event. It's not going to be online it's a training run. And the status is scheduled. So I have room to specify the start date and time and the end date and time. So my start date is June 13th, the end dates the same, and I have a little calendar widget here I can use to select that. Can actually use any date here. That's not super important just for trying this out, but let's go ahead and say June 13th for the start date and for the end date. And this is 12 noon to 2:00 o'clock PM. So for the start time, I need to use a 24 hour format. So that's going to be 12:00 and for the end time that's going to be 14:00 for 2:00 o'clock PM. The last thing I need to enter is the location. It's the last piece of information on my flyer that I haven't entered. So I'm going to copy that location Ramsett Park
>
> **[3:07](https://www.linkedin.com/learning/json-essential-training/work-with-json-ld-generators?u=76281980&t=187)** and put that in the location name. And now I've generated all of the JSON-LD for all of the information about my event. Each of the pieces of information I have has been tagged with one of the key names for the [schema.org](https://schema.org) event type. And so next I want to copy my JSON-LD code and paste it over into my file. Now this generator automatically generates opening and closing script tags for an [HTML](../../Skills/Web%20Development/HTML.md) file, but we're not putting this in HTML. We're going to keep this as just a JSON-LD snippet. So I want to copy everything except for the first and last line here. Everything but those opening and closing script tags. I'll copy that to the clipboard. And then over in my editor, in my 5ktraining.json.ld file, I'm going to paste in that code. Now I do notice that down here in the location section, I have all this information about the address that I haven't actually filled in. I just gave a name of a location. And so I'm just going to go ahead and take out this entire address section because it's not necessary for my data. And it doesn't really add much. And I need to take out the comma after Ramsett Park, because it's the last key in this sub object. And I'm going to save. And that is my completed JSON-LD snippet. Using the generator I was able to create a JSON-LD snippet for somewhat complex data without a lot of work. So that was a really great time saver. And now I'm ready to share this snippet wherever I need to.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (16), [HTML](../../Skills/Web%20Development/HTML.md) (2)
> **Env Vars:** json (15), html (2), url (1)
> **Ports:** :00 (4)
> **File Paths:** 5ktraining.txt (1), 5ktraining.json (1)
> **CLI Commands:** make (1)
> **URLs:** [schema.org](https://schema.org) (1)
> **Analogies:** for instance (1)
> **Speakers:** - json (1)

#### [Add a JSON-LD snippet to a web page](https://www.linkedin.com/learning/json-essential-training/add-a-json-ld-snippet-to-a-web-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/add-a-json-ld-snippet-to-a-web-page?u=76281980&t=0)** - [Instructor] [JSON](../../Skills/Web%20Development/JSON.md)-LD has a variety of uses. Perhaps the most common way that many of us consume JSON-LD content is on the web where JSON-LD snippets may be displayed as part of search results. Adding JSON-LD to a webpage uses the same script tags that are used for [JavaScript](../../Skills/Software%20Development/JavaScript.md). However, while the JavaScript file type is application/javascript, the type for JSON-LD is application/ld+json. An [HTML](../../Skills/Web%20Development/HTML.md) document contains two main sections, the head and the body. The head contains information about the page, while the body section includes the content that is rendered by a browser, or other user agent, and presented to the user. The JSON-LD goes in the head section. The snippet.jsonld file, for this video, contains a snippet for the H+ Sport Company. I want to add this to the product page for the company website, so search engines can use the information when indexing the site. So opening up the [products](../../Skills/Software%20Development/Microsoft%20Products.md).html file, at the very top of the file, I have a DOCTYPE on line one followed by the opening HTML tag, and then the opening head tag on line three. Several lines later, on line nine, is the closing head tag, which includes a slash before the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) head. My first step is to add a script tag, for my JSON-LD snippet, anywhere between the start and end head tags. I'm going to do that at the very end of the head section. So I'm going to add a new line, below line eight, just before that closing head tag. And I'm going to type script type=, and then in quotes, "application/ld+json".
>
> **[1:40](https://www.linkedin.com/learning/json-essential-training/add-a-json-ld-snippet-to-a-web-page?u=76281980&t=100)** Close the quotes, close my angle brackets, and then my editor immediately adds the closing tag for me, so I don't forget to type it. I'll press Enter, and I'll press Enter one more time. And now I can go up to the blank line, I can indent and now I'm ready to add my snippet. So I'll switch to the snippet.jsonld file, I'll select everything and I'll copy to the clipboard. And then, back in the products file, I will paste that in between those script tags. I got an extra blank line on line 23, so I'm going to take that out. The indent makes it clear that all of this content occurs between the opening and closing script tags. I will save those changes and now my web page includes a JSON-LD snippet, summarizing basic facts about the company, which can help search engines index the page. It's common for tools, that generate JSON-LD, to include the opening and closing script tags, automatically, in the code they generate. This let's you, simply, copy the generated snippet and paste it into the head section of an HTML document.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (11), [HTML](../../Skills/Web%20Development/HTML.md) (4), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (2), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Env Vars:** json (9), html (3), doctype (1)
> **File Paths:** products.html (1)
> **UI Navigation:** switch to (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Structure data with JSON-LD](https://www.linkedin.com/learning/json-essential-training/challenge-structure-data-with-json-ld?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/challenge-structure-data-with-json-ld?u=76281980&t=0)** (energetic music)
>
> **[0:08](https://www.linkedin.com/learning/json-essential-training/challenge-structure-data-with-json-ld?u=76281980&t=8)** - [Instructor] Ready to build your own [JSON](../../Skills/Web%20Development/JSON.md)-LD snippet? It's time for a challenge. The start code for this video includes an empty person.jsonld file. Your challenge is to add JSON-LD code that describes a person to this file. You can build a snippet that describes you, or you can pick a well-known person to build a snippet for. Your code should include at least three properties, so be sure to do research if necessary. Wikipedia displays a number of quick facts about famous people, so that can be a useful resource for this exercise. You can build your JSON-LD using the playground at [jasonld.org](https://jasonld.org), or using a generator, or you can create it from scratch. For an extra challenge, add a second level of data using a nested data type. You can refer to the person documentation on the [schema.org](https://schema.org) site to learn more about nested data that's available for a person object. This exercise should take you about 10 minutes. When you're done, join me in the next video, and I'll walk you through how I approached it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (3)
> **Env Vars:** json (3)
> **URLs:** [jasonld.org](https://jasonld.org) (1), [schema.org](https://schema.org) (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (energetic music) (1)

#### [Solution: Structure data with JSON-LD](https://www.linkedin.com/learning/json-essential-training/solution-structure-data-with-json-ld?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/solution-structure-data-with-json-ld?u=76281980&t=0)** - [Instructor] I could generate my [JSON](../../Skills/Web%20Development/JSON.md)-LD for a person using a generator, but I'm going to build it myself just to take it step by step what needs to be in the data, and how to put it together. So I'm going to go ahead and select Person in the JSON-LD playground. And I get a starting point here. I'm actually trying to create a person, so that's great. And I've got my context and my type already set because I am using the person type from [schema.org](https://schema.org). So I'm going to do my snippet on Nelson Mandela. And so I'm going to start by changing the name.
>
> **[0:46](https://www.linkedin.com/learning/json-essential-training/solution-structure-data-with-json-ld?u=76281980&t=46)** And I'm going to get to the job information a bit. I know I'm not going to have a telephone number. And I'm going to do the URL next. So I'm going to move that up after the name. I'm going to fix my indent. And I've got to make sure to add a comment here, and I have to take out the comma after the job title. And so for the URL, let me me look at the Wikipedia page, and I can just use this Wikipedia URL. Good a place as any. So I'll paste in that URL. And then I'm going to look at [schema.org](https://schema.org) at person. There's a whole bunch of different pieces of information I could provide here about a person. I think birthplace is a really good common thing to include, and we do have a BirthPlace property. So over in the playground, I'm gong to add BirthPlace. And for the value, I will look over in Wikipedia. We've got born and we got a description of the location. So I'm just going to grab this text here. And I'm going to put quotes in my snippet and paste that in. Take out that closing space. And put a comma at the end. Now, one of the other things that's notable about Nelson Mandela in particular is that he was the president of South Africa. So I want to include an occupation. And so looking through the person type,
>
> **[2:25](https://www.linkedin.com/learning/json-essential-training/solution-structure-data-with-json-ld?u=76281980&t=145)** we don't have an occupation option here. But I've poked through this a little bit, and there's a hasOccupation option, and when I click that, it tells me I can use Role as the property to talk about past professions and dates. So I'm going to go back to the page I was on and look for Role. It's not in person, and I don't see it in thing. But if I scroll down here to the bottom, I don't see it there either. So I'm going to do a search on this page for Role, and there's the reference. Use Role for expressing dates. And then all of the other uses seem to be just talking about the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) role. So let me do a broader search on [schema.org](https://schema.org). And I'll do a search in the search box here. See if I can find out about Role. So here we go. Role's another [schema.org](https://schema.org) type. So I'm going to open that up. And so here I'm going to be creating a sub-object it looks like. Now, this is a situation where it might be useful to use a generator to see how this is done but let's try it out. So back in the playground, I'm going to go ahead and take out jobTitle, and instead I'm going to put in hasOccupation, which was the key that was suggested by the description, and then for a role, this needs to be an object. And so within a sub-object,
>
> **[3:59](https://www.linkedin.com/learning/json-essential-training/solution-structure-data-with-json-ld?u=76281980&t=239)** the first thing we need to do is specify its [schema.org](https://schema.org) type. So I need an @Type, followed by Role. And then I want to add properties and values for Role. So looking over at Role, the properties specific to Role are roleName, startDate and endDate, and that seems like a great set of data to include. So let me go ahead and just put those properties in first. roleName. startDate. And endDate. All right, so then looking back in Wikipedia, got president of South Africa. So I'll go ahead and use that as the role name in quotes, making sure I have that comma after that, and then for the startDate, 10 May 1994. End quotes. And through 16 June 1999.
>
> **[5:07](https://www.linkedin.com/learning/json-essential-training/solution-structure-data-with-json-ld?u=76281980&t=307)** So I have basic information about Nelson Mandela, and then I have information about one occupation, which is a role and it has a roleName and a start and an endDate. I don't have any errors in the JSON-LD here. So the JSON-LD I've created here is valid. And again, I had to do a little bit of research and poking around on [schema.org](https://schema.org) to understand what was possible and how to actually insert this sub-object for hasOccupation and how to use the Role type. So again, in situations like these, especially when you're working with a new type of information, using a generator or looking at examples and seeing how things fit together can be really helpful. So I'm going to take out this blank space here that I don't need this empty line. Then I'm going to copy all this content to the clipboard and back in my files for this video, I'm going to past this into the person.jsonld file, and I'm going to save that. And now I have a JSON-LD snippet about a person.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (5), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Code Identifiers:** hasoccupation (3), rolename (3), startdate (3), enddate (3), jobtitle (1)
> **Env Vars:** json (5), url (4)
> **URLs:** [schema.org](https://schema.org) (6)
> **Definitions:** is a  (3)
> **CLI Commands:** make (1), find (1)
> **Cross-References:** go back to (1)
> **UI Navigation:** scroll down (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/json-essential-training/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/json-essential-training/next-steps?u=76281980&t=0)** - [Instructor] Thanks so much for joining me in this course. You now understand what [JSON](../../Skills/Web%20Development/JSON.md) is and a few of the many ways that it's used to structure and exchange data. If you want to learn more about programming check here in our library for a course on programming fundamentals. To learn more about how to share and access data on the web, try a course on APIs and web services. If you're interested in learning more about creating content, that's easy for search engines to index, check out a course on [search engine optimization](../../Skills/Data%20Science/Search%20Engine%20Optimization%20(SEO).md) or SEO. Feel free to follow me online. Now, take your new skills and try something new, good luck.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1), [Search engine optimization](../../Skills/Data%20Science/Search%20Engine%20Optimization%20(SEO).md) (1)
> **Env Vars:** json (1), seo (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [Sasha Vodnik](../../Instructors/Software%20Development/Sasha%20Vodnik.md)

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/json-essential-training-2887317)

## Skills Covered

- JSON

## Path Context

### In [Explore Web Development with Vanilla JavaScript](../../Paths/Software%20Development/Learning%20Paths/Explore%20Web%20Development%20with%20Vanilla%20JavaScript.md)
← [JavaScript- Modern Browser APIs](JavaScript-%20Modern%20Browser%20APIs.md) | **4 of 8** | [JavaScript- Ajax and Fetch](JavaScript-%20Ajax%20and%20Fetch.md) →

### In [Become a JavaScript Developer](../../Paths/Network%20and%20System%20Administration/Learning%20Paths/Become%20a%20JavaScript%20Developer.md)
← [Developing for Web Performance](../Web%20Development/Developing%20for%20Web%20Performance.md) | **11 of 13** | [JavaScript- Ajax and Fetch](JavaScript-%20Ajax%20and%20Fetch.md) →

## Appears In

- [Explore Web Development with Vanilla JavaScript](../../Paths/Software%20Development/Learning%20Paths/Explore%20Web%20Development%20with%20Vanilla%20JavaScript.md)
- [Become a JavaScript Developer](../../Paths/Network%20and%20System%20Administration/Learning%20Paths/Become%20a%20JavaScript%20Developer.md)

---

[↑ Back to top](#)