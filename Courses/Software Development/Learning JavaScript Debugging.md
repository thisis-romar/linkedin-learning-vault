---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: learning-javascript-debugging-2
url: "https://www.linkedin.com/learning/learning-javascript-debugging-2"
duration_minutes: 167
duration: 2h 47m
level: Intermediate
updated: 4/10/2023
learners: 2834
skills:
  - Debugging Code
  - JavaScript
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C4E0DAQEvTPISgiHW0A/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1601068625848?e=2147483647&amp;v=beta&amp;t=ehvmL_kf8fz0SnlMc2EOMyrvvKRLuldeLVYu40svwTU"
linkedin_topic: Software Development
learning_paths:
  - '[Advance Your JavaScript Skills](../../Paths/Software%20Development/Learning%20Paths/Advance%20Your%20JavaScript%20Skills.md)'
prev_courses:
  - '[Foundational JavaScript Security](Foundational%20JavaScript%20Security.md)'
next_courses:
  - '[End-to-End JavaScript Testing with Cypress.io](End-to-End%20JavaScript%20Testing%20with%20Cypress.io.md)'
path_nav: '[{"path":"Advance Your JavaScript Skills","position":8,"total":10,"prev":"Foundational JavaScript Security","next":"End-to-End JavaScript Testing with Cypress.io"}]'
path_count: 1
tags:
  - course
  - topic/software-development
  - skill/debugging-code
  - skill/javascript
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/Learning%20JavaScript%20Debugging.md)

![Learning JavaScript Debugging](https://media.licdn.com/dms/image/v2/C4E0DAQEvTPISgiHW0A/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1601068625848?e=2147483647&amp;v=beta&amp;t=ehvmL_kf8fz0SnlMc2EOMyrvvKRLuldeLVYu40svwTU)

# Learning JavaScript Debugging

> Whether you're new to coding or an experienced developer, bugs are an inevitable part of web development. Debuggers can make the task of finding and fixing those bugs much easier. In this course, learn how to work with a debugger to efficiently locate and resolve issues in your JavaScript projects. Instructor Joe Chellman provides a language-centered guide to dealing with the complexities involved

> [LinkedIn Learning](https://www.linkedin.com/learning/learning-javascript-debugging-2) | 2h 47m | Intermediate | 3K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (5 videos)
  - [Welcome](#welcome)
  - [What you should know](#what-you-should-know)
  - [Exercise files](#exercise-files)
  - [What's new in this update](#whats-new-in-this-update)
  - [Using challenges](#using-challenges)
- [**1. Understanding the Need for a Debugger**](#1-understanding-the-need-for-a-debugger) (5 videos)
  - [Introducing our friend, the debugger](#introducing-our-friend-the-debugger)
  - [Important jargon and concepts](#important-jargon-and-concepts)
  - [Debuggers in web browsers](#debuggers-in-web-browsers)
  - [The importance of a good editor and IDE](#the-importance-of-a-good-editor-and-ide)
  - [Why debugging AJAX can be tricky](#why-debugging-ajax-can-be-tricky)
- [**2. Debugger Basics**](#2-debugger-basics) (12 videos)
  - [Debugging without a debugger](#debugging-without-a-debugger)
  - [Leverage more of the Console API](#leverage-more-of-the-console-api)
  - [Stop and look around: Simple breakpoints](#stop-and-look-around-simple-breakpoints)
  - [Explore other breakpoint options](#explore-other-breakpoint-options)
  - [Challenge: Knowing when to stop](#challenge-knowing-when-to-stop)
  - [Solution: Knowing when to stop](#solution-knowing-when-to-stop)
  - [Step into, over, and out of functions](#step-into-over-and-out-of-functions)
  - [Find out where you are using the call stack](#find-out-where-you-are-using-the-call-stack)
  - [Monitor variables with watches](#monitor-variables-with-watches)
  - [Source maps to debug minified files](#source-maps-to-debug-minified-files)
  - [Challenge: Putting it all together](#challenge-putting-it-all-together)
  - [Solution: Putting it all together](#solution-putting-it-all-together)
- [**3. Debugging in Practice**](#3-debugging-in-practice) (12 videos)
  - [Debugging the use of a jQuery plugin](#debugging-the-use-of-a-jquery-plugin)
  - [Debugging a React application in Chrome](#debugging-a-react-application-in-chrome)
  - [Debugging on iOS using Simulator](#debugging-on-ios-using-simulator)
  - [Remote debugging on an Android device](#remote-debugging-on-an-android-device)
  - [Challenge: Other people's code](#challenge-other-peoples-code)
  - [Solution: Other people's code](#solution-other-peoples-code)
  - [Debugging a Node.js application in WebStorm](#debugging-a-nodejs-application-in-webstorm)
  - [Setting up the WordPress example](#setting-up-the-wordpress-example)
  - [Challenge: Debugging with an IDE](#challenge-debugging-with-an-ide)
  - [Solution: Debugging with an IDE](#solution-debugging-with-an-ide)
  - [Debug a WordPress theme in Visual Studio Code](#debug-a-wordpress-theme-in-visual-studio-code)
  - [Use a debugger as a learning tool on a live site](#use-a-debugger-as-a-learning-tool-on-a-live-site)
- [**Conclusion**](#conclusion) (2 videos)
  - [Next steps](#next-steps)
  - [Extra JavaScript resources](#extra-javascript-resources)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Welcome](https://www.linkedin.com/learning/learning-javascript-debugging-2/welcome?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/welcome?u=76281980&t=0)** - [Joe] Hi, I'm Joe Chellman, and welcome to Learning [JavaScript](../../Skills/Software%20Development/JavaScript.md) Debugging. As developers, our main job is to find ways to turn ideas into working [products](../../Skills/Software%20Development/Microsoft%20Products.md), apps, websites, art pieces, whatever. Knowing how to write code is a beautiful thing, with a computer doing exactly what we tell them to, or so we hope. But trouble can arise when the things we've written don't produce the results we're looking for. As cool a job description as we have, the actual work is often just trying to track down and fix problems in the code we've written or heaven forbid what someone else wrote. It can be really frustrating tracking down bugs, so every tool we can bring to bear is good for our physical and mental well-being, causing fewer face-shaped dents in our desks. In this course, we're going to go in depth on how to use a debugger to better understand your code, so you can find and fix issues more easily. Fortunately, these tools are pretty easy to use once you understand how they work, so it won't take long. We'll start by going over the basics, what software you need, what terminology you need to understand, and how each main feature of a debugger works. Next, we'll work through a series of examples. Like all software on the web, debuggers are constantly being refined to work better with the sites and projects all of us are building everyday. So, we'll look at how JavaScript debugging works in the context of jQuery, a [React](../../Skills/Web%20Development/React.js.md) project, mobile websites, [Node.js](../../Skills/Web%20Development/Node.js.md), and a [WordPress](../../Skills/Web%20Development/WordPress.md) theme. We'll also take a look at using an integrated development environment, an IDE, in a JavaScript project and what a difference that can make in terms of your productivity. Using a debugger is not the only way to better understand how your JavaScript works, but it is essential.
>
> **[1:32](https://www.linkedin.com/learning/learning-javascript-debugging-2/welcome?u=76281980&t=92)** So, let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (4), [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1), [React](../../Skills/Web%20Development/React.js.md) (1), [Node.js](../../Skills/Web%20Development/Node.js.md) (1), [WordPress](../../Skills/Web%20Development/WordPress.md) (1)
> **CLI Commands:** find (2), node (1), make (1)
> **File Paths:** node.js (1)
> **Code Identifiers:** jquery (1)
> **Env Vars:** ide (1)
> **Definitions:** is a  (1)
> **Speakers:** - [joe] (1)

#### [What you should know](https://www.linkedin.com/learning/learning-javascript-debugging-2/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/what-you-should-know?u=76281980&t=0)** - [Instructor] Let's talk about what you should know to get the most out of this course. You should understand [HTML](../../Skills/Web%20Development/HTML.md) and CSS at an intermediate level or so. Almost every example we're looking at here will be based in HTML, but we won't be talking through any of the specifics. For [JavaScript](../../Skills/Software%20Development/JavaScript.md), beginning to intermediate is good. You should be familiar with ES5, the version of JavaScript that runs in every environment out of the box. ES6 wasn't required to illustrate how debugging works, so I've left it out. Along with [Vanilla](../../Skills/Web%20Development/Vanilla.md) JavaScript, other JavaScript libraries and frameworks will appear, especially jQuery, [React](../../Skills/Web%20Development/React.js.md) and Node. We'll talk through the parts of these that we need to in order to understand the examples, but you don't need to be an expert in any of them to understand what we're doing. These examples could point you in some other directions you'll want to explore in a little more depth later.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (4), [HTML](../../Skills/Web%20Development/HTML.md) (2), [Vanilla](../../Skills/Web%20Development/Vanilla.md) (1), [React](../../Skills/Web%20Development/React.js.md) (1)
> **Env Vars:** html (2), css (1), es5 (1), es6 (1)
> **CLI Commands:** node (1)
> **Code Identifiers:** jquery (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)

#### [Exercise files](https://www.linkedin.com/learning/learning-javascript-debugging-2/exercise-files-2?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/exercise-files-2?u=76281980&t=0)** - [Instructor] The exercise files for this course are broken down by chapter and video. Some of the examples look similar when loaded in a browser but have some differences for the purpose of illustrating some debugging concept. A few of the examples will be best run through a local web server, which will involve copying files into the web root of your local server. When the time comes, we'll talk through those. One example targets [Node.js](../../Skills/Web%20Development/Node.js.md). So if you don't have that installed and want to follow along, you'll need to download it from [nodejs.org](https://nodejs.org). Otherwise you should be able to open the folder for a particular video and open the index.[HTML](../../Skills/Web%20Development/HTML.md) file in your browser of choice and proceed with the examples. I have a special note for [Windows](../../Glossary/Service/Windows.md) users. Please make sure to decompress or extract the exercise file's zip archive before you try to run any of the examples in a browser. Windows makes it possible if you just open this to browse a zip archive without extracting the files first, which is really nifty when you're working with say one file at a time. But the examples in this course load lots of related files. So if you try to use the examples that way, you'll have very unhappy pages with broken links all over the place and very little working as it should. So please extract the files first. The way you do that is by clicking the button that says extract up here in the toolbar if you need to and then clicking extract all. You can also do it by right clicking that zip archive right here on your desktop and choosing extract all. You'll get to choose a location where the files should be saved once they're extracted. And when that's finished, you'll end up with an exercise files folder which you can open and browse through normally.
>
> **[1:35](https://www.linkedin.com/learning/learning-javascript-debugging-2/exercise-files-2?u=76281980&t=95)** From there, you'll be able to open any of the files in your browser or editor as appropriate and expect them to work just as they should.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Windows](../../Glossary/Service/Windows.md) (2), [Node.js](../../Skills/Web%20Development/Node.js.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **UI Navigation:** open the (2), in the toolbar (1)
> **Exercise Files:** exercise files (2), exercise file (1)
> **File Paths:** node.js (1), index.html (1)
> **CLI Commands:** node (1), make (1)
> **URLs:** [nodejs.org](https://nodejs.org) (1)
> **Best Practices:** make sure to (1)
> **Prerequisites:** you'll need (1)

#### [What's new in this update](https://www.linkedin.com/learning/learning-javascript-debugging-2/what-s-new-in-this-update?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/what-s-new-in-this-update?u=76281980&t=0)** - [Instructor] This course has been updated since its original release. There are now several challenges in chapters two and three, the project chapters, to help reinforce what we're covering. These will all involve using the debuggers built into the browser and code editor in some fashion, helping us reason about our code to solve problems better. I hope you'll find them interesting and engaging. There are also a couple of updates to videos dealing with [JavaScript](../../Skills/Software%20Development/JavaScript.md) framework, [React](../../Skills/Web%20Development/React.js.md), and the editor visual studio code to account for updates in each that can impact working through the examples. As you proceed through the course, you may note that Chrome and its debugger look a little different in the original videos, but the locations of everything we're using are pretty much the same, and following along in the current version should not be an issue.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [React](../../Skills/Web%20Development/React.js.md) (1)
> **CLI Commands:** find (1)
> **Tools:** visual studio (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Using challenges](https://www.linkedin.com/learning/learning-javascript-debugging-2/using-challenges?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/using-challenges?u=76281980&t=0)** - [Instructor] This course includes several videos called challenges. Challenges are quick activities that give you a hands-on opportunity to practice and master what you're learning. I'll start each challenge by describing your core objectives for the activity. Every challenge involves sample files, so I'll tell you where to find them, and we'll go over anything you need to know about them before you start. I'll also give you a rough estimate of how much time it took me to complete the challenge. When you've completed a challenge, make sure to watch the matching solution video. In each solution, I'll show you how I solved the challenge and give you some of my thinking along the way. These challenges are a great opportunity for you to master what you've been learning. I hope you enjoy working through them.

> [!info]- Semantic Content
>
> **CLI Commands:** find (1), make (1)
> **Best Practices:** make sure to (1)
> **Speakers:** - [instructor] (1)


### 1. Understanding the Need for a Debugger

[↑ Back to Table of Contents](#table-of-contents)

#### [Introducing our friend, the debugger](https://www.linkedin.com/learning/learning-javascript-debugging-2/introducing-our-friend-the-debugger?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/introducing-our-friend-the-debugger?u=76281980&t=0)** - [Instructor] The tool we'll be using in this course is called a debugger, or sometimes a stepwise debugger. Almost every programming language has one of these, so once you get a feel for using one in [JavaScript](../../Skills/Software%20Development/JavaScript.md), you'll be able to take that knowledge and apply it directly to [PHP](../../Skills/Software%20Development/PHP.md), C, [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), whatever. My intention in this course is to teach you not how to write code, but how to use the debugger to better understand your code, so you can finish projects more quickly. A debugger is software that lets you closely inspect what's happening in other software in [Real-Time](../../Skills/Database%20Management/Real-Time.md). It's one of the most effective tools we have for finding and squashing bugs, and fortunately, in JavaScript, debuggers are quite easy to use. With many other languages, you have to install extra software to use a debugger. But the main engines we all use for JavaScript, web browsers, all have debuggers built in and ready to go. These aren't the only option for JavaScript, but for web work, they're just the ticket. As we'll see, you don't always have to use a debugger. If you've ever written any code, you've no doubt popped up alerts or logged console messages to help figure out what's happening in your code at a certain point. I'm not gonna tell you to stop doing that. For some kinds of problems, it's totally sufficient. But with a debugger, you can do all that basic stuff and other more advanced inspections too. You can pause the execution of your code and step through it line by line, function by function. And while it's stopped, you can look through every variable currently known to see what it contains. You can track particular variables over time, watch their values change or not change as you step through. You can even step back in time to see how variables changed at every step on the way to where you are. The first time you use a debugger, it can almost feel like magic. Overall, you end up spending much less time guessing, so you can spend more time thinking
>
> **[1:33](https://www.linkedin.com/learning/learning-javascript-debugging-2/introducing-our-friend-the-debugger?u=76281980&t=93)** and solving problems faster. And that's really what we're here for, right?

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (4), [PHP](../../Skills/Software%20Development/PHP.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Real-Time](../../Skills/Database%20Management/Real-Time.md) (1)
> **CLI Commands:** php (1), python (1)
> **Env Vars:** php (1)
> **Definitions:** is called (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)

#### [Important jargon and concepts](https://www.linkedin.com/learning/learning-javascript-debugging-2/important-jargon-and-concepts?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/important-jargon-and-concepts?u=76281980&t=0)** - [Instructor] When you start using a debugger, there are some important concepts and some important jargon that we need to go over to help you understand things better. The first is breakpoint. Now if you've had some exposure to [Responsive Web Design](../../Skills/Web%20Development/Responsive%20Web%20Design.md), this term may already have some meaning for you, but in the context of a debugger, a breakpoint is a place where you tell the debugger to stop execution of your code and do something. Usually for our purposes, that's just going to be stopping execution, suspending it so it doesn't go any further so you can look around at what's happening. Once you've stopped, you can start stepping through your code, and there are several ways that you can step. The first is to step over. This means to execute the line that you're on and step to the next one in the file that you're browsing, regardless of how complicated that line you're looking at might be. If it has a ton of function calls all on one single line, you'll just execute it and step to the next one. Next, there is step into, and just like it says, this will take you into the next function call on the line you're executing. Likewise, there is step out, which will take you from wherever you are and step you out of the function that you're currently executing, and this will all make sense when we're looking at an actual debugger. Next up is a watch. Now this does not refer to a timepiece, but rather to taking a look at a particular variable or an expression so you can watch it over time and have it in a specific place that you can always look at. Next up is the call stack. This is the series of functions that were called to get to the place where we have stopped and are actually looking around,
>
> **[1:34](https://www.linkedin.com/learning/learning-javascript-debugging-2/important-jargon-and-concepts?u=76281980&t=94)** and a call stack is made up of frames. That's just an entry in a call stack. It's usually a function call. Finally, there is IDE. That stands for integrated development environment, which is a piece of software that collects important things that you need when working as a programmer. This usually includes a powerful text editor and a debugger, among other features. We'll be looking at one example of a good IDE later in this course. That's a look at some of the important jargon and concepts that we'll be looking at later just so you have some idea of what we're going to see coming up next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Responsive Web Design](../../Skills/Web%20Development/Responsive%20Web%20Design.md) (1)
> **Definitions:** is a  (3), stands for (1)
> **Env Vars:** ide (2)
> **Cross-References:** later in (1), coming up (1)
> **CLI Commands:** make (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Debuggers in web browsers](https://www.linkedin.com/learning/learning-javascript-debugging-2/debuggers-in-web-browsers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/debuggers-in-web-browsers?u=76281980&t=0)** - [Narrator] In order to do some in browser debugging, we're actually going to need some browsers. So let's talk over the options that are available. No doubt, you're familiar with all of these on some level. But there are maybe some pieces that you don't know as well. So first of all, we'll talk about Chrome. It's pretty much the most popular browser in the world right now. You may not know that there's also a developer built called Chrome Canary. These are both provided by [Google](../../Glossary/Service/Google.md) and of course, they're free. The debugger that's built into chrome is called the developer tools. And that's what you'll be using on the desktop. If you're debugging a mobile device, there is a version of Chrome for [Android](../../Glossary/Sdk/Android.md) and you can connect the browser from your Android device to Chrome on your desktop using the remote debugging tools for Android. You can even connect iOS devices using a little more convoluted process called the RemoteDebug iOS WebKit Adapter, if that's of interest to you. Next up is Firefox. Firefox is from Mozilla, of course. And just like there's a developer version of Chrome, there's one for Firefox also called the Firefox Developer Edition. The debugger in Firefox is also called the developer tools. And there's also a Firefox for Android, mobile version which can be connected to the desktop version of Firefox. A lot of these browsers tend to follow similar processes for debugging. Next up, we'll look at Safari which of course is Apple's browser. Along with the shipping version of Safari, there's the Safari Technology Preview. Which you can download on the web and then it will be automatically updated through your App Store account. The debugger in Safari is called the Web Inspector. And if you're going to debug [Java](../../Skills/Software%20Development/Java.md) script
>
> **[1:33](https://www.linkedin.com/learning/learning-javascript-debugging-2/debuggers-in-web-browsers?u=76281980&t=93)** that's running in iOS, you have two options. One is the simulator which comes with [Xcode](../../Skills/Mobile%20Development/Xcode.md), used to be called the iOS simulator but now includes WatchOS as well. So it's just simulator. And then you can connect an actual iOS device to your Mac over USB. You'll be able to make a connection into Safari and use the Safari Web Inspector to debug the java script that's actually running on your device. And then finally, we'll look at [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md)'s browsers, Edge and Internet Explorer. Of course these are both free and available on all [Windows](../../Glossary/Service/Windows.md) machines. The debugger that's built into these browsers is called the F12 developer tools. So named because you invoke it by hitting the F12 key on your keyboard. Debugging mobile java script is done through Visual Studio. I have to admit, I'm not the biggest Windows user in the world, so this is not my area of tremendous expertise. But that seems to be best way to go about it. There are also the modern.ie project and [browserstack.com](https://browserstack.com) which give Mac users like me access to [Virtual Machines](../../Skills/Cloud%20Computing/Virtual%20Machines.md) and other instances of Microsoft Edge that we can run right in a browser to debug our projects. Both java script and just general websites. Microsoft has been offering the modern.ie project for quite a number of years now. Their partnership with BrowserStack is a little more recent and gives us access with a free trial account to those current versions of Edge. It's really nice. So those are the browser options that are available. Which one you use on a daily basis is a personal choice as with almost anything in development. But you're going to probably wanna touch
>
> **[3:08](https://www.linkedin.com/learning/learning-javascript-debugging-2/debuggers-in-web-browsers?u=76281980&t=188)** all of these at some point when you're checking your projects across browsers. So it's a good idea to get familiar with all of them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (4), [Java](../../Skills/Software%20Development/Java.md) (4), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (3), [Windows](../../Glossary/Service/Windows.md) (2), [Google](../../Glossary/Service/Google.md) (1)
> **Tools:** firefox (7), safari (6), visual studio (1)
> **Definitions:** is called (3), is a  (3)
> **Code Identifiers:** ios (5)
> **Env Vars:** f12 (2), usb (1)
> **CLI Commands:** make (1)
> **URLs:** [browserstack.com](https://browserstack.com) (1)
> **Analogies:** just like (1)

#### [The importance of a good editor and IDE](https://www.linkedin.com/learning/learning-javascript-debugging-2/the-importance-of-a-good-editor-and-ide?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/the-importance-of-a-good-editor-and-ide?u=76281980&t=0)** - [Instructor] The debugging tools we're talking about here are mainly in web browsers, but [Debugging Code](../../Skills/Software%20Development/Debugging%20Code.md) also means being able to understand it better and having a text editor or IDE that you like will give you a huge boost. Like all software, this is an area that changes a lot, but let's run through why a good editor is so important. Syntax highlighting is definitely top of my list. Code is instructions for a machine but it's also read by humans, ourselves and our collaborators, and being able to tell what the language keywords are at a glance is really helpful. All editors recommended here let you switch themes so you can get the colors just how you like them too. Looking at this, this is the editor that's just built into macOS. I have this really small [JavaScript](../../Skills/Software%20Development/JavaScript.md) file here. You can see I've got stuff like spell checking that I don't really care about and I can't really tell what anything is. Contrast that with the same file in Visual Studio Code. I can tell just at a glance what everything is here. All of my language keywords are highlighted in blue, the comments are green, strings are red. Much, much better. Most editors also include specific kinds of enhancements that go beyond reproducing exactly what we type. We type lots of punctuation in JavaScript, of course, as you can see here. Having an editor that manages quotation marks, parentheses, brackets, and all that kind of stuff saves keystrokes. For example, if I were to type a new function here. I type one paren and I get both of them there. They're automatically matched. If I delete that first one, they both go away. Likewise, my braces, if I start typing a string.
>
> **[1:35](https://www.linkedin.com/learning/learning-javascript-debugging-2/the-importance-of-a-good-editor-and-ide?u=76281980&t=95)** That kind of enhancement might seem rather small, but as you're typing lots and lots of code, it gets to be really, really helpful. Some editors also include tools like Emmet that can make writing blocks of [HTML](../../Skills/Web%20Development/HTML.md) and CSS a lot easier. For example, if I create a new file here, I'll switch it using the mode selector to HTML. I can type CSS-type syntax and you can see that I have this pop-up that says it recognizes this as an Emmet Abbreviation, so if I hit the Tab key, I get a whole bunch of nested divs just like that. There are abbreviations for CSS and HTML built-in right out-of-the-box, so that can make building a website a lot faster. Another thing that most editors do is opening a folder of files and treating them as a project. Visual Studio Code can do this. There's a giant file explorer, so I can see everything. The other nice thing about this is that once opened as a project, there's often a command to open any file in the project by name. In Visual Studio Code out-of-the-box on a Mac, that's Command + P and now I can start typing anything that I happen to know is in the project and it will show up. Really useful. Another big one is search and replace, especially across many files. Writing code or markup, we want not to repeat ourselves as much as possible. When it's time to change something in a bunch of different places, it's crucial to have search and replace that's easy to use and powerful. One specific thing that's usually important to me is regular expression support which is a way to describe what you're searching for
>
> **[3:07](https://www.linkedin.com/learning/learning-javascript-debugging-2/the-importance-of-a-good-editor-and-ide?u=76281980&t=187)** in a more abstract way. It's a big topic. There's a course and at least one really good book on the subject, but if you've never tried regular expression-based search before, I encourage you to look into it. I've been using BBEdit on the Mac for a long, long time and as much as I enjoy other editors, whenever I have to do multi-file, regular expression-based searches, I always go back to BBEdit. It's manual has an overview of learning regular expressions that I found to be really helpful in days of yore when I was first getting started with this stuff. This is an example of a really simple regular expression-type search. I turn it on by enabling Grep here, this checkbox, and then I get syntax highlighting for the special parts that are regular expressions. In this case, I'm looking for every script tag that starts with HTTP and I'm changing them to HTTPS. The scripts are gonna stay the same, but I'm just changing the path so they're reloaded over a secure protocol instead. This little bit of garbage here that you may not recognize is a regular expression that I can then substitute in the replacement, like this. What that means is that if I start back at the top of the file. Now, I can do a cleaver search and replace. I can do a Replace and Find all the way down and it switches all of these out. Now, of course, with only these five script tags, I could have done this in a much more simple way, but if you had some resources that you wanted loaded over HTTP and some that you wanted loaded over HTTPS for some reason, being able to restrict it in this way using regular expressions is really helpful.
>
> **[4:40](https://www.linkedin.com/learning/learning-javascript-debugging-2/the-importance-of-a-good-editor-and-ide?u=76281980&t=280)** That's just a very, very tiny example of something that could be worth looking into later. Let's talk about some of the available options for editors. Sublime Text is one that is wildly popular. It is technically paid, but it has a very generous free trial, very worth exploring. There are several open-source options that are also completely free. Visual Studio Code is the one we'll be using for the most part in this course, but there's also Brackets from Adobe and Atom from [GitHub](../../Skills/Software%20Development/GitHub.md). Finally, there's my old friend BBEdit, which I just love and always have to mention, but it is available on the Mac only. As with Sublime, it's a commercial product, but there is a very generous free trial that is basically unlimited. If you're a web designer or developer with a bit of experience, you probably already have an editor that you like, but you might not have tried an IDE. Editors like Sublime, Visual Studio Code, and other recent editors are closing the gap with more traditional IDEs with plugins and so forth, but with a purpose-filled IDE, you get a lot of extra smarts out of the proverbial box, both about JavaScript and about your own site or project. You open a folder in the IDE and it does a bunch of analysis on your code building a knowledgebase that it uses to help you. Some examples of the things that make an IDE great are debugging JavaScript everywhere. This means not just in the browser, but also on the command line or any place else that you might run into JavaScript somehow. Usually an IDE is going to be able to handle that. I should mention that IDEs are not the only place where you'll see at least some non-browser based debugging. Visual Studio Code, for example, has some quite robust tools built-in for debugging command line JavaScript.
>
> **[6:13](https://www.linkedin.com/learning/learning-javascript-debugging-2/the-importance-of-a-good-editor-and-ide?u=76281980&t=373)** Code completion is a feature that you'll see in many regular editors, but it's much better in IDEs most of the time. Because of the static analysis that an integrated development environment does on your code, not only will it be aware of JavaScript's language keywords and that sort of thing, but it will also know about all the code that you have written yourself. All the function names, all the classes, all that sort of thing and it'll be able to complete those names as you start to type them. Save lots and lots of keystrokes. It'll also know about any frameworks you've imported, jQuery, [React](../../Skills/Web%20Development/React.js.md), all that sort of thing. Mistake avoidance is another feature of IDEs that's particularly helpful. This can range from things like parameter hints which help you figure out what order the parameters might go in and what those values are for a given function, warnings for violations of strict mode or other things that you might only see when you load your JavaScript in a browser. An IDE can usually tell you those beforehand, and then other inspections and linting. Linting meaning error checks, checks against coding standards, that sort of thing. Full project navigation and search. Again, this is something that you'll often see to a certain degree in editors as well, but I'm talking about things that go beyond go to line or go to a certain file. There's also symbol inspection so you can go to a particular file where a certain function is defined, for example. Finally, refactoring assistance. This is like search and replace, but it's more context-aware and smart. It's one thing to rename a string, but it's another thing when your IDE can tell you, do you wanna do a search
>
> **[7:47](https://www.linkedin.com/learning/learning-javascript-debugging-2/the-importance-of-a-good-editor-and-ide?u=76281980&t=467)** and replace for this particular string in, say, just your code comments? Or just where it's used as a function name? That sort of thing. Here are some options for IDEs that you might want to explore. WebStorm and PhpStorm from JetBrains are commercial [products](../../Skills/Software%20Development/Microsoft%20Products.md) with very generous free trials. I happen to use PhpStorm in most of my work because I do JavaScript and [PHP](../../Skills/Software%20Development/PHP.md) development. PhpStorm basically wraps WebStorm with PHP as well. Komodo IDE from ActiveState is another very popular commercial product, very polished. NetBeans from Apache is totally free and then the Eclipse project from the Eclipse Foundation is also free. They're all worth checking out. If you've never used one before, checking out WebStorm or PhpStorm is what I would recommend first. Even if you can't afford to pay for them, the free trials will let you explore what the options are and then maybe help guide you into things that you will find useful with some of the other free options. That's an overview of some of the things that are great about editors and IDEs. It's a very personal choice and I recommend you check out all the options that are available and see what fits with you.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (9), [HTML](../../Skills/Web%20Development/HTML.md) (3), [PHP](../../Skills/Software%20Development/PHP.md) (2), [Debugging Code](../../Skills/Software%20Development/Debugging%20Code.md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Env Vars:** ide (9), html (3), css (3), http (2), https (2)
> **Tools:** visual studio (6), sublime (3), command line (2), atom (1), github (1)
> **CLI Commands:** make (3), find (2), php (2), grep (1), apache (1)
> **Definitions:** is a  (4), is an  (2)
> **Analogies:** for example (4), just like (1)
> **UI Navigation:** go to (2), checkbox (1)
> **Code Identifiers:** macos (1), jquery (1)

#### [Why debugging AJAX can be tricky](https://www.linkedin.com/learning/learning-javascript-debugging-2/why-debugging-ajax-can-be-tricky?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/why-debugging-ajax-can-be-tricky?u=76281980&t=0)** - [Instructor] When you're getting started debugging projects involving [AJAX](../../Skills/Web%20Development/AJAX.md), where your code is loading data dynamically, you might start to run into some trouble. Let's talk a little bit about what kinds of issues you might see and how to deal with them. There are a couple of main families of security issues you can expect to see at first. We're not gonna get super specific about what these are right now. I just want to make you aware of what you might you see. First is the same-origin policy. In a nutshell, this means that all of your assets and data need to come from the same place, the same place being sort of like the same server. Then there's CORS which is access control for sharing data across domains basically. If you just take an example [HTML](../../Skills/Web%20Development/HTML.md) file from the exercise files in this course, for example, and drop it into your browser, it'll be loaded with a file URL that looks like this. If you try to load data from another file or from the web from the scripts in that file, you'll almost certainly run afoul of one or both of these policies which can result in data not loading and errors filing up your [JavaScript](../../Skills/Software%20Development/JavaScript.md) console. So how, aside from frantic internet searches, do we deal with this? For basic debugging, the best way is to use a local web server. Once you're loading over HTTP from localhost or some other custom host name, you're safe from issues related to file URLs and, at least for a while, you won't have to deal with any same-origin or CORS issues. If you get to the point that you need to know more, you can refer to other videos in the library on those topics. If you don't already have a local web server, MAMP or XAMPP are easy to install options, and, as with everything in [Web Development](../../Topics/Web%20Development.md),
>
> **[1:32](https://www.linkedin.com/learning/learning-javascript-debugging-2/why-debugging-ajax-can-be-tricky?u=76281980&t=92)** there are countless other free options available as well. It's also possible to tweak settings in the browsers to get around some of these issues. I still recommend a local web server over any of these, but here are some of the things to look for in a few browsers. In Chrome, there is the disable-web-security flag. In Firefox, there is an advance preference called security.fileuri.strict_origin_policy. And then in Safari, you can look at Disable Local File Restrictions in the Develop menu. So to recap, when you're working with projects that involve AJAX, you're almost certainly going to want to load those using a local web server. It doesn't really matter what kind you use, but that is the best way to work around lots of problems. And if it turns out you need more help, there's lots of help available.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [AJAX](../../Skills/Web%20Development/AJAX.md) (2), [HTML](../../Skills/Web%20Development/HTML.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [Web Development](../../Topics/Web%20Development.md) (1)
> **Env Vars:** ajax (2), cors (2), html (1), url (1), http (1)
> **Tools:** firefox (1), safari (1)
> **Definitions:** means that (1), is an  (1)
> **Prerequisites:** getting started (1), install (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** strict_origin_policy (1)
> **Exercise Files:** exercise files (1)


### 2. Debugger Basics

[↑ Back to Table of Contents](#table-of-contents)

#### [Debugging without a debugger](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-without-a-debugger?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-without-a-debugger?u=76281980&t=0)** - [Instructor] In this video, we're going to look at basic debugging without a debugger using in-browser developer tools and the basics of the console API. So I'm looking here at an example file. This is from a fictitious business called hansel and petal. Just an example business we have for courses here, and I have this page with some available bouquets. The expected behavior is that if I click any of these flower images, it's gonna load in a little lightbox. If I click the images though, it loads the image directly, no [JavaScript](../../Skills/Software%20Development/JavaScript.md) happening here. So something is clearly broken and we need to find out why. To do that, we're going to open the developer tools. In Chrome, I can go to the view menu, developer, and open the developer tools. I can also use Command + Option + I on a Mac. And right now, it's loaded up over here on the right side. In this little dot menu, I have some various options for where I can place it. I can put it down here at the bottom, and I'm also going to make it a little bigger by clicking in those tools and hitting Command + Plus. Here we go. It's got a little bigger there. So the first thing I'm going to do is switch to the console, and we can see I have an uncaught reference error. The nice thing about this is that not only do I know what kind of error it is, and I can see a little call stack here, not that we know what a call stack is at this point, but we have some information, and most importantly, we can see the file where this error happens and the line number where it happens.
>
> **[1:34](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-without-a-debugger?u=76281980&t=94)** Now, if I click this, it opens up in the Chrome debugger, and I can see that here's my accidentally declared global. This is happening because I did not use the var keyword in front of this variable assignment, and I'm in strict mode. So the error isn't very nicely highlighted. So the error is very nicely highlighted, but let me show you something else. Over here in Firefox, I can turn on its developer tools by going into the tools menu, web developer, and toggle tools. It actually has the same key command as in Chrome, Command + Option + I here on the Mac. And I can see that I have a reference error again with a disclosable call stack that starts out collapsed. I have the file name and the line number, but then I also have this learn more link. This is pretty cool, it loads up documentation that refers directly to the error that I've triggered, and gives me some more information about it and how to solve it. That's a very nice bit of help that the Chrome console gives you that I haven't seen elsewhere. So switching back to Chrome, we're going to fix this error and reload the page to see that everything works. Now I could edit this directly in Chrome using local workspaces, but instead I'm going to switch to my editor. In this case, Visual Studio Code. So I'm going to use the go to line command up here, which is Control + G by default, and I'll go to line 309, just like it showed in the console. I'll add var before this variable assignment.
>
> **[3:09](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-without-a-debugger?u=76281980&t=189)** Now I can switch back to the browser and reload, and we should be in much better shape. Yes, so I can click those flowers and I have, and I have a slideshow that I can click through with the mouse or with arrow keys, loaded up in a nice lightbox. Okay, so we've gotten this to the point where there are enough problems fixed that the JavaScript can execute. Now let's go back to the file. We're going to add a couple of pieces of old school debugging that you might have used before, if you've never used a debugger before. Here in my colorbox method, this is jQuery, if you've never seen it before. And here, I've set up some swipe events. So basically this code loads up my lightbox, which is the Colorbox library for jQuery. And then I'm also tying into a TouchSwipe library because Colorbox doesn't support it by default, to enable swipe left and swipe right, so this colorbox will be usable on a mobile device. To make sure this works, I'm going to add an alert here in the swipeLeft method, and a console.log here in the swipeRight method. Now if you've done any debugging at all, you've probably used at least one of these methods before. So let's try them out. Alert will be swipe left, and the console.log will just be swipe right. Nothing too crazy here. I'll save the file, and we'll switch back to the browser and reload it. I'm gonna load the colorbox, and this TouchSwipe plug-in that I'm using can simulate touch events even if I'm using a desktop machine. So I can click and drag,
>
> **[4:44](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-without-a-debugger?u=76281980&t=284)** and here I got a swipe right logged to the console. If I swipe the other way, I get an alert or swipe left. So this kind of very very basic debugging let's you put things into your code that let you know, okay, I just tried something, I expected something to happen, did it actually happen? And those are different ways to see that yes, that thing actually did happen. As I say, if you've been playing with JavaScript for very long at all, you've probably used those before. But there are some other features of the console API that you might not have used, and we're going to look at those next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3)
> **UI Navigation:** go to (3), open the (2), switch to (2), toggle (1)
> **Code Identifiers:** jquery (2), swipeleft (1), swiperight (1)
> **CLI Commands:** make (2), find (1)
> **Env Vars:** api (2)
> **Tools:** firefox (1), visual studio (1)
> **Cross-References:** go back to (1)
> **Analogies:** just like (1)

#### [Leverage more of the Console API](https://www.linkedin.com/learning/learning-javascript-debugging-2/leverage-more-of-the-console-api?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/leverage-more-of-the-console-api?u=76281980&t=0)** - [Narrator] In this video, we're going to take a look at some other features of the console API that you might not be quite as familiar with. Here in my [JavaScript](../../Skills/Software%20Development/JavaScript.md) console, I can type console and as soon as I type dot, I get a list of all these methods that are available. There's a whole bunch of 'em. And of course, default JavaScript stuff as well. So you might not have used very many of these, and that's what we're gonna take a look at. Over here in Visual Studio Code, I'm going to jump down to the bottom of the file, one of the first things is that console.log doesn't just take a string as an argument. You can pass multiple arguments to it. So let's change this swipe left to console.log. We'll keep swipe left here but then we'll pass in a few other of these parameters so we can see what they are. Let's take direction and event. Okay, I'm gonna save this and switch back, I'll reload. Here's my light box, I'm going to swipe left and you can see that first I get my message, then I get the value of the first parameter and the value of the second parameter which in this case, is a JavaScript object that I can disclose to see all its contents. That's pretty cool. Now going back to the editor, I'm going to change this to console.error. I'll save and switch back, reload. Now if I swipe left again, you can see
>
> **[1:34](https://www.linkedin.com/learning/learning-javascript-debugging-2/leverage-more-of-the-console-api?u=76281980&t=94)** that this is now formatted as an error which of course, means it's red. But also, I have this reference that I can click to open my debugger and I have a call stack. It's nice to be able to insert messages like this into your code in spots where execution should never actually reach that point. And then you can find out what the conditions were under which that happened. Let's look at something else. I'm going to keep this console.log message. I'm just going to copy and paste it and this time I'm gonna change it to console.count. I'll save again and switch back, refresh. And now if I swipe right, you can see that I'm getting an automatic count next to the message. Normally, if I were just using console.log, I'm going to save and switch back and refresh. If the same thing happens more than once, I just get a little increment encounter here. So console.count is just another way of presenting how many times something has happened and this can be especially useful if you have lots and lots of console messages. But that particular one might be appearing at different places throughout a sea of other ones. Makes it much easier to track how often something has happened in a particular block of execution. Another feature of the console.log method is that instead of just passing a string as the first argument, that's just a simple string. I can pass this string with some special formatting [Tokens](../../Skills/Web%20Development/Tokens.md). And that will insert my other parameters
>
> **[3:10](https://www.linkedin.com/learning/learning-javascript-debugging-2/leverage-more-of-the-console-api?u=76281980&t=190)** into this original message. So if instead of saying, just swipe left and then having the other stuff barfed out afterwards, I can sort of put it together in a nicely formatted message if I'd like to. So the way that works is by using the percent sign in various tokens that you can look up in the console API for your browser of interest. %s means insert the next parameter as a string. And then, another one we'll look at is %O which we'll format that parameter as an object. So let's rephrase this message as swipe, this will be the direction, we'll say using this event formatted as an object, event. Like this. So I'll save and switch back to the browser, reload. And if I swipe left, here everything is grouped very nicely. So swipe left using this MouseEvent which is still disclosable and event at the end there. There are lots of features available through the console API. And aside from just looking through the Autocomplete that comes up in your browser's JavaScript console, you can also refer to the documentation that's available. These are available for Chrome, for Firefox, and for [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) Edge. A notable absence here is Safari. Apple used to have a console API page for Safari, but as of this recording, they don't have it. It's been quite a while since they've had it.
>
> **[4:44](https://www.linkedin.com/learning/learning-javascript-debugging-2/leverage-more-of-the-console-api?u=76281980&t=284)** I hope they bring it back at some point but for now, this is what you have. Fortunately, all the browsers tend to have pretty close feature parity as far as these API's go. Now let's look at one more thing related to formatting. I have a variable up here above my executing JavaScript which is called bigErrorStyle. And you may recognize this as a whole bunch of CSS. I'm gonna change this console message down here and I'm going to insert a special formatting token, %c at the beginning of this. So I'm gonna leave my message just as swipe right but a formatting token like this is supposed to be used with inline CSS like this. So I'm going to pass in bigErrorStyle. So basically what's going to happen is this message is going to be formatted using this CSS. This will be very entertaining, I promise. So I switch back to the browser, refresh. And I'll open up my color blocks and swipe right, and there we go, we get a giant message here in the console, way bigger than any error message you've ever seen before. The only time I've ever seen this in use in the wild is on [Facebook](../../Glossary/Service/Facebook.md)'s website where they tell you not to put anything into the JavaScript console. But you may enjoy using this in some of your own projects and I wanted to show it to you. So as we've seen, the console API has lots of features that you might not have touched and could be useful. There are a couple of places where it starts to come up short. If you're working with a lot of third party libraries, you really don't wanna be editing them unless it's an opensource library and you're a contributor. So adding console messages to those is really not practical.
>
> **[6:18](https://www.linkedin.com/learning/learning-javascript-debugging-2/leverage-more-of-the-console-api?u=76281980&t=378)** Nevermind that those files tend to be minified. So for example, we're loading jQuery here. This is a minified version of the file and it's a mess to try and read. This is good for performance but bad for readability. And very bad if you're going to try and insert a console message in there somewhere. Who knows where to put that? And that's the other main point. You have to edit the files and know where your messages would go or you'll just kinda end up putting them all over the place and guessing. Hoping you'll stumble on to the right location where that particular behavior that you're trying to fix might be happening. There are many kinds of problems that are just too hard to solve or at least too labor intensive to solve that way. So we're gonna leave the console API behind and step into debuggers next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (6), [Tokens](../../Skills/Web%20Development/Tokens.md) (2), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1), [Facebook](../../Glossary/Service/Facebook.md) (1)
> **Env Vars:** api (7), css (3)
> **Tools:** safari (2), visual studio (1), firefox (1)
> **Code Identifiers:** bigerrorstyle (2), jquery (1)
> **Definitions:** is a  (2), is called (1)
> **CLI Commands:** find (1)
> **Documentation:** the documentation (1)
> **Analogies:** for example (1)

#### [Stop and look around: Simple breakpoints](https://www.linkedin.com/learning/learning-javascript-debugging-2/stop-and-look-around-simple-breakpoints?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/stop-and-look-around-simple-breakpoints?u=76281980&t=0)** - [Narrator] In this video we're going to start using the debugger for real using breakpoints. So, first let's open up our developer tools with Command + Option + I here on the Mac. And I'm going to change the sources panel if I need to and open my index.[HTML](../../Skills/Web%20Development/HTML.md) file, which is where all the [JavaScript](../../Skills/Software%20Development/JavaScript.md) for this page lives. Let's scroll all the way to the bottom where my JavaScript is. Here we are. Now what I want to do is pause execution when I execute a swipe on this color box. So I'm gonna set a breakpoint right here inside swipe left, inside the callback function, right there. To set a breakpoint you just click in the gutter on the line number. Now, I can reload and we'll open up the color box. And I'll do a swipe left. Once I do that execution pauses. I can see a little message up top with a resume button and another message over here with another resume button that would let me resume execution, in other words, I've stopped now I can start again. And you'll notice that we have some new stuff highlighted over here in this right side panel. The main thing I wanna call your attention to right now is the scope area. This is where I can see all the variables that are currently defined and what their values are. This is called scope because there are various scopes available. In JavaScript in ES5 a function is what creates scope. So, every time you have a function call you have another layer of scope where there's another set of local variables.
>
> **[1:36](https://www.linkedin.com/learning/learning-javascript-debugging-2/stop-and-look-around-simple-breakpoints?u=76281980&t=96)** In this case we just have one local scope for this callback function, and then the global scope which is the window object and everything that's defined there. But if you're just using ES5 as we're using here you'll see scope anywhere there's functions. Okay, let's set another break point down here in the swipe right area. I'll let execution resume, and then let's swipe right. There we go I can see that I've stopped down here. And again, I can see the variables that are defined. So, now that we've stopped at either of these points I can see what's happening at that point in execution. Like right now I can see that the swipe direction was right and then if I resume and swipe left I can see here that the direction is set to left. Let's resume and refresh the page. Now you can see that the breakpoints are preserved across page refreshes, which is great. So, this will only work inside Chrome obviously. If I were to switch to another browser I'd have to set fresh breakpoints over there. But inside Chrome any breakpoints that I've set will stay the same even when I refresh the page. Now if I were to go and make an edit to the html file so that these script tags moved around or changed, they might shift around a little bit. The browsers do try to keep track of what you've done. And you might get lucky and have them stay in the right spot, but it's something you'll need to watch out for if you start making edits and then go back and check your breakpoints. Now these are breakpoints that don't execute immediately. They only execute when I do whatever this callback function is expecting. We can also set them on things that will execute every time, like say this variable assignment.
>
> **[3:08](https://www.linkedin.com/learning/learning-javascript-debugging-2/stop-and-look-around-simple-breakpoints?u=76281980&t=188)** So, I'll reload, and instantly we're stopped, we see here's my message here. And I can check out what is defined so far. Now you'll notice accidental global is currently undefined because I've stopped the line before the variable assignment actually happens. One thing that's worth noting with setting break points. I'll just resume here. Is that they can be set on executing lines. So, if I remove this breakpoint and then try to set one here on this comment. I clicked on line 301 but the breakpoint was set on line 309 which is the next executable piece of JavaScript. The way that different debuggers handle this sort of breakpoint movement will kind of vary. But it's something to watch out for. You won't always get the breakpoint exactly where you think if it's a place that is non-executing. One more thing about setting basic breakpoints. Now, as I said, any breakpoints that I set here in Chrome are not going to be carried over to the debuggers in other browsers or in other debugging environments like my IDE for example. But there is another way to do this. Gonna clear all these breakpoints out. And let's switch over to my editor. Here I am in the index.html file and if I click over here in the gutter it just selects lines but I can add debugger right here and save this.
>
> **[4:28](https://www.linkedin.com/learning/learning-javascript-debugging-2/stop-and-look-around-simple-breakpoints?u=76281980&t=268)** This is a special keyword that will trigger the debugger in any environment that supports JavaScript debugging. So, let's switch back to Chrome and refresh the page. You can see I have this debugger set here. So, if I open the color box and swipe left, there you go. I'm paused right there on debugger. Now this is nice because it lets me set a breakpoint that will be triggered anywhere. I'll resume this, and if I close the developer tools note that if I swipe left nothing happens. Breakpoints will only be triggered when the debugger is actually open like that. So when you're done with debugging statements like this, make sure to take them out before you ship your code. So, now we've seen how to set breakpoints in different environments and how you can look at the contents of variables at the time that you've paused. But this is not everything that you can do just with breakpoints by themselves. We'll be taking a look at some more features next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (5), [HTML](../../Skills/Web%20Development/HTML.md) (3)
> **Env Vars:** es5 (2), ide (1)
> **Definitions:** in other words (1), is called (1), is a  (1)
> **Warnings:** watch out (2), note that (1)
> **File Paths:** index.html (2)
> **CLI Commands:** make (2)
> **UI Navigation:** switch to (1), open the (1)
> **Analogies:** for example (1)

#### [Explore other breakpoint options](https://www.linkedin.com/learning/learning-javascript-debugging-2/explore-other-breakpoint-options?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/explore-other-breakpoint-options?u=76281980&t=0)** - [Instructor] breakpoints that pause execution are really useful by themselves, but we do have some other options for how we can stop and look around under different conditions. Let's take a look at some of those. Here's a fresh example that looks just like the ones we've seen before, but it's a little different, and has a small bug. Let's take a look at what that bug is. I'm going to open this colorbox, and now take a look at what's happening when I swipe in either direction with this image counter. So if I swipe left, I'm moving forward through the images, which is what I would expect, but then if I swipe right, I'm still moving forward through the images. So, we're not quite doing the right thing here, and we're gonna fix it. So I'm gonna open up my developer tools with command + option + I. And here in the sources pane, I'm going to open the index.[HTML](../../Skills/Web%20Development/HTML.md) file where my [JavaScript](../../Skills/Software%20Development/JavaScript.md) is, and scroll all the way down. So here's our new implementation. Instead of using swipe left and swipe right, like we've done before, we have a generic swipe callback, and we're using a switch statement inside this. Now eagle-eyed viewers might see a bug without any assistance, but bear with me. Also, we should never underestimate our own abilities to miss simple bugs from time to time. So, the problem is when we swipe right. Now in this very simple example, we could just set a breakpoint here under the right case, but we're gonna do things a little differently. I'm gonna set a breakpoint right inside the swipe method, and I'm going to right click my breakpoint, and I can see it's editable. So, this is kinda neat.
>
> **[1:34](https://www.linkedin.com/learning/learning-javascript-debugging-2/explore-other-breakpoint-options?u=76281980&t=94)** This is called a conditional breakpoint, and just like it says here, the breakpoint I've set here will stop only if the following expression is true, and so now it's up to me to determine what the condition should be under which this breakpoint should pause. So in this case, I know that the problem is when I swipe right, so let's check the direction. Here's the direction parameter which will be available in scope when we stop, and we only wanna stop when we're swiping right, because that's where the problem was. So it looks like this. Okay, now let's trigger the colorbox, and see what happens. If I swipe right, there we go. We stopped. I'll let this resume, and swipe left, and everything works just as we'd expect. No pausing. So there we go. We'll trigger this again, and you can see that the color has changed also. So now, if this were a more complicated bug, I could look around and see what else is going on, that might help me figure out what I'm doing. Now you can see, I just have the same colorbox method. It's always going next instead of previous, no matter what I do. So a pretty easy bug to fix, but if this were something a little more complicated, where you had a bunch of lines of code that you needed to look through, being able to use a conditional breakpoint for a situation like this could be really helpful. Now along with setting conditional breakpoints, you can also turn breakpoints on and off without just removing them. So once again if I right click, I can disable this breakpoint and now it fades out. Down here in the breakpoints inspector,
>
> **[3:09](https://www.linkedin.com/learning/learning-javascript-debugging-2/explore-other-breakpoint-options?u=76281980&t=189)** turning this check box on and off will do the same thing. So now if I resume and do some more swipes, you can see I never stop, because the breakpoint's disabled. This button up here is the deactivate breakpoint setting. Almost every debugger is gonna have something like this. If you have a whole bunch of breakpoints set, it might be a little bit tedious to go through and toggle them all on or off, or remove all of them. So if you turn this setting on, your breakpoints will be disabled and your code will execute no matter what. So let's set another couple of breakpoints just to test this. Turn this on. Now I can swipe, and swipe, and swipe and nothing happens. Let's remove these breakpoints, which I can do by right clicking and removing, or over here in the breakpoints inspector I can remove all my breakpoints. Here we go. And now let's try some other ways. I have two pause buttons here. The first one, if I just click this, it will pause at the first execution of any kind of JavaScript whatsoever. So if I click the close button, it opens up the jQuery colorbox plugin, which is minified, and it's paused wherever this click was handled. Now I can resume. That's just a one time thing, so if I wanted to do that again, I would need to hit pause once more and do something else, like click the right button. This other button sets up pause on exceptions, so if I click this, now any time an exception is triggered
>
> **[4:46](https://www.linkedin.com/learning/learning-javascript-debugging-2/explore-other-breakpoint-options?u=76281980&t=286)** and not caught, my code will pause. That's really useful if you're trying to track down all the exceptions in your code, and make sure that you handle all of them properly. You also have the option to pause even when exceptions are caught and handled, presumably properly, so let's turn that on and reload the page. I can see immediately, I'm paused. We have some exceptions that are fired inside jQuery itself, as the page loads. We can resume and there's another one. Just a couple of those. But if I turn this off and reload, no pause. So those exceptions were handled, and if we were writing jQuery ourselves, we wouldn't necessarily have to worry about that. There's a few other types of breakpoints that you can also set. These will vary widely across browsers. It seems like as of this recording, Chrome has the widest variety of breakpoints that you can set. It's probably one of the reasons that it's very popular among developers. If we look over here in the right pane, we have a couple of different kinds of breakpoints, aside from the regular ones. We have these XHR slash [Fetch](../../Skills/Web%20Development/Fetch.md) breakpoints, which are breakpoints that you can set, depending on [AJAX](../../Skills/Web%20Development/AJAX.md) type activity. Then there's DOM breakpoints. For these, I can use the elements inspector, which lets me see the state of the DOM as currently rendered. And I can say break on subtree modifications, attribute modifications, or node removal. So these are different conditions where something inside this DOM element changes, the attributes are modified, or if this element is removed entirely.
>
> **[6:22](https://www.linkedin.com/learning/learning-javascript-debugging-2/explore-other-breakpoint-options?u=76281980&t=382)** So if I set a breakpoint that way, it will now appear over here in my DOM breakpoints, and can be enabled and disabled just like a regular one. There's one more type of breakpoint I wanna show you, which is event listener breakpoints. That's down here at the bottom of the right panel, and you could see there's lots of different kinds here. These are breakpoints that will be triggered whenever JavaScript handles an event of a particular type. So for example, here under the mouse events. If I turn on the click breakpoint, anytime, any JavaScript anywhere on this page, handles a click event, I'll be paused by this click breakpoint. Notice that there are no other breakpoints set right now, but if I click, I'm paused. JQuery is handling this right now, and there it goes. So you can use this for any type of event. The list is huge. I find that in my own work, and I'm setting my own breakpoints most of the time, but it's nice to know that there are lots of other options available for different kinds of actions that you can take, if you're not necessarily sure where something is happening. You just know that it's happening in response to a particular event, or a particular type of action. So that's a look at some of the extended options that you have with breakpoints, and next, we'll be looking at some of the ways that you can move around after you've paused.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (4), [HTML](../../Skills/Web%20Development/HTML.md) (1), [Fetch](../../Skills/Web%20Development/Fetch.md) (1), [AJAX](../../Skills/Web%20Development/AJAX.md) (1)
> **Env Vars:** dom (4), xhr (1)
> **Analogies:** just like (3), for example (1)
> **CLI Commands:** make (1), node (1), find (1)
> **Code Identifiers:** jquery (3)
> **UI Navigation:** open the (1), toggle (1)
> **File Paths:** index.html (1)
> **Definitions:** is called (1)

#### [Challenge: Knowing when to stop](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-knowing-when-to-stop?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-knowing-when-to-stop?u=76281980&t=0)** (electronic music)
>
> **[0:06](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-knowing-when-to-stop?u=76281980&t=6)** - [Instructor] Here is our first challenge on using breakpoints. We're going to look at a page that has a couple of problems and use what we know about breakpoints to help diagnose what's wrong. Let's start by looking at the page and how to trigger those problems. This is a slightly enhanced version of the Hansel and Petal page that we've been looking at previously, but it has a couple of problems. First, we have a checkout panel over here that slides out when you click the checkout button. If I click that close button, it closes. That all works as we expect, but if I open it again and click this close this panel button, nothing happens. I'm expecting that both of them will work and they should both work using a single handler function. If I open up the developer tools, the code for that is down here at the bottom of the file in one of these script tags. Now, here we go. Here's my checkout panel and the event handlers that deal with it. The other problem that we see will come when we start opening our color box to look at these flowers. This all looks fine but if I close this, one of my flowers has disappeared. How mysterious. If I click again and close the color rocks, another one has disappeared. I could keep doing this all day. The point is that flowers are disappearing and we don't know why. If we open the developer tools again and scroll down to the script tags at the bottom of the file, we have the code that we've seen before
>
> **[1:39](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-knowing-when-to-stop?u=76281980&t=99)** and the code that deals with the checkout panel. So there's nothing here that seems to deal with that. What we need to do is set a breakpoint that will catch the code that does that wherever it lives. So again, here are the objectives for this breakpoint challenge. First we'll fix the checkout panel. There's a single event handler that is a single function that should work when either close button is clicked. So we want to find that handler, set a breakpoint there, and then diagnose what's wrong. For extra credit, you can open the file in your editor and fix it. Then we want to find the code that's causing the flowers to vanish. We need to set a breakpoint that will catch the code that modifies the DOM in that area, because it can be tricky to dig through a bunch of code to find out who's responsible for changes like that. So that's the brief. This challenge shouldn't take too terribly long. Five minutes or less. Good luck.

> [!info]- Semantic Content
>
> **CLI Commands:** find (3)
> **UI Navigation:** open the (2), scroll down (1)
> **Definitions:** is a  (2)
> **Env Vars:** dom (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (electronic music) (1)

#### [Solution: Knowing when to stop](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-knowing-when-to-stop?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-knowing-when-to-stop?u=76281980&t=0)** (electronic music)
>
> **[0:06](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-knowing-when-to-stop?u=76281980&t=6)** - [Instructor] Here's one possible solution to this breakpoint challenge. Let's look at the checkout first. I'm going to open up my developer tools and here in the sources panel, this is conveniently opened up to the script block at the bottom of the file because I was already looking at it. And here is my event handler that's dealing with these close buttons. I'm using the element query selector here to grab the button close class, which if we inspect this element, I can right click and inspect that. I can see that both close button up in the corner and the close button down in the panel both have that same class. So the intention here was to grab either one of them and have them both use this same event handler. If I set a break point in here, this close button is caught by that handler. So that one definitely works as we can see. But if I open this again, I can click this as much as I want and this never gets caught. The problem here is that we're using query selector where we really would want something that selects any of them. So we could try using query selector all to grab both of them but we can't add event listeners to both of them at once using that method. So let's look at the solution for this. I'm going to open up the developer tools over here on the solution page. If I open this file and scroll all the way down, here's the way that we can deal with this. Instead of adding the event listener on the buttons themselves, we can do what's called event delegation
>
> **[1:39](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-knowing-when-to-stop?u=76281980&t=99)** and add the listener to the panel. And then inside that function, I can check the event to see if its target matches the class that's assigned to both of those items. Everything else can be the same. What I'm doing here is I'm checking that if the event target doesn't match one of those close buttons, we just return out of the function, we don't do anything. That way clicks will still work on other elements in the checkout pane the way we expect them to. But now I can close this panel either way. I can send another breakpoint to watch it work. There we go. I paused when we do that and paused when we do this. So we can confirm they are definitely both being handled by the same handler function. Okay, so that's dealing with the checkout panel. Let's see what we need to do about these flowers. I'm going to inspect these as well and I'm going to set a special break point, one of those DOM breakpoints over here. There's different ways that we could do this. I could choose this last element because I've seen that it's the last one that disappears every time they go. So if I right click this and break on node removal, and then I click this, I can see that it's the improved .js file that has this mysterious set up breakpoint challenge function. This is what actually does the removal. So that kind of breakpoint catches it.
>
> **[3:17](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-knowing-when-to-stop?u=76281980&t=197)** But the second time I do this, nothing's caught. Another way I could do this is by selecting the parent of all the flowers. Right clicking and then breaking on sub tree modifications. So now anytime the sub tree of this node is changed and removal would certainly count as a change, I should stop as well. It takes me to the same place. So if I wanted to change that code or remove it, I now know where to do that. In this challenge, we looked at using regular breakpoints for code that we're already working with and know where it is, and DOM breakpoints that can direct us to code we'll need to edit when we don't necessarily know where to look.

> [!info]- Semantic Content
>
> **CLI Commands:** node (2)
> **Env Vars:** dom (2)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (electronic music) (1)

#### [Step into, over, and out of functions](https://www.linkedin.com/learning/learning-javascript-debugging-2/step-into-over-and-out-of-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/step-into-over-and-out-of-functions?u=76281980&t=0)** - [Instructor] In this video, we're going to try the various stepping functions that we can use to navigate our [JavaScript](../../Skills/Software%20Development/JavaScript.md) in the debugger once we're stopped. So I have my index.[HTML](../../Skills/Web%20Development/HTML.md) file open in my Chrome debugger, and I'm down here where my custom JavaScript exists, so I'm gonna set a break point right here at the very beginning of my custom JavaScript, so in this case, on line 311. Now I'll reload the page. Okay, I've stopped, and I'm right here on the first variable assignment for flowers here. Now I'm executing a little bit of jQuery. So the first stepping function that you'll probably use the most at first, is the step over, and this will step over particular functions to the next executable piece. The way the stepping functions will behave in different debuggers is gonna vary a little bit, but the general idea is the same. If there's a function that's executing on the line where you're stopped, step over will take you to whatever the next one is, so let's try it. First, we're going to step over the assignment of the flowers variable, and I can see now, instead of being undefined, it is now a jQuery object. So I can step over and flowerLinks will get assigned next, and then flowerOpacity. And I can just keep stepping like this, let's watch what happens. First, we're executing this fadeTo function, and then Chrome is jumping right down to the instantiation of the colorbox plugin. Let's switch over to Firefox and see how it behaves there. In Firefox, I have my developer tools open, and I'm in the Debugger pane. Let's open up the index.html file and scroll down
>
> **[1:34](https://www.linkedin.com/learning/learning-javascript-debugging-2/step-into-over-and-out-of-functions?u=76281980&t=94)** to line 311, where we were before. And I'll set a break point. Now let's refresh, and there we are, we're paused. So let's start stepping over things. Okay, we're going through the variable assignments. This is looking very much like the way Chrome worked. We get down to flowerLinks, and you can see that we stopped here on this line first, and then we hit the fadeTo method. Now unlike in Chrome, when I stepped over fadeTo, Firefox is processing the way that jQuery returns and goes back to the original flowerLinks object, and if I hit this again, now it goes down into the call backs inside the hover method, so this is a little different. It goes to one and then the other. Then we return back to flowerLinks, hit step over one more time, and then we get down to colorbox. So you can see the behaviors are a little different, and you may find that the way one debugger behaves is more suited to the way you think about how your code executes. Back here in Chrome, we're going to resume, and then start over by reloading. This time, instead of stepping over, we're going to use step into. This is going to step inside this executing piece of jQuery. So that's opened up the jQuery library for me automatically. And if I use the step out function, I can get out of whatever I've stepped into. There we go, so I went inside jQuery's dollar sign function, and then with step out, I got back out to where I originally started.
>
> **[3:07](https://www.linkedin.com/learning/learning-javascript-debugging-2/step-into-over-and-out-of-functions?u=76281980&t=187)** Let's go down a couple more levels. So I'm gonna step over to this fadeTo method here. Let's step into that. Here we are inside fadeTo, and here's my next executable function here, which is filter, let's step into that. All right, now we're down a couple of levels, and to keep track of where we are, we can use the Call Stack. We'll look at the Call Stack in a little more detail later, but you can see, here we are inside filter, and then here's fadeTo. So let's start stepping out again, and watch what happens to the Call Stack. Filter's gonna go away, and then fadeTo is gonna go away, as we step out. And now we're back where we started. Getting used to how the step in and step out functions work is really a matter of using a debugger for a little while. But if you ever get lost, and wonder what these buttons are going to do, the Call Stack can be a nice way to figure it out. Okay, let's look at one more way to step around. It doesn't involve any buttons at all. If I right click in the gutter, there's a Continue to here function, which will let me skip everything that happens between wherever I'm paused and this line, and the debugger will just jump right to that point. This covers all the major ways we can step through our code's execution that you'll see in any debugger. Step over, step into, and step out. Now you may notice, Chrome has one more button here which is simply called Step. This is a button that was recently added as of the time
>
> **[4:39](https://www.linkedin.com/learning/learning-javascript-debugging-2/step-into-over-and-out-of-functions?u=76281980&t=279)** I recorded this video, as a way to help developers who are used to older versions of Chrome. It modified the way that the step into function works. So if you've used these debuggers before, and step into doesn't work exactly the way you expect it to, this button is an alias to the old step into method. There's one more thing I wanna mention about navigating through all of this code base. In this example, I'm using the developer build which has everything non minified, it's much more readable. It's also much larger, and you wouldn't wanna ship this in a production website. For colorbox, we have the minified version here. Same thing with touchSwipe, and when you're in Chrome, you can use the Pretty print function to get another look at a minified version that becomes a little bit more readable. I still recommend going with the non minified developer versions of JavaScript libraries when you're in development, because it helps with your debugging a lot more. It makes those step into and step out functions work a lot better. So that covers the major points of navigating around once your execution has stopped.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (4), [HTML](../../Skills/Web%20Development/HTML.md) (2)
> **Code Identifiers:** fadeto (7), jquery (6), flowerlinks (4), floweropacity (1), touchswipe (1)
> **Tools:** firefox (3)
> **Definitions:** is a  (2), is an  (1)
> **File Paths:** index.html (2)
> **CLI Commands:** find (1)
> **UI Navigation:** scroll down (1)
> **Speakers:** - [instructor] (1)

#### [Find out where you are using the call stack](https://www.linkedin.com/learning/learning-javascript-debugging-2/find-out-where-you-are-using-the-call-stack?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/find-out-where-you-are-using-the-call-stack?u=76281980&t=0)** - [Instructor] In this video we're going to look at the Call Stack in a little more detail. And see how it's a little bit like a time machine. This example looks just the same but I've added a little be it a new [JavaScript](../../Skills/Software%20Development/JavaScript.md) for the purpose of illustrating the Call Stack. Let's scroll down a little bit. To this line. Where I have a function called goGetSomeData. Let's set a break point there and reload. Okay. So we've stopped right before this function is going to execute. Here's the Call Stack. I'm going to step into this function. We could see here we are inside goGetSomeData now. And as I'm stepping through this-- I scroll down to Scope. You could see my variables are starting to update. Returnables is now an empty array. And I have another function here called getMyReturnables. But before we get into that let's watch what happens when we click around in this Call Stack. I go back up, inside my anonymous ready function, here. To goGetSomeData. And then before that I'm into jQuery itself. Having functions look anonymous inside a Call Stack is not super helpful for debugging purposes. So let's resume this. And we're going to go into the Editor and rename that ready function. So, here in my Editor, this is what jQuery uses to fire a function as soon as a page is fully loaded and ready to go. Right now it's an anonymous function. So it appears in the Call Stack as Anonymous. Let's call it something else. Let's call it myReadyFunction.
>
> **[1:36](https://www.linkedin.com/learning/learning-javascript-debugging-2/find-out-where-you-are-using-the-call-stack?u=76281980&t=96)** I'll save this, switch back and reload. Everything looks the same. I still have my breakpoint in the same place but now instead of Anonymous I have myReadyFunction. This helps me orient myself when I'm stepping into an auto functions in the Call Stack. So, let's step into go get my data and try things again. So, I added goGetSomeData to the top of a Call Stack. Let's step down to this next function, called getMyReturnables. We'll step into that as well. You could see, here we go. We have getMyReturnables here at the top of the stack. And clicking through these I can see where I jumped into that next function in the stack. And also, my local Scope is updating every time. So, this is the local scope as of this piece of the Call Stack which is called a frame. So at this point in the execution when I was inside my ready function. These are all the variables that are defined in a local scope. Likewise, here in goGetSomeData I have a local scope for local variable and my returnables and then a closure which is another function scope for my ready function. And I can see what's defined. And I have access to inside this function from my original closure. And this works all the way down the Call Stack. So, I'm still operating inside my ready function and down inside a new local function with some new variables that are as yet undefined. So let's step through this a few times using Step Over. You can see down here in my local scope
>
> **[3:16](https://www.linkedin.com/learning/learning-javascript-debugging-2/find-out-where-you-are-using-the-call-stack?u=76281980&t=196)** that all these variables are updating. But still, the version of returnables that's here at this point is still an empty array. Which is just what we'd expect. But then. If I go back to get my returnables and step out. Now I'm back inside goGetSomeData and returnables is no longer and empty array. It's now these 10 items that I got from that for loop. So you can se that using the Call Stack I can go back in time to where a function was executed to see what the state of my data was at that point. And then return to where I am in the execution right now as I'm building up up that data that might be returned. Using the Call Stack can get a lot more complicated than this when you're dealing with a synchronous JavaScript. Your Call Stack can get much, much more complicated than this especially when you're dealing with [AJAX](../../Skills/Web%20Development/AJAX.md) and you have a synchronous pieces that are firing at different times. But this is the basics at how this works. And having seen this I hope you'll have a little bit better idea of how the Step Into and Step Out functions work. And how the scope tools can show you the state of your data at different times.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [AJAX](../../Skills/Web%20Development/AJAX.md) (1)
> **Code Identifiers:** gogetsomedata (6), getmyreturnables (3), jquery (2), myreadyfunction (2)
> **UI Navigation:** scroll down (2)
> **Cross-References:** go back to (1)
> **Definitions:** is called (1)
> **Speakers:** - [instructor] (1)

#### [Monitor variables with watches](https://www.linkedin.com/learning/learning-javascript-debugging-2/monitor-variables-with-watches?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/monitor-variables-with-watches?u=76281980&t=0)** - [Narrator] In this video, we're going to talk about watches which are a way to monitor variable values and expression values over time. So here in this example file, here's my custom [JavaScript](../../Skills/Software%20Development/JavaScript.md) at the bottom of the index.[HTML](../../Skills/Web%20Development/HTML.md). And I have some new functions here, some new data. So let's set a breakpoint here on line 325 for actionTime. And I'll refresh the page. So I've stopped at this point where this variable is going to be assigned. Now of course, when you wanna find what any particular variable is assigned to, you can look through the call stack. But depending on how deeply you have to go in here to find a particular piece, how many closures you have to go through, which part of the scope you wanna get into. Or how deep the data structure of your objects might go. It can be a little bit of a pain to have to hit all these triangles and go find the right thing. That's where watch expressions come in. So here in the watch expressions inspector, I can hit the plus sign to add a new expression to keep track of. Let's set one for this actionTime variable. So what this does is allow me to watch what's happening to this particular variable without having to look in the scope inspector. So if I step over that assignment, here it is, set to the current date and time. And now if I step down a little further, let's get a preview of where we're going here. actionTime is reset a little later on. So I'm going to need to swipe to see anything change
>
> **[1:35](https://www.linkedin.com/learning/learning-javascript-debugging-2/monitor-variables-with-watches?u=76281980&t=95)** and I'm also going to need to be paused for my watch expressions to work. So let's pause on each of these with a breakpoint. I'll call up my color blocks, and do some swipes. Here we go. actionTime is currently set to whatever it was set to up here. And now when I step over, it's going to be reassigned. And you can see it changed a little bit. And resume, swipe right. And I'm stopping here and it's going to be reset again when I step over. That was such a small change, you didn't even see it. Now the nice thing about watch expressions is that they don't just work for simple variables. You can put in entire expressions here. The way I usually find myself using this is digging into parts of objects. So object.property.value for example. Or you can call a method that's part of a variable like this. So for example, this is a data object so I can use toLocaleTimeString like this. I'll have to expand this a little more so we can see it. But this expression is evaluated against the current value of actionTime. So let's resume this and I'll do another swipe. And so, instead of having to look through this whole thing just to pick out just the time, I can use this to see it much more easily.
>
> **[3:09](https://www.linkedin.com/learning/learning-javascript-debugging-2/monitor-variables-with-watches?u=76281980&t=189)** Let's try this in another context. I'm gonna scroll down to my goGetSomeData function down here. Now set a breakpoint where returnables is assigned. Let's reload and here we are. I'm going to add a watch expression for returnables. Which currently is an empty array. Now let's step into this getMyReturnables function. This establishes a new scope and I'm defining a local variable for the getMyReturnables function which is also called returnables. Let's step through this a little bit. Of course it starts out as an empty array just like it did up here. I'm gonna step through this a few times, add some things onto the array and you can see my watch is updating. If I bring up my call stack and go back to goGetSomeData, you can see, returnables updated to the value as of this point in the call stack. So the watch expression stay updated based on my current frame in the call stack. If I step out again, I'm back up to goGetSomeData. getMyReturnables has returned and assigned returnables to that full set of data in the array. And of course, if I wanted to just look at a little piece of this, I could use the slice method which is available on any array, and I'll pass in zero and one to pull the first item off the front of the array. So if I were in a situation where an array
>
> **[4:43](https://www.linkedin.com/learning/learning-javascript-debugging-2/monitor-variables-with-watches?u=76281980&t=283)** was being modified over and over again and I wanted to monitor what the first item was or some other item somewhere in the array, anything that I can write as a JavaScript expression can be evaluated as a watch expression. This can be really useful if you don't need a variable in your code to track something. But you wanna track it just for the purposes of debugging. So that's a look at watch expressions. If can be much handier than having to dig through the scope inspector to find particular pieces of data that you're gonna watch over time as your code is executing.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Code Identifiers:** actiontime (5), gogetsomedata (3), getmyreturnables (3), tolocaletimestring (1)
> **CLI Commands:** find (5)
> **Analogies:** for example (2), just like (1)
> **Definitions:** is a  (1), is an  (1)
> **File Paths:** index.html (1)
> **UI Navigation:** scroll down (1)
> **Speakers:** - [narrator] (1)

#### [Source maps to debug minified files](https://www.linkedin.com/learning/learning-javascript-debugging-2/source-maps-to-debug-minified-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/source-maps-to-debug-minified-files?u=76281980&t=0)** - [Instructor] In this video we're going to look at source maps, which are tool you can use to map the minified production ready versions of [JavaScript](../../Skills/Software%20Development/JavaScript.md) files, to their corresponding, more readable development versions. This is a feature that is likely enabled out of the box in your debugger of choice, but just to make sure, we're going to look at the settings here in Chrome. So here in the contextual menu we're gonna open up the settings. If I scroll down a little bit, here under sources. Enable JavaScript source maps is checked. That is what I need. So depending on the browser that you're using, and the particular settings that you have in that browser, this example might need a little bit of tweaking before it's ready to use. What we're going to look at here is the source map that's associated with jQuery itself. It used to be that the online version of jQuery had a source map built into it. They're not doing that anymore. I guess people were getting a little bit confused by it. So, instead of calling the online version and then using the local version as fall back, we're just using the local version here. Which I've updated to include its source map. So now let's set a break point. We'll just set it on the first line and we'll reload to get started. And one more note is that most JavaScript compilation and minification tools like Webpack or Rollup and so forth, will provide a way to generate source maps. So for testing production builds of your own code where you control everything. You won't have to do this juggling of jQuery that I did here. Over something like jQuery that we didn't build ourselves, this is the way it goes.
>
> **[1:34](https://www.linkedin.com/learning/learning-javascript-debugging-2/source-maps-to-debug-minified-files?u=76281980&t=94)** So I'm going to step over, and I think I'll step into find here. And you can see here's jQuery, it does not say that it's minified. But down here it says that it's source mapped from the minified version. We'll just confirm here. We are loading the minified version of jQuery, but it has a reference inside to a source map which tells it where every function is defined and where you can find it in the non-minified version. So we can see this here, and you can also see, if we look in the source panel, which we could do by hand, I happen to know where to find it, it's down here in the petal folder and then under js. But we could also find this by right clicking and choosing reveal in navigator. This is a great way to find a file that you're working on if you just get lost, if you're opening a lot of files in your debugger. And you can see that this file name is italicized here. This is the version that's actually referenced in the file, and then this italicized version is called up because we're using a source map. So this allows me to step through my code, using this method which I know is called find. But in the minified version it's called something crazy because everything is minified and obfuscated. But this makes it much more obvious what's going on. Now there's a tricky thing about using source maps related to watch expressions. So let's step down a little bit. And at this point, I have some variables called i, ret, self, len,
>
> **[3:08](https://www.linkedin.com/learning/learning-javascript-debugging-2/source-maps-to-debug-minified-files?u=76281980&t=188)** and I can see from these little hints, and from the scope pane down here that the values for these are all known in some way, except for i which is currently undefined. But if I set watch expressions for these, I'll get different behavior. So let's try len, not available, how bout self? That's window. Right down here it's a jQuery object. So things are acting a little bit differently up here and the reason for that is because the watch expressions are evaluated against the currently executing code. Without respect to the source map. So in the minified version. There doesn't happen to be a variable which is actually called len, it's probably called something really short like x or I don't know, something else. So we can't really use watch expressions when we're working with source maps, or at least we can't expect them to be reliable most of the time. Now this is something that debuggers may become a little more clever about at some point. But for now it's something to watch out for. I would still recommend in general that when you're working in development that you just reference the non-minified versions of any libraries or code that you wanna be able to step through easily and have the full compliment of debugging tools and knowledge available to you. And then when you're ready to ship switch to the minified versions. But if that's too burdensome for some reason, or if you just only happen to have the minified versions available, you can look and see if there's a source map available as well that can help you keep your debugger in sync. So, source map support is available in almost any debugger, and if you do have it available it's quite useful.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3)
> **CLI Commands:** find (6), make (1)
> **Code Identifiers:** jquery (7)
> **Definitions:** is a  (2), is called (2)
> **UI Navigation:** scroll down (1), switch to (1)
> **Warnings:** watch out (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Putting it all together](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-putting-it-all-together?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-putting-it-all-together?u=76281980&t=0)** (upbeat electronic music)
>
> **[0:06](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-putting-it-all-together?u=76281980&t=6)** - [Instructor] In this challenge, we're going to look for and solve a few problems with another version of the Hansel and Petal page using the techniques we've covered so far. On this page, we have a little modal pop-up that appears to ask customers to join the mailing list. I'm going to reload this to make sure we can see it. After a couple of seconds, there it goes. We want it to fade in and out nicely and be dismissible via its Close button here or the Escape key. And once it's been displayed to the user, we want to make sure that they don't see it over and over again. So that's what we expect, but there are a few strange behaviors happening here. First of all, when I close the modal, it disappears way less gracefully than it appears. The modal is made with a couple of simple elements for its overlay and content with the animation coming from CSS transitions. You can look at the challenge.CSS file in the exercise files to see the active styles. But while you'll probably want to look at them, you should not change them for this challenge. Second, every time I load the page, the modal keeps appearing. So I just refreshed it. There it is again. Refresh again, and there it is. On a real site, we'd probably set a cookie to stop this, but cookies don't work on local files. Your page has to be served from a domain, even local host on a local web server for those to work. So to make sure this works without having to involve a server, we're using the sessionStorage API, which is another way to store data in the user's browser. The data are deleted when you quit the browser or close the window or tab. It works similarly enough for our purposes here. If I open up the developer tools here in Chrome,
>
> **[1:39](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-putting-it-all-together?u=76281980&t=99)** I can switch over to the Application Inspector to see the sessionStorage here or cookies, if I were using them. Of course, file URLs do not store cookies. So here we are, we're using the sessionStorage API, and we can see a key called modalViewed set to true. Finally, close the developer tools again, if I try to fill out this form, the modal disappears. We currently have a model that asks for the user's email address, but it doesn't stick around long enough to actually receive it. On a related note, yes, this is a hint, clicking regular links doesn't do anything either. There's only one file that needs to be changed for this challenge, challenge.JS in the exercise files. So I'll open up the developer tools and here in the sources pane I can see it over here in the sidebar, but if yours isn't open like this, you could hit Command or Control + P and open the file this way. And you can also open it in your editor of choice. I have it open here in Visual Studio Code. This is code that we're picking up for the first time and when this happens in a professional setting, ideally you'd see some comments and maybe get a walkthrough from whoever originally wrote it. Because this is an exercise in finding problem areas, the comments are brief and we're not going to go into too much detail. Don't want to give away everything yet. But let's take a quick tour. At the beginning of the file, we have this check for the modal viewed key in the sessionStorage. It's still labeled cookie because that is ideally what we'd really be doing. And if we see this key, we're supposed to return and not execute anything else.
>
> **[3:13](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-putting-it-all-together?u=76281980&t=193)** After that, we have a timeout that will make the modal display. We have an event listener for the key up event that will close the modal when we hit the Escape key, another one for clicking the Close button, and finally, the helper function, which is called from both of these that will actually hide the modal. Now let's recap the objectives for this challenge. First, we're going to fix the timing on the animation or transition when the modal closes. Remember you aren't editing CSS for this challenge, only [JavaScript](../../Skills/Software%20Development/JavaScript.md). Also, as a little hint, the transition stops when the element is hidden by the display property. Next, we need to stop this modal from appearing every time the page loads. This will involve checking how our sessionStorage is being used and subsequently checked. And finally, the modal disappears when we click the email field. In fact, clicking just about anything makes the modal disappear. This challenge should take roughly 10 minutes to complete. See you back here when you're finished for a solution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Code Identifiers:** sessionstorage (5), modalviewed (1)
> **Env Vars:** css (3), api (2)
> **CLI Commands:** make (4)
> **Definitions:** is a  (1), is an  (1), is called (1)
> **File Paths:** challenge.css (1), challenge.js (1)
> **UI Navigation:** in the sidebar (1), open the (1)
> **Exercise Files:** exercise files (2)

#### [Solution: Putting it all together](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-putting-it-all-together?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-putting-it-all-together?u=76281980&t=0)** - [Narrator] Okay, let's take a look at a solution to this challenge. We're trying to solve three issues. Let's take them one by one. First, we'll deal with the timing of the closing of the modal. So if I refresh this page to make it appear again, here it goes and close and it disappears super fast. So let's open the developer tools and here in the sources pane I'm going to open my challenge.JS file and my challenge.CSS file. In these styles, I can see that we have a transition set for the modal container, that's the overlay where its opacity changes over the course of one second, and then the inner portion with the actual form changes over two seconds. And we have a couple of classes here that will toggle the opacity. And we also have this display block, which defaults to none, so the overlay doesn't block anything on the page. All right, what we're dealing with is how the modal closes. So I'm going to go down to the helper function called hide modal, and we'll set a break point here to see what happens. I'll refresh and the modal is going to appear here. And now when I close this, we're stopped. So the first thing that's going to happen is we're going to remove the visible class from that container. If we step over this, it fades pretty smoothly, I can see in the background there.
>
> **[1:33](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-putting-it-all-together?u=76281980&t=93)** It didn't do that when we were not paused. And then here's our set timeout function that's going to remove that open class, which is what toggles between display block and display none. Okay. So having set that break point and paused, we can see that the behavior is different. In other words, if we're waiting longer, we get the fade that we're expecting. So what that tells me is that this timeout function is firing too quickly. And in fact, it is firing after only 100 milliseconds. We're toggling the display from block to none, just 100 milliseconds after we start this transition, and it's set to take quite a bit longer than that. So what we need to do is wait longer, make this larger. So back here in my editor, and I'll scroll down to that same helper function. Let's change this to 2100 milliseconds, 2.1 seconds. That'll make sure that all those transitions are taken care of before we remove that class and take the overlay off the page entirely. Okay, so I'll remove that break point and refresh. We can see that the code has updated here. And now if I close this, there we go. Smooth, just like when it opens. Let's look at the gate that should stop this thing from appearing every time the page loads now. That's going to involve the session storage
>
> **[3:06](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-putting-it-all-together?u=76281980&t=186)** up here at the top. So first we're going to check the application inspector. Down here, under session storage we can see that we have this modal viewed, which is set to true, okay. And over here we are checking when the page first loads for that value, it certainly looks like this should be working, but it's not. So let's set a break point and we'll add a watch expression to help us see this. So I've copied that line, I'll paste it here. Let's hide this sidebar over here that we don't need so we can make our sidebar over here a little larger. All right, load again. Now back in the application inspector, let's delete this so we can start over again totally clean. Okay, reload the page. Here we are, we're stopped. And just like I expect this first time I've loaded this, our modal viewed key is no. So if I let this play, there we go, we've displayed this. Now we can close it and let's refresh. Now we have a value here. But even though this looked correct over in the application inspector, says the value is true, I can see here in my debugger, looking at my watch expression that this is not true, the bullying, this is true, the string.
>
> **[4:40](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-putting-it-all-together?u=76281980&t=280)** This is a little trap that you might fall into when you're using session storage or local storage. These values are stored as strings. And if you pass it in a different kind of data, it's going to be cast to a string automatically. So using a strict equality check like this is going to fail. We need to change this so we're setting values that we actually want and we're checking them correctly. So back here in my editor, I will scroll up to the top. And what we want to do is check whether this is a string. And so we're being very clear about our intentions. Even though this does get converted to a string automatically, we'll still set this to a string and then save. Okay, let this run out. Probably don't need the break point anymore. We've displayed, now let's refresh the page and no modal appears, okay. We can clear it one more time and refresh just to make sure it really worked. There it goes, here's our key. Close it again, refresh, very good. We know that's working now. Okay, the final thing that we need to deal with. I want to make sure this appears again. So I'll delete that and refresh. The next thing that we need to sort out is how this disappears when we click that field like that. I'm going to have to keep deleting this now, I guess. So there's a couple of ways we could look at this.
>
> **[6:17](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-putting-it-all-together?u=76281980&t=377)** We could set a break point inside our click event listener to see why things are going wrong here. Refresh the page to bring that model back. Click here, and I can see that my click event handler is firing even when I click here. Okay, that doesn't seem good, that disappears. Let's click somewhere else. Still being handled by that same function. This thing is a little overeager. I'm looking at this, we can see that it's because indeed we've added this to the document. So the entire page is listening for click events and responding to them, trying to hide this modal. We have nothing inside to check what was clicked. Really, we don't need this to be added to the document, we need this to be added to the close button. So we'll delete this key one more time, refresh the page. Let's right click and inspect this item so we can see, okay, what we really want is for this to be attached to the modal close button. So back here in the editor, I'm going to scroll down to my click event handler here. I'm going to change this from just document.add event listener to document, and we'll use query selector to get that modal close button and add the click event listener to that. In fact, because we have this model container
>
> **[7:49](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-putting-it-all-together?u=76281980&t=469)** available in a variable, instead of using document, we could restrict this to the modal container. Get a little auto-complete love there from visual studio code. Now I can save this. This is going to add that event listener only to the element that I actually want to fire it. Very good. Let's switch back and we can refresh this. Of course, I have to clear out my key here. Refresh. There we go. And now, if I click the field, that works fine. Click anywhere else in the modal, click out here, all good. But if I click the close button, away, it goes. Very good. Now we've taken care of all three points. In this challenge, we used different techniques that we've covered so far, break points in different areas, stepping through our code, watch expressions to help us make clearer things like what the application inspector couldn't tell us. And generally did some debugging and reasoning about this page to solve several problems.

> [!info]- Semantic Content
>
> **CLI Commands:** make (7)
> **UI Navigation:** scroll down (2), open the (1), toggle (1), scroll up (1)
> **Definitions:** is a  (3), in other words (1)
> **File Paths:** challenge.js (1), challenge.css (1)
> **Analogies:** just like (2)
> **Env Vars:** css (1)
> **Versions:** 2.1 (1)
> **Tools:** visual studio (1)


### 3. Debugging in Practice

[↑ Back to Table of Contents](#table-of-contents)

#### [Debugging the use of a jQuery plugin](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-the-use-of-a-jquery-plugin-22149299?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-the-use-of-a-jquery-plugin-22149299?u=76281980&t=0)** - [Instructor] In this video, we're going to start putting together everything we've seen so far about debuggers and fix a bug in the usage of a jQuery plugin. Here's the page that we'll be troubleshooting. It's called Lettering.js Test, and it looks a lot like a very, very simple blog. Most CMSs don't let you insert markup into the titles of posts, so if you want to sort of art direct your titles and make parts of them look different, you have to come up with another option. There's a jQuery plugin called Lettering.js that can help with this. The goal is to make certain words look different from the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the title. Also included in the exercise files is the Lettering.js example page that I've enhanced a little bit to include a very fancy treatment of the plug-in name here using the plugin. This will let us see the expected behavior and kind of check out how it works. So, if I use command option U, or control alt U on non-MacOS platforms, we open the source. We can scroll down to the name of the plugin here, Lettering.js. You can see here that there's nothing other than these letters that spell out the logo's name in the source, and yet on the page, each of them is styled differently. So if I inspect this by right clicking and choosing inspect, I can see that what's happening is the plugin inserts a span tag around each letter in this case, and then there's CSS for each one that will style them however you want. That's the sort of behavior that we're expecting here on our test page, but it's not working. So let's see if we can figure out why. We'll open up the developer tools. And here on the sources pane,
>
> **[1:32](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-the-use-of-a-jquery-plugin-22149299?u=76281980&t=92)** we can start to look things over. First, I'll open the index.[HTML](../../Skills/Web%20Development/HTML.md) file. We'll scroll down to where the script tags are. Here's jQuery, and then the Lettering.js plugin, with its jQuery prefix that helps us know that it's a plugin for jQuery, and then here's our main.js file, which should have the custom [JavaScript](../../Skills/Software%20Development/JavaScript.md). So I could open that over here in the sidebar by clicking this. But also in the sources pane, I could use command P, or control P on [Windows](../../Glossary/Service/Windows.md) or [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md), to open any file that Chrome is aware of. I start typing main.js, and here it is. It's not such a hardship to find something that's in just a couple of folders deep, but when you're looking for something where you might have a very complicated folder and document structure, that shortcut can be really helpful. So let's set a break point here on line two, where the plugin is invoked, and we'll reload the page. And there we go, we're paused here on the invocation of the plugin. So I'm going to step into this function, and we can see what's going wrong. Okay, so we've jumped here into jQuery. This is the minified version of jQuery. It's pretty unreadable, even if we were to use the pretty print function. And I really don't actually care about what's going on in jQuery itself, so what I need to do at this point is ignore this script from debugging, also called blackboxing the script. That is, I'm going to force my debugger to ignore anything that takes place inside that jQuery file. You can do this to as many files as you need to make sure that you're only looking at the stuff
>
> **[3:04](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-the-use-of-a-jquery-plugin-22149299?u=76281980&t=184)** that's important to you when you're trying to debug. The way you do this in Chrome is right clicking anywhere in the sources pane, and then I can choose add script to ignore list. There are other ways to do that as well, even from the call stack. And as you can see, the call stack has just removed everything that was related to jQuery. By check show ignore listed frames, you can see there's a lot more here that's just from jQuery itself, so I'll uncheck that again, and now we just see what's in the main.js file. Now, anytime something cyclists into jQuery, I'll be taken out without having to look at it. Having scripts in an ignore list or a blackbox is really handy when you're working with a lot of third party libraries, and the way you can do it varies in different browsers. Over here in Firefox, I have the same example loaded, and here in the source pane, I can choose ignore source. Sometimes you'll see a little eyeball icon that you could click that will also give you an option to hide the script. Okay, back here in Chrome, I'm going to refresh, and we'll try stepping into this function again. Okay, now we've skipped jQuery, and we've just gone over to the lettering plugin, and because I'm in the developer build of jQuery lettering, it's not minified and I can really see what's going on here. And it's showing me that the method I've passed in
>
> **[4:36](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-the-use-of-a-jquery-plugin-22149299?u=76281980&t=276)** to the lettering function is [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md). Which sounds right, but it's not working. And then I can see that it's trying to look it up in this methods variable. It's not in the local scope, but if I open up the parent closure, I can see the methods here, and the ones that are available are init, lines, and words, and I'm passing in word. So now I know what the problem is and we can fix it. If I didn't want to go digging through the scopes pane, I could also have added methods here, in my watch expressions, and inspected it that way. In any case, now we can go fix this. So switching over here to my code editor, I have the index.html file, and I'm going to jump down to the bottom of the file with command down arrow, and then I can hold command and click this file to open it. And now I can change word to words and save.
>
> **[5:43](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-the-use-of-a-jquery-plugin-22149299?u=76281980&t=343)** I'll let the execution resume here in Chrome, close the developer tools, and refresh. And I can see that now my titles are starting to look partially different. That means that the CSS that was supposed to be loading and restyling those words is working again. Of course, there's more than one way to do this, but we've just applied some of what we've learned about using a debugger to fix this very simple bug.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (3), [HTML](../../Skills/Web%20Development/HTML.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [Windows](../../Glossary/Service/Windows.md) (1)
> **Code Identifiers:** jquery (14)
> **File Paths:** lettering.js (5), main.js (3), index.html (2)
> **UI Navigation:** open the (2), scroll down (2), in the sidebar (1)
> **CLI Commands:** make (3), find (1)
> **Env Vars:** css (2)
> **Tools:** firefox (1)
> **Exercise Files:** exercise files (1)

#### [Debugging a React application in Chrome](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-react-application-in-chrome-22151337?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-react-application-in-chrome-22151337?u=76281980&t=0)** - [Instructor] In this video, we're going to look at an example using [React](../../Skills/Web%20Development/React.js.md) with both the built-in debugger and React's own custom developer tools. If you've never seen React before, it's a very popular [JavaScript](../../Skills/Software%20Development/JavaScript.md) user interface framework based on components. So you break up your project into logical pieces with their own bits of data all built in JavaScript and assemble your UI from those components. As we'll see, the syntax used in building those components is different than you might have seen before. But ultimately, it's all JavaScript. This example involves some [AJAX](../../Skills/Web%20Development/AJAX.md), so to run it successfully, you'll need a local web server. I'm using MAMP, which is available for macOS or [Windows](../../Glossary/Service/Windows.md), but there are tons of options available. If you don't have a local web server like this and you want to learn more, you can search the library for Apache or local web server and find lots of videos with more detail. I've copied the exercise files into MAMP's htdocs folder, where it serves from by default. The current location, whatever that is configured to, is displayed on MAMP's home screen, and you can change it in the preferences under Server. As you can see here on my desktop, I've copied the files from the desktop folder into the htdocs folder for MAMP. One other thing I want to mention is that MAMP has a port setting in MAMP's preferences under Ports. Normally, a web server on the Internet would be serving a website from port 80, but MAMP ships with port 8888 by default for various reasons. And I've just decided to leave it at that default for now. And the button to start or stop the web server
>
> **[1:34](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-react-application-in-chrome-22151337?u=76281980&t=94)** is up here in the right-hand corner. All of that means that once I have loaded my files and turned on the web server, I can now access the exercise files at local host port 8888 here, separated by a colon, and then following the path down to where the files live. So this is our example. It's a page that's built in React, and this is supposed to be a company directory showing all the people that work at this company. But as we can see here, there's just a loading message and nothing else. So clearly, something's going wrong. Let's open up the developer tools. Use Command + Option + I here. And already, I'm prompted to install React's dev tools. And there's a link that'll take me there, so let's do that. These tools are available for Chrome, Firefox, or [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) Edge. I'm using Chrome here, so we'll go to the Chrome Web Store, following that link. And I can add them to Chrome right here. So I'll add the extension, and there we go. I can see that I have the dev tools available now. So I can close all this and go back to my page and refresh it. Now, what I expect to see here is a couple more panels. I'm going to close the dev tools, copy my URL, and I'm going to close the window, reload it totally from scratch, see if we have better luck this time. So we'll reopen the developer tools, and here are my new tabs. Great.
>
> **[3:07](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-react-application-in-chrome-22151337?u=76281980&t=187)** This is a bug in Chrome that I hope will be fixed by the time you see this video. But if it's not, you can try doing this to get these tabs back. Lots of frameworks offer tools like this, purpose-built developer extensions to give you knowledge that's harder to find with a general-purpose debugger. In this case, I can browse and inspect the components on the page using this Components tab and check for performance issues in the profiler. We're just going to use the Components tab for now. But first, let's take a look at the code. So over here in my editor, I've opened the directory.js file and the index.[HTML](../../Skills/Web%20Development/HTML.md) file. In React, components are either ES6 classes or functions. In this case, I'm using functions. And we have a Directory component down here at the bottom, which is the biggest one that contains everything else, a Filters component, which is used to filter the results that are displayed in the directory, a People component, which contains all of the Person components, which are supposed to list all the people who work here. But of course, we don't have any of these people on the page right now. One more thing I want to do, here in the sources tab in my debugger, I'm going to type Command+P to open that directory.js file. And we're going to find that Axios callback. Here it is. Going to set a breakpoint inside this callback function. This is called after Axios goes and gets that directorydata.[JSON](../../Skills/Web%20Development/JSON.md) file, which contains all the people that should be displayed on the page. I'm going to reload so we can trigger this, except it doesn't get triggered because Babel has transpiled this page. So sometimes the breakpoints don't exactly end up
>
> **[4:42](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-react-application-in-chrome-22151337?u=76281980&t=282)** where we think they should, and something that we expect to stop the page doesn't stop it. This is one of those cases where adding debugger to the actual file can be helpful, so let's go do that. Going to search for Axios here in this file. And inside its callback, I will just add debugger here. I'll switch back to Chrome, remove this breakpoint, and reload the page. You might need to hard reload the page to make sure that you get this. There we go. Now we've paused. And the reason I wanted to stop here is because, here inside this asynchronous request that changes my call stack quite a bit, I can see that I have a promise.then here, which corresponds with this Axios callback. And it's got a little divider line here telling me that this is a whole call stack across asynchronous requests. And if I scroll down even further, here's a postMessage. And if I scroll down even a little further, here's another XML HTTP request, which was initiated by Babel because it uses an Ajax request to grab the original file, directory.js in this case, and transpile it into the version that's injected into the browser. And that's why setting a break point in here, in the original code, won't necessarily work the way you expect it to. Okay, I can let this run out. And I will switch back to my editor and remove that debugger statement 'cause we don't need it for now. Returning to Chrome, I'm going to open up the Components tab and reload the page.
>
> **[6:18](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-react-application-in-chrome-22151337?u=76281980&t=378)** I can see I have a Directory component and a Filters component, which correspond to the functions I saw in my code. And if I click the Directory component, I can use this inspector over here to see the data that this component is managing. Depending on the kind of React components you're dealing with, you might see hooks, or you might see state, and you also see props. We're interested in these hooks right now. Normally, when you're using what are called hooks that manage the state of a component, it'll look like this one here, where there's no label showing you what these data correspond to. So I can see that I have something that looks like person data. But I don't know, just by looking here, which variable that's stored in. I've added a state with the label Custom React Hook that let's us get some more useful information about these other variables. So I can see I have one called isLoaded, which looks very promising. And there's also one called visiblePeople, which also has data inside. So it certainly looks like this page should have displayed these people, only it's telling me that it's still loading, right here. My isLoaded variable is still set to false. So I think what I need to do is deal with the fact that this is still set to false, even though we appear to have data. Switching back to the code, so it should be happening here is, after my request completes, I should've marked the page as having loaded the data. I have these functions that set all people,
>
> **[7:51](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-react-application-in-chrome-22151337?u=76281980&t=471)** set the visible people, set the job titles. This is obviously setting data based on what was returned. And if I scroll down the page, I could see here's that loading message that's displayed. I could've searched for that as well, since I know that that's something that's displayed on the page related to the bug I'm trying to fix. And so I'm checking the isLoaded flag, which I saw over there in the dev tools. And I can see that I have a function here called setIsLoaded, which Visual Studio Code has helpfully grayed out for me because I've never used it. So it looks like what we need to do is, after we complete our request, I need to set isLoaded to true instead of false. So I'll add that here and switch back to Chrome, close down my developer tools, and I will reload to see what we get. Aha, there we go. Now I have a directory of people, just like I expect. In the end, this was a pretty simple bug. But getting here, we get to see some more power in the debugger with a framework-specific developer extension, how the call stack can track asynchronous operations, and how we can apply what we know in a little more complex context, like React.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (9), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3), [AJAX](../../Skills/Web%20Development/AJAX.md) (2), [Windows](../../Glossary/Service/Windows.md) (1), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1)
> **Env Vars:** mamp (7), url (1), es6 (1), xml (1), http (1)
> **Code Identifiers:** isloaded (4), macos (1), postmessage (1), visiblepeople (1), setisloaded (1)
> **File Paths:** directory.js (3), index.html (1), directorydata.json (1)
> **CLI Commands:** find (3), apache (1), make (1)
> **Definitions:** is a  (3), means that (1), is called (1)
> **UI Navigation:** scroll down (3), go to (1)
> **Ports:** port 8888 (2), port 80 (1)

#### [Debugging on iOS using Simulator](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-on-ios-using-simulator?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-on-ios-using-simulator?u=76281980&t=0)** - [Instructor] In this video we're going to look at how to debug [JavaScript](../../Skills/Software%20Development/JavaScript.md) running on an iOS device using Safari's developer tools and the simulator built into x code. Most of what we see here applies when debugging on actual devices plugged in over USB as well. Here's my example loaded in Safari. All these items are draggable as you can see. And I want to respond to swipes as well, removing items when I swipe left. This is built using jQuery UI. And we're gonna look at it on an iOS device through a simulator. I need to do a little bit of setup for this all to work. First I'm gonna turn on the developer tools in Safari. To do that I'm going to open the preferences and then here under advanced, show develop menu in the menu bar. Once that's available I've all kinds of goodies including the Web Inspector which is right here. Once again the key command to open this is command option i, the same way we see with pretty much all the other developer tools. All right. This is good, now let's get the simulator running. Here in the simulator I'll show you where to turn on the Web Inspector on your iOS device. This is, I believe, enabled by default in the simulator. But you'll need to find the same thing in your mobile device when you connect it over USB. So you'll look under settings, and then Safari, and scroll down to advanced and here's the Web Inspector.
>
> **[1:33](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-on-ios-using-simulator?u=76281980&t=93)** So if this is not enabled, you'll just turn it on here and then you're good to go. So let's load up Safari, and then let's make a connection between Safari and the device. Here I am back in Safari, and now if I open the develop menu I can see the simulator here. And if I choose about blank, which is what's currently loaded over here in my simulator, I can see a Web Inspector for what is actively loaded in Safari on the mobile device. And I can hover over dom elements and they highlight over there. This will work exactly the same with your connected device. It's pretty neat. Now let's load the actual example file in this simulator. You could click over here and type it in, but it's easier to select the URL and copy it. Then switch over to the simulator and paste. Now it doesn't go anywhere yet, but this puts it in the device's clipboard. So now I can tap here, clear this out, and double tap to bring up the contextual menu. I could paste it in or I can use the shortcut paste and go, and there we have it. My example is loaded and I can start dragging these items around. Looks good. Now let's load up the debugger and do some actual work. Here in Safari's Web Inspector the debugger is called debugger, very helpful. The script file that I'm interested in is main.js. This has my custom JavaScript. So there's an inline library pasted in here that helps hook up jQuery UI to touch events, and then there's my code underneath.
>
> **[3:07](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-on-ios-using-simulator?u=76281980&t=187)** Now what we wanna do is take action and remove items when we're swiping left. So let's try it here, swipe left, yeah, nothing happens. Just responding to drags. So let's set a breakpoint here inside the swipe left method and see what our options are. Right now all I have is nonexecuting code. It does seem like I can set a breakpoint here. Let's make sure that it actually works. Nope, swiping left and nothing happens. So because this line can't execute we never stop. I can swipe all I want and nothing happens. So let's delete this breakpoint and I'll switch over here to my editor. And I'll scroll down and use debugger. I don't know what I wanna write yet, so this is a fine way to pause. I'll save that, switch back to Safari, and now I can use command r while I have Safari highlighted with the Web Inspector highlighted. Now watch over here, the page reloads. And now I have debugger set here. So if I swipe left, there we go, we're paused. And now we can see what our options are. So what I want to do is figure out what I know about this event and how I'm going to target the item that I want to get rid of. Usually you'll find this sort of thing in the target property of an event object. So let's open this and look around. We have current target and then we have target right here. So we're going to have to expand this a little bit to see better what's going on. So I can see this is a list item with a class of item. All right, that should be good enough.
>
> **[4:41](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-on-ios-using-simulator?u=76281980&t=281)** So I'll just resume this so it completes. Now I'll switch to my editor and I can write a little code. Okay, let's replace everything here with some new code. So I'm going to use a little bit of jQuery here. The item that I was looking for was in event.target. So I'm going to save that as a little variable. When I'm working with jQuery I like to prefix any variables that I save with a dollar sign to denote that they're jQuery objects, or at least I expect them to be. So I've saved that and now I'm going to inspect the item using item.is. This is a method that's built into jQuery that can tell me whether the item I'm looking at has certain classes or is otherwise a certain type of dom element. So I'm going to look and see if I have that item class. Because I'm executing the swipe using event delegation on the entire items list, I'm doing this to make sure that the event that I'm targeting is targeting just one of the individual items and not the list as a whole. So assuming I've gotten to this point, I now should be able to do something with this item. So there's various options for what we could do here. I'm gonna use jQuery's fadeout method. Make it fade out nice and fast like this. This is just a built-in method to jQuery that does a simple fadeout animation on the item to which you chain that method. So let's save this and go back and try it out. I'm going to hide the sidebar again
>
> **[6:16](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-on-ios-using-simulator?u=76281980&t=376)** and now let's reload. And I'll try swiping left and there we go. The items start to fade out and disappear. I was able to use the debugger to find out what I should know at that point in execution, and that helped me write the code that I wanted. Of course, when testing your JavaScript on mobile devices you'll want to use actual devices as well. But to get you started this way works great. Now you know how to connect Safari's developer tools, including the debugger, to the simulator or actual devices.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3)
> **Tools:** safari (12)
> **Code Identifiers:** jquery (8), ios (3)
> **CLI Commands:** make (4), find (3)
> **UI Navigation:** open the (2), scroll down (2), in the menu (1), select the (1), switch to (1)
> **Definitions:** is a  (3), is called (1)
> **Env Vars:** usb (2), url (1)
> **Prerequisites:** setup (1), you'll need (1)

#### [Remote debugging on an Android device](https://www.linkedin.com/learning/learning-javascript-debugging-2/remote-debugging-on-an-android-device?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/remote-debugging-on-an-android-device?u=76281980&t=0)** - [Instructor] In this video, we're going to look at how to do remote debugging on an [Android](../../Glossary/Sdk/Android.md) device. This is possible in Chrome or Firefox. And does involve a little bit of setup. We're going to use the same example as for iOS. Where the list items are drag-able. And you can swipe left to make things disappear. I've loaded this example on my computer's web server. And connected the Android device to the same network so they can see each other by IP address. And remember, I'm using Mac in it's default configuration which is why we have this 88 88 port here. My Android device is connected by USB. But I need to enable USB debugging. There are instructions on the Android Studio website for how to do this, but in particular, here's the funny part. You need to visit your device settings. And just like it says here, tap the build number seven times for the developer options to even show up. It's just kind of a little quirk that's funny about Android. Once that's done and your device is connected by USB, there will be a prompt on the device asking you if you want to allow your computer to debug the device, and of course, you want to say yes. With all that done, load up Chrome on your Android device. Incidentally, if this stuff doesn't work, make sure that it's fully updated. Chrome should be able to see your device now. But how can we tell? Back here on the example. I'm gonna load up the Chrome developer tools. Command option I. Now I don't see anything different here that indicates where I should be looking. What I want is here under more tools. Remote devices. This is all getting a little squished.
>
> **[1:32](https://www.linkedin.com/learning/learning-javascript-debugging-2/remote-debugging-on-an-android-device?u=76281980&t=92)** So let me undock this. Much better. Okay, now I can see what's going on here. What's going to show up here will vary depending on the device you have connected. But I can see that, here's Chrome. And I've loaded up the example from my local web server. Using the same URL but just on the IP address. And now all I need to do is click inspect. And at this point, I have a debugger running on the device. So, let's take a look at the screen of my Android device right now. Here in the elements inspector, I can hover over items and they highlight on the screen. So I know for sure that things are connected. Now in my sources panel, I can set a breakpoint in the debugger. Let's set it here inside swipe left. And now I'll try swiping left on the device. On that first item. And there we go. Execution has paused. And I can proceed through the debugger the same way as I would with the desktop instance. This is pretty sweet. And opens up great possibilities for compatibility testing. One other thing that's kind of nifty about the Chrome developer tools. Is that there's a screencast, as well. This looks like the button that you use to toggle the response design mode in the styles and elements inspectors. But this is an actual screencast of your device. So everything that I do on the device is going to happen over here. Pretty neat.
>
> **[3:08](https://www.linkedin.com/learning/learning-javascript-debugging-2/remote-debugging-on-an-android-device?u=76281980&t=188)** Many of these features are available for Firefox, as well. Especially the [JavaScript](../../Skills/Software%20Development/JavaScript.md) debugging. Look for the web IDE tool in Firefox to find your device there. And that's how to connect an Android device for remote debugging. It does involve a little bit of setup, but once your up and running, it's pretty excellent.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Android](../../Glossary/Sdk/Android.md) (8), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Env Vars:** usb (3), url (1), ide (1)
> **CLI Commands:** make (2), find (1)
> **Tools:** firefox (3)
> **Prerequisites:** setup (2)
> **Code Identifiers:** ios (1)
> **UI Navigation:** toggle (1)
> **Definitions:** is an  (1)

#### [Challenge: Other people's code](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-other-people-s-code?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-other-people-s-code?u=76281980&t=0)** - [Instructor] In this challenge, we're dipping into something that can be a source of consternation for many developers. Other People's Code. We will be debugging some code we've never seen before at all, using a framework that we might not be overly familiar with, that we're pretending someone else has given up on, and handed to us. I hope that doesn't sound too terrifying. It's not a ton of code, don't worry. But if you're asking, why are we doing this? Let me explain, and hopefully assuage any major concerns. First, if you're a professional, or enthusiastic web developer, you won't always be starting everything from scratch. You will almost certainly be handed other people's code to work on at some point. And there will be times when that code appears, and maybe sometimes is terrible. I also think it's true that dealing with the unfamiliar is intrinsically a good thing in code, and if you'll indulge me a little in life. All this means that along with merely increasing our skill with a debugger, which is mainly why we're here, exercising our skills in less familiar settings is a good mental exercise. Now let's take a look at this unfamiliar project. Here it is, a little card table that will deal out the number of cards you ask for. You want to have this running from your local web server, as we saw with our previous [react](../../Skills/Web%20Development/React.js.md) example. I've tried to keep the amount of very idiomatically react syntax moderate so those who have never used it before don't want to just throw their computers at the nearest window. This is supposed to be a challenge, not something that feels impossible. There's plenty of [Vanilla](../../Skills/Web%20Development/Vanilla.md) [JavaScript](../../Skills/Software%20Development/JavaScript.md) here. Okay, let's try it. Click shuffle and deal.
>
> **[1:37](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-other-people-s-code?u=76281980&t=97)** I asked for six cards, and I got, well, I'm no mathematician, but that's a lot more than six. Let's open the developer tools, and see what we have here under the hood. This is the root element, there's a mock up here where, this was originally mocked up without react. But here in the root is all the stuff that's been rendered. We can look over here in the components tab to see our components table, and a whole bunch of cards, mainly. And we can see over here, the data that react is tracking for us in the form of a couple items of state. Let's scroll down here, we can see that here are how many is represented by this state variable, and then all this other stuff is the cards. Okay, so there are actually 45 of them. That is just a few more than six. If we change six to seven, you can observe down here that, that has changed as well. This is react in a nutshell, we're keeping some data in sync with what we are rendering visually. In react, we store things in state if they're going to be used in our UI, or otherwise impact what we see. In this case, the hand that we're dealt is used, and represented in state, and the number of cards we need is also in state. So that's all we have there, and therefore that's all that's browsable in this section of the React Dev Tools. The deck that we're drawing from is also important, but it doesn't impact the rendering of the UI
>
> **[3:09](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-other-people-s-code?u=76281980&t=189)** so it's not stored in state, and therefore, you can't see it in these developer tools. Let's click one of these card components, we can't actually see it it's way up on the page, but that doesn't matter right now. The thing I want to show you is that this card component has a couple of things called props. If you've never seen these before, props are like arguments, or parameters passed into a component. Changing props is one reason that react components will get rerendered. So when the cards are shuffled and redealt, like if we just click this button again, the cards get new props for the rank and suit and rerender. Alright, so we've seen the behavior, the rather strange behavior, let's go take a look at the actual code. We'll take a quick tour through the code here. Right at the top we have cribbage hand size. Cribbage is my favorite card game so I decided to have our initial number of cards dealt be the number of cards you get in the hand of cribbage. And then we have our components defined here as a couple of functions card and table. Here we can see those props, suit and rank. Here's the other one, which are used to render what actually appears on the cards. Of course, we can see that the color is determined by whether the suit is H, which stands for hearts, or D, which stands for diamonds. Down here in the table component is where most of the work happens. We start out with an empty hand, that is an empty array, and then we have our state defined here. We have our hand, and our hand size,
>
> **[4:42](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-other-people-s-code?u=76281980&t=282)** that is the number of cards that should be dealt. And then we have our set of functions that will change those values that react stores for us, and then also triggers rerendering of the whole thing. We have a couple of event handler functions here redeal is the one that's wired up. If we scroll down a little further that's the one that's wired up to this button here for shuffling the cards. And then we have the change hand size, which responds to the change event on this select box. Right here is a variable called cards, which will determine what's shown on the table. It's either going to be a bunch of cards, or a little div that just shows a message for when there's nothing dealt yet. Just so we know that nothing is there, that we did that on purpose. Here's the line that actually renders that react component onto the page. And then we have some helper functions down below. We have one that builds a deck of cards as an array of objects, with all our ranks, and all our suits represented as just simple data. This is not going to win you any computer science awards, but it makes very clear what we're doing here. We have a helper function called get-pip, which will return Heart, Diamond, Club or Spade. The original author did it this way, so these can be swapped out for icons, or images, whatever you might want. And then we have a helper function called shuffle and deal,
>
> **[6:17](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-other-people-s-code?u=76281980&t=377)** which as we can see gets the deck, shuffles it, and then returns the portion that we want. And then we have a couple of helper functions at the bottom here, which are borrowed from the very popular Lodash Library, which just help us support the things that we want to do. You don't have to worry about those functions at all for the purpose of this challenge. Here are our objectives for this challenge, once again. First, of course, is fixing however many bugs exist that are preventing the page from working correctly. We don't know currently how many bugs there are. We want to make sure we get a different hand with the number of cards we want every time we click the button. The second objective is exercising a softer skill, if you will, but one that's no less important. Successfully navigating this challenge despite the parts that might be unfamiliar. React is pretty popular, so you might have worked with it before, or if you have maybe not in the way we have here. Either way, dealing with the unfamiliar is specifically a goal here. Before I turn you loose, here are a couple of little hints. As with the previous react exercise, we're running our code through Babel to allow the use of JSX for the components. Because there are no source maps when we work this way, breakpoints can behave a little strangely sometimes. So don't forget that you can use debugger to set an encode breakpoint. I'm not saying you will, or won't have to do this, I'm just saying don't forget. Also remember that the two helper functions at the bottom of the file borrowed from the Lodash Library are not the source of any bugs in this challenge. If you have worked with react before so all the syntax is pretty familiar this challenge might not take too long. For everyone, I think you'll probably be finished in 15 minutes or less.
>
> **[7:51](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-other-people-s-code?u=76281980&t=471)** Good luck.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (13), [Vanilla](../../Skills/Web%20Development/Vanilla.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Definitions:** is a  (2), stands for (2), means that (1), is an  (1)
> **UI Navigation:** scroll down (2), open the (1)
> **CLI Commands:** pip (1), make (1)
> **Best Practices:** don't forget (2)
> **Env Vars:** jsx (1)
> **Cross-References:** as we saw (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Other people's code](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-other-people-s-code?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-other-people-s-code?u=76281980&t=0)** (electronic music)
>
> **[0:06](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-other-people-s-code?u=76281980&t=6)** - [Instructor] Let's work through a solution to this other people's code challenge. First we're going to try shuffling and dealing a few times just to confirm that even though there's way too many cards, it does look like we're getting a variety here. So it's at least partially working. I don't see any duplicate cards. Some of them are getting hidden up here but it seems safe to say that we are not getting duplicates. And even though this is not working, giving us the right number of cards, there's at least some good in here. So we'll open up the developer tools and go to our debugger. Here's the cards.js file here. We know that we have a handler that takes care of shuffling and dealing. So let's go visit that and set a breakpoint. I'll click this and here we go. We're paused. The first thing that I want to do is confirm that the size of the deck that we're getting back is actually correct. So I've stepped over onto this line where I go and get the deck. Let's just step over that so we can get a value to look at. I can see here that I have the right number of cards. If I scroll down through here, I can see I have four aces, four twos, four threes, and so forth. So I think I can trust that part
>
> **[1:38](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-other-people-s-code?u=76281980&t=98)** is working the way I want it to. Here is where the dealing part happens, where I take a chunk off of this whole deck and return it. I can't actually see this yet because it's just directly returned, so let me step over again. Who will return. Now that value is being set over here in new hand. And there it is. I have 46 items where I really wanted, what was it? Four. That's sort of weird. 52 minus 46 is six, not four. So I might have a couple of things that I'm dealing with here. Let's go back. I'll let this play out again. Click that button again so we hit the same breakpoint. Everything seems to be fine up until the point that we slice off a chunk of that deck. So I'm wondering if I just forgot how to use the slice method on an array. Usually what I'll do is go and look up the syntax for a function. So I'm working with array.slice here. Here it is, array.prototype.slice. So I can see looking at this array that passing in just one parameter will give me the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the array after the first two. That is our problem. We should be starting at the beginning and just slicing off however many it is we want. Instead we're slicing those off
>
> **[3:12](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-other-people-s-code?u=76281980&t=192)** and then returning everything else. So back here in Visual Studio Code, let's revisit that same function here, the shuffle and deal. What we need to be doing here is starting at the beginning. We can see that Visual Studio Code, if I hover over slice will also give me some useful information. So I want to start at the beginning of the array. Take the first item off there and only go up to hand size. So we'll save that. Let this play out and reload. Close the developer tools so we can see a little better. Shuffle and deal. Oh, look at that. Six cards, wonderful. So we're done, right? Let's see. We'll get seven cards. Oh, nope. Not done. This part isn't working either. Referring back to the [react](../../Skills/Web%20Development/React.js.md) developer tools, I do know if I look at the table that the number of cards that I want is actually being tracked correctly by the component. I'm just not using it properly. So let's revisit the debugger. I don't think I need this breakpoint. Let's go up to the redeal handler. Here we go. This is where we get our new hand. Now I can shuffle and redeal. So getting my new hand by shuffling and redealing, and I'm using cribbage hand size, which was up here at the top.
>
> **[4:48](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-other-people-s-code?u=76281980&t=288)** Well, that is obviously our bug. If I happen not to notice that and I decided that I was going to step in here, I get some other hints as I go through. Hand size is set to six here. So another way that we might have noticed this was by observing that we had four selected up here. We know we had four in the react state and yet the parameter was passed in as a six. If I didn't notice it at first, I could step back through the call stack, look back here to see, oh yeah, look at that. Cribbage hand size. So I'm hard coding this as if we're playing cribbage, which as much as I enjoy cribbage, is not really what I want to do. So we can scroll up to that redeal function. What we actually want is hand size. This is the variable that gets updated every time we change that select box. We'll save again, can disable that breakpoint, close the dev tools and reload. This time we're going to ask for just four cards and it looks like it works. If keep doing this I get different cards every time. Let's go up to nine. There we go. Try this with any number and they all seem to behave as we want them to. It appears that we are done. We were able to employ our debugging tools, the regular debugger, and the react dev tools to learn more about how this little web app works
>
> **[6:20](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-other-people-s-code?u=76281980&t=380)** and why it wasn't behaving as it should, and we did it all in a setting that was a bit less familiar than the others we've seen so far.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (3), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **UI Navigation:** go to (1), scroll down (1), scroll up (1)
> **Tools:** visual studio (2)
> **File Paths:** cards.js (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (electronic music) (1)

#### [Debugging a Node.js application in WebStorm](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-node-js-application-in-webstorm?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-node-js-application-in-webstorm?u=76281980&t=0)** - [Instructor] In this video we're going to debug a simple node script in WebStorm, a very nice IDE. It was PhpStorm, WebStorms sibling, that turned me into an IDE believer. Our example is a little node script that will collect Lorem Ipsum placeholder text from this website, [lipsum.com](https://lipsum.com). We want to return the text in a form suitable for pasting into a page or wherever we might need some placeholder text. One of the nice things about [lipsum.com](https://lipsum.com) is that they offer an API that makes custom scripting possible. First, I'm going to show you how this script works in its current state. I'm here in a terminal where I've changed directories to the exercise files for this video, and you can see that I have the gitlipsum.js file here. so let's execute that through node. It prompts me for how many paragraphs I want. I'm going to choose two. It retrieves the data and then gives it back to me, but it's in the form of this massive [JSON](../../Skills/Web%20Development/JSON.md) blob. I want this as plain text that I can copy and paste. So successfully retrieving the data is good, but it's not in the form we want. Now over here in WebStorm, I'm going to open the project. If you're setting this up for the first time it will take you through the setup process where you might need to create an account with [jetbrains.com](https://jetbrains.com) and start a trial. But I'm going to open the exercise files, which are on the desktop, and I'm going to open the folder for this video. In most projects with an IDE
>
> **[1:34](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-node-js-application-in-webstorm?u=76281980&t=94)** there will be an indexing process where the IDE will read through all the files that are available and start to figure out what all the available functions are to give you code completion and other things. Because there's only one file in this project, it goes really fast. Let's open that file. And there's a bunch of squiggly lines under a bunch of pieces here. This suggests that WebStorm is not completely set up yet, so let's open the preferences and turn on node support. There's a search field here in the preferences which is good because there's a lot of options. And when I search for node, what I get selected here is under languages and frameworks, [Node.js](../../Skills/Web%20Development/Node.js.md). And what I want to turn on is this coding assistance for Node.js. Once I've done that, I can click Okay. There's a little bit more indexing, and now all these bits that had squiggly underlines are now highlighted because they are recognized keywords. That plugin enables code completion, parameter hints, nodes module system, and more. So make sure you turn that on if you're working with node scripts. There's another squiggly line down here, but that's just the built-in spell checker, which you can turn off in the preferences if you want to. Let's take a quick walk through this script to see what it's doing. First, I'm including a couple of nodes modules. The readline module for interfacing with data on the command line. And the HTTPS module for making web requests from secure web services. Then we're creating an interface for readline,
>
> **[3:07](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-node-js-application-in-webstorm?u=76281980&t=187)** just calling it RL, and I have the input and output options set to standard in and standard out. Then we're using the question method from readline and prompting for how many paragraphs we want. To that we pass this [Git](../../Skills/Software%20Development/Git.md) paragraphs callback function. The answer that the user gave is the parameter that's passed in, which we're calling numb paragraphs. And then just sanitizing this and making sure that the input is in a form we want. Basically, we want to make sure that there's more than zero but less than five. That's just what we've chosen kind of arbitrarily for this example. We post a little message to standard out using that write method, saying how many paragraphs we're going to get. And then we're executing an HTTP GET through the HTTPS module, and this is the URL that the [lipsum.com](https://lipsum.com) service provides. We're asking for the data in JSON format, asking for paragraphs, and how many we want. Then we have a process response callback function here which takes response as its argument. We're setting up our feed data, which is what will eventually get written out to standard out in the terminal. The HTTPS module is returning the data as a buffer. So first I need to collect all the data out of the buffer using its data event. And so basically this just keeps firing over and over again until we've collected all the data that's coming back. Then once we've collected all that data, the end event fires, and that's where we're doing the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the work. Note that when that end event fires,
>
> **[4:42](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-node-js-application-in-webstorm?u=76281980&t=282)** currently we're just basically barfing that JSON blob out to standard out, and then adding a new line. But we really need to transform that into copy and pasteable text. And to do all this, we need to know what format that data looks like from inside the script. And to do that, we'll hook up the debugger using a run configuration. So we're going to click edit configurations up here. It's also available through the run menu, edit configurations. I'm going to click the plus button and we can see all kinds of different configurations that can be run for debugging or run purposes. We're using a Node.js script, so we'll choose that one. These configurations allow you to run a file in your project using any version of node that happens to be installed. And as a quick sidebar, I currently have the latest version of Node installed globally, but it can be a good idea to use a Node Version Manager. These tools allow you to use different versions of Node with different projects on the same machine, which ensures that all the modules that you've installed will be exactly the same as any others, other people you're working with, are using. I use fnm myself, but nvm is probably the most popular one. Whatever you use, WebStorm does a good job of detecting any versions of node that you've installed, and then you can set those in your run configurations. In this case, because we're using the global one, we don't have to make any changes.
>
> **[6:14](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-node-js-application-in-webstorm?u=76281980&t=374)** I'm just going to give this a name, we'll call it lipsum. And then the [JavaScript](../../Skills/Software%20Development/JavaScript.md) file that we're going to run is all I need in terms of configuration. So I'll just set this to gitlipsum.js. You could also select it from the standard file dialogue using this folder icon, and we'll click Okay. And now this is available up here as the selected configuration. I'm going to set a couple of break points in the file. We'll set one on the outer block, and then one more down here after the git has completed in that callback function. Then I can click Debug to get this started, which is also available here in the run menu. And here we go. Now we have our stepwise debugger right here in the IDE, and it's got all the controls that we expect. There's a call stack here. There are controls for stepping over, in and out. This one is quite handy. This will show you the line at which the execution has stopped. So if it doesn't happen to be a breakpoint, you can click this and it will open the file that's currently executing and take you right to that line. There's also a global resume button here, a stop if you want to just stop execution completely. There's a breakpoint list which will show you all break points in all files. You can enable or disable them here and delete them. And there's also a button to mute break points. So that will just disable all of them while leaving them in place. So now that we're in here we can just start stepping through.
>
> **[7:48](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-node-js-application-in-webstorm?u=76281980&t=468)** And I'll note that here in the process console this is your terminal. And if I let execution resume it's asking me how many paragraphs I need, and I can type two and hit return. And now it's dropped me into that second break point inside the process response callback. So now I can start stepping through again. Here's that data event that's going to fire over and over again. So let me set one more break point down here inside the end callback, and we'll let execution resume again. Now I can get over to the debugger pane and start inspecting the variables that are active right now. Here's the feed data, which is coming through just as a JSON string. So the first thing that we want to do is instead of just writing that out, let's change this. And we'll get the feed JSON into a constant. We will parse that feed data. And then we can see that inside that JSON object we should expect a feed key and then a lipsum key which seems to map on to the text we're interested in. So we'll write out the feed.lipsum here. We can save this, let execution resume. And here in the process console you can see what was happening before. And now we can run this again. the same debugger control is down here in the debugger pane.
>
> **[9:23](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-node-js-application-in-webstorm?u=76281980&t=563)** So we do that one more time. I can remove this break point and let this run again. The process console is waiting for me to enter the number of paragraphs, which I will do again. We can remove this break point. Resume one more time into our end callback. We'll make sure that this feed.json has been parsed as we expect it to be. That's good. Let it run to completion. And now in the console, I can see that both the original JSON is being outputted. So let's remove that. And then down below, here's the raw text. The line breaks, there aren't enough, rephrase. But there aren't enough line breaks between the paragraphs. So instead of just writing out the lipsum directly, we'll add some line breaks with the string replace method. And we're going to replace a regular new line with two of them. We'll save that and execute one more time. We could do this with or without the debugger. We have the debugger running right here, so we might as well use it. And here in the process console, I will ask for two paragraphs. And with no break points left it just runs right through to the end. And this is looking pretty good. So I'll make sure this file is saved and we'll switch back to the terminal and check it there as well. I will clear this output. Click the up arrow a couple of times
>
> **[10:57](https://www.linkedin.com/learning/learning-javascript-debugging-2/debugging-a-node-js-application-in-webstorm?u=76281980&t=657)** to get my previous command. And that looks good. So now we've finished this exercise. As you can see, being able to debug a project in the same place that you're editing is very useful. WebStorm can do so much more than this, but hopefully this gives you an enjoyable taste of using an IDE for JavaScript development and debugging. One of my favorite things about WebStorm in particular is that it provides a lot of sensible defaults without my having to install additional plug-ins manually. If I do want to make changes, it's highly configurable, and generally a delight to use. But as long as you find something that's comfortable for you, that's what matters.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (8), [Node.js](../../Skills/Web%20Development/Node.js.md) (3), [Git](../../Skills/Software%20Development/Git.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** node (14), make (6), git (2), find (1)
> **Env Vars:** json (7), ide (6), https (3), api (1), http (1)
> **File Paths:** node.js (3), gitlipsum.js (2), feed.json (1)
> **Tools:** terminal (4), command line (1)
> **UI Navigation:** open the (5)
> **URLs:** [lipsum.com](https://lipsum.com) (3), [jetbrains.com](https://jetbrains.com) (1)
> **Prerequisites:** setup (1), set up (1), install (1)

#### [Setting up the WordPress example](https://www.linkedin.com/learning/learning-javascript-debugging-2/setting-up-the-wordpress-example-22153322?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/setting-up-the-wordpress-example-22153322?u=76281980&t=0)** - [Instructor] The next example is going to involve working on a [WordPress](../../Skills/Web%20Development/WordPress.md) theme in Visual Studio Code. In this video, we're going to do some setup for that WordPress portion. This will require a local web server, just as our [React](../../Skills/Web%20Development/React.js.md) example did. We're still using MAMP, but I'm going to make a configuration change this time. Previously, we didn't care about what port the web server was running on because perfect parody between your setup and mine wasn't important. But WordPress hard codes links to images and pages, so to try and save some headaches, we're going to get as close as possible to give you the best chance to follow along easily. First, we're going to copy or move this DBGWP folder from the exercise files on the desktop into MAMP's HT docs folder. I'm going to copy them. Next, we need to make our MAMP configuration change. We can stop the server if it's running. And now we can open the MAMP preferences and make the change. We're going to set the ports to the default ones, 80 and 3306 for sequel. And now we can restart the servers. All right, let's ignore this error. I'm going to secretly start the server again.
>
> **[1:36](https://www.linkedin.com/learning/learning-javascript-debugging-2/setting-up-the-wordpress-example-22153322?u=76281980&t=96)** And this will open the MAMP start page most likely. If it doesn't open immediately, you can open it yourself. And the reason we're interested in this is is because we are going to need to import the database. And we can do that here under the tools menu and go to [PHP](../../Skills/Software%20Development/PHP.md) My Admin. If it doesn't open it for you or you don't want to go to that start page, you can just go to localhost/phpmyadmin5. And now we're going to go to the import tab and choose the file that we want to import. I'm going to browse to the exercise files and this can be either on the desktop or in the ones you've copied into MAMP's folder and grab the _database.[SQL](../../Skills/Data%20Science/SQL.md) file. Scroll down and import. This will create the database for us, DBGJS_WP. And we can open this up to see that there are tables here in the database to make sure it worked. Most local development servers include PHP admin like this, but you can use any [Database Management](../../Topics/Database%20Management.md) tool you like, even the command line, if that's what your server of choice supports. As long as you're using the root user, this should all work. Now that we've done this, we can go to localhost/dbg-wp
>
> **[3:10](https://www.linkedin.com/learning/learning-javascript-debugging-2/setting-up-the-wordpress-example-22153322?u=76281980&t=190)** and if you see this sample WordPress site, you know you've got it working. If you've gotten to this point and you have this WordPress site, you're ready to go for this WordPress example. If something seems broken, you can check the wpconfig.php file and open that in your editor. Depending on the configuration for your local web server, you might need to change the database user or password and sometimes, the host. If you're using MAMP, all of this configuration should work right out of the box. With this setup complete, we can proceed with debugging this WordPress sites theme in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [WordPress](../../Skills/Web%20Development/WordPress.md) (7), [PHP](../../Skills/Software%20Development/PHP.md) (3), [React](../../Skills/Web%20Development/React.js.md) (1), [SQL](../../Skills/Data%20Science/SQL.md) (1), [Database Management](../../Topics/Database%20Management.md) (1)
> **Env Vars:** mamp (7), php (2), dbgwp (1), dbgjs_wp (1)
> **UI Navigation:** go to (5), open the (2), scroll down (1)
> **CLI Commands:** make (4), php (3)
> **Prerequisites:** setup (3)
> **File Paths:** _database.sql (1), wpconfig.php (1)
> **Tools:** visual studio (1), command line (1)
> **Exercise Files:** exercise files (2)

#### [Challenge: Debugging with an IDE](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-debugging-with-an-ide?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-debugging-with-an-ide?u=76281980&t=0)** (electronic music)
>
> **[0:06](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-debugging-with-an-ide?u=76281980&t=6)** - [Instructor] In our final challenge, we're going to look one more time at debugging from a code editor. Reinforcing this way of working is important. While having debuggers in the browser is absolutely invaluable and where you'll spend a lot of time, being able to debug and fix your code in the editor you enjoy is efficient and maybe delightful. As with every challenge so far, let's take a look at the patient we'll be diagnosing and helping. The files for this challenge are based on the first video from this chapter but with a twist. I have the solution loaded here in one tab and the challenge here in the other so we can see the expected behavior and then what we actually have. The expected behavior is that when we roll over these words, in every title on the page, they change color every time we do it. Works on all of these. Here in our starting point, that is not the case. Fixing that bug is part of this challenge but the main thing to practice is connecting our editor to the browser's debugger. I'll be using Visual Studio Code for this challenge so I'm going to start with the folder loaded up here and take it from there. So once again, here are the objectives for this editor based debugging challenge. Code wise this challenge is probably a little easier than the last one, but don't worry if it takes a little while to track down that issue. First we'll configure our editor or IDE of choice to debug the page. I'm going to use Visual Studio Code here, but if you've been working with WebStorm or others, by all means keep using that one. Basically you should be able to separate points in the editor and have the connected browser stop when they're reached. And one other note, you'll probably be able to work on this challenge with or without a web server.
>
> **[1:39](https://www.linkedin.com/learning/learning-javascript-debugging-2/challenge-debugging-with-an-ide?u=76281980&t=99)** In my experience and certainly in the editor I'll be using, local file URLs work just fine with these debugging configurations. But if you hit a snag, you might try putting the exercise files on your local web server and seeing if that helps. Once we're connected, we'll track down the bug or the words don't change when we hover the mouse over them. Depending on how easy the editor is to configure for debugging, this challenge could vary quite a bit, but I'm guessing it will take you 10 minutes or less. Now off we go.

> [!info]- Semantic Content
>
> **Tools:** visual studio (2)
> **Prerequisites:** configure (2)
> **Env Vars:** ide (1)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (electronic music) (1)

#### [Solution: Debugging with an IDE](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-debugging-with-an-ide?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-debugging-with-an-ide?u=76281980&t=0)** (upbeat electronic music)
>
> **[0:06](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-debugging-with-an-ide?u=76281980&t=6)** - [Instructor] Let's take a look at a solution for this editor-based debugger challenge. We need to set up a debugging configuration in the editor and then fix that bug with the headers not changing color when we mouse over them. So while I'm here, I'm going to copy the URL of this page that we're debugging, and then over here in Visual Studio Code, I'm going to create a debugging configuration. That's going to be here in the run or debug panel, and I'll create my launch.[JSON](../../Skills/Web%20Development/JSON.md) file against Chrome. I can leave everything just like it is except for the URL. I'm going to change that so we start in the right place. There we go, and incidentally, you don't have to debug in Chrome. There are other extensions if you search for debugger, there's one for Firefox, down here, you can see there's another one for [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) Edge. So we have some options. Chrome is probably the most popular one for this sort of thing, but you do have other options if you'd like to try them. Now in the run pane, I can launch my new debugging instance of Chrome. There it goes, helpfully offers to translate all of my placeholder faux Latin text there. Let's open our developer tools, and the first thing that we want to confirm by inspecting one of these words is that we actually have the span tags that we want surrounding all the words, and we do. So that part works. Each of these can be treated individually. Okay, so let's go to the [JavaScript](../../Skills/Software%20Development/JavaScript.md) folder, and we'll open main.js.
>
> **[1:42](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-debugging-with-an-ide?u=76281980&t=102)** Before we just had maybe one line here, but now we have some more code to look at. And I know that we have these event listeners on each [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md), we're getting a random color and then we're setting the style. Everything looks okay, so I guess we need to add a breakpoint inside this callback function that should colorize the words and see what is wrong. So let's mouse over one of these words and get into this function. Open up my debug pane so I can see what I'm doing here. Here's my variable for the new color, which is set to get random color. If I step over that, I get something that looks pretty plausible. Then I'm setting the style there. So I let this run out, open up my debugger again. I'm going to be careful not to mouse over any of those, so as not to launch myself back into the debugger again, and let's look and see what's going on here. Inside the header, yeah, that span tag does not have a color assigned. So let's take a closer look here. So we'll try it again. This time I will mouse over the word on purpose, and I'll step into the function this time, get random color, and I'll step down. Actually, let's set a breakpoint here where we return the color. I'll just play it again. So here's my color, and when we set the style.color to that new color, aha, the problem is we have the hex code here,
>
> **[3:16](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-debugging-with-an-ide?u=76281980&t=196)** but there's no pound sign in front of it that we need. So we can let this run out, and we need to change this so that color is going to start with the pound sign. We could also do it up here if we wanted to. For some reason, we felt like this was cleaner, we could do it like that. But I think I want this helper function to return the complete color that we can use directly when setting the style. Okay, so let's save that and try it again. Close these developer tools, mouse over that word.
>
> **[4:01](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-debugging-with-an-ide?u=76281980&t=241)** Looks like we haven't actually refreshed the copy of the script here, so let's relaunch. After saving this, we'll relaunch Chrome.
>
> **[4:17](https://www.linkedin.com/learning/learning-javascript-debugging-2/solution-debugging-with-an-ide?u=76281980&t=257)** If I step over this, there we go. Now we're starting to get the color with the pound sign attached. Now the colors are starting to change. So let me get rid of these breakpoints. Yes, there we go. Now it's behaving just the way we want it to. So in this challenge, we configured Visual Studio Code to debug this page that we were working on so we could solve a pretty small bug, but we got to do it all in one place.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (3), [JSON](../../Skills/Web%20Development/JSON.md) (1), [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Tools:** visual studio (2), firefox (1)
> **File Paths:** launch.json (1), main.js (1)
> **Env Vars:** url (2)
> **UI Navigation:** go to (1)
> **Analogies:** just like (1)
> **Warnings:** be careful (1)
> **Prerequisites:** set up (1)

#### [Debug a WordPress theme in Visual Studio Code](https://www.linkedin.com/learning/learning-javascript-debugging-2/debug-a-wordpress-theme-in-visual-studio-code-22151336?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/debug-a-wordpress-theme-in-visual-studio-code-22151336?u=76281980&t=0)** - [Instructor] In this video we're going to explore setting up web debugging in Visual Studio Code with Chrome. We'll be using this example [WordPress](../../Skills/Web%20Development/WordPress.md) site where the site's theme has a [JavaScript](../../Skills/Software%20Development/JavaScript.md) issue that needs to be resolved. First, we'll take a look at what the page is doing and what we're expecting it to do. So, we'll open up this debugging example page. And there are links up here that let you page through the images in this gallery. Clicking the links with the mouse works fine, but here, the keyboard can also be used. So if I hit the right arrow key I page through the images, and if I hit the left arrow key, nothing happens. So that's what we need to fix. Let's get debugging configured in Visual Studio Code. So, I'm going to open the extensions panel, and I'm going to confirm that the JavaScript debugger extension is installed and enabled. With recent versions of Visual Studio Code it should be here already, but if you've been a user of this application for a while you might have the Chrome debugger extension, or some older version of it. So this one is the production version, which is enabled globally as it says. There's also a nightly version if you're using an insider build of Visual Studio Code. But we have what we need here. I can close this. So, next we need to open the run and debug pane. I'm going to click run and debug, and choose web app Chrome. This creates a hidden.visual studio code folder in my project folder, and then creates a launch.[JSON](../../Skills/Web%20Development/JSON.md) file with this configuration. So, I can see that this is going to launch Chrome,
>
> **[1:37](https://www.linkedin.com/learning/learning-javascript-debugging-2/debug-a-wordpress-theme-in-visual-studio-code-22151336?u=76281980&t=97)** and it's going to use this URL. This is not actually the URL where our project is running. So, I'll go back to Chrome and just copy the actual URL for this project. And we'll paste that in like this. Now I can save this. And when I click this it's going to launch a new instance of Chrome with the debugger attached to it. To avoid confusion, I'm going to minimize my other copy of Chrome here. And now we have this new copy with a debugger connected to Visual Studio Code. So, we'll load the debugging example page. So we have our correct start point. And now back in the project files. First of all, I can see that I have my debugger transport up here with step over, in and out, all that kind of stuff. And also a call stack, watch expressions, variables, all the stuff I expect with a stepwise debugger. However, I do need a break point in a file in order to see what's going on. So, let me open up the project files in the Explorer tab. And in WP Content themes 2016 child and js. Here is the keyboard image navigation file. So, we can open this, and I can see that there's a key down event listener here. And then I have left arrow key code and right arrow key code. And what these are doing is directly setting the URL to whatever is in those previous and next links.
>
> **[3:12](https://www.linkedin.com/learning/learning-javascript-debugging-2/debug-a-wordpress-theme-in-visual-studio-code-22151336?u=76281980&t=192)** And then setting the window location directly to that URL. So, obviously, something here is wrong. Let's set a break point and give it a try in the browser. And again, we want to make sure that it's the copy of Chrome that has the debugger attached. So, if I hit a right arrow, that's the one that works. And now these panels have started to populate. So, I can inspect, here's the E object, which is the event. And this is really everything that we're looking for. I can step down and start to check these properties. So, I can look down at the e. witch property, or I can copy this into the watch expressions. So, I'll copy this and add a new expression. So, the key code is 39 for the right arrow, and this is the one that works. So as we expect everything matches, and if I step down, I should be getting the behavior that I want. So I can step over and resume. And back here in Chrome, I have indeed advanced to the next image. Let's try the left arrow. I'm hitting it and nothing's happening. So I think the connection to the debugger must have gotten broken. Back here in Visual Studio Code I can see that my breakpoint is an open circle. The S code calls this an unbound breakpoint. Usually if I just unset that and reset it that should get me back in business.
>
> **[4:46](https://www.linkedin.com/learning/learning-javascript-debugging-2/debug-a-wordpress-theme-in-visual-studio-code-22151336?u=76281980&t=286)** So back in Chrome, I'll hit the left arrow again, and now we can see what we're trying to see. I have stopped on that line and as I start to step through it I can see here in my watch expression for E.Witch that the actual left arrow key code is 37 and I have 32 here. So it's the wrong code and that must be the problem. So let's change this to seven, and now I'll remove the breakpoint, cause I don't think I'll need it anymore. And back in Chrome, we'll refresh the page. And now when I hit left arrow I go back and right arrow goes forward. Keyboard navigation is working again. And back here in Visual Studio Code, if I hit the stop button, that will close that newly spawned instance of Chrome and I'm totally done. So now you've seen how to configure Visual Studio Code for debugging a web application. The JavaScript debugger extension can handle both web apps and node js. And gives you an IDE quality experience right here, in this free open source editor. It's quite powerful.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3), [WordPress](../../Skills/Web%20Development/WordPress.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **Tools:** visual studio (9)
> **Env Vars:** url (5), ide (1)
> **CLI Commands:** make (1), node (1)
> **UI Navigation:** open the (2)
> **File Paths:** launch.json (1)
> **Cross-References:** go back to (1)
> **Definitions:** is an  (1)

#### [Use a debugger as a learning tool on a live site](https://www.linkedin.com/learning/learning-javascript-debugging-2/use-a-debugger-as-a-learning-tool-on-a-live-site-22149297?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/use-a-debugger-as-a-learning-tool-on-a-live-site-22149297?u=76281980&t=0)** - [Instructor] Up to this point, we've been using a [JavaScript](../../Skills/Software%20Development/JavaScript.md) debugger to look for problems in code we're editing, but there's another way to employ a debugger that's also quite useful. You can bring all the same tools to bear on a live website to learn about how it works. Let's give that a try with this one, hansel and petal. It's very similar to some examples that we've been using previously offline but this is the online version of it. This is a fictitious business that we use for demonstration purposes, and this is the cart page where we can choose some flowers to buy. So looking over the behaviors here of course we have some hover interactivity. And as we click these, they start to populate the cart. And for some of them we have a color selector that will change the image here. We've got some prices. And if we change the quantity, the totals update. So first, let's see how this color changing is taking place. We'll open up the developer tools. Give ourselves a little more room on the page. We'll scroll to the area of interest. And now I'm going to use the event listener breakpoints. And we're going to listen to the change event. So now anytime a change event is executed anywhere on the page, we're going to break immediately. So let's change to purple here. And the first thing that happens is we get stuck into jQuery, which we don't actually have any interest in. So up in the call stack, we can add that to the ignore list. So we'll resume execution and try this again.
>
> **[1:40](https://www.linkedin.com/learning/learning-javascript-debugging-2/use-a-debugger-as-a-learning-tool-on-a-live-site-22149297?u=76281980&t=100)** And now we're in improved.js. If I right click this and choose reveal in sidebar, it brings me to the location of that script. So that's handy. I know exactly where that is. And I have jQuery in the ignore list so now I won't get dropped in there. So let's take a look at this image item. I'm going to add a watch expression for this to make it easier to see what's happening with it. So now we'll step down and we can see that the value that we're collecting is this, looks like it's probably a file name. And if I mouse over this item, I can see the current value for the source of this image, is that purple calla lily. I've just chosen pink, so my expectation is that we're going to swap it out for the pink version. And looking at that file name it's 70 underscore purple and so forth. And that looks a lot like the value that's being passed in here. And so now if I step over this and resume, indeed, that gets swapped out. So now I know exactly where this is happening and what it's doing. Let's check out the quantity updater. This time I'm going to disable the change event. And because this is changed via keyboard, we'll use a keyboard key up event and we'll change this to let's just order tons of them. Let's order a hundred flowers.
>
> **[3:13](https://www.linkedin.com/learning/learning-javascript-debugging-2/use-a-debugger-as-a-learning-tool-on-a-live-site-22149297?u=76281980&t=193)** And that takes me into the same JavaScript file, but now the correct key up listener that handles all of these quantity updaters. And so anytime we have a key up event that isn't for return or enter, that's what key code 13 is, I looked it up. We're going to run this calc totals function. So let's step down to that and step in to look at it. And as we start to step down through this, I can see that for each of these quantity fields that are enabled on the page, we're going to take some actions. I don't see this BAB cost variable. It must be in the scope above local. There it is. So now we've set a formatted price for this. The totals will get updated with what we have so far. And if I mouse over these, some of these are elements selected on the page, and in the little popup I can mouse over the item to see its equivalent element highlighted on the page. That's pretty slick. So now we understand how that quantity updater works. Let's check out one more little thing. Usually styling is handled by css, but a lot of times there are classes added and the addition or removal of classes is typically handled by JavaScript.
>
> **[4:49](https://www.linkedin.com/learning/learning-javascript-debugging-2/use-a-debugger-as-a-learning-tool-on-a-live-site-22149297?u=76281980&t=289)** So now I can right click this and choose inspect to open the elements inspector, and I can watch these items as I click, and I can see that this selected element is added by JavaScript. And also there's an after pseudo selector here, that adds the check mark from this sprite image. So this is all quite informative. If you want to take apart a website and see how it's built visually and functionally, the combination of that debugger, the sources inspector, and the elements inspector can get you pretty much everything you need to know. In more complicated web applications, JavaScript files will often be midified and even obfuscated making them challenging and sometimes almost impossible to read. So you won't always be able to reveal all the inner workings using these tools. But the debugger is a really good starting place. Being able to use a debugger in this way for self-education is very handy.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (5)
> **Code Identifiers:** jquery (2)
> **Definitions:** is a  (2)
> **File Paths:** improved.js (1)
> **CLI Commands:** make (1)
> **Env Vars:** bab (1)
> **UI Navigation:** open the (1)
> **Analogies:** similar to (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/learning-javascript-debugging-2/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/next-steps?u=76281980&t=0)** - [Instructor] Now that you have a richer understanding of how to debug [JavaScript](../../Skills/Software%20Development/JavaScript.md), you might want to explore some other related parts of the library. The selection of courses is ever expanding. And of course, nearly anything you're interested in is likely just a search away. But I do have a few recommendations. Both for courses and for books. First up, a few courses. Learning chrome web developer tools. Is a course that will tell you much more about the huge variety of tools that are available in Chrome's web developer tools. We mostly looked at the debugger and a little bit at the element inspector. But there's a whole bunch of tools in there that are worth learning. [JavaScript Essential Training](JavaScript%20Essential%20Training.md) and Node dot JS essential training are comprehensive courses that will take you through JavaScript, in general. And then Node dot JS development. And then finally, there's my own course, [React](../../Skills/Web%20Development/React.js.md) for web designers. If you're interested in getting started with React, it's a great way to start to dip your toes in the water and see how it works. For books, you could start with the You Don't Know JavaScript series by Kyle Simpson. He also has some videos in the library that you can check out. He's one of my favorite authors on JavaScript. His writing is very technical but also very approachable. Eloquent JavaScript by Marijn Haverbeke is a great introduction to JavaScript as a whole. It's not quite as technical. But it is an excellent book for learning JavaScript. And then finally, there's a whole series of books in the Exploring JavaScript series, including Exploring ES6 and many more by Dr. Axel Rauschmeyer. His books are, kind of like, a specification, but written in a more readable fashion. So they're very technical.
>
> **[1:32](https://www.linkedin.com/learning/learning-javascript-debugging-2/next-steps?u=76281980&t=92)** But if you're looking to find out, let's say, the spread operator means and how to use it, in all the different ways you can use it in ES6. Our other pieces of more advanced in newer JavaScript, I couldn't recommend these books more. So there are a few options for courses and books to check out to learn more about JavaScript and debugging, in general.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (11), [React](../../Skills/Web%20Development/React.js.md) (2)
> **CLI Commands:** node (2), find (1)
> **Definitions:** is a  (2), is an  (1)
> **Env Vars:** es6 (2)
> **Documentation:** specification (1)
> **Analogies:** kind of like (1)
> **Prerequisites:** getting started (1)
> **Speakers:** - [instructor] (1)

#### [Extra JavaScript resources](https://www.linkedin.com/learning/learning-javascript-debugging-2/extra-javascript-resources?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-javascript-debugging-2/extra-javascript-resources?u=76281980&t=0)** - [Joe] Having explored [JavaScript](../../Skills/Software%20Development/JavaScript.md) debugging, you may be wondering what a possible next step would be in your learning journey. Let's look at a couple possibilities. First up is [Version Control](../../Skills/Web%20Development/Version%20Control.md). Version control software lets developers keep track of our work over time. The way I like to think of this is it lets you experiment fearlessly. If you have version control backing you up, you always know that you can try new things and not have to worry about breaking something because you can always go back. It's kind of like undo and redo on a larger scale. Some examples of version control software are [Git](../../Skills/Software%20Development/Git.md), which is definitely the most popular right now, but also Mercurial, which is abbreviated as Hg, and Subversion, which has been around for a long time. These tools keep getting easier to use, both the command line tools themselves and a whole bunch of graphical UIs that are built on top of them. We have a lot of courses on version control in the library. Here are a few examples you might look into. First is Version Control for Everyone, which is a course of mine that explores version control as applied to things that are not strictly just programming. [GitHub](../../Skills/Software%20Development/GitHub.md) for Web Designers is a great introduction to GitHub for web designers. [Learning Git and GitHub](Learning%20Git%20and%20GitHub.md) is more general. GitHub is the most popular service for working with version control pretty much all over the world, so between GitHub for Web Designers and [Learning Git and GitHub](Learning%20Git%20and%20GitHub.md), you should be covered in terms of how to use it. And then Learning Software Version Control is a more general course that helps you apply these concepts more generally. Next up, I'd recommend you look at [Test-Driven Development](../../Skills/Software%20Development/Test-Driven%20Development.md). This is a different way of working than you might be used to.
>
> **[1:32](https://www.linkedin.com/learning/learning-javascript-debugging-2/extra-javascript-resources?u=76281980&t=92)** With TDD, you describe how a feature should work in simple code, usually something very small, and you do that before you've written any of the code implementing that feature. A typical project will have lots and lots of tests for all the things that a site or an application is supposed to do. So for example, a test might be when I click the Add to Cart button, one instance of a selected item should be added to the cart. And then after I've written the test, then I write the code that implements the feature. Tests either pass or fail, and the goal is to get all of your tests to pass and to keep them that way. Tests should be rerun automatically whenever new code is added. Approaching a project this way really helps avoid regressions, meaning parts of the site or code base breaking without anybody noticing. TDD can be applied just to code or to the appearance of a website. Here are some of the tools available for working in this fashion. For writing tests, when we're working with JavaScript, we have Jasmine, Jest, Mocha, QUnit, and many others. Once you've described the tests, you need a tool to run them. Most people are using Karma these days. For end to end testing, which is more feature-based, where you might be interacting with a website, not just the JavaScript, there are many, many tools available here as well, including Headless Chrome, Puppeteer, and Nightmare, which all basically work with the Chrome web engine, only there's no user interface applied. This makes it much faster to run and execute your tests, and it basically gives you a programmable web browser. Some other tools that don't necessarily work with Chrome but work with other browsers as well
>
> **[3:04](https://www.linkedin.com/learning/learning-javascript-debugging-2/extra-javascript-resources?u=76281980&t=184)** are PhantomJS and [Selenium](../../Skills/Software%20Development/Selenium.md) and WebDriver. All of these tools are worth exploring. There are many courses on test-driven development in the library as well. Here are a couple of examples. Test-Driven Development with [Node.js](../../Skills/Web%20Development/Node.js.md) will show you how to work with all these tools in a JavaScript context for writing Node apps. And then [Programming Foundations- Test-Driven Development](Programming%20Foundations-%20Test-Driven%20Development.md) will explore all of these concepts in a more general fashion that you can apply to any programming language. So those are a couple of options for where you can look next, version control and test-driven development. Go check them out.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Version Control](../../Skills/Web%20Development/Version%20Control.md) (10), [GitHub](../../Skills/Software%20Development/GitHub.md) (6), [Test-Driven Development](../../Skills/Software%20Development/Test-Driven%20Development.md) (5), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (4), [Git](../../Skills/Software%20Development/Git.md) (3)
> **Tools:** github (6), command line (1)
> **CLI Commands:** git (3), node (2)
> **Definitions:** is a  (4)
> **Env Vars:** tdd (2)
> **Analogies:** kind of like (1), for example (1)
> **File Paths:** node.js (1)
> **Speakers:** - [joe] (1)


## Instructor

- [Joe Chellman](../../Instructors/Software%20Development/Joe%20Chellman.md)

## Resources

- Exercise files available

## Skills Covered

- Debugging Code
- JavaScript

## Path Context

### In [Advance Your JavaScript Skills](../../Paths/Software%20Development/Learning%20Paths/Advance%20Your%20JavaScript%20Skills.md)
← [Foundational JavaScript Security](Foundational%20JavaScript%20Security.md) | **8 of 10** | [End-to-End JavaScript Testing with Cypress.io](End-to-End%20JavaScript%20Testing%20with%20Cypress.io.md) →

## Appears In

- [Advance Your JavaScript Skills](../../Paths/Software%20Development/Learning%20Paths/Advance%20Your%20JavaScript%20Skills.md)

## Related Courses

_Courses sharing skills:_

- [End-to-End JavaScript Testing with Cypress.io](End-to-End%20JavaScript%20Testing%20with%20Cypress.io.md) — JavaScript
- [Foundational JavaScript Security](Foundational%20JavaScript%20Security.md) — JavaScript
- [JavaScript- Test-Driven Development (ES6)](JavaScript-%20Test-Driven%20Development%20(ES6).md) — JavaScript
- [JavaScript- Best Practices for Data](JavaScript-%20Best%20Practices%20for%20Data.md) — JavaScript
- [Javascript Best Practices For Code Formatting](Javascript%20Best%20Practices%20For%20Code%20Formatting.md) — JavaScript

---

[↑ Back to top](#)