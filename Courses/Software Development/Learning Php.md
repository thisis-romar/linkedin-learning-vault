---
type: course
cssclasses:
  - lle-course
  - lle-level-beginner
slug: learning-php-4
url: "https://www.linkedin.com/learning/learning-php-4"
level: Beginner
updated: 3/19/2021
learners: 35332
skills:
  - PHP
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C4E0DAQFWeu43pPltjg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1619134359103?e=2147483647&amp;v=beta&amp;t=3zAh3mfV9u7-rkW23tHE_NJVJF74m2OBcU7zJXZsJDY"
linkedin_topic: Software Development
learning_paths:
  - '[[Get Started with PHP]]'
next_courses:
  - '[[PHP Essential Training]]'
path_nav: '[{"path":"Get Started with PHP","position":1,"total":6,"prev":null,"next":"PHP Essential Training"}]'
path_count: 1
tags:
  - course
  - topic/software-development
  - topic/web-development
  - skill/php
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/Learning%20Php.md)

![Learning Php](https://media.licdn.com/dms/image/v2/C4E0DAQFWeu43pPltjg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1619134359103?e=2147483647&amp;v=beta&amp;t=3zAh3mfV9u7-rkW23tHE_NJVJF74m2OBcU7zJXZsJDY)

# Learning Php

> PHP remains one most popular server-side languages used to build dynamic websites. Although it is not especially difficult to use, nonprogrammers often find it intimidating. This introductory course was designed to change that by teaching you PHP through a series of clear, focused, and easy-to-follow lessons. After briefly explaining what PHP is, instructor Joe Casabona introduces the features of 

> [LinkedIn Learning](https://www.linkedin.com/learning/learning-php-4) | Beginner | 35K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Should you learn PHP?](#should-you-learn-php)
  - [A note on PHP 8.0](#a-note-on-php-80)
  - [Developing locally](#developing-locally)
- [**1. The Basics**](#1-the-basics) (8 videos)
  - [How PHP works](#how-php-works)
  - [Printing on the screen](#printing-on-the-screen)
  - [Using variables](#using-variables)
  - [Single vs. double quotes](#single-vs-double-quotes)
  - [What are arrays and how do they work?](#what-are-arrays-and-how-do-they-work)
  - [Defining arrays](#defining-arrays)
  - [Challenge: Display basic information on an HTML page](#challenge-display-basic-information-on-an-html-page)
  - [Solution: Display basic information on an HTML page](#solution-display-basic-information-on-an-html-page)
- [**2. Control Structures**](#2-control-structures) (8 videos)
  - [Boolean operators and the truth (update for PHP 8.0)](#boolean-operators-and-the-truth-update-for-php-80)
  - [Logical operators](#logical-operators)
  - [Creating if/else statements](#creating-ifelse-statements)
  - [Ternary operations](#ternary-operations)
  - [Switch statements](#switch-statements)
  - [Match expression](#match-expression)
  - [Challenge: Display content based on status](#challenge-display-content-based-on-status)
  - [Solution: Display content based on status](#solution-display-content-based-on-status)
- [**3. Math in PHP**](#3-math-in-php) (5 videos)
  - [Arithmetic operators](#arithmetic-operators)
  - [Figuring out even/odd with modulus](#figuring-out-evenodd-with-modulus)
  - [Incrementing and decrementing variables](#incrementing-and-decrementing-variables)
  - [Challenge: Implementing a complicated calculation](#challenge-implementing-a-complicated-calculation)
  - [Solution: Implementing a complicated calculation](#solution-implementing-a-complicated-calculation)
- [**4. Loops**](#4-loops) (6 videos)
  - [What are loops?](#what-are-loops)
  - [Do/while loops](#dowhile-loops)
  - [For loops](#for-loops)
  - [Working through arrays with foreach loops](#working-through-arrays-with-foreach-loops)
  - [Challenge: Calculating the Fibonacci sequence](#challenge-calculating-the-fibonacci-sequence)
  - [Solution: Calculating the Fibonacci sequence](#solution-calculating-the-fibonacci-sequence)
- [**5. Functions and Objects**](#5-functions-and-objects) (11 videos)
  - [What are functions?](#what-are-functions)
  - [Using built-in functions](#using-built-in-functions)
  - [Writing custom functions](#writing-custom-functions)
  - [Union typing variables](#union-typing-variables)
  - [Optional or named arguments](#optional-or-named-arguments)
  - [Anonymous functions](#anonymous-functions)
  - [What are objects and classes?](#what-are-objects-and-classes)
  - [Defining a simple person class (update for PHP 8.0)](#defining-a-simple-person-class-update-for-php-80)
  - [Using the person class](#using-the-person-class)
  - [Challenge: Sort an array of objects](#challenge-sort-an-array-of-objects)
  - [Solution: Sort an array of objects](#solution-sort-an-array-of-objects)
- [**6. Including External Files**](#6-including-external-files) (6 videos)
  - [Organizing your code](#organizing-your-code)
  - [Using include and require](#using-include-and-require)
  - [include_once and require_once](#include_once-and-require_once)
  - [Namespacing](#namespacing)
  - [Challenge: Build a simple templating system for the provided markup](#challenge-build-a-simple-templating-system-for-the-provided-markup)
  - [Solution: Build a simple templating system for the provided markup](#solution-build-a-simple-templating-system-for-the-provided-markup)
- [**7. Troubleshooting Your Code**](#7-troubleshooting-your-code) (7 videos)
  - [Common debugging tools in PHP](#common-debugging-tools-in-php)
  - [Types of PHP errors](#types-of-php-errors)
  - [Why is my page blank?](#why-is-my-page-blank)
  - [Common parse errors](#common-parse-errors)
  - [Common PHP errors](#common-php-errors)
  - [Try/catch statements (update for PHP 8.0)](#trycatch-statements-update-for-php-80)
  - [Breaking changes moving from 7.x to 8.0](#breaking-changes-moving-from-7x-to-80)
- [**8. Processing Forms**](#8-processing-forms) (9 videos)
  - [GET vs. POST](#get-vs-post)
  - [Handling basic form elements](#handling-basic-form-elements)
  - [Handing multiple-choice elements](#handing-multiple-choice-elements)
  - [Form validation: Making sure required fields are filled in](#form-validation-making-sure-required-fields-are-filled-in)
  - [Form validation: Appropriate input](#form-validation-appropriate-input)
  - [Form validation: Sanitizing input](#form-validation-sanitizing-input)
  - [Sending the form data in an email](#sending-the-form-data-in-an-email)
  - [Challenge: Process a simple form and send the email](#challenge-process-a-simple-form-and-send-the-email)
  - [Solution: Process a simple form and send the email](#solution-process-a-simple-form-and-send-the-email)
- [**9. Advanced Topics**](#9-advanced-topics) (4 videos)
  - [Reading files in PHP](#reading-files-in-php)
  - [Writing files](#writing-files)
  - [Maintaining state with cookies](#maintaining-state-with-cookies)
  - [Maintaining state with sessions](#maintaining-state-with-sessions)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Should you learn PHP?](https://www.linkedin.com/learning/learning-php-4/should-you-learn-php?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/should-you-learn-php?u=76281980&t=0)** - [Joe] One of the most popular languages for creating dynamic websites is [[PHP]], but with the popularization of [[JavaScript]] tools, like [[React.js|React]] and [[Angular]], is it worth investing your time in learning PHP? The answer unequivocally is yes. For many web applications, PHP is very much an integral part of what makes them work. Front end tools like React can not talk to a database or provide a backend infrastructure like PHP can. Further, popular [[Content Management Systems (CMS)|content management systems]] like [[WordPress]] and [[Drupal]] and websites like [[Facebook]], Wikipedia, and Yahoo, all use PHP. In fact, 80% of all websites use at least some PHP. Learning PHP will give you the skills you need to develop and maintain dynamic, interactive websites. Hi, I'm Joe Casabona, a front end developer and educator who's been using PHP since 2003 and WordPress since 2004. So join me in my [[LinkedIn]] Learning course, which will show you everything you need to know to start using PHP.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (9), [[React.js|React]] (2), [[WordPress]] (2), [[JavaScript]] (1), [[Angular]] (1)
> **CLI Commands:** php (9)
> **Env Vars:** php (9)
> **Speakers:** - [joe] (1)

#### [A note on PHP 8.0](https://www.linkedin.com/learning/learning-php-4/a-note-on-php-8-0?u=76281980)

#### [Developing locally](https://www.linkedin.com/learning/learning-php-4/developing-locally?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/developing-locally?u=76281980&t=0)** - [Instructor] While you will run [[PHP]] applications on a web server most of the time, setting a server up right now is not necessary. Instead, we can start with local development. A local development environment is generally free, or at least more affordable and faster than working with a server. In fact, a local development environment is basically a web server running on your computer. You also won't run the risk of crashing other websites on that server, so it's safer as well. There are a few ways you can develop locally. Depending on if you're on a Mac or PC, you can use MAMP or WAMP, respectively.
>
> **[0:48](https://www.linkedin.com/learning/learning-php-4/developing-locally?u=76281980&t=48)** Both of these applications will set up an Apache [[MySQL]] and PHP, or AMP, server right on your computer. You'll be able to manage files and view your web applications in the browser. Now, there's a lot to know about AMP servers, and while you can install one of them now and quickly be up and running, if you want to learn more, you can check out the Apache MySQL and PHP courses in the [[LinkedIn]] Learning library. These will give you more depth of knowledge on exactly what's happening on your computer. But again, for now you can just install MAMP, for example, and start writing PHP code for this course right away. You can also install an IDE, or integrated development environment. This is a program that will run PHP code directly in the program. No other tools are necessary. There are several IDEs available for PHP. A popular one is PhpStorm, which has a free 30 day trial. A free alternative is NetBeans. I'll be using VS Code to write the PHP, which I will then run in my browser using my local development environment. No matter what you choose, setting up a local environment is a fast, safe way to learn PHP.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (8), [[MySQL]] (2), [[LinkedIn]] (1)
> **Env Vars:** php (8), mamp (2), amp (2), wamp (1), ide (1)
> **CLI Commands:** php (8), apache (2), mysql (2)
> **Prerequisites:** install (3), set up (1)
> **Definitions:** is a  (2)
> **Tools:** vs code (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)


### 1. The Basics

[↑ Back to Table of Contents](#table-of-contents)

#### [How PHP works](https://www.linkedin.com/learning/learning-php-4/how-php-works?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/how-php-works?u=76281980&t=0)** - [Joe] [[PHP]] stands for PHP Hypertext Processor, it's a recursive acronym. But basically it's a programming language that allows you to add dynamic and backend functionality to your website. It's often used for tasks that a markup language like [[HTML]] can perform; processing [[Forms]], talking to a database, creating secure areas, reading from a text file and more. PHP is a server-side language, meaning our server or remote computer does all of the work and then sends the end results to our browsers. You can imagine the process like a translator. The server translates the PHP into a language our browsers can understand. In other words, PHP allows you to apply logic and perform interactive tasks based on a set of criteria. HTML strictly structures the data on a page. HTML, CSS and [[JavaScript]] are also client-side languages, meaning our computers, specifically a web browser, does all of the processing, and we can see all the code. That's not the case for PHP. The strong benefit of that is that you can write more secure code. Since not everything is exposed in the browser, you don't have to worry about showing passwords or other sensitive data. You can retrieve and sanitize data before sending it to the browser.
>
> **[1:35](https://www.linkedin.com/learning/learning-php-4/how-php-works?u=76281980&t=95)** The server knows which files use PHP because they end in .PHP, much like HTML files and in .HTML. The server takes the PHP files, processes them, and sends only the output to the browser. Further, the server knows only to process the PHP code. That code will be between opening PHP tags and closing PHP tags. The [[Representational State Transfer (REST)|rest]] will be treated as plain text or HTML. Because of this, you will see HTML and PHP commingling in the same files a lot. But remember, while HTML can go into PHP files, PHP cannot go into HTML files. Now that you know a little bit of how PHP works, let's start using it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (16), [[HTML]] (9), [[Forms]] (1), [[JavaScript]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **Env Vars:** php (16), html (9), css (1)
> **CLI Commands:** php (16)
> **Definitions:** stands for (1), is a  (1), in other words (1)
> **Analogies:** imagine (1)
> **Speakers:** - [joe] (1)

#### [Printing on the screen](https://www.linkedin.com/learning/learning-php-4/printing-on-the-screen?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/printing-on-the-screen?u=76281980&t=0)** - [Instructor] The most common task you'll perform in [[PHP]] is printing stuff on the screen. After all, your main goal is to create dynamic websites. The simplest way to print something out on the screen is to use a php echo statement. So I'll write my php opening tag, which is the less than sign, a question mark and then the letters php and then on a new line, I'll write echo hello world. Then I'll close my php tags. Echo tells php print out whatever follows this [[Microsoft Word|word]]. You can also see that there are single quotes around hello world. There are nuanced differences between a single and double quotes that you'll learn about later. Finally, this statement ends in a semicolon. This will be the case most of the time and you'll learn what the exceptions are later, but know that if you see an error, you may have missed a semicolon. There are a few other ways that you may see information printed in php. There's the shorthand echo tag, which is less than question mark equals and then hello world, which is a shorter way to write quick statements, but this is often not recommended by developers as it can be turned off on the server and that will lead to unintended results and errors.
>
> **[1:35](https://www.linkedin.com/learning/learning-php-4/printing-on-the-screen?u=76281980&t=95)** There's also the print statement, which functions pretty much exactly like the echo statement except we use the word print instead of echo. Echo is a little faster and a little more flexible, so it's more common. That's what this course will use unless otherwise noted. So let's look at what this produces in our browser. You can see we have the words hello world three times, but there are no line breaks. That's because php prints exactly what we tell it to. Later, you'll learn how to produce your own line breaks and other special characters. php code and [[HTML]] markup can also reside together in a single file on your server. For example, here is a plain html file. We can write this same output in php by adding php tags within the html. We just need to make sure our file ends in dot php. So here, we will use a basic echo statement to print out hello world. Now we'll save this file and if we go back to our browser, we see the words hello world here and if we look at the source, we don't see any php code, we just see the resulting html. Wrapping php commands, also known as statements, in html is a very common convention
>
> **[3:09](https://www.linkedin.com/learning/learning-php-4/printing-on-the-screen?u=76281980&t=189)** and there's one more that we should talk about and that's comments. Comments are a way for you to write nonfunctional code in php. In other words, you can leave notes to yourself or to other developers about what the code does. With two forward slashes, you can write a single line comment or with a forward slash and then an asterisk, you can start a multi-line comment. To end that multi-line comment, you would write asterisk and then a backslash. Output is a first and crucial step towards writing php programs and now you have the very basics down.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (16), [[HTML]] (5), [[Microsoft Word|Word]] (2)
> **CLI Commands:** php (16), make (1)
> **Definitions:** is a  (5), known as (1), in other words (1)
> **Cross-References:** go back to (1)
> **Analogies:** for example (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [instructor] (1)

#### [Using variables](https://www.linkedin.com/learning/learning-php-4/using-variables?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/using-variables?u=76281980&t=0)** - [Instructor] Printing information onto the screen is great, but we don't always know exactly what information we want to print while we're writing our code. For example, if we want to print the visitor's name, there's no possible way of knowing that information before they visit. That's where variables come in. Variables are a way to store information in order to reference it later. Variables can be used to store numbers, names, or other important data. In [[PHP]] variables must start with a dollar sign, followed by a letter or an underscore, then an alphanumeric character string. Here's an example. You see that we have our opening PHP tags, a dollar sign followed by the [[Microsoft Word|word]] age, and then an equal sign, and some value, in this case 34, and we have our semi-colon. Age is a placeholder for the number that we have stored in it, which is 34. PHP knows age is 34 because of the equal sign, which means is assigned to or gets. I'll usually say gets when referencing variables. We can then use age as if it's the number 34. Printing it, or even doing calculations on it, like adding, subtracting, or multiplying. If we decide later we want to change it we can do that too. Variables are easier to keep track of. You can change them on the fly, and most importantly, like the opening example,
>
> **[1:33](https://www.linkedin.com/learning/learning-php-4/using-variables?u=76281980&t=93)** sometimes you don't know certain information at runtime. There are also special variables called constants that are defined a little differently. In this case, you would use a function or a keyword called define with a parenthesis around it. In the parentheses you will have the name of the constant in quotes, and in this case, it's version, then a comma and the value of that constant, which in this case is 1.1. We can then treat the constant as if it's a variable without a dollar sign. A couple of things to note though, are that constants are case sensitive. So if we were to write version in lowercase here, it would not work. And the important part, the reason that constants are called constants is because they are immutable, or they can't be changed at runtime. So if we try to reassign a new value to version using either the gets symbol or the defined function, again, neither of those will work. Constants are great if you have some value that you know you absolutely don't want to change throughout runtime. In both cases for variables and constants, there are a set of predefined values that we can't use. These are known as reserved words or predefined variables. So for example, we can't use the word this
>
> **[3:07](https://www.linkedin.com/learning/learning-php-4/using-variables?u=76281980&t=187)** in a variable assignment. Similarly, we can't create a constant called PHP underscore version, or some other constants that you see here in the PHP manual. The last important aspect to know about variables right now is that computers are very specific about how different types of data are represented. Words are represented differently than numbers are. And further whole numbers are represented differently than decimal numbers. In programming languages you need to tell the computer, if you're giving it a word or a number. Luckily PHP handles most of this for us. We don't need to declare the kind of data our variables are most of the time. PHP will figure out how we want to use it, and then do it for us. This makes PHP a weak type language. That said there are a few different ways variables can be represented, as integers which are whole numbers like zero, one, or 45. Floats, which are numbers with decimal points. For example, 1.0 or 3.16. Characters are single letters, numbers, or symbols. It's worth noting a few things about characters. The first is that numbers with and without quotes are handled differently. You can do calculations with the integer one, but not the character one,
>
> **[4:40](https://www.linkedin.com/learning/learning-php-4/using-variables?u=76281980&t=280)** which is represented by quotes around the number one. It's also worth noting that capital letters are considered different from lowercase letters. So a capital A and a lowercase a are considered different by PHP. This is why you are able to use upper and lowercase letters in your passwords to make them more secure. The last two variable types here are strings, which are a collection of characters, such as hello, or Racer five. And booleans, booleans can have only one of two values, true or false. Now there is one more type of variable, and that's called an array. Arrays are collections or sets of any of the previously mentioned variable types. And you'll learn a lot more about those. While integers, floats, and booleans can be written without the use of quotes, strings and characters must be placed in quotes. So for example, we have a string with the variable name. So we have name gets Joe, and we have a variable called first underscore letter. So we have first underscore letter gets J. You'll notice I used both single quotes and double quotes in the example above, and there are nuanced differences between them, but for assignments like these, we can use the single and double quotes interchangeably. You might also notice that with first underscore letter
>
> **[6:16](https://www.linkedin.com/learning/learning-php-4/using-variables?u=76281980&t=376)** there's no space. Spaces are not allowed in variable names and often multi-word variables will replace spaces with an underscore. Variables are an extremely important part of working with PHP. You will use them for storing and retrieving data, and it will make writing complex programs much easier.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (10), [[Microsoft Word|Word]] (4)
> **CLI Commands:** php (10), make (2)
> **Env Vars:** php (10)
> **Analogies:** for example (4), such as (1)
> **Versions:** 1.1 (1), 1.0 (1), 3.16 (1)
> **Definitions:** is a  (1), known as (1)
> **Speakers:** - [instructor] (1)

#### [Single vs. double quotes](https://www.linkedin.com/learning/learning-php-4/single-vs-double-quotes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/single-vs-double-quotes?u=76281980&t=0)** - [Instructor] So you can use single or double quotes for strings but what's the difference? Let's take a look. The main difference between single and double quotes is any string in double quotes will be processed by [[PHP]] before it's outputted. That means, for example, any variables in strings with double quotes will become their literal value. Let's take a look at an example here where on the left side of the screen, I have my code and on the right side of the screen, I have the result in the browser. I'll type my opening php tag. And then I will create a variable called name. So we'll say name gets Joe. And I'll put Joe in single quotes. Then we'll say echo double quote Hello, comma space name. Remember, PHP prints exactly as we type within the quotes, so we need to add any punctuation and spaces and things like that. Then we'll add our semicolon. We'll save this and we'll fresh the page. You could see it says Hello, Joe. Now, if we change this string to single quotes, first of all, you'll notice that my editor has changed the highlighting to make this the maroon instead of the same blue as the variable. When I save this and refresh, the single quotes prints out the literal string that we've written here. Because single quotes don't go
>
> **[1:33](https://www.linkedin.com/learning/learning-php-4/single-vs-double-quotes?u=76281980&t=93)** through the PHP processor first, it makes them a little bit more efficient than double quotes but not so efficient that we should never use double quotes. Still, if you still want to print a variable with single quotes, we'll need to use something called string concatenation. So I've deleted the variable reference here and I'm going to write Hello comma and then a single quotes and then I'm going to add a period. That period is the concatenation character. So then I will write name in no quotes, another period and then I'll open the single quote again, add my exclamation point, close the single quote and add the semicolon. Now, I'll save this and I'll refresh. Notice that there's no space after the comma. That's because even though we have spaces within the concatenation, we don't have any within quotes. We need to add those, so I'll add the space after the comma or refresh the space and you see the space there. So this actually brings up another issue because the PHP processor is going to print exactly what we write. How do we print single or double quotes in strings? If I say something like Welcome to Joe's Website, you'll see that my highlighting is all messed up
>
> **[3:09](https://www.linkedin.com/learning/learning-php-4/single-vs-double-quotes?u=76281980&t=189)** and if I save this, I'm going to get an error because I have ended the quoted string too early. The answer to that is escape characters. So with our single quote here, we can write a backslash. You can see the backslash is bright red and the [[Representational State Transfer (REST)|rest]] of our string is now that maroon color again. If I refresh, we have no error and we have the statement. In short, escape characters allow you to print out otherwise unprintable characters, like quotes, new lines and more. They are primarily used with double-quoted strings, since that's where they are most necessary. Double-quoted strings also recognize all escape characters, unlike single-quoted strings, which only recognize a couple. You can find a full list of the escape characters on [php.net](https://php.net) but some of the important ones, the ones that you'll probably see the most often are on the screen here. \n for new line. \\ for a backslash. \$ for a dollar sign, since variables require a dollar sign in their name. And \t for tab. So let's look at a few more examples. Let's clear this code. And let's say we wanted to print two lines. So I could say echo This is line 1. And then add my own line here
>
> **[4:47](https://www.linkedin.com/learning/learning-php-4/single-vs-double-quotes?u=76281980&t=287)** and say echo This is line 2. But if I save and then refresh, you'll see that the lines show up right next to each other. And again, that's because we need our escape characters. So I'll change this to double quotes. And I'll say This is line 1 \n and I'll change this to double quotes for consistency. So now if we save this file, and refresh the browser, all right, so you'll see a space was added here and these are still on the same line. That's because we are rendering his code in the browser so it's subject to the way [[HTML]] is interpreted. If we view the page source though, you will see this information gets printed on to two lines. If I remove the escape character, and refresh, they're right next to each other. So let's look at one more example of escape characters. And we will use a full sentence here. So if I say Joe's Nickname in high school was Joey Calzone,
>
> **[6:00](https://www.linkedin.com/learning/learning-php-4/single-vs-double-quotes?u=76281980&t=360)** which is absolutely true, you could see that our PHP parser is upset. That's because we haven't properly escaped our single quote. Since we're using single quotes, we can use the escape character for single quotes. That's actually the only escape character single quotes will recognize. So if I save this and refresh, perfect. Now, if I change this to double quotes, the single quote will be fine because PHP is not expecting the end of the print statement until it hits the first double quote. But the nickname does need double quotes now. So I'll add those. Save this and we'll have the same results. In general, we're going to use single quotes because they are slightly faster to process in PHP. The exception is if there's a reason to use double quotes. Like printing a variable, or using some escape character.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (9), [[Representational State Transfer (REST)|Rest]] (1), [[HTML]] (1)
> **CLI Commands:** php (9), make (1), find (1)
> **Env Vars:** php (7), html (1)
> **URLs:** [php.net](https://php.net) (1)
> **UI Navigation:** open the (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [What are arrays and how do they work?](https://www.linkedin.com/learning/learning-php-4/what-are-arrays-and-how-do-they-work?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/what-are-arrays-and-how-do-they-work?u=76281980&t=0)** - [Instructor] Arrays are a bit different from the [[Representational State Transfer (REST)|rest]] of our variables. Arrays act as key-value pairs that essentially function as a set of variables, so this allows us to make a list of related items that we can reference as one big variable. The benefit of organizing data this way is it's easier to manage when we're working with related values or complex data sets, like when retrieving information from a database. Array values can be any of the data types that standard variables can be, and we can mix them. So we can have an array that includes strings only, an array that includes integers only, or an array that includes integers, strings, characters, or anything else you can think of. There are two types of arrays you should know about, indexable arrays, which use numbered keys, and associative arrays, which use strings as keys. You'll learn how to define each of those in [[PHP]] later but for now, let's learn a little bit about how they work as a concept. Indexable arrays are basically numbered lists. Each item in the array, or list, has an index assigned to it and that's how you reference it. Items in an array are known as elements, but there's a small catch. The indexing starts at zero, not one. So if we have an array called colors and we want the first element in the array, we'd ask for the zeroth element.
>
> **[1:34](https://www.linkedin.com/learning/learning-php-4/what-are-arrays-and-how-do-they-work?u=76281980&t=94)** That is, we'd use the index zero. Let's look at a colors array with three elements. This is an array of size three. The indexes here are numbered from zero through two, and here's how you can visualize it. We could say zero is red, one is blue, and two is green. Understanding this concept is important because when you reference data in your arrays, you might get the wrong data. If you're trying to get the third element, for example, and you ask for the index three item, you'll actually get an error saying the index is out of range or the offset is out of range. The other type of array is an associative array. This is an actual key-value pair. It allows you to turn your numbered index into a string, so you can define the index as a string and then reference the element that way. This makes keeping track of complex data a lot easier, and it works especially well if you want to store more than just a list of items. If you want to associate two pieces of data, you can do that with an array. Here's an example of names of people and their hometowns. You can see that the numbered indexes are replaced with names. So Joe is from Middletown, New York, Erin is from West Chester, Pennsylvania, Dave is from Exton, PA,
>
> **[3:07](https://www.linkedin.com/learning/learning-php-4/what-are-arrays-and-how-do-they-work?u=76281980&t=187)** and Brian is from Grand Rapids, Michigan. So now we can essentially ask the array, what is Erin's hometown? Now, one more note before we get out of the abstract and start coding these arrays. As mentioned earlier, array elements can take on any supported data type in PHP. That includes arrays. An array of arrays is called a multidimensional array, and these are especially common when working with [[Databases]]. You can have an entire table of data returned as an array where each row in the table is also in array, but we're getting ahead of ourselves. Let's actually look at how to define arrays in PHP.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (3), [[Representational State Transfer (REST)|Rest]] (1), [[Databases]] (1)
> **Definitions:** is an  (3), known as (1), is called (1)
> **CLI Commands:** php (3), make (1)
> **Env Vars:** php (3)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Defining arrays](https://www.linkedin.com/learning/learning-php-4/defining-arrays?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/defining-arrays?u=76281980&t=0)** - [Instructor] Now that you know what arrays are, let's look at how to use them in [[PHP]], starting with indexable arrays. I'll write my opening php tags. I'll add a comment here, Indexable Array. And let's create that colors array. So the array is going to have a name, just like any other variable with a dollar sign first. So I'll say colors get and then we're going to use the keyword array. And inside of the parentheses for the array function or keyword, we're going to have a comma-separated list of items. So we'll say red, blue, and green. So if we want to see what this array looks like, we can use a special function called print_r that will print the array nicely for us. So we'll say print_r colors. I'll save this and then I'll go to the browser. And you could see that the array is a simple array with our indexes as numbers, starting at zero. That means that if we want to reference a specific color, we would do so by writing echo colors, a square bracket and then the index. So if we wanted to print blue, we would say echo colors, square bracket one, close square bracket.
>
> **[1:33](https://www.linkedin.com/learning/learning-php-4/defining-arrays?u=76281980&t=93)** Now if we go back out, and refresh, you could see the [[Microsoft Word|word]] blue shows up here. And if we wanted to have it on its own line, we can add some [[HTML]], so I will add single quotes. I'll say echo, single quote, paragraph tag, close single quote, use our concatenation period and then after colors, I'll do the same thing. Period, single quote and p tag. And now, blue is on its own line. So that is an indexable array. Something that makes PHP arrays especially flexible when compared to other programming languages is the way you define and add elements. While stricter languages require you to define a size when you define the array, PHP does not. So we've defined our array up here with initial values but if we want to add a new value, we could simply say colors, square brackets with no index, gets and then whatever color we want to add. So let's say yellow. Now if we print our array, and refresh the page, we have a brand new element with the index three. So again, with stricter languages, you might have to say that you want an array of size four or you'd have to name the index here, which means that you keep track mentally of how many items are in your array.
>
> **[3:07](https://www.linkedin.com/learning/learning-php-4/defining-arrays?u=76281980&t=187)** PHP makes this a little bit easier. Now let's look at a slightly more complicated example with associative arrays. So let's use that home_towns example. I'll create a variable called home_towns. I'll make it an array but we want this to be an associative array. So whereas with the indexable array we only needed to find the values, with an associative array, we need to define the keys as well. So I'll say Joe and then the arrow operator. This is how we assign values in arrays. So we'll say Joe, arrow operator. Middletown, New York. Comma. Erin. And then West Chester. And notice, we also have to put a comma after every key-value pair here as well. Dave and Exton. And Brian from Grand Rapids. Now, I'm also going to add a comma after the last element here. This is perfectly valid syntax and it will make adding elements to the list, if we want to, easier because we won't have to remember to add this later and again, let's use print_r to visualize this.
>
> **[4:44](https://www.linkedin.com/learning/learning-php-4/defining-arrays?u=76281980&t=284)** All right, so this looks a little bit more complicated. Let's make this a little bit pretty. To do that, we will echo our pre tags before and after print. Great, that looks a lot nicer and you could see that the keys are the strings that we defined and the values are the values. So now, if we want to print out let's say Dave's hometown, we could say echo, we'll have our p tag, dot home_towns and then we would need to reference the string index and it's case sensitive too, so keep that in mind. We used a capital D for Dave. So we'll say home_towns square bracket Dave and then we'll close our p tag here. Now, if we refresh our page, it'll say Exton, PA. The last thing we'll take a look at is a multidimensional array. And to say some time, I've already printed this out for you. It is a multidimensional array where it has my brothers and me with various information about each of us. So we have our brothers variable gets array. And then the first key is my name and the value is another array with key-value pairs for age, job and state. If we want to show how this is visualized,
>
> **[6:21](https://www.linkedin.com/learning/learning-php-4/defining-arrays?u=76281980&t=381)** we can again, print out our pre tag here. So we'll say echo pre. Print_r brothers. And then echo and pre. Now let's go to that page. So you could see the visualization. It looks very similar to the definition here and it looks familiar to the other arrays that we saw. But if we try to do something like, for example, echo a key in the brothers array, so we'll say echo p brothers Rob. We'll save that. If we go back to our browser, you'll see that we get an error here that says notice array to string conversion. And just the word array. That's because we need to be more specific. Since Rob is a key that points to another array, we need to grab a key inside that second array to get their value. So if we want to get the state where Rob lives, we would need to add a second pair of square brackets here. So after the closing square bracket on Rob, we would open a square bracket at our quotes and then grab the key to the second array. So we'll say state, we'll close the square bracket, we'll save this. We'll refresh and you could see the letters Florida show up here
>
> **[7:54](https://www.linkedin.com/learning/learning-php-4/defining-arrays?u=76281980&t=474)** or the letters FL for Florida show up here. Arrays will be a big part of working with data in your code and it's good to get to know them as early as possible. Later, you'll learn how to easily traverse through an array using loops.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (5), [[Microsoft Word|Word]] (2), [[HTML]] (1)
> **CLI Commands:** php (5), make (3), find (1)
> **Definitions:** is a  (4), means that (2), is an  (1)
> **Env Vars:** php (4), html (1)
> **Code Identifiers:** home_towns (4)
> **Analogies:** just like (1), similar to (1), for example (1)
> **UI Navigation:** go to (2)
> **Cross-References:** go back to (1)

#### [Challenge: Display basic information on an HTML page](https://www.linkedin.com/learning/learning-php-4/challenge-display-basic-information-on-an-html-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/challenge-display-basic-information-on-an-html-page?u=76281980&t=0)** - [Instructor] All right, it's time to start writing your own code. Here you're given a set of variables, which you can find in the exercise files, and your challenge is to use them to print a story about a person who's going to a certain city to watch a movie with a friend where they select their favorite candy. You'll also notice that there are comments next to some of the variables. You should be sure to format the variable with that text.

> [!info]- Semantic Content
>
> **CLI Commands:** find (1)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Display basic information on an HTML page](https://www.linkedin.com/learning/learning-php-4/solution-display-basic-information-on-an-html-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/solution-display-basic-information-on-an-html-page?u=76281980&t=0)** - [Instructor] All right, so with our starting point here, let's look at a solution to this challenge. Now, keep in mind that your solution will probably be different cause I've asked you to write a narrative. But the way that you do certain things will likely be the same. So, let's take a look. So, first of all I have opted to print out all of the [[HTML]] with [[PHP]]. Another way we could have done it was to end our PHP tags, and then write the HTML, echoing the variables as needed. Like we're doing here. Either one of those solutions would be sufficient. So, we'll comment this one out so you could see both in the exercise files that are available with this lesson. You'll also notice that in the echo statement, I included two line breaks, you can see that I did that after each paragraph. And when we look at the browser version of this you'll see why. We've included the variables using double quotes in our echo statements, we've added some HTML as needed. Part of the reason that we use double quotes is so that we can add the variables easily but also because it was more likely that we were going to use some single quotes in this narrative.
>
> **[1:35](https://www.linkedin.com/learning/learning-php-4/solution-display-basic-information-on-an-html-page?u=76281980&t=95)** Something else worth pointing out is I included the price of the movie ticket so you can see how the dollar sign escape character works. And the last paragraph here does use double quotes. So, this is actually an echo statement that doesn't have any variables in it. So, I could have used single quotes here and then I wouldn't have had to use the escape characters. But this is one of Many solutions to the challenge. If we go look at this in the browser, you could see that, well, I am missing some text here, I forgot to add hello to the heading. So, save that, there we go. And this is printed out exactly as you would expect. If we look at the source, we'll see spaces between the paragraphs. So, I included the line break escape characters here, to make our source look a little bit more readable. If we hadn't included those line breaks, the source would be pretty much all on one line, it would still look like this in the browser, but the source would not be as readable. So, that's something to keep in mind as you write more and more complex websites, is that you'll want to format your source to be readable through the browser as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[HTML]] (3), [[PHP]] (2)
> **Env Vars:** html (3), php (2)
> **CLI Commands:** php (2), make (1)
> **Warnings:** keep in mind (2)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 2. Control Structures

[↑ Back to Table of Contents](#table-of-contents)

#### [Boolean operators and the truth (update for PHP 8.0)](https://www.linkedin.com/learning/learning-php-4/boolean-operators-and-the-truth-update-for-php-8-0-11628174?u=76281980)

#### [Logical operators](https://www.linkedin.com/learning/learning-php-4/logical-operators?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/logical-operators?u=76281980&t=0)** - [Instructor] Now that you know how to compare numbers, there's another type of Boolean operator you should know about. Logical operators. Logical operators let us combine any number of Boolean statements to check the overall truthfulness of that statement. No matter how many statements you combine, you'll still result in one Boolean value, true or false. The first of these operators is the AND operator represented by two ampersands. And this proceeds as true as long as all parts of the statement are true. So for example, true AND true would evaluate to true. It would be like saying, "The sky is blue "and the sun is yellow." However, as soon as any of those statements evaluates to false, the entire statement evaluates to false. Again, that would be like saying, "The sky is blue and the grass is pink." Only half of that statement is true. So the entire statement evaluates to false. Then there's the OR operator which is represented by two pipe symbols. An OR statement evaluates to true as long as any part of the statement evaluates to true. So if both sides of the statement are true, the sun is hot or the snow is cold, That is true because at least some part of the statement is true. However, if both parts of the statement are false, that is when an OR statement evaluates to false.
>
> **[1:36](https://www.linkedin.com/learning/learning-php-4/logical-operators?u=76281980&t=96)** The sun is cold or snow is made of candy. Neither of those statements are true. So the entire statement is false. It's also worth noting that AND and OR are commutative. So true or false evaluates the same as false or true. You are looking at the overall statement for truthfulness. So let's look at a couple of examples in code. Alright, my opening [[PHP]] statement. And then I'll create a couple of variables. We'll say A gets nine is less than five. B gets 10 is not equal to 10. C gets A and B. D gets A or B. E gets B and C. And F gets A or D.
>
> **[2:45](https://www.linkedin.com/learning/learning-php-4/logical-operators?u=76281980&t=165)** So now let's go ahead and evaluate all these. And just to make it easier, let's put them in an array. We'll call it logic. And if you'd like to pause the video here and try to evaluate them yourself before you see the answers, go ahead. So now we will print out this entire array using the print_r statement, and we'll wrap it in a pre block to make it just a little bit easier. Save that and then refresh our page in the browser. And you could see actually, this is an interesting quirk in PHP that when representing Boolean values, unless we explicitly tell PHP to, it's going to represent it as an integer where true is one and false is zero or null. So what we actually want to do here is use a different function called var_dump which has a little bit more information than print_r. And here it's a lot clearer. That we see an array and A is true. B is false. C is false because B is false. So that makes the entire AND statement false. D is true because A is true.
>
> **[4:19](https://www.linkedin.com/learning/learning-php-4/logical-operators?u=76281980&t=259)** E is false because C ends up evaluating the false. And F is true because A is still true. Now, it's worth noting that you can also use the words and and or for these operations. So you could say something like C gets A and B. But something interesting happens here. We'll do var_dump on C so that we could see the entire statement. And then we'll refresh. So C is evaluating to true here even though C evaluates to false when we use the double ampersand. That's because the double ampersand takes precedence over the assignment operator, but the [[Microsoft Word|word]] and does not. So what's happening here is C is getting assigned to A, and this statement, C gets A and B is being evaluated. So the entire statement evaluates to true. To err on the side of caution in this course, we'll always use the double ampersand and the double pipe symbol. Finally, there's one more operator you should know about. And that's the caret symbol which is XOR. XOR will evaluate to true as long as one statement but not all statements are true. So for example, true XOR false is true.
>
> **[5:58](https://www.linkedin.com/learning/learning-php-4/logical-operators?u=76281980&t=358)** False XOR true is true. True XOR true XOR false is true. But true XOR true is false and true XOR true XOR true is false. Again, if all statements are true, then XOR will evaluate to false. So again, logical operators will always evaluate to true or false. And now that you understand Boolean operators, it's time to start putting them to use.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (3), [[Microsoft Word|Word]] (1)
> **Env Vars:** xor (10), php (3)
> **CLI Commands:** php (3), make (2)
> **Code Identifiers:** var_dump (2)
> **Analogies:** for example (2)
> **Definitions:** is an  (1)
> **Warnings:** caution (1)
> **Speakers:** - [instructor] (1)

#### [Creating if/else statements](https://www.linkedin.com/learning/learning-php-4/creating-if-else-statements?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/creating-if-else-statements?u=76281980&t=0)** - [Instructor] Boolean statements can be used to help us decide what actions we want to take in our program. We can show a user a message based on whether they're logged in or not. We can perform a specific arithmetic operator based on a variable's value, and more. To make those decisions, we need control structures. Code that executes statements based on Boolean values. The most common of these control structures is If statements. An If statement will check for a condition to be true. And if it's true, it will execute a specific set of code. Here's a simple example. If A is greater than B, echo A is greater than B. This is a complete If statement. We start with the keyword, If, we have parentheses and inside those parentheses we have a Boolean statement, in this case, a comparison operator. Then we have an opening curly brace, the code we want to execute if that operator is true, and then the closing curly brace. It says if this condition is true, execute the following code. However, we can add more to that. We could do something else if the condition evaluates to false. So, here's a slightly extended If statement. We have if A is greater than B, echo A is greater than B, else echo A is not greater than B. So, after our basic If statement, we have the keyword else and then another set of curly braces.
>
> **[1:35](https://www.linkedin.com/learning/learning-php-4/creating-if-else-statements?u=76281980&t=95)** Inside those curly braces we have the code that we want executed if the Boolean statement evaluates to false. In between the if and the else, we can check for any number of other conditions. So, you see here we have an even more complex If statement. If A is greater than B, echo A is greater than B, else if B is greater than A, echo B is greater than A, else echo A and B appear to be equal. So in between our if statement and our else statement, we have what is an else if statement. We have the words else if, another Boolean statement, and then another set of curly braces in between which the code should execute if that second Boolean statement evaluates to true. When you structure If statements like this, there are a few things to keep in mind. A single "if" will always go first. The first condition to evaluate as true will be the only one that runs and the [[Representational State Transfer (REST)|rest]] will be ignored. And the else statement always goes last. It serves as a catch all if none of the conditions are true. So, before we write our own If statement, let's take a look at a complex If statement we have here. If 10 is less than one, we'll run some code, else if 10 is less than four, else if 10 is less than 11,
>
> **[3:08](https://www.linkedin.com/learning/learning-php-4/creating-if-else-statements?u=76281980&t=188)** else if 10 is less than 20, else. So just by glancing at this, you know that 10 is not less than one, and 10 is not less than four, but 10 is less than 11 and 20. So, we have at least two statements that will evaluate to true here. If we run this code in our browser, let's see what happens. It says we stopped at the second else if. So we stopped at the second else if is here on line seven. So if we go up to line six, we'll see this was the first condition that evaluated to true. Even though 10 is also less than 20, our If statement kicks out and would pick up right here on line 14. So if I write echo out of the if. So if we refresh our page, we have we stopped at the second else if, and then out of the if, which is on line 14. But If statements don't just need to use numbers either. Let's say we want to create a situation where we show the user a message based on some status. Let's create a variable called is logged in. And we'll set that to true first. So let's go ahead and write an If statement. We'll say if is logged in. This is all we need to include because is logged in is a Boolean. So we'll have a true statement here. Echo h3, Welcome back user, end h3,
>
> **[4:48](https://www.linkedin.com/learning/learning-php-4/creating-if-else-statements?u=76281980&t=288)** else echo, Welcome to the site. Alright, and if we save this and then refresh our page We'll have welcome back user. And if we set is logged in to false, the message will change to welcome to the site. You can write If statement in another format as well and this is called alternative syntax. You can see here that we have "if" the parentheses and the condition to check, but then we have a colon instead of a curly brace. And then when we have our elseif, it's worth pointing out here that elseif is one [[Microsoft Word|word]]. There's no space. A different condition followed by colon, else followed by a colon and then the words elseif. This alternative syntax is great, if you are printing out a lot of [[HTML]], where you have to exit and enter the [[PHP]] tags a lot. So in this example, we have if home_page, we'll say we have a variable called home_page, colon and then we close our PHP tags. Then we print a big block of HTML. Then we open our PHP tags again, and have the endif. The alternative syntax makes reading longer form If statements, especially if there's a lot of HTML easier to read, because you no longer have to trace curly braces.
>
> **[6:23](https://www.linkedin.com/learning/learning-php-4/creating-if-else-statements?u=76281980&t=383)** You can look for the actual word endif. And one more note about conditions in general, when checking the equality of a variable, I recommend using what's called Yoda conditions. You place the variable on the right side of the condition. This will prevent things like accidental assignments and bugs in your code. So, a good way to write this is if 10 equals I, then do something. And the bad way to write it is if I equals 10. These are called Yoda conditions because they read a bit backwards like Yoda from "Star Wars" would speak. If 10 this is then do something. And just to drive the point home here, let's say we'll just use our, is logged in, variable here. So we'll say I get 10. So if we're using Yoda conditions, and we accidentally forget the second equal sign, and I refresh, we have an error. However, if we're not using Yoda conditions, and I just say if I get 11. This is going to evaluate to true even though we intended for I to be 10. Because every assignment technically evaluates to true because it takes successful assignment. So again, if we were to be using Yoda conditions here, this would prevent a parser and a logic error.
>
> **[8:00](https://www.linkedin.com/learning/learning-php-4/creating-if-else-statements?u=76281980&t=480)** Both of which you'll learn about later. But this makes for safer, more error free code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[HTML]] (3), [[PHP]] (3), [[Microsoft Word|Word]] (2), [[Representational State Transfer (REST)|Rest]] (1)
> **Env Vars:** html (3), php (3)
> **CLI Commands:** php (3), make (1)
> **Definitions:** is a  (2), is an  (1), is called (1)
> **Code Identifiers:** home_page (2)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [Ternary operations](https://www.linkedin.com/learning/learning-php-4/ternary-operations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/ternary-operations?u=76281980&t=0)** - [Instructor] One common example of using if statements is to make variable assignments based on some condition. Here's what that might look like using our is_logged_in example. We have a variable is_logged_in gets true and then if is_logged_in, we can assign a message to the message variable, message gets Welcome back! Else message gets Hello There! But there's a much more succinct way of doing this with something called ternary operators, which is a shorthand conditional assignment. Let's look at that and then break down the syntax. So we'll write message gets is_logged_in ? Welcome back!
>
> **[0:49](https://www.linkedin.com/learning/learning-php-4/ternary-operations?u=76281980&t=49)** Colon Hello There. Let's expand this a little bit so you can see the whole thing on one line. And then if we echo message, and refresh, we have Welcome back! So this single line of code replaces these lines of code. And what we're saying here is message equals or message gets, then we have a condition. So this is replacing the if statement. Is_logged_in ? serves as the character that states if the condition is true, assign this value to our variable. The colon serves as the else statement in this operator. So if the statement is false, then assign Hello There! And we'll test this out by setting is_logged_in to false. And just to make sure we're super sure, we'll delete this if statement now. Great. But this sort of assignment isn't just for the front of a variable. We can actually do some pretty cool things with this. So if we always want to say welcome to our user, we can rewrite the statement as such. Message gets Welcome and then we can have the concatenation operator,
>
> **[2:24](https://www.linkedin.com/learning/learning-php-4/ternary-operations?u=76281980&t=144)** and then we can have our assignment. So we'll put this in parentheses, so it's easier to read. And we'll say is_logged_in, space back colon and if there are not logged in, we'll just say exclamation point. Now we'll echo this statement again. And refresh. And we have Welcome. If we set is_logged_in to true and save and refresh, we have Welcome back! The same thing is true of echo statements in general. We can actually remove the entire assignment here and just add this to an echo statement. So we could say echo Welcome, concatenation operator. And then have our condition. We'll save this and refresh and we have the same results. A place you'll commonly see ternary operators is when you want to set a variable based on dynamic content. You'll learn more about this technique later but for now, here's what that could look like. So let's say that we want to grab our logged in user's name and if they are logged in, there is a variable called name. We could say name gets isset, which is a function that basically checks to see if a variable exists. Name. Question mark name: Joe, and we'll just assume the user's name is Joe
>
> **[3:59](https://www.linkedin.com/learning/learning-php-4/ternary-operations?u=76281980&t=239)** if we don't know who they are. So what this is saying is name gets, if name is already set, use that value and if it's not, use Joe. So we'll echo name. And you could see Joe prints here. I'll comment out the first echo statement here. So we have Joe. Then if we set name at the top to be Alice, then Alice will print out. But there's an even shorter hand way of writing that. And that is using what's called the Elvis ternary operator. So this is really nifty. What we could say instead is we'll delete this entire statement and we'll say name gets name ?: Joe. So this shorthand tells [[PHP]] if name is null, then use this other backup value. And it's called the Elvis operator because it kind of looks like Elvis. So again, we'll save this, we'll refresh the page. Since name is assigned Alice, but if we get rid of this, and we'll set this to null so we don't have an undefined error here, we refresh the page and it says Joe because name is null. Now, I set this to null because if we delete the entire line, we'll get a notice that there is no variable called name and that's not good enough to crash the program
>
> **[5:34](https://www.linkedin.com/learning/learning-php-4/ternary-operations?u=76281980&t=334)** because we still have the output here. But just to be safe, we'll make sure that we do have a variable called name to prevent this notice from showing up. So with that, you have an understanding of how to use if statements and shorthand if statements but there is one more conditional control structure that you should know about.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (1)
> **Code Identifiers:** is_logged_in (7)
> **CLI Commands:** make (3), php (1)
> **Definitions:** is a  (3)
> **Env Vars:** php (1)
> **Speakers:** - [instructor] (1)

#### [Switch statements](https://www.linkedin.com/learning/learning-php-4/switch-statements?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/switch-statements?u=76281980&t=0)** - [Instructor] An alternative way of decision making is switch statements. Where if statements allow you to check to see if a condition is true, switch statements allow you to more clearly make decisions based on the value of a single variable. Here's a look at the syntax. Let's say we have a variable called total that has the value 10. We start with the keyword switch, followed by parentheses, and then the variable we're looking to evaluate, in this case, total. We have our curly braces and then we have a case. The keyword case is basically saying, does our variable in question equal the following value? So we have switch total case one is saying is total one. If it is, we have a colon and then the code we want to execute. In this case, we're printing out that total is one. And then we have a break statement. More on that later. But we can continue to have as many case statements as we want. So in this case we have case one and case two, and then we have the keyword, default. Default is our catchall here, so if neither case is true, then we print out, total is more than two. A few notes on switch statements. Unlike if's, they don't terminate upon finding a true statement, and because of that, break statements are absolutely crucial. A break statement will exit out of
>
> **[1:36](https://www.linkedin.com/learning/learning-php-4/switch-statements?u=76281980&t=96)** the switch statement for us. Default keyword serves as a catchall, and you can group cases together. So let's look at a few examples here. So let's write out a simple example using what we just saw. Total gets 10. Switch, total, then we have our curly braces. Case one, echo, put it in paragraphs. Total is one. Then we'll add our break statement. And then we'll copy this and paste it a few times for different values. And we'll go up to three in this example. So case one, case two, total is two, and case three total is three. Then we'll add our default. Echo, total is more than three. Now we don't need a break statement for default because that's going to be the last statement before we exit out of the switch completely. So we'll save that, we'll refresh. So we have total is more than three. Now, just for argument's sake here, let's go ahead and change this to one and then get rid of the break statement here, and we'll see if anything happens. We'll save this, refresh. So total is one
>
> **[3:11](https://www.linkedin.com/learning/learning-php-4/switch-statements?u=76281980&t=191)** and then it prints total is two, and then we have the break statement, so the [[Representational State Transfer (REST)|rest]] don't print, but if we don't have any of these, because one of the cases evaluated to true, we're going to have all of the values or all of the code execute. So again, these break statements are crucial. But unlike if statements, we don't necessarily just need to check for the truthfulness of a statement, right? So we can actually check strings as well. Let's look at another switch statement where we want to assign a variable based on some other variable. So I'm a big fan of the, "Ninja Turtles," so I'll say turtle, Leo, and then each Ninja Turtle has their own color of bandana. So we'll set this to nothing for now, and then we'll write our switch statement. So switch turtle, and then we can do the same checks, but now we can check for strings. Case, Leo. Bandana gets blue. Break. And then we'll repeat that process for the rest of the turtles. Case, Raph. Bandana, red. Break. Case, Mike. Bandana, orange. Break. And case, Don.
>
> **[4:46](https://www.linkedin.com/learning/learning-php-4/switch-statements?u=76281980&t=286)** Bandana, purple. Break. And then we don't necessarily need a default here, assuming that turtle can be one of these four values, so we'll just save that. And then if we want to echo bandana here, do it in the paragraph. It's going to be blue. Now, a couple more points about switch statements here is that you can also group cases together. So let's say we want to simplify our first switch statement here to say total is less than five. What we could do is just get rid of the echo and break statements for cases one through three, and then we'll add case four here. And then we can say total is five or more. So again, we'll save this, we'll refresh. Total is less than five, or if we change total to 10, total is five or more. So we've combined case statements here, which is nice because now we don't have to worry about creating in better examples or some complex grouping of conditional statements.
>
> **[6:20](https://www.linkedin.com/learning/learning-php-4/switch-statements?u=76281980&t=380)** And the last thing to mention here is that switch statements also have alternative syntax. So we can replace the curly braces with a colon and an end switch, and we have the alternative syntax, which will work just like the curly brace syntax. So when deciding which to use versus if or switch statements, consider if you're going to be checking for one of several values for a single variable, and not just the state of your program, like if the user is logged in, or if the user is on a specific page. If you find yourself writing if statements that's like, if total is one, do this, if total is two, do this, a switch statement is better suited for that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Representational State Transfer (REST)|Rest]] (2)
> **CLI Commands:** make (1), find (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Match expression](https://www.linkedin.com/learning/learning-php-4/match-expression?u=76281980)

#### [Challenge: Display content based on status](https://www.linkedin.com/learning/learning-php-4/challenge-display-content-based-on-status?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/challenge-display-content-based-on-status?u=76281980&t=0)** (bright music)
>
> **[0:05](https://www.linkedin.com/learning/learning-php-4/challenge-display-content-based-on-status?u=76281980&t=5)** - [Instructor] It's time for another challenge. Imagine you're presented with a randomly generated number between one and 50, and you have a guess. Create a way to give feedback to users such that if they guess correctly, you tell them. If they guess incorrectly, you tell them if they were too high or too low. And as a bonus, also check to see if the guessed number is out of range, that is less than one or more than 50. In the exercise files, you'll be given this starter that has a few variables for you. Min, max, guess, and num. You can change guess and num to whatever you'd like, or if you really want to go nuts, you can use the commented outline which assigns a random value to num within the range between min and max.

> [!info]- Semantic Content
>
> **Exercise Files:** exercise files (1)
> **Analogies:** imagine (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright music) (1)

#### [Solution: Display content based on status](https://www.linkedin.com/learning/learning-php-4/solution-display-content-based-on-status?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/solution-display-content-based-on-status?u=76281980&t=0)** - [Instructor] All right. Time for the solution. So, just to recap, you were to take the starter file, make a number guessing game out of it where if the guess was correct, you would let the user know. If the guess incorrect, you would tell them if it was too high or too low. And as a bonus, you would also check to see if the guessed number was out of range. And you were provided with two ways to assign num to a value. Just a static way, which was manually assign one, or use the random function built into [[PHP]]. Now, I included both so that you could easily test your code with a number where you knew the guessed value so you could create test cases based on that, and then when you were confident, you could uncomment this line and use the random value. So, let's get started using the static number 16. So, first, we want to see if the guess is out of range, so we'll say if guess is less than min or guess is greater than max then we can say echo our paragraph. Your guess is out of range. Close paragraph. And then there are one of two things that we can do here to move on to the next section. If the guess is out of range,
>
> **[1:33](https://www.linkedin.com/learning/learning-php-4/solution-display-content-based-on-status?u=76281980&t=93)** obviously, we don't want to continue because they are definitely going to be wrong. So, there's actually a built in function in PHP called exit that will stop the program from running at exactly that spot. Anything that comes after the exit statement will not happen. But we can also do something that is used fairly often, and that is a nested if statement. So, inside of the else condition we could say if, and then continue checking the guess. If num is equal to guess we can echo, and we'll make this a heading. You know? Make it real fun. That's correct. Then we can say else if num is greater than guess,
>
> **[2:27](https://www.linkedin.com/learning/learning-php-4/solution-display-content-based-on-status?u=76281980&t=147)** in that case it means that the guess is too low, right? 'Cause guess is less than num, so we'll say echo, "Sorry, that guess is too low." And then if it's neither equal to nor less than the actual number we can presume that guess is too high, so we could say echo, "Sorry, that guess is too high." Okay, so with the program written the way it is, it looks like we will get, "Sorry, that guess is too low." Let's go back to our browser and refresh. Sorry, that guess is too low. Now, let's do a little split screen so we can get some quick edits here. First, we want to see if our guess is out of range function works, so we can just simply say guess is zero. Okay, guess is out of range, or we could say guess is 100. Your guess is out of range. Perfect. So, we just saw guess is too low, so now let's see guess is too high. So, guess is 17. That will do it. Sorry, that guess is too high. And finally, we'll see if they're equal. That's correct. Perfect. So, now what we can do is uncomment this and now we'll actually see what happens, and just to add an extra level of testing here
>
> **[4:01](https://www.linkedin.com/learning/learning-php-4/solution-display-content-based-on-status?u=76281980&t=241)** we could do print_r, and then num just to see what the actual value was because it's being randomly generated. So, we'll refresh. Sorry, that guess is too low. Our guess was 16 and num was 25. So, that is it. That's the solution to this problem. Again, you also could've done exit here. Let's see what happens when we do that and then we'll get rid of the nested if statement, and we'll just have a second if statement here. So, let's make our guess out of range. Great, so without the exit statement we would've gotten, "Your guess is too high," right? So, again, if I uncomment this, "Your guess is out of range. Your guess is too high." But when I add the exit statement in we never get to the second if statement or the number being printed out.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (2)
> **CLI Commands:** make (4), php (2)
> **Env Vars:** php (2)
> **Definitions:** is a  (1), means that (1)
> **Cross-References:** go back to (1)
> **Exercise Files:** starter file (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 3. Math in PHP

[↑ Back to Table of Contents](#table-of-contents)

#### [Arithmetic operators](https://www.linkedin.com/learning/learning-php-4/arithmetic-operators?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/arithmetic-operators?u=76281980&t=0)** - [Instructor] There's no way around it, if you write programs, you will need to do some math. It could be as simple as incrementing and detrimenting or you can do some pretty advanced calculations. Heck, some gaming engines are made partially with [[PHP]]. So to start, you can perform the four traditional arithmetic operations. Addition with the plus sign, subtraction with the minus sign, multiplication with the asterix and division with a forward slash. You can perform these operations in variable assignments or directly output them using echo. So we could say, echo one plus one or numb gets one plus one and then echo numb, and in both instances, the result is two. PHP is smart enough to recognize that we are using literal numbers here and not strings. So, it evaluates the statement before the results are outputted. And here are some examples of other operations, we could say A gets two and then echo A plus two, which will output four. We could say B gets three, and then we can say, echo B minus A which will output one. We could say C gets A times B and then echo C, which will output six. And then we could say echo C divided by A, which will output three. You can also negate a value by pretending the minus sign. So like, negative three, so we can say numb gets 20
>
> **[1:36](https://www.linkedin.com/learning/learning-php-4/arithmetic-operators?u=76281980&t=96)** and then echo minus sign numb and the output would be negative 20. You can even do complex mathematical operations using parentheses or exponents. The rules of PEMDAS will usually apply, and as a refresher, PEMDAS stands for parentheses, exponents, multiplication, and division, and then addition and subtraction. So, let's go out to our code editor now and look at a couple of examples. Though parentheses are technically optional, they are very important to use them in your code to make operations explicit. Let's look at an example where parentheses matter, so we'll open our PHP tag, and then we'll say echo five times six, plus three minus one, so we'll save this and then we'll refresh our page and the answer is 32. Now, if we were to say, echo this statement again, but put parentheses around six plus three, refresh, and they're bumping up against each other so let's add a line break here, and the answer is 44. So again, the parentheses are going to matter, similarly, if we put parentheses around five times six, we should still get 32 there, right?
>
> **[3:09](https://www.linkedin.com/learning/learning-php-4/arithmetic-operators?u=76281980&t=189)** Because the operation is being done in order. And just for one more example, if we do three plus five times six,
>
> **[3:30](https://www.linkedin.com/learning/learning-php-4/arithmetic-operators?u=76281980&t=210)** we'll get 32. So, even in this example, the rules of PEMDAS are applying. And if you want to use exponent, you would do that with two asterisks, so, if we say echo five, asterix, asterix two, that's going to give us five squared, which is going to be 25, I'll get rid of these operators up here, and that's 25. Now, let's look at one more complex equation where we have an exponent included. So, we'll do five asterix, asterix two, times, we'll put the parentheses around six plus three so hopefully that will get done first because that's parentheses, we'll save and we get to 224. If we remove the parentheses, we should get a different answer, 152. So, these are the basic mathematical operators in PHP, but there are a few others that you should be aware of.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (4)
> **Env Vars:** php (4), pemdas (3)
> **CLI Commands:** php (4), make (1)
> **Definitions:** stands for (1)
> **Warnings:** be aware (1)
> **Speakers:** - [instructor] (1)

#### [Figuring out even/odd with modulus](https://www.linkedin.com/learning/learning-php-4/figuring-out-even-odd-with-modulus?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/figuring-out-even-odd-with-modulus?u=76281980&t=0)** - [Instructor] There's one more arithmetic operation that's a little different from the [[Representational State Transfer (REST)|rest]] because it doesn't give the actual results, it gives the remainder of dividing two numbers and it's called modulus or modulo. It's represented by a present sign and it gives you the remainder of dividing two numbers. So if we have a gets two and b gets three and we echo b modulus a, that will print out one because when we divide three by two, we have a remainder of one. Similarly, if we were to echo 15 modulus nine, the remainder would be six. This is often used to find out if some number is odd or even. So if we write some code here where a gets five if a modulo two equals zero, then echo a is even else echo a is odd. Close my paragraph tag up here. If you divide any even number by two, the remainder will be zero. Now this was used for a long time programmatically to apply CSS classes to even table cells or even rows, but now there is a CSS selector to do that
>
> **[1:36](https://www.linkedin.com/learning/learning-php-4/figuring-out-even-odd-with-modulus?u=76281980&t=96)** and that would be the recommended way is to use the CSS selector, but there still could be some utility in this if you're trying to figure out the evenness or oddness of a number. You can also see if one number is the factor of another number. So let's just reassign our variables here. I'll say a is 15 and b is five. So if we say echo and then we'll use a ternary operator here. a modulo b equals zero add our dollar sign here so we don't get an error. We could say b is a factor of a, otherwise b is not a factor of a. Okay, so if we save this. Check all of our syntax here, we missed a semi-colon here. So everything's looking good. We'll refresh. Five is odd and five as a factor of 15. So those are both accurate statements. Finally, you can get the negative remainder by negating the dividend. That is the first number in the expression. So if I say echo negative a modulo and I can't say b here because I know that b is a factor of a, so we'll get zero. I'll just say two.
>
> **[3:08](https://www.linkedin.com/learning/learning-php-4/figuring-out-even-odd-with-modulus?u=76281980&t=188)** So we should get a negative one as the answer here and there we go. So modulus will return the remainder after we do a division and it's often used to figure out if a number is odd or even or if the number is a factor of a another number.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Representational State Transfer (REST)|Rest]] (1)
> **Definitions:** is a  (4)
> **Env Vars:** css (3)
> **CLI Commands:** find (1)
> **Best Practices:** recommended (1)
> **Speakers:** - [instructor] (1)

#### [Incrementing and decrementing variables](https://www.linkedin.com/learning/learning-php-4/incrementing-and-decrementing-variables?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/incrementing-and-decrementing-variables?u=76281980&t=0)** - [Instructor] Arithmetic operators allow you to enumerate as needed. Perhaps you want to count the number rows in the table or create a numbered list. To increment based on what you know at this point, you would use the first line of code here. A get a plus one. This takes the variable a, adds one to it and reassigns the new value back to a, and the same goes for decrementing. You could say b gets b minus one. But there's a slightly shorter way that you can do this using a ++ or -- symbol. So the second line you see on the screen here, a++, would increment a by one. Similarly, b-- would decrement b by one. And you don't have to use these in just assignments either. You can add them to echo statements as well. Just bear in mind a few things. If you use this shorthand anywhere, including echo statements, the variable will still change values. So you can't simply say echo a++ and assume a retains its previous value. As a matter of fact, depending on how you do it, you'll get different results. And there are pre and post-increment actions. So let's actually take a look at those. Here we have a block of code and on line eight, I have exit just to make sure that these lines just print just yet. So first, we have the post increment action. A is five and then we say the value of a is a++.
>
> **[1:39](https://www.linkedin.com/learning/learning-php-4/incrementing-and-decrementing-variables?u=76281980&t=99)** This is going to still be five because [[PHP]] is processing a and then processing the ++. And then a will be six. So let's run this code. The value of a is five, as we expected. Now a is six because again, at this point in the print statement on line four, a is still five and that's what PHP is printing. Then it's incrementing it. So the next time that we print a, which is on line six, a will be incremented. Now let's clear these results and we'll just move our pre-increment actions up. Okay, so now a is five. And this time, we're putting the ++ on the left side of a, which means that PHP is processing the ++ first and then printing a. So this will print six and then a will remain six even though we didn't do a formal assignment, we just used the increment operator. So again, we'll print this out. The value of a is six. And it remains six. The same thing goes for decrementing. So we can decrement here and a will be four and then remain four. And if we remove our exit statement to do the post decrement, the value of a is five
>
> **[3:13](https://www.linkedin.com/learning/learning-php-4/incrementing-and-decrementing-variables?u=76281980&t=193)** 'cause it got reset on line nine. And then it is four because the post decrement happened after the first print. But those aren't the only ways that you can use increment in decrement. As a matter of fact, since all characters have some numeric representation, you can increment letters or characters the same way that you increment numbers. So if I have A and then I say echo a is, concatenate ++a, I've got to do the pre-increment here and then I'll add the line break statement. So A should print out as B. There we go, A is B, which is pretty cool. And further, we can use the shorthand arithmetic operations for assignments for more than just adding and subtracting, as well as more than just a single increment. So if I say a gets one, and then I reassign a by saying plus gets five, a will now be six right here. If I say a- gets five, it will be -4. If I say asterisk gets five, which is a multiply gets and then I refresh, a is five. And then if I say a / gets five, which is a divided by assignment,
>
> **[4:52](https://www.linkedin.com/learning/learning-php-4/incrementing-and-decrementing-variables?u=76281980&t=292)** a is .2. So this shorthand is especially useful for incrementing and decrementing as you learn other control structures like loops. But the shorthand syntax is a great way to write succinct operations and assignments.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (3)
> **CLI Commands:** php (3), make (1)
> **Definitions:** is a  (3), means that (1)
> **Env Vars:** php (3)
> **Speakers:** - [instructor] (1)

#### [Challenge: Implementing a complicated calculation](https://www.linkedin.com/learning/learning-php-4/challenge-implementing-a-complicated-calculation?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/challenge-implementing-a-complicated-calculation?u=76281980&t=0)** (upbeat electronic music)
>
> **[0:05](https://www.linkedin.com/learning/learning-php-4/challenge-implementing-a-complicated-calculation?u=76281980&t=5)** - [Instructor] All right, it's time for another challenge. To make sure you grasp the basics of [[PHP]], in this challenge, you will perform a set of mathematical operations and store them in variables. Store each of the following equations or the results of each equation in an array called results. Then at the end, print out those results. There is a starter file in the Exercise Files where you'll have these equations available to you. Please note that in A, the last operation you're doing there is squaring the entire result, so that's not just a multiply by two. That is the squared exponent.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (1)
> **CLI Commands:** make (1), php (1)
> **Exercise Files:** starter file (1), exercise files (1)
> **Env Vars:** php (1)
> **Definitions:** is a  (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat electronic music) (1)

#### [Solution: Implementing a complicated calculation](https://www.linkedin.com/learning/learning-php-4/solution-implementing-a-complicated-calculation?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/solution-implementing-a-complicated-calculation?u=76281980&t=0)** (bright music)
>
> **[0:05](https://www.linkedin.com/learning/learning-php-4/solution-implementing-a-complicated-calculation?u=76281980&t=5)** - [Instructor] All right, let's walk through the solution for this challenge. First, we'll set up our results array, like so, then we'll evaluate the first statement. So I'll write results, square brackets and then we'll write the arithmetic operation. So first off, there are parentheses around most of it. So we'll do parens 14 plus 82 minus and then we have 32 divided by two. We want to make sure that this is evaluated first because of proper PEMDAS rules. So we'll wrap that part in parentheses and then make sure we have our parentheses properly spaced. After that, we are dividing, then we need to square the entire statement and we'll do that by adding our double asterisks and two. So there's our first results. We can do the same thing for the [[Representational State Transfer (REST)|rest]] of the results as far as assignments go. That's the beauty of using an array and so, now we have 18 times three divided by six minus nine, in parentheses, times 10. So let's do 18 asterisks, then we need to get our first parens down and again, we want to make sure that three divided by six is being evaluated first.
>
> **[1:39](https://www.linkedin.com/learning/learning-php-4/solution-implementing-a-complicated-calculation?u=76281980&t=99)** So we'll add another set of parens here and say three is divided by six and then we'll subtract nine and then we'll multiply 10 and our last statement, we'll do results with the square brackets. This is a pretty complicated one because there's a lot of stuff going on inside the parentheses. So first, we need to say five times at our paren and then we have 12 divided by two minus eight times four plus 12 times six. We want to make sure to get parentheses around all of the multiplication and division. So we'll add our first parentheses 12 divided by two and then minus parentheses eight times four close parentheses plus 12 times six in another set of parentheses and that appears to be our entire statement. So now let's go ahead and print out the results. We'll want the results to be on a nice neat line here, so I will echo pre-print our results
>
> **[3:00](https://www.linkedin.com/learning/learning-php-4/solution-implementing-a-complicated-calculation?u=76281980&t=180)** echo close pre and let's see how we did here. All right, 6,400, negative 1,530 and 230. So there you go. Those are the results for this challenge. You may have done things a different way with different parentheses. One of the important things to take away from this challenge is that depending on the version of [[PHP]] you're using, you may run into some problems and so making the parentheses as explicit as possible will always help with your operations.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Representational State Transfer (REST)|Rest]] (1), [[PHP]] (1)
> **CLI Commands:** make (4), php (1)
> **Env Vars:** pemdas (1), php (1)
> **Definitions:** is a  (1)
> **Best Practices:** make sure to (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright music) (1)


### 4. Loops

[↑ Back to Table of Contents](#table-of-contents)

#### [What are loops?](https://www.linkedin.com/learning/learning-php-4/what-are-loops?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/what-are-loops?u=76281980&t=0)** - [Instructor] Sometimes you want to repeat a task until it's done. Practically, this could be looking at all of the records in a file or continuously incrementing a number until you hit the limit you want. The control structures you've seen so far focus on making a decision and then executing code once, but there is a different control structure for if you want to repeat a task some number of times and that's called a loop. Loops will execute a piece of code until some condition becomes false. So you can imagine the structure is like this. While some condition is true, execute some lines of code. If some condition becomes false, exit the loop and move on. there are three different types of loops you'll learn about, but they all have two important components for you to keep in mind. They all must have a true/false condition, like the ones that we saw in if statements and they all must include a statement that moves the condition towards being false. For example, if you have a structure like this where i is one and the condition is i is less than 10, within the code your loop is executing, you should have the statement i plus plus because that increment i and eventually moves the statement i is less than 10 towards being false because i will eventually get to 10,
>
> **[1:35](https://www.linkedin.com/learning/learning-php-4/what-are-loops?u=76281980&t=95)** which evaluates to false. If you don't have a condition that moves towards false, you will end up with an infinite loop. These are loops that never terminate and they will crash your website or your application, but luckily, there are a few loops with safeguards to make sure that doesn't happen. So let's take a look at three types of loops in [[PHP]], while loops, for loops and foreach loops.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (1)
> **CLI Commands:** make (1), php (1)
> **Analogies:** imagine (1), for example (1)
> **Env Vars:** php (1)
> **Definitions:** is a  (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [Do/while loops](https://www.linkedin.com/learning/learning-php-4/do-while-loops?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/do-while-loops?u=76281980&t=0)** - [Instructor] The most common form of loop is a do while loop. These will continue to run until some condition becomes false. They are general purpose loops that can be used for anything. And there are actually two types of syntax for these loops. The first uses the keyword "do." So we'll add our [[PHP]] tags. Let's create some condition here. We'll say i is zero, and then we'll start our loop. So this could say, do echo, i, while i is less than 10. So this loop will execute and print out the value of i, as long as i is less than 10. So we should see the numbers zero through nine, printed out on the page. But we don't. And you could see that our program is still running here. That's because we forgot one crucial part of the loop, which is the incrementor. So if we add, i++ save and refresh. Now, we have a list of numbers from zero through nine, and then our loop terminates. Do while loops are great if you want some condition to run at least once. For example, maybe you want to add a loop to a number guessing game, where you evaluate a guess and while the guess is wrong, you can allow the user to keep guessing. In that instance,
>
> **[1:35](https://www.linkedin.com/learning/learning-php-4/do-while-loops?u=76281980&t=95)** you would want to evaluate the guess at least once. However, most of the time you will only want the code to execute if the condition is true. In that case, we can essentially turn our do while loop upside down and say while i is less than 10, do some code. So you can just copy this right here. And then this will have the same results as our do while loop. However, if we set i to 10, save this and then refresh,
>
> **[2:16](https://www.linkedin.com/learning/learning-php-4/do-while-loops?u=76281980&t=136)** you'll see that only the number 10 prints out. That's because even though the do while loop has a condition for i is less than 10, the code is going to execute at least once. Whereas with the while loop, this condition is never true. So those are the two versions of while loops. While loops are great for lots of things like checking to see if some state is true or false, and then executing code based on that like with our number guessing game. In the instance we have here where we're simply incrementing and printing out the value of i, there's actually a better suited kind of loop for this, and that's called a for-loop.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (1)
> **CLI Commands:** php (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [For loops](https://www.linkedin.com/learning/learning-php-4/for-loops?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/for-loops?u=76281980&t=0)** - [Instructor] For loops are made specifically for enumeration. The starting assignment, the condition, and the incrementer are all in the for loop definition. So you see here, we have the keyword for, and then in parentheses, we have a starting assignment followed by a semi colon, a condition followed by a semi colon, and then the incrementer. So inside the loop, all we need is the code we want to execute. For comparison sake, let's take a look at our while loop again. You can see we have the starting assignment outside the loop. We have the condition in the parentheses and we have the incrementer inside the while loop. The incrementer as you saw can be pretty easy to forget. Now let's write a for loop that does a similar task. We'll write for and then we have the assignment, I gets zero. We have our condition, I is less than 10, and then we have our incrementer. Now it's important to note here that even though the incrementer is included in the loop definition, it's not enforced by the [[PHP]] compiler. So you can leave it off and still have a valid for loop. It's also worth noting that we can include or exclude the semi-colon here, I'm going to exclude it. Okay, so now we have our for loop let's copy the same echo statement we have in our while loop. Now, what we will see here is a list of numbers from zero through nine,
>
> **[1:33](https://www.linkedin.com/learning/learning-php-4/for-loops?u=76281980&t=93)** but we'll only see it once because we started I at 10 up here so this while loop will never execute. We'll save this and refresh the page. And we have a single list of numbers zero through nine. Again, for loops are perfect for incrementing or for moving through a numbered list or doing some tasks like that because everything is managing the flow of the loop in a single line that serves for the loops definition. It's also worth mentioning that I'm calling the third expression in this loop, the incrementer, but it could easily be a decrementer. So we can have the reverse here. I gets 10, I is greater than zero, I minus minus. Now we should have a reverse version of this loop, starting from 10 and going to one. And again, just because the condition or the incrementer is included, doesn't mean that we are prevented from having infinite loops. For example, if I change this incrementer back to an actual incrementer, our loop will never become false here because I starts at 10 which is more than zero and never moves towards zero. So now we have an infinite loop continually printing the numbers from 10 up to infinity until our program eventually crashes or stops executing. So we'll change that back.
>
> **[3:09](https://www.linkedin.com/learning/learning-php-4/for-loops?u=76281980&t=189)** Now, let's take a look at one more example of a for loop where we are traversing through an array. So I will comment out this echo statement here so that we can focus on the next loop we write. Let's say that we have a colors array. So we'll say colors, gets array red, blue, green and yellow.
>
> **[3:36](https://www.linkedin.com/learning/learning-php-4/for-loops?u=76281980&t=216)** A for loop allows us to traverse this array or move through it by saying for. And then we'll have some condition, we'll set I to zero. I will serve as the index. And remember that array indexes start at zero, and then we'll keep going while I is less than. And we want I to be less than the size of the array. So the array has for elements in it, which makes it an array of size for, but we don't always know the size of the array. So we can use a nifty built-in function for our PHP called size of, and we can pass it, our colors array. This will return for. So this is saying I is less than the size of the colors array which is for. And I plus plus. Now inside the loop, we can print out each item of the array because we are keeping track of our index. So we could say echo P I'll use concatenation here to make the syntax of the array a lot clearer than if we were using quotes and P. And now we should get a list of each color in the array, red, blue, green, yellow. There are a few things to keep in mind as you write a loop like this. First of all, array index is start at zero. So if you start at one, you will eventually get an error and you'll skip the first element in the array. The size of function sends the number of elements
>
> **[5:11](https://www.linkedin.com/learning/learning-php-4/for-loops?u=76281980&t=311)** in the array and not the highest number index. So in this case size of is four, but the highest number index is three. If you don't do your math exactly right. you will get an error in your code and your program will crash. Now you will probably traverse through a lot of arrays. That's how a lot of data is sent back, especially when you start working with files or [[Databases]]. And luckily when it comes to arrays, you don't need to do most of this because PHP has a loop specifically for arrays. The for each loop.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (3), [[Databases]] (1)
> **CLI Commands:** php (3), make (1)
> **Env Vars:** php (3)
> **Cross-References:** as you saw (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [Working through arrays with foreach loops](https://www.linkedin.com/learning/learning-php-4/working-through-arrays-with-foreach-loops?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/working-through-arrays-with-foreach-loops?u=76281980&t=0)** - [Instructor] Foreach loops are a little different from the other loops you've seen. They're designed specifically for traversing through arrays. So we have two types of syntax here. We have foreach array as item and then we can do something with the item or we have foreach array as key arrow value for arrays with key value pairs. Let's take a look at some examples. So on the screen we have the for loop and we're going to convert this to a foreach loop. We have our colors array defined on line two. So we could say foreach colors as color and then echo paragraph color. So now we should get two lists of colors: red, blue, green, yellow, red, blue, green, yellow. What this foreach loop is essentially saying is take each item in the colors array and assign it to the variable color one at a time, then print that color. The loop will end when there are no more elements in the array. There is much less room for error here than with a for loop. We don't need to worry about indexes, array sizes, or anything like that. [[PHP]] takes care of it all for us. This also works with associative arrays. So let's use the hometown arrays we saw earlier. You can find this in the exercise files
>
> **[1:34](https://www.linkedin.com/learning/learning-php-4/working-through-arrays-with-foreach-loops?u=76281980&t=94)** for the arrays lesson. The way we would loop through this is by saying foreach home_towns as and then we would mean the key and the value. So in this case, we could say name and then town. Then we can have our echo statement. So we'll say echo name lives in town. We'll save that. And then refresh our page. Joe lives in Middletown. Aaron lives in Westchester. Dave lives in Exton. Brian lives in Grand Rapids. Foreach loops make this a lot more manageable. We don't need to figure out how to do this with a while loop or a for loop, especially when it comes to big complex arrays like the brothers array we saw earlier. This will come in handy, especially when you're working with files and [[Databases]].

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (1), [[Databases]] (1)
> **CLI Commands:** php (1), find (1), make (1)
> **Code Identifiers:** home_towns (1)
> **Env Vars:** php (1)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Calculating the Fibonacci sequence](https://www.linkedin.com/learning/learning-php-4/challenge-calculating-the-fibonacci-sequence?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/challenge-calculating-the-fibonacci-sequence?u=76281980&t=0)** (upbeat electronic music)
>
> **[0:05](https://www.linkedin.com/learning/learning-php-4/challenge-calculating-the-fibonacci-sequence?u=76281980&t=5)** - [Instructor] The Fibonacci sequence is a great way to learn how loops work. If you don't know, the Fibonacci sequence is a series of numbers in which the next number is calculated by adding the two previous numbers. So the first few entries in the Fibonacci sequence would be one, one, two, three, five, eight, and so on. In this challenge, you'll print the numbers of the Fibonacci sequence without going over 200. There's no starter file for this one. You'll just be starting with a blank [[PHP]] file.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (1)
> **Definitions:** is a  (2)
> **CLI Commands:** php (1)
> **Env Vars:** php (1)
> **Exercise Files:** starter file (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat electronic music) (1)

#### [Solution: Calculating the Fibonacci sequence](https://www.linkedin.com/learning/learning-php-4/solution-calculating-the-fibonacci-sequence?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/solution-calculating-the-fibonacci-sequence?u=76281980&t=0)** - [Instructor] All right, so here's the solution to the challenge of printing every number in the Fibonacci sequence without going over 200. And we put a limit on there because the Fibonacci sequence goes on infinitely, right? So there are a lot of solutions to this problem, this is a pretty common computer science problem and therefore, a lot of beginner programmers think about it. But we're going to do this in a very straight forward way. So we're going to use three variables here, current, which we'll start at one because that's where the Fibonacci sequence starts, previous, which is going to be zero because we're going to need to add some previous value to the first entry and next, which we'll set to null for now. Then we'll have a while loop. We'll say while current is less than 200 and we can always put the limit in a variable as well, so we could say limit gets 200 just in case we ever want to change this, or we need to reference it somewhere later. So while current is less than limit, let's do some things. First, we're going to echo the current value. So we'll say echo current, and then we want to have a comma separated list of these values so that's what we'll add after current. Then we need to do some math,
>
> **[1:35](https://www.linkedin.com/learning/learning-php-4/solution-calculating-the-fibonacci-sequence?u=76281980&t=95)** so we'll say next gets current plus previous add a semi-colon, then we'll say previous gets current and then current gets next. Now there might be a pithier way to do this without using the next variable, but to do it in the most straightforward manner, we're going to use these three because you could run into a few weird off by one bugs if you don't. So there we go, there's our solution, let's see how well it does. I'll save this and refresh in the browser. One, one, two, three, five, eight, 13, 21, 34, 55, 89, 144 and if we were to add 144 and 89, that would definitely go over 200. So we stopped in the right place. Now, the last thing that we might want to do, right, is there is now an extra comma here. So if we want to take it one step further, we could actually create a new variable called sequence and set that to nothing. And then we can replace our echo statement with a variable assignment. So we could say sequence. get you saw the arithmetic assignments in an earlier video, but we can also do that with strings. So we can use the concatenation operator
>
> **[3:08](https://www.linkedin.com/learning/learning-php-4/solution-calculating-the-fibonacci-sequence?u=76281980&t=188)** with the assignment operator to say append this to the end of the last string. So with that, what we can do is a few fancy things on sequence once we exit the loop. The first thing we can do is trim all white space so there is a built in function called trim in [[PHP]] that will remove white space from the beginning and end of a string. So that will take care of the last space. And then we have one character that we want to get rid of, and that is the ending comma. So, again, we can use another built in function for that, we could say sequence gets substr so that we'll send a substring back to us and that sub string is going to be based on the string we send it. An integer as a starting point, starting at zero and then the length of a string. So we are going to send sequence. The starting point is going to be zero strings are indexed, just like arrays and then the length. There's another built in function in PHP called strlen, which functions a lot like size of for arrays that is going to accept a string. So we'll send that sequence, but we want this to be the length of the string minus one. So with that, we'll end this statement
>
> **[4:42](https://www.linkedin.com/learning/learning-php-4/solution-calculating-the-fibonacci-sequence?u=76281980&t=282)** just to run through this again, this says sequence gets a substring of sequence starting at zero, and then going to the full length of the string minus one. Now, if we echo sequence, we have our Fibonacci sequence with no ending comma. Now we could eliminate line 19 and say minus two, that would give us the same results but now we have a fully functioning Fibonacci sequence, never going over 200 that is also syntactically correct.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (2)
> **Definitions:** is a  (3)
> **CLI Commands:** php (2)
> **Env Vars:** php (2)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 5. Functions and Objects

[↑ Back to Table of Contents](#table-of-contents)

#### [What are functions?](https://www.linkedin.com/learning/learning-php-4/what-are-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/what-are-functions?u=76281980&t=0)** - [Instructor] Functions are reusable snippets of code that can be called multiple times. You can imagine functions as variables for blocks of code. And the reason that we would use functions is when we want to reuse a snippet of code more than once, instead of rewriting those multiple lines of code every time, and then having to change them if we ever want to update those lines of code, we can put them in an easy to use function. Functions can do one of two things. They can return a variable or a value that can be used as or assigned to a variable, or they can print something out. Here's the function syntax for printing something out. We have the keyword function and then the name of our function, the name of the function should be formatted similarly to the name of our variables, where spaces are replaced by underscores. But notice there's no dollar sign in front of the function name. Instead, the function name has an opening and closing parentheses after it. Then we have our curly braces and in between the curly braces, we have the code that we want to execute as the function. In this case, our function is printing out Hello World, and you could see it in use below. So we have our paragraph tag and then our opening [[PHP]] tags. We have the function name, Hello World with the parentheses and a semi-colon,
>
> **[1:35](https://www.linkedin.com/learning/learning-php-4/what-are-functions?u=76281980&t=95)** our closing PHP tag, and then our closing paragraph tag. This will print out a paragraph with the text Hello World. A function can also return a value. So again, we have our keyword function, the name of the function, Hello World, with the parentheses, the curly braces. And the only difference is that instead of the [[Microsoft Word|word]] echo, we have the keyword return, which basically says treat whatever you are sending back from this function as a variable. So in this case, we are using the word echo. Then we have our paragraph tag in quotes, the concatenation operator, our function call, Hello World, a concatenation operator, and then our closing paragraph tag with the semi-colon. So in this case, the function name, Hello World is being treated as a variable with the value Hello World. If a function were to return a value, it could be treated as its own variable or assigned to a separate one. So let's take a look at a simple example where a function returns a Boolean. We have a function here called is bigger and inside it returns the Boolean operator 10 is greater than or equal to five, which will be true. Then below that we assign our variable bigger to whatever is returned from is bigger. So we're saying bigger gets is bigger, the function. And then we can use that variable and an if statement,
>
> **[3:11](https://www.linkedin.com/learning/learning-php-4/what-are-functions?u=76281980&t=191)** but we can also just call the function as the condition is the if statement and skip the variable assignment altogether. So we have, if is bigger than echo, one thing, otherwise echo another thing. So here the function is being treated as a variable. Finally, you might be wondering why we have parentheses for the function name. Well, we can pass variables or arguments to functions. So our is bigger function is pretty useless if it's only evaluating 10 is greater than or equal to five, we might want something more dynamic than that. So you could see the syntax here, function is bigger, and then inside those parentheses, we have two arguments, A and B. A and B will take on the value of whatever we pass to the function. So in our assignment, we have bigger gets is bigger and then 10 and five. So in this instance, 10 will be assigned to A and five will be assigned to B. And functions can do more than just return Boolean values. Anything that can be stored in a variable can be returned from a function. The last thing it's worth noting here is that functions need to be defined before they are called, just like variables. So if we were to swap this syntax and have bigger gets is bigger before the function declaration, PHP would throw an error because it doesn't yet know what is
>
> **[4:46](https://www.linkedin.com/learning/learning-php-4/what-are-functions?u=76281980&t=286)** bigger is now. This is how we rate our own functions, but PHP has a bunch of built in functions as well. You've seen a few of those already, like size of and strlen. These will help you accomplish tasks very efficiently, like formatting the date with the date, grabbing the size of an array and more. Let's look at PHPs' built in functions now.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (4), [[Microsoft Word|Word]] (2)
> **CLI Commands:** php (4)
> **Env Vars:** php (4)
> **Analogies:** imagine (1), just like (1)
> **Speakers:** - [instructor] (1)

#### [Using built-in functions](https://www.linkedin.com/learning/learning-php-4/using-built-in-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/using-built-in-functions?u=76281980&t=0)** - [[PHP]] has an extensive library of functions to help us complete certain actions more efficiently. You saw it with the size of function earlier, without that function, we'd have to write our own much more resource, heavy way to get the number of elements in an array. But how do you find these built in functions? A good starting place is the phpmanual@[php.net](https://php.net). They will have an extensive list of functions and how they work. They'll also have additional comments from users on some things to think about as you use them. However, there are a few caveats, the search isn't great. If you don't already know the functions name. So if I were to search array size here, I would get a list of functions with the words array, but I wouldn't get the size of function. This is also developers talking to developers. So you'll get sample code, but not necessarily extensive explanation. Another good tool for using PHP functions is an IDE with auto-complete like PhpStorm or VS Code. So for example, if I say string gets substr, and then I add the parentheses, you'll see that the function auto completed, but VS Code is also telling me the arguments I need to pass to the function and what is returned from the function.
>
> **[1:34](https://www.linkedin.com/learning/learning-php-4/using-built-in-functions?u=76281980&t=94)** But again, this mostly helps. If you already know what you're looking for. All of that being said, the PHP documentation does have a way for you to search through the documentation with the function reference. If we scroll down, we have variable and type related extensions, or we can do a page search for the [[Microsoft Word|word]] string, and then we can look at the results there. So under text processing, there's an entire section for strings. And then there are functions that affect the string. One that might come in handy later is STR to lower.
>
> **[2:14](https://www.linkedin.com/learning/learning-php-4/using-built-in-functions?u=76281980&t=134)** This will take a string and convert it completely to lowercase letters. As you start to write your own code, you should look for opportunities to use and find helpful functions. Especially if what you're trying to do is a complicated task, but for now I do want to point out one function that will likely come in handy a lot. And that's the date function. This function allows you to describe a date format, which PHP will then print out for you. It accepts two arguments, the format, which is a string that describes how the date should be printed out and a time in seconds since the Unix epoch, which is the number of seconds since January 1st, 1970. Let's not worry about the time for now. One note about the documentation is that any arguments in square brackets are optional and you can see the default value with an assigned statement. So if we don't send a timestamp PHP, we'll grab the current time and use that. So let's say we wanted to print a date with the full name of the month, followed by a space and the two digit day followed by a comma and the four digit year let's look at this handy table on the format section. Our two digit year is represented by the character. D if we scroll down to month, the full name of the month is represented by the capital
>
> **[3:47](https://www.linkedin.com/learning/learning-php-4/using-built-in-functions?u=76281980&t=227)** letter F and year is represented by the capital letter Y again, we want the four digit year. So if we go back to our editor and we say eco date, we're going to write a simple string with the characters. We just pointed out F space D comma, Y then if we go view this in the browser, we'll see the current date. If at any time you want to use any of the format characters as the literal character in the date function, you could always use the backslash character to escape them. Luckily, most built in functions. Aren't quite as complicated as this, but the date function is an excellent one to know. And if you don't find a built in function, there's always another option. You can write your own.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (6), [[Microsoft Word|Word]] (1)
> **CLI Commands:** php (6), find (3)
> **Env Vars:** php (5), ide (1), str (1)
> **Definitions:** is an  (2), is a  (2)
> **Documentation:** the documentation (2)
> **Tools:** vs code (2)
> **UI Navigation:** scroll down (2)
> **URLs:** [php.net](https://php.net) (1)

#### [Writing custom functions](https://www.linkedin.com/learning/learning-php-4/writing-custom-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/writing-custom-functions?u=76281980&t=0)** - [Instructor] Now that you know a bit about functions, let's walk through writing our own, something that can actually work in a real program instead of just in the abstract. We're going to use one of my favorite examples from when I was in school and that is detecting if a string is a palindrome, that is a string that reads the same backwards and forwards. This will allow us to accomplish a few objectives, create a Boolean function, pass an argument to that function and use built in [[PHP]] functions. Let's get started. So we'll write our opening PHP tag, and then we will name the function. We'll use the keyword function and we'll call this is palindrome. Since this is going to be a Boolean function, it's always good to name those functions the question they're answering. So this is answering is the string we're passing a palindrome? And then it's going to accept some string, which we will call string. Now the simplest check here is to reverse the string. So we could say pal check gets, and then reverse the string. We could say string equal, equal, and then we could reverse the string manually, but we don't have to. There is a built in function that will give us the reverse of the string. It's called strrev and as I type it, you'll see that VS Code is autocompleting that for me. And it's also telling me that I need to pass it a string.
>
> **[1:36](https://www.linkedin.com/learning/learning-php-4/writing-custom-functions?u=76281980&t=96)** So we've named our function variable string here. So now this is going to get a true or false value. Either the string is the same backwards and forwards, or it's not. The last thing we'll do is return pal check. So here's our complete function and we can test this, right? We could say if is palindrome and notice that now that we've added a function, VS Code is autocompleting that for us too. So if is palindrome and then let's check a simple example, "wow." We will echo p wow, is a palindrome. Close our paragraph and just to make this a little more succinct and reusable, we'll create a new function called check string gets wow and that's the variable that we'll use in here. And then we could use that in the echo statement as well. So it looks like, thanks to syntax highlighting, we are missing something, and that is a closing parenthesis on our function. All right, so we'll save that and then we will refresh our page,
>
> **[3:10](https://www.linkedin.com/learning/learning-php-4/writing-custom-functions?u=76281980&t=190)** wow is a palindrome, great. So this is a working program for the case of wow. However, remember that capital and lowercase letters are different, so if we change the first w to capital and we refresh, nothing happens because this is no longer a palindrome. And in fact, if we have something like race car, which is a palindrome, but a straight reversal of it will keep the capital letters and the spaces which are uneven, that won't be marked as a palindrome either. So there are a few optimizations to our program that we can make. So the first thing we'll do is convert all characters to lowercase so we can use the string that you saw in the PHP manual, strtolower that simply accepts a string. So now our string is all lowercase, and then we want to remove all spaces. So again, we can do string and there's another function called str_replace. This function will take a searched character, a replaced character, and then the subject. So the searched character is going to be space, the replace character is going to be nothing and the subject is going to be string. And then we can reverse the string here.
>
> **[4:46](https://www.linkedin.com/learning/learning-php-4/writing-custom-functions?u=76281980&t=286)** So we'll save that, we'll refresh, race car is a palindrome. Now we'll check our other examples, wow. Wow is a palindrome and all lowercase should also work. Fantastic. Now this isn't going to account for all things, for example, we're not removing any punctuation, but the function can get pretty out of hands pretty quickly as you start to think about all the use cases. The last thing we'll do here is optimize this a little bit. So for example, we don't need a variable. We can just return the Boolean so we can remove pal check. And then we can also combine all of our string processing into one or two lines. So, instead of saying, do a string replace and strtolower separately, we could say, do a string replace on the value that's returned from strtolower and we should still get the same results. That's because of the value that we're passing to the first function from the second function is still going to be a string. So we probably could get a little more clever and put all of this into the return statement,
>
> **[6:19](https://www.linkedin.com/learning/learning-php-4/writing-custom-functions?u=76281980&t=379)** but we still want our function to be readable for other people. We should probably add some comments in here for what we're doing, like convert string to lowercase and remove spaces, and then check string against that same string reversed. So there you have it. Remember, as you write your own functions that the built in functions can be extremely useful here and check [php.net](https://php.net) to see what's available to you. They can save you a ton of time. Reversing a string by hand, for example, is very time consuming, would require a loop and can slow down your program.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (4)
> **Definitions:** is a  (10)
> **CLI Commands:** php (4), make (2)
> **Env Vars:** php (3)
> **Analogies:** for example (3)
> **Tools:** vs code (2)
> **Code Identifiers:** str_replace (1)
> **URLs:** [php.net](https://php.net) (1)

#### [Union typing variables](https://www.linkedin.com/learning/learning-php-4/union-typing-variables?u=76281980)

#### [Optional or named arguments](https://www.linkedin.com/learning/learning-php-4/optional-or-named-arguments?u=76281980)

#### [Anonymous functions](https://www.linkedin.com/learning/learning-php-4/anonymous-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/anonymous-functions?u=76281980&t=0)** - [Instructor] Another way to write functions is anonymously. Anonymous functions don't get a name and are often created at runtime to perform a specific task. This is slightly more efficient than defining a function if you're only going to use it once. These are also called closures or callback functions. The latter because sometimes functions in [[PHP]] require a different function as the second argument. PHP is basically saying, we will do some of the work, but you need to tell us how to do it. One common example of this is the usort function in PHP. You could see that it accepts an array as its first value and it is being passed by reference and then, the second argument is a callable compare function. So usort will sort the array, but you need to tell it how you want to compare the items. Since we're only going to use this function for usort, it doesn't make sense for us to define a separate function and keep it in PHPs memory if we're only going to use it once so we are going to write an anonymous function. Let's go ahead and do that now. So we have our PHP file here and let's create a simple array of names. So we'll say array, Joe, Erin,
>
> **[1:34](https://www.linkedin.com/learning/learning-php-4/anonymous-functions?u=76281980&t=94)** Teresa and Louis. We can then sort the array using the usort function. So the first thing we'll pass is the array, which is names, and then we're going to define an anonymous function. So to do that, we're going to use the keyword function, but we're not going to give it a name. We're just going to give it values or arguments. So we're going to pass two arguments, a and b, and then, we will open up our curly braces. Let's add the semi-colon to the end so we don't forget. So now we can just write our function normally. Now, PHP already has a sorting function that will sort strings by the first letter. So let's have a little fun here and sort it by the second letter and we can do this pretty cleverly. So we're just going to have a return function here and then we need to tell the sort function to compare the second letter of each string. Now PHP actually treats strings as a raise so we can reference each character by the index. Since indexing starts at zero, the second character in the string will be one. So we'll say return a one and then we can use a spaceship operator. This is a relatively new operator in PHP that will do a greater than, less than and equal to comparison all in one shot and return a value.
>
> **[3:07](https://www.linkedin.com/learning/learning-php-4/anonymous-functions?u=76281980&t=187)** So we're saying a one spaceship operator b one if in the event that the second character in a is greater than or equal to the second character in b, then the spaceship operator will return a, but if b is greater than a, the spaceship operator will return b. So with this sort function, let's go ahead and print out our array and we will add the pre-tags first and I'm actually going to do this a different way this time. I'm going to close our PHP and then I will open it again after I write the pre-tag, print r names and then I'll close the pre-tag. So I'll save this. I'll refresh the page. So let's see, we've got E-O-O-R. That works. Now if we wanted to get a little more fancy, we can compare more than one character, but we'll save that for a different part of the course. Anonymous functions are used in pretty specific cases, but they are great to know about as you'll likely come across them, especially if you need to do something like sort objects. Speaking of, let's take a look at objects.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (8)
> **CLI Commands:** php (8), make (1)
> **Env Vars:** php (8)
> **Definitions:** is a  (2)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor] (1)

#### [What are objects and classes?](https://www.linkedin.com/learning/learning-php-4/what-are-objects-and-classes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/what-are-objects-and-classes?u=76281980&t=0)** - [Instructor] Functions are a great way to have consistency throughout your code base. And you can reuse useful code. Another way to do that is with classes. A class is a way of grouping variables and functions together as a single referenceable unit. The powerful part of classes is you can have more than one as long as you name them differently. The different instantiations or versions of these classes are called objects. And the benefit is that you can have a single set of variables and functions and based on the object, reference them. [[PHP]] has many built-in functions but we can also write our own. So if you have a Person class, you can use it to create two Person objects, Alice and Bob. They'll have the same variables and functions available to them but the information will be different. You can think of classes as the mold for a toy. Then each individual toy with different color plastic and paint are the objects. Variables in classes are called properties. So in our Person class, we might have properties for a first and last name, occupation, and age. Functions in classes are called methods. We can use methods to access properties, remember scope comes into play with classes as well, and to perform tasks specific to an object's information. So we might have a method for our Person class called birthday where we increment the age. Functions that create the object
>
> **[1:36](https://www.linkedin.com/learning/learning-php-4/what-are-objects-and-classes?u=76281980&t=96)** are called the constructor. So you would instantiate an object using some code that you see here. Alice gets new Person and then we pass a string to it. This is all dependent on the definition of a class, which you'll do later. But the important parts here are that after the assignment operator, we have the keyword new. This tells PHP that we're about to create a new object based on the Person class. And we can do this as many times as we want. So we can create a second object called Bob. We'll say Bob gets new Person and then pass the string Bob to it. Similarly, we can get information from these objects based on the properties and methods available to us. So if we have a method called get name, we could say echo Alice get name to print out Alice or echo Bob get name to print out Bob. Notice that in these cases, when we're calling methods or properties that are based within classes, we need the arrow operator. So we say Alice arrow get name or Bob arrow get name. The same thing goes for sending information to an object. We could say something like Alice arrow set age and then pass an integer or Bob arrow set age to set Bob's age. And then we can reference those as well.
>
> **[3:09](https://www.linkedin.com/learning/learning-php-4/what-are-objects-and-classes?u=76281980&t=189)** So that's the basic syntax of a class or object. But it'll make more sense to see it in action. So with the basic syntax down, let's write a simple Person class.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (2)
> **CLI Commands:** php (2), make (1)
> **Env Vars:** php (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Defining a simple person class (update for PHP 8.0)](https://www.linkedin.com/learning/learning-php-4/defining-a-simple-person-class-update-for-php-8-0?u=76281980)

#### [Using the person class](https://www.linkedin.com/learning/learning-php-4/using-the-person-class-8393866?u=76281980)

#### [Challenge: Sort an array of objects](https://www.linkedin.com/learning/learning-php-4/challenge-sort-an-array-of-objects?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/challenge-sort-an-array-of-objects?u=76281980&t=0)** - [Instructor] Now that you know all about functions and classes, it's time to put that knowledge to use. On the screen here you see a simplified version of our person class. This information will be in the exercise files for this video. But your job will be to sort an array of person objects by last then first name and the array is provided for you in the exercise files as well. As a hint, you'll need to use a sort function that you already saw and be sure to have information about that spaceship operator handy. It will help.

> [!info]- Semantic Content
>
> **Exercise Files:** exercise files (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Sort an array of objects](https://www.linkedin.com/learning/learning-php-4/solution-sort-an-array-of-objects?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/solution-sort-an-array-of-objects?u=76281980&t=0)** (bright music)
>
> **[0:06](https://www.linkedin.com/learning/learning-php-4/solution-sort-an-array-of-objects?u=76281980&t=6)** - [Instructor] All right, here's the solution to the challenge. You might have guessed that we're going to use the usort function and we're going to pass it the array people. So now we need to define an anonymous function. We'll use a and b again for the variables we're passing to it and we'll add our curly braces and our semi-colon to the end so we don't forget. Now the first solution that comes to mind is to use the spaceship operator that you saw previously. So we could say something like a get last name and then the spaceship operator b get last name
>
> **[1:03](https://www.linkedin.com/learning/learning-php-4/solution-sort-an-array-of-objects?u=76281980&t=63)** and if we do this and then print out the results of the sort pre and then we'll add our [[PHP]] tags again
>
> **[1:19](https://www.linkedin.com/learning/learning-php-4/solution-sort-an-array-of-objects?u=76281980&t=79)** and pre and then refresh the page. We'll see we do have a sorted list of objects by last name. Casabona, Casabona, Casabona, Disney, Gates, Iger and Wozniack, but the first three people are not in alphabetical order, they all have the same last name, but Erin would go before Joe which would go before Rob. So what else can we do here? Well, luckily, the spaceship operator can also sort arrays. So what we'll do and this will be a fairly complex looking line. So I'm just going to expand my editor here a little bit. So we would say return and then I'm going to use the shorthand definition for arrays which is just simply square brackets. So I would say square bracket a get last name comma a arrow get first name and then close that square bracket and I'll do the same thing over here. Square bracket b arrow get last name comma b arrow get first name
>
> **[2:43](https://www.linkedin.com/learning/learning-php-4/solution-sort-an-array-of-objects?u=76281980&t=163)** close square bracket. So what this is saying is use the spaceship operator to first compare last names or the first element in the array and then compare the first name or the second element in the array. Now if I save this, reduce the window size again and refresh, now the Casabonas are sorted properly and the [[Representational State Transfer (REST)|rest]] of the array is, too. So that's how you can use an anonymous function within the built-in usort function to sort an array of person objects.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** php (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright music) (1)


### 6. Including External Files

[↑ Back to Table of Contents](#table-of-contents)

#### [Organizing your code](https://www.linkedin.com/learning/learning-php-4/organizing-your-code?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/organizing-your-code?u=76281980&t=0)** - [Instructor] As you can imagine, writing lots of code in a single file can get unwieldly and slow to run and manage. There is a better way. External files. [[PHP]] offers a few ways for you to include files inside your code but they are all referred to as server side includes. This, as the name suggests, includes all of the files in a single package that it sends to the browser. That means there's only one HTTP request. Unlike [[HTML]], CSS, and [[JavaScript]], which generally require one HTTP request per file, PHP does all of the processing, including the files on the server, and sends them as one. There are several benefits to using server side includes in your applications. First, it allows you to break up large sections of code. You can put your classes in separate files, you can group functions together, and more. As a result, you can keep output files separate from processing files. So if you have heavy HTML files, then you might not want to put them in a class file or a file that requires a lot of functions. You can separate them out. You can also organize your website into different files, taking the reusable parts and templatizing them. So far, we haven't talked much about the HTML output or organization side of PHP. We were focused more on how PHP works.
>
> **[1:37](https://www.linkedin.com/learning/learning-php-4/organizing-your-code?u=76281980&t=97)** But when you include files, PHP essentially inserts those files and the code wherever you include that file. So let's say on line one we have some code, on line five we have more code, and on line three we include an external file. PHP is essentially copying everything in that external file and putting it on line three. That means that if you want to break your website into different parts like a header, a footer, and a sidebar, then you can. You can use the same header and footer throughout the entire website. And whenever you change them, they'll change across every page that includes them. So if you have a website with 100 pages and you want to change one of the links in the navigation, you'd only have to do it once instead of 100 times. It's up to you to figure out how to best organize your files. And that might change based on the size and scope of the project you're working on. But for now, let's take a closer look at how to use server side includes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (6), [[HTML]] (3), [[JavaScript]] (1)
> **Env Vars:** php (6), html (3), http (2), css (1)
> **CLI Commands:** php (6)
> **Definitions:** is a  (1), means that (1)
> **Analogies:** imagine (1)
> **Speakers:** - [instructor] (1)

#### [Using include and require](https://www.linkedin.com/learning/learning-php-4/using-include-and-require?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/using-include-and-require?u=76281980&t=0)** - [Instructor] There are two ways to include external files in [[PHP]]. They are with the built-in directives or functions, Include and Require. There is a small distinction made because Include and Require don't have the parentheses around them, though you could write them that way. They're not technically functions. They're more like statements that you're making. They both work the same way though, with the small difference that with Include, if the file doesn't exist, Include will throw a warning or non-terminating error, and Require we'll throw a compile error, which will stop the program from running. So let's say that we have two files, Index and Variables, both are in the same directory. What we can do is copy the variables from Index and paste them into the Variables dot PHP file. We still need the opening PHP tag here, and then we'll save that. And instead, we could say Require will require this because our variables are being used later. So we don't want to cause an error, Require variables dot PHP. Now, if we open this in the browser, we see our [[HTML]] is printed out with our variables. And if we view the source, you'll see at the HTML,
>
> **[1:38](https://www.linkedin.com/learning/learning-php-4/using-include-and-require?u=76281980&t=98)** no traces of PHP here. If you want to add a folder structure, you can do that too. It would be the same as linking to HTML files. So if we move the variables file into the INC or INK folder, first, we'll save this and go back to our browser. Notice that we're getting a warning and then a fatal error because we required the Variables file. So if we go back and fix this to add INC slash variables and then refresh everything is working properly. You can also make, Include or Require statements within the classes and functions. You should just remember that the code will be within the scope of wherever you include it. So if we have a function here, we'll just say function INK. And then we put the Require call inside the function and then go back to our page, we're getting an undefined variable because now the variables are within the scope of our function. Delete those. So you should remember that the code is being inserted wherever the Include file is added. That way your PHP will definitely be valid after the insert. One more note about Include and Require. If you are using it to build website templates, you need to be mindful of the links
>
> **[3:10](https://www.linkedin.com/learning/learning-php-4/using-include-and-require?u=76281980&t=190)** as they appear to the user. So let's say I add a Navigation here. I'll say NAV, and then I will link to the About dot PHP page. Now this is going to be site-wide Navigation. So maybe I want to actually include it with a server side includes. I will cut this, and then I will open my PHP tag. I'll say Include this time. And I have a folder called Templates where I want to put all of my HTML templates. So I'll say Templates slash NAV dot PHP. Then I'll create a file in the Templates folder called NAV dot PHP. And I will type this properly, save this. Now you might be thinking that because About is being linked from one folder below the index file that we would need to use a relative link to move it up to the correct directory. However, we need to hyperlink as if it's all one file, because PHP makes it one file before sending it to the server. So we need to keep our relative links in mind for again, what will be available in the browser. If you lose track of this, you might run into a 404 error.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (11), [[HTML]] (4)
> **Env Vars:** php (11), html (4), nav (3), inc (2), ink (2)
> **CLI Commands:** php (11), make (1)
> **Cross-References:** go back to (2)
> **Warnings:** warning (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [include_once and require_once](https://www.linkedin.com/learning/learning-php-4/include-once-and-require-once?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/include-once-and-require-once?u=76281980&t=0)** - [Instructor] There are two other functions to help you include files, include once and require once. These files work exactly the same as include and require, except that if the file in question has already been included, these functions return the value true and the file isn't included again. This is great if you want to make sure you're not including a set of functions or a class more than once, because [[PHP]] throws an error if you try to redefine these functions or classes. So let's take a look at a quick example. You can see, I have my index page on the screen here and in the inc folder, there is a new file called functions.php. In functions.php I've got two functions, pre-print and para print to do some of the things we've been doing a lot. And if I go back to include, I can add it here on the page, I'll do it above the variables file inclusion. And first I'll use require here. So we'll say require inc/functions.php. And just to see if that's working, we can delete this line and instead say, para print description. So now we'll go to our browser. We'll refresh the page and nothing's changed because the functions are working properly.
>
> **[1:34](https://www.linkedin.com/learning/learning-php-4/include-once-and-require-once?u=76281980&t=94)** But let's say I forget what I'm doing for a minute and I realize, hey, I got to include my functions file, and I decide to do it in the variables file. So I'll do require functions.php. Notice I'm leaving off the inc because functions and variables are in the same directory. Now, if I go back out to my website, I refresh and I have a fatal error, cannot redeclare para print, which was previously declared. That's because I am requiring the functions file more than once. So I'll change this to require once and I'll do the same thing here require once. And just to show you what happens, we'll do a VAR dump here since this will return true if it's already been included. So I'll save both of these. I'll go back out and you can see we're no longer getting a fatal error. And there is a true value, which means that at this point, online six of variables.php functions has already been included.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (6)
> **CLI Commands:** php (6), make (1)
> **File Paths:** functions.php (3), inc/functions.php (1), variables.php (1)
> **Definitions:** is a  (2), means that (1)
> **Env Vars:** php (1), var (1)
> **Cross-References:** go back to (1)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)

#### [Namespacing](https://www.linkedin.com/learning/learning-php-4/namespacing?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/namespacing?u=76281980&t=0)** - [Illustrator] When you declare a variable, you can reassign values to it over and over again, even within the same scope. But when it comes to functions and classes, [[PHP]] isn't so open to that, and for good reason. Changing how a function works in the middle of a program would be like replacing your car engine with a bike pedals in the middle of the Daytona 500. That also means you need to be mindful of the code already written, especially if you're working within a larger framework with lots of contributing developers, something like [[WordPress]] or [[Laravel]]. You might not know all of the code that's written or could be written, and if your class or function names are not specific enough, you could unknowingly cause a site breaking errors. A more recent development in writing PHP is Namespacing. The idea here is that you might not have a class of functions, but it's common functionality that you don't want colliding with other code. This will allow you to make your own scope for functions. Let's look at a simple example. Let's say that we have a function here called double, but it doesn't do what we'd expected to do. It only increments the past value by two. So we have function double A and then return A plus two. Now, we can't later in the file redefined function to make it do what we need it to do. If we tried to do that, which I'm doing right now,
>
> **[1:35](https://www.linkedin.com/learning/learning-php-4/namespacing?u=76281980&t=95)** return A times two PHP will yell at us saying we can't redeclare double. So what do we do? We can create a namespace. Let's create a new file in our directory and we'll call it the Learning PHP Namespace. So learningPHP.PHP. The first thing that we want to do to create the namespace is actually define it. So we'll add our PHP tag and then the next line will be the [[Microsoft Word|word]] namespace, and then whatever we want the namespace to be. We'll call this learning PHP. Now, we can define our new function and save this file. Now, if we go back to index, let's include our namespace learningPHP.PHP add the semi-colon. And now we can use our new double function. The way we would do that is by adding the name of the namespace and then a backslash with the function, just like you would do in a file system or directory. So here we can write echo P learningPHP/double five,
>
> **[3:05](https://www.linkedin.com/learning/learning-php-4/namespacing?u=76281980&t=185)** and then add our closing paragraph tag. And now when we refresh the page, we have our incorrect function and then our properly namespaced function. Namespaces can be very handy, especially if you're working on big projects or when you want to keep a set of functions or classes together.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (8), [[WordPress]] (1), [[Laravel]] (1), [[Microsoft Word|Word]] (1)
> **CLI Commands:** php (8), make (2)
> **Env Vars:** php (8)
> **Code Identifiers:** learningphp (3)
> **File Paths:** learningphp.php (2)
> **Cross-References:** later in (1), go back to (1)
> **Analogies:** just like (1)
> **Speakers:** - [illustrator] (1)

#### [Challenge: Build a simple templating system for the provided markup](https://www.linkedin.com/learning/learning-php-4/challenge-build-a-simple-templating-system-for-the-provided-markup?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/challenge-build-a-simple-templating-system-for-the-provided-markup?u=76281980&t=0)** (upbeat electronic music)
>
> **[0:05](https://www.linkedin.com/learning/learning-php-4/challenge-build-a-simple-templating-system-for-the-provided-markup?u=76281980&t=5)** - [Instructor] Complex websites are significantly easier to manage when using [[PHP]] because you have the opportunity to templatize sections, which you can then change in one place and know they'll change everywhere. For this challenge, take the Exercise Files, the provided markup, and turn it into a set of templates. Then use those templates to create an about and a contact page.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (1)
> **CLI Commands:** php (1)
> **Env Vars:** php (1)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat electronic music) (1)

#### [Solution: Build a simple templating system for the provided markup](https://www.linkedin.com/learning/learning-php-4/solution-build-a-simple-templating-system-for-the-provided-markup?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/solution-build-a-simple-templating-system-for-the-provided-markup?u=76281980&t=0)** - [Instructor] All right, let's take a look at the solution for our website template challenge. Here's the resulting [[HTML]]. And if we go back to our directory, you can see that we have a single index.[[PHP]] file that we want to convert into templates. So I'm going to go ahead and create two directories here. And I should say right off the bat that you probably did this differently. There are a lot of different ways that you can solve this problem And this is just one of the many solutions. So I'm going to create a folder called Inc and a folder called templates. I'm doing this because I like to keep my projects organized in this way, where Inc is where straight up PHP files go. Things like variables or functions and templates are where the different HTML sections go. So, again, right off the bat, I see a set of variables here. I'm going to cut them out of this file and I'm going to create a new file inside Inc and call it variables. Then I'm going to add my PHP tag and paste them here. Now I'm not going to include them here yet because there's something else that we're definitely going to do here and that is create a header file.
>
> **[1:36](https://www.linkedin.com/learning/learning-php-4/solution-build-a-simple-templating-system-for-the-provided-markup?u=76281980&t=96)** So we want to grab all of the markup that is going to belong in the header. Anything that we want to show up before the main content qualifies as header markup. So we'll start here on line two, and I'm going to copy all the way down to the opening main tag. The opening main tag is where our main content will be. So I'm going to cut this. And then inside the templates folder, I'm going to create a file called header. Call it header.PHP, and then I'm going to paste in that markup and I'm going to save it. We don't necessarily need the PHP tags here in the opening because we're starting with HTML, but we do need to name this A.PHP file because we are referencing the variables. Which brings us to the next step. We want the variables to be included on every page so it makes sense to include them in the header. So on the very first line here, I'm going to write require once. And then the path to the variables file. Now this is a relative path and it is in the Inc folder, which is in the same directory as the templates folder. So we actually need to go up one directory and then back down into Inc and variables. If this is wrong, since I used require once, we will get an error when we look at the page.
>
> **[3:11](https://www.linkedin.com/learning/learning-php-4/solution-build-a-simple-templating-system-for-the-provided-markup?u=76281980&t=191)** Okay, so now our header file is set. Then we want to replace all of the markup we just moved with a include for the header. And we will do include here because the website won't completely break if we don't include the header. So let's say include and then templates, header.PHP, okay.
>
> **[3:40](https://www.linkedin.com/learning/learning-php-4/solution-build-a-simple-templating-system-for-the-provided-markup?u=76281980&t=220)** And if you want, sometimes what I'll do is I'll add an HTML comment here that says main content starts, just so if I'm working with a big directory or set of files, I kind of know what I'm looking at, even if I don't see the header.PHP file. So next we want to create a second template for anything that comes after the main content, which would be the footer. So I will copy everything else on the page from the closing main tag down. And I will add that into the templates directory as footer.PHP, and then I'll paste that markup. And that's all we need to do here. Again, if we didn't have this copyright line, we could get away with making this a .HTML file, but we are using some PHP in this, so we need to make it a PHP extension. And then we'll do the same thing here. So I will add a comment that says end main content, and then we want to include the footer.PHP.
>
> **[5:02](https://www.linkedin.com/learning/learning-php-4/solution-build-a-simple-templating-system-for-the-provided-markup?u=76281980&t=302)** Okay, so with the directory structure set, let's see how it looks in the browser. Nothing should have changed at this point if we did it right. All right, and it looks like require once, failed to open stream, so we are unsure of where the file is. And I reckon I know what happened because templates gets called here, this actually just needs to be inc.variables. And there we go. So now that our index file is set up, it's time to create an about page and a contact page. So I'm going to copy the index page and then paste it.
>
> **[5:54](https://www.linkedin.com/learning/learning-php-4/solution-build-a-simple-templating-system-for-the-provided-markup?u=76281980&t=354)** VS Code makes this very easy to do. We'll call this about.PHP. And now we can just modify the content here. So we can say about Joe or we have an author variable so we can say about author,
>
> **[6:19](https://www.linkedin.com/learning/learning-php-4/solution-build-a-simple-templating-system-for-the-provided-markup?u=76281980&t=379)** and then we can have some biographical text here. Okay, so we'll save this and then if we click about me, you could see everything stayed the same except for the main content. So we'll do the same thing for the contact page. I'll copy the about page, paste it and rename it. And we'll change this to contact. And then we'll change this text to email me Joe@[casabona.org](https://casabona.org), so we'll save that, then we'll click contact and that content has changed. So there you have it. We have successfully templatized our website. So now if we ever make a change on the footer or the header, it will change for all pages.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (13), [[HTML]] (5)
> **Env Vars:** php (13), html (5)
> **CLI Commands:** php (13), make (2)
> **File Paths:** header.php (3), footer.php (2), index.php (1), a.php (1), about.php (1)
> **Exercise Files:** template (2)
> **URLs:** [casabona.org](https://casabona.org) (1)
> **Cross-References:** go back to (1)
> **Tools:** vs code (1)


### 7. Troubleshooting Your Code

[↑ Back to Table of Contents](#table-of-contents)

#### [Common debugging tools in PHP](https://www.linkedin.com/learning/learning-php-4/common-debugging-tools-in-php?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/common-debugging-tools-in-php?u=76281980&t=0)** - [Instructor] Finding errors in code, especially when you're just starting out, isn't always the easiest thing to do. From syntax to runtime errors, you might spend some time troubleshooting, so it's good to look at what you'll come across. But before diving into common errors, here are a few tools that will help you quickly find and prevent bugs in your code. The first is your own code editor or IDE. Modern tools will have syntax highlighting, auto-completion, and will point out syntax errors in your code. So you could see on the screen here that VS Code highlights variables in blue and constant or literals in this teal green. So let's say I want to put some extra parentheses around a conditional. If I say a is less than two, I want to do something. Notice that the code editor added extra parentheses here. But if I forget the parentheses, echo "Hello", and then I save it, we'll have a red underline here, and VS Code will tell me the syntax error. If it knows the solution, it'll also tell me the solution. But in this case, I know to look on the line before, and that gets rid of the error. How you turn on errors will vary from environment to environment. You can turn on debugging in certain tools, or you can modify what's called your [[PHP]] INI file. We haven't talked much about the PHP INI file so far,
>
> **[1:37](https://www.linkedin.com/learning/learning-php-4/common-debugging-tools-in-php?u=76281980&t=97)** but it's basically a settings or configuration file for your server. You'll have access to it locally, but you probably won't on a server you don't manage. Still in the PHP INI file, you can say things like display errors and modify error reporting. In fact, error reporting is a built-in function in PHP. So if you don't have access to the PHP INI file, you might be able to use this function to do it. You have the function and then a level. The level is an integer, but there are some constants associated with error reporting. So if we want to turn off all error reporting, I could say error_reporting zero. So with error reporting turned on, now I'll do something that I know will cause a runtime error. And that is set a cookie, which you can only do before anything is printed out on the screen. And just to drive this point home, I will also print out a heading after the fact. So I'll save this. We'll go to our browser, refresh, and there's nothing on the page. It's completely blank, and there is no [[HTML]] outputted either. If we change error reporting to E_ALL which is a constant saying report all errors, we'll learn why the page is blank.
>
> **[3:12](https://www.linkedin.com/learning/learning-php-4/common-debugging-tools-in-php?u=76281980&t=192)** Again your mileage may vary on this because of the way your server is set-up. But you should have more control on a local development environment. Aside from your code editor and PHP built-in functions, printing data on the screen can be very helpful. You've seen us do this a lot with arrays and other variables, and there are two ways to do that. There's print_r, so if we print_r a at our PHP tags here, and we'll get rid of our error, and we var_dump a, we can see that both of these functions offer data to help us debug our code. So we have the number two for print_r. But for var_dump, we have the line in which our code is being called, the data type, and the value. This gets even more nifty with arrays. So if I create a colors array: red, blue, and green, and then we var_dump the colors array, you could see that var_dump print out information for each index. And I should say that again, Local by FlyWheel is adding some format to var_dump here to make it a little bit easier to read. But aside from printing in variables, printing in and around control structures can help you track the decision making that's going on in your code.
>
> **[4:44](https://www.linkedin.com/learning/learning-php-4/common-debugging-tools-in-php?u=76281980&t=284)** And this is especially helpful with certain type of errors, like logic errors. There are also tons of advanced debugging tools and tricks you can set up on your computer, like Xdebug. To learn more about those, check out the PHP debugging course in the [[LinkedIn]] learning library.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (8), [[HTML]] (1), [[LinkedIn]] (1)
> **Env Vars:** php (8), ini (4), ide (1), html (1), e_all (1)
> **CLI Commands:** php (8), find (1), make (1)
> **Code Identifiers:** var_dump (5), error_reporting (1)
> **Definitions:** is a  (2), is an  (1)
> **Tools:** vs code (2)
> **UI Navigation:** go to (1)
> **Prerequisites:** set up (1)

#### [Types of PHP errors](https://www.linkedin.com/learning/learning-php-4/types-of-php-errors?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/types-of-php-errors?u=76281980&t=0)** - [Instructor] There are several different types of errors in [[PHP]], and they will behave differently based on their severity. Syntax or parse errors are generally typos or misused characters in your code. The compiler will stop your code from running completely, and there will be no output with a parse error. You can see we have one here on line four, where I forgot to escape a single quote. The way this page is structured is there's a heading for each error, and then a paragraph after each error, to demonstrate whether or not output continues. In this case, you can already see that my syntax highlighting is off and the line in question, line four has a red underline. If I save this page and open it up in the browser, you can see before the parse error, we have a heading, but when I refresh, all of that goes away and there's a single parse error, that says unexpected 'VE' and then it tells you what it was expecting on line four. That helps us figure out exactly where the bug is. I know that there is a missing escape here. And so once I do that, the underline goes away and the [[Representational State Transfer (REST)|rest]] of my syntax highlighting is looking good. Warnings are errors that will not terminate your code, but will tell you of a situation that might cause bigger problems down the line. One example is a missing include file.
>
> **[1:36](https://www.linkedin.com/learning/learning-php-4/types-of-php-errors?u=76281980&t=96)** That will generate a warning that the file hasn't been included. Which in and of itself, will not cause your program to terminate, but it could be problematic if you defined functions, which will cause another kind of error. So you can see on line 10, I'm including a variables.php file that is not in my directory. If we go back out to our page and refresh, you'll see that we have output them we have the warning, but the output continues. The next kind of error is a fatal error. And as the name implies, the fatal error will terminate or kill the program because the error causes a breaking issue. The PHP manual calls, these errors, unrecoverable. They will make your program work unexpectedly. You can imagine the difference between a warning and a fatal error like this, a dent in your car is a warning. The door shouldn't be dented, and it could cause an issue like your door not fully closing down the line, but right now your car still works. But if you run out of gas, that's a fatal error because now your car will not drive. Here's an example of a fatal error. I'm calling a function called multiply that has not been defined. So if we go to our page and refresh, and I scroll down to the fatal error section, you'll see that we have a fatal error called undefined function, multiply on line 14, and no other output happened after that.
>
> **[3:13](https://www.linkedin.com/learning/learning-php-4/types-of-php-errors?u=76281980&t=193)** The last official error in PHP is a notice. This will tell you that something might be wrong, but it might not be either. This is a keen to your check engine light or your gaslight, you're being told something doesn't seem right, but everything also looks like it's working normally. An example of a notice is an undefined variable. Perhaps you haven't defined it yet, and technically it's not breaking anything. So let me comment out my fatal error here, so that we could see what a notice looks like. And I'll add some output here, notice has code terminated. I'll refresh, and then we get the notice that there's an undefined variable, but the code continues. There are also a logic errors. They aren't official PHP errors, but they occur because there are flow problems in your code. All of your code can be devoid of the other errors and notices, but there's still a problem based on some assumption you might have made. A great example of this as an infinite loop. Infinite loops don't throw errors per se, but you've made an assumption about a terminating condition that never happens. So with these four types of errors defined, plus the logic errors, let's take a look at some specific examples.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (5), [[Representational State Transfer (REST)|Rest]] (1)
> **Definitions:** is a  (6), is an  (1)
> **CLI Commands:** php (5), make (1)
> **Env Vars:** php (4)
> **Warnings:** warning (4)
> **UI Navigation:** go to (1), scroll down (1)
> **File Paths:** variables.php (1)
> **Analogies:** imagine (1)

#### [Why is my page blank?](https://www.linkedin.com/learning/learning-php-4/why-is-my-page-blank?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/why-is-my-page-blank?u=76281980&t=0)** - [Instructor] One common scenario that can be confusing if you've never seen it before is your website loading a completely blank page, no mark up or anything. Now modern servers or even modern browsers might treat this error differently by showing a 500 error so you won't get a completely blank page but your page will be broken and you won't know why. Outside of a completely blank page, you might have an error where you have some output but at some point it just stops. And in any of these cases, this will happen if you have error reporting turned off in [[PHP]]. This will be most common with syntax errors because they completely stop the code from compiling. Remember that fatal errors terminate the code during runtime so any code that produced output before that fatal error will still show up. Of course, if there's a fatal error before any output, the page will also be blank. The best thing to do here is revisit the error reporting function you learned about earlier. Or, if you have access to your PHP INI file, you want to make sure that error reporting is turned on to E all and display errors is also turned on. If you modify your PHP INI file, you will need to restart your server for those changes to take effect, but also remember to turn these errors off for production. Part of the reason to show a blank page instead of the error is your website is failing
>
> **[1:34](https://www.linkedin.com/learning/learning-php-4/why-is-my-page-blank?u=76281980&t=94)** more gracefully so users don't see the errors but it's also for security. You don't want to accidentally expose information that can help malicious people attack your website or server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (3)
> **Env Vars:** php (3), ini (2)
> **CLI Commands:** php (3), make (1)
> **Best Practices:** remember to (1)
> **Speakers:** - [instructor] (1)

#### [Common parse errors](https://www.linkedin.com/learning/learning-php-4/common-parse-errors?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/common-parse-errors?u=76281980&t=0)** - [Instructor] When it comes to syntax errors, your code editor should do a lot of the work for you. Still, if you don't have real time error checking or there's a lot of code to manage, you might miss certain syntax errors. Here are some of the common ones you might come across. On line two here, you have a missing semi-colon. Pretty much every line needs to end in a semi-colon. Starting out or if you have a lot of code, this one is easy to miss. If we refresh, we'll see that there is an unexpected echo on line four, so you'll also notice that the error is not happening on line two, it's happening with the next line of code on line four. That's something to keep in mind. Usually when you see the [[Microsoft Word|word]] unexpected, you could be missing a semi-colon. Incomplete quotes is a another error that you might come across and this could cause two different types of errors that might cause some confusion. Here on line four, you'll see the statement echo hello, but we haven't defined echo as a constant. In fact, we are intending to use it as a string, but we forgot the quotes. So if we refresh our page, we'll see that we see a parse error, but it's on line eight. Line eight does not have a parse error, the one above it does though, so just to make this error explicitly clear because this would be very hard to track down, let's get rid of the other errors.
>
> **[1:34](https://www.linkedin.com/learning/learning-php-4/common-parse-errors?u=76281980&t=94)** We'll save this and then we'll refresh. Now we see the actual error, which is a warning. Undefined constant hello, assumed and then hello in quotes, this will throw an error in a future version of [[PHP]]. So technically, if we had errors turned off, we would see nothing here, we would just see what we expected. However, this is a warning because in future versions of PHP, undefined constants will not be treated as strings. That's also why the other errors showed up first. So now let's add those back in and we will put quotes around hello so that we can continue on this journey. The other incomplete quote related error you might see results because you might have a misplaced or missing quote that confuses the compiler. So in this case, we're missing the opening quote around the second value in our associated array. If we refresh, we'll see unexpected comma T constant encapsed string. This means that there's unexpected white space and this error could be confusing because it might not show up or it might say that it's on the last line of the file depending on the error. So just keep that in mind as well. The last couple of errors pertain to missing syntax for control structures. So you can see that we have an error here.
>
> **[3:06](https://www.linkedin.com/learning/learning-php-4/common-parse-errors?u=76281980&t=186)** There's a missing parenthesis. If we refresh the page, it's another parse error that says unexpected curly brace here. So we know that the error happens before that and similarly, if we get rid of the closing curly brace, we'll get an unexpected end of file and what that means is that the file randomly ended and the compilers not sure why. So when you get either one of those, be sure to check your curly braces and your parentheses. As you work through parse errors, you might notice that the error seems to often happen on the next line. That's because the compiler doesn't know there's an error until it encounters unexpected code. Be sure to read the error closely and look at the few lines proceeding the line mentioned in the error.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (2), [[Microsoft Word|Word]] (1)
> **Definitions:** is a  (3), is an  (1), means that (1)
> **CLI Commands:** php (2), make (1)
> **Warnings:** warning (2), keep in mind (1)
> **Env Vars:** php (2)
> **Speakers:** - [instructor] (1)

#### [Common PHP errors](https://www.linkedin.com/learning/learning-php-4/common-php-errors?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/common-php-errors?u=76281980&t=0)** - [Instructor] While some errors can be clear, others can be pretty cryptic. Here are a few that you'll probably come across and what they really mean. The first is a failed to open stream warning. This happens when you have an include statement to a file that doesn't exist. You'll want to check to make sure that you don't have any typos and that you know you're in the right directory. In this case, the variable. Now, if we refresh the page, that error is gone, but it's replaced by another error. Warning, cannot modify header information, headers are already sent. This one is actually less common because recent versions of [[PHP]] offer a buffer, but it's a bit of a head scratcher. Basically the header directive or function allows you to send headers about the file. That's information about the file. One thing that you can do is redirect in PHP using the location directive. So, this should say location index.php, which means the file should redirect to a different page. Headers already sent means that there has already been output on the page. Now this is odd because there is no output on the page, except for the headers already sent error. So what's happening here? Well, there are two things. You could see that there is a blank line before the PHP declaration.
>
> **[1:36](https://www.linkedin.com/learning/learning-php-4/common-php-errors?u=76281980&t=96)** That technically counts as output. So, if we get rid of that, we still have that error, but it actually changed to the variables file. If we go there, you'll notice that we just have a simple variable in this declaration, but again, we have the closing PHP tag and two blank lines. That's causing output, so we should get rid of them. And actually you may have noticed throughout this course that if there are straight PHP files, I often left out the closing tag. It's to prevent errors like that. When you don't need to close the PHP tag to allow for [[HTML]] output, you should leave it out. Now, if we refresh, we do redirect, but there is no index.php page for this folder, so we get a not found error. Let's go ahead and comment this one out because we don't want the redirect happening while we're trying to look at other errors anyway. The last error I want to point out here is undefined offset. This could also be known as an undefined constant or an undefined index out of range. And basically what it means is you're trying to print a value in an array that doesn't exist. In our colors array we've got three colors, and we are trying to print the fourth index. We'll also get this error
>
> **[3:08](https://www.linkedin.com/learning/learning-php-4/common-php-errors?u=76281980&t=188)** if we try to print the third index, because as a reminder, indexing for array starts at zero and the highest index is one less than the number of elements in the array. This is not a comprehensive list by any stretch, but these are some common or confusing errors that tend to pop up in PHP a lot.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (9), [[HTML]] (1)
> **CLI Commands:** php (9), make (1)
> **Env Vars:** php (7), html (1)
> **Definitions:** is a  (2), means that (1), known as (1)
> **File Paths:** index.php (2)
> **Warnings:** warning (2)
> **Speakers:** - [instructor] (1)

#### [Try/catch statements (update for PHP 8.0)](https://www.linkedin.com/learning/learning-php-4/try-catch-statements-update-for-php-8-0?u=76281980)

#### [Breaking changes moving from 7.x to 8.0](https://www.linkedin.com/learning/learning-php-4/breaking-changes-moving-from-7-x-to-8-0?u=76281980)


### 8. Processing Forms

[↑ Back to Table of Contents](#table-of-contents)

#### [GET vs. POST](https://www.linkedin.com/learning/learning-php-4/get-vs-post?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/get-vs-post?u=76281980&t=0)** - [Instructor] Processing [[Forms]] is one of the most common things you'll probably do on your website in [[PHP]]. But before we get into actually processing the forms, there are two methods for processing that you should know about, GET and POST. This is associated with the method attribute in [[HTML]], which determines how the form data should be sent. GET is the default option, this method transmits data from one page to another as part of the URL. So if I fill out this form, and then press Submit, you'll see that all of the data I just filled out shows up in the URL up here. I've also created the page so that it prints out the resulting array. To retrieve this information, you would need to use what's called a superglobal variable in PHP, and that's the GET array. This superglobal has all of the information passed to our action page, which since it's not defined defaults to the same page the form is on, it's passed as an associative array, which you can see here. The array has name, which is the name of the first form field and then the value, the same thing with email and reason. You'll also notice that topics which are checkboxes and the movies, which is the multiselect box are passed as arrays as well. So this is a multi dimensional array.
>
> **[1:34](https://www.linkedin.com/learning/learning-php-4/get-vs-post?u=76281980&t=94)** Finally, you'll notice that the submit button is also passed. The other method is POST so if we change this method to POST and then let's add another condition up here, we'll say if not empty POST, var dump POST and then we will remove all of this,
>
> **[2:02](https://www.linkedin.com/learning/learning-php-4/get-vs-post?u=76281980&t=122)** refresh and if we fill it out again and press Submit. Now it's the POST array that's printing. You'll notice that none of the information is passed in the URL here. So how do you choose which to use and when? With GET, the data is entered by the user and visible in the URL. So you should never pass sensitive data like passwords, you also shouldn't pass data that can be tampered with. So if I do something like, question mark hello equals there that is now in the get array. Using get and therefore allowing form data to be visible and mutable is great if you want to make the results shareable or if you want to let the user save them. But on the other hand, the length of a GET request is limited the limit can range between 2000 and 8000 characters depending on the server and browser configuration. There is no limit on results when using POST and it's more secure, but the results cannot be shared nor can the result of a specific form submission be saved without extra processing. So with that, let's get into processing some forms.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Forms]] (3), [[PHP]] (2), [[HTML]] (1)
> **Env Vars:** post (7), url (4), php (2), html (1)
> **API Endpoints:** get  (4), post
 (3), post  (2)
> **CLI Commands:** php (2), make (1)
> **Definitions:** is a  (1)
> **Best Practices:** you should never (1)
> **Speakers:** - [instructor] (1)

#### [Handling basic form elements](https://www.linkedin.com/learning/learning-php-4/handling-basic-form-elements?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/handling-basic-form-elements?u=76281980&t=0)** - [Instructor] There are all sorts of form elements you can use to capture input from users. So what exactly do we mean by basic elements here? Essentially, a basic element is any element that sends a single value for the field. In other words, most elements. We'll go through some of the most common form fields in this section but it will not cover all elements. If you're ever unsure, be sure to use var dump or print R to see what's going on. Speaking of, let's take another look at the post array from earlier. You can see that it's an associative array and the keys match the name of the form field. So if we go back to our code, you'll see that our method is post, and now we have a new action attribute here. The action attribute is the page that should process the form. And I've created a new file called process.[[PHP]] here to make the form processing a little clearer. In order to process the basic form elements, we can just treat the post array as any other associative array. So on this page, I'm going to add a foreach loop. We could say foreach post as name value.
>
> **[1:29](https://www.linkedin.com/learning/learning-php-4/handling-basic-form-elements?u=76281980&t=89)** And then we can print out a paragraph for each element. So I'm going to say P and then I'll bold the name. So B name is value. And then close the paragraph. Now, if we want to be a little fancy, we can use a function called ucfirst here. We'll have to escape out of this. Ucfirst, and this is a built-in function that will make the string's first character uppercase since the convention for form field names is usually all lowercase. Okay, so we'll do that. And then we'll save this. We'll go out to our form and we'll fill it out. Joe, email address. I'll say consult. And I'll say hello there. Now I'll click submit. And we're now on the process page. And name is Joe. Email is joe@[casabona.org](https://casabona.org). Reason is contact. Messages is hello there. But there's also the submit. Remember that the submit field and all fields in the form, hidden or otherwise, are going to be included in this. So there's a simple check that we can do where we say if submit does not equal name. And then we can print out the results. Get rid of that space.
>
> **[3:06](https://www.linkedin.com/learning/learning-php-4/handling-basic-form-elements?u=76281980&t=186)** Okay, and now if we refresh the page, we'll resubmit the form and the submit button is no longer there. So the keys match the names of the form fields and the values are whatever the user filled in on the form. And this includes all form fields, including the submit button and any hidden. So with our simple form built, let's get a little bit more complicated and look at form elements that allow more than a single value for the answer.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (1)
> **CLI Commands:** make (2), php (1)
> **Definitions:** in other words (1), is a  (1)
> **File Paths:** process.php (1)
> **URLs:** [casabona.org](https://casabona.org) (1)
> **Cross-References:** go back to (1)
> **Speakers:** - [instructor] (1)

#### [Handing multiple-choice elements](https://www.linkedin.com/learning/learning-php-4/handing-multiple-choice-elements?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/handing-multiple-choice-elements?u=76281980&t=0)** - Each of the elements and most of the elements that you've seen on the forums so far have only had one value associated with them. So, the input was pretty easy to grab, but what about elements that allow multiple values like checkbox and selects? Let's take a look at how they work. First of all, you could see here that we've added the checkboxes for what topics are you interested in? We've named them topics, and we have a multi-select box for movies. What's your favorite movie or movies? And it's all of the 'Star Wars' movies. And we've named that movie. So, let's see what happens if we just throw this form through the processor we previously used, which was for a reminder, for each loop that assigns each key value pair as name and value. So we'll go ahead and fill this out. I'll select [[PHP]] and [[WordPress]], and then I'll select Star Wars IV and V, and I liked VII. And then I'll add the message, "Hello There!" Then I'll click Submit, and you could see that the form did process, but if we look at Topics, PHP is missing and so are the other 'Star Wars' movies I selected besides Star Wars, episode seven.
>
> **[1:37](https://www.linkedin.com/learning/learning-php-4/handing-multiple-choice-elements?u=76281980&t=97)** Because PHP inherently treats these fields as a string. It's the last value selected that wins. Much like when you override the value of a variable, it's the last assignment that the variable gets as the value. So how do we fix that? Well, if we go back to our form, we can look at the checkboxes here and we can add square brackets to the name of these form elements. So I'll do that for topics, and I'll do that for movie. This converts these form fields into an array, which PHP can then process. So if we go back to our form and refresh and we fill it out again, I'll select two options this time. And then I'll click Submit. And now the results are a little bit different, topics and movies are just array. And we're getting a notice about an array to string conversion. So there's one more thing that we need to do in our processor. And that is, take the result of the array and print them out as a string. So, let's go ahead and do that now. Starting on line five inside the if submit does not equal name, we'll add another if statement.
>
> **[3:11](https://www.linkedin.com/learning/learning-php-4/handing-multiple-choice-elements?u=76281980&t=191)** We could say, if is_array(), which is a built in PHP function, and then we'll pass a value. We might want to do something different with it. We'll say value gets, and then we'll use another built in PHP function called implode. This function is really nifty, it takes an array and converts it to a string. So the first argument is how we want to separate each element in the array and we'll use a comma and a space here, and then it wants the array. So we'll send value to it. So now we have converted value to a string. Looks like we're missing a parenthesis here. And then we proceed with printing out the name and the value. So since the form values haven't changed, just the processor, we can refresh the page and now our topics print out as a string. And the movies we selected, printed out as string. So with that, we are ready to take any of the data that is filled out on our form and do something with it. So far, we've only printed it out on the screen, but there are other things we can do. Like email it or save it to a database, but we will need to validate that information first.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (6), [[WordPress]] (1)
> **Env Vars:** php (6), vii (1)
> **CLI Commands:** php (6)
> **Cross-References:** go back to (2)
> **Code Identifiers:** is_array (1)
> **UI Navigation:** checkbox (1)
> **Definitions:** is a  (1)
> **Speakers:** - each (1)

#### [Form validation: Making sure required fields are filled in](https://www.linkedin.com/learning/learning-php-4/form-validation-making-sure-required-fields-are-filled-in?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/form-validation-making-sure-required-fields-are-filled-in?u=76281980&t=0)** - [Instructor] Form validation is mostly handled with [[HTML]] and [[JavaScript]] these days. But it's still important to make sure you're getting proper data on the backend because people could have JavaScript turned off or they could be using an older browser or the browser validation might not validate the same way you want to validate. So the first way to validate on the backend is to make sure all required fields are filled out and we're going to do this in line so that we can give the user the proper message right next to the form field instead of making them go to a different page, telling them the form wasn't right and then having them have to go back to the page. So I have removed the action here and then you'll also notice that we have the HTML5 attribute for required. So it's going to take a little bit of trickery to make sure that we are getting test data that we can make sure is valid. So the first thing I'm going to do here is right above the label for the first form element that's required, which is the name, I'm going to write some in-line [[PHP]] here and I'm going to say if isset, POST name
>
> **[1:25](https://www.linkedin.com/learning/learning-php-4/form-validation-making-sure-required-fields-are-filled-in?u=76281980&t=85)** and we will add a parentheses here just to make sure everything is being processed properly. And empty POST name. And we'll close our parentheses. We could say echo p class alert. We'll escape that. Name is required. And p. And now, this is a valid PHP statement. The if statement is all on one line, so we don't need the curly braces. If you want to make it a little bit more readable, we can definitely do that by adding the curly braces. And just making this a multi-line statement and that is a bit easier to read. So we'll do that just to make it formatted and easier for us to glance at. Okay, so let's test just this out first. If I refresh the page and try to submit, we get a validation from the browser. So the only way to test this would be to remove the required attribute but that's not really a real-world solution. We want to keep everything that HTML has to offer us, we just want to add an extra layer of PHP. However, let's go ahead and fill out this form where we make the name two spaces and then fill out everything else as required.
>
> **[3:04](https://www.linkedin.com/learning/learning-php-4/form-validation-making-sure-required-fields-are-filled-in?u=76281980&t=184)** If we submit, the form did properly submit there. And just to make sure that something actually is happening on the backend, let's go ahead and add our processing from the process.php file so that we could see the results. I'll save that. I'll go back and refresh. And here's our form data. You could see name is just two spaces. And while that's technically not blank, it's not a value either. So the way that we can fix that in PHP is to say is it empty after we use the trim function, which will remove space at the beginning and end of the string. So now, if we save this and go back to our browser. Let's go ahead and fill this in again with two spaces.
>
> **[4:04](https://www.linkedin.com/learning/learning-php-4/form-validation-making-sure-required-fields-are-filled-in?u=76281980&t=244)** And if we submit, now we get the error message name is required. So we actually don't want to do anything if a required field is not properly filled out. So we'll do one more check up here. And we'll say at the beginning, form_complete gets null. And then in the if statement, we'll say form_complete gets false. So now, we can choose to process this foreach loop if and only if form_complete is true.
>
> **[4:53](https://www.linkedin.com/learning/learning-php-4/form-validation-making-sure-required-fields-are-filled-in?u=76281980&t=293)** We'll indent all of this. So this is almost right but form_complete is null here and there's never a point before we get to the processing where form_complete can become true. So what we'll do is copy our if statement here and we'll leave form_complete null at the top. And then at the bottom of our file here, we can add our processing. And we can set form_complete to true if all of our checks worked out. And we could do that by using the null coalescing operator. So we could say if form_complete is not null, use form_complete. Otherwise, set everything to true and now we have a form processor. So let's see if that works. Let's go back to our blank form. We'll fill this out with two spaces. We'll fill out the [[Representational State Transfer (REST)|rest]] of the form. Then we'll click Submit. Name is required and the form is not processed. Fantastic. So now let's go ahead and add the other checks. The email check is going to be pretty hard for us to test because the email field actually makes sure that it's an appropriate email address. So we can't just put two spaces in there to test but the check will still be good.
>
> **[6:27](https://www.linkedin.com/learning/learning-php-4/form-validation-making-sure-required-fields-are-filled-in?u=76281980&t=387)** Again, just in case the user is using an old browser. And then finally, message is the last required field. So we'll add that here too. If isset message and trim message is empty, then we know that they just put in blank spaces. Okay. So that's how we validate our form for required fields. But what if we want to make sure the actual data is correct? Let's take a look at that now.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (5), [[HTML]] (2), [[JavaScript]] (2), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** make (10), php (5)
> **Code Identifiers:** form_complete (9)
> **Env Vars:** php (4), html (2), post (2), html5 (1)
> **Cross-References:** go back to (3)
> **API Endpoints:** post  (2)
> **Definitions:** is a  (2)
> **File Paths:** process.php (1)

#### [Form validation: Appropriate input](https://www.linkedin.com/learning/learning-php-4/form-validation-appropriate-input?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/form-validation-appropriate-input?u=76281980&t=0)** - [Instructor] Another type of validation you'll want to do on the server is making sure that data submitted is appropriate data. For example, you'll want to make sure the email address is an actual email address. Luckily, browsers do a lot of this for us now if we use the right input type. For example, the email input type will validate an email address before the form is submitted. But this is still worth knowing how to do. The way you'd accomplish this is by comparing the input to a description of what the input should look like. These descriptions are called regular expressions or RegEx for short. Now, diving deep into RegEx is a bit beyond the scope of this course, but I do want to show you an example of what that might look like. First we have our regular expression or a description of an email string. Now this looks complicated, but it's basically searching for any alphanumeric string of any length, followed by an at symbol, followed by another alphanumeric string with at least one dot. We're going to take this RegEx and add it to our form validation. So, we'll add our regular expression validation here with our empty email validation. The first thing we'll do is create a variable called, email RegEx and we'll add that regular expression. Then, we'll add an else if and our condition
>
> **[1:40](https://www.linkedin.com/learning/learning-php-4/form-validation-appropriate-input?u=76281980&t=100)** is going to use the function P reg match. So I'll say not P reg underscore match. And this is a function that accepts a pattern or regular expression and then a string that it should search on the regular expression. So we'll add email RegEx and then we'll add post email. If this doesn't find a match, it will return false. So we're going to check for the not operator here. And if that's the case, then we'll add an echo statement that says, please enter a valid email address.
>
> **[2:32](https://www.linkedin.com/learning/learning-php-4/form-validation-appropriate-input?u=76281980&t=152)** And we'll set form complete to false. Now this is a little bit trickier to test with our email type because that's got a pretty rock solid check, but let's go ahead and try it out. We'll refresh our page here. And it says we've got an undefined index. So it's already doing the check. We need to add one more check here and we need to add the is set, we forgot to add that here. Else if, is set, post email and P reg match. Okay, so I'm going to try to enter an invalid email.
>
> **[3:18](https://www.linkedin.com/learning/learning-php-4/form-validation-appropriate-input?u=76281980&t=198)** And the [[HTML]] field is telling us we need an at symbol. So I'll just try joe@casabona and submit. And actually, that's perfect. It doesn't check for the top level domain, which our regular expression does. So the form is not submitted because it's not valid. So, now we'll add our valid information in here. Click submit and our form submits properly. Now again, modern browsers will do a lot of this for us, but it's good to know when you want to double or triple check or in the case of the email address, it looks like it doesn't fully validate the way we want to. So it's good to have that other check. If you want to learn more about regular expressions, check out the learning regular expressions course in the [[LinkedIn]] Learning library.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[HTML]] (1), [[LinkedIn]] (1)
> **Definitions:** is a  (3), is an  (1)
> **CLI Commands:** make (1), find (1)
> **Analogies:** for example (2)
> **Env Vars:** html (1)
> **Speakers:** - [instructor] (1)

#### [Form validation: Sanitizing input](https://www.linkedin.com/learning/learning-php-4/form-validation-sanitizing-input-8400185?u=76281980)

#### [Sending the form data in an email](https://www.linkedin.com/learning/learning-php-4/sending-the-form-data-in-an-email?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/sending-the-form-data-in-an-email?u=76281980&t=0)** - [Instructor] Once you have your data nice and valid, it's time to do something with it. A common action is sending it via email somewhere. Let's take a look at how to do that now. For simplicity's sake, again, we're going to use a blank process.[[PHP]] file, and we're going to assume all of our data has been validated and sanitized. We want to focus on using the mail function in PHP right now. And the easiest way to send email via PHP is the mail function. The mail function accepts several arguments, but the first three are required. It's a two email address where we are sending the message, a subject line, and then the actual body of the email. So let's look at those first. First, let's put together the message, and we're actually going to use some code that we saw in a previous video. You can find this code in the eight three exercise files. But this is going to do everything we need it to do except instead of echo, we're going to create a new variable called message. We'll make it blank for now. And then we will use our concatenation assignment. So we'll say message.gets. And let's remove the [[HTML]] from this message,
>
> **[1:36](https://www.linkedin.com/learning/learning-php-4/sending-the-form-data-in-an-email?u=76281980&t=96)** because certain mailers don't necessarily handle HTML very well. Instead, we will add our line breaks in escape characters. So now we've put together our message, let's go ahead and actually put together the [[Representational State Transfer (REST)|rest]] of the email. We know that our to field is going to be pretty static, it's going to go to us. So we're going to hard code that. I'm going to put my full name, and then in angled brackets, the email address I want this message to go to. The subject is going to be the radio buttons that we have here, Reason for Contact. So we could say subject, reason for contact:, and then we'll concatenate this, POST reason.
>
> **[2:43](https://www.linkedin.com/learning/learning-php-4/sending-the-form-data-in-an-email?u=76281980&t=163)** With these three items created, we can use our mail function. So let's go ahead and do that. We'll say mail to subject and message. Now, this is all we would need to make sure that the mail actually sends, but let's say we want to print something to the user to let them know their email's sent. The mail function returns true or false based on the success of the mail sending. So we could say if mail to subject and message, then echo "<h3>" Your Message has been sent. End h3, add our semi-colon, we'll make this grammatically correct. And then we'll save this. So this code is the basics for how we would send an email, and let's see if that works. I'll use different contact information here. We'll say Question, PHP and [[WordPress]], Star Wars, and Hello There. Then we'll click Submit. Your message has been sent. And to double check that, we can look at my inbox. So here's the message that was sent. Reason for contact question to the right email address. Name is, we forgot a space in between our field names and our values, but that's okay,
>
> **[4:18](https://www.linkedin.com/learning/learning-php-4/sending-the-form-data-in-an-email?u=76281980&t=258)** because everything else sent fine. However, you'll notice that the from address doesn't look great. And we can actually fix that using the additional headers area. If we scroll down on this manual page, we'll see exactly the code we need for that. So you could see here in example two sending mail with extra headers, there's some extra information here. And the additional headers accepts any headers that could be sent via email. So there is a wide array of options we can use here. But we're going to copy this verbatim and then swap out the important information. We want to have a from address that is not a server email address, and then the reply to address, which we also want to make sure it's set correctly. And then finally, we're going to tell the email client that PHP, specifically our version of PHP, sent this email. So we'll copy this and we'll go back into our code. And then we'll add a section for the headers. So the from and reply to email addresses are going to be based on the information filled out via the form. So we will create a new variable called from, and we'll fill this out properly. We'll say POST name, and then we'll add our opening angle bracket, and then POST email,
>
> **[5:57](https://www.linkedin.com/learning/learning-php-4/sending-the-form-data-in-an-email?u=76281980&t=357)** and then our closing angle bracket. And then we can replace the text here. So this will be from, .from, and same thing here, from .from. Now let's also fix our spacing issue here. And then make sure we add the additional headers to our mail call. Okay, let's go ahead and send this again and see how it goes. We don't need to fill out the form again, we can just refresh this page and resubmit the form. Our message has been sent. And if we go back into MailHog, now the from address is proper and our message has been fixed. One thing to think about when you are sending email via your server is server reputation. Certain services like Gmail might send these emails to spam if they don't think it's coming from a reputable source. Using additional headers, which are any valid email headers, might help. So it's worth reading up on the PHP manual page to see if you're having any trouble.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (7), [[HTML]] (2), [[Representational State Transfer (REST)|Rest]] (1), [[WordPress]] (1)
> **CLI Commands:** php (7), make (5), find (1)
> **Env Vars:** php (7), post (3), html (2)
> **API Endpoints:** post  (3)
> **UI Navigation:** go to (2), scroll down (1)
> **File Paths:** process.php (1)
> **Cross-References:** previous video (1)
> **Exercise Files:** exercise files (1)

#### [Challenge: Process a simple form and send the email](https://www.linkedin.com/learning/learning-php-4/challenge-process-a-simple-form-and-send-the-email?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/challenge-process-a-simple-form-and-send-the-email?u=76281980&t=0)** (bright music)
>
> **[0:05](https://www.linkedin.com/learning/learning-php-4/challenge-process-a-simple-form-and-send-the-email?u=76281980&t=5)** - [Instructor] All right, now it's your turn. Build an email form from scratch including these fields. Name as a required field, email as a required field, reason for contact as a select box, which is also required, and message, which is also required. Then, validate the form. Make sure the required fields are filled out and the email is in a valid format and email it to yourself. You can definitely use the [[HTML]] form elements to help you, but be sure to validate on the server, too.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[HTML]] (1)
> **CLI Commands:** make (1)
> **Env Vars:** html (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright music) (1)

#### [Solution: Process a simple form and send the email](https://www.linkedin.com/learning/learning-php-4/solution-process-a-simple-form-and-send-the-email?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/solution-process-a-simple-form-and-send-the-email?u=76281980&t=0)** (bright music)
>
> **[0:05](https://www.linkedin.com/learning/learning-php-4/solution-process-a-simple-form-and-send-the-email?u=76281980&t=5)** - [Instructor] All right, time to look at the solution. Now we're not going to type this one out line by line because there's a lot to cover and you can get this entire working file in the exercise files, but there are a few things worth pointing out. First, I assign all of the data to a new array called form data just to keep it safe and sound in case something happens to post. Not totally necessary, but a practice that I started doing a long time ago. Second is we have our form is complete variable. Now we could set it to null but that's going to cause a lot of issues. So what we're doing first is we're seeing if the form has been filled out at all. If empty post, then the form is not complete and we can set it to false. However, if the form has been filled out, we'll set this to null with the understanding that during our required checks, the form is complete, variable could be set to false. If it's never set to false, then it must be true. Then we have two functions. One is check required and it gets a value and the other is check email and it gets an email address. Both of these are validation functions. Check required, make sure that after we trim our string, there's still a value in it. If there's not, we grab the global variable form is complete and set it to false. Similarly, with check email, we have our rejects. We make sure that the email address is in a proper format
>
> **[1:41](https://www.linkedin.com/learning/learning-php-4/solution-process-a-simple-form-and-send-the-email?u=76281980&t=101)** and if it's not, we grab the global form is complete variable and set it to false. If after these functions run, form is complete is never set to false, then we have a fully filled out and valid form. We're going to skip over send message for a minute and just check out the form that we have. Our error messages are in line. Our check required and check email are also in line. We're using the proper input types and the required attributes and again, we're checking the message, which is also required. Now at the bottom here, we're checking to see if form is complete is null. If it's not null, then use the current value, which should be false. If it is null, then we have a fully valid form and we'll set it to true and once we do that, we can send the message. Now I'm not using the null coalescing operator here or the Elvis operator because I am on a slightly older version of [[PHP]] on my local environment and those two operators are four later versions of php, so I want to make sure that this was fully working no matter what version of php you happen to be using. Now let's go back to the send mail function. So it accepts an array of data and then it sanitizes everything. So for name, we trimmed the name, we stripped the tags
>
> **[3:17](https://www.linkedin.com/learning/learning-php-4/solution-process-a-simple-form-and-send-the-email?u=76281980&t=197)** and we replaced the quotes with escaped quotes. With the email address, we know it's technically a valid email address already, but we filter to make sure it's actually proper. We set our two fields. We set our from field. We don't need to sanitize reason because it is a select box. Technically we probably should because if somebody tampers with the [[HTML]] before the information is sent, they could insert malicious scripts, but we don't here. We set our subject. Then we set our message. We strip tags, trim it and we escape the quotation marks. Then we put together our message with the email address information, the reason and the message, we put together our headers and we send the email. So this is a fully working form with lots of validation built in. Again, you can find this over on the exercise files.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (3), [[HTML]] (1)
> **CLI Commands:** make (4), php (3), find (1)
> **Exercise Files:** exercise files (2)
> **Definitions:** is a  (2)
> **Cross-References:** go back to (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (bright music) (1)


### 9. Advanced Topics

[↑ Back to Table of Contents](#table-of-contents)

#### [Reading files in PHP](https://www.linkedin.com/learning/learning-php-4/reading-files-in-php?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/reading-files-in-php?u=76281980&t=0)** - One of the ways to store and access data though it should absolutely be non-secure data, is by reading and writing to files. [[PHP]] allows you to read the contents of a file and if you have access to it, write to it as well. So first let's look at a couple of examples of reading files. On the screen here you see I have a file called treasure-island.txt, with a selection of lines from the Robert Louis Stevenson novel. There are two ways that we can read this file. The first is using a built-in function called readfile. So I can say readfile and then give it a file name, treasure-island.txt. This function simply grabs the contents of a file and puts it into the output buffer which is in most cases, immediately printed out onto the screen. Let's refresh our page here and see the results. You can see it's the entire file. And if we View Source, there is no formatting to it but it did break it up by lines. We just don't see that in the browser. But in case you don't immediately want to print out the results of a file on screen, there's actually another function that can help us. And that is file_get_contents, which reads the entire file as a single string,
>
> **[1:38](https://www.linkedin.com/learning/learning-php-4/reading-files-in-php?u=76281980&t=98)** stripping out any formatting and line breaks. So again if we save this, there's nothing on the screen here, we'd actually have to echo it. So we can store the file contents into a variable using get file contents. Perhaps the best way to get the contents of a file is with the file function. Which will read the entire file as an array where each line is a new entry into the array. So we can say lines, get file, and then the name of the file. If we do a simple var_dump here, we could see that each line gets its own entry in the array. So you can loop through this with a for each loop if you would like. But this is for more than just output. And it could be more than just files accessible on your server. This could be for any text file including web pages. So let's look at another example. I'll comment out var_dump here so that we can better read what we're about to do. So let's make a variable called casabona and we'll use file_get_contents. But this time, we'll call a web page. We'll call my homepage. Then if we var_dump casabona, you can see we've retrieved the entirety of the [[HTML]]
>
> **[3:18](https://www.linkedin.com/learning/learning-php-4/reading-files-in-php?u=76281980&t=198)** from my homepage. Now, this could be useful if I want to see if a site for example is using [[WordPress]], I could say if and then use a string search function called strpos for string position. And then I could search the text casabona for the text wp-content, which is an indicator that WordPress is being run and then I can echo, this website is using WordPress. So let's refresh the page now. This website is using WordPress. There can be countless applications for this. But just remember not to violate any copyrights by stealing or scraping content you don't have permission to do so on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[WordPress]] (4), [[PHP]] (1), [[HTML]] (1)
> **Code Identifiers:** var_dump (3), file_get_contents (2)
> **File Paths:** treasure-island.txt (2)
> **CLI Commands:** php (1), make (1)
> **Env Vars:** php (1), html (1)
> **Definitions:** is a  (1), is an  (1)
> **Analogies:** for example (1)
> **Speakers:** - one (1)

#### [Writing files](https://www.linkedin.com/learning/learning-php-4/writing-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/writing-files?u=76281980&t=0)** - [Instructor] Along with reading from files, you can write to files. In this case, you can only write to files you have permission to write to. So this is great if you want to store information, but don't necessarily have access to a database. On the outset here, I'll say that depending on where you live, there may be different laws governing how you store data. If you're going to store user data, make sure you comply with the appropriate laws. Okay, with that disclaimer out of the way, let's take a look at how to write to files. The simplest way would be to use the file put content function, which will open a file, dump the text and return the number of bytes written. So let's say we want to make a copy of our "Treasure Island" file. We could say text gets file get contents.
>
> **[0:57](https://www.linkedin.com/learning/learning-php-4/writing-files?u=76281980&t=57)** And then the treasure island file. And then we can say echo. So we have something to look at. It'll be the number of bytes. File put contents. The name of the file, we'll call this ti copy.txt and the text. Now you'll notice that we don't have a file called ti copy. File put contents will create the file if it doesn't exist. So let's go out to our browser, refresh the page. 1963 is the number of bytes that was written and we have a new file called ti copy. By default file put content will also overwrite whatever is in the file if it exists. But the function could also append with the file append flag. So if I add FILE APPEND to the end of this function, then we are telling [[PHP]] to just add the text to the bottom of the document. So now ti copy should be twice as long. This can be really helpful if you want to create a mailing list. So let's open a file I have here called mailing.php you'll see that we have a simple form for name and email. We'll assume that we are doing all of the checks
>
> **[2:31](https://www.linkedin.com/learning/learning-php-4/writing-files?u=76281980&t=151)** and sanitizing, but in our join mailing list function, we have a new section for writing to a file. We have the entry with the user's name, a comma, and then an email, and then the escape character for new line. This is really important because we want to make sure that all of our data doesn't run into each other on a single line. Then we have a file put contents, mailing-list.txt, the entry and FILE APPEND. Again, FILE APPEND is going to be really important here because we don't want to overwrite the contents of the file with just the latest person to sign up for the list. We want to keep this list going. So if I visit mailing.php and I sign up for the list and then click submit, we have a new file mailing list with my name, comma email address. The other way to write files is going to take a few more steps. You need to first open a file with fopen, then write to the file with fwrite then close with fclose. In fact file put contents wraps these three steps into one. So why use the other method? Well, if you're continuously writing to a file, it would be better to keep it open until you're done. Here's a simple example of that.
>
> **[4:04](https://www.linkedin.com/learning/learning-php-4/writing-files?u=76281980&t=244)** Let's say I want to take my mailing list and make a separate list of email addresses from anybody using an [adcasabona.org](https://adcasabona.org) email address. For that we'll have to beef up our mailing list of text file, so let's do that first. Okay, so now we have a lot of email addresses. You can see we're not validating to make sure that we don't have double entries, but that's okay. Because what we really want is a lot of email addresses with several from [casabona.org](https://casabona.org). So we'll save this, we'll go back to our index file and then we'll start the list. We'll say list gets file mailing list. So this is simply going to read our mailing list text file. Then we need to create a new file that we want to write to. So we're going to call this F handle. Handle is just the term that's been used in programming languages to talk about the current file that's open. So we have file underscore handle, we use fopen, and then we want to open a file called sub-list.txt and we want to append to it. Let's go to the PHP manual for a minute here, because this is slightly different from file put contents. The second argument for fopen is mode, and there are several modes to know about. R for example, is reading only
>
> **[5:37](https://www.linkedin.com/learning/learning-php-4/writing-files?u=76281980&t=337)** r plus is for reading and writing where the file pointer is placed at the beginning of the file, no matter what. W is for writing only and so on. A is for writing only and places the file pointer at the end of the file. If the file doesn't exist, attempt to create it. Then there's a plus is for reading and writing. And the file pointer is added to the end of the file. So we're going to use the a mode here. That's why we needed to check the manual. So now we have our file handle and our list of email addresses. Let's write a for each loop, we'll say list as entry. Now, since each line is just a comma separated list with the name and email, we can do a string search. So we'll do str pose on entry and we'll look for [casabona.org](https://casabona.org). And if that's found, then we will do an fwrite to our file handle with entry. The new line characters will actually already be included in the entry. So once we go through that entire list, we'll want to close this file with fclose and file handle. This is really important because if we don't close it, we might run into memory issues.
>
> **[7:10](https://www.linkedin.com/learning/learning-php-4/writing-files?u=76281980&t=430)** So let's save this. We'll go back to our index. We'll refresh this. We don't have any outputs, but if we look at sub lists, we'll see the four email addresses from mailing list that are from [casabona.org](https://casabona.org). Much like with reading files, when writing, make sure you're working with data you're allowed to work with and that you're not violating any laws or copyrights.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (4)
> **CLI Commands:** make (6), php (4)
> **Env Vars:** file (3), append (3), php (2)
> **File Paths:** mailing.php (2), copy.txt (1), mailing-list.txt (1), sub-list.txt (1)
> **URLs:** [casabona.org](https://casabona.org) (3), [adcasabona.org](https://adcasabona.org) (1)
> **Cross-References:** go back to (2)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)

#### [Maintaining state with cookies](https://www.linkedin.com/learning/learning-php-4/maintaining-state-with-cookies?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/maintaining-state-with-cookies?u=76281980&t=0)** - [Instructor] Cookies are a great way for [[PHP]] to remember the state of a user's browser. This can be anything for any information like their name or if they're logged in. See by default, PHP doesn't maintain state. so if you move from page to page, unless you do something to explicitly remember what a user did on the last page, PHP will not do that automatically for you and cookies persist as long as the browser stays open. If you've ever had a website remember some setting, like being logged in, that website was likely using a cookie. To set a cookie in PHP, you would use the function set cookie. This accepts a name, a value and an optional expiration. So let's create a cookie that tells us if the user has visited the site before. We'll set this to true and we won't set an expiration date. So we'll just let it stay as long as the browser stays open. To check that cookie, we would use the cookie super global array. So let's say you want to print a message to the user if they have already visited the site. We could say if is set $_cookie visited
>
> **[1:27](https://www.linkedin.com/learning/learning-php-4/maintaining-state-with-cookies?u=76281980&t=87)** then and we'll use the alternative syntax here. We could say welcome back. If they are new visitors, then we can use the alternative syntax again and say welcome. Okay, so now if we refresh this page, we see welcome back because that cookie was set. If I open this page in a private browser, we see welcome and then if we refresh, we see welcome back. So everything here is working as expected. If later you want to delete the cookie, you would use the same set cookie function with an expiration date for the past. So let's on the bottom of this page, destroy the cookie we'll say, set cookie visited.
>
> **[2:32](https://www.linkedin.com/learning/learning-php-4/maintaining-state-with-cookies?u=76281980&t=152)** We'll give it any value 'cause we're going to expire it. Time - 3600 so this is just the current time minus some number of milliseconds. So if we refresh, you'll see we get a warning here, cannot modify header information. This is important, we can't set cookies after information has been printed on the screen. So if we want to set a cookie we need to do it before anything is printed on the screen. So let's say if it's set cookie visited, maybe we only want to show the welcome back message once. We can immediately destroy the cookie. So now if we refresh, we'll see just the welcome message. Sending cookies is great if you want to remember some sort of information but you definitely need to do it before you print any information out on the screen. The drawback of cookies is that they are accessible on a user's computer. So they can temper with the cookies if they want If I go in here and I look at the visited cookie, I can block or remove it . Another way to maintain state that can't be tampered by the user is sessions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (4)
> **CLI Commands:** php (4)
> **Env Vars:** php (4)
> **Warnings:** warning (1)
> **Speakers:** - [instructor] (1)

#### [Maintaining state with sessions](https://www.linkedin.com/learning/learning-php-4/maintaining-state-with-sessions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/maintaining-state-with-sessions?u=76281980&t=0)** - [Instructor] Cookies are great for maintaining state, but one problem with this is they're on the user's computer. That means the user might be able to modify the value of a cookie. That's fine if you're storing simple data like their name or if they're logged in, but what if you're storing something like shopping cart data or other information that shouldn't be modified by the user? That's where sessions come in. Sessions like cookies expire when the browser closes, but the sessions are stored on the server, which users can access. Another benefit of sessions is they can be set at any time, unlike cookies, which need to be set before anything is output onto the screen. So for our sessions example, let's modify our mailing list. Let's say we only want to show this form to people who haven't signed up for the mailing list. That way, if people who have signed up visit the page, they won't be able to sign up again. So the first thing we'll do in our mailing.[[PHP]] file, which you can get in the exercise files, is at the very top of the page, we're going to write session start. This is going to tell php that we're starting a new session for this browser and to store information where you can. Now in our join mailing list function, we're going to add at the bottom,
>
> **[1:35](https://www.linkedin.com/learning/learning-php-4/maintaining-state-with-sessions?u=76281980&t=95)** after everything is printed out, a new session variable. So we're going to use the session super global array for this, very similar to the cookies super global array. So we'll say dollar sign underscore session. We're just going to set a new key here called signed up and we're going to set it to true. So now we've set a new sessions variable after the user signs up. Now we can do this. We can say at the top of the join mailing list header if is set session signed up and so we're going to make sure this is set and it's actually true and actually we want it to not be true. So and if it's set and it's not true, we're going to show the form. Again, we're going to use alternative syntax for this because we are writing a large block of [[HTML]]. Okay, so this says if is set session signed up and not session signed up, which means it's not true, then show the form. So we'll save this. We'll go back to our mailing list form. Our mailing list form has disappeared and that's because we're checking to see
>
> **[3:09](https://www.linkedin.com/learning/learning-php-4/maintaining-state-with-sessions?u=76281980&t=189)** if the session variable has been set, which it hasn't. So we can actually say if not is set or not session signed up, so this is going to say, if the session variable signed up has not been set or it doesn't evaluate to true, print the form. So let's go ahead and fill this out. We'll click submit and now it says thanks we're on the list and the form is still showing up, but that is because we set the session after we print this message, so if we refresh the page, the form is gone. So that's how we use sessions. If at any point you want to destroy a session, you can use the session destroy directive, like this. This will kill the session and any data associated with it. So with that, you have a basic understanding of how sessions work. Sessions and cookies are both very useful for temporarily saving data, but if you want something more long-term, you'll need to use a database or something similar.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (2), [[HTML]] (1)
> **CLI Commands:** php (2), make (1)
> **File Paths:** mailing.php (1)
> **Env Vars:** html (1)
> **Cross-References:** go back to (1)
> **Exercise Files:** exercise files (1)
> **Analogies:** similar to (1)
> **Prerequisites:** you'll need (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/learning-php-4/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-php-4/next-steps?u=76281980&t=0)** - [Joe] Congratulations, you've gotten a good look at [[PHP]] and how it works along with some nifty tricks. [Php.net](https://Php.net) will serve as a great resource for you as you continue learning PHP. Speaking of, if you want to continue your PHP journey, you should check out the "Become a PHP Developer" learning path. If you want to try your newly found PHP skills on a very popular CMS, you can check out the [[WordPress]] 5 Essentials Training. Finally, I'd love to hear from you about how you're using PHP. You can always find me on Twitter @jcasabona. If you'd like to learn from more developers and industry experts, check out my podcast, "How I Built It" over on howibuilt.it. Thanks so much for watching, and until next time, get out there and build something.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[PHP]] (7), [[WordPress]] (1)
> **CLI Commands:** php (7), find (1)
> **Env Vars:** php (6), cms (1)
> **URLs:** [php.net](https://php.net) (1)
> **Speakers:** - [joe] (1)


## Resources

- Exercise files available

## Skills Covered

- PHP

## Path Context

### In [[Get Started with PHP]]
**1 of 6** | [[PHP Essential Training]] →

## Appears In

- [[Get Started with PHP]]

## Related Courses

_Courses sharing skills:_

- [[Essential Statamic for PHP and Laravel Developers]] — PHP
- [[Essential Jigsaw for PHP and Laravel Developers]] — PHP
- [[Advanced PHP- Debugging Techniques]] — PHP
- [[PHP Techniques- Pagination]] — PHP
- [[PHP- User Authentication]] — PHP

---

[↑ Back to top](#)