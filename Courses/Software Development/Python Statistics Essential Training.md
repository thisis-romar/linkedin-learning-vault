---
type: course
cssclasses:
  - lle-course
  - lle-level-advanced
slug: python-statistics-essential-training-19258005
url: "https://www.linkedin.com/learning/python-statistics-essential-training-19258005"
duration_minutes: 159
duration: 2h 39m
level: Advanced
updated: 8/17/2023
learners: 139748
skills:
  - Python (Programming Language)
  - Statistics
exercise_files: true
github: "https://github.com/LinkedInLearning/python-statistics-essential-training-4433355/codespaces"
thumbnail: "https://media.licdn.com/dms/image/v2/D4E0DAQHUYNAoME92XQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1694730199516?e=2147483647&amp;v=beta&amp;t=aGGIQ3sT36VnpPTJD0qWDk28ODLGfSONStsUo-i8jRc"
linkedin_topic: Software Development
learning_paths:
  - '[Moving from Data Scientist to Data Analyst](../../Paths/Data%20Science/Learning%20Paths/Moving%20from%20Data%20Scientist%20to%20Data%20Analyst.md)'
  - '[Master Python for Data Science](../../Paths/Database%20Management/Learning%20Paths/Master%20Python%20for%20Data%20Science.md)'
prev_courses:
  - '[Data Visualization for Data Analysts and Analytics](../Data%20Science/Data%20Visualization%20for%20Data%20Analysts%20and%20Analytics.md)'
  - null
next_courses:
  - '[Deep Learning Getting Started](../Artificial%20Intelligence%20(AI)/Deep%20Learning%20Getting%20Started.md)'
  - '[Advanced Python- Working With Data](Advanced%20Python-%20Working%20With%20Data.md)'
path_nav: '[{"path":"Moving from Data Scientist to Data Analyst","position":5,"total":10,"prev":"Data Visualization for Data Analysts and Analytics","next":"Deep Learning Getting Started"},{"path":"Master Python for Data Science","position":1,"total":8,"prev":null,"next":"Advanced Python- Working With Data"}]'
path_count: 2
tags:
  - course
  - topic/data-science
  - topic/software-development
  - topic/database-management
  - skill/python-programming-language
  - skill/statistics
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/Python%20Statistics%20Essential%20Training.md)

![Python Statistics Essential Training](https://media.licdn.com/dms/image/v2/D4E0DAQHUYNAoME92XQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1694730199516?e=2147483647&amp;v=beta&amp;t=aGGIQ3sT36VnpPTJD0qWDk28ODLGfSONStsUo-i8jRc)

# Python Statistics Essential Training

> The field of statistics has become increasingly dependent on data analysis and interpretation using Python. With the rise of big data and data science, the demand for professionals who can effectively analyze and interpret data using Python has skyrocketed. In this course, Matt Harrison teaches you how to collect, clean, analyze, and visualize data using the powerful tools of the Python programmin

> [LinkedIn Learning](https://www.linkedin.com/learning/python-statistics-essential-training-19258005) | 2h 39m | Advanced | 140K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Being a Python statistics MVP](#being-a-python-statistics-mvp)
  - [What you should know](#what-you-should-know)
  - [Using GitHub Codespaces with this course](#using-github-codespaces-with-this-course)
- [**1. Loading and Cleaning Data**](#1-loading-and-cleaning-data) (6 videos)
  - [Loading data](#loading-data)
  - [Strings and categories](#strings-and-categories)
  - [Cleaning numbers](#cleaning-numbers)
  - [Shrinking numbers](#shrinking-numbers)
  - [Challenge: Clean Ames](#challenge-clean-ames)
  - [Solution: Clean Ames](#solution-clean-ames)
- [**2. Exploring and Visualizing**](#2-exploring-and-visualizing) (9 videos)
  - [Categorical exploration](#categorical-exploration)
  - [Histograms and distributions](#histograms-and-distributions)
  - [Outliers and Z-scores](#outliers-and-z-scores)
  - [Correlations](#correlations)
  - [Scatter plots](#scatter-plots)
  - [Visualizing categorical and numerical values](#visualizing-categorical-and-numerical-values)
  - [Comparing two categoricals](#comparing-two-categoricals)
  - [Challenge: Explore Ames](#challenge-explore-ames)
  - [Solution: Explore Ames](#solution-explore-ames)
- [**3. Linear Regressions**](#3-linear-regressions) (6 videos)
  - [Linear regression](#linear-regression)
  - [Interpreting linear regression models](#interpreting-linear-regression-models)
  - [Standardizing values](#standardizing-values)
  - [Regression with XGBoost](#regression-with-xgboost)
  - [Challenge: Predict Ames](#challenge-predict-ames)
  - [Solution: Predict Ames](#solution-predict-ames)
- [**4. Hypothesis Tests**](#4-hypothesis-tests) (6 videos)
  - [Exploring data](#exploring-data)
  - [Visualizing distributions](#visualizing-distributions)
  - [Running statistical tests](#running-statistical-tests)
  - [Testing for normality](#testing-for-normality)
  - [Challenge: Checking square footage distributions](#challenge-checking-square-footage-distributions)
  - [Solution: Checking square footage distributions](#solution-checking-square-footage-distributions)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Being a Python statistics MVP](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/being-a-python-statistics-mvp?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/being-a-python-statistics-mvp?u=76281980&t=0)** - Have you ever wanted to explore [Statistics](../../Skills/Data%20Science/Statistics.md) using a code-first approach? You've come to the right place. This [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md)-based course will dive into various techniques that form the backbone of statistics. We'll understand the data with summary statistics and visualizations. Then we'll create predictive models using both [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) and XGBoost. Finally, we'll explore hypothesis testing. Because we use Jupyter Notebooks and [GitHub](../../Skills/Software%20Development/GitHub.md) [Codespaces](../../Skills/Software%20Development/Codespaces.md), you can easily run the same code and follow along in a web-based environment without any complicated installation. Hi, I'm Matt Harrison, a Python and [Data Science](../../Topics/Data%20Science.md) corporate trainer. I'm excited to join you on this incredible statistics adventure together using a code-first approach.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Statistics](../../Skills/Data%20Science/Statistics.md) (4), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2), [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (1)
> **CLI Commands:** python (2)
> **Tools:** jupyter (1), github (1)
> **Speakers:** - have (1)

#### [What you should know](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/what-you-should-know?u=76281980)

#### [Using GitHub Codespaces with this course](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/using-github-codespaces-with-this-course?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/using-github-codespaces-with-this-course?u=76281980&t=0)** - [Instructor] In this lesson, I'm going to show you how to configure [Codespaces](../../Skills/Software%20Development/Codespaces.md) for this course. You want to navigate to the course repository and then click on this green Code button. You'll see that there's a local tab and a codespaces tab. If you wanted to run this locally, you could, if you know how to get a [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) environment set up, there's a requirements.txt that you can use PIP to install the requirements if you want to run locally. Alternatively, you can use codespaces, which is really easy. Click on this codespaces tab and then come over here and say create codespace on Main. That's going to open up a new tab here, which is going to set up your codespace. This is going to take a while to run. We'll let it do its thing, and then we'll come back when it's finished. Okay, this has launched codespaces. If you're not familiar with codespaces, basically this is a web environment where you can work on a project. What it's done is it's launched a virtual machine and we have a web environment that lets us talk to the machine. You can see that I'm in a terminal right here. I can say LS and I have access to all of my favorite [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) commands here. We're also launched in VS Code, so for this course, all we have to do is click on this Pystats.ipynb. This is the notebook for the course. There is a solutions notebook as well that says -Solutions. Don't open that one. You're going to want to open the one that doesn't have the -Solutions.
>
> **[1:32](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/using-github-codespaces-with-this-course?u=76281980&t=92)** So double click on this and it will open the notebook in VS Code.
>
> **[1:43](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/using-github-codespaces-with-this-course?u=76281980&t=103)** If you want to get rid of the explorer over here, you can click on that to remove that. Okay, so here is our notebook, and what I want you to do is just click on this first cell that has code on it. I'm just clicking on the left hand side of this rectangle. Hold down Control and hit Enter. That's going to run this cell, and you can see it has some options pop up when we do that, it says type to choose a kernel source. It says Python Environments and it says Existing Python Server. So we have a Python environment configured here. Let's just click on that, which is this Python 3.10.8. Just click on that. We'll let it do its thing. You can see in the lower right hand corner, it says it's connecting to a kernel. Now you can see it's running and it has finished running, at this point, you can see it says 2.0.2. In this lesson, we showed you how to open a project in [GitHub](../../Skills/Software%20Development/GitHub.md), create a codespace, and then connect a Jupyter Notebook to that codespace and execute it through codespaces. Codespaces is a nice tool that you find on GitHub that lets you easily start projects without having to worry about configuring environments. I recommend using them if you're not familiar with virtual environments in Python.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Codespaces](../../Skills/Software%20Development/Codespaces.md) (8), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (6), [GitHub](../../Skills/Software%20Development/GitHub.md) (2), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1)
> **UI Navigation:** click on (8), open the (2), navigate to (1)
> **CLI Commands:** python (6), pip (1), ls (1), find (1)
> **Tools:** vs code (2), github (2), terminal (1), jupyter (1)
> **Prerequisites:** set up (2), configure (1), install (1)
> **Versions:** python 3 (1), 10.8 (1), 2.0.2 (1)
> **Definitions:** is a  (3)
> **File Paths:** requirements.txt (1)


### 1. Loading and Cleaning Data

[↑ Back to Table of Contents](#table-of-contents)

#### [Loading data](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/loading-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/loading-data?u=76281980&t=0)** - [Instructor] In this lesson, we are going to look at loading the data. I've opened the notebook in [Codespaces](../../Skills/Software%20Development/Codespaces.md). If you want to run this locally, you can run this locally as well. I'm running the PyStats, IPy and B notebook and I'm just going to hide this so we have a little bit more screen real estate. So we're going to be looking at loading the data. We're going to be using the Ames, Iowa Housing Data set. This is a nice little data set. It's got a couple thousand lines of data about housing sales in Iowa, and it's got 80 plus columns of information around those cells. So this is going to be a great data set for us to look at and examine some [Statistics](../../Skills/Data%20Science/Statistics.md) using it. Let's go down and run some of these cells here. The first cell I'm going to run is this import cell, and to run that, I'm going to hold down control and hit enter. What that does is it tells Jupiter we want to run the cell. I like control and enter because it leaves the focus on the cell. What that allows me to do is hit enter and I can go in and edit this cell if I want to after that and then I can rerun that by holding down control and hitting enter again. So we're checking the version of [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) pd.__version__, we call that Dunderversion and we see that we are using Panda's version 2.02. So one thing to be aware of in this course is that we are using the latest version of Pandas as of the recording, Pandas Two
>
> **[1:33](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/loading-data?u=76281980&t=93)** and Pandas Two has a few features that allow it to do things more efficiently and with less memory than Pandas One. So I'm going to be using some Pandas Two functionality in this course. If you don't have Pandas Two, some of the code might not work for you, but hopefully I can convince you that you probably want to upgrade to Pandas Two to take advantage of the new features there. Let's load the data set in this cell. Again, I'm going to hold down control and hit enter and this should run. At the bottom here, you see I'm using the read_CSV function to load the data. I'm passing in a URL. We've got this locally on our code space. I also have a variable pointing to where you can get this on the internet if you want to as well. When you look at this read_CSV function, this probably looks different if you're used to Pandas One with the two parameters, engine and dtype_backend. Both of those we are specifying as pyarrow. What engine equals pyarrow does is it uses the arrow library to load the CSV file. This can be a lot quicker than using the standard Pandas logic to do that. And what dtype_backend does is it allows you to store the data types using the arrow library instead of NumPy, and that gives you a lot of memory benefits as well. Let's inspect the shape of this dataset. I'm going to go down and execute this next cell. Again, holding down, control and hitting enter to run that. You can see that this returns a tupple. There's 2,930 rows and 82 columns. Let's look at the first few rows of data.
>
> **[3:09](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/loading-data?u=76281980&t=189)** I'm going to use this head method to do that and you can see the first five rows. There are 82 columns in here. You can scroll over if you want to and see some of it. Note that we are getting some of the columns truncated here. You can see that there's an ellipses. We're only showing you the first 10 columns and the last 10 columns, but we've got 82 columns of, again, housing sales data from Ames, Iowa. One of the things I like to do is run the describe method and what the describe method does is gives us summary statistics for each of the numeric columns in the data frame. In the index now you can see on the left hand side where it says count, mean, STD, min, that is the index of this data frame and indexes don't have to be numeric. Pandas will let them be non-numeric, and in this case, we can see that we have non-numeric, we have string entries for the index representing the statistical results of each of these numeric columns. So for order, this is the order of cells, and you can see in the count row, 2,930. Now, one thing to be aware of with Pandas is that count probably doesn't mean what you think it means. Count in Pandas means the number of non missing values. So, if we look over a little bit at the lot frontage count, it says that that is 2,440. So what that means is that there are 2,440 entries there that had values and that there are a few of them
>
> **[4:45](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/loading-data?u=76281980&t=285)** that are missing values. We also have the mean, the standard deviation, the minimum value, and the maximum value, and then the inner quartiles there. I like to use this for a gut check to understand what the data looks like. You can see that a lot of these numeric values don't go very high and they're also non-negative. By default, Pandas is going to use 64 bits of information to store this data. We can probably save more memory by changing those types and I'll show how to do that in a future lesson. In this lesson, we loaded the data for the Ames, Iowa dataset and then we did some basic inspection and sanity checks to make sure that we have the data loaded.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (14), [Statistics](../../Skills/Data%20Science/Statistics.md) (2), [Codespaces](../../Skills/Software%20Development/Codespaces.md) (1)
> **Env Vars:** url (1), csv (1), std (1)
> **Warnings:** be aware (2), note that (1)
> **Code Identifiers:** dtype_backend (2)
> **CLI Commands:** make (1)
> **Versions:** version 2 (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Strings and categories](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980&t=0)** - [Instructor] In this lesson, we're going to look at strings and categories and understand these types of columns and how to deal with them in [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md). I've listed out some of the goals here. We want to be able to use the dtypes attribute. We want to look at using the select_dtypes method. We're also going to do summary [Statistics](../../Skills/Data%20Science/Statistics.md) using the describe method. And we're going to look at memory usage and converting string columns to category types. One of the attributes on a data frame is this dtypes attribute. And when you run this, what this is going to give you is a Pandas series. In the index of this series, we have the columns. Remember a Pandas index does not need to be numeric. In this case, Pandas is sticking the columns into the index and then the values for the index, we can see that says int64, and then in brackets, pyarrow, that is the values of this series. And we can see in brackets it says pyarrow indicating that we are using Pandas 2 and we are using that pyarrow backend. Again, because we're using the pyarrow backend, we're going to get some speed improvements, we're also going to get some memory improvements as well. One of the big features of Pandas 2 is that it has a native string type. We can see for MS zoning that the data type is a string, and in brackets, pyarrow. In Pandas 1, if we wanted to select the columns that were strings, we would say select_dtypes objects. And this doesn't work in Pandas 2 because the strings don't have the object type. This is what we need to do in Pandas 2.
>
> **[1:34](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980&t=94)** We're going to say select_dtypes and say string, and we're going to pass that in as a string. If you look at the output of this, at the bottom it says you have 2,930 rows and 43 columns. So this is selecting only the strings column. You can see in the comment up there I said, alternatively you can say strings, and in bracket, pyarrow, if you'd like the type. Now, one of the things you can do once you have this is you can throw on this describe method. So I'm going to actually throw on a describe and throw on the transpose on that as well. I like to sometimes transpose my data just to see a little bit more. Let's run this without the transpose, that's the capital T there. And you can see that we have 43 columns and I have to scroll over a little bit to see that. You can also see that there's an ellipsis here, so it's not showing all of that. Sometimes transposing the data, again, that's flipping the rows and the columns, lets us see just a little bit more. Remember we ran describe previously on our data frame and it gave us summary statistics for the numeric columns. In this case, we don't have any numeric columns because we said select_dtypes string. We just have string columns. And so when we run describe on a data frame composed of only string columns, we get summary statistics for the strings. In this case, we get the count of non-missing values. We get the number of unique entries for that. We also get the most common entry, so that's the entry with the column top, and the frequency of that. So for the MS zoning column,
>
> **[3:07](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980&t=187)** there are 2,930 values in the rows. There are seven different values. The most common value is RL and it occurred 2,273 times. You can see for most of these, that unique value is not very high. Statisticians sometimes call that cardinality. So a low cardinality might be a prime candidate for us to convert this into a categorical type in Pandas, allowing us to save even more memory. So in this cell, I'm going to run select_dtypes string and then I'm going to say memory_usage, deep is equal to true, and then I'm going to do sum. And what this is going to give me is the number of bytes used by the string type columns. This code is written as a chain. You can see that we have highlighted the parentheses here. There's a parenthesis above and below it. I like to write my code that way. What it allows me to do is put each operation on a single line and it makes your code read like a recipe. So let me just comment these out and we'll walk through what's going on in our chain. Here's our original data frame, and you can see that there's 2,930 rows here and 83 columns.
>
> **[4:25](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980&t=265)** If I select just the object columns, you can see that that goes down to 43 columns. And then I'm going to say on that, memory_usage deep is equal to true. So this is going to return a series and it's going to tell us how many bytes each column is using. And then I'm going to sum up this series and get the total of that. So it looks like this is using 957 kilobytes of data. In this next cell, I'm going to do a similar operation, but I'm going to insert this astype category. And when I run that, you can see that this uses quite a bit less memory. And what's going on under the covers is that Pandas is encoding all of the unique types into a number and then it's using basically an array with those unique values pointing back to the original values. So it doesn't have to store strings for every entry, it can just hold a numeric value. And you save quite a bit of memory if you do that. You can see that this is almost seven times more memory-efficient by converting that to a categorical type. Okay, so remember we have 2,930 rows. So one of the things I might want to do is look at the number of rows that are missing. And for numeric data, I would do a chain that looks something like this. Let me just walk through what's going on here. I'll comment this out and explain the code as we're going along. So the first operation I'm going to do is this isna method.
>
> **[6:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980&t=360)** What that's going to give me back is a data frame, but rather than having the original values, it has true/false values. And then what I'm going to do is I'm going to do the mean operation on that. One thing to be aware of in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), and also in Pandas because Pandas is in Python, is that Python treats true and false values as one and zero, respectively. So if I take the mean of this, this is going to essentially be taking the mean of a data frame with zeros and ones in it. And what that's going to give me is the fraction of the values that are missing. So you can see, for example, it says that lot frontage, 0.167 of those are missing. So if I multiply this by 100, I get the percentage that is missing. So 16% of the lot frontage values are missing. Now what I'm going to do is I'm going to use the pipe method here and filter out the values that are zero. What pipe allows us to do is pass in a function into it. In this case, I'm using a lambda function that takes the series that is passed in, the series that you're seeing on the screen here. And then I'm just going to use a Pandas operation to filter out the values where the series is greater than zero. And here's the entries for that. You can see that a lot of the lot frontage values are missing and the basement year built values are missing, and then a few of the other values are missing as well. One of the things that stands out to me when I look at this is, for example, this 0.03413. This just kind of makes my spidey-sense tingle a little bit
>
> **[7:37](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980&t=457)** because that same value is repeated in multiple places. So that probably means that there's probably a row or a few rows where these values are missing for all of those. That's probably what's happening there when you see those repeated values. That previous code cell that we ran, if you think about it, is actually not containing any string columns in it, these are all numeric columns. So this is a difference in Pandas 2. In Pandas 2, if a string column is missing a value, Pandas will actually set that to an empty string rather than the NA value. So if we want to find the missing string values, we actually need to, instead of making this isna data frame, we're going to check whether the data frame is equal to the empty string. Again, let me just run through this quickly. I'm going to comment this out. The chain makes it really easy to comment this out. So here's our data frame with the string columns and we're going to say, are these values equal to the empty string? And this gives us back our Boolean array, and then we can just repeat the process that we did there before and get the percentage of values that is missing. In this cell, what I'm going to do is I'm going to select any of the rows where the values are missing. So I'm going to pull this out here and I'm actually going to walk through what's going on here. Because this is a useful piece of code, let's walk through what's going on here.
>
> **[9:12](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980&t=552)** So I have it written in a single line. It's easier for me to show you what's going on if I write it in this chain style like this. And I'm going to comment this out and we'll just walk through this. Okay, so here is my string columns. Where are they equal to the empty string? And this is a data frame of true/false values. Now I'm going to do the any along the axis is equal to columns. So this is going to keep the same index. So if you look at this, you'll see the same index, but this is going to return back a series whether any value in the row was true. And you can see in this case, there are a bunch of falses, but presumably some of those are true. And that's what I'm passing in here, but I'm also passing in this tilde at the front here, which negates that. So what this is going to give me is it's going to give me the rows where there are missing values. And it looks something like this. We can scroll over a little bit, and it doesn't look like we're seeing very many missing values here. One thing to note is that we are seeing a bunch of NA values as well. This dataset is a little messy in that it has missing values that aren't in there at all, but it also has NA that is encoding missing values as well. So that's something to be aware of. Let's just repeat our operation here, but instead of looking for the empty string, looking for the NA string.
>
> **[10:46](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980&t=646)** And you can see, for example, that 93% of the alley values are missing and 48% of the fireplace quality values are missing. Okay, let's inspect this a little bit more. I'm going to inspect, where is the pool QC, the pool quality missing? Let's run that. And it looks like there aren't any values where it's missing. Again, remember that this Ames dataset encodes some of the missing values as NA. Let's run this with isna instead. And these are all the values where the pool QC or the quality of the pool is missing. Presumably, these houses don't have pools and hence this value is missing. So that's probably an appropriate encoding there for that. So one thing I might want to do is I might want to go in and clean up this data set a little bit. And so here's one of the ways that I would do that. I would use the assign method, and here I'm passing in a chain inside of assign. So I'm passing in select the dtypes of string, and with all of the string types, if there's any missing values, just fill that in with not applicable. And then what I'm going to do is I'm going to pass it into assign. Assign generally takes a parameter, which is the column name, and a value for the column name to make a new column. But if we pass in a data frame and we put these two stars in front of it,
>
> **[12:19](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980&t=739)** it will say, take in this data frame and just update the values with this data frame. So essentially, what this is giving us back is a new data frame with the empty string values replaced by the not applicable values. Okay, let's go a little bit further here. Let's look at electrical. There were some missing values in electrical, so let's look at that. And this is how I like to look at string columns. I like to use this value_counts method. What that gives me back is a Pandas series, and it gives me in the index the unique values, and in the values of the series, the counts of those. You can see, for example, this row right here is the empty string values there. So there's one that is missing, the electrical is missing. So it'd be good to check, why is the electrical missing? Is this a house that did not have an electrical system? Or did this data get dropped? We probably don't want to leave this as an empty string, we probably want to figure out what's going on there and clean that up. Let's look at the row where it actually is missing. This is how I would do that. I would use the query method and say a string, electrical, equals equals the empty string. And here's the row where that is missing. We can inspect what's going on here. It looks like this was a house that was sold in May of 2008 for $167,500. So let's look at another example.
>
> **[13:58](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980&t=838)** I'm going to do the same thing with fireplace quality. And in this case, you can see that there aren't empty strings, but we do have quite a few NA values, that's the most common value. Let's do the same thing with basement condition. Basement condition has both NA values and a missing value as well. So you can see that this dataset is not maybe as clean as we would like. Maybe we would want to do something like this where we say, okay, let's take this data frame and I'm going to update all of the string columns, replacing the empty values with not applicable, and then converting those to a category. This would be my operation that would do that. I can just test that out here and check that that does work. It looks like that was successful. And this returns 2,930 rows. Let's check how much memory usage that's going to use. That's going to give us a data frame that it looks like is using one meg of memory. Let's compare that with our original data frame that is not converted to a category, and that's using 1.8 megs of memory. So you can see by converting two categories, we will save quite a bit of memory. In this lesson, we looked at some of our string columns and we looked at various operations that we would do with string columns. We did summary statistics on them, but we also explored some of the missing values. We did that by using some Pandas manipulation
>
> **[15:34](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/strings-and-categories?u=76281980&t=934)** to find the percentage of values that are missing and then we dove into those using value_counts and also using query to examine those. Understanding what is in your data is an important step and I highly recommend that you do this step and don't skip it. The more you understand your data, the better you'll be able to explain the data and share insights with others.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (19), [Statistics](../../Skills/Data%20Science/Statistics.md) (4), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3)
> **Definitions:** is a  (9), means that (1), is an  (1)
> **Code Identifiers:** select_dtypes (5), memory_usage (2), value_counts (2)
> **CLI Commands:** python (3), find (2), make (1)
> **Analogies:** for example (4)
> **Versions:** 0.167 (1), 0.03413 (1), 1.8 (1)
> **UI Navigation:** select the (2)
> **Warnings:** be aware (2)

#### [Cleaning numbers](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980&t=0)** - [Instructor] In this lesson we're going to discuss cleaning numbers. We're looking at the PyStats notebook in the 0 1 0 3 section. Our goals are going to be using select D types, using describe, finding missing values, and I'll also show you how to view a little bit more data than the default values and style the output. In [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) one, there would be a lot of entries for the Float D type here. That's because in pandas one, when pandas encountered a integer value that had missing values, it converted it to a float. You can see that there aren't any missing values here. That's because we're using pandas two. Remember that PyArrow backend actually has support from missing values. And so if we say select D types is int we get 39 columns here. So there are no floating point columns there's just integer columns in here. So let's explore some of these columns and see what's going on. So again, one thing that we can do, is run the describe method to get summary [Statistics](../../Skills/Data%20Science/Statistics.md) on those. One of the things I like to do is look at the count, just to see how many values are missing. For example, you can see that lot frontage is missing quite a few values. I also like to look at the minimum values and the maximum values to see what the range is. You can also look at the mean, which a lot of people would refer to as the average, though a statistician would probably get upset at you if you said that. And you can look at the 50th percentile which a statistician would call the median. If the mean and the median are around the same, you might have data that's somewhat normal,
>
> **[1:34](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980&t=94)** though I also like to look at visualizations to understand what the lay of the land looks like or the distribution of that data looks like as well. Okay, so remember, we've got 2,930 columns there. So a lot of these columns aren't missing any values but some of them are. So let's look at this lot frontage one. This is how I would explore it. I'm going to run the query method and say, I want to look at where the lot frontage is missing. Let's dive into what's going on inside of query here. So query expects you to pass in a string that takes a column and some operation on the column. In this case, the column is lot frontage and, because lot frontage has a space in it, it is not a valid [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) attribute. So if you have a column name that has characters that aren't valid Python attributes, you need to surround them by backticks. That's what we see here. And then I'm going to say, okay, we've got that lot frontage column, let's just look where it is missing. And you can see that there are 490 rows where the lot frontage is missing. You can see it's represented here by NA in less than and greater than. One of the questions I frequently get when I teach people about missing values is how to deal with them. My typical response is, it depends, but probably the best way to deal with them is to find a subject matter expert, someone who knows why this data is missing, and they can often point you in the right direction. For example, lot frontage. Does NA mean that there is not a front area
>
> **[3:09](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980&t=189)** of this house or does this mean that lot frontage is missing that we don't have that information? That would be something that would be useful to find out. If there is not a front of the house, I would probably encode that as zero rather than NA. So again, it's important to talk to a subject matter expert and find out why exactly this data might be missing. Okay, remember I promised I would show you how to see more data and this is the secret to doing that. Pandas, by default, is not going to show you all of the columns and all the rows if you have a large data set. And, I think this is useful and a lot of people, as soon as I tell them this, ask me how do I get around this? I would caution you to refrain from trying to get around this as soon as you can. Humans aren't really meant for looking at large tables of data. We're not optimized for doing that and it's not an effective use of our time, really. What you want to do is you want to use something that is effective for pulling out data that is interesting. So my recommendation is when you feel that urge to look at more columns or more rows, think, can I use a query or a filter, to pull out the data that is interesting to me. Alternatively, can I visualize this, and might that give me more insight into the data? You can tell a really good story by visualization that's a lot stronger than presenting someone with a table of a bunch of numbers.
>
> **[4:43](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980&t=283)** So how we're going to do this in pandas is we're going to use the context manager, that's the with statement. And we're going to use this option context. Context manager is a construct in Python that allows you to enter into a context and exit out of that. In this case, when we use option context, in the indented portion here, we're calling display, while we are indented, what ever we pass into option context is going to overwrite the values. When we un-indent from the context manager it will revert back to what it was before. So we're going to change the minimum number of rows that we view to 30 and we're going to change the maximum columns to 82. So you can see that we're now seeing quite a bit more data and if we scroll over you can see that we are seeing a lot more columns than we were previously. If you're not familiar with this feature in Jupiter, one of the things you can do is put a question mark after a method or a function in Jupiter and it will pull up the documentation. So off of the style attribute, there are various methods on there. One of those is set sticky. What this allows us to do is set the column or the index to sticky so when we scroll we can see the index. This often comes in very handy. In this example I'm going to show a lot more data than I normally show. And then I'm going to run this query where I'm showing the missing values of lot frontage
>
> **[6:19](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980&t=379)** and then I'm going to set the sticky columns and the sticky index. The output looks like this, and if I scroll over, you can see that that index there stays on the left hand side. So this comes in useful when you need to find out what row is going on. You'll note that I do have a comment here at the top, that columns is broken. I think this is a pandas two bug, where, the columns are not being sticky here when I scroll down. Hopefully they fix that. Okay, let's look at where garage year built is missing. So here are the rows where garage year built is missing. It looks like there's 159 of those rows. Again, we could scroll through this data and try and determine what a missing garage year built means. I am assuming that it probably means there is not a garage on here. Hence, the missing value. In this case because year built is a year, putting in a default value of a zero to encode missing probably wouldn't be the right choice because if you wanted to look at the average of the year built that's going to throw that off. So in this case, garage year built, leaving those values as missing, might be useful. However, if you want to do some operations
>
> **[7:55](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980&t=475)** such as running [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) on this, that might be problematic, because, linear regression does not like to have missing values in the data. You might need to find a way to encode that, such that you can run some of these other tools with columns that have missing data in them. I'm going to take that year built column and I'm going to run summary statistics on that just to see what's going on there. And, you can see that the count is demonstrating that we are missing quite a few values. I like to look at the minimum value and the maximum value. Now this actually stands out to me, the maximum value here. 2 2 0 7, remember this is a year, and as of this recording we're recording this in 2023. So this garage is being built a few years into the future. That's somewhat problematic to me and that probably indicates that there is some sort of a typo going on here. This was probably 2007 but it got fat fingered on data entry. Let's look at that row where that is missing. And again, I'm going to use my option context just to display a few more of those columns. And, I'm going to put in as my query, garage year built, remember I'm going to use back ticks around that because there are spaces , and then greater than 2020. And maybe I'll scroll over here. You can see that the year remodel or add is 2006,
>
> **[9:30](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980&t=570)** and the garage year built is 2,207. And the year sold is 2007. So again, I'm going to assume that this is a typo and we might want to go in and clean that up. Now, because I've found this by going through the steps of manually looking at the data, looking for missing values and also looking at describes, this makes me want to look at maybe some of the other year columns, maybe some of those are fat fingered as well. So, one method that comes in handy is the filter method, and it allows me to pull off various columns. So I'm just going to pull up the documentation there. You can see that this says it subsets the data frame rows or columns according to a specified index. So there are various ways to do this using items like or regex. We can scroll down here and see that items means keep labels, which are in items. So in this case we're passing in a sequence of labels and we keep the labels which are in that sequence. You can pass in like which is a string. It says keep labels from the axis for which like in label is equal to true. That would allow us to do SUBSTRING matching, but apparently it would only allow us to do one substring, whereas items allows us to match multiple strings. Doesn't look like there's an option to match multiple substrings. However, we could possibly also use this next option, which is a regular expression, and we could probably formulate a regular expression that would allow us to match multiple substrings as well.
>
> **[11:07](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980&t=667)** Here's an example of using that. I say filter like is equal to yr. You can see there are two columns that have yr in them. If I want to check which of those has a column greater than 2023, which is the current year, I'm going to do this chain here. So again, here's our first column here, and let's throw in this pipe. What is this pipe doing? It's saying, with the data frame we see on the screen, we are going to do an index operation with this result right here. So we saw something similar to this before, we said any along columns is equal to axis. What that's going to give us is a Boolean array. It's going to have the same index as the data frame but it's going to have true false values. The true values will be where it's greater than 2023. Let's run that. And we only see that one column, that index 2, 2, 6, O, so there aren't any other values for year sold that are greater than 2023. What about the other columns? There are some columns that have year in them. So this would be my process to filter those out. I'd probably do something like that, where I'd say, okay let's take our data frame, rename our columns that are yr and make those year and then with those, let's do a filter, and say, take the columns that have year in them and then with those, let's add in our pipe here and see which ones are above our limit. It looks like, in this case,
>
> **[12:40](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980&t=760)** it was only that one row that was fat fingered. In this next chain, I'm showing an example of clipping. So let's take garage year built. This is just the series here. And then I'm going to use the clip method from pandas to say I want to limit this and have the upper values just be the year built max. And if we do that, that gives us a new series that has been clipped. And if we look at the value counts from that, we should see that there aren't any values that are greater than 2023. Now, this is sorted by the frequency. We could come in here and say sort index, and that looks like the highest value in the garage year built is 2010. At this point, I might want to update my data frame with this information. Now remember, in our previous lesson, we looked at replacing strings that had values that were missing and converting those to categoricals. So I'm going to leverage that. I'm going to make a chain here, and, I'm going to put that operation to clean up my strings into my chain and then I'm going to follow that with this other operation to clean up the garage year built. Now let's examine this line right here a little bit. What's going on with this line? There's quite a bit of syntax here. I am using what's called dictionary unpacking.
>
> **[14:18](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980&t=858)** I have a dictionary literal. and here's my dictionary. Why am I using a dictionary? Why don't I just say garage year built is equal to the current value of garage year built that's clipped. I'm using a dictionary because there are spaces in here. Because there are spaces, I can't say garage space year built is equal to that, so I can't use a parameter to do that, however, I can stick this into a dictionary and then use dictionary unpacking and that will update the column with a non-valid attribute name using pandas. And then maybe let's just look at our D types value counts after doing that. Looks like there are 39 int columns, or integer columns. And then it looks like there are categorical columns but, it doesn't sum these up. Why doesn't it sum these up? Because presumably, there are four categorical columns that have the same entries in them. There are three that have the same entries and then all of these other ones down here are one-offs. In this lesson, we looked at examining our numeric columns. I showed you how to do summary statistics on them. I showed you how to look for missing values and I showed you an example of when I looked at those summary statistics, I found something off and then I examined it a little bit more and then I used that to actually feed back into a chain to clean up the data. This is a process that you're going to want to go through when you load your data. You want to make sure that your data is clean,
>
> **[15:52](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/cleaning-numbers?u=76281980&t=952)** so that when you start doing analysis of it, the analysis makes sense.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (9), [Statistics](../../Skills/Data%20Science/Statistics.md) (4), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (2)
> **CLI Commands:** find (5), python (3), make (3)
> **Definitions:** is a  (9)
> **Analogies:** for example (2), such as (1), similar to (1)
> **Documentation:** the documentation (2)
> **UI Navigation:** scroll down (2)
> **Warnings:** caution (1), note that (1)
> **Env Vars:** substring (1)

#### [Shrinking numbers](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/shrinking-numbers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/shrinking-numbers?u=76281980&t=0)** - [Instructor] In this lesson, we're going to look at changing the size of our numbers to save memory. One thing is I will do is create a function that will automatically convert integer values to smaller types. And then I'm going to show how to apply this function to our dataframe. You can actually take this function and apply it to your data as well. We're also going to make another function. I'm going to call that clean_housing that combine some of the steps that we've been looking at to clean up our data. This is something that I recommend you do as well as you're going through your data, cleaning it up, throw that into a chain and then take that chain and put it into a function. This is going to be a practice that, if you use it, will make your life a lot easier. We're going to continue where we left off. In our last lesson, we saw this assign method where we converted strings into categoricals and we also cleaned up our Garage Yr Blt. Here's our summary [Statistics](../../Skills/Data%20Science/Statistics.md) of that. Looks like we haven't lost any fidelity by doing that. We should be good. We've actually cleaned up that Garage Yr Blt. Now, remember I said that I like to go through the maximum values here, and for example, the Overall Qual column. You can see that right here has minimum values of one and maximum values of 10. Remember that [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) is using a 64-bit integer to store that information which is actually a waste. So we could use a different data type
>
> **[1:36](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/shrinking-numbers?u=76281980&t=96)** and save memory without losing any information. Let me show you how we might want to do that. In this cell, I'm going to loop through NumPy, and NumPy has a function in it called, iinfo, integer info. I'm just going to loop through some NumPy types. Uint8 stands for an 8-bit unsigned integer. So if we say np.iinfo(uint8), it tells us that the minimum value for that is zero and the maximum value is 255. This is a type that we could use for that overall quality because zero and 10 certainly lie within this range. Now, sometimes you have positive numbers that go a little bit bigger. You might need to use 16-bit or 32-bit before you go up to 64. One thing to be aware of, these are NumPy types. These are the types that were used in Pandas 1. In Pandas 2, you can optionally use that PyArrow backend. However, these type ranges actually work for the PyArrow types as well. So in this cell, I've created a function. Again, you can use this function and leverage it for PyArrow-backed dataframes. What it's going to do is it's going to accept a dataframe as input, and then it's making a mapping dictionary. It's going to loop over all of the columns that are PyArrow 64-bit integers. And then it's going to look at the minimum and maximum values for those.
>
> **[3:09](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/shrinking-numbers?u=76281980&t=189)** And if the minimum is less than zero, it's going to continue. This is only dealing with unsigned integers. Otherwise, it's going to look at the maximum values and determine the appropriate type for that. Once you have this function, all you have to do is use it in combination with the pipe method. So if you're not familiar with that pipe method in Pandas, this is a super handy method, it allows you to do whatever you want to a dataframe. All you have to do is pass in a function into it. In this case, we're passing in the shrink_ints function. Your function should accept a dataframe if you're using pipe with a dataframe. If you're using pipe with a series, a series also has a pipe method, your function needs to accept a series. And then the function can return whatever it wants. In this case, we're actually returning a dataframe using that mapping to change the types there. And with the output of that, we can actually run a describe on that after we've done that. So let's run this and test this out. You'll note that I've still included what we've done previously here. So I want to impress upon you that when you see a chain that's a little bit longer, I don't create these chains from scratch. I am building them up as I'm going through my process. And so let's look at the output from that. The output looks good. Looks like we didn't lose any information by doing that. In this next cell, let's see how much memory this is using.
>
> **[4:42](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/shrinking-numbers?u=76281980&t=282)** And it looks like this is using 360 kilobytes of memory. Let's compare that with our default amount of memory. So shrinking our integers in combination with changing our types to categoricals has saved us five times the amount of memory by doing that. The next thing I would want to do after doing this is convert this into a function. I'm keeping my shrink_ints function that I defined above, but I'm also making a function called clean_housing. This is my chain. I'm just moving this chain into a function. Let's run the function and make sure that it works. I'm just going to inspect the dtypes after doing that. It looks like this is working successfully. In this lesson, I presented a function that you can use to shrink PyArrow types without losing data. We also looked at some of the benefits of that such as memory usage going down drastically by doing this. This is one of the ways that you can load much larger data sets and analyze them by using the correct types.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (4), [Statistics](../../Skills/Data%20Science/Statistics.md) (1)
> **Code Identifiers:** clean_housing (2), shrink_ints (2)
> **CLI Commands:** make (3)
> **Definitions:** is a  (2), stands for (1)
> **Analogies:** for example (1), such as (1)
> **Warnings:** be aware (1), note that (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Clean Ames](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/challenge-clean-ames?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/challenge-clean-ames?u=76281980&t=0)** - [Instructor] We're at our first challenge. These challenges are a great way for you to practice and I highly encourage you, in addition to following along with me in the code space, to try this challenge out. The science tells us that if you do the challenge, you're going to use a different portion of your brain and you will actually remember the content better if you do the challenge in addition to following along with the content. So I highly recommend that you do this challenge before proceeding to the next video. Let me explain what the challenge looks like. I want you to create a cell containing all of the imports necessary to run this notebook up to the current state, and then I want you to create a cell with the clean housing and the shrink functions that I showed you up above. I want you to also to add code to load the raw data and to create a housing variable from calling clean housing. Then I want you to take these cells that you just created and move them to the top of the notebook, restart your notebook, and make sure that these cells work. What this is going to do is give you a process that you can use in your own notebooks. I recommend following this process, once you've made a chain that is useful, put it in a function and then move that to the top of your notebook. This will allow you to come back to your notebook and quickly pick up where you left off. You don't have to scroll through a bunch of cells to figure out where you were. You can just have that at the very top, and then you can just keep working on your data analysis.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Cross-References:** next video (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Clean Ames](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-clean-ames?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-clean-ames?u=76281980&t=0)** - [Instructor] In this lesson, I'm going to show you how to do the first challenge. So here is my cell. I'm going to get my imports. My imports were up at the top already, so these are my imports right here. We used NumPy to inspect the types but we didn't really use it in processing our data but I will keep the numpy import there as well. That will allow me, if I'm using this notebook, to not have to worry about importing NumPy. Okay, the next thing that it wants me to do is use the clean_housing and shrink_ints function. So I'll just come up here and I'm going to copy these
>
> **[0:55](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-clean-ames?u=76281980&t=55)** and we'll paste those in here as well. I'll just put them in the same cell. I think that's fine. And then the next thing it says is add code to load the raw data and create a housing variable by calling clean_housing. So I'm going to do that, and that's this code right here.
>
> **[1:30](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-clean-ames?u=76281980&t=90)** And I'll put that at the bottom here. Maybe I'll just call this raw. Let me explain why I like to use the raw data and make a chain. Invariably, what I've found in my experience working and consulting, it's that when I make a report or I make a model, someone asks, "What's going on? Why is it doing this?" And if I have the raw data and a chain, it's really easy to trace back through that and understand what's going on. So I'm going to keep that raw data there. And this exercise asked me to create a variable called housing. So I'm going to make a variable called housing and say that is equal to clean_housing and passing in raw into it. At this point, I will have the original data and my cleaned up data if I need to compare those two. The next thing it asks me to do is move this cell to the top of the notebook. So I'm just going to click on the side here. I'm going to keep this cell here, but I'm going to also click on the side so I'm not in edit mode in my notebook and hit C to copy this. And then I'm going to scroll up to the very top of the notebook here. I'll actually put it below this heading section down here and hit V to paste it right here. So the next time I came to this notebook, I wouldn't have to scroll through all of those cells to find out what's going on here. It's just for me right at the top, the code to get my data so I can be off and running.
>
> **[3:12](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-clean-ames?u=76281980&t=192)** And finally, it says restart the notebook and make sure that those cells work. You'll notice that as I've been going through my notebook, I've been trying out my code, commenting it out, going through it, making sure that it works and you will want to do that as you're using your code. I'm going to hit this restart button to restart my kernel. It's going to pop up this warning. I'll hit restart. Note that when you do restart, you will lose all variables that you've created. I'm okay with that in this case. And then I'm going to run the cell and make sure that it works. Okay, it did not complain. I'll just make a new cell below this and look at housing. And it looks like it worked. In this lesson, I've given you a nice hint for how to make your notebooks a lot easier to use. And that is make a chain that cleans up your code, stick that into a function, and then pull that up to the top of your notebook so that when you come back to your notebook, you can easily start going and not have to scroll through and figure out what's going on with your code.

> [!info]- Semantic Content
>
> **CLI Commands:** make (9), find (1)
> **Code Identifiers:** clean_housing (3), shrink_ints (1)
> **UI Navigation:** click on (1), scroll up (1)
> **Warnings:** warning (1), note that (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 2. Exploring and Visualizing

[↑ Back to Table of Contents](#table-of-contents)

#### [Categorical exploration](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/categorical-exploration?u=76281980)

> [!transcript]- Transcript
>
> **[0:01](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/categorical-exploration?u=76281980&t=1)** - [Narrator] In this lesson, we're going to look at exploring our categories a little bit more. We're going to show the unique values of that but we're also going to visualize what's going on with that. Remember, visualization is a powerful tool you can use to tell a story very quickly and we're going to show how to do that for categoricals. I've got my cell that loads my data. So I've got the raw data and I've got the housing data. Remember, we can use describe to get summary [Statistics](../../Skills/Data%20Science/Statistics.md). By default, this is going to give us numerical summary statistics. It's not going to include the categoricals, or strings by default. Let's pull off a categorical. I'm going to use the zoning value. One of the things that's interesting about this is if you look at the dtype at the bottom here, it says that it is category and then here are the unique types for that category. Generally, when I have a category or a string column, I like to use the value counts method. We'll tack that on here. And this gives us a series. [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) uses series and data frames all over the place so it's good to get comfortable with those and also understand that these operations that we're doing with series and data frames are like tools in your tool belt and once you understand these basic operations, you can start leveraging them, combining them with other ones. So one of the things I might want to do with this is visualize what's going on here. This is a nice summary. We can see that the RL Zoning has 2,273 entries but maybe want to visualize that
>
> **[1:37](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/categorical-exploration?u=76281980&t=97)** and let's see how we can do that. The common visualization that we would use for this is a bar plot. Now look how easy it is to go from this value count to a plot. All I need to do is tack on the next operation in our chain which is .plot.bar. Let me explain how bar plots work in Pandas. Once you understand how they work, they're very easy to leverage. What a bar plot is going to do is it's going to take the index and put the index in the X axis. Note that the index here is not numeric. It is the unique entries from the MS Zoning column. That's perfectly fine. In fact, Pandas converts any index when you do a bar plot into a categorical or a string in the plot, which sometimes comes to bite us if you have dates in the index and you do a bar plot of that, it actually converts those dates into categoricals. For this case, it's fine. So the index is going to go into the X axis, and then in this case, we only have a single series. If you have a data frame, each column will be a bar and those bars will be located above the index. Let's run this and see what happens. This allows us to clearly see the RL is the most common entry for MS zoning, followed by RM and telling off very quickly. One of the things I don't like about this bar plot is that the labels are rotated 90 degrees. In fact, a common thing to do with a categorical count plot
>
> **[3:14](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/categorical-exploration?u=76281980&t=194)** like this is to convert it to a horizontal bar plot, and this again, is easy in Pandas. All we need to do is change the bar to barh, standing for bar horizontal. And we get something that looks like this. Again, telling the same story but a little bit easier for us to read. We don't have to tilt our head as much. In this lesson, I showed you how to summarize categorical columns. Again, value counts is going to be your friend and then once you know how bar plots work, it's easy to go from value counts to making a bar plot. And then generally you're going to want to make a horizontal bar plot. That is easy as well by using .barh instead of .bar.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (4), [Statistics](../../Skills/Data%20Science/Statistics.md) (2)
> **Definitions:** is a  (3)
> **CLI Commands:** rm (1), make (1)
> **Warnings:** note that (1)
> **Speakers:** - [narrator] (1)

#### [Histograms and distributions](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/histograms-and-distributions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/histograms-and-distributions?u=76281980&t=0)** - [Instructor] In this lesson, we're going to look at histograms and distributions for numerical columns. Again, we can start off with the describe method to get summary [Statistics](../../Skills/Data%20Science/Statistics.md) but oftentimes, we want to visualize this as well. I'll show you some ways to visualize these. Let's take the SalePrice column. Note that we can run describe on a single column or a series in addition to a data frame. When we run describe on a series, we get back a series. Again, in the index of this series, we have the descriptions of the statistical summaries that are coming back and the values, we have the corresponding values for each statistic. I'm just going to quickly go through these. Look at the minimum value. It looks like there's a house that sold for $12,000. That seems pretty cheap. I might want to check out that row or check out those low values. Also might want to go on that high side. We have a house that sold for 755,000. Make sure that that looks about right. Also, I could look at the mean and the median. Those look pretty close to each other and remember, our count is not the count of rows but the count of non-missing rows, but it looks like this is, in this case, the same as the number of rows. We don't have any SalePrice values that are missing. One of the ways to visualize a numeric column is to do a histogram, and again, this is very easy in [pandas](../../Skills/Software%20Development/Pandas%20(Software).md). We just say .hist, and here's the histogram.
>
> **[1:36](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/histograms-and-distributions?u=76281980&t=96)** You can see that this is somewhat skewed. It rises quickly to some value around $150,000 and then tails off and there's a long tail going out to around $700,000. We know that it goes up to $700,000 at least. If it didn't go up to that value, it wouldn't be on this chart. However, there aren't very many entries at that end. Now, you need to be careful with histograms because you can tell different stories with them. By default, pandas is going to give you 10 bins. People often ask, "Is 10 bins the correct number of bins?" I would say the answer is it depends, but it might not be. This is one of those things that people would write dissertations about the correct number of bins. I'm going to show you how you can change this. We can say bins is equal to 30, and we can get a little bit finer granularity in there. We could also come down here and say bins is equal to three. This probably isn't a story that we want to tell using bins is equal to three. From inspecting this data, it looks like somewhere around 150 is the most common value or the mode. When we do describe, we don't get a mode reported on there, but we did get a median and a mean, which were both around that value as well. You can see that for whatever reason,
>
> **[3:10](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/histograms-and-distributions?u=76281980&t=190)** there's a little bump here around 550 and there's another bump here around 600. Those are kind of interesting to me. They probably indicate that these houses sold at complete numbers like 600,000 or 550 rather than some value in between there. It's interesting also that we're not seeing that happen at other values where we're not seeing like big spikes at 300,000 or 200,000 or 100,000, and it also doesn't look like we're seeing bumps at the 50,000 level as well. We can bump up the number of bins and look at the finer granularity. That might yield useful information. It might just be noise. In this lesson, we took the SalePrice column and we did summary statistics on that. We also showed how to look at a very common visualization of that, which is the histogram. We showed how to change the number of bins in a histogram to tell a different story as well. Leveraging a histogram is a great way to get a feel for how the data is distributed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Statistics](../../Skills/Data%20Science/Statistics.md) (2), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (2)
> **Warnings:** note that (1), be careful (1)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Outliers and Z-scores](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/outliers-and-z-scores?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/outliers-and-z-scores?u=76281980&t=0)** - [Instructor] In this lesson, we are going to look at how we might find outliers or values that are at the extremes and I'll show two different ways to do that. I'll show one using our classic Z score and another one by using the inner quartile range, and we'll look at how we can use the [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) functionality to do that and make functions that you can use and leverage in your own code if you need to calculate values that tend to be on the extremes. So here is a function right here, calc Z and you'll notice that the input to calc Z, the first parameter is a data frame so a function that has the first parameter is a data frame is a candidate to use with the pipe method. This lets us leverage this in a chain. Let's run this and see what happens when we do this and I'll walk through the code. Okay, so what this is returning is a series and this is the Z-score of the sell price column. Let's just look at what these lines are doing. In this case, you'll note, even though I like chaining, I'm not using chaining all over the place. This is a case where I'm not using chaining, I'm calculating the mean of this column that I'm passing in here. So the DF that is coming in is whatever is passed in from the chain. In this case, this is the original housing, just coming in as the DF here and sale price is coming in as the column value.
>
> **[1:35](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/outliers-and-z-scores?u=76281980&t=95)** So we first calculate the mean of sale price, then we calculate the standard deviation of sale price and then the Z score is calculated by taking that column, subtracting the mean, and dividing by the standard deviation. We can leverage pandas to make this a vectorized operation and to make it work relatively easily. The Z-score gives us a quantification how many standard deviations we are away from the mean and generally an outlier is a value that has an absolute magnitude above three for data that has a normal distribution. One of the nice things about making this as a function is we can also use it in assign. Let me show you how this is going to work here. I've got this little chain. I'm going to comment out this and if you look at the column at the end here, you can see that there is now a Z-score column. This Z-score column is the Z-score of the sale price. If I want to find outliers, I can combine this with the query method. So now that I have a Z-score column, I can use this query saying, take the absolute value of the Z-score column and find out where that is greater than or equal to three. You can see the values that we're looking at here on the screen, none of those are equal to three. Let's run this and see what happens. You can see that this row 15 and this row 44,
>
> **[3:09](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/outliers-and-z-scores?u=76281980&t=189)** if we were to scroll over, presumably would have a Z score with an absolute magnitude greater than or equal to three, and indeed they do. Let's look at these values. These values all look like high-end houses or houses that are very expensive. These values all look like they're greater than three. It's possible that there are values less than three. Let's just quickly check and see if we can get those. I'm going to copy this and I'm going to just change it a little bit and say, where are we less than or equal to negative three? So it looks like there aren't any values that are outliers on the low end. Again, you need to remember that this is assuming that we have a normal distribution of the sale price. I'll show later on in the lesson how to calculate if that distribution is normal. Another mechanism for determining outliers is to calculate the inner quartile range and then values that are outside of that are classified as outliers by some statisticians. Here I'm making a function called calc IQR outlier. Again, this takes a data frame and a column. In this case, what it's doing is it's pulling off the series from that column and then we're saying calculate the 75th percent quantile and subtract the 25th percent quantile from that. That will give us a series of the inner quartile range.
>
> **[4:43](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/outliers-and-z-scores?u=76281980&t=283)** We can also calculate the median which is the 50th percentile, and the definition for an outlier using the inner quartile range is to take the median and subtract three times the inner quartile or the median and add three times that. So I'm making a variable called small mask and large mask and then I'm using the pipe to combine those together. What this returns is a boolean array, let me just show you that. So boolean array is a series that has true false values. Where these are true means that either this is true, it's low or it's high, and when we throw that into our index operation here, this gives us back the rows where this is true. And you can see by this calculation that 15 and 44 are both outliers. Let's show how to throw this into a column. In this example, I'm going to make a new column called IQR outlier and that is going to be the result of calling our function, passing in our data frame and indicating the sell price column as the column that we want to figure out whether those values are outliers or not. Now let's look at that result here. I'll scroll down and scroll over. You see this is true false value, so this is our boolean array. What I'm doing in this next part
>
> **[6:14](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/outliers-and-z-scores?u=76281980&t=374)** of the chain is just saying, filter out the roads where this is true in the boolean array and all of these IQR outliers should be true in this case. In this lesson, we made functions that allow us to calculate the outlier in two different ways. One by using the Z-score and another by calculating the inner quartile range and using that to determine whether values are outliers. These are functions that you can leverage in your own code. Feel free to take them and use them for outlier calculations.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (2)
> **Definitions:** is a  (9), means that (1)
> **CLI Commands:** make (4), find (3)
> **Env Vars:** iqr (3)
> **UI Navigation:** scroll down (1)
> **Speakers:** - [instructor] (1)

#### [Correlations](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/correlations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/correlations?u=76281980&t=0)** - [Instructor] In this lesson, we're going to look at calculating correlations. We'll look at the Pearson and Spearman correlation. I'll also show you how to make a heat map and give you some tips for formatting that, so you can quickly see where your correlations are. This is one of those calculations that's really easy to do in [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md). All you have to do is say .corr. Now, as I said that it was easy to do, it looked like it failed. Turns out that in this case, this is a Pandas 2 update. Pandas 2 is a little bit more strict about running operations on columns that aren't numerics. Let's scroll down to the bottom and you can see that the error is, that it says it "could not convert string to float: RL." That's a little hard to understand but basically what this is saying is that we passed in string columns and it didn't like doing the calculation on those. To get around this, what we're going to do is specify this parameter, numeric_only=True. What this returns is a Dataframe. In the index is all the numeric columns and in the columns is also all of the numeric columns. So we see the correlation between each numeric column and all the other numeric columns. This is using the Pearson correlation coefficient. That is a value between -1 and 1. The closer those values are to 1, the stronger the positive correlation, meaning as one value goes up, the other value goes up. The closer that value is to -1
>
> **[1:35](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/correlations?u=76281980&t=95)** the more negative the correlation is meaning as one value goes up, the other value goes down. If those values are around zero, that means that there is no correlation. As one value goes up, the other value might go up or down. They're not related. This is another one of those things that humans aren't optimized for looking at. I could give you this big table of data and ask you to find the biggest correlation and the smallest correlation and it would take a bit of time for you to do that. I'm going to give you some hints on how to get around that. Also, note that this is calculating the Pearson correlation coefficient, which assumes that there's a linear relationship between two values, ie. if you were to do a scatterplot of this, you should see a line. It's not always the case that there's linear relationships. Sometimes you have non-linear relationships. And you might even have a monotonic relationship meaning when one value goes up, the other value might go up, but it might be in a shape that is not a line. It might be like a logistic curve where it looks like a stretched out S. In that case, we might want to use the Spearman correlation coefficient. To do that, we specify the parameter, method='spearman'. And again, I'm going to say, numeric_only=True. And I'm also going to tack on this background gradient. Remember, we want to be able to see what's going on here easily, and one of the ways we can do that is leveraging visualization and color. So by default, when you do the background gradient, it puts on this blue background gradient
>
> **[3:09](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/correlations?u=76281980&t=189)** that immediately draws our eyes to the diagonal. And you can see that along the diagonal is a correlation of one. If you look at the diagonal, those are actually the correlations of one value versus the same value, meaning that as that value goes up, it goes up. Which in itself is not a particularly interesting insight and actually we kind of want to ignore the diagonal values when we're looking at this, but you could look off diagonal and see where the largest correlation is. Remember, this is a Spearman correlation coefficient so it's not necessarily there would be a straight line between those, but as one value's going up the other value's going up this makes it relatively easy to see that, for example, right here or right here, these are actually the same correlation. Year remodel or added to against year built. So as the year built goes up, also the year that it was remodeled goes up, and we can scroll through here to see if we see any others pop up. Here's one that has a particularly high correlation, garage year built, and year built. The default coloring used by background gradient, this going from white to blue, is okay for looking at positive correlations, but it's actually not great for looking at negative correlations. Why is that? This color map starts at a white value and it's going to pin the white value, so to speak,
>
> **[4:45](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/correlations?u=76281980&t=285)** at the smallest negative value. It looks like, for example, here it looks like pretty white and it's -0.4. We actually wouldn't want the smallest value to be -0.4. We would want it to be -1. And we actually don't want to use this color map that goes from white to blue. We'd want to use a different color map. So let's see if we can change this and use a better color map. I'm going to use this RdBu color map here. This is a red to blue color map. It's actually what's called a diverging color map. It goes from red through white to blue. And now we can look at the bluest things and the reddest things. The bluest things are the most positive and the reddest are the most negative. Again, we kind of want to ignore the diagonal. The diagonal by definition is going to be 1. So you'd want to look at the bluest off diagonal values and the reddest off diagonal values. Again, if you look at these, the reddest values look to be around -0.4, which is not what we want. So I'm going to give you one more hint. When you use a background gradient with a correlation heat map, you're going to want to pin the negative value. Let me show you how to do that. You specify vmin as -1. We can also put in vmax as 1, although by definition the diagonal will have a value of 1. And this tells a completely different story. If we look at this, you can see that those values around zero are indeed white which is what we want to see. Those are the values that don't have a correlation.
>
> **[6:17](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/correlations?u=76281980&t=377)** We kind of want to ignore those. In the other examples, those were distracting us. And we can look for the reddest values now and the bluest values. We can still see that that -0.42 looks like it is probably one of the reddest values. Let's just scroll through this and see. Here's another one that looks like it's pretty red, and here's one that is actually very red. This is an example of leveraging the power of color and visualization to help us read a table more effectively. In this lesson, we looked at correlations. We saw how we can easily calculate these in Pandas, and we saw how we can leverage color to help us understand these correlations and pull out the interesting values rapidly.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (4)
> **Definitions:** is a  (7), means that (1), is an  (1)
> **Versions:** 0.4 (3), 0.42 (1)
> **CLI Commands:** make (1), find (1)
> **Code Identifiers:** numeric_only (2)
> **Analogies:** for example (2)
> **UI Navigation:** scroll down (1)
> **Warnings:** note that (1)

#### [Scatter plots](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/scatter-plots?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/scatter-plots?u=76281980&t=0)** - [Instructor] In this lesson, we're going to look at scatter plots. Scatter plots are an excellent way to take two numeric columns and look at the relationship between those. I'm also going to give you some tips on how to make more effective scatter plots. Again, I think the key to understanding scatter plots is understanding the interface. The interface here is a little bit different than what we saw with bar plots or histograms. In the case of scatter plots, we are going to call .plot. And .plot has various methods for plotting on it. One of them is scatter, and the parameters that we specify for scatter include the column that we want in the X-axis and the column that we want in the Y-axis. Let's run that, and here's our plot. So this is the year built versus the overall condition. We want to see what's going on between these two. I'm going to calculate the correlation between those so you get a feel for what this looks like. We'll take housing. And I'm going to say pull off year built, and do a correlation with that with housing, and then look at overall condition. So that has a -0.36 correlation, meaning as one value goes up, the other value goes down slightly. We can also change this to a Spearman correlation coefficient and see if that changes that at all, and it actually makes it slightly more negative.
>
> **[1:36](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/scatter-plots?u=76281980&t=96)** So just from looking at this, I wouldn't necessarily say that as one value goes up, the other value goes down. So let's see if we can tease apart this plot a little bit. Let me tell you what I'm seeing when I look at this. I'm seeing a bunch of layers. What those layers indicate to me is that our values have been encoded at a certain granularity. In this case, this is the overall condition and these are whole numbers between one and nine. You can kind of consider these to be categorical in nature though they're encoded as numbers. I'm also seeing a bunch of dark values, and when I see dark values in scatter plots, that gives me pause. I'll generally want to use transparency to see what's going on there. So let's apply this alpha transparency. I'm going to pass in transparency of 0.1, and this tells a different story, I believe, than what we saw above. This is telling me that we have a lot of values at five and some values at six and seven. We could also look at a histogram of overall condition to see where those distributions lie, but we're kind of seeing that here. Again, compare that to what we saw here. I would not say that we had the most values at five just by looking at this, but when we change that alpha, we're seeing that. Let me give you a hint on how I like to set alpha. Let's maybe put alpha at 0.5, and when I see that there are a bunch of dark values,
>
> **[3:12](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/scatter-plots?u=76281980&t=192)** and there's still a bunch of dark values, I'm not seeing a transition around those. I kind of want to keep going down. So maybe I'll go down to 0.3. I can see it, five and six and seven. There are still a bunch of dark values, and let's put it down to 0.1. There I'm seeing that six and seven start to get faded out, so I think that's okay. I'm also seeing that five is not completely dark at some areas. So I think this is an okay value for alpha. Now I still am seeing those flat layers. How would I deal with that? I like to use jittering to deal with that. What is jittering? Jittering is shifting those values up by a random amount and I'm going to give you the code to do that right here. So this example, I'm taking my housing Dataframe and I'm overriding the overall condition column by taking the value of the overall condition and using NumPy to randomly add a value between zero and 0.8. And then I'm subtracting 0.4 to that, so I'm centering that. So this should give us a value between -0.4 and 0.4 on the positive side, but random. Now I'm going to plot this new overall condition. And this is what that looks like. You might ask why did I choose the value of 0.8 and 0.4? Well, if you look at the overall condition the overall condition is a whole numeric value, and I kind of want to see some segmentation between the five values and the six values.
>
> **[4:46](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/scatter-plots?u=76281980&t=286)** So if I would put that value instead of 0.8, put it up to one, then it would stretch all the way between one value and the next. So that leaves a little bit of breathing room between those, and then I subtract four so that it's centered at the value. Now, if you look at this, I think this tells a completely different story than what we're seeing above. This is telling me that we have a lot of values around the year 2000 in the condition five. Again, let's scroll back up to our original scatter plot and I don't know that you would've been able to tell that from looking at the original scatter plot, but by using some of these tricks such as adjusting the alpha and jittering, we are able to have some pretty big insight into what's going on here. This also helps us understand that correlation number that we looked at before. It looks like if you go from the left to the right the values are going slightly down, and that's easier to see after we've applied some of these tricks to our plot. Again, I'm going to go back to these traditional software engineering things that I do. If I want to use jitter in other places, I'm going to make it into a function so I can take advantage of it. In this case, I'm going to pass in a data frame as the first parameter, a column that I want to jitter, and the amount. I'm going to default the amount to 0.5, but we can bump that up to 0.8, and then I'll just test that function out here. Instead of using my code that I've commented out right here, I'm going to use my jitter function,
>
> **[6:20](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/scatter-plots?u=76281980&t=380)** and let's see if that works. Indeed, it looks like it does. One more way to visualize this that you get for free in [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) is to use a hex bin plot. You can see that this plot's little six-sided figures and then colors those based on the number of values that fall into those. In this case, you can see around the year 2000 the overall condition of five has a concentration of values. I personally don't like this plot as much as the scatter plot that I did above, but this gives you similar insights with very little code. In this lesson, we looked at creating scatter plots. Remember, scatter plots are a mechanism to look at the relationship between two numeric columns. We also looked at the correlation which is a quantification of that relationship and saw that we can explore that quantification by using a scatter plot, and adding some tricks on top of those. So the tricks that I like to use if I'm seeing that there's just a lot of dark spots, I'm going to lower the alpha until I start to see a transition between those spots. And then if I see things that are stacked up on top of each other in either columns or rows, I'm going to use jittering to spread those apart.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **Versions:** 0.8 (4), 0.4 (4), 0.1 (2), 0.5 (2), 0.36 (1)
> **Definitions:** is a  (3), is an  (1)
> **CLI Commands:** make (2)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Visualizing categorical and numerical values](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-categorical-and-numerical-values?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-categorical-and-numerical-values?u=76281980&t=0)** - [Instructor] In our previous lesson, we looked at comparing two numeric columns to each other, what happens if you have a numeric column and a categorical column? Let's look at how we can visualize and understand those. We're going to take two columns. We're going to take year built and overall condition. Now, both of these are numeric, but I'm going to say that both of them are also somewhat categorical as well. Some columns can be both numeric and categorical. You can think of a year as a numeric sequence that increases, but you can also think of events that happened in a year that would be categorical. Similar with overall condition. Overall condition is a number that represents the condition of a house. You could also think of the condition, one, being a house that is in very poor condition and a house that has a condition of nine to be very good condition. In this example, let's assume that year built is a categorical value and overall condition is numeric. How could we look at the relationship between those two? One of the things that we can do is we can use what's called a box plot. I'm going to say in the X axis, use the year built column and in the Y axis use the overall condition column. We get something that looks like this. It's a little bit unclear what's going on here. It says overall condition in the bottom and then on the left it looks
>
> **[1:32](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-categorical-and-numerical-values?u=76281980&t=92)** like we have the condition values from zero to nine. What is happening? This is basically taking all of the overall condition values and plotting a box plot for them. This isn't doing what we wanted to do, which was look at the conditions over the different categories of years. Let's see if we can figure that out here. I'm going to use some [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) code to manipulate my data. First of all, let's use what's called a pivot. You can see that I'm putting the year built into the column along the top, we have all the year built. I'm putting the overall condition column into the values but this is giving us as a data frame with the same number of rows as the original data but we see a bunch of missing values here. That's because this is a sparse data frame. What I want to do is I want to take those values for each year and basically push them up along the top and that's what this next line is going to do. I'm going to say, let's apply a function here. The function is going to take the values that are not missing and keep those in each column and then I'm going to reset the index and that'll gimme for every column, the values that are in there. That's what that looks like. You can see that all of the values are along the top. We still have missing values but those are just padding the bottom for the values that appear to be the longest, it looks like 2005 at least has 141 entries. In this case, when we call .plot.box,
>
> **[3:09](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-categorical-and-numerical-values?u=76281980&t=189)** it is going to take each column and it's going to stick each column in the index and for each column it's going to take the values in that column and do a box plot. In my opinion, the key to plotting in pandas is understanding how pandas moves from a data frame into a plot and it's not always the same. Here's the output of that. I don't think this plot is particularly useful. There's too much information going on here. It's hard to even read the X axis. What would I do to this? I would limit it to a certain number of years or I would bin years maybe into decades. Here's a chain. Let's walk through this chain and see what's going on here. Here's our pivot, which looks like what we had before. I'm going to do my apply, which is going to again, move the values up to the top, and instead of having almost 3000 rows, now we have 142 rows and now I'm going to use loc to filter out rows and columns. Loc allows us to specify rows that we want to pull out and columns that we want to pull out. In this case, this colon here is the row selector. If you just put a bare colon in the row selector, it is going to take all of the rows. So then we have a comma, that comma separates the row selector from the column selector. Here is the column selector. It looks like we're just going to take the years 1900, 1920, 1940, 1960, 1980, and 2000.
>
> **[4:46](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-categorical-and-numerical-values?u=76281980&t=286)** so at this point we can do our box plot on that and there are the box plots for those specific years. To me, it looks like this is saying the condition of the median house tends to go down over time slightly. Recall that this is only looking at the values from those specific years. Maybe we want to group by decade. Let's see how we could do that. I've got a chain that will do that. Let's walk through this chain. I've said this previously, but once you understand that pandas is basically a toolbox for manipulating data, if you can get your data into the specified [Forms](../../Skills/Web%20Development/Forms.md) by making these chains, that will give you super powers. Let's look at what this chain is doing. It is making a new column called decade. How are we calculating that? We're taking the year built column and we're doing an integer or a floor division by 10 and then we're multiplying that by 10, so if I do a floor division, let me show you an example of that, I'm going to do 2006 and then I'm going to do double divide 10 and that gives me 200. If I just do divide 10, that's going to gimme 200.6, so this is called floor division or integer division. If I did 1993, 10, that should give me 199 and then I'm just multiplying that by 10 to give us the decade, so this is giving us every decade.
>
> **[6:20](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-categorical-and-numerical-values?u=76281980&t=380)** Again, we can scroll over and just validate that that works. Can see that the decades are all multiples of 10. Now that we have that we can group by the decade and get summary [Statistics](../../Skills/Data%20Science/Statistics.md) for each of those decades. Now that we have that, we can pivot by decade. I'm going to put the decade into the columns and we get values that look like this. This is what we had before. We're going to push those values up using the same code that we had before and now we're going to do our box plot with that, there we go and it looks like we're seeing that same thing that the condition of the house goes down over time. I'm not a subject matter expert in this dataset. This might indicate that people with old houses take good care of them. Maybe they're antiques. Maybe the new houses are not up to par compared to ones that are built in the past. It is interesting to see this. I would naively expect the opposite, that the overall condition of a newer house would be better than an older house. I've given you a bunch of tools leveraging pandas to group by category and look at numeric values. Let's see if we can use that using some other tools. I'm going to use the Seaborn library. If you're not familiar with the Seaborne library, it is a library that is built on top of mapplotlib and pandas for doing statistical plotting and we can say, let's do a box plot. Here's our data and here's the column, in the X, we want year built in Y, we want the overall condition.
>
> **[7:56](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-categorical-and-numerical-values?u=76281980&t=476)** This allows us to get that plot that we got earlier but in one line of code. Again, is this plot useful? I don't think it's particularly useful. There's too much going on here. You can pull the documentation for this by just putting a question mark on here. There's actually a lot of documentation and we're going to use the order here to filter what is going on with the years. You can come down here and look at the output. Right now it's truncated but the order allows you to specify which things you want in the X axis. I'm going to specify a subset of those years. Let's run this again and now you can see a box plot for each of those years. Note that this is not doing any grouping. This is just pulling off the values that happened at those years. A trick that Seaborn has up its sleeve is it has other plots that you can do as well. One of those is the violin plot And this has the same interface as our box plot above. This is basically doing a kernel density estimation and flipping it on its side and putting another one that's mirrored on the other side of it, so you can see that, for example, there are a bunch of values here around six or seven for 1940. For 1900, there are a bunch of values around eight. Another plot that you might want to make is a boxen plot. This is a more modern box plot. In this case, it is showing you a big rectangle. The big rectangle is between the 25th percentile at the bottom and the 75th percentile at the top.
>
> **[9:32](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-categorical-and-numerical-values?u=76281980&t=572)** The line in the middle is the 50th percentile or the median. This next rectangle down below here is from the 25th percentile to halfway between that and zero so this would be the 12.5 percentile and basically these diamonds are outliers, above that. Oftentimes you'll see another rectangle and another rectangle beyond that. In this case, it looks like the 12.5 and in this case this is the 75th, and this is halfway between the 75th and the 100th, so that would be 87.5. Most of the values in this year, 1920, are between 12.5 and 87.5. You can see for 1900, most of the values lie between the 25th percentile and the 75th percentile. In this lesson, I gave you a bunch of tools that you can use to compare numeric values by category. Again, once you start understanding how to leverage pandas and these techniques in pandas, those are going to be nice tools in your tool belt to do some very powerful operations.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (8), [Forms](../../Skills/Web%20Development/Forms.md) (1), [Statistics](../../Skills/Data%20Science/Statistics.md) (1)
> **Definitions:** is a  (6), is called (1)
> **Versions:** 12.5 (3), 87.5 (2), 200.6 (1)
> **CLI Commands:** make (1)
> **Documentation:** the documentation (1)
> **Analogies:** for example (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Comparing two categoricals](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/comparing-two-categoricals?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/comparing-two-categoricals?u=76281980&t=0)** - [Instructor] In this lesson, we are going to look at how you might take two categorical columns and compare them and understand the relationship between them. Generally we're going to want to use some sort of cross-tabulation. We can also do a visualization, a bar plot, or a stacked bar plot On top of that. Let's look at some of our dtypes here. We can see that we have a bunch of categorical values. Let's pick some of them. So I'm going to pick the Overall Quality and the Basement Condition. Generally when you compare these, you want to make what's called a cross-tabulation. For each category in one column, you want to count the number of categories from another column and you make a matrix of the result. This is how we do this in [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md). Here's our original data. We're going to group by the two columns that we want the tabulation for. Pandas is lazy, when you do a groupby, it does not calculate anything. So let's do an aggregation to make it run a calculation. We're going to say size. Let's look at the output of this. This gives us a series and this is a series that has what's called a multi-index or a hierarchical index. In the left-hand side, we see Overall Qual and Basement Condition. Those are both in the index, so there are actually two indexes here. And then zero, zero, zero, three on the right, that's the values in the series. What I'm going to do now is a pretty powerful and sometimes confusing operation,
>
> **[1:33](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/comparing-two-categoricals?u=76281980&t=93)** I'm going to call unstack. What this is going to do is it's going to take that Basement Condition index. It's going to rotate it, pull it out, and put it in the columns so every value from Basement Condition will be a unique column. Let's run that and see what happens. Look at that, and here is our cross-tabulation. We have all of the Overall Quality values in the index and we have all of the Basement Conditions in the column. Because this is a common operation, Pandas actually gives this to us for free with the crosstab function. We can call crosstab and say in the index I want the Overall Quality column and in the columns I want the Basement Condition column. You can see that this is the same result that we see up above, but it's one line of code versus those four lines of code up there. I show you this because again, I think it's useful to have these powers to understand grouping, to understand size and unstack. Even though crosstab will give all of these to you for free, sometimes you do want to do these grouped by operations. Going back to what we've said previously, humans aren't great at looking at tables of data. This is a table of data. So generally if you have a table of data, you want to visualize it if you can or filter out the pieces of information that are useful in it. In this case, we're going to use style and background gradient to color it. I'm going to use a color map called Viridis which is a transitional color map.
>
> **[3:07](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/comparing-two-categoricals?u=76281980&t=187)** It goes from purple to yellow, passing through blue and green along the way. This tells us that we have a bunch of entries with Overall Quality of five and Basement Condition TA. So again, if we want to pull up the documentation, we can stick a question mark after background gradient and one of the things that you might want to do is figure out how it's doing this coloring here, and there is an access parameter here which determines how it calculates the color. Is it doing it by row, by column, or by everything in there? So you can pull up the documentation for that. Basically if you say axis is None, that's going to color by everything in the data frame. I'm also going to use loc here to reorder my columns. Note that these condition columns have a meaning. Ex means excellent, Gd means good. TA means typical, FA means fair, and PO means poor. So I'm going to order these from best to worst. I also have Missing and NA at the end here. Let's run this now and this tells us a little bit different story, that we have a bunch of values here that are in this typical value in the middle here. Another way to visualize this is to do a bar plot. I'm going to do a stacked bar plot.
>
> **[4:40](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/comparing-two-categoricals?u=76281980&t=280)** This is taking the results of my cross-tabulation and I'm going to say do a bar plot but stacked is equal to true. So let me walk you through what Pandas does when you do a bar plot. Again, it's going to take the index and it's going to put the index in the x-axis. In this case, the overall condition. We have multiple columns here so generally when we do a plot, it's going to plot each of those values above the index. So let's look at what it would normally look like, something like that. I'm going to say stack this and it's going to stack those values on top of each other. In this case, this is allowing us to see that for Overall Quality five, six, seven, and eight, we have a lot of TA entries. We can easily visualize that by looking at this plot. In this lesson, we looked at taking two categorical values and understanding how we can summarize those. Generally if we do that with a cross-tabulation, we said that a cross-tabulation is a table of data. We can use our techniques for visualization to understand that table better by either coloring the values in there or actually making a bar plot. Using a stacked bar plot is one way to understand a cross-tabulation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (4)
> **Definitions:** is a  (6), is an  (1)
> **CLI Commands:** make (3)
> **Documentation:** the documentation (2)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Explore Ames](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/challenge-explore-ames?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/challenge-explore-ames?u=76281980&t=0)** (electronic music)
>
> **[0:05](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/challenge-explore-ames?u=76281980&t=5)** - [Instructor] It's time for our next challenge. In this challenge, what I want you to do is make a scatterplot of the first floor square footage against the sell price. We want to see what that relationship is between the size of the house and how much the house sold for. You might consider using some of the tricks that we talked about as we visualize the scatter plot.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Cross-References:** we talked about (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (electronic music) (1)

#### [Solution: Explore Ames](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-explore-ames?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-explore-ames?u=76281980&t=0)** - [Instructor] Let's look at the solution for this. I'm going to take my housing data set. I'm going to call .plot.scatter, and let's choose to put SalePrice in the X axis, and we're going to put first floor square footage in the Y axis. Let's run that and see what it looks like. We get something that looks like this. So my first thought when seeing this is it looks like as sell price goes up, first floor square footage goes up. Also, it looks like there's a hard line here along the bottom edge, which looks like there's some relationship going on there that you can't have a house that is very small going for a high price. It looks like we're not really seeing that. There are some outliers here where we are seeing large houses that are cheap. It'd be interesting to know what's going on with these outliers that are sort of out here. It looks like there's a cone where most of the values lie. Now, another thought that I have, when I'm looking at this, I'm not seeing columns or rows necessarily, but I am seeing a big dark patch, and I don't like to see big dark patches in a scatterplot. Remember, one of the things I said you could do is adjust the alpha. Another thing you can do is if you have too much data, and you just keep lowering that alpha, but you're not seeing transparency
>
> **[1:33](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-explore-ames?u=76281980&t=93)** is to come in here and do samples. We could say, let's look at maybe 300 of those entries and see what's going on here. That might be another way to see where the majority of the data is lying. However, I'm going to go and use our alpha, and I'll start off by setting that .5. That looks okay. It's telling a different story, but you can see down here it's just completely dark. I don't like to see it completely dark. I like to see some transition in there. Let's go down to .2. I think that's looking better. I might even go down to .1, and I think that tells a different story. This is actually fascinating, if you look at this. I'm maybe going to try a little bit lower here. Okay, and I'm seeing like a cluster of data here, but I'm also seeing a cluster down here below that, which is kind of interesting. It looks like there's a pattern up here, and there's another pattern up here. This looks to me like there is a bunch of houses that for some given size are less expensive, and then there's a cluster of houses for the same size that are more expensive. Might be interesting to see what's going on there. Is that a different neighborhood? Are these built in different years? These are the the sorts of ideas I have when I see a scatterplot like this, and note that if I don't adjust that alpha, I'm not going to have that insight here.
>
> **[3:08](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-explore-ames?u=76281980&t=188)** I don't see that at all if I leave the alpha as the default value. I hope this was useful to you. In this example, I showed you how to plot a scatter plot and how to adjust the alpha to see some patterns that aren't apparent with the default scatter plot.

> [!info]- Semantic Content
>
> **Definitions:** is a  (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 3. Linear Regressions

[↑ Back to Table of Contents](#table-of-contents)

#### [Linear regression](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/linear-regression?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/linear-regression?u=76281980&t=0)** - [Instructor] In this lesson, we're going to look at [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md). The goal of linear regression is to take data and predict numeric values from that. So we're going to take our housing data and we're going to see if we can predict the sales price given the other numeric values from that. I'm going to show you how to do that using [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) and we're going to talk about splitting your data into a training set and testing set and evaluating how your model performs. We're going to be using the scikit-learn library. The scikit-learn library is a library that's very popular in academia, but it's also popular in industry. One of the nice things about the scikit-learn model is that it has a common interface. That common interface makes it very easy to try out different models. We're going to be looking at linear regression here, but we'll also be looking at some other types of models that can perform regression analysis. And because they conform to the scikit-learn API, it's very easy to swap them out and see how they perform. In order to run a model, we have to make two datasets. One is typically referred to as capital X and that is a matrix of data. Generally, it can be a [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) DataFrame. And if you look at our X here, we are saying, okay, take our housing data and then pull off the numeric columns and also drop the sales price column. Remember, we're going to try and predict the sales price column. This X is the features or the columns that we're going to try and use to predict sales price. So keeping sales price in there would be cheating
>
> **[1:35](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/linear-regression?u=76281980&t=95)** if we're training the model with the sales price and asking it to predict the sales price. Then the other data set that we need is the target that we want. In this case, it is the SalePrice column. I'm going to use a function from scikit-learn called train_test_split. And what that's going to do is, it's going to take our X and our Y data and split it up. We're going to split it up into a training set and a testing set. This is very important to do when you're using machine learning. The reason is you want to understand how your model performs. And if you evaluate your model on data that's seen before, it's pretty trivial to make a model that can perform very well on data that's seen before. All it has to do is memorize that data. So we're going to hold out some of our data and that's what train_test_split does. The output of train_test_split is four things. It's the X training data, the X testing data, the Y training data, and the Y testing data. Let's run this code and make sure that it works. Looks like it did work. Let's look at what X_train looks like after running that. Here is X_train. If you look at the index on the left side, you can see that it's not sequential. What this has done is randomly pulled out different rows to train our model on. If we looked at the X_test data, it would be the data that is not in the training data. The remaining rows in there. Here's the Y training data. And you can see that the index for y_train is actually the same as the index for X_train. However, this is a series. Remember, in scikit-learn, our X is going to be a data frame
>
> **[3:09](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/linear-regression?u=76281980&t=189)** and you can have a data frame with one column. But generally, data frames have multiple columns, but a series is only a single column. These are the values that we want to predict. Here is how we make our model. We're first going to say linear_model.LinearRegression. This makes an instance of the model. And then training the model is one line of code. It's calling lr.fit. We pass in the training data with the training label. In this case, it's y_train. And then the next line is a mechanism to score our data and see how it scores. And we're going to pass in X_test and y_test. And when we run this, it actually does not work. Let's look at our error and see what happens. And we got a value error. It says the "Input X contains NaN." And if you remember, we had a bunch of missing data in there. It turns out that linear regression does not like missing data. In fact, if you have missing values in your data, it will not work. Let's just double-check to see if there is missing data in there. And it looks like, for example, in Lot Frontage, there's missing data and a bunch of other columns have missing data as well. We talked a little bit about handling missing data previously. Again, my recommendation is to find an expert to understand why the data might be missing. That expert might be able to guide you into appropriate actions to deal with the missing data. In our case, I'm going to make this new function called clean_housing_no_na. And if you look at that,
>
> **[4:43](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/linear-regression?u=76281980&t=283)** what that's going to do is, it's going to add in this extra line into our chain here that's going to say, take the current state of the data frame after shrinking the integers, and pull off the numeric types, fill in the missing values with zero, and stick that back into your data frame. So housing2 after running this should have a bunch of zeros instead of missing numbers. Now, let me make an assign here. Is this the right thing to do? I'll say, probably not. We could have a whole course that talks about filling in missing values. For the purposes of this course, I'm introducing linear regression and not talking about all the ins and outs about filling in missing values. Let's, again, split our data and now let's run our model. And it looks like we get a score of 0.84. So the score that comes out of this is often called the R2 or R squared score or the coefficient of determination. This is a value between zero, typically, and one. The closer this is to one, the better the model is. What this represents is the amount of variance in the label or target that is explained by the features or columns in the data. So 84% of the variance in the label that we're trying to predict or that sales price is predicted by the data that we're passing into this. If we had another model that had a score of 0.9,
>
> **[6:18](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/linear-regression?u=76281980&t=378)** that would be a better model. If we had a model that scored 0.8, that would be a worse performing model. Question people often asks, is 0.84 good enough or is your score good enough? And the answer to this is, typically, an unsatisfying it depends. It might be good enough, it might not be. Further analysis and understanding what the predictions often mean and how far off they are can often tell you from a business context whether it would make sense to deploy a model or not. We can have a whole class on linear regression. For the purposes of this class, we're introducing it. In this lesson, we created a linear regression model. We were able to predict the sales price from the training data from the other numeric columns in our dataset and we saw that we got a score of 0.84 when we did that. We also saw that linear regression did not like it if we had missing values. So we took a shortcut. We filled in those missing values with zero.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (8), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **CLI Commands:** make (7), python (1), find (1)
> **Code Identifiers:** train_test_split (3), y_train (2), linear_model (1), y_test (1), clean_housing_no_na (1)
> **Versions:** 0.84 (3), 0.9 (1), 0.8 (1)
> **Definitions:** is a  (5)
> **Env Vars:** api (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Interpreting linear regression models](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/interpreting-linear-regression-models?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/interpreting-linear-regression-models?u=76281980&t=0)** - [Instructor] We just made a [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) model. Now we want to try and understand what's going on with the model. Let me tell you about a convention in scikit-learn. Again, scikit-learn is full of conventions. Once you understand these conventions, it makes it really easy to use the model, and it also includes a bunch of different models, so it makes it easy to try out different models and see how they perform. One of the conventions in scikit-learn is that if an attribute ends in underscore, it was an attribute that was discovered or learned by fitting the model. In this case, our linear regression model has an attribute called coef_. This attribute represents the coefficients. If you have taken a math class, you might have learned about the formula for a line, y is equal to mx+b, where m is the slope, and b is the intercept. It turns out that linear regression has the same thing. The coefficient is called the slope, and the intercept is the intercept. And this coefficient represents the weights that are multiplied by each of the values in the columns to make the prediction. We also have lr.intercept_, and this is the base value. In this case, this is saying that our housing value starts at it looks like $15 million. To create a prediction, we add up the intercept value with the linear combination of the coefficient, with the values from each corresponding column.
>
> **[1:36](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/interpreting-linear-regression-models?u=76281980&t=96)** That will give us what the prediction is. Here are the column names that correspond with those coefficients. We can stick those into a panda series. At this point, we can take this panda series and plot it. So we're going to do a bar plot with this. And if you look at this bar plot, what this is telling us is this is telling us the weights that impact the model the most. In this case, it is saying overall quality pushes the model towards a more positive price, and kitchen above ground pushes the model towards a more negative price, as that increases. You can see in the middle here, there are a bunch of the features or columns that have a small value. These columns are features that don't have a large impact on the output of the model. What I'm going to do is use some [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) code to just do a little bit of filtering here. So we're going to start off with our series of the coefficients, and then I'm going to use this pipe here to just get the values that have an absolute value of greater than 100, and we'll do a bar plot of that. So these are the coefficients that tend to have the most impact on our model. One of the nice things about coefficients is that they have both a magnitude and a sign. So again, if that sign is positive, if that value goes up, it tends to push the value of the final prediction up. If the sign is negative, if that value would go up,
>
> **[3:12](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/interpreting-linear-regression-models?u=76281980&t=192)** it would push the result down. In this cell, I'm pulling off the features that have the most impact on the model. It turns out that we might want to use these later on, and pull those out. You just pull off the index from the series. In this lesson, we looked at understanding a model. One of the nice things about a linear regression model is that it's completely explainable. What do I mean by that? I mean that you can take this intercept value, and you can take the labels that are passed into the model to make a prediction, multiply them by these coefficients, and if you sum those up, you get the prediction from the model. This turns out to be a nice feature that you can interpret the model. You can understand why it's making the predictions it has. Also, you can look at the magnitudes of these coefficients. The larger the magnitude of them, the more impact they have on the model. The coefficients that have a small magnitude mean that the feature is not having as much impact on the final prediction. In future lesson, we'll look at different models and compare and contrast those with linear regression.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (5), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **CLI Commands:** make (2)
> **Definitions:** is called (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (typing) (1)

#### [Standardizing values](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/standardizing-values?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/standardizing-values?u=76281980&t=0)** - [Instructor] In this lesson, we're going to look at standardizing values. We're going to talk first about what this concept of standardization means, and then we'll learn how to do this from scikit-learn. Again, once you understand the conventions of scikit-learn, this is not particularly hard. And then we'll look and see if this impacts our score. We're also going to reexamine our coefficients after doing that and see if those change as well. So standardization has a specific meaning to statisticians. And oftentimes, people say normalize, and when they say normalize, they mean standardization. What standardization means is that we're going to take our X or our dataframe and we're going to basically collapse the values or shrink them into a given range or maybe expand them into a given range and center them. More specifically, it means we give each column a mean value of zero and the standard deviation of one. Why is that? You could imagine that certain models are impacted by the scale of the data. And if you have a housing model that's predicting price, it might have one column that represents the number of bathrooms in a house, which might be like from one to five bathrooms, might be the maximum in a house. You might have another column that represents the square footage, which might be from like 200 for a mini house up to like 20,000 for a mansion. The scale of the 20,000,
>
> **[1:34](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/standardizing-values?u=76281980&t=94)** that's a couple order of magnitudes bigger than the number of bathrooms. And certain [Algorithms](../../Skills/Software%20Development/Algorithms.md) are impacted by the relationship between the magnitude of the columns. One of the ways to do that is using standardization. Let's look at how to do this using scikit-learn. There is a pre-processing module in scikit-learn. And you can make an instance of the pre-processing module. A StandardScaler is a transformer in scikit-learn. That means that it has at least two methods. One is fit and the other is transform. In the case of the training data, we're actually going to call fit_transform, which combines fitting and transforming. So fit trains the model, and then transform takes input and transforms the input to a given output. In the case of a StandardScaler, it takes data after it's been trained, the training tells the model, "Hey, this column should have a mean value of this and this column should have a mean value of this," and it knows how to shrink and move those means around. And then the transform actually does that transform for you. So here we're going to call fit_transform on the training data. This gives us a new X_train. And then on the testing data, we have to be careful here. We don't want to call fit again on the testing data, we just want to call transform on the testing data. So, once we've made a model, we want to evaluate it on data that it hasn't seen before. If we retrain the model, call fit on data that it hasn't seen before,
>
> **[3:08](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/standardizing-values?u=76281980&t=188)** we're kind of cheating. So we want to make sure that we only call transform on that testing data. Let's run that and make sure that it works. It looks like it did. Let's run our model now with our new data that's been standardized. And it looks like in this case, we get the same score. So our score did not particularly change by doing that. Let's look at our coefficients. And it looks like our coefficients are actually different now. I'm going to sort the values of our coefficients. You can see that we have some coefficients that are pretty negative and some other coefficients that are pretty positive. A couple of orders of magnitude above the others. Let's do a bar plot there with some filtering just to [Zoom](../../Skills/Software%20Development/Zoom.md) in on those. And it looks like we have basement square footage, basement unfinished square footage, ground living area, basement square footage 2. It looks like a lot of the features that impact our model are square footage features, or how big the house is. And intuitively, it makes sense that square footage would be one factor that determines how much a house sells for. And if we want to pull those values off of the index, we can. In this, I'm looking at the union of the features from our model that we had not standardized and the data that we did standardize here below. Let's look at the correlations between those and just see if there happened to be some correlations between all of these features
>
> **[4:41](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/standardizing-values?u=76281980&t=281)** that tended to be important in these two models. Again, I'm going to set a background gradient to this diverging color map. Red going through white to blue. And I'm also going to pin that negative value to -1. So we can look at the redest values and the bluest values here. So we do find some blue values that are off diagonal. That's because I filtered the columns here. So the diagonal here is not necessarily going to be the values that are one. But we can see the overall quality has a pretty high correlation with sell price, as does year built, year remodeled. Here I'll just scroll this over and look at the correlations there. You can see the garage area, wooden deck square footage, open porch square footage have a slightly positive correlation with our sell price. In this lesson, we looked at standardizing our data. Generally, you will want to standardize your data when you're doing [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md). We saw in our model that performing standardization did not impact this model, but that's not always the case. It is possible that standardizing your variables can have a significant impact on how your model performs. We did see that it actually changed the coefficients. And we had different coefficients emerging as more important when we standardized our data. In the next lesson, we'll look at using other models,
>
> **[6:16](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/standardizing-values?u=76281980&t=376)** and we'll see how other models perform to our linear regression model.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (2), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1), [Zoom](../../Skills/Software%20Development/Zoom.md) (1)
> **CLI Commands:** make (3), find (1)
> **Definitions:** is a  (2), means that (1)
> **Code Identifiers:** fit_transform (2)
> **Cross-References:** in the next (1)
> **Analogies:** imagine (1)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)

#### [Regression with XGBoost](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/regression-with-xgboost?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/regression-with-xgboost?u=76281980&t=0)** - [Instructor] In this lesson, we're going to use a very popular library called XGBoost to make a regression model as well. We're going to compare and contrast the performance and the interpretability of XGBoost with [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md). Let me quickly explain what XGBoost does. It makes a decision tree to make predictions and those predictions, in this case, the price of the house are probably going to be off from the actual values. So the boosting comes from making subsequent trees and the subsequent trees take that same input and try and fix the error. The error is called the residual. And you can add as many trees as you want. Each of those trees keeps fixing that error. I like to compare this to golfing. When you golf generally you don't just hit the ball once. You have multiple times to hit the ball and that's what this is doing. The first tree hits the ball, so to speak. It's not going to go in the hole probably. It'll be away from the hole by some amount, there'll be some residual and then the next tree will basically take a new club and hit the ball from there and try and fix the error, get it closer to the hole, so to speak. So this is like golfing and having multiple times to hit the ball to get it in the hole. That's a good analogy for XGBoost. Now, one of the nice things about XGBoost is that it uses the same interface, that scikit-learn interface.
>
> **[1:33](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/regression-with-xgboost?u=76281980&t=93)** So if you have data that works in scikit-learn you can easily use XGBoost and make a model. One of the downsides of XGBoost is that it is not completely interpretable. That is to say with linear regression, we could look at the intercept and we could look at that coefficient and we could explain how the model is working. With XGBoost, it's a little bit more difficult. We have a bunch of [Decision Trees](../../Skills/Artificial%20Intelligence%20(AI)/Decision%20Trees.md) and you could have hundreds of decision trees and going through each of those decision trees and looking at how they sum up at the end could be a little tedious. You could do it if you wanted to, but in practice no one does because it's too much work. Okay, one more thing that's useful about XGBoost is that you don't have to clean up missing values. In fact, it will work with columns that have missing values. In addition, XGBoost will also work with columns that are not numeric. So this gives it a lot of power. You can take your data and without doing a ton of pre-processing, you can throw it into an XGBoost model. It also doesn't even require your data to be standardized. So this makes XGBoost a nice model to try out and see how it performs. Here, I'm going to make x and y. Note that I am not filling in my missing values with zero, I'm just leaving those missing, and then I'm going to split my data. In this case, I will standardize it. It turns out that standardization does not impact XGBoost model one way or the other. It will perform the same because
>
> **[3:08](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/regression-with-xgboost?u=76281980&t=188)** if you think about how a decision tree is working, it's looking at a single column, it's not comparing one column to another, and so that relative size of the columns is not really important for a tree based model. Let's run that. Okay, it looks like that worked and we're going to come down here and make our model. Now look at this. This looks almost identical to what we had above when we made our linear regression model. We made an instance of the model. In this case, we're making an XGBoost regressor and then we called our fit with the training data and we called score with the testing data. Again, one of the nice things about scikit-learn is that it has a common interface and there are many models that have that interface. Look at this. Our score here of our XGBoost model is 0.89. So this is a better model, or it performs better than our linear regression model. A question you might ask yourself is 0.89 a sufficiently good model? And that's a different answer. The answer again, is this unsatisfying, it depends. It is a better model than linear regression in that it performs better, but we don't know if that performance is sufficiently good enough to impact our business. That would require us diving into how far off the predictions are and if we would make or save money by leveraging that model. Note that XGBoost has an attribute called feature_importances_ that is learned by doing the fit here. This is like the coefficients
>
> **[4:40](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/regression-with-xgboost?u=76281980&t=280)** but it's a little bit different. It doesn't have a direction, it only has a size. So in this case, it is saying that the feature that impacted our model the most was the overall quality. After that, it was the garage cars. And at a high level what's going on here, you can think we have a bunch of trees. The features that are at the top of the trees where it makes those decisions tend to be the most important features. So this is basically a way of measuring which features come at the top of the trees. You can see that there are some of these features at the bottom that have very low scores, meaning that those features don't really impact or drive the model too much. In fact, we could probably make a semi-decent model just by looking at the overall quality, how big the garage is, the number of baths, and the living area. In this example, I'm going to use categories in my XGBoost model. One of the things about our XGBoost model is that our XGBoost model doesn't like the pyarrow types. So I've got a chain here. I am selecting the numeric values. These are the pyarrow numbers and I'm converting these to N64s. These are the [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) values that XGBoost is okay with and I'm also dropping the sales price column. Note that I did not remove the categorical values, like I did with the other examples here. Then I'm going to split my data again and I'm going to say XGBoost Regressor.
>
> **[6:13](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/regression-with-xgboost?u=76281980&t=373)** I'm going to say enable categorical is true, telling it I want categoricals to work in here. And then if I want categoricals to work I also have to say that the tree method which is the way that XGBoost makes the trees is this string hist. And let's just run that. When you run that calling fit and score we see that we got a model of 0.91. So remember we went from 0.84 with linear regression to 0.89 with XGBoost with the numbers to 0.91 with XGBoost with the categories. So we do get a boost from using those categories indicating that those categories might be important. So let's ask XGBoost what are the important features of the categorical model? And it looks like the first categorical model that I'm seeing is neighborhood. If you remember back in our scatter plot we had it looks like two clusters. The neighborhood might have explained that. It might have been a fancier neighborhood that costs more for the same size house. So we are probably seeing that there is value including that neighborhood categorical into our model. Further exploration would be useful in validating that theory. In this lesson, we looked at using XGBoost, a popular library, to make our regression model. We saw that it performs slightly better out of the box. We saw that if we add categoricals, it performs even better. Again, one of the downsides of this XGBoost model
>
> **[7:49](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/regression-with-xgboost?u=76281980&t=469)** relative to a linear regression model, is that the explainability suffers a little bit. A linear regression model is completely explainable. An XGBoost model is kind of explainable but it's tedious to explain. That might be a trade-off. The performance versus the explainability might be a business decision that you need to consider whether you use a better performing model if you can't explain it. Think of a situation like a business loan. You might want to offer someone a loan, but if you can't explain why you're denying them a loan that might make the customer upset, causing them to churn. Finally, remember, because we're using that scikit-learn interface it makes it easy to try out these different models. I highly encourage you to not just try one model. Try out a few models and see how they perform.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (8), [Decision Trees](../../Skills/Artificial%20Intelligence%20(AI)/Decision%20Trees.md) (3), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1)
> **CLI Commands:** make (9)
> **Versions:** 0.89 (3), 0.91 (2), 0.84 (1)
> **Definitions:** is a  (2), is called (1)
> **Warnings:** note that (3)
> **Analogies:** compare this to (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Predict Ames](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/challenge-predict-ames?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/challenge-predict-ames?u=76281980&t=0)** - [Instructor] In this challenge, you're going to predict the sales price of the Ames housing data. What I would like you to do is make a [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) model, but I want you to only use the top five features from the XG Boost model that was not using categorical data. I want you to pull off those features. We saw how to do that in a previous lesson, and then I want you to train a linear regression model with just those features from the training set and then evaluate the model. What is the score or coefficient of determination or our squared value of that model?

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (2)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Predict Ames](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-predict-ames?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-predict-ames?u=76281980&t=0)** - [Instructor] Let's explore a solution to this challenge. The first thing I want to do is inspect my XG boost model and see if I can get those features from that. So here's my XG boost model. Remember, we discussed that it has an attribute called feature_importances_ that ends in underscore, again that underscore meaning that attribute was learned when we fit the model. So here's our feature_importances_, this is a NumPy array of the importance of each feature. This corresponds with the training data. So we should be able to stick this into a Panda series and get the names of that. So I'm going to say let's stick those values into a Panda series. And for the index I'm going to use the columns from the training data. Let's look at what that looks like. That's looking pretty good. I'm going to stick this into a chain. Now I want to sort these values. Let's just look at what that looks like when we sort that. It looks like we're going to want to take the values at the bottom. Those have the highest score. So to take the values at the bottom, I'm going to pull off the index. Let's just evaluate that and make sure that that looks correct. Okay, yeah, we want those values at the end and we want the last five values at the end. So to get those, I'm going to use a slice syntax and I'm going to say minus five colon. If you use that negative indexing, it's going to go back five.
>
> **[1:34](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-predict-ames?u=76281980&t=94)** And then the colon says go to, and because we didn't put anything on the right side of the colon, it's just going to go to the end. So it's going to take the last five values. Let's just run that and evaluate that. It looks like that did work. Okay, these are the features that we want to use. So I'm going to come up here and I'm going to store these features in a list. I'm going to say top five is equal to, and I'll stick those into a list. And then what I'm going to do is I'm going to make a new model down here. I'm going to call this the [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) model. I'm going to say it's the top five features and then I'm going to use my linear model. And I'm going to say linear regression. We'll make an instance of that. And then I need to fit my model. So I'm going to say linear regression, top five. We're going to call fit. I'm going to call it with X_train. And I'm going to use loc here to specify a subset of this. Now remember, loc takes a row selector and a column selector. I'm going to specify just a colon here for the row selector, that's a slice, that means take all of the rows. And then for my column selector, I'm going to pass in top five. Let's just run this right here and make sure that that works. And I got an error. It says it is missing a positional argument, y, so it did not work. Why didn't it work? 'Cause I need to pass in y_train as well. Let's try it again.
>
> **[3:07](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-predict-ames?u=76281980&t=187)** The challenge is asking us what is the score of this model? To get the score of this model we need to call the score method. This looks very similar to what we have above. I'm going to say lr top five score. I'm going to say X_test. And then again, I need to subset what I'm selecting. So I'm going to say take all of the rows but just the top five columns. And then we need to pass in the actual true values so it can evaluate how it did on its performance. And we got a score of 79. So our score dropped a little bit when we used a subset of these features, but we're only using five features. This might be a trade-off that you're willing to make to have a very simple model even if it performs slightly less.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linear Regression](../../Skills/Data%20Science/Linear%20Regression.md) (3)
> **CLI Commands:** make (5)
> **UI Navigation:** go to (2)
> **Code Identifiers:** y_train (1)
> **Cross-References:** we discussed (1)
> **Definitions:** is a  (1)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)


### 4. Hypothesis Tests

[↑ Back to Table of Contents](#table-of-contents)

#### [Exploring data](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/exploring-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/exploring-data?u=76281980&t=0)** - [Instructor] We're going to start looking at doing some hypothesis testing using [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md). In this lesson, we're going to look at the data that we're going to explore. Let's run this first cell here. We are going to use the stats module from the SciPi library. Let's make sure that we import that, and we're going to be looking at neighborhoods and comparing neighborhoods. Because neighborhood is a categorical value I'm going to use my favorite method on that, value counts, to look and see what we have in here. It looks like the two most popular neighborhoods are NAmes and it looks like probably college, CR College Creek, College something. So let's limit our analysis to those two neighborhoods. Another way to look at these categoricals is to use a group by, and then for our aggregation, do a describe. Look at what the output of this looks like. This is kind of cool. In the index, that because we grouped by neighborhood we have all the neighborhoods and in the columns, now we have hierarchical or multi-index in the columns, we actually have two levels of columns. For each numeric value, we have the outer column and then in the inner columns, under that, we have the summary [Statistics](../../Skills/Data%20Science/Statistics.md) for those. Let's limit those to the two neighborhoods that I'm interested in looking at. I'm going to call one College Creek and the other one North Ames. So let's look at those two rows and let's just pull out the sale price. Here we can see the summary statistics for those two neighborhoods. College Creek looks like it has 267 entries.
>
> **[1:35](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/exploring-data?u=76281980&t=95)** The mean sales price for College Creek is around $200,000, whereas North Ames has a mean sales price around $145,000. We might want to ask ourselves, is this data different or is it the same? It looks like it's somewhat different in that the mean is different, let's look at the median, the median actually looks different as well. The maximum values are different and the minimum values are different. It looks like from just looking at these summary statistics here that College Creek is a more expensive neighborhood than North Ames. Let me just show you an option that you can do sometimes when you do a describe that gets a little wide, if you want to often see a little bit more data you can use T there to transpose the data. Makes it a little bit more compact sometimes and fits in the screen a little better. We want to explore two neighborhoods, so we're going to look at this North Ames in this College Creek neighborhood. In this lesson, we did some summary statistics to compare the sales price of those two neighborhoods.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Statistics](../../Skills/Data%20Science/Statistics.md) (4), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **CLI Commands:** python (1), make (1)
> **Definitions:** is a  (2)
> **Speakers:** - [instructor] (1)

#### [Visualizing distributions](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-distributions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-distributions?u=76281980&t=0)** - [Instructor] In the last lesson, we looked at summary [Statistics](../../Skills/Data%20Science/Statistics.md) to understand these two neighborhoods. Let's use some visualization now to understand them as well. I'm going to pull out the North Ames and College Creek data into their own series so that we can look at them a little easier. I'll just use some [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) to do that. And the next thing I'm going to do is I'm going to make a histogram of both of those. So I want to plot these histograms on top of each other. What I'm going to do is call hist on our North Ames series. I'm going to give that a label as well, so a legend will appear in that, and that will return a map plot lib axis. I'm going to take that axis and pass it into the College Creek histogram call so that College Creek is plotted on the same axis or same plot as we have. I'm also giving that a label, and then after I do both of those histograms, I'm going to tell map plot lib to throw a legend on there with AX dot legend. This is the result of that. Now, note that both of these are fully opaque and because we plotted College Creek after we plotted North Ames, it is occluding some of that. It might be the case, probably isn't, but it might be the case that what College Creek is occluding, North Ames matches directly, but it's hard to tell because we don't know what's going on underneath College Creek there. To deal with this, I'm going to adjust the transparency, and we do that by changing the alpha parameter. So here I'm going to set the alpha to 0.7.
>
> **[1:36](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-distributions?u=76281980&t=96)** Remember, alpha is how transparent something is. This should let us understand what's going on below that. Let's look at this. From a quick look at the histogram, these look like different distributions. We already looked at the summary statistics. We thought that they were different distributions as well. It looks like this histogram is confirming that. Let's look at one more plot that's useful for looking at distributions, that's continuous distribution function. I'm going to show how to do that with pandas. Let me just walk through this chain here because we have a few steps of that. So we have our North Ames series. I'm going to convert this to a data frame by saying two-frame. Now it's a data frame with a single column. That's fine. A data frame is two dimensions, but it is possible to have a data frame with a single column. Now that I have a data frame, I can add a new column to it. I'm going to make a column called CDF, and that is going to be taking these values and ranking them. And the CDF column is going to be the result of calling the rank method on our data and converting that into a percentage. So it looks like the first entry there is around the 96 percentile. The next entry is around the six percentile of entries. The next thing I'm going to do is I'm going to sort the values by sales price, and we should see when we do this, the index should change. And indeed it does. The next thing that I'm going to do
>
> **[3:08](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-distributions?u=76281980&t=188)** is I'm going to plot this. I've said multiple times in this course, one of the keys to plotting in pandas is understanding how pandas makes plots. In this case, I'm just going to call the plot method, which will make a line plot. Now with a line plot, I can specify X and Y. If I don't specify X, it will use the index for X. I don't want to use the index for X, what I want in the X axis is the cell price and in the Y axis I want that percentage or that CDF. Let's un-comment that and run that, see what it looks like. We get something that looks like this. So if we created a CDF of College Creek and North Ames, and they had the same distributions, we would expect these plots to overlap each other, to trace each other, essentially. Let's see if they do. I'm going to create a function called plot CDF and it's going to take a series as the input and an optional map plot lib axis and a label. It's going to do the logic that I just showed above, converting a series to a frame, making a CDF column, sorting it, and then doing the plot, but it's going to return the series as the output. Let's run that and see what happens, just make sure that it works. You can see that this returned the series. You can see at the bottom, there is a series there, and then below that it had a side effect of making that plot. With that function in hand, let's now call that on both of our data sets. I'm going to call that with North Ames and College Creek,
>
> **[4:43](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/visualizing-distributions?u=76281980&t=283)** passing in the same map plot lib axis for both, and it should plot them on the same plot. I got an error, it says map plot lib is not defined. I ran into this because I restarted my code space and my map plot lib library was not installed. So if I were to do this in the real world, I would come up here to the top, where I've put my imports here, and note that I don't have map plot lib here, I would come in here and say, import map plot lib dot py plot as PLT. That will make it so, in the future, when I run this, I'm not going to have that issue. Let's run this again. You can see that it prints out a series here because plot CDF returns a series. Below that we should see our plot, and we can see that these CDFs do not overlap, giving us further evidence that these do not have the same distribution. In this lesson, we looked at comparing distributions by looking at plots. We looked at histograms and continuous distribution functions. We saw that, for these two neighborhoods that we're looking at, these don't appear to overlap, suggesting that they are not the same distribution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (4), [Statistics](../../Skills/Data%20Science/Statistics.md) (2)
> **Env Vars:** cdf (7), plt (1)
> **CLI Commands:** make (5)
> **Warnings:** note that (2)
> **Versions:** 0.7 (1)
> **Cross-References:** in the last (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Running statistical tests](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/running-statistical-tests?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/running-statistical-tests?u=76281980&t=0)** - [Instructor] In this lesson, we're going to use the scipy.stats module to run a statistical test to give us a numeric value as to whether these two distributions are the same. Imported that scipy.stats module up above. Scipy is a collection of different utilities for manipulating numerical data. One of those modules is the stats library. Let's just look and see what's in there. There are a bunch of things that you can do with this stats module. One of them is to run statistical tests, another one is to create distributions, there are also some plotting capabilities as well. In this case, we're going to run this ks_2samp function. Let's pull up the documentation for that. It says, it performs a two sample Kolmogorv-Smirnov test for goodness of fit. Basically what this is doing is it's testing whether these two distributions are the same. Let's look at what we need to pass into it. We need to pass in data1 and data2 and we already have those two series, so we should be good with that. What does this return? This says it returns a p-value. I'm going to view as a scrollable element here and let's scroll this up. This says, it returns an object containing two attributes, the KS statistic and a p-value as a result.
>
> **[1:39](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/running-statistical-tests?u=76281980&t=99)** And if we scroll down a little bit, we should see some examples. One of the nice things about a lot of the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) numeric libraries such as [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md), Matplotlib, Scikit-learn and Scipy is they have really good documentation. You can see an example of using it right here. We just create the two distributions and then we call ks_2samp with our two samples and this gives us this KstestResult. You can see that the first part here is a statistic and the next part is a p-value. In this case, we're mostly interested with the p-value. You can see that says, if the p-value is lower than a threshold of 0.05, we reject the null hypothesis in favor of the two-sided alternative, which is the data were not drawn from the same distribution. Here's another example. You can see that these two samples, the p-value is 0.5 and so because the p-value is 0.5, it's not below that threshold. We cannot reject the null hypothesis or we fail to reject that. The null hypothesis in this case would be that the two samples were from the same distribution, so we cannot reject that, in essence saying that we cannot prove that these were not from the same distribution, i.e. we think they are from the same distribution. Let's do this with our data now. I'm just going to pass in n_ames and college_cr into that and I'll print out the results there.
>
> **[3:12](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/running-statistical-tests?u=76281980&t=192)** We have the statistic and the p-value. The p-value is very low. Down here I have an IF statement that lets us check the p-value. If the p-value is greater than 0.05, we fail to reject the null hypothesis, i.e. these two are from the same distribution. If it is less than 0.05, we reject the null hypothesis, meaning that they are not from the same distribution. In this case, this is a very small number, it is certainly less than 0.05 and we are going to reject the null hypothesis that these two data sets are from a different distribution. This confirms what we saw with both our statistical summaries and our visualizations of these two distributions. In this lesson, we looked at using the scipy.stats library to do a statistical test to compare whether two samples were from the same distribution. The scipy.stats Library makes this really easy to run statistical tests such as these and you can leverage the scipy.stats library to confirm what you see when you do visualizations or summary [Statistics](../../Skills/Data%20Science/Statistics.md).

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1), [Statistics](../../Skills/Data%20Science/Statistics.md) (1)
> **Versions:** 0.05 (4), 0.5 (2)
> **Definitions:** is a  (4)
> **Code Identifiers:** n_ames (1), college_cr (1)
> **Analogies:** such as (2)
> **CLI Commands:** python (1)
> **Documentation:** the documentation (1)
> **UI Navigation:** scroll down (1)

#### [Testing for normality](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/testing-for-normality?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/testing-for-normality?u=76281980&t=0)** - [Instructor] In this lesson we're going to use the SciPi library to test whether a distribution is normal. We're also going to create a visualization to let us evaluate that visually as well. Typical test for testing for normality is called the Shapiro-Wilks Test, and it turns out that SciPi stats module has a function that will do that. And similar to the function that we looked at in the previous lesson, this will give us back a statistic and a P value. So let's see if northern aims is normal, We're going to run that and then we'll look at the P value there. It says that the distribution of the series is likely not normal, so presumably that P value was low enough, so we are going to reject the hypothesis which is that it is a normal distribution. Just print out that P value so you can see what it is, and it is indeed a small number. Another way to evaluate normality is to use a [Probability](../../Skills/Data%20Science/Probability.md) plot. It turns out that this module has that capability as well. You can pull up the documentation if you want to. The key thing here is that you pass in the data and I'm also going to pass in a parameter for this plot to tell us to use map plot lib to plot that. Here's my code. I am going to create a figure in an axis using map plot lib and then I'm going to call probplot and pass in the axes. Here's our probability plot. Let me explain how we interpret this.
>
> **[1:36](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/testing-for-normality?u=76281980&t=96)** We don't have a hard numeric value to say whether or not something is normal. By looking at this, this is a visual interpretation. We would like to see the blue dots track that red line, and if they do track that red line, that would be a strong indication that this is normal. You can see that it looks like it's overlapping the red line at a few points, but it's a little bit above on the small end. Then it goes below the red line and then it goes back up above the red line. From looking at this probability plot, I would say that this is not normal. It's close to normal on the left hand side, but on the right hand side it is not. Let me just plot a histogram of this so we can see what that histogram looks like and understand what's going on with this probability plot. Again, we're going to look at the north aims blue histogram, and we said that the left hand side of this looks somewhat normal, but the right hand side does not. Indeed, the right hand side has a tail where it's dragging out a little bit, and you can see that that tail is represented by this going up and above. Now, on the left hand side, it's going above as well. That indicates that it's actually a little bit short here. We would like to see that drag out a little bit more, so if this had a long tail on the left hand side, we would actually see these blue lines go below that red line. So that's the interpretation there. Let's actually repeat this with the College Creek data. This doesn't look normal either.
>
> **[3:10](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/testing-for-normality?u=76281980&t=190)** Let's look at what the probability plot looks like for that. So here's the probability plot for our College Creek. You can see that it's quite a bit above on the left-hand side. What that indicates is that it's not tailing off as much as we would like for it to be normal. You can see that it then goes below here. I am interpreting that as this bump right here. We would like that to go up a little bit more, but it's not, looks like along the middle there we're okay. Then we're going back down below, and then it's going back up above, indicating that this tail is a little bit too long. In this lesson, we looked at the ability to do a probability plot. A probability plot is a visualization that lets us understand whether our data looks normal. It does not have a numeric yes or no answer, but this is somewhat fuzzy. We want to see the blue dots tracking that red line, and I showed you how you can interpret when these values are off by comparing that to the histogram. This is a nice tool to have in your tool belt if you need to check if your data is normal.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Probability](../../Skills/Data%20Science/Probability.md) (8)
> **Definitions:** is a  (5), is called (1)
> **Documentation:** the documentation (1)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Checking square footage distributions](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/challenge-checking-square-footage-distributions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/challenge-checking-square-footage-distributions?u=76281980&t=0)** (upbeat electronic music)
>
> **[0:05](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/challenge-checking-square-footage-distributions?u=76281980&t=5)** - [Instructor] In this challenge, you're going to get a chance to play around with statistic tests. What I want you to do is get the first floor square footage from North Ames and College Creek and compare those distributions. Are they the same or not?

> [!info]- Semantic Content
>
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat electronic music) (1)

#### [Solution: Checking square footage distributions](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-checking-square-footage-distributions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-checking-square-footage-distributions?u=76281980&t=0)** - [Instructor] Let's look at a solution for this challenge. The first thing that I want to do is I want to get the square footage from both of these neighborhoods. Let's use our [pandas](../../Skills/Software%20Development/Pandas%20(Software).md) code to do that. Say from housing, let's do a query and I'm going to say neighborhood is equal to, and I'll put in double quotes here because my string has a single quote and NAmes. Close my double quote there, close my single quote and then I need another parentheses at the end for my chain. Let's run that and just scroll down and look at how many rows. Okay, there's 400 rows, so it looks like this is getting the Northern Ames or North Ames neighborhood. Let's pull off the first floor square footage from that. I'm going to use .loc to do that. So remember, loc takes a row selector. I'm going to take all of the rows from this and then a column selector. Now, if I want to get a series from this, I need to just put in a scaler value from this. That should give me a series. Let's run that and make sure. Okay, there's my series. If I wanted to get a data frame with just one dimension in that, I could put in a list of the columns and just have a single column in there and that should give me a data frame. Okay, so there's my data frame. I don't want a data frame. I want a series.
>
> **[1:35](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-checking-square-footage-distributions?u=76281980&t=95)** That looks like that's pretty good. Okay, so this is going to be my NAmes and then I'll call this _sf. Okay, now I'm going to just copy this and I'm going to leverage this for making the College Creek value here. Let's change this to college_cr, and I'm going to come down here and change NAmes to CollgCr. Let's run that and see if it works. It looks like it does work. Let's just look at the output of that. Okay, it looks like that did work as well. Okay, now I have my two datasets here. I want to see if these distributions are the same. So before I run my statistical test, I'm going to visualize these. So let's make a histogram to visualize these. I'm going to say ax is equal to n_ames_sf and then I'll do a hist here. There's the histogram of the square footage of the first floor. And let's plot on top of that my College Creek square footage. I'm going to say hist, and then I'm going to say ax is equal to that ax that I just created from North Ames. I'm going to get something that looks like this. In this case, I can see that I don't really need to play with the alpha here, but I can tweak it a little bit.
>
> **[3:10](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/solution-checking-square-footage-distributions?u=76281980&t=190)** I'll just change it to 0.7 just on College Creek. These do not look like the same distribution but let's quantify that using our summary [Statistics](../../Skills/Data%20Science/Statistics.md). Okay, so we're going to use the Kolmogorov-Smirnov test and we're going to get a statistic from that and we're going to get a P-value from that. And we're going to say stats. And this is the ks_2sample and we're going to pass in n_ames_sf and College Creek sf and we'll just print the P-value from this. Okay, remember, our null hypothesis is that the values have the same distribution. The P-value is less than 0.05, giving us an indication that these do not have the same distribution. In this lesson, we looked at how we can apply a Kolmogorov-Smirnov test to two samples. We also verified that by looking at the histogram and understanding that these two distributions graphically also did not look like they were the same distribution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (1), [Statistics](../../Skills/Data%20Science/Statistics.md) (1)
> **Code Identifiers:** n_ames_sf (2), college_cr (1)
> **CLI Commands:** make (2)
> **Versions:** 0.7 (1), 0.05 (1)
> **UI Navigation:** scroll down (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/python-statistics-essential-training-19258005/next-steps?u=76281980&t=0)** - Congratulations on completing the [Statistics](../../Skills/Data%20Science/Statistics.md) with [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) course. Now that you've acquired a solid statistics foundation, you might wonder what are the next steps? I highly recommend practicing the concepts and techniques covered in this course. Like any skill, the more you practice, the more proficient you become. Consider working on improving your [Data Manipulation](../../Skills/Data%20Science/Data%20Manipulation.md) skills. As we've seen, many libraries are easy to use if the data is in the appropriate format. One valuable resource for this is my book, "Effective [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md)." This book provides comprehensive examples and snippets for data manipulation using Pandas. It offers real world examples as well as exercises to reinforce your learning. Finally, apply the knowledge and techniques you gained in this course to your own projects and data sets. Whether you're analyzing survey data, conducting AB testing, or exploring customer behavior, using statistics and Python will enable you to extract valuable insights and make data-driven decisions. Completing a project will impress your boss and is also helpful in demonstrating your skills to potential employers. Thanks for completing this course and I wish you continued success in your statistical journey.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Statistics](../../Skills/Data%20Science/Statistics.md) (3), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2), [Data Manipulation](../../Skills/Data%20Science/Data%20Manipulation.md) (2), [Pandas](../../Skills/Software%20Development/Pandas%20(Software).md) (2)
> **CLI Commands:** python (2), make (1)
> **Speakers:** - congratulations (1)


## Instructor

- [Matt Harrison](../../Instructors/Data%20Science/Matt%20Harrison.md)

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/python-statistics-essential-training-4433355/codespaces)

## Skills Covered

- Python (Programming Language)
- Statistics

## Path Context

### In [Moving from Data Scientist to Data Analyst](../../Paths/Data%20Science/Learning%20Paths/Moving%20from%20Data%20Scientist%20to%20Data%20Analyst.md)
← [Data Visualization for Data Analysts and Analytics](../Data%20Science/Data%20Visualization%20for%20Data%20Analysts%20and%20Analytics.md) | **5 of 10** | [Deep Learning Getting Started](../Artificial%20Intelligence%20(AI)/Deep%20Learning%20Getting%20Started.md) →

### In [Master Python for Data Science](../../Paths/Database%20Management/Learning%20Paths/Master%20Python%20for%20Data%20Science.md)
**1 of 8** | [Advanced Python- Working With Data](Advanced%20Python-%20Working%20With%20Data.md) →

## Appears In

- [Moving from Data Scientist to Data Analyst](../../Paths/Data%20Science/Learning%20Paths/Moving%20from%20Data%20Scientist%20to%20Data%20Analyst.md)
- [Master Python for Data Science](../../Paths/Database%20Management/Learning%20Paths/Master%20Python%20for%20Data%20Science.md)

## Related Courses

_Courses sharing skills:_

- [Learning ArcGIS Python Scripting](../Data%20Science/Learning%20ArcGIS%20Python%20Scripting.md) — Python (Programming Language)
- [QGIS and Python for AEC](../Data%20Science/QGIS%20and%20Python%20for%20AEC.md) — Python (Programming Language)
- [Python Data Structures- Trees](../Database%20Management/Python%20Data%20Structures-%20Trees.md) — Python (Programming Language)
- [Using SQL with Python](../Data%20Science/Using%20SQL%20with%20Python.md) — Python (Programming Language)
- [Python- Working with Predictive Analytics (2019)](Python-%20Working%20with%20Predictive%20Analytics%20(2019).md) — Python (Programming Language)

---

[↑ Back to top](#)