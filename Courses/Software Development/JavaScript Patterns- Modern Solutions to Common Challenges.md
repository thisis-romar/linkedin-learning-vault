---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: javascript-patterns-modern-solutions-to-common-challenges
url: "https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges"
duration_minutes: 94
duration: 1h 34m
level: Intermediate
updated: 5/16/2025
learners: 400716
skills:
  - JavaScript
  - Coding Practices
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D4E0DAQF1L-ByY75vUw/learning-public-crop_675_1200/B4EZbKgjCRGQAY-/0/1747154252674?e=2147483647&amp;v=beta&amp;t=EX33dWi3R2clFIjO6kM56n79hLCQWdk3Pqftx4ryC7M"
linkedin_topic: Software Development
learning_paths:
  - '[Improve Your JavaScript Language Skills](../../Paths/Software%20Development/Learning%20Paths/Improve%20Your%20JavaScript%20Language%20Skills.md)'
prev_courses:
  - '[JavaScript- Patterns](JavaScript-%20Patterns.md)'
path_nav: '[{"path":"Improve Your JavaScript Language Skills","position":8,"total":8,"prev":"JavaScript- Patterns","next":null}]'
path_count: 1
tags:
  - course
  - topic/software-development
  - skill/javascript
  - skill/coding-practices
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/JavaScript%20Patterns-%20Modern%20Solutions%20to%20Common%20Challenges.md)

![JavaScript Patterns: Modern Solutions to Common Challenges](https://media.licdn.com/dms/image/v2/D4E0DAQF1L-ByY75vUw/learning-public-crop_675_1200/B4EZbKgjCRGQAY-/0/1747154252674?e=2147483647&amp;v=beta&amp;t=EX33dWi3R2clFIjO6kM56n79hLCQWdk3Pqftx4ryC7M)

# JavaScript Patterns: Modern Solutions to Common Challenges

> "This code works, but there must be a simpler way." JavaScript hides lots of power behind techniques with approaches or even names that are not obvious. Developers who want to create more streamlined code with JavaScript’s less visible—and often more powerful—options may need some extra information. In this course, web designer, author, and trainer Joe Chellman highlights hard-to-find techniques t

> [LinkedIn Learning](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges) | 1h 34m | Intermediate | 401K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (4 videos)
  - [Learning Javascript: Finding the utility in less common patterns](#learning-javascript-finding-the-utility-in-less-common-patterns)
  - [What you should know](#what-you-should-know)
  - [Using the exercise files](#using-the-exercise-files)
  - [How to work with CoderPad challenges](#how-to-work-with-coderpad-challenges)
- [**1. Improving on Loops**](#1-improving-on-loops) (5 videos)
  - [Pain point: Sequential "for" loops become tedious](#pain-point-sequential-for-loops-become-tedious)
  - [Solution: Three alternatives sequential "for" loops](#solution-three-alternatives-sequential-for-loops)
  - [Pain point: Processing arrays with loops is inelegant](#pain-point-processing-arrays-with-loops-is-inelegant)
  - [Solution: Use filter(), map(), and reduce()](#solution-use-filter-map-and-reduce)
  - [Solution: Transform a list of places](#solution-transform-a-list-of-places)
- [**2. Objects Can Confuse**](#2-objects-can-confuse) (6 videos)
  - [Pain point: Copied objects are not actually copies](#pain-point-copied-objects-are-not-actually-copies)
  - [Solution: JSON.stringify() and JSON.parse()](#solution-jsonstringify-and-jsonparse)
  - [Pain point: The value of "this" can be hard to follow](#pain-point-the-value-of-this-can-be-hard-to-follow)
  - [Solution: Fix "this" using bind()](#solution-fix-this-using-bind)
  - [ES6+ solution: Arrow functions](#es6-solution-arrow-functions)
  - [Solution: Creating a unique robot army](#solution-creating-a-unique-robot-army)
- [**3. Oh Functions, My Functions**](#3-oh-functions-my-functions) (7 videos)
  - [Pain point: Send extra parameters to an event handler](#pain-point-send-extra-parameters-to-an-event-handler)
  - [Solution: Use bind() a different way](#solution-use-bind-a-different-way)
  - [Pain point: Function signature doesn’t accept arrays](#pain-point-function-signature-doesnt-accept-arrays)
  - [Solution: Apply and spread](#solution-apply-and-spread)
  - [Pain point: Refactoring function signatures](#pain-point-refactoring-function-signatures)
  - [Solution: Objects for arguments](#solution-objects-for-arguments)
  - [Solution overview: Story generator](#solution-overview-story-generator)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps and thank you](#next-steps-and-thank-you)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Learning Javascript: Finding the utility in less common patterns](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/learning-javascript-finding-the-utility-in-less-common-patterns?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/learning-javascript-finding-the-utility-in-less-common-patterns?u=76281980&t=0)** - Do you ever hear about parts of [JavaScript](../../Skills/Software%20Development/JavaScript.md) and wonder, what on earth is that for? I know I have. Every programming language has useful features that are understood a little less. Especially with the pace of change in the JavaScript world, it's easy to lose track of features, new or old, that could be a game changer. This course comes from moments where I've found that, yes, indeed, there is some language feature, programming pattern, or even a new element of syntax that's purpose built for the problem I'm facing. Some of them are newer, some are less so, but either way, I'm very excited to share what I've learned. I'm Joe Chellman, and I've worked with JavaScript for many years, building websites, tools, and apps. I've read a lot about the language and it seems like there are always features that I'm aware of, but not using much, or at all. So let's go from, what's that for, to, oh, that's what that's for, together.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3)
> **Speakers:** - do (1)

#### [What you should know](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/what-you-should-know-14180830?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/what-you-should-know-14180830?u=76281980&t=0)** - [Instructor] Let's talk about the experience you should have coming into this course. And a few things to be aware of generally. To appreciate the pain points and have felt them you'll need [JavaScript](../../Skills/Software%20Development/JavaScript.md) experience, not necessarily a lot, but you should have been working with JavaScript regularly for at least a year or two. That being said, if you do have some experience, some of the pain points we'll discuss will be things that you may have a solution for. Your solution may be different. And after seeing what I'm showing, you might think that yours is better. That's fine, that's good even, share it with us in the course Q&A. Some of the example code in this course is set up in ways that are appropriate for examples, but that you wouldn't necessarily use on a live website. For example, there are some script tags in the head tag of the [HTML](../../Skills/Web%20Development/HTML.md) documents, which can block rendering. So, just remember that the code itself is sound, but the way the examples are set up is to support the goal of making a good and maintainable course, which can be different from websites for live use. A terminology note, ES6 is short for [ECMAScript](../../Skills/Web%20Development/ECMAScript.md) 6, which is itself an alias for ECMAScript 2015. All these terms are used interchangeably in this course and elsewhere. This is to differentiate it from ECMAScript 5, the version with broad support among current, and older web browsers, and other JavaScript environments. And finally, if you're looking at the way that the course examples are built and wondering about the CSS, we're using Tailwind, processed by PostCSS. As a result, the base CSS file might look a little strange. It's a nice way to build a site. Just know that the main CSS file might not look exactly how you expect.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3), [ECMAScript](../../Skills/Web%20Development/ECMAScript.md) (3), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** css (3), html (1), es6 (1)
> **Prerequisites:** set up (2), you'll need (1)
> **Definitions:** short for (1)
> **Analogies:** for example (1)
> **Warnings:** be aware (1)
> **Speakers:** - [instructor] (1)

#### [Using the exercise files](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/using-the-exercise-files-14186127?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/using-the-exercise-files-14186127?u=76281980&t=0)** - [Instructor] Most videos in this course have files to work with. These exercise files come in folders broken down by chapter, and then by video. There's a folder for each chapter that requires files and a folder for each video within that chapter where code editing takes place, which is most of the examples. There's a start.js file for where you'll start and there's an [HTML](../../Skills/Web%20Development/HTML.md) file that goes with that start file named index.html. There's also an end.js and end.html file pair with the code and markup as they stand at the end of that video. I have a special note for [Windows](../../Glossary/Service/Windows.md) users. Please make sure to decompress or extract the exercise file zip archive before you try to run any of the examples in a browser. Windows makes it possible to just open the zip file without extracting the files first, which is really nifty when you're working with one file at a time, but the examples in this course load lots of related files. So if you try to use the examples without extracting or unzipping, you'll have some very unhappy pages with broken links all over the place, very little working as it should. So please extract the files first. The code editor I'm using here is Visual Studio Code because it's free, available for all platforms, and it's good. I have a couple customizations enabled that would be particularly noticeable here other than a nice big font size. I've installed the extension for the code formatter Prettier. And I have format on save enabled here,
>
> **[1:35](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/using-the-exercise-files-14186127?u=76281980&t=95)** which means that every time I save the file, it will be run through the Prettier code formatter. I've also moved the sidebar over to the right side using this sidebar location setting. These exercise files do work best with an internet connection to load a font, but are designed to work totally offline without even a local web server. You should just be able to open a file directly from the exercise files in a browser and be able to work from there.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (3), [Windows](../../Glossary/Service/Windows.md) (2)
> **Exercise Files:** exercise files (3), exercise file (1), zip file (1)
> **File Paths:** start.js (1), index.html (1), end.js (1), end.html (1)
> **CLI Commands:** make (1)
> **Env Vars:** html (1)
> **Tools:** visual studio (1)
> **UI Navigation:** open the (1)
> **Definitions:** means that (1)

#### [How to work with CoderPad challenges](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/how-to-work-with-coderpad-challenges?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/how-to-work-with-coderpad-challenges?u=76281980&t=0)** - [Instructor] This course includes automated code challenges that appear when you click the challenge links in the course's table of contents. Each challenge includes instructions and a couple of code editors you can use to create and test your own solution to the challenge. These challenges are hosted by CoderPad and they appear in the same area of the course page where you watch the course's videos. We recommend using a desktop browser for the best experience with code challenges, but you can use the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning mobile app if you prefer. The code challenge has four areas: Instructions in the top left, a code editor for your answer in the top right, another code editor where you see how your code is used in the bottom right, and a console for output in the bottom left. You can use these drag handles, like this, to reallocate the space as you like. To get even more horizontal space for the code editors, you can collapse the course's table of contents on the left. Each challenge has instructions that include a description of the challenge, and the challenge's parameters and the desired result. Parameters are values that will be passed into your code and they have to be of a particular data type. The return value also has to be of a particular type, and you'll also see that noted in the instructions. The Constraints section, if included, has useful information about the parameters that will be passed in. The examples show different parameter values and what the result would be for each of those test cases. You'll create your answer in the top right code editor. There are usually comments in the starting code showing where to put your solution. When you click Test my code,
>
> **[1:35](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/how-to-work-with-coderpad-challenges?u=76281980&t=95)** you'll see a message indicating whether your code returned a correct result. Initially, it will be incorrect. If your code isn't successful, you can ask for help. Each of these variables show expected result and show hints can be set to true to give you some more information about the expected result under the current circumstances and any hints that are available. The code editor on the lower right shows how your solution is used. You can change that code to experiment with different scenarios if you'd like, like if we change this largest number to 33 instead and run Test my code, you can see that now the expected result is 33. Eventually, when you get the correct result, the output will change there as well. So let's get the correct result for this example.
>
> **[2:41](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/how-to-work-with-coderpad-challenges?u=76281980&t=161)** You can see, in this case, all available tests have passed and we get the message that we did it and the result is correct. Regardless of whether your answer is correct, you'll see messages in that console and if any messages are too long to fit, you can scroll sideways to see all the text. When you finished each code challenge, return to the course's table of contents, and click the next video to see my solution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Cross-References:** next video (1)
> **Speakers:** - [instructor] (1)


### 1. Improving on Loops

[↑ Back to Table of Contents](#table-of-contents)

#### [Pain point: Sequential "for" loops become tedious](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-sequential-for-loops-become-tedious-14183623?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-sequential-for-loops-become-tedious-14183623?u=76281980&t=0)** - [Instructor] Our first pain point addressed in this video will be sequential for Loops. These little things are very useful, even powerful, but they are also in my opinion, tedious and a little prone to error. The sticking points in regular sequential for Loops are not difficult, but they are tiny decisions and small pitfalls that we'd often rather not deal with. Let's take a second to break down what writing a for loop entails. So we can think about these decisions more carefully. Over here in my editor, I have the [JavaScript](../../Skills/Software%20Development/JavaScript.md) file that goes with this video. I have to arrays here. A regular one that just has a list of names comprising a band, and then another array of arrays with three bands listing each one's member's. Going to operate on this first simple array right now. So we're going to write a for loop here, which of course starts with for. The first thing I have to think of is my counter variable. So I have to declare it of course, I have to think about what I'm going to name it. I have to think about whether I'm going to make it zero indexed or not. Usually the answer is going to be yes, but sometimes I might want to start with one in case I have some use for one more than zero in the work I'm going to do inside the loop. But once I have that going to set the upper range for this thing, which will be the length of that band array. And there we go. So I'm counting forward all the way through the array. You've probably done this dozens, hundreds, thousands of times, but now that I've written this, I might not notice at first that I've accidentally created a global variable here.
>
> **[1:34](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-sequential-for-loops-become-tedious-14183623?u=76281980&t=94)** I have strict mode enabled. So it will get caught when I actually try to execute this code. But it's just another little mistake that I can make. Something that would be nice to avoid. And now inside the loop, I can do my work, but again, I have to think a tiny bit about whether to operate on the item directly using the index, or if I want to set it to a variable say member, because it's a member of this band. Now, usually yes, I am going to create a variable, but different people have different approaches. And it's something everyone has to decide every time we write one of these things. So you can see, there are a few decision points that I have to approach every time I write one. Now, is this a problem on the level of growing potatoes on Mars or something? No, but every little decision we have to make can introduce a small bug. Another way to look at this is it's a little micromanagy. What we ultimately want to do is perform some kind of operation on our data. Even something really simple, like just printing out all the names, but instead of writing code that looks pretty much like what we mean. There's all this extra stuff. The counter variable, explicitly tracking the length of the array, all that sort of thing. If we can get rid of all that stuff, I would argue that the code becomes more readable or expressive of the intent. Back here in my editor, things become even worse when I'm operating on a nested set of arrays. Now, not only do I have to keep track of all the same stuff I did previously, I have to create a new counter variable here. This is going to go up to the length of that whole band's array.
>
> **[3:07](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-sequential-for-loops-become-tedious-14183623?u=76281980&t=187)** Now I have to write another one and I have to create another counter variable. Let's call it C for now. I have to do all the same stuff over and over again. This will be up to band.length. If I create a variable that goes with that and make sure that I count all the way through here. So it's a lot of little things to have to write. And if I save that it gets reformatted and collapses those braces for me, we can see there are a lot of little decisions and a lot of little things to deal with even in simpler arrays and it gets even worse when we have to operate on more complex arrays. Coming up next, we'll see how we can deal with this in ways that are a little more readable and a little more expressive.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **CLI Commands:** make (4)
> **Cross-References:** coming up (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Three alternatives sequential "for" loops](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-three-alternatives-14183625?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-three-alternatives-14183625?u=76281980&t=0)** - [Instructor] In this video, we're going to address our, or okay, my previous concerns about For Loops with three solutions. A different kind of For Loop and then two array methods. I've loaded up the index.[HTML](../../Skills/Web%20Development/HTML.md) file that goes with this exercise here in my browser and I'm going to open the developer tools with Command option I, that works here in Chrome. And Control option I if you're on [Windows](../../Glossary/Service/Windows.md) or [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md). So I'm going to click here in the developer tools and just make this a little bigger. Using the [Zoom](../../Skills/Software%20Development/Zoom.md) command, which is Command + on a Mac, make this a little easier to see, and now we'll go to the code. All right, so we're going to start by rewriting this simple For Loop. The first way I'm going to do that is with a For of Loop. It's a different kind of For Loop, it's a little less verbose and more expressive. So instead of having to keep track of all this stuff and then set a variable inside the For Loop, I can set the variable directly right here. Then I use the keyword, of, and pass in the array.
>
> **[1:13](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-three-alternatives-14183625?u=76281980&t=73)** So that gets rid of all that other stuff, and it's very expressive, I can see that what I'm doing is iterating over members of my band. Nice. Now this is still a For Loop, it's a much nicer For Loop, but it's still a For Loop. We have other ways of operating directly on the array using array member methods. We're going to look at two. The first will be forEach. So this will be band.forEach and this method takes a call back function. So I'm going to start with just a regular anonymous function like this. And it's first parameter represents each item in the array as you're iterating through it. So I'm going to call it member here and let's actually do something this time. I will log to the console an introduction for each person. Introducing this member, I can save this. Now a few things to mention about forEach, it doesn't return anything, or rather it returns undefined and just like with anything else in [JavaScript](../../Skills/Software%20Development/JavaScript.md), you can pass it call back functions that are anonymous functions like this one. You could also define a function in advance and pass that in and if your environment supports it, you could also do arrow functions, like this. I'm going to stick with my original call back function here. We'll save this and switch over to the browser to make sure it actually works. So I'll refresh and there we go. I get everybody in the band introduced individually. There are various other signatures for this function that you can see in the documentation.
>
> **[2:47](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-three-alternatives-14183625?u=76281980&t=167)** But I specifically want to mention another way that you can call this call back function that's especially useful for our nest of arrays down here. So we're going to rewrite these For Loops using forEach. So this will be bands.forEach, I'll pass in another call back function. In this case, each of the items in this array represents a single band. So we'll call that band and then inside we do the same thing again, band.forEach. Give it another function. And this time, I've gotten down to the members. So we can copy and paste our little introduction here. Now we have that, so we'll introduce every single member of these bands one by one, but we can't necessarily tell who's who. So I'm going to pass in a second parameter for the index. This is a counter variable, just like we had with a regular sequential For Loop, but we didn't have to explicitly declare it unless we actually need it. So in this case, I'll log another message saying, introducing band and we'll pass through the index. So this is going to run one time per band and this one is going to run one time for each member of each band. Now we can save this, get a little bit of reformatting and we'll switch back to our browser and take a look. So we can see that first we iterate
>
> **[4:19](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-three-alternatives-14183625?u=76281980&t=259)** through our original sample array and then we get the introductions for the bands that are inside our nested array of arrays. Band zero, band one and band two. Now there's no decision here about whether we're going to make our counting variables zero indexed or one indexed, however we want to do it, it's always zero indexed, there's no thinking here except to remember that it's zero indexed. Okay, so we've seen a lot of forEached, let's take a look at our other method, which is very similar. So similar, that I'm just going to copy and paste our forEach example. This method is called every and it runs very similarly to forEach. I'm going to comment out my original loop here and comment out the one below. Okay, so I'll save the file and switch over and try this. We will quickly see the difference between the two of them. If I refresh, I can see that I only get the first item from the array. Does that mean that my array doesn't work? No. This is the difference. Whereas forEach will always execute through every single item in the array no matter what, basically, that's true. Sometimes there are ways to work around this, but it's best to think of forEach as one that will always operate on every single item no matter what, there's no stopping it. With every, you can stop it. And indeed, to make it operate on every single item, you need to explicitly return true from your call back function. So I'll save this and if we switch back to the browser again and refresh, now we get everything. As long as you're returning true,
>
> **[5:53](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-three-alternatives-14183625?u=76281980&t=353)** this works exactly like forEach. It has all the same ways that it can be called including index, and others, but you can also stop it from running if you get to a point where you want to stop. So in this case, let's say that I want it to stop after I introduced Ingrid Jensen. So if member equals Ingrid Jensen, we will return false instead. Otherwise, we're going to return true. Now I'll save this and switch back one more time. Refresh one more time and there we go, we just go through our first four members and then we stop. Not very egalitarian in terms of the band, but it illustrates the example. Unlike forEach, every does return a value. It just returns a bullion, so it's not of a great deal of use, but it does return a value if you happen to need it. So there are three methods for operating on these arrays using a more pleasant For Loop and then a couple of other ways. If you happen to be stuck supporting older browsers or other environments that don't support these more modern JavaScript language features, you do have options in a couple of popular libraries. First, there's the forEach method in Lodash. This works just like forEach in terms of the way that it's called and its call back functions. So we have our value and then we have our value and our key or index. One nice thing about forEach in Lodash is that it doesn't just work on arrays, it also works on objects. You can iterate through all the keys of an object without having to write a bunch of extra code.
>
> **[7:28](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-three-alternatives-14183625?u=76281980&t=448)** If you happen to be working on a project that includes jQuery, that also has a generic iterator function which is called each. It works very much like forEach does, over in Lodash, but it is a little different in the way that its call back function is structured. Instead of having the value being the first parameter, the index is the first parameter. So if you're using jQuery instead, you pretty much always have to pass both if you want to get the functionality of getting the value parameter for free. And just like with Lodash, it also works on objects as well as arrays. So that's three or maybe four ways of iterating through these arrays where you don't have to micromanage every single detail. The code becomes a little shorter and a little more expressive.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [HTML](../../Skills/Web%20Development/HTML.md) (1), [Windows](../../Glossary/Service/Windows.md) (1), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1), [Zoom](../../Skills/Software%20Development/Zoom.md) (1)
> **Code Identifiers:** foreach (16), jquery (2), foreached (1)
> **CLI Commands:** make (5)
> **Definitions:** is a  (2), is called (2)
> **Analogies:** just like (4)
> **UI Navigation:** open the (1), go to (1)
> **File Paths:** index.html (1)
> **Documentation:** the documentation (1)

#### [Pain point: Processing arrays with loops is inelegant](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-processing-arrays-with-loops-is-inelegant-14182725?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-processing-arrays-with-loops-is-inelegant-14182725?u=76281980&t=0)** - [Instructor] The next Pain Point up for inspection is processing arrays using loops. This comes up when you have a dataset, often an array of objects, that needs to be winnowed down, have calculations performed against it, or, otherwise, be programmatically processed. Writing a lot of for loops to do this can get quite verbose, and can result in code that's kind of hard to understand without very careful reading. Thankfully there's help. But, first, let's remind ourselves of the lack of joy that can come with processing arrays with loops. Here's a list of musicians, some of the instruments they play and when their first record appeared on [allmusic.com](https://allmusic.com), which I'm using as a crude measure of how long they've been active professionally. Our goal, here, is to extract all the drummers, change their names to uppercase and find out the total number of years of experience. The for loops are already written in here to save us a little bit of time. I've broken this up into three sections, based on the logical steps that we're going to perform here. The first thing that we're going to do is filter for all the drummers. So first we create our array, which is going to contain all those drummers, and then we loop over the musicians, extract a person they're from, and then if that person has drums in their list of instruments, which is an array here, we add that person to the drummer's array. And we're just using indexOf, here, and just confirming that it is not negative one. If it's negative one, they are not a drummer. Okay, so, at that point, we have our array of drummers. Our next step is to set all their names in uppercase.
>
> **[1:35](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-processing-arrays-with-loops-is-inelegant-14182725?u=76281980&t=95)** So we loop over that drummer's array, and then we collect a little variable for the drummer's name, which is just the index sub name, which is then processed by two uppercase. We're going to log that out to the console, and then set that drummer name back into the array, right at that key. Finally, we're going to get the total number of years of experience for all these drummers. So we're going to get a variable for the current year, and then here is the result into which we're going to insert our calculation as we go. We loop over all these drummers again, and this time, every time through, we just add on to that total years, the current year minus the drummer's first active year. Once we've done that, we have a total number of years for all these drummers. I should note that we do have some duplication, here, just because of the fact that I've broken these things up into logical units. If you were going to stick with using for loops for this task, you could gain some efficiency by adding all these things up into a single for loop, but to keep things comprehensible, we're breaking it up into these steps. All right, let's check the browser to see that everything actually works. So, over here, when the page loads I've looped through, and I have confirmed that I'm getting uppercase names for all of these drummers. And then here's the total number of years, which strikes me as pretty reasonable. We could go back through and check the math, but, generally speaking, it looks pretty good. So by doing it this way, once again, we're managing lots of counter variables,
>
> **[3:09](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-processing-arrays-with-loops-is-inelegant-14182725?u=76281980&t=189)** indexes within our arrays and so on. But now that we've established our loop-based baseline, we can move on and see how this works using a family of array processing functions that work very well together processing these arrays.

> [!info]- Semantic Content
>
> **CLI Commands:** find (1)
> **Code Identifiers:** indexof (1)
> **URLs:** [allmusic.com](https://allmusic.com) (1)
> **Definitions:** is an  (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Use filter(), map(), and reduce()](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-filter-map-and-reduce-14181691?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-filter-map-and-reduce-14181691?u=76281980&t=0)** - [Instructor] The solution to the pain of processing arrays with loops, is a small family of methods, filter, map, and reduce. You may have heard of these three, I know I had for quite a while before I finally learned what they were for, but it's okay. We're all here together now. All three of these functions, process an array using a callback function and return the processed array or other data. The original array is left alone. It's also worth noting that many other languages have methods like these as well. So the techniques, the mode of thinking especially can usually be applied elsewhere. Let's go through these one by one, then put them to use. Filter, well, it filters items from an array using its callback function as the test. If the function returns false, the item is removed, return true and the item is preserved. Map transforms each item of an array using a callback function. You'll often see these used in [react](../../Skills/Web%20Development/React.js.md) or other [JavaScript](../../Skills/Software%20Development/JavaScript.md) frameworks to print out a list of Dom elements generated from an array. It can be used to transform an array from one form to another, with the expectation that every item in the array will be processed, just like we saw with for each. Finally there's reduce, which is the most complicated of the three, but quite powerful. Reduce takes an array and reduces it to a single value. That value could be anything, a number of string, object, another array, as long as it can be contained in a single variable, it can be produced by reduce. You can think of reduce as a way to provide an answer when a single answer is required of a set of data. Of course the original array is untouched,
>
> **[1:34](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-filter-map-and-reduce-14181691?u=76281980&t=94)** so it can be called as many times as is required to get however many answers one needs. This is all fine in the abstract, but let's look at our code and rewrite the for loops using filter, map and reduce. Here we are back in the editor, I'm going to fold up the list of musicians for now. And our first task is to rewrite this filtering for just the drummers, using the filter method. So instead of all this, we're going to operate directly on musicians array. We're going to call filter. We're going to save this into the array called drummers. This is going to take a callback function and we'll call our variable inside musician. I guess we used person the first time, so let's go with person. Then we'll copy and paste this line, but we're going to treat this differently. Filter we'll preserve the item, if this callback function returns true. So, we want the drummers to stay in. So we're going to rephrase this as just returning this entire expression. This expression will evaluate to true if that musician has drums in their list of instruments. So we've cut down all of this, to just this. And it's very clear what we're doing here because we're calling a method called filter. So I will save this and we'll jump over to the browser to take a look and make sure this still works. So refresh this.
>
> **[3:07](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-filter-map-and-reduce-14181691?u=76281980&t=187)** And everything is acting just as it did before. I'm still getting this list of drummers, the other loops are processing things just as they did before, nothing has changed, great. Next, we're going to run map over this array of drummers, to do that transformation into uppercase. So we're going to call drummers.map and we have our callback function once again. This time because we know that everybody in here is a drummer, we'll call our variable drummer. And we're going to use the same transformation that we did before. And we'll operate directly on the drummer's name. And we'll add our console.log message here. And a very important thing when operating using map that you might forget when bringing code over from a loop is that you always need to return the value that you're operating on. So I'm going to return the drummer. If I don't do this, I'm going to end up with a list that's the same length as the original array, but has no values inside. Everything is going to be undefined and that's not going to work for us at all. So now we can get rid of this loop, and we'll save and try this again, over in the browser. I can see that I used a variable name that was in my for loop that doesn't exist in my callback function, so let's fix that. And here it is. We're just going to stick with drummer.name here, save again and come back and refresh. There we go.
>
> **[4:41](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-filter-map-and-reduce-14181691?u=76281980&t=281)** We're back to everything working as expected. One nice thing about working with filter, map and reduce is that if it's appropriate, you can actually chain these methods together. So, I could move my comment here inside the function that's doing the work. and then get rid of all this and chain map directly onto the result of the filter function. So I'll save again and prettier reformats this nicely for me, making it even more obvious what I'm doing here. That I'm operating on musicians, filtering for the drummers, and then mapping to convert all these names to uppercase and also logging a console message. So we'll confirm one more time by switching over to the browser and refreshing everything does still work, that is good. Now we can take our final step of rewriting this for loop using reduce. This is a case where a loop is probably easier to understand at first, but coming to grips with reduce opens up quite a bit of power. So thinking about what we're trying to do here, the data set that we're looking at is drummers. And the answer we want is total years. So we are going to get rid of this zero and we're going to operate on the drummer's array using reduce. Once again, call back function. This call back function has at least two important parameters. As we've seen with other callback functions like this, there are different parameters that can be passed in different numbers thereof, to help you achieve different results.
>
> **[6:14](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-filter-map-and-reduce-14181691?u=76281980&t=374)** With reduce, you're always going to need at least two. The first one is called the accumulator variable usually, which in this case, it's going to be total years. This is the one going to collect the answer for us as we're looping through our array with reduce. And then the second parameter is the value that we are operating on at that time. So this will be one of our drummers. As with map we need to return a value, only this time we're returning the modified version of the accumulator value. In this case, we're just going to add to it. But in other circumstances, we might be adding keys to an object, multiple items to an array, doing more interesting arithmetic or any number of other points disabilities. So let's copy what we had before up into our function here. And we're going to return the processed version of total years. Just to make this a little clearer, I'm going to wrap it in parentheses. And now instead of drummers sub D, I'm just going to look directly at that drummer. We're almost done with reduce. So we have our accumulator and the item that we're operating on. And finally, we need to pass a second parameter to reduce itself, not to the callback function within reduce, but to reduce itself. And that's the initial value. So in this case, we're going to start with total years being zero. And then every time through the loop, we're going to add on to total years, the number of years that that drummer has been active. So as you can see, this is a little more verbose
>
> **[7:49](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-filter-map-and-reduce-14181691?u=76281980&t=469)** than the original loop, but once you become comfortable with this, you'll be able to do a lot of stuff within a reduced function. So I can delete all this, save. And we'll go back to the browser and check it out. And once again, everything is exactly the same. So we've successfully refactored all our code from using for loops into using filter, map and reduce. So we've confirmed that everything works. Let's do a little something extra with reduce to see how it works. So here we're doing a relatively basic operation using reduce, where we're just totaling up the total number of years. But what if we want it to get the average number of years? Of course we could just do this by dividing total years by the length of the drummers array. But just for illustration purposes, we're going to do this a little differently. I'm going to copy and paste all of this. And this time, we're going to get the average number of years. I can pass in an additional parameter into the reduced callback function for the index of the current item. And the reason I'm going to do this is because I'm going to calculate, reduce inside my callback function. The first several times through the array, we're going to add onto our accumulator. So total years is going to be added onto like this, but we're not going to return the value just yet. What we're going to do is find out where in the array we are. Once we've gotten to the end,
>
> **[9:22](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-filter-map-and-reduce-14181691?u=76281980&t=562)** instead of returning the total years, we're going to do our division there and then return. So in other words, if our index is the last one in the drummer's array, so that would be the length minus one. We're going to return the average in this case, which will be total years divided by the length of that drummer's array. If we are at any other point in this array, we're going to return total years. So every time through this is acting as a sum function, but then the very last time, when we want to get our final answer, it's going to act as an average. Now again, this might not be the best way for readability for yourself or for other people on your team. That's something that it's a pretty personal decision, but it does illustrate that there's more that you can do with reduce, without going too overboard in complexity. So we'll save this and change this. These drummers have been active for an average of that many years. Okay, we save, get a little bit of reformatting and we'll go back to the browser and see. So we can see that these four drummers have been active for a total of almost 60 years for an average of just shy of 15 years. So as we've seen filter, map and reduce, allow for succinct and powerful processing of arrays. I'll grant that reduce can be a little tricky at first,
>
> **[10:55](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-filter-map-and-reduce-14181691?u=76281980&t=655)** when this mode of working is quite new. So, it might not be the right choice in every case, but it's certainly worth getting comfortable with it and adding it to your toolbox.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Definitions:** is a  (4), is called (1), in other words (1)
> **CLI Commands:** make (2), find (1)
> **Cross-References:** go back to (2)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: Transform a list of places](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-iterate-differently-25798165?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-iterate-differently-25798165?u=76281980&t=0)** - [Instructor] Let's go over my solution to this challenge. Remember that the brief was to use array reduce to produce an array of only the states with no duplicates. So to start off, we're going to return places.reduce,
>
> **[0:27](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-iterate-differently-25798165?u=76281980&t=27)** and I'm going to pass a function with two parameters. States will be the accumulator to capture every non-duplicate state as I go, and then the place that we're analyzing. And we want to make sure that we're returning the accumulator as we go, and that we start out with an empty array. Now we just need the logic to decide whether to add a state to that list or not. So what we're going to do is check if states.index of place.state
>
> **[1:18](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-iterate-differently-25798165?u=76281980&t=78)** is negative one, meaning we haven't seen it yet in this list. And if that's true, we're going to push place.state onto our list. Now let's test the code and see how it goes. There we go. We've produced a list of all the states. It's unique, and we used reduce.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 2. Objects Can Confuse

[↑ Back to Table of Contents](#table-of-contents)

#### [Pain point: Copied objects are not actually copies](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-copied-objects-are-not-actually-copies-14181695?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-copied-objects-are-not-actually-copies-14181695?u=76281980&t=0)** - [Instructor] The next several sticking points we'll look at live in the world of objects. This first one is about trying to make copies or clones of objects. If you've ever tried to do that, you surely know where this is going. If you haven't run into this tricky aspect of [JavaScript](../../Skills/Software%20Development/JavaScript.md) yet, here's the issue. In this little block of code where template is an object with one property called item, you might think that assigning myCopy to template will create a copy that I can modify by, say, setting item in that copy to two. Instead we end up with apparently both objects being updated. The property item has the value of two in both template and myCopy. The reason we get this strange behavior is that assigning a variable to an object doesn't create a copy with the same value as it does for other simple data types like numbers, strings or booleans. Instead the new variable merely refers or points to the same location in memory as the original object. We therefore call the variable a reference or a pointer. So when you try to update a value in the apparent copy, we're actually updating the original and any other variables that were assigned to the same object in memory. Let's try this with an example in the browser. Here I am in Chrome. I'm going to open the developer tools with command option I or control option I depending on which platform you're on, and I have defined in the source code for this example a variable called kitty. We just type that in and hit return.
>
> **[1:34](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-copied-objects-are-not-actually-copies-14181695?u=76281980&t=94)** We can see its value. Kitty's name is Squiggy, it's an orange cat and is five years old and here's the chip ID in case the kitty gets lost. So let's say that we're going to add a new kitten to the household. We'll start with the same values as kitty. So we'll say we'll have a kitten here and we're going to make it the same as kitty. We'll check its value here. Alright, so far, this is what we're expecting. We're going to change the kitty's name to, say, Moose, and the kittens age will be one. That's all we'll change for now. Now if we write out kitten again, everything looks right here. Moose, age one, but, if we look at kitty, it is the same. So now that we've seen that simple assignment is not enough to make independent copies of objects, what can we do? We'll take a look at a few options next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **CLI Commands:** make (3), cat (1)
> **Exercise Files:** template (3), source code (1)
> **Code Identifiers:** mycopy (2)
> **UI Navigation:** open the (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Solution: JSON.stringify() and JSON.parse()](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-json-stringify-and-json-parse-14184434?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-json-stringify-and-json-parse-14184434?u=76281980&t=0)** - [Instructor] In this video, we'll address how to make actual copies of objects in [JavaScript](../../Skills/Software%20Development/JavaScript.md). There is, as far as I know, anyway, no completely simple answer to this question, but there are good ones. We just need to consider what's in our objects before we choose one. So I'd recommend loading up the index.[HTML](../../Skills/Web%20Development/HTML.md) file for this example in your browser before we start. Back here in my editor, I have the JavaScript file that goes with the example, and I've made my kitty object a little more complex here. I have the same properties as I did before, basic strings and a number, but I also have a key called offspring, which contains an array. This is all the kittens that this kitty might've had. An object for their best friend, which let's say here is a parakeet named crackers, and then a function called meow, which will return a string where the kitty says meow, and we get their own name. So we have a few different types of data that are going to get copied with this object when we attempt our various cloning procedures. We'll start with one that's fairly simple, and that you see in a lot of places, this is taking the entire object and running it through a couple of [JSON](../../Skills/Web%20Development/JSON.md) methods. So we'll create a new kitty, maybe call it mudge. And we're going to stringify the original object. This converts this entire object into a string representation in JSON. And then we're going to immediately take that same string and re-parse it.
>
> **[1:35](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-json-stringify-and-json-parse-14184434?u=76281980&t=95)** This will clone everything that is clonable using JSON. A lot of times you'll see this described as a way to deep clone an object, which is to say you get every single thing, no matter how deeply nested your object might go. And there is truth to this, but there are drawbacks. Because it will only work on things that are possible as JSON, things like this function, won't come through. Let's try this now. I'm going to set mudge as name and we'll change the chip ID as well. But this time, instead of using console log messages, I'm going to drop into the debugger using the debugger keyword. This will only work if I have the developer tools open at the time when this executes. So back here in the browser, I'm going to refresh, and there we go. This has popped up the sources panel in Chrome, revealing my debugger statement. And now I can take a look at the values of these right here in the debugger. So I'll give myself a little more room here. And I can see that kitty still has the name squiggy. All these data seem to be just as they were before. Best friend offspring, the meow function. Let's take a look at mudge. Mudge has a best friend, chip ID, offspring, but we do not have that meow function. So you need to be careful of certain data types,
>
> **[3:08](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-json-stringify-and-json-parse-14184434?u=76281980&t=188)** functions, regular expressions, date objects, any of these things that can't be captured in a normal JSON serialization to a string, will get lost if you clone an object this way. If you don't have types of data like that, if you're basing things on strings and objects and the usual things that you can parse in JSON, this is a fast way to get the entire object and make sure that every single property is unique, completely independent of the original object. Let's try another way. This time, I'm going to use the assign method on the global object, and let's create another cat called knuckle, this way. Object assign can be used for various different purposes, but for making a clone, we first parse it a new empty object, and then the template that we want to base our copy on. So it looks like this. And just like before we will give this knuckle kitty a new name, and we can re-implement the meow function, to prove that this actually works too with this method. So in this case, instead of saying meow, we'll say, yeow, and we'll drop into the debugger to see how this works. Let's switch back to the browser and I'll stop this and refresh it. Now I can see all my kitties so far. Here is mudge, who does not have that meow function.
>
> **[4:43](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-json-stringify-and-json-parse-14184434?u=76281980&t=283)** And here's the cat called knuckle, which does have meow here. And if we try and execute this, we get a different result than if we execute the same function on our original object kitty. So knuckle says, yeow, kitty says, meow. This looks like it works great. What's the drawback? If I also try to update knuckle's best friend. Give knuckle's best friend name of bones, say real tough guy. Now save this and switch back to the browser. Stop this and refresh. Everything looks good here. Knuckle's best friend is named bones. Let's check out our original object. We have another reference here. This is what's often called shallow copying, where some properties are shared with the original object, it's not a complete deep clone. So if you have nested objects inside your original, this is something to be aware of. If you happen to be using EA6 or greater, you may have seen this spread operator, which works basically just like object assign. Only it looks like this. The three dots are a spread operator, and you're taking all the properties of the original object and spreading them out into a new object that you assigned to knuckle. But this has exactly the same behavior in terms of nested objects that object.assign has. So if all those fails,
>
> **[6:20](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-json-stringify-and-json-parse-14184434?u=76281980&t=380)** there's no shame in going out and using a library. If you need to make a complete clone of an object, copying every single thing, a good method for doing this is the clone deep method in low dash. I've imported this into the project so we can try it. Let's have another kitty, we'll name it, comma. And we're going to use the underscore method here from low dash and do a clone deep of kitty. I'm also just going to pass this in from the global namespace right into my function here. So now I can set comma's name and we'll try and sit in comma's best friend properly. Let's say that creature's name is dash, we'll drop into the debugger again here. Saving and switching back, I can now stop this reload one more time, and here's my menagerie. So the original kitty has had their best friend's name updated by knuckle. But comma has updated the best friend's name and it stays dash here without affecting anyone else. With these various methods, you can make clones of most objects. If you graduate to a level where you have to consider the prototype chain, whether custom properties are innumerable and other more advanced matters, you'll probably be looking into this topic more deeply. But I expect that these options will cover the needs
>
> **[7:52](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-json-stringify-and-json-parse-14184434?u=76281980&t=472)** of most developers, most of the time.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (6), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** json (6), ea6 (1)
> **CLI Commands:** make (4), cat (2)
> **Definitions:** is a  (2)
> **Analogies:** just like (2)
> **Warnings:** be careful (1), be aware (1)
> **File Paths:** index.html (1)
> **Exercise Files:** template (1)

#### [Pain point: The value of "this" can be hard to follow](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-the-value-of-this-like-and-always-14183629?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-the-value-of-this-like-and-always-14183629?u=76281980&t=0)** - [Instructor] In every [object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) language I've used there's a way for an object to refer to itself. That way is usually through the keyword, this. In this video, we're going to see how, this, can get tricky. And later, a relatively easy way to resolve anything confusion. Over here in the browser, we have our example file. Main thing to note is that we just have this button. And we're going to wire up an event listener to this button to take some action. Over here in Visual Studio Code, I have that [HTML](../../Skills/Web%20Development/HTML.md) file pulled up. The only thing to note is that we have our go button here. And its ID is go. Then here in the script file, we're pretending to run a website that uses the popular [PHP](../../Skills/Software%20Development/PHP.md) CMS, [Drupal](../../Skills/Web%20Development/Drupal.md). Drupal uses an eponymous global [JavaScript](../../Skills/Software%20Development/JavaScript.md) object that is its name, Drupal, which has a property called behaviors. If we want to add some script-based behavior, we add an object to that behaviors object. And to find a method called attach inside that object. Down here, is a snippet of code that works pretty much just like Drupal does. It looks for any behavior objects that are defined on that global. And invokes the attach method that's on there if it's available. So the point here is that we have a global object where our custom code lives. And it's nested a couple of levels deep global object. In our case, it's called myModule. And it has a few properties here, the URL property and a getData method. All it's doing right now is locking a console message. But the idea is that if this were implemented in full, it would go and retrieve some data from that example URL and do something with it. Here in the attach method
>
> **[1:32](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-the-value-of-this-like-and-always-14183629?u=76281980&t=92)** that's actually going to be invokes when this thing starts, I'm just grabbing that button. And we're going to add an event listener to that button so we can call getData and take that action. So let's do that now. We'll add the event listener. This will be for a click event. And we'll call this function clickGo. It can be nice to name these so you can find them in your call stack in the debugger. I'll pass in the event. And first, I'll prevent any default actions from firing. And then I'm just going to call getData. I'm going to use, this, keyword to refer to this object. And I'm going to call getData. So the hope is that I will just call this very simply using the, this, keyword to refer back up here. So we can save this and switch over to the browser to see if it works. Let's refresh. If I click Go, I can see that this.getData is not a function here. Can I open our file in the debugger? Can you use Command P here in the Chrome debugger tools? I'm going to load start.JS. Here's the line where that error took place. So let's set a breakpoint here, and we'll try it again. There's my breakpoint. And now, I can see what the value of this actually is right now. This is the button, it's not the object. So in most cases, this is rebound in an event listener
>
> **[3:08](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-the-value-of-this-like-and-always-14183629?u=76281980&t=188)** to refer to the object which was clicked. And that's going to be the same as event.currenttarget as we can see here. So when that doesn't work, a solution people will often come up with is just to refer to the object directly without using, this, like so. So if we save and switch back, get rid of this breakpoint. And we'll refresh. Checking the console. I can click Go. And there we go. The function does fire. So this does work, but it's very verbose, not ideal. Keeping track of the value of this can be tricky. So let's look at one solution to this issue next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Drupal](../../Skills/Web%20Development/Drupal.md) (4), [Object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1), [PHP](../../Skills/Software%20Development/PHP.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Code Identifiers:** getdata (5), mymodule (1), clickgo (1)
> **Env Vars:** url (2), html (1), php (1), cms (1)
> **CLI Commands:** find (2), php (1)
> **File Paths:** start.js (1)
> **Tools:** visual studio (1)
> **Definitions:** is a  (1)
> **Analogies:** just like (1)

#### [Solution: Fix "this" using bind()](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-14183630?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-14183630?u=76281980&t=0)** - [Instructor] In this video, we're going to see how to ensure we always know the value of this so we don't feel like we have to spell things out in this manner. Indeed, we'll tell our code exactly what it should be, using the global function object's bind method. If you've used [React](../../Skills/Web%20Development/React.js.md) before, you've probably seen this pattern. It was actually through using React that I first came to understand how bind works, and now I use it in any project, React or not. In essence, what we're going to do is invoke bind on a function we've defined, and a copy of the function will be returned where this is set to whatever we tell it. When I was first reading about this, I didn't know what the point of that was exactly. The examples I read seemed kind of arbitrary. But in a context like this one, I really started to get it. Here's how it works. I've defined this function expression here. And I can actually call methods right here after the closing brace of that function. So I'm going to call bind here. And I'm going to tell this event listener what I want this to be inside the object. I can do this the verbose way first where I put in the full specification for this myModule object. And now inside here, I can call this. So we'll save this and switch over to the browser to try it out. I'll refresh, click Go, and indeed, it does work. Over here in the debugger, I'm going to use Command + P to open that start.js file. I'm going to set a breakpoint down here. Right here on line 14.
>
> **[1:34](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-14183630?u=76281980&t=94)** I'm going to refresh the page. And now I can see what everything is bound to. So I'll set another breakpoint inside my callback function but I can see right here that this refers to the object with attach and getData and url. So it's referring to myModule. So again, right now inside the attach method, this refers to the object that I'm interested in. What this means is that when I call bind down here, I don't have to spell it all out. I can actually pass this in down here as well. Before we do that, let's look inside our callback function. So I'll let this play. And then I'll click. And I can see that here inside, because we've done this bind, this refers to the same object. Okay, so everything is exactly how we want it but we can make this less verbose by changing this whole object specification down to this. Let's confirm that. Get rid of these breakpoints. And now back here in the editor, I can actually just bind this directly. I'll save again. Switch over here and refresh. I'll pull up the console and click the button. And there we go, that works great. So the way that I think about this and use it most often is in situations like this with event handlers where I want to make sure that inside that event handler's callback function, this refers to the same thing as wherever it was defined. So I'll almost always do it by passing in this directly,
>
> **[3:10](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-14183630?u=76281980&t=190)** instead of using a full object spec like this. But whatever works for your application is just fine. We don't have to just bind on function expressions like this one. Let's say that we wanted to pull this clickGo method out into a property of myModule as well. So let's grab the body of this function. We'll cut it out here. clickGo will now be a function. Find is a method on the object. And so now I'm going to call this.clickGo. There. Now, I can save this and switch over to the browser again. I'll refresh. And when I click Go, I get the same this.getData is not a function error. Now we know what to do here. We're going to call bind this on clickGo. We're going to do it right here. The syntax looks a little funny at first but the idea is that we are now rebinding this on the method here to make sure that this inside the function is what we expect. So I can save. Switch over and refresh. And we're back in business. This would also work if we'd decided to define this just inside the attach method. So if clickGo were a variable here inside,
>
> **[4:48](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-14183630?u=76281980&t=288)** we call it this way. Refresh and try it again. And switching back, we can even do it this way where you attach bind here on the function expression instead of down here when we add the EventListener. Refreshing. Clicking again, it still works. So as you can see, basically anywhere we can define a function, we can use bind to set the value that this should take inside the body of the function. This pattern has existed in [JavaScript](../../Skills/Software%20Development/JavaScript.md) for many years and I for one am glad that I finally understand it. It can feel a little tedious sometimes to pepper a large object with lots of binds but I find the trade-off is worth it to simplify the bodies of those functions. There's actually another way to deal with the problem of this in modern JavaScript environments that support ES6, and we'll look at that next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (3), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2)
> **Code Identifiers:** clickgo (5), mymodule (3), getdata (2)
> **CLI Commands:** make (3), find (2)
> **Definitions:** refers to (4), is a  (1)
> **Documentation:** specification (2), spec (1)
> **File Paths:** start.js (1)
> **Env Vars:** es6 (1)
> **Speakers:** - [instructor] (1)

#### [ES6+ solution: Arrow functions](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/es6-plus-solution-arrow-functions-14180825?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/es6-plus-solution-arrow-functions-14180825?u=76281980&t=0)** - [Instructor] Using the function objects bind method lets us prescribe what this should be inside a function. In this video, we're going to take a quick look at how to address the same issue using arrow functions, a feature of ES6 or [ECMAScript](../../Skills/Web%20Development/ECMAScript.md) 2015. If you haven't seen it before, an arrow function is a compact way to write a function. In some cases, like this arrow function that doubles a number, they can be very compact, indeed. The parameter on the left side of the arrow, when I say the arrow, I mean that equal sign and the greater than sign stitched together, and then the expression to return is on the right side. With only one parameter and only an expression to return, you can leave out the parentheses, the braces, and the return statement like you see here. It takes some getting used to, but it can save a lot of keystrokes. Here's the same function to find in a way that's a little more verbose. Parameters are enclosed in parentheses and the body embraces, just like a traditional function. The parentheses are required if you have multiple arguments to pass and you need to include return, as you would normally. Along with the difference in syntax, the main thing that we're interested in now is that arrow functions maintain the lexical scope of where they're defined. In other words, the value of this outside the arrow function is maintained inside it. As a result, using arrow functions for event handlers and timer functions like set timeout and set interval can be pretty handy. How handy? Let's try it. So back here in Visual Studio Code, the first thing I'm going to do is rewrite this clickGo function to make it an arrow function. So I can get rid of the keyword function, add an arrow between my parameter and the function body,
>
> **[1:36](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/es6-plus-solution-arrow-functions-14180825?u=76281980&t=96)** and now I can get rid of bind. And really that's it. Now I can save and we'll try it in the browser. Refreshing, I click go, it still works. And over here in the debug panel, I'm going to open, start that JS with command + P. Let's set a breakpoint here and try it one more time. We can confirm that indeed, this is set to exactly what we expect with no fuss or muss. We can also do this with an anonymous arrow function where we replace this variable with that function right here. So if I save that, switch over and refresh again, we'll open the console, click the go button, and it still works. So we've seen that arrow functions are very helpful. They let us get rid of all that binding this all over the place. But why might we not use them? Arrow functions don't create their own scope, which is great for our purposes here, but it also limits where they can be used. For a couple of examples, they can't be used with bind, call, or apply or the new keyword. So even if you love arrow functions, there will still be some times that you'll just have to use traditional functions. Arrow functions also require that the environment where they're executing supports ECMAScript 2015. All modern browsers and versions of Node do, but if you need to support older browsers, you'll either have to not use them or transpile your code using a tool like Babel. As with any language feature that encourages terse code,
>
> **[3:10](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/es6-plus-solution-arrow-functions-14180825?u=76281980&t=190)** arrow functions can be a little challenging for us and our fellow humans to parse, especially when they're very concise. This is mainly a matter of getting used to them, but if you're working with other people who aren't as up to speed on modern [JavaScript](../../Skills/Software%20Development/JavaScript.md) as you are, you can expect a little bit of explaining to take place. So at this point, we've covered a couple of different ways of dealing with the problem of this inside our event handlers and other functions inside our objects. We can use the bind method on the global function object or we can use arrow functions, where supported.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [ECMAScript](../../Skills/Web%20Development/ECMAScript.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **CLI Commands:** find (1), make (1), node (1)
> **Definitions:** is a  (1), in other words (1)
> **Code Identifiers:** clickgo (1)
> **Env Vars:** es6 (1)
> **Tools:** visual studio (1)
> **UI Navigation:** open the (1)
> **Analogies:** just like (1)

#### [Solution: Creating a unique robot army](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-creating-a-friendly-robot-army?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-creating-a-friendly-robot-army?u=76281980&t=0)** - [Instructor] Let's go over my solution to this Robot Army Challenge. The main things that we need to do are make sure that the robot template is actually copied, not just referenced. So we'll start with that. Simple assignment like this is not going to work. We do have a few options for how we might approach this. We could use Object.assign,
>
> **[0:36](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-creating-a-friendly-robot-army?u=76281980&t=36)** passing an empty object, and then the robotTemplate. We could also go the ES 6 very short way
>
> **[0:50](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-creating-a-friendly-robot-army?u=76281980&t=50)** and just spread the properties into a new object. If we want to make a totally deep copy, we could use [JSON](../../Skills/Web%20Development/JSON.md).parse and JSON.stringify on the template.
>
> **[1:14](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-creating-a-friendly-robot-army?u=76281980&t=74)** The serial number is supposed to increment by 10 for every single robot. So instead of just adding 10, we're going to add 10 and multiply it by whatever number we are in the cycle of robots. So first it'll be zero and then 10, 20 and so forth. The name doesn't need to be touched. And then depending on how we did the copy, these two lines may or may not work. If we use the JSON Parse and Stringify method, this actually should work. And if I test it, we can see that all three tests pass. But if we did it one of the shallow ways, like say using spread, I'll try running the test again. We can see that all four of the locations are the same here. So what we need to do instead in that case is completely reset that location object. So we would reset the X property to this randomInt and Y to the other.
>
> **[2:37](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-creating-a-friendly-robot-army?u=76281980&t=157)** Now if I rerun the test, all of them pass again. So we just need to be pretty careful about how we execute that copy. And depending on the mechanism we use, we need to treat some of the fields a little bit differently.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (3)
> **Env Vars:** json (3)
> **CLI Commands:** make (2)
> **Code Identifiers:** robottemplate (1), randomint (1)
> **Exercise Files:** template (2)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 3. Oh Functions, My Functions

[↑ Back to Table of Contents](#table-of-contents)

#### [Pain point: Send extra parameters to an event handler](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-send-extra-parameters-to-an-event-handler-14183622?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-send-extra-parameters-to-an-event-handler-14183622?u=76281980&t=0)** - [Joe] This section is roughly grouped under the heading of functions, starting with this video where we're looking at event handlers and other functions where there's an expected parameter or set of parameters. Is it possible to add to those parameters, and if so, how? If you've written anything [JavaScript](../../Skills/Software%20Development/JavaScript.md) for the web, you've surely written an event handler like this countless times. You pass the event object if needed inside the function. And if you need other contexts, it comes from variables to find outside the function. The same logic generally applies to other uses of callback functions like timers, promises, and [AJAX](../../Skills/Web%20Development/AJAX.md) requests. There's usually some known parameters passed in and that's it, or at least that's how it appears. This can start to feel limiting when your callback function could use a little more context that isn't immediately available. It can be especially tricky when you're depending on external values that might change over time. Or if you need to rely on some data that's inaccessible to the handler function. External data can change when there's asynchronous network activity taking place on a different schedule from when this function might execute. Inaccessible data can happen in component-based frameworks like [React](../../Skills/Web%20Development/React.js.md) or [Vue](../../Skills/Web%20Development/Vue.js.md), where components you've built can't always know the data contained in the others. To solve problems like these, it would be nice to add the extra data we need as parameters, as we see here with the color parameter. Doing so should protect the function from trying to act on changing or hard to access data from outside its own scope. Making all function inputs explicit like this is also just good practice for making sure the function's purpose is clear for human readers. An area where I experienced this in real life was in building a program guide for an independent TV network. There were rows of videos to watch in groups
>
> **[1:34](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-send-extra-parameters-to-an-event-handler-14183622?u=76281980&t=94)** by category or purpose like favorites or recent. The page configuration contained an array of the AJAX requests necessary to [Fetch](../../Skills/Web%20Development/Fetch.md) those items. And we had to make sure that the rows appeared in the correct order, no matter how long the network requests took to respond and be rendered. The row render function was generic, so the same helper function would be called when any of these requests completed. If we just rendered the rows in the order that they completed, the difference in request response times could mean that the rows end up in the wrong order like this. To solve it, we rendered the rows first with nothing in them, noting the parent ID associated with the request. Then as the requests completed that row ID could be passed along with the data returned. So the row could be rendered in the correct position, regardless of when it completed. This is the general idea of what that rendering callback would look like. The data parameter is the usual one that we expect in a callback to a network request with rowId being the custom extra parameter that we've passed in. We've now outlined one reason this language feature is desirable. For quite a while, until fairly recently in fact, how to do it wasn't clear to me, but we're going to look at how to pass extra parameters into any function in the upcoming example.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [AJAX](../../Skills/Web%20Development/AJAX.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [React](../../Skills/Web%20Development/React.js.md) (1), [Vue](../../Skills/Web%20Development/Vue.js.md) (1), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **CLI Commands:** find (1), make (1)
> **Env Vars:** ajax (2)
> **Code Identifiers:** rowid (1)
> **Best Practices:** good practice (1)
> **Speakers:** - [joe] (1)

#### [Solution: Use bind() a different way](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-again-14186131?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-again-14186131?u=76281980&t=0)** - [Instructor] In this video, we're going to see how to pass additional parameters into callback functions. We'll be using the same bind method we've seen previously for setting the value of this and how it can do even more. I have the developer tools open on my example here. I can see I have these two buttons. If I click button one, an item is placed in column one, and if I click button two, same thing. You can do this until I get an error down here that I've run out of items, but I can see that the two buttons are doing exactly the same thing, where what I'd really like is for button one to place items in the first column and button two to place items in the second. Here in my editor, I could see my two buttons with the IDs one and two spelled out as a [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md). And then I have a slot, slot one and slot two, for the first and second column where those items will be placed. Now in my [JavaScript](../../Skills/Software%20Development/JavaScript.md) file, everything is wrapped up in a little myApp object, which is initialized via an init method. And here's my dataset of items that I'm going to pull from. I'm going to fold this up for now. And now I have DOM elements for my two slots, I have my click handler, which we'll look at it again in a second, here's my helper function that adds items into those slots, and then finally, here's the init function where I grab each of the two buttons and add the same event listener to both of them. And scrolling up to look at that handle click function,
>
> **[1:35](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-again-14186131?u=76281980&t=95)** I'm pulling an item off of the dataset and then adding it to a slot, and the slot is currently hard-coded to one. I would like this to be generic, so maybe it's slot number. But that's not a parameter that's passed into a click event handler. So I want to pass this in, and I just want to note that I do know there are other ways to do this involving inspecting the event object to see which item was clicked and branching from there, but the goal is to illustrate this without getting super complicated, so please bear with me. So what I want is to be able to pass in slot number somehow into this click event handler. And the way to do this is using bind. I'm already binding this so that my click handler has access to this inside it for calling add item to slot. But if I pass additional parameters, those will show up as additional parameters on that callback like this. So in this case, I want button two to put items in slot two, and button one to pass items into slot one. Now that I've done that bind, I can add those parameters up here. The trick is that those custom parameters go before the default ones, so in this case, I'm going to call this slot number and this needs to come first. Now I can save this and switch over to the browser to give it a try. So here's column one and there's column two,
>
> **[3:10](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-again-14186131?u=76281980&t=190)** put the next one in column two as well, and the third one back to column one, and then we run out of items. So this is handy because it makes it very clear what I'm expecting to use here in my click function without having to dive into the event object and look around. Most of the time up 'til now, we've been using bind to set this, but we don't have to do that. If there were no reason to use this inside my handler function, but I still wanted to bind additional data, I could just pass in undefined here or null, and that way I would just have my extra data with no need for this. In this case, we do need it, so I'll leave it just like it is. And just make sure that you get the parameter order correct here. If you were to accidentally place it afterwards, you could get unexpected results. Let's save this and switch over to the browser to see what happens. So we can see that event.preventDefault is not a function here. If I opened the debugger and opened my script file with command p, I'll set a break point inside the event so we can watch what happens. Now I can see that event is one and slot number is my event. So just make sure you get the order of the parameters correct so you get the right results. Back in the editor, I will reset the order and save. Switching back to the browser and refreshing,
>
> **[4:47](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-again-14186131?u=76281980&t=287)** I can remove my break point, let it play out, and now we have the expected behavior once again. Here's one more example we can look at, which is revisiting our challenge from chapter two with all these robots. Back here in Visual Studio Code, let's go down to line 94. I'm going to go there directly with control g. Type in 94. And what I'm looking at here is the callback to my every iterating function. Here, instead of having this tiny wrapper function around placeBot, we're going to just use placeBot directly as the callback.
>
> **[5:31](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-again-14186131?u=76281980&t=331)** That's going to look like this. So we're binding this, but placeBot expects some other parameters as well. Expects the width and the height of the new robot. Now we're going to check placeBot to see if it's actually going to work with these new parameters. Here's placeBot right here. Its first parameter is robot, and then there's the width and the height. But now it's being used directly as a callback for that every iterator, and in that case, robot is the item that we're iterating over, so that makes it the default argument, which means it has to go last. It has to follow our custom ones. Should be robot there. So now I can save this and switch over to the browser. Going to refresh this and try it out. And I can see I'm only getting one robot rendered. And the reason for that is that I'm using this as a callback for every, which means that I need to return true to make sure I keep going through the loop. So I'll save that again, go back and refresh. Now I should be getting all my robots, and indeed, I am. Now we've seen how bind can be used not only to set the value for this in a callback function, but also how we can use it to pass in anything we want. This isn't always the only way to accomplish a goal in our code,
>
> **[7:04](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-bind-again-14186131?u=76281980&t=424)** but it can be quite an elegant helper.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Code Identifiers:** placebot (5), myapp (1), preventdefault (1)
> **CLI Commands:** make (3)
> **Env Vars:** dom (1)
> **Tools:** visual studio (1)
> **Definitions:** means that (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Pain point: Function signature doesn’t accept arrays](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-function-signature-doesn-t-accept-arrays-14180827?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-function-signature-doesn-t-accept-arrays-14180827?u=76281980&t=0)** - [Instructor] Every once in a while you might find a function whose method of invocation doesn't work with your needs but, otherwise, it's a match. In this video, we're trying an example of this, where we have an array of data, but the function we want to use only accepts individual parameters. A built-in example of this is the max method on the global math object, which returns the largest number from an argument list, but wouldn't work with an array of numbers like we have here. Let's look at another real example in the browser. I'm just going to click this go button, wait a few seconds. And then I get the results of some mocked up network requests inserted here into the page. Let's take a look at the code now. I have this promise simulator function that returns a promise that will resolve to the value I specify after a delay that I specify in milliseconds. This is much easier than trying to use a web server and a mocked-up API. I have my button here and my event listener, which is going to remove the classes and set some content in the button so I know that something is happening. And then I'm simulating three promises that will resolve after one second, two seconds and a quarter of a second. The method that we're looking at here is when, which is built into jQuery, which I've loaded up for this example. It's similar to the built-in all method on the global promise object, except promise.all accepts arrays. jQuery's when method has to use individual parameters. But it does have the virtue of compatibility with older browsers. But the idea is that once all of these promises complete,
>
> **[1:34](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-function-signature-doesn-t-accept-arrays-14180827?u=76281980&t=94)** then my callback function is called with the data from all three of them, again, passed as individual parameters, and then I can act on them all at once, which in this case just means setting the text content of each of these slots to the return data. There's a lot of duplication here and it feels pretty inelegant to have to pass these three promises as individual parameters, instead of just chunking them together as an array and passing them all in at once. In the next video, we'll see how to make jQuery's when method do our bidding and reduce some of this duplication.

> [!info]- Semantic Content
>
> **Code Identifiers:** jquery (3)
> **CLI Commands:** find (1), make (1)
> **Env Vars:** api (1)
> **Cross-References:** in the next (1)
> **Definitions:** is called (1)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Apply and spread](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-function-prototype-apply-14181693?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-function-prototype-apply-14181693?u=76281980&t=0)** - [Instructor] In this video, we're going to see how to change an array into a separate list of parameters for functions that can't accept arrays. To do it, we'll use a method of the global function object called apply. It has some similarities to bind as we'll see. We're also going to reduce some duplication in this code using some more modern [JavaScript](../../Skills/Software%20Development/JavaScript.md) tools. Recapping our example here, we're using jQueries when method down here on line 25, which needs an argument list and can't accept arrays. We're going to change this. So first I'm going to package up these promises into an array. I'm going to make a rectangular selection here in Visual Studio Code, using options Shift, select like this. We can get it in the right place, there we go. Now I have these items separate and clicking back on the first line, I'm going to create a variable called promises, which will be an array of all of these three items. And I'd like to pass this in right here, but of course that isn't going to work. Instead, I'm going to invoke apply, which takes two parameters. The first one is the value that should be set for this inside the function, just like when we've used bind. And we don't need that here so we can just set that first parameter to null. The second one is the array. You can think of this as converting the array into the built-in arguments list that every function has in JavaScript.
>
> **[1:32](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-function-prototype-apply-14181693?u=76281980&t=92)** Arguments is an array like object, but not a real array. Now that I've done this, I can save and switch over to the browser to try it out. So I will refresh, go my developer tools open here to the JavaScript console. If I click Go, there we go, everything still works wonderful. So that's how apply works. Now we're going to use some recent JavaScript features to help us reduce duplication and look at a feature ES6 that's similar to apply for our purpose here. So the first thing that I notice is that I have these three variables all named something similar, and they're all doing something similar with some similar DOM selectors. This seems like a good place to add a loop of some sort. As I mentioned, every function has an argument's list. It's an array like object, so let's try and call for each on it. I had a little callback function here, and we're going to copy one of these items into this loop. Cause we're basically doing the same thing over and over again. And we're going to need two things. We're going to need the item that should actually be set in the text content. So that will be our item and we'll need the index as well so we can insert things into the correct slot. So I'm going to change this into index + 1, because index is zero indexed. And now instead of data one, I will use item.
>
> **[3:08](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-function-prototype-apply-14181693?u=76281980&t=188)** Now I can get rid of all these parameters because we're just using the arguments object, which is available in every function. So I will save this and get a little bit of reformatting there and we'll switch over to the browser and try it out. Refresh and click Go, and after a second, we get an error here. It says, "arguments.forEach is not a function." The reason we get this error is that arguments is an array like object, not an actual array, which is say, I cannot invoke for each on this arguments list. It would need to be a real array for that to work. And as it happens, the global array object in modern JavaScript has a method called from. And if I wrap arguments in this, I get a real array. What Array.from does is basically copy the array or an array like object into an actual array. It's a shallow copy not a full clone, but that's perfect for what we need right now. Once I've wrapped this, I actually can invoke for each the way I'm expecting to. So let's save that and try it out. I'll refresh, click Go. Now we're back in business. So we've reduced quite a bit of duplication here. This is looking very nice, but there's a couple other things that we can try. First of all, we're going to get rid of apply and look at another way to do the same thing using a feature of ES6 called the spread operator. There are a couple of different places in ES6 where you'll see this operator or an operator that looks like it. It's three dots right in a row.
>
> **[4:41](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-function-prototype-apply-14181693?u=76281980&t=281)** And what this does is it spreads an array out into separate parameters. It basically does the same thing that applied to us, but in a very compact syntax. And of course this only works in ES6 . So we'll save that and try it. Refreshing and clicking Go, it all works. There's one more thing I can do to get rid of this Array.from. I can use what's called the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) operator, which looks just like the spread operator, except it works in the opposite direction. So I'm going to put in my three dots here and I'll call this args just to differentiate it from the built-in arguments object. Now I can put this in here. This rest operator takes parameters and collects them into a real array. We're using it here with all the parameters, but you can also do it with just the last one. So you could name your first parameters. Like if I wanted to keep data one and data two, I could just leave all the rest of them using this rest operator to be collected into an array. I actually want to just have all of them in one array here. So this is all we need. Saving again, switching over to the browser and refreshing. It also works. These three dots are quite a handy edition from [ECMAScript](../../Skills/Web%20Development/ECMAScript.md) 2015, but it's certainly a little tricky at first to remember which one is which. If it helps try remembering that spread is for invocation. That's the one that converts an array into a list
>
> **[6:15](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-function-prototype-apply-14181693?u=76281980&t=375)** while rest can collect the rest of the available parameters. It's the one that takes a list and converts it into an array. Now we've seen how to use the global function objects apply method to turn an array into an argument list and set this if we need it. We've also seen the spread and rest operators from ES6 , which can tidy up our code quite nicely.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (7), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (5), [ECMAScript](../../Skills/Web%20Development/ECMAScript.md) (1)
> **Env Vars:** es6 (5), dom (1)
> **Analogies:** just like (2), similar to (1)
> **Code Identifiers:** jqueries (1), foreach (1)
> **Definitions:** is an  (2)
> **CLI Commands:** make (1)
> **Tools:** visual studio (1)
> **Speakers:** - [instructor] (1)

#### [Pain point: Refactoring function signatures](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-refactoring-function-signatures-14179881?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-refactoring-function-signatures-14179881?u=76281980&t=0)** - [Instructor] In this video, we're going to talk about function signatures, by which I mean, the parameters passed into the functions that we write. It can be a little painful to refactor a function that has a bunch of parameters keeping track of the order, their names and default values. So first we'll look at the problem. We're revisiting our robot app from earlier. Let's say our task is to get rid of some hard coded values in the robot placement function to make it more configurable. So we'll scroll down to placeBOT here and looking at its body. I see a couple of twenties, hard-coded values. These are the buffer in pixels from the center of their little area inside which the robots shouldn't be placed. We can add some parameters for this, call them top padding and left padding say, and those can become additional arguments for the function and as we do this, of course, we have to remember that robot needs to be the last one, because this is used as a callback function for every, and we need to update any other places where the function is invoked. For now we just have the one. So we'll pass in our default values right here. We'd also have to deal gracefully with default values when a parameter is missing. So depending on whether you're using ES6, you can do it this way if you have ES6 available. Or we can do type checks here, if the type of top padding is undefined,
>
> **[1:38](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/pain-point-refactoring-function-signatures-14179881?u=76281980&t=98)** we can set top padding two 20 and do the same thing for left padding. And we could of course do this for the width and height as well. Certainly easier to do this with ES6. A good IDE can help with some of this, but it's all just a little bit more friction than we'd like to deal with. Coming up, we'll see one way to structure our function signatures that offers more flexibility, especially when we're working with modern [JavaScript](../../Skills/Software%20Development/JavaScript.md).

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Env Vars:** es6 (3), ide (1)
> **CLI Commands:** make (1)
> **Code Identifiers:** placebot (1)
> **Cross-References:** coming up (1)
> **UI Navigation:** scroll down (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Objects for arguments](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-objects-for-arguments-14183626?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-objects-for-arguments-14183626?u=76281980&t=0)** - [Instructor] In this video, we're going to see how to make our function signatures easier to manage using objects and some new syntax from ES6 that makes it even easier. In a nutshell, we're going to take our separate function parameters and pass them all at once as a single object. Doing this, means more keystrokes initially, but has a few benefits. Order of these parameters doesn't matter anymore, it's always just one object. There are places where we'll need to account for parameter order, but much less often. The parameter object also could have a lot more data than we actually need in the function that's using it. We can just pull out the parts we do need and ignore the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md). This example shows that to an extent so far only using parameter p1. Well, let's see another. In this example, we have a user object with a few properties. If we have a function to get a user API record by ID and another one that sends an email to an email address, they can both accept the same user object as a parameter. And later if the generator of that user object adds more fields like a job title or a phone number, none of the calling functions have to change because they just use what they need ignoring what they don't. So with these benefits outlined, let's give this a try and some code. In the editor, we're going to refactor play spot to use an object for its arguments first with older [JavaScript](../../Skills/Software%20Development/JavaScript.md), and then with some help from ES6. So we'll start by scrolling down to where the function is actually invoked. Right here.
>
> **[1:33](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-objects-for-arguments-14183626?u=76281980&t=93)** And I'll start by creating a params object and we'll pass in all the same arguments that are down there, passing the width, the height, then topPadding and leftPadding. Now I can get rid of all these parameters and just pass in params. That's part one. Now we need to edit the function itself, so it'll consume these correctly. I can just command click to jump up to the definition of the function. And now I'm going to cut all these and replace it with a params object. And I'm going to pull these out into named variables. Named just the same as they were when they were parameters. So we'll have the height which will be params.height and the width, params.width, the topPadding and the leftPadding.
>
> **[2:43](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-objects-for-arguments-14183626?u=76281980&t=163)** And I can also set some defaults while I'm here, because I don't expect any of these ever to be zero or some other false value. I can do the short version with the double pipes. So either it's going to evaluate to params.height, if it exists or 300. We didn't even have default values for height and width now, but no reason not to. Doing this much gives us the benefit of parameters passed in any order and only pulling out the pieces we need from the received object. Let's switch over to the browser and now we can refresh and try it out and make sure it still works. One robot, two robots, three and so on. Great. So everything is still working as we expect. [ECMAScript](../../Skills/Web%20Development/ECMAScript.md) 2015 brought us some nice enhancements to syntax that make objects less verbose to work with, particularly as object parameters. So let's try that out now. We'll go back to the invocation of this function. And the first change we'll make of getting rid of these duplicated key names where the key and the variable that represents its value are the same. In ES6 You can just use that variable and it will be expanded out into the key and the variable name automatically. I'll save that, that's all we need to do here. Now we can go back to the definition of the function and make some more changes. I'm going to modify the parameters object to use an ES6 technique called de-structuring assignment. For our purpose, it means pulling out the pieces
>
> **[4:17](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-objects-for-arguments-14183626?u=76281980&t=257)** that we want from the params object in a syntax that looks very object like. So I can replace params with a set of empty curly braces here. And inside, I'm going to start pulling out what I want. First I'm going to want the height parameter, and just like when I first assigned these, I don't have to spell out the full name. For situations where the incoming key and the variable that I want to be assigned from it are the same, I can just use one of them. So do that for height and width, then there's topPadding and leftPadding. If I do happen to want to change the names of these once they're in my function, I can use the very object like syntax here, only this time instead of being a key and a value, it's the incoming key name, and then the variable name that it should be set to. So I could change this to say tp and this one to lp. If I'm not into the whole verbosity thing, then down here where I start to use it, I can change these to tp and lp. If I want to set default values, I don't have to do that as I'm doing here, I can use the regular ES6 syntax of an equal sign followed by the default value. And that works for these where I'm resetting the key names as well. So now that I've done that I can get rid of all these and all these, and of course I can change the order of these if I want to. I believe originally width was first, but it's an object
>
> **[5:49](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-objects-for-arguments-14183626?u=76281980&t=349)** and I'm just de-structuring the pieces of it. So it doesn't matter what order these appear in now. I can save this and we'll switch over to the browser and confirm that it's still working. I'll just refresh and indeed the behavior is just what I expect. I don't expect anyone to go running to their code bases and change all their argument lists to objects. I certainly don't do this myself all the time, but I see this pattern increasingly out in the world, especially with de-structuring assignment. And I do think it has a lot of benefits, especially on projects that are large or just very active, where parameters might change a lot. With objects for arguments, you have a little bit of extra typing initially, but you gain flexibility afterward.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [ECMAScript](../../Skills/Web%20Development/ECMAScript.md) (1)
> **Code Identifiers:** toppadding (3), leftpadding (3)
> **Env Vars:** es6 (5), api (1)
> **CLI Commands:** make (5)
> **Cross-References:** go back to (2)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution overview: Story generator](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-overview-story-generator-25790976?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-overview-story-generator-25790976?u=76281980&t=0)** (upbeat instrumental music)
>
> **[0:05](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-overview-story-generator-25790976?u=76281980&t=5)** - [Instructor] Let's go over the solution to this Story Generator Challenge. Remember that we need to use an object as the one argument into the makeStory function, and we need to compute the minimum and maximum [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) length results correctly as well.
>
> **[0:27](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-overview-story-generator-25790976?u=76281980&t=27)** So, the first thing we'll do is change the function signature here. If I cut out all these parameters I can replace them with just a single argument, which we'll call words. Probably the easiest way to make these available inside the function is to use destructuring assignment. Just paste in all those same parameters, and we'll just get them from words. That will take care of generating the story up here, but now we need to deal with the calculation of the word lengths. We're not going to use arguments anymore because we have an object here. So, instead we'll use Object.keys, passing in the words object. We can run map over that, but instead of the words coming out of this each iteration is going to have the key. And so, we aren't going to be able to compute this directly on word, we need to get the word_key reference inside that words object. So, every time we go through it's going to be name, noun, adjective, and so forth. And so, we'll dereference the correct word and get its length. Then finally, Math.max and Math.min don't take arrays. So, we have at least a couple of options for how to deal with that. We can spread those arrays into separate arguments for those functions.
>
> **[2:05](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/solution-overview-story-generator-25790976?u=76281980&t=125)** Or we could use apply and set this to null, but pass in the wordLength array from there. And so, I'll just leave one of each here, and we can test this. There we go. All tests pass, and we have the correct output. And that concludes the Story Generator Challenge.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (4)
> **Code Identifiers:** makestory (1), word_key (1), wordlength (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat instrumental music) (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps and thank you](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/next-steps-and-thank-you-14186129?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/next-steps-and-thank-you-14186129?u=76281980&t=0)** - [Joe] Congratulations, before we go, let's talk about a few possible avenues to explore next. First off, I'm curious for your feedback. This course is a bit of a new idea, and it could be expanded, and updated in the future. Have you been working on a [JavaScript](../../Skills/Software%20Development/JavaScript.md) project, and suddenly had a eureka, or an aha moment with a language feature? Are there aspects of JavaScript you think are tricky and that you're just on the verge of understanding, but not quite getting there? If so, leave a message in the course Q&A, or reach out directly to me on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md). It could be covered in a future edition of this course. I first really thought about getting rid of loops while reading the book Refactoring to Collections by Adam Wathan. The [PHP](../../Skills/Software%20Development/PHP.md) framework [Laravel](../../Skills/Web%20Development/Laravel.md) includes a very rich suite of functions for dealing with collections of things in a way that are much more expressive, and fun than using loops. It was reading this book that really got me thinking this way, even though the book is written for a PHP audience, it might be worth considering for expanding that mindset. There's also a pretty decent entry point for JavaScript developers, collect.js. If you've decided that you'd rather not write loops of any kind ever again, you might be interested in this library, it takes inspiration from Laravel, providing a very similar API for processing, transforming and iterating over arrays, and objects way beyond what you get with core JavaScript. We occasionally discuss the idea of keeping our functions self-contained with the only inputs being the parameters passed, instead of relying on external data. This is one of the tenants of [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md), a way of thinking and doing to which JavaScript is pretty well suited. If this sounds interesting to you, Learning Functional Programming with JavaScript ES6+ is a good introduction
>
> **[1:33](https://www.linkedin.com/learning/javascript-patterns-modern-solutions-to-common-challenges/next-steps-and-thank-you-14186129?u=76281980&t=93)** to functional programming in JavaScript. One of the most critical tools for any developer in any language is a debugger. With JavaScript, we have one built into just about every web browser. We used one a few times in this course, and if that was new to you, I have a course to recommend. [Learning JavaScript Debugging](Learning%20JavaScript%20Debugging.md) is another of mine that goes in depth on how to use a stepwise JavaScript debugger. There's nothing wrong with using console log messages, but getting comfortable with a debugger makes for much happier programming very quickly. Thanks very much for watching this course. I hope it's made some of the tricky parts of JavaScript feel a little more familiar and usable.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (11), [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) (3), [PHP](../../Skills/Software%20Development/PHP.md) (2), [Laravel](../../Skills/Web%20Development/Laravel.md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Env Vars:** php (2), api (1), es6 (1)
> **Definitions:** is a  (3)
> **CLI Commands:** php (2)
> **File Paths:** collect.js (1)
> **Speakers:** - [joe] (1)


## Instructor

- [Joe Chellman](../../Instructors/Software%20Development/Joe%20Chellman.md)

## Resources

- Exercise files available

## Skills Covered

- JavaScript
- Coding Practices

## Path Context

### In [Improve Your JavaScript Language Skills](../../Paths/Software%20Development/Learning%20Paths/Improve%20Your%20JavaScript%20Language%20Skills.md)
← [JavaScript- Patterns](JavaScript-%20Patterns.md) | **8 of 8**

## Appears In

- [Improve Your JavaScript Language Skills](../../Paths/Software%20Development/Learning%20Paths/Improve%20Your%20JavaScript%20Language%20Skills.md)

## Related Courses

_Courses sharing skills:_

- [End-to-End JavaScript Testing with Cypress.io](End-to-End%20JavaScript%20Testing%20with%20Cypress.io.md) — JavaScript
- [Learning JavaScript Debugging](Learning%20JavaScript%20Debugging.md) — JavaScript
- [Foundational JavaScript Security](Foundational%20JavaScript%20Security.md) — JavaScript
- [JavaScript- Test-Driven Development (ES6)](JavaScript-%20Test-Driven%20Development%20(ES6).md) — JavaScript
- [JavaScript- Best Practices for Data](JavaScript-%20Best%20Practices%20for%20Data.md) — JavaScript

---

[↑ Back to top](#)