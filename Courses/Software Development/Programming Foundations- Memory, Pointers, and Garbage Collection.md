---
type: course
cssclasses:
  - lle-course
slug: programming-foundations-memory-pointers-and-garbage-collection
url: "https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection"
duration_minutes: 151
duration: 2h 31m
exercise_files: false
thumbnail: "https://media.licdn.com/dms/image/v2/C4D0DAQFbN4uqlWyxgg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1663000615486?e=2147483647&amp;v=beta&amp;t=JfkDQ6JgmswOu21E5DKAT2zPxX5DSIwVq-SIFD23bJ8"
linkedin_topic: Software Development
learning_paths:
  - '[Become a Programmer- Foundations](../../Paths/Software%20Development/Learning%20Paths/Become%20a%20Programmer-%20Foundations.md)'
prev_courses:
  - '[Programming Foundations Databases](Programming%20Foundations%20Databases.md)'
next_courses:
  - '[Programming Foundations- APIs and Web Services](Programming%20Foundations-%20APIs%20and%20Web%20Services.md)'
path_nav: '[{"path":"Become a Programmer- Foundations","position":8,"total":12,"prev":"Programming Foundations Databases","next":"Programming Foundations- APIs and Web Services"}]'
path_count: 1
tags:
  - course
  - topic/software-development
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/Programming%20Foundations-%20Memory%2C%20Pointers%2C%20and%20Garbage%20Collection.md)

![Programming Foundations: Memory, Pointers, and Garbage Collection](https://media.licdn.com/dms/image/v2/C4D0DAQFbN4uqlWyxgg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1663000615486?e=2147483647&amp;v=beta&amp;t=JfkDQ6JgmswOu21E5DKAT2zPxX5DSIwVq-SIFD23bJ8)

# Programming Foundations: Memory, Pointers, and Garbage Collection

> Did you know that computers have both short- and long-term memory just like you? It’s true. If you peel back the layers of what it really means to be a programmer, you find that most programming tasks involve moving values around in memory. Many contemporary programming languages hide those mechanics, but as you advance they get harder to avoid.Join instructor Maaike van Putten to take your founda

> [LinkedIn Learning](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection) | 2h 31m
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Getting started with memory management](#getting-started-with-memory-management)
  - [Are you ready to learn memory management?](#are-you-ready-to-learn-memory-management)
- [**1. Memory Management Concepts**](#1-memory-management-concepts) (9 videos)
  - [What is memory?](#what-is-memory)
  - [Why do we need to manage the memory?](#why-do-we-need-to-manage-the-memory)
  - [Stack memory](#stack-memory)
  - [Heap memory](#heap-memory)
  - [Heap vs. stack memory](#heap-vs-stack-memory)
  - [Allocating memory](#allocating-memory)
  - [Deallocating memory](#deallocating-memory)
  - [Garbage collection](#garbage-collection)
  - [Actual removing or sweeping](#actual-removing-or-sweeping)
- [**2. Memory Management in Code**](#2-memory-management-in-code) (5 videos)
  - [The C way: Allocating memory](#the-c-way-allocating-memory)
  - [The C way: Deallocating and reallocating memory](#the-c-way-deallocating-and-reallocating-memory)
  - [The automagic way in modern languages](#the-automagic-way-in-modern-languages)
  - [Memory management in Python](#memory-management-in-python)
  - [Memory in Python visualized](#memory-in-python-visualized)
- [**3. Memory Leaks and Best Practices**](#3-memory-leaks-and-best-practices) (4 videos)
  - [What are memory leaks?](#what-are-memory-leaks)
  - [Out of Memory error](#out-of-memory-error)
  - [Avoiding memory leaks](#avoiding-memory-leaks)
  - [Best practices with memory](#best-practices-with-memory)
- [**Glossaries**](#glossaries) (0 videos)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps in memory management](#next-steps-in-memory-management)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Getting started with memory management](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/getting-started-with-memory-management?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/getting-started-with-memory-management?u=76281980&t=0)** - [Maaike] Look at this number 6 5 4 8 6 7 2. Nice number, right? Can you remember it? All right, have a good luck and I'll pause. Can you recall the number? Great, okay. Another question. What's your favorite childhood memory? Again, take a pause and perhaps a cup of tea and think about those. Welcome back. No, you're not in the wrong course. I just wanted you to access your two different types of memory, short term and long term memory. The computer has two types of memory as well and we'll focus on the computer's short term memory in this course. First, we'll go over the basic concepts of [Memory Management](../../Skills/Web%20Development/Memory%20Management.md). The exact implementation depends on your programming environment. I may or may not make this point another a hundred times throughout this course. You'll learn how a program runs and the way it interacts with memory. Next up, we're going to see how it's done in the C language and compare and contrast it with Biden's memory management. Finally, we'll have a look at memory leaks and best practices for memory management. At this point, can you still remember your favorite childhood memory? I sure hope so. It's in your long term memory. What about the number? Can you remember it? Maybe you can, maybe you won't, this number was or is in your short term memory and if you haven't forgotten it yet, you will by tomorrow, I hope. My name is Maaike and I'm a software developer and trainer.
>
> **[1:33](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/getting-started-with-memory-management?u=76281980&t=93)** Let me help you to get this valuable knowledge that will boost your ability to debug and help you to become application performance rockstar.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (3)
> **CLI Commands:** make (1)
> **Speakers:** - [maaike] (1)

#### [Are you ready to learn memory management?](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/are-you-ready-to-learn-memory-management?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/are-you-ready-to-learn-memory-management?u=76281980&t=0)** - [Narrator] Before we get started with [Memory Management](../../Skills/Web%20Development/Memory%20Management.md), let's first make sure that this course is for you. In order to be able to follow along, you'll have to know the basics of any programming language. This means that you need to be able to create variables, objects, and functions. It's also necessary to be able to create loops and if conditions. If you are not familiar with a programming language at this level yet, there are some excellent courses on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md), such as Learning [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), Learning [Java](../../Skills/Software%20Development/Java.md), Learning C#, and so many more. If you already know at least one programming language at this level, then let's get moving.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **CLI Commands:** make (1), python (1)
> **Definitions:** means that (1)
> **Analogies:** such as (1)
> **Speakers:** - [narrator] (1)


### 1. Memory Management Concepts

[↑ Back to Table of Contents](#table-of-contents)

#### [What is memory?](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/what-is-memory?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/what-is-memory?u=76281980&t=0)** - [Instructor] Sometimes you'd want something to be stored for a really long time. For example, the photos of your child on your phone. Sometimes you don't want something to be stored for a long time, such as an [Instagram](../../Glossary/Service/Instagram.md) story or a Snapchat message. Let's start by discussing what memory is when we don't think about computers. We humans tend to have short-term memory and long-term memory. Short-term memory is, for example, if we need to remember a code that was texted to us and enter in a two-factor authentication field. Most people won't remember the code the next day, or two seconds later, in my case. Long-term memories are things we remember for, well, longer, such as a lovely party, a fun activity, the craziest things friends did, or beloved childhood memories, such as your mom learning you how to ride a bike. All right, so let's now switch to computers. Computers also have two types of memory, main memory and secondary storage. The difference is similar to human short-term and long-term memory. The main memory is the short-term memory. When you shut down a computer, the temporary main memory gets erased. The secondary storage is the long-term memory. For example, files and programs on your hard drive. Clearly it's more complicated than this. Definitely if you're going to [Zoom](../../Skills/Software%20Development/Zoom.md) in. And we are going to zoom in, but only on the main memory and not on the secondary storage. The main memory is also often referred to as RAM,
>
> **[1:35](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/what-is-memory?u=76281980&t=95)** which is an abbreviation of random-access memory. This is the memory that can be accessed by the CPU, which stands for central processing unit. This RAM is stored on the motherboard of the computer. And in order to run a script on your computer, for example, to analyze some data, it needs to be loaded into the main memory. If the script or data set is really big and it doesn't fit into the RAM, the secondary storage or virtual memory on the hard drive can be involved to manage the large script or data set. The issue with using secondary storage, though, is it will slow down your process tremendously, because using the RAM is actually very fast to access for the CPU. When you write code, this code will be loaded into the RAM when it's being executed. The variables and objects that you are creating with your code also live on, you may have guessed this, the RAM memory. When you are creating more objects and variables than you're having memory, this becomes a problem, and RAM memory gets full. In some programming languages, you'll have to allocate and deallocate memory manually. In other languages, this is done for you. The main memory doesn't only consist of the RAM, though. There's also ROM. This is the read-only memory. As is in the name, data cannot be written to the ROM. It's slower than RAM, and the memory is non-volatile, just like the secondary storage. As a result, it doesn't disappear when the computer is shut down. The ROM typically has the logic needed
>
> **[3:09](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/what-is-memory?u=76281980&t=189)** to start a computer and operating system so that the computer becomes ready for usage. So let's end this video with a fun fact. Referring to the main memory as RAM is officially incorrect because it consists of not only RAM, but also ROM. It is common, though, when we talk about main memory that we just mean the RAM. Just know, they're not really the same thing. RAM is main memory, but not all main memory is RAM. Also, in this course, we're just talking about the RAM part, since that's the part that's being used by the applications that we'll write.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Zoom](../../Skills/Software%20Development/Zoom.md) (2), [Instagram](../../Glossary/Service/Instagram.md) (1)
> **Env Vars:** ram (15), rom (4), cpu (2)
> **Analogies:** for example (4), such as (3), just like (1)
> **Definitions:** is an  (1), stands for (1)
> **UI Navigation:** switch to (1)
> **Speakers:** - [instructor] (1)

#### [Why do we need to manage the memory?](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/why-do-we-need-to-manage-the-memory?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/why-do-we-need-to-manage-the-memory?u=76281980&t=0)** - [Speaker] Now we know what a memory is, it's time to have a closer look at how does this work and when we are coding. So, we already learned that opening a program means that the program gets loaded into domain memory coming from the secondary storage, and from there, it gets executed by the CPU. The RAM is the physical memory that's being used. This memory gets managed at roughly three levels, [Hardware](../../Topics/Hardware.md), OS and applications. We'll focus on [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) by the application. This comes down to two type of actions. A piece of memory gets allocated for a certain bit of information, and when it's no longer needed. This piece of memory gets released again, this is called deallocation. Then there are also two types of memory allocation out there, static and dynamic. Static allocation occurs before the program is executed such as the loading of the classes. Dynamic allocation happens during runtime. This means during the execution of the program. This is, for example, used for the objects and variables that are created during run time. When the dynamic memory is no longer used, it should be released to free up memory and avoid unnecessary memory shortage during the execution of your program. When the memory gets full, even before it's full, the computer will become slow. So, the allocation and the releasing of the memory are considered memory management. This is important to have a well-performing application.
>
> **[1:35](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/why-do-we-need-to-manage-the-memory?u=76281980&t=95)** That's actually not as easy as it sounds. Complex problems like what block of memory to allocate, and when is memory ready to be released again need to be dealt with. There are roughly two ways of managing the memory, manually and automatically. Manual memory management means that the developer has to allocate and deallocate memory in the code, such as necessary in C and C++. This might sound tedious, but it actually comes with some advantages. First of all, if you learn coding this way, you really understand what's going on with memory, or at least you should in order to do it well. And when memory is tight, this approach can be more efficient than the automatic way. Clearly, it's a bit tedious to do. Every time, the developer needs to code the allocation and the deallocation. Therefore, manual memory management gives a lot of boilerplate code, and it's rather prone to errors 'cause something goes wrong when you forget to deallocate or allocate a piece of memory. So, what about the automatic memory management then? This is where the language has a built-in memory management service. This service knows when to allocate and deallocate memory by itself. This saves boilerplate code, and it avoids a lot of common human mistakes in the management of the memory. The developer can just focus on the features they're building instead of on the memory management code. Programming languages such as [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), [JavaScript](../../Skills/Software%20Development/JavaScript.md), [Java](../../Skills/Software%20Development/Java.md), Perl, and many others take care of memory management for you.
>
> **[3:09](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/why-do-we-need-to-manage-the-memory?u=76281980&t=189)** In order to do so, many of these languages work with heap memory and stack memory. Let's see what these actually are in the next few videos.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (8), [Hardware](../../Topics/Hardware.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Analogies:** such as (3), for example (1)
> **Env Vars:** cpu (1), ram (1)
> **Exercise Files:** boilerplate (2)
> **Definitions:** means that (1), is called (1)
> **CLI Commands:** python (1)
> **Cross-References:** in the next (1)
> **Speakers:** - [speaker] (1)

#### [Stack memory](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/stack-memory?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/stack-memory?u=76281980&t=0)** - [Instructor] Many languages, like C, C++, [Java](../../Skills/Software%20Development/Java.md), and C#, manage the memory by having a heap and a stack. How this is implemented differs amongst different languages. A stack is actually a special data type that works with LIFO principle: last in, first out. Really think of a stack of plates. If you put new plates, you do so on top of the stack. And if you need plates, you grab them from the top of the stack, or at least you should. With a stack memory, this isn't optional, though. The stack area is a part of the RAM memory. So, what do we store on the stack? We store the variables created by your functions on the stack. When our functions call other functions, we create a new stack record to store that function's temporary variables. And if that function calls another function, we create another stack record to store that function's variables. When the function is done, the stack memory gets deallocated. Variables created by functions are only accessible to these functions theirselves. The order of the function calls are typically stored on another stack, the call stack. Sometimes they're just a stack pointer. This depends on the system that you're using. The call stack points to the corresponding stack record on the stack memory. All right, let's have a look at this visualized. So, say we're having our stack memory over here. Right now, there's nothing on the stack memory. But, when a memory gets executed, a stack record is created.
>
> **[1:35](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/stack-memory?u=76281980&t=95)** And as you can see, indicated by the arrow, this is the current record, meaning this is the latest record, and this is what we can access. But, this method is actually going to be calling another method. And right now, the arrow changed one up, meaning that this one is now the current method. This is what we can access in terms of values and references, only the ones for this method. But, this method is yet calling another method. And as you can see right now, on top, a new stack record has been created. When this method 3 is done executing, it actually deallocates the stack memory immediately, and the current record is the previous one, method two. When this one is done, it also gets deallocated, and now we're back in method one. Clearly this is a bit simplified. These stack records can actually receive the output of the method they've called via special mechanisms. But again, this differs depending on the implementations. So, when this method is done, our stack is going to be all empty again. The stack usually has a limit of records it can take, and the records have a maximum size as well. If that limit is exceeded, you get a stack overflow error. This is actually what a famous troubleshooting firm called Stack Overflow is named after. And what you're looking at right now is the homepage. Chances are you haven't ever seen it yet, because you've just entered some sort of thread using [Google](../../Glossary/Service/Google.md). Also have another memory area, the heap.
>
> **[3:09](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/stack-memory?u=76281980&t=189)** The items on the stack can point at memory locations on the heap. Let's see what this heap exactly is in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1), [Google](../../Glossary/Service/Google.md) (1)
> **Env Vars:** lifo (1), ram (1)
> **Documentation:** stack overflow (2)
> **Definitions:** is a  (2)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Heap memory](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/heap-memory?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/heap-memory?u=76281980&t=0)** - [Instructor] The Heap memory is also part of the RAM. It's used for storing values that need to be accessed throughout the entire application. As I just mentioned, the stack is only accessible from within the method. Sometimes you'd want something to be accessible outside of the method. And in this case, we could think a bit of a freezer. And at this point you may wonder whether you're in the right course, but you are. This is just an analogy that might help you to understand the concept a bit better. So if I'm cooking some food, it would be sad if it were only accessible at the point where I'm cooking the food, right? So what I would want to do then is that when I'm done cooking, I want to store the food in the freezer. This means that it can also be accessed at a later moment when I'm not in a cooking method anymore. Also, it's not only accessible by me. It's actually accessible for everyone who's in the household with access to the freezer. So that means if someone comes over before I'm back, the content of the freezer may actually have changed since in my head I may be holding a reference to some leftovers, but if someone else comes over and eats the said leftovers, they want to get back, they're actually going to be gone or at least partly. So by holding this reference as something that more people have access to, it can be changed when I'm going to access it again. Also in the case of the Heap and the stack, some things can simply not be stored on the stack. And the same is true for our minds. We can remember things but we cannot store food on our minds, we'll need a freezer for that. So back to computer memory, as we just mentioned
>
> **[1:37](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/heap-memory?u=76281980&t=97)** the stack memory has blocks for every function. In this case, we have a main function, which is calling a function, which on it's turn is calling another function. If a certain value needs to be available to different functions and even different stacks at the same time, this is where the Heap comes in. So in this case, we have some local variables, but this special zed variable, it needs to be accessed by all these functions. So this is why we put it on the Heap. The stack can then hold the memory addresses of the value on Heap and access it using this address. But remember, just as with the freezer, if someone comes along and goes ahead and modifies this value, if you're holding a pointer to this address and you're going come back later, if it's modified, it's still going to be modified. In this very specific example in some languages six might actually be not modifiable. There will be new value created, but that's beside the point for now. Whether certain objects are actually being able to be modified depends on the language and it's implementation. So say that a Function Two is going to be done. The variable in or function that was holding the address is gone now, but there might be another variable elsewhere on the stack holding the same address, as is the case for the local variable set in Function One, and also for the set in a main function. If one of the functions alters the values stored on the Heap, so for example, we're changing zed to nine from Function One over here. When we're going to be accessing it later
>
> **[3:11](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/heap-memory?u=76281980&t=191)** it will be changed no matter where we access it from. So the Heap is for holding values that need to be accessible globally, but typically also bigger values such as objects are stored on the Heap, since this stack memory is rather limited. Next, let's take a closer look at some of the biggest differences between the Heap and the stack, so we can gain a better insight into how this all works. I will review the differences that may exist among different languages.

> [!info]- Semantic Content
>
> **Env Vars:** ram (1)
> **Definitions:** means that (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Heap vs. stack memory](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/heap-vs-stack-memory?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/heap-vs-stack-memory?u=76281980&t=0)** - Let's compare and contrast the stack and the heap area. Both are areas of the RAM. which again is part of the main memory of the computer and needed to run an application. Both of them can hold values needed for the runtime of your program. So the most obvious difference between the heap and the stack it's different purpose they're serving. The heap memory is used for variables, values and objects that need to be accessible globally and that can be accessed by multiple threats. The stack keeps the order of execution and the internal values of each function. This is done in the stack record. Stack records get added for every function that's being called. The next difference is Deallocation. Stack records are removed after a function is finished executing. As long as a function hasn't finished executing the stack will remain and can continue to grow. As for heat memory it can stay there for the full application run time. Memory allocated for items on the heap get deallocated when the item is no longer required. After that, the memory can be allocated to another part of the program again. Another difference is the speed. Accessing elements on the stack is typically faster than accessing elements on heap. Elements on heap are accessed via their memory address. This address can be on the stack but also on another object on heap. For example, when a certain object has another object, think of a person object, having an address object,
>
> **[1:34](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/heap-vs-stack-memory?u=76281980&t=94)** the pointer to the address is actually stored on a person object. From there, the memory address needs to be located. And this means that the stack is built a memory as an actual stack. This means that there's no real need to search when adding or removing from the stack, since the stack records are released in the reverse order they were added. This also makes the adding to the stack straightforward since the memory location is fixed. This is not the case for the heap. A special algorithm is used to find a memory block that's a favorable. Clearly this is more costly and it will take more time. Also, the heap becomes fragmented over time. However, there's typically less space reserved for stack 10 for the heap since stack memory consists of stack records that can only be accessed from within the same function. Closely related to this last point there's not a cool thing to know. The stack is usually only accessible to its owner threat. The heap can be accessed by any threat. Therefore, the stack is usually considered more secure. This can be a bit confusing, so let's break it down. First of all, in an application multiple things can happen at the same time. This multiple things happening at the same time is actually an important computer science concept. You may have heard of it, it's called concurrency. In order to make this possible multiple threads are active at the same time. A thread is a part of execution within an application and every thread has its own stack. So one thread can be calling an API
>
> **[3:07](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/heap-vs-stack-memory?u=76281980&t=187)** to [Fetch](../../Skills/Web%20Development/Fetch.md) a certain calculation while the other thread is awaiting a call to the database. So let's say some function is initiated by a certain thread that has a certain stack. This thread is the one creating the stack record on its own stack and only this thread can access the stack record. The heap can be accessed by any thread since all these threads share the same heap. So in order to understand this we need to dive into the concurrency topic briefly but before we do so, actually I want to reflect on the freezer analogy of the previous videos. So, concurrency is actually happening when you have a household with multiple people. So if there's multiple people in the household it's probably multiple parts of execution happening. So some people might be doing the households and other people might be studying or working or maybe just relaxing. But these threats can actually sometimes have sort of miscommunications happening and this needs to be managed. So say there's this really nice piece of fine refrigerator and both the housemates hold a reference in their head to it. So let's say that person one goes ahead and use this reference in its head to eat piece of pie. When person two is going to try and do so the piece of pie is going to be gone. So for now, think of threads just as things happening in an application, and you're good to go. Let's now have a look at computer memory and multiple threads. Sometimes there is just one thread and when there's one thread, there's one stack. And if there's only one thread
>
> **[4:41](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/heap-vs-stack-memory?u=76281980&t=281)** focused on executing the logic of the program not a lot will go wrong in terms of memory conflicts. But sometimes there are multiple threads executing simultaneously. Concurrency can be applied to increase the performance of your application, but it comes with consequences for [Memory Management](../../Skills/Web%20Development/Memory%20Management.md). So to go back to the household example again, if I'm doing the household for two people, it's probably going to be more efficient and done faster but things need to be managed a bit better than when I'm doing it myself since we need to split tasks and communicate well. In here we see two stack that are going to be trying to access a value on heap at the same time, and actually they want to increment it. If they both read it at the same time and then increment it, it'll only be incremented by one. Why is that you may wonder? Well, say that the first one comes over and read six first and then the second one comes over and still read six as well. They're both going to be incrementing six by one and conclude that it's seven. Then whoever comes first is going to write seven to the place in the heap and then the second one is just going to be overriding it with the same value. This is different outcome from when the order of things was a bit different. So for example, if right now they're going to do the same thing again but now function four is going to access the value first and increment it by one, and then after that, function one is going to be accessing the value and then also increment by one, It will be incremented by two
>
> **[6:15](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/heap-vs-stack-memory?u=76281980&t=375)** even though we execute the same logic. So what happens when we work with concurrency? Well, if we don't manage it well, we actually don't know, It can either be the one or the other. Neither one of these is correct or incorrect in this case, It really depends on the exact business needs. Unpredictable outcomes are typically not what you want for a business need though. And this is something to be very mindful of when working with heap memory. Now that we've sorted out a difference between the stack and the heap, let's [Zoom](../../Skills/Software%20Development/Zoom.md) in on the concepts of allocation and deallocation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (1), [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (1), [Zoom](../../Skills/Software%20Development/Zoom.md) (1)
> **Definitions:** means that (2), is a  (1)
> **CLI Commands:** find (1), make (1)
> **Env Vars:** ram (1), api (1)
> **Analogies:** for example (2)
> **Cross-References:** go back to (1)
> **Speakers:** - let (1)

#### [Allocating memory](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/allocating-memory?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/allocating-memory?u=76281980&t=0)** - [Instructor] The assigning of a piece of memory, a memory address, and reserving it to store a certain value, program, or process is called memory allocation. We already mentioned that we had two ways of allocation, static and dynamic. Static memory allocation is for compile time allocation and dynamic memory allocation is for runtime allocation. Dynamic memory allocation happens whenever we need to create new values on the heap. What happens is that a memory area gets selected to put their value in. The value gets written to the heap or it is reserved for certain purposes, and a variable on the stack holds the pointer to the element on the heap. This point to the element on the heap can be passed to other functions. The exact terminology for these principles differs per programming language. Also, allocating the memory is part of the [Memory Management](../../Skills/Web%20Development/Memory%20Management.md), and how this is done differs for different systems and languages. It can be done manually by coding the allocation of the memory, and it can be done automatically by having it built into a language or interpreter. It depends on the exact language, how it will be done. Some languages make a distinction between allocation stack and heap memory. For example, in C, stack memory is automatically managed, but heap is manually managed. Most programming languages have an allocator for the heap memory that will take care of the allocation requests, either made by the programmer
>
> **[1:32](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/allocating-memory?u=76281980&t=92)** or the automatic memory manager. Allocating memory means that less memory becomes available. Luckily we can also deallocate memory and make it available again. This is exactly what we're going to be looking at next, deallocation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (1)
> **CLI Commands:** make (2)
> **Definitions:** is called (1), means that (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Deallocating memory](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/deallocating-memory?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/deallocating-memory?u=76281980&t=0)** - [Instructor] The releasing of a piece of memory and making it available again is called deallocation. For the stack, this is sort of easy. When a function is done, the stack memory that was holding the variables for the function gets released by the system and pointer of the stack or called stack goes down by one to the previously called function. For the heap, this is slightly more complicated. First of all, the heap memory gets fragmented by this process. When the heap memory is reserved, blocks are reserved in a sequence. Releasing blocks makes the heap fragmented. And this means that finding a memory space for a bigger type can be tough since it may not fit in the gap and the allocator could have to add it to the end or defragment the heap. All of this actually depends on the system and the logic of the allocate of the language that you're using. But this is not all. In some situations, it's easy. For example, writing a tiny piece of logic you can see. You allocate a bit of memory and at the end of the function, it could be deallocated and that will be it. And as a bonus, it's hard to determine when an item is ready for deallocation. It can become way more complicated. Say that we're creating a multi-thread application. Every thread has its own stack and things are happening on these stacks. All the threads share one heap. How to know that none of the threads is still using an allocated memory spot and that it's ready
>
> **[1:33](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/deallocating-memory?u=76281980&t=93)** for deallocation? In some languages, when we have to do it manually, you'll need to manage which threads are accessing the memory spot when. And in some other languages, this is done automatically by a process called garbage collection. In the next video, we're going to have a look at this garbage collection process in more detail.

> [!info]- Semantic Content
>
> **Definitions:** is called (1), means that (1)
> **Cross-References:** in the next (1)
> **Analogies:** for example (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Garbage collection](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/garbage-collection?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/garbage-collection?u=76281980&t=0)** - [Narrator] So freeing up the memory of the heap can be done by a process called garbage collection. Some languages like C and C++ require manual garbage collection by code. Typically, it's not called garbage collection then. Languages that have an automatic garbage collection building are for example, CCR, [Java](../../Skills/Software%20Development/Java.md), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) and actually many other high level languages. There are some common errors that are not happening when we use a garbage collector. First of all, the dangling pointer bugs can be avoided. This is when the developer did free up the memory space but the pointer is still pointing to the old place where a certain variable is stored. You can imagine that this can lead to unpredictable events such as a pointer returning a surprise value, when the memory location has been reassigned to another value. Another common problem that developers with automatic garbage collection don't run into is the bug of freeing up a memory region twice. This can especially be a problem when it has been reassigned to some other value that should not be deleted yet. This is all not a problem for high-level language developers. Thank you, garbage collector. And lastly, all search memory leaks by not freeing up memory that should be freed up but cannot be reached anymore because the pointers are gone, all solved. Not a problem when we use the garbage collector. Well, this garbage collector must sound pretty awesome by now. But when are elements on the heap ready for garbage collection? Well, when they no longer have a connection to the stack.
>
> **[1:36](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/garbage-collection?u=76281980&t=96)** So how do we know? Now, that's a fun, classic struggle of computer scientists. Let's explore some of the strategies and issues for garbage collection together. In order to do so, let's grab a simple example of stack and heap. Now, if you were a garbage collector and you had to find out which element still had a connection with heap, how would you go about that? There's one obvious two-step approach that would make sense to suggest here. First of all, we could check out the stack, see what connections with the heap we're having and add the connected elements to a list and then check all the elements in the heap and see which ones are not on a connection with stack list. So in this example, object one, object five and object six are on the stack. So they're good to go. And in this specific case, they have relations to other objects on the heap. So object five has relation to object two, object six has a relation to object seven, and object seven on its turn has a relation to object four. So these are all good to go. In some languages, this phase would be called marking. We're going to mark all the items that have a connection and garbage collects all the unmarked items. But what if an item gets created after checking the stack? It would be unmarked but still have a connection to the stack and then be freed up by the garbage collector because it was unmarked. And that's a problem right there. Do we have a solution? Sure, let's pause the execution
>
> **[3:10](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/garbage-collection?u=76281980&t=190)** until we're done with the garbage collection. And we remove the elements that we no longer need. You may wonder isn't that bad for the performance of our application though? And you are right. It's sure is bad because we would have to pause our application until we're done removing the objects. All right, so isn't there a less costly way you may wonder? And there is, we could keep a counter on every object on heap. And this counter shows how often they were referenced. When a reference was removed, the counter would be decremented. And when reference was added, the counter would be incremented. When the counter is zero, it could be removed by the garbage collector. As far as probably, this sounds like a very clever and more efficient way, but there is one problem. A problem with cyclic references. You may wonder cyclic references, what do you mean? Well, let's have a look. So let's say that we have two objects here that are referencing each other. In this case, object eight and object nine are referencing each other. They both have the counter of one right now, even though they do not have a connection to the stack, they can never be called from within the application anymore. And these so called islands of isolation would live unnecessarily on the heap forever. We could probably add another counter here and that's for direct connections to the stack. And then say something like, if the item is not connected to an item that's connected indirectly or directly, to an item that connects to the stack, it should be removed.
>
> **[4:43](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/garbage-collection?u=76281980&t=283)** But again, this will only work if we're going to be pausing our application again. Cyclic references are two objects that reference each other. So let's have a look at situation right now. We're having object one, two, five, and six on our stack. So therefore, they all have a one but object two is actually being referenced by object five as well and therefore, as a counter of two. There are three objects with no reference to the stack. These are object three, eight and nine, but what if object eight and nine would hold a reference to each other? Their counters would be one for both of them. And even though they do not have a connection to the stack because there's only connection to one, two, five, six, and also indirectly to seven and four, they would live on the heap forever. Usually when I teach this someone asks, but what if you would add another counter here? And this counters actually going to be for the direct connection to the stack or to items with a direct connection to the stack. So say something like if the item is not connected to an item that's connected indirectly or directly to an item with a connection to the stack, it should be removed. Sure, this would work, but it would require pausing the application until the garbage collection process is done and it would become costly again because it would not be able to do anything else until all the objects that we don't need were removed. So what is the best option? Well, first of all, garbage collection
>
> **[6:17](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/garbage-collection?u=76281980&t=377)** is an expensive process in terms of performance, but it's also necessary and we don't have limited memory, so we need it anyways. Different languages have different implementations. Amongst them, define the heap in different parts and having objects that live longer being checked less often to make the past shorter. This is simplified explanation. And again, the exact implementation of the garbage collection depends on the system and language that you're using. In the next video, let's have a closer look at the actual removing also called sweeping.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **CLI Commands:** make (2), python (1), find (1)
> **Analogies:** for example (1), imagine (1), such as (1)
> **Env Vars:** ccr (1)
> **Cross-References:** in the next (1)
> **Definitions:** is an  (1)
> **Speakers:** - [narrator] (1)

#### [Actual removing or sweeping](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/actual-removing-or-sweeping?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/actual-removing-or-sweeping?u=76281980&t=0)** - [Instructor] The actual removing is often referred to as sweeping. So sweeping is just removing objects that we no longer need. Sweeping is also something that can be done in different ways. We have three options for sweeping: Normal sweeping, sweeping with compacting, and sweeping with copying. Let's start out with normal. Say, this is the memory usage here and these bits are marked. They can be deleted. This is what happens after deletion. The memory is fragmented. It's the old memory, but with available blocks of free space in between. So if you want to store a new bits like this one, we can store it here. And if we'd like to store a bigger value, it might not fit in the middle, but it'll fit at the end, but say we'd like to store an even bigger value. We do not have enough memory available. Well, we do have enough memory available in total but not in big enough block. So this will actually result in an out-of-memory error, even though we technically have enough space available. So this could have been avoided if you would've used the second option for sweeping, sweeping with compacting. So say that this is our memory again, and these bits are marked and thus will be swept. We don't just delete these bits and leave gaps, but apply compacting, so that after compacting, the memory looks like this. And if we want to store the bigger value now, we do have memory block large enough available,
>
> **[1:35](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/actual-removing-or-sweeping?u=76281980&t=95)** and we don't get an out of memory error. There is a similar strategy to sweeping with compacting available and that is sweeping with copying. Here is a memory again but this time we're having an extra piece of memory since we're going to copy. These are marked objects and can be removed. Instead of removing them and then compacting the memory, we're just going to be copying the unmarked elements to the other memory region, and then delete a full block. You may think this sounds a lot better than just sweeping with compacting. So why choose one or the other? And the key is in performance and memory usage here. The garbage collection topic is a real fun one and a true brain teaser and want to really improve the performance of an application with, when done well. Usually you just use a high-level language and the garage collector will do the work for you. And in most situations you don't need to understand how it's working, but where there are cases in which it's of great importance to understand how this is working exactly. For example, to avoid memory leaks, improve performance and understand certain concurrency problems better. You'll thank yourself having a good understanding of this process.

> [!info]- Semantic Content
>
> **Definitions:** is a  (3)
> **Analogies:** for example (1)
> **Best Practices:** the key is (1)
> **Speakers:** - [instructor] (1)


### 2. Memory Management in Code

[↑ Back to Table of Contents](#table-of-contents)

#### [The C way: Allocating memory](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-allocating-memory?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-allocating-memory?u=76281980&t=0)** - [Instructor] In order to explain [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) in real life, I'd like to start with C. Before you think something along the lines of, but Micah, why should we go back to something that was hot in the early 70s When we have fancy languages like [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), [Java](../../Skills/Software%20Development/Java.md), and C# now? Well, first of all, Python's most popular implementation is CPython and it actually means that it's written in C. Second of all, memory management is beautifully visible in C and even though it's probably not the best idea to write your new web backend in C, it's a great idea to start with C to understand memory management in coding. Now, what's really happening, and what is memory management like in C? The memory in C consists of different parts, the Stack, the Heap, BSS which stands for block starting symbol, DS, data segment, and the text segment. These last three may sound very new to you, so let's go over them briefly. BSS is a special segment for a static and global variables that are un-initialized at compiled time. The data segment usually just abbreviated to DS is for the initialized global and static variables. Then there's one last segment and that's for a compiled code that's being stored, this is called the text segment. When we're coding, we're using the first four memory areas in the list, we're not going to be worrying about static variables
>
> **[1:33](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-allocating-memory?u=76281980&t=93)** for now, just know that they get special region in the memory, let's have a look at the Stack memory. Local variables in C are placed in the Stack. Local variables in C are sometimes also called automatic variables because they're giving us space in memory automatically, this memory space gets freed up automatically as well when they're no longer needed or actually accessible, 'cause the method is done executing. All right, so here we're having a very basic C program. Let's just go ahead now and create a variable which is going to be placed on the Stack. Here we have in a and I'm going to be assigning 1 to int a, at this point our int a is now a local variable and it's automatically managed. I do not need to assign a memory to it. I can actually do this in multiple ways, I could first declare it on the one line and then actually initialize it on the second line. These are all still local variables. Also, when I go ahead and I'm going to be creating a new method up here which is going to be returning an int, let's say a very silly one, adding two numbers, clearly we could just use the plus symbol for this but let's go ahead and create a method for this. And in here I can be returning the result of x + y, now, x and y are actually local variables to the method add and not to the method main. And in here I could create a new one called maybe result
>
> **[3:08](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-allocating-memory?u=76281980&t=188)** and just store the result of adding a and b in there. So these are local variables and they're stored on the Stack and when I'm sending them to our method, add, this means that I'm actually just sending the value over not to reference to this number so to say. So this is what the Stack memory looks like when we're in a method add, on the Stack, this frame for method add is the current frame and method main is the one that we're going to return to when method add is done executing, you can see the local variables for each here. There are some problems with using the Stack memory though, first, it can only be used if the size of the variables is known at compile time. And secondly, Stack space is limited. Lastly, elements on the Stack can only be accessed by the function that they're declared in and this is where the Heap comes in. The Heap allows dynamic memory allocation. The one that happens during run time, this means that you don't need the size yet during compile time. And this is actually where we have to allocate memory manually in C, let's first have a look at the visualization before we start with some possibly confusing symbols. There are three concepts we need to think of here. If we have an element on the Heap, we need to know where this element is. This is called the address. The address to this element can be considered a pointer, in order for the Stack to know
>
> **[4:40](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-allocating-memory?u=76281980&t=280)** where to find element on the Heap, we stored a pointer on the Stack, this is a special type of variable. The variable on the Stack is not holding the actual element but it's just the location. So if you have two variables holding the same pointer and one of them uses the point to access the element and update it, the other variable, holding the same pointer, is also going to see the updated element since it's pointing to the same element, the point is here, pun intended, there's only one element and two pointers to this very same element. In C, we actually have three out-of-the-box methods that we can use to assign a memory area to a pointer, malloc, calloc, and realloc, malloc and calloc are used to assign a memory area on the Heap and realloc is used to reassign a new area where you need to resize the memory block, meaning that you can actually make it smaller and bigger. The allocator is going to look for a suitable memory block of the right size. This is actually a difference in which the Stack would be especially faster than the Heap, since for the Stack it's very clear where the memory block is going to be, just the next one on the Stack. The Heap can be more fragmented and this is actually the difference in speed. Let's have look at malloc. Malloc is a function that returns a pointer to some memory address without it being typed. This is indicated with the void asterisk return type. We have to send in the size of a memory block as the parameter.
>
> **[6:12](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-allocating-memory?u=76281980&t=372)** For example, we could say malloc 8 and this would return pointer to 8 bytes of memory. So, let's go ahead and create pointer. The asterisk here is indicating that this is going to be a pointer of type int, but at this point, our pointer is not pointing to a memory address yet, in order to have it point to a memory location, we need to assign the memory location and we're going to do this with malloc. So I'm going to go ahead and say x = and then I'm going to be casting the result of our malloc to the right int pointer and then I'm going to say malloc and now I'm just going to give it a 4, meaning that's going to be the size of 4 bytes. Now I can go ahead and just assign a value to this address and I'm going to give it the value of 42. So I could go ahead and print this just like this, so I can say our value and then in here I'm going to say %d and in your line, oh, a bit early,
>
> **[7:23](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-allocating-memory?u=76281980&t=443)** and then I'm going to say here I want to print the value of x. Let's go ahead and see what happens if we run this program, as you can see, it's printing our value 42. Let's just see if I can make this slightly more complicated for you to still understand what's going on. So I'm also going to be creating a pointer of type int called y right now and I'm going to be assigning the same memory address as I did for x by assigning x to it. So what I'm then going to do, I'm again going to be printing a value, this time I'm going to be printing y and I'm going to say before update here and you'll see y again, pun intended, just in a second now because clearly what I'm going to do is going to be updating x and then see whether y has changed or not. So what do you think? Is y going to be changed here? So then I'm going to be updating x, let's just assign 20 to it now. And then again, let's go ahead and print y after,
>
> **[8:49](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-allocating-memory?u=76281980&t=529)** all right, let's run our program again As you can see now, our value for x still 42, before we update x, y is 42, and after the update, it's 20. So this proves our point that actually it's pointed to the same memory address since y changes when x does. So this is not a course on C, but there's actually one thing in here that's a bit silly to do that I'd like to point out. And the thing is you usually don't assign real values to this, you typically go ahead and say sizeof and then just go ahead and say sizeof int which actually would also have been 4. And if you need more of them, say, you would like to have four times this size, we would go ahead say four times the size of int. I'm not going to dive into calloc because it's doing something similar and even though it's very interesting, we're just trying to understand memory management basics here. There is something that I haven't discussed yet, how long do these variables stay on the Heap? Well, in this case until the end of the program. C doesn't know when you're done with the variable in the Heap, so you need to remove it manually and if you don't, it'll live there until the end of the program. In this case, our program is very small and there's not a lot of time between the end of the variable and until when we need the variables. So, it's not that bad in this particular case but it's not hard to imagine situations
>
> **[10:21](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-allocating-memory?u=76281980&t=621)** in which you're storing a lot more on the Heap and in which case it would be very bad. So, what to do, we need to help C a bit. In the next video, we're going to have a look at deallocation to do just that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (5), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Definitions:** is a  (3), means that (2), is called (2), stands for (1)
> **CLI Commands:** python (2), make (2), find (1)
> **Analogies:** for example (1), just like (1), imagine (1)
> **Env Vars:** bss (2)
> **Cross-References:** in the next (1)
> **Prerequisites:** before we start (1)
> **Speakers:** - [instructor] (1)

#### [The C way: Deallocating and reallocating memory](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-deallocating-and-reallocating-memory?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-deallocating-and-reallocating-memory?u=76281980&t=0)** - [Instructor] As we've just discussed, if we don't remove a variable, it will live on the heap forever, or, well, that's a bit dramatic. It will live on the heap until a program ends. Of course, we don't want that. If we leave it there when we no longer need it, the heap will get unnecessarily full, and this can lead to memory problems, such as running out of memory. Yikes. So what do we do? We clean up. And how do we do that? We deallocate the memory when we're done with it. And how do we do that, you may wonder. We do so with a special method, free. Free takes the starting address of the memory block that should be freed as an argument and then frees the memory. What this exactly means depends on the exact C implementation, but it could be that the memory gets erased, or that the memory just gets released and can be overwritten with new data when we use malloc again. So in here, I'm just going to go ahead, and when we're done with it, I'm going to be setting the memory address free. So I'm just going to call free, and then I'm going to be sending in x here. All right, let's just go ahead and run this program and see what happens. As you can see, we don't see any noticeable changes here, because we're just freeing memory at the end of a program, and it was going to be released at the end of our program as well. Actually, I should change the pointers to point to NULL now, because now we have two dangling pointers
>
> **[1:33](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-deallocating-and-reallocating-memory?u=76281980&t=93)** that are still pointing to the freed up memory address. So you may wonder what happens. Well, let's just go ahead and print a value of y after freeing your memory region.
>
> **[1:51](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-deallocating-and-reallocating-memory?u=76281980&t=111)** As you can see, after freeing, it's still going to be 20. This means that my implementation did not reset this memory block. It just marked it to be ready for allocation again. So, let's see if we can break it. I'm going to be copying this first line here. And I'm just going to be assigning some new memory area, but this time it's going to be z. And also, I'm going to be adding some values in z here. Just go for a long number, and run it again.
>
> **[2:32](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-deallocating-and-reallocating-memory?u=76281980&t=152)** As you can see, this time we have something really weird when we want to have the memory address for y. Why is this, you may wonder. Well, on line 15, we called the free method. This means that the memory address that both x and y pointer were pointing to are ready for new allocation again. And this is actually what happened on line 16. It was a bit of a guess, but it was a very educated guess. There's nothing else happening. It's very likely that C will just grab the first free block of memory that it finds. And it did so the previous time as well. So the first block is still going to be the same block. And in this case, I'm assigning a value on line 17, which is then where y is pointing to accidentally as well. So what happened? Well, this is actually the dangling pointer bug in action. So what should we do? We should point y and x to NULL. So let's just go ahead and do just that.
>
> **[3:35](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-deallocating-and-reallocating-memory?u=76281980&t=215)** So let's go ahead and run it again, so you can see what will happen now. At this point, we get an exception. And you may wonder how that's better. And trust me, this is better. You want to have the exception in this case, because if you don't get the exception, you get a surprise, and it's going to be terrible to debug. So always make sure that when you're freeing a memory address that you also set your pointers to NULL. So now we can allocate and deallocate. There's one more important concept that we're just going to be discussing here. It's reallocate. This is done with the realloc function. It assigns a new block with a new size to a variable. This means that we're going to move our memory block to another memory block with a different size. And we do so with the realloc function. This will assign a new block with a new size or resize the old block, if possible. It is done by sending in the old block and then specifying the new size. The result is again a void pointer. If we want to have a specific type, we need to cast it. So realloc returns a new block in a memory, and if it's possible, it will just stretch the old block, but if there's something else that's coming after the old block, then it will have to go ahead and assign a new block. And after having returned this, it's not going to free the old memory. It's just going to be returning the new address. It's not freeing anything up. If there's values on there, they will remain on there until they're overwritten. So this is how [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) works if you have to do it manually.
>
> **[5:07](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-c-way-deallocating-and-reallocating-memory?u=76281980&t=307)** Nowadays, we have the luxury of not having to worry about it in many languages. Let's explore this in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (1)
> **Env Vars:** null (3)
> **Definitions:** means that (3)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **Analogies:** such as (1)
> **Best Practices:** always make sure (1)
> **Speakers:** - [instructor] (1)

#### [The automagic way in modern languages](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-automagic-way-in-modern-languages?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-automagic-way-in-modern-languages?u=76281980&t=0)** - [Instructor] All right, there is a lot of good news here. Many popular languages nowadays will help you so much with [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) that you don't need to worry about allocation and deallocation at all. Well, sort of. It's still happening, but there's no manual allocation and deallocation. Actually, some more good news. Using the language with automatic memory management helps avoid so many bugs. Automatic memory management will prevent dangling pointer bugs and it'll also avoid freeing of memory regions twice accidentally. Also, performance issues with your application due to poor memory management are less common. It's even so great that whenever you have the opportunity it's best to use the language with automatic memory management. Then there's a tiny downside. Since it's always going to be done for you, it can be hard to grasp what's going on beneath the surface, especially when something goes wrong. It can be hard to tell what exactly. Therefore, it really helps to understand how automatic memory management is working. It may not surprise you at this point, but this is going to be different depending on the language, the implementation of the language, the settings, and the system that you're using. Some languages that implement automatic memory management are [Java](../../Skills/Software%20Development/Java.md), C#, [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md), Ruby, [JavaScript](../../Skills/Software%20Development/JavaScript.md), and actually many, many more. They all have their own system in place for allocating memory. And the most common automatic memory management for releasing of the memory is one that we have seen, the garbage collector, which is as you can guess
>
> **[1:35](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/the-automagic-way-in-modern-languages?u=76281980&t=95)** different for every system and language that you're using. As I already said, the languages are going to have different implementations of automatic memory management and handle allocation and garbage collection differently. Let's have a look at how is Python is putting this off in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (8), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2), [Java](../../Skills/Software%20Development/Java.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **CLI Commands:** python (2), ruby (1)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)
> **Best Practices:** it's best to (1)
> **Speakers:** - [instructor] (1)

#### [Memory management in Python](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/memory-management-in-python?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/memory-management-in-python?u=76281980&t=0)** - [Narrator] [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) is a very popular language amongst beginning programmers, well, not only amongst beginners. It has a beautiful, clean syntax, but one of the reasons for this popularity is that it helps with both [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) and that it's dynamically typed. Well, what do I mean with dynamically typed? In Python, when you create a variable, you don't need to tell Python what type the variable will have, Python will figure it out for you. How Python manages the memory exactly? Depends on the Python implementation that you're using. CPython is by far the most popular implementation, so let's dive into how it's done for CPython. Since CPython is written in C, this story might sound familiar but it's going to be a bit more detailed. When we run a Python program, we do so in a designated area of the RAM. Our computer is using the RAM memory to run more applications than just our Python program though. So only the reserved area is going to be meant for Python. In Python, there's a stack area and a private heap area. On this private heap, we're dealing with objects, and almost everything in Python is an object, so there's a lot living on the heap in Python. The stack is just keeping track of the method we're executing and the variables of this method. This might sound familiar to what we have seen before. The variables on the stack hold addresses of objects on the heap. Here, we see the Python docs and more specifically, the type PyObject. Every object in Python is a PyObject
>
> **[1:35](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/memory-management-in-python?u=76281980&t=95)** and this PyObject holds the type of the PyObject, and the number of times the object is referenced. The reference counter. Reference counter gets used for garbage collection. It literally is a count of the references to a certain object. If an object has a reference count of zero, it's safe to say that it can be garbage collected since it's not being used anywhere anymore. It then gets freed. The allocation is more complicated. The CPython implementation has a special allocator that's allocating a memory for any object that needs a memory area. The allocator is a complex piece of code that's using the C functions to allocate memory. CPython has a special way of allocating memory. In order to understand it well, we need to know the three key concepts, arenas, pools, and blocks, these are all bits of memory. Arenas are the largest bits of memory, they are parts of the computer memory that have sequential blocks of memory. The Python memory consists of multiple arenas and these are kept in a link list. On the arenas, we have pools, and pools contain blocks. Blocks are the actual memory addresses that are going to be assigned. These pools contain blocks of memory of a certain same size. So if you need a certain size, you need to select a certain pool for this. The allocator is going to look for an available block of the right size, so we can have a pool of four by blocks and a pool of eight by blocks, etcetera.
>
> **[3:09](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/memory-management-in-python?u=76281980&t=189)** All the blocks on the pool are the same size, this way, it's easy for the allocator to find a block of the right size. But how does the allocator know which arena has a pool with a free block? The arenas are on a doubly linked list called usable arenas, and this list is sorted by pool availability. The arenas with the fullest pools are on the front of the list. Then, there is a special list that holds pools that are used, the used pools list. And there's a special list that holds the empty pools, the free pools list. Python tries to use the arenas with the fullest pools first. And on these fullest arenas, it's going to try to use the used pools first. Why? Well, when memory on a pool gets freed, it doesn't get freed for the whole OS to use, but just for Python to use. Only when a complete arena gets freed, this is given back to the OS. If Python would use an unnecessary amount of arenas, Python would have an unnecessary large footprint on the system's main memory. So how do pools get organized? Pools have a state, they can either be used, full, or empty. If memory on a full one gets freed, it changes back to used. And if memory on an empty pool gets used, it changes, well, to used. CPython tries to find a block on used one first. If there is not a large enough block of memory available, it'll use a block on an empty pool,
>
> **[4:42](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/memory-management-in-python?u=76281980&t=282)** that's going to be containing blocks of the size of the memory block that we needed. And this previously empty pool is then removed from the free pools list and you may have guessed it, added to the used pools list. Blocks on a pool hold a state as well, untouched, free, and allocated. Untouched means that a block was never allocated during the run time of a program. Free means that it was allocated, but that it got freed and can be overwritten. Allocated means that the data on the memory block is allocated and still in use. So this is roughly how the allocator works. There's one more concept that you should be familiar with when we talk about memory management, and that's the add complexity of multithreading and memory management. Multitreading means that there are multiple things happening at the same time. And when there are multiple things happening at the same time, it's possible that multiple things are trying to access the same memory resource to do something with this. Things may get messy. The multiple things happening at the same time is also called concurrency. Luckily, there is a mechanism in place to avoid multiple threads accessing the same shared resource. This is the global interpreter lock. When a CPython thread access to memory, the global interpreter lock is activated and this guarantees that no funny business will happen because it avoids that multiple threads can access the same shared resource, this way, shared resources can be used safely. All right. So this was all very theoretical.
>
> **[6:17](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/memory-management-in-python?u=76281980&t=377)** Let's have a look at the simplified [Simulation](../../Skills/Hardware/Simulation.md) of what happens to the stack and the heap when we're coding Python.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (20), [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (3), [Simulation](../../Skills/Hardware/Simulation.md) (1)
> **CLI Commands:** python (20), find (2)
> **Definitions:** is a  (6), means that (3), is an  (1)
> **Env Vars:** ram (2)
> **Speakers:** - [narrator] (1)

#### [Memory in Python visualized](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/memory-in-python-visualized?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/memory-in-python-visualized?u=76281980&t=0)** - [Instructor] Let's make it more visual. We're going to have a look at [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) code and a visualization of what is happening with the stack and the heap for every line of code. We're not going to show the allocator as explained in a previous video, but just assume that there is some space on the heap available. In order to build it up slowly, let's just start with something simple in our main method.
>
> **[0:29](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/memory-in-python-visualized?u=76281980&t=29)** On the stack, we have a frame for the main method. And our main method contains a variable x that is pointing to an object on the heap. And that object is of type int and holds the number 5. So if we add a variable, say y is 7, we're going to be adding a variable to our main that is pointing to another object on the heap, namely an object that is storing an int of 7. Please note that in Python, if you would've made y 5, it would not have created a new object of 5 on the heap, but just pointed to the old one. Numbers in Python are immutable. So if the x would be modified, say x would turn into 2, it would lead to the creation of a new object, and y would still be pointing to the 5. So this would not lead to [Data Integrity](../../Skills/Cybersecurity/Data%20Integrity.md) problems and y all of a sudden pointing at 2 as well. All right, so let's go back to our situation and add a method to add these numbers.
>
> **[1:39](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/memory-in-python-visualized?u=76281980&t=99)** And in here I'm going to be creating yet another variable, result, and that's going to be the result of number 1 plus number 2, and will return result. Please note that I'm not learning you how to code Python efficiently, but I'm just trying to point out how the memory is working here. And when we call these methods from our main, a new block on the stack would be created. And this block would have the variables that are necessary in the block, meaning the number 1, the number 2, and the result. As you can see, the number 1 and number 2 are double. And they point to the same objects as x and y of our main. When the add method is done executing, the stack frame will disappear. And the objects on the heap that have no relation anymore to the stack will disappear as well, because their counter is going to be 0. When the main is done executing, also that frame will be removed, and our stack is going to be empty. All right, so that's how CPython memory is working. There's plenty of room to dive deeper, but this is enough knowledge to understand the foundation of [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) in Python. And it will help you understand how other languages do it as well, even though they might do it a bit differently. You may think, "Now we have automatic memory management. What could possibly go wrong?" But still, it's very important to be aware of what could possibly go wrong. So next up let's have a look at memory leaks.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (5), [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (2), [Data Integrity](../../Skills/Cybersecurity/Data%20Integrity.md) (1)
> **CLI Commands:** python (5), make (1)
> **Warnings:** note that (2), be aware (1)
> **Cross-References:** previous video (1), go back to (1)
> **Speakers:** - [instructor] (1)


### 3. Memory Leaks and Best Practices

[↑ Back to Table of Contents](#table-of-contents)

#### [What are memory leaks?](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/what-are-memory-leaks?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/what-are-memory-leaks?u=76281980&t=0)** - [Instructor] Let's talk about one of the biggest problems that can come from poor [Memory Management](../../Skills/Web%20Development/Memory%20Management.md), memory leaks. When I ask students what they think a memory leak is at least one of them always answers that this is random memory loss. This is not what it means, but it makes sense considering how we use leaks in day-to-day vocabulary. So with our memory leaks? Memory leaks are the accumulation of objects on memory that are actually no longer needed. Objects are created, but the memory doesn't get freed when they are no longer needed. The reasons for this may vary. And the number of objects on a memory will just keep on increasing, sometimes this goes very slowly, but it can also go quite fast. What happens if we have a memory leak? Eventually, the memory gets so full, it slows down the application and possibly entire system. If the memory is really full, it'll crash. Memory leaks are serious problem. Even nowadays, when most applications are developed in a high level language with memory management built-in. And also even on super fast servers, RAM is a limited resource that can get full. And if you host your application in the cloud, poor memory management and actually poor configuration of the cloud, will result in unnecessarily high cloud cost because you need to run instance that's bigger than it should be due to memory leaks. Crashed applications are unavailable applications, but also when it's very slowed down, it might be unavailable
>
> **[1:35](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/what-are-memory-leaks?u=76281980&t=95)** or perceived as malfunctioning by your end users. There is no need to tell that unavailable applications are not good for keeping, let alone growing users of your application. Let's see, pun intended, some common causes for memory leaks. In C, and languages where you have to manage the releasing of research manually, there are different causes for memory leaks than for other languages. Because memory leaks can just come from forgetting to call the free method. If you do this at one spot, that's rarely used, it is a memory leak, but the impact will be very small and it won't lead to a crashed application very soon. But if you do this at a very important piece of your code, or even in a loop, the impact will be noticeable way sooner. In languages with automatic memory management, this is a different story. For various reasons, the automatic garbage collector sometimes doesn't perceive the useless object as garbage. This is typically due to a coding or design flaw of the code. For example, given the sort of database aesthetic variable, this would keep all the objects on the (indistinct) alive and it's a horrible practice. When a memory is so full and a memory block we need is not available anymore, We'll encounter some sort of out-of-memory error. This kills the application. Let's have a look at this error and create it in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (4)
> **Definitions:** is a  (3)
> **Env Vars:** ram (1)
> **Cross-References:** in the next (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Out of Memory error](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/out-of-memory-error?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/out-of-memory-error?u=76281980&t=0)** - [Instructor] The out-of-memory error occurs when you're, well, out of memory. This happens when the system cannot allocate a requested memory block anymore. Usually, before this happens, the application starts to slow down first. Some applications that have memory leak will request a restart every now and then, sometimes even only every few weeks or months, in order to free the unnecessarily kept memory and make the application perform well again. If you're not running the application on a memory restricted system, for example in the cloud with a lot of resources available, the sign might not be a slow application but an unnecessarily high cloud provider bill. The slowing down is one of the signs that your application is having a memory leak, and it may end in an out-of-memory error when it's not restarted to boost performance again. Part of the reason that the application gets slow before running out of memory is due to the system having issues with finding free spots for allocation of variables, so the allocation process takes longer than when a lot of memory is still free. Another reason for slowing down beforehand is that some languages with built-in garbage collectors can get very slow before ending in an out-of-memory error, since a garbage collector will work very, very hard and this will pause the program in many cases, and it does so trying to avoid the out-of-memory error. This has a huge impact on performance. Other optimizations might also slow down the application.
>
> **[1:34](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/out-of-memory-error?u=76281980&t=94)** For example, reorganizing the fragmented memory in order to free bigger blocks. If all this fails, we'll get an out-of-memory error and our program crashes. Let's go ahead and cause an out-of-memory in C. All right, so here we're having a very basic C program. Let's now just go ahead and create a pointer X, and X is actually going to be pointing to a really big block of memory. It's an end pointer, and we're going to call malloc here and say that we want to have, well, 200,000 times the size of an inch.
>
> **[2:18](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/out-of-memory-error?u=76281980&t=138)** All right. Will this make our application go out of memory? Well, in my case, definitely not, in your case, probably not. But if I'm going to do this in a loop, then that's actually a different story.
>
> **[2:37](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/out-of-memory-error?u=76281980&t=157)** Let's go ahead and see what will happen if we run our program. I'm going to run it in debug mode so I can see the output in a debug console.
>
> **[2:53](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/out-of-memory-error?u=76281980&t=173)** As you can see, it's running right now, and it won't be long before it actually runs out of memory. And there we have it. Now it's running out of memory, but it keeps doing so. Why, you may wonder. Well, we're calling this in a loop, and it's not dying because of the error, it's just returning NULL, and we get the error: malloc can't allocate region. And it keeps on doing so. So what's a better way to do this? Well, first of all, don't do this, but if you must, go ahead and make sure that your malloc is actually going to be returning a value. So how to do this? Well, I'm going to say if our X is NULL then I'd like to break the program. So if we run the program again right now, you'll see that it's only going to show the error once, and then it breaks and it shuts down.
>
> **[3:58](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/out-of-memory-error?u=76281980&t=238)** The reason that it shuts down is because we're having nothing after the program. Our program would actually just continue if you had other statements in place. And there we have the error. As you can see, it's only printing it once now. In the next video, we're going to have a look at avoiding memory leaks.

> [!info]- Semantic Content
>
> **CLI Commands:** make (3)
> **Env Vars:** null (2)
> **Analogies:** for example (2)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Avoiding memory leaks](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/avoiding-memory-leaks?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/avoiding-memory-leaks?u=76281980&t=0)** - [Instructor] How to avoid memory leaks? Of course, this depends a bit on the exact system and language, but let's have a look at [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md). How to avoid memory leaks in Python? Well, in order to answer that question, let's start with creating some memory leaks in Python. When Python runs out of memory, you get a memory error. An easy one to run out of memory is to load a file that is too large. This is actually something that can easily happen when working with large data sets or by keeping references unnecessarily. Data can be fetched using special generator functions or processing it in batches instead. The generator functions return an iterator to loop over the data without having all the data in the RAM. There are actually solutions to avoid working with huge data sets in the memory that can be used out of the box, such as [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) or Keras. This way, you can operate on large data sets and get the data only when you need it. They manage the memory effectively for you. There's actually an important side note to make for Python in particular. It can happen that you feel that your RAM size should definitely be big enough for a data set, but you still get a memory error. It might be due to the Python version you are using. Make sure that you have the 60-bit version if your system is 64-bit. There's another great way to run out of memory, and that's allocating objects and not letting them go. It's a bit like not managing your house. You may have space for all the things you need,
>
> **[1:34](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/avoiding-memory-leaks?u=76281980&t=94)** but you wouldn't have enough space if you kept everything that you don't need. So, here we're having a little example that's keeping objects for too long. Let's have a look at what's happening line by line. On the first line, we are importing resource. And then, on the fourth line, we could also choose to just go ahead and set the limit for address space. In my case, it doesn't really matter, because it's not working on any of these, which is why I'm going to show you how to run this in a different environment. But let's continue first. Just note that if you have a MacBook and you try to code along, this might not work for you. Why I want to set the limit, you may wonder. Well, I'm going to tell you in a minute. So, we're setting this limit, and we're setting this limit very low. Then, on line 6, I'm creating a list, just some list, doesn't really matter. And on line 8 to 10, I'm just having a class which has the property x. Then, I'm creating a loop, and I'm using a counter in this loop. And I'm just going to be appending endlessly elements of Example, with an incremented i to our some_list. And this is why I've set a limit, because if you don't set a limit and you have a good computer, this is taking super long before it's going to run out of memory. But, this is why I'm going to be copying this code over to the browser and run it over there to show you that you get a memory error if you do this. So, I'm using Replit for this, and I have selected Python 3. So, I'm going to go ahead and copy our code.
>
> **[3:09](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/avoiding-memory-leaks?u=76281980&t=189)** Now I'm going to run our program, and you will soon see we get a memory error. It managed to get up to 525, and then it got our memory error. So, memory errors like these are perhaps a bit less likely to happen in Python than it would be in C#, [Java](../../Skills/Software%20Development/Java.md), or a similar language. Why, you may wonder. Well, it's more common over there to keep, even though it's a horrible practice, things in static variables for a really long time. So the over-allocating, so pretty much allocating many, many objects, less likely to happen in Python. But, that doesn't mean it won't happen in Python. I've definitely seen this happen in Python as well. Well, how to solve this? That's actually not too bad. Just remove the references when you no longer need them anymore. This way, the reference count will reach zero, and the garbage collector will pick them up. Let's have a look at some best practices for [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) and Python.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (12), [TensorFlow](../../Skills/Artificial%20Intelligence%20(AI)/TensorFlow.md) (1), [Java](../../Skills/Software%20Development/Java.md) (1), [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (1)
> **CLI Commands:** python (12), make (2)
> **Env Vars:** ram (2)
> **Code Identifiers:** some_list (1)
> **Versions:** python 3 (1)
> **Analogies:** such as (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Best practices with memory](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/best-practices-with-memory?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/best-practices-with-memory?u=76281980&t=0)** - [Instructor] This point, you already know what pointers or references are. We have seen that when we share the reference, the object on the heap can be accessed from multiple places. This means that if we modify the object on the heap via one pointer, the object also was modified for other places in our code that are holding the same pointer, since there's only one object, and this very object has changed. So no news there. But this passing by reference is also true for returning objects from methods or for creating objects using other objects. The reference to the object gets sent. This way, the object can be accessed by the caller of the method. This can also happen through third-party libraries in our application. Whenever this happens without it being the intention, we call this escaping references. Even though this might not be too surprising to you, security breaches that this behavior can cause might be a surprise. Let's have a look at a very simple escaping reference example. So here we have some code. Let me go over it. On line 1 to 4, I'm creating a class, Course. It has two properties, topic and learners. Then I have two methods. Delete_first, which is going to do nothing but deleting the first item of some list. Then I have method main. And in this method main, I have a list of learners. I'm printing them on line 12. Then on line 13, I'm creating a new Course called [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) with a list of learners.
>
> **[1:34](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/best-practices-with-memory?u=76281980&t=94)** Then I'm going to be calling the delete_first using my list of learners as an argument. Then on line 15 and 16, I'm going to be printing the learners. First of all the learners in the main, but also the learners that are in our course. You can probably guess what the outcome will be based on my previous explanation, but let's put it to the test. As you can see, the outcome says the learners contains four items. Then the list in main and the list in Course have changed after calling the delete_first, because there's only one list, and if this gets changed, even if this is inside some other methods, it's going to be changed everywhere. This is why you should be very careful with this principle called passing by reference. And often you would want a solution for this and avoid security breaches. You will only need a solution for objects that are mutable, since immutable objects are not changed, but a new object at a new address is being created. So this won't influence the behavior at different locations. The solution for mutable objects is typically creating copies. Be careful to not just create a copy of the object, because this would be considered a shallow copy. References of the object to other mutual objects are copied literally. So these references may escape. In order to be safe, a deep copy needs to be created. A deep copy is a copy of the object
>
> **[3:10](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/best-practices-with-memory?u=76281980&t=190)** and a copy of all the mutual attributes and a copy of all their mutual attributes, and so on. So in our situation, we would've grabbed the learners inside our init method in Course and actually have made a copy of it over there to make sure that they could not be modified from outside. Then there are some other practices to be aware of. In [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) specifically, you cannot really control the memory management, at least not directly. We have already discussed some best practices to avoid memory leaks, but there're also some best practices to not use more memory than necessary. So what can you control? Well, first of all, you can control object creation, and you can also control to not unnecessarily recreate an object or getting rid of the object later than necessary to some extent. The first tip would be to not utilize the plus for string concatenation. At least not when you want to do many of them. The string is immutable, and you'll be creating a new string for every concatenation. So if you are concatenating five separate words, how many strings have been created on the fly? Well, five actually, because for every step in the concatenation, a new string is created. It's better to use the join method. I actually have a little script for you. In the script, on line 1, I'm importing timeit, and timeit doesn't say everything about memory management, but it gives some good indications about performance, though.
>
> **[4:43](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/best-practices-with-memory?u=76281980&t=283)** So on line 4 to 8, I'm having a method add. And in add, I'm sending in a list of items, and in a for loop, I'm just going to be concatenating all these items to the result. Then I'm returning the result. Then on line 11, I'm having a huge list. Well, actually, that's maybe a bit overexaggerated. It's actually only 10 hi's. Then on line 12 and 13, I'm doing timeit, and I'm doing it in two ways. I'm using the add function for this, and I'm using the join function for this. And then I'm measuring how long it takes me to actually do this. So let's have a look what this is printing. You may have some ideas based on the story I just told you. And as you can see, the first one takes much longer, and this is the old-fashioned add. The second one with join is much faster. I could run this a few times, but it comes down to numbers like this all the time. So what does this tell us? Well, that join is a much better choice for performance. Another best practice for the memory would be to work with setting the slots property for your own Python classes. Whatever you specify in slots are the attributes for which space will be allocated. This way, the object is not using a dynamic dict, and better in terms of memory usage, since it saves space in the memory. So I've written a little program here, and let's go over it briefly.
>
> **[6:16](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/best-practices-with-memory?u=76281980&t=376)** I have two classes, one class Course on line 4 to 7. We have seen this one already. It has a topic and a list of learners. Then we have another class, Course_Restricted. And it's actually the same as our previous course, but this time we have a slots attribute, and we specify only topic and learners as allowed attributes. This has the added side effect that if we want to extend our object, this is not possible. So on line 18, I go ahead and I'm creating some learners. Then on line 19, I'm creating a Course object, and on line 20, I'm creating a Course_Restricted object. So if I would go ahead and expand c, this would be fine. I could just say something like c.level equals beginner. But I cannot go ahead and say cr.level equals beginner. Why? Well, it's restricted, and there's no level in the list of allowed attributes. Let me show the error that we'll get. As you can see, we get the AttributeError. It simply says Course_Restricted has no attribute level, and we cannot add it. Great. So let's clean this up again, because actually what I'm doing next is I'm measuring two things. I'm measuring Course_Restricted, and I'm measuring Course. How am I doing this? I'm using tracemalloc for this. And what we'll get are two printed values. One for Course_Restricted, one for the regular Course.
>
> **[7:51](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/best-practices-with-memory?u=76281980&t=471)** Let me add this in so we can see which one it is.
>
> **[8:11](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/best-practices-with-memory?u=76281980&t=491)** So let's go ahead and run this program. Here we have our sizes in the output. And as you can see, the Course is a lot bigger than the Course_Restricted. So it definitely pays off to use slots if you know which attribute your object is going to get and you really need to mind your memory. And lastly for now, if you do not know what's going on, explore it. Most languages have tools to get insights in memory usage. For Python, for example, we have heapy, but here we're going to have a bit of a light version to see what's going on. 'Cause we just have a simple example, and we're going to be working with a basic memory profiler. Of course, you would need to install it with pip first. So here's my script. If you haven't done this, you need to install two packages. We need wheel, and we need memory profiler. Let me show you. First, you'll need wheel, pip install wheel. I already have it. That makes sense, because I've already set this up. And then you would also need the memory profiler, pip install memory_profiler. Also I have this one already. Good. So let's have a look at what I'm doing in my code. Actually, there's not a lot of code. I'm just importing profile from the memory profiler. And I'm using this as a decorator for my main function. This is the function that the memory profiler is going to analyze,
>
> **[9:45](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/best-practices-with-memory?u=76281980&t=585)** which makes a lot of sense, since there's only one function in here that's actually doing something. Okay, so I'm just creating some variables on line 5 and 6, and I'm creating some bigger variables lists on line 7 and 8, and on line 9, I'm deleting one of these bigger ones. So let's just go ahead and run this. I need to run this in a special way, though, to activate the memory profiler. So I'm going to be opening up a terminal. So I'm going to say python3, and then I'm going to say -m memory_profiler, and then I'm going to say analyze.py, and I'm going to go ahead and run this. And there's our output. I really like this output. It's so simple, but it's telling me what's happening on every exact line. So it starts at the @profile decorator, and the memory usage at that point is 16.6. Then you can see at line 5 and 6, 0 gets added. This is technically not true, but it's so small that you won't notice it, because we're talking about MiBs here. Then on line 7, you can see quite something gets added there. And on line 8, only a little bit gets added. Then on line 9, we'll remove the big one, and you can see that the increment right now is negative. We actually get something else when we install the memory profiler, and that's mprof. Let me show this to you. I'm going to say mprof run,
>
> **[11:18](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/best-practices-with-memory?u=76281980&t=678)** and then I'm going to run our analyze.py again. And now this is done. I'm going to go mprof plot. As you can see, it says matplotlib is needed for plotting. So we need to import pylab. So let's go ahead and say pip install matplotlib over here.
>
> **[11:50](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/best-practices-with-memory?u=76281980&t=710)** And now let's run mprof plot again. And it has opened it on my other screen. So let me show you what it created. There we have it. This is our graph. And you can see the similar numbers as we just saw on the table. We can easily see the memory used, versus your time in seconds, in this table. There's actually quite a bit more possible, but that's something you'll definitely find out once you need it. All right, we've seen quite a bit of the best practices. We have seen the memory error and what not to do. We've seen how to work with string concatenation. We have seen slots. And we've made a very, very brief introduction to profiling memory. Let's continue with our conclusion and see which next steps you could take.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (3), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3)
> **CLI Commands:** pip (4), python (3), make (1), python3 (1), find (1)
> **Prerequisites:** install (6), you'll need (1)
> **Definitions:** is a  (3), means that (1), we call this (1)
> **Code Identifiers:** delete_first (2), memory_profiler (2)
> **File Paths:** analyze.py (2)
> **Warnings:** be careful (1), be aware (1)
> **Versions:** 16.6 (1)


### Glossaries

[↑ Back to Table of Contents](#table-of-contents)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps in memory management](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/next-steps-in-memory-management?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/programming-foundations-memory-pointers-and-garbage-collection/next-steps-in-memory-management?u=76281980&t=0)** - [Instructor] Congratulations, you've made it truly [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) fundamentals. You're ready for some next topics now. Topics that could be a great fit at this moment are for example, math and general computer science topics, but also a deeper dive in your programming language that you know most about. Or perhaps, it's the right time for you to learn a new programming language. And depending on your current level, it might actually be nice to pick up some more advanced topics, such as [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) and concurrency. Some of the courses that would be a great follow up based on this are [Programming Foundations](../../Skills/Software%20Development/Programming%20Foundations.md), discreet mathematics, but also monitoring and logging, or for example, as a new programming language, [JavaScript Essential Training](JavaScript%20Essential%20Training.md) if you don't know [JavaScript](../../Skills/Software%20Development/JavaScript.md) yet. Also interesting would be advanced design patterns, design principles, if you're a bit further already. All right, thank you so much for taking my course, and let's connect. You can find me on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md), my name is Maaike van Putten. If you share you finish this course and you tag me, I'll make sure to like your post. So that concludes our course. But wait, there's one more thing, do you remember the number that we've placed in your short term memory, at the very beginning of the course? If you do go ahead and post on LinkedIn, perhaps share what you've learned in this course and tag me. I will then congratulate you on finishing the course. Hopefully this course will become a happy, long-term memory for you. Thank you so much and good luck.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Design Patterns](../../Skills/Software%20Development/Design%20Patterns.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2), [Memory Management](../../Skills/Web%20Development/Memory%20Management.md) (1), [Programming Foundations](../../Skills/Software%20Development/Programming%20Foundations.md) (1)
> **Analogies:** for example (2), such as (1)
> **CLI Commands:** find (1), make (1)
> **Best Practices:** make sure to (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [Maaike van Putten](../../Instructors/Software%20Development/Maaike%20van%20Putten.md)

## Path Context

### In [Become a Programmer- Foundations](../../Paths/Software%20Development/Learning%20Paths/Become%20a%20Programmer-%20Foundations.md)
← [Programming Foundations Databases](Programming%20Foundations%20Databases.md) | **8 of 12** | [Programming Foundations- APIs and Web Services](Programming%20Foundations-%20APIs%20and%20Web%20Services.md) →

## Appears In

- [Become a Programmer- Foundations](../../Paths/Software%20Development/Learning%20Paths/Become%20a%20Programmer-%20Foundations.md)

---

[↑ Back to top](#)