---
type: course
cssclasses:
  - lle-course
  - lle-level-beginner
slug: c-sharp-essential-training-2-generics-collections-and-linq
url: "https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq"
duration_minutes: 195
duration: 3h 15m
level: Beginner
updated: 12/8/2023
learners: 45099
skills:
  - Language Integrated Query (LINQ)
  - C#
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQEEIn0Zoh7ySw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1702331417325?e=2147483647&amp;v=beta&amp;t=jEEKVyoa5ZugROjjzkNxq7pRCCia5ocmVs57mFMJfoE"
linkedin_topic: Software Development
learning_paths:
  - '[C- Foundations- From Basics to Building Blocks](../../Paths/Software%20Development/Learning%20Paths/C-%20Foundations-%20From%20Basics%20to%20Building%20Blocks.md)'
  - '[Getting Started as a C- Developer](../../Paths/Software%20Development/Learning%20Paths/Getting%20Started%20as%20a%20C-%20Developer.md)'
prev_courses:
  - '[C Sharp Essential Training 1 Types And Control Flow](C%20Sharp%20Essential%20Training%201%20Types%20And%20Control%20Flow.md)'
  - '[C Sharp Essential Training 1 Types And Control Flow](C%20Sharp%20Essential%20Training%201%20Types%20And%20Control%20Flow.md)'
next_courses:
  - '[Debugging in C-](Debugging%20in%20C-.md)'
  - '[C- and .NET Essential Training](C-%20and%20.NET%20Essential%20Training.md)'
path_nav: '[{"path":"C- Foundations- From Basics to Building Blocks","position":2,"total":5,"prev":"C Sharp Essential Training 1 Types And Control Flow","next":"Debugging in C-"},{"path":"Getting Started as a C- Developer","position":2,"total":9,"prev":"C Sharp Essential Training 1 Types And Control Flow","next":"C- and .NET Essential Training"}]'
path_count: 2
tags:
  - course
  - topic/software-development
  - skill/language-integrated-query-linq
  - skill/c
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/C-%20Essential%20Training%202-%20Generics%2C%20Collections%2C%20and%20LINQ.md)

![C# Essential Training 2: Generics, Collections, and LINQ](https://media.licdn.com/dms/image/v2/D560DAQEEIn0Zoh7ySw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1702331417325?e=2147483647&amp;v=beta&amp;t=jEEKVyoa5ZugROjjzkNxq7pRCCia5ocmVs57mFMJfoE)

# C# Essential Training 2: Generics, Collections, and LINQ

> As one of the most popular programming languages, C# continues to make programming easier and more fun with each new version. In this course, instructor Matt Milner digs deeper into essential features of C#, such as generic types, collections, handling exceptions, asynchronous programming with tasks, and more. Matt begins with helping you understand and work with generic types, then moves on to un

> [LinkedIn Learning](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq) | 3h 15m | Beginner | 45K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Essentials deep dive](#essentials-deep-dive)
  - [What you should know](#what-you-should-know)
  - [CoderPad tour](#coderpad-tour)
- [**1. Generics**](#1-generics) (5 videos)
  - [Understanding generics](#understanding-generics)
  - [Working with generics](#working-with-generics)
  - [Defining generic types and methods](#defining-generic-types-and-methods)
  - [Generic constraints](#generic-constraints)
  - [Solution: Generic type with constraints](#solution-generic-type-with-constraints)
- [**2. Working with Collections**](#2-working-with-collections) (6 videos)
  - [Understanding collection types in the BCL](#understanding-collection-types-in-the-bcl)
  - [Choosing the right collection type](#choosing-the-right-collection-type)
  - [Working with collections](#working-with-collections)
  - [Working with keyed collections](#working-with-keyed-collections)
  - [Understanding concurrent collections](#understanding-concurrent-collections)
  - [Solution: Choose the right collection](#solution-choose-the-right-collection)
- [**3. Exception Handling**](#3-exception-handling) (6 videos)
  - [Basics of try-catch-finally](#basics-of-try-catch-finally)
  - [Exception filtering by type with catch](#exception-filtering-by-type-with-catch)
  - [Exception filtering with when](#exception-filtering-with-when)
  - [Throwing and rethrowing exceptions](#throwing-and-rethrowing-exceptions)
  - [Creating custom exceptions](#creating-custom-exceptions)
  - [Solution: Multiple exception types](#solution-multiple-exception-types)
- [**4. Programming Threads with async and await**](#4-programming-threads-with-async-and-await) (5 videos)
  - [Threading basics](#threading-basics)
  - [Basics of using async and await](#basics-of-using-async-and-await)
  - [Task management](#task-management)
  - [Handling errors when using await](#handling-errors-when-using-await)
  - [Solution: Task management](#solution-task-management)
- [**5. Delegates and Events**](#5-delegates-and-events) (7 videos)
  - [Understanding delegates](#understanding-delegates)
  - [Understanding events](#understanding-events)
  - [Common delegates with Action<T> and Func<T>](#common-delegates-with-actiont-and-funct)
  - [Understanding lambda expressions](#understanding-lambda-expressions)
  - [Expressions as delegates](#expressions-as-delegates)
  - [Other uses for expressions](#other-uses-for-expressions)
  - [Solution: Delegate using Func<T>](#solution-delegate-using-funct)
- [**6. Extension Methods**](#6-extension-methods) (5 videos)
  - [The case for extension methods](#the-case-for-extension-methods)
  - [Writing an extension method](#writing-an-extension-method)
  - [Using an extension method](#using-an-extension-method)
  - [Common extension methods on collections](#common-extension-methods-on-collections)
  - [Solution: Extension method](#solution-extension-method)
- [**7. Language Integrated Query (LINQ)**](#7-language-integrated-query-linq) (5 videos)
  - [Understand the purpose of LINQ](#understand-the-purpose-of-linq)
  - [Querying collections with LINQ](#querying-collections-with-linq)
  - [Filtering and ordering queries](#filtering-and-ordering-queries)
  - [Other sources for LINQ](#other-sources-for-linq)
  - [Solution: LINQ query to filter and sort](#solution-linq-query-to-filter-and-sort)
- [**Conclusion**](#conclusion) (1 videos)
  - [Build something and continue learning](#build-something-and-continue-learning)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Essentials deep dive](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/essentials-deep-dive?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/essentials-deep-dive?u=76281980&t=0)** - [Instructor] C# is one of the most popular programming languages in use and continues to make programming easier and more fun with each new version. In this course, we'll dig deeper into some essential features of C#, like generic types, lambda expressions, [language integrated query](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md), and more. I'm Matt Milner, and I've been programming with C# for 20 years. I continue using C# because I love how the language has evolved in the improvements I've seen to make programming easier. So please join me as we explore more of the essential parts of C#.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Language integrated query](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) (1)
> **CLI Commands:** make (2)
> **Speakers:** - [instructor] (1)

#### [What you should know](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/what-you-should-know?u=76281980&t=0)** - [Instructor] As we get started to explore part two of C Sharp Essential Training, there are a couple things that you should know. First, I'm expecting that you've got some knowledge of programming languages in general. Maybe you've used another programming language, or you've tried them out, or you've got some basic programming knowledge. Also, as this is a part two, I'm assuming at you've watched or know the content of C Sharp Essential Training one, and the Learning C Sharp course on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning, which was a prerequisite for that course. All of that's really going to help in this course for you to make sense of what we're talking about and really get the most out of it. In this course, I'm going to use Visual Studio 2022 on [Windows](../../Glossary/Service/Windows.md). I'm going to be using C# 10 and .NET 6. If you're following along, the easiest way to do that is by using this setup. So if you have Visual Studio 2022 Community edition, that should be fine, and you'll get C Sharp 10 and .NET 6 with that. If you want to do something else, for example if you're on a Mac, there are some other options. Visual Studio for Mac 2022 is in preview at the time of this recording, but you should be able to follow along in there for the core C Sharp work that we're doing here. You could also use Visual Studio Code, which has support for C Sharp and the ability to work with the .NET 6 compiler and C Sharp 10. That said, I'd really only recommend this approach if you're very familiar with those tools already,
>
> **[1:34](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/what-you-should-know?u=76281980&t=94)** or if you don't have another option because you are on Mac OS, you don't have the ability to use a Windows machine and the full Visual Studio. But again, I'll be doing things on Windows. It'd be very similar to what you'd do in Visual Studio for Mac, with some slight UI differences. In Visual Studio Code, the C Sharp itself, the editor will be very similar, but there'll be some shortcuts I take and things like that that will be different.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Windows](../../Glossary/Service/Windows.md) (3), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Tools:** visual studio (7)
> **Env Vars:** net (3)
> **Analogies:** for example (1), similar to (1)
> **Prerequisites:** prerequisite (1), setup (1)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [CoderPad tour](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/coderpad-tour?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/coderpad-tour?u=76281980&t=0)** - [Instructor] This course includes automated code challenges that appear when you click on the challenge links in the course's table of contents. Each challenge includes instructions and a couple of code editors you can use to create and test your own solution to the challenge. These challenges are hosted by CoderPad and they appear in the same area of the course page where you watch the course's videos. We recommend using a desktop browser for the best experience with code challenges, but you can use the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning mobile app if you prefer. The code challenge has four areas: instructions in the top left, a code editor for your answer in the top right, another code editor where you can see how your code is used in the bottom right and a console for output in the bottom left. You can use these drag handles to change the amount of space that each of these has as it works for you to be able to expand the areas you need to focus on. You can also get more horizontal space for the code editors by collapsing the course's table of contents on the left. Each challenge has instructions that include a description of the challenge and the challenge's desired results, parameters, or values that'll be passed into your code, and they may have to be of a particular data type. The return value also has to be of a particular type, and you'll also see that noted in the instructions. The constraint section has useful information about the parameters. The constraint section has useful information about the parameters that'll be passed in. The examples show different parameter values
>
> **[1:33](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/coderpad-tour?u=76281980&t=93)** and what result would be returned for each of those test cases. Create your answer in the top right code editor. There may be examples or options in the comments here, generally some guidance that you can use around what you need to do in addition to the instructions. And when you're ready, you can click test my code to see if you've got the correct answer. If your code isn't successful, you can ask for help. I can change these values to true or show expected result and show hints. Now, if I test my code, you see I didn't get the correct answer this time. Time for another try. We see the help down at the bottom. The expected result is 19, and the hint tells us we can either iterate through the array to do our own calculation or use a link method. Regardless of whether your answer is successful, you'll see messages in the console output in the lower left. If anything's too long to fit, you can scroll sideways to see all the text. When you finish each code challenge, return to the course's table of contents, and click the next video to see my solution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Cross-References:** next video (1)
> **UI Navigation:** click on (1)
> **Speakers:** - [instructor] (1)


### 1. Generics

[↑ Back to Table of Contents](#table-of-contents)

#### [Understanding generics](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-generics?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-generics?u=76281980&t=0)** - [Instructor] In C# Essentials part one, we talked a lot about defining types. We defined those types with very specific plans in mind for how they'd be used. To start part two, I want to talk about generics 'cause this is a natural extension of defining types, but it allows 'em to be more flexible. Let's look at an example to help you understand why generics are important and why they were introduced into the C# language. Say I've got this method here called swap, and what I want to be able to do is have two objects or two references or variables and I want to swap them. Pretty simple, nothing too complicated there. So I take the second object, I assign it to a variable called temp, and now I point second at first and I point first at temp. As we're getting started, you may find some problems with this already and hopefully we'll come to that. Let's say I just want to try this with some simple types. I'll say x=5 and y=7 and those are just integers. And then I want to do Console.WriteLine. So I might use my string interpolation and I can say x, and I'll put x there and y and we'll put y there. So we want to put out those values. And in between then I want to call swap. Then I want to just pass not x and 5 but x and y into that method. This seems like it should work. So if x is 5 and y is 7. When we swap these and write it out, it should say x is 7 and y is 5, but it doesn't.
>
> **[1:35](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-generics?u=76281980&t=95)** Now if you watched C# Essentials Part One, you know why. 'Cause these are value types. And so when they go into this method and come back out of this method, they have different scope. We're actually copying values in there. Ah, you're thinking, let's use some reference types. Great. I have a person class that I've defined over here in my library. That library is referenced by my [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) essentials program. And it's just a simple class called Person, first and last name and age and an ID. And of course you can grab these files from the exercise files and follow along. Now I've got some reference types. I should be able to come down and call swap on those. So I'll do Swap(p1,p2) and I want to swap those references. And now I can do a Console.WriteLine. And now I can do something like this or I'll say Person 1 and we'll do P1.FirstName. We'll just leave it at that. So if we swap these, we should then see that first name is going to be Amanda because we're swapping those two around. We'll run that again. Notice again we get person one is Matt. Why is that? Same reason we saw before with the value types. So again, you're thinking, well we'll add ref here 'cause this is going to allow us not to just change the parts
>
> **[3:10](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-generics?u=76281980&t=190)** of the object, but actually change what they point to. And of course we have to go back to the swap, we'll say ref here as well. And now we start running into some problems. We've got red squigglies. And if I go highlight that, it's going to tell me that it can't convert this from a ref of person to a ref of object. And the reason is a ref of object is very vague. That's kind of what we want here. We want this to be something that can point to anything, but that means I could pass in this person, but I could change it in the method to be an array or a date time or any other kind of object. And so the compiler's not going to allow me to do that. So we're a little bit foiled in our goal here. And this isn't actually the most ideal way to do this either. Even with our first example of swapping x and y, the problem was we're taking integers and we're treating them as objects. Now, C# allows you to do that, but it involves what's called boxing and unboxing and value type in state times can be treated as an object. That's a core principle of the language. But because they're treated differently in how the memory is allocated, it's an expensive operation. And this is where generics come in. What we want to be able to do is provide this swap method, allowed to use it with any type, but we don't want to use something like object. So what we can do is specify
>
> **[4:44](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-generics?u=76281980&t=284)** in our method this definition of a generic parameter called T, and that's a type parameter. So we're saying when you call the swap method, you're going to have to provide a type. And now instead of object, we'll put T here. And then inside the method, we don't want to treat it as an object here. Again, we want to set that as T. So let's go back up here to our person and we're just going to put that type, the person type in there and pass those in and write that out. Now for this swap, we could come in and do an int and now we can come in and say ref. And that's actually going to allow us from the compiler's perspective to do that. So we'd expect if we swap the people, is that P1.FirstName should now be Amanda, which was P2's first name, and x should be seven and y should be five. So we should see person one, Amanda, print out. And then we should see x is seven, y is five. And that's in fact what we get. So that one little change, just being able to specify dynamically the type for our method has made it much simpler for us to create a useful utility method or some sort of functionality. And then when we compile, it's the calling code that provides that type. So it's still going to get compiled in as that type. And we do know the type at compile time,
>
> **[6:17](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-generics?u=76281980&t=377)** so it can do all sorts of checks and other things that we would expect in our application. So these generic types as we'll see can be used not only on methods, but on interface definitions or class definitions as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Definitions:** is a  (2)
> **CLI Commands:** find (1)
> **Cross-References:** go back to (1)
> **Exercise Files:** exercise files (1)
> **Prerequisites:** getting started (1)
> **Speakers:** - [instructor] (1)

#### [Working with generics](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-generics?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-generics?u=76281980&t=0)** - [Instructor] When you're working with C sharp and generic types, you're going to either be working with an actual generic type, meaning a class, a record that has type parameters, or a generic method. So a generic method might be something like we've seen. I'm just going to piece in a little bit of code here. So this is just a string that represents some [JSON](../../Skills/Web%20Development/JSON.md), some [JavaScript](../../Skills/Software%20Development/JavaScript.md) object notation. Pretty common format these days for serializing objects and it just indicates that we've got an ID property, first name, last name, and age, which should look familiar. That's what our person class looks like. So now we might do something like VAR PJ, or person from JSON equal system.text.json and we want to use what's called the JSON serializer that's going to allow us to serialize and deserialize objects and you'll notice the first option is a deserialized method that returns an object, but I know that the JSON I have represents a person, be deserialized into a person, it's going to choose the generic method and I can see here then that it has a type parameter, it just says T value and I can also see that the return type is a knowable T value. So in here, I could say I want this to be person and then I'm going to pass it the JSON person text, and I should now have a person object that's been deserialized from that text. We could do something like this.
>
> **[1:34](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-generics?u=76281980&t=94)** We write out to the console, say JSON person, and then we can do PJ and I'll use this because it is a nullable person, so that helps me make sure that I avoid any null references. I'll do first name, then I can come across and say is, and then we could do PJ question mark.age Got an extra semi colon there. So that's a generic method, where you're going to give the type of the method and that's generally going to map onto one or more parameters, or the return type or both. We'll run this quick and we should see that JSON person come out as our output at the beginning and there we see JSON person, Matt is 50 and if we look again, that's what we had in our text. Our JSON, first name was Matt, age was 50. In C Sharp Essentials Part One, we actually worked with a generic type, albeit a little bit indirectly. We worked with nullable value types like this. What that shortcut was really referencing was a nullable of T int. So we can write it like this. That's a generic type. What does that mean? Well, if we look at X now, we can see that there are properties on there called value and it has a return type of int 'cause that's the generic type parameter we provided.
>
> **[3:08](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-generics?u=76281980&t=188)** If we look at [Git](../../Skills/Software%20Development/Git.md) value or default, that's going to return an int. Again, because of the type parameter that we provided. It may be that what it's going to provide is zero because that's the default for an int, or it's going to provide us with that value. Likewise, we take this now and we do a nullable of date time. We'll call it maybe date, and we'll be explicit. We'll set it to null for now. Now if we look at maybe date, the value again is date time. So it's still a nullable, but it's got a type parameter that is going to be used throughout that class, the nullable class, in order to represent return values of parameters of properties and methods. So our value has the date time. Again, if we went to the git value or default, the return type is going to be a date time, but it may be a valid date time, or in our case, it's going to be the default. So let's go ahead and do git value or default. We can write that out and we'll see if we run this, what do we get? We got a build error there. Oh, didn't like my X. We'll call that A, 'cause I already have X down below. There's a JSON person,
>
> **[4:41](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-generics?u=76281980&t=281)** and there's our default value of the date. We said git value or default. Default is January 1st, 0001 at 12:00 AM. So when you're using generic types, you're going to provide one or more of these generic parameters and that's going to influence parameter types, return types in that class or for that method, depending on the scope at which you're using the generic declaration.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (11), [Git](../../Skills/Software%20Development/Git.md) (4), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Env Vars:** json (10), var (1)
> **CLI Commands:** git (4), make (1)
> **Definitions:** is a  (3)
> **File Paths:** system.text.json (1)
> **Ports:** :00 (1)
> **Speakers:** - [instructor] (1)

#### [Defining generic types and methods](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/defining-generic-types-and-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/defining-generic-types-and-methods?u=76281980&t=0)** - [Instructor] We've seen how to use generics. Let's look at how to define them. I'm going to right click on my library here and add a new item. I'm going to choose interface. I'm going to call it I Mapper. This is something I've used recently in some client scenarios where I needed to map multiple types. We're going to make this public instead of internal, and I'm going to define this interface with generic types, so I'm going to say S and T. And what that means is, I've got two different generic type parameters, and then I can define a method inside of this particular interface. So I could say I'm going to return a T. I'm going to call it map, and then the method is going to take an S, and that's going to be the source. So this interface says give me two types, and I've used S and T here for source and target, the source type and the target type, and that's something you'll want to do when you define generics, is come up with good values for those type parameters, and maybe simple letters like this, or for example, you might use T key to indicate that the type is for the key. So now we've got an interface. Let's go create a new class. So we've got our person. We'll go add a new class, and we're going to call that customer. That'll be our customer class, and we're going to map from a customer to a person. So I'm just going to cut this,
>
> **[1:36](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/defining-generic-types-and-methods?u=76281980&t=96)** and we'll just paste the customer in. So it's very similar to person, it has an ID and a first and last name, but it has a create date instead of having an age. So we'll save that. We'll go create another class, and this one's going to be our actual mapper. So I'm going to call this my customer to person mapper We'll put that in there. Again, we're going to make that public, 'cause we're going to want to use it, and I'm going to implement the I mapper, and now for those types, the source is going to be customer and the target is going to be person. And if I go over this, it's going to tell me I'm not implementing that method, so we'll go ahead and implement the interface, and notice the method that gets generated. It's a public method called map that takes in a customer and returns a person. So we could simply come in here, return new person, we'll use the object initialization. We can say the ID equals source.id. That will be the same. We can say the first name equals source.FirstName, last name equals source.LastName, and we have the age, which is an int, and so that's just going to end up being zero, 'cause we don't have a value for it here, but we could go in and put a default value there or provide some other value.
>
> **[3:10](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/defining-generic-types-and-methods?u=76281980&t=190)** So a very simple example, you'll notice I defined a type, in this case, an interface. Could be a class as well, just like that, with two different types, and then I've implemented that here because I'm having a concrete implementation of that interface. We'll go into our program. Let's go ahead and we'll just get rid of all the bits that we've had so far. We'll go back and we'll use our essentials two library. Get that back in, and now we can do a new customer. Again, we can have an ID. We'll say that's seven. First name equals customer, last name equals first,
>
> **[3:59](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/defining-generic-types-and-methods?u=76281980&t=239)** just to be a little confusing, and then the create date equals new date only. We can specify that a number of different ways. We're going to go ahead and do option. That allows us to do year, month, and day. So let's do 2022. Let's say it's January 17th. And now that's our creation. Got my semicolon in the wrong spot there. We've got our customer. Now we can create our mapper. We've got our customer person mapper. We could do mapper.Map, and now we can pass in the source, which is that customer. And what we'll get out, then, is a person. Of course, we could write that out. p.FirstName so we can see what happens there. Notice that at this point, I really don't see any of the generics. I've got a class customer to person mapper. It's got methods on it. It's very strongly typed, as I would want so that I can use these very specific types. It's that interface that's very flexible for me now. I can create another class that's a different kind of mapper, still implements this interface, but the types can be different. So we'll run this quick, make sure that everything builds and we get the expected outcome when we map our customer to a person, and there we go. We've got the first name mapped onto the person, comes across as customer.
>
> **[5:36](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/defining-generic-types-and-methods?u=76281980&t=336)** Now, as we've seen, we've got those types that can have a generic. You also have methods. So we can add a method here, and you can see how we can combine these type parameters. I want to say I've got a public method that's going to return a type, T, called map of T. That's how you generally refer to these generics. So you're going to be able to provide the output type. In addition, you're going to need to provide me the mapper. I'm going to say provide me a mapper, and remember, it was source and target, and the source type is going to be customer, because I'm mapping from within this customer class. It's going to be a set source, and then the target is going to be that type, T. What we've got is a method called map of T that will return an instance of type T as long as you give me an I mapper that has a source of customer and a return type of T. So I'm going to call this mapper. Come down here, and we can simply say return mapper.Map, and we're going to pass in this, the customer. So with that mapper, because it's an I mapper of customer T, is going to be able to return a T. We go back out to our program here. We can go right here where we used our mapper before. Instead of calling the mapper directly, I can use the customer.Map method. I'm going to map to a person, and then I'm going to pass an I mapper of person to customer. So I can pass that in there. And again, we can run this,
>
> **[7:08](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/defining-generic-types-and-methods?u=76281980&t=428)** and we should get the same output we got before, which is customer. So that's an example of a generic method where you specify that type, and I also want to point out that you can pass those type parameters to additional generics as well as use very specific values, like the customer that we did here.

> [!info]- Semantic Content
>
> **CLI Commands:** make (3)
> **Definitions:** is a  (2), is an  (1)
> **Analogies:** for example (1), similar to (1), just like (1)
> **UI Navigation:** click on (1)
> **Prerequisites:** initialization (1)
> **Speakers:** - [instructor] (1)

#### [Generic constraints](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/generic-constraints?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/generic-constraints?u=76281980&t=0)** - [Instructor] I want to look at another concept around generics, which is the notion of constraints. So I'll right click over here, and we'll add a class. I'm going to call this Sorter.cs. So the idea is that I want to create a class that's going to allow me to sort something. Maybe I'll do a sorter of T, and I want to be able to then sort a set of items that are of type T. So I create a method, say public void sort. We've already got the type up there in the class definition, so now I can just say it's an array of T called items, and now I can start implementing my sort. The problem is, I don't really know what T is, and I don't know if I can compare them. For example, I could look at it and say, if I would say items, we'll assume that there's more than one for now, bad practice in our index. And we'll say, well, if that's greater than the other... But the problem is since we don't know what T stands for at this point, we can't really identify a comparison, because we don't know if the type supports the comparison operators. What we need to be able to do is indicate that type T has to follow some rules in order for us to be able to use it in our class. So we follow that class definition
>
> **[1:32](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/generic-constraints?u=76281980&t=92)** with a where, T, and then colon. Now I can say things like I want to make sure it's a class or it's a struct if you want to do that. I can say that it derives from a particular type, or in my case, I want to know that it implements a particular interface, I comparable of T, meaning if you want me to sort a person class, if that's the T you're going to pass in, then that class needs to implement I comparable of person. You can additionally indicate that something supports being created or constructed with an empty constructor, so if you need to create instances within one of your methods, you can stipulate that that's one of the rules that that type has to implement. With that in place, our sort implementation becomes a little easier. 'cause you can now implement a for. I can say for I equals zero, I is less than items.Length. And actually, I'm going to set I equal to one. I want to start with the second item. I'll assume we've got some items here. We should do some error checking or bounds checking. Then I can say if items I dot compare to, we're using that I comparable now, items I minus one, and we could say if that's less than zero, that means we know that this particular item at the second index is less than the item before it, and we want to swap those.
>
> **[3:06](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/generic-constraints?u=76281980&t=186)** We might do something like this. We'll say swap, pass the array, and we'll pass I into I minus one, and we'll swap the positions of those two things. So we could come in here, add a little swap method similar to what we did before. So I've got swap array of T called items and the two indexes. I'm just going to swap those items out. By using this constraint, the compiler knows that whatever T stands for, whatever that type is that gets used, it's going to be able to support this compare to method because it has to have I comparable of T. So we can go back over to our customer now. We can say that it's going to implement I comparable of customer, so that if we want to create now a sorter for customers, we can do that. Implement the interface here for the compare. And again, I'll just paste in a little bit of code so you don't have to watch me type that one. Let's say if the ID of the other item is equal, we'll return zero. That's what the I comparable definition would indicate, zero is equality. If the other ID is greater than this, that means we're smaller, and so we're going to return a negative one. Otherwise, we'll return one. So when we compare customers, we're essentially just comparing the IDs. Whatever your class definition is, whatever it means to compare two things to indicate if one is greater than the other, that's the logic that you'd put in here.
>
> **[4:41](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/generic-constraints?u=76281980&t=281)** So now we have an I comparable and we have our sorter. That's a generic class that requires an I comparable. We can come back out here, we could create another customer. We'll pass that in. We could say, well, that's got a lower ID of three. We'll call that customer two, and we'll just change the last name to second, and then we want to be able to create an array of those items, and we want to pass it to the sorter. So let's create the sorter. So now it's a new sorter of customer. Get that on the screen a little better there. And we need an array of customers, so let's say customers equals new customer array, And we'll just put C and C2 in there. We've got our two instances, and now we can sort that. sorter.Sort. We can pass in customers, and it should sort that array of customers for us, so we can do a for each, for each customer in customers, and then we can write something out here. And then we'll do the customer ID and then we can just do a colon maybe, and we'll do the customer last name. Go ahead and run that, and while it's building up. So now we have our sorter of T,
>
> **[6:15](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/generic-constraints?u=76281980&t=375)** which we've identified as a sorter of customer. We've got array of customers. Customer C has an ID of seven, and C2 has an ID of three, so they're not currently in low to high order. We execute our sorter, we loop through, and now we can see those items have been sorted. Three and seven are now the indexes as they are in order in the array. Not the most efficient sort algorithm. That's not really the focus here. I just want to show you how to use these generic definitions to make more flexible classes and to use the constraints on these generic types to make sure you can operate on the instances of those types to fulfill your class's purpose. And we'll look at generics a bit more throughout the class, especially as we talk about collections up next.

> [!info]- Semantic Content
>
> **CLI Commands:** make (3)
> **Definitions:** stands for (2)
> **Analogies:** for example (1), similar to (1)
> **Tools:** notion (1)
> **Best Practices:** bad practice (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Generic type with constraints](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-generic-type-with-constraints?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-generic-type-with-constraints?u=76281980&t=0)** - [Instructor] In this example, we're supposed to write a method that is a generic method, and it has constraints. So, we're going to change this findLargest. I'm going to say it's a findLargest of T, and then I'm going to ask for a t solver as the second parameter. And what I want to be able to do is simply use that solver, and just like above, I want to say solve numbers. But if we look, we'll say T doesn't have a definition for that. So that's where we come in with a constraint. We say where T isolver. That means where T implements the isolver interface like we've defined up above here. So, we still have some squigglies here, but if we test our code. You can see we've got the right answer here. Now that solver is able to take in our array of integers, and solve the problem for us by finding the largest value. So, it's that constraint that tells this, it's a constraint that tells the compiler whatever comes in for this type T, it's going to implement isolver. That way we know it has the solve method that takes in the array of numbers and returns that int. So, in our test code down below, we're able to call that. So, if we change this, make this 23, just a double check, test our code, we still get the right answer.

> [!info]- Semantic Content
>
> **Code Identifiers:** findlargest (2)
> **CLI Commands:** make (1)
> **Definitions:** is a  (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)


### 2. Working with Collections

[↑ Back to Table of Contents](#table-of-contents)

#### [Understanding collection types in the BCL](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-collection-types-in-the-bcl?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-collection-types-in-the-bcl?u=76281980&t=0)** - [Instructor] Now that we understand generics, I want to look at a particular part of the base class library that you're going to use regularly in your C programming. And we're going to see that it's a good use case or candidate for using generics later on. I'm going to clear out my Program.cshere just a little bit and I'll paste in a little code. So you can see I've got an array of strings with a couple of names in it. And then I'm just doing a Console.WriteLine and I'm writing those names out. So it's going to write out hello, Matt Milner. And that's fine and arrays are a great way to manage collections of items or manage more than one instance of a type. So we've been focusing on creating types, whether they're generic or our interfaces, our classes, structs and records. But now we're going to work with multiple instances and we need a way to do that. And again, arrays are fine, you've probably used them if you've done programming in other languages. Got this concept of zero-based indexing where we can index through each of those in order to get particular items or set their values. However, collections are going to make working with multiple items a lot easier for us. So we're going to start where most people would start and that is we'll create a al variable here. We're going to say new System.Collections. And this is where most people would get in and say, "Great, this is where I go when I want to create collections." ArrayList looks pretty interesting. Let's create a new ArrayList. When we created our string array up top,
>
> **[1:33](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-collection-types-in-the-bcl?u=76281980&t=93)** we gave it a capacity. I can do the same thing with ArrayList and now I've got a class and I could go add items to it. So I could take that and I can now add, and if I wanted to, I could add in say the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) First so we can see that we've got a particular item in there. If you notice, looking at the signature of Add, it takes an object. So we can pass any type in here. We can use an ArrayList to manage any type. Let's take a closer look at ArrayList. I'm going to use F12. We can use right click and go to definition. You'll see that the ArrayList implements three very important interfaces when we're working with collections. We'll start with IEnumerable. So again, you can right click, go to definition, or you can press F12. You can see that the IEnumerable interface has one method on it, GetEnumerator. And that enumerator, if we go there, is going to give us the current item. It's going to allow us to move next or reset, essentially to enumerate through the items in this collection. We go back, go back two, we can see ICollection. And if we go look at that, notice that ICollection also extends or inherits IEnumerable. So if your class, your collection implements ICollection, you can also enumerate through it using that interface and collection simply gives us a notion of a count, how many items are in there, the ability to copy it to an array, and some notion of synchronized access.
>
> **[3:09](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-collection-types-in-the-bcl?u=76281980&t=189)** We'll get into that a bit later. And most importantly, IList. This is the interface that you're going to use most often with your collections, and you'll see that it also extends that ICollection and IEnumerable interface. And we've got add, insert, remove logic here. We've got a contains check to see if something's in there. And we have indexing just like an array where we can go in and access items at a particular index. So this really gives us that list functionality. What you'll notice though is my string array up above had a size of two. And you've probably run into this if you've done any programming where if you go above or below that index, you're going to get an error. With the ArrayList however, I can go in and say I want to add a range. I could say I want to do a new string array. I could come in here and do second, third, and even fourth. So now I'm going to add all these items to that ArrayList. Now, the initial capacity I set was two, but the ArrayList, unlike an array, can actually expand to handle more items. Obviously, we'd want to provide initial capacity that is close to what we're going to use. So all the space gets allocated, but we're not constrained like we are with arrays and we don't have to do that allocation ourself. Now let's use some of those interfaces. So for the ICollection, we could come in and say collection size is, and we can use that al.count.
>
> **[4:43](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-collection-types-in-the-bcl?u=76281980&t=283)** So that's that ICollection. We can also get an indexed item. So we could say indexed item from collection, and we can do two here. So we're saying that's where we're going to get that value. And I'll use a different model for the string formatting here where I just put the index of the parameter in those curly braces and I'll say al two. So we're going to get that item. So using that IList interface, we can index into that collection and get a specific item. And then we could do something like this. We'll say all items in the list and now we can do a foreach. So that foreach, say var item in in our collection is our ArrayList. And now I can just iterate over and pass that out. And this is that IEnumerable. The foreach is going to enumerate over those items in the collection and write them out. So we run this, we should get first our output from the string array. It says hello. Then we see the collection size is four. So we started with that first string. We added a range of three and it expanded from its initial capacity of two to four. We were able to get the indexed item. You can see that zero-based indexing means that's the string third. And then for all items in the list, we iterate through and we get first, second, third, fourth.
>
> **[6:18](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-collection-types-in-the-bcl?u=76281980&t=378)** And so that gives you a general sense of the notion of collections that you can use in C# from that base class library. But I want to caution and we'll talk about this next, that the System.Collections namespace isn't necessarily the best place to start. You've got some hints of this from our earlier conversations around generics and of course, what we've seen here where everything is an object.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Tools:** notion (3)
> **Env Vars:** f12 (2)
> **UI Navigation:** go to (2)
> **CLI Commands:** make (1)
> **Definitions:** is an  (1)
> **Analogies:** just like (1)
> **Warnings:** caution (1)

#### [Choosing the right collection type](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/choosing-the-right-collection-type?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/choosing-the-right-collection-type?u=76281980&t=0)** - [Instructor] The ArrayList is a great improvement over a basic array. It's a little easier to work with. We can do a number of different things 'cause it implements those three key interfaces. But I've added this CollectionSamples file into the library project. So if you're following along in the exercise files, you want to grab them for this particular movie 'cause I've got this new class in here. Or you can type it out. And it's just a static class with some static methods, an example we learned about in C# Essentials 1, and I've got two methods in here. I've got one called Queue, and one called Stack just to show you the different collection types are available for different types of collection management. You can see with a Queue, we can enqueue multiple items, as I've done on line eight and nine. And then I can dequeue items, I can pull them out of there. And I can then write out that value, as I've done line 14 through 18. Notice on line 17, I'm actually checking that the queue has something to dequeue. I'm using that ICollection interface to let me know is there anything else that I should pull out of here. And if not, then I'm going to break. 'Cause if I call dequeue and there's nothing there, I'm going to get an error. The Stack works a little differently. So a Queue is first in, first out. So I've got first item, second item. When I dequeue, I should see first item, second item. The Stack is last in, first out. So I'm going to push things onto the Stack.
>
> **[1:33](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/choosing-the-right-collection-type?u=76281980&t=93)** So I'm going to push first item, and then on top of that I'm going to push second item. And so if I go through these with that same kind of while logic, popping instead of dequeueing, then it's going to pop the top item. So I should see second item, first item. So if we go back to our program out here, we can use that CollectionSamples, I can call Queue, that's going to run that method. Call that again with Stack. And I can even throw a return in here just so we don't see the other bits of code we've got from our previous examples. And so the Queue, again, first in, first out. We should see first item, second item. Whereas the Stack, we see second item, first item. Now if we look here, what's something that stands out to you? If you were listening in the last movie, I talked about the System.Collections namespace. And you can see that when we pop items off the Stack or when we dequeue from the Queue, I'm having to cast that result to a string. And that's because, again, these are based on the notion of objects from the early days of .NET. We have these original .NET collection types in that System.Collections. So we have the Stack and Queue for that last in, first out, or first in, first out, ordered in certain removal type operations. Got our ArrayList for simple collections of objects. Works like an array, but dynamically expands and gives us more capabilities. Got a HashTable for using a hash of a key to look things up.
>
> **[3:07](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/choosing-the-right-collection-type?u=76281980&t=187)** Got DictionaryBase, which allows us to have a variety of different implementations for a dictionary. Again, a key value sort of storage. SortedList and a NameValueCollection, where we can have a name that has one or more values. But as I mentioned, all of these older types, these were around from the original .NET from early 2000s, almost 20 years ago, are obsolete. You can still use them, but instead, you're going to want to use the generic collection types because they're going to give you better performance, they're going to be easier to work with in a typed fashion. So instead of just a Stack, we'd use a Stack of T. Instead of a list in the ArrayList, we use a list of T. It's probably one of the most common collection types that I use. Instead of a HashTable, you got a dictionary. And here we see multiple generic types provided. What type is the key, and what type is the value? Same thing with DictionaryBase or SortedList. We have generic versions of SortedLists, SortedDictionary. Now the NameValueCollection, 'cause that was all strings, is still valid. It's used throughout the framework for things like HTTP cookies, for example, where you have a given name and you can have one or more values associated with that name. As you can see, there are a number of different collection types, each with different semantics for how you use them. Again, the list is one I use most often 'cause I often just need to manage a collection of items, with dictionary probably being the second most used for me. But you may have [Programming Concepts](../../Skills/Software%20Development/Programming%20Concepts.md) in your applications
>
> **[4:43](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/choosing-the-right-collection-type?u=76281980&t=283)** where the Stack and Queue are really relevant. If we come back in here, I'm just going to change this. And instead of System.Collections, I'm going to do generic. And now I've got a Stack, and I'm going to change it to a Stack of string. So it's a Stack of T providing it a string. Now I can get rid of this cast because if I look at pop, it's going to return a string. Now, strings are pretty easy to move back and forth from objects, so it's not quite as exciting. Could easily have been one of our person or our customer classes we've been working with as well that we could push or pop on here. So it shows you just how easy it is to use those generic types. But it gives me a much cleaner interface, gives me better performance because I'm not having to convert or cast between these items. And, of course, we run that and we should get the same output we had before. We could go through and change the Queue to a generic Queue of T, but I'll leave that to you to try on your own.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Programming Concepts](../../Skills/Software%20Development/Programming%20Concepts.md) (1)
> **Env Vars:** net (3), http (1)
> **Cross-References:** go back to (1), in the last (1)
> **Tools:** notion (1)
> **Exercise Files:** exercise files (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Working with collections](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-collections?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-collections?u=76281980&t=0)** - [Instructor] I want to talk about some of the different ways you'll use these Collection classes as you write your programs in C Sharp. If you're following along with the exercise files, you want to grab the ones from the Begin folder for this section, as I've added some things here to our CollectionSamples class. Remember, it's a static class. So I've added a static field called customers of type List of T or List of Customer, and I have a static constructor here on line 42. So I'm initializing that customer list into a new instance and then I'm going to iterate through from one to 11 and I'm just going to add a new customer for each index of i. So the first name will just be whatever i is, we'll write it as a string. Last name will always be customer. And then we'll create a date, slightly dynamic. It'll always be October of 2021, but will have the day be whatever that index is at the time. So now we've got a list of customers, what can we do with that? Well, in this indexing method, it's just a driver for some of these examples. You can see that I can use the indexing just like I would an array on line 60 to grab a particular customer out of that list. So even though it's a generic list instead of an array, I still get that same easy syntax to grab a very specific item based on position. So we'll find that and we'll write out to the console, Hey, I found this customer with this particular ID at this index.
>
> **[1:35](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-collections?u=76281980&t=95)** So it says index two. Next on line 64, I'm going to use something that's a little more advanced, which is the Contains method. So I check, does customers contain a particular customer? And in this case, I'm using that variable customerThree. That's the item I just pulled out of the collection. So we would expect that that does in fact come back as a true. That means our string does contain, will be does and we'll write out customers does contain this customer. So before we move on, let's run those two. I'm just going to run this real quick. We're going to have a little extra content on our screen here that we'll get to, but we can see Found customer 3 at index 2. So that indexing piece that we just saw, and then Customers does contain this customer. Now, as a follow-up, I've got a couple of extra challenges you can try here. What do you think happens if you create a new customer object with the same property values? So it's got the same id, first name, last name, and the create date. If you ask, does the collection contain that item, what's it going to say? If you add the item and check it, what will happen? And then again, on line 67, same thing, you want to create a customer object, but what if customer isn't a class or a reference type as we've got here? What if it's a record type or a struct? What happens there?
>
> **[3:10](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-collections?u=76281980&t=190)** Just a couple of things for you to try out to understand how the collections might be behave differently with different types. You can go back to C Sharp Essentials Part One if you need a refresher on some of the differences between those reference types and value types or records. And finally here, we've got a predicate to find an item in the collection. So this is another great feature of the List beyond just holding those items and being able to either index or walk through them. We saw, we could see if a collection contains something, but we can also find a particular item or find the first item that matches. So we use a customers.Find, and this takes what's referred to as a predicate, which is essentially a method that returns a Boolean value. So you can see down on line 83, I've defined a method called Customers Match. It takes a customer and it returns a Boolean. Now, my instance, I'm simply doing a check to say, is this the customer with ID seven? Does this customer object that you're passing me have an idea of seven? If so, I'll return true, otherwise this would return false. So when we call customers.Find, it's going to look at the customers and it will take the customers then, call this method. As soon as it gets a true, it's going to give us back that object. And so we should be able to look and see that we do get customerSeven. Again, we run that.
>
> **[4:41](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-collections?u=76281980&t=281)** We can see that was our third line there where it says, Found customer 7. Another follow-up option for you here, you can change what this CustomerIsMatch does. Maybe see what happens if you match on last name. All of these items have a last name of customer. Which one's going to get returned, if any of them. So those are a couple of different ways you can work with that List of T and a number of other collections. And you want to find objects, you can also insert, you can remove, you can find the index of items. So instead of finding the actual item, you could find out that a particular item is at index three if you wanted to. And then the last bit here is this notion of iterating over the collection or working with the collection as a whole. And so there's a couple of handy methods on the list. You'll see on line 92, you have the option to reverse the list. Now, this doesn't have anything to do with sorting or looking at the objects in a particular way. It simply takes them in the order they are and it reverses that. So we've got our one through 10. When we reverse, we should now be 10 through one. And then I want to show you two different ways to work with that IEnumerable. You can see on line 94, I can explicitly get the enumerator for that collection. And on line 95, I can call MoveNext on the enumerator. And if that returns true, that means it was able to move to the next item. So I go in there on line 97,
>
> **[6:16](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-collections?u=76281980&t=376)** I can get the current item from the enumerator, which is going to be my customer and I can write that out. So this gives you very explicit control over that and a very manual process to move through. On line 105, you can see the much simpler option. If you just want to iterate through them and do something, you can use the foreach. So we do foreach customer in customers. And then we can write out that value. You can work on that customer object. This is a lot simpler, but essentially is doing the same thing as what you see above. And just for fun, on line 102 and between those two things, I called customers.Sort. Now, in our case, Customer, if you remember, implements IComparable of Customer. I've simplified the implementation of the CompareTo method here since we were just comparing IDs, but this is what's going to get used then if we call Sort. So it's going to sort that based on the IComparable that we've got set up. So we go back out to our program, you can see we're running the indexing here. Go ahead and comment that out and just run the Iterating.
>
> **[7:30](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-collections?u=76281980&t=450)** I'm going to go back to our samples that we reverse the order. And then we use that enumerator to walk through. You can see we do have them in reverse order, 10, 9, 8, all the way down to one. And then, we sort using the IComparable that sorts on ID. So when we use the foreach, they come out one through 10. Hopefully that gives you a sense of how flexible these collections are and some of the great features that have been added on top of the concept of just a basic array. So this notion of being able to work at various points, be able to find items, add items at different points, iterate over the items in those collections makes them a critical piece of most C Sharp applications that you'll write.

> [!info]- Semantic Content
>
> **CLI Commands:** find (10)
> **Code Identifiers:** customerthree (1), customerseven (1)
> **Cross-References:** go back to (2)
> **Tools:** notion (2)
> **Definitions:** is a  (1), essentially is (1)
> **Exercise Files:** exercise files (1)
> **Analogies:** just like (1)
> **Prerequisites:** set up (1)

#### [Working with keyed collections](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-keyed-collections?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-keyed-collections?u=76281980&t=0)** - [Instructor] We're back in our collection samples file here, but I have updated it with some code. So you may want to go to the begin directory for this particular movie, and load that up if you want to follow along. I've added this dictionary method. I'm going to show you how to work with those generic dictionaries. You can see here on line 115, I've created a generic dictionary where the key type is string, and the value type is person. Meaning the key, what we're going to use to uniquely identify this item, and look it up or index it is a string, and what we're going to get back or put into this dictionary as a person. So the dictionary gives us this really great way to store a set of items, but to have that unique key to go, and work with that specific item. So we don't need to know it's position in there, we don't need to go find it. We can directly access it by that key. And then on the next few lines, I'm just adding two new person objects. So I've added a person for me, person for Larry Lawnmower, and I've given each of 'em a key with just the letter that I'm using in my alliterative names here. Now, quick question, is this a good key? No, it's not. It's a terrible key because it's not very unique. M is one of 26 letters in the US alphabet, and that's not going to give me the uniqueness I need. So we're just using this for examples here, but you're going to want that key to be unique to that item. That might mean it's not a string,
>
> **[1:32](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-keyed-collections?u=76281980&t=92)** maybe it's a unique identifier, maybe it's an integer, or another numeric type. You can define your dictionary then with that key type. Now, a couple things we can do now that we have this dictionary with a couple of items in it. One, we can use the ContainsKey method on that dictionary to find out if that item already exists, specifically an item with that key. We aren't really looking at the item itself or the value, we're just doing the key 'cause that's going to be the quicker lookup. That's what everything's going to be indexed on. So we can find out if that collection contains the key, and then we could safely go and access that particular item. So you can see on line 138, I'm going to use the result of that ContainsKey, my keyFound variable. And if that's true, now, I know it's safe to go in, use that key to index into the dictionary, and get out a person and I'll write out the first, and last name. If I don't check that, if I try and index in, and the key doesn't exist, I'm going to get an exception indicating exactly that. Here's a follow-up that you can do if you want to try this out on your own. Line 143, there's a TryGetValue method on the dictionary. It's going to allow you to provide a key, it's going to give you back a Boolean value indicating whether it was able to get the item, and it's going to use an Out parameter. So that's a another good way for you to be able to safely try to get a value based on the key, and already have a true, false value letting you know whether it succeeded.
>
> **[3:06](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-keyed-collections?u=76281980&t=186)** And finally, for the dictionary on line 145, you can see I'm going to do a foreach over my dictionary. Now, in this case, that's going to use the i enumerable, and the enumerator to allow me to walk over that. And because that's a var, we don't necessarily know the type. I like to use var when I'm working with collections and enumerating. So I can mouse over like this, and I can see that the type of that item is a key value pair of type, string and person. That means I'm going to get a class, or an object that has a property called Key, that's a string and a property called Value, that's a person. And I'm going to be able to work with that in my iteration. So now, I can write out the item.Key, which is going to be that string. And then the item.Value. Since it's a person, I know it's going to have a first name, and a last name property on it that I can access. So let's go run this dictionary code. We've got our two people added in there. We're going to check for that M key. And then if we find it, we'll write out that person. And then we're going to iterate through both items in the dictionary. So back on my program CS, you can see I'm just calling collection samples dictionary. For our first line, person with key M is Matt. So it found that person, it was able to validate that that key did exist and then index in and get that. And then we iterated over and we got the key, and value for each of those items. Now, if I try to, say, take this l,
>
> **[4:48](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-keyed-collections?u=76281980&t=288)** and I want to add a new person, we'll call it Leticia, and we'll just change the ID to three. Because I've already used that key, I'm going to run into some problems here. I'm going to start this again, we'll run it, you can see, "An item with the same key has already been added." So dictionaries require that that key be unique. So here's another item for you, we'll put it in here. So I'll follow up, use the TryAdd method to try to add this person, and see what kind of success you have using that as a safe way to try and add something, and get back a Boolean value indicating whether you're able to add it or not. And lastly, I just want to quickly go over the NameValueCollection, because we have seen how we can use the dictionary with a unique key, but I've also mentioned that the NameValueCollection allows you to use the same key for multiple items. So if I go out to my program CS here, we'll uncomment and we'll comment. So we're going to be running the name values real quickly, we'll see we've got Matt and Leticia. Those are the two that I've added there. But let's say we want to bring Larry back in, so we want to items.Add, want to use that same name, a l. But now, the value's going to be Larry.
>
> **[6:21](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/working-with-keyed-collections?u=76281980&t=381)** What's going to happen here? This isn't a dictionary, it's the NameValueCollection. So for each of these items then in all keys, notice on 166, I'm iterating through the keys. And then on 168, I'm actually indexing into the NameValueCollection based on the key. So it's slightly different, we don't have the key value here. We have all the keys and we can access those values. But I'm simply going to iterate the keys and then index. And notice that for that key, l, we get both Leticia and Larry as values for that. So the NameValueCollection's really handy as it provides a different semantic for working with this, where one key or one name is able to have more than one value.

> [!info]- Semantic Content
>
> **CLI Commands:** find (4), go run (1)
> **Definitions:** is a  (2)
> **Code Identifiers:** keyfound (1)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)

#### [Understanding concurrent collections](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-concurrent-collections?u=76281980)

> [!transcript]- Transcript
>
> **[3:43](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-concurrent-collections?u=76281980&t=223)** if you're switching from, say a list of person to a concurrent bag of person. Keep that in mind as well as you choose those collection types.

#### [Solution: Choose the right collection](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-choose-the-right-collection?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-choose-the-right-collection?u=76281980&t=0)** - [Instructor] In this challenge, you're supposed to find the right collection type to give you the behavior where we add a number of items to our collection or our answer class. Then we're going to retrieve an item, and it should be the first item that we added. So I'm going to add a private static queue of product here.
>
> **[0:25](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-choose-the-right-collection?u=76281980&t=25)** I'm going to call this my list equals new queue of product. Now we got to make sure that we come up here to the using statements and we add system.collections. Oops, system.collections.generic so that we have those generic collections, like queue, in scope. So we can add it in. So now I can say, I've got my list, and I can enqueue that product, and then when we go to retrieve, I can come in and use the queue then again in a list and I could just dequeue, like this, and return that, but what's the problem with that? The problem with that is what if the queue is empty? So, I want to say product P equals null. And now we can say if, then we could do list.trydequeue out P. So if we get that, now we can return P, and then we can come down here and say return null. So that way we don't get an exception trying to read off of the queue when there's nothing there. On our example, we're going to add two [products](../../Skills/Software%20Development/Microsoft%20Products.md) in here, the widget and the gizmo, and then we're going to go through and add all those from the array and then call retrieve. Great, we got the product back. It was the the right product. We did a comparison in there to make sure that we've got the right item from the array.
>
> **[2:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-choose-the-right-collection?u=76281980&t=120)** So the queue was the right choice here. If you'd used a stack, you would have a first in last out model, which is not what the goal was.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Products](../../Skills/Software%20Development/Microsoft%20Products.md) (1)
> **CLI Commands:** make (2), find (1)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)


### 3. Exception Handling

[↑ Back to Table of Contents](#table-of-contents)

#### [Basics of try-catch-finally](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-try-catch-finally?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-try-catch-finally?u=76281980&t=0)** - [Instructor] I want to start talking about exception handling, dealing with those cases where something goes wrong. You're following along with the exercise files in chapter three, 01, Begin. You're going to find that LinkedInEssentials2 solution and I've added a few things to it. So if we go over here and look, you'll see over in the library, I've got an Employee class that's a simple record type of employee with an Id, FirstName and LastName. I've got a [JSON](../../Skills/Web%20Development/JSON.md) file in the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md).Essentials2, the main program that just has some [JavaScript](../../Skills/Software%20Development/JavaScript.md) Object Notation, or JSON, of an employee. It's got that Id, FirstName and LastName. And in the Program.cs, I'm going to call out to ExceptionSamples.BasicExceptions, which is a new class, ExceptionSamples that I've added, and a method that wraps up this example. What we're going to do is take that JSON file, try and read it in and deserialize it into an employee. And you've seen bits and pieces of this before. So I've got two different paths, a wrongPath and a rightPath. The rightPath is how we're going to get to that matt.json file under our project. And the wrongPath is just the file name, which would throw an exception. So here on line 13, that's where I'm going to choose to either use the rightPath or the wrongPath. I'm going to use a fileStream that I declare on line 14 and I'm going to write out the current directory. That's just so you can understand why the path I use for the rightPath is the correct path. And then we're going to go into the basics of the try catch.
>
> **[1:36](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-try-catch-finally?u=76281980&t=96)** And you may have seen this in other languages, so I just want to make sure you understand the fundamentals and then we'll get into more specific examples and some of the intricacies in later videos. We wrap the code that might throw an exception in a try block. So I start my try block on line 16 and 17. Everything between that closing bracket on line 23 is what I'm trying. So if an exception happens there, I want to catch it. You can see that I'm opening a file, that can cause problems. I'm deserializing an employee from the file contents or that stream, that could cause an exception. And then I'm writing out that employee to the console, which hopefully won't cause an exception, but certainly could. And here that record type's built-in ability to have a toString that writes out its properties comes in handy as we write that out to the console. Line 24, you see we have a catch. So after a try, we have a catch. We define the type of exception that we want to catch. In this case, we're going to use the base System.Exception and then I use ex as the variable name for that exception so that I can use it inside the catch block. Here I'm writing it out to the console, just writing the message. Often you'd use a logging framework to log this out to a permanent store or some sort of system where you would alert somebody that there was a problem. And then we have the finally block and this is code that's going to run regardless of whether there's an exception. So if we have no exceptions,
>
> **[3:10](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-try-catch-finally?u=76281980&t=190)** we'll do everything in the try block, and then the finally. And we have an exception, we'll do everything we can in the try block until the exception happens. Then we'll execute the catch block and then we'll execute the finally. And so I need to make sure that file that I opened up gets cleaned up. I want to make sure that I close that and I do that by calling dispose on that fileStream. So let's go ahead and run this. We're currently using the rightPath. So you can see on the second line, it says Employee read from file. We get the information from that JSON file and that current directory, you'll notice at the end, in addition to the path of the project is a \bin\Debug\net6.0. That's where our code is currently running and that's why if we just use the file name, it doesn't work. So if we switch this to the wrongPath here, now we should see an exception happen. We should write out the exception, but we're also going to close up that file. So current directory prints, our standard exception is caught, so it could not find the file. You can see why because it is looking in that bin\Debug\net6.0 for matt.json. One other important concept I want to point out here. I'm using this fileStream and I'm managing it, but it's very important that I clean it up. And so I've followed this pattern of making sure in the finally that I call dispose. fileStreams, like lots of classes in .NET, especially those that access external resources,
>
> **[4:44](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-try-catch-finally?u=76281980&t=284)** network connections, [SQL](../../Skills/Data%20Science/SQL.md) database connections, for example, implement something called the IDisposable interface. And that means they have this dispose method to clean up whatever resources they might have. Often that might be something outside of .NET, like a file handle. So one of the things I can do is I can comment this out, I'm going to say using var fileStream and then I'm going to close that up and now I'm going to use curly brackets and I'm going to come down to the end of my try block and I'm going to close that out. What that means is I can actually comment out this cleanup because the using statement understands IDisposable. And what it says is for this block, so from lines 19 to 24, I'm going to have this fileStream, but regardless of how I exit, if there's an exception or if I just fall through the [Control Flow](../../Skills/Software%20Development/Control%20Flow.md), I want to make sure to dispose of whatever the object is that I've declared in this using statement. So that fileStream has to be something that implements IDisposable. So again, we can run this, we'll get our standard exception, we'll get the same kind of cleanup as we would've with the manual process I used before. And we'll set this back to the rightPath so we end with code that works and we're able to read that employee from the JSON file
>
> **[6:20](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-try-catch-finally?u=76281980&t=380)** and write it out.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (7), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [SQL](../../Skills/Data%20Science/SQL.md) (1), [Control Flow](../../Skills/Software%20Development/Control%20Flow.md) (1)
> **Code Identifiers:** rightpath (6), filestream (6), wrongpath (4), tostring (1), filestreams (1)
> **Env Vars:** json (5), net (2), sql (1)
> **CLI Commands:** make (4), find (2)
> **File Paths:** matt.json (2)
> **Exercise Files:** exercise files (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)

#### [Exception filtering by type with catch](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/exception-filtering-by-type-with-catch?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/exception-filtering-by-type-with-catch?u=76281980&t=0)** - [Instructor] We have some basics down. We're catching an exception if it occurs and we're logging it. But as I said, there's a couple different problems we might run into. One is file-based issue, trying to open that file and read it. And the other is with the [JSON](../../Skills/Web%20Development/JSON.md) serialization. So just catching an exception while it will handle all of those, doesn't really tell us what the specific problem is. So one thing you might try and do is come down and catch an IO exception like this. And what we're going to see is that we're going to get a red squiggly telling us there's a problem. And what it's telling us is, these catch blocks are in order, they're going to be applied in order, and that first one is going to match everything. So this guy will never get hit. Let's take it, put it up here, and now the more specific exception comes first. So if it's an IO exception, we'll do this logic. If it's not, but it is a regular exception, then we'll do that. So we can do something like this, we can say we've got a IO exception, and then we'll put that message in there. So we'll do ioex.Message and now we've got an IO exception. Likewise, we can put another one in here and add another catch block for a JSON exception. So we've got that. We can add our curly braces and now we can write out JSON exception. And then we've got our jsoex.message to write that out.
>
> **[1:40](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/exception-filtering-by-type-with-catch?u=76281980&t=100)** So if we change this back to the wrong path you remember, that should give us a problem with the file and that should be an IO exception. So let's give it a try and see what happens. There's our current directory, and sure enough, there's our IO exception, could not find the file and then it gives us the path. Now if we have a problem with the JSON, for example, let's say instead of a string here, I try and put a number and then we go back to our samples and we use the right path. So we're going to have the path to the file, correct, but the file has a problem. Now we get our JSON exception. JSON value could not be converted to a string and it gives us the information about that particular exception. You can provide as many catch statements as you want. Again, you want to go from more specific to less specific. So for example, I could even add a file not found exception, and that is going to be the more specific IO exception. It actually derives from IO exception. So we'll just say file not found in that case. Now to get the file not found, I have to go back and use the wrong path again. Keep switching that back and forth. And now we just see file not found. So we're not getting the generic IO exception, we're getting the specific file not found exception. Again, if we go to the definition for that, you can see it does derive from that IO exception,
>
> **[3:14](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/exception-filtering-by-type-with-catch?u=76281980&t=194)** but it's more specific and therefore matches first. So depending on what you're trying to do, how specific you want to get with your logging or your error handling, you can identify these catch statements for the different kinds or different types of exceptions and handle them differently. One last note here, you'll see that for the file not found exception, I get a little green squiggly and that's because I'm not actually using that variable name, that fnfex. So I can just get rid of that. I can tell it just catch this exception type. I don't need a variable to hold the exception, I still want to run that particular exception handler for the type. So if you're not going to use that variable, if you're not going to do something with the actual exception, you can simply catch on the type and leave that out.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (6)
> **Env Vars:** json (6)
> **Analogies:** for example (2)
> **CLI Commands:** find (1)
> **Cross-References:** go back to (1)
> **UI Navigation:** go to (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Exception filtering with when](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/exception-filtering-with-when?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/exception-filtering-with-when?u=76281980&t=0)** - [Instructor] It's great when you've got a specific exception you can catch that's very focused, like a FileNotFoundException. That's a derived type of an IOException that tells you exactly what went wrong. But sometimes, the exceptions are more general, like a [JSON](../../Skills/Web%20Development/JSON.md) exception. There's a number of reasons that there might be a problem with our JSON. So let me go back up here, make sure we're using the right path 'cause we're going to have a problem with our JSON, not our file location. And you remember, I left this with this 809 for my FirstName. So let's go back here. We've got a breakpoint set in our catch block here. So let's run this in the debugger. You can see we hit a breakpoint, our JSON exception. If I mouse over or look down in the locals window, I can see a message that says, "The JSON value could not be converted." So that tells us we've got a problem there with a cast or a conversion. So I'm going to add a filter here. I'm going to say when. Then I'm going to put some parentheses, jsoex.Message.Contains, I'll say "converted".
>
> **[1:10](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/exception-filtering-with-when?u=76281980&t=70)** Then I'm going to use a string comparison. I'm going to use the OrdinalIgnoreCase. So, basically saying, ignore the case in there. I don't care if the capitalization matches. I'm really just looking for converted, and I'm not going to worry about specific language here. And so now, my catch is very specific. I want a JSON exception where that message has the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) "converted" in it. So I'm really looking for that particular problem that we just saw. So we should still hit that exception. Let's just run it and see. Yeah, we get JSON exception. JSON value could not be converted. That's the exception message. Exactly what we wanted. Now, if I go break my JSON a different way. Let's say I have a string in there that's right. But, as is usually the problem, I leave out a comma. Something simple that always seems to give us big headaches. So what would we expect to see here? Well, I can tell you that the error message we're going to get does not say anything about converting a type. So let's just run it. And notice we get a Standard exception caught. And that means we went into this exception block on line 38. It didn't catch a JSON exception. And you could see the error message there as it says, the quotation mark's invalid after a value. It's looking for some other syntax. So because we filtered with that when clause, that particular kind of JSON exception didn't get handled by this catch block,
>
> **[2:45](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/exception-filtering-with-when?u=76281980&t=165)** but it did still get caught by the exception block. So here's a to-do for you. Add a catch block with a when statement to catch problems with syntax. Missing comma, for example. So that'll give you something to try out for yourself. So you're going to catch a JSON exception, but you're going to need a when block to make sure that you either specifically call out something about that kind of exception, or you make it somehow different from the one block that we have here.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (12), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Env Vars:** json (12)
> **CLI Commands:** make (3)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Throwing and rethrowing exceptions](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/throwing-and-rethrowing-exceptions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/throwing-and-rethrowing-exceptions?u=76281980&t=0)** - [Instructor] We've looked at catching exceptions, but now I want to take a look at throwing exceptions. When you get into a situation in your code where there's a problem, you may need to report that up through the application code as an exception. So I'm going to add a method, here, to our Exception samples class called throw exceptions. Notice it's going to take a knowable bool called shouldThrow. And in this case, on line 59, I'm going to check if shouldThrow has a value. If it doesn't, I want to throw an exception. Now, I could just throw an ArgumentException, but that isn't quite as descriptive and I would have to provide a message and tell you what the problem was. If I throw an ArgumentNullException, I get that more specific example. I'd simply need to provide the name of the parameter or argument to provide enough context for that exception to make sense. Now, if shouldThrow does have a value and that value's true, I'm going to throw an exception just as an example of how you do that. And you'll notice I've called out here that while I'm throwing a new System.Exception, you really shouldn't do that. It's not very descriptive, it's very, very general, and we'll see, next, how to create your own exceptions to be more descriptive. But this will get the job done for now in order to throw an exception from this method. And then finally, if our shouldThrow.Value is false, then we're just going to write out "No exceptions being thrown here." Save that, go back out to our program,
>
> **[1:34](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/throwing-and-rethrowing-exceptions?u=76281980&t=94)** and now I want to add some code here to call that method. So we'll paste that in. So we're going to use our try catch. I'm going to try the throw exceptions, and then we'll catch an exception and write it out. Now, let's start with null here. So we can just pass null. Remember, if it's nullable, we should then see an ArgumentNullException. And since we're catching it, we're going to simply write out all the information about that exception. Before we do that, we're going to comment out our old BasicExceptions, here, and now we'll run that. You can see at the top we get a System.ArgumentNnullException. That's our Console.WriteLine, there, telling us that value cannot be null as the predefined message in that ArgumentNullException, and that it includes the parameter that we passed in, shouldThrow. Notice on line three at the end, there, it's telling us this exception happened, in ExceptionSamples.cs on line 61. And then two lines down we see program.cs:line 17. That's where we caught that exception. Let's keep that in mind, it's going to be important later on. Let's try again. If we pass in true, now we're passing this in and we should be throwing our system exception. So we've caught that, we're writing it out, was told to throw this. Again, we see on line three ExceptionSamples:line 67.
>
> **[3:06](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/throwing-and-rethrowing-exceptions?u=76281980&t=186)** And again, then on line five we see the program.cs:line 17, where we've now caught that particular exception. This's known as the stack trace. It tells us the various pieces of our application where this exception has been handled. So as you get deeper into a program, these stack traces can get pretty detailed and very deep, but you can see where did the exception start and where did it propagate up through the application code to finally get handled? In our case, on line 17 of our program.cs. We've thrown and now caught exceptions. And one of the things you can do when you catch an exception is instead of writing it out I could throw that exception. And there's two ways you can do this. I'm going to start with just the throw keyword and I'm not going to pass at anything. And what that's telling the system to do is rethrow this exception ex as it was, with its state. So the high-level application is going to handle this or catch it. So now you can see on our first line we have Unhandled exception. That's something we didn't catch, we didn't write, but the application domain in which our code is running caught this unhandled exception, it's logging it out. We can see that's our system exception. And again, we see line 67 and we see program.cs:line 17, just like we did before. So we didn't handle it, we didn't write it out,
>
> **[4:39](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/throwing-and-rethrowing-exceptions?u=76281980&t=279)** but all that stack trace information was captured. Now, what you'll sometimes see is this. Somebody will say, "Well, I caught this exception, I'm going to throw this exception again." And let's see the subtle difference when we do that. There's a lot less detail. Still an unhandled exception. We still see the message, "System.Exception: I was told to throw this!" But notice what we get for a stack trace. We simply see program.cs:line 21. What's line 21? That's where we threw that exception. So by including it, we've lost the stack trace. We've kind of started over and said, "Here's the level at which you're going to get the detail," or "Here's the point at which the detail will start." So I generally don't use that. You may have some use cases where you do provide that. I'm going to go ahead and just comment it out so you can see it, there. But generally, if you want to rethread an exception, you would use the throw without that. Now, you can do both. You don't have to just throw or log or handle the exception. We could still log this exception to our logging framework or something else and then throw it, because we want code upstream to be able to deal with it if it impacts that part of the program.

> [!info]- Semantic Content
>
> **Code Identifiers:** shouldthrow (5)
> **CLI Commands:** make (1)
> **Definitions:** known as (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Creating custom exceptions](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/creating-custom-exceptions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/creating-custom-exceptions?u=76281980&t=0)** - [Instructor] If we go back to our exceptions samples here, remember I said not to throw this general exception, we want to be more explicit. We want to provide more information when we throw errors, so I'm going to come out here and I'm going to right click on our library project. I'm going to choose to add a class. In this case, I'll just call it an invalid option exception.
>
> **[0:37](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/creating-custom-exceptions?u=76281980&t=37)** Mark that public and we'll derive from exception to our system exception and then let's give it a constructor. So I'm going to type CTOR and tab twice and we'll say that it takes a string called message and we're simply going to go to the base and pass that message. So our constructor itself is pretty simple. Save that. I'm going to come back to my exception samples and let's take this and now we'll change this to our invalid option exception. I was told to throw this and let's see what happens. We run this. So this is if we get this true passed in. If we come back out to the program, we are passing true. We're going to write that. So we can see, essentials library invalid option exception. I was told to throw this. We've got our right line, which is that first big block, the first six lines up there and then the unhandled exception, 'cause we threw it, it's getting written out again. So that seemed to work. We have the invalid option exception, worked just like the exception, but it's a little bit more explicit. However, you are going to likely run into problems with this definition in certain scenarios when you try to use this exception class.
>
> **[2:10](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/creating-custom-exceptions?u=76281980&t=130)** So we're going to want to do a couple things. We're going to want to mark this as serializable, meaning this particular class can be serialized by the .net framework. If it needs to take the class instance and write it over a network, write it out to a file. So that's one piece. We're also going to want to have some additional constructors. So we'll want a blank constructor and we're going to want to pass down to the base in there and then we want another one that takes both a message and an inner exception. So we'll do our trick there for CTOR. We're going to take our existing bit up here, just to make it simpler, then we're going to add to that and say exception, inner exception and we'll pass that in as well. So now we can take a message as well as an inner exception. So for example, I catch an exception and now I want to throw a new exception, but I want to hold onto the context of that one so I can wrap it together. So I'll show you how we do that when we're done here and the last one to follow these best practices for the serializable and this one we can actually make protected. We've got our invalid option exception and here we want to take a serialization info.
>
> **[3:44](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/creating-custom-exceptions?u=76281980&t=224)** Now that's not coming up right now because we need to add system runtime serialization up at the top. So using statements, I'm going to add that in here. Just take control period to get those options. So we'll call that info. Then I'm going to take a streaming context and just call that context and then we'll go to our base. We'll do info and context.
>
> **[4:19](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/creating-custom-exceptions?u=76281980&t=259)** Oh, looks like I've got one too many parentheses on there, so we can clean this up a little bit. Of course, you can add logic into these constructors if you want to, but this gives us the base construct for this exception. So again, this is for certain scenarios, something you'll need in terms of this bottom one especially for serializable and the serializable attribute and then the others are really to give you more options for creating this exception and by you, I generally mean yourself, but also other developers that are going to use your code. If they need to throw this exception, they can throw it with a message, they can throw it with a message and the inner exception. So we'll save that. We'll come back out to program. Remember when we caught the exception, we were throwing it. One of the other things you can do instead of throw ex is you can throw a new, we could say application exception. So we can add something like this, app exception, but notice that inner exception and here we can pass in, that would be our invalid option exception. So we're creating an exception, but we're providing the context of the exception that we caught. So let's see how that all looks when we run it. There you see at the top, our right line. So again, we still have our essentials two invalid option exception, that really doesn't change. It's when we get to the unhandled exception on line seven, where it says system application exception, app exception,
>
> **[5:56](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/creating-custom-exceptions?u=76281980&t=356)** that's what we we put there. Then inside of that, we can see the inner exception, that invalid option exception, I was told to throw this and all of the details that go along with that. So we've seen two key things there. One is if you're writing your own exception class, yes, you derive from exception, probably an empty constructor, these other two with a string message or message and inner exception and then add that constructor with the serialization info in the streaming context, as well as the serializable attribute in order to make this something that can be serialized and we looked at how you can throw an exception and wrap another exception that you've caught inside of that as the inner exception, so you get more context and again, you don't lose that stack trace, but your exception in this case should provide additional context, or should be providing additional ability for the calling code to catch a very specific exception and in this case, you don't lose all the context of the original problem.

> [!info]- Semantic Content
>
> **CLI Commands:** make (3)
> **UI Navigation:** go to (2), click on (1)
> **Env Vars:** ctor (2)
> **Cross-References:** go back to (1), coming up (1)
> **Analogies:** just like (1), for example (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Multiple exception types](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-multiple-exception-types?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-multiple-exception-types?u=76281980&t=0)** - [Instructor] In this challenge, we're supposed to be able to handle multiple kinds of exceptions that might come back when we call the FindLargest method here. So let's go ahead and start. I'm going to say int result = zero for now, and then I'm going to go ahead and return the result at the end. And now, instead of returning here, I'm going to add a try. I'll say, try to call the solve and get me that number. Instead of returning it, we'll say result equals. Now I want to be able to catch different exceptions. So if I come in here, I can catch an index out of range exception, and then I can handle that particular exception, but I can also add an additional catch statement. And the other type we're supposed to catch is a null reference exception. So I can add that in as well. And we can add as many of these as we want, generally going from more specific to generic. And then, we're supposed to throw this exception inside of another exception. So for the index out of range, I should throw a new application exception and I'm going to give it the text. We'll say error in computing. And then I'm going to pass in that inner exception. That then is going to be part
>
> **[1:34](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-multiple-exception-types?u=76281980&t=94)** of the call stack that goes up. I want to copy that, we'll paste it down here. And instead of an application exception, if we caught the null reference exception, then we're supposed to return an argument null exception. And here we'll pass the NRE up. So we're going to try to call this FindLargest. We know it could throw either of these exceptions, so we're going to catch both of them because we might want to do something different depending on the exception. We might want to retry in some cases. So in our code down here, you can see we've got a series of numbers. I've also got a try catch in the calling code because it's expecting some sort of exception. So we'll try to get the result, we're going to catch the exception, and that's your result that goes into the test. So we got the expected result. It was an argument null exception, or error in computing, and we have the inner exception is that system null reference exception. We can try again 'cause we have random results for this. In this case, system application exception is what we returned because the method threw an index out of range exception. So we've caught both kinds of exceptions. We've handled them differently, although in this case pretty much similarly. We've been able to have logic for dealing with different kinds of problems in our code.

> [!info]- Semantic Content
>
> **Env Vars:** nre (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 4. Programming Threads with async and await

[↑ Back to Table of Contents](#table-of-contents)

#### [Threading basics](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/threading-basics?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/threading-basics?u=76281980&t=0)** - [Instructor] Next, I want to take a look at threading. Now, we're not going to get too deep into threading, it's a fairly advanced concept, but I do want to talk about some basic language constructs that make working with multi-threaded applications or APIs a lot simpler. If we start by looking at some basics around threading. And you may be familiar with this if you've used multi-threaded programming in other languages. The idea is that when we run an application, we have this main application thread and that's going to sequentially execute the commands in our program. But sometimes we need to do something like access a file, and that can be a long operation. If it's a large file and we need to read it or write to it, then we may want to do that on another thread. And what that allows us to do is move that into a separate area of execution where that doesn't block the application itself. Now, this is really useful if you're writing, say, a desktop application, and that main thread happens to be controlling the user interface. You don't want to have some operation that makes it freeze up while you wait for that file to be read. Similarly, network access maybe something else that you would put on a different thread. Anything dealing with IO where you're reading or writing to some sort of stream, or if you've got really intensive computations that might chew up a lot of processor time. So you can see we spin off or create these additional threads. And then when that work is done,
>
> **[1:34](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/threading-basics?u=76281980&t=94)** we need to join back into the main application thread. And all of this can get fairly complicated depending on the programming interface you're using or the programming language. One of the things that I love about C# is how much easier it's made working with these kinds of APIs. And you're going to see that they're very standard in C# 10. If we go back to our example code here, I have added some things. So you'll want to grab the exercise files from the begin folder. And you'll see I've added this item over here called thread samples. So we've got some items in there. I'm going to go back out here before I forget. We'll comment out our exception code from before and then I'll do thread samples, Simple Thread so that when we run this, we'll execute my code here. And I should point out, I've also fixed the Matt dot Jason file. We're going to use this in these examples. And so if you've pulled that across and you manipulated it to cause errors by taking that comma out or changing the value, you'll want to fix that. If we look at the thread samples then, what I've got is a method called Simple Thread. That's the one we're calling from our main program. And I'm simply going to change the color of the console text, write out the thread ID by using thread, current thread, managed thread ID. Just so we can see visually that we're running on different threads. And then I'll reset the color.
>
> **[3:08](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/threading-basics?u=76281980&t=188)** On line 18, I create a new thread and I'm passing it a method called Do File Work. Essentially telling it, here's the chunk of work I want you to go do on this new thread. On line 20, I tell it to start. Go create that thread now physically on the system and do the work in that method I gave you. While that's happening, I'm going to do a console right line here saying there's work happening in the main thread. And then I'm going to call thread join on line 22. So I'm saying I am waiting then for that work that's happening on the thread to complete, and that's going to be where I block. So we'll wait. Now we've done other work in our main thread, now we're ready to pull back in the work from the additional thread that's doing our file work. So what's the file work doing? Well, this should look mostly familiar. We're going out to that [JSON](../../Skills/Web%20Development/JSON.md) file. We're going to read the text and then we're going to deserialize it into an employee and we'll write that out. Notice, on line 33, I'm using a method on the file class called Read All Text. Now our file's very small, that's going to be pretty quick, but if we were reading a large file, this is a potentially blocking operation in that, IO category. We're going to read from that file stream and it may take a while and it may block this thread. So that's why we want to spin this off onto this other thread. So we'll call the Simple Thread method.
>
> **[4:43](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/threading-basics?u=76281980&t=283)** It's going to create a new thread, run that code to read from the file and create our object, and we'll see what the results look like. We have our main thread ID. Its ID is one. Notice then that we have work happening in the main thread. So that's now printed after we started the thread. Then we see the file access thread ID, that's a seven, and we get the output that's happening inside of that thread with the employee's been read from the file. We can make this even more clear. We could come add another one here. Say something like after all done. And we can see that that goes back then to that initial method, that Simple Thread method after the thread has completed. And we can continue executing our main application logic. And that's the basic idea behind Threads, here fairly simple to create a new one, start it, then when I know I want to be done I can join, and do other work after the fact. But it's that join that can get a little complicated and managing what happens after the threat is done. So we'll see some additional ways that the language makes this even easier.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **CLI Commands:** make (2)
> **Definitions:** is a  (2)
> **Env Vars:** json (1)
> **Cross-References:** go back to (1)
> **Exercise Files:** exercise files (1)
> **Speakers:** - [instructor] (1)

#### [Basics of using async and await](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-using-async-and-await?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-using-async-and-await?u=76281980&t=0)** - [Instructor] With those threads, you can get some great benefits of being able to manage those and start up all that asynchronous work. But it is a bit of a struggle sometimes to manage all the threads as you get more and more of them. So there's also always been an asynchronous programming model in .net. And I'm going to write a little pseudo code here just so you can see the old model, which will help you understand why the new model, the task-based programming with a weight and async, is a little bit better. Now in the old model, if we look at, for example, line 34, up there where we said we were going to do a File.ReadAllText, that's something that can take a while and we might want to do that asynchronously. And so the old model, you would do something like File.BeginReadAllText, and then you'd need to provide the path just like we did, so the parameters, but you'd also give a callback. So we might do FinishReadAllText or something like that. You'd be able to name it, and you could have an object or something like that that would state that you were going to then pass into the callback. And then, you'd have your method, so FinishReadAllText down here, that's your callback, and that would then take in probably that state, often had what was called an IAsyncResult that would let you know the result of the operation, and you would then implement that method, and that's where you would do the work.
>
> **[1:34](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-using-async-and-await?u=76281980&t=94)** So here's where you would handle the file contents, for example. This was great in the sense that it gave you that asynchronous model. So I could start this work, please go read all that text, and then I could move on with my program, and then when all of that work was done, I'd get a call back, so I would have this asynchronous model. where the problems came in is that with this model, you often ended up chaining lots of these calls together. So you might open the file and then you might read the file and in fact, you might be reading from a streams, you might be reading bytes over and over and over, and you'd have all these callbacks and all this state that you were passing forward. Now all that again is a setup for what we have now. I'm going to paste in some new methods to replace the ones we had. So you'll see I have, instead of simple thread, a new method on line 41 called SimpleThreadAsync. And if you look at the method signature, you'll see not only is the name different, but I have a new keyword, async, and the return type, instead of void, is task. This is the task-based programming model. And using that task is going to allow me to do a number of different things. I've also added on line 52 a method called DoFileWorkAsync that similarly, has the async keyword and returns a task. If we start back at our SimpleThreadAsync here, you'll notice on line 47, I'm going to call that DoFileWorkAsync,
>
> **[3:07](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-using-async-and-await?u=76281980&t=187)** but now I'm using another keyword, which is await. And what that says is I want you to go start up this work, whatever's happening in DoFileWorkAsync, you're going to give me back a task, that's the return type, and I'm going to await that task to complete. So this is essentially similar to that callback, but here is the callback code. So rather than having to write a whole different method, rather than having to separate that out, chain those things together, we can have our callbacks here. And in fact, then I could await some other work, meaning I can chain those callbacks together in what looks like a very linear fashion, was very easy to read code. Looking at DoFileWorkAsync, again, it has the async keyword, and it returns a task. Notice on line 60, now I'm reading that [JSON](../../Skills/Web%20Development/JSON.md) by doing File.ReadAllTextAsync, and I'm awaiting that operation. So a couple key things here, one is ReadAllTextAsync, just like I said, DoFileWorkAsync. It's a very common pattern to put the async on the end of your method name to indicate that it's asynchronous or returns that task, and I'm also getting back a return value. So I'm awaiting that, but I'm going to put the result into my employee JSON, notice it's a knowable string. That's because if we look at ReadAllTextAsync,
>
> **[4:41](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-using-async-and-await?u=76281980&t=281)** it is an async method that returns a task of string or a task of T, a generic that we learned about. That means it's a task, so I can await it, but it's also going to return me a string. So after I've awaited, I'm going to get that string. And again, this greatly simplifies that model, where now I'm not passing around some generic state object or looking at an IAsyncResult, I'm just getting the string from the operation that I wanted to call. In order to use the await keyword, you have to be inside an asynchronous operation. So that means your operation signature has to have that async keyword. Also, in order to await an operation, it has to return a task. So those are two key things to keep in mind as you look at this. So if we go back out to our program, we can come in here and do our thread samples. We can do SimpleThreadAsync and notice I get a little green underline telling me it's not an error, but I've got some information for you. And if we look, it's going to tell me that that is an asynchronous operation, and you're not awaiting it. That's kind of like saying thread start and then wandering off. We're kicking off some asynchronous work, but we're not waiting for it to finish. And in the case of our program here, the next thing we're going to do is exit, because that's our last line of code. So we absolutely want to put our await there, so that we wait for that operation to complete before we continue on.
>
> **[6:16](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-using-async-and-await?u=76281980&t=376)** I'm going to go ahead and run that. There are certainly scenarios where you might want to run an asynchronous operation and not await it. It's not as common, but if you want to start that work, and you're not worried about waiting for it. You can see both outputs here now from our simple thread and our SimpleThreadAsync, both start on the main thread ID. Notice that in the first instance, the FileAccessThread had an ID of eight, whereas in the second, the FileAccessThread is still one. And I'll go back to the code in a second and show you why, but we're still getting the same output, we're still getting that same work happening in that main thread. Looking back at the code, you can see, there's that work happening in main thread. That was after all the work happened in our DoFileWorkAsync. So we awaited it once it was done, that was our callback essentially. That's where we went in and did some more work after that work finished. If you look down on line 56, that's where, in our DoFileWorkAsync, we're writing out the thread ID, notice that this is different from our first example. In our first example, we created a new thread and started it. That's why we got a new thread ID in that operation. We haven't done that here. We've just invoked an asynchronous operation, and while it was starting these first four lines of code, everything was synchronous. We set the foreground color, we did a write line, we reset the color, we set a string variable. Then, only on line 60,
>
> **[7:49](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/basics-of-using-async-and-await?u=76281980&t=469)** did we actually start some asynchronous work. So at no point did we start up a new thread as we began this work, that's why we have the same thread ID, but there may be a thread that gets fired up in our ReadAllTextAsync behind the scenes for us, and we're awaiting that asynchronous work. If we go back and compare this with our thread start, when we use the await, it's similar to doing something like this. We take the thread, join, cut and paste, we're saying go start that work and then wait for it to finish, and that's essentially what's happening when we do the await, so it's a little bit different. We're not starting up a thread and then being able to do a lot of work while that thread is happening, we're actually awaiting it, but it does give us that very succinct model for handling the callbacks when the work is done. And next, we'll see an option if you want to start up multiple pieces of work and wait for them all to be done.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (2)
> **Analogies:** for example (2), just like (2), similar to (2), kind of like (1)
> **Definitions:** is a  (3), is an  (2)
> **Env Vars:** json (2)
> **Cross-References:** go back to (1)
> **UI Navigation:** open the (1)
> **Warnings:** keep in mind (1)
> **Prerequisites:** setup (1)

#### [Task management](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/task-management?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/task-management?u=76281980&t=0)** - [Instructor] As we've looked at these two models where we specifically create a thread and start and join and then also, when we come down here and used the async and await keywords, you may have asked yourself, how can I get something in between? I like this async await. It's very simple, gives me a very simple model without all those callbacks and I don't have to manage the thread, but I would like to queue up multiple pieces of work. Let's look at how we can use tasks to do that kind of work. We'll take our do file work async here. I'm just going to create a parameter, we'll call it employee name, and then instead of my name, we're just going to use that employee name here. I'll put the dollar sign here to get us the string interpolation. So now we can actually parameterize this and we can say, I want to do this work on a particular file. We'll copy and then paste our file here. Rename that. We'll call it Felicia. Open it up, and then we'll just change the name. So let's call this Felicia Employee, and we'll change the employee number here as well, so we'll say she was employee 327, and we'll save that. Now we should be able to call the do file work async with either Matt or Felicia.
>
> **[1:36](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/task-management?u=76281980&t=96)** So we could come in here, and we've got one with Matt. Then we can copy that, and we can call it with Felicia. Now, the problem is, if we do this await, what's going to happen? We're using that callback model, which means we'll do the one with Matt, then we'll wait here. After that's done, we'll do the work for the Felicia file, and that's not really what we want. In this case, we actually want both of those things to go off and start, and when they're both done, we want to go on. So instead of await here, we can have a task. Remember, that function returns a task. Await is just a nice way for us to say, I know I'm getting back a task. I want to wait for that task. So we're going to do the same thing here. Now we're going to create our task variables, so we've got that information, and now, essentially, we want to be able to await both of those things. So we can say I want to await Task.WhenAll, and then I can pass in those two operations or those two tasks. Now, if you go look at task, we've used when all. You can also use wait all, which is more of a blocking call. We've got the await there. You've got the ability to run them, you've got a variety of different options for managing tasks, but for our purposes, we're just going to focus on this notion
>
> **[3:10](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/task-management?u=76281980&t=190)** that we can get back a task from an async operation rather than awaiting it, and we can then manage those tasks by waiting for all of them. So I've got this code now we're writing out with our simple thread async. We're going to have two different tasks for Matt and Felicia, and we're going to have, after they're done, this console write line work happening in the main thread. So let's move this up here and see what happens.
>
> **[3:45](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/task-management?u=76281980&t=225)** We'll go ahead and run. So you can see we've got, in our second part there, that second yellow line, the main thread ID, file access thread one, and then notice we have employee read from file employee, and that's the Matt file, and then the next line is work happening in the main thread. So that shows us that these tasks are off and running, and they're happening asynchronously. So we're able to start those. They started running. We're still back in our main thread here doing our console write line, and then we wait for all of them, and we could then go on and do other code that applies once they're done. We've achieved our goal. We've created two different tasks, we've started them both, and we've been able to wait for both to finish and in between while they're running, we have other code that can be executing on our main thread. As you look at the output here, you may be wondering, why is it in the first example, when we created the thread and did start and join, we have a thread ID of one, and then in the file access, we had a thread ID seven, but if I look down to the second part where we use the async, everything says thread ID one? Well, that's the difference of how deep we're going and how much we're managing this. Note when we created a thread, we literally created that thread and everything in the do file work is running on that thread. That's why we see that different thread ID,
>
> **[5:18](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/task-management?u=76281980&t=318)** 'cause we very specifically created it. If you look here, remember, we're awaiting a method. We're awaiting that do file work async, and that starts processing, and while it does, it's still on the main thread. So our file access thread here, we haven't really created a thread for this to work on. It's when we get to line 73 and we do an async operation that under the covers, another thread will be created if needed, and we're not writing that out, so we don't see that that thread is being created in the background. Using tasks, then, gives you a little more control and allows you to have multiple pieces of work that you can then start and wait on in whatever fashion makes sense to you, if it's one of them completing, if it's all of them completing, before you move on on that main thread.

> [!info]- Semantic Content
>
> **Tools:** notion (1)
> **Speakers:** - [instructor] (1)

#### [Handling errors when using await](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/handling-errors-when-using-await?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/handling-errors-when-using-await?u=76281980&t=0)** - [Instructor] We've now seen two different ways to use the task model to do asynchronous work. If we want to use the await, we can do that inside an async method. So line 48, our SimpleThreadAsync is an async method. That means we can await things in there. Or the model we just used where we can manage the tasks ourselves and we can use the task then to wait for all of them to complete or for any of them to complete, for example. We also talked about exceptions earlier. And so I want to talk about those two together because when we move to this model where we manage the tasks, we can get different kinds of exceptions. So I'm going to change down here from awaiting this to using the WaitAll and then we're going to change a couple things here to cause some errors. So I'm going to change the name here and that means we're going to not find the right file. And then I'm going to come in and I'm going to remove that comma. So now that [JSON](../../Skills/Web%20Development/JSON.md) file's invalid, so the code on line 54 should fail because the JSON exception's going to happen and the code on line 55's going to fail because we're not going to find that file. But because these are tasks and they're asynchronous, they're not going to fail right away. It's really this WaitAll on line 59 that's going to cause problems. So let's do a try and we'll wrap the whole thing in the try and then we're going to catch, and here I'm going to catch an AggregateException
>
> **[1:33](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/handling-errors-when-using-await?u=76281980&t=93)** and this is something that the task-based asynchronous model uses to capture all the exceptions that happen inside that asynchronous work we do. So we'll go ahead and write out the message just for now, but we're also going to put a break point here so we can see this exception. And the last thing I'm going to do is go out to our program and just comment out our simple thread. So we're only running this asynchronous one and we can see it in isolation. So again, we've got two tasks. Each should fail for different reasons and we're doing a WaitAll here to wait for all of them to complete. So we'll debug that with F5 or by clicking here. You can see now we're in that aggregate exception. And if I go into that, the message is going to be a number of things, one or more errors occurred. But what's great about the aggregate exception is I can drill into it. I have inner exceptions and you can see that I have the JSON exception. I go to the inner exceptions here. I've got a collection now of the actual exceptions with each of their stack traces. So now I can see here this one is my JSON exception. If I go down to the other one, I'm going to see that's the could not find file in there and we'll exit out of that. So we wrote that exception out. Now one of the things you can do with this aggregate exception is you can handle particular exceptions. So if we look at the aggregate exception, we have,
>
> **[3:08](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/handling-errors-when-using-await?u=76281980&t=188)** of course those inner exceptions, we have the ability to flatten out all of these. So if you have a number of exceptions or multiple aggregate exceptions, you can flatten them out. But we also have this Handle method and that takes a method essentially, we're going to talk about delegates in the next chapter, but for now it takes something that's going to take in an exception and return a Boolean. So well let's name this inner, that makes more sense, and we're going to pass that in. And so I'm just going to say inner is JsonException. Now notice I don't need to put a return there 'cause it's just a single statement. What that means is if the exception, the inner exception here, is a JSON exception, I'm returning true, that means I've handled it, but it also means I haven't handled the file-based exception. So let's go ahead and run this now. We still have the two different exceptions. You're going to see now when it comes out here, the aggregate exception got rethrown. So I go into the details. There's my exception again, the inner exceptions. Now the count is one and if we look we can see that is the could not find file exception. That's our IO exception. So we've handled the JSON exception, but since we didn't handle the other one, it continued to propagate up through the stack and it's now getting caught essentially here
>
> **[4:42](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/handling-errors-when-using-await?u=76281980&t=282)** at the program level. Just keep continuing and we'll get back out to our code and we'll see that gets thrown up there. When we use the await pattern, like we did before with the await and async, we don't worry about this as much because .NET handles for us the translation or the mapping of that exception onto the actual exception type. So if I were to await the DoFileWorkAsync for these two, then I would see that the actual exceptions are what I'm going to try and catch in my try catch. And in fact, that's a good thing for you to try. So we'll do a follow up, switch back to await the two method calls and catch the appropriate exceptions. So you can give that a try. See what those exception types are. If you await the DoFileWorkAsync on line 56 and 57 and you comment out the task WaitAll on 61, you're going to see slightly different behavior. But that aggregate exception is really helpful when you're managing those tasks and you're waiting for multiple things and you have that Handle operation so that you can say some of these are okay, I've handled them, I've logged them, I've done whatever I need to do. And if you've handled all of them, great, that's a regular catch. If you don't handle all of them, if you return false for some reason, then you're going to get that exception propagated up.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (6)
> **Env Vars:** json (6), net (1)
> **CLI Commands:** find (4)
> **Definitions:** is an  (1), is a  (1)
> **Cross-References:** in the next (1)
> **UI Navigation:** go to (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Task management](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-task-management?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-task-management?u=76281980&t=0)** - [Instructor] In this challenge, we need to use tasks to make some asynchronous calls. We see on lines 11 through 17, or 18, that our Solver class has these asynchronous MinAsync and MaxAsync operations that take an array of numbers and return a Task<int>. So these are asynchronous functions. Maybe we're calling out to something over the network to actually do this highly complex work, and so in our solution, we then need to be able to manage those two tasks. So I want to say I've got a Task<int>, 'cause that's the return type, so I'm going to say tMin =, and now I'll do solver.MinAsync, pass in the numbers. Likewise, I've got a Task<int> tMax = solver.MaxAsync, pass in the numbers. So I have a reference to those tasks that now are off and running, so now I can choose to Task.WaitAll, and I can pass in tMin and tMax. And when those are done, I now have the results in the Task that I can return, so I've got min and max there, so I need to say min = tMin.Result, and max = tMax.result. And now we'll return that two-pole with the min and max. Keep in mind, we'd want to put some exception handling around this, doing these asynchronous tasks and things kind of get lost if we don't do that exception handling,
>
> **[1:35](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-task-management?u=76281980&t=95)** but this is the simplest example to go out and execute those. So if we test our code, it should get five and 19, and that is in fact what we get. So maybe we make 19 a negative 19, try it again. We'll see that now negative 19 is the min and 17 becomes the max. So because our function is not asynchronous, line 29, it's static, it's not asynchronous, can't use the Await keywords here, so we've used the task to get back the reference to those things and then we can wait for them to complete before we continue on.

> [!info]- Semantic Content
>
> **Code Identifiers:** tmin (3), tmax (3)
> **CLI Commands:** make (2)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)


### 5. Delegates and Events

[↑ Back to Table of Contents](#table-of-contents)

#### [Understanding delegates](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-delegates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-delegates?u=76281980&t=0)** - [Instructor] I'm going to go ahead and clean up our environment here a little bit. Just leave our Program.cs. And we're going to comment this out, because now, we're going to start talking about delegates and events and expressions, all kind of fit together. We start with this notion that I have a method, so I have this method called WriteHello returns void, takes in a string and then it's going to do something. A delegate, in simplest terms, is a pointer to a method or function. So I can declare Delegate, del, equals WriteHello. Now, I actually don't want those parentheses that were put on there automatically for me, because I'm pointing to that method. I'm not invoking that method right there. Now, I have this delegate del variable that can invoke that method because it points to it. So I could do something like this. I could say dynamic invoke, and you'll notice the signature is that it takes in an array of objects and they can all be null and it returns a nullable object. We're just going to call that and you'll notice because WriteHello takes a string, we should pass something in there. So I'm just going to pass in my name for right now. We've got a method, we've simply declared a pointer to it, and then we've said, I want to invoke that. So I'm going to Ctrl + F5 to run without debugging. You can see we ran our WriteHello method to write out hello Matt.
>
> **[1:35](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-delegates?u=76281980&t=95)** I'm going to go ahead and go out to my library project here and I'm going to add a class, call it DelegateSamples, and we'll make it public and static like the other samples. And now, what I want to do is the other side of this, I want to be able to declare a delegate. I can actually have a type with a signature, so I can do public delegate. The return type is void. I'm going to give it a name. Let's just call it Del for now, and what is the signature? Well, could say name, could say input. We can call that whatever we want. Now, notice there's no implementation here. This is just a declaration that says a delegate called Del takes in a string and returns void. Well, now I can use that in some work or some function. So I could say public static void PassMeWork. And what I want then is a Del as the parameter and we'll call it work. What is work? Work now is a delegate. So a pointer to a function that takes a string and returns void. So I can call work. It's got the signature, and I can say delegates. That means I can now pass to this function my own function. I can use it as a callback or I can use it as a completion.
>
> **[3:11](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-delegates?u=76281980&t=191)** There are a number of different scenarios where you want to be able to pass a method or a method implementation into another method, so that it can be invoked. So now if we go back to our program, we're going to use this DelegateSamples and call the PassMeWork. DelegateSamples, PassMeWork, and we can pass in WriteHello, just like that. So what's going to happen? Well, our WriteHello is a method that takes a string and it writes hello, whatever that string is. Our DelegateSamples over here is going to get passed that. It's going to invoke it with the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) delegates. So if we run, we should get our first one, which is hello matt. And then we get hello delegates, because we've invoked this WriteHello method and passed it delegates. This allows some great flexibility in being able to define and pass around these functions. And if you're writing a library or a class, you may need to do some work, but you may also need to offload or delegate some of that work back to the caller, so that you can call back to the caller and have it apply some logic or do some work in the scope of this particular operation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Definitions:** is a  (4)
> **CLI Commands:** make (1)
> **Cross-References:** go back to (1)
> **Tools:** notion (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Understanding events](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-events?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-events?u=76281980&t=0)** - [Instructor] Another case where we want to use delegates is when we want to raise events. And this is what's often referred to as a multicast delegate, meaning I'm going to be able to invoke a method once in my code, but it may then trigger or invoke many different delegates. So, I'm going to come into my class and declare this event. So, we're in a static class, so it needs to be static. We use the keyword event, then I need the delegate. What is the signature for this event? When I go and raise this event, or I call all these delegates, what is that signature? And here, I've used my delegate that I've defined with a string input. Then I need a name for my event. So, we'll just keep it simple here, something happened. Now we have an event, what are we going to do with it? Well, we can invoke it or raise it. So, I'm going to just paste this bit in here, got a method, do something. I'm going to WriteLine, I'm about to do something, so we'll get some status there. And then very importantly, I'm going to make sure that event isn't null. That means that some class has registered for this event. It said I want to have my delegate invoked when this event happens, and then I simply invoke that delegate. So, I use the event name. The signature is going to be whatever the delegate was that I provided as the type for that. So, you can see here it's my Del that takes a string. This gives us another way to let external classes
>
> **[1:35](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-events?u=76281980&t=95)** or code get callbacks or get delegates invoked, but in this case, when I call something happened, it may be one, five, 10 different classes whose delegates will get invoked. So, if we come back here, we'll see we need to go to our delegate samples. Something happened, notice it's got a little lightning bolt there means it's an event. And so, I can choose that and then I can plus equal, saying I want to add a delegate to that. And in my case it's my WriteHello that I've passed in. I'm saying here's my delegate. I'm adding it to any other delegates that have been created for this. So, when something happens, I want you to call that. And then, if we use our delegate samples again, we can DoSomething. Remember that's the method that's actually going to invoke that event or those delegates. Then, we'll go ahead and run that. So, we've got our hello Matt and our hello delegates from before and now we've got the, I'm about to do something. Remember line 41 we called DoSomething. That's the first line of code in there. And then, we raised the event. We invoked our delegate and so it says hello 'cause that's what our delegate does. And then, the string that was passed to it, I did something. Again, we go back here, there's our WriteLine and then there is a listener. And now, we say something happened with what we want
>
> **[3:09](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-events?u=76281980&t=189)** that delegate to receive as a string input. Close that. Another example we can use, and I want to show you another thing about events. I used a very simple delegate there, but if we look at the console, we've been using it to WriteLine where you can see a cancel key press here. So, we can say I want to plus equal that and we'll say something like OnCancel without the parameters. That means we need a method called OnCancel that matches that delegate. And the delegate for that is a console cancel event handler. So, let's go down here and we'll add a static void OnCancel, and a common pattern for event handlers is to have an object, that's the sender. And then, event arguments, console cancel event arguments passed in as well. So, the sender can be null, can also be the class with the object that sent it if that's important to you, but the ARGs are often typed in order to give you control over that event or have you [react](../../Skills/Web%20Development/React.js.md) to that event in a certain way. So, for a cancel event, notice in the ARGs we have a cancel property that's a Boolean. So, I can actually indicate if I want to allow this to cancel or if I want it to continue. So, we can say cancel equal to true like that.
>
> **[4:48](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-events?u=76281980&t=288)** So, I've registered the cancel. We're going to have all this other stuff happening. What I'm going to do is a for loop and we'll keep zero and we'll go out to say a thousand, 10,000. And we'll just have that print out that number. This is just so that something is happening here. And then, I should probably tell us to hit control plus C to cancel. So, let's go ahead and put a break point in here so we can see it and let's debug that, but we're now setting up an event handler for the cancel. We're going to make sure our code is going to do something for a while, so it's cancelable. And then when we cancel, we should get this event firing our delegate. Control C, there it is. We've got our cancel. Notice cancel is set to faults. By default, we're going to set it to true and I'll hit F5 to continue. And because I said true, it's actually going to continue through here, hit control C again. I'm back to that. I'll do F10 to step, come down in the immediate window and we'll say args.cancel equals false. So, we can see that's actually false now. Now if I continue, you'll notice it's stopped. It's actually canceled.
>
> **[6:21](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-events?u=76281980&t=381)** We're on our read line and then it completes. It's a very common pattern for events to have the delegate be an event handler, which is essentially a delegate, but it follows this signature where you have the object sender and then some kind of event ARGs class or derived class that provides information about the event. One last thing I'll mention here is that for events you should clean up those handlers. So, for example, when I came in here, this was my callback for that event. If I'm done listening for that event, I just needed to know about it once, in that case, then I would want to say delegate samples, and I've got that, SomethingHappened. Now I'm going to do minus equals, WriteHello I'm removing that one handler, mine, from the list, so it doesn't left out there dangling. I don't now have a pointer back to this method. So, it's a good idea when you're done with those events for you to clean that up. I'll leave it to you to do the same thing down here with the on cancel. Thinking about what's the scenario when you would want to remove the event handler or delegate. So, that might be testing that ARGs cancel. That might be depending on what you're setting it to. So, in our case we're setting it to true.
>
> **[7:55](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-events?u=76281980&t=475)** We probably don't want to cancel the event handler because we might come back in here. We didn't actually cancel the process.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [React](../../Skills/Web%20Development/React.js.md) (1)
> **Definitions:** is a  (3), means that (1)
> **CLI Commands:** make (2)
> **Env Vars:** f10 (1)
> **UI Navigation:** go to (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Common delegates with Action<T> and Func<T>](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/3055829?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/3055829?u=76281980&t=0)** - I've gone ahead and cleaned up our program CS a little bit here. I had a lot of stuff there and it was getting a little out of sorts. I'm going to go ahead and comment all this out right now. So control + E + C and comment that all out just so we're not running everything. Then I'm going to go into my delegate samples. If we look at this delegate we declared, where we take a string input and return void, you might have thought to yourself, "Gosh, why do we have to define that?" It's a method signature, it takes an input and we have generics. We learned about generics earlier. Let's do something better. Instead of having to always define delegates that are going to essentially be method signatures with typed parameters, it'd be great if we had something like this. Action of T or all the way from T one to T 16. You can have up to 16 generic parameters is a built-in delegate or method that executes has one to 16 type parameters or actually zero to 16 type parameters and is going to not return anything. So simply by changing that to action of string my PassMeWork should work now. I can actually get rid of my delegate definition. Now that's going to mess up my event. Can I change that to action of string? Sure. That's a delegate. That's all I need there. So we're back to my PassMeWork.
>
> **[1:34](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/3055829?u=76281980&t=94)** Let's go back out here, go down to the bottom and we'll call it again, delegate samples, PassMeWork. What do we need to pass? We need to pass that method again so we could just pass our WriteHello. Going to have to go up and uncomment that, but it still works the exact same way. So control + E U uncomment that I'm going to go ahead and get rid of that event unregistration, 'cause we're not using the events right now and let's change this to say actions in action and whatever that game is. Again, the functionality is the same, but actions are really going to help us reduce the need to declare our own delegates. So there we go, actions and action, delegates because when we invoked PassMeWork, it invoked our delegate passing in delegates. Actions then can have up to those 16 input parameters but they don't return anything. Whereas we might want to have a delegate that does return something. So we can do a static void. We'll say pass me logic this time and now I want a func of T. So let's say I'm going to pass in a string and it's going to return an int. We'll call that worker. Now I can invoke that worker. So I can say int count = worker
>
> **[3:13](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/3055829?u=76281980&t=193)** invoke notice it takes a string and it's going to return an int. So I'm just going to pass in "Hello World" here. So some function that is going to be passed to me is going to give me a count based on that string. I don't really know what the logic is. I don't know if it's all the characters, if it's non-white space characters, I should know that. But for now, all I know is this method takes a string and returns an int. It might tell me how many times L happens in there. Some we can write out. Function returned and then we'll put in the count. So what's missing now is a method that takes a string and returns an int. We've got a parameter of that type. We are using it in here, but we actually need to invoke and pass something in. So let's go back out here. So I have a static int calculate length. It'll take a string input and we're going to return the length of input. Now if input is null there, we could get an exception, but we'll return that length back out. And that means that we can now do delegate samples PassMeLogic and it wants that function. So we can say calculate length, and pass that in.
>
> **[4:50](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/3055829?u=76281980&t=290)** So now when we pass it work, we're passing it in action. A method that takes parameters but doesn't have a return value. And when we pass logic, we're passing in a function that does have a return value. So you can see when we run that action, it again says actions in action, when we invoke and passed the function, it counted up the letters, and so the function returned 11. Inside of our method here we were able to call back out to the caller or invoke the function past and then use the results of that inside of our application logic. So action and function are really just helpers for delegates so that you don't have to go out and define them like we did on line 13 over and over again with different names and different names of parameters. They're all essentially the same, methods with typed parameters. So we get generic delegates to simplify that.

> [!info]- Semantic Content
>
> **Definitions:** is a  (2), means that (1)
> **Speakers:** - i (1)

#### [Understanding lambda expressions](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-lambda-expressions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-lambda-expressions?u=76281980&t=0)** - [Instructor] Action and Func help us not have to write the delegate declarations. By the same token, Lambda expressions are going to help us not have to write methods. So for those cases where, really, the implementation of that delegate is very focused, we don't need a standalone method for it, we want to be able to have a way to write a method without having to fully declare it or write that logic. And these expressions come in two [Forms](../../Skills/Web%20Development/Forms.md). So I'm just going to call this var T, and have a parameter list. So string S. I'm going to use this expression to point to the Lambda. So now I've defined the parameters on the left, now the implementation comes on the right. A simple expression would look like this, where I would now, write console right line, and pass the S. I'm not returning anything, I'm not doing anything there. I'm simply writing out I've got a statement or an expression there, and that means that I should be able to invoke that by doing something like this, hello world, because that expression is a method. Now, another way we can write those expressions is to have the full statement there. So can use this, I can say I'm taking in a string S, and then I'm going to use my Lambda expression symbol here.
>
> **[1:33](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-lambda-expressions?u=76281980&t=93)** And then I can use curly braces and I can then have multiple statements in here. So I could write out the S. I could also then return S.length. So I have a full set of statements there. This is essentially a method declaration, but I've simplified it in this particular case. Of note, if we were going to use this expression model, or we just have the single expression, let's say I'm going to have this copy paste, I'm going to call this T two, and instead of writing it out, what I want to do is return the length like I did below because it's an expression, I don't actually need to use the return keyword. I simply write S.length. And that's now understood to be a value that's returned, that's providing a value. The expression evaluates to some value, and that then is what would get returned to T two. Those are the key things to understand about basics of the Lambda expressions. So on line 70 and 71, you can see we have our parameter list. In this case, we're defining the type, string. Then we've got the operator that indicates that we're going to have an expression. And in both these cases, line 70 and 71, we simply have an expression. First one doesn't return anything, so it's going to simply write that out. The second one does evaluate to a value, so this is a case where we're passing in a string
>
> **[3:05](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understanding-lambda-expressions?u=76281980&t=185)** and we're going to return that length and down on line 75 through 77, you can see the statement based where maybe when we have this method, an anonymous method, essentially with our Lambda expression, we don't just have one single expression to evaluate. We've got multiple things to do. We can use the curly braces to encapsulate everything to the right of the operator and make that essentially a method body. And again, there we can use the return if we need to provide a value back when those operations are done.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Forms](../../Skills/Web%20Development/Forms.md) (1)
> **Definitions:** is a  (2), means that (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Expressions as delegates](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/expressions-as-delegates?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/expressions-as-delegates?u=76281980&t=0)** - [Instructor] Since Lambda expressions are essentially anonymous methods, we can use them when we talk about delegates. So we think about our PassMeWork here where we passed WriteHello, what does WriteHello do? Well, it takes a string and it does something. So that means we could, instead of passing a method there, pass an expression. We could say, "hello expression" and then we could pass in whatever that s is that comes in, and just drop this down onto another line. Got some extra punctuation at the end there. But not that much. So we have PassMeWork, and instead of passing a pointer to a method, instead of passing that WriteHello, I can just pass an expression. It says, here's your delegate. It matches the signature because it takes a string and it does something in there. Notice that I don't have to say string + s in parentheses, because PassMeWork has already defined that signature. If we go back and look at that method, you'll remember that it takes an Action<string>, so it's looking for a delegate that takes a string or a method that takes a string. We've provided one here, and we don't have to then also specify over here the string s. That's already implied by that signature that's provided. Likewise, for the CalculateLength, if you think about it,
>
> **[1:36](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/expressions-as-delegates?u=76281980&t=96)** we could just take this expression we had down here, copy that, come up and paste it here. Again, we have the string, but we don't have to put that there, we can get rid of it, because we know that this PassMeLogic is expecting that. Get rid of that one extra semicolon there, and now, when we've called PassMeLogic, again, we've given it an expression. We didn't have to have a separate method defined for that. We can define the method in line here using the expressions. We can get rid of CalculateLength, just comment that out. We'll leave these other ones down here as well. We're going to write those out. When we run this, we should see that PassMeWork is going to run and call "hello expressions" with the s, and PassMeLogic then is going to write out whatever string gets passed to it, and it's going to return the length, which remember, we use over here to get a count. So on line 23, we're going to be able to see that count come out because we've invoked that expression from this method. So we've got our hello expression delegates, that's the PassMeWork getting invoked there, calling back into our Lambda expression now with the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) delegates. Then we have our hello World,
>
> **[3:10](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/expressions-as-delegates?u=76281980&t=190)** that's what was called or passed into that function, when we call worker here, we're calling that expression we defined, and we first do a WriteLine, and then the function says what it returned here on line 23.
>
> **[3:31](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/expressions-as-delegates?u=76281980&t=211)** So we get all of that logic simply embedded, those anonymous methods being able to be passed as delegates because they match the signature. Now if I did something like this, well now I no longer match the signature. Now I have a method that takes two parameters, but PassMeLogic is expecting a method that takes one parameter. So our expression is going to have to match that signature. And if I look here, PassMeLogic, I can see that it takes a Func of string int, and so I know I need to pass a string, and the second one is going to be the return type, because it's not an Action, it's a Func. We've seen this in use. If we go back and look at our thread samples, you remember when we did our AggregateException on line 65 here, that AggregateException had a method called Handle, and it takes a function, or a Func, that takes in an exception and returns a bool. So this was a Lambda expression that we passed in. It takes in an exception, we named it inner, and then on the right side of the operator, we're able to use that inner, which is of type exception, in our logic. And of course, this Lambda expression just has the expression, simply inner is JsonException. That results in a true or false value that gets returned. We could have also done a statement-based expression,
>
> **[5:07](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/expressions-as-delegates?u=76281980&t=307)** we could have done a number of things, like write out the message from the inner one, then we could come down and return inner is JsonException, 'cause now we are inside that statement and we do need the return statement. We didn't go over it while we were here 'cause we hadn't touched on it yet, but I wanted to loop back here and show you this in use with a part of the framework so you can see how you can use those Lambda expressions or those anonymous methods. I didn't have to go define a whole method here that matched the signature and figure all that out, because this logic is really only going to happen in this catch block. I don't need a whole separate method for it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Speakers:** - [instructor] (1)

#### [Other uses for expressions](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/other-uses-for-expressions?u=76281980)

> [!transcript]- Transcript
>
> **[0:45](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/other-uses-for-expressions?u=76281980&t=45)** and that now is a MethodBody. So, I've provided an expression instead of the normal method for defining a method in order to provide that implementation. Likewise, we can use that on properties. So, maybe I've got a private string fName that we'll use for our first name. So, if I come down here for my get, for example, I can just use the lambda operator. I know that this is going to return a string, so I can just put fName. That's what that's going to return. I could use set or I could change this to init. I can use that and now I can say fName equals value because I know that's what that operation signature for the property looks like. It takes in a value of that type and we want to set it into that variable. You can use it for properties, you can use it for methods and you could use it for constructors. With the caveat being that the constructor has to take a single parameter, so you can set it in a expression. So, we'll do a follow up here. You create a constructor to initialize the first name.
>
> **[2:07](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/other-uses-for-expressions?u=76281980&t=127)** to set either the fName field or you can go through the first name property based on a parameter.

> [!info]- Semantic Content
>
> **Code Identifiers:** fname (4)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Warnings:** caveat (1)

#### [Solution: Delegate using Func<T>](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/4548039?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/4548039?u=76281980&t=0)** - [Instructor] In this challenge, you're supposed to update the FindLargest method here in the Answer class in order to use a delegate to solve for the largest. So if we look down in the test code, you can see we're going to call FindLargest, pass in the numbers, but we're also passing in a function here, this FindLargestSolver that's going to give us the largest answer. So we need to be able to come up here and add a new parameter then. And we're going to use the Func of TT. So what am I going to pass in? An integer array. And I'm going to get back and int. So that's my solver. So now we'll come in and we'll just call solver and pass in the numbers. So we're getting a delegate. That means it is a function or it is a method that we can call. So I'm going to invoke it. I know it takes an array of integers and it returns an int so it matches my signature. And that should find me the largest number. So go ahead and give it a test, and that's what we got. 19. So let's change this to 75 and try it again just to make sure. Sure enough, we get 75. So this allows us to pass in a function or a method that we can then invoke from our code here to get back the result.

> [!info]- Semantic Content
>
> **CLI Commands:** find (1), make (1)
> **Definitions:** is a  (2)
> **Speakers:** - [instructor] (1)


### 6. Extension Methods

[↑ Back to Table of Contents](#table-of-contents)

#### [The case for extension methods](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/the-case-for-extension-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/the-case-for-extension-methods?u=76281980&t=0)** - [Instructor] I want to talk about one of my favorite parts of C# and that's extension methods. But first, let's talk about why we need them. So I'm going to close up a couple of things here just to clean things up, get us back to our program, and I'm going to add a new class to our library project. So we'll add a class. I'm going to call that StringExtensions. I'm going to make it public and static, just like we have the other ones so we can get at the methods easily. And I'm going to come up here and just add to the namespace. So I'm going to add extensions on here and I'll talk about why a little bit later, but the key thing to remember is our StringExtensions class is in the Essentials2.Library.Extensions namespace. I often want to work with a string and grab different bits of it. A lot of the code where I work on client applications, I need to be able to parse out specific pieces. And the string class doesn't have a right method to grab the right most characters. So I might do something like this, (string input) and I probably want to make that static, just like the class. Let's say I've got the Right. And so if(string.IsNullOrEmpty(input))
>
> **[1:20](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/the-case-for-extension-methods?u=76281980&t=80)** then we're just going to return string.Empty. So we want to make sure we handle those cases. Otherwise, let's say we want the right X number. So we want to add on here maybe an int for numChars. How many characters do we want from the right end of the string? And now I can go in, I can do input.Substring and then I can provide a start index. So I can take the length of the input minus the number of characters, so Length - numChars. That gives me my starting point and I want to return that. Now we could put some additional error checking around here, right? We might want to ERROR check that the length is greater than the length is greater than the numChars. That would be a good idea. But you get the point. Our implementation simply allows me to get the right N or X number of characters from this input. And so if I go to my program, we'll comment out the code that we had here back in our program cs just to clean things up.
>
> **[2:48](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/the-case-for-extension-methods?u=76281980&t=168)** We'll go down to the bottom and we'll add some additional code. So that's in our Essentials2.Library.Extensions.stingExtensions.
>
> **[3:01](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/the-case-for-extension-methods?u=76281980&t=181)** We can choose Right, and now let's say I want to pass in something like this, ("right four") and then I want the 4 characters. So var right = we'll grab those and then you can write out the right. Well let's run that. We'll see if we've got all our logic correct here. We don't have any errors. Great, I've now got that functionality to grab the right most N number of characters from a string. But notice how I had to do this. I've now got to call the right method on my StringExtensions class. What I would love is if I had a string like this string s = "right four" then I could do s.Right. I have a pad right. That's not what I want. I want to be able to grab and maybe just do those four characters. So I want to be able to call something like that. Keeps correcting it to PadRight 'cause that's a method that exists. This doesn't currently exist and I don't own the string class. That's part of the .NET Framework. I can't go in and change the string class, wouldn't be a good idea based on how many things depend on it, but I don't have the control to do that. And yet I really want this to appear as a function of the string.
>
> **[4:33](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/the-case-for-extension-methods?u=76281980&t=273)** And that is where extension methods come in and we'll see how to implement them next.

> [!info]- Semantic Content
>
> **Code Identifiers:** numchars (3), stingextensions (1)
> **CLI Commands:** make (3)
> **Env Vars:** error (1), net (1)
> **Analogies:** just like (2)
> **UI Navigation:** go to (1)
> **Speakers:** - [instructor] (1)

#### [Writing an extension method](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/writing-an-extension-method?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/writing-an-extension-method?u=76281980&t=0)** - [Instructor] Now I want to go change that method, in my string extensions, to be an extension method, and it couldn't be easier. I'm going to go to the method, I'm going to type this in front of the type, string, for that input, and now this is an extension method. And it extends whatever that first type is, so it extends string, in this case, 'cause that's the first parameter. Importantly, when I'm in here, notice that, as I get the input, I don't have access to any additional functionality of the string class than I would in my normal method or operation that I had here. I have all of the same access, and I have all the same methods and properties available to me. So I'm not getting internal access, I'm not changing the way that I can work with string, I'm simply changing how this method is going to be accessed on the class. So, now, same idea, we're going to get that input, we're going to check if its empty, and then we're going to get that Substring based on the length minus the number of characters. And this may not seem all that exciting or important right now, that we're able to create this method because we don't get that internal access, but we are now adding functionality to the string class for consumers. It will look like a method on the string, and it will simplify the programming model.

> [!info]- Semantic Content
>
> **UI Navigation:** go to (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Using an extension method](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/using-an-extension-method?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/using-an-extension-method?u=76281980&t=0)** - Now that we've got our extension method, we can go out and use it. So I can come back out here and comment this and voila, that should work. But I'm getting that red squiggly. It doesn't understand that that right method is part of the string. And I'm showing you this because it's important and you're probably going to run into it. I can't tell you how many times I've found a method, the documentation, or I've worked with a developer who's seen an example and they're looking at it and they're trying to understand it, but it just doesn't work for them because it doesn't know about this method. And this will go back to C-Sharp Essentials Part One and the notion of namespaces. You remember I put that type in a slightly longer namespace with the extensions name on it and I told you it would make sense later. And hopefully now it does. I'm going to save that, go back down to the end and now it understands the right method. That static class that has our extension method in it has to be in scope, has to be in namespace scope. And sometimes when people write libraries or even in the framework, those extension methods are in a slightly different namespace or they may just be in a namespace that you haven't put in scope with the using statement. So we could change this to something like write fives just because that makes it five characters and we'll be slightly different. And we can write this out now because that's going to return the right five characters.
>
> **[1:35](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/using-an-extension-method?u=76281980&t=95)** So we should see four and then fives if we run this. And there we go. So now we've achieved what I wanted on line 91, I can now have a string variable and it has a method on it called right that will gimme the right most characters. And I've encapsulated that logic in one place. So now in my string extensions, if there's a problem with my logic, if I need to add additional error checking, I can do it in one place. And I know that it will get used in all the places where my library or my class is getting used and I've fixed it for all those places. And I'm not duplicating the same logic of checking for null, of checking length, of grabbing that sub-string based on the length minus the number of characters. There are a number of different places in the framework where you're going to see extension methods and you're going to see how they extend different classes in the framework, but you can also use them to extend those classes yourself. You can create these utility methods that you can use throughout your projects.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Cross-References:** go back to (1)
> **Documentation:** the documentation (1)
> **Tools:** notion (1)
> **Speakers:** - now (1)

#### [Common extension methods on collections](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/common-extension-methods-on-collections?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/common-extension-methods-on-collections?u=76281980&t=0)** - [Instructor] One of the most common places you'll find and use extension methods is on collections. We're going to look at, in the next chapter, [language integrated query](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md), and it's going to take advantage of these extension methods extensively. If I just take a look at a list of string here, I've got all of these items. I can look at names dot, and because the namespace is in scope, I get all sorts of different options here. I see this AsQueryable and AsQueryable of T. I get an Average. I get the ability to cast, I get some ElementAt or Distinct. So we're starting to see some things around selecting different items here. First, we can do, well, there's a new one with C Sharp 10 called MinBy where we can get a minimum. So let's do that. Let's do MinBy. What am I going to get? I'm going to get a string. And it wants a function of type string. So we're going to go in, we're going to pass it that s, we'll use our lambda, give it s.length. So what's that going to be? Var shortest, equal, Shortest name. And then we can do shortest. So what that's doing, we look at our expression we passed in, we're saying if you give me the string,
>
> **[1:34](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/common-extension-methods-on-collections?u=76281980&t=94)** I'm going to return the length. And then we're going to do a MinBy. So give me a string from this list. I want to find the minimum and I want to use this function to decide what the value is. So let's go ahead and comment out these other steps up here and we'll run that with our collection. We can see that the shortest name is xi, of course, it's only two letters. That's one example. So you'll see that these extension methods aren't on the Collection class itself. List of T doesn't have these. They extend it. And I can do things like OrderBy. And again, I'm going to pass the string in and we'll use that string .Length just 'cause it's a simple example. And what's that going to give us? Well, the OrderBy is going to return another collection of those strings. So we'll call that orderedNames. And then we can just do a foreach name in orderedNames, and we can write out the name. So if we've ordered those by the length, we should see those come out in order based on how short or long the names are. So we've got the shortest name and then you can see, in fact, we did go from shortest to the longest there. Those are just a few examples of some of the extension methods. And we're going to see in the next chapter when we talk about language integrated query, how we can use those
>
> **[3:09](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/common-extension-methods-on-collections?u=76281980&t=189)** to work with collections and work with a variety of different sources of information to query for values we want, for items we want.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Language integrated query](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) (2)
> **CLI Commands:** find (2)
> **Code Identifiers:** orderednames (2)
> **Cross-References:** in the next (2)
> **Speakers:** - [instructor] (1)

#### [Solution: Extension method](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-extension-method?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-extension-method?u=76281980&t=0)** - [Narrator] In this challenge, you're supposed to create an extension method that's going to allow you on an array of integers to call median, as you see in the test code, and get the median value or the middle number. So in order to do that, one of the things we know we've got to do is have a static method and use the this keyword on the target type, in this case, an int array. But we also have to make the class static in order for this to be an extension method. And of course we don't have to worry about it here, but it also has to be in scope in terms of the namespace. That is really the key to making this an extension method. You can see the compiler is happy down there in the test code. I'm going to go ahead and just paste an implementation in here. Not a particularly great implementation. It's not optimized for performance. This is just a little fun aside from the extension methods. I'm going to clone the array, sort the array on line 19. And then on line 21 and 22, I'm going to figure out how many items and what's the midpoint. And then I'm going to calculate the median. So if it's an even number, I'm going to do one thing. If it's an odd number, I'm just going to return the middle. So you can see here (double)cNumbers[Mid], want that even then, I'm going to take those two middle numbers and I'm going to divide by two to get the average of those. So again, not optimized, not really the focus here, but if we test our code, we got 13. So five and seven are the bottom numbers, 17 and 19 are the top numbers, 13's the middle. If we went ahead and added something in here,
>
> **[1:34](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-extension-method?u=76281980&t=94)** like let's say 16, then we should see the 16 and 13 added together get 29, and we should get the average of that, which is 14.5. So we've made sure it's a static class and a static method. We use the this keyword. And then down here in our test code, we're able to just call median on that int array. which is not a function that is inherently or natively part of that type.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Code Identifiers:** cnumbers (1)
> **Versions:** 14.5 (1)
> **Speakers:** - [narrator] (1)


### 7. Language Integrated Query (LINQ)

[↑ Back to Table of Contents](#table-of-contents)

#### [Understand the purpose of LINQ](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understand-the-purpose-of-linq?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understand-the-purpose-of-linq?u=76281980&t=0)** - [Instructor] In the previous two chapters, we've looked at expressions and extension methods. And while they both have great usability and provide excellent features in C# on their own, they're also part of a foundation of language-integrated query or [LINQ](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md). So let's take a look. If we comment out our names here which is just a simple collection of strings, let's add a collection of some types. We've got our employee type here that we've used. With the extension methods that we looked at for collections, we can do a number of different things. For example, we could take a look at our employees and I could say I want to do employees, and I can add a Where statement on there and now we're going to have an expression. So we've got a extension method with the Where method and we have an expression. So now we need our expression with our input and we're saying Where, and now I'm going to return a true or false. So I can look at this and say e.id is greater than two. And that's going to return, if we look by mousing over, see, it's going to return what's called an IEnumerable innumerable of employee. That's one of our collection interfaces that we can enumerate over. If we do that, we have our filtered employees, so we're going to have some subset of that that comes back out. So let's go ahead and we'll just comment out the last bit here.
>
> **[1:35](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understand-the-purpose-of-linq?u=76281980&t=95)** And now because it's an innumerable, we can iterate over it, our filteredEmployees, and now we could write out because that's an employee, we've got all that information, but let's just write out the first name. If we look at the IDs then, we should see three different employees that get returned. Sure enough, we see Pinal, Amanda, and Xi come back when we have the ID greater than two so they all fit that clause. And this is great and because these extension methods return an IEnumerable, we can actually chain things together. So if I do a dot here, I'm going to see a lot of those same extension methods come back. So I have the Where, but maybe I don't need the whole object. I don't need everything in there. I can use a Select and now I can do es for eselect as my parameter maybe. I can do es.FirstName and now if we look, we're getting a IEnumerable of string. So we're really just getting back to a collection of strings. But we also learned in C# Essentials Part One about anonymous types. So I could say I want to return a new, say FirstName equals. Now I'm just using the curly braces here
>
> **[3:08](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/understand-the-purpose-of-linq?u=76281980&t=188)** and LastName equals, yes, LastName. We'll add the closing curly brace so we've now returned an anonymous type that has a first name and a last name. So we've stripped off the ID now from our employees, but we've already filtered them. So if we look at filtered employees, it's now an IEnumerable of that anonymous type. It should still have a first name and a last name. So if we run that, we really shouldn't see any difference. Still able to use the Where extension and only return those three, and then I'm selecting or now projecting those results into an anonymous type. And all of this is great, but what you're starting to notice probably is it's getting a little messy. We've got these extension methods chained together, lots of expressions together. And so the extension methods and the expressions are getting us some functionality. But when we talk about language-integrated query, we really want the querying to be integrated into the C# language. Let's take a look at that next.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linq](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) (1)
> **Code Identifiers:** filteredemployees (1)
> **Env Vars:** linq (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Querying collections with LINQ](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/querying-collections-with-linq?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/querying-collections-with-linq?u=76281980&t=0)** - [Instructor] Looking at this example, we can actually rewrite this in a slightly different way. Let's do a var, we'll just say fEmployees, equal. Now I can use from, I can say emp in employees, so I'm now using some integrated query language. You can see that from and in here are keywords. I can now say where, emp, and we can do the same thing we did before. ID is greater than two. Select, oops, I've got to properly case the ID there. Now we could do the same thing. We can select a new object. In fact, we could just copy and paste this.
>
> **[1:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/querying-collections-with-linq?u=76281980&t=60)** So now we can simply change this to fEmployees. We should see the same results. If you've done any querying of [Databases](../../Skills/Software%20Development/Databases.md), for example, or other systems, a lot of these keywords, a lot of this structure is very similar. It's slightly different from, say, a [SQL](../../Skills/Data%20Science/SQL.md) query where you'd start with the select first, then the from and the where. But it's the same kind of idea. So we set up that query context of saying, what am I querying against? Well, the employee's collection, and I'm going to represent each item with an emp variable, that's like the input to our expressions. So when I say where emp.id is greater than two, you can see how that maps onto the where extension method up above where I had an input into my expression, and I then created the expression that returned a true or false. We need to fix up our expression parameter names there for this so that it works. We can still have the filtered employees, but we're going to walk through the fEmployees as we execute that. You'll see we get the same output that we got before, but we have a much cleaner syntax for writing the query here. Let's go ahead and set a break point here, and we'll try this with the debugger on. There we go. And we'll just hit F10, and we'll step over our debug spot here
>
> **[2:33](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/querying-collections-with-linq?u=76281980&t=153)** where you can use the toolbar up here. And now if I mouse over fEmployees, or if we look at it down here in our locals window, you can see that the type is now system link innumerable where select list iterator. It's a much longer and internal type name. And if we expand that, you'll see that current is null. And if you look at the results view, it says that expanding that will enumerate the I enumerable. What that means is at this point, fEmployees hasn't been iterated through. Of course then, we're going to do that here. That will become important as we talk later about other places where we can apply this language-integrated query to query against different sources. So this is a fantastic way to work with collections of objects, be able to manipulate them to get the results that you need for your business requirements. And it's all integrated into C#. So this is all understood under the covers. The where, the select, all of that, is really getting converted into those extension methods that we saw back on line 103, but it's a much better way to write these queries in your C# language.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Databases](../../Skills/Software%20Development/Databases.md) (1), [SQL](../../Skills/Data%20Science/SQL.md) (1)
> **Code Identifiers:** femployees (5)
> **Env Vars:** sql (1), f10 (1)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Filtering and ordering queries](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/filtering-and-ordering-queries?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/filtering-and-ordering-queries?u=76281980&t=0)** - [Instructor] We've got a basic query here with some filtering with our wear clause and our ability to select. We're selecting an anonymous type or projection, but we could also just select emp if we wanted to, just like that. So we can select the object itself. We don't have to do the projection,
>
> **[0:25](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/filtering-and-ordering-queries?u=76281980&t=25)** Get these cleaned back up. Now we can also add to this. So maybe we want the emp ID to also be less than five. So now if we look at our employees up top there, greater than two and less than five, we should really just see Pinal and Amanda. Could run that quick and confirm. Yep, that's what we get. We can also order this. So we can say order by. We can do emp ID descending. So now we're still filtering. We should still just get Pinal and Amanda, but because we're ordering by ID descending, Amanda should come first. In fact, if we run that, that's what we see. Another thing you can do is combine this kind of syntax with extension methods if you need to 'cause not everything is integrated into the query. So if I wrap this whole statement in the parentheses, that gives me the item and then I could do something like this. I could say I want to skip one item and then I want to take three items. Skip is going to skip over the first item in the collection. Take is going to identify a number of items we want to take out of the collection. The where statement was filtering based on some criteria. It was looking at the employee objects and returning of Boolean value. This is simply looking at them in order, skipping over some
>
> **[1:59](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/filtering-and-ordering-queries?u=76281980&t=119)** and then grabbing a certain number of those. So we're going to order by the employee ID descending. We're going to skip over the first one and take three. So if you look at the top there, get your guesses lined up as to what you think this should be. And we get Amanda, Pinal and Felicia 'cause we've ordered it descending, and then we've skipped over Xi and we've taken only three. So we get Amanda, Pinal, and Felicia. Now, there's a lot more complex queries that you can write. You can do joins, so you can be querying from multiple collections or multiple sources. You can use grouping to get values that are grouped together in that projection. I'm just showing you a sampling of what the [language integrated query](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) can provide in terms of giving you this quick, easy way to query against collections in order to get out just what you need. I've found that I tend to use this with collections more than even some of the other sources that we'll see in the next clip.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Language integrated query](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) (1)
> **Cross-References:** in the next (1)
> **UI Navigation:** select the (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Other sources for LINQ](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/other-sources-for-linq?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/other-sources-for-linq?u=76281980&t=0)** - [Instructor] We've used [LINQ](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) to work with these collections of objects, and that's great. There are a number of other places where you're going to query data usually. So when you're working with a database, you're probably going to use Entity Framework or another library that helps you access that. But those tend to use collections, so the syntax and things don't look a lot differently. I will say one thing about them, which is in your expressions, you may run into some problems where certain things, that you would normally type in here, don't work. So for example, when we're working with our orderbuy, we're just simply saying Employee Id. If we tried to do something like Employee Id ToString, it wouldn't necessarily work. And the reason is that there's two different ways LINQ expressions can be interpreted. And one, when we're working with objects here, and we create these expressions, and they get executed in C#. Another is to take this LINQ statement, everything on line 106 to line 109, and make an expression tree. But then a provider might translate that into something that [SQL Server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) or a database server can understand. So you can't have, for example, method calls in here that wouldn't be understood on the server. But all that said, another interesting place is if you're working with XML, for example, and I want to show you this primarily, because it does show the flexibility, but it also shows how things are a little bit different depending on what kinds of data you're working with.
>
> **[1:33](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/other-sources-for-linq?u=76281980&t=93)** So if we see something like var xEmployees equals, and then I'm going to use something called Xelement. And that's not going to be in scope, I need to add the namespace. I'll do a Control + . and I'm going to use System.Xml.Linq. Now that's going to put, as we know, the XElement class, but also maybe extension methods and other things, into scope. So now I can load, and I'm going to load, just like we did with our [JSON](../../Skills/Web%20Development/JSON.md) files, I'm going to load an Employees.xml. We'll put a semicolon there. Well, what the heck is that? I've added this into our project files. So you'll want to look in the Begin folder, you'll see that I've added the Employees.xml there, under the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Essentials 2 project. And at the top we have an Employees element. And then underneath it, we have repeating Employee elements. And this is going to look familiar, 'cause it's just like our objects we had, just so we can see the context here of XML. Each Employee has a FirstName element, a LastName element, and an Id element. Otherwise, this is the same data we were working with before, but we'll see now how this looks a little bit different. So if we want to do our expression, let's say something like xEmpLinq equals, and now we can do our from X Employee in xEmployees, but that's not really going to give us what we want,
>
> **[3:07](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/other-sources-for-linq?u=76281980&t=187)** 'cause if we look at xEmployees, that is an XElement, a knowable XElement. And we need a collection or we need a set of items that we can query over. So I'm going to use .Descendants, if I can spell it right, and that will allow us to pass in a string. And say, well what name do these descendants have? Employee. So when I load it, the top level XElement is going to be that Employees. Now I'm saying get me all the Employee elements underneath that, and that's what I'm querying against. And I could use the where. So I might want to do something like xemp, notice, I have Name, Value, NodeType. Those are the suggestions coming up on the top there. What I'm not going to find, if I go down through this list, so I'm not going to find FirstName and LastName, because this isn't an object, it is XML. And so it doesn't necessarily know the structure. But I can say I want an Element, Id. And I want to be able to do what I did before and say I want that to be greater than two. Now when I get that element, it's a string. Everything in that file is a string, but built into the LINQ to XML is the notion that an element can be easily cast to different types. I happen to know that Element's an int and so I can cast it to an int, and now I can compare it to that too. Now this is where again, we might want to use our projection
>
> **[4:45](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/other-sources-for-linq?u=76281980&t=285)** to project a new object, because what we don't want is a bunch of XML. We want to get this mapped into or projected into some objects. So I can do FirstName equals, and then I can do xemp, Element, and I can do FirstName. And I can do LastName equals xemp, Element, LastName. Now those are both strings, so I don't need to do any casting or anything here. Those are going to come back as strings and be assumed to be strings when they come back. And then we can do something like before, I'm just going to paste in here a foreach. So it'll iterate over that collection now of those anonymous types coming back. And we can write out the FirstName value and the LastName value. Now why is that happening? Why do I need that value there? Let's run through it and we'll take a look. Notice I've got this FirstName, but if you look at it, it's actually the XML. So when I got the element and I got it back as a string, it gave me the whole thing, and that's not really what I wanted. So I used the value here. Now we know how we could clean this up by simply coming up here and using Value. And now what we're going to get is the actual value on there. So we can take that off of here,
>
> **[6:21](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/other-sources-for-linq?u=76281980&t=381)** 'cause it no longer makes sense. The objects we get back are simply going to be strings.
>
> **[6:30](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/other-sources-for-linq?u=76281980&t=390)** Now we'll run that. We should get that same output. Now we get the FirstName and LastName. And we still see those first three lines from our previous example, but then our next three. So we've done the filtering. We've done the projection across that XML document. So it's not quite as clean as what we were doing with objects, because with objects we know the types, we know the different properties, and what their data types are. The XML, it's not quite as easy, but with that implicit casting of the element that we can do with the ability to get the descendants, or query down in there, you can really work with XML in a much simpler way as you're trying to query through it in your C# code. That's one example. As I said, you can use LINQ with Entity Framework. You can use LINQ with anything really that creates those objects. But there are special cases, like Entity Framework, where it can translate your LINQ expressions into something that an external provider, like a database, can understand to actually execute the query. Hopefully that gets you started with LINQ in a number of different scenarios within C#, and there's a lot more that you can learn to dig into Language-Integrated Query.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linq](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) (9), [Sql server](../../Skills/Software%20Development/Microsoft%20SQL%20Server.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Env Vars:** xml (9), linq (8), sql (1), json (1)
> **Analogies:** for example (3), just like (2)
> **Code Identifiers:** xemployees (3), xemplinq (1)
> **File Paths:** employees.xml (2), system.xml (1)
> **CLI Commands:** find (2), make (1)
> **Definitions:** is a  (2), is an  (1)
> **Cross-References:** coming up (2)

#### [Solution: LINQ query to filter and sort](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-linq-query-to-filter-and-sort?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-linq-query-to-filter-and-sort?u=76281980&t=0)** - [Instructor] In this challenge, you need to use [LINQ](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) in order to take a array or collection of orders. You see our Order record defined there on line six, and we want orders with a totals greater than 1,000, and we want to sort them by the order date. If we go here to our implementation and replace the null, and we'll say from o in Orders, and then we put our filter on, where o.OrderTotal > 1,000. And then, we can say, orderby o.OrderDate, select o, not zero, o. And then, because the return type is list, we need to call ToList(). So, give me the orders where the total's greater than 1,000 order by the date. If we look down here in our test code, we've got eight different items there. Got a number of different values for the order total. It looks like we have four that are over and they have different dates, so we're going to query those and test that. And we've got the right answer. So, we've got the values there. If for example, I switch this to less than 1,000, I'd be filtering those other items and we're not going to get it. Now, another way that you can write this is to use the extension methods. So, you can take the orders.Where, you can say o, pass that in, o.OrderTotal > 1,000. And then, you can OrderBy,
>
> **[1:36](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/solution-linq-query-to-filter-and-sort?u=76281980&t=96)** pass in o, o.orderDate, and toList(). This uses extension methods whereas previously, I was using the [Language Integrated Query](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) right in the C# language, but it's all based on these extension methods, so we should see success using either method.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Linq](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) (1), [Language integrated query](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) (1)
> **Code Identifiers:** orderdate (1), tolist (1)
> **Env Vars:** linq (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Build something and continue learning](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/build-something-and-continue-learning?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/build-something-and-continue-learning?u=76281980&t=0)** - [Matt] Thank you so much for joining me in C# Essential Training 2. I hope you've enjoyed the learning journey and that you're excited about the applications you can write with C#. As you look to the next steps, there's some additional C# training you could take. We really touched on [LINQ](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) at the very end, or [Language Integrated Query](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md), but there's so much more you can do with it, and there's more complex operations and grouping that you can do as well. So if that's something you think you're going to use, I would recommend you go out and dive a little deeper with some of the LINQ training. Asynchronous programming is another piece where we touched on the basics of the async/await model of tasks, but again, if you're getting into multi-threaded programming, you'll want to dig deeper into some additional courses on asynchronous programming with C# so you really grasp the fundamentals underneath that. Additionally, just like I said at the end of C# Essentials, one, other C# language features that you can look into, and then other frameworks. So Entity Framework is a very common framework for people to use for data access to all sorts of [Database Systems](../../Skills/Software%20Development/Database%20Systems.md). [ASP.NET](https://ASP.NET) is a framework for building web applications. You have Blazor for building client applications using web technologies, like [HTML](../../Skills/Web%20Development/HTML.md) and [Cascading Style Sheets](../../Skills/Web%20Development/Cascading%20Style%20Sheets%20(CSS).md), but C# instead of [JavaScript](../../Skills/Software%20Development/JavaScript.md). You have Xamarin and .NET MAUI coming out soon, all for building cross-platform applications,
>
> **[1:34](https://www.linkedin.com/learning/c-sharp-essential-training-2-generics-collections-and-linq/build-something-and-continue-learning?u=76281980&t=94)** especially mobile applications for iOS, [Android](../../Glossary/Sdk/Android.md), and [Windows](../../Glossary/Service/Windows.md) and Mac. And you have Windows Presentation Foundation, and other UI frameworks if you're specifically targeting Windows. So depending on the kinds of applications you want to build, any of these last four may be a good place for you to move next with your C#, and if you're going to be accessing [Databases](../../Skills/Software%20Development/Databases.md) in any of those kinds of applications. Entity Framework is [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md)'s offering in that space that you'll want to take a look at. Now, other than watch courses, the main thing I can recommend is that you go build something you're excited about. When you set up some requirements for an application and what it needs to do, and then you set out to achieve that with a programming language, I found that's the best way to learn the language and to learn how to achieve the tasks that you're going to need in real programming projects. Again, thanks so much for joining me in C# Essentials 2. I hope you've got a lot out of it, and have a great time building applications with C#.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Windows](../../Glossary/Service/Windows.md) (3), [Linq](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) (2), [Language integrated query](../../Skills/Software%20Development/Language%20Integrated%20Query%20(LINQ).md) (1), [Database Systems](../../Skills/Software%20Development/Database%20Systems.md) (1), [ASP.NET](../../Skills/Web%20Development/ASP.NET.md) (1)
> **Env Vars:** linq (2), net (2), asp (1), html (1), maui (1)
> **Definitions:** is a  (2)
> **CLI Commands:** go build (1)
> **Code Identifiers:** ios (1)
> **URLs:** [asp.net](https://asp.net) (1)
> **Analogies:** just like (1)
> **Prerequisites:** set up (1)


## Instructor

- [Matt Milner](../../Instructors/Software%20Development/Matt%20Milner.md)

## Resources

- Exercise files available

## Skills Covered

- Language Integrated Query (LINQ)
- C#

## Path Context

### In [C- Foundations- From Basics to Building Blocks](../../Paths/Software%20Development/Learning%20Paths/C-%20Foundations-%20From%20Basics%20to%20Building%20Blocks.md)
← [C Sharp Essential Training 1 Types And Control Flow](C%20Sharp%20Essential%20Training%201%20Types%20And%20Control%20Flow.md) | **2 of 5** | [Debugging in C-](Debugging%20in%20C-.md) →

### In [Getting Started as a C- Developer](../../Paths/Software%20Development/Learning%20Paths/Getting%20Started%20as%20a%20C-%20Developer.md)
← [C Sharp Essential Training 1 Types And Control Flow](C%20Sharp%20Essential%20Training%201%20Types%20And%20Control%20Flow.md) | **2 of 9** | [C- and .NET Essential Training](C-%20and%20.NET%20Essential%20Training.md) →

## Appears In

- [C- Foundations- From Basics to Building Blocks](../../Paths/Software%20Development/Learning%20Paths/C-%20Foundations-%20From%20Basics%20to%20Building%20Blocks.md)
- [Getting Started as a C- Developer](../../Paths/Software%20Development/Learning%20Paths/Getting%20Started%20as%20a%20C-%20Developer.md)

## Related Courses

_Courses sharing skills:_

- [Learning C-](Learning%20C-.md) — C#
- [Cert Prep- Unity Certified Associate Game Developer Scripting with C-](Cert%20Prep-%20Unity%20Certified%20Associate%20Game%20Developer%20Scripting%20with%20C-.md) — C#
- [Nail Your C- Interview](Nail%20Your%20C-%20Interview.md) — C#
- [C- Design Patterns Part 2](C-%20Design%20Patterns%20Part%202.md) — C#
- [C- Design Patterns Part 1](C-%20Design%20Patterns%20Part%201.md) — C#

---

[↑ Back to top](#)