---
type: course
cssclasses:
  - lle-course
  - lle-level-beginner
slug: learning-the-javascript-language-22309208
url: "https://www.linkedin.com/learning/learning-the-javascript-language-22309208"
level: Beginner
updated: 8/14/2024
learners: 400716
skills:
  - JavaScript
exercise_files: true
github: "https://github.com/LinkedInLearning/learning-the-javascript-language-4369302/codespaces"
thumbnail: "https://media.licdn.com/dms/image/v2/D560DAQEpa5LhtPUlUg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1683586021773?e=2147483647&amp;v=beta&amp;t=eWdXogROIX3NTc6aUHOyMCTpm9PuwajA52aAEOLOsdU"
linkedin_topic: Software Development
learning_paths:
  - '[[JavaScript Foundations Professional Certificate by Mozilla]]'
  - '[[Become a JavaScript Developer]]'
prev_courses:
  - null
  - '[[JavaScript Essential Training]]'
next_courses:
  - '[[JavaScript Essential Training]]'
  - '[[Learning ECMAScript 6+ (ES6+)]]'
path_nav: '[{"path":"JavaScript Foundations Professional Certificate by Mozilla","position":1,"total":2,"prev":null,"next":"JavaScript Essential Training"},{"path":"Become a JavaScript Developer","position":2,"total":13,"prev":"JavaScript Essential Training","next":"Learning ECMAScript 6+ (ES6+)"}]'
path_count: 2
tags:
  - course
  - topic/software-development
  - topic/web-development
  - topic/network-and-system-administration
  - skill/javascript
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/Learning%20The%20Javascript%20Language.md)

![Learning The Javascript Language](https://media.licdn.com/dms/image/v2/D560DAQEpa5LhtPUlUg/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1683586021773?e=2147483647&amp;v=beta&amp;t=eWdXogROIX3NTc6aUHOyMCTpm9PuwajA52aAEOLOsdU)

# Learning The Javascript Language

> JavaScript is one of the most popular programming languages in the world, used to create dynamic websites along with a host of other applications. And like any language—whether spoken or for programming—it takes time and practice to become conversant. In this course, Joe Chellman teaches you how to “speak” JavaScript by familiarizing yourself with the most commonly encountered topics and concepts,

> [LinkedIn Learning](https://www.linkedin.com/learning/learning-the-javascript-language-22309208) | Beginner | 401K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Become conversant in JavaScript](#become-conversant-in-javascript)
  - [What you should know](#what-you-should-know)
  - [Exercise files and Codespaces](#exercise-files-and-codespaces)
- [**1. Getting Started**](#1-getting-started) (5 videos)
  - [Syntax: Learning to read and write](#syntax-learning-to-read-and-write)
  - [Versions of JavaScript](#versions-of-javascript)
  - [Places to tinker](#places-to-tinker)
  - [Your friend the text editor](#your-friend-the-text-editor)
  - [Additional helpful resources](#additional-helpful-resources)
- [**2. Variables and Types**](#2-variables-and-types) (6 videos)
  - [Declaring and assigning variables](#declaring-and-assigning-variables)
  - [Strings](#strings)
  - [String properties and methods](#string-properties-and-methods)
  - [Numbers](#numbers)
  - [Booleans for what is true](#booleans-for-what-is-true)
  - [Variable mutability](#variable-mutability)
- [**3. Objects, Arrays, and More**](#3-objects-arrays-and-more) (9 videos)
  - [Objects](#objects)
  - [Using objects for data modeling](#using-objects-for-data-modeling)
  - [Manipulating objects](#manipulating-objects)
  - [Jargon: References](#jargon-references)
  - [Arrays](#arrays)
  - [Manipulating arrays](#manipulating-arrays)
  - [Readability: Whitespace](#readability-whitespace)
  - [Readability: Comments](#readability-comments)
  - [Regular expressions](#regular-expressions)
- [**4. Operators and Control Structures**](#4-operators-and-control-structures) (8 videos)
  - [Simple comparisons](#simple-comparisons)
  - [Arithmetic operators](#arithmetic-operators)
  - [Logical operators](#logical-operators)
  - [Conditionals: If](#conditionals-if)
  - [Conditionals: Switch](#conditionals-switch)
  - [Terse ifs: One-liners](#terse-ifs-one-liners)
  - [Terse ifs: Ternaries](#terse-ifs-ternaries)
  - [Type checking](#type-checking)
- [**5. Iterating with Loops**](#5-iterating-with-loops) (4 videos)
  - [For loops: Sequential](#for-loops-sequential)
  - [For loops: Enumerative](#for-loops-enumerative)
  - [Set and Map: Newer loopable data types](#set-and-map-newer-loopable-data-types)
  - [While loops](#while-loops)
- [**6. Functions**](#6-functions) (10 videos)
  - [Basic functions](#basic-functions)
  - [Arguments and parameters in functions](#arguments-and-parameters-in-functions)
  - [More on function arguments](#more-on-function-arguments)
  - [Objects, references, and functions](#objects-references-and-functions)
  - [Functions are objects](#functions-are-objects)
  - [Jargon: Scope in JavaScript](#jargon-scope-in-javascript)
  - [Functions and scope](#functions-and-scope)
  - [Writing shorter functions with arrows](#writing-shorter-functions-with-arrows)
  - [Three little dots collect the rest](#three-little-dots-collect-the-rest)
  - [Callback functions and looping](#callback-functions-and-looping)
- [**7. A Few More Advanced Pieces**](#7-a-few-more-advanced-pieces) (6 videos)
  - [Asynchronous code: Waiting is the hardest part](#asynchronous-code-waiting-is-the-hardest-part)
  - [Promises, async, and await](#promises-async-and-await)
  - [Object-oriented JavaScript: Prototypes and classes](#object-oriented-javascript-prototypes-and-classes)
  - [Jargon: Strong vs. loosely typed](#jargon-strong-vs-loosely-typed)
  - [Modern JavaScript tooling](#modern-javascript-tooling)
  - [Responsible JavaScript](#responsible-javascript)
- [**Conclusion**](#conclusion) (1 videos)
  - [Continue your JavaScript development journey](#continue-your-javascript-development-journey)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Become conversant in JavaScript](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/become-conversant-in-javascript?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/become-conversant-in-javascript?u=76281980&t=0)** - Back in the old days of the web, [[HTML]] was the only language available and you had to code static sites page by page. But now with [[JavaScript]], the web is extremely programmable and because of its popularity, JavaScript can be used to program much more than just the web. This course will walk you through the basics of JavaScript syntax, with the goal of becoming conversant in the language. You'll begin to understand most of the major pieces of JavaScript, such that you'll be able to read and write enough code to get started on your own projects. Hi, I'm Joe Chellman, and I've been using JavaScript for an almost alarmingly long time, at least 20 years. Just thinking about that fact makes me want to take a nap. If you're curious about JavaScript, I think you are, whether you're a total beginner or someone making the jump from another language, welcome to this [[LinkedIn]] Learning course. Let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (6), [[HTML]] (1), [[LinkedIn]] (1)
> **Env Vars:** html (1)
> **Speakers:** - back (1)

#### [What you should know](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/what-you-should-know?u=76281980&t=0)** - [Instructor] Here's what you should know to get the most out of this course. We don't have a lot of prerequisites here. We'll be approaching [[JavaScript]] from a web perspective for the most part, so mainly, I assume that you've used the internet for a while and that you have a pretty solid grasp of how a web browser works, more or less basic computer literacy for the current time. There will be a couple of very small mentions of [[HTML]], but prior experience with it is not necessary. Beyond that, you don't need much. If you do have programming experience in another language, that's great but not required.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (1), [[HTML]] (1)
> **Env Vars:** html (1)
> **Speakers:** - [instructor] (1)

#### [Exercise files and Codespaces](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/exercise-files-and-codespaces?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/exercise-files-and-codespaces?u=76281980&t=0)** - [Tutor] The exercise files for this course include everything you'll need to follow along, because this is a course about learning how to read [[JavaScript]], as well as write it, the files are mostly in the form of transcripts of what you see, as well as some examples for copying and pasting. There are folders for each chapter, and each video containing anything you'll need to try what you've seen. You can open these and read through them at your own pace. The files are provided in a [[GitHub]] repository, which gives you many options for how to use them. You can download a ZIP archive of the files to your machine, unzip it, and open them in your favorite text editor, we'll discuss editors shortly, and if you've used GitHub before, you should feel free to fork the code to your own account if you like. However, there's a new and very powerful way to interact with this courses files that allows you to follow along exactly, using everything I'm using in exactly the same way. GitHub [[Codespaces]]. Codespaces are [[Virtual Machines]] running on GitHub servers that give you Visual Studio Code, a powerful text editor with the same plugins I'm using, and an installation of [[Node.js]], a JavaScript environment that runs in Visual Studio codes built in Terminal, so you can do everything in the same browser window. In order to use codespaces, you'll need a free GitHub account From the course page On [[LinkedIn]] Learning, you'll see a button that will take you to the exercise files on GitHub, labeled GitHub Codespaces here, and it will allow you to create a codespace on Main. This means you're creating your own individual codespace
>
> **[1:36](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/exercise-files-and-codespaces?u=76281980&t=96)** from the code on the LinkedIn Learning GitHub account, Main is the name of the branch of the code you're using. The [[Version Control]] software under the hood is called [[Git]], and branches are used to track different revisions to code over time. In this course, there's only one that's called Main, but in other courses, you might see many. You can also create a codespace from the main exercise files page, here under Code, you would click Codespaces. Now I already have one, which is showing up right here, for you, there will be a Create Codespace on Main button. I could also create additional codespaces using the plus button up here. Codespaces can be started from the LinkedIn Learning Repository here, or if you decide to fork the code to your own account, which means you have a totally separate copy of the code attached to your GitHub account to play with however you like, you can create a codespace from there, all the configuration for the codespace will come with the forked code. I'll click on my codespace name here to launch it. It will take a minute or so to load, depending on your connection speed, and once it's finished, you'll be in an online version of Visual Studio Code, and you can open any of the course files, which again, are transcripts and runable examples. Here under the Files tab, you can see all the chapter numbers here, and these are the videos associated with each chapter that have exercise files. So I can open any of these to see a transcript, which is what you'll see in most cases, and every once in a while,
>
> **[3:09](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/exercise-files-and-codespaces?u=76281980&t=189)** there will be another file or two as well. We'll be running most of the course code in the Editor's Terminal via Node. My codespace has the terminal open already. If you don't see that, you can go to the Menu button up here, and then under Terminal, Create a New Terminal. If you already have one running, there'll be a little sidebar that shows up that lets you choose from whichever ones you've launched. I'm going to click the little trash button to leave me with just the one that I need, and we'll be running the code using Node, which you can start just by typing "node" there in the terminal. So that's what a codespace looks like when it's running. I should note that Codespaces are free to use for 60 hours a month. That's a lot of time, but if you're working through a lot of courses using codespaces, it is possible that you'll consume all of those hours. If that happens, you can choose to pay for more time, these prices are shown in US dollars for me, or just wait until the beginning of the next month when you get a new, free block of 60 hours to play with. With codepaces, there's nothing to install to get the same high-quality developer experience as me. As you become more comfortable with JavaScript, you can potentially create entire projects using a codespace, but for now, it gives you a very convenient way to access the files and tools for this course all in one place. I hope it works well for you. Offering courses with codespaces is a relatively new offering from LinkedIn Learning, so if you run into any trouble with the exercise files, please submit requests for help in the course Q&A, or directly to LinkedIn Learning Support.
>
> **[4:42](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/exercise-files-and-codespaces?u=76281980&t=282)** We want to make sure this experience works well for everyone.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Codespaces]] (10), [[GitHub]] (9), [[LinkedIn]] (5), [[JavaScript]] (3), [[Virtual Machines]] (1)
> **Tools:** github (9), terminal (6), visual studio (3)
> **CLI Commands:** node (4), unzip (1), git (1), make (1)
> **Exercise Files:** exercise files (5)
> **Prerequisites:** you'll need (3), install (1)
> **Definitions:** is a  (2), is called (1)
> **UI Navigation:** click on (1), go to (1)
> **File Paths:** node.js (1)


### 1. Getting Started

[↑ Back to Table of Contents](#table-of-contents)

#### [Syntax: Learning to read and write](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/syntax-learning-to-read-and-write?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/syntax-learning-to-read-and-write?u=76281980&t=0)** - [Instructor] In the next several chapters of this course, we'll be going over the syntax of [[JavaScript]]. This is not going to be a complete coverage of every single element of JavaScript syntax because the goal here is to allow you to get conversant in JavaScript. Just as with any other spoken language, you won't be totally fluent in a matter of a few hours, but you will be able to read code like this which is loaded with non-executable comments and some actual functionality and you'll be able to make sense of it. You'll understand control structures, the way we make decisions about what to do and when. You'll understand the different kinds of data types that are available, like numbers, lists, and so on. You'll know how to use variables which are places to store data. You'll understand comments and white space which help us understand what we've written and make sure we can actually read it. Generally, you'll have a pretty solid understanding of how the language is put together, enough that you can start working with it on your own. That's the goal. Not perfect fluency, but to become conversant. Programming languages are a lot like human spoken languages. You need to learn the grammar in order to use it, but that's not enough to become totally fluent. You need to put in a lot of time reading the language, writing the language, generally using it in whatever way you want to. With human spoken languages, of course, you could simply speak it with your voice and get a lot of use from it. But with JavaScript, you need to be able to read and write it. And after going through this course, you should be able to read and write code and begin to do what you need to using the language.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (4)
> **CLI Commands:** make (3)
> **Cross-References:** in the next (1)
> **Speakers:** - [instructor] (1)

#### [Versions of JavaScript](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/versions-of-javascript?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/versions-of-javascript?u=76281980&t=0)** - [Narrator] Programming languages go through revisions over time just like apps do. Bugs are fixed, new features are added. In [[JavaScript]], the feature editions are what people tend to get excited about, which isn't a surprise really. Let's talk briefly about the versions of JavaScript that you'll hear about. Though JavaScript is the language's most commonly used name, the body that maintains its standards is ECMA International, so versions are named accordingly. [[ECMAScript]] 5 or ES5 is the version that runs on all modern web browsers really since 2012 or so. There are older versions, but this one is a general baseline. And until fairly recently, ES5 was the version people would usually learn first since there were still enough places where you couldn't expect anything newer to run safely. Indeed, that was the version of JavaScript mainly covered in prior versions of this very course. As of now, we're supporting the next version, ECMAScript 2015, also known as ES6, as the baseline. There are a lot of new features in this version, and browser support is generally good enough to use in any modern environment safely. As you might guess, there are other newer versions as well finalized pretty much every year that continually add more features and make the language feel new and shiny all over again. For the most part, ES5 and ES6 make up the lion's share of JavaScript in common use. If you want to use the features of a newer version of JavaScript without sacrificing compatibility, there exist tools called transpilers, which can take your newer, less compatible JavaScript and convert it to ES5 or whatever prior version you need, so it can run on older web browsers.
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/versions-of-javascript?u=76281980&t=94)** The most popular of these is called Babel, available at [babeljs.io](https://babeljs.io). If your JavaScript journey will go into more recent features and you need to add support for older browsers with the code you write, you'll want to be familiar with transpilers. The main takeaways for now are that JavaScript is constantly evolving. We are covering ECMAScript 2015 or ES6 as the baseline here in this course, but everything we cover in this course remains useful as JavaScript continues to evolve.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (10), [[ECMAScript]] (3)
> **Env Vars:** es5 (4), es6 (3), ecma (1)
> **Definitions:** is a  (1), known as (1), is called (1)
> **CLI Commands:** make (2)
> **URLs:** [babeljs.io](https://babeljs.io) (1)
> **Analogies:** just like (1)
> **Speakers:** - [narrator] (1)

#### [Places to tinker](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/places-to-tinker?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/places-to-tinker?u=76281980&t=0)** - [Instructor] When you're beginning to work with [[JavaScript]], it's important to start trying it out as soon as possible. Fortunately, there are lots of easy ways for doing this. In this video, we're going to look at a couple of the easiest. One is the JavaScript console built into any web browser and the other is the console of [[Node.js]], accessible in this course via the [[GitHub]] code space. We'll start with web browsers, since everyone has one, and as far as I know, every single web browser on a desktop computer has a JavaScript console. Here's what the console in Chrome looks like. I can type a line of JavaScript in here and hit return. So let's say I'm going to type alert and I'm going to make it say boo. And the browser executes it right away. A couple of notes. The console only shows one line, but you can type shift return to get more lines. And wherever your cursor is, if you just hit return, whatever you've typed will execute. Different browsers will place the menu item that activates the console in different places. We'll go through some of the popular ones quickly here. In Chrome, it appears in the view menu under developer. Here's JavaScript console and it's also part of the developer tools. If you use the developer tools, it'll pop up probably on this elements pane, and then you can just switch to console. In Firefox, it's under tools, browser tools, web developer tools. And then from here you can switch to the JavaScript console. Note that in Firefox there's also a browser console,
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/places-to-tinker?u=76281980&t=94)** but that's different. In [[Microsoft]] Edge, the menu item is under tools, developer, JavaScript console, just like in Chrome. And finally, in Safari, you'll need to turn on the develop menu via the advanced preferences. So if I open the preferences via command comma or the preferences item in the Safari menu, under advanced preferences I need to turn on the develop menu in the menu bar. Once I have that, it's under show JavaScript console. If you're interested in using JavaScript for backend development, you'll probably want to look at Node.js, which is installed as a command line utility called Node. If I run it with no options, it immediately drops into what's called a REPL, a read, evaluate, print loop where you can play with a JavaScript just as you would in a browser. One nice thing about Node is that it detects when something you're typing should extend into multiple lines. So if I start typing out a function, which we'll talk about later, just bear with me for the moment, when I type return for the first time node knows what I'm doing and gives me a chance to write more before it executes what I've typed. And there we go. So that's pretty neat. To exit, type Ctrl D or .exit. Here's the node.js website where you can download
>
> **[3:07](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/places-to-tinker?u=76281980&t=187)** the version for your operating system of choice. I'm loading this page on a Mac, so it's giving me the Mac versions to install. And then if you want to learn more about how to use that node REPL, that information is here on their documentation site. In this course, we'll have access to both the browser console and node because we'll access everything via this GitHub code space. This environment runs in a browser, so we'll have access to the browser console when we need it and we'll also have Node available here in the terminal. We have everything we need without having to install anything special. Now you've seen some of the possibilities for where to play around with JavaScript. I'm expecting most of you will use this GitHub code space and web browsers at first, but Node is also available.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (11), [[Node.js]] (3), [[GitHub]] (3), cursor (1), [[Microsoft]] (1)
> **CLI Commands:** node (10), make (1)
> **Tools:** github (3), firefox (2), safari (2), command line (1), terminal (1)
> **UI Navigation:** switch to (2), open the (1), in the menu (1)
> **File Paths:** node.js (3)
> **Prerequisites:** install (2), you'll need (1)
> **Env Vars:** repl (2)
> **Analogies:** just like (1)

#### [Your friend the text editor](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/your-friend-the-text-editor?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/your-friend-the-text-editor?u=76281980&t=0)** - In this video, I'm going to extol the virtues of a good text editor giving you some reasons why you might actually want to have one in your toolkit whatever your level of experience. If you're a web designer coming to [[JavaScript]], you might have an editor you like for [[HTML]] and CSS and that editor will probably be great for JavaScript as well. I've been teaching new developers for years and I'm continually impressed with how many high quality free editors are available. We'll mostly talk about the features that are noticeable when you're working on very small projects with a small number of files and not a lot of code. Editors can of course work with projects including thousands of files but they're great for small things you'll be looking at when you start as well, and it's worth noting that using [[GitHub]] code spaces for this course means that you'll have a nice editor without any further effort. We'll be looking at editors for other projects that you might work on in the future. The first thing that you'll notice when using a code editor is syntax highlighting the editor uses colors and styles to mark the different kinds of code to help us know what we're looking at at a glance. So that means the difference between this and this. syntax highlighting basically takes the grammar of the language and makes it visible. Understanding what we're trying to do is much easier when we can see this much detail. Code completion is the next big one. Your editor saves you typing by giving you options for how to finish what you're typing based on what it knows about JavaScript and about the code in the files you're working on. So for example, here in a empty JavaScript file, if I start typing console,
>
> **[1:35](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/your-friend-the-text-editor?u=76281980&t=95)** I get completions on the things that JavaScript knows I might want. Most editors these days have some awareness of complete projects and can draw on that knowledge to save you work. So that means you'll get code completion not just for JavaScript itself, but anything you or your team have defined as well. Editors can also help with the tedious parts of writing code, like managing special punctuation. JavaScript uses a lot of parenthesis, quotation marks braces, and other punctuation that often come in pairs. You can see a lot of those just here at the beginning of this massive file. Pretty much all editors will automatically autotype these for you and auto delete them if you type too many. So if I start to define a function, I know we don't necessarily know what that is, but just bear with me for a moment. If I type that first of my pair of parentheses, the editor gives me the other one automatically. You can see my cursor is between them, but I've only typed one of them. And if I change my mind and just hit delete one time, they both go away. This works for other kinds of punctuation as well. There are also features to help you avoid common errors, reformat code, so it's more readable, but still executes correctly and more. Eventually, you might even step up to an integrated development environment or IDE, which has all the features of a great editor but also adds features to help professional and enthusiastic amateur programmers get their work done faster. I said there are a lot of great options. So let's look at a few all of which are available for MacOS, [[Windows]], and [[Linux]]. Visual Studio Code is from [[Microsoft]].
>
> **[3:10](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/your-friend-the-text-editor?u=76281980&t=190)** It's open source and free and particularly JavaScript friendly. It's updated almost every month with lots of useful features and is very popular right now. It's also the editor that GitHub Code Spaces uses so it's what you'll see in this course. Sublime Text is another very popular editor. It's not free, but it can be used without payment for a long time. Unlike many other current options, which are kind of like high-powered web apps running on your machine, sublime is native which should make it feel faster to use. Brackets is another open source option, founded by Adobe but now developed by its own community. It's targeted at web designers with support for CSS pre-processors out of the box, among other things. And of course, it's also great for JavaScript. If you're wondering what I use in my daily work, I mostly work in PhpStorm, an IDE that supports JavaScript and [[PHP]] equally well, which is important for me. It's a little heavier to work with than these other editors. It takes a very little bit of configuration for any project that I add to it, but once it's set up, I get so much useful help that I miss it every time I use another editor for Serious Work. As you grow in your skills as a JavaScript programmer, you might consider looking at WebStorm, Jet Brains JavaScript only IDE and see if you like it. My recommendation for now is that you pick one of these editors, download it and stick with it for at least a couple of weeks, learning its keyboard shortcuts and maybe even reading some documentation so you can really get the most out of them. You'll probably start to recognize the things that you like and what you wish you could change. If the editor doesn't work a way that feels natural to you,
>
> **[4:43](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/your-friend-the-text-editor?u=76281980&t=283)** you can try another one. There's lots of options available, and they're all great.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (12), [[GitHub]] (2), [[HTML]] (1), cursor (1), [[Windows]] (1)
> **Env Vars:** ide (3), css (2), html (1)
> **Tools:** github (2), sublime (2), visual studio (1)
> **CLI Commands:** make (1), php (1)
> **Analogies:** for example (1), kind of like (1)
> **Definitions:** defined as (1)
> **Prerequisites:** set up (1)
> **Speakers:** - in (1)

#### [Additional helpful resources](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/additional-helpful-resources?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/additional-helpful-resources?u=76281980&t=0)** - [Instructor] As you're proceeding through this course, you may find that some things don't seem clear right away. Depending on your learning style, having some additional support immediately available could be useful. In this video, I'll offer a few books and other resources that have been helpful to me and I hope will be helpful to you as well. First, a few books, many of which can be read online for free. "Eloquent [[JavaScript]]" by Marijn Haverbeke is available online for free. It's in its third edition as of this recording and a modern classic. It's not short, but it is excellent. Dr. Axel Rauschmayer has a series of JavaScript books available on his website for free reading in the browser. He goes into tremendous depth, so I think of these as reference material more than books I would necessarily read straight through, but I always look to Dr. Rauschmayer's books when I need to understand some small detail. He leaves no stone unturned. His books cover all versions of JavaScript, so if you're wondering where JavaScript is going, look no further. Kyle Simpson has a series of books called "You Don't Know JS." They're short, very readable and excellent. Every book I've read has helped me to reach a better understanding. As of this recording, he's working on the second edition of the books, and you can read both the first and second editions for free here on his [[GitHub]] site. He also has a series of excellent JavaScript video courses. Now, a few other online resources. I think the Mozilla Developer Network has the finest online reference material on JavaScript and other web technologies like [[HTML]], CSS, and so on. There's good example code
>
> **[1:32](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/additional-helpful-resources?u=76281980&t=92)** for almost everything you might want to know about. "Can I Use" is a compendium of information on browser support for various features of JavaScript and other web technologies. When you're learning newer versions of JavaScript, it's helpful to have a place to look for whether that thing you're learning can actually be used in the wild. Similarly, [quirksmode.org](https://quirksmode.org) has been around for a very long time in internet years. It remains a great place to research implementation differences between browsers when we discover something doesn't work exactly as expected. As you're working through this course or afterward, I'd encourage you to bookmark and refer to any and all of these resources.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (8), [[GitHub]] (1), [[HTML]] (1)
> **Env Vars:** html (1), css (1)
> **CLI Commands:** find (1)
> **URLs:** [quirksmode.org](https://quirksmode.org) (1)
> **Tools:** github (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 2. Variables and Types

[↑ Back to Table of Contents](#table-of-contents)

#### [Declaring and assigning variables](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/declaring-and-assigning-variables?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/declaring-and-assigning-variables?u=76281980&t=0)** - [Instructor] In this video, we're going to introduce variables and how to make them. I have the chorus code space loaded up here and we're going to open a terminal to serve as our [[JavaScript]] playground. So in the Menu, I'm going to go to Terminal and then start a New Terminal and from there I'm going to run Node. Great. If you prefer, you can also use a browser console. So here in Chrome, I can load up the JavaScript console. This one's pretty big, and you can drag this around. So basically the console takes up the whole page if you want. And you'll have plenty of room to play with the JavaScript right here on this little prompt. Now, a variable, as you may have heard in math class, is a place to keep things that are unknown. That's one way of looking at it. For our purposes, you could think of a variable as a box with a label on it. We can label the box whatever we want and put pretty much anything we want into the box and we end up with a convenient way to refer to some data. Remember that as a programmer, you are in charge and the computer is going to do what you tell it to do. Nothing more, nothing less. You should assume that the computer basically doesn't know anything unless you tell it something. And variables are an important part of how we do this. In the course of programming, we're dealing with a lot of unknowns, or at least things that are unknown at first. We then use variables to accumulate the knowledge we need to accomplish whatever the goals of our program are. So here's an example. Say we want to write a function that's going to let someone draw a line on screen.
>
> **[1:33](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/declaring-and-assigning-variables?u=76281980&t=93)** Some of the things we don't know at first are the points where the line will start and where it will end. We could have variables for these points and refer to them later by name when it comes time to draw the line. So anyway, that's the theory. Here's how it works. If we were approaching this the way you'd approach it in math class, you'd have variables called things like x. In JavaScript, the way to declare a variable, is to type "var" and then the name. So in this case, that will be "x". That's enough to create a variable but it's just an empty box at this point. To give the variable "x" a value, we would add a space and then an equal sign, and then we give it the value. So let's give it "32" and we end the line with a semicolon. Strictly speaking, this is optional, but it's a good idea and I recommend ending every line with a semicolon. There may be times in this course where it's left out, but that's just to save a little typing in the examples. What we've done here is called assigning a value to a variable. So now I hit Return and what I've just typed is executed. Nothing really happens here except the assignment. So I see undefined returned. Undefined is JavaScript's way of representing something that doesn't exist. You'll see it often. But if I type the name of the variable I just created and do nothing except end the line with a semicolon, I get back the value that I assigned. So I know it really works. And you can also see that Node is interpreting this for me and showing the value even before I hit Return. But if I do, there it is, "32".
>
> **[3:08](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/declaring-and-assigning-variables?u=76281980&t=188)** Calling things "x" is something that gets a lot of complaints in math class and we can certainly do better in our programs. Variables can be named almost anything that starts with a letter, an underscore or a dollar sign. I can create a variable called "whereAmI" and set it to "Santa Barbara, CA". The part in quotes is a string which we'll discuss in a little more detail later but it's how you store things like words or sentences. Now that I've stored a couple of variables, I can see their values. So if I start to type, "whereAmI", I can see the value there. Or if I start to type "x", I can see that. The JavaScript console now knows about those variables and will start to complete the names for me which is pretty useful. I can also keep the label, but change what's in the box. If I leave off the "var" and just do the assignment and the equal sign, I can change "x" to "45" and I can change "whereAmI" to "Los Angeles, CA".
>
> **[4:17](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/declaring-and-assigning-variables?u=76281980&t=257)** I could also change "whereAmI" to "75". That doesn't make any sense but JavaScript doesn't care what I assign. We'll have a short digression on what that means a little later in the course. Meantime, I can also assign several variables at once. So I'll create a few monster variables here, "monster1" can be "Grover". I'll separate these by commas, "monster2" can be "Cookie Monster" and "monster3" can be "Animal". This just saves me a little typing when I need to create several at once. But now if I type each variable's name and hit Return, I can see their values. A couple more details. Your variables can be named almost anything you want and I recommend you use names that are descriptive because programs aren't just for computers, they're for other humans to read as well. The computer isn't going to care what you call your variables for the most part but humans definitely will. So make sure you name variables something that indicates the sort of thing that they're actually going to contain. If I were writing a program dealing with how much it costs to ship a package, I might have variable names for things like "packageDimensions" or "taxRate" and so on.
>
> **[5:41](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/declaring-and-assigning-variables?u=76281980&t=341)** Starting a name with a lowercase letter and mashing on additional words with capital letters like that is called CamelCase and it's a pretty common way to name things in JavaScript. There are a few words you can't use for your variable names because they're part of JavaScript itself. They're called Reserved Words. From what we've seen so far, you wouldn't be able to use "var" as a variable name because JavaScript already claimed it. If you're curious, there is a list of reserved words available on the Mozilla JavaScript reference site, and of course, if you're using a nice code editor, it will highlight those words for you so you won't accidentally use them. So that's an overview of variables which are the way that we give names to data. If we're thinking of JavaScript like a spoken language, they're sort of like nouns.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (12)
> **Code Identifiers:** whereami (4), packagedimensions (1), taxrate (1)
> **CLI Commands:** make (3), node (2)
> **Definitions:** is a  (3), is called (2)
> **Tools:** terminal (3)
> **Cross-References:** later in (1)
> **UI Navigation:** in the menu (1)
> **Speakers:** - [instructor] (1)

#### [Strings](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/strings?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/strings?u=76281980&t=0)** - [Instructor] In this video, we're going to look at strings. A string is character data. That means things like sentences, words, single letters, even numbers or punctuation. Basically, it means anything between quote marks. So for example, I could write, "this is a string". When you formulate a string like this, it's actually called a string literal. Literal here basically means you're making data of some type using the simplest notation possible. So making a string literal means I'm putting string data in quotes. There are other less convenient ways to make strings as well, but this is what we use 99% of the time. A string doesn't have to be a sentence. It can be a number inside quotes. So, if it's a string of 1, 2 instead of 12, that's a string. These are two different things as far as [[JavaScript]] is concerned. You can make strings using these double quote characters. You can also make strings using single quote characters. Like 'this is also a string'. Whichever you use, you need to begin and end the string using the same character. So I can't do this, This is a string, with a single quote at the beginning and a double quote at the end. If I try to execute that, I get a syntax error. Sometimes you're going to need to write a string that will have a quotation mark inside it. This comes up a lot actually, if you're working with [[HTML]] in JavaScript. So if you're writing a piece of HTML like this,
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/strings?u=76281980&t=94)** this would be an anchor tag. It works because I put the single quote at the beginning and the end with the attribute, in this case, the href attribute as a double quote. This is one of the many times where syntax highlighting would be helpful, so, let's open a file in the editor. Here is the transcript file that's associated with this particular video. So here's what I've typed so far, and here are some other bits. The color scheme that I'm running here shows strings in a sort of salmon color, so I can see whether a string is enclosed correctly. If it is, everything stays the salmon color, and if not it'll end up turning white, or some other non-string color. The double quotes with the single quotes inside works fine and the opposite works as well. So in this case, I have a single quote here inside the string, but because there are double quotes on the outside, it works great. You will at some point need to include quote marks of both kinds inside a string, like this, for example, here's Joe's favorite string. I have both a single quote mark here and double quotes here. So what JavaScript sees, we can see this based on the coloring, is a string, followed by something that is unknown, and is going to make JavaScript mad, and then another string. To fix this, so it's all one string,
>
> **[3:08](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/strings?u=76281980&t=188)** I need to use a backslash character like we see here. This is what's called escaping the string. For each quote mark that's causing trouble, I can add that backslash. Once I've done that, this is formatted correctly and will execute properly Just to prove it works, let's try this down in the console. I'll copy and paste this and you can see that works. Different environments will give you this result in different ways. Node is showing me this version enclosed in back ticks, you may see it with other kinds of variations on the quotation marks, as well. Back slashes let us do one more helpful thing, break strings up into multiple lines like you can see here. If we want to write a string across several lines we can do it that way, making sure that the only thing that comes after the back slash is a return, and then end the string with that final quotation mark that matches the first one. For something this short, it doesn't really matter, but when you need to edit long runs of text, this can be helpful. And that is a quick introduction to strings.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (4), [[HTML]] (2)
> **Definitions:** is a  (5), basically means (1)
> **CLI Commands:** make (4), node (1)
> **Env Vars:** html (2)
> **Analogies:** for example (2)
> **Speakers:** - [instructor] (1)

#### [String properties and methods](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/string-properties-and-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/string-properties-and-methods?u=76281980&t=0)** - [Instructor] In this video, we're going to discover some things that [[JavaScript]] knows about strings and some tools we can use with them. Along with the data that we've put in quote marks, strings have a few other tricks up their sleeve. We can access what are called properties and functions, also called methods on any string. They're like a toolbox included with every string which we can open up and use. A property is a single piece of data, like the length of a string, and a method is a bit of code to do something more involved, like convert a string to uppercase. I can assign a string to a variable. So let's create one called myString. This is my string, leave it alone. Now, if I take myString and add a dot, then hit tab and note, I need to hit tab twice. I can see all these methods and properties that are available. I can't tell which are the properties and which are the methods, but the Mozilla documentation can tell you and you'll get used to it as you use JavaScript more. Length is a number, how many characters are in the string. To uppercase is a method that does just what you'd think. In that case, you would type its name and add parenthesis on the end and hit return. And that shows me the string where everything is converted to uppercase. Interestingly, you can do this without variables, too. JavaScript can take a string literal, I'll add one. This is my string.
>
> **[1:35](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/string-properties-and-methods?u=76281980&t=95)** Then I can add the dot and type length and get what the length of that string is, as well. It's much more common to do this with variables, but you'll see this usage sometimes, as well. We've talked about building strings with basic literals, but there's one more handy way to build a string called a template literal. Instead of using single or double quotes, these are delineated by back ticks, which is found in the upper left-hand corner of US keyboards. Looks like that. With a template literal, you don't have to worry about single or double quotes inside your strings, which is nice. So let me create a variable called declaration. And we'll say, this I say to you, good morning, huzzah. Now if I type the name of the variable again, I can see right there that it worked. But what's even nicer is that you can pretty easily build a string from other strings. So I already have a variable called myString and I'm going to redeclare my declaration. Inside the back ticks I will say, This I say to you. I'll add my double quotes, but now I'm going to insert a special token which is a dollar sign, and then an open and closed curly brace with the name of the variable inside. Everything else will be the same. And so now declaration says, This I say to you, and then the value of my string has been inserted.
>
> **[3:14](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/string-properties-and-methods?u=76281980&t=194)** So template literals are a very handy way to build strings. It's a relatively new feature, but it works great in all browsers. All of JavaScript's built-in data types have these properties and methods and string has many more than we covered here, but now you have an idea of what's available.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (4)
> **Definitions:** is a  (5)
> **Code Identifiers:** mystring (3)
> **Exercise Files:** template (3)
> **Cross-References:** we covered (1)
> **Speakers:** - [instructor] (1)

#### [Numbers](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/numbers?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/numbers?u=76281980&t=0)** - [Instructor] In this video, we're going to talk about another of the basic data types in [[JavaScript]], the number, and we're also going to talk a little bit about math. You know what a number is? 12 is a number. Some programming languages have different types of numbers like integers, floats, doubles, and so on. All numbers in JavaScript are of the same type, called number. So 12 in JavaScript is the same thing as 12.0, which makes sense in terms of how we deal with numbers in the real world. In programming, that's not always the case, but in JavaScript it is. They're both the same thing, and you can have numbers with lots and lots of decimal points. So, if I add a whole bunch more here, it gets truncated a little bit. So you can see, even though I put in a whole bunch of extra digits after that decimal point, JavaScript can't handle that much precision. You probably won't care about that most of the time unless you're getting into some pretty serious math. Along with regular numbers, you can of course have negative numbers, and all that sort of thing. JavaScript can also represent infinity if you need it to. It's spelled just like this, and it is case sensitive, so it starts with that capital I. Likewise, of course, you can have negative infinity. In JavaScript infinity basically means any number that's larger than a very, very large upper bound. Odds are you won't run into it, but there is such a thing in JavaScript as infinity and negative infinity. There's also this: NaN. Capitalized exactly this way,
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/numbers?u=76281980&t=94)** which stands for not a number, and this is what you get when you try to do math and the result, perhaps because of an error in your code, or for various other reasons, ends up yielding something which is not a number. Be on the lookout for this, if you're trying to do any kind of math and you start getting unexpected behaviors. If I assign a number to a variable, let's call this one, myNumber, you can see that there are methods available on numbers just as there are with strings. So I type myNumber and a dot, hit tab a couple times, I can see some methods here, and some properties. I don't find myself using these all that often but they are available. More interesting than the methods the numbers have is the built-in object which is called Math. We'll talk about objects later, but this is sort of like another toolbox with functions or methods for doing things to numbers like rounding, chopping off decimal points, taking square roots and so on. So if I type Math, capitalize just like this, and then the dot, I'll type tab a couple of times to open up that toolbox and all these methods are shown. Next, I would add a method name and I can tell the method what number to act upon by enclosing it in parentheses. So let's try Math.round and put a number in here with a whole bunch of decimal points. That rounds the number down to 12. Here's another little snippet, and this time the number gets rounded up because it's 12.9
>
> **[3:09](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/numbers?u=76281980&t=189)** which will go up instead of down. Math has a lot of useful functions for working with numbers, including one for generating a random number. It's actually technically pseudo random but it's good enough for us. Between zero and one. So let's see what that looks like. I'll do this a few times. You can see that the number changes. So it's a different result every time which is handy if you need some randomness in your program simulating the role of a dye or something like that. This is not nearly everything that pertains to numbers in math in JavaScript, but now we know a little bit about them, and can recognize them in code. We'll see a little bit more later in this course, and you can also refer to the documentation on the Mozilla Developer network for more.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (9)
> **Definitions:** is a  (1), basically means (1), stands for (1), is called (1)
> **Code Identifiers:** mynumber (2)
> **Versions:** 12.0 (1), 12.9 (1)
> **Analogies:** just like (2)
> **CLI Commands:** find (1)
> **Cross-References:** later in (1)
> **Documentation:** the documentation (1)

#### [Booleans for what is true](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/booleans-for-what-is-true?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/booleans-for-what-is-true?u=76281980&t=0)** - [Instructor] In this video, we're going to talk about booleans, that is true and false and why booleans matter to us. You may know from the way people talk about computers, sort of colloquially, that computers deal with ones and zeros, on and off, true and false, that sort of dichotomy. Boolean is a data type that can capture true or false for us. In [[JavaScript]], a boolean is either true, spelled this way, or false, spelled this way. They need to be spelled exactly like this, all lowercase, and of course, spelled correctly in order for JavaScript to know that you mean true and false. If I write true with a capital T, JavaScript is going to think you mean a variable by that name and I can see I get a reference error when I try that. Incidentally, because it's so similar to that existing keyword, you would do really well to avoid naming any variable true like this. Likewise, all caps or any other alternate spelling, none of those will work. Only fully lowercase. One quick note on terminology. Boolean is three syllables and maybe for this reason, booleans are often also called flags. So instead of saying, I have a boolean called is enabled, I might say, I have a flag called is enabled. So if you hear the term flag, it just means a boolean. I can set variables to true or false and by doing so, I would say that this variable is a boolean. So I could create a variable called button has been clicked, and set it to false.
>
> **[1:40](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/booleans-for-what-is-true?u=76281980&t=100)** This might track whether someone has clicked a particular button on a page and started out as set to false and then sometime later, when the button actually is clicked, set it to true and act on that information somehow. Booleans are important because they're your way of getting answers from your programs about the state of your program's world. What I mean is you can ask questions in the form of is this true or is it false? It's one of the questions that JavaScript can answer for you without you having to do a bunch of extra work. Questions like this, where you ask something in code, get back the answers are very often in the form of comparisons. We'll be looking at those in more detail a little later, but just to give you a preview, it'll be something like this. I'll make two variables. And I'll say, my first variable is my location. I'll set this to Santa Barbara. And then I'll set my other location to Los Angeles, and now I can ask a question of JavaScript. Are these two variables the same? And the way that you ask that question is by writing a statement, and adding three equal signs. This will tell me, are these two things exactly the same? So I start to type my other location and I can use the right arrow to complete it without having to type the whole thing. And of course, JavaScript returns false. It knows the values of the two variables and it can tell me whether they are the same. I defined them right next to each other myself, so this is not all that exciting really.
>
> **[3:15](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/booleans-for-what-is-true?u=76281980&t=195)** But if there's a lot of other code in between the definition and the comparison where the values might change at some point, this could be a very useful question to get an answer to. So now if I change my other location to Santa Barbara, like that, and I ask the same question again, and in this case, instead of retyping it, I'll use the up arrow a couple of times to bring back that code that I wrote previously, I can hit return and I get back true. We're going to look at comparisons later. But one thing to note for now, variables are assigned with one equal sign. Comparisons use more than one. This is a very popular typo that introduces fun little bugs for every programmer, regardless of level of experience. So that's a look at booleans, true and false. Very useful for answering questions in your programs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (6)
> **Definitions:** is a  (3)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Variable mutability](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/variable-mutability?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/variable-mutability?u=76281980&t=0)** - [Instructor] Before we move on from variables, I want to take a moment to look at other ways that variables can be declared. The variables that we've seen so far have been changeable or mutable as it's hugely described in programming. Declaring a variable with VAR yields a variable whose value can be changed later. But there are times in our code where we'd like to have variables that can be set once, but never changed later. If we want to represent facts, like the birthdays of family members or how many items are in a dozen, you wouldn't want those facts to be changed later accidentally. In situations like this, we want to create constants. A constant is an immutable variable. In modern [[JavaScript]], the keyword const can do this. If I'm going to write some code to simulate a bakery, I could set a few variables for common quantities like dozen, half dozen and bakers dozen. Creating these constants, I can use them just like any other variable, which can make my code more readable than having to type six, 12, and 13 all over the place. So I can see that here's dozen and then here's half dozen and bakers dozen. And most importantly, if I try to change the value of any of these constants, I get an error. So let's say I wanted to set dozen to baker's dozen or 13. I get an error. Errors like this can help programmers avoid mistakes. We always prefer our programs to yield an error
>
> **[1:36](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/variable-mutability?u=76281980&t=96)** than to just continue quietly on with hidden mistakes that could bite us later on. Along with VAR, I can create mutable, that is, changeable variables using the keyword let. So at a basic level, the following are equivalent. If I'm going to create a variable called cookie count and set this to five, this would work, but also let would work. Doing this, I can change cookie count to something else and it works just fine. Cookie count is now six. To this point, we've seen three keywords that can be used to create variables, VAR, let and const. There are some subtleties beyond mutability that we'll talk about later in a discussion on scope, but as we're continuing to build our vocabulary, this is a good place to leave off.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (1)
> **Env Vars:** var (3)
> **Definitions:** is an  (1), is a  (1)
> **CLI Commands:** make (1)
> **Cross-References:** later in (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)


### 3. Objects, Arrays, and More

[↑ Back to Table of Contents](#table-of-contents)

#### [Objects](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/objects?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/objects?u=76281980&t=0)** - [Instructor] So far, we've looked at some of the simplest types of data [[JavaScript]] knows about. In this video, we're going to look at objects which are a little more complex but completely foundational and super useful. A lot of what we're doing in the code we write could be viewed as teaching the machine to do things for us. JavaScript comes with a lot of simple data types built in. We've seen numbers, we've seen strings, we've seen Boolean .
>
> **[0:29](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/objects?u=76281980&t=29)** If we want to represent things that are more complex, that's where objects come in. Remember that we're assuming the scripts that we're writing know nothing unless we give them something to know. Figuring out what knowledge our programs need and in what form to represent that knowledge is a key decision. Objects are the main way in JavaScript and various other languages to encapsulate that knowledge in ways that are more complex than simpler data types allow on their own. The simplest way to make an object is with an object literal, similar to the string literals we saw before. Just type a curly brace and another one that closes it. That's an object. Now, to be able to inspect what we've done, we need to actually assign this to a variable. So let's say empty object equals an empty object. Now, if I look at the value of this empty object, I can see that JavaScript knows it's an object but it has nothing in it. Technically, it actually does have some properties but that's an implementation detail that we're not going to look at right now. For all intents and purposes, this is an object with nothing in it. To be really useful, of course, an object needs to have stuff. You can think of an object sort of like a box with labeled folders. Every useful object has a series of labels known as keys and values which are generally the data that we care about that are all grouped together in the object. Here's what this would look like with actual data. I'm going to assign another variable.
>
> **[2:02](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/objects?u=76281980&t=122)** We'll call this not empty object. And when I say it's like a filing system, it has labels. Now here in Node, I can just hit Return, and Node adds the three dots here, knowing that I'm continuing to write the same thing without executing it. The label that I'm assigning is a string. And when you're working in JavaScript you'll see these labels with quote marks or without. It's safest to use quote marks. So you don't accidentally use a JavaScript reserved [[Microsoft Word|word]] for a key, but because it's fewer keystrokes not to use them you'll see that a lot too. So anyway, we'll put in the name of the key which I'll use label in this case, then a colon, and then the value that that should contain, we'll just use the string value here. That value can be any type of data that JavaScript knows about. It could be numbers, strings, Boolean , even other objects, anything. Now that I've done that, I'll add my closing curly brace. I can hit Return and now I'm done. Let's check that value of not empty object. There it is. I have my label, and my value. To add more folders to your box, you can do the same thing. I'll just redo this, and now if I add a comma, I can add more labels. So here's my second one, and I will add the quotation marks this time. So the value of this one can be value two.
>
> **[3:39](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/objects?u=76281980&t=219)** And now having done so, I've redefined this variable And it has two properties and their values. And you can do this for as many items as you need in the object. The last value can have a comma on the end that used to cause issues in older versions of JavaScript, but it doesn't anymore. And so now I can type the variable name and get the whole thing printed out as I've done here. Objects are very simple to create, but you can make some very sophisticated [[Data Structures]] using nothing but these objects.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (8), [[Microsoft Word|Word]] (1), [[Data Structures]] (1)
> **CLI Commands:** make (2), node (2)
> **Definitions:** is a  (2), is an  (1), known as (1)
> **Analogies:** similar to (1), it's like (1)
> **Speakers:** - [instructor] (1)

#### [Using objects for data modeling](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/using-objects-for-data-modeling?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/using-objects-for-data-modeling?u=76281980&t=0)** - [Instructor] In this video we'll look at some objects that are a little more substantial to get a sense of how we can use them to do [[Data Modeling]]. I'm here in the code editor looking at a couple of objects. These show you some ways to model data by which I mean representing knowledge as code. These are still very simple and we're still talking at a somewhat abstract level because the requirements of any given program are going to be up to you in your own work. But these will at least start to give you an idea. I've used these objects to represent two animals, a bird and a bear. We'll look at the bird first. What pieces of data I choose to include are totally up to me and because I don't yet have an actual program to write I just picked a few for fun. So I have a genus, a species, a commonName, this bird is a raven. It's callType is squawky. It has an associated quote, a maximum number of offspring, and whether it's noisy and deadly. These last couple are Booleans, which remember are also called flags sometimes. So let's try one of them in the console. I will select all of this and copy it. I'm actually going to move my Terminal panel over to the right. Since this is a little long this will let us see a little more. So I'll paste this into the Terminal and I do, in fact, want to paste all this. There we go. Great. So now I can see everything over here. And now here's my second animal, a bear.
>
> **[1:35](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/using-objects-for-data-modeling?u=76281980&t=95)** All the key names in this bear object are the same but they store different data. This is something you can expect to do often where you create objects that have a similar structure but different data contained inside. With this idea, you're going to start to edge into the territory of [[Object-Oriented Programming (OOP)|object-oriented programming]], but we'll talk about that just a little bit later. These animal objects map onto things in the real world but you can make objects of anything you need, concrete or abstract. Down here, I've made a sort of book of knowledge. I can refer to it to find out things like, when lunchtime is, what the ultimate answer to life, the universe and everything is, an idea of the best song and what earth might be considered. As you can see, what you actually do with objects is going to vary a lot depending on the kind of program that you're writing. But objects are a very powerful way, despite their simplicity, of representing data and making sure that the computer understands the things that you need it to.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Data Modeling]] (1), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (1)
> **CLI Commands:** make (1), find (1)
> **Code Identifiers:** commonname (1), calltype (1)
> **Tools:** terminal (2)
> **Definitions:** is a  (2)
> **Speakers:** - [instructor] (1)

#### [Manipulating objects](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/manipulating-objects?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/manipulating-objects?u=76281980&t=0)** - [Instructor] In this video we're going to look at objects again, and see some of the things we can actually do with and to them. So I have my bird object here with a bunch of properties. If I want to inspect it and find out what one of those properties is, first I need to bring it into my terminal. Paste this in. If you happen to get an error when you do this, if you're following along and you've already brought this bird object in, you can use the .exit command to quit out of node and then just restart it again here in the terminal. So anyway, if I want to inspect this object and find out what one of its properties is, I'll start with the variable name. And then if I want to find out, say what the quote is, I'll just write out quote after the dot. And I get back Nevermore. Now remember, I said each of these keys is a string. And when you're creating the objects, to be safe we can surround each key with quotation marks, single or double. But when I'm retrieving the value from an object using this dot notation, I don't need those quotes. And indeed, if I try to use them like this, I'm going to get a syntax error. But there's another syntax that I can use, where I actually do use the full string, which looks like this. I've got square braces, and then inside I can type out the name as a string. And that does work. If a key that I'm trying to access
>
> **[1:32](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/manipulating-objects?u=76281980&t=92)** includes special characters like spaces, you have to use this other notation. It's exactly the same thing, an accessor for the property of the object, it's just a different syntax. So that's how we get things out of the object. And let's say I wanted to modify this bird object and add some other information to it. The same syntax that you use to get things out, you can use to put things back in using assignment the same way that we assign variables. So if I wanted to give this bird a color, I can use dot notation like this without quote marks. And I can set this to black. And now if I inspect the bird object, down here at the bottom I can see there's the color set to black. Along with all the other properties that I had before. And if I want to use that same string syntax that will also work, like this. And it's mandatory if I wanted to use a key with spaces or other characters. So let's say, for example, I wanted to say where the bird lives. So I could add a key called where it lives and set that to in a tree. And if I wanted to do this I have to use that square braces notation. Another way I could do this without having to use the square braces notation would be to phrase my keys differently. So in this case, if I wanted to say where it lives, I could use camel case notation, where the first letter is lowercase
>
> **[3:04](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/manipulating-objects?u=76281980&t=184)** and each subsequent letter of each [[Microsoft Word|word]] that's jammed together is uppercase. This isn't required by any means, it's just a common way of writing variables and properties in [[JavaScript]]. And again, I can set this to in a tree. And just as I used those two kinds of syntax to get the properties into the object, I can use the same thing to get them both out.
>
> **[3:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/manipulating-objects?u=76281980&t=214)** So that's how you add things to an object. If I want to remove a property from an object entirely, I can also do that. The way to do that is the delete keyword. So I would type delete bird.color to remove that color property from this bird object. The return value from delete is true, but now if I look at the bird object again, the two versions of where it lives that I added previously are there, but the color property is totally gone. And delete works with either notation. Finally, I'll just note that whether an object starts empty or not, all this adding and deleting business works the same. So that's an overview of some of the manipulations that we can perform on objects. Retrieving things from them, adding things to them, and deleting things from them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (1), [[JavaScript]] (1)
> **CLI Commands:** find (3), node (1)
> **Tools:** terminal (2)
> **Definitions:** is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Jargon: References](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/jargon-references?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/jargon-references?u=76281980&t=0)** - [Instructor] When working with [[JavaScript]] objects, there's an important concept called references. Objects are references to particular locations in the computer's memory and that impacts your ability to work with them in different contexts. So let's create an animal object one more time and check its value. I'm going to copy everything from the start of this file into Node, and now when I check the value, I can see everything looks right. Now I'm going to create another variable called animal2 and assign animal to it. If I inspect animal2, it's all the same stuff. Now the question here is, what have I actually done when I executed that assignment statement? Did I make a copy of the animal object? As we will see the answer turns out to be no. To demonstrate this, let's change animal2's genus to ursus, a bear. So now when I inspect animal2, I see that the genus has been set to ursus, which is what I expect, but if I check the original animal, it has also been changed. What we've exposed here is references. That assignment statement did not actually make a copy of the object. This was actually saying, assign this animal2 location in memory to the same location in memory as the original variable. So these two variables reference the same memory
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/jargon-references?u=76281980&t=94)** and really are the same thing. This is an important point that affects all types of JavaScript objects, both the ones that we're looking at now and others that are based on objects internally, like arrays, maps, and sets. That assignment does not make a copy of the object. It just gives us two variables that point to the same value, the exact same value. So if I want animal2 to be a copy of all the same stuff, one way to do that is to execute the whole original data blob a second time, but assign it to animal2 this time. So if I copy and paste all this and now I check animal2. Right here, I can see that it's genus is corvis, and animal one is still set to ursus. As an aside, creating a complete accurate copy of an object is a little bit tricky in JavaScript. There are a few ways that you might see this done in the wild, and I have a few a little further down in this file. The first is using the assigned method on the global object Object. As used here, we're copying every property from the original animal object into an empty object and assigning that new copy to animal2. As with most things in programming, there are subtleties that you'll run into if you work with JavaScript for a while, but that's a good start. You might see the same line written like this, with the three dots.
>
> **[3:07](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/jargon-references?u=76281980&t=187)** This is called the spread operator. Those three dots are called the spread operator in [[ECMAScript]] 2015, and it basically means, spread out all the data from this object, in this case into a new copy. These two lines are basically equivalent. These three dots are used in other ways in JavaScript as well, which is a bit confusing at first, but you'll get used to it with time, experience, and probably many trips to your favorite search engine. Finally, there's this method which uses the built-in [[JSON]] object and we're taking that animal object, turning it into a JSON representation that's a string, and then parsing it again into a new object from that original JSON. JSON, if you haven't heard of it before, stands for JavaScript Object Notation and it's a way to represent data in a way that looks like JavaScript, but is very portable and represented as plain text. If your object is made of data that can be represented as JSON, this is a good method to use and you'll likely see it in code out in the world. Anyway, once that type of copying is done, I can change animal2 however I want and animal one will not be affected. The takeaway here is to keep references in mind when you're working with objects. When you assign an existing object to a new variable, you're not making a copy of it. You're reusing a reference to the same value, the same location in the computer's memory. If you want a copy, you need to do a little extra work to make one.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (7), [[JSON]] (5), [[ECMAScript]] (1)
> **Definitions:** is a  (3), is an  (1), is called (1), basically means (1), stands for (1)
> **CLI Commands:** make (4), node (1)
> **Env Vars:** json (5)
> **Speakers:** - [instructor] (1)

#### [Arrays](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arrays?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arrays?u=76281980&t=0)** - [Instructor] In this video we're going to talk about arrays which are a way of storing lists of data that preserve their order. We've already looked at objects which let you group data together, but there are some cases where you want to store say a list of things, and the order of those things actually matters. For example, let's say you want it to store a list of the days of the week, which of course appear in a certain order, or the months of the year, something like that. In cases like those, you'll want to use an array. So how do we make one? Let's create a variable and call it myArray. And instead of using curly braces, as we did with objects, we use square braces like this. That is the form of an array literal. And so now if I check the value of my array, I get back those same empty square braces. If I wanted to create a list of days of the week, I'll just add those two square braces again and now I'll fill it up with some of the days of the week. I'll just add a few in here. Certainly don't need to do everything. As you can see, unlike with an object where the order of the keys isn't guaranteed, an array does preserve the order that you put things in. And there are no keys here, only the values. Strictly speaking, that's not true. There are keys, they're just not being displayed because this is an array. Under the hood, an array in [[JavaScript]]
>
> **[1:33](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arrays?u=76281980&t=93)** is actually an object that gets special treatment. This is not the case in every programming language. In many others, there are objects and there are arrays, and they're totally different. In JavaScript, arrays are based on objects internally but arrays do have a few special qualities. One is that the order is preserved and another is that the keys are automatically assigned, and they're assigned as numbers starting with zero. So even if I don't care about the order, I can fill an array with a list of stuff and just not have to think about naming a key for every stored value. And again, you can create arrays of any kind of data. Let's create a new one, call it myList, and fill it up with some things. Let me actually open the file for this video to save a little typing. I can get it from the File Explorer over here or this is the way I like to do it in Visual Studio Code, I can use the search file by name command. The default keyboard shortcut for this is Command + P on macOS or Control + P anywhere else. And so I can start to type the video chapter name and that brings it up for me. I'll scroll down to myList and I'll just copy the [[Representational State Transfer (REST)|rest]] of this expression down here. So we have some numbers, some strings, a couple of Booleans. Now I can check out myList and see all those same values in the same order.
>
> **[3:09](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arrays?u=76281980&t=189)** Arrays are often used for lists, so if, for example, you wanted to store the counties for some area because you're making a program that needs some kind of geographic knowledge, arrays can be a good fit. We'll save some time with a little copy and paste again. So here are a few counties. Now my program is aware of this list of counties. You can put them in any order you want. In the case of something like this, the order might not for the purposes of your program, make any difference, but if it did make a difference, you'd have already guaranteed that they'd appear in this order. Let's make one more new one. We'll call it list of stuff here. I will not paste, I will copy and bring it down here. And so here's the thing. Arrays can contain objects. So I made an object here as the first item. You can even have another array inside the array. So here's a list of stuff and there are all these different types of items. You can nest arrays inside other arrays as deeply as you care to. The point here is that you can store anything in arrays the same way you can with objects. Arrays in JavaScript are really just specially treated objects after all. The main limits are your needs and your imagination. There's one more thing I want to point out about arrays that's different from regular objects which is that an array knows how many items are in it. Just as strings know how long they are, an array also knows how long it is.
>
> **[4:45](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arrays?u=76281980&t=285)** So if I want to see how many things are in my list of stuff, I can use the dot notation and get its length property. All arrays have that length property. I can see this one is four items long. In other words, it has four items inside it. That doesn't account for anything that's nested, it's only how many objects are at the root level of this array of stuff. There will be some occasions in your programming where you're going to want to use arrays and there will be some times where you'll want to use objects and you'll want to combine them in various interesting ways. It all depends on the requirements of whatever you're writing at the time. Right now we're just going through syntax so you can know some of the tools that are available to you in the JavaScript toolbox. And that was a look at arrays, which can store lists of anything you want preserving the order that you put them in.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (4), [[Representational State Transfer (REST)|Rest]] (1)
> **Code Identifiers:** mylist (3), myarray (1), macos (1)
> **CLI Commands:** make (4)
> **UI Navigation:** open the (1), scroll down (1)
> **Definitions:** is an  (1), in other words (1)
> **Analogies:** for example (2)
> **Tools:** visual studio (1)
> **Speakers:** - [instructor] (1)

#### [Manipulating arrays](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/manipulating-arrays?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/manipulating-arrays?u=76281980&t=0)** - [Instructor] In this video, we're going to get into manipulating arrays, how we can see what's in an array, and how we can take action on the contents of that array. We'll be using this list of counties with just a few counties in it for demonstration purposes. If you haven't already pasted this into Node in the previous video, you can do so now. I already have it here. I'll just confirm that it has a value. If I want to get a specific item from an array, I need to retrieve it using the square braces notation we saw with objects. Although arrays are technically objects, the dot notation will not work, only square braces. So if I wanted to get the first item, I need to access it using the square braces notation and its key which is a number starting with zero. Always remember that arrays are what we call zero indexed. Each item in an array is assigned an index. Same as the key in an object, but with arrays we usually call it an index and those indices start with zero always. So just to be 100% clear on this, the first item is zero and any other subsequent item would be retrieved by its index starting at zero. So this is the first item and then we'd have one, two, and three. So if I want to get the third item, I need to use index two. The way this is often read out loud where two here is the index is counties sub two. This index might also be called a subscript.
>
> **[1:36](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/manipulating-arrays?u=76281980&t=96)** We can also use this notation to modify the values in the array. So I can say that I want the second index in that array and I'm using the up arrow to bring back that last line. I want to assign that to Cheshire instead of Carroll. Now if I check the list of counties, I can see that here Cheshire has now replaced the original Carroll County. Enthusiasts of United States geography may have noted that these are counties in the state of New Hampshire in the United States. So that's how you can manipulate things that are already in there. Now I want to add this array and there's different ways that I can do that using the same notation. If I want to add something to the end of the array, I can just do it by hand counting to what the last index should be. So I know this should be zero, one, two, and three, so the next index would be four and I can add another county. I'll use the up arrow to pull back this line and I'll bring back Carroll County. Just make it the last item in the array this time. But in the context of programming, doing things this way is not always going to work. There's going to be a lot of unknowns, a lot of uncertainty at any point during the execution of your program. You're going to need to do things in a more abstract way. So if I want to stick something on the end of the array I can type out counties, the square braces, and now inside the square braces I can use an expression that will evaluate to a number.
>
> **[3:08](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/manipulating-arrays?u=76281980&t=188)** Here I can put the name of the array again, counties, and use its length property. So what this will do is evaluate to the length property of that array and then that will be the index that I'll use for setting the next value. So if I want to add another county here, I can assign this to Merrimack. And now if I look at counties again, you can see that without my having to figure out on my own what the last index was, I can just use that expression and stick Merrimack County right on the end of the list. There's another way that I can add something to the end of an array using the push method that every array knows about. I can write this out as counties.push. Add parenthesis, and then in those parentheses, I can add the name of whatever I want to push onto the array. In this case, I'm going to push Coos County onto my list and that is pronounced "Co-os", not "Coos." And I can just verify that Coos is now on my list of counties. So that's a few ways of pushing things on the end of an array and therefore adding to it. Now if I want to remove something from an array, I can use a similar method to push. Only in this case it's called pop and it doesn't take any parameters, and it just returns the last value. And if I check that counties array again, I can see that it has been modified. The last item is gone.
>
> **[4:42](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/manipulating-arrays?u=76281980&t=282)** So these methods of push and pop are often used in combination with arrays to grab something from wherever it is. We're talking in abstract terms I realize, but sometimes in your program you'll need to store some items and retrieve them later. You can push an item onto an array and then pull it back off later whenever you need it. For example, you could write a piece of code that's going to look through the entire content of a webpage and find all the images that are of a certain size. Every time you find one, you could add its URL to an array. You could push the image's URL onto the array every time you find one. Then later on in the program when you're ready to do something with all of them, you can pop them back off. Push and pop work on the end of an array and there are equivalent methods called shift and unshift that work similarly except they add things and remove things from the front of the array. I'm going to show you just one more of the ways that you can manipulate arrays. What if you want to delete something from the middle of the array? We've seen this with objects where we used delete to get rid of a particular item, but this doesn't work with arrays in the same way it does with regular objects. So for example, if I delete counties substitute sub two which is the third item in the array and then look at that counties array, it's still the same length. It's just that the item that used to be there has been replaced by what Node calls one empty item. In other environments you might see undefined. So the array has not changed in size.
>
> **[6:16](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/manipulating-arrays?u=76281980&t=376)** It's just that that one index has had its value changed. If I want to actually delete something from the middle of an array, I need to use a different method called splice which works like this. I'll write out counties.splice. I'll add parenthesis, and then it takes two parameters. The first one is the index in the array that I want to do the operation on. So if we want to get rid of this undefined empty item, I'll put in that same index of two. Then a comma, and the second parameter is how many items I want to get rid of. So in this case I'll use one 'cause I just want to get rid of one item and it returns an array containing that one item that was removed. So now let's take a look at that counties array again, and there we go. We've actually cut the array down so it's length, the number of items in it, is what we expect. This is an important thing to remember with arrays. The length always refers to the number of slots in the array, not whether the slots have something meaningful in them. For more on arrays, you can check out the Mozilla [[JavaScript]] documentation for a full list of what's available. But you've now had a taste of how to manipulate arrays, how to read what's in them, add to, and remove from them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (1)
> **CLI Commands:** find (3), node (2), make (1)
> **Definitions:** is a  (1), is an  (1), refers to (1)
> **Env Vars:** url (2)
> **Analogies:** for example (2)
> **Cross-References:** previous video (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Readability: Whitespace](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/readability-whitespace?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/readability-whitespace?u=76281980&t=0)** - [Instructor] In this video, we're going to talk about how to make your code more readable through the use of whitespace. In other words, spaces, returns, and so forth. I have at the top of this file a little block of code that assigns a few variables individually. There's pretty much no whitespace at all here, but it does work. If I copy and paste that into my node terminal here, I can see that all those variables have actually been assigned. It's just not that easy to read. So what we want to do is add a little whitespace, just a few spaces here and there. That whitespace is generally ignored by [[JavaScript]], but it does make the code much easier to read. Now you can go totally crazy, adding a whole bunch of space in between all of these pieces. Functionally, all of these are the same. But it's possible to go overboard like this and have so much space that the code gets hard to read in a different way. It is also valid to put these assignments on different lines as you see here. Sometimes, you'll even see code where people have really been careful about curating their whitespace, making sure that all the equal signs line up perfectly. This does look pretty nice, but it's a little tedious to do by hand. So if that's a style that you like, look for a plugin for your text editor that can help you do that. Here is an object literal that has also been written with no whitespace. You can pretty much read it for the most part because it's a pretty simple object. But if I were to add some whitespace to it like this,
>
> **[1:35](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/readability-whitespace?u=76281980&t=95)** it becomes much better. And I will copy this and paste it in just to prove that it does work. We saw previously that you can break strings across multiple lines using back slashes like this. That's another use of whitespace. When you do this, just make sure that your lines really end with back slashes. No spaces can sneak in. As you can see, the built-in code checker gets very angry when I have that extra space in there. Now, the way you set up your whitespace is something that programmers will, for lack of a better [[Microsoft Word|word]], fight over. There are elements of personal preference or if you're working on a team, there may be a coding standard everyone is expected to follow to make sure that everybody can read each other's code well and that the results are nice and uniform. There are even tools like Prettier that can take your code, however you've written it, and automatically add whitespace and do other formatting to make it readable. I've become a huge fan of Prettier and use it all the time in my JavaScript projects. In fact, the Prettier Extension for Visual Studio Code is built right into this code space. So anytime you save a file in these exercise files, it gets reformatted automatically. That doesn't happen on this particular file because there's a configuration dotfile, this prettierignore file down here, that lists file names that are not to be included in Prettier's processing. In any case, when you're starting out,
>
> **[3:09](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/readability-whitespace?u=76281980&t=189)** you might just be working on your own projects for yourself and the main issue is to keep your code readable for yourself. However carefully you want to do it, I recommend embracing the use of whitespace to make sure that your code is readable without affecting how it's executed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2), [[Microsoft Word|Word]] (1)
> **CLI Commands:** make (6), node (1)
> **Tools:** terminal (1), visual studio (1)
> **Definitions:** in other words (1), is an  (1)
> **Exercise Files:** exercise files (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Readability: Comments](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/readability-comments?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/readability-comments?u=76281980&t=0)** - [Instructor] In this video, we're going to talk about comments which are used for documenting and for disabling execution of parts of your code. The most important function for comments is documentation, that is, adding pieces of code that don't execute, but can be read by other human beings or by yourself to understand what's going on in your program. These come in two styles in [[JavaScript]], line comments and block comments. The first is the line comment. We can see that on the first line of this file, it starts with two slash characters and anything after those on the same line doesn't execute. These need to be immediately following one another. No spaces in between or anything like that. Just two slashes. And they can be at the beginning of the line or anywhere else. So in this case, I have one that's at the beginning of the line, and then this one down here is at the end of the line. This particular one will not be nominated for a documentation of the year. It's pretty clear what this assignment does, but at least you can see that it works. The other style is block comments. You can see that right here. This starts with a slash followed by an asterisk with no space in between, and then you can write as much as you want across multiple lines, and it finally needs to end with an asterisk followed by a slash. You'll see this all the time in JavaScript for inserting long paragraphs or just other elements where a lot of text is needed in one place, and maybe it would get a little bit tedious to add those double slashes in front of every single line. My text editor here makes it really obvious
>
> **[1:33](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/readability-comments?u=76281980&t=93)** what's contained in the comment thanks to syntax highlighting. If I remove this ending asterisk, and scroll down a little bit, you can see that the entire [[Representational State Transfer (REST)|rest]] of the file ends up becoming a comment, almost. You can see a little semicolon down here, which we'll get to in a second. But block comments are really useful and you'll see them all the time. You just need to be careful with these, because, every once in a while, there'll be something like this, which is not actually a comment piece, it's part of a regular expression, but because it uses the same characters if I've forgotten the ending token for that block comment, this will be interpreted as a comment. But thanks to syntax highlighting, I can see what's going on here and I can add that asterisk and slash back in to restore order to this file. So if you're commenting out a huge block of code, watch your syntax highlighting carefully to make sure that everything that you think is disabled really is. The other function of a JavaScript comment is just to disable parts. So on this tiny Almanac line, if I wanted to, say, disable the day, I can put a line comment here, and that will no longer execute. So I'm going to take this and copy it into my Node terminal down here, paste that in, and I can see that tiny Almanac has a year, a month, and a holiday, but no day, because that line did not execute. Indeed, if I tried to access that property,
>
> **[3:10](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/readability-comments?u=76281980&t=190)** I can see that it's undefined. You might also get a reference error in this case. So when you're working through your code and fixing things, being able to comment bits out like this so that only certain portions execute is really handy for debugging. Now let's take a look at a very large file. I'm going to close my terminal so we can see more here. This is jQuery, a very popular JavaScript library. This is the version of it that hasn't been optimized, so it's loaded with comments starting with this big block comment at the top, and then many line comments that follow. As you scroll down, you can see that nearly every block has comments to explain what's going on. This is very nice for people wanting to contribute to the project and for developers wanting to learn how it works. And of course, jQuery is a very large JavaScript library. There's a lot going on in this file. No amount of comments are going to make it perfectly clear the first time you read through it what's going on. You still have to do a lot of reading and actually try using the code to understand it. But having these comments in here is a huge, huge help. I really can't recommend enough making sure that even if you're the only person that you ever expect to read the code, that you add lots of comments, because it will always help you. It's just a little bit of extra effort and it can pay off huge if you set your code aside for a while. To be able to come back and have something to read other than the code itself
>
> **[4:43](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/readability-comments?u=76281980&t=283)** that tells you what your intentions were is really useful. So that's a look at comments in JavaScript and what they do, which is allowing you to document your code for your own readability and to disable portions of it, which can be helpful in debugging.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (6), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** make (2), node (1)
> **Code Identifiers:** jquery (2)
> **Tools:** terminal (2)
> **UI Navigation:** scroll down (2)
> **Definitions:** is a  (2)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)

#### [Regular expressions](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/regular-expressions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/regular-expressions?u=76281980&t=0)** - [Instructor] In this lesson, we're going to take a look at regular expressions, which are a way of searching text for patterns. When you're working with text like strings and [[JavaScript]], but also if you're working with long runs of text in prose, there may be times when you want to find a particular something, but maybe not an exact something. Maybe you can describe it with a pattern that it should follow, like two digits followed by any number of letters, or any [[Microsoft Word|word]] that starts with A and ends with S. A regular expression is a way to describe something that you're searching for with an abstract pattern. In JavaScript, a regular expression is another data type just like numbers or strings, and it looks like this. Now, if we just have two slashes with nothing in between 'em, that is, of course, a line comment, but it becomes a regular expression when there's something in between those slashes. That can be words, it can be numbers, it can be various combinations, and it can even be patterns. We're going to look at some simple examples in this lesson, but entire books have been written on regular expressions. This particular one called "Mastering Regular Expressions" is the one that I've read myself. Regular expressions are tremendously powerful and usable in all good text editors and word processors. If you spend much time writing code or, indeed, any kind of writing, learning the basics of regular expressions is really worthwhile. I don't want to go too overboard and say they're life-changing, but it's honestly pretty close. Anyway, here's how they work. What we're going to do here is take these four strings,
>
> **[1:35](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/regular-expressions?u=76281980&t=95)** which have some different stuff in them, as you can see, similar but different, paste them in here, so they're all available in Node. And then I have this regular expression. It's a variable called regex, and it's just very simple with the word this in between those two slashes. What we're going to do is we're going to take this regex and we're going to use its test method. And so I can copy and paste any of these and try them out. In fact, let's take all four of them. We'll execute all of them together. So we can see the result of each test. When we checked it against the first string, the result was false. It did not find this. Likewise, the second one, the only one where it returned true was the third string. And I can see that the word this does appear here, but it's not in the same case. This can be useful in a lot of different ways. You can check for a word or a phrase or whatever in a block of text of any length and that test happens really fast. There are a lot of modifiers that you can add to regular expressions. We're going to try a few variations down here below. First, we're we're going to try adding the i flag onto the end to modify that regular expression. Flags like this that you add on the end,
>
> **[3:08](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/regular-expressions?u=76281980&t=188)** it's like a boolean, but this enables a feature, which, in this case, means make this regular expression case insensitive. So now I can run these tests again. And this time with the regular expression set to be case insensitive, string four, which was previously false, comes back true. And that should be the case for any of these others as well. Now they all come back true. There are some other basic modifiers that we can add to the regular expression as well. A carrot will match against the beginning of a string. So if I modify this to match against the beginning, and then run these four tests again, now string one, two, and four come back true and three comes back false, because this appears at the beginning of each of these strings and in the middle of string three. I can also check for whether this appears at the end by following what I'm matching with a dollar sign.
>
> **[4:27](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/regular-expressions?u=76281980&t=267)** So now we can try this same test against all four strings. And because this does not appear at the end of any of them, it comes back false for each one. Just a couple more here. In this one, we're trying to test if ever appears at the end. And I would expect this to come back true for two of these. So we'll modify our expression and run these tests. One and two come back true, and then three and four come back false. But there's something tricky about that particular regular expression we just tried, because a dot has a special meaning in a regular expression. It doesn't check for a literal period or full stop. This means any character. So if I want to check for a literal dot, I would have to escape it like this. But there are lots of different special characters that can appear in regular expressions to do different kinds of matching. We've only begun to scratch the surface of what regular expressions can do. Along with checking for matches, as we've done here, you can extract chunks of matching text for further transformations, search and replace by patterns, instead of by literal strings, and tons more. This topic has applications way beyond JavaScript. Regular expressions are extremely powerful and widely available. If you find yourself wanting to process any kind of text, regular expressions can come to the rescue most of the time.
>
> **[6:02](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/regular-expressions?u=76281980&t=362)** I know when I first learned how to use them, it felt like the text world was my oyster, and I hope they're helpful to you as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (5), [[JavaScript]] (3)
> **CLI Commands:** find (3), node (1), make (1)
> **Analogies:** just like (1), it's like (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 4. Operators and Control Structures

[↑ Back to Table of Contents](#table-of-contents)

#### [Simple comparisons](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/simple-comparisons?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/simple-comparisons?u=76281980&t=0)** - [Instructor] In this video, we're going to look at simple comparisons, which are the beginning of the way that we can ask the computer a question about something and get back a meaningful answer. Going to maximize the terminal here to give us as much room as we can possibly have. The first one we'll look at is equality. That is knowing whether something is the same as something else. We'll start out really easy with this and I'll create a couple of variables here. We'll create a variable called one, which will contain the number one and another variable called two, which will contain the number two. We can just type these out to make sure that they have the values we expect. Most of the time at least one of the items will be asking a question about will be a variable. So having these two available is helpful. First, we're going to ask if one and one are the same. The way I'm going to do that is using an operator made of three equal signs, one after another, 1, 2, 3. This operator is called the strict equality operator and it tests whether the object on the left is identical to the one on the right. And as we can see here, the answer is true. There's a similar operator that asks whether this item on the left is not identical to the one on the right, that's called strict inequality. And to do that, we just substitute that first equal sign for an exclamation mark, which programmers also call a bang. So in this case we're asking is one not identical to one? We would expect the answer to be false, and indeed it is.
>
> **[1:38](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/simple-comparisons?u=76281980&t=98)** Now, if we change that second one to our variable two, and because we're asking here are these two things strictly not the same, we get back the answer true. And then finally we'll change that identical, that not identical back to the strict equality operator again. And our answer is false. So at this point we've started to ask some questions and started to get back some answers. These kind of comparisons are all over the place in most programs. You might be asking, is this page's URL about dot [[HTML]] or is this button that was just clicked the main menu button? Checking whether something is equal to something else is a comparison you'll just be doing a lot. There's another pair of operators that are similar, but act differently. You'll see them a lot in programs out in the wild. These are simply the equality and inequality operators with only two equal signs or one bang and one equal sign. These also check equality, but in a way that's not as strict hence the difference in names. So let's try them out. First, we'll do a regular equality check on the variables one with itself and that still comes back as true because they are the same. After this, things get a little weird. So let's try one. And for the thing that we will compare it to let's create a string with the number one in the string. So it's a string, but it has one inside it.
>
> **[3:15](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/simple-comparisons?u=76281980&t=195)** And in terms of this kind of not strict equality, the answer is true. Now, strictly they are different, but this operator sort of implicitly converts the data types from string to number in this case and says, "Yeah, sure, they're pretty much the same." The inequality operator does the same thing for inequality, so we get the opposite of the previous weird result. It is generally recommended that you use the strict versions of these operators for the most predictable results in your code, but people do still often use the non-strict versions as well. The particulars of which kinds of values will return true or false for the equality and inequality operator are a little bit too detailed to go into here, especially for people who are just starting out. You can look at the equality operators section of the operators page on the Mozilla developer network for more information on this. [[JavaScript]] also has relational operators, which are straight out of your math class. So for example, we could ask is one less than two? Of course it is. And so that returns true. Then we can ask is one greater than two just by flipping that angle bracket. And indeed that's false. We can also combine greater or less than with an equal sign. Less than or equal to is the same angle bracket as we saw above directly followed by an equal sign. So now we're asking is one less than or equal to two?
>
> **[4:52](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/simple-comparisons?u=76281980&t=292)** And indeed it is. So that returns true. Likewise, we can ask is one less than or equal to one? The answer is true because indeed one is equal to one. Of course we can ask is one greater than or equal to two? The answer of course is no. So that returns false. And of course we don't have to use variables. Our final question will be is 10 greater than or equal to two, just the number 10. And of course that is true. So that concludes our first look at comparisons, the various equality operators, and the relational operators. Expect to use all of these early and often.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[HTML]] (1), [[JavaScript]] (1)
> **Env Vars:** url (1), html (1)
> **Definitions:** is called (1), is a  (1)
> **CLI Commands:** make (1)
> **Cross-References:** as we saw (1)
> **Tools:** terminal (1)
> **Analogies:** for example (1)
> **Best Practices:** recommended (1)

#### [Arithmetic operators](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arithmetic-operators?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arithmetic-operators?u=76281980&t=0)** - [Instructor] In this video, we're going to look at some simple operators that are used for doing math. You're not going to need a lot of math in the examples we're going through in this course, but simple arithmetic does come up pretty often. The first four operators are pretty simple and if you've ever used a calculator program before, these will probably be familiar to you. We have of course, the plus operator which takes two numbers and adds them together. So two plus five equals seven. We also have minus which is actually a hyphen, so four minus three is one. Of course, we're not restricted to the counting numbers. We can do negative numbers as well. So five minus nine equals negative four. To do multiplication, we don't use an x or a dot. We use an asterisk. So three asterisk five is three times five which is 15. And then finally, we use a slash, also known as a forward slash, to do division. So 36 divided by six is six. And when you start getting into fractions, the result is expressed using a decimal point. So we can divide 36 by five, we get 7.2. So far these are all pretty straightforward. Probably nothing you haven't seen before, but now we're going to look at a few things that might be a little unusual depending on the sort of math you've been exposed to before. The first one is the modulus operator which is a percent sign. That modulus operator takes the first operand,
>
> **[1:33](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arithmetic-operators?u=76281980&t=93)** divides it by the second, and then returns the remainder of that division. This is often used for determining programmatically whether a number is even or odd, but you could also use it for something like if I'm showing a list of search results in groups of 20. How many will be on that last page of results? Anyway here's what it looks like in usage. So we'll start with 20 modulus or you might just say mod two. 20 divided by two is 10 and there's no remainder, so we expect this to return zero and indeed it does. 19 is of course, an odd number. 19 divided by two is nine with a remainder of one. If we wanted to find out if a number was even, we'd do a comparison that looks like this. 20 mod two, and then use our strict equality operator to ask is this equal to zero? Normally we'd probably be using a variable somewhere in here instead of just two scaler values. That's what it's called when we're using a value directly instead of assigning it to a variable first, but this makes it very clear what we're checking. Now let's answer that how many search results are on the last page of this query question? I'll create a couple of fake variables for how many items are on each page and another one to fake how many results we have. So we'll say per page is going to be 20 and we'll make another one for the total results. Let's say that's 254 results.
>
> **[3:10](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arithmetic-operators?u=76281980&t=190)** So now I want to find out the remainder of total results. I'm going to need this to be capitalized properly. So I want the remainder of total results when divided by the number of pages. So that's going to be 14 because 20 times 20 is 240, plus 14 is 254. Now let's look at incrementing and decrementing which is something you do all the time in programming. Let's say I have a variable called counter which starts out at zero. Many times when you're working through something in programming, you need to keep track of how often you've done that thing. Every time you do whatever that unit of work is, you tell your counter all right, I've done this one more time so increment yourself, and there are various ways that we can do that. The first one is to take counter and assign it to itself plus one. This does work, counter is now one, but it's a little verbose. There's always a tension in programming between keeping things verbose enough that they're easy to read and not having to type a lot for operations that are very simple. So there are various ways to express the same thing that are just a little shorter. The first one is to take your variable and then use the plus-equals, also known as the addition assignment operator. So in this case, if I say counter plus equals one, this is expressing exactly the same thing
>
> **[4:43](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arithmetic-operators?u=76281980&t=283)** as the previous statement but it's just a little shorter. Still pretty clear what it's doing though. Another common way is to take the variable name and simply add two plus signs on the end of it. Now notice that the return value here was still two. But if I check the value of counter, it actually was incremented. Another thing to watch out for is that this will only work incrementing by one. Whereas with plus-equals, you can increment by any arbitrary number. So I could say counter plus equals five if I wanted to. I can even use a negative number. So I could add to my count negative four, and that still works. You can use whatever feels right to you and similar to incrementing, there's also decrementing operators. They're exactly the same thing except instead of using a plus sign, you use minus. So if I want to count down from somewhere, I can say counter minus-equals one. Likewise there is a variation with two minus signs. Again, the value returned is three, but the actual value in the variable ends up being two. One thing that's worth mentioning is that all of the arithmetic operators we've seen so far work with this assignment syntax. So for example, counter asterisk equals two will double the value in counter. I don't see this construct very often,
>
> **[6:18](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arithmetic-operators?u=76281980&t=378)** but if you ever do, that's what it means. One more note about these arithmetic operators. The type of data you use them on can yield different results. This is particularly important in a language like [[JavaScript]] that doesn't care what data you put in the variables. Plus also works on strings. But when you use it with strings, it's called a concatenation operator. So if I say cat plus dog, I get back catdog. They're just stuck together. If you ever want to combine strings as words, you need to make sure that spaces are included in there somewhere. So I can add a space to that first cat string and now the result is cat space dog. And you aren't limited to two strings. You can do this as many times as you want. So cat and dog, and we get back a little phrase, cat and dog. So that plus operator works on strings to concatenate them together. While working in arithmetic terms, it's just simple addition. In the previous video, we saw how numbers containing strings can sometimes act as if they contain numbers instead of strings, and here's a little trap you want to avoid. Say we have two strings with one and two inside and I use that plus operator on them. The result is a string containing those two digits squashed together, not the numbers added together.
>
> **[7:50](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arithmetic-operators?u=76281980&t=470)** So this is a string with 12, not three. It's concatenation, not addition. I think that it might be an unofficial rite of passage for all beginning developers to do this accidentally at least once, myself surely included. That is an overview of the most common and probably most useful arithmetic operators and what they do, with a short detour on string concatenation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (1)
> **CLI Commands:** cat (5), find (2), make (2)
> **Definitions:** is a  (4), known as (2), is an  (1)
> **Analogies:** similar to (1), for example (1)
> **Versions:** 7.2 (1)
> **Cross-References:** previous video (1)
> **Warnings:** watch out (1)
> **Speakers:** - [instructor] (1)

#### [Logical operators](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/logical-operators?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/logical-operators?u=76281980&t=0)** - [Instructor] In this video, we're going to introduce some of [[JavaScript]]'s logical operators. There are many, but we'll look at the three most common, the logical and, or, and not, also known as conjunction, disjunction, and negation, if you're fancy. When working through a JavaScript program, there will be many times when you want to do comparisons on more than one thing at a time, basically asking a question in multiple parts. Logical operators let us combine these parts in structured ways. We'll start with and and or. So as you can see, in JavaScript, those look like two ampersands for and and two pipe characters for or, which on an American keyboard you can find above the return key, on the backslash key. You hold the shift key, type a backslash, and you get one of these pipes. That's what they look like, but how do we use them? I'm going to paste in a one-liner here, setting three variables. animal1 is a monkey, animal2 is a bear, and animal3 is a tiger. After these are established in our program, let's assume that there's other code that might change what's in each variable. Later on in the program, we want to check what's actually in the variables at that point. I can use the strict equality operator to ask in JavaScript, is animal1 still a monkey? And maybe I want to ask at the same time, is animal2 still a bear? If it's important to check them both,
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/logical-operators?u=76281980&t=94)** and then execute a piece of code only if both of those are true at the same time, I can use the logical and right here in the middle to check them both simultaneously. So here, I'm asking, is animal1 a monkey, and is animal2 a bear? And JavaScript says, "Yes, this is true, because the whole statement evaluates down to true." Now, if I wanted to find out if animal1 is an ape and animal2 is a bear, we'll change that first one to ape. And JavaScript returns false here. The idea is that the and operator will return true only if every condition that you pass into it, separated by that and, is true. This is the case no matter how many comparisons I add on. So let's check animal3 as well. If any one of these conditions is false, the entire statement evaluates down to false. But then if I go back to the beginning, and I change ape to monkey, all three are true, so the entire statement with all those ands evaluates to true. If I only care about one of these conditions, I can use or, instead. I can ask, is animal1 a monkey, or, using the two pipes, is animal2 a bear? And that is true.
>
> **[3:09](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/logical-operators?u=76281980&t=189)** So once again, I'm going to change that first monkey to ape. And now I'm asking, is animal1 an ape, or is animal2 a bear? If either one of them is true, the final answer for the whole statement will be true. For an or to return false, everything needs to be false. So let me change bear to ostrich. And now, because neither of these is true, in other words, they both are false, the entire statement is false. You can also combine these ands and ors on a single line. But now things get a little more complicated. JavaScript has a notion of operator precedence, which determines which of these things will be evaluated first, and then collapsed into its true or false value before being compared against the next thing. And in JavaScript, with operator precedence, the ands are always evaluated down before the ors. You can also combine these ands and ors on a single line. But now things get a little more complicated. JavaScript has a notion of operator precedence, which determines which of these things will be evaluated first, and then collapsing those true and false values before comparing them to the next statement. In JavaScript, with operator precedence, the ands are always evaluated before the ors. So in this case, because these two are joined by an and, they would be evaluated first before being joined with an or to this first statement.
>
> **[4:45](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/logical-operators?u=76281980&t=285)** So animal3 is a tiger, but animal2 is not a monkey. So this would evaluate down to false. And then animal1, it is a monkey, so this would be true. Therefore, we have true or false, and the whole statement evaluates to true. So if I have a series of comparisons like this, and I want to make sure that the ors are kept together and evaluated first, I can add parentheses. And now these two are evaluated first, which would be true, and then checked against animal3 equals tiger with that and. And the result is again true here. Now, I know at a certain point your eyes could be rolling into the back of your head from seeing true and false all the time, but there will be times in writing your code, or in reading other folks' code, where you're going to see lots of comparisons. And hopefully they will have made things easier for you by putting in parentheses where needed and generally trying to make things as clear as possible. Let's talk about one more thing with logical operators, negation, or not. This is written as an exclamation mark, also called the bang, to invert from true to false, or the other way around. So here's how it works. If I write not true, or bang true, the result is false. Likewise, not false returns true. You can apply not to entire comparisons as well, wrapping the whole thing in parentheses
>
> **[6:18](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/logical-operators?u=76281980&t=378)** and sticking a not in front of it. So let's take one of these from above. I'll copy this. I'll make animal2 a zebra. So this is false, but then if I put a not in front of it, wrap the whole thing in parentheses, it becomes true. As an aside, sometimes you might want to take an expression that's entirely negated this way and write it out without parentheses for reasons of readability or maybe clarity of intent. When converting an expression like this one, the not isn't just altering these strict equality operators. It also flips any ands to ors and vice versa. And and or are logical opposites. So if I wanted to rewrite this without the negation, and I only changed the equality operators, this evaluates to false. So this is not equivalent to the line before. All of these have to be flipped to their opposite. So that and becomes an or, and now it evaluates to true. Logical operators are rarely used in standalone statements like this. In the real world, they're almost always used in control structures, which determine what parts of a program will be executed and which ones will not. Control structures like conditional statements and loops are coming up next. And you use these and other comparisons we've looked at in this chapter. Now that you've seen several kinds of operators that can make comparisons
>
> **[7:49](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/logical-operators?u=76281980&t=469)** and answer questions about the state of a program, we're ready to move on to conditionals.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (10)
> **Definitions:** is a  (6), known as (1), is an  (1), in other words (1)
> **CLI Commands:** make (4), find (2)
> **Cross-References:** go back to (1), coming up (1)
> **Tools:** notion (2)
> **Speakers:** - [instructor] (1)

#### [Conditionals: If](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-if?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-if?u=76281980&t=0)** - [Instructor] A control structure in programming is a way to modify the flow of execution. Making decisions like whether to execute a portion of code at all or how many times to execute it. In this video, we're going to look at one of the fundamental control structures, the if statement. With an if statement, you can create forks in the logical road that your code will follow depending on certain conditions. This is also called branching. What we're going to do first is assign a variable based on a prompt for input from the user. To run these examples, we're going to open a new browser tab and open a [[JavaScript]] console. I'm using Chrome and I have a new tab here just on the about blank page. And then in the Developer menu, I'm going to open the JavaScript console. I have it set over here on the right and I've made it nice and large. This code is web specific. Since prompting the user for input is a little more complicated to write in node. Here in the transcript file for this video, I'm going to copy this first line. And so what we're doing here is assigning the variable answer to the result of a method called confirm, which is on the global window object that's part of every web browser. Let's watch what happens if we hit Return and execute this. So I get a little dialogue box that says click OK to get true, click Cancel and get false. And you can see that the string that I passed into that confirm function is rendered here. So based on whichever of these buttons I click,
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-if?u=76281980&t=94)** I'm going to get a different value in that answer variable. I'll click OK and the return result is undefined. But if I type out answer, I got true. If I hit up arrow a couple of times and run this again, this time I'm going to hit Cancel, hit up arrow again to get back, answer so I can check its value, and that time it's false. So in the course of a program, you might want to ask the user for an answer to some question. This window.confirm method is a way that you can do that. Having retrieved an answer, now I need to take some action based on what the user said. One way I can do that is with an if statement. So I can type if, and then in parenthesis I put my condition or conditions. If the answer is true, then I'll just log a message to the console. So we'll say, if answer is true, and I'm going to hit Shift and Return to get a new line here in the console. If I just hit Return, it will execute the line instead. And I'm going to use another function which is part of the global console object that's available in web browsers and in node to just send a message to the console. We'll say, You said true! I'll add a closing brace to this block. Now because answer is currently false, this code does not appear to execute. But if I run this one more time and click OK,
>
> **[3:08](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-if?u=76281980&t=188)** now answer is true. And if I execute that same block again by hitting up arrow a couple of times and hit Return, I get back You said true! So let's extend this to take a different action based on whether the answer was true or if it was false. So if I hit up arrow again to bring this back, I'm going to add an else clause. Again with a beginning curly brace and I'll hit Shift + Return to get a new line. And if the answer was not true, this one will execute instead and will say You said something else. And this with another closing curly brace. Answer is true, so that first line executes. We'll hit up arrow a couple of times to get our confirm line. I'll hit Cancel. Up arrow two more times to get my block with if and else, execute it. And there we are. Answer was false, so we get You said something else. So this is how you can branch in your code and do certain things under certain conditions and other stuff in other conditions. If statements can be extended even further. Let's take a look at a longer block of code. This time we're going to use the window.prompt method, which is going to prompt me using a text field. So I'm instructing the user to type YES, NO, or MAYBE and then click OK.
>
> **[4:42](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-if?u=76281980&t=282)** And then I'm going to take various actions based on what the user said. So let's copy this whole block. I'll clear this console and paste in that entire block. So let's execute this code and see what happens. Here's my prompt with my text box. So I'll try it with YES. And there we go. The answer was YES. So that first branch is what executed, You said YES! Let's try it again. Let's try MAYBE. You said maybe. I don't know what to make of that. That's the second one here. And we'll do it one more time for completeness. And this time, I'm going to say COOKIE. And then that very last line executes because none of the other conditions fit. And it said You rebel, you! All right, now that we've done this, let's take a look at the code back in the code space with this nice syntax highlighting and break it down. Along with the if and the else that we've seen before, we have these other lines that combine the two as else if. And these are executed in order with each one checking and trying to match. And if it doesn't match, the next one will execute in sequence. So the first time we typed YES, and immediately we got to this one. It executed the block and we stopped. The next time it was MAYBE. So we tried the first one. Answer was not yes, so we went down to the next one. The answer was MAYBE, so that line executed.
>
> **[6:17](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-if?u=76281980&t=377)** And finally we did COOKIE. This one didn't match. Neither did this one. Neither did this one. And so finally we got to the very last case at which point this line executed. If I strip any of these out, it won't be taken into account anymore. Say I decide I don't need to specify MAYBE anymore. I could simply remove this condition by removing everything that was in those curly braces. Then I'll only need to check specifically for YES or NO with specific responses to each of those. Anything else, whatever the person types, would go into the else clause. And you can have as many of these lines as you want. And the conditions in these parentheses don't have to be single comparisons. You can also add logical operators. As in this case, we are checking if answer equals YES or answer equals NO where we might take some common actions for YES or NO. Anything that appears in those parentheses is the condition under which this whole block in these curly braces that match here will execute. You can also nest if blocks inside other ones. So in these curly braces, I have another block of if and else. We check the same is answer YES and then have an else here. So this allows you to do common actions that would match for either of these, because we're using an OR, and then get into specifics inside that clause.
>
> **[7:53](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-if?u=76281980&t=473)** You can see here that there's a lot of power in being able to branch off and do different things in your code based on different inputs or different other conditions, whether it's input from the user or conditions that you created somewhere in your own code. And that is how you can use if statements to execute different code under different conditions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2)
> **Env Vars:** yes (9), maybe (5), cookie (2)
> **CLI Commands:** node (2), make (1)
> **Definitions:** is a  (3)
> **UI Navigation:** open the (1)
> **Speakers:** - [instructor] (1)

#### [Conditionals: Switch](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-switch?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-switch?u=76281980&t=0)** - [Instructor] In this video, we're going to take a look at another way of doing conditional branching in [[JavaScript]]: the switch statement. It's similar to if and else statements in results, but it looks different and acts differently at times. Here's a familiar example using if statements to do some branching based on user response to a question. It works great, but if we have a lot of else-ifs, it can start to get tedious writing this same sort of thing over and over and over again, adding all the braces and so forth. At times like these, we might consider using a switch block instead. I'm going to close this terminal and we can see the same thing down here written as a switch. We start by saying switch and then in parentheses, we add an expression that we want to evaluate. Most of the time when you see switch statements, this will take the form of just a variable, but it could be more. In any case, it needs to evaluate down to something that you can check with conditions called cases down below. Usually what you'll see is the variable and then you're checking some various states of what the value in that variable might be. So as I said, each of these is called a case block. You literally use the [[Microsoft Word|word]] case followed by a value and a colon. The colon can have spaces before it or not. Then there's whatever is supposed to happen just like in an if block, but there are no surrounding curly braces. And at the end of each case, you have the break keyword. So just as we had here,
>
> **[1:35](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-switch?u=76281980&t=95)** we were checking first if answer equals yes, then if answer equals maybe, no, and then a final else condition. In switch terms, we're looking at answer with the switch statement. And then the first case we're looking at is yes, in other words, is answer equal to yes. If that is true, this block will execute. If not, we continue down to the next one, which is maybe, then to no, and finally to one called default which is the same as an else statement. If everything else doesn't work, this is what will execute. Just as before, we're going to execute this in a web browser because we're using web code here with the prompt method on the window object. So first, we'll paste in our answer prompt, we'll go with yes first, and now we can copy the switch block and paste it into the console as well. So we just answered yes to the prompt, so answer is yes, and there we are getting yes. If I execute the prompt method again, we'll try no this time. Come down to the bottom line, hit up arrow a couple of times to bring back our giant switch block. And just as before, it's running down the cases, yes didn't match, maybe didn't match, but no did
>
> **[3:09](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-switch?u=76281980&t=189)** and so that's the line that executes and that's what we see in the console. We'll try this one more time to trigger our default case. We'll try a cookie again. Up arrow to bring back the block and there it is. Nothing else matched and so we get the You rebel, you line. There's one thing that's a little bit tricky with switch statements that you need to be aware of. They can cascade in a way that if statements don't. So let's say I remove this brake in my yes block and then I execute the same thing again.
>
> **[3:54](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/conditionals-switch?u=76281980&t=234)** Let's see what happens if I get my prompt back and type yes and now paste in my modified block that doesn't have the break statement in this yes case. So what I get is You said YES, which I expect because that was actually in the case block for yes. But then I also get You said maybe, so this is the thing about switch statements. If you don't include a break, the execution will cascade into subsequent case blocks until a break is reached. So when I typed in yes, that first block got executed, but there's no break here so it continued into the next case. You want to be sure that if you want just a single block to execute that you include that break statement, but you can also leave the break off on purpose if you want a certain block of code to be able to cascade to certain other cases. This can be a little bit tough to do with if statements. So most of the way that if blocks and switch blocks work is the same in popular usage and which you want can just be a matter of taste For lots of branching, a switch can be more pleasant to read and write. You can use either one. It's really up to you.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (1), [[Microsoft Word|Word]] (1)
> **Definitions:** is called (1), in other words (1)
> **Analogies:** similar to (1), just like (1)
> **Env Vars:** yes (1)
> **Tools:** terminal (1)
> **Warnings:** be aware (1)
> **Speakers:** - [instructor] (1)

#### [Terse ifs: One-liners](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/terse-ifs-one-liners?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/terse-ifs-one-liners?u=76281980&t=0)** - [Instructor] In this video, we're going to look at a few of the [[Forms]] in which if statements will be made shorter. You may write these in your own code and you will certainly see them if you start looking at other people's code. Let's start with the one-line if statement. For this to work, I'm going to create a little variable here. We'll make it a cherub whose name is Cupid, and then the one-line if statement might be if the cherub is equal to Cupid. Without any curly braces, we can immediately just log a message that might say something like, "Ouch, an arrow!" "Ooo, I'm in love!," something like that and you can see that that works. We can also tack on an else statement to that. I'm going to enter the editor mode here in Node because without the curly braces, I don't expect it to recognize that I'm trying to do this the way I'm doing it. So we have our one-liner and then we can add on an else statement again, with no braces. So in this case, we'll log, "I feel nothing," so bleak.
>
> **[1:19](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/terse-ifs-one-liners?u=76281980&t=79)** Okay, Control + D to exit that and everything still works. And if we change our cherub to someone else, let's say let's make it a cherub named Norman. I'll enter editor mode one more time. Copy and paste that same block. Hit Control + D to let it all execute and this time we feel nothing. One-liners are fine for quick checks. But if you want multiple lines of actions to be executed after a condition is evaluated by an if statement, you do need to add those curly braces back in. Only that first line is bound by the if when there are no braces. For that reason, I rarely use the one-line version myself. I always add curly braces in case I need to add a condition later, and I do think it makes it easier to read. Another thing you'll see is a condition that's just a variable by itself. Here's a scenario to illustrate that. When writing applications, we want to track errors and report them back to the user at some sensible point. So in this first case, we'll have a variable called errorMsg and we'll just start out as an empty string. If there's an error to report to the user, we can set that string to a message about the error like that field is required or what have you. And then later if we find that the error message variable has something in it, we can take some sort of action. In the if statements we've seen so far, we have a full expression that then evaluates to being true or false. We can just check a single variable
>
> **[2:54](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/terse-ifs-one-liners?u=76281980&t=174)** or a part of a variable in the same way For this type of evaluation, we're not concerned with an expression evaluating to exactly true or exactly false. Here there's a concept of truthy and falsey. All values in [[JavaScript]] can be considered truthy or falsey for this purpose. Generally speaking, if the variable has something in it, it's probably truthy and empty values are falsey. There's a page here on the Mozilla JavaScript documentation that gives you a lot of examples of truthy and falsey values if you're curious. So if I want to know if there's an error to concern myself with, I can just say if errorMsg which is a lot simpler than if errorMsg.length is greater than zero or something of that nature. If the string has anything in it, it will be considered truthy and the error message would be logged. So we could say console.error. "There was an error," and we can include the error message here, something like that. And because errorMsg is empty, it is considered falsey and so that does not execute. Negation will take truthy values and make them falsey, just as it will invert true and false. So if I rewrite this as if not errorMsg and this time instead of a console.error, I could do console.log, "Yay!" Something simple like just "Yay, no errors!" And that actually does execute
>
> **[4:29](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/terse-ifs-one-liners?u=76281980&t=269)** because negating this falsey value turns it to truthy and so it'll execute. One more example of truthiness. Instead of taking a single error message in a string, let's have an array so we can track multiple errors and report them all at once. How might you do a simple test of whether there are errors to report before logging them? If you thought if errors would work, it's a totally great guess but it wouldn't work exactly as you expect because an empty array is still a truthy value. It's kind of counterintuitive, but that's the truth. We need to check the length property of errors instead. When there's nothing in there, length will be zero and if there's anything in the array, it will be greater than zero which is truthy. So in that case, we'd be able to say console.error, "Please fix these errors," and just do a bit of simple logging there. So this would work as expected. So be sure to test scenarios or reference documentation when dealing with truthiness and falseness. Making if blocks and the conditions that they contain shorter is a very common practice. While I don't recommend one-line if statements, you now know of their existence. Employing truthiness in conditions is very common indeed, so feel free to use it but be sure to test the behavior of those conditions as you're coming to grips with it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2), [[Forms]] (1)
> **CLI Commands:** make (3), node (1), find (1)
> **Code Identifiers:** errormsg (5)
> **Definitions:** is a  (3)
> **Speakers:** - [instructor] (1)

#### [Terse ifs: Ternaries](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/terse-ifs-ternaries?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/terse-ifs-ternaries?u=76281980&t=0)** - [Instructor] In this video, we'll look at one more quite terse if construct, which is called the ternary operator or the conditional operator. It's an expression of some kind followed by a question mark, followed by an action to take if that expression evaluates to true, then a colon, and then the other action to take if the expression evaluates to false. So I'm going to start with a variable to give me something to play with here. We'll create this animal that is a cat, and now I'll write out an expression using this ternary operator. So my expression will be if the animal is a cat. Actually, let's go into the editor mode 'cause I think it would be better to spread this across a couple of lines. So if the animal is a cat, and then I'm going to add in a question mark. And if it is, we'll log a message to the console that says "You will be a cat herder." And if this is false, we'll log a second message. "You will be a dog catcher." Okay, now I can press Control + D to execute this whole thing, and it says I will be a cat herder. So if we reset animal to dog,
>
> **[1:32](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/terse-ifs-ternaries?u=76281980&t=92)** just to confirm that this works, and we'll go back into editor mode, we'll copy this whole thing, paste it back in, and execute it with Control + D. You will be a dog catcher. So again, we have our expression to test , question mark, the action to take if it's true, followed by a colon and the action to take if it's false. There's no curly braces here. So basically this is for executing a couple of consecutive one liners and just making it look shorter. The ternary operator is often used to set a variable based on certain conditions. So let's say we wanted to create a variable called job and based on whatever animal we have here, let's say if it's a cat, the job will be cat herder. And if it's any other animal we'll be a dog catcher instead. So let's check the value of job right now. And it is dog catcher because back here we set animal to dog. So if I use the up arrow to go back to where animal is cat, oh, and I accidentally chose the assignment version of this. There we go. Take out two of those equals signs to change it back to assignment. So now animal is cat. And if I set job one more time, now it's cat herder.
>
> **[3:06](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/terse-ifs-ternaries?u=76281980&t=186)** The ternary operator is quite terse. And remembering which order the colon and the question mark can go in is a little bit tricky at first, or at least I thought so. I avoided it for a little while myself, but at this point I've gotten used to it and have started using it occasionally. You can also put parentheses in here to make it a little clearer. So for example we could surround our expression with parentheses so we don't have quite so many operators just going in sequence with no grouping, and that still works fine. The job is still cat herder. As with anything in any kind of programming there's always a trade off between terseness and readability. But ternaries are fairly common in the wild as are the shorter versions of the if statements that we've seen previously. Even if you don't use them much it's very useful to recognize and understand them and you may find uses for this very thing in your own code.

> [!info]- Semantic Content
>
> **CLI Commands:** cat (11), make (1), find (1)
> **Definitions:** is a  (4), is called (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Type checking](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/type-checking?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/type-checking?u=76281980&t=0)** - [Instructor] At this point, we're now aware of many data types in [[JavaScript]]. We know about numbers, strings, booleans, objects, arrays, and so forth. There will be times in JavaScript where you want to know what type of data a variable contains. We're going to look at that in this lesson. JavaScript doesn't particularly care about data types to the same extent that other languages do. In other words, it's loosely typed. There's a video about strongly versus loosely typed languages later in this course. But for now, just know that JavaScript lets you do pretty much whatever you want with variables. Going to make a variable called thing and put a number in it. Just put 12 in there. If I decide later that I want to change thing to a string, in this case, we'll write out the [[Microsoft Word|word]] twelve to avoid a little confusion, it's fine if I do that. JavaScript doesn't care at all. In some languages, you might get a warning if you tried to change a variable from containing a number to containing a string, but not here. During the execution of a program, there will be many times when you're dealing with uncertainty. You can get a variable in a particular portion of your program and not know necessarily what's in there, but you know what sort of data should be in there. Then if the data type is correct, you might follow one branch in your code, and anything else might trigger an error. In situations like this, you need to check the data type. To do that, Javascript offers the typeof operator. It looks like this, typeof,
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/type-checking?u=76281980&t=94)** all lowercase, one word. And so if I check my variable thing, it currently has a string in it, and typeof will return the word "string" as a string. typeof always returns a string. It isn't always the word "string" like this, but it is a string with the name of the type in all lowercase. So if I change thing back to a number again, check its type, now it's a number. Let's change thing to false. This time, the type of thing should be boolean. And there it is. Let's change thing to an empty object. Let's check its type again. And now it's object. This is all just what we expect. Now let's try something else. Let's change thing to an empty array. We'll see what happens. So if I check the type of thing now, I get object, not array. Now, we do know that, technically, JavaScript arrays are objects, but there are differences that can be important to us. So if we want to check whether thing is really an array, not just a regular object, we have to do something a little different. We can check if the type is object, and then also use a method that all objects have, called hasOwnProperty, and pass in the name of a property that we're looking for. Let's start to write this out. So if I wanted to check whether thing is an array, I could check whether the type of thing is an object,
>
> **[3:10](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/type-checking?u=76281980&t=190)** and then I can use this hasOwnProperty, which you note is kindly auto completing for me. And the property that I want to see whether thing has, in this case, is length. Arrays are objects that also have the length property. That's not the only thing that differentiates them, but it is a very convenient way to check. So right now, thing is an array. And our rather verbose type checker returns true. If I set thing back to an empty object, I'll use the up arrow a few times to get that code back, and then run the same long type check again, I get false. So that entire expression will tell me whether thing is an array or not. There are other instances where typeof will not return something that you expect. So for example, we know that there is this thing called Not a Number, spelled like this. It's called Not a Number. So if I check the type of Not a Number, what do I get? Number. Call that a little counterintuitive. So if we're doing some mathematical work, and we want to see if the result is Not a Number, using typeof is not the way you want to do that. There's a built-in Number object, which has a method called is Not a Number. And that's what will help you in that case. So if we pass Not a Number to is Not a Number, we get true, which is quite a relief.
>
> **[4:44](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/type-checking?u=76281980&t=284)** And if we pass in a regular number, we get false. Likewise, we can check the type of null. We get object, which is honestly pretty deeply weird. So if we want to check whether a variable is null, we would want to use the strict equality operator. Let me set thing to null, and then we can use strict equality to check whether thing is null. And it is true. typeof can also return undefined, in some instances. A couple of places where you'll see this is checking the type of a variable that was initialized, but never had any associated data, or when the variable simply doesn't exist. So let me create a variable called somethingLater. Let's check its type. And there it is. Undefined. Likewise, if I check the type of a variable that I haven't even created, just call it nothingSilly, still undefined. With type checking, there are many subtleties that you'll run into with different special values in JavaScript that won't necessarily give you back the type that you expect. So you need to sort of careful with this. It's an area where many, myself included, have needed to reference the documentation often. Despite that, it can still be useful for basic type checking. If you need or want to get more serious about type checking in your JavaScript code, you might consider using helper functions from popular libraries like Lodash. If you search the documentation on Lodash for "is,"
>
> **[6:20](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/type-checking?u=76281980&t=380)** you'll see a whole bunch of helper methods that can be used for checking lots of different kind of objects. You could even use TypeScript, which is sort of a version of JavaScript that includes strict type checking. But that's well past where we are right now. Just know that basic type checking is included in JavaScript with the typeof operator, and there are options out there for more if you need them.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (11), [[Microsoft Word|Word]] (4)
> **Definitions:** is an  (4), is a  (2), in other words (1)
> **Code Identifiers:** hasownproperty (2), somethinglater (1), nothingsilly (1)
> **Documentation:** the documentation (2)
> **CLI Commands:** make (1)
> **Cross-References:** later in (1)
> **Analogies:** for example (1)
> **Warnings:** warning (1)


### 5. Iterating with Loops

[↑ Back to Table of Contents](#table-of-contents)

#### [For loops: Sequential](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-sequential?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-sequential?u=76281980&t=0)** - [Instructor] When we talk about programming computers it's always with the goal of making the machine do something that we're not so good at, or just don't want to do. One of the things that a computer can do really well is the same thing over and over again. What I'm talking about is called looping or iterating. In this lesson we're going to look at for loops. Specifically we're going to look at sequential for loops. This is a way of telling the machine to do something over and over again a certain number of times. To start off, we'll write a for loop that logs a console message 10 times. It looks like this. You write the [[Microsoft Word|word]] for, and then in some parentheses you'll put three statements. First, you initialize some sort of variable, which is usually referred to as the loops counter or index. It's very typical when you see for loops in the wild for the variable to be called i for index. It's just sort of a convention. But you can call it anything you want. For its value, you'll often see zero, very similar to what we've seen with arrays. This is a convention people use, starting with zero. It's not required, but because it's very common, maybe the most common, that's what we'll use here. So we'll start out with that initialization setting the variable i to zero, and then we'll add a semicolon. The next statement is a condition. This condition determines when we will stop looping. In this case, we want to loop 10 times,
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-sequential?u=76281980&t=94)** so I want to make sure that i is less than 10. We started with zero, so if we were to go through until our counter equals 10, that would actually be 11 times through the loop or 11 iterations. So we write this in as i is less than 10, and add another semicolon. So far we're just starting out with zero, and then checking that i is less than 10. So now we have those first two parts, but how do we get to the end? Every time through this loop, we're going to increment i. There are various ways we can increment a variable, as we have seen previously. We could do i plus equals one. One way you'll often see it in for loops is i plus plus as well. Either one works. We'll go with i plus equals one for now. And then in some curly braces, we'll put the things that we want to happen every time this loop runs. Now here in node, I don't have to enter editor mode to get onto a second line because node recognizes, I think based on the curly braces, that we're continuing to edit the same block, and so it just does this for me. Those dots are added automatically just to show that it's doing some indentation. When you're writing your own code in an editor, you won't use any dots like this. So in those curly braces we put the things that we want to happen every time the loop goes. In this case, we're just going to do something simple. Which is using the console.log method to write something to the console. In this case, we'll put the value i.
>
> **[3:09](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-sequential?u=76281980&t=189)** So every time this loop runs, in the console we will get whatever the value of i is at the time that that particular step through the loop is executing. Then we'll just add our final curly brace and that's it. To recap, this for loop has three statements. We initialize the variable called i to zero and then we stop once i is no longer less than 10. And every time through the loop we increment i by one. Inside the braces each time we go through the loop, we're writing the value of i to the console. Now let's execute this. We'll just hit Return. There we go. So we can see i started out at zero and each line is written out to the console one by one. The next time i is incremented by one, and we go from one to two and so forth. Continues all the way down the line. When i is nine, that line is written out, i is incremented one more time. Its value becomes 10. So the loop stops and we never see 10 written to the console. And just so it's super clear, I don't have to use zero for initialization. I'll copy this and paste it in. I could change i so it starts at one. And then I need to let the loop go as long as i is less than or equal to 10 this time. But everything else can be the same here, log i to the console, add my closing curly brace. Then when I execute it, this time instead of going
>
> **[4:42](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-sequential?u=76281980&t=282)** from zero to nine, I go from one to 10. But otherwise it executes the same number of times and works the same way. And of course you can have as much stuff inside these curly braces as you want. You can take all kinds of action inside loops. Let's take a look at an example loop that's a little more like a real world example. So I'm going to minimize my terminal and open the two files from this chapter. A very common use case for for loops is to loop over the contents of an array. So here we have an array of page names. Home, about us, and so forth. There are six items in the array. Now in the real world, we might come up with some clever way of populating this array with page titles instead of writing them out statically as I've done here. Putting data in like this by hand is called hard-coding. And on real projects it can be something to avoid. For now, it's better for us to see exactly what data we have in advance, so it's fine. We have this array of page names, and we're going to write a piece of code that will look through every item in the array, and tell us whether the one we're looking at matches the actual page title of the page we're on. To do this on a real webpage in a web browser, we would be accessing the document variable web browsers provide via something called the [[Document Object Model (DOM)|document object model]] or dom. Which is the interface a web browser provides [[JavaScript]] developers for interacting with what's on the page. This document object has a property called title, which is the contents of the title tag on that page.
>
> **[6:17](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-sequential?u=76281980&t=377)** You can see it here in this little snippet. So here's what we're doing with this for loop. We're starting with this variable set to zero, because as you remember, the first index in any array is zero. And then we're looping until we hit the length of the array. We're going over each one in sequence so we increment by one each time. And then inside the for loop, you can see these matching curly braces, we have an if block. We check if the title, using document.title, is the same as the current page name, which we're retrieving from this array using the index or counter. And if the current title matches the one that we're looking at in the array, we're going to log a message that says, "We are here," with the name of that page. And if it doesn't match, we're going to say, "We are not here." And again include the page title. So the first time through this we'd expect to look at the page name where i equals zero, which would be Home. The second time through it would be About Us. And every single time through we're going to take the same action that's inside the loop. If the document title matches, it's going to write the message to the console saying, "We're here." And if not, it writes the other one. To actually try this out, we're going to need an actual page. We can do this here in [[GitHub]] Code Spaces by opening the file, which is called sandbox.[[HTML]]. And then using the Go Live function. That opens a little stub web server with this page loaded. It doesn't have much meaningful in it, just a little bit of text.
>
> **[7:51](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-sequential?u=76281980&t=471)** But having loaded this, I can open up the developer tools. Here in Chrome, that's in the View Developer, Developer Tools path. And now I'm going to have the dom available in the JavaScript console. Here in the Elements Inspector, I can look up at the HTML tag for the head of the document. And here's the title. In JavaScript, this is what's represented by that document.title property we looked at before. So let's take this code from the transcript file. We'll copy this, bring it over to our console, paste it, and let it execute. So let's note that it does run through every single item in the array, and here are all the messages. We're not here: home, about us, or contact us. We are here: JavaScript playground. You can see the title up here. We saw it in the Elements Inspector as well. And then News and Blog. We're not at either of those places either. If I wanted to stop the loop from running after I successfully find the right one, I can add a break statement. So here's the we are here condition. If I added a break here, this would break the execution of this for loop. I'm going to copy this block one more time and we'll try it out. I can hit this button here to clear the console. That just clears what's displayed.
>
> **[9:24](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-sequential?u=76281980&t=564)** It doesn't clear the previously set values. Now if I hit Return, I can see all the ones that we are not, up until we reach the one that's correct and then the loop stops. One more quick note about this code. As programmers, we very much like not to repeat ourselves. So you may look through this block of code as you're writing and say, "Ah, look at this. I have this little piece where I'm getting into that array by index, and I'm doing that three times." This is ripe for rewriting. So what we can do is every time through the loop we can set a variable which could be called current page title and set it to page names sub i. Then we can rewrite our comparisons so they use current page title instead. This is nice because not only are we not having to rewrite this somewhat obscure-looking construct several times, but because we've given this a variable with a descriptive name, it makes it very clear what we're doing when we use it in these subsequent lines. That just makes it a little easier to read what's going on. In a block of code this short, that's not really a big win, but it's something. And helping yourself out by using meaningful variable names instead of something like page names sub i, maybe just a little bit less meaningful can be a great help. Especially as your program builds in size over time. And that completes our first look at using sequential for loops to do the same thing over and over again.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (4), [[HTML]] (2), [[Microsoft Word|Word]] (1), [[Document Object Model (DOM)|Document object model]] (1), [[GitHub]] (1)
> **Definitions:** is called (3), is a  (3)
> **CLI Commands:** node (2), make (1), find (1)
> **Tools:** terminal (1), github (1)
> **Prerequisites:** initialization (2)
> **File Paths:** sandbox.html (1)
> **Env Vars:** html (1)
> **UI Navigation:** open the (1)

#### [For loops: Enumerative](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-enumerative?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-enumerative?u=76281980&t=0)** - [Instructor] In this video, we're going to look at another type of for loop, the Enumerative for loop. This one is used on arrays or objects. It can take various [[Forms]] and while useful, you do need to pay attention when using this tool. We'll look first at iterating over the items in an array. I have this variable called "pageNames". It's an array of six items. We've seen how to iterate over this array using Sequential for loops, where you would set a variable to zero, give it a stopping condition and increment every time through. A less verbose way to do this is with the for in loop. This is what it looks like. I have the [[Microsoft Word|word]] "for" followed by parenthesis and then the expression inside is "var", in this case, you could also use "let" and then I choose whatever variable name I want. I'm using "p" in this case, which is short for pageName, from "pageNames". You might often see counter variables in loops abbreviated from their source variable name in this fashion. Next is the keyword "in", and then the name of the object or in this case the array over which I'm going to iterate and then of course close the parenthesis. And I have curly braces to enclose what should happen each time through the loop. Each time through this for in loop, "p" will get set to a different key from the array. With an array, the keys are numbers, so I'm expecting "p" to get set to a number each time for as many of these items as there are. A caveat, the order of the keys
>
> **[1:33](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-enumerative?u=76281980&t=93)** is not guaranteed by the [[JavaScript]] spec. In other words, "p" is not guaranteed to start at zero and proceed upward the way it would with a Sequential for loop. In my experience, the keys generally do come in a sensible order with these arrays but I'm compelled to mention that because we can't always depend on it. This is a little shorter to write than an equivalent Sequential for loop. But if the order of the original array items matters for your loop, you can't trust a for in loop completely. Now let's try this. I will copy this variable name over into my Node console. We'll enter Editor mode so we can paste in a multi-line block and then I'll finish with Control D. And now I can paste in my for in loop. And because I have curly braces here, I don't have to use Editor mode. I can paste this in line by line if I want to. And there we go. 0 is "Home", 1 is "About Us" and so forth. Now, like I said, often with arrays, a Sequential for loop is more reliable in terms of key order but you will see this in other people's code, I'm sure. So I wanted you to see it here. There's another version of the Enumerative type of for loop, this time using "of" instead of "in". Instead of iterating over the keys of the array, we would iterate over the values directly. Let's see what this one looks like in action.
>
> **[3:07](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-enumerative?u=76281980&t=187)** So I will copy this and paste it in. I can just copy all three lines and Node will pick them up 'cause the curly braces denote that this is a multi-line block. And there we have it. We get each value in sequence, set to the value "v" from pageNames. A for in loop can also be used on an object in a way that's much harder to do with a Sequential for loop. So here's a page's object with various properties. I'm just using ordinal words here. And then the names of the pages. And down here is my for in loop. I have the variable "p" from "pages", our original object. And then skipping this line for a second, it's the same deal. We're going to log the variable "p" and the value from the "pages" array to the console. So each time through this we would say, first is "Home", second is "About Us", and so on. So let's bring these into the console. I'm going to clear this by typing Control L. I'm bringing in the "pages" array and we'll bring in and execute this loop. And so there we go. Each time through, we've logged the key name and then the value that goes with that key. Now again, you can't plan on these keys being in the exact order that they were in when you originally created the object.
>
> **[4:40](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-enumerative?u=76281980&t=280)** That's a very important point. So in your code, if the order in which the things come out of the object is important, you'll probably need to use an array and a Sequential for loop. But let's go back and look at this code a little more carefully. Specifically this line where we're looking at "pages.hasOwnProperty". What I'm asking is, does this object, "pages" have its own, in other words, an inherited property of "p", a property that is directly part of this object. Without going too far into the weeds, JavaScript is an object-oriented language that uses a model called Prototypal Inheritance. There's a video about this later in the course, but for our purposes right now, it means that JavaScript can be modified very deeply. And an object like this could have inherited properties from other parts of code that we can't see in this particular file. And those could potentially show up when you try to enumerate the object. Using this "hasownProperty" line is your way of safeguarding yourself against enumerating things that you didn't mean to. As I mentioned, the details of this are a little on the esoteric side for the moment, and there's certainly a lot of code out there where you won't run into this problem at all. But if you're going to be enumerating over an object, it's pretty important to include this check. So just file that away and know that it's something to look out for. And if you ever see it, in the meantime, you'll know what it means. So that is how you can use Enumerative for loops to iterate
>
> **[6:15](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/for-loops-enumerative?u=76281980&t=375)** over both arrays and objects.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (3), [[Forms]] (1), [[Microsoft Word|Word]] (1)
> **Definitions:** in other words (2), is a  (2), short for (1), is an  (1), means that (1)
> **Code Identifiers:** pagenames (3), hasownproperty (2), pagename (1)
> **CLI Commands:** node (2)
> **Cross-References:** later in (1)
> **Documentation:** spec (1)
> **Warnings:** caveat (1)
> **Speakers:** - [instructor] (1)

#### [Set and Map: Newer loopable data types](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/set-and-map-newer-loopable-data-types?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/set-and-map-newer-loopable-data-types?u=76281980&t=0)** - [Instructor] In the last video, we looked at arrays and objects as data types that can be used for looping. At this point, I want to pause briefly to introduce a couple other data types that have been added to [[JavaScript]] more recently which can store data in a similar way but act differently in ways that impact loops. Set and map. Set and map are special types of objects. To create one, we use the "new" keyword, which gives you a new instance of the type of object it's given. Using new to create a new instance is a common paradigm in [[Object-Oriented Programming (OOP)|object-oriented programming]] in many languages. After the object name you include parentheses which can be empty to create an empty instance of the object, or some data can be included to pre-populate your object as long as those data are passed in the correct format for the type of object you're creating. Creating an object this way is called using its constructor function. All JavaScript data types have constructors that one could use, though in my experience, they're rarely used for things like arrays or objects, let alone numbers and other basic types. Sets behave very similarly to arrays in that they both store lists of values. The main obvious difference is that a set will store each value exactly once. If you try to insert the same value more than once, any time you try after that first successful time, nothing happens. Unlike arrays, direct access to the items in the set by index or subscript isn't possible. You have to use methods, special functions that are part of the set object to interact with its items.
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/set-and-map-newer-loopable-data-types?u=76281980&t=94)** To add an item for instance, you use a method called appropriately, "add." Set doesn't actually include a get method that would be analogous to subscripts or indices on an array. They're meant for storing a bunch of unique items, potentially looping over those items and checking if an item exists in the set. The has method shown here will tell you if that string "juno" exists in my set. A map behaves a lot like a standard object, storing pairs of keys and their values. The main differences for our current purposes are that unlike an object, a map remembers the order that its keys and values were inserted. When looping over a map, the keys and values will always come back in the same order. Their behavior is predictable in this regard. Just like with sets, items and a map need to be accessed using a special set of methods like "get" and "has" instead of via direct access by property name the way you can with an object. This makes using them a little less convenient for basic stuff than plain objects. There are other differences between sets and maps, arrays and objects that you might find useful as you start writing more code of your own. But I'll leave it to you to explore those differences further on your own. Now let's take a look at a bit of code so we can see what these data types look like in practice. Let's look at sets first. We'll start with an array where you'll notice the first item is repeated. Creating this with an array literal like this is pretty quick and easy. To do the same thing with a set,
>
> **[3:08](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/set-and-map-newer-loopable-data-types?u=76281980&t=188)** we might use the add method to add each item. So save myself a little typing. I'm doing a little copy and paste here but I can just call my set and add one. And then if I try to do this again we can see that the set doesn't change. There's no error thrown, but nothing changes. So I can do this over and over again to add all the items that are in that array but it gets a little tedious. Fortunately, there is a handy shortcut. If you pass an array into the set constructor, it will be turned into a set for you, removing all the duplicates without any extra effort. So with this line here, I'm creating set two which is calling the set constructor, but passing in the original array as the data to initialize it with. So now if I look at set two, its contents are the same except the duplicates are gone. Sets are clever enough to remove those duplicates no matter where they are in the list. Once the items are in, we can use the has method to check if an item is actually in that set. So I can see if my set has the number one or I can check if it has 12, which returns false. To loop over the items in a set easily, we can use a for of loop. Just copy and paste this here. We'll use my set two for this because it has everything.
>
> **[4:42](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/set-and-map-newer-loopable-data-types?u=76281980&t=282)** There we go. It loops over every item and returns their values logging the message to the console. Now let's check out a map compared to an object. Clear the console first and here is a bird object with a genus, species and common name. Creating an object is very simple using an object literal like this. To create a map, we use its set method, after we initialize it, of course. That set method has two strings in the parentheses, the key a comma, and then the value for that key. These three lines would create the equivalent map to the bird object. This is pretty verbose compared to an object literal. If you want the order that the items were inserted to be meaningful, though, this extra bit of verbosity should be worthwhile. So I'll copy and execute these lines, and that results in a map with the same structure as our original object. We can use the get method on a map to retrieve a value by its key, which is not possible with a set. Maps do also have a has method that will tell you if a key exists in the map but it doesn't work on values. So has "genus" will return true because there is a key called genus. But if we try the same thing on this value, which is a part of the map but isn't a key,
>
> **[6:16](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/set-and-map-newer-loopable-data-types?u=76281980&t=376)** that's going to return false. If we loop over the items of the map using a for of loop you'll see something a little different than we see with a set. It returns a series of arrays, each containing the key and value. It does not just return the values. A list of arrays in this form is called an entry list and you can get it from a map using its entries method, which looks like this. So it's very similar to what we get from this for loop, just formatted a little differently. Now as it happens, objects can also be turned into entries using the global Objects.entries method. Object with a capital O is a built-in global variable containing, among other things, methods that can manipulate objects. So if we call Object.entries on our bird object, our original just regular bird object, not the map, we get back an array of arrays. This gives us a less verbose way to construct a map from an object literal. So if we wanted to create one more bird map, we can pass this construct into the constructor function for the map, and that creates a bird map from the object without having to use that set method over and over again. That was a lot of pixels lit up about sets and maps.
>
> **[7:51](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/set-and-map-newer-loopable-data-types?u=76281980&t=471)** Hopefully not too much. Both are handy tools to have available. In my experience, you'll see arrays and objects more often in most people's code, but the extra features that these data types give you can prove useful.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (1)
> **Definitions:** is a  (6), is called (2)
> **Analogies:** for instance (1), just like (1), similar to (1)
> **CLI Commands:** find (1)
> **Cross-References:** in the last (1)
> **Speakers:** - [instructor] (1)

#### [While loops](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/while-loops?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/while-loops?u=76281980&t=0)** - [Instructor] In this video, we're going to talk about one more way of looping called the while loop. I have two loops here. One is a sequential for loop, and the second one is a while loop. These basically do exactly the same thing, to prove that I'll copy this and paste it into Node, and so as we can see we're iterating from zero to nine. Just logging out a message each time. I'm going to clear that with control L, and then we'll do the same thing with this Y loop, except in this case, I'm setting a counter separately. We're going to copy and paste this first, just to prove what we're doing. We start at zero and go all the way up to nine. So in this case, it looks like I'm basically sort of breaking apart the pieces of this for loop, where I initialize my counter, and then I say while and in parentheses I have my condition, at which point I'm going to stop. I do some stuff including changing my counter, and this will keep going over and over again until this ending condition is hit. So it appears that I can basically do the same thing with either kind of loop. So this might raise the question, why would I use a while loop when we could use a for loop or vice versa? One reason you might use a for loop, other than personal taste is that it can be shorter and easier to read when you need to stop before going through a whole set of iterable data. With a for loop, you have to write test conditions
>
> **[1:36](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/while-loops?u=76281980&t=96)** and break statements explicitly inside the loop to stop it. With a while loop, the condition that can stop it is right there, right up front. So here's an example of that. I have this array called my list, and it's just full of a whole bunch of booleans. Now of course, you know, I just made this up, so I know what's in there, but there will be many, many times as I've said before in the programs you're writing where you won't necessarily know at a particular time during the program's execution what the contents of a given variable might be. It could be a bunch of data from a database or components on a webpage that people have interacted with. So the current state can't be known precisely, regardless, for now, let's pretend that I don't know what's in this thing. I have a variable, my item here, and then a while loop. So my condition is that as long as my item is not false, we're going to do some stuff. And that stuff is first I'm going to log to the console the state of my array, how many items are in it, and then at the end I'm going to pop one of the items off the array which will take the last item off the array, thereby shortening it. So my list is going to get shorter every single time through this loop. So just to walk through how this would work. The first time in the loop, my item is going to be null, which is not false.
>
> **[3:09](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/while-loops?u=76281980&t=189)** So I do what's in here and finish by popping a value off of my list into my item. The first time I do that, it's true. So I go again, pop again, get another true, go again. But the last time I pop, and these are popping off the end, so the next time I do that, I'm going to get false. And so when I check it, this condition is no longer true, and so the loop will stop, and the statements inside these braces don't execute. If we did this with a for loop based on how long this array is, we'd still get the false value, but we go until we exhausted the whole thing, unless we included a break statement. A while loop can express this intent a little more clearly. Let's actually try it. So we'll bring in my list, our initial value of my item, copy and pasting, and then our actual while loop. And so there we have it. When we start out, the list has one, two, three, four, five, six items, and it will keep going until we pop false. Then it has five items, and then it has four, and the item that we get is false, and then we stop. This really helps to deal with situations where you don't know the contents of a set of data, and you have a condition where you're sure you'll be able to break at some point.
>
> **[4:44](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/while-loops?u=76281980&t=284)** But here's the danger with while loops, really with any loop, but I think it's a particular danger with while loops, it's easy to create what's called an infinite loop. That is one that never breaks or stops. Here's a very contrived example of an infinite loop. We start with a counter of one. So far so good. Then we have a while loop whose condition is true. Nothing is going to change this, it's just true. And so every time through this loop, we would log a message to the console saying what value is in our counter. And then we increment it. Now, I've included a break statement here to make sure that if you happen to copy and paste this, it won't just go ad infinitum, so it will stop. But if you were to comment this out or remove it, every time this loop executes the counter is going to get incremented, but the condition is always true. So this will just keep going and keep going and keep going. It will just print out numbers until there are no more numbers to print out, your machine, or at least your browser or node might run out of memory, or it might say, hey, your script is running away, would you like to abort? Thankfully, with modern computers, we don't have to worry so much about infinite loops taking down the entire system, but it's still something you want to watch out for because it can certainly be a pain to debug. There's one more formulation of the while loop that I'd like to show you which is called the do while loop. Here we have my list again with its list of booleans.
>
> **[6:20](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/while-loops?u=76281980&t=380)** We're going to start my item out with a value of false, and the while condition says do some stuff while my item is not false. With a normal while loop everything inside here would not execute because my item starts out as false, and we have said, while my item is not false, there might be some things to do. But with this do statement, we are guaranteed that no matter what the condition is for the while loop, everything in here will be executed at least once that very first time. So if there's a situation where you're writing a while loop, and you want to make sure that everything in the curly braces is executed at least that one time, you need to use a do while loop instead of just a regular while loop. So now you've seen the while loop and its variation the do while loop. Yet another way of iterating in [[JavaScript]]. Use it with caution because sometimes you could end up creating an infinite loop without meaning to, but it's a great way to do some looping when you don't want to loop over a whole set of data, and you want to make sure your break condition is clear. There's one more form of looping to be discussed later, but first we need to talk about functions.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (1)
> **CLI Commands:** make (3), node (2)
> **Definitions:** is a  (2), is called (1)
> **Warnings:** watch out (1), caution (1)
> **Speakers:** - [instructor] (1)


### 6. Functions

[↑ Back to Table of Contents](#table-of-contents)

#### [Basic functions](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/basic-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/basic-functions?u=76281980&t=0)** - [Instructor] In this video, we're going to begin looking at functions, which are a way of packaging up lines of code in one unit that you can name, invoke, and maintain separately. We've been dancing around the idea of functions a bit in this course. It's kind of difficult to talk about anything in [[JavaScript]] that's meaningful without touching on functions on some level. So I'm glad we're getting to it properly now. Anytime I've talked about methods that are part of objects, those have been functions. And now it's time to talk about making our own. We're going to start out with these four lines of code. Just four lines of logging messages to the console with animal noises, not rocket science, but just bear with me. I'm going to grab these out of the file, copy them, and I'm going to open a browser tab, and open up a JavaScript console. If I'm going to print these animal noises repeatedly, it's easy enough. I can just hit the up arrow here in the browser console. All four lines are brought back all at once. I can hit Return again to execute them. Or, if I'm working in a JavaScript file, I could take these four lines and copy and paste them over and over again. It's not exactly elegant. It does technically work. But if I ever needed to change this, like if I wanted the first line not to be arf, but neigh, say, I would have to go back and change each instance of that in my code and that's kind of tedious.
>
> **[1:32](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/basic-functions?u=76281980&t=92)** What you would do in that case is define a function which would wrap all four of these lines in a single unit that can be named and used whenever you need. Then, if you need to change how all four of them work together, you can just change it in that one place, in that function. Here's how we do that. At its most basic, to define a function, you simply write the [[Microsoft Word|word]] function followed by a space and then the name of your function. We'll call it speak here. Next, a pair of parentheses with nothing in them for now. And then curly braces, in which you're going to put the lines that will comprise the function. This is called the function body. So in this case, I'm going to put in my animal noises.
>
> **[2:26](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/basic-functions?u=76281980&t=146)** Just paste those in. Hit Return and then I can enter my closing curly brace. And with that done, I've defined a function called speak. In order to use this handy new function, we do what's called invoking the function, which is to write out its name followed by parentheses and an optional semicolon. The same way we would end any line in JavaScript. Now instead of writing those four lines out again, if I want to do what those lines do, I just need to execute that one line again. This function is created as a function declaration, but there are other ways to create functions as well, and I want to highlight one now that's also very common, the function expression. I'm going to look in the editor for this one. Here is the same grouping of functionality, but this time, defined as a function expression instead of as a function declaration. This might look like I'm just rearranging things a little bit, and for now, you can think of it that way. Instead of just creating the function directly in this expression, I'm setting a variable and assigning to that variable the function declaration. So the name goes here on the variable instead of after the keyword function. But defined this way, it will work almost exactly the same way. You just need to make sure that you've defined it before you try to use it.
>
> **[4:01](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/basic-functions?u=76281980&t=241)** As we'll see in upcoming lessons, this is not nearly everything you can do with functions, but even if it were, this by itself would be worth knowing. Functions at their most basic can tie together lines of code and give you one executable unit that has its own name and can be executed as many times as you need. It's really useful.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (4), [[Microsoft Word|Word]] (1)
> **Definitions:** is called (1), defined as (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Arguments and parameters in functions](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arguments-and-parameters-in-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arguments-and-parameters-in-functions?u=76281980&t=0)** - [Instructor] In this video, we're going to extend our knowledge of functions using arguments to solve a couple different kinds of problems. The first is taking some data and modifying it. The second is getting an answer to a question in a way we'll need to use repeatedly. We'll start by looking at modifying some data. Let's say I wanted to be able to take any string and change it so it looks like it was being said by Elmer Fudd from the "Looney Tunes" Bugs Bunny cartoons. His manner of speaking changes all R sounds to W sounds. So I have a function called "fuddify", loaded here in Node that does that. We're going to try it out before we look at it. So for example, if I feed that function, "Be very quiet, I'm hunting rabbits," I'll copy and paste that here into my terminal and I get, "Be very quiet, I'm hunting wabbits." And we'll just have a whole bunch of the sorts of things that Elmer Fudd might say. Like, "You screwy rabbit." Comes back, "You scwewy wabbit." and everybody's favorite, if you watch these cartoons, "Rabbit tracks!", "Wabbit twacks!" and it maintains the capitalization of these different letters in the responses. Because I have this function, I don't have to write the lines of code that would do this processing on these strings over and over again. All I need to do is invoke the function and pass all the different strings that I want to pass in. All right, so how does it work? Let's take a look at the definition of this fuddify function. So I have my keyword function followed by the name of the function, in this case "fuddify",
>
> **[1:35](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arguments-and-parameters-in-functions?u=76281980&t=95)** and then inside the parentheses I have what's called an argument or a parameter. The terms can be used interchangeably and both refer to the data that a function is designed to act on. There's only one parameter here called "speech" but there can be as many as you need. Parameters basically acting can be used like variables inside the function body between these curly braces, even though strictly speaking, they're not exactly variables. So I'm passing in a perimeter called "speech" which is probably going to contain a string to be converted, at least a string is what I want. So the first line checks to make sure that it actually is a string using type of, and if it isn't, it will log a message that says, "Nice twy, wabbit!" using console.error, which we can use to report back error messages. Depending on the [[JavaScript]] engine you're using, be it Node or a browser, using "error" instead of "log" here can provide different formatting from the message and it can also give you a trace back of where the error happened in your code, which can be helpful. And after the error, we return from the function, that means to finish and optionally deliver back some kind of result. As used here, just return by itself, its purpose is to just stop the [[Representational State Transfer (REST)|rest]] of the function from executing. Stopping when there's more code that could execute, is called shortcircuiting the function. If this line executes, the rest of the code in the function, which is also known as the function body, is ignored.
>
> **[3:09](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arguments-and-parameters-in-functions?u=76281980&t=189)** So assuming speech is a string, we continue and replace all lowercase and uppercase "R"s using regular expressions and then return the modified speech. Now that we've walked through this, let's try it one more time using a variable which we'll call say, "utterance" and we'll fuddify one more thing, we'll try, "You screwy rabbit" again. We can check the value of utterance. And there it is, "You scwewy wabbit", and by the way, we're using the replace method on this string, reassigning the same variable again with the result of that replacement. We're passing that replace method two parameters. First, the regular expression and then the string that should replace what that regular expression matches. The first instance takes lowercase "r"s and replaces them with lowercase "w"s. And we do it again for uppercase "R"s and uppercase "W"s. The G flag on the regular expression means Global. That will replace every instance of this pattern in the string, not just the first one we might find. We'll see this again later but when I say a method in this context, I mean a function that's part of an object. Here, replace is a function defined on the Global string object. So when I have a string, that method becomes available. Okay? That's how you can take data and process it using a function. Now we'll address the second current use case where we define a function that can return an answer
>
> **[4:43](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arguments-and-parameters-in-functions?u=76281980&t=283)** to a question for frequent reuse. For example, we know that we can determine whether a number is odd by using the modulus operator. So 12 mod 2, for example, return zero. And that's how we know that the number is even. Using that expression directly doesn't make clear what we're trying to do at a glance. It's preferable to create a function with a descriptive name just as we do with variables. In this case, I'm checking whether a number is odd or even so I could define a function called "isEven" and return true for even numbers, false for odds. Here's what that might look like. We can call it a function "isEven" and we'll pass it a number, give us a little more space here, and then we could say if that number, mod 2 is equal to 0, we will "return true". And if not using "else", we'll "return false". Invoking this function with a name that we've defined is much easier to understand than trying to parse out a mathematical expression, even one that's pretty simple. My general guidance for myself and anyone else is don't be shy about creating lots of functions. Indeed, there are ways of programming that result in nearly every single line being wrapped in a function. How deep into that you go is like so many things, personal preference. Before we totally leave this function alone, it could be shortened to just an expression we're evaluating.
>
> **[6:18](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/arguments-and-parameters-in-functions?u=76281980&t=378)** I'll show you what that looks like down here. The condition we're checking evaluates on its own to true or false. So instead of evaluating it and then returning true or false separately, we can just return the expression result directly. You can choose whether you prefer this construction in the code you write, but you should know that it's possible and something many folks will use. Now, we've seen two ways that functions can come in immediately handy when you're working with your JavaScript. One is to process data and return back the modified version and the other is to package up a way to ask a question and get back an answer to it over and over again.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2), [[Representational State Transfer (REST)|Rest]] (2)
> **CLI Commands:** node (2), make (2), find (1)
> **Definitions:** is a  (3), is called (1), known as (1)
> **Analogies:** for example (3)
> **Code Identifiers:** iseven (2)
> **Tools:** terminal (1)
> **Speakers:** - [instructor] (1)

#### [More on function arguments](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/more-on-function-arguments-24352685?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/more-on-function-arguments-24352685?u=76281980&t=0)** - [Presenter] In this video, we're going to look at arguments and functions a little more. We'll see how to define functions with variable numbers of parameters and how to make arguments optional by defining default values for them. First, I'm going to show this function I've defined called speak something. I've pasted in the code defining function ahead of time so we can look at how it works before we actually see that code, which takes two arguments. Your functions don't have to have only one. They can have as many arguments as you need. Speak something can take two arguments. It takes what to say like good morning and how many times to say it as a second parameter. Five times in this case. And when I call this, it says good morning and also tells me how many times it has said it so far. So you could see that you can have more than one argument, but what if you want to provide default values for some of these? What if I know that I want to say good morning, but I don't care how many times, so I can call this again and just leave off how many times to say it. The function is defined so it can be invoked this way. The default value is 10. I can even provide no arguments. When I do that, it provides a default remark, default speech here, and it says it a default number of times. So how do we do this? Let's take a look at the definition of speak something. I have the argument, what, and the argument, how many. To set a default value, we just add an equal sign.
>
> **[1:36](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/more-on-function-arguments-24352685?u=76281980&t=96)** And then the default value here in the parentheses, defining the arguments. In older versions of [[JavaScript]], this functionality wasn't available and you'd have to check if the arguments were of an undefined type, which was pretty awkward. This is much better. When you have multiple arguments like this, the order in which you use them matters. So I can't just invoke speak something 10 or rather I can, but it's just going to say 10, 10 times because 10 will be used as that first argument. So you can provide default values, but the order will matter. Multiple arguments to find in this fashion are called positional arguments because they're positioned in line going into the function, determines which is which. There are more advanced techniques for defining functions that can handle arguments in different orders and quantities. But when you're starting out, it's probably more likely to turn your brain into a pretzel. So we'll just leave that aside for now. We'll come back to my node console and I have another function called adding machine, just like speak something, I pasted in the function definition before starting the recording. It can take any number of arguments at all, doesn't care. So I can add together some numbers. I'll pass it in one, two, and three, and it returns six. Or I can go totally crazy and add a whole bunch of numbers here. Why not? Let's just fill it right up and I still get a result. Let's take a look at the definition of adding machine. Here is that function adding machine.
>
> **[3:12](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/more-on-function-arguments-24352685?u=76281980&t=192)** And you can see in the parentheses, I have no arguments at all. We're taking advantage of an object that every function knows about called arguments. It's an array like object that gives us some of the tools of an array, like a real array, it has a length property, and even with nothing in those parentheses, I have access to any arguments passed into the function. What I'm doing here is using a four loop to iterate over that arguments object and pick up the number that is each individual argument here in this variable called number. And then I'm adding that number to the running total as long as the number actually is a number. So starting from the top, we start with a total of zero. We have a four loop that goes over every single argument. We pull off the number that represents the argument of interest, do a quick type check, and then add it to our total. After all that is done, we've run through all the arguments available, we return the total. You can use the arguments array not just to run through everything that's passed in, but you could also pull out individual arguments using their index. So for example, back in the speak something function, I have these two arguments. What and how many, and I can still specify them by name, but if I wanted to address them as arguments, I could do that with arguments subzero for what and arguments sub one for how many, that would also work.
>
> **[4:45](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/more-on-function-arguments-24352685?u=76281980&t=285)** So now you know that functions can take any number of arguments you need and you can set default values for them. You can name the arguments or you can use the arguments array to address them. And you can use a for loop to iterate over that entire arguments array, allowing completely arbitrary numbers of arguments.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (1)
> **CLI Commands:** make (1), find (1), node (1)
> **Analogies:** just like (1), for example (1)
> **Definitions:** is a  (1)
> **Speakers:** - [presenter] (1)

#### [Objects, references, and functions](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/objects-references-and-functions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/objects-references-and-functions?u=76281980&t=0)** - [Instructor] In this video, we're going to talk about objects and functions, how objects are passed by reference into functions, and why we need to pay attention to that fact. To start off, let's look at this object. It's called calvin. And it has this form, a property called name which is Calvin, bestFriend is Hobbes, and it takes the form of a human boy. Along with this object, I have a function I've written which is called the transmogrifier. This function can take a calvin object and transform it into something else. If it sounds like I'm talking absolute nonsense when I say this, I recommend reading the wonderful comic books Calvin and Hobbes by Bill Watterson. It's fun for all ages. Anyhow, let's take a look at how this function works. I'm going to pass in this calvin. And then I'm going to look at the object again. And I can see that Calvin has been transformed from a human boy into a grey wolf. Let's take a look at the definition of this transmogrify function. It accepts this calvin object and we do a little type checking at the top here to make sure that the argument is an object. When you're writing your functions, it's a good idea to make sure that the input you're accepting into the function is of a structure that you expect, otherwise your code later could break down and you'd have a lot of debugging to do. This is especially important if you're writing a function that could accept input directly from a user, not from you the trusted programmer. But in any case, for now we're dealing with just simple type checking,
>
> **[1:33](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/objects-references-and-functions?u=76281980&t=93)** making sure that this thing coming into the function is an object. And if it isn't, we're logging an error to the console and returning. After that, we generate a random number between one and five. To do that, we use the math objects random method here. That will give you a number between zero and one. Multiplying that times a number will give us a number that's between zero and one less than that number we provide, which in this case is five. We use math's floor method to round that number down basically trimming off anything after the decimal point. And finally we add one, ending up with a number that is between one and in this case five. Once we have that random number, we're just running a switch statement. And for any of these five numbers that might be produced by that random number generator, we're changing the form of calvin into one of these five [[Forms]], tyrannosaurus, grey wolf, and so forth. This algorithm could be modeled differently and made shorter, but this way keeps everything pretty clear using what we've learned so far. Now we need to notice something about this function. There's no return statement at the bottom. Why is that? The reason is that the argument that's being passed in is an object and objects are passed into functions differently from other types of data. Instead of a simple value being passed in a reference to the value is passed in. And when I say a reference, that means the object I've created has a location in the computer's memory,
>
> **[3:07](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/objects-references-and-functions?u=76281980&t=187)** and when I pass it into this transmogrify function, I'm passing in not a copy of the object that's totally separate from the original but a copy of the object's location in memory. Then in my function, these lines that change the form are actually operating on the original object because the Calvin parameter points to the data referenced by the original calvin object. This all generally follows the programming model of function parameters being passed by reference as opposed to being passed by value. Stripping away the subtleties with references you're acting on the original data and with values you're dealing with copies. So what happens when I run this transmogrifier function and pass calvin into it? As we saw he starts out as a human boy, and then after having run it, we would check this calvin value and see that the form has changed. This is important because there may be times when we actually don't want the original object to be modified. You might want a copy to come back out of this function instead. So in that case, we would have a version of the function where instead of passing a calvin object in as we did previously and just modifying it in place, we might want to set up a new calvin object that gets returned from the transmogrifier leaving the original alone. And I have a version of the function that does just this. There's a definition of the calvin object up here at the beginning of the file and I'm going to just change this to newCalvin and copy it and bring it in here to Node.
>
> **[4:41](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/objects-references-and-functions?u=76281980&t=281)** I'll just clear the console with Control + L, paste it in. The other version of this function is called transmogrifyCopy. We'll pass in newCalvin to this function. And so we can take a look at the original newCalvin which remains a human boy, but the result of the transmogrifyCopy function is a new object where he has taken the form of a grizzly bear. So this version does not modify the contents of the original variable. Going to minimize the terminal. And we'll split this editor so we can look at the two functions side by side. Here's transmogrifier and here's the transmogrifier Copy. As we can see, a lot of the code is the same. We start with the same type checking in both. We have the same random number generator. But in the copy function we're saving a copy of the form here, calling it newForm. And then after we have our random number generator instead of modifying the calvin object directly we're just changing this newForm variable. It's one of these various animals. And then at the end, we create an entirely new object by copying in the values of the name and bestFriend properties into this new object, and the form is the newForm. And then when it comes time for the function to end, we just return that new object
>
> **[6:15](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/objects-references-and-functions?u=76281980&t=375)** which has the same name, same bestFriend, but the new form. That's one way we can get a copy out of this function instead of modifying the original object. Now note that this business of passing a copy of a reference is not just for objects that are clearly objects. Ones that look like this object literal up here. This principle also applies to arrays which you may remember are really objects in [[JavaScript]]. So if you pass an array into a function and you modify that array, you're modifying the properties of the original object. Always keep that in mind. So to sum up, when you're working with functions and objects, be careful about whether you're passing in and modifying the original or whether you want to get a copy back instead. Write your functions accordingly to make sure that you're getting back what you expect.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Forms]] (1), [[JavaScript]] (1)
> **Code Identifiers:** bestfriend (3), newcalvin (3), newform (3), transmogrifycopy (2)
> **Definitions:** is an  (3), is called (2), is a  (1)
> **CLI Commands:** make (3), node (1)
> **Warnings:** note that (1), be careful (1)
> **Cross-References:** as we saw (1)
> **Tools:** terminal (1)
> **Prerequisites:** set up (1)

#### [Functions are objects](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/functions-are-objects?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/functions-are-objects?u=76281980&t=0)** - [Instructor] In this video, we're going to talk about how functions are first class citizens in [[JavaScript]]. Indeed, they are objects that have the power to be invoked. Let's talk about what that means and look a little bit at how useful it can be. Here's a definition of a function called "speakSomething". We've seen this kind of construction before, where we have the keyword "function" followed by space, followed by the name of the function, any arguments and its definition. And so all this does is take this "what" argument and then log it to the console 10 times using a "for" loop. Written this way, it's called a Function Declaration. This function can be rewritten another way that we looked at briefly in the first video of this chapter. In this version, I'm creating a variable called speakSomething and then I'm assigning to it what's called a Function Expression. In other words, the function code is evaluated and assigned to that variable, the same way that any other expression can be evaluated and assigned to a variable. The actual function definition inside the curly braces is the same. Something else that's different here is that the function expression, here, has no defined name. This is often called an Anonymous Function and some languages call it a Lambda. This notion comes up in a lot of languages. For our purposes right now, these two constructions are basically equivalent. Functions defined either way are invoked the same way, just write out "speakSomething" and invoke it like that. But they illustrate the idea that a function is actually an object. It's a data type that can be passed
>
> **[1:32](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/functions-are-objects?u=76281980&t=92)** around as a value in JavaScript. One of the things that makes this interesting is that you can feed functions into other functions as arguments and why would you do that? I'll give you an example. When you're working with JavaScript on the web, there's a Global window object, and in Node, a Global object is actually called Global. Each Global object has many methods and properties available, more or less tools to play with. In both browsers and Node, one of them is setTimeout, a function that takes for its first argument a function. setTimeout will set a timer after which a function that you pass in will execute. So for that first argument, it takes an actual function. Let's say I want to run speakSomething after a certain amount of time. I'd feed it as the first argument into setTimeout like this. It doesn't matter which way I've defined speakSomething, whether it's as an expression or a declaration. I can pass it in either way by name. Note that I'm not including the parenthesis here because I'm not invoking it at this point, setTimeout will do that later. Then the second parameter for setTimeout is the number of milliseconds after which I want this function to execute. In this case, 5,000 milliseconds means five seconds. If this is a new concept, it might seem quite odd at first, even if you've written code in other languages before. You're actually taking a function and passing it as a value. All functions are objects in JavaScript. They actually inherit from a Global Function object which is spelled like this with a capital F,
>
> **[3:08](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/functions-are-objects?u=76281980&t=188)** but that's just a technical detail. The important thing to note is that functions are objects and you can do stuff like this with them. Now let's try it out. I'll open up a terminal and start Node and I'll pick one of these ways of defining speakSomething. I'll copy and paste that in and then we'll bring in our setTimeout. And before I paste this in, I noted that there is a Global object and setTimeout is defined on that Global object. So you can invoke it like this, but because it's Global, you can also just write out "setTimeout" by itself as we have in this file. So now if I run setTimeout, five seconds later, I get that result. Function expressions also allow us to create objects with methods just like the Global object that setTimeout appears on or the log method on the console object. I could define an object like this. I'll just call it "obj" and then I could add a property to it called "sayHello" which for now is the only one here. For its value, I can use an anonymous function, which does nothing, but in this case, log, a message, "Hello" to the console. So you can see I'm using a function as a value again and if I copy this object into Node, now I can invoke "obj.sayHello" and it logs back that message, "Hello".
>
> **[4:45](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/functions-are-objects?u=76281980&t=285)** So the key lesson here is that functions are objects and they can be passed around as values, just like any other value in JavaScript. This comes up constantly in modern JavaScript as more developers have come to understand how function oriented JavaScript is and really start to leverage that power. You may not use it yourself in your initial forays into using the language, but you'll certainly see it in the code that other people write.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (7)
> **Code Identifiers:** settimeout (10), speaksomething (6), sayhello (2)
> **CLI Commands:** node (4)
> **Definitions:** is a  (2), in other words (1)
> **Tools:** notion (1), terminal (1)
> **Analogies:** just like (2)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Jargon: Scope in JavaScript](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/jargon-scope-in-javascript?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/jargon-scope-in-javascript?u=76281980&t=0)** - [Instructor] It's time for a brief digression to deal with a little jargon you might hear about. [[JavaScript]] is super popular and draws a lot of attention from developers of many skill and experience levels. So knowing where some jargon comes from can help you with possibly boring parties. Here, we're going to talk about scope. Scope refers to where variables are defined and what parts of our code can access the variables that we define. Often when we talk about scope, we differentiate between global scope and local scope. Global refers to variables that are defined in such a way that they are accessible anywhere. Local means that a variable is accessible in a way that's more limited, perhaps only inside a particular function. One piece of guidance almost every beginning programmer gets is to avoid creating global variables. Global variables are accessible everywhere, which is very convenient, but the global name space as it's often called, is a resource that's shared among all scripts that are in use at any given time, and there may be a lot of those variables. Global variables can be accessed, but also modified by any piece of code. So your little island that you're working on could have bugs introduced by someone else's code if you're both trying to use a global variable with the same name. Unfortunately, in JavaScript it's easy to create global variables. If I just create a variable like this with no var keyword in front of it, it will always be a global variable. Unless, I've enabled strict mode by putting this string at the top of my file, in which case, I'll get a warning, but the point is, it's easy to do this accidentally.
>
> **[1:35](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/jargon-scope-in-javascript?u=76281980&t=95)** To properly create variables, JavaScript uses the keywords, var, let, and const. With var, you create a variable whose scope is determined by the function in which it's defined. This will keep the variable in local scope about which we'll talk more in the next video. In [[ECMAScript]] 2015 and above, there are other keywords that can create locally-scoped variables, let and const. These also create local variables, but they can be scoped inside blocks of code, even down to something as small as an if statement. Basically, their scope is delimited by their enclosing curly braces. With let, a variable you create can be reassigned, much like var. With const, you create a variable that can't be changed, although sneakily, an object created as a constant can potentially have its properties inside changed. We'll get into more detail about scope with upcoming code examples.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (3), [[ECMAScript]] (1)
> **Definitions:** refers to (2), means that (1), is a  (1)
> **Cross-References:** in the next (1)
> **Warnings:** warning (1)
> **Speakers:** - [instructor] (1)

#### [Functions and scope](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/functions-and-scope?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/functions-and-scope?u=76281980&t=0)** - [Instructor] In this video, we're going to talk more about variable scope and how functions relate to it. Here's an example. I'll create a variable called myNum and set it to 32 and another one called myResult which will set to success. Then I can write a function, or in this case, I'm going to copy and paste a function. It's called randomizer and it takes a limit as its argument. Inside the function, I create a variable called randomNumber and assign A randomly generated number to it. Then I create another variable, which is called myNum, just like the one we created out here originally, outside the function, and then assign that the value of the random number which is between zero and that limit that was passed in. Then we're going to log a couple of lines before returning myNum. So back here, I set myNum to 32 and myResult to success and now we're going to invoke this randomizer function, passing in 10 as the limit. This is going to set myNum here inside the function to some random number between zero and 10. So the question is, what's going to happen here? We have a copy of myNum before the function and one inside the function. Are they the same? Upon executing this function,
>
> **[1:33](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/functions-and-scope?u=76281980&t=93)** looking at the messages, we can see that the version defined inside the function has a value of three and then the global one keeps its value of 32. To differentiate them, we use the global object from node which is where any variable that's not defined inside a function could appear. It's a property of the global object, as well as just being a variable. What we're seeing here is a difference in the scope of these two versions that are both called myNum. Functions in [[JavaScript]], along with everything else they do, create scope. This version of myNum inside the function is its own separate variable even though it uses the same name because that name only applies inside this function. If I hadn't used var to define it, I would've been resetting the value of the original global variable, not the one that's inside the function. In this other function, doubleIt, I can reuse the same variable name again as long as I use var. The version inside this function is boxed off from the previous one. Of course, you might also have noticed that this randomizer function also references myResult. That variable is not defined in this function, but because it's global, also known as being in the global scope or global namespace, I can still refer to it here even though it was defined externally. And again, to refer to these two versions of myNum, if I just use myNum by itself inside the function,
>
> **[3:07](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/functions-and-scope?u=76281980&t=187)** it's going to pull the local version and I need to use the property on the global object to get the global version. And know that global object is called global and in web browsers it's called window. At the top of this file, there's a little snippet of code that will allow you to use all of the code in this file, either in a node context or in a web browser context by just making sure that they have the same value in either context. Any local variables that I create inside a function are inaccessible outside the context of that function, outside the scope of that function. So even after executing this randomizer function, that random number variable is undefined because it only exists inside the function. Functions are one of the main delimiters of scope in JavaScript classically, but with [[ECMAScript]] 2015, the let and const keywords let you create variables that are scoped to any block of code, not just functions. This little block illustrates that point. I have this very topological block that will always execute because one does indeed always equal one. Inside this block, I'm defining a constant with the keyword const whose scope is the curly braces here and here. This console log is within those curly braces. So if we were to execute this, we can see the value. oneIsOne was set to Yes indeed and here it is right here logged from that block.
>
> **[4:43](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/functions-and-scope?u=76281980&t=283)** If I try to execute that same line outside the context of that block, we get a reference error because that variable that I've tried to refer to doesn't exist in the global namespace. It only existed inside those curly braces. And that concludes our brief look at scope in JavaScript. Var creates variables scoped to functions. Let and const create variables scoped to blocks.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (3), [[ECMAScript]] (1)
> **Code Identifiers:** mynum (10), myresult (3), randomnumber (1), doubleit (1), oneisone (1)
> **Definitions:** is called (2), is a  (1), known as (1)
> **CLI Commands:** node (2)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Writing shorter functions with arrows](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/writing-shorter-functions-with-arrows?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/writing-shorter-functions-with-arrows?u=76281980&t=0)** - [Instructor] Writing functions doesn't take a ton of keystrokes, but in this video we'll explore arrow functions, which are part of [[ECMAScript]] 2015 that allow functions to be defined with fewer keystrokes. [[JavaScript]] is very function-oriented. Indeed, you can use it in a formally functional way, which pretty much means everything would be done via functions. As a result, being able to define a function with less typing is pretty handy. Let's look at this speech function that prints a few animal noises. This is written as a function expression assigned to a constant called speak. This is just like what we've seen previously. We have our function keyword. It's unnamed because we're assigning it to this variable and then we have our curly braces with the things we want it to do. With an arrow function, we can save even having to type that [[Microsoft Word|word]] function. We just leave it off. But in order for this to be recognized as a function, we need to add an equal sign followed by a greater than sign, which looks a little bit like an arrow. Otherwise, it's the same. So in a nutshell, that is an arrow function, but there are a few more details that we'll look at. These arrow functions aren't always the absolute shortest way to define a function, at least not immediately. In this example, we have an is even function declared and it is quite short. To rewrite this as a basic arrow function, we actually end up with a few more characters. You can sort of see this sticking out a little bit further on its line.
>
> **[1:33](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/writing-shorter-functions-with-arrows?u=76281980&t=93)** However, along with being able to remove the word function, you can potentially remove even more. If the function is only returning a result, not requiring any other work on more lines of code, you can leave off the curly braces and the word return. That results in a one-liner. So our parameters are here in a set of parentheses. We have our arrow and then here's the expression that should be returned. We have no word return. If you're doing other work than just returning something simple, you'll want to keep those curly braces in and actually return something with the return keyword. For what it's worth, this form took me a little while to get used to as a longtime user of the previous method of defining functions, and yet this still is not the absolute shortest it could be. If there's only one parameter, you can even leave off the parenthesis here. The code formatter that I have installed in this code space, prettier, prefers to keep the parameters, even if they're optional, which is why I have this prettier ignore comment here before that line. I prefer to leave the parenthesis in for readability, but you'll probably see the absolute shortest version like this in code out in the world, so it's important to know how to parse it. Arrow functions are a handy way to write functions with the fewest keystrokes. They can be used for nearly anything a regular function expression can be used for. They have a couple subtle differences from regular functions that will be worth looking into as you gain more experience, but for now,
>
> **[3:06](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/writing-shorter-functions-with-arrows?u=76281980&t=186)** they're a useful feature you can expect to see in the code of others and most likely in your own, as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft Word|Word]] (4), [[ECMAScript]] (1), [[JavaScript]] (1)
> **Definitions:** is an  (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Three little dots collect the rest](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/three-little-dots-collect-the-rest?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/three-little-dots-collect-the-rest?u=76281980&t=0)** - [Instructor] In our efforts to become conversant in [[JavaScript]], there's a bit of newer syntax that we might see that's going to appear a little bit mysterious. It looks like an ellipses, but it's actually three dots typed as three periods or full stops in the parenthesis of a functions declaration. This is called [[Representational State Transfer (REST)|rest]] parameters, and the result is an array of arguments. Previously, we've seen the array like object called arguments but this is an actual JavaScript array. So along with the length property it has all the methods that regular arrays have for sorting and otherwise manipulating the items. That might not be something you'll put into use very often but it's good to know. Here's the adding machine function we saw previously rewritten to use rest parameters. Because it's the only parameter in the function, all arguments are captured in terms here and can be looped over just like we did previously with the arguments object. For simple cases like this, neither one is better or very much different. The main difference with rest parameters is that you can use them in addition to regular positional arguments. Here's a bake function for cakes with named parameters for temperature and time and a rest parameter for flavors. So let's copy this and bring it into Node. Now we can try running it a few ways, save a little typing, I have these ready to copy and paste. I can run it with everything, including many flavors. And if we compare the result
>
> **[1:38](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/three-little-dots-collect-the-rest?u=76281980&t=98)** to what we're expecting in the function body, we can see that it's going to say let's bake the cake for the temperature degrees for the amount of time in minutes. Then here's our rest parameter for flavors and if there are any in the array, it's going to log a message with all of them included. You can see it comes through as a regular array. And by comparison the arguments object contains everything that was passed in including our named parameters. So that's what it looks like if we include everything. If we include just one flavor, note that this time, even though there's only one extra parameter, flavors here is still an array. It's just an array with one item. And of course, if I run this with no parameters my default values are used for temperature and time and flavors and arguments are both empty. This construction allows me to provide names for parameters that I'd like to have names for, but provide additional options if I want them. I could do this with the arguments object but that will include every argument named or not. So if I wanted to do something only with the extras, I would've to remember to skip the first two arguments like we saw up here. With rest parameters, I don't have to make any special effort. There's a little something extra I want to mention in this function as well. It's here on these first two console lines. They're using a different way to build a string
>
> **[3:11](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/three-little-dots-collect-the-rest?u=76281980&t=191)** which is called a template literal. These are delimited by these back ticks which are in the upper left corner of American keyboards to the left of the number one, but might also be found in the upper right or elsewhere depending on your keyboard layout template. Literals make it easy to insert other values into a string as we're doing here with the temp and the time parameters. Inside those back ticks, you just include included dollar sign and curly braces to set apart your item of interest. The second message also uses slash "n" to insert a carriage return in the output so all the messages don't appear one right after the other. You can see that right here. With rest parameters, you have all the power of the arguments object we've seen before with the flexibility of letting it contain only the subset of parameters you want to collect together. The fact that it's a real array, might also come in handy. The three dot syntax comes up in other places in JavaScript so it can be a little tricky at first, but this version is very common and relatively easy to understand.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Representational State Transfer (REST)|Rest]] (7), [[JavaScript]] (3)
> **Definitions:** is called (2), is an  (2)
> **CLI Commands:** make (2), node (1)
> **Exercise Files:** template (2)
> **Analogies:** just like (1)
> **Best Practices:** remember to (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Callback functions and looping](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/callback-functions-and-looping?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/callback-functions-and-looping?u=76281980&t=0)** - [Instructor] In this video we're going to talk about callback functions. We've seen these in use a couple of times but I want to give them a little specific attention because they're used a lot in modern [[JavaScript]]. Callback function is a piece of jargon, meaning a function that's passed as an argument, into another function and executed in that function. Because functions are first class citizens in JavaScript, you'll see this all over the place. Here's a function called doubleIt which takes a number and returns it multiplied by two using the multiplication assignment operator. And then here's a variable called myNumbers with the digits one through five in it. A start up node down here. And let's bring these in. Copy and paste so we have them available. JavaScript arrays have a method called map that takes a callback for the parameter. That callback has one required parameter itself, the item in the array that we're acting on. That's represented by number in this doubleIt function that we're going to use as a callback. The callback function is then executed on every item in the array, in turn, creating a new array with the result of that execution. So by running this line here, we're taking the myNumbers list and mapping onto it the doubleIt function, which is going to double every item in turn. And then we can take a look at what myDoubles contains. All the same numbers, just each one is doubled. As long as the callback has its arguments set up correctly,
>
> **[1:35](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/callback-functions-and-looping?u=76281980&t=95)** this works great. The map method on arrays is used a lot in modern JavaScript frameworks like [[React.js|React]] for rendering lists of items in a user interface. So instead of taking a list of numbers and generating a new list of doubled numbers, you would take a list of some kind of data and turn it into a list of user interface components. Another example of an array method that takes a callback is forEach. ForEach doesn't return anything. It just iterates over an array, passing each value from the array as the argument. It's a shorter way, perhaps a more expressive way, of looping over an array than a four loop. It's not as easy to stop as a four loop, the brake keyword doesn't do anything in this context, but you might find it more pleasant to use. So we can take this and try it out. And this is executing again, against, the original myNumbers list here and so after I execute it, it just loops right over them and prints out each one. In this example, I'm using an anonymous callback function inserted directly as the argument instead of defining it in advance like we did with doubleIt and this just logs that value. The anonymous function can of course also be rewritten as an arrow function to save a few keystrokes. One more example of callback functions that's very common in real world usage is the event handler. These are functions called when something of interest happens and your code would like to respond to that event. Events can be things like the click or tap of a button, text being entered into a form field, the window scrolling and much more. This snippet is an example showing
>
> **[3:08](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/callback-functions-and-looping?u=76281980&t=188)** how one might handle the event of text being typed into a form field. This is code that would actually work but you'd need a webpage to use it with and of course a web browser. We don't have to go over all the particulars right now, just enough to make the intent clear and show the use of the callback. So first, we would collect the text field of interest from the page. Next, we would call addEventListener to wire up a callback function to the keyup event taking place on that text field. The callback function here is an anonymous arrow function just a set of open parens, the arrow, and then the braces to enclose the body. This function will be triggered every single time the user types something into that form field. In this bit of code all that will be done is logging a message to the console, but you get the idea. The last thing I want to note about callbacks is this. If you have a scenario in which a callback function can be added don't assume it's the only one, especially on the web, with event listeners, there may be lots of callback functions listening for and handling different types of events. You need to assume that the code you write is usually one of many players in the overall game. That concludes our look at callback functions which are just functions passed to other functions for later execution. They can be executed on arrays as part of loops, in event handlers, and can generally be used to add powerful customization in the code we write and use.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (4), [[React.js|React]] (1)
> **Code Identifiers:** doubleit (4), mynumbers (3), mydoubles (1), foreach (1), addeventlistener (1)
> **CLI Commands:** node (1), find (1), make (1)
> **Definitions:** is an  (2), is a  (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)


### 7. A Few More Advanced Pieces

[↑ Back to Table of Contents](#table-of-contents)

#### [Asynchronous code: Waiting is the hardest part](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/asynchronous-code-waiting-is-the-hardest-part?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/asynchronous-code-waiting-is-the-hardest-part?u=76281980&t=0)** - [Instructor] In the next several videos, we're going to talk about some pieces of [[JavaScript]] that are more on the advanced side, but that I think are important enough to introduce conceptually. In this video, we're going to talk about the term asynchronous. When we think about giving instructions to a computer, we generally think linearly. Here's step one, followed by step two, and so forth. We don't move on to step two until step one is completely finished. This is very easy to follow conceptually, and a lot of code that we write follows this mode of thinking, which is all fine and good. Code written this way is said to be synchronous with every step blocking the ones that come after it. Step three doesn't run until step two is totally finished and so on. But what if, say, step four is a step that takes a while and involves waiting for something that steps five and those after don't need right away? In order for the entire task to be complete, step four does need to be finished, but it doesn't need to be finished before step five can start. If we're baking a cake, for example, we need to get that cake in the oven so it can bake, but we don't have to wait for the cake to be finished before we can make the frosting. We need the cake and the frosting to be finished and cooled before we can put them together and call the whole job done. But if we waited for the cake to be baked and cooled before even starting the frosting, it would just take much longer. Code involving steps that can be run at the same time with the results of some steps coming in later
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/asynchronous-code-waiting-is-the-hardest-part?u=76281980&t=94)** is called asynchronous because some steps can be run in a way that doesn't block others. The results will be needed eventually, just not immediately, so we can let other steps happen while waiting for the stuff that takes a while. And when I say a while here, I mean it in computer terms. In other words, a delay that a human might consider very brief, a couple of seconds, or even a fraction of a second can be slow enough that working asynchronously is noticeably better. The main situation where we need to work asynchronously is when our code needs to load data from somewhere, usually somewhere on the internet. This could be loading some inventory data from a store's back [[Microsoft Office|office]], a photo from a catalog, anything where our code needs to make a request. If you're using node, loading files from disk is an asynchronous operation by default because disks can be relatively slow. We always assume that the network or other sources of data can be slow sometimes, and if all the code on a page had to wait for that data to load, everything would feel slow and unresponsive, which is bad. As you start writing intermediate to advanced JavaScript code, you will almost certainly need to deal with various ways of writing asynchronous code. In the next video, we'll look at some examples.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2), [[Microsoft Office|Office]] (1)
> **Definitions:** is a  (1), is called (1), in other words (1), is an  (1)
> **CLI Commands:** make (2), node (1)
> **Cross-References:** in the next (2)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Promises, async, and await](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/promises-async-and-await?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/promises-async-and-await?u=76281980&t=0)** - [Instructor] In this video, we're going to look at various approaches to writing asynchronous code, so you'll recognize these approaches in the wild. We'll look at using callbacks, promises, and the new async and await keywords. Here's some example code that uses these three approaches. The first one uses the get method from the jQuery [[JavaScript]] library which is a function that takes a URL as its first parameter and a callback function as its second. That callback gets the response from the server as its argument which you can then act on in the body of the function. These calls are to [httpbin.org](httpbin.org), a real website that provides an API for testing just this sort of code. This works fine for a single call but if there's another request that depends on the first one and maybe more after that, you end up having to nest all these callback functions within one another, and it gets to be a bit of a mess to write, and can be hard to understand if the functions get at all long. Programmers call this sort of situation "Callback hell." Promises are a much more pleasant way to work. Promises are objects that capture the result of an asynchronous action with a particular programming interface or API for handling when the promised data successfully comes through, or failed to, for some reason. Axios is a popular library for making network requests and returning promises, which looks like this. I make a request using Axios' get method, but then instead of a callback,
>
> **[1:33](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/promises-async-and-await?u=76281980&t=93)** I directly chain on a method called then, which is part of the promise specification, which then contains a callback where my data comes back. This works because Axios get method returns a promise and promises have a method called then. Inside that callback function, if I need to make another request, I can return another promise allowing me to chain another then method at the same level as the first one. I know this might sound like a lot right now but the point is that this is how most of the better code involving network requests is written, and I want you to be able to recognize it when you see it. Browser support isn't perfect, but there are lots of JavaScript libraries we can add onto our projects to polyfill or patch in support of the parts of the promise specification that your older browser of interest doesn't necessarily support out of the box. But these days, most browsers support promises without any additional code needed. And finally, there's async and await, which are part of [[ECMAScript]] 2017. These make working with promises even easier to read. Functions can be marked as asynchronous with the async keyword, and then network requests can have their return values stored directly instead of being handled inside a callback function. All you need to do that is use the await keyword. This can really shorten code that makes a lot of network requests, like get lots of things over here. This is relatively new, and many developers are still coming to grips with it,
>
> **[3:06](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/promises-async-and-await?u=76281980&t=186)** but many browsers and the current version of Node all support this, so it's worth being aware of. So now you've seen some examples of asynchronous JavaScript in the form of old school callbacks, regular promises, and then the new async and await.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (3), [[ECMAScript]] (1)
> **CLI Commands:** make (3), node (1)
> **Env Vars:** api (2), url (1)
> **Documentation:** specification (2)
> **Definitions:** is a  (2)
> **Code Identifiers:** jquery (1)
> **URLs:** [httpbin.org](httpbin.org) (1)
> **Speakers:** - [instructor] (1)

#### [Object-oriented JavaScript: Prototypes and classes](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/object-oriented-javascript-prototypes-and-classes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/object-oriented-javascript-prototypes-and-classes?u=76281980&t=0)** - [Guide] In this video, we're going to talk briefly about object-oriented [[JavaScript]] because the term object-oriented appears often in programming, so it's good to have a bit of exposure to it and what it means for JavaScript. We've seen JavaScript objects already. They're a generic way to bundle up data that belong together, but when we talk about [[Object-Oriented Programming (OOP)|object-oriented programming]], we're talking about the same thing but particularly at a concept level: being able to reuse code by setting a template for a type of data, and then every time I need to use it, getting an instance of that template. Different languages handle this concept in different ways, especially in the way objects can be related to one another. Let's say I wanted to make a cake template, which has some features like the number of layers, the primary flavor like chocolate, [[Vanilla]], or lemon, and some functions like bake and cool; all the things that any cake is going to need. Anytime I want to make a cake programmatically, I could get an instance of my Cake object and be off to a good start just by writing code for whatever features are unique to that instance. If I'm going to make a lot of chocolate cakes, I might want an object called ChocolateCake that inherits the shared features of the base object Cake but has its own features that are more specific than Cake but general enough to be reusable in all chocolate cakes, like making its primary flavor chocolate by default so I don't have to set it every time. The relationship between the original object and other objects that are derived from it
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/object-oriented-javascript-prototypes-and-classes?u=76281980&t=94)** is called inheritance, and the way inheritance works is central to any object-oriented language. So the question is what mechanism is used to relate these objects? The way JavaScript works is different from most other popular languages. It uses a model called Prototypal Inheritance where every object has a link to a parent object from which it inherited some data and functions and that parent has the same link to its parent and so forth. So ChocolateCake inherits from its parent, Cake, which inherits from the global Object. These links are called __proto__ internally, and comprise what's called the prototype chain. This topic is one that's worth looking into in more detail as you become a more proficient developer, but in terms of our goal of making you conversant and recognizing common code idioms, I want you to recognize something like this. This is how you would add a method to an object using its prototype, which makes it available to all objects that use that prototype wherever they are down the chain. In this case, we're adding a function called Bake which takes a temperature and a number of minutes. Once it's defined this way, any cake or any chocolate cake would have access to that function and can invoke it the same way. In [[ECMAScript]] 6, there's a way of building these objects that looks more like the syntax of other programming languages, particularly the class keyword.
>
> **[3:08](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/object-oriented-javascript-prototypes-and-classes?u=76281980&t=188)** Along with being a little less verbose looking, it allows developers with previous object-oriented experience to feel more comfortable in JavaScript. That said, ultimately, this will still get turned into the prototype based declaration we saw before. Because these are enhancements to the way the language looks but don't really change how it works underneath, additions like this are often called syntactic sugar. There's a lot more to learn about object-oriented JavaScript if you're interested, but this quick overview gave you a few things to look for as you're developing familiarity with the language.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (6), [[Object-Oriented Programming (OOP)|Object-oriented programming]] (1), [[Vanilla]] (1), [[ECMAScript]] (1)
> **CLI Commands:** make (3)
> **Exercise Files:** template (3)
> **Definitions:** is called (1)
> **Speakers:** - [guide] (1)

#### [Jargon: Strong vs. loosely typed](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/jargon-strong-vs-loosely-typed?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/jargon-strong-vs-loosely-typed?u=76281980&t=0)** - [Instructor] Programming languages are categorized in lots of ways, one of which is whether they're considered strongly or loosely typed. This thankfully does not refer to how hard you hammer on the keyboard when writing your code. [[JavaScript]] is loosely typed and I want to give you a little information on what that means and why at some point you might care. I'm bringing up loose versus strong typing because it comes up with variables. In JavaScript, you've seen that we can create a variable, assign it a number, and then turn right around and assign it a [[Microsoft Word|word]], a string later. Being able to reuse variables however you want makes JavaScript very flexible. Very experienced programmers can have strong and different opinions on whether this is good, but either way, JavaScript is a loosely typed language. In contrast with a strongly typed language, broadly speaking, your variables know about and enforce what kind of data they can store. The language will trigger some sort of error if you try to say put a number where you originally had a string. This can feel constraining, but it does help avoid bugs, especially in larger programs and applications. Let's say you have a variable called author name that will be used later in some way that makes sense for a person's name. If you later assign a number, your program might do something weird later because you fed it a number instead of a name. A strongly typed language would flag this for you and note that it could be a problem. One of the most popular solutions to this issue is TypeScript, which is JavaScript but with some extra features added on, like stronger typing.
>
> **[1:37](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/jargon-strong-vs-loosely-typed?u=76281980&t=97)** Many large JavaScript projects and frameworks write their original code in TypeScript, and transform it into JavaScript for running on the web. So you may see this discussed or become curious about it at some point, and I wanted you to know about it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (7), [[Microsoft Word|Word]] (1)
> **Cross-References:** later in (1)
> **Definitions:** is a  (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Modern JavaScript tooling](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/modern-javascript-tooling?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/modern-javascript-tooling?u=76281980&t=0)** - [Instructor] In this video, we're going to talk about some of the popular tools in the [[JavaScript]] ecosystem that help with various common tasks. When you're just starting out, you don't really need any of this stuff, but if you want to get into JavaScript professionally at some point, I want you to know about these tools so you can start exploring them in more detail. JavaScript powers so many things on the web and elsewhere. Some projects are enormous, involving millions of lines of code and are handled by teams of dozens or hundreds of people. Open-source JavaScript projects can have contributors of many experience levels and all over the world. With a continual maturation and proliferation of JavaScript, it was inevitable that developers would start writing tools to help solve various issues that come up. One issue is how to handle JavaScript libraries and modules. As projects grow, it makes sense to start splitting files into smaller ones that contain related code. In an object-oriented project, there could be files for each type or class of an object or just a file containing all the helper functions you've written. Once you've broken them up, you need a way to load the ones that you need. Browsers load scripts with script tags, but managing those manually can be a pain. What if script A doesn't work without script B having been loaded first? And oh, by the way, what if script B needs script N first and so on and so forth. Until recently, there was nothing in the official JavaScript spec that specified how to handle loading shared JavaScript libraries. That specification exists now called Dynamic Imports, but browser support isn't super good right now
>
> **[1:34](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/modern-javascript-tooling?u=76281980&t=94)** and the problem has been around much longer than the specification has existed so tools have been written to handle this. Some of the most popular ones are probably Webpack, Rollup, and Vite. Module loading is just a small part of what these tools can do. They have plugins that can handle lots of other tasks like removing comments and white space from code so it takes up less space and can be loaded faster and tons more. If you start using a JavaScript library like [[React.js|React]] or [[Vue.js|Vue]], you'll almost certainly start using one of these tools. There are also tools called Package Managers for managing the installation of JavaScript libraries. If that doesn't sound important to you, think about a simple example, installing a jQuery slideshow plugin. If you don't already have jQuery, you'll need it and then you'll need a slideshow plugin. Then maybe you need a plugin to support touch input for mobile devices. For something small, that's still a lot to download and deal with manually. To help with this, there exists [[npm]] and Yarn which handle downloading JavaScript libraries and packages, making sure to install everything you need in a standard folder structure. Webpack, Rollup, and Vite are all aware of those standards as well, making dynamic importing of files installed this way very easy. Learning NPM, the Node Package Manager, and Learning [[Package Management]] with Yarn are both courses that can help you a lot more with these topics.
>
> **[3:07](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/modern-javascript-tooling?u=76281980&t=187)** One other kind of tool that's used all the time is a transpiler or compiler. Babel is a transpiler that's wildly popular for converting ES6 and other more modern versions of JavaScript into [[ECMAScript]] 5, and TypeScript has its own compiler to convert it into [[Vanilla]] JavaScript. Either of these can be integrated with Webpack or Rollup or Vite, giving you a one-stop shop for building your finalized JavaScript when working on a larger project. As I said earlier, you don't have to use any of these tools at first. Depending on how you use JavaScript, you might not ever need to. But if you work with the language for a while, you might start to notice some friction in your workflow and tools like these might be able to help.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (15), [[npm]] (2), [[React.js|React]] (1), [[Vue.js|Vue]] (1), [[Package Management]] (1)
> **CLI Commands:** npm (2), yarn (2), node (1)
> **Env Vars:** npm (2), es6 (1)
> **Documentation:** specification (2), spec (1)
> **Prerequisites:** you'll need (2), install (1)
> **Code Identifiers:** jquery (2)
> **Definitions:** is a  (2)
> **Speakers:** - [instructor] (1)

#### [Responsible JavaScript](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/responsible-javascript?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/responsible-javascript?u=76281980&t=0)** - In this video, we're going to discuss responsible [[JavaScript]] use. I think one of the first rules of JavaScript is to try not to use it. That sounds weird, I know. Depending on what you're trying to accomplish, there may be another tool that satisfies the need. If you're trying to animate things on the web, for example, there are ways to do many kinds of animation entirely with [[Cascading Style Sheets (CSS)|cascading style sheets]], CSS. Sometimes with just a tiny bit of JavaScript to make them go. If you want to look into this more, you can explore CSS transitions, transforms, key frame animations, page transitions, and more. The courses, CSS: Animation and CSS: Transforms and Transitions can tell you a lot more about this. Even [[HTML]] is pretty powerful on its own. There are a lot of elements to make web [[Forms]], dialogue boxes, time and date pickers, sliders, and more. These aren't necessarily glamorous, but they're useful and don't require JavaScript to work. Keeping up with updates to web technologies can be, to be honest, a little exhausting, but it can also be pretty exciting, so try to watch for what's coming and what other folks are using and interested in. Assuming that using JavaScript is the right call, and many times it is, don't get me wrong, you want to make sure you're using it for good and aware of how it's use will impact others. JavaScript opens so many possibilities on the web and elsewhere. In fact, it seems like there's little you can't do with JavaScript. That being said, it can be argued that all this power has been weaponized and has implications for everyone, both those of us who write JavaScript and everyone who uses it on all our different devices.
>
> **[1:37](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/responsible-javascript?u=76281980&t=97)** First, there are the security risks. JavaScript can be used to load data from anywhere on the web and can be injected into HTML tags that load data like image tags, and script tags, and so forth. Attacks like these are called cross site scripting attacks or XSS, since they inject JavaScript that's not part of the site into a visitor's browser. When authors become careless and leave these vulnerabilities, the results can be damaging. Websites can be defaced, site visitors can have their personal information revealed or stolen, and more. You can find out more about XSS in the course is [[Cybersecurity Foundations]]: [[Application Security]] and [[Programming Foundations- Web Security]]. Next are performance issues. Many websites, especially larger commercial sites, include JavaScript to [[Fetch]] and display ads, and tracking scripts to gather information on their user's behavior and activities, some of which are quite invasive of our [[Privacy]]. These scripts are often large and of course need to be downloaded, which takes valuable time, include very many of them, and page load times start to go way up, resulting in slow, poor performance. Keeping JavaScript lean is vital for good [[User Experience (UX)|user experience]]. Long downloads are annoying and having to run a lot of JavaScript can slow down a browser and drain phone and laptop batteries. Beware of using lots of scripts, your visitors and customers time and attention is very valuable. There's more information on optimizing websites for better performance in the courses [[Developing for Web Performance]], [[Vanilla JavaScript- Web Performance Optimization APIs]], and Learning Enterprise Web Application Performance,
>
> **[3:13](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/responsible-javascript?u=76281980&t=193)** among others. For these reasons and others, JavaScript can be blocked by users. Sometimes entirely, often partially. Content blockers on mobile and desktop browsers can try to block advertising and tracking scripts from ever loading, and some will take even more aggressive steps to try and protect user privacy. There are institutional firewalls that may block some or all JavaScript, and of course, some users will simply opt out disabling JavaScript as a feature of their browsers. Not many in the grand scheme of things, but enough to be aware of. All of this is to say, feel free to use JavaScript, but be attentive and judicious about it. When you're first learning, you don't have to think about these concerns too terribly much. If you end up working in a job with JavaScript as part of it, these issues will be very important. To recap, security, performance, and the fact that JavaScript can be blocked are all critical aspects of its use to be remembered. So be careful when using JavaScript to display data users have entered. Be mindful of how much JavaScript you're adding to a project. Keep things as small and fast as you can manage. And though JavaScript is exceedingly popular and available, keep in mind that not all users will have it available or turned on.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (23), [[HTML]] (2), [[Privacy]] (2), [[Cascading Style Sheets (CSS)|Cascading style sheets]] (1), [[Forms]] (1)
> **Env Vars:** css (4), html (2), xss (2)
> **CLI Commands:** make (3), find (1)
> **Warnings:** be aware (1), be careful (1), keep in mind (1)
> **Analogies:** for example (1)
> **Speakers:** - in (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Continue your JavaScript development journey](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/continue-your-javascript-development-journey?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/continue-your-javascript-development-journey?u=76281980&t=0)** - Congratulations on completing this course. We covered a lot of ground. We learned about how to use variables to store data, and many of the types of data we can play with. From there, we discussed objects, which are the base on which nearly everything meaningful we do in [[JavaScript]] is built. Control structures and operators came next, showing how we can go in different logical directions and take different actions based on those decisions. In an effort to really make our code handle tedious work, we looked at different ways to run loops or iterate. Our last big stop was functions which allow us to wrap chunks of logic in reusable callable units which makes code a lot easier to understand and maintain. Your next steps from here could go many directions. In this video we'll look at some courses that can be helpful for some of the directions you might go for the kinds of things you might try to build with your newfound knowledge of JavaScript. If you're interested in writing JavaScript the way it was originally intended to make webpages more interactive, I'd recommend you learn JavaScript's companions, [[HTML]] and CSS next. Making actual HTML pages is not as common as it used to be, but for making templates, it's still an essential skill. [[HTML Essential Training]] will give you a very thorough understanding of the language which will let you start building your own pages so you have a place to experiment with JavaScript. Likewise, introduction to CSS will give you an overview of the language that makes webpages look how they do. That's the very simplest way to put it. There's a lot you can do with CSS, including animation.
>
> **[1:35](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/continue-your-javascript-development-journey?u=76281980&t=95)** Knowledge of CSS is essential for any web designer, and there's a series of CSS essential training courses to get you fully up to speed. As for JavaScript on the web, look out. I'm a working web developer, so my main lens for viewing JavaScript is how it can be used on the web, and that's been the impetus for the lion's share of my courses. If you haven't tired of the mellifluous sound of my voice, you can check out several of my courses as possible next steps within the JavaScript world. JavaScript for web designers is a beginner to intermediate level course where you build small, but useful pages to build your skills with playing JavaScript on the web. jQuery for web designers is similar, but makes use of jQuery, which is a collection of JavaScript functions that's still used on countless websites. Many people beginning with JavaScript actually start with jQuery because of how easy it makes certain aspects of working with webpages, so it's worth a look. View and [[React.js|React]] for web designers are companion courses at a solid intermediate to more advanced level so you'll want to bookmark those for later most likely. View and React are two very popular frameworks for making sites that are mostly driven by JavaScript, and can also be used to add data-centric interactivity to smaller parts of sites. And of course, if you've heard of these and want to see what they're like, you can check out either course even if you're not ready to work on the projects just yet. One more course of mine. [[Learning JavaScript Debugging]] is a course where we go in depth with tools for debugging JavaScript, using web browsers, Node, integrated development
>
> **[3:08](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/continue-your-javascript-development-journey?u=76281980&t=188)** environments and more. I'd recommend that course as a good one to watch fairly early in your JavaScript journey, but only after you've tried writing some code of your own and you've run into some situations where something isn't behaving how you want it to. I'm certainly not the only one with JavaScript courses available. [[JavaScript Essential Training]] is a comprehensive course to take you more deeply into the language than we covered here and can be another helpful resource as you start building code of your own. To get a taste for what else you can do with JavaScript beyond basic interactivity, check out these courses. Data visualization is the art of presenting information visually, charts, [[Infographics]], and so forth. One of the most popular JavaScript toolkits for creating such things is called [[D3.js|D3]], and you can learn all about it in this course. Interactive Animations will teach you about the capabilities of CSS animation and the extra power of JavaScript and how to put them together to make animations with precise timing and user control. If you've heard of machine learning and want to learn more, learning [[TensorFlow]] will give you a window into that world using JavaScript. JavaScript is probably not the most popular language for machine learning out there, but it can be done. You can also use JavaScript to make 3D graphics. Learning 3D graphics will show you how using Three.js, another popular JavaScript toolkit. If you're interested in using JavaScript outside the confines of a web browser, I recommend learning [[Node.js]] as a next step. It's a relatively brief course that will get you introduced to JavaScript running on the server instead of the browser.
>
> **[4:44](https://www.linkedin.com/learning/learning-the-javascript-language-22309208/continue-your-javascript-development-journey?u=76281980&t=284)** JavaScript [[Test-Driven Development]], ES6, is a course that can introduce you to test-driven development. A different way of working than many people might think of when first approaching a programming language. It's also a really good way to work that's worth exploring so I'd recommend looking into the approach relatively early in your journey. As with debugging, it will make the most sense after you've written a little bit of code to get your feet wet, but don't wait too long so you don't get too set in your ways. There are, of course, tons of JavaScript courses available. Start writing a little code of your own in whatever domain is interesting to you and see where your curiosity takes you. I'd love to hear what you're doing next. You can leave questions in the course Q and A, and follow me here on [[LinkedIn]]. Thank you very much for watching, and I hope you have an excellent day.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (29), [[HTML]] (3), [[React.js|React]] (2), [[Test-Driven Development]] (2), [[Infographics]] (1)
> **Env Vars:** css (6), html (3), es6 (1)
> **CLI Commands:** make (5), node (2)
> **Definitions:** is a  (5), is called (1)
> **Code Identifiers:** jquery (3)
> **Cross-References:** we covered (2), we discussed (1)
> **File Paths:** three.js (1), node.js (1)
> **Speakers:** - congratulations (1)


## Instructor

- [[Joe Chellman]]

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/learning-the-javascript-language-4369302/codespaces)

## Skills Covered

- JavaScript

## Path Context

### In [[JavaScript Foundations Professional Certificate by Mozilla]]
**1 of 2** | [[JavaScript Essential Training]] →

### In [[Become a JavaScript Developer]]
← [[JavaScript Essential Training]] | **2 of 13** | [[Learning ECMAScript 6+ (ES6+)]] →

## Part of

- [[JavaScript Foundations Professional Certificate by Mozilla]]

## Appears In

- [[JavaScript Foundations Professional Certificate by Mozilla]]
- [[Become a JavaScript Developer]]

## Related Courses

_Courses sharing skills:_

- [[End-to-End JavaScript Testing with Cypress.io]] — JavaScript
- [[Learning JavaScript Debugging]] — JavaScript
- [[Foundational JavaScript Security]] — JavaScript
- [[JavaScript- Test-Driven Development (ES6)]] — JavaScript
- [[JavaScript- Best Practices for Data]] — JavaScript

---

[↑ Back to top](#)