---
type: course
cssclasses:
  - lle-course
  - lle-level-advanced
slug: vanilla-javascript-building-on-the-document-object-model-dom
url: "https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom"
duration_minutes: 107
duration: 1h 47m
level: Advanced
updated: 6/29/2021
learners: 400716
skills:
  - Document Object Model (DOM)
  - JavaScript
exercise_files: true
github: "https://github.com/LinkedInLearning/vanilla-js-DOM-2876283"
thumbnail: "https://media.licdn.com/dms/image/v2/C560DAQEL1YDA4pMuSQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1624636988037?e=2147483647&amp;v=beta&amp;t=8rrIP4NN2s_q3nQuIvT0KiT86gGqhsjREA3oLRT4JuY"
linkedin_topic: Software Development
learning_paths:
  - '[Explore Web Development with Vanilla JavaScript](../../Paths/Software%20Development/Learning%20Paths/Explore%20Web%20Development%20with%20Vanilla%20JavaScript.md)'
prev_courses:
  - '[Vanilla JavaScript- Web Performance Optimization APIs](Vanilla%20JavaScript-%20Web%20Performance%20Optimization%20APIs.md)'
path_nav: '[{"path":"Explore Web Development with Vanilla JavaScript","position":8,"total":8,"prev":"Vanilla JavaScript- Web Performance Optimization APIs","next":null}]'
path_count: 1
tags:
  - course
  - topic/software-development
  - topic/web-development
  - skill/document-object-model-dom
  - skill/javascript
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/Vanilla%20JavaScript-%20Building%20on%20the%20Document%20Object%20Model%20(DOM).md)

![Vanilla JavaScript: Building on the Document Object Model (DOM)](https://media.licdn.com/dms/image/v2/C560DAQEL1YDA4pMuSQ/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1624636988037?e=2147483647&amp;v=beta&amp;t=8rrIP4NN2s_q3nQuIvT0KiT86gGqhsjREA3oLRT4JuY)

# Vanilla JavaScript: Building on the Document Object Model (DOM)

> At the heart of every web application and framework (such as Angular or React) lies the DOM, the Document Object Model. In this course, instructor W. Scott Means explores the DOM from the bottom up. After a fast-paced introduction to the Node interface and DOM trees, Scott takes things to the next level by constructing a functioning HTML parser. He shows you how HTML source code is translated into

> [LinkedIn Learning](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom) | 1h 47m | Advanced | 401K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (2 videos)
  - [Learn the JavaScript DOM inside and out](#learn-the-javascript-dom-inside-and-out)
  - [Where to start](#where-to-start)
- [**1. Key DOM Concepts**](#1-key-dom-concepts) (6 videos)
  - [The Node interface](#the-node-interface)
  - [DOM-specific maps and lists](#dom-specific-maps-and-lists)
  - [Random access with querySelector()](#random-access-with-queryselector)
  - [CRUD in the DOM](#crud-in-the-dom)
  - [Challenge: queryAll++](#challenge-queryall)
  - [Solution: queryAll++](#solution-queryall)
- [**2. HTML to DOM**](#2-html-to-dom) (9 videos)
  - [Where does the DOM come from?](#where-does-the-dom-come-from)
  - [DIY HTML parsing](#diy-html-parsing)
  - [Building a firm foundation: The lexer](#building-a-firm-foundation-the-lexer)
  - [Parsing text](#parsing-text)
  - [Parsing comments](#parsing-comments)
  - [Parsing elements](#parsing-elements)
  - [Parsing attributes](#parsing-attributes)
  - [Challenge: Void elements](#challenge-void-elements)
  - [Solution: Void elements](#solution-void-elements)
- [**3. Transforming the DOM**](#3-transforming-the-dom) (9 videos)
  - [Traversing the tree](#traversing-the-tree)
  - [Building a pretty printer](#building-a-pretty-printer)
  - [Processing document fragments](#processing-document-fragments)
  - [Processing comments](#processing-comments)
  - [Processing elements](#processing-elements)
  - [Processing element attributes](#processing-element-attributes)
  - [Handling text and cleanup](#handling-text-and-cleanup)
  - [Challenge: Empty tags](#challenge-empty-tags)
  - [Solution: Empty tags](#solution-empty-tags)
- [**Conclusion**](#conclusion) (1 videos)
  - [Level up your DOM skills](#level-up-your-dom-skills)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Learn the JavaScript DOM inside and out](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/learn-the-javascript-dom-inside-and-out?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/learn-the-javascript-dom-inside-and-out?u=76281980&t=0)** - This course covers the DOM interface, nodes, trees, and how to access them from [JavaScript](../../Skills/Software%20Development/JavaScript.md). But it also dives deeper, showing you how the browser parses your [HTML](../../Skills/Web%20Development/HTML.md) and how you can manipulate it yourself, using nothing but a text editor. My name is Scott Means and I started writing code while the movie "ET" was still in theaters. I was lucky enough to be growing up at the same time personal computers were. So I got to understand them at a fundamental level. And as complex as things have become, understanding the fundamentals is still the secret to becoming an experienced, productive and happy programmer. This course covers the DOM interface, nodes, trees, and how to access them from JavaScript. But it also dives deeper showing you how the browser parses your HTML and how you can manipulate it yourself, using nothing but a text editor. If you want to understand the DOM like a framework or browser developer does, join me in my [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) learning course, as we explore the Dom API, build a pretty printer, and even write our own HTML parser.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (3), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Env Vars:** dom (3), html (3), api (1)
> **Speakers:** - this (1)

#### [Where to start](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/where-to-start?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/where-to-start?u=76281980&t=0)** - [Instructor] In order to get the most out of this course, you should be familiar with [JavaScript](../../Skills/Software%20Development/JavaScript.md), recursion, arrow functions, regular expressions, and just general JavaScript syntax. You should also be comfortable with HTML5, custom element tags, and basic CSS syntax. If you aren't familiar with all these topics, don't let that stop you. Try a few lessons, and if you need to brush up, try these other great courses first.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2)
> **Env Vars:** html5 (1), css (1)
> **Speakers:** - [instructor] (1)


### 1. Key DOM Concepts

[↑ Back to Table of Contents](#table-of-contents)

#### [The Node interface](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/the-node-interface?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/the-node-interface?u=76281980&t=0)** - [Instructor] Have you ever wondered how your lovingly crafted [HTML](../../Skills/Web%20Development/HTML.md) source code is transformed from a bunch of characters in a file to a fully rendered webpage in your browser? The truth is it's not a direct route. before the browser can figure out where and how to display everything on your webpage, it needs to construct a document tree. This process of converting your HTML to this object tree or [document object model](../../Skills/Web%20Development/Document%20Object%20Model%20(DOM).md) is called parsing. Every tag, fragment of text, every comma, is parsed by the browser and stored and the resulting document tree in a node. These nodes are arranged in a tree through the use of references. The node interface defines common attributes that refer from a parent node to its child nodes from a node to its next sibling and from child nodes back to their parent. These five core attributes allow your code to navigate from any node in your document to almost any other node. By following the parent node references, you can get to the top level document node. By following the next sibling references, you can find all other nodes located at the same level of the document hierarchy. By following first child, you can navigate to nodes farther down the tree and using the owner document reference. You can get to the top level document object from any node in the tree, but why the almost above why can't we follow references from any node to any other node of the document? Because even though the node interface is the common base class for all the objects and a dom document, not all those objects are the same. In fact, although the dom level three core standard defines 12 node types and practice, these five types are the only ones you'll encounter when doing normal things in normal HTML documents. The [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the node types are mostly used in XML document processing. Four of these types of part of the primary dom tree, but attribute nodes are special.
>
> **[1:33](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/the-node-interface?u=76281980&t=93)** And the only way we can access them through the node interface is by using the attributes property. Now beside the node type, there are a few other useful node properties that are worth understanding. The node name attribute returns to descriptive string for the node based on its type. This is super handy when debugging dom code another useful property is the node value property for things like element attributes, comments, and texts, it provides an easy way to get the contents of the node as a string, but for more complex node types like elements of documents, it simply returns null. Ana although we've covered the most important parts of the node interface, there are other useful features that you can discover for yourself. Take a look at the W3C recommendation. It's not very long and you might be surprised what you find.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (3), [Document object model](../../Skills/Web%20Development/Document%20Object%20Model%20(DOM).md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** node (23), find (2)
> **Env Vars:** html (3), xml (1), w3c (1)
> **Documentation:** w3c (1)
> **UI Navigation:** navigate to (1)
> **Exercise Files:** source code (1)
> **Definitions:** is called (1)
> **Speakers:** - [instructor] (1)

#### [DOM-specific maps and lists](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/dom-specific-maps-and-lists?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/dom-specific-maps-and-lists?u=76281980&t=0)** - [Instructor] One of the more frustrating aspects of working directly with the DOM is the use of some DOM-specific collections for certain Node properties within the DOM tree. So to illustrate that, we're going to build a little application today. So let's show you my development environment that I've set up here. Is pretty simple, I'm running the built-in [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) web server. So I'm just going to the project folder and running python-m http.server. So if you're running Python 3+, then this should work fine. And I've got my text editor with my code loaded up here, and I'm running Chrome to view my web page. So as you can see, here's the webpage, I've gone ahead and created this basic webpage that has [JavaScript](../../Skills/Software%20Development/JavaScript.md) file being loaded and it's executing my DOMcontentLoaded event handler. So the DOMcontentLoaded event handler is called as soon as your page is parsed and the DOM tree is built, but all the external resources may not have been loaded yet. So some of your external images or style sheets and things like that may still be loading. This is a good event in an intercept if you have a complicated application, you want to get some code running before everything else has pulled in. Otherwise if used a load event, then you have to wait for everything to be downloaded. Anyway, just to verify, it did actually get executed because when I refresh, hello cookies. So, my code's being called. So what I want to do in this application, I have a bunch of ingredients in my recipe, and I want to add up all of the weight, which are in this right-hand column in grams. And if we look at the underlying [HTML](../../Skills/Web%20Development/HTML.md), you'll see that I have a table, and inside tbody, I have a bunch of table rows. So to get access to the table rows,
>
> **[1:32](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/dom-specific-maps-and-lists?u=76281980&t=92)** I'm going to use this ingredients ID. And that will allow me to get a hold of this tbody element in my code and then iterate through these table rows. So let's go to our JavaScript, and let's go ahead and create a new variable ingredients. And we're going to use document.getElementById. It's pretty old school, but it works well. And it works on almost every browser in the world, so you don't have to be concerned about that. Now we're going to iterate through, let's iterate through the child Nodes first. So ingredients.childNodes returns all of the Nodes that are a part of the sub tree that belongs to this table body. So forEach and say tr, there. And we're going to loop through, and just log into the console for now. So we'll save this off. Now let's go back to our browser, refresh, and let's take a look at our developer tools. And we'll see over here that we are in fact viewing all of the table rows, we're also seeing these Nodes here, which are text Nodes. So the reason for that is when you do childNodes, every piece of text in your original HTML is actually translated into a Node in the DOM tree. So this white space is actually being inserted into the DOM as a text Node. So there are a couple of approaches we could use, we could filter out the ones we care about by looking at the Node and looking at the Node type of things. But I would prefer to just,
>
> **[3:04](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/dom-specific-maps-and-lists?u=76281980&t=184)** let's use the children property, which is actually only the elements for that, that only the child elements for that element. So let's go ahead back to the browser, we refresh, and then now we're going to run into this. And what you're going to see is, okay, ingredients.childrenforEach is not a function, that's because if you look at ingredients.children, let's look at the constructor. And the constructor is an HTML collection not an array. So if you look at the constructor of a regular array, that's a built in JavaScript array. So HTML collection does not implement forEach. So long story short, there are various ways we can iterate over this collection, but I tend to like to use forEach because it's kind of the new way of doing things in JavaScript and it's pretty fast. So what we're going to do is we're going to invoke that method directly. So we're going to go to the array prototype forEach, and we're going to use call. See best methods in JavaScript actually are objects themselves, and it has a call method that I can execute to actually loop over any variable that I want. So I'm going to take ingredients.children, and I'm going to loop over every row. Let's save that back in here, and now you'll see, I do have all the rows. Now that I have access to these rows, I want to get access to the right most column for each of these, including the blank one at the end. If you'll look at my HTML page again here, each of these rows has a quantity in the second td element. And then the very last row actually has a blank td element, which I want to put the total of all those weights into,
>
> **[4:40](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/dom-specific-maps-and-lists?u=76281980&t=280)** and is identified by this totals ID. So let's go back into here to JavaScript, and let's go ahead and create a variable to hold our total. start it off at zero. And then inside here, what we need to do is we need to differentiate between rows that are the total row and rows that aren't, so, the clearest way to do that is look for that ID. So let's say if tr, let's look at the attributes collection for a second. Attributes ID equals totals, then we're in the total row, right? Otherwise, we'll just continue to do what we're doing now. All right, save that off, connect to browser, refresh, and you'll notice that it never said we were in the totals row, that's because if you look at that element, totals.attributes.collection, what is the attributes collection? The constructor of that is it in fact, a named Node map, it is not a normal JavaScript diction, even though it kind of behaves like one. So when you actually get access to that ID attribute, it's not returning a string, it's returning an object. An adder object. So, there are a couple of ways we could go about that, and it gets kind of evolved because, if we wanted to get the value, we could technically do this, we could say Node value. But the problem is all the other rows will have no attributes. So then we'll be trying to invoke the property Node value,
>
> **[6:14](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/dom-specific-maps-and-lists?u=76281980&t=374)** property on a null reference, undefined reference and that won't work. So, this was mainly just to illustrate that, attributes looks like a dictionary, but it's not, and so you need to be aware of that when you're writing some code that references attributes. And what we're going to do instead is we're going to use the built-in ID properties. So let's go ahead here and just use the property that's built in to every element that has its own ID property, which can be edified if it doesn't have one. Save it off, refresh, now we're in the totals row, so that's good. So now all we have to do down here is, let's get our weight. And the weight we're going to get by, let's see. Let's go ahead and get a reference to that second td cell for everything. So let's do const td equals table row children. So this is going to be a child element again, sub one, because that's zero-based, we're going to get the second to td for every row, right? So we're going to get the weight, and we're going to have to, it's going to be a string, so we're going to parse float of the inner text to that element, right? And then we will add that to the weight. Let's go ahead and log without it, liberal logging never hurts, it's easy to see what you're doing if something goes wrong. Total, let's save that off, refresh, okay? So it looks like we're getting our total here. Now, all we have to do is update our totals rows. So once again, we add that and our text equals total. Clean it back, clean it back,
>
> **[7:50](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/dom-specific-maps-and-lists?u=76281980&t=470)** save it off, and, you see, now we have our total right down here at the bottom. The major takeaway is that DOM and JavaScript collections are not identical. Keeping this in mind will make your coding and debugging life much easier.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (8), [HTML](../../Skills/Web%20Development/HTML.md) (5), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3)
> **CLI Commands:** node (8), python (3), make (1)
> **Env Vars:** dom (7), html (5)
> **Code Identifiers:** foreach (4), childnodes (2), getelementbyid (1), childrenforeach (1)
> **Definitions:** is called (1), is a  (1), is an  (1)
> **UI Navigation:** go to (2)
> **Versions:** python 3 (1)
> **Cross-References:** go back to (1)

#### [Random access with querySelector()](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/random-access-with-queryselector?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/random-access-with-queryselector?u=76281980&t=0)** - [Instructor] Being able to efficiently locate specific elements in a complex DOM tree is one of the most frequent tasks you'll encounter when writing a [Vanilla](../../Skills/Web%20Development/Vanilla.md) [JavaScript](../../Skills/Software%20Development/JavaScript.md) DOM application. Thanks to the W3C selectors recommendation, we can quickly sift through our DOM tree to access the elements we need. To make it easier to experiment with selectors, we'll build a simple query selector application that'll let us load some [HTML](../../Skills/Web%20Development/HTML.md), and then run querySelectorAll against it. We'll be starting from a base application that can load HTML into our page. So to demonstrate, I'm going to go ahead and grab some HTML from Donald Knuth's biography over here on Wikipedia. You'll see I'm using the developers tools to come in here to the tree, and grab basically the body of this article. I'm going to copy it. I'm going to come back over to the app. And I'm going to paste this into this text area, and you'll see it's being parsed and inserted into our page. So we have a nice chunk of kind of complicated DOM here that we can use to do a querySelectorAll against. And if we go back to the source code for this, you'll see that it's a pretty basic HTML5 page that has a text area element, and also a section called output. It's got an ID of output. So in the source code, I'm intercepting the DOMContentLoaded event, which, again, is called when your page is fully parsed and the DOM tree is ready, but before all of your external resources have been loaded. So it's a good event to catch if you want to start coding and having your code execute before your page is fully loaded. It's kind of faster than the pageload event, if you wanted, especially in a more complicated page. And then I'm going to find that text area, and then I'm going to add an event listener for input. So whenever that text area changes, I will refresh that output section. So refreshOutput is called with the HTML from that input,
>
> **[1:35](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/random-access-with-queryselector?u=76281980&t=95)** and all I do is assign it to the innerHTML of that output element. And then that's how we end up with this. So now that we have a way to insert some DOM content into our page, I want to be able to type in a selector and pass it directly to querySelectorAll, so I'm going to go back to my HTML, and I'm going to create a form that will allow me to type in a selector and then execute it. And I tend to use [Forms](../../Skills/Web%20Development/Forms.md) a lot, because the browser has really great support for forms. It gives you a lot of behaviors for free, kind of. So, we're going to call this section, the new section, query. And in this section, we're going to create a form. And the form's name is queryall. And then inside that I'm going to create, let's do something a little fancy, we'll use a pre tag so we can use preformatted text, monospace, make it look more code like. And let's go ahead and call a querySelectorAll so it looks like we're typing into the function call itself. So, and then we'll create an input, and its name will be selector. And then after that, we will go here, and we'll create a submit button. So the button type="submit". And let's call that button "run." And it all goes on one line like this, because I want to use this pre tag for formatting, and whitespace is significant, so if I started wrapping it, it would actually wrap on the screen. So, now we should have our form. I'll just save that.
>
> **[3:07](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/random-access-with-queryselector?u=76281980&t=187)** Now that I've created this form, let's go ahead and take a quick look at it, how that behaves in our application. I'm going to refresh the page, and I want to show you something. So if I put something in here, and just say div, whatever, and if I add a selector here and I hit run, you'll notice the page refreshed, and it actually added that form field as a query parameter. If I don't have a method or an action attribute on my form, it'll just do a GET request. So the first thing we need to do is go back into our code, and let's add an event handler for that form to prevent it from submitting like that. So, inside our DOMContentLoaded event, we're going to come in here, we're going to add a new event handler. And the great thing about using forms is we can use document.forms.queryall, and that's our form, and we can add an event listener directly here. And we'll add one for submit. And do another arrow function. And then all we need to do is in our event, we're going to prevent default, which prevents the browser from doing the normal thing, which would be to submit the page. And then we're going to return false. And if we go back into the browser and refresh, now this time if I type something in here, you'll notice I hit Enter, and nothing's happening, and if I hit run, nothing's happening. That's because it's actually being intercepted in that function. So to prove it, let's come back into our code, and I'm going to create a function called refresh query to actually run the query itself. And I'll have access to that form field, because in the event, the target in this case is the form, and I can reference the field by name. So selector, and then grab the value.
>
> **[4:41](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/random-access-with-queryselector?u=76281980&t=281)** So, now I can write my refresh query function down here. Okay. And let's just log it to the console. And go back into our browser, refresh. And now if I type something here, I'll open up my developer tools to see what's going on with that, let's say a div, whatever. Now you'll see that I'm actually getting output over there, so then I can run querySelectorAll next. Let's go back into the code again. Now I want to run querySelectorAll, and then I need a place to put my results. So what I would like to do is I'm going to put them in an unordered list. So let's go back into the HTML page, and create a place to put these results. So I'm going to call this Matches, and then I'm going to create an unordered list, and let's call the id matches. And then I can save this way. Now let's go back into our code, and we'll write the actual loop that will populate that list. So, the first thing I'm going to need to do is get access to the output div itself. So, the easiest way to do that is output=document.querySelector. The only difference between querySelector and querySelectorAll is that querySelector only returns one result, even if multiples would match, and querySelectorAll returns a list. So we know that there should only be one output div, because it's using an id, which are supposed to be unique. So, then we'll also get access to the matches using the same mechanism.
>
> **[6:17](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/random-access-with-queryselector?u=76281980&t=377)** querySelector of #matches. And we'll save that. Okay, so now let's go ahead and let's clear out the old matches. This normally is something you'd end up doing on the second pass when you realize you didn't clear the element out, but I've done this so many times that, by now, I know the next thing I need to do is clear out this element. So the way to do it in this case is I'm going to set innerHTML to nothing. And believe me, there are many ways to clear out an element in vanilla JavaScript, and I went down the rabbit hole of various stack overflow posts and performance numbers, and innerHTML is a perfectly acceptable way to do it. It will clear the element, and it's pretty easy to understand, so I tend to use that. So let's go ahead and get ahold of the output element's querySelectorAll function. And the selector was passed in as an argument to this function, so let's go ahead and do a forEach. So for each match, and in this case, the match will be an element. We'll do an arrow function here. Now, what I need to do is, that's an unordered list, and I'm going to add a list item with the text of the element that matched. So first I need to create a new list item, so let's create a list item using document.createElement, li. So we've got a new list item element, and then we're going to set the innerText of that, the text, you notice, because I don't want to actually insert the match as HTML into my list, I want to see the source code of it. So I'm going to say the text of this list item is equal to the HTML, the outerHTML of my match. And then I'm going to append it to the matches.
>
> **[7:58](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/random-access-with-queryselector?u=76281980&t=478)** And then let's save that off. Now let's go back and see what is happening in our code. So first of all, let's get some interesting source code. I'm going to go back to Donald Knuth here and copy his bio again. And we'll grab that. Let's go back into our page, and let's paste this in here. So now we have some DOM content to select, and let's get ahold of all of the image tags. And, oh, there they are. So now I can see the source code for all the images in that DOM snippet. I'm going to do one more little bit of formatting, I'd like to see this look more like code. I'm going to go back into my editor here, into queryall.html. Let's go up into the style sheet, and let's do, all the items here could be formatted with font-family: monospace; Let's say. Now let's go back. Refresh. Paste this in. Now that looks a little nicer. I like it. So yeah, now we can do a querySelectorAll all day long. There are a lot of interesting things we could find. You could find all the paragraph tags. You could find all the a tags that have a title for example, all the hyperlinks that have a title. There they all are. We can also find all the a tags that don't have a title. Which is interesting. And there they are. Mastering selectors will make your front end development life much easier. Check out the W3C selectors recommendation,
>
> **[9:32](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/random-access-with-queryselector?u=76281980&t=572)** and I guarantee you'll find at least one selector you haven't seen before.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (9), [Forms](../../Skills/Web%20Development/Forms.md) (4), [Vanilla](../../Skills/Web%20Development/Vanilla.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2)
> **Code Identifiers:** queryselectorall (10), queryselector (4), innerhtml (3), refreshoutput (1), foreach (1)
> **Env Vars:** dom (8), html (8), w3c (2), html5 (1)
> **CLI Commands:** find (6), make (3)
> **Exercise Files:** source code (5)
> **Cross-References:** go back to (3)
> **Documentation:** w3c (2), stack overflow (1)
> **Definitions:** is called (2), is a  (1)

#### [CRUD in the DOM](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/crud-in-the-dom?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/crud-in-the-dom?u=76281980&t=0)** - [Instructor] CRUD is one of those acronyms that make you laugh the first time you hear it, but you remember it for the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of your career. Create, read, update, and delete are the four basic operations for almost every UI-driven application. We'll base our credit application on the querySelector testbed application we built in the previous video. That allowed us to type or paste arbitrary [HTML](../../Skills/Web%20Development/HTML.md) source code into this webpage, have it be parsed, and then run querySelectorAll on it. And to demonstrate again, I'm going to use Donald Knuth's biography from Wikipedia. It's got a lot of interesting content in it. And I'm going to copy this using the developer tools. I'm going to copy the source code, back, and paste it into my page. And so now, like I said, I can [Fetch](../../Skills/Web%20Development/Fetch.md) all the image tags or all the P tags. And so now that I can do that, the first operation I want to implement is delete. So let's go back into our source code. And I'm going to find the form where I'm actually allowing the user to type in the selector that they're matching against. And I'm going to add a normal button. Not an actual submit button, but a normal button for various reasons. If you do a submit button, it's kind of hard to find out what button was clicked. So better off just doing a regular HTML button. So we're going to do type=button. The label is delete, okay? And we're going to save this off. And we're going to go back to our browser and refresh. And now, we have a delete button and it doesn't do anything. So the next thing we need to do is actually intercept the event when the user clicks on delete. So to do that, we're going to go back
>
> **[1:34](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/crud-in-the-dom?u=76281980&t=94)** into our [JavaScript](../../Skills/Software%20Development/JavaScript.md) file here, queryall.js. And similarly, to the way we handled these other events, we're going to add a new event handler here in our content loaded processing logic. And we're going to once again, grab a hold of that form, document.[Forms](../../Skills/Web%20Development/Forms.md).queryall. And we're going to add an event listener on the form itself. And this is a useful technique, especially if you're manipulating the DOM manually. Putting a listener on a higher up element, like the form element, like the parent. Allows you to manipulate lower-level DOM elements without having to worry about whether you've accidentally disconnected a listener. Or you've corrupted your listening tree. And just listening at a higher level allows you to process things more centrally. It makes your code generally cleaner. So now, we're going to add a click event with an error function. And inside that event handler, what I'm going to do is I'm going to, first of all, make sure that a button was clicked. Because this will be called anytime anything inside the form is clicked. So the easiest way to do that is look at the event target. And let's look at the tag name, which would be the element tag of that particular element that was clicked. And it was not equal to button. And notice it as upper case. It has always upper case. I'm not sure why that is the case, but you will have to always compare against an uppercase string. If it's not a button, we're just going to return. Now, assuming it was a button, I'm going to do a switch statement. And for literary types, that's what we call foreshadowing because we're going to have some other handlers later.
>
> **[3:07](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/crud-in-the-dom?u=76281980&t=187)** So I'm going to switch off of the target, which is once again the button, innerText. And I'm just going to use the text of the button to tell me what operation is being performed. In this case, it's going to be delete. And I'm going to call a new function that doesn't exist yet called deleteMatches. And with breaks, don't ever forget your break when you're using the case statement. You'll only do that a few times before you realized that those bugs are very hard to find. So we're going to go ahead and create that new deleteMatches function. And just for kicks, we'll say console.log deleteMatches called. And make sure it is being called. Let's save that off. Now, we'll switch back to our browser, refresh, bring up our developer tools. Yeah, and deleteMatches is being called. So now, we can go back and actually implement the deleteMatches function. So let's go ahead back to our code. And what this is going to do is it wants to grab the output element where all the target DOM stuff lives. We're going to get the selector from the form and we're going to do a querySelectorAll. And then we'll remove each matched element. So the first thing I need to do is get a reference to the output section. I'm going to use the same code as I used for refreshQuery because it's the same thing. The next thing I need is a copy of a selector. So let's get the selector text, which is available. Just use document.forms.queryall.selector.value. And that'll be the string.
>
> **[4:41](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/crud-in-the-dom?u=76281980&t=281)** Now, let's do output.querySelectorAll for the selector.
>
> **[4:50](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/crud-in-the-dom?u=76281980&t=290)** And for each matching element, we'll do an error function. And all we need to do is remove the element. And we're going to use removeChild. So that's called on the parentElement. So we have the element, we get the parentElement, which is another property that's available. RemoveChild of the element itself. Now, we save that off. Let's go back into our page, refresh. Let's grab Donald's information again, come back in here, and copy this, and paste it in here.
>
> **[5:30](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/crud-in-the-dom?u=76281980&t=330)** And let's go ahead and match all the image tags. So we'll do IMG, we got all the images. And let's delete them. Now, if we scroll down, oh, this article no longer has any images. And we didn't update our match list. So you can see that that's kind of unfortunate. So let's go ahead back and fix that. That's kind of a fit to finish things. Let's go in here. And all we have to do is refreshQuery. And I think we need to pass in the selector, right? So that will automatically clean that up. Let's go back into our page, refresh, paste our code in again, find our images, and delete. Now, that's cleaned up. Nice! So now, we have the create, read, and delete. So the C, R, and D of our CRUD acronym. Let's go ahead and add the update function. So the U of that. And then we'll have a complete set. So the operation I want to implement is not too complex. What I want to do is I want to flatten any elements that are matched by querySelectorAll. So instead of having an element with nested text nodes or nested comment notes, I'm going to take all the texts of a given matched element and I'm going to flatten it into a single text node. So it sounds complicated, but in operation, it'll be pretty clear. So let's go ahead in. Remember, I told you that switch statement was foreshadowing because we're going to add another function here. Let's do a case textify. And we're going to call textifyMatches. And let's go ahead, back in here.
>
> **[7:03](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/crud-in-the-dom?u=76281980&t=423)** And we'll write our textifyMatches function. And you'll find that it very closely resemble deleteMatches. In fact, it differs in only one line. So I'm going to go ahead and copy the body of deleteMatches. I'm going to paste it down here to textifyMatches. And the only thing that changes is the body of this loop. So instead of removing this child, what we're going to do is we're going to create a new text node. So const text = document.createTextNode. And we're going to grab the innerText of our element. So innerText, and that will essentially flatten that element. And then all we're going to do is replaceChild. You can replace a child. And the ones again, you need to get the parentElement, replaceChild. And it's kind of an odd syntax. You give the new node that you want to put in there, the new element that you have in there. So we're going to give it the text node we just created. And then you give the reference node, which is the one will be replaced. So essentially, this is going to swap out our current element with this new text note. So we're going to save this off. Now, let's go back into our HTML file and add that button so we can actually call our new function. So once again, it's going to be button typebutton. And the name on this one is textify. So we'll save this off. Now, let's go back in here and refresh our page. And let's just go ahead and copy Donald's information one more time, paste it in here.
>
> **[8:38](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/crud-in-the-dom?u=76281980&t=518)** Let's match all the hyperlinks, let's match the A tags. So let's go and run that. And you can see there are quite a few of them down here. And I'm going to textify them. So textify. Now, you see that there are no more A tags in this DOM at all. And in fact, they've all been flattened into plain text. So that basically covers all of the CRUD. The create, read, update, and delete functions that you can do with a DOM. Now, thanks to CRUD, we have all the building blocks we need to tackle some more ambitious applications using [Vanilla](../../Skills/Web%20Development/Vanilla.md) JavaScript and the DOM.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (3), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [Forms](../../Skills/Web%20Development/Forms.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Code Identifiers:** deletematches (7), queryselectorall (4), innertext (3), parentelement (3), textifymatches (3)
> **Env Vars:** dom (6), crud (4), html (3), img (1)
> **CLI Commands:** find (5), node (5), make (3)
> **Exercise Files:** source code (3)
> **Cross-References:** previous video (1), go back to (1)
> **Definitions:** is a  (2)
> **File Paths:** queryall.js (1)

#### [Challenge: queryAll++](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/challenge-queryall-plus-plus?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/challenge-queryall-plus-plus?u=76281980&t=0)** - So the querySelector demo we built is nice, but it could be even nicer. So I've added a feature to highlight the selected elements in the output DOM tree. So if I search for, let's say image tags, you'll see there's a little yellow border around this image here. If I search for, let's say hyperlinks. I'll search for that. You can see now all the hyperlinks are selected with that yellow background. So your challenge, should you choose to accept it, is to add that feature to the QueryAll code you'll find in chapter one, 0105 folder of the project files. And I also used one of the less known HTML5 elements to hide the match list. So I didn't have to scroll past it every time when I wanted to see my results. So if you want some extra credit, you can do that too. And don't feel that your solution has to look exactly like mine. If you have a different, better way to differentiate the selected elements, I'd love to see it. So good luck.

> [!info]- Semantic Content
>
> **Env Vars:** dom (1), html5 (1)
> **CLI Commands:** find (1)
> **Code Identifiers:** queryselector (1)
> **Speakers:** - so (1)
> **Non-Speech:** (upbeat music) (1)

#### [Solution: queryAll++](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/solution-queryall-plus-plus?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/solution-queryall-plus-plus?u=76281980&t=0)** - [Tutor] Were you able to add the element of highlighting feature? How did you do it? Did you use inline styles, CSS, or did you come up with some other answer? Now let's look at my solution. My solution was to add a new CSS class to every matching element, so I added my code to the refresh query function. So let's go back to the source code and find refreshed query. And here it is, and basically for every matching element, I added a new class called match. So, you know, that class list that's the CSS class list add match, so I added a match. And then to make it visible, let's go back into our [HTML](../../Skills/Web%20Development/HTML.md) inline style sheet here. And I added any new rule and it is match, and I made background gold and I added a small bit of padding because some elements the background's not visible at all, so I made a one point pad around that so we can see it. So let's go into the application, let's refresh it. Let's go grab our sample, back to Donald Knuth. And once again, his content is kind of interesting, so let's go ahead and copy that, paste it back in here. Now you can see, we have something to look at. And let's just match the image tag, I see an image tag there, so image, and you'll see the matches are shown in this list here, and it's kind of irritating I have to scroll a lot and that's why the extra credit
>
> **[1:32](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/solution-queryall-plus-plus?u=76281980&t=92)** was given in the challenge question part, and so I'll show you that next because I'm tired of scrolling. But as you can see, there is a golden border around the image, right? So that's working, what if I select, let's say all the links and now there's going to be a lot of those. So I grabbed this and scroll down. And as you can see, when we get there, there are a lot of links selected, but also the image is still selected as well. So there's a bug, we got to go back in and clear our old matches. And then that way the new matches would be the only ones with a golden border. So let's go back into the editor, and here in query all, first of all, actually, I'm going to do the extra credit because I don't want to have to scroll anywhere. I'm going to use the, one of the lesser known HTML five tags, the details tag, and I'm going to move all this into the details. It gives you a collapsible element for free, essentially. So details, and the use summary inside that to the summary is what's going to be displayed all the time, so we'll just show our label all the time and save that. Now let's go back into our [JavaScript](../../Skills/Software%20Development/JavaScript.md) code and to refresh query. And what we're going to do, is essentially we're going to find all of the elements that have the match class. So we're going to use document.querySelector all again, and the selector we're going to want to match against is dot match. So we're going to find that, and we're going to do it for each element, we're going to do an error function. And for that element, we're going to get the class list and we're going to remove the match class, so that will moved that class. And what's interesting is of course, this is mutating this Dom list, as we iterate through it,
>
> **[3:06](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/solution-queryall-plus-plus?u=76281980&t=186)** that list is getting shorter, but luckily the browser deals with that for us we don't have to think about it too much. So let's save this off, and let's go back to our code and refreshed to make sure we got the new stuff. And you'll see that we have the matches are now inside this detail's home, which is nice. Let's go ahead and grab our sample HTML content from Donald Knuth here. So we'll copy as HTML, paste it in here, all right? And let's go ahead and search for the image tag again. So when we search for image and there it is, it's selected. Now let's search for A tags, now the image is no longer highlighted, but the A tags are. So that fixed that issue. And also you'll notice that my matches are in here, they're just collapsed, which is nice, you don't want to see all that. So, that's it, and so if you had a different solution or had questions about my answer, you can ask questions through the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) learning website and I'd love to hear what you think and what you did.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (4), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Env Vars:** html (4), css (3)
> **CLI Commands:** find (3), make (2)
> **Cross-References:** go back to (2)
> **Code Identifiers:** queryselector (1)
> **UI Navigation:** scroll down (1)
> **Exercise Files:** source code (1)
> **Definitions:** is a  (1)


### 2. HTML to DOM

[↑ Back to Table of Contents](#table-of-contents)

#### [Where does the DOM come from?](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/where-does-the-dom-come-from?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/where-does-the-dom-come-from?u=76281980&t=0)** - [Instructor] As software has become more complex with layers on top of frameworks, on top of other frameworks, we sometimes start to treat the lower level functionality like a magic box. It does what we want, but who knows how it does it. Having a good working knowledge of how lower-level systems work will always make your high-level code better. To really get acquainted with the relationship between [HTML](../../Skills/Web%20Development/HTML.md) and the DOM, we're going to build an HTML pretty printer. So I've already developed a basic skeleton application here. You can see there's a text area where we can type or paste HTML source code. And then I'm going to have an output element down here, where I'll show the resulting pretty print eventually. And let's go take a quick look at the source code for this. It's a basic HTML5 page, and you can see that I have that text area in here. I have a custom element that I'm using for the output. And if we look at this [JavaScript](../../Skills/Software%20Development/JavaScript.md) file, pretty parsed JS, I'm intercepting the DOMContentLoaded event, and I'm adding an input listener to that text area. So when it changes, then I call this refreshOutput function and it is parsing the HTML that I got from the text area through the value property. And if we look at the parsing function down here, it's using a very common pattern. You'll see when you need to parse HTML in JavaScript, a lot of times you just create a div or some other element using create element for the document object. And then you set the inner HTML to the HTML you want to parse. And then I look through the child nodes and I add them to a document fragment. It can contain child nodes, but it doesn't have any markup of its own. And so let's go ahead and go back over to our app. And actually, first I'm going to show you, I'm using the standard set up I've been using for the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of this course where I'm using the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) built-in web server to host this,
>
> **[1:32](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/where-does-the-dom-come-from?u=76281980&t=92)** so nothing fancy. And we're going to go back in here and let's go ahead and type in something. So let's say: hello, pretty printer. And as you can see, I have some markup appearing down here. Now let's get a slightly more complex HTML example to work with. So I'll come over here for an HTML5 cheat sheet that gives some examples of semantic tags and how they can be used. So let's copy this and let's go back into our application and I'm going to paste it in and nothing. So that's kind of interesting. Let's go ahead and open our developer tools and see why. So we'll come over here and I'm going to find my output element, which is in the body here somewhere. There's our output section, here's our output tag. And you'll see, I have exactly one node. I have this comment node. This is the only thing that came through and that's really peculiar. So let's do this. Let's add a character turn. Oh! And now I have all the rest of my content. So when I was developing this application, I actually found a bug in the HTML5 parser in Chrome. And whenever you get rid of that character turn, all I get is the comment node, when I put the character turn back, it shows up. So it looks like we have two possible paths we can take now. We can either: one, leave the character turn in and continue with the pretty printer as as we were planning to, right? Or two, we can write our own HTML parser in JavaScript. And you know, while we could do number one, where's the fun in that? So let's stay tuned as we enter the fascinating world of recursive descent parsers and lexical analyzers.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (9), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **Env Vars:** html (9), html5 (3), dom (1)
> **CLI Commands:** node (3), make (1), python (1), find (1)
> **Exercise Files:** source code (2)
> **Code Identifiers:** refreshoutput (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [DIY HTML parsing](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/diy-html-parsing?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/diy-html-parsing?u=76281980&t=0)** - [Instructor] Writing the code to convert human readable [HTML](../../Skills/Web%20Development/HTML.md) source code into a browser friendly DOM tree structure, can seem overwhelming at first. There are many different approaches that can be taken, but one of the simplest to understand is a recursive descent parser. Recursive descent parser is a top-down parser, meaning that you start by writing a function that will consume the entire text to be parsed. Let's call this function ParseContent. It will be able to parse any sequence of HTML syntax we give it. While our parser could technically be built to read the input text one character at a time like a file, most real-world parsers use what is called a lexical analyzer or a lexer to make things simpler. A lexer is like a smart file interface, but instead of just returning single characters, it can recognize and return groups of characters called [Tokens](../../Skills/Web%20Development/Tokens.md). So our parseContent function will be able to peek at the next content to be read from our source file and delegate parsing to a more specific parsing function, which in turn may recursively call another parsing function. For example, elements can contain arbitrary HTML content; so parse element we'll call parse content as needed to consume nested HTML. So our plan is to write a top level parsing function called prettyParse, which will use a lexer object to read our HTML source code, and then return a completed DOM tree structure. To do that, we'll be making use of [JavaScript](../../Skills/Software%20Development/JavaScript.md) classes, lambdas and regular expressions. So let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (5), [Tokens](../../Skills/Web%20Development/Tokens.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Env Vars:** html (5), dom (2)
> **Definitions:** is a  (2), is called (1)
> **Code Identifiers:** parsecontent (1), prettyparse (1)
> **Exercise Files:** source code (2)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Building a firm foundation: The lexer](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-firm-foundation-the-lexer?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-firm-foundation-the-lexer?u=76281980&t=0)** - [Instructor] Having a flexible, reliable lexical analyzer can make the difference between a nice clean performant parser and a mass of unmaintainable spaghetti. So before we start coding the parser, we're going to write a lexer class to support it. And we're going to be working from the pretty parser code base that was introduced in an earlier video, for the most part. So once again, it's just a simple [HTML](../../Skills/Web%20Development/HTML.md) page. It's got a text area on it that allows us to type in or paste HTML. I'm actually pre-filling it with a bunch of HTML so we don't have to keep cutting and pasting that in here. And whenever you modify that text area, it calls this refresh output function. And for the moment, I'm going to remove this pretty parse function, because this is the old pretty parse function that used the built-in browser parsing function. We're going to rewrite that completely. So for my purposes, I'm going to get rid of it. And here we're going to implement our new class. We're going to call this class lexer. And so this class is going to be a file like class. So when we construct it, so we're going to have a constructor that accepts source code. And what it does is it's going to store that in a property. So this.source equals source, and it's going to have a file pointer. And in this case, the file pointer is going to be initialized to zero. So let's see, let's save that off. Also, it's going to implement initially only one method, which is a read. So just like a file, you can read a character at a time. So what read will do is if this file pointer is less than zero, or this file pointer is greater than or equal to
>
> **[1:36](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-firm-foundation-the-lexer?u=76281980&t=96)** the source length, then we'll return undefined. I can easily try to read before the beginning of the file or after the end of the file. Otherwise we'll just return this, the character at this.source, position file pointer, plus plus. (indistinct) all right. So that will enable us to read it character by character. So in order to exercise this, I'm going to create a temporary test bed. I'm going to create a test lexer method, which will accept the source code that we typed in here in our text area, so. And it's going to be basically a unit test. So we'll go down here, we'll create a new function called test lexer, which accepts source code. And then, inside here, mostly what we're going to be doing is just asserting that things work. So for now let's create a new lexer. So we're going to use the constructor, right? So we're going to say new lexer with our HTML. And I'm going to create a temporary variable, a string. And I'm just going to read the entire source from the lexer and confirm that it does match because it should be exactly the same thing. So while, while there are more characters to read. So in order to know when I'm at the end of file, I'm going to add a new called EOF. So EOF, end of file is going to tell me false if there are more characters to read, or true, if there aren't,
>
> **[3:07](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-firm-foundation-the-lexer?u=76281980&t=187)** so we're going to return this file pointer. If it's greater than or equal to this source length, then we're at the end of file. So now I have an EOF property. I can go down here while, but not lexer EOF output. I will append the next character lexer read. So we'll read a character. And to confirm all of this works, I can just do a console.assert that my new string or that my old string equals my new string. So let's save all this off, go back to the browser, and look in the console. Now, obviously there's no output. Does that mean that it worked or it didn't work? Just to be sure. Let's go ahead in here and we'll add another assertion.
>
> **[4:03](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-firm-foundation-the-lexer?u=76281980&t=243)** To make sure that our code is actually being executed. That's always a good rule. Yep. So you'll see our code is being run. So get rid of that. Now let's start adding some more useful methods to support parsing, but before we do that, we need to create a function to reset our file pointer, right now at the end of file here, we just read the entire thing. So I'm going to create a new method up here in our lexer called rewind. And its only job is to reset the file pointer to zero. So this.filepointer equals zero. Now we can start reading again from the beginning. So let's save that off. Now one of the most common operations you need to do while parsing is to match the text that's currently under the file pointers. So if you can picture it, the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the text that you haven't parsed yet against a sequence of characters or a regular expression. So we're going to write a new method called match. So let's go ahead and write a method here after rewind, we'll call it match. And it's going to accept a token to match against. It's a very simple function, but what it needs to do is look at the remaining text. And in order to support that, I'm going to create a new property called remainder. So let's go ahead and write that down here below EOF, we're going to create a new property called remainder. And what it does is it returns the text that remains to be read from our file. So it's pretty simple, return this.source, and we're going to return a sub string, starting at this.filepointer. And that will return the text that remains to be read from our source code. So we're going to use that remainder
>
> **[5:35](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-firm-foundation-the-lexer?u=76281980&t=335)** and we're going to search it for the token. And if it strictly equals zero, then we've matched. Okay? So we'll save that off. And let's test our new function. So first we're going to have to rewind our lexer. So we're not at the beginning of file or not the end of file anymore. And let's do a console.assert. Now if you look at our browser, you can see over here that the very first token to be read is the beginning of this comment. So we're going to go ahead and look for that. So we go back into our source code. And we're going to assert flexor match, that it matches that token, which is the beginning of an HTML comment. All right, let's go ahead and save that. Go back into our browser, refresh. Now you'll notice that no assertions were thrown, which is good because we shouldn't be getting any output here. Just to be sure that our lexer match logic is right, let's go ahead and add another assertion here, a negative case. We're going to assert match random text. Let me go back here, refresh. Oh. So if you see, back in our code, yep that didn't work. And just to make sure we don't break match in the future, we'll leave that in there and say it should match that, it shouldn't match random text. Before we move on an important detail to notice here in the match function is we're using the string search method and the search method is great because it can accept either a string or a regular expression. And pretty soon we're going to be taking advantage
>
> **[7:07](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-firm-foundation-the-lexer?u=76281980&t=427)** of that regular expression functionality. So that was not an accident. Just want to be able to come back to that later. Now although we've matched that initial string, that comment opening sequence, it didn't consume it. So we're still, the file pointer hasn't moved and we're still looking at the beginning of that token. So another very common operation is to consume the thing that you've matched. So in order to support that, we're going to write another new method called consume match. So let's go back into our lexer class, right after match here. And we're going to call this one consume match, it excepts a token as well, which could be once again, a string or a regular expression. And this is important because consume match is a little more complex than you would think it should be. But what we're going to do is we're going to say the match equals the result of the remaining text, so remainder match token. Now if matches truthy, so, say match and match length, so match is an array of matches and that if it's a string, it's going to be a length of one. But if it's a regular expression, it could be more than one. So we're going to say if the length is truthy as well, and match index, if it matches the entire expression, so a lot of this doesn't make sense unless you dig into regular expressions and matching capturing groups, which unfortunately we don't have time to show all of in this course, but there's a course on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) I believe about regular expressions and matching. So if you're not familiar with regular expressions, it's definitely worth checking out. So now that we have the match logic in here,
>
> **[8:39](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-firm-foundation-the-lexer?u=76281980&t=519)** we'll say this file pointer, we need to add the size of the thing that was matched to the file pointer. And so that's available in the match group at location zero length. So now we'll be skipping the thing that we found and returning true if we matched it. And now if we didn't match it, return false. And let's test that logic. Let's go back into our unit test function. And let's consume the match. So console assert lexer.consume the beginning token for the HTML comment. And we'll save this off, we'll go back to our browser and refresh. And no assertions, so we are consuming it successfully. So now that we have this matching logic in place, the next thing that we're going to implement is a function that allows us to read a stream until a particular condition is met. And this happens all the time. For example, in this case, if we look at our source code again, now that I'm sitting here, I want to read everything until I get to the closing tag. So I'm going to create a new method in our lexer called read until. So let's go back up into the lexer, so right below consume match here, read until, and read until is going to accept a call back. So I'm going to call this call back condition, and all read until will do is get the start pointer. So we're going to save our file pointer. So start pointer equals this.filepointer.
>
> **[10:13](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-firm-foundation-the-lexer?u=76281980&t=613)** Now, while we're not at end of file, and condition, so we're going to call back the function that was passed into us. And we're going to pass in this as the argument, it's going to send the lexer to the callback function. So while not condition, so while we're not at end of file and our condition isn't met, this.filepointer plus plus. So we're going to continue skipping through the file. And then what we're going to do is return the sub string that was read. So return this.source, .substring and the start index is going to be the start pointer. And the end index is going to be the file pointer. And to see this in action, let's come down to our test right here. And now what we need to do is get our comment text, and we're going to do that by reading until we see the closing part of the comment tag. So lexer.readuntil, and our condition is going to be an arrow function. It accepts a lexer. Lexer.match, and we're going to match the end of the comment. So let's save that off. Actually, let's go ahead and print it out. Console dot log comment, and we'll see what we got. oops All right, let's go and check our code. Refresh. And now you can see, we have our comment. So now that we have our comment,
>
> **[11:45](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-firm-foundation-the-lexer?u=76281980&t=705)** let's go ahead and consume that ending markup. So consume match, the end of the comment, just we don't have to deal with that, because the next thing we're going to do is we're going to parse that header tag. So let's go back and look at our source code again. So we want to parse this tag next. So the first thing we're going to do is we're going to consume that angle bracket there. So let's assert console.assert, lexer.consume match. And we're going to consume that, so that we'll skip over that. Now we're going to add a new method that doesn't exist yet, but we're going to call this one, let's say tag equals lexer.readidentifier and read identifier is just going to read the characters that comprise an identifier. So from here, it'll stop reading when it gets to the end, because this is not part of an identifier. So that's going to be just a convenience method. We're going to wrap around read until. So let's go back into here, and let's say read identifier, and it's going to use a regular expression, but we're going to return this.readuntil, and we'll use once again, we get the lexer in here and an arrow function. We're going to read until we don't match a regular expression. And the regular expression we want is [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) character. So we're going to use w, so basically until we match a non-word character, we will keep reading. So this will read an entire identifier. And then just to be sure that everything is copacetic over here, we're going to do an assertion. So down here let's well, let's just print it out first. Let's console log our tag, and see what we get.
>
> **[13:20](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-firm-foundation-the-lexer?u=76281980&t=800)** So let's save this, go back to the code, refresh and our tag is header. So that's good. And the next thing we need to do is, I want to show you something, let's go back in here. Remember from the previous video, possibly my gripe was, if you hit character turn things break, you'll see down here, it broke. So the last convenience function we're going to introduce actually skips over white space. And it looks almost exactly like the one you've seen before, the read identifier function. In fact, it's so similar, I will probably end up copying and pasting this, so. We're going to skip white space. So we're going to read until going to call us when skip white space. All right. And what it returns, this part of the arrow function, all we got to do is change this regular expression. So we're going to read as long as it's a space character, which is backslash S, so now we'll come down here, and right after the comment, we're going to add some code to right here. Lexer skip white space. Let's go back in here and refresh. And everything is good to go. So that concludes the lexer class. This is the entire thing. And with these methods, we will be able to write our complete HTML parser. So stay tuned for the rest of that. And we will start parsing HTML.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (8), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Env Vars:** html (8), eof (5)
> **Exercise Files:** source code (7)
> **CLI Commands:** make (4)
> **Cross-References:** go back to (3), previous video (1)
> **Definitions:** is a  (3), is an  (1)
> **Analogies:** just like (1), picture (1), for example (1)
> **Prerequisites:** before we start (1)

#### [Parsing text](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-text?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-text?u=76281980&t=0)** - [Instructor] Now that we have a complete, tested Lexer class, we're ready to begin work on the parser itself. So the first thing we're going to do, I'm going to go back into my editor and I have the same setup as before, I have my text editor here. I'm running the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) built-in web server in my Chapter 2\02_04 project folder. And I am using Chrome here to browse. So let's go back to the editor. The first thing I'm going to do is I'm going to get rid of this test code that I had to exercise the lexer when we were building that. It's no longer necessary, so we can get rid of this, this lexer function. And I'm going to write my prettyParse function. So function prettyParse, and it's going to accept [HTML](../../Skills/Web%20Development/HTML.md). And in the end it will be returning a DOM. Now we'll append the response from prettyParse to our output elements, so output appendChild(prettyParse) and it accepts our HTML source code. Now, with that housekeeping out of the way, we can focus on our parser function. So inside prettyParse, we're going to write a scope function called parseContent. Now parseContent's job is to parse any and all HTML content that is passed into prettyParse. So the way we're going to handle that is we're going to create a lexer at the top level here that will be part of the syntax scope of this prettyParse function. There's going to be a new lexer object which has our HTML source code. Now inside of parseContent, we're going to have an accumulator that's going to accumulate the text that we parsed as we parse it. So text is just going to be a string. There we have it. And parseContent will also, it'll return a document fragment because its job is to parse
>
> **[1:35](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-text?u=76281980&t=95)** any sequence of HTML we're given, so it could be a series of text nodes and comment nodes and elements, we're not sure. So we're going to create a fragment, document.createDocumentFragment. And that's what it will be cumulating its parsed elements into a parsed node. So let's do a while loop. While we're not at the end of file, we will read the next thing we need to look at. So inside this loop, we're going to accumulate texts. So right now it's going to parse the entire thing as if it were one big block of text. So we're going to do that.
>
> **[2:09](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-text?u=76281980&t=129)** - And now, instead of blindly using this accumulator at the end of this while loop, we're going to write ourselves another inner function here called flushText. And this is because it'll become apparent later, why we need to call this code in various different places. So flushText's job is to say, if there is text in the accumulator, then we're going to create a new text node and we're going to append it to our fragment. So fragment appendChild(document.createTextNode()) with our text and we reset the text to blank. And now let's see. So now we have that in place and at the end of this, we're going to flush our text and we're going to return our fragment. We'll save this off. Now, all we to do for prettyParse itself at the end is return parseContent. In the end, we'll parse any sequence of HTML content we come across. So let's go ahead and save this off and let's go back to our browser and refresh. Now, you'll see if we look in here in the developer tools that we have one big text node that has all the texts from our HTML source. And that was the goal at this point. So from now on, we're going to start differentiating between the markup inside of our source code and start returning more sophisticated DOM content. So let's do that in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (7), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1)
> **Code Identifiers:** prettyparse (8), parsecontent (5), appendchild (2), flushtext (2), createdocumentfragment (1)
> **Env Vars:** html (7), dom (2)
> **CLI Commands:** node (3), python (1)
> **Cross-References:** go back to (2), in the next (1)
> **Exercise Files:** source code (3)
> **Speakers:** - [instructor] (1), - and (1)
> **Prerequisites:** setup (1)

#### [Parsing comments](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-comments?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-comments?u=76281980&t=0)** - Now that we have a basic parsing loop implemented, we need to start recognizing different types of [HTML](../../Skills/Web%20Development/HTML.md) markup. And the simplest type of markup to recognize is the HTML comments. So let's start there. Let's go back to our browser briefly. And in our HTML sample, you can see we have a comment right here at the very beginning, and it consists of this opening tag syntax here, which is a less than bang dash dash. And it's ended with dash dash greater than 10. And there are some more complicated rules about comments, but we're not going to mess with them. We're just going to stick with this. So let's go back to our code and in our parsing loop here, we're going to just add an if statement. So what we can do is if we find this opening token, so we're going to use the lexer consume a match so remember consume match looks for a token, and then it will actually discard that token if it finds it, but it'll return true. So we will know we've consumed at the beginning of a comment. So the first thing we need to do, the reason we've wrapped this flushed text code. You notice up here, if we've accumulated any texts, we need to append that to our document fragment. We need to add flush text here to flush any texts we've accumulated so far. And then all we have to do is parse that comment and appended it to our fragment. So fragment append a child, and we're going to write a new rabbit function called parse comment, and it will use the lecture we already have. So it doesn't have any context. And that's kind of the way we do the recursive descent parsing is everybody has a shared context, like a shared lexer that way we don't have to keep passing things through in arguments. It gets kind of tedious. So we're going to write this new private function up here called option parse comment and parse comment will first we'll get the comment text.
>
> **[1:37](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-comments?u=76281980&t=97)** So the comment text is going to be everything between those two [Tokens](../../Skills/Web%20Development/Tokens.md), right? So we're going to use lexer read until again, and it accepts the lexer as an argument on the callback. And we're going to match. The closing syntax that's dash dash greater than sign, right? So that will give us everything between, if you look back at our example here, that will give us all this text between this opening syntax and the closing syntax. So we have that stored in comment text. Now we need to consume this token. Otherwise, when we resume parsing, we'll get this stuff and we'll show up as text in our Dom tree, and we don't want that. So we need to actually consume the match. So lexer consume match our closed syntax there. And then all we have to do is return a new comment node. And so we do that using document dot create comment. So return document, create comment, put that comment text. Okay. Let's go back down into our loop and double check something. Okay. So the logic down here is if we consume this match, if we actually find the beginning of a comment, we do this or else. We're going to accumulate more texts. So let's move this into our else branch here. So if we don't recognize the opening of a new comment, we're just going to keep accumulating texts. So let's go ahead and save this off. And let's go back into our browser and refresh and make sure I've started up my server. So it's going to do that. Sorry. So I'm in my project folder, [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) dash M HTTP dot server and there it goes. So let's go back into the browser.
>
> **[3:10](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-comments?u=76281980&t=190)** And now we can look in here and we'll see, there is my comment node. And then there is a big old text node with the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of my source code, just as a simple text right now. So now we can recognize texts and comments. That's great. So now we need to do is add support for elements and attributes, and we will have our very own bare-bone HTML parser. Before you watch the following videos, take a minute to think about how you would implement the element parsing logic, which lexer calls would you need what Dom calls, we would use. Take a little time to think about it. This will do wonders for your code sense and your problem solving skills.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (4), [Tokens](../../Skills/Web%20Development/Tokens.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** node (3), find (2), make (1), python (1)
> **Env Vars:** html (4), http (1)
> **Cross-References:** go back to (2)
> **Definitions:** is a  (2)
> **Exercise Files:** source code (1)
> **Speakers:** - now (1)

#### [Parsing elements](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-elements?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-elements?u=76281980&t=0)** - [Instructor] Now, parsing element content is the most complex task we need to do to complete our [HTML](../../Skills/Web%20Development/HTML.md) parser. This is because some elements have no closed tag, some attributes have no values, and many elements have nested element content. It's this nesting that the recursive part of our recursive descent parser will handle. So once again, our development environment is very simplistic. We're going to be using the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) built-in web server. So Python dash M http server that's comes with Python three in our project folder, and we're going to be using Chrome as our browser. So you can see over here, this is the current version of the code, and I'll be using my text editor, which at the moment I'm using Atom, which I still kind of like. So in order to support element parsing, we're going to be modifying the parse loop, which is located here in the parse content function and as you can see, we're currently parsing comment because we know comments begin with this open comment tags syntax. So how do we know that we're parsing an element? We basically just need to match a less than sign. So, we're going to do it else, if lexar.consumeMatch less than time. Then we're parsing an element, and we can get rid of that. So logically at this point, all we really need to do. It looks very similar to what we did for comments. We're going to flush text in case we parsed any texts, and accumulated it up to this point. And we're going to append the element that we're going to parse. So fragment.appendChild, and we're going to write a new function called parse element. and it will behave very similarly to parse comments.
>
> **[1:34](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-elements?u=76281980&t=94)** So let's go back up here. Above comment, I guess, and we will write parse element. Actually we'll do it right after parse comment. So function parse element. Now the parse element function. If you think about where we're at, let's go take a quick look at the source code. We just consumed this character. So the next thing we're going to do is we're going to get the tag name. And so we've already added. If we look back into our lexer, we added a read identifier method to that. So that'll be pretty easy. We can grab our tag name here by doing const tag name equals lexer.read identifier. So there's our identifier. Now that we have the identifier, we can go ahead and create our element. So we're going to create an element node using element, and we're going to use document.create element for that tag name, there we have it. So now we have a place to put the stuff that we're going to start parsing. So let's save early. Save often, make sure we're not losing anything here. We're going to come back in here and we have our element, and we're going to, there are a couple of different cases we need to deal with. First of all, let's go back and look here. I mean, our header tag is pretty simple because there is no attribute content or anything here, but that's not the case everywhere. You'll see, for example, there's a link here is going to have attributes stuff. So the first thing we need to do is deal with attributes, and for the purpose of making this somewhat more concise and easier to understand we're going to just skip attributes for now. So we're going to put in a nice, big TBD comment here, and we're going to say parse attributes,
>
> **[3:10](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-elements?u=76281980&t=190)** but that's going to happen later, for now we're just going to skip them. So all we need to do to skip them is Lexar read until There are couple different cases we need to look for. So we're going to use as a regular expression to know when we've reached the end of the tag. So lexer match, and we're going to use a regular expression. It's going to look a little complicated, but I'll walk through it with you. So first we're looking for an optional slash character because of the tag has a trailing slash that means it's empty. It has no content. Then most definitely followed by a greater than sign, which means with the end of the open tag, and these two slashes basically delimit this whole thing as a regular expression in [JavaScript](../../Skills/Software%20Development/JavaScript.md). So now we've got all of our closing parentheses there. So lets go and save that off. Now at this point, we know we're looking at either one of two possible strings. We're either looking at a greater than time by itself or a slash greater than sign. So what we're going to do is we're first going to deal with a case where there's nested content. So lexer.consumeMatch greater than time. That means we are at the end of an open tag. That's going to have nested content for our purposes. So element.appendChild, and we get to call parse content again. So this is the recursive part of the recursive descent parser because elements can contain arbitrary sequences of nodes. So we're going to call parse content to get the contained HTML. Then down here, we're going to consume our close tags. So consume match. We know clothes tags start with less than 10 and a slash, and all we're going to do for our purposes now is we're not validating this or anything.
>
> **[4:41](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-elements?u=76281980&t=281)** We're just going to dump everything until we get to the close greater than sign. So now we're going to read until lexer, and our condition in this case is until we see a greater than sign. So Luxor dot consume match. And that means we're not only going to read to that greater than sign, but we're going to discard it as well. So when we're done, if you picture what happened, the parser in this case, let's say we were parsing something that had nested element content. So we parsed this thing. Let's parse that definition list, arc parse content, call parsed all of this stuff, and then we read this token. If you look back at the code. That was consumed by this, right? And then we're going to basically consume everything up until this character, as well as that character. So we end up here. And so as soon as this sequence of code is complete, our lexer is sitting parked right here at the end of that close tag. And that completes the case where we have an open tag that has nested content. So there's one more case we care about, which is a empty tag or has no close tag. So in this case, we're just going to consume match this token which means there was nothing inside that element. It has no nested content. And now there's nothing left to do except return the element so let's return the element. So now let's go back to our loop here and it's going to basically, we're going to step through let's just go ahead and run this really quickly, and see what happens. So we're going to refresh this page, and we see we've got nothing. Let's take a look at why. Okay. Now this is interesting.
>
> **[6:16](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-elements?u=76281980&t=376)** We ended up here with a tag name of blank. And the reason for that is we don't know when to stop parsing If you look at our parcel loop inside the inner loop here, it reads until the end of the file. But when we're parsing nested content, for example, we've parsed this header tag, let's say, and we want to look at everything within the header. We actually want to stop parsing when we get to the beginning of the close tag. So we need to modify our loop here slightly. We're going to actually look for one more condition. So it'll either get to the end of the file, or in this case we're going to match the beginning of a close tag. Now let's save that off let's refresh, and voilà, while we have HTML content, so we now have nested element content. And once we remove this pesky TBD comment in our code, up in here where we're parsing or failing to parse attributes, we will have ourselves a fully functional if pretty simple HTML, parser.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (4), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (3), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Env Vars:** html (4), tbd (2)
> **CLI Commands:** python (3), node (1), make (1)
> **Analogies:** for example (2), kind of like (1), similar to (1), picture (1)
> **Code Identifiers:** consumematch (2), appendchild (2)
> **Cross-References:** go back to (1)
> **Tools:** atom (1)
> **Exercise Files:** source code (1)

#### [Parsing attributes](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-attributes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-attributes?u=76281980&t=0)** - [Instructor] So we're on the home stretch with our home grown [HTML](../../Skills/Web%20Development/HTML.md) parser now. The last thing we need to implement is attribute parsing, So let's go back to our source code again, and once again, I'm running the [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) web server here, that's included with Python 3, so I'm running it in my Project folder. So back here in the editor, you'll see that we previously left a TBD comment in here somewhere. So, yep. Right there. So we wanted to parse our attributes later. We just skipped over them for now. So now it's time for us to actually implement that for real. So there are a couple of interesting cases that we have to think about. If you look over here, our HTML is not super complicated, but you'll see that we have an attribute here, an HREF attribute, which has a value and has equal sign and then has a quoted string, but the quotes are actually optional. You can have unquoted values, and you can also have attributes that have no values. For example, you could say, you know, disabled, although that isn't honored on the A tag. You can have attributes like that. So we need to accommodate all of those cases of attributes and do the right thing. So let's go back into our code. The first thing we're going to do is create a attribute parsing loop here. So it'll resemble our parsing loop we have down in parseContent, but let's do, first we need to skip the initial whitespace, if there is any. Skip whitespace is completely innocuous. If there's no whitespace, it won't do anything, so- So now while we're not at the end of file, and we haven't matched the end of the tag, so that's going to to be the lexer match. And we're going to do a regular expression here, but we're going to look for, optionally, a forward slash,
>
> **[1:34](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-attributes?u=76281980&t=94)** because it could be an empty, no content tag. And so we're going to use the question mark attribute there, so that basically says an optional forward slash followed by a greater than sign. And that will terminate the open tag. So while we have not reached the end of the open tag, now, all we're going to do is we're going to take our element and we're going to set an attribute. So we can use this setAttributeNode method, which will accept an attributeNode type, remember we talked about node types in another video. So we're going to write a parseAttribute function, which will return the attribute in the form of a node. So that will include both its name and its value. And that's a very compact way for us to parse that attribute and send it back. And then all we need to do here is skip whitespace again, because there might be trailing whitespace, or it would be considered preceding the next attribute possibly. So now all we have to do is write the parseAttribute function. So let's go ahead up here, and we will create this parseAtribute function right above our parseElement function. So parseAttribute. Now it's important to keep context and know where we are in the stream. And if you look at the sample code we have here, let's pretend we were parsing this A tag. So you can see down here in our loop, we've gotten the identifier. So right now in our stream, we are sitting right at the end of this identifier. And then our initial skip whitespace code is run. So we're going to skip over that. And now we're sitting right in front of the attribute name, no matter what,
>
> **[3:05](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-attributes?u=76281980&t=185)** even if it was an attribute that had no value, the next identifier we could read is the attribute name. So let's go back up to attribute, parseAttribute here, and we're going to get the attribute name. And we're just going to use, lexer.readIdentifier. And so now we have the attribute name. We can use the browser's createAttribute. So we're going to use, we're going to call it attribute equals document createAttribute with our attribute name. Now, so now we have a node. Now this attribute node at the moment has no value. So the next thing we need to do is look and see if there is, in fact, a value. The way we're going to do that is we're going to look for the equal sign, possibly preceded and followed by spaces. So we're going to use once again, regular expressions are your friend, especially if you're writing parsers. So in this case, we're going to consume a match. We're going to consume a match. You remember consume match will attempt to match either a regular expression or a string, and then swallow that string and not allow it to be parsed any further. So we're going to look for zero or more spaces, followed by an equals sign, followed by zero or more spaces again. And that's the end of our expression here. So let's go ahead and do that. And if this matches, if it's true, then that means this attribute has value. And then save this off. Now this is going to look a little weird, but I'm going to go ahead and create a value variable, but I'm not going to initialize it. So it's going to be deliberately undefined at this point, because there are two possible cases. If this value has quotes around it,
>
> **[4:39](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-attributes?u=76281980&t=279)** so let's look for that. Lexer.match, we're going to look for one of two possible quote characters. So we're going to use a regular expression, again, we're going to use a character class. And in this case, we're going to use single quotes or double quotes. And my editor's being really smart, but I'm looking for either a single quote or a double quote. So if this matches, and you notice it did not consume, but it matched, right? So if this value begins with a single quote or a double quote, we can get the closing character. So we're going to read the initial character and that will be the character we're looking for to close. So closing char equals lexer.read. We'll read the next single character. And now we know what we're looking for at the end of this. So we're going to say value equals lexer.readUntil, and we're going to to read until we see that closing character. So I'll just add a lexer match, closingChar. And notice that we are not consuming this because then it will become part of the value and then we'd have to pull it out and everything, so- Now we have our value lexer.consumeMatch. We need to consume that closing character so it doesn't get left in the stream to be parsed later. That's good, and save that off. Now, there's just one more little case here. So if there was no quote, we have an equals sign followed by something. So we're going to take everything up until the first whitespace or the end of the opening tag. So in order to do that, we're going to say the value will equal lexer.readUntil again, and we're going to use a regular expression again with a character class. Match, and this case our regular expression is going
>
> **[6:12](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/parsing-attributes?u=76281980&t=372)** to consist of either a space character or a forward slash or the end of the open tag with a greater than sign. And we'll end our regular expression there. And we'll save this off, make sure I actually wrote my regular expression correctly. So we got that there, that's better. So now, either one of these branches, either we've gotten a quoted string as our value for the attribute, or we have the unquoted value. Let's set the attribute value. So attribute.value equals value. And all we have to do now is return our attribute. We should have, if we go back to the browser, we refresh, and we scroll down, we have a working hyperlink because our attribute was, in fact, parsed. So obviously this parser is not fully compliant with the HTML5 spec and it will not deal well with badly behaved HTML source code, but it should give you a good idea of what goes on under the covers of your favorite browser and give you some ideas for your own language parsing adventures.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (3), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (2)
> **Code Identifiers:** parseattribute (4), createattribute (2), readuntil (2), parsecontent (1), setattributenode (1)
> **CLI Commands:** node (4), python (2), make (1)
> **Env Vars:** html (3), tbd (1), href (1), html5 (1)
> **Cross-References:** go back to (2), we talked about (1)
> **Exercise Files:** source code (2), sample code (1)
> **Versions:** python 3 (1)
> **Documentation:** spec (1)

#### [Challenge: Void elements](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/challenge-void-elements?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/challenge-void-elements?u=76281980&t=0)** (upbeat techno music)
>
> **[0:06](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/challenge-void-elements?u=76281980&t=6)** - [Instructor] I have a confession to make. We didn't really build an [HTML](../../Skills/Web%20Development/HTML.md) parser. It's really more of an XHTML parser and the reason is that it doesn't recognize void elements. Now in normal HTML, these tags in this list don't require and shouldn't have closing tags. Your challenge today is to modify the pretty parse code to honor this list of void elements. And I'll show you quickly in the application what that means. So if we come into the browser here that's running our code and we add an hr tag, let's say, to the footer. You'll notice I didn't use the self-closing syntax and all of the contents of the footer were included inside the hr, which made it look a border sort of. So if I use the self-closing XHTML syntax, you'll see I now have a normal hr. So the challenge is to make our parser recognize that list of void elements which don't require closing. So I got my list from the W3C and you can too. Good luck.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (2)
> **Env Vars:** html (2), xhtml (2), w3c (1)
> **CLI Commands:** make (2)
> **Documentation:** w3c (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat techno music) (1)

#### [Solution: Void elements](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/solution-void-elements?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/solution-void-elements?u=76281980&t=0)** (happy techno music plays)
>
> **[0:06](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/solution-void-elements?u=76281980&t=6)** - So, the challenge was to add support for void elements to "prettyparse". So we were given a link to the W3C site where a list of valid void elements can be found, so. We're going to start by getting that list and we're going to turn it into a [JavaScript](../../Skills/Software%20Development/JavaScript.md) array. Now, this is a very common task you'll run into a lot. You'll have to pull some reference material and manipulate it. This is once again a good use of regular expression. So if you'll find, I'm in my editor here, I'm going to look for words or [Tokens](../../Skills/Web%20Development/Tokens.md), you know, by looking for any one or more [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) characters, and I'm just going to replace it with a single quoted version of that. So now you'll see, I have a good list of void element names. So copy that. Now I need to go to the code. The place we need to modify this, is in our "parseElement" function. Let's go to "parseElement". And what you'll see is in here, this particular "If" branch, First of all, let's create our array of void element names. So "voidElements" equals an array with these strings. And we can argue style all day long, so we're not going to do it. I mean, I'll just show you how I would tend to format this for readability as best I could. I tend to do a double indent here, just so I know that it's not part of another brand new statement or something, and I'm just going to wrap it because I don't want to pick up however many lines that would be like 12, 13 lines in my code. And I'm going to go ahead and do this, just to make it clear that that's what we're saving here. This is our "voidElement" array.
>
> **[1:38](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/solution-void-elements?u=76281980&t=98)** So, now we have that "voidElement" array. The next thing we need to do is if we look down here, this "If" statement, if we consume that closing greater-than sign, that means we had a tag with no self-closing slash included. So what we're going to do, is inside this "If" statement, if we're looking at a void element, so we're going to use the "voidElements.includes" that will tell us if the tag name is inside that array. If it's not in the array, we're going to process the contained markup. So, and save this off. Now, let's go back into our browser, refresh. Make sure I'm running my actual web server, so. And now it's running. Now, let's go back here and let's add an HR tag to our footer. So here we have added horizontal rule and you'll see it's an actual horizontal rule instead of the bizarre kind of border effect we were getting before. And we didn't break our self-closing syntax either. It still works. And actually this is going to kind of work, I think, but I wouldn't rely on this being the case. It's not, you're not supposed to be able to have a closing tag for one of these, but yeah. So now we actually have support for void elements. That's great. And that concludes our work on the parser. And so in the next chapter, we're going to use this parser to build an [HTML](../../Skills/Web%20Development/HTML.md) Pretty Printer that will create attractive output from our HTML source code.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [Tokens](../../Skills/Web%20Development/Tokens.md) (1), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Code Identifiers:** parseelement (2), voidelements (2), voidelement (2)
> **CLI Commands:** make (2), find (1)
> **Env Vars:** html (2), w3c (1)
> **UI Navigation:** go to (2)
> **Cross-References:** in the next (1)
> **Documentation:** w3c (1)
> **Exercise Files:** source code (1)


### 3. Transforming the DOM

[↑ Back to Table of Contents](#table-of-contents)

#### [Traversing the tree](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/traversing-the-tree?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/traversing-the-tree?u=76281980&t=0)** - [Instructor] Before starting work on our [HTML](../../Skills/Web%20Development/HTML.md) pretty printer, let's do a quick review of tree traversal [Algorithms](../../Skills/Software%20Development/Algorithms.md). We'll be using this odd, but perfectly valid, HTML snippet, which is parsed into this DOM tree. Note that I've eliminated all the text nodes for clarity. A tree traversal is just an algorithm that visits each node of the tree in turn. The order in which we visit the elements is determined by the traversal method we choose. The most useful ones for dealing with the HTML DOM are preorder, postorder, and breadth-first. The first and most common order is preorder, or document order. In a preordered traversal, the elements are visited in the same order that they appear in the source HTML. Preorder can also be thought of as document order. If you want to manually copy a DOM tree, you should traverse it in preorder to make sure that the parent elements exist before their children are processed. The basic traversal algorithm is to visit the current, or root, node first, then visit its children. A much less common, but still useful traversal method, is postorder. In postorder traversal, the children are visited before their parents. Postorder traversal can be considered bottom up. Since children are processed before their parents, it is possible to delete a DOM tree using postorder traversal. The traversal function should visit the current, or root node's, children then process the root itself. The last traversal type we will cover is breadth-first, sometimes called level order. This traversal visits every node at a given depth, or level of the tree before moving to the next. Level order can also be thought of as back to front. In the case of the DOM, this traversal is useful when rendering the nodes on the screen, The first nodes visited are rendered under the later nodes. The algorithm is more complex than those for pre and postorder, requiring a level to be calculated for each node.
>
> **[1:33](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/traversing-the-tree?u=76281980&t=93)** And it generally requires multiple traversals of the tree to visit every level. While these are the most common traversal methods, there are definitely others. Take a minute to think of other ways to visit every node in a tree. What might they be useful for?

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (4), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (1)
> **Env Vars:** dom (5), html (4)
> **CLI Commands:** node (6), make (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Building a pretty printer](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-pretty-printer?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/building-a-pretty-printer?u=76281980&t=0)** - [Instructor] So as a quick refresher, the goal of our pretty printer application is to transform boring [HTML](../../Skills/Web%20Development/HTML.md) source code like this into attractive, colorful source code like this. The way we'll accomplish this is by doing a recursive transformation of our input DOM tree into a fully annotated output tree. This is a little overwhelming so let's [Zoom](../../Skills/Software%20Development/Zoom.md) in for a closer look. Take this comment markup. When parsed, it yields a DOM tree with a single comment node. Comment nodes aren't visible on screen so we need to transform them into element nodes that can then be displayed and styled. Now, let's look at transforming element content. Elements are potentially more complex than comments. Yeah. One reason is that besides the normal DOM content nodes, elements may also include attribute nodes. So to transform this single a element, we'll need to omit the opening tag element, including all the associated attributes, the closing tag element, and all the nested content in between. No problem. We'll just write a function called prettyPrint that accepts a DOM node, and we'll return the transformed output, style it with CSS, and we're a business.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (1), [Zoom](../../Skills/Software%20Development/Zoom.md) (1)
> **Env Vars:** dom (4), html (1), css (1)
> **CLI Commands:** node (2)
> **Exercise Files:** source code (2)
> **Code Identifiers:** prettyprint (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Processing document fragments](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-document-fragments?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-document-fragments?u=76281980&t=0)** - [Instructor] Sometimes getting started with a new project is the hardest part. Writing an entire [HTML](../../Skills/Web%20Development/HTML.md) Pretty Printer sounds complex but each step in the process is actually pretty simple. So let's go ahead and get started. My development environment, once again is very basic. It's a [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) web server running in the project folder. You can find that in the [GitHub](../../Skills/Software%20Development/GitHub.md) repo for this course. I'm using Chrome as my browser and I'm using Atom as my editor and you can obviously use whatever tools you like better. But the thing we're going to do is write our prettyPrint function, which will be called here from refreshOutput. You'll see this is the DOM tree that come out of our parser and prettyPrint essentially transforms that DOM tree into another DOM tree that has HTML markup and CSS styling to make it attractive. So let's call our function prettyPrint. And it accepts a node as an argument. And for now, all it's going to do is return a copy of that node. So node.cloneNode, recursive. So when you say true, that means deep. So it's recursively going to clone all of the children to that node as well. Let's go back to our refreshOutput function and we will do a prettyPrint of our output from our parser. So I'm going to save this off and if we go back into the browser, nothing should change, and nothing did change. That's good. And now we need to differentiate between node types. So let's go back into our prettyPrint function and we're going to use a switch statement. So stich based on node.nodeType and nodeType values, they're integers but there actually are constants available in the built-in Node class so we can do case Node dot, the first one we're going to process is a DOCUMENT_FRAGMENT_NODE.
>
> **[1:33](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-document-fragments?u=76281980&t=93)** DOCUMENT_FRAGMENT_NODE. And it is a special type of node that can contain children but doesn't have any external wrapping. Like an element can contain children but then there's the element itself. A DOCUMENT_FRAGMENT_NODE is like an element that doesn't have any tags or anything associated with it. And now we're going to create a function called, let's see, we're going to return the output of this function and we'll call this function printChildNodes and it's going to accept node as its argument. We'll write it as a scoped function so we don't pollute the global namespace anymore than we already have. So we'll call this printChildNodes. And it is going to first create a new document fragment. So fragment, and it's going to be created by calling document.createDocumentFragment. Next, we need to loop through all of the child nodes of our node parameter. So we're going to do node.childNodes.forEach. And we're going to pass in, we're going to use an arrow function and the name of the parameter will be child. And for each child, what we're going to do is we're going to recursively call prettyPrint on that child node and we're going to append it to our fragment. So fragment.appendChild prettyPrint our child there. So we will do this and that will essentially call the prettyPrint function on each child of our fragment recursively. And so this is part of the whole scheme, which is to continually traverse our DOM tree to the bottom and append the necessary markup around it. So let's go ahead and save that, run it in our browser. Nothing should change. Oh, but something did change.
>
> **[3:05](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-document-fragments?u=76281980&t=185)** That's because we forgot one crucial step. Let's go back into the editor here. Forgot to return the fragment. We'll save that off. Now, when we refresh. All right, everything looks exactly the same. That's good, that's just like the Hippocratic Oath. The programmer's oath should be first do no harm. So now we have the infrastructure we need to start implementing the node-specific formatting logic and the CSS styling around it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (2), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Code Identifiers:** prettyprint (8), refreshoutput (2), nodetype (2), printchildnodes (2), clonenode (1)
> **CLI Commands:** node (14), python (1), find (1), make (1)
> **Env Vars:** dom (4), document_fragment_node (3), html (2), css (2)
> **Tools:** github (1), atom (1)
> **Definitions:** is a  (2)
> **Cross-References:** go back to (1)
> **Exercise Files:** github repo (1)

#### [Processing comments](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-comments?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-comments?u=76281980&t=0)** - One of the simplest node types for us to format is the common node. You'll see, we have a comment note in our source code here. So let's start by adding a comment note handler to the pretty print function. We'll go back into our editor and go to the pretty print function. And you'll see here that we have our switch statement down here that lets us choose based on the node type to have different handling. So let's do a case node, comment node. So when we receive a comment node, we're going to create a new element. And this element is um going to be called a PP comment element. So comment equals document dot, create element. You know, we have to create elements so we can display them on the screen. So PP comment. And so this is once again a custom tag. And we're going to make the inner text of this comment. The node value of our node. So node node value. So that's the comment text. So the node value. Now we're going to return that comment node. I'll save that off. Now, let's go back into our browser and we'll refresh. And you'll see now that our comment, which was previously invisible, is visible in our DOM tree, but it doesn't look a lot like a comment. So let's add some styling to make it look more like a comment. To add that styling, we're going to use CSS. So we're actually going to create a new CSS file and let's see the rule we're going to add is for our PP comment, custom element, let's just set the color of the text to a medium kind of gray. So that would be hex 8, 8, 8. We'll save that off.
>
> **[1:35](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-comments?u=76281980&t=95)** We'll call it, pretty parsed dot CSS. Now we have to add a reference to that CSS file to our [HTML](../../Skills/Web%20Development/HTML.md) file, and I'll be honest, I have to look the syntax up every single time. It's been 20 years and it's so hard to remember, but REL equals a style sheet and then type equals text CSS. And H ref is our CSS file name, pretty parse dot CSS. And it's avoid tag if you remember from our last chapter. So we'll save all that off and let's go back and refresh your browser. And you'll notice that our comment now is um kind of a light gray. So it looks more like a comment, but you notice it's missing the markup. It's missing this opening and closing mark up here. There are a few ways we can handle this and they have their pros and cons. But for the purpose of this course, I'm going to use CSS to do this because we can actually insert content into our tree using CSS. So let's go back into our CSS file and we're going to use a couple of pseudo selected rules. We're going to use the before and after a pseudo selector. So comment before hand, we're going to insert content of this markup here. So this is the opening tag for comment, and let's do a comment after. Once again, it's going to be the closing markup. So we'll save that off, go back into our browser and refresh. And it looks like a comment. You'll notice that you can't select those because they're not technically part of the DOM, but they're there. Now that we have a working approach to formatting, let's tackle our most complex task, which is processing elements.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **CLI Commands:** node (12), make (2)
> **Env Vars:** css (10), dom (2), html (1), rel (1)
> **UI Navigation:** go to (1)
> **Exercise Files:** source code (1)
> **Speakers:** - one (1)

#### [Processing elements](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-elements?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-elements?u=76281980&t=0)** - [Instructor] Now it's time to process element Nodes. Now element Nodes are complex because they can not only contain other Nodes, but they may also have attribute Nodes that need formatting. This is a typical [HTML](../../Skills/Web%20Development/HTML.md) element in a element, it has an open tag which contains an attribute, it has some nested content, in this case, a text node, `and a close tag. to represent all this in our pretty printer, we'll use some custom tags that will allow us to format the different parts of the element on the screen. The entire element will be contained in a pp-element element, which will contain a pp-opentag element, the element name will be nested in a pp-tagname element. Now for now, we're going to ignore the attributes, but this is where they would be appended to the element. Then all of the nested content will be appended followed by the close tag, which will produce output markup that looks like this. Now that we understand what we want to accomplish, let's start coding. So first of all, let's go back into our pretty print function, so we can go to the bottom here somewhere and this is pretty much pretty print. And we're going to add a new case to our switch statement to process elements. So we're going to process element Nodes here, so we'll do Node.ELEMENT_NODE. And inside here, we're going to create an element that will contain everything about that element. So that element is going to be called variable elements, and we decided before, we're going to use a custom tag called pp-elements. So document.createElement, pp-element, and that will contain everything related to this element, including the open tags. So we're going to create our open tag element,
>
> **[1:34](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-elements?u=76281980&t=94)** which is going to be document.createElement, pp-opentag. So that will give us some structure. And now within the open tag, we need to put the tag name. So we're going to create another one called tag name. And once again, it's going to be create element pp-tagname. The tag name will contain the element tag name, so we'll get that from the Node. So we'll say tagName.Innertext equals node.nodeName. The nodeName is the tag name. And we're going to append this to our open tags. So openTag.appendChild tagName. Now at this point, we would want to append all of the element attributes to our open tag, but we're not going to do that right now, it's a little too complex to tackle, so we're going to do this later. So we'll put in a nice TBD comment, process element attributes here. Next we're going to append our open tag to our elements. So element.appendchild openTag. And let's go ahead and return the element from our function, and let's go into the browser, and let's see what we have. So when we refresh, you'll see, now we only have three tags here, we have our header, main and footer. And if you look over here, those are the three top level elements we have. We aren't processing any of the nested content. Plus all the tags are uppercase and I really kind of hate that. So let's go ahead back in here, and first all, let's fix the uppercase problem. So ToLowerCase that tag name. And that we need to start processing the contents of this element.
>
> **[3:07](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-elements?u=76281980&t=187)** Right after we append the open tag, we're going to see if that node has any children. So node.hasChildNodes, and that's a really old function as well, but it tells you if it has any child Nodes. If it does, then we're going to append the return from printChildNodes, which we've already written to handle document fragment. So we're going to pass in our Node, and so this will recursively process all of the Nodes within our element. So let's go and save that off and go back in the browser, see how that looks. Okay. So now we have more stuff, we have some texts Nodes, and we have our nested element open tags. Now let's go ahead and add our close tags because once again, we need to have an open tag element and a close tag element so we can display them both on screen. So close tag is going to be document.createElement, we're going to use pp-closetag for our element name. And down here, we need to put in a tag name. We already have our tag name element, but we can't just append it. So we'll do closeTag.appendChild. We can't just say tag name, because if we did this, it would move it from the open tag to the close tag, but we can clone it. So let's do cloneNode true, so that will give us a deep clone of that. And then let's append this whole thing to the element. Now we should have, if go back to the browser, let's refresh. So you see we have an H1 here, we have an H1 there. Now it's time to start doing some more formatting so this looks a little bit better. So we're going to go back to our CSS. So back in the CSS file, we'll create a new top level rule for our output elements. So we have pp-output,
>
> **[4:41](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-elements?u=76281980&t=281)** which currently has no styling, so it's being displayed inline. So first of all, let's display block. The next thing we're going to want to do for that is let's change the font family to fix size font. So, family monospace. And the next thing, the way we handle white space, if you look in the browser right now, the white spaces are collapsed out. Let's go ahead and use a different white space option. So we use white-space pre-wrap, which is kind of a hybrid between pre-formatting, pre where white space is honored, but it also allows wrapping when things would bust out of their containers. And we're going to change the overall color of all of our characters to Oa6ab6, which is something I picked out of the web and I kind of like it, it's a dark blue, I believe. So let's go back to the browser. Now, you'll see that our code is starting to show some white space and structure. So the next thing we want to do is let's differentiate our tag names. So we're going to go back into the CSS file, and let's add a rule for our pp-tagname, and let's make the color for that 0c98ea, choose that light blue. So let's refresh. Okay, so now you can see that the tags are there, they're a little different. The next thing we need to do is add the markup around the tags, because we need to show the less than sign at the beginning of the open and closed tags, and we need to show the greater than sign at the end. So, we're going to use the same scheme we used before, we're going to use our before and after pseudo selectors. So, let's do open tag before, and the content here will be a less than sign. And for our close tag, it's a little different,
>
> **[6:17](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-elements?u=76281980&t=377)** it's going to be a less than sign, followed by a slash. So let's save that off, go back and refresh. Okay, starting to look more like HTML. Now, what we need to do is we need to put our greater than sign at the end of both of them. So we'll do pp-opentag after, and pp-closetag after, and the content there will be the same for both, it'll be a greater than sign. So we'll save this off. Now we'll go back to our browser, and refresh, it's starting to look like our original source code, but prettier. So the next thing we're going to do is we're going to get rid of this pesky TBD comment here and start admitting attributes in our element open tags.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (2)
> **Code Identifiers:** createelement (3), tagname (2), nodename (2), opentag (2), appendchild (2)
> **CLI Commands:** node (7), make (1)
> **Env Vars:** css (3), html (2), tbd (2), element_node (1)
> **Cross-References:** go back to (4)
> **UI Navigation:** go to (1)
> **Exercise Files:** source code (1)
> **Definitions:** is a  (1)

#### [Processing element attributes](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-element-attributes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-element-attributes?u=76281980&t=0)** - [Instructor] Our pretty printer is looking pretty good, but it would look even better if it displayed element attributes. As you can see this a tag here has an href and in our output, it has nothing. So let's go back into the code and let's finally get rid of that pesky TBD comment we left in there. So here's our TBD process element attributes. And what we're going to do is we're going to look at the node attributes collection. Now, attributes collection, we can't just do a forEach directly because it's not an actual array. So what we're going to do for our purposes, we're going to say: Array.from node.attributes. I'd be curious as to why it's addressed in a different video in the series, but let's go ahead, in our arrow function, we have an attribute node now, and what we're going to do is we're going to recursively call pretty print. So we're going to append to our openTag. We're going to append the output from pretty print for that attr node Let's go ahead and put our semi there and save that off. So that means that we need to add support for attribute nodes to our case statement, to our switch statement. So down here, we're going to do case Node.ATTRIBUTE_NODE. (keyboard typing) Now this pattern is going to look very similar to what we did for the element itself, but we're going to have an outer attribute element that will contain everything related to that attribute. So document.createElement 'pp-attribute', and then within that, we're going to create an element to contain the attribute name. So const attrName = document.createElement 'pp-attrname'
>
> **[1:40](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-element-attributes?u=76281980&t=100)** and as we did with the element tag name, the inner text of the attrName will contain the node name of our nodes, so node.nodeName, and we're going to make it lowercase as well, because it's just more attractive that way. So that will set the attribute name into there and we will append this to the attribute element itself. So we'll appendChild attrName; save that up. Now let's return the attribute and go back to the browser and see what we've got. We'll refresh and now you'll see in our A tag, especially if we inspect it, we have an attribute and attrName element within our A tag. So that's good. Now all we need to do is process the attribute value. So let's go back to our code and here's our attribute where we append the attribute name to the attribute. Let's go ahead and look and see if the node has a value. So some nodes don't have values and in that case, we don't necessarily want to add a node value element because things like disable to read only, they don't necessarily have a value. So node.nodeValue if the node value is not equal to an empty string, then that means we have something there. So we'll create an attrValue node. So createElement 'pp-attrValue' and within that attrValue element, we will set the inner text equal to the value of that node, so node.nodeValue, and that would be the actual text of the attribute and we'll append it to our attribute. (keyboard typing) So we'll append attrValue to the attribute itself. And we will save this off, go back to the browser, refresh.
>
> **[3:18](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-element-attributes?u=76281980&t=198)** And now we can see that we have elements for the attribute name, attrName and attrValue. And we have basically all of the markup we need now to display everything, we just need to go style it. So let's go back to the CSS file and start styling these new elements. So back into our source code and let's open up our CSS file. We'll go to the end. And a lot of this is going to look similar to what we did for the elements earlier, but we're going to go ahead and give the attribute name a different color. So attrName is going to be color. In this case, it's CA6836, I honestly forgot what color that is. Let's save it, go refresh the browser. And it's kind of a orange, light orange. I like it. Now, we're going to start using pseudo selectors again, to put some white space. If you look in the output here, we want a space before the attribute, we want the equal sign and a quote here, and a quote at the end of that. So we're going to use the before and after pseudo selectors again, to give ourselves some white space. So pp-attribute::before content, there's going to be a space. So that'll put a space for all of our attributes. Now, pp-attrValue, so the value itself, before that, we will place a equal sign and a double quote. So actually since we're going to do a double quote, we're going to use single quotes around this. So single quote all that, save that. And now we're going to do one more for pp-attrValue::after, and we need to put another closing double quote. So content equals a double quote.
>
> **[4:55](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/processing-element-attributes?u=76281980&t=295)** And let's get rid of that, there we go. Save all that off, go back into the browser and refresh. And now it looks like we have full support for elements and attributes. We have our attribute being displayed. We have quotes around it. So all we have to do now is process our text nodes, do a little bit of cleanup, and our pretty printer is ready to ship.

> [!info]- Semantic Content
>
> **Code Identifiers:** attrvalue (7), attrname (6), createelement (3), nodevalue (2), foreach (1)
> **CLI Commands:** node (14), make (1)
> **Env Vars:** tbd (2), css (2), attribute_node (1), ca6836 (1)
> **Cross-References:** go back to (4)
> **Non-Speech:** (keyboard typing) (2)
> **UI Navigation:** go to (1)
> **Exercise Files:** source code (1)
> **Definitions:** means that (1)

#### [Handling text and cleanup](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/handling-text-and-cleanup?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/handling-text-and-cleanup?u=76281980&t=0)** - [Narrator] There was a saying I heard when I was working in the OS division of [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) a long time ago, it was the first 90% of the work only takes 10% of the time. Unfortunately, the last 10% takes 90% of the time. So doing that last little bit of fit and finish, it isn't glamorous, but good programmers know it's worth it. So if we look right now at our output, you'll see that we have these text nodes in our page. Let's take a look at them in the browser here. So if I look at this, our text is just included as basic text node you see right here. So we don't have any styling on it, so let's fix that. So we're going to go back into our editor and we're going to update our pretty print function. You know, this function right here. We're going to go into our switch statement here, and we're going to add support for text nodes. So case, node, dot, text node. And it's going to be pretty straightforward. So we're going to create a new text elements and that's going to be PP texts. So document, dot, create element, PP-text. And all we need to do is set the inner text of that to the content of our node. So node, dot, node value and return text. So we'll save that off. Let's go back into the browser and refresh. And now if we look at this in our develop tools. You'll see that we have wrapped that text inside this element. Which means we can style it. So let's go back into the editor again, and let's go into our CSS file. And let's add a styling rule. And for now I'm just going to make the text black. So the color zero, zero, zero.
>
> **[1:36](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/handling-text-and-cleanup?u=76281980&t=96)** Save that off, go back to the browser and refresh. And now all of our text is black, but our attribute text is still that blue color. And we basically have formatted source code. That's great. So let's actually try and make this pretty printer live up to its name. Let's try and print. So, if we go into the print dialogue, what's it going to look like? Well, doesn't look that great. So the last thing we're going to do. Is we're going to use some media queries to clean that up. So let's go back into the editor. And we're going to put this media query. We're not going to put it in pretty parse CSS because this is all pretty much related to the pretty printer itself. We're going to put it in the [HTML](../../Skills/Web%20Development/HTML.md) file. We have this inline style here already. So at the very tippy top. We're going to say media print. So whenever we're actually printing this thing, the header, the inputs, the output. Now that's interesting, input, the output label. We're all going to display none. So let's save that off, space there. Go back into the browser, refresh. Now, when we go to print. Now we have nothing but clean, colorful source code. Nice. That wraps it up for our pretty printer project. Don't forget to try the challenge at the end of the chapter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Microsoft](../../Instructors/Artificial%20Intelligence%20(AI)/Microsoft.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **CLI Commands:** node (6), make (2)
> **Env Vars:** css (2), html (1)
> **Exercise Files:** source code (2)
> **Cross-References:** go back to (1)
> **UI Navigation:** go to (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [narrator] (1)

#### [Challenge: Empty tags](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/challenge-empty-tags?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/challenge-empty-tags?u=76281980&t=0)** (upbeat techno music) For this challenge, we'll be looking at the mirror image of the challenge from chapter two where we were parsing void elements. To see the issue, let's add a horizontal rule to our pretty printed source code, so do an hr tag. That open and close empty tag isn't very pretty at all. So your challenge is to modify the pretty printer so instead of displaying empty elements like this, display them like this. Good luck.

> [!info]- Semantic Content
>
> **Exercise Files:** source code (1)
> **Non-Speech:** (upbeat techno music) (1)

#### [Solution: Empty tags](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/solution-empty-tags?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/solution-empty-tags?u=76281980&t=0)** - [Instructor] To recap. The challenge is to modify the pretty printer to display empty elements in syntax for elements that have no content. So to solve this, let's look at our current output dom. So first thing I'm going to do is I'm going to add an HR void element here, and let's take a look at what got generated from that. I use my developer tools. I come in here to look at the element. This is the HR element. So it has an open tag and it has a close tag. So the first thing I want to do is get rid of the close tag. That's not necessary anymore since it's empty. So let's go back to our editor and let's go down to where we're processing our element node. And inside this block, you'll see right here, we have an if statement, if there are any children. So we're going to move this close tag logic up inside the if statement. So now we'll only admit a close tag if there actually is content,. We'll go back to the browser, refresh again, we'll put in our HR, our HR element is now being displayed as a void element, which is fine, but I actually want to use the empty elements in syntax at all time. So the next thing we need to do is we need to format our open tag differently if it has no content. So the way we're going to handle that is right here in this if statement, we can also add not a promise, a new class, we're going to add a class to the open tag. So open tag class list, add empty. So basically we're going to add a CSS class, to empty open tags,
>
> **[1:33](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/solution-empty-tags?u=76281980&t=93)** and that will enable us to go into the CSS here and change how it's formatted. So right here, we have our rule for displaying the end of the open tag. So we're going to do this pp-opentag when it's empty, we're actually going to display the empty elements into XO contact intent equals slash greater than sign. And we'll save that off. Go back to the browser, refresh. Now we put it in a horizontal rule and we have our empty element syntax. So voila, it's fixed. And how was your solution similar to mine? How was it different? Let me know. I'd love to hear from you and see your work.

> [!info]- Semantic Content
>
> **Cross-References:** go back to (3)
> **Env Vars:** css (2)
> **CLI Commands:** node (1)
> **Analogies:** similar to (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Level up your DOM skills](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/level-up-your-dom-skills?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/vanilla-javascript-building-on-the-document-object-model-dom/level-up-your-dom-skills?u=76281980&t=0)** - [Instructor] I want to thank you for taking my course. And also, I love hearing your feedback and questions. So be sure to follow me on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) and ask questions in the QA tab if you have them. At this point, I could direct you to some more courses you could take, but the best advice I can give you if you really want to improve at coding is go build something just for fun, like this app I wrote to visualize the DOM in 3D. Your project doesn't have to be big or complicated. A tiny JSFiddle will do, but build something using what you've just learned. Good luck and keep coding.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **CLI Commands:** go build (1)
> **Env Vars:** dom (1)
> **Speakers:** - [instructor] (1)


## Instructor

- [W. Scott Means](../../Instructors/Software%20Development/W.%20Scott%20Means.md)

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/vanilla-js-DOM-2876283)

## Skills Covered

- Document Object Model (DOM)
- JavaScript

## Path Context

### In [Explore Web Development with Vanilla JavaScript](../../Paths/Software%20Development/Learning%20Paths/Explore%20Web%20Development%20with%20Vanilla%20JavaScript.md)
← [Vanilla JavaScript- Web Performance Optimization APIs](Vanilla%20JavaScript-%20Web%20Performance%20Optimization%20APIs.md) | **8 of 8**

## Appears In

- [Explore Web Development with Vanilla JavaScript](../../Paths/Software%20Development/Learning%20Paths/Explore%20Web%20Development%20with%20Vanilla%20JavaScript.md)

## Related Courses

_Courses sharing skills:_

- [End-to-End JavaScript Testing with Cypress.io](End-to-End%20JavaScript%20Testing%20with%20Cypress.io.md) — JavaScript
- [Learning JavaScript Debugging](Learning%20JavaScript%20Debugging.md) — JavaScript
- [Foundational JavaScript Security](Foundational%20JavaScript%20Security.md) — JavaScript
- [JavaScript- Test-Driven Development (ES6)](JavaScript-%20Test-Driven%20Development%20(ES6).md) — JavaScript
- [JavaScript- Best Practices for Data](JavaScript-%20Best%20Practices%20for%20Data.md) — JavaScript

---

[↑ Back to top](#)