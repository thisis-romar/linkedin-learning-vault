---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: learning-playwright
url: "https://www.linkedin.com/learning/learning-playwright"
level: Intermediate
updated: 11/21/2024
learners: 27269
skills:
  - Test Automation Tools
  - Automation
exercise_files: true
github: "https://github.com/LinkedInLearning/learning-playwright-5911873"
thumbnail: "https://media.licdn.com/dms/image/v2/D4D0DAQH9KXFauT3_nw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1730827611420?e=2147483647&amp;v=beta&amp;t=Hgonc3KqLVcsREG50BjKBFZ07NP2DY-pksa9-Oweu7Q"
linkedin_topic: Software Development
learning_paths:
  - '[[Master Test Automation with Playwright]]'
  - '[[Getting Started with Software Testing]]'
prev_courses:
  - null
  - '[[Learning Selenium- Structure, Scale, Run, and Optimize Automated Tests]]'
next_courses:
  - '[[Playwright Essential Training- Abstractions, Fixtures, and Complex Scenarios]]'
  - '[[JMeter- Performance and Load Testing]]'
path_nav: '[{"path":"Master Test Automation with Playwright","position":1,"total":4,"prev":null,"next":"Playwright Essential Training- Abstractions, Fixtures, and Complex Scenarios"},{"path":"Getting Started with Software Testing","position":6,"total":12,"prev":"Learning Selenium- Structure, Scale, Run, and Optimize Automated Tests","next":"JMeter- Performance and Load Testing"}]'
path_count: 2
tags:
  - course
  - topic/software-development
  - skill/test-automation-tools
  - skill/automation
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/Learning%20Playwright.md)

![Learning Playwright](https://media.licdn.com/dms/image/v2/D4D0DAQH9KXFauT3_nw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1730827611420?e=2147483647&amp;v=beta&amp;t=Hgonc3KqLVcsREG50BjKBFZ07NP2DY-pksa9-Oweu7Q)

# Learning Playwright

> It’s no surprise that Playwright has joined the ranks of other top-rated open-source automation tools. Playwright’s comprehensive feature set allows testers and developers to quickly create new test automation projects without having to combine multiple libraries and other tools. Whether you’re a manual tester, a quality assurance specialist, a software developer, or a seasoned automation engineer

> [LinkedIn Learning](https://www.linkedin.com/learning/learning-playwright) | Intermediate | 27K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Unlock the future of test automation with Playwright](#unlock-the-future-of-test-automation-with-playwright)
  - [What is Playwright, and why choose it?](#what-is-playwright-and-why-choose-it)
  - [Using the exercise files](#using-the-exercise-files)
- [**1. Playwright Installation and Configuration**](#1-playwright-installation-and-configuration) (7 videos)
  - [Installing Playwright](#installing-playwright)
  - [Updating the Playwright config](#updating-the-playwright-config)
  - [Playwright config browsers and projects](#playwright-config-browsers-and-projects)
  - [Exploring the test runner command line interface](#exploring-the-test-runner-command-line-interface)
  - [Overview of package.json](#overview-of-packagejson)
  - [Exploring the VS Code extension](#exploring-the-vs-code-extension)
  - [Exploring the Playwright UI Mode](#exploring-the-playwright-ui-mode)
- [**2. Creating Tests with Playwright**](#2-creating-tests-with-playwright) (11 videos)
  - [Overview of the website under test](#overview-of-the-website-under-test)
  - [Generating tests with codegen](#generating-tests-with-codegen)
  - [Overview of locator strategies in Playwright](#overview-of-locator-strategies-in-playwright)
  - [Overview of assertions in Playwright](#overview-of-assertions-in-playwright)
  - [Structuring Playwright tests](#structuring-playwright-tests)
  - [Handling cookie authentication in Playwright](#handling-cookie-authentication-in-playwright)
  - [Visual testing in Playwright](#visual-testing-in-playwright)
  - [API testing in Playwright](#api-testing-in-playwright)
  - [Automating the right things with Playwright](#automating-the-right-things-with-playwright)
  - [Challenge: Create three automated tests](#challenge-create-three-automated-tests)
  - [Solution: Create three automated tests](#solution-create-three-automated-tests)
- [**3. Maintaining Playwright Tests**](#3-maintaining-playwright-tests) (4 videos)
  - [Introduction to maintaining tests with Playwright](#introduction-to-maintaining-tests-with-playwright)
  - [Playwright screenshots, videos, and reporters](#playwright-screenshots-videos-and-reporters)
  - [Playwright trace viewer](#playwright-trace-viewer)
  - [Scaling Playwright tests](#scaling-playwright-tests)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Unlock the future of test automation with Playwright](https://www.linkedin.com/learning/learning-playwright/unlock-the-future-of-test-automation-with-playwright?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/unlock-the-future-of-test-automation-with-playwright?u=76281980&t=0)** - [Butch] Have you seen all the buzz using Playwright for [[Test Automation]] and feeling left out? That's probably because Playwright is the test automation tool of the future. I'm Butch Mayhew, a Playwright ambassador. I've helped countless people with learning Playwright through my blog, [playwrightsolutions.com](https://playwrightsolutions.com), and I'd like to help you too. Are you ready to learn the basics of Playwright? Come join me as we commence on this journey together.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Test Automation]] (2)
> **URLs:** [playwrightsolutions.com](https://playwrightsolutions.com) (1)
> **Speakers:** - [butch] (1)

#### [What is Playwright, and why choose it?](https://www.linkedin.com/learning/learning-playwright/what-is-playwright-why-choose-playwright?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/what-is-playwright-why-choose-playwright?u=76281980&t=0)** - [Instructor] So, you decided to go for it. You've made the right choice. Now it's time to start our learning journey together. Let's first begin by defining Playwright. Playwright is an open-source tool built by [[Microsoft]] that enables reliable end-to-end UI and API automation testing for modern web apps. This tool is gaining a lot of traction by providing a ton of value to companies as a leading choice within the [[Test Automation]] tooling. Playwright provides access to multiple browsers, which include Chromium, Chrome, Microsoft Edge, Firefox, Webkit, which is what Safari is built from. Every Playwright release will have a set version of each of these browsers bundled when installing Playwright. Let's take a look deeper into what makes Playwright special. It's highly rated on [[GitHub]], currently over 66,000 stars at the time of this recording. They release new features around once a month. They support TypeScript, [[JavaScript]], [[Python (Programming Language)|Python]], .NET and [[Java]] officially, but there's also a Ruby and Go Community version. TypeScript is where we will focus in this course. Playwright is closer to [[Selenium]] WebDriver than Cypress. Why? It's an out-of-process automation driver that is not limited
>
> **[1:34](https://www.linkedin.com/learning/learning-playwright/what-is-playwright-why-choose-playwright?u=76281980&t=94)** by the scope of in-page JavaScript execution. This is one of the Cypress limitations. Playwright, on the other hand, drives the browser that's being used from the outside like Selenium WebDriver, rather than being baked into the browser. Playwright can be used as a library or as a test framework. This course will be focusing on covering the Playwright Test framework specifically. So, what exactly is Playwright Test? Playwright Test has a built-in test runner that's very similar to Jest. This allows you to build out test steps using Playwright-specific syntax. Playwright Test allows you to run all your tests in parallel with no cost restrictions. It also allows you to run X amount of retries. Playwright Test shines the brightest with the ability to utilize built-in reporters to view detailed test runs, screenshots, videos, and trace files that show the exact state of the browser was in when the test was running. There's also a helpful VS Code extension that allows you to build, maintain, and debug tests all from within VS Code. So, this begs the question, why choose Playwright rather than some other tool? Well, the ability to run API and UI regression tests in the same framework
>
> **[3:08](https://www.linkedin.com/learning/learning-playwright/what-is-playwright-why-choose-playwright?u=76281980&t=188)** was a big factor in my decision. I found developers and testers using one framework for both of these types of tests is a big benefit. We've mentioned some of the rich reporting and ability to run tests in parallel. This is only a portion of the built-in functionality that Playwright provides. There's excellent error handling. When a Playwright test fails, it's always super clear what the problem is from the error message. Not all tools have error messages as helpful as Playwright. The ability to configure projects in many different ways is also a game-changer when it comes to the flexibility Playwright offers. Playwright locators also have built-in auto-waiting and retry abilities. In the end, the reason why you should choose Playwright? Using Playwright for regression testing can give you confidence in future software releases. This is the point. We want to ensure we have high-quality releases, and Playwright is a great tool to help solve that problem.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft]] (2), [[JavaScript]] (2), [[Selenium]] (2), [[Test Automation]] (1), [[GitHub]] (1)
> **Tools:** vs code (2), firefox (1), safari (1), github (1)
> **Env Vars:** api (2), net (1)
> **Definitions:** is a  (2), is an  (1)
> **CLI Commands:** python (1), ruby (1)
> **Analogies:** similar to (1)
> **Prerequisites:** configure (1)
> **Speakers:** - [instructor] (1)

#### [Using the exercise files](https://www.linkedin.com/learning/learning-playwright/using-the-exercise-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/using-the-exercise-files?u=76281980&t=0)** - [Instructor] As a part of this course, we've provided a [[GitHub]] repository that you can use during each video. The main README provides some guidelines on the branch-naming strategies, and provide some useful tips for getting through error messages. As you scroll down, there are also links to install Node, [[Git]] or GitHub Desktop, VS Code, and the VS Code extension. Within this repo, I've also included a RESOURCES.md. In this Markdown file, I've included links to specific Playwright documentation and links to different GitHub branches that are created within the repository to follow along with each video. I would recommend either having this file open on a separate screen locally, or browsing the GitHub repository page and viewing it from there. Let's get this party started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (4), [[Git]] (1)
> **Tools:** github (4), vs code (2)
> **Env Vars:** readme (1), git (1), resources (1)
> **CLI Commands:** node (1), git (1)
> **File Paths:** resources.md (1)
> **UI Navigation:** scroll down (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)


### 1. Playwright Installation and Configuration

[↑ Back to Table of Contents](#table-of-contents)

#### [Installing Playwright](https://www.linkedin.com/learning/learning-playwright/installing-playwright?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/installing-playwright?u=76281980&t=0)** - [Instructor] Before we embark on this learning journey, we must first build our development environment that we'll be using during this course. This will include installing Node, installing Playwright through the terminal commands, installing VS Code, and installing the Playwright VS Code extension. If you're on a Mac or [[Linux]] machine, you're more than likely already have Node installed. If not, you can follow the link and install it how you see fit. I'm a big fan of using the Node Prebuilt Installer that can be found at this link. Once installed and configured, you can pull up a terminal and you should be able to run the command, node --version, and it should return a number. I currently have version 18.18.2 installed. Any newer version of Node should work for this exercise in this lesson. Next, we're going to install Playwright through a very specific initialize command that the Playwright team has provided. It's going to walk us through a bit of an installation wizard. Now would actually be a good time, if you're not already, go ahead and download the Learning Playwright Repo and go ahead and swap to the 01_01b branch. So I have Learning Playwright Repository pulled down locally, and I am on the branch 01_01b, which stands for beginning. From here on out, we will be using this repository
>
> **[1:34](https://www.linkedin.com/learning/learning-playwright/installing-playwright?u=76281980&t=94)** to track all of our changes. Once you have this branch checked out, we're going to go ahead and install Playwright at the root of this directory. We're going to do that by using the command [[npm]] init playwright@latest.
>
> **[1:52](https://www.linkedin.com/learning/learning-playwright/installing-playwright?u=76281980&t=112)** When you hit enter, it's going to walk you through a step of questions. You may have to hit yes to first get things started, but the first question here is, do you want to install TypeScript or [[JavaScript]]? We're going to hit enter and choose the default TypeScript. Where do you want to put your end-to-end tests? I'm going to hit enter and choose the test folder. Do you want to add a [[GitHub]] Actions workflow? For this, I'm going to leave this as false as well, so I'm going to hit enter. And install Playwright browsers. This can be done manually via npx playwright install. Yes or no? It is set to true here. I'm going to hit enter. And if you already have the browsers installed, it's not going to take very long, but if you don't have 'em installed, you're going to have to wait two or three, four minutes for all those browsers to install. Once everything is done running, you'll see a screen like this. Happy hacking! To ensure that everything is installed properly, the next thing we can do is actually run the tests and make sure everything's working properly on our machine. So to do that, I'm going to type in npx playwright test. I'm going to hit enter and this should kick off all the tests. So it's running six tests using four workers. Six tests passed. It took 3.6 seconds. and I successfully ran Playwright Test on my computer. One of the final steps, it's somewhat optional, but highly recommended, is going ahead and installing VS Code. You can find that from the link in the resources guide.
>
> **[3:28](https://www.linkedin.com/learning/learning-playwright/installing-playwright?u=76281980&t=208)** Once it's downloaded and installed, it should look like this. Now we want to open the repo that we're working from within VS Code. To do this, you can go to File, Open Folder, and select the Learning playwright folder in your directory, wherever you downloaded it to. Clicking Open will open the directory in VS Code and I can see all the different files. Once this folder's open, you'll likely receive a popup in the bottom right-hand corner asking you to install some different extensions. I would recommend you go ahead and install those extensions. That should install [[ESLint]], the GitHub Theme, the Playwright Test extension, and the Prettier Code formatter. These extensions will help you as you're building out your Playwright Tests. To recap, we installed and validated that we had Node installed. We initiated a new Playwright Test TypeScript project, and we installed the appropriate browser dependencies. We ensured the example tests provided were able to run, and we also installed VS Code and the Code extension. We are now ready to set sail on our learning adventure.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (2), [[Linux]] (1), [[npm]] (1), [[JavaScript]] (1), [[ESLint]] (1)
> **CLI Commands:** node (6), npx (2), npm (1), make (1), find (1)
> **Tools:** vs code (6), terminal (2), github (2)
> **Prerequisites:** install (10)
> **UI Navigation:** open the (2), go to (1), select the (1)
> **Versions:** version 18 (1), 18.2 (1), 3.6 (1)
> **Definitions:** stands for (1)
> **Best Practices:** recommended (1)

#### [Updating the Playwright config](https://www.linkedin.com/learning/learning-playwright/updating-the-playwright-config?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/updating-the-playwright-config?u=76281980&t=0)** - [Instructor] Now that we have Playwright installed, we will be taking some time to work on our configuration for our project. We're going to spend this lesson reviewing the playwright.config.ts file. Let's go ahead and open that up in VS Code and take a look. The first thing to note is this file was automatically generated when we ran the playwright init command. Inspecting the file, we can see the main method is actually defineConfig. With this, there are different levels and options available. The top most level is where a lot of the Playwright test runner options are found. Within the main test runner config, let's break down a few of what these options are doing. Starting with fullyParallel, which is set to true. This option allows multiple test files to be run in parallel. More details can be found in the docs provided in this hover over section. Another important section is the retries option. Playwright gives us the ability to assign how many retries we want to attempt based on the values we add for retries. As you can see, the default value is process.env.CI with a question mark, with a two, a colon, and a zero. This is a conditional. It's actually a ternary operator. It's basically telling us that if process.env.ci, when it's evaluated, if it's true, it's going to use the first value, two. If it's false, it's going to use the second value, zero. By default, anything run in a CI environment
>
> **[1:36](https://www.linkedin.com/learning/learning-playwright/updating-the-playwright-config?u=76281980&t=96)** such as [[GitHub]] Actions with that environment variable set, we'll have two retry attempts, but anything run on a local machine will have zero retry attempts. Let's go ahead and refactor this to have one retry when we're running on our local machine from changing the value from a zero to a one. Let's also go ahead and update the text. Retry on CI two times and locally one time. You can see that the workers option has a similar value with a ternary operator. Having a programmable configuration file is really helpful in that we can add creative logic to open a ton of other configuration possibilities. A list of different options on this level can be found within the Playwright docs under the testconfig class. We also have reporters. [[HTML]] is by default. We're not going to dive into that here, but we will in a later video. There are some default timeouts in Playwright that aren't listed in the config that I find it useful to go ahead and add to the config so I know what to expect and make them easy to change in the future. So at the top of our defineConfig, let's go ahead and add a new line, and let's type in timeout:, and I'm going to type in 30_000. This is indicating 30 seconds or 30,000 milliseconds. So this is the test timeout.
>
> **[3:13](https://www.linkedin.com/learning/learning-playwright/updating-the-playwright-config?u=76281980&t=193)** If 30 seconds goes by and the test is not complete, the test will fail. There's also a globalTimeout. Let's type that, globalTimeout, and we're going to set that to 10 minutes. This is some other syntax you can use. So we're saying 10 minutes or 10 * 60 * 1000. This is just another way to visualize 10 minutes in the config file. This config says if all of your tests take longer than 10 minutes to run, your Playwright test will fail. So if you have a longer test suite, or as your test suite grows, you may have to increase this timeout. The use section down below here is really where we get into the nitty gritty details of the browser, the browser configuration settings, and how the browser is going to interact when running test. I'll go ahead and mention, even though we're not going to dive into this now, there's another section called projects below. The projects section also has a use section. The use on the highest level underneath the defineConfig where we're looking now is the overall default that is set, but on an individual project level, you can override these settings. You can even do it on the test level by calling test.use. Let's go ahead and add a few things to the use section that will be useful for us. So let's start by adding actionTimeout: 0. (keyboard clacking)
>
> **[4:49](https://www.linkedin.com/learning/learning-playwright/updating-the-playwright-config?u=76281980&t=289)** I'm going to hit comma, I'm also going to add ignoreHTTPSErrors, we're going to set that to true. And I'm a big fan of enabling traces to always be on by default is set on-first-retry. You know, if I hover over this, I can see the different options. I'm going to just set this to on, so I'm going to update this config. The trace file is going to include things like network request, console error logs, and all sorts of different elements on the screen along with a timeline view of the how that happened. We'll look into that deeper in a future video. I also like to set a video to retain on failure, so let's add that in here, video: 'retain-on-failure'. Let's also add that for screenshot as well, screenshot: 'only-on-failure'. Let's also go ahead and add headless, and we're going to set this to true. This is the default. I don't want my browser popping up and interrupting my flow when I want to run a test from my command line. There are ways to enable this through the [[CLI]], which we'll look at a little later, but it's always best to set this as true. The last thing we're going to look at is this baseURL. It's currently already set here, but it's commented out. So we're going to uncomment this, and we're going to update this to our test website. That's going to be [https://practicesoftwaretesting.com](https://practicesoftwaretesting.com).
>
> **[6:30](https://www.linkedin.com/learning/learning-playwright/updating-the-playwright-config?u=76281980&t=390)** This baseURL is unique in that it allows you to set a URL. It's a base, so you can have your own .com or io or .net domain. You can also use a variable here in this config file if you wanted to use that and override that based on other configs. By default, this baseURL is available in your test. By default, it's going to attempt to use this first. So you can pass things like page.goto and just pass in /login instead of the full URL, and it will go to this full URL /login for you. This is really useful as we're setting up our test, and you'll see this use later. There are also lower level config options in use, such as web server and different expect things that you can read about in the official Playwright docs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (1), [[HTML]] (1), [[CLI]] (1)
> **Code Identifiers:** defineconfig (3), baseurl (3), globaltimeout (2), fullyparallel (1), actiontimeout (1)
> **Env Vars:** url (3), html (1), cli (1)
> **File Paths:** process.env (2), playwright.config.ts (1)
> **Tools:** vs code (1), github (1), command line (1)
> **CLI Commands:** find (1), make (1)
> **Analogies:** such as (2)
> **URLs:** [https://practicesoftwaretesting.com](https://practicesoftwaretesting.com) (1)

#### [Playwright config browsers and projects](https://www.linkedin.com/learning/learning-playwright/playwright-config-browsers-and-projects?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/playwright-config-browsers-and-projects?u=76281980&t=0)** - [Instructor] It's time to journey deeper into the Playwright config as we look at how you can configure the browsers and project settings of the config. So let's jump in. As we pull up our existing config, you'll notice in the projects section, you have different browsers. This is very important to the Playwright test runner. You can think of these projects as different projects that will run all the tests for each different project. So by default, the playwright init command, which we ran in lesson 0101, created the default settings with Chrome Desktop browser with Firefox Desktop, with Desktop Safari. The original playwright init also provides you with mobile viewports, but these are commented out. By default, you could uncomment these and run against all sorts of different sized browsers as well. This is very useful if you want to do cross-browser testing. That means run your existing tests across different browsers to validate that websites work against all these different configurations. The pro is Playwright gives you these browsers out of the box, and you're able to use them with very little configuration. One of the drawbacks when using Playwright is you're locked into a specific version of Chromium, Firefox, or WebKit. This version that is packaged with each version released with Playwright. To see these, you can go visit the release notes. Let's check out the browser version
>
> **[1:34](https://www.linkedin.com/learning/learning-playwright/playwright-config-browsers-and-projects?u=76281980&t=94)** for Playwright release 1.46. So let's find our Playwright release notes. This is version 1.46. If I scroll down, I can see the browsers released with 1.46 are Chromium 128, Firefox 128, and WebKit 18. What this means is you'll be limited to using whatever browser version is bundled with Playwright. This is one of the trade-offs you take with Playwright with the benefit of the browsers can be installed with a single command. Let's go back to our project code. Projects can also be used to set up data, to segregate tests, or to override certain browser configurations on a per-project basis. So let's go ahead and walk through the steps on how to create a project with a setup dependency. This will ensure that the setup project is always run and passes before running any other projects. So to do this, the first thing we want to do is find our project array and we're going to add a new object. So it's going to be open curlys, we're going to type in name. The name: "setup", we're going to add a comma. We're going to add testMatch, and we're going to add some regex or a regular expression. You don't have to know exactly what this does. You can trust me a bit here. This option is what Playwright looks at
>
> **[3:09](https://www.linkedin.com/learning/learning-playwright/playwright-config-browsers-and-projects?u=76281980&t=189)** to know how to match file names as test for the project. Specifically, we'll be naming our setup files as name.setup.ts rather than name.spec.ts, which is a default setting. Next, we'll go into the other projects that are active, and add the option dependencies with the value array with a string of setup in between. So under the chromium project, we're going to add dependencies. We're going to make an array. We're going to type in setup. We're going to add a comma at the end, and we're going to add this to the other sections as well, so WebKit and Firefox. What this is going to do is before each of these projects run, it's going to ensure that the dependencies, whatever is set here, which is the setup name, run successfully before those run. It's important to note that the Playwright config does have a lot of defaults that are used even though they aren't listed as options in the command. For example, I mentioned the default setting for spec matching name.spec.ts. Well, let's take a look at the docs and what that actually looks like as a test matcher. So the actual code or the actual default underneath is this glob pattern here that's actually looking for spec or test dot j or ts. That's what that is doing.
>
> **[4:44](https://www.linkedin.com/learning/learning-playwright/playwright-config-browsers-and-projects?u=76281980&t=284)** This means that [[JavaScript]] or TypeScript files with .test or .spec will get picked up by default. This default setting will be applied to the projects unless we overwrite it. Some other useful defaults that are worth calling out, grep, this will match the test title or tags. This is useful to group certain tests to a certain project. We also have grepInvert. This will match tests that are not included in the test or title tags. We've got testDir. This allows you to set a test directory that the project will recursively scan to identify test and the directory. We also have testIgnore. This allows you to specify patterns to match that will not be executed as test files. And then there's use, which we discussed earlier. This section is where you can set many different options. Those options can be found here in this section. One option that we want to go ahead and add to our test is the permission for clipboard-read. So let's go ahead and do that. We're going to add this for chromium. We're going to come after our devices, we're going to add a comma. We're going to add permissions with an S. We're going to make an array, we're going to type in clipboard-read. We're just going to make sure everything is good. We'll save it. This will allow me to programmatically access the clipboard within Chrome. So these are the types of things that you can pass in
>
> **[6:20](https://www.linkedin.com/learning/learning-playwright/playwright-config-browsers-and-projects?u=76281980&t=380)** to different projects. We've only touched the surface of what can be done through the Playwright config, but this should be enough to get you headed in the right direction for success.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (1)
> **Prerequisites:** setup (8), configure (1), set up (1)
> **CLI Commands:** make (3), find (2), grep (1)
> **Documentation:** spec (5), the docs (1)
> **Tools:** firefox (4), safari (1)
> **Code Identifiers:** testmatch (1), grepinvert (1), testdir (1), testignore (1)
> **File Paths:** name.spec.ts (2), name.setup.ts (1)
> **Versions:** 1.46 (2), version 1 (1)

#### [Exploring the test runner command line interface](https://www.linkedin.com/learning/learning-playwright/exploring-the-test-runner-command-line-interface?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/exploring-the-test-runner-command-line-interface?u=76281980&t=0)** - [Instructor] One of the main ways that Playwright tests are run is through the command line interface, or as I'll refer to it in this video, the [[CLI]]. To run commands within VS Code this way, you can select the Terminal dropdown, and select New Terminal. In this video, we'll walk through and run tests using some of the most useful commands. A full list of the commands can be found in the official Playwright docs. One thing to note is any options passed into the Playwright command when running from the CLI will override any configuration settings that are set in the Playwright config. For example, if I wanted to run all my tests in headed mode, meaning the browsers will pop up and I'll be able to see the actions, there's two things I can do. I can go update the Playwright config file, or I can just pass in the option through the CLI headed in the command line interface. So let's go ahead and run that command. It is going to be npx, space, playwright, space, test, --headed. (keys clicking) This should run all the projects in headed mode. So you see Firefox, you see Chrome, you see WebKit all opened up. So six tests passed, six tests will run. Another popular option is passing in the --project with a project name. For our example, let's pass in the Chromium project name. So npx, space, playwright, space, test,
>
> **[1:34](https://www.linkedin.com/learning/learning-playwright/exploring-the-test-runner-command-line-interface?u=76281980&t=94)** - -project, space, chromium. I'm going to type Enter. And now, we'll see only two tests get run. So we're only running the Chromium project. What's really nice about this command is you can pass in multiple projects by passing in another project option. So I'm going to hit the up arrow key to get my previous command, and I'm just going to add onto it --project, space, firefox. I'm expecting, when I run this, for four tests to run. Four tests, four workers, Firefox, Chromium, everything passed. Another thing that's nice about the project option is it is an argument, and the argument can also be matched to regular expressions. So for example, I can type in npx playwright test --project, space, I'm going to put a quote here, "*omium," and that is going to match the Chromium project. I did have to put quotes here due to the star. In some terminals, you may have to escape that with a backslash. It depends on what operating system you're on. To make things simpler in our project, I'm going to take a minute and comment out our Firefox and WebKit projects in our Playwright config. So I'm going to click playwright.config. We're going to scroll down to our project section, and I'm going to go ahead and comment these two out.
>
> **[3:09](https://www.linkedin.com/learning/learning-playwright/exploring-the-test-runner-command-line-interface?u=76281980&t=189)** So by doing this, I'm holding down Shift as I click from top to bottom, I'm going to hold down Command or Control, and I'm going to press the back slash key. That's the key that is connected with a question mark. And that's going to comment out all my selected files. I'm going to go ahead and save my file. I also might need to take out this last comma on this object. So let's go ahead and I'm just going to run the previous command, just to make sure all the tests pass. Alright, my config's good, all the tests passed, everything's good. Before we move on, there are a few other useful commands that could come in handy as your Playwright project grows. For example, you can narrow down your runs further by running all the tests within a specific file. To do this, I'm going to hit the up arrow key to get the previous command, I'm going to add on here /example.spec.ts. I'm going to run that, and that should run the two tests in this example file. What's even a little more crazy is you can run the specific test from the file if you specify the line number of the file. So in our example, the second test starts on line 10. So I'm going to hit the up arrow key in my terminal again, add a colon, and add 10 to indicate the 10th line. Running this test, I would only expect one test to run, the test get started link. And it did, and it passed.
>
> **[4:43](https://www.linkedin.com/learning/learning-playwright/exploring-the-test-runner-command-line-interface?u=76281980&t=283)** You may have run into a situation where you've had a group of tests that you didn't want to group in the same folder, but you did want to run them all together. There's another option called grep, G-R-E-P, it lets you accomplish this goal. First you must categorize your test, and for this, we're going to use the Playwright tagging functionality. So if I hover over this test option, yeah, I've got some nice inline documentation. And here, there is details about tags, "You can tag tests by providing additional test details. Alternately, you can include tags in the test title." This example, they have the test, the test title, and they have a comma, an open param, tag, with a string, and then the closed param. This is all before the async page. So let's go ahead and add a tag to this test, on line 10. (keys clicking) Tag, colon. And we're going to make this called first, and we're going to add the comma afterwards. We should save it, and everything is happy. Now, if we wanted to run this code or just the first test, the way we would do that is by typing npx playwright test. We're going to do --grep, space, @first. This should only run the first tag in the test. Well, let's say I want to run all the tests except for the tag first,
>
> **[6:17](https://www.linkedin.com/learning/learning-playwright/exploring-the-test-runner-command-line-interface?u=76281980&t=377)** well there's a nice command for that. I'm going to hit the up arrow again, and we're going to change this grep to grep-invert. And this will run all the tests that do not have the first tag. Learning these commands will allow you to build very specific CI/CD pipelines, ensuring the right tests are run against the right browsers and projects and feature set.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[CLI]] (3)
> **CLI Commands:** npx (4), grep (4), make (3), cd (1)
> **Tools:** firefox (4), terminal (3), command line (2), vs code (1)
> **Env Vars:** cli (3)
> **UI Navigation:** select the (1), dropdown (1), scroll down (1)
> **Analogies:** for example (3)
> **File Paths:** playwright.config (1), example.spec.ts (1)
> **Non-Speech:** (keys clicking) (2)

#### [Overview of package.json](https://www.linkedin.com/learning/learning-playwright/overview-of-package-json?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/overview-of-package-json?u=76281980&t=0)** - [Instructor] Now that we have a good understanding of the different ways we can run our Playwright tests from the command line, let's take a look at the package.[[JSON]] file. We'll start by, what is it? It is a file that gets generated as a part of the initialization of a project. You can think of it as a cookbook for your project. This file tells you what packages and versions should be installed in the node_modules directory. Let's pull up our package.json file now. As you can see, from the file and from the file extension, this is a JSON file. So any additions or updates to this must respect that format. In our project, we have two dependencies currently, playwright/test and types/node. Each of them also have a version associated with them. As you add additional packages, this list will grow. So what are some of the features? You know, all the [[npm]] commands that you've run from the command line, those are commands that use the NPM [[CLI]] command line interface. npm install, for example, is one of the most common commands. What that does is it creates and updates this package-lock.json file, and it installs all the different libraries in this node_modules folder. For this course and [[Test Automation]] projects in general, there are only two areas in the package.json that matter most. This dependencies,
>
> **[1:33](https://www.linkedin.com/learning/learning-playwright/overview-of-package-json?u=76281980&t=93)** this is where new packages will get installed and you can manage the version of these packages. For example, when Playwright 48 gets released, I can come in here and update this to 48, save the file, do an npm install and it will install the latest version of Playwright. There's also the scripts section on line six. This allows us to create an alias for longer commands to be run from the terminal, making our lives and our developers lives much easier as our project grows. So let's look at a few examples. For our first example, let's create a basic alias named test. The goal of test, for it to be able to run NPX Playwright test. So let's do that. So here, I'm going to add a string of test. This is kind of like the key. The value here will be npx space playwright space test, and we're going to save it. Now, if I run npm run test, it will now execute npx playwright test. So you can think this is like an alias. Let's add a few more commands. Now let's create a command to run the chromium project. So to run that, I'm going to add a comma. I'm going to set the name to test:chromium, and the command we're going to run is npx space playwright
>
> **[3:09](https://www.linkedin.com/learning/learning-playwright/overview-of-package-json?u=76281980&t=189)** space test dash dash project space chromium. So now I should be able to run npm run test:chromium, and this should run the chromium project. I'm a really big fan of using this colon syntax in my script commands because it makes it really easy to read and really easy to follow. So we're going to go ahead and add a few more of these commands, and I'm just going to just do a little bit of copying and pasting to save us a little bit of typing, but it'll be test. Let's add one for the first tag, so we're going to run test:first. And here, we will have a dash dash grep and the tag will be first. And let's go ahead and create one for, you know, maybe we want to run our test against a local branch, so maybe we'll bypass the base URL. So this is a common example, even though we won't be using this in this course, but this will be useful. So we're going to set the BASE_URL, the BASE_URL environment variable, set to http, localhost 4200 is where the UI runs for this one. So what this allows me to do now is if I'm running the project on my local, I can run just this different test command, test:local, and it will pass in this environment variable called BASE_URL. All right, we're going to add two more.
>
> **[4:46](https://www.linkedin.com/learning/learning-playwright/overview-of-package-json?u=76281980&t=286)** We're going to add one with a test:report. So this is going to by default run the test, and then after the test is run, it's going to go ahead and run a second command to actually open up the test:report. So the test:report will open whether it passes or failed, playwright show-report. So in this example, I'm actually chaining two different commands. So test colon:report will run this first command. Once that's done, it's going to run the npx playwright show-report command. And the last one we're going to add down here is test:ui And what this is going to do is it's just a shortcut to run Playwright in UI mode, which is useful when you are debugging your tests, dash dash UI, and we're going to save this. Having scripts built and stored in the package.json file will empower you and other developers and testers and CI pipelines to the project to have easy access to specific ways to run your automated tests.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (6), [[npm]] (6), [[CLI]] (1), [[Test Automation]] (1)
> **CLI Commands:** npm (6), npx (5), node (1), grep (1)
> **Env Vars:** base_url (3), npm (2), json (1), cli (1), npx (1)
> **File Paths:** package.json (4), package-lock.json (1)
> **Tools:** command line (3), terminal (1)
> **Analogies:** for example (2), think of it as (1), kind of like (1)
> **Prerequisites:** install (3), initialization (1)
> **Definitions:** is a  (3)

#### [Exploring the VS Code extension](https://www.linkedin.com/learning/learning-playwright/exploring-the-vs-code-extension?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/exploring-the-vs-code-extension?u=76281980&t=0)** - [Instructor] Another tool you'll find useful while in your Playwright journey is the Playwright Test VS code extension. This can be installed via the URL and the resources handout, or in the extensions tab, you can type in Playwright test and look for the extension that's released by [[Microsoft]]. Once this is installed, which you may have to reboot your VS code, you'll have access to this test speaker. If for some reason you don't see any of your tests loading in this section up here, one thing you'll always want to check is just to ensure that the projects that you have set up in your config file are checked. So if I were to uncheck our Chromium and refresh this, it's likely these tests would not show up. But with these set up and Chromium installed, we see the test load and everything is happy. Now this test is the directory. The example .spec is the spec file that was bundled. This is our default Playwright test, and we're just going to interact with this section up here. So you've got a way to filter different tests. So if I need to filter by has or found a filter by @first, which is one of the tags that we added, you can actually see that things will get filtered properly. So this is the get started link test that is filtered there. And you also have a way to run test
>
> **[1:34](https://www.linkedin.com/learning/learning-playwright/exploring-the-vs-code-extension?u=76281980&t=94)** in lots of different ways. I can run all the tests by just clicking this play button. I can run a specific spec file, which in this case are two different tests, or I can run a specific test just by clicking these buttons over here. This second button here, that's a play button with a bug on it, is a way to debug your test. Now this is a really useful tool that you'll use a lot when you are writing tests and maintaining tests and trying to understand what's happening within the browser. And to enable this, I'm going to add a debug breakpoint here. I'm going to debug this test. Now, anytime you debug a test, this will open up a browser and it's going to pause the browser on the step that you are debugging. So I went to this webpage. I'm waiting to get this role by link, name, get started. And the goal on this line of code is to click it. So you can actually see this get started. It is actually highlighted. This is Playwright actually showing this off, and I can use this debug toolbar up here to do different things. So this button right here will continue the test. So it would continue it all the way to the end. This step over will basically step over to the next line of code. And then this is very interesting. This is where you can actually step into code. So this would actually step into this page .getrole and actually dip down into the Playwright logic
>
> **[3:08](https://www.linkedin.com/learning/learning-playwright/exploring-the-vs-code-extension?u=76281980&t=188)** if you really want to understand what's happening. And you've got a restart and you've got a stop as well. So in this scenario, I'm just going to step over. I found this is a really useful thing. So it should have clicked the get started link. And now we are expecting the page role heading name installation to be visible. So I'm just going to pull up the browser here. And it looks like this is actually the element that it's looking for, and it looks like it's visible. So this test should pass. So I'm going to step over to the end. I'm actually going to continue all the way through. And the test passed. Debugging is also really useful if you're wanting to look at different variables or things like that. And we will show an example later in this course on how to debug a little deeper. Another useful icon here is this icon here. This will take you to the test. So if you're working with a big long list of tests, and you want to go directly to test, you can click that button and it will take you to the test specifically. So you're not having to do a bunch of searching. And this last icon is also very interesting. This is turning on continuous run. So what this will do, you can enable this for your whole suite or just for a specific test, is if you are making changes to the test file, and as you save those changes, it's actually going to attempt to rerun the test and see if it passes or fails. So I'm just going to change this to installation. It's going to be wrong. I'm expecting this to fail.
>
> **[4:45](https://www.linkedin.com/learning/learning-playwright/exploring-the-vs-code-extension?u=76281980&t=285)** Looks like it'll fail in a couple seconds, I think. Yep. So hey, that's not correct. It's failing. If I go fix it, it's going to run it, it's going to pass. But anytime I save the file, it's going to execute the test, which can be really useful if you're debugging through some stuff. All right, so I'm going to un-click that, and we're going to dive down into this next section down here, the Playwright section, specifically the settings to allow us to show a browser. So by default, when we're running these, the browser's not popping up. But if I wanted to show the browser, I could enable that and the browser button will stay up. And as I run another test, it'll actually continue to pop the browser up. So I'm going to un-click that. You can also show the trace viewer. That's another very useful tool. So you can actually see what happened during the test. We'll look into this a little later as we explore things. And this is another tool that we're going to look at as well. This is the record a new test. So I'm going to click this button. What this is going to do is it's going to open up a web browser. We've got the test that has basically been written down here, and as we interact with the web browser, it's going to record things. I'm going to go ahead and type in the Playwright site, [Playwright.dev](https://Playwright.dev). I'm going to enter, and from here, any of these actions, I can click on them, interact with them.
>
> **[6:20](https://www.linkedin.com/learning/learning-playwright/exploring-the-vs-code-extension?u=76281980&t=380)** I'm actually going to click on community, I'm going to find ambassadors. Let's go ahead and click on that. And so I've got a couple different options up here. So I can pick a locator, assert visibility, assert some text. So let's just go ahead and assert some text. We're going to assert that Butch Mayhew is still on the ambassador page. You know, maybe one day I'm not. But as you can see, we've actually got some code that's been written for us. So I'm going to go ahead and close this. So that's going to, you know, stop the recording. And let's just play this. I'm going to show the browser, make sure it is working. All right, so it ran, it passed, and now we have a new test. Testone.spec.ts. Recording a new test is actually a powerful tool that we'll use in a future section. Feel free to take a few minutes and explore the functionality on your own before the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Microsoft]] (1)
> **Documentation:** spec (4)
> **CLI Commands:** find (2), make (1)
> **UI Navigation:** click on (3)
> **Definitions:** is a  (3)
> **Cross-References:** later in (1), next video (1)
> **Tools:** vs code (2)
> **Prerequisites:** set up (2)

#### [Exploring the Playwright UI Mode](https://www.linkedin.com/learning/learning-playwright/exploring-playwright-ui-mode?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/exploring-playwright-ui-mode?u=76281980&t=0)** - [Instructor] For those times when you want to create and update Playwright test on your local machine and get immediate feedback anytime a test is changed, Playwright UI mode is the tool to do it. To start playwright UI mode, we can use the MPM run script that we created from the previous videos, "[[npm]] run [[Representational State Transfer (REST)|rest]]:ui". This is going to run the NPX Playwright test UI command, and you can see it is loaded. On the left hand side, you'll notice a similar interface to VS Code in order to run your test. You'll also notice that this first tag is one of the tags we added, is available here. I can even click on it and filter down by all those tags, which is really useful when you have a larger test suite. You can also run all the tests here or use F5, the keyboard shortcut to do that. I'm going to go ahead and run these. So all the tests ran, everything passed. These did run in headless mode, but you can see on the right hand side when I go look at some of the details, that we actually do have a picture of the dom. So even though the browser didn't pop up, we've got this information over here. The first thing you can notice is each of these different steps will change what you see on the webpage and you can even hover over this timeline and go into the future or back to the future. Feeling like Marty McFly a little bit?
>
> **[1:35](https://www.linkedin.com/learning/learning-playwright/exploring-playwright-ui-mode?u=76281980&t=95)** Not only can you see the steps over here, but you can also look at the source code. So as each step happens, you can actually step and see, you know, what code is actually being called to run these things. Below the web view, you can also see a ton of other information. I'm specifically going to look at the logs. So these are Playwright logs. So for example, to be able to click this doc in the sidebar, this is all the steps that Playwright did to go do that. This can be really helpful when debugging or looking into errors into your test if you're having issues with trying to find a locator. You also have access to different errors if there's on the page or any console errors. You also have access to network requests. This is a superpower, we will look into this in the future, but just know that it exists. Let's shift our focus up to this timeline view. Another nice thing is you can click and drag and actually show which steps based on this timeline. So just another useful thing, there's a lot of hidden goodies that aren't noticeable when you first start looking at it. I'll show one other section in this locator click, so this is the ambassador click step. You can see the action, which is currently highlighted, but you can also see, what did it look like before the action? And then what did it look like after the action?
>
> **[3:09](https://www.linkedin.com/learning/learning-playwright/exploring-playwright-ui-mode?u=76281980&t=189)** So you can kind of see how the page changes, how the link turned green, you know, once it's active. And you can also see the action itself, indicated by the red circle. Not only that, but let's say I wanted to interact with the video element, I can use this pick locator, it looks like a target. I can click video link and now I have Playwright generated code that I could go add to my code and update a locator if I wanted to click on something different. So let's see what happens if a test fails in this scenario. Let me go pull up my test file. We're going to go use this section. Let's say that we're looking for Butch Maghew and I'm going to go ahead and rerun the test in this mode
>
> **[4:07](https://www.linkedin.com/learning/learning-playwright/exploring-playwright-ui-mode?u=76281980&t=247)** and we can see the test is running, I can kind of see it in real time, and it looks like it failed. So I can click here to see why it failed. I can see up in this section, you know, the timeline of which it failed, and I can come down here and see the errors. And this is it. "The expected string Butch Maghew is not included in this receive string." So if I wanted to fix this, let's just go ahead and turn on the watch mode. So it should automatically run as soon as I save it. Let's go fix our test. We'll save it. Looks like it's already run, it's already passed, and everything is good to go. So the reason this tool is so important and so helpful is it gives you instantaneous feedback as you're building your test, as you're running your test, without having to click through or open manual reports. Everything is right here at your fingertips, ready to go.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[npm]] (1), [[Representational State Transfer (REST)|Rest]] (1)
> **CLI Commands:** npm (1), npx (1), find (1)
> **UI Navigation:** click on (2), in the sidebar (1)
> **Env Vars:** mpm (1), npx (1)
> **Analogies:** picture (1), for example (1)
> **Tools:** vs code (1)
> **Exercise Files:** source code (1)
> **Definitions:** is a  (1)


### 2. Creating Tests with Playwright

[↑ Back to Table of Contents](#table-of-contents)

#### [Overview of the website under test](https://www.linkedin.com/learning/learning-playwright/overview-of-the-website-under-test?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/overview-of-the-website-under-test?u=76281980&t=0)** - [Instructor] From here on out, we'll be using a dedicated site to test against, [practicesoftwaretesting.com](https://practicesoftwaretesting.com). This is a site built by Roy de Kleijn and is useful for practicing [[Software Testing]], hence the name. It's always a good idea to take some time and explore the website you plan to automate, so let's do that now. In the resource guide, we have a link to it. The first thing I notice when I visit the site is there's a lot of images that are tools. It almost looks like it's a shop, maybe an [[E-Commerce]] store. On the left hand side, I do notice there's a lot of different ways to filter things. I can filter down by a wrench and I see different wrenches. I can filter down by a specific brand and that will filter things down on the brand. If I go look at the URL, I see there's no URL parameters. That's interesting. I wonder how it knows how to filter? This also looks like there's a sign in page and maybe there's a cart, potentially? If I click one of these, okay, so I can probably add something to cart. So these are all interesting things. Let's take a look a little deeper and see on the network tab. So I right clicked, I clicked inspect, I opened up the network tab and I clicked this [[Fetch]] XHR. This filters down any request only to API calls. Let me go ahead and click the wrench category again.
>
> **[1:36](https://www.linkedin.com/learning/learning-playwright/overview-of-the-website-under-test?u=76281980&t=96)** I see there's a new call down here and I see it's actually hitting an API. So api.[practicesoftwaretesting.com/products](https://practicesoftwaretesting.com/products) and then that has some query parameters with a price and some other stuff. And I can actually see the data that's actually returned matches up with what's on the screen. One, two, three different items. So this is going to be a nice site to test with 'cause it's probably similar to a site you might be testing with in a professional setting. Let's take a look now at the sign in page. So I'm going to close the network tab. Let's click sign in. Let me just go ahead and look at the URL. So this is [practicesoftwaretesting.com/off/login](https://practicesoftwaretesting.com/off/login). And we need a login. Good thing Mr. Roy has built a nice little [[GitHub]] repo page, which actually has some provided logins. So this is another companion that's in your resource page to go check out the GitHub repo that this site lives in. If I scroll down, I can see we've got some default accounts. We've got [[John the Ripper|John]] Doe, Jane Doe with an email address and a password. This is going to be helpful for testing different scenarios. There's also some different versions of the website. So if you want to look at a version with bugs, those exist. We're not going to get into that in this lesson, but feel free to explore that on your own. Let's go ahead and use one of these logins.
>
> **[3:09](https://www.linkedin.com/learning/learning-playwright/overview-of-the-website-under-test?u=76281980&t=189)** We'll use the user login. So customer, I'm just going to copy this. I'm going to take note that the password is welcome01, and let's go ahead and utilize this. I'm going to paste customer@practicesoftwaretesting. I'm going to type in welcome01. I'm going to click login. And I see that yes, I'm in an authenticated section where I've got a profile and some other information. So in all this, let's go ahead and just run through maybe the happiest path scenario that I can think of that's more of an end-to-end test. So let's go try to add one of these tools to our cart and attempt to check out. So I'm going to pick the combination pliers. We're going to add it to cart. Okay, so it looks like we've got a little notification. We've got a cart icon that shows up. It looks like I have to click on the cart icon to go through the process. And we're just going to walk through this, step through this and add any additional information as needed. So maybe we'll add a state, maybe add a postal code. And we're just adding random test data, payment method. Let's choose cash on delivery and see what happens. We'll click confirm. All right, it tells me the payment is successful. This is useful information as we can walk through and validate this in a test. Feel free to take some additional time
>
> **[4:43](https://www.linkedin.com/learning/learning-playwright/overview-of-the-website-under-test?u=76281980&t=283)** and explore the site before the next video. Feel free to look deeper into web tools, see what other APIs are available. All these are available for us to utilize with Playwright. So understanding the site under tests will only make it easier as we start automating certain actions in the site.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (2), [[Software Testing]] (1), [[E-Commerce]] (1), [[Fetch]] (1), [[Microsoft Products|Products]] (1)
> **Env Vars:** url (3), api (2), xhr (1)
> **URLs:** [practicesoftwaretesting.com](https://practicesoftwaretesting.com) (3)
> **Tools:** github (2)
> **UI Navigation:** scroll down (1), click on (1)
> **Exercise Files:** github repo (2)
> **CLI Commands:** make (1)
> **Cross-References:** next video (1)

#### [Generating tests with codegen](https://www.linkedin.com/learning/learning-playwright/generating-tests-with-codegen?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/generating-tests-with-codegen?u=76281980&t=0)** - [Instructor] One of the most useful tools that Playwright offers when getting started with your first test is the Code Gen tool. This is short for Code Generator. This tool will help us give a headstart by recording locators that we interact with to build out our first test from scratch against the website that we're testing against. To run the Code Gen tool, you can run the command MPX space Playwright, space Code Gen. So let's go ahead and run that. (keyboard clacking) Upon hitting enter, you'll notice two [[Windows]] pop up. You've got the Playwright inspector. This is going to be where all of our code is generated to. And you've got the Chrome browser over here. This is where we're going to be browsing to the webpage. So let's go ahead and put in the address bar, our [PracticeSoftwareTesting.com](https://PracticeSoftwareTesting.com) URL. We're going to browse to it and for our first test, we are going to go ahead and record what a sign in flow would look like for the first customer. So let's go ahead and click sign in. Let's click our email. We're going to type in customer@[practicesoftwaretesting.com](https://practicesoftwaretesting.com). The password, it's going to be welcome01. We're going to click login and notice each time we interacted with the webpage, we have code that is generated for us. Now this isn't quite a test yet, in my opinion.
>
> **[1:34](https://www.linkedin.com/learning/learning-playwright/generating-tests-with-codegen?u=76281980&t=94)** We're doing a bunch of actions on the page, but we are not making any assertions. So to make an assertion, we're going to go find this little AB. This is going to let us assert on text. I'm going to click it. I'm going to go find some text that I want to assert on. So I could choose Jane Doe. I could choose my account. Maybe we'll do both. I'll choose Jane Doe first. We're going to assert that the element contains the text, and let's also go ahead and just validate that my account is on the screen as well. And you can see the code is being generated over here. Now let's take a look at this code a little deeper. But before we just get done, I want to copy this code. So I should be able to click this button, should copy the code to the clipboard. I'm going to go to my test folder. We're going to create a new file. We're going to call it login.spec.TS, and we're just going to paste that code in this file. I'm going to go ahead and save it. Also want to go ahead and run this just to make sure everything is working properly. So one way to do that would be clicking on the testing icon and finding the test. Right now is just named test. Let's go ahead and name this to login test. Or you can just click this green button here next to the test and we'll ensure that everything passes. All right, so the test ran.
>
> **[3:07](https://www.linkedin.com/learning/learning-playwright/generating-tests-with-codegen?u=76281980&t=187)** The test passed. I'm going to go ahead and unclick this show browser, so in the future that will not be popping up on me. Let's just run it one more time. Everything looks good, it passed. So I'm going to go ahead and commit this code to our repository and congratulations. We just completed our first test. Now the Code Gen tool is still running in the background. You can either close it up here or you can hit control or command C at the terminal, and that will close the windows for us. One other thing to note before we save and commit this is we will need to clean this up a bit. So right now we are visiting the site. We are clicking sign in, that's needed. Page locator clicking email. I don't think we need to click the email. I think we can just fill it. So I'm going to take off the click of the email and I'm also going to take off the click of the password field, and we're just going to fill those instead of clicking. All right, I think everything's good. Let's just run it one more time to make sure everything's passing. All right. You'll also notice in this video there are locators that are created with this data-test attribute. This is something we're going to be covering in the next videos as we look at different ways we can build locators for our tests from scratch.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Windows]] (2)
> **CLI Commands:** make (3), find (2)
> **URLs:** [practicesoftwaretesting.com](https://practicesoftwaretesting.com) (2)
> **Env Vars:** mpx (1), url (1)
> **File Paths:** login.spec.ts (1)
> **Cross-References:** in the next (1)
> **Documentation:** spec (1)
> **Tools:** terminal (1)

#### [Overview of locator strategies in Playwright](https://www.linkedin.com/learning/learning-playwright/overview-of-locator-strategies-in-playwright?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/overview-of-locator-strategies-in-playwright?u=76281980&t=0)** - [Instructor] Much like other test-automation tools, Playwright is able to interact with the DOM, which stands for [[Document Object Model (DOM)|Document Object Model]]. In many cases today, the DOM will look different than the original [[HTML]] as the DOM includes any [[JavaScript]] modifications. The DOM is what is viewable when you use Chrome DevTools and look in the elements tab. Playwright interacts with a DOM using two types of locators, the recommended built-in locators and the legacy locators, which include things like CSS selectors and XPATH, that you may be familiar with if you've done [[Test Automation]] before. We will dive into different types of locators, along with some examples in this video. This is a list of the built-in locators recommended to use first by the Playwright development team. Let's take a look at a few of these. The first is page.getByRole. This example shows the syntax , we are getting the role button with the name sign in and we're clicking the button as the action. One thing to note is if you're interacting with or asserting against an element, you will need the await syntax at the beginning of the locator. You'll see this over and over in our examples, so take note. In this next example you can see we are also able to get locators by text within the DOM. The element within the DOM we want to interact with has text, Jane Doe.
>
> **[1:34](https://www.linkedin.com/learning/learning-playwright/overview-of-locator-strategies-in-playwright?u=76281980&t=94)** In this specific line of code is expecting this element to be visible. If for some reason an element that was matching this locator didn't exist or was hidden state, the Playwright test would fail. When we look at getByLabel locator, this is typically useful for inputs within applications as labels are used for making websites more accessible. In this example, we're calling the .fill method and passing in an email address. This will automatically add the email string into the input box. It uses JavaScript to do this. If you need to actually type each letter, there is another command to do that. Press sequentially, which you can check out in the Playwright docs. Before we cover get by test ID, we will need to add this configuration option to our Playwright Config. Within the define config use-section, we'll need to add testIdAttribute and set this to data-test because the website we are testing with uses this as the test ID attribute. This overrides the Playwright default. So let's take a minute and go ahead and update our local config file with this change, as we'll need it to be in place for the next session. So I've opened up my Playwright config and I'm going to scroll down to the use section. And within this use section, this is where we'll add this test ID attribute.
>
> **[3:08](https://www.linkedin.com/learning/learning-playwright/overview-of-locator-strategies-in-playwright?u=76281980&t=188)** (keyboard keys clicking) We're going to set it to string data-test. We'll have to add a comma here because this is a [[JSON]] file. With that change in place against our test site, we can see the code getByTestId nav-sign-in.click. We'll now click the sign-in button. The previous get by test ID example can also be seen here using the legacy locator pattern. This is setting the locator based on the data test ID custom attribute. Note this example and the previous example are doing the exact same thing, but the syntax is slightly different. The CSS locator's example allows you to pass any CSS selector after CCS equals. This specific example is very generic, and if there is more than one button within the DOM, this test would likely fail with an error message letting us know that there are multiple buttons that were matched. For examples where you want to combine locators, you can use any CSS selector. In this example, we use the article and utilize the has text matcher that is provided within playwright locators. More details can, of course, be found in the Playwright docs. Legacy locators can also have two locators when splitting them with a comma. This code would click a button with a text sign-in or login.
>
> **[4:46](https://www.linkedin.com/learning/learning-playwright/overview-of-locator-strategies-in-playwright?u=76281980&t=286)** This isn't something that is used often, as most of our tests should be deterministic, but could be useful if you're writing tests for AB testing scenarios. You're also able to utilize Xpath within the legacy locators. I usually use this as a last resort as it's more difficult to maintain and less readable. To review, you should use the recommended locators. This is a good indication if your website is testable or not. You're able to chain locators together or filter items to get to specific elements within the DOM that you want to interact or assert against. Feel free to use legacy locators if needed, and always remember to use a weight when interacting with or asserting against elements.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2), [[Document Object Model (DOM)|Document object model]] (1), [[HTML]] (1), [[Test Automation]] (1), [[JSON]] (1)
> **Env Vars:** dom (9), css (4), html (1), xpath (1), json (1)
> **Code Identifiers:** getbyrole (1), getbylabel (1), testidattribute (1), getbytestid (1)
> **Definitions:** is a  (3), stands for (1)
> **Best Practices:** recommended (3), remember to (1)
> **Tools:** chrome devtools (1)
> **UI Navigation:** scroll down (1)
> **Speakers:** - [instructor] (1)

#### [Overview of assertions in Playwright](https://www.linkedin.com/learning/learning-playwright/overview-of-assertions-in-playwright?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/overview-of-assertions-in-playwright?u=76281980&t=0)** - [Presenter] Assertions are an essential part of building out your [[Test Automation]] framework. Without an assertion to validate what you're expecting, are you really even testing? With Playwright, there are two general categories of assertions, locator assertions and value assertions. The locator assertions are special in that this type of assertion fails, it will automatically get retried until it either passes or it reaches the timeout. The value assertion, on the other hand, will be evaluated one time and either pass or fail. Here are a few the locator assertions. One thing to note here is that we are using an await at the beginning of each of these assertions. This is because we are interacting with and asserting against the browser or the DOM. The way this may play out is if I'm on a page URL and I click a button which submits a form, if there are a few redirects to different URLs before landing on the final URL, if I use await expect(page).toHaveURL() with the URL value assertion, I can put the final URL I would expect, and the test won't fail immediately if it gets back one of the URLs that are still a part of the redirect process. This can be really useful to ensure that your page is on the correct page before moving on with any further automation. This assertion also acts as an implicit wait in your test.
>
> **[1:39](https://www.linkedin.com/learning/learning-playwright/overview-of-assertions-in-playwright?u=76281980&t=99)** Here are some examples of different value assertions. Note that this doesn't have the await syntax in front of the expect, though you may potentially need to await certain values in your test, depending on what values are used. A full list of assertions can be found in the docs. Let's jump into some code and see how this works in action. Let's start by writing a test for the practice site's homepage. So let's go ahead and open our VS Code, let's go into the test folder, and let's create a new file called home.spec.ts. This is going to house the new test that we're going to write together, and we're going to go ahead and start by importing (keys clicking) test and expect (keys clicking) from (keys clicking) @playwright/test. So we've imported test and expect, so we can interact with it. I'm going to go ahead and add a test block here. It's going to be "Home page." After the title, we're going to add a comma, async. After async, it's going to be open parentheses, open brackets. We're going to add page in there. We're going to add a hash rocket. This is going to be an equal and a greater than sign, and then an open bracket, and this is where our tests are going to go. So this is the Playwright syntax to create a test. And the first thing we definitely want to do is
>
> **[3:15](https://www.linkedin.com/learning/learning-playwright/overview-of-assertions-in-playwright?u=76281980&t=195)** we want to do an await page.goto. The goto command allows you to go browse a site, [practicesoftwaretesting.com](https://practicesoftwaretesting.com). We'll go ahead and add a slash to the end, and from here, we're going to go to this page. Let's actually, I'm going to show the browser 'cause I want to see it running. So I'm just going to run this test, and I should expect the webpage to load that tells me everything is running the way it should. And let's go ahead and identify what are those tests going to be. So I think the first thing we can do is ensure the sign-in link is present. So let's Ensure the sign-in link is present, and I'm just adding comments here just so I have some placeholder text so we can add some additional stuff. We're going to Check the title of the page, so this is going to be what would be displayed in a tab. We're going to Check the count of items displayed, (keys clicking) and we'll make that a comment 'cause it's not real code. And we're also going to make an assertion for SEarch for Thor Hammer and check the result. So the first thing we want to do is ensure the sign-in link is present, so let's just walk through what that would look like. So I'm going to go pull up the browser that we have over here, and I just want to validate that this sign-in link is present.
>
> **[4:52](https://www.linkedin.com/learning/learning-playwright/overview-of-assertions-in-playwright?u=76281980&t=292)** So the easiest way to validate this stuff is open up a web browser, right click on the page, run an element, click inspect. This will pull up your developer tools, and we're going to look inside the elements and kind of drive into the element we want. I really like viewing this on the bottom versus on the side, so I'm going to dock this to the bottom by clicking the three dots here and choosing the bottom. And I'm going to go ahead and click this inspector tool, and I'm going to click this sign-in. This is going to take me to the exact element, which I can see there is a data-test value here. In a previous episode, we set our data attribute ID to data-test, so we should be able to ensure that the sign-in link is present by doing an await expect page.getByTestId,
>
> **[5:52](https://www.linkedin.com/learning/learning-playwright/overview-of-assertions-in-playwright?u=76281980&t=352)** and let's go see what that ID is. The ID is nav-sign-n, so I'm just going to copy that, and after the expect, I want to have a toHaveText.
>
> **[6:11](https://www.linkedin.com/learning/learning-playwright/overview-of-assertions-in-playwright?u=76281980&t=371)** We're going to pass and Sign in or sign in. Is it uppercase or lowercase, this is important. It is lowercase, okay, so we'll leave that lowercase. What I'm going to do is I'm just going to run this test. I'm going to make sure it passes. Okay, it passes. I also just want to make sure that if something does change, it does fail, so let's just make Sign In capital and see, I should expect this to fail. All right, it fails. It tells me why it fails because it is expecting the capital I-N. It got the lowercase I-N, so always a good idea to just make sure your test fails when it should fail on certain assertions before you commit the code. So that's pretty simple. We have a test that makes sure the sign-in button is there. Let's go ahead and check the title of the page. What this is going to look like is this is going to be an await, we're going to do an expect, and we're going to pass in page and we're going to expect the page or the browser context that Playwright knows about toHaveTitle, and it's actually going to be Practice [[Software Testing]] - Tool Shop - v.0 It's going to be this right here. So this information can be gettable. It's basically up here at the top. Practice Software Testing - Tool Shop - v.0 So let's go ahead and run this,
>
> **[7:46](https://www.linkedin.com/learning/learning-playwright/overview-of-assertions-in-playwright?u=76281980&t=466)** make sure both of these pass, okay, they do. Let's just make sure this does fail again. We want to make sure that our tests are doing what they should be doing. All right, so that is going to fail, all right, wonderful. So we're going to set it back to 5.0. And the next thing is to check the count of items displayed, so let's go back to our webpage. And we want it to just expect 1, 2, 3, 4, 5, 6, 7, 8, 9 items are displayed on this homepage, so to do that, we need to identify how can we count these? So first thing we need to do is figure out like what kind of div are they in, or where are they in the DOM, how can we find them? So I used my little helper tool and I kind of just hovered over until I found an area where it looks like it's capturing all of them. It's this div container. Now, this does have a pretty gnarly ID here. We're not going to use that. I think that gets randomly created. It does have a data test attribute, but there's no value, so we're not going to be able to use this, but let's see if there's anything else we can use that is similar to this. So it looks like there is a div right above this container that does have a class of col-md-9, so we're going to try to use that, and I'm going to show you a little trick that can come in handy. So we're going to call that the product grid, so productGrid, and we are going to set that equal to,
>
> **[9:21](https://www.linkedin.com/learning/learning-playwright/overview-of-assertions-in-playwright?u=76281980&t=561)** actually, we're going to set it as a constant variable. This const syntax, we'll set it as a variable, a variable that is not going to change. So productGrid = page.locator. So this is using a legacy locator, if you remember from our first locator lesson. We're going to pass in .col-md-9. The productGrid here is set to the page.locator.col-md-9, and this dot indicates it's a class, which can be identified here as well, class = . So what this allows me to do is this value is now this locator, so I can actually use that when interacting with another locator. So let's say for our expect, we're going to do an await expect, and we're going to say productGrid, and then within the productGrid, we're going to do a getByRole("link"), and we want this .toHaveCount(9); all right?
>
> **[10:41](https://www.linkedin.com/learning/learning-playwright/overview-of-assertions-in-playwright?u=76281980&t=641)** So what we're saying is within this productGrid, go get all the links, and we expect that to have a count of nine. So what that would look like if I was doing it manually is within this grid, I'm going to go find all the different A tags, which are links. There should be nine. I want it to have that count of nine. I'll also walk you through how you would do this using a non-locator assertion. So in this example, we're using a locator assertion to validate it, but you can also use a non-locator assertion as well, and this is what that will look like. Expect, we're going to do an await productGrid.GetByRole,
>
> **[11:30](https://www.linkedin.com/learning/learning-playwright/overview-of-assertions-in-playwright?u=76281980&t=690)** we're going to pass in ("link"), so a lot of the syntax is the same, but you can see the ordering is a little different. And inside this expect, I'm actually going to do a count, so I'm going to basically say get the role count, and we want to assert that to be nine, right? The difference here is this is a locator assertion where I'm actually expecting, I'm passing it a locator to have a count, and this is going to wait, whereas this is going to have the await inside the expect, and it's actually going to get a count, and it's just going to say, is nine equal to nine versus, you know, does it have a count of nine inside the elements? So there's a lot of flexibility here, and there's a lot of documentation to kind of walk through those different things, but that is one example. Let's just run this and make sure everything looks good. All right, everything passed, good to go. Now it's going to be your turn, so let's go back to our slides. Here's a mini challenge for you. I want you to take a few minutes and see if you can build out the fourth assertion. So search for ThorHammer is the action, and then check the result in the grid is the assertion. I'll walk through the code for this in the next section as we look at organizing our test.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Software Testing]] (2), [[Test Automation]] (1)
> **Code Identifiers:** productgrid (7), tohaveurl (1), getbytestid (1), tohavetext (1), tohavetitle (1)
> **CLI Commands:** make (10), find (2)
> **Env Vars:** url (4), dom (2)
> **Definitions:** is a  (4)
> **Non-Speech:** (keys clicking) (4)
> **Cross-References:** go back to (2), in the next (1)
> **Documentation:** the docs (1), spec (1)

#### [Structuring Playwright tests](https://www.linkedin.com/learning/learning-playwright/structuring-playwright-tests?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/structuring-playwright-tests?u=76281980&t=0)** - [Instructor] Let's take a look at the fourth assertion challenge from the previous lesson. First, we need to find the test ID, so let's walk through what that would look like for the search input box. So let's use our selector, select search, and we can see the data test ID is search query. From here, we'd want to fill in Thor hammer, and then we would also want to click the search key. So let's see the search button. So we can see that is search-submit. Once that is clicked, we can use the container that we identified before and it looks like there is a get by alt text, Thor hammer. That can be our locator. So this is the alt text here. So that may be for the overall picture. So that is the alt text for the image that is actually showing up there that Playwright's kind of showing us. So these are the things we can search by and assert against. So this is what the code could look like. Get by test ID search query, we're going to fill with Thor hammer. We're going to get the search submit button and click it, and then we're going to have an expect down here. Expect this to have a count of one. And we're also going to expect that the alt text, so the picture of Thor hammer is visible.
>
> **[1:36](https://www.linkedin.com/learning/learning-playwright/structuring-playwright-tests?u=76281980&t=96)** And I ran this, everything looked good and everything is great. As we continue to build out test on this page though, we can see it's going to get really tricky, right, if I have 50 more assertions I want to write on this page. We could continue to write lots of other specific assertions for these elements on the page, and other interactions, but now it's actually a good time to start discussing how to structure your Playwright test. For example, we now have four different tests in this one test block. So let's take a few minutes and work through refactoring this file and structuring our test so they're more actionable and maintainable. There will be a few new concepts introduced. We're going to introduce the describe block, which is used to group tests together, the before all and before each and after each blocks. These are things that can be run from within the describe block, outside of a test context. So let's go ahead and start with that. At the top of your test, let's add a new line. It's going to be test.describe. We're going to pass in homepage. We're going to use open parentheses, we're going to do the hash rocket, and then we're going to open bracket, and you can see this is a describe block. Now we actually just want to wrap our existing test in this describe block. So I'm going to copy these last two, close bracket and close parentheses.
>
> **[3:10](https://www.linkedin.com/learning/learning-playwright/structuring-playwright-tests?u=76281980&t=190)** I'm going to right click, I'm going to cut these, and I'm going to move them all the way to the bottom of the file and right click and paste. And this should reformat everything upon you saving, and indent everything properly. So we have a test describe block and a test block as well. Now let's split up our test into different checks. To do this, let's go ahead and comment out our assertions that we aren't using for now. So we're going to go ahead and come all the way to the bottom. We're going to select all of these. I'm going to hold down command or control, and I'm going to hit the back slash button, the one that's on the question mark to comment out the test. Let's start by renaming the first test to check sign in. So we've got homepage, check sign in. All right, let's go ahead and run this test and just make sure everything is working properly, everything passed. So this test is actually complete. I'm going to go ahead and move the bottom of this test block to the bottom of this test block. We're going to go ahead and create the next test in its own test block. So let's hit enter and we're going to add a new test block for validate page title. It's going to be the same syntax. Pass in page, it's going to be the hash rocket. And I'm just going to go ahead and cut this
>
> **[4:43](https://www.linkedin.com/learning/learning-playwright/structuring-playwright-tests?u=76281980&t=283)** and paste it in this test block. And then I'm also going to select it. I'm going to hit command or control in the backslash to uncomment that out, and we can remove this comment as well. So in this test we're going to validate the page title. We're just going to run this assertion. So let's go ahead and run our test and see what happens. And we're noticing this second test fails. So let's go look at the Chrome window and it looks like it's not on a webpage. This leads to an important thing to remember when writing Playwright test. For every test block, you have a brand new browser context. Think of it like a clean web browser with no history, no cookies. This may seem like a negative thing at first, but I can tell you from experience, it's a blessing which will prevent you from having a lot of flaky tests in the future. So to handle this scenario, let's utilize a before block. We have two options. We can use before all or before each. For this specific scenario, we'll need a before each block. So let's add the code there and run our test, and it should pass. So underneath the describe block, we're going to hit enter down. We're going to type in test.before each, open parentheses a sync, we're going to have a space, open parentheses, open bracket. We're going to pass in page and I'm going to arrow key out twice. We're going to have our hash rocket, open bracket,
>
> **[6:18](https://www.linkedin.com/learning/learning-playwright/structuring-playwright-tests?u=76281980&t=378)** we're going to hit enter, and this is where we're going to move this await page go-to code. So I'm just going to select this, right click cut, and we're going to right click paste. And when we save this, it should format it properly. So let's go ahead and run this test now and see if everything passes. So now everything passes. I'm going to clean up my comments, and I'm going to go ahead and work on writing out the other two test blocks for the next two sections. For this, I'm going to take a bit of a shortcut. I'm just going to copy this test block. I'm going to copy the end of the test block, and we're going to update the page title to grid loads with nine items. And we're going to go ahead and uncomment this. We'll clean up this comment and we're going to do that one more time for searching for Thor hammer. And we're going to uncomment this code. We're going to type, search for Thor hammer. All right, and I'm going to run these things. I do see a red squiggly line, so we'll take a look and see if it fails. It should fail, I think, if there's a red squiggly. Let's see what it says. It says, "Reference error, product grid is not defined." So the product grid is something that we did define
>
> **[7:54](https://www.linkedin.com/learning/learning-playwright/structuring-playwright-tests?u=76281980&t=474)** in the third test, but because we put these all in their own test blocks, this product grid is not available down here. For now, we're going to go ahead and copy this variable. We're just going to duplicate it within this test, and in a later lesson, we'll talk more about how we can reduce duplication of code and really work on making our code DRY. DRY stands for, "Do not repeat yourself." So let's run our test one more time, make sure everything is passing, and we are good. Let's go take a look at the test files. And now we actually have our homepage. We're going to check the sign in, validate page title, the grid loads. So I have very clear names. My test is structured well and is super clear that if something were to fail, where the failure occurred. So as an analogy of what I mean here is when going to look at a new home to buy, you'd expect your realtor to send you a specific address and a house number rather than a street name and a house color, right? You want to make it as easy as possible for your future self to locate the issues in your test.

> [!info]- Semantic Content
>
> **CLI Commands:** make (3), find (1)
> **Analogies:** picture (2), for example (1), think of it like (1)
> **Definitions:** is a  (2), stands for (1)
> **Env Vars:** dry (2)
> **Speakers:** - [instructor] (1)

#### [Handling cookie authentication in Playwright](https://www.linkedin.com/learning/learning-playwright/handling-cookie-authentication-in-playwright?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/handling-cookie-authentication-in-playwright?u=76281980&t=0)** - [Instructor] Now that we've got a few homepage tests in place, let's look at what it's like testing the system from a logged-in user's perspective. We created a login spec already, but from the last lesson, you may notice a challenge we may be facing. Every test creates a new browser context. How can we quickly and easily get our test into a logged-in state for the tests that require login? The simple answer is we just go through the login steps before each step. But this will add a lot of time to our test execution time and it'd be a lot of repeated code. Thankfully the Playwright team has built some awesome tools to help us store an authentication state, all within Playwright. When we were setting up the project section in the Playwright config file, You may remember that, with the setup project, with something we built, we'll be using this project to handle authentication within the system once, and saving the state to be used within other tests. So let's start by creating a new file in the test directory. We'll name it auth.setup.ts. We'll start by importing test as setup, and expect from @playwright. Using the syntax makes it so I can type setup rather than test when creating my setup file. Let's name the setup step as create customer 01 auth, and we'll need to pass both the page and the context here,
>
> **[1:35](https://www.linkedin.com/learning/learning-playwright/handling-cookie-authentication-in-playwright?u=76281980&t=95)** as the browser context is needed to save the storage seat. So setup, Create customer 01 auth, async.
>
> **[1:47](https://www.linkedin.com/learning/learning-playwright/handling-cookie-authentication-in-playwright?u=76281980&t=107)** Open parentheses, open paren. We're going to pass in page, and then context. And then after those is going to be the hash rocket. And then open brackets. And in this section, this is where we can create our steps. We're going to be using the same customer that we logged in with before, but rather than typing out the email in our test, I'm going to go ahead and create a variable. So we're going to create a const variable named email. We're going to set it to customer@[practicesoftwaretesting.com](https://practicesoftwaretesting.com). And I'm going to add a second variable named password. Welcome01. And I'm also going to go ahead and create a constant for the customer01AuthFile, which we'll be using a little later. In this scenario, I'm going to create a new folder, a .auth folder. Within that folder we're going to create a customer01.[[JSON]] file that will be saved, and this is where the credentials or the cookies will be saved for customer 01. Let's go ahead and write the code to go log in to the site. So we're going to write await page.goto. We're going to pass in our [practicesoftwaretesting.com](https://practicesoftwaretesting.com). And I know what page I want to go to. I want to go to the /auth/login. So rather than go to a page and clicking on a button,
>
> **[3:24](https://www.linkedin.com/learning/learning-playwright/handling-cookie-authentication-in-playwright?u=76281980&t=204)** I want to go directly to this page. Once we get to the login site, there are going to be three things we'll want to do. One is fill the email. We'll also want to fill the password. We're also going to want to click the Submit button. And then after that we'll want to save off the authentication file. So these are little placeholders for me so we can follow along. So the first thing we want to do is we want to do an await page, and let's see what our email element is. Let's go back to this page, use our selector. Looks like our data test ID is email. We're going to go ahead and check the password 'cause I think we can remember that. The password is going to be 'Password,' and the login button is going to be login/submit. All right, so await page.getByTestID, email, .fill.
>
> **[4:29](https://www.linkedin.com/learning/learning-playwright/handling-cookie-authentication-in-playwright?u=76281980&t=269)** And for the fill we want to pass in this email constant. So we're going to get the TestID, email, .fill. And we're going to pass in the email. We're going to do the same thing for the password. Await page.getByTestId, password. The action is going to be fill with the password. And again, this password is this Welcome01 constant that we created. And to click Submit, we're going to do an await page.getByTestId, login-submit. .click. It's important to have your open-close parens at the end of a function. So this should take me to a authenticated place. I'm going to go ahead and run this. Even though it says setup, it's still a test. We're just renaming it to setup. All right, so I'm actually on this page. I'll also want to go ahead and validate that the nav-menu contains Jane Doe. That is something we wrote in one of our other tests. So let's go ahead and paste that in here from our other test. I'm going to go ahead and remove our comments so things are clear. I do like to group my different steps with spaces where it makes sense. And then after our expect, this is where the magic happens. We're going to use await context.storageState,
>
> **[6:08](https://www.linkedin.com/learning/learning-playwright/handling-cookie-authentication-in-playwright?u=76281980&t=368)** and state is capital there, the S in State. And then we're going to pass in a path. And the path is going to be the customer01AuthFile. So the path is going to be this right here. So what this is going to do, when this runs, I believe it will, we might have to go manually create a folder, but we'll find out. I'm going to run this. And let's see if it actually creates the folder in the file. So it creates the folder, it creates the file. Within this file is all the cookie information that that browser session has. So one thing that we'll want to go ahead and do, 'cause you don't want to be committing this to a repository, is we're going to go ahead and update our gitignore file. So let's go ahead and click on that. Let's go ahead and add .auth/, here. so this doesn't get picked up and sent to [[GitHub]] or any of your repositories. 'Cause this is going to get updated and changed every single test run. Because we are using the setup spec file. And if you remember back to our Playwright config, we have the setup project that is going to run. And the Chromium project has a dependency on setup. Anytime we run new test, you'll notice that the setup project is going to run first and then the [[Representational State Transfer (REST)|rest]] of the test will follow. Now, we've been able to save browser context, but how do we use that in a test?
>
> **[7:41](https://www.linkedin.com/learning/learning-playwright/handling-cookie-authentication-in-playwright?u=76281980&t=461)** Let's walk through that. So we're going to go back to our home spec, and we're going to refactor this a bit to walk through an example. So in this test.describe section, we're just going to go ahead and note this, that this is homepage with no auth. So there's no authorization in these homepage tests. But we're going to go ahead and write some new tests that will have auth down below. So you can have multiple test.describe blocks within a single file. But each test.describe block is its own group. So we're going to come down to the bottom and we're going to create a new test.describe block. We're going to call the group Home page customer 01 auth. So this is visiting the homepage with the auth of customer 01. We're going to have our logic here. And within this describe block, this is where the magic happens. We're going to use a test.use, open parentheses, open bracket. We're going to type in storageState, and we're going to pass it a file name in a string. So .auth/customer01.json. We're going to save it. And this should apply the storage state. Let's go ahead and add the beforeEach block. Test.beforeEach, async. We're going to pass in page here.
>
> **[9:17](https://www.linkedin.com/learning/learning-playwright/handling-cookie-authentication-in-playwright?u=76281980&t=557)** Mess in the hash rocket. And we're going to do the await page.goto. We're going to pass in our website that we're using. So I'm just going to steal it from up here. Copy, paste. And for our specific test, I'm going to go ahead and space that down. We're going to check that customer 01 is signed. And we're going to pass the async. We're going to pass in page, 'cause we're going to be interacting with a page. Hash rocket syntax. We're going to say await expect page, getByTestId. We're going to do the nav-sign-in. And here. So this is, you know, pretty much the same exact test that we are doing up here on this, expecting it to be in. We're expecting the page to have this text. We're actually going to, down here, say we want to expect this not.toBeBisible. So with every assertion, you actually have this not clause. So you can say, "Is it visible or is it not to be visible?" So let's go ahead and run this, and make sure everything works properly. I'm just going to run this one describe block. So the auth setup is running. That's happening. And we're actually logged in, and this test passed.
>
> **[10:51](https://www.linkedin.com/learning/learning-playwright/handling-cookie-authentication-in-playwright?u=76281980&t=651)** So if we wanted to go ahead and, you know, add an additional check, we could say that getByTestId, nav-menu does contain Jane Doe as well. So we can have, like, a positive test, and a negative test in this. So let's go ahead and run these tests, and just make sure we didn't break these. So this is running through the login test, setting the auth, and then it's going through all the different tests and the non-authenticated describe block, and notice that we're not signed in. And all the tasks passed. Now, if we wanted to run all these together, one way to do that would be npx playwright test. And if you remember back to our [[CLI]] command, we can just pass in home.spec. And this is going to run all the tests together in the same file. A part of the same worker automation run, right? Looks like everything is passing. From here we have a pattern to follow, and we could create an admin setup step, a customer O2 setup step, or any other username and password combination we wanted to create.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (2), [[GitHub]] (1), [[Representational State Transfer (REST)|Rest]] (1), [[CLI]] (1)
> **Prerequisites:** setup (16)
> **Code Identifiers:** getbytestid (5), customer01authfile (2), storagestate (2), beforeeach (2), tobebisible (1)
> **CLI Commands:** make (2), find (1), npx (1)
> **Documentation:** spec (4)
> **UI Navigation:** go to (3), click on (1)
> **File Paths:** auth.setup.ts (1), customer01.json (1), auth/customer01.json (1)
> **URLs:** [practicesoftwaretesting.com](https://practicesoftwaretesting.com) (2)

#### [Visual testing in Playwright](https://www.linkedin.com/learning/learning-playwright/visual-testing-in-playwright?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/visual-testing-in-playwright?u=76281980&t=0)** - [Instructor] Another popular way to test web applications is through visual testing. The concept here is while the test is in a certain state, you take a screenshot of the page or certain elements on the page and save them as a snapshot. This snapshot is a part of the repository. It is used as a baseline image to compare future screenshots against. This can be a useful strategy to use depending on the application and context you're testing in. Let's build out an example of a visual test using the home.spec.ts we've been working on. We'll add two different visual tests within each describe block. The syntax is as easy as adding the .toHaveScreenshot with a name assertion to a test file. We'll add one for auth and one for no auth, so let's start at the top. We're going to create its own test block. (keys clicking) Test, we're going to call this "visual test", async. We're going to pass in ({ page }), we're going to do an await expect (page) .toHaveScreenshot, and we're going to give it a specific name, ("home-page-no-auth.png"); So I'm going to go ahead and execute this test, and we'll see what happens. So npx playwright test tests/home.spec.ts:8. So I'm going to run the test on line eight
>
> **[1:39](https://www.linkedin.com/learning/learning-playwright/visual-testing-in-playwright?u=76281980&t=99)** from the command line. So it's running up the setup. It's making sure that we have a fresh authentication. Alright, and we have a failure. The message here is, let's go see why it failed. Error: a snapshot doesn't exist, writing actual. So this test actually failed, but in the process of failing, it went ahead and created this snapshot for us, and this is going to be the new baseline. So if we run this test again, we'll just run the same thing, I would expect it would pass. Alright, it passed, so let's go ahead and add a visual test for the authorized describe block. So we'll add it down here. We'll say test. We're going to call it visual test, we're going to say authorized, and we'll pass an async, ({ page }) => We're going to do the same thing, await expect(page).toHaveScreenshot, and we're going to pass, this name is going to be ("home-page-customer01.png") It did not like that 'cause I did not type it right. Alright, now everybody's happy, and this should be png. Alright, so I'm going to go ahead and run 42,
>
> **[3:14](https://www.linkedin.com/learning/learning-playwright/visual-testing-in-playwright?u=76281980&t=194)** but rather than just running this and it failing, I'm going to show you a nice little command line that you can use. So npx playwright test If I pass in the tests/home.spec, I can pass in a new flag, --update-snapshots What this will do is it will regenerate snapshots for any of these assertions by default. So because this doesn't have one, it's going to create a new one, but it's not going to fail the test, and you can see it wrote that actual. So now we've got, this one is not authenticated, this one is authenticated. Doesn't quite look like Jane Doe actually rendered, but this is what it was captured. My guess is it would probably render right after everything loaded. So let's go ahead and let's just rename this one to the other one. I want to demonstrate a failure. So I'm just going to rename this. We're going to call this .old, and we're just going to pop this one into the new one. So this is the home-page-no-auth. That's what I want to run, and I want to demonstrate a failure and what that looks like. So we're back on test number eight, npx Playwright test tests/home.spec.ts, and we're going to run line eight. I'm going to expect this test to fail, and it's going to fail
>
> **[4:49](https://www.linkedin.com/learning/learning-playwright/visual-testing-in-playwright?u=76281980&t=289)** because the image comparison didn't match. It's a different snapshot, but let's see what that looks like in the Playwright report. So it's going through a second retry. Alright, so here's the Playwright report, and it looks like, ooh, this actually failed waiting on a timeout, but it looks like it eventually passed, and then our failure for the homepage spec, this failed twice, so it's very clear that it expected one thing, it received one, and there's a diff, there's a bunch of text up here, but if I scroll down, I can actually see a nice tool down here to see what are the differences. So it looks like all this stuff seems the same, and just the stuff that's highlighted in red and orange, those are the things that are different. So this is the actual, this is the expected, and you can see one has a sign-in button, one is missing the sign-in button. You can also look at these side by side, or there's also a nice little slider where you can actually see the differences manually checking this out. You've also got links to the files here, which can be really helpful, along with the video recording of what was visible. So we're going to not keep the snapshots, so we're just going to update all of our snapshots. Update snapshots, so this will get these files in a clean state.
>
> **[6:23](https://www.linkedin.com/learning/learning-playwright/visual-testing-in-playwright?u=76281980&t=383)** I might have to delete this 'cause it said .old, it's not going to get overwritten and we don't want to commit that. But everything is written, everything is passed. You may run into issues at some point where certain fields, maybe an ad block is actually not rendering, and you may need a way to hide that. So I'm going to walk you through one simple way to do that. To do that, we're going to go look at the Playwright documentation to have screenshot documentation, and you can see all the different arguments that you can pass in here. The specific argument that we care about is the mask argument. This is where we can specify specific locators that can be masked by default with a pink box, but you can also change the colors if you want. This makes this visual checking a lot more focused on the things you care about and the things that you aren't expecting to change. So let's go ahead and implement that in our two tests and we'll wrap this lesson up. So to do that, we are going to, at the end of our png, add a comma, an open object. We're going to type in mask, and then we're going to pass in the locator that we want to mask. So I know we want to mask: [Page.getByTitle] and it's going to be, I need to go find our browser so we can actually find it. So let me go ahead and run this test.
>
> **[7:59](https://www.linkedin.com/learning/learning-playwright/visual-testing-in-playwright?u=76281980&t=479)** It might fail, but I want the browser. Alright, I actually want to take this tool shop, this moving. This is what I care about masking. So let's go find what the locator is in this thing. So it looks like there is a title of practice [[Software Testing]] -tool shop. So if you all can see that this title is what I want to work off of. So I'm going to copy this text. I'm going to paste this in here, and I'm also going to add the same mask to the next page. So I'm just going to copy this into our other visual test. So it's going to be after this. Note that this needs a comma before this open bracket. And we're just going to run the test. We're going to update the snapshots one more time. What this should do is it should change these snapshots from a full picture to a pink box up here. So we'll see if it does that once they're running. I'm going to click up. It actually updated that one. It updated that one. I think we are in a good spot. As you're building out visual tests, one thing you'll want to do is ensure the browser is in a consistent state before you take the screenshot. One way to help ensure this is by adding in await page.waitForLoadState,
>
> **[9:37](https://www.linkedin.com/learning/learning-playwright/visual-testing-in-playwright?u=76281980&t=577)** and you've got a couple different options. I believe that network idle is a good thing to wait for. This waits for all of the network requests to complete before taking the screenshot, which typically by that time, all the elements on the page will load. So we're going to add that to the visual authorized test, and we're also going to add it to the non-authorized visual test. At this point, we can go ahead and recreate our snapshots. So let's update our snapshots, ensure everything is built, gets saved properly with our masks, and let's go ahead and run our test without updating our snapshots to ensure everything is still working properly. So here you can see the whole page loads. We've got Jane Doe, we've got the sign in. All the tools are loading, everything is happy. One caveat to note that if you are running visual tests in CI servers and they have different operating systems than the tests you're building the test on or generating the snapshots on, you will have to generate CI specific snapshots. There are ways to do that using docker containers that are available through the Playwright documentation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Software Testing]] (1)
> **CLI Commands:** npx (3), find (3), docker (1)
> **Code Identifiers:** tohavescreenshot (3), getbytitle (1), waitforloadstate (1)
> **Documentation:** spec (5)
> **File Paths:** tests/home.spec.ts (2), home.spec.ts (1)
> **Definitions:** is a  (3)
> **Warnings:** note that (2), caveat (1)
> **Tools:** command line (2)

#### [API testing in Playwright](https://www.linkedin.com/learning/learning-playwright/api-testing-in-playwright?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/api-testing-in-playwright?u=76281980&t=0)** - [Instructor] API stands for Application Programming Interface. Most modern websites are built with an API first approach where the business logic lives behind an API. A major feature that Playwright supports is the ability to interact with network traffic during a UI test, but it also provides tools to test APIs directly. What's nice about this is that you can use Playwright test framework for both UI and API test, all within the same repository. We'll write two API test examples, one for GET request and another for a POST request. So let's go ahead and create a new file. We're going to name it api.spec.ts. We'll do it in our tests directory, and we're going to go ahead and import Playwright the same way we have in the past. We're going to import test and expect from @playwright/test. This looks familiar. Now we're going to go ahead and create a test block, and we're going to make a test to get the [[Microsoft Products|products]] endpoint. It's going to be async. It's going to be open parentheses, open brackets. Instead of importing page, we're actually going to import request. This is going to be what is made to make the API request. We're going to add our hash rocket, and our open bracket,
>
> **[1:38](https://www.linkedin.com/learning/learning-playwright/api-testing-in-playwright?u=76281980&t=98)** and we have a test begun. But before we go too much further, let's go ahead and explore the site and get a full understanding of what we want to automate. So here's the site here. I've opened up dev tools by right clicking and clicking inspect. I've moved to the Network tab, and I've gone ahead and cleared any previous network requests. And I'm just going to refresh this homepage, and with our request filter down to XHR or [[Fetch]]/XHR, there's only a few requests in here, and I'm going to be looking for products. So this is an API call made to api.[practicesoftwaretesting.com/products](https://practicesoftwaretesting.com/products). And then there's some query parameters that are passed in. It's a GET request, and the response it got back was a 200. There's also some response headers, so these are headers that are received. And then request headers, these are headers that the browser sent to the API. One important thing to note is this URL is api.practicesoftwaretesting, whereas the main site is just [practicesoftwaretesting.com](https://practicesoftwaretesting.com). So when we create our URL, we're going to be using the api.practicesoftwaretesting. We can also see if there's a payload sent. So here we just see the query parameters, but if there was a request body, we could see that here. And then we've got two tabs to see the response.
>
> **[3:12](https://www.linkedin.com/learning/learning-playwright/api-testing-in-playwright?u=76281980&t=192)** One is Preview and one is Response. The Preview lets you kind of click through and you get to open up the things you want to look at. Whereas Response is just a big giant [[JSON]] object. So let's go ahead and codify this example and make a GET request to this URL. So I'm going to go ahead and copy this request URL. In this scenario, I'm going to create a variable, constant variable, called apiUrl. We're going to set it equal to the URL. I'm going to go ahead and remove the end here. And we're just going to add this as a part of our request. So the next thing we want to do is we want to set a variable for the response. So const response equals await. Here's where we're going to use request. And on request, as soon as I hit dot, you can see all the different options I've got available to request. In this scenario, we're going to make a GET request, and we are going to pass in the apiUrl, and we're going to pipe in the end of the URL here. So this is going to bring this down here and then push this as well. When we get the response back, the first thing you typically want to do is expect the response status to be what the response status should be. So let's go ahead and write the expect for that, expect response.status.
>
> **[4:48](https://www.linkedin.com/learning/learning-playwright/api-testing-in-playwright?u=76281980&t=288)** So this is a method that can be called on the response. It's going to be to, oh, I did not spell this right, response. This is going to be toBe 200. All right, so I should be able to run this and this should get a 200 response. Let's see if this passes or fails. All right, so it looks like it passed. So it did get a 200 response. And let's go ahead and just make another assertion. So with this response body, there's a lot of things that come back from the request URL, such as the JSON object that gets returned. But to get access to that, we're going to create another variable called body. This is going to be the response body, and we're going to set it equal to await. So we do have to await this, response.json. So what this is going to do is going to set the body variable to this response. I'm going to go ahead and just do a console.log body. So we will log this out, but the main reason I want to do this is just so I have a place to put a break point, 'cause we're going to debug this test and really look inside this body and see what exists. So I'm going to right click on the test. I'm going to click Debug Test. It is going to run the setup file, because that is a dependency even though we're not using it in this specific test.
>
> **[6:22](https://www.linkedin.com/learning/learning-playwright/api-testing-in-playwright?u=76281980&t=382)** And now we should have access to the body. So if I look over here in my debug window, I see this body variable is actually set to the JSON response that's returned. So let's come up with a few things we can assert on. One could be that this, it looks like this is an array that will be returned in this data object. It looks like it has a length of nine. Looks like there's also a value total of 50. Let's go ahead and write assertions for those two things. So I'm going to go ahead and let this finish. All right, and we're going to remove this console.log. We don't need that anymore. We're going to go ahead and make our assertions. So the first is going to be expect body. So that's our response. The first thing was data, and we're going to say the length of the data. So data was an array. We're going to say the length of that, to be nine. So we're expecting the data to have nine things within the array, and we also want to expect the body that total to be 50.
>
> **[7:45](https://www.linkedin.com/learning/learning-playwright/api-testing-in-playwright?u=76281980&t=465)** Now if these were strings, I could assert that they're strings. These did appear to be numbers. So let's just run this and confirm. All right, so two passed, everything looks good, our assertions passed. Let's just make sure that our assertions were actually checking something. So I'm going to make it fail. All right, it failed, it received 50, it expected 51. So that tells me that we've got a decent test written. So let's go ahead and write a second test. So this is test for GET /products. Let's go ahead and write a second test for posting the user login. So let's just walk through that in the UI. So I'm going to log out, going to inspect the page, pull up the Network tab. We're going to view the Fetch/XHR. We're going to go ahead and log in. But before we click Login, I want to go ahead and clear the history. I also want to click on Preserve log. This will preserve any of our API requests. So I'm going to type in, customer@[practicesoftwaretesting.com](https://practicesoftwaretesting.com). The password will be welcome01. We're going to click Login, and we should have a login API recall. So this is a POST to api.[practicesoftwaretesting.com/user/login](https://practicesoftwaretesting.com/user/login). So just to make things simple, I'm going to go ahead and copy our first test, and we're just going to adjust it.
>
> **[9:21](https://www.linkedin.com/learning/learning-playwright/api-testing-in-playwright?u=76281980&t=561)** I'm going to change this a bit. And this is going to be a POST, and it's going to be a post to /users/login. I really like to make the titles the actually path that we're testing, that makes your test really readable when you're running them back and you know, trying to figure out what tests failed. So this apiUrl, it's going to stay the same. As you can see we could probably even maybe move that to a before block now that we're starting to see some repeated code in some of our tests. And we're going to keep a response, but our request is not going to make a GET. It's actually going to make a POST. And we're going to make a POST call to the URL plus users/login. Now this one is going to be a little different, because we actually want to post a body into this request. So let's go take a look at the actual request that was made. There's a POST, the payload that it sent includes this request body that has an email and a password. So it's really just this object here. If I click View source, this is probably an easier way to see it. So email with a username, and then a password with a password. So for the body, the syntax for that is we're going to go to our test title. We're going to add a comma, we're going to do a open bracket. Within that open bracket, we are going to add data. This is going to be data that we are posting to the server, and we're going to pass in email,
>
> **[10:54](https://www.linkedin.com/learning/learning-playwright/api-testing-in-playwright?u=76281980&t=654)** and we're going to have a string. We're going to add the actual email address here, and then we're going to pass a password. This is going to be welcome01. So the string is going to be customer@[practicesoftwaretesting.com](https://practicesoftwaretesting.com). All right, so we've got our email, we've got our password, we've got the end of our data block. At this point, we just want to make sure that we have some kind of assertion, right? So I can run this, but if we're not asserting on the response, this API request could fail and we would never know. So let's go ahead and add an expect response.status toBe 200.
>
> **[11:39](https://www.linkedin.com/learning/learning-playwright/api-testing-in-playwright?u=76281980&t=699)** Maybe it's going to be a 200. Let's see. Let's see what the response body was. Yeah, the status code is a 200. So that's what we're going to expect. And we can go ahead and set the const body equals to await response.json, so we can assert on the body. Let's actually see what gets returned. Let's see, this is what we send, and it looks like we don't even get a response, at least not in this view, but let's go take a look. Maybe we will just debug this and see if anything does come back, console.log body. All right, so I'm going to add another break point here, and we're going to debug this test. We're going to see just what comes back in the response body. All right, so it looks like in the body, this wasn't visible in Chrome, which is interesting, but it looks like we have an access_token, we have an expires_in 300, and there's a token_type bearer. So that's what's coming back. So one easy test we can add to this could just be to make sure that the access token actually exists on the response. So to do that, we're going to do an expect. We're going to say body.access_token, and we're going to say toBeTruthy. So basically saying this body should have an access token. If that's true, pass. If not fail. So let's go ahead and run this test.
>
> **[13:15](https://www.linkedin.com/learning/learning-playwright/api-testing-in-playwright?u=76281980&t=795)** Just make sure everything passes. All right, everything is good. For more information, the Playwright docs provide even greater detail into the possibilities of what you can do when testing API requests with Playwright.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JSON]] (5), [[Microsoft Products|Products]] (4), [[Fetch]] (2)
> **Env Vars:** api (12), url (8), post (6), xhr (3), json (3)
> **CLI Commands:** make (16)
> **Code Identifiers:** apiurl (3), tobe (2), access_token (2), expires_in (1), token_type (1)
> **API Endpoints:** get  (5), post  (2), post
 (1)
> **Definitions:** is a  (4), is an  (2)
> **URLs:** [practicesoftwaretesting.com](https://practicesoftwaretesting.com) (5)
> **File Paths:** response.json (2), api.spec.ts (1)

#### [Automating the right things with Playwright](https://www.linkedin.com/learning/learning-playwright/automating-the-right-things-with-playwright?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/automating-the-right-things-with-playwright?u=76281980&t=0)** - [Instructor] As we've learned about a lot of the core functionality that Playwright offers, it's easy to get carried away and want to automate all the things. I mean, that's probably what your manager wants, right? Well I believe it's more important to automate all the right things. When I say all the right things, I mean to utilize Playwright for its strengths and utilize other tooling for their strengths. Let's take a look at some of the different types of [[Test Automation]] that can be done. You've got unit or component tests, you've got API regression tests, you've got UI regression tests, smoke tests, performance tests, and security tests. Ask yourself, which of these areas do you believe Playwright is best used for? As I hope you've seen from the video so far, Playwright really shines when doing automated API, UI regression, and smoke testing. Let's take a look slightly deeper at a model of a login flow and what happens from a technical perspective. This is an example of what happens within a web application when you try and log in. From the highest level, you're interacting with the UI, entering the username and password. [[JavaScript]] on the page fires when you click submit. This will send an API request to the API server, potentially with a CSFR token. This may talk to a security service to ensure that you're able to connect to the database and validate the CSFR token.
>
> **[1:34](https://www.linkedin.com/learning/learning-playwright/automating-the-right-things-with-playwright?u=76281980&t=94)** Then a call is made to the database to validate the username and password that they're valid. At that point, an access token could be granted, giving you access to the site that would get returned in an API response, in a response header, or in a body. Typically, the JavaScript on the UI would get this authenticated response and load the authenticated homepage along with any additional information that would be retrieved from the API. Where we can focus with Playwright are the interfaces that are exposed to test with, specifically the UI, or as I like to say, the DOM to the database, or through the API all the way down to the database. Playwright excels at interacting with both of these in testing.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[JavaScript]] (2), [[Test Automation]] (1)
> **Env Vars:** api (7), csfr (2), dom (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Challenge: Create three automated tests](https://www.linkedin.com/learning/learning-playwright/challenge-create-3-automated-tests?u=76281980)

> [!transcript]- Transcript
>
> **[0:05](https://www.linkedin.com/learning/learning-playwright/challenge-create-3-automated-tests?u=76281980&t=5)** - [Instructor] Now that we've covered a lot of the basics with Playwright [[Test Automation]], now it's your turn to write some code. For this challenge, I want you to create an automated test that adds an item to the shopping cart, goes through the checkout process, and when paying, selects buy now, pay later. I also want you to create a visual test to the checkout flow to validate the final payment screen. And lastly, create an API test against the endpoint slash [[Microsoft Products|products]] slash unique ID, ensuring the correct product details are returned in the API response. I'll leave you with a few hints to help you get started. Back in video six, where we walked through setting up the storage state, this includes an authenticated user for one of the customers. Using this will save you a few steps in your test. Also, for the API test, the product IDs get automatically updated every two hours due to the database resetting. This is a practice test site. You'll need to find a way to reliably get the product ID to make the API call in the test. With that, go ahead and try on your own and I'll see you in the next video where I'll walk through how I would solve the challenge.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Test Automation]] (1), [[Microsoft Products|Products]] (1)
> **Env Vars:** api (4)
> **CLI Commands:** find (1), make (1)
> **Cross-References:** in the next (1)
> **Definitions:** is a  (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [Solution: Create three automated tests](https://www.linkedin.com/learning/learning-playwright/solution-create-3-automated-tests?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/solution-create-3-automated-tests?u=76281980&t=0)** - [Instructor] Hopefully you were able to solve the challenge or get very close on your own. I'll walk through my solution and explain my whys on the decisions I made when writing the code. You also may pick up on a few features we haven't talked about as well. So I started off by creating a challenge.spec.ts, and I have two different describe blocks. So I'm going to go ahead and collapse these down. So I've got a describe, this is the, "Checkout Challenge," and a describe, this is the, "API Challenge." We'll start with the, "Checkout Challenge," first. The first thing I do within the describe block is I go ahead and use the storageState from the .auth/customer01. This is what gets generated in our setup file. I then have a beforeEach block that goes to the practicesoftwaretesting page So if I add more tests, I've already got the browser going there. And I've written my test down here. I'm passing in headless, which we'll use a little later. But page is the main thing we are interacting with the page on. I'm going and finding the Claw Hammer. I'm adding it to cart by clicking it, and then I go ahead and make an assertion to validate that the cart quantity is 1. So I'm going to go ahead and run this, debug this,
>
> **[1:35](https://www.linkedin.com/learning/learning-playwright/solution-create-3-automated-tests?u=76281980&t=95)** so we can see it, and we'll walk through the [[Representational State Transfer (REST)|rest]] of it in debug mode. So there's my setup file. Now we are running and we are paused on this cart-quantity. So let's see if we can move this to the right, and we can just look at these together. All right, so you can see up here that we're validating that that 1 does exist on the cart. All right, we're going to go ahead and step through. The next thing we're going to do, is click on the nav-cart. And from here, there's not really any action to do. I could've made some assertions, but I decided we'll go ahead and click to proceed. So I'm just using TestID proceed-1, TestID proceed-2. So this is the sign in page. The next is the billing address. Here I'm actually validating that the step-indicator filter has a 2, and I'm validating that it has the CSS background-color, and this is green. So I'm basically validating that when I'm on the Billing Address page that this is actually green. It's a little deep for an assertion, but it just kind of shows some other examples of things you can assert on along the way. This next section, I'm actually just filling out the address information.
>
> **[3:08](https://www.linkedin.com/learning/learning-playwright/solution-create-3-automated-tests?u=76281980&t=188)** So we're going to just click through this. You can see I click through on the left hand side, and I went ahead and clicked proceed-3. Here, I actually do a negative test. So by default, this Confirm button should not be active, it should be disabled. I saw that through exploring the test. So I want to go ahead and just make an assertion that, "Hey, this is disabled right now." And then we're going to go ahead and select our payment option. So I am basically getting the payment-method ID. And then the selectOption, I'm just passing in the string, "Buy Now Pay Later," and it's going to go select that out of the dropdown. All right, once that's set, there's actually a new dropdown that comes by and here is where we're going to set it to 6 Monthly Installments. Next, we're going to click Finish, which it is now enabled, and we're going to expect this help-block to show, Payment was Successful. So I could change this and ensure it fails and run it again. This next section is where we use headless. So what headless does, let's see if we can, if it will tell us what it does by hovering over it. I guess it doesn't want to right now. Maybe it will up here. It does not, 'cause the test is running.
>
> **[4:40](https://www.linkedin.com/learning/learning-playwright/solution-create-3-automated-tests?u=76281980&t=280)** What headless does is it lets us know if we're running the test with the browser open or with the browser closed and behind the scenes. This is a ternary operator, so this headless currently is false, because I've got the window right here, and when it is false, it's going to skip this test step, and it's just going to log the console. We're running in Headed mode, no screenshot comparison. So this is my Screenshot Comparison Test. I actually just made it a part of this test. I just create an additional test.step Step one important thing is you do have to await any test.steps that you create, but you can give it a nice title and you can kind of wrap different logic within there. And that's where I'm basically just doing the expect the page to have a Screenshot, and then we're creating a snapshot, and then we are masking the Toolshop logo up here. And again, by default, this is not going to run when headless is false, but when it is running headless, it will capture the screenshot and do a comparison. The reason I have it like this is you might have recognized the screenshot comparison is different if you're running in headless versus running in a browser. Specifically, if you're using the VS code extension to run your test, opening the browser, your visual comparisons are going to be pixels off and it's going to be frustrating. So this is the approach I took in just making my test less flaky.
>
> **[6:15](https://www.linkedin.com/learning/learning-playwright/solution-create-3-automated-tests?u=76281980&t=375)** So let's go ahead and finish stepping through this, and we're going to run the test past, as we can see back here. All right, so that is the first two challenges. The next challenge, which is a little more complicated, is this, "API Challenge." Let's go ahead and minimize this. Open up the, "API Challenge." And I took somewhat of a different approach here. So let me go ahead and just walk through some of my thoughts through the website. So I wanted to validate that Thor hammer, we've already looked a little bit at that API. I wanted to get the ID and then I wanted to go make a GET request against that ID. So specifically, if I inspect this page, I go to Network, I come over here and type in Thor hammer, (keyboard clicks) click Search. I'm going to make a API request. It's also going to show up on the front end here. It's going to return this data. And I wanted to get this ID and go directly so that there's another API request that happens when you click specifically on an item. It makes this request, which is APIpracticesoftwaretesting/[[Microsoft Products|products]]/id. So I'm going to make two API calls in my test.
>
> **[7:50](https://www.linkedin.com/learning/learning-playwright/solution-create-3-automated-tests?u=76281980&t=470)** The first one to get the ID. The second one is where we're actually going to do the assertions. So let's just walk through that here. So I've got a debug point here. And as we start walking through this. All right, so we've got our debug field here. So the first thing we're doing, we're setting our API URL. This is a getProductResponse variable. And we're making our first request.get to the search the Thor hammer. And what we're saving is this is the productBody we're getting. So there's a data array and then there's an ID in here. So I'm basically creating a new variable called productID and I'm setting it equal to the productBody.data. The zero is like the first item in the array .id. So I'm trying to set this ID to this variable. So as we walk through that, you can see now, if I hover over that, that value is set. Now I'm able to go ahead and make my second API call. I find that saving the response variable name is useful for the final API response that you're making an API request on. You can really use any name you want, but I've just found that to be a good pattern when I'm writing API tests. So we're going to set response equal to the post,
>
> **[9:24](https://www.linkedin.com/learning/learning-playwright/solution-create-3-automated-tests?u=76281980&t=564)** and then if we look over here, we're actually passing in the productID to the get request. So we'll go ahead and step through these. We're going to expect the response is 200. We're going to set the body to response body. So let's go look and see what information is here now. So we've got Brand, we've got Category, we've got a lot of information in here. And we're basically just validating that these things are false.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Representational State Transfer (REST)|Rest]] (1), [[Microsoft Products|Products]] (1)
> **Env Vars:** api (12), css (1), url (1)
> **Code Identifiers:** productbody (2), productid (2), storagestate (1), beforeeach (1), selectoption (1)
> **CLI Commands:** make (6), find (1)
> **UI Navigation:** dropdown (2), click on (1), go to (1)
> **Definitions:** is a  (3)
> **Prerequisites:** setup (2)
> **File Paths:** challenge.spec.ts (1)


### 3. Maintaining Playwright Tests

[↑ Back to Table of Contents](#table-of-contents)

#### [Introduction to maintaining tests with Playwright](https://www.linkedin.com/learning/learning-playwright/introduction-to-maintaining-tests-with-playwright?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/introduction-to-maintaining-tests-with-playwright?u=76281980&t=0)** - [Instructor] From my experience, the time maintaining an automated test is close to equal of the time it takes to create a specific test. Writing the test is only the first step in the automation journey. Once written, the test is there to provide the testers and developers on the team feedback automatically. It's still up to us to take that feedback, the passing or the failing of a test and doing something with it. Typically, most of this maintaining time is looking into failed tests. I found that failed tests typically fall into four different categories: A bug. The code that we are testing is broken. A bug was introduced that failed our assertion or prevented automation actions from occurring. An intended change. The code that we are testing has changed. A change to the system was made, but it was intended, and we need to go update our automated tests to account for it. Before we go too much further, flaky pie crust is good. Flakey test? Not so good, they're bad. Let's define what a flakey test is. This means when your tests pass sometimes and other times they fail, regardless of the state of the website that's being tested. Typically, the test doesn't follow best practices or web first assertions like you're making an assertion for a value that may not be loaded on the page yet,
>
> **[1:34](https://www.linkedin.com/learning/learning-playwright/introduction-to-maintaining-tests-with-playwright?u=76281980&t=94)** or the test doesn't account for certain actions within the webpage, like a page is redirecting or content that dynamically loads after scrolling down a page isn't there yet to assert against or interact with. Or it could be a test is using test data, which causes a failure, something like an invalid email address or an invalid phone number. It also could be a test that may rely on some specific configuration or state of the website that another test might have changed, and then there's flakey infrastructure. The infrastructure that the code is running on is having issues. This could mean the code is perfectly fine. The logic in the code is fine, but the server's crashing or is causing some kind of network errors. Some examples are the site may be under too much load and it just crashed. It could be the database responses were taking longer than 30 seconds and our tests just timed out and it failed the test. This could be a bug too, so don't just assume infrastructure is your problem. It could be a domain name that expired for a test site, et cetera. In each of these areas, it'll be important to be able to review and inspect failed tests and interpret the results. We'll be exploring Playwright's rich feature set that will help you maintain your test as your automation project grows.

> [!info]- Semantic Content
>
> **Speakers:** - [instructor] (1)

#### [Playwright screenshots, videos, and reporters](https://www.linkedin.com/learning/learning-playwright/playwright-screenshots-videos-and-reporters?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/playwright-screenshots-videos-and-reporters?u=76281980&t=0)** - [Instructor] As you execute Automated Test, it's critical that you inspect the results of each run. By default, Playwright has many built-in Test Reporters. In this video, we'll walk through a few of the different options. A link can be found in the playwright.config near the Reporter's option. So here is Reporter and we can follow this link and you can see here's all the different built-in Reporters we have access to. Before we get going, let's go ahead and update our screenshot to on. This is going to be temporarily. And let's update our video to on, so we can actually see what these look like in the Reports. This will ensure that every test this run, if it's passed or failed, we'll have a screenshot and a video for our testing purposes. We'll save our previous settings back at the end of the video. Once the config is saved, let's run [[npm]] run test. (keyboard clicks) Let's hope there's no failures. Their test run is complete and I have this command, npx playwright show-report to view the [[HTML]] Reporter. (keyboard clicks) So let's run that command. You'll notice this opens up a HDP server, and I am able to quickly view any of the tests.
>
> **[1:36](https://www.linkedin.com/learning/learning-playwright/playwright-screenshots-videos-and-reporters?u=76281980&t=96)** I can search by Tags or by Projects very quickly.
>
> **[1:45](https://www.linkedin.com/learning/learning-playwright/playwright-screenshots-videos-and-reporters?u=76281980&t=105)** Here's the first Tag. And I can also click into each of these. So let's go find the, "Checkout Challenge." I can see all the different test steps. I can see the screenshot of where the test completed. I've got an option to View Traces, which is also very useful, and we've got the video, so I can actually see what was happening on the screen during the test. By default, the HTML Report is saved to the playwright-report folder. This folder can be uploaded to an S3 bucket or any kind of web server to be viewable for any of these single test runs. Let's go ahead and add a second Reporter List. In order to add the List Reporter, we have to convert this string to an array of arrays. This is how Playwright supports multiple options in the config file for the Reporter. So let's convert this. We're going to have two arrays. We've got the first array, and we're going to add the List Reporter, we've got to close this out. Don't forget to add your comma, and let's go ahead and run the same command, npm run test. As the tests are running, you can see the List Reporter in action. As the test runs, you've got the output of what's complete. You've got the green check mark on the left.
>
> **[3:17](https://www.linkedin.com/learning/learning-playwright/playwright-screenshots-videos-and-reporters?u=76281980&t=197)** You've got the time on the right. I find this is a very useful Reporter to use when you're either debugging tests that are running in parallel or with multiple workers, and also to visualize what tests are running in a CI environment. As you can see, there are more details that get presented right there within the command line. This Reporter can be really useful when debugging tests locally or running tests in CI. You can watch the progress in [[Real-Time]]. Let's open up the Test Reporters Documentation and look at a few of these other Reporters. Another popular Reporter is the Blob Reporter. This Reporter allows you to easily manage parallel runs when running in a CI environment and combine the Reports back together. There's also the [[JSON]] Reporter. This Reporter provides JSON output of the test run. Another popular option is the [[JUnit]] Reporter. This is one of the most Standardized Test Reporter Formats in the testing industry. These Reports can be imported into many different tools. You can also build your own Custom Reporter, or you can use an existing Third Party Reporter that exists. There are many, many different options to meet whatever needs you have. Feel free to take some extra time after this video and explore some of the other built-in and Third Party Playwright Reporter options on your own. Before we move on, let's go ahead and set our video
>
> **[4:53](https://www.linkedin.com/learning/learning-playwright/playwright-screenshots-videos-and-reporters?u=76281980&t=293)** and screenshots back to their defaults. We'll leave the Reporter Test there, so I'm just going to revert this change and revert. It looks like both of those reverted, and we've still got our list. This way we're keeping our HTML Reporter size small.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[HTML]] (3), [[npm]] (2), [[JSON]] (2), [[Real-Time]] (1), [[JUnit]] (1)
> **Env Vars:** html (3), json (2), hdp (1)
> **CLI Commands:** npm (2), find (2), npx (1)
> **File Paths:** playwright.config (1)
> **Tools:** command line (1)
> **Definitions:** is a  (1)
> **Best Practices:** don't forget (1)
> **Speakers:** - [instructor] (1)

#### [Playwright trace viewer](https://www.linkedin.com/learning/learning-playwright/playwright-trace-viewer?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/playwright-trace-viewer?u=76281980&t=0)** - [Instructor] In the previous video, we showed off the [[HTML]] reporter. This next session will be somewhat of a review from what we saw when we discussed the Playwright UI mode. We're going to take a look and explore the trace viewer that is built into the HTML report. All right, let's start by running our test. We're going to use the mpm run test:report command. What this is going to do is run the test command and then directly after it, go ahead and show the Playwright report all in one command. All right, let's choose the Create customer 01 auth, this is the setup test, and let's go ahead and scroll down to the traces section and click on this image down here. There are a few things I want us to observe while we're here. First, let's look at the console tab. In my view, I can see there are two console error messages that were produced while this test was running. This information can be really useful. For some reason, you're getting a [[JavaScript]] error in the code as this error will be persisted as long as the HTML test report exists. Let's back out of this test and go look at one of the API tests, the GET/[[Microsoft Products|products]] test. Let's go look at the trace, and here I want us to view the network tab. In this network tab
>
> **[1:33](https://www.linkedin.com/learning/learning-playwright/playwright-trace-viewer?u=76281980&t=93)** we have access to the same thing we have when using Chrome dev tools. I can click, I can see the request, I can see the response, I can see the response headers, and I can even see the response body. Having access to this after a test has run to be able to go inspect the state of what the system was in while the test was running is a crucial tool. It's going to give us a ton of observability into our [[Test Automation]]. What's nice about this network tab as well is that it shows all the HTTP requests that were made from within a Playwright test, not just the URL that you're testing against, but any third parties that may have requests as well. One last thing I want us to review is let's open the checkout spec. This is the spec that we did for the challenge. If you didn't complete the challenge, you can download the repository and check out the branch 03_03B. Once you've got that open, let's go click on the traces, and by selecting the image within the Trace Viewer section up here at the top, you can hover over each step and actually see the test completing through all the different steps. This is really useful if you're not sure what was happening on the screen when a test failed.
>
> **[3:05](https://www.linkedin.com/learning/learning-playwright/playwright-trace-viewer?u=76281980&t=185)** And all this information is also updated as each step is clicked into in this actions section. One other tip is you can also click on this target icon in the top left and also select different DOM elements. This is very similar to the UI mode. This is also very helpful if you need to quickly interact with different DOM elements during a test step.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[HTML]] (3), [[JavaScript]] (1), [[Microsoft Products|Products]] (1), [[Test Automation]] (1)
> **Env Vars:** html (3), dom (2), api (1), http (1), url (1)
> **UI Navigation:** click on (2), scroll down (1), open the (1)
> **Cross-References:** previous video (1), we discussed (1)
> **Documentation:** spec (2)
> **Exercise Files:** download the (1)
> **Definitions:** is a  (1)
> **Analogies:** similar to (1)

#### [Scaling Playwright tests](https://www.linkedin.com/learning/learning-playwright/scaling-playwright-tests?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/scaling-playwright-tests?u=76281980&t=0)** - [Instructor] As your Playwright [[Test Automation]] suite grows, you'll find the need for greater organization and maintainability as needed within your test. You'll also need to have some strategies on hand in order to abstract away duplicate code and simplify your code as much as possible. Let's take a few minutes and add some organization to our test today. Let's go ahead and create a new folder called API within tests, we can have all of our API tests live in this directory. Let's move our API spec into that folder. You can do that by dragging and dropping it. Now we will go ahead and create folders for the other tests. Checkout, homepage, and login, and let's go ahead and drop each test in their folders.
>
> **[1:02](https://www.linkedin.com/learning/learning-playwright/scaling-playwright-tests?u=76281980&t=62)** All right, so we've got our login. We'll go to login. I'm going to go ahead and say, don't ask me again. Yes, update the imports. Homepage will go to homepage. This can be deleted. We can go ahead and delete this too. We're cleaning house. And then our challenge is currently broken up into, I've got one big test file for multiple things. So the best thing to do here would be to go ahead and break this out to two different files. So we're going to go ahead and add this first one to checkout. So I'm going to create a new file. I'm going to call this the checkoutChallenge.spec.ts and we'll just copy and paste some code over there. So I'm going to go get this whole first test. We'll dump that here and we'll go back to our challenge spec. I'm going to leave the import, I'm going to take out everything from the first describe block down, and we'll go ahead and rename this to our API challenge. And we'll drop this in the API folder. So now we've got things a little more organized. One thing to note is you will need to update the snapshots
>
> **[2:40](https://www.linkedin.com/learning/learning-playwright/scaling-playwright-tests?u=76281980&t=160)** to the right folder as well. So I'm going to grab these homes, drop it in the homepage folder. So these need to live together. If you do update the home.spec file name, you will need to also rename folder and file names for the snapshots. We'll also go ahead and drop this in the checkout folder. And again, since we rename this, we'll actually just rerecord these. We're not going to worry about renaming 'em. All right, let's go ahead and run our test and regenerate any snapshots, npx playwright test --update-snapshots.
>
> **[3:30](https://www.linkedin.com/learning/learning-playwright/scaling-playwright-tests?u=76281980&t=210)** All right, looks like we've got updated snapshots, everything passed, and we also don't need this test examples folder. This comes by default with Playwright. So we're going to go ahead and clean this up as well. One thing we haven't discussed is the ability to abstract away complex logic from your test files into other files. This can be done in Playwright using functions that can be used across any of your tests utilizing a page object class or utilizing Playwright fixtures. We won't have time to explore all these concepts in this course, but we will look at one example of what it would look like to implement a page object in our code base. So I've created the pages folder up here, and I've created a login folder within that. And I've created a login page, page object model. This is our page object class. Within the page object file, I've created locators, and below the constructor with the locators, I have some different functions. So I've got a go-to and a login using the different locators. For example, this go-to function will navigate, it will do a page, do go to, and it will go to practice [[Software Testing]], plus off login. The login function, when calling that, I can pass in an email and a password, and it will input the email in the email field,
>
> **[5:04](https://www.linkedin.com/learning/learning-playwright/scaling-playwright-tests?u=76281980&t=304)** the password and the password input field, and it'll go ahead and click the login button for us. I could also add an assertion to the end of this just to confirm that the page loaded and things passed. It's another option. So let's see what this looks like in the Playwright test. The login test. So I've gone ahead and updated the original test to be login without page object. I didn't change it outside of that. I did import the login page. So this is what it would look like to import this class into your file. And I've gone ahead and written this test down here. So this is the same exact test as what's above. We're going to log in with page object. We're going to first set the variable login page to the login page class passing in the page into the class. At that point, we call the goto function. Then we're manually inputting the password input field, we're clicking the login button, and then we have an assertion. It's possible that I could replace these three fields with an await loginPage.login and I could pass in this string, comma, welcome01.
>
> **[6:32](https://www.linkedin.com/learning/learning-playwright/scaling-playwright-tests?u=76281980&t=392)** And this would do the same thing as that. So I could comment this out, I could go to the login page, I could then log in with this username and password, and let's just run this and ensure everything works. All right, everything passed, things were able to log in. As you could see, the login page is much easier to read. It's a little smaller, depending on how you want to organize and structure your test. I'm going to go ahead and leave this a little more verbose for now. Now that we have this example, another thing to consider is the ability to run our test automation scripts across multiple environments. A good pattern I've seen used is using a .env file and updating your test with a process.env.URL. So let's walk through what that would look like, implementing it within the page object. So the first thing I would want to do is go to the playwright.config, and you can see at the top of the playwright.config, I've got three lines commented out. This is importing the dotenv node package and the path from [[JavaScript]]. And then it's initializing the dotenv, looking for an .env file within our file directory. So I'm going to go ahead and uncomment this out. You'll notice that this is currently red because it says I can't find this module.
>
> **[8:04](https://www.linkedin.com/learning/learning-playwright/scaling-playwright-tests?u=76281980&t=484)** That's because we need to install the module. So I'm going to go to my terminal. I'm going to type in [[npm]] install dotenv --save. What this is going to do is install the dotenv plugin. It's going to update my package.[[JSON]] file. So you're going to see a new dependency added. We're going to do a save dev. So by typing npm install dotenv --save-dev, this installed the dotenv file under my dev dependencies. This also installed the dotenv information into my node_modules. The last thing I need to do to get this to work is I need to create a new file named ".env" at the root directory. And we're going to go ahead and set a URL. So I'm going to just, in capital letters, URL=[https://practicesoftwaretesting.com](https://practicesoftwaretesting.com)
>
> **[9:14](https://www.linkedin.com/learning/learning-playwright/scaling-playwright-tests?u=76281980&t=554)** and now I should be able to go to my page object for login, and I should be able to replace this string with process.env.URL. Now, this should pull this in for any of our testing on the login page. So let's go run the login spec with the page object. Let's just make sure everything passes. Voila. As you build out your Playwright test automation projects and begin to scale, you'll find this and other methods to be able to abstract and reuse code extremely useful. In doing so, will cut down on your maintenance cost as well. My advice is, whatever strategies you choose, pick a pattern, stick to it, and as long as it's working for you and your team, don't worry about what everyone else is doing.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[Test Automation]] (3), [[npm]] (2), [[Software Testing]] (1), [[JavaScript]] (1), [[JSON]] (1)
> **CLI Commands:** find (3), npm (2), npx (1), node (1), go run (1)
> **Env Vars:** api (5), url (4)
> **UI Navigation:** go to (8)
> **File Paths:** process.env (2), playwright.config (2), checkoutchallenge.spec.ts (1), package.json (1)
> **Documentation:** spec (5)
> **Prerequisites:** install (4)
> **Code Identifiers:** checkoutchallenge (1), loginpage (1), node_modules (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/learning-playwright/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/learning-playwright/next-steps?u=76281980&t=0)** - [Butch] Congratulations for completing this voyage and learning Playwright with me. As you continue your learning journey, I want to encourage you to find ways to use the skills you've learned to build automated tests with Playwright. Through this course, we only touch the tip of the iceberg of the functionality that the Playwright team has built in this tool. I want to encourage you to go forth and read the official docs. When I first started learning Playwright, I spent around one hour a day just reading the official Playwright docs, and over a few weeks, I had a huge advantage. Even though I didn't memorize everything, I had an idea of Playwright's capabilities, and I found myself remembering where to look when I did face a problem. You can also check out the Playwright YouTube channel. You can also join the Playwright Discord, connect with others, ask questions, find articles and video content, and eventually, you'll be able to help others with their problems. You can also check out the Playwright [[GitHub]] repo. Here, you can subscribe to releases, report bugs, or even request features, or vote on feature requests. The developers and the Playwright team, they live here. This is where they prioritize their work. You can also subscribe to my newsletter at [PlaywrightSolutions.com](https://PlaywrightSolutions.com). As I write, I am trying to always provide actionable solutions,
>
> **[1:34](https://www.linkedin.com/learning/learning-playwright/next-steps?u=76281980&t=94)** things that you can replicate with a test repo, or with test code on your own machine. You can also connect with me on [[LinkedIn]]. I love hearing stories about people learning and growing, so please do reach out, and let me know if this course was helpful to you. I'll leave you with this. Stay curious, and never stop learning.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [[GitHub]] (1), [[LinkedIn]] (1)
> **CLI Commands:** find (2)
> **Tools:** discord (1), github (1)
> **URLs:** [playwrightsolutions.com](https://playwrightsolutions.com) (1)
> **Documentation:** official docs (1)
> **Exercise Files:** github repo (1)
> **Speakers:** - [butch] (1)


## Instructor

- [[Butch Mayhew]]

## Resources

- Exercise files available
- [GitHub Repository](https://github.com/LinkedInLearning/learning-playwright-5911873)

## Skills Covered

- Test Automation Tools
- Automation

## Path Context

### In [[Master Test Automation with Playwright]]
**1 of 4** | [[Playwright Essential Training- Abstractions, Fixtures, and Complex Scenarios]] →

### In [[Getting Started with Software Testing]]
← [[Learning Selenium- Structure, Scale, Run, and Optimize Automated Tests]] | **6 of 12** | [[JMeter- Performance and Load Testing]] →

## Appears In

- [[Master Test Automation with Playwright]]
- [[Getting Started with Software Testing]]

## Related Courses

_Courses sharing skills:_

- [[Jira Administration- Automation]] — Automation
- [[Ai Agents For Everyday Professionals Simple Automations To Speed Up Your Work No Code Required]] — Automation

---

[↑ Back to top](#)