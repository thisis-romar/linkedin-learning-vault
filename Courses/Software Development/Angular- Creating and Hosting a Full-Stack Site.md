---
type: course
cssclasses:
  - lle-course
  - lle-level-intermediate
slug: angular-creating-and-hosting-a-full-stack-site
url: "https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site"
duration_minutes: 261
duration: 4h 21m
level: Intermediate
updated: 8/21/2020
learners: 20776
skills:
  - Angular
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/C4D0DAQE6-0tyxuH2Og/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1597852457143?e=2147483647&amp;v=beta&amp;t=cXvpZzOxccuTOlqtWfDikKsRP-w5fpM4XA-S34y3PTI"
linkedin_topic: Software Development
learning_paths:
  - '[Explore Web Development with Angular](../../Paths/Software%20Development/Learning%20Paths/Explore%20Web%20Development%20with%20Angular.md)'
prev_courses:
  - '[Angular- Testing and Debugging](Angular-%20Testing%20and%20Debugging.md)'
next_courses:
  - '[Angular- Cloud-Powered Apps with Firebase](Angular-%20Cloud-Powered%20Apps%20with%20Firebase.md)'
path_nav: '[{"path":"Explore Web Development with Angular","position":4,"total":6,"prev":"Angular- Testing and Debugging","next":"Angular- Cloud-Powered Apps with Firebase"}]'
path_count: 1
tags:
  - course
  - topic/software-development
  - topic/web-development
  - skill/angular
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/Angular-%20Creating%20and%20Hosting%20a%20Full-Stack%20Site.md)

![Angular: Creating and Hosting a Full-Stack Site](https://media.licdn.com/dms/image/v2/C4D0DAQE6-0tyxuH2Og/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1597852457143?e=2147483647&amp;v=beta&amp;t=cXvpZzOxccuTOlqtWfDikKsRP-w5fpM4XA-S34y3PTI)

# Angular: Creating and Hosting a Full-Stack Site

> You've learned Angular and can create amazing front-end interfaces—but deploying them requires more than just Angular. You need logic running on the server, and a place to put that logic. If you want to apply your Angular skills to a broader range of projects—but you're not very familiar with server-side development—then this course is for you. Join instructor Shaun Wassell as he steps through how

> [LinkedIn Learning](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site) | 4h 21m | Intermediate | 21K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (5 videos)
  - [Front-end to back-end creating and hosting a full-stack site](#front-end-to-back-end-creating-and-hosting-a-full-stack-site)
  - [What you should know](#what-you-should-know)
  - [Install Node.js and npm](#install-nodejs-and-npm)
  - [Exercise files](#exercise-files)
  - [The approach](#the-approach)
- [**1. Creating an Angular Front End**](#1-creating-an-angular-front-end) (14 videos)
  - [Why Angular?](#why-angular)
  - [Set up an Angular project](#set-up-an-angular-project)
  - [Angular project structure](#angular-project-structure)
  - [Create your first Angular component](#create-your-first-angular-component)
  - [Routes and routing in Angular](#routes-and-routing-in-angular)
  - [Add fake data](#add-fake-data)
  - [Implement the Listings page](#implement-the-listings-page)
  - [Create a Listing Detail page](#create-a-listing-detail-page)
  - [Create a Contact Seller page](#create-a-contact-seller-page)
  - [Create a My Listings page](#create-a-my-listings-page)
  - [Create a New Listings page](#create-a-new-listings-page)
  - [Create a Listing Data form](#create-a-listing-data-form)
  - [Create an Edit Listing page](#create-an-edit-listing-page)
  - [Create a navigation bar](#create-a-navigation-bar)
- [**2. Creating a Node Back End**](#2-creating-a-node-back-end) (7 videos)
  - [Why Node.js?](#why-nodejs)
  - [Set up a hapi server](#set-up-a-hapi-server)
  - [Test a hapi server with Postman](#test-a-hapi-server-with-postman)
  - [Create a listings endpoint](#create-a-listings-endpoint)
  - [Restart a Node server automatically](#restart-a-node-server-automatically)
  - [Create an individual listing endpoint](#create-an-individual-listing-endpoint)
  - [Errors in hapi using hapi/boom](#errors-in-hapi-using-hapiboom)
- [**3. Adding MySQL to Your Back End**](#3-adding-mysql-to-your-back-end) (10 videos)
  - [Why MySQL?](#why-mysql)
  - [Install MySQL and MySQL Workbench](#install-mysql-and-mysql-workbench)
  - [Set up a MySQL database](#set-up-a-mysql-database)
  - [Connect to a MySQL database from Node](#connect-to-a-mysql-database-from-node)
  - [Rewrite the listings endpoints](#rewrite-the-listings-endpoints)
  - [Create an endpoint for adding views](#create-an-endpoint-for-adding-views)
  - [Create an endpoint to get a user listing](#create-an-endpoint-to-get-a-user-listing)
  - [Create an endpoint to create listings](#create-an-endpoint-to-create-listings)
  - [Create an endpoint to edit listings](#create-an-endpoint-to-edit-listings)
  - [Create an endpoint to delete listings](#create-an-endpoint-to-delete-listings)
- [**4. Connecting the Front and Back Ends**](#4-connecting-the-front-and-back-ends) (7 videos)
  - [Services in Angular](#services-in-angular)
  - [RxJS and the Angular HttpClient](#rxjs-and-the-angular-httpclient)
  - [Avoid CORS errors](#avoid-cors-errors)
  - [Connect the Listing Detail page](#connect-the-listing-detail-page)
  - [Connect the My Listings page](#connect-the-my-listings-page)
  - [Connect the New Listing page](#connect-the-new-listing-page)
  - [Connect the Edit Listing page](#connect-the-edit-listing-page)
- [**5. Authentication**](#5-authentication) (9 videos)
  - [What is Firebase Auth?](#what-is-firebase-auth)
  - [Set up a Firebase account](#set-up-a-firebase-account)
  - [Add Firebase to Angular](#add-firebase-to-angular)
  - [Add Firebase to hapi](#add-firebase-to-hapi)
  - [Sign in and out](#sign-in-and-out)
  - [Load user listings with Auth](#load-user-listings-with-auth)
  - [Create listings with Auth](#create-listings-with-auth)
  - [Edit listings with Auth](#edit-listings-with-auth)
  - [Delete listings with Auth](#delete-listings-with-auth)
- [**6. Hosting Your Site**](#6-hosting-your-site) (6 videos)
  - [Move a full-stack app to the cloud](#move-a-full-stack-app-to-the-cloud)
  - [Installing the Google Cloud CLI](#installing-the-google-cloud-cli)
  - [Moving MySQL to the cloud](#moving-mysql-to-the-cloud)
  - [Environment variables](#environment-variables)
  - [Host a Node server on Google Cloud](#host-a-node-server-on-google-cloud)
  - [Build and serve an Angular app](#build-and-serve-an-angular-app)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Front-end to back-end creating and hosting a full-stack site](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/front-end-to-back-end-creating-and-hosting-a-full-stack-site?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/front-end-to-back-end-creating-and-hosting-a-full-stack-site?u=76281980&t=0)** - [Shaun] It's a really exciting time to be a [JavaScript](../../Skills/Software%20Development/JavaScript.md) developer where companies used to need separate developers to work on their front end, backend, mobile apps and [Databases](../../Skills/Software%20Development/Databases.md) since all of those required experience with different types of programming, JavaScript can now be used to develop on each of these platforms, meaning that one JavaScript developer can now create something that used to take an entire team and the rise of front end frameworks such as [Angular](../../Skills/Web%20Development/Angular.md) and server runtime such as [Node.js](../../Skills/Web%20Development/Node.js.md) mean that JavaScript has become something of a common language in the development world. In this course, I'll show you how to create and deploy a full stack Angular powered website from scratch. We'll start off by using Angular to create an interactive front end for our site, and then move on to writing a Node.JS server tied in with a [MySQL](../../Skills/Software%20Development/MySQL.md) database that will support our front end. We'll then close out by seeing how you can host your full stack site on [Google Cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) and access it from anywhere. I'm Shaun Wassell and I'm a senior web developer. Join me in my [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) learning course to learn how to build a full stack Angular powered website from scratch.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (4), [Angular](../../Skills/Web%20Development/Angular.md) (4), [Node.js](../../Skills/Web%20Development/Node.js.md) (2), [Databases](../../Skills/Software%20Development/Databases.md) (1), [MySQL](../../Skills/Software%20Development/MySQL.md) (1)
> **CLI Commands:** node (2), mysql (1)
> **File Paths:** node.js (2)
> **Analogies:** such as (2)
> **Speakers:** - [shaun] (1)

#### [What you should know](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/what-you-should-know?u=76281980&t=0)** - [Narrator] To get the most out of this course, there are a few things that would be helpful for you to know. First of all, it would be very helpful to have a basic knowledge of [JavaScript](../../Skills/Software%20Development/JavaScript.md) and ideally a basic knowledge of how frontend frameworks work as well. If you haven't already worked with these a little, I'd recommend you take a look at one of the JavaScript basics courses in the library first, and then come back to this one, since we won't be covering any of the very basics in this course. It would also be helpful for you to have some experience with basic command line operations. All that we'll be using for this course is basic commands like mkdir to make directories, cd to change directories, and ls to see the contents of a directory, as well as a few commands specific to [Node.js](../../Skills/Web%20Development/Node.js.md) that I'll show you later on. And keep in mind also that if you're on [Windows](../../Glossary/Service/Windows.md), the commands might be a little different. If that's the case, take a look at the link that I have here to an article showing many of the basic [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) commands and their Windows equivalents. I also highly recommend that you follow along with me as I write code, but I've also included the start and finish state for all the code that I write in this course in the exercise files for your reference. Basically, this course is intended for developers who want to learn the ins and outs of building a full-stack website that incorporates frontend, backend, [Databases](../../Skills/Software%20Development/Databases.md), user authentication, and hosting. If that sounds like something you're interested in, then you're definitely in the right place.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (2), [Windows](../../Glossary/Service/Windows.md) (2), [Node.js](../../Skills/Web%20Development/Node.js.md) (1), [Linux](../../Skills/Network%20and%20System%20Administration/Linux.md) (1), [Databases](../../Skills/Software%20Development/Databases.md) (1)
> **CLI Commands:** mkdir (1), make (1), cd (1), ls (1), node (1)
> **File Paths:** node.js (1)
> **Tools:** command line (1)
> **Exercise Files:** exercise files (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [narrator] (1)

#### [Install Node.js and npm](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/install-node-js-and-npm?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/install-node-js-and-npm?u=76281980&t=0)** - [Instructor] Let's install the tools necessary for this course. [Node.js](../../Skills/Web%20Development/Node.js.md) will be really important for running our code and installing a few libraries we'll be using to make our programming easier. If you're not already familiar with Node.js, all you need to know for this course is that Node.js is an incredibly popular platform that allows you to run [JavaScript](../../Skills/Software%20Development/JavaScript.md) code outside of the browser. This is really helpful since it means your JavaScript code doesn't have to be loaded and executed as part of a webpage. In other words, you can write a single JavaScript file and run it from the command line. If you don't already have Node.js installed, you can download it from Node.js's website, [nodejs.org](https://nodejs.org). We're going to click on this button here and download it. And once it downloads, we're going to click on it to open it. And this should take you through the steps required for installation. Note that if you're using [Windows](../../Glossary/Service/Windows.md) or a Unix distribution the steps might be a little different, but Node.js has instructions for all of these as well. And once you have Node.js installed, you'll want to make sure that you have the most recent version of [npm](../../Skills/Web%20Development/npm.md) installed. We'll be using a few libraries in this course and NPM is a package manager for Node.js that allows us to easily install them. To install the most recent version of NPM, just open your terminal and type NPM install -g npm@latest,
>
> **[1:20](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/install-node-js-and-npm?u=76281980&t=80)** and you might need to run this with sudo as well. After that, type NPM -v and it should show the latest version. If the NPM and Node versions on your computer are higher than mine, don't worry about it. Everything should still run correctly. But I do highly recommend having at least these version numbers here. And that's all there is to it. Node.js and NPM are now installed on your computer.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Node.js](../../Skills/Web%20Development/Node.js.md) (9), [npm](../../Skills/Web%20Development/npm.md) (8), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3), [Windows](../../Glossary/Service/Windows.md) (1)
> **CLI Commands:** node (10), npm (8), make (2), sudo (1)
> **File Paths:** node.js (9)
> **Env Vars:** npm (7)
> **Prerequisites:** install (4)
> **Definitions:** is an  (1), in other words (1), is a  (1)
> **Tools:** command line (1), terminal (1)
> **UI Navigation:** click on (2)

#### [Exercise files](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/exercise-files?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/exercise-files?u=76281980&t=0)** - [Instructor] As with most courses on this platform, this course includes exercise files to help you follow along with the code I write. For the most part, it's probably more or less what you're used to. There are just two things that are important for you to know about the exercise files for this course. The first is that there are two separate projects for this course, a front end project and a backend project. For both of these, before you're able to run any of the code, you'll need to run the command, [npm](../../Skills/Web%20Development/npm.md) install, inside the project directory. And what this will do is install all the associated npm packages that each project needs. The second thing to note is that once we get into the section on user authentication, the exercise files will require that you also follow along with the corresponding setup on [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) and [Firebase](../../Skills/Web%20Development/Firebase.md). In other words, they won't work if you just try and run them as is. Well, that's about all you need to know for that. Let's take a look at the project we'll be building.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [npm](../../Skills/Web%20Development/npm.md) (2), [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) (1), [Firebase](../../Skills/Web%20Development/Firebase.md) (1)
> **Prerequisites:** install (2), you'll need (1), setup (1)
> **Exercise Files:** exercise files (3)
> **CLI Commands:** npm (2)
> **Definitions:** in other words (1)
> **Speakers:** - [instructor] (1)

#### [The approach](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/the-approach?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/the-approach?u=76281980&t=0)** - [Instructor] So as I mentioned, in this course, we'll be using a number of different technologies to build and host a Full Stack web application. And the application that we'll be building will be a listing site, something similar to sites like Craigslist, where users will be able to list items for sale and view items that other users want to sell. And this site will also include things like user authentication, that is it'll allow users to log in and out using their [Google](../../Glossary/Service/Google.md) account and use that to keep track of who's listings are who's. So the front end of this site will be built using [Angular](../../Skills/Web%20Development/Angular.md), which again is a very popular and easy to use front end framework, and this front end site will be served by a backend written for a [Node.js](../../Skills/Web%20Development/Node.js.md) environment. And it'll use a very popular server framework called hapi to make the task of creating an API easier. And the backend will also store all its data in a Maya [SQL](../../Skills/Data%20Science/SQL.md) database. And after we've built all that, we'll be adding user authentication to our site using something called [Firebase](../../Skills/Web%20Development/Firebase.md) Auth, and then, we'll host our entire Full Stack application on [Google Cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md). Well, that's about it. Let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google](../../Glossary/Service/Google.md) (1), [Angular](../../Skills/Web%20Development/Angular.md) (1), [Node.js](../../Skills/Web%20Development/Node.js.md) (1), [SQL](../../Skills/Data%20Science/SQL.md) (1), [Firebase](../../Skills/Web%20Development/Firebase.md) (1)
> **CLI Commands:** node (1), make (1)
> **Env Vars:** api (1), sql (1)
> **File Paths:** node.js (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)


### 1. Creating an Angular Front End

[↑ Back to Table of Contents](#table-of-contents)

#### [Why Angular?](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/why-angular?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/why-angular?u=76281980&t=0)** - [Instructor] So we're going to be building the front-end of our buy and sell site using [Angular](../../Skills/Web%20Development/Angular.md). Now, you might be wondering why we choose to use Angular instead of just creating our site using basic [HTML](../../Skills/Web%20Development/HTML.md), CSS, and [JavaScript](../../Skills/Software%20Development/JavaScript.md), or instead of using some other low code solutions such as [WordPress](../../Skills/Web%20Development/WordPress.md) or Wix. Well, compared to hand coding a site, Angular has a lot of advantages. The main advantage is that it makes the task of creating a performance website much simpler and faster, without taking away any of the fine grain control from the programmer. It allows us to create these modular reusable components that can then be arranged into a fully functional website, instead of hand coding each individual page on our site. Imagine, for example, that we have a blog site with a hundred articles, each with pretty much the same structure. Now, what if we wanted to add a signup form to the top of all of our pages? Well, if our site was just a collection of HTML documents, we'd have to make the same exact change to a hundred separate files, which, you know, isn't a pleasant task by any stretch of the imagination. With Angular, on the other hand, all of our pages would be based on a single reusable page component, so we'd only have to make this change once to see it across all the pages. So, the next question is, why you'd use Angular over a platform like WordPress, or Wix, or Squarespace, which allow you to create a website without writing much or in any cases, any code? Well, first of all, since this course is aimed primarily at developers, if you want to create a blog and you're not already a fairly strong developer, I actually would recommend that you use one of these platforms instead of Angular,
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/why-angular?u=76281980&t=94)** since it'll probably end up being much more difficult for you in the long run if you try to create a code based site, if you still struggle with basic coding. So, another question is why you'd choose Angular over some of the other web frameworks like [React](../../Skills/Web%20Development/React.js.md) or [Vue](../../Skills/Web%20Development/Vue.js.md). And the answer to this question is that it kind of depends on your personal situation and needs. Angular is definitely more opinionated than the other major frameworks. And what this means is that, as we'll see while we're building our site, for most use cases, Angular provides some sort of guidance on what exactly the preferred way to do things is, and this is something you wouldn't find in React, for example, where there are often many different ways to accomplish pretty much the same thing. And this means that Angular can be especially helpful if you're fairly new to [Front-End Development](../../Skills/Web%20Development/Front-End%20Development.md), and don't yet have the instincts that only come from years of creating web applications. And ultimately, if you're still wondering whether to use Angular, or React, or Vue, really the best way to see the differences between all these frameworks is to just try them out for yourself, try building the same apps using all three and see what you like and don't like about each one. All that being said, Angular is a fantastic tool for programmers who want to create, basically, any type of web application, and with technologies out there like NativeScript and Ionic, it's become a tool for creating mobile apps as well. Angular developers are currently highly sought after by many companies who are looking to modernize their web apps or stay ahead of the curve. So with that knowledge, let's move on and see how we can quickly set up an Angular powered front-end for our web application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Angular](../../Skills/Web%20Development/Angular.md) (14), [React](../../Skills/Web%20Development/React.js.md) (3), [HTML](../../Skills/Web%20Development/HTML.md) (2), [WordPress](../../Skills/Web%20Development/WordPress.md) (2), [Vue](../../Skills/Web%20Development/Vue.js.md) (2)
> **Analogies:** for example (2), such as (1), imagine (1)
> **CLI Commands:** make (2), find (1)
> **Env Vars:** html (2), css (1)
> **Definitions:** means that (1), is a  (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Set up an Angular project](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-an-angular-project?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-an-angular-project?u=76281980&t=0)** - [Instructor] Now that we know a little bit more about [Angular](../../Skills/Web%20Development/Angular.md) and some of its benefits let's get started creating an Angular front end for our own project. Now, there are a few steps to doing this, but overall it's a fairly straightforward process. The first thing we're going to do is open up a terminal and I'm using VS Code and VS Code's built in terminal here and inside that terminal, we're going to run the command [npm](../../Skills/Web%20Development/npm.md) install -g @angular/[CLI](../../Skills/Network%20and%20System%20Administration/CLI.md)
>
> **[0:30](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-an-angular-project?u=76281980&t=30)** and hit enter and you may need to put the sudo keyword before this command if you get something about permissions like I am here and now we have what's called the [Angular CLI](../../Skills/Web%20Development/Angular%20CLI.md) installed globally on our computer and what this means is that we can use commands such as ng to perform Angular related tasks, such as creating new projects. So what we're going to do is change directories into the directory that we want to create our actual project into and once we're there, we're going to run the command ng new and then the name of the project that we want to create. In our case, since we're creating a buy and sell site, we'll say ng new buy-and-sell and hit enter and it's going to come up with a question asking us if we want to add Angular [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md), and we want to answer yes to that and then they'll ask us, what style sheet format we want to use for our project. We're just going to select CSS here and once that completes, we can change directories into the new project that we created. So we can say cd buy-and-sell and then in order to actually run our project, what we can do is type ng serve and we can optionally add this -O tag here, which will open up a new tab with our app in it and here we see our app, which contains a lot of boilerplate that the Angular generator, generates for us, open in a browser. Obviously we're going to make a lot of changes to this here. We're pretty much going to delete this whole app and start fresh, but the point is that we now have a running Angular app.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Angular](../../Skills/Web%20Development/Angular.md) (7), [npm](../../Skills/Web%20Development/npm.md) (1), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (1), [Angular CLI](../../Skills/Web%20Development/Angular%20CLI.md) (1), [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) (1)
> **Tools:** terminal (3), vs code (2)
> **CLI Commands:** npm (1), sudo (1), cd (1), make (1)
> **Env Vars:** cli (1), css (1)
> **Analogies:** such as (2)
> **Exercise Files:** boilerplate (1)
> **Prerequisites:** install (1)
> **Speakers:** - [instructor] (1)

#### [Angular project structure](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/angular-project-structure?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/angular-project-structure?u=76281980&t=0)** - [Instructor] So now that we have our [Angular](../../Skills/Web%20Development/Angular.md) project created and running, let's actually open it in our IDE, and once we've done that, this is a great chance to take a look at the structure of our Angular project. So in our file tree here, you'll see that there's a lot of files that Angular generated for us. Most of what we're going to be concerned with is inside the source directory. Outside of there, you have mostly just a bunch of configuration files that you can change at will, but then if we go inside our source directory, let's take a look at some of the files and directories inside of here. So first of all, we have this app directory, which contains all of our component code. This is where we'll be spending most of our time in this course. And then we have this assets directory, which contains any special assets our project needs, such as images. And then we have this environments directory, which contains files for specifying different environment variables, which can be very useful for having a dev in a production environment. And we also have this index.[HTML](../../Skills/Web%20Development/HTML.md) file. This is where the Angular app gets rendered into, and specifically, it gets rendered into this app root element here. Let's see, we also have this main.ts file, which is the entry point for our Angular application, as well as this polyfills file, which contains any special packages that we need to import for browser compatibility. I usually use this to import packages that'll make certain things work on Internet Explorer, for example. And we also have the styles.css file, which is where we can add any global styling
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/angular-project-structure?u=76281980&t=94)** that we want to our project. And lastly, we have this test.ts file, which launches testing for our application. We won't be using this at all in this course, but if you want to do stuff like [Test-Driven Development](../../Skills/Software%20Development/Test-Driven%20Development.md), this is where you do it from. So anyway, that's the basic high level structure. Let's take a closer look at this app directory inside our source directory here. Now the first thing to notice inside this directory are these app.component.ts, app.component.HTML, and appcomponent.css files. Now, as you'll see, these are the files that control the various pieces of our application component, which is basically the component in our Angular application that contains the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the components we'll create. And as you'll see, all components in Angular consist of these three files, the CSS file, the HTML file, and the TypeScript file, as well as this spec file, which is used for testing. So the HTML file, for example, is where we define what the actual dom structure of our component looks like when it gets rendered into the user's browser. Right now, it just contains a bunch of boilerplate code, which is what we saw when we ran our project. And then we have our app.component.ts file, which is where we define the data that gets displayed inside our component, as well as the logic that sort of goes on behind the scenes. And of course, the CSS file here is where we define component-specific styling. So anyway, those are the basic files for our app component, which as I mentioned is the root level component
>
> **[3:07](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/angular-project-structure?u=76281980&t=187)** for our application. Some of the other files that we have in here are the app [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) module file, which contains the routing logic for our app, and we'll see exactly how to use this file in a few minutes. And we also have the app.module.ts file. When we want to import a module into our Angular application, we usually have to add them here, and we'll see how this works when we do things like add network requests to our front end when we link it up to the server we'll be creating. Note too that any other components we create in our application need to be added here as well, although this will usually happen automatically as long as we use Angular scripts to create our new components, and we'll see more on that in a minute.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Angular](../../Skills/Web%20Development/Angular.md) (9), [HTML](../../Skills/Web%20Development/HTML.md) (4), [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) (2), [Test-Driven Development](../../Skills/Software%20Development/Test-Driven%20Development.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **File Paths:** app.component.ts (2), index.html (1), main.ts (1), styles.css (1), test.ts (1)
> **Env Vars:** html (4), css (2), ide (1)
> **Analogies:** for example (2), such as (1)
> **CLI Commands:** make (1)
> **Documentation:** spec (1)
> **Exercise Files:** boilerplate (1)
> **Definitions:** is a  (1)

#### [Create your first Angular component](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-your-first-angular-component?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-your-first-angular-component?u=76281980&t=0)** - [Instructor] So now that we've seen the basic directory structure of our [Angular](../../Skills/Web%20Development/Angular.md) project, let's move on to creating the first component for our application. Components are the basic units of organization in Angular projects. They're the sort of modular pieces of the interface that we can define and display inside other components. And as I mentioned already, the app component that we looked at is the component in Angular applications that contains the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the components we create. The first component we're going to create for our buy and sell site is going to be the listings page, which will display all the currently available listings that a user can buy. Now, when creating a new component in our Angular projects, instead of creating all the files and setting up all the boilerplate code ourselves, Angular provides a script that we could run to generate all of that for us. So in order to generate our listings page component, for example, we can just open up a terminal and run the command ng generate component and then the name of the component we want to create. In our case, it'll be listings-page. And hit Enter. And that'll run and generate all the files and code we'll need for that component. If we look inside our app directory now, we see that we now have this listings page folder inside our app directory that contains the same files we saw with our app component: the CSS, [HTML](../../Skills/Web%20Development/HTML.md), spec, and TS files. And if we take a look at our HTML file, we see that the HTML that will be displayed for this component just says listing page works. And we're going to change this to something
>
> **[1:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-your-first-angular-component?u=76281980&t=92)** more useful shortly but for now let's see how to actually display this component in our application. If we look inside our component's component.ts file here, we see that we have this funny-looking component decorator here, which basically just contains [Metadata](../../Skills/Web%20Development/Metadata.md) about our Angular components. Don't let this syntax throw you off here. And inside this decorator, we see that we have this selector property, which is app-listings-page. And this is the tag that we can use to insert this component into another component's HTML. So for now, what we're going to do is open up our app component's HTML file. And the first thing we're going to do here is remove all the sample code they gave us. And we want to delete everything in this file except for this router outlet here. And we'll talk about what this is exactly and why we want to keep it in a bit. And then what we're going to do to display our listings page component is we're going to simply say app-listings-page. And close that bracket. And this will insert the listings page component we just created into our app component. And if we run our project again by saying ng serve -o, we see that it's now displaying the listings page here. Now, this isn't super exciting yet but that's the basics of creating a new component and displaying it inside another component in Angular.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Angular](../../Skills/Web%20Development/Angular.md) (7), [HTML](../../Skills/Web%20Development/HTML.md) (5), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Metadata](../../Skills/Web%20Development/Metadata.md) (1)
> **Env Vars:** html (5), css (1)
> **Exercise Files:** boilerplate (1), sample code (1)
> **File Paths:** component.ts (1)
> **Documentation:** spec (1)
> **Tools:** terminal (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Routes and routing in Angular](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/routes-and-routing-in-angular?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/routes-and-routing-in-angular?u=76281980&t=0)** - [Instructor] So right now we've got our listings page component displaying inside our application. And we'll get into actually implementing our listings page soon. But first let's take a look at how [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) works in [Angular](../../Skills/Web%20Development/Angular.md). As in virtually every web application we'll want the different components and pages of the application we're going to create to be displayed as a user navigates to different routes in their browser. For example, we'll want our listings page to be displayed on our /listings route. And we'll want our my listings page to be displayed on our my-listings route. And so on and so forth. So implementing this kind of functionality in Angular is actually really simple to do. All we have to do is open up our app-routing.module.ts file here. And you see this const routes array here? Well what we need to do is add an object to this array for every route that our application needs. So in the case of our listings component, here's what that's going to look like. We're going to create a new object here and we're going to say path. And then the URL that we want our component to be displayed at. In our case, it's going to be listings. And then we have to specify the component that we want to be displayed at that route. In our case, that's going to be our listings page component, which we have to actually import from its file. So we'll say import ListingsPageComponent. From ./listings page /listings-page.component.
>
> **[1:35](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/routes-and-routing-in-angular?u=76281980&t=95)** And one less thing that we're going to add to this object is pathMatch, and then the string full. And we need to add this pathMatch thing because of another page we'll be adding that will allow our users to see the details of a specific listing. You'll see why in a second. And now let's save this file and go back to our app components [HTML](../../Skills/Web%20Development/HTML.md) file. And we're just going to delete this app-listings-page tag that we put in there before. And save it. And if we take a look at our application now, what we're going to have to do is go to /listings here. And then and only then will we see our listings page components HTML displayed. So anyway, that brings us back to our app's HTML file. You see this router-outlet tag here? What that is, is the location on the page where the component matching the current route will be rendered. And right now that's not very exciting since it's the only thing in the HTML. But just to illustrate, let's add a heading above our router outlet that says something like buy and sell app. And now what we see is that our heading gets displayed no matter what route we're on. While our router outlet gets replaced with whatever route specific content we've defined in our app routing file. And as you might've guessed, this kind of thing is very useful when we to do things like add a navbar to our application. And that's actually something we're going to be doing shortly. But first while we're on the topic of routing
>
> **[3:07](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/routes-and-routing-in-angular?u=76281980&t=187)** and displaying different components at different routes, why don't we take this opportunity to create the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the pages we'll be needing for our application and add routes to them. So here's what that'll look like. Let's open up a terminal and we'll temporarily kill our app. And we're going to use that same ng generate component command to generate all the rest of the pages for our application. So the first page we're going to need is a page for displaying more details about a given listing. So in our terminal we're going to say ng generate component. And we'll call this new page listing-detail-page and hit Enter. And we'll also want a page for contacting the owner of a given listing, which we'll call the contact page. So we'll say ng generate component, contact-page, and hit Enter. And then we're going to add a page that will display a list of the user's own listings. So we'll say ng generate component, and we'll call this the my-listings-page. And we're also going to add pages for creating new listings. Ng generate component, new-listing-page.
>
> **[4:35](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/routes-and-routing-in-angular?u=76281980&t=275)** And also a page for editing existing listings. So we'll say ng generate component, edit-listing-page. And that should be all the page components we'll need for now. So the next thing we're going to do is inside our app-routing module file, we're going to add a route for each of the pages we just created. So we'll start off with our listing detail page. So we'll say path listings /:id. And right off the bat there's something different about this route. We had this :id thing here, which is what's called a URL parameter. We'll be using this to display the data of a particular listing on this page. For example, we might have/listings/123. Which would display the data for the listing with the id 123 on the page. We'll see exactly how this works a little later. And note also that this route is the reason that we had to use this pathMatch full thing on the route above it. If we didn't use that our listings page route would automatically match and take precedence over this route. Since technically the route/listings matches when the browser's URL is/listings/123 or/listings/anything else. And that's just something to keep in mind. So anyway let's continue with this route. We're going to want to display the ListingsDetailPageComponent here. And we'll import all of these components at the very end by the way.
>
> **[6:09](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/routes-and-routing-in-angular?u=76281980&t=369)** And then we're going to define another route for our contact page. So we'll say path contact, and we're going to use this :id thing again. And the component for that will be our ContactPageComponent. And actually now that I'm thinking about it, this is listing detail not listings detailed page component. And after that we're going to have our edit listing page component, which is going to be edit-listing and the URL parameter thing again. And for that component we're going to say EditListingPage component. And then we'll have our my-listings page, which will be my listings. And the component will be MyListingsPageComponent. And last but not least, we're going to have our new listing route. Which is going to look like this, path new-listing component, NewListingPageComponent. And of course, we're going to have to import all of these components. Which is going to be a little bit of a pain, but nothing we can't handle. So we're going to say import ListingDetailPageComponent from ./listing-detail
>
> **[7:36](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/routes-and-routing-in-angular?u=76281980&t=456)** - page/listing-detail-page.component. Import ContactPageComponent from contact-page/contact-page.component. Import EditListingPageComponent from edit-listing-page/edit-listing-page.component. Import MyListingsPageComponent from my-listings-page/my-listings-page.component. And last but not least, import NewListingPageComponent from new-listing-page /new-listing-page.component. And now that we have all that, the last thing we're going to do here is set up a redirect so that when the user visits the home route, that is just the slash route. They're automatically redirected to the listings route here. And here's what that's going to look like. We're going to have one last route whose path is just a empty string. And we're going to say redirectTo /listings and the pathMatch here is going to be full to prevent it from interfering with our other routes. And now if we have our application running, which I have to restart mine by saying ng serve -o. And now what we can do is go to any of the routes
>
> **[9:08](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/routes-and-routing-in-angular?u=76281980&t=548)** that we define such as my-listings. And see the corresponding page component displayed at that route. So if we go to /listings/123 for example, we see our listings detail page. If we go to new listing, we see our new listing page and so on and so forth. And also if we just go to our home route, we see that it automatically redirects us to the /listings route. So one last thing here is let's just delete this heading tag and save it. And that should be all we need for now.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Routing](../../Skills/Network%20and%20System%20Administration/Routing.md) (5), [HTML](../../Skills/Web%20Development/HTML.md) (4), [Angular](../../Skills/Web%20Development/Angular.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2)
> **Env Vars:** url (4), html (4)
> **Code Identifiers:** pathmatch (4), redirectto (1)
> **UI Navigation:** go to (5)
> **Analogies:** for example (3), such as (1)
> **Tools:** terminal (2)
> **Speakers:** - [instructor] (1), - page (1)
> **File Paths:** app-routing.module.ts (1)

#### [Add fake data](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-fake-data?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-fake-data?u=76281980&t=0)** - [Instructor] Now that we've got our pages and their corresponding router set up, we going to start implementing each page of our application. But before we do that, there are a few things we have to do first. The first thing is that we have to add some fake data to our application. Later on in the course, when we create our backend, our front end application will just load all of its data from there. But for now while we're just building out our front end, we going to define some fake data that we can use instead. So inside our app directory, let's create a new file and call it fake-data.js. And then in order to populate this file with fake data, what we going to do is copy and paste that data from this project's exercise files. So just go to the end state for this video and copy and paste the data from there. And then we'll paste that inside here. It'll look something like this here. And once we've done that, the second thing we need to do here is since the fake data we just copied and pasted contained a new type script type, we need to actually define that type so that we can use it throughout the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our front end. So inside our app directory, let's create another new file called types.ts. And then inside this file, we going to define a listing type, which will look something like this. We'll say export interface, listing id: string, name: string, description: string, and price will be a number.
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-fake-data?u=76281980&t=94)** And let's save that file. And then we'll go back to our fake data file and import that type we just defined. So we'll say, import, listing, from , types. And it looks like I accidentally named this file .js that should have been .ts. So we'll rename that here. And now that we've got all our fake data set up, there's one last thing we going to do here. In order to make our application look a little more exciting than just basic unstyled elements, what we going to to do is copy and paste some CSS that I've written for this app. Again, if you go into the projects exercise files and take a look at styles.css in the app directory, you should copy all of those styles into your own projects styles .css file. And that'll look something like this. And then we're going to save that file. And that's all we really need to do here. Now note that in a full scale application, having all of the styles for our entire application in one spot like this, is probably not the approach we'd want to take. What we'd want to do instead is split these styles out and put them in the CSS files of their corresponding components. But in order to make it easier for you to just copy and paste, I've put them all in one file like this. And keep in mind too that as we develop the rest of our front end, we going to to be adding some classes in IDs to our [HTML](../../Skills/Web%20Development/HTML.md) elements. For the sole purpose of making them match up with these styles here.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **File Paths:** fake-data.js (1), types.ts (1), styles.css (1)
> **Env Vars:** css (2), html (1)
> **CLI Commands:** make (2)
> **Exercise Files:** exercise files (2)
> **Warnings:** note that (1), keep in mind (1)
> **Prerequisites:** set up (2)
> **Cross-References:** go back to (1)

#### [Implement the Listings page](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/implement-the-listings-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/implement-the-listings-page?u=76281980&t=0)** - [Instructor] Now that we've got all of that setup out of the way, let's start implementing our listings page component. Remember that this page is going to display a list, showing all the available listings on our site. Now in reality, our site might have thousands of listings, and in this case, we definitely want to paginate our results, but in this course, we're going to ignore that and just display all our listings on a single page. So here's what implementing our listings page is going to look like. It's going to involve making changes to two files here, our listings page TypeScript file, and our listings page [HTML](../../Skills/Web%20Development/HTML.md) file. So let's open both of those files up. Now there's really only one main thing we'll want to do inside the TypeScript file for now, and that's defined a listings array to contain the listings that we want to display. So up at the top of our component, let's define this variable. We're going to say listings, and it's going to be an array of our listing type, and the initial value is going to be an empty array, and then of course, we have to actually import this listing type up at the top of our file. So we'll say import, listing, from types,
>
> **[1:16](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/implement-the-listings-page?u=76281980&t=76)** and while we're at it, let's also import our fake listing data that we copied and pasted from [GitHub](../../Skills/Software%20Development/GitHub.md). That's going to look like this. We'll say import, fake listings from..dot/fake-data,
>
> **[1:31](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/implement-the-listings-page?u=76281980&t=91)** and then inside this ngOnInit method of our component, we're going to set our listings member variable equal to these fake listings that we imported. So we'll say this.listings= fake listings. So one question you might have right here is, why aren't we just setting our listings equal to our fake listings, right where we define the member variable, right? Why don't we just say listings equals fake listings up here? Well, the reason we're doing it this way is because right now we're just using fake data, but later on, we'll be loading this data from the server and the proper place to do this at [Angular](../../Skills/Web%20Development/Angular.md) is inside the ngOnInit method, not in the constructor or up here at the top of our component. So now that we have this data defined for our component, let's save our file and then head over to the HTML file, and define our components structure. So here's what that's going to look like. We're going to delete this listings page works thing here, and we're going to wrap our entire component in a div, and then we're going to say, div class equals content-box. Again, this is just for styling, so that it matches up with the styles we copied and pasted. And then we're going to do something that I'll explain in just a moment. We're going to say *ngFor= let listing of listings.
>
> **[2:56](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/implement-the-listings-page?u=76281980&t=176)** So this ngFor is something new here. It's, what's called a structural directive. Structural directives in angular allow us to change the structure of the HTML on our page, depending on the underlying data in our TypeScript file. So in this case, what we want to do, is display a separate one of these divs, for each and every listing in our listings array, which is what this statement here does. And by the way, the little asterisk here is how we denote structural directives in angular. So the next thing we're going to do is display the name and price of each of our listings, inside a link. And here's what that's going to look like. We're going to say a routerLink equals, and I'm going to explain all this syntax in just a second here, just follow along for now, slash listings, slash double curly braces, listing.id,
>
> **[3:54](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/implement-the-listings-page?u=76281980&t=234)** and inside those tags, we're going to say each three, double curly braces again, listing.name dash, and then a dollar sign, and then double curly braces listing dot price. So there's a few new things here, right. The first is this double curly brace thing. This is how we insert the values of variables from our TypeScript file into our HTML page. In this case, we're inserting the values of the listings ID, name, and price into various parts of our HTML. And the second thing to notice here, is that inside our anchor tag, we're not using the HRef attribute like we normally would an HTML, instead we're using this router link attribute. What this allows angular to do in our app, is change the url in our browser, in the content displayed in our app, without reloading the page, which is what would happen if we use the atrium attribute. And that's pretty much all we need for this component. If we make sure our app is running, which we can do by running ng serve-o, and then head over to our app in the browser, we see that it's now displaying our fake listing items perfectly, and that we can click on the names of each of these items and it will link us to our listing detail page.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (6), [Angular](../../Skills/Web%20Development/Angular.md) (4), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **Env Vars:** html (6)
> **Code Identifiers:** ngoninit (2), ngfor (2), routerlink (1)
> **CLI Commands:** make (1)
> **Tools:** github (1)
> **UI Navigation:** click on (1)
> **Prerequisites:** setup (1)
> **Speakers:** - [instructor] (1)

#### [Create a Listing Detail page](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listing-detail-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listing-detail-page?u=76281980&t=0)** - [Instructor] So now that we've got our listings page working and linking to the corresponding detail page for each listing, it's time to actually implement the listing-detail-page. So let's open up our listing-detail-page TypeScript and [HTML](../../Skills/Web%20Development/HTML.md) files and before we begin here, there's something to know about this page. Remember that earlier when we were creating the routes for our applications, there were several routes that we created with this ID URL parameter thing. Now, you may have noticed that when we navigate from our listings-page to our listing-detail-page, the URL that we're navigating to contains the ID of the individual listing that we clicked on. So what we're going to have to do on this page is somehow get access to the value of this part of the URL and use that to display the corresponding listing from our fake listings array on our page. So here's how we do that in [Angular](../../Skills/Web%20Development/Angular.md). Let's open up our TypeScript file here. And then we're going to start off by importing something up at the top and this is going to be something called ActivatedRoute. So we'll say import ActivatedRoute from @angular/router. And once we've done that, we're going to do something here that'll explain in just a second. Inside the parentheses of our component's constructor method, we're going to say private route: ActivatedRoute. Now, the syntax of adding things like this is a very common pattern in Angular. In Angular, there're a pretty large number
>
> **[1:33](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listing-detail-page?u=76281980&t=93)** of different so-called providers that we can add to our components in order to enable them to do certain things. In this case, this ActivatedRoute provider will allow our listing-detail-page to access the value of the URL parameter in the current path. And here's how that's done. Inside our ngOnInit method, we're going to say const id and then to get the id parameter from the URL, we're going to say this.route.snapshot.paramMap, it's a pretty long series of dots here, .get and then a string that says id. So that'll get us the URL parameter with the name id. So now that we have that ID, what we're going to do is use it to display the correct listing from our fake listings data. So first, let's import that fake data. We're going to say import fakeListings from ../fake-data and let's import our listing type as well. Import Listing from ../types. And next, we're going to add a member variable to our component called listing, that'll contain the selected listing that should be displayed. So we'll say listing: Listing. And we'll reference this listing in our HTML in a second. And then what we're going to do is down here inside the ngOnIt method where we got the ID, we're going to assign the matching fake listing
>
> **[3:07](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listing-detail-page?u=76281980&t=187)** to this variable by saying this.listing equals fakeListings.find and then we're going to find the listing whose id is equal to the id parameter that we got from the URL. And that's about it for our component's TypeScript file. So next what we're going to do is head over to our component's HTML file and build out the structure for our component. So here's what that's going to look like. We're going to say div class equals content-box for the styling and then we're going to have a link that goes back to our listings page. This will be the sort of back button here. So we're going to say routerLink equals /listings. And inside there, this is pretty much just to make it look good. We're going to have a button that says Back. And then underneath this Back button, we're going to display some information about our listing. So we're going to have an h2 heading that contains the name of the listing. So we'll say listing.name inside curly braces and then we'll display the listing's description. So listing.description. And then we'll display the price for our listing. So we'll have a dollar sign and then we'll say listing.price and then at the very bottom of this page, we're going to have another link that'll take the user to the contact seller page so that they can send them a message
>
> **[4:39](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listing-detail-page?u=76281980&t=279)** saying they're interested about this product. So we're going to say a routerLink. Oops, and I'm noticing that I said routeLink here instead of routerLink. That should have an R. routerLink equals /contact slash and then listing.id in curly braces. And that's going to have a button inside it as well. So we're going to say button Contact Seller and that should be it for our listing-detail-page. If we go back to our browser where our app is running, we should see that we have all those things. We have the back button that takes us back to the listings page. And we have the basic information about the given listing. And we have the Contact Seller button that takes us to the contact-page.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Angular](../../Skills/Web%20Development/Angular.md) (4), [HTML](../../Skills/Web%20Development/HTML.md) (3)
> **Code Identifiers:** routerlink (4), fakelistings (2), ngoninit (1), parammap (1), ngonit (1)
> **Env Vars:** url (7), html (3)
> **CLI Commands:** find (2), make (1)
> **Cross-References:** go back to (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Create a Contact Seller page](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-contact-seller-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-contact-seller-page?u=76281980&t=0)** - [Instructor] So we have the listing detail page set up to link to the corresponding contact seller page. So what we need to do now, is something very similar to what we did with the URL parameters in our listing detail page. We're going to pull the listing ID out of the URL for our contact page, and use that to allow a user to send a message to the seller saying they're interested in buying the item. Well, in this course, we're not actually going to cover sending emails programmatically. If you want to see how that's done, I'd recommend taking a look at my [Firebase](../../Skills/Web%20Development/Firebase.md) course, where I show how to do this from the back end using a package called Nodemailer. For now what we're going to do is just display an alert saying that the user's message was sent. So let's head over to our ID, and let's open up our contact pages TypeScript and [HTML](../../Skills/Web%20Development/HTML.md) files. And we're going to start off in our TypeScript file by importing a few things. We're going to say import ActivatedRoute, which we'll be needing just like we needed in our listing detail page. And we're also going to need something called router, which we'll be using to navigate programmatically. You'll see how this has done in just a second here. And we're going to say from [Angular](../../Skills/Web%20Development/Angular.md)/router, and then we're going to import our listing type from our types file. And we're going to import our fake listings from our fake data file. And the next thing we're going to do is add a few member variables to our component, the first one is going to be called email, and it's going to be a string
>
> **[1:33](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-contact-seller-page?u=76281980&t=93)** with the starting value of just an empty string. What this is going to do here, is store the current value of the input for the user's email address. We'll see how to link that up to an actual input shortly when we work on our HTML file. And we're going to have several other text inputs on our page, we're going to have one for the message that the user wants to send to the seller, and that'll be a string as well. And then we're going to have another member variable that'll be the corresponding listing from the ID URL parameter. So the next thing we're going to do, is we're going to inject this activated route and router provider things here into our component by adding them to our constructor, just like we saw with our listing detail page. So we'll say private route, ActivatedRoute, and private router Router. And the next thing we're going to do, is just like in our listing detail page, we're going to pull the listing ID out of the URL parameter and use it to select the correct listing from our fake listings array. And again, that'll look like this. We're going to say const ID equals this.route.snapshot.paramMap .get ID. And then we're going to say this.listing equals fakeListings.find. And we're going to find the listing whose ID
>
> **[3:10](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-contact-seller-page?u=76281980&t=190)** is equal to the ID that we got from the URL parameter. And then what we're going to do, is we're going to use this listing that we pulled out to set a default message for the message input. So here's what that's going to look like. We're going to say this.message equals, and then in back ticks here, we're going to say, "Hi, I'm interested in your," and then we'll just have the name of the listing here. So we'll say this.listing.name, and we'll convert that to lowercase here. And we'll put an exclamation mark at the end. So again, that'll be the starting value of the message input for the user. And then what we're going to do is underneath this ngOnInit method, we're going to add a new method to our component that'll get called when the user clicks this Send button on our page. We haven't implemented that yet, but we'll get to that in just a second. And what that's going to look like is something like this. We're just going to say, sendMessage, and that won't return anything, so we'll say void. And what we're going to do is just display an alert that says something like "Your message has been sent." And then we're going to say this.router, that's our router provider that we had up here. We're going to say this.router.navigateByUrl, and we're going to have it navigate to our listings route.
>
> **[4:46](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-contact-seller-page?u=76281980&t=286)** So it'll just display an alert here, and then it'll navigate back to our listings page. So that's all for our components TypeScript file, let's move on to the HTML file. Let's make sure we save this first, and then move on to the HTML file. And there are going to be a few new concepts here, so watch closely. The first thing we're going to do is just have a div with the class of content box, just like with our other ones. And then we're going to have a h3 heading that says something like "Send the owner a message."
>
> **[5:24](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-contact-seller-page?u=76281980&t=324)** And we're going to have a div here that'll contain our form. So we're going to have a label, and we're going to have a label that says "Your email address," and then an input whose name is going to be email. And then we're going to do something a little different that I'll explain in just a second. We're going to do square brackets, then parentheses, and then inside that ngModel, and then we're going to say that that's equal to email. Okay, so this thing here is our first point of interest. What this square bracket parentheses ngModel thing does, is creates a two way binding between the value of our text input and the email member variable of our component. In other words, whenever the value of our input is changed by the user, the value of our email variable will change as well, and vice versa. Now, this won't actually work at the moment, in order for this to work, we have to actually open up our app module file. So app.module.ts. And remember earlier in the course, I said that this file is where we define some important imports and exports of our angular project. Basically, there are quite a few important things that we can import into this file that extend the basic functionality of some aspects of angular. In our case, in order to get the two way binding thing to work for our project, what we have to do is import something called the [Forms](../../Skills/Web%20Development/Forms.md) module from Angular, which will look like this.
>
> **[6:59](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-contact-seller-page?u=76281980&t=419)** We're going to say import FormsModule from @angular/forms. And then we have to add this forms module thing to our imports array here. So we're just going to say FormsModule. And now that we've done that, let's head back to our HTML file and finish it up. So we're going to add another div here, and then another label. And we're going to say Your message and we'll have a text area here, whose name is going to be equal to message, and we'll just say it has five rows, and 20 columns. And then we're going to use the ngModel thing for this as well. So we're going to say square brackets parentheses ngModel equals, and this is going to be equal to the message member variable from our component. And then at the bottom of our page, outside this last div here, we're going to say, button. And then in parentheses, we're going to say click, I'll explain this in a second. And we're going to say sendMessage with parentheses after it, and the button will say send on it. So the last thing to notice here, is this click thing in parentheses. As you may have guessed, this is how we hook into events such as clicks in Angular.
>
> **[8:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-contact-seller-page?u=76281980&t=512)** In this case, what we're saying is that we want the send button to call our components send message method, remember this method that we defined right here when it's clicked. And it's as simple as that really. So now we should be able to go back to our contact page and see that everything is working as it should be. We can input an email address of ours, and we can add something else to the message. And when we click send, it's just going to display an alert that says "Your message has been sent." And when we click Okay, it's going to navigate us back to our listings page.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Angular](../../Skills/Web%20Development/Angular.md) (6), [HTML](../../Skills/Web%20Development/HTML.md) (5), [Forms](../../Skills/Web%20Development/Forms.md) (3), [Firebase](../../Skills/Web%20Development/Firebase.md) (1)
> **Code Identifiers:** ngmodel (4), sendmessage (2), parammap (1), fakelistings (1), ngoninit (1)
> **Env Vars:** url (5), html (5)
> **Analogies:** just like (4), such as (1)
> **CLI Commands:** find (2), make (1)
> **Cross-References:** earlier in (1), go back to (1)
> **UI Navigation:** select the (1), navigate to (1)
> **File Paths:** app.module.ts (1)

#### [Create a My Listings page](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-my-listings-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-my-listings-page?u=76281980&t=0)** - Okay, so far we've made it through the whole listings page flow. We have the listings page itself along with the listing detail page and the contact seller page. So now what we're going to do is we're going to move over to the other side of things. We're going to create the pages that will allow users to view, create, and edit their own listings on the site. The first of these pages we're going to implement is the My Listings page, so let's go back to our IDE and open those files. We're going to open the typescript file and the [HTML](../../Skills/Web%20Development/HTML.md) file. So this page will just display a list of the listings that a user has created. And as with the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our data on the front end so far, later on in the course we'll be loading a user's listings from the back end, but for now, we're going to import them from our fake data file. So inside our typescript file, let's say import fakeMyListings from fake-data, and then we'll import the listing type as well. Import { Listing } from "../types." And for now, the implementation of this component is going to be pretty boring. Mostly this page will just link to our other pages for creating or editing listings, so here's what our component will look like. We're going to start off by giving it a listings member variable, which is going to be an array of type listing and then inside ngOnInit, we're going to say, this.listings = fakeMyListings. And then we're just going to create a method
>
> **[1:33](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-my-listings-page?u=76281980&t=93)** for deleting our listings, which will look like this. We're going to say, onDeleteClicked, listingId will be a string, and it's going to return void. And for now, this method won't actually delete anything yet. We'll get to that when we add a back end to our application. For now, we're just going to say alert and back ticks, deleting your listing with id. ListingId. And that's it for our component's typescript for now. So let's head over to the HTML file, and it's going to look something like this. We're going to say div class="content-box" And we're going to use another structural directive here to map over all of our component's listings, so we're going to say *ngFor="let listing of listings"
>
> **[2:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-my-listings-page?u=76281980&t=154)** and then we're just going to display some very basic information about our listing. So, we'll have an h3 heading with the listing's name, so listing.name inside curly braces. Then we'll have the listing's description, so listing.description. And then, we'll have a dollar sign for a listing.price. And then we're going to have two buttons for each listing. One that will allow us to edit the listing and one that will allow us to delete the listing. So here's what that's going to look like. We're going to say div class="buttons-container" This is for styling purposes here and then an anchor tag with a routerLink =edit-listing. This will link to our edit listing page with the correct listing id. And inside there, we're going to have a button that says Edit. And then we're going to have a button underneath that that actually has something linked up to its click method here. So we're going to say (click)="onDeleteClicked(listing.id)"
>
> **[3:58](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-my-listings-page?u=76281980&t=238)** and this button will have the text Delete on it. And that should be it for a my listings page implementation. So let's head over to our application, and we see that it's working just like we want it to. If we click on edit, it links to our edit listing page. And if we click on delete, it shows us an alert saying, "Deleting your listing with id 789." And we click OK. And of course, once we actually hook the front end to our back end, it'll actually delete those listings. But for now, it just shows that alert.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Code Identifiers:** fakemylistings (2), ondeleteclicked (2), ngoninit (1), listingid (1), ngfor (1)
> **Env Vars:** html (2), ide (1)
> **UI Navigation:** click on (2), open the (1)
> **Cross-References:** go back to (1)
> **Analogies:** just like (1)
> **Speakers:** - okay (1)

#### [Create a New Listings page](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-new-listings-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-new-listings-page?u=76281980&t=0)** - [Instructor] Okay, next let's implement the new listing page. So first of all, we need to actually add a button for getting to the new listing page. And for that, we're going to go back to our my-listings-page-component.[HTML](../../Skills/Web%20Development/HTML.md) file. At the bottom, underneath this div that we're using the structural directive on, we're just going to add an anchor tag with the router link equal to /new-listing, and inside that we're just going to put a button that says, Create New Listing. And now we have this button that we should be able to click and it takes us to our new listing page. So let's implement this page. We'll start off by opening up our new listing page, html, and TypeScript files. And the implementation of this component for now will be very similar to what we saw with our contact page component. It's going to contain a form with text inputs whose values will be linked to member variables inside this TypeScript file. So what we're going to do first is define those member variables. Each of these is going to be linked to a given text input on our page. So we're going to say name, and this is going to be a string. It's going to start off as an empty string, and then we're going to say description, and the same thing for that, and price is going to be a string right now, as well. And then up at the top of our component,
>
> **[1:36](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-new-listings-page?u=76281980&t=96)** we're going to import router from [Angular](../../Skills/Web%20Development/Angular.md)/router. We're going to be using this to navigate programmatically, just like we did in the contact seller page. And inside this constructor we're going to say, private, router, router. And then the last thing we're going to do inside this TypeScript file is define a new method for our component called onSubmit. And it's going to be void. And inside this, what we're going to do for now is we're just going to display an alert that says, Creating a new listing, and then underneath that we're going to use this router provider here to navigate back to our My Listings page. So we'll say this.router.navigateByUrl slash my-listings.
>
> **[2:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-new-listings-page?u=76281980&t=154)** So, there's nothing too strange in this TypeScript file. Let's move on to our html file now. So we're going to delete that boilerplate code there, and we're going to say div class equals content-box, and an h2 heading that says, Create New Listing, and then we're going to have a form here. And then in parentheses we're going to say, ngSubmit equals onSubmit. And what this is doing is saying that when the user clicks the submit button on our form it'll call the onSubmit method of our component. This is very similar to the click method that we've seen on buttons. So anyway, the implementation of this form is going to look like this. We're going to have a div and a label inside of it that says for equals name, and that'll say name. And underneath that, we're going to say input id equals name, name equals name, and type equals text. And then we're going to use the ngModel thing again, so square brackets, parentheses, ngModel. And we're going to link the value of this text input to the name member variable inside our TypeScript file, and that's it for that input. The other inputs are going to look pretty much the same as this. We're going to have a div that has a label for description,
>
> **[4:11](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-new-listings-page?u=76281980&t=251)** and we're going to say, Item Description here. And then we're going to say id description, name description, type equals text, and ngModel is going to be description this time. And last but not least, we're going to have text input for our price, so we'll say label for price, price, id price, name price, and ngModel, we're going to link that to the price member variable. And then underneath all of those, we're going to have the submit button for our form. Says button, type equals submit, and we'll make the text of that say, Create Listing. So that should be all we need for our new listing page. And now if we head over to our new listing page again, we see that we have this form that we can use. So we can just fill out some basic data, click on create, and actually that's not going to work because I typed this in wrong. It should be ngSubmit. That's one of my very common typos there. Save that, and now... Whoops, we need to fill out that info again. And if we click Create Listing now, we get an alert that says, creating new listing, and if we click okay then it takes us
>
> **[5:44](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-new-listings-page?u=76281980&t=344)** back to our My Listings page.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (3), [Angular](../../Skills/Web%20Development/Angular.md) (1)
> **Code Identifiers:** ngmodel (4), onsubmit (3), ngsubmit (2), navigatebyurl (1)
> **Analogies:** similar to (2), just like (1)
> **File Paths:** my-listings-page-component.html (1)
> **CLI Commands:** make (1)
> **UI Navigation:** click on (1)
> **Exercise Files:** boilerplate (1)
> **Speakers:** - [instructor] (1)

#### [Create a Listing Data form](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listing-data-form?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listing-data-form?u=76281980&t=0)** - [Instructor] The next thing we're going to do is move on to implementing the edit listing page. Now, the edit listing page component will look very similar to the new listing page. In fact, it'll look so similar to this page, that it would really be a shame to waste so much code, implementing basically the same exact component. So, what we're going to do instead, is create an entirely separate component for the form that both of these pages will have in common. And then simply reuse this form on both pages, instead of duplicating the logic in [HTML](../../Skills/Web%20Development/HTML.md). So, what we're going to do here first, is open up a new terminal and we're going to generate a new component called listing data form, by saying NG generate component, listing data form, and hit enter. And now that we've created that component, the first thing we're going to do is see how to actually display it on our edit listing page. So let's open up our edit listing page components, HTML. And this is pretty simple really, let's just add a few things here. We're going to say div class equals content box. We'll have an H2 heading, that says edit listing. And then, to insert our listing data form into this component. We're going to say app listing data form, just like that. And now, if we take a look at our edit listing page, we should see this little listing data form works message here.
>
> **[1:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listing-data-form?u=76281980&t=92)** So, now let's actually implement our listing data form component. Let's open up both the HTML and TypeScript files for it. And really, what we can do here is open up some of the files from our new listing page component, the TypeScript and the HTML files. And we can copy a lot of that code into our listing data form component. So for example, we're going to copy this router thing here, and we're going to put that inside our listing data form component's, TypeScript file. And then we're going to copy basically the whole body of this component here, and paste that into our listing data form component's body. Just like that. And then we're going to do the same thing with the HTML. So we're going to go into our new listing page components, HTML file. And we're going to copy this whole form component. We're not going to copy the outer div or H2 tags. And then inside our listing data form component's, HTML file, we're going to paste all of that HTML and save it. And now if we look back at our edit listing page, we see that it has the form and the button that we want it to have. However, before we get too smug here, there are a few things that aren't quite right about this setup yet. For one, we need to display different texts on the form button, depending on where we're actually displaying it from. Right now, it just has this create listing thing, hard coded into it.
>
> **[3:05](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listing-data-form?u=76281980&t=185)** And the second thing that we need to do is allow whatever component is displaying the listing data form component, to handle the on submit action itself. Cause right now the listing data form is set up to just create new listings, right? If we click on this button, it'll display creating a new listing, even though we're on the edit listing page. So how do we do things like this in [Angular](../../Skills/Web%20Development/Angular.md)? Well, actually this brings us to a very important concept called inputs and outputs in angular. Notice how, what the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the HTML elements on our page, we're able to use attributes to customize the elements in some way, whether that's by assigning a class to an element or applying inline styling or a lot of other things. And also notice how we've been able to tap into things like button click events, or submit events, with this parentheses syntax here. And as a matter of fact, it's possible to do these same things with the components that we create. So let's see how to do this.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (9), [Angular](../../Skills/Web%20Development/Angular.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Env Vars:** html (9)
> **Analogies:** just like (2), similar to (1), for example (1)
> **Prerequisites:** setup (1), set up (1)
> **Tools:** terminal (1)
> **UI Navigation:** click on (1)
> **Speakers:** - [instructor] (1)

#### [Create an Edit Listing page](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980&t=0)** - [Instructor] The first thing we're going to look at is inputs. Basically what inputs allow us to do is pass data into our components from their parent components in a way that looks almost identical to defining attributes on simple [HTML](../../Skills/Web%20Development/HTML.md) elements. In our case, let's say that on our edit listing page, we want to be able to pass in an input to our listing data form component that defines what text is displayed in the [Forms](../../Skills/Web%20Development/Forms.md) button. Something like this, for example. We could say button text equals save changes. Well, in order to make this work, we have to go into our listing data forms TypeScript file, and up at the top, along with component and OnInit, we're going to import something called input. And then down inside the body of the component, we're going to use this funny looking syntax to define an input called button text. So we'll say at input with parentheses after it button text, and then inside our listing data forms HTML file, instead of having this create listing text hard coded here, we're going to use our double brackets and save button text. And now if we go back to our edit listing page here, we see that it says save changes, which is the prop that we pass through from our edit listing page components HTML. So that's the very basics of inputs there. What about outputs? Well, here's how we do that. Inside our listing data forms TypeScript file again, we're going to import two more things up here.
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980&t=94)** One is going to be Output and the other is going to be EventEmitter and we'll save that file. And we're also going to want our listing type. Import listing from types. And then in the body of our component, we're going to define a new output called onSubmit, which will look something like this. We'll say at output with parentheses after it, onSubmit equals new EventEmitter of type listing.
>
> **[2:15](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980&t=135)** And then we need to change the name of this components on submit method to something like on button clicked or something like that. And instead of displaying an alert and navigating inside this method, we're going to simply say this.onSubmit which is the output we just defined. .emit And we're going to define the data that we want to send to whatever component is listening. In our case, it's going to be ID. Null name is going to be the current value of the name input. The description is going to be the current value of the description input, and the price is going to be the current value of the price input converted to a number since that's what we defined in our TypeScript file. And what this will do again, is allow parent components to use the parentheses syntax in their HTML files to tap into whenever this component calls onSubmit.emit like this. And we'll see how this works exactly. But first we have to go back into our listing data forms HTML file. And up here, we're going to change this from onSubmit to onButtonClicked since we changed the name of that method there, remember, and that should be all we need. Now, what we can do is go into our edit listing component and connect the output we just defined to our edit listing pages own onSubmit method.
>
> **[3:48](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980&t=228)** So here's what that'll look like. We have our button text input, and we're going to say onSubmit, this is how we tap into that output there. And whenever we receive an onSubmit action, we're going to say onSubmit and call our components own onSubmit method, which we defined. We have to open up our edit listing page TypeScript file. And actually we haven't defined it yet. We're going to have to define a method called onSubmit. And what this is going to do is just display an alert, saying saving changes to the listing. And then it's going to say this.router.navigate by URL /my-listings. We're going to go back to my listings page after the alert is displayed. And then we have to actually import the router. Import router from [Angular](../../Skills/Web%20Development/Angular.md)/router and provide that in the constructor here, private router router, and this should work now. If we go back to our edit listing page and click on save changes, we should see that it now says saving changes to the listing, which means that the event, the submit event from this form here is sort of bubbling up to our edit listing page and being handled by this onSubmit
>
> **[5:23](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980&t=323)** method that we just defined. And while we're at this, let's make these same kind of changes to our new listing page component to make it use our new listing data form component. So we're going to open up our new listing page component, and we're going to delete this form and add the app listing data form component with a button text input that says create listing and on output that taps into the onSubmit output of the listing data form and calls this component's own onSubmit method. And inside the TypeScript file for our new listing page, we can delete these things since we no longer need them and let's make sure that all works still. So let's go into our create new listing file. Just click on create listing, it'll say creating a new listing, which is exactly what we want, and it will navigate back to the my listings page. So this is all working great so far, but the last thing that we want to do for our edit listing page is to set the initial values of the forms fields to the current values of the selected listing, right? So when we click on the bicycle listing here and click edit, it should populate that with bicycle and the item description and price so that we can see what actual changes we're making to the item we're editing.
>
> **[6:57](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980&t=417)** So the way that we're going to do that is by adding three more inputs to our listing data form component. I'm just going to clean up all these tabs here. And we'll open up our listing data form component again. Open up the TypeScript file. And up here above our member variables, we're going to define three more inputs. We're going to have an input called currentName, an input called currentDescription, input currentDescription, and an input called currentPrice. And then what we're going to do is inside our ngOnInit method of our listing data form, we're going to say this.name equals this.currentName. this.description equals this.currentDescription and this.price equals this.currentPrice. So basically we're just setting the initial values of our text inputs to whatever gets passed in through these inputs here. And just one more thing is we're going to want to default values for these inputs in case the parent component doesn't pass those in. So we'll just make that an empty string for all of these. And now what we're going to do is open up our edit listing page, open up the TypeScript file and the HTML file. And then inside the TypeScript file, what we're going to do is find the appropriate listing based
>
> **[8:31](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980&t=511)** on the ID URL parameter. This one up here, this is pretty much the same that we've seen in several other components. So what we're going to do is say import activated route, and we're also going to import our listing type. Import listing from types. And we're going to import our fakeMyListings from fake data.
>
> **[9:03](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980&t=543)** And then inside our constructor, we're going to say private route activated route, and inside our ngOnInit method, we're going to get the ID from the URL parameter. So const ID equals this.route.snapshot.paramMap.get id.
>
> **[9:26](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980&t=566)** And then we're going to say this.listing. We actually have to define that listing number variable first. We're going to say listing listing, and then say this.listing equals fakeMylisting.find and find the my listing who's ID matches the ID URL parameter. And then last but not least, what we're going to do is we're going to go into our edit listing pages HTML file. And we're going to pass the listings data into the inputs that we defined on our listing data form. So that'll look like this. We're going to have square brackets for these. I'll explain why that is in a second. So we're going to say currentName equals listing.name,
>
> **[10:19](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980&t=619)** currentDescription equals listing.description.
>
> **[10:29](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-edit-listing-page?u=76281980&t=629)** currentPrice equals listing.price. And that's it. Now, the reason we need these square brackets is because we want to pass the actual values of these variables through to the inputs. In our case, since these are all strings that we're passing, this would be pretty much the same thing as if we had just said currentName equals listing.name with curly braces. But in the case that we want to pass through some other type of data, like an object, the way we do that is by putting square brackets around the input name, and then just referencing the member variable that we want to pass through. So now, if we go back to our edit listing page for one of our listings, we'll see that it automatically fills the field with the current values, which is exactly what we wanted it to do.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (7), [Forms](../../Skills/Web%20Development/Forms.md) (6), [Angular](../../Skills/Web%20Development/Angular.md) (1)
> **Code Identifiers:** onsubmit (14), currentname (4), currentdescription (4), currentprice (3), ngoninit (2)
> **Env Vars:** html (7), url (4)
> **CLI Commands:** make (5), find (3)
> **Cross-References:** go back to (4)
> **UI Navigation:** click on (3)
> **Definitions:** means that (1)
> **Analogies:** for example (1)

#### [Create a navigation bar](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-navigation-bar?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-navigation-bar?u=76281980&t=0)** - [Instructor] So we've got the basic framework for all of our pages set up at this point. The last piece of the front end we're going to create now is the nav-bar. This will be a very simple component that's going to be displayed regardless of the route the user is currently on. We'll see how to do that in a second. First, let's start off by creating our component and to do that inside a terminal, we just have to run ng generate component nav-bar and hit enter, and this will generate the necessary files for us. The only one we're going to need to edit here will be the [HTML](../../Skills/Web%20Development/HTML.md) file. So let's open that up, nav-bar.component.html, and we're going to add some stuff to it like this. We're going to say div id equals top-bar, and we're going to have a link, routerLink, and it's going to link to our listings page and what this is going to be is the heading of our application. So we're just going to have an h1 heading with the id app-heading, again, that's for styling purposes. And we're going to make that say buy and sell. And then we're going to have another link that links to the my listings page. So we're going to say routerLink equals my-listings, and this is going to be a button with the id of my-listings-button. And inside that,
>
> **[1:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-navigation-bar?u=76281980&t=92)** it's just going to say my listings and we'll save it. So that's all we need for our nav-bar. Now, what we're going to do is put this component inside our app component. So let's open that up. We're looking for app.component.html. And what we're going to do is above our router outlet, we're going to say app-nav-bar, which will insert our nav-bar into the HTML of our app component. And lastly, for styling purposes, we're just going to add a few more divs here. We're just going to say div id equals page-wrap, div id equals max-width-column, and we're going to put our nav-bar and router outlet inside this div, and then surrounding our router outlet we're going to have one more div that says id content-wrap, and we'll put our router outlet inside of there. And that's it. If we look at our app now, we'll see that it's looking really good and we can click both parts of our nav-bar to get to different places in our app. So we can click our my listings button here and it'll take us to our my listings page and we can click the buy and sell heading up here and it will take us to just our regular listings page.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (4)
> **File Paths:** nav-bar.component.html (1), app.component.html (1)
> **Code Identifiers:** routerlink (2)
> **Env Vars:** html (2)
> **CLI Commands:** make (1)
> **Tools:** terminal (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)


### 2. Creating a Node Back End

[↑ Back to Table of Contents](#table-of-contents)

#### [Why Node.js?](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/why-node-js?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/why-node-js?u=76281980&t=0)** - [Instructor] Now that we've got our client side working nicely, it's time to start building the backend of our blog and to do this, we're going to be using [Node.js](../../Skills/Web%20Development/Node.js.md). Now you've probably heard about Node.js by now, and it's very important to us because it allows us to write our backend in [JavaScript](../../Skills/Software%20Development/JavaScript.md), instead of [PHP](../../Skills/Software%20Development/PHP.md) or [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) or [Java](../../Skills/Software%20Development/Java.md). Now it used to be that in order to be a full stack developer, you had to be fairly fluent in more than one programming language, but with Node.js, which has become an incredibly popular platform since it was released, our entire web application can be written in JavaScript, which is great news for us JavaScript developers. Now there are many different [npm](../../Skills/Web%20Development/npm.md) packages that can be used to write Node.js servers and the one that we're going to use in these videos is called hapi. Hapi is a good choice for us because it's popular, it's easy to get started with, and it's also very well documented. And additionally, there are a ton of add-ons that can be used to extend its functionality, which, as you'll see later on, really comes in handy. So without further ado, let's get started.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Node.js](../../Skills/Web%20Development/Node.js.md) (4), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (3), [PHP](../../Skills/Software%20Development/PHP.md) (1), [Python](../../Skills/Software%20Development/Python%20(Programming%20Language).md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **CLI Commands:** node (4), php (1), python (1), npm (1)
> **File Paths:** node.js (4)
> **Env Vars:** php (1), npm (1)
> **Definitions:** is called (1), is a  (1)
> **Speakers:** - [instructor] (1)

#### [Set up a hapi server](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-hapi-server?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-hapi-server?u=76281980&t=0)** - [Instructor] To get started creating a backend for our app with node JS in Hapi, let's create a new directory outside of our front-end directory. Since our front-end code is in a directory called buy and sell, we're going to call this directory buy and sell backend. So we're going to click new folder and say buy-and-sell-backend and click Create and then open. And just as a side note here, in theory, we could have the backend code inside of our original directory, but in practice, it's usually just easier to keep the front-end and backend code of our app separated, especially as our code base gets larger and more complex. So inside our directory, let's open up a terminal and we're going to create a new [npm](../../Skills/Web%20Development/npm.md) package by typing npm init-y and hitting Enter. And once we've done that, let's install hapi into our project by saying npm install @hapi/hapi and hit Enter. And then we're going to create a new directory inside of our project called source and inside of that, we're going to create a new file called server.js. This file will be the entry point for our app, but before we write any code in here, there's something we need to do first. You see currently, node JS doesn't have native support for [JavaScript](../../Skills/Software%20Development/JavaScript.md)'s modern ES6 syntax and things such as import and export. So we'll have to make a few small changes to allow us to write our backend using modern JavaScript.
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-hapi-server?u=76281980&t=94)** We didn't have to do this for our front-end because this is something that the [Angular](../../Skills/Web%20Development/Angular.md) scripts take care of when we create a new project. So anyway, the first thing that we have to do is install a few packages from Babel, which if you haven't heard of it before, it's a transpiler that allows us to write our code base in modern JavaScript syntax, and still have it work on systems where modern JavaScript isn't supported natively. So in our terminal, we're going to run npm install--save-dev @babel/core @babel/node @babel/preset-env
>
> **[2:12](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-hapi-server?u=76281980&t=132)** and @babel/plugin-transform-runtime and hit Enter.
>
> **[2:22](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-hapi-server?u=76281980&t=142)** And once that installs, there's one more Babel package that we want to install. So we're going to npm install @babel/runtime, which is just the production version of one of the other packages that we install for development. So the next thing that we're going to do is create a new file in the root directory of our project called .babelrc. So .babelrc, and this is basically where we tell Babel how we want it to transform the modern JavaScript code that we write into JavaScript that node JS can execute. Fortunately, this is pretty simple. This is going to be a [JSON](../../Skills/Web%20Development/JSON.md) object, and the first property is going to be presets and that's going to have an array and the only thing in that array is going to be @babel/preset-env. And then we're going to have another property that's called plugins, and this is going to be an array and inside that we're going to have another array that'll have @babel/plugin-transform-runtime,
>
> **[3:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-hapi-server?u=76281980&t=212)** and an object containing the property regenerator true. Don't worry too much about this stuff, it's basically just boilerplate code that will make it so that our server will run correctly. So we're going to save that file and now we're free to start writing our app in modern JavaScript. So let's go back to our server.js file here and we're going to write our first bit of server-side code. We're going to start off with an incredibly simple Hapi server that just sends us a message when we send a request to it. So the first thing we're going to have to do is import Hapi we're going to say import Hapi from @hapi/hapi. And then setting up a basic Hapi server looks something like this. This is just boilerplate code, don't worry too much about the details here. We're going to say const and create a function called start, and this is going to be an async function. And inside there, we're going to create our server by saying const server equals Hapi.server and then we parse it a configuration object, which contains the port that we want our server to run on. In our case, that'll be 8000, and the host that we want it to run on, which in this case will be localhost. And once we've created the server, we're going to say await server.start, which will start up our server and then we're simply going to log something to the console saying, server is listening on and then we're going to say server.info.uri,
>
> **[5:08](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-hapi-server?u=76281980&t=308)** which will give us the basic path to access the server from. And now that we've created this start function that we can call to start up our server, what we're going to do is add another thing here that says process on unhandledRejection. This will just listen for unhandled rejections in our node server. And we're going to give it a callback where we simply log out whatever error happened, so console.log error, and process.exit with exit code one.
>
> **[5:43](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-hapi-server?u=76281980&t=343)** And last but not least, we're going to call the start function that we created here to start up our server. So let's save that for now. So now that we have this server set up to run, we can define different end points and what we want it to do when one of those end points is hit. So let's start off very simply. Let's make it so that when our app receives a GET request on an end point /hello, it simply responds with a message saying hello. Now to make our Hapi server do that, all we have to do is this, before where we say await server.start, we're going to say server.route, and then parse a configuration object for the route we want to define. This will contain a few properties such as method, which is the method that we want this to listen for. We're going to say GET and then the path that we want it to listen at, we're going to say /hello and then we're going to define a property called handler. Now this handler property is a callback that'll get called whenever our server receives the correct type of request on the end point that we specified in this path property. Now this callback has two main arguments, it has a request object, which contains details about the request that we received and then it's got what's called a response toolkit, which just by convention uses the letter H. We can use this argument to customize the response we send back to whoever sent the request in the first place. And for now, we're not going to be using either of these arguments, but we'll see how to do so very shortly.
>
> **[7:18](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-hapi-server?u=76281980&t=438)** Now, the key thing to note for our handler is that whatever we return from our handler will be sent back to the client as the response. So for example, we could say return and then the string hello and what this will do is send back a response to the client with the string hello. And for those of you who might be wondering, when we just return a response like this, Hapi automatically provides a 200 status code on our response. If you wanted to set your own custom status code, that would look something like this. We could say return h.response, hello, and then say .code, and then whatever code we want it to send back. But for now, we don't need all of that, a 200 status code is fine. And that's all there is to it really. All we have to do now is have node JS run our Hapi server. So let's open up a terminal here. And since we're using Babel to run our server, the command that we use for this isn't going to be just node source/server.js, instead, we're going to use the command npx babel-node source/server.js, and then we'll hit Enter, and we'll see the message from the callback telling us that the app is listening on port 8000. And now all we have to do is go to our browser, type in localhost8000/hello and hit Enter, and we'll see the response from our Hapi server
>
> **[8:50](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-hapi-server?u=76281980&t=530)** as we hit the hello end point with our browser.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (7), [npm](../../Skills/Web%20Development/npm.md) (5), [Angular](../../Skills/Web%20Development/Angular.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **CLI Commands:** node (8), npm (5), make (4), npx (1)
> **Prerequisites:** install (7), set up (1)
> **File Paths:** server.js (2), source/server.js (2)
> **Tools:** terminal (3)
> **Analogies:** such as (2), for example (1)
> **API Endpoints:** get  (2)
> **Env Vars:** es6 (1), json (1)

#### [Test a hapi server with Postman](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/test-a-hapi-server-with-postman?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/test-a-hapi-server-with-postman?u=76281980&t=0)** - [Instructor] So as we move forward with our server development, it's going to be very important for us to have an easy and reliable way to test it. So far, just typing the URL into our browser has worked. But for some of the complicated operations we're going to want to implement for our app, this won't quite be good enough. What a lot of people do to test their backend is to develop a feature on their front end first, and then use that to test the backend. But in practice, I found that it's generally much easier just to develop the front end and backend in isolation and then bring them together. And that's what we're going to be doing in this course. So a tool that's used by many developers for doing this sort of backend only testing is a free piece of software called postman. So if you don't already have postman, you can download it from the link that you see here. So once you've downloaded and installed postman, let's use it to query our backend. So you're going to want to make sure your server is running. And then we're going to open up postman and make sure we're sending a GET request here to the URL localhost 8,000 slash Hello. And just for those of you who aren't familiar with different request types such as GET and POST, all you need to know for this course is that GET requests like the one that we're going to be sending here, are usually used to get information from the server such as in our application, we're going to be getting listings. And POST requests are usually used to modify something on the server such as when we want to create a new listing or edit an existing listing. And additionally, POST requests have the ability to carry extra information
>
> **[1:33](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/test-a-hapi-server-with-postman?u=76281980&t=93)** in something called a request body, while the only information they GET requests carry is contained in the URL. And don't worry if you don't quite understand all of this. You'll be able to get along very well without a full knowledge of network requests. So we're going to want to make sure GET is selected there and then we'll click Send. And we're going to see that we get the same response back here as we got when we used our browser to send a GET request. So that's how to send a GET request to our server. Let's take a look at a quick example of making a POST request to our server. Now POST requests as I mentioned before, allow us to pass extra data along to our backend. So why don't we head over to our server file here? And what we're going to do is change this route. We're going to change the method to POST. And what we're going to have this route do is we're going to be able to send our name along to it in the request body and it'll reply back Hello, with our name after it. So here's what that's going to look like. If we go back to postman, and set our request to a POST request, we're going to click on this body tab here, which is where we can define what extra data we want to send along with our POST request. And we're going to make sure [JSON](../../Skills/Web%20Development/JSON.md) is selected from this little drop down here. And what we're going to do is send along a simple jSON object with a property that says name and I'm going to put my name here. And then in our server, in order to access that name, what we're going to be able to do is say const, payload equals request.payload.
>
> **[3:11](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/test-a-hapi-server-with-postman?u=76281980&t=191)** And then we can say const name equals payload.name. And that will get the value of this property that we included along with our request. So what we can do now is change this to use back ticks. And we'll say hello, name and that should be all we need to do. So now let's restart our server. We need to restart it in order to make our changes take effect. And we're going to head back over to here and send this post request that we've created here. Click Send. And we'll see that down at the bottom, we get this response that says, Hello Shawn, or Hello whatever your name is. And that's exactly what we wanted. So that's how to test GET and POST requests using postman. We'll be using this to build out the other end points of our backend throughout this course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (2)
> **API Endpoints:** get  (7), post  (7), get
 (1)
> **Env Vars:** post (9), url (3)
> **Tools:** postman (6)
> **CLI Commands:** make (5)
> **Analogies:** such as (3)
> **Code Identifiers:** json (2)
> **Cross-References:** go back to (1)

#### [Create a listings endpoint](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listings-endpoint?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listings-endpoint?u=76281980&t=0)** - [Instructor] So what we've seen so far has been a really simple example, but now that we understand the basic mechanics of adding routes to our hapi server and interacting with those routes using Postman, we can move on to making our server do some more helpful things. Now, the first of those things we're going to do is add a listings endpoint for our front end listings page to load data from, but before we create our routes, let's do what we did with our front end and copy and paste some fake data into our server. Basically, what we can do is just take the fake data from the front end and put it into a new file in our backend. Now, the first thing we're going to do is create a new folder in our source directory. We'll call this routes, and as you might've guessed, this is the directory that will hold all the files for our server's routes, and then inside this routes thing, before we actually create any routes, we're going to create a new file called fake-data.js, and then we're going to copy and paste the data from our front end into here and since we're not actually writing our server code in TypeScript, let's just remove these types here and now we're good to go. Keep in mind of course, that in a little bit, we'll be adding database functionality to our servers. So we'll only be using this fake data until then. So now let's move on to creating our first real server route. Now when creating a bunch of different routes for a server, I'd highly recommend that we create a new file for each route. This is usually considered something of a best practice
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listings-endpoint?u=76281980&t=94)** since it helps us keep all the different routes of our application separate. So for our listings route, let's create a new file inside our routes directory. We'll call it, getAllListings.js, and then inside it, we're going to define our route. This is actually going to be an incredibly simple route for now. We're going to start off by importing our fake listings from our fake data file and then we're going to say, export const getAllListingsRoute equals, and the method for this route is going to be get, the path for this route is going to be /api/listings and we're going to see later on in the course, why exactly it is that we're starting each of our routes with /api here, for now just make sure to do it, and then for our route handler, we're going to say request and H and all this route is going to do is just say return fakeListings. That's it, very simple route here. So now that we've defined this route, we need to actually add it to our server. So here's how we're going to do that. Instead of explicitly importing each and every route into our server.js file, I'm going to show you the way that I usually tend to do things. What we're going to do is inside our routes file, we're going to create an index.js file and then what we're going to do is import the route we just created. So we'll say import getAllListingsRoute
>
> **[3:10](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listings-endpoint?u=76281980&t=190)** from .getAllListings and then what we're going to do, is we're going to export an array that will eventually contain all of our routes. For now, it'll just contain our getAllListingsRoute. So we'll say export default getAllListingsRoute and now that we have this, let's save that file and open up our server.js file. and what we're going to do is up at the top, we're going to say import routes from ./routes which we'll import that array we just exported and then we're going to remove this hello route because we don't need that anymore and then what we're going to do is we're going to use for each to loop through all our routes and add them to our hapi server like this. We're going to say routes.forEach and for each route, we're going to say server.route and add our route. So simple as that. Okay and that should be it. Now if we restart our server and there's one more thing that I'm remembering in the fake-data.js file here, we need to remove this listing array type as well, otherwise we'll get an error and anyway, now if we restart our server, what we should be able to do is go into Postman, change it to get and send a request to localhost8000/api/listings and hit send
>
> **[4:46](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-a-listings-endpoint?u=76281980&t=286)** and we should see that we're getting our fake data back from the server, which is exactly what we wanted.

> [!info]- Semantic Content
>
> **Code Identifiers:** getalllistingsroute (4), getalllistings (2), fakelistings (1), foreach (1)
> **File Paths:** fake-data.js (2), server.js (2), getalllistings.js (1), index.js (1)
> **API Endpoints:** /api/listings (2)
> **Tools:** postman (2)
> **Best Practices:** best practice (1), make sure to (1)
> **CLI Commands:** make (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)

#### [Restart a Node server automatically](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/restart-a-node-server-automatically?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/restart-a-node-server-automatically?u=76281980&t=0)** - [Instructor] So you may have noticed that until now we've had to manually restart our server every time we make a change to our code in order for those changes to take effect. And wouldn't it be nice if our server could sense when our files changed and automatically restart itself? Well, as a matter of fact, there's an [npm](../../Skills/Web%20Development/npm.md) package that does this for us, and it's called no demon. Let's install this package into our project like this. We're going to say npm install --savedev, nodemon and hit enter, and once that installs, all we have to do is change the command that we use to run our server. Instead of running npx babel-node src/server.js, we're going to write npx nodemon
>
> **[0:52](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/restart-a-node-server-automatically?u=76281980&t=52)** - -exec npx babel-node src/server.js. The command is a little longer, but it saves us a lot of work in the long run, and then hit enter, and what we're doing here is telling no demon to rerun whatever command we put after this exec flag, whenever our files change. So in other words, it's going to run npx babel-node src/server.js every time the files in our project change, which is really useful for us. And if we go back to Postman and hit send again, we see that we're still getting back what we want, but if we were to change this route to something, say, just return Here are the listings, or something like that, and then send another request, we see that it updates automatically without us having to manually kill and restart the server. So let's change that back. There we go. And I'll admit that this no demon command might be a little long to remember. So while we're at it, we're going to make one more small change to our code base. If we open up our package.[JSON](../../Skills/Web%20Development/JSON.md) file, we'll see that there's this script object with a default test script inside it. Now you may or may not be familiar with this part of package.json, but these scripts are basically just aliases for longer commands, and we can create our own scripts and call them whatever we want. What we're going to do here is create an alias for our long no demon command. So we're going to call our script dev,
>
> **[2:28](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/restart-a-node-server-automatically?u=76281980&t=148)** and then for our dev script, we're just going to run npx nodemon --exec npx babel-node src/server.js, and as a matter of fact, we can actually remove this npx here, since this is inside our package.json file. And don't forget to add the comma at the end here, and that's it. So now if we want to run this command, what we can do instead of typing the whole thing out is just type npm run, and then the name of the script we just created, so dev, and it'll run it for us. And from now on, that's what we'll be using to run our dev server since it's a lot easier to remember and a lot shorter to type.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [npm](../../Skills/Web%20Development/npm.md) (3), [JSON](../../Skills/Web%20Development/JSON.md) (3)
> **CLI Commands:** npx (7), node (4), npm (3), make (2)
> **File Paths:** src/server.js (4), package.json (3)
> **Definitions:** is a  (1), in other words (1)
> **Prerequisites:** install (2)
> **Env Vars:** npm (1)
> **Cross-References:** go back to (1)
> **Tools:** postman (1)

#### [Create an individual listing endpoint](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-individual-listing-endpoint?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-individual-listing-endpoint?u=76281980&t=0)** - [Instructor] So far, we've only implemented one route on our server, a route that returns all the listings. And before we move on to actually incorporating an [SQL](../../Skills/Data%20Science/SQL.md) database into our back end, at which point we'll switch our existing routes over to use it as well as create several more, we're going to see how URL parameters work with Hapi servers by creating a route that will allow our front end to request data for individual listings by their IDs. So here's how we do that. First, inside our routes directory, we're going to create a new file called getListing.js. And then inside this file, we're going to create our route. We're going to say import fakeListings from ./fake-data.
>
> **[0:44](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-individual-listing-endpoint?u=76281980&t=44)** And then we're going to say export const getListingRoute. And this route is going to listen for the get method on the path /api/listings/{id}. And it's worth noting that in Hapi, We don't use the colon syntax for URL parameters. We instead use curly braces around the name of the parameter. And then for our handler, we're going to say request and H. And the way that we actually get access to the value of this ID URL parameter here is like this. We're going to say const id equals request.params.id. And then we're going to say return fakeListings.find, listing where the listings ID is equal to ID. This is pretty much the same exact thing that we saw on our front end. And now that we've created that route, we're going to go into the index.js file of our routes directory. And we're going to import our getListingRoute from getListing and then we're going to export that as part of this array here. So getListingRoute, like that. And we should have our server running in the background with the new dev script that we created in a previous video. So what we can do now is just go over to Postman
>
> **[2:20](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-individual-listing-endpoint?u=76281980&t=140)** and send a request to /API/listings/123 and hit Send. And we should see that it returns the corresponding listing. So that's the basics of using URL parameters in a Hapi server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [SQL](../../Skills/Data%20Science/SQL.md) (1)
> **Code Identifiers:** getlistingroute (3), getlisting (2), fakelistings (2)
> **Env Vars:** url (4), sql (1), api (1)
> **File Paths:** getlisting.js (1), index.js (1)
> **CLI Commands:** find (1)
> **API Endpoints:** /api/listings/ (1)
> **Cross-References:** previous video (1)
> **Tools:** postman (1)

#### [Errors in hapi using hapi/boom](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/errors-in-hapi-using-hapi-boom?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/errors-in-hapi-using-hapi-boom?u=76281980&t=0)** - [Instructor] So we've created two routes on our server so far. And before we continue on to adding a database and other routes, we're going to take a little detour and look at a very important concept when writing back in code, and that's how to work with errors. So Hapi's approach to this topic is a little different than what you might find on a lot of other service side frameworks. And most of it revolves around an [npm](../../Skills/Web%20Development/npm.md) package called boom, which we can install in our project by running NPM install at Hapi slash boom, and hit enter. Now, this package is our first experience so far with what's sometimes referred to as the Hapi ecosystem. You see, one of the really nice things about working with Hapi, over other possible frameworks like Express, is that Hapi has a pretty extensive collection of different official modules and plugins that allow us to add in functionality to our server without a whole lot of setup. If you're interested, you can find a list of all these modules and plugins as well as a lot more information about each of them on Hapi's website, which is [Hapi.dev](https://Hapi.dev). So anyway, this package in particular, the boom package that is, is used to make it easier to send back error responses to the client. In all of our server routes, if something bad happens during execution, we want to let the client know. And this could be an actual error such as if the server can't connect to the database, or if we accidentally pushed some code into production that doesn't work, or it could be something more expected, like if the client is trying to get information about a listing that doesn't exist. In which case we'd want to respond with a 404 error code. Now without the boom package that we just installed,
>
> **[1:36](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/errors-in-hapi-using-hapi-boom?u=76281980&t=96)** we'd be forced to set the error codes and other response headers ourselves, which can be a bit of a pain. So what boom does, is it provides us with a suite of very common errors that we might encounter on our server. And when we throw these errors from inside our route handlers, Hapi will automatically set the appropriate response headers in status code for us. So that might sound pretty complicated, but it's actually pretty simple. So let's take a look at a demonstration. In order to demonstrate this, let's open up our get-listing route and add in some error handling for what to do, if the client requests info for a listing that doesn't exist. In other words, if they send us an ID for which we have no matching listing. The first thing we're going to do is up at the top, we're going to import boom, like this. We're going to say import boom, from @hapi/boom. And then we're going to rewrite our route handler to look like this. Instead of just saying return fake listings.find We're going to define an intermediate variable here. So we're going to say const listings = fake listings.find, and find that matching listing. And then underneath that, we're going to say if that listing doesn't exist and actually we want to call this listing instead of listings, we're going to say if that listing doesn't exist, throw boom.notfound with a message that we can define. So for this, we're going to use back ticks, and we're just going to say, listing does not exist with ID, and then just insert the ID that the user sent.
>
> **[3:15](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/errors-in-hapi-using-hapi-boom?u=76281980&t=195)** So what's going on here is when we want to respond to the client with an error code such as 404, in this case, we simply use the throw keyword and then use the appropriate boom error. In this case we're using not found, but there are many others to choose from for different situations. And once we've done this, Hapi takes care of the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md). So the last thing we want to do in this route, now that we have that error set up is we're just going to say, return listing if the listing does exist. So let's say this and our servers should be running still. And now if we try and send a get request to API slash listing slash, and then put an ID that doesn't exist and click send, we see that we get back a 404 response that contains some other information as well, such as the message we pass to the boom dot, not found function. So that's the basics of using the boom package to send back error responses. All we have to do is say, throw boom, and then the corresponding error that we want to return to the client. And we'll see more examples of boom in some of the other end points that we're going to add to our server as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [npm](../../Skills/Web%20Development/npm.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** find (5), npm (2), make (1)
> **Prerequisites:** install (2), setup (1), set up (1)
> **Env Vars:** npm (2), api (1)
> **Analogies:** such as (3)
> **Definitions:** is a  (1), in other words (1)
> **URLs:** [hapi.dev](https://hapi.dev) (1)
> **Speakers:** - [instructor] (1)


### 3. Adding MySQL to Your Back End

[↑ Back to Table of Contents](#table-of-contents)

#### [Why MySQL?](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/why-mysql?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/why-mysql?u=76281980&t=0)** - [Instructor] So now that we have our client side built out and a rudimentary server with a few routes, it's time to start integrating a database into our backend. And this will enable us to store users' data and persist that data indefinitely. In this course, the database we're going to be using is [MySQL](../../Skills/Software%20Development/MySQL.md). Now, MySQL is an extremely popular [Database Management](../../Topics/Database%20Management.md) system and it's also open source, which means that it can be installed and used for free. First of all, as you might have guessed by the name, MySQL uses [SQL](../../Skills/Data%20Science/SQL.md) to make [Database Queries](../../Skills/Software%20Development/Database%20Queries.md). In practice, SQL queries can get really as complex as you need them to be but in this course we'll be keeping our queries as simple as possible. So don't worry if you're not already familiar with SQL. This will be a great opportunity to get your first contact with it. The second thing to know about MySQL [Databases](../../Skills/Software%20Development/Databases.md), if you haven't already worked with them, is that they're [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md). What this means is that the data they contain is structured into rows and columns. Now, rows are basically just individual records, such as users or in our case, listings. And columns denote the different pieces of data that each record has such as name, description, price, et cetera. The data in our database is also organized into different tables, each of which contains records that all have the same column attributes. So we might have a users table that contains all of our website's users and a listings table with information such as the listings ID, name, description, and price, as well as the user ID of the user who listed it and whatever other stuff we need to store about each of our listings. Now, you may have heard of a different kind of database
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/why-mysql?u=76281980&t=94)** called a non-relational or a [NoSQL](../../Skills/Software%20Development/NoSQL.md) database. Mongo DB is probably the most popular of these databases. In contrast with relational databases, which is what we're going to be using in this course, non-relational databases primarily store [Unstructured Data](../../Skills/Software%20Development/Unstructured%20Data.md). And usually what this means is that instead of storing data in rows and columns, the data in these non-relational databases is just stored in [JSON](../../Skills/Web%20Development/JSON.md) objects. And there are certain trade-offs to doing this, none of which is really going to be relevant for this course. For now, I just wanted you to be aware of the distinction here. At the end of the day, it's best just to try both ways and see which is best for you. But in this course, we're going to be using a relational SQL database.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [MySQL](../../Skills/Software%20Development/MySQL.md) (4), [SQL](../../Skills/Data%20Science/SQL.md) (4), [Databases](../../Skills/Software%20Development/Databases.md) (3), [Relational Databases](../../Skills/Software%20Development/Relational%20Databases.md) (3), [Database Queries](../../Skills/Software%20Development/Database%20Queries.md) (1)
> **CLI Commands:** mysql (4), make (1), mongo (1)
> **Env Vars:** sql (4), json (1)
> **Analogies:** such as (3)
> **Definitions:** is an  (1), means that (1)
> **Warnings:** be aware (1)
> **Speakers:** - [instructor] (1)

#### [Install MySQL and MySQL Workbench](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/install-mysql-and-mysql-workbench?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/install-mysql-and-mysql-workbench?u=76281980&t=0)** - [Instructor] The first thing to do here is if you don't already have [MySQL](../../Skills/Software%20Development/MySQL.md) installed on your computer, which is likely the case, we're going to download and install that. And in this video, we're going to see how to do that, as well as download and install another tool that we're going to be using called MySQL Workbench. So the first thing you're going to want to do is go to dev.[mysql.com/downloads/mysql](https://mysql.com/downloads/mysql) and download the latest version of MySQL. And you're going to want to make sure your operating system is selected from this dropdown here. So I'm going to click Download on this DMG archive here, and I'm going to click No thanks, just start my download, 'cause I don't want to create another account now. And once that downloads, let's click on that. And that'll bring up this thing here, let's click on that. And we're going to click Continue and we're just going to go through each of the steps here. Just click Continue, Continue, Agree, Install. And I have to put in our password. And that'll install MySQL on our computer. So we're going to click Next and just leave Use Strong Password Encryption selected there, click Next, and then it's going to ask us to enter a password for the root user. Basically, the root user on our local computer is going to be the user that's able to do everything with our database, right? They have permissions to do basically anything we need to do.
>
> **[1:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/install-mysql-and-mysql-workbench?u=76281980&t=92)** So I'm just going to put it in a simple password that I'll remember here and leave this Start MySQL Server once the installation is complete thing checked. So then we're going to click Finish and we might have to put in our password again, and then we're going to click Close and MySQL is now going to be active behind the scenes. So the next thing that we're going to do is install a tool called MySQL Workbench. Basically, what MySQL Workbench is is a user interface for interacting with our local MySQL [Databases](../../Skills/Software%20Development/Databases.md). And it doesn't just work with local databases either. It can work with basically any MySQL database that we have access to. So in order to download that, we're going to go to dev.[mysql.com/downloads/workbench](https://mysql.com/downloads/workbench). We're downloading it from the same site here, and then we're going to select our operating system and click Download. And again, we're going to click No thanks, just start my download. And once this file downloads, we're going to click on it here and then we're going to drag it over into our applications folder if you're on Mac, that's just how Mac does things here. And I actually already have MySQL Workbench installed on my computer, so I'm just going to click Stop, but you probably won't even get that message so that won't be a problem. So once we do that, let's open MySQL Workbench. And you'll see that we have this local instance 3306 thing up here. That is our local database and we can connect to it
>
> **[3:08](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/install-mysql-and-mysql-workbench?u=76281980&t=188)** by clicking on this and putting in the password that we put in earlier. And now we're successfully connected to our local MySQL database.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [MySQL](../../Skills/Software%20Development/MySQL.md) (16), [Databases](../../Skills/Software%20Development/Databases.md) (2)
> **CLI Commands:** mysql (16), make (1)
> **UI Navigation:** click on (3), go to (2), dropdown (1)
> **Prerequisites:** install (5)
> **URLs:** [mysql.com](https://mysql.com) (2)
> **Env Vars:** dmg (1)
> **Exercise Files:** download the (1)
> **Definitions:** is a  (1)

#### [Set up a MySQL database](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-mysql-database?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-mysql-database?u=76281980&t=0)** - [Instructor] So now that we're connected to our local my [SQL](../../Skills/Data%20Science/SQL.md) instance, what we're going to do is set up a database that our backend will be able to connect to and store data in. The first thing we have to do here is we have to actually create a specific database that our backend will use and the way that we do that is by clicking on this little database thing with the plus sign up at the top here and we're going to create a new schema which is basically just a new database than an application can use. So we're going to call this schema buy and sell just like the name of the site that we've been working on. And we're going to leave the default character set and default collation on and click apply. And then we're going to click apply here. And we should see at the bottom here that that was successful so the next thing we're going to do is create a table to hold the listings for our application. In a larger application, our database might have several tables, one for holding listings, one for holdings users, one for holding messages and so on but for this one we're just going to have one table called listings. So to do that go over to schemas, make sure buy and sell is selected and click the table thing up here with the plus sign next to it. And this can be a little tricky sometimes, you have to double click this schema name here in order for this to work. And we're going to call this new table listings and then what we have to do is we have to define all of the different columns that we expect our data to have in this database. Essentially, these are going to be all the different
>
> **[1:33](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-mysql-database?u=76281980&t=93)** properties that our data will have. So the first one is going to be the unique ID and this will be a 36 character VARCHAR. And we're going to make sure the primary key and not null boxes are checked next to it. And we're also get the unique box. And then we're going to have a column that holds the name of our given listing and we're just going to leave that at 45 characters, in reality we might want to make that a little bigger but for our purposes right now, 45 is plenty. And we're going to select not null and next we're going to have the description column and we'll make that a little longer, we'll make that 1000 characters and that will be not null as well. Then we're going to have the price which is going to be a decimal value so we're going to select decimal and then we have to specify the number of digits that we want it to be accurate to as well as the number of digits after the decimal point. So we're just going to put 10 and two there, in reality we might want to put something different but that'll work for now. And then we're going to have a user ID column, representing the unique ID of the user that this listing belongs to and that'll be a VARCHAR of length 36 as well and that'll be not null. And last but not least, we're going to add another column to this database that our fake data on our backend didn't have and that's going to be called views. What we're going to be doing is we're going to use this property
>
> **[3:09](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-mysql-database?u=76281980&t=189)** to count how many times each listing has been looked at by users of our site. And for this one we're just going to select int and we're going to have that be not null as well and the default value for that, we'll put zero. And that should be all we need to do for now. So now if we click on apply here and then click apply, we should see that our SQL statements were successfully executed so we can click close now. And the last thing we're going to do is now that we have our listings table created, we're going to insert some data into it. So the way that we do that, this is the first real SQL statement that we're going to see, is we're going to say insert into and then the table that we want to insert this data into which is going to be listings, and then inside parentheses we're going to put the columns that we want to insert the data into so we're just going to say ID name, description, price, user ID, and views.
>
> **[4:14](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-mysql-database?u=76281980&t=254)** And close the parentheses. And then we're going to say that we want to insert the values and then we're going to insert several records, each of which is going to be enclosed in parentheses like this. So we're going to say 123, the name is going to be guitar, for the description we'll just type something there. Then we'll put the price and then the ID of the user that this belongs to, we're going to say 12345 here and last but not least the number of views. And then we're going to insert a few more records which you're free to copy and paste from the exercise files. And once we have all of that we're going to click on this little lightning bolt icon up here which will execute our my SQL statement and down at the bottom we should see that that turns green and now what we can do is get all of our listings from our table by typing select star from listings and hitting this lightning bolt icon again. And that'll give us the result which will show all the things that we just inserted into our database.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [SQL](../../Skills/Data%20Science/SQL.md) (4)
> **Env Vars:** sql (4), varchar (2)
> **CLI Commands:** make (5)
> **SQL:** varchar (2)
> **UI Navigation:** click on (2)
> **Exercise Files:** exercise files (1)
> **Analogies:** just like (1)
> **Prerequisites:** set up (1)

#### [Connect to a MySQL database from Node](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-to-a-mysql-database-from-node?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-to-a-mysql-database-from-node?u=76281980&t=0)** - [Instructor] So now we have [MySQL](../../Skills/Software%20Development/MySQL.md) installed and set up with a working database, which is pretty exciting but this won't actually do us any good yet until we know how to connect to it from our hapi server. And this is actually a fairly simple process, so let's take a look at how it's done. The first thing we have to do is to install the MySQL [npm](../../Skills/Web%20Development/npm.md) package into our project, which we can do by running npm install mysql, and hitting Enter. And next, we're going to create a new file in our src directory. And we're going to call it database.js. And this is going to contain the logic for setting up a database connection. So here's what that'll look like. We're going to start off by importing the MySQL driver like this. We're going to say import mysql from mysql. And then we're going to create a connection that our server can use to communicate with a local database we created previously. Here's what that'll look like. We're going to say const connection equals mysql.createConnection. And then we're going to pass a configuration object to it. We're going to say host: localhost and user is going to be the username that our server is going to log into our MySQL database with. So what we're going to do is we're going to define a new user at some point. We'll call it hapi-server. We'll have to come back to that later. And then for the password, we're just going to say something like this. We'll have to remember that
>
> **[1:33](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-to-a-mysql-database-from-node?u=76281980&t=93)** and define that on our database as well. And then for the database name, we're going to put the name of the schema that we created earlier. And that'll be buy-and-sell and that should be it. So what we're going to do now is we're going to export an object from this file that sort of abstracts away a lot of the complexities behind working with the MySQL driver. So we're going to say export const db and this db object is something that the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our application will use. And the first property of this objet is going to be a function called connect that our server can call to establish a connection with the database. And we'll see how and where to do this shortly. So we're just going to say connect and then say connection.connect. And the next thing we're going to define is a property called query, which will be a function that wraps MySQL's provided querying functionality. Now, what we're going to do here might be a little bit technical for those of you who aren't familiar with promises and async await but basically, the MySQL driver currently only allows us to use callbacks to handle query results. In order to allow ourselves to use async await, which in my opinion is much nicer, what we're going to do is we're going to convert that functionality into a promise, which means that we'll be able to use the await keyword with it. So here's what that'll all look like. And again, don't worry. This is going to be a little bit technical but this is the only place in the course where we're going to have to do this. So we're going to say query and then it's going to take two arguments, queryString and escapedValues.
>
> **[3:08](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-to-a-mysql-database-from-node?u=76281980&t=188)** We'll discuss these later on in the course. And then what we're going to do is we're going to return a new Promise with resolve, and reject arguments. And then inside here, what we're going to do is we're going to say connection.query and we're going to pass the queryString and the escapedValues through. And then we need to pass the callback to basically just take the results and fulfill the promise with them. So this callback is going to take error, results, and fields arguments. And then inside this callback, we're just going to say if error, then we want to reject the promise with that error. Otherwise, we want to resolve the promise, passing the results and fields through. And again, don't worry too much about the details here. All we're doing is converting the MySQL query function into a promise. And finally, the last thing we're going to do here is we're going to return an end function that should be called when our server shuts down. So we're going to say end and this will simply call connection.end. So now that we've created this database wrapper thing, let's see how to use it on our server. All we have to do is open up our server.js file. And we're going to import the database object we just defined.
>
> **[4:42](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-to-a-mysql-database-from-node?u=76281980&t=282)** So we'll say import db from ./database and then down in here, we're going to connect to our database right before we call server.start. So right here, we're going to say db.connect. And then what we need to do is close our database connection when the server is killed. And here's how we do that. You see, when we first set up our server, we coated this unhandledRejection listener thing here, which gets called when an unhandled error occurs in our server. And we're going to add another one of these callbacks for when we kill the server manually. So here's what that'll look like. We're going to say process.on. And then all in caps, SIGINT. This is basically when we press Control + C to kill our server. And then we're going to define a callback that'll get called when that happens. And inside here, we're just going to say console.log. Stopping server. And then we're going to say await server.stop. And we need to pass an object here with the property timeout, which will tell it how long to wait before it forces the server to quit. And then underneath this, we're going to call db.end, like that. Now, the only other thing is in order to refer to the server inside this callback, we actually have to go up here and say let server up at the top of our file.
>
> **[6:16](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-to-a-mysql-database-from-node?u=76281980&t=376)** And instead of saying const server here, we're just going to say server equals Hapi.server. This will allow us to use server down here in our callback.
>
> **[6:29](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-to-a-mysql-database-from-node?u=76281980&t=389)** Anyway, after we've called db.end, we're just going to say console.log. Server stopped. And then say process.exit with the exist code zero.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [MySQL](../../Skills/Software%20Development/MySQL.md) (12), [npm](../../Skills/Web%20Development/npm.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** mysql (12), npm (2)
> **Code Identifiers:** querystring (2), escapedvalues (2), createconnection (1), unhandledrejection (1)
> **Prerequisites:** set up (2), install (2)
> **File Paths:** database.js (1), server.js (1)
> **Definitions:** is a  (1), means that (1)
> **Env Vars:** sigint (1)
> **Speakers:** - [instructor] (1)

#### [Rewrite the listings endpoints](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rewrite-the-listings-endpoints?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rewrite-the-listings-endpoints?u=76281980&t=0)** - [Instructor] Now that we have our service set up to connect to the database on startup, and then close that connection when the server is killed, let's move on to seeing how to make actual [Database Queries](../../Skills/Software%20Development/Database%20Queries.md) from our routes. To demonstrate this, what we're going to be doing is rewriting both our getAllListings route and our getListing route to query the database and then send back that data to the client instead of the fake data that we've been using so far. So let's start off with our getAllListings route. The first thing we're going to do here is import the db object that we exported from our database file. So we'll say import db from ../database. And then we're going to modify this route handler here. Instead of returning fakeListings like we've been doing, we're actually going to make a query to our database using the query function of the database object we just imported. So here's what that will look like. We're going to say const results equals await db.query. And then as an argument for this db.query function, we're going to pass a [MySQL](../../Skills/Software%20Development/MySQL.md) query string. This will be our first real contact with MySQL inside our server here. So again, if you're not already familiar with MySQL, don't worry too much about the details here. What we want to do is retrieve all of the listing records from the listings table we created. And the query for that is going to look like this. We're just going to say SELECT star FROM listings, and that's it. So that's how we load all of our listings from our database. And we also have to add the async keyword
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rewrite-the-listings-endpoints?u=76281980&t=94)** to this route handler function since we're using await inside of it. And then the last thing we're going to want to do here is return all of our results, which will send them back to the client. So we'll say return results. And we can also delete this fakeListings here since we don't need that anymore. So now if we use Postman to make a request to this endpoint, we'll say GET localhost /api/listings, and click Send. And it looks like I forgot to add the async keyword here as well, so we just need to add that async keyword to the callback that gets called on process.on SIGINT. And one more thing that I almost forgot is that we have to actually create this user for our database in order for our server to connect to it. So what we're going to do is go back to MySQL Workbench, and we're going to go over to this Administration tab here, and click on Users and Privileges, and then we're going to click on Add Account down at the bottom here. And for the Login Name, we're going to use hapi-server, which is what we put here for the username. Authentication Type, Standard. We're going to leave that as the percentage sign there. And for the Password, we're going to use the password that we put in here, even though it will tell us, of course, that it's a weak password. And we're going to click Apply, and that should create the new server for us. And the next thing we have to do is we have to go over to Schema Privileges and click Add Entry,
>
> **[3:09](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rewrite-the-listings-endpoints?u=76281980&t=189)** and we're going to go down to Selected schema here and select our buy-and-sell schema since we only want the permissions here to apply to that. And then what we're going to do is we're going to give our server certain rights. So we're going to say it's allowed to insert, it's allowed to update, it's allowed to select, it's allowed to delete, and that should be about all we need for our server. So now we're going to click Apply, and that should be all we need to do. So now let's go back and restart our server, so [npm](../../Skills/Web%20Development/npm.md) run dev, and we should see that our server successfully connects and is running now. So if we go over to Postman now and try and send a request to /api/listings and click Send, we should see that we get back all of the listings from our database. So that's how we add MySQL to our getAllListings route. Now let's take a look at how to add it to our route for getting individual listings. So for that, we're going to click on getListing, and, again, we're going to start off by importing our database object, import db from database, and we can delete this fake-data here. And then the first thing we need to do is we need to make the route handler async since we'll be using the await keyword just like with our other route. And then instead of using the fakeListings here, we're going to use our database object to load the corresponding listing from our local database. And this query will look a bit different here, so I'm just going to type out the whole thing, and then I'll explain it afterward.
>
> **[4:43](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rewrite-the-listings-endpoints?u=76281980&t=283)** What we're going to say is const results equals await db.query. And then for the query string here, we're going to say SELECT star FROM listings WHERE, this is where we add a filter to it, id equals question mark. And then we're going to put a comma and add a second argument to our db.query function here, which will be an array containing the id that we get from request.parameters.id. So what we're doing here is we're selecting the listing from our listings table with the id that the client requested. And this question mark thing here is how we insert the values of variables into an [SQL](../../Skills/Data%20Science/SQL.md) query in Node. When we make this query, what will happen is that each question mark inside the query string will be replaced one by one with the values we provide inside this array that we're passing as the second argument. Now, you might be wondering why we need to do it this way. Why can't we just do something like this, where we just say plus id, right? Well, the answer is that doing it using the question marks prevents hackers from using something called SQL injection to attack our database. And for those of you who aren't familiar with SQL injection, basically, SQL injection is when hackers pass clever values to the variables that we're inserting into our SQL queries. So for example, instead of using an actual id in the query that we're sending to api/listings/id,
>
> **[6:18](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rewrite-the-listings-endpoints?u=76281980&t=378)** they could say something like api/listings/, and I'm just going to paraphrase here. The real URL would look slightly different. They could say DROP TABLE listings, for example, as the id property. And what this would do is that if we were just inserting this raw string into our SQL here, it would actually delete our listings table. So basically, using this question mark thing instead of just randomly inserting values into our SQL queries prevents this kind of thing from happening by inserting the values properly. So anyway, now that we have our results from that query, which is going to be either an array with a single element since listing ids are unique, or it will be an empty array if the listing doesn't exist, what we can do is we can just say const listing equals results index zero, and then the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our code will still be valid here. We just want to return the listing if it exists. Otherwise, we'll use the Boom package that we saw earlier to return a notFound response to the client. So now if we test this by saying /api/listings/123, or any of the other ones that we see down here, and click Send, we should see that we just get back an individual listing from our server endpoint. And if we try and do SQL injection here, which would look something like this, which I'm just going to copy and paste, and click Send, we'll just get something that says Listing does not exist with id DROP TABLE listings.
>
> **[7:54](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rewrite-the-listings-endpoints?u=76281980&t=474)** And one more thing to note here, too, about hapi service in general, is that in the event that some error happens on our endpoints, for example, if they can't connect to the database or something like that, hapi will automatically send back an appropriate response with the 500 error, which is just a generic way of saying that something went wrong on the server. So unless we really want to implement a separate response for cases like this, we're fine to just leave our route handlers without a try/catch block.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [SQL](../../Skills/Data%20Science/SQL.md) (8), [MySQL](../../Skills/Software%20Development/MySQL.md) (5), [Database Queries](../../Skills/Software%20Development/Database%20Queries.md) (1), [npm](../../Skills/Web%20Development/npm.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Env Vars:** sql (8), select (2), drop (2), table (2), sigint (1)
> **CLI Commands:** make (5), mysql (5), npm (1), node (1)
> **Code Identifiers:** getalllistings (3), fakelistings (3), getlisting (2), notfound (1)
> **SQL:** select (2), drop table (2), where (1)
> **API Endpoints:** /api/listings (2), get  (1), /api/listings/123 (1)
> **Analogies:** for example (3), just like (1)
> **UI Navigation:** click on (3)

#### [Create an endpoint for adding views](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-for-adding-views?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-for-adding-views?u=76281980&t=0)** - So we've seen the basics of querying our database now, and our server is able to load listings from our database and send them back to the client. The next thing we're going to add to our server is an end point for keeping track of how many times each listing has been viewed. Now, we actually, haven't built this into our front end yet. We'll get to that in a little while, but what we want at the top of each listing page is a little message that says something like this listing has been viewed 125 times, for example. And of course we'll want to increment this number every time someone visits the page. So in order to implement this functionality, what we're going to do is inside our routes folder, we're going to create a new file and we're going to call it, add view to listing.js and inside that we're going to define a route that looks something like this. We're going to import our database object from dot dot slash database. Then we're going to say export const, add view to listing route. And then for the method, this is going to be a post route. All we've seen up until now have been get routes, but this one's going to be a post route. And we're going to say path is going to be slash API slash listings slash ID in curly braces slash add view. And then for the handler, this is going to be an async callback. And we're going to say request and H then we're going to say, const ID equals request
>
> **[1:37](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-for-adding-views?u=76281980&t=97)** dot params dot ID. That'll get the ID from the URL here. And then we're going to make a query to our database to increment the number of views on the listing whose ID matches this ID here. And that'll look like this. We're just going to say, await DB dot query update. And then the table named listings. And we're going to say set views equals views plus one. And we want that to happen where the ID is equal to and we're going to use the question mark thing there to insert the value of the ID. And then we're just going to have an array with the ID as a member. And again, if you're new to [SQL](../../Skills/Data%20Science/SQL.md) here, don't worry too much about this one. All we're doing here is saying that we want to increment the number of views on the listing with the ID matching the ID URL parameter. So the next thing we're going to do here is that it's pretty common for routes that update data to send back an updated version of whatever data was modified. So what we're going to do is get the updated data for the listing whose views we incremented, and to do that, we'll say const results equals await DB dot query, select star from listings, where ID equals question mark. And then we're going to insert the ID URL parameter, just like we did before. And after that,
>
> **[3:11](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-for-adding-views?u=76281980&t=191)** we're going to send that updated listing back to the client. So we'll say const updated listing equals results zero. And we're going to say return updated listing. And it would help if I spelled that right. There we go. And we're not being super careful about error handling here. For example, we're not really defining what we want to happen if this ID doesn't correspond to a listing in our database. But that's a pretty easy thing to do. So if you want, you can always go back and do that. But for the sake of time here, we're just going to move on. And now that we've defined that route, we're just going to export it from our routes index dot JS file. So we're going to import it. I'm going to say import add view to listing route from add view to listing. And we're going to put that in the export default array add view to listing route. And actually now that I think about it, one last thing that we're going to want to do here is go back to our front end code and add the views property to our listing type. So let's open up our types dot TS file here. And remember that we didn't have this views field in our fake data that we've been working with so far. So what we're going to do is we're just going to say views number, and this will break our fake data file.
>
> **[4:44](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-for-adding-views?u=76281980&t=284)** So what we're going to do is we're just going to go through and add views zero to each of these. Not that this will even be relevant since we'll be replacing this with actual server calls, but for now, we're just going to do that to avoid errors and make sure to save both those files. So, anyway, going back to our add view to listing and point, what we should be able to do now is send a post request to API slash listings slash we'll, just do one, two, three slash add dash view and click send, and it'll send back the updated version of that listing with the views incremented. And we can click it a couple times if we really want to see it working.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [SQL](../../Skills/Data%20Science/SQL.md) (1)
> **Env Vars:** url (3), api (2), sql (1)
> **Analogies:** for example (2), just like (1)
> **CLI Commands:** make (2)
> **Definitions:** is an  (1), is a  (1)
> **File Paths:** listing.js (1)
> **Cross-References:** go back to (1)
> **Best Practices:** make sure to (1)

#### [Create an endpoint to get a user listing](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-get-a-user-listing?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-get-a-user-listing?u=76281980&t=0)** - [Instructor] So that's the endpoint for adding views to listings. The next endpoint we're going to create is the endpoint for getting all of the current users listings. And this endpoint will be called by the my listings page on our front end. So here's what this is going to look like. Inside our routes directory, we're going to create a new file. And we're going to call it getUserListings.js. And we're going to import our database object from database. And we're going to say export const getUserListingsRoute. And the method for this is going to be GET. The path for this is going to be /api/users/ in curly braces, {userId}/Listings And finally, for the handler, we're going to say async( req, h). And the first thing we're going to do inside this route handler. Is get the userId from the URL parameter. So we're going to say const userId=req.params.userId. And underneath here is where later on, we're going to check to make sure that the client requesting this data is actually the user with this ID. Otherwise, what we'll do is we'll return an error saying that the user isn't authorized to get this data. Since we only want users to be able to [Fetch](../../Skills/Web%20Development/Fetch.md) their own listings and not the listings of some other user. And granted in our application, this wouldn't be that big of a deal, since users can see all of each other's listings anyway.
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-get-a-user-listing?u=76281980&t=94)** But still, that's just something to think about. So after that, what we're going to do here is get all the listings in our database with their userId property equal to the ID of the user that requested it. That is this userId URL parameter here. So we're going to say const {results} = await db.query. And for the query string, we're going to say SELECT* FROM Listings WHERE user_id=?.
>
> **[2:05](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-get-a-user-listing?u=76281980&t=125)** And then we're going to insert the value of the userId variable up here. And then all we have to do is send back the results to the client. So we'll just say return results. So that's it for defining the route. We just have to export it from our index.js file now. So we're going to say import {getUserListingsRoute} from getUserListings. And then we're going to export it, along with our other routes. getUserListingsRoute. And now provided our server is running, we should be able to send a request to API/users/12345/listings and hit Send
>
> **[2:59](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-get-a-user-listing?u=76281980&t=179)** and if you forgot to change this from POST to GET like I did, you'll see 404 not found. But if we change it to GET and click Send, we'll see that it returns all of the listings that belong to the user_id 12345.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Code Identifiers:** userid (7), getuserlistingsroute (3), getuserlistings (2), user_id (2)
> **Env Vars:** url (2), select (1), where (1), api (1), post (1)
> **API Endpoints:** get  (2), /api/users/ (1), post  (1)
> **File Paths:** getuserlistings.js (1), index.js (1)
> **SQL:** select (1), where (1)
> **CLI Commands:** make (1)
> **Speakers:** - [instructor] (1)

#### [Create an endpoint to create listings](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-create-listings?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-create-listings?u=76281980&t=0)** - [Instructor] Okay. The next route we're going to create, is the route that will allow users to create new listings. And this will be hit when the user fills out the form and hits the create button on the new listing page. So the first thing we have to do here is install a package called UUID. We'll be using this package in our route to generate random, unique IDs for the new listings that users create. So we're going to say [npm](../../Skills/Web%20Development/npm.md) install, UUID and hit enter. And just a note about this package, depending on the version of NOJS you're using on your computer, this package may throw a weird error for you when you try and use it on our server. In particular version 13.5, which is the version I showed you that I was using back at the beginning of the course was causing problems for me so I had to switch back to using version 12.18 here. And keep in mind that the latest versions of Node would probably work as well, but just so you know, if you get a weird error here, that might be why. So anyway, now that we've got that package installed, let's create a new file in our routes directory. And we're going to call it, CreateNewListing.js, and then at the top, we're going to say import V4 as you UUID, from UUID, that's just how we import the function that we're going to be using there. And then we're going to say import and import the DB object from our database file. And then to define the route itself, we're going to say, export const, Create new listing route.
>
> **[1:38](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-create-listings?u=76281980&t=98)** And the method for this one is going to be POST, this is going to be a POST route and the path will just be /api/listings. And then we'll define the handler, which just like the others will be async, so async request H. so we're going to start off here by creating a new random ID for the new listing. To do that, we just have to say, const ID equals UUID. And then we're going to get the data for the new listing that the client sent along with the request. So we need to get that out of the request payload like this, we'll just say const name, and we're going to be providing default values for all of these, by the way, in case the client forgot to send one of these. And then description, and price, equals request.payload.
>
> **[2:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-create-listings?u=76281980&t=152)** Remember that this is how we get the payload from a post request. And once we've done that, we're going to need the user ID so that we can add it to the new listing here that we're creating. For now, we're just going to hard code that like this, we're going to say const user ID equals one, two, three, four, five, but later on, we're going to be using authentication to make sure we really know which user is making this request. And once we've done that, we're going to say const views and the starting value for that will be zero, and that's all the data we need. Now for the query, it's going to look like this, we're going to say, await db.query. I'm going to use back ticks here because I want this to be a multiline string, because it's going to get a little bit long. We're going to say insert into listings. And then the names of the columns that we want to insert our values into, that'll be ID, name, description, price, user_id, and views. And then we're going to say values along with the values that we want to insert. So we're just going to say question mark, question mark, question mark, question mark, that'll be six question marks in all. And as a second argument to our query function, we're going to pass in the ID, name, description, price, user ID, and views variables that we defined.
>
> **[4:08](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-create-listings?u=76281980&t=248)** And then what we're going to do is now that this new listing has been created inside our database, instead of actually querying it from our database, we're going to return the relevant data from the listing that was just created, which we can do simply by using all these values that we defined up here. There's no need to query the actual data from the database this time. We're going to say, return ID, name, description, price, user ID, user ID. We just have to do that there because we don't like to use underscore names as variable names in [JavaScript](../../Skills/Software%20Development/JavaScript.md), and then views. And now that we've created the route, we just have to export it from our routes index.js file. So we're going to say import create new listing route from create new listing. And we're going to add that here, create new listing route. And now what we can do is we can test our route by sending a post request with the correct body to the end point we just created. So we're going to send a post request to api/listings. And we're going to want to define the request body here. So we're just going to say, name and we'll just give our new listing a name. We'll say, Air conditioner, something that you see fairly often on listing sites. And then for the description,
>
> **[5:43](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-create-listings?u=76281980&t=343)** we'll just say something like this. And for the price, we'll have it be a hundred bucks. And now if we click send, we should see the data from the new listing that was created with this randomly generated user ID up top and all the information we provided. And now if we simply sent a get request to api/ listings, we should see that the new listing that we just created is part of that.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [npm](../../Skills/Web%20Development/npm.md) (1), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **Env Vars:** uuid (5), post (2), npm (1), nojs (1)
> **CLI Commands:** npm (1), node (1), make (1)
> **File Paths:** createnewlisting.js (1), index.js (1)
> **API Endpoints:** post  (1), /api/listings (1)
> **Versions:** version 13 (1), version 12 (1)
> **Prerequisites:** install (2)
> **Code Identifiers:** user_id (1)

#### [Create an endpoint to edit listings](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-edit-listings?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-edit-listings?u=76281980&t=0)** - [Narrator] Okay, so we're getting to the end of our Server Routes. The next one we're going to create now, is a route that will allow users to edit their listings. So let's get right down to business We're going to call this one update listing dot js. And then we're going to define our route. So we'll say import db from dot data slash database. And then export const we're going to call it update listing route. And this will be a POST route so we'll say method POST. The path for this route is going to be slash api slash listings slash id in curly braces. And then we have the handler which like all of our other ones is going to be async. So what we're going to do first is we're going to start off by getting the id from our URL parameters. So we'll say const id equals req dot params and then we're going to get the updated name, description and price values from the request payload. So we're going to say name, description, and price equals request dot payload. And then just like we did in our create listing route, we're going to hard code the user id for now. So we'll say const user id equals 12345. And note that at some point, we'll also want to check to make sure that the clients sending this request really is the one who created the listing otherwise, users would be able
>
> **[1:31](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-edit-listings?u=76281980&t=91)** to edit each other's listings. So now we come to the query, which is going to look something like this. We're just going to say await db dot query and I'm going to use backticks for this one, since it's going to be another multi line query. We're going to say update listings and this is just how we update things in [MySQL](../../Skills/Software%20Development/MySQL.md). So we're going to say set name equals question mark we'll be inserting that later description, Price and then we're going to say where id equals question mark and user id equals question mark. We're going to make sure that both the listing id and the user id match on that listing, which will help when we're actually using real user ids here. And then for the second argument to our db dot query function, we're going to pass an array with the values name, description, price, id, and user id, which will fill in all of the question marks in this query string. And then what we're going to do is we're going to get the updated listing from our database and send it back to the user, which is going to look like this. We're just going to say const results equals await db dot query and we're just going to say select star from listings where id equals question mark and user id equals question mark.
>
> **[3:09](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-edit-listings?u=76281980&t=189)** And then we're going to insert id and user id into that query. And finally, we're going to say return results index zero, since this results will only ever contain one result. And then we're going to go and export this from the index js file. So we'll say import, update listing route from update listing and then down here in the array, update listing route. And now that we've done that, what we can do is we can go back to Postman, and send a POST request to slash API slash listings slash and then the id of the listing that we want to change. In our case, we'll use this one that we just created and for the body, we're just going to make a nice simple change. And let's say we want to increase the price, and now if we click send, we should see that we get the updated version of our listing sent back to us. And if we send a get request to the same path, we should see that that was indeed updated on the server since this is now being returned to us from the server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [MySQL](../../Skills/Software%20Development/MySQL.md) (1)
> **Env Vars:** post (3), url (1), api (1)
> **CLI Commands:** make (3), mysql (1)
> **API Endpoints:** post  (2)
> **Cross-References:** go back to (1)
> **Tools:** postman (1)
> **Definitions:** is a  (1)
> **Analogies:** just like (1)

#### [Create an endpoint to delete listings](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-delete-listings?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-delete-listings?u=76281980&t=0)** - [Instructor] So now we've come to our last route and that's the route that will allow users to delete listings. So here's what that'll look like. Let's create a new route file called deleteListing.js. And then we're going to implement the route like this. We're going to say import db from ../database. And then export const deleteListingRoute. And the method for this route is actually going to be delete, which is just another method that the client can send like get or post. And the path is going to be /API/listings/{id}. And then for the route handler, it's going to be async. And inside there, we're going to get this ID URL parameter by saying const id equals request.params. And just as a side note, at some point here too we're going to want to check to make sure that the client who's sending this request is actually the owner of this listing 'cause otherwise users would be able to delete each other's listings, which obviously wouldn't be ideal. But for now, what we're going to do is we're just going to make the query. So we're going to say await db.query. And for the query string, it's just going to look like this. Delete from listings where ID equals question mark. And for the ID, we're going to pass the value of the ID URL parameter. And then at the end of the route handler,
>
> **[1:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-an-endpoint-to-delete-listings?u=76281980&t=92)** we want to send something back to the client. Now, what we could do is send the info for the listing that we just deleted back to the client. But all we're going to do in this case is just send back an object with a message that says success, just like that. And now let's export this route from our index.js file. So we're going to say import deleteListingRoute from deleteListing and export that along with the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our routes. And now we should be able to send a delete request to api/listing/ and then this big, long ID of the listing that we created and click Send. And we should just get back message success as a response. And now, if we try and get this listing and click Send, it'll send us back 404 not found and we'll see the message that we defined earlier, listing does not exist with the ID.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Code Identifiers:** deletelisting (2), deletelistingroute (2)
> **Env Vars:** url (2), api (1)
> **File Paths:** deletelisting.js (1), index.js (1)
> **CLI Commands:** make (2)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)


### 4. Connecting the Front and Back Ends

[↑ Back to Table of Contents](#table-of-contents)

#### [Services in Angular](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/services-in-angular?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/services-in-angular?u=76281980&t=0)** - [Instructor] So at this point in our app, we have a fully functioning front end and we have a fully functioning back end. What we need to do now is link them up. And that's what we'll be doing over the course of the next few videos. Now, there are a few tools and tricks to getting this sort of front end, back end communication to work properly, but before we get into all of those, we're going to take a look at something called services in [Angular](../../Skills/Web%20Development/Angular.md). Basically Angular services are like Angular components, except they don't have any corresponding [HTML](../../Skills/Web%20Development/HTML.md) that gets rendered on the page. So then if services aren't rendered to the page, what exactly do we use them for? Well, the main thing that we're going to be using services for in this course is as a place to put the logic for communicating with our server. You see, the way that we have our front end set up now, all of our components are only displaying fake data. For example, in our listings page component, we're just saying this.listings = fake.Listings that we defined inside our fake data file. Obviously, however, now that we have our back end set up, we'll what the data that each of our components displays to be loaded from our server. Now, one possible way of interacting with the server from our front end would be to simply include all the logic for doing so inside our components themselves. So for example, we would [Fetch](../../Skills/Web%20Development/Fetch.md) our listings here. However, this is generally not a good idea. First of all, it reduces code reuse in our code base. And second of all, adding all of the raw logic for interacting with the server to our components can dramatically increase the size of our components, making our code base less and less maintainable over time.
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/services-in-angular?u=76281980&t=94)** So pulling this logic out into its own separate file, a service in the case of Angular, solves both of these problems. So what do Angular services actually look like anyway? And how do they work? Well to answer these questions, here's what we're going to do. We're going to create a listing service that abstracts away all the communications with the various listings routes that we created on our back end. Our components will then be able to use this listing service to do things like load data, create, update, and delete listings, and so on. Basically all the things that we created routes for on our server. We'll see exactly how all this works very shortly. So first of all, to generate a new service in our application, we can do the same sort of thing that we did to generate all the components in our application. That is we can run the Angular generates script by saying ng generate. And then we're going to say service instead of component. And we're going to call this service listings. And then we're going to hit enter. And that will create a new service in our project called listings, which will only include two files, one being the actual logic of our service and the other one being a test file. Now, if we open up the listings.service.ts file here, we'll see that it's fairly empty. The first thing to note here is that instead of this decorator at the top of our listing servicing @component, as it did in all of our component files, it says @injectable. And what this means is that it can be injected into any of our components in the constructor method, just like we saw with things like Angular's activated route or router. And we'll see more on that in a second.
>
> **[3:07](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/services-in-angular?u=76281980&t=187)** For now, just for demonstration purposes, what we're going to do is create a get listings method on our listing service that returns some fake listings. And then we're going to rewrite our listings page to use this service. And this will allow us to see more or less what it looks like to use a service from inside a component. So we're going to start off by importing our fake listings and the listing type into our listing service. So we're going to say import, fakeListings, from fake-data, and then import Listing from types.
>
> **[3:43](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/services-in-angular?u=76281980&t=223)** And then we're going to add a method to our listing service called getListings, which will simply return the fake listings. So we'll say getListings, and that's going to return an array of listings. And then we're just going to say return fakeListings from inside of there. And now that we've done that, we're going to head over to our listings page component and use our listings service to get our fake listings, instead of just importing them straight into our component. Here's what that'll look like. First, we're going to import our newly created listing service into our file. And we can also delete these fake listings here since we won't be needing that anymore. And we're just going to say import ListingsService from ../listings.service. And then, in order to use the service from our component, we're going to inject it via the component's constructor, just like we've seen with a few of the other tools we've used. So inside the constructor, we're going to say private listingsService, ListingsService. And finally, instead of directly assigning the fake listings to our listings, we're going to use the get listings method that we defined on our listings service. So we're going to say, this.listingsService.getListings, like that.
>
> **[5:09](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/services-in-angular?u=76281980&t=309)** And that should be about it. If we run our front end and we're going to just have to fix a type error that happened from when we changed our types earlier. So to do that, we're just going to open up our listing data form component.
>
> **[5:26](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/services-in-angular?u=76281980&t=326)** And then down here, we just have to add views zero,
>
> **[5:35](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/services-in-angular?u=76281980&t=335)** and now our app will run successfully. So now if we open up a browser and take a look at our app, we should see that it still works like before, but now we're using an Angular service to have this fake data, instead of just hard coding it directly into our component file.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Angular](../../Skills/Web%20Development/Angular.md) (8), [HTML](../../Skills/Web%20Development/HTML.md) (1), [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **Code Identifiers:** getlistings (3), fakelistings (2), listingsservice (2)
> **Analogies:** for example (2), just like (2)
> **Prerequisites:** set up (2)
> **File Paths:** listings.service.ts (1)
> **Env Vars:** html (1)
> **Speakers:** - [instructor] (1)

#### [RxJS and the Angular HttpClient](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rxjs-and-the-angular-httpclient?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rxjs-and-the-angular-httpclient?u=76281980&t=0)** - [Instructor] Now that we've learned about services in [Angular](../../Skills/Web%20Development/Angular.md) and created our own service that our front end can use to interact with the server, it's time to take a look at the tools we'll be using to actually do that communication. In order to make network requests in Angular, there are two main tools we need to use. And these are called RxJS and HttpClient. RxJS is a library that makes working with asynchronous and event based code a lot easier and HttpClient is an Angular module that we can use to actually make the request to the server. The best way to go further in our understanding here is really just going to be to see both of these tools in action so let's do that. The first thing we're going to have to do here is open up our @module file and add our HttpClient to it. This will make it so our Angular components and services are able to inject HttpClient as a dependency. So up at the top of our file we're going to import something called HttpClient module. So we'll say import HttpClientModule and do make sure that in Http only the H is capitalized, I make that mistake all the time. So we're going to say import HttpClientModule from @angular/common/http and then the next thing we need to do is just add that HttpClientModule to our app's list of imports like this.
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rxjs-and-the-angular-httpclient?u=76281980&t=94)** HttpClientModule, and now that we've actually done that we can head over to our listing service and use HttpClient and RxJS to actually load data from our server. So here's what that's actually going to look like. The way we have it right now, our listing services get listings method is actually just synchronously returning an array of listings. However, now that we want to load these listings from a server, this is going to be an asynchronous operation and therefore we have to write this method a little differently. First of all, instead of returning an array of listings directly, this method is going to return something called an observable. And this observable thing is a generic type and in this case we're just going to say observable listing array, so this observable thing is how Angular actually incorporates asynchronous logic into it's apps. An observable is basically just some task that our app is executing. In this case, loading data from a server that our components can subscribe to. And we'll see exactly how this subscription stuff works in a minute. For now, we want to import this observable type from RxJS by saying import observable from RxJS
>
> **[2:50](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rxjs-and-the-angular-httpclient?u=76281980&t=170)** and then we're going to import the HttpClient as well by saying import HttpClient from @angular/common/http.
>
> **[3:04](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rxjs-and-the-angular-httpclient?u=76281980&t=184)** And we're also going to delete our fake listings import since we won't be needing that anymore. And then next thing we're going to do now is we're going to provide the HttpClient in the service's constructor like this, we're going to say private Http, HttpClient, and now we come to the part that you've been waiting for. The way we actually make a request to our server is by saying return this.http.get with the generic listing type here and then the URL that we want to send the get request to. In our case, that's going to be api/listings. And the reason that we're returning this is that the http.get function is an observable and we want our components to be able to subscribe to it. So now that we've got this method, let's head over to our listings page and see exactly how this subscribing works. In reality, it's actually pretty simple. What we were doing before was just saying this.listings equals listingService.getListings. But now that this getListings method is returning an observable instead of just an array, what we say instead is just this.listingsService.getListings and then on the end of this we say .subscribe and we'll get a callback function that'll get called whenever this getListings observable produces an event. So here's what our callback will look like.
>
> **[4:36](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/rxjs-and-the-angular-httpclient?u=76281980&t=276)** It'll say listings, this.listings equals listings. So basically what's going to happen is when our getListings call returns some data, it's going to call this callback with the listings that it got from the server and we're going to set the listings member variable on our listings page so that those listings will be displayed.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Angular](../../Skills/Web%20Development/Angular.md) (7)
> **Code Identifiers:** getlistings (5), listingservice (1), listingsservice (1)
> **CLI Commands:** make (6)
> **Definitions:** is a  (2), is an  (2)
> **Env Vars:** url (1)
> **Speakers:** - [instructor] (1)

#### [Avoid CORS errors](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/avoid-cors-errors?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/avoid-cors-errors?u=76281980&t=0)** - [Instructor] So, we've got our front-end set up to make requests to our back-end. However, if we were to try and make an actual request to our server now, we'd end up getting something called a CORS error telling us that the back-end has blocked our request. CORS errors are security related errors that are very common in local development environments. And they happen when in our case, we try to make a request from one origin to another. In order to get around this for local development purposes, we need to set up something called a Proxy. And to do that, there are a few things we need to add to our project. The first thing we need to do is in the root of our app directory, we need to create a new file. And we're going to call that file "proxy.config.[JSON](../../Skills/Web%20Development/JSON.md)". And this will contain some configuration info that our app can use to set up a Proxy for our requests. Here's what that'll look like. We're going to have a JSON object in here, and we're going to say "/api/*" and you may notice that this key here is the prefix that we gave to all of our server routes. This is why we started all those paths with "/api". It makes it easy for our front-end to proxy our requests to the back-end, because it can just look at the beginning of the path we're making a request to. And if it starts with "/api", it proxies our request to wherever our server is running. So, inside this object here, we're just going to add a few keys. I'm not going to go into any detail here on these. It's just sort of the setup that we have to have. We're going to say "target" and the path of our server. In this case, it's "localhost:8000".
>
> **[1:38](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/avoid-cors-errors?u=76281980&t=98)** And we're going to say "secure false", "logLevel: debug", and "changeOrigin: true". So, now that we've got that set up, we should be able to run our app now and our requests should work like we want them to. First, makes sure our back-end is running since otherwise you'll definitely get errors. And now to run our front-end. And now to run our front-end, we actually have to run our app with a slightly different command. Instead of just saying "ng serve", we have to actually tell our app where to find our proxy configuration that we just defined. And that'll look like this. We just have to add a flag that says "--proxy-config" and then the path to our proxy config file, which is just "proxy.config.json" and hit ENTER. And now if we open up our Listings Page, we should see our Listings displayed after a second or two. And these Listings are from our local database returned to our front-end by the Listings endpoint on our server. And this is really exciting because for the first time in this course, our app is now officially a full-stack application. That is it's got a front-end, a server and a database, all communicating with each other to provide the user with a web page. Now, obviously, we still have to convert all of our other pages over to use the server as well but this is a great first step. So, before we move on, there's one little change I'd like
>
> **[3:11](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/avoid-cors-errors?u=76281980&t=191)** to make in order to make things easier for us. This new command that we're running our front-end with is a bit long, and I definitely don't expect you to remember it. So, what we can do here instead is create a shortcut script for it in our "package.json" file, which will look like this. Let's open up our "package.json" file and then down here in the scripts directory, we're going to find the "start script", which is just a shortcut for "ng serve" and we're going to change that to "ng serve --proxy-config proxy.config.json".
>
> **[3:52](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/avoid-cors-errors?u=76281980&t=232)** And now instead of typing that whole command out like this, all we have to do is type "[npm](../../Skills/Web%20Development/npm.md) run start" or we can just type "npm start" that works too. And if we take a look at our Listings Page again, we see that our app is successfully loading data from our happy server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JSON](../../Skills/Web%20Development/JSON.md) (6), [npm](../../Skills/Web%20Development/npm.md) (2)
> **CLI Commands:** make (5), find (2), npm (2)
> **File Paths:** proxy.config.json (3), package.json (2)
> **Prerequisites:** set up (4), setup (1)
> **Env Vars:** cors (2), json (1), enter (1)
> **Code Identifiers:** loglevel (1), changeorigin (1)
> **Definitions:** is a  (2)
> **URLs:** [localhost:8000](https://localhost:8000) (1)

#### [Connect the Listing Detail page](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-listing-detail-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-listing-detail-page?u=76281980&t=0)** - Okay, so far our app is loading our listings for the listings page from our backend. So over the course of the next few videos, we're going to be changing over the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the pages in our app to do the same thing. So what we're going to do is we're going to start off with the listing detail page and contact page, which will behave fairly similarly. Basically, we're going to have our listing detail page load the data for an individual listing from the get listing route that we defined on our server. And we're also going to have it send a request to the add view to listing route that we defined to increment the number of views that a listing has. So here's what all of that's going to look like. We're going to open up our listing service if you don't already have it open. And the first thing we're going to do is since adding views to a listing is going to be a post request, we're going to have to add some headers to the request we're sending. And this isn't strictly necessary for the adding views request per se, but for the rest of the post requests where we send an actual request body will be needing this. So anyway, to add headers to our post requests, we're going to need to import something called HttpHeaders, which we do from the same place as HttpClient. So we're going to say import HttpHeaders, and then down here outside of our listing service, we're going to define something called HttpOptions. And then we're going to say equals object. And this object is going to have a key called headers, which is going to be new HttpHeaders. And we're going to pass the content type header
>
> **[1:36](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-listing-detail-page?u=76281980&t=96)** whose value will be application/[JSON](../../Skills/Web%20Development/JSON.md). And basically what this does is it just tells our server that the request body of our post request is a json object. And now that we have that, let's define two more methods on our listing service. One for each of the requests that we want to make. We're going to start off with the method for getting individual listing data, which will look like this. We're going to say getListingById and the id is going to be a string. And the type that it's going to return is going to be an observable that calls it's call backs with a listing object. And then we're going to say return this.http.get the type listing and the route that we want to send this get request to is going to be /api/listings. And we're going to insert the id into there. So that's simple enough. Now let's do the method for adding views to listings, which is going to look something like this. We're going to say, addViewToListing, and then the id of the listing that we want to add that to. And this is going to return an observable listing as well. And for this one, we're going to say return this.http.post, since this is a post request instead of a get request, post listing, and then the route that we want to send this to
>
> **[3:10](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-listing-detail-page?u=76281980&t=190)** is /api/listings/id/add-view.
>
> **[3:18](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-listing-detail-page?u=76281980&t=198)** And then we're going to pass an empty object for the body of the post request. And for the third argument, we're going to pass this HttpOptions object that we created. So now that we've defined these two methods, let's head over to our listing detail page component and use these methods we just defined. So we're going to open the listing detail page .ts file up, and we're going to import our listing service and delete our fake listings. So we'll say import ListingService from ../Listings.service, and then what we're going to do since our data isn't being set on our component instantaneously anymore is we're going to define a new member variable here called is loading that we'll be using to display a loading message on our page. So this will just be a boolean and it's starting value will be true. We'll see how this is used in a second. So the next thing we're going to do now is we're going to inject our listing service through the constructor. So we'll say private listingService: ListingService, and then down here in ngOnInit, instead of just saying this.listing = fake listing.fine, we're going to use the methods that we defined on our listing service. So we're going to say this.listingService.getListingByID
>
> **[4:53](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-listing-detail-page?u=76281980&t=293)** and for that, we're going to pass this id that we're getting out of the route parameter. And then we're going to say .subscribe. And our callback here is going to get called with a listing. And when that happens, we want to say this.listing = listing. And we want to say this.isLoading, set that equal to false. So that's how we load our listing. The second thing we want to do in ngOnInit is add a view to our listing, which we can do by saying this.listingService.addViewToListing id. And then we're going to add .subscribe because we do need to add .subscribe in order for this addViewToListing thing to actually execute. And when that finishes, we're just going to say console.log Views updated, and that should be all we need. So that's pretty much it for our TypeScript component. The next thing we're going to do is open up our listing detail page [HTML](../../Skills/Web%20Development/HTML.md). And remember that when we built this page originally, we didn't put anywhere on our page where we displayed the actual number of views that a given listing had. So let's add a component for doing that. Basically, all this is going to look like is inside our content box here we're just going to have a paragraph tag that says something like This listing has been viewed, listing.views times. And then what we're going to do
>
> **[6:28](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-listing-detail-page?u=76281980&t=388)** is we're going to use this new is loading member variable that we added to either display the page itself or a loading message. So here's what that will look like. We're going to add a structural directive to this outer div here. We're going to say ngIf, and we only want the actual page to display. If the isLoading member variable is false. Otherwise, what we can do is we're going to do div class equals content-box. And then we're going to say ngIf, and we're going to say if isLoading is true, what we're going to do is just display a little heading that says Loading... So now if we go back to our application and click on one of these here, we'll see that it says this listing has been viewed zero times. And then if we go back and forth with these again, we'll see that it increments the number of times that a listing has been upvoted. So that should be it for our listing detail page. What we're going to do now is do something very similar with our contact page. So let's open up our contact page here, and we're going to go into component.ts. And what we're going to do is inside ngOnInit instead of using fake listings here, we're going use our listing service. And first we need to import our listing service. So we're going to say import listing service from../listings.service. And then we're going to add that to our components constructor.
>
> **[8:06](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-listing-detail-page?u=76281980&t=486)** So private ListingsService: ListingService, and we're going to use that inside our ngOnInit method, by saying this.listingservice.getListingById id.
>
> **[8:27](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-listing-detail-page?u=76281980&t=507)** And then we're going to say .subscribe listing and when the listing is loaded, we're going to say this.listing equals listing. And that's also where we're going to set the message here, Hi, I'm interested in your, and then the listing name. So now if we go back and take a look at our app and click on contact seller, we should see that it loads the correct data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (2), [JSON](../../Skills/Web%20Development/JSON.md) (2), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Code Identifiers:** ngoninit (4), getlistingbyid (3), addviewtolisting (3), listingservice (3), isloading (3)
> **UI Navigation:** click on (2), open the (1)
> **API Endpoints:** /api/listings (1), /api/listings/id/add-view (1)
> **Definitions:** is a  (2)
> **File Paths:** component.ts (1)
> **CLI Commands:** make (1)
> **Env Vars:** html (1)

#### [Connect the My Listings page](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-my-listings-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-my-listings-page?u=76281980&t=0)** - [Narrator] The next page that we're going to connect to our server is the my listings page. Now there are two pieces of functionality on this page that need to make requests. The first is that we actually need to load the user's listings in the first place. And the second is that we need to delete listings whenever the user clicks the delete button on a given listing. So just like with our listing detail page, let's start off by adding the corresponding methods for these two tasks to our listing Service. So let's open up our listing Service here. And the first method we're going to add is going to be called get listings for user. This will load the user's listings for the my listings page. And this is going to return an observable that calls its callback with a listing array. And for this, we're just going to say return this dot http dot get. And this is going to return a listing array as well. And the URL we want to send this to is going to be slash api slash users slash 12345. We're just going to hard code this user ID right now. But once we add authentication to our app, this will be put in dynamically, and then slash listings. So that's our get listings for user method. The next method we're going to define is going to be called delete listing. And it's going to take the ID of the listing we want to delete which is going to be a string. And it's going to return an observable. But since this endpoint doesn't actually respond with a listing, we're just going to put any for the type of the observable here.
>
> **[1:35](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-my-listings-page?u=76281980&t=95)** And we're just going to say return this dot http dot delete. And we can put any here as well. Or we can just leave it without anything. And the URL that we're going to send this request to, we're going to use back ticks and say slash api slash listings, and then put the ID into the URL. And now that we've done that, let's head over to our my listings page and actually use these methods. So here's what that'll look like. Let's open up our my listings page component.
>
> **[2:11](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-my-listings-page?u=76281980&t=131)** And up at the top here, we're going to import our listing Service, import listing service from dot dot slash listings dot service. And we're going to delete fake my listings here, since we won't need that anymore. And inside the constructor, we're going to inject our listing Service, like this private listing Service Listing Service. And then in our ngOInit method, instead of just saying this dot listings equals fake my listings, we're going to do is subscribe to our observable, like we've done in our other components. So we're going to say this dot Listing Service dot get listings for user. And then we're going to say dot subscribe, listings. And when this completes, we're going to set this dot listings equal to the listings we get back from the server. So that's how we load the users listings for the page. The next thing we need to do is actually delete a corresponding listing when the user clicks the delete button. So here's what that'll look like. We're going to delete this alert here. And we're going to save this dot listing service, dot delete listing, listing ID. This will be getting passed to us from the button that we click on. And then we're going to say dot subscribe. And then when this completes, we're going to use a little trick to prevent us from having to reload the listings again,
>
> **[3:44](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-my-listings-page?u=76281980&t=224)** what we're going to do is just say this dot listings equals this dot listings dot filter, and then filter out the listing that we just deleted on the server. We're going to say listing and we're going to say we only want the listings where the listing ID is not equal, to the listing ID that we just deleted up here. And that should be all we need to do. Now if we test this out in the browser, we should see that it's loading our listings from our [MySQL](../../Skills/Software%20Development/MySQL.md) database. And furthermore, if we click on the delete button, it deletes the listing. And just to make sure if we refresh, sure enough, that listing is gone. So that's exactly what we wanted at this point. However, note that if you delete all of your listings, there's not really a good way to get them back yet. For that, we'll need to hook up our new listing page to the server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [MySQL](../../Skills/Software%20Development/MySQL.md) (1)
> **CLI Commands:** make (2), mysql (1)
> **Env Vars:** url (3)
> **UI Navigation:** click on (2)
> **Code Identifiers:** ngoinit (1)
> **Analogies:** just like (1)
> **Warnings:** note that (1)
> **Speakers:** - [narrator] (1)

#### [Connect the New Listing page](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-new-listing-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-new-listing-page?u=76281980&t=0)** - [Instructor] So here's what adding server requests to our new listing page will look like. Like we've done with our other pages, we're going to start off by adding a new method to our listing service. So let's go back to our listing service and the method we're going to add is going to be called create listing and it's going to take three arguments. The name of the new listing, the description of the new listing, which is going to be a string as well and the price of the new listing, which will be a number and this method is going to return an observable that calls it's callback with a listing type and then inside this method, we just have to say return this.http.post. Remember that we have to use a post request since we want to send along this extra information when we create our new listing and this post request is going to respond to the listing and the URL we want to send this request to is going to be /api/listings and for the response body, we're going to pass along our name, description and price arguments inside a [JavaScript](../../Skills/Software%20Development/JavaScript.md) object and then we just need to pass along the HTTP options that we defined up at the top just like we did when we added views to listings. So that's all we need to do for our listing service. Now let's open up our new listing page and just like with our other pages,
>
> **[1:36](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-new-listing-page?u=76281980&t=96)** we're going to start off by importing our listing service. So import listing service from ../listings.service and we're going to inject that service into our constructor, like this, private listings service, listings service and the next thing we're going to do is we're going to rewrite our onSubmit method here. So instead of just displaying an alert, we're going to say this, .listingsservice.createlisting and then for the name, description and price, we're going to have to get those through the arguments of this onSubmit method. I'll show you how to do that in just a second. For now, to do that, we're just going to say name, description and price, like that and then we're going to pass those as arguments to our create listing method, just like this, name, description and price and then we're going to say .subscribe and when this finishes, we're going to put this this .router.navigatebyURL statement inside the callback here so that when this operation completes, it'll navigate us back to our my listings page. So in order to actually get this data, remember that in our listing data form component, we call this this.onsubmit.admit thing with the data that the user filled out in the inputs and the way that we actually access this data from inside our new listing page
>
> **[3:10](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-new-listing-page?u=76281980&t=190)** appear on our onSubmit method is by opening up our new listing pages [HTML](../../Skills/Web%20Development/HTML.md) file
>
> **[3:22](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-new-listing-page?u=76281980&t=202)** and here where we're tapping into the onSubmit output of our listing data form, all we have to do is add this little dollar sign event thing to the parentheses of the onSubmit method. What this will do is it'll pass whatever data we called the admit function with to this onSubmit method. So we're going to save that now and now we should be able to head back to our app and create new listings by filling out stuff like this. We'll just fill this out with some random data here and click create listing and we should see the listing that we just created show up on our my listings page.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Code Identifiers:** onsubmit (6), navigatebyurl (1)
> **Env Vars:** url (1), http (1), html (1)
> **Analogies:** just like (3)
> **API Endpoints:** /api/listings (1)
> **Cross-References:** go back to (1)
> **Definitions:** we call this (1)
> **Speakers:** - [instructor] (1)

#### [Connect the Edit Listing page](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-edit-listing-page?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-edit-listing-page?u=76281980&t=0)** - [Narrator] Okay, so we've come to the last page that we need to hook up to our server. And that is the edit listing page. So here's what connecting to the server is going to look like. As usual, we're going to start off by creating a new method in our Listing Service. We're going to call this one edit listing. And we're going to pass the ID of the listing that we want to change, as well as the updated name, description, and price of the listing that we filled out in the inputs. And this will return an observable that calls its callback with a listing. And then inside this method, we're going to say return this dot HTTP dot post. And this request will respond with a listing. And for the URL that we want to send it to, that's going to be slash API slash listings, slash ID and for the request body we're going to pass along our name, description, and price, as well as the HTTP options.
>
> **[1:15](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-edit-listing-page?u=76281980&t=75)** And now that we have that, let's head over to our edit listing page. And we're going to import our listings service up at the top. So import listing service from dot dot slash listings, dot service. And then we're going to inject it in our constructor. So private Listing Service, Listing Service. And then inside our ngOn init method, instead of saying this dot listing equals fake my listings dot find, we're going to delete that and say this, dot Listing Service, dot get listing by ID So remember that we're loading the corresponding listing here. That's also why this page didn't get populated when we first visited it. And then we're going to say that subscribe. And when we get the listing back in the response, we're going to say this, dot listing equals listing. And then inside our on submit method is where we're going to actually send our edit listing request. So instead of just saying alert, up here, we're going to say this, dot Listing Service, dot edit listing. And for the listing ID, we're going to say this dot listing dot ID. And we're also going to pass the name, description and price, which we have to get out of the arguments here, just like we did in our new listing page.
>
> **[2:49](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-edit-listing-page?u=76281980&t=169)** So we're going to say name, description, and price up here as well. And then we're going to subscribe, so dot subscribe. And when this completes, we're going to put this, this dot router dot navigate by URL thing inside the callback. And the last thing we have to do is just like we did with our new listing page component, where we added this event thing. We're going to add that to our edit listing pages [HTML](../../Skills/Web%20Development/HTML.md). So let's open that file up. And we're going to add dollar sign event to the parentheses of our on submit method here. And we're also going to make a change here so that it only displays this page after the data is actually loaded. So what we can do is we're going to add an ngIf structural directive here, and we're going to say if we have a listing, display this, otherwise we're going to say, div class equals content box for styling. And then ngIf, and if there isn't a listing. What we want to do, is we want to simply display an h3 tag that says loading dot, dot dot, and that we've prevent this page from displaying before we've loaded our listing. So now if we go over to our edit listing page, we see that it's successfully populating all of the existing values for our listing, and that we should be able to make changes to it. And click on the Save Changes button.
>
> **[4:24](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/connect-the-edit-listing-page?u=76281980&t=264)** And we see that those changes are now reflected in the data that we're loading from the server.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (1)
> **Env Vars:** http (2), url (2), api (1), html (1)
> **CLI Commands:** make (2), find (1)
> **Code Identifiers:** ngif (2), ngon (1)
> **Analogies:** just like (2)
> **UI Navigation:** click on (1)
> **Speakers:** - [narrator] (1)


### 5. Authentication

[↑ Back to Table of Contents](#table-of-contents)

#### [What is Firebase Auth?](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/what-is-firebase-auth?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/what-is-firebase-auth?u=76281980&t=0)** - [Instructor] Okay, at this point, we've got a fully functional full stack app. We built out an entire front-end and back-end, and now they're communicating with each other just the way they should be. However, before we release our application into production, there's one thing that's a very important part of most applications, and that's authentication. Now at its most basic, authentication simply means that our application allows users to create accounts and then log in and log out. And it also means that our application keeps track of what belongs to who in our application and prevents users from doing things that they shouldn't be able to do. For example, we don't want users to be able to edit or delete each other's listings, of course. In this section, we're going to see how to add basic authentication functionality to our app. Now obviously, one possibility of adding authentication to our app would be to build out the entire auth flow from scratch. However, authentication can be a very complex and time consuming thing to implement, and at the end of the day, the humble fact is that most of us just don't have the expertise to implement a [Secure Authentication](../../Skills/Web%20Development/Secure%20Authentication.md) flow. So what we can do instead is use what's called an auth provider. Basically, an auth provider is a third-party that takes care of all the complexities of authentication for us. In this course, the auth provider we're going to be using is called [Firebase](../../Skills/Web%20Development/Firebase.md) Auth, which is a tool provided by [Google](../../Glossary/Service/Google.md), largely for free. So as you'll see in coming videos, Firebase Auth makes the user authentication process incredibly easy. We don't have to worry about all these things like encryptions, salt, et cetera. All we have to do is call a single function
>
> **[1:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/what-is-firebase-auth?u=76281980&t=92)** to do things like sign in and sign out. And we'll go into more detail shortly about how exactly this all works, as well as set up our own Firebase project in the Firebase console, which is what this is. So Firebase Auth gives us a pretty wide range of possibilities for how we want to allow our users to authenticate. And this includes standard email password authentication, phone authentication, and a number of OAuth providers, as well as things like email link authentication, which allows users to simply sign in by sending a clickable link to their email address. And adding all of these things to our application takes very little groundwork, as you'll see. In this course, we're just going to be adding some standard OAuth functionality that will allow users to log in with Google, and this'll make it very simple and straightforward for us to allow users to log in and out.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Firebase](../../Skills/Web%20Development/Firebase.md) (5), [Google](../../Glossary/Service/Google.md) (2), [Secure Authentication](../../Skills/Web%20Development/Secure%20Authentication.md) (1)
> **Definitions:** means that (2), is a  (2), is called (1)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Prerequisites:** set up (1)
> **Speakers:** - [instructor] (1)

#### [Set up a Firebase account](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-firebase-account?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-firebase-account?u=76281980&t=0)** - [Narrator] So now that we know what [Firebase](../../Skills/Web%20Development/Firebase.md) of is and why we're going to use it, the first thing we have to do in order to add authentication to our application is create a Firebase account. And then we'll need to create a project for our application in Firebase. So we're going to take a look at how to do those things and as we walk through all of this, keep in mind that the Firebase console might change a bit over time as most websites do, so if you see something that's not quite the same as what I have, don't worry about it too much. The basic flow and concepts that we'll see here should stay pretty much the same. So to create an account, you're going to want to open up a browser and go to firebase.[google.com](https://google.com). And once you're there, you're going to want to click on this get started button. And at this point if you're not already signed in to a [Google](../../Glossary/Service/Google.md) account, it's going to ask you to sign in to one of those. So anyway once you've done that, Firebase should take you to the Firebase console which looks pretty similar to the page we just came from except that now instead of a get started button, we see a create project button. Projects are basically just how Firebase organizes your different applications. So let's click on create a project. And for our project name we can really use whatever we want. This is just an internal string that we use to identify our projects, so it's nothing our users will ever see and you can change it whenever you want to. So for now, we're just going to say buy and sell. And you'll see that as you type your project name, Firebase will have this little box underneath it. And this is your project's project ID. So once you've decided on a project name and project ID, you're going to click this little checkbox here to say that you've already read all the Firebase terms thoroughly,
>
> **[1:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-firebase-account?u=76281980&t=92)** and then click continue. And this will bring us to a page that asks if you want to enable Google Analytics for the project. I'm just going to leave this turned on for this course, and click continue. And finally it'll take you to a page for configuring Google Analytics, this is an overly relevant to the course. So I'm just going to leave the defaults. Click accept for both of these and click on create project. And once you do that, this little spinner thing will go for a while while Firebase is setting up your new app. And once it says your project is ready, let's click on continue. And this will bring you to the project console for your app. Now there's a lot of stuff to look at here, but over on the left we see that we have this sidebar with a few tabs. For example we have this develop tab which allows us to set up things like authentication which is what we're going to be using for this course. So let's actually click on this authentication button here. And the last piece of setup we're going to need to do in this console is we're going to need to create a sign in method for our application. A sign in method is basically just a way that we're going to allow our users to sign in. For example with their email address and password, with their Google account, with their [Facebook](../../Glossary/Service/Facebook.md) account and so on. And for this application we're just going to have users sign in with their Gmail account. So let's click on sign in method here, and we're going to find Google in the providers list and click on edit. And then we're just going to click on enable, and for the project support email we're just going to select our email address here. And then we just have to click save,
>
> **[3:05](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/set-up-a-firebase-account?u=76281980&t=185)** and that's all we need to do for now in the console.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Firebase](../../Skills/Web%20Development/Firebase.md) (11), [Google](../../Glossary/Service/Google.md) (6), [Facebook](../../Glossary/Service/Facebook.md) (1)
> **UI Navigation:** click on (8), go to (1), checkbox (1)
> **Analogies:** for example (2), similar to (1)
> **Prerequisites:** set up (1), setup (1)
> **CLI Commands:** find (1)
> **URLs:** [google.com](https://google.com) (1)
> **Definitions:** is an  (1)
> **Warnings:** keep in mind (1)

#### [Add Firebase to Angular](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-firebase-to-angular?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-firebase-to-angular?u=76281980&t=0)** - [Narrator] Now that we've created the [Firebase](../../Skills/Web%20Development/Firebase.md) project for our app, which will allow us to add authentication functionality, we need to actually hook our [Angular](../../Skills/Web%20Development/Angular.md) app up to this Firebase project we created. In the Firebase console we need to go back to our project's home page, which you can get to by clicking on this project overview in the top left. And then in the middle of the screen here, we're going to click on this web icon. This will ask us to name the specific app. And by the way, Firebase uses the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) app to refer to specific platforms that a given project runs on. So in our case, we're going to call ours Angular Front-end and just leave this checkbox unchecked. And then click on register app. And once we've done that, Firebase will spit out all this code for us to copy and paste into our project, which contains things like the API key, as well as the behind the scenes URLs. And note that all this information is actually safe to share. In other words, none of these things is a secret key that the client can steal and use to exploit our application. Now since the application we're using is an angular app, we're actually not going to copy and paste all this code as is. What we're going to do instead is leave this window up and head over to our front end project code. And what we need to do is we need to install a few [npm](../../Skills/Web%20Development/npm.md) packages into our front end. So we're going to say npm install @angular/fire. This is a package that helps us add Firebase to Angular. And then we're going to install the Firebase package itself and hit enter. And once we've done that,
>
> **[1:31](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-firebase-to-angular?u=76281980&t=91)** here's how we actually use these packages. Let's open up our app.module file and we're going to add a few things here. The first thing we need to do is import a few modules up at the top. So we're going to say import AngularFireModule from @angular/fire and we're going to say import Angular and we're going to say import AngularFireAuthModule from @angular/fire/auth. And then we need to add these two modules to our app's imports. So we need to say AngularFireModule, and this is going to be a little different from our other imports here. We have to actually say AngularFireModule.initializeApp and then we're going to say environment.firebase. And we'll see what this environment.firebase thing is in a second. And after that we're just going to add AngularFireAuthModule. And that should be all we need. Basically, what these modules do is when we run our app, they allow our app to communicate with the Firebase project we created in the Firebase console. So now all we have left to do is go back to the browser and copy this Firebase config [JSON](../../Skills/Web%20Development/JSON.md) thing that Firebase spat out for us. And then we're going to go back to our front end app and we're going to look for a folder called environments, right here.
>
> **[3:05](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-firebase-to-angular?u=76281980&t=185)** And inside that folder, there will be a file called environments.ts. So let's click on that. And then inside this environment object, right underneath the production key, we're going to define another key called firebase and we're going to paste what we just copied from the Firebase console into here. And this, as you may have guessed, is what the environment.firebase thing was in our app.module file. So now, we just need to import environment from the file we were just in. So we can say import environment from ../environments/environment.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Firebase](../../Skills/Web%20Development/Firebase.md) (16), [Angular](../../Skills/Web%20Development/Angular.md) (8), [npm](../../Skills/Web%20Development/npm.md) (2), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [JSON](../../Skills/Web%20Development/JSON.md) (1)
> **UI Navigation:** click on (3), checkbox (1)
> **Definitions:** is a  (2), in other words (1), is an  (1)
> **Prerequisites:** install (3)
> **CLI Commands:** npm (2)
> **Env Vars:** api (1), json (1)
> **Cross-References:** go back to (2)
> **File Paths:** environments.ts (1)

#### [Add Firebase to hapi](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-firebase-to-hapi?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-firebase-to-hapi?u=76281980&t=0)** - Now that should be all we need to do to add [Firebase](../../Skills/Web%20Development/Firebase.md) to our front end. The next thing we're going to do is add Firebase to our backend. Now on our front end, we're going to be using Firebase Auth to allow users to log in and log out, and we'll see how that's done shortly, but it's only half of the equation. What we need to do also, is use Firebase Auth on our backend to do things like verify users and make sure that users are only allowed to edit and delete their own listings. So adding Firebase to our backend is pretty similar to what we just did with our front end, except the names of the packages are going to be slightly different. And actually, the only package we'll need to install here is called Firebase Admin, which we can install by saying [npm](../../Skills/Web%20Development/npm.md) install, firebase admin, and hitting Enter.
>
> **[0:49](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-firebase-to-hapi?u=76281980&t=49)** And once we've done that, we'll need to open up our server.js file. And up at the top of the file, we need to import this Firebase Admin thing like this. We're going to say import * as admin from firebase admin. And then at the very top of our file, up above where we even defined our server here, we're just going to say admin.initializeApp. Now, before this will actually work, what we need to do is give our server the right credentials. Our server will be doing some pretty sensitive things like verifying users auth [Tokens](../../Skills/Web%20Development/Tokens.md). So we need to generate a secret key in Firebase and give that to our server to allow it to do that kind of thing. So here's how we do that. We're going to need to go back to our Firebase console. And if you still have this page up, you can just click on Continue to console. And then we're going to open up our project settings by clicking on this gear icon here and clicking project settings. And then we're going to go over to this tab here that says Service accounts and click on that. And then down here near the bottom of the page, we're going to click this button that says Generate new private key. And then we're going to click Generate key, and this will download a [JSON](../../Skills/Web%20Development/JSON.md) file for us containing some very sensitive information. So be careful with it. Now what we're going to do is, we're going to move this file into our project. So what I'm going to do here is open up a terminal and say mv ~/Downloads/, and then whatever the name of your project is.
>
> **[2:23](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-firebase-to-hapi?u=76281980&t=143)** And you should be able to hit Tab here and it'll auto fill it for you. And then I'm going to move that to this current project and just copy the name here, just like that. And then if we take a look at our file tree here, we should see that that file is now inside the root directory of our project. And then just for ease of use here, what we're going to do is we're going to rename this to something like credentials.json instead of that big, long name. So what we can do now is back inside our server file, we're going to import the data from that file we just moved into our directory and we're going to say import credentials from ../credentials.json, and then what we need to do is we need to pass these credentials to our initializeApp function. So that'll look something like this. We're going to say credential admin.credentail.cert and pass our credentials JSON object to that. And that's all we really need to do to set up Firebase on our backend. Now, one thing to remember is that this credential.json file does contain secret keys. So make sure that you don't commit it as is to [GitHub](../../Skills/Software%20Development/GitHub.md) or anywhere else that people might see it. So if you have a gitignore file, for example, you might want to add the name of that file to it, so credentials.json. And also keep in mind that for these very security reasons, the exercise files for this and all following videos, won't include this credentials.json file, since it points at my own app. So you'll have to add in your own file
>
> **[3:56](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/add-firebase-to-hapi?u=76281980&t=236)** if you want to run those.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Firebase](../../Skills/Web%20Development/Firebase.md) (12), [JSON](../../Skills/Web%20Development/JSON.md) (7), [npm](../../Skills/Web%20Development/npm.md) (1), [Tokens](../../Skills/Web%20Development/Tokens.md) (1), [GitHub](../../Skills/Software%20Development/GitHub.md) (1)
> **File Paths:** credentials.json (4), server.js (1), credential.json (1)
> **CLI Commands:** make (2), npm (1), mv (1)
> **Prerequisites:** install (3), set up (1)
> **Code Identifiers:** initializeapp (2)
> **Env Vars:** json (2)
> **Tools:** terminal (1), github (1)
> **UI Navigation:** click on (2)

#### [Sign in and out](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/sign-in-and-out?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/sign-in-and-out?u=76281980&t=0)** - [Instructor] Okay, so now that we've added the basic [Firebase](../../Skills/Web%20Development/Firebase.md) off set up to both our front and backend, the next logical step is to allow our users to log in and log out. And the fact is that while all that setup we just went through was maybe a bit complicated, now that we have it all done, actually performing the authentication is almost laughably easy. In this video, we're going to see how to do that, as well as how to display some message when users aren't logged in, basically just a screen that says something like, hey, you need to log in first. So the first thing we're going to do is, we're going to open up our app component file. This is the component where we'll be letting users log in and out from. And up at the top here, we're going to say import AngularFireAuth. That's the package that we'll be using to log in with. From @[Angular](../../Skills/Web%20Development/Angular.md)/fire/auth. And then we're going to say import auth from firebase/app. And then what we're going to need to do is we're going to need to start off by adding a constructor method to our app component, and that'll look like this. And then, inside that constructor, we're going to inject our AngularFireAuth package, but instead of saying private for this package, what we're going to do is we're going to say public auth AngularFireAuth, and you'll see why we're doing public in a second. And what we're going to do now is we're going to define two methods inside of our app component. One for signing users in, this will be triggered when they click the sign in button, which we haven't defined yet,
>
> **[1:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/sign-in-and-out?u=76281980&t=92)** by the way, we'll get to that soon. And this method will look something like this. We're going to say signInClicked. And you can put void for the type here, since it's not going to return anything. And then we're going to say this.auth.signInWithPopup. And then inside of there, we're going to say new auth.GoogleAauthProvider with parentheses after it. So what this is going to do here is it's going to bring up a new tab in our user's browser that'll have them sign in with their [Google](../../Glossary/Service/Google.md) account. We'll see exactly what that looks like shortly as well. So that's our sign in method. The next thing we're going to do is define a method for signing out, which will be triggered when the user clicks the sign out button, which we also haven't defined quite yet. So that's going to look like this. We're just going to say, signOutClicked. This will be void as well. And we're just going to say this.auth.signOut. So signing users in and signing them out when using Google Auth is really just as simple as this. It's a simple function called to sign in and a simple function call to sign out. So that pretty much completes our changes to our app's TypeScript file. The next thing we're going to do is make a few changes to our app component's [HTML](../../Skills/Web%20Development/HTML.md) file. Basically, what we're going to do here is first, we're going to create two different screens that will be displayed depending whether the user is logged in or not, and second, we're going to add sign in and sign out buttons that are going to be linked to the methods we just created.
>
> **[3:07](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/sign-in-and-out?u=76281980&t=187)** And that'll look like this. We're going to start off by having this div with our router outlet and nav bar only be displayed if the user is logged in. So what we're going to do is we're going to use an ngIf structural directive here, and we're going to say auth.user pipe async, and all this async thing here means is that auth.user is asynchronous and we want to listen for changes to it. So once we've done that, the next thing we're going to do is, underneath this content-wrap div, we're going to add a sign out button, since this whole div will be shown when the user is signed in. So we're going to say button, and then for the button's click event, we're going to say signOutClicked, and then sign out. And the next thing we're going to do now is define the elements that should be displayed if the user is not currently logged in. So here's what that's going to look like. We're going to say div ID equal max-width-column, and we're going to say star ngIf, and we're going to do the opposite of the structural directive that we had up here. So we're going to say not, and then we need to use parentheses here to wrap what we're going to type. So not aith.user pipe async, and that'll just display whatever's inside this div only if there isn't an auth user, and inside this div, when the user's not auth, we want to just display a heading that says something like, you need to sign in first. And underneath that, we'll have the sign in button, which will look like this.
>
> **[4:39](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/sign-in-and-out?u=76281980&t=279)** For the click event, we're going to call our signInClicked. And we'll say sign in with Google for the button's text. And now we can test this whole flow out by opening up our app. You should make sure that your frontend and backend are both running before you do this. And if we go over and take a look at our app, we see that we have this sign in with Google button now that we can click on. And this will open up a new tab that allows us to pick the Google account we want to sign in with. So I'm going to log in with this email address here, and that'll send us back to our frontend, and lo and behold, we're now logged in and we can see the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the app. And we can also sign out again if we want to by clicking the sign out button we created, and we'll just see the sign in screen again.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google](../../Glossary/Service/Google.md) (5), [Firebase](../../Skills/Web%20Development/Firebase.md) (2), [Angular](../../Skills/Web%20Development/Angular.md) (1), [HTML](../../Skills/Web%20Development/HTML.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **Code Identifiers:** signinclicked (2), signoutclicked (2), ngif (2), signinwithpopup (1), signout (1)
> **CLI Commands:** make (2)
> **Prerequisites:** set up (1), setup (1)
> **Env Vars:** html (1)
> **UI Navigation:** click on (1)
> **Speakers:** - [instructor] (1)

#### [Load user listings with Auth](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/load-user-listings-with-auth?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/load-user-listings-with-auth?u=76281980&t=0)** - [Instructor] So now that we've allowed users to sign in and out, it's time to start incorporating [Firebase](../../Skills/Web%20Development/Firebase.md) Auth into some of the other parts of our app. Recall that earlier in the course, there were several places in both our front end and back end, where we sort of faked a user by hard coding a user ID. The get listings for user method and our Listing Service was a good example of this, where we simply hard coded one two three four five for the requests that our front end made to our back end. But now that our users are actually logged in, the currently logged in user will have a unique user ID that Firebase Auth automatically assigns to them. So let's take a look at how we can use this ID to make our app actually load the correct listings for a user. Now, this will involve making changes to both the front end and back end. But we're going to start with the front end here. So inside our listings dot service file, let's make a few changes. The first thing we're going to do is up at the top, we're going to say import, [Angular](../../Skills/Web%20Development/Angular.md) fire auth from at Angular, slash fire slash auth. And then what we're going to do is just like we define this HTTP option, saying that we passed along with some of our requests, we're going to define a function called HTTP options with auth token. And what this will do is it's going to take the user's auth token, and pass that along with the requests headers. So what we're going to do is we're just going to copy this headers thing here, and put that inside of here. And then underneath content type,
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/load-user-listings-with-auth?u=76281980&t=94)** we're going to add auth token and pass along the token that we're getting from the argument. So now that we have that, the next thing we're going to do is we're going to provide the angular fire auth module to our Listing Service so that it can use it. So underneath our private HTTP here, we're going to say private auth Angular fire auth. And next we're going to modify our get listings for user methods so that it actually uses the currently logged in user's ID and auth token. Now, I'm just going to code all of this out first, and then I'll explain it, it's going to get a little lengthy but don't worry too much about it. What we're going to do is we're going to say return new observable, we're going to create a new observable here, listing array. And then we're going to pass a callback to this new observable here. That'll say, observer, and what we're going to do is we're going to say this dot off dot user dot subscribe And when we get a new user, we're going to check to see if the user exists. And if they do, we're going to say user dot get ID token parentheses dot then and when we get their ID token back, we're going to check to see if the user and token exist. And if they do, then what we're going to do is, we're going to say this dot HTTP dot get. And instead of hard coding one two three four five here, we're going to change this to backticks,
>
> **[3:09](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/load-user-listings-with-auth?u=76281980&t=189)** and insert the user's unique ID here. And we do that by saying user dot uid. And then we're going to actually subscribe to this inside of here. So we're going to say, dot subscribe. And for the callback, we're going to say listings. And we're going to say observer dot next, and pass along those listings to whoever's listening to this get listings for user method. And one more thing we need to do here is as the second argument here, we're going to pass along our HTTP options with auth token and pass the token object that we got for the user. And one more thing we're going to do here is just add an else clause to this if statement. So if either the user or token don't exist, we're just going to call observer, next with an empty array, which will pass just an empty array to whoever subscribes to this method. Okay, so what's all going on here exactly? Well, instead of just returning this HTTP dot get observable that our components can subscribe to, what we're doing is we're creating our own observable whose behavior is a little more complex. Basically, what we have here is a series of nested observables, where we start off by getting the current user, then getting their auth token, and then we make the HTTP dot get request with those two extra parameters. And then if everything went well, what we do is we return the listings
>
> **[4:41](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/load-user-listings-with-auth?u=76281980&t=281)** that we loaded from the back end, otherwise, we simply return an empty array. So that should be all the changes we need to make to our front end for now, let's move on to our back end. This is going to involve making some changes to our get user listings route, so let's open that up.
>
> **[5:02](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/load-user-listings-with-auth?u=76281980&t=302)** And then up at the top, we're going to say import star as admin, from firebase admin. And this firebase admin package is the package that will allow us to do things like verify the user's auth token, which we can do like this. We're going to start off inside our route handler by getting the auth token header that we're sending from the front end. So up here, we're going to say const token equals request dot headers, dot auth token. And then we're going to get the corresponding user from the auth token and make sure that that user matches the user whose data is being requested. If it doesn't, then we'll throw a boom error saying that whoever is sending the request isn't authorized. So here's what that will look like. We're going to say const user equals await, admin, dot auth, parentheses, dot verify ID token. And we're going to verify the token that we got from the request header. And then we're going to do is, underneath this user ID thing here, we're going to say if the user that we got from firebase auth, if their user ID is not equal to the user ID that's being requested, then what we're going to do is we're going to say throw, boom dot unauthorized, with a message saying users can only access their own listings.
>
> **[6:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/load-user-listings-with-auth?u=76281980&t=392)** And I'm just realizing now that this should be request dot headers, dot auth token instead of request header. So make sure to change that. And that should be about all we need for now. If we test our application now, we might need to sign in again first, if you signed out. At this point, we should be able to go over to our My Listings page. And at this point, we'll see nothing. But that's just because we're now logged in with our gmail account, and so the user ID that we're now sending with our requests is not the one two three four five ID that we created a lot of our listings with. For that we'll need to make the same kind of changes that we made in this video to the Create Listing Flow.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Firebase](../../Skills/Web%20Development/Firebase.md) (5), [Angular](../../Skills/Web%20Development/Angular.md) (4)
> **CLI Commands:** make (7)
> **Env Vars:** http (7)
> **Definitions:** is a  (2)
> **Cross-References:** earlier in (1)
> **Analogies:** just like (1)
> **Best Practices:** make sure to (1)
> **Speakers:** - [instructor] (1)

#### [Create listings with Auth](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-listings-with-auth?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-listings-with-auth?u=76281980&t=0)** - [Instructor] So let's take a look at how to incorporate authentication into our new listings page and the corresponding server end point. Now, just like with getting a user's listings, we're going to start off by modifying the create listing method in the listing service. This will probably look very familiar. We're going to say something like this. We're going to say return new observable listing, and then we're going to say observer, and we're going to say this dot auth dot user dot subscribe, and get the currently authenticated user. And then we're going to get the user's auth token, so we're going to say user and user dot get ID token, and then we're going to say dot then, since this is a promise, and we're going to say token, and once we have the token we're going to copy and paste the original request like this without the return statement. So we're going to say this dot http dot post listing. We're going to send it to the API slash listings end point with the payload of the name, description, and price that we want to create a new listing with. And then instead of http options, we're going to have to change this to http options with auth token and pass the auth token that we got. And then we're going to say dot subscribe, and whenever something happens, we're just going to say observer dot next. Just like that.
>
> **[1:33](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-listings-with-auth?u=76281980&t=93)** So that's the front end. For the back end, we're going to make a few simple changes to our create listing route, so let's open that up here. Open up create new listing dot js, and we're going to start off my importing [Firebase](../../Skills/Web%20Development/Firebase.md) admin, so import star as admin from firebase admin. And then just like with the route that we saw previously, we're going to get the auth token from the request headers and verify it, which will look like this. We're going to say const token equals request dot headers dot auth token. And then we're going to get the actual user by saying const user equals await admin dot auth dot verify ID token, token. And then instead of saying const user ID equals one two three four five and just hard coding that here, we're going to say const user ID equals user dot user ID.
>
> **[2:41](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-listings-with-auth?u=76281980&t=161)** And note that at this point, we might want to check to see if this user actually exists, so that we're not just creating a new listing without a given user. But for now I'm just going to skip over that since you know how to add that already. And those should be all the changes we need. The [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) will be reflected down here. The new user ID that we're no longer hard coding will be inserted into our [SQL](../../Skills/Data%20Science/SQL.md) query and that should make it work exactly the way we want it to. So now if we save this file and go back to our my listings page, we should be able to click on create new listing and fill something out like this,
>
> **[3:24](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/create-listings-with-auth?u=76281980&t=204)** and click on create listing. And we should see that show up inside our my listings list here.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Firebase](../../Skills/Web%20Development/Firebase.md) (2), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [SQL](../../Skills/Data%20Science/SQL.md) (1)
> **Analogies:** just like (3)
> **CLI Commands:** make (2)
> **Env Vars:** api (1), sql (1)
> **UI Navigation:** click on (1)
> **Definitions:** is a  (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Edit listings with Auth](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/edit-listings-with-auth?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/edit-listings-with-auth?u=76281980&t=0)** - [Instructor] Now that we've done the create listing flow, our next step is to incorporate user authentication into the edit listing flow. This will actually be incredibly similar to what we just saw. What we're going to do is head over to our front end. And scroll down to the editListing method. And inside there, we're going to say return new Observable Listing. And then we're going to say observer. And this is going to be a callback. And inside there, we're going to say this.auth.user.subscribe. And we're going to use that user object to get the user's ID token. So we're going to say user && user.getIdToken.then.
>
> **[0:49](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/edit-listings-with-auth?u=76281980&t=49)** And once we have the token, we're going to copy and paste the original request here into this callback. And we're going to change this to httpOptionsWithAuthToken. Token. And then we're going to say .subscribe and just call observer. Oops, and actually this should be observer up here. So we're going to say observer.next. So that's the only change we really have to make on our front end. On our back end, we're going to make some changes to our update listing route. So let's open that up. And up at the top, just like we've seen in our other routes, we're going to import * as admin from [Firebase](../../Skills/Web%20Development/Firebase.md)-admin. And then down here, underneath where we get the name, description, and price from the payload, we're going to say const token = req.headers.authtoken. And then we're going to say const user = await admin.auth .verifyIdToken. Then we're going to verify that token that we're getting from the headers. And then instead of saying const userId = 12345 and hard coding it here, we're going to say const userId = user.user_id.
>
> **[2:25](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/edit-listings-with-auth?u=76281980&t=145)** And that should be all we need to do now. So if we take a look at our application again, we should be able to edit it. And click Save Changes. And we should see those changes updated from our database.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Firebase](../../Skills/Web%20Development/Firebase.md) (1)
> **Code Identifiers:** userid (2), editlisting (1), getidtoken (1), httpoptionswithauthtoken (1), verifyidtoken (1)
> **CLI Commands:** make (2)
> **UI Navigation:** scroll down (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Delete listings with Auth](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/delete-listings-with-auth?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/delete-listings-with-auth?u=76281980&t=0)** - [Instructor] All right, we've done the, my listings page, the create listings page and the edit listings page. So the last step in this section is going to be the add user authentication to the delete listing flow. So here's what that's going to look like. Inside our listing service's delete listing method, what we're going to do is we're going to say, return new Observable, and this is going to be observable any, instead of observable listings since the delete listing end point doesn't actually return a listing. And we're going to say observer. And then this dot auth, dot user, dot subscribe, pretty much the same as what we've seen anywhere else. And then user and user dot get ID token dot then token.
>
> **[0:51](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/delete-listings-with-auth?u=76281980&t=51)** And we're going to copy and paste this request and say this, dot HTTP dot delete. And we're going to pass the HTTP options with auth header, and we're going to pass the HTTP options with auth token to this delete request, just like that. And then we're just going to say subscribe. And when the updates, we're going to call observer dot next. So nothing too complicated there. Pretty much what we've seen before. Now let's move over to the backend and we're basically going to do what we've done with the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our routes. So let's open up our delete listing route. And then underneath here, we're going to say const token equals request dot headers dot auth token. And then const user equals await admin dot auth dot verify ID token, token. And of course we need to actually import off of at the top. And of course we need to actually import admin up at the top. So we're going to say import star as admin from [Firebase](../../Skills/Web%20Development/Firebase.md) admin. And then we're just going to say const user ID, equals user, that user ID. And what we're going to do is we're going to insert this user ID into my [SQL](../../Skills/Data%20Science/SQL.md) query to make sure that the listing will only get deleted
>
> **[2:26](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/delete-listings-with-auth?u=76281980&t=146)** if the correct user is actually requesting it. So we're just going to add, And user ID equals question mark, and then we're going to add user ID to this array argument here. And now we should be able to go back to our app and delete our listings and it'll work fine. If we refresh the page, it'll still be gone and that's about it. So keep in mind too, that we didn't add all of this auth stuff to every single route on our server. And while you may or may not want to eventually do that at some point, the routes that we've done are the critical routes where we need to prevent users from doing things that they shouldn't be able to do such as delete each other's listings. For things like fetching a list of all the users on the site, the need isn't quite as strong to make sure that users are authenticated.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1), [Firebase](../../Skills/Web%20Development/Firebase.md) (1), [SQL](../../Skills/Data%20Science/SQL.md) (1)
> **Env Vars:** http (3), sql (1)
> **CLI Commands:** make (2)
> **Analogies:** just like (1), such as (1)
> **Cross-References:** go back to (1)
> **Warnings:** keep in mind (1)
> **Speakers:** - [instructor] (1)


### 6. Hosting Your Site

[↑ Back to Table of Contents](#table-of-contents)

#### [Move a full-stack app to the cloud](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/move-a-full-stack-app-to-the-cloud?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/move-a-full-stack-app-to-the-cloud?u=76281980&t=0)** - [Instructor] Okay, at this point in the course we've got our entire full-stack application set up. We've got the [Angular](../../Skills/Web%20Development/Angular.md) front end, we have the Hapi JS server, we have a [MySQL](../../Skills/Software%20Development/MySQL.md) database, and we have [Firebase](../../Skills/Web%20Development/Firebase.md) Auth as our auth provider. And all of this is something to be really proud of since not many web developers know how to do what we just did. Most web developers spend their entire careers working on either the front end or the back end without ever seeing how all the pieces fit together like we've done so far in this course. However, at this point, all of these pieces are still running locally on our own computer and the only ones that can access them are ourselves. So in this section, we're going to see how we can move our app to the cloud so that it can be accessed by anyone. And to host our app, we're going to be using a platform called [Google Cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md). Now, if you've never deployed a full-stack app before, this might seem pretty scary. As developers, we're generally used to just working on the code and letting someone else take care of all the deployment and hosting stuff. And to be honest, it might be a little bit out of your comfort zone at first but once you've seen how it all works and all the details to be aware of, I guarantee you'll become comfortable with it fairly quickly. Now, before we get into deploying our app, let's first take a look at the basic process we're going to be following. This'll help put everything we'll be doing throughout the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of the course into perspective. Here are the basic steps we're going to be following. The first thing we need to do is install Google Cloud's command line tools. These tools contain scripts that we can run that will quickly and easily deploy our code to Google Cloud. Next, we're going to be setting up a MySQL instance
>
> **[1:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/move-a-full-stack-app-to-the-cloud?u=76281980&t=94)** on our Google Cloud. Currently, we've been storing all our data on a local database but in order to release our app, we'll have to have our database in a more accessible and available location. After that, we're going to move our Hapi server to Google Cloud, followed by building our front end app, having our Hapi server serve our front end's files, and then deploying all of that to Google Cloud. And that should be about it. Now, you may have noticed that we don't really need to do anything with Firebase Auth in this process. And that's because Firebase Auth is already on the cloud. If we really wanted to, we could have run Firebase Auth locally but that's a slightly involved process and it's not really that relevant to the course. Anyway, that's just one less thing we have to do in this section.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) (6), [Firebase](../../Skills/Web%20Development/Firebase.md) (4), [MySQL](../../Skills/Software%20Development/MySQL.md) (2), [Angular](../../Skills/Web%20Development/Angular.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** mysql (2)
> **Prerequisites:** set up (1), install (1)
> **Tools:** command line (1)
> **Warnings:** be aware (1)
> **Speakers:** - [instructor] (1)

#### [Installing the Google Cloud CLI](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/installing-the-google-cloud-cli?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/installing-the-google-cloud-cli?u=76281980&t=0)** - [Instructor] So now that we know a little bit more about how the process of hosting our app is going to work, the first step we're going to take is to install and set up the [Google Cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) tools. So the first thing we're going to want to do here is go to the Google Cloud console in your browser. And this is a different thing than the [Firebase](../../Skills/Web%20Development/Firebase.md) console that we used earlier when we were adding authentication to our app. You can get there by going to [console.cloud](https://console.cloud).[google.com](https://google.com). And when you go there, you might need to set up your account. At the moment, Google Cloud is offering a free trial for new accounts. So I'm going to click on agree, agree and continue. And I'm going to click on try for free here. And just walk through the basic setup. And you might need to fill out some of your billing information. However, Google Cloud will not bill you until you turn on automatic billing. So don't worry about that. And once you click start my free trial, it'll bring you here and just click got it. And now that we've set up our Google Cloud console, let's head back to our terminal and install the command line tools. So here's how we do that. We're going to say curl [https://sdk.cloud.google.com](https://sdk.cloud.google.com).
>
> **[1:15](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/installing-the-google-cloud-cli?u=76281980&t=75)** And then | bash. And hit Enter. And then it's going to ask you a few questions. I usually just leave the defaults for those.
>
> **[1:33](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/installing-the-google-cloud-cli?u=76281980&t=93)** And then we're going to need to run the command exec -l $SHELL to reload our terminal. And now we should be able to say gcloud init. And hit Enter. And this will take us through configuring our Google Cloud CLI. So it'll ask us if we want to log in. And we're going to say yes for that and hit Enter. And that's going to open up a login window for us here. So just pick the account that you set up your Google Cloud account with. And we're going to click on Allow. And now we're going to go back to our terminal. And it's going to ask us which cloud project we want to use. We're going to select our buy and sell project. And this was created when we created our Firebase project in the Firebase console since those two things are linked on [Google](../../Glossary/Service/Google.md). So I'm going to enter two. Your option might be different. And that should be all we need to do. Just to make sure that Google cloud is set up with our correct project, we're just going to type in gcloud config get-value project. And that should show the project ID of our buy and sell site that we created.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) (8), [Firebase](../../Skills/Web%20Development/Firebase.md) (3), [Google](../../Glossary/Service/Google.md) (3), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2)
> **Prerequisites:** set up (5), install (2), setup (1)
> **Tools:** terminal (3), command line (1), bash (1)
> **CLI Commands:** gcloud (2), curl (1), make (1)
> **URLs:** [console.cloud](https://console.cloud) (1), [google.com](https://google.com) (1), [https://sdk.cloud.google.com](https://sdk.cloud.google.com) (1)
> **Env Vars:** cli (2), shell (1)
> **UI Navigation:** click on (3)
> **Cross-References:** go back to (1)

#### [Moving MySQL to the cloud](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/moving-mysql-to-the-cloud?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/moving-mysql-to-the-cloud?u=76281980&t=0)** - [Instructor] So now that we've got our [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) setup, the next thing we're going to do is move our My [SQL](../../Skills/Data%20Science/SQL.md) database to the cloud. And to do that, we're basically just going to create a new My SQL database on the cloud and walk through the same kind of setup that we did earlier in the course when we created our local database. So let's open up the Google cloud console and the first thing we need to do is make sure that we're on our buy and sell project by clicking on the dropdown menu up here. And once we're there, we're going to open up the menu on the left and scroll down to where it says SQL. And we're going to click on that. And then will just ask us for a billing account and we'll select my billing account and click set account. And then we're going to click create instance, and we're going to select [MySQL](../../Skills/Software%20Development/MySQL.md) here. And then we're going to need to fill out a few pieces of data about our new database. For the instance ID let's put in, buy and sell DB. And then for the root password, just put in something you'll remember, and then select whatever region is closest to you and leave zone on any. And we're going to leave database version on MySQL 5.7 and click create. And after that, we'll need to wait a few minutes for Google cloud to set up our database. But once that finishes, we're going to click on the name of this database we just created. And the next thing we're going to do is connect to this new database using the My SQL workbench program that we used earlier. So let's open that up and you're going to want to go back to the homepage here. And once you're there,
>
> **[1:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/moving-mysql-to-the-cloud?u=76281980&t=92)** we're going to click on this little plus button next to My SQL connections, and this will bring up a pop up window that we need to fill out. So for the connection name here, we're going to name this buy and sell DB and now for the host name we're going to need to go back to our Google cloud console and find the public IP address of the database we created. That'll be right here. So we're going to copy that and paste that in the host name box here. And then for username we're going to leave it on route and we're going to need to provide the password that we created for this later. And then we're going to leave this last box blank and click Okay and that'll add our connection to the list here. Now, before we can actually connect to our Google cloud database, we're going to need to tell Google cloud to allow our computer to connect to it otherwise it'll block us. So let's go back to the cloud console. And over on the left here, we're going to click on connections and then we need to scroll down to this authorized network section here and add our own computer to it. So we're going to click on add network. And then for the name, we're just going to say something like local computer, and then for the network, we'll need to find out what our IP address is. And to do this, you can just open up [Google](../../Glossary/Service/Google.md) and search for what's my IP address. And then we're going to copy that and paste it inside the network box. And then we're going to click done. And then we need to click save here. And this might take a little while to complete as well.
>
> **[3:07](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/moving-mysql-to-the-cloud?u=76281980&t=187)** So now that we've done that we should be able to connect to our cloud database. So in my SQL workbench, let's click on the new connection that we created, and that should ask us for our password here, just input whatever password you assigned there. And now we're connected to our cloud instance of My SQL. So the setup here is going to be almost identical to what we did earlier. We're going to create a new schema called buy and sell. So click on this little button up top and we're going to name it buy and sell and then click apply and apply and close. And then we're going to create a listings table to store our listings. Now, instead of doing this all manually, what I'm going to do is simply copy and paste a My SQL query here that'll do all the basic setup for us, and you can find that in the exercise files too, and you can find that in the exercise files so that you can do the same thing. So I'm just going to paste this query for setting up the table in this window here, and then click the little lightning bolt icon. And we should see that it completed successfully down at the bottom. And once we've created the table, let's populate it with a few new records and for that, I'm going to copy and paste some SQL as well, which will also be in the exercise files for you. And then we're going to click the lightning bolt icon again. And actually in order for this query to work inside MySQL workbench, we have to go over to schemas and double click on buy and sell
>
> **[4:40](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/moving-mysql-to-the-cloud?u=76281980&t=280)** and then click the lightning bolt icon, not very self explanatory, but that's what we have to do. And once we've done that, we need to add another user to this database, just like we did before. And we're going to click on users and privileges, and then we're going to go to add account and for the login name here, we're going to use happy server, just like before, for the password, I'm just going to do something simple and then confirm the password. And then we're going to click on apply, make sure that this happy server user is selected and we're going to go to schema privileges, and we're going to click on add entry and we're going to do selected schema, buy and sell just like we did before. And then for object rights, we're going to give it select, insert, update and delete privileges and click apply. And that should be all of the setup that we need to do for our database for the time being. So now what we're going to do is we're going to head over to our backend repository and open up our database.JSfile. And we're going to need to make some changes to the object that we're passing to MySQL.createconnection here. First off for the host property, we're going to have to change this to the public IP address of our My SQL instance on the cloud. So let's copy that again and paste that in here. And then if you gave a different username or password to the user we just created
>
> **[6:13](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/moving-mysql-to-the-cloud?u=76281980&t=373)** you'll need to change those here. I gave mine the same one. And then for the database, this is going to be the name of the schema that we created earlier. So that's just going to be buy and sell. And now we're going to want to save this file and it's also not a bad idea to restart our server. And if we take a look at our app now we should see that this is now connected to the MySQL database that we created on Google cloud and in my listings, we still have nothing there. So we should be able to create a new listing now, I'll just use the same ones that I've already created and that works and deleting it works as well. So everything is still working like we want it to except now the MySQL database is on Google cloud instead of running locally.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [SQL](../../Skills/Data%20Science/SQL.md) (10), [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) (8), [MySQL](../../Skills/Software%20Development/MySQL.md) (6), [Google](../../Glossary/Service/Google.md) (1)
> **UI Navigation:** click on (11), scroll down (2), go to (2), dropdown (1)
> **CLI Commands:** mysql (6), find (4), make (3)
> **Env Vars:** sql (10)
> **Prerequisites:** setup (5), set up (1), you'll need (1)
> **Cross-References:** go back to (3), earlier in (1)
> **Exercise Files:** exercise files (3)
> **Analogies:** just like (3)

#### [Environment variables](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/environment-variables?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/environment-variables?u=76281980&t=0)** - [Instructor] Okay, so previously we saw how to point our hapi server at the cloud based [MySQL](../../Skills/Software%20Development/MySQL.md) instance that we created. However, in real life when we have several different [Databases](../../Skills/Software%20Development/Databases.md) available for our back end to connect to, we'll want to have a way to break those details out into separate files that get loaded into our server automatically, and the way that we generally do this in full stack apps is by using something called environment files. Basically, environment variables just contain a number of different variables and values that should be different based on where our code is being executed from. So for example, what the values that we passed to this create connection to be different depending on whether we're running our server locally, or whether it's running on the cloud. So here's how we use environment files on a node server. We're going to start off by installing a package called dotenv, that will basically take an environment file and assign whatever values it contains to our apps environment, and we'll see exactly how this works shortly. So to install this dotenv package, let's run [npm](../../Skills/Web%20Development/npm.md) install dotenv and hit Enter. And then we're going to create an environment file, which is just called dotenv by default, hence the name of that package we just installed. So in the root directory of our project, let's create a new file called dotenv, and then inside here, we're going to add the old values that we just replaced to it. So we're going to say DB user equals hapi server. DB pass equals abc one, two, three exclamation point,
>
> **[1:37](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/environment-variables?u=76281980&t=97)** DB name equals buy and sell. And then for DB host, we're going to put localhost because this environment file is what's going to be used when we run our code locally. We'll want it to be localhost here. And once we've done that, we're going to go into our server file, and up at the very top, we're going to import our dotenv package. So import dotenv from dotenv. And then we're going to say dotenv dot config, and that should take care of taking all these variables that we've defined inside this file, and setting those environment variables on our server when it runs. So what we're going to do now is instead of having all these strings hard coded here, we're going to use environment variables that we just defined. So for host, we're going to say process dot env, which is how we access environment variables in [Node.js](../../Skills/Web%20Development/Node.js.md), and then we're going to say DB host. And then for user, we're going to say process dotenv dot DB user. And then for password, process dotenv dot DB pass and then for database process dot.env dot DB name, and then what we need to do is we need to move this code where we define our connection into this connect function. So we're going to just copy
>
> **[3:11](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/environment-variables?u=76281980&t=191)** connection equals MySQL dot create connection. And we're going to need to change this from a comma to a semi colon. And then up here, we're going to need to say let connection so that the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) of our app has access to that. And the reason that we're doing this here is to make sure that these environment variables are already set before this code actually gets executed. So now let's run our server again with npm run dev. And now we should be able to take a look at our app again and see that it's connecting to our local database, which is what we defined inside our environment file.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [MySQL](../../Skills/Software%20Development/MySQL.md) (2), [npm](../../Skills/Web%20Development/npm.md) (2), [Databases](../../Skills/Software%20Development/Databases.md) (1), [Node.js](../../Skills/Web%20Development/Node.js.md) (1), [Rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md) (1)
> **CLI Commands:** mysql (2), node (2), npm (2), make (1)
> **File Paths:** node.js (1), dot.env (1)
> **Prerequisites:** install (2)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Host a Node server on Google Cloud](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/host-a-node-server-on-google-cloud?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/host-a-node-server-on-google-cloud?u=76281980&t=0)** - [Instructor] Okay, so now that we've got some environment, variable stuff set up, the next thing we're going to do is we're going to move our entire Hapi server to [Google Cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md), and to make this happen, what we need to do is create a new file in the root of our backends directory, so we're going to say New File, and this file is going to be called app.yaml, and then inside this file, we're just going to add a few configuration options. So we're going to say runtime nodejs, environment flex and beta_settings. We need to say cloud_sql_instances, and then what we need to do is we need to go back to the Cloud console and we need to find this connection name here and copy that, and then we need to paste that here. And once we've done that, the last thing we need to add to this file is some environment variables that will be loaded into our server when it's running on Google Cloud. So this is in contrast to the .env file that we just created in the previous video, and that's going to look something like this, we just have to say env_variables, and then we're going to define the same kind of variables that we did in our environment file. We're going to have DB_USER, and that's going to be hapi-server, DB_PASS is going to be abc123, DB_NAME is going to be buy-and-sell, and then since these variables
>
> **[1:33](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/host-a-node-server-on-google-cloud?u=76281980&t=93)** are what's going to be loaded when it's running on Google Cloud, instead of having DB_HOST, which actually won't work in our particular situation, we're going to add something called DB_SOCKET, and you'll see how this is used in a little while, and the value for this is going to be /cloudsql/, and then this connection name that we just copied and pasted from the Cloud console. So we're going to paste that there, and then we're going to save this file, and then we need to go into our database.js file, and because we're using this DB_SOCKET environment variable when it's running on Google Cloud, instead of DB_HOST, what we need to do is add another thing to our create connection object called socketPath, and the value for that is going to be process.env.DB_SOCKET. And next, there are just two changes that we need to make to our server.js file, so let's open that up. What we need to do is we need to change the port number and the host name that our server is running on. See, Google Cloud expects our server to be running on port 8080, and in order for all of this to work, we'll also have to change the host name here from local host to 0.0.0.0. and that's just the way we have to do it on Google Cloud. So now that we've done those things, there's one last thing we're going to have to do on our backend. You see, we've been using the Babel node package during development to allow us to write and run modern [JavaScript](../../Skills/Software%20Development/JavaScript.md) code, but to run our code in production,
>
> **[3:06](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/host-a-node-server-on-google-cloud?u=76281980&t=186)** we'll need to use Babel to actually build all of our code, and then we're going to need to run the built version of our code. So let's open up our server's package.[JSON](../../Skills/Web%20Development/JSON.md) file here, and down here in the script section, we're going to add a build script to it, and for that script, we're just going to say babel and then ./src,
>
> **[3:31](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/host-a-node-server-on-google-cloud?u=76281980&t=211)** - -out-dir, ./build, what we're saying is we want it to build all of our files and put it into the build directory. And we need to add a comma to the line above that, and the next thing we're going to need to do is we're going to add a start script to our project. This is what Google Cloud will be looking for and what it'll call once we've deployed our server to make it actually run. So we're going to say start, and for that, we're just going to say node, and then run the built version of our code inside build/server.js. And now that we've done all of that, let's kill our server here, and now that we have those scripts defined, we're going to need to actually build our applications. So first we're going to need to install something called Babel [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) so that our Babel command will actually work. So say [npm](../../Skills/Web%20Development/npm.md) install @babel/cli, save-dev,
>
> **[4:34](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/host-a-node-server-on-google-cloud?u=76281980&t=274)** and once we've done that, we should be able to build our code by running npm run build., and what that'll do is create this build directory for us that contains a transpiled version of our code that can be executed in a node environment. And one last thing that we're going to have to add into our package.json here is a property called engine that will tell Google Cloud what version of Node to use while running our server. So that's going to look like this. We're just going to say node, and then for the version, we're going to say greater than or equal to 12.0 0., and the reason we need this is because Hapi doesn't work with versions of Node that are less than 12, so we just need to make sure that our server is running on a node version that's higher than 12. And make sure you add the comma there. And the command to deploy our server is going to be gcloud app deploy, and hit enter, and since we've had this terminal open and haven't refreshed it since we installed gcloud, what we're going to do is just open up a new terminal and run it from there where gcloud should be available from. So we're going to say gcloud app deploy, and hit enter, and it's going to ask us what region we want our app to be running in. We're going to want to choose whichever one is closest to us here, so I'm going to do us-central, and for that, I'm going to enter in 14, and then it's going to ask us if we want to continue,
>
> **[6:05](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/host-a-node-server-on-google-cloud?u=76281980&t=365)** and we're going to say yes, and generally, this deployment does take a pretty long time, so don't worry if it runs for a while. And once this whole deployment process completes, we should be able to connect to it from our front end, so let's head over to our front end, and inside our proxy.config file, we originally had our front end pointing at our server running locally, but what we're going to do now is change that to the URL that our backend is currently being served on on the Google Cloud. So that URL is right here where it says deployed service to, and then we're going to copy this and paste it inside this target string here, and we might have to restart our front end as well, so we're going to say npm run start, and now we should be able to run our front end locally, all while accessing our server running on Google Cloud, which is pretty cool.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) (10), [npm](../../Skills/Web%20Development/npm.md) (3), [JSON](../../Skills/Web%20Development/JSON.md) (2), [CLI](../../Skills/Network%20and%20System%20Administration/CLI.md) (2), [JavaScript](../../Skills/Software%20Development/JavaScript.md) (1)
> **CLI Commands:** node (7), make (5), gcloud (4), npm (3), find (1)
> **Env Vars:** db_socket (3), db_host (2), url (2), db_user (1), db_pass (1)
> **File Paths:** package.json (2), app.yaml (1), database.js (1), process.env (1), server.js (1)
> **Code Identifiers:** beta_settings (1), cloud_sql_instances (1), env_variables (1), socketpath (1)
> **Prerequisites:** install (2), set up (1)
> **Versions:** 0.0.0 (1), 12.0 (1)
> **Cross-References:** go back to (1), previous video (1)

#### [Build and serve an Angular app](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/build-and-serve-an-angular-app?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/build-and-serve-an-angular-app?u=76281980&t=0)** - [Instructor] So now we've come to the last piece of our full stack hosting puzzle and that's building and serving our front end from [Google Cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md). Now, this process has a few steps. The first step here is going to be to build our front end application and to do that, we need to run the command ng build and hit Enter. And what this command does is it compiles our entire [Angular](../../Skills/Web%20Development/Angular.md) application into basic [HTML](../../Skills/Web%20Development/HTML.md), CSS and [JavaScript](../../Skills/Software%20Development/JavaScript.md) files that can simply be loaded and executed by the end user's browser. And once this script finishes running, all of this compiled code is put into this dist directory here. So here's how this is going to work. What we're going to do is have our hapi server serve all the files in this dist directory, along with all the API routes that we've created throughout the course. So to do that, the first thing we're going to do is we're going to copy this dist directory and we're going to paste it into our backend's root directory. So we're going to copy and then we're going to paste it right here. And then what we're going to do is we need to install another hapi module package into our backend project. So we're going to say [npm](../../Skills/Web%20Development/npm.md) install and the package we're going to be installing is called @hapi/inert. And this package makes it easier for hapi to serve static files. So once we've done that, we're going to open up our server.js file and we're going to import the package we just installed. So we're going to say import inert from @hapi/inert.
>
> **[1:39](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/build-and-serve-an-angular-app?u=76281980&t=99)** And then we need to register this package with our server by saying await server.register inert. And the next thing we have to do here is define some routes for serving the front end files we copied and pasted. So here's what that's going to look like. We're going to start off by defining a route for serving static files from the dist folder. So inside our routes folder, we're going to create an new file called files.js. And inside of there, we're going to say export const staticFilesRoute equals and the method for this is going to be GET. And then for the path here, we're going to say slash curly braces params*, which basically will match any route and then we're going to have handler, directory and path: dist/ and then, we need to look inside of our dist folder to see what the name actually is. In our case, it's just buy-and-sell. So we're going to say dist/buy-and-sell. And the next thing we have to do is define routes on our server for each route on our front end, basically for all of those routes, like /listing, /edit-listing and so on, we'll simply want to return the index.html file found inside this dist folder.
>
> **[3:13](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/build-and-serve-an-angular-app?u=76281980&t=193)** And once we do that, Angular will take care of the [rest](../../Skills/Web%20Development/Representational%20State%20Transfer%20(REST).md). So the way that we're going to do this is inside the same file, we're going to define an array called angularRoutePaths. And this will contain all the paths that we want to open up to our front end. So we're going to say slash. We're going to have the /listings route. We're going to have the /contact/id route. We're going to have the edit-listing route. We're going to have the listings/id route. We're going to have the /my-listings route. And last but not least, we're going to have the new-listing route. And now that we have this, what we're going to do is we're going to map each of these paths to a route that will return the index.html file of our front end. So here's what that will look like. We're going to say export const filesRoutes equals angularRoutePaths.map and for each path, what we're going to do is return a route with the method GET and the given path that we're mapping. And then for the route handler, we're going to say request and h. And we're going to say return h.file
>
> **[4:48](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/build-and-serve-an-angular-app?u=76281980&t=288)** and then dist/buy-and-sell/index.html.
>
> **[4:57](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/build-and-serve-an-angular-app?u=76281980&t=297)** And then we're going to need to export both the files routes and the static files route from our routes index.js file. So we're going to say import staticFilesRoute and filesRoutes from files. And then down here at the bottom of our array, we're going to say staticFilesRoute and then use the spread operator to put all of our file's routes into this array. And now that we've made changes to our app, we're going to have to actually build it again by running npm run build. And once we've done that, we should be able to run gcloud app deploy and hit Enter. And we're going to say yes. And while that's deploying, there's one more thing we need to do. We need to tell [Firebase](../../Skills/Web%20Development/Firebase.md) Auth to trust the URL that our app will be running on. And the way that we do this is by going to the Firebase console, and we're going to go over here to Authentication and click on that. And then we're going to click on Sign-in method and then we're going to scroll down to the section that says Authorized domains. And what we're going to need to do is we're going to need to add a domain here and the domain that we're going to add is the one that we put inside our proxy config on our front end.
>
> **[6:31](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/build-and-serve-an-angular-app?u=76281980&t=391)** So we're going to copy that. And paste it into here. And click Add. And that should be all we need to do for Firebase Auth to work with our deployed application. So now all we have to do is wait for the deploy to finish. And once our application has been successfully deployed, what we can do is we can either copy and paste this link into our browser or we can just type gcloud app browse. And that'll bring up our entire application in a browser on a URL that anyone can visit. So let's just walk through to make sure everything works. We're going to sign in with [Google](../../Glossary/Service/Google.md). Editors, please blur these out. And we can go through and take a look at some of the listings. We can then go to My Listings and create a new listing.
>
> **[7:31](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/build-and-serve-an-angular-app?u=76281980&t=451)** And there it is. And we can also edit it. And save our changes and we can also delete it. And that's it. We've built and deployed a full stack Angular application.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [HTML](../../Skills/Web%20Development/HTML.md) (4), [Angular](../../Skills/Web%20Development/Angular.md) (3), [Firebase](../../Skills/Web%20Development/Firebase.md) (3), [npm](../../Skills/Web%20Development/npm.md) (2), [Google cloud](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) (1)
> **Code Identifiers:** staticfilesroute (3), angularroutepaths (2), filesroutes (2)
> **File Paths:** index.html (2), server.js (1), files.js (1), dist/buy-and-sell/index.html (1), index.js (1)
> **CLI Commands:** npm (2), gcloud (2), make (1)
> **Env Vars:** url (2), html (1), css (1), api (1)
> **UI Navigation:** click on (2), scroll down (1), go to (1)
> **Prerequisites:** install (2)
> **API Endpoints:** get
 (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/next-steps?u=76281980&t=0)** - [Shaun] So we've covered quite a few topics in this course. We started off by creating a front end application using [Angular](../../Skills/Web%20Development/Angular.md) and a backend using [Node.js](../../Skills/Web%20Development/Node.js.md), Hapi, and [MySQL](../../Skills/Software%20Development/MySQL.md). We then saw how to bring the front end and back end together using Angular's HTTP client to make network requests. And then we saw how to add user authentication to our application using [Firebase](../../Skills/Web%20Development/Firebase.md) Auth. And this allowed us to protect our end points and keep better track of our users in the listings they created. And finally, we saw how to move the entire application that we created to the cloud using the [Google Cloud Platform](../../Skills/Software%20Development/Google%20Cloud%20Platform%20(GCP).md) so that users would be able to actually access our application. Now, I want to reiterate that all of this work that we've completed is a pretty impressive undertaking, and it's something that most web developers don't know how to do. So you should be very proud of yourself if you've followed along with all the steps and understand all the concepts we've covered here, since that'll put you very far ahead of the vast majority of web developers today. So now that you've got all this [Full-Stack Development](../../Skills/Software%20Development/Full-Stack%20Development.md) knowledge, you might be wondering where to go next. Well, there are several things that I'd recommend. First and foremost, I'd recommend that you use everything you've learned here to build your own site. Not only will this allow you to apply your knowledge to solve real world problems, but whatever you end up building will be a great portfolio piece for potential employers if you're looking for a full-stack development job. Never underestimate the power of being able to walk into a job interview with a real world full-stack site that you've built that your interviewers can visit. Well, that's about it for now. Thank you so much for watching, and if you want to get in touch with me,
>
> **[1:32](https://www.linkedin.com/learning/angular-creating-and-hosting-a-full-stack-site/next-steps?u=76281980&t=92)** please don't hesitate to connect with me on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md). I'm on LinkedIn quite often and would love to hear any questions or comments from you, or if you just want to say hi, that's okay too. Well, thanks again, and I hope you enjoyed the course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Angular](../../Skills/Web%20Development/Angular.md) (2), [Full-Stack Development](../../Skills/Software%20Development/Full-Stack%20Development.md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2), [Node.js](../../Skills/Web%20Development/Node.js.md) (1), [MySQL](../../Skills/Software%20Development/MySQL.md) (1)
> **CLI Commands:** node (1), mysql (1), make (1)
> **File Paths:** node.js (1)
> **Env Vars:** http (1)
> **Definitions:** is a  (1)
> **Speakers:** - [shaun] (1)


## Instructor

- [Shaun Wassell](../../Instructors/Software%20Development/Shaun%20Wassell.md)

## Resources

- Exercise files available

## Skills Covered

- Angular

## Path Context

### In [Explore Web Development with Angular](../../Paths/Software%20Development/Learning%20Paths/Explore%20Web%20Development%20with%20Angular.md)
← [Angular- Testing and Debugging](Angular-%20Testing%20and%20Debugging.md) | **4 of 6** | [Angular- Cloud-Powered Apps with Firebase](Angular-%20Cloud-Powered%20Apps%20with%20Firebase.md) →

## Appears In

- [Explore Web Development with Angular](../../Paths/Software%20Development/Learning%20Paths/Explore%20Web%20Development%20with%20Angular.md)

## Related Courses

_Courses sharing skills:_

- [Building Angular and Django Apps](../Web%20Development/Building%20Angular%20and%20Django%20Apps.md) — Angular
- [Building Angular and ASP.NET Core Applications](../Web%20Development/Building%20Angular%20and%20ASP.NET%20Core%20Applications.md) — Angular
- [WebSocket Communications with Node and Angular](WebSocket%20Communications%20with%20Node%20and%20Angular.md) — Angular
- [Angular- Cloud-Powered Apps with Firebase](Angular-%20Cloud-Powered%20Apps%20with%20Firebase.md) — Angular
- [Angular- Testing and Debugging](Angular-%20Testing%20and%20Debugging.md) — Angular

---

[↑ Back to top](#)