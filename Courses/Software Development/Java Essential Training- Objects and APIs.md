---
type: course
cssclasses:
  - lle-course
  - lle-level-beginner
slug: java-essential-training-objects-and-apis-16021820
url: "https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820"
duration_minutes: 130
duration: 2h 10m
level: Beginner
updated: 9/17/2024
learners: 48312
skills:
  - Java
exercise_files: true
thumbnail: "https://media.licdn.com/dms/image/v2/D4D0DAQGbRg1XEBD8lw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1725395859168?e=2147483647&amp;v=beta&amp;t=0QgSKDrLBSy9cfUj4w0wO6ISl-ijyshjyNHJo9sUT18"
linkedin_topic: Software Development
learning_paths:
  - '[The Top Skills Engineering Professionals Have Right Now](../../Paths/Software%20Development/Learning%20Paths/The%20Top%20Skills%20Engineering%20Professionals%20Have%20Right%20Now.md)'
  - '[Getting Started as a Java Developer](../../Paths/Data%20Science/Learning%20Paths/Getting%20Started%20as%20a%20Java%20Developer.md)'
  - '[Java Foundations Professional Certificate by JetBrains](../../Paths/Data%20Science/Professional%20Certificates/Java%20Foundations%20Professional%20Certificate%20by%20JetBrains.md)'
prev_courses:
  - '[JavaScript Essential Training](JavaScript%20Essential%20Training.md)'
  - '[Java Essential Training Syntax And Structure](../Data%20Science/Java%20Essential%20Training%20Syntax%20And%20Structure.md)'
  - '[Java Essential Training Syntax And Structure](../Data%20Science/Java%20Essential%20Training%20Syntax%20And%20Structure.md)'
next_courses:
  - '[SOLIDWORKS 2022 Essential Training](SOLIDWORKS%202022%20Essential%20Training.md)'
  - '[Learning Spring 6 With Spring Boot](Learning%20Spring%206%20With%20Spring%20Boot.md)'
  - '[Java Object-Oriented Programming](../Data%20Science/Java%20Object-Oriented%20Programming.md)'
path_nav: '[{"path":"The Top Skills Engineering Professionals Have Right Now","position":3,"total":9,"prev":"JavaScript Essential Training","next":"SOLIDWORKS 2022 Essential Training"},{"path":"Getting Started as a Java Developer","position":2,"total":10,"prev":"Java Essential Training Syntax And Structure","next":"Learning Spring 6 With Spring Boot"},{"path":"Java Foundations Professional Certificate by JetBrains","position":2,"total":5,"prev":"Java Essential Training Syntax And Structure","next":"Java Object-Oriented Programming"}]'
path_count: 3
tags:
  - course
  - topic/software-development
  - topic/data-science
  - topic/web-development
  - skill/java
status: not-started
created: 2026-05-03
---

> <!-- lle:github-source --> [View this note on GitHub](https://github.com/thisis-romar/linkedin-learning-vault/blob/github-browse/Courses/Software%20Development/Java%20Essential%20Training-%20Objects%20and%20APIs.md)

![Java Essential Training: Objects and APIs](https://media.licdn.com/dms/image/v2/D4D0DAQGbRg1XEBD8lw/learning-public-crop_675_1200/learning-public-crop_675_1200/0/1725395859168?e=2147483647&amp;v=beta&amp;t=0QgSKDrLBSy9cfUj4w0wO6ISl-ijyshjyNHJo9sUT18)

# Java Essential Training: Objects and APIs

> Knowing Java—one of the most popular programming languages in the world—can be beneficial if you’re interested in building applications for mobile devices, desktop computers, or the web. You also need to stay up to date as the language evolves and changes over time. In this course, Certified Java developer Angie Jones walks you through the fundamentals of objects and APIs in the most recent long-t

> [LinkedIn Learning](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820) | 2h 10m | Beginner | 48K learners
> [Jump to Path Context ↓](#path-context)

## Table of Contents

### Chapter Overview

- [**Introduction**](#introduction) (3 videos)
  - [Java objects and APIs](#java-objects-and-apis)
  - [What you should know](#what-you-should-know)
  - [CoderPad tour](#coderpad-tour)
- [**1. Inheritance**](#1-inheritance) (7 videos)
  - [Inheritance between classes](#inheritance-between-classes)
  - [Constructors in inheritance](#constructors-in-inheritance)
  - [Overriding and overloading inherited methods](#overriding-and-overloading-inherited-methods)
  - [Chain of inheritance](#chain-of-inheritance)
  - [Limiting access in inheritance](#limiting-access-in-inheritance)
  - [Sealed classes](#sealed-classes)
  - [Code Solution: Cake! Cake! Cake!](#code-solution-cake-cake-cake)
- [**2. Polymorphism**](#2-polymorphism) (4 videos)
  - [Polymorphism with objects](#polymorphism-with-objects)
  - [Object typecasting](#object-typecasting)
  - [instanceof operator](#instanceof-operator)
  - [Code Solution: Employee report](#code-solution-employee-report)
- [**3. Abstraction**](#3-abstraction) (4 videos)
  - [Abstract classes and methods](#abstract-classes-and-methods)
  - [Inheriting from abstract classes](#inheriting-from-abstract-classes)
  - [Creating objects with abstract types](#creating-objects-with-abstract-types)
  - [Code Solution: Orchestra concert](#code-solution-orchestra-concert)
- [**4. Interfaces**](#4-interfaces) (5 videos)
  - [Creating an interface](#creating-an-interface)
  - [Implementing interfaces](#implementing-interfaces)
  - [Instantiating objects with interface types](#instantiating-objects-with-interface-types)
  - [Default and static methods](#default-and-static-methods)
  - [Code Solution: Shirt product](#code-solution-shirt-product)
- [**5. Data Structures**](#5-data-structures) (9 videos)
  - [Collections framework](#collections-framework)
  - [Sets](#sets)
  - [Lists](#lists)
  - [Queues](#queues)
  - [Maps](#maps)
  - [Iterators](#iterators)
  - [Enhanced for loops](#enhanced-for-loops)
  - [forEach() and lambdas](#foreach-and-lambdas)
  - [Code Solution: Gradebook](#code-solution-gradebook)
- [**6. Functional Programming**](#6-functional-programming) (4 videos)
  - [Functional interfaces](#functional-interfaces)
  - [Streams](#streams)
  - [Common stream operations](#common-stream-operations)
  - [Code Solution: Color filter](#code-solution-color-filter)
- [**7. Exception Handling**](#7-exception-handling) (9 videos)
  - [Try and catch blocks](#try-and-catch-blocks)
  - [Stack trace and exception message](#stack-trace-and-exception-message)
  - [Checked vs. unchecked exceptions](#checked-vs-unchecked-exceptions)
  - [Handling multiple exceptions](#handling-multiple-exceptions)
  - [Finally block](#finally-block)
  - [Try with resources](#try-with-resources)
  - [Throwing exceptions](#throwing-exceptions)
  - [Rethrowing exceptions](#rethrowing-exceptions)
  - [Code Solution: Unauthorized user](#code-solution-unauthorized-user)
- [**Conclusion**](#conclusion) (1 videos)
  - [Next steps](#next-steps)

### Introduction

[↑ Back to Table of Contents](#table-of-contents)

#### [Java objects and APIs](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/java-objects-and-apis?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/java-objects-and-apis?u=76281980&t=0)** - [Angie] [Java](../../Skills/Software%20Development/Java.md) is one of the world's most popular programming languages. It's a highly sought after skill for enterprise developers because Java runs on billions of devices all over the world. Java 17 is the newest long-term support version. It's rich with features and this course provides a comprehensive view of the language. We'll solve lots of challenges together so that you gain a practical understanding of the concepts. There are even individual challenges you'll work on by yourself to really cement the knowledge. I'm Angie Jones, a certified Java developer and Java champion. Join me in this [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning course as we dive into core Java programming.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (6), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Versions:** java 17 (1)
> **Speakers:** - [angie] (1)

#### [What you should know](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/what-you-should-know?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/what-you-should-know?u=76281980&t=0)** - [Instructor] To get the most out of this course, there are a few things you should already know. You should know how to create and use classes, methods, and variables within a [Java](../../Skills/Software%20Development/Java.md) program. You should be familiar with decision structures, such as if-else statements. You should know how to use repetition structures, such as loops. And you should be able to create and use Java objects. If you need to learn or revisit these concepts before starting this course, you're in luck. This is the second course in my Java 17 Essential series. The first course covers Java basics, such as syntax, variables, decision and repetition structures, objects, and other foundational knowledge you'll need to be successful in this course. If you already have experience writing programs in Java or another [object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) language, you should be ready to jump into this course.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (5), [Object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) (1)
> **Analogies:** such as (3)
> **Versions:** java 17 (1)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [CoderPad tour](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/coderpad-tour?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/coderpad-tour?u=76281980&t=0)** - [Lecturer] This course includes automated code challenges that appear when you click on the challenge links in the course's table of contents. Each challenge includes instructions and a couple of code editors you can use to create and test your own solution to the challenge. These challenges are hosted by CoderPad and they appear in the same area of the course page where you watch the course's videos. We recommend using a desktop browser for the best experience with code challenges, but you can use the [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning mobile app if you prefer. The code challenge has four areas, instructions in the top left, a code editor for your answer in the top right, another code editor where you see how your code is used in the bottom right, and a console for output in the bottom left. You can use these drag handles to allocate space as you like. To get more horizontal space for the code editors, you can collapse the course's table of contents on the left. Each challenge has instructions that include a description of the challenge and the challenge's parameters, and desired results. Parameters are values that will be passed into your code and they have to be of a particular data type. The return value also has to be of a particular type, and you'll see that noted in the Instructions. The Constraints section has useful information about the parameters that will be passed in, and the examples show different parameter values and what result will be returned for each
>
> **[1:33](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/coderpad-tour?u=76281980&t=93)** of those test cases. Create your answer in the top right code editor. There are comments in the starting code showing where to put your solution. When you click Test my code, you'll see a message indicating whether your code returned the correct results. If your code isn't successful, you can ask for help. The showExpectedResults and showHints variables determine whether you see the expected result in any hints. We can change these values to true to control the output.
>
> **[2:09](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/coderpad-tour?u=76281980&t=129)** The code editor in the lower right shows how your solution is used. You can change that code to experiment with different test cases. Regardless of whether your answer is successful. You'll see messages in the console output in the lower left. If any messages are too long to fit in that area, you can scroll to the right to see all of the text. When you finish, each co-challenge returns to the course's table of contents, and click the next video to see my solution.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (1)
> **Code Identifiers:** showexpectedresults (1), showhints (1)
> **Cross-References:** next video (1)
> **UI Navigation:** click on (1)
> **Speakers:** - [lecturer] (1)


### 1. Inheritance

[↑ Back to Table of Contents](#table-of-contents)

#### [Inheritance between classes](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/inheritance-between-classes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/inheritance-between-classes?u=76281980&t=0)** - [Instructor] Inheritance is where one class becomes an extension of another class, therefore inheriting the members of the extended class. There are two parties involved in an inheritance: a parent and a child. The parent is known as the superclass, or sometimes referred to as the parent class or base class. The child is known as the subclass, or sometimes referred to as the child class or the derived class. When an inheritance relationship is created between two classes, the child class inherits the members of the parent class. This allows classes to reuse data that already exists within other classes. Subclasses should be a more specialized form of the superclasses that they inherit from. Let's demonstrate inheritance with code. Given this Person class, we have fields for name, age, and gender, as well as getter and setter methods for each. Here, we have an Employee class that we'd like to inherit from the Person class. To inherit from another class you use the reserved [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) extends within the subclass's header. Extends [Forms](../../Skills/Web%20Development/Forms.md) an inheritance relationship. After the extends reserved word, you specify the class that you want to inherit from. We want to inherit from Person.
>
> **[1:36](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/inheritance-between-classes?u=76281980&t=96)** That's all it takes to create an inheritance relationship. With this, now the Employee class has access to all of the methods that we created in the Person class. In addition to what's inherited, the Employee class can have additional things that are specific to an employee. So as the programmer, I don't have to worry about defining the name, the age or gender because Employee has already inherited that. Instead, I can focus on fields specific to an employee such as the employeeId and the employee's title. Let's create those now. So we'll say private string employeeId, and then we'll also create one for the title. And we can use to generate setter and getter option to create methods for these fields. So we'll say generate and getter and setter.
>
> **[2:40](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/inheritance-between-classes?u=76281980&t=160)** So now we have the Person class, which is serving as the superclass, and we have the Employee class, which is serving as the subclass. There's nothing I need to do in the Person class to make this inheritance relationship. That is only done in the subclass with the reserved word extends. To see inheritance in action, within this InheritanceChecker class, we have a person object defined and an employee defined. If we use the dot operator on person we see the getter and setters methods for age, gender, and name. And if we use the dot operator on employee, not only do we have the getter and setter methods that exist in the Person class, but we also have the methods that were defined in the Employee class. Notice we don't see the fields themselves. This is because the fields were marked as private in the Person class. Anything that's marked as private is not inherited. Only protected and public fields and methods can be inherited. Inheritance is one of the pillars of [object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md). It's particularly useful for reusing code between related classes. Great job learning about this important concept in [Java](../../Skills/Software%20Development/Java.md).

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (3), [Forms](../../Skills/Web%20Development/Forms.md) (1), [Object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** employeeid (2)
> **Definitions:** known as (2)
> **CLI Commands:** make (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)

#### [Constructors in inheritance](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/constructors-in-inheritance?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/constructors-in-inheritance?u=76281980&t=0)** - [Instructor] There are some interesting details about how constructors work when inheritance is involved. Here we have a default constructor in the person class that prints out in-person default constructor and we have a default constructor in the employee class which prints out in-employee default constructor. When we create a new instance of a subclass, it makes a call to the constructor of the superclass before it executes the subclass constructor. Let's run this class to demonstrate. Notice we made a call to the default constructor of employee. However, the person default constructor was called before the employee default constructor. If we go inside of the employee constructor, there's nothing here that explicitly states that we're going to go inside of the person constructor. However, by inheriting from the person class, it's default constructor was called first. And if we really think about it, it makes sense. As we are inheriting from this class, we want to make sure that everything is properly constructed in the superclass before we actually try to use anything we inherited from there. This is pretty straightforward. If we're okay with the superclass's default constructor being the one that's executed. But what if we needed a different constructor from the superclass to be called first. We'd need to explicitly specify our intent. So let's create a new constructor in our superclass person. So we'll save public person.
>
> **[1:38](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/constructors-in-inheritance?u=76281980&t=98)** And this one will take a name as a parameter. And let's just add a statement that lets us know that we are in the second constructor. We'll say name is set. Inside of the employee constructor, we can explicitly make a call to any of the superclass's constructors by using the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) super. If we call super without anything in parenthesis, this will call the default constructor of the superclass which is person. However, if we wanted to call into any of the other constructors, we would just pass the required arguments. So let's pass a name to the super and [Java](../../Skills/Software%20Development/Java.md) will know to call the superclass's constructor passing in a string. Now let's run our inheritance checker. Notice this time the default constructor was not called for the superclass, instead the second constructor was called and then the subclass's constructor. Note that if you're going to make an explicit call to a constructor from a superclass, then that call must be the very first line of the subclasses constructor. If we were to move this call to super under our print statement, notice that we do get a compilation error stating that super must be the first statement
>
> **[3:14](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/constructors-in-inheritance?u=76281980&t=194)** in the constructor's body. Another rule when dealing with constructors in inheritance, is if the superclass does not have a default constructor, then the subclass must explicitly call one of its other constructors. So let's remove the super call from this employee constructor. And then let's remove the default constructor from the person class. If we go back to employee, now we have an error saying that there's no default constructor available in the person class. So again by default, the subclass's default constructor is making an implicit call to the person default constructor. And because person does not have a default constructor, we get an error. To fix this, we would have to make a call to one of the other constructors that does exist so we can move our super call back to the top line and we now satisfy the requirements. Just to reiterate the rules about constructors in inheritance. A superclass's constructor is run before the subclass's constructor. Explicit calls can be made to superclass's constructor from one of the subclass constructors by using super. Explicit calls to the superclass's constructor must be the first statement in the subclass constructor. And if the superclass's now have a default constructor,
>
> **[4:49](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/constructors-in-inheritance?u=76281980&t=289)** the subclass must explicitly call one of its other constructors.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **CLI Commands:** make (4)
> **Cross-References:** go back to (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Overriding and overloading inherited methods](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/overriding-and-overloading-inherited-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/overriding-and-overloading-inherited-methods?u=76281980&t=0)** - [Instructor] A subclass inherits the members of its super class. However, a subclass may want to change the behavior of a method that it inherited. This is allowed by overriding the inherited method. For example, a square is a rectangle, meaning we could have a square class that inherits from a rectangle class. One of the methods that was inherited is used to calculate the perimeter. Well, the formula for calculating the perimeter of a square is different than the formula for calculating the perimeter of a rectangle. So while it's great that the square class inherits this method, that class needs to change the behavior of the method. This is called overriding a method. Let's code this example. In the rectangle class, we have fields for the length, width, and sides as well as getter and setter methods for all. In addition, we have this calculate perimeter method that calculates the perimeter of a rectangle. The square class inherits from the rectangle class and gets all of these goodies for free. However, the implementation of the calculate perimeter method is not correct for a square. So we'd like to change it and we can do so by overriding the method. So override a method, we create the method with the exact same signature as the one that exists in the super class. So I'm just going to copy this signature just to make sure that it's exactly correct. Then we can change the implementation within our new method. The formula to calculate the perimeter of a square is sides
>
> **[1:36](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/overriding-and-overloading-inherited-methods?u=76281980&t=96)** times length. Notice I didn't declare sides or length anywhere in the square class, but I inherited them from the rectangle class because these fields are declared as protected It's encouraged to use the override annotation which is the @ symbol followed by the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) override. This is not required, but is strongly encouraged. This lets [Java](../../Skills/Software%20Development/Java.md) know that your intention is to override the method that you inherited from your super class. Without this override annotation, you could mistakenly think that you are overriding a method. However, if the signature does not exactly match the inherited method, then you wouldn't actually be overriding it. With the override annotation, Java will catch this and inform you of the error. Now let's discuss overloading methods. Overloading methods is when you have multiple methods with the exact same name within the same scope, but with different parameter lists. When dealing with a subclass, we can overload a method that we have inherited from a super class even though that method lives in another class let's update the rectangle and square classes to demonstrate how this is done. I'm going to create a new method in the rectangle class that simply prints out a statement. So we'll say public void print and inside we will just print, I am a rectangle.
>
> **[3:16](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/overriding-and-overloading-inherited-methods?u=76281980&t=196)** And then in the square class, I'm going to overload this by creating a method with the same name but it's going to take a string. So we'll say public void print and this time string as the parameter, and then we'll print this out as well. I am a what. Now let's look at this in action. Notice when I use rectangle and try to call the print method, there's only one method by this name available in the rectangle class. However, when I attempt to call print on the square class, there's two methods, the one that we inherited and also the overloaded one. As you can see, you can override and overload inherited methods within a subclass. They sound similar. So it's easy to mix them up. Remember to override a method, you keep the same signature, but change the body of the method and to overload a method, you keep the same name but change the signature of the method.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Definitions:** is a  (1), is called (1)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Best Practices:** remember to (1)
> **Speakers:** - [instructor] (1)

#### [Chain of inheritance](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/chain-of-inheritance?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/chain-of-inheritance?u=76281980&t=0)** - [Instructor] A [Java](../../Skills/Software%20Development/Java.md) class can only directly inherit from one superclass. However, a superclass can also inherit from another class, thus forming a chain of inheritance where the subclass inherits from their ancestor classes as well. Here we have a Vehicle class with fields for color and mileage and getter and setter methods for these fields. And we also have a Car class which inherits from Vehicle and has a field for doors as well as a getter and setter method. Then a Coupe class which inherits from Car and sets the number of doors to 2. To test our chain of inheritance, let's use an instance of Coupe, and we'll call this myCar. And let's set the color of our car to red. Notice that we can set the color on myCar even though the setColor method is not available in the Coupe class, nor is it available in the Car class. This is because the Coupe class inherited it through its ancestor, Vehicle. Now let's print the color of the car, which was inherited from the Vehicle class, and the number of doors the car has, which was inherited from the Car class and its value set in the Coupe class. So we'll do a print statement,
>
> **[1:34](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/chain-of-inheritance?u=76281980&t=94)** and we'll say String.format, "My car is blank and has blank doors." And then we'll fill in those blanks with myCar.getColor and myCar.getDoors. Awesome, let's run this. Great, we see "My car is red and has 2 doors."
>
> **[2:10](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/chain-of-inheritance?u=76281980&t=130)** Now notice when we type myCar., we see other methods available to the Coupe class that were not explicitly defined in Coupe, Car, or Vehicle. These methods are the methods that were inherited from the object class, which every single class in Java inherits from. While Java does not support multiple inheritance, this is one way classes can inherit from a hierarchy of other classes.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3)
> **Code Identifiers:** mycar (5), setcolor (1), getcolor (1), getdoors (1)
> **Speakers:** - [instructor] (1)

#### [Limiting access in inheritance](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/limiting-access-in-inheritance?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/limiting-access-in-inheritance?u=76281980&t=0)** - [Instructor] When a subclass inherits from a superclass, not everything is inherited. Constructors are not technically members of a class, and therefore they are not inherited. All the public and protected methods and fields in a superclass, those are indeed inherited, but the private methods and fields are not. Also, if there are any final methods, meaning methods that have the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) final in the header, these are inherited but cannot be overridden. Final is a non access modifier that indicates that this is the final implementation for this method, and it cannot be changed by any subclasses. In the Square class, we were able to use the fields side and length because we inherit those from the Rectangle class. The reason we were able to inherit them is because they have an access modifier of protected. Any class members that are labeled as protected or public are inheritable. The same is true for members that do not have an access modifier label. However, if we were to label sides as private, notice now in the Square class we have an error that says that sides has private access. Therefore we didn't inherit it, and we cannot use it outside of the Rectangle class. There is another access limitation to be aware of when overriding inherited methods. Notice the calculatePerimeter method has public access.
>
> **[1:35](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/limiting-access-in-inheritance?u=76281980&t=95)** In the Square class, we have overridden this method, and it also has a public access modifier. If we attempt to declare it as anything stricter, such as protected, we would get an error. The error states that there's a clash because we are attempting to assign stricter access privileges. So this is not allowed. It doesn't mean we can't change it at all. It just cannot be stricter. For example, in the superclass, if we were to change calculatePerimeter to protected,
>
> **[2:18](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/limiting-access-in-inheritance?u=76281980&t=138)** we could then use public in the overridden methods' access modifier because this is less strict than the original access modifier. Access modifiers enable superclasses to control which members can be shared to their subclasses.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Code Identifiers:** calculateperimeter (2)
> **Analogies:** such as (1), for example (1)
> **Definitions:** is a  (1)
> **Warnings:** be aware (1)
> **Speakers:** - [instructor] (1)

#### [Sealed classes](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/sealed-classes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/sealed-classes?u=76281980&t=0)** - [Instructor] Not only do [Java](../../Skills/Software%20Development/Java.md) classes have control over which of its members can be inherited, but they can also restrict inheritance altogether to certain sub-classes. Classes that restrict inheritance to specific classes are known as sealed classes. Sealed classes are useful for modeling a specific domain and control the relationship between classes in the domain. For example, we can declare that the only classes that can directly inherit from shape are rectangle and circle, but we would still like to allow square to inherit from rectangle. Let's model this with code. Here, we have four classes; shape, circle, rectangle, and square. To make shape a sealed class, we write the reserved [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) sealed before the word class. And then, before the open and curly brace, we must add the reserved word permits, and within a comma-delimited list, specify which classes are allowed to inherit from this sealed class. If a sealed class lists the sub-class in the permits clause, the sub-class is required to already exist and must have already extended the sealed class. Children of sealed classes are also required to declare themselves as sealed, non-sealed, or final. Again, a sealed class controls who can inherit from it. In our domain, rectangle would make a good sealed class
>
> **[1:34](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/sealed-classes?u=76281980&t=94)** as we only want to allow square to extend it. Non-sealed means that the class is open to be extended by any other class. We'll declare circle as non-sealed. Any class that extends circle is not obligated to define themselves as sealed, non-sealed, or final. It will simply be a normal inheritance. Final means that the class cannot have any sub-classes. In other words, it cannot be inherited from. Square would be the last of the descendants from this hierarchy. To recap, a sealed class grants inheritance permission to a specific list of classes. Every permitted sub-class must already exist and must explicitly extend the sealed class. Every permitted sub-class is required to declare itself as sealed, non-sealed, or final.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (3), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Definitions:** means that (2), known as (1), in other words (1)
> **CLI Commands:** make (2)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Code Solution: Cake! Cake! Cake!](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-cake-cake-cake?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-cake-cake-cake?u=76281980&t=0)** - [Narrator] In this challenge you were provided with a cake class and were tasked with implementing the birthday cake and wedding cake subclasses. Let's start with the birthday cake class. To inherit from the cake class, we use the extends keywords followed by the class we want to inherit from. So that's cake. Birthday cake has a field of its own called candles, and the default flavor of a birthday cake should be chocolate. So we have a constructor that makes a call to cakes constructor, which sets the flavor. Then finally, for this class, we have methods to get and set the value of candles. Now let's look at the wedding cake class. It also extends the cake class to inherit from it. It has its own field called tears. The default flavor of a wedding cake should be almond. So we have a constructor that calls cakes constructor, and sets the flavor, and we have methods to get and set the value of tiers.

> [!info]- Semantic Content
>
> **Speakers:** - [narrator] (1)
> **Non-Speech:** (upbeat music) (1)


### 2. Polymorphism

[↑ Back to Table of Contents](#table-of-contents)

#### [Polymorphism with objects](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/polymorphism-with-objects?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/polymorphism-with-objects?u=76281980&t=0)** - [Instructor] A core pillar of [Object-Oriented Programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) is Polymorphism. Polymorphism is the ability to take multiple [Forms](../../Skills/Web%20Development/Forms.md), specifically in Object-Oriented Programming, Polymorphism is where a subclass can define their own unique behaviors and yet share some of the same behaviors of their superclass. An example of Polymorphism is where an object has a superclass type but is an instance of a subclass. Here, we have an object called dog and it's defined as type animal, but it is an instance of dog. We're able to do this because dog inherits from animal and therefore dog is an animal. Let's demonstrate an example of Polymorphism between an animal, dog, and cats. In the animal class there's a simple method called makeSound which prints a generic message, dog inherits from animal and overrides its makeSound method to print a sound that's specific to a dog. In addition to the inherited makeSound method, there's also a [Fetch](../../Skills/Web%20Development/Fetch.md) method. Cat also inherits from animal and overrides the makeSound method, it also contains a scratch method. Given these classes, let's illustrate Polymorphism, first let's create a normal dog object and name him Rocky. Rocky can fetch because he is a dog
>
> **[1:36](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/polymorphism-with-objects?u=76281980&t=96)** and when he makes a sound, it should print woof, since we overrode the makeSound method in the dog class. So let's say rock.fetch and then rocky.makeSound, let's go ahead and run this, yep, fetch is fun, and the makeSound method is the one from the dog class, not the animal class. This is typical inheritance, now let's get to the fun part, Polymorphism. Let's create a new animal named Sasha and instantiate Sasha as a dog. So Sasha is of type animal, but an instance of dog, when Sasha makes a sound, which version of makeSound will be called? The one from animal or the one from dog? Well, let's run it and see. It prints woof, which means it ran the one from the dog class. So even though Sasha is an animal, she is an instance of dog, so it ran the overwritten method from the dog class. Now let's make things really interesting, we're going to actually change Sasha from a dog to a cat. So we'll say sasha = new Cat. Now that's some Polymorphism, how is this possible? Well, because Sasha's type is defined as animal
>
> **[3:14](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/polymorphism-with-objects?u=76281980&t=194)** and the dog class and the cat class both inherit from animal, Sasha can morph into any one of these. If we get Sasha to make a sound now we should see two different sounds printed to the console. So let's say sasha.makeSound, and we'll print again and notice when Sasha was a dog, it made the woof sound, when we changed Sasha to a cat, it made the meow sound. Polymorphism is a really powerful tool within [Java](../../Skills/Software%20Development/Java.md), here are the key points to remember. On object can have a superclass type in a subclass instance and if a method is overwritten by the subclass, the polymorphic object will execute the overwritten method at runtime.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (4), [Forms](../../Skills/Web%20Development/Forms.md) (1), [Object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** makesound (9)
> **Definitions:** is an  (5), is a  (2), defined as (1)
> **CLI Commands:** cat (5), make (2)
> **Speakers:** - [instructor] (1)

#### [Object typecasting](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/object-typecasting?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/object-typecasting?u=76281980&t=0)** - [Instructor] When working with polymorphism, sometimes objects need to be type cast. Type casting is the implicit or explicit act of converting an object's type into a different type. Casting can be performed upward or downward in an inheritance tree. Given we have Dog and Cat classes that inherit from Animal, when we create an object with a type of the superclass but instantiated with the type of a subclass, this is an implicit up cast. In an implicit up cast, the object now only has access to the methods of the class it was casted up to. For example, even though the object dog is an instance of the Dog class, its type is Animal. So this object cannot reference members that are specific to the Dog class, such as [Fetch](../../Skills/Web%20Development/Fetch.md). In order for this object to access methods from the subclass, we'd need to explicitly down cast it to type Dog. Here I'd like sasha to be able to scratch, but when using the dot operator, the scratch method is not available even though the scratch method exists in the Cat class. That's because sasha is of type Animal. So she only has access to the Animal methods. Let's down cast sasha to type Cat. To explicitly cast an object from one type to another,
>
> **[1:36](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/object-typecasting?u=76281980&t=96)** we need to write the name of the subclass that we want to change to inside of a set of parentheses before the object's name. So let's put Cat in a set of parentheses and then the object name right beside it. Then we encompass the class and the object within another set of parentheses. So we'll put a parenthesis there and here. This temporarily casts sasha to a Cat. And now when we use the dot operator, we have access to the scratch method that exists within the Cat class. This is not changing the overall object. Sasha is still of type Animal, but in this specific call it will make sasha of type Cat just so that we can execute the scratch method. If we wanted to store a down casted object, we could do so by assigning the cast to an object of that same type. Here on the right side of the second statement, we casted sasha to a Dog. Then we assign that to a new dog object called sashaTheDog. Be very careful to test your code when casting objects. The compiler will let you explicitly cast an object to any type without error. However, when you attempt to execute the code, it will crash with what's called a class cast exception with a message stating that a Cat cannot be cast to a Dog.
>
> **[3:12](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/object-typecasting?u=76281980&t=192)** This is because Dog is not a subclass of Cat. Type casting should only be used when absolutely necessary. And again, be sure to carefully test your code when you do use it.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Fetch](../../Skills/Web%20Development/Fetch.md) (1)
> **CLI Commands:** cat (9), make (1)
> **Definitions:** is an  (2)
> **Analogies:** for example (1), such as (1)
> **Code Identifiers:** sashathedog (1)
> **Speakers:** - [instructor] (1)

#### [instanceof operator](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/instanceof-operator?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/instanceof-operator?u=76281980&t=0)** - [Instructor] [Java](../../Skills/Software%20Development/Java.md) provides a handy operator that allows you to check whether or not an object is an instance of a specific class. This operator is fittingly known as instanceof. Here, we have the animal super class and two subclasses, dog and cat. We create an object named Sasha who has a type of animal and an instance of dog. And let's check to see if Sasha is an instance of animal. To do so, we write the object, followed by the instanceof operator, followed by the class that we want to check against. This asks a question. Is Sasha an instance of the animal class? The result is always a Boolean value of true or false. This will return true because Sasha is in fact an instanceof animal since dog is a subclass of an animal. When we check if Sasha is an instanceof dog, this also returns true because Sasha was instantiated as a dog. However, when we check if Sasha is an instanceof cat, we get false because the cat class is not in Sasha's hierarchy. Let's create a method that will feed any animal, but the type of food varies based on the kind of animal it is. We have a method named feed that takes an animal object. While technically Rocky is a dog, we're still able to pass Rocky into the feed method because of polymorphism. Because the feed method accepts the superclass of animal,
>
> **[1:34](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/instanceof-operator?u=76281980&t=94)** that means we can pass in an animal object or any subclass of animal. The same is true for Sasha. Within the feed method, I want to feed the animals different things based on the kind of animal it is. If it's a dog, I want to give it dog food. And if it's a cat, I'd like to give it cat food. Even though our parameter is of type animal, we can use the instanceof operator to figure out exactly what the subclass is. Let's see, if animal is an instanceof dog,
>
> **[2:12](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/instanceof-operator?u=76281980&t=132)** we'll print out, here's your dog food. Otherwise, if animal is an instanceof cat,
>
> **[2:26](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/instanceof-operator?u=76281980&t=146)** we'll print out, here's your cat food. Let's run this. We see that Rocky is fed the dog food and Sasha is fed the cat food. The instanceof operator is very useful and comes in quite handy if you want to safely cast an object. In this condition, we not only check if Sasha is a dog, but if so, we can cast Sasha as a dog and store it in this newly introduced sashathedog variable. This is called pattern matching. And essentially allows us to perform two actions in one. It's equivalent to doing the instanceof check, casting the object, and storing the casted object into a new object. Great job learning the various aspects of polymorphism.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Definitions:** is an  (6), is a  (3), known as (1), is called (1)
> **CLI Commands:** cat (8)
> **Speakers:** - [instructor] (1)

#### [Code Solution: Employee report](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-employee-report?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-employee-report?u=76281980&t=0)** - [Instructor] Given an array of employees, we need to determine, how many are managers, engineers, and interns? We'll look through the array of employees and use the instanceof operator to determine which subclass was used to create the object. If the employee is an instanceof Manager, we increment the managerCount variable. However, if the employee is an instanceof the engineer class, we increment the engineerCount variable. And if the employee is an instanceof Intern, we increment the internCount variable. Then, finally, we return an employee report containing these counts.

> [!info]- Semantic Content
>
> **Code Identifiers:** managercount (1), engineercount (1), interncount (1)
> **Definitions:** is an  (2)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 3. Abstraction

[↑ Back to Table of Contents](#table-of-contents)

#### [Abstract classes and methods](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/abstract-classes-and-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/abstract-classes-and-methods?u=76281980&t=0)** - [Narrator] Abstraction is a core principle of [object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md). Abstraction is defined as something that exists in theory but does not have a concrete existence. In [Java](../../Skills/Software%20Development/Java.md), we have the reserved [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) abstract which is a non access modifier that can be used on classes and methods. It is used when you want to define a template for a class or a method, but do not intend for it to be used as is. An abstract class is not designed to be instantiated. It's designed to be extended, thus pushing the burden of implementation onto the subclass. For example, a shape is an abstract concept. It can perhaps specify some general behaviors that a type of shape would have, but it's too abstract of a concept to actually define what that behavior should be. So this class cannot be instantiated, but its purpose is to serve as a super class for more specific subclasses to extend from. An abstract method has no body. Only the signature of the method is defined. And it is not designed to be run as is. It's designed to be overwritten by a subclass. In this case, we know that one should be able to calculate the area of a shape but how that is done depends on the type of shape that it is. There's no one such formula to add to the body
>
> **[1:35](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/abstract-classes-and-methods?u=76281980&t=95)** of the calculate area method in the shape class because it literally does not exist. This is meant to be a template to set a standard that any subclass of shape should provide behavior for this method. Here we have the shape class and we'll define an abstract method, calculate area. To do so we need to use the reserved word, abstract, which is a non access modifier. And then we provide the return type, followed by the method name. Then we add the parameter list. In our case we don't need any parameters, so we'll leave this blank. And we end the method definition with a semicolon. That's it. You don't need curly braces or bodies for abstract methods. But notice we do have an error. It's saying that the abstract method is in a non-abstract class. And if we hover over the class error it says that the class must either be declared abstract or implement its abstract methods. These errors enforce the rule that any class that contains an abstract method must be abstract itself. We can make a class abstract by simply writing the word abstract before class. An abstract class doesn't only have to contain abstract methods. It can contain implemented methods as well. So let's create a print method that outputs, I am a shape. So we'll say, public void print, and we'll print out,
>
> **[3:17](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/abstract-classes-and-methods?u=76281980&t=197)** I am a shape. This print method can be used by all subclasses of shape. Abstract classes and methods are excellent for setting standards and expectations for the implementation of related concepts.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (3), [Object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) (1), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Definitions:** is a  (3), defined as (1), is an  (1)
> **Exercise Files:** template (2)
> **CLI Commands:** make (1)
> **Analogies:** for example (1)
> **Speakers:** - [narrator] (1)

#### [Inheriting from abstract classes](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/inheriting-from-abstract-classes?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/inheriting-from-abstract-classes?u=76281980&t=0)** - [Instructor] An abstract class is designed to be extended by a subclass. In this rectangle class, let's extend the abstract class Shape. Right away, we get a compilation error, stating that this class must either be declared abstract, or implement the abstract method, calculate area in Shape. The rectangle class inherited an abstract method when it extended Shape, and now, it's responsible for the implementation of that method. You can choose to implement the method or declare the rectangle class as abstract as well, since it now has become a class that technically contains abstract methods. If we were to make rectangle abstract, we'd further push the burden of implementing the abstract methods down to any class that extends rectangle. In this basic case, it makes sense to implement the one inherited abstract method. However, what if Shape had 20 abstract methods, and rectangle only wanted to implement five of them? We could do that, and then declare rectangle abstract and the remaining 15 methods would need to be implemented by a subclass of rectangle. To implement the abstract method that rectangle inherited, we need to add that method to the class. An easy way to do that in IntelliJ is to right click, click generate, and then choose to implement methods. This will allow you to choose
>
> **[1:33](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/inheriting-from-abstract-classes?u=76281980&t=93)** from the inherited abstract methods that need to be implemented. Let's choose calculate area. Notice that a default implementation was provided for us. However, we'll want to change that to something more meaningful. The area of a rectangle is length times width, so we'll change it to that. With this, we've met the responsibility of extending an abstract class.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Tools:** intellij (1)
> **Speakers:** - [instructor] (1)

#### [Creating objects with abstract types](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/creating-objects-with-abstract-types?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/creating-objects-with-abstract-types?u=76281980&t=0)** - [Instructor] Abstract classes are meant to be a template and not designed to be used as is. Given that shape is an abstract class, let's create a shape object that we'll call rectangle. An attempt to make it a new instance of shape. Notice we get a compilation error and it states that you cannot instantiate an abstract class. So you can use an abstract class as a type but you would have to instantiate it with one of its non-abstract subclasses. So we can use rectangle here instead and we'll need to pass it a length and a width. Now let's call rectangle dot calculate area, and we'll print that out. Even though rectangle is of type shape, and there is a calculate area method in the shape class, because it has been overwritten and implemented in the rectangle class, that is the one that will be used. Let's see this in action. Yep, we get 35 for the area because the rectangle class implemented the calculate area method to multiply the link by the width. Abstraction, as its name implies, is a tough concept to grasp. The key points to remember are abstract classes and methods are templates that are meant to be implemented by their subclasses. The classes and methods are declared abstract by using the abstract reserved [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md).
>
> **[1:33](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/creating-objects-with-abstract-types?u=76281980&t=93)** If a subclass inherits from an abstract class, it must implement its abstract methods or the class itself must be declared as abstract. Abstract classes cannot be instantiated. They are only to be used as a super class. Awesome job hanging in there. And if you need to review the videos on abstraction again, know that that's common and totally okay.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Definitions:** is a  (2), is an  (1)
> **CLI Commands:** make (1)
> **Exercise Files:** template (1)
> **Speakers:** - [instructor] (1)

#### [Code Solution: Orchestra concert](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-orchestra-concert?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-orchestra-concert?u=76281980&t=0)** - [Instructor] In this challenge, your task was to inherit from the abstract class instrument and implement its abstract methods. The flute, horn, and violin classes, all used the extends keyword to inherit from instruments, and each of them have implemented the inherited abstract method, play sound. So all requirements are met. Now let's add these instruments to the orchestra.
>
> **[0:37](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-orchestra-concert?u=76281980&t=37)** The add horn method returns an instantiation of horn, but notice that this method's return type is instrument. Same for flute, as well as violin. When executed, we see the implemented methods were called.

> [!info]- Semantic Content
>
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 4. Interfaces

[↑ Back to Table of Contents](#table-of-contents)

#### [Creating an interface](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/creating-an-interface?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/creating-an-interface?u=76281980&t=0)** - [Instructor] An interface is similar to an abstract class in that it is a template of an abstract concept. However, interfaces are meant to be purely abstract, and therefore, do not have state. This means that interfaces do not have constructors. The values of their fields cannot be changed, and other classes implements interfaces as opposed to extending them. Let's create an interface to represent a product. To create an interface, you create a [Java](../../Skills/Software%20Development/Java.md) class, but you can select interface as the kind of class. Then give your interface a name. The same rules and conventions of naming classes apply to interfaces. If you forget to choose interface, that's perfectly okay. You can simply change the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) class to interface. One trait that every product has is a name. So let's create a field for that. We'll say private String name. Notice we get a compilation error stating that this field cannot be private. In normal classes, it's a good practice to encapsulate the fields by making them private. However, an interface is not a normal class. Its purpose is to serve as a template that other classes can later provide
>
> **[1:33](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/creating-an-interface?u=76281980&t=93)** implementation details for. Private fields cannot be accessed outside of the scope in which they are defined. So there is no purpose for private fields within interfaces, as classes that implement interfaces would not have access to private methods of that interface. Likewise, fields within an interface cannot be protected either, only public. Because of this, any field defined within an interface must be constant, meaning it's static and final. Even if these modifiers are not explicitly written when declaring the fields in an interface, it is still implicitly, both static and final. Because fields within an interface must be final, they require a value at the time of declaration, and this cannot be changed by an implementing class. Therefore, it's okay to omit the fields altogether and only provide methods. We can define methods with no bodies, just like in abstract classes, except we don't have to write the abstract reserved word, as this is implicit in interfaces. So let's make getter and setter methods for name, since they would be specific to any given product. So we'll simply write the return type, and the name of the method, and then void set name. And here's a case where we take a parameter. while our interface seems very basic,
>
> **[3:09](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/creating-an-interface?u=76281980&t=189)** this is a very powerful structure in Java, as interfaces are used to achieve abstraction, polymorphism in multiple inheritance.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (2)
> **Definitions:** is a  (3), means that (1)
> **Exercise Files:** template (2)
> **Analogies:** similar to (1), just like (1)
> **CLI Commands:** make (1)
> **Best Practices:** good practice (1)
> **Speakers:** - [instructor] (1)

#### [Implementing interfaces](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/implementing-interfaces?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/implementing-interfaces?u=76281980&t=0)** - [Instructor] Any class that implements an interface must implement all of its abstract methods or it must declare itself as an abstract class. Let's have this Book class implement the Product interface. We're not going to use the "extends" reserved [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md), because only interfaces are allowed to extend other interfaces. Classes must implement an interface. To do that, we use the reserved word "implements", and then specify the name of the interface. Notice, we get an error because the class must implement the abstract methods from the interface or declare itself as abstract. So, we'll right click within the class and choose to generate, and implement these methods. And let's also create the field that these methods will utilize. So we'll say, "private String name", and then let's clean up these methods as well. (keyboard keys clicking) Great, with this we've successfully held up our end of the contract in implementing the Product interface. We can enhance our Book class even more, by adding fields and methods that are specific to a book, such as the author, the pages, and the ISBN.
>
> **[1:39](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/implementing-interfaces?u=76281980&t=99)** And we can generate getter and setter methods for those as well.
>
> **[1:53](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/implementing-interfaces?u=76281980&t=113)** While a class can only extend one class, it can implement multiple interfaces. This is how to achieve multiple inheritance in [Java](../../Skills/Software%20Development/Java.md). To implement multiple interfaces, the class header must specify a comma delimited list of names of all of the interfaces that it would like to implement. If a class declares that it will implement multiple interfaces, then it must provide implementation for the abstract methods from all of those interfaces. If more than one of the interfaces being implemented contains a method with the same signature, the implementing class can provide one overwritten implementation, and that will fulfill the obligation with all of the relevant interfaces. However, if multiple interfaces being implemented contain a method with the same name but with a different return type, the class will receive an error when attempting to implement them, indicating that there is a clash.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (2), [Java](../../Skills/Software%20Development/Java.md) (1)
> **Env Vars:** isbn (1)
> **Definitions:** is a  (1)
> **Analogies:** such as (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (keyboard keys clicking) (1)

#### [Instantiating objects with interface types](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/instantiating-objects-with-interface-types?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/instantiating-objects-with-interface-types?u=76281980&t=0)** - [Instructor] Given the product interface in the book class which implements product, let's create a book object. We'll make an object of type product called book in attempt to instantiate that with the interface. We get an error message stating that product cannot be instantiated. Just like with abstract classes, we cannot instantiate interfaces. We can instead use any class that implements product. So let's use book. Now let's set the name of the book which will call the set name method from the book class. So we'll say book.setName and we'll call the book in the kitchen with H+ Sport. It's not a requirement to use the interface to declare objects, but it could be helpful if you need the object to be polymorphic.

> [!info]- Semantic Content
>
> **CLI Commands:** make (1)
> **Code Identifiers:** setname (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Default and static methods](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/default-and-static-methods?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/default-and-static-methods?u=76281980&t=0)** - [Instructor] In addition to abstract methods, interfaces can also contain default and static methods. Unlike abstract methods, both of these types of methods provide implementation. Let's explore these with a scenario. Here we have the Product interface that has methods to get and set the name of the product. And here we have the Book class, which implements Product. At this point, we realize that every product also has a price, and we like to add relative methods to the Product interface. So let's add a method to get the price. And then we'll add one to set the price.
>
> **[0:48](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/default-and-static-methods?u=76281980&t=48)** In interfaces, unless otherwise specified, the methods are implicitly public and abstract. So notice, with our new addition, we have broken any non-abstract class that implemented the Product interface because they are no longer adhering to the contract between an interface and an implementing class. To avoid breaking all implementing classes, you can define newly added methods as default. Default methods or methods within an interface that allow you to provide a default implementation for this method. This enables you to ensure backward compatibility in implementing classes when changes are made to the interface state implements. To indicate that the method is a default method, we write the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) default at the beginning of the methods header, and we give it a body. Same for set price. We can also provide a default implementation. For example, getPrice has an error because this method is supposed to return a double. So, we can give it a default price. And say return_0. But be very careful when implementing default methods because they are now available to objects that are instantiated with any of the classes that implement the interface. Using the dot operator on book2 shows us that the getPrice method
>
> **[2:23](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/default-and-static-methods?u=76281980&t=143)** is now an available method, even though the Book class did not override and implement this method. So the price of the book would be 0 because that's what we defined as the default implementation in the Product interface. That wouldn't really be good for business, so let's consider making the default price 50 so that our fictional company doesn't lose too much money. While default methods provide implementation, they are not final. Classes that implement the interface can override default methods and provide their own implementation. However, authors of those classes would need to be aware of the newly added methods to the interface. If there is no good default implementation, it may be best to leave the new method as abstract. So that implementing classes will get a signal in the form of a compilation error letting them know that an implementation is required. In addition to abstract and default methods, there's one more type of method allowed in interfaces, static methods. Like default methods, static methods provide implementation. However, they differ, in that, static methods are not inherited by implementing classes and, therefore, cannot be overwritten. This means that an interface of static methods are not available from the instance of an implementing class and can only be accessed from the interface itself. Interfaces cannot be instantiated, though,
>
> **[3:59](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/default-and-static-methods?u=76281980&t=239)** which is why the methods must be defined as static. This enables us to call the static method by using the interface's name. No instantiation is required to access static members. Key points to remember. Our interfaces cannot be instantiated. Interfaces can be implemented by classes and extended by other interfaces. Any class that implements an interface must implement all of its methods or declare itself as abstract. By default, methods in an interface are public and abstract. Explicit declaration of such is not required. In addition to abstract methods, interfaces may also contain default and static methods as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Definitions:** defined as (2), is a  (1), means that (1)
> **Code Identifiers:** getprice (2)
> **Analogies:** for example (1)
> **Warnings:** be aware (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)

#### [Code Solution: Shirt product](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-shirt-product?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-shirt-product?u=76281980&t=0)** - [Instructor] In this challenge, your task was to update the shirt class to implement the product interface. We used the implements keyword to implement the product interface. And when doing so, the shirt was then required to implement the abstract methods of the product interface. So getter and setter methods for name and price. And these were also added as fields as well.

> [!info]- Semantic Content
>
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 5. Data Structures

[↑ Back to Table of Contents](#table-of-contents)

#### [Collections framework](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/collections-framework?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/collections-framework?u=76281980&t=0)** - [Instructor] A collection is an object that holds references to other objects. Collections are considered [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) and the objects within a collection are known as elements. There are various types of collections. Some collections allow duplicate elements, while others require every element to be unique. Some collections are ordered while others are not. [Java](../../Skills/Software%20Development/Java.md) provides a collections framework which consists of interfaces, classes, and methods to store and manipulate these groups of objects. All of the interfaces and classes for the collections framework can be found in the java.util package. Collection itself is an interface and is the root of the hierarchy. Java does not provide any direct implementations of the collection interface, but there are other interfaces which inherit from collection. The most common ones are set, list, queue, and map. Set is a collection which cannot contain duplicate elements. An example use case for set would be a standard deck of 52 playing cards where each card would be an element in the collection. And each of them are unique. List is a collection where the elements are ordered. List can contain duplicate elements and the elements can be accessed by the position they appear within the list. An example of where list might be used
>
> **[1:34](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/collections-framework?u=76281980&t=94)** would be the phone numbers from your call history. They are listed in order. And some numbers may appear more than once. Queue is a collection of ordered elements, which is typically used to hold items that are going to be processed in some way. An example use case would be the people in the checkout line at a supermarket. People who are new to the queue are added at the end. And the processing of the queue is handled from the beginning. This type of processing is well known as first in first out. Map is a special data structure, as it does not inherit from the collection interface. However, it contains collection-like operations which enable them to be used as collections. Maps are used to hold key-value pairs. An example for this would be a list of bank transactions where each map entry has a unique transaction ID serving as the key. And the value would be the actual transaction object. Data structures and [Algorithms](../../Skills/Software%20Development/Algorithms.md) go hand in hand. In addition to the collections themselves, Java also provides a collections class which contains common algorithms that are needed to work with the data structures. Common ones include binarySearch, copy, frequency, reverse, shuffle, and sort.
>
> **[3:09](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/collections-framework?u=76281980&t=189)** To learn more about these methods and others available in the collections utility class, be sure to check out the Java doc. Collections are a fundamental part of the Java language and are commonly used in everyday programming to store and manipulate groups of objects.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (6), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (3), [Algorithms](../../Skills/Software%20Development/Algorithms.md) (2)
> **Definitions:** is a  (4), is an  (2), known as (1)
> **Code Identifiers:** binarysearch (1)
> **Speakers:** - [instructor] (1)

#### [Sets](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/sets?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/sets?u=76281980&t=0)** - [Instructor] A set is an unordered collection of unique objects. Because set is an interface, it cannot be instantiated, but it can be an objects type. Let's create a set of fruits. Some implementations of set are hash set, linked hash set, and tree sets. If you'd like to learn more about these implementations, be sure to review the [Java](../../Skills/Software%20Development/Java.md) document. For this example, let's use hash set. Right now we have an empty list. To add an item to a set, we use the add method. So let's add an apple, and we'll add a few more fruit as well. How about a banana? And let's add a lemon. Set is a collection of unique items, meaning there cannot be duplicate items in a set. If we add another apple here, notice we don't get an error. However, the duplicate element will simply not be stored. Let's print this out so that we can see what the set looks like. The output shows the three unique fruits we added to the set and not the duplicate apple. Also notice the items within the set are not in the order that we added them.
>
> **[1:35](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/sets?u=76281980&t=95)** Set is an unordered collection. So the order in which you place the items within the set are not necessarily persisted. And we have no control over where our elements will be placed. Another important note is that while we're using strings in this example, collections can hold any type of object. To detect whether a given collection contains a specific element, we can use the contains method. So let's print out have lemon and we will say fruits.contains. And we can just specify the object that we want to look for, in this case, lemon. If we run this, notice that it does indeed print true because lemon is a part of the set. The contains method is particularly useful when you didn't populate the collection yourself and maybe you've received it from another method. Another useful method is the remove method which allows you to delete an element from the collection. So we can say fruits, remove lemon. And there's also the size method which returns the number of elements in the collection. So let's ask how many elements are there after we've removed the lemon.
>
> **[3:07](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/sets?u=76281980&t=187)** We'll say number of elements after removing lemon and we'll say fruits.size.
>
> **[3:24](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/sets?u=76281980&t=204)** And notice after removing the lemon there are only two elements left. To add items to the set, we called the add method for every single element we wanted to place into the collection. There's an easier way to add elements to the collection and that's by using the static method of. Let's create a new set and we'll call this one more fruit. And then we'll say set.of. In here, we can enter a comma delimited list of objects we'd like to add. So let's add a pear, we'll add raisins, and let's add a cherry. And we'll go ahead and print out more fruit as well.
>
> **[4:16](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/sets?u=76281980&t=256)** Nice. Notice also not in the order that we added them. While the of method provides a great convenience in setting up this collection, it does come with a cost. By setting a collection in this way, the collection becomes immutable meaning it cannot change. Let's try calling the add and remove methods on this collection after using the set.of. We'll say more fruit.add cranberry
>
> **[4:52](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/sets?u=76281980&t=292)** and more fruit.remove pear.
>
> **[5:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/sets?u=76281980&t=300)** We won't get a compilation error. However, if we run this now, our program crashes with an unsupported operation exception. These are a few of the most common methods for set. Feel free to review the Java doc and try out some of the other methods of set.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2)
> **Definitions:** is an  (3), is a  (2)
> **Warnings:** important note (1)
> **Speakers:** - [instructor] (1)

#### [Lists](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/lists?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/lists?u=76281980&t=0)** - [Instructor] List is one of the most widely used collections. Some implementations of lists are array lists, link lists, stack, and vector. To review these implementations in greater detail, check out the [Java](../../Skills/Software%20Development/Java.md) doc. For this demonstration, we'll instantiate fruits as an array list. To add an item to a list, we use the add method. So let's add four fruits, we'll say an apple and then let's add a lemon. We'll add a banana and let's add an orange.
>
> **[0:54](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/lists?u=76281980&t=54)** Now let's print our list.
>
> **[1:02](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/lists?u=76281980&t=62)** Notice that the elements remained in the same order. we added them. This is because lists are ordered. Because of this, we can access elements of a list by their position. To do so, we call the get method. So let's print out fruits.get. And within the parameters we pass in an index. Just as with arrays, the index starts at zero. So if we get an index of two, that will print out the banana. Great. If we need to replace an element in the list, we can do so by calling the set method and passing in the index that you want the element to be placed at. Let's replace the banana with a grape. So we can do so by saying fruits.set and we pass in the index, which is two. And we're going to say grape. Now let's print this. And notice banana is no longer in the list. And now the grape appears at index two. A list can also contain duplicate elements. So let's add another lemon to our list. So we'll say fruits.add lemon, and now let's run it.
>
> **[2:40](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/lists?u=76281980&t=160)** Notice we have lemon within the list twice. List also has a few more methods that allow you to access the position of elements. Let's try asking for the index of lemon. So we'll say index of first lemon and we'll print fruits.index of and then lemon.
>
> **[3:11](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/lists?u=76281980&t=191)** And we see that the index of the first lemon is one. Or if we wanted the index of the last lemon, we can call last index of. So let's change this a bit and we'll say last lemon. And then here, we'll call last index of. And notice that index is four. To remove an element from a list. we call the remove method. There are two overloaded methods for remove, one that allows us to remove by index and another that lets us remove by object. Be careful in removing by object since list allows duplicates. If you're removed by object, it will remove the first found instance of that object. To remove the last lemon we added, we can use remove and pass in the index, which is four. So let's do that now. We'll say fruits.remove and we'll pass in index four and we'll print it. And now we see the last lemon has been removed. To create an immutable list, we call the list.of method. So let's say list more fruit. And we'll say list.of and add all of the objects within one line.
>
> **[4:47](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/lists?u=76281980&t=287)** So we can add our cherry. We'll add a cranberry and we'll add a plum. This gives you a pretty good overview of list. If you like to explore this collection further, check out the Java doc.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2)
> **Warnings:** be careful (1)
> **Speakers:** - [instructor] (1)

#### [Queues](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/queues?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/queues?u=76281980&t=0)** - [Instructor] A queue is a collection that is designed to hold items that need to be processed. The most common implementations of queue are linked lists and priority queue, which you can read more about in the [Java](../../Skills/Software%20Development/Java.md) docs. Let's use a linked list. In addition to the basic collection operations, queue also provides additional methods for insertion, removal, and inspection of elements. Let's add a few elements to the queue by using the add method, and then we'll print it out. Notice that the items do remain in the same order that we place them in, thus making queue an ordered collection. Queues also allow duplicates, so let's add another lemon to the queue.
>
> **[0:57](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/queues?u=76281980&t=57)** And then, as expected, the lemon does appear twice. Queues typically follow the first in, first out algorithm, which means elements are processed in the order in which they are entered. For example, if we call fruits.remove, we don't have to specify which object to remove. It will remove the first one in the queue because that's the one that's next in line. Remove also returns the element that was removed from the queue. Again, this is so you can process it. Let's store that inside of a variable. We'll say var removed = fruits.remove. Let's go ahead and print out which object was removed. We'll say print removed.
>
> **[1:55](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/queues?u=76281980&t=115)** Let's run it. Yep, it said it removed the apple, which was the first element in the queue. The first element of the queue is known as the head. By calling remove, we removed apple from the queue, which was the head. After removing apple, lemon now becomes the head. The last element of the queue is known as the tail. Sometimes we may want to know what's the next item to be processed before we actually do anything. For that, you can use the peek method. This returns the head of the queue without actually removing it. Let's say, what's the head of queue?
>
> **[2:45](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/queues?u=76281980&t=165)** We'll call fruits.peek. We can run this one. Yep, notice after we removed apple, lemon now became the head. This covers the basics of the queue collection. If you like to explore more, be sure to check out the Java doc.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2)
> **Definitions:** known as (2), is a  (1)
> **Analogies:** for example (1)
> **Speakers:** - [instructor] (1)

#### [Maps](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/maps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/maps?u=76281980&t=0)** - [Instructor] The Map interface maps unique keys to values. Here's a map of various fruits and their calories. Popular implementations of the Map interface are HashMap, TreeMap and LinkedHashMap, which you can read more about in the [Java](../../Skills/Software%20Development/Java.md) docs. In this example, we instantiated our map with HashMap. A map is not technically a collection, meaning it does not inherit from the collection interface but it's still considered part of the collection's framework. Map does not have access to the add method that we've seen in the other [Data Structures](../../Skills/Software%20Development/Data%20Structures.md). Map has a put method which is used to add the elements. The put method takes two arguments: a key and a value. We use the fruit's name as a key and the calories as the value. This example uses a string as the key and an integer as the value. However, you can use any type of object for either the key or value. Let's print the contents of this map. Notice the elements are unordered, meaning they do not appear in the order that we added them. You can access an element in the map by its key. While the keys are required to be unique, the values are not. If we call put passing in a key that already exists, the map will update that entry with the new value that was passed in. Let's try that by calling put with lemon again.
>
> **[1:35](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/maps?u=76281980&t=95)** So we'll save fruitCalories.put, and we'll pass in lemon and we'll change the value to 17. Let's print this again. The value of lemon is now 17 as opposed to 20. So what we did was technically an update. Let's write a little comment so we can remember. Overrides existing value. Great. There's another method called putIfAbsent, which you can call to prevent yourself from unintentionally overriding something that's already in the map. So let's comment this one out and we'll run again with putIfAbsent. So fruitCalories.putIfAbsent, we pass in the key, just like the other method and the value. And when we run again, we'll see that this time lemon was not updated because it's not absent. It already existed. There is also a replace method that is designed specifically for replacing an entry. In the case of replace, if the element doesn't already exist, it is not added. To get an object from the map, we pass in the key to the get method and it will return the corresponding value. So let's print out the value of banana. So we'll just say banana and we'll call fruitCalories.get, and we'll pass in the key, banana.
>
> **[3:12](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/maps?u=76281980&t=192)** And when we print this, it will print out the value of banana. So 105. To remove an element from a map, we call the remove method and we pass in the key of the element. We can also check to see if a map contains a certain key or a certain value. To do so, we can use the contains key and contains value methods. Map.of allows you to create an immutable map of key value pairs in one statement. The arguments are key followed by value and you can repeat this to add multiple elements to the map. While you can do this all on one line, it helps with readability and debugging to place each pair on its own line. Map also has a couple of methods that will return a set which inherits from the collection interface and therefore is a true collection. EntrySet will return the map elements as a set and keySet will return a set of just the keys as the map elements. With these, you can then use the set methods on the collection. The values method returns a collection of values. In this case, it would be a collection of integers. Map is a very common data structure. If you'd like to explore it more, be sure to check out the Java docs.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2), [Data Structures](../../Skills/Software%20Development/Data%20Structures.md) (1)
> **Code Identifiers:** fruitcalories (3), putifabsent (3), keyset (1)
> **Definitions:** is a  (2)
> **Analogies:** just like (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)

#### [Iterators](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/iterators?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/iterators?u=76281980&t=0)** - [Instructor] A common task in [Java](../../Skills/Software%20Development/Java.md) is to loop through the elements of a collection. One way to do this is with iterators. The collection interface provides an iterator for you. Set is an unordered collection, so there are no methods on Set that will allow us to get a certain item by its index, but we can loop through the Set with an iterator which we obtained by calling the iterator method on the collection. So we can say var i equals, and we'll use the name of the collection, .iterator. To use the iterator to access all elements of the collection, we'll need a loop, so let's make a while loop. The condition of the loop is i.hasNext. The hasNext method will return true if there are more items to process and false if it has reached the end of the collection. Inside the loop, we'll print a element by calling the next method on the iterator. So let's just say, i.next. As its name implies, this method returns the next element in the collection. In our case, we're simply printing it out, but you can do any type of processing you'd like on the elements. Let's go ahead and run this just so that you can see how it works. Yep, notice it went through and printed out every element of the collection.
>
> **[1:33](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/iterators?u=76281980&t=93)** The iterator method is provided by the collection interface so it is available on all collections that inherit from collection. Map, while a part of the collection's framework, does not inherit from the collection interface and therefore does not have the iterator method. However, map does provide the entry set method, which returns the map elements as a set. Given this set, you can then use the iterator method to loop through the elements. Iterators are just one way to loop through elements of a collection. I'll cover a more common approach in the next video.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** hasnext (2)
> **CLI Commands:** make (1)
> **Cross-References:** in the next (1)
> **Definitions:** is an  (1)
> **Speakers:** - [instructor] (1)

#### [Enhanced for loops](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/enhanced-for-loops?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/enhanced-for-loops?u=76281980&t=0)** - [Instructor] Just like with arrays, the need to loop through the elements of a collection is very common in programming. There's a special form of the for loop made just for this. It's referred to as an enhanced for loop. Let's iterate through this list of fruits and print each one of them. Just like with the normal for loop, we start the statement with the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) for and a set of parentheses. Inside of the parentheses, we declare the current element we're working with. Every element in this list is a string. So we can write the word string and then give the elements a variable name. Let's call it fruit, which is a singular element from the fruits collection. Next we add a colon and the name of the collection we're iterating through. The name of our list is fruits. So we'll add fruits here. This reads as for the next fruit element in the fruits collection. Notice we have an error stating incompatible types. When we defined our list, we didn't specify the data type of the elements we were adding. So by default, they are considered of type object. But when we're declaring fruit within our enhanced for loop, we're saying the elements are of type string. If we want to be more specific when declaring a collection, we can do so by specifying the data type
>
> **[1:33](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/enhanced-for-loops?u=76281980&t=93)** of the elements this collection can hold. This is done inside of a pair of angle brackets. So we can add string here and another set of empty angle brackets on the right side. Now we're okay to use the enhanced for loop. Inside of the loop, we can add any statements we like to execute for each fruit of the collection. So let's add a print statement, and we'll just print out the current fruit. And let's run this. Great, we see that each element of the collection was printed. While this example demonstrates iterating over lists, the enhanced for loop works for every type of collection. Although, with maps you'll need to iterate over the entry set as opposed to directly over the map itself.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (2)
> **Definitions:** is a  (2)
> **Analogies:** just like (2)
> **Prerequisites:** you'll need (1)
> **Speakers:** - [instructor] (1)

#### [forEach() and lambdas](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/foreach-and-lambdas?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/foreach-and-lambdas?u=76281980&t=0)** - [Instructor] The enhanced for-loop provides a more convenient approach to iterating over collections than the iterator. However, there's an even more straightforward approach to do so by using the forEach method that is available on every collection in Map. Here we have a list of fruits, and we want to loop through the collection and print each element. On our collection, we can call the forEach method. Look how straightforward this is. There's no need to even define a loop. Inside of the parenthesis of the forEach method, we provide the actions that we want performed for every element of this collection. One way to express the action to perform is with a lambda expression. A lambda expression is a short block of code that, just like a method, accepts parameters and returns a value. They differ from methods in that they do not have names, and they're executed in-line versus being called by other methods. Let's write a Lambda expression to print each element of the collection. The first step is to provide a name for the current elements. By convention, these are usually short one-letter names because they are scoped to just this block of code, and they will not be referenced outside of the lambda expression. However, it's perfectly okay to use full variable names if you want. Let's call ours "f."
>
> **[1:35](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/foreach-and-lambdas?u=76281980&t=95)** The next step is to indicate that we're going to specify the action. To do this, we type a dash, followed by the greater than symbol to make what looks like an arrow. Finally, we provide the action to perform. So let's say system.out.println, and we'll pass in "f." That's it, a simple one-liner that will perform an action for every element in the collection. When the action to perform is a single method call, we can simplify this even further with a method reference. So as opposed to declaring the element as input and output, it's assumed. So we only need to provide the class name. Let's do that now. So we'll say fruits forEach, and then we provide the class name, two colons and then the method name. So println. This reads as call this method and pass in the current element we're on. Let's run this. Notice we have apple, lemon, banana, orange and apple, lemon, banana, orange. So each of these shows the exact same output. Which one you use is a matter of preference. With iterators and the enhanced for-loop,
>
> **[3:08](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/foreach-and-lambdas?u=76281980&t=188)** you're able to apply multiple statements to each element of the collection. You can do the same with forEach. So let's say fruits.forEach. We'll add our variable "f" and our arrow and then to enclose multiple statements, we add a set of curly braces. Inside of here, we can add any statements we need. So let's say f = fruits: + f, and then let's print f out,
>
> **[3:44](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/foreach-and-lambdas?u=76281980&t=224)** and we'll run this one, and notice that multiple statements were executed. Now, let's look at an example with Map, as they have both a key and a value pair. So the lambda expression requires two inputs. So let's say fruitCalories.forEach and within the forEach method, we can define variables for both the key and the value within an additional set of parentheses. So we'll say k for key and v for value. Next, we add our arrow and then finally, we can add our action. So we'll say print k +. We'll put a little colon in between them and then V, and let's run this one. Great, we see that it printed the key: and then the value. ForEach is the most modern approach to iterating over [Java](../../Skills/Software%20Development/Java.md) collections. So be sure to practice this with your own collections.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** foreach (8), fruitcalories (1)
> **Definitions:** is a  (3)
> **CLI Commands:** make (1)
> **Analogies:** just like (1)
> **Speakers:** - [instructor] (1)

#### [Code Solution: Gradebook](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-gradebook?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-gradebook?u=76281980&t=0)** - [Instructor] Your task for this challenge was to evaluate two maps of test scores, gradebook, and makeupGrades, and return a single map which contains the best of the two scores for each student. Let's start by looping over the original map of grades, which is gradebook, and say, for each entry in this map, we'll call the key student and the value originalGrade. Then we use that key to get the value of the new test score for that student from the makeupGrades map, and we store that value into the makeupGrade variable. Now, we can compare the new makeup grade with the original grade, and if the makeup grade is better, we'll update the gradebook map by using the put method to replace the student's old grade with the new one.

> [!info]- Semantic Content
>
> **Code Identifiers:** makeupgrades (2), originalgrade (1), makeupgrade (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 6. Functional Programming

[↑ Back to Table of Contents](#table-of-contents)

#### [Functional interfaces](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/functional-interfaces?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/functional-interfaces?u=76281980&t=0)** - [Instructor] [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) is another style of developing software that uses mathematical functions on an input set to produce an output set. While [Java](../../Skills/Software%20Development/Java.md) is an [object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) language, it does provide the java.util.function package, which hosts several functional interfaces. A functional interface is an interface with just one abstract method. That one method is what lambda expressions and method references implement. Here is a list of countries. If we wanted to print each country, we could use forEach and a lambda expression that says to print. The lambda expression is actually an implementation of the Consumer functional interface from the java.util.function package. The Consumer interface has one abstract method that accepts a single input argument and returns no result. Specifically, our lambda expression is the implementation of the functional interface's single abstract method, which in this case is accept. To illustrate this, I've created a variable called print whose type is Consumer. When we call forEach and pass in this interface, the implementation we provided for accept is what is executed. Even without forEach, we can call the abstract method for the Consumer interface accept and pass in any object.
>
> **[1:38](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/functional-interfaces?u=76281980&t=98)** It will use the implementation that we provided, which is to print the string. In addition to a lambda expression, the implementation can also be in the form of a method reference. In addition to Consumer, here are other core functional interfaces provided by the java.util.function package. Note that for the abstract method signature, T stands for the type of the input, R stands for the type of the return, and U stands for the type of the second argument if there is one. We have Supplier, which returns a value but does not accept any input. Predicate, which represents a function that accepts one argument as input and returns a Boolean value as output. Function, which represents a function that accepts one argument as input and produces a result. Then there are specific types of function operators, such as the UnaryOperator, which represents an operation that accepts a single argument as input then returns an output which is of the same type as the input. UnaryOperator extends the function interface. And then there's BinaryOperator, which represents an operation that accepts two inputs of the same type and returns an output of that same type. BinaryOperator extends the BiFunction interface. The remaining functional interfaces in the java.util.function package
>
> **[3:12](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/functional-interfaces?u=76281980&t=192)** are specific variations of these core interfaces. In addition to the one abstract method available in every functional interface, the interface can also provide default and static methods. For example, the function interface provides a default method named andThen, which allows you to perform another operation on the result from the previous operation. Here we have two separate statements. Square, which uses a lambda expression to implement the apply method of function to return the square of the input. And add, which adds the input to itself and returns the sum. We can say first execute square and then execute add. By calling apply, we provide the actual input of 5, and we get 50 as the result. While the functional interfaces in the java.util.function package cover most use cases you may have, you can create your own functional interfaces as well. It's recommended that your interface is annotated with @FunctionalInterface so that Java will enforce the rule of only one abstract method. Then your abstract method will let implementers know if any arguments are required and if any results will be returned. Optionally, you may also include additional static or default methods that may be useful.
>
> **[4:44](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/functional-interfaces?u=76281980&t=284)** With this knowledge, you're now able to use Java for both object-oriented and functional programming.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (8), [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) (2), [Object-oriented programming](../../Skills/Software%20Development/Object-Oriented%20Programming%20(OOP).md) (1)
> **Definitions:** stands for (3), is an  (2), is a  (1)
> **Code Identifiers:** foreach (3), andthen (1)
> **Analogies:** such as (1), for example (1)
> **Best Practices:** recommended (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Streams](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/streams?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/streams?u=76281980&t=0)** - [Instructor] In addition to the [Java](../../Skills/Software%20Development/Java.md).util.function package, Java also provides the java.util.stream package for your [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) needs. A stream represents a sequence of elements from a collection, array or other input source. A stream does not store elements like collections do, but instead, allows you to perform operations on the data without modifying the original source. The java.util.stream package contains built-in interfaces and classes to perform these operations. Here's an array of even numbers. We could create a stream of this array, then perform an operation on them. This will print a sequence of odd numbers without affecting the original array. The stream API also provides a method to process operations on the elements in parallel. When doing so, multiple threads are used to process the elements of the stream at the same time. Notice the output of this run is no longer sequential and is likely to vary every time this code is executed. The stream API is a powerful tool for [Data Processing](../../Skills/Database%20Management/Data%20Processing.md).

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (4), [Functional Programming](../../Skills/Software%20Development/Functional%20Programming.md) (1), [Data Processing](../../Skills/Database%20Management/Data%20Processing.md) (1)
> **Env Vars:** api (2)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Common stream operations](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/common-stream-operations?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/common-stream-operations?u=76281980&t=0)** - [Instructor] The stream API provides several useful operations that can be performed on streams. These operations can be either intermediate or terminal. Intermediate operations return the resulting stream after performing an operation. This enables you to continue to perform more operations on the stream if you desire. Terminal operations close the stream and return a result of the operation. This result can either be void or be a value. After a terminal operation, no more operations can be performed on the stream. Given this list of vegetables, let's look at examples of a few of the most common stream operations. Let's see if there are any veggies that are more than one [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md). To do so, we can use the anyMatch operation. The anyMatch operation looks through the stream to determine if any of the elements match a given condition. The condition is passed into the anyMatch method in the form of a predicate. So our lambda expression must return a Boolean value. We can use the contains method of string and pass in a white space which represents a space in the veggie name. Since the stream contains green beans and Brussels sprouts, this would return true. Because it returns true and not a resulting stream, anyMatch is a terminal operation and the stream is now closed.
>
> **[1:34](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/common-stream-operations?u=76281980&t=94)** The allMatch operation determines if every element in the stream matches a certain condition. Like anyMatch, the all match operation also takes a predicate. We ask if all of the elements contain an S and because cabbage does not, this will return false. AllMatch is terminal, so the stream is now closed. To narrow the stream down to only elements that meet a certain condition, we can use the filter operation. Filter is an intermediate operation, so it returns the resulting stream. From here, we can perform another operation. So let's print the elements of the stream by using forEach. Cabbage and carrots are printed. ForEach is a terminal operation, so after that call, the stream is closed. There are also stream operations that allow you to manipulate the data. One such operation is map which accepts a function. Let's use map to convert every element to uppercase. We can do so by calling the string toUpperCase method. Since the input to this function is the element itself, we can use a method reference as opposed to a lambda expression. Map returns a resulting stream so we can perform further operations. If we print the elements of the stream, we see they are now in uppercase. What's printed is the results of the stream operations. It's important to note that our original list of veggies
>
> **[3:09](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/common-stream-operations?u=76281980&t=189)** was not modified at all. The reduce operation is used to combine the sequence of elements into a single value. It's commonly used when dealing with numbers. Reduce optionally accepts an initial value to start from. This is known as the identity in a required binary operator, which is used to accumulate the values Here, we are calling the current element A and the next value B, and are saying to add A to B. This continues until the end of the stream is reached. The resulting value of 30 is returned which is the sum of all the numbers. And the stream is then closed. Reduce can also be used with other objects, such as streams. The sorted operation will rearrange the stream in alphabetical order. Then the reduced method is saying, begin with an empty string and concatenate the stream with the pipe between each word. This will reduce all of the elements into one pipe delimited stream. The collect operation takes the resulting stream and returns a data structure of your choice. Let's filter the stream of veggies to only include those that end with an S. This returns a new stream, which we then can call collect on. Collect takes a collector. Let's turn our stream into a new list by using collectors.tolist
>
> **[4:45](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/common-stream-operations?u=76281980&t=285)** and assigning it to a new list object. Collect is a terminal operation. So if we want to print the elements, we need to do it on our new list. As you can see, the stream API is very convenient and provides a number of operations that allow you to process data. What's really powerful is the ability to chain together multiple intermediate operations, which are all performed with one pass through the data.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (2)
> **Code Identifiers:** anymatch (5), allmatch (1), foreach (1), touppercase (1)
> **Tools:** terminal (7)
> **Definitions:** is a  (3), is an  (1), known as (1)
> **Env Vars:** api (2)
> **Analogies:** such as (1)
> **Warnings:** note that (1)
> **Speakers:** - [instructor] (1)

#### [Code Solution: Color filter](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-color-filter?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-color-filter?u=76281980&t=0)** - [Instructor] In this challenge, your task was to use stream operations to filter a list of colors into a sub list of only the colors that contain the letter E. So given the original list of colors, we can get a stream, and then from here, we'll call filter. And inside a filter, we need to pass in the predicates, specifying the condition that an element must match in order to be included. So we'll say for every color in the list, check that color to see if it contains the letter E. After filtering the list, we collect these filtered elements into a new list, which we can obtain by saying Collectors.toList.
>
> **[0:58](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-color-filter?u=76281980&t=58)** When we run this, we see that all of the colors that were filtered indeed contain the letter E.

> [!info]- Semantic Content
>
> **Code Identifiers:** tolist (1)
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### 7. Exception Handling

[↑ Back to Table of Contents](#table-of-contents)

#### [Try and catch blocks](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/try-and-catch-blocks?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/try-and-catch-blocks?u=76281980&t=0)** - [Instructor] An exception is an event that occurs at runtime due to an error. Exceptions disrupt the normal flow of a program. This code compiles. There are no errors or warnings. However, if we run this, we get an arrow when the loop attempts to access index three of this array, because there is no index three. We receive an array index out of bounds exception and the program would crash. You can handle exceptions within your code so that instead of allowing the program to crash, you can handle the exception in a graceful manner. Let's write a program that creates a file on the file system and handles any exceptions that may occur. We're going to use the file class from [Java](../../Skills/Software%20Development/Java.md) to create a file object, and we'll give it a path that does not exist. We'll say resources/nonexistent.txt. The file class has a method named createNewFile. This will create the new file if the resources directory exists, which it does not. Notice we have a compilation error stating there's an unhandled exception, and it specifies the name of the exception is IO exception. This means that in case of an error, the createNewFile method will throw an IO exception. We can handle this with a try catch block. When there's a statement that throws a possible exception, you can place that statement inside of a try block. We write try in a set of curly braces,
>
> **[1:36](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/try-and-catch-blocks?u=76281980&t=96)** then we place the statement inside of the try block. We follow the try block with a catch block, which has a set of parentheses, as well as a set of curly braces. Inside of the catch parentheses, we have to specify the exception that we're going to catch. CreateNewFile has the potential of throwing an IO exception, so we want to catch what it's throwing. We specify that as the type and then we give this a variable name. By convention, people typically name this E. Java is going to execute the createNewFile method call. If an exception happens to be thrown, we have provided a catch block. Java will then execute the code inside of the catch block as opposed to crashing the program. Let's print a message here to inform the user of the error. We'll say, sorry, an error has occurred. Let's run this. We know that the resources directory does not exist, so an exception is thrown and our graceful message is printed to the screen. While we're simply printing a message here, you could do anything, such as creating the directory that's needed and then re-executing the code. As you can imagine, exception handling gives us greater control over the [user experience](../../Skills/Web%20Development/User%20Experience%20(UX).md) of our applications.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3), [User experience](../../Skills/Web%20Development/User%20Experience%20(UX).md) (1)
> **Code Identifiers:** createnewfile (3)
> **Definitions:** is an  (1), means that (1)
> **Analogies:** such as (1), imagine (1)
> **File Paths:** resources/nonexistent.txt (1)
> **Speakers:** - [instructor] (1)

#### [Stack trace and exception message](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/stack-trace-and-exception-message?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/stack-trace-and-exception-message?u=76281980&t=0)** - [Instructor] Exceptions are objects, which means we have access to helpful methods for dealing with them. One of which is getMessage. The getMessage method details a message of why the exception was thrown. We can append that message to the one we print to the users. So let's add that here. We'll say e, which is what we called our exception .getMessage. Let's run this. The message reads no such file or directory. Now our end users have much more information about exactly what went wrong. Exceptions also have stack traces. A stack trace is the history of method calls made within the program's execution that led up to the exception being thrown. It allows programmers to trace the path the program has taken, and it's helpful in figuring out what and where things may have gone wrong. It's a common practice to use the exception to print the stack trace within the catch block. So we say e.printStackTrace, and let's run this again. The stack trace is here in red and it shows each class, method and line number that the program went through before the exception occurred. And it even allows you to click on any stack entry. The last line of the stack trace is where the code that caused the exception began.
>
> **[1:36](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/stack-trace-and-exception-message?u=76281980&t=96)** This points to line 11, which is the File.createNewFile method. The next entry is in [Java](../../Skills/Software%20Development/Java.md)'s file class. Clicking this entry takes us inside of the create new file method to see exactly which line of that method leads us to trouble. As we can see it's the filesystem.createFileExclusively method, called by Java. The next entry doesn't give us a line number, because a native method was called. A native method is one that was not written in Java and therefore we don't have access to inspect it from the Stack Trace. And then the final entry, at the very top of the trace gives us the exception and the message. Exception messages and stack traces are extremely helpful in debugging your code to determine exactly what caused an error.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3)
> **Code Identifiers:** getmessage (3), printstacktrace (1), createnewfile (1), createfileexclusively (1)
> **UI Navigation:** click on (1)
> **Speakers:** - [instructor] (1)

#### [Checked vs. unchecked exceptions](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/checked-vs-unchecked-exceptions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/checked-vs-unchecked-exceptions?u=76281980&t=0)** - [Instructor] There are two categories of exceptions; checked and unchecked. The header of the createNewFile method of the file class specifies that it throws an IO exception. If we attempt to call this method outside of a tri-block, we get a compilation error. The header of the nextDouble method of scanner does not specify that it throws any exceptions. However, if a scanner object attempts to read input from the console using the nextDouble method but the input is a string, an input mismatch exception will be thrown. Even though this code is not within a tri-block, there are no compilation errors at all. This is because input mismatch exception is an unchecked exception. An IO exception is a checked exception. Checked exceptions are exceptions that are verified by the compiler before the code is executed. If a method throws a checked exception, it must declare that in its method's signature. This act is what lets the compiler know that any code that calls such methods needs to account for the possibility of having an exception thrown. Unchecked exceptions are exceptions that are not verified by the compiler. Runtime exception and all of its sub-classes are considered unchecked exceptions. Unchecked exceptions can be thrown by a method, but the method signature does not have to declare that it will throw the exception.
>
> **[1:33](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/checked-vs-unchecked-exceptions?u=76281980&t=93)** Methods typically use checked exceptions when it's reasonable that a calling method can recover from the error. Unchecked exceptions are used when there isn't really anything the calling code can do about it. While unchecked exceptions are not required to be handled, a programmer can still choose to catch it if they desire.

> [!info]- Semantic Content
>
> **Code Identifiers:** nextdouble (2), createnewfile (1)
> **Definitions:** is a  (2), is an  (1)
> **Speakers:** - [instructor] (1)

#### [Handling multiple exceptions](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/handling-multiple-exceptions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/handling-multiple-exceptions?u=76281980&t=0)** - [Instructor] Sometimes the code we write has the possibility of throwing various exceptions, and we want to be prepared to handle any of them. There are three ways to handle multiple exceptions at once. Polymorphism, a series of catch blocks, and one block that catches multiple exceptions. When handling exceptions, you can use a super class, as a way to catch broader exceptions. If we look at the [Java](../../Skills/Software%20Development/Java.md) doc for the rate index out of bound exception, we see that it inherits from a chain of other exceptions, with the top level exception class being exception. All exceptions inherit from the exception class. In the case of the file.createNewFile method, we're catching the IO exception, because we know that's what the method is going to throw. However, what if this method threw more than one exception? Or we weren't quite sure exactly what exception it was going to throw? We could use polymorphism, and instead of catching the IO exception, we can specify one of its super classes. Knowing that exception is the top most super class for all exceptions, that's a safe bet. An alternative option is to use multiple catch clauses to handle different types of exceptions. Let's write a program that reads decimal numbers from a file, and then we're going to handle two types of exceptions that come from that program. The file not found exception, and input mismatch exception. Here we have a file object that points
>
> **[1:33](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/handling-multiple-exceptions?u=76281980&t=93)** to this numbers.txt file, which does exist. And we are going to use the scanner object to read from that file. The compilation error states that we have an unhandled exception, the file not found exception. So let's place this within a try catch block. Next, we want to read every line from the file, so we're going to use a while loop to print the numbers to the console. So let's say, while fileReader.hasNext, go ahead and print fileReader.nextDouble.
>
> **[2:13](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/handling-multiple-exceptions?u=76281980&t=133)** The next double method throws an unchecked exception. So there's a possibility that when we're reading from the file, if one of the lines in the file is not an actual double, which we see here on line five, then we would get an input mismatch exception. We can handle the case of the file not found exception, and we can also add another catch block for the input mismatch exception. So let's do that now. We're going to catch input mismatch exception, and we'll also call that E, and then we'll print the stack trace. So now, if either one of these exceptions is thrown, it will go into the respective catch clause. If we wanted to be extra safe, and catch any other unexpected exception that may occur, we could add a catch clause for exception. Let's do that up top. So we'll say catch exception, E. Notice this gives two compilation errors, stating that file not found exception, and input mismatch exception have already been caught, because exception is a super class for both. If the multiple catch clauses contain related exceptions, the subclasses catch clause must appear first, otherwise it will never have the possibility of reaching the other catch clauses. So we can simply move exception to the last catch block. Let's talk about the third way
>
> **[3:44](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/handling-multiple-exceptions?u=76281980&t=224)** of handling multiple exceptions. If you find the code within your catch blocks will be the same, you can eliminate duplication by having one catch block handle multiple named exceptions. To do so, you list all of the exceptions, and separate them with the pipe. This one block will catch either the file not found exception, or the input mismatch exception, and will execute the same code, no matter which one is caught. You've now learned three ways to handle multiple exceptions at once. Polymorphism should be used when you want to handle many exceptions in a specific hierarchy of exceptions. The series of catch blocks can be used when you want to handle different exceptions in different ways, thus providing different code in each of the catch blocks. And the multiple catch block can be used when you want to handle multiple exceptions in the same way, even if the exceptions have no hierarchical relation.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1)
> **Code Identifiers:** filereader (2), createnewfile (1), hasnext (1), nextdouble (1)
> **File Paths:** numbers.txt (1)
> **CLI Commands:** find (1)
> **Definitions:** is a  (1)
> **Speakers:** - [instructor] (1)

#### [Finally block](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/finally-block?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/finally-block?u=76281980&t=0)** - [Instructor] Consider this code that opens a file and reads from it within a try block. Exceptions interrupt the flow of the program. If an exception was thrown on line 14 when we tried to instantiate scanner, then the catch block would be executed and nothing else in the try block would run. Adding fileReader.close inside of the try block is not a good idea. Likewise, if we place the close method outside of the try catch block and an exception is thrown, the program will end after executing the catch block, so this still is not guaranteed to execute. If we place the close call within the catch block, then it would not execute in the event that an exception is not thrown. A finally clause can optionally be added below any catch clauses. After all of the catch clauses, we type the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) finally. This clause does not have parentheses, but it does have a set of curly braces. Inside of here, you can place any code that you wish to execute, no matter what happens with the try and the catch clauses. Let's add the call to close here. The finally block will execute no matter what, if exceptions are thrown or if exceptions are not thrown. Finally, we'll execute.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Code Identifiers:** filereader (1)
> **Speakers:** - [instructor] (1)

#### [Try with resources](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/try-with-resources?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/try-with-resources?u=76281980&t=0)** - When resources such as input or output streams are opened and an exception interrupts the flow of the program those resources aren't closed properly unless the programmer took extra precautions to do so. One way to solve this is by closing the resources in a finally block. But a better approach is to use try-with-resources. Try-with-resources allows you to declare resources that will be used within the try block. And [Java](../../Skills/Software%20Development/Java.md) will automatically close them for you after the execution of the block. To try-with-resources we use a set of parentheses after the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) try, and inside we declare and initialize the resource. So let's say scanner we'll call this file reader and we'll set it equal to new scanner and pass in our input file. When we have the resource declared at this level we don't need a finally clause to close the resource. You can also specify multiple resources to close. For example, if we wanted to open another file for writing we could also place that resource here. We'd add a semicolon after the first resource and then declare and initialize the next resource. So, let's go ahead and put this on a new line so that it's nice and clear. We'll go ahead and use a print writer to write to the output file and we'll call this file writer and we'll say equals new print writer
>
> **[1:36](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/try-with-resources?u=76281980&t=96)** and we'll pass in our output file. And I'll change the systems I print to use the file writer.
>
> **[1:51](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/try-with-resources?u=76281980&t=111)** These resources will be closed in the reverse order that they were defined. So the file writer will be closed first followed by the file reader. Try-with-resources only works with classes that implement the closable or auto-closable interfaces. Both scanner and print writer implement these interfaces. Try-with-resources is a great option when you're working with resources and you are only going to use a finally block to close the resource. You may have cases where finally is a good approach to execute other statements, and that's fine as well.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (1), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Definitions:** is a  (2)
> **Analogies:** such as (1), for example (1)
> **Speakers:** - when (1)

#### [Throwing exceptions](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/throwing-exceptions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/throwing-exceptions?u=76281980&t=0)** - [Instructor] Sometimes you may find yourself needing to flag errors within the code you write. You can do this by throwing exceptions. This calculate salary method accepts the hours worked and pay rate and then returns the pay. However, perhaps your application doesn't allow for overtime which is anything more than 40 hours. There's no [Java](../../Skills/Software%20Development/Java.md) exception that will be automatically thrown for such a thing as this is only an error from the programmer's perspective. So you can throw the exception yourself. Let's add a condition that checks if ours is more than 40. So we'll say, if ours is greater than 40, then to throw an exception, we write the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) throw followed by an instantiation of an existing exception. You can throw one of the many exceptions provided by the Java language or you can even create a new exception class for your needs. For this case, let's throw the illegal argument exception. This one is provided by Java. In this constructor, we pass in a message that will be printed in the stack trace as well as returned by the exceptions, get message method. So let's say, hours must be less than or equal to 40.
>
> **[1:25](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/throwing-exceptions?u=76281980&t=85)** Now let's look inside of this Illegal Argument Exception Class. Because illegal argument exception extends runtime exception, it is an unchecked exception and we do not need to declare that our method throws this exception. Let's try throwing a checked exception as well. In fact, we'll create our own. So I'm going to create a new class, and let's call this class Negative Input Exception.
>
> **[1:59](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/throwing-exceptions?u=76281980&t=119)** And this class will extend exception. That's all that's required to make your own exception. We'll go ahead and create two constructors. A default one and one that accepts a custom message. So I'll say, public Negative Input Exception. This one won't take any arguments, but it'll call into another constructor here that takes the message. So we'll give just a default message here saying input must be greater than or equal to zero. And let's fix this typo, great. And then let's make one more constructor. And this one will take a message. And we'll call into super which is from the exception class and pass the message. Now from our calling method, we can throw our custom exception if the input is negative. So after this check, let's do another one. And we'll say if ours is less than zero, or if the pay rate is less than zero, let's throw our exception. Throw new Negative Input Exception. Because our method is throwing a checked exception, we must specify that in the method signature. We can do that by adding the word throws in the name of the unchecked exception
>
> **[3:34](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/throwing-exceptions?u=76281980&t=214)** that this method throws. So that goes right here. After the argument list, we'll say throws Negative Input Exception. Now you know how to throw checked and unchecked exceptions from your methods. And even how to create your own custom exceptions. Excellent work.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (3), [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (2)
> **CLI Commands:** make (2), find (1)
> **Definitions:** is an  (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)

#### [Rethrowing exceptions](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/rethrowing-exceptions?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/rethrowing-exceptions?u=76281980&t=0)** - [Instructor] If your method has code that has the potential of throwing a checked exception, you must either catch that exception or rethrow it. Let's write a program that creates a new file but does not catch the exceptions. We're attempting to create a new file, but we have a compilation error on the createNewFile method stating that there's an unhandled exception, the IOException. Of course, we can place this within a try-catch block, but we're not required to. Instead, we can rethrow the exception. This means our method will not handle the thrown exception and instead will pass the burden on to any method that calls this one. To rethrow the exception, within the method signature we add the [word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) throws followed by the exception that we're not catching. In our case, that's the IOException. Notice now that the compilation has moved to the main method, which is the method that is calling this one. The main method is now on the hook for catching the exception or rethrowing it. Let's rethrow it again. So we'll say throws IOException. If we run this program, we'll see that the exception goes uncaught, and the program crashes. If we need to throw more than one exception,
>
> **[1:33](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/rethrowing-exceptions?u=76281980&t=93)** we could list them all in the method signature separated by commas. For example, we could say this throws the IOException, comma, and then specify another exception, such as FileNotFound. Polymorphism also works in the throws clause of a method signature. For example, if we instantiate Scanner outside of a try block, we didn't get a compilation error even though the Scanner constructor throws a FileNotFoundException. That's because FileNotFoundException extends IOException. So by declaring that this method throws the IOException, that covers not just IOException itself, but also any of its subclasses. You now have multiple options for dealing with the exceptions that your methods may encounter.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Word](../../Skills/Cloud%20Computing/Microsoft%20Word.md) (1)
> **Analogies:** for example (2), such as (1)
> **Code Identifiers:** createnewfile (1)
> **Prerequisites:** required to (1)
> **Speakers:** - [instructor] (1)

#### [Code Solution: Unauthorized user](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-unauthorized-user?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/code-solution-unauthorized-user?u=76281980&t=0)** - [Instructor] For this challenge, your task was to throw an unauthorized exception if the username that was passed in does not exist in your authorized user's list. To do so, we can say if authorized users does not contain the username, so let's code that now. If not, and we'll say authorized users, that contains a username. Then we want to throw a new unauthorized exception, and we're going to pass in this access denied message. So we'll say access denied message. Okay, great. So we get a little compilation error here, and that's because since we may throw a checked exception, we must declare this in the methods header. So in the header here, we'll say throws unauthorized exception. Great, when we test this, we should see that the code properly threw an exception because the username, Angie does not exist in this authorized user's list.

> [!info]- Semantic Content
>
> **Speakers:** - [instructor] (1)
> **Non-Speech:** (upbeat music) (1)


### Conclusion

[↑ Back to Table of Contents](#table-of-contents)

#### [Next steps](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/next-steps?u=76281980)

> [!transcript]- Transcript
>
> **[0:00](https://www.linkedin.com/learning/java-essential-training-objects-and-apis-16021820/next-steps?u=76281980&t=0)** - [Angie] Congratulations. You've made it to the end of the course. You've learned quite a lot about [Java](../../Skills/Software%20Development/Java.md) programming. But building projects really helps to cement your newfound knowledge. I encourage you to create a new program of your own and add it to your portfolio. If you need some ideas, consider a [Banking](../../Skills/Software%20Development/Banking.md) system that allows a user to create a new checking or savings account, and deposit, withdraw, and check the balance of their account. Or a car reservation system that keeps an inventory of cars, shows a potential customer which cars are available for rental, and allows them to place a reservation. Or maybe a text-based adventure game that allows a player to navigate various locations of the game world and make decisions via the console. As you work on your project, consider revisiting the course videos as you implement each concept. If you're ready for another course, there are some great options here on [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) Learning. Just search Java in the top bar. Thanks so much for taking this course. If you'd like to keep in contact, you can follow me on LinkedIn, or on Twitter, where my handle is techgirl1908.

> [!info]- Semantic Content
>
> **Frameworks & Libraries:** [Java](../../Skills/Software%20Development/Java.md) (2), [LinkedIn](../../Skills/Web%20Development/LinkedIn.md) (2), [Banking](../../Skills/Software%20Development/Banking.md) (1)
> **CLI Commands:** make (1)
> **Speakers:** - [angie] (1)


## Instructor

- [Angie Jones](../../Instructors/Software%20Development/Angie%20Jones.md)

## Resources

- Exercise files available

## Skills Covered

- Java

## Path Context

### In [The Top Skills Engineering Professionals Have Right Now](../../Paths/Software%20Development/Learning%20Paths/The%20Top%20Skills%20Engineering%20Professionals%20Have%20Right%20Now.md)
← [JavaScript Essential Training](JavaScript%20Essential%20Training.md) | **3 of 9** | [SOLIDWORKS 2022 Essential Training](SOLIDWORKS%202022%20Essential%20Training.md) →

### In [Getting Started as a Java Developer](../../Paths/Data%20Science/Learning%20Paths/Getting%20Started%20as%20a%20Java%20Developer.md)
← [Java Essential Training Syntax And Structure](../Data%20Science/Java%20Essential%20Training%20Syntax%20And%20Structure.md) | **2 of 10** | [Learning Spring 6 With Spring Boot](Learning%20Spring%206%20With%20Spring%20Boot.md) →

### In [Java Foundations Professional Certificate by JetBrains](../../Paths/Data%20Science/Professional%20Certificates/Java%20Foundations%20Professional%20Certificate%20by%20JetBrains.md)
← [Java Essential Training Syntax And Structure](../Data%20Science/Java%20Essential%20Training%20Syntax%20And%20Structure.md) | **2 of 5** | [Java Object-Oriented Programming](../Data%20Science/Java%20Object-Oriented%20Programming.md) →

## Part of

- [Java Foundations Professional Certificate by JetBrains](../../Paths/Data%20Science/Professional%20Certificates/Java%20Foundations%20Professional%20Certificate%20by%20JetBrains.md)

## Appears In

- [The Top Skills Engineering Professionals Have Right Now](../../Paths/Software%20Development/Learning%20Paths/The%20Top%20Skills%20Engineering%20Professionals%20Have%20Right%20Now.md)
- [Getting Started as a Java Developer](../../Paths/Data%20Science/Learning%20Paths/Getting%20Started%20as%20a%20Java%20Developer.md)
- [Java Foundations Professional Certificate by JetBrains](../../Paths/Data%20Science/Professional%20Certificates/Java%20Foundations%20Professional%20Certificate%20by%20JetBrains.md)

## Related Courses

_Courses sharing skills:_

- [Practice It- Java](Practice%20It-%20Java.md) — Java
- [Hands-On Introduction- Java](Hands-On%20Introduction-%20Java.md) — Java
- [Learning Java 17](Learning%20Java%2017.md) — Java
- [Creating a Book Search Engine from Scratch Using Java and GitHub Copilot](../Artificial%20Intelligence%20(AI)/Creating%20a%20Book%20Search%20Engine%20from%20Scratch%20Using%20Java%20and%20GitHub%20Copilot.md) — Java
- [Advanced Java- Hands-on with Streams, Lambda Expressions, Collections, Generics and More](../Data%20Science/Advanced%20Java-%20Hands-on%20with%20Streams%2C%20Lambda%20Expressions%2C%20Collections%2C%20Generics%20and%20More.md) — Java

---

[↑ Back to top](#)